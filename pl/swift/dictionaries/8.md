---
language: swift
exerciseType: 2
---

# --description--

Podobnie jak w przypadku tablic, możemy iterować po elementach słownika za pomocą słów kluczowych `for..in`.
Aby uzyskać zarówno klucz, jak i wartość w pętli, nie musimy podawać żadnej właściwości:
```swift
for (key, value) in dictName {
    print("\(key): \(value)")
}
```

# --instructions--

Przejdź przez elementy słownika w pętli

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
for ([/], [/]) in [/] {
    print("\(key) = [/]")
}
```

# --answers--

- key
- value
- value
- user
- "user"
- \(value)
- \("value")

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
for (key, value) in user {
    print("\(key) = \(value)")
}
```

# --output--

first_name = John
last_name = Hood
age = 30
