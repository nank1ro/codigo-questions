---
language: swift
exerciseType: 2
---

# --description--

Możemy również użyć właściwości `isEmpty`, której używaliśmy w tablicach, aby sprawdzić, czy słownik jest pusty

# --instructions--

Sprawdź, czy słownik `user` jest pusty

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if ([/][/][/]) {
    print("user dictionary is empty")
} else {
    print("user dictionary is NOT empty")
}
```

# --answers--

- user
- .
- isEmpty
- isEmpty()
- is_empty

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if (user.isEmpty) {
    print("user dictionary is empty")
} else {
    print("user dictionary is NOT empty")
}
```

# --output--

user dictionary is NOT empty
