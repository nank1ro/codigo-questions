---
language: swift
exerciseType: 1
---

# --description--

Rozszerzenia działają na dowolnym typie, nawet na takich, do których nie masz kodu źródłowego. `String` pochodzi z biblioteki standardowej, ale nadal możesz nadać mu nowe metody:
```swift
extension String {
    func whisper() -> String {
        return self.lowercased() + "..."
    }
}
print("HELLO".whisper()) // hello...
```
Wewnątrz rozszerzenia możesz pominąć `self.` przy wywoływaniu innych składowych typu: samo `lowercased()` oznacza `self.lowercased()`.

# --instructions--

Napisz rozszerzenie `String` z metodą `shout()`, która zwraca ciąg znaków wielkimi literami, po których następuje wykrzyknik `!`

Przykład wywołania metody:
```swift
print("hi".shout())
// prints HI!
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Musisz użyć rozszerzenia `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`"hi".shout()` musi zwrócić `"HI!"`

```swift
tryCatch("hi".shout() == "HI!")
```

`"Swift is fun".shout()` musi zwrócić `"SWIFT IS FUN!"`

```swift
tryCatch("Swift is fun".shout() == "SWIFT IS FUN!")
```

`"".shout()` musi zwrócić `"!"`

```swift
tryCatch("".shout() == "!")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    func shout() -> String {
        return uppercased() + "!"
    }
}
```
