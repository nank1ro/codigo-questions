---
language: swift
exerciseType: 1
---

# --description--

Statyczne metody w rozszerzeniach to dobre miejsce dla małych funkcji fabrycznych budujących wartość typu. `String(repeating:count:)` to standardowy inicjalizator powtarzający fragment tekstu określoną liczbę razy:
```swift
extension String {
    static func dashes(_ count: Int) -> String {
        return String(repeating: "-", count: count)
    }
}
print(String.dashes(4)) // ----
```

# --instructions--

Napisz rozszerzenie `String` ze statyczną metodą `stars(_ count: Int)`, która zwraca ciąg znaków złożony z `count` gwiazdek `*`

Przykład wywołania metody:
```swift
print(String.stars(3))
// prints ***
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Musisz użyć rozszerzenia `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Metoda musi być `static`

```json
{
  "regex": "static\\s+func\\s+stars",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`String.stars(3)` musi zwrócić `"***"`

```swift
tryCatch(String.stars(3) == "***")
```

`String.stars(1)` musi zwrócić `"*"`

```swift
tryCatch(String.stars(1) == "*")
```

`String.stars(0)` musi zwrócić pusty ciąg znaków

```swift
tryCatch(String.stars(0) == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    static func stars(_ count: Int) -> String {
        return String(repeating: "*", count: count)
    }
}
```
