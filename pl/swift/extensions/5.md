---
language: swift
exerciseType: 1
---

# --description--

Właściwości obliczeniowe w rozszerzeniach naturalnie pasują także do `String`. Metoda `reversed()` zwraca znaki w odwrotnej kolejności, a `String(...)` zamienia je z powrotem w ciąg znaków:
```swift
extension String {
    var backwards: String {
        return String(reversed())
    }
}
print("swift".backwards) // tfiws
```
Tak jak w przypadku metod, `reversed()` wewnątrz rozszerzenia oznacza `self.reversed()`.

# --instructions--

Napisz rozszerzenie `String` z właściwością obliczeniową `isPalindrome` typu `Bool`, która jest `true`, gdy ciąg znaków czytany od przodu i od tyłu jest taki sam. Porównanie rozróżnia wielkość liter

Przykład użycia:
```swift
print("level".isPalindrome)
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Musisz użyć rozszerzenia `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`"level".isPalindrome` musi być `true`

```swift
tryCatch("level".isPalindrome == true)
```

`"swift".isPalindrome` musi być `false`

```swift
tryCatch("swift".isPalindrome == false)
```

`"Anna".isPalindrome` musi być `false` (porównanie rozróżnia wielkość liter)

```swift
tryCatch("Anna".isPalindrome == false)
```

`"".isPalindrome` musi być `true`

```swift
tryCatch("".isPalindrome == true)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    var isPalindrome: Bool {
        return self == String(reversed())
    }
}
```
