---
language: swift
exerciseType: 4
---

# --description--

`Int`, `String`, tablice i struktury to **typy wartościowe**: metoda nie może zmienić wartości, na której jest wywołana, chyba że jest oznaczona jako `mutating`. Rozszerzenia też mogą dodawać metody mutating:
```swift
extension Int {
    mutating func increment() {
        self += 1
    }
}
var count = 1
count.increment()
print(count) // 2
```
Wewnątrz metody mutating możesz przypisywać wartość do `self`. Wartość musi być przechowywana w `var`: wywołanie `increment()` na stałej `let` to błąd kompilacji.

# --instructions--

Ułóż elementy tak, aby najpierw zdefiniować rozszerzenie `Int` z metodą mutating `double()`, następnie zadeklarować zmienną `n` o wartości `4`, podwoić ją i wyświetlić

# --answers--

```swift
extension Int {
```

```swift
    mutating func double() { self *= 2 }
```

```swift
}
```

```swift
var n = 4
```

```swift
n.double()
```

```swift
print(n)
```

# --solutions--

```swift
extension Int {
    mutating func double() { self *= 2 }
}
var n = 4
n.double()
print(n)
```

# --output--

8
