---
language: swift
exerciseType: 1
---

# --description--

`Array` jest typem generycznym: `[Int]` i `[String]` to tablice z różnym typem **`Element`**. Rozszerzenie `Array` dotyczy ich wszystkich, co stanowi problem, gdy nowa składowa ma sens tylko dla niektórych elementów: nie można dodać do siebie liczb, które są ciągami znaków.
Klauzula `where` ogranicza rozszerzenie do tablic, których `Element` jest danym typem:
```swift
extension Array where Element == Int {
    var largest: Int {
        var result = Int.min
        for number in self {
            if number > result {
                result = number
            }
        }
        return result
    }
}
print([3, 9, 2].largest) // 9
```
`[3, 9, 2].largest` działa, natomiast `["a", "b"].largest` to błąd kompilacji: właściwość nie istnieje dla `[String]`.

# --instructions--

Napisz rozszerzenie `Array` ograniczone do `Element == Int` z metodą `sum()`, która zwraca sumę elementów. Nie używaj `reduce`

Przykład wywołania metody:
```swift
print([1, 2, 3].sum())
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Musisz użyć rozszerzenia `Array` z klauzulą `where Element == Int`

```json
{
  "regex": "extension\\s+Array\\s+where\\s+Element\\s*==\\s*Int",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Nie możesz użyć `reduce`

```json
{
  "regex": "reduce",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`[1, 2, 3].sum()` musi zwrócić `6`

```swift
tryCatch([1, 2, 3].sum() == 6)
```

`[10, -4].sum()` musi zwrócić `6`

```swift
tryCatch([10, -4].sum() == 6)
```

`[7].sum()` musi zwrócić `7`

```swift
tryCatch([7].sum() == 7)
```

Suma pustej tablicy musi być `0`

```swift
let _empty: [Int] = []
tryCatch(_empty.sum() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension Array where Element == Int {
    func sum() -> Int {
        var total = 0
        for number in self {
            total += number
        }
        return total
    }
}
```
