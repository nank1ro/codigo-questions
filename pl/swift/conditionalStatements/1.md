---
language: swift
exerciseType: 2
---

# --description--

Podejmowanie decyzji jest wymagane, gdy chcemy wykonać kod tylko wtedy, gdy spełniony jest określony warunek.
Załóżmy, że chcemy bawić się na zewnątrz tylko wtedy, gdy pogoda jest ładna.
W programowaniu możemy zapisać zmienną logiczną `niceWeather` i wykonać czynność zabawy na zewnątrz `if` ta zmienna jest `true`, tak jak:
```swift
var niceWeather = true
if niceWeather {
    // play outside
}
```

# --instructions--

Utwórz instrukcję `if` sprawdzającą, czy `time` jest mniejsze niż `12`, a następnie w bloku kodu wydrukuj ciąg `"It's morning"`

# --seed--

```swift
var time = 8
[/] time < 12 {
    print("It's morning")
}
```

# --answers--

- if
- ==
- =
- else
- ?

# --solutions--

```swift
var time = 8
if time < 12 {
    print("It's morning")
}
```

# --output--

It's morning
