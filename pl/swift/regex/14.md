---
language: swift
exerciseType: 4
---

# --description--

Sprawdzanie całego tekstu za pomocą `^` i `$` działa, ale `wholeMatch(of:)` mówi to wprost: zwraca dopasowanie tylko wtedy, gdy wzorzec pokrywa tekst od pierwszego do ostatniego znaku, a w przeciwnym razie `nil`:
```swift
print("1a2b".wholeMatch(of: #/[0-9a-f]+/#) != nil) // true
print("1z".wholeMatch(of: #/[0-9a-f]+/#) != nil)   // false
```
Używaj `firstMatch(of:)`, aby znaleźć coś wewnątrz tekstu, a `wholeMatch(of:)`, aby sprawdzić, czy tekst ma dokładnie jeden konkretny kształt.

# --instructions--

Uporządkuj elementy, aby zbudować program, który definiuje `isHex` przed pętlą i wypisuje wynik dla każdej wartości z `values`

# --answers--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
```

```swift
let values = ["1a2b", "1z", "ff"]
```

```swift
for value in values {
    print(isHex(value))
}
```

# --solutions--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
let values = ["1a2b", "1z", "ff"]
for value in values {
    print(isHex(value))
}
```

```swift
let values = ["1a2b", "1z", "ff"]
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
for value in values {
    print(isHex(value))
}
```

# --output--

true
false
true
