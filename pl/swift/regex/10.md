---
language: swift
exerciseType: 1
---

# --description--

Wzorzec może zawierać kilka grup. Są numerowane od lewej do prawej według ich nawiasu otwierającego, więc druga to `m.2`, trzecia `m.3` i tak dalej:
```swift
let text = "size=10"
if let m = text.firstMatch(of: #/([a-z]+)=(\d+)/#) {
    print(m.1, m.2) // size 10
}
```
`m.0` zawsze pozostaje całym dopasowaniem, niezależnie od liczby grup.

# --instructions--

Napisz funkcję `setting`, która przyjmuje tekst (`String`) bez etykiety argumentu, znajduje ustawienie zapisane jako małe litery, znak `=` i cyfry, i zwraca nazwę oraz wartość połączone dwukropkiem, albo `"none"`, gdy tekst nie zawiera takiego ustawienia

Przykład wywołania funkcji:
```swift
print(setting("size=10"))
// wypisuje size:10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func setting() {

}
```

# --asserts--

Funkcja musi używać literału wyrażenia regularnego zapisanego z `#/`

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`setting("size=10")` musi zwrócić `"size:10"`

```swift
tryCatch(setting("size=10") == "size:10")
```

`setting("use width=800 here")` musi zwrócić `"width:800"`

```swift
tryCatch(setting("use width=800 here") == "width:800")
```

`setting("a=1 b=2")` musi zwrócić `"a:1"`

```swift
tryCatch(setting("a=1 b=2") == "a:1")
```

`setting("nothing here")` musi zwrócić `"none"`

```swift
tryCatch(setting("nothing here") == "none")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func setting(_ text: String) -> String {
    if let m = text.firstMatch(of: #/([a-z]+)=(\d+)/#) {
        return "\(m.1):\(m.2)"
    }
    return "none"
}
```
