---
language: swift
exerciseType: 2
---

# --description--

Liczenie nawiasów staje się kruche, gdy tylko wzorzec urośnie. Nadaj grupie zamiast tego **nazwę**, pisząc `?<name>` zaraz po jej nawiasie otwierającym, i odczytuj ją jako właściwość dopasowania:
```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>\d+)/#) {
    print(m.key)   // size
    print(m.value) // 10
}
```
Grupy nazwane nadal mają numery, więc `m.1` dalej działa, ale `m.key` mówi, co przechowuje, i przetrwa zmianę wzorca.

# --instructions--

Uzupełnij kod tak, aby grupa value dopasowywała cyfry, a klucz został wypisany przed nią, dając na wyjściu `size 10`

# --seed--

```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>[/]+)/#) {
    print(m.[/], m.value)
}
```

# --answers--

- \d
- \s
- [A-Z]
- key
- name
- 0

# --solutions--

```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>\d+)/#) {
    print(m.key, m.value)
}
```

# --output--

size 10
