---
language: swift
exerciseType: 1
---

# --description--

Gdy żaden ze skrótów nie pasuje, wypisz akceptowane znaki w nawiasach kwadratowych. `[abc]` dopasowuje jedno `a`, jedno `b` lub jedno `c`, a myślnik zapisuje zakres:
```swift
print("f".contains(#/[a-f]/#))  // true
print("Z".contains(#/[A-Z]/#))  // true
print("5".contains(#/[0-9a-f]/#)) // true
```
Liczba w nawiasach klamrowych mówi dokładnie, ile razy powtarza się poprzedni wzorzec: `{3}` oznacza trzy razy, `{2,4}` oznacza od dwóch do czterech razy:
```swift
print("aaa".contains(#/^a{3}$/#))  // true
print("aa".contains(#/^a{3}$/#))   // false
```
Otaczanie wzorca w `^` i `$` z licznikiem to zwykły sposób sprawdzenia, czy cały tekst ma dany kształt.

# --instructions--

Napisz funkcję `isCode`, która przyjmuje tekst (`String`) bez etykiety argumentu i zwraca `true` tylko wtedy, gdy cały tekst to dokładnie trzy wielkie litery, używając wyrażenia regularnego

Przykład wywołania funkcji:
```swift
print(isCode("ABC"))
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func isCode() {

}
```

# --asserts--

Funkcja musi używać literału wyrażenia regularnego zapisanego z `#/`

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`isCode("ABC")` musi zwrócić `true`

```swift
tryCatch(isCode("ABC") == true)
```

`isCode("XYZ")` musi zwrócić `true`

```swift
tryCatch(isCode("XYZ") == true)
```

`isCode("AB")` musi zwrócić `false`

```swift
tryCatch(isCode("AB") == false)
```

`isCode("ABCD")` musi zwrócić `false`

```swift
tryCatch(isCode("ABCD") == false)
```

`isCode("abc")` musi zwrócić `false`

```swift
tryCatch(isCode("abc") == false)
```

`isCode("A1C")` musi zwrócić `false`

```swift
tryCatch(isCode("A1C") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func isCode(_ text: String) -> Bool {
    return text.contains(#/^[A-Z]{3}$/#)
}
```
