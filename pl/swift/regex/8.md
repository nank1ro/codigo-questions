---
language: swift
exerciseType: 1
---

# --description--

Ponieważ `Int(_:)` przyjmuje `Substring`, zamiana znalezionego tekstu na liczby to jeden krok. `compactMap` sprawdza się tutaj doskonale: odrzuca wartości, które wychodzą jako `nil`:
```swift
let text = "a1 b22"
let numbers = text.matches(of: #/\d+/#).compactMap { Int($0.0) }
print(numbers) // [1, 22]
```
Używaj `map`, gdy każdy element da się przekonwertować, a `compactMap`, gdy niektóre mogą się nie udać.

# --instructions--

Napisz funkcję `numbers`, która przyjmuje tekst (`String`) bez etykiety argumentu i zwraca każdy zawarty w nim ciąg cyfr jako tablicę `Int`, w kolejności, w jakiej występują

Przykład wywołania funkcji:
```swift
print(numbers("a1 b22 c333"))
// wypisuje [1, 22, 333]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func numbers() {

}
```

# --asserts--

Funkcja musi używać `matches(of:`

```json
{
  "regex": "matches\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`numbers("a1 b22 c333")` musi zwrócić `[1, 22, 333]`

```swift
tryCatch(numbers("a1 b22 c333") == [1, 22, 333])
```

`numbers("7")` musi zwrócić `[7]`

```swift
tryCatch(numbers("7") == [7])
```

`numbers("abc")` musi zwrócić `[]`

```swift
tryCatch(numbers("abc") == [])
```

`numbers("10-20")` musi zwrócić `[10, 20]`

```swift
tryCatch(numbers("10-20") == [10, 20])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func numbers(_ text: String) -> [Int] {
    return text.matches(of: #/\d+/#).compactMap { Int($0.0) }
}
```
