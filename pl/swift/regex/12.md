---
language: swift
exerciseType: 1
---

# --description--

`replacing(_:with:)` zamienia każde dopasowanie na ustalony tekst i zwraca nowy `String`, zostawiając oryginał nietknięty:
```swift
let text = "a1 b22"
print(text.replacing(#/\d+/#, with: "#")) // a# b#
print(text)                               // a1 b22
```
Zwróć uwagę, że `\d+` zamienia cały ciąg cyfr na jeden `#`, podczas gdy `\d` zamieniałoby po jednej cyfrze naraz. To wzorzec decyduje, jak dużo znika.

# --instructions--

Napisz funkcję `mask`, która przyjmuje tekst (`String`) bez etykiety argumentu i zwraca go z każdą pojedynczą cyfrą zamienioną na `#`

Przykład wywołania funkcji:
```swift
print(mask("a1 b22"))
// prints a# b##
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func mask() {

}
```

# --asserts--

Funkcja musi używać `replacing(`

```json
{
  "regex": "replacing\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`mask("a1 b22")` musi zwrócić `"a# b##"`

```swift
tryCatch(mask("a1 b22") == "a# b##")
```

`mask("2024")` musi zwrócić `"####"`

```swift
tryCatch(mask("2024") == "####")
```

`mask("abc")` musi zwrócić `"abc"`

```swift
tryCatch(mask("abc") == "abc")
```

`mask("")` musi zwrócić `""`

```swift
tryCatch(mask("") == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func mask(_ text: String) -> String {
    return text.replacing(#/\d/#, with: "#")
}
```
