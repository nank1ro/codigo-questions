---
language: swift
exerciseType: 1
---

# --description--

`m.0` nie jest `String`, lecz `Substring`: widokiem oryginalnego tekstu, a nie kopią. Wypisuje się dokładnie jak ciąg znaków, ale tam, gdzie wymagany jest `String`, musisz go przekonwertować:
```swift
let text = "order 42"
if let m = text.firstMatch(of: #/\d+/#) {
    let found: String = String(m.0)
    print(found) // 42
}
```
Inicjalizatory liczb przyjmują `Substring` bezpośrednio, więc `Int(m.0)` działa bez tej dodatkowej czynności.

# --instructions--

Napisz funkcję `firstNumber`, która przyjmuje tekst (`String`) bez etykiety argumentu i zwraca pierwszy ciąg cyfr jako `String`, albo `"none"`, gdy tekst nie zawiera żadnej cyfry

Przykład wywołania funkcji:
```swift
print(firstNumber("order 42 today 7"))
// wypisuje 42
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func firstNumber() {

}
```

# --asserts--

Funkcja musi używać `firstMatch(of:`

```json
{
  "regex": "firstMatch\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstNumber("order 42 today 7")` musi zwrócić `"42"`

```swift
tryCatch(firstNumber("order 42 today 7") == "42")
```

`firstNumber("a1")` musi zwrócić `"1"`

```swift
tryCatch(firstNumber("a1") == "1")
```

`firstNumber("100")` musi zwrócić `"100"`

```swift
tryCatch(firstNumber("100") == "100")
```

`firstNumber("abc")` musi zwrócić `"none"`

```swift
tryCatch(firstNumber("abc") == "none")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstNumber(_ text: String) -> String {
    if let m = text.firstMatch(of: #/\d+/#) {
        return String(m.0)
    }
    return "none"
}
```
