---
language: swift
exerciseType: 4
---

# --description--

`matches(of:)` zwraca **wszystkie** dopasowania zamiast pierwszego, w postaci tablicy. Ta tablica nigdy nie jest `nil`: gdy nic nie pasuje, jest po prostu pusta, więc można od razu po niej iterować lub ją przekształcać:
```swift
let text = "a1 b22"
print(text.matches(of: #/\d+/#).count) // 2
```
Każdy element to dopasowanie, więc `$0.0` wewnątrz `map` to dopasowany tekst:
```swift
let found = text.matches(of: #/\d+/#).map { String($0.0) }
print(found) // ["1", "22"]
```

# --instructions--

Uporządkuj elementy, aby zbudować program, który zbiera każdy ciąg cyfr z `text` do `found` i wypisuje go

# --answers--

```swift
let text = "a1 b22 c333"
```

```swift
let pattern = #/\d+/#
```

```swift
let found = text.matches(of: pattern).map { String($0.0) }
```

```swift
print(found)
```

# --solutions--

```swift
let text = "a1 b22 c333"
let pattern = #/\d+/#
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

```swift
let pattern = #/\d+/#
let text = "a1 b22 c333"
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

# --output--

["1", "22", "333"]
