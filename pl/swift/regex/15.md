---
language: swift
exerciseType: 1
---

# --description--

Literał `#/ ... /#` jest ustalony w chwili kompilacji. Gdy wzorzec poznajesz dopiero w trakcie działania programu, na przykład dlatego, że wpisał go użytkownik, zbuduj go za pomocą `Regex(_:)`:
```swift
let regex = try Regex("[0-9]+")
print("abc123".contains(regex)) // true
```
Ten inicjalizator **rzuca wyjątek**: nieprawidłowy wzorzec taki jak `"["` zostaje wykryty dopiero podczas działania programu, więc wywołanie wymaga `try`, a błąd trzeba albo obsłużyć przez `do`/`catch` (lub `try?`), albo przekazać dalej, oznaczając otaczającą funkcję jako `throws`, tak jak robi to to ćwiczenie. Wyrażenie regularne zbudowane w ten sposób nie ma numerowanych właściwości znanych w czasie kompilacji, ale `contains`, `matches(of:)` i `replacing` działają dokładnie tak samo jak wcześniej.

# --instructions--

Napisz funkcję `countMatches`, która przyjmuje tekst (`String`) i wzorzec (`String`), oba bez etykiet argumentów, jest oznaczona jako `throws` i zwraca liczbę wystąpień wzorca w tekście. Nieprawidłowy wzorzec musi wypuścić błąd na zewnątrz

Przykład wywołania funkcji:
```swift
print(try countMatches("a1 b22", "[0-9]+"))
// prints 2
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func countMatches() {

}
```

# --asserts--

Funkcja musi budować wyrażenie regularne za pomocą `try Regex(`

```json
{
  "regex": "try\\s+Regex\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`countMatches("a1 b22", "[0-9]+")` musi zwrócić `2`

```swift
tryCatch((try? countMatches("a1 b22", "[0-9]+")) == 2)
```

`countMatches("abc", "[0-9]+")` musi zwrócić `0`

```swift
tryCatch((try? countMatches("abc", "[0-9]+")) == 0)
```

`countMatches("hello world", "o")` musi zwrócić `2`

```swift
tryCatch((try? countMatches("hello world", "o")) == 2)
```

`countMatches("abc", "[")` musi rzucić wyjątek

```swift
tryCatch((try? countMatches("abc", "[")) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func countMatches(_ text: String, _ pattern: String) throws -> Int {
    let regex = try Regex(pattern)
    return text.matches(of: regex).count
}
```
