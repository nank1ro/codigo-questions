---
language: swift
exerciseType: 1
---

# --description--

Praca z indeksami jest rozwlekła, więc Swift oferuje skróty dla najczęstszych przypadków:
- `first` i `last` zwracają pierwszy i ostatni znak jako opcjonalny `Character?` (`nil` dla pustego ciągu)
- `prefix(n)` zwraca pierwsze `n` znaków, a `suffix(n)` ostatnie `n`
```swift
let word = "Swift"
print(word.first!)     // S
print(word.prefix(2))  // Sw
print(word.suffix(3))  // ift
```
`prefix` i `suffix` zwracają `Substring`, widok oryginalnego tekstu. Aby zapisać go jako prawdziwy `String`, otocz go w `String(...)`. Jeśli `n` jest większe niż `count`, po prostu otrzymujesz cały ciąg znaków.

# --instructions--

Napisz funkcję `firstTwo`, która przyjmuje `String` (pomiń etykietę argumentu) i zwraca jej pierwsze dwa znaki jako `String`, oraz funkcję `lastTwo`, która zwraca jej ostatnie dwa znaki jako `String`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`firstTwo("Swift")` musi zwrócić `"Sw"`

```swift
tryCatch(firstTwo("Swift") == "Sw")
```

`firstTwo("a")` musi zwrócić `"a"`

```swift
tryCatch(firstTwo("a") == "a")
```

`lastTwo("Swift")` musi zwrócić `"ft"`

```swift
tryCatch(lastTwo("Swift") == "ft")
```

`lastTwo("")` musi zwrócić `""`

```swift
tryCatch(lastTwo("") == "")
```

`firstTwo` musi zwrócić `String`

```swift
tryCatch(type(of: firstTwo("Swift")) == String.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstTwo(_ text: String) -> String {
    return String(text.prefix(2))
}

func lastTwo(_ text: String) -> String {
    return String(text.suffix(2))
}
```
