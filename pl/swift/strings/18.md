---
language: swift
exerciseType: 4
---

# --description--

`Character` nie jest `String`, więc nie można go bezpośrednio połączyć z ciągiem znaków operatorem `+`. Najpierw przekonwertuj go za pomocą `String(...)`:
```swift
let letter: Character = "a"
let text = String(letter) + "bc" // abc
```
Połączenie tego z pętlą `for`-`in` pozwala odbudować ciąg znaków znak po znaku, na przykład umieszczając każdy nowy znak przed tymi już zebranymi.

# --instructions--

Ułóż elementy, aby wydrukować `swift` odwrócone, budując wynik znak po znaku.
Zadeklaruj `word` przed `reversed`

# --answers--

```swift
let word = "swift"
```

```swift
var reversed = ""
```

```swift
for letter in word {
```

```swift
    reversed = String(letter) + reversed
```

```swift
}
```

```swift
print(reversed)
```

# --solutions--

```swift
let word = "swift"
var reversed = ""
for letter in word {
    reversed = String(letter) + reversed
}
print(reversed)
```

# --output--

tfiws
