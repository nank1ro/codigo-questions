---
language: swift
exerciseType: 1
---

# --description--

`String` to kolekcja wartości `Character`. `Character` to pojedyncza litera, cyfra, symbol lub spacja i zapisuje się go w tych samych cudzysłowach co ciąg znaków, więc aby go uzyskać, potrzebujesz adnotacji typu:
```swift
let letter: Character = "a"
let text = "abc"
print(text.count) // 3
```
Sprawdzanie `isEmpty` jest lepsze niż porównywanie `count` z `0`: czyta się lepiej i nie trzeba liczyć każdego znaku.

# --instructions--

Napisz funkcję `length`, która przyjmuje `String` (pomiń etykietę argumentu) i zwraca liczbę jej znaków, oraz funkcję `hasText`, która przyjmuje `String` (pomiń etykietę argumentu) i zwraca `true` tylko wtedy, gdy ciąg nie jest pusty

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`length("Swift")` musi zwrócić `5`

```swift
tryCatch(length("Swift") == 5)
```

`length("a b c")` musi zwrócić `5`

```swift
tryCatch(length("a b c") == 5)
```

`length("")` musi zwrócić `0`

```swift
tryCatch(length("") == 0)
```

`hasText("x")` musi zwrócić `true`

```swift
tryCatch(hasText("x") == true)
```

`hasText("")` musi zwrócić `false`

```swift
tryCatch(hasText("") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func length(_ text: String) -> Int {
    return text.count
}

func hasText(_ text: String) -> Bool {
    return !text.isEmpty
}
```
