---
language: swift
exerciseType: 1
---

# --description--

Framework Foundation dodaje wiele dodatkowych metod dla ciągów znaków. Jedną z najbardziej przydatnych jest `replacingOccurrences(of:with:)`, która zwraca nowy ciąg znaków, w którym każde wystąpienie pierwszego tekstu jest zastępowane drugim:
```swift
import Foundation

let path = "a/b/c"
print(path.replacingOccurrences(of: "/", with: "-")) // a-b-c
```
Pamiętaj, aby na początku pliku umieścić `import Foundation`, inaczej metoda nie będzie dostępna. Wywołania metod można łączyć w łańcuch, więc `text.lowercased().replacingOccurrences(of: " ", with: "_")` jest poprawne.

# --instructions--

Napisz funkcję `snakeCase`, która przyjmuje `String` (pomiń etykietę argumentu) i zwraca ją małymi literami, z każdą spacją zastąpioną podkreśleniem `_`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`snakeCase("Hello World")` musi zwrócić `"hello_world"`

```swift
tryCatch(snakeCase("Hello World") == "hello_world")
```

`snakeCase("My First Post")` musi zwrócić `"my_first_post"`

```swift
tryCatch(snakeCase("My First Post") == "my_first_post")
```

`snakeCase("swift")` musi zwrócić `"swift"`

```swift
tryCatch(snakeCase("swift") == "swift")
```

`snakeCase("")` musi zwrócić `""`

```swift
tryCatch(snakeCase("") == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func snakeCase(_ text: String) -> String {
    return text.lowercased().replacingOccurrences(of: " ", with: "_")
}
```
