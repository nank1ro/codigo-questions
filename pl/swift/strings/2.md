---
language: swift
exerciseType: 1
---

# --description--

**Interpolacja ciągów znaków** wstawia wartość wyrażenia wewnątrz literału ciągu znaków. Otocz wyrażenie w `\()`:
```swift
let name = "Ada"
let age = 36
print("\(name) is \(age) years old") // Ada is 36 years old
```
Można interpolować dowolny typ: liczby, wartości logiczne i inne ciągi znaków są automatycznie konwertowane na tekst.

# --instructions--

Napisz funkcję `greet`, która przyjmuje `String` (pomiń etykietę argumentu) i zwraca tekst `Hello, <name>!`, gdzie `<name>` to argument.

Przykład wywołania funkcji:
```swift
print(greet("Ada"))
// prints Hello, Ada!
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`greet("Ada")` musi zwrócić `"Hello, Ada!"`

```swift
tryCatch(greet("Ada") == "Hello, Ada!")
```

`greet("Swift")` musi zwrócić `"Hello, Swift!"`

```swift
tryCatch(greet("Swift") == "Hello, Swift!")
```

`greet("")` musi zwrócić `"Hello, !"`

```swift
tryCatch(greet("") == "Hello, !")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greet(_ name: String) -> String {
    return "Hello, \(name)!"
}
```
