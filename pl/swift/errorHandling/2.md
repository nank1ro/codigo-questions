---
language: swift
exerciseType: 1
---

# --description--

Funkcja zgłaszająca błąd może nadal zwracać wartość. Słowo kluczowe `throws` znajduje się między listą parametrów a strzałką zwracanego typu:
```swift
func square(_ n: Int) throws -> Int {
    if n < 0 {
        throw SquareError.negative
    }
    return n * n
}
```
Czytając to na głos: *square przyjmuje `Int`, może zgłosić błąd i zwraca `Int`*.

W miejscu wywołania wartość istnieje tylko wtedy, gdy nie został zgłoszony żaden błąd, więc przypisanie znajduje się wewnątrz bloku `do`:
```swift
do {
    let result = try square(4)
    print(result) // 16
} catch {
    print("failed")
}
```
`try` nie jest opcjonalną ozdobą: kompilator odrzuca wywołanie bez niego, więc czytający zawsze widzi, które linie mogą zawieść.

# --instructions--

Oznacz `half` jako zgłaszającą błąd, spraw, aby zgłaszała `MathError.notEven`, gdy `n` jest nieparzyste, a w przeciwnym razie zwracała `n / 2`

Przykład wywołania funkcji:
```swift
do {
    print(try half(10))
} catch {
    print("odd")
}
// wypisuje 5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum MathError: Error {
    case notEven
}

func half(_ n: Int) -> Int {
    
}
```

# --before-asserts--

```swift
func _half(_ n: Int) -> Int {
    do {
        return try half(n)
    } catch {
        return -999
    }
}
```

# --asserts--

`half(10)` musi zwrócić `5`

```swift
tryCatch(_half(10) == 5)
```

`half(0)` musi zwrócić `0`

```swift
tryCatch(_half(0) == 0)
```

`half(7)` musi zgłosić błąd

```swift
tryCatch(_half(7) == -999)
```

`half(-4)` musi zwrócić `-2`

```swift
tryCatch(_half(-4) == -2)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum MathError: Error {
    case notEven
}

func half(_ n: Int) throws -> Int {
    if n % 2 != 0 {
        throw MathError.notEven
    }
    return n / 2
}
```
