---
language: swift
exerciseType: 4
---

# --description--

To, gdzie znajduje się blok `do`, decyduje o tym, ile kosztuje pojedyncza awaria. Umieść go **wewnątrz** pętli, a każdy element dostanie własną próbę, dzięki czemu jedna zła wartość zostanie pominięta, a reszta i tak się wykona:
```swift
for age in [4, -1, 7] {
    do {
        print(try label(age))
    } catch {
        print("skipped")
    }
}
```
Owinięcie całej pętli w jeden `do` zatrzymałoby się natomiast na pierwszym błędzie i nigdy nie dotarłoby do `7`. Żadne z tych podejść nie jest błędne — to różnica między *jednym złym elementem* a *poddaniem się*.

# --instructions--

Ułóż elementy tak, aby wypisać poniższe linie, deklarując najpierw typ błędu, a funkcję przed pętlą
```
age 4
skipped
age 7
```

# --answers--

```swift
enum AgeError: Error {
    case negative
}
```

```swift
func label(_ age: Int) throws -> String {
    if age < 0 {
        throw AgeError.negative
    }
    return "age \(age)"
}
```

```swift
for age in [4, -1, 7] {
    do {
```

```swift
        print(try label(age))
```

```swift
    } catch {
        print("skipped")
    }
}
```

# --solutions--

```swift
enum AgeError: Error {
    case negative
}
func label(_ age: Int) throws -> String {
    if age < 0 {
        throw AgeError.negative
    }
    return "age \(age)"
}
for age in [4, -1, 7] {
    do {
        print(try label(age))
    } catch {
        print("skipped")
    }
}
```

# --output--

age 4
skipped
age 7
