---
language: swift
exerciseType: 1
---

# --instructions--

Napisz walidator, wykorzystując wszystko z tego rozdziału:
- nadaj `FormError` drugi przypadek `tooLong` niosący `maximum` typu `Int`
- `validate` zgłasza `FormError.empty`, gdy imię jest puste, zgłasza `FormError.tooLong(maximum: 10)`, gdy ma więcej niż 10 znaków, a w przeciwnym razie zwraca imię
- `safeName` zwraca zwalidowane imię albo `"guest"`, gdy `validate` zgłosi błąd

Przykład wywołania funkcji:
```swift
print(safeName(""))
// wypisuje guest
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum FormError: Error {
    case empty
}

func validate(_ name: String) throws -> String {
    
}

func safeName(_ name: String) -> String {
    
}
```

# --before-asserts--

```swift
func _kind(_ name: String) -> String {
    do {
        return try validate(name)
    } catch FormError.empty {
        return "empty"
    } catch FormError.tooLong(let maximum) {
        return "max \(maximum)"
    } catch {
        return "other"
    }
}
```

# --asserts--

`validate("Ada")` musi zwrócić `Ada`

```swift
tryCatch(_kind("Ada") == "Ada")
```

`validate("")` musi zgłosić `empty`

```swift
tryCatch(_kind("") == "empty")
```

`validate("12345678901")` musi zgłosić `tooLong` z wartością `10`

```swift
tryCatch(_kind("12345678901") == "max 10")
```

imię o dokładnie 10 znakach musi zostać zaakceptowane

```swift
tryCatch(_kind("1234567890") == "1234567890")
```

`safeName("")` musi zwrócić `guest`

```swift
tryCatch(safeName("") == "guest")
```

`safeName("Ada")` musi zwrócić `Ada`

```swift
tryCatch(safeName("Ada") == "Ada")
```

`safeName("12345678901")` musi zwrócić `guest`

```swift
tryCatch(safeName("12345678901") == "guest")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum FormError: Error {
    case empty
    case tooLong(maximum: Int)
}

func validate(_ name: String) throws -> String {
    guard !name.isEmpty else {
        throw FormError.empty
    }
    guard name.count <= 10 else {
        throw FormError.tooLong(maximum: 10)
    }
    return name
}

func safeName(_ name: String) -> String {
    return (try? validate(name)) ?? "guest"
}
```
