---
language: swift
exerciseType: 1
---

# --description--

Ponieważ `try?` daje w wyniku opcjonał, operator `??` dopełnia reszty, podając wartość zapasową:
```swift
let port = (try? readPort(text)) ?? 8080
```
Nawiasy mają znaczenie. Bez nich `try?` próbowałby objąć całe wyrażenie razem z `??`, a kompilator prosi o wyraźne zaznaczenie, gdzie kończy się zawodzące wywołanie.

Przeczytaj linię jako jedno zdanie: *użyj portu, który udało się odczytać, w przeciwnym razie 8080*. Dwie linie `do`/`catch` zwijają się w jedną, gdy ratunek to naprawdę po prostu wartość domyślna.

# --instructions--

Uzupełnij `portOrDefault` tak, aby zwracał port odczytany z `text`, albo `8080`, gdy `readPort` zgłosi błąd

Przykład wywołania funkcji:
```swift
print(portOrDefault("abc"))
// wypisuje 8080
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum ConfigError: Error {
    case notANumber
}

func readPort(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ConfigError.notANumber
    }
    return value
}

func portOrDefault(_ text: String) -> Int {
    
}
```

# --asserts--

`portOrDefault("3000")` musi zwrócić `3000`

```swift
tryCatch(portOrDefault("3000") == 3000)
```

`portOrDefault("80")` musi zwrócić `80`

```swift
tryCatch(portOrDefault("80") == 80)
```

`portOrDefault("abc")` musi użyć wartości zapasowej `8080`

```swift
tryCatch(portOrDefault("abc") == 8080)
```

`portOrDefault("")` musi użyć wartości zapasowej `8080`

```swift
tryCatch(portOrDefault("") == 8080)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum ConfigError: Error {
    case notANumber
}

func readPort(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ConfigError.notANumber
    }
    return value
}

func portOrDefault(_ text: String) -> Int {
    return (try? readPort(text)) ?? 8080
}
```
