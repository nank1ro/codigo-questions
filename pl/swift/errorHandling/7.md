---
language: swift
exerciseType: 1
---

# --description--

Walidator czyta się najlepiej, gdy odrzucenia przychodzą najpierw, a właściwa praca pozostaje niewcięta na dole. `guard` powstał właśnie do tego: określa warunek, który musi być spełniony, a jego blok `else` wykonuje się, gdy nie jest.
```swift
func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
Blok `else` w `guard` musi opuścić bieżący zasięg, a `throw` to jeden ze sposobów, aby to zrobić, obok `return`, `break` i `continue`. Kilka `guard`ów ułożonych na początku funkcji czyta się jak listę reguł, którym dane wejściowe muszą sprostać.

# --instructions--

Uzupełnij `priceFor` o `guard`, który zgłasza `OrderError.notPositive`, chyba że `quantity` jest większe od `0`

Przykład wywołania funkcji:
```swift
do {
    print(try priceFor(4))
} catch {
    print("invalid")
}
// wypisuje 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    
    return quantity * 3
}
```

# --before-asserts--

```swift
func _price(_ quantity: Int) -> Int {
    do {
        return try priceFor(quantity)
    } catch OrderError.notPositive {
        return -1
    } catch {
        return -2
    }
}
```

# --asserts--

`priceFor(4)` musi zwrócić `12`

```swift
tryCatch(_price(4) == 12)
```

`priceFor(1)` musi zwrócić `3`

```swift
tryCatch(_price(1) == 3)
```

`priceFor(0)` musi zgłosić `notPositive`

```swift
tryCatch(_price(0) == -1)
```

`priceFor(-5)` musi zgłosić `notPositive`

```swift
tryCatch(_price(-5) == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
