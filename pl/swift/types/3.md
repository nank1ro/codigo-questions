---
language: swift
exerciseType: 1
---

# --description--

Funkcja `type(of:)` zwraca typ wartości, co przydaje się do sprawdzenia, co Swift wnioskował:
```swift
print(type(of: 42))    // Int
print(type(of: 2.5))   // Double
print(type(of: "hi"))  // String
```
Gdy chcesz typ inny niż wnioskowany, dodaj adnotację. Literal liczby całkowitej może być przechowany w stałej `Double` lub `Float`, a literal jednego znaku w stałej `Character`:
```swift
let ratio: Double = 3       // 3.0, not an Int
let half: Float = 0.5
let initial: Character = "S"
print(type(of: ratio))      // Double
```

# --instructions--

Zadeklaruj trzy stałe:
- `ratio`, typu `Double`, przechowującą literal liczby całkowitej `3`
- `half`, typu `Float`, przechowującą `0.5`
- `initial`, typu `Character`, przechowującą `"S"`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`ratio` musi być typu `Double` i być równe `3.0`

```swift
tryCatch(type(of: ratio) == Double.self && ratio == 3.0)
```

`half` musi być typu `Float` i być równe `0.5`

```swift
tryCatch(type(of: half) == Float.self && half == 0.5)
```

`initial` musi być typu `Character` i być równe `"S"`

```swift
tryCatch(type(of: initial) == Character.self && initial == "S")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let ratio: Double = 3
let half: Float = 0.5
let initial: Character = "S"
```
