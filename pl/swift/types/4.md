---
language: swift
exerciseType: 2
---

# --description--

Swift nigdy nie konwertuje typów liczbowych sam z siebie: dodanie `Int` do `Double` to błąd kompilacji, mimo że oba są liczbami.
```swift
let apples = 3
let price = 1.5
let total = apples * price // error: Int and Double can't be mixed
```
Aby je połączyć, tworzysz nową wartość potrzebnego typu, przekazując wartość do inicjalizatora tego typu:
```swift
let total = Double(apples) * price // 4.5
```
To samo działa w drugą stronę: `Int(4.5)` daje `Int`, zachowując tylko część całkowitą liczby.

# --instructions--

Uzupełnij konwersje tak, aby `total` było kosztem jabłek jako `Double`, a `whole` jego częścią całkowitą jako `Int`, tak aby program wyświetlił `4.5` i `4`

# --seed--

```swift
let apples = 3
let price = 1.5
let total = [/](apples) * price
let whole = [/](total)
print(total)
print(whole)
```

# --answers--

- Double
- Int
- Float
- String
- Character

# --solutions--

```swift
let apples = 3
let price = 1.5
let total = Double(apples) * price
let whole = Int(total)
print(total)
print(whole)
```

# --output--

4.5
4
