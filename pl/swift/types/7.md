---
language: swift
exerciseType: 1
---

# --description--

Gdy funkcja musi zwrócić wynik dziesiętny obliczony z liczb całkowitych, skonwertuj operandy do `Double` przed dzieleniem i zadeklaruj typ zwracany jako `Double`:
```swift
func ratio(_ part: Int, _ total: Int) -> Double {
    return Double(part) / Double(total)
}
print(ratio(1, 4)) // 0.25
```
Pamiętaj, że `count` tablicy też jest `Int`, więc wymaga tej samej konwersji.

# --instructions--

Napisz funkcję `average`, która przyjmuje tablicę `Int` (pomiń etykietę argumentu) i zwraca średnią jej elementów jako `Double`. Tablica nigdy nie jest pusta

Przykład wywołania funkcji:
```swift
print(average([1, 2]))
// prints 1.5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`average([1, 2])` musi zwrócić `1.5`

```swift
tryCatch(average([1, 2]) == 1.5)
```

`average([3, 4, 5, 6])` musi zwrócić `4.5`

```swift
tryCatch(average([3, 4, 5, 6]) == 4.5)
```

`average([10])` musi zwrócić `10.0`

```swift
tryCatch(average([10]) == 10.0)
```

`average([1, 1, 2])` musi zwrócić `Double`

```swift
tryCatch(type(of: average([1, 1, 2])) == Double.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func average(_ numbers: [Int]) -> Double {
    var sum = 0
    for number in numbers {
        sum += number
    }
    return Double(sum) / Double(numbers.count)
}
```
