---
language: swift
exerciseType: 1
---

# --description--

Ponieważ `Int(text)` i `Double(text)` zwracają `nil` w razie niepowodzenia, porównanie wyniku z `nil` mówi ci, czy tekst jest liczbą danego rodzaju:
```swift
print(Int("42") != nil)     // true
print(Double("4.2") != nil) // true
print(Double("42") != nil)  // true, a whole number is also a valid Double
```
Zwróć uwagę na ostatnią linię: każdy tekst zaakceptowany przez `Int` jest też akceptowany przez `Double`, więc sprawdź najpierw `Int`, gdy chcesz je odróżnić.

# --instructions--

Napisz funkcję `classify`, która przyjmuje `String` (pomiń etykietę argumentu) i zwraca `"Int"`, jeśli tekst jest poprawną liczbą całkowitą, `"Double"`, jeśli jest poprawną liczbą dziesiętną, a `"String"` w przeciwnym razie

Przykład wywołania funkcji:
```swift
print(classify("3.5"))
// prints Double
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`classify("42")` musi zwrócić `"Int"`

```swift
tryCatch(classify("42") == "Int")
```

`classify("-7")` musi zwrócić `"Int"`

```swift
tryCatch(classify("-7") == "Int")
```

`classify("3.5")` musi zwrócić `"Double"`

```swift
tryCatch(classify("3.5") == "Double")
```

`classify("hello")` musi zwrócić `"String"`

```swift
tryCatch(classify("hello") == "String")
```

`classify("")` musi zwrócić `"String"`

```swift
tryCatch(classify("") == "String")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func classify(_ text: String) -> String {
    if Int(text) != nil {
        return "Int"
    }
    if Double(text) != nil {
        return "Double"
    }
    return "String"
}
```
