---
language: swift
exerciseType: 2
---

# --description--

Jak widać w lekcjach o ciągach znaków, iterowanie po `String` daje po jednym `Character` na raz. `Character` nie jest `String`, więc aby użyć go jako tekstu, konwertujesz go za pomocą `String(c)`.
Gdy znak jest cyfrą, właściwość `wholeNumberValue` daje jego wartość liczbową jako `Int?`: jest `nil` dla znaków, które nie są cyframi.
```swift
for c in "a1" {
    print(c.wholeNumberValue)
}
// nil
// Optional(1)
```

# --instructions--

Uzupełnij pętlę tak, aby sumowała cyfry ciągu znaków, tak aby program wyświetlił `8`

# --seed--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.[/] {
        sum [/] digit
    }
}
print(sum)
```

# --answers--

- wholeNumberValue
- +=
- asciiValue
- isNumber
- -=
- =

# --solutions--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.wholeNumberValue {
        sum += digit
    }
}
print(sum)
```

# --output--

8
