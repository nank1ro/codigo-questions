---
language: swift
exerciseType: 1
---

# --description--

`Int(x)` nie zaokrągla: **obcina**, po prostu odrzucając część dziesiętną, więc `Int(3.99)` to `3`, a `Int(-3.99)` to `-3`.
Aby zaokrąglić do najbliższej liczby całkowitej, najpierw wywołaj `rounded()` na `Double`, a potem skonwertuj:
```swift
let x = 3.99
print(Int(x))            // 3
print(Int(x.rounded()))  // 4
```
Wartości połówkowe, jak `2.5`, są zaokrąglane w stronę oddalania się od zera: `2.5` staje się `3.0`, a `-2.5` staje się `-3.0`.

# --instructions--

Napisz funkcję `roundedAverage`, która przyjmuje dwa `Int` (pomiń obie etykiety argumentów) i zwraca ich średnią zaokrągloną do najbliższej liczby całkowitej, jako `Int`

Przykład wywołania funkcji:
```swift
print(roundedAverage(3, 4))
// prints 4
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`roundedAverage(3, 4)` musi zwrócić `4` (średnia to `3.5`)

```swift
tryCatch(roundedAverage(3, 4) == 4)
```

`roundedAverage(2, 4)` musi zwrócić `3`

```swift
tryCatch(roundedAverage(2, 4) == 3)
```

`roundedAverage(10, 15)` musi zwrócić `13` (średnia to `12.5`)

```swift
tryCatch(roundedAverage(10, 15) == 13)
```

`roundedAverage(-3, -4)` musi zwrócić `-4`

```swift
tryCatch(roundedAverage(-3, -4) == -4)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func roundedAverage(_ a: Int, _ b: Int) -> Int {
    let average = Double(a + b) / 2
    return Int(average.rounded())
}
```
