---
language: swift
exerciseType: 1
---

# --description--

Rzutowania warunkowe łączą się naturalnie z `else if`, aby obsłużyć kilka możliwych typów, konwertując każdy z nich na typ potrzebny do wyniku:
```swift
let item: Any = 2.5
if let number = item as? Int {
    print(Double(number))
} else if let number = item as? Double {
    print(number)
}
```
`Int` przechowany w `Any` nadal jest `Int`: `as? Double` na nim zwraca `nil`, ponieważ `as?` sprawdza typ, a nie konwertuje liczb.

# --instructions--

Napisz funkcję `sumNumbers`, która przyjmuje tablicę `Any` (pomiń etykietę argumentu) i zwraca sumę swoich elementów `Int` i `Double` jako `Double`, ignorując elementy innego typu

Przykład wywołania funkcji:
```swift
print(sumNumbers([1, 2.5, "x"]))
// prints 3.5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`sumNumbers([1, 2.5, "x"])` musi zwrócić `3.5`

```swift
tryCatch(sumNumbers([1, 2.5, "x"]) == 3.5)
```

`sumNumbers(["3", 4])` musi zwrócić `4.0`

```swift
tryCatch(sumNumbers(["3", 4]) == 4.0)
```

`sumNumbers([0.5, 0.25])` musi zwrócić `0.75`

```swift
tryCatch(sumNumbers([0.5, 0.25]) == 0.75)
```

`sumNumbers(["a", "b"])` musi zwrócić `0.0`

```swift
tryCatch(sumNumbers(["a", "b"]) == 0.0)
```

`sumNumbers([10])` musi zwrócić `Double`

```swift
tryCatch(type(of: sumNumbers([10])) == Double.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func sumNumbers(_ values: [Any]) -> Double {
    var total = 0.0
    for value in values {
        if let number = value as? Int {
            total += Double(number)
        } else if let number = value as? Double {
            total += number
        }
    }
    return total
}
```
