---
language: swift
exerciseType: 4
---

# --description--

Rozszerzenie protokołu może też dodawać składowe, których protokół nigdy nie wymienił jako wymagań. Są to dodatkowe udogodnienia, dostępne w każdym zgodnym typie:
```swift
protocol Sized {
    var count: Int { get }
}

extension Sized {
    var isEmpty: Bool {
        return count == 0
    }
}
```
`isEmpty` nie jest wymaganiem, więc zgodny typ nie musi go zapewniać — po prostu je dostaje.

# --instructions--

Ułóż elementy tak, aby wypisać `HEY`, deklarując najpierw protokół, potem jego rozszerzenie, następnie strukturę, a na końcu `print`

# --answers--

```swift
protocol Loud {
    var text: String { get }
}
```

```swift
extension Loud {
    func shout() -> String { return text.uppercased() }
}
```

```swift
struct Message: Loud {
    var text: String
}
```

```swift
print(Message(text: "hey").shout())
```

# --solutions--

```swift
protocol Loud {
    var text: String { get }
}
extension Loud {
    func shout() -> String { return text.uppercased() }
}
struct Message: Loud {
    var text: String
}
print(Message(text: "hey").shout())
```

# --output--

HEY
