---
language: swift
exerciseType: 4
---

# --description--

Protokół zwykle zbiera więcej niż jedno wymaganie, a zgodny typ musi spełnić każde z nich:
```swift
protocol Vehicle {
    var wheels: Int { get }
    func move() -> String
}

struct Bike: Vehicle {
    var wheels = 2

    func move() -> String {
        return "pedalling"
    }
}
```
Kolejność wymagań wewnątrz klamer nie ma znaczenia, podobnie jak kolejność, w jakiej zgodny typ je zapewnia: kompilator sprawdza tylko, czy niczego nie brakuje.

# --instructions--

Ułóż elementy tak, aby wypisać `Moo`, deklarując najpierw protokół, potem strukturę, a instancję przed `print`

# --answers--

```swift
protocol Animal {
    func sound() -> String
}
```

```swift
struct Cow: Animal {
    func sound() -> String { return "Moo" }
}
```

```swift
let cow = Cow()
```

```swift
print(cow.sound())
```

# --solutions--

```swift
protocol Animal {
    func sound() -> String
}
struct Cow: Animal {
    func sound() -> String { return "Moo" }
}
let cow = Cow()
print(cow.sound())
```

# --output--

Moo
