---
language: swift
exerciseType: 1
---

# --description--

`Task.sleep` wstrzymuje bieżące zadanie na chwilę, nie blokując niczego innego. Może zostać przerwane, więc jest to rzucające wywołanie asynchroniczne i wymaga `try await`. Czas trwania podaje się za pomocą metod pomocniczych, takich jak `.seconds`, `.milliseconds` czy `.nanoseconds`:
```swift
func slowGreeting() async throws -> String {
    try await Task.sleep(for: .milliseconds(50))
    return "hello"
}
```
To standardowy sposób symulowania powolnej pracy w przykładzie zamiast prawdziwego wywołania sieciowego. Zauważ, że nie zamraża programu: podczas gdy jedno zadanie śpi, pozostałe działają dalej.

# --instructions--

Napisz funkcję `delayed`, która przyjmuje liczbę całkowitą (pomiń etykietę argumentu), jest asynchroniczna, może zgłaszać błąd, czeka `50` milisekund za pomocą `Task.sleep`, a następnie zwraca liczbę pomnożoną przez `3`

Przykład wywołania funkcji:
```swift
print(try await delayed(2))
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func delayed() {

}
```

# --asserts--

Funkcja musi być oznaczona jako `async throws`

```json
{
  "regex": "func\\s+delayed\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Funkcja musi użyć `Task.sleep`

```json
{
  "regex": "Task\\.sleep",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`delayed(2)` musi zwrócić `6`

```swift
tryCatch((try? await delayed(2)) == 6)
```

`delayed(0)` musi zwrócić `0`

```swift
tryCatch((try? await delayed(0)) == 0)
```

`delayed(-4)` musi zwrócić `-12`

```swift
tryCatch((try? await delayed(-4)) == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func delayed(_ n: Int) async throws -> Int {
    try await Task.sleep(for: .milliseconds(50))
    return n * 3
}
```
