---
language: swift
exerciseType: 1
---

# --description--

`async let` działa, gdy podczas pisania kodu wiesz, ile jest wywołań. Dla listy, której rozmiar jest znany dopiero w czasie działania, użyj **grupy zadań**.

`withTaskGroup(of:)` otwiera grupę, `addTask` uruchamia jedno zadanie podrzędne na element, a grupę czyta się następnie za pomocą `for await`, które dostarcza wyniki w miarę ich kończenia:
```swift
let total = await withTaskGroup(of: Int.self) { group in
    for n in numbers {
        group.addTask {
            return await square(n)
        }
    }
    var sum = 0
    for await value in group {
        sum += value
    }
    return sum
}
```
`of: Int.self` deklaruje, co zwraca każde zadanie podrzędne. Całe wywołanie `withTaskGroup` to jedno wyrażenie, więc potrzebuje pojedynczego `await` przed sobą i nie zwraca wyniku, dopóki każde zadanie podrzędne się nie zakończy.

# --instructions--

Napisz funkcję `sumOfSquares`, która przyjmuje tablicę liczb całkowitych (pomiń etykietę argumentu), jest asynchroniczna i zwraca sumę wyników `square` zastosowanej do każdego elementu, obliczając je za pomocą `withTaskGroup`

Przykład wywołania funkcji:
```swift
print(await sumOfSquares([1, 2, 3]))
// wypisuje 14
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func square(_ n: Int) async -> Int {
    return n * n
}

func sumOfSquares() {

}
```

# --asserts--

Funkcja musi użyć `withTaskGroup`

```json
{
  "regex": "withTaskGroup",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`sumOfSquares([1, 2, 3])` musi zwrócić `14`

```swift
tryCatch(await sumOfSquares([1, 2, 3]) == 14)
```

`sumOfSquares([4])` musi zwrócić `16`

```swift
tryCatch(await sumOfSquares([4]) == 16)
```

`sumOfSquares([])` musi zwrócić `0`

```swift
tryCatch(await sumOfSquares([]) == 0)
```

`sumOfSquares([-2, 5])` musi zwrócić `29`

```swift
tryCatch(await sumOfSquares([-2, 5]) == 29)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func square(_ n: Int) async -> Int {
    return n * n
}

func sumOfSquares(_ numbers: [Int]) async -> Int {
    return await withTaskGroup(of: Int.self) { group in
        for n in numbers {
            group.addTask {
                return await square(n)
            }
        }
        var total = 0
        for await value in group {
            total += value
        }
        return total
    }
}
```
