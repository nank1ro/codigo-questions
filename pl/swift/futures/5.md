---
language: swift
exerciseType: 2
---

# --description--

Kiedy nie obchodzi cię *dlaczego* wywołanie się nie powiodło, `try?` jest krótsze niż blok `do`. Zamienia wywołanie rzucające w **opcjonał**: wartość w razie sukcesu, `nil` w razie niepowodzenia. W połączeniu z `await` zapisuje się to jako `try? await`:
```swift
let value = try? await parse("42")  // Optional(42)
let broken = try? await parse("x")  // nil
```
Ponieważ wynik jest opcjonałem, pasuje wprost do `if let`:
```swift
if let value = try? await parse("x") {
    print(value)
} else {
    print("not a number")
}
```
Używaj `try? await` do szybkiego obejścia, a `do` / `catch`, gdy sam błąd ma znaczenie.

# --instructions--

Uzupełnij kod tak, aby nieudane wywołanie dawało `nil`, a program wypisał `not a number`

# --seed--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = [/] await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --answers--

- try?
- try
- try!
- await
- throws

# --solutions--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = try? await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --output--

not a number
