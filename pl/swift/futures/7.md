---
language: swift
exerciseType: 1
---

# --description--

Aby uruchomić dwa niezależne wywołania jednocześnie, zadeklaruj je za pomocą **`async let`**. Praca zaczyna się natychmiast, a program działa dalej bez czekania:
```swift
async let left = step("A")
async let right = step("B")
```
Wartość nie jest jeszcze dostępna, więc nie możesz użyć wiązania bezpośrednio: musisz użyć `await` w miejscu, w którym naprawdę jej potrzebujesz. Jedno `await` przed wyrażeniem obejmuje każde `async let` wewnątrz niego:
```swift
let both = await left + right
```
Jeśli każde wywołanie trwa jedną sekundę, wersja sekwencyjna potrzebuje dwóch sekund, a wersja z `async let` około jednej, ponieważ oba wywołania nakładają się na siebie.

# --instructions--

Napisz funkcję `total`, która przyjmuje dwie liczby całkowite (pomiń etykiety argumentów), jest asynchroniczna i zwraca sumę wyników `slowValue` zastosowanej do obu, uruchamiając oba wywołania współbieżnie za pomocą `async let`

Przykład wywołania funkcji:
```swift
print(await total(1, 2))
// wypisuje 30
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total() {

}
```

# --asserts--

Funkcja musi użyć `async let`

```json
{
  "regex": "async\\s+let",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`total(1, 2)` musi zwrócić `30`

```swift
tryCatch(await total(1, 2) == 30)
```

`total(0, 0)` musi zwrócić `0`

```swift
tryCatch(await total(0, 0) == 0)
```

`total(5, -2)` musi zwrócić `30`

```swift
tryCatch(await total(5, -2) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total(_ a: Int, _ b: Int) async -> Int {
    async let first = slowValue(a)
    async let second = slowValue(b)
    return await first + second
}
```
