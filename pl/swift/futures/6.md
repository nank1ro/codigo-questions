---
language: swift
exerciseType: 4
---

# --description--

Kilka wywołań `await` zapisanych jedno po drugim działa **sekwencyjnie**: drugie wywołanie w ogóle się nie zaczyna, dopóki pierwsze nie zwróci wyniku. Kod czyta się od góry do dołu, dokładnie jak zwykły kod:
```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}

let a = await step("A")
print(a)
let b = await step("B")
print(b)
// start A
// done A
// start B
// done B
```
Tego chcesz, gdy drugie wywołanie potrzebuje wyniku pierwszego. Gdy wywołania są niezależne, czekanie na jedno przed rozpoczęciem drugiego to strata czasu, a kolejne ćwiczenia pokazują, jak tego uniknąć.

# --instructions--

Ułóż linie tak, aby wypisać poniższe wyjście, definiując `step` przed kodem najwyższego poziomu
```
start A
done A
start B
done B
```

# --answers--

```swift
func step(_ name: String) async -> String {
```

```swift
    print("start \(name)")
```

```swift
    return "done \(name)"
```

```swift
}
```

```swift
let a = await step("A")
```

```swift
print(a)
```

```swift
let b = await step("B")
```

```swift
print(b)
```

# --solutions--

```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}
let a = await step("A")
print(a)
let b = await step("B")
print(b)
```

# --output--

start A
done A
start B
done B
