---
language: swift
exerciseType: 1
---

# --description--

Możemy również przypisywać zmiennym wartości innych zmiennych. Tutaj możemy przypisać zmiennej `y` wartość `x`
```swift
var x = 5
var y = x
print(y) // prints 5
```

# --instructions--

Utwórz nową zmienną o nazwie `y` i przypisz jej wartość zmiennej `x`.

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
var x = 1
// write after this line
```

# --asserts--

`y` must be equal to `x`.

```swift
tryCatch(x == y)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```
# --solutions--

```swift
var x = 1
var y = x
```
