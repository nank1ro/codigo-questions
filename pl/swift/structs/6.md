---
language: swift
exerciseType: 4
---

# --instructions--

Ułóż elementy tak, aby wypisać `12`, deklarując najpierw strukturę, a instancję przed `print`

# --answers--

```swift
struct Rectangle {
    var width: Int
    var height: Int
    func area() -> Int {
        return width * height
    }
}
```

```swift
let r = Rectangle(width: 3, height: 4)
```

```swift
print(r.area())
```

# --solutions--

```swift
struct Rectangle {
    var width: Int
    var height: Int
    func area() -> Int {
        return width * height
    }
}
let r = Rectangle(width: 3, height: 4)
print(r.area())
```

# --output--

12
