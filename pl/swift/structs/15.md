---
language: swift
exerciseType: 1
---

# --description--

Struktura to typ jak każdy inny, więc można ją przechowywać w tablicy, słowniku lub zbiorze, a każde narzędzie, które już znasz, nadal na niej działa:
```swift
struct Item {
    var name: String
    var price: Int
}

let items = [Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]

for item in items {
    print(item.name)
}

let names = items.map { $0.name }
let total = items.reduce(0) { $0 + $1.price }
print(total) // 10
```
Pamiętaj, że tablica przechowuje *kopie*: odczytanie `items[0]` do zmiennej i zmiana jej nie dotyka tablicy.

# --instructions--

Napisz funkcję `totalPrice`, która przyjmuje tablicę `Item` bez etykiety argumentu i zwraca sumę `price` wszystkich elementów albo `0`, gdy tablica jest pusta

Przykład wywołania funkcji:
```swift
print(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]))
// prints 10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice() {

}
```

# --asserts--

Dwa elementy o cenach `3` i `7` muszą dać razem `10`

```swift
tryCatch(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]) == 10)
```

Pojedynczy element musi dać swoją własną cenę

```swift
tryCatch(totalPrice([Item(name: "Pen", price: 2)]) == 2)
```

Pusta tablica musi dać `0`

```swift
tryCatch(totalPrice([]) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice(_ items: [Item]) -> Int {
    return items.reduce(0) { $0 + $1.price }
}
```
