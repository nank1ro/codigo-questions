---
language: swift
exerciseType: 4
---

# --instructions--

Ułóż elementy tak, aby wypisać `EUR 12`, deklarując najpierw strukturę, a instancję przed `print`

# --answers--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int
}
```

```swift
let price = Money(amount: 12)
```

```swift
print("\(Money.currency) \(price.amount)")
```

# --solutions--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int
}
let price = Money(amount: 12)
print("\(Money.currency) \(price.amount)")
```

# --output--

EUR 12
