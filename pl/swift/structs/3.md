---
language: swift
exerciseType: 2
---

# --description--

Właściwości przechowywanej można nadać **wartość domyślną** dokładnie w miejscu jej deklaracji. Swift wywnioskuje jej typ z tej wartości, więc możesz pominąć adnotację typu:
```swift
struct Counter {
    var label: String
    var value = 0
}
```
Inicjalizator memberwise zamienia każdą właściwość z wartością domyślną na argument opcjonalny: przekaż go, aby nadpisać wartość domyślną, lub pomiń, aby ją zachować.
```swift
let a = Counter(label: "clicks")
print(a.value) // 0

let b = Counter(label: "clicks", value: 7)
print(b.value) // 7
```

# --instructions--

Uzupełnij kod tak, aby `value` zaczynało od `0`, a `c` został utworzony bez przekazywania `value`

# --seed--

```swift
struct Counter {
    var label: String
    var value = [/]
}

let c = Counter([/]: "clicks")
print(c.label)
print(c.value)
```

# --answers--

- 0
- label
- 1
- value
- Counter
- name

# --solutions--

```swift
struct Counter {
    var label: String
    var value = 0
}

let c = Counter(label: "clicks")
print(c.label)
print(c.value)
```

# --output--

clicks
0
