---
language: swift
exerciseType: 1
---

# --description--

Niektóre wartości należą do samego typu, a nie do pojedynczej instancji: kod waluty, wspólna wartość domyślna, fabryka tworząca typowy przypadek. Oznacz je jako `static` i odczytuj przez nazwę typu:
```swift
struct Money {
    static let currency = "EUR"
    var amount: Int

    static func zero() -> Money {
        return Money(amount: 0)
    }
}

print(Money.currency)     // EUR
print(Money.zero().amount) // 0
```
Tutaj `currency` jest zadeklarowane słowem `let`, ponieważ nigdy się nie zmienia, więc jest to stała współdzielona przez cały program. `Money.currency` działa bez tworzenia choćby jednego `Money`, podczas gdy `amount` potrzebuje instancji.

# --instructions--

Dodaj do `Money` statyczną stałą `currency` równą `"EUR"` oraz statyczną metodę `zero`, która nie przyjmuje żadnego parametru i zwraca `Money` z `amount` równym `0`

Przykład użycia:
```swift
print(Money.currency)
// prints EUR
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Money {
    var amount: Int

}
```

# --asserts--

`currency` musi być statyczną stałą

```json
{
  "regex": "static\\s+let\\s+currency",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Money.currency` musi być `"EUR"`

```swift
tryCatch(Money.currency == "EUR")
```

`Money.zero()` musi mieć `amount` równe `0`

```swift
tryCatch(Money.zero().amount == 0)
```

Inicjalizator memberwise musi nadal działać

```swift
tryCatch(Money(amount: 7).amount == 7)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int

    static func zero() -> Money {
        return Money(amount: 0)
    }
}
```
