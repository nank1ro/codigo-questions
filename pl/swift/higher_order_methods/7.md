---
language: swift
exerciseType: 1
---

# --description--

Funkcje wyższego rzędu działają na dowolnych tablicach, także na tablicach twoich własnych struktur. Typowym sposobem wybrania niektórych elementów i wyodrębnienia z każdego z nich wartości jest połączenie w łańcuch `filter`, a następnie `map`:
```swift
struct Book {
    var title: String
    var pages: Int
}
let books = [Book(title: "Dune", pages: 412), Book(title: "Haiku", pages: 40)]
let long = books.filter { $0.pages > 100 }.map { $0.title }
print(long) // ["Dune"]
```
Zrobienie tego w odwrotnej kolejności, najpierw `map`, potem `filter`, straciłoby własność `pages`, zanim sprawdzenie mogłoby jej użyć.

# --instructions--

Struktura `Product` jest podana. Napisz funkcję `cheapNames`, która przyjmuje tablicę `Product` (pomiń etykietę argumentu) i zwraca nazwy produktów, których `price` jest niższe niż `10`, w oryginalnej kolejności, łącząc `filter` i `map` w łańcuch

Przykład wywołania funkcji:
```swift
print(cheapNames([Product(name: "Pen", price: 2.5), Product(name: "Bag", price: 30)]))
// prints ["Pen"]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Product {
    var name: String
    var price: Double
}
```

# --asserts--

Musisz użyć `filter`

```json
{
  "regex": "\\.filter",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć `map`

```json
{
  "regex": "\\.map",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`cheapNames` musi zwrócić `["Pen"]` dla długopisu za `2.5` i torby za `30`

```swift
tryCatch(cheapNames([Product(name: "Pen", price: 2.5), Product(name: "Bag", price: 30)]) == ["Pen"])
```

`cheapNames` musi zachować oryginalną kolejność

```swift
tryCatch(cheapNames([Product(name: "Tea", price: 4), Product(name: "Cup", price: 9.99), Product(name: "Pot", price: 25)]) == ["Tea", "Cup"])
```

Produkt wyceniony dokładnie na `10` musi zostać wykluczony

```swift
tryCatch(cheapNames([Product(name: "Cap", price: 10)]) == [])
```

`cheapNames([])` musi zwrócić `[]`

```swift
tryCatch(cheapNames([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Product {
    var name: String
    var price: Double
}
func cheapNames(_ products: [Product]) -> [String] {
    return products.filter { $0.price < 10 }.map { $0.name }
}
```
