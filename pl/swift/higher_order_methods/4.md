---
language: swift
exerciseType: 2
---

# --description--

`reduce` buduje nową skumulowaną wartość w każdym kroku, co jest marnotrawstwem, gdy wynikiem jest tablica lub słownik. `reduce(into:)` przekazuje domknięciu akumulator jako parametr `inout`, więc może być modyfikowany w miejscu bez `return`:
```swift
let words = ["a", "b", "a"]
let counts = words.reduce(into: [String: Int]()) { result, word in
    result[word, default: 0] += 1
}
print(counts["a"]!) // 2
```
`[String: Int]()` tworzy pusty słownik, a `result[word, default: 0]` odczytuje bieżącą liczbę lub `0`, gdy klucz nie istnieje.

# --instructions--

Uzupełnij kod tak, aby za pomocą `reduce(into:)` policzyć, ile razy pojawia się każda litera, tak aby program wypisał `3`

# --seed--

```swift
let letters = ["x", "y", "x", "x"]
let counts = letters.reduce([/] [String: Int]()) { result, letter in
    result[letter, [/] 0] += 1
}
print(counts["x"]!)
```

# --answers--

- into:
- default:
- 0,
- initial:
- or:

# --solutions--

```swift
let letters = ["x", "y", "x", "x"]
let counts = letters.reduce(into: [String: Int]()) { result, letter in
    result[letter, default: 0] += 1
}
print(counts["x"]!)
```

# --output--

3
