---
language: swift
exerciseType: 4
---

# --description--

`contains(where:)` i `allSatisfy` zastępują częsty wzorzec pętli ze zmienną flagową. Obie zatrzymują się, gdy tylko odpowiedź jest znana: `contains(where:)` przy pierwszym dopasowaniu, `allSatisfy` przy pierwszym elemencie, który go nie spełnia.
```swift
let ages = [15, 22, 40]
let anyMinor = ages.contains { $0 < 18 }  // true
let allAdults = ages.allSatisfy { $0 >= 18 } // false
```
`contains { ... }` to forma trailing closure dla `contains(where:)`, nie należy jej mylić z `contains(_:)`, która szuka konkretnej wartości.

# --instructions--

Ułóż elementy tak, aby sprawdzić, czy każdy wynik ma co najmniej `60` i czy którykolwiek wynik wynosi dokładnie `100`, a następnie wypisać oba wyniki w jednym wierszu.
Najpierw zadeklaruj `scores`, a wypisz na końcu

# --answers--

```swift
let scores = [72, 100, 64]
```

```swift
let allPassed = scores.allSatisfy { $0 >= 60 }
```

```swift
let anyPerfect = scores.contains { $0 == 100 }
```

```swift
print("\(allPassed) \(anyPerfect)")
```

# --solutions--

```swift
let scores = [72, 100, 64]
let allPassed = scores.allSatisfy { $0 >= 60 }
let anyPerfect = scores.contains { $0 == 100 }
print("\(allPassed) \(anyPerfect)")
```

```swift
let scores = [72, 100, 64]
let anyPerfect = scores.contains { $0 == 100 }
let allPassed = scores.allSatisfy { $0 >= 60 }
print("\(allPassed) \(anyPerfect)")
```

# --output--

true true
