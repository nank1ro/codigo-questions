---
language: swift
exerciseType: 2
---

# --description--

`zip` paruje elementy dwóch sekwencji pozycja po pozycji, tworząc sekwencję krotek. Zatrzymuje się na końcu krótszej z nich:
```swift
let names = ["Ann", "Bob"]
let ages = [31, 27, 99]
let pairs = zip(names, ages).map { "\($0) is \($1)" }
print(pairs) // ["Ann is 31", "Bob is 27"]
```
Wewnątrz domknięcia `$0` to element z pierwszej sekwencji, a `$1` ten z drugiej. `zip` to wolna funkcja, a nie metoda: piszesz `zip(a, b)`, a nie `a.zip(b)`.

# --instructions--

Uzupełnij kod tak, aby sparować każde miasto z jego temperaturą i zachować tylko miasta cieplejsze niż `20` stopni, tak aby program wypisał `["Rome", "Cairo"]`

# --seed--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = [/](cities, temps).[/] { $1 > 20 }.map { $0.0 }
print(warm)
```

# --answers--

- zip
- filter
- map
- enumerated
- compactMap

# --solutions--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = zip(cities, temps).filter { $1 > 20 }.map { $0.0 }
print(warm)
```

# --output--

["Rome", "Cairo"]
