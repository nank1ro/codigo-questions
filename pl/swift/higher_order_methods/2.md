---
language: swift
exerciseType: 1
---

# --description--

`flatMap` jest przeznaczony dla domknięć, które zwracają **tablicę**: zamiast budować tablicę tablic, łączy wszystkie zwrócone tablice w jeden płaski wynik:
```swift
let teams = [["Ann", "Bob"], ["Cid"]]
print(teams.map { $0 })     // [["Ann", "Bob"], ["Cid"]]
print(teams.flatMap { $0 }) // ["Ann", "Bob", "Cid"]
```
Domknięcie może też przekształcić każdą wewnętrzną tablicę przed jej spłaszczeniem, na przykład `teams.flatMap { $0.reversed() }` daje `["Bob", "Ann", "Cid"]`.

# --instructions--

Napisz funkcję `flatten`, która przyjmuje tablicę tablic `Int` (pomiń etykietę argumentu) i zwraca pojedynczą tablicę ze wszystkimi liczbami w kolejności, używając `flatMap`

Przykład wywołania funkcji:
```swift
print(flatten([[1, 2], [3]]))
// prints [1, 2, 3]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Musisz użyć `flatMap`

```json
{
  "regex": "\\.flatMap",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`flatten([[1, 2], [3]])` musi zwrócić `[1, 2, 3]`

```swift
tryCatch(flatten([[1, 2], [3]]) == [1, 2, 3])
```

`flatten([[5], [], [6, 7]])` musi zwrócić `[5, 6, 7]`

```swift
tryCatch(flatten([[5], [], [6, 7]]) == [5, 6, 7])
```

`flatten([])` musi zwrócić `[]`

```swift
tryCatch(flatten([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func flatten(_ lists: [[Int]]) -> [Int] {
    return lists.flatMap { $0 }
}
```
