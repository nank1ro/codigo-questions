---
language: swift
exerciseType: 1
---

# --description--

`map` i `filter` na tablicy są **gorliwe**: każda z nich przetwarza całą tablicę i buduje nową, zanim uruchomi się kolejny krok. Na dużej kolekcji, albo gdy potrzebujesz tylko pierwszego wyniku, to zmarnowana praca.
Własność `lazy` zwraca widok, którego operacje działają dopiero wtedy, gdy element jest faktycznie żądany, po jednym elemencie w całym łańcuchu:
```swift
let firstBig = (1...1000).lazy.map { $0 * $0 }.first { $0 > 50 }
print(firstBig!) // 64
```
Tutaj tylko `1, 2, ..., 8` zostają podniesione do kwadratu: `first(where:)` żąda elementów, dopóki jeden nie spełni warunku, i tam łańcuch się zatrzymuje. Bez `lazy` `map` podniosłaby najpierw do kwadratu wszystkie 1000 liczb.

# --instructions--

Napisz funkcję `firstCubeAbove`, która przyjmuje `Int` o nazwie `limit` (pomiń etykietę argumentu) i zwraca najmniejszy sześcian (`n * n * n` dla `n` zaczynającego się od `1`) większy niż `limit`, używając `lazy` i `map` na zakresie `1...limit + 1` oraz `first(where:)`

Przykład wywołania funkcji:
```swift
print(firstCubeAbove(10))
// prints 27
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Musisz użyć `lazy`

```json
{
  "regex": "\\.lazy",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Nie możesz użyć pętli

```json
{
  "regex": "\\b(for|while)\\b",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`firstCubeAbove(10)` musi zwrócić `27`

```swift
tryCatch(firstCubeAbove(10) == 27)
```

`firstCubeAbove(27)` musi zwrócić `64`

```swift
tryCatch(firstCubeAbove(27) == 64)
```

`firstCubeAbove(0)` musi zwrócić `1`

```swift
tryCatch(firstCubeAbove(0) == 1)
```

`firstCubeAbove(1000000)` musi zwrócić `1030301`

```swift
tryCatch(firstCubeAbove(1000000) == 1030301)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstCubeAbove(_ limit: Int) -> Int {
    return (1...limit + 1).lazy.map { $0 * $0 * $0 }.first(where: { $0 > limit })!
}
```
