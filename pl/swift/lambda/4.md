---
language: swift
exerciseType: 2
---

# --description--

Ponieważ domknięcia są wartościami, funkcja może przyjąć jedno z nich jako parametr. Typ parametru to po prostu typ funkcyjny:
```swift
func apply(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(n)
}
print(apply(5, { $0 + 1 })) // 6
```
Funkcja `apply` nie wie, co robi `operation`, wie tylko, że przyjmuje `Int` i zwraca `Int`, i wywołuje ją jak każdą inną funkcję.

# --instructions--

Uzupełnij funkcję `apply` tak, aby jej drugim parametrem było domknięcie przyjmujące `Int` i zwracające `Int`, a funkcja zwracała wynik wywołania tego domknięcia na `n`, tak aby program wypisał `12`

# --seed--

```swift
func apply(_ n: Int, _ operation: [/]) -> Int {
    return [/]
}
print(apply(6, { $0 * 2 }))
```

# --answers--

- (Int) -> Int
- operation(n)
- Int
- (Int) -> Void
- operation
- n

# --solutions--

```swift
func apply(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(n)
}
print(apply(6, { $0 * 2 }))
```

# --output--

12
