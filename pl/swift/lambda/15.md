---
language: swift
exerciseType: 1
---

# --description--

Domyślnie domknięcie przekazane do funkcji może być używane tylko podczas działania tej funkcji. Jeśli funkcja przechowuje domknięcie albo zwraca inne domknięcie, które go używa, domknięcie **ucieka** (escapes) z funkcji, a jego parametr musi być oznaczony `@escaping`:
```swift
func twice(_ task: @escaping () -> Int) -> () -> Int {
    return { task() * 2 }
}
let answer = twice { 21 }
print(answer()) // 42
```
Bez `@escaping` kompilator zgłasza błąd, ponieważ zwrócone domknięcie używałoby `task` już po zakończeniu działania `twice`.

# --instructions--

Napisz funkcję `compose`, która przyjmuje dwa domknięcia typu `(Int) -> Int` (pomiń obie etykiety argumentów) i zwraca domknięcie, które stosuje do swojego argumentu najpierw pierwsze, a potem drugie

Przykład wywołania funkcji:
```swift
let addThenDouble = compose({ $0 + 1 }, { $0 * 2 })
print(addThenDouble(3))
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`compose({ $0 + 1 }, { $0 * 2 })(3)` musi zwrócić `8`

```swift
tryCatch(compose({ $0 + 1 }, { $0 * 2 })(3) == 8)
```

`compose({ $0 * 2 }, { $0 + 1 })(3)` musi zwrócić `7`

```swift
tryCatch(compose({ $0 * 2 }, { $0 + 1 })(3) == 7)
```

`compose({ $0 }, { $0 })(9)` musi zwrócić `9`

```swift
tryCatch(compose({ $0 }, { $0 })(9) == 9)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func compose(_ first: @escaping (Int) -> Int, _ second: @escaping (Int) -> Int) -> (Int) -> Int {
    return { second(first($0)) }
}
```
