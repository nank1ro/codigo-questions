---
language: swift
exerciseType: 4
---

# --description--

Domknięcie zapisane w stałej może być przekazane wszędzie tam, gdzie oczekiwany jest argument-domknięcie, przy użyciu etykiety argumentu parametru:
```swift
let ascending = { (a: Int, b: Int) -> Bool in a < b }
print([3, 1, 2].sorted(by: ascending)) // [1, 2, 3]
```

# --instructions--

Ułóż elementy, aby zapisać w `byLength` domknięcie, które porządkuje dwa ciągi znaków od najkrótszego do najdłuższego, a następnie wydrukować `words` posortowane za jego pomocą.
Zadeklaruj `words` przed `byLength`

# --answers--

```swift
let words = ["kiwi", "fig", "banana"]
```

```swift
let byLength = { (a: String, b: String) -> Bool in
```

```swift
    a.count < b.count
```

```swift
}
```

```swift
print(words.sorted(by: byLength))
```

# --solutions--

```swift
let words = ["kiwi", "fig", "banana"]
let byLength = { (a: String, b: String) -> Bool in
    a.count < b.count
}
print(words.sorted(by: byLength))
```

# --output--

["fig", "kiwi", "banana"]
