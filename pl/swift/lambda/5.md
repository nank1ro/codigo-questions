---
language: swift
exerciseType: 1
---

# --description--

Gdy domknięcie jest **ostatnim** argumentem funkcji, możesz zapisać je po nawiasie zamykającym wywołanie. To jest składnia **trailing closure**:
```swift
print(apply(5) { $0 + 1 }) // 6
```
Jeśli domknięcie jest jedynym argumentem, nawiasy można całkowicie pominąć:
```swift
func run(_ task: () -> Int) -> Int {
    return task()
}
print(run { 42 }) // 42
```
Obie formy wywołują dokładnie tę samą funkcję, składnia trailing jest po prostu łatwiejsza do odczytania, gdy domknięcie jest długie.

# --instructions--

Napisz funkcję `applyTwice`, która przyjmuje `Int` i domknięcie typu `(Int) -> Int` (pomiń obie etykiety argumentów) i zwraca wynik dwukrotnego zastosowania domknięcia do liczby

Przykład wywołania funkcji:
```swift
print(applyTwice(3) { $0 * 2 })
// prints 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`applyTwice(3) { $0 * 2 }` musi zwrócić `12`

```swift
tryCatch(applyTwice(3) { $0 * 2 } == 12)
```

`applyTwice(1) { $0 + 10 }` musi zwrócić `21`

```swift
tryCatch(applyTwice(1) { $0 + 10 } == 21)
```

`applyTwice(2, { $0 * $0 })` musi zwrócić `16`

```swift
tryCatch(applyTwice(2, { $0 * $0 }) == 16)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func applyTwice(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(operation(n))
}
```
