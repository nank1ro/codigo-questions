---
language: swift
exerciseType: 1
---

# --description--

Zapisywanie każdego typu wewnątrz domknięcia jest często niepotrzebne. Gdy stała ma jawny **typ funkcyjny**, Swift wnioskuje typy parametrów i typ zwracany, więc przed `in` wystarczy podać tylko nazwy parametrów:
```swift
let triple: (Int) -> Int = { n in
    return n * 3
}
```
Typ `(Int) -> Int` czyta się jako "funkcja, która przyjmuje `Int` i zwraca `Int`".
Gdy treść to pojedyncze wyrażenie, słowo kluczowe `return` również można pominąć, nazywa się to **niejawnym zwracaniem**:
```swift
let triple: (Int) -> Int = { n in n * 3 }
print(triple(4)) // 12
```

# --instructions--

Zapisz w stałej `square` domknięcie typu `(Int) -> Int`, które zwraca swój argument pomnożony przez siebie samego

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Musisz użyć wyrażenia domknięcia, a nie `func`

```json
{
  "regex": "func\\s+square",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`square(4)` musi zwrócić `16`

```swift
tryCatch(square(4) == 16)
```

`square(-3)` musi zwrócić `9`

```swift
tryCatch(square(-3) == 9)
```

`square(0)` musi zwrócić `0`

```swift
tryCatch(square(0) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let square: (Int) -> Int = { n in n * n }
```
