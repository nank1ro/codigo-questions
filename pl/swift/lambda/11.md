---
language: swift
exerciseType: 2
---

# --description--

Domknięcie może korzystać ze zmiennych zadeklarowanych poza jego treścią. **Przechwytuje** je: zmienna nadal istnieje tak długo, jak istnieje domknięcie, nawet po zakończeniu funkcji, która ją zadeklarowała.
Dzięki temu funkcja może zbudować domknięcie z własnym, prywatnym stanem:
```swift
func makeCounter() -> () -> Int {
    var count = 0
    return {
        count += 1
        return count
    }
}
```
`() -> Int` to typ domknięcia bez parametrów, które zwraca `Int`. Każde wywołanie zwróconego domknięcia zwiększa to samo przechwycone `count`:
```swift
let counter = makeCounter()
print(counter()) // 1
print(counter()) // 2
```

# --instructions--

Uzupełnij `makeCounter` tak, aby zwracała domknięcie bez parametrów, które zwiększa i zwraca przechwycone `count`, tak aby program wypisał `1`, `2` i `3` w trzech liniach

# --seed--

```swift
func makeCounter() -> [/] {
    [/] count = 0
    return {
        count += 1
        return count
    }
}
let counter = makeCounter()
print(counter())
print(counter())
print(counter())
```

# --answers--

- () -> Int
- var
- let
- Int
- () -> Void

# --solutions--

```swift
func makeCounter() -> () -> Int {
    var count = 0
    return {
        count += 1
        return count
    }
}
let counter = makeCounter()
print(counter())
print(counter())
print(counter())
```

# --output--

1
2
3
