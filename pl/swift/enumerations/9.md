---
language: swift
exerciseType: 1
---

# --instructions--

Dodaj odpowiedni typ do wyliczenia, aby móc po nim iterować

# --seed--

```swift
enum Gender {
    case male, female
}
```

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`Gender` musi być iterowalne.

```swift
    func test1() {
        XCTAssertEqual(Gender.allCases.count, 2, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
enum Gender: CaseIterable {
    case male, female
}
```
