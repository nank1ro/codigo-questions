---
language: swift
exerciseType: 2
---

# --description--

Gdy klasa posiada własne funkcje, te funkcje są nazywane __metodami__.

# --instructions--

Dodaj metodę `description` do swojej klasy `Animal`.
Używając dwóch oddzielnych instrukcji `print`, powinna wydrukować `gender` i `legs` zwierzęcia, na którym jest wywoływana.
Następnie wywołaj metodę `description`.

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4

    [/][/]() {
        if let gender = self.gender { print(gender) }
        print([/].legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.[/]
```

# --answers--

-  description
- func
- description()
- self
- description[]
- function
- method

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4

    func description() {
        if let gender = self.gender { print(gender) }
        print(self.legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.description()
```

# --output--

female
4
