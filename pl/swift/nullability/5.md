---
language: swift
exerciseType: 1
---

# --description--

Sprawdzanie `nil`, a potem wymuszone rozpakowanie jest rozwlekłe. Swift oferuje **wiązanie opcjonalne** za pomocą `if let`, które rozpakowuje opcjonał i zapisuje wartość w nowej stałej w jednym kroku:
```swift
let score: Int? = 10
if let value = score {
    print("Score: \(value)") // value jest typu Int, a nie Int?
} else {
    print("No score")
}
```
Ciało `if` wykonuje się tylko wtedy, gdy opcjonał zawiera wartość; wewnątrz niego `value` jest zwykłym `Int` i nie wymaga `!`.

# --instructions--

Napisz funkcję `describe`, która przyjmuje `Int?` (pomiń etykietę argumentu) i zwraca `"Value: "` z dołączoną liczbą (na przykład `"Value: 5"` dla `5`), gdy opcjonał zawiera wartość, albo `"No value"`, gdy jest `nil`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`describe(5)` musi zwrócić `"Value: 5"`

```swift
tryCatch(describe(5) == "Value: 5")
```

`describe(42)` musi zwrócić `"Value: 42"`

```swift
tryCatch(describe(42) == "Value: 42")
```

`describe(nil)` musi zwrócić `"No value"`

```swift
tryCatch(describe(nil) == "No value")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func describe(_ number: Int?) -> String {
    if let value = number {
        return "Value: \(value)"
    } else {
        return "No value"
    }
}
```
