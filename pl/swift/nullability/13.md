---
language: swift
exerciseType: 1
---

# --description--

Pojedyncze `if let` lub `guard let` może rozpakować kilka opcjonałów naraz: wystarczy oddzielić wiązania przecinkami.
Ciało wykonuje się tylko wtedy, gdy każdy opcjonał ma wartość:
```swift
let first: String? = "Ada"
let last: String? = "Lovelace"
if let first = first, let last = last {
    print("\(first) \(last)")
}
```
Możesz też dodać warunek logiczny po wiązaniach, na przykład `if let n = number, n > 0`.

# --instructions--

Napisz funkcję `add`, która przyjmuje dwie wartości `Int?` (pomiń etykiety argumentów) i zwraca ich sumę jako `Int?`, albo `nil`, jeśli którejś z nich brakuje

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`add(2, 3)` musi zwrócić `5`

```swift
tryCatch(add(2, 3) == 5)
```

`add(nil, 3)` musi zwrócić `nil`

```swift
tryCatch(add(nil, 3) == nil)
```

`add(2, nil)` musi zwrócić `nil`

```swift
tryCatch(add(2, nil) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func add(_ a: Int?, _ b: Int?) -> Int? {
    guard let a = a, let b = b else { return nil }
    return a + b
}
```
