---
language: swift
exerciseType: 1
---

# --description--

Czasami chcesz przekształcić wartość znajdującą się wewnątrz opcjonału i zachować wynik jako opcjonalny, bez ręcznego rozpakowywania i ponownego pakowania.
Opcjonały mają metodę `map`: stosuje domknięcie do wartości, jeśli taka istnieje, a w przeciwnym razie zwraca `nil`.
```swift
let score: Int? = 10
let doubled = score.map { $0 * 2 } // Int? zawierający 20
let missing: Int? = nil
let stillMissing = missing.map { $0 * 2 } // nil
```
Połączona z konwersją, która może się nie powieść, tworzy zwięzły potok: `Int(text).map { $0 + 1 }`.

# --instructions--

Napisz funkcję `squareText`, która przyjmuje `String` (pomiń etykietę argumentu), konwertuje ją na `Int` i zwraca jej kwadrat jako `Int?`, albo `nil`, gdy konwersja się nie powiedzie.
Użyj `map`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`squareText("4")` musi zwrócić `16`

```swift
tryCatch(squareText("4") == 16)
```

`squareText("12")` musi zwrócić `144`

```swift
tryCatch(squareText("12") == 144)
```

`squareText("x")` musi zwrócić `nil`

```swift
tryCatch(squareText("x") == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func squareText(_ text: String) -> Int? {
    return Int(text).map { $0 * $0 }
}
```
