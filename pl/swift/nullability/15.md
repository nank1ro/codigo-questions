---
language: swift
exerciseType: 1
---

# --description--

Wiele operacji może się nie powieść, a Swift zgłasza niepowodzenie, zwracając opcjonał.
Konwersja tekstu na liczbę to klasyczny przykład: `Int("42")` zwraca `Int?` z wartością `42`, natomiast `Int("abc")` zwraca `nil`.
`Int("3.5")` też jest `nil`, ponieważ tekst nie jest liczbą całkowitą; użyj `Double("3.5")` dla liczb dziesiętnych.
```swift
let typed = "42"
if let number = Int(typed) {
    print(number + 1) // 43
}
```
Innymi przykładami są `array.first` (`nil` dla pustej tablicy) i `dictionary[key]` (`nil`, gdy brakuje klucza).

# --instructions--

Napisz funkcję `parseAge`, która przyjmuje `String` (pomiń etykietę argumentu) i zwraca ją przekonwertowaną na `Int`, albo `-1`, gdy tekst nie jest poprawną liczbą całkowitą

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`parseAge("42")` musi zwrócić `42`

```swift
tryCatch(parseAge("42") == 42)
```

`parseAge("abc")` musi zwrócić `-1`

```swift
tryCatch(parseAge("abc") == -1)
```

`parseAge("3.5")` musi zwrócić `-1`

```swift
tryCatch(parseAge("3.5") == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func parseAge(_ text: String) -> Int {
    return Int(text) ?? -1
}
```
