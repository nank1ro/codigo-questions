---
language: swift
exerciseType: 1
---

# --instructions--

Masz `60` produktów w `store`, w ciągu dnia sprzedałeś `50` z nich i `bought` `55` nowych.
Zaktualizuj ilość w `store`.

# --seed--

```swift
var store = 60
var sold = 50
var bought = 55
// write after this line
```

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`store` musi być równe `65`.

```swift
    func test1() {
        XCTAssertEqual(store, 65, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
var store = 60
var sold = 50
var bought = 55
store += bought - sold;
```
