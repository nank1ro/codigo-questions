---
language: swift
exerciseType: 2
---

# --description--

Możesz użyć typu krotki jako typu zwracanego przez funkcję, aby zwrócić wiele wartości jako część jednej złożonej wartości zwracanej.

# --instructions--

Uzupełnij kod, aby utworzyć poprawną funkcję o nazwie `minMax` przekazując następującą tablicę liczb `[15, 24, 31, 79, 44]`

# --seed--

```swift
func minMax(array: [Int]) -> [/]min: Int, max: Int[/] {
    var currentMin = array[0]
    var currentMax = array[0]
    array.forEach { num in
        if num < currentMin {
            currentMin = num
        } else if num > currentMax {
            currentMax = num
        }
    }
    return [/][/], [/][/]
}
let numbers: [Int] = [15, 24, 31, 79, 44]
print(minMax([/]: numbers))
```

# --answers--

- (
- )
- [
- ]
- {
- }
- (
- )
- array
- numbers
- currentMin
- currentMax

# --solutions--

```swift
func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    array.forEach { num in
        if num < currentMin {
            currentMin = num
        } else if num > currentMax {
            currentMax = num
        }
    }
    return (currentMin, currentMax)
}
let numbers: [Int] = [15, 24, 31, 79, 44]
print(minMax(array: numbers))
```

# --output--

(min: 15, max: 79)
