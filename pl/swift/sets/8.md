---
language: swift
exerciseType: 4
---

# --description--

Zbiory obsługują klasyczne operacje teorii mnogości. Każda z nich zwraca **nowy** zbiór i pozostawia oryginały niezmienione:
- `a.union(b)` zawiera każdy element, który jest w `a`, w `b`, lub w obu
- `a.intersection(b)` zawiera tylko elementy, które są w **obu** `a` i `b`
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.union(b).sorted())        // [1, 2, 3, 4]
print(a.intersection(b).sorted()) // [3]
```

# --instructions--

Ułóż elementy tak, aby wypisać każdą liczbę, która występuje w `odds` lub w `primes`, w kolejności rosnącej.
Zadeklaruj `odds` przed `primes`

# --answers--

```swift
let odds: Set<Int> = [1, 3, 5]
```

```swift
let primes: Set<Int> = [2, 3, 5]
```

```swift
let all = odds.union(primes)
```

```swift
print(all.sorted())
```

# --solutions--

```swift
let odds: Set<Int> = [1, 3, 5]
let primes: Set<Int> = [2, 3, 5]
let all = odds.union(primes)
print(all.sorted())
```

# --output--

[1, 2, 3, 5]
