---
language: swift
exerciseType: 1
---

# --description--

Podobnie jak tablice, zbiory mogą być stałymi (`let`) lub zmiennymi (`var`). Tylko zbiór `var` można zmienić po utworzeniu.
Aby utworzyć pusty zbiór, wywołujesz inicjalizator typu, ponieważ sam pusty literał `[]` nie powiedziałby Swiftowi, jakiego typu elementów użyć:
```swift
var visited = Set<String>()
print(visited.isEmpty) // true
```
Właściwość `isEmpty` ma wartość `true`, gdy zbiór nie ma elementów, dokładnie tak jak w przypadku tablic.

# --instructions--

Zadeklaruj stały zbiór ciągów znaków o nazwie `colors` zawierający `"red"`, `"green"` i `"blue"`, oraz pusty zmienny zbiór liczb całkowitych o nazwie `scores`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`colors` musi zawierać dokładnie `"red"`, `"green"` i `"blue"`

```swift
tryCatch(colors == ["red", "green", "blue"])
```

`colors` musi być typu `Set<String>`

```swift
tryCatch(type(of: colors) == Set<String>.self)
```

`scores` musi być pusty

```swift
tryCatch(scores.isEmpty)
```

`scores` musi być typu `Set<Int>`

```swift
tryCatch(type(of: scores) == Set<Int>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let colors: Set<String> = ["red", "green", "blue"]
var scores = Set<Int>()
```
