---
language: swift
exerciseType: 1
---

# --description--

Dwie kolejne operacje uzupełniają tę rodzinę:
- `a.subtracting(b)` zawiera elementy `a`, których **nie ma** w `b`
- `a.symmetricDifference(b)` zawiera elementy, które są w `a` lub w `b`, ale **nie w obu**
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.subtracting(b).sorted())          // [1, 2]
print(a.symmetricDifference(b).sorted())  // [1, 2, 4]
```
W przeciwieństwie do `union` i `intersection`, `subtracting` nie jest symetryczne: `a.subtracting(b)` i `b.subtracting(a)` zazwyczaj się różnią.

# --instructions--

Napisz dwie funkcje, które przyjmują dwa `Set<String>` (pomiń etykiety argumentów):
- `common` zwraca zbiór wartości obecnych w obu zbiorach
- `onlyInFirst` zwraca zbiór wartości obecnych w pierwszym zbiorze, ale nie w drugim

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`common(["a", "b", "c"], ["b", "c", "d"])` musi zwrócić `["b", "c"]`

```swift
tryCatch(common(["a", "b", "c"], ["b", "c", "d"]) == ["b", "c"])
```

`common(["a"], ["b"])` musi zwrócić pusty zbiór

```swift
tryCatch(common(["a"], ["b"]).isEmpty)
```

`onlyInFirst(["a", "b", "c"], ["b", "c", "d"])` musi zwrócić `["a"]`

```swift
tryCatch(onlyInFirst(["a", "b", "c"], ["b", "c", "d"]) == ["a"])
```

`onlyInFirst(["x", "y"], ["z"])` musi zwrócić `["x", "y"]`

```swift
tryCatch(onlyInFirst(["x", "y"], ["z"]) == ["x", "y"])
```

Obie funkcje muszą zwracać `Set<String>`

```swift
tryCatch(type(of: common(["a"], ["a"])) == Set<String>.self && type(of: onlyInFirst(["a"], ["a"])) == Set<String>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func common(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.intersection(second)
}

func onlyInFirst(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.subtracting(second)
}
```
