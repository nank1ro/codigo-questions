---
language: swift
exerciseType: 1
---

# --description--

`TODO` zwykle znajduje się obok elementu zastępczego, który pozwala kodowi się kompilować, dopóki nie zostanie napisana właściwa implementacja. Gdy ukończysz pracę, zastąp element zastępczy i usuń znacznik w tej samej zmianie, aby komentarz nigdy nie kłamał o stanie kodu.

# --instructions--

Wykonaj pracę opisaną przez komentarz `TODO` w `isEven`, a następnie usuń komentarz, ponieważ praca jest gotowa

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func isEven(_ n: Int) -> Bool {
    // TODO: return true when n is even, false otherwise
    return false
}
```

# --asserts--

Komentarz `TODO` musi zostać usunięty

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`isEven(4)` musi zwrócić `true`

```swift
tryCatch(isEven(4) == true)
```

`isEven(7)` musi zwrócić `false`

```swift
tryCatch(isEven(7) == false)
```

`isEven(0)` musi zwrócić `true`

```swift
tryCatch(isEven(0) == true)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func isEven(_ n: Int) -> Bool {
    return n % 2 == 0
}
```
