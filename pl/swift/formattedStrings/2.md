---
language: swift
exerciseType: 1
---

# --description--

Drugim narzędziem jest `String(format:)`, które pochodzi z frameworka **Foundation**, więc plik musi zaczynać się od `import Foundation`.

Przyjmuje on **ciąg formatujący**, a po nim wartości do wstawienia. Wewnątrz ciągu formatującego **specyfikator** zaczynający się od `%` oznacza, gdzie trafia każda wartość i jak zostanie zapisana. Specyfikatorem dla liczby całkowitej jest `%d`:
```swift
import Foundation

let count = 7
let text = String(format: "Item %d", count)
print(text) // Item 7
```
`String(format:)` zwraca zwykły `String`, więc możesz go wypisać, zapisać lub zwrócić z funkcji.

# --instructions--

Napisz funkcję `label`, która przyjmuje liczbę całkowitą bez etykiety argumentu i zwraca tekst `Item ` wraz z liczbą, używając `String(format:)` z `%d`

Przykład wywołania funkcji:
```swift
print(label(7))
// prints Item 7
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func label() {

}
```

# --asserts--

Funkcja musi użyć `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`label(7)` musi zwrócić `"Item 7"`

```swift
tryCatch(label(7) == "Item 7")
```

`label(42)` musi zwrócić `"Item 42"`

```swift
tryCatch(label(42) == "Item 42")
```

`label(0)` musi zwrócić `"Item 0"`

```swift
tryCatch(label(0) == "Item 0")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func label(_ n: Int) -> String {
    return String(format: "Item %d", n)
}
```
