---
language: swift
exerciseType: 1
---

# --description--

Inną flagą jest `0`: zamiast spacji pole jest wypełniane zerami po lewej. W ten sposób otrzymujesz liczby jak `007` czy `00042`:
```swift
print(String(format: "%05d", 42))  // 00042
print(String(format: "%03d", 7))   // 007
print(String(format: "%03d", 1234)) // 1234
```
Tak jak przy spacjach, wartość dłuższa niż szerokość nigdy nie jest obcinana.

# --instructions--

Napisz funkcję `ticket`, która przyjmuje liczbę całkowitą bez etykiety argumentu i zwraca `T-` wraz z liczbą dopełnioną zerami do szerokości `5`, używając `String(format:)`

Przykład wywołania funkcji:
```swift
print(ticket(42))
// prints T-00042
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func ticket() {

}
```

# --asserts--

Funkcja musi użyć `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ticket(42)` musi zwrócić `"T-00042"`

```swift
tryCatch(ticket(42) == "T-00042")
```

`ticket(7)` musi zwrócić `"T-00007"`

```swift
tryCatch(ticket(7) == "T-00007")
```

`ticket(12345)` musi zwrócić `"T-12345"`

```swift
tryCatch(ticket(12345) == "T-12345")
```

`ticket(123456)` musi zwrócić `"T-123456"`

```swift
tryCatch(ticket(123456) == "T-123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func ticket(_ n: Int) -> String {
    return String(format: "T-%05d", n)
}
```
