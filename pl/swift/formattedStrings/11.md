---
language: swift
exerciseType: 1
---

# --description--

Ponieważ `%` rozpoczyna specyfikator, dosłowny znak procenta musi być zapisany jako `%%`. Tak formatujesz wartość procentową:
```swift
let ratio = 0.4567
print(String(format: "%.1f%%", ratio * 100)) // 45.7%
```
Najpierw pomnóż stosunek przez `100`, a potem wybierz precyzję: `%.0f%%` daje `46%`, `%.1f%%` daje `45.7%`.

# --instructions--

Napisz funkcję `percent`, która przyjmuje część i całość (obie `Double`, bez etykiet argumentów) i zwraca część jako procent całości z jednym miejscem dziesiętnym, zakończony `%`

Przykład wywołania funkcji:
```swift
print(percent(9, 20))
// prints 45.0%
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func percent() {

}
```

# --asserts--

Ciąg formatujący musi kończyć się `%%`

```json
{
  "regex": "%%",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`percent(9, 20)` musi zwrócić `"45.0%"`

```swift
tryCatch(percent(9, 20) == "45.0%")
```

`percent(1, 3)` musi zwrócić `"33.3%"`

```swift
tryCatch(percent(1, 3) == "33.3%")
```

`percent(2, 2)` musi zwrócić `"100.0%"`

```swift
tryCatch(percent(2, 2) == "100.0%")
```

`percent(0, 5)` musi zwrócić `"0.0%"`

```swift
tryCatch(percent(0, 5) == "0.0%")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func percent(_ part: Double, _ total: Double) -> String {
    return String(format: "%.1f%%", part / total * 100)
}
```
