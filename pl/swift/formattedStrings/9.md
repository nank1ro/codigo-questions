---
language: swift
exerciseType: 1
---

# --description--

Aby wstawić `String` do ciągu formatującego, użyj specyfikatora `%@`:
```swift
let name = "Ada"
let age = 36
print(String(format: "%@ is %d years old", name, age)) // Ada is 36 years old
```
`%@` przyjmuje bezpośrednio `String` ze Swifte. Nie używaj `%s` z ciągiem znaków Swifta: ten specyfikator oczekuje ciągu znaków z języka C i wypisuje śmieci lub powoduje awarię.

# --instructions--

Napisz funkcję `greeting`, która przyjmuje imię (`String`) i wiek (`Int`), oba bez etykiet argumentów, i zwraca `<name> is <age> years old`, używając `String(format:)` z `%@` i `%d`

Przykład wywołania funkcji:
```swift
print(greeting("Ada", 36))
// prints Ada is 36 years old
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func greeting() {

}
```

# --asserts--

Funkcja musi użyć `String(format:` z `%@`

```json
{
  "regex": "String\\(format:[^\\n]*%@",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greeting("Ada", 36)` musi zwrócić `"Ada is 36 years old"`

```swift
tryCatch(greeting("Ada", 36) == "Ada is 36 years old")
```

`greeting("Linus", 54)` musi zwrócić `"Linus is 54 years old"`

```swift
tryCatch(greeting("Linus", 54) == "Linus is 54 years old")
```

`greeting("Bo", 7)` musi zwrócić `"Bo is 7 years old"`

```swift
tryCatch(greeting("Bo", 7) == "Bo is 7 years old")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greeting(_ name: String, _ age: Int) -> String {
    return String(format: "%@ is %d years old", name, age)
}
```
