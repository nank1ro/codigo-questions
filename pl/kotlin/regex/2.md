---
language: kotlin
exerciseType: 1
---

# --description--

`matches` jest często zbyt surowe: zwykle chcesz tylko wiedzieć, czy wzorzec pojawia się **gdziekolwiek** w tekście. Do tego służy `containsMatchIn`:
```kotlin
val digits = Regex("\\d+")
println(digits.matches("order 42"))          // false, the whole string is not digits
println(digits.containsMatchIn("order 42"))  // true, "42" is in there
```
Obok `\d` są jeszcze dwa skróty, których będziesz używać bez przerwy: `\w` to znak słowa (litera, cyfra lub `_`), a `\s` to znak białej spacji. Każdy z nich można powtórzyć za pomocą **kwantyfikatora**:
- `+` jeden lub więcej
- `*` zero lub więcej
- `?` zero lub jeden
- `{3}` dokładnie trzy, `{2,4}` od dwóch do czterech

Podwajanie każdego ukośnika wstecznego jest męczące, więc wzorce zapisuje się zwykle jako **surowe ciągi znaków** w potrójnych cudzysłowach, gdzie `\` jest zwykłym znakiem:
```kotlin
val digits = Regex("""\d+""") // same as Regex("\\d+")
```

# --instructions--

Napisz funkcję `hasNumber(text: String): Boolean`, która zwraca `true`, gdy `text` zawiera co najmniej jedną cyfrę, używając `containsMatchIn`.

Przykład: `hasNumber("room 12")` zwraca `true`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`hasNumber("room 12")` musi zwrócić `true`

```kotlin
    tryCatch(hasNumber("room 12") == true)
```

`hasNumber("9lives")` musi zwrócić `true`

```kotlin
    tryCatch(hasNumber("9lives") == true)
```

`hasNumber("no digits here")` musi zwrócić `false`

```kotlin
    tryCatch(hasNumber("no digits here") == false)
```

`hasNumber("")` musi zwrócić `false`

```kotlin
    tryCatch(hasNumber("") == false)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun hasNumber(text: String): Boolean {
    return Regex("""\d""").containsMatchIn(text)
}
```
