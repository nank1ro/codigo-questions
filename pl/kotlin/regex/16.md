---
language: kotlin
exerciseType: 1
---

# --description--

Realistyczny wzorzec zwykle miesza wszystko naraz: grupy, aby zachować potrzebne części, `\.` z ucieczką dla dosłownych kropek oraz lambdę zastępującą, aby odbudować wokół nich tekst.
```kotlin
val email = Regex("""(\w+)@(\w+\.\w+)""")
println(email.replace("ping ann@mail.com now") { m -> "<" + m.groupValues[2] + ">" })
// ping <mail.com> now
```
Trzymaj wzorce tak proste, jak pozwala na to zadanie: wzorzec próbujący opisać każdy poprawny adres e-mail jest nieczytelny, podczas gdy `\w+@\w+\.\w+` w zupełności wystarczy, aby znaleźć adresy w zdaniu.

# --instructions--

Napisz funkcję `redactEmails(text: String): String`, która zastępuje nazwę przed `@` każdego adresu ciągiem `***`, zachowując domenę. Adres to ciąg znaków słowa, `@`, ciąg znaków słowa, kropka i ciąg znaków słowa.

Przykład: `redactEmails("write to ann@mail.com")` zwraca `write to ***@mail.com`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`redactEmails("write to ann@mail.com")` musi zwrócić `write to ***@mail.com`

```kotlin
    tryCatch(redactEmails("write to ann@mail.com") == "write to ***@mail.com")
```

`redactEmails("bob@site.org and cy@site.org")` musi ukryć oba adresy

```kotlin
    tryCatch(redactEmails("bob@site.org and cy@site.org") == "***@site.org and ***@site.org")
```

`redactEmails("no address here")` musi zwrócić tekst bez zmian

```kotlin
    tryCatch(redactEmails("no address here") == "no address here")
```

`redactEmails("")` musi zwrócić pusty ciąg znaków

```kotlin
    tryCatch(redactEmails("") == "")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun redactEmails(text: String): String {
    return Regex("""(\w+)@(\w+\.\w+)""").replace(text) { m -> "***@" + m.groupValues[2] }
}
```
