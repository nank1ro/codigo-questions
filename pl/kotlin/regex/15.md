---
language: kotlin
exerciseType: 4
---

# --description--

Ponieważ `findAll` daje sekwencję, znane ci już metody agregujące działają także na dopasowaniach: `sumOf`, `maxOfOrNull`, `filter`, `sortedBy`. Wydobycie liczb ze swobodnego tekstu to zadanie dwuetapowe: dopasuj je, a potem zamień tekst na liczbę.
```kotlin
val total = Regex("""\d+""").findAll("2 apples, 3 pears").sumOf { it.value.toInt() }
println(total) // 5
```

# --instructions--

Czy potrafisz uporządkować te elementy tak, aby wypisać `total: 17`, sumując każdą liczbę w tekście?

# --answers--

```kotlin
val text = "ann scored 8 and bob scored 9"
```

```kotlin
val numbers = Regex("""\d+""").findAll(text)
```

```kotlin
val total = numbers.sumOf { it.value.toInt() }
```

```kotlin
println("total: $total")
```

# --solutions--

```kotlin
val text = "ann scored 8 and bob scored 9"
val numbers = Regex("""\d+""").findAll(text)
val total = numbers.sumOf { it.value.toInt() }
println("total: $total")
```

# --output--

total: 17
