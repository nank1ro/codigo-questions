---
language: kotlin
exerciseType: 1
---

# --description--

Pisanie `if (...) throw IllegalArgumentException(...)` przy każdym argumencie robi się uciążliwe, więc Kotlin dostarcza dwa skróty, które czyta się jak zwykłe zdania:

```kotlin
require(n >= 0) { "n must not be negative" }   // zgłasza IllegalArgumentException
check(started) { "not started" }               // zgłasza IllegalStateException
```
Obie funkcje przyjmują warunek i blok tworzący komunikat, i obie zgłaszają wyjątek **gdy warunek jest fałszywy**. Jedyną różnicą jest typ wyjątku, a ta różnica jest wiadomością dla czytelnika:

* `require` pilnuje **argumentów** przekazanych przez wywołującego i kończy się niepowodzeniem z `IllegalArgumentException`.
* `check` pilnuje **stanu** obiektu lub programu i kończy się niepowodzeniem z `IllegalStateException`.

Blok jest obliczany tylko wtedy, gdy warunek zawiedzie, więc budowanie komunikatu nic nie kosztuje na szczęśliwej ścieżce.

# --instructions--

Napisz dwie funkcje.

`square(n: Int): Int` musi użyć `require` z komunikatem `n must not be negative`, aby odrzucić ujemne `n`, a następnie zwrócić `n * n`.

`finish(started: Boolean): String` musi użyć `check` z komunikatem `not started`, aby odrzucić `false`, a następnie zwrócić `done`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --seed--

```kotlin
fun square(n: Int): Int {
    
}

fun finish(started: Boolean): String {
    
}
```

# --asserts--

`square(4)` must return `16`

```kotlin
    tryCatch(square(4) == 16)
```

`square(-1)` must throw an `IllegalArgumentException` saying `n must not be negative`

```kotlin
    var argMessage = ""
    try { square(-1) } catch (e: IllegalArgumentException) { argMessage = e.message ?: "" }
    tryCatch(argMessage == "n must not be negative")
```

`finish(true)` must return `done`

```kotlin
    tryCatch(finish(true) == "done")
```

`finish(false)` must throw an `IllegalStateException` saying `not started`

```kotlin
    var stateMessage = ""
    try { finish(false) } catch (e: IllegalStateException) { stateMessage = e.message ?: "" }
    tryCatch(stateMessage == "not started")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun square(n: Int): Int {
    require(n >= 0) { "n must not be negative" }
    return n * n
}

fun finish(started: Boolean): String {
    check(started) { "not started" }
    return "done"
}
```
