---
language: kotlin
exerciseType: 2
---

# --description--

Ponieważ `try` jest wyrażeniem, można go użyć wszędzie tam, gdzie oczekiwana jest wartość — także jako całe ciało funkcji zapisanej za pomocą `=`:
```kotlin
fun length(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    -1
}
```
Oba bloki muszą zwracać wartość tego samego typu, tutaj `Int`. Zapisz wartość zastępczą jako ostatnie wyrażenie bloku `catch`; w żadnym z bloków nie ma `return`.

# --instructions--

Uzupełnij funkcję tak, aby zwracała liczbę zawartą w `text`, a `0`, gdy tekst nie jest liczbą.

# --seed--

```kotlin
fun size(text: String): Int = [/] {
    text.toInt()
} [/] (e: NumberFormatException) {
    [/]
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --answers--

- try
- catch
- 0
- finally
- throw
- 1

# --solutions--

```kotlin
fun size(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    0
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --output--

15
0
