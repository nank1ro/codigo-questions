---
language: kotlin
exerciseType: 2
---

# --description--

Because `try` is an expression, it can be used anywhere a value is expected — including as the whole body of a function written with `=`:
```kotlin
fun length(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    -1
}
```
Both blocks must produce a value of the same type, here `Int`. Write the fallback as the last expression of the `catch` block; there is no `return` inside either block.

# --instructions--

Complete the function so that it gives back the number in `text`, and `0` when the text is not a number.

# --seed--

```kotlin
fun size(text: String): Int = [/] {
    text.toInt()
} [/] (e: NumberFormatException) {
    [/]
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --answers--

- try
- catch
- 0
- finally
- throw
- 1

# --solutions--

```kotlin
fun size(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    0
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --output--

15
0
