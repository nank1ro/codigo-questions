---
language: kotlin
exerciseType: 2
---

# --description--

Na końcu można dodać blok `finally`. Wykonuje się on **niezależnie od tego, co się stanie**: po udanym `try`, po tym, jak `catch` naprawi sytuację, a nawet gdy wyjątek w ogóle nie zostanie przechwycony.

```kotlin
try {
    println("reading")
    println("abc".toInt())
} catch (e: NumberFormatException) {
    println("bad number")
} finally {
    println("closing")
}
```
```
reading
bad number
closing
```
To czyni go odpowiednim miejscem na sprzątanie, które nie może zostać pominięte, na przykład zamknięcie pliku. `try` wymaga co najmniej `catch` albo `finally`, ale może mieć oba.

# --instructions--

Uzupełnij kod tak, aby nieudana konwersja została przechwycona, a linia sprzątająca zawsze się wykonała.

# --seed--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } [/] (e: NumberFormatException) {
        println("bad number")
    } [/] {
        println("closing")
    }
}
```

# --answers--

- catch
- finally
- try
- throw
- else
- do

# --solutions--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } catch (e: NumberFormatException) {
        println("bad number")
    } finally {
        println("closing")
    }
}
```

# --output--

bad number
closing
