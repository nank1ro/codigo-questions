---
language: kotlin
exerciseType: 1
---

# --description--

W przypadku `reduce` kształt wyniku zależy od lambdy. Działa każda operacja łącząca dwie wartości: suma, iloczyn, zachowanie większej z nich.
```kotlin
val numbers = listOf(3, 9, 4)
println(numbers.reduce { acc, n -> if (n > acc) n else acc }) // 9
```
Zauważ, że `reduce` rzuca wyjątek na pustej liście, ponieważ nie ma pierwszego elementu, od którego można zacząć.

# --instructions--

Napisz funkcję `product(numbers: List<Int>): Int`, która zwraca iloczyn wszystkich liczb na liście, używając `reduce`. Lista nigdy nie jest pusta.

Przykład: `product(listOf(2, 3, 4))` zwraca `24`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`product(listOf(2, 3, 4))` musi zwracać `24`

```kotlin
    tryCatch(product(listOf(2, 3, 4)) == 24)
```

`product(listOf(5))` musi zwracać `5`

```kotlin
    tryCatch(product(listOf(5)) == 5)
```

`product(listOf(2, -3, 5))` musi zwracać `-30`

```kotlin
    tryCatch(product(listOf(2, -3, 5)) == -30)
```

`product(listOf(7, 0, 9))` musi zwracać `0`

```kotlin
    tryCatch(product(listOf(7, 0, 9)) == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun product(numbers: List<Int>): Int {
    return numbers.reduce { acc, n -> acc * n }
}
```
