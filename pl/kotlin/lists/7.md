---
language: kotlin
exerciseType: 2
---

# --description--

Modyfikowalna lista nie musi mieć stałej długości.
Możesz dodawać elementy na końcu listy w dowolnym momencie!
Aby dodać element do modyfikowalnej listy, używamy funkcji `add` lub skrótu `+=`:
```kotlin
val letters = mutableListOf("a", "b")
letters.add("c")
println(letters)
// prints [a, b, c]
```

# --instructions--

Uzupełnij kod, aby dodać trzy kolejne elementy do listy `numbers`, w następującej kolejności: `6`, potem `8` i na końcu `9`

# --seed--

```kotlin
val numbers = mutableListOf(1, 3, 9)
numbers.[/]([/])
numbers.add([/])
[/].add(9)
println(numbers)
```

# --answers--

- append
- add
- 6
- 8
- numbers

# --solutions--

```kotlin
val numbers = mutableListOf(1, 3, 9)
numbers.add(6)
numbers.add(8)
numbers.add(9)
println(numbers)
```

# --output--

[1, 3, 9, 6, 8, 9]
