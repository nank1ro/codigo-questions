---
language: kotlin
exerciseType: 4
---

# --instructions--

Uporządkuj elementy, aby wypisać `Slow down`. Zadeklaruj najpierw enum, potem funkcję `action`, a na końcu `main`, zachowując gałęzie `when` w tej samej kolejności co wartości enuma.

# --answers--

```kotlin
enum class Light {
    RED, YELLOW, GREEN
}
```

```kotlin
fun action(light: Light): String = when (light) {
```

```kotlin
    Light.RED -> "Stop"
```

```kotlin
    Light.YELLOW -> "Slow down"
```

```kotlin
    Light.GREEN -> "Go"
```

```kotlin
}
```

```kotlin
fun main() {
    println(action(Light.YELLOW))
}
```

# --solutions--

```kotlin
enum class Light {
    RED, YELLOW, GREEN
}
fun action(light: Light): String = when (light) {
    Light.RED -> "Stop"
    Light.YELLOW -> "Slow down"
    Light.GREEN -> "Go"
}
fun main() {
    println(action(Light.YELLOW))
}
```

# --output--

Slow down
