---
language: kotlin
exerciseType: 1
---

# --description--

Aby przejść od `String` z powrotem do wartości enum, użyj funkcji `valueOf`. Wyszukuje ona wartość, której `name` dokładnie pasuje:
```kotlin
val direction = Direction.valueOf("EAST")
println(direction == Direction.EAST) // true
```
Dopasowanie rozróżnia wielkość liter: `Direction.valueOf("east")` rzuca `IllegalArgumentException`, ponieważ żadna wartość nie ma takiej nazwy.

# --instructions--

Zadeklaruj klasę enum `Status` z wartościami `ACTIVE` i `INACTIVE`. Następnie napisz funkcję `statusOf(text: String)`, która zwraca `Status` pasujący do `text`, niezależnie od wielkości liter (tak aby `"active"`, `"Active"` i `"ACTIVE"` dawały `Status.ACTIVE`).

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`statusOf("ACTIVE")` musi zwrócić `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("ACTIVE") == Status.ACTIVE)
```

`statusOf("inactive")` musi zwrócić `Status.INACTIVE`

```kotlin
    tryCatch(statusOf("inactive") == Status.INACTIVE)
```

`statusOf("Active")` musi zwrócić `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("Active") == Status.ACTIVE)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
enum class Status {
    ACTIVE, INACTIVE
}

fun statusOf(text: String): Status {
    return Status.valueOf(text.uppercase())
}
```
