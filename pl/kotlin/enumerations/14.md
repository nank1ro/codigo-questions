---
language: kotlin
exerciseType: 4
---

# --instructions--

Uporządkuj elementy, aby zadeklarować enum `Planet` i wypisać dla każdej wartości jej nazwę oraz liczbę księżyców. Zadeklaruj najpierw enum, potem `main`. Program musi wypisać:

```
MERCURY: 0
EARTH: 1
MARS: 2
```

# --answers--

```kotlin
enum class Planet(val moons: Int) {
```

```kotlin
    MERCURY(0),
```

```kotlin
    EARTH(1),
```

```kotlin
    MARS(2)
```

```kotlin
}
```

```kotlin
fun main() {
    for (planet in Planet.entries) {
```

```kotlin
        println("${planet.name}: ${planet.moons}")
```

```kotlin
    }
}
```

# --solutions--

```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
fun main() {
    for (planet in Planet.entries) {
        println("${planet.name}: ${planet.moons}")
    }
}
```

# --output--

MERCURY: 0
EARTH: 1
MARS: 2
