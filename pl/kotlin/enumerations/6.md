---
language: kotlin
exerciseType: 2
---

# --description--

Każda klasa enum udostępnia właściwość `entries`: listę wszystkich jej wartości w kolejności deklaracji. Jest to przydatne przy iteracji:
```kotlin
for (direction in Direction.entries) {
    println(direction.name)
}
// NORTH
// SOUTH
// EAST
// WEST
```
Ponieważ `entries` jest listą, obsługuje też `size` oraz indeksowanie, np. `Direction.entries[0]` to `NORTH`.

Starszy kod używa zamiast tego funkcji `values()`, która zwraca tablicę; `entries` jest zalecanym wyborem od Kotlina 1.9.

# --instructions--

Uzupełnij pętlę, aby wypisywała ordinal i nazwę każdej wartości `Size`, oddzielone spacją

# --seed--

```kotlin
enum class Size {
    SMALL, MEDIUM, LARGE
}

fun main() {
    for (size in Size.[/]) {
        println("${size.[/]} ${size.[/]}")
    }
}
```

# --answers--

- entries
- cases
- list
- ordinal
- name
- index
- value

# --solutions--

```kotlin
enum class Size {
    SMALL, MEDIUM, LARGE
}

fun main() {
    for (size in Size.entries) {
        println("${size.ordinal} ${size.name}")
    }
}
```

# --output--

0 SMALL
1 MEDIUM
2 LARGE
