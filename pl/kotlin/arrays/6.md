---
language: kotlin
exerciseType: 2
---

# --description--

Wypisanie tablicy bezpośrednio nie pokazuje jej elementów, wypisuje coś w rodzaju `[Ljava.lang.String;@1b6d3586`.
Użyj `joinToString`, aby zbudować czytelny ciąg znaków, opcjonalnie z niestandardowym separatorem (domyślny to `", "`), albo `contentToString`, aby uzyskać elementy w nawiasach kwadratowych:
```kotlin
val nums = intArrayOf(1, 2, 3)
println(nums.joinToString())      // 1, 2, 3
println(nums.joinToString("-"))   // 1-2-3
println(nums.contentToString())   // [1, 2, 3]
```

# --instructions--

Uzupełnij kod, aby wypisać owoce rozdzielone `" - "`, a następnie całą tablicę w nawiasach kwadratowych

# --seed--

```kotlin
val fruits = arrayOf("apple", "kiwi", "plum")
println(fruits.[/]([/]))
println(fruits.[/]())
```

# --answers--

- joinToString
- contentToString
- toString
- join
- " - "
- "-"

# --solutions--

```kotlin
val fruits = arrayOf("apple", "kiwi", "plum")
println(fruits.joinToString(" - "))
println(fruits.contentToString())
```

# --output--

apple - kiwi - plum
[apple, kiwi, plum]
