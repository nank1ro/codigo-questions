---
language: kotlin
exerciseType: 2
---

# --description--

`uppercase()` zwraca kopię ciągu znaków ze wszystkimi literami zapisanymi wielkimi literami, `lowercase()` robi coś przeciwnego.
Oryginalny ciąg znaków nie zostaje zmodyfikowany:
```kotlin
val name = "Kotlin"
println(name.uppercase()) // KOTLIN
println(name.lowercase()) // kotlin
println(name)             // Kotlin
```

# --instructions--

Uzupełnij kod, aby wypisać `word` wielkimi literami, a następnie `shout` małymi literami

# --seed--

```kotlin
val word = "quiet"
val shout = "LOUD"
println(word.[/]())
println(shout.[/]())
```

# --answers--

- uppercase
- lowercase
- upper
- lower
- titlecase

# --solutions--

```kotlin
val word = "quiet"
val shout = "LOUD"
println(word.uppercase())
println(shout.lowercase())
```

# --output--

QUIET
loud
