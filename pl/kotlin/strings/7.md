---
language: kotlin
exerciseType: 2
---

# --description--

`split` dzieli ciąg znaków na `List` fragmentów wokół separatora, natomiast `joinToString` robi coś przeciwnego: łączy elementy kolekcji w jeden ciąg znaków z wybranym przez ciebie separatorem:
```kotlin
val csv = "a,b,c"
val parts = csv.split(",")
println(parts)                  // [a, b, c]
println(parts.joinToString("/")) // a/b/c
```

# --instructions--

Uzupełnij kod, aby podzielić `csv` po przecinku i wypisać kolory oddzielone `" - "`

# --seed--

```kotlin
val csv = "red,green,blue"
val colors = csv.[/](",")
println(colors.[/](" - "))
```

# --answers--

- split
- joinToString
- join
- separate
- substring

# --solutions--

```kotlin
val csv = "red,green,blue"
val colors = csv.split(",")
println(colors.joinToString(" - "))
```

# --output--

red - green - blue
