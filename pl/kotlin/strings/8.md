---
language: kotlin
exerciseType: 4
---

# --description--

Ponieważ `split` zwraca `List`, możesz przechodzić po jej elementach jak po każdej innej liście:
```kotlin
for (part in "a-b".split("-")) {
    println(part)
}
// prints a, then b
```

# --instructions--

Czy potrafisz uporządkować te linie tak, aby wypisać każde słowo z `sentence` wielkimi literami, po jednym w linii?

# --answers--

```kotlin
val sentence = "the quick brown fox"
```

```kotlin
val words = sentence.split(" ")
```

```kotlin
for (word in words) {
```

```kotlin
    println(word.uppercase())
```

```kotlin
}
```

# --solutions--

```kotlin
val sentence = "the quick brown fox"
val words = sentence.split(" ")
for (word in words) {
    println(word.uppercase())
}
```

# --output--

THE
QUICK
BROWN
FOX
