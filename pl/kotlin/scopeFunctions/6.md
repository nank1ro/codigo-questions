---
language: kotlin
exerciseType: 1
---

# --description--

`apply` nie jest ograniczone do obiektów, które właśnie utworzono: działa na każdym obiekcie, a ponieważ oddaje ten obiekt z powrotem, całe wyrażenie można użyć wszędzie tam, gdzie oczekiwany jest obiekt.
```kotlin
val box = Box()
box.apply { label = "tools" }        // zmienia box i go zwraca
println(listOf(Box().apply { label = "nails" }).size) // 1
```
Blok jest zwykłym blokiem kodu, więc może zawierać dowolną liczbę instrukcji.

# --instructions--

Utwórz klasę `Profile` z dwiema właściwościami: `var name: String` z wartością początkową `""` oraz `var age: Int` z wartością początkową `0`.

Następnie napisz funkcję `makeProfile(n: String, a: Int): Profile`, która tworzy `Profile()` i używa `apply`, aby ustawić `name` na `n` oraz `age` na `a`, zwracając skonfigurowany obiekt.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

Nowy `Profile()` musi zaczynać z pustą nazwą i wiekiem `0`

```kotlin
    val fresh = Profile()
    tryCatch(fresh.name == "" && fresh.age == 0)
```

`makeProfile("Ada", 36)` musi mieć `name` równe `Ada`

```kotlin
    tryCatch(makeProfile("Ada", 36).name == "Ada")
```

`makeProfile("Ada", 36)` musi mieć `age` równe `36`

```kotlin
    tryCatch(makeProfile("Ada", 36).age == 36)
```

`makeProfile("Bob", 7)` musi mieć `name` równe `Bob` i `age` równe `7`

```kotlin
    val bob = makeProfile("Bob", 7)
    tryCatch(bob.name == "Bob" && bob.age == 7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Profile {
    var name: String = ""
    var age: Int = 0
}

fun makeProfile(n: String, a: Int): Profile {
    return Profile().apply {
        name = n
        age = a
    }
}
```
