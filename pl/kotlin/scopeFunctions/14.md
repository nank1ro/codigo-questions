---
language: kotlin
exerciseType: 2
---

# --instructions--

Uzupełnij kod tak, aby opis psa został zbudowany wewnątrz bloku `with`, a program go wypisał.

# --seed--

```kotlin
class Dog(val name: String, val age: Int)

fun main() {
    val dog = Dog("Rex", 4)
    val info = [/](dog) {
        name + " is " + [/]
    }
    println(info)
}
```

# --answers--

- with
- age
- run
- also
- apply
- name

# --solutions--

```kotlin
class Dog(val name: String, val age: Int)

fun main() {
    val dog = Dog("Rex", 4)
    val info = with(dog) {
        name + " is " + age
    }
    println(info)
}
```

# --output--

Rex is 4
