---
language: kotlin
exerciseType: 2
---

# --description--

Aby przekonwertować `Set` na `List`, możemy użyć funkcji `toList()`

# --instructions--

Uzupełnij kod, aby przekonwertować `Set` na `List`

# --seed--

```kotlin
val uniqueNumbers = setOf(1, 2, 3, 3)
var numbers = [/][/][/]
numbers += 4
println(numbers)
```

# --answers--

- uniqueNumbers
- .
- toList()
- toList
- distinct()
- asList()

# --solutions--

```kotlin
val uniqueNumbers = setOf(1, 2, 3, 3)
var numbers = uniqueNumbers.toList()
numbers += 4
println(numbers)
```

# --output--

[1, 2, 3, 4]
