---
language: kotlin
exerciseType: 2
---

# --description--

Przeanalizujmy ten fragment kodu.
```kotlin
var counter = 0 // [1]
while (counter < 100) { // [2]
    counter += 10 // [3]
    println(counter)
}
```
- __[1]__: Inicjalizujemy zmienną `counter` wartością __0__.
- __[2]__: Wyrażenie warunkowe dla _while_ mówi: "powtarzaj instrukcje w treści tak długo, jak counter jest mniejszy niż _100_".
- __[3]__: Operator `+=` dodaje _10_ do `counter` i przypisuje wynik do `counter` w jednej operacji.

Wynik powyższego kodu to _10_, _20_, _30_, _40_, _50_, _60_, _70_, _80_, _90_, _100_

# --instructions--

Uzupełnij puste miejsca pętlą `while`, aby powtórzyć blok kodu i wyświetlić pierwsze pięć wielokrotności liczby _5_.

# --seed--

```kotlin
var counter = 1
[/] (counter[/]5) {
    println(counter[/]5)
    counter[/]
}
```

# --answers--

- while
- "while"
- loop
-  <= 
-  < 
- for
-  * 
-  ** 
- ++


# --solutions--

```kotlin
var counter = 1
while (counter <= 5) {
    println(counter * 5)
    counter++
}
```

# --output--

5
10
15
20
25
