---
language: kotlin
exerciseType: 3
---

# --instructions--

Co dzieje się z tym programem?

```kotlin
interface Movable {
    fun move(): String = "moving"
}

class Robot : Movable

fun main() {
    println(Robot().move())
}
```

# --answers--

- Wypisuje `moving`, ponieważ `Robot` dziedziczy domyślną implementację
- Nie kompiluje się: `Robot` musi nadpisać `move()`
- Nie kompiluje się: interfejs nie może zawierać ciała funkcji
- Wypisuje `null`

# --solutions--

- Wypisuje `moving`, ponieważ `Robot` dziedziczy domyślną implementację

