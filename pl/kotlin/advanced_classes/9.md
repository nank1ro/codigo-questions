---
language: kotlin
exerciseType: 1
---

# --description--

`companion object` to singleton należący do klasy. Poza stałymi jego naturalnym zadaniem jest przechowywanie **funkcji fabrykujących**: funkcji, które sprawdzają lub przekształcają dane wejściowe przed zbudowaniem instancji i mogą zwrócić `null`, gdy dane wejściowe nie mają sensu.

Oznaczenie konstruktora jako `private` zmusza każdego wywołującego do przejścia przez fabrykę:

```kotlin
class Age private constructor(val years: Int) {
    companion object {
        fun of(years: Int): Age? = if (years >= 0) Age(years) else null
    }
}

println(Age.of(30)?.years) // 30
println(Age.of(-1))        // null
```

Companion wywołuje się na nazwie klasy, `Age.of(...)`, i ma dostęp do prywatnego konstruktora, ponieważ znajduje się wewnątrz klasy.

# --instructions--

Zadeklaruj klasę `Temperature` z **prywatnym** konstruktorem przyjmującym właściwość `val` `celsius` typu `Int`.

Dodaj companion object z funkcją fabrykującą `fromCelsius(value: Int): Temperature?`, która zwraca `Temperature`, gdy `value` wynosi co najmniej `-273`, a w przeciwnym razie `null`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`Temperature.fromCelsius(20)` must hold `20` degrees

```kotlin
    tryCatch(Temperature.fromCelsius(20)?.celsius == 20)
```

`Temperature.fromCelsius(-273)` must be accepted

```kotlin
    tryCatch(Temperature.fromCelsius(-273)?.celsius == -273)
```

`Temperature.fromCelsius(-300)` must return `null`

```kotlin
    tryCatch(Temperature.fromCelsius(-300) == null)
```

`Temperature.fromCelsius(0)` must be accepted

```kotlin
    tryCatch(Temperature.fromCelsius(0)?.celsius == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Temperature private constructor(val celsius: Int) {
    companion object {
        fun fromCelsius(value: Int): Temperature? {
            return if (value >= -273) Temperature(value) else null
        }
    }
}
```
