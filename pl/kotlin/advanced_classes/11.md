---
language: kotlin
exerciseType: 1
---

# --description--

`interface` wymienia, co dany typ potrafi. Jego składowe są domyślnie abstrakcyjne, ale interfejs może też dostarczyć **domyślną implementację**, czyli ciało, które każda implementująca klasa dziedziczy za darmo i może nadpisać:

```kotlin
interface Greeter {
    val name: String              // abstrakcyjna, klasa musi ją dostarczyć
    fun greet(): String = "Hi, $name"  // domyślna implementacja
}

class Person(override val name: String) : Greeter

class Robot(override val name: String) : Greeter {
    override fun greet(): String = "BEEP $name"
}

println(Person("Ann").greet()) // Hi, Ann
println(Robot("R2").greet())   // BEEP R2
```

Interfejs nie może przechowywać stanu (nie ma pól zaplecza), więc właściwość abstrakcyjną musi zaimplementować klasa, zwykle przez `override val` w konstruktorze. W przeciwieństwie do klasy, typ może implementować dowolnie wiele interfejsów.

# --instructions--

Zadeklaruj interfejs `Greeter` z abstrakcyjną właściwością `val` `name` typu `String` oraz metodą `greet(): String`, której domyślna implementacja zwraca `"Hello, Ann!"` dla `name` równego `"Ann"`.

Następnie zadeklaruj klasę `Person`, która implementuje `Greeter` i przyjmuje `name` w konstruktorze, oraz klasę `Pirate`, która również implementuje `Greeter`, ale nadpisuje `greet()`, aby zwracała `"Ahoy, Bo!"` dla `name` równego `"Bo"`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`Person("Ann").greet()` must return `"Hello, Ann!"`

```kotlin
    tryCatch(Person("Ann").greet() == "Hello, Ann!")
```

`Person("Cy").name` must return `"Cy"`

```kotlin
    tryCatch(Person("Cy").name == "Cy")
```

`Pirate("Bo").greet()` must return `"Ahoy, Bo!"`

```kotlin
    tryCatch(Pirate("Bo").greet() == "Ahoy, Bo!")
```

Both classes must be usable as a `Greeter`

```kotlin
    val greeters: List<Greeter> = listOf(Person("Ann"), Pirate("Bo"))
    tryCatch(greeters.map { it.greet() } == listOf("Hello, Ann!", "Ahoy, Bo!"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
interface Greeter {
    val name: String
    fun greet(): String = "Hello, $name!"
}

class Person(override val name: String) : Greeter

class Pirate(override val name: String) : Greeter {
    override fun greet(): String = "Ahoy, $name!"
}
```
