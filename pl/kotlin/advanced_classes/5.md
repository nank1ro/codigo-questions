---
language: kotlin
exerciseType: 1
---

# --description--

Klasa `sealed` opisuje **zamknięty** zbiór wariantów: dozwolone są tylko podklasy napisane w tym samym pakiecie i module, więc kompilator zna je wszystkie.

```kotlin
sealed class Shape
data class Circle(val radius: Int) : Shape()
data class Square(val side: Int) : Shape()
```

Korzyścią jest **wyczerpujące `when`**: gdy rozgałęziasz się po typie sealed i pokryjesz każdą podklasę, możesz pominąć gałąź `else`. Jeśli później dodasz nową podklasę, kompilator wskaże każde `when`, którego zapomniałeś zaktualizować, zamiast po cichu wejść w `else`.

```kotlin
fun name(shape: Shape): String = when (shape) {
    is Circle -> "circle"
    is Square -> "square"
}
```

Po `is Circle` wartość podlega smart castowi, więc `shape.radius` jest dostępne w tej gałęzi bez żadnego ręcznego rzutowania.

# --instructions--

Zadeklaruj klasę sealed `Shape` z dwiema podklasami będącymi data class: `Square`, z właściwością `val` `side` typu `Int`, oraz `Rectangle`, z właściwościami `val` `width` i `height` typu `Int`.

Następnie napisz funkcję `area(shape: Shape): Int`, która zwraca pole figury, używając wyczerpującego `when` bez gałęzi `else`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`area(Square(3))` must return `9`

```kotlin
    tryCatch(area(Square(3)) == 9)
```

`area(Rectangle(2, 5))` must return `10`

```kotlin
    tryCatch(area(Rectangle(2, 5)) == 10)
```

`area(Square(0))` must return `0`

```kotlin
    tryCatch(area(Square(0)) == 0)
```

`Square` and `Rectangle` must be subclasses of `Shape`

```kotlin
    val shapes: List<Shape> = listOf(Square(4), Rectangle(3, 3))
    tryCatch(shapes.map { area(it) } == listOf(16, 9))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
sealed class Shape
data class Square(val side: Int) : Shape()
data class Rectangle(val width: Int, val height: Int) : Shape()

fun area(shape: Shape): Int = when (shape) {
    is Square -> shape.side * shape.side
    is Rectangle -> shape.width * shape.height
}
```
