---
language: kotlin
exerciseType: 2
---

# --description--

Załóżmy, że mamy kolejny warunek do sprawdzenia, jak w tym przykładzie:
```kotlin
var num = 3
if (num == 2) {
    println("the number is 2")
} else if (num == 3) {
    println("the number is 3")
} else {
    println("do something else")
}
```
a wynik tego kodu to `the number is 3`.
Na początku sprawdzamy, czy liczba jest równa 2 – to jest fałsz.
Więc przechodzimy do drugiej instrukcji i sprawdzamy, czy `num` jest równe 3. Ponieważ to prawda, wykonujemy następujący blok kodu, wyświetlając `the number is 3`

# --instructions--

Utwórz instrukcję `else if` sprawdzającą, czy `time` jest równe `12` i w bloku kodu wyświetl ciąg `"It's midday"`

# --seed--

```kotlin
var time = 12
if (time < 12) {
    println("It's morning")
} [/]([/][/][/])[/]
    println("[/]")
} else {
    println("It's afternoon")
}
```

# --answers--

- It's midday
- 12
- else if 
-  < 
-  == 
- :
- ;
- time
-  {

# --solutions--

```kotlin
var time = 12
if (time < 12) {
    println("It's morning")
} else if (time == 12) {
    println("It's midday")
} else {
    println("It's afternoon")
}
```

# --output--

It's midday
