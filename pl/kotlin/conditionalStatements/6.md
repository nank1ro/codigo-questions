---
language: kotlin
exerciseType: 2
---

# --description--

Możemy też zagnieżdżać instrukcje warunkowe (`if`, `else if` lub `else`) wewnątrz innej instrukcji warunkowej, tworząc bardziej złożoną strukturę.
```kotlin
var num = 4
if (num < 3) {
    println("the number is lower than 3")
} else {
    if (num == 3) {
        println("the number is 3")
    } else if (num == 4) {
        println("the number is 4")
    } else {
        println("the number is greather than 4")
    }
}
```
a wynik tego kodu to `the number is 4`.

# --instructions--

Zmieńmy program dotyczący pory dnia, zagnieżdżając instrukcję warunkową wewnątrz instrukcji `else`.
Zagnieżdżona instrukcja warunkowa powinna sprawdzać, czy `time` jest *mniejsze* niż `18` i w bloku kodu wyświetlać `"It's afternoon"`.
Jeśli poprzedni warunek jest `false`, wyświetl `"It's evening"`

# --seed--

```kotlin
var time = 19
if (time > 0 && time < 12) {
    println("It's morning")
} else if (time == 12) {
    println("It's midday")
} else if (time == 0) {
    println("It's midnight")
} else {
    [/]([/][/][/]) {
        println("It's afternoon")
    } [/] {
        println("It's evening")
    }
}
```

# --answers--

- 6
- elif 
-  < 
-  == 
- :
- ;
- time
- else if:
- elseif:
- if 
- else

# --solutions--

```kotlin
var time = 19
if (time > 0 && time < 12) {
    println("It's morning")
} else if (time == 12) {
    println("It's midday")
} else if (time == 0) {
    println("It's midnight")
} else {
    if (time < 6) {
        println("It's afternoon")
    } else {
        println("It's evening")
    }
}
```

# --output--

It's evening
