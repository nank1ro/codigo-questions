---
language: kotlin
exerciseType: 2
---

# --description--

Funkcja z więcej niż jednym parametrem dostaje jeden znacznik `@param` dla każdego z nich, zapisany w tej samej kolejności co parametry:
```kotlin
/**
 * Returns the area of a rectangle.
 * @param width the horizontal side, in centimetres
 * @param height the vertical side, in centimetres
 * @return the product of [width] and [height]
 */
fun area(width: Int, height: Int): Int {
    return width * height
}
```
Znacznik wciąż jest tylko komentarzem: jeśli zmienisz nazwę parametru i zapomnisz o znaczniku, nic się nie zepsuje, ale dokumentacja zacznie kłamać. Aktualizuj KDoc razem z sygnaturą.

# --instructions--

Uzupełnij dokumentację `price`: pierwszy znacznik musi opisywać parametr `quantity`, drugi parametr `unitPrice`, a trzeci wartość zwracaną przez funkcję

# --seed--

```kotlin
/**
 * Returns the total price of an order.
 * [/] quantity how many items were ordered
 * [/] unitPrice the price of a single item
 * [/] the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --answers--

- @param
- @param
- @return
- @throws
- @property

# --solutions--

```kotlin
/**
 * Returns the total price of an order.
 * @param quantity how many items were ordered
 * @param unitPrice the price of a single item
 * @return the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --output--

15
