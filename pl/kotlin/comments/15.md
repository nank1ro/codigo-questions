---
language: kotlin
exerciseType: 1
---

# --description--

`FIXME` różni się od `TODO`: kod już istnieje, ale wiadomo, że jest błędny. Dobry `FIXME` mówi, na czym polega błąd i, jeśli to możliwe, podaje przykład, który go pokazuje, aby następna osoba mogła go szybko naprawić. Podobnie jak przy `TODO`, usuń znacznik po naprawieniu błędu, ale zostaw komentarz dokumentacyjny, który nadal jest prawdziwy.

# --instructions--

Napraw błąd opisany przez komentarz `FIXME` w `average` (dodaj nawiasy, aby suma była obliczana przed dzieleniem), a następnie usuń komentarz `FIXME`. Zostaw komentarz dokumentacyjny

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --seed--

```kotlin
/**
 * Returns the average of [a] and [b], rounded down.
 */
fun average(a: Int, b: Int): Int {
    // FIXME: divides before adding, so average(4, 6) gives 7 instead of 5
    return a + b / 2
}
```

# --asserts--

Komentarz `FIXME` musi zostać usunięty

```json
{
  "regex": "FIXME",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

Komentarz dokumentacyjny musi zostać zachowany

```json
{
  "regex": "Returns the average of",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`average(4, 6)` musi zwrócić `5`

```kotlin
    tryCatch(average(4, 6) == 5)
```

`average(10, 20)` musi zwrócić `15`

```kotlin
    tryCatch(average(10, 20) == 15)
```

`average(3, 4)` musi zwrócić `3`

```kotlin
    tryCatch(average(3, 4) == 3)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
/**
 * Returns the average of [a] and [b], rounded down.
 */
fun average(a: Int, b: Int): Int {
    return (a + b) / 2
}
```
