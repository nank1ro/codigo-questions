---
language: kotlin
exerciseType: 1
---

# --description--

W przeciwieństwie do `//`, który kończy się na końcu linii, komentarz `/*` kończy się dopiero przy `*/`. Jeśli zapomnisz go zamknąć, kompilator potraktuje cały dalszy kod jako część komentarza i zgłosi błąd:
```kotlin
val width = 10 /* w centymetrach
println(width) // nadal wewnątrz komentarza: błąd, komentarz nigdy się nie zamyka
```
Zarówno `//`, jak i `/* */` działają jako komentarze końcowe, ale przy `/*` zawsze upewnij się, że `*/` jest na miejscu.

# --instructions--

`twice` się nie kompiluje, ponieważ komentarz końcowy w pierwszej linii nigdy nie zostaje zamknięty i pochłania `return`. Zamknij komentarz na końcu tej samej linii

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --seed--

```kotlin
fun twice(n: Int): Int {
    val result = n * 2 /* twice the input
    return result
}
```

# --asserts--

Komentarz musi zostać zamknięty w tej samej linii

```json
{
  "regex": "/\\* twice the input\\s*\\*/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`twice(4)` musi zwrócić `8`

```kotlin
    tryCatch(twice(4) == 8)
```

`twice(-3)` musi zwrócić `-6`

```kotlin
    tryCatch(twice(-3) == -6)
```

`twice(0)` musi zwrócić `0`

```kotlin
    tryCatch(twice(0) == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun twice(n: Int): Int {
    val result = n * 2 /* twice the input */
    return result
}
```
