---
language: kotlin
exerciseType: 4
---

# --description--

Po podsumowaniu komentarz dokumentacyjny może opisać parametry i zwracaną wartość za pomocą **znaczników KDoc**, które zawsze zaczynają się od `@`:
```kotlin
/**
 * Returns the number of seconds in the given minutes.
 * @param minutes a whole number of minutes, never negative
 * @return [minutes] multiplied by sixty
 */
fun toSeconds(minutes: Int): Int {
    return minutes * 60
}
```
Po `@param` podaje się nazwę parametru, a następnie jego opis; jest jeden `@param` na każdy parametr. `@return` opisuje wartość zwracaną przez funkcję. Kolejność jest zawsze taka sama: najpierw podsumowanie, potem znaczniki `@param`, potem `@return`.

# --instructions--

Uporządkuj elementy, aby udokumentować `twice` podsumowaniem, potem znacznikiem `@param`, potem znacznikiem `@return`, wszystko w jednym komentarzu dokumentacyjnym umieszczonym bezpośrednio nad funkcją, a następnie wypisz `twice(21)`

# --answers--

```kotlin
/**
 * Returns the double of a number.
```

```kotlin
 * @param n the number to double
```

```kotlin
 * @return [n] multiplied by two
```

```kotlin
 */
```

```kotlin
fun twice(n: Int): Int {
    return n * 2
}
```

```kotlin
println(twice(21))
```

# --solutions--

```kotlin
/**
 * Returns the double of a number.
 * @param n the number to double
 * @return [n] multiplied by two
 */
fun twice(n: Int): Int {
    return n * 2
}
println(twice(21))
```

# --output--

42
