---
language: kotlin
exerciseType: 1
---

# --description--

Funkcje mogą mieć wiele parametrów różnych typów:
```kotlin
fun introduce(name: String, age: Int): String {
    return "My name is $name and I am $age years old."
}
println(introduce("Bob", 30))
// prints My name is Bob and I am 30 years old.
```
Nazwane argumenty pozwalają przekazywać wartości w dowolnej kolejności, używając nazwy parametru:
```kotlin
println(introduce(age = 30, name = "Bob"))
```

# --instructions--

Zdefiniuj funkcję o nazwie `formatScore`, która przyjmuje parametr `String` `player` i parametr `Int` `score`, i zwraca ciąg znaków `"$player: $score"`. Wywołaj ją z `"Alice"` i `42` używając nazwanych argumentów (w dowolnej kolejności) i zapisz wynik w zmiennej o nazwie `result`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`result` musi być równe `"Alice: 42"`.

```kotlin
    tryCatch(result == "Alice: 42")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun formatScore(player: String, score: Int): String {
    return "$player: $score"
}
val result = formatScore(score = 42, player = "Alice")
```
