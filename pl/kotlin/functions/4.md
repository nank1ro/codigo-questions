---
language: kotlin
exerciseType: 1
---

# --description--

Kotlin obsługuje domyślne wartości parametrów. Jeśli wywołujący nie poda argumentu, używana jest wartość domyślna:
```kotlin
fun greet(name: String = "World") {
    println("Hello, $name!")
}
greet()         // prints Hello, World!
greet("Alice")  // prints Hello, Alice!
```
Wartości domyślne sprawiają, że parametry są opcjonalne, zmniejszając potrzebę przeciążania funkcji.

# --instructions--

Zdefiniuj funkcję o nazwie `multiply`, która przyjmuje dwa parametry `Int`: `a` oraz `b` z domyślną wartością `2`. Zwróć `a * b`. Wywołaj `multiply` tylko z argumentem `5` i zapisz wynik w zmiennej o nazwie `result`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`result` musi być równe `10`.

```kotlin
    tryCatch(result == 10)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun multiply(a: Int, b: Int = 2): Int {
    return a * b
}
val result = multiply(5)
```
