---
language: kotlin
exerciseType: 1
---

# --description--

Gdy ciało funkcji składa się z jednego wyrażenia, można użyć składni pojedynczego wyrażenia z `=`:
```kotlin
fun square(n: Int): Int = n * n
```
Jest to bardziej zwięzłe niż forma z blokiem. Kotlin często może wywnioskować typ zwracany, więc można też napisać:
```kotlin
fun square(n: Int) = n * n
```
Funkcje z pojedynczym wyrażeniem to powszechny idiom Kotlina dla prostych obliczeń.

# --instructions--

Zdefiniuj funkcję z pojedynczym wyrażeniem o nazwie `addThree`, która przyjmuje parametr `Int` `n` i zwraca `n + 3`. Wywołaj ją z `10` i zapisz wynik w zmiennej o nazwie `result`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`result` musi być równe `13`.

```kotlin
    tryCatch(result == 13)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun addThree(n: Int) = n + 3
val result = addThree(10)
```
