---
language: kotlin
exerciseType: 2
---

# --instructions--

Uzupełnij kod, aby zadeklarować `data class` o nazwie `Color` z właściwościami `val` o nazwach `red`, `green` i `blue` typu `Int`.

# --seed--

```kotlin
[/] [/] Color(val red: Int, val green: Int, val blue: Int)
```

# --answers--

- data
- class
- object
- open
- abstract
- sealed

# --solutions--

```kotlin
data class Color(val red: Int, val green: Int, val blue: Int)
```
