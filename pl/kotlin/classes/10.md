---
language: kotlin
exerciseType: 2
---

# --instructions--

Uzupełnij kod, aby `Cat` dziedziczyła po `Animal` i nadpisywała metodę `speak()` zwracającą `"Meow"`.

# --seed--

```kotlin
open class Animal(val name: String) {
    open fun speak(): String = "..."
}

class Cat(name: String) [/] Animal(name) {
    [/] fun speak(): String = "Meow"
}
```

# --answers--

- :
- override
- extends
- open
- super
- implements

# --solutions--

```kotlin
open class Animal(val name: String) {
    open fun speak(): String = "..."
}

class Cat(name: String) : Animal(name) {
    override fun speak(): String = "Meow"
}
```
