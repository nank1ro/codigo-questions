---
language: kotlin
exerciseType: 1
---

# --description--

Aby odczytać wartość, indeksujesz mapę jej kluczem, używając nawiasów kwadratowych lub funkcji `get`:
```kotlin
val ages = mapOf("Alice" to 30, "Bob" to 25)
println(ages["Alice"])    // 30
println(ages.get("Bob"))  // 25
```

Jeśli klucz nie istnieje, wynikiem jest `null`, więc typ `ages["Alice"]` to `Int?`, a nie `Int`:
```kotlin
println(ages["Zoe"]) // null
```

# --instructions--

Napisz funkcję `priceOf(prices: Map<String, Int>, item: String): Int?`, która zwraca cenę zapisaną dla `item`, lub `null`, jeśli produktu nie ma w mapie.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

Cena `"apple"` w `{apple=2, bread=3}` musi wynosić `2`

```kotlin
    val prices = mapOf("apple" to 2, "bread" to 3)
    tryCatch(priceOf(prices, "apple") == 2)
```

Cena `"bread"` musi wynosić `3`

```kotlin
    tryCatch(priceOf(prices, "bread") == 3)
```

Cena brakującego produktu musi wynosić `null`

```kotlin
    tryCatch(priceOf(prices, "kiwi") == null)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun priceOf(prices: Map<String, Int>, item: String): Int? {
    return prices[item]
}
```
