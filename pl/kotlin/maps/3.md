---
language: kotlin
exerciseType: 2
---

# --description--

Gdy klucz może nie istnieć, `getOrDefault` pozwala wybrać wartość używaną zamiast `null`:
```kotlin
val ages = mapOf("Alice" to 30)
println(ages.getOrDefault("Alice", 0)) // 30
println(ages.getOrDefault("Zoe", 0))   // 0
```
Pierwszy argument to klucz, drugi to wartość domyślna zwracana, gdy klucz nie zostanie znaleziony.

# --instructions--

Uzupełnij kod, aby wypisać stan magazynowy `"kiwi"`, używając `0`, gdy owocu nie ma w mapie

# --seed--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.[/]("kiwi", [/]))
```

# --answers--

- getOrDefault
- get
- getOrNull
- 0
- null
- "kiwi"

# --solutions--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.getOrDefault("kiwi", 0))
```

# --output--

0
