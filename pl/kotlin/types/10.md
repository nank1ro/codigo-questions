---
language: kotlin
exerciseType: 1
---

# --description--

`toIntOrNull()` kończy się sukcesem tylko wtedy, gdy cały tekst jest poprawną liczbą całkowitą, z opcjonalnym znakiem:
```kotlin
println("42".toIntOrNull())   // 42
println("-7".toIntOrNull())   // -7
println("3.5".toIntOrNull())  // null, not a whole number
println(" 42".toIntOrNull())  // null, spaces are not allowed
println("abc".toIntOrNull())  // null
```
Dla tekstu dziesiętnego użyj `toDoubleOrNull()`, które przyjmuje `"3.5"` i analogicznie zwraca `Double?`.

# --instructions--

Napisz funkcję `parseAge(text: String): Int`, która zwraca liczbę całkowitą zapisaną w `text`, lub `-1`, gdy tekst nie jest poprawną liczbą całkowitą.

Przykład: `parseAge("42")` zwraca `42`, a `parseAge("abc")` zwraca `-1`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`parseAge("42")` musi zwrócić `42`

```kotlin
    tryCatch(parseAge("42") == 42)
```

`parseAge("abc")` musi zwrócić `-1`

```kotlin
    tryCatch(parseAge("abc") == -1)
```

`parseAge("3.5")` musi zwrócić `-1`

```kotlin
    tryCatch(parseAge("3.5") == -1)
```

`parseAge("-7")` musi zwrócić `-7`

```kotlin
    tryCatch(parseAge("-7") == -7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun parseAge(text: String): Int {
    return text.toIntOrNull() ?: -1
}
```
