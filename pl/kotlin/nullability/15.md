---
language: kotlin
exerciseType: 4
---

# --description--

Funkcję rozszerzającą można zadeklarować na **odbiorniku nullable**, więc można ją wywołać nawet na wartości `null`. Wewnątrz `this` jest nullable i musi zostać sprawdzone:
```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val none: String? = null
println(none.orDash()) // -
```
Zauważ, że w miejscu wywołania nie trzeba używać `?.`: funkcja sama obsługuje przypadek `null`.
Biblioteka standardowa używa tej sztuczki w `isNullOrEmpty()` i `orEmpty()`, które można bezpiecznie wywołać na dowolnym `String?`.

# --instructions--

Czy potrafisz uporządkować te linie tak, aby zdefiniować funkcję rozszerzającą `orDash` oraz wypisać `-` dla brakującej nazwy, a następnie `kotlin` dla obecnej? Zdefiniuj najpierw funkcję

# --answers--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
```

```kotlin
val a: String? = null
val b: String? = "kotlin"
```

```kotlin
println(a.orDash())
```

```kotlin
println(b.orDash())
```

# --solutions--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val a: String? = null
val b: String? = "kotlin"
println(a.orDash())
println(b.orDash())
```

# --output--

-
kotlin
