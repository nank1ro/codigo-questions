---
language: kotlin
exerciseType: 1
---

# --description--

Gdy `null` oznacza, że wywołujący popełnił błąd, zakończ działanie wcześnie za pomocą `requireNotNull`.
Zwraca ona wartość jako non-null, gdy jest obecna, a gdy jest `null`, rzuca `IllegalArgumentException` z opcjonalnym komunikatem:
```kotlin
fun greet(name: String?): String {
    val safeName = requireNotNull(name) { "name is required" }
    return "Hello, $safeName"
}
greet(null) // IllegalArgumentException: name is required
```
Po wywołaniu kompilator wykonuje również smart cast samego `name` na `String`, więc `name.length` jest dozwolone od tej linii wzwyż.
W przeciwieństwie do `!!` awaria niesie czytelny komunikat i wskazuje, że to *argument* był nieprawidłowy.

# --instructions--

Napisz funkcję `safeUpper(text: String?): String`, która zwraca `text` wielkimi literami. Gdy `text` jest `null`, musi rzucić `IllegalArgumentException` z komunikatem `text is required`, używając `requireNotNull`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`safeUpper("abc")` musi zwrócić `"ABC"`

```kotlin
    tryCatch(safeUpper("abc") == "ABC")
```

`safeUpper("Kotlin")` musi zwrócić `"KOTLIN"`

```kotlin
    tryCatch(safeUpper("Kotlin") == "KOTLIN")
```

`safeUpper(null)` musi rzucić `IllegalArgumentException`

```kotlin
    var threw = false
    try {
        safeUpper(null)
    } catch (e: IllegalArgumentException) {
        threw = true
    }
    tryCatch(threw)
```

Komunikat wyjątku musi być równy `text is required`

```kotlin
    var message = ""
    try {
        safeUpper(null)
    } catch (e: IllegalArgumentException) {
        message = e.message ?: ""
    }
    tryCatch(message == "text is required")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun safeUpper(text: String?): String {
    val value = requireNotNull(text) { "text is required" }
    return value.uppercase()
}
```
