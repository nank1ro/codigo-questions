---
language: kotlin
exerciseType: 1
---

# --description--

Wiele funkcji standardowych zwraca `null` zamiast zawieść. `toIntOrNull()` przekształca ciąg znaków w `Int` albo zwraca `null`, gdy tekst nie jest liczbą całkowitą:
```kotlin
println("42".toIntOrNull())  // 42
println("4x2".toIntOrNull()) // null
```
`mapNotNull` przekształca każdy element jak `map`, ale pomija wyniki, które są `null`:
```kotlin
val words = listOf("1", "two", "3")
println(words.mapNotNull { it.toIntOrNull() }) // [1, 3]
```

# --instructions--

Napisz funkcję `parseAll(tokens: List<String>): List<Int>`, która zwraca liczby znalezione w `tokens`, w tej samej kolejności, ignorując ciągi znaków, które nie są liczbami całkowitymi.

Przykład: `parseAll(listOf("1", "x", "3"))` zwraca `[1, 3]`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`parseAll(listOf("1", "x", "3"))` musi zwrócić `[1, 3]`

```kotlin
    tryCatch(parseAll(listOf("1", "x", "3")) == listOf(1, 3))
```

`parseAll(listOf("a", "b"))` musi zwrócić pustą listę

```kotlin
    tryCatch(parseAll(listOf("a", "b")) == listOf<Int>())
```

`parseAll(listOf("-4", "2.5", "10"))` musi zwrócić `[-4, 10]`

```kotlin
    tryCatch(parseAll(listOf("-4", "2.5", "10")) == listOf(-4, 10))
```

`parseAll(listOf())` musi zwrócić pustą listę

```kotlin
    tryCatch(parseAll(listOf()) == listOf<Int>())
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun parseAll(tokens: List<String>): List<Int> {
    return tokens.mapNotNull { it.toIntOrNull() }
}
```
