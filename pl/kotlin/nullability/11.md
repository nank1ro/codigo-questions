---
language: kotlin
exerciseType: 2
---

# --description--

Kolekcje również mogą przechowywać elementy nullable: `List<Int?>` może zawierać wpisy `null`, podczas gdy `List<Int>` nigdy.
`filterNotNull()` zwraca nową listę bez wpisów `null`, a typ jej elementów staje się non-null, dzięki czemu możesz swobodnie korzystać z elementów:
```kotlin
val scores: List<Int?> = listOf(10, null, 20)
val valid = scores.filterNotNull() // List<Int> [10, 20]
println(valid.sum())               // 30
```

# --instructions--

Uzupełnij kod, aby zadeklarować listę, która może zawierać liczby `null`, a następnie usunąć wpisy `null` i wypisać pozostałe wartości oraz ich sumę

# --seed--

```kotlin
val values: List<[/]> = listOf(3, null, 5, null)
val present = values.[/]()
println(present)
println(present.sum())
```

# --answers--

- Int?
- Int
- filterNotNull
- filter
- mapNotNull
- requireNotNull

# --solutions--

```kotlin
val values: List<Int?> = listOf(3, null, 5, null)
val present = values.filterNotNull()
println(present)
println(present.sum())
```

# --output--

[3, 5]
8
