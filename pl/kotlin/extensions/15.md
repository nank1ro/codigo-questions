---
language: kotlin
exerciseType: 1
---

# --description--

Funkcję rozszerzającą z dokładnie **jednym** parametrem można oznaczyć jako `infix`. Funkcję infix można wywołać bez kropki i nawiasów, z odbiorcą po lewej i argumentem po prawej, co czyta się niemal jak zdanie:
```kotlin
infix fun Int.percentOf(total: Int): Int = total * this / 100

println(20 percentOf 50)   // 10
println(20.percentOf(50))  // 10, the normal call still works
```
Kotlin używa tego także w niektórych wbudowanych funkcjach: `1 to "one"` buduje `Pair`, a `1 until 5` buduje zakres.

# --instructions--

Napisz infiksową funkcję rozszerzającą `toThe(exponent: Int)` dla `Int`, która zwraca odbiorcę podniesionego do podanej potęgi, tak aby `2 toThe 10` dawało `1024`. Wykładnik nigdy nie jest ujemny, a dowolna liczba do potęgi `0` to `1`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`2 toThe 10` musi zwracać `1024`

```kotlin
    tryCatch((2 toThe 10) == 1024)
```

`3 toThe 3` musi zwracać `27`

```kotlin
    tryCatch((3 toThe 3) == 27)
```

`5 toThe 0` musi zwracać `1`

```kotlin
    tryCatch((5 toThe 0) == 1)
```

`7.toThe(1)` musi zwracać `7`

```kotlin
    tryCatch(7.toThe(1) == 7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
infix fun Int.toThe(exponent: Int): Int {
    var result = 1
    repeat(exponent) { result *= this }
    return result
}
```
