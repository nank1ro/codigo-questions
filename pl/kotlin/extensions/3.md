---
language: kotlin
exerciseType: 1
---

# --description--

Funkcja rozszerzająca może przyjmować parametry jak każda inna funkcja. Odbiorca pozostaje po lewej stronie kropki, a parametry trafiają w nawiasy:
```kotlin
fun Int.isDivisibleBy(other: Int): Boolean {
    return this % other == 0
}
println(12.isDivisibleBy(4)) // true
println(12.isDivisibleBy(5)) // false
```
Typ przed kropką jest zwykłym typem, więc w ten sam sposób możesz rozszerzyć `List<Int>`, `Double` albo własną klasę.

# --instructions--

Napisz funkcję rozszerzającą `isBetween(low: Int, high: Int)` dla `Int`, która zwraca `true`, gdy odbiorca jest większy lub równy `low` i mniejszy lub równy `high`.

Przykład: `5.isBetween(1, 10)` zwraca `true`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`5.isBetween(1, 10)` musi zwracać `true`

```kotlin
    tryCatch(5.isBetween(1, 10) == true)
```

`15.isBetween(1, 10)` musi zwracać `false`

```kotlin
    tryCatch(15.isBetween(1, 10) == false)
```

`10.isBetween(1, 10)` musi zwracać `true`

```kotlin
    tryCatch(10.isBetween(1, 10) == true)
```

`0.isBetween(1, 10)` musi zwracać `false`

```kotlin
    tryCatch(0.isBetween(1, 10) == false)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun Int.isBetween(low: Int, high: Int): Boolean {
    return this >= low && this <= high
}
```
