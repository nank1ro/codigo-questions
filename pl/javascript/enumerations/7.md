---
language: javascript
exerciseType: 1
---

# --description--

Enumeracje naturalnie łączą się z instrukcją `switch`, która porównuje jedną wartość z listą etykiet `case` i uruchamia kod pierwszej pasującej.
Każda gałąź kończy się `return` lub `break`, a opcjonalna gałąź `default` uruchamia się, gdy nic nie pasuje:
```javascript
const Light = Object.freeze({ RED: "red", GREEN: "green" });

function action(light) {
  switch (light) {
    case Light.RED:
      return "stop";
    case Light.GREEN:
      return "go";
    default:
      return "unknown";
  }
}
console.log(action(Light.GREEN));
// prints go
```
Zawsze porównuj ze składowymi (`Light.RED`), nigdy z surowymi wartościami (`"red"`): jeśli wartość kiedykolwiek się zmieni, `switch` nadal będzie działać.

# --instructions--

Napisz funkcję `weekend(day)`, która używa `switch` na enumeracji `Day` i zwraca `true` dla `Day.SATURDAY` i `Day.SUNDAY`, a `false` dla każdej innej wartości

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {

}
```

# --asserts--

`weekend(Day.SATURDAY)` musi zwrócić `true`.

```javascript
tryCatch(weekend(Day.SATURDAY) === true);
```

`weekend(Day.SUNDAY)` musi zwrócić `true`.

```javascript
tryCatch(weekend(Day.SUNDAY) === true);
```

`weekend(Day.MONDAY)` musi zwrócić `false`.

```javascript
tryCatch(weekend(Day.MONDAY) === false);
```

`weekend("holiday")` musi zwrócić `false`, ponieważ nie jest składową.

```javascript
tryCatch(weekend("holiday") === false);
```

Funkcja musi używać instrukcji `switch`.

```javascript
tryCatch(/switch\s*\(/.test(weekend.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {
  switch (day) {
    case Day.SATURDAY:
    case Day.SUNDAY:
      return true;
    default:
      return false;
  }
}
```
