---
language: javascript
exerciseType: 2
---

# --description--

Składowe mogą również przechowywać **liczby**. Wartości liczbowe są przydatne, gdy składowe mają naturalny porządek, ponieważ można je porównywać zwykłymi operatorami:
```javascript
const Priority = Object.freeze({
  LOW: 1,
  MEDIUM: 2,
  HIGH: 3,
});
console.log(Priority.HIGH > Priority.LOW);
// prints true
```
Kompromisem jest czytelność: wypisanie `Priority.HIGH` pokazuje `3`, co mówi znacznie mniej niż powiedziałby ciąg znaków `"high"`.

# --instructions--

Zadeklaruj zamrożoną enumerację liczbową `Level` z `EASY` równym `1`, `NORMAL` równym `2` i `HARD` równym `3`, a następnie wypisz, czy `HARD` jest większe od `EASY`, oraz różnicę między `HARD` a `EASY`

# --seed--

```javascript
const Level = Object.freeze({
  EASY: [/],
  NORMAL: 2,
  HARD: [/],
});
console.log(Level.HARD [/] Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --answers--

- 1
- 3
- >
- 0
- <
- ===

# --solutions--

```javascript
const Level = Object.freeze({
  EASY: 1,
  NORMAL: 2,
  HARD: 3,
});
console.log(Level.HARD > Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --output--

true
2
