---
language: javascript
exerciseType: 4
---

# --description--

Gdy każda składowa potrzebuje kilku danych lub własnych metod, rolę enumeracji może pełnić **klasa**. Każda składowa jest instancją klasy, przechowywaną we właściwości `static`, czyli właściwości należącej do samej klasy, a nie do każdej instancji:
```javascript
class Planet {
  static MERCURY = new Planet("Mercury", 0.4);
  static EARTH = new Planet("Earth", 1);

  constructor(name, gravity) {
    this.name = name;
    this.gravity = gravity;
  }
}
console.log(Planet.EARTH.name);
// prints Earth
```
Wywołaj `Object.freeze(Planet)` po klasie, aby uniemożliwić komukolwiek dodawanie lub zastępowanie składowych, i zamroź każdą instancję w konstruktorze za pomocą `Object.freeze(this)`, aby same składowe pozostały tylko do odczytu.

# --instructions--

Uporządkuj linie, aby zadeklarować opartą na klasie enumerację `Coin` i wypisać centy składowej `DIME`.
Zadeklaruj `PENNY` przed `DIME`, obie przed konstruktorem, i zamroź klasę przed wypisaniem

# --answers--

```javascript
class Coin {
```

```javascript
  static PENNY = new Coin("penny", 1);
```

```javascript
  static DIME = new Coin("dime", 10);
```

```javascript
  constructor(name, cents) { this.name = name; this.cents = cents; }
```

```javascript
}
```

```javascript
Object.freeze(Coin);
```

```javascript
console.log(Coin.DIME.cents);
```

# --solutions--

```javascript
class Coin {
  static PENNY = new Coin("penny", 1);
  static DIME = new Coin("dime", 10);
  constructor(name, cents) { this.name = name; this.cents = cents; }
}
Object.freeze(Coin);
console.log(Coin.DIME.cents);
```

# --output--

10
