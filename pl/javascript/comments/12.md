---
language: javascript
exerciseType: 4
---

# --description--

Kolejność wewnątrz komentarza JSDoc jest zawsze taka sama: najpierw podsumowanie, potem po jednym `@param` na parametr w kolejności ich deklaracji, a na końcu `@returns`. Otwierające `/**` i zamykające ` */` obejmują całość, a komentarz znajduje się bezpośrednio nad funkcją, którą opisuje:
```javascript
/**
 * Returns the number of seconds in the given minutes.
 * @param {number} minutes a whole number of minutes
 * @returns {number} minutes multiplied by sixty
 */
function toSeconds(minutes) {
  return minutes * 60;
}
```

# --instructions--

Uporządkuj elementy tak, aby udokumentować `twice` podsumowaniem, linią `@param` i linią `@returns`, w tej kolejności i bezpośrednio nad funkcją, a następnie wypisz `twice(21)` po zdefiniowaniu funkcji

# --answers--

```javascript
/**
```

```javascript
 * Returns the double of a number.
```

```javascript
 * @param {number} n the number to double
```

```javascript
 * @returns {number} n multiplied by two
```

```javascript
 */
```

```javascript
function twice(n) {
  return n * 2;
}
```

```javascript
console.log(twice(21));
```

# --solutions--

```javascript
/**
 * Returns the double of a number.
 * @param {number} n the number to double
 * @returns {number} n multiplied by two
 */
function twice(n) {
  return n * 2;
}
console.log(twice(21));
```

# --output--

42
