---
language: javascript
exerciseType: 2
---

# --description--

Funkcje mogą mieć wiele parametrów wejściowych, które są zapisywane w nawiasach funkcji, oddzielone przecinkami.
```javascript
function sayHello(name, newUser) {
  var greet = `Hello ${name}!`;
  if (newUser) {
    greet += " Welcome on board :)";
  }
  return greet;
}
// prints "Hello Smith! Welcome on board :)"
console.log(sayHello("Smith", true));
```

# --instructions--

Uzupełnij kod, aby utworzyć poprawną funkcję o nazwie `sumAllNumbers`, przekazując następującą tablicę liczb `[15, 24, 31, 79]` oraz `true`, aby wydrukować wynik

# --seed--

```javascript
const reducer = (accumulator, currentValue) => accumulator + currentValue;

function sumAllNumbers(nums, [/]) {
    let result = nums.reduce(reducer);
    if (printResult) { [/](result); }
    return result;
}

let result = [/]([15, 24, 31, [/]], [/]);
```

# --answers--

- sumAllNumbers
- sum
- 79
- ]
- [
- true
- false
- True
- printResult
- console.log
- true

# --solutions--

```javascript
const reducer = (accumulator, currentValue) => accumulator + currentValue;

function sumAllNumbers(nums, printResult) {
    let result = nums.reduce(reducer);
    if (printResult) { console.log(result); }
    return result;
}

let result = sumAllNumbers([15, 24, 31, 79], true);
```

# --output--

149
