---
language: javascript
exerciseType: 2
---

# --description--

`delete(value)` zwraca `true`, gdy wartość została usunięta, i `false`, gdy nie było jej w secie.
Aby usunąć **wszystkie** wartości naraz, wywołaj `clear()`:
```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Usuń `"pen"` z `cart`, wypisując wynik usunięcia, a następnie opróżnij cały set i wypisz, ile wartości pozostało

# --seed--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
