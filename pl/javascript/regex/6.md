---
language: javascript
exerciseType: 1
---

# --description--

Ponieważ `match` zwraca `null`, gdy wzorzec nie występuje, odczytanie od razu `match[0]` rzuca `TypeError: Cannot read properties of null`. Zabezpiecz to:
```javascript
function firstWord(text) {
  const match = text.match(/[a-z]+/);
  if (match === null) {
    return "";
  }
  return match[0];
}
```
Operator scalania nullish zapisuje tę samą ochronę w jednej linii, ponieważ `match?.[0]` daje `undefined`, gdy `match` jest `null`:
```javascript
return text.match(/[a-z]+/)?.[0] ?? "";
```

# --instructions--

Napisz funkcję `firstNumber`, która przyjmuje łańcuch `text` i zwraca pierwszy zawarty w nim ciąg cyfr jako łańcuch znaków, albo pusty łańcuch znaków, gdy nie ma żadnego

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function firstNumber(text) {
  
}
```

# --asserts--

`firstNumber("order 42 shipped")` musi zwracać `42`.

```javascript
tryCatch(firstNumber("order 42 shipped") === "42");
```

`firstNumber("a1b22")` musi zwracać `1`.

```javascript
tryCatch(firstNumber("a1b22") === "1");
```

`firstNumber("no digits here")` musi zwracać pusty łańcuch znaków.

```javascript
tryCatch(firstNumber("no digits here") === "");
```

`firstNumber("2026 and 2027")` musi zwracać `2026`.

```javascript
tryCatch(firstNumber("2026 and 2027") === "2026");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function firstNumber(text) {
  return text.match(/\d+/)?.[0] ?? "";
}
```
