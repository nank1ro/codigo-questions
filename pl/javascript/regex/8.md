---
language: javascript
exerciseType: 1
---

# --description--

Przechwytuj tylko to, czego potrzebujesz. Grupa to nie tylko sposób na odczytanie fragmentu; mówi też czytelnikowi, która część wzorca jest ważna. We wzorcu czasu, w którym chcesz tylko minut, pogrupuj same minuty i zostaw resztę bez grupowania:
```javascript
const minutes = "at 14:35:02".match(/\d{2}:(\d{2}):\d{2}/)?.[1];
console.log(minutes);
// wypisuje 35
```
Cały wzorzec nadal musi pasować, więc godziny i sekundy są nadal wymagane; po prostu nie są przechwytywane. Mniej grup oznacza mniej liczb do śledzenia przy odczytywaniu `match[1]`, `match[2]` i tak dalej.

# --instructions--

Napisz funkcję `monthOf`, która przyjmuje łańcuch `date`, który może zawierać datę zapisaną jako `YYYY-MM-DD`, możliwie otoczoną innym tekstem, i zwraca jej miesiąc jako łańcuch znaków, albo pusty łańcuch znaków, gdy `date` nie zawiera takiej daty. Na przykład `monthOf("2026-09-12")` zwraca `09`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function monthOf(date) {
  
}
```

# --asserts--

`monthOf("2026-09-12")` musi zwracać `09`.

```javascript
tryCatch(monthOf("2026-09-12") === "09");
```

`monthOf("born on 1999-12-31")` musi zwracać `12`.

```javascript
tryCatch(monthOf("born on 1999-12-31") === "12");
```

`monthOf("no date")` musi zwracać pusty łańcuch znaków.

```javascript
tryCatch(monthOf("no date") === "");
```

`monthOf("12-31-1999")` musi zwracać pusty łańcuch znaków.

```javascript
tryCatch(monthOf("12-31-1999") === "");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function monthOf(date) {
  return date.match(/\d{4}-(\d{2})-\d{2}/)?.[1] ?? "";
}
```
