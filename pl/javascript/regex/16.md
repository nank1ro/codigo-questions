---
language: javascript
exerciseType: 1
---

# --description--

Ostatnia flaga dopełnia zbiór. Domyślnie `^` i `$` oznaczają początek i koniec **całego łańcucha**, więc wzorzec zakotwiczony za pomocą `^` może pasować tylko na samym początku, nawet gdy tekst ma kilka linii.

Flaga **`m`** (multiline) zmienia to: `^` i `$` pasują wtedy także tuż po i tuż przed każdym znakiem nowej linii, więc każda linia jest zakotwiczona osobno:
```javascript
const text = "note a\nb\nnote c";
console.log(text.match(/^note.*/g));
// wypisuje [ 'note a' ]
console.log(text.match(/^note.*/gm));
// wypisuje [ 'note a', 'note c' ]
```
Tutaj ważne są dwa szczegóły. Domyślnie `.` nie pasuje do znaku nowej linii (zmienia to tylko flaga `s`), więc `.*` sam zatrzymuje się na końcu linii. A `match` z flagą `g` zwraca `null`, a nie pustą tablicę, gdy nic nie pasuje, więc paruj go z `?? []`, gdy obiecasz zwrócić tablicę.

# --instructions--

Napisz funkcję `todoLines`, która przyjmuje łańcuch `text` i zwraca tablicę każdej linii zaczynającej się od `todo`, ignorując wielkość liter. Zwróć pustą tablicę, gdy nie ma żadnej

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function todoLines(text) {
  
}
```

# --asserts--

`todoLines("todo a\nb\nTODO c")` musi zwracać obie linie z zadaniami.

```javascript
tryCatch(JSON.stringify(todoLines("todo a\nb\nTODO c")) === '["todo a","TODO c"]');
```

`todoLines("nothing here")` musi zwracać pustą tablicę.

```javascript
tryCatch(JSON.stringify(todoLines("nothing here")) === '[]');
```

`todoLines("Todo write tests")` musi zwracać tę linię.

```javascript
tryCatch(JSON.stringify(todoLines("Todo write tests")) === '["Todo write tests"]');
```

`todoLines("a todo b")` musi zwracać pustą tablicę, ponieważ linia nie zaczyna się od todo.

```javascript
tryCatch(JSON.stringify(todoLines("a todo b")) === '[]');
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function todoLines(text) {
  return text.match(/^todo.*/gim) ?? [];
}
```
