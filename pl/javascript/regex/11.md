---
language: javascript
exerciseType: 1
---

# --description--

Flaga `g` daje każdy dopasowany łańcuch, ale wyrzuca grupy. Gdy potrzebujesz grup *każdego* dopasowania, użyj **`matchAll`**. Zwraca on iterator pełnych obiektów dopasowania, z których każdy jest dokładnie taki jak wynik zwykłego `match`:
```javascript
const text = "a=1;b=2";
for (const match of text.matchAll(/(?<key>\w+)=(?<value>\w+)/g)) {
  console.log(`${match.groups.key} -> ${match.groups.value}`);
}
// prints a -> 1
// prints b -> 2
```
`matchAll` wymaga flagi `g`; bez niej rzuca `TypeError`. Ponieważ zwraca iterator, rozwiń go za pomocą `[...text.matchAll(pattern)]`, gdy chcesz otrzymać prawdziwą tablicę, i zauważ, że nie daje niczego, gdy wzorzec nigdy nie pasuje.

# --instructions--

Napisz funkcję `parsePairs`, która przyjmuje łańcuch `text`, znajduje w nim każdą parę `key=value`, gdzie klucze i wartości to ciągi znaków słowa, i zwraca obiekt mapujący każdy klucz na jego wartość. Na przykład `parsePairs("host=local;port=80")` zwraca `{ host: "local", port: "80" }`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function parsePairs(text) {
  
}
```

# --asserts--

`parsePairs("host=local;port=80")` musi zwracać obiekt z `host` i `port`.

```javascript
tryCatch(JSON.stringify(parsePairs("host=local;port=80")) === '{"host":"local","port":"80"}');
```

`parsePairs("a=1")` musi zwracać obiekt tylko z `a`.

```javascript
tryCatch(JSON.stringify(parsePairs("a=1")) === '{"a":"1"}');
```

`parsePairs("nothing here")` musi zwracać pusty obiekt.

```javascript
tryCatch(JSON.stringify(parsePairs("nothing here")) === '{}');
```

`parsePairs("x=1 y=2 z=3")` musi zwracać wszystkie trzy pary.

```javascript
tryCatch(JSON.stringify(parsePairs("x=1 y=2 z=3")) === '{"x":"1","y":"2","z":"3"}');
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function parsePairs(text) {
  const pairs = {};
  for (const match of text.matchAll(/(?<key>\w+)=(?<value>\w+)/g)) {
    pairs[match.groups.key] = match.groups.value;
  }
  return pairs;
}
```
