---
language: javascript
exerciseType: 1
---

# --description--

Wzorce stają się użyteczne, gdy opisują *rodzaj* znaku zamiast jednego dokładnego znaku. Kilka **sekwencji ucieczki** pokrywa większość potrzeb:
- `\d` dowolna cyfra, od `0` do `9`
- `\w` dowolny znak słowa: litera, cyfra lub `_`
- `\s` dowolny biały znak: spacja, tabulator, znak nowej linii

```javascript
console.log(/\d/.test("room 12"));
// wypisuje true
console.log(/\d/.test("lobby"));
// wypisuje false
```
**Kwantyfikator** mówi, ile razy poprzedni fragment może się powtórzyć. Najczęstszy to `+`, oznaczający „jeden lub więcej”:
```javascript
console.log(/\d+/.test("42"));
// wypisuje true
```
Zatem `/\d/` pasuje do pojedynczej cyfry, a `/\d+/` pasuje do ciągu cyfr. Dla zwykłego `test` oba zachowują się tak samo, ponieważ oba wymagają jedynie obecności jednej cyfry.

# --instructions--

Napisz funkcję `hasDigit`, która przyjmuje łańcuch `text` i zwraca `true`, gdy `text` zawiera co najmniej jedną cyfrę, używając `test`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function hasDigit(text) {
  
}
```

# --asserts--

Metoda `test` musi być użyta.

```json
{
  "regex": "\\.test\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`hasDigit("room 12")` musi zwracać `true`.

```javascript
tryCatch(hasDigit("room 12") === true);
```

`hasDigit("lobby")` musi zwracać `false`.

```javascript
tryCatch(hasDigit("lobby") === false);
```

`hasDigit("7")` musi zwracać `true`.

```javascript
tryCatch(hasDigit("7") === true);
```

`hasDigit("")` musi zwracać `false`.

```javascript
tryCatch(hasDigit("") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function hasDigit(text) {
  return /\d/.test(text);
}
```
