---
language: javascript
exerciseType: 2
---

# --description--

Metoda `split(separator)` dzieli łańcuch na **tablicę** fragmentów, przecinając przy każdym wystąpieniu `separator`:
```javascript
let sentence = "I like JavaScript";
let words = sentence.split(" ");
console.log(words);
// prints [ 'I', 'like', 'JavaScript' ]
```
Przeciwieństwem jest metoda tablicy `join(separator)`, która skleja fragmenty z powrotem w łańcuch:
```javascript
console.log(words.join("-"));
// prints I-like-JavaScript
```

# --instructions--

Podziel `csv` przy każdym przecinku i wydrukuj, ile kolorów zawiera

# --seed--

```javascript
let csv = "red,green,blue";
let colors = csv.[/]([/]);
console.log(colors.[/]);
```

# --answers--

- split
- ","
- " "
- join
- length
- size

# --solutions--

```javascript
let csv = "red,green,blue";
let colors = csv.split(",");
console.log(colors.length);
```

# --output--

3
