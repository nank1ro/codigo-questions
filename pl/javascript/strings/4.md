---
language: javascript
exerciseType: 1
---

# --description--

Aby sprawdzić, czy łańcuch zawiera inny łańcuch, użyj tych metod, które zawsze zwracają wartość logiczną:
- `includes(text)` to `true`, jeśli `text` występuje gdziekolwiek
- `startsWith(text)` to `true`, jeśli łańcuch zaczyna się od `text`
- `endsWith(text)` to `true`, jeśli łańcuch kończy się na `text`

```javascript
let file = "photo.png";
console.log(file.includes("."));
// prints true
console.log(file.startsWith("ph"));
// prints true
console.log(file.endsWith(".jpg"));
// prints false
```
Porównanie rozróżnia wielkość liter: `"Hello".includes("h")` to `false`.

# --instructions--

Napisz funkcję `isImage(fileName)`, która zwraca `true`, gdy `fileName` kończy się na `".png"`, a w przeciwnym razie `false`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isImage(fileName) {

}
```

# --asserts--

`isImage("cat.png")` musi zwracać `true`.

```javascript
tryCatch(isImage("cat.png") === true);
```

`isImage("cat.jpg")` musi zwracać `false`.

```javascript
tryCatch(isImage("cat.jpg") === false);
```

`isImage("png.txt")` musi zwracać `false`.

```javascript
tryCatch(isImage("png.txt") === false);
```

`isImage("a.png.txt")` musi zwracać `false`.

```javascript
tryCatch(isImage("a.png.txt") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isImage(fileName) {
  return fileName.endsWith(".png");
}
```
