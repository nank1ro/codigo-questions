---
language: javascript
exerciseType: 2
---

# --description--

**Map** przechowuje **pary klucz-wartość**: każda wartość jest zapisywana pod kluczem, a ty używasz tego klucza, aby ponownie znaleźć wartość.
Tworzysz pusty Map za pomocą `new Map()`, dodajesz parę za pomocą `set(key, value)` i odczytujesz wartość za pomocą `get(key)`:
```javascript
let ages = new Map();
ages.set("Ann", 30);
ages.set("Bob", 25);
console.log(ages.get("Ann"));
// prints 30
```
Wywołanie `set()` z kluczem, który już istnieje, zastępuje jego wartość.

# --instructions--

Utwórz pusty Map `ages`, zapisz `30` pod kluczem `"Ann"`, a następnie wydrukuj wartość zapisaną dla `"Ann"`

# --seed--

```javascript
let ages = new [/]();
ages.[/]("Ann", 30);
console.log(ages.[/]("Ann"));
```

# --answers--

- Map
- set
- get
- Object
- add
- put
- read

# --solutions--

```javascript
let ages = new Map();
ages.set("Ann", 30);
console.log(ages.get("Ann"));
```

# --output--

30
