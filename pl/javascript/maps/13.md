---
language: javascript
exerciseType: 2
---

# --description--

`delete(key)` zwraca `true`, gdy para została usunięta, i `false`, gdy klucza tam nie było.
Aby usunąć **wszystkie** pary naraz, wywołaj `clear()`:
```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Usuń `"pen"` z `cart`, drukując wynik usunięcia, a następnie opróżnij cały Map i wydrukuj, ile par zostało

# --seed--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
