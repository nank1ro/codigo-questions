---
language: javascript
exerciseType: 1
---

# --description--

Map ma jeszcze kilka istotnych metod i właściwości:
- `has(key)` zwraca `true`, jeśli klucz istnieje
- `delete(key)` usuwa parę z tym kluczem
- `size` to liczba przechowywanych par

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
console.log(stock.has("apple"));
// prints true
stock.delete("pear");
console.log(stock.size);
// prints 1
```
Zauważ, że `size` jest właściwością, a nie metodą, więc nie ma nawiasów.

# --instructions--

Usuń `"pear"` z `stock`, dodaj `"kiwi"` z wartością `2`, a następnie zapisz w `hasPear`, czy `stock` nadal zawiera klucz `"pear"`, a w `count` ile par zawiera

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
```

# --asserts--

`hasPear` musi być `false`.

```javascript
tryCatch(hasPear === false);
```

`count` musi być `2`.

```javascript
tryCatch(count === 2);
```

`stock` musi zawierać `"kiwi"` z wartością `2`.

```javascript
tryCatch(stock.get("kiwi") === 2);
```

`stock` nie może zawierać `"pear"`.

```javascript
tryCatch(stock.has("pear") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
stock.delete("pear");
stock.set("kiwi", 2);
let hasPear = stock.has("pear");
let count = stock.size;
```
