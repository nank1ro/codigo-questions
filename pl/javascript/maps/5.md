---
language: javascript
exerciseType: 2
---

# --description--

Map zapamiętuje kolejność, w jakiej dodawano pary, i możesz go przeglądać za pomocą `for...of`.
Metoda `entries()` zwraca każdą parę jako tablicę `[key, value]`, którą możesz od razu rozpakować w pętli:
```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
// prints apple: 3
// prints pear: 5
```
Przeglądanie bezpośrednio Map, `for (const [name, qty] of stock)`, robi dokładnie to samo.

# --instructions--

Przejdź przez wpisy `stock` i wydrukuj każdą nazwę wraz z jej ilością

# --seed--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [/] of stock.[/]()) {
  console.log(`${name}: ${qty}`);
}
```

# --answers--

- [name, qty]
- [qty, name]
- name, qty
- entries
- pairs
- items

# --solutions--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
```

# --output--

apple: 3
pear: 5
