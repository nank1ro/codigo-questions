---
language: javascript
exerciseType: 2
---

# --description--

Mamy już zainicjowaną zmienną `total`
```javascript
var total = 5;
```
Powiedzmy, że chcemy dodać liczbę `2` do zmiennej `total`, możemy napisać
```javascript
total = total + 2;
```
Działa! Ale istnieje krótszy sposób na zrobienie tego samego:
```javascript
total += 2;
```
Znak `+=` nazywa się **operatorem przypisania z dodawaniem**.
Dodaje wartość do wartości zmiennej i przypisuje wynik do tej zmiennej.

# --instructions--

Dodaj do zmiennej `num` wartość `15` używając operatora *przypisania z dodawaniem*, a następnie wyświetl jej wartość

# --seed--

```javascript
var num = 75;
[/][/][/];
console.log(num);
```

# --answers--

- +
- +=
- =+
- num 
-  15

# --solutions--

```javascript
var num = 75;
num += 15;
console.log(num);
```

# --output--

90
