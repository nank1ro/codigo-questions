---
language: javascript
exerciseType: 2
---

# --description--

Wiesz już, że `extends` czyni jedną klasę potomną drugiej. To, co to słowo kluczowe naprawdę daje, to **dziedziczenie**: klasa potomna dostaje za darmo każdą właściwość i metodę klasy nadrzędnej i może dodawać na nich własne.

Elementem, który czyni dziedziczenie użytecznym, jest **`super`**. Wewnątrz konstruktora klasy potomnej `super(...)` wywołuje konstruktor klasy nadrzędnej, dzięki czemu rodzic może skonfigurować tę część obiektu, do której należy:
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
```
Tutaj `super(name)` przekazuje `name` do `Animal`, które je zapisuje, a `Dog` musi już martwić się tylko o `breed`.

# --instructions--

Uzupełnij kod tak, aby `Dog` dziedziczył po `Animal` i przekazywał `name` do konstruktora klasy nadrzędnej

# --seed--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog [/] Animal {
    constructor(name, breed) {
        [/](name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --answers--

- extends
- inherits
- super
- parent
- new Animal

# --solutions--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --output--

Max is a Beagle
