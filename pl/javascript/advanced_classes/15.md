---
language: javascript
exerciseType: 1
---

# --description--

Operator **`instanceof`** pyta, czy obiekt został zbudowany z danej klasy lub z dowolnej klasy, która po niej dziedziczy:
```javascript
class Animal {}
class Dog extends Animal {}
const max = new Dog();
console.log(max instanceof Dog);    // true
console.log(max instanceof Animal); // true, Dog extends Animal
```
JavaScript nie ma słowa kluczowego `abstract`, ale tę samą ideę zapisuje się ręcznie: klasa bazowa definiuje kształt, a każda metoda, którą klasa potomna *musi* dostarczyć, po prostu rzuca wyjątek:
```javascript
class Shape {
    area() {
        throw new Error("area() must be implemented");
    }
}
```
Klasa potomna, która zapomni nadpisać `area`, głośno zawodzi przy pierwszym użyciu, zamiast po cichu zwracać `undefined`.

# --instructions--

Napisz klasę `Shape`, której metoda `area` rzuca `Error`, oraz klasę `Square`, która po niej dziedziczy, przyjmuje `side` i nadpisuje `area`, aby zwracała pole kwadratu

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Shape {
  
}

class Square {
  
}
```

# --asserts--

`new Square(3).area()` musi zwrócić `9`.

```javascript
tryCatch(new Square(3).area() === 9);
```

`new Square(5).area()` musi zwrócić `25`.

```javascript
tryCatch(new Square(5).area() === 25);
```

Kwadrat musi być `Shape`.

```javascript
tryCatch(new Square(3) instanceof Shape === true);
```

Wywołanie `area` na klasie bazowej musi rzucić wyjątek.

```javascript
let _threw = false;
try {
  new Shape().area();
} catch (e) {
  _threw = true;
}
tryCatch(_threw === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Shape {
  area() {
    throw new Error("area() must be implemented");
  }
}

class Square extends Shape {
  constructor(side) {
    super();
    this.side = side;
  }
  area() {
    return this.side * this.side;
  }
}
```
