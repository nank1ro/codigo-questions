---
language: javascript
exerciseType: 1
---

# --description--

Gdy klasa potomna definiuje metodę, którą ma już klasa nadrzędna, wygrywa wersja z klasy potomnej. Nazywa się to **nadpisywaniem**:
```javascript
class Animal {
    speak() {
        return "some sound";
    }
}
class Dog extends Animal {
    speak() {
        return "Woof";
    }
}
console.log(new Dog().speak());
// wypisuje Woof
```
Nadpisywanie nie usuwa wersji z klasy nadrzędnej, tylko ją ukrywa. Wewnątrz metody klasy potomnej `super.nazwaMetody(...)` nadal się do niej dociera, co pozwala rozszerzyć zachowanie klasy nadrzędnej zamiast je zastępować:
```javascript
class Puppy extends Dog {
    speak() {
        return super.speak() + "!";
    }
}
console.log(new Puppy().speak());
// wypisuje Woof!
```
Zwróć uwagę na różnicę: `super(...)` wywołuje **konstruktor** klasy nadrzędnej, a `super.nazwa(...)` wywołuje **metodę** klasy nadrzędnej.

# --instructions--

Napisz klasę `LoudGreeter`, która dziedziczy po `Greeter` i nadpisuje `greet` tak, aby zwracała powitanie klasy nadrzędnej wielkimi literami, po którym następuje wykrzyknik

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Greeter {
  constructor(name) {
    this.name = name;
  }
  greet() {
    return `Hello ${this.name}`;
  }
}

class LoudGreeter {
  
}
```

# --asserts--

`new LoudGreeter("Ann").greet()` musi zwrócić `"HELLO ANN!"`.

```javascript
tryCatch(new LoudGreeter("Ann").greet() === "HELLO ANN!");
```

`new LoudGreeter("bob").greet()` musi zwrócić `"HELLO BOB!"`.

```javascript
tryCatch(new LoudGreeter("bob").greet() === "HELLO BOB!");
```

Klasa nadrzędna musi pozostać nietknięta.

```javascript
tryCatch(new Greeter("Ann").greet() === "Hello Ann");
```

`LoudGreeter` musi ponownie użyć metody klasy nadrzędnej za pomocą `super`.

```json
{
  "regex": "super\\.greet\\s*\\(\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Greeter {
  constructor(name) {
    this.name = name;
  }
  greet() {
    return `Hello ${this.name}`;
  }
}

class LoudGreeter extends Greeter {
  greet() {
    return super.greet().toUpperCase() + "!";
  }
}
```
