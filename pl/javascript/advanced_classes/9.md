---
language: javascript
exerciseType: 4
---

# --description--

Bardzo częstym zastosowaniem metody statycznej jest **fabryka**: metoda, która buduje instancję z danych w innym kształcie i ją zwraca. Trzyma `new` w jednym miejscu i nadaje konstrukcji nazwę, która mówi, co robi:
```javascript
class Duration {
    constructor(seconds) {
        this.seconds = seconds;
    }
    static fromMinutes(minutes) {
        return new Duration(minutes * 60);
    }
}
console.log(Duration.fromMinutes(2).seconds);
// wypisuje 120
```
Fabrykę można wywołać, zanim jeszcze istnieje jakakolwiek instancja, czego zwykła metoda nie potrafi.

# --instructions--

Uporządkuj elementy tak, aby powstał działający program, który buduje punkt z tablicy i wypisuje jego współrzędne

# --answers--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
```

```javascript
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
```

```javascript
const p = Point.fromArray([3, 4]);
```

```javascript
console.log(`${p.x},${p.y}`);
```

# --solutions--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
const p = Point.fromArray([3, 4]);
console.log(`${p.x},${p.y}`);
```

# --output--

3,4
