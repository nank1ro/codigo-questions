---
language: javascript
exerciseType: 2
---

# --description--

Do tej pory każda właściwość była tworzona wewnątrz konstruktora. **Pole klasy** pozwala zadeklarować je bezpośrednio w ciele klasy, z opcjonalną wartością początkową:
```javascript
class Counter {
    count = 0;
    step = 1;
}
console.log(new Counter().count);
// wypisuje 0
```
Pola są przypisywane każdej nowej instancji, zanim wykona się ciało konstruktora, więc konstruktor może już na nich polegać. Pole bez wartości nadal jest deklarowane, po prostu zaczyna jako `undefined`:
```javascript
class Task {
    done = false;
    title;
}
```
Zwróć uwagę na składnię: żadnego `let`, żadnego `const`, żadnego `this` w deklaracji, a linia kończy się średnikiem.

# --instructions--

Uzupełnij klasę tak, aby `count` zaczynało od `0`, a `step` od `1`, a następnie wypisz licznik po dwóch zwiększeniach

# --seed--

```javascript
class Counter {
    [/] = 0;
    step [/] 1;
    increment() {
        this.count += this.step;
    }
}
const c = new Counter();
c.increment();
c.increment();
console.log(c.count);
```

# --answers--

- count
- let count
- this.count
- =
- :
- ==

# --solutions--

```javascript
class Counter {
    count = 0;
    step = 1;
    increment() {
        this.count += this.step;
    }
}
const c = new Counter();
c.increment();
c.increment();
console.log(c.count);
```

# --output--

2
