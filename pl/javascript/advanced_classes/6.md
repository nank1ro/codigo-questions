---
language: javascript
exerciseType: 4
---

# --description--

Kolejność ma znaczenie, gdy klasy po sobie dziedziczą. Deklaracja `class` **nie** jest hoistowana tak, jak `function`: nazwa istnieje dopiero od linii, w której zapisano klasę. Dlatego klasa potomna musi pojawić się *po* klasie nadrzędnej, po której dziedziczy, w przeciwnym razie klauzula `extends` kończy się błędem `ReferenceError`.

# --instructions--

Uporządkuj elementy tak, aby powstał działający program. Pamiętaj, że klasa nadrzędna musi być zdefiniowana przed klasą potomną

# --answers--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
```

```javascript
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
```

```javascript
console.log(new Car().describe());
```

# --solutions--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
console.log(new Car().describe());
```

# --output--

a vehicle with four wheels
