---
language: javascript
exerciseType: 1
---

# --description--

Klasa potomna nie musi ponownie definiować niczego, co dostarcza już klasa nadrzędna. Metody również są dziedziczone, więc instancja klasy potomnej może wywoływać je tak, jakby były jej własnymi:
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
    speak() {
        return `${this.name} makes a sound`;
    }
}
class Dog extends Animal {}
console.log(new Dog("Max").speak());
// wypisuje Max makes a sound
```
Gdy klasa potomna deklaruje własny konstruktor, wywołanie `super(...)` wewnątrz niego jest **obowiązkowe**: bez tego obiekt nigdy nie zostaje zainicjowany, a JavaScript rzuca `ReferenceError`. Klasa potomna bez żadnego konstruktora jest w porządku, ponieważ JavaScript pisze za nią taki, który przekazuje każdy argument do klasy nadrzędnej.

# --instructions--

Napisz klasę `Cat`, która dziedziczy po `Animal`, przyjmuje `name` i `lives`, przekazuje `name` do konstruktora klasy nadrzędnej i zapisuje `lives` w instancji

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Cat {
  
}
```

# --asserts--

`new Cat("Kitty", 9).name` musi zwrócić `"Kitty"`.

```javascript
tryCatch(new Cat("Kitty", 9).name === "Kitty");
```

`new Cat("Kitty", 9).lives` musi wynosić `9`.

```javascript
tryCatch(new Cat("Kitty", 9).lives === 9);
```

`Cat` musi dziedziczyć po `Animal`.

```javascript
tryCatch(new Cat("Kitty", 9) instanceof Animal);
```

`Cat` musi dziedziczyć `speak` po `Animal`.

```javascript
tryCatch(new Cat("Kitty", 9).speak() === "Kitty makes a sound");
```

Drugi kot musi zachowywać swoje własne wartości.

```javascript
tryCatch(new Cat("Tom", 7).lives === 7);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Cat extends Animal {
  constructor(name, lives) {
    super(name);
    this.lives = lives;
  }
}
```
