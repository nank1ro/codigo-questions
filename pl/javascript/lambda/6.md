---
language: javascript
exerciseType: 4
---

# --description--

Jeszcze dwie metody tablic przyjmują callback.
`forEach(callback)` wywołuje callback raz dla każdego elementu i nic nie zwraca; używa się jej do efektów ubocznych, takich jak wypisywanie.
`reduce(callback, initialValue)` zwija tablicę do pojedynczej wartości: callback otrzymuje dotychczasową skumulowaną wartość oraz bieżący element i zwraca nową skumulowaną wartość:
```javascript
const numbers = [1, 2, 3];
numbers.forEach((n) => console.log(n));
// wypisuje 1, 2 i 3 w trzech liniach
const total = numbers.reduce((sum, n) => sum + n, 0);
console.log(total);
// wypisuje 6
```

# --instructions--

Uporządkuj linie tak, aby obliczyć sumę `prices` za pomocą `reduce` zaraz po zadeklarowaniu tablicy, wypisać każdą cenę w osobnej linii za pomocą `forEach`, a następnie wypisać sumę

# --answers--

```javascript
const prices = [4, 6, 10];
```

```javascript
const total = prices.reduce((sum, p) => sum + p, 0);
```

```javascript
prices.forEach((p) => console.log(p));
```

```javascript
console.log(total);
```

# --solutions--

```javascript
const prices = [4, 6, 10];
const total = prices.reduce((sum, p) => sum + p, 0);
prices.forEach((p) => console.log(p));
console.log(total);
```

# --output--

4
6
10
20
