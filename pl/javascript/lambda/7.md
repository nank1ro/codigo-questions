---
language: javascript
exerciseType: 2
---

# --description--

`sort(compare)` porządkuje tablicę w miejscu za pomocą callbacka, który otrzymuje dwa elementy i zwraca liczbę ujemną, gdy pierwszy powinien być pierwszy, dodatnią, gdy drugi powinien być pierwszy, albo `0`, gdy są równe. Dla liczb `(a, b) => a - b` sortuje rosnąco, a `(a, b) => b - a` malejąco.
`find(callback)` zwraca pierwszy element, dla którego callback zwraca `true`, albo `undefined`, gdy takiego nie ma:
```javascript
const scores = [50, 90, 70];
scores.sort((a, b) => a - b);
console.log(scores);
// prints [ 50, 70, 90 ]
console.log(scores.find((s) => s > 60));
// prints 70
```

# --instructions--

Posortuj `ages` rosnąco, następnie znajdź pierwszy wiek ściśle większy od `21` i wypisz zarówno posortowaną tablicę, jak i ten wiek

# --seed--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a [/] b);
const adult = ages.find((age) => age [/] 21);
console.log(ages);
console.log(adult);
```

# --answers--

- -
- >
- +
- <
- ===
- *

# --solutions--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a - b);
const adult = ages.find((age) => age > 21);
console.log(ages);
console.log(adult);
```

# --output--

[ 18, 25, 31 ]
25
