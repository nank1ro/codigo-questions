---
language: javascript
exerciseType: 4
---

# --instructions--

Uporządkuj linie, aby zdefiniować `makeCounter`, który zwraca funkcję strzałkową inkrementującą i zwracającą swój prywatny `count`, następnie utwórz licznik `next` i wypisz wynik dwóch wywołań w tej samej linii

# --answers--

```javascript
const makeCounter = () => {
```

```javascript
  let count = 0;
```

```javascript
  return () => {
```

```javascript
    count += 1;
```

```javascript
    return count;
```

```javascript
  };
```

```javascript
};
```

```javascript
const next = makeCounter();
```

```javascript
console.log(next(), next());
```

# --solutions--

```javascript
const makeCounter = () => {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
};
const next = makeCounter();
console.log(next(), next());
```

# --output--

1 2
