---
language: javascript
exerciseType: 4
---

# --description--

Funkcja `async` czyta się z góry na dół jak każda inna funkcja: `await` po prostu wstrzymuje ją, dopóki oczekiwana obietnica nie zostanie spełniona, a potem wykonanie biegnie dalej w następnej linii.
```javascript
async function main() {
  console.log("start");
  const value = await Promise.resolve("data");
  console.log(value);
  console.log("done");
}

main();
// prints start, then data, then done
```
Zwróć uwagę na ostatnią linię: funkcję `async` nadal trzeba **wywołać**. Napisanie `main` bez nawiasów definiuje pracę, ale nigdy jej nie rozpoczyna i nic nie zostaje wypisane.

# --instructions--

Ułóż elementy w program, który definiuje funkcję `async` `load` zwracającą `data`, a potem funkcję `async` `main`, która wypisuje `start`, oczekuje na `load()` w `value`, wypisuje `value` i na koniec wypisuje `done`. Zdefiniuj najpierw `load`, potem `main`, a `main()` wywołaj w ostatniej linii

# --answers--

```javascript
async function load() {
  return "data";
}
```

```javascript
async function main() {
```

```javascript
  console.log("start");
```

```javascript
  const value = await load();
```

```javascript
  console.log(value);
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
async function load() {
  return "data";
}
async function main() {
  console.log("start");
  const value = await load();
  console.log(value);
  console.log("done");
}
main();
```

# --output--

start
data
done
