---
language: javascript
exerciseType: 1
---

# --description--

Praca asynchroniczna też może się nie udać. Funkcja przekazana do `new Promise` otrzymuje drugi callback, **`reject`**: wywołaj `reject(error)`, a obietnica stanie się odrzucona zamiast spełnionej.
```javascript
function readAge(age) {
  return new Promise((resolve, reject) => {
    if (age >= 0) {
      resolve(age);
    } else {
      reject(new Error("negative age"));
    }
  });
}
```
Zawsze odrzucaj obiektem `Error`: niesie on `message` i ślad stosu, czego nie robi sam ciąg znaków.

Odrzucenie odczytuje się za pomocą **`.catch(callback)`**, lustrzanego odbicia `.then`. **`Promise.reject(error)`** buduje obietnicę już odrzuconą, tak jak `Promise.resolve` buduje spełnioną:
```javascript
readAge(-1).catch((error) => {
  console.log(error.message);
  // prints negative age
});
```
Wywołanie zarówno `resolve`, jak i `reject`, albo dwukrotne wywołanie, nic nie zmienia: liczy się tylko pierwsze wywołanie.

# --instructions--

Napisz funkcję `checkAge`, która przyjmuje liczbę `age` i zwraca obietnicę spełnioną z `age`, gdy `age` ma co najmniej `18`, a odrzuconą z `new Error("too young")` w przeciwnym razie. Użyj `new Promise`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function checkAge(age) {
  
}
```

# --asserts--

`await checkAge(20)` musi dawać `20`.

```javascript
(async () => {
  tryCatch((await checkAge(20)) === 20);
})().catch(() => tryCatch(false));
```

`await checkAge(18)` musi dawać `18`, ponieważ `18` to wystarczający wiek.

```javascript
(async () => {
  tryCatch((await checkAge(18)) === 18);
})().catch(() => tryCatch(false));
```

`checkAge(10)` musi zostać odrzucona z komunikatem `"too young"`.

```javascript
(async () => {
  let message = "";
  await checkAge(10).catch((error) => { message = error.message; });
  tryCatch(message === "too young");
})().catch(() => tryCatch(false));
```

`checkAge(0)` musi zostać odrzucona obiektem `Error`, a nie ciągiem znaków.

```javascript
(async () => {
  let reason = null;
  await checkAge(0).catch((error) => { reason = error; });
  tryCatch(reason instanceof Error);
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function checkAge(age) {
  return new Promise((resolve, reject) => {
    if (age >= 18) {
      resolve(age);
    } else {
      reject(new Error("too young"));
    }
  });
}
```
