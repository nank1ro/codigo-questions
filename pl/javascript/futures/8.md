---
language: javascript
exerciseType: 2
---

# --description--

`.then`, `.catch` i `.finally` to ogniwa tego samego łańcucha. Odrzucenie pomija każde `.then`, aż trafi na `.catch`; gdy callback `.catch` zwróci wartość, łańcuch znów jest spełniony i biegnie normalnie.

**`.finally(callback)`** wykonuje się, gdy łańcuch się rozstrzygnie, niezależnie od tego, czy został spełniony, czy odrzucony. Jego callback nie przyjmuje argumentu, a jego wartość zwracana jest ignorowana, więc wartość dalej płynie do następnego `.then`. To miejsce na porządki, takie jak ukrycie spinnera:
```javascript
Promise.reject(new Error("no network"))
  .then((value) => `ok: ${value}`)
  .catch((error) => `error: ${error.message}`)
  .finally(() => console.log("cleanup"))
  .then((message) => console.log(message));
// prints cleanup, then error: no network
```

# --instructions--

Uzupełnij łańcuch tak, aby niepowodzenie zostało zamienione na tekst `error: ` połączony z komunikatem, `cleanup` było wypisywane niezależnie od tego, co się stanie, a tekst został wypisany na końcu

# --seed--

```javascript
function load(ok) {
  return ok ? Promise.resolve("data") : Promise.reject(new Error("no network"));
}

load(false)
  .then((value) => `ok: ${value}`)
  .[/]((error) => `error: ${error.message}`)
  .[/](() => console.log("cleanup"))
  .then((message) => console.log(message));
```

# --answers--

- catch
- finally
- then
- reject
- always

# --solutions--

```javascript
function load(ok) {
  return ok ? Promise.resolve("data") : Promise.reject(new Error("no network"));
}

load(false)
  .then((value) => `ok: ${value}`)
  .catch((error) => `error: ${error.message}`)
  .finally(() => console.log("cleanup"))
  .then((message) => console.log(message));
```

# --output--

cleanup
error: no network
