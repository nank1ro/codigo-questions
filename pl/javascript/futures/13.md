---
language: javascript
exerciseType: 4
---

# --description--

Różnica między czekaniem **sekwencyjnym** a **równoległym** zależy od tego, *gdzie* postawisz `await`:
```javascript
// sequential: about 300 + 300 = 600 ms
const a = await load("a");
const b = await load("b");

// parallel: about 300 ms
const [a, b] = await Promise.all([load("a"), load("b")]);
```
W pierwszej wersji drugie pobieranie zaczyna się dopiero, gdy pierwsze się skończy, ponieważ `await` wstrzymuje funkcję w tej linii. W drugiej oba wywołania są wykonane, zanim cokolwiek będziemy oczekiwać, więc oba pobierania już działają, podczas gdy `Promise.all` czeka.

Używaj sekwencyjnych `await` tylko wtedy, gdy drugie zadanie naprawdę potrzebuje wyniku pierwszego. W przeciwnym razie uruchom najpierw wszystko, a oczekuj razem.

# --instructions--

Ułóż elementy w program, który definiuje `later`, a potem funkcję `async` `main`, która wypisuje `loading`, uruchamia oba wywołania `later` razem za pomocą `Promise.all`, wypisuje dwa wyniki połączone spacją i na koniec wypisuje `done`. Zdefiniuj najpierw `later`, potem `main`, a `main()` wywołaj w ostatniej linii

# --answers--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
```

```javascript
async function main() {
```

```javascript
  console.log("loading");
```

```javascript
  const results = await Promise.all([later("one", 30), later("two", 10)]);
```

```javascript
  console.log(results.join(" "));
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
async function main() {
  console.log("loading");
  const results = await Promise.all([later("one", 30), later("two", 10)]);
  console.log(results.join(" "));
  console.log("done");
}
main();
```

# --output--

loading
ready two
ready one
one two
done
