---
language: javascript
exerciseType: 4
---

# --description--

Komparator może patrzeć na dowolną część elementów, więc tablicę obiektów sortuje się według jednej z ich właściwości, po prostu porównując tę właściwość:
```javascript
const items = [{ name: "b", size: 3 }, { name: "a", size: 1 }];
const bySize = [...items].sort((x, y) => x.size - y.size);
console.log(bySize.map((item) => item.name));
// prints [ 'a', 'b' ]
```
Sortowanie kopii zostawia `items` w pierwotnej kolejności.

# --instructions--

Uporządkuj wiersze tak, aby posortować kopię `players` rosnąco po `score`, wypisać ich imiona w tej kolejności za pomocą `map`, a potem wypisać imię pierwszego elementu `players`, aby pokazać, że się nie zmienił. Zadeklaruj `ranked` przed `names`

# --answers--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
```

```javascript
const ranked = [...players].sort((a, b) => a.score - b.score);
```

```javascript
const names = ranked.map((p) => p.name);
```

```javascript
console.log(names);
```

```javascript
console.log(players[0].name);
```

# --solutions--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
const ranked = [...players].sort((a, b) => a.score - b.score);
const names = ranked.map((p) => p.name);
console.log(names);
console.log(players[0].name);
```

# --output--

[ 'Leo', 'Zoe', 'Mia' ]
Zoe
