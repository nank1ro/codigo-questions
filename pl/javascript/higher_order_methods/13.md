---
language: javascript
exerciseType: 1
---

# --description--

Każda funkcja ma metodę `bind`, która zwraca **nową** funkcję z częścią rzeczy ustaloną z góry. Jej pierwszy argument staje się `this` nowej funkcji; pozostałe argumenty trafiają przed te, z którymi nowa funkcja zostanie wywołana (**częściowa aplikacja**):
```javascript
function multiply(a, b) {
  return a * b;
}
const double = multiply.bind(null, 2);
console.log(double(21));
// prints 42
```
Ustalenie `this` ma znaczenie dla metod. Gdy metodę skopiujemy poza jej obiekt i wywołamy samodzielnie, `this` nie wskazuje już obiektu, więc `this.name` staje się `undefined`. `bind` przypina je do obiektu:
```javascript
const user = {
  name: "Ana",
  hello() { return `Hi ${this.name}`; },
};
const loose = user.hello;
console.log(loose());
// prints Hi undefined
const bound = user.hello.bind(user);
console.log(bound());
// prints Hi Ana
```
Oryginalna funkcja nigdy się nie zmienia: `bind` zawsze buduje nową, której `name` to pierwotna nazwa poprzedzona `bound `.

# --instructions--

Funkcja `power` i obiekt `counter` są dane. Zapisz w stałej `square` wersję `power` związaną z `this` ustawionym na `null` i `exponent` ustalonym na `2`, a w stałej `tick` wersję `counter.increment` związaną z `counter`, tak aby działała także wywołana samodzielnie

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = 
const tick = 
```

# --asserts--

`square(3)` musi zwrócić `9`.

```javascript
tryCatch(square(3) === 9);
```

`square(12)` musi zwrócić `144`.

```javascript
tryCatch(square(12) === 144);
```

`square` musi zostać utworzone przez `bind`, więc jego nazwa to `"bound power"`.

```javascript
tryCatch(square.name === "bound power");
```

`tick` musi być nową funkcją utworzoną przez `bind`, a nie samym `counter.increment`.

```javascript
tryCatch(tick !== counter.increment && tick.name === "bound increment");
```

Dwukrotne samodzielne wywołanie `tick()` musi zwrócić `1`, a potem `2`, a `counter.count` musi wynosić `2`.

```javascript
tryCatch(tick() === 1 && tick() === 2 && counter.count === 2);
```

`tick` musi nadal działać, gdy zapiszemy je w innej zmiennej i wywołamy stamtąd.

```javascript
tryCatch((() => { const alone = tick; return alone() === 3; })());
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = power.bind(null, 2);
const tick = counter.increment.bind(counter);
```
