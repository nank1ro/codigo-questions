---
language: javascript
exerciseType: 2
---

# --description--

W JavaScripcie funkcja jest **wartością**: możesz zapisać ją w zmiennej, umieścić w tablicy i przekazać innej funkcji jako argument. Funkcja, która przyjmuje funkcję jako argument albo zwraca funkcję, nazywa się **funkcją wyższego rzędu**. Przekazana funkcja nazywa się **callbackiem**, ponieważ odbiorca *wywołuje ją zwrotnie*, gdy jej potrzebuje:
```javascript
function shout(text) {
  return text.toUpperCase() + "!";
}
function twice(fn, value) {
  return fn(fn(value));
}
console.log(twice(shout, "hi"));
// prints HI!!
```
Zwróć uwagę, że `shout` przekazujemy **bez nawiasów**: `twice(shout, "hi")` przekazuje samą funkcję, natomiast `twice(shout("hi"), "hi")` najpierw wywołałoby `shout` i przekazało jego wynik, łańcuch `"HI!"`, którego nie da się wywołać.

# --instructions--

Uzupełnij `applyTo` tak, aby zwracała wynik wywołania `fn` z `value`, a następnie wywołaj ją z `double` i z `negate` (przekazując same funkcje), obie z `21`, uzyskując wynik `42 -21`

# --seed--

```javascript
function double(n) {
  return n * 2;
}
function negate(n) {
  return -n;
}
function applyTo(fn, value) {
  return [/](value);
}
console.log(applyTo([/], 21), applyTo([/], 21));
```

# --answers--

- fn
- double
- negate
- double()
- negate(21)
- callback

# --solutions--

```javascript
function double(n) {
  return n * 2;
}
function negate(n) {
  return -n;
}
function applyTo(fn, value) {
  return fn(value);
}
console.log(applyTo(double, 21), applyTo(negate, 21));
```

# --output--

42 -21
