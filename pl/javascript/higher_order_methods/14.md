---
language: javascript
exerciseType: 4
---

# --description--

Prawdziwe programy łączą te metody w **potok**: odfiltruj interesujące cię elementy, przekształć je na potrzebne wartości i zredukuj do wyniku. Zapisywanie pośrednich tablic w stałych utrzymuje czytelność każdego kroku i pozwala je ponownie wykorzystać:
```javascript
const adults = people.filter((p) => p.age >= 18);
const names = adults.map((p) => p.name);
const totalAge = adults.reduce((sum, p) => sum + p.age, 0);
```
`names.join(", ")` zamienia tablicę łańcuchów w jeden łańcuch, w którym elementy są oddzielone przecinkiem i spacją.

# --instructions--

Uporządkuj wiersze tak, aby zostawić tylko pozycje koszyka z dodatnim `qty`, zebrać ich nazwy, zsumować dla nich `price * qty` i wypisać nazwy połączone przez `", "` oraz sumę w jednym wierszu. Wypisz pozycje w kolejności pen, book, bag i zadeklaruj `inStock`, `names` i `total` w tej kolejności

# --answers--

```javascript
const cart = [
```

```javascript
  { name: "pen", price: 2, qty: 3 },
```

```javascript
  { name: "book", price: 12, qty: 1 },
```

```javascript
  { name: "bag", price: 30, qty: 0 },
```

```javascript
];
```

```javascript
const inStock = cart.filter((item) => item.qty > 0);
```

```javascript
const names = inStock.map((item) => item.name);
```

```javascript
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
```

```javascript
console.log(names.join(", "), total);
```

# --solutions--

```javascript
const cart = [
  { name: "pen", price: 2, qty: 3 },
  { name: "book", price: 12, qty: 1 },
  { name: "bag", price: 30, qty: 0 },
];
const inStock = cart.filter((item) => item.qty > 0);
const names = inStock.map((item) => item.name);
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
console.log(names.join(", "), total);
```

# --output--

pen, book 18
