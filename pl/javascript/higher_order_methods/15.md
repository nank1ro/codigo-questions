---
language: javascript
exerciseType: 1
---

# --description--

Domknięcia pozwalają zwróconej funkcji zachować **prywatny stan** między wywołaniami. Klasycznym tak zbudowanym pomocnikiem jest `once(fn)`: zwraca funkcję, która uruchamia `fn` tylko przy pierwszym wywołaniu, zapamiętuje wynik i przy każdym kolejnym wywołaniu zwraca ten sam wynik bez ponownego uruchamiania `fn`:
```javascript
let calls = 0;
const init = once(() => {
  calls += 1;
  return "ready";
});
console.log(init(), init(), calls);
// prints ready ready 1
```
Opakowanie potrzebuje dwóch prywatnych zmiennych: czy `fn` już się uruchomiło oraz zapamiętanego wyniku. Obie żyją w domknięciu, niewidoczne z zewnątrz. Aby przekazać do `fn` wszystkie argumenty opakowania, zadeklaruj je z parametrem reszty `(...args)` i wywołaj `fn(...args)`.

# --instructions--

Napisz funkcję `once(fn)`, która zwraca funkcję wywołującą `fn` z tymi samymi argumentami tylko przy pierwszym wywołaniu i zwracającą wynik `fn`, a przy każdym kolejnym wywołaniu zwracającą ten zapamiętany wynik bez ponownego wywołania `fn`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function once(fn) {

}
```

# --asserts--

Opakowana funkcja musi uruchomić `fn` tylko raz i dalej zwracać jej wynik.

```javascript
tryCatch((() => { let calls = 0; const f = once(() => { calls += 1; return 42; }); f(); f(); f(); return f() === 42 && calls === 1; })());
```

Argumenty pierwszego wywołania muszą trafić do `fn`: `once((a, b) => a + b)(2, 3)` musi zwrócić `5`.

```javascript
tryCatch(once((a, b) => a + b)(2, 3) === 5);
```

Kolejne wywołania muszą zwracać zapamiętany wynik nawet z innymi argumentami.

```javascript
tryCatch((() => { const add = once((a, b) => a + b); add(1, 1); return add(5, 5) === 2; })());
```

`fn` nie może uruchomić się ponownie nawet wtedy, gdy jej wynikiem jest `undefined`.

```javascript
tryCatch((() => { let calls = 0; const f = once(() => { calls += 1; }); f(); f(); return calls === 1 && f() === undefined; })());
```

Dwa opakowania tej samej funkcji muszą być niezależne.

```javascript
tryCatch((() => { let n = 0; const inc = () => { n += 1; return n; }; const a = once(inc); const b = once(inc); a(); a(); b(); b(); return a() === 1 && b() === 2 && n === 2; })());
```

`once` musi zwrócić funkcję.

```javascript
tryCatch(typeof once(() => {}) === "function");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function once(fn) {
  let done = false;
  let result;
  return (...args) => {
    if (!done) {
      done = true;
      result = fn(...args);
    }
    return result;
  };
}
```
