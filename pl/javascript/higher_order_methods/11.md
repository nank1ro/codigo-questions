---
language: javascript
exerciseType: 1
---

# --description--

Funkcja zwracająca funkcję to również naturalny sposób na **dostosowanie** callbacku. Załóżmy, że masz predykat, a do `filter` potrzebujesz jego przeciwieństwa: zamiast pisać go od nowa, opakuj go:
```javascript
const isLong = (word) => word.length > 4;
const isShort = (word) => !isLong(word);
console.log(["tree", "forest"].filter(isShort));
// prints [ 'tree' ]
```
Ogólne `not(predicate)` zrobiłoby to dla dowolnego predykatu: zwraca nową funkcję, która wywołuje `predicate` z tym samym argumentem i odwraca wynik przez `!`. Predykaty dla `filter`, `find`, `some` i `every` dostają element jako pierwszy argument, więc opakowanie musi przekazać dalej tylko tę jedną wartość.

# --instructions--

Napisz funkcję `not(predicate)`, która zwraca nowy predykat zwracający `true` dokładnie wtedy, gdy `predicate` zwraca `false` dla tego samego argumentu

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function not(predicate) {

}
```

# --asserts--

`not((n) => n % 2 === 0)(3)` musi zwrócić `true`.

```javascript
tryCatch(not((n) => n % 2 === 0)(3) === true);
```

`not((n) => n % 2 === 0)(4)` musi zwrócić `false`.

```javascript
tryCatch(not((n) => n % 2 === 0)(4) === false);
```

`[1, 2, 3, 4].filter(not(isEven))` musi zwrócić `[1, 3]`.

```javascript
tryCatch((() => { const isEven = (n) => n % 2 === 0; return JSON.stringify([1, 2, 3, 4].filter(not(isEven))) === "[1,3]"; })());
```

`["a", "", "b"].filter(not((s) => s === ""))` musi zwrócić `["a", "b"]`.

```javascript
tryCatch(JSON.stringify(["a", "", "b"].filter(not((s) => s === ""))) === '["a","b"]');
```

Dwukrotne zaprzeczenie musi dać pierwotną odpowiedź: `not(not(isEven))(4)` musi zwrócić `true`.

```javascript
tryCatch(not(not((n) => n % 2 === 0))(4) === true);
```

`not` musi zwrócić funkcję.

```javascript
tryCatch(typeof not((n) => n > 0) === "function");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function not(predicate) {
  return (value) => !predicate(value);
}
```
