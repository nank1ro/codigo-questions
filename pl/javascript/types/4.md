---
language: javascript
exerciseType: 2
---

# --description--

Wartość można przekonwertować na inny typ **jawnie**, wywołując typ jak funkcję:
- `Number(value)` konwertuje na liczbę: `Number("42")` to `42`
- `String(value)` konwertuje na ciąg znaków: `String(42)` to `"42"`
- `Boolean(value)` konwertuje na wartość logiczną: `Boolean("")` to `false`

Wynikiem jest zupełnie nowa wartość; oryginał nie ulega zmianie:
```javascript
const input = "7";
const count = Number(input) + 1;
console.log(count, typeof count);
// prints 8 number
console.log(String(count) + "!");
// prints 8!
```
Jawna konwersja uwidacznia twoje intencje: każdy, kto czyta `Number(input)`, wie, że `input` był tekstem.

# --instructions--

Uzupełnij kod tak, aby `count` było `input` przekonwertowanym na liczbę plus `1`, `label` było `count` przekonwertowanym na ciąg znaków i powtórzonym dwukrotnie, a `flag` było pustym ciągiem znaków przekonwertowanym na wartość logiczną, dając na wyjściu `43 4343 false`

# --seed--

```javascript
const input = "42";
const count = [/](input) + 1;
const label = [/](count).repeat(2);
const flag = [/]("");
console.log(count, label, flag);
```

# --answers--

- Number
- String
- Boolean
- Int
- Bool
- toString

# --solutions--

```javascript
const input = "42";
const count = Number(input) + 1;
const label = String(count).repeat(2);
const flag = Boolean("");
console.log(count, label, flag);
```

# --output--

43 4343 false
