---
language: javascript
exerciseType: 1
---

# --description--

`Number()` jest rygorystyczna: cały ciąg znaków musi być liczbą, w przeciwnym razie wynikiem jest `NaN` ("Not a Number"):
```javascript
console.log(Number("12px"));
// prints NaN
```
`parseInt()` i `parseFloat()` są łagodniejsze: czytają cyfry od początku ciągu znaków, pomijają wiodące spacje i zatrzymują się na pierwszym znaku, który nie jest częścią liczby. `parseInt` zachowuje tylko część całkowitą:
```javascript
console.log(parseInt("12px"), parseFloat("1.5kg"));
// prints 12 1.5
console.log(parseInt("3.9em"), parseInt("-4px"));
// prints 3 -4
```
Gdy ciąg znaków nie zaczyna się od czegoś, co może rozpocząć liczbę (opcjonalny znak, a potem cyfra), one również zwracają `NaN`:
```javascript
console.log(parseInt("auto"));
// prints NaN
```
`NaN` to jedyna wartość, która nie jest równa samej sobie, więc `x === NaN` jest zawsze `false`; aby ją wykryć, użyj `Number.isNaN(x)`.

# --instructions--

Napisz funkcję `toPixels(text)`, która zwraca liczbę całkowitą znajdującą się na początku `text` (na przykład `12` dla `"12px"`), albo `0`, gdy `text` nie zaczyna się od liczby, używając `parseInt`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function toPixels(text) {

}
```

# --asserts--

`toPixels("12px")` musi zwrócić `12`.

```javascript
tryCatch(toPixels("12px") === 12);
```

`toPixels("3.9em")` musi zwrócić `3`.

```javascript
tryCatch(toPixels("3.9em") === 3);
```

`toPixels("-4px")` musi zwrócić `-4`.

```javascript
tryCatch(toPixels("-4px") === -4);
```

`toPixels("auto")` musi zwrócić `0`.

```javascript
tryCatch(toPixels("auto") === 0);
```

`toPixels("")` musi zwrócić `0`.

```javascript
tryCatch(toPixels("") === 0);
```

Funkcja musi używać `parseInt`.

```javascript
tryCatch(/parseInt\(/.test(toPixels.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function toPixels(text) {
  const value = parseInt(text);
  if (Number.isNaN(value)) {
    return 0;
  }
  return value;
}
```
