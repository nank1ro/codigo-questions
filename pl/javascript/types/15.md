---
language: javascript
exerciseType: 1
---

# --description--

Ponieważ `typeof` odpowiada `"object"` dla tablic, obiektów i `null`, rozróżnienie ich wymaga dwóch dodatkowych sprawdzeń.
`Array.isArray(value)` jest `true` tylko dla tablic:
```javascript
console.log(Array.isArray([1, 2]), Array.isArray({}));
// prints true false
```
Dla `null` porównaj bezpośrednio, `value === null`. Połączenie tego daje pełny obraz dowolnej wartości:
```javascript
function kind(value) {
  if (value === null) return "null";
  if (Array.isArray(value)) return "array";
  return typeof value;
}
```
Najpierw sprawdź `null` i tablice, ponieważ zwykły `typeof` nie potrafi ich rozróżnić.

# --instructions--

Napisz funkcję `typeName(value)`, która zwraca `"null"` dla `null`, `"array"` dla tablic oraz wynik `typeof` dla każdej innej wartości, używając `Array.isArray`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function typeName(value) {

}
```

# --asserts--

`typeName(null)` musi zwrócić `"null"`.

```javascript
tryCatch(typeName(null) === "null");
```

`typeName([1, 2])` i `typeName([])` muszą zwrócić `"array"`.

```javascript
tryCatch(typeName([1, 2]) === "array" && typeName([]) === "array");
```

`typeName({})` musi zwrócić `"object"`.

```javascript
tryCatch(typeName({}) === "object");
```

`typeName(42)`, `typeName("a")` i `typeName(true)` muszą zwrócić `"number"`, `"string"` i `"boolean"`.

```javascript
tryCatch(typeName(42) === "number" && typeName("a") === "string" && typeName(true) === "boolean");
```

`typeName(undefined)`, `typeName(10n)` i `typeName(Symbol())` muszą zwrócić `"undefined"`, `"bigint"` i `"symbol"`.

```javascript
tryCatch(typeName(undefined) === "undefined" && typeName(10n) === "bigint" && typeName(Symbol()) === "symbol");
```

`typeName(function () {})` musi zwrócić `"function"`.

```javascript
tryCatch(typeName(function () {}) === "function");
```

Funkcja musi używać `Array.isArray`.

```javascript
tryCatch(/Array\.isArray\(/.test(typeName.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function typeName(value) {
  if (value === null) {
    return "null";
  }
  if (Array.isArray(value)) {
    return "array";
  }
  return typeof value;
}
```
