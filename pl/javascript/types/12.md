---
language: javascript
exerciseType: 4
---

# --description--

JavaScript ma jeden typ `number` dla liczb całkowitych i dziesiętnych: każda liczba jest 64-bitową wartością zmiennoprzecinkową (*double*). Dlatego `5` i `5.0` to ta sama wartość, a osobnego typu całkowitoliczbowego nie ma:
```javascript
console.log(5 === 5.0, 10 / 2);
// prints true 5
```
Aby zapytać, czy liczba nie ma części ułamkowej, użyj `Number.isInteger`:
```javascript
console.log(Number.isInteger(5.0), Number.isInteger(3.5));
// prints true false
```
Literały szablonowe konwertują interpolowaną wartość na ciąg znaków według tych samych reguł co `String()`, więc `${5.0}` staje się `"5"`, a nie `"5.0"`.

# --instructions--

Ułóż linie kodu tak, aby zdefiniować `half`, która dzieli `n` przez dwa i zwraca zdanie mówiące, czy wynik jest liczbą całkowitą, a następnie wypisz wynik dla `10` i dla `7`. Najpierw zdefiniuj funkcję

# --answers--

```javascript
function half(n) {
```

```javascript
  const result = n / 2;
```

```javascript
  if (Number.isInteger(result)) {
```

```javascript
    return `${result} is whole`;
```

```javascript
  }
```

```javascript
  return `${result} is not whole`;
```

```javascript
}
```

```javascript
console.log(half(10));
```

```javascript
console.log(half(7));
```

# --solutions--

```javascript
function half(n) {
  const result = n / 2;
  if (Number.isInteger(result)) {
    return `${result} is whole`;
  }
  return `${result} is not whole`;
}
console.log(half(10));
console.log(half(7));
```

# --output--

5 is whole
3.5 is not whole
