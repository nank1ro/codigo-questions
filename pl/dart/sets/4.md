---
language: dart
exerciseType: 2
---

# --description--

Metoda `.remove(value)` usuwa wartość ze zbioru. Zwraca `true`, jeśli wartość tam była, i `false` w przeciwnym razie:

```dart
var numbers = {1, 2, 3};
print(numbers.remove(2)); // true
print(numbers.remove(9)); // false
print(numbers); // {1, 3}
```

Aby usunąć wszystkie elementy naraz, użyj `.clear()`.

# --instructions--

Uzupełnij kod, aby usunąć liczbę `2` ze zbioru, a następnie go wypisz.

# --seed--

```dart
var numbers = {1, 2, 3};
numbers.[/](2);
print(numbers);
```

# --answers--

- remove
- removeAt
- delete
- drop
- clear
- pop

# --solutions--

```dart
var numbers = {1, 2, 3};
numbers.remove(2);
print(numbers);
```

# --output--

{1, 3}
