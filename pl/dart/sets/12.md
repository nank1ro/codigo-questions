---
language: dart
exerciseType: 1
---

# --description--

Konwersja listy na zbiór to najprostszy sposób na **usunięcie duplikatów**: każda lista ma metodę `.toSet()`, która zwraca zbiór jej unikalnych elementów, w kolejności pierwszego wystąpienia. Zbiór ma metodę `.toList()`, która działa w drugą stronę, więc połączenie obu daje listę bez duplikatów:

```dart
var votes = ['a', 'b', 'a', 'c', 'b'];
Set<String> unique = votes.toSet();
print(unique); // {a, b, c}
List<String> cleaned = votes.toSet().toList();
print(cleaned); // [a, b, c]
```

# --instructions--

Mając listę `votes`, utwórz `List<String>` o nazwie `cleaned`, która zawiera jej elementy bez duplikatów, w kolejności pierwszego wystąpienia.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var votes = ['yes', 'no', 'yes', 'maybe', 'no', 'yes'];
```

# --after-seed--

```dart
  return cleaned;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`cleaned` powinno być `['yes', 'no', 'maybe']`

```dart
  test('test1', () {
    expect(result(), ['yes', 'no', 'maybe'], reason: '--err-t1--');
  });
```

`cleaned` powinno być `List`

```dart
  test('test2', () {
    expect(result(), isA<List>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var votes = ['yes', 'no', 'yes', 'maybe', 'no', 'yes'];
List<String> cleaned = votes.toSet().toList();
```
