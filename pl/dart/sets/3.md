---
language: dart
exerciseType: 1
---

# --description--

Metoda `.add(value)` wstawia pojedynczą wartość. Zwraca `true`, jeśli wartość została dodana, i `false`, jeśli była już w zbiorze — w takim przypadku nic się nie zmienia. Metoda `.addAll(iterable)` wstawia wszystkie elementy z listy lub innego zbioru, również pomijając te, które już są obecne:

```dart
var tags = {'dart'};
tags.add('web');    // true
tags.add('dart');   // false, already there
tags.addAll(['web', 'mobile']);
print(tags); // {dart, web, mobile}
```

Pusty literał `{}` jest **mapą**, a nie zbiorem. Aby utworzyć pusty zbiór, nadaj mu typ:

```dart
var empty = <String>{};
Set<int> other = {};
```

# --instructions--

Mając zbiór `tags`, dodaj `'web'` za pomocą `.add()`, a następnie dodaj wszystkie elementy listy `['web', 'mobile']` za pomocą `.addAll()`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var tags = {'dart'};
```

# --after-seed--

```dart
  return tags;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`tags` powinno być `{'dart', 'web', 'mobile'}`

```dart
  test('test1', () {
    expect(result(), {'dart', 'web', 'mobile'}, reason: '--err-t1--');
  });
```

`tags` powinno mieć 3 elementy

```dart
  test('test2', () {
    expect(result().length, 3, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var tags = {'dart'};
tags.add('web');
tags.addAll(['web', 'mobile']);
```
