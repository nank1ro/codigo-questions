---
language: dart
exerciseType: 1
---

# --description--

Aby sprawdzić, czy wartość znajduje się w zbiorze, użyj `.contains(value)`, która zwraca `bool`. Właściwość `.isEmpty` ma wartość `true`, gdy zbiór nie ma elementów, a `.isNotEmpty` — gdy ma co najmniej jeden:

```dart
var seen = {'x', 'y'};
print(seen.contains('x')); // true
print(seen.contains('z')); // false
print(seen.isEmpty);       // false
print(seen.isNotEmpty);    // true
```

# --instructions--

Mając zbiór `seen`, zapisz w `bool` o nazwie `hasX`, czy zawiera on `'x'`, w `bool` o nazwie `hasZ`, czy zawiera `'z'`, oraz w `bool` o nazwie `empty`, czy jest pusty.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var seen = {'x', 'y'};
```

# --after-seed--

```dart
  return [hasX, hasZ, empty];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasX` powinno być `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasZ` powinno być `false`

```dart
  test('test2', () {
    expect(result()[1], false, reason: '--err-t2--');
  });
```

`empty` powinno być `false`

```dart
  test('test3', () {
    expect(result()[2], false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var seen = {'x', 'y'};
bool hasX = seen.contains('x');
bool hasZ = seen.contains('z');
bool empty = seen.isEmpty;
```
