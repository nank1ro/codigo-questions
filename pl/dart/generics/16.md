---
language: dart
exerciseType: 1
---

# --description--

`typedef` nadaje nazwę typowi i może przyjmować własne parametry typu. Zwykłym powodem jest nazwanie rodziny typów funkcyjnych raz, zamiast zapisywać ją przy każdym użyciu:

```dart
typedef Transform<I, O> = O Function(I input);

final Transform<String, int> length = (word) => word.length;
print(length('kiwi')); // 4
```

`Transform<String, int>` to tylko inny sposób zapisania `int Function(String)`, więc oba zapisy są zamienne. Zyskiem jest czytelność: parametr zadeklarowany jako `Transform<I, O> transform` mówi, do czego funkcja służy, a `O Function(I)` mówi tylko, jak wygląda.

Generyczny typedef i generyczna funkcja łączą się naturalnie: własne parametry typu funkcji wypełniają parametry typedefa.

# --instructions--

Zadeklaruj generyczny typedef `Transform` z dwoma parametrami typu `I` i `O`, który nazywa funkcję przyjmującą `I` i zwracającą `O`.

Następnie napisz generyczną funkcję `convertAll` z parametrami typu `I` i `O`, która przyjmuje `List<I>` `items` oraz `Transform<I, O>` `transform` i zwraca `List<O>` przechowującą każdy przekonwertowany element, w kolejności.

Przykład:
```dart
print(convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)); // [3, 4]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)` powinno dać `[3, 4]`

```dart
  test('test1', () {
    expect(
      convertAll<String, int>(['fig', 'kiwi'], (w) => w.length),
      [3, 4],
      reason: '--err-t1--',
    );
  });
```

`convertAll` powinno zwracać `List<O>`, więc konwersja liczb int na ciągi znaków powinna dać `List<String>`

```dart
  test('test2', () {
    final result = convertAll<int, String>([1, 2], (n) => 'n$n');
    expect(result, ['n1', 'n2'], reason: '--err-t2--');
    expect(result, isA<List<String>>(), reason: '--err-t2--');
  });
```

Typedef `Transform` powinien nazywać typ funkcyjny, a pusta lista powinna dać pustą listę

```dart
  test('test3', () {
    final Transform<int, int> twice = (n) => n * 2;
    expect(convertAll<int, int>([1, 2, 3], twice), [2, 4, 6], reason: '--err-t3--');
    expect(convertAll<String, int>(<String>[], (w) => w.length), <int>[], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
typedef Transform<I, O> = O Function(I input);

List<O> convertAll<I, O>(List<I> items, Transform<I, O> transform) {
  return items.map(transform).toList();
}
```
