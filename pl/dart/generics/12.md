---
language: dart
exerciseType: 1
---

# --description--

Ograniczenie może wspominać sam parametr typu. `Comparable<T>` to interfejs wszystkiego, co wie, jak porównać się ze swoim własnym rodzajem, za pomocą `compareTo`:

```dart
print('fig'.compareTo('kiwi')); // negative: fig comes first
print('kiwi'.compareTo('fig')); // positive
print('fig'.compareTo('fig'));  // zero
```

Tak więc `T extends Comparable<T>` czyta się jako „dowolny typ, który można porównać z nim samym”, co jest dokładnie tym, czego potrzebują funkcje sortujące albo szukające maksimum:

```dart
T maxOf<T extends Comparable<T>>(T a, T b) => a.compareTo(b) >= 0 ? a : b;

print(maxOf('fig', 'kiwi')); // kiwi
```

`String` i `DateTime` spełniają go bezpośrednio. `int` i `double` implementują `Comparable<num>`, więc lista liczb jest po prostu porównywana jako `num`.

# --instructions--

Napisz generyczną funkcję `largest` z parametrem typu `T` ograniczonym przez `Comparable<T>`, która przyjmuje niepustą `List<T>` `items` i zwraca największy element, porównując za pomocą `compareTo`.

Przykład:
```dart
print(largest(['fig', 'kiwi', 'apple'])); // kiwi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest(['fig', 'kiwi', 'apple'])` powinno dać `'kiwi'`

```dart
  test('test1', () {
    expect(largest(['fig', 'kiwi', 'apple']), 'kiwi', reason: '--err-t1--');
  });
```

`largest` powinno też działać z liczbami i datami

```dart
  test('test2', () {
    expect(largest([3, 9, 5]), 9, reason: '--err-t2--');
    expect(
      largest([DateTime(2020), DateTime(2024), DateTime(2001)]),
      DateTime(2024),
      reason: '--err-t2--',
    );
  });
```

`largest` powinno zwracać jedyny element listy jednoelementowej i ostatni, gdy to on jest największy, oraz powinno przyjmować jawny argument typu, jak `largest<String>(['a', 'z'])`

```dart
  test('test3', () {
    expect(largest<String>(['a', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['only']), 'only', reason: '--err-t3--');
    expect(largest(['a', 'b', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['z', 'b', 'a']), 'z', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
T largest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) > 0) {
      best = item;
    }
  }
  return best;
}
```
