---
language: dart
exerciseType: 1
---

# --description--

Nieograniczony `T` mógłby być czegokolwiek, więc wewnątrz ciała możesz używać tylko tego, co ma każdy obiekt. To się nie kompiluje:

```dart
T twice<T>(T value) => value + value; // error: + is not defined for T
```

Naprawia to **ograniczenie**. Zapis `T extends num` mówi, że „`T` może być tylko liczbą”, a w zamian ciało może używać wszystkiego, co oferuje `num`:

```dart
T twice<T extends num>(T value) => (value + value) as T;

num half<T extends num>(T value) => value / 2;
```

Ograniczenie jest sprawdzane w miejscu wywołania: `half(4)` i `half(2.5)` są w porządku, a `half('fig')` to błąd czasu kompilacji. Ograniczenie to obietnica w obie strony: węższe argumenty w zamian za większe możliwości w środku.

# --instructions--

Napisz generyczną funkcję `total` z parametrem typu `T` ograniczonym przez `num`, która przyjmuje `List<T>` `values` i zwraca sumę elementów jako `num`. Pusta lista daje `0`.

Przykład:
```dart
print(total([1, 2, 3]));  // 6
print(total([1.5, 2.5])); // 4.0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total([1, 2, 3])` powinno dać `6`

```dart
  test('test1', () {
    expect(total([1, 2, 3]), 6, reason: '--err-t1--');
  });
```

`total` powinno też działać z liczbami double, więc `total([1.5, 2.5])` powinno dać `4.0`, i powinno przyjmować jawny argument typu, jak `total<int>([1, 2])`

```dart
  test('test2', () {
    expect(total([1.5, 2.5]), 4.0, reason: '--err-t2--');
    expect(total([10, 20, 30, 40]), 100, reason: '--err-t2--');
    expect(total<int>([1, 2]), 3, reason: '--err-t2--');
    expect(total<double>([1.5, 1.5]), 3.0, reason: '--err-t2--');
  });
```

`total` powinno dać `0` dla pustej listy

```dart
  test('test3', () {
    expect(total(<int>[]), 0, reason: '--err-t3--');
    expect(total(<double>[]), 0, reason: '--err-t3--');
    expect(total([-5, 5, 7]), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
num total<T extends num>(List<T> values) {
  num sum = 0;
  for (final value in values) {
    sum += value;
  }
  return sum;
}
```
