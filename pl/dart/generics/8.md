---
language: dart
exerciseType: 2
---

# --description--

Przy sound null safety znak zapytania może wylądować w dwóch różnych miejscach i oznaczają one dwie różne rzeczy:

```dart
Box<int?> a = Box(null); // a box that exists and holds a nullable int
Box<int>? b = null;      // no box at all, but if there is one it holds an int
```

W `Box<int?>` **argument typu** jest nullowalny, więc `a.value` ma typ `int?` i może być `null`, podczas gdy samo `a` zawsze istnieje. W `Box<int>?` **zmienna** jest nullowalna, więc `b` może być `null` i żeby sięgnąć do środka, potrzebujesz `b?.value` albo `b!.value`.

Zwykłe `T` oznacza `T extends Object?`, więc nullowalny argument typu, taki jak `Box<int?>`, jest całkowicie legalny.

# --instructions--

Uzupełnij kod tak, aby `holdsNull` było pudełkiem przechowującym `null`, a `missing` było zmienną, która może w ogóle nie przechowywać żadnego pudełka.

# --seed--

```dart
class Box<T> {
  final T value;
  Box(this.value);
}

void main() {
  final holdsNull = Box<[/]>(null);
  Box<int>[/] missing = null;
  print('value: ${holdsNull.value}');
  print('box: $missing');
}
```

# --answers--

- int?
- ?
- int
- !
- ??

# --solutions--

```dart
class Box<T> {
  final T value;
  Box(this.value);
}

void main() {
  final holdsNull = Box<int?>(null);
  Box<int>? missing = null;
  print('value: ${holdsNull.value}');
  print('box: $missing');
}
```

# --output--

value: null
box: null
