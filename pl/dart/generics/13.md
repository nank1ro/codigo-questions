---
language: dart
exerciseType: 4
---

# --description--

To samo ograniczenie działa równie dobrze dla najmniejszego elementu: zmienia się tylko znak porównania. `compareTo` zwraca liczbę ujemną, gdy odbiorca jest pierwszy, więc `item.compareTo(best) < 0` znaczy „ten jest mniejszy”.

# --instructions--

Ułóż elementy tak, aby zdefiniować generyczną funkcję `smallest` ograniczoną przez `Comparable<T>`, która zwraca najmniejszy element listy, oraz `main`, który wypisuje najmniejszy z `['pear', 'fig', 'kiwi']`. Zdefiniuj funkcję przed `main`. Program musi wypisać:

```
fig
```

# --answers--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
```

```dart
  var best = items.first;
```

```dart
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
```

```dart
  return best;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(smallest(['pear', 'fig', 'kiwi']));
```

```dart
}
```

# --solutions--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
  return best;
}

void main() {
  print(smallest(['pear', 'fig', 'kiwi']));
}
```

# --output--

fig
