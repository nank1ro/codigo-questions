---
language: dart
exerciseType: 4
---

# --description--

Rozszerzenia nie służą tylko do typów z SDK: możesz rozszerzać też **własne klasy**. Jest to przydatne, gdy klasa pochodzi z pakietu, którym nie zarządzasz, albo gdy chcesz utrzymać klasę małą i dodać opcjonalne funkcje pomocnicze obok kodu, który ich potrzebuje.

```dart
class Circle {
  final double radius;
  Circle(this.radius);
}

extension CircleMath on Circle {
  double get diameter => radius * 2;
}

void main() {
  print(Circle(3).diameter); // 6.0
}
```

Rozszerzenie widzi publiczne pola i metody klasy, dokładnie tak jak kod napisany poza klasą.

# --instructions--

Ułóż elementy, aby zdefiniować klasę `Rectangle` z polami `width` i `height`, potem rozszerzenie `RectangleMath` na `Rectangle` z getterem `area`, a na końcu `main`, który wypisuje pole prostokąta `4` na `2.5`. Zdefiniuj najpierw klasę, potem rozszerzenie, potem `main`. Wewnątrz klasy zadeklaruj oba pola przed konstruktorem. Program musi wypisać:

```
10.0
```

# --answers--

```dart
class Rectangle {
```

```dart
  final double width;
```

```dart
  final double height;
```

```dart
  Rectangle(this.width, this.height);
```

```dart
}
```

```dart
extension RectangleMath on Rectangle {
```

```dart
  double get area => width * height;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Rectangle(4, 2.5).area);
```

```dart
}
```

# --solutions--

```dart
class Rectangle {
  final double width;
  final double height;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

```dart
class Rectangle {
  final double height;
  final double width;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

# --output--

10.0
