---
language: dart
exerciseType: 1
---

# --description--

Rozszerzenie może deklarować składowe **statyczne**. Jak w klasie, należą one do samego rozszerzenia, a nie do żadnej wartości, i dostęp do nich uzyskuje się przez **nazwę rozszerzenia**, a nie przez rozszerzany typ:

```dart
extension Temperatures on double {
  static const double boiling = 100.0;

  static bool isBoiling(double celsius) => celsius >= boiling;
}

void main() {
  print(Temperatures.boiling);         // 100.0
  print(Temperatures.isBoiling(37.5)); // false
  print(double.boiling);               // error: 'boiling' isn't defined for 'double'
}
```

Składowe statyczne to wygodne miejsce na stałe i funkcje pomocnicze związane z rozszerzanym typem.

# --instructions--

Napisz rozszerzenie o nazwie `Dice` na `int` z:
- statyczną stałą `sides` typu `int` o wartości `6`
- statyczną metodą `isValid`, która przyjmuje parametr `value` typu `int` i zwraca `true`, gdy mieści się między `1` a `sides` (włącznie)

Przykład:
```dart
print(Dice.sides);      // 6
print(Dice.isValid(7)); // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Dice.sides` powinno być `6`

```dart
  test('test1', () {
    expect(Dice.sides, 6, reason: '--err-t1--');
  });
```

`Dice.isValid(1)` i `Dice.isValid(6)` powinno być `true`

```dart
  test('test2', () {
    expect(Dice.isValid(1), true, reason: '--err-t2--');
    expect(Dice.isValid(6), true, reason: '--err-t2--');
  });
```

`Dice.isValid(0)` i `Dice.isValid(7)` powinno być `false`

```dart
  test('test3', () {
    expect(Dice.isValid(0), false, reason: '--err-t3--');
    expect(Dice.isValid(7), false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Dice on int {
  static const int sides = 6;

  static bool isValid(int value) => value >= 1 && value <= sides;
}
```
