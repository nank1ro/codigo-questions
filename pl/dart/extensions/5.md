---
language: dart
exerciseType: 1
---

# --description--

Rozszerzenia na `List<int>` nie można użyć na `List<String>`. Aby napisać jedno rozszerzenie działające dla **każdego** typu elementów, nadaj rozszerzeniu **parametr typu**, zapisany w nawiasach kątowych po jego nazwie, i użyj go w typie po `on`:

```dart
extension Firsts<T> on List<T> {
  T get firstOrLast => length > 1 ? this[0] : this[length - 1];
}
```

`T` to symbol zastępczy dla „cokolwiek to jest za typ elementów”: na `List<int>` staje się `int`, na `List<String>` staje się `String`, więc getter powyżej zwraca odpowiednio `int` albo `String`. Kompilator wstawia `T` za ciebie przy każdym wywołaniu.

```dart
print([7, 8, 9].firstOrLast); // 7
print(['a', 'b'].firstOrLast); // a
```

# --instructions--

Napisz rozszerzenie o nazwie `Seconds` z parametrem typu `T` na `List<T>`, z getterem `secondOrNull` typu `T?`, który zwraca element o indeksie `1`, albo `null`, gdy lista ma mniej niż dwa elementy.

Przykład:
```dart
print([10, 20, 30].secondOrNull); // 20
print(['solo'].secondOrNull);     // null
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`[10, 20, 30].secondOrNull` powinno być `20`

```dart
  test('test1', () {
    expect([10, 20, 30].secondOrNull, 20, reason: '--err-t1--');
  });
```

`['a', 'b'].secondOrNull` powinno być `'b'`

```dart
  test('test2', () {
    expect(['a', 'b'].secondOrNull, 'b', reason: '--err-t2--');
  });
```

`['solo'].secondOrNull` i `<int>[].secondOrNull` powinno być `null`

```dart
  test('test3', () {
    expect(['solo'].secondOrNull, null, reason: '--err-t3--');
    expect(<int>[].secondOrNull, null, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Seconds<T> on List<T> {
  T? get secondOrNull => length > 1 ? this[1] : null;
}
```
