---
language: dart
exerciseType: 1
---

# --description--

Dart pozwala typowi zdefiniować, co operatory takie jak `+`, `*` czy `==` oznaczają dla jego wartości, za pomocą metody, której nazwą jest słowo kluczowe `operator`, a po nim symbol. Prawa strona operatora to parametr metody:

```dart
extension Scaling on List<int> {
  List<int> operator *(int factor) => map((n) => n * factor).toList();
}

void main() {
  print([1, 2, 3] * 10); // [10, 20, 30]
}
```

Ponieważ rozszerzenia mogą deklarować operatory, możesz nadać istniejącemu typowi nowy operator, którego jeszcze nie ma. `String` ma `+` i `*`, ale nie ma `-`, więc rozszerzenie może zdefiniować, co oznacza `'hello world' - 'o'`.

# --instructions--

Napisz rozszerzenie o nazwie `Removing` na `String` z operatorem `-`, który przyjmuje parametr `part` typu `String` i zwraca ciąg znaków z usuniętymi wszystkimi wystąpieniami `part`. Użyj `replaceAll`.

Przykład:
```dart
print('hello world' - 'o'); // hell wrld
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`'hello world' - 'o'` powinno być `'hell wrld'`

```dart
  test('test1', () {
    expect('hello world' - 'o', 'hell wrld', reason: '--err-t1--');
  });
```

`'banana' - 'an'` powinno być `'ba'`

```dart
  test('test2', () {
    expect('banana' - 'an', 'ba', reason: '--err-t2--');
  });
```

`'abc' - 'z'` powinno być `'abc'`, a `'aaa' - 'a'` powinno być `''`

```dart
  test('test3', () {
    expect('abc' - 'z', 'abc', reason: '--err-t3--');
    expect('aaa' - 'a', '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Removing on String {
  String operator -(String part) => replaceAll(part, '');
}
```
