---
language: dart
exerciseType: 2
---

# --description--

Rozszerzenia na `String` nie można wywołać na `String?`: wartość może być `null`, a kompilator odrzuca takie wywołanie. Jeśli zadeklarujesz rozszerzenie na typie **nullable**, metodę można wywołać bezpośrednio na `String?`, a wewnątrz niej `this` ma typ `String?`, więc musisz sam obsłużyć przypadek `null`, na przykład za pomocą `??`:

```dart
extension Defaults on int? {
  int orZero() => this ?? 0;
}

void main() {
  int? count = null;
  print(count.orZero()); // 0
  print(5.orZero());     // 5
}
```

Wartość `int` non-nullable można przekazać tam, gdzie oczekiwane jest `int?`, więc rozszerzenie działa na obu.

# --instructions--

Uzupełnij rozszerzenie tak, aby `orEmpty` można było wywołać na ciągu znaków typu nullable i aby zwracał ten ciąg znaków albo pusty ciąg znaków, gdy jest `null`. Program musi wypisać `[]` i `[Ada]`.

# --seed--

```dart
extension Defaults on [/] {
  String orEmpty() => this [/] '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --answers--

- String?
- ??
- String
- +
- ?.
- !

# --solutions--

```dart
extension Defaults on String? {
  String orEmpty() => this ?? '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --output--

[]
[Ada]
