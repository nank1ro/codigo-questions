---
language: dart
exerciseType: 1
---

# --instructions--
Napisz funkcję, która zwraca pierwszą liczbę ujemną znalezioną w `List<int>`. Użyj pętli `for` z `break`, aby zatrzymać się zaraz po znalezieniu liczby ujemnej. Zwróć `0`, jeśli nie ma liczby ujemnej.

# --before-seed--
```dart
int firstNegative(List<int> numbers) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Znajduje pierwszą liczbę ujemną
```dart
    test('finds first negative', () {
      expect(firstNegative([3, -5, -2, 1]), -5, reason: '--err-t1--');
    });
```

Zwraca 0, gdy nie ma liczby ujemnej
```dart
    test('no negative', () {
      expect(firstNegative([1, 2, 3]), 0, reason: '--err-t2--');
    });
```

Pierwszy element jest ujemny
```dart
    test('first is negative', () {
      expect(firstNegative([-7, 4, 2]), -7, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = 0; i < numbers.length; i++) {
    if (numbers[i] < 0) {
      result = numbers[i];
      break;
    }
  }
```
