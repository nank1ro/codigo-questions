---
language: dart
exerciseType: 1
---

# --instructions--
Napisz funkcję, która zwraca wartość na pozycji (`row`, `col`) tabliczki mnożenia. Użyj zagnieżdżonych pętli `for`, gdzie zewnętrzna pętla iteruje po wierszach, a wewnętrzna po kolumnach (obie indeksowane od 1). Zwróć iloczyn na podanej pozycji.

# --before-seed--
```dart
int multiplicationCell(int row, int col) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Wiersz 3, kolumna 4 równa się 12
```dart
    test('3x4 = 12', () {
      expect(multiplicationCell(3, 4), 12, reason: '--err-t1--');
    });
```

Wiersz 1, kolumna 1 równa się 1
```dart
    test('1x1 = 1', () {
      expect(multiplicationCell(1, 1), 1, reason: '--err-t2--');
    });
```

Wiersz 7, kolumna 8 równa się 56
```dart
    test('7x8 = 56', () {
      expect(multiplicationCell(7, 8), 56, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int r = 1; r <= row; r++) {
    for (int c = 1; c <= col; c++) {
      if (r == row && c == col) {
        result = r * c;
      }
    }
  }
```
