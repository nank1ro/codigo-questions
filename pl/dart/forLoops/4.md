---
language: dart
exerciseType: 2
---

# --description--
Pętla `for-in` iteruje po każdym elemencie kolekcji bez potrzeby używania indeksu:

```dart
List<String> fruits = ['apple', 'banana', 'cherry'];
for (var fruit in fruits) {
  print(fruit);
}
```

Każda iteracja przypisuje kolejny element do zmiennej pętli (tutaj `fruit`).

# --instructions--
Uzupełnij pętlę `for-in`, która wypisuje każdą liczbę z listy.

# --seed--
```dart
List<int> numbers = [10, 20, 30];
for ([/] n [/] numbers) {
  [/](n);
}
```

# --answers--

- var
- in
- print
- int
- of
- write
- final
- println

# --solutions--
```dart
List<int> numbers = [10, 20, 30];
for (var n in numbers) {
  print(n);
}
```

# --output--
10
20
30
