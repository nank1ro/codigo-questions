---
language: dart
exerciseType: 2
---

# --description--

Deklaracji `mixin` nie można instancjonować ani po niej dziedziczyć, a zwykłej `class` nie można użyć po `with`. Gdy potrzebujesz jednej deklaracji działającej **na oba** sposoby, napisz **`mixin class`**:

```dart
mixin class Serializable {
  String toText() => 'data';
}

final s = Serializable();            // działa: to jest klasa
class Record extends Serializable {} // działa: to jest klasa
class Row with Serializable {}       // działa: to jest mixin
```

`mixin class` płaci za tę elastyczność dwoma ograniczeniami: musi dziedziczyć po `Object`, więc nie może mieć własnej klauzuli `extends`, i nie może deklarować konstruktora, ponieważ mixin nigdy żadnego nie uruchamia.

# --instructions--

Uzupełnij deklarację tak, aby `Tagged` można było zarówno instancjonować samodzielnie, jak i użyć w `Post` po `with`.

# --seed--

```dart
[/] class Tagged {
  String tag() => 'tagged';
}

class Post with Tagged {}

void main() {
  print(Tagged().tag());
  print(Post().tag());
}
```

# --answers--

- mixin
- abstract
- base
- final
- sealed
- on

# --solutions--

```dart
mixin class Tagged {
  String tag() => 'tagged';
}

class Post with Tagged {}

void main() {
  print(Tagged().tag());
  print(Post().tag());
}
```

# --output--

tagged
tagged
