---
language: dart
exerciseType: 1
---

# --description--

Ciało mixinu wygląda jak ciało klasy: metody, getter i pola zapisuje się dokładnie tak samo. Różnica polega na tym, co można zrobić z samą deklaracją.

```dart
mixin Timestamped {
  String get stamp => '[log]';

  String format(String text) => '$stamp $text';
}

class Server with Timestamped {}
class Printer with Timestamped {}
```

Nazwa mixinu jest też **typem**, więc `Server() is Timestamped` ma wartość `true`, a zmienną można zadeklarować jako `Timestamped t = Server();`. Dwie niespokrewnione klasy współdzielą teraz jedną implementację, bez dziedziczenia po sobie.

# --instructions--

Zadeklaruj mixin `Shouter` z metodą `shout`, która przyjmuje `String` `text` i zwraca go wielkimi literami, po których następuje wykrzyknik.

Zadeklaruj klasę `Speaker`, która korzysta z `Shouter` i nie dodaje niczego od siebie.

Przykład:
```dart
print(Speaker().shout('hi')); // HI!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Speaker().shout('hi')` powinno być `'HI!'`

```dart
  test('test1', () {
    expect(Speaker().shout('hi'), 'HI!', reason: '--err-t1--');
  });
```

`Speaker().shout('Dart rocks')` powinno być `'DART ROCKS!'`, a `Speaker().shout('')` powinno być `'!'`

```dart
  test('test2', () {
    expect(Speaker().shout('Dart rocks'), 'DART ROCKS!', reason: '--err-t2--');
    expect(Speaker().shout(''), '!', reason: '--err-t2--');
  });
```

`Speaker` powinien mieć `shout` z mixinu, więc `Speaker` powinien być też `Shouter`

```dart
  test('test3', () {
    expect(Speaker(), isA<Shouter>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Shouter {
  String shout(String text) => '${text.toUpperCase()}!';
}

class Speaker with Shouter {}
```
