---
language: dart
exerciseType: 2
---

# --description--

Niektóre zachowanie ma sens tylko na wierzchu konkretnej klasy i do działania potrzebuje składowych tej klasy. Klauzula **`on`** wyraża ten wymóg:

```dart
class Animal {
  String get name => 'animal';
}

mixin Noisy on Animal {
  String shout() => '${name.toUpperCase()}!';
}

class Dog extends Animal with Noisy {
  @override
  String get name => 'dog';
}

void main() {
  print(Dog().shout()); // DOG!
}
```

`on Animal` robi dwie rzeczy: pozwala mixinowi korzystać ze składowych `Animal`, jak `name` powyżej, i ogranicza, kto może użyć mixinu. `class Rock with Noisy {}` to błąd kompilacji, ponieważ `Rock` nie jest `Animal`.

# --instructions--

Uzupełnij kod tak, aby `Described` był mixinem ograniczonym do `Vehicle`, a `Car` było `Vehicle` korzystającym z niego.

# --seed--

```dart
class Vehicle {
  int get wheels => 4;
}

mixin Described [/] Vehicle {
  String describe() => 'has $wheels wheels';
}

class Car [/] Vehicle [/] Described {}

void main() {
  print(Car().describe());
}
```

# --answers--

- on
- extends
- with
- mixin
- class
- of

# --solutions--

```dart
class Vehicle {
  int get wheels => 4;
}

mixin Described on Vehicle {
  String describe() => 'has $wheels wheels';
}

class Car extends Vehicle with Described {}

void main() {
  print(Car().describe());
}
```

# --output--

has 4 wheels
