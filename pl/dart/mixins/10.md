---
language: dart
exerciseType: 1
---

# --description--

Ciało klasy znajduje się na samym końcu łańcucha, więc składowa zadeklarowana w klasie **nadpisuje** tę samą składową pochodzącą z dowolnego z jej mixinów. Wewnątrz nadpisania **`super`** sięga wersji dostarczonej przez mixin:

```dart
mixin Polite {
  String greet() => 'Hello';
}

class Host with Polite {
  @override
  String greet() => '${super.greet()}, welcome!';
}

class Guest with Polite {}

void main() {
  print(Host().greet());  // Hello, welcome!
  print(Guest().greet()); // Hello
}
```

Sam mixin pozostaje nietknięty: `Guest` nadal dostaje oryginalne `greet`. To właśnie `super.greet()` pozwala `Host` bazować na współdzielonym zachowaniu zamiast je kopiować.

# --instructions--

Zadeklaruj mixin `Signed` z metodą `sign` zwracającą `'signed'`.

Zadeklaruj klasę `Letter`, która korzysta z `Signed` i nadpisuje `sign` tak, aby zwracała wynik mixinu, po którym następuje `' by Ada'`, uzyskany przez wywołanie `super`.

Zadeklaruj klasę `Note`, która korzysta z `Signed` i niczego nie nadpisuje.

Przykład:
```dart
print(Letter().sign()); // signed by Ada
print(Note().sign());   // signed
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Letter().sign()` powinno być `'signed by Ada'`

```dart
  test('test1', () {
    expect(Letter().sign(), 'signed by Ada', reason: '--err-t1--');
  });
```

`Note().sign()` powinno nadal być `'signed'`, ponieważ mixin pozostaje bez zmian

```dart
  test('test2', () {
    expect(Note().sign(), 'signed', reason: '--err-t2--');
  });
```

Obie klasy powinny korzystać z mixinu, a nadpisanie powinno być widoczne także przez typ mixinu

```dart
  test('test3', () {
    final Signed letter = Letter();
    expect(letter.sign(), 'signed by Ada', reason: '--err-t3--');
    expect(Note(), isA<Signed>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Signed {
  String sign() => 'signed';
}

class Letter with Signed {
  @override
  String sign() => '${super.sign()} by Ada';
}

class Note with Signed {}
```
