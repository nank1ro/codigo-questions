---
language: dart
exerciseType: 1
---

# --instructions--

Zadeklaruj zmienną `int` na poziomie kodu `fuel` z wartością `100`. Następnie użyj `-=`, aby odjąć od niej `35`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`fuel` powinien być `65`

```dart
  test('test1', () {
    expect(fuel, 65, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int fuel = 100;

void main() {
  fuel -= 35;
  print(fuel);
}
```
