---
language: dart
exerciseType: 3
---

# --description--

Operatory przypisania łączą operację arytmetyczną z przypisaniem w jednym kroku.

Na przykład, `x += 3` to skrót dla `x = x + 3`.

# --instructions--

Co robi `x += 5`, gdy `x` wynosi obecnie `10`?

# --answers--

- Ustawia `x` na `15`
- Ustawia `x` na `5`
- Ustawia `x` na `10`
- Ustawia `x` na `50`

# --solutions--

- Ustawia `x` na `15`
