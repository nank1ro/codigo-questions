---
language: dart
exerciseType: 1
---

# --instructions--

Sklep stosuje 20% rabat na przedmiot. Zadeklaruj zmienną `double` na poziomie kodu `price` z wartością `50.0`. Następnie użyj `*=`, aby zastosować rabat poprzez pomnożenie `price` przez `0.8`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`price` powinien być `40.0`

```dart
  test('test1', () {
    expect(price, closeTo(40.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double price = 50.0;

void main() {
  price *= 0.8;
  print(price);
}
```
