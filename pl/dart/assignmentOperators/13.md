---
language: dart
exerciseType: 1
---

# --instructions--

Zadeklaruj zmienną `int` na poziomie kodu `remainder` z wartością `29`. Następnie użyj `%=`, aby przypisać jej resztę z dzielenia przez `6`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`remainder` powinien być `5`

```dart
  test('test1', () {
    expect(remainder, 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 29;

void main() {
  remainder %= 6;
  print(remainder);
}
```
