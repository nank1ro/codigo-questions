---
language: dart
exerciseType: 1
---

# --instructions--

Zadeklaruj zmienną `int` `n` z wartością `2`. Następnie w `main` zastosuj poniższe operatory przypisania w kolejności:
- `n *= 10` (pomnóż przez 10)
- `n += 5` (dodaj 5)
- `n -= 3` (odejmij 3)

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int n = 2;
```

# --after-seed--

```dart
  return n;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`n` powinien być `22`

```dart
  test('test1', () {
    expect(result(), 22, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 2;
n *= 10;
n += 5;
n -= 3;
```
