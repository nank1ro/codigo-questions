---
language: dart
exerciseType: 1
---

# --instructions--

Zadeklaruj zmienną `double` na poziomie kodu `value` z wartością `20.0`. Następnie użyj `/=`, aby podzielić ją przez `4`. Zauważ, że `/=` zawsze tworzy `double` w Dart.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value` powinien być `5.0`

```dart
  test('test1', () {
    expect(value, closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;

void main() {
  value /= 4;
  print(value);
}
```
