---
language: dart
exerciseType: 4
---

# --description--

Nullowalność kształtuje sposób deklarowania **parametrów nazwanych**. Parametr nazwany o typie nullable jest opcjonalny: gdy wywołujący go pominie, jest po prostu `null`.

```dart
String label({String? title}) => title ?? 'untitled';

print(label());               // untitled
print(label(title: 'Notes')); // Notes
```

Parametr nazwany o typie non-nullable i bez wartości domyślnej nie miałby wartości, gdyby go pominięto, więc Dart wymaga oznaczenia go jako `required`; wywołujący musi go wtedy zawsze przekazać:

```dart
String label({required String name, String? title}) { ... }

label(name: 'Ada');               // ok
label(name: 'Ada', title: 'Dr.'); // ok
label(title: 'Dr.');              // error: the named parameter 'name' is required
```

# --instructions--

Ułóż elementy, aby zdefiniować funkcję `label` z wymaganym parametrem nazwanym `name` i opcjonalnym parametrem nazwanym `title` typu nullable, zwracającą samo `name`, gdy `title` jest `null`, a w przeciwnym razie `'$title $name'`. Najpierw zdefiniuj funkcję, potem `main`. Program musi wypisać:

```
Ada
Countess Lovelace
```

# --answers--

```dart
String label({required String name, String? title}) {
```

```dart
  if (title == null) return name;
```

```dart
  return '$title $name';
```

```dart
}
```

```dart
void main() {
```

```dart
  print(label(name: 'Ada'));
```

```dart
  print(label(name: 'Lovelace', title: 'Countess'));
```

```dart
}
```

# --solutions--

```dart
String label({required String name, String? title}) {
  if (title == null) return name;
  return '$title $name';
}

void main() {
  print(label(name: 'Ada'));
  print(label(name: 'Lovelace', title: 'Countess'));
}
```

# --output--

Ada
Countess Lovelace
