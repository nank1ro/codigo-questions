---
language: dart
exerciseType: 2
---

# --description--

Wywołanie metody lub odczyt właściwości na `null` spowodowałoby awarię, więc Dart nie pozwala robić tego na wartości nullable za pomocą zwykłej kropki:

```dart
String? text;
print(text.length); // error: the property 'length' can't be unconditionally accessed
```

Rozwiązuje to operator **dostępu null-aware** `?.`: jeśli wartość jest `null`, całe wyrażenie jest `null` i nic więcej nie jest obliczane, w przeciwnym razie działa jak zwykła `.`:

```dart
String? text = 'Dart';
print(text?.length); // 4

text = null;
print(text?.length); // null
```

Ponieważ wynik może być `null`, jego typ jest nullable: `text?.length` to `int?`, a nie `int`.

# --instructions--

Uzupełnij funkcję `lengthOf`, tak aby przyjmowała `String` typu nullable i zwracała jego długość albo `null`, gdy tekstu brakuje. Program musi wypisać `4`, a potem `null`.

# --seed--

```dart
int? lengthOf([/] text) {
  return text[/]length;
}

void main() {
  print(lengthOf('Dart'));
  print(lengthOf(null));
}
```

# --answers--

- String?
- ?.
- String
- .
- !.
- ??

# --solutions--

```dart
int? lengthOf(String? text) {
  return text?.length;
}

void main() {
  print(lengthOf('Dart'));
  print(lengthOf(null));
}
```

# --output--

4
null
