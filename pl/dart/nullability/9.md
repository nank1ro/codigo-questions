---
language: dart
exerciseType: 1
---

# --description--

Sprawdzenie wartości nullable za pomocą `if` jest bezpieczniejsze niż `!`, a Dart cię za to nagradza. Po sprawdzeniu takim jak `if (x != null)` kompilator wie, że `x` nie może być `null` wewnątrz bloku, więc traktuje tam `x` jako non-nullable. Nazywa się to **promocją typu**:

```dart
int twice(int? n) {
  if (n != null) {
    return n * 2; // here n is an int, no ! needed
  }
  return 0;
}
```

Promocja działa też po wcześniejszym zwrocie:

```dart
int twice(int? n) {
  if (n == null) return 0;
  return n * 2; // n is an int from here on
}
```

Promocja dotyczy **zmiennych lokalnych i parametrów**, których wartość nie może zmienić się za twoimi plecami między sprawdzeniem a użyciem.

# --instructions--

Napisz funkcję `half`, która przyjmuje parametr `int` typu nullable o nazwie `n` i zwraca `n` podzielone przez `2` dzieleniem całkowitym `~/` albo `0`, gdy `n` jest `null`. Użyj sprawdzenia `if`, a nie `!`.

Przykład:
```dart
print(half(10));   // 5
print(half(null)); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`half(10)` powinno dać `5`

```dart
  test('test1', () {
    expect(half(10), 5, reason: '--err-t1--');
  });
```

`half(7)` powinno dać `3`

```dart
  test('test2', () {
    expect(half(7), 3, reason: '--err-t2--');
  });
```

`half(null)` powinno dać `0`

```dart
  test('test3', () {
    expect(half(null), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int half(int? n) {
  if (n != null) {
    return n ~/ 2;
  }
  return 0;
}
```
