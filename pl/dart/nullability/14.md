---
language: dart
exerciseType: 1
---

# --description--

Wiele funkcji bibliotecznych używa `null`, aby zgłosić, że czegoś **nie dało się zrobić**. Zamiana tekstu na liczbę to klasyczny przykład: `int.parse` zgłasza `FormatException`, gdy tekst nie jest liczbą, natomiast `int.tryParse` zwraca zamiast tego `null` i pozwala ci zdecydować, co dalej:

```dart
print(int.tryParse('42'));  // 42
print(int.tryParse('4x2')); // null
print(int.tryParse(''));    // null
```

Typem zwracanym przez `int.tryParse` jest `int?`, więc wszystko, czego się nauczyłeś, ma zastosowanie: `??` dla wartości domyślnej, `?.` do łączenia wywołań i sprawdzenie `if` dla promocji. `double.tryParse` działa tak samo.

# --instructions--

Napisz funkcję `parseOrZero`, która przyjmuje parametr `String` o nazwie `text` i zwraca zawartą w nim liczbę całkowitą albo `0`, gdy tekst nie jest poprawną liczbą całkowitą. Użyj `int.tryParse`.

Przykład:
```dart
print(parseOrZero('42'));  // 42
print(parseOrZero('abc')); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parseOrZero('42')` powinno dać `42`

```dart
  test('test1', () {
    expect(parseOrZero('42'), 42, reason: '--err-t1--');
  });
```

`parseOrZero('-5')` powinno dać `-5`

```dart
  test('test2', () {
    expect(parseOrZero('-5'), -5, reason: '--err-t2--');
  });
```

`parseOrZero('abc')` i `parseOrZero('')` powinny dać `0`

```dart
  test('test3', () {
    expect(parseOrZero('abc'), 0, reason: '--err-t3--');
    expect(parseOrZero(''), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int parseOrZero(String text) {
  return int.tryParse(text) ?? 0;
}
```
