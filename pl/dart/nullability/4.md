---
language: dart
exerciseType: 1
---

# --description--

Często brakującą wartość należy zastąpić **wartością domyślną**. Operator **if-null** `??` zwraca lewy operand, gdy nie jest on `null`, a w przeciwnym razie prawy:

```dart
String? nickname;
print(nickname ?? 'anonymous'); // anonymous

nickname = 'Ada';
print(nickname ?? 'anonymous'); // Ada
```

`??` reaguje wyłącznie na `null`: pusty łańcuch `''` czy liczba `0` to prawdziwe wartości, więc zostają zachowane.

`??` dobrze łączy się z `?.`, ponieważ `?.` daje wynik typu nullable:

```dart
String? text;
print(text?.length ?? 0); // 0
```

# --instructions--

Napisz funkcję `greet`, która przyjmuje parametr `String` typu nullable o nazwie `name` i zwraca `'Hello, '` z dołączonym `name` albo `'stranger'`, gdy `name` jest `null`. Użyj `??`.

Przykład:
```dart
print(greet('Ada')); // Hello, Ada
print(greet(null));  // Hello, stranger
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greet('Ada')` powinno dać `'Hello, Ada'`

```dart
  test('test1', () {
    expect(greet('Ada'), 'Hello, Ada', reason: '--err-t1--');
  });
```

`greet(null)` powinno dać `'Hello, stranger'`

```dart
  test('test2', () {
    expect(greet(null), 'Hello, stranger', reason: '--err-t2--');
  });
```

`greet('')` powinno dać `'Hello, '`, ponieważ pusty łańcuch nie jest `null`

```dart
  test('test3', () {
    expect(greet(''), 'Hello, ', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String greet(String? name) {
  return 'Hello, ${name ?? 'stranger'}';
}
```
