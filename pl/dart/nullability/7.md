---
language: dart
exerciseType: 1
---

# --description--

Czasem **ty** wiesz, że wartość nullable nie jest `null` w danym miejscu, nawet jeśli kompilator tego nie widzi. Operator **null assertion** `!` zamienia `String?` w `String`, obiecując, że wartość jest obecna:

```dart
String? text = 'Dart';
String sure = text!;
print(sure.length); // 4
```

Uwaga: `!` przenosi sprawdzenie z czasu kompilacji na czas działania. Jeśli wartość **jest** `null`, program zgłasza błąd i się zatrzymuje:

```dart
String? text;
print(text!.length); // Null check operator used on a null value
```

Używaj `!` oszczędnie i tylko wtedy, gdy `null` w tym miejscu i tak byłby błędem.

# --instructions--

Napisz funkcję `shout`, która przyjmuje parametr `String` typu nullable o nazwie `text` i zwraca go wielkimi literami z `!` na końcu. Użyj operatora `!`, tak aby funkcja zgłaszała błąd, gdy `text` jest `null`.

Przykład:
```dart
print(shout('hello')); // HELLO!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shout('hello')` powinno dać `'HELLO!'`

```dart
  test('test1', () {
    expect(shout('hello'), 'HELLO!', reason: '--err-t1--');
  });
```

`shout('Dart')` powinno dać `'DART!'`

```dart
  test('test2', () {
    expect(shout('Dart'), 'DART!', reason: '--err-t2--');
  });
```

`shout(null)` powinno zgłosić błąd

```dart
  test('test3', () {
    expect(() => shout(null), throwsA(anything), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String shout(String? text) {
  return '${text!.toUpperCase()}!';
}
```
