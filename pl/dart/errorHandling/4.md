---
language: dart
exerciseType: 1
---

# --description--

Po bloku `try` może następować **kilka** klauzul, z których każda radzi sobie z innym niepowodzeniem. Dart porównuje rzucony obiekt z nimi od góry do dołu i wykonuje **pierwszą**, która pasuje:

```dart
try {
  return names[int.parse(text)];
} on FormatException {
  return 'not a number';
} on RangeError {
  return 'out of range';
} catch (e) {
  return 'unknown problem';
}
```

Odczytanie listy indeksem, który nie istnieje, rzuca **`RangeError`**, więc dwa niepowodzenia tej jednej linii dostają różne odpowiedzi.

Ponieważ wygrywa pierwsze dopasowanie, kolejność ma znaczenie: klauzula dla ogólnego typu umieszczona nad bardziej konkretną zawsze wygrałaby, czyniąc konkretną klauzulę nieosiągalną. Zapisuj najpierw klauzule konkretne, a nagiego `catch` na końcu, jeśli chcesz mieć siatkę bezpieczeństwa.

Klauzula `on RangeError` powyżej jest tu tylko po to, aby pokazać, jak porządkuje się kilka klauzul. `RangeError` sygnalizuje błąd w kodzie, a nie warunek, nad którym program nie miał kontroli, a późniejsze ćwiczenie wyjaśnia, dlaczego takiemu niepowodzeniu należy zapobiegać, zamiast go łapać.

# --instructions--

Napisz funkcję `lookup`, która przyjmuje `List<int>` `scores` i `String` `text`, a zwraca `'score '` wraz z elementem `scores` na pozycji, którą opisuje `text`.

Zwróć `'not a number'`, gdy `text` nie jest liczbą całkowitą, `'out of range'`, gdy pozycja nie istnieje, i `'unknown problem'` dla wszystkiego innego.

Przykład:
```dart
print(lookup([10, 20, 30], '1')); // score 20
print(lookup([10, 20, 30], 'two')); // not a number
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`lookup([10, 20, 30], '1')` powinno zwrócić `'score 20'`

```dart
  test('test1', () {
    expect(lookup([10, 20, 30], '1'), 'score 20', reason: '--err-t1--');
    expect(lookup([10, 20, 30], '0'), 'score 10', reason: '--err-t1--');
  });
```

`lookup([10, 20, 30], 'two')` powinno zwrócić `'not a number'`

```dart
  test('test2', () {
    expect(lookup([10, 20, 30], 'two'), 'not a number', reason: '--err-t2--');
    expect(lookup([10, 20, 30], ''), 'not a number', reason: '--err-t2--');
  });
```

`lookup([10, 20, 30], '7')` powinno zwrócić `'out of range'`

```dart
  test('test3', () {
    expect(lookup([10, 20, 30], '7'), 'out of range', reason: '--err-t3--');
    expect(lookup([10, 20, 30], '-1'), 'out of range', reason: '--err-t3--');
    expect(lookup(<int>[], '0'), 'out of range', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String lookup(List<int> scores, String text) {
  try {
    return 'score ${scores[int.parse(text)]}';
  } on FormatException {
    return 'not a number';
  } on RangeError {
    return 'out of range';
  } catch (e) {
    return 'unknown problem';
  }
}
```
