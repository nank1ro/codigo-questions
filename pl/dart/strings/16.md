---
language: dart
exerciseType: 1
---

# --description--

Ponieważ ciągi znaków są niemutowalne, budowanie długiego tekstu za pomocą `+=` w pętli tworzy nowy ciąg znaków przy każdym kroku. **StringBuffer** zbiera fragmenty tekstu w wydajny sposób i tworzy końcowy ciąg znaków dopiero wtedy, gdy o to poprosisz:

- `.write(value)` dołącza wartość (dowolny typ jest konwertowany na tekst)
- `.writeln(value)` dołącza wartość, a po niej znak nowej linii
- `.toString()` zwraca ciąg znaków zbudowany do tej pory

```dart
var buffer = StringBuffer();
buffer.write('Hello');
buffer.write(', ');
buffer.writeln('Dart!');
buffer.write(42);
print(buffer.toString()); // Hello, Dart!\n42
```

# --instructions--

Mając listę `items`, użyj `StringBuffer`, aby zbudować `String` o nazwie `menu`, w którym każdy element znajduje się w osobnej linii, poprzedzony swoją pozycją zaczynającą się od `1` i kropką, na przykład `1. milk`. Każda linia musi kończyć się znakiem nowej linii (użyj `.writeln()`).

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var items = ['milk', 'eggs', 'bread'];
```

# --after-seed--

```dart
  return menu;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`menu` powinno być `'1. milk\n2. eggs\n3. bread\n'`

```dart
  test('test1', () {
    expect(result(), '1. milk\n2. eggs\n3. bread\n', reason: '--err-t1--');
  });
```

`menu` powinno być typu `String`

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var items = ['milk', 'eggs', 'bread'];
var buffer = StringBuffer();
for (var i = 0; i < items.length; i++) {
  buffer.writeln('${i + 1}. ${items[i]}');
}
String menu = buffer.toString();
```
