---
language: dart
exerciseType: 1
---

# --description--

Metoda `.substring(start, end)` zwraca fragment ciągu znaków od indeksu `start` do indeksu `end`, **bez jego uwzględnienia**. Jeśli pominiesz `end`, zwrócony zostanie fragment aż do końca ciągu:

```dart
var text = 'Hello Dart';
print(text.substring(0, 5));  // Hello
print(text.substring(6));     // Dart
```

# --instructions--

Mając ciąg znaków `date` w formacie `YYYY-MM-DD`, zapisz w `String` o nazwie `year` pierwsze cztery znaki, a w `String` o nazwie `day` dwa ostatnie znaki.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var date = '2024-06-15';
```

# --after-seed--

```dart
  return [year, day];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`year` powinno być `'2024'`

```dart
  test('test1', () {
    expect(result()[0], '2024', reason: '--err-t1--');
  });
```

`day` powinno być `'15'`

```dart
  test('test2', () {
    expect(result()[1], '15', reason: '--err-t2--');
  });
```

`year` powinno być typu `String`

```dart
  test('test3', () {
    expect(result()[0], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var date = '2024-06-15';
String year = date.substring(0, 4);
String day = date.substring(8);
```
