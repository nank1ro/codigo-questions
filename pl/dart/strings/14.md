---
language: dart
exerciseType: 1
---

# --description--

Pod spodem każdy znak ciągu jest przechowywany jako liczba, jego **code unit** (kod UTF-16). `.codeUnitAt(index)` zwraca kod jednego znaku, a `.codeUnits` zwraca całą listę. `String.fromCharCode(code)` robi coś odwrotnego, budując ciąg znaków z kodu:

```dart
var word = 'AB';
print(word.codeUnitAt(0));         // 65
print(word.codeUnits);             // [65, 66]
print(String.fromCharCode(67));    // C
```

Kolejne litery mają kolejne kody: `'A'` to 65, `'B'` to 66 i tak dalej.

# --instructions--

Mając ciąg znaków `letter`, zapisz w `int` o nazwie `code` jego code unit, a w `String` o nazwie `next` znak, którego kod to `code + 1`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var letter = 'D';
```

# --after-seed--

```dart
  return [code, next];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`code` powinno być `68`

```dart
  test('test1', () {
    expect(result()[0], 68, reason: '--err-t1--');
  });
```

`next` powinno być `'E'`

```dart
  test('test2', () {
    expect(result()[1], 'E', reason: '--err-t2--');
  });
```

`next` powinno być typu `String`

```dart
  test('test3', () {
    expect(result()[1], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var letter = 'D';
int code = letter.codeUnitAt(0);
String next = String.fromCharCode(code + 1);
```
