---
language: dart
exerciseType: 1
---

# --description--

Metoda `.split(separator)` dzieli ciąg znaków na `List<String>` przy każdym wystąpieniu separatora. Odwrotnością jest `.join(separator)`, metoda list, która skleja elementy w jeden ciąg znaków:

```dart
var csv = 'a,b,c';
List<String> parts = csv.split(',');
print(parts);            // [a, b, c]
print(parts.join(' - ')); // a - b - c
```

Wywołanie `.split('')` z pustym separatorem zwraca listę z każdym pojedynczym znakiem.

# --instructions--

Mając ciąg znaków `csv`, zapisz w `List<String>` o nazwie `colors` jego części rozdzielone przecinkiem, a w `String` o nazwie `joined` te części połączone za pomocą `' | '`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var csv = 'red,green,blue';
```

# --after-seed--

```dart
  return [colors, joined];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`colors` powinno być `['red', 'green', 'blue']`

```dart
  test('test1', () {
    expect(result()[0], ['red', 'green', 'blue'], reason: '--err-t1--');
  });
```

`joined` powinno być `'red | green | blue'`

```dart
  test('test2', () {
    expect(result()[1], 'red | green | blue', reason: '--err-t2--');
  });
```

`colors` powinno być typu `List`

```dart
  test('test3', () {
    expect(result()[0], isA<List>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var csv = 'red,green,blue';
List<String> colors = csv.split(',');
String joined = colors.join(' | ');
```
