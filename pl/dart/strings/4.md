---
language: dart
exerciseType: 1
---

# --description--

Każdy ciąg znaków wie, ile znaków zawiera, dzięki właściwości `.length`. Spacje i znaki interpunkcyjne również liczą się jako znaki:

```dart
var word = 'hello';
print(word.length);      // 5
print('a b'.length);     // 3
```

Możesz odczytać pojedynczy znak za pomocą nawiasów kwadratowych i jego **indeksu**, zaczynając od `0`. Wynikiem jest jednoznakowy `String`:

```dart
print(word[0]);                // h
print(word[word.length - 1]);  // o
```

Odczytanie indeksu spoza ciągu znaków (jak `word[5]`) zgłasza błąd.

# --instructions--

Mając ciąg znaków `word`, zapisz w `int` o nazwie `size` liczbę jego znaków, w `String` o nazwie `first` jego pierwszy znak, a w `String` o nazwie `last` jego ostatni znak.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var word = 'Flutter';
```

# --after-seed--

```dart
  return [size, first, last];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`size` powinno być `7`

```dart
  test('test1', () {
    expect(result()[0], 7, reason: '--err-t1--');
  });
```

`first` powinno być `'F'`

```dart
  test('test2', () {
    expect(result()[1], 'F', reason: '--err-t2--');
  });
```

`last` powinno być `'r'`

```dart
  test('test3', () {
    expect(result()[2], 'r', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var word = 'Flutter';
int size = word.length;
String first = word[0];
String last = word[word.length - 1];
```
