---
language: dart
exerciseType: 2
---

# --description--

Metoda `.replaceAll(from, to)` zwraca nowy ciąg znaków, w którym **każde** wystąpienie `from` jest zastąpione przez `to`. `.replaceFirst(from, to)` zastępuje tylko pierwsze wystąpienie:

```dart
var text = 'a-b-c';
print(text.replaceAll('-', '+'));    // a+b+c
print(text.replaceFirst('-', '+'));  // a+b-c
```

# --instructions--

Uzupełnij kod, aby zastąpić każde `cats` przez `dogs` i wypisać wynik.

# --seed--

```dart
var text = 'cats here, cats there';
print(text.[/]('cats', [/]));
```

# --answers--

- replaceAll
- 'dogs'
- replaceFirst
- replace
- 'cats'
- dogs

# --solutions--

```dart
var text = 'cats here, cats there';
print(text.replaceAll('cats', 'dogs'));
```

# --output--

dogs here, dogs there
