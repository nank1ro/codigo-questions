---
language: dart
exerciseType: 1
---

# --description--

Funkcja anonimowa nie musi być nigdzie zapisywana: można ją **wywołać natychmiast**, pisząc argumenty bezpośrednio po jej nawiasie zamykającym. Jest to wygodne do obliczenia wartości z kilkoma tymczasowymi zmiennymi, które nie powinny przedostawać się do otaczającego kodu:

```dart
var area = (double radius) {
  var pi = 3.14;
  return pi * radius * radius;
}(2.0);

print(area); // 12.56
```

Tutaj `pi` istnieje tylko wewnątrz funkcji anonimowej, a `area` otrzymuje zwróconą wartość.

# --instructions--

Zmienna `limit` typu `int` jest już zdefiniowana. Zadeklaruj zmienną `sum` zainicjowaną przez natychmiastowe wywołanie anonimowej funkcji bez parametrów, która sumuje wszystkie liczby całkowite od `1` do `limit` za pomocą pętli i zwraca sumę.

# --before-seed--

```dart
dynamic result(int limit) {
```

# --seed--

```dart
var sum = 
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` powinno być typu `int`

```dart
  test('test1', () {
    expect(result(100), isA<int>(), reason: '--err-t1--');
  });
```

`sum` powinno być `5050`, gdy `limit` wynosi `100`

```dart
  test('test2', () {
    expect(result(100), 5050, reason: '--err-t2--');
  });
```

`sum` powinno być `55`, gdy `limit` wynosi `10`

```dart
  test('test3', () {
    expect(result(10), 55, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var sum = () {
  var total = 0;
  for (var i = 1; i <= limit; i++) {
    total += i;
  }
  return total;
}();
```
