---
language: dart
exerciseType: 1
---

# --description--

Funkcje, które przyjmują i zwracają inne funkcje, można swobodnie łączyć. Klasycznym przykładem jest **kompozycja**: budowanie nowej funkcji, która uruchamia jedną funkcję, a następnie przekazuje jej wynik do innej:

```dart
int Function(int) then(int Function(int) first, int Function(int) second) {
  return (n) => second(first(n));
}
```

# --instructions--

Napisz funkcję `compose`, która przyjmuje dwa parametry `f` i `g`, oba typu `int Function(int)`, i zwraca funkcję typu `int Function(int)`, która stosuje `f` do swojego argumentu, a następnie `g` do wyniku.

Przykład:
```dart
var addThenDouble = compose((n) => n + 1, (n) => n * 2);
print(addThenDouble(3)); // 8
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`compose((n) => n + 1, (n) => n * 2)(3)` powinno być `8`

```dart
  test('test1', () {
    expect(compose((n) => n + 1, (n) => n * 2)(3), 8, reason: '--err-t1--');
  });
```

`compose((n) => n * 2, (n) => n + 1)(3)` powinno być `7`

```dart
  test('test2', () {
    expect(compose((n) => n * 2, (n) => n + 1)(3), 7, reason: '--err-t2--');
  });
```

Zwrócona funkcja powinna mieć typ `int Function(int)`

```dart
  test('test3', () {
    var negate = compose((n) => -n, (n) => n * n);
    expect(negate, isA<int Function(int)>(), reason: '--err-t3--');
    expect(negate(5), 25, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int Function(int) compose(int Function(int) f, int Function(int) g) {
  return (int n) => g(f(n));
}
```
