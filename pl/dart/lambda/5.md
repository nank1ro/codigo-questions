---
language: dart
exerciseType: 1
---

# --description--

Ponieważ typ funkcyjny jest zwykłym typem, funkcja może przyjmować **inną funkcję jako parametr**. W ciele funkcji parametr wywołuje się jak każdą inną funkcję:

```dart
int apply(int n, int Function(int) operation) {
  return operation(n);
}

print(apply(5, (n) => n * 2)); // 10
print(apply(5, (n) => n - 1)); // 4
```

Tutaj wywołujący decyduje, co robi `apply`, przekazując anonimową funkcję jako drugi argument.

# --instructions--

Napisz funkcję `applyTwice`, która przyjmuje parametr `n` typu `int` oraz parametr funkcyjny `operation` typu `int Function(int)` i zwraca wynik dwukrotnego zastosowania `operation` do `n`.

Przykład:
```dart
print(applyTwice(3, (n) => n * 2)); // 12
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`applyTwice(3, (n) => n * 2)` powinno być `12`

```dart
  test('test1', () {
    expect(applyTwice(3, (n) => n * 2), 12, reason: '--err-t1--');
  });
```

`applyTwice(10, (n) => n + 5)` powinno być `20`

```dart
  test('test2', () {
    expect(applyTwice(10, (n) => n + 5), 20, reason: '--err-t2--');
  });
```

`applyTwice(2, (n) => n * n)` powinno być `16`

```dart
  test('test3', () {
    expect(applyTwice(2, (n) => n * n), 16, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int applyTwice(int n, int Function(int) operation) {
  return operation(operation(n));
}
```
