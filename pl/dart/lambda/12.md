---
language: dart
exerciseType: 1
---

# --description--

Domknięcie nie tylko odczytuje przechwycone zmienne: może je też **modyfikować**, a zmiany są zachowywane między wywołaniami. Dzięki temu można przechowywać prywatny stan bez klasy:

```dart
int Function() makeTimer() {
  var seconds = 0;
  return () {
    seconds += 10;
    return seconds;
  };
}

var timer = makeTimer();
print(timer()); // 10
print(timer()); // 20
```

Każde wywołanie `makeTimer()` tworzy zupełnie nową zmienną `seconds`, więc dwa zegary nigdy nie współdzielą swojego licznika.

# --instructions--

Napisz funkcję `makeCounter` bez parametrów, która zwraca funkcję typu `int Function()`. Za każdym razem, gdy zwrócona funkcja jest wywoływana, musi inkrementować przechwycony licznik i zwracać go, zaczynając od `1`.

Przykład:
```dart
var counter = makeCounter();
print(counter()); // 1
print(counter()); // 2
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Pierwsze wywołanie licznika powinno zwrócić `1`

```dart
  test('test1', () {
    var counter = makeCounter();
    expect(counter(), 1, reason: '--err-t1--');
  });
```

Trzy wywołania tego samego licznika powinny zwrócić `1`, `2` i `3`

```dart
  test('test2', () {
    var counter = makeCounter();
    expect([counter(), counter(), counter()], [1, 2, 3], reason: '--err-t2--');
  });
```

Dwa liczniki powinny być niezależne

```dart
  test('test3', () {
    var a = makeCounter();
    var b = makeCounter();
    a();
    a();
    expect(b(), 1, reason: '--err-t3--');
    expect(a(), 3, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int Function() makeCounter() {
  var count = 0;
  return () {
    count++;
    return count;
  };
}
```
