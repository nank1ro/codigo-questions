---
language: dart
exerciseType: 2
---

# --description--

Gdy ciało jest pojedynczym wyrażeniem, funkcja anonimowa może użyć **składni strzałki** `=>`, tak jak funkcja nazwana. Strzałka zastępuje nawiasy klamrowe i słowo kluczowe `return`:

```dart
var add = (int a, int b) => a + b;

print(add(2, 3)); // 5
```

Ta krótka forma jest zdecydowanie najczęstszym sposobem pisania funkcji anonimowych w Dart.

# --instructions--

Uzupełnij kod, aby zapisać w `triple` funkcję strzałkową zwracającą swój argument pomnożony przez `3`, tak aby program wypisał `12`.

# --seed--

```dart
void main() {
  var triple = (int n) [/] n [/] 3;
  print(triple(4));
}
```

# --answers--

- =>
- *
- ->
- +
- return
- {

# --solutions--

```dart
void main() {
  var triple = (int n) => n * 3;
  print(triple(4));
}
```

# --output--

12
