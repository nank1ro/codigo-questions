---
language: dart
exerciseType: 4
---

# --description--

Funkcje anonimowe obsługują te same rodzaje parametrów co funkcje nazwane: parametry **opcjonalne pozycyjne** w `[]` i parametry **nazwane** w `{}`, oba z wartościami domyślnymi:

```dart
var repeat = (String text, [int times = 2]) => text * times;
print(repeat('ab'));    // abab
print(repeat('ab', 3)); // ababab

var describe = ({required String item, int count = 1}) => '$count x $item';
print(describe(item: 'apple'));           // 1 x apple
print(describe(item: 'pear', count: 4));  // 4 x pear
```

# --instructions--

Posortuj elementy, aby zapisać w `greet` anonimową funkcję z nazwanym parametrem `name`, którego wartością domyślną jest `'World'`, a następnie wywołać ją najpierw bez argumentów, a potem z `name: 'Dart'`. Program musi wypisać:

```
Hello, World!
Hello, Dart!
```

# --answers--

```dart
void main() {
```

```dart
  var greet = ({String name = 'World'}) {
```

```dart
    print('Hello, $name!');
```

```dart
  };
```

```dart
  greet();
```

```dart
  greet(name: 'Dart');
```

```dart
}
```

# --solutions--

```dart
void main() {
  var greet = ({String name = 'World'}) {
    print('Hello, $name!');
  };
  greet();
  greet(name: 'Dart');
}
```

# --output--

Hello, World!
Hello, Dart!
