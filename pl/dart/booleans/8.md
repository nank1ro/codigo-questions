---
language: dart
exerciseType: 1
---

# --instructions--

Zadeklaruj zmienną logiczną o nazwie `isEmpty` i przypisz jej wartość `false`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isEmpty` musi być równa `false`.

```dart
  test('test1', () {
    expect(isEmpty, false, reason: '--err-t1--');
  });
```

`isEmpty` musi być typu `bool`.

```dart
  test('test2', () {
    expect(isEmpty, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isEmpty = false;
```
