---
language: dart
exerciseType: 1
---

# --instructions--

Połącz `&&`, `||` i `!` aby uzyskać wynik.

Zadeklaruj zmienną logiczną o nazwie `result` używając tego dokładnego wyrażenia: `!false && (true || false)`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` musi być równa `true`.

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result` musi być typu `bool`.

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = !false && (true || false);
```
