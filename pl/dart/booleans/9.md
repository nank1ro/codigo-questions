---
language: dart
exerciseType: 1
---

# --instructions--

Zadeklaruj zmienną logiczną o nazwie `flag` z wartością `true`, a następnie zadeklaruj inną zmienną logiczną o nazwie `isFlag` i użyj słowa kluczowego `is` aby sprawdzić, czy `flag` jest typu `bool`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`flag` musi być równa `true`.

```dart
  test('test1', () {
    expect(flag, true, reason: '--err-t1--');
  });
```

`isFlag` musi być `true` (wynik sprawdzenia `is bool`).

```dart
  test('test2', () {
    expect(isFlag, true, reason: '--err-t2--');
  });
```

`isFlag` musi być typu `bool`.

```dart
  test('test3', () {
    expect(isFlag, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool flag = true;
bool isFlag = flag is bool;
```
