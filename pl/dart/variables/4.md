---
language: dart
exerciseType: 1
---

# --description--

Typ `int` pozwala przechowywać liczby całkowite.
Aby zamiast tego zapisać liczby dziesiętne, możemy użyć typu `double`:

```dart
double pi = 3.14159;
```

Ten przykład jest podobny do poprzedniego. Tym razem jednak zmienna jest typu `double`, który pozwala przechowywać liczby dziesiętne z wysoką precyzją.


# --instructions--

Przypisz zmiennej `x` wartość `6.28`.

# --before-seed--

```dart
dynamic number() {
```

# --after-seed--

```dart
  return x;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x` musi być równe `6.28`.

```dart
  test('test1', () {
    expect(number(), 6.28, reason: '--err-t1--');
  });
```

`x` musi być typu `double`

```dart
    test('test2', () {
      expect(number(), TypeMatcher<double>(), reason: '--err-t2--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double x = 6.28;
```
