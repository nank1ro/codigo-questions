---
language: dart
exerciseType: 1
---

# --instructions--

Użyj pętli `while` z `break`, aby znaleźć pierwszą wielokrotność `7` większą niż `50`. Zwróć tę liczbę.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` musi być równe `56`

```dart
  test('test1', () {
    expect(computeValue(), 56, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int result = 0;
int n = 51;
while (true) {
  if (n % 7 == 0) {
    result = n;
    break;
  }
  n++;
}
```
