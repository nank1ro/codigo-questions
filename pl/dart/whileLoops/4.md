---
language: dart
exerciseType: 2
---

# --description--

Warunek pętli `while` może zawierać dowolne wyrażenie logiczne. Gdy tylko warunek przyjmuje wartość `false`, pętla się kończy.

```dart
int n = 100;
while (n > 1) {
  n ~/= 2;
}
print(n); // 0
```

Tutaj `n` jest dzielone przez dwa przy każdej iteracji przy użyciu dzielenia całkowitego (`~/`).

# --instructions--

Uzupełnij pętlę `while`, która ciągle dzieli `n` przez dwa (używając `~/= 2`) dopóki jest większe od `1`, a następnie wyświetla wynik.

# --seed--

```dart
void main() {
  int n = 64;
  while (n [/] 1) {
    n [/] 2;
  }
  print(n);
}
```

# --answers--

- >
- <
- ~/=
- /=
- *=

# --solutions--

```dart
void main() {
  int n = 64;
  while (n > 1) {
    n ~/= 2;
  }
  print(n);
}
```

# --output--

1
