---
language: dart
exerciseType: 1
---

# --instructions--

Zacznij od `product = 1` i użyj pętli `while`, aby ciągle mnożyć przez `2`, dopóki `product` nie będzie większe lub równe `100`. Zwróć `product`.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return product;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`product` musi być równe `128`

```dart
  test('test1', () {
    expect(computeValue(), 128, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int product = 1;
while (product < 100) {
  product *= 2;
}
```
