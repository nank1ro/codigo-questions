---
language: dart
exerciseType: 1
---

# --instructions--

Użyj pętli `do-while`, aby dodawać `3` do zmiennej `total` zaczynającej od `0`, dopóki `total` nie osiągnie lub nie przekroczy `12`. Zwróć `total`.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` musi być równe `12`

```dart
  test('test1', () {
    expect(computeValue(), 12, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 0;
do {
  total += 3;
} while (total < 12);
```
