---
language: dart
exerciseType: 2
---

# --description--

Słowo kluczowe `break` natychmiast przerywa pętlę, niezależnie od jej warunku.

```dart
int i = 0;
while (true) {
  if (i == 3) break;
  print(i);
  i++;
}
```

Bez `break` pętla `while (true)` działałaby w nieskończoność. Tutaj zatrzymuje się, gdy `i` osiągnie `3`.

# --instructions--

Uzupełnij pętlę, która wyświetla liczby od `0` i używa `break`, aby zatrzymać się gdy `i` równa się `4`.

# --seed--

```dart
void main() {
  int i = 0;
  while ([/]) {
    if (i == [/]) [/];
    print(i);
    i++;
  }
}
```

# --answers--

- true
- false
- 4
- 3
- break
- continue
- return

# --solutions--

```dart
void main() {
  int i = 0;
  while (true) {
    if (i == 4) break;
    print(i);
    i++;
  }
}
```

# --output--

0
1
2
3
