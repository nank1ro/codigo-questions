---
language: dart
exerciseType: 1
---

# --description--

Wewnątrz funkcji `async` `await` pozwala pisać kroki asynchroniczne tak, jakby były zwykłym kodem sekwencyjnym. Każdy `await` czeka na swoje future, a następna linia wykonuje się dopiero wtedy, gdy wartość jest już dostępna:

```dart
Future<int> width() => Future.delayed(const Duration(milliseconds: 5), () => 4);
Future<int> height() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<int> area() async {
  final w = await width();
  final h = await height();
  return w * h; // 12
}
```

`await` można też stosować bezpośrednio w wyrażeniu: `return await width() * await height();` daje ten sam wynik.

# --instructions--

Funkcja `price(item)` zwraca `Future<int>` z ceną `item`. Użyj `await`, aby pobrać cenę `'apple'` i cenę `'milk'`, i zapisz ich sumę w `total`.

# --before-seed--

```dart
const prices = {'apple': 3, 'bread': 5, 'milk': 2};

Future<int> price(String item) {
  return Future.delayed(const Duration(milliseconds: 5), () => prices[item]!);
}

Future<dynamic> result() async {
```

# --seed--

```dart
var total = 0;
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` powinno wynosić `5`

```dart
  test('test1', () async {
    expect(await result(), 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var total = 0;
total = await price('apple') + await price('milk');
```
