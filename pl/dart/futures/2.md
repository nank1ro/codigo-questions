---
language: dart
exerciseType: 1
---

# --description--

Oznaczenie funkcji jako `async` robi dwie rzeczy: pozwala używać `await` w ciele i sprawia, że funkcja **zwraca `Future`**. To, co zwrócisz przez `return`, staje się wartością, którą future się kończy, więc zadeklarowany typ zwracany to `Future<T>`, mimo że ciało zwraca zwykłe `T`:

```dart
Future<String> label(int n) async {
  return 'item $n';
}

Future<void> main() async {
  print(await label(3)); // item 3
}
```

Nie potrzebujesz tu `Future.value`: słowo kluczowe `async` samo opakowuje zwróconą wartość.

# --instructions--

Napisz funkcję `async` `greet`, która przyjmuje `String` `name` i zwraca `Future<String>` kończące się wartością `Hello, ` z dopisanym imieniem i wykrzyknikiem.

Przykład:
```dart
print(await greet('Ada')); // Hello, Ada!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greet('Ada')` powinno zakończyć się wartością `'Hello, Ada!'`

```dart
  test('test1', () async {
    expect(await greet('Ada'), 'Hello, Ada!', reason: '--err-t1--');
  });
```

`greet('Linus')` powinno zakończyć się wartością `'Hello, Linus!'`

```dart
  test('test2', () async {
    expect(await greet('Linus'), 'Hello, Linus!', reason: '--err-t2--');
  });
```

`greet` powinno zwracać `Future<String>`

```dart
  test('test3', () async {
    expect(greet('Bob'), isA<Future<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<String> greet(String name) async {
  return 'Hello, $name!';
}
```
