---
language: dart
exerciseType: 4
---

# --description--

`Future.wait` to narzędzie do "wczytaj kilka rzeczy, a potem działaj dalej". Typowy schemat to: zbuduj listę future, zrób na niej `await Future.wait`, a potem użyj powstałej listy:

```dart
Future<int> stock() => Future.delayed(const Duration(milliseconds: 10), () => 8);
Future<int> orders() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<void> main() async {
  final counts = await Future.wait([stock(), orders()]);
  print(counts); // [8, 3]
}
```

`orders()` kończy się pierwsze, ale lista i tak zachowuje kolejność wywołań: najpierw `stock()`, potem `orders()`.

# --instructions--

Uporządkuj elementy, aby zdefiniować funkcję `one`, która kończy się wartością `1` po `10` milisekundach, funkcję `two`, która kończy się wartością `2` po `5` milisekundach, oraz `main` z `async`, która wypisuje `loading`, czeka na obie funkcje przez `Future.wait` do `values` i wypisuje `values`. Zdefiniuj najpierw `one`, potem `two`, potem `main`. Program musi wypisać:

```
loading
[1, 2]
```

# --answers--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
```

```dart
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);
```

```dart
Future<void> main() async {
```

```dart
  print('loading');
```

```dart
  final values = await Future.wait([one(), two()]);
```

```dart
  print(values);
```

```dart
}
```

# --solutions--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);

Future<void> main() async {
  print('loading');
  final values = await Future.wait([one(), two()]);
  print(values);
}
```

# --output--

loading
[1, 2]
