---
language: dart
exerciseType: 1
---

# --description--

Gdy kilka future nie zależy od siebie nawzajem, przekaż je wszystkie do **`Future.wait`**: przyjmuje `List<Future<T>>`, pozwala im działać w tym samym czasie i zwraca jedno `Future<List<T>>`, które kończy się, gdy **wszystkie** są gotowe. Wyniki zachowują kolejność listy wejściowej, niezależnie od tego, które future skończyło pierwsze:

```dart
Future<String> slow() => Future.delayed(const Duration(milliseconds: 20), () => 'slow');
Future<String> fast() => Future.delayed(const Duration(milliseconds: 5), () => 'fast');

final results = await Future.wait([slow(), fast()]);
print(results); // [slow, fast]
```

`await slow(); await fast();` zajmuje `25` milisekund, bo `fast()` jest wywoływane dopiero, gdy `slow()` się zakończy; `await Future.wait([slow(), fast()])` zajmuje około `20`, czyli tyle, ile trwa najdłuższe.

# --instructions--

Napisz funkcję `async` `sumOfFutures`, która przyjmuje `List<Future<int>>` `futures`, czeka na wszystkie za pomocą `Future.wait` i zwraca `Future<int>` kończące się sumą ich wartości. Pusta lista daje `0`.

Przykład:
```dart
final futures = [Future.value(1), Future.delayed(const Duration(milliseconds: 5), () => 2)];
print(await sumOfFutures(futures)); // 3
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumOfFutures` dla future kończących się wartościami `1`, `2` i `3` powinno zakończyć się wartością `6`

```dart
  test('test1', () async {
    final futures = [
      Future.value(1),
      Future.delayed(const Duration(milliseconds: 5), () => 2),
      Future.value(3),
    ];
    expect(await sumOfFutures(futures), 6, reason: '--err-t1--');
  });
```

`sumOfFutures` dla pojedynczego future kończącego się wartością `10` powinno zakończyć się wartością `10`

```dart
  test('test2', () async {
    expect(await sumOfFutures([Future.delayed(const Duration(milliseconds: 5), () => 10)]), 10, reason: '--err-t2--');
  });
```

`sumOfFutures` dla pustej listy powinno zakończyć się wartością `0`, a `sumOfFutures` powinno zwracać `Future<int>`

```dart
  test('test3', () async {
    expect(await sumOfFutures(<Future<int>>[]), 0, reason: '--err-t3--');
    expect(sumOfFutures([Future.value(4)]), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> sumOfFutures(List<Future<int>> futures) async {
  final values = await Future.wait(futures);
  var total = 0;
  for (final v in values) {
    total += v;
  }
  return total;
}
```
