---
language: dart
exerciseType: 1
---

# --description--

Ponieważ `values` jest listą, możesz łączyć ją z metodami list, które już znasz. Na przykład `.map()` z `.name` zamienia wartości na listę ciągów znaków:

```dart
enum Direction { north, south, east, west }

List<String> names = Direction.values.map((d) => d.name).toList();
print(names); // [north, south, east, west]
```

# --instructions--

Zadeklaruj enum `Weekday` z wartościami `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday` i `sunday`, w tej kolejności. Następnie napisz funkcję `dayNames()`, która zwraca `List<String>` z nazwą każdego dnia tygodnia, w kolejności deklaracji.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Weekday.values.length` powinno być `7`

```dart
  test('test1', () {
    expect(Weekday.values.length, 7, reason: '--err-t1--');
  });
```

`Weekday.values[2]` powinno być `Weekday.wednesday`

```dart
  test('test2', () {
    expect(Weekday.values[2], Weekday.wednesday, reason: '--err-t2--');
  });
```

`dayNames()` musi zwracać siedem nazw w kolejności

```dart
  test('test3', () {
    expect(dayNames(), ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Weekday { monday, tuesday, wednesday, thursday, friday, saturday, sunday }

List<String> dayNames() {
  return Weekday.values.map((day) => day.name).toList();
}
```
