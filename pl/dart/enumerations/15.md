---
language: dart
exerciseType: 1
---

# --description--

Wartości enum świetnie sprawdzają się jako **klucze mapy**: są unikalne, łatwe do porównania, a kompilator sprawdza, że używasz tylko rzeczywistych wartości. Zadeklaruj mapę z enumem jako typem klucza i wyszukuj wartości za pomocą `[]`:

```dart
enum Direction { north, south, east, west }

Map<Direction, String> arrows = {
  Direction.north: '^',
  Direction.south: 'v',
  Direction.east: '>',
  Direction.west: '<',
};

print(arrows[Direction.east]); // >
```

Podobnie jak w każdej mapie, wyszukanie zwraca `null`, gdy brakuje klucza, więc użyj `??`, aby podać wartość zastępczą.

# --instructions--

Mając `enum Size { small, medium, large }`, zadeklaruj na najwyższym poziomie `Map<Size, int>` o nazwie `prices`, w której `small` kosztuje `2`, `medium` kosztuje `3`, a `large` kosztuje `4`. Następnie napisz funkcję `totalPrice(List<Size> order)`, która zwraca sumę cen wszystkich rozmiarów w `order`.

# --seed--

```dart
enum Size { small, medium, large }
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`prices[Size.medium]` powinno być `3`

```dart
  test('test1', () {
    expect(prices[Size.medium], 3, reason: '--err-t1--');
  });
```

`totalPrice([Size.small, Size.large])` powinno być `6`

```dart
  test('test2', () {
    expect(totalPrice([Size.small, Size.large]), 6, reason: '--err-t2--');
  });
```

`totalPrice([Size.medium, Size.medium, Size.small])` powinno być `8`

```dart
  test('test3', () {
    expect(totalPrice([Size.medium, Size.medium, Size.small]), 8, reason: '--err-t3--');
  });
```

`totalPrice([])` powinno być `0`

```dart
  test('test4', () {
    expect(totalPrice([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Size { small, medium, large }

Map<Size, int> prices = {
  Size.small: 2,
  Size.medium: 3,
  Size.large: 4,
};

int totalPrice(List<Size> order) {
  int total = 0;
  for (var size in order) {
    total += prices[size] ?? 0;
  }
  return total;
}
```
