---
language: dart
exerciseType: 1
---

# --description--

Każda wartość enum ma dwie wbudowane właściwości:

- `name` to nazwa wartości jako `String`
- `index` to jej pozycja w deklaracji, licząc od `0`

```dart
enum Direction { north, south, east, west }

print(Direction.east.name);  // east
print(Direction.east.index); // 2
```

# --instructions--

Zadeklaruj enum `Season` z wartościami `spring`, `summer`, `autumn` i `winter`, w tej kolejności.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Season.spring.index` powinno być `0`

```dart
  test('test1', () {
    expect(Season.spring.index, 0, reason: '--err-t1--');
  });
```

`Season.autumn.index` powinno być `2`

```dart
  test('test2', () {
    expect(Season.autumn.index, 2, reason: '--err-t2--');
  });
```

`Season.winter.index` powinno być `3`

```dart
  test('test3', () {
    expect(Season.winter.index, 3, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Season { spring, summer, autumn, winter }
```
