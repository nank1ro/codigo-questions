---
language: dart
exerciseType: 2
---

# --description--

Od Dart 3 `switch` może być również używany jako **wyrażenie**, które zwraca wartość. Każdy case zapisuje się jako `wzorzec => wartość`, a case'y oddziela się przecinkami, bez słowa kluczowego `case` i bez `break`:

```dart
enum Direction { north, south, east, west }

String arrow(Direction direction) => switch (direction) {
      Direction.north => '^',
      Direction.south => 'v',
      Direction.east => '>',
      Direction.west => '<',
    };
```

Podobnie jak w formie instrukcji, wyrażenie switch nad enumem musi być wyczerpujące.

# --instructions--

Uzupełnij wyrażenie switch, aby `label` zwracało pierwszą literę każdego rozmiaru, a następnie wypisz label dla `Size.large`.

# --seed--

```dart
enum Size { small, medium, large }

String label(Size size) => [/] (size) {
      Size.small [/] 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --answers--

- switch
- =>
- case
- when
- ->
- :

# --solutions--

```dart
enum Size { small, medium, large }

String label(Size size) => switch (size) {
      Size.small => 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --output--

L
