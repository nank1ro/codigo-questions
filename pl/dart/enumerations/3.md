---
language: dart
exerciseType: 2
---

# --description--

Każdy enum ma też stałą listę o nazwie `values`, która zawiera wszystkie jego wartości w kolejności deklaracji. Możesz ją indeksować jak każdą listę, odczytać jej `length` lub przejść przez nią pętlą `for-in`:

```dart
enum Direction { north, south, east, west }

print(Direction.values.length); // 4
print(Direction.values[1]);     // Direction.south

for (var direction in Direction.values) {
  print(direction.name);
}
```

# --instructions--

Uzupełnij kod, aby wypisać nazwę `Planet.earth`, jej indeks i całkowitą liczbę planet.

# --seed--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.[/]);
  print(Planet.earth.[/]);
  print(Planet.[/].length);
}
```

# --answers--

- name
- index
- values
- ordinal
- position
- count

# --solutions--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.name);
  print(Planet.earth.index);
  print(Planet.values.length);
}
```

# --output--

earth
2
3
