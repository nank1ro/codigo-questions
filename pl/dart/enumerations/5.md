---
language: dart
exerciseType: 4
---

# --description--

Iterowanie po `values` to zwykły sposób na przetworzenie każdej wartości enuma. Wewnątrz pętli bieżąca wartość zachowuje się jak każdy inny obiekt, więc możesz odczytać jej `index` i `name` i użyć ich bezpośrednio w interpolacji łańcucha:

```dart
enum Direction { north, south, east, west }

for (var direction in Direction.values) {
  print('${direction.index}: ${direction.name}');
}
```

# --instructions--

Uporządkuj elementy, aby zadeklarować enum `Level` i wypisać dla każdej wartości jej indeks oraz nazwę. Zadeklaruj enum przed `main`. Program musi wypisać:

```
0: low
1: medium
2: high
```

# --answers--

```dart
enum Level { low, medium, high }
```

```dart
void main() {
```

```dart
  for (var level in Level.values) {
```

```dart
    print('${level.index}: ${level.name}');
```

```dart
  }
}
```

# --solutions--

```dart
enum Level { low, medium, high }
void main() {
  for (var level in Level.values) {
    print('${level.index}: ${level.name}');
  }
}
```

# --output--

0: low
1: medium
2: high
