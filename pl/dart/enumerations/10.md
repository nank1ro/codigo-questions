---
language: dart
exerciseType: 1
---

# --description--

Od Dart 2.17 enum może deklarować **pola** i **konstruktor**, tak jak klasa. Nazywa się to *rozszerzonym enumem*. Każda wartość przekazuje wtedy swoje własne argumenty do konstruktora:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);
}

print(Planet.mars.moons); // 2
```

Zwróć uwagę na trzy zasady: lista wartości kończy się **średnikiem** `;`, pola muszą być `final`, a konstruktor musi być `const`.

# --instructions--

Zadeklaruj enum `Coin` z polem `final int` o nazwie `cents` oraz wartościami `penny(1)`, `nickel(5)`, `dime(10)` i `quarter(25)`, w tej kolejności.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Coin.penny.cents` powinno być `1`, a `Coin.nickel.cents` powinno być `5`

```dart
  test('test1', () {
    expect(Coin.penny.cents, 1, reason: '--err-t1--');
    expect(Coin.nickel.cents, 5, reason: '--err-t2--');
  });
```

`Coin.dime.cents` powinno być `10`

```dart
  test('test2', () {
    expect(Coin.dime.cents, 10, reason: '--err-t3--');
  });
```

`Coin.quarter.cents` powinno być `25`

```dart
  test('test3', () {
    expect(Coin.quarter.cents, 25, reason: '--err-t4--');
  });
```

`Coin.values` powinno mieć 4 wartości, z `penny` na pierwszym miejscu

```dart
  test('test4', () {
    expect(Coin.values.length, 4, reason: '--err-t5--');
    expect(Coin.values[0], Coin.penny, reason: '--err-t6--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Coin {
  penny(1),
  nickel(5),
  dime(10),
  quarter(25);

  final int cents;

  const Coin(this.cents);
}
```
