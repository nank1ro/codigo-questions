---
language: dart
exerciseType: 1
---

# --description--

Metoda `.remove(value)` usuwa **pierwszy** element równy `value` z listy. Zwraca `true`, jeśli element został usunięty, lub `false`, jeśli wartości nie znaleziono.

```dart
var colors = ['red', 'green', 'blue'];
bool removed = colors.remove('green');
print(removed); // true
print(colors); // [red, blue]
```

# --instructions--

Zacznij od `List<String>` zawierającej `['cat', 'dog', 'bird']`. Usuń `'dog'` za pomocą `.remove()`. Zwróć wynikową listę.

# --before-seed--

```dart
List<String> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Lista nie zawiera już 'dog'

```dart
  test('dog removed', () {
    expect(computeValue(), ['cat', 'bird'], reason: '--err-t1--');
  });
```

Długość wynosi 2

```dart
  test('length is 2', () {
    expect(computeValue().length, 2, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<String> result = ['cat', 'dog', 'bird'];
  result.remove('dog');
```
