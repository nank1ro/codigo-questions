---
language: dart
exerciseType: 1
---

# --instructions--

Deklaruj zmienną `bool` na poziomie górnym o nazwie `canVote`. Powinna być `true` tylko wtedy, gdy zmienna `age` wynosi `18` LUB `parental_consent` jest `true`. Użyj zmiennych `age` = `16` i `parental_consent` = `true`.

# --solutions--

```dart
int number = 42;
int min = 1;
int max = 100;
bool inRange = number >= min && number <= max;

void main() {
  print(inRange);
}
```

# --asserts--

`canVote` musi być `true` i być typu `bool`

```dart
  test('test1', () {
    expect(inRange, true, reason: '--err-t1--');
    expect(inRange, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```
