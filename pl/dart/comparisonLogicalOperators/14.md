---
language: dart
exerciseType: 1
---

# --description--

Operator **AND** `&&` łączy dwa wyrażenia logiczne. Zwraca `true` tylko wtedy, gdy **obie** strony są `true`. Jeśli choćby jedna strona jest `false`, wynik to `false`.

```dart
bool a = true;
bool b = true;
bool result = a && b; // true

bool c = true;
bool d = false;
bool result2 = c && d; // false
```

# --instructions--

Deklaruj dwie zmienne `int` na poziomie górnym: `age` o wartości `25` i `score` o wartości `80`. Następnie deklaruj zmienną `bool` na poziomie górnym o nazwie `qualifies`, która jest `true` tylko wtedy, gdy `age` jest większy lub równy `18` I `score` jest większy lub równy `60`.

# --solutions--

```dart
int age = 25;
int score = 80;
bool qualifies = age >= 18 && score >= 60;

void main() {
  print(qualifies);
}
```

# --asserts--

`qualifies` musi być `true` i być typu `bool`

```dart
  test('test1', () {
    expect(qualifies, true, reason: '--err-t1--');
    expect(qualifies, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```
