---
language: dart
exerciseType: 4
---

# --instructions--

Posortuj linie kodu, aby zadeklarować zmienną logiczną, połączyć dwie zmienne za pomocą operatora `&&`, zapisać wynik w zmiennej i wydrukować go. Wynik powinien być `true`.

# --answers--

```dart
void main() {
```

```dart
  bool hasPass = false;
```

```dart
  bool isAdmin = true;
```

```dart
  bool outcome = hasPass || isAdmin && !hasPass;
```

```dart
  print(outcome);
```

```dart
}
```

# --solutions--

```dart
void main() {
  bool hasPass = false;
  bool isAdmin = true;
  bool outcome = hasPass || isAdmin && !hasPass;
  print(outcome);
}
```

# --output--

true
