---
language: dart
exerciseType: 2
---

# --description--

Gdy konstruktor podklasy musi wywołać konstruktor nadklasy, używa się słowa kluczowego `super` na **liście inicjalizacyjnej**:

```dart
class Vehicle {
  String brand;
  Vehicle(this.brand);
}

class Car extends Vehicle {
  int doors;
  Car(String brand, this.doors) : super(brand);
}
```

`super(brand)` przekazuje argument `brand` do konstruktora klasy `Vehicle`.

# --instructions--

Uzupełnij konstruktor klasy `Cat`, tak aby wywoływał konstruktor nadklasy `Animal` z argumentem `name`.

# --seed--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Cat extends Animal {
  Cat(String name) : [/](name);
}
```

# --answers--

- super
- Animal
- parent
- base
- this

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Cat extends Animal {
  Cat(String name) : super(name);
}
```
