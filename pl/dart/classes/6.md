---
language: dart
exerciseType: 1
---

# --description--

`this` odnosi się do **bieżącej instancji** klasy, czyli obiektu, na którym wywołano metodę. Wewnątrz metody można go użyć, aby uzyskać dostęp do pól tego obiektu:

```dart
class Circle {
  double radius;

  Circle(this.radius);

  double diameter() {
    return this.radius * 2;
  }
}
```

Tutaj `this.radius` odczytuje pole `radius` okręgu, na którym wywołano `diameter()`. Gdy nie ma innej zmiennej o tej samej nazwie, `this.` można pominąć: `radius * 2` działa tak samo.

# --instructions--

Utwórz klasę `Rectangle` z dwoma polami `width` i `height` typu `double`, ustawianymi przez konstruktor `Rectangle(this.width, this.height)`. Dodaj metodę `area()`, która zwraca `this.width * this.height`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Rectangle` ma poprawne pole powierzchni

```dart
    test('area', () {
      var r = Rectangle(4.0, 5.0);
      expect(r.area(), 20.0, reason: '--err-t1--');
    });
```

`width` i `height` są poprawnie przechowywane

```dart
    test('fields', () {
      var r = Rectangle(3.0, 7.0);
      expect(r.width, 3.0, reason: '--err-t2--');
      expect(r.height, 7.0, reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Rectangle {
  double width;
  double height;

  Rectangle(this.width, this.height);

  double area() {
    return this.width * this.height;
  }
}
```
