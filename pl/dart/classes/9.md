---
language: dart
exerciseType: 2
---

# --description--

**Setter** to specjalna metoda pozwalająca przypisać wartość z jednoczesnym uruchomieniem logiki walidacji. Definiuje się ją za pomocą słowa kluczowego `set`:

```dart
class Temperature {
  double _celsius = 0;

  double get celsius => _celsius;

  set celsius(double value) {
    if (value < -273.15) throw ArgumentError('Too cold!');
    _celsius = value;
  }
}
```

Nazwa pola jest często poprzedzona znakiem `_`, co oznacza, że jest prywatna.

# --instructions--

Uzupełnij setter `age` w klasie `Person`, który przypisuje `value` do `_age` tylko wtedy, gdy `value >= 0`.

# --seed--

```dart
class Person {
  int _age = 0;

  int get age => _age;

  [/] age(int value) {
    if (value >= 0) _age = [/];
  }
}
```

# --answers--

- set
- value
- get
- _age
- age
- 0

# --solutions--

```dart
class Person {
  int _age = 0;

  int get age => _age;

  set age(int value) {
    if (value >= 0) _age = value;
  }
}
```
