---
language: dart
exerciseType: 2
---

# --description--

**Getter** to specjalna metoda odczytująca obliczoną lub prywatną wartość, wyglądająca jak dostęp do właściwości. Definiuje się ją za pomocą słowa kluczowego `get`:

```dart
class Circle {
  double radius;

  Circle(this.radius);

  double get area => 3.14159 * radius * radius;
}
```

Do gettera odwołuje się jak do pola: `circle.area` (bez nawiasów).

# --instructions--

Uzupełnij getter `perimeter` w klasie `Square`, który zwraca `4 * side`.

# --seed--

```dart
class Square {
  double side;

  Square(this.side);

  double [/] perimeter => [/] * side;
}
```

# --answers--

- get
- 4
- set
- 2
- double
- perimeter

# --solutions--

```dart
class Square {
  double side;

  Square(this.side);

  double get perimeter => 4 * side;
}
```
