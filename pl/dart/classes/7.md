---
language: dart
exerciseType: 2
---

# --description--

Dart obsługuje **nazwane konstruktory**, które umożliwiają definiowanie dodatkowych sposobów tworzenia obiektu. Nazwane konstruktory zapisuje się jako `NazwaKlasy.nazwaKonstruktora`:

```dart
class Point {
  double x;
  double y;

  Point(this.x, this.y);

  Point.origin()
      : x = 0,
        y = 0;
}
```

Obiekt w początku układu współrzędnych tworzy się wówczas za pomocą: `var p = Point.origin();`

# --instructions--

Uzupełnij nazwany konstruktor `zero` klasy `Counter`, który ustawia `count` na `0`.

# --seed--

```dart
class Counter {
  int count;

  Counter(this.count);

  Counter.[/]() : count = [/];
}
```

# --answers--

- zero
- 0
- count
- init
- 1
- reset
- start

# --solutions--

```dart
class Counter {
  int count;

  Counter(this.count);

  Counter.zero() : count = 0;
}
```
