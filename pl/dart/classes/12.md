---
language: dart
exerciseType: 1
---

# --description--

**Nadpisywanie metod** pozwala podklasie na dostarczenie własnej implementacji metody, która już istnieje w nadklasie. Używa się do tego adnotacji `@override`:

```dart
class Shape {
  double area() => 0;
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() => 3.14159 * radius * radius;
}
```

Adnotacja `@override` informuje Dart (i innych programistów), że celowo zastępujesz metodę nadklasy.

# --instructions--

Utwórz klasę `Animal` z metodą `sound()`, która zwraca `String` `'...'`. Następnie utwórz podklasę `Dog`, która nadpisuje `sound()` i zwraca `'Woof'`. Dodaj kolejną podklasę `Cat`, która nadpisuje `sound()` i zwraca `'Meow'`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Animal.sound()` zwraca `'...'`

```dart
    test('animal sound', () {
      expect(Animal().sound(), '...', reason: '--err-t1--');
    });
```

`Dog.sound()` zwraca `'Woof'`

```dart
    test('dog sound', () {
      expect(Dog().sound(), 'Woof', reason: '--err-t2--');
    });
```

`Cat.sound()` zwraca `'Meow'`

```dart
    test('cat sound', () {
      expect(Cat().sound(), 'Meow', reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String sound() => '...';
}

class Dog extends Animal {
  @override
  String sound() => 'Woof';
}

class Cat extends Animal {
  @override
  String sound() => 'Meow';
}
```
