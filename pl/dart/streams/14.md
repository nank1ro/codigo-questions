---
language: dart
exerciseType: 2
---

# --description--

Kontroler może utworzyć strumień broadcast bezpośrednio, za pomocą nazwanego konstruktora `StreamController<T>.broadcast()`. Jego `stream` przyjmuje dowolną liczbę słuchaczy, a każde zdarzenie jest dostarczane do wszystkich, w kolejności, w jakiej się zasubskrybowali:

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>.broadcast();
  controller.stream.listen((msg) => print('first: $msg'));
  controller.stream.listen((msg) => print('second: $msg'));
  controller.add('hi');
  controller.close();
}
// first: hi
// second: hi
```

Jak każdy strumień broadcast, nie buforuje: zdarzenia dodane, zanim słuchacz się zasubskrybuje, są dla niego stracone.

# --instructions--

Uzupełnij kod, aby utworzyć kontroler broadcast, zasubskrybować dwóch słuchaczy `A` i `B`, wysłać wartości `1` i `2`, a potem zamknąć kontroler, tak aby program wypisał `A got 1`, `B got 1`, `A got 2`, `B got 2`.

# --seed--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.[/]();
  controller.stream.[/]((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.[/](1);
  controller.add(2);
  controller.[/]();
}
```

# --answers--

- broadcast
- listen
- add
- close
- asBroadcastStream
- subscribe
- sink

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.broadcast();
  controller.stream.listen((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
```

# --output--

A got 1
B got 1
A got 2
B got 2
