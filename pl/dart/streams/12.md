---
language: dart
exerciseType: 4
---

# --description--

`StreamController` często jest tworzony i konsumowany w tym samym miejscu: subskrybujesz `controller.stream` przez `listen`, potem dodajesz zdarzenia przez `add` i zamykasz kontroler przez `close`. Ponieważ `listen` nie czeka, zdarzenia są dostarczane po zakończeniu bieżącego kodu, ale zawsze w kolejności, w jakiej zostały dodane:

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>();
  controller.stream.listen((n) => print('got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
// got 1
// got 2
```

# --instructions--

Uporządkuj elementy, aby zaimportować `dart:async`, utworzyć `StreamController<String>`, zasubskrybować jego strumień, wypisując każdą wartość, dodać `hello`, a potem `world`, i na koniec zamknąć kontroler. Zasubskrybuj przez `listen`, zanim dodasz jakąkolwiek wartość. Program musi wypisać:

```
hello
world
```

# --answers--

```dart
import 'dart:async';
```

```dart
void main() {
```

```dart
  final controller = StreamController<String>();
```

```dart
  controller.stream.listen((word) => print(word));
```

```dart
  controller.add('hello');
```

```dart
  controller.add('world');
```

```dart
  controller.close();
```

```dart
}
```

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>();
  controller.stream.listen((word) => print(word));
  controller.add('hello');
  controller.add('world');
  controller.close();
}
```

# --output--

hello
world
