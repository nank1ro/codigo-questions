---
language: dart
exerciseType: 4
---

# --description--

Strumień może przenosić nie tylko wartości, ale też **błędy**. Wewnątrz generatora `async*` `throw` wysyła zdarzenie błędu i kończy strumień; `StreamController` może wysłać takie zdarzenie przez `addError`.

Po stronie konsumenta pętla `await for` rzuca błąd ponownie w miejscu pętli, więc obsługujesz go zwykłym `try`/`catch` wokół pętli:

```dart
Stream<int> risky() async* {
  yield 1;
  throw StateError('sensor offline');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('caught: $e');
  }
}
// 1
// caught: Bad state: sensor offline
```

Przy `listen` przekaż zamiast tego callback `onError`: `stream.listen(print, onError: (e) => print('caught: $e'));`

# --instructions--

Uporządkuj elementy, aby zdefiniować funkcję `async*` `risky`, która emituje `1`, a potem rzuca `FormatException('bad')`, oraz `async` `main`, które wypisuje każdą wartość `risky()` wewnątrz `try`/`catch`, wypisuje `error: ` i po nim błąd, gdy go przechwyci, oraz wypisuje `finished` po całym bloku `try`/`catch`, poza nim. Zdefiniuj `risky` przed `main`. Program musi wypisać:

```
1
error: FormatException: bad
finished
```

# --answers--

```dart
Stream<int> risky() async* {
```

```dart
  yield 1;
```

```dart
  throw FormatException('bad');
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  try {
```

```dart
    await for (final n in risky()) {
```

```dart
      print(n);
```

```dart
    }
```

```dart
  } catch (e) {
```

```dart
    print('error: $e');
```

```dart
  }
```

```dart
  print('finished');
```

```dart
}
```

# --solutions--

```dart
Stream<int> risky() async* {
  yield 1;
  throw FormatException('bad');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('error: $e');
  }
  print('finished');
}
```

# --output--

1
error: FormatException: bad
finished
