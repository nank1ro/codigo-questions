---
language: dart
exerciseType: 1
---

# --description--

Generatory produkują zdarzenia wewnątrz jednej funkcji. Gdy zdarzenia pochodzą **skądinąd** (z przycisku, z callbacku sieciowego, z innego obiektu), potrzebujesz **`StreamController`**. Znajduje się on w bibliotece `dart:async`, więc plik musi zaczynać się od `import 'dart:async';`.

Kontroler jest właścicielem strumienia i pozwala wpychać do niego zdarzenia:

```dart
import 'dart:async';

Stream<int> dice() {
  final controller = StreamController<int>();
  controller.add(4);
  controller.add(2);
  controller.close();
  return controller.stream;
}
```

- `add(value)` wysyła jedno zdarzenie
- `close()` kończy strumień; zapomnienie o nim oznacza, że słuchacze czekają w nieskończoność
- `stream` to `Stream`, który konsumują słuchacze

Zdarzenia dodane, zanim ktokolwiek zacznie nasłuchiwać, są przechowywane w buforze, więc powyższy kod jest bezpieczny: słuchacz, który pojawia się później, i tak otrzyma `4` i `2`.

# --instructions--

Napisz funkcję `announce`, która przyjmuje `List<String>` `names` i zwraca `Stream<String>` zbudowany za pomocą `StreamController`: dodaj każde imię wielkimi literami, zamknij kontroler i zwróć jego strumień. Pamiętaj o imporcie `dart:async`.

Przykład:
```dart
print(await announce(['ada', 'linus']).toList()); // [ADA, LINUS]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`announce(['ada', 'linus'])` powinno emitować `'ADA'`, `'LINUS'`

```dart
  test('test1', () async {
    expect(await announce(['ada', 'linus']).toList(), ['ADA', 'LINUS'], reason: '--err-t1--');
  });
```

`announce(['Dart'])` powinno emitować `'DART'`

```dart
  test('test2', () async {
    expect(await announce(['Dart']).toList(), ['DART'], reason: '--err-t2--');
  });
```

`announce([])` nie powinno emitować niczego i powinno się zakończyć, a `announce` powinno zwracać `Stream<String>`

```dart
  test('test3', () async {
    expect(await announce([]).toList(), <String>[], reason: '--err-t3--');
    expect(announce(['x']), isA<Stream<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
import 'dart:async';

Stream<String> announce(List<String> names) {
  final controller = StreamController<String>();
  for (final name in names) {
    controller.add(name.toUpperCase());
  }
  controller.close();
  return controller.stream;
}
```
