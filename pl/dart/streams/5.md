---
language: dart
exerciseType: 2
---

# --description--

`await for` wstrzymuje bieżącą funkcję, dopóki strumień nie zostanie zakończony. Gdy chcesz reagować na zdarzenia **bez czekania**, wywołaj `listen` i przekaż callback: jest wywoływany raz na zdarzenie, a kod po `listen` wykonuje się od razu.

```dart
final ticks = Stream.fromIterable([1, 2]);
ticks.listen((tick) => print('tick $tick'));
print('not waiting');
// not waiting
// tick 1
// tick 2
```

`listen` przyjmuje także nazwany parametr `onDone`, funkcję bez argumentów wywoływaną, gdy strumień się kończy:

```dart
final ticks2 = Stream.fromIterable([1, 2]);
ticks2.listen((tick) => print(tick), onDone: () => print('no more ticks'));
```

# --instructions--

Uzupełnij kod tak, aby strumień był nasłuchiwany, każda litera została wypisana, a `done` zostało wypisane, gdy strumień się skończy. Program musi wypisać `listening`, a potem `a`, `b` i `done`.

# --seed--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.[/]((letter) => print(letter), [/]: () => print('done'));
  print('listening');
}
```

# --answers--

- listen
- onDone
- onError
- forEach
- await

# --solutions--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.listen((letter) => print(letter), onDone: () => print('done'));
  print('listening');
}
```

# --output--

listening
a
b
done
