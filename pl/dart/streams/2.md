---
language: dart
exerciseType: 1
---

# --description--

`Stream.fromIterable` potrzebuje wszystkich wartości z góry. Aby **produkować** wartości po jednej, napisz **generator asynchroniczny**: funkcję, której ciało jest oznaczone `async*`, a typ zwracany to `Stream<T>`. W jej wnętrzu `yield` wysyła jedno zdarzenie do strumienia:

```dart
Stream<int> countTo(int n) async* {
  for (var i = 1; i <= n; i++) {
    yield i;
  }
}
```

Ciało nie wykonuje się, gdy wywołujesz `countTo(3)`: wykonuje się leniwie, w miarę jak słuchacz prosi o wartości, a strumień jest zakończony, gdy ciało się skończy.

Aby zebrać wszystkie zdarzenia do `List`, wywołaj `toList()`. Zwraca on `Future<List<T>>`, więc czekasz na niego przez `await`:

```dart
final values = await countTo(3).toList();
print(values); // [1, 2, 3]
```

# --instructions--

Napisz funkcję `async*` `countdown`, która przyjmuje `int` `from` i zwraca `Stream<int>` emitujący wartości od `from` w dół do `1`. Jeśli `from` wynosi `0` lub mniej, strumień nie emituje niczego.

Przykład:
```dart
print(await countdown(3).toList()); // [3, 2, 1]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countdown(3)` powinno emitować `3`, `2`, `1`

```dart
  test('test1', () async {
    expect(await countdown(3).toList(), [3, 2, 1], reason: '--err-t1--');
  });
```

`countdown(1)` powinno emitować tylko `1`

```dart
  test('test2', () async {
    expect(await countdown(1).toList(), [1], reason: '--err-t2--');
  });
```

`countdown(0)` nie powinno emitować niczego, a `countdown` powinno zwracać `Stream<int>`

```dart
  test('test3', () async {
    expect(await countdown(0).toList(), <int>[], reason: '--err-t3--');
    expect(countdown(2), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> countdown(int from) async* {
  for (var i = from; i >= 1; i--) {
    yield i;
  }
}
```
