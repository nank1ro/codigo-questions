---
language: dart
exerciseType: 1
---

# --instructions--

Napisz funkcję `seasonName(int month)`, która zwraca nazwę pory roku dla podanego numeru miesiąca, używając instrukcji `switch`. Miesiące `12`, `1`, `2` → `'Winter'`; `3`, `4`, `5` → `'Spring'`; `6`, `7`, `8` → `'Summer'`; `9`, `10`, `11` → `'Autumn'`. Zwróć `'Unknown'` dla każdej innej wartości.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`seasonName(1)` musi zwracać `'Winter'`

```dart
  test('test1', () {
    expect(seasonName(1), 'Winter', reason: '--err-t1--');
  });
```

`seasonName(4)` musi zwracać `'Spring'`

```dart
  test('test2', () {
    expect(seasonName(4), 'Spring', reason: '--err-t2--');
  });
```

`seasonName(7)` musi zwracać `'Summer'`

```dart
  test('test3', () {
    expect(seasonName(7), 'Summer', reason: '--err-t3--');
  });
```

`seasonName(10)` musi zwracać `'Autumn'`

```dart
  test('test4', () {
    expect(seasonName(10), 'Autumn', reason: '--err-t4--');
  });
```

`seasonName(13)` musi zwracać `'Unknown'`

```dart
  test('test5', () {
    expect(seasonName(13), 'Unknown', reason: '--err-t5--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String seasonName(int month) {
  switch (month) {
    case 12:
    case 1:
    case 2:
      return 'Winter';
    case 3:
    case 4:
    case 5:
      return 'Spring';
    case 6:
    case 7:
    case 8:
      return 'Summer';
    case 9:
    case 10:
    case 11:
      return 'Autumn';
    default:
      return 'Unknown';
  }
}

void main() {
  print(seasonName(7));
}
```
