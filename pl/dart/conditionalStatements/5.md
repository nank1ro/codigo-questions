---
language: dart
exerciseType: 2
---

# --description--

**Operator trójargumentowy** `condition ? expr1 : expr2` to zwięzły sposób zapisu prostego wyrażenia `if-else`.

```dart
int x = 10;
String label = x > 0 ? 'positive' : 'non-positive';
print(label); // positive
```

Jeśli warunek jest `true`, używane jest `expr1`; w przeciwnym razie używane jest `expr2`.

# --instructions--

Uzupełnij wyrażenie trójargumentowe, aby przypisać `'Even'` do `parity`, gdy `n` jest podzielne przez `2`, a `'Odd'` w przeciwnym razie.

# --seed--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 [/] 'Even' [/] 'Odd';
  print(parity);
}
```

# --answers--

- ?
- :
- ==
- ;

# --solutions--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 ? 'Even' : 'Odd';
  print(parity);
}
```

# --output--

Even
