---
language: dart
exerciseType: 1
---

# --instructions--

Napisz funkcję `canVote(int age, bool isCitizen)`, która zwraca `true` tylko wtedy, gdy `age >= 18` **i** `isCitizen` jest `true`, używając zagnieżdżonych instrukcji `if`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canVote(20, true)` musi zwracać `true`

```dart
  test('test1', () {
    expect(canVote(20, true), true, reason: '--err-t1--');
  });
```

`canVote(16, true)` musi zwracać `false`

```dart
  test('test2', () {
    expect(canVote(16, true), false, reason: '--err-t2--');
  });
```

`canVote(25, false)` musi zwracać `false`

```dart
  test('test3', () {
    expect(canVote(25, false), false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool canVote(int age, bool isCitizen) {
  if (age >= 18) {
    if (isCitizen) {
      return true;
    }
  }
  return false;
}

void main() {
  print(canVote(20, true));
}
```
