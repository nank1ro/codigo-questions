---
language: dart
exerciseType: 1
---

# --instructions--

Napisz funkcję `letterGrade(int score)`, która zwraca ocenę literową używając łańcucha `else if`: `'A'` dla `score >= 90`, `'B'` dla `score >= 80`, `'C'` dla `score >= 70`, `'D'` dla `score >= 60` i `'F'` dla wszystkiego poniżej `60`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`letterGrade(92)` musi zwracać `'A'`

```dart
  test('test1', () {
    expect(letterGrade(92), 'A', reason: '--err-t1--');
  });
```

`letterGrade(85)` musi zwracać `'B'`

```dart
  test('test2', () {
    expect(letterGrade(85), 'B', reason: '--err-t2--');
  });
```

`letterGrade(73)` musi zwracać `'C'`

```dart
  test('test3', () {
    expect(letterGrade(73), 'C', reason: '--err-t3--');
  });
```

`letterGrade(61)` musi zwracać `'D'`

```dart
  test('test4', () {
    expect(letterGrade(61), 'D', reason: '--err-t4--');
  });
```

`letterGrade(45)` musi zwracać `'F'`

```dart
  test('test5', () {
    expect(letterGrade(45), 'F', reason: '--err-t5--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String letterGrade(int score) {
  if (score >= 90) {
    return 'A';
  } else if (score >= 80) {
    return 'B';
  } else if (score >= 70) {
    return 'C';
  } else if (score >= 60) {
    return 'D';
  } else {
    return 'F';
  }
}

void main() {
  print(letterGrade(85));
}
```
