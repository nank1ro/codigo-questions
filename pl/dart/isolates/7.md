---
language: dart
exerciseType: 4
---

# --description--

Uruchomiony izolat zawsze przechodzi te same cztery kroki: otwórz skrzynkę, uruchom pracownika z jego adresem, poczekaj na odpowiedź, użyj jej.

```dart
import 'dart:isolate';

void worker(SendPort port) {
  port.send('done');
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

`await` przed `Isolate.spawn` czeka, aż izolat się *rozpocznie*, a nie aż skończy swoją pracę: wynik przychodzi później, przez port.

# --instructions--

Uporządkuj elementy, aby zbudować program, który uruchamia izolat `worker` odsyłający `7 * 6` do izolatu głównego, który go wypisuje. Umieść import na początku i zdefiniuj `worker` przed `main`. Program musi wypisać `42`.

# --answers--

```dart
import 'dart:isolate';
```

```dart
void worker(SendPort port) {
```

```dart
  port.send(7 * 6);
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  final receivePort = ReceivePort();
```

```dart
  await Isolate.spawn(worker, receivePort.sendPort);
```

```dart
  print(await receivePort.first);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
void worker(SendPort port) {
  port.send(7 * 6);
}
Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

# --output--

42
