---
language: dart
exerciseType: 1
---

# --description--

Każde `Isolate.run` uruchamia własny izolat, więc kilka z nich naprawdę działa w tym samym momencie, na tylu rdzeniach, ile ma maszyna. Schemat jest ten, który już znasz z future: najpierw wystartuj każde obliczenie, a potem poczekaj na wszystkie za pomocą `Future.wait`, które zachowuje wyniki w kolejności wejścia.

```dart
Future<List<int>> doubleAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * 2)));
}
```

Uruchomienie izolatu nie jest darmowe: kosztuje pamięć i kilka milisekund. Podzielenie jednego długiego obliczenia między garstkę izolatów się opłaca, wysyłanie tysiąca trywialnych dodań do tysiąca izolatów już nie.

# --instructions--

Napisz funkcję `squareAll`, która przyjmuje `List<int>` `numbers` i zwraca `Future<List<int>>` kończące się kwadratem każdej liczby, w tej samej kolejności, gdzie każdy kwadrat jest obliczany na własnym izolacie.

Przykład:
```dart
print(await squareAll([1, 2, 3])); // [1, 4, 9]
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`squareAll([1, 2, 3])` powinno zakończyć się wartością `[1, 4, 9]`

```dart
  test('test1', () async {
    expect(await squareAll([1, 2, 3]), [1, 4, 9], reason: '--err-t1--');
  });
```

`squareAll([-4, 5])` powinno zakończyć się wartością `[16, 25]`, zachowując kolejność wejścia

```dart
  test('test2', () async {
    expect(await squareAll([-4, 5]), [16, 25], reason: '--err-t2--');
  });
```

`squareAll([])` powinno zakończyć się pustą listą, a `squareAll` powinno zwracać `Future<List<int>>`

```dart
  test('test3', () async {
    expect(await squareAll(<int>[]), <int>[], reason: '--err-t3--');
    expect(squareAll([2]), isA<Future<List<int>>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<List<int>> squareAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * n)));
}
```
