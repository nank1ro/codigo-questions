---
language: dart
exerciseType: 1
---

# --description--

Funkcja przekazana do `Isolate.run` może **przechwytywać** zmienne ze swojego otoczenia. Te wartości są kopiowane do nowego izolatu razem z funkcją, więc obliczenie może zależeć od wywołującego:

```dart
Future<int> triple(int n) {
  return Isolate.run(() => n * 3);
}
```

`Isolate.run` zwraca `Future` z tym, co zwraca funkcja, więc `triple` może po prostu go zwrócić: nie potrzeba `async` ani `await`, kiedy tylko przekazujesz future dalej.

Cała idea przenoszenia pracy do innego izolatu polega na tym, że długie obliczenia przestają zamrażać izolat główny. Pętla działająca przez sekundę blokuje wszystko, gdy działa na izolacie głównym; wewnątrz `Isolate.run` działa gdzie indziej, a izolat główny wciąż obsługuje własne zdarzenia.

# --instructions--

Napisz funkcję `sumTo`, która przyjmuje `int` `n` i zwraca `Future<int>` kończące się sumą liczb od `1` do `n`, obliczoną na innym izolacie za pomocą `Isolate.run`.

Przykład:
```dart
print(await sumTo(4)); // 10
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumTo(4)` powinno zakończyć się wartością `10`

```dart
  test('test1', () async {
    expect(await sumTo(4), 10, reason: '--err-t1--');
  });
```

`sumTo(100)` powinno zakończyć się wartością `5050`, a `sumTo(1)` wartością `1`

```dart
  test('test2', () async {
    expect(await sumTo(100), 5050, reason: '--err-t2--');
    expect(await sumTo(1), 1, reason: '--err-t2--');
  });
```

`sumTo(0)` powinno zakończyć się wartością `0`, a `sumTo` powinno zwracać `Future<int>`

```dart
  test('test3', () async {
    expect(await sumTo(0), 0, reason: '--err-t3--');
    expect(sumTo(3), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<int> sumTo(int n) {
  return Isolate.run(() {
    var total = 0;
    for (var i = 1; i <= n; i++) {
      total += i;
    }
    return total;
  });
}
```
