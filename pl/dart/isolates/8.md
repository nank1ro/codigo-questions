---
language: dart
exerciseType: 1
---

# --description--

`Isolate.spawn` przekazuje dokładnie **jedną** wiadomość do punktu wejścia, a pracownik zwykle potrzebuje zarówno `SendPort` do odpowiedzi, jak i danych do pracy. Zwykłym trikiem jest spakowanie wszystkiego w jedną `List` i rozpakowanie jej po drugiej stronie:

```dart
void multiply(List<Object> message) {
  final port = message[0] as SendPort;
  final value = message[1] as int;
  port.send(value * 2);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(multiply, <Object>[receivePort.sendPort, 21]);
  print(await receivePort.first); // 42
}
```

Lista jest kopiowana w drodze do środka, więc pracownik czyta własne wartości. `SendPort` to jedna z niewielu rzeczy, które nie są kopiowane, lecz współdzielone: wciąż wskazuje na skrzynkę izolatu, który go utworzył, i właśnie dlatego może służyć za adres zwrotny.

# --instructions--

Napisz funkcję najwyższego poziomu `sendSum`, która przyjmuje `List<Object>` `message`, gdzie element `0` to `SendPort`, a elementy `1` i `2` to `int`y, i wysyła ich sumę do portu.

Potem napisz funkcję `addInIsolate`, która przyjmuje dwa `int`y `a` i `b` i zwraca `Future<int>` kończące się sumą obliczoną przez uruchomiony izolat `sendSum`.

Przykład:
```dart
print(await addInIsolate(2, 3)); // 5
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendSum` powinno wysłać sumę dwóch liczb wiadomości do portu

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendSum(<Object>[port.sendPort, 4, 6]);
    expect(await port.first, 10, reason: '--err-t1--');
  });
```

`addInIsolate(2, 3)` powinno zakończyć się wartością `5`

```dart
  test('test2', () async {
    expect(await addInIsolate(2, 3), 5, reason: '--err-t2--');
  });
```

`addInIsolate(10, -4)` powinno zakończyć się wartością `6`, a `addInIsolate` powinno zwracać `Future<int>`

```dart
  test('test3', () async {
    expect(await addInIsolate(10, -4), 6, reason: '--err-t3--');
    expect(addInIsolate(1, 1), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendSum(List<Object> message) {
  final port = message[0] as SendPort;
  final a = message[1] as int;
  final b = message[2] as int;
  port.send(a + b);
}

Future<int> addInIsolate(int a, int b) async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendSum, <Object>[receivePort.sendPort, a, b]);
  return await receivePort.first as int;
}
```
