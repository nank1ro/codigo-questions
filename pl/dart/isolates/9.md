---
language: dart
exerciseType: 2
---

# --description--

`first` odczytuje jedną wiadomość i zamyka skrzynkę. `ReceivePort` jest też **`Stream`em**, więc żeby odczytać wiele wiadomości, iterujesz po nim pętlą `await for`.

Pętla nigdy nie kończy się sama: port pozostaje otwarty i czeka na wiadomość, która może nigdy nie przyjść. Pracownik wysyła więc ostatnią wartość jako sygnał, często `null`, a słuchacz reaguje wywołaniem **`close()`**, które kończy strumień i pętlę:

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(countdown, receivePort.sendPort);
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --instructions--

Uzupełnij kod tak, aby uruchomiony izolat wysłał `3`, `2`, `1`, a potem `null`, a izolat główny wypisał każdą liczbę, zamknął skrzynkę przy `null` i na koniec wypisał `done`.

# --seed--

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.[/](countdown, receivePort.sendPort);
  await [/] (final message in receivePort) {
    if (message == null) {
      receivePort.[/]();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --answers--

- spawn
- for
- close
- run
- while
- listen
- first

# --solutions--

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(countdown, receivePort.sendPort);
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --output--

3
2
1
done
