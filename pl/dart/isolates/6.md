---
language: dart
exerciseType: 1
---

# --description--

Funkcja przekazywana do `Isolate.spawn` nazywa się **punktem wejścia**. Musi to być funkcja najwyższego poziomu (albo statyczna), która przyjmuje dokładnie jeden parametr: wiadomość, którą przekazuje jej `Isolate.spawn`.

```dart
void reportAnswer(SendPort port) {
  port.send(42);
}
```

Wiadomości przychodzące do `ReceivePort` mają statyczny typ `dynamic`, bo wysłana mogła być dowolna wartość. Kiedy wiesz, co wysyła drugi izolat, zrzuć typ:

```dart
final receivePort = ReceivePort();
await Isolate.spawn(reportAnswer, receivePort.sendPort);
final answer = await receivePort.first as int;
```

# --instructions--

Napisz funkcję najwyższego poziomu `sendGreeting`, która przyjmuje `SendPort` `port` i wysyła do niego napis `hello from the isolate`.

Potem napisz funkcję `greetFromIsolate`, która zwraca `Future<String>`: tworzy `ReceivePort`, uruchamia `sendGreeting` z jego `sendPort` i kończy się pierwszą odebraną wiadomością.

Przykład:
```dart
print(await greetFromIsolate()); // hello from the isolate
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendGreeting` powinno wysłać `'hello from the isolate'` do portu, który otrzymuje

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendGreeting(port.sendPort);
    expect(await port.first, 'hello from the isolate', reason: '--err-t1--');
  });
```

`greetFromIsolate()` powinno zakończyć się wartością `'hello from the isolate'`

```dart
  test('test2', () async {
    expect(await greetFromIsolate(), 'hello from the isolate', reason: '--err-t2--');
  });
```

`greetFromIsolate` powinno zwracać `Future<String>` i powinno działać przy dwukrotnym wywołaniu

```dart
  test('test3', () async {
    expect(greetFromIsolate(), isA<Future<String>>(), reason: '--err-t3--');
    expect(await greetFromIsolate(), 'hello from the isolate', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendGreeting(SendPort port) {
  port.send('hello from the isolate');
}

Future<String> greetFromIsolate() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendGreeting, receivePort.sendPort);
  return await receivePort.first as String;
}
```
