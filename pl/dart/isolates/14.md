---
language: dart
exerciseType: 4
---

# --description--

Złożone w całość, program z `Isolate.run` czyta się jak zwykły kod sekwencyjny: linia przed wywołaniem działa na izolacie głównym, obliczenie działa gdzie indziej, a linia po `await` znów działa na izolacie głównym, z wynikiem w ręku.

```dart
import 'dart:isolate';

int twice(int n) => n * 2;

Future<void> main() async {
  print('start');
  final result = await Isolate.run(() => twice(4));
  print(result);
}
// start
// 8
```

# --instructions--

Uporządkuj elementy, aby zbudować program, który wypisuje `start`, podnosi `9` do kwadratu na innym izolacie za pomocą `Isolate.run` i wypisuje wynik. Umieść import na początku i zdefiniuj `square` przed `main`. Program musi wypisać:

```
start
81
```

# --answers--

```dart
import 'dart:isolate';
```

```dart
int square(int n) => n * n;
```

```dart
Future<void> main() async {
```

```dart
  print('start');
```

```dart
  final result = await Isolate.run(() => square(9));
```

```dart
  print(result);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
int square(int n) => n * n;
Future<void> main() async {
  print('start');
  final result = await Isolate.run(() => square(9));
  print(result);
}
```

# --output--

start
81
