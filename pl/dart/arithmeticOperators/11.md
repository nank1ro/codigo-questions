---
language: dart
exerciseType: 1
---

# --instructions--

Zadeklaruj dwie zmienne najwyższego poziomu `int`: `x` z wartością `25` i `y` z wartością `9`. Następnie zadeklaruj zmienną najwyższego poziomu `int` o nazwie `diff`, która przechowuje wynik odejmowania `y` od `x`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`diff` powinno równać się `16`

```dart
  test('test1', () {
    expect(diff, 16, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 25;
int y = 9;
int diff = x - y;

void main() {
  print(diff);
}
```
