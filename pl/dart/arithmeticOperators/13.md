---
language: dart
exerciseType: 1
---

# --instructions--

Zadeklaruj zmienną najwyższego poziomu `double` o nazwie `result`, która przechowuje wynik dzielenia `7` przez `2`. Pamiętaj, że `/` w Dart zawsze zwraca `double`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` powinno być `double` z wartością `3.5`

```dart
  test('test1', () {
    expect(result, isA<double>(), reason: '--err-t1--');
    expect(result, closeTo(3.5, 0.001), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double result = 7 / 2;

void main() {
  print(result);
}
```
