---
language: dart
exerciseType: 1
---

# --instructions--

Użyj operatora modulo `%` aby znaleźć resztę z dzielenia `17` przez `5`. Przechowaj wynik w zmiennej najwyższego poziomu o nazwie `remainder`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Zmienna `remainder` powinna równać się `2`

```dart
  test('test1', () {
    expect(remainder, 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 17 % 5;

void main() {
  print(remainder);
}
```
