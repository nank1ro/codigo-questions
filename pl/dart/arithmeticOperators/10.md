---
language: dart
exerciseType: 1
---

# --instructions--

Zadeklaruj dwie zmienne najwyższego poziomu `int`: `a` z wartością `12` i `b` z wartością `8`. Następnie zadeklaruj trzecią zmienną najwyższego poziomu `int` o nazwie `total`, która przechowuje ich sumę.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`a` powinno być `12`, `b` powinno być `8`, a `total` powinno być `20`

```dart
  test('test1', () {
    expect(a, 12, reason: '--err-t1--');
    expect(b, 8, reason: '--err-t2--');
    expect(total, 20, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 12;
int b = 8;
int total = a + b;

void main() {
  print(total);
}
```
