---
language: dart
exerciseType: 1
---

# --instructions--

Zadeklaruj dwie zmienne najwyższego poziomu `int`: `total` z wartością `29` i `perGroup` z wartością `4`. Następnie zadeklaruj zmienną najwyższego poziomu `int` o nazwie `groups`, która przechowuje ile kompletnych grup `4` mieści się w `29` (użyj dzielenia całkowitego `~/`).

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`groups` powinno równać się `7`

```dart
  test('test1', () {
    expect(groups, 7, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 29;
int perGroup = 4;
int groups = total ~/ perGroup;

void main() {
  print(groups);
}
```
