---
language: dart
exerciseType: 1
---

# --instructions--

Sklep sprzedaje `itemCount` przedmiotów po `pricePerItem` dolarów każdy, z `discount` kwotą w dolarach odejmowaną od całości. Zadeklaruj te trzy zmienne najwyższego poziomu `int`: `itemCount` = `6`, `pricePerItem` = `15`, `discount` = `10`. Następnie zadeklaruj zmienną najwyższego poziomu `int` o nazwie `totalPrice`, która oblicza całość po rabacie.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`totalPrice` powinno równać się `80`

```dart
  test('test1', () {
    expect(totalPrice, 80, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int itemCount = 6;
int pricePerItem = 15;
int discount = 10;
int totalPrice = itemCount * pricePerItem - discount;

void main() {
  print(totalPrice);
}
```
