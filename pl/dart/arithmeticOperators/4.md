---
language: dart
exerciseType: 2
---

# --description--

Operator **dzielenia** `/` dzieli jedną wartość przez drugą. W Dart `/` **zawsze zwraca `double`**, nawet gdy wynik jest liczbą całkowitą.

```dart
int a = 10;
int b = 4;
double result = a / b; // 2.5

double exact = 10 / 2; // 5.0  (nie 5!)
```

Zawsze używaj zmiennej `double` do przechowywania wyniku `/`.

# --instructions--

Uzupełnij kod, aby podzielić `9` przez `4` i przechowaj wynik w `result`.

# --seed--

```dart
void main() {
  double result = 9 [/] 4;
  print(result);
}
```

# --answers--

- /
- *
- +
- -

# --solutions--

```dart
void main() {
  double result = 9 / 4;
  print(result);
}
```

# --output--

2.25
