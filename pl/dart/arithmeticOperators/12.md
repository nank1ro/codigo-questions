---
language: dart
exerciseType: 1
---

# --instructions--

Zadeklaruj dwie zmienne najwyższego poziomu `int`: `width` z wartością `7` i `height` z wartością `9`. Następnie zadeklaruj zmienną najwyższego poziomu `int` o nazwie `area`, która przechowuje ich iloczyn.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`area` powinno równać się `63`

```dart
  test('test1', () {
    expect(area, 63, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int width = 7;
int height = 9;
int area = width * height;

void main() {
  print(area);
}
```
