---
language: dart
exerciseType: 1
---

# --description--

Metoda `.putIfAbsent(key, ifAbsent)` dodaje parę **tylko wtedy, gdy** klucza jeszcze nie ma w mapie. Drugi argument to funkcja, która tworzy wartość. Jeśli klucz już istnieje, mapa pozostaje niezmieniona. W obu przypadkach zwracana jest wartość aktualnie przechowywana pod tym kluczem:

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99); // Ann is already there, nothing changes
ages.putIfAbsent('Bob', () => 25); // Bob is added
print(ages); // {Ann: 30, Bob: 25}
```

# --instructions--

Mając mapę `ages`, użyj `.putIfAbsent()` dwukrotnie: najpierw dla klucza `'Ann'` z wartością `99`, a następnie dla klucza `'Bob'` z wartością `25`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var ages = {'Ann': 30};
```

# --after-seed--

```dart
  return ages;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`ages` powinien być `{'Ann': 30, 'Bob': 25}`

```dart
  test('test1', () {
    expect(result(), {'Ann': 30, 'Bob': 25}, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99);
ages.putIfAbsent('Bob', () => 25);
```
