---
language: dart
exerciseType: 2
---

# --description--

Metoda `.remove(key)` usuwa klucz i jego wartość z mapy. Zwraca usuniętą wartość albo `null`, jeśli klucza nie było:

```dart
var ages = {'Ann': 30, 'Bob': 25};
int? removed = ages.remove('Ann');
print(removed); // 30
print(ages); // {Bob: 25}
```

Właściwość `.length` zwraca liczbę par klucz-wartość:

```dart
print(ages.length); // 1
```

# --instructions--

Uzupełnij kod, aby usunąć `Bob` z mapy, a następnie wypisać, ile par zostało.

# --seed--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.[/]('Bob');
print(ages.[/]);
```

# --answers--

- remove
- length
- delete
- removeAt
- size
- count

# --solutions--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.remove('Bob');
print(ages.length);
```

# --output--

2
