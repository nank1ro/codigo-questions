---
language: dart
exerciseType: 1
---

# --instructions--

Mając listę `colors`, zbuduj `Map<String, int>` o nazwie `counts`, w której każdy klucz to kolor, a jej wartość to liczba wystąpień tego koloru na liście.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var colors = ['red', 'blue', 'red', 'green', 'red', 'blue'];
```

# --after-seed--

```dart
  return counts;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`counts` powinien być `{'red': 3, 'blue': 2, 'green': 1}`

```dart
  test('test1', () {
    expect(result(), {'red': 3, 'blue': 2, 'green': 1}, reason: '--err-t1--');
  });
```

`counts` powinien mieć 3 klucze

```dart
  test('test2', () {
    expect(result().length, 3, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var colors = ['red', 'blue', 'red', 'green', 'red', 'blue'];
var counts = <String, int>{};
for (var color in colors) {
  counts.update(color, (n) => n + 1, ifAbsent: () => 1);
}
```
