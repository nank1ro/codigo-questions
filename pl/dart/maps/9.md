---
language: dart
exerciseType: 1
---

# --description--

Pusty literał mapy `{}` nie ma par, z których można wywnioskować typy, więc nadaj mu jawne typy za pomocą `<K, V>{}` lub adnotacji typu:

```dart
var cart = <String, int>{};
Map<String, int> other = {};
```

Właściwość `.isEmpty` jest `true`, gdy mapa nie ma par, a `.isNotEmpty` jest `true`, gdy ma co najmniej jedną:

```dart
print(cart.isEmpty); // true
cart['pen'] = 2;
print(cart.isNotEmpty); // true
```

# --instructions--

Utwórz pustą `Map<String, int>` o nazwie `cart`. Zapisz, czy jest `.isEmpty` w `bool` o nazwie `wasEmpty`, następnie dodaj klucz `'pen'` z wartością `2` i zapisz, czy jest `.isNotEmpty` w `bool` o nazwie `hasItems`.

# --before-seed--

```dart
dynamic result() {
```

# --after-seed--

```dart
  return [wasEmpty, hasItems, cart];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`wasEmpty` powinien być `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasItems` powinien być `true`

```dart
  test('test2', () {
    expect(result()[1], true, reason: '--err-t2--');
  });
```

`cart` powinien być `{'pen': 2}`

```dart
  test('test3', () {
    expect(result()[2], {'pen': 2}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var cart = <String, int>{};
bool wasEmpty = cart.isEmpty;
cart['pen'] = 2;
bool hasItems = cart.isNotEmpty;
```
