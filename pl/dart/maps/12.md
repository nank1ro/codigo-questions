---
language: dart
exerciseType: 1
---

# --description--

Mapa nie jest `Iterable`, więc nie można jej przechodzić bezpośrednio za pomocą `for-in`. Zamiast tego przechodź po `.entries`: każdy element to `MapEntry` z `.key` i `.value`:

```dart
var ages = {'Ann': 30, 'Bob': 25};
for (var entry in ages.entries) {
  print('${entry.key}: ${entry.value}');
}
// Ann: 30
// Bob: 25
```

# --instructions--

Mając mapę `ages`, przejdź po jej `.entries` za pomocą `for-in` i dodaj do `List<String>` o nazwie `lines` po jednym stringu na parę, w formacie `'Ann is 30'`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
```

# --after-seed--

```dart
  return lines;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`lines` powinien być `['Ann is 30', 'Bob is 25', 'Cid is 41']`

```dart
  test('test1', () {
    expect(result(), ['Ann is 30', 'Bob is 25', 'Cid is 41'], reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
List<String> lines = [];
for (var entry in ages.entries) {
  lines.add('${entry.key} is ${entry.value}');
}
```
