---
language: dart
exerciseType: 2
---

# --description--

Funkcje mogą przyjmować **parametry** — wartości przekazywane podczas wywoływania funkcji. Parametry deklaruje się wewnątrz nawiasów, podając ich typ i nazwę:

```dart
int square(int n) {
  return n * n;
}

void main() {
  print(square(4)); // prints 16
}
```

Każdy parametr ma typ (`int`) i nazwę (`n`). Wiele parametrów oddziela się przecinkami.

# --instructions--

Uzupełnij funkcję `multiply`, która przyjmuje dwa parametry `int` o nazwach `a` i `b` i zwraca ich iloczyn.

# --seed--

```dart
int multiply([/] a, [/] b) {
  return a [/] b;
}

void main() {
  print(multiply(3, 4));
}
```

# --answers--

- int
- int
- *
- +
- String
- double

# --solutions--

```dart
int multiply(int a, int b) {
  return a * b;
}

void main() {
  print(multiply(3, 4));
}
```

# --output--

12
