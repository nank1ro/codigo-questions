---
language: dart
exerciseType: 1
---

# --description--

Niektóre metody wyższego rzędu odpowiadają na pytanie o kolekcję zamiast budować nową. Przyjmują predykat i zwracają `bool`:

- `any` jest `true`, gdy **co najmniej jeden** element spełnia predykat
- `every` jest `true`, gdy **wszystkie** elementy go spełniają

```dart
final numbers = [1, 2, 3];
print(numbers.any((n) => n > 2));   // true
print(numbers.every((n) => n > 2)); // false
```

Obie zatrzymują się, gdy tylko odpowiedź jest pewna: `any` na pierwszym elemencie, który pasuje, `every` na pierwszym, który nie pasuje.

Na pustej kolekcji `any` jest `false`, a `every` jest `true`: nie ma elementu, który potwierdziłby pierwszą, ani takiego, który złamałby drugą.

# --instructions--

Napisz dwie funkcje, obie przyjmujące `List<int>` `numbers`:

- `hasNegative` zwraca `true`, jeśli co najmniej jeden element jest mniejszy od `0`
- `allEven` zwraca `true`, jeśli każdy element jest parzysty

Przykład:
```dart
print(hasNegative([1, -2, 3])); // true
print(allEven([2, 4]));         // true
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasNegative([1, -2, 3])` powinno być `true`, a `hasNegative([1, 2, 3])` powinno być `false`

```dart
  test('test1', () {
    expect(hasNegative([1, -2, 3]), true, reason: '--err-t1--');
    expect(hasNegative([1, 2, 3]), false, reason: '--err-t1--');
  });
```

`allEven([2, 4, 6])` powinno być `true`, a `allEven([2, 5])` powinno być `false`

```dart
  test('test2', () {
    expect(allEven([2, 4, 6]), true, reason: '--err-t2--');
    expect(allEven([2, 5]), false, reason: '--err-t2--');
  });
```

Na pustej liście `hasNegative` powinno być `false`, a `allEven` powinno być `true`

```dart
  test('test3', () {
    expect(hasNegative(<int>[]), false, reason: '--err-t3--');
    expect(allEven(<int>[]), true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool hasNegative(List<int> numbers) {
  return numbers.any((n) => n < 0);
}

bool allEven(List<int> numbers) {
  return numbers.every((n) => n % 2 == 0);
}
```
