---
language: dart
exerciseType: 1
---

# --description--

Każda z tych metod zwraca `Iterable`, a każdy `Iterable` znów ma te same metody. To pozwala je **łączyć w łańcuchy**: całe obliczenie czyta się jak potok od lewej do prawej, gdzie każdy krok pracuje na tym, co wyprodukował poprzedni.

```dart
final words = ['kiwi', 'fig', 'banana', 'date'];
print(words.where((w) => w.length == 4).map((w) => w.toUpperCase()).toList());
// [KIWI, DATE]
```

Tylko ostatni krok potrzebuje `toList()`: wywołanie go w środku zbudowałoby listę, której nikt nie zachowa.

Typ zmienia się wzdłuż łańcucha, a wraz z nim to, co otrzymuje następna funkcja: po `where` na `List<String>` nadal masz ciągi znaków, ale po `map((w) => w.length)` następny krok widzi liczby.

Ponieważ każdy krok jest leniwy, kolejność ma znaczenie dla wykonanej pracy, a nie tylko dla wyniku: filtrowanie najpierw za pomocą `where` oznacza, że `map` jest wywoływane na mniejszej liczbie elementów.

# --instructions--

Napisz funkcję `shortUpper`, która przyjmuje `List<String>` `words` i zwraca nową `List<String>` ze słowami o co najwyżej `4` znakach, wielkimi literami, w ich oryginalnej kolejności.

Przykład:
```dart
print(shortUpper(['kiwi', 'banana', 'fig'])); // [KIWI, FIG]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shortUpper(['kiwi', 'banana', 'fig'])` powinno zwrócić `['KIWI', 'FIG']`

```dart
  test('test1', () {
    expect(shortUpper(['kiwi', 'banana', 'fig']), ['KIWI', 'FIG'],
        reason: '--err-t1--');
  });
```

Słowa o dokładnie `4` znakach powinny zostać zachowane, a dłuższe odrzucone

```dart
  test('test2', () {
    expect(shortUpper(['date', 'grape']), ['DATE'], reason: '--err-t2--');
    expect(shortUpper(['melon']), <String>[], reason: '--err-t2--');
  });
```

`shortUpper` powinno zwrócić `List<String>` i nie może zmieniać otrzymanej listy

```dart
  test('test3', () {
    expect(shortUpper(['a']), isA<List<String>>(), reason: '--err-t3--');
    final original = ['fig', 'banana'];
    shortUpper(original);
    expect(original, ['fig', 'banana'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<String> shortUpper(List<String> words) {
  return words.where((w) => w.length <= 4).map((w) => w.toUpperCase()).toList();
}
```
