---
language: dart
exerciseType: 1
---

# --description--

`where` przyjmuje funkcję zwracającą `bool`, nazywaną **predykatem**, i zachowuje tylko elementy, dla których odpowiada ona `true`. Kolejność przetrwałych elementów nigdy się nie zmienia:

```dart
final numbers = [4, -2, 7, 0];
print(numbers.where((n) => n > 0).toList()); // [4, 7]
```

Podobnie jak `map`, `where` zwraca `Iterable` i nigdy nie modyfikuje oryginalnej kolekcji, więc `toList()` ponownie zamienia wynik w `List`.

W innych językach ta metoda nazywa się `filter`; w Dart to `where`.

# --instructions--

Napisz funkcję `positives`, która przyjmuje `List<int>` `numbers` i zwraca nową `List<int>` zawierającą tylko elementy większe od `0`, w ich oryginalnej kolejności.

Przykład:
```dart
print(positives([4, -2, 7, 0])); // [4, 7]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`positives([4, -2, 7, 0])` powinno zwrócić `[4, 7]`

```dart
  test('test1', () {
    expect(positives([4, -2, 7, 0]), [4, 7], reason: '--err-t1--');
  });
```

`positives` powinno zachować oryginalną kolejność i zwrócić `List<int>`

```dart
  test('test2', () {
    expect(positives([9, 1, -5, 3]), [9, 1, 3], reason: '--err-t2--');
    expect(positives([1]), isA<List<int>>(), reason: '--err-t2--');
  });
```

`positives` powinno zwrócić pustą listę, gdy nic nie jest dodatnie, i nie może zmieniać otrzymanej listy

```dart
  test('test3', () {
    expect(positives([-1, -2]), <int>[], reason: '--err-t3--');
    final original = [1, -1, 2];
    positives(original);
    expect(original, [1, -1, 2], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> positives(List<int> numbers) {
  return numbers.where((n) => n > 0).toList();
}
```
