---
language: dart
exerciseType: 1
---

# --description--

Zebrane razem, te metody zastępują większość pętli pisanych ręcznie. Potok zwykle czyta się w trzech etapach: **wybierz** elementy za pomocą `where`, **przekształć** je za pomocą `map`, a następnie **połącz** je za pomocą `fold`:

```dart
final prices = [12, 40, 7];
final cheapTotal = prices.where((p) => p < 20).fold(0, (acc, p) => acc + p);
print(cheapTotal); // 19
```

Ponieważ `fold` wybiera własną wartość początkową, może też kończyć łańcuch typem, który nie ma nic wspólnego z elementami, na przykład `String` rosnącym kawałek po kawałku:

```dart
final words = ['fig', 'kiwi'];
final firstLetters = words.fold('', (acc, w) => acc + w[0]);
print(firstLetters); // fk
```

Każdy etap pozostaje krótki i mówi, co robi, i to jest prawdziwy powód, aby przedkładać je nad pętlę robiącą wszystkie trzy naraz.

# --instructions--

Napisz funkcję `initials`, która przyjmuje `List<String>` `names`, ignoruje puste i zwraca `String` złożony z pierwszej litery każdego pozostałego imienia, wielką literą i w kolejności.

Przykład:
```dart
print(initials(['ada', '', 'linus'])); // AL
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`initials(['ada', '', 'linus'])` powinno zwrócić `'AL'`

```dart
  test('test1', () {
    expect(initials(['ada', '', 'linus']), 'AL', reason: '--err-t1--');
  });
```

Litery powinny być wielkie i w kolejności, a imiona już zapisane wielkimi literami powinny pozostać bez zmian

```dart
  test('test2', () {
    expect(initials(['grace', 'Bob', 'ada']), 'GBA', reason: '--err-t2--');
    expect(initials(['x']), 'X', reason: '--err-t2--');
  });
```

`initials` powinno zwrócić pusty `String`, gdy nie ma niepustego imienia

```dart
  test('test3', () {
    expect(initials(<String>[]), '', reason: '--err-t3--');
    expect(initials(['', '']), '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String initials(List<String> names) {
  return names
      .where((n) => n.isNotEmpty)
      .map((n) => n[0].toUpperCase())
      .fold('', (acc, letter) => acc + letter);
}
```
