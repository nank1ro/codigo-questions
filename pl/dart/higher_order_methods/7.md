---
language: dart
exerciseType: 1
---

# --description--

`reduce` to krótszy krewny `fold`. Nie przyjmuje wartości początkowej: **pierwszy element** jest początkowym akumulatorem, a funkcja jest wykonywana dla każdego pozostałego elementu:

```dart
final numbers = [1, 2, 3, 4];
print(numbers.reduce((a, b) => a + b)); // 10
```

Ponieważ nie ma wartości początkowej, wynik zawsze ma **ten sam typ co elementy**, a wywołanie `reduce` na pustej kolekcji rzuca `StateError`: nie ma pierwszego elementu, od którego można zacząć. `fold` nie ma takiego problemu, dlatego jest bezpieczniejszym domyślnym wyborem.

`reduce` sprawdza się najlepiej, gdy szukasz jednego elementu wśród wielu, na przykład największego:

```dart
print(numbers.reduce((a, b) => a > b ? a : b)); // 4
```

# --instructions--

Napisz funkcję `largest`, która przyjmuje niepustą `List<int>` `numbers` i zwraca największy element, używając `reduce`.

Przykład:
```dart
print(largest([3, 9, 4])); // 9
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest([3, 9, 4])` powinno zwrócić `9`

```dart
  test('test1', () {
    expect(largest([3, 9, 4]), 9, reason: '--err-t1--');
  });
```

`largest` powinno działać, gdy największy element jest pierwszy lub ostatni, oraz dla pojedynczego elementu

```dart
  test('test2', () {
    expect(largest([10, 2, 5]), 10, reason: '--err-t2--');
    expect(largest([1, 2, 8]), 8, reason: '--err-t2--');
    expect(largest([7]), 7, reason: '--err-t2--');
  });
```

`largest` powinno działać z liczbami ujemnymi i nie może zmieniać otrzymanej listy

```dart
  test('test3', () {
    expect(largest([-4, -1, -9]), -1, reason: '--err-t3--');
    final original = [2, 5, 1];
    largest(original);
    expect(original, [2, 5, 1], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int largest(List<int> numbers) {
  return numbers.reduce((a, b) => a > b ? a : b);
}
```
