---
language: dart
exerciseType: 1
---

# --description--

Gdy funkcja przekazana do `map` zwraca kolekcję dla każdego elementu, kończysz z sekwencją kolekcji. **`expand`** wykonuje tę samą pracę, ale następnie łączy je wszystkie w jedną płaską sekwencję:

```dart
final numbers = [1, 2];
print(numbers.map((n) => [n, -n]).toList());    // [[1, -1], [2, -2]]
print(numbers.expand((n) => [n, -n]).toList()); // [1, -1, 2, -2]
```

Kolejność jest zachowana: najpierw pojawia się wszystko wyprodukowane przez pierwszy element, potem wszystko wyprodukowane przez drugi i tak dalej.

Ponieważ zwracana kolekcja może mieć dowolny rozmiar, `expand` to również sposób na wyprodukowanie **więcej lub mniej** elementów niż na początku: zwrócenie pustej listy dla elementu po prostu go pomija.

```dart
print(['a b', 'c'].expand((s) => s.split(' ')).toList()); // [a, b, c]
```

# --instructions--

Napisz funkcję `doubleUp`, która przyjmuje `List<int>` `numbers` i zwraca nową `List<int>`, w której każdy element występuje dwa razy pod rząd, używając `expand`.

Przykład:
```dart
print(doubleUp([1, 2])); // [1, 1, 2, 2]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`doubleUp([1, 2])` powinno zwrócić `[1, 1, 2, 2]`

```dart
  test('test1', () {
    expect(doubleUp([1, 2]), [1, 1, 2, 2], reason: '--err-t1--');
  });
```

`doubleUp` powinno zachować oryginalną kolejność i zwrócić `List<int>`

```dart
  test('test2', () {
    expect(doubleUp([5, 0, -3]), [5, 5, 0, 0, -3, -3], reason: '--err-t2--');
    expect(doubleUp([7]), isA<List<int>>(), reason: '--err-t2--');
  });
```

`doubleUp` powinno zwrócić pustą listę dla pustej listy i nie może zmieniać otrzymanej listy

```dart
  test('test3', () {
    expect(doubleUp(<int>[]), <int>[], reason: '--err-t3--');
    final original = [4, 6];
    doubleUp(original);
    expect(original, [4, 6], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> doubleUp(List<int> numbers) {
  return numbers.expand((n) => [n, n]).toList();
}
```
