---
language: python
exerciseType: 1
---

# --description--

`pop()` usuwa **dowolny element** ze zbioru i go zwraca.
Ponieważ zbiór nie ma kolejności, nie możesz wybrać, który element zostanie usunięty; wywołanie `pop()` na pustym zbiorze zgłasza `KeyError`:
```python
tickets = {101, 102, 103}
picked = tickets.pop()
print(len(tickets))  # 2
```
`clear()` usuwa **wszystkie** elementy, pozostawiając pusty zbiór:
```python
tickets.clear()
print(len(tickets))  # 0
```

# --instructions--

Usuń jeden element z `tickets` za pomocą `pop()` i zapisz go w zmiennej `picked`, następnie opróżnij zbiór `drafts` za pomocą `clear()`

# --seed--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `pop()`

```json
{
  "regex": "\\.pop\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć `clear()`

```json
{
  "regex": "\\.clear\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`picked` musi być jednym z oryginalnych ticketów

```python
    def test_1(self):
        self.assertIn(picked, {101, 102, 103}, "--err-t1--")
```

`tickets` musi zawierać 2 elementy i nie może już zawierać `picked`

```python
    def test_2(self):
        self.assertEqual(len(tickets), 2, "--err-t2--")
        self.assertNotIn(picked, tickets, "--err-t2--")
```

`drafts` musi być puste

```python
    def test_3(self):
        self.assertEqual(drafts, set(), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
picked = tickets.pop()
drafts.clear()
```
