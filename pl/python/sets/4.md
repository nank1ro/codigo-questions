---
language: python
exerciseType: 2
---

# --description--

Zbiory są **mutowalne**: możesz dodawać i usuwać elementy po ich utworzeniu.
`add(value)` wstawia wartość; dodanie takiej, która już jest obecna, nic nie zmienia:
```python
letters = {"a", "b"}
letters.add("c")
letters.add("a")
print(len(letters))  # 3
```
Istnieją dwa sposoby usunięcia elementu:
- `remove(value)` go usuwa, ale zgłasza `KeyError`, jeśli wartości nie ma w zbiorze
- `discard(value)` usuwa ją, jeśli jest obecna, i **nic nie robi** w przeciwnym razie, bez błędu
```python
letters.remove("a")
letters.discard("z")  # "z" is not there, but no error
letters.remove("z")   # KeyError: 'z'
```

# --instructions--

Uzupełnij kod, aby dodać `"c"` do `letters`, następnie usunąć `"z"` (którego nie ma w zbiorze) bez zgłaszania błędu, a potem wypisać, ile elementów zawiera zbiór

# --seed--

```python
letters = {"a", "b"}
letters.[/]("c")
letters.[/]("z")
print([/](letters))
```

# --answers--

- add
- discard
- len
- remove
- append
- size

# --solutions--

```python
letters = {"a", "b"}
letters.add("c")
letters.discard("z")
print(len(letters))
```

# --output--

3
