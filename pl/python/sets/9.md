---
language: python
exerciseType: 1
---

# --description--

**Różnica** `a - b` zawiera elementy `a`, których **nie ma** w `b`.
Kolejność ma znaczenie: `a - b` i `b - a` zwykle się różnią:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a - b)  # {1}
print(b - a)  # {4}
```
**Różnica symetryczna** `a ^ b` zawiera elementy, które są w **dokładnie jednym** z dwóch zbiorów:
```python
print(a ^ b)  # {1, 4}
```
Formy metodowe to `difference()` i `symmetric_difference()`.

# --instructions--

Zapisz w `only_ann` hobby `ann`, których `bob` nie ma, za pomocą `-`, a w `not_shared` hobby, które ma tylko jedno z nich, za pomocą `^`

# --seed--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć operatora `-`

```json
{
  "regex": "\\s-\\s",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć operatora `^`

```json
{
  "regex": "\\^",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`only_ann` musi być równe `{'tennis', 'yoga'}`

```python
    def test_1(self):
        self.assertEqual(only_ann, {"tennis", "yoga"}, "--err-t1--")
```

`not_shared` musi być równe `{'tennis', 'yoga', 'swimming'}`

```python
    def test_2(self):
        self.assertEqual(not_shared, {"tennis", "yoga", "swimming"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
only_ann = ann - bob
not_shared = ann ^ bob
```
