---
language: python
exerciseType: 1
---

# --description--

Dwa zbiory są **równe**, gdy zawierają te same elementy, niezależnie od kolejności, w jakiej zostały zapisane:
```python
print({1, 2, 3} == {3, 1, 2})  # True
```
Porównanie długości kolekcji z długością jej zbioru to szybki sposób na wykrycie duplikatów: jeśli zbiór jest **mniejszy**, jakaś wartość pojawiła się więcej niż raz:
```python
emails = ["a@x.com", "b@x.com", "a@x.com"]
print(len(set(emails)) < len(emails))  # True
```
Metoda listy `count(value)` mówi, ile razy pojawia się dana wartość, co pomaga znaleźć, *które* wartości są zduplikowane.

# --instructions--

Zapisz w `has_duplicates`, czy `emails` zawiera jakikolwiek powtórzony adres, porównując długość listy z długością jej zbioru, a następnie zapisz w `duplicates` zbiór adresów, które pojawiają się więcej niż raz, za pomocą wyrażenia zbiorowego i `count()`

# --seed--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `set()`

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć wyrażenia zbiorowego

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_duplicates` musi być równe `True`

```python
    def test_1(self):
        self.assertEqual(has_duplicates, True, "--err-t1--")
```

`duplicates` musi być równe `{'ann@x.com', 'bob@x.com'}`

```python
    def test_2(self):
        self.assertEqual(duplicates, {"ann@x.com", "bob@x.com"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
has_duplicates = len(set(emails)) < len(emails)
duplicates = {e for e in emails if emails.count(e) > 1}
```
