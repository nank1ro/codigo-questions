---
language: python
exerciseType: 1
---

# --description--

Zbiór może zawierać tylko elementy **haszowalne**, czyli wartości, które nie mogą się zmieniać: liczby, ciągi znaków, `True`/`False` i **krotki**.
Próba dodania listy, słownika lub innego zbioru zgłasza `TypeError`:
```python
points = set()
points.add((1, 2))  # ok, a tuple
points.add([1, 2])  # TypeError: unhashable type: 'list'
```
Zbiory krotek są przydatne do śledzenia unikalnych par, jak współrzędne czy rekordy (name, age):
```python
visits = [(1, 2), (1, 2), (3, 4)]
print(len(set(visits)))  # 2
```

# --instructions--

Zapisz w `points` zbiór odrębnych współrzędnych z `visits`, dodaj do niego punkt `(0, 0)`, a następnie zapisz w `count`, ile odrębnych punktów zawiera

# --seed--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `add()`

```json
{
  "regex": "\\.add\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`points` musi być równe `{(0, 0), (1, 2), (3, 4), (5, 6)}`

```python
    def test_1(self):
        self.assertEqual(points, {(0, 0), (1, 2), (3, 4), (5, 6)}, "--err-t1--")
```

`count` musi być równe `4`

```python
    def test_2(self):
        self.assertEqual(count, 4, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
points = set(visits)
points.add((0, 0))
count = len(points)
```
