---
language: python
exerciseType: 1
---

# --description--

**Suma** dwóch zbiorów to nowy zbiór z elementami **obu**, bez duplikatów.
Użyj operatora `|` lub metody `union()`:
```python
a = {1, 2}
b = {2, 3}
print(a | b)        # {1, 2, 3}
print(a.union(b))   # {1, 2, 3}
```
Ani `a`, ani `b` nie zostają zmodyfikowane: operacje na zbiorach zawsze zwracają nowy zbiór.

# --instructions--

Zapisz w `skills` sumę `frontend` i `backend` za pomocą operatora `|`, następnie zapisz w `total`, ile odrębnych umiejętności zawiera

# --seed--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć operatora `|`

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`skills` musi być równe `{'html', 'css', 'js', 'python', 'sql'}`

```python
    def test_1(self):
        self.assertEqual(skills, {"html", "css", "js", "python", "sql"}, "--err-t1--")
```

`total` musi być równe `5`

```python
    def test_2(self):
        self.assertEqual(total, 5, "--err-t2--")
```

`frontend` nie może zostać zmodyfikowane

```python
    def test_3(self):
        self.assertEqual(frontend, {"html", "css", "js"}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
skills = frontend | backend
total = len(skills)
```
