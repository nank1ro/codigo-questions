---
language: python
exerciseType: 1
---

# --description--

Wbudowana funkcja `set()` buduje zbiór z dowolnej kolekcji, na przykład listy lub ciągu znaków.
Ponieważ zbiór przechowuje każdą wartość tylko raz, jest to klasyczny sposób na **usunięcie duplikatów**:
```python
nums = [1, 2, 2, 3]
unique = set(nums)
print(len(unique))  # 3
```
Aby sprawdzić, czy wartość jest obecna, użyj operatora `in`, który zwraca `True` lub `False`:
```python
print(2 in unique)  # True
print(9 in unique)  # False
```
Sprawdzanie przynależności w zbiorze jest bardzo szybkie, nawet przy tysiącach elementów.

# --instructions--

Zapisz w zmiennej `unique` zbiór zbudowany z `nums` za pomocą `set()`, a w `has_three` czy wartość `3` znajduje się w `unique`, używając `in`

# --seed--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `set()`

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć operatora `in`

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`unique` musi być równe `{1, 2, 3, 4}`

```python
    def test_1(self):
        self.assertEqual(unique, {1, 2, 3, 4}, "--err-t1--")
```

`has_three` musi być równe `True`

```python
    def test_2(self):
        self.assertEqual(has_three, True, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
unique = set(nums)
has_three = 3 in unique
```
