---
language: python
exerciseType: 1
---

# --description--

Funkcja może też **zwracać** funkcję. Zdefiniuj funkcję wewnętrzną przez `def` i zwróć ją bez wywoływania:
```python
def make_greeter(greeting):
    def greet(name):
        return greeting + ", " + name
    return greet

hello = make_greeter("Hello")
print(hello("Ada"))  # Hello, Ada
```
Funkcja wewnętrzna nadal używa `greeting` nawet po zakończeniu `make_greeter`: **pamięta** zmienne z zakresu, w którym powstała. Taka funkcja nazywa się **domknięciem**. Każde wywołanie `make_greeter` tworzy nowe, niezależne domknięcie z własnym `greeting`.

# --instructions--

Napisz funkcję `make_multiplier`, która przyjmuje liczbę `factor` i zwraca funkcję przyjmującą liczbę i zwracającą ją pomnożoną przez `factor`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`make_multiplier(2)` musi zwracać funkcję

```python
    def test_1(self):
        self.assertTrue(callable(make_multiplier(2)), "--err-t1--")
```

`make_multiplier(2)(5)` musi zwracać `10`

```python
    def test_2(self):
        double = make_multiplier(2)
        self.assertEqual(double(5), 10, "--err-t2--")
        self.assertEqual(double(0), 0, "--err-t2--")
```

Każdy mnożnik musi pamiętać własny współczynnik

```python
    def test_3(self):
        double = make_multiplier(2)
        triple = make_multiplier(3)
        self.assertEqual(triple(4), 12, "--err-t3--")
        self.assertEqual(double(4), 8, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def make_multiplier(factor):
    def multiply(number):
        return number * factor
    return multiply
```
