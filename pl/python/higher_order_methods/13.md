---
language: python
exerciseType: 1
---

# --description--

Dekorator przyjmujący tylko jeden argument ma niewielkie zastosowanie. Aby opakować **dowolną** funkcję, opakowanie zbiera wszystkie argumenty pozycyjne w `*args` i wszystkie argumenty słownikowe w `**kwargs`, a następnie przekazuje je bez zmian:
```python
def uppercase(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs).upper()
    return wrapper

@uppercase
def greet(name, punctuation="!"):
    return "hello " + name + punctuation

print(greet("ada"))                    # HELLO ADA!
print(greet("ada", punctuation="?"))   # HELLO ADA?
```
Wewnątrz opakowania `args` jest krotką, a `kwargs` słownikiem; `*` i `**` w wywołaniu rozpakowują je z powrotem na osobne argumenty.

# --instructions--

Napisz dekorator `exclaim`, który zwraca opakowanie przyjmujące dowolne argumenty, wywołuje opakowaną funkcję z nimi i zwraca jej wynik z dołączonym `"!"`; następnie użyj go do udekorowania funkcji `greet`, która przyjmuje `name` i zwraca `"hello <name>"`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz zastosować dekorator przez `@exclaim`

```json
{
  "regex": "^@exclaim\\s*$",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greet("ada")` musi zwracać `"hello ada!"`

```python
    def test_1(self):
        self.assertEqual(greet("ada"), "hello ada!", "--err-t1--")
```

`exclaim` musi działać z funkcją dwóch argumentów

```python
    def test_2(self):
        joined = exclaim(lambda a, b: a + " " + b)
        self.assertEqual(joined("good", "day"), "good day!", "--err-t2--")
```

`exclaim` musi przekazywać argumenty słownikowe

```python
    def test_3(self):
        tag = exclaim(lambda word, times=1: word * times)
        self.assertEqual(tag("hi", times=2), "hihi!", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def exclaim(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs) + "!"
    return wrapper

@exclaim
def greet(name):
    return "hello " + name
```
