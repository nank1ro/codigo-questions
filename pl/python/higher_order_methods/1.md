---
language: python
exerciseType: 2
---

# --description--

W Pythonie funkcja jest **wartością**, tak jak liczba czy ciąg znaków. Możesz zapisać ją w zmiennej, umieścić na liście lub przekazać do innej funkcji. Tylko nawiasy ją wywołują: `shout` to sama funkcja, `shout("hi")` to jej wynik:
```python
def shout(text):
    return text.upper() + "!"

say = shout
print(say("hi"))  # HI!
```
Funkcja, która przyjmuje inną funkcję jako parametr albo ją zwraca, nazywa się **funkcją wyższego rzędu**. Wewnątrz niej parametr jest wywoływany z nawiasami jak każda inna funkcja:
```python
def apply(func, value):
    return func(value)

print(apply(shout, "bye"))  # BYE!
print(apply(len, "bye"))    # 3
```

# --instructions--

Uzupełnij kod tak, aby `apply` wywoływało otrzymaną funkcję, a `say` odnosiło się do `shout` bez jego wywoływania, tworząc wynik `HI!`, `3`

# --seed--

```python
def shout(text):
    return text.upper() + "!"

def apply(func, value):
    return [/](value)

say = [/]
print(say("hi"))
print(apply(len, "bye"))
```

# --answers--

- func
- shout
- shout()
- value
- print

# --solutions--

```python
def shout(text):
    return text.upper() + "!"

def apply(func, value):
    return func(value)

say = shout
print(say("hi"))
print(apply(len, "bye"))
```

# --output--

HI!
3
