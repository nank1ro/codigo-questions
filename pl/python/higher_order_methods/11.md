---
language: python
exerciseType: 1
---

# --description--

`partial` przydaje się z wbudowanymi funkcjami przyjmującymi opcje. `int(text, base=16)` analizuje szesnastkowy ciąg znaków; ustalenie podstawy daje konwerter o jednym argumencie, który pasuje do `map`:
```python
from functools import partial

hex_to_int = partial(int, base=16)
print(hex_to_int("ff"))                      # 255
print(list(map(hex_to_int, ["a", "10"])))    # [10, 16]
```
Obiekt partial pamięta, co opakowuje: `hex_to_int.func` to `int`, a `hex_to_int.keywords` to `{'base': 16}`.

# --instructions--

Zdefiniuj dwie funkcje przez `partial`: `hex_to_int`, która konwertuje szesnastkowy ciąg znaków na `int`, oraz `binary_to_int`, która konwertuje binarny ciąg znaków na `int`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`hex_to_int("ff")` musi zwracać `255`

```python
    def test_1(self):
        self.assertEqual(hex_to_int("ff"), 255, "--err-t1--")
        self.assertEqual(hex_to_int("10"), 16, "--err-t1--")
```

`binary_to_int("1010")` musi zwracać `10`

```python
    def test_2(self):
        self.assertEqual(binary_to_int("1010"), 10, "--err-t2--")
        self.assertEqual(binary_to_int("11"), 3, "--err-t2--")
```

Obie muszą być obiektami partial opakowującymi `int`

```python
    def test_3(self):
        self.assertIs(hex_to_int.func, int, "--err-t3--")
        self.assertIs(binary_to_int.func, int, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import partial

hex_to_int = partial(int, base=16)
binary_to_int = partial(int, base=2)
```
