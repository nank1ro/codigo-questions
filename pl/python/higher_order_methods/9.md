---
language: python
exerciseType: 1
---

# --description--

`reduce(func, iterable, initial)` z modułu `functools` zwija sekwencję do **jednej wartości**. Wywołuje `func` z wynikiem dotychczasowym i kolejnym elementem, zaczynając od `initial`:
```python
from functools import reduce

total = reduce(lambda acc, n: acc + n, [1, 2, 3], 0)
print(total)  # 6
```
Kroki to `0 + 1`, potem `1 + 2`, potem `3 + 3`. Gdy `initial` jest pominięte, pierwszym elementem jest wartość początkowa, ale wtedy pusta sekwencja zgłasza `TypeError`, więc podawaj wartość początkową zawsze, gdy sekwencja może być pusta.

# --instructions--

Napisz funkcję `product`, która przyjmuje listę liczb i zwraca je wszystkie pomnożone przez siebie, używając `reduce`; iloczyn pustej listy musi wynosić `1`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `reduce`

```json
{
  "regex": "reduce\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`product([2, 3, 4])` musi zwracać `24`

```python
    def test_1(self):
        self.assertEqual(product([2, 3, 4]), 24, "--err-t1--")
```

`product([5])` musi zwracać `5`

```python
    def test_2(self):
        self.assertEqual(product([5]), 5, "--err-t2--")
```

`product([])` musi zwracać `1`

```python
    def test_3(self):
        self.assertEqual(product([]), 1, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import reduce

def product(numbers):
    return reduce(lambda acc, n: acc * n, numbers, 1)
```
