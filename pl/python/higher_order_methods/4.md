---
language: python
exerciseType: 1
---

# --description--

Wbudowana funkcja `filter(func, iterable)` zachowuje tylko te elementy, dla których `func` zwraca wartość prawdziwą. Podobnie jak `map` zwraca leniwy obiekt, który trzeba zamienić na listę:
```python
numbers = [4, -2, 7, 0]
print(list(filter(lambda n: n > 0, numbers)))  # [4, 7]
```
Funkcja przekazana do `filter` nazywa się **predykatem**: przyjmuje jeden element i odpowiada na pytanie tak/nie o tym elemencie. Przekazanie `None` zamiast funkcji zachowuje elementy prawdziwe same w sobie, odrzucając `0`, `""` i `None`.

# --instructions--

Napisz funkcję `positives`, która przyjmuje listę liczb i zwraca nową listę zawierającą tylko liczby większe od zera, używając `filter`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `filter`

```json
{
  "regex": "filter\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`positives([3, -1, 0, 5])` musi zwracać `[3, 5]`

```python
    def test_1(self):
        self.assertEqual(positives([3, -1, 0, 5]), [3, 5], "--err-t1--")
```

Wynikiem musi być lista

```python
    def test_2(self):
        self.assertIsInstance(positives([1, 2]), list, "--err-t2--")
        self.assertEqual(positives([1, 2]), [1, 2], "--err-t2--")
```

`positives([-4, 0])` musi zwracać pustą listę

```python
    def test_3(self):
        self.assertEqual(positives([-4, 0]), [], "--err-t3--")
        self.assertEqual(positives([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def positives(numbers):
    return list(filter(lambda n: n > 0, numbers))
```
