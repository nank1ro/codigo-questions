---
language: python
exerciseType: 1
---

# --description--

`None` to osobna wartość, która oznacza "nic tu nie ma". Zwraca ją funkcja, która nie ma instrukcji `return`, i jest to popularny symbol zastępczy dla wartości jeszcze nieznanej.
Ponieważ istnieje tylko jedno `None`, sprawdzaj je operatorem `is`, a nie `==`:
```python
result = None
if result is None:
    print("no result yet")
```
Każdy typ ma atrybut `__name__` zawierający jego nazwę jako napis, co przydaje się w komunikatach:
```python
print(type(3).__name__)     # int
print(type(None).__name__)  # NoneType
```

# --instructions--

Napisz funkcję `describe`, która przyjmuje wartość i zwraca napis `"nothing"`, gdy wartość to `None`, a w przeciwnym razie nazwę jej typu

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz porównać za pomocą `is None`

```json
{
  "regex": "is\\s+None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć `__name__`

```json
{
  "regex": "__name__",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe(None)` musi zwrócić `"nothing"`

```python
    def test_1(self):
        self.assertEqual(describe(None), "nothing", "--err-t1--")
```

`describe(5)` musi zwrócić `"int"`, a `describe(2.0)` musi zwrócić `"float"`

```python
    def test_2(self):
        self.assertEqual(describe(5), "int", "--err-t2--")
        self.assertEqual(describe(2.0), "float", "--err-t2--")
```

`describe("a")` musi zwrócić `"str"`, a `describe(True)` musi zwrócić `"bool"`

```python
    def test_3(self):
        self.assertEqual(describe("a"), "str", "--err-t3--")
        self.assertEqual(describe(True), "bool", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe(value):
    if value is None:
        return "nothing"
    return type(value).__name__
```
