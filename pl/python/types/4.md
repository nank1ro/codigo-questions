---
language: python
exerciseType: 1
---

# --description--

Operatory arytmetyczne już znasz. Tutaj liczy się **typ wyniku**.
Połączenie `int` z `float` daje `float`, nawet gdy część dziesiętna wynosi zero:
```python
print(3 + 0.5)   # 3.5
print(2 * 1.0)   # 2.0
```
**Dzielenie rzeczywiste** `/` zawsze zwraca `float`, nawet gdy liczby dzielą się bez reszty:
```python
print(10 / 5)    # 2.0
print(7 / 2)     # 3.5
```
**Dzielenie całkowite** `//` zaokrągla wynik w dół do najbliższej liczby całkowitej (więc `-7 // 2` to `-4`) i zwraca `int`, gdy oba argumenty są całkowite. Razem z resztą `%` dzieli wielkość na całe części:
```python
print(7 // 2)    # 3
print(7 % 2)     # 1
```

# --instructions--

Zapisz w `each` wynik dzielenia `total` przez `people` operatorem `/`, w `whole` to samo dzielenie operatorem `//`, a w `rest` resztę

# --seed--

```python
total = 17
people = 5
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `//`

```json
{
  "regex": "//",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`each` musi być typu float i równe `3.4`

```python
    def test_1(self):
        self.assertIsInstance(each, float, "--err-t1--")
        self.assertEqual(each, 3.4, "--err-t1--")
```

`whole` musi być liczbą całkowitą `3`

```python
    def test_2(self):
        self.assertIsInstance(whole, int, "--err-t2--")
        self.assertEqual(whole, 3, "--err-t2--")
```

`rest` musi być równe `2`

```python
    def test_3(self):
        self.assertEqual(rest, 2, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
total = 17
people = 5

each = total / people
whole = total // people
rest = total % people
```
