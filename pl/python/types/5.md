---
language: python
exerciseType: 4
---

# --description--

Wartości nie zmieniają typu same z siebie: aby zamienić wartość na inny typ, wywołujesz nazwę typu jak funkcję. Nazywa się to **konwersją** (albo *rzutowaniem*):
```python
print(int("42"))      # 42
print(float("3.5"))   # 3.5
print(str(7))         # '7'
print(int(3.9))       # 3
```
`int()` i `float()` odczytują liczby zapisane jako tekst, czyli to, co dostajesz od użytkownika albo z plików. `str()` zamienia cokolwiek na tekst, więc można to połączyć operatorem `+` z innymi napisami.
Zwróć uwagę, że `int(3.9)` nie zaokrągla: odrzuca część dziesiętną.

# --instructions--

Uporządkuj elementy tak, aby zamienić `age` na liczbę, dodać do niej `1` i wypisać wynik połączony z tekstem `Next year: `

# --answers--

```python
age = "30"
```

```python
years = int(age)
```

```python
next_year = years + 1
```

```python
print("Next year: " + str(next_year))
```

# --solutions--

```python
age = "30"
years = int(age)
next_year = years + 1
print("Next year: " + str(next_year))
```

# --output--

Next year: 31
