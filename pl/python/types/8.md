---
language: python
exerciseType: 2
---

# --description--

`bool` jest **podklasą** `int`: `True` zachowuje się jak `1`, a `False` jak `0` wszędzie tam, gdzie oczekiwana jest liczba:
```python
print(True + 1)     # 2
print(True == 1)    # True
print(False * 10)   # 0
```
`sum()` sumuje elementy listy, więc zsumowanie listy wartości logicznych **zlicza**, ile z nich to `True`:
```python
answers = [True, False, True]
print(sum(answers))  # 2
```
Ze względu na tę relację podklasy `isinstance(True, int)` zwraca `True`, podczas gdy `type(True)` to nadal `bool`.

# --instructions--

Uzupełnij kod tak, aby zliczał poprawne odpowiedzi, dodawał `1` do `True` i sprawdzał, czy `True` jest instancją `int`, wypisując `3`, `2` i `True`

# --seed--

```python
answers = [True, False, True, True]
correct = [/](answers)
print(correct)
print(True [/] 1)
print(isinstance(True, [/]))
```

# --answers--

- sum
- +
- int
- len
- ==
- str

# --solutions--

```python
answers = [True, False, True, True]
correct = sum(answers)
print(correct)
print(True + 1)
print(isinstance(True, int))
```

# --output--

3
2
True
