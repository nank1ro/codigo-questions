---
language: python
exerciseType: 1
---

# --description--

W przeciwieństwie do wielu języków liczby całkowite w Pythonie **nie mają maksymalnego rozmiaru**: `int` rośnie, aby pomieścić tyle cyfr, ile trzeba, więc duże obliczenia pozostają dokładne:
```python
print(3 ** 100)  # 515377520732011331036461129765621272702107522001
```
`float` natomiast zachowuje tylko około 15 cyfr znaczących, więc ta sama potęga jako float traci precyzję:
```python
print(3.0 ** 100)  # 5.153775207320113e+47
```
Ponieważ `str()` działa na dowolnym `int`, szybkim sposobem policzenia cyfr liczby jest zmierzenie długości jej tekstu.

# --instructions--

Zapisz w `big` wartość `2` podniesioną do potęgi `200`, a następnie napisz funkcję `digits`, która przyjmuje dodatnią liczbę `int` i zwraca, ile ma cyfr

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `str()`

```json
{
  "regex": "str\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`big` musi być równe `2 ** 200`

```python
    def test_1(self):
        self.assertEqual(big, 2 ** 200, "--err-t1--")
        self.assertIsInstance(big, int, "--err-t1--")
```

`digits(big)` musi zwrócić `61`

```python
    def test_2(self):
        self.assertEqual(digits(big), 61, "--err-t2--")
```

`digits(7)` musi zwrócić `1`, a `digits(10 ** 50)` musi zwrócić `51`

```python
    def test_3(self):
        self.assertEqual(digits(7), 1, "--err-t3--")
        self.assertEqual(digits(10 ** 50), 51, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
big = 2 ** 200

def digits(number):
    return len(str(number))
```
