---
language: python
exerciseType: 1
---

# --description--

Tekst pochodzący z zewnątrz jest zawsze typu `str` i to twój program musi ustalić, jaki typ naprawdę zawiera.
Częstym podejściem jest wypróbowanie najpierw **najbardziej restrykcyjnej** konwersji i przejście do kolejnej, gdy zgłosi `ValueError`:
```python
try:
    value = int(text)
except ValueError:
    value = float(text)
```
Zagnieżdżenie drugiego `try` wewnątrz bloku `except` pozwala cofnąć się jeszcze raz, na przykład aby zachować tekst bez zmian.

# --instructions--

Napisz funkcję `parse_value`, która przyjmuje napis i zwraca go jako `int`, jeśli jest liczbą całkowitą, jako `float`, jeśli jest liczbą dziesiętną, a w przeciwnym razie bez zmian

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz przechwycić `ValueError` za pomocą `except`

```json
{
  "regex": "except\\s+ValueError",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_value("42")` musi zwrócić `int` `42`

```python
    def test_1(self):
        self.assertEqual(parse_value("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_value("42"), int, "--err-t1--")
        self.assertEqual(parse_value("-8"), -8, "--err-t1--")
```

`parse_value("3.5")` musi zwrócić `float` `3.5`

```python
    def test_2(self):
        self.assertEqual(parse_value("3.5"), 3.5, "--err-t2--")
        self.assertIsInstance(parse_value("3.5"), float, "--err-t2--")
```

`parse_value("abc")` musi zwrócić `"abc"`

```python
    def test_3(self):
        self.assertEqual(parse_value("abc"), "abc", "--err-t3--")
        self.assertEqual(parse_value(""), "", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_value(text):
    try:
        return int(text)
    except ValueError:
        try:
            return float(text)
        except ValueError:
            return text
```
