---
language: python
exerciseType: 1
---

# --description--

Formatowanie zmienia tylko sposób wyświetlania liczby. Aby otrzymać zaokrągloną **wartość**, użyj wbudowanej funkcji `round()`:
```python
print(round(3.14159, 2))  # 3.14
print(round(2.71828, 1))  # 2.7
```
Z jednym argumentem `round()` zaokrągla do najbliższej liczby całkowitej i zwraca `int`; z liczbą miejsc dziesiętnych zwraca `float`:
```python
print(round(3.7))     # 4
print(round(3.7, 0))  # 4.0
```
Zwróć uwagę, że wartości dokładnie w połowie między dwiema liczbami zaokrąglają się do **parzystej**: `round(2.5)` to `2`, a `round(3.5)` to `4`.

# --instructions--

Napisz funkcję `average`, która przyjmuje listę liczb i zwraca ich średnią zaokrągloną do dwóch miejsc dziesiętnych

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `round()`

```json
{
  "regex": "round\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`average([1, 2, 3, 4])` musi zwrócić `2.5`

```python
    def test_1(self):
        self.assertEqual(average([1, 2, 3, 4]), 2.5, "--err-t1--")
```

`average([1, 2, 2])` musi zwrócić `1.67`

```python
    def test_2(self):
        self.assertEqual(average([1, 2, 2]), 1.67, "--err-t2--")
        self.assertEqual(average([1, 1, 1, 2]), 1.25, "--err-t2--")
```

`average([10])` musi zwrócić float `10.0`

```python
    def test_3(self):
        self.assertIsInstance(average([10]), float, "--err-t3--")
        self.assertEqual(average([10]), 10.0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def average(numbers):
    return round(sum(numbers) / len(numbers), 2)
```
