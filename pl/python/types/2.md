---
language: python
exerciseType: 1
---

# --description--

`type()` zwraca klasę wartości, więc możesz ją porównać z nazwą klasy za pomocą `is`:
```python
age = 30
print(type(age) is int)  # True
```
Najczęściej jednak chcesz tylko wiedzieć, **czy** wartość jest danego typu. Do tego służy `isinstance(value, cls)`, które zwraca `True` lub `False`:
```python
print(isinstance(age, int))    # True
print(isinstance(age, str))    # False
```
Drugim argumentem może być też **krotka** klas: wynikiem jest `True`, jeśli wartość należy do którejkolwiek z nich:
```python
print(isinstance(2.5, (int, float)))  # True
```

# --instructions--

Zapisz w `age_type` typ `age`, używając `type()`, w `is_text` czy `name` jest typu `str`, a w `is_number` czy `price` jest typu `int` lub `float`, używając `isinstance()`

# --seed--

```python
age = 30
price = 9.99
name = "Ada"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `type()`

```json
{
  "regex": "type\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć `isinstance()`

```json
{
  "regex": "isinstance\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`age_type` musi być `int`

```python
    def test_1(self):
        self.assertIs(age_type, int, "--err-t1--")
```

`is_text` musi być `True`

```python
    def test_2(self):
        self.assertIs(is_text, True, "--err-t2--")
```

`is_number` musi być `True`

```python
    def test_3(self):
        self.assertIs(is_number, True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
age = 30
price = 9.99
name = "Ada"

age_type = type(age)
is_text = isinstance(name, str)
is_number = isinstance(price, (int, float))
```
