---
language: python
exerciseType: 1
---

# --description--

`startswith(prefix)` i `endswith(suffix)` zwracają `True` lub `False` w zależności od tego, jak zaczyna się lub kończy ciąg znaków:
```python
url = "https://codigo.dev"
print(url.startswith("https"))  # True
print(url.endswith(".com"))     # False
```
To standardowy sposób sprawdzania rozszerzeń plików, protokołów lub prefiksów.

# --instructions--

Napisz funkcję `is_python_file(name)`, która zwraca `True`, jeśli `name` kończy się na `.py`, oraz funkcję `is_comment(line)`, która zwraca `True`, jeśli `line` zaczyna się od `#`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `endswith()`

```json
{
  "regex": "\\.endswith\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć `startswith()`

```json
{
  "regex": "\\.startswith\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`is_python_file("main.py")` musi zwrócić `True`

```python
    def test_1(self):
        self.assertEqual(is_python_file("main.py"), True, "--err-t1--")
```

`is_python_file("notes.txt")` musi zwrócić `False`

```python
    def test_2(self):
        self.assertEqual(is_python_file("notes.txt"), False, "--err-t2--")
```

`is_comment("# todo")` musi zwrócić `True`

```python
    def test_3(self):
        self.assertEqual(is_comment("# todo"), True, "--err-t3--")
```

`is_comment("x = 1  # todo")` musi zwrócić `False`

```python
    def test_4(self):
        self.assertEqual(is_comment("x = 1  # todo"), False, "--err-t4--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_python_file(name):
    return name.endswith(".py")

def is_comment(line):
    return line.startswith("#")
```
