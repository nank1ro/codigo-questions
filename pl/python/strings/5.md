---
language: python
exerciseType: 1
---

# --description--

**Wycinek** (slice) wyodrębnia część ciągu znaków.
Napisz `[początek:koniec]` w nawiasach kwadratowych: znak na pozycji `początek` jest uwzględniony, a ten na pozycji `koniec` jest **wykluczony**:
```python
word = "python"
print(word[0:3])  # pyt
print(word[2:4])  # th
```
Możesz pominąć `początek`, aby wycinać od początku, lub `koniec`, aby wycinać do końca:
```python
print(word[:2])  # py
print(word[2:])  # thon
```
Wycinanie nigdy nie powoduje błędu: `koniec` większy niż długość po prostu zatrzymuje się na ostatnim znaku.

# --instructions--

Zapisz w zmiennej `first` pierwsze 4 znaki `word`, a w `rest` wszystkie pozostałe, używając wycinków

# --seed--

```python
word = "notebook"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć wycinka `[:]`

```json
{
  "regex": "\\[[^\\]]*:[^\\]]*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`first` musi być równe `"note"`

```python
    def test_1(self):
        self.assertEqual(first, "note", "--err-t1--")
```

`rest` musi być równe `"book"`

```python
    def test_2(self):
        self.assertEqual(rest, "book", "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
word = "notebook"
first = word[:4]
rest = word[4:]
```
