---
language: python
exerciseType: 4
---

# --description--

Ponieważ każda metoda ciągu znaków zwraca nowy ciąg znaków, możesz **łączyć** metody jedna po drugiej.
Każde wywołanie działa na wyniku poprzedniego:
```python
text = "  Hello World  "
print(text.strip().lower())  # hello world
```
Wycinek akceptuje też trzecią wartość, **krok**.
Krok `-1` przechodzi przez ciąg znaków od tyłu, co jest klasyczną sztuczką na jego odwrócenie:
```python
print("abc"[::-1])  # cba
```

# --instructions--

Ułóż elementy, aby oczyścić `word`, odwrócić go i wydrukować, czy oczyszczone słowo jest palindromem (czyta się tak samo od tyłu)

# --answers--

```python
word = "  Level  "
```

```python
clean = word.strip().lower()
```

```python
reversed_word = clean[::-1]
```

```python
print(clean == reversed_word)
```

# --solutions--

```python
word = "  Level  "
clean = word.strip().lower()
reversed_word = clean[::-1]
print(clean == reversed_word)
```

# --output--

True
