---
language: python
exerciseType: 1
---

# --description--

`split()` dzieli ciąg znaków na **listę** fragmentów.
Bez argumentów dzieli po białych znakach; z argumentem dzieli po podanym separatorze:
```python
print("a b  c".split())      # ['a', 'b', 'c']
print("2024-01-31".split("-"))  # ['2024', '01', '31']
```
`join()` robi coś odwrotnego: skleja elementy listy w jeden ciąg znaków.
Jest wywoływana na **separatorze**, a lista jest argumentem:
```python
words = ["one", "two", "three"]
print(", ".join(words))  # one, two, three
print("".join(words))    # onetwothree
```

# --instructions--

Zapisz w `words` listę słów z `sentence`, a następnie zapisz w `joined` te same słowa sklejone z `-` między nimi

# --seed--

```python
sentence = "the quick brown fox"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `split()`

```json
{
  "regex": "\\.split\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć `join()`

```json
{
  "regex": "\\.join\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`words` musi być równe `['the', 'quick', 'brown', 'fox']`

```python
    def test_1(self):
        self.assertEqual(words, ["the", "quick", "brown", "fox"], "--err-t1--")
```

`joined` musi być równe `"the-quick-brown-fox"`

```python
    def test_2(self):
        self.assertEqual(joined, "the-quick-brown-fox", "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
sentence = "the quick brown fox"
words = sentence.split()
joined = "-".join(words)
```
