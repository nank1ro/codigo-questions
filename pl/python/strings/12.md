---
language: python
exerciseType: 4
---

# --description--

`replace(old, new)` zwraca kopię ciągu znaków, w której **każde** wystąpienie `old` jest zastąpione przez `new`:
```python
text = "a-b-c"
print(text.replace("-", "+"))  # a+b+c
```
Ponieważ ciągi znaków są niemutowalne, pamiętaj, aby zapisać wynik, jeśli chcesz go zachować.

# --instructions--

Ułóż elementy, aby zastąpić każde `cats` przez `dogs` w `msg` i wydrukować wynik

# --answers--

```python
msg = "I like cats, cats are great"
```

```python
new_msg = msg.replace("cats", "dogs")
```

```python
print(new_msg)
```

# --solutions--

```python
msg = "I like cats, cats are great"
new_msg = msg.replace("cats", "dogs")
print(new_msg)
```

# --output--

I like dogs, dogs are great
