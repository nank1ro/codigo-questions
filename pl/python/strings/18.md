---
language: python
exerciseType: 1
---

# --description--

**Slug** to przyjazna dla URL-i wersja tytułu: małe litery, bez spacji wokół, a słowa oddzielone myślnikami, jak `hello-world`.
Zbudowanie go to po prostu łańcuch metod, których się nauczyłeś: `strip()`, `lower()` i `replace()`.

# --instructions--

Napisz funkcję `slugify(title)`, która zwraca `title` bez spacji wokół, małymi literami i z każdą spacją zastąpioną przez `-`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `replace()`

```json
{
  "regex": "\\.replace\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`slugify("Hello World")` musi zwrócić `"hello-world"`

```python
    def test_1(self):
        self.assertEqual(slugify("Hello World"), "hello-world", "--err-t1--")
```

`slugify("  Learn Python Strings  ")` musi zwrócić `"learn-python-strings"`

```python
    def test_2(self):
        self.assertEqual(slugify("  Learn Python Strings  "), "learn-python-strings", "--err-t2--")
```

`slugify("codigo")` musi zwrócić `"codigo"`

```python
    def test_3(self):
        self.assertEqual(slugify("codigo"), "codigo", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def slugify(title):
    return title.strip().lower().replace(" ", "-")
```
