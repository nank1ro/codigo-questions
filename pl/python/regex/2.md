---
language: python
exerciseType: 1
---

# --description--

`re.search` przeszukuje cały tekst, ale `re.match` wypróbowuje wzorzec tylko na **samym początku**:
```python
import re

print(re.search(r"\d+", "order 42"))  # <re.Match object; span=(6, 8), match='42'>
print(re.match(r"\d+", "order 42"))   # None
print(re.match(r"\d+", "42 orders"))  # <re.Match object; span=(0, 2), match='42'>
```

Obie funkcje zwracają `None`, gdy nic nie pasuje, a obiekt dopasowania jest zawsze prawdziwy, więc zwykłym sposobem na pytanie "czy dopasowano?" jest prosty `if`:
```python
if re.search(r"\d", text):
    print("there is a digit")
```
Gdy potrzebna jest prawdziwa wartość `True` lub `False`, porównaj z `is not None` lub owiń wywołanie w `bool(...)`.

# --instructions--

Napisz funkcję `has_digit(text)`, która zwraca `True`, gdy tekst zawiera cyfrę gdziekolwiek, oraz funkcję `starts_with_digit(text)`, która zwraca `True` tylko wtedy, gdy tekst zaczyna się od cyfry

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`has_digit` musi znaleźć cyfrę gdziekolwiek w tekście

```python
    def test_1(self):
        self.assertEqual(has_digit("order 42"), True, "--err-t1--")
        self.assertEqual(has_digit("7up"), True, "--err-t1--")
```

`has_digit` musi zwracać `False`, gdy nie ma cyfry

```python
    def test_2(self):
        self.assertEqual(has_digit("no numbers here"), False, "--err-t2--")
        self.assertEqual(has_digit(""), False, "--err-t2--")
```

`starts_with_digit` musi patrzeć tylko na początek tekstu

```python
    def test_3(self):
        self.assertEqual(starts_with_digit("42 orders"), True, "--err-t3--")
        self.assertEqual(starts_with_digit("order 42"), False, "--err-t3--")
        self.assertEqual(starts_with_digit(""), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def has_digit(text):
    return re.search(r"\d", text) is not None

def starts_with_digit(text):
    return re.match(r"\d", text) is not None
```
