---
language: python
exerciseType: 1
---

# --description--

`str.split` potrafi ciąć tylko po stałym separatorze. `re.split(pattern, text)` tnie po wszystkim, co opisuje wzorzec, czego zwykle potrzebują nieuporządkowane dane wejściowe:
```python
import re

print("a, b;c".split(","))            # ['a', ' b;c']
print(re.split(r"[,;]", "a, b;c"))    # ['a', ' b', 'c']
print(re.split(r"[,;\s]+", "a, b;c")) # ['a', 'b', 'c']
```
Zapisanie separatora jako `[,;\s]+` sprawia, że cały ciąg przecinków, średników i spacji liczy się jako jedno cięcie, zamiast zostawiać między nimi puste ciągi znaków.

Argument `maxsplit` zatrzymuje się po podanej liczbie cięć, zostawiając resztę tekstu w ostatnim elemencie: `re.split(r"\s+", "a b c", maxsplit=1)` daje `['a', 'b c']`.

# --instructions--

Napisz funkcję `split_fields(line)`, która zwraca listę pól w wierszu, traktując każdy ciąg przecinków, średników i spacji jako jeden separator

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`split_fields` musi ciąć po przecinkach, średnikach i spacjach

```python
    def test_1(self):
        self.assertEqual(split_fields("a, b;c"), ["a", "b", "c"], "--err-t1--")
        self.assertEqual(split_fields("red green blue"), ["red", "green", "blue"], "--err-t1--")
```

Ciąg separatorów musi dawać jedno cięcie

```python
    def test_2(self):
        self.assertEqual(split_fields("x ; ; y"), ["x", "y"], "--err-t2--")
        self.assertEqual(split_fields("1,,2"), ["1", "2"], "--err-t2--")
```

Wiersz bez separatorów musi zwracać jedno pole

```python
    def test_3(self):
        self.assertEqual(split_fields("solo"), ["solo"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def split_fields(line):
    return re.split(r"[,;\s]+", line)
```
