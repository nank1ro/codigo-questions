---
language: python
exerciseType: 1
---

# --description--

Gdy potrzebna jest pozycja lub grupy każdego dopasowania, właściwym wyborem jest `re.finditer(pattern, text)`: przechodzi przez tekst i dostarcza **obiekt dopasowania** dla każdego dopasowania, po jednym na raz:
```python
import re

for match in re.finditer(r"\d+", "a1 bb22"):
    print(match.group(), match.start())
# 1 1
# 22 5
```
`finditer` produkuje iterator, a nie listę, więc można go użyć w pętli `for` albo w list comprehension. Tam gdzie `findall` daje tylko tekst, `finditer` daje wszystko, co wie obiekt dopasowania.

# --instructions--

Napisz funkcję `word_positions(text)`, która zwraca listę krotek, po jednej dla każdego ciągu liter w tekście, zawierającą słowo i indeks, od którego ono zaczyna się

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`word_positions("hi there")` musi zwrócić `[("hi", 0), ("there", 3)]`

```python
    def test_1(self):
        self.assertEqual(word_positions("hi there"), [("hi", 0), ("there", 3)], "--err-t1--")
```

Cyfry i symbole nie mogą być częścią słowa

```python
    def test_2(self):
        self.assertEqual(word_positions("a1b"), [("a", 0), ("b", 2)], "--err-t2--")
        self.assertEqual(word_positions("Ada, Bob"), [("Ada", 0), ("Bob", 5)], "--err-t2--")
```

Tekst bez liter musi zwrócić pustą listę

```python
    def test_3(self):
        self.assertEqual(word_positions("123 456"), [], "--err-t3--")
        self.assertEqual(word_positions(""), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def word_positions(text):
    return [(match.group(), match.start()) for match in re.finditer(r"[a-zA-Z]+", text)]
```
