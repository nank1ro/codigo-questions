---
language: python
exerciseType: 1
---

# --instructions--

Zdefiniuj skompilowany wzorzec `PAIR_RE`, który pasuje do ustawienia zapisanego jako jedna lub więcej liter, znak `=` i jedna lub więcej cyfr, z obiema częściami w nazwanych grupach `key` i `value`; następnie napisz funkcję `parse_settings(text)`, która zwraca słownik mapujący każdy klucz znaleziony w tekście na jego wartość jako `int`

# --before-asserts--

```python
import re
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`PAIR_RE` musi być skompilowanym wzorcem z nazwanymi grupami `key` i `value`

```python
    def test_1(self):
        self.assertIsInstance(PAIR_RE, re.Pattern, "--err-t1--")
        self.assertEqual(PAIR_RE.search("width=10").groupdict(), {"key": "width", "value": "10"}, "--err-t1--")
```

`parse_settings` musi zwracać każde ustawienie z wartością całkowitą

```python
    def test_2(self):
        self.assertEqual(parse_settings("width=10 height=20"), {"width": 10, "height": 20}, "--err-t2--")
        self.assertEqual(parse_settings("size=3; depth=7"), {"size": 3, "depth": 7}, "--err-t2--")
```

Tekst bez ustawień musi zwracać pusty słownik

```python
    def test_3(self):
        self.assertEqual(parse_settings("nothing to read"), {}, "--err-t3--")
        self.assertEqual(parse_settings(""), {}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

PAIR_RE = re.compile(r"(?P<key>[a-zA-Z]+)=(?P<value>\d+)")

def parse_settings(text):
    settings = {}
    for match in PAIR_RE.finditer(text):
        settings[match.group("key")] = int(match.group("value"))
    return settings
```
