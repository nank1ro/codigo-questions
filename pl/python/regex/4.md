---
language: python
exerciseType: 1
---

# --description--

Obiekt dopasowania niesie ze sobą więcej niż dopasowany tekst. Oprócz `.group()` oferuje pozycję dopasowania wewnątrz oryginalnego ciągu znaków:
```python
import re

match = re.search(r"\d+", "order 42 shipped")
print(match.group())  # 42
print(match.start())  # 6
print(match.end())    # 8
print(match.span())   # (6, 8)
```
`.start()` to indeks pierwszego dopasowanego znaku, `.end()` to indeks tuż za ostatnim, a `.span()` zwraca obie te wartości jako krotkę. To znaczy, że `text[match.start():match.end()]` jest zawsze równe `match.group()`.

Ponieważ `re.search` może zwrócić `None`, bezpośrednie odczytanie `.group()` podnosi `AttributeError`, gdy nic nie pasowało; najpierw sprawdź wynik.

# --instructions--

Napisz funkcję `first_number(text)`, która zwraca krotkę z pierwszym ciągiem cyfr, jego indeksem początkowym i indeksem końcowym, albo `None`, gdy tekst nie zawiera cyfry

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_number("order 42 shipped")` musi zwrócić `("42", 6, 8)`

```python
    def test_1(self):
        self.assertEqual(first_number("order 42 shipped"), ("42", 6, 8), "--err-t1--")
```

Dopasowanie musi zaczynać się od indeksu `0`, gdy tekst zaczyna się od cyfr

```python
    def test_2(self):
        self.assertEqual(first_number("7up"), ("7", 0, 1), "--err-t2--")
        self.assertEqual(first_number("100 items"), ("100", 0, 3), "--err-t2--")
```

`first_number` musi zwracać `None`, gdy nie ma cyfry

```python
    def test_3(self):
        self.assertIsNone(first_number("no digits here"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def first_number(text):
    match = re.search(r"\d+", text)
    if match is None:
        return None
    return (match.group(), match.start(), match.end())
```
