---
language: python
exerciseType: 2
---

# --description--

Możesz wycinać ciąg znaków dokładnie tak samo jak listę! W rzeczywistości możesz myśleć o ciągach znaków jako o listach znaków: każdy znak jest kolejnym elementem listy, zaczynając od indeksu `0`.
```python
list_name[:2]
# Grabs the first two items
list_name[3:]
# Grabs the fourth through last items
```
Jeśli wycięty fragment listy obejmuje pierwszy lub ostatni element listy (lub ciągu znaków), indeks tego elementu nie musi być podany.

# --instructions--

- Przypisz do `first_name` wycinek `full_name` od indeksu `0` aż do *ale nie włącznie* indeksu `4`.
- Przypisz do `last_name` wycinek `full_name` od indeksu `4` do końca ciągu znaków

# --seed--

```python
full_name = "Sam Smith"
first_name = [/][/]
last_name = [/][/]
print(first_name)
print(last_name)
```

# --answers--

- full_name
- [:4]
- [:3]
- full_name
- [4:]
- [4:2]

# --solutions--

```python
full_name = "Sam Smith"
first_name = full_name[:3]
last_name = full_name[4:]
print(first_name)
print(last_name)
```

# --output--

Sam
Smith
