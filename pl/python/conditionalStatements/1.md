---
language: python
exerciseType: 2
---

# --description--

Podejmowanie decyzji jest wymagane, gdy chcemy wykonać kod tylko wtedy, gdy spełniony jest określony warunek.
Załóżmy, że chcemy bawić się na zewnątrz tylko wtedy, gdy pogoda jest ładna.
W programowaniu możemy zapisać zmienną logiczną `nice_weather` i wykonać czynność zabawy na zewnątrz `if` ta zmienna jest `True`, jak:
```python
nice_weather = True
if (nice_weather):
    # play outside
```

# --instructions--

Utwórz instrukcję `if` sprawdzającą, czy `time` jest mniejszy niż `12`, a następnie w bloku kodu wydrukuj ciąg `"It's morning"`

# --seed--

```python
time = 8
[/] (time < 12):
    print("It's morning")
```

# --answers--

- if
- ==
- =
- else
- ?

# --solutions--

```python
time = 8
if (time < 12):
    print("It's morning")
```

# --output--

It's morning
