---
language: python
exerciseType: 1
---

# --description--

Wyrażenie po lewej stronie może być dowolną rzeczą, która tworzy wartość: obliczeniem, wywołaniem funkcji, wywołaniem metody.
Zmienna pętli może mieć dowolną nazwę i istnieje tylko wewnątrz nawiasów kwadratowych:
```python
prices = [10, 20]
with_tax = [price * 1.2 for price in prices]
print(with_tax)  # [12.0, 24.0]
```

# --instructions--

Zapisz w zmiennej `squares` listę kwadratów `nums`, używając list comprehension

# --seed--

```python
nums = [1, 2, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć list comprehension

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`squares` musi być równe `[1, 4, 9, 16]`

```python
    def test_1(self):
        self.assertEqual(squares, [1, 4, 9, 16], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 3, 4]
squares = [n * n for n in nums]
```
