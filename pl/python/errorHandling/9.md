---
language: python
exerciseType: 1
---

# --description--

Wyjątek jest obiektem, a `as` wiąże go z nazwą, dzięki czemu program obsługi może go obejrzeć:
```python
try:
    int("abc")
except ValueError as e:
    print(e)                   # invalid literal for int() with base 10: 'abc'
    print(type(e).__name__)    # ValueError
```
`str(e)` — czyli to, czego używa `print(e)` i pole f-stringa — daje komunikat, z którym wyjątek został utworzony, a `type(e).__name__` daje nazwę klasy jako tekst. Nazwa związana przez `as` istnieje tylko wewnątrz bloku `except`; Python usuwa ją po zakończeniu bloku.

# --instructions--

Napisz funkcję `error_label(text)`, która zwraca `int(text)`, a gdy to zgłosi `ValueError`, zwraca napis złożony z nazwy klasy wyjątku, `": "` i komunikatu wyjątku, na przykład `"ValueError: ..."`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`error_label("8")` musi zwrócić `8`

```python
    def test_1(self):
        self.assertEqual(error_label("8"), 8, "--err-t1--")
```

`error_label("abc")` musi zaczynać się od nazwy klasy

```python
    def test_2(self):
        self.assertTrue(error_label("abc").startswith("ValueError: "), "--err-t2--")
```

`error_label("abc")` musi też zawierać komunikat, który wspomina o `abc`

```python
    def test_3(self):
        self.assertIn("abc", error_label("abc")[12:], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def error_label(text):
    try:
        return int(text)
    except ValueError as e:
        return f"{type(e).__name__}: {e}"
```
