---
language: python
exerciseType: 1
---

# --description--

Blok `finally` wykonuje się **cokolwiek się stanie**: po bezbłędnym bloku `try`, po bloku `except`, nawet gdy nieprzechwycony przez nikogo wyjątek wędruje na zewnątrz, a także wtedy, gdy blok `try` lub `except` wykona `return`:
```python
def read(text):
    try:
        return int(text)
    except ValueError:
        return 0
    finally:
        print("done")
```
Obie ścieżki wypisują `done`, zanim wartość opuści funkcję. Ta gwarancja jest właśnie tym, do czego służy `finally`: zamknięcie pliku, zwolnienie blokady, przywrócenie ustawienia. Pełna postać to `try` / `except` / `else` / `finally`; `try` potrzebuje co najmniej jednego `except` albo `finally`, a `else` działa tylko obok `except`.

# --instructions--

Napisz funkcję `record(text)`, która zaczyna od pustej listy, dopisuje do niej `int(text)`, zamiast tego dopisuje napis `"bad"`, gdy `int(text)` zgłosi `ValueError`, zawsze dopisuje `"done"` w bloku `finally` i zwraca listę

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`record("5")` musi zwrócić `[5, "done"]`

```python
    def test_1(self):
        self.assertEqual(record("5"), [5, "done"], "--err-t1--")
```

`record("x")` musi zwrócić `["bad", "done"]`

```python
    def test_2(self):
        self.assertEqual(record("x"), ["bad", "done"], "--err-t2--")
```

każde wywołanie musi zaczynać od nowej listy

```python
    def test_3(self):
        record("1")
        self.assertEqual(record("2"), [2, "done"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def record(text):
    log = []
    try:
        log.append(int(text))
    except ValueError:
        log.append("bad")
    finally:
        log.append("done")
    return log
```
