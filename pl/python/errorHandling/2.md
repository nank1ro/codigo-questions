---
language: python
exerciseType: 1
---

# --description--

Blok `try` zatrzymuje się na **pierwszej** instrukcji, która zgłasza wyjątek; kolejne wiersze są pomijane, a sterowanie przechodzi do bloku `except`. Nic z bloku `try` nie jest cofane, więc trzymaj go możliwie krótkim:
```python
def half(n):
    try:
        return 10 / n
    except ZeroDivisionError:
        return "undefined"

print(half(2))  # 5.0
print(half(0))  # undefined
```
`return` wewnątrz `except` działa jak każdy inny `return`, co czyni `try`/`except` naturalnym sposobem na zwrócenie wartości zastępczej zamiast awarii programu.

# --instructions--

Napisz funkcję `safe_divide(a, b)`, która zwraca `a / b` albo napis `"undefined"`, gdy `b` wynosi `0`, przechwytując `ZeroDivisionError`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_divide(9, 3)` musi zwrócić `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_divide(9, 3), 3.0, "--err-t1--")
```

`safe_divide(1, 0)` musi zwrócić `"undefined"`

```python
    def test_2(self):
        self.assertEqual(safe_divide(1, 0), "undefined", "--err-t2--")
```

`safe_divide(-5, 2)` musi zwrócić `-2.5`

```python
    def test_3(self):
        self.assertEqual(safe_divide(-5, 2), -2.5, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "undefined"
```
