---
language: python
exerciseType: 1
---

# --description--

Wybór najwęższego typu, który obejmuje spodziewaną awarię, jest tym, co czyni obsługę błędów godną zaufania. Funkcja czytająca tekst powinna radzić sobie ze złym tekstem (`ValueError`), ale nie może ukrywać wywołania z argumentem niewłaściwego rodzaju (`TypeError`) — ten błąd należy do wywołującego, więc przepuść go dalej.

# --instructions--

Napisz funkcję `parse_age(text)`, która zwraca `int(text)` i zwraca `-1`, gdy `text` nie jest poprawną liczbą całkowitą. Przechwyć tylko `ValueError`, tak aby `parse_age(None)` nadal zgłaszało `TypeError`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`parse_age("30")` musi zwrócić `30`

```python
    def test_1(self):
        self.assertEqual(parse_age("30"), 30, "--err-t1--")
```

`parse_age("old")` musi zwrócić `-1`

```python
    def test_2(self):
        self.assertEqual(parse_age("old"), -1, "--err-t2--")
```

`parse_age(None)` musi nadal zgłaszać `TypeError`

```python
    def test_3(self):
        with self.assertRaises(TypeError, msg="--err-t3--"):
            parse_age(None)
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_age(text):
    try:
        return int(text)
    except ValueError:
        return -1
```
