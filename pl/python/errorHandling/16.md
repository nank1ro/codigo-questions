---
language: python
exerciseType: 1
---

# --description--

Gdy żaden wbudowany typ nie pasuje, zdefiniuj własny, dziedzicząc po `Exception`. Puste ciało zwykle wystarcza — nazwa jest komunikatem dla czytelnika:
```python
class ConfigError(Exception):
    pass
```
Zachowuje się jak każdy inny wyjątek: `raise ConfigError("bad port")`, a `except ConfigError:` go przechwytuje.

Przetłumaczenie niskopoziomowej awarii na własny typ jest częste, a pierwotny błąd nie powinien przy tym zginąć. `raise NewError(...) from original` **łączy** je w łańcuch: zapisuje `original` w atrybucie `__cause__` nowego wyjątku, a traceback pokazuje oba pod napisem *The above exception was the direct cause of the following exception*:
```python
try:
    port = int(text)
except ValueError as e:
    raise ConfigError("bad port") from e
```
Bez `from e` oba nadal są powiązane niejawnie, ale `from` mówi wprost, że pierwszy błąd spowodował drugi.

# --instructions--

Zdefiniuj klasę wyjątku `ConfigError`, która dziedziczy po `Exception`, i napisz funkcję `read_port(text)`, która zwraca `int(text)`, a gdy to zgłosi `ValueError`, zgłasza `ConfigError("bad port")` połączony z nim łańcuchem przez `raise ... from`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`read_port("8080")` musi zwrócić `8080`, a `ConfigError` musi dziedziczyć po `Exception`

```python
    def test_1(self):
        self.assertEqual(read_port("8080"), 8080, "--err-t1--")
        self.assertTrue(issubclass(ConfigError, Exception), "--err-t1--")
```

`read_port("abc")` musi zgłosić `ConfigError` z komunikatem `"bad port"`

```python
    def test_2(self):
        with self.assertRaises(ConfigError, msg="--err-t2--") as ctx:
            read_port("abc")
        self.assertEqual(str(ctx.exception), "bad port", "--err-t2--")
```

zgłoszony `ConfigError` musi być połączony łańcuchem z pierwotnym `ValueError`

```python
    def test_3(self):
        with self.assertRaises(ConfigError, msg="--err-t3--") as ctx:
            read_port("abc")
        self.assertIsInstance(ctx.exception.__cause__, ValueError, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
class ConfigError(Exception):
    pass

def read_port(text):
    try:
        return int(text)
    except ValueError as e:
        raise ConfigError("bad port") from e
```
