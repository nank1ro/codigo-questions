---
language: python
exerciseType: 1
---

# --description--

`Flag` to enumeracja, której elementy można **łączyć**: jedna wartość może przechowywać kilka elementów naraz, jak zestaw opcji.
Zadeklaruj jej elementy za pomocą `auto()`, które dla `Flag` przypisuje potęgi dwójki (`1`, `2`, `4`, ...), dzięki czemu każda kombinacja ma odrębną wartość:
```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```
Użyj `|`, aby łączyć elementy, `in`, aby sprawdzić, czy element jest częścią kombinacji, oraz `value`, aby zobaczyć powstałą liczbę:
```python
full = Permission.READ | Permission.WRITE | Permission.EXECUTE
print(Permission.WRITE in full)  # True
print(full.value)                # 7
print(full)                      # Permission.READ|WRITE|EXECUTE
```

# --instructions--

Zapisz w `editor` kombinację `Permission.READ` i `Permission.WRITE` za pomocą `|`, w `can_write`, czy `Permission.WRITE` jest w `editor`, a w `bits` wartość `editor`

# --seed--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć operatora `|`

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć operatora `in`

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`editor` musi być równe `Permission.READ | Permission.WRITE`

```python
    def test_1(self):
        self.assertEqual(editor, Permission.READ | Permission.WRITE, "--err-t1--")
```

`can_write` musi być `True`

```python
    def test_2(self):
        self.assertEqual(can_write, True, "--err-t2--")
```

`bits` musi być równe `3`

```python
    def test_3(self):
        self.assertEqual(bits, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()

editor = Permission.READ | Permission.WRITE
can_write = Permission.WRITE in editor
bits = editor.value
```
