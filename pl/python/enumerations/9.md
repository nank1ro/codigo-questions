---
language: python
exerciseType: 1
---

# --description--

Enumeracja jest klasą, więc może mieć **metody** i **właściwości** jak każda inna klasa.
Wewnątrz nich `self` to element, na którym wywołano metodę, więc możesz sprawdzać `self.name`, `self.value` albo porównywać `self` z innymi elementami:
```python
from enum import Enum

class Light(Enum):
    RED = 1
    GREEN = 2

    def can_go(self):
        return self is Light.GREEN

    @property
    def label(self):
        return self.name.lower()

print(Light.GREEN.can_go())  # True
print(Light.RED.label)       # red
```
Każda zwykła wartość przypisana w ciele klasy staje się elementem, natomiast funkcje i właściwości nigdy, niezależnie od tego, gdzie się pojawią.

# --instructions--

Dodaj do `Season` metodę `is_warm()`, która zwraca `True` tylko dla `SPRING` i `SUMMER`, oraz właściwość `label`, która zwraca nazwę elementu małymi literami

# --seed--

```python
from enum import Enum

class Season(Enum):
    WINTER = 1
    SPRING = 2
    SUMMER = 3
    AUTUMN = 4
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz zdefiniować metodę `is_warm(self)`

```json
{
  "regex": "def is_warm\\(self\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć dekoratora `@property`

```json
{
  "regex": "@property",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Season.SUMMER.is_warm()` musi być `True`

```python
    def test_1(self):
        self.assertEqual(Season.SUMMER.is_warm(), True, "--err-t1--")
        self.assertEqual(Season.SPRING.is_warm(), True, "--err-t1--")
```

`Season.WINTER.is_warm()` musi być `False`

```python
    def test_2(self):
        self.assertEqual(Season.WINTER.is_warm(), False, "--err-t2--")
        self.assertEqual(Season.AUTUMN.is_warm(), False, "--err-t2--")
```

`Season.AUTUMN.label` musi być równe `"autumn"`

```python
    def test_3(self):
        self.assertEqual(Season.AUTUMN.label, "autumn", "--err-t3--")
        self.assertEqual(Season.WINTER.label, "winter", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Season(Enum):
    WINTER = 1
    SPRING = 2
    SUMMER = 3
    AUTUMN = 4

    def is_warm(self):
        return self in (Season.SPRING, Season.SUMMER)

    @property
    def label(self):
        return self.name.lower()
```
