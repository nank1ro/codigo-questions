---
language: python
exerciseType: 4
---

# --description--

Iterowanie po klasie i wyszukiwanie elementów po nazwie dobrze ze sobą współpracują, gdy przetwarzasz dane pochodzące z zewnątrz, na przykład linie logu lub plik.
Dictionary comprehension po klasie przygotowuje jeden wpis dla każdego elementu, a `Level[name]` przekształca każdy przychodzący ciąg znaków na pasujący element:
```python
counts = {level: 0 for level in Level}
counts[Level["INFO"]] += 1
```
Ponieważ enumeracja zachowuje kolejność deklaracji, iterowanie po `counts` daje później elementy w tej samej kolejności.

# --instructions--

Ułóż elementy tak, aby zadeklarować `Level` z automatycznymi wartościami, przygotować licznik dla każdego elementu, zliczyć nazwy z listy, a następnie wypisać nazwę każdego elementu wraz z jego liczbą

# --answers--

```python
from enum import Enum, auto
```

```python
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
```

```python
counts = {level: 0 for level in Level}
```

```python
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
```

```python
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --solutions--

```python
from enum import Enum, auto
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
counts = {level: 0 for level in Level}
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --output--

DEBUG: 0
INFO: 2
ERROR: 1
