---
language: python
exerciseType: 2
---

# --description--

Często dokładne wartości nie mają znaczenia: potrzebujesz jedynie, aby elementy były różne.
W takim przypadku możesz pozwolić, aby Python dobrał wartości za pomocą `auto()`, również importowanego z modułu `enum`.
Przypisuje on `1` pierwszemu elementowi, a następnie liczy w górę:
```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    SOUTH = auto()
    EAST = auto()

print(Direction.EAST.value)  # 3
```

# --instructions--

Uzupełnij kod, aby zaimportować `auto` i wypisać wartość elementu `LARGE` enumeracji `Size`

# --seed--

```python
from enum import Enum, [/]

class Size(Enum):
    SMALL = auto()
    MEDIUM = auto()
    LARGE = auto()

print(Size.LARGE.[/])
```

# --answers--

- auto
- value
- name
- Enum
- unique
- index

# --solutions--

```python
from enum import Enum, auto

class Size(Enum):
    SMALL = auto()
    MEDIUM = auto()
    LARGE = auto()

print(Size.LARGE.value)
```

# --output--

3
