---
language: python
exerciseType: 1
---

# --description--

Każdy element enumeracji ma dwa atrybuty: `name`, czyli identyfikator zapisany w klasie, oraz `value`, czyli przypisaną mu wartość:
```python
from enum import Enum

class Color(Enum):
    RED = 1
    GREEN = 2

print(Color.GREEN.name)   # GREEN
print(Color.GREEN.value)  # 2
```
Wartość może być dowolnego typu, nie tylko liczbą całkowitą: częstym wyborem są ciągi znaków, krotki i liczby zmiennoprzecinkowe.
Element jest zwykłym obiektem, więc możesz zapisać go w zmiennej i później odczytać jego atrybuty:
```python
chosen = Color.RED
print(chosen.value)  # 1
```

# --instructions--

Zapisz w `home` element `Planet.EARTH`, w `label` jego nazwę, a w `position` jego wartość

# --seed--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `.name`

```json
{
  "regex": "\\.name\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć `.value`

```json
{
  "regex": "\\.value\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`home` musi być `Planet.EARTH`

```python
    def test_1(self):
        self.assertIs(home, Planet.EARTH, "--err-t1--")
```

`label` musi być równe `"EARTH"`

```python
    def test_2(self):
        self.assertEqual(label, "EARTH", "--err-t2--")
```

`position` musi być równe `3`

```python
    def test_3(self):
        self.assertEqual(position, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3

home = Planet.EARTH
label = home.name
position = home.value
```
