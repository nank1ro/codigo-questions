---
language: python
exerciseType: 1
---

# --description--

Oprócz zwykłych metod enumeracja może definiować **metody klasy** za pomocą `@classmethod`. Otrzymują one samą klasę enumeracji jako `cls`, więc to właściwe miejsce na alternatywne sposoby znajdowania elementu:
```python
class Color(Enum):
    RED = auto()
    BLUE = auto()

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]

print(Color.parse(" blue "))  # Color.BLUE
```
Razem z `auto()`, metodami, właściwościami i wyszukiwaniem pozwala to budować enumeracje, które niosą własne zachowanie.

# --instructions--

Zadeklaruj enumerację `Direction` z elementami `NORTH`, `EAST`, `SOUTH` i `WEST`, w tej kolejności, używając `auto()`.
Dodaj metodę `opposite()`, która zwraca element po przeciwnej stronie (`NORTH` i `SOUTH`, `EAST` i `WEST`), oraz metodę klasy `parse(text)`, która zwraca element, którego nazwa pasuje do `text`, ignorując wielkość liter i otaczające spacje

# --seed--

```python
from enum import Enum, auto
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `auto()`

```json
{
  "regex": "auto\\(\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć dekoratora `@classmethod`

```json
{
  "regex": "@classmethod",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Direction` musi mieć elementy `NORTH`, `EAST`, `SOUTH` i `WEST` z wartościami od `1` do `4`

```python
    def test_1(self):
        self.assertEqual([d.name for d in Direction], ["NORTH", "EAST", "SOUTH", "WEST"], "--err-t1--")
        self.assertEqual([d.value for d in Direction], [1, 2, 3, 4], "--err-t1--")
```

`Direction.NORTH.opposite()` musi być `Direction.SOUTH`, a `Direction.WEST.opposite()` musi być `Direction.EAST`

```python
    def test_2(self):
        self.assertIs(Direction.NORTH.opposite(), Direction.SOUTH, "--err-t2--")
        self.assertIs(Direction.SOUTH.opposite(), Direction.NORTH, "--err-t2--")
        self.assertIs(Direction.EAST.opposite(), Direction.WEST, "--err-t2--")
        self.assertIs(Direction.WEST.opposite(), Direction.EAST, "--err-t2--")
```

`Direction.parse(" west ")` musi być `Direction.WEST`

```python
    def test_3(self):
        self.assertIs(Direction.parse(" west "), Direction.WEST, "--err-t3--")
        self.assertIs(Direction.parse("North"), Direction.NORTH, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    EAST = auto()
    SOUTH = auto()
    WEST = auto()

    def opposite(self):
        opposites = {
            Direction.NORTH: Direction.SOUTH,
            Direction.SOUTH: Direction.NORTH,
            Direction.EAST: Direction.WEST,
            Direction.WEST: Direction.EAST,
        }
        return opposites[self]

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]
```
