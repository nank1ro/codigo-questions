---
language: python
exerciseType: 4
---

# --description--

Enumeracje naturalnie łączą się z instrukcją `match` (dostępną od Pythona 3.10), która porównuje wartość z serią wzorców `case` i wykonuje pierwszy pasujący:
```python
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
```
Zawsze zapisuj element razem z jego klasą, na przykład `Light.RED`: sama nazwa, jak `case RED:`, nie porównywałaby niczego — po prostu przechwyciłaby wartość do nowej zmiennej `RED` i pasowałaby do wszystkiego.
Wzorzec `case _:` jest domyślny i musi wystąpić jako ostatni, ponieważ żaden wzorzec po nim nie mógłby zostać osiągnięty.

# --instructions--

Ułóż elementy tak, aby zadeklarować enumerację `Light`, zdefiniować `action()` z przypadkiem `RED` jako pierwszym, potem z przypadkiem `GREEN`, a na końcu domyślnym, i wypisać akcję dla `Light.YELLOW`

# --answers--

```python
from enum import Enum
```

```python
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
```

```python
def action(light):
```

```python
    match light:
```

```python
        case Light.RED:
            return "Stop"
```

```python
        case Light.GREEN:
            return "Go"
```

```python
        case _:
            return "Slow down"
```

```python
print(action(Light.YELLOW))
```

# --solutions--

```python
from enum import Enum
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
print(action(Light.YELLOW))
```

# --output--

Slow down
