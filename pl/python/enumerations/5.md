---
language: python
exerciseType: 1
---

# --description--

Element możesz uzyskać na podstawie jego **wartości**, wywołując klasę jak funkcję, albo na podstawie jego **nazwy**, używając nawiasów kwadratowych:
```python
print(Color(2))        # Color.GREEN
print(Color["BLUE"])   # Color.BLUE
```
Oba sposoby są przydatne, gdy wartość lub nazwa pochodzą spoza programu, na przykład z pliku lub od użytkownika.
Jeśli nic nie pasuje, `Color(9)` zgłasza `ValueError`, a `Color["PINK"]` zgłasza `KeyError`.

# --instructions--

Zapisz w `by_value` element `Status`, którego wartością jest `code`, wywołując `Status(...)`, a w `by_name` element, którego nazwą jest `label`, używając `Status[...]`

# --seed--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz wywołać `Status(...)` z `code`

```json
{
  "regex": "Status\\(\\s*code\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć `Status[...]` z `label`

```json
{
  "regex": "Status\\[\\s*label\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`by_value` musi być `Status.DONE`

```python
    def test_1(self):
        self.assertIs(by_value, Status.DONE, "--err-t1--")
```

`by_name` musi być `Status.FAILED`

```python
    def test_2(self):
        self.assertIs(by_name, Status.FAILED, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
by_value = Status(code)
by_name = Status[label]
```
