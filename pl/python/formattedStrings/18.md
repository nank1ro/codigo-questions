---
language: python
exerciseType: 1
---

# --instructions--

Użyj otwierającego `{` i zamykającego `}` nawiasu klamrowego, aby wstawić liczbę `500` w puste miejsce sformatowanego ciągu znaków

# --seed--

```python
print(f"There are  items")
```

# --solutions--

```python
print(f"There are {500} items")
```

# --output--

There are 500 items
