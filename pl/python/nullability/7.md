---
language: python
exerciseType: 1
---

# --description--

Funkcja, która zwraca liczbę **lub** `None`, powinna powiedzieć o tym w swojej sygnaturze. **Podpowiedź typu** (type hint) to adnotacja dokumentująca oczekiwany typ: `name: str` dla parametru i `-> int` dla wartości zwracanej. Python nie wymusza podpowiedzi, ale edytory i czytelnicy na nich polegają:
```python
def parse_age(text: str) -> int | None:
    if text.isdigit():
        return int(text)
    return None
```
`int | None` czytamy "`int` albo `None`". Starszy zapis `Optional[int]` z modułu `typing` oznacza dokładnie to samo i wciąż można go spotkać w istniejącym kodzie.
Za każdym razem, gdy zobaczysz `| None` w sygnaturze, pamiętaj, aby sprawdzić wynik, zanim go użyjesz.

# --instructions--

Napisz funkcję `parse_age` z podpowiedzią typu, która przyjmuje ciąg znaków i zwraca go przekonwertowanego na `int`, gdy zawiera tylko cyfry, a `None` w przeciwnym razie

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Typ zwracany musi być opisany podpowiedzią `int | None`

```json
{
  "regex": "->\\s*(int\\s*\\|\\s*None|None\\s*\\|\\s*int|Optional\\[int\\])",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_age("42")` musi zwracać `42`

```python
    def test_1(self):
        self.assertEqual(parse_age("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_age("42"), int, "--err-t1--")
```

`parse_age("abc")` musi zwracać `None`

```python
    def test_2(self):
        self.assertIsNone(parse_age("abc"), "--err-t2--")
        self.assertIsNone(parse_age("4a"), "--err-t2--")
```

`parse_age("")` musi zwracać `None`

```python
    def test_3(self):
        self.assertIsNone(parse_age(""), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_age(text: str) -> int | None:
    if text.isdigit():
        return int(text)
    return None
```
