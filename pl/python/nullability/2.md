---
language: python
exerciseType: 1
---

# --description--

Każde wywołanie funkcji daje wartość, nawet gdy funkcja pozornie niczego nie zwraca. Funkcja **bez** instrukcji `return` albo z samotnym `return` oddaje `None`:
```python
def say_hello():
    print("hello")

result = say_hello()
print(result)  # None
```
Dlatego wywołanie `print(my_list.append(3))` pokazuje `None`: `append` zmienia listę w miejscu i nic nie zwraca.
Funkcja, która tylko wykonuje czynność (wypisywanie, zapisywanie, zmienianie listy), zwykle zwraca `None`, podczas gdy funkcja, która coś oblicza, musi to jawnie zwrócić za pomocą `return`.

# --instructions--

Napisz funkcję `store`, która przyjmuje wartość i dodaje ją do listy `saved`, niczego nie zwracając

# --seed--

```python
saved = []

def store(value):
    pass
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `append`

```json
{
  "regex": "\\.append\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`store("a")` musi zwracać `None`

```python
    def test_1(self):
        self.assertIsNone(store("a"), "--err-t1--")
```

Po `store("b")` lista `saved` musi kończyć się elementem `"b"`

```python
    def test_2(self):
        store("b")
        self.assertEqual(saved[-1], "b", "--err-t2--")
```

Każda zapisana wartość musi zostać zachowana, w tej kolejności

```python
    def test_3(self):
        before = len(saved)
        store(1)
        store(2)
        self.assertEqual(saved[before:], [1, 2], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
saved = []

def store(value):
    saved.append(value)
```
