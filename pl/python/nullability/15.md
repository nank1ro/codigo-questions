---
language: python
exerciseType: 1
---

# --instructions--

Napisz funkcję `oldest`, która przyjmuje listę słowników z kluczem `"name"` i opcjonalnym kluczem `"age"`, który może nie występować lub mieć wartość `None`; musi zwracać imię osoby w najstarszym wieku, ignorując osoby bez wieku, a `None`, gdy nikt nie ma wieku

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć `get`

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz sprawdzać za pomocą `is None` lub `is not None`

```json
{
  "regex": "is\\s+(not\\s+)?None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Najstarszą spośród Ada (36), Linus (28) i Grace (45) musi być `"Grace"`

```python
    def test_1(self):
        people = [{"name": "Ada", "age": 36}, {"name": "Linus", "age": 28}, {"name": "Grace", "age": 45}]
        self.assertEqual(oldest(people), "Grace", "--err-t1--")
```

Osoby z brakującym wiekiem lub wiekiem `None` muszą być ignorowane

```python
    def test_2(self):
        people = [{"name": "Tim"}, {"name": "Ada", "age": 36}, {"name": "Bob", "age": None}]
        self.assertEqual(oldest(people), "Ada", "--err-t2--")
```

Wynik musi wynosić `None`, gdy nikt nie ma wieku, a wiek `0` musi się liczyć

```python
    def test_3(self):
        self.assertIsNone(oldest([{"name": "Tim"}, {"name": "Bob", "age": None}]), "--err-t3--")
        self.assertIsNone(oldest([]), "--err-t3--")
        self.assertEqual(oldest([{"name": "Baby", "age": 0}, {"name": "Tim"}]), "Baby", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def oldest(people):
    best_name = None
    best_age = None
    for person in people:
        age = person.get("age")
        if age is None:
            continue
        if best_age is None or age > best_age:
            best_age = age
            best_name = person["name"]
    return best_name
```
