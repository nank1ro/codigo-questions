---
language: python
exerciseType: 1
---

# --description--

Gdy `0` lub `""` muszą zostać zachowane, a zastąpione ma być tylko `None`, wartość zapasowa wymaga jawnego sprawdzenia `is None`. Zwartą formą jest **wyrażenie warunkowe**, `a if condition else b`, które przyjmuje wartość `a`, gdy warunek jest prawdziwy, a `b` w przeciwnym razie:
```python
timeout = 0
seconds = timeout if timeout is not None else 30
print(seconds)  # 0, not 30
```

# --instructions--

Napisz funkcję `port_or_default`, która przyjmuje `port` i zwraca go, gdy nie jest `None`, a w przeciwnym razie `8080`; wartość `0` musi zostać zwrócona bez zmian

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz sprawdzać za pomocą `is not None` lub `is None`

```json
{
  "regex": "is\\s+(not\\s+)?None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`port_or_default(3000)` musi zwracać `3000`

```python
    def test_1(self):
        self.assertEqual(port_or_default(3000), 3000, "--err-t1--")
```

`port_or_default(None)` musi zwracać `8080`

```python
    def test_2(self):
        self.assertEqual(port_or_default(None), 8080, "--err-t2--")
```

`port_or_default(0)` musi zwracać `0`

```python
    def test_3(self):
        self.assertEqual(port_or_default(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def port_or_default(port):
    return port if port is not None else 8080
```
