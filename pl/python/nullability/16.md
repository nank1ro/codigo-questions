---
language: python
exerciseType: 1
---

# --instructions--

Napisz funkcję `get_path`, która przyjmuje słownik `data`, listę `keys` i opcjonalny `default` (`None`, jeśli nie podano); musi przechodzić po kluczach o jeden poziom naraz i zwracać znalezioną wartość albo `default`, gdy jakiegoś klucza brakuje, gdy ma on wartość `None` albo gdy któryś krok nie jest słownikiem; wartości takie jak `0` i `""` muszą zostać zwrócone bez zmian

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Wartością domyślną `default` musi być `None`

```json
{
  "regex": "default\\s*=\\s*None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`get_path({"address": {"city": "Rome"}}, ["address", "city"])` musi zwracać `"Rome"`

```python
    def test_1(self):
        data = {"address": {"city": "Rome"}}
        self.assertEqual(get_path(data, ["address", "city"]), "Rome", "--err-t1--")
        self.assertEqual(get_path(data, ["address"]), {"city": "Rome"}, "--err-t1--")
```

Brakujący krok, krok `None` albo krok niebędący słownikiem muszą dawać wartość domyślną

```python
    def test_2(self):
        data = {"address": None, "name": "Ada"}
        self.assertIsNone(get_path(data, ["address", "city"]), "--err-t2--")
        self.assertEqual(get_path(data, ["phone", "home"], "n/a"), "n/a", "--err-t2--")
        self.assertEqual(get_path(data, ["name", "first"], "n/a"), "n/a", "--err-t2--")
```

`0` i `""` muszą zostać zwrócone bez zmian

```python
    def test_3(self):
        data = {"stats": {"count": 0, "note": ""}}
        self.assertEqual(get_path(data, ["stats", "count"], -1), 0, "--err-t3--")
        self.assertEqual(get_path(data, ["stats", "note"], "none"), "", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def get_path(data, keys, default=None):
    current = data
    for key in keys:
        if not isinstance(current, dict):
            return default
        current = current.get(key)
        if current is None:
            return default
    return current
```
