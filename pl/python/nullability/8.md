---
language: python
exerciseType: 4
---

# --description--

Funkcje, które mogą otrzymać `None`, często zaczynają od **strażnika** (guard): instrukcji `if`, która kończy działanie wcześniej, gdy nie ma nic do roboty. Reszta funkcji może wtedy zakładać, że wartość jest obecna, bez zagnieżdżania wszystkiego w `else`:
```python
def shout(text):
    if text is None:
        return None
    return text.upper() + "!"
```
Strażnicy stoją na początku, w kolejności, w jakiej sprawdzania muszą nastąpić: nie można wywołać `text.split()`, zanim dowiesz się, że `text` nie jest `None`.

# --instructions--

Ułóż elementy, aby zdefiniować `first_word`, które zwraca `None`, gdy `text` jest `None` lub nie zawiera słów, a w przeciwnym razie pierwsze słowo; trzymaj strażników przed kodem, który od nich zależy

# --answers--

```python
def first_word(text):
```

```python
    if text is None:
        return None
```

```python
    words = text.split()
```

```python
    if len(words) == 0:
        return None
```

```python
    return words[0]
```

```python
print(first_word("hello world"))
print(first_word(None))
print(first_word("   "))
```

# --solutions--

```python
def first_word(text):
    if text is None:
        return None
    words = text.split()
    if len(words) == 0:
        return None
    return words[0]
print(first_word("hello world"))
print(first_word(None))
print(first_word("   "))
```

# --output--

hello
None
None
