---
language: python
exerciseType: 3
---

# --description--

Aby sprawdzić, czy zmienna przechowuje `None`, używaj `is` i `is not`, nigdy `==`:
```python
if user is None:
    print("nobody logged in")
if user is not None:
    print("welcome")
```
`==` pyta "czy te wartości są *równe*?", i każda klasa może odpowiedzieć na to pytanie po swojemu, definiując metodę `__eq__`. `is` pyta "czy to *ten sam obiekt*?", i nic nie może zmienić tej odpowiedzi.
Skoro `None` jest tylko jedno, `is None` jest zawsze poprawne i nieco szybsze, podczas gdy `== None` może dać zaskakującą odpowiedź dla obiektów z własnym `__eq__`.

# --instructions--

Co wypisze ten kod?
```python
class Anything:
    def __eq__(self, other):
        return True

a = Anything()
print(a == None, a is None)
```

# --answers--

- `True False`, ponieważ `==` wywołuje własne `__eq__`, podczas gdy `is` sprawdza tożsamość
- `False False`, ponieważ `a` nie jest `None` w żadnym z tych przypadków
- `True True`, ponieważ `__eq__` steruje także wynikiem `is`
- `TypeError`, ponieważ obiektu nie można porównać z `None`

# --solutions--

- `True False`, ponieważ `==` wywołuje własne `__eq__`, podczas gdy `is` sprawdza tożsamość
