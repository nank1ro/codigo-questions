---
language: python
exerciseType: 1
---

# --description--

Parametr może mieć **wartość domyślną**, używaną wtedy, gdy wywołujący pomija argument. `None` to typowa wartość domyślna oznaczająca "nie podano":
```python
def greet(name=None):
    if name is None:
        name = "stranger"
    print("Hello, " + name)

greet()       # Hello, stranger
greet("Ada")  # Hello, Ada
```
Ma to znaczenie dla list i słowników. Wartość domyślna jest obliczana **raz**, przy definiowaniu funkcji, więc `def add(item, items=[])` współdzieli tę samą listę między wszystkimi wywołaniami, które pomijają `items`, a elementy się kumulują. Rozwiązaniem jest użycie `None` jako wartości domyślnej i utworzenie świeżej listy wewnątrz funkcji:
```python
def add(item, items=None):
    if items is None:
        items = []
    items.append(item)
    return items
```

# --instructions--

Napisz funkcję `add_tag`, która przyjmuje ciąg znaków `tag` i opcjonalną listę `tags`; musi dodać `tag` do `tags` i zwrócić listę, tworząc nową listę, gdy `tags` nie został podany

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Wartością domyślną `tags` musi być `None`

```json
{
  "regex": "tags\\s*=\\s*None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`add_tag("new")` musi zwracać `["new"]`

```python
    def test_1(self):
        self.assertEqual(add_tag("new"), ["new"], "--err-t1--")
```

Dwa wywołania bez `tags` muszą zwracać niezależne listy

```python
    def test_2(self):
        first = add_tag("a")
        second = add_tag("b")
        self.assertEqual(first, ["a"], "--err-t2--")
        self.assertEqual(second, ["b"], "--err-t2--")
```

`add_tag("c", ["a", "b"])` musi zwracać `["a", "b", "c"]`

```python
    def test_3(self):
        existing = ["a", "b"]
        self.assertEqual(add_tag("c", existing), ["a", "b", "c"], "--err-t3--")
        self.assertIs(add_tag("d", existing), existing, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def add_tag(tag, tags=None):
    if tags is None:
        tags = []
    tags.append(tag)
    return tags
```
