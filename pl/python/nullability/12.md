---
language: python
exerciseType: 1
---

# --description--

Wyszukanie połączone ze sprawdzeniem `None` zwykle wymaga dwóch linii: jednej do zapisania wyniku i jednej do jego przetestowania. Operator **wyrażenia przypisania** `:=`, nazywany *walrusem*, przypisuje wartość **wewnątrz** wyrażenia, więc oba kroki mieszczą się w `if`:
```python
ages = {"Ada": 36}
if (age := ages.get("Ada")) is not None:
    print(age + 1)  # 37
```
Nawiasy są wymagane: bez nich `:=` próbowałby przypisać całe porównanie. Po `if` zmienna `age` pozostaje dostępna jak każda inna zmienna.

# --instructions--

Napisz funkcję `describe_user`, która przyjmuje słownik `users` mapujący imiona na wieki oraz `name`; musi zwracać `"<name> is <age>"`, gdy imię jest obecne, a `"unknown user"` w przeciwnym razie, używając operatora walrus do zapisania wyniku `get`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć operatora `:=`

```json
{
  "regex": ":=",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć `get`

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe_user({"Ada": 36}, "Ada")` musi zwracać `"Ada is 36"`

```python
    def test_1(self):
        self.assertEqual(describe_user({"Ada": 36}, "Ada"), "Ada is 36", "--err-t1--")
```

`describe_user({"Ada": 36}, "Grace")` musi zwracać `"unknown user"`

```python
    def test_2(self):
        self.assertEqual(describe_user({"Ada": 36}, "Grace"), "unknown user", "--err-t2--")
```

Wiek `0` nadal musi się liczyć jako obecny

```python
    def test_3(self):
        self.assertEqual(describe_user({"Tim": 0}, "Tim"), "Tim is 0", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe_user(users, name):
    if (age := users.get(name)) is not None:
        return f"{name} is {age}"
    return "unknown user"
```
