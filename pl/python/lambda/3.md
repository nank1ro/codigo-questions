---
language: python
exerciseType: 2
---

# --description--

Lambda może przyjmować **więcej niż jeden parametr**.
Oddziel je przecinkami, dokładnie tak jak w funkcji `def`:
```python
power = lambda base, exp: base ** exp
print(power(2, 3))  # 8
```

# --instructions--

Uzupełnij lambdę tak, aby przyjmowała parametry `a` i `b` i zwracała ich sumę

# --seed--

```python
add = lambda a[/] b[/] a + b
print(add(3, 4))
```

# --answers--

- ,
- :
- ;
- ->

# --solutions--

```python
add = lambda a, b: a + b
print(add(3, 4))
```

# --output--

7
