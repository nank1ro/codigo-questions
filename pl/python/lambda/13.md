---
language: python
exerciseType: 4
---

# --description--

`max()` i `min()` również przyjmują funkcję `key`, tak jak `sorted()`:
```python
words = ["hi", "hello", "hey"]
print(max(words, key=lambda w: len(w)))  # hello
```

# --instructions--

Posortuj elementy, aby wypisać imiona w kolejności alfabetycznej z pominięciem wielkości liter, a następnie najdłuższe imię

# --answers--

```python
names = ["bob", "Alice", "Charlie"]
```

```python
ordered = sorted(names, key=lambda s: s.lower())
```

```python
longest = max(ordered, key=lambda s: len(s))
```

```python
print(ordered)
print(longest)
```

# --solutions--

```python
names = ["bob", "Alice", "Charlie"]
ordered = sorted(names, key=lambda s: s.lower())
longest = max(ordered, key=lambda s: len(s))
print(ordered)
print(longest)
```

# --output--

['Alice', 'bob', 'Charlie']
Charlie
