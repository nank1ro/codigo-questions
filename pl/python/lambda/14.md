---
language: python
exerciseType: 1
---

# --description--

**Kiedy powinieneś preferować `def`?**
Lambda świetnie sprawdza się jako krótka, jednorazowa funkcja przekazywana jako argument.
Jeśli logika wymaga nazwy, kilku linii, docstringa lub jest używana w wielu miejscach, funkcja `def` jest czytelniejsza.
Ostatnia sztuczka: `sorted()` przyjmuje też `reverse=True`, aby najpierw uzyskać największe wartości:
```python
print(sorted([3, 1, 2], reverse=True))  # [3, 2, 1]
```

# --instructions--

Zapisz w `ranking` listę `students` posortowaną od najwyższej do najniższej `grade`, następnie zapisz w `names` listę imion z `ranking`, używając `sorted()` i `map()` z lambdami

# --seed--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Musisz użyć wyrażenia `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Musisz użyć `map()`

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ranking` musi zaczynać się od studenta `Cy`

```python
    def test_1(self):
        self.assertEqual(ranking[0]["name"], "Cy", "--err-t1--")
```

`names` musi być równe `['Cy', 'Ana', 'Bo']`

```python
    def test_2(self):
        self.assertEqual(names, ["Cy", "Ana", "Bo"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
ranking = sorted(students, key=lambda s: s["grade"], reverse=True)
names = list(map(lambda s: s["name"], ranking))
```
