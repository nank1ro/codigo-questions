---
language: python
exerciseType: 1
---

# --description--

Operatory logiczne łączą wartości logiczne. `and` daje `True` tylko wtedy, gdy obie strony są `True`, `or` gdy choć jedna nią jest, a `not` odwraca pojedynczą wartość:
```python
age = 20
member = False
print(age >= 18 and member)  # False
print(age >= 18 or member)   # True
print(not member)            # True
```
Porównania wiążą mocniej niż operatory logiczne, więc `age >= 18 and member` nie wymaga nawiasów. Nawiasy są potrzebne, aby zgrupować `or` wewnątrz `and`: `a and (b or c)`.

# --instructions--

Napisz funkcję `is_leap(year)`, która zwraca `True`, gdy `year` jest podzielny przez `4`, ale nie przez `100`, chyba że jest też podzielny przez `400`, a `False` w przeciwnym razie

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`2024` i `2000` są latami przestępnymi

```python
    def test_1(self):
        self.assertIs(is_leap(2024), True, "--err-t1--")
        self.assertIs(is_leap(2000), True, "--err-t1--")
```

`2023` nie jest rokiem przestępnym

```python
    def test_2(self):
        self.assertIs(is_leap(2023), False, "--err-t2--")
```

`1900` jest podzielny przez `100`, ale nie przez `400`, więc nie jest rokiem przestępnym

```python
    def test_3(self):
        self.assertIs(is_leap(1900), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_leap(year):
    return year % 4 == 0 and (year % 100 != 0 or year % 400 == 0)
```
