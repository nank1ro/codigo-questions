---
language: python
exerciseType: 1
---

# --description--

Operatory porównania nie ograniczają się do liczb. Łańcuchy znaków są porównywane znak po znaku według ich punktów kodowych, więc `"apple" < "banana"` to `True`, a ponieważ każda wielka litera jest przed małymi, `"Zoo" < "apple"` również daje `True`. Listy i krotki są porównywane element po elemencie w ten sam sposób:
```python
print("cat" < "dog")       # True
print([1, 2, 3] < [1, 3])  # True
print((1, 2) == (1, 2))    # True
```
Porównanie jest wyrażeniem, więc funkcja może użyć `return a < b` bezpośrednio, zamiast opakowywać je w `if`.

# --instructions--

Napisz funkcję `comes_first(a, b)`, która zwraca `True`, gdy łańcuch `a` jest uporządkowany przed łańcuchem `b`, a `False`, gdy są równe lub `a` jest po `b`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`comes_first("apple", "banana")` musi zwrócić `True`

```python
    def test_1(self):
        self.assertIs(comes_first("apple", "banana"), True, "--err-t1--")
```

`comes_first("pear", "apple")` i `comes_first("kiwi", "kiwi")` muszą zwrócić `False`

```python
    def test_2(self):
        self.assertIs(comes_first("pear", "apple"), False, "--err-t2--")
        self.assertIs(comes_first("kiwi", "kiwi"), False, "--err-t2--")
```

Wielkie litery są przed małymi, więc `comes_first("Zoo", "apple")` musi zwrócić `True`

```python
    def test_3(self):
        self.assertIs(comes_first("Zoo", "apple"), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def comes_first(a, b):
    return a < b
```
