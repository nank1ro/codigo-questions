---
language: python
exerciseType: 1
---

# --description--

`any(iterable)` zwraca `True`, jeśli co najmniej jeden element jest prawdziwy, a `all(iterable)`, jeśli każdy jest (`all([])` to `True`). W połączeniu z wyrażeniem generatorowym wyrażają warunek dla całej kolekcji i stosują skrócone obliczanie jak `or` i `and`:
```python
print(any(ch.isdigit() for ch in "abc1"))  # True
print(all(ch.isalpha() for ch in "ab1"))   # False
```

# --instructions--

Napisz funkcję `is_valid_password(password)`, która zwraca `True` tylko wtedy, gdy łańcuch ma od `8` do `20` znaków włącznie, zawiera co najmniej jedną cyfrę i nie zawiera spacji, a `False` w przeciwnym razie

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`"secret123"` jest poprawne, podobnie jak hasła o dokładnie `8` lub `20` znakach

```python
    def test_1(self):
        self.assertIs(is_valid_password("secret123"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefg1"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefghij123456789x"), True, "--err-t1--")
```

Hasła zbyt krótkie lub zbyt długie nie są poprawne

```python
    def test_2(self):
        self.assertIs(is_valid_password("short1"), False, "--err-t2--")
        self.assertIs(is_valid_password("abcdefghij1234567890x"), False, "--err-t2--")
```

Hasło bez cyfr lub ze spacją nie jest poprawne

```python
    def test_3(self):
        self.assertIs(is_valid_password("nodigitshere"), False, "--err-t3--")
        self.assertIs(is_valid_password("pass word1"), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_valid_password(password):
    return (
        8 <= len(password) <= 20
        and any(ch.isdigit() for ch in password)
        and " " not in password
    )
```
