---
language: python
exerciseType: 1
---

# --description--

Skrócone obliczanie to bezpieczny sposób na **zabezpieczenie** operacji, która zawiodłaby dla niektórych wartości. W `word is not None and len(word) < 4` `len(word)` wykonuje się tylko wtedy, gdy `word` nie jest `None`, więc wywołanie nigdy nie zgłasza błędu. Zabezpieczenie musi być pierwsze:
```python
def is_short(word):
    return word is not None and len(word) < 4

print(is_short(None))   # False
print(is_short("cat"))  # True
```
Pamiętaj, że `and` zwraca argument: `word and len(word) < 4` daje `None` dla `None` i `""` dla pustego łańcucha, a nie `False`. Zabezpiecz prawdziwym porównaniem, gdy wymagana jest wartość logiczna.

# --instructions--

Napisz funkcję `first_is_zero(numbers)`, która zwraca `True`, gdy lista `numbers` nie jest pusta, a jej pierwszy element to `0`, a `False` w przeciwnym razie. Wynik musi być wartością logiczną nawet dla pustej listy

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_is_zero([0, 5])` musi zwrócić `True`

```python
    def test_1(self):
        self.assertIs(first_is_zero([0, 5]), True, "--err-t1--")
        self.assertIs(first_is_zero([0]), True, "--err-t1--")
```

`first_is_zero([3, 0])` musi zwrócić `False`

```python
    def test_2(self):
        self.assertIs(first_is_zero([3, 0]), False, "--err-t2--")
```

`first_is_zero([])` musi zwrócić `False` bez zgłaszania błędu

```python
    def test_3(self):
        self.assertIs(first_is_zero([]), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def first_is_zero(numbers):
    return len(numbers) > 0 and numbers[0] == 0
```
