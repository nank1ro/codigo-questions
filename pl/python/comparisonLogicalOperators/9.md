---
language: python
exerciseType: 4
---

# --description--

Operatory logiczne działają w trybie **skróconego obliczania**: `and` zatrzymuje się, gdy tylko jeden argument jest fałszywy, a `or`, gdy tylko jeden jest prawdziwy, ponieważ wynik jest już znany. Pozostałe argumenty nigdy nie są obliczane, więc jeśli są wywołaniami funkcji, nie wykonują się:
```python
def check(n):
    print("checking", n)
    return n > 0

check(-1) and check(5)  # prints only "checking -1"
check(2) or check(-3)   # prints only "checking 2"
```

# --instructions--

Uporządkuj elementy, aby zdefiniować `is_positive`, obliczyć `first` za pomocą `and`, a następnie `second` za pomocą `or`, i wyświetlić oba wyniki, dając wynik `checking -1`, `checking 2`, `False True`

# --answers--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
```

```python
first = is_positive(-1) and is_positive(5)
```

```python
second = is_positive(2) or is_positive(-3)
```

```python
print(first, second)
```

# --solutions--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
first = is_positive(-1) and is_positive(5)
second = is_positive(2) or is_positive(-3)
print(first, second)
```

# --output--

checking -1
checking 2
False True
