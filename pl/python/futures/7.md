---
language: python
exerciseType: 1
---

# --description--

Współbieżność opłaca się, gdy liczba operacji nie jest ustalona. Zbuduj obiekty korutyn w list comprehension, rozpakuj je do `gather` i użyj `await` na całej partii naraz:
```python
import asyncio

async def fetch(url):
    await asyncio.sleep(0.02)
    return len(url)

async def fetch_all(urls):
    return await asyncio.gather(*[fetch(u) for u in urls])

print(asyncio.run(fetch_all(["a.com", "bb.com"])))  # [5, 6]
```
Dziesięć adresów URL nadal zajmuje łącznie około 20 milisekund zamiast 200, a wyniki pozostają w kolejności adresów URL.

# --instructions--

Napisz funkcję asynchroniczną `fetch(item_id)`, która czeka `0.02` sekundy i zwraca `item_id * 10`, oraz funkcję asynchroniczną `fetch_all(ids)`, która wykonuje `fetch` dla każdego identyfikatora **równolegle** za pomocą `asyncio.gather` i zwraca listę wyników

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch(3)` musi zwracać `30`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch(3)), 30, "--err-t1--")
```

`fetch_all([1, 2, 3])` musi zwracać `[10, 20, 30]`, a `fetch_all([])` musi zwracać `[]`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(fetch_all([1, 2, 3])), [10, 20, 30], "--err-t2--")
        self.assertEqual(asyncio.run(fetch_all([])), [], "--err-t2--")
```

`fetch_all` z pięcioma identyfikatorami musi wykonać je równolegle, zajmując znacznie mniej niż `0.1` sekundy

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(fetch_all([1, 2, 3, 4, 5]))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [10, 20, 30, 40, 50], "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch(item_id):
    await asyncio.sleep(0.02)
    return item_id * 10

async def fetch_all(ids):
    return await asyncio.gather(*[fetch(i) for i in ids])
```
