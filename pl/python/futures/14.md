---
language: python
exerciseType: 2
---

# --description--

`asyncio.wait_for(awaitable, timeout)` używa `await` na czymś, ale poddaje się po `timeout` sekundach: operacja jest anulowana i zgłaszany jest `TimeoutError`, który można złapać jak każdy wyjątek:
```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())  # timed out
```
Z limitem czasu `0.1` ten sam kod wypisałby `done`. `asyncio.TimeoutError` to inna nazwa wbudowanego `TimeoutError`.

# --instructions--

Uzupełnij kod tak, aby `slow()` było użyte z `await` z limitem czasu `0.01` sekundy i limit czasu został złapany, tworząc wynik `timed out`

# --seed--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.[/](slow(), [/]=0.01)
        print(result)
    except [/]:
        print("timed out")

asyncio.run(main())
```

# --answers--

- wait_for
- timeout
- TimeoutError
- gather
- delay
- ValueError

# --solutions--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())
```

# --output--

timed out
