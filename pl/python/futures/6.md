---
language: python
exerciseType: 2
---

# --description--

Używanie `await` na korutynach jedna po drugiej wykonuje je **sekwencyjnie**: trzy oczekiwania po 10 milisekund zajmują 30 milisekund. `asyncio.gather` wykonuje kilka korutyn **równolegle**: podczas gdy jedna śpi, inne robią postępy, więc trzy oczekiwania razem zajmują około 10 milisekund. Zwraca listę z wynikami w tej samej kolejności co argumenty:
```python
import asyncio

async def double(n):
    await asyncio.sleep(0.01)
    return n * 2

async def main():
    results = await asyncio.gather(double(1), double(2), double(3))
    print(results)  # [2, 4, 6]

asyncio.run(main())
```
Samo `gather` musi być użyte z `await`, a korutyny przyjmuje jako osobne argumenty. Aby przekazać listę, rozpakuj ją: `asyncio.gather(*coroutines)`.

# --instructions--

Uzupełnij kod tak, aby trzy wywołania `double` wykonywały się równolegle za pomocą `asyncio.gather`, a ich wyniki zostały wypisane, tworząc wynik `[2, 4, 6]`

# --seed--

```python
import asyncio

async def double(n):
    await asyncio.sleep(0.01)
    return n * 2

async def main():
    results = [/] asyncio.[/](double(1), double(2), double(3))
    print(results)

asyncio.run(main())
```

# --answers--

- await
- gather
- run
- async
- wait
- all

# --solutions--

```python
import asyncio

async def double(n):
    await asyncio.sleep(0.01)
    return n * 2

async def main():
    results = await asyncio.gather(double(1), double(2), double(3))
    print(results)

asyncio.run(main())
```

# --output--

[2, 4, 6]
