---
language: python
exerciseType: 1
---

# --description--

Korutyny wywołują się nawzajem za pomocą `await`. Korutyna może użyć `await` na dowolnej innej korutynie, otrzymać jej wartość zwracaną i kontynuować działanie, dokładnie jak łańcuch wywołań funkcji:
```python
import asyncio

async def fetch_price(item):
    await asyncio.sleep(0.01)  # simulates a slow lookup
    return 10

async def total(item, quantity):
    price = await fetch_price(item)
    return price * quantity

print(asyncio.run(total("pen", 3)))  # 30
```
Przez `asyncio.run` przechodzi tylko najbardziej zewnętrzna korutyna; do każdej wewnętrznej dochodzi się przez `await`. Gdyby `total` było zwykłym `def`, nie mogłoby w ogóle użyć `await`: słowo kluczowe `async` rozprzestrzenia się na każdą funkcję w łańcuchu, która musi czekać.

# --instructions--

Napisz funkcję asynchroniczną `fetch_user(user_id)`, która czeka `0.01` sekundy i zwraca ciąg znaków `"user"` połączony z identyfikatorem, oraz funkcję asynchroniczną `greet_user(user_id)`, która używa `await` na `fetch_user` i zwraca `"Hello, "` połączone z pobraną nazwą

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch_user(1)` musi zwracać `"user1"`, a `fetch_user(42)` musi zwracać `"user42"`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch_user(1)), "user1", "--err-t1--")
        self.assertEqual(asyncio.run(fetch_user(42)), "user42", "--err-t1--")
```

`greet_user(1)` musi zwracać `"Hello, user1"`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(greet_user(1)), "Hello, user1", "--err-t2--")
```

Zarówno `fetch_user`, jak i `greet_user` muszą być funkcjami asynchronicznymi

```python
    def test_3(self):
        self.assertTrue(inspect.iscoroutinefunction(fetch_user), "--err-t3--")
        self.assertTrue(inspect.iscoroutinefunction(greet_user), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch_user(user_id):
    await asyncio.sleep(0.01)
    return f"user{user_id}"

async def greet_user(user_id):
    name = await fetch_user(user_id)
    return f"Hello, {name}"
```
