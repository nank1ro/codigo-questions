---
language: python
exerciseType: 1
---

# --description--

Korutyna może przyjmować parametry, a `return` zwraca wartość dokładnie jak w zwykłej funkcji. Wartość nie jest dostępna w momencie utworzenia obiektu korutyny, lecz dopiero po wykonaniu korutyny. `asyncio.run` zwraca to, co zwróciła korutyna:
```python
import asyncio

async def double(n):
    return n * 2

result = asyncio.run(double(21))
print(result)  # 42
```
Wszystko, co wiesz o funkcjach, obowiązuje także wewnątrz `async def`: zmienne lokalne, warunki, pętle i wiele instrukcji `return`.

# --instructions--

Napisz funkcję asynchroniczną `add(a, b)` ze słowem `async def`, która zwraca sumę `a` i `b`

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`add` musi być funkcją asynchroniczną

```python
    def test_1(self):
        self.assertTrue(inspect.iscoroutinefunction(add), "--err-t1--")
```

Uruchomienie `add(1, 3)` musi zwracać `4`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(add(1, 3)), 4, "--err-t2--")
```

Uruchomienie `add(200, 210)` musi zwracać `410`

```python
    def test_3(self):
        self.assertEqual(asyncio.run(add(200, 210)), 410, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
async def add(a, b):
    return a + b
```
