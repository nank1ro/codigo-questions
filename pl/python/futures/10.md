---
language: python
exerciseType: 1
---

# --description--

`asyncio.create_task(coro)` opakowuje korutynę w **Task** i planuje jej wykonanie w tle. W przeciwieństwie do `await` zwraca natychmiast, więc bieżąca korutyna może dalej pracować, podczas gdy zadanie się wykonuje. Zadanie faktycznie startuje przy najbliższej pauzie bieżącej korutyny na `await`. Późniejsze użycie `await` na zadaniu daje jego wynik:
```python
import asyncio

async def background():
    print("task started")
    await asyncio.sleep(0.01)
    return "task done"

async def main():
    task = asyncio.create_task(background())
    print("main continues")
    print(await task)

asyncio.run(main())
```
Wypisuje to najpierw `main continues`, ponieważ `background` startuje dopiero, gdy `main` użyje `await`, a potem `task started` i `task done`.

# --instructions--

Napisz funkcję asynchroniczną `background(log)`, która dopisuje `"start"` do listy `log`, czeka `0.01` sekundy i dopisuje `"done"`, oraz funkcję asynchroniczną `main(log)`, która planuje `background(log)` za pomocą `asyncio.create_task`, dopisuje `"main"` do `log`, a następnie używa `await` na zadaniu i zwraca `log`

# --before-asserts--

```python
import asyncio
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`background([])` musi zwracać `None` i pozostawiać log jako `["start", "done"]`

```python
    def test_1(self):
        log = []
        self.assertIsNone(asyncio.run(background(log)), "--err-t1--")
        self.assertEqual(log, ["start", "done"], "--err-t1--")
```

`main([])` musi zwracać `["main", "start", "done"]`, ponieważ zadanie startuje dopiero, gdy `main` użyje `await`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(main([])), ["main", "start", "done"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def background(log):
    log.append("start")
    await asyncio.sleep(0.01)
    log.append("done")

async def main(log):
    task = asyncio.create_task(background(log))
    log.append("main")
    await task
    return log
```
