---
language: python
exerciseType: 2
---

# --description--

Zadanie wykonuje się dalej niezależnie od tego, czy ktoś na nie czeka. Utwórz je wcześnie, rób inną pracę, a `await` użyj dopiero w miejscu, w którym potrzebny jest jego wynik: oczekiwanie jest krótsze, bo część zadania wykonała się już w tle. Zadanie można też sprawdzić za pomocą `task.done()`, która zwraca `True`, gdy już się zakończyło.

# --instructions--

Uzupełnij kod tak, aby `compute()` było zaplanowane jako zadanie, `"waiting"` zostało wypisane podczas jego wykonywania, a zadanie zostało potem użyte z `await`, aby uzyskać jego wartość, tworząc wynik `waiting`, `42`

# --seed--

```python
import asyncio

async def compute():
    await asyncio.sleep(0.01)
    return 42

async def main():
    task = asyncio.[/](compute())
    print("waiting")
    value = [/] task
    print(value)

asyncio.run(main())
```

# --answers--

- create_task
- await
- gather
- run
- sleep
- result

# --solutions--

```python
import asyncio

async def compute():
    await asyncio.sleep(0.01)
    return 42

async def main():
    task = asyncio.create_task(compute())
    print("waiting")
    value = await task
    print(value)

asyncio.run(main())
```

# --output--

waiting
42
