---
language: python
exerciseType: 1
---

# --description--

Wyjątek zgłoszony wewnątrz korutyny nie pojawia się tam, gdzie utworzono obiekt korutyny: jest zgłaszany przy `await`, które go wykonuje, albo przez `asyncio.run` dla najbardziej zewnętrznej. Dlatego `try`/`except` musi obejmować **`await`**:
```python
import asyncio

async def load(path):
    await asyncio.sleep(0.01)
    if path == "":
        raise FileNotFoundError("empty path")
    return "contents"

async def safe_load(path):
    try:
        return await load(path)
    except FileNotFoundError:
        return ""

print(asyncio.run(safe_load("")))  # prints an empty line
```
Wyjątek, którego nikt nie łapie, propaguje przez każde `await` aż do `asyncio.run`, które zgłasza go ponownie w kodzie synchronicznym, dokładnie jak zwykły stos wywołań.

# --instructions--

Napisz funkcję asynchroniczną `divide(a, b)`, która czeka `0.01` sekundy i zwraca `a / b`, oraz funkcję asynchroniczną `safe_divide(a, b)`, która używa `await` na `divide(a, b)` i zwraca jej wynik, albo `None`, gdy zostanie zgłoszony `ZeroDivisionError`

# --before-asserts--

```python
import asyncio
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`divide(10, 4)` musi zwracać `2.5`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(divide(10, 4)), 2.5, "--err-t1--")
```

`divide(1, 0)` musi zgłaszać `ZeroDivisionError` przy uruchomieniu

```python
    def test_2(self):
        with self.assertRaises(ZeroDivisionError, msg="--err-t2--"):
            asyncio.run(divide(1, 0))
```

`safe_divide(10, 2)` musi zwracać `5.0`, a `safe_divide(1, 0)` musi zwracać `None`

```python
    def test_3(self):
        self.assertEqual(asyncio.run(safe_divide(10, 2)), 5.0, "--err-t3--")
        self.assertIsNone(asyncio.run(safe_divide(1, 0)), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def divide(a, b):
    await asyncio.sleep(0.01)
    return a / b

async def safe_divide(a, b):
    try:
        return await divide(a, b)
    except ZeroDivisionError:
        return None
```
