---
language: python
exerciseType: 1
---

# --description--

Elementy łączą się naturalnie. Aby wykonać wiele operacji równolegle, każda z własnym limitem czasu, opakuj każdą z nich w małą korutynę, która stosuje `wait_for` i łapie `TimeoutError`, a potem połącz opakowania przez `gather`:
```python
async def guarded(coro, limit):
    try:
        return await asyncio.wait_for(coro, timeout=limit)
    except TimeoutError:
        return None
```
`gather(*[guarded(job(x), limit) for x in items])` zwraca wtedy wartość dla każdego zadania, które zdążyło na czas, i `None` dla każdego, które nie zdążyło, w pierwotnej kolejności, a cała partia zajmuje najwyżej około `limit` sekund.

# --instructions--

Napisz funkcję asynchroniczną `job(delay)`, która czeka `delay` sekund i zwraca `delay`, oraz funkcję asynchroniczną `run_jobs(delays, limit)`, która wykonuje `job` dla każdego opóźnienia **równolegle**, każdą z limitem `limit` sekund za pomocą `asyncio.wait_for`, i zwraca listę z opóźnieniem każdego zadania, które zdążyło na czas, i `None` dla każdego zadania, które przekroczyło limit czasu, w pierwotnej kolejności

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`job(0.01)` musi zwracać `0.01`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(job(0.01)), 0.01, "--err-t1--")
```

`run_jobs([0.01, 0.06, 0.02], 0.04)` musi zwracać `[0.01, None, 0.02]`, a `run_jobs([], 0.01)` musi zwracać `[]`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(run_jobs([0.01, 0.06, 0.02], 0.04)), [0.01, None, 0.02], "--err-t2--")
        self.assertEqual(asyncio.run(run_jobs([], 0.01)), [], "--err-t2--")
```

Pięć zadań po `0.02` sekundy z limitem `0.05` muszą wszystkie się zakończyć i wykonywać równolegle, zajmując znacznie mniej niż `0.1` sekundy

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(run_jobs([0.02] * 5, 0.05))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [0.02] * 5, "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def job(delay):
    await asyncio.sleep(delay)
    return delay

async def guarded(delay, limit):
    try:
        return await asyncio.wait_for(job(delay), timeout=limit)
    except TimeoutError:
        return None

async def run_jobs(delays, limit):
    return await asyncio.gather(*[guarded(d, limit) for d in delays])
```
