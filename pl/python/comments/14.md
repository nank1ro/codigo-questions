---
language: python
exerciseType: 1
---

# --description--

Komentarz umieszczony nad funkcją, aby powiedzieć, co funkcja robi, jest w złym miejscu. Właśnie do tego służy docstring: jest przypisany do funkcji, znajduje go `help()` i pokazują edytory, podczas gdy komentarz `#` nad `def` jest dla nich wszystkich niewidoczny.

```python
# adds a and b
def add(a, b):
    return a + b
```
Przeniesienie tego samego zdania o linię niżej, między potrójne cudzysłowy, czyni z niego prawdziwą dokumentację:
```python
def add(a, b):
    """Returns the sum of a and b."""
    return a + b
```

# --instructions--

Usuń dwa komentarze `#` nad `subtract` i udokumentuj funkcję za pomocą docstringa

# --seed--

```python
# subtract
# this function subtracts b from a
def subtract(a, b):
    return a - b
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Komentarze `#` nad `subtract` muszą zostać usunięte

```json
{
  "regex": "#\\s*this function subtracts",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

Komentarz `# subtract` też musi zostać usunięty

```json
{
  "regex": "#\\s*subtract",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`subtract` musi mieć docstring

```python
    def test_1(self):
        self.assertIsNotNone(subtract.__doc__, "--err-t1--")
```

`subtract(5, 3)` musi zwracać `2`

```python
    def test_2(self):
        self.assertEqual(subtract(5, 3), 2, "--err-t2--")
```

`subtract(0, 4)` musi zwracać `-4`

```python
    def test_3(self):
        self.assertEqual(subtract(0, 4), -4, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def subtract(a, b):
    """Returns b subtracted from a."""
    return a - b
```
