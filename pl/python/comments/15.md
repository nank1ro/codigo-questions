---
language: python
exerciseType: 1
---

# --description--

Python szuka `#` tylko w kodzie, nigdy wewnątrz **ciągu znaków**. Między cudzysłowami `#` jest zwykłym znakiem:
```python
print("black is #000000")  # a hex colour
```
Pierwszy `#` jest częścią tekstu, a drugi rozpoczyna prawdziwy komentarz. To samo dotyczy `"""` wewnątrz komentarza `#`: są tam po prostu trzema znakami cudzysłowu i niczego nie rozpoczynają.

# --instructions--

Napisz funkcję `label(name)`, która zwraca `name` ze znakiem `#` z przodu, tak aby `label("python")` zwracało `"#python"`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`label("python")` musi zwracać `"#python"`

```python
    def test_1(self):
        self.assertEqual(label("python"), "#python", "--err-t1--")
```

`label("ai")` musi zwracać `"#ai"`

```python
    def test_2(self):
        self.assertEqual(label("ai"), "#ai", "--err-t2--")
```

`label("")` musi zwracać `"#"`

```python
    def test_3(self):
        self.assertEqual(label(""), "#", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def label(name):
    """Returns name with a # in front of it."""
    return "#" + name
```
