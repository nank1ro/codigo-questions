---
language: python
exerciseType: 1
---

# --description--

Gdy ciąg znaków jest **pierwszą instrukcją** wewnątrz funkcji, Python traktuje go jako dokumentację tej funkcji. Nazywa się go **docstringiem**:
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"
```
Zgodnie z konwencją docstring zapisuje się w potrójnych cudzysłowach, `"""`, nawet gdy mieści się w jednej linii, aby mógł później urosnąć bez zmiany cudzysłowów.

Zapisuj podsumowanie w trzeciej osobie, jakby opisywało funkcję: "Returns...", "Adds...", "Checks...". Docstring musi poprzedzać każdą inną instrukcję w ciele funkcji, w przeciwnym razie jest to po prostu zwykły ciąg znaków.

# --instructions--

Napisz funkcję `double(n)`, która zwraca `n` pomnożone przez dwa, i dodaj jej jednolinijkowy docstring

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`double` musi mieć docstring jako swoją pierwszą instrukcję

```python
    def test_1(self):
        self.assertIsNotNone(double.__doc__, "--err-t1--")
```

`double(21)` musi zwracać `42`

```python
    def test_2(self):
        self.assertEqual(double(21), 42, "--err-t2--")
```

`double(-3)` musi zwracać `-6`, a `double(0)` musi zwracać `0`

```python
    def test_3(self):
        self.assertEqual(double(-3), -6, "--err-t3--")
        self.assertEqual(double(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def double(n):
    """Returns n multiplied by two."""
    return n * 2
```
