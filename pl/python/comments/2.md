---
language: python
exerciseType: 1
---

# --description--

Komentarz nie musi mieć własnej linii: może następować po kodzie w tej samej linii. To **komentarz w linii**, dobre miejsce na krótką notatkę o tej konkretnej instrukcji:
```python
retries = 3  # poddaj się po trzech próbach
```
Wszystko od `#` do końca linii jest ignorowane, a kod przed nim wykonuje się jak zwykle.

Przewodnik stylu Pythona, **PEP 8**, wymaga tu odrobiny odstępów: co najmniej **dwóch spacji** między kodem a `#` oraz **jednej spacji** po `#`. Komentarz w osobnej linii potrzebuje tylko spacji po `#`.

# --instructions--

Dodaj komentarz w linii po `seats = 4`, wyjaśniający tę wartość, na przykład `# seats per table`. Zostaw dwie spacje przed `#`

# --seed--

```python
seats = 4
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Linia `seats = 4` musi kończyć się komentarzem z `#`, z dwiema spacjami przed `#` i jedną spacją po nim

```json
{
  "regex": "seats = 4[ \\t]{2,}#[ \\t]\\S",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`seats` nadal musi być równe `4`

```python
    def test_1(self):
        self.assertEqual(seats, 4, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
seats = 4  # miejsc przy stole
```
