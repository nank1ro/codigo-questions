---
language: python
exerciseType: 1
---

# --description--

Niektóre komentarze są zgodne z konwencją, którą rozumieją edytory. Najczęstsze **znaczniki** to:
- `# TODO: ...` oznacza coś, co jeszcze trzeba napisać
- `# FIXME: ...` oznacza kod, o którym wiadomo, że jest błędny i wymaga poprawy

```python
limit = 10
# TODO: read the limit from the settings
```
Dla Pythona to zwykłe komentarze; edytory zbierają je w osobnym panelu, więc zaległa praca jest łatwa do znalezienia. `TODO` zwykle stoi obok kodu zastępczego, który utrzymuje program w działaniu, dopóki prawdziwy kod nie zostanie napisany.

Kiedy skończysz pracę, zamień kod zastępczy i usuń znacznik w tej samej zmianie, aby komentarz nigdy nie kłamał o stanie kodu.

# --instructions--

Wykonaj pracę opisaną w komentarzu `TODO` w `is_even`, a następnie usuń komentarz, ponieważ praca jest już gotowa

# --seed--

```python
def is_even(n):
    # TODO: return True when n is even, False otherwise
    return False
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Komentarz `TODO` musi zostać usunięty

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`is_even(4)` musi zwracać `True`

```python
    def test_1(self):
        self.assertEqual(is_even(4), True, "--err-t1--")
```

`is_even(7)` musi zwracać `False`

```python
    def test_2(self):
        self.assertEqual(is_even(7), False, "--err-t2--")
```

`is_even(0)` musi zwracać `True`

```python
    def test_3(self):
        self.assertEqual(is_even(0), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_even(n):
    return n % 2 == 0
```
