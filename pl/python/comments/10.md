---
language: python
exerciseType: 3
---

# --description--

Docstringi i komentarze `#` wyglądają podobnie, ale odpowiadają na różne pytania.

**Docstring** jest dla tego, kto kod **używa**: co funkcja robi, czego oczekuje i co zwraca. Przetrwa w `__doc__`, czyta go `help()`, pokazują go edytory, a narzędzia dokumentacyjne go zbierają.

**Komentarz** jest dla tego, kto kod **czyta**: dlaczego ta linia jest zapisana w ten sposób, co oznacza dziwna liczba, jaki błąd obejmuje. Istnieje tylko w pliku źródłowym i znika, gdy tylko program ruszy.

```python
def timeout():
    """Returns the number of seconds to wait for the server."""
    # serwer zrywa bezczynne połączenia po 35 sekundach
    return 30
```
A więc: dokumentacja funkcji trafia do docstringa, a notatki o implementacji — do komentarzy.

# --instructions--

Jaka jest różnica między docstringiem a komentarzem `#`?

# --answers--

- Docstring to ciąg znaków przechowywany w `__doc__` i pokazywany przez narzędzia, podczas gdy komentarz `#` istnieje tylko w pliku źródłowym
- Nie ma różnicy: oba są wyrzucane przed uruchomieniem programu
- Docstring można używać tylko wewnątrz funkcji, podczas gdy komentarz `#` można używać wszędzie
- Komentarz `#` jest przechowywany w `__doc__`, podczas gdy docstring jest wyrzucany przed uruchomieniem programu

# --solutions--

- Docstring to ciąg znaków przechowywany w `__doc__` i pokazywany przez narzędzia, podczas gdy komentarz `#` istnieje tylko w pliku źródłowym
