---
language: c
exerciseType: 2
---

# --description--

Od C99 nagłówek `stdbool.h` dostarcza typ `bool` oraz stałe `true` (czyli `1`) i `false` (czyli `0`):
```c
#include <stdbool.h>

bool open = true;
bool full = 3 > 5;
printf("%d %d\n", open, full); // prints "1 0"
```
Typ `bool` pod spodem nadal jest liczbą całkowitą: jest wypisywany za pomocą `%d` i działa z `&&`, `||` oraz `!` dokładnie jak wynik porównania. Po prostu lepiej oddaje intencje niż zwykły `int`.

# --instructions--

Uzupełnij kod tak, aby `open` było stałą logiczną oznaczającą prawdę, a `closed` jej negacją, a następnie wypisz obie

# --seed--

```c
bool open = [/];
bool closed = [/]open;
printf("%d %d\n", open, closed);
```

# --answers--

- true
- !
- false
- -
- ~

# --solutions--

```c
bool open = true;
bool closed = !open;
printf("%d %d\n", open, closed);
```

# --output--

1 0
