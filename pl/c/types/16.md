---
language: c
exerciseType: 4
---

# --description--

Podsumowanie: wybieraj typ na podstawie rodzaju wartości, dopasuj każdy specyfikator `printf` do typu jego argumentu i rzutuj, gdy obliczenie musi odbyć się w innym typie niż typ jego argumentów.

# --instructions--

Uporządkuj linie, aby zbudować program, który zapisuje `7` w `total` i `2` w `count`, oblicza `total` podzielone przez `count` jako `double` w `avg`, oraz wypisuje dzielenie całkowitoliczbowe `total / count`, a po nim `avg` z jednym miejscem po przecinku

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int total = 7, count = 2;
```

```c
    double avg = (double) total / count;
```

```c
    printf("%d %.1f\n", total / count, avg);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int total = 7, count = 2;
    double avg = (double) total / count;
    printf("%d %.1f\n", total / count, avg);
    return 0;
}
```

# --output--

3 3.5
