---
language: c
exerciseType: 2
---

# --instructions--

Uzupełnij puste miejsca, aby wykonać blok kodu

# --seed--

```c
bool is_morning = true;
bool is_afternoon = !is_morning;
if ([/]) {
    printf("Good morning!\n");
}
```

# --answers--

- is_morning
- is_afternoon
- "is_morning"
- "is_afternoon"

# --solutions--

```c
bool is_morning = true;
bool is_afternoon = !is_morning;
if (is_morning) {
    printf("Good morning!\n");
}
```

# --output--

Good morning!
