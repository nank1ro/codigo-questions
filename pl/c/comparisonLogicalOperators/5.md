---
language: c
exerciseType: 2
---

# --description--

Operator **lub** `||` daje `1`, gdy przynajmniej jedna strona jest prawdziwa, i `0` tylko wtedy, gdy obie strony są fałszywe:
```c
int day = 1;
printf("%d\n", day == 1 || day == 7); // wypisuje "1"
```
Każda strona musi być pełnym porównaniem: `day == 6 || 7` nie oznacza "6 lub 7" (dowiesz się dlaczego później).

# --instructions--

Uzupełnij kod, aby `weekend` miało wartość `1`, gdy `day` wynosi `6` lub `day` wynosi `7`, a następnie wypisz je

# --seed--

```c
int day = 7;
int weekend = day == 6 [/] day == [/];
printf("%d\n", weekend);
```

# --answers--

- ||
- 7
- &&
- 6
- =

# --solutions--

```c
int day = 7;
int weekend = day == 6 || day == 7;
printf("%d\n", weekend);
```

# --output--

1
