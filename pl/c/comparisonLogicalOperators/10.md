---
language: c
exerciseType: 4
---

# --description--

Operatory mają **pierwszeństwo**, które decyduje, co jest obliczane najpierw:
1. najpierw stosowane jest `!`
2. potem porównania relacyjne `<`, `>`, `<=`, `>=`
3. potem porównania równości `==`, `!=`
4. potem `&&`
5. potem `||`

Dlatego `a > 0 && a < 10` nie potrzebuje nawiasów: oba porównania są obliczane przed `&&`.
A `x == 1 || y == 2 && z == 3` oznacza `x == 1 || (y == 2 && z == 3)`, ponieważ `&&` wiąże silniej niż `||`.

# --instructions--

Uporządkuj linie, aby zapisać w `inside`, czy `a` mieści się między `1` a `9`, zapisać jego negację w `outside` i wypisać obie wartości oddzielone spacją

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int a = 5;
```

```c
    int inside = a > 0 && a < 10;
```

```c
    int outside = !inside;
```

```c
    printf("%d %d\n", inside, outside);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int a = 5;
    int inside = a > 0 && a < 10;
    int outside = !inside;
    printf("%d %d\n", inside, outside);
    return 0;
}
```

# --output--

1 0
