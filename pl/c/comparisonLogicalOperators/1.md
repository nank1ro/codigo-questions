---
language: c
exerciseType: 2
---

# --description--

**Operatory porównania** porównują dwie wartości i dają wynik: `1`, gdy porównanie jest prawdziwe, i `0`, gdy nie jest.
Operator **równości** `==` sprawdza, czy dwie wartości są takie same, a operator **różności** `!=` sprawdza, czy się różnią:
```c
int a = 7, b = 3;
printf("%d\n", a == b);
// wypisuje "0"
printf("%d\n", a != b);
// wypisuje "1"
```
Uważaj: `==` (dwa znaki) porównuje, a pojedyncze `=` przypisuje wartość.

# --instructions--

Uzupełnij kod, aby wypisać, czy `a` jest równe `b`, a następnie czy `a` **nie** jest równe `b`

# --seed--

```c
int a = 7, b = 3;
printf("%d\n", a [/] b);
printf("%d\n", a [/] b);
```

# --answers--

- ==
- !=
- =
- !

# --solutions--

```c
int a = 7, b = 3;
printf("%d\n", a == b);
printf("%d\n", a != b);
```

# --output--

0
1
