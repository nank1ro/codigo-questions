---
language: c
exerciseType: 4
---

# --description--

Parametr typu `bool` może być użyty bezpośrednio jako operand `&&` lub `||`, bez porównywania go z `true`: napisz `age >= 18 && citizen`, a nie `citizen == true`.

# --instructions--

Uporządkuj linie, aby zdefiniować `can_vote`, która zwraca `true`, gdy `age` wynosi co najmniej `18`, a `citizen` to `true`, oraz `main`, która wypisuje wynik dla `20, true`, a potem dla `20, false`. Zdefiniuj funkcję przed `main`

# --answers--

```c
#include <stdio.h>
#include <stdbool.h>
```

```c
bool can_vote(int age, bool citizen) {
```

```c
    return age >= 18 && citizen;
}
```

```c
int main() {
```

```c
    printf("%d\n", can_vote(20, true));
```

```c
    printf("%d\n", can_vote(20, false));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
#include <stdbool.h>
bool can_vote(int age, bool citizen) {
    return age >= 18 && citizen;
}
int main() {
    printf("%d\n", can_vote(20, true));
    printf("%d\n", can_vote(20, false));
    return 0;
}
```

# --output--

1
0
