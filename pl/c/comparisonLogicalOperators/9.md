---
language: c
exerciseType: 4
---

# --description--

Ocena skrócona pomija także wywołania funkcji: w `0 && check()` funkcja `check` nigdy nie zostaje wywołana, więc żaden jej efekt uboczny (np. aktualizacja licznika) nie zachodzi.

# --instructions--

Uporządkuj linie, aby zdefiniować licznik `calls`, funkcję `check`, która go zwiększa i zwraca `1`, oraz `main`, która oblicza `0 && check()` i wypisuje wynik, a po nim `calls`. Zdefiniuj funkcję przed `main`

# --answers--

```c
#include <stdio.h>
int calls = 0;
```

```c
int check() {
```

```c
    calls++;
    return 1;
}
```

```c
int main() {
```

```c
    int result = 0 && check();
```

```c
    printf("%d %d\n", result, calls);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int calls = 0;
int check() {
    calls++;
    return 1;
}
int main() {
    int result = 0 && check();
    printf("%d %d\n", result, calls);
    return 0;
}
```

# --output--

0 0
