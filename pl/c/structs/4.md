---
language: c
exerciseType: 2
---

# --description--

Pisanie `struct Point` za każdym razem jest uciążliwe. Za pomocą `typedef` nadajesz strukturze krótką nazwę typu, a sama struktura może pozostać anonimowa:
```c
typedef struct {
    double celsius;
} Temperature;

Temperature t = {21.5};
```
Nowej nazwy `Temperature` używa się samodzielnie, bez słowa kluczowego `struct` przed nią. To najczęstszy sposób deklarowania struktur w prawdziwych programach.

# --instructions--

Uzupełnij kod, aby zdefiniować anonimową strukturę z polem `celsius` pod nazwą typu `Temperature`, a następnie zadeklaruj `t` z tym typem

# --seed--

```c
typedef [/] {
    double celsius;
} Temperature;
[/] t = {21.5};
printf("%.1f\n", t.celsius);
```

# --answers--

- struct
- enum
- typedef
- Temperature
- struct Temperature
- Temperature t

# --solutions--

```c
typedef struct {
    double celsius;
} Temperature;
Temperature t = {21.5};
printf("%.1f\n", t.celsius);
```

# --output--

21.5
