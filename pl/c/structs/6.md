---
language: c
exerciseType: 4
---

# --description--

Struktury można przechowywać w tablicy jak każdy inny typ. Każdy element inicjalizuje się jego własnymi nawiasami klamrowymi, a pętla odwiedza je jeden po drugim, najpierw indeksując, a potem używając kropki:
```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};

for (int i = 0; i < 3; i++) {
    printf("%d\n", path[i].y);
}
```

# --instructions--

Uporządkuj linie, aby zdefiniować typ `Point`, zapisać trzy punkty w `path`, zsumować ich pola `x` w `sum` i wypisać wynik. Zadeklaruj `path` przed `sum`

# --answers--

```c
typedef struct { int x; int y; } Point;
```

```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
```

```c
int sum = 0;
```

```c
for (int i = 0; i < 3; i++) {
```

```c
    sum += path[i].x;
```

```c
}
```

```c
printf("%d\n", sum);
```

# --solutions--

```c
typedef struct { int x; int y; } Point;
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
int sum = 0;
for (int i = 0; i < 3; i++) {
    sum += path[i].x;
}
printf("%d\n", sum);
```

# --output--

6
