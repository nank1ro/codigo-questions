---
language: c
exerciseType: 2
---

# --description--

Przez wskaźnik pola osiąga się operatorem **strzałki** `->` zamiast kropki:
```c
void grow(Box *b) {
    b->size = b->size * 2;
}
```
`b->size` to skrót od `(*b).size`: najpierw podążasz za wskaźnikiem, potem bierzesz pole. Kropka działa tylko na strukturze, strzałka tylko na wskaźniku do struktury.
Wywołujący przekazuje adres swojej zmiennej za pomocą `&`, a zmiana dokonana przez wskaźnik jest widoczna po wywołaniu.

# --instructions--

Uzupełnij `grow`, aby podwajała `size` przez wskaźnik, oraz uzupełnij wywołanie, aby przekazywało adres `box`

# --seed--

```c
#include <stdio.h>

typedef struct { int size; } Box;

void grow(Box *b) {
    b[/]size = b->size * 2;
}

int main() {
    Box box = {5};
    grow([/]box);
    printf("%d\n", box.size);
    return 0;
}
```

# --answers--

- ->
- .
- &
- *
- (*b)

# --solutions--

```c
#include <stdio.h>

typedef struct { int size; } Box;

void grow(Box *b) {
    b->size = b->size * 2;
}

int main() {
    Box box = {5};
    grow(&box);
    printf("%d\n", box.size);
    return 0;
}
```

# --output--

10
