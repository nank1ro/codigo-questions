---
language: c
exerciseType: 2
---

# --description--

Struktura często przechowuje tekst, zapisany jako pole będące tablicą `char` o stałym rozmiarze:
```c
typedef struct {
    char name[20];
    int age;
} Person;
```
Polu będącemu tablicą nie można przypisać wartości za pomocą `=` po deklaracji: `p.name = "Ann"` się nie skompiluje. Skopiuj do niego tekst za pomocą `strcpy` z `string.h`, przekazując pole jako miejsce docelowe:
```c
Person p;
strcpy(p.name, "Ann");
p.age = 30;
```
Tylko inicjalizator w nawiasach klamrowych przy deklaracji przyjmuje ciąg znaków bezpośrednio: `Person p = {"Ann", 30};`.

# --instructions--

Uzupełnij kod, aby zastąpić imię `p` wartością `"Ann"`, a następnie wypisz imię i wiek

# --seed--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
[/](p.name, "Ann");
printf("%s is %d\n", p[/]name, p.age);
```

# --answers--

- strcpy
- strcat
- strcmp
- strlen
- .
- ->

# --solutions--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
strcpy(p.name, "Ann");
printf("%s is %d\n", p.name, p.age);
```

# --output--

Ann is 30
