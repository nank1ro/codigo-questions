---
language: c
exerciseType: 4
---

# --description--

Tablicy nie można przypisać wartości operatorem `=` po jej deklaracji:
```c
char copy[20];
copy = "Codigo"; // error
```
Aby skopiować ciąg znaków, użyj `strcpy(destination, source)` z `string.h`.
Cel musi być wystarczająco duży, aby pomieścić wszystkie znaki plus `'\0'`.

# --instructions--

Ułóż linie, aby skopiować `source` do `copy` i go wyświetlić. Zadeklaruj `source` przed `copy`

# --answers--

```c
char source[] = "Codigo";
```

```c
char copy[20];
```

```c
strcpy(copy, source);
```

```c
printf("%s\n", copy);
```

# --solutions--

```c
char source[] = "Codigo";
char copy[20];
strcpy(copy, source);
printf("%s\n", copy);
```

# --output--

Codigo
