---
language: c
exerciseType: 4
---

# --instructions--

Ułóż linie tak, aby program porównał `password` z `attempt` i wyświetlił `Access granted`. Zadeklaruj `password` przed `attempt` i umieść każdy komunikat w jego własnej gałęzi

# --answers--

```c
char password[] = "secret";
```

```c
char attempt[] = "secret";
```

```c
if (strcmp(password, attempt) == 0) {
```

```c
    printf("Access granted\n");
```

```c
} else {
```

```c
    printf("Access denied\n");
```

```c
}
```

# --solutions--

```c
char password[] = "secret";
char attempt[] = "secret";
if (strcmp(password, attempt) == 0) {
    printf("Access granted\n");
} else {
    printf("Access denied\n");
}
```

# --output--

Access granted
