---
language: c
exerciseType: 2
---

# --description--

Wyliczenie jest też typem: możesz zadeklarować zmienną tego typu, pisząc `enum`, a następnie nazwę wyliczenia, i przypisać jej jedną z jego stałych:
```c
enum Color favorite = GREEN;
```
Ponieważ stałe są liczbami całkowitymi, zmienne typu enum porównuje się zwykłymi operatorami:
```c
if (favorite == GREEN) {
    printf("Green it is\n");
}
```

# --instructions--

Uzupełnij kod, aby zadeklarować zmienną `light` typu `enum Light` przechowującą `GREEN`, tak aby program wypisał `Go`

# --seed--

```c
enum Light { RED, YELLOW, GREEN };
[/] Light light = [/];
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --answers--

- enum
- struct
- GREEN
- RED
- YELLOW
- "GREEN"

# --solutions--

```c
enum Light { RED, YELLOW, GREEN };
enum Light light = GREEN;
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --output--

Go
