---
language: c
exerciseType: 4
---

# --description--

Odwracanie listy obraca każdy wskaźnik `next`, w miejscu, za pomocą trzech wskaźników: `prev` to już odwrócona część, `head` przetwarzany węzeł, a `next` kopia reszty listy, zapisana przed zmianą powiązania. W każdym kroku bieżący węzeł jest kierowany z powrotem na `prev`, a potem zarówno `prev`, jak i `head` przesuwają się do przodu:
```c
Node *next = head->next;
head->next = prev;
prev = head;
head = next;
```
Gdy `head` dotrze do `NULL`, każde powiązanie zostało odwrócone, a `prev` to nowa głowa.

# --instructions--

Ułóż linie w kolejności, aby zdefiniować `reverse`, która odwraca listę w miejscu i zwraca jej nową głowę, oraz `main`, który wypisuje odwróconą listę `1, 2, 3`. Zdefiniuj funkcję przed `main`

# --answers--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
```

```c
Node *reverse(Node *head) {
    Node *prev = NULL;
```

```c
    while (head != NULL) {
        Node *next = head->next;
```

```c
        head->next = prev;
```

```c
        prev = head;
```

```c
        head = next;
    }
```

```c
    return prev;
}
```

```c
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
```

```c
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
Node *reverse(Node *head) {
    Node *prev = NULL;
    while (head != NULL) {
        Node *next = head->next;
        head->next = prev;
        prev = head;
        head = next;
    }
    return prev;
}
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --output--

3
2
1
