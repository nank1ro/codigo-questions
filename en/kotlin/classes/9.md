---
language: kotlin
exerciseType: 2
---

# --instructions--

Complete the code to declare a `data class` named `Color` with `val` properties `red`, `green`, and `blue` of type `Int`.

# --seed--

```kotlin
[/] [/] Color(val red: Int, val green: Int, val blue: Int)
```

# --answers--

- data
- class
- object
- open
- abstract
- sealed

# --solutions--

```kotlin
data class Color(val red: Int, val green: Int, val blue: Int)
```
