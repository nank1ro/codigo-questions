---
language: kotlin
exerciseType: 2
---

# --instructions--

Complete the code to add a `companion object` inside `AppConfig` that holds a `val` property `version` equal to `"1.0"`.

# --seed--

```kotlin
class AppConfig {
    [/] [/] {
        val version = "1.0"
    }
}
```

# --answers--

- companion
- object
- class
- static
- init
- val

# --solutions--

```kotlin
class AppConfig {
    companion object {
        val version = "1.0"
    }
}
```
