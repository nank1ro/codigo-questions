---
language: kotlin
exerciseType: 2
---

# --instructions--

Complete the code to make `Cat` inherit from `Animal` and override the `speak()` method to return `"Meow"`.

# --seed--

```kotlin
open class Animal(val name: String) {
    open fun speak(): String = "..."
}

class Cat(name: String) [/] Animal(name) {
    [/] fun speak(): String = "Meow"
}
```

# --answers--

- :
- override
- extends
- open
- super
- implements

# --solutions--

```kotlin
open class Animal(val name: String) {
    open fun speak(): String = "..."
}

class Cat(name: String) : Animal(name) {
    override fun speak(): String = "Meow"
}
```
