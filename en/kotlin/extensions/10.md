---
language: kotlin
exerciseType: 1
---

# --description--

An extension can work on a family of types at once thanks to a **type parameter**: a placeholder for a type, declared between angle brackets right after `fun`, that Kotlin fills in at every call. This makes the extension **generic**:
```kotlin
fun <T> List<T>.second(): T {
    return this[1]
}
println(listOf(1, 2, 3).second())      // 2
println(listOf("a", "b").second())     // b
```
With `listOf(1, 2, 3)` the placeholder `T` is `Int`, with `listOf("a", "b")` it is `String`, so the same function returns the right type every time.

# --instructions--

Write a generic extension function `penultimate()` on `List<T>` that returns the second-to-last element of the list. The list always has at least two elements.

Example: `listOf(1, 2, 3).penultimate()` returns `2`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`listOf(1, 2, 3).penultimate()` must return `2`

```kotlin
    tryCatch(listOf(1, 2, 3).penultimate() == 2)
```

`listOf("a", "b").penultimate()` must return `"a"`

```kotlin
    tryCatch(listOf("a", "b").penultimate() == "a")
```

`listOf(true, false, true, false).penultimate()` must return `true`

```kotlin
    tryCatch(listOf(true, false, true, false).penultimate() == true)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun <T> List<T>.penultimate(): T {
    return this[size - 2]
}
```
