---
language: kotlin
exerciseType: 1
---

# --description--

An extension function with exactly **one** parameter can be marked `infix`. An infix function can be called without the dot and the parentheses, with the receiver on the left and the argument on the right, which reads almost like a sentence:
```kotlin
infix fun Int.percentOf(total: Int): Int = total * this / 100

println(20 percentOf 50)   // 10
println(20.percentOf(50))  // 10, the normal call still works
```
Kotlin uses this for some built-in functions too: `1 to "one"` builds a `Pair`, and `1 until 5` builds a range.

# --instructions--

Write an infix extension function `toThe(exponent: Int)` on `Int` that returns the receiver raised to the given power, so that `2 toThe 10` is `1024`. The exponent is never negative, and any number to the power `0` is `1`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`2 toThe 10` must return `1024`

```kotlin
    tryCatch((2 toThe 10) == 1024)
```

`3 toThe 3` must return `27`

```kotlin
    tryCatch((3 toThe 3) == 27)
```

`5 toThe 0` must return `1`

```kotlin
    tryCatch((5 toThe 0) == 1)
```

`7.toThe(1)` must return `7`

```kotlin
    tryCatch(7.toThe(1) == 7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
infix fun Int.toThe(exponent: Int): Int {
    var result = 1
    repeat(exponent) { result *= this }
    return result
}
```
