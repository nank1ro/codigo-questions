---
language: kotlin
exerciseType: 2
---

# --description--

A `finally` block can be added at the end. It runs **whatever happens**: after a successful `try`, after a `catch` has recovered, and even when the exception is not caught at all.

```kotlin
try {
    println("reading")
    println("abc".toInt())
} catch (e: NumberFormatException) {
    println("bad number")
} finally {
    println("closing")
}
```
```
reading
bad number
closing
```
That makes it the place for clean-up that must not be skipped, such as closing a file. A `try` needs at least a `catch` or a `finally`, but it may have both.

# --instructions--

Complete the code so that the bad conversion is caught and the clean-up line always runs.

# --seed--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } [/] (e: NumberFormatException) {
        println("bad number")
    } [/] {
        println("closing")
    }
}
```

# --answers--

- catch
- finally
- try
- throw
- else
- do

# --solutions--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } catch (e: NumberFormatException) {
        println("bad number")
    } finally {
        println("closing")
    }
}
```

# --output--

bad number
closing
