---
language: kotlin
exerciseType: 2
---

# --description--

In a chain, the type of `it` changes at every step: after `filter` on a `List<String>` you still have strings, but after `map { it.length }` you have a `List<Int>`, so the next lambda sees numbers.
```kotlin
val words = listOf("kiwi", "fig")
println(words.map { it.length }.filter { it > 3 }) // [4]
```
Every step returns a **new** list and never touches the previous one, so a chain can be split into named intermediate values without changing the result.

# --instructions--

Complete the code so that `result` holds the fruits starting with `a`, in uppercase, so that the program prints `[APPLE, AVOCADO]`

# --seed--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.[/] { it.startsWith("a") }.[/] { it.[/] }
println(result)
```

# --answers--

- filter
- map
- uppercase()
- forEach
- count
- lowercase()
- length

# --solutions--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.filter { it.startsWith("a") }.map { it.uppercase() }
println(result)
```

# --output--

[APPLE, AVOCADO]
