---
language: kotlin
exerciseType: 4
---

# --description--

The pieces of this topic are usually combined: an `enum class` whose entries carry their own properties models a fixed set of tags, while a `data class` carries the payload that goes with them.

```kotlin
enum class Speed(val surcharge: Int) {
    STANDARD(0),
    EXPRESS(15)
}

data class Order(val total: Int, val speed: Speed)

val order = Order(100, Speed.EXPRESS)
println(order.total + order.speed.surcharge) // 115
```

# --instructions--

Sort the items to print the total cost of the order. Declare the enum first, then the data class, then `main`.

# --answers--

```kotlin
enum class Shipping(val surcharge: Int) { STANDARD(0), EXPRESS(15) }
```

```kotlin
data class Order(val total: Int, val shipping: Shipping)
```

```kotlin
fun main() {
```

```kotlin
    val order = Order(100, Shipping.EXPRESS)
```

```kotlin
    println(order.total + order.shipping.surcharge)
```

```kotlin
}
```

# --solutions--

```kotlin
enum class Shipping(val surcharge: Int) { STANDARD(0), EXPRESS(15) }
data class Order(val total: Int, val shipping: Shipping)
fun main() {
    val order = Order(100, Shipping.EXPRESS)
    println(order.total + order.shipping.surcharge)
}
```

# --output--

115
