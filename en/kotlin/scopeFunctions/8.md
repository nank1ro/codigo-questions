---
language: kotlin
exerciseType: 1
---

# --description--

When the block needs the object as an **argument** of something else, `also` reads better than `apply`: `it` can be handed over directly, while `this` would have to be spelled out.
```kotlin
val names = mutableListOf<String>()
val user = "ada".also { names.add(it) }
println(user)  // ada
println(names) // [ada]
```
The value of the expression is still `"ada"`: `also` only watches it go by.

# --instructions--

Declare a top-level `val log = mutableListOf<String>()`.

Then write a function `record(word: String): String` that returns `word` in uppercase and, with `also`, adds that same uppercase value to `log` before returning it.

Example: after `record("hi")` the function returns `HI` and `log` contains `HI`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`log` must start empty

```kotlin
    tryCatch(log.isEmpty())
```

`record("hi")` must return `HI`

```kotlin
    tryCatch(record("hi") == "HI")
```

After the first call `log` must contain only `HI`

```kotlin
    tryCatch(log == listOf("HI"))
```

A second call must append to `log` without losing the first value

```kotlin
    record("bye")
    tryCatch(log == listOf("HI", "BYE"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
val log = mutableListOf<String>()

fun record(word: String): String {
    return word.uppercase().also { log.add(it) }
}
```
