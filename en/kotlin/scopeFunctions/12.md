---
language: kotlin
exerciseType: 4
---

# --instructions--

Can you order these items so that the program prints the list after the new element has been added, and then its size?

# --answers--

```kotlin
val words = mutableListOf("fig")
```

```kotlin
val count = words
```

```kotlin
    .apply { add("kiwi") }
```

```kotlin
    .also { println("now: $it") }
```

```kotlin
    .size
```

```kotlin
println("size: $count")
```

# --solutions--

```kotlin
val words = mutableListOf("fig")
val count = words
    .apply { add("kiwi") }
    .also { println("now: $it") }
    .size
println("size: $count")
```

# --output--

now: [fig, kiwi]
size: 2
