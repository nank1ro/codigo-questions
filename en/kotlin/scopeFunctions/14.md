---
language: kotlin
exerciseType: 2
---

# --instructions--

Complete the code so that the description of the dog is built inside a `with` block and the program prints it.

# --seed--

```kotlin
class Dog(val name: String, val age: Int)

fun main() {
    val dog = Dog("Rex", 4)
    val info = [/](dog) {
        name + " is " + [/]
    }
    println(info)
}
```

# --answers--

- with
- age
- run
- also
- apply
- name

# --solutions--

```kotlin
class Dog(val name: String, val age: Int)

fun main() {
    val dog = Dog("Rex", 4)
    val info = with(dog) {
        name + " is " + age
    }
    println(info)
}
```

# --output--

Rex is 4
