---
language: kotlin
exerciseType: 1
---

# --description--

Inside a `let` block you are not forced to call the object `it`: you can give the lambda parameter a name, which keeps the code readable when blocks are nested or when `it` would say nothing useful.
```kotlin
val price: Int? = 12
println(price?.let { amount -> "$amount EUR" }) // 12 EUR
```
The same naming works for every scope function that uses `it`, so for `let` and `also`.

# --instructions--

Write a function `shout(word: String?): String` that returns `word` in uppercase followed by `!` when it is not `null`, and `silence` when it is `null`. Use `?.let` and `?:`.

Example: `shout("hi")` returns `HI!`, `shout(null)` returns `silence`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`shout("hi")` must return `HI!`

```kotlin
    tryCatch(shout("hi") == "HI!")
```

`shout("Bye")` must return `BYE!`

```kotlin
    tryCatch(shout("Bye") == "BYE!")
```

`shout(null)` must return `silence`

```kotlin
    tryCatch(shout(null) == "silence")
```

An empty string is not `null`, so `shout("")` must return `!`

```kotlin
    tryCatch(shout("") == "!")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun shout(word: String?): String {
    return word?.let { it.uppercase() + "!" } ?: "silence"
}
```
