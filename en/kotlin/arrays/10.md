---
language: kotlin
exerciseType: 2
---

# --description--

Arrays support the same transformation functions as lists. `filter` keeps the elements matching a condition and `map` transforms every element.
Both return a new `List`, not an array:
```kotlin
val nums = intArrayOf(1, 2, 3)
val odds = nums.filter { it % 2 == 1 } // List [1, 3]
val tripled = nums.map { it * 3 }      // List [3, 6, 9]
println(tripled)                       // [3, 6, 9]
```
Since the result is a list, printing it directly shows its elements.

# --instructions--

Complete the code in order to keep only the even numbers, then double each of them

# --seed--

```kotlin
val nums = intArrayOf(1, 2, 3, 4, 5, 6)
val evens = nums.[/] { it % 2 == 0 }
val doubled = evens.[/] { it * 2 }
println(doubled)
```

# --answers--

- filter
- map
- forEach
- find
- where

# --solutions--

```kotlin
val nums = intArrayOf(1, 2, 3, 4, 5, 6)
val evens = nums.filter { it % 2 == 0 }
val doubled = evens.map { it * 2 }
println(doubled)
```

# --output--

[4, 8, 12]
