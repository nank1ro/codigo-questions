---
language: kotlin
exerciseType: 4
---

# --instructions--

Can you order these lines to build the squares of `0`, `1`, `2` and `3`, turn them into labels and print each label with its index?

# --answers--

```kotlin
val squares = Array(4) { it * it }
```

```kotlin
val labels = squares.map { "sq=$it" }
```

```kotlin
for ((i, label) in labels.withIndex()) {
```

```kotlin
    println("$i $label")
```

```kotlin
}
```

# --solutions--

```kotlin
val squares = Array(4) { it * it }
val labels = squares.map { "sq=$it" }
for ((i, label) in labels.withIndex()) {
    println("$i $label")
}
```

# --output--

0 sq=0
1 sq=1
2 sq=4
3 sq=9
