---
language: kotlin
exerciseType: 2
---

# --description--

Printing an array directly does not show its elements, it prints something like `[Ljava.lang.String;@1b6d3586`.
Use `joinToString` to build a readable string, optionally with a custom separator (the default is `", "`), or `contentToString` to get the elements between square brackets:
```kotlin
val nums = intArrayOf(1, 2, 3)
println(nums.joinToString())      // 1, 2, 3
println(nums.joinToString("-"))   // 1-2-3
println(nums.contentToString())   // [1, 2, 3]
```

# --instructions--

Complete the code in order to print the fruits separated by `" - "`, then the whole array between square brackets

# --seed--

```kotlin
val fruits = arrayOf("apple", "kiwi", "plum")
println(fruits.[/]([/]))
println(fruits.[/]())
```

# --answers--

- joinToString
- contentToString
- toString
- join
- " - "
- "-"

# --solutions--

```kotlin
val fruits = arrayOf("apple", "kiwi", "plum")
println(fruits.joinToString(" - "))
println(fruits.contentToString())
```

# --output--

apple - kiwi - plum
[apple, kiwi, plum]
