---
language: kotlin
exerciseType: 2
---

# --description--

An **array** stores a fixed number of values of the same type under a single variable name.
You create one with `arrayOf`, read an element with square brackets and an **index** starting from `0`, and get the number of elements with `size`:
```kotlin
val colors = arrayOf("red", "green", "blue")
println(colors[0])   // red
println(colors.size) // 3
```
The last element is at index `size - 1`.

# --instructions--

Complete the code in order to create the array with `arrayOf`, print its number of elements, then its last element

# --seed--

```kotlin
val numbers = [/](3, 5, 7)
println(numbers.[/])
println(numbers[[/]])
```

# --answers--

- arrayOf
- Array
- size
- length
- count
- 2
- 3

# --solutions--

```kotlin
val numbers = arrayOf(3, 5, 7)
println(numbers.size)
println(numbers[2])
```

# --output--

3
7
