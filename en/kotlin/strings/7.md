---
language: kotlin
exerciseType: 2
---

# --description--

`split` breaks a string into a `List` of pieces around a separator, while `joinToString` does the opposite: it glues the elements of a collection into one string with the separator you choose:
```kotlin
val csv = "a,b,c"
val parts = csv.split(",")
println(parts)                  // [a, b, c]
println(parts.joinToString("/")) // a/b/c
```

# --instructions--

Complete the code in order to split `csv` on the comma and print the colors separated by `" - "`

# --seed--

```kotlin
val csv = "red,green,blue"
val colors = csv.[/](",")
println(colors.[/](" - "))
```

# --answers--

- split
- joinToString
- join
- separate
- substring

# --solutions--

```kotlin
val csv = "red,green,blue"
val colors = csv.split(",")
println(colors.joinToString(" - "))
```

# --output--

red - green - blue
