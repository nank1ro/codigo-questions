---
language: kotlin
exerciseType: 2
---

# --description--

A `String` is a sequence of characters written between double quotes.
The `length` property tells how many characters a string contains, spaces included:
```kotlin
val greeting = "Hello world"
println(greeting.length) // 11
println("".length)       // 0
```

# --instructions--

Complete the code in order to print the number of characters in `word`

# --seed--

```kotlin
val word = "Kotlin"
println(word.[/])
```

# --answers--

- length
- size
- len
- length()
- count

# --solutions--

```kotlin
val word = "Kotlin"
println(word.length)
```

# --output--

6
