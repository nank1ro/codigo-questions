---
language: kotlin
exerciseType: 2
---

# --description--

`replace(old, new)` returns a copy of the string where **every** occurrence of `old` is replaced by `new`:
```kotlin
val text = "a-b-c"
println(text.replace("-", "+")) // a+b+c
println(text)                   // a-b-c
```

# --instructions--

Complete the code in order to print `I like dogs`

# --seed--

```kotlin
val text = "I like cats"
println(text.[/]("cats", [/]))
```

# --answers--

- replace
- replaceAll
- substring
- "dogs"
- "dog"
- dogs

# --solutions--

```kotlin
val text = "I like cats"
println(text.replace("cats", "dogs"))
```

# --output--

I like dogs
