---
language: kotlin
exerciseType: 2
---

# --description--

Collections can hold nullable elements too: a `List<Int?>` may contain `null` entries, while a `List<Int>` never does.
`filterNotNull()` returns a new list with the `null` entries removed, and its element type becomes non-null, so you can use the elements freely:
```kotlin
val scores: List<Int?> = listOf(10, null, 20)
val valid = scores.filterNotNull() // List<Int> [10, 20]
println(valid.sum())               // 30
```

# --instructions--

Complete the code in order to declare a list that may contain `null` numbers, then remove the `null` entries and print the remaining values and their sum

# --seed--

```kotlin
val values: List<[/]> = listOf(3, null, 5, null)
val present = values.[/]()
println(present)
println(present.sum())
```

# --answers--

- Int?
- Int
- filterNotNull
- filter
- mapNotNull
- requireNotNull

# --solutions--

```kotlin
val values: List<Int?> = listOf(3, null, 5, null)
val present = values.filterNotNull()
println(present)
println(present.sum())
```

# --output--

[3, 5]
8
