---
language: kotlin
exerciseType: 1
---

# --description--

When a `null` means the caller made a mistake, fail early with `requireNotNull`.
It returns the value as non-null when it is present, and throws an `IllegalArgumentException` when it is `null`, with an optional message:
```kotlin
fun greet(name: String?): String {
    val safeName = requireNotNull(name) { "name is required" }
    return "Hello, $safeName"
}
greet(null) // IllegalArgumentException: name is required
```
After the call the compiler also smart casts `name` itself to `String`, so `name.length` is allowed from that line on.
Unlike `!!`, the failure carries a clear message and states that the *argument* was wrong.

# --instructions--

Write a function `safeUpper(text: String?): String` that returns `text` in upper case. When `text` is `null` it must throw an `IllegalArgumentException` with the message `text is required`, using `requireNotNull`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`safeUpper("abc")` must return `"ABC"`

```kotlin
    tryCatch(safeUpper("abc") == "ABC")
```

`safeUpper("Kotlin")` must return `"KOTLIN"`

```kotlin
    tryCatch(safeUpper("Kotlin") == "KOTLIN")
```

`safeUpper(null)` must throw an `IllegalArgumentException`

```kotlin
    var threw = false
    try {
        safeUpper(null)
    } catch (e: IllegalArgumentException) {
        threw = true
    }
    tryCatch(threw)
```

The exception message must be `text is required`

```kotlin
    var message = ""
    try {
        safeUpper(null)
    } catch (e: IllegalArgumentException) {
        message = e.message ?: ""
    }
    tryCatch(message == "text is required")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun safeUpper(text: String?): String {
    val value = requireNotNull(text) { "text is required" }
    return value.uppercase()
}
```
