---
language: kotlin
exerciseType: 4
---

# --description--

An extension function can be declared on a **nullable receiver**, so it can be called even on a `null` value. Inside, `this` is nullable and must be checked:
```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val none: String? = null
println(none.orDash()) // -
```
Note that no `?.` is needed at the call site: the function itself handles the `null` case.
The standard library uses this trick in `isNullOrEmpty()` and `orEmpty()`, which are safe to call on any `String?`.

# --instructions--

Can you order these lines to define the extension function `orDash` and print `-` for a missing name, then `kotlin` for a present one? Define the function first

# --answers--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
```

```kotlin
val a: String? = null
val b: String? = "kotlin"
```

```kotlin
println(a.orDash())
```

```kotlin
println(b.orDash())
```

# --solutions--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val a: String? = null
val b: String? = "kotlin"
println(a.orDash())
println(b.orDash())
```

# --output--

-
kotlin
