---
language: kotlin
exerciseType: 1
---

# --description--

The right side of `?:` can be any expression, including `return`. This gives a compact way to bail out of a function as soon as a value is missing:
```kotlin
fun firstUpper(text: String?): Char? {
    val first = text?.firstOrNull() ?: return null
    return first.uppercaseChar() // first is a Char here
}
```
Every tool you have seen combines well: nullable parameters and return types describe *where* a value may be missing, and `?.`, `?:`, `let`, smart casts and `toIntOrNull` handle it without ever crashing.

# --instructions--

Write a function `divide(a: String?, b: String?): Int?` that converts both strings into whole numbers and returns `a / b`. It must return `null` when either string is `null`, when either is not a whole number, or when `b` is `0`.

Example: `divide("10", "2")` returns `5`, `divide("10", "x")` returns `null`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`divide("10", "2")` must return `5`

```kotlin
    tryCatch(divide("10", "2") == 5)
```

`divide("10", "x")` must return `null`

```kotlin
    tryCatch(divide("10", "x") == null)
```

`divide(null, "2")` must return `null`

```kotlin
    tryCatch(divide(null, "2") == null)
```

`divide("9", null)` must return `null`

```kotlin
    tryCatch(divide("9", null) == null)
```

`divide("7", "0")` must return `null`

```kotlin
    tryCatch(divide("7", "0") == null)
```

`divide("-9", "3")` must return `-3`

```kotlin
    tryCatch(divide("-9", "3") == -3)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun divide(a: String?, b: String?): Int? {
    val x = a?.toIntOrNull() ?: return null
    val y = b?.toIntOrNull() ?: return null
    if (y == 0) return null
    return x / y
}
```
