---
language: kotlin
exerciseType: 4
---

# --instructions--

Can you order these lines so that the program prints `{Ben=15}`?

# --answers--

```kotlin
val scores = mutableMapOf("Ann" to 10)
```

```kotlin
scores["Ben"] = scores.getOrDefault("Ann", 0) + 5
```

```kotlin
scores.remove("Ann")
```

```kotlin
println(scores)
```

# --solutions--

```kotlin
val scores = mutableMapOf("Ann" to 10)
scores["Ben"] = scores.getOrDefault("Ann", 0) + 5
scores.remove("Ann")
println(scores)
```

# --output--

{Ben=15}
