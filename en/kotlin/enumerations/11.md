---
language: kotlin
exerciseType: 4
---

# --instructions--

Sort the items to print `Slow down`. Declare the enum first, then the `action` function, then `main`, and keep the `when` branches in the same order as the enum entries.

# --answers--

```kotlin
enum class Light {
    RED, YELLOW, GREEN
}
```

```kotlin
fun action(light: Light): String = when (light) {
```

```kotlin
    Light.RED -> "Stop"
```

```kotlin
    Light.YELLOW -> "Slow down"
```

```kotlin
    Light.GREEN -> "Go"
```

```kotlin
}
```

```kotlin
fun main() {
    println(action(Light.YELLOW))
}
```

# --solutions--

```kotlin
enum class Light {
    RED, YELLOW, GREEN
}
fun action(light: Light): String = when (light) {
    Light.RED -> "Stop"
    Light.YELLOW -> "Slow down"
    Light.GREEN -> "Go"
}
fun main() {
    println(action(Light.YELLOW))
}
```

# --output--

Slow down
