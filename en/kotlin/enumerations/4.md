---
language: kotlin
exerciseType: 2
---

# --description--

An enum class can have a **constructor**, just like a regular class. Each entry then passes its own arguments, and the values are stored in properties:
```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
println(Planet.MARS.moons) // 2
```
Constructor properties are usually declared with `val`, since an entry's data is not meant to change.

# --instructions--

Complete the code so that every `Coin` stores its value in cents, then print the value of a `PENNY` (which is worth `1` cent)

# --seed--

```kotlin
enum class Coin([/] cents: Int) {
    PENNY([/]),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.[/])
}
```

# --answers--

- val
- fun
- const
- 1
- "1"
- cents
- name
- ordinal

# --solutions--

```kotlin
enum class Coin(val cents: Int) {
    PENNY(1),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.cents)
}
```

# --output--

1
