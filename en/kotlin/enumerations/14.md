---
language: kotlin
exerciseType: 4
---

# --instructions--

Sort the items to declare the enum `Planet` and print, for each entry, its name and its number of moons. Declare the enum first, then `main`. The program must print:

```
MERCURY: 0
EARTH: 1
MARS: 2
```

# --answers--

```kotlin
enum class Planet(val moons: Int) {
```

```kotlin
    MERCURY(0),
```

```kotlin
    EARTH(1),
```

```kotlin
    MARS(2)
```

```kotlin
}
```

```kotlin
fun main() {
    for (planet in Planet.entries) {
```

```kotlin
        println("${planet.name}: ${planet.moons}")
```

```kotlin
    }
}
```

# --solutions--

```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
fun main() {
    for (planet in Planet.entries) {
        println("${planet.name}: ${planet.moons}")
    }
}
```

# --output--

MERCURY: 0
EARTH: 1
MARS: 2
