---
language: kotlin
exerciseType: 1
---

# --description--

To go from a `String` back to an entry, use the `valueOf` function. It looks up the entry whose `name` matches exactly:
```kotlin
val direction = Direction.valueOf("EAST")
println(direction == Direction.EAST) // true
```
The match is case-sensitive: `Direction.valueOf("east")` throws an `IllegalArgumentException` because no entry has that name.

# --instructions--

Declare an enum class `Status` with the entries `ACTIVE` and `INACTIVE`. Then write a function `statusOf(text: String)` that returns the `Status` matching `text`, regardless of its case (so `"active"`, `"Active"` and `"ACTIVE"` all give `Status.ACTIVE`).

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`statusOf("ACTIVE")` must return `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("ACTIVE") == Status.ACTIVE)
```

`statusOf("inactive")` must return `Status.INACTIVE`

```kotlin
    tryCatch(statusOf("inactive") == Status.INACTIVE)
```

`statusOf("Active")` must return `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("Active") == Status.ACTIVE)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
enum class Status {
    ACTIVE, INACTIVE
}

fun statusOf(text: String): Status {
    return Status.valueOf(text.uppercase())
}
```
