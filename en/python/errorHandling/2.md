---
language: python
exerciseType: 1
---

# --description--

The `try` block stops at the **first** statement that raises; the lines after it are skipped and control moves to the `except` block. Nothing in the `try` block is undone, so keep it as short as possible:
```python
def half(n):
    try:
        return 10 / n
    except ZeroDivisionError:
        return "undefined"

print(half(2))  # 5.0
print(half(0))  # undefined
```
A `return` inside `except` works like any other `return`, which makes `try`/`except` a natural way to give back a fallback value instead of crashing.

# --instructions--

Write a function `safe_divide(a, b)` that returns `a / b`, or the string `"undefined"` when `b` is `0`, catching `ZeroDivisionError`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_divide(9, 3)` must return `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_divide(9, 3), 3.0, "--err-t1--")
```

`safe_divide(1, 0)` must return `"undefined"`

```python
    def test_2(self):
        self.assertEqual(safe_divide(1, 0), "undefined", "--err-t2--")
```

`safe_divide(-5, 2)` must return `-2.5`

```python
    def test_3(self):
        self.assertEqual(safe_divide(-5, 2), -2.5, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "undefined"
```
