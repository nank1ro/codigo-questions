---
language: python
exerciseType: 1
---

# --description--

An exception is an object, and `as` binds it to a name so the handler can look at it:
```python
try:
    int("abc")
except ValueError as e:
    print(e)                   # invalid literal for int() with base 10: 'abc'
    print(type(e).__name__)    # ValueError
```
`str(e)` — which is what `print(e)` and an f-slot use — gives the message the exception was built with, and `type(e).__name__` gives the class name as text. The name bound by `as` only exists inside the `except` block; Python deletes it when the block ends.

# --instructions--

Write a function `error_label(text)` that returns `int(text)`, and when that raises `ValueError` returns a string made of the exception class name, `": "`, and the exception message, for example `"ValueError: ..."`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`error_label("8")` must return `8`

```python
    def test_1(self):
        self.assertEqual(error_label("8"), 8, "--err-t1--")
```

`error_label("abc")` must start with the class name

```python
    def test_2(self):
        self.assertTrue(error_label("abc").startswith("ValueError: "), "--err-t2--")
```

`error_label("abc")` must also contain the message, which mentions `abc`

```python
    def test_3(self):
        self.assertIn("abc", error_label("abc")[12:], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def error_label(text):
    try:
        return int(text)
    except ValueError as e:
        return f"{type(e).__name__}: {e}"
```
