---
language: python
exerciseType: 1
---

# --description--

When several failures deserve the **same** recovery, listing them as a tuple in one clause is shorter than repeating the block:
```python
try:
    value = int(text) / divisor
except (ValueError, ZeroDivisionError):
    value = 0
```
The brackets are required: `except ValueError, ZeroDivisionError:` is a syntax error in Python 3. A tuple is still an explicit list of types.

# --instructions--

Write a function `safe_ratio(a, b)` that returns `a / b`, and returns `None` when the division raises `ZeroDivisionError` or `TypeError`, using a single `except` clause with a tuple of both types

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_ratio(9, 3)` must return `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_ratio(9, 3), 3.0, "--err-t1--")
```

`safe_ratio(1, 0)` must return `None`

```python
    def test_2(self):
        self.assertIsNone(safe_ratio(1, 0), "--err-t2--")
```

`safe_ratio(1, "x")` must return `None`

```python
    def test_3(self):
        self.assertIsNone(safe_ratio(1, "x"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_ratio(a, b):
    try:
        return a / b
    except (ZeroDivisionError, TypeError):
        return None
```
