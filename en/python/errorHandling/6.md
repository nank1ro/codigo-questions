---
language: python
exerciseType: 2
---

# --description--

One `try` block can be followed by **several** `except` clauses, each handling a different failure with a different recovery. Python compares the raised exception against them from top to bottom and runs the **first** one that matches; the others are skipped:
```python
try:
    value = 100 / int(text)
except ValueError:
    print("not a number")
except ZeroDivisionError:
    print("cannot divide by zero")
```
Because the first match wins, order matters when the types are related: a clause for a general type placed above a clause for a more specific one would always win, leaving the specific clause unreachable.

# --instructions--

Complete the code so that unreadable text and a zero divisor are reported separately, producing the output `2.0 not a number cannot divide by zero`

# --seed--

```python
def describe(text):
    try:
        return 10 / int(text)
    except [/]:
        return "not a number"
    except [/]:
        return "cannot divide by zero"

print(describe("5"), describe("abc"), describe("0"))
```

# --answers--

- ValueError
- ZeroDivisionError
- TypeError
- KeyError

# --solutions--

```python
def describe(text):
    try:
        return 10 / int(text)
    except ValueError:
        return "not a number"
    except ZeroDivisionError:
        return "cannot divide by zero"

print(describe("5"), describe("abc"), describe("0"))
```

# --output--

2.0 not a number cannot divide by zero
