---
language: python
exerciseType: 1
---

# --description--

A `finally` block runs **whatever happens**: after a clean `try` block, after an `except` block, even while an exception nobody caught is travelling outwards, and even when the `try` or `except` block executes a `return`:
```python
def read(text):
    try:
        return int(text)
    except ValueError:
        return 0
    finally:
        print("done")
```
Both paths print `done` before the value leaves the function. That guarantee is what `finally` is for: closing a file, releasing a lock, restoring a setting. The full shape is `try` / `except` / `else` / `finally`; a `try` needs at least one `except` or a `finally`, and `else` only works alongside an `except`.

# --instructions--

Write a function `record(text)` that starts from an empty list, appends `int(text)` to it, appends the string `"bad"` instead when `int(text)` raises `ValueError`, always appends `"done"` in a `finally` block, and returns the list

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`record("5")` must return `[5, "done"]`

```python
    def test_1(self):
        self.assertEqual(record("5"), [5, "done"], "--err-t1--")
```

`record("x")` must return `["bad", "done"]`

```python
    def test_2(self):
        self.assertEqual(record("x"), ["bad", "done"], "--err-t2--")
```

each call must start from a fresh list

```python
    def test_3(self):
        record("1")
        self.assertEqual(record("2"), [2, "done"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def record(text):
    log = []
    try:
        log.append(int(text))
    except ValueError:
        log.append("bad")
    finally:
        log.append("done")
    return log
```
