---
language: python
exerciseType: 2
---

# --description--

Conversions can be combined. `int("3.7")` fails, but `float("3.7")` works, and `int()` of a `float` drops the decimal part:
```python
number = float("3.7")   # 3.7
print(int(number))      # 3
```
`bool()` follows the truthiness rules: `bool("")` is `False`, and note that `bool("False")` is `True`, because it is a non-empty string.

# --instructions--

Complete the code to convert `text` into a number, print its double, then print the number converted to an `int` and the boolean value of an empty string, so that it prints `7.4`, `3` and `False`

# --seed--

```python
text = "3.7"
number = [/](text)
print(number * 2)
print([/](number))
print([/](""))
```

# --answers--

- float
- int
- bool
- str
- len

# --solutions--

```python
text = "3.7"
number = float(text)
print(number * 2)
print(int(number))
print(bool(""))
```

# --output--

7.4
3
False
