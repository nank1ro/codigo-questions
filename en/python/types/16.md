---
language: python
exerciseType: 1
---

# --description--

Text coming from the outside is always a `str`, and it is up to your program to figure out which type it really holds.
A common approach is to try the **strictest** conversion first, and fall back to the next one when it raises a `ValueError`:
```python
try:
    value = int(text)
except ValueError:
    value = float(text)
```
Nesting a second `try` inside the `except` block lets you fall back once more, for example to keep the text as it is.

# --instructions--

Write a function `parse_value` that takes a string and returns it as an `int` if it is a whole number, as a `float` if it is a decimal number, and unchanged otherwise

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must catch `ValueError` with `except`

```json
{
  "regex": "except\\s+ValueError",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_value("42")` must return the `int` `42`

```python
    def test_1(self):
        self.assertEqual(parse_value("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_value("42"), int, "--err-t1--")
        self.assertEqual(parse_value("-8"), -8, "--err-t1--")
```

`parse_value("3.5")` must return the `float` `3.5`

```python
    def test_2(self):
        self.assertEqual(parse_value("3.5"), 3.5, "--err-t2--")
        self.assertIsInstance(parse_value("3.5"), float, "--err-t2--")
```

`parse_value("abc")` must return `"abc"`

```python
    def test_3(self):
        self.assertEqual(parse_value("abc"), "abc", "--err-t3--")
        self.assertEqual(parse_value(""), "", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_value(text):
    try:
        return int(text)
    except ValueError:
        try:
            return float(text)
        except ValueError:
            return text
```
