---
language: python
exerciseType: 4
---

# --description--

Values do not change type on their own: to turn a value into another type you call the type name like a function. This is called **conversion** (or *casting*):
```python
print(int("42"))      # 42
print(float("3.5"))   # 3.5
print(str(7))         # '7'
print(int(3.9))       # 3
```
`int()` and `float()` read numbers written as text, which is what you get from user input or from files. `str()` turns anything into text, so it can be joined with `+` to other strings.
Note that `int(3.9)` does not round: it drops the decimal part.

# --instructions--

Sort the items to convert `age` into a number, add `1` to it and print the result joined to the text `Next year: `

# --answers--

```python
age = "30"
```

```python
years = int(age)
```

```python
next_year = years + 1
```

```python
print("Next year: " + str(next_year))
```

# --solutions--

```python
age = "30"
years = int(age)
next_year = years + 1
print("Next year: " + str(next_year))
```

# --output--

Next year: 31
