---
language: python
exerciseType: 2
---

# --description--

`bool` is a **subclass** of `int`: `True` behaves like `1` and `False` like `0` wherever a number is expected:
```python
print(True + 1)     # 2
print(True == 1)    # True
print(False * 10)   # 0
```
`sum()` adds up the items of a list, so summing a list of booleans **counts** how many are `True`:
```python
answers = [True, False, True]
print(sum(answers))  # 2
```
Because of the subclass relation, `isinstance(True, int)` returns `True`, while `type(True)` is still `bool`.

# --instructions--

Complete the code so that it counts the correct answers, adds `1` to `True` and checks that `True` is an instance of `int`, printing `3`, `2` and `True`

# --seed--

```python
answers = [True, False, True, True]
correct = [/](answers)
print(correct)
print(True [/] 1)
print(isinstance(True, [/]))
```

# --answers--

- sum
- +
- int
- len
- ==
- str

# --solutions--

```python
answers = [True, False, True, True]
correct = sum(answers)
print(correct)
print(True + 1)
print(isinstance(True, int))
```

# --output--

3
2
True
