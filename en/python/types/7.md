---
language: python
exerciseType: 3
---

# --description--

Every value can be interpreted as a boolean. `bool()` converts a value to `True` or `False`, and the same rule is applied when a value is used directly in an `if`.
The values that count as **false** are the "empty" ones:
- the number `0` (and `0.0`)
- the empty string `""`
- empty collections such as `[]`, `{}`, `()` and `set()`
- `None`

Any non-empty value is **true**, including negative numbers and strings that only look empty, like `"0"` or `" "`:
```python
print(bool(0))     # False
print(bool(-1))    # True
print(bool(""))    # False
print(bool("0"))   # True
```
This is why `if name:` is a common way to check that a string is not empty.

# --instructions--

What does this code print?
```python
print(bool(0), bool("0"), bool([]), bool(None))
```

# --answers--

- `False True False False`, because `"0"` is a non-empty string
- `False False False False`, because all of them represent zero or nothing
- `True True False False`, because numbers are always true
- A `TypeError`, because `None` cannot be converted to a boolean

# --solutions--

- `False True False False`, because `"0"` is a non-empty string
