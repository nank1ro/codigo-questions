---
language: python
exerciseType: 1
---

# --description--

Unlike many languages, Python integers have **no maximum size**: an `int` grows to hold as many digits as needed, so big calculations stay exact:
```python
print(3 ** 100)  # 515377520732011331036461129765621272702107522001
```
A `float`, on the other hand, keeps only about 15 significant digits, so the same power as a float loses precision:
```python
print(3.0 ** 100)  # 5.153775207320113e+47
```
Since `str()` works on any `int`, a quick way to count the digits of a number is to measure the length of its text.

# --instructions--

Store in `big` the value of `2` to the power of `200`, then write a function `digits` that takes a positive `int` and returns how many digits it has

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use `str()`

```json
{
  "regex": "str\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`big` must be `2 ** 200`

```python
    def test_1(self):
        self.assertEqual(big, 2 ** 200, "--err-t1--")
        self.assertIsInstance(big, int, "--err-t1--")
```

`digits(big)` must return `61`

```python
    def test_2(self):
        self.assertEqual(digits(big), 61, "--err-t2--")
```

`digits(7)` must return `1` and `digits(10 ** 50)` must return `51`

```python
    def test_3(self):
        self.assertEqual(digits(7), 1, "--err-t3--")
        self.assertEqual(digits(10 ** 50), 51, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
big = 2 ** 200

def digits(number):
    return len(str(number))
```
