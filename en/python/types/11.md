---
language: python
exerciseType: 1
---

# --description--

Formatting only changes how a number is displayed. To get a rounded **value** use the built-in `round()`:
```python
print(round(3.14159, 2))  # 3.14
print(round(2.71828, 1))  # 2.7
```
With a single argument `round()` rounds to the nearest whole number and returns an `int`; with a number of decimals it returns a `float`:
```python
print(round(3.7))     # 4
print(round(3.7, 0))  # 4.0
```
Note that values exactly halfway between two numbers round to the **even** one: `round(2.5)` is `2` and `round(3.5)` is `4`.

# --instructions--

Write a function `average` that takes a list of numbers and returns their average rounded to two decimals

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use `round()`

```json
{
  "regex": "round\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`average([1, 2, 3, 4])` must return `2.5`

```python
    def test_1(self):
        self.assertEqual(average([1, 2, 3, 4]), 2.5, "--err-t1--")
```

`average([1, 2, 2])` must return `1.67`

```python
    def test_2(self):
        self.assertEqual(average([1, 2, 2]), 1.67, "--err-t2--")
        self.assertEqual(average([1, 1, 1, 2]), 1.25, "--err-t2--")
```

`average([10])` must return the float `10.0`

```python
    def test_3(self):
        self.assertIsInstance(average([10]), float, "--err-t3--")
        self.assertEqual(average([10]), 10.0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def average(numbers):
    return round(sum(numbers) / len(numbers), 2)
```
