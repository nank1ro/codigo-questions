---
language: python
exerciseType: 1
---

# --description--

A conversion can fail. `int("abc")` cannot produce a number, so it raises a `ValueError` and the program stops:
```python
int("abc")   # ValueError: invalid literal for int() with base 10: 'abc'
int("3.5")   # ValueError as well: "3.5" is not a whole number
```
To keep the program running you can catch the error with `try` / `except`: the code in the `try` block runs, and if it raises the named error the `except` block runs instead:
```python
try:
    number = int(text)
except ValueError:
    number = 0
```
When the conversion succeeds the `except` block is skipped.

# --instructions--

Write a function `to_int` that takes a string and returns it converted to an `int`, or `None` when the conversion raises a `ValueError`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must catch `ValueError` with `except`

```json
{
  "regex": "except\\s+ValueError",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`to_int("42")` must return `42`

```python
    def test_1(self):
        self.assertEqual(to_int("42"), 42, "--err-t1--")
        self.assertEqual(to_int("-7"), -7, "--err-t1--")
```

`to_int("abc")` must return `None`

```python
    def test_2(self):
        self.assertIsNone(to_int("abc"), "--err-t2--")
```

`to_int("3.5")` must return `None`

```python
    def test_3(self):
        self.assertIsNone(to_int("3.5"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def to_int(text):
    try:
        return int(text)
    except ValueError:
        return None
```
