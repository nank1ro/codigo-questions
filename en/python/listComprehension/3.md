---
language: python
exerciseType: 2
---

# --description--

A comprehension can also **filter** the items.
Add an `if` condition after the `for` part: only the items for which the condition is `True` end up in the new list:
```python
nums = [5, 12, 8, 20]
big = [n for n in nums if n > 10]
print(big)  # [12, 20]
```
This is the same as a loop with an `if` inside, and it replaces `filter()` with a lambda in a more readable way.

# --instructions--

Complete the comprehension to keep only the even numbers, so that the program prints `[2, 4]`

# --seed--

```python
nums = [1, 2, 3, 4]
evens = [n for n in nums [/] n % 2 [/] 0]
print(evens)
```

# --answers--

- if
- ==
- !=
- when
- while

# --solutions--

```python
nums = [1, 2, 3, 4]
evens = [n for n in nums if n % 2 == 0]
print(evens)
```

# --output--

[2, 4]
