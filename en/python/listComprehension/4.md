---
language: python
exerciseType: 1
---

# --description--

The filter condition can be any expression that gives a boolean, including function calls like `len()`:
```python
words = ["a", "bee", "cat"]
short = [w for w in words if len(w) < 3]
print(short)  # ['a']
```

# --instructions--

Store in the variable `long_words` the list of the words in `words` that have more than 4 characters, using a list comprehension

# --seed--

```python
words = ["sun", "planet", "moon", "star", "galaxy"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use a list comprehension with an `if` filter

```json
{
  "regex": "\\[.*for .* in .* if .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`long_words` must be equal to `['planet', 'galaxy']`

```python
    def test_1(self):
        self.assertEqual(long_words, ["planet", "galaxy"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
words = ["sun", "planet", "moon", "star", "galaxy"]
long_words = [w for w in words if len(w) > 4]
```
