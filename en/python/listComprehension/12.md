---
language: python
exerciseType: 4
---

# --description--

The inner `for` can use the variable of the outer one.
This is the classic way to **flatten** a list of lists into a single list:
```python
matrix = [[1, 2], [3, 4]]
flat = [n for row in matrix for n in row]
print(flat)  # [1, 2, 3, 4]
```
The `sum()` function then adds up all the numbers of a list.

# --instructions--

Sort the items to flatten `matrix` and print the flat list together with the sum of its numbers

# --answers--

```python
matrix = [[1, 2], [3, 4]]
```

```python
flat = [n for row in matrix for n in row]
```

```python
total = sum(flat)
```

```python
print(flat, total)
```

# --solutions--

```python
matrix = [[1, 2], [3, 4]]
flat = [n for row in matrix for n in row]
total = sum(flat)
print(flat, total)
```

# --output--

[1, 2, 3, 4] 10
