---
language: python
exerciseType: 1
---

# --description--

**When should you use a comprehension?**
It is perfect when the result is a list (or dict, or set) and the logic fits on one readable line: a simple transformation, an optional filter.
If you need several statements, more than two nested `for`, or the line gets hard to read, write a plain `for` loop instead: the code will be longer, but clearer.
A comprehension also replaces most uses of `map()` and `filter()` with lambdas:
```python
doubled = list(map(lambda n: n * 2, nums))
doubled = [n * 2 for n in nums]  # same result, easier to read
```

# --instructions--

Define a function `initials(names)` that returns a list with the first letter of each name in `names` in uppercase, skipping empty strings. Use a list comprehension

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use a list comprehension

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`initials(["ana", "bob"])` must return `['A', 'B']`

```python
    def test_1(self):
        self.assertEqual(initials(["ana", "bob"]), ["A", "B"], "--err-t1--")
```

`initials(["", "cy", ""])` must return `['C']`

```python
    def test_2(self):
        self.assertEqual(initials(["", "cy", ""]), ["C"], "--err-t2--")
```

`initials([])` must return `[]`

```python
    def test_3(self):
        self.assertEqual(initials([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def initials(names):
    return [name[0].upper() for name in names if name != ""]
```
