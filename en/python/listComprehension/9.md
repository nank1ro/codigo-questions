---
language: python
exerciseType: 1
---

# --description--

The two conditions can be combined in the same comprehension: an `if-else` to choose the value, and a filter `if` at the end to skip some items.
```python
nums = [1, 2, 3, 4]
result = ["big" if n > 2 else "small" for n in nums if n != 3]
print(result)  # ['small', 'small', 'big']
```

# --instructions--

Store in the variable `clipped` a copy of `nums` where every negative number is replaced by `0`, using a list comprehension with an `if-else` expression

# --seed--

```python
nums = [4, -2, 7, -5]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use a list comprehension with an `if-else` expression

```json
{
  "regex": "\\[.* if .* else .* for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`clipped` must be equal to `[4, 0, 7, 0]`

```python
    def test_1(self):
        self.assertEqual(clipped, [4, 0, 7, 0], "--err-t1--")
```

`nums` must not be modified

```python
    def test_2(self):
        self.assertEqual(nums, [4, -2, 7, -5], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [4, -2, 7, -5]
clipped = [n if n >= 0 else 0 for n in nums]
```
