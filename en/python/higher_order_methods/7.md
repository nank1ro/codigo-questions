---
language: python
exerciseType: 1
---

# --description--

A function can also **return** a function. Define an inner function with `def` and return it without calling it:
```python
def make_greeter(greeting):
    def greet(name):
        return greeting + ", " + name
    return greet

hello = make_greeter("Hello")
print(hello("Ada"))  # Hello, Ada
```
The inner function keeps using `greeting` even after `make_greeter` has finished: it **remembers** the variables of the scope where it was created. Such a function is called a **closure**. Each call to `make_greeter` creates a new, independent closure with its own `greeting`.

# --instructions--

Write a function `make_multiplier` that takes a number `factor` and returns a function which takes a number and returns it multiplied by `factor`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`make_multiplier(2)` must return a function

```python
    def test_1(self):
        self.assertTrue(callable(make_multiplier(2)), "--err-t1--")
```

`make_multiplier(2)(5)` must return `10`

```python
    def test_2(self):
        double = make_multiplier(2)
        self.assertEqual(double(5), 10, "--err-t2--")
        self.assertEqual(double(0), 0, "--err-t2--")
```

Each multiplier must remember its own factor

```python
    def test_3(self):
        double = make_multiplier(2)
        triple = make_multiplier(3)
        self.assertEqual(triple(4), 12, "--err-t3--")
        self.assertEqual(double(4), 8, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def make_multiplier(factor):
    def multiply(number):
        return number * factor
    return multiply
```
