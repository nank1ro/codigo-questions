---
language: python
exerciseType: 1
---

# --description--

`reduce(func, iterable, initial)` from the `functools` module folds a sequence into a **single value**. It calls `func` with the result so far and the next element, starting from `initial`:
```python
from functools import reduce

total = reduce(lambda acc, n: acc + n, [1, 2, 3], 0)
print(total)  # 6
```
The steps are `0 + 1`, then `1 + 2`, then `3 + 3`. When `initial` is omitted, the first element is used as the starting value, but then an empty sequence raises a `TypeError`, so give an initial value whenever the sequence may be empty.

# --instructions--

Write a function `product` that takes a list of numbers and returns them all multiplied together, using `reduce`; the product of an empty list must be `1`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use `reduce`

```json
{
  "regex": "reduce\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`product([2, 3, 4])` must return `24`

```python
    def test_1(self):
        self.assertEqual(product([2, 3, 4]), 24, "--err-t1--")
```

`product([5])` must return `5`

```python
    def test_2(self):
        self.assertEqual(product([5]), 5, "--err-t2--")
```

`product([])` must return `1`

```python
    def test_3(self):
        self.assertEqual(product([]), 1, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import reduce

def product(numbers):
    return reduce(lambda acc, n: acc * n, numbers, 1)
```
