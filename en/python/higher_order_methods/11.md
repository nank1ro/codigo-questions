---
language: python
exerciseType: 1
---

# --description--

`partial` is handy with built-ins that take options. `int(text, base=16)` parses a hexadecimal string; fixing the base gives a one-argument converter that fits `map`:
```python
from functools import partial

hex_to_int = partial(int, base=16)
print(hex_to_int("ff"))                      # 255
print(list(map(hex_to_int, ["a", "10"])))    # [10, 16]
```
The partial object remembers what it wraps: `hex_to_int.func` is `int`, and `hex_to_int.keywords` is `{'base': 16}`.

# --instructions--

Define two functions with `partial`: `hex_to_int` that converts a hexadecimal string to an `int`, and `binary_to_int` that converts a binary string to an `int`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`hex_to_int("ff")` must return `255`

```python
    def test_1(self):
        self.assertEqual(hex_to_int("ff"), 255, "--err-t1--")
        self.assertEqual(hex_to_int("10"), 16, "--err-t1--")
```

`binary_to_int("1010")` must return `10`

```python
    def test_2(self):
        self.assertEqual(binary_to_int("1010"), 10, "--err-t2--")
        self.assertEqual(binary_to_int("11"), 3, "--err-t2--")
```

Both must be partial objects wrapping `int`

```python
    def test_3(self):
        self.assertIs(hex_to_int.func, int, "--err-t3--")
        self.assertIs(binary_to_int.func, int, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import partial

hex_to_int = partial(int, base=16)
binary_to_int = partial(int, base=2)
```
