---
language: python
exerciseType: 4
---

# --description--

The `key` function can pick **any part** of an element. For a list of tuples, `lambda s: s[1]` sorts by the second item of each tuple; for a list of dictionaries, `lambda d: d["age"]` sorts by a value:
```python
pairs = [("b", 2), ("a", 3), ("c", 1)]
print(sorted(pairs, key=lambda p: p[1]))  # [('c', 1), ('b', 2), ('a', 3)]
```
`min` and `max` accept the same `key` parameter, so `max(pairs, key=lambda p: p[1])` returns `('a', 3)`: the whole tuple, not just the number.

# --instructions--

Sort the items to print the scores from the highest to the lowest, one per line as `name: score`

# --answers--

```python
scores = [("Ada", 92), ("Linus", 85), ("Grace", 97)]
```

```python
ranked = sorted(scores, key=lambda s: s[1], reverse=True)
```

```python
for name, score in ranked:
```

```python
    print(f"{name}: {score}")
```

# --solutions--

```python
scores = [("Ada", 92), ("Linus", 85), ("Grace", 97)]
ranked = sorted(scores, key=lambda s: s[1], reverse=True)
for name, score in ranked:
    print(f"{name}: {score}")
```

# --output--

Grace: 97
Ada: 92
Linus: 85
