---
language: python
exerciseType: 1
---

# --description--

`str.split` can only cut on a fixed separator. `re.split(pattern, text)` cuts on anything the pattern describes, which is what messy input usually needs:
```python
import re

print("a, b;c".split(","))            # ['a', ' b;c']
print(re.split(r"[,;]", "a, b;c"))    # ['a', ' b', 'c']
print(re.split(r"[,;\s]+", "a, b;c")) # ['a', 'b', 'c']
```
Writing the separator as `[,;\s]+` makes a whole run of commas, semicolons and spaces count as one cut, instead of leaving empty strings between them.

A `maxsplit` argument stops after a given number of cuts, leaving the rest of the text in the last element: `re.split(r"\s+", "a b c", maxsplit=1)` gives `['a', 'b c']`.

# --instructions--

Write a function `split_fields(line)` that returns the list of fields in the line, treating any run of commas, semicolons and spaces as a single separator

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`split_fields` must cut on commas, semicolons and spaces

```python
    def test_1(self):
        self.assertEqual(split_fields("a, b;c"), ["a", "b", "c"], "--err-t1--")
        self.assertEqual(split_fields("red green blue"), ["red", "green", "blue"], "--err-t1--")
```

A run of separators must produce a single cut

```python
    def test_2(self):
        self.assertEqual(split_fields("x ; ; y"), ["x", "y"], "--err-t2--")
        self.assertEqual(split_fields("1,,2"), ["1", "2"], "--err-t2--")
```

A line without separators must return a single field

```python
    def test_3(self):
        self.assertEqual(split_fields("solo"), ["solo"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def split_fields(line):
    return re.split(r"[,;\s]+", line)
```
