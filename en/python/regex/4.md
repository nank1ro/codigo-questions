---
language: python
exerciseType: 1
---

# --description--

A match object carries more than the matched text. Besides `.group()` it offers the position of the match inside the original string:
```python
import re

match = re.search(r"\d+", "order 42 shipped")
print(match.group())  # 42
print(match.start())  # 6
print(match.end())    # 8
print(match.span())   # (6, 8)
```
`.start()` is the index of the first matched character, `.end()` is the index just after the last one, and `.span()` returns both as a tuple. That means `text[match.start():match.end()]` is always equal to `match.group()`.

Because `re.search` may return `None`, reading `.group()` straight away raises `AttributeError` when nothing matched; check the result first.

# --instructions--

Write a function `first_number(text)` that returns a tuple with the first run of digits, its start index and its end index, or `None` when the text contains no digit

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_number("order 42 shipped")` must return `("42", 6, 8)`

```python
    def test_1(self):
        self.assertEqual(first_number("order 42 shipped"), ("42", 6, 8), "--err-t1--")
```

The match must start at index `0` when the text begins with digits

```python
    def test_2(self):
        self.assertEqual(first_number("7up"), ("7", 0, 1), "--err-t2--")
        self.assertEqual(first_number("100 items"), ("100", 0, 3), "--err-t2--")
```

`first_number` must return `None` when there is no digit

```python
    def test_3(self):
        self.assertIsNone(first_number("no digits here"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def first_number(text):
    match = re.search(r"\d+", text)
    if match is None:
        return None
    return (match.group(), match.start(), match.end())
```
