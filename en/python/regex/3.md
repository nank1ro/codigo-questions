---
language: python
exerciseType: 3
---

# --description--

There is a third entry point, `re.fullmatch`, which succeeds only when the pattern covers the **whole** text from the first character to the last. It is the right tool for validation:
```python
import re

print(bool(re.fullmatch(r"\d+", "2026")))    # True
print(bool(re.fullmatch(r"\d+", "2026!")))   # False
```

So the three functions differ only in where the pattern is allowed to sit: `re.match` at the start of the text, `re.search` anywhere in the text, and `re.fullmatch` over the entire text.

# --instructions--

What does this code print?
```python
import re

print(bool(re.match(r"\d+", "12abc")), bool(re.search(r"\d+", "12abc")), bool(re.fullmatch(r"\d+", "12abc")))
```

# --answers--

- `True True False`, because the digits are at the start and inside the text, but they do not cover the whole text
- `True True True`, because all three functions succeed as soon as the pattern is found somewhere
- `False True False`, because `re.match` needs the pattern to cover the whole text
- `True False False`, because `re.search` only looks after the first character

# --solutions--

- `True True False`, because the digits are at the start and inside the text, but they do not cover the whole text
