---
language: python
exerciseType: 2
---

# --description--

Comparison operators compare two values and return a **boolean**, `True` or `False`: `==` equal, `!=` not equal, `<` less than, `>` greater than, `<=` less than or equal, `>=` greater than or equal:
```python
age = 18
print(age == 18)  # True
print(age != 18)  # False
print(age < 18)   # False
print(age >= 18)  # True
```
The result can be stored in a variable or printed directly. A single `=` is an assignment, not a comparison.

# --instructions--

Complete the code so that the three lines check whether `score` is equal to `75`, less than `100` and greater than `50`, producing the output `True`, `True`, `True`

# --seed--

```python
score = 75
print(score [/] 75)
print(score [/] 100)
print(score [/] 50)
```

# --answers--

- ==
- <
- >
- =
- !==

# --solutions--

```python
score = 75
print(score == 75)
print(score < 100)
print(score > 50)
```

# --output--

True
True
True
