---
language: python
exerciseType: 1
---

# --description--

Logical operators combine booleans. `and` is `True` only when both sides are `True`, `or` when at least one side is, and `not` flips a single value:
```python
age = 20
member = False
print(age >= 18 and member)  # False
print(age >= 18 or member)   # True
print(not member)            # True
```
Comparisons bind tighter than logical operators, so `age >= 18 and member` needs no parentheses. Parentheses are needed to group an `or` inside an `and`: `a and (b or c)`.

# --instructions--

Write a function `is_leap(year)` that returns `True` when `year` is divisible by `4` but not by `100`, unless it is also divisible by `400`, and `False` otherwise

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`2024` and `2000` are leap years

```python
    def test_1(self):
        self.assertIs(is_leap(2024), True, "--err-t1--")
        self.assertIs(is_leap(2000), True, "--err-t1--")
```

`2023` is not a leap year

```python
    def test_2(self):
        self.assertIs(is_leap(2023), False, "--err-t2--")
```

`1900` is divisible by `100` but not by `400`, so it is not a leap year

```python
    def test_3(self):
        self.assertIs(is_leap(1900), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_leap(year):
    return year % 4 == 0 and (year % 100 != 0 or year % 400 == 0)
```
