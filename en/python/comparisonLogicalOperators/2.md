---
language: python
exerciseType: 1
---

# --description--

Comparison operators are not limited to numbers. Strings are compared character by character using their code points, so `"apple" < "banana"` is `True` and, because every uppercase letter comes before the lowercase ones, `"Zoo" < "apple"` is also `True`. Lists and tuples are compared element by element in the same way:
```python
print("cat" < "dog")       # True
print([1, 2, 3] < [1, 3])  # True
print((1, 2) == (1, 2))    # True
```
A comparison is an expression, so a function can `return a < b` directly instead of wrapping it in an `if`.

# --instructions--

Write a function `comes_first(a, b)` that returns `True` when the string `a` is ordered before the string `b`, and `False` when they are equal or `a` comes after `b`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`comes_first("apple", "banana")` must return `True`

```python
    def test_1(self):
        self.assertIs(comes_first("apple", "banana"), True, "--err-t1--")
```

`comes_first("pear", "apple")` and `comes_first("kiwi", "kiwi")` must return `False`

```python
    def test_2(self):
        self.assertIs(comes_first("pear", "apple"), False, "--err-t2--")
        self.assertIs(comes_first("kiwi", "kiwi"), False, "--err-t2--")
```

Uppercase letters come before lowercase ones, so `comes_first("Zoo", "apple")` must return `True`

```python
    def test_3(self):
        self.assertIs(comes_first("Zoo", "apple"), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def comes_first(a, b):
    return a < b
```
