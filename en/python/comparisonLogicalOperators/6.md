---
language: python
exerciseType: 2
---

# --description--

Every value has a **truth value**. `bool(value)` returns `False` for `0`, `0.0`, `None`, the empty string `""` and empty containers such as `[]`, `{}` and `set()`; every other value is truthy, including `"0"` and `[0]`:
```python
print(bool(0), bool(""), bool(None))   # False False False
print(bool(42), bool("0"), bool([0]))  # True True True
```
`if`, `while`, `and`, `or` and `not` use this rule, so `if items:` checks that the list is not empty and `not name` checks that the string is empty; there is no need to write `len(items) > 0` or `name == ""`.

# --instructions--

Complete the code so that the first line prints the truth value of `cart`, `name` and `count`, and the `if` prints `empty cart` when the list is empty, producing the output `False True False`, `empty cart`

# --seed--

```python
cart = []
name = "Ada"
count = 0
print([/](cart), bool(name), bool(count))
if [/] cart:
    print("empty cart")
```

# --answers--

- bool
- not
- len
- str
- is

# --solutions--

```python
cart = []
name = "Ada"
count = 0
print(bool(cart), bool(name), bool(count))
if not cart:
    print("empty cart")
```

# --output--

False True False
empty cart
