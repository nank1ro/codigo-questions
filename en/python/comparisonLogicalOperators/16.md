---
language: python
exerciseType: 1
---

# --description--

`any(iterable)` returns `True` if at least one element is truthy and `all(iterable)` if every element is (`all([])` is `True`). Combined with a generator expression they express a condition over a whole collection, and they short-circuit like `or` and `and`:
```python
print(any(ch.isdigit() for ch in "abc1"))  # True
print(all(ch.isalpha() for ch in "ab1"))   # False
```

# --instructions--

Write a function `is_valid_password(password)` that returns `True` only when the string has between `8` and `20` characters inclusive, contains at least one digit and contains no spaces, and `False` otherwise

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`"secret123"` is valid, and so are passwords of exactly `8` or `20` characters

```python
    def test_1(self):
        self.assertIs(is_valid_password("secret123"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefg1"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefghij123456789x"), True, "--err-t1--")
```

Too short or too long passwords are not valid

```python
    def test_2(self):
        self.assertIs(is_valid_password("short1"), False, "--err-t2--")
        self.assertIs(is_valid_password("abcdefghij1234567890x"), False, "--err-t2--")
```

A password without digits or with a space is not valid

```python
    def test_3(self):
        self.assertIs(is_valid_password("nodigitshere"), False, "--err-t3--")
        self.assertIs(is_valid_password("pass word1"), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_valid_password(password):
    return (
        8 <= len(password) <= 20
        and any(ch.isdigit() for ch in password)
        and " " not in password
    )
```
