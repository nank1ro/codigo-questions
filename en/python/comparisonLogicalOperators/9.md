---
language: python
exerciseType: 4
---

# --description--

Logical operators are **short-circuit**: `and` stops as soon as one operand is falsy and `or` as soon as one is truthy, because the result is already known. The remaining operands are never evaluated, so if they are function calls they do not run:
```python
def check(n):
    print("checking", n)
    return n > 0

check(-1) and check(5)  # prints only "checking -1"
check(2) or check(-3)   # prints only "checking 2"
```

# --instructions--

Sort the items to define `is_positive`, compute `first` with `and` and then `second` with `or`, and print both results, producing the output `checking -1`, `checking 2`, `False True`

# --answers--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
```

```python
first = is_positive(-1) and is_positive(5)
```

```python
second = is_positive(2) or is_positive(-3)
```

```python
print(first, second)
```

# --solutions--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
first = is_positive(-1) and is_positive(5)
second = is_positive(2) or is_positive(-3)
print(first, second)
```

# --output--

checking -1
checking 2
False True
