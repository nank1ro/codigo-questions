---
language: python
exerciseType: 2
---

# --description--

Comparisons can be **chained**: `1 < x < 10` checks that `x` is greater than `1` **and** less than `10`, exactly like `1 < x and x < 10`, but `x` is evaluated only once:
```python
x = 5
print(1 < x < 10)   # True
print(1 < x <= 5)   # True
print(10 < x < 20)  # False
```
Any comparison operators can be chained and each one applies to its two neighbours: `a < b == c` means `a < b and b == c`. Reading a chain as a range, `low < x < high`, is the most common use.

# --instructions--

Complete the chained comparison so that it checks that `x` is strictly between `1` and `10`, producing the output `True`, `False`

# --seed--

```python
x = 5
print(1 [/] x [/] 10)
print(10 < x < 20)
```

# --answers--

- <
- <
- >
- >=
- ==

# --solutions--

```python
x = 5
print(1 < x < 10)
print(10 < x < 20)
```

# --output--

True
False
