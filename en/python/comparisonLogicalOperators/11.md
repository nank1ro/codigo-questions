---
language: python
exerciseType: 1
---

# --description--

Short-circuit evaluation is a safe way to **guard** an operation that would fail on some values. In `word is not None and len(word) < 4`, `len(word)` runs only when `word` is not `None`, so the call never raises. The guard must come first:
```python
def is_short(word):
    return word is not None and len(word) < 4

print(is_short(None))   # False
print(is_short("cat"))  # True
```
Keep in mind that `and` returns an operand: `word and len(word) < 4` gives `None` for `None` and `""` for the empty string, not `False`. Guard with a real comparison when a boolean is required.

# --instructions--

Write a function `first_is_zero(numbers)` that returns `True` when the list `numbers` is not empty and its first element is `0`, and `False` otherwise. The result must be a boolean even for an empty list

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_is_zero([0, 5])` must return `True`

```python
    def test_1(self):
        self.assertIs(first_is_zero([0, 5]), True, "--err-t1--")
        self.assertIs(first_is_zero([0]), True, "--err-t1--")
```

`first_is_zero([3, 0])` must return `False`

```python
    def test_2(self):
        self.assertIs(first_is_zero([3, 0]), False, "--err-t2--")
```

`first_is_zero([])` must return `False` without raising

```python
    def test_3(self):
        self.assertIs(first_is_zero([]), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def first_is_zero(numbers):
    return len(numbers) > 0 and numbers[0] == 0
```
