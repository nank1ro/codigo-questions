---
language: python
exerciseType: 4
---

# --description--

Enums pair naturally with the `match` statement (available since Python 3.10), which compares a value against a series of `case` patterns and runs the first one that matches:
```python
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
```
Always write the member with its class, like `Light.RED`: a bare name such as `case RED:` would not compare anything, it would just capture the value into a new variable `RED` and match everything.
The wildcard `case _:` is the default and must come last, because any pattern after it could never be reached.

# --instructions--

Sort the items to declare the enum `Light`, define `action()` with the `RED` case first, then the `GREEN` case, then the default, and print the action for `Light.YELLOW`

# --answers--

```python
from enum import Enum
```

```python
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
```

```python
def action(light):
```

```python
    match light:
```

```python
        case Light.RED:
            return "Stop"
```

```python
        case Light.GREEN:
            return "Go"
```

```python
        case _:
            return "Slow down"
```

```python
print(action(Light.YELLOW))
```

# --solutions--

```python
from enum import Enum
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
print(action(Light.YELLOW))
```

# --output--

Slow down
