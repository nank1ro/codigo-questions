---
language: python
exerciseType: 2
---

# --description--

If two members share the same value, the second one is not a new member but an **alias** of the first:
```python
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # alias of ACTIVE

print(Status.ENABLED)  # Status.ACTIVE
print(len(Status))     # 1
```
Aliases are skipped when iterating and are not counted by `len()`.
Usually a duplicated value is a mistake. The `unique` decorator, imported from `enum`, makes Python raise a `ValueError` as soon as an enum with aliases is declared:
```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # ValueError: duplicate values found
```

# --instructions--

Complete the code to import the `unique` decorator and apply it to `Status`, so that duplicated values are rejected, then print the number of members

# --seed--

```python
from enum import Enum, [/]

[/]
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --answers--

- unique
- @unique
- auto
- @auto
- @property
- @classmethod

# --solutions--

```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --output--

2
