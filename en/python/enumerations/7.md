---
language: python
exerciseType: 1
---

# --description--

A plain `Enum` member cannot be compared with `<` or added to a number.
When the members represent **levels** that need ordering, inherit from `IntEnum` instead: its members are also integers, so they support comparisons, arithmetic and sorting:
```python
from enum import IntEnum

class Priority(IntEnum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

print(Priority.HIGH > Priority.LOW)  # True
print(Priority.LOW + 1)              # 2
print(int(Priority.MEDIUM))          # 2
```
An `IntEnum` member is also equal to its integer value: `Priority.LOW == 1` is `True`.

# --instructions--

Store in `is_urgent` whether `Priority.HIGH` is greater than `Priority.MEDIUM`, in `ordered` the list `tasks` sorted with `sorted()`, and in `score` the sum of `Priority.LOW` and `Priority.HIGH`

# --seed--

```python
from enum import IntEnum

class Priority(IntEnum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

tasks = [Priority.HIGH, Priority.LOW, Priority.MEDIUM]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use `sorted()`

```json
{
  "regex": "sorted\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`is_urgent` must be `True`

```python
    def test_1(self):
        self.assertEqual(is_urgent, True, "--err-t1--")
```

`ordered` must be equal to `[Priority.LOW, Priority.MEDIUM, Priority.HIGH]`

```python
    def test_2(self):
        self.assertEqual(ordered, [Priority.LOW, Priority.MEDIUM, Priority.HIGH], "--err-t2--")
```

`score` must be equal to `4`

```python
    def test_3(self):
        self.assertEqual(score, 4, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import IntEnum

class Priority(IntEnum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

tasks = [Priority.HIGH, Priority.LOW, Priority.MEDIUM]
is_urgent = Priority.HIGH > Priority.MEDIUM
ordered = sorted(tasks)
score = Priority.LOW + Priority.HIGH
```
