---
language: python
exerciseType: 3
---

# --description--

`StrEnum` (available since Python 3.11) is the string counterpart of `IntEnum`: its members are also strings, equal to their value.
This makes them convenient wherever plain strings are expected, like configuration keys or API parameters:
```python
from enum import StrEnum

class Mode(StrEnum):
    LIGHT = "light"
    DARK = "dark"

print(Mode.DARK == "dark")  # True
print(Mode.DARK.upper())    # DARK
```
Unlike a plain `Enum`, converting a `StrEnum` member to text with `str()` or inside an f-string gives its **value**, not `Mode.DARK`.

# --instructions--

What does this code print?
```python
from enum import StrEnum

class Mode(StrEnum):
    LIGHT = "light"
    DARK = "dark"

print(f"theme: {Mode.DARK}")
```

# --answers--

- `theme: dark`, because a `StrEnum` member is formatted as its value
- `theme: Mode.DARK`, because an f-string always shows the class and member names
- `theme: DARK`, because an f-string shows the member name
- `theme: <Mode.DARK: 'dark'>`, because an f-string uses the `repr()` of the member

# --solutions--

- `theme: dark`, because a `StrEnum` member is formatted as its value
