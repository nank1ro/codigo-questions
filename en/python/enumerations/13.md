---
language: python
exerciseType: 1
---

# --description--

Enum members are **hashable**, so they can be used as dictionary keys and as set elements.
A dictionary keyed by an enum is a clean way to attach data to each member, and looking it up with a member is safer than using a raw string that could be misspelled:
```python
class Size(Enum):
    SMALL = 1
    LARGE = 2

prices = {Size.SMALL: 2.5, Size.LARGE: 4.5}
print(prices[Size.LARGE])  # 4.5
```
Since members can appear in any collection, everything you know about lists, sets and comprehensions works with them too:
```python
order = [Size.LARGE, Size.SMALL, Size.LARGE]
print(len(set(order)))  # 2
```

# --instructions--

Store in `large_price` the price of `Size.LARGE` read from `prices`, in `total` the sum of the prices of the sizes in `order`, and in `sizes` the set of the distinct sizes in `order`

# --seed--

```python
from enum import Enum

class Size(Enum):
    SMALL = 1
    MEDIUM = 2
    LARGE = 3

prices = {Size.SMALL: 2.5, Size.MEDIUM: 3.5, Size.LARGE: 4.5}
order = [Size.LARGE, Size.SMALL, Size.LARGE]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must read `prices` with `Size.LARGE` as key

```json
{
  "regex": "prices\\[\\s*Size\\.LARGE\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`large_price` must be equal to `4.5`

```python
    def test_1(self):
        self.assertEqual(large_price, 4.5, "--err-t1--")
```

`total` must be equal to `11.5`

```python
    def test_2(self):
        self.assertEqual(total, 11.5, "--err-t2--")
```

`sizes` must be equal to `{Size.SMALL, Size.LARGE}`

```python
    def test_3(self):
        self.assertEqual(sizes, {Size.SMALL, Size.LARGE}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Size(Enum):
    SMALL = 1
    MEDIUM = 2
    LARGE = 3

prices = {Size.SMALL: 2.5, Size.MEDIUM: 3.5, Size.LARGE: 4.5}
order = [Size.LARGE, Size.SMALL, Size.LARGE]
large_price = prices[Size.LARGE]
total = sum(prices[size] for size in order)
sizes = set(order)
```
