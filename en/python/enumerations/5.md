---
language: python
exerciseType: 1
---

# --description--

You can obtain a member starting from its **value** by calling the class like a function, or from its **name** using square brackets:
```python
print(Color(2))        # Color.GREEN
print(Color["BLUE"])   # Color.BLUE
```
Both are handy when the value or the name comes from outside the program, like a file or user input.
If nothing matches, `Color(9)` raises a `ValueError` and `Color["PINK"]` raises a `KeyError`.

# --instructions--

Store in `by_value` the member of `Status` whose value is `code`, calling `Status(...)`, and in `by_name` the member whose name is `label`, using `Status[...]`

# --seed--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must call `Status(...)` with `code`

```json
{
  "regex": "Status\\(\\s*code\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

You must use `Status[...]` with `label`

```json
{
  "regex": "Status\\[\\s*label\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`by_value` must be `Status.DONE`

```python
    def test_1(self):
        self.assertIs(by_value, Status.DONE, "--err-t1--")
```

`by_name` must be `Status.FAILED`

```python
    def test_2(self):
        self.assertIs(by_name, Status.FAILED, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
by_value = Status(code)
by_name = Status[label]
```
