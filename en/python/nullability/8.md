---
language: python
exerciseType: 4
---

# --description--

Functions that may receive `None` often start with a **guard**: an `if` that returns early when there is nothing to work on. The rest of the function can then assume that the value is present, without nesting everything inside an `else`:
```python
def shout(text):
    if text is None:
        return None
    return text.upper() + "!"
```
Guards come first, in the order the checks must happen: you cannot call `text.split()` before knowing that `text` is not `None`.

# --instructions--

Sort the items to define `first_word`, which returns `None` when `text` is `None` or has no words, and the first word otherwise; keep the guards before the code that depends on them

# --answers--

```python
def first_word(text):
```

```python
    if text is None:
        return None
```

```python
    words = text.split()
```

```python
    if len(words) == 0:
        return None
```

```python
    return words[0]
```

```python
print(first_word("hello world"))
print(first_word(None))
print(first_word("   "))
```

# --solutions--

```python
def first_word(text):
    if text is None:
        return None
    words = text.split()
    if len(words) == 0:
        return None
    return words[0]
print(first_word("hello world"))
print(first_word(None))
print(first_word("   "))
```

# --output--

hello
None
None
