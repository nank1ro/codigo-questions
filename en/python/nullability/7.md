---
language: python
exerciseType: 1
---

# --description--

A function that returns a number **or** `None` should say so in its signature. A **type hint** is an annotation that documents the expected type: `name: str` for a parameter and `-> int` for the return value. Python does not enforce hints, but editors and readers rely on them:
```python
def parse_age(text: str) -> int | None:
    if text.isdigit():
        return int(text)
    return None
```
`int | None` reads "an `int` or `None`". The older spelling `Optional[int]` from the `typing` module means exactly the same thing, and you will still meet it in existing code.
Every time you see `| None` in a signature, remember to check the result before using it.

# --instructions--

Write a function `parse_age` with a type hint that takes a string and returns it converted to an `int` when it contains only digits, and `None` otherwise

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

The return type must be hinted as `int | None`

```json
{
  "regex": "->\\s*(int\\s*\\|\\s*None|None\\s*\\|\\s*int|Optional\\[int\\])",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_age("42")` must return `42`

```python
    def test_1(self):
        self.assertEqual(parse_age("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_age("42"), int, "--err-t1--")
```

`parse_age("abc")` must return `None`

```python
    def test_2(self):
        self.assertIsNone(parse_age("abc"), "--err-t2--")
        self.assertIsNone(parse_age("4a"), "--err-t2--")
```

`parse_age("")` must return `None`

```python
    def test_3(self):
        self.assertIsNone(parse_age(""), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_age(text: str) -> int | None:
    if text.isdigit():
        return int(text)
    return None
```
