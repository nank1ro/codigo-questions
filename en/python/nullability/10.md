---
language: python
exerciseType: 1
---

# --description--

When `0` or `""` must be kept and only `None` replaced, the fallback needs an explicit `is None` check. The compact form is the **conditional expression**, `a if condition else b`, which evaluates to `a` when the condition is true and to `b` otherwise:
```python
timeout = 0
seconds = timeout if timeout is not None else 30
print(seconds)  # 0, not 30
```

# --instructions--

Write a function `port_or_default` that takes `port` and returns it when it is not `None`, and `8080` otherwise; the value `0` must be returned as it is

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must check with `is not None` or `is None`

```json
{
  "regex": "is\\s+(not\\s+)?None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`port_or_default(3000)` must return `3000`

```python
    def test_1(self):
        self.assertEqual(port_or_default(3000), 3000, "--err-t1--")
```

`port_or_default(None)` must return `8080`

```python
    def test_2(self):
        self.assertEqual(port_or_default(None), 8080, "--err-t2--")
```

`port_or_default(0)` must return `0`

```python
    def test_3(self):
        self.assertEqual(port_or_default(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def port_or_default(port):
    return port if port is not None else 8080
```
