---
language: python
exerciseType: 1
---

# --description--

A lookup followed by a `None` check usually needs two lines: one to store the result, one to test it. The **assignment expression** operator `:=`, nicknamed the *walrus*, assigns a value **inside** an expression, so both steps fit in the `if`:
```python
ages = {"Ada": 36}
if (age := ages.get("Ada")) is not None:
    print(age + 1)  # 37
```
The parentheses are required: without them, `:=` would try to assign the whole comparison. After the `if`, `age` stays available like any other variable.

# --instructions--

Write a function `describe_user` that takes a dictionary `users` mapping names to ages and a `name`; it must return `"<name> is <age>"` when the name is present, and `"unknown user"` otherwise, using the walrus operator to store the result of `get`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use the `:=` operator

```json
{
  "regex": ":=",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

You must use `get`

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe_user({"Ada": 36}, "Ada")` must return `"Ada is 36"`

```python
    def test_1(self):
        self.assertEqual(describe_user({"Ada": 36}, "Ada"), "Ada is 36", "--err-t1--")
```

`describe_user({"Ada": 36}, "Grace")` must return `"unknown user"`

```python
    def test_2(self):
        self.assertEqual(describe_user({"Ada": 36}, "Grace"), "unknown user", "--err-t2--")
```

An age of `0` must still count as present

```python
    def test_3(self):
        self.assertEqual(describe_user({"Tim": 0}, "Tim"), "Tim is 0", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe_user(users, name):
    if (age := users.get(name)) is not None:
        return f"{name} is {age}"
    return "unknown user"
```
