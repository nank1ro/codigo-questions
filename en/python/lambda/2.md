---
language: python
exerciseType: 1
---

# --description--

Notice that the lambda body has no `return` keyword.
The body is a **single expression**, and its value is returned automatically:
```python
square = lambda n: n * n
print(square(3))  # 9
```

# --instructions--

Store in the variable `square` a lambda that takes one parameter `n` and returns `n` multiplied by itself

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use a `lambda` expression

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`square(4)` must return `16`

```python
    def test_1(self):
        self.assertEqual(square(4), 16, "--err-t1--")
```

`square(7)` must return `49`

```python
    def test_2(self):
        self.assertEqual(square(7), 49, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
square = lambda n: n * n
```
