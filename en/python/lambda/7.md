---
language: python
exerciseType: 1
---

# --description--

Where lambdas really shine is as **arguments to other functions**.
`sorted()` accepts a `key` parameter: a function that is called on each item, and the result decides the order.
A lambda is the perfect fit:
```python
words = ["banana", "kiwi", "apple"]
print(sorted(words, key=lambda w: len(w)))
# ['kiwi', 'apple', 'banana']
```

# --instructions--

Store in the variable `by_length` the list `words` sorted from the shortest to the longest word, using `sorted()` with a lambda as `key`

# --seed--

```python
words = ["elephant", "cat", "tiger", "ox"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use a `lambda` expression

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`by_length` must be equal to `['ox', 'cat', 'tiger', 'elephant']`

```python
    def test_1(self):
        self.assertEqual(by_length, ["ox", "cat", "tiger", "elephant"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
words = ["elephant", "cat", "tiger", "ox"]
by_length = sorted(words, key=lambda w: len(w))
```
