---
language: python
exerciseType: 1
---

# --description--

Lambdas are not limited to built-in functions: **your own functions** can take a function as a parameter and call it.
```python
def apply(func, value):
    return func(value)

print(apply(lambda x: x * 10, 3))  # 30
```

# --instructions--

Define a function `apply_twice(func, value)` that applies `func` to `value` two times and returns the result.
Then store in `result` the value of `apply_twice` called with a lambda that adds `3`, and the value `10`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use a `lambda` expression

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`apply_twice(lambda x: x * 2, 5)` must return `20`

```python
    def test_1(self):
        self.assertEqual(apply_twice(lambda x: x * 2, 5), 20, "--err-t1--")
```

`apply_twice(lambda s: s + "!", "hi")` must return `hi!!`

```python
    def test_2(self):
        self.assertEqual(apply_twice(lambda s: s + "!", "hi"), "hi!!", "--err-t2--")
```

`result` must be equal to `16`

```python
    def test_3(self):
        self.assertEqual(result, 16, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def apply_twice(func, value):
    return func(func(value))

result = apply_twice(lambda x: x + 3, 10)
```
