---
language: python
exerciseType: 1
---

# --description--

Lambda parameters support **default values** too:
```python
greet = lambda name="World": f"Hello, {name}!"
print(greet())       # Hello, World!
print(greet("Ana"))  # Hello, Ana!
```

# --instructions--

Store in the variable `multiply` a lambda that takes `n` and a second parameter `factor` with default value `2`, and returns `n` multiplied by `factor`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use a `lambda` expression

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`multiply(5)` must return `10`

```python
    def test_1(self):
        self.assertEqual(multiply(5), 10, "--err-t1--")
```

`multiply(5, 3)` must return `15`

```python
    def test_2(self):
        self.assertEqual(multiply(5, 3), 15, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
multiply = lambda n, factor=2: n * factor
```
