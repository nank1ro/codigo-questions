---
language: python
exerciseType: 2
---

# --description--

A lambda can take **more than one parameter**.
Separate them with commas, exactly like in a `def` function:
```python
power = lambda base, exp: base ** exp
print(power(2, 3))  # 8
```

# --instructions--

Complete the lambda so that it takes the parameters `a` and `b` and returns their sum

# --seed--

```python
add = lambda a[/] b[/] a + b
print(add(3, 4))
```

# --answers--

- ,
- :
- ;
- ->

# --solutions--

```python
add = lambda a, b: a + b
print(add(3, 4))
```

# --output--

7
