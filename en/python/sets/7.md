---
language: python
exerciseType: 1
---

# --description--

The **union** of two sets is a new set with the elements of **both**, without duplicates.
Use the `|` operator or the `union()` method:
```python
a = {1, 2}
b = {2, 3}
print(a | b)        # {1, 2, 3}
print(a.union(b))   # {1, 2, 3}
```
Neither `a` nor `b` is modified: set operations always return a new set.

# --instructions--

Store in `skills` the union of `frontend` and `backend` using the `|` operator, then store in `total` how many distinct skills it contains

# --seed--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use the `|` operator

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`skills` must be equal to `{'html', 'css', 'js', 'python', 'sql'}`

```python
    def test_1(self):
        self.assertEqual(skills, {"html", "css", "js", "python", "sql"}, "--err-t1--")
```

`total` must be equal to `5`

```python
    def test_2(self):
        self.assertEqual(total, 5, "--err-t2--")
```

`frontend` must not be modified

```python
    def test_3(self):
        self.assertEqual(frontend, {"html", "css", "js"}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
skills = frontend | backend
total = len(skills)
```
