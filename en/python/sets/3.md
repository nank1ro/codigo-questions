---
language: python
exerciseType: 3
---

# --description--

There is a trap when creating an **empty set**.
Curly braces are also the syntax of dictionaries, so `{}` creates an empty **dictionary**, not a set:
```python
empty = {}
print(type(empty))  # <class 'dict'>
```
To get an empty set you must call `set()` with no arguments:
```python
empty = set()
print(type(empty))  # <class 'set'>
print(len(empty))   # 0
```

# --instructions--

What does this code print?
```python
empty = {}
print(type(empty))
```

# --answers--

- `<class 'dict'>`, because `{}` creates an empty dictionary
- `<class 'set'>`, because curly braces create a set
- `<class 'NoneType'>`, because the set has no elements
- A `SyntaxError`, because braces must contain at least one element

# --solutions--

- `<class 'dict'>`, because `{}` creates an empty dictionary
