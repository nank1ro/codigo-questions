---
language: python
exerciseType: 1
---

# --description--

Two sets are **equal** when they contain the same elements, whatever the order they were written in:
```python
print({1, 2, 3} == {3, 1, 2})  # True
```
Comparing the length of a collection with the length of its set is a quick way to detect duplicates: if the set is **smaller**, some value appeared more than once:
```python
emails = ["a@x.com", "b@x.com", "a@x.com"]
print(len(set(emails)) < len(emails))  # True
```
The list method `count(value)` tells how many times a value appears, which helps to find *which* values are duplicated.

# --instructions--

Store in `has_duplicates` whether `emails` contains any repeated address, comparing the length of the list with the length of its set, then store in `duplicates` the set of the addresses that appear more than once, using a set comprehension and `count()`

# --seed--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use `set()`

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

You must use a set comprehension

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_duplicates` must be `True`

```python
    def test_1(self):
        self.assertEqual(has_duplicates, True, "--err-t1--")
```

`duplicates` must be equal to `{'ann@x.com', 'bob@x.com'}`

```python
    def test_2(self):
        self.assertEqual(duplicates, {"ann@x.com", "bob@x.com"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
has_duplicates = len(set(emails)) < len(emails)
duplicates = {e for e in emails if emails.count(e) > 1}
```
