---
language: python
exerciseType: 2
---

# --description--

The **intersection** of two sets contains only the elements present in **both**.
Use the `&` operator or the `intersection()` method:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a & b)               # {2, 3}
print(a.intersection(b))   # {2, 3}
```
If the sets have nothing in common, the result is an empty set.

# --instructions--

Complete the code to store in `common` the elements present in both `a` and `b`, so that the program prints `[3, 4]`

# --seed--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a [/] b
print(sorted(common))
```

# --answers--

- &
- |
- -
- ^
- and

# --solutions--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a & b
print(sorted(common))
```

# --output--

[3, 4]
