---
language: python
exerciseType: 4
---

# --description--

Sets can also be **compared** with each other.
`a.issubset(b)`, or `a <= b`, is `True` when every element of `a` is also in `b`.
`a.issuperset(b)`, or `a >= b`, is `True` when `a` contains every element of `b`.
`a.isdisjoint(b)` is `True` when the two sets have **no** element in common:
```python
small = {1, 2}
big = {1, 2, 3}
print(small <= big)              # True
print(big.issuperset(small))     # True
print(small.isdisjoint({8, 9}))  # True
```

# --instructions--

Sort the items to build `palette` from `primary`, then print, in this order: whether `primary` is a subset of `palette`, whether `palette` is a subset of `primary`, and whether `primary` and `{"black"}` are disjoint

# --answers--

```python
primary = {"red", "blue", "yellow"}
```

```python
palette = primary | {"green"}
```

```python
print(primary <= palette)
```

```python
print(palette.issubset(primary))
```

```python
print(primary.isdisjoint({"black"}))
```

# --solutions--

```python
primary = {"red", "blue", "yellow"}
palette = primary | {"green"}
print(primary <= palette)
print(palette.issubset(primary))
print(primary.isdisjoint({"black"}))
```

# --output--

True
False
True
