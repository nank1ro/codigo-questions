---
language: python
exerciseType: 1
---

# --description--

The built-in function `set()` builds a set from any collection, for example a list or a string.
Since a set keeps each value only once, this is the classic way to **remove duplicates**:
```python
nums = [1, 2, 2, 3]
unique = set(nums)
print(len(unique))  # 3
```
To check whether a value is present use the `in` operator, which returns `True` or `False`:
```python
print(2 in unique)  # True
print(9 in unique)  # False
```
Membership checks on a set are very fast, even with thousands of elements.

# --instructions--

Store in the variable `unique` a set built from `nums` using `set()`, and in `has_three` whether the value `3` is in `unique`, using `in`

# --seed--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use `set()`

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

You must use the `in` operator

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`unique` must be equal to `{1, 2, 3, 4}`

```python
    def test_1(self):
        self.assertEqual(unique, {1, 2, 3, 4}, "--err-t1--")
```

`has_three` must be `True`

```python
    def test_2(self):
        self.assertEqual(has_three, True, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
unique = set(nums)
has_three = 3 in unique
```
