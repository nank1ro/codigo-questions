---
language: python
exerciseType: 1
---

# --description--

The **difference** `a - b` contains the elements of `a` that are **not** in `b`.
The order matters: `a - b` and `b - a` are usually different:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a - b)  # {1}
print(b - a)  # {4}
```
The **symmetric difference** `a ^ b` contains the elements that are in **exactly one** of the two sets:
```python
print(a ^ b)  # {1, 4}
```
The method forms are `difference()` and `symmetric_difference()`.

# --instructions--

Store in `only_ann` the hobbies of `ann` that `bob` does not have, using `-`, and in `not_shared` the hobbies that only one of them has, using `^`

# --seed--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use the `-` operator

```json
{
  "regex": "\\s-\\s",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

You must use the `^` operator

```json
{
  "regex": "\\^",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`only_ann` must be equal to `{'tennis', 'yoga'}`

```python
    def test_1(self):
        self.assertEqual(only_ann, {"tennis", "yoga"}, "--err-t1--")
```

`not_shared` must be equal to `{'tennis', 'yoga', 'swimming'}`

```python
    def test_2(self):
        self.assertEqual(not_shared, {"tennis", "yoga", "swimming"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
only_ann = ann - bob
not_shared = ann ^ bob
```
