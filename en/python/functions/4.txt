<section id="options">
{
"language": "python",
"type": "fillEmptySpaces"
}
</section>
<section id="description">
In functions we can add an _optional comment_ that explains what the function does:
```
"""
Prints 'Hello World' to the console.
"""
```
</section>
<section id="instructions">
Add a valid comment to the function
</section>
<section id="fillEmptySpaces">
{
    "seed": ["def product(n1, n2):\n","[/]","\nReturns the product between n1 and n2 numbers\n","[/]","\nreturn n1 * n2\n\nresult = product(5, 5)\nprint(result)"],
	"buttons": ["\"\"\"","\"\"\"","''","''"],
	"correct": "def product(n1, n2):\n\"\"\"\nReturns the product between n1 and n2 numbers\n\"\"\"\nreturn n1 * n2\n\nresult = product(5, 5)\nprint(result)"
}
</section>
<section id="preOutput">
25
</section>