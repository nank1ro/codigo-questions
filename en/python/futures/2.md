---
language: python
exerciseType: 1
---

# --description--

A coroutine can take parameters and `return` a value exactly like a normal function. The value is not available when the coroutine object is created, only once the coroutine has been run. `asyncio.run` returns whatever the coroutine returned:
```python
import asyncio

async def double(n):
    return n * 2

result = asyncio.run(double(21))
print(result)  # 42
```
Everything you know about functions still applies inside `async def`: local variables, conditions, loops and multiple `return` statements.

# --instructions--

Write a coroutine function `add(a, b)` with `async def` that returns the sum of `a` and `b`

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`add` must be a coroutine function

```python
    def test_1(self):
        self.assertTrue(inspect.iscoroutinefunction(add), "--err-t1--")
```

Running `add(1, 3)` must return `4`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(add(1, 3)), 4, "--err-t2--")
```

Running `add(200, 210)` must return `410`

```python
    def test_3(self):
        self.assertEqual(asyncio.run(add(200, 210)), 410, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
async def add(a, b):
    return a + b
```
