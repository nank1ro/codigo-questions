---
language: python
exerciseType: 4
---

# --description--

Since every string method returns a new string, you can **chain** methods one after the other.
Each call works on the result of the previous one:
```python
text = "  Hello World  "
print(text.strip().lower())  # hello world
```
A slice also accepts a third value, the **step**.
The step `-1` walks the string backwards, which is the classic trick to reverse it:
```python
print("abc"[::-1])  # cba
```

# --instructions--

Sort the items to clean `word`, reverse it and print whether the cleaned word is a palindrome (reads the same backwards)

# --answers--

```python
word = "  Level  "
```

```python
clean = word.strip().lower()
```

```python
reversed_word = clean[::-1]
```

```python
print(clean == reversed_word)
```

# --solutions--

```python
word = "  Level  "
clean = word.strip().lower()
reversed_word = clean[::-1]
print(clean == reversed_word)
```

# --output--

True
