---
language: python
exerciseType: 1
---

# --description--

A **slice** extracts a part of a string.
Write `[start:end]` between square brackets: the character at `start` is included, the one at `end` is **excluded**:
```python
word = "python"
print(word[0:3])  # pyt
print(word[2:4])  # th
```
You can omit `start` to slice from the beginning, or `end` to slice up to the end:
```python
print(word[:2])  # py
print(word[2:])  # thon
```
Slicing never raises an error: an `end` bigger than the length simply stops at the last character.

# --instructions--

Store in the variable `first` the first 4 characters of `word` and in `rest` all the remaining ones, using slices

# --seed--

```python
word = "notebook"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use a slice `[:]`

```json
{
  "regex": "\\[[^\\]]*:[^\\]]*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`first` must be equal to `"note"`

```python
    def test_1(self):
        self.assertEqual(first, "note", "--err-t1--")
```

`rest` must be equal to `"book"`

```python
    def test_2(self):
        self.assertEqual(rest, "book", "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
word = "notebook"
first = word[:4]
rest = word[4:]
```
