---
language: python
exerciseType: 1
---

# --description--

The built-in function `len()` returns the **length** of a string, that is how many characters it contains.
Spaces and punctuation count as characters too:
```python
print(len("hello"))     # 5
print(len("hi there"))  # 8
print(len(""))          # 0
```

# --instructions--

Store in the variable `length` the number of characters of `word`, using `len()`

# --seed--

```python
word = "notebook"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

You must use `len()`

```json
{
  "regex": "len\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`length` must be equal to `8`

```python
    def test_1(self):
        self.assertEqual(length, 8, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
word = "notebook"
length = len(word)
```
