---
language: python
exerciseType: 1
---

# --description--

Python looks for `#` only in the code, never inside a **string**. Between quotes, `#` is an ordinary character:
```python
print("black is #000000")  # a hex colour
```
The first `#` is part of the text, the second one starts a real comment. The same holds for `"""` inside a `#` comment: there it is just three quote characters, and it starts nothing.

# --instructions--

Write a function `label(name)` that returns `name` with a `#` in front of it, so `label("python")` returns `"#python"`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`label("python")` must return `"#python"`

```python
    def test_1(self):
        self.assertEqual(label("python"), "#python", "--err-t1--")
```

`label("ai")` must return `"#ai"`

```python
    def test_2(self):
        self.assertEqual(label("ai"), "#ai", "--err-t2--")
```

`label("")` must return `"#"`

```python
    def test_3(self):
        self.assertEqual(label(""), "#", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def label(name):
    """Returns name with a # in front of it."""
    return "#" + name
```
