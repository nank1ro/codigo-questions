---
language: python
exerciseType: 4
---

# --description--

The docstring has to be the **first line of the body**, above every other statement. A string written after the `return`, or anywhere else in the body, is only a string: `__doc__` stays `None` and no tool will ever show it.

# --instructions--

Sort the items to build a program that defines `to_seconds` with its docstring as the first line of the body, and then prints `to_seconds(2)`

# --answers--

```python
def to_seconds(minutes):
```

```python
    """Returns the number of seconds in the given minutes."""
```

```python
    return minutes * 60
```

```python
print(to_seconds(2))
```

# --solutions--

```python
def to_seconds(minutes):
    """Returns the number of seconds in the given minutes."""
    return minutes * 60
print(to_seconds(2))
```

# --output--

120
