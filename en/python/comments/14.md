---
language: python
exerciseType: 1
---

# --description--

A comment placed above a function to say what the function does is in the wrong place. The docstring is the place for that: it is attached to the function, `help()` finds it and editors show it, while a `#` comment above the `def` is invisible to all of them.

```python
# adds a and b
def add(a, b):
    return a + b
```
Moving the same sentence one line down, between triple quotes, makes it real documentation:
```python
def add(a, b):
    """Returns the sum of a and b."""
    return a + b
```

# --instructions--

Remove the two `#` comments above `subtract` and document the function with a docstring instead

# --seed--

```python
# subtract
# this function subtracts b from a
def subtract(a, b):
    return a - b
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

The `#` comments above `subtract` must be removed

```json
{
  "regex": "#\\s*this function subtracts",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

The `# subtract` comment must be removed too

```json
{
  "regex": "#\\s*subtract",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`subtract` must have a docstring

```python
    def test_1(self):
        self.assertIsNotNone(subtract.__doc__, "--err-t1--")
```

`subtract(5, 3)` must return `2`

```python
    def test_2(self):
        self.assertEqual(subtract(5, 3), 2, "--err-t2--")
```

`subtract(0, 4)` must return `-4`

```python
    def test_3(self):
        self.assertEqual(subtract(0, 4), -4, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def subtract(a, b):
    """Returns b subtracted from a."""
    return a - b
```
