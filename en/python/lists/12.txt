<section id="options">
{
"language": "python",
"type": "fillEmptySpaces"
}
</section>
<section id="description">
There may be times when we want to change our tuple into a list.
In order to do this, we can use the `list()` function
</section>
<section id="instructions">
Use the `list()` function to convert the tuple into a list
</section>
<section id="fillEmptySpaces">
{
    "seed": ["tuple1 = (\"dog\", \"bear\", \"cat\")\nlist1 = ", "[/]", "\nlist1.append(\"frog\")\nprint(list1)"],
	"buttons": ["list[tuple1]", "list(tuple1)"],
	"correct": "tuple1 = (\"dog\", \"bear\", \"cat\")\nlist1 = list(tuple1)\nlist1.append(\"frog\")\nprint(list1)"
}
</section>
<section id="preOutput">
['dog', 'bear', 'cat', 'frog']
</section>