---
language: dart
exerciseType: 4
---

# --instructions--

Sort the lines of code to count how many times each word appears in a list, using `.update()` with `ifAbsent`, and print the resulting map.

# --answers--

```dart
void main() {
```

```dart
  var counts = <String, int>{};
```

```dart
  for (var word in ['a', 'b', 'a']) {
```

```dart
    counts.update(word, (n) => n + 1, ifAbsent: () => 1);
```

```dart
  }
```

```dart
  print(counts);
```

```dart
}
```

# --solutions--

```dart
void main() {
  var counts = <String, int>{};
  for (var word in ['a', 'b', 'a']) {
    counts.update(word, (n) => n + 1, ifAbsent: () => 1);
  }
  print(counts);
}
```

# --output--

{a: 2, b: 1}
