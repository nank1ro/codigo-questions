---
language: dart
exerciseType: 1
---

# --description--

An empty map literal `{}` has no pairs to infer the types from, so give it explicit types with `<K, V>{}` or with a type annotation:

```dart
var cart = <String, int>{};
Map<String, int> other = {};
```

The `.isEmpty` property is `true` when a map has no pairs, and `.isNotEmpty` is `true` when it has at least one:

```dart
print(cart.isEmpty); // true
cart['pen'] = 2;
print(cart.isNotEmpty); // true
```

# --instructions--

Create an empty `Map<String, int>` called `cart`. Store whether it `.isEmpty` in a `bool` called `wasEmpty`, then add the key `'pen'` with value `2` and store whether it `.isNotEmpty` in a `bool` called `hasItems`.

# --before-seed--

```dart
dynamic result() {
```

# --after-seed--

```dart
  return [wasEmpty, hasItems, cart];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`wasEmpty` should be `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasItems` should be `true`

```dart
  test('test2', () {
    expect(result()[1], true, reason: '--err-t2--');
  });
```

`cart` should be `{'pen': 2}`

```dart
  test('test3', () {
    expect(result()[2], {'pen': 2}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var cart = <String, int>{};
bool wasEmpty = cart.isEmpty;
cart['pen'] = 2;
bool hasItems = cart.isNotEmpty;
```
