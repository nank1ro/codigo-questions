---
language: dart
exerciseType: 2
---

# --description--

Assigning with `map[key] = value` either **adds** a new pair, when the key is not in the map yet, or **updates** the value stored under an existing key:

```dart
var ages = {'Ann': 30};
ages['Bob'] = 25; // adds Bob
ages['Ann'] = 31; // updates Ann
print(ages); // {Ann: 31, Bob: 25}
```

New keys are appended after the existing ones, so a map remembers the insertion order.

# --instructions--

Complete the code to update the count of `apple` to `5` and then add `pear` with a count of `2`.

# --seed--

```dart
var stock = {'apple': 3};
stock[/] = 5;
stock[/] = 2;
print(stock);
```

# --answers--

- ['apple']
- ['pear']
- .apple
- ('pear')
- 'apple'
- [0]

# --solutions--

```dart
var stock = {'apple': 3};
stock['apple'] = 5;
stock['pear'] = 2;
print(stock);
```

# --output--

{apple: 5, pear: 2}
