---
language: dart
exerciseType: 1
---

# --description--

The `.putIfAbsent(key, ifAbsent)` method adds a pair **only if** the key is not in the map yet. The second argument is a function that produces the value. If the key already exists, the map is left untouched. In both cases the value now stored under the key is returned:

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99); // Ann is already there, nothing changes
ages.putIfAbsent('Bob', () => 25); // Bob is added
print(ages); // {Ann: 30, Bob: 25}
```

# --instructions--

Given the map `ages`, use `.putIfAbsent()` twice: first for the key `'Ann'` with the value `99`, then for the key `'Bob'` with the value `25`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var ages = {'Ann': 30};
```

# --after-seed--

```dart
  return ages;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`ages` should be `{'Ann': 30, 'Bob': 25}`

```dart
  test('test1', () {
    expect(result(), {'Ann': 30, 'Bob': 25}, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99);
ages.putIfAbsent('Bob', () => 25);
```
