---
language: dart
exerciseType: 2
---

# --description--

The `.remove(key)` method deletes a key and its value from the map. It returns the removed value, or `null` if the key was not there:

```dart
var ages = {'Ann': 30, 'Bob': 25};
int? removed = ages.remove('Ann');
print(removed); // 30
print(ages); // {Bob: 25}
```

The `.length` property returns the number of key-value pairs:

```dart
print(ages.length); // 1
```

# --instructions--

Complete the code to remove `Bob` from the map and then print how many pairs are left.

# --seed--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.[/]('Bob');
print(ages.[/]);
```

# --answers--

- remove
- length
- delete
- removeAt
- size
- count

# --solutions--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.remove('Bob');
print(ages.length);
```

# --output--

2
