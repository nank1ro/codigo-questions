---
language: dart
exerciseType: 4
---

# --description--

Many methods of Dart collections take a function as an argument, and anonymous functions are the natural way to pass one. The simplest is `forEach`, which calls the given function once for every element of a list:

```dart
var fruits = ['apple', 'kiwi'];

fruits.forEach((fruit) {
  print('I like $fruit');
});
// I like apple
// I like kiwi
```

The parameter type is inferred from the list, so `fruit` is a `String` without writing it.

# --instructions--

Sort the items to print a greeting for each name in `names` using `forEach`. The program must print:

```
Hi Ann
Hi Bob
```

# --answers--

```dart
void main() {
```

```dart
  var names = ['Ann', 'Bob'];
```

```dart
  names.forEach((name) {
```

```dart
    print('Hi $name');
```

```dart
  });
```

```dart
}
```

# --solutions--

```dart
void main() {
  var names = ['Ann', 'Bob'];
  names.forEach((name) {
    print('Hi $name');
  });
}
```

# --output--

Hi Ann
Hi Bob
