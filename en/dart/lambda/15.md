---
language: dart
exerciseType: 1
---

# --description--

An anonymous function does not have to be stored anywhere: it can be **called immediately** by writing the arguments right after its closing brace. This is handy to compute a value with a few temporary variables that should not leak into the surrounding code:

```dart
var area = (double radius) {
  var pi = 3.14;
  return pi * radius * radius;
}(2.0);

print(area); // 12.56
```

Here `pi` exists only inside the anonymous function, and `area` receives the returned value.

# --instructions--

An `int` variable `limit` is already defined. Declare a variable `sum` initialized by immediately invoking an anonymous function with no parameters that adds up all the integers from `1` to `limit` with a loop and returns the total.

# --before-seed--

```dart
dynamic result(int limit) {
```

# --seed--

```dart
var sum = 
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` should be an `int`

```dart
  test('test1', () {
    expect(result(100), isA<int>(), reason: '--err-t1--');
  });
```

`sum` should be `5050` when `limit` is `100`

```dart
  test('test2', () {
    expect(result(100), 5050, reason: '--err-t2--');
  });
```

`sum` should be `55` when `limit` is `10`

```dart
  test('test3', () {
    expect(result(10), 55, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var sum = () {
  var total = 0;
  for (var i = 1; i <= limit; i++) {
    total += i;
  }
  return total;
}();
```
