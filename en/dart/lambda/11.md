---
language: dart
exerciseType: 2
---

# --description--

A function can also **return a function**. The return type is then a function type, and the body returns an anonymous function:

```dart
int Function(int) makeAdder(int amount) {
  return (int n) => n + amount;
}

var addTen = makeAdder(10);
print(addTen(5)); // 15
```

Notice that the returned function still uses `amount`, a parameter of `makeAdder`, even after `makeAdder` has finished. A function that remembers the variables around it like this is called a **closure**.

# --instructions--

Complete `makeMultiplier` so that it returns a function that multiplies its argument by `factor`, then call the returned function so that the program prints `15`.

# --seed--

```dart
int Function(int) makeMultiplier(int factor) {
  return ([/] n) => n [/] factor;
}

void main() {
  var triple = makeMultiplier(3);
  print(triple([/]));
}
```

# --answers--

- int
- *
- 5
- +
- 15
- String

# --solutions--

```dart
int Function(int) makeMultiplier(int factor) {
  return (int n) => n * factor;
}

void main() {
  var triple = makeMultiplier(3);
  print(triple(5));
}
```

# --output--

15
