---
language: dart
exerciseType: 1
---

# --instructions--

Declare three top-level `int` variables: `number` with the value `42`, `min` with the value `1`, and `max` with the value `100`. Then declare a top-level `bool` variable named `inRange` that is `true` when `number` is greater than or equal to `min` AND less than or equal to `max`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`inRange` must be `true` and of type `bool`

```dart
  test('test1', () {
    expect(inRange, true, reason: '--err-t1--');
    expect(inRange, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int number = 42;
int min = 1;
int max = 100;
bool inRange = number >= min && number <= max;

void main() {
  print(inRange);
}
```
