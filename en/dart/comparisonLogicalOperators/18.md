---
language: dart
exerciseType: 1
---

# --instructions--

Declare three top-level `int` variables: `x` with the value `5`, `y` with the value `3`, and `z` with the value `0`. Then declare a top-level `bool` variable named `result` that evaluates `!(x > 10) || (y < 5 && z == 0)`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` must be `true` and of type `bool`

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 5;
int y = 3;
int z = 0;
bool result = !(x > 10) || (y < 5 && z == 0);

void main() {
  print(result);
}
```
