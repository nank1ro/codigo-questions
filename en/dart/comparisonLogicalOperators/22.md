---
language: dart
exerciseType: 4
---

# --instructions--

Sort the lines of code to declare two boolean variables, combine them with `||` and `&&` as shown, store the result in `outcome`, and print it. The output should be `true`.

# --answers--

```dart
void main() {
```

```dart
  bool hasPass = false;
```

```dart
  bool isAdmin = true;
```

```dart
  bool outcome = hasPass || isAdmin && !hasPass;
```

```dart
  print(outcome);
```

```dart
}
```

# --solutions--

```dart
void main() {
  bool hasPass = false;
  bool isAdmin = true;
  bool outcome = hasPass || isAdmin && !hasPass;
  print(outcome);
}
```

# --output--

true
