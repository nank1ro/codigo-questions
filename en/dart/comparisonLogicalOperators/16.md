---
language: dart
exerciseType: 1
---

# --instructions--

The **NOT operator** `!` inverts a boolean value: `!true` becomes `false` and `!false` becomes `true`.

Declare a top-level `bool` variable named `isOffline` with the value `false`. Then declare a top-level `bool` variable named `isOnline` that stores the negation of `isOffline` using the `!` operator.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isOnline` must be `true` and of type `bool`

```dart
  test('test1', () {
    expect(isOnline, true, reason: '--err-t1--');
    expect(isOnline, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isOffline = false;
bool isOnline = !isOffline;

void main() {
  print(isOnline);
}
```
