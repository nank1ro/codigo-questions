---
language: dart
exerciseType: 4
---

# --instructions--

Sort the lines of code to declare a temperature value, check whether it is within the range `0` to `100` inclusive using `&&`, store the result in `isValid`, and print it. The output should be `true`.

# --answers--

```dart
void main() {
```

```dart
  int temperature = 37;
```

```dart
  bool isValid = temperature >= 0 && temperature <= 100;
```

```dart
  print(isValid);
```

```dart
}
```

# --solutions--

```dart
void main() {
  int temperature = 37;
  bool isValid = temperature >= 0 && temperature <= 100;
  print(isValid);
}
```

# --output--

true
