---
language: dart
exerciseType: 2
---

# --description--

The **equal operator** `==` compares two values and returns `true` if they are equal, or `false` if they are not.

```dart
int a = 5;
int b = 5;
bool result = a == b; // true
```

The result of a comparison is always a `bool` value.

# --instructions--

Complete the code to check if `10` is equal to `10` and store the result in `isEqual`.

# --seed--

```dart
void main() {
  bool isEqual = 10 [/] 10;
  print(isEqual);
}
```

# --answers--

- ==
- !=
- =
- >=

# --solutions--

```dart
void main() {
  bool isEqual = 10 == 10;
  print(isEqual);
}
```

# --output--

true
