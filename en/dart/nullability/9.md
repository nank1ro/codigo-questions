---
language: dart
exerciseType: 1
---

# --description--

Checking a nullable value with `if` is safer than `!`, and Dart rewards you for it. After a check like `if (x != null)`, the compiler knows that `x` cannot be `null` inside the block, so it treats `x` as non-nullable there. This is called **type promotion**:

```dart
int twice(int? n) {
  if (n != null) {
    return n * 2; // here n is an int, no ! needed
  }
  return 0;
}
```

Promotion also works after an early return:

```dart
int twice(int? n) {
  if (n == null) return 0;
  return n * 2; // n is an int from here on
}
```

Promotion applies to **local variables and parameters**, whose value cannot change behind your back between the check and the use.

# --instructions--

Write a function `half` that takes a nullable `int` parameter `n` and returns `n` divided by `2` with integer division `~/`, or `0` when `n` is `null`. Use an `if` null check, not `!`.

Example:
```dart
print(half(10));   // 5
print(half(null)); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`half(10)` should be `5`

```dart
  test('test1', () {
    expect(half(10), 5, reason: '--err-t1--');
  });
```

`half(7)` should be `3`

```dart
  test('test2', () {
    expect(half(7), 3, reason: '--err-t2--');
  });
```

`half(null)` should be `0`

```dart
  test('test3', () {
    expect(half(null), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int half(int? n) {
  if (n != null) {
    return n ~/ 2;
  }
  return 0;
}
```
