---
language: dart
exerciseType: 1
---

# --description--

Often a missing value should be replaced by a **default**. The **if-null** operator `??` returns the left operand when it is not `null`, and the right operand otherwise:

```dart
String? nickname;
print(nickname ?? 'anonymous'); // anonymous

nickname = 'Ada';
print(nickname ?? 'anonymous'); // Ada
```

`??` only reacts to `null`: an empty string `''` or the number `0` are real values, so they are kept.

`??` combines nicely with `?.`, because `?.` produces a nullable result:

```dart
String? text;
print(text?.length ?? 0); // 0
```

# --instructions--

Write a function `greet` that takes a nullable `String` parameter `name` and returns `'Hello, '` followed by `name`, or by `'stranger'` when `name` is `null`. Use `??`.

Example:
```dart
print(greet('Ada')); // Hello, Ada
print(greet(null));  // Hello, stranger
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greet('Ada')` should be `'Hello, Ada'`

```dart
  test('test1', () {
    expect(greet('Ada'), 'Hello, Ada', reason: '--err-t1--');
  });
```

`greet(null)` should be `'Hello, stranger'`

```dart
  test('test2', () {
    expect(greet(null), 'Hello, stranger', reason: '--err-t2--');
  });
```

`greet('')` should be `'Hello, '`, because an empty string is not `null`

```dart
  test('test3', () {
    expect(greet(''), 'Hello, ', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String greet(String? name) {
  return 'Hello, ${name ?? 'stranger'}';
}
```
