---
language: dart
exerciseType: 1
---

# --description--

Sometimes **you** know that a nullable value is not `null` at a certain point, even if the compiler cannot tell. The **null assertion** operator `!` turns a `String?` into a `String` by promising that the value is present:

```dart
String? text = 'Dart';
String sure = text!;
print(sure.length); // 4
```

Be careful: `!` moves the check from compile time to run time. If the value **is** `null`, the program throws an error and stops:

```dart
String? text;
print(text!.length); // Null check operator used on a null value
```

Use `!` sparingly, and only when a `null` there would be a bug anyway.

# --instructions--

Write a function `shout` that takes a nullable `String` parameter `text` and returns it in upper case followed by `!`. Use the `!` operator, so that the function throws when `text` is `null`.

Example:
```dart
print(shout('hello')); // HELLO!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shout('hello')` should be `'HELLO!'`

```dart
  test('test1', () {
    expect(shout('hello'), 'HELLO!', reason: '--err-t1--');
  });
```

`shout('Dart')` should be `'DART!'`

```dart
  test('test2', () {
    expect(shout('Dart'), 'DART!', reason: '--err-t2--');
  });
```

`shout(null)` should throw

```dart
  test('test3', () {
    expect(() => shout(null), throwsA(anything), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String shout(String? text) {
  return '${text!.toUpperCase()}!';
}
```
