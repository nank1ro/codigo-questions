---
language: dart
exerciseType: 2
---

# --description--

Calling a method or reading a property on `null` would crash, so Dart does not let you do it on a nullable value with the usual dot:

```dart
String? text;
print(text.length); // error: the property 'length' can't be unconditionally accessed
```

The **null-aware access** operator `?.` solves this: if the value is `null` the whole expression is `null` and nothing else is evaluated, otherwise it works like a normal `.`:

```dart
String? text = 'Dart';
print(text?.length); // 4

text = null;
print(text?.length); // null
```

Since the result may be `null`, its type is nullable: `text?.length` is an `int?`, not an `int`.

# --instructions--

Complete the function `lengthOf` so that it accepts a nullable `String` and returns its length, or `null` when the text is missing. The program must print `4` and then `null`.

# --seed--

```dart
int? lengthOf([/] text) {
  return text[/]length;
}

void main() {
  print(lengthOf('Dart'));
  print(lengthOf(null));
}
```

# --answers--

- String?
- ?.
- String
- .
- !.
- ??

# --solutions--

```dart
int? lengthOf(String? text) {
  return text?.length;
}

void main() {
  print(lengthOf('Dart'));
  print(lengthOf(null));
}
```

# --output--

4
null
