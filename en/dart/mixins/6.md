---
language: dart
exerciseType: 4
---

# --description--

Putting the pieces together, a program that uses a mixin has three parts: the `mixin` declaration, one or more classes that pick it up `with`, and the code that calls the shared member.

```dart
mixin Barker {
  String bark() => 'Woof!';
}

class Dog with Barker {}

void main() {
  print(Dog().bark()); // Woof!
}
```

Top-level declarations can be written in any order in Dart, but reading a file top to bottom is easier when the mixin comes before the classes that use it.

# --instructions--

Sort the items to define a mixin `Purrer` with a method `purr` returning `Purr...`, a class `Cat` that uses it, and a `main` that prints the result of `purr`. Define the mixin first, then the class, then `main`. The program must print:

```
Purr...
```

# --answers--

```dart
mixin Purrer {
```

```dart
  String purr() => 'Purr...';
```

```dart
}
```

```dart
class Cat with Purrer {}
```

```dart
void main() {
```

```dart
  print(Cat().purr());
```

```dart
}
```

# --solutions--

```dart
mixin Purrer {
  String purr() => 'Purr...';
}

class Cat with Purrer {}

void main() {
  print(Cat().purr());
}
```

# --output--

Purr...
