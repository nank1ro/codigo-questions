---
language: dart
exerciseType: 1
---

# --description--

The body of a mixin looks like the body of a class: methods, getters and fields, written exactly the same way. The difference is what you can do with the declaration itself.

```dart
mixin Timestamped {
  String get stamp => '[log]';

  String format(String text) => '$stamp $text';
}

class Server with Timestamped {}
class Printer with Timestamped {}
```

The name of a mixin is also a **type**, so `Server() is Timestamped` is `true` and a variable can be declared as `Timestamped t = Server();`. Two unrelated classes now share one implementation without either of them inheriting from the other.

# --instructions--

Declare a mixin `Shouter` with a method `shout` that takes a `String` `text` and returns it in uppercase followed by an exclamation mark.

Declare a class `Speaker` that uses `Shouter` and adds nothing of its own.

Example:
```dart
print(Speaker().shout('hi')); // HI!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Speaker().shout('hi')` should be `'HI!'`

```dart
  test('test1', () {
    expect(Speaker().shout('hi'), 'HI!', reason: '--err-t1--');
  });
```

`Speaker().shout('Dart rocks')` should be `'DART ROCKS!'` and `Speaker().shout('')` should be `'!'`

```dart
  test('test2', () {
    expect(Speaker().shout('Dart rocks'), 'DART ROCKS!', reason: '--err-t2--');
    expect(Speaker().shout(''), '!', reason: '--err-t2--');
  });
```

`Speaker` should get `shout` from the mixin, so a `Speaker` should also be a `Shouter`

```dart
  test('test3', () {
    expect(Speaker(), isA<Shouter>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Shouter {
  String shout(String text) => '${text.toUpperCase()}!';
}

class Speaker with Shouter {}
```
