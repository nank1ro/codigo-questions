---
language: dart
exerciseType: 1
---

# --description--

The class body sits at the very end of the chain, so a member declared in the class **overrides** the same member coming from any of its mixins. Inside the override, **`super`** reaches the version the mixin provided:

```dart
mixin Polite {
  String greet() => 'Hello';
}

class Host with Polite {
  @override
  String greet() => '${super.greet()}, welcome!';
}

class Guest with Polite {}

void main() {
  print(Host().greet());  // Hello, welcome!
  print(Guest().greet()); // Hello
}
```

The mixin itself is untouched: `Guest` still gets the original `greet`. `super.greet()` is what lets `Host` build on the shared behaviour instead of copying it.

# --instructions--

Declare a mixin `Signed` with a method `sign` returning `'signed'`.

Declare a class `Letter` that uses `Signed` and overrides `sign` so that it returns the mixin's result followed by `' by Ada'`, obtained by calling `super`.

Declare a class `Note` that uses `Signed` and overrides nothing.

Example:
```dart
print(Letter().sign()); // signed by Ada
print(Note().sign());   // signed
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Letter().sign()` should be `'signed by Ada'`

```dart
  test('test1', () {
    expect(Letter().sign(), 'signed by Ada', reason: '--err-t1--');
  });
```

`Note().sign()` should still be `'signed'`, because the mixin is unchanged

```dart
  test('test2', () {
    expect(Note().sign(), 'signed', reason: '--err-t2--');
  });
```

Both classes should use the mixin, and the override should be used through the mixin type too

```dart
  test('test3', () {
    final Signed letter = Letter();
    expect(letter.sign(), 'signed by Ada', reason: '--err-t3--');
    expect(Note(), isA<Signed>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Signed {
  String sign() => 'signed';
}

class Letter with Signed {
  @override
  String sign() => '${super.sign()} by Ada';
}

class Note with Signed {}
```
