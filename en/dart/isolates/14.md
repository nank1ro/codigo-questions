---
language: dart
exerciseType: 4
---

# --description--

Put together, an `Isolate.run` program reads like ordinary sequential code: the line before the call runs on the main isolate, the computation runs elsewhere, and the line after the `await` runs back on the main isolate with the result in hand.

```dart
import 'dart:isolate';

int double(int n) => n * 2;

Future<void> main() async {
  print('start');
  final result = await Isolate.run(() => double(4));
  print(result);
}
// start
// 8
```

# --instructions--

Sort the items to build a program that prints `start`, squares `9` on another isolate with `Isolate.run` and prints the result. Put the import first and define `square` before `main`. The program must print:

```
start
81
```

# --answers--

```dart
import 'dart:isolate';
```

```dart
int square(int n) => n * n;
```

```dart
Future<void> main() async {
```

```dart
  print('start');
```

```dart
  final result = await Isolate.run(() => square(9));
```

```dart
  print(result);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
int square(int n) => n * n;
Future<void> main() async {
  print('start');
  final result = await Isolate.run(() => square(9));
  print(result);
}
```

# --output--

start
81
