---
language: dart
exerciseType: 1
---

# --description--

`Stream.fromIterable` needs all the values up front. To **produce** values one at a time, write an **asynchronous generator**: a function whose body is marked `async*` and whose return type is `Stream<T>`. Inside it, `yield` sends one event to the stream:

```dart
Stream<int> countTo(int n) async* {
  for (var i = 1; i <= n; i++) {
    yield i;
  }
}
```

The body does not run when you call `countTo(3)`: it runs lazily, as the listener asks for values, and the stream is done when the body finishes.

To collect every event into a `List`, call `toList()`. It returns a `Future<List<T>>`, so you `await` it:

```dart
final values = await countTo(3).toList();
print(values); // [1, 2, 3]
```

# --instructions--

Write an `async*` function `countdown` that takes an `int` `from` and returns a `Stream<int>` yielding the values from `from` down to `1`. If `from` is `0` or less, the stream emits nothing.

Example:
```dart
print(await countdown(3).toList()); // [3, 2, 1]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countdown(3)` should emit `3`, `2`, `1`

```dart
  test('test1', () async {
    expect(await countdown(3).toList(), [3, 2, 1], reason: '--err-t1--');
  });
```

`countdown(1)` should emit only `1`

```dart
  test('test2', () async {
    expect(await countdown(1).toList(), [1], reason: '--err-t2--');
  });
```

`countdown(0)` should emit nothing and `countdown` should return a `Stream<int>`

```dart
  test('test3', () async {
    expect(await countdown(0).toList(), <int>[], reason: '--err-t3--');
    expect(countdown(2), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> countdown(int from) async* {
  for (var i = from; i >= 1; i--) {
    yield i;
  }
}
```
