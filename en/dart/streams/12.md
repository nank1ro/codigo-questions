---
language: dart
exerciseType: 4
---

# --description--

A `StreamController` is often created and consumed in the same place: subscribe to `controller.stream` with `listen`, then `add` events and `close` the controller. Because `listen` does not wait, the events are delivered after the current code finishes, but always in the order they were added:

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>();
  controller.stream.listen((n) => print('got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
// got 1
// got 2
```

# --instructions--

Sort the items to import `dart:async`, create a `StreamController<String>`, subscribe to its stream printing each value, add `hello` and then `world`, and finally close the controller. Subscribe with `listen` before adding any value. The program must print:

```
hello
world
```

# --answers--

```dart
import 'dart:async';
```

```dart
void main() {
```

```dart
  final controller = StreamController<String>();
```

```dart
  controller.stream.listen((word) => print(word));
```

```dart
  controller.add('hello');
```

```dart
  controller.add('world');
```

```dart
  controller.close();
```

```dart
}
```

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>();
  controller.stream.listen((word) => print(word));
  controller.add('hello');
  controller.add('world');
  controller.close();
}
```

# --output--

hello
world
