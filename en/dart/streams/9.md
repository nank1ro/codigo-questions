---
language: dart
exerciseType: 1
---

# --description--

Besides `toList()`, a stream offers other methods that **consume** all its events and return a single `Future`:

- `first` and `last` complete with the first or last event
- `length` completes with the number of events
- `join(separator)` completes with all events joined into one `String`
- `reduce(combine)` combines the events two at a time into one value

```dart
final numbers = Stream.fromIterable([3, 9, 2]);
print(await numbers.reduce((a, b) => a + b)); // 14
```

`reduce` calls `combine` with the result so far and the next event. It throws if the stream is empty, so only use it when at least one event is guaranteed.

# --instructions--

Write two functions:
- `largest`, which takes a `Stream<int>` `source` and returns a `Future<int>` completing with the largest value, using `reduce`. The stream always has at least one event.
- `sentence`, which takes a `Stream<String>` `words` and returns a `Future<String>` completing with all the words joined by a single space.

Example:
```dart
print(await largest(Stream.fromIterable([3, 9, 2])));        // 9
print(await sentence(Stream.fromIterable(['I', 'am', 'Dart']))); // I am Dart
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest` of a stream of `3, 9, 2` should complete with `9`

```dart
  test('test1', () async {
    expect(await largest(Stream.fromIterable([3, 9, 2])), 9, reason: '--err-t1--');
  });
```

`largest` of a stream of `-4, -1, -7` should complete with `-1` and `largest` of a stream of `5` should complete with `5`

```dart
  test('test2', () async {
    expect(await largest(Stream.fromIterable([-4, -1, -7])), -1, reason: '--err-t2--');
    expect(await largest(Stream.fromIterable([5])), 5, reason: '--err-t2--');
  });
```

`sentence` of a stream of `'I', 'am', 'Dart'` should complete with `'I am Dart'`

```dart
  test('test3', () async {
    expect(await sentence(Stream.fromIterable(['I', 'am', 'Dart'])), 'I am Dart', reason: '--err-t3--');
    expect(await sentence(Stream.fromIterable(['solo'])), 'solo', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> largest(Stream<int> source) {
  return source.reduce((a, b) => a > b ? a : b);
}

Future<String> sentence(Stream<String> words) {
  return words.join(' ');
}
```
