---
language: dart
exerciseType: 1
---

# --description--

The streams seen so far are **single-subscription**: they allow exactly one listener. Calling `listen`, `await for` or any consuming method a second time throws a `StateError` ("Stream has already been listened to").

To share a stream between several listeners, convert it to a **broadcast** stream with `asBroadcastStream()`:

```dart
final shared = Stream.fromIterable([1, 2, 3]).asBroadcastStream();
final total = shared.reduce((a, b) => a + b);
final count = shared.length;
print(await total); // 6
print(await count); // 3
```

A broadcast stream does not buffer: a listener only receives the events emitted **after** it subscribed. In the example both listeners subscribe before the first `await`, so both receive every event.

# --instructions--

Write a function `sumAndCount` that takes a `Stream<int>` `source` and returns a `Future<List<int>>` completing with a list of two elements: the sum of the values and the number of values. Convert `source` to a broadcast stream, start both consuming calls **before** awaiting either of them, then await them. The stream always has at least one event.

Example:
```dart
print(await sumAndCount(Stream.fromIterable([1, 2, 3]))); // [6, 3]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumAndCount` of a stream of `1, 2, 3` should complete with `[6, 3]`

```dart
  test('test1', () async {
    expect(await sumAndCount(Stream.fromIterable([1, 2, 3])), [6, 3], reason: '--err-t1--');
  });
```

`sumAndCount` of a stream of `10, -4` should complete with `[6, 2]`

```dart
  test('test2', () async {
    expect(await sumAndCount(Stream.fromIterable([10, -4])), [6, 2], reason: '--err-t2--');
  });
```

`sumAndCount` of a stream of `7` should complete with `[7, 1]`

```dart
  test('test3', () async {
    expect(await sumAndCount(Stream.fromIterable([7])), [7, 1], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<List<int>> sumAndCount(Stream<int> source) async {
  final shared = source.asBroadcastStream();
  final sum = shared.reduce((a, b) => a + b);
  final count = shared.length;
  return [await sum, await count];
}
```
