---
language: dart
exerciseType: 2
---

# --description--

Because `where`, `map` and `take` each return a stream, you can chain them and finish with `toList()` to get the result as a list. Only the final `toList()` needs an `await`, since it is the only call that returns a `Future`:

```dart
final numbers = Stream.fromIterable([5, 12, 8, 20, 3]);
final big = await numbers.where((n) => n > 6).take(2).toList();
print(big); // [12, 8]
```

# --instructions--

Complete the code to keep only the words with exactly `3` letters, stop after the first `2` of them, and collect them into a list, so that the program prints `[sun, sky]`.

# --seed--

```dart
Future<void> main() async {
  final words = Stream.fromIterable(['sun', 'moon', 'star', 'sky', 'fog']);
  final short = await words.[/]((w) => w.length == 3).[/](2).[/]();
  print(short);
}
```

# --answers--

- where
- take
- toList
- map
- skip
- first

# --solutions--

```dart
Future<void> main() async {
  final words = Stream.fromIterable(['sun', 'moon', 'star', 'sky', 'fog']);
  final short = await words.where((w) => w.length == 3).take(2).toList();
  print(short);
}
```

# --output--

[sun, sky]
