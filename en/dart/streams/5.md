---
language: dart
exerciseType: 2
---

# --description--

`await for` pauses the current function until the stream is done. When you want to react to events **without waiting**, call `listen` and pass a callback: it is invoked once per event, and the code after `listen` runs right away.

```dart
final ticks = Stream.fromIterable([1, 2]);
ticks.listen((tick) => print('tick $tick'));
print('not waiting');
// not waiting
// tick 1
// tick 2
```

`listen` also accepts an `onDone` named parameter, a function with no arguments called when the stream ends:

```dart
final ticks2 = Stream.fromIterable([1, 2]);
ticks2.listen((tick) => print(tick), onDone: () => print('no more ticks'));
```

# --instructions--

Complete the code so that the stream is listened to, each letter is printed, and `done` is printed when the stream ends. The program must print `listening`, then `a`, `b` and `done`.

# --seed--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.[/]((letter) => print(letter), [/]: () => print('done'));
  print('listening');
}
```

# --answers--

- listen
- onDone
- onError
- forEach
- await

# --solutions--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.listen((letter) => print(letter), onDone: () => print('done'));
  print('listening');
}
```

# --output--

listening
a
b
done
