---
language: dart
exerciseType: 1
---

# --description--

Generators produce events from inside one function. When events come from **elsewhere** (a button, a network callback, another object) you need a **`StreamController`**. It lives in the `dart:async` library, so the file must start with `import 'dart:async';`.

A controller owns a stream and lets you push events into it:

```dart
import 'dart:async';

Stream<int> dice() {
  final controller = StreamController<int>();
  controller.add(4);
  controller.add(2);
  controller.close();
  return controller.stream;
}
```

- `add(value)` sends one event
- `close()` ends the stream; forgetting it means listeners wait forever
- `stream` is the `Stream` that listeners consume

Events added before anyone listens are kept in a buffer, so the code above is safe: a listener that arrives later still receives `4` and `2`.

# --instructions--

Write a function `announce` that takes a `List<String>` `names` and returns a `Stream<String>` built with a `StreamController`: add each name in uppercase, close the controller and return its stream. Remember the `dart:async` import.

Example:
```dart
print(await announce(['ada', 'linus']).toList()); // [ADA, LINUS]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`announce(['ada', 'linus'])` should emit `'ADA'`, `'LINUS'`

```dart
  test('test1', () async {
    expect(await announce(['ada', 'linus']).toList(), ['ADA', 'LINUS'], reason: '--err-t1--');
  });
```

`announce(['Dart'])` should emit `'DART'`

```dart
  test('test2', () async {
    expect(await announce(['Dart']).toList(), ['DART'], reason: '--err-t2--');
  });
```

`announce([])` should emit nothing and end, and `announce` should return a `Stream<String>`

```dart
  test('test3', () async {
    expect(await announce([]).toList(), <String>[], reason: '--err-t3--');
    expect(announce(['x']), isA<Stream<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
import 'dart:async';

Stream<String> announce(List<String> names) {
  final controller = StreamController<String>();
  for (final name in names) {
    controller.add(name.toUpperCase());
  }
  controller.close();
  return controller.stream;
}
```
