---
language: dart
exerciseType: 1
---

# --description--

The last piece is throwing an `Error` on purpose. A function that documents what it accepts should refuse anything else loudly, and `ArgumentError` is the object for that:

```dart
int setVolume(int level) {
  if (level < 0 || level > 100) {
    throw ArgumentError('level must be between 0 and 100');
  }
  return level;
}
```

The message is reachable as `e.message`, and `toString()` prints `Invalid argument(s): ` followed by it.

This does not contradict the rule from before. Throwing an `ArgumentError` is right, catching one is not: it tells the *caller's* author that the call itself is wrong, and the fix is a check before the call, not a handler around it.

# --instructions--

Write a function `checkAge` that takes an `int` `age` and returns it, but throws `ArgumentError('age must be between 0 and 120')` when the age is below `0` or above `120`.

Example:
```dart
print(checkAge(30)); // 30
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`checkAge(30)` should return `30`, and the two ends of the range should be accepted

```dart
  test('test1', () {
    expect(checkAge(30), 30, reason: '--err-t1--');
    expect(checkAge(0), 0, reason: '--err-t1--');
    expect(checkAge(120), 120, reason: '--err-t1--');
  });
```

`checkAge(-1)` and `checkAge(121)` should throw an `ArgumentError`

```dart
  test('test2', () {
    expect(() => checkAge(-1), throwsArgumentError, reason: '--err-t2--');
    expect(() => checkAge(121), throwsArgumentError, reason: '--err-t2--');
  });
```

The thrown error should carry the message `'age must be between 0 and 120'`

```dart
  test('test3', () {
    try {
      checkAge(200);
      fail('--err-t3--');
    } on ArgumentError catch (e) {
      expect(e.message, 'age must be between 0 and 120', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int checkAge(int age) {
  if (age < 0 || age > 120) {
    throw ArgumentError('age must be between 0 and 120');
  }
  return age;
}
```
