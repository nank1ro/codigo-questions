---
language: dart
exerciseType: 1
---

# --description--

Where the `try` block sits decides how much work a single failure destroys. Around a loop, the first bad element ends the whole batch; **inside** the loop, only that element is lost and the rest is still processed:

```dart
for (final text in texts) {
  try {
    total += int.parse(text);
  } on FormatException {
    continue;
  }
}
```

This is the everyday shape for importing a file, reading a list of settings or handling a queue of messages: one damaged row should not throw away the good ones. The rule stays the same as before — keep the `try` block around the statement that can fail, and no larger.

# --instructions--

Write a function `sumOf` that takes a `List<String>` `texts` and returns the sum of the ones that describe a whole number, ignoring the others.

Use `int.parse` with a `try`/`catch` inside the loop, so that one unreadable entry does not stop the rest.

Example:
```dart
print(sumOf(['1', 'x', '3'])); // 4
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumOf(['1', 'x', '3'])` should return `4`

```dart
  test('test1', () {
    expect(sumOf(['1', 'x', '3']), 4, reason: '--err-t1--');
  });
```

`sumOf(['10', '20', '30'])` should return `60`

```dart
  test('test2', () {
    expect(sumOf(['10', '20', '30']), 60, reason: '--err-t2--');
    expect(sumOf(['-5', '5']), 0, reason: '--err-t2--');
  });
```

A list with no readable number, and an empty list, should both give `0`

```dart
  test('test3', () {
    expect(sumOf(['x', 'y', '']), 0, reason: '--err-t3--');
    expect(sumOf(<String>[]), 0, reason: '--err-t3--');
    expect(sumOf(['x', '7']), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sumOf(List<String> texts) {
  var total = 0;
  for (final text in texts) {
    try {
      total += int.parse(text);
    } on FormatException {
      continue;
    }
  }
  return total;
}
```
