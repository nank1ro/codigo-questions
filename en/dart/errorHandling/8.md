---
language: dart
exerciseType: 1
---

# --description--

Sometimes a handler is not the right place to recover: you only want to *notice* the failure and let it continue to the caller who can actually deal with it. The `rethrow` keyword does that, inside a `catch` or `on ... catch` block:

```dart
try {
  return int.parse(text);
} on FormatException {
  log.add('bad input: $text');
  rethrow;
}
```

`rethrow` sends the **same** object onwards, so the caller sees the original failure. Writing `throw e` instead would also work, but it restarts the journey and loses where the failure first happened.

A `finally` block in the same statement still runs, even on the way out.

# --instructions--

Write a function `parseOrLog` that takes a `String` `text` and a `List<String>` `log`, and returns `int.parse(text)`.

When the text is not a whole number, add the string `'bad input: '` followed by the text to `log`, then rethrow the `FormatException` so the caller still sees it.

Example:
```dart
final log = <String>[];
print(parseOrLog('42', log)); // 42
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parseOrLog('42', log)` should return `42` and leave the log empty

```dart
  test('test1', () {
    final log = <String>[];
    expect(parseOrLog('42', log), 42, reason: '--err-t1--');
    expect(log, isEmpty, reason: '--err-t1--');
  });
```

`parseOrLog('oops', log)` should throw a `FormatException`

```dart
  test('test2', () {
    expect(() => parseOrLog('oops', <String>[]), throwsFormatException, reason: '--err-t2--');
  });
```

The log should collect one line per failure, and good values should not be logged

```dart
  test('test3', () {
    final log = <String>[];
    expect(() => parseOrLog('oops', log), throwsFormatException, reason: '--err-t3--');
    expect(log, ['bad input: oops'], reason: '--err-t3--');
    expect(parseOrLog('7', log), 7, reason: '--err-t3--');
    expect(() => parseOrLog('x', log), throwsFormatException, reason: '--err-t3--');
    expect(log, ['bad input: oops', 'bad input: x'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int parseOrLog(String text, List<String> log) {
  try {
    return int.parse(text);
  } on FormatException {
    log.add('bad input: $text');
    rethrow;
  }
}
```
