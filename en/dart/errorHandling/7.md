---
language: dart
exerciseType: 1
---

# --description--

Your own code throws the same way the library does. `Exception('message')` builds a plain exception carrying a short explanation, and `throw` sends it on its way:

```dart
if (amount > balance) {
  throw Exception('insufficient funds');
}
```

The message is not lost: `toString()` puts the word `Exception`, a colon and the message together, which is exactly what the unhandled-exception report prints.

```dart
print(Exception('insufficient funds')); // Exception: insufficient funds
```

Throwing beats returning a made-up value such as `-1`: the caller cannot forget to look at it, and the reason travels with it.

# --instructions--

Write a function `withdraw` that takes two `int` values `balance` and `amount` and returns the remaining balance.

When `amount` is greater than `balance`, throw `Exception('insufficient funds')` instead.

Example:
```dart
print(withdraw(100, 30)); // 70
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`withdraw(100, 30)` should return `70`

```dart
  test('test1', () {
    expect(withdraw(100, 30), 70, reason: '--err-t1--');
    expect(withdraw(50, 50), 0, reason: '--err-t1--');
  });
```

`withdraw(100, 300)` should throw an `Exception`

```dart
  test('test2', () {
    expect(() => withdraw(100, 300), throwsA(isA<Exception>()), reason: '--err-t2--');
  });
```

The thrown exception should print as `Exception: insufficient funds`

```dart
  test('test3', () {
    try {
      withdraw(0, 1);
      fail('--err-t3--');
    } catch (e) {
      expect(e.toString(), 'Exception: insufficient funds', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int withdraw(int balance, int amount) {
  if (amount > balance) {
    throw Exception('insufficient funds');
  }
  return balance - amount;
}
```
