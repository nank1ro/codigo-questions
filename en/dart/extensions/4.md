---
language: dart
exerciseType: 4
---

# --description--

The type after `on` can be a **parameterized** type such as `List<int>`. The extension then applies only to lists of that element type: `[1, 2].total()` works, `['a', 'b'].total()` does not compile.

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}
```

Inside the extension, `this` is the list, so you can loop over it, index it, or call `length` as usual.

# --instructions--

Sort the items to declare an extension `Totals` on `List<int>` with a method `total` that adds up the elements using a `for` loop, then a `main` that prints the total of `[3, 4, 5]`. Declare the extension before `main`. The program must print:

```
12
```

# --answers--

```dart
extension Totals on List<int> {
```

```dart
  int total() {
```

```dart
    var sum = 0;
```

```dart
    for (final n in this) {
```

```dart
      sum += n;
```

```dart
    }
```

```dart
    return sum;
```

```dart
  }
```

```dart
}
```

```dart
void main() {
```

```dart
  print([3, 4, 5].total());
```

```dart
}
```

# --solutions--

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}

void main() {
  print([3, 4, 5].total());
}
```

# --output--

12
