---
language: dart
exerciseType: 4
---

# --description--

Extensions are not only for SDK types: you can extend **your own classes** too. This is useful when the class comes from a package you do not control, or when you want to keep the class small and add optional helpers next to the code that needs them.

```dart
class Circle {
  final double radius;
  Circle(this.radius);
}

extension CircleMath on Circle {
  double get diameter => radius * 2;
}

void main() {
  print(Circle(3).diameter); // 6.0
}
```

The extension sees the class's public fields and methods, exactly like code written outside the class.

# --instructions--

Sort the items to define a class `Rectangle` with `width` and `height`, then an extension `RectangleMath` on `Rectangle` with a getter `area`, then a `main` that prints the area of a `4` by `2.5` rectangle. Define the class first, then the extension, then `main`. Inside the class, declare both fields before the constructor. The program must print:

```
10.0
```

# --answers--

```dart
class Rectangle {
```

```dart
  final double width;
```

```dart
  final double height;
```

```dart
  Rectangle(this.width, this.height);
```

```dart
}
```

```dart
extension RectangleMath on Rectangle {
```

```dart
  double get area => width * height;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Rectangle(4, 2.5).area);
```

```dart
}
```

# --solutions--

```dart
class Rectangle {
  final double width;
  final double height;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

```dart
class Rectangle {
  final double height;
  final double width;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

# --output--

10.0
