---
language: dart
exerciseType: 1
---

# --description--

An extension can also declare **getters**, which are read like properties, without parentheses. Inside an extension you can call the type's own members directly: `this.` is optional, exactly as inside a class.

```dart
extension Sizes on String {
  bool get isLong => length > 10;      // same as this.length
  String get firstChar => this[0];
}

void main() {
  print('Dart'.isLong);           // false
  print('extension'.firstChar);   // e
}
```

Choose a getter when the member only **reads** a value and takes no parameters; choose a method when it does work or needs arguments.

# --instructions--

Write an extension named `Blank` on `String` with a getter `isBlank` of type `bool` that is `true` when the string is empty or contains only spaces. Use `trim()` and `isEmpty`.

Example:
```dart
print('   '.isBlank);  // true
print(' hi '.isBlank); // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`''.isBlank` and `'   '.isBlank` should be `true`

```dart
  test('test1', () {
    expect(''.isBlank, true, reason: '--err-t1--');
    expect('   '.isBlank, true, reason: '--err-t1--');
  });
```

`' hi '.isBlank` should be `false`

```dart
  test('test2', () {
    expect(' hi '.isBlank, false, reason: '--err-t2--');
    expect('x'.isBlank, false, reason: '--err-t2--');
  });
```

`isBlank` should be a `bool`

```dart
  test('test3', () {
    expect('  '.isBlank, isA<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Blank on String {
  bool get isBlank => trim().isEmpty;
}
```
