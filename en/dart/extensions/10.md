---
language: dart
exerciseType: 1
---

# --description--

Dart lets a type define what operators like `+`, `*` or `==` mean for its values, using a method whose name is the keyword `operator` followed by the symbol. The right-hand side of the operator is the method's parameter:

```dart
extension Scaling on List<int> {
  List<int> operator *(int factor) => map((n) => n * factor).toList();
}

void main() {
  print([1, 2, 3] * 10); // [10, 20, 30]
}
```

Since extensions can declare operators, you can give an existing type a new operator it does not already have. `String` has `+` and `*`, but no `-`, so an extension can define what `'hello world' - 'o'` means.

# --instructions--

Write an extension named `Removing` on `String` with an operator `-` that takes a `String` parameter `part` and returns the string with every occurrence of `part` removed. Use `replaceAll`.

Example:
```dart
print('hello world' - 'o'); // hell wrld
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`'hello world' - 'o'` should be `'hell wrld'`

```dart
  test('test1', () {
    expect('hello world' - 'o', 'hell wrld', reason: '--err-t1--');
  });
```

`'banana' - 'an'` should be `'ba'`

```dart
  test('test2', () {
    expect('banana' - 'an', 'ba', reason: '--err-t2--');
  });
```

`'abc' - 'z'` should be `'abc'` and `'aaa' - 'a'` should be `''`

```dart
  test('test3', () {
    expect('abc' - 'z', 'abc', reason: '--err-t3--');
    expect('aaa' - 'a', '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Removing on String {
  String operator -(String part) => replaceAll(part, '');
}
```
