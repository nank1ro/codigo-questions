---
language: dart
exerciseType: 1
---

# --description--

An extension on `List<int>` cannot be used on a `List<String>`. To write one extension that works for **every** element type, give the extension a **type parameter**, written between angle brackets after its name, and use it in the `on` type:

```dart
extension Firsts<T> on List<T> {
  T get firstOrLast => length > 1 ? this[0] : this[length - 1];
}
```

`T` is a placeholder for "whatever the element type is": on a `List<int>` it becomes `int`, on a `List<String>` it becomes `String`, so the getter above returns an `int` or a `String` accordingly. The compiler fills in `T` for you at each call.

```dart
print([7, 8, 9].firstOrLast); // 7
print(['a', 'b'].firstOrLast); // a
```

# --instructions--

Write an extension named `Seconds` with a type parameter `T` on `List<T>`, with a getter `secondOrNull` of type `T?` that returns the element at index `1`, or `null` when the list has fewer than two elements.

Example:
```dart
print([10, 20, 30].secondOrNull); // 20
print(['solo'].secondOrNull);     // null
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`[10, 20, 30].secondOrNull` should be `20`

```dart
  test('test1', () {
    expect([10, 20, 30].secondOrNull, 20, reason: '--err-t1--');
  });
```

`['a', 'b'].secondOrNull` should be `'b'`

```dart
  test('test2', () {
    expect(['a', 'b'].secondOrNull, 'b', reason: '--err-t2--');
  });
```

`['solo'].secondOrNull` and `<int>[].secondOrNull` should be `null`

```dart
  test('test3', () {
    expect(['solo'].secondOrNull, null, reason: '--err-t3--');
    expect(<int>[].secondOrNull, null, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Seconds<T> on List<T> {
  T? get secondOrNull => length > 1 ? this[1] : null;
}
```
