---
language: dart
exerciseType: 2
---

# --description--

The **multiplication assignment operator** `*=` multiplies a variable by a value and stores the result back in that variable.

```dart
int side = 4;
side *= 3; // side is now 12
```

This is shorthand for `side = side * 3`.

# --instructions--

Complete the code to multiply `price` by `3` using `*=`.

# --seed--

```dart
void main() {
  int price = 7;
  price [/] 3;
  print(price);
}
```

# --answers--

- *=
- =
- +=
- *

# --solutions--

```dart
void main() {
  int price = 7;
  price *= 3;
  print(price);
}
```

# --output--

21
