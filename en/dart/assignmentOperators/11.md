---
language: dart
exerciseType: 1
---

# --instructions--

Declare a `double` variable `value` with value `20.0`. Then use `/=` to divide it by `4`. Note that `/=` always produces a `double` in Dart.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
double value = 20.0;
```

# --after-seed--

```dart
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value` should be `5.0`

```dart
  test('test1', () {
    expect(result(), closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;
value /= 4;
```
