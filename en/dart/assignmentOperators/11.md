---
language: dart
exerciseType: 1
---

# --instructions--

Declare a top-level `double` variable `value` with value `20.0`. Then use `/=` to divide it by `4`. Note that `/=` always produces a `double` in Dart.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value` should be `5.0`

```dart
  test('test1', () {
    expect(value, closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;

void main() {
  value /= 4;
  print(value);
}
```
