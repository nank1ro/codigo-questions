---
language: dart
exerciseType: 3
---

# --description--

Assignment operators combine an arithmetic operation with an assignment in a single step.

For example, `x += 3` is shorthand for `x = x + 3`.

# --instructions--

What does `x += 5` do when `x` is currently `10`?

# --answers--

- Sets `x` to `15`
- Sets `x` to `5`
- Sets `x` to `10`
- Sets `x` to `50`

# --solutions--

- Sets `x` to `15`
