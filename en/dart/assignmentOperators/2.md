---
language: dart
exerciseType: 2
---

# --description--

The **addition assignment operator** `+=` adds a value to a variable and stores the result back in that variable.

```dart
int score = 10;
score += 5; // score is now 15
```

This is shorthand for `score = score + 5`.

# --instructions--

Complete the code to add `8` to the variable `total` using `+=`.

# --seed--

```dart
void main() {
  int total = 12;
  total [/] 8;
  print(total);
}
```

# --answers--

- +=
- =
- -=
- +

# --solutions--

```dart
void main() {
  int total = 12;
  total += 8;
  print(total);
}
```

# --output--

20
