---
language: dart
exerciseType: 1
---

# --instructions--

A shop is applying a 20% discount to an item. Declare a top-level `double` variable `price` with value `50.0`. Then use `*=` to apply the discount by multiplying `price` by `0.8`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`price` should be `40.0`

```dart
  test('test1', () {
    expect(price, closeTo(40.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double price = 50.0;

void main() {
  price *= 0.8;
  print(price);
}
```
