---
language: dart
exerciseType: 1
---

# --instructions--

Declare a top-level `int` variable `n` with value `2`. Then, in `main`, apply the following assignment operators in order:
- `n *= 10` (multiply by 10)
- `n += 5` (add 5)
- `n -= 3` (subtract 3)

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`n` should be `22`

```dart
  test('test1', () {
    expect(n, 22, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 2;

void main() {
  n *= 10;
  n += 5;
  n -= 3;
  print(n);
}
```
