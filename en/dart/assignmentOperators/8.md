---
language: dart
exerciseType: 1
---

# --instructions--

Declare a top-level `int` variable `points` with value `20`. Then use `+=` to add `15` to it.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`points` should be `35`

```dart
  test('test1', () {
    expect(points, 35, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int points = 20;

void main() {
  points += 15;
  print(points);
}
```
