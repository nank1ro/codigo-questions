---
language: dart
exerciseType: 1
---

# --instructions--

Declare an `int` variable `points` with value `20`. Then use `+=` to add `15` to it.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int points = 20;
```

# --after-seed--

```dart
  return points;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`points` should be `35`

```dart
  test('test1', () {
    expect(result(), 35, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int points = 20;
points += 15;
```
