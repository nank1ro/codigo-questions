---
language: dart
exerciseType: 1
---

# --instructions--

Declare a top-level `int` variable `fuel` with value `100`. Then use `-=` to subtract `35` from it.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`fuel` should be `65`

```dart
  test('test1', () {
    expect(fuel, 65, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int fuel = 100;

void main() {
  fuel -= 35;
  print(fuel);
}
```
