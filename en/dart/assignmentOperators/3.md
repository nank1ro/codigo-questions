---
language: dart
exerciseType: 2
---

# --description--

The **subtraction assignment operator** `-=` subtracts a value from a variable and stores the result back in that variable.

```dart
int lives = 3;
lives -= 1; // lives is now 2
```

This is shorthand for `lives = lives - 1`.

# --instructions--

Complete the code to subtract `5` from the variable `stock` using `-=`.

# --seed--

```dart
void main() {
  int stock = 20;
  stock [/] 5;
  print(stock);
}
```

# --answers--

- -=
- =
- +=
- -

# --solutions--

```dart
void main() {
  int stock = 20;
  stock -= 5;
  print(stock);
}
```

# --output--

15
