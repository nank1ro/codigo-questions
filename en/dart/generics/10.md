---
language: dart
exerciseType: 1
---

# --description--

An unbounded `T` could be anything, so inside the body you may only use what every object has. This does not compile:

```dart
T twice<T>(T value) => value + value; // error: + is not defined for T
```

A **bound** fixes that. Writing `T extends num` says "`T` may only be a number", and in exchange the body may use everything a `num` offers:

```dart
T twice<T extends num>(T value) => (value + value) as T;

num half<T extends num>(T value) => value / 2;
```

The bound is checked at the call site: `half(4)` and `half(2.5)` are fine, `half('fig')` is a compile-time error. A bound is a promise in both directions, narrower arguments for more power inside.

# --instructions--

Write a generic function `total` with a type parameter `T` bounded by `num` that takes a `List<T>` `values` and returns the sum of the elements as a `num`. An empty list gives `0`.

Example:
```dart
print(total([1, 2, 3]));  // 6
print(total([1.5, 2.5])); // 4.0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total([1, 2, 3])` should return `6`

```dart
  test('test1', () {
    expect(total([1, 2, 3]), 6, reason: '--err-t1--');
  });
```

`total` should also work with doubles, so `total([1.5, 2.5])` should return `4.0`, and it should accept an explicit type argument like `total<int>([1, 2])`

```dart
  test('test2', () {
    expect(total([1.5, 2.5]), 4.0, reason: '--err-t2--');
    expect(total([10, 20, 30, 40]), 100, reason: '--err-t2--');
    expect(total<int>([1, 2]), 3, reason: '--err-t2--');
    expect(total<double>([1.5, 1.5]), 3.0, reason: '--err-t2--');
  });
```

`total` should return `0` for an empty list

```dart
  test('test3', () {
    expect(total(<int>[]), 0, reason: '--err-t3--');
    expect(total(<double>[]), 0, reason: '--err-t3--');
    expect(total([-5, 5, 7]), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
num total<T extends num>(List<T> values) {
  num sum = 0;
  for (final value in values) {
    sum += value;
  }
  return sum;
}
```
