---
language: dart
exerciseType: 4
---

# --description--

The same bound works just as well for the smallest element: only the sign of the comparison changes. `compareTo` returns a negative number when the receiver comes first, so `item.compareTo(best) < 0` means "this one is smaller".

# --instructions--

Sort the items to define a generic function `smallest` bounded by `Comparable<T>` that returns the smallest element of a list, and a `main` that prints the smallest of `['pear', 'fig', 'kiwi']`. Define the function before `main`. The program must print:

```
fig
```

# --answers--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
```

```dart
  var best = items.first;
```

```dart
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
```

```dart
  return best;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(smallest(['pear', 'fig', 'kiwi']));
```

```dart
}
```

# --solutions--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
  return best;
}

void main() {
  print(smallest(['pear', 'fig', 'kiwi']));
}
```

# --output--

fig
