---
language: dart
exerciseType: 4
---

# --description--

Inside a generic class the type parameter is visible everywhere: in fields, in constructor parameters, in method signatures and in method bodies. It is declared once, next to the class name, and every member can use it.

```dart
class Holder<T> {
  final T item;

  Holder(this.item);

  String describe() => 'holding $item';
}
```

Creating the object is what decides the type: `Holder<String>('fig')` makes `item` a `String`, `Holder<int>(3)` makes it an `int`.

# --instructions--

Sort the items to define a generic class `Holder` with a field `item` of type `T`, a constructor, and a method `describe`, then a `main` that prints the description of a `Holder<String>` built with `'fig'`. Define the class before `main`. The program must print:

```
holding fig
```

# --answers--

```dart
class Holder<T> {
  final T item;
  Holder(this.item);
```

```dart
  String describe() => 'holding $item';
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Holder<String>('fig').describe());
```

```dart
}
```

# --solutions--

```dart
class Holder<T> {
  final T item;
  Holder(this.item);
  String describe() => 'holding $item';
}

void main() {
  print(Holder<String>('fig').describe());
}
```

# --output--

holding fig
