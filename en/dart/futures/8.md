---
language: dart
exerciseType: 1
---

# --description--

A future can also complete with an **error**. When an `async` function throws, the exception does not escape immediately: it becomes the error of the returned future. Whoever `await`s that future sees the error thrown at the `await`, so it can be handled with an ordinary `try`/`catch`:

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s); // throws FormatException for 'abc'
}

Future<int> orZero(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return 0;
  }
}
```

The `await` inside the `try` is essential: `return parseLater(s);` would hand the future to the caller **without waiting**, so the error would arrive after the `try` block is already over and the `catch` would never run.

# --instructions--

The function `parseLater(s)` returns a `Future<int>` that completes with the number written in `s`, or with an error if `s` is not a number. Write an `async` function `safeParse` that takes a `String` `s` and returns a `Future<int>` completing with the parsed number, or with `-1` if `parseLater` fails.

Example:
```dart
print(await safeParse('12'));  // 12
print(await safeParse('abc')); // -1
```

# --before-seed--

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s);
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeParse('12')` should complete with `12`

```dart
  test('test1', () async {
    expect(await safeParse('12'), 12, reason: '--err-t1--');
  });
```

`safeParse('abc')` should complete with `-1` instead of throwing

```dart
  test('test2', () async {
    expect(await safeParse('abc'), -1, reason: '--err-t2--');
  });
```

`safeParse('-7')` should complete with `-7` and `safeParse('')` with `-1`

```dart
  test('test3', () async {
    expect(await safeParse('-7'), -7, reason: '--err-t3--');
    expect(await safeParse(''), -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> safeParse(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return -1;
  }
}
```
