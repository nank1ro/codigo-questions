---
language: dart
exerciseType: 1
---

# --description--

Marking a function `async` does two things: it allows `await` inside the body, and it makes the function **return a `Future`**. Whatever you `return` becomes the value the future completes with, so the declared return type is `Future<T>` even though the body returns a plain `T`:

```dart
Future<String> label(int n) async {
  return 'item $n';
}

Future<void> main() async {
  print(await label(3)); // item 3
}
```

You do not need `Future.value` here: the `async` keyword wraps the returned value for you.

# --instructions--

Write an `async` function `greet` that takes a `String` `name` and returns a `Future<String>` completing with `Hello, ` followed by the name and an exclamation mark.

Example:
```dart
print(await greet('Ada')); // Hello, Ada!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greet('Ada')` should complete with `'Hello, Ada!'`

```dart
  test('test1', () async {
    expect(await greet('Ada'), 'Hello, Ada!', reason: '--err-t1--');
  });
```

`greet('Linus')` should complete with `'Hello, Linus!'`

```dart
  test('test2', () async {
    expect(await greet('Linus'), 'Hello, Linus!', reason: '--err-t2--');
  });
```

`greet` should return a `Future<String>`

```dart
  test('test3', () async {
    expect(greet('Bob'), isA<Future<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<String> greet(String name) async {
  return 'Hello, $name!';
}
```
