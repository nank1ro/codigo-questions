---
language: dart
exerciseType: 1
---

# --instructions--

Declare two top-level `int` variables: `x` with the value `25` and `y` with the value `9`. Then declare a top-level `int` variable named `diff` that stores the result of subtracting `y` from `x`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`diff` should equal `16`

```dart
  test('test1', () {
    expect(diff, 16, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 25;
int y = 9;
int diff = x - y;

void main() {
  print(diff);
}
```
