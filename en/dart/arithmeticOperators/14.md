---
language: dart
exerciseType: 1
---

# --instructions--

Declare two top-level `int` variables: `total` with the value `29` and `perGroup` with the value `4`. Then declare a top-level `int` variable named `groups` that stores how many complete groups of `4` fit into `29` (use integer division `~/`).

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`groups` should equal `7`

```dart
  test('test1', () {
    expect(groups, 7, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 29;
int perGroup = 4;
int groups = total ~/ perGroup;

void main() {
  print(groups);
}
```
