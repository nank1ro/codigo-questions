---
language: dart
exerciseType: 1
---

# --description--

Dart follows standard mathematical **order of operations** (precedence): multiplication and division are evaluated before addition and subtraction.

```dart
int result = 2 + 3 * 4; // 14, not 20
```

Use parentheses to change the order:

```dart
int result = (2 + 3) * 4; // 20
```

# --instructions--

Declare a top-level `int` variable named `result` that evaluates `3 + 4 * 2 - 1`. Let Dart apply its default operator precedence (do not add parentheses).

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` should equal `10`

```dart
  test('test1', () {
    expect(result, 10, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int result = 3 + 4 * 2 - 1;

void main() {
  print(result);
}
```
