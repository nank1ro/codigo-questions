---
language: dart
exerciseType: 2
---

# --description--

The **multiplication operator** `*` multiplies two values together.

```dart
int a = 5;
int b = 6;
int product = a * b; // 30
```

Multiplying an `int` by another `int` produces an `int`. Multiplying with a `double` produces a `double`.

# --instructions--

Complete the code to multiply `8` by `6` and store the result in `product`.

# --seed--

```dart
void main() {
  int product = 8 [/] 6;
  print(product);
}
```

# --answers--

- *
- +
- -
- /

# --solutions--

```dart
void main() {
  int product = 8 * 6;
  print(product);
}
```

# --output--

48
