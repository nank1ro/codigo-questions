---
language: dart
exerciseType: 3
---

# --instructions--

What does the `+` operator do in Dart when used with two `int` values?

# --answers--

- Concatenates them into a String
- Adds them together and returns an int
- Adds them together and always returns a double
- Raises the first to the power of the second

# --solutions--

- Adds them together and returns an int
