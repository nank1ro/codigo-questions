---
language: dart
exerciseType: 2
---

# --description--

The **division operator** `/` divides one value by another. In Dart, `/` **always returns a `double`**, even when the result is a whole number.

```dart
int a = 10;
int b = 4;
double result = a / b; // 2.5

double exact = 10 / 2; // 5.0  (not 5!)
```

Always use a `double` variable to store the result of `/`.

# --instructions--

Complete the code to divide `9` by `4` and store the result in `result`.

# --seed--

```dart
void main() {
  double result = 9 [/] 4;
  print(result);
}
```

# --answers--

- /
- *
- +
- -

# --solutions--

```dart
void main() {
  double result = 9 / 4;
  print(result);
}
```

# --output--

2.25
