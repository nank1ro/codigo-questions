---
language: dart
exerciseType: 2
---

# --description--

The **integer division operator** `~/` divides two numbers and **truncates** the result to an integer (discards any decimal part).

```dart
int a = 10;
int b = 3;
int result = a ~/ b; // 3  (not 3.333...)
```

Use `~/` when you need a whole-number quotient without a remainder.

# --instructions--

Complete the code to perform integer division of `17` by `5` and store the result in `result`.

# --seed--

```dart
void main() {
  int result = 17 [/] 5;
  print(result);
}
```

# --answers--

- ~/
- /
- %
- *

# --solutions--

```dart
void main() {
  int result = 17 ~/ 5;
  print(result);
}
```

# --output--

3
