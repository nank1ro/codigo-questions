---
language: dart
exerciseType: 1
---

# --instructions--

A shop sells `itemCount` items at `pricePerItem` dollars each, with a `discount` dollar amount subtracted from the total. Declare these three top-level `int` variables: `itemCount` = `6`, `pricePerItem` = `15`, `discount` = `10`. Then declare a top-level `int` variable named `totalPrice` that computes the total after the discount.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`totalPrice` should equal `80`

```dart
  test('test1', () {
    expect(totalPrice, 80, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int itemCount = 6;
int pricePerItem = 15;
int discount = 10;
int totalPrice = itemCount * pricePerItem - discount;

void main() {
  print(totalPrice);
}
```
