---
language: dart
exerciseType: 1
---

# --instructions--
Write a function that returns the count of even numbers in the range from `start` to `end` (both inclusive).

# --before-seed--
```dart
int countEvens(int start, int end) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Range 1 to 10 has 5 evens
```dart
    test('1 to 10', () {
      expect(countEvens(1, 10), 5, reason: '--err-t1--');
    });
```

Range 2 to 6 has 3 evens
```dart
    test('2 to 6', () {
      expect(countEvens(2, 6), 3, reason: '--err-t2--');
    });
```

Range 1 to 1 has 0 evens
```dart
    test('1 to 1', () {
      expect(countEvens(1, 1), 0, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = start; i <= end; i++) {
    if (i % 2 == 0) result++;
  }
```
