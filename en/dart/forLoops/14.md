---
language: dart
exerciseType: 1
---

# --instructions--
Write a function that returns a list containing only the odd numbers from a `List<int>`. Use a `for` loop with `continue` to skip even numbers.

# --before-seed--
```dart
List<int> filterOdds(List<int> numbers) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Filters out even numbers
```dart
    test('filters evens', () {
      expect(filterOdds([1, 2, 3, 4, 5]), [1, 3, 5], reason: '--err-t1--');
    });
```

All even numbers returns empty list
```dart
    test('all even', () {
      expect(filterOdds([2, 4, 6]), [], reason: '--err-t2--');
    });
```

All odd numbers returns same values
```dart
    test('all odd', () {
      expect(filterOdds([1, 3, 5]), [1, 3, 5], reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  List<int> result = [];
  for (int i = 0; i < numbers.length; i++) {
    if (numbers[i] % 2 == 0) continue;
    result.add(numbers[i]);
  }
```
