---
language: dart
exerciseType: 4
---

# --description--

Text typed by a user often has extra spaces around it. The `.trim()` method returns a copy of the string without leading and trailing whitespace (spaces, tabs and newlines). `.trimLeft()` and `.trimRight()` remove it only on one side:

```dart
var input = '   hello  ';
print('[${input.trim()}]');      // [hello]
print('[${input.trimLeft()}]');  // [hello  ]
```

# --instructions--

Sort the lines of code to remove the surrounding spaces from `input` and print the cleaned text in uppercase.

# --answers--

```dart
void main() {
```

```dart
  var input = '  hello  ';
```

```dart
  var clean = input.trim();
```

```dart
  print(clean.toUpperCase());
```

```dart
}
```

# --solutions--

```dart
void main() {
  var input = '  hello  ';
  var clean = input.trim();
  print(clean.toUpperCase());
}
```

# --output--

HELLO
