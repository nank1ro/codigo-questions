---
language: dart
exerciseType: 1
---

# --description--

Every string knows how many characters it holds through its `.length` property. Spaces and punctuation count as characters too:

```dart
var word = 'hello';
print(word.length);      // 5
print('a b'.length);     // 3
```

You can read a single character with square brackets and its **index**, starting from `0`. The result is a one-character `String`:

```dart
print(word[0]);                // h
print(word[word.length - 1]);  // o
```

Reading an index outside the string (like `word[5]`) throws an error.

# --instructions--

Given the string `word`, store in an `int` called `size` its number of characters, in a `String` called `first` its first character, and in a `String` called `last` its last character.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var word = 'Flutter';
```

# --after-seed--

```dart
  return [size, first, last];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`size` should be `7`

```dart
  test('test1', () {
    expect(result()[0], 7, reason: '--err-t1--');
  });
```

`first` should be `'F'`

```dart
  test('test2', () {
    expect(result()[1], 'F', reason: '--err-t2--');
  });
```

`last` should be `'r'`

```dart
  test('test3', () {
    expect(result()[2], 'r', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var word = 'Flutter';
int size = word.length;
String first = word[0];
String last = word[word.length - 1];
```
