---
language: dart
exerciseType: 1
---

# --description--

Under the hood, every character of a string is stored as a number, its **code unit** (a UTF-16 code). `.codeUnitAt(index)` gives the code of one character and `.codeUnits` gives the whole list. `String.fromCharCode(code)` does the opposite, building a string from a code:

```dart
var word = 'AB';
print(word.codeUnitAt(0));         // 65
print(word.codeUnits);             // [65, 66]
print(String.fromCharCode(67));    // C
```

Consecutive letters have consecutive codes: `'A'` is 65, `'B'` is 66, and so on.

# --instructions--

Given the string `letter`, store in an `int` called `code` its code unit, and in a `String` called `next` the character whose code is `code + 1`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var letter = 'D';
```

# --after-seed--

```dart
  return [code, next];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`code` should be `68`

```dart
  test('test1', () {
    expect(result()[0], 68, reason: '--err-t1--');
  });
```

`next` should be `'E'`

```dart
  test('test2', () {
    expect(result()[1], 'E', reason: '--err-t2--');
  });
```

`next` should be a `String`

```dart
  test('test3', () {
    expect(result()[1], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var letter = 'D';
int code = letter.codeUnitAt(0);
String next = String.fromCharCode(code + 1);
```
