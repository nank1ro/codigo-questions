---
language: dart
exerciseType: 1
---

# --description--

Several methods let you search inside a string:

- `.contains(other)` returns `true` if `other` appears anywhere in the string
- `.startsWith(other)` and `.endsWith(other)` check the beginning and the end
- `.indexOf(other)` returns the index of the first occurrence, or `-1` if it is not found

```dart
var file = 'report.pdf';
print(file.contains('port'));    // true
print(file.startsWith('rep'));   // true
print(file.endsWith('.txt'));    // false
print(file.indexOf('.'));        // 6
print(file.indexOf('x'));        // -1
```

All of them are case-sensitive: `'Dart'.contains('dart')` is `false`.

# --instructions--

Given the string `email`, store in a `bool` called `hasAt` whether it contains the `@` character, in an `int` called `atIndex` the index of that character, and in a `bool` called `isExample` whether the email ends with `example.com`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var email = 'ada@example.com';
```

# --after-seed--

```dart
  return [hasAt, atIndex, isExample];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasAt` should be `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`atIndex` should be `3`

```dart
  test('test2', () {
    expect(result()[1], 3, reason: '--err-t2--');
  });
```

`isExample` should be `true`

```dart
  test('test3', () {
    expect(result()[2], true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var email = 'ada@example.com';
bool hasAt = email.contains('@');
int atIndex = email.indexOf('@');
bool isExample = email.endsWith('example.com');
```
