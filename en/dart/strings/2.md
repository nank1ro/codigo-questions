---
language: dart
exerciseType: 1
---

# --description--

Two strings can be joined into a new one with the `+` operator, called **concatenation**:

```dart
var greeting = 'Hello' + ', ' + 'Dart';
print(greeting); // Hello, Dart
```

Dart also joins two string **literals** that are written next to each other, without any operator. This is handy for splitting a long text over several lines:

```dart
var text = 'Hello, '
    'Dart';
print(text); // Hello, Dart
```

Only strings can be concatenated with `+`: `'Age: ' + 30` is a compile error, because `30` is an `int`.

# --instructions--

Given the variables `first` and `last`, create a `String` called `full` that contains the first name, a space, and the last name.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var first = 'Ada';
var last = 'Lovelace';
```

# --after-seed--

```dart
  return full;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`full` should be `'Ada Lovelace'`

```dart
  test('test1', () {
    expect(result(), 'Ada Lovelace', reason: '--err-t1--');
  });
```

`full` should be a `String`

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var first = 'Ada';
var last = 'Lovelace';
String full = first + ' ' + last;
```
