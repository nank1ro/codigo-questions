---
language: dart
exerciseType: 2
---

# --description--

The __negation operator__ `!` (also called "not") flips a boolean value.
Applying `!` to `true` gives `false`, and applying `!` to `false` gives `true`.

```dart
bool isActive = true;
print(!isActive); // false
```

This is useful when you want to check the opposite of a condition.

# --instructions--

Given the variable `isBlocked` set to `false`, print its negated value using `!`.

# --seed--

```dart
bool isBlocked = false;
print([/]isBlocked);
```

# --answers--

- !
- !!
- ~
- not

# --solutions--

```dart
bool isBlocked = false;
print(!isBlocked);
```

# --output--

true
