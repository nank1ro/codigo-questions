---
language: dart
exerciseType: 2
---

# --description--

A boolean variable can also hold the value `false`.
Use `false` when something is not the case.

```dart
bool isLoggedIn = false;
print(isLoggedIn); // false
```

Just like `true`, `false` must be written in lowercase.

# --instructions--

Declare a boolean variable called `isExpired` and assign it the value `false`, then print it.

# --seed--

```dart
[/] [/] = [/];
print([/]);
```

# --answers--

- bool
- isExpired
- false
- true
- print
- String

# --solutions--

```dart
bool isExpired = false;
print(isExpired);
```

# --output--

false
