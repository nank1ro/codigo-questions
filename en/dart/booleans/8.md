---
language: dart
exerciseType: 1
---

# --instructions--

Declare a boolean variable called `isHidden` and assign it the value `false`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHidden` must be equal to `false`.

```dart
  test('test1', () {
    expect(isHidden, false, reason: '--err-t1--');
  });
```

`isHidden` must be of type `bool`.

```dart
  test('test2', () {
    expect(isHidden, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isHidden = false;
```
