---
language: dart
exerciseType: 1
---

# --instructions--

Declare a boolean variable called `isReady` and assign it the value `true`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isReady` must be equal to `true`.

```dart
  test('test1', () {
    expect(isReady, true, reason: '--err-t1--');
  });
```

`isReady` must be of type `bool`.

```dart
  test('test2', () {
    expect(isReady, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isReady = true;
```
