---
language: dart
exerciseType: 1
---

# --instructions--

The `||` operator (OR) returns `true` when **at least one** operand is `true`.

Declare a boolean variable called `result` that combines `false || true` using the `||` operator.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` must be equal to `true`.

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result` must be of type `bool`.

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = false || true;
```
