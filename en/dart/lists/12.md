---
language: dart
exerciseType: 1
---

# --instructions--

Given the list `[5, 10, 15, 20]`, return `true` if it contains `15`, otherwise return `false`. Use `.contains()`.

# --before-seed--

```dart
bool computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Returns true for 15

```dart
  test('contains 15', () {
    expect(computeValue(), true, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var numbers = [5, 10, 15, 20];
  bool result = numbers.contains(15);
```
