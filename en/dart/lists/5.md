---
language: dart
exerciseType: 2
---

# --description--

The `.contains()` method checks whether a list includes a given value. It returns `true` if found, `false` otherwise:

```dart
var fruits = ['apple', 'mango', 'grape'];
print(fruits.contains('mango'));  // true
print(fruits.contains('orange')); // false
```

This is useful for membership checks without needing a loop.

# --instructions--

Complete the code to check whether `42` is in the list and print the result.

# --seed--

```dart
var numbers = [10, 42, 77, 5];
print(numbers.[/](42));
```

# --answers--

- contains
- includes
- has
- find
- exists
- indexOf

# --solutions--

```dart
var numbers = [10, 42, 77, 5];
print(numbers.contains(42));
```

# --output--

true
