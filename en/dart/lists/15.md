---
language: dart
exerciseType: 1
---

# --instructions--

Given the list `[3, 7, 2, 9, 4]`, use a `for-in` loop to sum all elements. Return the sum.

# --before-seed--

```dart
int computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Sum is 25

```dart
  test('sum is 25', () {
    expect(computeValue(), 25, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var numbers = [3, 7, 2, 9, 4];
  int result = 0;
  for (var n in numbers) {
    result += n;
  }
```
