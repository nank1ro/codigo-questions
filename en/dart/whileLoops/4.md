---
language: dart
exerciseType: 2
---

# --description--

A `while` loop condition can involve any boolean expression. As soon as the condition evaluates to `false`, the loop ends.

```dart
int n = 100;
while (n > 1) {
  n ~/= 2;
}
print(n); // 0
```

Here, `n` is halved each iteration using integer division (`~/`).

# --instructions--

Complete the `while` loop that keeps halving `n` (using `~/= 2`) while it is greater than `1`, then prints the result.

# --seed--

```dart
void main() {
  int n = 64;
  while (n [/] 1) {
    n [/] 2;
  }
  print(n);
}
```

# --answers--

- >
- <
- ~/=
- /=
- *=

# --solutions--

```dart
void main() {
  int n = 64;
  while (n > 1) {
    n ~/= 2;
  }
  print(n);
}
```

# --output--

1
