---
language: dart
exerciseType: 1
---

# --instructions--

Use a `while` loop to compute the sum of all **even** numbers from `2` to `20` (inclusive). Return the sum.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` must equal `110`

```dart
  test('test1', () {
    expect(computeValue(), 110, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sum = 0;
int i = 2;
while (i <= 20) {
  sum += i;
  i += 2;
}
```
