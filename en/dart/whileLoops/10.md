---
language: dart
exerciseType: 1
---

# --instructions--

Use a `while` loop to compute the sum of numbers from `1` to `10` (inclusive) and return the result.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` must equal `55`

```dart
  test('test1', () {
    expect(computeValue(), 55, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sum = 0;
int i = 1;
while (i <= 10) {
  sum += i;
  i++;
}
```
