---
language: dart
exerciseType: 2
---

# --description--

A counter variable controls how many times a `while` loop runs.

```dart
int count = 0;
while (count < 5) {
  count++;
}
print(count); // 5
```

The counter starts at `0`, and the loop increments it each iteration until `count < 5` is `false`.

# --instructions--

Complete the loop that counts from `1` to `5` and prints each value.

# --seed--

```dart
void main() {
  int count = [/];
  while (count [/] 5) {
    print(count);
    [/];
  }
}
```

# --answers--

- 1
- 0
- <=
- <
- count++
- count--
- count += 2

# --solutions--

```dart
void main() {
  int count = 1;
  while (count <= 5) {
    print(count);
    count++;
  }
}
```

# --output--

1
2
3
4
5
