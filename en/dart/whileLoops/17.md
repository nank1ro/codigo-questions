---
language: dart
exerciseType: 1
---

# --instructions--

Use a `while` loop to find the first number greater than `30` that is divisible by both `3` and `5`. Return that number.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` must equal `45`

```dart
  test('test1', () {
    expect(computeValue(), 45, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int result = 31;
while (result % 3 != 0 || result % 5 != 0) {
  result++;
}
```
