---
language: dart
exerciseType: 1
---

# --instructions--

Use a `while` loop to compute the factorial of `6` (i.e., `6!`). Return the result.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return factorial;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`factorial` must equal `720`

```dart
  test('test1', () {
    expect(computeValue(), 720, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int factorial = 1;
int i = 1;
while (i <= 6) {
  factorial *= i;
  i++;
}
```
