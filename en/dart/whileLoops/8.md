---
language: dart
exerciseType: 3
---

# --instructions--

What does a `do-while` loop guarantee compared to a `while` loop?

# --answers--

- The body executes at least once, even if the condition is initially `false`
- The body executes at most once
- The condition is checked before the body runs
- The loop runs forever unless `break` is used

# --solutions--

- The body executes at least once, even if the condition is initially `false`
