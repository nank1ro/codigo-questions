---
language: dart
exerciseType: 1
---

# --instructions--

Write a function `trafficLight(String color)` that returns `'Go'` for `'green'`, `'Slow down'` for `'yellow'`, `'Stop'` for `'red'`, and `'Invalid'` for any other value, using a `switch` statement with a `default` clause.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`trafficLight('green')` must return `'Go'`

```dart
  test('test1', () {
    expect(trafficLight('green'), 'Go', reason: '--err-t1--');
  });
```

`trafficLight('yellow')` must return `'Slow down'`

```dart
  test('test2', () {
    expect(trafficLight('yellow'), 'Slow down', reason: '--err-t2--');
  });
```

`trafficLight('red')` must return `'Stop'`

```dart
  test('test3', () {
    expect(trafficLight('red'), 'Stop', reason: '--err-t3--');
  });
```

`trafficLight('blue')` must return `'Invalid'`

```dart
  test('test4', () {
    expect(trafficLight('blue'), 'Invalid', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String trafficLight(String color) {
  switch (color) {
    case 'green':
      return 'Go';
    case 'yellow':
      return 'Slow down';
    case 'red':
      return 'Stop';
    default:
      return 'Invalid';
  }
}

void main() {
  print(trafficLight('green'));
}
```
