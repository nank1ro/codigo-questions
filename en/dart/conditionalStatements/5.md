---
language: dart
exerciseType: 2
---

# --description--

The **ternary operator** `condition ? expr1 : expr2` is a compact way to write a simple `if-else` expression.

```dart
int x = 10;
String label = x > 0 ? 'positive' : 'non-positive';
print(label); // positive
```

If the condition is `true`, `expr1` is used; otherwise `expr2` is used.

# --instructions--

Complete the ternary expression to assign `'Even'` to `parity` when `n` is divisible by `2`, and `'Odd'` otherwise.

# --seed--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 [/] 'Even' [/] 'Odd';
  print(parity);
}
```

# --answers--

- ?
- :
- ==
- ;

# --solutions--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 ? 'Even' : 'Odd';
  print(parity);
}
```

# --output--

Even
