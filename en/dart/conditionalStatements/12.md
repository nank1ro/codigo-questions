---
language: dart
exerciseType: 1
---

# --instructions--

Write a function `describe(int n)` that returns `'positive'` when `n` is greater than `0`, and `'non-positive'` otherwise, using `if-else`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`describe(10)` must return `'positive'`

```dart
  test('test1', () {
    expect(describe(10), 'positive', reason: '--err-t1--');
  });
```

`describe(0)` must return `'non-positive'`

```dart
  test('test2', () {
    expect(describe(0), 'non-positive', reason: '--err-t2--');
  });
```

`describe(-5)` must return `'non-positive'`

```dart
  test('test3', () {
    expect(describe(-5), 'non-positive', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String describe(int n) {
  if (n > 0) {
    return 'positive';
  } else {
    return 'non-positive';
  }
}

void main() {
  print(describe(10));
}
```
