---
language: dart
exerciseType: 1
---

# --instructions--

Declare a `bool` variable named `isHot`. Assign it `true` when the `int` variable `temp` (set to `35`) is greater than `30`, using an `if` statement.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int temp = 35;
bool isHot = false;
```

# --after-seed--

```dart
  return isHot;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHot` must be `true`

```dart
  test('test1', () {
    expect(result(), true, reason: '--err-t1--');
    expect(result(), TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int temp = 35;
bool isHot = false;
if (temp > 30) {
  isHot = true;
}
```
