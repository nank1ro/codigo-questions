---
language: dart
exerciseType: 2
---

# --description--

An **`if` statement** executes a block of code only when a condition is `true`.

```dart
int age = 20;
if (age >= 18) {
  print('Adult');
}
```

The code inside the curly braces runs only if the condition `age >= 18` evaluates to `true`.

# --instructions--

Complete the code so the `if` statement checks whether `score` is greater than `50`.

# --seed--

```dart
void main() {
  int score = 80;
  [/] (score [/] 50) {
    print('Passed');
  }
}
```

# --answers--

- if
- >
- else
- <
- ==

# --solutions--

```dart
void main() {
  int score = 80;
  if (score > 50) {
    print('Passed');
  }
}
```

# --output--

Passed
