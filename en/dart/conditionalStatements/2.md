---
language: dart
exerciseType: 2
---

# --description--

You can use `print()` inside an `if` block to display a message when a condition is met.

```dart
bool isRaining = true;
if (isRaining) {
  print('Take an umbrella');
}
```

If `isRaining` is `false`, nothing is printed.

# --instructions--

Complete the code to print `'Hello, Dart!'` when `isGreeted` is `true`.

# --seed--

```dart
void main() {
  bool isGreeted = true;
  if ([/]) {
    [/]('Hello, Dart!');
  }
}
```

# --answers--

- isGreeted
- print
- false
- write

# --solutions--

```dart
void main() {
  bool isGreeted = true;
  if (isGreeted) {
    print('Hello, Dart!');
  }
}
```

# --output--

Hello, Dart!
