---
language: dart
exerciseType: 2
---

# --description--

The `.remove(value)` method deletes a value from the set. It returns `true` if the value was there and `false` otherwise:

```dart
var numbers = {1, 2, 3};
print(numbers.remove(2)); // true
print(numbers.remove(9)); // false
print(numbers); // {1, 3}
```

To delete every element at once, use `.clear()`.

# --instructions--

Complete the code to remove the number `2` from the set, then print the set.

# --seed--

```dart
var numbers = {1, 2, 3};
numbers.[/](2);
print(numbers);
```

# --answers--

- remove
- removeAt
- delete
- drop
- clear
- pop

# --solutions--

```dart
var numbers = {1, 2, 3};
numbers.remove(2);
print(numbers);
```

# --output--

{1, 3}
