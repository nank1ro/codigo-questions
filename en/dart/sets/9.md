---
language: dart
exerciseType: 1
---

# --description--

Sets support the classic set operations. Each one returns a **new** set and leaves the originals untouched:

- `a.union(b)` contains the elements that are in `a` **or** in `b`
- `a.intersection(b)` contains the elements that are in **both** `a` and `b`
- `a.difference(b)` contains the elements of `a` that are **not** in `b`

```dart
var a = {1, 2, 3};
var b = {2, 3, 4};
print(a.union(b));        // {1, 2, 3, 4}
print(a.intersection(b)); // {2, 3}
print(a.difference(b));   // {1}
```

# --instructions--

Given the sets `a` and `b`, create a set called `both` with their union, a set called `common` with their intersection, and a set called `onlyA` with the elements of `a` that are not in `b`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var a = {1, 2, 3, 4};
var b = {3, 4, 5};
```

# --after-seed--

```dart
  return [both, common, onlyA];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`both` should be `{1, 2, 3, 4, 5}`

```dart
  test('test1', () {
    expect(result()[0], {1, 2, 3, 4, 5}, reason: '--err-t1--');
  });
```

`common` should be `{3, 4}`

```dart
  test('test2', () {
    expect(result()[1], {3, 4}, reason: '--err-t2--');
  });
```

`onlyA` should be `{1, 2}`

```dart
  test('test3', () {
    expect(result()[2], {1, 2}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var a = {1, 2, 3, 4};
var b = {3, 4, 5};
var both = a.union(b);
var common = a.intersection(b);
var onlyA = a.difference(b);
```
