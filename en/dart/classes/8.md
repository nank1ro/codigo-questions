---
language: dart
exerciseType: 2
---

# --description--

A **getter** is a special method that reads a computed or private value and looks like a property access. You define it with the `get` keyword:

```dart
class Circle {
  double radius;

  Circle(this.radius);

  double get area => 3.14159 * radius * radius;
}
```

You access a getter like a field: `circle.area` (no parentheses).

# --instructions--

Complete the getter `perimeter` in the `Square` class that returns `4 * side`.

# --seed--

```dart
class Square {
  double side;

  Square(this.side);

  double [/] perimeter => [/] * side;
}
```

# --answers--

- get
- 4
- set
- 2
- double
- perimeter

# --solutions--

```dart
class Square {
  double side;

  Square(this.side);

  double get perimeter => 4 * side;
}
```
