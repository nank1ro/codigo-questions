---
language: dart
exerciseType: 2
---

# --description--

A class can have **instance variables** (also called fields) that hold data for each object. You declare them inside the class body, giving each one an initial value:

```dart
class Animal {
  String name = '';
  int age = 0;
}
```

Each object created from this class will have its own `name` and `age`.

# --instructions--

Complete the `Person` class by declaring the field `name` of type `String`.

# --seed--

```dart
class Person {
  [/] name = '';
}
```

# --answers--

- String
- string
- int
- Text

# --solutions--

```dart
class Person {
  String name = '';
}
```
