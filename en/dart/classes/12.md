---
language: dart
exerciseType: 1
---

# --description--

**Method overriding** allows a subclass to provide its own implementation of a method that already exists in the superclass. Use the `@override` annotation:

```dart
class Shape {
  double area() => 0;
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() => 3.14159 * radius * radius;
}
```

The `@override` annotation tells Dart (and other developers) that you are intentionally replacing the superclass method.

# --instructions--

Create a class `Animal` with a method `sound()` that returns the `String` `'...'`. Then create a subclass `Dog` that overrides `sound()` to return `'Woof'`. Add another subclass `Cat` that overrides `sound()` to return `'Meow'`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Animal.sound()` returns `'...'`

```dart
    test('animal sound', () {
      expect(Animal().sound(), '...', reason: '--err-t1--');
    });
```

`Dog.sound()` returns `'Woof'`

```dart
    test('dog sound', () {
      expect(Dog().sound(), 'Woof', reason: '--err-t2--');
    });
```

`Cat.sound()` returns `'Meow'`

```dart
    test('cat sound', () {
      expect(Cat().sound(), 'Meow', reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String sound() => '...';
}

class Dog extends Animal {
  @override
  String sound() => 'Woof';
}

class Cat extends Animal {
  @override
  String sound() => 'Meow';
}
```
