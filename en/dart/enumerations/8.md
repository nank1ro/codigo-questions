---
language: dart
exerciseType: 2
---

# --description--

Since Dart 3 a `switch` can also be used as an **expression** that produces a value. Each case is written as `pattern => value` and the cases are separated by commas, with no `case` keyword and no `break`:

```dart
enum Direction { north, south, east, west }

String arrow(Direction direction) => switch (direction) {
      Direction.north => '^',
      Direction.south => 'v',
      Direction.east => '>',
      Direction.west => '<',
    };
```

Like the statement form, a switch expression over an enum must be exhaustive.

# --instructions--

Complete the switch expression so that `label` returns the first letter of each size, then print the label of `Size.large`.

# --seed--

```dart
enum Size { small, medium, large }

String label(Size size) => [/] (size) {
      Size.small [/] 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --answers--

- switch
- =>
- case
- when
- ->
- :

# --solutions--

```dart
enum Size { small, medium, large }

String label(Size size) => switch (size) {
      Size.small => 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --output--

L
