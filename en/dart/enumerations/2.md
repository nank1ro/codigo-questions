---
language: dart
exerciseType: 1
---

# --description--

Every enum value has two built-in properties:

- `name` is the value's name as a `String`
- `index` is its position in the declaration, starting from `0`

```dart
enum Direction { north, south, east, west }

print(Direction.east.name);  // east
print(Direction.east.index); // 2
```

# --instructions--

Declare an enum `Season` with the values `spring`, `summer`, `autumn` and `winter`, in this order.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Season.spring.index` should be `0`

```dart
  test('test1', () {
    expect(Season.spring.index, 0, reason: '--err-t1--');
  });
```

`Season.autumn.index` should be `2`

```dart
  test('test2', () {
    expect(Season.autumn.index, 2, reason: '--err-t2--');
  });
```

`Season.winter.index` should be `3`

```dart
  test('test3', () {
    expect(Season.winter.index, 3, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Season { spring, summer, autumn, winter }
```
