---
language: dart
exerciseType: 1
---

# --description--

An enum can **implement an interface** with the `implements` keyword. The enum then promises to provide every member the interface declares, and its values can be used wherever that interface type is expected:

```dart
abstract class Describable {
  String describe();
}

enum Animal implements Describable {
  dog,
  cat;

  @override
  String describe() => 'I am a $name';
}

Describable pet = Animal.cat;
print(pet.describe()); // I am a cat
```

A getter declared in the interface can be implemented either with a getter or with a `final` field of the same name.

# --instructions--

Declare an enum `Ticket` that implements `Priced`, with a `final int` field `price` and the values `child(5)`, `adult(12)` and `senior(8)`, in this order. Then write a function `total(List<Priced> items)` that returns the sum of the prices of all items.

# --seed--

```dart
abstract class Priced {
  int get price;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Ticket.adult.price` should be `12`

```dart
  test('test1', () {
    expect(Ticket.adult.price, 12, reason: '--err-t1--');
  });
```

A `Ticket` can be used as a `Priced`

```dart
  test('test2', () {
    Priced item = Ticket.senior;
    expect(item.price, 8, reason: '--err-t2--');
  });
```

`total([Ticket.child, Ticket.adult, Ticket.senior])` should be `25`

```dart
  test('test3', () {
    expect(total([Ticket.child, Ticket.adult, Ticket.senior]), 25, reason: '--err-t3--');
  });
```

`total([])` should be `0`

```dart
  test('test4', () {
    expect(total([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
abstract class Priced {
  int get price;
}

enum Ticket implements Priced {
  child(5),
  adult(12),
  senior(8);

  @override
  final int price;

  const Ticket(this.price);
}

int total(List<Priced> items) {
  int sum = 0;
  for (var item in items) {
    sum += item.price;
  }
  return sum;
}
```
