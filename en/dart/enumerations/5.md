---
language: dart
exerciseType: 4
---

# --description--

Looping over `values` is the usual way to process every value of an enum. Inside the loop the current value behaves like any other object, so you can read its `index` and its `name` and use them directly in a string interpolation:

```dart
enum Direction { north, south, east, west }

for (var direction in Direction.values) {
  print('${direction.index}: ${direction.name}');
}
```

# --instructions--

Sort the items to declare the enum `Level` and print, for each value, its index and its name. Declare the enum before `main`. The program must print:

```
0: low
1: medium
2: high
```

# --answers--

```dart
enum Level { low, medium, high }
```

```dart
void main() {
```

```dart
  for (var level in Level.values) {
```

```dart
    print('${level.index}: ${level.name}');
```

```dart
  }
}
```

# --solutions--

```dart
enum Level { low, medium, high }
void main() {
  for (var level in Level.values) {
    print('${level.index}: ${level.name}');
  }
}
```

# --output--

0: low
1: medium
2: high
