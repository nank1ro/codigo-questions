---
language: dart
exerciseType: 2
---

# --description--

Dart supports **arrow functions** using the `=>` syntax. When a function body is a single expression, you can shorten it with `=>` instead of `{ return ...; }`:

```dart
// Regular function
int double(int n) {
  return n * 2;
}

// Arrow function — same result
int double(int n) => n * 2;
```

Arrow functions make code more concise. The `=>` replaces both the curly braces and the `return` keyword.

# --instructions--

Complete the arrow function `cube` that takes an `int` parameter `n` and returns `n * n * n`.

# --seed--

```dart
int cube(int n) [/] n * n * n;

void main() {
  print(cube(3));
}
```

# --answers--

- =>
- ->
- return
- ==
- >=

# --solutions--

```dart
int cube(int n) => n * n * n;

void main() {
  print(cube(3));
}
```

# --output--

27
