---
language: dart
exerciseType: 2
---

# --description--

Functions can accept **parameters** — values passed in when calling the function. Parameters are declared inside the parentheses with their type and name:

```dart
int square(int n) {
  return n * n;
}

void main() {
  print(square(4)); // prints 16
}
```

Each parameter has a type (`int`) and a name (`n`). Multiple parameters are separated by commas.

# --instructions--

Complete the function `multiply` that takes two `int` parameters `a` and `b` and returns their product.

# --seed--

```dart
int multiply([/] a, [/] b) {
  return a [/] b;
}

void main() {
  print(multiply(3, 4));
}
```

# --answers--

- int
- int
- *
- +
- String
- double

# --solutions--

```dart
int multiply(int a, int b) {
  return a * b;
}

void main() {
  print(multiply(3, 4));
}
```

# --output--

12
