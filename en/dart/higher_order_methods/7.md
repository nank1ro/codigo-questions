---
language: dart
exerciseType: 1
---

# --description--

`reduce` is the shorter relative of `fold`. It takes no starting value: the **first element** is the starting accumulator, and the function runs for every remaining element:

```dart
final numbers = [1, 2, 3, 4];
print(numbers.reduce((a, b) => a + b)); // 10
```

Because there is no starting value, the result always has the **same type as the elements**, and calling `reduce` on an empty collection throws a `StateError`: there is no first element to start from. `fold` has no such problem, which is why it is the safer default.

`reduce` is at its best when you are looking for one element among many, such as the largest:

```dart
print(numbers.reduce((a, b) => a > b ? a : b)); // 4
```

# --instructions--

Write a function `largest` that takes a non-empty `List<int>` `numbers` and returns the biggest element, using `reduce`.

Example:
```dart
print(largest([3, 9, 4])); // 9
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest([3, 9, 4])` should return `9`

```dart
  test('test1', () {
    expect(largest([3, 9, 4]), 9, reason: '--err-t1--');
  });
```

`largest` should work when the biggest element is first or last, and on a single element

```dart
  test('test2', () {
    expect(largest([10, 2, 5]), 10, reason: '--err-t2--');
    expect(largest([1, 2, 8]), 8, reason: '--err-t2--');
    expect(largest([7]), 7, reason: '--err-t2--');
  });
```

`largest` should work with negative numbers and must not change the list it receives

```dart
  test('test3', () {
    expect(largest([-4, -1, -9]), -1, reason: '--err-t3--');
    final original = [2, 5, 1];
    largest(original);
    expect(original, [2, 5, 1], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int largest(List<int> numbers) {
  return numbers.reduce((a, b) => a > b ? a : b);
}
```
