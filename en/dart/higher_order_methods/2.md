---
language: dart
exerciseType: 1
---

# --description--

`where` takes a function that returns a `bool`, called a **predicate**, and keeps only the elements for which it answers `true`. The order of the surviving elements never changes:

```dart
final numbers = [4, -2, 7, 0];
print(numbers.where((n) => n > 0).toList()); // [4, 7]
```

Like `map`, `where` returns an `Iterable` and never modifies the original collection, so `toList()` is again what turns the result into a `List`.

In other languages this method is called `filter`; in Dart it is `where`.

# --instructions--

Write a function `positives` that takes a `List<int>` `numbers` and returns a new `List<int>` containing only the elements greater than `0`, in their original order.

Example:
```dart
print(positives([4, -2, 7, 0])); // [4, 7]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`positives([4, -2, 7, 0])` should return `[4, 7]`

```dart
  test('test1', () {
    expect(positives([4, -2, 7, 0]), [4, 7], reason: '--err-t1--');
  });
```

`positives` should keep the original order and return a `List<int>`

```dart
  test('test2', () {
    expect(positives([9, 1, -5, 3]), [9, 1, 3], reason: '--err-t2--');
    expect(positives([1]), isA<List<int>>(), reason: '--err-t2--');
  });
```

`positives` should return an empty list when nothing is positive, and must not change the list it receives

```dart
  test('test3', () {
    expect(positives([-1, -2]), <int>[], reason: '--err-t3--');
    final original = [1, -1, 2];
    positives(original);
    expect(original, [1, -1, 2], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> positives(List<int> numbers) {
  return numbers.where((n) => n > 0).toList();
}
```
