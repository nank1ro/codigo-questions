---
language: dart
exerciseType: 1
---

# --description--

Put together, these methods replace most hand-written loops. A pipeline usually reads in three stages: **select** the elements with `where`, **transform** them with `map`, then **combine** them with `fold`:

```dart
final prices = [12, 40, 7];
final cheapTotal = prices.where((p) => p < 20).fold(0, (acc, p) => acc + p);
print(cheapTotal); // 19
```

Because `fold` chooses its own starting value, it can also end a chain with a type that has nothing to do with the elements, such as a `String` grown one piece at a time:

```dart
final words = ['fig', 'kiwi'];
final firstLetters = words.fold('', (acc, w) => acc + w[0]);
print(firstLetters); // fk
```

Each stage stays short and says what it does, which is the real reason to prefer them over a loop that does all three at once.

# --instructions--

Write a function `initials` that takes a `List<String>` `names`, ignores the empty ones, and returns a `String` made of the first letter of each remaining name, in upper case and in order.

Example:
```dart
print(initials(['ada', '', 'linus'])); // AL
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`initials(['ada', '', 'linus'])` should return `'AL'`

```dart
  test('test1', () {
    expect(initials(['ada', '', 'linus']), 'AL', reason: '--err-t1--');
  });
```

Letters should be upper case and in order, and already upper-case names should stay as they are

```dart
  test('test2', () {
    expect(initials(['grace', 'Bob', 'ada']), 'GBA', reason: '--err-t2--');
    expect(initials(['x']), 'X', reason: '--err-t2--');
  });
```

`initials` should return an empty `String` when there is no non-empty name

```dart
  test('test3', () {
    expect(initials(<String>[]), '', reason: '--err-t3--');
    expect(initials(['', '']), '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String initials(List<String> names) {
  return names
      .where((n) => n.isNotEmpty)
      .map((n) => n[0].toUpperCase())
      .fold('', (acc, letter) => acc + letter);
}
```
