---
language: dart
exerciseType: 1
---

# --description--

Every one of these methods returns an `Iterable`, and every `Iterable` has the same methods again. That is what lets them be **chained**: a whole computation reads as a pipeline from left to right, each step working on what the previous one produced.

```dart
final words = ['kiwi', 'fig', 'banana', 'date'];
print(words.where((w) => w.length == 4).map((w) => w.toUpperCase()).toList());
// [KIWI, DATE]
```

Only the last step needs `toList()`: calling it in the middle would build a list nobody keeps.

The type changes along the chain, and so does what the next function receives: after `where` on a `List<String>` you still have strings, but after `map((w) => w.length)` the next step sees numbers.

Because each step is lazy, the order matters for the work done, not only for the result: filtering first with `where` means `map` is called on fewer elements.

# --instructions--

Write a function `shortUpper` that takes a `List<String>` `words` and returns a new `List<String>` with the words of at most `4` characters, in upper case, in their original order.

Example:
```dart
print(shortUpper(['kiwi', 'banana', 'fig'])); // [KIWI, FIG]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shortUpper(['kiwi', 'banana', 'fig'])` should return `['KIWI', 'FIG']`

```dart
  test('test1', () {
    expect(shortUpper(['kiwi', 'banana', 'fig']), ['KIWI', 'FIG'],
        reason: '--err-t1--');
  });
```

Words of exactly `4` characters should be kept, longer ones dropped

```dart
  test('test2', () {
    expect(shortUpper(['date', 'grape']), ['DATE'], reason: '--err-t2--');
    expect(shortUpper(['melon']), <String>[], reason: '--err-t2--');
  });
```

`shortUpper` should return a `List<String>` and must not change the list it receives

```dart
  test('test3', () {
    expect(shortUpper(['a']), isA<List<String>>(), reason: '--err-t3--');
    final original = ['fig', 'banana'];
    shortUpper(original);
    expect(original, ['fig', 'banana'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<String> shortUpper(List<String> words) {
  return words.where((w) => w.length <= 4).map((w) => w.toUpperCase()).toList();
}
```
