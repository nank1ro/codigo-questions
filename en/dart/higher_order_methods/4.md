---
language: dart
exerciseType: 1
---

# --description--

Some higher-order methods answer a question about the collection instead of building a new one. They take a predicate and return a `bool`:

- `any` is `true` when **at least one** element satisfies the predicate
- `every` is `true` when **all** the elements satisfy it

```dart
final numbers = [1, 2, 3];
print(numbers.any((n) => n > 2));   // true
print(numbers.every((n) => n > 2)); // false
```

Both stop as soon as the answer is certain: `any` at the first element that matches, `every` at the first that does not.

On an empty collection `any` is `false` and `every` is `true`: there is no element to prove the first, and none to break the second.

# --instructions--

Write two functions, both taking a `List<int>` `numbers`:

- `hasNegative` returns `true` if at least one element is smaller than `0`
- `allEven` returns `true` if every element is even

Example:
```dart
print(hasNegative([1, -2, 3])); // true
print(allEven([2, 4]));         // true
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasNegative([1, -2, 3])` should be `true` and `hasNegative([1, 2, 3])` should be `false`

```dart
  test('test1', () {
    expect(hasNegative([1, -2, 3]), true, reason: '--err-t1--');
    expect(hasNegative([1, 2, 3]), false, reason: '--err-t1--');
  });
```

`allEven([2, 4, 6])` should be `true` and `allEven([2, 5])` should be `false`

```dart
  test('test2', () {
    expect(allEven([2, 4, 6]), true, reason: '--err-t2--');
    expect(allEven([2, 5]), false, reason: '--err-t2--');
  });
```

On an empty list `hasNegative` should be `false` and `allEven` should be `true`

```dart
  test('test3', () {
    expect(hasNegative(<int>[]), false, reason: '--err-t3--');
    expect(allEven(<int>[]), true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool hasNegative(List<int> numbers) {
  return numbers.any((n) => n < 0);
}

bool allEven(List<int> numbers) {
  return numbers.every((n) => n % 2 == 0);
}
```
