---
language: c
exerciseType: 4
---

# --description--

`printf` has no specifier for a whole struct. The usual solution is a small function that prints the members in a fixed format, so every part of the program shows the value the same way:
```c
void print_person(Person p) {
    printf("%s (%d)\n", p.name, p.age);
}
```

# --instructions--

Sort the lines to define the `Point` type, a function `print_point` that prints a point as `(x, y)`, and a `main` that prints the point `(3, 4)`. Define the function before `main`

# --answers--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
```

```c
void print_point(Point p) {
```

```c
    printf("(%d, %d)\n", p.x, p.y);
```

```c
}
```

```c
int main() {
```

```c
    Point p = {3, 4};
```

```c
    print_point(p);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
void print_point(Point p) {
    printf("(%d, %d)\n", p.x, p.y);
}
int main() {
    Point p = {3, 4};
    print_point(p);
    return 0;
}
```

# --output--

(3, 4)
