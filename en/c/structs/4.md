---
language: c
exerciseType: 2
---

# --description--

Writing `struct Point` every time is verbose. With `typedef` you give the struct a short type name, and the struct itself can stay anonymous:
```c
typedef struct {
    double celsius;
} Temperature;

Temperature t = {21.5};
```
The new name `Temperature` is used on its own, without the `struct` keyword in front of it. This is the most common way to declare structs in real programs.

# --instructions--

Complete the code to define the anonymous struct with the member `celsius` under the type name `Temperature`, then declare `t` with that type

# --seed--

```c
typedef [/] {
    double celsius;
} Temperature;
[/] t = {21.5};
printf("%.1f\n", t.celsius);
```

# --answers--

- struct
- enum
- typedef
- Temperature
- struct Temperature
- Temperature t

# --solutions--

```c
typedef struct {
    double celsius;
} Temperature;
Temperature t = {21.5};
printf("%.1f\n", t.celsius);
```

# --output--

21.5
