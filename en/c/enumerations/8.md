---
language: c
exerciseType: 2
---

# --description--

Writing `enum Color` every time is verbose. With `typedef` you give the enumeration a short type name, and the enumeration itself can stay anonymous:
```c
typedef enum { RED, GREEN, BLUE } Color;

Color favorite = BLUE;
```
The new name `Color` is used on its own, without the `enum` keyword in front of it.

# --instructions--

Complete the code to define the anonymous enumeration with the constants `OFF` and `ON` under the type name `State`, then declare `lamp` with that type

# --seed--

```c
typedef [/] { OFF, ON } State;
[/] lamp = ON;
printf("%d\n", lamp);
```

# --answers--

- enum
- struct
- State
- enum State
- typedef

# --solutions--

```c
typedef enum { OFF, ON } State;
State lamp = ON;
printf("%d\n", lamp);
```

# --output--

1
