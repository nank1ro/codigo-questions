---
language: c
exerciseType: 2
---

# --description--

An enumeration is also a type: you can declare a variable of that type by writing `enum` followed by the enumeration name, and assign one of its constants to it:
```c
enum Color favorite = GREEN;
```
Since the constants are integers, you compare enum variables with the usual operators:
```c
if (favorite == GREEN) {
    printf("Green it is\n");
}
```

# --instructions--

Complete the code to declare the variable `light` of type `enum Light` holding `GREEN`, so the program prints `Go`

# --seed--

```c
enum Light { RED, YELLOW, GREEN };
[/] Light light = [/];
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --answers--

- enum
- struct
- GREEN
- RED
- YELLOW
- "GREEN"

# --solutions--

```c
enum Light { RED, YELLOW, GREEN };
enum Light light = GREEN;
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --output--

Go
