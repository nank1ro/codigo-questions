---
language: c
exerciseType: 4
---

# --description--

Putting it together: choose the type from the kind of value, match every `printf` specifier to its argument type, and cast when a computation must happen in a different type than its operands.

# --instructions--

Sort the lines to build a program that stores `7` in `total` and `2` in `count`, computes `total` divided by `count` as a `double` in `avg`, and prints the integer division `total / count` followed by `avg` with one decimal

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int total = 7, count = 2;
```

```c
    double avg = (double) total / count;
```

```c
    printf("%d %.1f\n", total / count, avg);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int total = 7, count = 2;
    double avg = (double) total / count;
    printf("%d %.1f\n", total / count, avg);
    return 0;
}
```

# --output--

3 3.5
