<section id="options">
{
"language": "c",
"type": "answerButtons"
}
</section>
<section id="instructions">
What's wrong with this code?
```
int manchester_united = 0;
int barcelona = 1;
printf("The score is %i : %i\n", manchester_united, barcelona);
```
</section>
<section id="answerButtons">
{
	"buttons": ["Nothing is wrong with the code","manchester_united and barcelona don't have values","We can't use variables inside the formatted string","We can't use %i twice in the same string"],
	"correct": "Nothing is wrong with the code"
}
</section>