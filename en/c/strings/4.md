---
language: c
exerciseType: 2
---

# --description--

Since a string is an array, every character has an index starting from `0`:
```c
char word[] = "Coding";
word[0]; // 'C'
word[5]; // 'g'
```
A single character is printed with `%c`. Characters can also be replaced:
```c
word[0] = 'K'; // word is now "Koding"
```

# --instructions--

Print the first and the last character of `word`

# --seed--

```c
char word[] = "Coding";
printf("%c%c\n", word[[/]], word[[/]]);
```

# --answers--

- 0
- 5
- 6
- 1

# --solutions--

```c
char word[] = "Coding";
printf("%c%c\n", word[0], word[5]);
```

# --output--

Cg
