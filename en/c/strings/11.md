---
language: c
exerciseType: 2
---

# --description--

The header `ctype.h` provides functions that work on a single character.
`toupper(c)` returns the uppercase version of a letter and `tolower(c)` the lowercase one; any other character is returned unchanged:
```c
char letter = toupper('a'); // 'A'
```

# --instructions--

Print `Ab` by converting `first` to uppercase and `second` to lowercase

# --seed--

```c
#include <stdio.h>
#include <ctype.h>

int main() {
    char first = 'a';
    char second = 'B';
    printf("%c%c\n", [/](first), [/](second));
    return 0;
}
```

# --answers--

- toupper
- tolower
- upper
- lower
- capitalize

# --solutions--

```c
#include <stdio.h>
#include <ctype.h>

int main() {
    char first = 'a';
    char second = 'B';
    printf("%c%c\n", toupper(first), tolower(second));
    return 0;
}
```

# --output--

Ab
