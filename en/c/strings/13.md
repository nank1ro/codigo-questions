---
language: c
exerciseType: 4
---

# --description--

`sprintf` works like `printf`, but writes the formatted text into a char array instead of the screen:
```c
char buffer[30];
sprintf(buffer, "%d items", 3); // buffer is "3 items"
```
The buffer must be large enough for the whole text and its `'\0'`.

# --instructions--

Sort the lines to build the text `Age: 25` inside `buffer` and print it. Declare `buffer` before `age`

# --answers--

```c
char buffer[30];
```

```c
int age = 25;
```

```c
sprintf(buffer, "Age: %d", age);
```

```c
printf("%s\n", buffer);
```

# --solutions--

```c
char buffer[30];
int age = 25;
sprintf(buffer, "Age: %d", age);
printf("%s\n", buffer);
```

# --output--

Age: 25
