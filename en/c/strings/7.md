---
language: c
exerciseType: 2
---

# --description--

`strcat(destination, source)` appends `source` to the end of `destination`:
```c
char text[20] = "Hello";
strcat(text, " World");
// text is now "Hello World"
```
As with `strcpy`, the destination array must have enough room for the result.

# --instructions--

Append `" World"` to `greeting` so the program prints `Hello World`

# --seed--

```c
char greeting[20] = "Hello";
[/](greeting, [/]);
printf("%s\n", greeting);
```

# --answers--

- strcat
- strcpy
- strlen
- " World"
- "World"
- 'World'

# --solutions--

```c
char greeting[20] = "Hello";
strcat(greeting, " World");
printf("%s\n", greeting);
```

# --output--

Hello World
