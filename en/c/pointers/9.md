---
language: c
exerciseType: 4
---

# --description--

Since `p + 1` is the next element, `p++` moves a pointer to the next element in place. A loop can walk an array by advancing a pointer instead of an index:
```c
int *p = values;
for (int i = 0; i < 3; i++) {
    printf("%d\n", *p);
    p++;
}
```
Each turn prints the element `p` points to, then moves `p` one element forward.

# --instructions--

Sort the lines to store `1`, `2` and `3` in `values`, point `p` at its first element and print every element by advancing the pointer

# --answers--

```c
int values[3] = {1, 2, 3};
```

```c
int *p = values;
```

```c
for (int i = 0; i < 3; i++) {
```

```c
    printf("%d\n", *p);
```

```c
    p++;
```

```c
}
```


# --solutions--

```c
int values[3] = {1, 2, 3};
int *p = values;
for (int i = 0; i < 3; i++) {
    printf("%d\n", *p);
    p++;
}
```

# --output--

1
2
3
