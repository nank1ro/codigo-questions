---
language: c
exerciseType: 2
---

# --description--

Removing the first node is the mirror of `push_front`: save the address of the second node, free the first one and return the saved address as the new head. The caller stores the result back into its head variable:
```c
Node *next = head->next;
free(head);
return next;
```
Popping until the head is `NULL` frees the whole list, one node per call.

# --instructions--

Complete `pop_front` so it frees the first node and returns the second one as the new head

# --seed--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

Node *pop_front(Node *head) {
    Node *next = [/];
    [/](head);
    return [/];
}

int main() {
    Node *head = NULL;
    head = push_front(head, 3);
    head = push_front(head, 2);
    head = push_front(head, 1);
    head = pop_front(head);
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        head = pop_front(head);
    }
    return 0;
}
```

# --answers--

- head->next
- free
- next
- head
- NULL

# --solutions--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

Node *pop_front(Node *head) {
    Node *next = head->next;
    free(head);
    return next;
}

int main() {
    Node *head = NULL;
    head = push_front(head, 3);
    head = push_front(head, 2);
    head = push_front(head, 1);
    head = pop_front(head);
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        head = pop_front(head);
    }
    return 0;
}
```

# --output--

2
3
