---
language: c
exerciseType: 2
---

# --description--

Walking a list, called **traversal**, is a loop that starts at the head and follows `next` until it reaches `NULL`. A `for` loop expresses it in a single line, with a pointer as the loop variable:
```c
for (Node *n = head; n != NULL; n = n->next) {
    printf("%d\n", n->value);
}
```
There is no index: the only way to reach a node is through the pointer stored in the one before it.

# --instructions--

Complete the loop so it starts from `a` and prints the value of every node in the list

# --seed--

```c
typedef struct Node {
    int value;
    struct Node *next;
} Node;
Node c = {3, NULL};
Node b = {2, &c};
Node a = {1, &b};
for (Node *n = [/]; n [/] NULL; n = [/]) {
    printf("%d\n", n->value);
}
```

# --answers--

- &a
- a
- !=
- ==
- n->next
- n.next

# --solutions--

```c
typedef struct Node {
    int value;
    struct Node *next;
} Node;
Node c = {3, NULL};
Node b = {2, &c};
Node a = {1, &b};
for (Node *n = &a; n != NULL; n = n->next) {
    printf("%d\n", n->value);
}
```

# --output--

1
2
3
