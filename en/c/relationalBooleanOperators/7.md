---
language: c
exerciseType: 2
---

# --description--

Since C99 the header `stdbool.h` provides the type `bool` and the constants `true` (which is `1`) and `false` (which is `0`):
```c
#include <stdbool.h>

bool open = true;
bool full = 3 > 5;
printf("%d %d\n", open, full); // prints "1 0"
```
A `bool` is still an integer underneath: it is printed with `%d`, and it works with `&&`, `||` and `!` exactly like a comparison result. It just makes the intent clearer than a plain `int`.

# --instructions--

Complete the code so that `open` is the boolean constant for true and `closed` is its negation, then print both

# --seed--

```c
bool open = [/];
bool closed = [/]open;
printf("%d %d\n", open, closed);
```

# --answers--

- true
- !
- false
- -
- ~

# --solutions--

```c
bool open = true;
bool closed = !open;
printf("%d %d\n", open, closed);
```

# --output--

1 0
