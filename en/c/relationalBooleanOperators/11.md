---
language: c
exerciseType: 2
---

# --description--

Operators have a **precedence** that decides what is computed first:
1. `!` is applied first
2. then the relational comparisons `<`, `>`, `<=`, `>=`
3. then the equality comparisons `==`, `!=`
4. then `&&`
5. then `||`

So `a > 0 && a < 10` needs no parentheses, and `a && b || c` means `(a && b) || c` because `&&` binds tighter than `||`. Use parentheses to force a different grouping or simply to make the intent readable.

# --instructions--

Complete the code so that `without` computes `a && b || c` with the default precedence and `with` forces `b || c` to be evaluated first, then print both

# --seed--

```c
int a = 0, b = 1, c = 1;
int without = a && b [/] c;
int with = a && [/]b || c[/];
printf("%d %d\n", without, with);
```

# --answers--

- ||
- (
- )
- &&
- !

# --solutions--

```c
int a = 0, b = 1, c = 1;
int without = a && b || c;
int with = a && (b || c);
printf("%d %d\n", without, with);
```

# --output--

1 0
