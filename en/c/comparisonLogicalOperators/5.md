---
language: c
exerciseType: 2
---

# --description--

The **or** operator `||` gives `1` when at least one side is true, and `0` only when both sides are false:
```c
int day = 1;
printf("%d\n", day == 1 || day == 7); // prints "1"
```
Each side must be a complete comparison: `day == 6 || 7` does not mean "6 or 7" (you will see why later).

# --instructions--

Complete the code so that `weekend` is `1` when `day` is `6` or `day` is `7`, then print it

# --seed--

```c
int day = 7;
int weekend = day == 6 [/] day == [/];
printf("%d\n", weekend);
```

# --answers--

- ||
- 7
- &&
- 6
- =

# --solutions--

```c
int day = 7;
int weekend = day == 6 || day == 7;
printf("%d\n", weekend);
```

# --output--

1
