---
language: c
exerciseType: 4
---

# --description--

Operators have a **precedence** that decides what is computed first:
1. `!` is applied first
2. then the relational comparisons `<`, `>`, `<=`, `>=`
3. then the equality comparisons `==`, `!=`
4. then `&&`
5. then `||`

So `a > 0 && a < 10` needs no parentheses: both comparisons are computed before `&&`.
And `x == 1 || y == 2 && z == 3` means `x == 1 || (y == 2 && z == 3)`, because `&&` binds tighter than `||`.

# --instructions--

Sort the lines to store in `inside` whether `a` is between `1` and `9`, store its negation in `outside` and print both values separated by a space

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int a = 5;
```

```c
    int inside = a > 0 && a < 10;
```

```c
    int outside = !inside;
```

```c
    printf("%d %d\n", inside, outside);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int a = 5;
    int inside = a > 0 && a < 10;
    int outside = !inside;
    printf("%d %d\n", inside, outside);
    return 0;
}
```

# --output--

1 0
