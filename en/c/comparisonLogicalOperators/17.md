---
language: c
exerciseType: 4
---

# --description--

A `bool` parameter can be used directly as an operand of `&&` or `||`, without comparing it to `true`: write `age >= 18 && citizen`, not `citizen == true`.

# --instructions--

Sort the lines to define `can_vote`, which returns `true` when `age` is at least `18` and `citizen` is `true`, and a `main` that prints the result for `20, true` and then for `20, false`. Define the function before `main`

# --answers--

```c
#include <stdio.h>
#include <stdbool.h>
```

```c
bool can_vote(int age, bool citizen) {
```

```c
    return age >= 18 && citizen;
}
```

```c
int main() {
```

```c
    printf("%d\n", can_vote(20, true));
```

```c
    printf("%d\n", can_vote(20, false));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
#include <stdbool.h>
bool can_vote(int age, bool citizen) {
    return age >= 18 && citizen;
}
int main() {
    printf("%d\n", can_vote(20, true));
    printf("%d\n", can_vote(20, false));
    return 0;
}
```

# --output--

1
0
