---
language: swift
exerciseType: 1
---

# --description--

`replacing(_:with:)` swaps every match for a fixed text and returns a new `String`, leaving the original untouched:
```swift
let text = "a1 b22"
print(text.replacing(#/\d+/#, with: "#")) // a# b#
print(text)                               // a1 b22
```
Note that `\d+` replaces a whole run of digits with a single `#`, while `\d` would replace one digit at a time. The pattern decides how much disappears.

# --instructions--

Write a function `mask` that takes a text (`String`) without argument label and returns it with every single digit replaced by a `#`

Example of function call:
```swift
print(mask("a1 b22"))
// prints a# b##
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func mask() {

}
```

# --asserts--

The function must use `replacing(`

```json
{
  "regex": "replacing\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`mask("a1 b22")` must return `"a# b##"`

```swift
tryCatch(mask("a1 b22") == "a# b##")
```

`mask("2024")` must return `"####"`

```swift
tryCatch(mask("2024") == "####")
```

`mask("abc")` must return `"abc"`

```swift
tryCatch(mask("abc") == "abc")
```

`mask("")` must return `""`

```swift
tryCatch(mask("") == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func mask(_ text: String) -> String {
    return text.replacing(#/\d/#, with: "#")
}
```
