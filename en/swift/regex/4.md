---
language: swift
exerciseType: 1
---

# --description--

When none of the shorthands fit, list the characters you accept between square brackets. `[abc]` matches one `a`, one `b` or one `c`, and a dash writes a range:
```swift
print("f".contains(#/[a-f]/#))  // true
print("Z".contains(#/[A-Z]/#))  // true
print("5".contains(#/[0-9a-f]/#)) // true
```
A number in braces says exactly how many times the previous pattern repeats: `{3}` means three times, `{2,4}` means between two and four times:
```swift
print("aaa".contains(#/^a{3}$/#))  // true
print("aa".contains(#/^a{3}$/#))   // false
```
Wrapping a pattern in `^` and `$` with a count is the usual way to check that a whole text has a given shape.

# --instructions--

Write a function `isCode` that takes a text (`String`) without argument label and returns `true` only when the whole text is exactly three uppercase letters, using a regex

Example of function call:
```swift
print(isCode("ABC"))
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func isCode() {

}
```

# --asserts--

The function must use a regex literal written with `#/`

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`isCode("ABC")` must return `true`

```swift
tryCatch(isCode("ABC") == true)
```

`isCode("XYZ")` must return `true`

```swift
tryCatch(isCode("XYZ") == true)
```

`isCode("AB")` must return `false`

```swift
tryCatch(isCode("AB") == false)
```

`isCode("ABCD")` must return `false`

```swift
tryCatch(isCode("ABCD") == false)
```

`isCode("abc")` must return `false`

```swift
tryCatch(isCode("abc") == false)
```

`isCode("A1C")` must return `false`

```swift
tryCatch(isCode("A1C") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func isCode(_ text: String) -> Bool {
    return text.contains(#/^[A-Z]{3}$/#)
}
```
