---
language: swift
exerciseType: 1
---

# --description--

Because `Int(_:)` accepts a `Substring`, turning found text into numbers is one step. `compactMap` is handy here: it drops the values that come back `nil`:
```swift
let text = "a1 b22"
let numbers = text.matches(of: #/\d+/#).compactMap { Int($0.0) }
print(numbers) // [1, 22]
```
Use `map` when every element converts, `compactMap` when some may fail.

# --instructions--

Write a function `numbers` that takes a text (`String`) without argument label and returns every run of digits it holds as an array of `Int`, in the order they appear

Example of function call:
```swift
print(numbers("a1 b22 c333"))
// prints [1, 22, 333]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func numbers() {

}
```

# --asserts--

The function must use `matches(of:`

```json
{
  "regex": "matches\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`numbers("a1 b22 c333")` must return `[1, 22, 333]`

```swift
tryCatch(numbers("a1 b22 c333") == [1, 22, 333])
```

`numbers("7")` must return `[7]`

```swift
tryCatch(numbers("7") == [7])
```

`numbers("abc")` must return `[]`

```swift
tryCatch(numbers("abc") == [])
```

`numbers("10-20")` must return `[10, 20]`

```swift
tryCatch(numbers("10-20") == [10, 20])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func numbers(_ text: String) -> [Int] {
    return text.matches(of: #/\d+/#).compactMap { Int($0.0) }
}
```
