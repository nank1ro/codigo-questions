---
language: swift
exerciseType: 4
---

# --description--

`matches(of:)` returns **every** match instead of the first one, as an array. The array is never `nil`: when nothing matches it is simply empty, so it can be looped over or transformed straight away:
```swift
let text = "a1 b22"
print(text.matches(of: #/\d+/#).count) // 2
```
Each element is a match, so `$0.0` inside a `map` is the matched text:
```swift
let found = text.matches(of: #/\d+/#).map { String($0.0) }
print(found) // ["1", "22"]
```

# --instructions--

Sort the items to build a program that collects every run of digits of `text` into `found` and prints it

# --answers--

```swift
let text = "a1 b22 c333"
```

```swift
let pattern = #/\d+/#
```

```swift
let found = text.matches(of: pattern).map { String($0.0) }
```

```swift
print(found)
```

# --solutions--

```swift
let text = "a1 b22 c333"
let pattern = #/\d+/#
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

```swift
let pattern = #/\d+/#
let text = "a1 b22 c333"
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

# --output--

["1", "22", "333"]
