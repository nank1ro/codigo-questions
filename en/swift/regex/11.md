---
language: swift
exerciseType: 2
---

# --description--

Counting brackets gets fragile as soon as a pattern grows. Give a group a **name** instead, by writing `?<name>` right after its opening bracket, and read it as a property of the match:
```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>\d+)/#) {
    print(m.key)   // size
    print(m.value) // 10
}
```
Named groups are still numbered, so `m.1` keeps working, but `m.key` says what it holds and survives a change in the pattern.

# --instructions--

Complete the code so the value group matches digits and the key is printed before it, producing the output `size 10`

# --seed--

```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>[/]+)/#) {
    print(m.[/], m.value)
}
```

# --answers--

- \d
- \s
- [A-Z]
- key
- name
- 0

# --solutions--

```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>\d+)/#) {
    print(m.key, m.value)
}
```

# --output--

size 10
