---
language: swift
exerciseType: 1
---

# --description--

`m.0` is not a `String` but a `Substring`: a view into the original text, not a copy. It prints exactly like a string, but where a `String` is required you have to convert it:
```swift
let text = "order 42"
if let m = text.firstMatch(of: #/\d+/#) {
    let found: String = String(m.0)
    print(found) // 42
}
```
Number initialisers accept a `Substring` directly, so `Int(m.0)` works without the detour.

# --instructions--

Write a function `firstNumber` that takes a text (`String`) without argument label and returns the first run of digits as a `String`, or `"none"` when the text holds no digit

Example of function call:
```swift
print(firstNumber("order 42 today 7"))
// prints 42
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func firstNumber() {

}
```

# --asserts--

The function must use `firstMatch(of:`

```json
{
  "regex": "firstMatch\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstNumber("order 42 today 7")` must return `"42"`

```swift
tryCatch(firstNumber("order 42 today 7") == "42")
```

`firstNumber("a1")` must return `"1"`

```swift
tryCatch(firstNumber("a1") == "1")
```

`firstNumber("100")` must return `"100"`

```swift
tryCatch(firstNumber("100") == "100")
```

`firstNumber("abc")` must return `"none"`

```swift
tryCatch(firstNumber("abc") == "none")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstNumber(_ text: String) -> String {
    if let m = text.firstMatch(of: #/\d+/#) {
        return String(m.0)
    }
    return "none"
}
```
