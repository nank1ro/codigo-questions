<section id="options">
{
"language": "swift",
"type": "answerButtons"
}
</section>
<section id="instructions">
What's wrong with this code?
```
let manchesterUnited = 2
let barcelona = 2
print("The final score is \(manchesterUnited) : \(barcelona)")
```
</section>
<section id="answerButtons">
{
	"buttons": ["Nothing is wrong with the code","manchesterUnited and barcelona don't have values","We can't use variables inside the curly braces","We can't use \\() twice in the same string"],
	"correct": "Nothing is wrong with the code"
}
</section>