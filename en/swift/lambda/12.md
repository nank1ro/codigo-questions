---
language: swift
exerciseType: 1
---

# --description--

Returning a closure is a handy way to build customized functions. The parameters of the outer function are captured by the closure it returns:
```swift
func makeAdder(_ amount: Int) -> (Int) -> Int {
    return { $0 + amount }
}
let addFive = makeAdder(5)
print(addFive(10)) // 15
```
The return type `(Int) -> Int` describes the closure, and the shorthand `$0` refers to the argument of that closure, not of `makeAdder`.

# --instructions--

Write a function `makeMultiplier` that takes an `Int` called `factor` (omit the argument label) and returns a closure of type `(Int) -> Int` that multiplies its argument by `factor`

Example of function call:
```swift
let double = makeMultiplier(2)
print(double(10))
// prints 20
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`makeMultiplier(2)(10)` must return `20`

```swift
tryCatch(makeMultiplier(2)(10) == 20)
```

`makeMultiplier(3)(4)` must return `12`

```swift
tryCatch(makeMultiplier(3)(4) == 12)
```

`makeMultiplier(0)(99)` must return `0`

```swift
tryCatch(makeMultiplier(0)(99) == 0)
```

A closure returned by `makeMultiplier(5)` must keep working across calls

```swift
let _timesFive = makeMultiplier(5)
tryCatch(_timesFive(1) == 5 && _timesFive(6) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func makeMultiplier(_ factor: Int) -> (Int) -> Int {
    return { $0 * factor }
}
```
