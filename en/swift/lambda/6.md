---
language: swift
exerciseType: 4
---

# --description--

Closures shine with the array methods that take one as argument. `map` calls the closure on every element and returns a new array with the results:
```swift
let nums = [1, 2, 3]
let doubled = nums.map { $0 * 2 }
print(doubled) // [2, 4, 6]
```
The original array is not changed. Because `map` takes a single closure argument, the trailing closure syntax is the usual way to call it.

# --instructions--

Sort the items to build an array with the squares of `[1, 2, 3, 4]` using `map`, and print it

# --answers--

```swift
let nums = [1, 2, 3, 4]
```

```swift
let squares = nums.map { $0 * $0 }
```

```swift
print(squares)
```

# --solutions--

```swift
let nums = [1, 2, 3, 4]
let squares = nums.map { $0 * $0 }
print(squares)
```

# --output--

[1, 4, 9, 16]
