---
language: swift
exerciseType: 3
---

# --description--

Every call to a function that returns a closure creates a **new** captured variable. Two closures built by separate calls don't share their state:
```swift
let first = makeCounter()
let second = makeCounter()
print(first())  // 1
print(first())  // 2
print(second()) // 1
```
The state is shared only between calls of the same closure.

# --instructions--

What does this code print? (`makeCounter` is the function from the previous exercises)
```swift
let a = makeCounter()
let b = makeCounter()
_ = a()
_ = a()
_ = b()
print(a() + b())
```

# --answers--

- 5
- 6
- 4
- 2

# --solutions--

- 5
