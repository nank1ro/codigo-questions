---
language: swift
exerciseType: 1
---

# --description--

`Array` is a generic type: `[Int]` and `[String]` are both arrays, with a different **`Element`** type. An extension of `Array` applies to all of them, which is a problem when the new member only makes sense for some elements: you can't add numbers that are strings.
A `where` clause restricts the extension to the arrays whose `Element` is a given type:
```swift
extension Array where Element == Int {
    var largest: Int {
        var result = Int.min
        for number in self {
            if number > result {
                result = number
            }
        }
        return result
    }
}
print([3, 9, 2].largest) // 9
```
`[3, 9, 2].largest` works, while `["a", "b"].largest` is a compile error: the property doesn't exist on `[String]`.

# --instructions--

Write an extension of `Array` restricted to `Element == Int` with a method `sum()` that returns the sum of the elements. Don't use `reduce`

Example of method call:
```swift
print([1, 2, 3].sum())
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

You must use an extension of `Array` with a `where Element == Int` clause

```json
{
  "regex": "extension\\s+Array\\s+where\\s+Element\\s*==\\s*Int",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

You must not use `reduce`

```json
{
  "regex": "reduce",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`[1, 2, 3].sum()` must return `6`

```swift
tryCatch([1, 2, 3].sum() == 6)
```

`[10, -4].sum()` must return `6`

```swift
tryCatch([10, -4].sum() == 6)
```

`[7].sum()` must return `7`

```swift
tryCatch([7].sum() == 7)
```

The sum of an empty array must be `0`

```swift
let _empty: [Int] = []
tryCatch(_empty.sum() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension Array where Element == Int {
    func sum() -> Int {
        var total = 0
        for number in self {
            total += number
        }
        return total
    }
}
```
