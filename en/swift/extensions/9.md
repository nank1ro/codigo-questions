---
language: swift
exerciseType: 2
---

# --description--

An extension can add new **initializers** to a type. For a struct this is the best place to put them: an `init` written inside the struct body replaces the automatic memberwise initializer, while one added in an extension keeps it.
The new initializer usually delegates to an existing one with `self.init(...)`:
```swift
struct Size {
    var width: Double
    var height: Double
}
extension Size {
    init(square side: Double) {
        self.init(width: side, height: side)
    }
}
let a = Size(square: 3)          // from the extension
let b = Size(width: 2, height: 5) // memberwise, still available
```

# --instructions--

Complete the extension so that `Point(both: 2.5)` creates a point with `x` and `y` equal to `2.5`, so that the program prints `2.5 2.5` and `1.0 2.0`

# --seed--

```swift
struct Point {
    var x: Double
    var y: Double
}
extension Point {
    [/](both: Double) {
        [/](x: both, y: both)
    }
}
let p = Point(both: 2.5)
let q = Point(x: 1, y: 2)
print(p.x, p.y)
print(q.x, q.y)
```

# --answers--

- init
- self.init
- func
- Point
- self

# --solutions--

```swift
struct Point {
    var x: Double
    var y: Double
}
extension Point {
    init(both: Double) {
        self.init(x: both, y: both)
    }
}
let p = Point(both: 2.5)
let q = Point(x: 1, y: 2)
print(p.x, p.y)
print(q.x, q.y)
```

# --output--

2.5 2.5
1.0 2.0
