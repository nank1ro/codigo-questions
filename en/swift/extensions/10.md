---
language: swift
exerciseType: 1
---

# --description--

Extensions are not only for other people's types. A common way to organise your own code is to keep the stored properties in the struct or class body and add the behaviour in one or more extensions, each grouping related members:
```swift
struct Circle {
    var radius: Double
}
extension Circle {
    var diameter: Double {
        return radius * 2
    }
    func grown(by amount: Double) -> Circle {
        return Circle(radius: radius + amount)
    }
}
```
Members added in an extension can use the stored properties directly, exactly as if they were written inside the type.

# --instructions--

The struct `Rectangle` is given. Write an extension of `Rectangle` with:
- a computed property `area` of type `Double`, the product of `width` and `height`
- a method `scaled(by factor: Double)` that returns a new `Rectangle` with both sides multiplied by `factor`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Rectangle {
    var width: Double
    var height: Double
}
```

# --asserts--

You must use an extension of `Rectangle`

```json
{
  "regex": "extension\\s+Rectangle",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

The area of a `2` by `3` rectangle must be `6.0`

```swift
tryCatch(Rectangle(width: 2, height: 3).area == 6.0)
```

The area of a `1.5` by `4` rectangle must be `6.0`

```swift
tryCatch(Rectangle(width: 1.5, height: 4).area == 6.0)
```

A `2` by `3` rectangle scaled by `2` must be `4` by `6`

```swift
let _r = Rectangle(width: 2, height: 3).scaled(by: 2)
tryCatch(_r.width == 4.0 && _r.height == 6.0)
```

Scaling must not change the original rectangle

```swift
let _o = Rectangle(width: 2, height: 3)
let _s = _o.scaled(by: 0.5)
tryCatch(_o.width == 2.0 && _o.height == 3.0 && _s.area == 1.5)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Rectangle {
    var width: Double
    var height: Double
}
extension Rectangle {
    var area: Double {
        return width * height
    }
    func scaled(by factor: Double) -> Rectangle {
        return Rectangle(width: width * factor, height: height * factor)
    }
}
```
