---
language: swift
exerciseType: 2
---

# --description--

Besides methods, an extension can add **computed properties**: properties that don't store a value but calculate it every time they are read.
A computed property is declared with `var`, a type annotation and a body between curly braces that returns the value:
```swift
extension Int {
    var isNegative: Bool {
        return self < 0
    }
}
print((-3).isNegative) // true
print(7.isNegative)    // false
```
It is read like any property, without parentheses: `7.isNegative`, not `7.isNegative()`.

# --instructions--

Complete the extension so that `isEven` is a computed property that tells whether the number is even, so that the program prints `true` and `false`

# --seed--

```swift
extension Int {
    [/] isEven: [/] {
        return self % 2 == 0
    }
}
print(4.isEven)
print(7.isEven)
```

# --answers--

- var
- Bool
- let
- func
- Int

# --solutions--

```swift
extension Int {
    var isEven: Bool {
        return self % 2 == 0
    }
}
print(4.isEven)
print(7.isEven)
```

# --output--

true
false
