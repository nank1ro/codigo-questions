---
language: swift
exerciseType: 1
---

# --description--

Static methods in extensions are a good home for small factory functions that build a value of the type. `String(repeating:count:)` is the standard initializer that repeats a piece of text a number of times:
```swift
extension String {
    static func dashes(_ count: Int) -> String {
        return String(repeating: "-", count: count)
    }
}
print(String.dashes(4)) // ----
```

# --instructions--

Write an extension of `String` with a static method `stars(_ count: Int)` that returns a string made of `count` asterisks `*`

Example of method call:
```swift
print(String.stars(3))
// prints ***
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

You must use an extension of `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

The method must be `static`

```json
{
  "regex": "static\\s+func\\s+stars",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`String.stars(3)` must return `"***"`

```swift
tryCatch(String.stars(3) == "***")
```

`String.stars(1)` must return `"*"`

```swift
tryCatch(String.stars(1) == "*")
```

`String.stars(0)` must return an empty string

```swift
tryCatch(String.stars(0) == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    static func stars(_ count: Int) -> String {
        return String(repeating: "*", count: count)
    }
}
```
