---
language: swift
exerciseType: 1
---

# --description--

`flatMap` is for closures that return an **array**: instead of building an array of arrays, it joins all the returned arrays into one flat result:
```swift
let teams = [["Ann", "Bob"], ["Cid"]]
print(teams.map { $0 })     // [["Ann", "Bob"], ["Cid"]]
print(teams.flatMap { $0 }) // ["Ann", "Bob", "Cid"]
```
The closure can also transform each inner array before it is flattened, for example `teams.flatMap { $0.reversed() }` gives `["Bob", "Ann", "Cid"]`.

# --instructions--

Write a function `flatten` that takes an array of arrays of `Int` (omit the argument label) and returns a single array with all the numbers in order, using `flatMap`

Example of function call:
```swift
print(flatten([[1, 2], [3]]))
// prints [1, 2, 3]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

You must use `flatMap`

```json
{
  "regex": "\\.flatMap",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`flatten([[1, 2], [3]])` must return `[1, 2, 3]`

```swift
tryCatch(flatten([[1, 2], [3]]) == [1, 2, 3])
```

`flatten([[5], [], [6, 7]])` must return `[5, 6, 7]`

```swift
tryCatch(flatten([[5], [], [6, 7]]) == [5, 6, 7])
```

`flatten([])` must return `[]`

```swift
tryCatch(flatten([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func flatten(_ lists: [[Int]]) -> [Int] {
    return lists.flatMap { $0 }
}
```
