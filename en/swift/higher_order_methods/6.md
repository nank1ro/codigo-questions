---
language: swift
exerciseType: 4
---

# --description--

`contains(where:)` and `allSatisfy` replace the common pattern of a loop with a flag variable. Both stop as soon as the answer is known: `contains(where:)` at the first match, `allSatisfy` at the first element that fails.
```swift
let ages = [15, 22, 40]
let anyMinor = ages.contains { $0 < 18 }  // true
let allAdults = ages.allSatisfy { $0 >= 18 } // false
```
`contains { ... }` is the trailing closure form of `contains(where:)`, not to be confused with `contains(_:)`, which looks for a specific value.

# --instructions--

Sort the items to check whether every score is at least `60` and whether any score is exactly `100`, then print both results on one line.
Declare `scores` first and print last

# --answers--

```swift
let scores = [72, 100, 64]
```

```swift
let allPassed = scores.allSatisfy { $0 >= 60 }
```

```swift
let anyPerfect = scores.contains { $0 == 100 }
```

```swift
print("\(allPassed) \(anyPerfect)")
```

# --solutions--

```swift
let scores = [72, 100, 64]
let allPassed = scores.allSatisfy { $0 >= 60 }
let anyPerfect = scores.contains { $0 == 100 }
print("\(allPassed) \(anyPerfect)")
```

```swift
let scores = [72, 100, 64]
let anyPerfect = scores.contains { $0 == 100 }
let allPassed = scores.allSatisfy { $0 >= 60 }
print("\(allPassed) \(anyPerfect)")
```

# --output--

true true
