---
language: swift
exerciseType: 2
---

# --description--

`zip` pairs up the elements of two sequences position by position, producing a sequence of tuples. It stops at the end of the shorter one:
```swift
let names = ["Ann", "Bob"]
let ages = [31, 27, 99]
let pairs = zip(names, ages).map { "\($0) is \($1)" }
print(pairs) // ["Ann is 31", "Bob is 27"]
```
Inside the closure `$0` is the element from the first sequence and `$1` the one from the second. `zip` is a free function, not a method: you write `zip(a, b)`, not `a.zip(b)`.

# --instructions--

Complete the code to pair each city with its temperature and keep only the cities warmer than `20` degrees, so that the program prints `["Rome", "Cairo"]`

# --seed--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = [/](cities, temps).[/] { $1 > 20 }.map { $0.0 }
print(warm)
```

# --answers--

- zip
- filter
- map
- enumerated
- compactMap

# --solutions--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = zip(cities, temps).filter { $1 > 20 }.map { $0.0 }
print(warm)
```

# --output--

["Rome", "Cairo"]
