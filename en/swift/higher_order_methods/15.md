---
language: swift
exerciseType: 1
---

# --description--

You can write your own higher-order functions. A function that takes a closure and **returns a new closure** built from it is a common pattern: the returned closure captures the original one, so the parameter must be `@escaping`.
For example, `negate` turns a predicate into its opposite, ready to be passed to `filter`:
```swift
func negate(_ predicate: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { !predicate($0) }
}
let isEven: (Int) -> Bool = { $0 % 2 == 0 }
print([1, 2, 3, 4].filter(negate(isEven))) // [1, 3]
```
Note that `filter(negate(isEven))` passes the closure as a normal argument, without trailing closure syntax.

# --instructions--

Write a function `both` that takes two closures of type `(Int) -> Bool` (omit both argument labels) and returns a closure of the same type that is `true` only when both of them are `true` for the same argument

Example of function call:
```swift
let smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
print([4, 12, 7, 8].filter(smallEven))
// prints [4, 8]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`both({ $0 < 10 }, { $0 % 2 == 0 })` must be `true` for `4`

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(4) == true)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })` must be `false` for `12`

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(12) == false)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })` must be `false` for `7`

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(7) == false)
```

The returned closure must work with `filter`

```swift
let _smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
tryCatch([4, 12, 7, 8].filter(_smallEven) == [4, 8])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func both(_ first: @escaping (Int) -> Bool, _ second: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { first($0) && second($0) }
}
```
