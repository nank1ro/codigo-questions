---
language: swift
exerciseType: 1
---

# --description--

`map` and `filter` on an array are **eager**: each one processes the whole array and builds a new one before the next step runs. On a large collection, or when you only need the first result, that is wasted work.
The `lazy` property returns a view whose operations run only when an element is actually requested, one element at a time through the whole chain:
```swift
let firstBig = (1...1000).lazy.map { $0 * $0 }.first { $0 > 50 }
print(firstBig!) // 64
```
Here only `1, 2, ..., 8` are squared: `first(where:)` asks for elements until one satisfies the condition, and the chain stops there. Without `lazy`, `map` would square all 1000 numbers first.

# --instructions--

Write a function `firstCubeAbove` that takes an `Int` called `limit` (omit the argument label) and returns the smallest cube (`n * n * n` for `n` starting from `1`) greater than `limit`, using `lazy` and `map` on the range `1...limit + 1` and `first(where:)`

Example of function call:
```swift
print(firstCubeAbove(10))
// prints 27
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

You must use `lazy`

```json
{
  "regex": "\\.lazy",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

You must not use a loop

```json
{
  "regex": "\\b(for|while)\\b",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`firstCubeAbove(10)` must return `27`

```swift
tryCatch(firstCubeAbove(10) == 27)
```

`firstCubeAbove(27)` must return `64`

```swift
tryCatch(firstCubeAbove(27) == 64)
```

`firstCubeAbove(0)` must return `1`

```swift
tryCatch(firstCubeAbove(0) == 1)
```

`firstCubeAbove(1000000)` must return `1030301`

```swift
tryCatch(firstCubeAbove(1000000) == 1030301)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstCubeAbove(_ limit: Int) -> Int {
    return (1...limit + 1).lazy.map { $0 * $0 * $0 }.first(where: { $0 > limit })!
}
```
