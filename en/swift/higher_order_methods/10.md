---
language: swift
exerciseType: 1
---

# --description--

`enumerated()` turns an array into a sequence of `(offset, element)` pairs, so a closure can use the position of each element together with its value:
```swift
let steps = ["mix", "bake"]
let numbered = steps.enumerated().map { pair in
    "\(pair.offset + 1). \(pair.element)"
}
print(numbered) // ["1. mix", "2. bake"]
```
Because each pair is a tuple, the closure can also destructure it: `.map { (i, step) in "\(i + 1). \(step)" }`.

# --instructions--

Write a function `evenPositions` that takes an array of `String` (omit the argument label) and returns the elements found at even indices (0, 2, 4, ...), using `enumerated()`, `filter` and `map`

Example of function call:
```swift
print(evenPositions(["a", "b", "c", "d", "e"]))
// prints ["a", "c", "e"]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

You must use `enumerated()`

```json
{
  "regex": "\\.enumerated\\s*\\(\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

You must not use a `for` loop

```json
{
  "regex": "\\bfor\\b",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`evenPositions(["a", "b", "c", "d", "e"])` must return `["a", "c", "e"]`

```swift
tryCatch(evenPositions(["a", "b", "c", "d", "e"]) == ["a", "c", "e"])
```

`evenPositions(["x", "y"])` must return `["x"]`

```swift
tryCatch(evenPositions(["x", "y"]) == ["x"])
```

`evenPositions([])` must return `[]`

```swift
tryCatch(evenPositions([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func evenPositions(_ items: [String]) -> [String] {
    return items.enumerated().filter { $0.offset % 2 == 0 }.map { $0.element }
}
```
