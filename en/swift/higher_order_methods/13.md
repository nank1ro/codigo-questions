---
language: swift
exerciseType: 1
---

# --description--

`Dictionary(grouping:by:)` splits a collection into a dictionary of arrays. The closure computes the **key** of each element, and all the elements with the same key end up in the same array:
```swift
let words = ["apple", "bee", "avocado"]
let byInitial = Dictionary(grouping: words, by: { $0.first! })
print(byInitial["a"]!) // ["apple", "avocado"]
```
`mapValues` transforms every value of a dictionary while keeping the keys, so it is the natural next step after grouping:
```swift
let sizes = byInitial.mapValues { $0.count }
print(sizes["a"]!) // 2
```

# --instructions--

Write a function `countByLength` that takes an array of `String` (omit the argument label) and returns a dictionary of type `[Int: Int]` mapping each word length to how many words have that length, using `Dictionary(grouping:by:)` and `mapValues`

Example of function call:
```swift
print(countByLength(["hi", "yo", "hey"])[2]!)
// prints 2
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

You must use `Dictionary(grouping:by:)`

```json
{
  "regex": "Dictionary\\s*\\(\\s*grouping",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

You must use `mapValues`

```json
{
  "regex": "\\.mapValues",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`countByLength(["hi", "yo", "hey"])` must return `[2: 2, 3: 1]`

```swift
tryCatch(countByLength(["hi", "yo", "hey"]) == [2: 2, 3: 1])
```

`countByLength(["a", "bb", "cc", "dd"])` must return `[1: 1, 2: 3]`

```swift
tryCatch(countByLength(["a", "bb", "cc", "dd"]) == [1: 1, 2: 3])
```

`countByLength([])` must return an empty dictionary

```swift
tryCatch(countByLength([]) == [:])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func countByLength(_ words: [String]) -> [Int: Int] {
    return Dictionary(grouping: words, by: { $0.count }).mapValues { $0.count }
}
```
