---
language: swift
exerciseType: 2
---

# --description--

To check whether a value is in a set use the `contains(_:)` method, which returns a `Bool`:
```swift
let primes: Set<Int> = [2, 3, 5, 7]
print(primes.contains(5)) // true
print(primes.contains(6)) // false
```
This check is very fast on a set, even with thousands of elements, which is one of the main reasons to prefer a set over an array for membership tests.

# --instructions--

Print `Welcome` if the `role` is one of the allowed roles, otherwise print `Access denied`

# --seed--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed[/]([/]) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --answers--

- .contains
- .has
- .includes
- role
- "role"
- allowed

# --solutions--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed.contains(role) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --output--

Welcome
