---
language: swift
exerciseType: 2
---

# --instructions--

Print, in ascending order, the days on which there is no meeting

# --seed--

```swift
let weekdays: Set<String> = ["mon", "tue", "wed"]
let meetings: Set<String> = ["tue", "sat"]
let free = weekdays.[/](meetings)
print(free.sorted())
```

# --answers--

- subtracting
- subtract
- intersection
- union
- symmetricDifference

# --solutions--

```swift
let weekdays: Set<String> = ["mon", "tue", "wed"]
let meetings: Set<String> = ["tue", "sat"]
let free = weekdays.subtracting(meetings)
print(free.sorted())
```

# --output--

["mon", "wed"]
