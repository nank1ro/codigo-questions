---
language: swift
exerciseType: 1
---

# --description--

Like arrays, sets can be constants (`let`) or variables (`var`). Only a `var` set can be changed after it is created.
To create an empty set you call the type's initializer, because an empty literal `[]` alone wouldn't tell Swift which element type to use:
```swift
var visited = Set<String>()
print(visited.isEmpty) // true
```
The `isEmpty` property is `true` when the set has no elements, exactly as with arrays.

# --instructions--

Declare a constant set of strings called `colors` containing `"red"`, `"green"` and `"blue"`, and an empty variable set of integers called `scores`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`colors` must contain exactly `"red"`, `"green"` and `"blue"`

```swift
tryCatch(colors == ["red", "green", "blue"])
```

`colors` must be a `Set<String>`

```swift
tryCatch(type(of: colors) == Set<String>.self)
```

`scores` must be empty

```swift
tryCatch(scores.isEmpty)
```

`scores` must be a `Set<Int>`

```swift
tryCatch(type(of: scores) == Set<Int>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let colors: Set<String> = ["red", "green", "blue"]
var scores = Set<Int>()
```
