---
language: swift
exerciseType: 4
---

# --description--

Sets support the classic operations of set theory. Each one returns a **new** set and leaves the originals unchanged:
- `a.union(b)` contains every element that is in `a`, in `b`, or in both
- `a.intersection(b)` contains only the elements that are in **both** `a` and `b`
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.union(b).sorted())        // [1, 2, 3, 4]
print(a.intersection(b).sorted()) // [3]
```

# --instructions--

Sort the items to print every number that appears in `odds` or in `primes`, in ascending order.
Declare `odds` before `primes`

# --answers--

```swift
let odds: Set<Int> = [1, 3, 5]
```

```swift
let primes: Set<Int> = [2, 3, 5]
```

```swift
let all = odds.union(primes)
```

```swift
print(all.sorted())
```

# --solutions--

```swift
let odds: Set<Int> = [1, 3, 5]
let primes: Set<Int> = [2, 3, 5]
let all = odds.union(primes)
print(all.sorted())
```

# --output--

[1, 2, 3, 5]
