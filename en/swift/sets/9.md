---
language: swift
exerciseType: 1
---

# --description--

Two more operations complete the family:
- `a.subtracting(b)` contains the elements of `a` that are **not** in `b`
- `a.symmetricDifference(b)` contains the elements that are in `a` or in `b`, but **not in both**
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.subtracting(b).sorted())          // [1, 2]
print(a.symmetricDifference(b).sorted())  // [1, 2, 4]
```
Unlike `union` and `intersection`, `subtracting` is not symmetric: `a.subtracting(b)` and `b.subtracting(a)` are usually different.

# --instructions--

Write two functions that take two `Set<String>` (omit the argument labels):
- `common` returns the set of the values present in both sets
- `onlyInFirst` returns the set of the values present in the first set but not in the second

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`common(["a", "b", "c"], ["b", "c", "d"])` must return `["b", "c"]`

```swift
tryCatch(common(["a", "b", "c"], ["b", "c", "d"]) == ["b", "c"])
```

`common(["a"], ["b"])` must return an empty set

```swift
tryCatch(common(["a"], ["b"]).isEmpty)
```

`onlyInFirst(["a", "b", "c"], ["b", "c", "d"])` must return `["a"]`

```swift
tryCatch(onlyInFirst(["a", "b", "c"], ["b", "c", "d"]) == ["a"])
```

`onlyInFirst(["x", "y"], ["z"])` must return `["x", "y"]`

```swift
tryCatch(onlyInFirst(["x", "y"], ["z"]) == ["x", "y"])
```

Both functions must return a `Set<String>`

```swift
tryCatch(type(of: common(["a"], ["a"])) == Set<String>.self && type(of: onlyInFirst(["a"], ["a"])) == Set<String>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func common(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.intersection(second)
}

func onlyInFirst(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.subtracting(second)
}
```
