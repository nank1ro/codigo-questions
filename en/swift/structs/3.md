---
language: swift
exerciseType: 2
---

# --description--

A stored property can be given a **default value** right where it is declared. Swift infers its type from that value, so you can drop the type annotation:
```swift
struct Counter {
    var label: String
    var value = 0
}
```
The memberwise initializer turns every defaulted property into an optional argument: pass it to override the default, leave it out to keep it.
```swift
let a = Counter(label: "clicks")
print(a.value) // 0

let b = Counter(label: "clicks", value: 7)
print(b.value) // 7
```

# --instructions--

Complete the code so `value` starts at `0` and `c` is created without passing `value`

# --seed--

```swift
struct Counter {
    var label: String
    var value = [/]
}

let c = Counter([/]: "clicks")
print(c.label)
print(c.value)
```

# --answers--

- 0
- label
- 1
- value
- Counter
- name

# --solutions--

```swift
struct Counter {
    var label: String
    var value = 0
}

let c = Counter(label: "clicks")
print(c.label)
print(c.value)
```

# --output--

clicks
0
