---
language: swift
exerciseType: 2
---

# --description--

When the memberwise initializer is not the way you want your type to be built, write your own **initializer**. It is declared with `init`, takes the parameters you choose, and must give every stored property a value before it ends. Inside it, `self` is the instance being created:
```swift
struct Square {
    var side: Int

    init(_ side: Int) {
        self.side = side
    }
}

let s = Square(5)
print(s.side) // 5
```
Writing an `init` inside the braces of the struct replaces the memberwise one, so from now on `Square(side: 5)` no longer exists.

# --instructions--

Complete the initializer so that `Square(5)` stores `5` in `side`

# --seed--

```swift
struct Square {
    var side: Int

    [/](_ side: [/]) {
        [/].side = side
    }
}

let s = Square(5)
print(s.side)
```

# --answers--

- init
- Int
- self
- func
- String
- this

# --solutions--

```swift
struct Square {
    var side: Int

    init(_ side: Int) {
        self.side = side
    }
}

let s = Square(5)
print(s.side)
```

# --output--

5
