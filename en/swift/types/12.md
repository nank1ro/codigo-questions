---
language: swift
exerciseType: 1
---

# --description--

`Int.max` and `Int.min` are useful as starting values when you search for an extreme: any real number is smaller than `Int.max`, so it is a safe initial value for "the smallest seen so far":
```swift
var smallest = Int.max
for number in [8, 3, 5] {
    if number < smallest {
        smallest = number
    }
}
print(smallest) // 3
```

# --instructions--

Write a function `smallest` that takes an array of `Int` (omit the argument label) and returns its smallest element, or `Int.max` when the array is empty. Don't use the `min()` method

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

You must not use `min()`

```json
{
  "regex": "\\.min\\(",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`smallest([8, 3, 5])` must return `3`

```swift
tryCatch(smallest([8, 3, 5]) == 3)
```

`smallest([-5, 5])` must return `-5`

```swift
tryCatch(smallest([-5, 5]) == -5)
```

`smallest([Int.min, 0])` must return `Int.min`

```swift
tryCatch(smallest([Int.min, 0]) == Int.min)
```

`smallest([])` must return `Int.max`

```swift
tryCatch(smallest([]) == Int.max)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func smallest(_ numbers: [Int]) -> Int {
    var result = Int.max
    for number in numbers {
        if number < result {
            result = number
        }
    }
    return result
}
```
