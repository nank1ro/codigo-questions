---
language: swift
exerciseType: 2
---

# --description--

Swift never converts between number types on its own: adding an `Int` to a `Double` is a compile error, even though both are numbers.
```swift
let apples = 3
let price = 1.5
let total = apples * price // error: Int and Double can't be mixed
```
To combine them you create a new value of the type you need, by passing the value to the type's initializer:
```swift
let total = Double(apples) * price // 4.5
```
The same works the other way round: `Int(4.5)` produces an `Int`, keeping only the whole part of the number.

# --instructions--

Complete the conversions so that `total` is the cost of the apples as a `Double` and `whole` is its whole part as an `Int`, so that the program prints `4.5` and `4`

# --seed--

```swift
let apples = 3
let price = 1.5
let total = [/](apples) * price
let whole = [/](total)
print(total)
print(whole)
```

# --answers--

- Double
- Int
- Float
- String
- Character

# --solutions--

```swift
let apples = 3
let price = 1.5
let total = Double(apples) * price
let whole = Int(total)
print(total)
print(whole)
```

# --output--

4.5
4
