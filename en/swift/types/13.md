---
language: swift
exerciseType: 2
---

# --description--

As you saw in the strings lessons, iterating over a `String` gives you one `Character` at a time. A `Character` is not a `String`, so to use it as text you convert it with `String(c)`.
When the character is a digit, the `wholeNumberValue` property gives you its numeric value as an `Int?`: it is `nil` for characters that aren't digits.
```swift
for c in "a1" {
    print(c.wholeNumberValue)
}
// nil
// Optional(1)
```

# --instructions--

Complete the loop so that it adds up the digits of the string, so that the program prints `8`

# --seed--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.[/] {
        sum [/] digit
    }
}
print(sum)
```

# --answers--

- wholeNumberValue
- +=
- asciiValue
- isNumber
- -=
- =

# --solutions--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.wholeNumberValue {
        sum += digit
    }
}
print(sum)
```

# --output--

8
