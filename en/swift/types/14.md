---
language: swift
exerciseType: 1
---

# --description--

Because `Int(text)` and `Double(text)` return `nil` on failure, comparing the result with `nil` tells you whether a text is a number of that kind:
```swift
print(Int("42") != nil)     // true
print(Double("4.2") != nil) // true
print(Double("42") != nil)  // true, a whole number is also a valid Double
```
Note the last line: every text accepted by `Int` is accepted by `Double` too, so check for `Int` first when you want to tell them apart.

# --instructions--

Write a function `classify` that takes a `String` (omit the argument label) and returns `"Int"` if the text is a valid whole number, `"Double"` if it is a valid decimal number, and `"String"` otherwise

Example of function call:
```swift
print(classify("3.5"))
// prints Double
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`classify("42")` must return `"Int"`

```swift
tryCatch(classify("42") == "Int")
```

`classify("-7")` must return `"Int"`

```swift
tryCatch(classify("-7") == "Int")
```

`classify("3.5")` must return `"Double"`

```swift
tryCatch(classify("3.5") == "Double")
```

`classify("hello")` must return `"String"`

```swift
tryCatch(classify("hello") == "String")
```

`classify("")` must return `"String"`

```swift
tryCatch(classify("") == "String")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func classify(_ text: String) -> String {
    if Int(text) != nil {
        return "Int"
    }
    if Double(text) != nil {
        return "Double"
    }
    return "String"
}
```
