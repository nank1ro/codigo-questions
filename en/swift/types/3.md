---
language: swift
exerciseType: 1
---

# --description--

The `type(of:)` function returns the type of a value, which is handy to check what Swift inferred:
```swift
print(type(of: 42))    // Int
print(type(of: 2.5))   // Double
print(type(of: "hi"))  // String
```
When you want a type different from the inferred one, add an annotation. A whole-number literal can be stored in a `Double` or `Float` constant, and a one-character literal in a `Character` constant:
```swift
let ratio: Double = 3       // 3.0, not an Int
let half: Float = 0.5
let initial: Character = "S"
print(type(of: ratio))      // Double
```

# --instructions--

Declare three constants:
- `ratio`, a `Double` holding the whole-number literal `3`
- `half`, a `Float` holding `0.5`
- `initial`, a `Character` holding `"S"`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`ratio` must be a `Double` equal to `3.0`

```swift
tryCatch(type(of: ratio) == Double.self && ratio == 3.0)
```

`half` must be a `Float` equal to `0.5`

```swift
tryCatch(type(of: half) == Float.self && half == 0.5)
```

`initial` must be a `Character` equal to `"S"`

```swift
tryCatch(type(of: initial) == Character.self && initial == "S")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let ratio: Double = 3
let half: Float = 0.5
let initial: Character = "S"
```
