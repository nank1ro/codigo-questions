<section id="options">
{
"language": "swift",
"type": "runCode",
"showRun": true
}
</section>

<section id="description">
__"Hello, World!"__ is the traditional first program for beginning programming in a new language or environment.
</section>

<section id="instructions">
Write a function that returns the string "Hello, World!".
</section>

<section id="seed">
func hello() -> String {
    
}
</section>

<section id="test">
import XCTest

class HelloWorldTests: XCTestCase {

    func testHi() {
        let expected = "Hello, World!"
        XCTAssertEqual(hello(), expected, "Your function should return \"Hello, World!\"")
    }

    static var allTests: [(String, (HelloWorldTests) -> () throws -> Void)] {
        return [
            ("testHi", testHi),
        ]
    }
}

XCTMain([testCase(HelloWorldTests.allTests),])
</section>