---
language: swift
exerciseType: 4
---

# --description--

Binding several optionals in one `if let` keeps the code flat: one `else` branch covers every missing value.

# --instructions--

Sort the items to print `Area: 20` when both `width` and `height` have a value, and `Missing size` otherwise.
Declare `width` before `height`

# --answers--

```swift
let width: Int? = 4
```

```swift
let height: Int? = 5
```

```swift
if let w = width, let h = height {
```

```swift
    print("Area: \(w * h)")
```

```swift
} else {
    print("Missing size")
}
```

# --solutions--

```swift
let width: Int? = 4
let height: Int? = 5
if let w = width, let h = height {
    print("Area: \(w * h)")
} else {
    print("Missing size")
}
```

# --output--

Area: 20
