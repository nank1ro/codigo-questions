---
language: swift
exerciseType: 4
---

# --description--

Forced unwrapping is safe only when you have already checked that the optional is not `nil`:
```swift
if score != nil {
    print(score! * 2)
}
```

# --instructions--

Sort the items so that `price` is doubled and printed only after checking that it is not `nil`

# --answers--

```swift
let price: Double? = 9.5
```

```swift
if price != nil {
```

```swift
    print(price! * 2)
```

```swift
}
```

# --solutions--

```swift
let price: Double? = 9.5
if price != nil {
    print(price! * 2)
}
```

# --output--

19.0
