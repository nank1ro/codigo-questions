---
language: swift
exerciseType: 1
---

# --description--

A single `if let` or `guard let` can unwrap several optionals at once: separate the bindings with commas.
The body runs only if every optional has a value:
```swift
let first: String? = "Ada"
let last: String? = "Lovelace"
if let first = first, let last = last {
    print("\(first) \(last)")
}
```
You can also append a boolean condition after the bindings, like `if let n = number, n > 0`.

# --instructions--

Write a function `add` that takes two `Int?` values (omit the argument labels) and returns their sum as an `Int?`, or `nil` if either of them is missing

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`add(2, 3)` must return `5`

```swift
tryCatch(add(2, 3) == 5)
```

`add(nil, 3)` must return `nil`

```swift
tryCatch(add(nil, 3) == nil)
```

`add(2, nil)` must return `nil`

```swift
tryCatch(add(2, nil) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func add(_ a: Int?, _ b: Int?) -> Int? {
    guard let a = a, let b = b else { return nil }
    return a + b
}
```
