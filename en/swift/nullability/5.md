---
language: swift
exerciseType: 1
---

# --description--

Checking for `nil` and then force unwrapping is verbose. Swift offers **optional binding** with `if let`, which unwraps the optional and stores the value in a new constant in a single step:
```swift
let score: Int? = 10
if let value = score {
    print("Score: \(value)") // value is an Int, not an Int?
} else {
    print("No score")
}
```
The body of the `if` runs only when the optional contains a value; inside it `value` is a plain `Int` and needs no `!`.

# --instructions--

Write a function `describe` that takes an `Int?` (omit the argument label) and returns `"Value: "` followed by the number (for example `"Value: 5"` for `5`) when the optional contains a value, or `"No value"` when it is `nil`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`describe(5)` must return `"Value: 5"`

```swift
tryCatch(describe(5) == "Value: 5")
```

`describe(42)` must return `"Value: 42"`

```swift
tryCatch(describe(42) == "Value: 42")
```

`describe(nil)` must return `"No value"`

```swift
tryCatch(describe(nil) == "No value")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func describe(_ number: Int?) -> String {
    if let value = number {
        return "Value: \(value)"
    } else {
        return "No value"
    }
}
```
