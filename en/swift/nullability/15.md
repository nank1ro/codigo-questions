---
language: swift
exerciseType: 1
---

# --description--

Many operations can fail, and Swift reports the failure by returning an optional.
Converting text to a number is the classic example: `Int("42")` returns an `Int?` holding `42`, while `Int("abc")` returns `nil`.
`Int("3.5")` is `nil` as well, because the text is not a whole number; use `Double("3.5")` for decimals.
```swift
let typed = "42"
if let number = Int(typed) {
    print(number + 1) // 43
}
```
Other examples are `array.first` (`nil` for an empty array) and `dictionary[key]` (`nil` when the key is missing).

# --instructions--

Write a function `parseAge` that takes a `String` (omit the argument label) and returns it converted to an `Int`, or `-1` when the text is not a valid whole number

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`parseAge("42")` must return `42`

```swift
tryCatch(parseAge("42") == 42)
```

`parseAge("abc")` must return `-1`

```swift
tryCatch(parseAge("abc") == -1)
```

`parseAge("3.5")` must return `-1`

```swift
tryCatch(parseAge("3.5") == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func parseAge(_ text: String) -> Int {
    return Int(text) ?? -1
}
```
