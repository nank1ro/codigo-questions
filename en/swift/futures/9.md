---
language: swift
exerciseType: 1
---

# --description--

`async let` is tied to the scope where it is written. To start concurrent work and keep a handle on it, use a **`Task`**. The closure passed to `Task { }` runs on its own, and the task can be stored, passed around or returned:
```swift
let job = Task {
    return await double(21)
}
```
The result is read later with **`.value`**, which is awaited:
```swift
print(await job.value)
// prints 42
```
The type of the handle says what it produces and what it can throw: `Task<Int, Never>` is a task that returns an `Int` and never throws. Unlike `async let`, a `Task` can be created from ordinary, non-asynchronous code.

# --instructions--

Write a function `startDoubling` that takes an integer without argument label and returns a `Task<Int, Never>` producing twice that number

Example of function call:
```swift
print(await startDoubling(4).value)
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func startDoubling() {

}
```

# --asserts--

The function must create a `Task`

```json
{
  "regex": "Task\\s*\\{",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`startDoubling(4).value` must be `8`

```swift
tryCatch(await startDoubling(4).value == 8)
```

`startDoubling(0).value` must be `0`

```swift
tryCatch(await startDoubling(0).value == 0)
```

`startDoubling(-6).value` must be `-12`

```swift
tryCatch(await startDoubling(-6).value == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func startDoubling(_ n: Int) -> Task<Int, Never> {
    return Task {
        return n * 2
    }
}
```
