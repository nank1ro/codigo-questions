---
language: swift
exerciseType: 1
---

# --description--

`Task.sleep` pauses the current task for a while without blocking anything else. It can be interrupted, so it is a throwing asynchronous call and needs `try await`. The duration is given with helpers such as `.seconds`, `.milliseconds` or `.nanoseconds`:
```swift
func slowGreeting() async throws -> String {
    try await Task.sleep(for: .milliseconds(50))
    return "hello"
}
```
This is the standard way to simulate slow work in an example, instead of a real network call. Note that it does not freeze the program: while one task sleeps, the others keep running.

# --instructions--

Write a function `delayed` that takes an integer without argument label, is asynchronous, can throw, waits `50` milliseconds with `Task.sleep`, and then returns the number multiplied by `3`

Example of function call:
```swift
print(try await delayed(2))
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func delayed() {

}
```

# --asserts--

The function must be marked `async throws`

```json
{
  "regex": "func\\s+delayed\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

The function must use `Task.sleep`

```json
{
  "regex": "Task\\.sleep",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`delayed(2)` must return `6`

```swift
tryCatch((try? await delayed(2)) == 6)
```

`delayed(0)` must return `0`

```swift
tryCatch((try? await delayed(0)) == 0)
```

`delayed(-4)` must return `-12`

```swift
tryCatch((try? await delayed(-4)) == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func delayed(_ n: Int) async throws -> Int {
    try await Task.sleep(for: .milliseconds(50))
    return n * 3
}
```
