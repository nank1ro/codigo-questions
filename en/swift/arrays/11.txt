<section id="options">
{
"language": "swift",
"type": "fillEmptySpaces"
}
</section>
<section id="description">
**Tuples** are like arrays but you can name the elements and use those names to refer to them
To create a tuple we use the round brackets `()` 
```
var person = (firstName: "John", lastName: "Smith")
var firstName = person.firstName // John
var lastName = person.lastName // Smith
```
</section>
<section id="instructions">
Create a valid tuple
</section>
<section id="fillEmptySpaces">
{
    "seed": ["var coordinates = ", "[/]","x: 5, y: 15","[/]","\nprint(coordinates.x)\nprint(coordinates.y)"],
	"buttons": ["(", ")", "[", "]", "{", "}"],
	"correct": "var coordinates = (x: 5, y: 15)\nprint(coordinates.x)\nprint(coordinates.y)"
}
</section>
<section id="preOutput">
5
15
</section>