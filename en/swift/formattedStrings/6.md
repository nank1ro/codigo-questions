---
language: swift
exerciseType: 1
---

# --description--

Another flag is `0`: instead of spaces, the field is filled with zeros on the left. This is how you get numbers like `007` or `00042`:
```swift
print(String(format: "%05d", 42))  // 00042
print(String(format: "%03d", 7))   // 007
print(String(format: "%03d", 1234)) // 1234
```
As with spaces, a value longer than the width is never cut.

# --instructions--

Write a function `ticket` that takes an integer without argument label and returns `T-` followed by the number padded with zeros to width `5`, using `String(format:)`

Example of function call:
```swift
print(ticket(42))
// prints T-00042
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func ticket() {

}
```

# --asserts--

The function must use `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ticket(42)` must return `"T-00042"`

```swift
tryCatch(ticket(42) == "T-00042")
```

`ticket(7)` must return `"T-00007"`

```swift
tryCatch(ticket(7) == "T-00007")
```

`ticket(12345)` must return `"T-12345"`

```swift
tryCatch(ticket(12345) == "T-12345")
```

`ticket(123456)` must return `"T-123456"`

```swift
tryCatch(ticket(123456) == "T-123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func ticket(_ n: Int) -> String {
    return String(format: "T-%05d", n)
}
```
