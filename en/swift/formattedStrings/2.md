---
language: swift
exerciseType: 1
---

# --description--

The second tool is `String(format:)`, which comes from the **Foundation** framework, so the file must start with `import Foundation`.

It takes a **format string** followed by the values to insert. Inside the format string, a **specifier** starting with `%` marks where each value goes and how it is written. The specifier for an integer is `%d`:
```swift
import Foundation

let count = 7
let text = String(format: "Item %d", count)
print(text) // Item 7
```
`String(format:)` returns a normal `String`, so you can print it, store it or return it from a function.

# --instructions--

Write a function `label` that takes an integer without argument label and returns the text `Item ` followed by the number, using `String(format:)` with `%d`

Example of function call:
```swift
print(label(7))
// prints Item 7
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func label() {

}
```

# --asserts--

The function must use `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`label(7)` must return `"Item 7"`

```swift
tryCatch(label(7) == "Item 7")
```

`label(42)` must return `"Item 42"`

```swift
tryCatch(label(42) == "Item 42")
```

`label(0)` must return `"Item 0"`

```swift
tryCatch(label(0) == "Item 0")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func label(_ n: Int) -> String {
    return String(format: "Item %d", n)
}
```
