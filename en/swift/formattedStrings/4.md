---
language: swift
exerciseType: 1
---

# --description--

A number between `%` and the letter sets the **minimum width** of the field. If the value is shorter, spaces are added on the left so it is **right-aligned**; if it is longer, nothing is cut:
```swift
print(String(format: "%5d|", 42))    //    42|
print(String(format: "%5d|", 12345)) // 12345|
```
Width and precision combine: `%8.2f` means "at least 8 characters wide, with 2 decimals":
```swift
print(String(format: "%8.2f|", 3.14159)) //     3.14|
```
Fixed widths are what line up the columns of a table.

# --instructions--

Write a function `rightAligned` that takes an integer without argument label and returns it as a string right-aligned in a field of width `5`, using `String(format:)`

Example of function call:
```swift
print(rightAligned(42) + "|")
// prints    42|
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func rightAligned() {

}
```

# --asserts--

The function must use `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`rightAligned(42)` must return `"   42"`

```swift
tryCatch(rightAligned(42) == "   42")
```

`rightAligned(7)` must return `"    7"`

```swift
tryCatch(rightAligned(7) == "    7")
```

`rightAligned(12345)` must return `"12345"`

```swift
tryCatch(rightAligned(12345) == "12345")
```

`rightAligned(123456)` must return `"123456"` (nothing is cut)

```swift
tryCatch(rightAligned(123456) == "123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func rightAligned(_ n: Int) -> String {
    return String(format: "%5d", n)
}
```
