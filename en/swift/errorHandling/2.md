---
language: swift
exerciseType: 1
---

# --description--

A throwing function can still return a value. The `throws` keyword goes between the parameter list and the return arrow:
```swift
func square(_ n: Int) throws -> Int {
    if n < 0 {
        throw SquareError.negative
    }
    return n * n
}
```
Reading it out loud: *square takes an `Int`, may throw, and returns an `Int`*.

At the call site the value only exists when nothing was thrown, so the assignment lives inside the `do` block:
```swift
do {
    let result = try square(4)
    print(result) // 16
} catch {
    print("failed")
}
```
The `try` is not optional decoration: the compiler refuses the call without it, so a reader always sees which lines can fail.

# --instructions--

Mark `half` as throwing, make it throw `MathError.notEven` when `n` is odd, and otherwise return `n / 2`

Example of function call:
```swift
do {
    print(try half(10))
} catch {
    print("odd")
}
// prints 5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum MathError: Error {
    case notEven
}

func half(_ n: Int) -> Int {
    
}
```

# --before-asserts--

```swift
func _half(_ n: Int) -> Int {
    do {
        return try half(n)
    } catch {
        return -999
    }
}
```

# --asserts--

`half(10)` must return `5`

```swift
tryCatch(_half(10) == 5)
```

`half(0)` must return `0`

```swift
tryCatch(_half(0) == 0)
```

`half(7)` must throw

```swift
tryCatch(_half(7) == -999)
```

`half(-4)` must return `-2`

```swift
tryCatch(_half(-4) == -2)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum MathError: Error {
    case notEven
}

func half(_ n: Int) throws -> Int {
    if n % 2 != 0 {
        throw MathError.notEven
    }
    return n / 2
}
```
