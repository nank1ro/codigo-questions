---
language: swift
exerciseType: 4
---

# --description--

A `Character` is not a `String`, so it can't be joined to a string with `+` directly. Convert it first with `String(...)`:
```swift
let letter: Character = "a"
let text = String(letter) + "bc" // abc
```
Combining this with a `for`-`in` loop lets you rebuild a string one character at a time, for example putting each new character in front of the ones collected so far.

# --instructions--

Sort the items to print `swift` reversed, building the result one character at a time.
Declare `word` before `reversed`

# --answers--

```swift
let word = "swift"
```

```swift
var reversed = ""
```

```swift
for letter in word {
```

```swift
    reversed = String(letter) + reversed
```

```swift
}
```

```swift
print(reversed)
```

# --solutions--

```swift
let word = "swift"
var reversed = ""
for letter in word {
    reversed = String(letter) + reversed
}
print(reversed)
```

# --output--

tfiws
