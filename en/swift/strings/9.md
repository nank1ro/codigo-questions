---
language: swift
exerciseType: 1
---

# --description--

Working with indices is verbose, so Swift offers shortcuts for the most common cases:
- `first` and `last` return the first and last character as an optional `Character?` (`nil` for an empty string)
- `prefix(n)` returns the first `n` characters and `suffix(n)` the last `n`
```swift
let word = "Swift"
print(word.first!)     // S
print(word.prefix(2))  // Sw
print(word.suffix(3))  // ift
```
`prefix` and `suffix` return a `Substring`, a view on the original text. To store it as a real `String` wrap it in `String(...)`. If `n` is larger than `count`, you simply get the whole string.

# --instructions--

Write a function `firstTwo` that takes a `String` (omit the argument label) and returns its first two characters as a `String`, and a function `lastTwo` that returns its last two characters as a `String`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`firstTwo("Swift")` must return `"Sw"`

```swift
tryCatch(firstTwo("Swift") == "Sw")
```

`firstTwo("a")` must return `"a"`

```swift
tryCatch(firstTwo("a") == "a")
```

`lastTwo("Swift")` must return `"ft"`

```swift
tryCatch(lastTwo("Swift") == "ft")
```

`lastTwo("")` must return `""`

```swift
tryCatch(lastTwo("") == "")
```

`firstTwo` must return a `String`

```swift
tryCatch(type(of: firstTwo("Swift")) == String.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstTwo(_ text: String) -> String {
    return String(text.prefix(2))
}

func lastTwo(_ text: String) -> String {
    return String(text.suffix(2))
}
```
