---
language: swift
exerciseType: 2
---

# --description--

`uppercased()` and `lowercased()` return a **new** string with every letter converted to upper or lower case. The original string is not modified:
```swift
let name = "Swift"
print(name.uppercased()) // SWIFT
print(name.lowercased()) // swift
print(name)              // Swift
```
Both are methods, so don't forget the parentheses.

# --instructions--

Print `quiet please` in upper case, then print `QUIET` in lower case

# --seed--

```swift
let shout = "quiet please".[/]()
print(shout)
print("QUIET".[/]())
```

# --answers--

- uppercased
- lowercased
- uppercase
- lowercase
- capitalized

# --solutions--

```swift
let shout = "quiet please".uppercased()
print(shout)
print("QUIET".lowercased())
```

# --output--

QUIET PLEASE
quiet
