---
language: swift
exerciseType: 2
---

# --description--

Two strings can be joined with the `+` operator, which produces a new string:
```swift
let full = "Hello" + " " + "world" // Hello world
```
To add text to the end of an existing string variable use `+=`. The variable must be declared with `var`, because its value changes:
```swift
var log = "Start"
log += "..."
print(log) // Start...
```

# --instructions--

Declare a variable `message` holding `Hello`, append `, world` to it with the compound assignment operator, then print it

# --seed--

```swift
[/] message = "Hello"
message [/] ", world"
print(message)
```

# --answers--

- var
- let
- +=
- +
- =
- -=

# --solutions--

```swift
var message = "Hello"
message += ", world"
print(message)
```

# --output--

Hello, world
