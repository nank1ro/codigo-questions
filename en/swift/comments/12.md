---
language: swift
exerciseType: 4
---

# --description--

After the summary, a documentation comment can describe the parameters and the return value with special Markdown list items that Xcode recognises:
```swift
/// Returns the number of seconds in the given minutes.
/// - Parameter minutes: a whole number of minutes, never negative
/// - Returns: `minutes` multiplied by sixty
func toSeconds(_ minutes: Int) -> Int {
    return minutes * 60
}
```
The order is always the same: summary first, then `- Parameter name:` for each parameter, then `- Returns:`.

# --instructions--

Sort the items to document `twice` with a summary, a `Parameter` line and a `Returns` line, in this order and directly above the function, then print `twice(21)`

# --answers--

```swift
/// Returns the double of a number.
```

```swift
/// - Parameter n: the number to double
```

```swift
/// - Returns: `n` multiplied by two
```

```swift
func twice(_ n: Int) -> Int {
    return n * 2
}
```

```swift
print(twice(21))
```

# --solutions--

```swift
/// Returns the double of a number.
/// - Parameter n: the number to double
/// - Returns: `n` multiplied by two
func twice(_ n: Int) -> Int {
    return n * 2
}
print(twice(21))
```

# --output--

42
