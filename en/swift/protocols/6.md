---
language: swift
exerciseType: 4
---

# --description--

A protocol usually collects more than one requirement, and a conforming type must satisfy every one of them:
```swift
protocol Vehicle {
    var wheels: Int { get }
    func move() -> String
}

struct Bike: Vehicle {
    var wheels = 2

    func move() -> String {
        return "pedalling"
    }
}
```
The order of the requirements inside the braces does not matter, and neither does the order in which the conforming type provides them: the compiler only checks that nothing is missing.

# --instructions--

Sort the items to print `Moo`, declaring the protocol first, then the struct, then the instance before the `print`

# --answers--

```swift
protocol Animal {
    func sound() -> String
}
```

```swift
struct Cow: Animal {
    func sound() -> String { return "Moo" }
}
```

```swift
let cow = Cow()
```

```swift
print(cow.sound())
```

# --solutions--

```swift
protocol Animal {
    func sound() -> String
}
struct Cow: Animal {
    func sound() -> String { return "Moo" }
}
let cow = Cow()
print(cow.sound())
```

# --output--

Moo
