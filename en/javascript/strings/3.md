---
language: javascript
exerciseType: 2
---

# --description--

Strings come with many built-in **methods**. Two of the simplest change the case of every letter:
```javascript
let word = "Hello";
console.log(word.toUpperCase());
// prints HELLO
console.log(word.toLowerCase());
// prints hello
```
Both methods take no arguments, so remember the parentheses.

# --instructions--

Print `name` in uppercase, then in lowercase

# --seed--

```javascript
let name = "Ada";
console.log(name.[/]());
console.log(name.[/]());
```

# --answers--

- toUpperCase
- toLowerCase
- upper
- lower
- capitalize

# --solutions--

```javascript
let name = "Ada";
console.log(name.toUpperCase());
console.log(name.toLowerCase());
```

# --output--

ADA
ada
