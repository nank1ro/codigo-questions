---
language: javascript
exerciseType: 2
---

# --description--

Strings are **immutable**: once created, a string can never be changed.
Assigning to an index does nothing, and every string method returns a **new** string instead of modifying the original:
```javascript
let word = "hello";
word[0] = "j";
console.log(word);
// prints hello
word.toUpperCase();
console.log(word);
// prints hello
```
To keep a result, assign it back to the variable:
```javascript
word = word.toUpperCase();
```

# --instructions--

The first `console.log` prints `hello` because `toUpperCase()` did not change `word`. Complete the code so that the second `console.log` prints `HELLO`

# --seed--

```javascript
let word = "hello";
word.toUpperCase();
console.log(word);
[/] = word.[/]();
console.log(word);
```

# --answers--

- word
- let word
- const word
- toUpperCase
- upper

# --solutions--

```javascript
let word = "hello";
word.toUpperCase();
console.log(word);
word = word.toUpperCase();
console.log(word);
```

# --output--

hello
HELLO
