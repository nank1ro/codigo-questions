---
language: javascript
exerciseType: 1
---

# --description--

To check whether a string contains another string, use these methods, which all return a boolean:
- `includes(text)` is `true` if `text` appears anywhere
- `startsWith(text)` is `true` if the string begins with `text`
- `endsWith(text)` is `true` if the string ends with `text`

```javascript
let file = "photo.png";
console.log(file.includes("."));
// prints true
console.log(file.startsWith("ph"));
// prints true
console.log(file.endsWith(".jpg"));
// prints false
```
The comparison is case-sensitive: `"Hello".includes("h")` is `false`.

# --instructions--

Write a function `isImage(fileName)` that returns `true` when `fileName` ends with `".png"` and `false` otherwise

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isImage(fileName) {

}
```

# --asserts--

`isImage("cat.png")` must return `true`.

```javascript
tryCatch(isImage("cat.png") === true);
```

`isImage("cat.jpg")` must return `false`.

```javascript
tryCatch(isImage("cat.jpg") === false);
```

`isImage("png.txt")` must return `false`.

```javascript
tryCatch(isImage("png.txt") === false);
```

`isImage("a.png.txt")` must return `false`.

```javascript
tryCatch(isImage("a.png.txt") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isImage(fileName) {
  return fileName.endsWith(".png");
}
```
