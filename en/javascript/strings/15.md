---
language: javascript
exerciseType: 4
---

# --instructions--

Sort the lines to print a receipt row: `item` padded with dots to 8 characters, followed by the price right-aligned in 6 characters

# --answers--

```javascript
let item = "Tea";
```

```javascript
let row = item.padEnd(8, ".");
```

```javascript
row = row + "2.50".padStart(6);
```

```javascript
console.log(row);
```

# --solutions--

```javascript
let item = "Tea";
let row = item.padEnd(8, ".");
row = row + "2.50".padStart(6);
console.log(row);
```

# --output--

Tea.....  2.50
