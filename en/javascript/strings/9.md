---
language: javascript
exerciseType: 2
---

# --description--

The `split(separator)` method breaks a string into an **array** of pieces, cutting at every `separator`:
```javascript
let sentence = "I like JavaScript";
let words = sentence.split(" ");
console.log(words);
// prints [ 'I', 'like', 'JavaScript' ]
```
The opposite is the array method `join(separator)`, which glues the pieces back into a string:
```javascript
console.log(words.join("-"));
// prints I-like-JavaScript
```

# --instructions--

Split `csv` at every comma and print how many colors it contains

# --seed--

```javascript
let csv = "red,green,blue";
let colors = csv.[/]([/]);
console.log(colors.[/]);
```

# --answers--

- split
- ","
- " "
- join
- length
- size

# --solutions--

```javascript
let csv = "red,green,blue";
let colors = csv.split(",");
console.log(colors.length);
```

# --output--

3
