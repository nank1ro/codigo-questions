---
language: javascript
exerciseType: 1
---

# --description--

The `padStart(targetLength, padString)` method adds `padString` to the **start** of the string until it reaches `targetLength` characters. `padEnd()` does the same at the end:
```javascript
console.log("7".padStart(3, "0"));
// prints 007
console.log("Tea".padEnd(6, "."));
// prints Tea...
```
If the string is already long enough, it is returned unchanged.
Numbers do not have string methods, so convert them first with `String(number)`.

# --instructions--

Write a function `twoDigits(number)` that returns `number` as a string of at least two characters, padded on the left with `"0"`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function twoDigits(number) {

}
```

# --asserts--

`twoDigits(5)` must return `"05"`.

```javascript
tryCatch(twoDigits(5) === "05");
```

`twoDigits(12)` must return `"12"`.

```javascript
tryCatch(twoDigits(12) === "12");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function twoDigits(number) {
  return String(number).padStart(2, "0");
}
```
