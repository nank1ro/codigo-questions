---
language: javascript
exerciseType: 2
---

# --description--

`static` works on fields too. A **static property** is stored once on the class, not once per instance, which makes it the natural place for a shared counter or a constant:
```javascript
class Circle {
    static PI = 3.14;
}
console.log(Circle.PI);
// prints 3.14
```
Because there is only one copy, every instance that updates it updates the same value. Inside a constructor you reach it through the class name, `Circle.PI`, and not through `this`: `this.PI` would look for a property on the instance, find nothing, and give you `undefined`.

# --instructions--

Complete the code so that `User` counts how many users have been created, then print the total

# --seed--

```javascript
class User {
    static [/] = 0;
    constructor(name) {
        this.name = name;
        [/].count += 1;
    }
}
new User("ann");
new User("bob");
console.log(User.count);
```

# --answers--

- count
- static count
- User
- this
- new User

# --solutions--

```javascript
class User {
    static count = 0;
    constructor(name) {
        this.name = name;
        User.count += 1;
    }
}
new User("ann");
new User("bob");
console.log(User.count);
```

# --output--

2
