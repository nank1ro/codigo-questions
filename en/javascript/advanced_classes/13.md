---
language: javascript
exerciseType: 1
---

# --description--

Methods can be private too. Prefix the name with `#` and the method disappears from the public surface of the class, while still being callable from any other method with `this.#name(...)`:
```javascript
class Receipt {
    #format(n) {
        return `$${n}`;
    }
    print(n) {
        return this.#format(n);
    }
}
console.log(new Receipt().print(7));
// prints $7
```
This is how you keep helper steps out of the API: the caller sees `print`, not the formatting detail behind it. Private fields and private methods together give a class a clear inside and outside.

# --instructions--

Write a class `Score` with a private field `#points` starting at `0`, a private method `#add` that adds a number only when it is greater than `0`, a public method `record` that forwards its argument to `#add`, and a getter `total` that returns the points

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Score {
  
}
```

# --asserts--

A new score must start at `0`.

```javascript
tryCatch(new Score().total === 0);
```

Positive values must be added.

```javascript
const s1 = new Score();
s1.record(5);
s1.record(3);
tryCatch(s1.total === 8);
```

Negative values must be ignored.

```javascript
const s2 = new Score();
s2.record(5);
s2.record(-4);
tryCatch(s2.total === 5);
```

Zero must be ignored.

```javascript
const s3 = new Score();
s3.record(0);
tryCatch(s3.total === 0);
```

The points must be private, so the instance must have no public property.

```javascript
tryCatch(Object.keys(new Score()).length === 0);
```

The helper must be a private method.

```json
{
  "regex": "this\\.#add\\s*\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Score {
  #points = 0;
  #add(value) {
    if (value > 0) {
      this.#points += value;
    }
  }
  record(value) {
    this.#add(value);
  }
  get total() {
    return this.#points;
  }
}
```
