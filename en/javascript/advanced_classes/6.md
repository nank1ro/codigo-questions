---
language: javascript
exerciseType: 4
---

# --description--

Order matters when classes inherit from each other. A `class` declaration is **not** hoisted the way a `function` is: the name only exists from the line where the class is written onwards. So a child class has to appear *after* the parent it extends, otherwise the `extends` clause fails with a `ReferenceError`.

# --instructions--

Sort the items into a working program. Remember that the parent class must be defined before the child

# --answers--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
```

```javascript
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
```

```javascript
console.log(new Car().describe());
```

# --solutions--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
console.log(new Car().describe());
```

# --output--

a vehicle with four wheels
