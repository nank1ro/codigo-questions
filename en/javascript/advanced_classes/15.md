---
language: javascript
exerciseType: 1
---

# --description--

The **`instanceof`** operator asks whether an object was built from a class, or from any class that inherits from it:
```javascript
class Animal {}
class Dog extends Animal {}
const max = new Dog();
console.log(max instanceof Dog);    // true
console.log(max instanceof Animal); // true, Dog extends Animal
```
JavaScript has no `abstract` keyword, but the same idea is written by hand: a base class defines the shape and every method that a child *must* provide simply throws:
```javascript
class Shape {
    area() {
        throw new Error("area() must be implemented");
    }
}
```
A child that forgets to override `area` fails loudly the first time it is used, instead of silently returning `undefined`.

# --instructions--

Write a class `Shape` whose method `area` throws an `Error`, and a class `Square` that extends it, takes a `side` and overrides `area` to return the area of the square

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Shape {
  
}

class Square {
  
}
```

# --asserts--

`new Square(3).area()` must return `9`.

```javascript
tryCatch(new Square(3).area() === 9);
```

`new Square(5).area()` must return `25`.

```javascript
tryCatch(new Square(5).area() === 25);
```

A square must be a `Shape`.

```javascript
tryCatch(new Square(3) instanceof Shape === true);
```

Calling `area` on the base class must throw.

```javascript
let _threw = false;
try {
  new Shape().area();
} catch (e) {
  _threw = true;
}
tryCatch(_threw === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Shape {
  area() {
    throw new Error("area() must be implemented");
  }
}

class Square extends Shape {
  constructor(side) {
    super();
    this.side = side;
  }
  area() {
    return this.side * this.side;
  }
}
```
