---
language: javascript
exerciseType: 2
---

# --description--

You already know that `extends` makes one class a child of another. What that keyword really gives you is **inheritance**: the child gets every property and method of the parent for free, and can add its own on top.

The piece that makes inheritance useful is **`super`**. Inside a child constructor, `super(...)` calls the parent constructor, so the parent can set up the part of the object it owns:
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
```
Here `super(name)` hands `name` to `Animal`, which stores it, and `Dog` only has to worry about `breed`.

# --instructions--

Complete the code so that `Dog` inherits from `Animal` and passes `name` up to the parent constructor

# --seed--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog [/] Animal {
    constructor(name, breed) {
        [/](name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --answers--

- extends
- inherits
- super
- parent
- new Animal

# --solutions--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --output--

Max is a Beagle
