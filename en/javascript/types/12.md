---
language: javascript
exerciseType: 4
---

# --description--

JavaScript has a single `number` type for whole numbers and decimals: every number is a 64-bit floating point value (a *double*). So `5` and `5.0` are the same value, and there is no separate integer type:
```javascript
console.log(5 === 5.0, 10 / 2);
// prints true 5
```
To ask whether a number has no fractional part, use `Number.isInteger`:
```javascript
console.log(Number.isInteger(5.0), Number.isInteger(3.5));
// prints true false
```
Template literals convert the interpolated value to a string with the same rules as `String()`, so `${5.0}` becomes `"5"`, not `"5.0"`.

# --instructions--

Sort the lines to define `half`, which divides `n` by two and returns a sentence saying whether the result is whole, then print the result for `10` and for `7`. Define the function first

# --answers--

```javascript
function half(n) {
```

```javascript
  const result = n / 2;
```

```javascript
  if (Number.isInteger(result)) {
```

```javascript
    return `${result} is whole`;
```

```javascript
  }
```

```javascript
  return `${result} is not whole`;
```

```javascript
}
```

```javascript
console.log(half(10));
```

```javascript
console.log(half(7));
```

# --solutions--

```javascript
function half(n) {
  const result = n / 2;
  if (Number.isInteger(result)) {
    return `${result} is whole`;
  }
  return `${result} is not whole`;
}
console.log(half(10));
console.log(half(7));
```

# --output--

5 is whole
3.5 is not whole
