---
language: javascript
exerciseType: 1
---

# --description--

When JavaScript needs a boolean, for example in an `if` condition or in `Boolean(value)`, it converts the value. Only eight values become `false`; they are called **falsy**:
`false`, `0`, `-0`, `0n`, `""`, `null`, `undefined` and `NaN`.
**Everything else is truthy**, including some values that look empty:
```javascript
console.log(Boolean(0), Boolean(""), Boolean(NaN));
// prints false false false
console.log(Boolean("0"), Boolean("false"), Boolean([]), Boolean({}));
// prints true true true true
```
`"0"` is a non-empty string, so it is truthy; an empty array is an object, so it is truthy too.

# --instructions--

Write a function `countTruthy(values)` that returns how many elements of the array `values` are truthy

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function countTruthy(values) {

}
```

# --asserts--

`countTruthy([1, "a", true])` must return `3`.

```javascript
tryCatch(countTruthy([1, "a", true]) === 3);
```

`countTruthy([0, "", null, undefined, NaN, false])` must return `0`.

```javascript
tryCatch(countTruthy([0, "", null, undefined, NaN, false]) === 0);
```

`countTruthy(["0", "false", [], {}])` must return `4`.

```javascript
tryCatch(countTruthy(["0", "false", [], {}]) === 4);
```

`countTruthy([0n, -0, " "])` must return `1`, because only the space string is truthy.

```javascript
tryCatch(countTruthy([0n, -0, " "]) === 1);
```

`countTruthy([])` must return `0`.

```javascript
tryCatch(countTruthy([]) === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function countTruthy(values) {
  let count = 0;
  for (const value of values) {
    if (value) {
      count++;
    }
  }
  return count;
}
```
