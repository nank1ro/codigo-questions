---
language: javascript
exerciseType: 1
---

# --description--

Because numbers are doubles, some decimals cannot be stored exactly and small errors appear:
```javascript
console.log(0.1 + 0.2);
// prints 0.30000000000000004
console.log(0.1 + 0.2 === 0.3);
// prints false
```
The `toFixed(digits)` method rounds a number to `digits` decimals, but it returns a **string**, which is fine for display and wrong for further math:
```javascript
const price = (0.1 + 0.2).toFixed(2);
console.log(price, typeof price);
// prints 0.30 string
```
To get a rounded **number**, convert the result back with `Number()`:
```javascript
console.log(Number((0.1 + 0.2).toFixed(1)));
// prints 0.3
```

# --instructions--

Write a function `roundTo(value, decimals)` that returns `value` rounded to `decimals` decimal places as a **number**, using `toFixed` and converting the result back

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function roundTo(value, decimals) {

}
```

# --asserts--

`roundTo(3.14159, 2)` must return `3.14`.

```javascript
tryCatch(roundTo(3.14159, 2) === 3.14);
```

`roundTo(0.1 + 0.2, 1)` must return `0.3`.

```javascript
tryCatch(roundTo(0.1 + 0.2, 1) === 0.3);
```

`roundTo(2.5, 0)` must return `3`.

```javascript
tryCatch(roundTo(2.5, 0) === 3);
```

`roundTo(10, 2)` must return `10`.

```javascript
tryCatch(roundTo(10, 2) === 10);
```

The result must be a number, not a string.

```javascript
tryCatch(typeof roundTo(1.005, 1) === "number");
```

The function must use `toFixed`.

```javascript
tryCatch(/toFixed\(/.test(roundTo.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function roundTo(value, decimals) {
  return Number(value.toFixed(decimals));
}
```
