---
language: javascript
exerciseType: 4
---

# --description--

When each member needs several pieces of data or its own methods, a **class** can play the role of the enumeration. Every member is an instance of the class, stored in a `static` property, that is a property that belongs to the class itself instead of to each instance:
```javascript
class Planet {
  static MERCURY = new Planet("Mercury", 0.4);
  static EARTH = new Planet("Earth", 1);

  constructor(name, gravity) {
    this.name = name;
    this.gravity = gravity;
  }
}
console.log(Planet.EARTH.name);
// prints Earth
```
Call `Object.freeze(Planet)` after the class to stop anyone from adding or replacing members, and freeze each instance in the constructor with `Object.freeze(this)` so that the members themselves stay read-only.

# --instructions--

Sort the lines to declare the class-based enumeration `Coin` and print the cents of the `DIME` member.
Declare `PENNY` before `DIME`, both before the constructor, and freeze the class before printing

# --answers--

```javascript
class Coin {
```

```javascript
  static PENNY = new Coin("penny", 1);
```

```javascript
  static DIME = new Coin("dime", 10);
```

```javascript
  constructor(name, cents) { this.name = name; this.cents = cents; }
```

```javascript
}
```

```javascript
Object.freeze(Coin);
```

```javascript
console.log(Coin.DIME.cents);
```

# --solutions--

```javascript
class Coin {
  static PENNY = new Coin("penny", 1);
  static DIME = new Coin("dime", 10);
  constructor(name, cents) { this.name = name; this.cents = cents; }
}
Object.freeze(Coin);
console.log(Coin.DIME.cents);
```

# --output--

10
