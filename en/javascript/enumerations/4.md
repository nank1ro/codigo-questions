---
language: javascript
exerciseType: 2
---

# --description--

Members can also hold **numbers**. Numeric values are handy when the members have a natural order, because you can compare them with the usual operators:
```javascript
const Priority = Object.freeze({
  LOW: 1,
  MEDIUM: 2,
  HIGH: 3,
});
console.log(Priority.HIGH > Priority.LOW);
// prints true
```
The trade-off is readability: printing `Priority.HIGH` shows `3`, which tells you much less than the string `"high"` would.

# --instructions--

Declare the frozen numeric enumeration `Level` with `EASY` equal to `1`, `NORMAL` equal to `2` and `HARD` equal to `3`, then print whether `HARD` is greater than `EASY` and the difference between `HARD` and `EASY`

# --seed--

```javascript
const Level = Object.freeze({
  EASY: [/],
  NORMAL: 2,
  HARD: [/],
});
console.log(Level.HARD [/] Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --answers--

- 1
- 3
- >
- 0
- <
- ===

# --solutions--

```javascript
const Level = Object.freeze({
  EASY: 1,
  NORMAL: 2,
  HARD: 3,
});
console.log(Level.HARD > Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --output--

true
2
