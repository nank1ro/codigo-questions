---
language: javascript
exerciseType: 2
---

# --description--

Enumeration values are often used as **keys** of another object, for example to map every member to a label or a price. Inside an object literal, wrapping a key in square brackets `[ ]` evaluates the expression and uses its result as the key (a **computed key**). This works both with string and with symbol members:
```javascript
const Status = Object.freeze({ ACTIVE: "active", DONE: "done" });
const labels = {
  [Status.ACTIVE]: "In progress",
  [Status.DONE]: "Completed",
};
console.log(labels[Status.DONE]);
// prints Completed
```
Without the brackets, `Status.DONE: "Completed"` would be a syntax error, and `"Status.DONE"` would be a plain string key.

# --instructions--

Build the object `prices` that maps `Size.SMALL` to `3` and `Size.LARGE` to `5` using the enumeration members as keys, then print the price of a large size

# --seed--

```javascript
const Size = Object.freeze({ SMALL: "small", LARGE: "large" });
const prices = {
  [/]: 3,
  [/]: 5,
};
console.log(prices[/]);
```

# --answers--

- [Size.SMALL]
- [Size.LARGE]
- [Size.LARGE]
- Size.SMALL
- Size.LARGE
- .LARGE
- (Size.LARGE)

# --solutions--

```javascript
const Size = Object.freeze({ SMALL: "small", LARGE: "large" });
const prices = {
  [Size.SMALL]: 3,
  [Size.LARGE]: 5,
};
console.log(prices[Size.LARGE]);
```

# --output--

5
