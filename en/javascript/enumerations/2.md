---
language: javascript
exerciseType: 1
---

# --description--

The value stored in each member is up to you. **Strings** are the most common choice because they are readable when printed, logged or saved to a file:
```javascript
const Status = Object.freeze({
  ACTIVE: "active",
  DONE: "done",
});
console.log(Status.DONE);
// prints done
```
Once frozen, the object cannot receive new properties either, and `Object.isFrozen(obj)` tells you whether an object has been frozen:
```javascript
console.log(Object.isFrozen(Status));
// prints true
```

# --instructions--

Declare the frozen enumeration `Direction` with the members `UP`, `DOWN`, `LEFT` and `RIGHT`, each holding its own name in lowercase (`"up"`, `"down"`, `"left"`, `"right"`)

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Direction = 
```

# --asserts--

`Direction.UP` must be `"up"`.

```javascript
tryCatch(Direction.UP === "up");
```

`Direction.DOWN` must be `"down"`.

```javascript
tryCatch(Direction.DOWN === "down");
```

`Direction.LEFT` must be `"left"` and `Direction.RIGHT` must be `"right"`.

```javascript
tryCatch(Direction.LEFT === "left" && Direction.RIGHT === "right");
```

`Direction` must have exactly four members.

```javascript
tryCatch(Object.keys(Direction).length === 4);
```

`Direction` must be frozen.

```javascript
tryCatch(Object.isFrozen(Direction) === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Direction = Object.freeze({
  UP: "up",
  DOWN: "down",
  LEFT: "left",
  RIGHT: "right",
});
```
