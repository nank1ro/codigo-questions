---
language: javascript
exerciseType: 2
---

# --description--

When one side is a string and the other a number, `<=` converts the string to a number first, so `"7" <= 8` is `true`.

# --instructions--

Check if `num` is **less than or equal to** to `8`, printing the boolean result

# --seed--

```javascript
var num = 7;
console.log([/][/][/]);
```

# --answers--

- num
- 'num'
-  <= 
-  = 
-  >= 
- 8

# --solutions--

```javascript
var num = 7;
console.log(num <= 8);
```

# --output--

true
