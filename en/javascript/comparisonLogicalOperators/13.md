---
language: javascript
exerciseType: 2
---

# --description--

`>=` is satisfied by either half of its name: `8 >= 8` is `true` because the two values are equal, while the stricter `8 > 8` is `false`.

# --instructions--

Check if `num` is **greater than or equal to** to `8`, printing the boolean result

# --seed--

```javascript
var num = 8;
console.log([/][/][/]);
```

# --answers--

- num 
- 'num'
- == 
- = 
- >= 
- 8

# --solutions--

```javascript
var num = 8;
console.log(num >= 8);
```

# --output--

true
