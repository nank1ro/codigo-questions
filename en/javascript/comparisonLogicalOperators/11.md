---
language: javascript
exerciseType: 2
---

# --description--

`!=` converts before comparing, just like `==`, so `"5" != 5` is `false`. Its strict counterpart `!==` treats a string and a number as different whatever they contain.
```javascript
console.log("5" !== 5); // true
```

# --instructions--

Check if `word` is **NOT** equal to `dog`, printing the boolean result

# --seed--

```javascript
var word = "cat";
console.log([/][/][/]);
```

# --answers--

- word 
-  "dog"
- == 
- = 
- dog
- !=

# --solutions--

```javascript
var word = "cat";
console.log(word != "dog");
```

# --output--

true
