---
language: javascript
exerciseType: 2
---

# --description--

`==` compares its two sides after converting them to a common type, so `"5" == 5` is `true`. The strict operator `===` skips that conversion and requires the types to match as well.
```javascript
console.log("5" == 5);  // true
console.log("5" === 5); // false
```

# --instructions--

Check if `num` is equal to `5`, printing the boolean result

# --seed--

```javascript
var num = 5;
console.log([/][/][/]);
```

# --answers--

- num 
- num 
- == 
- = 
- 5

# --solutions--

```javascript
var num = 5;
console.log(num == 5);
```

# --output--

true
