---
language: javascript
exerciseType: 2
---

# --description--

A **Map** stores **key-value pairs**: every value is saved under a key, and you use that key to find the value again.
You create an empty map with `new Map()`, add a pair with `set(key, value)` and read a value with `get(key)`:
```javascript
let ages = new Map();
ages.set("Ann", 30);
ages.set("Bob", 25);
console.log(ages.get("Ann"));
// prints 30
```
Calling `set()` with a key that already exists replaces its value.

# --instructions--

Create an empty map `ages`, store `30` under the key `"Ann"`, then print the value stored for `"Ann"`

# --seed--

```javascript
let ages = new [/]();
ages.[/]("Ann", 30);
console.log(ages.[/]("Ann"));
```

# --answers--

- Map
- set
- get
- Object
- add
- put
- read

# --solutions--

```javascript
let ages = new Map();
ages.set("Ann", 30);
console.log(ages.get("Ann"));
```

# --output--

30
