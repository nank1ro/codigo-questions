---
language: javascript
exerciseType: 3
---

# --description--

So when should you reach for a `Map` instead of a plain object?
- Use a **Map** when keys are added and removed at runtime, when they are not strings, or when you need `size` and reliable ordering
- Use an **object** for a fixed record with known field names, like `{ name, email }`, and whenever you need to convert the data to JSON, since `JSON.stringify()` ignores the content of a map

# --instructions--

Which situation is a better fit for a `Map` than for a plain object?

# --answers--

- Tracking how many times each button of a page was clicked, using the button object itself as the key
- Describing a user with the fields name and email
- Sending settings to a server as JSON
- Storing the width and height of a rectangle

# --solutions--

- Tracking how many times each button of a page was clicked, using the button object itself as the key
