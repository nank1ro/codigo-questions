---
language: javascript
exerciseType: 2
---

# --description--

Wrapping the object literal in parentheses is the standard way to build objects with a one-line arrow function, for example when you turn a couple of values into a record:
```javascript
const user = (name, age) => ({ name: name, age: age });
console.log(user("Ana", 30).age);
// prints 30
```
An arrow function with no parameters starts with an empty pair of parentheses `()`:
```javascript
const empty = () => ({});
console.log(empty());
// prints {}
```

# --instructions--

Complete `point` so that it returns an object with the properties `x` and `y`, and `origin` so that it takes no parameters, then print `point(3, 4).y` and `origin().x`

# --seed--

```javascript
const point = (x, y) => [/]{ x: x, y: y }[/];
const origin = [/] => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --answers--

- (
- )
- ()
- [
- ]
- function
- this

# --solutions--

```javascript
const point = (x, y) => ({ x: x, y: y });
const origin = () => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --output--

4
0
