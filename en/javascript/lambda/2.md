---
language: javascript
exerciseType: 1
---

# --description--

Arrow functions get shorter in two common cases.
When the body is a **single expression**, you can drop the curly braces and the `return` keyword: the value of the expression is returned automatically (an **implicit return**):
```javascript
const add = (a, b) => a + b;
console.log(add(2, 3));
// prints 5
```
When there is **exactly one parameter**, you can also drop the parentheses around it:
```javascript
const double = n => n * 2;
console.log(double(4));
// prints 8
```
With zero parameters or with two or more, the parentheses are required: `() => 42` and `(a, b) => a + b`.

# --instructions--

Store in the constant `square` an arrow function that returns its argument multiplied by itself, using an implicit return (no curly braces and no `return`)

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const square = 
```

# --asserts--

`square(3)` must return `9`.

```javascript
tryCatch(square(3) === 9);
```

`square(-4)` must return `16`.

```javascript
tryCatch(square(-4) === 16);
```

`square(0)` must return `0`.

```javascript
tryCatch(square(0) === 0);
```

`square` must be an arrow function, not a `function`.

```javascript
tryCatch(typeof square === "function" && !/function/.test(square.toString()));
```

`square` must use an implicit return.

```javascript
tryCatch(!/return/.test(square.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const square = n => n * n;
```
