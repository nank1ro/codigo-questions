---
language: javascript
exerciseType: 2
---

# --description--

`sort(compare)` orders an array in place using a callback that receives two elements and returns a negative number when the first should come first, a positive number when the second should come first, or `0` when they are equal. For numbers, `(a, b) => a - b` sorts in ascending order and `(a, b) => b - a` in descending order.
`find(callback)` returns the first element for which the callback returns `true`, or `undefined` if there is none:
```javascript
const scores = [50, 90, 70];
scores.sort((a, b) => a - b);
console.log(scores);
// prints [ 50, 70, 90 ]
console.log(scores.find((s) => s > 60));
// prints 70
```

# --instructions--

Sort `ages` in ascending order, then find the first age strictly greater than `21` and print both the sorted array and that age

# --seed--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a [/] b);
const adult = ages.find((age) => age [/] 21);
console.log(ages);
console.log(adult);
```

# --answers--

- -
- >
- +
- <
- ===
- *

# --solutions--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a - b);
const adult = ages.find((age) => age > 21);
console.log(ages);
console.log(adult);
```

# --output--

[ 18, 25, 31 ]
25
