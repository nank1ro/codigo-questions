---
language: javascript
exerciseType: 1
---

# --description--

Because a function is a value, an arrow function can **return another arrow function**. Chaining two arrows is a compact way to write a function that builds functions:
```javascript
const makeAdder = (amount) => (n) => n + amount;
const addTen = makeAdder(10);
console.log(addTen(5));
// prints 15
console.log(makeAdder(1)(5));
// prints 6
```
Read it from left to right: `makeAdder` takes `amount` and returns `(n) => n + amount`, an arrow function that captures `amount` through a closure. `makeAdder(1)(5)` calls the returned function immediately.

# --instructions--

Store in the constant `makeMultiplier` an arrow function that takes a `factor` and returns an arrow function which multiplies its argument by `factor`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const makeMultiplier = 
```

# --asserts--

`makeMultiplier(3)` must return a function.

```javascript
tryCatch(typeof makeMultiplier(3) === "function");
```

`makeMultiplier(3)(4)` must return `12`.

```javascript
tryCatch(makeMultiplier(3)(4) === 12);
```

The function returned by `makeMultiplier(2)` must return `20` when called with `10`.

```javascript
tryCatch((() => { const twice = makeMultiplier(2); return twice(10) === 20; })());
```

Two multipliers must be independent: `makeMultiplier(5)(2)` must be `10` and `makeMultiplier(0)(2)` must be `0`.

```javascript
tryCatch(makeMultiplier(5)(2) === 10 && makeMultiplier(0)(2) === 0);
```

Both `makeMultiplier` and the function it returns must be arrow functions.

```javascript
tryCatch(!/function/.test(makeMultiplier.toString()) && /=>/.test(makeMultiplier(3).toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const makeMultiplier = (factor) => (n) => n * factor;
```
