---
language: javascript
exerciseType: 1
---

# --description--

A **rest parameter** `...name` collects any number of arguments into an array, and it works in arrow functions too:
```javascript
const count = (...items) => items.length;
console.log(count("a", "b", "c"));
// prints 3
```
Regular functions also have a hidden array-like `arguments` object holding every argument they received. Arrow functions **do not**: inside an arrow, `arguments` refers to the surrounding function's `arguments` or does not exist at all. Whenever you need "all the arguments" in an arrow function, use a rest parameter.

# --instructions--

Store in the constant `sum` an arrow function that accepts any number of numbers through a rest parameter and returns their total, or `0` when called without arguments

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const sum = 
```

# --asserts--

`sum(1, 2, 3)` must return `6`.

```javascript
tryCatch(sum(1, 2, 3) === 6);
```

`sum(5)` must return `5`.

```javascript
tryCatch(sum(5) === 5);
```

`sum()` must return `0`.

```javascript
tryCatch(sum() === 0);
```

`sum(10, -4, 2.5)` must return `8.5`.

```javascript
tryCatch(sum(10, -4, 2.5) === 8.5);
```

`sum` must be an arrow function that uses a rest parameter.

```javascript
tryCatch(!/function/.test(sum.toString()) && /\(\s*\.\.\.\w+\s*\)/.test(sum.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const sum = (...numbers) => numbers.reduce((total, n) => total + n, 0);
```
