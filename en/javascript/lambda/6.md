---
language: javascript
exerciseType: 4
---

# --description--

Two more array methods take a callback.
`forEach(callback)` calls the callback once per element and returns nothing; use it for side effects such as printing.
`reduce(callback, initialValue)` folds the array into a single value: the callback receives the accumulated value so far and the current element, and returns the new accumulated value:
```javascript
const numbers = [1, 2, 3];
numbers.forEach((n) => console.log(n));
// prints 1, 2 and 3 on three lines
const total = numbers.reduce((sum, n) => sum + n, 0);
console.log(total);
// prints 6
```

# --instructions--

Sort the lines to compute the total of `prices` with `reduce` right after declaring the array, print every price on its own line with `forEach`, then print the total

# --answers--

```javascript
const prices = [4, 6, 10];
```

```javascript
const total = prices.reduce((sum, p) => sum + p, 0);
```

```javascript
prices.forEach((p) => console.log(p));
```

```javascript
console.log(total);
```

# --solutions--

```javascript
const prices = [4, 6, 10];
const total = prices.reduce((sum, p) => sum + p, 0);
prices.forEach((p) => console.log(p));
console.log(total);
```

# --output--

4
6
10
20
