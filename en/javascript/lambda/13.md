---
language: javascript
exerciseType: 2
---

# --description--

You do not have to store an arrow function to call it. Wrap it in parentheses and add the argument list right after: this is an **immediately invoked** arrow function:
```javascript
const doubled = ((n) => n * 2)(5);
console.log(doubled);
// prints 10
```
The parentheses around the arrow are required: without them `(n) => n * 2(5)` would try to call the number `2`. Immediately invoked functions are handy to compute a value with a few temporary variables that should not leak into the rest of the program.

# --instructions--

Complete the code so that `message` receives the result of an immediately invoked arrow function returning `"ready"`, then print `message` and the result of immediately calling `(n) => n * 3` with `4`

# --seed--

```javascript
const message = [/]() => "ready"[/]();
console.log(message);
console.log([/](n) => n * 3)(4));
```

# --answers--

- (
- )
- (
- [
- ]
- {
- }

# --solutions--

```javascript
const message = (() => "ready")();
console.log(message);
console.log(((n) => n * 3)(4));
```

# --output--

ready
12
