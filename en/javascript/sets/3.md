---
language: javascript
exerciseType: 3
---

# --description--

`add()` returns the set itself, so several calls can be chained:
```javascript
let letters = new Set();
letters.add("a").add("b");
```
Whether you chain or not, a value that is already present is never added a second time, so `size` counts each distinct value only once.

# --instructions--

What does this code print?
```javascript
let letters = new Set();
letters.add("a").add("b").add("a");
console.log(letters.size);
```

# --answers--

- 2
- 3
- 1
- An error, because "a" is already in the set

# --solutions--

- 2
