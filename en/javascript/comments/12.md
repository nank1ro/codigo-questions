---
language: javascript
exerciseType: 4
---

# --description--

The order inside a JSDoc comment is always the same: the summary first, then one `@param` per parameter in the order they are declared, then `@returns` last. The opening `/**` and the closing ` */` wrap everything, and the comment sits directly above the function it describes:
```javascript
/**
 * Returns the number of seconds in the given minutes.
 * @param {number} minutes a whole number of minutes
 * @returns {number} minutes multiplied by sixty
 */
function toSeconds(minutes) {
  return minutes * 60;
}
```

# --instructions--

Sort the items to document `twice` with a summary, a `@param` line and a `@returns` line, in this order and directly above the function, then print `twice(21)` after defining the function

# --answers--

```javascript
/**
```

```javascript
 * Returns the double of a number.
```

```javascript
 * @param {number} n the number to double
```

```javascript
 * @returns {number} n multiplied by two
```

```javascript
 */
```

```javascript
function twice(n) {
  return n * 2;
}
```

```javascript
console.log(twice(21));
```

# --solutions--

```javascript
/**
 * Returns the double of a number.
 * @param {number} n the number to double
 * @returns {number} n multiplied by two
 */
function twice(n) {
  return n * 2;
}
console.log(twice(21));
```

# --output--

42
