<section id="options">
{
"language": "javascript",
"type": "answerButtons"
}
</section>
<section id="instructions">
What's wrong with this code?
```
var pieces = 5;
var label = `we have ${pieces} pieces`;
```
</section>
<section id="answerButtons">
{
	"buttons": ["We can't save template literals in variables","The characters ${} are missing","We can't use variables inside of ${}","Nothing is wrong with the code"],
	"correct": "Nothing is wrong with the code"
}
</section>
