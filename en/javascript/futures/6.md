---
language: javascript
exerciseType: 4
---

# --description--

An `async` function reads from top to bottom like any other function: `await` simply pauses it until the awaited promise is fulfilled, then execution continues on the next line.
```javascript
async function main() {
  console.log("start");
  const value = await Promise.resolve("data");
  console.log(value);
  console.log("done");
}

main();
// prints start, then data, then done
```
Notice the last line: an `async` function still has to be **called**. Writing `main` without the parentheses defines the work but never starts it, and nothing is printed.

# --instructions--

Sort the items into a program that defines an `async` function `load` returning `data`, then an `async` function `main` that prints `start`, awaits `load()` into `value`, prints `value` and finally prints `done`. Define `load` first, then `main`, and call `main()` on the last line

# --answers--

```javascript
async function load() {
  return "data";
}
```

```javascript
async function main() {
```

```javascript
  console.log("start");
```

```javascript
  const value = await load();
```

```javascript
  console.log(value);
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
async function load() {
  return "data";
}
async function main() {
  console.log("start");
  const value = await load();
  console.log(value);
  console.log("done");
}
main();
```

# --output--

start
data
done
