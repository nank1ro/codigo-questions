---
language: javascript
exerciseType: 1
---

# --description--

Asynchronous work can also fail. The function given to `new Promise` receives a second callback, **`reject`**: call `reject(error)` and the promise becomes rejected instead of fulfilled.
```javascript
function readAge(age) {
  return new Promise((resolve, reject) => {
    if (age >= 0) {
      resolve(age);
    } else {
      reject(new Error("negative age"));
    }
  });
}
```
Always reject with an `Error` object: it carries a `message` and a stack trace, which a bare string does not.

A rejection is read with **`.catch(callback)`**, the mirror image of `.then`. **`Promise.reject(error)`** builds a promise that is already rejected, just like `Promise.resolve` builds a fulfilled one:
```javascript
readAge(-1).catch((error) => {
  console.log(error.message);
  // prints negative age
});
```
Calling `resolve` and `reject` both, or twice, changes nothing: only the first call counts.

# --instructions--

Write a function `checkAge` that takes a number `age` and returns a promise fulfilled with `age` when `age` is at least `18`, and rejected with `new Error("too young")` otherwise. Use `new Promise`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function checkAge(age) {
  
}
```

# --asserts--

`await checkAge(20)` must be `20`.

```javascript
(async () => {
  tryCatch((await checkAge(20)) === 20);
})().catch(() => tryCatch(false));
```

`await checkAge(18)` must be `18`, because `18` is old enough.

```javascript
(async () => {
  tryCatch((await checkAge(18)) === 18);
})().catch(() => tryCatch(false));
```

`checkAge(10)` must be rejected with the message `"too young"`.

```javascript
(async () => {
  let message = "";
  await checkAge(10).catch((error) => { message = error.message; });
  tryCatch(message === "too young");
})().catch(() => tryCatch(false));
```

`checkAge(0)` must be rejected with an `Error` object, not with a string.

```javascript
(async () => {
  let reason = null;
  await checkAge(0).catch((error) => { reason = error; });
  tryCatch(reason instanceof Error);
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function checkAge(age) {
  return new Promise((resolve, reject) => {
    if (age >= 18) {
      resolve(age);
    } else {
      reject(new Error("too young"));
    }
  });
}
```
