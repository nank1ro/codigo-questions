---
language: javascript
exerciseType: 4
---

# --description--

Real programs combine these methods into a **pipeline**: filter the elements you care about, map them to the values you need, and reduce them to a result. Storing the intermediate arrays in constants keeps each step readable and lets you reuse them:
```javascript
const adults = people.filter((p) => p.age >= 18);
const names = adults.map((p) => p.name);
const totalAge = adults.reduce((sum, p) => sum + p.age, 0);
```
`names.join(", ")` turns an array of strings into a single string with the elements separated by a comma and a space.

# --instructions--

Sort the lines to keep only the cart items with a positive `qty`, collect their names, add up `price * qty` for them and print the names joined by `", "` and the total on one line. List the items in the order pen, book, bag and declare `inStock`, `names` and `total` in this order

# --answers--

```javascript
const cart = [
```

```javascript
  { name: "pen", price: 2, qty: 3 },
```

```javascript
  { name: "book", price: 12, qty: 1 },
```

```javascript
  { name: "bag", price: 30, qty: 0 },
```

```javascript
];
```

```javascript
const inStock = cart.filter((item) => item.qty > 0);
```

```javascript
const names = inStock.map((item) => item.name);
```

```javascript
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
```

```javascript
console.log(names.join(", "), total);
```

# --solutions--

```javascript
const cart = [
  { name: "pen", price: 2, qty: 3 },
  { name: "book", price: 12, qty: 1 },
  { name: "bag", price: 30, qty: 0 },
];
const inStock = cart.filter((item) => item.qty > 0);
const names = inStock.map((item) => item.name);
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
console.log(names.join(", "), total);
```

# --output--

pen, book 18
