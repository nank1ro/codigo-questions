---
language: javascript
exerciseType: 1
---

# --description--

Three methods answer questions about an array with a predicate:
- `find(predicate)` returns the **first** element for which the predicate is `true`, or `undefined` if there is none
- `some(predicate)` returns `true` if **at least one** element satisfies the predicate
- `every(predicate)` returns `true` if **all** elements do (and `true` for an empty array)
```javascript
const scores = [72, 45, 90];
console.log(scores.find((s) => s < 60));
// prints 45
console.log(scores.some((s) => s === 90), scores.every((s) => s >= 60));
// prints true false
```
All three stop as soon as the answer is known, so they never look at more elements than needed.

# --instructions--

Write a function `report(scores)` that returns `"all passed"` when every score is at least `60`, and otherwise `"first failing: X"` where `X` is the first score below `60`, using `every` and `find`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function report(scores) {

}
```

# --asserts--

`report([70, 80])` must return `"all passed"`.

```javascript
tryCatch(report([70, 80]) === "all passed");
```

`report([70, 40, 30])` must return `"first failing: 40"`.

```javascript
tryCatch(report([70, 40, 30]) === "first failing: 40");
```

`report([59])` must return `"first failing: 59"`.

```javascript
tryCatch(report([59]) === "first failing: 59");
```

`report([])` must return `"all passed"`.

```javascript
tryCatch(report([]) === "all passed");
```

The function must use `every` and `find`.

```javascript
tryCatch(/\.every\(/.test(report.toString()) && /\.find\(/.test(report.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function report(scores) {
  if (scores.every((s) => s >= 60)) {
    return "all passed";
  }
  return `first failing: ${scores.find((s) => s < 60)}`;
}
```
