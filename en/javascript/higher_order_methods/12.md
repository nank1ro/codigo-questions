---
language: javascript
exerciseType: 2
---

# --description--

The accumulator of `reduce` does not have to be a number: it can be a string, an array or an object. Starting from an empty object `{}` you can count or group things in one pass. Remember to **return the accumulator** from the callback, otherwise the next step receives `undefined`:
```javascript
const votes = ["yes", "no", "yes"];
const tally = votes.reduce((acc, vote) => {
  acc[vote] = (acc[vote] ?? 0) + 1;
  return acc;
}, {});
console.log(tally);
// prints { yes: 2, no: 1 }
```
`acc[vote] ?? 0` reads the current count, or `0` when that key does not exist yet.

# --instructions--

Complete the code so that `counts` maps every first letter to how many words start with it, using `reduce` with an empty object as the starting accumulator and `?? 0` for missing keys, producing the output `{ a: 2, b: 2, c: 1 }`

# --seed--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.[/]((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] [/] 0) + 1;
  return [/];
}, [/]);
console.log(counts);
```

# --answers--

- reduce
- ??
- acc
- {}
- map
- filter
- &&
- word
- 0

# --solutions--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.reduce((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] ?? 0) + 1;
  return acc;
}, {});
console.log(counts);
```

# --output--

{ a: 2, b: 2, c: 1 }
