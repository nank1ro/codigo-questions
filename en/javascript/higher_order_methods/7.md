---
language: javascript
exerciseType: 4
---

# --description--

The comparator can look at any part of the elements, so an array of objects is sorted by one of their properties just by comparing that property:
```javascript
const items = [{ name: "b", size: 3 }, { name: "a", size: 1 }];
const bySize = [...items].sort((x, y) => x.size - y.size);
console.log(bySize.map((item) => item.name));
// prints [ 'a', 'b' ]
```
Sorting the copy leaves `items` in its original order.

# --instructions--

Sort the lines to sort a copy of `players` by ascending `score`, print their names in that order with `map`, then print the name of the first element of `players` to show that it was not changed. Declare `ranked` before `names`

# --answers--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
```

```javascript
const ranked = [...players].sort((a, b) => a.score - b.score);
```

```javascript
const names = ranked.map((p) => p.name);
```

```javascript
console.log(names);
```

```javascript
console.log(players[0].name);
```

# --solutions--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
const ranked = [...players].sort((a, b) => a.score - b.score);
const names = ranked.map((p) => p.name);
console.log(names);
console.log(players[0].name);
```

# --output--

[ 'Leo', 'Zoe', 'Mia' ]
Zoe
