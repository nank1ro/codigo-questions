---
language: javascript
exerciseType: 4
---

# --description--

A pattern can replace a parameter name in a function declaration, so the unpacking happens as the call is made:
```javascript
function area({ width, height }) {
    return width * height;
}
console.log(area({ width: 4, height: 3 }));
// prints 12
```
Inside the body there is no object variable at all, only `width` and `height`. The caller passes one object, but the signature documents exactly which keys the function reads, and the keys can arrive in any order.

# --instructions--

Sort the items into a program that defines the function first and then prints the area of the rectangle

# --answers--

```javascript
function area({ width, height }) {
```

```javascript
  return width * height;
}
```

```javascript
const rect = { width: 4, height: 3 };
```

```javascript
console.log(area(rect));
```

# --solutions--

```javascript
function area({ width, height }) {
  return width * height;
}
const rect = { width: 4, height: 3 };
console.log(area(rect));
```

# --output--

12
