---
language: javascript
exerciseType: 2
---

# --description--

Objects can be destructured too, with curly braces instead of square ones. Here position means nothing: each name is matched against the **key** that spells the same:
```javascript
const user = { name: "Ada", age: 36 };
const { age, name } = user;
console.log(name, age);
// prints Ada 36
```
Swapping `age` and `name` in the pattern changes nothing, and keys that the pattern does not mention are simply left behind. A name with no matching key becomes `undefined`.

# --instructions--

Complete the two patterns so that the program prints `Dune (1965) by Herbert`

# --seed--

```javascript
const book = { title: "Dune", author: "Herbert", year: 1965 };
const { title, [/] } = book;
const { author } = [/];
console.log(`${title} (${year}) by ${author}`);
```

# --answers--

- year
- book
- title
- published
- novel
- 1965

# --solutions--

```javascript
const book = { title: "Dune", author: "Herbert", year: 1965 };
const { title, year } = book;
const { author } = book;
console.log(`${title} (${year}) by ${author}`);
```

# --output--

Dune (1965) by Herbert
