<section id="options">
{
"language": "javascript",
"type": "fillEmptySpaces"
}
</section>
<section id="description">
An array index behaves like any other variable name.
It can be used to access as well as assign values.
You saw how to access an array index like this:
```
var names = ["Jeremiah", "Barney", "Ivan", "Noel"];
// Prints the value "Jeremiah"
console.log(names[0]);
```
This is how an assignment works:
```
var names = ["Jeremiah", "Barney", "Ivan", "Noel"];
// Assign the new value "Jordan"
names[0] = "Jordan";
// Prints the value "Jordan"
console.log(names[0]);
```
</section>
<section id="instructions">
Complete the code in order to replace the item that currently holds the value `"Mia"` with the name `"Sophie"`
</section>
<section id="fillEmptySpaces">
{
    "seed": ["var names = [\"Charlotte\", \"Mia\", \"Jessica\", \"Hollie\"];\n","[/]","[/]"," = \"Sophie\";\nfor (name of names) {\n\tconsole.log(name);\n}"],
	"buttons": ["[0]", "[1]", "[2]", "[3]", "[4]", "names"],
	"correct": "var names = [\"Charlotte\", \"Mia\", \"Jessica\", \"Hollie\"];\nnames[1] = \"Sophie\";\nfor (name of names) {\n\tconsole.log(name);\n}"
}
</section>
<section id="preOutput">
Charlotte
Sophie
Jessica
Hollie
</section>