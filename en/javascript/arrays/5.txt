<section id="options">
{
"language": "javascript",
"type": "fillEmptySpaces"
}
</section>
<section id="description">
An array doesn't have to have a fixed length.
You can add items to the end of an array any time you like!
To add an item to an array we use the `push` function:
```
var letters = ["a", "b"];
letters.push("c");
console.log(letters);
// Prints ["a", "b", "c"]
```
</section>
<section id="instructions">
Complete the code to push three more items to the `numbers` array, in the following order `6`, then `8` and finally `9`
</section>
<section id="fillEmptySpaces">
{
    "seed": ["var numbers = [1, 3, 9];\nnumbers.","[/]","(","[/]",");\nnumbers.push(","[/]",");\n", "[/]" ,".push(9);\nconsole.log(numbers);"],
	"buttons": ["push", "add", "6", "8", "numbers"],
	"correct": "var numbers = [1, 3, 9];\nnumbers.push(6);\nnumbers.push(8);\nnumbers.push(9);\nconsole.log(numbers);"
}
</section>
<section id="preOutput">
[1, 3, 9, 6, 8, 9]
</section>