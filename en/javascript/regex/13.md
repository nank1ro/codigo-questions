---
language: javascript
exerciseType: 1
---

# --description--

To rewrite **every** match instead of the first one you have two options:
```javascript
console.log("a1 b2".replace(/\d/g, "#"));
// prints a# b#
console.log("a1 b2".replaceAll(/\d/g, "#"));
// prints a# b#
```
**`replaceAll`** is the clearer of the two, and it also accepts a plain string as the pattern. When you give it a regex, that regex **must** carry the `g` flag, otherwise it throws a `TypeError`; this is exactly what stops the silent bug of writing `replace` and only fixing the first match.

# --instructions--

Write a function `redact` that takes a string `text` and returns it with every run of digits replaced by a single `#`, using `replaceAll`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function redact(text) {
  
}
```

# --asserts--

The `replaceAll` method must be used.

```json
{
  "regex": "\\.replaceAll\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`redact("call 123 or 45")` must return `call # or #`.

```javascript
tryCatch(redact("call 123 or 45") === "call # or #");
```

`redact("no digits")` must return the text unchanged.

```javascript
tryCatch(redact("no digits") === "no digits");
```

`redact("2026")` must return `#`.

```javascript
tryCatch(redact("2026") === "#");
```

`redact("a1b2c3")` must return `a#b#c#`.

```javascript
tryCatch(redact("a1b2c3") === "a#b#c#");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function redact(text) {
  return text.replaceAll(/\d+/g, "#");
}
```
