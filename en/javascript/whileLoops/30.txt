<section id="options">
{
"language": "javascript",
"type": "answerButtons"
}
</section>
<section id="instructions">
How do we know this loop doesn't stop?
```
var isLoading = true;

while (isLoading) {
    console.log("Loading...");
    isLoading = true;
}
```
</section>
<section id="answerButtons">
{
	"buttons": ["Because the variable doesn't update to false inside the code block","Because all while loops don't stop", "Because the variable updates to false"],
	"correct": "Because the variable doesn't update to false inside the code block"
}
</section>
