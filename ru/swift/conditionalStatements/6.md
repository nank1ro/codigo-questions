---
language: swift
exerciseType: 2
---

# --description--

Мы также можем вложить условный оператор (`if`, `else if` или `else`) внутрь другого условного оператора, чтобы создать более сложную структуру.
```swift
var num = 4
if num < 3 {
    print("the number is lower than 3")
} else {
    if num == 3 {
        print("the number is 3")
    } else if num == 4 {
        print("the number is 4")
    } else {
        print("the number is greather than 4")
    }
}
```
и результат выполнения этого кода -- `the number is 4`.

# --instructions--

Давайте изменим программу определения времени суток, вложив условный оператор внутрь оператора `else`.
Вложенный условный оператор должен проверять, *меньше* ли `time` чем `18`, и в блоке кода должен выводить `"It's afternoon"`.
Если предыдущее условие равно `false`, выведите `"It's evening"`

# --seed--

```swift
var time = 19
if time > 0 && time < 12 {
    print("It's morning")
} else if time == 12 {
    print("It's midday")
} else if time == 0 {
    print("It's midnight")
} else {
    [/][/][/][/] {
        print("It's afternoon")
    } [/] {
        print("It's evening")
    }
}
```

# --answers--

- 6
- elif 
-  < 
-  == 
- :
- ;
- time
- else if:
- elseif:
- if 
- else

# --solutions--

```swift
var time = 19
if time > 0 && time < 12 {
    print("It's morning")
} else if time == 12 {
    print("It's midday")
} else if time == 0 {
    print("It's midnight")
} else {
    if time < 6 {
        print("It's afternoon")
    } else {
        print("It's evening")
    }
}
```

# --output--

It's evening
