---
language: swift
exerciseType: 2
---

# --description--

Допустим, у нас есть ещё одно условие для проверки, как в этом примере:
```swift
var num = 3
if num == 2 {
    print("the number is 2")
} else if num == 3 {
    print("the number is 3")
} else {
    print("do something else")
}
```
и результат выполнения этого кода -- `the number is 3`.
Сначала проверяем, равно ли число 2 -- это ложь.
Поэтому переходим ко второму оператору и проверяем, равна ли `num` 3 -- это истина, и мы выполняем следующий блок кода, выводя `the number is 3`

# --instructions--

Создайте оператор `else if`, проверяющий, равна ли `time` значению `12`, и в блоке кода выведите строку `"It's midday"`

# --seed--

```swift
var time = 12
if time < 12 {
    print("It's morning")
} [/][/][/][/][/]
    print("[/]")
} else {
    print("It's afternoon")
}
```

# --answers--

- It's midday
- 12
- else if 
-  < 
-  == 
- :
- ;
- time
-  {

# --solutions--

```swift
var time = 12
if time < 12 {
    print("It's morning")
} else if time == 12 {
    print("It's midday")
} else {
    print("It's afternoon")
}
```

# --output--

It's midday
