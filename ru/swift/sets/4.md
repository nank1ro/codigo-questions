---
language: swift
exerciseType: 2
---

# --description--

Чтобы проверить, есть ли значение в множестве, используйте метод `contains(_:)`, который возвращает `Bool`:
```swift
let primes: Set<Int> = [2, 3, 5, 7]
print(primes.contains(5)) // true
print(primes.contains(6)) // false
```
Эта проверка выполняется очень быстро для множества, даже с тысячами элементов, что является одной из главных причин предпочитать множество массиву при проверке принадлежности.

# --instructions--

Выведите `Welcome`, если `role` является одной из разрешённых ролей, иначе выведите `Access denied`

# --seed--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed[/]([/]) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --answers--

- .contains
- .has
- .includes
- role
- "role"
- allowed

# --solutions--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed.contains(role) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --output--

Welcome
