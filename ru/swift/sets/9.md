---
language: swift
exerciseType: 1
---

# --description--

Ещё две операции дополняют это семейство:
- `a.subtracting(b)` содержит элементы `a`, которых **нет** в `b`
- `a.symmetricDifference(b)` содержит элементы, которые есть в `a` или в `b`, но **не в обоих**
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.subtracting(b).sorted())          // [1, 2]
print(a.symmetricDifference(b).sorted())  // [1, 2, 4]
```
В отличие от `union` и `intersection`, `subtracting` не симметрична: `a.subtracting(b)` и `b.subtracting(a)` обычно различаются.

# --instructions--

Напишите две функции, которые принимают два `Set<String>` (без меток аргументов):
- `common` возвращает множество значений, присутствующих в обоих множествах
- `onlyInFirst` возвращает множество значений, присутствующих в первом множестве, но не во втором

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`common(["a", "b", "c"], ["b", "c", "d"])` должна вернуть `["b", "c"]`

```swift
tryCatch(common(["a", "b", "c"], ["b", "c", "d"]) == ["b", "c"])
```

`common(["a"], ["b"])` должна вернуть пустое множество

```swift
tryCatch(common(["a"], ["b"]).isEmpty)
```

`onlyInFirst(["a", "b", "c"], ["b", "c", "d"])` должна вернуть `["a"]`

```swift
tryCatch(onlyInFirst(["a", "b", "c"], ["b", "c", "d"]) == ["a"])
```

`onlyInFirst(["x", "y"], ["z"])` должна вернуть `["x", "y"]`

```swift
tryCatch(onlyInFirst(["x", "y"], ["z"]) == ["x", "y"])
```

Обе функции должны возвращать `Set<String>`

```swift
tryCatch(type(of: common(["a"], ["a"])) == Set<String>.self && type(of: onlyInFirst(["a"], ["a"])) == Set<String>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func common(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.intersection(second)
}

func onlyInFirst(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.subtracting(second)
}
```
