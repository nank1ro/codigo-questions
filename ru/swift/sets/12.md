---
language: swift
exerciseType: 4
---

# --description--

Множества также можно сравнивать друг с другом. Эти методы возвращают `Bool`:
- `a.isSubset(of: b)` равно `true`, когда каждый элемент `a` также есть в `b`
- `a.isSuperset(of: b)` равно `true`, когда `a` содержит каждый элемент `b`
- `a.isDisjoint(with: b)` равно `true`, когда у `a` и `b` нет общих элементов
```swift
let small: Set<Int> = [1, 2]
let big: Set<Int> = [1, 2, 3]
print(small.isSubset(of: big))   // true
print(big.isSuperset(of: small)) // true
print(small.isDisjoint(with: big)) // false
```

# --instructions--

Расположите элементы в правильном порядке, чтобы сначала проверить, есть ли каждый фрукт из `basket` также в `fruits`, а затем — не имеют ли два множества ничего общего.
Объявите `fruits` перед `basket`

# --answers--

```swift
let fruits: Set<String> = ["apple", "pear", "plum"]
```

```swift
let basket: Set<String> = ["pear", "plum"]
```

```swift
print(basket.isSubset(of: fruits))
```

```swift
print(fruits.isDisjoint(with: basket))
```

# --solutions--

```swift
let fruits: Set<String> = ["apple", "pear", "plum"]
let basket: Set<String> = ["pear", "plum"]
print(basket.isSubset(of: fruits))
print(fruits.isDisjoint(with: basket))
```

# --output--

true
false
