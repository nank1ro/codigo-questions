---
language: swift
exerciseType: 1
---

# --description--

Как и массивы, множества могут быть константами (`let`) или переменными (`var`). Изменить после создания можно только множество `var`.
Чтобы создать пустое множество, вы вызываете инициализатор типа, потому что один лишь пустой литерал `[]` не сообщил бы Swift, какой тип элементов использовать:
```swift
var visited = Set<String>()
print(visited.isEmpty) // true
```
Свойство `isEmpty` равно `true`, когда множество не содержит элементов, точно так же, как и у массивов.

# --instructions--

Объявите константное множество строк с именем `colors`, содержащее `"red"`, `"green"` и `"blue"`, и пустое переменное множество целых чисел с именем `scores`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`colors` должен содержать ровно `"red"`, `"green"` и `"blue"`

```swift
tryCatch(colors == ["red", "green", "blue"])
```

`colors` должен быть `Set<String>`

```swift
tryCatch(type(of: colors) == Set<String>.self)
```

`scores` должен быть пустым

```swift
tryCatch(scores.isEmpty)
```

`scores` должен быть `Set<Int>`

```swift
tryCatch(type(of: scores) == Set<Int>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let colors: Set<String> = ["red", "green", "blue"]
var scores = Set<Int>()
```
