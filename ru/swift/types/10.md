---
language: swift
exerciseType: 4
---

# --description--

**Псевдоним типа** даёт существующему типу новое имя с помощью ключевого слова `typealias`:
```swift
typealias Score = Int
let best: Score = 100
print(best + 1) // 101
```
`Score` и `Int` — это один и тот же тип, поэтому они свободно смешиваются. Псевдоним не добавляет безопасности: он лишь делает код более читаемым, когда обычный тип имеет особое значение в вашей программе.

# --instructions--

Расположите элементы так, чтобы определить `Celsius` как псевдоним `Double` в первой строке, преобразовать точку кипения воды в Фаренгейты и вывести её

# --answers--

```swift
typealias Celsius = Double
```

```swift
let boiling: Celsius = 100
```

```swift
let fahrenheit = boiling * 9 / 5 + 32
```

```swift
print(fahrenheit)
```

# --solutions--

```swift
typealias Celsius = Double
let boiling: Celsius = 100
let fahrenheit = boiling * 9 / 5 + 32
print(fahrenheit)
```

# --output--

212.0
