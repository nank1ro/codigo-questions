---
language: swift
exerciseType: 2
---

# --description--

Swift никогда не преобразует числовые типы самостоятельно: сложение `Int` с `Double` — ошибка компиляции, хотя оба являются числами.
```swift
let apples = 3
let price = 1.5
let total = apples * price // error: Int and Double can't be mixed
```
Чтобы объединить их, вы создаёте новое значение нужного типа, передавая значение в инициализатор этого типа:
```swift
let total = Double(apples) * price // 4.5
```
То же самое работает и в обратную сторону: `Int(4.5)` создаёт `Int`, сохраняя только целую часть числа.

# --instructions--

Дополните преобразования так, чтобы `total` была стоимостью яблок как `Double`, а `whole` — её целой частью как `Int`, и программа вывела `4.5` и `4`

# --seed--

```swift
let apples = 3
let price = 1.5
let total = [/](apples) * price
let whole = [/](total)
print(total)
print(whole)
```

# --answers--

- Double
- Int
- Float
- String
- Character

# --solutions--

```swift
let apples = 3
let price = 1.5
let total = Double(apples) * price
let whole = Int(total)
print(total)
print(whole)
```

# --output--

4.5
4
