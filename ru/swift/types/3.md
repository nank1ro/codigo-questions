---
language: swift
exerciseType: 1
---

# --description--

Функция `type(of:)` возвращает тип значения, что удобно для проверки того, что вывел Swift:
```swift
print(type(of: 42))    // Int
print(type(of: 2.5))   // Double
print(type(of: "hi"))  // String
```
Когда вам нужен тип, отличный от выведенного, добавьте аннотацию. Целочисленный литерал можно сохранить в константе `Double` или `Float`, а литерал из одного символа — в константе `Character`:
```swift
let ratio: Double = 3       // 3.0, not an Int
let half: Float = 0.5
let initial: Character = "S"
print(type(of: ratio))      // Double
```

# --instructions--

Объявите три константы:
- `ratio`, типа `Double`, содержащую целочисленный литерал `3`
- `half`, типа `Float`, содержащую `0.5`
- `initial`, типа `Character`, содержащую `"S"`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`ratio` должна быть `Double`, равной `3.0`

```swift
tryCatch(type(of: ratio) == Double.self && ratio == 3.0)
```

`half` должна быть `Float`, равной `0.5`

```swift
tryCatch(type(of: half) == Float.self && half == 0.5)
```

`initial` должна быть `Character`, равной `"S"`

```swift
tryCatch(type(of: initial) == Character.self && initial == "S")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let ratio: Double = 3
let half: Float = 0.5
let initial: Character = "S"
```
