---
language: swift
exerciseType: 1
---

# --description--

Структура — это тип значения, поэтому метод, изменяющий одно из её хранимых свойств, должен быть помечен как `mutating`. Когда такой метод является требованием протокола, протокол тоже должен это указать:
```swift
protocol Togglable {
    mutating func toggle()
}

struct Light: Togglable {
    var isOn = false

    mutating func toggle() {
        isOn = !isOn
    }
}

var lamp = Light()
lamp.toggle()
print(lamp.isOn) // true
```
Без `mutating` в протоколе структура никогда не смогла бы удовлетворить требование. Классы — ссылочные типы и никогда не нуждаются в этом ключевом слове: класс выполняет `mutating`-требование обычным методом. Вызов мутирующего метода требует `var` — на `let` это ошибка компиляции.

# --instructions--

Сделайте `Score` соответствующим `Resettable` с методом `reset()`, который устанавливает `points` в `0`

Пример использования:
```swift
var score = Score(points: 10)
score.reset()
print(score.points)
// prints 0
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Resettable {
    mutating func reset()
}

struct Score {
    var points: Int
}
```

# --before-asserts--

```swift
func _reset<T: Resettable>(_ v: inout T) {
    v.reset()
}
```

# --asserts--

Счёт `10` должен стать `0` после сброса

```swift
var _s1 = Score(points: 10)
_reset(&_s1)
tryCatch(_s1.points == 0)
```

Счёт `-4` должен стать `0` после сброса

```swift
var _s2 = Score(points: -4)
_reset(&_s2)
tryCatch(_s2.points == 0)
```

Счёт `0` должен остаться `0` после сброса

```swift
var _s3 = Score(points: 0)
_reset(&_s3)
tryCatch(_s3.points == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Resettable {
    mutating func reset()
}

struct Score: Resettable {
    var points: Int

    mutating func reset() {
        points = 0
    }
}
```
