---
language: swift
exerciseType: 1
---

# --description--

Стандартная библиотека построена из протоколов, и ваши собственные типы могут их принимать.

`Equatable` даёт типу оператор `==`. Для структуры, все хранимые свойства которой `Equatable`, достаточно объявить соответствие — Swift напишет `==` за вас:
```swift
struct Point: Equatable {
    var x: Int
    var y: Int
}

print(Point(x: 1, y: 2) == Point(x: 1, y: 2)) // true
```
`Comparable` наследует от `Equatable` и добавляет упорядочивание. Вы реализуете один оператор, `<`, записанный как `static func`, принимающий оба значения, и бесплатно получаете `>`, `<=`, `>=`, а также `sorted()`, `min()` и `max()` на коллекциях:
```swift
struct Version: Comparable {
    var number: Int

    static func < (lhs: Version, rhs: Version) -> Bool {
        return lhs.number < rhs.number
    }
}
```

# --instructions--

Сделайте `Weight` соответствующим `Comparable`, упорядочивая по `grams`

Пример использования:
```swift
print(Weight(grams: 100) < Weight(grams: 250))
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Weight {
    var grams: Int
}
```

# --asserts--

`100` граммов должны быть легче `250` граммов

```swift
tryCatch(Weight(grams: 100) < Weight(grams: 250))
```

`500` граммов должны быть тяжелее `250` граммов

```swift
tryCatch(Weight(grams: 500) > Weight(grams: 250))
```

Два веса по `80` граммов должны быть равны

```swift
tryCatch(Weight(grams: 80) == Weight(grams: 80))
```

Самый тяжёлый из `3` и `1` граммов должен быть `3` грамма

```swift
tryCatch([Weight(grams: 3), Weight(grams: 1)].max() == Weight(grams: 3))
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Weight: Comparable {
    var grams: Int

    static func < (lhs: Weight, rhs: Weight) -> Bool {
        return lhs.grams < rhs.grams
    }
}
```
