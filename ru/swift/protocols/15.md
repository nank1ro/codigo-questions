---
language: swift
exerciseType: 1
---

# --description--

Имя протокола само по себе — не тип, а ограничение, поэтому Swift просит вас указать, что из двух вы имеете в виду.

`some Shape` означает *один конкретный соответствующий тип*, фиксированный во время компиляции. Вызывающий код никогда не узнаёт, какой именно, но он всегда один и тот же:
```swift
func unitSquare() -> some Shape {
    return Square(side: 1)
}
```
`any Shape` означает *коробку, которая может хранить любой соответствующий тип*, и два значения такого типа могут хранить разные типы. Это нужно всякий раз, когда конкретный тип может меняться, например внутри смешанного массива:
```swift
let shapes: [any Shape] = [Square(side: 2), Rect(width: 2, height: 3)]
```
Оба позволяют вызывать требования протокола. Предпочитайте `some`, когда достаточно одного типа, потому что это ничего не стоит во время выполнения; обращайтесь к `any`, когда вам действительно нужно смешивать типы.

# --instructions--

Напишите функцию `totalArea`, которая принимает массив `any Shape` без метки аргумента и возвращает сумму их площадей

Пример вызова функции:
```swift
print(totalArea([Square(side: 2), Rect(width: 2, height: 3)]))
// prints 10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Shape {
    func area() -> Int
}

struct Square: Shape {
    var side: Int

    func area() -> Int {
        return side * side
    }
}

struct Rect: Shape {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}

func unitSquare() -> some Shape {
    return Square(side: 1)
}

func totalArea() {

}
```

# --asserts--

Квадрат со стороной `2` и прямоугольник `2` на `3` должны в сумме дать `10`

```swift
tryCatch(totalArea([Square(side: 2), Rect(width: 2, height: 3)]) == 10)
```

Пустой массив должен в сумме дать `0`

```swift
tryCatch(totalArea([]) == 0)
```

Один прямоугольник `5` на `4` должен в сумме дать `20`

```swift
tryCatch(totalArea([Rect(width: 5, height: 4)]) == 20)
```

Один единичный квадрат должен в сумме дать `1`

```swift
tryCatch(totalArea([unitSquare()]) == 1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Shape {
    func area() -> Int
}

struct Square: Shape {
    var side: Int

    func area() -> Int {
        return side * side
    }
}

struct Rect: Shape {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}

func unitSquare() -> some Shape {
    return Square(side: 1)
}

func totalArea(_ shapes: [any Shape]) -> Int {
    var total = 0
    for shape in shapes {
        total += shape.area()
    }
    return total
}
```
