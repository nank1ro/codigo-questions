---
language: swift
exerciseType: 1
---

# --description--

Протоколами не ограничиваются структуры. **Класс** соответствует точно так же, указывая протокол после двоеточия:
```swift
protocol Openable {
    func open() -> String
}

class Door: Openable {
    func open() -> String {
        return "creak"
    }
}
```
Если класс также наследует другой класс, суперкласс идёт первым в списке, а протоколы следуют за ним. Тип может принимать несколько протоколов одновременно, разделяя их запятыми.

# --instructions--

Сделайте класс `Box` соответствующим `Sized`, возвращая количество хранимых в нём элементов

Пример использования:
```swift
print(Box(items: ["pen", "key"]).size())
// выводит 2
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Sized {
    func size() -> Int
}

class Box {
    var items: [String]

    init(items: [String]) {
        self.items = items
    }
}
```

# --before-asserts--

```swift
func _asSized(_ s: Sized) -> Int {
    return s.size()
}
```

# --asserts--

Коробка с `pen` и `key` должна иметь размер `2`

```swift
tryCatch(_asSized(Box(items: ["pen", "key"])) == 2)
```

Пустая коробка должна иметь размер `0`

```swift
tryCatch(_asSized(Box(items: [])) == 0)
```

Коробка с тремя элементами должна иметь размер `3`

```swift
tryCatch(_asSized(Box(items: ["a", "b", "c"])) == 3)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Sized {
    func size() -> Int
}

class Box: Sized {
    var items: [String]

    init(items: [String]) {
        self.items = items
    }

    func size() -> Int {
        return items.count
    }
}
```
