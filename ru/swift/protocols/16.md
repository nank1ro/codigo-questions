---
language: swift
exerciseType: 4
---

# --instructions--

Расположите элементы так, чтобы вывести `[G]`, сначала объявив протокол, затем его расширение, затем перечисление, затем `print`

# --answers--

```swift
protocol Badge {
    var symbol: String { get }
}
```

```swift
extension Badge {
    func banner() -> String { return "[\(symbol)]" }
}
```

```swift
enum Rank: Badge {
    case gold

    var symbol: String { return "G" }
}
```

```swift
print(Rank.gold.banner())
```

# --solutions--

```swift
protocol Badge {
    var symbol: String { get }
}
extension Badge {
    func banner() -> String { return "[\(symbol)]" }
}
enum Rank: Badge {
    case gold

    var symbol: String { return "G" }
}
print(Rank.gold.banner())
```

# --output--

[G]
