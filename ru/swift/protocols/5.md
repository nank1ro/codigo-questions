---
language: swift
exerciseType: 2
---

# --description--

**Перечисление** тоже может соответствовать. У него нет хранимых свойств, поэтому требование к свойству обычно выполняется вычисляемым свойством, которое выполняет `switch` по вариантам:
```swift
protocol Priced {
    var price: Int { get }
}

enum Ticket: Priced {
    case child, adult

    var price: Int {
        switch self {
        case .child: return 5
        case .adult: return 12
        }
    }
}

print(Ticket.adult.price) // 12
```
Структуры, классы и перечисления: все три принимают протоколы одинаково, и код, написанный против протокола, работает со всеми ними.

# --instructions--

Дополните код так, чтобы `Priced` требовал читаемое свойство `price`, а `Drink` соответствовал ему

# --seed--

```swift
protocol Priced {
    var price: Int { [/] }
}

enum Drink: [/] {
    case tea, coffee

    var price: Int {
        [/] self {
        case .tea: return 3
        case .coffee: return 5
        }
    }
}

print(Drink.coffee.price)
```

# --answers--

- get
- Priced
- switch
- set
- if
- return

# --solutions--

```swift
protocol Priced {
    var price: Int { get }
}

enum Drink: Priced {
    case tea, coffee

    var price: Int {
        switch self {
        case .tea: return 3
        case .coffee: return 5
        }
    }
}

print(Drink.coffee.price)
```

# --output--

5
