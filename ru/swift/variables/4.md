---
language: swift
exerciseType: 1
---

# --description--

Когда мы обновляем переменную, она забывает своё предыдущее значение. Здесь мы можем вывести переменную `x` дважды и увидеть, как её значение обновляется.
```swift
var x = 5
print(x) // выводит 5
x = 10
print(x) // выводит 10
```

# --instructions--

Обновите значение переменной `x` числом `3`.

# --seed--

```swift
var x = 1
// write after this line
```

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`x` должно быть равно `3`.

```c
    func test1() {
        XCTAssertEqual(x, 3, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```c
var x = 1
x = 3
```
