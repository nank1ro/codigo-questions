---
language: swift
exerciseType: 2
---

# --description--

Ненадёжные преобразования естественно сочетаются с `guard let`: преобразуйте, прервите выполнение, если результат равен `nil`, а затем работайте с обычным значением.

# --instructions--

Дополните функцию так, чтобы она преобразовывала текст в `Int` и возвращала его удвоенное значение, или `nil`, если преобразование не удалось.
Затем выведите результаты, используя `0` в качестве значения по умолчанию

# --seed--

```swift
func doubled(_ text: String) -> Int? {
    guard let number = [/](text) else { return [/] }
    return number * 2
}
print(doubled("21") [/] 0)
print(doubled("x") ?? 0)
```

# --answers--

- Int
- nil
- ??
- Int?
- String
- !

# --solutions--

```swift
func doubled(_ text: String) -> Int? {
    guard let number = Int(text) else { return nil }
    return number * 2
}
print(doubled("21") ?? 0)
print(doubled("x") ?? 0)
```

# --output--

42
0
