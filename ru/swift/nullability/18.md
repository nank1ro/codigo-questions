---
language: swift
exerciseType: 1
---

# --description--

Иногда нужно преобразовать значение внутри опционального значения и сохранить результат опциональным, не разворачивая и не оборачивая его вручную.
У опциональных значений есть метод `map`: он применяет замыкание к значению, если оно есть, и возвращает `nil` в противном случае.
```swift
let score: Int? = 10
let doubled = score.map { $0 * 2 } // Int?, содержащий 20
let missing: Int? = nil
let stillMissing = missing.map { $0 * 2 } // nil
```
В сочетании с ненадёжным преобразованием получается компактный конвейер: `Int(text).map { $0 + 1 }`.

# --instructions--

Напишите функцию `squareText`, которая принимает `String` (без метки аргумента), преобразует её в `Int` и возвращает её квадрат как `Int?`, или `nil`, если преобразование не удалось.
Используйте `map`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`squareText("4")` должна вернуть `16`

```swift
tryCatch(squareText("4") == 16)
```

`squareText("12")` должна вернуть `144`

```swift
tryCatch(squareText("12") == 144)
```

`squareText("x")` должна вернуть `nil`

```swift
tryCatch(squareText("x") == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func squareText(_ text: String) -> Int? {
    return Int(text).map { $0 * $0 }
}
```
