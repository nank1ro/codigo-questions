---
language: swift
exerciseType: 1
---

# --description--

Один `if let` или `guard let` может развернуть сразу несколько опциональных значений: разделите привязки запятыми.
Тело выполняется только тогда, когда каждое опциональное значение содержит значение:
```swift
let first: String? = "Ada"
let last: String? = "Lovelace"
if let first = first, let last = last {
    print("\(first) \(last)")
}
```
Вы также можете добавить логическое условие после привязок, например `if let n = number, n > 0`.

# --instructions--

Напишите функцию `add`, которая принимает два значения `Int?` (без меток аргументов) и возвращает их сумму как `Int?`, или `nil`, если хотя бы одно из них отсутствует

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`add(2, 3)` должна вернуть `5`

```swift
tryCatch(add(2, 3) == 5)
```

`add(nil, 3)` должна вернуть `nil`

```swift
tryCatch(add(nil, 3) == nil)
```

`add(2, nil)` должна вернуть `nil`

```swift
tryCatch(add(2, nil) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func add(_ a: Int?, _ b: Int?) -> Int? {
    guard let a = a, let b = b else { return nil }
    return a + b
}
```
