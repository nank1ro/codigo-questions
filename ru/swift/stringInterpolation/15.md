---
language: swift
exerciseType: 3
---

# --instructions--

Что не так с этим кодом?
```swift
print("\(10} friends")
```

# --answers--

- Интерполяция строки должна быть внутри \()
- Значение внутри \() не является числом
- В строке отсутствуют кавычки " "
- Значение внутри \() должно быть именем переменной

# --solutions--

- Интерполяция строки должна быть внутри \()

