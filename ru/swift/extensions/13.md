---
language: swift
exerciseType: 2
---

# --description--

Расширения могут добавлять **статические** члены: свойства и методы, принадлежащие самому типу, а не отдельному значению, помеченные ключевым словом `static` и доступные через имя типа.
`static let` разрешён, даже если он хранит значение, потому что существует только одна копия для всего типа, а не по одной на экземпляр:
```swift
extension Int {
    static let answer = 42
    static func zeroes(_ count: Int) -> [Int] {
        return Array(repeating: 0, count: count)
    }
}
print(Int.answer)     // 42
print(Int.zeroes(3))  // [0, 0, 0]
```
У статического члена нет значения `self` для работы: `Int.answer` читается на типе, а не на числе.

# --instructions--

Дополните расширение статической константой `goldenRatio` и вызовите статический метод `half(of:)` через имя типа, чтобы программа вывела `1.618` и `4.5`

# --seed--

```swift
extension Double {
    [/] let goldenRatio = 1.618
    static func half(of value: Double) -> Double {
        return value / 2
    }
}
print(Double.goldenRatio)
print([/].half(of: 9))
```

# --answers--

- static
- Double
- var
- class
- self

# --solutions--

```swift
extension Double {
    static let goldenRatio = 1.618
    static func half(of value: Double) -> Double {
        return value / 2
    }
}
print(Double.goldenRatio)
print(Double.half(of: 9))
```

# --output--

1.618
4.5
