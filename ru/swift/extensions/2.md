---
language: swift
exerciseType: 1
---

# --description--

Расширения работают с любым типом, даже с тем, для которого у вас нет исходного кода. `String` берётся из стандартной библиотеки, но вы всё равно можете добавить ей новые методы:
```swift
extension String {
    func whisper() -> String {
        return self.lowercased() + "..."
    }
}
print("HELLO".whisper()) // hello...
```
Внутри расширения можно опускать `self.` при вызове других членов типа: одно лишь `lowercased()` означает `self.lowercased()`.

# --instructions--

Напишите расширение `String` с методом `shout()`, который возвращает строку в верхнем регистре, за которой следует восклицательный знак `!`

Пример вызова метода:
```swift
print("hi".shout())
// prints HI!
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Вы должны использовать расширение `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`"hi".shout()` должен возвращать `"HI!"`

```swift
tryCatch("hi".shout() == "HI!")
```

`"Swift is fun".shout()` должен возвращать `"SWIFT IS FUN!"`

```swift
tryCatch("Swift is fun".shout() == "SWIFT IS FUN!")
```

`"".shout()` должен возвращать `"!"`

```swift
tryCatch("".shout() == "!")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    func shout() -> String {
        return uppercased() + "!"
    }
}
```
