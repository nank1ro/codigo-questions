---
language: swift
exerciseType: 4
---

# --description--

`Int`, `String`, массивы и структуры — это **типы значений**: метод не может изменить значение, для которого он вызван, если он не помечен как `mutating`. Расширения тоже могут добавлять мутирующие методы:
```swift
extension Int {
    mutating func increment() {
        self += 1
    }
}
var count = 1
count.increment()
print(count) // 2
```
Внутри мутирующего метода можно присваивать `self`. Значение должно храниться в `var`: вызов `increment()` для константы `let` — ошибка компиляции.

# --instructions--

Расположите элементы так, чтобы сначала определить расширение `Int` с мутирующим методом `double()`, затем объявить переменную `n` со значением `4`, удвоить её и напечатать

# --answers--

```swift
extension Int {
```

```swift
    mutating func double() { self *= 2 }
```

```swift
}
```

```swift
var n = 4
```

```swift
n.double()
```

```swift
print(n)
```

# --solutions--

```swift
extension Int {
    mutating func double() { self *= 2 }
}
var n = 4
n.double()
print(n)
```

# --output--

8
