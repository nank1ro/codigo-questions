---
language: swift
exerciseType: 1
---

# --description--

Вычисляемые свойства в расширениях естественно подходят и для `String`. Метод `reversed()` возвращает символы в обратном порядке, а `String(...)` превращает их обратно в строку:
```swift
extension String {
    var backwards: String {
        return String(reversed())
    }
}
print("swift".backwards) // tfiws
```
Как и с методами, `reversed()` внутри расширения означает `self.reversed()`.

# --instructions--

Напишите расширение `String` с вычисляемым свойством `isPalindrome` типа `Bool`, равным `true`, когда строка читается одинаково в обоих направлениях. Сравнение чувствительно к регистру

Пример использования:
```swift
print("level".isPalindrome)
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Вы должны использовать расширение `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`"level".isPalindrome` должно быть равно `true`

```swift
tryCatch("level".isPalindrome == true)
```

`"swift".isPalindrome` должно быть равно `false`

```swift
tryCatch("swift".isPalindrome == false)
```

`"Anna".isPalindrome` должно быть равно `false` (сравнение чувствительно к регистру)

```swift
tryCatch("Anna".isPalindrome == false)
```

`"".isPalindrome` должно быть равно `true`

```swift
tryCatch("".isPalindrome == true)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    var isPalindrome: Bool {
        return self == String(reversed())
    }
}
```
