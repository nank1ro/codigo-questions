---
language: swift
exerciseType: 2
---

# --description--

Помимо методов, расширение может добавлять **вычисляемые свойства**: свойства, которые не хранят значение, а вычисляют его при каждом чтении.
Вычисляемое свойство объявляется с помощью `var`, аннотации типа и тела между фигурными скобками, которое возвращает значение:
```swift
extension Int {
    var isNegative: Bool {
        return self < 0
    }
}
print((-3).isNegative) // true
print(7.isNegative)    // false
```
Оно читается как любое свойство, без круглых скобок: `7.isNegative`, а не `7.isNegative()`.

# --instructions--

Дополните расширение так, чтобы `isEven` было вычисляемым свойством, сообщающим, чётно ли число, и программа вывела `true` и `false`

# --seed--

```swift
extension Int {
    [/] isEven: [/] {
        return self % 2 == 0
    }
}
print(4.isEven)
print(7.isEven)
```

# --answers--

- var
- Bool
- let
- func
- Int

# --solutions--

```swift
extension Int {
    var isEven: Bool {
        return self % 2 == 0
    }
}
print(4.isEven)
print(7.isEven)
```

# --output--

true
false
