---
language: swift
exerciseType: 2
---

# --description--

Расширение может добавлять типу новые **инициализаторы**. Для структуры это лучшее место для них: `init`, написанный внутри тела структуры, заменяет автоматический memberwise-инициализатор, а добавленный в расширении сохраняет его.
Новый инициализатор обычно делегирует существующему с помощью `self.init(...)`:
```swift
struct Size {
    var width: Double
    var height: Double
}
extension Size {
    init(square side: Double) {
        self.init(width: side, height: side)
    }
}
let a = Size(square: 3)          // from the extension
let b = Size(width: 2, height: 5) // memberwise, still available
```

# --instructions--

Дополните расширение так, чтобы `Point(both: 2.5)` создавал точку с `x` и `y`, равными `2.5`, и программа вывела `2.5 2.5` и `1.0 2.0`

# --seed--

```swift
struct Point {
    var x: Double
    var y: Double
}
extension Point {
    [/](both: Double) {
        [/](x: both, y: both)
    }
}
let p = Point(both: 2.5)
let q = Point(x: 1, y: 2)
print(p.x, p.y)
print(q.x, q.y)
```

# --answers--

- init
- self.init
- func
- Point
- self

# --solutions--

```swift
struct Point {
    var x: Double
    var y: Double
}
extension Point {
    init(both: Double) {
        self.init(x: both, y: both)
    }
}
let p = Point(both: 2.5)
let q = Point(x: 1, y: 2)
print(p.x, p.y)
print(q.x, q.y)
```

# --output--

2.5 2.5
1.0 2.0
