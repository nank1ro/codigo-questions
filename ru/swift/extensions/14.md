---
language: swift
exerciseType: 1
---

# --description--

Статические методы в расширениях — хорошее место для небольших фабричных функций, создающих значение типа. `String(repeating:count:)` — стандартный инициализатор, который повторяет фрагмент текста заданное число раз:
```swift
extension String {
    static func dashes(_ count: Int) -> String {
        return String(repeating: "-", count: count)
    }
}
print(String.dashes(4)) // ----
```

# --instructions--

Напишите расширение `String` со статическим методом `stars(_ count: Int)`, который возвращает строку из `count` звёздочек `*`

Пример вызова метода:
```swift
print(String.stars(3))
// prints ***
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Вы должны использовать расширение `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Метод должен быть `static`

```json
{
  "regex": "static\\s+func\\s+stars",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`String.stars(3)` должен возвращать `"***"`

```swift
tryCatch(String.stars(3) == "***")
```

`String.stars(1)` должен возвращать `"*"`

```swift
tryCatch(String.stars(1) == "*")
```

`String.stars(0)` должен возвращать пустую строку

```swift
tryCatch(String.stars(0) == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    static func stars(_ count: Int) -> String {
        return String(repeating: "*", count: count)
    }
}
```
