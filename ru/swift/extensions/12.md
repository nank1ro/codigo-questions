---
language: swift
exerciseType: 1
---

# --description--

`Array` — обобщённый тип: `[Int]` и `[String]` — оба массивы, но с разным типом **`Element`**. Расширение `Array` применяется ко всем им, что является проблемой, когда новый член имеет смысл лишь для некоторых элементов: нельзя складывать числа, которые являются строками.
Условие `where` ограничивает расширение массивами, чей `Element` имеет заданный тип:
```swift
extension Array where Element == Int {
    var largest: Int {
        var result = Int.min
        for number in self {
            if number > result {
                result = number
            }
        }
        return result
    }
}
print([3, 9, 2].largest) // 9
```
`[3, 9, 2].largest` работает, а `["a", "b"].largest` — ошибка компиляции: свойства не существует у `[String]`.

# --instructions--

Напишите расширение `Array`, ограниченное условием `Element == Int`, с методом `sum()`, возвращающим сумму элементов. Не используйте `reduce`

Пример вызова метода:
```swift
print([1, 2, 3].sum())
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Вы должны использовать расширение `Array` с условием `where Element == Int`

```json
{
  "regex": "extension\\s+Array\\s+where\\s+Element\\s*==\\s*Int",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Вы не должны использовать `reduce`

```json
{
  "regex": "reduce",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`[1, 2, 3].sum()` должен возвращать `6`

```swift
tryCatch([1, 2, 3].sum() == 6)
```

`[10, -4].sum()` должен возвращать `6`

```swift
tryCatch([10, -4].sum() == 6)
```

`[7].sum()` должен возвращать `7`

```swift
tryCatch([7].sum() == 7)
```

Сумма пустого массива должна быть равна `0`

```swift
let _empty: [Int] = []
tryCatch(_empty.sum() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension Array where Element == Int {
    func sum() -> Int {
        var total = 0
        for number in self {
            total += number
        }
        return total
    }
}
```
