---
language: swift
exerciseType: 2
---

# --description--

Две структуры нельзя сравнивать с помощью `==`, пока тип не заявит о поддержке этого. Вы делаете это, реализуя **протокол** `Equatable`, который записывается после двоеточия в объявлении:
```swift
struct Point: Equatable {
    var x: Int
    var y: Int
}

let a = Point(x: 1, y: 2)
let b = Point(x: 1, y: 2)
print(a == b) // true
```
Вам не нужно писать `==` самостоятельно: когда каждое хранимое свойство уже является `Equatable`, Swift синтезирует его за вас, сравнивая свойства одно за другим. Два экземпляра равны, когда равны все их свойства — именно этого и ожидаешь от значения.

# --instructions--

Дополните код так, чтобы `Point` можно было сравнивать, затем выведите, равны ли `a` и `b`

# --seed--

```swift
struct Point: [/] {
    var x: Int
    var y: Int
}

let a = Point(x: 1, y: 2)
let b = Point(x: 1, y: 2)
print(a [/] b)
```

# --answers--

- Equatable
- ==
- Comparable
- !=
- ===
- Codable

# --solutions--

```swift
struct Point: Equatable {
    var x: Int
    var y: Int
}

let a = Point(x: 1, y: 2)
let b = Point(x: 1, y: 2)
print(a == b)
```

# --output--

true
