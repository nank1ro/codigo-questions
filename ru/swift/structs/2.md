---
language: swift
exerciseType: 1
---

# --description--

Вы не писали код, который создаёт `Book`, но `Book(title: "Swift", pages: 120)` сработал. Swift пишет его за вас: каждая структура получает бесплатный **memberwise-инициализатор** — инициализатор, параметры которого являются его хранимыми свойствами, в порядке их объявления, причём каждый из них использует имя свойства в качестве метки аргумента:
```swift
struct Point {
    var x: Int
    var y: Int
}

let p = Point(x: 3, y: 4)
print(p.x) // 3
```
Классы не получают этого бесплатно, и это одна из причин, почему структуры — самый быстрый способ смоделировать значение.

# --instructions--

Напишите функцию `makePoint`, которая принимает два целых числа без меток аргументов и возвращает `Point`, созданный с помощью memberwise-инициализатора

Пример вызова функции:
```swift
print(makePoint(3, 4).x)
// prints 3
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Point {
    var x: Int
    var y: Int
}

func makePoint() {

}
```

# --asserts--

`makePoint(3, 4)` должен иметь `x`, равный `3`

```swift
tryCatch(makePoint(3, 4).x == 3)
```

`makePoint(3, 4)` должен иметь `y`, равный `4`

```swift
tryCatch(makePoint(3, 4).y == 4)
```

`makePoint(0, -5)` должен иметь `y`, равный `-5`

```swift
tryCatch(makePoint(0, -5).y == -5)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Point {
    var x: Int
    var y: Int
}

func makePoint(_ x: Int, _ y: Int) -> Point {
    return Point(x: x, y: y)
}
```
