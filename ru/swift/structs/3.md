---
language: swift
exerciseType: 2
---

# --description--

Хранимому свойству можно задать **значение по умолчанию** прямо в месте его объявления. Swift выводит его тип из этого значения, поэтому аннотацию типа можно опустить:
```swift
struct Counter {
    var label: String
    var value = 0
}
```
Memberwise-инициализатор превращает каждое свойство со значением по умолчанию в необязательный аргумент: передайте его, чтобы переопределить значение по умолчанию, или опустите, чтобы сохранить его.
```swift
let a = Counter(label: "clicks")
print(a.value) // 0

let b = Counter(label: "clicks", value: 7)
print(b.value) // 7
```

# --instructions--

Дополните код так, чтобы `value` начиналось с `0`, а `c` создавалось без передачи `value`

# --seed--

```swift
struct Counter {
    var label: String
    var value = [/]
}

let c = Counter([/]: "clicks")
print(c.label)
print(c.value)
```

# --answers--

- 0
- label
- 1
- value
- Counter
- name

# --solutions--

```swift
struct Counter {
    var label: String
    var value = 0
}

let c = Counter(label: "clicks")
print(c.label)
print(c.value)
```

# --output--

clicks
0
