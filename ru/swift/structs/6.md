---
language: swift
exerciseType: 4
---

# --instructions--

Отсортируйте элементы так, чтобы выводилось `12`: сначала объявите структуру, а экземпляр — перед `print`

# --answers--

```swift
struct Rectangle {
    var width: Int
    var height: Int
    func area() -> Int {
        return width * height
    }
}
```

```swift
let r = Rectangle(width: 3, height: 4)
```

```swift
print(r.area())
```

# --solutions--

```swift
struct Rectangle {
    var width: Int
    var height: Int
    func area() -> Int {
        return width * height
    }
}
let r = Rectangle(width: 3, height: 4)
print(r.area())
```

# --output--

12
