---
language: swift
exerciseType: 1
---

# --description--

Структура также может содержать **методы**: функции, написанные внутри фигурных скобок, которые работают с экземпляром, у которого они вызваны. Внутри метода вы используете имена свойств напрямую, без какого-либо префикса:
```swift
struct Rectangle {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}

let r = Rectangle(width: 3, height: 4)
print(r.area()) // 12
```
Если параметр метода затеняет имя свойства, напишите `self.width`, чтобы обратиться к свойству экземпляра.

# --instructions--

Добавьте в `Rectangle` метод `area`, который не принимает параметров и возвращает ширину, умноженную на высоту

Пример вызова функции:
```swift
print(Rectangle(width: 3, height: 4).area())
// prints 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Rectangle {
    var width: Int
    var height: Int

}
```

# --asserts--

Прямоугольник `3` на `4` должен иметь площадь `12`

```swift
tryCatch(Rectangle(width: 3, height: 4).area() == 12)
```

Прямоугольник `1` на `1` должен иметь площадь `1`

```swift
tryCatch(Rectangle(width: 1, height: 1).area() == 1)
```

Прямоугольник `0` на `7` должен иметь площадь `0`

```swift
tryCatch(Rectangle(width: 0, height: 7).area() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Rectangle {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}
```
