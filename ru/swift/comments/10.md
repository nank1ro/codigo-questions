---
language: swift
exerciseType: 1
---

# --description--

Первая строка документирующего комментария — это **краткое описание**: короткое предложение о том, что делает функция. Пишите его в третьем лице, как будто описываете функцию: "Returns...", "Adds...", "Checks...".
```swift
/// Returns `true` when `n` is divisible by two.
func isEven(_ n: Int) -> Bool {
    return n % 2 == 0
}
```
Комментарий должен находиться прямо над объявлением, без пустой строки между ними, иначе Xcode не свяжет его с функцией.

# --instructions--

Добавьте документирующий комментарий `///` с кратким описанием в одну строку прямо над `square`, например `/// Returns `n` multiplied by itself.`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func square(_ n: Int) -> Int {
    return n * n
}
```

# --asserts--

A `///` comment must be placed directly above `func square`

```json
{
  "regex": "///\\s*\\S[^\\n]*\\n\\s*func square",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`square(3)` must return `9`

```swift
tryCatch(square(3) == 9)
```

`square(-5)` must return `25`

```swift
tryCatch(square(-5) == 25)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
/// Returns `n` multiplied by itself.
func square(_ n: Int) -> Int {
    return n * n
}
```
