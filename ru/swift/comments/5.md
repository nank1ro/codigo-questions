---
language: swift
exerciseType: 1
---

# --description--

В отличие от `//`, который заканчивается в конце строки, комментарий `/*` заканчивается только на `*/`. Если забыть его закрыть, компилятор посчитает весь следующий код частью комментария и сообщит об ошибке:
```swift
let width = 10 /* in centimetres
print(width) // still inside the comment: error, the comment is never closed
```
И `//`, и `/* */` работают как завершающие комментарии, но с `/*` всегда убеждайтесь, что `*/` на месте.

# --instructions--

`twice` не компилируется, потому что завершающий комментарий в первой строке никогда не закрывается и поглощает `return`. Закройте комментарий в конце этой же строки

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func twice(_ n: Int) -> Int {
    let result = n * 2 /* twice the input
    return result
}
```

# --asserts--

The comment must be closed on the same line

```json
{
  "regex": "/\\* twice the input \\*/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`twice(4)` must return `8`

```swift
tryCatch(twice(4) == 8)
```

`twice(-3)` must return `-6`

```swift
tryCatch(twice(-3) == -6)
```

`twice(0)` must return `0`

```swift
tryCatch(twice(0) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func twice(_ n: Int) -> Int {
    let result = n * 2 /* twice the input */
    return result
}
```
