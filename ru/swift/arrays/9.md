---
language: swift
exerciseType: 2
---

# --description--

Иногда нужно найти элемент в массиве.
В Swift мы можем использовать метод `firstIndex()`:
```swift
var names: [String] = ["Trevor", "Zac", "Glenn"]
if let index = names.firstIndex(of: "Zac") {
  print(index)
}
// prints 1
```
Код выше выводит первый индекс, содержащий строку `"Zac"`, в данном случае `1`.
Мы также можем вставлять элементы в массив по определённому индексу с помощью метода `insert()`:
```swift
names.insert("Ali", at: 1)
// prints ["Trevor", "Ali", "Zac", "Glenn"]
```
Код выше вставляет `"Ali"` по индексу `1`, что сдвигает все элементы после этого индекса на 1 позицию вниз

# --instructions--

- Используйте метод `firstIndex()`, чтобы вывести индекс `"dog"`.
- Затем используйте метод `insert()`, чтобы добавить `"bear"` первым элементом массива

# --seed--

```swift
var animals = ["cat", "monkey", "dog"]
if let i = animals.[/](of: [/]) {
    print(i)
}
animals.[/]("bear", at: [/])
print(animals)
```

# --answers--

- index
- firstIndex
- dog
- "dog"
- insert
- append
- add
- 0
- 1

# --solutions--

```swift
var animals = ["cat", "monkey", "dog"]
if let i = animals.firstIndex(of: "dog") {
    print(i)
}
animals.insert("bear", at: 0)
print(animals)
```

# --output--

2
["bear", "cat", "monkey", "dog"]
