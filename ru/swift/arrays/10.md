---
language: swift
exerciseType: 2
---

# --description--

В Swift мы можем перебирать массив очень просто, используя ключевые слова `for..in`:
```swift
var numbers = [1, 2, 3]
for num in numbers {
    print(num)
}
// prints 1, 2, 3 
```
После ключевого слова `for` следует имя переменной, которой поочерёдно будет присваиваться значение каждого элемента массива.

# --instructions--

Выведите каждый элемент массива, умножив его на `2`

# --seed--

```swift
var numbers = [2, 4, 6]
[/][/][/][/]{
    print([/][/][/])
}
```

# --answers--

- for 
- num 
- in 
- numbers 
- num 
- * 
- ** 
- 2

# --solutions--

```swift
var numbers = [2, 4, 6]
for num in numbers {
    print(num * 2)
}
```

# --output--

4
8
12
