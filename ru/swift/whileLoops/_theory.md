Часто в программировании нам нужно повторять блок кода, например:
```swift
print("2 seconds")
print("3 seconds")
print("4 seconds")
print("5 seconds")
```
Это даёт следующий вывод:
```swift
2 seconds
3 seconds
4 seconds
5 seconds
```
Очевидно, что для длинных инструкций мы потратим много времени на написание кода, но, к счастью, мы можем использовать циклы.
Давайте изучим цикл `while`, получив тот же вывод, что и выше.
```swift
var count = 2
while count <= 5 {
    print("\(count) seconds")
    count += 1
}
```
Итак, мы создали переменную `count`, присвоив ей `2` — начальное значение.
Затем мы использовали оператор `while`, который будет выполнять блок кода, пока условие `count <= 5` равно `true`.
Внутри блока кода мы **НЕ ДОЛЖНЫ** забывать добавить строку `count += 1`.
Она увеличивает значение `count`, иначе наш цикл будет бесконечным

---

Чтобы контролировать количество повторений цикла `while`, мы начинаем с переменной, установленной в число.
Мы называем эту переменную переменной-счётчиком

---

Затем мы используем сравнение в условии, чтобы сравнить переменную `counter` с числом.

---

Внутри блока кода, чтобы остановить цикл `while`, мы увеличиваем переменную `counter`.

---

Порядок написания кода влияет на вывод.

---

В Swift также есть вариация цикла `while` — **repeat-while**.
Она сначала выполняет один проход по блоку цикла, _перед_ проверкой условия цикла.
Затем цикл продолжает повторяться, пока условие не станет `false`.
Цикл __repeat-while__ в Swift аналогичен циклу __do-while__ в других языках
