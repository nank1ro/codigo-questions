---
language: swift
exerciseType: 2
---

# --description--

Для __добавления__ или __изменения__ значений в словаре мы также можем использовать метод `updateValue(_:forKey:)`

# --instructions--

Обновите ключ `age` значением `20` в словаре

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let [/] = [/][/][/]([/], forKey: [/]) {
    print("The old age was \(oldAge)")
}
print(user)
```

# --answers--

- update
- add
- insert
- user
- .
- updateValue
- 20
- "age"
- oldAge

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let oldAge = user.updateValue(20, forKey: "age") {
    print("The old age was \(oldAge)")
}
print(user)
```

# --output--

The old age was 30
["age": 20, "first_name": "John", "last_name": "Hood"]
