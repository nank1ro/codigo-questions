---
language: swift
exerciseType: 2
---

# --description--

Когда класс имеет собственные функции, эти функции называются __методами__.

# --instructions--

Добавьте метод `description` в ваш класс `Animal`.
Используя два отдельных оператора `print`, он должен выводить `gender` и `legs` животного, для которого вызывается.
Затем вызовите метод `description`.

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4

    [/][/]() {
        if let gender = self.gender { print(gender) }
        print([/].legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.[/]
```

# --answers--

-  description
- func
- description()
- self
- description[]
- function
- method

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4

    func description() {
        if let gender = self.gender { print(gender) }
        print(self.legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.description()
```

# --output--

female
4
