---
language: swift
exerciseType: 1
---

# --description--

`async let` привязан к области видимости, в которой он написан. Чтобы запустить параллельную работу и сохранить её дескриптор, используйте **`Task`**. Замыкание, переданное в `Task { }`, выполняется само по себе, а задачу можно сохранить, передать или вернуть:
```swift
let job = Task {
    return await double(21)
}
```
Результат позже читается через **`.value`**, к которому применяется `await`:
```swift
print(await job.value)
// prints 42
```
Тип дескриптора говорит, что он создаёт и что может бросить: `Task<Int, Never>` — это задача, которая возвращает `Int` и никогда не бросает. В отличие от `async let`, `Task` можно создать из обычного, не асинхронного кода.

# --instructions--

Напишите функцию `startDoubling`, которая принимает целое число (без метки аргумента) и возвращает `Task<Int, Never>`, создающий удвоенное число

Пример вызова функции:
```swift
print(await startDoubling(4).value)
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func startDoubling() {

}
```

# --asserts--

Функция должна создавать `Task`

```json
{
  "regex": "Task\\s*\\{",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`startDoubling(4).value` должно быть равно `8`

```swift
tryCatch(await startDoubling(4).value == 8)
```

`startDoubling(0).value` должно быть равно `0`

```swift
tryCatch(await startDoubling(0).value == 0)
```

`startDoubling(-6).value` должно быть равно `-12`

```swift
tryCatch(await startDoubling(-6).value == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func startDoubling(_ n: Int) -> Task<Int, Never> {
    return Task {
        return n * 2
    }
}
```
