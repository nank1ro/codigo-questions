---
language: swift
exerciseType: 2
---

# --description--

Когда вам неважно, *почему* вызов завершился ошибкой, `try?` короче, чем блок `do`. Он превращает бросающий вызов в **опциональное значение**: значение при успехе, `nil` при неудаче. В сочетании с `await` это записывается как `try? await`:
```swift
let value = try? await parse("42")  // Optional(42)
let broken = try? await parse("x")  // nil
```
Поскольку результат опциональный, он сразу подходит для `if let`:
```swift
if let value = try? await parse("x") {
    print(value)
} else {
    print("not a number")
}
```
Используйте `try? await` для быстрого запасного варианта, а `do` / `catch` — когда важна сама ошибка.

# --instructions--

Дополните код так, чтобы неудавшийся вызов давал `nil`, а программа выводила `not a number`

# --seed--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = [/] await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --answers--

- try?
- try
- try!
- await
- throws

# --solutions--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = try? await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --output--

not a number
