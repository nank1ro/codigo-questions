---
language: swift
exerciseType: 4
---

# --description--

Несколько вызовов с `await`, записанных один за другим, выполняются **последовательно**: второй вызов даже не начнётся, пока первый не вернёт результат. Код читается сверху вниз, в точности как обычный код:
```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}

let a = await step("A")
print(a)
let b = await step("B")
print(b)
// start A
// done A
// start B
// done B
```
Это то, что нужно, когда второй вызов требует результат первого. Когда вызовы независимы, ожидание одного перед запуском другого — потерянное время, и следующие упражнения показывают, как этого избежать.

# --instructions--

Расположите строки так, чтобы вывести результат ниже, определив `step` перед кодом верхнего уровня
```
start A
done A
start B
done B
```

# --answers--

```swift
func step(_ name: String) async -> String {
```

```swift
    print("start \(name)")
```

```swift
    return "done \(name)"
```

```swift
}
```

```swift
let a = await step("A")
```

```swift
print(a)
```

```swift
let b = await step("B")
```

```swift
print(b)
```

# --solutions--

```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}
let a = await step("A")
print(a)
let b = await step("B")
print(b)
```

# --output--

start A
done A
start B
done B
