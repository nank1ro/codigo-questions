---
language: swift
exerciseType: 1
---

# --description--

Асинхронная функция остаётся обычной функцией: она может принимать параметры и возвращать значение любого типа. Меняются только две вещи: ключевое слово `async` в сигнатуре и `await` в каждом месте вызова:
```swift
func price(of quantity: Int) async -> Double {
    return Double(quantity) * 2.5
}

let total = await price(of: 4)
print(total)
// выводит 10.0
```
Возвращаемое значение — обычный `Double`, а не обёртка: когда `await` завершён, вы работаете с ним в точности как обычно.

# --instructions--

Напишите функцию `double`, которая принимает целое число (без метки аргумента), является асинхронной и возвращает удвоенное число

Пример вызова функции:
```swift
print(await double(4))
// выводит 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func double() {

}
```

# --asserts--

Функция должна быть помечена как `async`

```json
{
  "regex": "func\\s+double\\s*\\([^)]*\\)\\s*async",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`double(4)` должна вернуть `8`

```swift
tryCatch(await double(4) == 8)
```

`double(0)` должна вернуть `0`

```swift
tryCatch(await double(0) == 0)
```

`double(-7)` должна вернуть `-14`

```swift
tryCatch(await double(-7) == -14)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func double(_ n: Int) async -> Int {
    return n * 2
}
```
