---
language: swift
exerciseType: 1
---

# --description--

Чтобы запустить два независимых вызова одновременно, объявите их с помощью **`async let`**. Работа начинается сразу, и программа продолжает выполняться, не ожидая:
```swift
async let left = step("A")
async let right = step("B")
```
Значения ещё нет, поэтому использовать привязку напрямую нельзя: `await` ставится там, где значение наконец понадобилось. Один `await` перед выражением покрывает все `async let` внутри него:
```swift
let both = await left + right
```
Если каждый вызов занимает одну секунду, последовательной версии нужны две секунды, а версии с `async let` — около одной, потому что два вызова перекрываются.

# --instructions--

Напишите функцию `total`, которая принимает два целых числа (без меток аргументов), является асинхронной и возвращает сумму результатов `slowValue` для обоих, запуская два вызова параллельно с `async let`

Пример вызова функции:
```swift
print(await total(1, 2))
// выводит 30
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total() {

}
```

# --asserts--

Функция должна использовать `async let`

```json
{
  "regex": "async\\s+let",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`total(1, 2)` должна вернуть `30`

```swift
tryCatch(await total(1, 2) == 30)
```

`total(0, 0)` должна вернуть `0`

```swift
tryCatch(await total(0, 0) == 0)
```

`total(5, -2)` должна вернуть `30`

```swift
tryCatch(await total(5, -2) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total(_ a: Int, _ b: Int) async -> Int {
    async let first = slowValue(a)
    async let second = slowValue(b)
    return await first + second
}
```
