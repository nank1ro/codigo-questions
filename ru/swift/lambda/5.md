---
language: swift
exerciseType: 1
---

# --description--

Когда замыкание является **последним** аргументом функции, его можно записать после закрывающей скобки вызова. Это синтаксис **завершающего замыкания** (trailing closure):
```swift
print(apply(5) { $0 + 1 }) // 6
```
Если замыкание — единственный аргумент, скобки можно опустить полностью:
```swift
func run(_ task: () -> Int) -> Int {
    return task()
}
print(run { 42 }) // 42
```
Обе формы вызывают ровно одну и ту же функцию, завершающий синтаксис просто легче читать, когда замыкание длинное.

# --instructions--

Напишите функцию `applyTwice`, которая принимает `Int` и замыкание типа `(Int) -> Int` (опустите обе метки аргументов) и возвращает результат применения замыкания к числу два раза

Пример вызова функции:
```swift
print(applyTwice(3) { $0 * 2 })
// prints 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`applyTwice(3) { $0 * 2 }` должна вернуть `12`

```swift
tryCatch(applyTwice(3) { $0 * 2 } == 12)
```

`applyTwice(1) { $0 + 10 }` должна вернуть `21`

```swift
tryCatch(applyTwice(1) { $0 + 10 } == 21)
```

`applyTwice(2, { $0 * $0 })` должна вернуть `16`

```swift
tryCatch(applyTwice(2, { $0 * $0 }) == 16)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func applyTwice(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(operation(n))
}
```
