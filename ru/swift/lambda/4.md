---
language: swift
exerciseType: 2
---

# --description--

Поскольку замыкания являются значениями, функция может принимать одно из них в качестве параметра. Тип параметра — это просто тип функции:
```swift
func apply(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(n)
}
print(apply(5, { $0 + 1 })) // 6
```
Функция `apply` не знает, что делает `operation`, ей известно только, что та принимает `Int` и возвращает `Int`, и она вызывает её как любую другую функцию.

# --instructions--

Дополните функцию `apply` так, чтобы её вторым параметром было замыкание, принимающее `Int` и возвращающее `Int`, а сама функция возвращала результат вызова этого замыкания на `n`, чтобы программа вывела `12`

# --seed--

```swift
func apply(_ n: Int, _ operation: [/]) -> Int {
    return [/]
}
print(apply(6, { $0 * 2 }))
```

# --answers--

- (Int) -> Int
- operation(n)
- Int
- (Int) -> Void
- operation
- n

# --solutions--

```swift
func apply(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(n)
}
print(apply(6, { $0 * 2 }))
```

# --output--

12
