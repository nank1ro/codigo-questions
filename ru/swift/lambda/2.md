---
language: swift
exerciseType: 1
---

# --description--

Указывать все типы внутри замыкания часто не нужно. Когда константа имеет явный **тип функции**, Swift выводит типы параметров и возвращаемого значения, поэтому перед `in` достаточно перечислить только имена параметров:
```swift
let triple: (Int) -> Int = { n in
    return n * 3
}
```
Тип `(Int) -> Int` читается как "функция, которая принимает `Int` и возвращает `Int`".
Когда тело состоит из одного выражения, ключевое слово `return` тоже можно опустить, это называется **неявным возвратом**:
```swift
let triple: (Int) -> Int = { n in n * 3 }
print(triple(4)) // 12
```

# --instructions--

Сохраните в константе `square` замыкание типа `(Int) -> Int`, которое возвращает свой аргумент, умноженный на самого себя

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Необходимо использовать выражение замыкания, а не `func`

```json
{
  "regex": "func\\s+square",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`square(4)` должна вернуть `16`

```swift
tryCatch(square(4) == 16)
```

`square(-3)` должна вернуть `9`

```swift
tryCatch(square(-3) == 9)
```

`square(0)` должна вернуть `0`

```swift
tryCatch(square(0) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let square: (Int) -> Int = { n in n * n }
```
