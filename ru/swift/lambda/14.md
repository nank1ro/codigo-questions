---
language: swift
exerciseType: 3
---

# --description--

Каждый вызов функции, возвращающей замыкание, создаёт **новую** захваченную переменную. Два замыкания, созданные разными вызовами, не разделяют своё состояние:
```swift
let first = makeCounter()
let second = makeCounter()
print(first())  // 1
print(first())  // 2
print(second()) // 1
```
Состояние разделяется только между вызовами одного и того же замыкания.

# --instructions--

Что напечатает этот код? (`makeCounter` — функция из предыдущих упражнений)
```swift
let a = makeCounter()
let b = makeCounter()
_ = a()
_ = a()
_ = b()
print(a() + b())
```

# --answers--

- 5
- 6
- 4
- 2

# --solutions--

- 5
