---
language: swift
exerciseType: 4
---

# --description--

Замыкания особенно хороши в методах массивов, принимающих их в качестве аргумента. `map` вызывает замыкание для каждого элемента и возвращает новый массив с результатами:
```swift
let nums = [1, 2, 3]
let doubled = nums.map { $0 * 2 }
print(doubled) // [2, 4, 6]
```
Исходный массив не изменяется. Поскольку `map` принимает единственный аргумент-замыкание, синтаксис завершающего замыкания — обычный способ его вызова.

# --instructions--

Расположите элементы в правильном порядке, чтобы с помощью `map` построить массив из квадратов `[1, 2, 3, 4]` и вывести его

# --answers--

```swift
let nums = [1, 2, 3, 4]
```

```swift
let squares = nums.map { $0 * $0 }
```

```swift
print(squares)
```

# --solutions--

```swift
let nums = [1, 2, 3, 4]
let squares = nums.map { $0 * $0 }
print(squares)
```

# --output--

[1, 4, 9, 16]
