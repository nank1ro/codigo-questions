---
language: swift
exerciseType: 1
---

# --description--

`filter` оставляет только те элементы, для которых замыкание возвращает `true`. Замыкание получает один элемент и должно возвращать `Bool`:
```swift
let nums = [5, 12, 8, 20]
let big = nums.filter { $0 > 10 }
print(big) // [12, 20]
```
Элементы сохраняют свой исходный порядок, а результатом является новый массив того же типа элементов.

# --instructions--

Напишите функцию `evens`, которая принимает массив `Int` (опустите метку аргумента) и возвращает новый массив только с чётными числами, используя `filter`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Необходимо использовать `filter`

```json
{
  "regex": "\\.filter",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`evens([1, 2, 3, 4, 5, 6])` должна вернуть `[2, 4, 6]`

```swift
tryCatch(evens([1, 2, 3, 4, 5, 6]) == [2, 4, 6])
```

`evens([7, 9])` должна вернуть `[]`

```swift
tryCatch(evens([7, 9]) == [])
```

`evens([0, -2, 3])` должна вернуть `[0, -2]`

```swift
tryCatch(evens([0, -2, 3]) == [0, -2])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func evens(_ nums: [Int]) -> [Int] {
    return nums.filter { $0 % 2 == 0 }
}
```
