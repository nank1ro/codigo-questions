---
language: swift
exerciseType: 2
---

# --description--

Вы можете использовать тип кортежа в качестве возвращаемого типа функции, чтобы вернуть несколько значений как часть одного составного возвращаемого значения.

# --instructions--

Дополните код, чтобы создать корректную функцию `minMax`, передав следующий массив чисел `[15, 24, 31, 79, 44]`

# --seed--

```swift
func minMax(array: [Int]) -> [/]min: Int, max: Int[/] {
    var currentMin = array[0]
    var currentMax = array[0]
    array.forEach { num in
        if num < currentMin {
            currentMin = num
        } else if num > currentMax {
            currentMax = num
        }
    }
    return [/][/], [/][/]
}
let numbers: [Int] = [15, 24, 31, 79, 44]
print(minMax([/]: numbers))
```

# --answers--

- (
- )
- [
- ]
- {
- }
- (
- )
- array
- numbers
- currentMin
- currentMax

# --solutions--

```swift
func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    array.forEach { num in
        if num < currentMin {
            currentMin = num
        } else if num > currentMax {
            currentMax = num
        }
    }
    return (currentMin, currentMax)
}
let numbers: [Int] = [15, 24, 31, 79, 44]
print(minMax(array: numbers))
```

# --output--

(min: 15, max: 79)
