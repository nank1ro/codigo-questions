---
language: swift
exerciseType: 2
---

# --description--

Вы можете определить значение _по умолчанию_ для любого параметра функции, присвоив значение параметру после указания его типа.
Если значение по умолчанию определено, вы можете опустить этот параметр при вызове функции
```swift
func someFunction(parameterWithoutDefault: Int, parameterWithDefault: Int = 12) {
    // do stuff here
}
```

# --instructions--

Дополните код, чтобы вызвать функцию без аргумента _по умолчанию_

# --seed--

```swift
func sayHello(_ name: String[/][/]) {
    print("Hello \(name)!")
}
[/]()
```

# --answers--

-  = 
- "World"
-  == 
- sayHello

# --solutions--

```
func sayHello(_ name: String = "World") {
    print("Hello \(name)!")
}
sayHello()
```

# --output--

Hello World!
