---
language: swift
exerciseType: 1
---

# --description--

Ещё один флаг — `0`: вместо пробелов поле заполняется нулями слева. Так получаются числа вроде `007` или `00042`:
```swift
print(String(format: "%05d", 42))  // 00042
print(String(format: "%03d", 7))   // 007
print(String(format: "%03d", 1234)) // 1234
```
Как и с пробелами, значение длиннее ширины никогда не обрезается.

# --instructions--

Напишите функцию `ticket`, которая принимает целое число (без метки аргумента) и возвращает `T-` с числом, дополненным нулями до ширины `5`, используя `String(format:)`

Пример вызова функции:
```swift
print(ticket(42))
// prints T-00042
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func ticket() {

}
```

# --asserts--

Функция должна использовать `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ticket(42)` должна вернуть `"T-00042"`

```swift
tryCatch(ticket(42) == "T-00042")
```

`ticket(7)` должна вернуть `"T-00007"`

```swift
tryCatch(ticket(7) == "T-00007")
```

`ticket(12345)` должна вернуть `"T-12345"`

```swift
tryCatch(ticket(12345) == "T-12345")
```

`ticket(123456)` должна вернуть `"T-123456"`

```swift
tryCatch(ticket(123456) == "T-123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func ticket(_ n: Int) -> String {
    return String(format: "T-%05d", n)
}
```
