---
language: swift
exerciseType: 4
---

# --description--

Соедините оба инструмента, чтобы вывести таблицу: `padding` выравнивает текст каждой строки по левому краю, а `String(format:)` выравнивает числа по правому краю с фиксированной шириной и точностью:
```swift
import Foundation

let items = [("Tea", 2.5), ("Cake", 12.0)]
for (name, price) in items {
    let label = name.padding(toLength: 6, withPad: ".", startingAt: 0)
    print(label + String(format: "%6.2f", price))
}
// Tea...  2.50
// Cake.. 12.00
```
Поскольку каждая строка имеет одинаковую ширину, десятичные точки оказываются в одном столбце.

# --instructions--

Расставьте элементы по порядку, чтобы вывести таблицу ниже, начиная с `import Foundation` и вычисляя `label` до её вывода
```
Tea...  2.50
Cake.. 12.00
```

# --answers--

```swift
import Foundation
```

```swift
let items = [("Tea", 2.5), ("Cake", 12.0)]
```

```swift
for (name, price) in items {
```

```swift
    let label = name.padding(toLength: 6, withPad: ".", startingAt: 0)
```

```swift
    print(label + String(format: "%6.2f", price))
```

```swift
}
```

# --solutions--

```swift
import Foundation
let items = [("Tea", 2.5), ("Cake", 12.0)]
for (name, price) in items {
    let label = name.padding(toLength: 6, withPad: ".", startingAt: 0)
    print(label + String(format: "%6.2f", price))
}
```

# --output--

Tea...  2.50
Cake.. 12.00
