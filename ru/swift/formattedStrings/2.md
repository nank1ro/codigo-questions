---
language: swift
exerciseType: 1
---

# --description--

Второй инструмент — `String(format:)`, который входит в фреймворк **Foundation**, поэтому файл должен начинаться с `import Foundation`.

Он принимает **строку формата**, за которой следуют значения для вставки. Внутри строки формата **спецификатор**, начинающийся с `%`, отмечает, куда подставляется каждое значение и как оно записывается. Спецификатор для целого числа — `%d`:
```swift
import Foundation

let count = 7
let text = String(format: "Item %d", count)
print(text) // Item 7
```
`String(format:)` возвращает обычную `String`, поэтому её можно вывести, сохранить или вернуть из функции.

# --instructions--

Напишите функцию `label`, которая принимает целое число (без метки аргумента) и возвращает текст `Item ` с числом, используя `String(format:)` с `%d`

Пример вызова функции:
```swift
print(label(7))
// prints Item 7
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func label() {

}
```

# --asserts--

Функция должна использовать `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`label(7)` должна вернуть `"Item 7"`

```swift
tryCatch(label(7) == "Item 7")
```

`label(42)` должна вернуть `"Item 42"`

```swift
tryCatch(label(42) == "Item 42")
```

`label(0)` должна вернуть `"Item 0"`

```swift
tryCatch(label(0) == "Item 0")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func label(_ n: Int) -> String {
    return String(format: "Item %d", n)
}
```
