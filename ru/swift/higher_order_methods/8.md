---
language: swift
exerciseType: 2
---

# --description--

Когда замыкание только читает одно свойство, вместо него можно передать **ключевой путь**: `\.name` означает «свойство `name` элемента», а `map(\.name)` — это то же самое, что `map { $0.name }`.
Сортировка по свойству использует обычное замыкание с двумя аргументами, сравнивающее это свойство у обоих элементов:
```swift
struct City {
    var name: String
    var population: Int
}
let cities = [City(name: "Oslo", population: 700), City(name: "Rome", population: 2800)]
let byPopulation = cities.sorted { $0.population > $1.population }
print(byPopulation.map(\.name)) // ["Rome", "Oslo"]
```

# --instructions--

Дополните код так, чтобы отсортировать игроков от наибольшего к наименьшему `score` и вывести их имена с помощью ключевого пути, чтобы программа вывела `["Mia", "Leo", "Zoe"]`

# --seed--

```swift
struct Player {
    var name: String
    var score: Int
}
let players = [Player(name: "Leo", score: 40), Player(name: "Mia", score: 75), Player(name: "Zoe", score: 12)]
let ranking = players.sorted { $0.score [/] $1.score }
print(ranking.map([/]))
```

# --answers--

- >
- \.name
- <
- \.score
- $0.name

# --solutions--

```swift
struct Player {
    var name: String
    var score: Int
}
let players = [Player(name: "Leo", score: 40), Player(name: "Mia", score: 75), Player(name: "Zoe", score: 12)]
let ranking = players.sorted { $0.score > $1.score }
print(ranking.map(\.name))
```

# --output--

["Mia", "Leo", "Zoe"]
