---
language: swift
exerciseType: 1
---

# --description--

`map` и `filter` на массиве **энергичны**: каждый обрабатывает весь массив и строит новый, прежде чем выполнится следующий шаг. На большой коллекции, или когда нужен только первый результат, это напрасная работа.
Свойство `lazy` возвращает представление, операции которого выполняются только тогда, когда элемент реально запрошен, по одному элементу за раз через всю цепочку:
```swift
let firstBig = (1...1000).lazy.map { $0 * $0 }.first { $0 > 50 }
print(firstBig!) // 64
```
Здесь возводятся в квадрат только `1, 2, ..., 8`: `first(where:)` запрашивает элементы, пока один не удовлетворит условию, и цепочка останавливается там. Без `lazy`, `map` сначала возвела бы в квадрат все 1000 чисел.

# --instructions--

Напишите функцию `firstCubeAbove`, которая принимает `Int` с именем `limit` (опустите метку аргумента) и возвращает наименьший куб (`n * n * n` для `n`, начиная с `1`), больший чем `limit`, используя `lazy` и `map` на диапазоне `1...limit + 1` и `first(where:)`

Пример вызова функции:
```swift
print(firstCubeAbove(10))
// prints 27
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Необходимо использовать `lazy`

```json
{
  "regex": "\\.lazy",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Нельзя использовать цикл

```json
{
  "regex": "\\b(for|while)\\b",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`firstCubeAbove(10)` должна вернуть `27`

```swift
tryCatch(firstCubeAbove(10) == 27)
```

`firstCubeAbove(27)` должна вернуть `64`

```swift
tryCatch(firstCubeAbove(27) == 64)
```

`firstCubeAbove(0)` должна вернуть `1`

```swift
tryCatch(firstCubeAbove(0) == 1)
```

`firstCubeAbove(1000000)` должна вернуть `1030301`

```swift
tryCatch(firstCubeAbove(1000000) == 1030301)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstCubeAbove(_ limit: Int) -> Int {
    return (1...limit + 1).lazy.map { $0 * $0 * $0 }.first(where: { $0 > limit })!
}
```
