---
language: swift
exerciseType: 1
---

# --description--

Функции высшего порядка работают с любым массивом, включая массивы ваших собственных структур. Цепочка из `filter`, а затем `map` — обычный способ выбрать некоторые элементы и извлечь значение из каждого:
```swift
struct Book {
    var title: String
    var pages: Int
}
let books = [Book(title: "Dune", pages: 412), Book(title: "Haiku", pages: 40)]
let long = books.filter { $0.pages > 100 }.map { $0.title }
print(long) // ["Dune"]
```
Сделав это в обратном порядке — сначала `map`, затем `filter`, — вы потеряли бы свойство `pages` до того, как проверка смогла бы его использовать.

# --instructions--

Структура `Product` дана. Напишите функцию `cheapNames`, которая принимает массив `Product` (опустите метку аргумента) и возвращает названия продуктов, у которых `price` ниже `10`, в исходном порядке, соединяя `filter` и `map` в цепочку

Пример вызова функции:
```swift
print(cheapNames([Product(name: "Pen", price: 2.5), Product(name: "Bag", price: 30)]))
// prints ["Pen"]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Product {
    var name: String
    var price: Double
}
```

# --asserts--

Необходимо использовать `filter`

```json
{
  "regex": "\\.filter",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Необходимо использовать `map`

```json
{
  "regex": "\\.map",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`cheapNames` должна вернуть `["Pen"]` для ручки по цене `2.5` и сумки по цене `30`

```swift
tryCatch(cheapNames([Product(name: "Pen", price: 2.5), Product(name: "Bag", price: 30)]) == ["Pen"])
```

`cheapNames` должна сохранять исходный порядок

```swift
tryCatch(cheapNames([Product(name: "Tea", price: 4), Product(name: "Cup", price: 9.99), Product(name: "Pot", price: 25)]) == ["Tea", "Cup"])
```

Продукт с ценой ровно `10` должен быть исключён

```swift
tryCatch(cheapNames([Product(name: "Cap", price: 10)]) == [])
```

`cheapNames([])` должна вернуть `[]`

```swift
tryCatch(cheapNames([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Product {
    var name: String
    var price: Double
}
func cheapNames(_ products: [Product]) -> [String] {
    return products.filter { $0.price < 10 }.map { $0.name }
}
```
