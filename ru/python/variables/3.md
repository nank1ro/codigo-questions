---
language: python
exerciseType: 1
---

# --description--

Мы также можем присвоить переменным значения других переменных. Здесь мы присваиваем переменной `y` значение `x`:
```python
>>> x = 5
>>> y = x
>>> print(y)
5
```

# --instructions--

Создайте новую переменную с именем `y` и присвойте ей значение переменной `x`.

# --seed--

```python
x = 1
# write after this line
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`y` должно быть равно `x`.

```python
    def test_1(self):
        self.assertEqual(x, y, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
x = 1
y = x
```
