---
language: python
exerciseType: 1
---

# --description--

Объект match несёт больше информации, чем совпавший текст. Помимо `.group()` он предоставляет позицию совпадения внутри исходной строки:
```python
import re

match = re.search(r"\d+", "order 42 shipped")
print(match.group())  # 42
print(match.start())  # 6
print(match.end())    # 8
print(match.span())   # (6, 8)
```
`.start()` — это индекс первого совпавшего символа, `.end()` — индекс сразу после последнего, а `.span()` возвращает оба числа в виде кортежа. Это значит, что `text[match.start():match.end()]` всегда равен `match.group()`.

Поскольку `re.search` может вернуть `None`, чтение `.group()` сразу же вызовет `AttributeError`, когда совпадения не было; сначала проверьте результат.

# --instructions--

Напишите функцию `first_number(text)`, которая возвращает кортеж из первой последовательности цифр, её начального индекса и её конечного индекса, или `None`, когда в тексте нет цифр

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_number("order 42 shipped")` должна возвращать `("42", 6, 8)`

```python
    def test_1(self):
        self.assertEqual(first_number("order 42 shipped"), ("42", 6, 8), "--err-t1--")
```

Совпадение должно начинаться с индекса `0`, когда текст начинается с цифр

```python
    def test_2(self):
        self.assertEqual(first_number("7up"), ("7", 0, 1), "--err-t2--")
        self.assertEqual(first_number("100 items"), ("100", 0, 3), "--err-t2--")
```

`first_number` должна возвращать `None`, когда цифр нет

```python
    def test_3(self):
        self.assertIsNone(first_number("no digits here"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def first_number(text):
    match = re.search(r"\d+", text)
    if match is None:
        return None
    return (match.group(), match.start(), match.end())
```
