---
language: python
exerciseType: 1
---

# --description--

Считать скобки, чтобы найти группу `3`, быстро надоедает. Группе можно дать имя с помощью `(?P<name>...)`, а затем прочитать её через `match.group("name")`:
```python
import re

match = re.search(r"(?P<hour>\d\d):(?P<minute>\d\d)", "starts at 09:30")
print(match.group("hour"))    # 09
print(match.group("minute"))  # 30
print(match.groupdict())      # {'hour': '09', 'minute': '30'}
```
`match.groupdict()` возвращает все именованные группы в виде словаря. Именованные группы сохраняют и свой номер, поэтому `match.group(1)` по-прежнему работает.

В примере также используется **квантификатор** с фигурными скобками: `\d{2}` означает ровно две цифры, `\d{2,4}` — от двух до четырёх, а `\d{2,}` — две и более. Это точная версия `+` (один или более), `*` (ноль или более) и `?` (ноль или один).

# --instructions--

Напишите функцию `parse_date(text)`, которая находит дату в виде четырёх цифр, дефиса, двух цифр, дефиса и двух цифр и возвращает словарь с ключами `year`, `month` и `day`, или `None`, когда такой даты нет

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`parse_date` должна читать все три части даты

```python
    def test_1(self):
        self.assertEqual(parse_date("due 2026-09-13 ok"), {"year": "2026", "month": "09", "day": "13"}, "--err-t1--")
```

`parse_date` должна работать, когда дата — весь текст целиком

```python
    def test_2(self):
        self.assertEqual(parse_date("1999-12-31"), {"year": "1999", "month": "12", "day": "31"}, "--err-t2--")
```

`parse_date` должна возвращать `None`, когда даты нет

```python
    def test_3(self):
        self.assertIsNone(parse_date("no date here"), "--err-t3--")
        self.assertIsNone(parse_date("26-09-13"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def parse_date(text):
    match = re.search(r"(?P<year>\d{4})-(?P<month>\d{2})-(?P<day>\d{2})", text)
    if match is None:
        return None
    return match.groupdict()
```
