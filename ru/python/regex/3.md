---
language: python
exerciseType: 3
---

# --description--

Есть и третий вход в библиотеку — `re.fullmatch`, который завершается успехом, только когда шаблон покрывает **весь** текст от первого символа до последнего. Это правильный инструмент для валидации:
```python
import re

print(bool(re.fullmatch(r"\d+", "2026")))    # True
print(bool(re.fullmatch(r"\d+", "2026!")))   # False
```

Таким образом, три функции различаются лишь тем, где шаблону позволено находиться: `re.match` — в начале текста, `re.search` — в любом месте текста, а `re.fullmatch` — по всему тексту целиком.

# --instructions--

Что выведет этот код?
```python
import re

print(bool(re.match(r"\d+", "12abc")), bool(re.search(r"\d+", "12abc")), bool(re.fullmatch(r"\d+", "12abc")))
```

# --answers--

- `True True False`, потому что цифры есть в начале и внутри текста, но они не покрывают весь текст
- `True True True`, потому что все три функции завершаются успехом, как только шаблон найден где угодно
- `False True False`, потому что `re.match` требует, чтобы шаблон покрывал весь текст
- `True False False`, потому что `re.search` смотрит только после первого символа

# --solutions--

- `True True False`, потому что цифры есть в начале и внутри текста, но они не покрывают весь текст
