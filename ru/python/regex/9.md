---
language: python
exerciseType: 1
---

# --description--

Когда нужны позиция или группы каждого совпадения, правильный выбор — `re.finditer(pattern, text)`: он проходит по тексту и выдаёт **объект match** для каждого совпадения, по одному за раз:
```python
import re

for match in re.finditer(r"\d+", "a1 bb22"):
    print(match.group(), match.start())
# 1 1
# 22 5
```
`finditer` создаёт итератор, а не список, поэтому его можно использовать в цикле `for` или в генераторе. Там, где `findall` даёт только текст, `finditer` даёт всё, что знает объект match.

# --instructions--

Напишите функцию `word_positions(text)`, которая возвращает список кортежей — по одному для каждой последовательности букв в тексте, — содержащий слово и индекс, с которого оно начинается

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`word_positions("hi there")` должна возвращать `[("hi", 0), ("there", 3)]`

```python
    def test_1(self):
        self.assertEqual(word_positions("hi there"), [("hi", 0), ("there", 3)], "--err-t1--")
```

Цифры и символы не должны быть частью слова

```python
    def test_2(self):
        self.assertEqual(word_positions("a1b"), [("a", 0), ("b", 2)], "--err-t2--")
        self.assertEqual(word_positions("Ada, Bob"), [("Ada", 0), ("Bob", 5)], "--err-t2--")
```

Текст без букв должен возвращать пустой список

```python
    def test_3(self):
        self.assertEqual(word_positions("123 456"), [], "--err-t3--")
        self.assertEqual(word_positions(""), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def word_positions(text):
    return [(match.group(), match.start()) for match in re.finditer(r"[a-zA-Z]+", text)]
```
