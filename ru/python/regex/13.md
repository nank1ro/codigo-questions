---
language: python
exerciseType: 1
---

# --description--

`str.split` может разрезать строку только по фиксированному разделителю. `re.split(pattern, text)` режет по всему, что описывает шаблон, — именно это обычно нужно для неаккуратного ввода:
```python
import re

print("a, b;c".split(","))            # ['a', ' b;c']
print(re.split(r"[,;]", "a, b;c"))    # ['a', ' b', 'c']
print(re.split(r"[,;\s]+", "a, b;c")) # ['a', 'b', 'c']
```
Запись разделителя как `[,;\s]+` заставляет целую последовательность запятых, точек с запятой и пробелов считаться одним разрезом, а не оставлять пустые строки между ними.

Аргумент `maxsplit` останавливается после заданного числа разрезов, оставляя остаток текста в последнем элементе: `re.split(r"\s+", "a b c", maxsplit=1)` даёт `['a', 'b c']`.

# --instructions--

Напишите функцию `split_fields(line)`, которая возвращает список полей строки, считая любую последовательность запятых, точек с запятой и пробелов одним разделителем

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`split_fields` должна разрезать по запятым, точкам с запятой и пробелам

```python
    def test_1(self):
        self.assertEqual(split_fields("a, b;c"), ["a", "b", "c"], "--err-t1--")
        self.assertEqual(split_fields("red green blue"), ["red", "green", "blue"], "--err-t1--")
```

Последовательность разделителей должна давать один разрез

```python
    def test_2(self):
        self.assertEqual(split_fields("x ; ; y"), ["x", "y"], "--err-t2--")
        self.assertEqual(split_fields("1,,2"), ["1", "2"], "--err-t2--")
```

Строка без разделителей должна возвращать одно поле

```python
    def test_3(self):
        self.assertEqual(split_fields("solo"), ["solo"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def split_fields(line):
    return re.split(r"[,;\s]+", line)
```
