---
language: python
exerciseType: 2
---

# --description--

Часто точные значения не важны: нужно лишь, чтобы элементы различались.
В этом случае вы можете позволить Python выбрать значения с помощью `auto()`, который также импортируется из модуля `enum`.
Он присваивает `1` первому элементу, а затем увеличивает счётчик:
```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    SOUTH = auto()
    EAST = auto()

print(Direction.EAST.value)  # 3
```

# --instructions--

Дополните код, чтобы импортировать `auto` и вывести значение элемента `LARGE` перечисления `Size`

# --seed--

```python
from enum import Enum, [/]

class Size(Enum):
    SMALL = auto()
    MEDIUM = auto()
    LARGE = auto()

print(Size.LARGE.[/])
```

# --answers--

- auto
- value
- name
- Enum
- unique
- index

# --solutions--

```python
from enum import Enum, auto

class Size(Enum):
    SMALL = auto()
    MEDIUM = auto()
    LARGE = auto()

print(Size.LARGE.value)
```

# --output--

3
