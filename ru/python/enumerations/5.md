---
language: python
exerciseType: 1
---

# --description--

Вы можете получить элемент по его **значению**, вызвав класс как функцию, или по его **имени**, используя квадратные скобки:
```python
print(Color(2))        # Color.GREEN
print(Color["BLUE"])   # Color.BLUE
```
Оба способа удобны, когда значение или имя приходят извне программы, например из файла или пользовательского ввода.
Если ничего не совпадает, `Color(9)` вызывает `ValueError`, а `Color["PINK"]` вызывает `KeyError`.

# --instructions--

Сохраните в переменной `by_value` элемент `Status`, значение которого равно `code`, вызвав `Status(...)`, а в `by_name` — элемент, имя которого равно `label`, используя `Status[...]`

# --seed--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо вызвать `Status(...)` с `code`

```json
{
  "regex": "Status\\(\\s*code\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Необходимо использовать `Status[...]` с `label`

```json
{
  "regex": "Status\\[\\s*label\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`by_value` должно быть `Status.DONE`

```python
    def test_1(self):
        self.assertIs(by_value, Status.DONE, "--err-t1--")
```

`by_name` должно быть `Status.FAILED`

```python
    def test_2(self):
        self.assertIs(by_name, Status.FAILED, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
by_value = Status(code)
by_name = Status[label]
```
