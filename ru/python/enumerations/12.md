---
language: python
exerciseType: 4
---

# --description--

Перечисления естественно сочетаются с инструкцией `match` (доступной начиная с Python 3.10), которая сравнивает значение с серией шаблонов `case` и выполняет первый совпавший:
```python
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
```
Всегда записывайте элемент с его классом, например `Light.RED`: голое имя, такое как `case RED:`, ничего не сравнит, оно просто захватит значение в новую переменную `RED` и совпадёт со всем.
Подстановочный `case _:` — это вариант по умолчанию, и он должен идти последним, потому что любой шаблон после него никогда не будет достигнут.

# --instructions--

Расположите элементы в правильном порядке, чтобы объявить перечисление `Light`, определить `action()` сначала со случаем `RED`, затем со случаем `GREEN`, затем с вариантом по умолчанию, и вывести действие для `Light.YELLOW`

# --answers--

```python
from enum import Enum
```

```python
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
```

```python
def action(light):
```

```python
    match light:
```

```python
        case Light.RED:
            return "Stop"
```

```python
        case Light.GREEN:
            return "Go"
```

```python
        case _:
            return "Slow down"
```

```python
print(action(Light.YELLOW))
```

# --solutions--

```python
from enum import Enum
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
print(action(Light.YELLOW))
```

# --output--

Slow down
