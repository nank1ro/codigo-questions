---
language: python
exerciseType: 1
---

# --description--

У каждого элемента перечисления есть два атрибута: `name` — идентификатор, который вы написали в классе, и `value` — значение, которое вы ему присвоили:
```python
from enum import Enum

class Color(Enum):
    RED = 1
    GREEN = 2

print(Color.GREEN.name)   # GREEN
print(Color.GREEN.value)  # 2
```
Значение может быть любого типа, не только целого числа: строки, кортежи и числа с плавающей запятой — распространённый выбор.
Элемент — это обычный объект, поэтому вы можете сохранить его в переменную и позже прочитать его атрибуты:
```python
chosen = Color.RED
print(chosen.value)  # 1
```

# --instructions--

Сохраните в переменной `home` элемент `Planet.EARTH`, в `label` — его имя, а в `position` — его значение

# --seed--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо использовать `.name`

```json
{
  "regex": "\\.name\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Необходимо использовать `.value`

```json
{
  "regex": "\\.value\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`home` должно быть `Planet.EARTH`

```python
    def test_1(self):
        self.assertIs(home, Planet.EARTH, "--err-t1--")
```

`label` должно быть равно `"EARTH"`

```python
    def test_2(self):
        self.assertEqual(label, "EARTH", "--err-t2--")
```

`position` должно быть равно `3`

```python
    def test_3(self):
        self.assertEqual(position, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3

home = Planet.EARTH
label = home.name
position = home.value
```
