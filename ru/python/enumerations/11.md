---
language: python
exerciseType: 1
---

# --description--

`Flag` — это перечисление, элементы которого можно **комбинировать**: значение может содержать несколько элементов одновременно, как набор опций.
Объявите его элементы с помощью `auto()`, который для `Flag` присваивает степени двойки (`1`, `2`, `4`, ...), так что каждая комбинация имеет отличающееся значение:
```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```
Используйте `|`, чтобы комбинировать элементы, `in`, чтобы проверить, входит ли элемент в комбинацию, и `value`, чтобы увидеть итоговое число:
```python
full = Permission.READ | Permission.WRITE | Permission.EXECUTE
print(Permission.WRITE in full)  # True
print(full.value)                # 7
print(full)                      # Permission.READ|WRITE|EXECUTE
```

# --instructions--

Сохраните в переменной `editor` комбинацию `Permission.READ` и `Permission.WRITE` с помощью `|`, в `can_write` — входит ли `Permission.WRITE` в `editor`, а в `bits` — значение `editor`

# --seed--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо использовать оператор `|`

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Необходимо использовать оператор `in`

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`editor` должно быть равно `Permission.READ | Permission.WRITE`

```python
    def test_1(self):
        self.assertEqual(editor, Permission.READ | Permission.WRITE, "--err-t1--")
```

`can_write` должно быть `True`

```python
    def test_2(self):
        self.assertEqual(can_write, True, "--err-t2--")
```

`bits` должно быть равно `3`

```python
    def test_3(self):
        self.assertEqual(bits, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()

editor = Permission.READ | Permission.WRITE
can_write = Permission.WRITE in editor
bits = editor.value
```
