---
language: python
exerciseType: 1
---

# --description--

Обычный элемент `Enum` нельзя сравнивать с помощью `<` или прибавлять к числу.
Когда элементы представляют **уровни**, которые нужно упорядочивать, вместо этого наследуйтесь от `IntEnum`: его элементы также являются целыми числами, поэтому они поддерживают сравнения, арифметику и сортировку:
```python
from enum import IntEnum

class Priority(IntEnum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

print(Priority.HIGH > Priority.LOW)  # True
print(Priority.LOW + 1)              # 2
print(int(Priority.MEDIUM))          # 2
```
Элемент `IntEnum` также равен своему целочисленному значению: `Priority.LOW == 1` — это `True`.

# --instructions--

Сохраните в переменной `is_urgent`, больше ли `Priority.HIGH`, чем `Priority.MEDIUM`, в `ordered` — список `tasks`, отсортированный с помощью `sorted()`, а в `score` — сумму `Priority.LOW` и `Priority.HIGH`

# --seed--

```python
from enum import IntEnum

class Priority(IntEnum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

tasks = [Priority.HIGH, Priority.LOW, Priority.MEDIUM]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо использовать `sorted()`

```json
{
  "regex": "sorted\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`is_urgent` должно быть `True`

```python
    def test_1(self):
        self.assertEqual(is_urgent, True, "--err-t1--")
```

`ordered` должно быть равно `[Priority.LOW, Priority.MEDIUM, Priority.HIGH]`

```python
    def test_2(self):
        self.assertEqual(ordered, [Priority.LOW, Priority.MEDIUM, Priority.HIGH], "--err-t2--")
```

`score` должно быть равно `4`

```python
    def test_3(self):
        self.assertEqual(score, 4, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import IntEnum

class Priority(IntEnum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

tasks = [Priority.HIGH, Priority.LOW, Priority.MEDIUM]
is_urgent = Priority.HIGH > Priority.MEDIUM
ordered = sorted(tasks)
score = Priority.LOW + Priority.HIGH
```
