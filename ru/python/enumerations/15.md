---
language: python
exerciseType: 4
---

# --description--

Перебор класса и поиск элементов по имени хорошо работают вместе, когда вы обрабатываете данные, приходящие извне, например строки лога или файл.
Генератор словаря по классу подготавливает по одной записи на каждый элемент, а затем `Level[name]` превращает каждую входящую строку в соответствующий элемент:
```python
counts = {level: 0 for level in Level}
counts[Level["INFO"]] += 1
```
Поскольку перечисление сохраняет порядок объявления, последующий перебор `counts` выдаёт элементы в том же порядке.

# --instructions--

Расположите элементы в правильном порядке, чтобы объявить `Level` с автоматическими значениями, подготовить счётчик для каждого элемента, подсчитать имена из списка, а затем вывести имя каждого элемента вместе с его счётчиком

# --answers--

```python
from enum import Enum, auto
```

```python
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
```

```python
counts = {level: 0 for level in Level}
```

```python
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
```

```python
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --solutions--

```python
from enum import Enum, auto
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
counts = {level: 0 for level in Level}
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --output--

DEBUG: 0
INFO: 2
ERROR: 1
