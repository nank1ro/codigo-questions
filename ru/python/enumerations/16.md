---
language: python
exerciseType: 1
---

# --description--

Кроме обычных методов, перечисление может определять **методы класса** с помощью `@classmethod`. Они получают сам класс перечисления как `cls`, поэтому это подходящее место для альтернативных способов найти элемент:
```python
class Color(Enum):
    RED = auto()
    BLUE = auto()

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]

print(Color.parse(" blue "))  # Color.BLUE
```
Вместе с `auto()`, методами, свойствами и поиском элементов это позволяет создавать перечисления, которые несут собственное поведение.

# --instructions--

Объявите перечисление `Direction` с элементами `NORTH`, `EAST`, `SOUTH` и `WEST`, именно в этом порядке, используя `auto()`.
Добавьте метод `opposite()`, который возвращает противоположный элемент (`NORTH` и `SOUTH`, `EAST` и `WEST`), и метод класса `parse(text)`, который возвращает элемент, чьё имя совпадает с `text`, без учёта регистра и окружающих пробелов

# --seed--

```python
from enum import Enum, auto
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо использовать `auto()`

```json
{
  "regex": "auto\\(\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Необходимо использовать декоратор `@classmethod`

```json
{
  "regex": "@classmethod",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Direction` должно содержать элементы `NORTH`, `EAST`, `SOUTH` и `WEST` со значениями от `1` до `4`

```python
    def test_1(self):
        self.assertEqual([d.name for d in Direction], ["NORTH", "EAST", "SOUTH", "WEST"], "--err-t1--")
        self.assertEqual([d.value for d in Direction], [1, 2, 3, 4], "--err-t1--")
```

`Direction.NORTH.opposite()` должно быть `Direction.SOUTH`, а `Direction.WEST.opposite()` должно быть `Direction.EAST`

```python
    def test_2(self):
        self.assertIs(Direction.NORTH.opposite(), Direction.SOUTH, "--err-t2--")
        self.assertIs(Direction.SOUTH.opposite(), Direction.NORTH, "--err-t2--")
        self.assertIs(Direction.EAST.opposite(), Direction.WEST, "--err-t2--")
        self.assertIs(Direction.WEST.opposite(), Direction.EAST, "--err-t2--")
```

`Direction.parse(" west ")` должно быть `Direction.WEST`

```python
    def test_3(self):
        self.assertIs(Direction.parse(" west "), Direction.WEST, "--err-t3--")
        self.assertIs(Direction.parse("North"), Direction.NORTH, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    EAST = auto()
    SOUTH = auto()
    WEST = auto()

    def opposite(self):
        opposites = {
            Direction.NORTH: Direction.SOUTH,
            Direction.SOUTH: Direction.NORTH,
            Direction.EAST: Direction.WEST,
            Direction.WEST: Direction.EAST,
        }
        return opposites[self]

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]
```
