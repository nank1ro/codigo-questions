---
language: python
exerciseType: 3
---

# --description--

`StrEnum` (доступен начиная с Python 3.11) — это строковый аналог `IntEnum`: его элементы также являются строками и равны своему значению.
Это делает их удобными везде, где ожидаются обычные строки, например в ключах конфигурации или параметрах API:
```python
from enum import StrEnum

class Mode(StrEnum):
    LIGHT = "light"
    DARK = "dark"

print(Mode.DARK == "dark")  # True
print(Mode.DARK.upper())    # DARK
```
В отличие от обычного `Enum`, преобразование элемента `StrEnum` в текст с помощью `str()` или внутри f-строки даёт его **значение**, а не `Mode.DARK`.

# --instructions--

Что выведет этот код?
```python
from enum import StrEnum

class Mode(StrEnum):
    LIGHT = "light"
    DARK = "dark"

print(f"theme: {Mode.DARK}")
```

# --answers--

- `theme: dark`, потому что элемент `StrEnum` форматируется как своё значение
- `theme: Mode.DARK`, потому что f-строка всегда показывает имена класса и элемента
- `theme: DARK`, потому что f-строка показывает имя элемента
- `theme: <Mode.DARK: 'dark'>`, потому что f-строка использует `repr()` элемента

# --solutions--

- `theme: dark`, потому что элемент `StrEnum` форматируется как своё значение
