---
language: python
exerciseType: 2
---

# --description--

Если два элемента имеют одно и то же значение, второй не является новым элементом, а представляет собой **псевдоним** первого:
```python
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # alias of ACTIVE

print(Status.ENABLED)  # Status.ACTIVE
print(len(Status))     # 1
```
Псевдонимы пропускаются при итерации и не учитываются `len()`.
Обычно дублирующееся значение — это ошибка. Декоратор `unique`, импортируемый из `enum`, заставляет Python вызвать `ValueError` сразу после объявления перечисления с псевдонимами:
```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # ValueError: duplicate values found
```

# --instructions--

Дополните код, чтобы импортировать декоратор `unique` и применить его к `Status`, так чтобы дублирующиеся значения отклонялись, а затем вывести количество элементов

# --seed--

```python
from enum import Enum, [/]

[/]
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --answers--

- unique
- @unique
- auto
- @auto
- @property
- @classmethod

# --solutions--

```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --output--

2
