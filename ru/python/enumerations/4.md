---
language: python
exerciseType: 4
---

# --description--

Класс перечисления **итерируем**: цикл `for` по классу проходит по каждому элементу в том порядке, в котором они были объявлены:
```python
for color in Color:
    print(color.name, color.value)
```
`len()` возвращает количество элементов перечисления, а `list(Color)` строит список из них:
```python
print(len(Color))    # 3
print(list(Color))   # [<Color.RED: 1>, <Color.GREEN: 2>, <Color.BLUE: 3>]
```
Внутри списка элементы отображаются с помощью своего `repr()`, который включает значение в угловых скобках.

# --instructions--

Расположите элементы в правильном порядке, чтобы объявить перечисление `Weekday`, вывести количество его элементов, а затем вывести имя и значение каждого элемента, по одному в строке

# --answers--

```python
from enum import Enum
```

```python
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
```

```python
print(len(Weekday))
```

```python
for day in Weekday:
```

```python
    print(day.name, day.value)
```

# --solutions--

```python
from enum import Enum
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
print(len(Weekday))
for day in Weekday:
    print(day.name, day.value)
```

# --output--

3
MON 1
TUE 2
WED 3
