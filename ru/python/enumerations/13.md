---
language: python
exerciseType: 1
---

# --description--

Элементы перечисления **хешируемы**, поэтому их можно использовать как ключи словаря и как элементы множества.
Словарь с ключами-элементами перечисления — это удобный способ привязать данные к каждому элементу, и поиск по элементу безопаснее, чем по обычной строке, в которой можно опечататься:
```python
class Size(Enum):
    SMALL = 1
    LARGE = 2

prices = {Size.SMALL: 2.5, Size.LARGE: 4.5}
print(prices[Size.LARGE])  # 4.5
```
Поскольку элементы могут находиться в любой коллекции, всё, что вы знаете о списках, множествах и генераторах, работает и с ними:
```python
order = [Size.LARGE, Size.SMALL, Size.LARGE]
print(len(set(order)))  # 2
```

# --instructions--

Сохраните в переменной `large_price` цену `Size.LARGE`, прочитанную из `prices`, в `total` — сумму цен размеров из `order`, а в `sizes` — множество различных размеров из `order`

# --seed--

```python
from enum import Enum

class Size(Enum):
    SMALL = 1
    MEDIUM = 2
    LARGE = 3

prices = {Size.SMALL: 2.5, Size.MEDIUM: 3.5, Size.LARGE: 4.5}
order = [Size.LARGE, Size.SMALL, Size.LARGE]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо прочитать `prices` с ключом `Size.LARGE`

```json
{
  "regex": "prices\\[\\s*Size\\.LARGE\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`large_price` должно быть равно `4.5`

```python
    def test_1(self):
        self.assertEqual(large_price, 4.5, "--err-t1--")
```

`total` должно быть равно `11.5`

```python
    def test_2(self):
        self.assertEqual(total, 11.5, "--err-t2--")
```

`sizes` должно быть равно `{Size.SMALL, Size.LARGE}`

```python
    def test_3(self):
        self.assertEqual(sizes, {Size.SMALL, Size.LARGE}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Size(Enum):
    SMALL = 1
    MEDIUM = 2
    LARGE = 3

prices = {Size.SMALL: 2.5, Size.MEDIUM: 3.5, Size.LARGE: 4.5}
order = [Size.LARGE, Size.SMALL, Size.LARGE]
large_price = prices[Size.LARGE]
total = sum(prices[size] for size in order)
sizes = set(order)
```
