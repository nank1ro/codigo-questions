---
language: python
exerciseType: 1
---

# --instructions--

Используйте открывающую `{` и закрывающую `}` фигурные скобки, чтобы вставить число `500` в пустое место форматированной строки

# --seed--

```python
print(f"There are  items")
```

# --solutions--

```python
print(f"There are {500} items")
```

# --output--

There are 500 items
