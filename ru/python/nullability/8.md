---
language: python
exerciseType: 4
---

# --description--

Функции, которые могут получить `None`, часто начинаются с **защитной проверки**: условия `if`, которое завершает функцию досрочно, когда не с чем работать. Остальная часть функции может затем считать, что значение присутствует, не оборачивая весь код в `else`:
```python
def shout(text):
    if text is None:
        return None
    return text.upper() + "!"
```
Защитные проверки идут первыми, в том порядке, в котором проверки должны выполняться: нельзя вызвать `text.split()`, не зная, что `text` — не `None`.

# --instructions--

Расположите элементы по порядку, чтобы определить `first_word`, которая возвращает `None`, когда `text` — это `None` или не содержит слов, и первое слово в противном случае; защитные проверки должны идти перед кодом, который от них зависит

# --answers--

```python
def first_word(text):
```

```python
    if text is None:
        return None
```

```python
    words = text.split()
```

```python
    if len(words) == 0:
        return None
```

```python
    return words[0]
```

```python
print(first_word("hello world"))
print(first_word(None))
print(first_word("   "))
```

# --solutions--

```python
def first_word(text):
    if text is None:
        return None
    words = text.split()
    if len(words) == 0:
        return None
    return words[0]
print(first_word("hello world"))
print(first_word(None))
print(first_word("   "))
```

# --output--

hello
None
None
