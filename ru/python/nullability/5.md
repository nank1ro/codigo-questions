---
language: python
exerciseType: 1
---

# --description--

Параметр может иметь **значение по умолчанию**, которое используется, когда вызывающий код опускает аргумент. `None` — обычное значение по умолчанию для случая "не предоставлено":
```python
def greet(name=None):
    if name is None:
        name = "stranger"
    print("Hello, " + name)

greet()       # Hello, stranger
greet("Ada")  # Hello, Ada
```
Это важно для списков и словарей. Значение по умолчанию вычисляется **один раз**, при определении функции, поэтому `def add(item, items=[])` использует один и тот же список во всех вызовах, где `items` опущен, и элементы накапливаются. Решение — использовать `None` по умолчанию и создавать новый список внутри функции:
```python
def add(item, items=None):
    if items is None:
        items = []
    items.append(item)
    return items
```

# --instructions--

Напишите функцию `add_tag`, которая принимает строку `tag` и необязательный список `tags`; она должна добавить `tag` в `tags` и вернуть список, создавая новый список, когда `tags` не предоставлен

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Значение `tags` по умолчанию должно быть `None`

```json
{
  "regex": "tags\\s*=\\s*None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`add_tag("new")` должна возвращать `["new"]`

```python
    def test_1(self):
        self.assertEqual(add_tag("new"), ["new"], "--err-t1--")
```

Два вызова без `tags` должны возвращать независимые списки

```python
    def test_2(self):
        first = add_tag("a")
        second = add_tag("b")
        self.assertEqual(first, ["a"], "--err-t2--")
        self.assertEqual(second, ["b"], "--err-t2--")
```

`add_tag("c", ["a", "b"])` должна возвращать `["a", "b", "c"]`

```python
    def test_3(self):
        existing = ["a", "b"]
        self.assertEqual(add_tag("c", existing), ["a", "b", "c"], "--err-t3--")
        self.assertIs(add_tag("d", existing), existing, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def add_tag(tag, tags=None):
    if tags is None:
        tags = []
    tags.append(tag)
    return tags
```
