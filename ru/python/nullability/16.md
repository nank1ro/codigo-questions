---
language: python
exerciseType: 1
---

# --instructions--

Напишите функцию `get_path`, которая принимает словарь `data`, список `keys` и необязательный `default` (`None`, если не предоставлен); она должна проходить по ключам по одному уровню за раз и вернуть найденное значение или `default`, когда какой-либо ключ отсутствует, содержит `None` или когда очередной шаг — не словарь; такие значения, как `0` и `""`, должны быть возвращены как есть

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Значение `default` по умолчанию должно быть `None`

```json
{
  "regex": "default\\s*=\\s*None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`get_path({"address": {"city": "Rome"}}, ["address", "city"])` должна возвращать `"Rome"`

```python
    def test_1(self):
        data = {"address": {"city": "Rome"}}
        self.assertEqual(get_path(data, ["address", "city"]), "Rome", "--err-t1--")
        self.assertEqual(get_path(data, ["address"]), {"city": "Rome"}, "--err-t1--")
```

Отсутствующий, `None` или несловарный шаг должен давать значение по умолчанию

```python
    def test_2(self):
        data = {"address": None, "name": "Ada"}
        self.assertIsNone(get_path(data, ["address", "city"]), "--err-t2--")
        self.assertEqual(get_path(data, ["phone", "home"], "n/a"), "n/a", "--err-t2--")
        self.assertEqual(get_path(data, ["name", "first"], "n/a"), "n/a", "--err-t2--")
```

`0` и `""` должны быть возвращены как есть

```python
    def test_3(self):
        data = {"stats": {"count": 0, "note": ""}}
        self.assertEqual(get_path(data, ["stats", "count"], -1), 0, "--err-t3--")
        self.assertEqual(get_path(data, ["stats", "note"], "none"), "", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def get_path(data, keys, default=None):
    current = data
    for key in keys:
        if not isinstance(current, dict):
            return default
        current = current.get(key)
        if current is None:
            return default
    return current
```
