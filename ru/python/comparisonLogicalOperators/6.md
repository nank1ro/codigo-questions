---
language: python
exerciseType: 2
---

# --description--

У каждого значения есть **истинностное значение**. `bool(value)` возвращает `False` для `0`, `0.0`, `None`, пустой строки `""` и пустых контейнеров вроде `[]`, `{}` и `set()`; любое другое значение истинно, включая `"0"` и `[0]`:
```python
print(bool(0), bool(""), bool(None))   # False False False
print(bool(42), bool("0"), bool([0]))  # True True True
```
`if`, `while`, `and`, `or` и `not` используют это правило, поэтому `if items:` проверяет, что список не пуст, а `not name` проверяет, что строка пуста; писать `len(items) > 0` или `name == ""` не нужно.

# --instructions--

Дополните код так, чтобы первая строка выводила истинностное значение `cart`, `name` и `count`, а `if` выводил `empty cart`, когда список пуст, давая вывод `False True False`, `empty cart`

# --seed--

```python
cart = []
name = "Ada"
count = 0
print([/](cart), bool(name), bool(count))
if [/] cart:
    print("empty cart")
```

# --answers--

- bool
- not
- len
- str
- is

# --solutions--

```python
cart = []
name = "Ada"
count = 0
print(bool(cart), bool(name), bool(count))
if not cart:
    print("empty cart")
```

# --output--

False True False
empty cart
