---
language: python
exerciseType: 2
---

# --description--

С классами всё работает так же: строка, стоящая первой инструкцией в теле класса, — это docstring этого класса, и она хранится в `__doc__`:
```python
class Point:
    """A point on a grid."""

    def __init__(self, x, y):
        self.x = x
        self.y = y

print(Point.__doc__)  # A point on a grid.
```
У каждого метода внутри класса тоже может быть свой docstring, который читается через `Point.__init__.__doc__`. Итак, три места, где допустим docstring, — это начало модуля, начало класса и начало функции.

# --instructions--

Дополните код так, чтобы у `Point` появился docstring и программа вывела его

# --seed--

```python
class Point:
    [/]A point on a grid.[/]

    def __init__(self, x, y):
        self.x = x
        self.y = y

print(Point.[/])
```

# --answers--

- """
- """
- __doc__
- __name__
- #

# --solutions--

```python
class Point:
    """A point on a grid."""

    def __init__(self, x, y):
        self.x = x
        self.y = y

print(Point.__doc__)
```

# --output--

A point on a grid.
