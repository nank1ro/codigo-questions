---
language: python
exerciseType: 1
---

# --description--

Когда строка является **первой инструкцией** внутри функции, Python считает её документацией этой функции. Она называется **docstring**:
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"
```
По соглашению docstring записывают в тройных двойных кавычках, `"""`, даже если она умещается в одну строку, чтобы позже её можно было расширить, не меняя кавычки.

Пишите краткое описание в третьем лице, как будто описываете функцию: "Returns...", "Adds...", "Checks...". Docstring должна идти раньше любой другой инструкции в теле, иначе это просто обычная строка.

# --instructions--

Напишите функцию `double(n)`, которая возвращает `n`, умноженное на два, и добавьте ей однострочный docstring

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`double` должна иметь docstring в качестве первой инструкции

```python
    def test_1(self):
        self.assertIsNotNone(double.__doc__, "--err-t1--")
```

`double(21)` должна возвращать `42`

```python
    def test_2(self):
        self.assertEqual(double(21), 42, "--err-t2--")
```

`double(-3)` должна возвращать `-6`, а `double(0)` — `0`

```python
    def test_3(self):
        self.assertEqual(double(-3), -6, "--err-t3--")
        self.assertEqual(double(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def double(n):
    """Returns n multiplied by two."""
    return n * 2
```
