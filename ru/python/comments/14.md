---
language: python
exerciseType: 1
---

# --description--

Комментарий над функцией, объясняющий, что делает функция, находится не там. Для этого существует docstring: он прикреплён к функции, его находит `help()` и показывают редакторы, а комментарий `#` над `def` невидим для них всех.

```python
# adds a and b
def add(a, b):
    return a + b
```
Сдвиньте ту же фразу на строку вниз, в тройные кавычки, — и она станет настоящей документацией:
```python
def add(a, b):
    """Returns the sum of a and b."""
    return a + b
```

# --instructions--

Удалите два комментария `#` над `subtract` и вместо них задокументируйте функцию с помощью docstring

# --seed--

```python
# subtract
# this function subtracts b from a
def subtract(a, b):
    return a - b
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Комментарии `#` над `subtract` должны быть удалены

```json
{
  "regex": "#\\s*this function subtracts",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

Комментарий `# subtract` тоже должен быть удалён

```json
{
  "regex": "#\\s*subtract",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

У `subtract` должен быть docstring

```python
    def test_1(self):
        self.assertIsNotNone(subtract.__doc__, "--err-t1--")
```

`subtract(5, 3)` должна возвращать `2`

```python
    def test_2(self):
        self.assertEqual(subtract(5, 3), 2, "--err-t2--")
```

`subtract(0, 4)` должна возвращать `-4`

```python
    def test_3(self):
        self.assertEqual(subtract(0, 4), -4, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def subtract(a, b):
    """Returns b subtracted from a."""
    return a - b
```
