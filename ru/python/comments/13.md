---
language: python
exerciseType: 1
---

# --description--

Некоторые комментарии следуют соглашению, которое понимают редакторы. Самые распространённые **маркеры**:
- `# TODO: ...` помечает то, что ещё нужно написать
- `# FIXME: ...` помечает код, который заведомо неверен и требует исправления

```python
limit = 10
# TODO: считать лимит из настроек
```
Для Python это обычные комментарии; редакторы собирают их в отдельную панель, так что незавершённую работу легко найти. `TODO` обычно стоит рядом с заглушкой, которая позволяет программе работать, пока настоящий код не написан.

Закончив работу, замените заглушку и удалите маркер одним изменением, чтобы комментарий никогда не лгал о состоянии кода.

# --instructions--

Выполните работу, описанную комментарием `TODO` в `is_even`, а затем удалите комментарий, так как работа сделана

# --seed--

```python
def is_even(n):
    # TODO: вернуть True, если n чётное, иначе False
    return False
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Комментарий `TODO` должен быть удалён

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`is_even(4)` должна возвращать `True`

```python
    def test_1(self):
        self.assertEqual(is_even(4), True, "--err-t1--")
```

`is_even(7)` должна возвращать `False`

```python
    def test_2(self):
        self.assertEqual(is_even(7), False, "--err-t2--")
```

`is_even(0)` должна возвращать `True`

```python
    def test_3(self):
        self.assertEqual(is_even(0), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_even(n):
    return n % 2 == 0
```
