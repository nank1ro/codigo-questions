---
language: python
exerciseType: 1
---

# --description--

Lambda не ограничивается встроенными функциями: **ваши собственные функции** тоже могут принимать функцию в качестве параметра и вызывать её.
```python
def apply(func, value):
    return func(value)

print(apply(lambda x: x * 10, 3))  # 30
```

# --instructions--

Определите функцию `apply_twice(func, value)`, которая применяет `func` к `value` два раза и возвращает результат.
Затем сохраните в `result` значение `apply_twice`, вызванной с lambda, прибавляющей `3`, и значением `10`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо использовать выражение `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`apply_twice(lambda x: x * 2, 5)` должна возвращать `20`

```python
    def test_1(self):
        self.assertEqual(apply_twice(lambda x: x * 2, 5), 20, "--err-t1--")
```

`apply_twice(lambda s: s + "!", "hi")` должна возвращать `hi!!`

```python
    def test_2(self):
        self.assertEqual(apply_twice(lambda s: s + "!", "hi"), "hi!!", "--err-t2--")
```

`result` должно быть равно `16`

```python
    def test_3(self):
        self.assertEqual(result, 16, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def apply_twice(func, value):
    return func(func(value))

result = apply_twice(lambda x: x + 3, 10)
```
