---
language: python
exerciseType: 1
---

# --description--

По-настоящему lambda раскрывается как **аргумент, передаваемый другим функциям**.
`sorted()` принимает параметр `key`: функцию, вызываемую для каждого элемента, результат которой определяет порядок.
Lambda подходит для этого идеально:
```python
words = ["banana", "kiwi", "apple"]
print(sorted(words, key=lambda w: len(w)))
# ['kiwi', 'apple', 'banana']
```

# --instructions--

Сохраните в переменной `by_length` список `words`, отсортированный от самого короткого слова к самому длинному, используя `sorted()` с lambda в качестве `key`

# --seed--

```python
words = ["elephant", "cat", "tiger", "ox"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо использовать выражение `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`by_length` должно быть равно `['ox', 'cat', 'tiger', 'elephant']`

```python
    def test_1(self):
        self.assertEqual(by_length, ["ox", "cat", "tiger", "elephant"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
words = ["elephant", "cat", "tiger", "ox"]
by_length = sorted(words, key=lambda w: len(w))
```
