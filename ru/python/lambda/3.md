---
language: python
exerciseType: 2
---

# --description--

Lambda может принимать **больше одного параметра**.
Разделяйте их запятыми, точно так же, как в функции `def`:
```python
power = lambda base, exp: base ** exp
print(power(2, 3))  # 8
```

# --instructions--

Дополните lambda так, чтобы она принимала параметры `a` и `b` и возвращала их сумму

# --seed--

```python
add = lambda a[/] b[/] a + b
print(add(3, 4))
```

# --answers--

- ,
- :
- ;
- ->

# --solutions--

```python
add = lambda a, b: a + b
print(add(3, 4))
```

# --output--

7
