---
language: python
exerciseType: 1
---

# --description--

`map()` применяет функцию к **каждому элементу** списка.
Она возвращает специальный _объект map_, поэтому оберните его в `list()`, чтобы увидеть значения:
```python
nums = [1, 2, 3]
doubled = list(map(lambda n: n * 2, nums))
print(doubled)  # [2, 4, 6]
```

# --instructions--

Сохраните в переменной `squares` список квадратов `nums`, используя `map()` с lambda

# --seed--

```python
nums = [1, 2, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо использовать выражение `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Необходимо использовать `map()`

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`squares` должно быть равно `[1, 4, 9, 16]`

```python
    def test_1(self):
        self.assertEqual(squares, [1, 4, 9, 16], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 3, 4]
squares = list(map(lambda n: n * n, nums))
```
