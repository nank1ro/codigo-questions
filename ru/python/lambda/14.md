---
language: python
exerciseType: 1
---

# --description--

**Когда лучше предпочесть `def`?**
Lambda отлично подходит для короткой одноразовой функции, передаваемой в качестве аргумента.
Если логике нужно имя, несколько строк, docstring или она используется во многих местах, функция `def` понятнее.
Последняя хитрость: `sorted()` также принимает `reverse=True`, чтобы сначала получить наибольшие значения:
```python
print(sorted([3, 1, 2], reverse=True))  # [3, 2, 1]
```

# --instructions--

Сохраните в `ranking` список `students`, отсортированный от наивысшей `grade` к наинизшей, затем сохраните в `names` список имён из `ranking`, используя `sorted()` и `map()` с lambda

# --seed--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо использовать выражение `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Необходимо использовать `map()`

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ranking` должен начинаться со студента `Cy`

```python
    def test_1(self):
        self.assertEqual(ranking[0]["name"], "Cy", "--err-t1--")
```

`names` должно быть равно `['Cy', 'Ana', 'Bo']`

```python
    def test_2(self):
        self.assertEqual(names, ["Cy", "Ana", "Bo"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
ranking = sorted(students, key=lambda s: s["grade"], reverse=True)
names = list(map(lambda s: s["name"], ranking))
```
