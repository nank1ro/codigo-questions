---
language: python
exerciseType: 1
---

# --description--

Сопрограмма может принимать параметры и возвращать значение с помощью `return` точно так же, как обычная функция. Значение недоступно в момент создания объекта-сопрограммы — только после того, как сопрограмма была выполнена. `asyncio.run` возвращает то, что вернула сопрограмма:
```python
import asyncio

async def double(n):
    return n * 2

result = asyncio.run(double(21))
print(result)  # 42
```
Всё, что вы знаете о функциях, работает и внутри `async def`: локальные переменные, условия, циклы и несколько инструкций `return`.

# --instructions--

Напишите с помощью `async def` функцию-сопрограмму `add(a, b)`, которая возвращает сумму `a` и `b`

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`add` должна быть функцией-сопрограммой

```python
    def test_1(self):
        self.assertTrue(inspect.iscoroutinefunction(add), "--err-t1--")
```

Запуск `add(1, 3)` должен возвращать `4`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(add(1, 3)), 4, "--err-t2--")
```

Запуск `add(200, 210)` должен возвращать `410`

```python
    def test_3(self):
        self.assertEqual(asyncio.run(add(200, 210)), 410, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
async def add(a, b):
    return a + b
```
