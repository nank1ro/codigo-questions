---
language: python
exerciseType: 3
---

# --description--

Когда одна из сопрограмм, переданных в `gather`, выбрасывает исключение, оно распространяется до строки `await asyncio.gather(...)`, и результаты остальных теряются, хотя они продолжают выполняться. Передача `return_exceptions=True` меняет это: `gather` никогда не выбрасывает исключений, а объект исключения занимает место отсутствующего результата в списке:
```python
import asyncio

async def ok():
    return 1

async def fail():
    raise ValueError("bad")

async def main():
    results = await asyncio.gather(ok(), fail(), return_exceptions=True)
    print(results)  # [1, ValueError('bad')]

asyncio.run(main())
```
Затем каждый элемент можно проверить с помощью `isinstance(result, Exception)`, чтобы отделить неудачи от значений.

# --instructions--

Что выведет этот код?
```python
import asyncio

async def ok():
    return 1

async def fail():
    raise ValueError("bad")

async def main():
    results = await asyncio.gather(ok(), fail(), return_exceptions=True)
    print(len(results), isinstance(results[1], ValueError))

asyncio.run(main())
```

# --answers--

- `2 True`, потому что с `return_exceptions=True` исключение возвращается в списке вместо того, чтобы быть выброшенным
- `ValueError`, потому что исключение в любой сопрограмме всегда распространяется за пределы `gather`
- `1 False`, потому что завершившаяся неудачей сопрограмма исключается из результатов
- `2 False`, потому что `gather` заменяет неудавшиеся результаты на `None`

# --solutions--

- `2 True`, потому что с `return_exceptions=True` исключение возвращается в списке вместо того, чтобы быть выброшенным
