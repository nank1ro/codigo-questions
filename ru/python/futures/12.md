---
language: python
exerciseType: 1
---

# --description--

Исключение, возникшее внутри сопрограммы, не появляется там, где был создан объект-сопрограмма: оно возникает в `await`, который её выполняет, или в `asyncio.run` — для самой внешней. Поэтому `try`/`except` должен обрамлять именно **`await`**:
```python
import asyncio

async def load(path):
    await asyncio.sleep(0.01)
    if path == "":
        raise FileNotFoundError("empty path")
    return "contents"

async def safe_load(path):
    try:
        return await load(path)
    except FileNotFoundError:
        return ""

print(asyncio.run(safe_load("")))  # prints an empty line
```
Исключение, которое никто не перехватывает, распространяется через каждый `await` вплоть до `asyncio.run`, который выбрасывает его заново в синхронном коде — совсем как обычный стек вызовов.

# --instructions--

Напишите функцию-сопрограмму `divide(a, b)`, которая ждёт `0.01` секунды и возвращает `a / b`, и функцию-сопрограмму `safe_divide(a, b)`, которая ожидает с `await` `divide(a, b)` и возвращает её результат, либо `None`, если возникло `ZeroDivisionError`

# --before-asserts--

```python
import asyncio
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`divide(10, 4)` должна возвращать `2.5`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(divide(10, 4)), 2.5, "--err-t1--")
```

`divide(1, 0)` должна выбрасывать `ZeroDivisionError` при запуске

```python
    def test_2(self):
        with self.assertRaises(ZeroDivisionError, msg="--err-t2--"):
            asyncio.run(divide(1, 0))
```

`safe_divide(10, 2)` должна возвращать `5.0`, а `safe_divide(1, 0)` — `None`

```python
    def test_3(self):
        self.assertEqual(asyncio.run(safe_divide(10, 2)), 5.0, "--err-t3--")
        self.assertIsNone(asyncio.run(safe_divide(1, 0)), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def divide(a, b):
    await asyncio.sleep(0.01)
    return a / b

async def safe_divide(a, b):
    try:
        return await divide(a, b)
    except ZeroDivisionError:
        return None
```
