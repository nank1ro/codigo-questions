---
language: python
exerciseType: 2
---

# --description--

Ожидание сопрограмм одна за другой выполняет их **последовательно**: три ожидания по 10 миллисекунд занимают 30 миллисекунд. `asyncio.gather` выполняет несколько сопрограмм **конкурентно**: пока одна спит, остальные продвигаются вперёд, поэтому три ожидания вместе занимают около 10 миллисекунд. Она возвращает список с результатами в том же порядке, что и аргументы:
```python
import asyncio

async def double(n):
    await asyncio.sleep(0.01)
    return n * 2

async def main():
    results = await asyncio.gather(double(1), double(2), double(3))
    print(results)  # [2, 4, 6]

asyncio.run(main())
```
Саму `gather` нужно ожидать с `await`, и она принимает сопрограммы как отдельные аргументы. Чтобы передать список, распакуйте его: `asyncio.gather(*coroutines)`.

# --instructions--

Дополните код так, чтобы три вызова `double` выполнялись конкурентно с помощью `asyncio.gather` и их результаты выводились, давая вывод `[2, 4, 6]`

# --seed--

```python
import asyncio

async def double(n):
    await asyncio.sleep(0.01)
    return n * 2

async def main():
    results = [/] asyncio.[/](double(1), double(2), double(3))
    print(results)

asyncio.run(main())
```

# --answers--

- await
- gather
- run
- async
- wait
- all

# --solutions--

```python
import asyncio

async def double(n):
    await asyncio.sleep(0.01)
    return n * 2

async def main():
    results = await asyncio.gather(double(1), double(2), double(3))
    print(results)

asyncio.run(main())
```

# --output--

[2, 4, 6]
