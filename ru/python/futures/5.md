---
language: python
exerciseType: 1
---

# --description--

Сопрограммы вызывают друг друга с помощью `await`. Сопрограмма может ожидать любую другую сопрограмму, получать её возвращаемое значение и продолжать работу — совсем как цепочка вызовов функций:
```python
import asyncio

async def fetch_price(item):
    await asyncio.sleep(0.01)  # simulates a slow lookup
    return 10

async def total(item, quantity):
    price = await fetch_price(item)
    return price * quantity

print(asyncio.run(total("pen", 3)))  # 30
```
Через `asyncio.run` проходит только самая внешняя сопрограмма; до каждой внутренней добираются с помощью `await`. Если бы `total` была обычной `def`, она вообще не могла бы использовать `await`: ключевое слово `async` распространяется на каждую функцию в цепочке, которой нужно ждать.

# --instructions--

Напишите функцию-сопрограмму `fetch_user(user_id)`, которая ждёт `0.01` секунды и возвращает строку `"user"`, за которой следует id, и функцию-сопрограмму `greet_user(user_id)`, которая ожидает с `await` `fetch_user` и возвращает `"Hello, "`, за которым следует полученное имя

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch_user(1)` должна возвращать `"user1"`, а `fetch_user(42)` — `"user42"`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch_user(1)), "user1", "--err-t1--")
        self.assertEqual(asyncio.run(fetch_user(42)), "user42", "--err-t1--")
```

`greet_user(1)` должна возвращать `"Hello, user1"`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(greet_user(1)), "Hello, user1", "--err-t2--")
```

И `fetch_user`, и `greet_user` должны быть функциями-сопрограммами

```python
    def test_3(self):
        self.assertTrue(inspect.iscoroutinefunction(fetch_user), "--err-t3--")
        self.assertTrue(inspect.iscoroutinefunction(greet_user), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch_user(user_id):
    await asyncio.sleep(0.01)
    return f"user{user_id}"

async def greet_user(user_id):
    name = await fetch_user(user_id)
    return f"Hello, {name}"
```
