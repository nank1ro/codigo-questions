---
language: python
exerciseType: 2
---

# --description--

`asyncio.wait_for(awaitable, timeout)` ожидает переданный объект, но сдаётся по истечении `timeout` секунд: операция отменяется и выбрасывается `TimeoutError`, который можно перехватить, как любое исключение:
```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())  # timed out
```
С таймаутом `0.1` тот же код вывел бы `done`. `asyncio.TimeoutError` — это другое имя встроенного `TimeoutError`.

# --instructions--

Дополните код так, чтобы `slow()` ожидалась с `await` с таймаутом `0.01` секунды и таймаут был перехвачен, давая вывод `timed out`

# --seed--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.[/](slow(), [/]=0.01)
        print(result)
    except [/]:
        print("timed out")

asyncio.run(main())
```

# --answers--

- wait_for
- timeout
- TimeoutError
- gather
- delay
- ValueError

# --solutions--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())
```

# --output--

timed out
