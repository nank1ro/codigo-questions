---
language: python
exerciseType: 1
---

# --description--

`await` приостанавливает текущую сопрограмму, пока ожидаемая операция не завершится, а затем отдаёт её результат. Пока сопрограмма приостановлена, цикл событий свободно выполняет другие сопрограммы. `await` разрешён только внутри `async def`.

`asyncio.sleep(seconds)` — простейший ожидаемый объект: он ждёт указанное время, не блокируя цикл. В отличие от `time.sleep`, его нужно ожидать с помощью `await`:
```python
import asyncio

async def countdown():
    print("ready")
    await asyncio.sleep(0.05)
    print("go")

asyncio.run(countdown())
```
Программа выводит `ready`, ждёт 50 миллисекунд и выводит `go`. Запись `asyncio.sleep(0.05)` без `await` создаёт объект-сопрограмму, но никогда не запускает его, поэтому никакого ожидания не происходит.

# --instructions--

Напишите функцию-сопрограмму `wait_and_return(value, delay)`, которая ждёт `delay` секунд с помощью `asyncio.sleep`, а затем возвращает `value`

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`wait_and_return("ok", 0.01)` должна возвращать `"ok"`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(wait_and_return("ok", 0.01)), "ok", "--err-t1--")
```

`wait_and_return(7, 0.02)` должна возвращать `7` и выполняться не менее `0.02` секунд

```python
    def test_2(self):
        start = time.perf_counter()
        result = asyncio.run(wait_and_return(7, 0.02))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, 7, "--err-t2--")
        self.assertGreaterEqual(elapsed, 0.018, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def wait_and_return(value, delay):
    await asyncio.sleep(delay)
    return value
```
