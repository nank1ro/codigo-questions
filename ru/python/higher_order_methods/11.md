---
language: python
exerciseType: 1
---

# --description--

`partial` удобен со встроенными функциями, принимающими параметры. `int(text, base=16)` разбирает шестнадцатеричную строку; фиксация основания даёт преобразователь с одним аргументом, который подходит для `map`:
```python
from functools import partial

hex_to_int = partial(int, base=16)
print(hex_to_int("ff"))                      # 255
print(list(map(hex_to_int, ["a", "10"])))    # [10, 16]
```
Объект partial помнит, что он оборачивает: `hex_to_int.func` — это `int`, а `hex_to_int.keywords` — это `{'base': 16}`.

# --instructions--

Определите две функции с помощью `partial`: `hex_to_int`, которая преобразует шестнадцатеричную строку в `int`, и `binary_to_int`, которая преобразует двоичную строку в `int`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`hex_to_int("ff")` должна возвращать `255`

```python
    def test_1(self):
        self.assertEqual(hex_to_int("ff"), 255, "--err-t1--")
        self.assertEqual(hex_to_int("10"), 16, "--err-t1--")
```

`binary_to_int("1010")` должна возвращать `10`

```python
    def test_2(self):
        self.assertEqual(binary_to_int("1010"), 10, "--err-t2--")
        self.assertEqual(binary_to_int("11"), 3, "--err-t2--")
```

Обе должны быть объектами partial, оборачивающими `int`

```python
    def test_3(self):
        self.assertIs(hex_to_int.func, int, "--err-t3--")
        self.assertIs(binary_to_int.func, int, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import partial

hex_to_int = partial(int, base=16)
binary_to_int = partial(int, base=2)
```
