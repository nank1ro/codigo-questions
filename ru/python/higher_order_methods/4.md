---
language: python
exerciseType: 1
---

# --description--

Встроенная функция `filter(func, iterable)` оставляет только те элементы, для которых `func` возвращает истинное значение. Как и `map`, она возвращает ленивый объект, который нужно превратить в список:
```python
numbers = [4, -2, 7, 0]
print(list(filter(lambda n: n > 0, numbers)))  # [4, 7]
```
Функция, переданная в `filter`, называется **предикатом**: она принимает один элемент и отвечает на вопрос о нём «да» или «нет». Если вместо функции передать `None`, останутся элементы, истинные сами по себе, а `0`, `""` и `None` будут отброшены.

# --instructions--

Напишите функцию `positives`, которая принимает список чисел и возвращает новый список только с числами больше нуля, используя `filter`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо использовать `filter`

```json
{
  "regex": "filter\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`positives([3, -1, 0, 5])` должна возвращать `[3, 5]`

```python
    def test_1(self):
        self.assertEqual(positives([3, -1, 0, 5]), [3, 5], "--err-t1--")
```

Результат должен быть списком

```python
    def test_2(self):
        self.assertIsInstance(positives([1, 2]), list, "--err-t2--")
        self.assertEqual(positives([1, 2]), [1, 2], "--err-t2--")
```

`positives([-4, 0])` должна возвращать пустой список

```python
    def test_3(self):
        self.assertEqual(positives([-4, 0]), [], "--err-t3--")
        self.assertEqual(positives([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def positives(numbers):
    return list(filter(lambda n: n > 0, numbers))
```
