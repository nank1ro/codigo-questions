---
language: python
exerciseType: 1
---

# --description--

`reduce(func, iterable, initial)` из модуля `functools` сворачивает последовательность в **одно значение**. Она вызывает `func` с результатом, накопленным на данный момент, и следующим элементом, начиная с `initial`:
```python
from functools import reduce

total = reduce(lambda acc, n: acc + n, [1, 2, 3], 0)
print(total)  # 6
```
Шаги такие: `0 + 1`, затем `1 + 2`, затем `3 + 3`. Если `initial` опущен, начальным значением становится первый элемент, но тогда пустая последовательность вызывает `TypeError`, поэтому задавайте начальное значение всякий раз, когда последовательность может быть пустой.

# --instructions--

Напишите функцию `product`, которая принимает список чисел и возвращает их произведение, используя `reduce`; произведение пустого списка должно быть равно `1`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо использовать `reduce`

```json
{
  "regex": "reduce\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`product([2, 3, 4])` должна возвращать `24`

```python
    def test_1(self):
        self.assertEqual(product([2, 3, 4]), 24, "--err-t1--")
```

`product([5])` должна возвращать `5`

```python
    def test_2(self):
        self.assertEqual(product([5]), 5, "--err-t2--")
```

`product([])` должна возвращать `1`

```python
    def test_3(self):
        self.assertEqual(product([]), 1, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import reduce

def product(numbers):
    return reduce(lambda acc, n: acc * n, numbers, 1)
```
