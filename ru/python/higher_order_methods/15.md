---
language: python
exerciseType: 4
---

# --instructions--

Расположите элементы так, чтобы оставить только слова длиннее трёх букв, перевести их в верхний регистр и вывести отсортированными по длине

# --answers--

```python
words = ["pear", "fig", "banana", "kiwi"]
```

```python
long_words = filter(lambda w: len(w) > 3, words)
```

```python
shouted = map(str.upper, long_words)
```

```python
print(sorted(shouted, key=len))
```

# --solutions--

```python
words = ["pear", "fig", "banana", "kiwi"]
long_words = filter(lambda w: len(w) > 3, words)
shouted = map(str.upper, long_words)
print(sorted(shouted, key=len))
```

# --output--

['PEAR', 'KIWI', 'BANANA']
