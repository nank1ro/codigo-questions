---
language: python
exerciseType: 2
---

# --description--

В Python функция — это **значение**, такое же как число или строка. Её можно сохранить в переменную, положить в список или передать в другую функцию. Вызывают её только скобки: `shout` — это сама функция, а `shout("hi")` — её результат:
```python
def shout(text):
    return text.upper() + "!"

say = shout
print(say("hi"))  # HI!
```
Функция, которая принимает другую функцию как параметр или возвращает её, называется **функцией высшего порядка**. Внутри неё параметр вызывается со скобками, как любая другая функция:
```python
def apply(func, value):
    return func(value)

print(apply(shout, "bye"))  # BYE!
print(apply(len, "bye"))    # 3
```

# --instructions--

Дополните код так, чтобы `apply` вызывала полученную функцию, а `say` ссылалась на `shout` без вызова, и вывод был `HI!`, `3`

# --seed--

```python
def shout(text):
    return text.upper() + "!"

def apply(func, value):
    return [/](value)

say = [/]
print(say("hi"))
print(apply(len, "bye"))
```

# --answers--

- func
- shout
- shout()
- value
- print

# --solutions--

```python
def shout(text):
    return text.upper() + "!"

def apply(func, value):
    return func(value)

say = shout
print(say("hi"))
print(apply(len, "bye"))
```

# --output--

HI!
3
