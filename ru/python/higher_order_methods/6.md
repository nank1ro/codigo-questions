---
language: python
exerciseType: 4
---

# --description--

Функция `key` может выбирать **любую часть** элемента. Для списка кортежей `lambda s: s[1]` сортирует по второму элементу каждого кортежа; для списка словарей `lambda d: d["age"]` сортирует по значению:
```python
pairs = [("b", 2), ("a", 3), ("c", 1)]
print(sorted(pairs, key=lambda p: p[1]))  # [('c', 1), ('b', 2), ('a', 3)]
```
`min` и `max` принимают тот же параметр `key`, поэтому `max(pairs, key=lambda p: p[1])` возвращает `('a', 3)`: весь кортеж, а не только число.

# --instructions--

Расположите элементы так, чтобы вывести результаты от наибольшего к наименьшему, по одному в строке в виде `name: score`

# --answers--

```python
scores = [("Ada", 92), ("Linus", 85), ("Grace", 97)]
```

```python
ranked = sorted(scores, key=lambda s: s[1], reverse=True)
```

```python
for name, score in ranked:
```

```python
    print(f"{name}: {score}")
```

# --solutions--

```python
scores = [("Ada", 92), ("Linus", 85), ("Grace", 97)]
ranked = sorted(scores, key=lambda s: s[1], reverse=True)
for name, score in ranked:
    print(f"{name}: {score}")
```

# --output--

Grace: 97
Ada: 92
Linus: 85
