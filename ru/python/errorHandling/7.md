---
language: python
exerciseType: 1
---

# --description--

Когда несколько сбоев заслуживают **одинакового** восстановления, короче перечислить их кортежем в одной ветке, чем повторять блок:
```python
try:
    value = int(text) / divisor
except (ValueError, ZeroDivisionError):
    value = 0
```
Скобки обязательны: `except ValueError, ZeroDivisionError:` — синтаксическая ошибка в Python 3. Кортеж при этом остаётся явным списком типов.

# --instructions--

Напишите функцию `safe_ratio(a, b)`, которая возвращает `a / b` и возвращает `None`, когда деление вызывает `ZeroDivisionError` или `TypeError`, используя одну ветку `except` с кортежем из обоих типов

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_ratio(9, 3)` должна возвращать `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_ratio(9, 3), 3.0, "--err-t1--")
```

`safe_ratio(1, 0)` должна возвращать `None`

```python
    def test_2(self):
        self.assertIsNone(safe_ratio(1, 0), "--err-t2--")
```

`safe_ratio(1, "x")` должна возвращать `None`

```python
    def test_3(self):
        self.assertIsNone(safe_ratio(1, "x"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_ratio(a, b):
    try:
        return a / b
    except (ZeroDivisionError, TypeError):
        return None
```
