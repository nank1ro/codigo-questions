---
language: python
exerciseType: 1
---

# --description--

Когда ни один встроенный тип не подходит, определите свой, унаследовав от `Exception`. Пустого тела обычно достаточно — имя и есть послание читателю:
```python
class ConfigError(Exception):
    pass
```
Он ведёт себя как любое другое исключение: `raise ConfigError("bad port")`, а `except ConfigError:` его перехватывает.

Превращать сбой низкого уровня в свой тип — обычное дело, и исходная ошибка не должна теряться в этом процессе. `raise NewError(...) from original` **связывает** их: он сохраняет `original` в атрибуте `__cause__` нового исключения, и трассировка показывает оба под заголовком *The above exception was the direct cause of the following exception*:
```python
try:
    port = int(text)
except ValueError as e:
    raise ConfigError("bad port") from e
```
Без `from e` они всё равно связаны неявно, но `from` прямо говорит, что первая ошибка стала причиной второй.

# --instructions--

Определите класс исключения `ConfigError`, унаследованный от `Exception`, и напишите функцию `read_port(text)`, которая возвращает `int(text)`, а когда тот вызывает `ValueError`, вызывает `ConfigError("bad port")`, связанный с ним через `raise ... from`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`read_port("8080")` должна возвращать `8080`, а `ConfigError` должен быть подклассом `Exception`

```python
    def test_1(self):
        self.assertEqual(read_port("8080"), 8080, "--err-t1--")
        self.assertTrue(issubclass(ConfigError, Exception), "--err-t1--")
```

`read_port("abc")` должна вызывать `ConfigError` с сообщением `"bad port"`

```python
    def test_2(self):
        with self.assertRaises(ConfigError, msg="--err-t2--") as ctx:
            read_port("abc")
        self.assertEqual(str(ctx.exception), "bad port", "--err-t2--")
```

вызванный `ConfigError` должен быть связан с исходным `ValueError`

```python
    def test_3(self):
        with self.assertRaises(ConfigError, msg="--err-t3--") as ctx:
            read_port("abc")
        self.assertIsInstance(ctx.exception.__cause__, ValueError, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
class ConfigError(Exception):
    pass

def read_port(text):
    try:
        return int(text)
    except ValueError as e:
        raise ConfigError("bad port") from e
```
