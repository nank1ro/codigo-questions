---
language: python
exerciseType: 1
---

# --description--

Блок `try` останавливается на **первой** инструкции, которая вызывает исключение; следующие за ней строки пропускаются, и управление переходит к блоку `except`. Ничего из сделанного в блоке `try` не отменяется, поэтому держите его как можно короче:
```python
def half(n):
    try:
        return 10 / n
    except ZeroDivisionError:
        return "undefined"

print(half(2))  # 5.0
print(half(0))  # undefined
```
`return` внутри `except` работает как любой другой `return`, поэтому `try`/`except` — естественный способ вернуть запасное значение вместо падения программы.

# --instructions--

Напишите функцию `safe_divide(a, b)`, которая возвращает `a / b`, или строку `"undefined"`, когда `b` равен `0`, перехватывая `ZeroDivisionError`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_divide(9, 3)` должна возвращать `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_divide(9, 3), 3.0, "--err-t1--")
```

`safe_divide(1, 0)` должна возвращать `"undefined"`

```python
    def test_2(self):
        self.assertEqual(safe_divide(1, 0), "undefined", "--err-t2--")
```

`safe_divide(-5, 2)` должна возвращать `-2.5`

```python
    def test_3(self):
        self.assertEqual(safe_divide(-5, 2), -2.5, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "undefined"
```
