---
language: python
exerciseType: 4
---

# --description--

За блоком `try` может следовать блок `else`, который выполняется, **только когда блок `try` завершился без исключения**:
```python
try:
    number = int(text)
except ValueError:
    print("not a number")
else:
    print(number * 2)
```
Поместить `print(number * 2)` внутрь блока `try` тоже сработало бы, но тогда `ValueError`, вызванный самим выводом, можно было бы принять за сбой преобразования. `else` удерживает блок `try` в рамках одной защищаемой инструкции и вмещает всё, что должно произойти при успехе.

# --instructions--

Расположите элементы по порядку, чтобы получить программу, которая определяет `parse`, а затем вызывает её с `"21"` и с `"abc"` именно в таком порядке, выводя удвоенное число при успехе и сообщение при неудаче

# --answers--

```python
def parse(text):
```

```python
    try:
        number = int(text)
```

```python
    except ValueError:
        print(f"{text} is not a number")
```

```python
    else:
        print(number * 2)
```

```python
parse("21")
```

```python
parse("abc")
```

# --solutions--

```python
def parse(text):
    try:
        number = int(text)
    except ValueError:
        print(f"{text} is not a number")
    else:
        print(number * 2)
parse("21")
parse("abc")
```

# --output--

42
abc is not a number
