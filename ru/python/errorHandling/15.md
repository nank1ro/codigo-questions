---
language: python
exerciseType: 4
---

# --description--

Иногда обработчик должен отреагировать на сбой, не беря за него ответственность: записать в журнал, посчитать, закрыть что-то — а дальше пусть с этим разбирается вызывающий. Одинокий `raise` внутри блока `except` **повторно вызывает** обрабатываемое исключение с исходными типом, сообщением и трассировкой:
```python
try:
    value = int(text)
except ValueError:
    print("could not read the value")
    raise
```
Написать вместо этого `raise ValueError(...)` значило бы создать новое исключение с собственной трассировкой — это была бы уже не та же ошибка, которую вы поймали, а именно эту идентичность и сохраняет одинокий `raise`.

# --instructions--

Расположите элементы по порядку, чтобы получить программу, которая определяет `load`, выводит результат загрузки `"5"`, а затем вызывает её с `"abc"` внутри блока `try`, сообщающего о сбое, — сначала сообщение от `load`, а затем от вызывающего

# --answers--

```python
def load(text):
```

```python
    try:
        return int(text)
```

```python
    except ValueError:
        print(f"could not read {text}")
        raise
```

```python
print(load("5"))
```

```python
try:
    load("abc")
except ValueError:
    print("handled by the caller")
```

# --solutions--

```python
def load(text):
    try:
        return int(text)
    except ValueError:
        print(f"could not read {text}")
        raise
print(load("5"))
try:
    load("abc")
except ValueError:
    print("handled by the caller")
```

# --output--

5
could not read abc
handled by the caller
