---
language: python
exerciseType: 1
---

# --description--

Исключение — это объект, и `as` привязывает его к имени, чтобы обработчик мог на него посмотреть:
```python
try:
    int("abc")
except ValueError as e:
    print(e)                   # invalid literal for int() with base 10: 'abc'
    print(type(e).__name__)    # ValueError
```
`str(e)` — его используют `print(e)` и f-слот — даёт сообщение, с которым исключение было создано, а `type(e).__name__` даёт имя класса в виде текста. Имя, привязанное через `as`, существует только внутри блока `except`; Python удаляет его, когда блок заканчивается.

# --instructions--

Напишите функцию `error_label(text)`, которая возвращает `int(text)`, а когда тот вызывает `ValueError`, возвращает строку из имени класса исключения, `": "` и сообщения исключения, например `"ValueError: ..."`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`error_label("8")` должна возвращать `8`

```python
    def test_1(self):
        self.assertEqual(error_label("8"), 8, "--err-t1--")
```

`error_label("abc")` должна начинаться с имени класса

```python
    def test_2(self):
        self.assertTrue(error_label("abc").startswith("ValueError: "), "--err-t2--")
```

`error_label("abc")` должна также содержать сообщение, в котором упоминается `abc`

```python
    def test_3(self):
        self.assertIn("abc", error_label("abc")[12:], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def error_label(text):
    try:
        return int(text)
    except ValueError as e:
        return f"{type(e).__name__}: {e}"
```
