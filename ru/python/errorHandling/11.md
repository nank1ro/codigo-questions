---
language: python
exerciseType: 1
---

# --description--

Блок `finally` выполняется **при любом исходе**: после безошибочного блока `try`, после блока `except`, даже пока неперехваченное исключение распространяется наружу, и даже когда блок `try` или `except` выполняет `return`:
```python
def read(text):
    try:
        return int(text)
    except ValueError:
        return 0
    finally:
        print("done")
```
Оба пути выводят `done` до того, как значение покинет функцию. Именно ради такой гарантии существует `finally`: закрыть файл, освободить блокировку, вернуть настройку. Полная форма — `try` / `except` / `else` / `finally`; блоку `try` нужен хотя бы один `except` или `finally`, а `else` работает только вместе с `except`.

# --instructions--

Напишите функцию `record(text)`, которая начинает с пустого списка, добавляет в него `int(text)`, добавляет вместо этого строку `"bad"`, когда `int(text)` вызывает `ValueError`, всегда добавляет `"done"` в блоке `finally` и возвращает список

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`record("5")` должна возвращать `[5, "done"]`

```python
    def test_1(self):
        self.assertEqual(record("5"), [5, "done"], "--err-t1--")
```

`record("x")` должна возвращать `["bad", "done"]`

```python
    def test_2(self):
        self.assertEqual(record("x"), ["bad", "done"], "--err-t2--")
```

каждый вызов должен начинаться с нового списка

```python
    def test_3(self):
        record("1")
        self.assertEqual(record("2"), [2, "done"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def record(text):
    log = []
    try:
        log.append(int(text))
    except ValueError:
        log.append("bad")
    finally:
        log.append("done")
    return log
```
