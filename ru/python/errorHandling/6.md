---
language: python
exerciseType: 2
---

# --description--

За одним блоком `try` могут следовать **несколько** веток `except`, каждая из которых обрабатывает свой сбой своим способом восстановления. Python сравнивает вызванное исключение с ними сверху вниз и выполняет **первую** подходящую; остальные пропускаются:
```python
try:
    value = 100 / int(text)
except ValueError:
    print("not a number")
except ZeroDivisionError:
    print("cannot divide by zero")
```
Поскольку побеждает первое совпадение, порядок важен, когда типы связаны: ветка для общего типа, поставленная над веткой для более конкретного, всегда побеждала бы, делая конкретную ветку недостижимой.

# --instructions--

Дополните код так, чтобы нечитаемый текст и нулевой делитель сообщались отдельно, давая вывод `2.0 not a number cannot divide by zero`

# --seed--

```python
def describe(text):
    try:
        return 10 / int(text)
    except [/]:
        return "not a number"
    except [/]:
        return "cannot divide by zero"

print(describe("5"), describe("abc"), describe("0"))
```

# --answers--

- ValueError
- ZeroDivisionError
- TypeError
- KeyError

# --solutions--

```python
def describe(text):
    try:
        return 10 / int(text)
    except ValueError:
        return "not a number"
    except ZeroDivisionError:
        return "cannot divide by zero"

print(describe("5"), describe("abc"), describe("0"))
```

# --output--

2.0 not a number cannot divide by zero
