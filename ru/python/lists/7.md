---
language: python
exerciseType: 2
---

# --description--

Строку можно нарезать точно так же, как список! По сути, строку можно рассматривать как список символов: каждый символ является последовательным элементом списка, начиная с индекса `0`.
```python
list_name[:2]
# Берёт первые два элемента
list_name[3:]
# Берёт элементы с четвёртого до последнего
```
Если срез включает самый первый или последний элемент списка (или строки), индекс для этого элемента можно не указывать.

# --instructions--

- Присвойте переменной `first_name` срез `full_name` от индекса `0` до индекса `4` (*не включая* его).
- Присвойте переменной `last_name` срез `full_name` от индекса `4` до конца строки

# --seed--

```python
full_name = "Sam Smith"
first_name = [/][/]
last_name = [/][/]
print(first_name)
print(last_name)
```

# --answers--

- full_name
- [:4]
- [:3]
- full_name
- [4:]
- [4:2]

# --solutions--

```python
full_name = "Sam Smith"
first_name = full_name[:3]
last_name = full_name[4:]
print(first_name)
print(last_name)
```

# --output--

Sam
Smith
