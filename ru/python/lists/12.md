---
language: python
exerciseType: 2
---

# --description--

Иногда может потребоваться преобразовать кортеж в список.
Для этого можно использовать функцию `list()`

# --instructions--

Используйте функцию `list()`, чтобы преобразовать кортеж в список

# --seed--

```python
tuple1 = ("dog", "bear", "cat")
list1 = [/]
list1.append("frog")
print(list1)
```

# --answers--

- list[tuple1]
- list(tuple1)

# --solutions--

```python
tuple1 = ("dog", "bear", "cat")
list1 = list(tuple1)
list1.append("frog")
print(list1)
```

# --output--

['dog', 'bear', 'cat', 'frog']
