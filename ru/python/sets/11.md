---
language: python
exerciseType: 4
---

# --description--

Множества также можно **сравнивать** друг с другом.
`a.issubset(b)`, или `a <= b`, равно `True`, когда каждый элемент `a` также есть в `b`.
`a.issuperset(b)`, или `a >= b`, равно `True`, когда `a` содержит каждый элемент `b`.
`a.isdisjoint(b)` равно `True`, когда у двух множеств **нет** общих элементов:
```python
small = {1, 2}
big = {1, 2, 3}
print(small <= big)              # True
print(big.issuperset(small))     # True
print(small.isdisjoint({8, 9}))  # True
```

# --instructions--

Расположите элементы по порядку, чтобы построить `palette` из `primary`, а затем вывести, в этом порядке: является ли `primary` подмножеством `palette`, является ли `palette` подмножеством `primary`, и не пересекаются ли `primary` и `{"black"}`

# --answers--

```python
primary = {"red", "blue", "yellow"}
```

```python
palette = primary | {"green"}
```

```python
print(primary <= palette)
```

```python
print(palette.issubset(primary))
```

```python
print(primary.isdisjoint({"black"}))
```

# --solutions--

```python
primary = {"red", "blue", "yellow"}
palette = primary | {"green"}
print(primary <= palette)
print(palette.issubset(primary))
print(primary.isdisjoint({"black"}))
```

# --output--

True
False
True
