---
language: python
exerciseType: 1
---

# --description--

Множество может содержать только **хешируемые** элементы, то есть значения, которые не могут изменяться: числа, строки, `True`/`False` и **кортежи**.
Попытка добавить список, словарь или другое множество вызывает `TypeError`:
```python
points = set()
points.add((1, 2))  # ok, a tuple
points.add([1, 2])  # TypeError: unhashable type: 'list'
```
Множества кортежей удобны для отслеживания уникальных пар, таких как координаты или записи (name, age):
```python
visits = [(1, 2), (1, 2), (3, 4)]
print(len(set(visits)))  # 2
```

# --instructions--

Сохраните в `points` множество различающихся координат из `visits`, добавьте в него точку `(0, 0)`, затем сохраните в `count`, сколько различающихся точек оно содержит

# --seed--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Необходимо использовать `add()`

```json
{
  "regex": "\\.add\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`points` должно быть равно `{(0, 0), (1, 2), (3, 4), (5, 6)}`

```python
    def test_1(self):
        self.assertEqual(points, {(0, 0), (1, 2), (3, 4), (5, 6)}, "--err-t1--")
```

`count` должно быть равно `4`

```python
    def test_2(self):
        self.assertEqual(count, 4, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
points = set(visits)
points.add((0, 0))
count = len(points)
```
