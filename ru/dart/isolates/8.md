---
language: dart
exerciseType: 1
---

# --description--

`Isolate.spawn` передаёт ровно **одно** сообщение в точку входа, а работнику обычно нужны и `SendPort` для ответа, и какие-то данные для работы. Обычный приём — упаковать всё в один `List` и распаковать на той стороне:

```dart
void multiply(List<Object> message) {
  final port = message[0] as SendPort;
  final value = message[1] as int;
  port.send(value * 2);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(multiply, <Object>[receivePort.sendPort, 21]);
  print(await receivePort.first); // 42
}
```

Список копируется на входе, поэтому работник читает свои собственные значения. `SendPort` — одна из немногих вещей, которые не копируются, а разделяются: он продолжает указывать на почтовый ящик изолята, который его создал, и именно поэтому его можно использовать как обратный адрес.

# --instructions--

Напиши функцию верхнего уровня `sendSum`, которая принимает `List<Object>` `message`, где элемент `0` — это `SendPort`, а элементы `1` и `2` — `int`, и отправляет их сумму в порт.

Затем напиши функцию `addInIsolate`, которая принимает два `int` `a` и `b` и возвращает `Future<int>`, завершающийся суммой, вычисленной запущенным изолятом `sendSum`.

Пример:
```dart
print(await addInIsolate(2, 3)); // 5
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendSum` должна отправить сумму двух чисел сообщения в порт

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendSum(<Object>[port.sendPort, 4, 6]);
    expect(await port.first, 10, reason: '--err-t1--');
  });
```

`addInIsolate(2, 3)` должна завершиться значением `5`

```dart
  test('test2', () async {
    expect(await addInIsolate(2, 3), 5, reason: '--err-t2--');
  });
```

`addInIsolate(10, -4)` должна завершиться значением `6`, а `addInIsolate` должна возвращать `Future<int>`

```dart
  test('test3', () async {
    expect(await addInIsolate(10, -4), 6, reason: '--err-t3--');
    expect(addInIsolate(1, 1), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendSum(List<Object> message) {
  final port = message[0] as SendPort;
  final a = message[1] as int;
  final b = message[2] as int;
  port.send(a + b);
}

Future<int> addInIsolate(int a, int b) async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendSum, <Object>[receivePort.sendPort, a, b]);
  return await receivePort.first as int;
}
```
