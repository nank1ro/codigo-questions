---
language: dart
exerciseType: 4
---

# --description--

Запущенный изолят всегда проходит одни и те же четыре шага: открыть почтовый ящик, запустить работника с его адресом, дождаться ответа, использовать его.

```dart
import 'dart:isolate';

void worker(SendPort port) {
  port.send('done');
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

`await` перед `Isolate.spawn` ждёт *запуска* изолята, а не завершения его работы: результат приходит позже, через порт.

# --instructions--

Расставь элементы так, чтобы собрать программу, которая запускает изолят `worker`, отправляющий `7 * 6` обратно главному изоляту, который выводит его. Импорт поставь первым, а `worker` объяви до `main`. Программа должна вывести `42`.

# --answers--

```dart
import 'dart:isolate';
```

```dart
void worker(SendPort port) {
```

```dart
  port.send(7 * 6);
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  final receivePort = ReceivePort();
```

```dart
  await Isolate.spawn(worker, receivePort.sendPort);
```

```dart
  print(await receivePort.first);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
void worker(SendPort port) {
  port.send(7 * 6);
}
Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

# --output--

42
