---
language: dart
exerciseType: 1
---

# --description--

Функция, которую ты передаёшь в `Isolate.run`, может **захватывать** переменные из окружения. Эти значения копируются в новый изолят вместе с функцией, поэтому вычисление может зависеть от вызывающего кода:

```dart
Future<int> triple(int n) {
  return Isolate.run(() => n * 3);
}
```

`Isolate.run` возвращает `Future` с тем, что возвращает функция, поэтому `triple` может просто вернуть его: ни `async`, ни `await` не нужны, когда ты просто пробрасываешь future дальше.

Смысл переноса работы в другой изолят в том, что долгие вычисления больше не замораживают главный. Цикл, работающий секунду, блокирует всё, пока он выполняется на главном изоляте; внутри `Isolate.run` он выполняется в другом месте, а главный изолят продолжает обрабатывать свои события.

# --instructions--

Напиши функцию `sumTo`, которая принимает `int` `n` и возвращает `Future<int>`, завершающийся суммой чисел от `1` до `n`, вычисленной на другом изоляте с помощью `Isolate.run`.

Пример:
```dart
print(await sumTo(4)); // 10
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumTo(4)` должна завершиться значением `10`

```dart
  test('test1', () async {
    expect(await sumTo(4), 10, reason: '--err-t1--');
  });
```

`sumTo(100)` должна завершиться значением `5050`, а `sumTo(1)` — значением `1`

```dart
  test('test2', () async {
    expect(await sumTo(100), 5050, reason: '--err-t2--');
    expect(await sumTo(1), 1, reason: '--err-t2--');
  });
```

`sumTo(0)` должна завершиться значением `0`, а `sumTo` должна возвращать `Future<int>`

```dart
  test('test3', () async {
    expect(await sumTo(0), 0, reason: '--err-t3--');
    expect(sumTo(3), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<int> sumTo(int n) {
  return Isolate.run(() {
    var total = 0;
    for (var i = 1; i <= n; i++) {
      total += i;
    }
    return total;
  });
}
```
