---
language: dart
exerciseType: 2
---

# --description--

`first` читает одно сообщение и закрывает почтовый ящик. `ReceivePort` — это ещё и **`Stream`**, поэтому чтобы прочитать много сообщений, перебирай его циклом `await for`.

Цикл сам по себе никогда не заканчивается: порт остаётся открытым в ожидании сообщения, которое может никогда не прийти. Поэтому работник отправляет последнее значение как сигнал, часто `null`, а слушатель реагирует вызовом **`close()`**, который завершает поток и цикл:

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(countdown, receivePort.sendPort);
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --instructions--

Дополни код так, чтобы запущенный изолят отправил `3`, `2`, `1`, а затем `null`, а главный изолят вывел каждое число, закрыл почтовый ящик на `null` и в конце вывел `done`.

# --seed--

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.[/](countdown, receivePort.sendPort);
  await [/] (final message in receivePort) {
    if (message == null) {
      receivePort.[/]();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --answers--

- spawn
- for
- close
- run
- while
- listen
- first

# --solutions--

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(countdown, receivePort.sendPort);
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --output--

3
2
1
done
