---
language: dart
exerciseType: 1
---

# --description--

Между изолятами копируются только **данные**; **код** — нет. Каждый изолят программы и так видит все функции верхнего уровня и классы этой программы, поэтому вычисление, переданное в `Isolate.run`, может свободно вызывать их:

```dart
int slowLength(String text) => text.length;

Future<int> lengthInIsolate(String text) {
  return Isolate.run(() => slowLength(text));
}
```

В пути находятся захваченный `text` на входе и результирующий `int` на выходе, каждый — копия. Схема всегда одна и та же: оставь тяжёлую функцию там, где она есть, и оберни **вызов** в `Isolate.run`.

# --instructions--

Функция `wordCount(text)` считает слова строки. Напиши функцию `countWords`, которая принимает `String` `text` и возвращает `Future<int>`, завершающийся значением `wordCount(text)`, вычисленным на другом изоляте с помощью `Isolate.run`.

Пример:
```dart
print(await countWords('hello isolate world')); // 3
```

# --before-seed--

```dart
import 'dart:isolate';

int wordCount(String text) {
  return text.split(' ').where((w) => w.isNotEmpty).length;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countWords('hello isolate world')` должна завершиться значением `3`

```dart
  test('test1', () async {
    expect(await countWords('hello isolate world'), 3, reason: '--err-t1--');
  });
```

`countWords('Dart')` должна завершиться значением `1`, а `countWords('')` — значением `0`

```dart
  test('test2', () async {
    expect(await countWords('Dart'), 1, reason: '--err-t2--');
    expect(await countWords(''), 0, reason: '--err-t2--');
  });
```

`countWords('  a  b ')` должна завершиться значением `2`, а `countWords` должна возвращать `Future<int>`

```dart
  test('test3', () async {
    expect(await countWords('  a  b '), 2, reason: '--err-t3--');
    expect(countWords('one two'), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<int> countWords(String text) {
  return Isolate.run(() => wordCount(text));
}
```
