---
language: dart
exerciseType: 4
---

# --description--

Если собрать всё вместе, программа с `Isolate.run` читается как обычный последовательный код: строка перед вызовом выполняется на главном изоляте, вычисление выполняется в другом месте, а строка после `await` снова выполняется на главном изоляте, уже с результатом на руках.

```dart
import 'dart:isolate';

int twice(int n) => n * 2;

Future<void> main() async {
  print('start');
  final result = await Isolate.run(() => twice(4));
  print(result);
}
// start
// 8
```

# --instructions--

Расставь элементы так, чтобы собрать программу, которая выводит `start`, возводит `9` в квадрат на другом изоляте с помощью `Isolate.run` и выводит результат. Импорт поставь первым, а `square` объяви до `main`. Программа должна вывести:

```
start
81
```

# --answers--

```dart
import 'dart:isolate';
```

```dart
int square(int n) => n * n;
```

```dart
Future<void> main() async {
```

```dart
  print('start');
```

```dart
  final result = await Isolate.run(() => square(9));
```

```dart
  print(result);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
int square(int n) => n * n;
Future<void> main() async {
  print('start');
  final result = await Isolate.run(() => square(9));
  print(result);
}
```

# --output--

start
81
