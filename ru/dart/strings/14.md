---
language: dart
exerciseType: 1
---

# --description--

Под капотом каждый символ строки хранится как число, его **code unit** (код UTF-16). `.codeUnitAt(index)` даёт код одного символа, а `.codeUnits` — весь список. `String.fromCharCode(code)` делает обратное, строя строку из кода:

```dart
var word = 'AB';
print(word.codeUnitAt(0));         // 65
print(word.codeUnits);             // [65, 66]
print(String.fromCharCode(67));    // C
```

Последовательные буквы имеют последовательные коды: `'A'` — это 65, `'B'` — 66, и так далее.

# --instructions--

Дана строка `letter`, сохраните в `int` по имени `code` её code unit, а в `String` по имени `next` — символ, код которого равен `code + 1`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var letter = 'D';
```

# --after-seed--

```dart
  return [code, next];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`code` должно быть `68`

```dart
  test('test1', () {
    expect(result()[0], 68, reason: '--err-t1--');
  });
```

`next` должно быть `'E'`

```dart
  test('test2', () {
    expect(result()[1], 'E', reason: '--err-t2--');
  });
```

`next` должно быть типа `String`

```dart
  test('test3', () {
    expect(result()[1], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var letter = 'D';
int code = letter.codeUnitAt(0);
String next = String.fromCharCode(code + 1);
```
