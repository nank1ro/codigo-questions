---
language: dart
exerciseType: 1
---

# --description--

Поскольку строки неизменяемы, построение длинного текста с помощью `+=` в цикле создаёт новую строку на каждом шаге. **StringBuffer** эффективно собирает фрагменты текста и создаёт итоговую строку только тогда, когда вы её запрашиваете:

- `.write(value)` добавляет значение (любой тип преобразуется в текст)
- `.writeln(value)` добавляет значение, за которым следует перенос строки
- `.toString()` возвращает строку, собранную к этому моменту

```dart
var buffer = StringBuffer();
buffer.write('Hello');
buffer.write(', ');
buffer.writeln('Dart!');
buffer.write(42);
print(buffer.toString()); // Hello, Dart!\n42
```

# --instructions--

Дан список `items`, используйте `StringBuffer`, чтобы построить `String` по имени `menu`, где каждый элемент находится на своей строке, начинаясь с его позиции (начиная с `1`) и точки, например `1. milk`. Каждая строка должна заканчиваться переносом строки (используйте `.writeln()`).

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var items = ['milk', 'eggs', 'bread'];
```

# --after-seed--

```dart
  return menu;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`menu` должно быть `'1. milk\n2. eggs\n3. bread\n'`

```dart
  test('test1', () {
    expect(result(), '1. milk\n2. eggs\n3. bread\n', reason: '--err-t1--');
  });
```

`menu` должно быть типа `String`

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var items = ['milk', 'eggs', 'bread'];
var buffer = StringBuffer();
for (var i = 0; i < items.length; i++) {
  buffer.writeln('${i + 1}. ${items[i]}');
}
String menu = buffer.toString();
```
