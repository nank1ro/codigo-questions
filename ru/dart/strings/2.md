---
language: dart
exerciseType: 1
---

# --description--

Две строки можно объединить в новую с помощью оператора `+`, который называется **конкатенацией**:

```dart
var greeting = 'Hello' + ', ' + 'Dart';
print(greeting); // Hello, Dart
```

Dart также объединяет два строковых **литерала**, записанных рядом друг с другом, без какого-либо оператора. Это удобно для разбиения длинного текста на несколько строк:

```dart
var text = 'Hello, '
    'Dart';
print(text); // Hello, Dart
```

С помощью `+` можно объединять только строки: `'Age: ' + 30` — это ошибка компиляции, потому что `30` имеет тип `int`.

# --instructions--

Даны переменные `first` и `last`, создайте `String` по имени `full`, содержащую имя, пробел и фамилию.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var first = 'Ada';
var last = 'Lovelace';
```

# --after-seed--

```dart
  return full;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`full` должно быть `'Ada Lovelace'`

```dart
  test('test1', () {
    expect(result(), 'Ada Lovelace', reason: '--err-t1--');
  });
```

`full` должно быть типа `String`

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var first = 'Ada';
var last = 'Lovelace';
String full = first + ' ' + last;
```
