---
language: dart
exerciseType: 1
---

# --description--

Каждая строка знает, сколько символов она содержит, через своё свойство `.length`. Пробелы и знаки препинания тоже считаются символами:

```dart
var word = 'hello';
print(word.length);      // 5
print('a b'.length);     // 3
```

Можно прочитать один символ с помощью квадратных скобок и его **индекса**, начиная с `0`. Результатом будет `String` из одного символа:

```dart
print(word[0]);                // h
print(word[word.length - 1]);  // o
```

Чтение индекса за пределами строки (например, `word[5]`) вызывает ошибку.

# --instructions--

Дана строка `word`, сохраните в `int` по имени `size` количество её символов, в `String` по имени `first` её первый символ, а в `String` по имени `last` — её последний символ.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var word = 'Flutter';
```

# --after-seed--

```dart
  return [size, first, last];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`size` должно быть `7`

```dart
  test('test1', () {
    expect(result()[0], 7, reason: '--err-t1--');
  });
```

`first` должно быть `'F'`

```dart
  test('test2', () {
    expect(result()[1], 'F', reason: '--err-t2--');
  });
```

`last` должно быть `'r'`

```dart
  test('test3', () {
    expect(result()[2], 'r', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var word = 'Flutter';
int size = word.length;
String first = word[0];
String last = word[word.length - 1];
```
