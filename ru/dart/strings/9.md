---
language: dart
exerciseType: 2
---

# --description--

Метод `.replaceAll(from, to)` возвращает новую строку, в которой **каждое** вхождение `from` заменено на `to`. `.replaceFirst(from, to)` заменяет только первое вхождение:

```dart
var text = 'a-b-c';
print(text.replaceAll('-', '+'));    // a+b+c
print(text.replaceFirst('-', '+'));  // a+b-c
```

# --instructions--

Дополните код, чтобы заменить каждое `cats` на `dogs` и вывести результат.

# --seed--

```dart
var text = 'cats here, cats there';
print(text.[/]('cats', [/]));
```

# --answers--

- replaceAll
- 'dogs'
- replaceFirst
- replace
- 'cats'
- dogs

# --solutions--

```dart
var text = 'cats here, cats there';
print(text.replaceAll('cats', 'dogs'));
```

# --output--

dogs here, dogs there
