---
language: dart
exerciseType: 4
---

# --description--

Текст, введённый пользователем, часто содержит лишние пробелы вокруг. Метод `.trim()` возвращает копию строки без начальных и конечных пробельных символов (пробелов, табуляций и переносов строк). `.trimLeft()` и `.trimRight()` убирают их только с одной стороны:

```dart
var input = '   hello  ';
print('[${input.trim()}]');      // [hello]
print('[${input.trimLeft()}]');  // [hello  ]
```

# --instructions--

Отсортируйте строки кода, чтобы убрать пробелы вокруг `input` и вывести очищенный текст в верхнем регистре.

# --answers--

```dart
void main() {
```

```dart
  var input = '  hello  ';
```

```dart
  var clean = input.trim();
```

```dart
  print(clean.toUpperCase());
```

```dart
}
```

# --solutions--

```dart
void main() {
  var input = '  hello  ';
  var clean = input.trim();
  print(clean.toUpperCase());
}
```

# --output--

HELLO
