---
language: dart
exerciseType: 1
---

# --description--

Несколько методов позволяют искать внутри строки:

- `.contains(other)` возвращает `true`, если `other` встречается где-либо в строке
- `.startsWith(other)` и `.endsWith(other)` проверяют начало и конец
- `.indexOf(other)` возвращает индекс первого вхождения или `-1`, если оно не найдено

```dart
var file = 'report.pdf';
print(file.contains('port'));    // true
print(file.startsWith('rep'));   // true
print(file.endsWith('.txt'));    // false
print(file.indexOf('.'));        // 6
print(file.indexOf('x'));        // -1
```

Все они чувствительны к регистру: `'Dart'.contains('dart')` — это `false`.

# --instructions--

Дана строка `email`, сохраните в `bool` по имени `hasAt`, содержит ли она символ `@`, в `int` по имени `atIndex` — индекс этого символа, а в `bool` по имени `isExample` — заканчивается ли email на `example.com`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var email = 'ada@example.com';
```

# --after-seed--

```dart
  return [hasAt, atIndex, isExample];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasAt` должно быть `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`atIndex` должно быть `3`

```dart
  test('test2', () {
    expect(result()[1], 3, reason: '--err-t2--');
  });
```

`isExample` должно быть `true`

```dart
  test('test3', () {
    expect(result()[2], true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var email = 'ada@example.com';
bool hasAt = email.contains('@');
int atIndex = email.indexOf('@');
bool isExample = email.endsWith('example.com');
```
