---
language: dart
exerciseType: 1
---

# --description--

Сложенные миксины — это способ объединить маленькие независимые правила в одном классе. Каждый миксин переопределяет один и тот же член, выполняет свою часть работы и вызывает `super`, чтобы передать работу дальше. Поскольку вызов сначала входит в **последний** миксин, порядок списка `with` решает, какое правило сработает раньше какого:

```dart
class Account {
  int balance = 0;

  void deposit(int amount) {
    balance += amount;
  }
}

mixin Doubled on Account {
  @override
  void deposit(int amount) {
    super.deposit(amount * 2);
  }
}
```

`class A extends Account with Doubled {}` удваивает каждый вклад. Добавь второй миксин после `Doubled`, и он получит вклад первым, прежде чем его увидит `Doubled`.

# --instructions--

Класс `Account` уже написан: у него есть поле `balance` типа `int`, начинающееся с `0`, и метод `deposit`, который прибавляет свой `int`-аргумент к `balance`.

Объяви миксин `Audited` с условием `on Account`, у которого есть поле `log` типа `List<String>`, начинающееся пустым, и который переопределяет `deposit` так, что сначала добавляет в `log` строку `'deposit '` с суммой, а затем вызывает `super.deposit`.

Объяви миксин `Limited` с условием `on Account`, который переопределяет `deposit` так, что сумма больше `100` игнорируется, а любая другая передаётся дальше в `super.deposit`.

Объяви класс `SafeAccount`, который наследует `Account` и использует оба миксина, чтобы отклонённый вклад никогда не попадал в журнал.

Пример:
```dart
final account = SafeAccount();
account.deposit(30);
account.deposit(500);
print(account.balance); // 30
print(account.log);     // [deposit 30]
```

# --before-seed--

```dart
class Account {
  int balance = 0;

  void deposit(int amount) {
    balance += amount;
  }
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Вклад `50` должен быть принят и записан в журнал

```dart
  test('test1', () {
    final account = SafeAccount();
    account.deposit(50);
    expect(account.balance, 50, reason: '--err-t1--');
    expect(account.log, ['deposit 50'], reason: '--err-t1--');
  });
```

Вклад `500` должен быть проигнорирован и не должен попасть в журнал

```dart
  test('test2', () {
    final account = SafeAccount();
    account.deposit(500);
    expect(account.balance, 0, reason: '--err-t2--');
    expect(account.log, <String>[], reason: '--err-t2--');
  });
```

Учитываться должны только принятые вклады, а `100` всё ещё должен быть принят

```dart
  test('test3', () {
    final account = SafeAccount();
    account.deposit(30);
    account.deposit(200);
    account.deposit(70);
    account.deposit(100);
    expect(account.balance, 200, reason: '--err-t3--');
    expect(account.log, ['deposit 30', 'deposit 70', 'deposit 100'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Audited on Account {
  final List<String> log = [];

  @override
  void deposit(int amount) {
    log.add('deposit $amount');
    super.deposit(amount);
  }
}

mixin Limited on Account {
  @override
  void deposit(int amount) {
    if (amount > 100) return;
    super.deposit(amount);
  }
}

class SafeAccount extends Account with Audited, Limited {}
```
