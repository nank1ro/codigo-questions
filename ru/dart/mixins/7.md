---
language: dart
exerciseType: 1
---

# --description--

Класс может использовать **несколько миксинов сразу**, перечисленных после `with` и разделённых запятыми. Dart применяет их **слева направо**, накладывая каждый поверх предыдущего, поэтому когда два миксина объявляют один и тот же член, побеждает **последний** в списке:

```dart
mixin A {
  String who() => 'A';
}

mixin B {
  String who() => 'B';
}

class First with A, B {}
class Second with B, A {}

void main() {
  print(First().who());  // B, the last mixin in the list
  print(Second().who()); // A, the last mixin in the list
}
```

Такое наложение называется **линеаризацией**: `with A, B` строит цепочку `Object` → `A` → `B` → сам класс.

# --instructions--

Объяви миксин `Quiet` с методом `volume`, возвращающим `'quiet'`, и миксин `Loud` с методом `volume`, возвращающим `'loud'`.

Объяви класс `Speaker`, который использует оба миксина так, чтобы `Speaker().volume()` был `'loud'`, и класс `Whisperer`, который использует оба миксина так, чтобы `Whisperer().volume()` был `'quiet'`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Speaker().volume()` должен быть `'loud'`

```dart
  test('test1', () {
    expect(Speaker().volume(), 'loud', reason: '--err-t1--');
  });
```

`Whisperer().volume()` должен быть `'quiet'`

```dart
  test('test2', () {
    expect(Whisperer().volume(), 'quiet', reason: '--err-t2--');
  });
```

Оба класса должны использовать оба миксина

```dart
  test('test3', () {
    expect(Speaker(), isA<Quiet>(), reason: '--err-t3--');
    expect(Speaker(), isA<Loud>(), reason: '--err-t3--');
    expect(Whisperer(), isA<Quiet>(), reason: '--err-t3--');
    expect(Whisperer(), isA<Loud>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Quiet {
  String volume() => 'quiet';
}

mixin Loud {
  String volume() => 'loud';
}

class Speaker with Quiet, Loud {}

class Whisperer with Loud, Quiet {}
```
