---
language: dart
exerciseType: 2
---

# --description--

Поскольку побеждает последний миксин, порядок списка `with` — часть смысла класса, а не деталь стиля. Изменив порядок, ты меняешь то, какая реализация в итоге окажется у объекта:

```dart
mixin Plain {
  String format(String text) => text;
}

mixin Starred {
  String format(String text) => '*$text*';
}

class Fancy with Plain, Starred {}  // format comes from Starred
class Simple with Starred, Plain {} // format comes from Plain
```

Члены, которые объявляет только один миксин, никогда не конкурируют: они доступны при любом порядке. Читай `with X, Y` как «начни с `X`, затем позволь `Y` переопределить его».

# --instructions--

Дополни список `with` так, чтобы `Message` использовал каждый миксин ровно один раз, его `format` оставлял текст без изменений, а `frame` был доступен.

# --seed--

```dart
mixin Plain {
  String format(String text) => text;
}

mixin Starred {
  String format(String text) => '*$text*';

  String frame(String text) => '($text)';
}

class Message with [/], [/] {}

void main() {
  print(Message().format('hi'));
  print(Message().frame('hi'));
}
```

# --answers--

- Starred
- Plain
- Message
- format
- frame

# --solutions--

```dart
mixin Plain {
  String format(String text) => text;
}

mixin Starred {
  String format(String text) => '*$text*';

  String frame(String text) => '($text)';
}

class Message with Starred, Plain {}

void main() {
  print(Message().format('hi'));
  print(Message().frame('hi'));
}
```

# --output--

hi
(hi)
