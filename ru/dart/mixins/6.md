---
language: dart
exerciseType: 4
---

# --description--

Если собрать всё вместе, программа, использующая миксин, состоит из трёх частей: объявления `mixin`, одного или нескольких классов, подхватывающих его через `with`, и кода, вызывающего общий член.

```dart
mixin Barker {
  String bark() => 'Woof!';
}

class Dog with Barker {}

void main() {
  print(Dog().bark()); // Woof!
}
```

Объявления верхнего уровня в Dart можно писать в любом порядке, но читать файл сверху вниз проще, когда миксин идёт перед классами, которые его используют.

# --instructions--

Расставь элементы так, чтобы определить миксин `Purrer` с методом `purr`, возвращающим `Purr...`, класс `Cat`, который его использует, и `main`, который выводит результат `purr`. Сначала определи миксин, затем класс, затем `main`. Программа должна вывести:

```
Purr...
```

# --answers--

```dart
mixin Purrer {
```

```dart
  String purr() => 'Purr...';
```

```dart
}
```

```dart
class Cat with Purrer {}
```

```dart
void main() {
```

```dart
  print(Cat().purr());
```

```dart
}
```

# --solutions--

```dart
mixin Purrer {
  String purr() => 'Purr...';
}

class Cat with Purrer {}

void main() {
  print(Cat().purr());
}
```

# --output--

Purr...
