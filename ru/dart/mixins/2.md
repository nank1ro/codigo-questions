---
language: dart
exerciseType: 1
---

# --description--

Тело миксина выглядит как тело класса: методы, геттеры и поля записываются точно так же. Разница — в том, что можно делать с самим объявлением.

```dart
mixin Timestamped {
  String get stamp => '[log]';

  String format(String text) => '$stamp $text';
}

class Server with Timestamped {}
class Printer with Timestamped {}
```

Имя миксина — это ещё и **тип**, поэтому `Server() is Timestamped` равно `true`, а переменную можно объявить как `Timestamped t = Server();`. Теперь два никак не связанных класса используют одну реализацию, не наследуясь при этом друг от друга.

# --instructions--

Объяви миксин `Shouter` с методом `shout`, который принимает `String` `text` и возвращает его в верхнем регистре с восклицательным знаком на конце.

Объяви класс `Speaker`, который использует `Shouter` и не добавляет ничего своего.

Пример:
```dart
print(Speaker().shout('hi')); // HI!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Speaker().shout('hi')` должен быть `'HI!'`

```dart
  test('test1', () {
    expect(Speaker().shout('hi'), 'HI!', reason: '--err-t1--');
  });
```

`Speaker().shout('Dart rocks')` должен быть `'DART ROCKS!'`, а `Speaker().shout('')` — `'!'`

```dart
  test('test2', () {
    expect(Speaker().shout('Dart rocks'), 'DART ROCKS!', reason: '--err-t2--');
    expect(Speaker().shout(''), '!', reason: '--err-t2--');
  });
```

`Speaker` должен получить `shout` из миксина, поэтому `Speaker` также должен быть `Shouter`

```dart
  test('test3', () {
    expect(Speaker(), isA<Shouter>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Shouter {
  String shout(String text) => '${text.toUpperCase()}!';
}

class Speaker with Shouter {}
```
