---
language: dart
exerciseType: 1
---

# --description--

Тело класса стоит в самом конце цепочки, поэтому член, объявленный в классе, **переопределяет** одноимённый член из любого его миксина. Внутри переопределения **`super`** достаёт версию, предоставленную миксином:

```dart
mixin Polite {
  String greet() => 'Hello';
}

class Host with Polite {
  @override
  String greet() => '${super.greet()}, welcome!';
}

class Guest with Polite {}

void main() {
  print(Host().greet());  // Hello, welcome!
  print(Guest().greet()); // Hello
}
```

Сам миксин при этом не тронут: `Guest` по-прежнему получает оригинальный `greet`. Именно `super.greet()` позволяет `Host` строить на общем поведении, а не копировать его.

# --instructions--

Объяви миксин `Signed` с методом `sign`, возвращающим `'signed'`.

Объяви класс `Letter`, который использует `Signed` и переопределяет `sign` так, чтобы он возвращал результат миксина с добавленным `' by Ada'`, полученным вызовом `super`.

Объяви класс `Note`, который использует `Signed` и ничего не переопределяет.

Пример:
```dart
print(Letter().sign()); // signed by Ada
print(Note().sign());   // signed
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Letter().sign()` должен быть `'signed by Ada'`

```dart
  test('test1', () {
    expect(Letter().sign(), 'signed by Ada', reason: '--err-t1--');
  });
```

`Note().sign()` по-прежнему должен быть `'signed'`, потому что миксин не изменился

```dart
  test('test2', () {
    expect(Note().sign(), 'signed', reason: '--err-t2--');
  });
```

Оба класса должны использовать миксин, и переопределение должно работать и через тип миксина

```dart
  test('test3', () {
    final Signed letter = Letter();
    expect(letter.sign(), 'signed by Ada', reason: '--err-t3--');
    expect(Note(), isA<Signed>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Signed {
  String sign() => 'signed';
}

class Letter with Signed {
  @override
  String sign() => '${super.sign()} by Ada';
}

class Note with Signed {}
```
