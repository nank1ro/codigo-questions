---
language: dart
exerciseType: 1
---

# --description--

Enum может **реализовывать интерфейс** с помощью ключевого слова `implements`. При этом enum обязуется предоставить каждый член, объявленный в интерфейсе, и его значения можно использовать везде, где ожидается этот тип интерфейса:

```dart
abstract class Describable {
  String describe();
}

enum Animal implements Describable {
  dog,
  cat;

  @override
  String describe() => 'I am a $name';
}

Describable pet = Animal.cat;
print(pet.describe()); // I am a cat
```

Геттер, объявленный в интерфейсе, можно реализовать либо геттером, либо полем `final` с тем же именем.

# --instructions--

Объявите enum `Ticket`, который реализует `Priced`, с полем `final int` с именем `price` и значениями `child(5)`, `adult(12)` и `senior(8)` именно в таком порядке. Затем напишите функцию `total(List<Priced> items)`, которая возвращает сумму цен всех элементов.

# --seed--

```dart
abstract class Priced {
  int get price;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Ticket.adult.price` должно быть `12`

```dart
  test('test1', () {
    expect(Ticket.adult.price, 12, reason: '--err-t1--');
  });
```

`Ticket` можно использовать как `Priced`

```dart
  test('test2', () {
    Priced item = Ticket.senior;
    expect(item.price, 8, reason: '--err-t2--');
  });
```

`total([Ticket.child, Ticket.adult, Ticket.senior])` должно быть `25`

```dart
  test('test3', () {
    expect(total([Ticket.child, Ticket.adult, Ticket.senior]), 25, reason: '--err-t3--');
  });
```

`total([])` должно быть `0`

```dart
  test('test4', () {
    expect(total([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
abstract class Priced {
  int get price;
}

enum Ticket implements Priced {
  child(5),
  adult(12),
  senior(8);

  @override
  final int price;

  const Ticket(this.price);
}

int total(List<Priced> items) {
  int sum = 0;
  for (var item in items) {
    sum += item.price;
  }
  return sum;
}
```
