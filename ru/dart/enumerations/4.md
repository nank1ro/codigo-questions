---
language: dart
exerciseType: 1
---

# --description--

Поскольку `values` — это список, вы можете сочетать его с уже известными вам методами списков. Например, `.map()` вместе с `.name` превращает значения в список строк:

```dart
enum Direction { north, south, east, west }

List<String> names = Direction.values.map((d) => d.name).toList();
print(names); // [north, south, east, west]
```

# --instructions--

Объявите enum `Weekday` со значениями `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday` и `sunday` именно в таком порядке. Затем напишите функцию `dayNames()`, которая возвращает `List<String>` с именем каждого дня недели в порядке объявления.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Weekday.values.length` должно быть `7`

```dart
  test('test1', () {
    expect(Weekday.values.length, 7, reason: '--err-t1--');
  });
```

`Weekday.values[2]` должно быть `Weekday.wednesday`

```dart
  test('test2', () {
    expect(Weekday.values[2], Weekday.wednesday, reason: '--err-t2--');
  });
```

`dayNames()` должна возвращать семь имён по порядку

```dart
  test('test3', () {
    expect(dayNames(), ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Weekday { monday, tuesday, wednesday, thursday, friday, saturday, sunday }

List<String> dayNames() {
  return Weekday.values.map((day) => day.name).toList();
}
```
