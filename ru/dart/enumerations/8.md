---
language: dart
exerciseType: 2
---

# --description--

Начиная с Dart 3, `switch` также можно использовать как **выражение**, которое возвращает значение. Каждый case записывается как `паттерн => значение`, а case'ы разделяются запятыми, без ключевого слова `case` и без `break`:

```dart
enum Direction { north, south, east, west }

String arrow(Direction direction) => switch (direction) {
      Direction.north => '^',
      Direction.south => 'v',
      Direction.east => '>',
      Direction.west => '<',
    };
```

Как и форма-оператор, switch-выражение над enum должно быть исчерпывающим.

# --instructions--

Дополните switch-выражение так, чтобы `label` возвращала первую букву каждого размера, а затем выведите label для `Size.large`.

# --seed--

```dart
enum Size { small, medium, large }

String label(Size size) => [/] (size) {
      Size.small [/] 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --answers--

- switch
- =>
- case
- when
- ->
- :

# --solutions--

```dart
enum Size { small, medium, large }

String label(Size size) => switch (size) {
      Size.small => 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --output--

L
