---
language: dart
exerciseType: 1
---

# --description--

Начиная с Dart 2.17, enum может объявлять **поля** и **конструктор**, как и класс. Это называется *расширенным enum*. Каждое значение передаёт свои собственные аргументы конструктору:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);
}

print(Planet.mars.moons); // 2
```

Обратите внимание на три правила: список значений заканчивается **точкой с запятой** `;`, поля должны быть `final`, а конструктор должен быть `const`.

# --instructions--

Объявите enum `Coin` с полем `final int` с именем `cents` и значениями `penny(1)`, `nickel(5)`, `dime(10)` и `quarter(25)` именно в таком порядке.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Coin.penny.cents` должно быть `1`, а `Coin.nickel.cents` должно быть `5`

```dart
  test('test1', () {
    expect(Coin.penny.cents, 1, reason: '--err-t1--');
    expect(Coin.nickel.cents, 5, reason: '--err-t2--');
  });
```

`Coin.dime.cents` должно быть `10`

```dart
  test('test2', () {
    expect(Coin.dime.cents, 10, reason: '--err-t3--');
  });
```

`Coin.quarter.cents` должно быть `25`

```dart
  test('test3', () {
    expect(Coin.quarter.cents, 25, reason: '--err-t4--');
  });
```

`Coin.values` должно иметь 4 значения, с `penny` первым

```dart
  test('test4', () {
    expect(Coin.values.length, 4, reason: '--err-t5--');
    expect(Coin.values[0], Coin.penny, reason: '--err-t6--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Coin {
  penny(1),
  nickel(5),
  dime(10),
  quarter(25);

  final int cents;

  const Coin(this.cents);
}
```
