---
language: dart
exerciseType: 1
---

# --description--

Значения enum отлично подходят в качестве **ключей карты**: они уникальны, их легко сравнивать, и компилятор проверяет, что вы используете только реальные значения. Объявите карту с enum в качестве типа ключа и ищите значения с помощью `[]`:

```dart
enum Direction { north, south, east, west }

Map<Direction, String> arrows = {
  Direction.north: '^',
  Direction.south: 'v',
  Direction.east: '>',
  Direction.west: '<',
};

print(arrows[Direction.east]); // >
```

Как и в любой карте, поиск возвращает `null`, если ключ отсутствует, поэтому используйте `??`, чтобы указать значение по умолчанию.

# --instructions--

Имея `enum Size { small, medium, large }`, объявите на верхнем уровне `Map<Size, int>` с именем `prices`, где `small` стоит `2`, `medium` стоит `3`, а `large` стоит `4`. Затем напишите функцию `totalPrice(List<Size> order)`, которая возвращает сумму цен всех размеров в `order`.

# --seed--

```dart
enum Size { small, medium, large }
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`prices[Size.medium]` должно быть `3`

```dart
  test('test1', () {
    expect(prices[Size.medium], 3, reason: '--err-t1--');
  });
```

`totalPrice([Size.small, Size.large])` должно быть `6`

```dart
  test('test2', () {
    expect(totalPrice([Size.small, Size.large]), 6, reason: '--err-t2--');
  });
```

`totalPrice([Size.medium, Size.medium, Size.small])` должно быть `8`

```dart
  test('test3', () {
    expect(totalPrice([Size.medium, Size.medium, Size.small]), 8, reason: '--err-t3--');
  });
```

`totalPrice([])` должно быть `0`

```dart
  test('test4', () {
    expect(totalPrice([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Size { small, medium, large }

Map<Size, int> prices = {
  Size.small: 2,
  Size.medium: 3,
  Size.large: 4,
};

int totalPrice(List<Size> order) {
  int total = 0;
  for (var size in order) {
    total += prices[size] ?? 0;
  }
  return total;
}
```
