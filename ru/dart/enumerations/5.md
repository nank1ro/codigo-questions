---
language: dart
exerciseType: 4
---

# --description--

Перебор `values` — обычный способ обработать каждое значение перечисления. Внутри цикла текущее значение ведёт себя как любой другой объект, поэтому можно прочитать его `index` и `name` и сразу использовать их в интерполяции строки:

```dart
enum Direction { north, south, east, west }

for (var direction in Direction.values) {
  print('${direction.index}: ${direction.name}');
}
```

# --instructions--

Расположите элементы в правильном порядке, чтобы объявить enum `Level` и вывести для каждого значения его индекс и имя. Объявите enum перед `main`. Программа должна вывести:

```
0: low
1: medium
2: high
```

# --answers--

```dart
enum Level { low, medium, high }
```

```dart
void main() {
```

```dart
  for (var level in Level.values) {
```

```dart
    print('${level.index}: ${level.name}');
```

```dart
  }
}
```

# --solutions--

```dart
enum Level { low, medium, high }
void main() {
  for (var level in Level.values) {
    print('${level.index}: ${level.name}');
  }
}
```

# --output--

0: low
1: medium
2: high
