---
language: dart
exerciseType: 1
---

# --description--

`this` ссылается на **текущий экземпляр** класса. Он используется для различия между переменными экземпляра и параметрами с одинаковым именем:

```dart
class Circle {
  double radius;

  Circle(double radius) {
    this.radius = radius;
  }
}
```

Здесь `this.radius` ссылается на поле, а `radius` (без `this`) — на параметр конструктора.

# --instructions--

Создайте класс `Rectangle` с двумя полями `width` и `height` типа `double`. Добавьте конструктор, принимающий `width` и `height` в качестве параметров и присваивающий их через `this`. Добавьте метод `area()`, который возвращает `width * height`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Rectangle` имеет правильную площадь

```dart
    test('area', () {
      var r = Rectangle(4.0, 5.0);
      expect(r.area(), 20.0, reason: '--err-t1--');
    });
```

`width` и `height` хранятся корректно

```dart
    test('fields', () {
      var r = Rectangle(3.0, 7.0);
      expect(r.width, 3.0, reason: '--err-t2--');
      expect(r.height, 7.0, reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Rectangle {
  double width;
  double height;

  Rectangle(double width, double height) {
    this.width = width;
    this.height = height;
  }

  double area() {
    return width * height;
  }
}
```
