---
language: dart
exerciseType: 1
---

# --description--

`this` ссылается на **текущий экземпляр** класса, то есть на объект, у которого был вызван метод. Внутри метода его можно использовать для доступа к собственным полям объекта:

```dart
class Circle {
  double radius;

  Circle(this.radius);

  double diameter() {
    return this.radius * 2;
  }
}
```

Здесь `this.radius` читает поле `radius` окружности, у которой был вызван `diameter()`. Если нет другой переменной с таким же именем, `this.` можно опустить: `radius * 2` работает так же.

# --instructions--

Создайте класс `Rectangle` с двумя полями `width` и `height` типа `double`, задаваемыми через конструктор `Rectangle(this.width, this.height)`. Добавьте метод `area()`, который возвращает `this.width * this.height`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Rectangle` имеет правильную площадь

```dart
    test('area', () {
      var r = Rectangle(4.0, 5.0);
      expect(r.area(), 20.0, reason: '--err-t1--');
    });
```

`width` и `height` хранятся корректно

```dart
    test('fields', () {
      var r = Rectangle(3.0, 7.0);
      expect(r.width, 3.0, reason: '--err-t2--');
      expect(r.height, 7.0, reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Rectangle {
  double width;
  double height;

  Rectangle(this.width, this.height);

  double area() {
    return this.width * this.height;
  }
}
```
