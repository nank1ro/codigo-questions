---
language: dart
exerciseType: 2
---

# --description--

**Геттер** — это специальный метод, который читает вычисляемое или приватное значение и выглядит как обращение к свойству. Он определяется с помощью ключевого слова `get`:

```dart
class Circle {
  double radius;

  Circle(this.radius);

  double get area => 3.14159 * radius * radius;
}
```

К геттеру обращаются как к полю: `circle.area` (без скобок).

Стрелка `=> expr` — это сокращение для тела, которое только возвращает значение: `{ return expr; }`. Она работает для геттеров и для любой функции или метода:

```dart
double half(double n) => n / 2;
```

# --instructions--

Завершите геттер `perimeter` в классе `Square`, который возвращает `4 * side`.

# --seed--

```dart
class Square {
  double side;

  Square(this.side);

  double [/] perimeter => [/] * side;
}
```

# --answers--

- get
- 4
- set
- 2
- double
- perimeter

# --solutions--

```dart
class Square {
  double side;

  Square(this.side);

  double get perimeter => 4 * side;
}
```
