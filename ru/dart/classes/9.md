---
language: dart
exerciseType: 2
---

# --description--

**Сеттер** — это специальный метод, позволяющий присвоить значение с одновременным выполнением логики валидации. Он определяется с помощью ключевого слова `set`:

```dart
class Temperature {
  double _celsius = 0;

  double get celsius => _celsius;

  set celsius(double value) {
    if (value < -273.15) throw ArgumentError('Too cold!');
    _celsius = value;
  }
}
```

Имя поля часто начинается с `_`, что означает его приватность.

# --instructions--

Завершите сеттер `age` в классе `Person`, который присваивает `value` переменной `_age` только при `value >= 0`.

# --seed--

```dart
class Person {
  int _age = 0;

  int get age => _age;

  [/] age(int value) {
    if (value >= 0) _age = [/];
  }
}
```

# --answers--

- set
- value
- get
- _age
- age
- 0

# --solutions--

```dart
class Person {
  int _age = 0;

  int get age => _age;

  set age(int value) {
    if (value >= 0) _age = value;
  }
}
```
