---
language: dart
exerciseType: 2
---

# --description--

**Конструктор** — это специальный метод, который вызывается при создании (инстанцировании) объекта класса. Конструктор имеет то же имя, что и класс:

```dart
class Animal {
  String name;

  Animal(this.name);
}
```

Параметр, записанный как `this.name`, сохраняет значение, переданное в конструктор, непосредственно в поле `name` нового объекта. Полю, задаваемому таким образом, не требуется начальное значение.

Объект создаётся с помощью ключевого слова `new` (необязательного в Dart) или непосредственно по имени класса:

```dart
var dog = Animal('Rex');
```

# --instructions--

Завершите конструктор класса `Person`.

# --seed--

```dart
class Person {
  String name;

  [/](this.name);
}
```

# --answers--

- Person
- person
- constructor
- init
- new

# --solutions--

```dart
class Person {
  String name;

  Person(this.name);
}
```
