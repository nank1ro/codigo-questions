---
language: dart
exerciseType: 4
---

# --description--

Генератор `async*` может передать дальше **каждое событие другого потока** с помощью `yield*` (yield-star). Это как цикл `await for`, который выдаёт каждое значение, но в одну строку:

```dart
Stream<int> ones() async* {
  yield 1;
  yield 1;
}

Stream<int> sequence() async* {
  yield 0;
  yield* ones();
  yield 2;
}
// sequence() emits 0, 1, 1, 2
```

Внешний поток продолжает своими `yield`, как только внутренний поток завершён.

# --instructions--

Расставь элементы, чтобы определить `async*`-функцию `pair`, которая выдаёт `1` и `2`, `async*`-функцию `all`, которая выдаёт `0`, затем каждое значение `pair()`, затем `3`, и `main`, который выводит каждое значение `all()`. Определи сначала `pair`, потом `all`, потом `main`. Программа должна вывести:

```
0
1
2
3
```

# --answers--

```dart
Stream<int> pair() async* {
```

```dart
  yield 1;
```

```dart
  yield 2;
```

```dart
}
```

```dart
Stream<int> all() async* {
```

```dart
  yield 0;
```

```dart
  yield* pair();
```

```dart
  yield 3;
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  await for (final n in all()) {
```

```dart
    print(n);
```

```dart
  }
```

```dart
}
```

# --solutions--

```dart
Stream<int> pair() async* {
  yield 1;
  yield 2;
}

Stream<int> all() async* {
  yield 0;
  yield* pair();
  yield 3;
}

Future<void> main() async {
  await for (final n in all()) {
    print(n);
  }
}
```

# --output--

0
1
2
3
