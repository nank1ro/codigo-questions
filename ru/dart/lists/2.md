---
language: dart
exerciseType: 2
---

# --description--

Списки в Dart **индексируются с нуля**, то есть первый элемент находится по индексу `0`, второй — по индексу `1` и так далее.

```dart
var colors = ['red', 'green', 'blue'];
print(colors[0]); // red
print(colors[2]); // blue
```

Доступ к элементу по индексу осуществляется с помощью синтаксиса `list[index]`.

# --instructions--

Дополните код, чтобы вывести второй элемент списка.

# --seed--

```dart
var animals = ['cat', 'dog', 'bird'];
print(animals[/]);
```

# --answers--

- [1]
- [0]
- [2]
- .get(1)
- (1)
- [3]

# --solutions--

```dart
var animals = ['cat', 'dog', 'bird'];
print(animals[1]);
```

# --output--

dog
