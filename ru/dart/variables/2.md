---
language: dart
exerciseType: 1
---

# --description--

В предыдущем примере мы видели создание переменной:

```dart
int number = 1;
```

Не дайте символу `=` ввести вас в заблуждение.
Это не знак равенства, как в математике, а так называемый __оператор присваивания__, потому что он присваивает значение переменной.
Знак равенства, в свою очередь, — это `==`.

# --instructions--

Присвойте переменной `x` значение `5`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x` должно быть равно `5`.

```dart
  test('test1', () {
    expect(x, 5, reason: '--err-t1--');
  });
```

`x` должно быть типа `int`

```dart
  test('test2', () {
    expect(x, TypeMatcher<int>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 5;
```
