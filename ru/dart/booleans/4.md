---
language: dart
exerciseType: 2
---

# --description--

__Оператор отрицания__ `!` (также называемый "not" или "не") переворачивает булево значение.
Применение `!` к `true` дает `false`, а применение `!` к `false` дает `true`.

```dart
bool isActive = true;
print(!isActive); // false
```

Это полезно, когда вы хотите проверить противоположность условия.

# --instructions--

Имея переменную `isBlocked` установленную на `false`, выведите её отрицаемое значение, используя `!`.

# --seed--

```dart
bool isBlocked = false;
print([/]isBlocked);
```

# --answers--

- !
- !!
- ~
- not

# --solutions--

```dart
bool isBlocked = false;
print(!isBlocked);
```

# --output--

true
