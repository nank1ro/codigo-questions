---
language: dart
exerciseType: 1
---

# --instructions--

Объявите булеву переменную с именем `isHidden` и присвойте ей значение `false`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHidden` должна быть равна `false`.

```dart
  test('test1', () {
    expect(isHidden, false, reason: '--err-t1--');
  });
```

`isHidden` должна быть типа `bool`.

```dart
  test('test2', () {
    expect(isHidden, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isHidden = false;
```
