---
language: dart
exerciseType: 1
---

# --instructions--

Объедините `&&`, `||` и `!` для получения результата.

Объявите булеву переменную с именем `result` используя это точное выражение: `!false && (true || false)`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` должна быть равна `true`.

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result` должна быть типа `bool`.

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = !false && (true || false);
```
