---
language: dart
exerciseType: 1
---

# --description--

Расширение может объявлять **статические** члены. Как и в классе, они принадлежат самому расширению, а не какому-либо значению, и доступ к ним осуществляется через **имя расширения**, а не через расширяемый тип:

```dart
extension Temperatures on double {
  static const double boiling = 100.0;

  static bool isBoiling(double celsius) => celsius >= boiling;
}

void main() {
  print(Temperatures.boiling);         // 100.0
  print(Temperatures.isBoiling(37.5)); // false
  print(double.boiling);               // error: 'boiling' isn't defined for 'double'
}
```

Статические члены — удобное место для констант и вспомогательных функций, связанных с расширяемым типом.

# --instructions--

Напишите расширение с именем `Dice` на `int` с:
- статической константой `sides` типа `int` со значением `6`
- статическим методом `isValid`, который принимает параметр `value` типа `int` и возвращает `true`, когда он находится между `1` и `sides` (оба включительно)

Пример:
```dart
print(Dice.sides);      // 6
print(Dice.isValid(7)); // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Dice.sides` должно быть `6`

```dart
  test('test1', () {
    expect(Dice.sides, 6, reason: '--err-t1--');
  });
```

`Dice.isValid(1)` и `Dice.isValid(6)` должны быть `true`

```dart
  test('test2', () {
    expect(Dice.isValid(1), true, reason: '--err-t2--');
    expect(Dice.isValid(6), true, reason: '--err-t2--');
  });
```

`Dice.isValid(0)` и `Dice.isValid(7)` должны быть `false`

```dart
  test('test3', () {
    expect(Dice.isValid(0), false, reason: '--err-t3--');
    expect(Dice.isValid(7), false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Dice on int {
  static const int sides = 6;

  static bool isValid(int value) => value >= 1 && value <= sides;
}
```
