---
language: dart
exerciseType: 1
---

# --description--

Расширения работают с любыми типами, включая числа. Это расширение даёт каждому `int` метод, удваивающий его:

```dart
extension Doubling on int {
  int doubled() => this * 2;
}
```

Поскольку расширение применяется к **типу**, метод можно вызвать и на переменной, и непосредственно на **литерале**. Отрицательным литералам нужны скобки, иначе точка читается раньше знака минус:

```dart
var n = 21;
print(n.doubled());    // 42
print(4.doubled());    // 8
print((-3).doubled()); // -6
```

# --instructions--

Напишите расширение с именем `Squaring` на `int` с методом `squared`, который не принимает параметров и возвращает число, умноженное само на себя.

Пример:
```dart
print(5.squared()); // 25
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`5.squared()` должно быть `25`

```dart
  test('test1', () {
    expect(5.squared(), 25, reason: '--err-t1--');
  });
```

`(-3).squared()` должно быть `9`

```dart
  test('test2', () {
    expect((-3).squared(), 9, reason: '--err-t2--');
  });
```

`0.squared()` должно быть `0` и быть типом `int`

```dart
  test('test3', () {
    expect(0.squared(), 0, reason: '--err-t3--');
    expect(12.squared(), isA<int>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Squaring on int {
  int squared() => this * this;
}
```
