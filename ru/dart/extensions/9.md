---
language: dart
exerciseType: 4
---

# --description--

Расширения нужны не только для типов SDK: можно расширять и **собственные классы**. Это полезно, когда класс приходит из пакета, который вы не контролируете, или когда вы хотите оставить класс небольшим и добавить необязательные помощники рядом с кодом, которому они нужны.

```dart
class Circle {
  final double radius;
  Circle(this.radius);
}

extension CircleMath on Circle {
  double get diameter => radius * 2;
}

void main() {
  print(Circle(3).diameter); // 6.0
}
```

Расширение видит публичные поля и методы класса, точно как код, написанный вне класса.

# --instructions--

Упорядочьте элементы, чтобы определить класс `Rectangle` с полями `width` и `height`, затем расширение `RectangleMath` на `Rectangle` с геттером `area`, а затем `main`, который выводит площадь прямоугольника `4` на `2.5`. Сначала определите класс, затем расширение, затем `main`. Внутри класса объявите оба поля до конструктора. Программа должна вывести:

```
10.0
```

# --answers--

```dart
class Rectangle {
```

```dart
  final double width;
```

```dart
  final double height;
```

```dart
  Rectangle(this.width, this.height);
```

```dart
}
```

```dart
extension RectangleMath on Rectangle {
```

```dart
  double get area => width * height;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Rectangle(4, 2.5).area);
```

```dart
}
```

# --solutions--

```dart
class Rectangle {
  final double width;
  final double height;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

```dart
class Rectangle {
  final double height;
  final double width;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

# --output--

10.0
