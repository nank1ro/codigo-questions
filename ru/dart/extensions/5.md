---
language: dart
exerciseType: 1
---

# --description--

Расширение на `List<int>` нельзя применить к `List<String>`. Чтобы написать одно расширение, работающее с **любым** типом элементов, задайте расширению **параметр типа**, записываемый в угловых скобках после его имени, и используйте его в типе после `on`:

```dart
extension Firsts<T> on List<T> {
  T get firstOrLast => length > 1 ? this[0] : this[length - 1];
}
```

`T` — это заполнитель для «какого бы ни был тип элементов»: на `List<int>` он становится `int`, на `List<String>` — `String`, поэтому геттер выше возвращает `int` или `String` в зависимости от ситуации. Компилятор подставляет `T` за вас при каждом вызове.

```dart
print([7, 8, 9].firstOrLast); // 7
print(['a', 'b'].firstOrLast); // a
```

# --instructions--

Напишите расширение с именем `Seconds` с параметром типа `T` на `List<T>` с геттером `secondOrNull` типа `T?`, который возвращает элемент с индексом `1` или `null`, когда в списке меньше двух элементов.

Пример:
```dart
print([10, 20, 30].secondOrNull); // 20
print(['solo'].secondOrNull);     // null
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`[10, 20, 30].secondOrNull` должно быть `20`

```dart
  test('test1', () {
    expect([10, 20, 30].secondOrNull, 20, reason: '--err-t1--');
  });
```

`['a', 'b'].secondOrNull` должно быть `'b'`

```dart
  test('test2', () {
    expect(['a', 'b'].secondOrNull, 'b', reason: '--err-t2--');
  });
```

`['solo'].secondOrNull` и `<int>[].secondOrNull` должны быть `null`

```dart
  test('test3', () {
    expect(['solo'].secondOrNull, null, reason: '--err-t3--');
    expect(<int>[].secondOrNull, null, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Seconds<T> on List<T> {
  T? get secondOrNull => length > 1 ? this[1] : null;
}
```
