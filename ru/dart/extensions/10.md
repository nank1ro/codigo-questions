---
language: dart
exerciseType: 1
---

# --description--

В Dart тип может определить, что означают операторы вроде `+`, `*` или `==` для своих значений, с помощью метода, имя которого — ключевое слово `operator`, за которым следует символ. Правый операнд оператора — это параметр метода:

```dart
extension Scaling on List<int> {
  List<int> operator *(int factor) => map((n) => n * factor).toList();
}

void main() {
  print([1, 2, 3] * 10); // [10, 20, 30]
}
```

Поскольку расширения могут объявлять операторы, вы можете дать существующему типу новый оператор, которого у него ещё нет. У `String` есть `+` и `*`, но нет `-`, поэтому расширение может определить, что означает `'hello world' - 'o'`.

# --instructions--

Напишите расширение с именем `Removing` на `String` с оператором `-`, который принимает параметр `part` типа `String` и возвращает строку без всех вхождений `part`. Используйте `replaceAll`.

Пример:
```dart
print('hello world' - 'o'); // hell wrld
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`'hello world' - 'o'` должно быть `'hell wrld'`

```dart
  test('test1', () {
    expect('hello world' - 'o', 'hell wrld', reason: '--err-t1--');
  });
```

`'banana' - 'an'` должно быть `'ba'`

```dart
  test('test2', () {
    expect('banana' - 'an', 'ba', reason: '--err-t2--');
  });
```

`'abc' - 'z'` должно быть `'abc'`, а `'aaa' - 'a'` должно быть `''`

```dart
  test('test3', () {
    expect('abc' - 'z', 'abc', reason: '--err-t3--');
    expect('aaa' - 'a', '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Removing on String {
  String operator -(String part) => replaceAll(part, '');
}
```
