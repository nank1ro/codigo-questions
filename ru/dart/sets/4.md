---
language: dart
exerciseType: 2
---

# --description--

Метод `.remove(value)` удаляет значение из множества. Он возвращает `true`, если значение там было, и `false` в противном случае:

```dart
var numbers = {1, 2, 3};
print(numbers.remove(2)); // true
print(numbers.remove(9)); // false
print(numbers); // {1, 3}
```

Чтобы удалить все элементы сразу, используйте `.clear()`.

# --instructions--

Дополните код, чтобы удалить число `2` из множества, а затем выведите множество.

# --seed--

```dart
var numbers = {1, 2, 3};
numbers.[/](2);
print(numbers);
```

# --answers--

- remove
- removeAt
- delete
- drop
- clear
- pop

# --solutions--

```dart
var numbers = {1, 2, 3};
numbers.remove(2);
print(numbers);
```

# --output--

{1, 3}
