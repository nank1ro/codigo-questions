---
language: dart
exerciseType: 2
---

# --description--

Метод `.remove(key)` удаляет ключ и его значение из карты. Он возвращает удалённое значение или `null`, если такого ключа не было:

```dart
var ages = {'Ann': 30, 'Bob': 25};
int? removed = ages.remove('Ann');
print(removed); // 30
print(ages); // {Bob: 25}
```

Свойство `.length` возвращает количество пар ключ-значение:

```dart
print(ages.length); // 1
```

# --instructions--

Дополните код, чтобы удалить `Bob` из карты, а затем вывести, сколько пар осталось.

# --seed--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.[/]('Bob');
print(ages.[/]);
```

# --answers--

- remove
- length
- delete
- removeAt
- size
- count

# --solutions--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.remove('Bob');
print(ages.length);
```

# --output--

2
