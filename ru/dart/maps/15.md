---
language: dart
exerciseType: 1
---

# --description--

Метод `.putIfAbsent(key, ifAbsent)` добавляет пару **только если** ключа ещё нет в карте. Второй аргумент — функция, которая создаёт значение. Если ключ уже существует, карта остаётся без изменений. В обоих случаях возвращается значение, теперь хранящееся под этим ключом:

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99); // Ann is already there, nothing changes
ages.putIfAbsent('Bob', () => 25); // Bob is added
print(ages); // {Ann: 30, Bob: 25}
```

# --instructions--

Дана карта `ages`. Дважды используйте `.putIfAbsent()`: сначала для ключа `'Ann'` со значением `99`, затем для ключа `'Bob'` со значением `25`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var ages = {'Ann': 30};
```

# --after-seed--

```dart
  return ages;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`ages` должна быть `{'Ann': 30, 'Bob': 25}`

```dart
  test('test1', () {
    expect(result(), {'Ann': 30, 'Bob': 25}, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99);
ages.putIfAbsent('Bob', () => 25);
```
