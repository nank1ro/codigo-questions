---
language: dart
exerciseType: 1
---

# --instructions--

Дан список `colors`. Постройте `Map<String, int>` с именем `counts`, где каждый ключ — это цвет, а его значение — сколько раз этот цвет встречается в списке.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var colors = ['red', 'blue', 'red', 'green', 'red', 'blue'];
```

# --after-seed--

```dart
  return counts;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`counts` должна быть `{'red': 3, 'blue': 2, 'green': 1}`

```dart
  test('test1', () {
    expect(result(), {'red': 3, 'blue': 2, 'green': 1}, reason: '--err-t1--');
  });
```

у `counts` должно быть 3 ключа

```dart
  test('test2', () {
    expect(result().length, 3, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var colors = ['red', 'blue', 'red', 'green', 'red', 'blue'];
var counts = <String, int>{};
for (var color in colors) {
  counts.update(color, (n) => n + 1, ifAbsent: () => 1);
}
```
