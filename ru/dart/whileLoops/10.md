---
language: dart
exerciseType: 1
---

# --instructions--

Используйте цикл `while` для вычисления суммы чисел от `1` до `10` (включительно) и верните результат.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` должна равняться `55`

```dart
  test('test1', () {
    expect(computeValue(), 55, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sum = 0;
int i = 1;
while (i <= 10) {
  sum += i;
  i++;
}
```
