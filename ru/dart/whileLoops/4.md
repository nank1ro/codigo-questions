---
language: dart
exerciseType: 2
---

# --description--

Условие цикла `while` может содержать любое булево выражение. Как только условие принимает значение `false`, цикл завершается.

```dart
int n = 100;
while (n > 1) {
  n ~/= 2;
}
print(n); // 0
```

Здесь `n` делится пополам при каждой итерации с помощью целочисленного деления (`~/`).

# --instructions--

Завершите цикл `while`, который продолжает делить `n` пополам (используя `~/= 2`), пока оно больше `1`, затем выводит результат.

# --seed--

```dart
void main() {
  int n = 64;
  while (n [/] 1) {
    n [/] 2;
  }
  print(n);
}
```

# --answers--

- >
- <
- ~/=
- /=
- *=

# --solutions--

```dart
void main() {
  int n = 64;
  while (n > 1) {
    n ~/= 2;
  }
  print(n);
}
```

# --output--

1
