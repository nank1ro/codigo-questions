---
language: dart
exerciseType: 1
---

# --instructions--

Используйте цикл `while`, чтобы найти первое число больше `30`, которое делится и на `3`, и на `5`. Верните это число.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` должен равняться `45`

```dart
  test('test1', () {
    expect(computeValue(), 45, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int result = 31;
while (result % 3 != 0 || result % 5 != 0) {
  result++;
}
```
