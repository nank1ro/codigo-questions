---
language: dart
exerciseType: 1
---

# --instructions--

Используйте цикл `do-while`, чтобы прибавлять `3` к переменной `total`, начиная с `0`, пока `total` не достигнет или не превысит `12`. Верните `total`.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` должна равняться `12`

```dart
  test('test1', () {
    expect(computeValue(), 12, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 0;
do {
  total += 3;
} while (total < 12);
```
