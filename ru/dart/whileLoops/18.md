---
language: dart
exerciseType: 1
---

# --instructions--

Используйте цикл `while` для вычисления факториала числа `6` (т.е. `6!`). Верните результат.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return factorial;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`factorial` должен равняться `720`

```dart
  test('test1', () {
    expect(computeValue(), 720, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int factorial = 1;
int i = 1;
while (i <= 6) {
  factorial *= i;
  i++;
}
```
