---
language: dart
exerciseType: 2
---

# --description--

Ключевое слово `break` немедленно завершает цикл, независимо от его условия.

```dart
int i = 0;
while (true) {
  if (i == 3) break;
  print(i);
  i++;
}
```

Без `break` цикл `while (true)` выполнялся бы бесконечно. Здесь он останавливается, когда `i` достигает `3`.

# --instructions--

Завершите цикл, который выводит числа начиная с `0` и использует `break` для остановки, когда `i` равно `4`.

# --seed--

```dart
void main() {
  int i = 0;
  while ([/]) {
    if (i == [/]) [/];
    print(i);
    i++;
  }
}
```

# --answers--

- true
- false
- 4
- 3
- break
- continue
- return

# --solutions--

```dart
void main() {
  int i = 0;
  while (true) {
    if (i == 4) break;
    print(i);
    i++;
  }
}
```

# --output--

0
1
2
3
