---
language: dart
exerciseType: 1
---

# --instructions--

Напишите цикл `while`, который считает от `1` до `5` и возвращает конечное значение счётчика.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return counter;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`counter` должен равняться `5`

```dart
  test('test1', () {
    expect(computeValue(), 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int counter = 1;
while (counter < 5) {
  counter++;
}
```
