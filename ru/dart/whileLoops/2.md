---
language: dart
exerciseType: 2
---

# --description--

Переменная-счётчик управляет количеством выполнений цикла `while`.

```dart
int count = 0;
while (count < 5) {
  count++;
}
print(count); // 5
```

Счётчик начинается с `0`, и цикл увеличивает его при каждой итерации, пока `count < 5` не станет `false`.

# --instructions--

Завершите цикл, который считает от `1` до `5` и выводит каждое значение.

# --seed--

```dart
void main() {
  int count = [/];
  while (count [/] 5) {
    print(count);
    [/];
  }
}
```

# --answers--

- 1
- 0
- <=
- <
- count++
- count--
- count += 2

# --solutions--

```dart
void main() {
  int count = 1;
  while (count <= 5) {
    print(count);
    count++;
  }
}
```

# --output--

1
2
3
4
5
