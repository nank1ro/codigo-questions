---
language: dart
exerciseType: 1
---

# --instructions--
Напишите функцию, которая возвращает значение на позиции (`row`, `col`) таблицы умножения. Используйте вложенные циклы `for`, где внешний цикл перебирает строки, а внутренний — столбцы (оба с индексацией от 1). Верните произведение на указанной позиции.

# --before-seed--
```dart
int multiplicationCell(int row, int col) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Строка 3, столбец 4 равно 12
```dart
    test('3x4 = 12', () {
      expect(multiplicationCell(3, 4), 12, reason: '--err-t1--');
    });
```

Строка 1, столбец 1 равно 1
```dart
    test('1x1 = 1', () {
      expect(multiplicationCell(1, 1), 1, reason: '--err-t2--');
    });
```

Строка 7, столбец 8 равно 56
```dart
    test('7x8 = 56', () {
      expect(multiplicationCell(7, 8), 56, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int r = 1; r <= row; r++) {
    for (int c = 1; c <= col; c++) {
      if (r == row && c == col) {
        result = r * c;
      }
    }
  }
```
