---
language: dart
exerciseType: 1
---

# --instructions--
Напишите функцию, которая возвращает сумму всех целых чисел от 1 до `n` (включительно) с помощью цикла `for`.

# --before-seed--
```dart
int sumTo(int n) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Сумма от 1 до 5 равна 15
```dart
    test('sum 1..5', () {
      expect(sumTo(5), 15, reason: '--err-t1--');
    });
```

Сумма от 1 до 1 равна 1
```dart
    test('sum 1..1', () {
      expect(sumTo(1), 1, reason: '--err-t2--');
    });
```

Сумма от 1 до 10 равна 55
```dart
    test('sum 1..10', () {
      expect(sumTo(10), 55, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = 1; i <= n; i++) {
    result += i;
  }
```
