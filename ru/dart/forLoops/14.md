---
language: dart
exerciseType: 1
---

# --instructions--
Напишите функцию, которая возвращает список, содержащий только нечётные числа из `List<int>`. Используйте цикл `for` с `continue` для пропуска чётных чисел.

# --before-seed--
```dart
List<int> filterOdds(List<int> numbers) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Отфильтровывает чётные числа
```dart
    test('filters evens', () {
      expect(filterOdds([1, 2, 3, 4, 5]), [1, 3, 5], reason: '--err-t1--');
    });
```

Все чётные числа возвращают пустой список
```dart
    test('all even', () {
      expect(filterOdds([2, 4, 6]), [], reason: '--err-t2--');
    });
```

Все нечётные числа возвращают те же значения
```dart
    test('all odd', () {
      expect(filterOdds([1, 3, 5]), [1, 3, 5], reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  List<int> result = [];
  for (int i = 0; i < numbers.length; i++) {
    if (numbers[i] % 2 == 0) continue;
    result.add(numbers[i]);
  }
```
