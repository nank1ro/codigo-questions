---
language: dart
exerciseType: 1
---

# --instructions--
Напишите функцию, которая принимает `List<int>` и возвращает новый список, где каждый элемент умножен на 2.

# --before-seed--
```dart
List<int> doubleElements(List<int> items) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Удваивает каждый элемент
```dart
    test('doubles elements', () {
      expect(doubleElements([1, 2, 3]), [2, 4, 6], reason: '--err-t1--');
    });
```

Пустой список возвращает пустой список
```dart
    test('empty list', () {
      expect(doubleElements([]), [], reason: '--err-t2--');
    });
```

Список с одним элементом
```dart
    test('single element', () {
      expect(doubleElements([5]), [10], reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  List<int> result = [];
  for (var item in items) {
    result.add(item * 2);
  }
```
