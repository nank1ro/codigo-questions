---
language: dart
exerciseType: 1
---

# --description--

`typedef` даёт имя типу и может иметь собственные параметры типа. Обычно это нужно, чтобы один раз назвать семейство функциональных типов, а не выписывать его при каждом использовании:

```dart
typedef Transform<I, O> = O Function(I input);

final Transform<String, int> length = (word) => word.length;
print(length('kiwi')); // 4
```

`Transform<String, int>` — просто другой способ записи `int Function(String)`, поэтому они взаимозаменяемы. Выигрыш — читаемость: параметр, объявленный как `Transform<I, O> transform`, говорит, для чего функция, а `O Function(I)` — только то, как она выглядит.

Обобщённый typedef и обобщённая функция естественно сочетаются: собственные параметры типа функции заполняют параметры typedef.

# --instructions--

Объявите обобщённый typedef `Transform` с двумя параметрами типа `I` и `O`, который называет функцию, принимающую `I` и возвращающую `O`.

Затем напишите обобщённую функцию `convertAll` с параметрами типа `I` и `O`, которая принимает `List<I>` `items` и `Transform<I, O>` `transform` и возвращает `List<O>` со всеми преобразованными элементами по порядку.

Пример:
```dart
print(convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)); // [3, 4]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)` должна вернуть `[3, 4]`

```dart
  test('test1', () {
    expect(
      convertAll<String, int>(['fig', 'kiwi'], (w) => w.length),
      [3, 4],
      reason: '--err-t1--',
    );
  });
```

`convertAll` должна возвращать `List<O>`, поэтому преобразование int в строки должно дать `List<String>`

```dart
  test('test2', () {
    final result = convertAll<int, String>([1, 2], (n) => 'n$n');
    expect(result, ['n1', 'n2'], reason: '--err-t2--');
    expect(result, isA<List<String>>(), reason: '--err-t2--');
  });
```

Typedef `Transform` должен называть тип функции, а пустой список должен давать пустой список

```dart
  test('test3', () {
    final Transform<int, int> twice = (n) => n * 2;
    expect(convertAll<int, int>([1, 2, 3], twice), [2, 4, 6], reason: '--err-t3--');
    expect(convertAll<String, int>(<String>[], (w) => w.length), <int>[], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
typedef Transform<I, O> = O Function(I input);

List<O> convertAll<I, O>(List<I> items, Transform<I, O> transform) {
  return items.map(transform).toList();
}
```
