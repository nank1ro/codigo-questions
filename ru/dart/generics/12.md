---
language: dart
exerciseType: 1
---

# --description--

Ограничение может упоминать сам параметр типа. `Comparable<T>` — интерфейс всего, что умеет сравнивать себя с себе подобными с помощью `compareTo`:

```dart
print('fig'.compareTo('kiwi')); // negative: fig comes first
print('kiwi'.compareTo('fig')); // positive
print('fig'.compareTo('fig'));  // zero
```

Поэтому `T extends Comparable<T>` читается как «любой тип, который можно сравнить с самим собой» — именно это нужно функции сортировки или поиска максимума:

```dart
T maxOf<T extends Comparable<T>>(T a, T b) => a.compareTo(b) >= 0 ? a : b;

print(maxOf('fig', 'kiwi')); // kiwi
```

`String` и `DateTime` удовлетворяют ему напрямую. `int` и `double` реализуют `Comparable<num>`, поэтому список чисел просто сравнивается как `num`.

# --instructions--

Напишите обобщённую функцию `largest` с параметром типа `T`, ограниченным `Comparable<T>`, которая принимает непустой `List<T>` `items` и возвращает наибольший элемент, сравнивая с помощью `compareTo`.

Пример:
```dart
print(largest(['fig', 'kiwi', 'apple'])); // kiwi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest(['fig', 'kiwi', 'apple'])` должна вернуть `'kiwi'`

```dart
  test('test1', () {
    expect(largest(['fig', 'kiwi', 'apple']), 'kiwi', reason: '--err-t1--');
  });
```

`largest` должна работать и с числами, и с датами

```dart
  test('test2', () {
    expect(largest([3, 9, 5]), 9, reason: '--err-t2--');
    expect(
      largest([DateTime(2020), DateTime(2024), DateTime(2001)]),
      DateTime(2024),
      reason: '--err-t2--',
    );
  });
```

`largest` должна возвращать единственный элемент одноэлементного списка и последний элемент, когда он наибольший, а также принимать явный аргумент типа, как `largest<String>(['a', 'z'])`

```dart
  test('test3', () {
    expect(largest<String>(['a', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['only']), 'only', reason: '--err-t3--');
    expect(largest(['a', 'b', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['z', 'b', 'a']), 'z', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
T largest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) > 0) {
      best = item;
    }
  }
  return best;
}
```
