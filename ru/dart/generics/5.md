---
language: dart
exerciseType: 2
---

# --description--

Функция может быть обобщённой сама по себе, не находясь в обобщённом классе. Параметр типа указывается между именем и списком параметров:

```dart
T firstOf<T>(List<T> items) => items.first;

print(firstOf(['fig', 'kiwi'])); // fig, здесь T — String
print(firstOf([10, 20]));        // 10, здесь T — int
```

Одно тело функции, проверенное один раз, переиспользуется для каждого типа. Аргумент типа обычно выводится из аргументов, но его можно указать явно, когда выведению не на что опереться:

```dart
final empty = firstOf<String>(<String>[]); // выбрасывает исключение, но тип понятен
```

Методы внутри класса следуют ровно тому же правилу.

# --instructions--

Дополните код так, чтобы `lastOf` была обобщённой по `T` и возвращала последний элемент списка.

# --seed--

```dart
T lastOf[/](List<T> items) => items[/];

void main() {
  print(lastOf(['fig', 'kiwi']));
  print(lastOf([10, 20]));
}
```

# --answers--

- <T>
- .last
- <int>
- <String>
- .first
- [0]

# --solutions--

```dart
T lastOf<T>(List<T> items) => items.last;

void main() {
  print(lastOf(['fig', 'kiwi']));
  print(lastOf([10, 20]));
}
```

# --output--

kiwi
20
