---
language: dart
exerciseType: 4
---

# --description--

То же ограничение отлично работает и для наименьшего элемента: меняется только знак сравнения. `compareTo` возвращает отрицательное число, когда получатель идёт первым, поэтому `item.compareTo(best) < 0` означает «этот элемент меньше».

# --instructions--

Расставьте элементы так, чтобы определить обобщённую функцию `smallest` с ограничением `Comparable<T>`, возвращающую наименьший элемент списка, и `main`, который выводит наименьший из `['pear', 'fig', 'kiwi']`. Определите функцию до `main`. Программа должна вывести:

```
fig
```

# --answers--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
```

```dart
  var best = items.first;
```

```dart
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
```

```dart
  return best;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(smallest(['pear', 'fig', 'kiwi']));
```

```dart
}
```

# --solutions--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
  return best;
}

void main() {
  print(smallest(['pear', 'fig', 'kiwi']));
}
```

# --output--

fig
