---
language: dart
exerciseType: 2
---

# --description--

При надёжной null safety вопросительный знак может стоять в двух разных местах, и означают они разное:

```dart
Box<int?> a = Box(null); // коробка, которая существует и содержит nullable int
Box<int>? b = null;      // коробки вообще нет, но если бы была, она хранила бы int
```

В `Box<int?>` допускает null **аргумент типа**, поэтому `a.value` имеет тип `int?` и может быть `null`, тогда как сама `a` всегда существует. В `Box<int>?` допускает null **переменная**, поэтому `b` может быть `null`, и чтобы добраться до её содержимого, нужны `b?.value` или `b!.value`.

Простой `T` означает `T extends Object?`, поэтому допускающий null аргумент типа, такой как в `Box<int?>`, совершенно допустим.

# --instructions--

Дополните код так, чтобы `holdsNull` была коробкой, хранящей `null`, а `missing` — переменной, которая может вообще не хранить никакой коробки.

# --seed--

```dart
class Box<T> {
  final T value;
  Box(this.value);
}

void main() {
  final holdsNull = Box<[/]>(null);
  Box<int>[/] missing = null;
  print('value: ${holdsNull.value}');
  print('box: $missing');
}
```

# --answers--

- int?
- ?
- int
- !
- ??

# --solutions--

```dart
class Box<T> {
  final T value;
  Box(this.value);
}

void main() {
  final holdsNull = Box<int?>(null);
  Box<int>? missing = null;
  print('value: ${holdsNull.value}');
  print('box: $missing');
}
```

# --output--

value: null
box: null
