---
language: dart
exerciseType: 2
---

# --description--

Оператор **меньше** `<` возвращает `true`, когда левое значение строго меньше правого значения.

```dart
int a = 2;
int b = 9;
bool result = a < b; // true
```

Если левое значение равно или больше правого, он возвращает `false`.

# --instructions--

Заполните код, чтобы проверить, меньше ли `3` `10`, и сохраните результат в `isSmaller`.

# --seed--

```dart
void main() {
  bool isSmaller = 3 [/] 10;
  print(isSmaller);
}
```

# --answers--

- <
- >
- <=
- !=

# --solutions--

```dart
void main() {
  bool isSmaller = 3 < 10;
  print(isSmaller);
}
```

# --output--

true
