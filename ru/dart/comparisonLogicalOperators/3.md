---
language: dart
exerciseType: 2
---

# --description--

Оператор **больше** `>` возвращает `true`, когда левое значение строго больше правого значения.

```dart
int a = 8;
int b = 3;
bool result = a > b; // true
```

Если левое значение равно или меньше правого, он возвращает `false`.

# --instructions--

Заполните код, чтобы проверить, больше ли `15` `6`, и сохраните результат в `isGreater`.

# --seed--

```dart
void main() {
  bool isGreater = 15 [/] 6;
  print(isGreater);
}
```

# --answers--

- >
- <
- >=
- ==

# --solutions--

```dart
void main() {
  bool isGreater = 15 > 6;
  print(isGreater);
}
```

# --output--

true
