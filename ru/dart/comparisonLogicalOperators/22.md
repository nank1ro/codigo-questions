---
language: dart
exerciseType: 4
---

# --instructions--

Отсортируйте строки кода, чтобы объявить логическую переменную, объединить две переменные с помощью оператора `&&`, сохранить результат в переменной и вывести его. Результат должен быть `true`.

# --answers--

```dart
void main() {
```

```dart
  bool hasPass = false;
```

```dart
  bool isAdmin = true;
```

```dart
  bool outcome = hasPass || isAdmin && !hasPass;
```

```dart
  print(outcome);
```

```dart
}
```

# --solutions--

```dart
void main() {
  bool hasPass = false;
  bool isAdmin = true;
  bool outcome = hasPass || isAdmin && !hasPass;
  print(outcome);
}
```

# --output--

true
