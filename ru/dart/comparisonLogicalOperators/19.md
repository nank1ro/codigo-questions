---
language: dart
exerciseType: 1
---

# --instructions--

Объявите переменную `bool` верхнего уровня с именем `canVote`. Она должна быть `true` только если переменная `age` равна `18` ИЛИ `parental_consent` имеет значение `true`. Используйте переменные `age` = `16` и `parental_consent` = `true`.

# --solutions--

```dart
int number = 42;
int min = 1;
int max = 100;
bool inRange = number >= min && number <= max;

void main() {
  print(inRange);
}
```

# --asserts--

`canVote` должна быть `true` и быть типом `bool`

```dart
  test('test1', () {
    expect(inRange, true, reason: '--err-t1--');
    expect(inRange, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```
