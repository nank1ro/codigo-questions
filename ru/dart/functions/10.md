---
language: dart
exerciseType: 1
---

# --instructions--

Напишите **стрелочную функцию** `square`, которая принимает параметр `int` с именем `n` и возвращает `n * n`.

Используйте синтаксис `=>` (без фигурных скобок и ключевого слова `return`).

Пример:
```dart
print(square(4));  // 16
print(square(5));  // 25
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`square(4)` должна возвращать `16`
```dart
  test('test1', () {
    expect(square(4), 16, reason: '--err-t1--');
  });
```

`square(5)` должна возвращать `25`
```dart
  test('test2', () {
    expect(square(5), 25, reason: '--err-t2--');
  });
```

`square(0)` должна возвращать `0`
```dart
  test('test3', () {
    expect(square(0), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int square(int n) => n * n;
```
