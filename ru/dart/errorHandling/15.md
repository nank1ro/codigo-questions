---
language: dart
exerciseType: 1
---

# --description--

Расположение блока `try` определяет, сколько работы уничтожает один сбой. Вокруг цикла первый плохой элемент завершает всю партию; **внутри** цикла теряется только этот элемент, а остальные всё ещё обрабатываются:

```dart
for (final text in texts) {
  try {
    total += int.parse(text);
  } on FormatException {
    continue;
  }
}
```

Это повседневная форма для импорта файла, чтения списка настроек или обработки очереди сообщений: одна повреждённая строка не должна выбрасывать хорошие. Правило остаётся прежним — держите блок `try` вокруг инструкции, которая может дать сбой, и не больше.

# --instructions--

Напишите функцию `sumOf`, которая принимает `List<String>` `texts` и возвращает сумму тех из них, которые описывают целое число, игнорируя остальные.

Используйте `int.parse` с `try`/`catch` внутри цикла, чтобы одна нечитаемая запись не остановила остальные.

Пример:
```dart
print(sumOf(['1', 'x', '3'])); // 4
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumOf(['1', 'x', '3'])` должна вернуть `4`

```dart
  test('test1', () {
    expect(sumOf(['1', 'x', '3']), 4, reason: '--err-t1--');
  });
```

`sumOf(['10', '20', '30'])` должна вернуть `60`

```dart
  test('test2', () {
    expect(sumOf(['10', '20', '30']), 60, reason: '--err-t2--');
    expect(sumOf(['-5', '5']), 0, reason: '--err-t2--');
  });
```

Список без читаемого числа и пустой список должны оба дать `0`

```dart
  test('test3', () {
    expect(sumOf(['x', 'y', '']), 0, reason: '--err-t3--');
    expect(sumOf(<String>[]), 0, reason: '--err-t3--');
    expect(sumOf(['x', '7']), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sumOf(List<String> texts) {
  var total = 0;
  for (final text in texts) {
    try {
      total += int.parse(text);
    } on FormatException {
      continue;
    }
  }
  return total;
}
```
