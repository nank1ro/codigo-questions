---
language: dart
exerciseType: 1
---

# --description--

За одним блоком `try` могут следовать **несколько** предложений, каждое из которых восстанавливается после своего сбоя. Dart сравнивает выброшенный объект с ними сверху вниз и выполняет **первое** совпавшее:

```dart
try {
  return names[int.parse(text)];
} on FormatException {
  return 'not a number';
} on RangeError {
  return 'out of range';
} catch (e) {
  return 'unknown problem';
}
```

Чтение списка по индексу, которого не существует, выбрасывает **`RangeError`**, поэтому два сбоя одной и той же строки получают разные ответы.

Поскольку побеждает первое совпадение, порядок важен: предложение для общего типа, размещённое выше более специфичного, всегда победит, оставив специфичное предложение недостижимым. Пишите специфичные предложения первыми, а голый `catch` — последним, если нужна страховка.

Предложение `on RangeError` выше приведено лишь для того, чтобы показать, как упорядочены несколько предложений. `RangeError` сигнализирует об ошибке в коде, а не о состоянии, которое программа не могла контролировать, и в одном из следующих упражнений объясняется, почему такой сбой нужно предотвращать, а не перехватывать.

# --instructions--

Напишите функцию `lookup`, которая принимает `List<int>` `scores` и `String` `text` и возвращает `'score '`, за которым следует элемент `scores` на позиции, которую описывает `text`.

Возвращайте `'not a number'`, когда `text` не является целым числом, `'out of range'`, когда позиции не существует, и `'unknown problem'` во всех остальных случаях.

Пример:
```dart
print(lookup([10, 20, 30], '1')); // score 20
print(lookup([10, 20, 30], 'two')); // not a number
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`lookup([10, 20, 30], '1')` должна вернуть `'score 20'`

```dart
  test('test1', () {
    expect(lookup([10, 20, 30], '1'), 'score 20', reason: '--err-t1--');
    expect(lookup([10, 20, 30], '0'), 'score 10', reason: '--err-t1--');
  });
```

`lookup([10, 20, 30], 'two')` должна вернуть `'not a number'`

```dart
  test('test2', () {
    expect(lookup([10, 20, 30], 'two'), 'not a number', reason: '--err-t2--');
    expect(lookup([10, 20, 30], ''), 'not a number', reason: '--err-t2--');
  });
```

`lookup([10, 20, 30], '7')` должна вернуть `'out of range'`

```dart
  test('test3', () {
    expect(lookup([10, 20, 30], '7'), 'out of range', reason: '--err-t3--');
    expect(lookup([10, 20, 30], '-1'), 'out of range', reason: '--err-t3--');
    expect(lookup(<int>[], '0'), 'out of range', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String lookup(List<int> scores, String text) {
  try {
    return 'score ${scores[int.parse(text)]}';
  } on FormatException {
    return 'not a number';
  } on RangeError {
    return 'out of range';
  } catch (e) {
    return 'unknown problem';
  }
}
```
