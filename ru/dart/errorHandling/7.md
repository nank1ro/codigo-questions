---
language: dart
exerciseType: 1
---

# --description--

Ваш собственный код выбрасывает исключения так же, как библиотека. `Exception('message')` создаёт простое исключение с кратким пояснением, а `throw` отправляет его в путь:

```dart
if (amount > balance) {
  throw Exception('insufficient funds');
}
```

Сообщение не теряется: `toString()` складывает вместе слово `Exception`, двоеточие и сообщение — именно это печатает отчёт о необработанном исключении.

```dart
print(Exception('insufficient funds')); // Exception: insufficient funds
```

Выбрасывание лучше, чем возврат выдуманного значения вроде `-1`: вызывающий код не может забыть его проверить, а причина путешествует вместе с исключением.

# --instructions--

Напишите функцию `withdraw`, которая принимает два значения `int` — `balance` и `amount` — и возвращает оставшийся баланс.

Когда `amount` больше `balance`, вместо этого выбросьте `Exception('insufficient funds')`.

Пример:
```dart
print(withdraw(100, 30)); // 70
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`withdraw(100, 30)` должна вернуть `70`

```dart
  test('test1', () {
    expect(withdraw(100, 30), 70, reason: '--err-t1--');
    expect(withdraw(50, 50), 0, reason: '--err-t1--');
  });
```

`withdraw(100, 300)` должна выбросить `Exception`

```dart
  test('test2', () {
    expect(() => withdraw(100, 300), throwsA(isA<Exception>()), reason: '--err-t2--');
  });
```

Выброшенное исключение должно печататься как `Exception: insufficient funds`

```dart
  test('test3', () {
    try {
      withdraw(0, 1);
      fail('--err-t3--');
    } catch (e) {
      expect(e.toString(), 'Exception: insufficient funds', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int withdraw(int balance, int amount) {
  if (amount > balance) {
    throw Exception('insufficient funds');
  }
  return balance - amount;
}
```
