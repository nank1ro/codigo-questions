---
language: dart
exerciseType: 4
---

# --description--

Nullability влияет на то, как вы объявляете **именованные параметры**. Именованный параметр с nullable-типом является необязательным: если вызывающий его опустит, он просто будет `null`.

```dart
String label({String? title}) => title ?? 'untitled';

print(label());               // untitled
print(label(title: 'Notes')); // Notes
```

Именованный параметр с non-nullable типом и без значения по умолчанию не имел бы значения при пропуске, поэтому Dart требует пометить его как `required`; тогда вызывающий обязан всегда его передавать:

```dart
String label({required String name, String? title}) { ... }

label(name: 'Ada');               // ok
label(name: 'Ada', title: 'Dr.'); // ok
label(title: 'Dr.');              // error: the named parameter 'name' is required
```

# --instructions--

Расположите элементы так, чтобы определить функцию `label` с обязательным именованным параметром `name` и необязательным nullable именованным параметром `title`, возвращающую только `name`, когда `title` равен `null`, и `'$title $name'` в противном случае. Сначала определите функцию, затем `main`. Программа должна вывести:

```
Ada
Countess Lovelace
```

# --answers--

```dart
String label({required String name, String? title}) {
```

```dart
  if (title == null) return name;
```

```dart
  return '$title $name';
```

```dart
}
```

```dart
void main() {
```

```dart
  print(label(name: 'Ada'));
```

```dart
  print(label(name: 'Lovelace', title: 'Countess'));
```

```dart
}
```

# --solutions--

```dart
String label({required String name, String? title}) {
  if (title == null) return name;
  return '$title $name';
}

void main() {
  print(label(name: 'Ada'));
  print(label(name: 'Lovelace', title: 'Countess'));
}
```

# --output--

Ada
Countess Lovelace
