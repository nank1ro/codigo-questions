---
language: dart
exerciseType: 2
---

# --description--

Оператор **вычитания с присваиванием** `-=` вычитает значение из переменной и сохраняет результат обратно в переменной.

```dart
int lives = 3;
lives -= 1; // lives теперь равна 2
```

Это сокращение для `lives = lives - 1`.

# --instructions--

Заполните код, чтобы вычесть `5` из переменной `stock` используя `-=`.

# --seed--

```dart
void main() {
  int stock = 20;
  stock [/] 5;
  print(stock);
}
```

# --answers--

- -=
- =
- +=
- -

# --solutions--

```dart
void main() {
  int stock = 20;
  stock -= 5;
  print(stock);
}
```

# --output--

15
