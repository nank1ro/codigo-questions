---
language: dart
exerciseType: 1
---

# --instructions--

Магазин предоставляет 20% скидку на товар. Объявите переменную `double` `price` со значением `50.0`. Затем используйте `*=`, чтобы применить скидку, умножив `price` на `0.8`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
double price = 50.0;
```

# --after-seed--

```dart
  return price;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`price` должна быть `40.0`

```dart
  test('test1', () {
    expect(result(), closeTo(40.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double price = 50.0;
price *= 0.8;
```
