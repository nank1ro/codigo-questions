---
language: dart
exerciseType: 1
---

# --instructions--

Объявите переменную `int` верхнего уровня `remainder` со значением `29`. Затем используйте `%=`, чтобы присвоить ей остаток от деления на `6`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`remainder` должна быть `5`

```dart
  test('test1', () {
    expect(remainder, 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 29;

void main() {
  remainder %= 6;
  print(remainder);
}
```
