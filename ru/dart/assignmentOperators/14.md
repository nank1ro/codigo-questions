---
language: dart
exerciseType: 1
---

# --instructions--

Объявите переменную `int` `n` со значением `2`. Затем в `main` примените следующие операторы присваивания в порядке:
- `n *= 10` (умножить на 10)
- `n += 5` (добавить 5)
- `n -= 3` (вычесть 3)

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int n = 2;
```

# --after-seed--

```dart
  return n;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`n` должна быть `22`

```dart
  test('test1', () {
    expect(result(), 22, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 2;
n *= 10;
n += 5;
n -= 3;
```
