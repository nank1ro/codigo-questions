---
language: dart
exerciseType: 1
---

# --instructions--

Объявите переменную `int` верхнего уровня `width` со значением `6`. Затем используйте `*=`, чтобы умножить её на `4`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`width` должна быть `24`

```dart
  test('test1', () {
    expect(width, 24, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int width = 6;

void main() {
  width *= 4;
  print(width);
}
```
