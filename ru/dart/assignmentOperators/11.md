---
language: dart
exerciseType: 1
---

# --instructions--

Объявите переменную `double` верхнего уровня `value` со значением `20.0`. Затем используйте `/=`, чтобы разделить её на `4`. Обратите внимание, что `/=` в Dart всегда производит `double`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value` должна быть `5.0`

```dart
  test('test1', () {
    expect(value, closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;

void main() {
  value /= 4;
  print(value);
}
```
