---
language: dart
exerciseType: 2
---

# --description--

Оператор **умножения с присваиванием** `*=` умножает переменную на значение и сохраняет результат обратно в переменной.

```dart
int side = 4;
side *= 3; // side теперь равна 12
```

Это сокращение для `side = side * 3`.

# --instructions--

Заполните код, чтобы умножить `price` на `3` используя `*=`.

# --seed--

```dart
void main() {
  int price = 7;
  price [/] 3;
  print(price);
}
```

# --answers--

- *=
- =
- +=
- *

# --solutions--

```dart
void main() {
  int price = 7;
  price *= 3;
  print(price);
}
```

# --output--

21
