---
language: dart
exerciseType: 2
---

# --description--

Оператор **сложения с присваиванием** `+=` добавляет значение к переменной и сохраняет результат обратно в переменной.

```dart
int score = 10;
score += 5; // score теперь равна 15
```

Это сокращение для `score = score + 5`.

# --instructions--

Заполните код, чтобы добавить `8` к переменной `total` используя `+=`.

# --seed--

```dart
void main() {
  int total = 12;
  total [/] 8;
  print(total);
}
```

# --answers--

- +=
- =
- -=
- +

# --solutions--

```dart
void main() {
  int total = 12;
  total += 8;
  print(total);
}
```

# --output--

20
