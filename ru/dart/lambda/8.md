---
language: dart
exerciseType: 1
---

# --description--

Поскольку и `map`, и `where` возвращают `Iterable`, их вызовы можно **объединять в цепочку** друг за другом. Каждый шаг получает результат предыдущего, а `toList()` вызывается один раз в конце:

```dart
var numbers = [1, 2, 3, 4, 5, 6];

var result = numbers.where((n) => n > 3).map((n) => n * 10).toList();
print(result); // [40, 50, 60]
```

# --instructions--

Напишите функцию `oddSquares`, которая принимает параметр `List<int>` с именем `numbers` и возвращает `List<int>`, содержащий квадраты только нечётных чисел, в исходном порядке. Используйте `where` и `map` с анонимными функциями.

Пример:
```dart
print(oddSquares([1, 2, 3, 4, 5])); // [1, 9, 25]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`oddSquares([1, 2, 3, 4, 5])` должна возвращать `[1, 9, 25]`

```dart
  test('test1', () {
    expect(oddSquares([1, 2, 3, 4, 5]), [1, 9, 25], reason: '--err-t1--');
  });
```

`oddSquares([2, 4, 6])` должна возвращать `[]`

```dart
  test('test2', () {
    expect(oddSquares([2, 4, 6]), [], reason: '--err-t2--');
  });
```

`oddSquares([7, 10, 3])` должна возвращать `[49, 9]`

```dart
  test('test3', () {
    expect(oddSquares([7, 10, 3]), [49, 9], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> oddSquares(List<int> numbers) {
  return numbers.where((n) => n % 2 != 0).map((n) => n * n).toList();
}
```
