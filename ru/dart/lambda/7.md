---
language: dart
exerciseType: 2
---

# --description--

Два других очень распространённых метода, принимающих анонимную функцию, — это `map` и `where`:

- `map` преобразует каждый элемент с помощью функции и возвращает новые значения
- `where` оставляет только те элементы, для которых функция возвращает `true`

Оба возвращают ленивый `Iterable`; вызовите `toList()`, чтобы превратить результат в `List`:

```dart
var numbers = [1, 2, 3];

var squares = numbers.map((n) => n * n).toList();
print(squares); // [1, 4, 9]

var big = numbers.where((n) => n > 1).toList();
print(big); // [2, 3]
```

# --instructions--

Дополните код так, чтобы `doubled` содержало все числа, умноженные на `2`, а `evens` — только чётные числа.

# --seed--

```dart
void main() {
  var numbers = [1, 2, 3, 4];
  var doubled = numbers.[/]((n) => n * 2).toList();
  var evens = numbers.[/]((n) => n % 2 == 0).toList();
  print(doubled);
  print(evens);
}
```

# --answers--

- map
- where
- filter
- forEach
- select
- each

# --solutions--

```dart
void main() {
  var numbers = [1, 2, 3, 4];
  var doubled = numbers.map((n) => n * 2).toList();
  var evens = numbers.where((n) => n % 2 == 0).toList();
  print(doubled);
  print(evens);
}
```

# --output--

[2, 4, 6, 8]
[2, 4]
