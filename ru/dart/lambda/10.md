---
language: dart
exerciseType: 1
---

# --description--

`reduce` объединяет все элементы списка в одно значение. Его анонимная функция принимает два параметра: значение, **накопленное к данному моменту**, и **следующий элемент**, и возвращает новое накопленное значение. Первый элемент используется в качестве отправной точки:

```dart
var numbers = [2, 3, 4];

var product = numbers.reduce((total, n) => total * n);
print(product); // 24
```

`reduce` выбрасывает ошибку на пустом списке, поскольку нет первого элемента, с которого можно начать.

# --instructions--

Напишите функцию `longest`, которая принимает непустой параметр `List<String>` с именем `words` и возвращает самое длинное слово, используя `reduce` с анонимной функцией. Если два слова имеют одинаковую длину, оставьте то, которое идёт первым.

Пример:
```dart
print(longest(['sun', 'planet', 'moon'])); // planet
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`longest(['sun', 'planet', 'moon'])` должна возвращать `'planet'`

```dart
  test('test1', () {
    expect(longest(['sun', 'planet', 'moon']), 'planet', reason: '--err-t1--');
  });
```

`longest(['a'])` должна возвращать `'a'`

```dart
  test('test2', () {
    expect(longest(['a']), 'a', reason: '--err-t2--');
  });
```

`longest(['red', 'blue', 'pink'])` должна возвращать `'blue'`

```dart
  test('test3', () {
    expect(longest(['red', 'blue', 'pink']), 'blue', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String longest(List<String> words) {
  return words.reduce((best, word) => word.length > best.length ? word : best);
}
```
