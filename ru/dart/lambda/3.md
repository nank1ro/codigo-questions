---
language: dart
exerciseType: 2
---

# --description--

Когда тело состоит из одного выражения, анонимная функция может использовать **стрелочный синтаксис** `=>`, как и именованная функция. Стрелка заменяет фигурные скобки и ключевое слово `return`:

```dart
var add = (int a, int b) => a + b;

print(add(2, 3)); // 5
```

Эта краткая форма — безусловно самый распространённый способ записи анонимных функций в Dart.

# --instructions--

Дополните код так, чтобы в `triple` сохранялась стрелочная функция, возвращающая свой аргумент, умноженный на `3`, и программа выводила `12`.

# --seed--

```dart
void main() {
  var triple = (int n) [/] n [/] 3;
  print(triple(4));
}
```

# --answers--

- =>
- *
- ->
- +
- return
- {

# --solutions--

```dart
void main() {
  var triple = (int n) => n * 3;
  print(triple(4));
}
```

# --output--

12
