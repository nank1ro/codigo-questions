---
language: dart
exerciseType: 4
---

# --description--

Многие методы коллекций Dart принимают функцию в качестве аргумента, и анонимные функции — естественный способ её передать. Простейший из них — `forEach`, который вызывает данную функцию один раз для каждого элемента списка:

```dart
var fruits = ['apple', 'kiwi'];

fruits.forEach((fruit) {
  print('I like $fruit');
});
// I like apple
// I like kiwi
```

Тип параметра выводится из списка, поэтому `fruit` — это `String`, без необходимости указывать это явно.

# --instructions--

Упорядочьте элементы, чтобы вывести приветствие для каждого имени из `names` с помощью `forEach`. Программа должна вывести:

```
Hi Ann
Hi Bob
```

# --answers--

```dart
void main() {
```

```dart
  var names = ['Ann', 'Bob'];
```

```dart
  names.forEach((name) {
```

```dart
    print('Hi $name');
```

```dart
  });
```

```dart
}
```

# --solutions--

```dart
void main() {
  var names = ['Ann', 'Bob'];
  names.forEach((name) {
    print('Hi $name');
  });
}
```

# --output--

Hi Ann
Hi Bob
