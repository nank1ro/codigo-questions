---
language: dart
exerciseType: 1
---

# --description--

Анонимная функция может принимать параметры и `return` значение, точно так же, как именованная. Тип возвращаемого значения не указывается: Dart **выводит** его из операторов `return` в теле.

```dart
var add = (int a, int b) {
  return a + b;
};

print(add(2, 3)); // 5
```

# --instructions--

Сохраните в переменную `square` анонимную функцию, которая принимает параметр `int` с именем `n` и возвращает `n`, умноженное на само себя.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var square = 
```

# --after-seed--

```dart
  return square;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`square` должно быть функцией

```dart
  test('test1', () {
    expect(result(), isA<Function>(), reason: '--err-t1--');
  });
```

`square(4)` должно быть `16`

```dart
  test('test2', () {
    expect(result()(4), 16, reason: '--err-t2--');
  });
```

`square(-3)` должно быть `9`

```dart
  test('test3', () {
    expect(result()(-3), 9, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var square = (int n) {
  return n * n;
};
```
