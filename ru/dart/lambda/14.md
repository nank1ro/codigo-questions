---
language: dart
exerciseType: 4
---

# --description--

Анонимные функции поддерживают те же виды параметров, что и именованные функции: **необязательные позиционные** параметры в `[]` и **именованные** параметры в `{}`, оба со значениями по умолчанию:

```dart
var repeat = (String text, [int times = 2]) => text * times;
print(repeat('ab'));    // abab
print(repeat('ab', 3)); // ababab

var describe = ({required String item, int count = 1}) => '$count x $item';
print(describe(item: 'apple'));           // 1 x apple
print(describe(item: 'pear', count: 4));  // 4 x pear
```

# --instructions--

Упорядочьте элементы так, чтобы в `greet` сохранялась анонимная функция с именованным параметром `name`, значение по умолчанию которого равно `'World'`, затем вызовите её сначала без аргументов, а потом с `name: 'Dart'`. Программа должна вывести:

```
Hello, World!
Hello, Dart!
```

# --answers--

```dart
void main() {
```

```dart
  var greet = ({String name = 'World'}) {
```

```dart
    print('Hello, $name!');
```

```dart
  };
```

```dart
  greet();
```

```dart
  greet(name: 'Dart');
```

```dart
}
```

# --solutions--

```dart
void main() {
  var greet = ({String name = 'World'}) {
    print('Hello, $name!');
  };
  greet();
  greet(name: 'Dart');
}
```

# --output--

Hello, World!
Hello, Dart!
