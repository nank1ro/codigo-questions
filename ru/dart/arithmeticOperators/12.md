---
language: dart
exerciseType: 1
---

# --instructions--

Объявите две переменные верхнего уровня `int`: `width` со значением `7` и `height` со значением `9`. Затем объявите переменную верхнего уровня `int` с именем `area`, которая сохраняет их произведение.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`area` должно быть равно `63`

```dart
  test('test1', () {
    expect(area, 63, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int width = 7;
int height = 9;
int area = width * height;

void main() {
  print(area);
}
```
