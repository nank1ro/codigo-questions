---
language: dart
exerciseType: 1
---

# --instructions--

Объявите две переменные верхнего уровня `int`: `total` со значением `29` и `perGroup` со значением `4`. Затем объявите переменную верхнего уровня `int` с именем `groups`, которая сохраняет сколько полных групп `4` помещается в `29` (используйте целое деление `~/`).

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`groups` должно быть равно `7`

```dart
  test('test1', () {
    expect(groups, 7, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 29;
int perGroup = 4;
int groups = total ~/ perGroup;

void main() {
  print(groups);
}
```
