---
language: dart
exerciseType: 1
---

# --instructions--

Объявите две переменные верхнего уровня `int`: `x` со значением `25` и `y` со значением `9`. Затем объявите переменную верхнего уровня `int` с именем `diff`, которая сохраняет результат вычитания `y` из `x`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`diff` должно быть равно `16`

```dart
  test('test1', () {
    expect(diff, 16, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 25;
int y = 9;
int diff = x - y;

void main() {
  print(diff);
}
```
