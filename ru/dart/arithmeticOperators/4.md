---
language: dart
exerciseType: 2
---

# --description--

Оператор **деления** `/` делит одно значение на другое. В Dart `/` **всегда возвращает `double`**, даже когда результат — целое число.

```dart
int a = 10;
int b = 4;
double result = a / b; // 2.5

double exact = 10 / 2; // 5.0  (не 5!)
```

Всегда используйте переменную `double` для сохранения результата `/`.

# --instructions--

Завершите код, чтобы разделить `9` на `4` и сохраните результат в `result`.

# --seed--

```dart
void main() {
  double result = 9 [/] 4;
  print(result);
}
```

# --answers--

- /
- *
- +
- -

# --solutions--

```dart
void main() {
  double result = 9 / 4;
  print(result);
}
```

# --output--

2.25
