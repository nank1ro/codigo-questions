---
language: dart
exerciseType: 1
---

# --instructions--

Используйте оператор модуля `%` чтобы найти остаток при делении `17` на `5`. Сохраните результат в переменную верхнего уровня с именем `remainder`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Переменная `remainder` должна быть равна `2`

```dart
  test('test1', () {
    expect(remainder, 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 17 % 5;

void main() {
  print(remainder);
}
```
