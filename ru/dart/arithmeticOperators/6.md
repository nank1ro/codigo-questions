---
language: dart
exerciseType: 3
---

# --instructions--

Что делает оператор `+` в Dart при использовании с двумя значениями `int`?

# --answers--

- Объединяет их в String
- Складывает их вместе и возвращает int
- Складывает их вместе и всегда возвращает double
- Возводит первое в степень второго

# --solutions--

- Складывает их вместе и возвращает int
