---
language: dart
exerciseType: 1
---

# --instructions--

Объявите переменную верхнего уровня `double` с именем `result`, которая сохраняет результат деления `7` на `2`. Помните, что `/` в Dart всегда возвращает `double`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` должно быть `double` со значением `3.5`

```dart
  test('test1', () {
    expect(result, isA<double>(), reason: '--err-t1--');
    expect(result, closeTo(3.5, 0.001), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double result = 7 / 2;

void main() {
  print(result);
}
```
