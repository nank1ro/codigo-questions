---
language: dart
exerciseType: 1
---

# --instructions--

Магазин продает `itemCount` товаров по `pricePerItem` долларов каждый с `discount` дольларов, вычтенными из суммы. Объявите эти три переменные верхнего уровня `int`: `itemCount` = `6`, `pricePerItem` = `15`, `discount` = `10`. Затем объявите переменную верхнего уровня `int` с именем `totalPrice`, которая вычисляет итоговую сумму после скидки.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`totalPrice` должно быть равно `80`

```dart
  test('test1', () {
    expect(totalPrice, 80, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int itemCount = 6;
int pricePerItem = 15;
int discount = 10;
int totalPrice = itemCount * pricePerItem - discount;

void main() {
  print(totalPrice);
}
```
