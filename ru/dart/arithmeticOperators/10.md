---
language: dart
exerciseType: 1
---

# --instructions--

Объявите две переменные верхнего уровня `int`: `a` со значением `12` и `b` со значением `8`. Затем объявите третью переменную верхнего уровня `int` с именем `total`, которая сохраняет их сумму.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`a` должно быть `12`, `b` должно быть `8`, а `total` должно быть `20`

```dart
  test('test1', () {
    expect(a, 12, reason: '--err-t1--');
    expect(b, 8, reason: '--err-t2--');
    expect(total, 20, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 12;
int b = 8;
int total = a + b;

void main() {
  print(total);
}
```
