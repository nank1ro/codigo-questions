---
language: dart
exerciseType: 1
---

# --description--

Чтобы запустить два future одновременно, `Future.wait` не обязателен. `async`-функция начинает выполняться сразу, как только её **вызвали**, вплоть до первого `await`; future, который ты получаешь, — это уже идущая работа. Поэтому приём такой: **сначала вызвать, потом дождаться**:

```dart
Future<int> pages() => Future.delayed(const Duration(milliseconds: 20), () => 300);
Future<int> words() => Future.delayed(const Duration(milliseconds: 20), () => 90000);

Future<double> average() async {
  final p = pages();          // both timers start now
  final w = words();
  return await w / await p;   // waits once, about 20 ms in total
}
```

Сравни с `return await words() / await pages();`, где `pages()` вызывается только после завершения `words()`: результат тот же, времени вдвое больше. Предпочитай конкурентную форму всякий раз, когда второму вызову не нужен результат первого.

# --instructions--

Функция `slow(value)` возвращает `Future<int>`, завершающийся значением `value` после короткой задержки, и записывает `start <value>` при вызове и `end <value>` при завершении в список `calls`. Напиши `async`-функцию `addConcurrently`, которая принимает два `int` `a` и `b`, вызывает `slow(a)` и `slow(b)` так, чтобы оба выполнялись **одновременно**, и возвращает `Future<int>`, завершающийся суммой двух результатов.

Пример:
```dart
print(await addConcurrently(2, 3)); // 5
print(calls); // [start 2, start 3, end 2, end 3]
```

# --before-seed--

```dart
final calls = <String>[];

Future<int> slow(int value) async {
  calls.add('start $value');
  await Future.delayed(const Duration(milliseconds: 5));
  calls.add('end $value');
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`addConcurrently(2, 3)` должна завершиться значением `5`

```dart
  test('test1', () async {
    calls.clear();
    expect(await addConcurrently(2, 3), 5, reason: '--err-t1--');
  });
```

`addConcurrently(2, 3)` должна вызвать `slow(2)` и `slow(3)` до того, как хотя бы один из них завершится, поэтому `calls` должен быть `[start 2, start 3, end 2, end 3]`

```dart
  test('test2', () async {
    calls.clear();
    await addConcurrently(2, 3);
    expect(calls, ['start 2', 'start 3', 'end 2', 'end 3'], reason: '--err-t2--');
  });
```

`addConcurrently(10, -4)` должна завершиться значением `6`, и оба вызова должны стартовать до того, как хотя бы один из них завершится

```dart
  test('test3', () async {
    calls.clear();
    expect(await addConcurrently(10, -4), 6, reason: '--err-t3--');
    expect(calls, ['start 10', 'start -4', 'end 10', 'end -4'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> addConcurrently(int a, int b) async {
  final first = slow(a);
  final second = slow(b);
  return await first + await second;
}
```
