---
language: dart
exerciseType: 4
---

# --description--

`Future.wait` — инструмент для сценария «загрузить несколько вещей, а потом продолжить». Типичная форма такова: собрать список future, применить к нему `await Future.wait`, а затем использовать получившийся список:

```dart
Future<int> stock() => Future.delayed(const Duration(milliseconds: 10), () => 8);
Future<int> orders() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<void> main() async {
  final counts = await Future.wait([stock(), orders()]);
  print(counts); // [8, 3]
}
```

`orders()` завершается первой, но список всё равно следует порядку вызовов: сначала `stock()`, затем `orders()`.

# --instructions--

Расставь элементы так, чтобы определить функцию `one`, которая завершается значением `1` через `10` миллисекунд, функцию `two`, которая завершается значением `2` через `5` миллисекунд, и `async`-функцию `main`, которая печатает `loading`, дожидается обеих функций через `Future.wait` в `values` и печатает `values`. Определи сначала `one`, затем `two`, затем `main`. Программа должна вывести:

```
loading
[1, 2]
```

# --answers--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
```

```dart
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);
```

```dart
Future<void> main() async {
```

```dart
  print('loading');
```

```dart
  final values = await Future.wait([one(), two()]);
```

```dart
  print(values);
```

```dart
}
```

# --solutions--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);

Future<void> main() async {
  print('loading');
  final values = await Future.wait([one(), two()]);
  print(values);
}
```

# --output--

loading
[1, 2]
