---
language: dart
exerciseType: 1
---

# --description--

Future может завершиться и **ошибкой**. Когда `async`-функция бросает исключение, оно не выходит наружу сразу: оно становится ошибкой возвращённого future. Тот, кто делает `await` над этим future, видит ошибку брошенной в точке `await`, поэтому её можно обработать обычным `try`/`catch`:

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s); // throws FormatException for 'abc'
}

Future<int> orZero(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return 0;
  }
}
```

`await` внутри `try` принципиально важен: `return parseLater(s);` передал бы future вызывающему **без ожидания**, поэтому ошибка пришла бы, когда блок `try` уже закончился, и `catch` никогда бы не выполнился.

# --instructions--

Функция `parseLater(s)` возвращает `Future<int>`, который завершается числом, записанным в `s`, или ошибкой, если `s` — не число. Напиши `async`-функцию `safeParse`, которая принимает `String` `s` и возвращает `Future<int>`, завершающийся разобранным числом или значением `-1`, если `parseLater` завершится ошибкой.

Пример:
```dart
print(await safeParse('12'));  // 12
print(await safeParse('abc')); // -1
```

# --before-seed--

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s);
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeParse('12')` должна завершиться значением `12`

```dart
  test('test1', () async {
    expect(await safeParse('12'), 12, reason: '--err-t1--');
  });
```

`safeParse('abc')` должна завершиться значением `-1`, а не бросить исключение

```dart
  test('test2', () async {
    expect(await safeParse('abc'), -1, reason: '--err-t2--');
  });
```

`safeParse('-7')` должна завершиться значением `-7`, а `safeParse('')` — значением `-1`

```dart
  test('test3', () async {
    expect(await safeParse('-7'), -7, reason: '--err-t3--');
    expect(await safeParse(''), -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> safeParse(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return -1;
  }
}
```
