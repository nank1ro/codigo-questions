---
language: dart
exerciseType: 1
---

# --description--

Внутри `async`-функции `await` позволяет писать асинхронные шаги так, будто это обычный последовательный код. Каждый `await` ждёт свой future, и следующая строка выполняется только тогда, когда значение уже есть:

```dart
Future<int> width() => Future.delayed(const Duration(milliseconds: 5), () => 4);
Future<int> height() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<int> area() async {
  final w = await width();
  final h = await height();
  return w * h; // 12
}
```

`await` можно использовать и прямо внутри выражения: `return await width() * await height();` даёт тот же результат.

# --instructions--

Функция `price(item)` возвращает `Future<int>` с ценой `item`. Используй `await`, чтобы получить цену `'apple'` и цену `'milk'`, и сохрани их сумму в `total`.

# --before-seed--

```dart
const prices = {'apple': 3, 'bread': 5, 'milk': 2};

Future<int> price(String item) {
  return Future.delayed(const Duration(milliseconds: 5), () => prices[item]!);
}

Future<dynamic> result() async {
```

# --seed--

```dart
var total = 0;
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` должно быть равно `5`

```dart
  test('test1', () async {
    expect(await result(), 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var total = 0;
total = await price('apple') + await price('milk');
```
