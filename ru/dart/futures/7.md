---
language: dart
exerciseType: 4
---

# --description--

Когда функция доходит до `await`, она **приостанавливается** на этой строке, а остальная часть программы продолжает работу. Строки после `await` выполнятся только тогда, когда future завершится. Поэтому чтение `async`-функции сверху вниз показывает точный порядок её эффектов:

```dart
Future<int> load() async {
  await Future.delayed(const Duration(milliseconds: 5));
  return 7;
}

Future<void> main() async {
  print('loading');
  final n = await load();
  print('value: $n');
  print('done');
}
// loading
// value: 7
// done
```

# --instructions--

Расставь элементы так, чтобы определить `async`-функцию `fetchName`, которая ждёт `10` миллисекунд и затем возвращает `'Ada'`, и `async`-функцию `main`, которая печатает `fetching`, затем ждёт `fetchName()` в `name`, затем печатает `Hello, `, а за ним имя и `!`, и в конце печатает `done`. Определи `fetchName` до `main`. Программа должна вывести:

```
fetching
Hello, Ada!
done
```

# --answers--

```dart
Future<String> fetchName() async {
```

```dart
  await Future.delayed(const Duration(milliseconds: 10));
```

```dart
  return 'Ada';
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  print('fetching');
```

```dart
  final name = await fetchName();
```

```dart
  print('Hello, $name!');
```

```dart
  print('done');
```

```dart
}
```

# --solutions--

```dart
Future<String> fetchName() async {
  await Future.delayed(const Duration(milliseconds: 10));
  return 'Ada';
}

Future<void> main() async {
  print('fetching');
  final name = await fetchName();
  print('Hello, $name!');
  print('done');
}
```

# --output--

fetching
Hello, Ada!
done
