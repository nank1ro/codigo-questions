---
language: dart
exerciseType: 1
---

# --description--

Пометка функции как `async` делает две вещи: разрешает `await` в теле и заставляет функцию **возвращать `Future`**. То, что ты возвращаешь через `return`, становится значением, которым завершается future, поэтому объявленный тип возврата — `Future<T>`, хотя тело возвращает обычное `T`:

```dart
Future<String> label(int n) async {
  return 'item $n';
}

Future<void> main() async {
  print(await label(3)); // item 3
}
```

`Future.value` здесь не нужен: ключевое слово `async` само оборачивает возвращаемое значение.

# --instructions--

Напиши `async`-функцию `greet`, которая принимает `String` `name` и возвращает `Future<String>`, завершающийся значением `Hello, `, за которым идут имя и восклицательный знак.

Пример:
```dart
print(await greet('Ada')); // Hello, Ada!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greet('Ada')` должна завершиться значением `'Hello, Ada!'`

```dart
  test('test1', () async {
    expect(await greet('Ada'), 'Hello, Ada!', reason: '--err-t1--');
  });
```

`greet('Linus')` должна завершиться значением `'Hello, Linus!'`

```dart
  test('test2', () async {
    expect(await greet('Linus'), 'Hello, Linus!', reason: '--err-t2--');
  });
```

`greet` должна возвращать `Future<String>`

```dart
  test('test3', () async {
    expect(greet('Bob'), isA<Future<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<String> greet(String name) async {
  return 'Hello, $name!';
}
```
