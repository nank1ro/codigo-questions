---
language: dart
exerciseType: 1
---

# --description--

В Dart нет метода `sorted`. `sort` принадлежит `List`: он меняет порядок элементов списка **на месте** и ничего не возвращает:

```dart
final numbers = [3, 1, 2];
numbers.sort();
print(numbers); // [1, 2, 3]
```

Поскольку он возвращает `void`, результатом нельзя воспользоваться вовсе: `final sorted = numbers.sort();` даёт значение, которое компилятор не позволит прочитать. Идиома для упорядоченной **копии** — `toList()` с последующим каскадом `..sort()`: `toList()` создаёт копию, а `..` вызывает на ней `sort`, при этом возвращая саму копию.

```dart
final numbers = [3, 1, 2];
final sorted = numbers.toList()..sort();
print(sorted);  // [1, 2, 3]
print(numbers); // [3, 1, 2], untouched
```

`sort` также принимает **компаратор**: функцию от двух элементов, возвращающую отрицательное число, когда первый должен идти перед вторым, `0`, когда они равны, и положительное число в противном случае. `compareTo` возвращает именно это, поэтому упорядочивание по любому ключу умещается в одну строку:

```dart
final words = ['kiwi', 'fig', 'banana'];
print(words.toList()..sort((a, b) => a.length.compareTo(b.length)));
// [fig, kiwi, banana]
```

# --instructions--

Напишите функцию `byLength`, которая принимает `List<String>` `words` и возвращает **новый** список с теми же словами, упорядоченными от самого короткого к самому длинному, не изменяя `words`.

Пример:
```dart
print(byLength(['kiwi', 'fig', 'banana'])); // [fig, kiwi, banana]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`byLength(['kiwi', 'fig', 'banana'])` должна возвращать `['fig', 'kiwi', 'banana']`

```dart
  test('test1', () {
    expect(byLength(['kiwi', 'fig', 'banana']), ['fig', 'kiwi', 'banana'],
        reason: '--err-t1--');
  });
```

`byLength` должна возвращать `List<String>` и работать на пустом списке

```dart
  test('test2', () {
    expect(byLength(['bb', 'a']), isA<List<String>>(), reason: '--err-t2--');
    expect(byLength(['bb', 'a']), ['a', 'bb'], reason: '--err-t2--');
    expect(byLength(<String>[]), <String>[], reason: '--err-t2--');
  });
```

`byLength` не должна изменять полученный список

```dart
  test('test3', () {
    final original = ['kiwi', 'fig', 'banana'];
    final result = byLength(original);
    expect(original, ['kiwi', 'fig', 'banana'], reason: '--err-t3--');
    expect(result, ['fig', 'kiwi', 'banana'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<String> byLength(List<String> words) {
  return words.toList()..sort((a, b) => a.length.compareTo(b.length));
}
```
