---
language: dart
exerciseType: 1
---

# --description--

Вместе эти методы заменяют большинство циклов, написанных вручную. Конвейер обычно читается в три этапа: **выбрать** элементы с помощью `where`, **преобразовать** их с помощью `map`, затем **объединить** их с помощью `fold`:

```dart
final prices = [12, 40, 7];
final cheapTotal = prices.where((p) => p < 20).fold(0, (acc, p) => acc + p);
print(cheapTotal); // 19
```

Поскольку `fold` сам выбирает начальное значение, он может также завершить цепочку типом, не связанным с элементами, например `String`, собираемым по одному кусочку за раз:

```dart
final words = ['fig', 'kiwi'];
final firstLetters = words.fold('', (acc, w) => acc + w[0]);
print(firstLetters); // fk
```

Каждый этап остаётся коротким и говорит, что он делает, — вот настоящая причина предпочитать их циклу, который делает все три вещи сразу.

# --instructions--

Напишите функцию `initials`, которая принимает `List<String>` `names`, игнорирует пустые и возвращает `String`, составленный из первой буквы каждого оставшегося имени, в верхнем регистре и по порядку.

Пример:
```dart
print(initials(['ada', '', 'linus'])); // AL
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`initials(['ada', '', 'linus'])` должна возвращать `'AL'`

```dart
  test('test1', () {
    expect(initials(['ada', '', 'linus']), 'AL', reason: '--err-t1--');
  });
```

Буквы должны быть в верхнем регистре и по порядку, а уже написанные в верхнем регистре имена должны оставаться как есть

```dart
  test('test2', () {
    expect(initials(['grace', 'Bob', 'ada']), 'GBA', reason: '--err-t2--');
    expect(initials(['x']), 'X', reason: '--err-t2--');
  });
```

`initials` должна возвращать пустой `String`, когда нет непустого имени

```dart
  test('test3', () {
    expect(initials(<String>[]), '', reason: '--err-t3--');
    expect(initials(['', '']), '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String initials(List<String> names) {
  return names
      .where((n) => n.isNotEmpty)
      .map((n) => n[0].toUpperCase())
      .fold('', (acc, letter) => acc + letter);
}
```
