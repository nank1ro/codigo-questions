---
language: kotlin
exerciseType: 2
---

# --description--

Функция с более чем одним параметром получает по тегу `@param` на каждый из них, записанные в том же порядке, что и параметры:
```kotlin
/**
 * Returns the area of a rectangle.
 * @param width the horizontal side, in centimetres
 * @param height the vertical side, in centimetres
 * @return the product of [width] and [height]
 */
fun area(width: Int, height: Int): Int {
    return width * height
}
```
Тег — это по-прежнему всего лишь комментарий: если вы переименуете параметр и забудете про тег, ничего не сломается, но документация начнёт лгать. Обновляйте KDoc вместе с сигнатурой.

# --instructions--

Дополните документацию `price`: первый тег должен описывать параметр `quantity`, второй — параметр `unitPrice`, а третий — значение, которое возвращает функция

# --seed--

```kotlin
/**
 * Returns the total price of an order.
 * [/] quantity how many items were ordered
 * [/] unitPrice the price of a single item
 * [/] the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --answers--

- @param
- @param
- @return
- @throws
- @property

# --solutions--

```kotlin
/**
 * Returns the total price of an order.
 * @param quantity how many items were ordered
 * @param unitPrice the price of a single item
 * @return the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --output--

15
