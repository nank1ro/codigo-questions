---
language: kotlin
exerciseType: 1
---

# --description--

Функции могут возвращать значение. Тип возвращаемого значения указывается после скобок через двоеточие:
```kotlin
fun getNumber(): Int {
    return 42
}
```
Ключевое слово `return` передаёт значение обратно вызывающему коду:
```kotlin
var result = getNumber()
println(result) // prints 42
```
Тип возвращаемого значения должен соответствовать типу возвращаемого значения функции.

# --instructions--

Определите функцию с именем `getAge`, которая возвращает значение `Int` равное `25`, затем сохраните результат в переменную с именем `age`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`age` должно быть равно `25`.

```kotlin
    tryCatch(age == 25)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun getAge(): Int {
    return 25
}
val age = getAge()
```
