---
language: kotlin
exerciseType: 2
---

# --description--

Функция с одним выражением использует `=` вместо блочного тела:
```kotlin
fun triple(n: Int) = n * 3
```
Это сокращение для:
```kotlin
fun triple(n: Int): Int {
    return n * 3
}
```

# --instructions--

Дополните функцию с одним выражением, которая складывает два целых числа.

# --seed--

```kotlin
fun add(a: Int, b: Int) [/] a [/] b
fun main() {
    println(add(3, 4))
}
```

# --answers--

- =
- +
- -
- *
- return
- ->

# --solutions--

```kotlin
fun add(a: Int, b: Int) = a + b
fun main() {
    println(add(3, 4))
}
```

# --output--

7
