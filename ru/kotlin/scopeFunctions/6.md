---
language: kotlin
exerciseType: 1
---

# --description--

`apply` не ограничен объектами, которые вы только что создали: он работает с любым объектом, и поскольку он возвращает объект обратно, всё выражение можно использовать везде, где ожидается этот объект.
```kotlin
val box = Box()
box.apply { label = "tools" }        // изменяет box и возвращает его
println(listOf(Box().apply { label = "nails" }).size) // 1
```
Блок — это обычный блок кода, поэтому он может содержать столько инструкций, сколько нужно.

# --instructions--

Создайте класс `Profile` с двумя свойствами: `var name: String`, начинающимся с `""`, и `var age: Int`, начинающимся с `0`.

Затем напишите функцию `makeProfile(n: String, a: Int): Profile`, которая создаёт `Profile()` и использует `apply`, чтобы присвоить `name` значение `n`, а `age` — значение `a`, возвращая настроенный объект.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

Новый `Profile()` должен начинаться с пустого имени и возраста `0`

```kotlin
    val fresh = Profile()
    tryCatch(fresh.name == "" && fresh.age == 0)
```

`makeProfile("Ada", 36)` должен иметь `name` равным `Ada`

```kotlin
    tryCatch(makeProfile("Ada", 36).name == "Ada")
```

`makeProfile("Ada", 36)` должен иметь `age` равным `36`

```kotlin
    tryCatch(makeProfile("Ada", 36).age == 36)
```

`makeProfile("Bob", 7)` должен иметь `name` равным `Bob` и `age` равным `7`

```kotlin
    val bob = makeProfile("Bob", 7)
    tryCatch(bob.name == "Bob" && bob.age == 7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Profile {
    var name: String = ""
    var age: Int = 0
}

fun makeProfile(n: String, a: Int): Profile {
    return Profile().apply {
        name = n
        age = a
    }
}
```
