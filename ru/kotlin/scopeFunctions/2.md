---
language: kotlin
exerciseType: 1
---

# --description--

Поскольку `let` возвращает значение своего последнего выражения, это удобный способ **превратить значение во что-то другое**, не вводя промежуточную переменную:
```kotlin
val price = 12
val label = price.let { "$it EUR" }
println(label) // 12 EUR
```
Внутри блока вы можете использовать `it` столько раз, сколько нужно:
```kotlin
println("kiwi".let { "${it.uppercase()} has ${it.length} letters" })
// KIWI has 4 letters
```

# --instructions--

Напишите функцию `describe(word: String): String`, которая возвращает слово в верхнем регистре, за которым следуют пробел и его длина в круглых скобках. Используйте `let` так, чтобы `word` читался только через `it`.

Пример: `describe("kiwi")` возвращает `KIWI (4)`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`describe("kiwi")` должен вернуть `KIWI (4)`

```kotlin
    tryCatch(describe("kiwi") == "KIWI (4)")
```

`describe("a")` должен вернуть `A (1)`

```kotlin
    tryCatch(describe("a") == "A (1)")
```

`describe("")` должен вернуть ` (0)`

```kotlin
    tryCatch(describe("") == " (0)")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun describe(word: String): String {
    return word.let { "${it.uppercase()} (${it.length})" }
}
```
