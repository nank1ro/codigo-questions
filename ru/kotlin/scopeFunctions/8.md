---
language: kotlin
exerciseType: 1
---

# --description--

Когда блоку нужен объект как **аргумент** чего-то другого, `also` читается лучше, чем `apply`: `it` можно передать напрямую, тогда как `this` пришлось бы выписывать явно.
```kotlin
val names = mutableListOf<String>()
val user = "ada".also { names.add(it) }
println(user)  // ada
println(names) // [ada]
```
Значением выражения остаётся `"ada"`: `also` лишь наблюдает, как оно проходит мимо.

# --instructions--

Объявите свойство верхнего уровня `val log = mutableListOf<String>()`.

Затем напишите функцию `record(word: String): String`, которая возвращает `word` в верхнем регистре и с помощью `also` добавляет то же значение в верхнем регистре в `log` перед возвратом.

Пример: после `record("hi")` функция возвращает `HI`, а `log` содержит `HI`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`log` должен начинаться пустым

```kotlin
    tryCatch(log.isEmpty())
```

`record("hi")` должен вернуть `HI`

```kotlin
    tryCatch(record("hi") == "HI")
```

После первого вызова `log` должен содержать только `HI`

```kotlin
    tryCatch(log == listOf("HI"))
```

Второй вызов должен дополнить `log`, не теряя первого значения

```kotlin
    record("bye")
    tryCatch(log == listOf("HI", "BYE"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
val log = mutableListOf<String>()

fun record(word: String): String {
    return word.uppercase().also { log.add(it) }
}
```
