---
language: kotlin
exerciseType: 1
---

# --description--

Теперь все пять функций области видимости на столе, и каждая из них — просто точка на двух осях:
- `let` - объект — это `it`, возвращает результат блока
- `run` - объект — это `this`, возвращает результат блока
- `with` - объект — это `this` (передаётся как аргумент), возвращает результат блока
- `apply` - объект — это `this`, возвращает сам объект
- `also` - объект — это `it`, возвращает сам объект

Выбирайте нужную строку: `it` читается лучше, когда вы передаёте объект дальше чему-то другому, `this` — когда вы затрагиваете многие его члены; возвращайте результат блока, когда вам нужно новое значение, и сам объект, когда вы хотите продолжать работать с ним.

# --instructions--

Создайте класс `Point` с двумя свойствами только для чтения, `val x: Int` и `val y: Int`, оба берутся из первичного конструктора.

Затем напишите функцию `label(point: Point): String`, которая использует `with`, чтобы вернуть координаты в форме `(x, y)`.

Пример: `label(Point(2, 5))` возвращает `(2, 5)`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`label(Point(2, 5))` должен вернуть `(2, 5)`

```kotlin
    tryCatch(label(Point(2, 5)) == "(2, 5)")
```

`label(Point(0, 0))` должен вернуть `(0, 0)`

```kotlin
    tryCatch(label(Point(0, 0)) == "(0, 0)")
```

`label(Point(-3, 12))` должен вернуть `(-3, 12)`

```kotlin
    tryCatch(label(Point(-3, 12)) == "(-3, 12)")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Point(val x: Int, val y: Int)

fun label(point: Point): String {
    return with(point) {
        "($x, $y)"
    }
}
```
