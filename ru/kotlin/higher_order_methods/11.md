---
language: kotlin
exerciseType: 1
---

# --description--

`zip` соединяет элементы двух списков позиция за позицией. Без лямбды он возвращает список значений `Pair`, половины которых читаются через `.first` и `.second`; с лямбдой ей передаются два элемента каждой позиции, а результаты собираются в список:
```kotlin
val names = listOf("Ann", "Bob")
val ages = listOf(31, 25)
println(names.zip(ages))                            // [(Ann, 31), (Bob, 25)]
println(names.zip(ages) { name, age -> "$name:$age" }) // [Ann:31, Bob:25]
```
Результат по длине равен **более короткому** из двух списков: лишние элементы более длинного игнорируются.

# --instructions--

Напишите функцию `dotProduct(a: List<Int>, b: List<Int>): Int`, которая с помощью `zip` перемножает элементы `a` и `b` позиция за позицией и возвращает сумму произведений. Лишние элементы более длинного списка игнорируются.

Пример: `dotProduct(listOf(1, 2, 3), listOf(4, 5, 6))` возвращает `32`, то есть `1*4 + 2*5 + 3*6`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`dotProduct(listOf(1, 2, 3), listOf(4, 5, 6))` должна вернуть `32`

```kotlin
    tryCatch(dotProduct(listOf(1, 2, 3), listOf(4, 5, 6)) == 32)
```

`dotProduct(listOf(1, 2, 3), listOf(10, 10))` должна вернуть `30`, игнорируя лишнюю `3`

```kotlin
    tryCatch(dotProduct(listOf(1, 2, 3), listOf(10, 10)) == 30)
```

`dotProduct(listOf(2), listOf(-3, 100))` должна вернуть `-6`

```kotlin
    tryCatch(dotProduct(listOf(2), listOf(-3, 100)) == -6)
```

`dotProduct(listOf(), listOf(1, 2))` должна вернуть `0`

```kotlin
    tryCatch(dotProduct(listOf(), listOf(1, 2)) == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun dotProduct(a: List<Int>, b: List<Int>): Int {
    return a.zip(b) { x, y -> x * y }.sum()
}
```
