---
language: kotlin
exerciseType: 1
---

# --description--

`sortedBy` возвращает новый список, упорядоченный по значению, которое лямбда вычисляет для каждого элемента, от меньшего к большему. `sortedByDescending` упорядочивает от большего к меньшему. Когда сравнивать нужно сами элементы, `sorted()` и `sortedDescending()` обходятся без лямбды:
```kotlin
val words = listOf("kiwi", "fig", "banana")
println(words.sortedBy { it.length })            // [fig, kiwi, banana]
println(words.sortedByDescending { it.length })  // [banana, kiwi, fig]
println(words.sorted())                          // [banana, fig, kiwi]
```
Сортировка **устойчивая**: элементы с одинаковым ключом сохраняют исходный относительный порядок. Исходный список никогда не изменяется.

# --instructions--

Напишите функцию `byLength(words: List<String>): List<String>`, которая с помощью `sortedBy` возвращает слова из `words`, упорядоченные от самого короткого к самому длинному. Слова одинаковой длины должны сохранить исходный порядок.

Пример: `byLength(listOf("pear", "fig", "kiwi"))` возвращает `[fig, pear, kiwi]`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`byLength(listOf("pear", "fig", "kiwi"))` должна вернуть `[fig, pear, kiwi]`

```kotlin
    tryCatch(byLength(listOf("pear", "fig", "kiwi")) == listOf("fig", "pear", "kiwi"))
```

`byLength(listOf("banana", "kiwi", "a", "date"))` должна вернуть `[a, kiwi, date, banana]`

```kotlin
    tryCatch(byLength(listOf("banana", "kiwi", "a", "date")) == listOf("a", "kiwi", "date", "banana"))
```

`byLength(listOf())` должна вернуть пустой список

```kotlin
    tryCatch(byLength(listOf()) == listOf<String>())
```

`byLength` не должна изменять исходный список

```kotlin
    val original = listOf("bb", "a")
    byLength(original)
    tryCatch(original == listOf("bb", "a"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun byLength(words: List<String>): List<String> {
    return words.sortedBy { it.length }
}
```
