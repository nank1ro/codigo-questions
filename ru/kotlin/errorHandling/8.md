---
language: kotlin
exerciseType: 1
---

# --description--

Выбрасывание и перехват не бесплатны, и для распространённых преобразований Kotlin предлагает более дешёвый вариант, который просто возвращает `null` вместо выбрасывания: `toIntOrNull()`, `toDoubleOrNull()`, `toLongOrNull()`.

```kotlin
println("42".toIntOrNull())  // 42
println("abc".toIntOrNull()) // null
```
В сочетании с элвис-оператором `?:`, который подставляет замену, когда значение слева равно `null`, вся конструкция `try`/`catch` сжимается в одну строку:
```kotlin
val n = "abc".toIntOrNull() ?: 0
println(n) // 0
```
Используйте `try`/`catch`, когда сбой действительно исключителен; используйте `toIntOrNull()`, когда плохой ввод ожидаем.

# --instructions--

Напишите функцию `safeSum(a: String, b: String): Int`, которая возвращает сумму двух чисел, считая любой текст, не являющийся числом, равным `0`. Используйте `toIntOrNull()` и элвис-оператор, а не `try`/`catch`.

Пример вызова функции:
```kotlin
println(safeSum("2", "x"))
// выводит 2
```

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --seed--

```kotlin
fun safeSum(a: String, b: String): Int {
    
}
```

# --asserts--

`safeSum("2", "3")` должен вернуть `5`

```kotlin
    tryCatch(safeSum("2", "3") == 5)
```

`safeSum("2", "x")` должен вернуть `2`

```kotlin
    tryCatch(safeSum("2", "x") == 2)
```

`safeSum("a", "b")` должен вернуть `0`

```kotlin
    tryCatch(safeSum("a", "b") == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun safeSum(a: String, b: String): Int {
    return (a.toIntOrNull() ?: 0) + (b.toIntOrNull() ?: 0)
}
```
