---
language: kotlin
exerciseType: 1
---

# --description--

Писать `if (...) throw IllegalArgumentException(...)` для каждого аргумента становится шумно, поэтому Kotlin предоставляет два сокращения, которые читаются как обычные предложения:

```kotlin
require(n >= 0) { "n must not be negative" }   // выбрасывает IllegalArgumentException
check(started) { "not started" }               // выбрасывает IllegalStateException
```
Оба принимают условие и блок, создающий сообщение, и оба выбрасывают исключение, **когда условие ложно**. Единственная разница — тип исключения, и эта разница — сообщение читателю:

* `require` охраняет **аргументы**, переданные вызывающим, и завершается ошибкой с `IllegalArgumentException`.
* `check` охраняет **состояние** объекта или программы и завершается ошибкой с `IllegalStateException`.

Блок вычисляется только при неудачной проверке, поэтому построение сообщения ничего не стоит, когда всё идёт хорошо.

# --instructions--

Напишите две функции.

`square(n: Int): Int` должна использовать `require` с сообщением `n must not be negative`, чтобы отклонить отрицательный `n`, а затем вернуть `n * n`.

`finish(started: Boolean): String` должна использовать `check` с сообщением `not started`, чтобы отклонить `false`, а затем вернуть `done`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --seed--

```kotlin
fun square(n: Int): Int {
    
}

fun finish(started: Boolean): String {
    
}
```

# --asserts--

`square(4)` должен вернуть `16`

```kotlin
    tryCatch(square(4) == 16)
```

`square(-1)` должен выбросить `IllegalArgumentException` с сообщением `n must not be negative`

```kotlin
    var argMessage = ""
    try { square(-1) } catch (e: IllegalArgumentException) { argMessage = e.message ?: "" }
    tryCatch(argMessage == "n must not be negative")
```

`finish(true)` должен вернуть `done`

```kotlin
    tryCatch(finish(true) == "done")
```

`finish(false)` должен выбросить `IllegalStateException` с сообщением `not started`

```kotlin
    var stateMessage = ""
    try { finish(false) } catch (e: IllegalStateException) { stateMessage = e.message ?: "" }
    tryCatch(stateMessage == "not started")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun square(n: Int): Int {
    require(n >= 0) { "n must not be negative" }
    return n * n
}

fun finish(started: Boolean): String {
    check(started) { "not started" }
    return "done"
}
```
