---
language: kotlin
exerciseType: 2
---

# --description--

В конце можно добавить блок `finally`. Он выполняется **при любом исходе**: после успешного `try`, после восстановившегося `catch` и даже когда исключение вообще не перехвачено.

```kotlin
try {
    println("reading")
    println("abc".toInt())
} catch (e: NumberFormatException) {
    println("bad number")
} finally {
    println("closing")
}
```
```
reading
bad number
closing
```
Поэтому это место для очистки, которую нельзя пропустить, например закрытия файла. `try` требует минимум `catch` или `finally`, но может иметь и оба.

# --instructions--

Дополните код так, чтобы неудачное преобразование было перехвачено, а строка очистки выполнялась всегда.

# --seed--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } [/] (e: NumberFormatException) {
        println("bad number")
    } [/] {
        println("closing")
    }
}
```

# --answers--

- catch
- finally
- try
- throw
- else
- do

# --solutions--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } catch (e: NumberFormatException) {
        println("bad number")
    } finally {
        println("closing")
    }
}
```

# --output--

bad number
closing
