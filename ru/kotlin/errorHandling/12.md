---
language: kotlin
exerciseType: 1
---

# --description--

Когда ни один из встроенных типов не описывает ваш сбой, объявите собственный. Исключение — это обычный класс, расширяющий `Exception` и передающий свой текст родителю:

```kotlin
class InsufficientFundsException(message: String) : Exception(message)
```
Эта одна строка — полный тип исключения. Он выбрасывается и перехватывается как любой другой, а `e.message` возвращает текст, с которым он был создан:
```kotlin
try {
    throw InsufficientFundsException("balance too low")
} catch (e: InsufficientFundsException) {
    println(e.message) // balance too low
}
```
Выигрыш — в точности: вызывающий код может перехватить только `InsufficientFundsException` и позволить всем остальным сбоям продолжить путь.

# --instructions--

Класс исключения `InsufficientFundsException` уже объявлен.

Напишите функцию `withdraw(balance: Int, amount: Int): Int`, которая выбрасывает его с сообщением `balance too low`, когда `amount` больше `balance`, а иначе возвращает оставшийся баланс.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```kotlin
class InsufficientFundsException(message: String) : Exception(message)

fun withdraw(balance: Int, amount: Int): Int {
    
}
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`withdraw(100, 30)` должен вернуть `70`

```kotlin
    tryCatch(withdraw(100, 30) == 70)
```

`withdraw(10, 50)` должен выбросить `InsufficientFundsException` с сообщением `balance too low`

```kotlin
    var message = ""
    try { withdraw(10, 50) } catch (e: InsufficientFundsException) { message = e.message ?: "" }
    tryCatch(message == "balance too low")
```

Снятие всего баланса должно быть разрешено

```kotlin
    tryCatch(withdraw(40, 40) == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class InsufficientFundsException(message: String) : Exception(message)

fun withdraw(balance: Int, amount: Int): Int {
    if (amount > balance) throw InsufficientFundsException("balance too low")
    return balance - amount
}
```
