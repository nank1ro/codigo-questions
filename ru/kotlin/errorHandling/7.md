---
language: kotlin
exerciseType: 2
---

# --description--

Поскольку `try` — это выражение, его можно использовать везде, где ожидается значение, — в том числе как всё тело функции, записанной через `=`:
```kotlin
fun length(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    -1
}
```
Оба блока должны производить значение одного и того же типа, здесь `Int`. Запишите запасное значение последним выражением блока `catch`; внутри ни одного из блоков нет `return`.

# --instructions--

Дополните функцию так, чтобы она возвращала число из `text`, а `0` — когда текст не является числом.

# --seed--

```kotlin
fun size(text: String): Int = [/] {
    text.toInt()
} [/] (e: NumberFormatException) {
    [/]
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --answers--

- try
- catch
- 0
- finally
- throw
- 1

# --solutions--

```kotlin
fun size(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    0
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --output--

15
0
