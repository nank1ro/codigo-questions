---
language: kotlin
exerciseType: 2
---

# --description--

Когда ключ может отсутствовать, `getOrDefault` позволяет выбрать значение, используемое вместо `null`:
```kotlin
val ages = mapOf("Alice" to 30)
println(ages.getOrDefault("Alice", 0)) // 30
println(ages.getOrDefault("Zoe", 0))   // 0
```
Первый аргумент — это ключ, второй — значение по умолчанию, возвращаемое, если ключ не найден.

# --instructions--

Дополните код, чтобы вывести запас `"kiwi"`, используя `0`, если фрукта нет в map

# --seed--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.[/]("kiwi", [/]))
```

# --answers--

- getOrDefault
- get
- getOrNull
- 0
- null
- "kiwi"

# --solutions--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.getOrDefault("kiwi", 0))
```

# --output--

0
