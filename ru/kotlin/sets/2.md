---
language: kotlin
exerciseType: 2
---

# --description--

`Set` — это коллекция __уникальных__ элементов без определённого порядка.

```kotlin
val numbers = setOf(1, 1, 2) // [1]
println(numbers)
// выводит [1, 2]
```

В __[1]__ мы пытаемся создать множество с числом __1__, присутствующим дважды, но, как видите, каждый элемент должен быть уникальным, и второй __1__ автоматически отбрасывается.

# --instructions--

Дополните код, чтобы вывести все уникальные элементы `fruits`

# --seed--

```kotlin
val fruits = listOf("apple", "mango", "apple", "mango")
val [/] = [/][/][/]
println(uniqueFruits)
```

# --answers--

- uniqueFruits
- fruits
- .
- toSet()
- asSet()
- setOf()
- toSet
- setOf

# --solutions--

```kotlin
val fruits = listOf("apple", "mango", "apple", "mango")
val uniqueFruits = fruits.toSet()
println(uniqueFruits)
```

# --output--

[apple, mango]
