---
language: kotlin
exerciseType: 2
---

# --description--

Наиболее распространённая операция с `Set` — это проверка принадлежности с помощью `in` или `contains()`

```kotlin
val numbers = setOf(1, 2, 3)
println(2 in numbers) // выводит true
println(numbers.contains(5)) // выводит false
```

Как видно выше, `in` и `contains` возвращают `Bool`, указывающий, присутствует ли переданный элемент в множестве

# --instructions--

Дополните код, чтобы проверить, содержит ли меню `pizza`, в этом случае съесть пиццу, иначе съесть первое блюдо из меню.

# --seed--

```kotlin
val dishes = setOf("rice", "noodles", "pizza")
var selectedDish =  dishes.first();
if ([/][/][/]) {
    selectedDish = "[/]"
}
println("I eat [/]")
```

# --answers--

- "pizza"
- pizza
- contains
-  in 
- dishes
- selectedDish
- pizza
- $selectedDish

# --solutions--

```kotlin
val dishes = setOf("rice", "noodles", "pizza")
var selectedDish =  dishes.first();
if ("pizza" in dishes) {
    selectedDish = "pizza"
}
println("I eat $selectedDish")
```

# --output--

I eat pizza
