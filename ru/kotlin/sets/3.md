---
language: kotlin
exerciseType: 2
---

# --description--

В Kotlin существует два типа `Set`:

- `Set` нельзя изменить после создания.
- `MutableSet` можно изменять после создания, то есть можно добавлять, удалять или обновлять его элементы.

```kotlin
val numbers = setOf(1, 2, 3)
numbers.add(4) // [1]
```
__[1]__ вызывает ошибку, потому что `Set` является _только для чтения_.

Чтобы создать изменяемое множество, используйте ключевое слово `mutableSetOf`
```kotlin
val numbers = mutableSetOf(1, 2, 3)
numbers.add(4)
println(numbers)
// выводит [1, 2, 3, 4]
```

# --instructions--

Дополните код, чтобы создать корректное изменяемое множество

# --seed--

```kotlin
val [/] = [/]<String>[/]"apple", "banana"[/]
fruits.add("mango")
println(fruits)
```

# --answers--

- fruits
- "fruits"
- [
- ]
- (
- )
- listOf
- setOf
- mutableListOf
- mutableSetOf

# --solutions--

```kotlin
val fruits = mutableSetOf<String>("apple", "banana")
fruits.add("mango")
println(fruits)
```

# --output--

[apple, banana, mango]
