---
language: kotlin
exerciseType: 2
---

# --description--

Изменяемый список не обязан иметь фиксированную длину.
Вы можете добавлять элементы в конец списка в любое время!
Чтобы добавить элемент в изменяемый список, используйте функцию `add` или сокращение `+=`:
```kotlin
val letters = mutableListOf("a", "b")
letters.add("c")
println(letters)
// выводит [a, b, c]
```

# --instructions--

Дополните код, чтобы добавить ещё три элемента в список `numbers` в следующем порядке: `6`, затем `8` и наконец `9`

# --seed--

```kotlin
val numbers = mutableListOf(1, 3, 9)
numbers.[/]([/])
numbers.add([/])
[/].add(9)
println(numbers)
```

# --answers--

- append
- add
- 6
- 8
- numbers

# --solutions--

```kotlin
val numbers = mutableListOf(1, 3, 9)
numbers.add(6)
numbers.add(8)
numbers.add(9)
println(numbers)
```

# --output--

[1, 3, 9, 6, 8, 9]
