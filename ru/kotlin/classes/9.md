---
language: kotlin
exerciseType: 2
---

# --instructions--

Дополните код, чтобы объявить `data class` с именем `Color` со свойствами `val` с именами `red`, `green` и `blue` типа `Int`.

# --seed--

```kotlin
[/] [/] Color(val red: Int, val green: Int, val blue: Int)
```

# --answers--

- data
- class
- object
- open
- abstract
- sealed

# --solutions--

```kotlin
data class Color(val red: Int, val green: Int, val blue: Int)
```
