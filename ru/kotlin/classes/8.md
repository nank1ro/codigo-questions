---
language: kotlin
exerciseType: 2
---

# --instructions--

Дополните код, чтобы определить `class` с именем `Book` с основным конструктором, содержащим свойство `val` с именем `title` типа `String`.

# --seed--

```kotlin
[/] Book([/] title: String)
```

# --answers--

- class
- val
- var
- fun
- object
- data

# --solutions--

```kotlin
class Book(val title: String)
```
