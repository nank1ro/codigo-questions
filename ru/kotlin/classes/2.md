---
language: kotlin
exerciseType: 1
---

# --description--

Классы могут иметь несколько свойств в своём **основном конструкторе**. Каждое свойство объявляется с помощью `val` (только для чтения) или `var` (изменяемое) непосредственно в списке параметров конструктора:

```kotlin
class Person(val name: String, var age: Int)
```

Ключевое слово `val` делает `name` доступным только для чтения после создания, тогда как `var` делает `age` изменяемым — его можно изменить позже:

```kotlin
val p = Person("Alice", 30)
p.age = 31  // разрешено, так как age является var
```

# --instructions--

Создайте класс `Person` со свойством `val` с именем `name` типа `String` и свойством `var` с именем `age` типа `Int`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`Person("Alice", 30)` должен иметь `name` равным `"Alice"`

```kotlin
    val p = Person("Alice", 30)
    tryCatch(p.name == "Alice")
```

`Person("Alice", 30)` должен иметь `age` равным `30`

```kotlin
    tryCatch(p.age == 30)
```

Изменение `age` на `31` должно обновить свойство

```kotlin
    p.age = 31
    tryCatch(p.age == 31)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Person(val name: String, var age: Int)
```
