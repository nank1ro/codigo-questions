---
language: kotlin
exerciseType: 2
---

# --instructions--

Дополните код, чтобы добавить `companion object` внутри `AppConfig`, содержащий свойство `val` с именем `version`, равное `"1.0"`.

# --seed--

```kotlin
class AppConfig {
    [/] [/] {
        val version = "1.0"
    }
}
```

# --answers--

- companion
- object
- class
- static
- init
- val

# --solutions--

```kotlin
class AppConfig {
    companion object {
        val version = "1.0"
    }
}
```
