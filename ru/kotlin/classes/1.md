---
language: kotlin
exerciseType: 1
---

# --description--

**Класс** — это шаблон для создания объектов. В Kotlin класс объявляется с помощью ключевого слова `class`, за которым следует имя класса.

Класс может содержать **свойства** (данные) и **методы** (поведение). Чтобы объявить свойство непосредственно в конструкторе, добавьте перед ним `val` или `var`:

```kotlin
class Dog(val name: String)
```

Это создаёт класс `Dog` со свойством `name`. Экземпляр создаётся вызовом класса как функции:

```kotlin
val dog = Dog("Rex")
println(dog.name) // Rex
```

# --instructions--

Создайте класс `Car` со свойством `val` с именем `brand` типа `String`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

Создание `Car` с маркой `"Toyota"` должно иметь `brand` равным `"Toyota"`

```kotlin
    val car = Car("Toyota")
    tryCatch(car.brand == "Toyota")
```

Создание `Car` с маркой `"Honda"` должно иметь `brand` равным `"Honda"`

```kotlin
    val car2 = Car("Honda")
    tryCatch(car2.brand == "Honda")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Car(val brand: String)
```
