---
language: kotlin
exerciseType: 1
---

# --description--

Правая часть `?:` может быть любым выражением, включая `return`. Это даёт компактный способ выйти из функции, как только значение отсутствует:
```kotlin
fun firstUpper(text: String?): Char? {
    val first = text?.firstOrNull() ?: return null
    return first.uppercaseChar() // first is a Char here
}
```
Все инструменты, которые вы видели, хорошо сочетаются: параметры и возвращаемые типы, допускающие null, описывают *где* значение может отсутствовать, а `?.`, `?:`, `let`, умные приведения и `toIntOrNull` обрабатывают это, не допуская падения.

# --instructions--

Напишите функцию `divide(a: String?, b: String?): Int?`, которая преобразует обе строки в целые числа и возвращает `a / b`. Она должна вернуть `null`, когда любая строка равна `null`, когда любая из них не является целым числом или когда `b` равен `0`.

Пример: `divide("10", "2")` возвращает `5`, `divide("10", "x")` возвращает `null`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`divide("10", "2")` должен вернуть `5`

```kotlin
    tryCatch(divide("10", "2") == 5)
```

`divide("10", "x")` должен вернуть `null`

```kotlin
    tryCatch(divide("10", "x") == null)
```

`divide(null, "2")` должен вернуть `null`

```kotlin
    tryCatch(divide(null, "2") == null)
```

`divide("9", null)` должен вернуть `null`

```kotlin
    tryCatch(divide("9", null) == null)
```

`divide("7", "0")` должен вернуть `null`

```kotlin
    tryCatch(divide("7", "0") == null)
```

`divide("-9", "3")` должен вернуть `-3`

```kotlin
    tryCatch(divide("-9", "3") == -3)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun divide(a: String?, b: String?): Int? {
    val x = a?.toIntOrNull() ?: return null
    val y = b?.toIntOrNull() ?: return null
    if (y == 0) return null
    return x / y
}
```
