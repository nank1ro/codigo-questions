---
language: kotlin
exerciseType: 4
---

# --description--

Функцию-расширение можно объявить для **получателя, допускающего null**, поэтому её можно вызывать даже на значении `null`. Внутри `this` допускает null и должен быть проверен:
```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val none: String? = null
println(none.orDash()) // -
```
Обратите внимание, что `?.` в месте вызова не нужен: сама функция обрабатывает случай `null`.
Стандартная библиотека использует этот приём в `isNullOrEmpty()` и `orEmpty()`, которые безопасно вызывать на любом `String?`.

# --instructions--

Сможете расположить эти строки так, чтобы определить функцию-расширение `orDash` и вывести `-` для отсутствующего имени, а затем `kotlin` для присутствующего? Определите функцию первой

# --answers--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
```

```kotlin
val a: String? = null
val b: String? = "kotlin"
```

```kotlin
println(a.orDash())
```

```kotlin
println(b.orDash())
```

# --solutions--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val a: String? = null
val b: String? = "kotlin"
println(a.orDash())
println(b.orDash())
```

# --output--

-
kotlin
