---
language: kotlin
exerciseType: 2
---

# --description--

Коллекции тоже могут хранить элементы, допускающие null: `List<Int?>` может содержать элементы `null`, а `List<Int>` — никогда.
`filterNotNull()` возвращает новый список без элементов `null`, и тип его элементов становится не допускающим null, поэтому элементами можно пользоваться свободно:
```kotlin
val scores: List<Int?> = listOf(10, null, 20)
val valid = scores.filterNotNull() // List<Int> [10, 20]
println(valid.sum())               // 30
```

# --instructions--

Дополните код, чтобы объявить список, который может содержать числа `null`, затем удалите элементы `null` и выведите оставшиеся значения и их сумму

# --seed--

```kotlin
val values: List<[/]> = listOf(3, null, 5, null)
val present = values.[/]()
println(present)
println(present.sum())
```

# --answers--

- Int?
- Int
- filterNotNull
- filter
- mapNotNull
- requireNotNull

# --solutions--

```kotlin
val values: List<Int?> = listOf(3, null, 5, null)
val present = values.filterNotNull()
println(present)
println(present.sum())
```

# --output--

[3, 5]
8
