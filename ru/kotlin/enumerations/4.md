---
language: kotlin
exerciseType: 2
---

# --description--

Класс enum может иметь **конструктор**, как и обычный класс. Каждая запись при этом передаёт свои собственные аргументы, а значения сохраняются в свойствах:
```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
println(Planet.MARS.moons) // 2
```
Свойства конструктора обычно объявляют с помощью `val`, так как данные записи не должны изменяться.

# --instructions--

Дополните код так, чтобы каждая `Coin` хранила своё значение в центах, а затем выведите значение `PENNY` (которая стоит `1` цент)

# --seed--

```kotlin
enum class Coin([/] cents: Int) {
    PENNY([/]),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.[/])
}
```

# --answers--

- val
- fun
- const
- 1
- "1"
- cents
- name
- ordinal

# --solutions--

```kotlin
enum class Coin(val cents: Int) {
    PENNY(1),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.cents)
}
```

# --output--

1
