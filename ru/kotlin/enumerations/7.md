---
language: kotlin
exerciseType: 1
---

# --description--

Чтобы получить запись обратно из `String`, используйте функцию `valueOf`. Она ищет запись, чьё `name` совпадает точно:
```kotlin
val direction = Direction.valueOf("EAST")
println(direction == Direction.EAST) // true
```
Сопоставление чувствительно к регистру: `Direction.valueOf("east")` выбрасывает `IllegalArgumentException`, потому что ни одна запись не имеет такого имени.

# --instructions--

Объявите класс enum `Status` с записями `ACTIVE` и `INACTIVE`. Затем напишите функцию `statusOf(text: String)`, которая возвращает `Status`, соответствующий `text`, независимо от регистра (так что `"active"`, `"Active"` и `"ACTIVE"` дают `Status.ACTIVE`).

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`statusOf("ACTIVE")` должна возвращать `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("ACTIVE") == Status.ACTIVE)
```

`statusOf("inactive")` должна возвращать `Status.INACTIVE`

```kotlin
    tryCatch(statusOf("inactive") == Status.INACTIVE)
```

`statusOf("Active")` должна возвращать `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("Active") == Status.ACTIVE)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
enum class Status {
    ACTIVE, INACTIVE
}

fun statusOf(text: String): Status {
    return Status.valueOf(text.uppercase())
}
```
