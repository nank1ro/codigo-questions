---
language: kotlin
exerciseType: 1
---

# --description--

Функция-расширение может принимать параметры, как и любая другая функция. Получатель остаётся слева от точки, а параметры идут в скобках:
```kotlin
fun Int.isDivisibleBy(other: Int): Boolean {
    return this % other == 0
}
println(12.isDivisibleBy(4)) // true
println(12.isDivisibleBy(5)) // false
```
Тип перед точкой — обычный тип, поэтому точно так же можно расширить `List<Int>`, `Double` или написанный вами класс.

# --instructions--

Напишите функцию-расширение `isBetween(low: Int, high: Int)` для `Int`, которая возвращает `true`, когда получатель больше или равен `low` и меньше или равен `high`.

Пример: `5.isBetween(1, 10)` возвращает `true`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`5.isBetween(1, 10)` должна вернуть `true`

```kotlin
    tryCatch(5.isBetween(1, 10) == true)
```

`15.isBetween(1, 10)` должна вернуть `false`

```kotlin
    tryCatch(15.isBetween(1, 10) == false)
```

`10.isBetween(1, 10)` должна вернуть `true`

```kotlin
    tryCatch(10.isBetween(1, 10) == true)
```

`0.isBetween(1, 10)` должна вернуть `false`

```kotlin
    tryCatch(0.isBetween(1, 10) == false)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun Int.isBetween(low: Int, high: Int): Boolean {
    return this >= low && this <= high
}
```
