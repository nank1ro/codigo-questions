---
language: kotlin
exerciseType: 1
---

# --description--

Расширение может работать сразу с целым семейством типов благодаря **параметру типа**: заполнителю для типа, объявленному в угловых скобках сразу после `fun`, который Kotlin подставляет при каждом вызове. Это делает расширение **обобщённым**:
```kotlin
fun <T> List<T>.second(): T {
    return this[1]
}
println(listOf(1, 2, 3).second())      // 2
println(listOf("a", "b").second())     // b
```
Для `listOf(1, 2, 3)` заполнитель `T` — это `Int`, для `listOf("a", "b")` — `String`, поэтому одна и та же функция каждый раз возвращает нужный тип.

# --instructions--

Напишите обобщённую функцию-расширение `penultimate()` для `List<T>`, которая возвращает предпоследний элемент списка. В списке всегда есть хотя бы два элемента.

Пример: `listOf(1, 2, 3).penultimate()` возвращает `2`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`listOf(1, 2, 3).penultimate()` должна вернуть `2`

```kotlin
    tryCatch(listOf(1, 2, 3).penultimate() == 2)
```

`listOf("a", "b").penultimate()` должна вернуть `"a"`

```kotlin
    tryCatch(listOf("a", "b").penultimate() == "a")
```

`listOf(true, false, true, false).penultimate()` должна вернуть `true`

```kotlin
    tryCatch(listOf(true, false, true, false).penultimate() == true)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun <T> List<T>.penultimate(): T {
    return this[size - 2]
}
```
