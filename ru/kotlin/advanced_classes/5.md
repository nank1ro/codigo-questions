---
language: kotlin
exerciseType: 1
---

# --description--

Класс `sealed` описывает **закрытый** набор вариантов: разрешены только подклассы, написанные в том же пакете и модуле, поэтому компилятор знает их все.

```kotlin
sealed class Shape
data class Circle(val radius: Int) : Shape()
data class Square(val side: Int) : Shape()
```

Выигрыш — **исчерпывающий `when`**: когда вы ветвитесь по sealed-типу и покрываете каждый подкласс, ветку `else` можно не писать. Добавьте позже новый подкласс, и компилятор укажет на каждый `when`, который вы забыли обновить, вместо того чтобы молча уйти в `else`.

```kotlin
fun name(shape: Shape): String = when (shape) {
    is Circle -> "circle"
    is Square -> "square"
}
```

После `is Circle` значение подвергается умному приведению, поэтому `shape.radius` доступно внутри этой ветки без ручного приведения типа.

# --instructions--

Объявите sealed-класс `Shape` с двумя подклассами-data class: `Square` со свойством `val` `side` типа `Int` и `Rectangle` со свойствами `val` `width` и `height` типа `Int`.

Затем напишите функцию `area(shape: Shape): Int`, которая возвращает площадь фигуры, используя исчерпывающий `when` без ветки `else`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`area(Square(3))` must return `9`

```kotlin
    tryCatch(area(Square(3)) == 9)
```

`area(Rectangle(2, 5))` must return `10`

```kotlin
    tryCatch(area(Rectangle(2, 5)) == 10)
```

`area(Square(0))` must return `0`

```kotlin
    tryCatch(area(Square(0)) == 0)
```

`Square` and `Rectangle` must be subclasses of `Shape`

```kotlin
    val shapes: List<Shape> = listOf(Square(4), Rectangle(3, 3))
    tryCatch(shapes.map { area(it) } == listOf(16, 9))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
sealed class Shape
data class Square(val side: Int) : Shape()
data class Rectangle(val width: Int, val height: Int) : Shape()

fun area(shape: Shape): Int = when (shape) {
    is Square -> shape.side * shape.side
    is Rectangle -> shape.width * shape.height
}
```
