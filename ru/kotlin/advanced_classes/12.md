---
language: kotlin
exerciseType: 3
---

# --instructions--

Что происходит с этой программой?

```kotlin
interface Movable {
    fun move(): String = "moving"
}

class Robot : Movable

fun main() {
    println(Robot().move())
}
```

# --answers--

- Выводится `moving`, потому что `Robot` наследует реализацию по умолчанию
- Код не компилируется: `Robot` должен переопределить `move()`
- Код не компилируется: интерфейс не может содержать тело функции
- Выводится `null`

# --solutions--

- Выводится `moving`, потому что `Robot` наследует реализацию по умолчанию

