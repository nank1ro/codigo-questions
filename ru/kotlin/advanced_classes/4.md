---
language: kotlin
exerciseType: 3
---

# --description--

Сгенерированные члены учитывают только свойства, объявленные в **первичном конструкторе**. Свойство, объявленное в **теле** класса, является обычным свойством: оно не входит в `toString()`, `equals()`, `hashCode()` или `copy()`.

```kotlin
data class Item(val name: String) {
    var quantity: Int = 0
}

val a = Item("nail")
a.quantity = 5
println(a) // Item(name=nail)
```

Об этом легко забыть, поэтому помещайте в первичный конструктор всё, что идентифицирует объект, а производное или временное состояние оставляйте в теле.

# --instructions--

Что выводит эта программа?

```kotlin
data class Item(val name: String) {
    var quantity: Int = 0
}

fun main() {
    val a = Item("nail")
    a.quantity = 5
    val b = Item("nail")
    b.quantity = 9
    println(a == b)
}
```

# --answers--

- `true`, потому что сгенерированный `equals()` сравнивает только `name`
- `false`, потому что `quantity` отличается
- `Item(name=nail, quantity=5)`
- Код не компилируется: data class не может объявлять свойства в своём теле

# --solutions--

- `true`, потому что сгенерированный `equals()` сравнивает только `name`
