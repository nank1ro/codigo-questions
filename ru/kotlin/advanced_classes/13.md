---
language: kotlin
exerciseType: 1
---

# --description--

Класс `abstract` находится между интерфейсом и обычным классом: его нельзя инстанцировать, и он сочетает **абстрактные** члены, у которых нет тела и которые обязательно переопределяются, с конкретными, которые подклассы наследуют как есть.

```kotlin
abstract class Vehicle(val name: String) {
    abstract fun wheels(): Int
    fun describe(): String = "$name has ${wheels()} wheels"
}

class Bike(name: String) : Vehicle(name) {
    override fun wheels(): Int = 2
}

println(Bike("BMX").describe()) // BMX has 2 wheels
```

В отличие от интерфейса, у абстрактного класса есть конструктор и он может хранить состояние в свойствах, поэтому подкласс передаёт `name` наверх через `: Vehicle(name)`. Класс может наследовать только один класс, поэтому берите абстрактный класс, когда подклассы разделяют данные, и интерфейс, когда они разделяют только поведение. Абстрактные члены переопределяются без добавления `open`.

# --instructions--

Объявите абстрактный класс `Employee` со свойством `val` `name` типа `String` в конструкторе, абстрактным методом `monthlyPay(): Int` и конкретным методом `yearlyPay(): Int`, который возвращает месячную оплату, умноженную на `12`.

Затем объявите класс `Manager`, который наследует `Employee`, принимает `name` и свойство `val` `bonus` типа `Int`, и чей `monthlyPay()` возвращает `5000` плюс бонус.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`Manager("Ann", 500).monthlyPay()` must return `5500`

```kotlin
    tryCatch(Manager("Ann", 500).monthlyPay() == 5500)
```

`Manager("Ann", 500).yearlyPay()` must return `66000`

```kotlin
    tryCatch(Manager("Ann", 500).yearlyPay() == 66000)
```

`Manager("Bo", 0).name` must return `"Bo"`

```kotlin
    tryCatch(Manager("Bo", 0).name == "Bo")
```

A `Manager` must be usable as an `Employee`

```kotlin
    val employee: Employee = Manager("Cy", 1000)
    tryCatch(employee.yearlyPay() == 72000)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
abstract class Employee(val name: String) {
    abstract fun monthlyPay(): Int

    fun yearlyPay(): Int = monthlyPay() * 12
}

class Manager(name: String, val bonus: Int) : Employee(name) {
    override fun monthlyPay(): Int = 5000 + bonus
}
```
