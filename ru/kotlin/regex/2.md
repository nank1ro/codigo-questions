---
language: kotlin
exerciseType: 1
---

# --description--

`matches` часто слишком строг: обычно нужно лишь узнать, встречается ли шаблон **где-либо** в тексте. Для этого есть `containsMatchIn`:
```kotlin
val digits = Regex("\\d+")
println(digits.matches("order 42"))          // false, the whole string is not digits
println(digits.containsMatchIn("order 42"))  // true, "42" is in there
```
Помимо `\d`, есть ещё два сокращения, которые вы будете использовать постоянно: `\w` — это символ слова (буква, цифра или `_`), а `\s` — пробельный символ. Каждый из них можно повторить с помощью **квантификатора**:
- `+` — один или несколько
- `*` — ноль или несколько
- `?` — ноль или один
- `{3}` — ровно три, `{2,4}` — от двух до четырёх

Удваивать каждый обратный слеш утомительно, поэтому шаблоны обычно записывают как **сырые строки** в тройных кавычках, где `\` — просто символ:
```kotlin
val digits = Regex("""\d+""") // same as Regex("\\d+")
```

# --instructions--

Напишите функцию `hasNumber(text: String): Boolean`, которая возвращает `true`, когда `text` содержит хотя бы одну цифру, используя `containsMatchIn`.

Пример: `hasNumber("room 12")` возвращает `true`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`hasNumber("room 12")` должна вернуть `true`

```kotlin
    tryCatch(hasNumber("room 12") == true)
```

`hasNumber("9lives")` должна вернуть `true`

```kotlin
    tryCatch(hasNumber("9lives") == true)
```

`hasNumber("no digits here")` должна вернуть `false`

```kotlin
    tryCatch(hasNumber("no digits here") == false)
```

`hasNumber("")` должна вернуть `false`

```kotlin
    tryCatch(hasNumber("") == false)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun hasNumber(text: String): Boolean {
    return Regex("""\d""").containsMatchIn(text)
}
```
