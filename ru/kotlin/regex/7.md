---
language: kotlin
exerciseType: 1
---

# --description--

`replace` переписывает текст: он возвращает **новую** строку, где каждое совпадение заменено подстановкой, а исходная строка остаётся нетронутой.
```kotlin
println(Regex("""\d+""").replace("a1 b22", "#")) // a# b#
```
В строке замены `$1`, `$2`, ... обозначают захваченные группы этого совпадения, поэтому совпавшие куски можно переставлять или использовать повторно:
```kotlin
val name = Regex("""(\w+) (\w+)""")
println(name.replace("Ann Lee", "$2 $1")) // Lee Ann
```
`$0` — это всё совпадение целиком. Если в замене нужен литеральный `$`, экранируйте его как `\$`.
`replace` переписывает **каждое** совпадение, поэтому если переписать нужно только полную строку, закрепите шаблон **якорями** `^` (начало текста) и `$` (конец текста):
```kotlin
println(Regex("""^\w+$""").replace("one two", "x")) // one two, nothing is replaced
```

# --instructions--

Напишите функцию `lastNameFirst(name: String): String`, которая превращает `"Ann Lee"` в `"Lee, Ann"`, используя `replace` со ссылками на группы. Если `name` — не два слова, разделённые пробелом, верните его без изменений.

Пример: `lastNameFirst("Ann Lee")` возвращает `Lee, Ann`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`lastNameFirst("Ann Lee")` должна вернуть `Lee, Ann`

```kotlin
    tryCatch(lastNameFirst("Ann Lee") == "Lee, Ann")
```

`lastNameFirst("Marco Rossi")` должна вернуть `Rossi, Marco`

```kotlin
    tryCatch(lastNameFirst("Marco Rossi") == "Rossi, Marco")
```

`lastNameFirst("Cher")` должна вернуть `Cher`

```kotlin
    tryCatch(lastNameFirst("Cher") == "Cher")
```

`lastNameFirst("")` должна вернуть пустую строку

```kotlin
    tryCatch(lastNameFirst("") == "")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun lastNameFirst(name: String): String {
    return Regex("""^(\w+) (\w+)$""").replace(name, "$2, $1")
}
```
