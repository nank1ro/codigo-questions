---
language: kotlin
exerciseType: 1
---

# --description--

Реалистичный шаблон обычно смешивает всё сразу: группы, чтобы сохранить нужные части, экранированный `\.` для буквальных точек и замену лямбдой, чтобы пересобрать текст вокруг них.
```kotlin
val email = Regex("""(\w+)@(\w+\.\w+)""")
println(email.replace("ping ann@mail.com now") { m -> "<" + m.groupValues[2] + ">" })
// ping <mail.com> now
```
Держите шаблоны настолько простыми, насколько позволяет задача: шаблон, который пытается описать каждый допустимый адрес электронной почты, нечитаем, а `\w+@\w+\.\w+` достаточно, чтобы найти адреса в предложении.

# --instructions--

Напишите функцию `redactEmails(text: String): String`, которая заменяет имя перед `@` каждого адреса на `***`, сохраняя домен. Адрес — это последовательность символов слова, затем `@`, последовательность символов слова, точка и последовательность символов слова.

Пример: `redactEmails("write to ann@mail.com")` возвращает `write to ***@mail.com`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`redactEmails("write to ann@mail.com")` должна вернуть `write to ***@mail.com`

```kotlin
    tryCatch(redactEmails("write to ann@mail.com") == "write to ***@mail.com")
```

`redactEmails("bob@site.org and cy@site.org")` должна скрыть оба адреса

```kotlin
    tryCatch(redactEmails("bob@site.org and cy@site.org") == "***@site.org and ***@site.org")
```

`redactEmails("no address here")` должна вернуть текст без изменений

```kotlin
    tryCatch(redactEmails("no address here") == "no address here")
```

`redactEmails("")` должна вернуть пустую строку

```kotlin
    tryCatch(redactEmails("") == "")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun redactEmails(text: String): String {
    return Regex("""(\w+)@(\w+\.\w+)""").replace(text) { m -> "***@" + m.groupValues[2] }
}
```
