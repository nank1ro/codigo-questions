---
language: kotlin
exerciseType: 1
---

# --description--

Круглые скобки в шаблоне создают **группу захвата**: часть совпадения, которую вы хотите прочитать отдельно. `MatchResult.groupValues` хранит их: индекс `0` — всё совпадение целиком, а `1`, `2`, ... — группы слева направо:
```kotlin
val date = Regex("""(\d{4})-(\d{2})-(\d{2})""")
val match = date.find("released 2026-09-12 in Rome")
println(match?.groupValues?.get(0)) // 2026-09-12
println(match?.groupValues?.get(1)) // 2026
println(match?.groupValues?.get(2)) // 09
```
Когда совпадения нет вовсе, `find` возвращает `null` и читать нечего, поэтому функция, извлекающая группу, обычно сама решает, что вернуть в таком случае:
```kotlin
val match = date.find("no date here")
println(match?.groupValues?.get(1) ?: "unknown") // unknown
```

# --instructions--

Напишите функцию `monthOf(date: String): String`, которая возвращает месяц даты, записанной в формате `YYYY-MM-DD`, или пустую строку, если `date` не содержит такой даты.

Пример: `monthOf("2026-09-12")` возвращает `09`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`monthOf("2026-09-12")` должна вернуть `09`

```kotlin
    tryCatch(monthOf("2026-09-12") == "09")
```

`monthOf("1999-12-31")` должна вернуть `12`

```kotlin
    tryCatch(monthOf("1999-12-31") == "12")
```

`monthOf("due 2024-01-05 sharp")` должна вернуть `01`

```kotlin
    tryCatch(monthOf("due 2024-01-05 sharp") == "01")
```

`monthOf("no date")` должна вернуть пустую строку

```kotlin
    tryCatch(monthOf("no date") == "")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun monthOf(date: String): String {
    val match = Regex("""(\d{4})-(\d{2})-(\d{2})""").find(date)
    return match?.groupValues?.get(2) ?: ""
}
```
