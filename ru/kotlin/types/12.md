---
language: kotlin
exerciseType: 1
---

# --description--

`Double` хранит дробные числа в двоичном виде, поэтому некоторые значения нельзя представить точно, и в последних цифрах появляются небольшие погрешности:
```kotlin
println(0.1 + 0.2) // 0.30000000000000004
```
Чтобы показать фиксированное количество знаков после запятой, используйте `String.format` со строкой формата: `"%.2f"` означает «дробное число с 2 цифрами после точки». Результат — `String`, округлённый до этого количества цифр:
```kotlin
println(String.format("%.2f", 0.1 + 0.2)) // 0.30
println(String.format("%.1f", 3.14159))   // 3.1
println(String.format("%.2f", 2.0))       // 2.00
```

# --instructions--

Напишите функцию `formatPrice(price: Double): String`, которая возвращает цену ровно с двумя знаками после запятой.

Пример: `formatPrice(3.14159)` возвращает `"3.14"`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`formatPrice(3.14159)` должна вернуть `"3.14"`

```kotlin
    tryCatch(formatPrice(3.14159) == "3.14")
```

`formatPrice(2.0)` должна вернуть `"2.00"`

```kotlin
    tryCatch(formatPrice(2.0) == "2.00")
```

`formatPrice(0.1 + 0.2)` должна вернуть `"0.30"`

```kotlin
    tryCatch(formatPrice(0.1 + 0.2) == "0.30")
```

`formatPrice(9.999)` должна вернуть `"10.00"`

```kotlin
    tryCatch(formatPrice(9.999) == "10.00")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun formatPrice(price: Double): String {
    return String.format("%.2f", price)
}
```
