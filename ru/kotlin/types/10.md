---
language: kotlin
exerciseType: 1
---

# --description--

`toIntOrNull()` выполняется успешно, только когда весь текст является корректным целым числом, возможно со знаком:
```kotlin
println("42".toIntOrNull())   // 42
println("-7".toIntOrNull())   // -7
println("3.5".toIntOrNull())  // null, not a whole number
println(" 42".toIntOrNull())  // null, spaces are not allowed
println("abc".toIntOrNull())  // null
```
Для десятичного текста используйте `toDoubleOrNull()`, которая принимает `"3.5"` и аналогично возвращает `Double?`.

# --instructions--

Напишите функцию `parseAge(text: String): Int`, которая возвращает целое число, записанное в `text`, или `-1`, если текст не является корректным целым числом.

Пример: `parseAge("42")` возвращает `42`, а `parseAge("abc")` возвращает `-1`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`parseAge("42")` должна вернуть `42`

```kotlin
    tryCatch(parseAge("42") == 42)
```

`parseAge("abc")` должна вернуть `-1`

```kotlin
    tryCatch(parseAge("abc") == -1)
```

`parseAge("3.5")` должна вернуть `-1`

```kotlin
    tryCatch(parseAge("3.5") == -1)
```

`parseAge("-7")` должна вернуть `-7`

```kotlin
    tryCatch(parseAge("-7") == -7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun parseAge(text: String): Int {
    return text.toIntOrNull() ?: -1
}
```
