---
language: kotlin
exerciseType: 2
---

# --description--

Лямбда может использовать переменные, объявленные вокруг неё, даже после того, как окружающий код завершился. Это называется **замыканием**: лямбда *захватывает* нужные ей переменные.
В отличие от многих других языков, Kotlin позволяет лямбде **изменять** захваченную `var`:
```kotlin
var clicks = 0
val onClick = { clicks++ }
onClick()
onClick()
println(clicks) // 2
```
Каждый вызов `onClick` обновляет ту же переменную `clicks`, которую видит внешний код.

# --instructions--

Дополните лямбду `add` так, чтобы каждый вызов увеличивал захваченную переменную `total` на `n`, чтобы программа вывела `12`

# --seed--

```kotlin
var total = 0
val add = { n: Int -> total [/] n }
add(5)
add(7)
println(total)
```

# --answers--

- +=
- +
- =
- -=
- ==

# --solutions--

```kotlin
var total = 0
val add = { n: Int -> total += n }
add(5)
add(7)
println(total)
```

# --output--

12
