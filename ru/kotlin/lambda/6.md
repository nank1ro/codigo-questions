---
language: kotlin
exerciseType: 1
---

# --description--

Лямбда, возвращающая `Boolean`, называется **предикатом**. Несколько функций коллекций принимают его:
- `filter` оставляет только элементы, для которых предикат равен `true`
- `count` возвращает, сколько элементов ему удовлетворяет
- `any` и `all` сообщают, удовлетворяет ли ему хотя бы один или каждый элемент
```kotlin
val numbers = listOf(1, 2, 3, 4, 5, 6)
println(numbers.filter { it % 2 == 0 }) // [2, 4, 6]
println(numbers.count { it > 4 })        // 2
println(numbers.any { it > 5 })          // true
```
Вызовы можно **объединять в цепочки**: каждая функция возвращает новый список, с которым работает следующая.
```kotlin
println(numbers.filter { it % 2 == 0 }.map { it * 10 }) // [20, 40, 60]
```

# --instructions--

Напишите функцию `evenSquares(numbers: List<Int>): List<Int>`, которая возвращает квадраты чётных чисел из `numbers`, в том же порядке, объединяя в цепочку `filter` и `map`.

Пример: `evenSquares(listOf(1, 2, 3, 4))` возвращает `[4, 16]`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`evenSquares(listOf(1, 2, 3, 4))` должен вернуть `[4, 16]`

```kotlin
    tryCatch(evenSquares(listOf(1, 2, 3, 4)) == listOf(4, 16))
```

`evenSquares(listOf(1, 3, 5))` должен вернуть пустой список

```kotlin
    tryCatch(evenSquares(listOf(1, 3, 5)) == listOf<Int>())
```

`evenSquares(listOf(6, -2, 0))` должен вернуть `[36, 4, 0]`

```kotlin
    tryCatch(evenSquares(listOf(6, -2, 0)) == listOf(36, 4, 0))
```

`evenSquares(listOf())` должен вернуть пустой список

```kotlin
    tryCatch(evenSquares(listOf()) == listOf<Int>())
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun evenSquares(numbers: List<Int>): List<Int> {
    return numbers.filter { it % 2 == 0 }.map { it * it }
}
```
