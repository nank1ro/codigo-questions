---
language: kotlin
exerciseType: 4
---

# --description--

Поскольку `split` возвращает `List`, вы можете перебирать её элементы, как и любой другой список:
```kotlin
for (part in "a-b".split("-")) {
    println(part)
}
// prints a, then b
```

# --instructions--

Сможете расставить эти строки, чтобы вывести каждое слово из `sentence` заглавными буквами, по одному на строку?

# --answers--

```kotlin
val sentence = "the quick brown fox"
```

```kotlin
val words = sentence.split(" ")
```

```kotlin
for (word in words) {
```

```kotlin
    println(word.uppercase())
```

```kotlin
}
```

# --solutions--

```kotlin
val sentence = "the quick brown fox"
val words = sentence.split(" ")
for (word in words) {
    println(word.uppercase())
}
```

# --output--

THE
QUICK
BROWN
FOX
