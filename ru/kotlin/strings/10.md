---
language: kotlin
exerciseType: 2
---

# --description--

`replace(old, new)` возвращает копию строки, в которой **каждое** вхождение `old` заменено на `new`:
```kotlin
val text = "a-b-c"
println(text.replace("-", "+")) // a+b+c
println(text)                   // a-b-c
```

# --instructions--

Дополните код, чтобы вывести `I like dogs`

# --seed--

```kotlin
val text = "I like cats"
println(text.[/]("cats", [/]))
```

# --answers--

- replace
- replaceAll
- substring
- "dogs"
- "dog"
- dogs

# --solutions--

```kotlin
val text = "I like cats"
println(text.replace("cats", "dogs"))
```

# --output--

I like dogs
