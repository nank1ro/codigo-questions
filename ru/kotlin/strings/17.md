---
language: kotlin
exerciseType: 1
---

# --description--

Построение длинной строки по частям с помощью `+` создаёт новую строку на каждом шаге. `StringBuilder` — это изменяемый текстовый буфер, созданный именно для этой задачи: `append` добавляет текст в конец (и возвращает сам builder, поэтому вызовы можно объединять в цепочку), а `toString()` даёт итоговую `String`:
```kotlin
val sb = StringBuilder()
sb.append("Hello")
sb.append(", ").append("world")
println(sb.toString()) // Hello, world
```
`append` принимает строки, символы и числа.

# --instructions--

Напишите функцию `initials(fullName: String): String`, которая разбивает `fullName` по пробелам и возвращает первый символ каждого слова, соединённые вместе.

Пример: `initials("Ada Lovelace")` возвращает `"AL"`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`"Ada Lovelace"` должно дать `"AL"`

```kotlin
    tryCatch(initials("Ada Lovelace") == "AL")
```

`"Grace Brewster Murray Hopper"` должно дать `"GBMH"`

```kotlin
    tryCatch(initials("Grace Brewster Murray Hopper") == "GBMH")
```

`"Plato"` должно дать `"P"`

```kotlin
    tryCatch(initials("Plato") == "P")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun initials(fullName: String): String {
    val sb = StringBuilder()
    for (word in fullName.split(" ")) {
        sb.append(word[0])
    }
    return sb.toString()
}
```
