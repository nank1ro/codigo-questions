---
language: javascript
exerciseType: 1
---

# --description--

`Number()` строг: вся строка должна быть числом, иначе результат — `NaN` ("Not a Number"):
```javascript
console.log(Number("12px"));
// prints NaN
```
`parseInt()` и `parseFloat()` более снисходительны: они читают цифры с начала строки, пропускают начальные пробелы и останавливаются на первом символе, который не является частью числа. `parseInt` сохраняет только целую часть:
```javascript
console.log(parseInt("12px"), parseFloat("1.5kg"));
// prints 12 1.5
console.log(parseInt("3.9em"), parseInt("-4px"));
// prints 3 -4
```
Когда строка не начинается с того, с чего может начинаться число (необязательный знак, затем цифра), они тоже возвращают `NaN`:
```javascript
console.log(parseInt("auto"));
// prints NaN
```
`NaN` — единственное значение, которое не равно самому себе, поэтому `x === NaN` всегда `false`; чтобы его обнаружить, используйте `Number.isNaN(x)`.

# --instructions--

Напишите функцию `toPixels(text)`, которая возвращает целое число в начале `text` (например, `12` для `"12px"`) или `0`, когда `text` не начинается с числа, используя `parseInt`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function toPixels(text) {

}
```

# --asserts--

`toPixels("12px")` должна возвращать `12`.

```javascript
tryCatch(toPixels("12px") === 12);
```

`toPixels("3.9em")` должна возвращать `3`.

```javascript
tryCatch(toPixels("3.9em") === 3);
```

`toPixels("-4px")` должна возвращать `-4`.

```javascript
tryCatch(toPixels("-4px") === -4);
```

`toPixels("auto")` должна возвращать `0`.

```javascript
tryCatch(toPixels("auto") === 0);
```

`toPixels("")` должна возвращать `0`.

```javascript
tryCatch(toPixels("") === 0);
```

Функция должна использовать `parseInt`.

```javascript
tryCatch(/parseInt\(/.test(toPixels.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function toPixels(text) {
  const value = parseInt(text);
  if (Number.isNaN(value)) {
    return 0;
  }
  return value;
}
```
