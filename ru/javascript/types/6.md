---
language: javascript
exerciseType: 2
---

# --description--

Есть два способа спросить «это `NaN`?», и они отвечают на разные вопросы.
Старая глобальная функция `isNaN(value)` сначала **преобразует** `value` в число, а затем проверяет. Поэтому она говорит `true` для всего, что не может стать числом, даже если это вовсе не `NaN`:
```javascript
console.log(isNaN("hello"));
// prints true, because Number("hello") is NaN
console.log(isNaN("42"));
// prints false, because Number("42") is 42
```
`Number.isNaN(value)` **не** преобразует: она равна `true`, только когда `value` — действительно число `NaN`:
```javascript
console.log(Number.isNaN("hello"));
// prints false, a string is not NaN
console.log(Number.isNaN(Number("hello")));
// prints true
```
Предпочитайте `Number.isNaN`, а если хотите узнать, не удалось ли преобразование, сначала выполните преобразование.

# --instructions--

Дополните код так, чтобы первая строка использовала глобальную `isNaN`, вторая — `Number.isNaN` на самой строке, а третья преобразовывала `value` в число перед проверкой, давая вывод `true`, `false`, `true`

# --seed--

```javascript
const value = "hello";
console.log([/](value));
console.log([/](value));
console.log(Number.isNaN([/]));
```

# --answers--

- isNaN
- Number.isNaN
- Number(value)
- String(value)
- value
- isNumber

# --solutions--

```javascript
const value = "hello";
console.log(isNaN(value));
console.log(Number.isNaN(value));
console.log(Number.isNaN(Number(value)));
```

# --output--

true
false
true
