---
language: javascript
exerciseType: 1
---

# --description--

JavaScript выполняет преобразования и **неявно**, и чаще всего здесь преподносит сюрпризы оператор `+`. Если хотя бы одна из сторон — строка, `+` **конкатенирует**, а другая сторона преобразуется в строку:
```javascript
console.log("5" + 3);
// prints 53
console.log(1 + 2 + "3");
// prints 33, because 1 + 2 is computed first
```
Все остальные арифметические операторы преобразуют обе стороны в **числа**:
```javascript
console.log("6" - 2, "3" * "4");
// prints 4 12
```
Поэтому сложение значений, приходящих из текста (пользовательский ввод, файлы, URL), может незаметно построить строку вместо суммы. Чтобы быть уверенным, преобразуйте с помощью `Number()` перед сложением.

# --instructions--

Напишите функцию `total(prices)`, которая возвращает числовую сумму элементов массива `prices`, где каждый элемент — либо число, либо строка, содержащая число, например `"2.5"`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function total(prices) {

}
```

# --asserts--

`total([1, 2, 3])` должна возвращать `6`.

```javascript
tryCatch(total([1, 2, 3]) === 6);
```

`total(["1", 2])` должна возвращать число `3`, а не строку `"12"`.

```javascript
tryCatch(total(["1", 2]) === 3);
```

`total(["2.5", "0.5"])` должна возвращать `3`.

```javascript
tryCatch(total(["2.5", "0.5"]) === 3);
```

`total(["10"])` должна возвращать число `10`.

```javascript
tryCatch(total(["10"]) === 10 && typeof total(["10"]) === "number");
```

`total([])` должна возвращать `0`.

```javascript
tryCatch(total([]) === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function total(prices) {
  let sum = 0;
  for (const price of prices) {
    sum += Number(price);
  }
  return sum;
}
```
