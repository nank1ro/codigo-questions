---
language: javascript
exerciseType: 1
---

# --description--

Комментарию не нужна отдельная строка: он может идти после кода в той же строке. Это **строчный комментарий** (или завершающий комментарий), и он хорошо подходит для короткой заметки об этой конкретной инструкции:
```javascript
const retries = 3; // give up after three attempts
```
Всё от `//` до конца строки игнорируется, а код перед ним выполняется как обычно.

# --instructions--

Добавьте строчный комментарий после `let seats = 4;`, объясняющий это значение, например `// seats per table`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
let seats = 4;
```

# --asserts--

The line `let seats = 4;` must end with a `//` comment.

```json
{
  "regex": "let seats = 4;[ \\t]*//[ \\t]*\\S",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`seats` must still be equal to `4`.

```javascript
tryCatch(seats === 4);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
let seats = 4; // seats per table
```
