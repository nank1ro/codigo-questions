---
language: javascript
exerciseType: 4
---

# --description--

Порядок внутри комментария JSDoc всегда один и тот же: сначала краткое описание, затем по одному `@param` на каждый параметр в порядке их объявления, и в конце `@returns`. Открывающее `/**` и закрывающее ` */` обрамляют всё это, а сам комментарий стоит прямо над функцией, которую описывает:
```javascript
/**
 * Returns the number of seconds in the given minutes.
 * @param {number} minutes a whole number of minutes
 * @returns {number} minutes multiplied by sixty
 */
function toSeconds(minutes) {
  return minutes * 60;
}
```

# --instructions--

Расположите элементы так, чтобы задокументировать `twice` кратким описанием, строкой `@param` и строкой `@returns` — именно в этом порядке и прямо над функцией, — а затем вывести `twice(21)` после определения функции

# --answers--

```javascript
/**
```

```javascript
 * Returns the double of a number.
```

```javascript
 * @param {number} n the number to double
```

```javascript
 * @returns {number} n multiplied by two
```

```javascript
 */
```

```javascript
function twice(n) {
  return n * 2;
}
```

```javascript
console.log(twice(21));
```

# --solutions--

```javascript
/**
 * Returns the double of a number.
 * @param {number} n the number to double
 * @returns {number} n multiplied by two
 */
function twice(n) {
  return n * 2;
}
console.log(twice(21));
```

# --output--

42
