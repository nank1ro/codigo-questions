---
language: javascript
exerciseType: 1
---

# --description--

Захватывайте только то, что нужно. Группа — это не просто способ прочитать кусок обратно; она также подсказывает читателю, какая часть шаблона важна. В шаблоне времени, где вам нужны только минуты, сгруппируйте одни минуты и оставьте остальное без группировки:
```javascript
const minutes = "at 14:35:02".match(/\d{2}:(\d{2}):\d{2}/)?.[1];
console.log(minutes);
// выводит 35
```
Весь шаблон всё равно должен совпасть, поэтому часы и секунды по-прежнему обязательны; они просто не захватываются. Меньше групп — меньше чисел, которые нужно держать в голове, когда вы читаете `match[1]`, `match[2]` и так далее.

# --instructions--

Напишите функцию `monthOf`, которая принимает строку `date`, которая может содержать дату, записанную как `YYYY-MM-DD`, возможно окружённую другим текстом, и возвращает её месяц в виде строки, или пустую строку, когда `date` не содержит такой даты. Например, `monthOf("2026-09-12")` возвращает `09`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function monthOf(date) {
  
}
```

# --asserts--

`monthOf("2026-09-12")` должна возвращать `09`.

```javascript
tryCatch(monthOf("2026-09-12") === "09");
```

`monthOf("born on 1999-12-31")` должна возвращать `12`.

```javascript
tryCatch(monthOf("born on 1999-12-31") === "12");
```

`monthOf("no date")` должна возвращать пустую строку.

```javascript
tryCatch(monthOf("no date") === "");
```

`monthOf("12-31-1999")` должна возвращать пустую строку.

```javascript
tryCatch(monthOf("12-31-1999") === "");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function monthOf(date) {
  return date.match(/\d{4}-(\d{2})-\d{2}/)?.[1] ?? "";
}
```
