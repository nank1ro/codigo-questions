---
language: javascript
exerciseType: 1
---

# --description--

Поскольку `match` возвращает `null`, когда шаблона нет, чтение `match[0]` сразу же выбрасывает `TypeError: Cannot read properties of null`. Защититесь от этого:
```javascript
function firstWord(text) {
  const match = text.match(/[a-z]+/);
  if (match === null) {
    return "";
  }
  return match[0];
}
```
Оператор нулевого слияния записывает ту же защиту в одну строку, потому что `match?.[0]` — это `undefined`, когда `match` равен `null`:
```javascript
return text.match(/[a-z]+/)?.[0] ?? "";
```

# --instructions--

Напишите функцию `firstNumber`, которая принимает строку `text` и возвращает первую последовательность содержащихся в ней цифр в виде строки, или пустую строку, когда цифр нет

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function firstNumber(text) {
  
}
```

# --asserts--

`firstNumber("order 42 shipped")` должна возвращать `42`.

```javascript
tryCatch(firstNumber("order 42 shipped") === "42");
```

`firstNumber("a1b22")` должна возвращать `1`.

```javascript
tryCatch(firstNumber("a1b22") === "1");
```

`firstNumber("no digits here")` должна возвращать пустую строку.

```javascript
tryCatch(firstNumber("no digits here") === "");
```

`firstNumber("2026 and 2027")` должна возвращать `2026`.

```javascript
tryCatch(firstNumber("2026 and 2027") === "2026");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function firstNumber(text) {
  return text.match(/\d+/)?.[0] ?? "";
}
```
