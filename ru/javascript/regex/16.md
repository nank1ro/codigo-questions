---
language: javascript
exerciseType: 1
---

# --description--

Последний флаг завершает набор. По умолчанию `^` и `$` означают начало и конец **всей строки**, поэтому шаблон, заякоренный через `^`, может совпасть только в самом начале, даже когда текст состоит из нескольких строк.

Флаг **`m`** (multiline) меняет это: `^` и `$` тогда также совпадают сразу после и прямо перед каждым переводом строки, поэтому каждая строка заякорена сама по себе:
```javascript
const text = "note a\nb\nnote c";
console.log(text.match(/^note.*/g));
// выводит [ 'note a' ]
console.log(text.match(/^note.*/gm));
// выводит [ 'note a', 'note c' ]
```
Здесь важны две детали. По умолчанию `.` не совпадает с переводом строки (это меняет только флаг `s`), поэтому `.*` сам останавливается в конце строки. А `match` с флагом `g` возвращает `null`, а не пустой массив, когда ничего не совпадает, так что объединяйте его с `?? []`, когда вы обещаете вернуть массив.

# --instructions--

Напишите функцию `todoLines`, которая принимает строку `text` и возвращает массив всех строк, начинающихся с `todo`, без учёта регистра. Возвращайте пустой массив, когда таких строк нет

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function todoLines(text) {
  
}
```

# --asserts--

`todoLines("todo a\nb\nTODO c")` должна возвращать обе строки с todo.

```javascript
tryCatch(JSON.stringify(todoLines("todo a\nb\nTODO c")) === '["todo a","TODO c"]');
```

`todoLines("nothing here")` должна возвращать пустой массив.

```javascript
tryCatch(JSON.stringify(todoLines("nothing here")) === '[]');
```

`todoLines("Todo write tests")` должна возвращать эту строку.

```javascript
tryCatch(JSON.stringify(todoLines("Todo write tests")) === '["Todo write tests"]');
```

`todoLines("a todo b")` должна возвращать пустой массив, потому что строка не начинается с todo.

```javascript
tryCatch(JSON.stringify(todoLines("a todo b")) === '[]');
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function todoLines(text) {
  return text.match(/^todo.*/gim) ?? [];
}
```
