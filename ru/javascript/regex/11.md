---
language: javascript
exerciseType: 1
---

# --description--

Флаг `g` даёт вам каждую совпавшую строку, но выбрасывает группы. Когда вам нужны группы *каждого* совпадения, используйте **`matchAll`**. Он возвращает итератор полных объектов совпадений, каждый из которых в точности похож на результат обычного `match`:
```javascript
const text = "a=1;b=2";
for (const match of text.matchAll(/(?<key>\w+)=(?<value>\w+)/g)) {
  console.log(`${match.groups.key} -> ${match.groups.value}`);
}
// выводит a -> 1
// выводит b -> 2
```
`matchAll` требует флаг `g`; без него он выбрасывает `TypeError`. Поскольку он возвращает итератор, разверните его через `[...text.matchAll(pattern)]`, когда вам нужен настоящий массив, и учтите, что он не выдаёт ничего вовсе, когда шаблон ни разу не совпадает.

# --instructions--

Напишите функцию `parsePairs`, которая принимает строку `text`, находит в ней каждую пару `key=value`, где ключи и значения — последовательности словесных символов, и возвращает объект, отображающий каждый ключ в его значение. Например, `parsePairs("host=local;port=80")` возвращает `{ host: "local", port: "80" }`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function parsePairs(text) {
  
}
```

# --asserts--

`parsePairs("host=local;port=80")` должна возвращать объект с `host` и `port`.

```javascript
tryCatch(JSON.stringify(parsePairs("host=local;port=80")) === '{"host":"local","port":"80"}');
```

`parsePairs("a=1")` должна возвращать объект только с `a`.

```javascript
tryCatch(JSON.stringify(parsePairs("a=1")) === '{"a":"1"}');
```

`parsePairs("nothing here")` должна возвращать пустой объект.

```javascript
tryCatch(JSON.stringify(parsePairs("nothing here")) === '{}');
```

`parsePairs("x=1 y=2 z=3")` должна возвращать все три пары.

```javascript
tryCatch(JSON.stringify(parsePairs("x=1 y=2 z=3")) === '{"x":"1","y":"2","z":"3"}');
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function parsePairs(text) {
  const pairs = {};
  for (const match of text.matchAll(/(?<key>\w+)=(?<value>\w+)/g)) {
    pairs[match.groups.key] = match.groups.value;
  }
  return pairs;
}
```
