---
language: javascript
exerciseType: 1
---

# --description--

Ещё два строительных блока позволяют описать почти любую форму текста.

**Символьный класс** — это набор символов в квадратных скобках; он соответствует ровно одному из них. Дефис записывает диапазон, а `^` в начале инвертирует набор:
```javascript
/[aeiou]/   // one vowel
/[a-z]/     // one lowercase letter
/[A-Z0-9]/  // one uppercase letter or one digit
/[^0-9]/    // one character that is not a digit
```
**Квантификаторы** говорят, сколько раз повторяется предыдущая часть: `+` — один или больше, `*` — ноль или больше, `?` — ноль или один, а `{n}` — ровно `n` раз.

Наконец, **якоря** привязывают шаблон к краям текста: `^` означает «начинай здесь», а `$` — «заканчивай здесь». Без них шаблон может совпасть в любом месте строки, поэтому `/\d{2}/.test("abc12def")` — это `true`. С обоими якорями совпасть должна вся строка целиком:
```javascript
console.log(/^\d{2}$/.test("abc12def"));
// prints false
console.log(/^\d{2}$/.test("12"));
// prints true
```

# --instructions--

Напишите функцию `isCode`, которая принимает строку `text` и возвращает `true` только когда `text` — это ровно три прописные буквы, за которыми следуют две цифры, например `ABC12`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isCode(text) {
  
}
```

# --asserts--

`isCode("ABC12")` должна возвращать `true`.

```javascript
tryCatch(isCode("ABC12") === true);
```

`isCode("abc12")` должна возвращать `false`.

```javascript
tryCatch(isCode("abc12") === false);
```

`isCode("ABC123")` должна возвращать `false`.

```javascript
tryCatch(isCode("ABC123") === false);
```

`isCode("xABC12")` должна возвращать `false`.

```javascript
tryCatch(isCode("xABC12") === false);
```

`isCode("AB12")` должна возвращать `false`.

```javascript
tryCatch(isCode("AB12") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isCode(text) {
  return /^[A-Z]{3}\d{2}$/.test(text);
}
```
