---
language: javascript
exerciseType: 1
---

# --description--

Оператор **`instanceof`** спрашивает, был ли объект создан из класса или из любого класса, который от него наследует:
```javascript
class Animal {}
class Dog extends Animal {}
const max = new Dog();
console.log(max instanceof Dog);    // true
console.log(max instanceof Animal); // true, Dog extends Animal
```
В JavaScript нет ключевого слова `abstract`, но ту же идею пишут вручную: базовый класс задаёт форму, а каждый метод, который дочерний класс *обязан* предоставить, просто выбрасывает ошибку:
```javascript
class Shape {
    area() {
        throw new Error("area() must be implemented");
    }
}
```
Дочерний класс, забывший переопределить `area`, громко падает при первом же использовании, вместо того чтобы молча вернуть `undefined`.

# --instructions--

Напишите класс `Shape`, метод `area` которого выбрасывает `Error`, и класс `Square`, который его расширяет, принимает `side` и переопределяет `area`, возвращая площадь квадрата

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Shape {
  
}

class Square {
  
}
```

# --asserts--

`new Square(3).area()` должен вернуть `9`.

```javascript
tryCatch(new Square(3).area() === 9);
```

`new Square(5).area()` должен вернуть `25`.

```javascript
tryCatch(new Square(5).area() === 25);
```

Квадрат должен быть `Shape`.

```javascript
tryCatch(new Square(3) instanceof Shape === true);
```

Вызов `area` на базовом классе должен выбрасывать ошибку.

```javascript
let _threw = false;
try {
  new Shape().area();
} catch (e) {
  _threw = true;
}
tryCatch(_threw === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Shape {
  area() {
    throw new Error("area() must be implemented");
  }
}

class Square extends Shape {
  constructor(side) {
    super();
    this.side = side;
  }
  area() {
    return this.side * this.side;
  }
}
```
