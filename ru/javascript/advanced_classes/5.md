---
language: javascript
exerciseType: 2
---

# --description--

До сих пор каждое свойство создавалось внутри конструктора. **Поле класса** позволяет объявить его прямо в теле класса, с необязательным начальным значением:
```javascript
class Counter {
    count = 0;
    step = 1;
}
console.log(new Counter().count);
// выводит 0
```
Поля присваиваются каждому новому экземпляру до выполнения тела конструктора, поэтому конструктор уже может на них рассчитывать. Поле без значения всё равно объявлено, просто начинается со значения `undefined`:
```javascript
class Task {
    done = false;
    title;
}
```
Обратите внимание на синтаксис: никаких `let`, `const` и `this` в объявлении, а строка заканчивается точкой с запятой.

# --instructions--

Дополните класс так, чтобы `count` начинался с `0`, а `step` — с `1`, затем выведите счётчик после двух увеличений

# --seed--

```javascript
class Counter {
    [/] = 0;
    step [/] 1;
    increment() {
        this.count += this.step;
    }
}
const c = new Counter();
c.increment();
c.increment();
console.log(c.count);
```

# --answers--

- count
- let count
- this.count
- =
- :
- ==

# --solutions--

```javascript
class Counter {
    count = 0;
    step = 1;
    increment() {
        this.count += this.step;
    }
}
const c = new Counter();
c.increment();
c.increment();
console.log(c.count);
```

# --output--

2
