---
language: javascript
exerciseType: 2
---

# --description--

`static` работает и с полями. **Статическое свойство** хранится один раз на классе, а не по одному на каждый экземпляр, что делает его естественным местом для общего счётчика или константы:
```javascript
class Circle {
    static PI = 3.14;
}
console.log(Circle.PI);
// prints 3.14
```
Так как копия всего одна, каждый экземпляр, который её обновляет, изменяет одно и то же значение. Внутри конструктора до него добираются через имя класса, `Circle.PI`, а не через `this`: `this.PI` стал бы искать свойство на экземпляре, ничего не нашёл бы и вернул `undefined`.

# --instructions--

Дополните код так, чтобы `User` считал, сколько пользователей было создано, затем выведите итог

# --seed--

```javascript
class User {
    static [/] = 0;
    constructor(name) {
        this.name = name;
        [/].count += 1;
    }
}
new User("ann");
new User("bob");
console.log(User.count);
```

# --answers--

- count
- static count
- User
- this
- new User

# --solutions--

```javascript
class User {
    static count = 0;
    constructor(name) {
        this.name = name;
        User.count += 1;
    }
}
new User("ann");
new User("bob");
console.log(User.count);
```

# --output--

2
