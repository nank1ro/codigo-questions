---
language: javascript
exerciseType: 4
---

# --description--

Вывод объекта обычно даёт что-то бесполезное. Всякий раз, когда JavaScript нужна строка, а получает он объект, вызывается метод объекта **`toString`**, и стандартный возвращает `[object Object]`. Собственное определение заменяет его:
```javascript
class Money {
    constructor(amount) {
        this.amount = amount;
    }
    toString() {
        return `$${this.amount}`;
    }
}
console.log(`${new Money(7)}`);
// prints $7
```
Тот же метод используется при конкатенации строк и в `String(value)`. Если вам нужно ещё и осмысленное **число**, определите `[Symbol.toPrimitive](hint)`, который получает `"string"`, `"number"` или `"default"` и решает, что вернуть; если он есть, он побеждает `toString`.

# --instructions--

Расположите элементы так, чтобы получилась работающая программа, которая выводит цену через шаблонную строку

# --answers--

```javascript
class Money {
  constructor(amount) {
    this.amount = amount;
  }
```

```javascript
  toString() {
    return `$${this.amount}`;
  }
}
```

```javascript
const price = new Money(7);
```

```javascript
console.log(`Total: ${price}`);
```

# --solutions--

```javascript
class Money {
  constructor(amount) {
    this.amount = amount;
  }
  toString() {
    return `$${this.amount}`;
  }
}
const price = new Money(7);
console.log(`Total: ${price}`);
```

# --output--

Total: $7
