---
language: javascript
exerciseType: 2
---

# --description--

Булеву переменную, даже построенную с помощью отрицания `!`, как `isAfternoon`, можно использовать напрямую в качестве условия `if`, без сравнения.

# --instructions--

Заполните пропуски, чтобы выполнить блок кода

# --seed--

```javascript
var isMorning = false;
var isAfternoon = !isMorning;
if ([/]) {
    console.log("Good afternoon!");
}
```

# --answers--

- isMorning
- isAfternoon
- "isMorning"
- "isAfternoon"

# --solutions--

```javascript
var isMorning = false;
var isAfternoon = !isMorning;
if (isAfternoon) {
    console.log("Good afternoon!");
}
```

# --output--

Good afternoon!
