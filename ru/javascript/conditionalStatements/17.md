---
language: javascript
exerciseType: 2
---

# --description--

Именно фигурные скобки объединяют несколько выражений в один блок. Без них `if` управляет только одним выражением, следующим сразу за ним, поэтому `if (true) console.log("Hello!");` — валидный JavaScript.

# --instructions--

Заполните пропуски, чтобы выполнить блок кода

# --seed--

```javascript
[/][/]
    [/]
[/]
```

# --answers--

- if (true)
- if (False)
- if "true"
- if ("False")
- if
- else
- elif
-  {
- console.log("Hello!");
- }

# --solutions--

```javascript
if (true) {
    console.log("Hello!");
}
```

# --output--

Hello!
