---
language: javascript
exerciseType: 1
---

# --description--

Чтобы проверить, содержит ли строка другую строку, используйте эти методы — все они возвращают логическое значение:
- `includes(text)` — `true`, если `text` встречается где-либо
- `startsWith(text)` — `true`, если строка начинается с `text`
- `endsWith(text)` — `true`, если строка заканчивается на `text`

```javascript
let file = "photo.png";
console.log(file.includes("."));
// prints true
console.log(file.startsWith("ph"));
// prints true
console.log(file.endsWith(".jpg"));
// prints false
```
Сравнение чувствительно к регистру: `"Hello".includes("h")` — это `false`.

# --instructions--

Напишите функцию `isImage(fileName)`, которая возвращает `true`, если `fileName` заканчивается на `".png"`, и `false` в противном случае

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isImage(fileName) {

}
```

# --asserts--

`isImage("cat.png")` должна возвращать `true`.

```javascript
tryCatch(isImage("cat.png") === true);
```

`isImage("cat.jpg")` должна возвращать `false`.

```javascript
tryCatch(isImage("cat.jpg") === false);
```

`isImage("png.txt")` должна возвращать `false`.

```javascript
tryCatch(isImage("png.txt") === false);
```

`isImage("a.png.txt")` должна возвращать `false`.

```javascript
tryCatch(isImage("a.png.txt") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isImage(fileName) {
  return fileName.endsWith(".png");
}
```
