---
language: javascript
exerciseType: 2
---

# --description--

Метод `split(separator)` разбивает строку на **массив** частей, разрезая её на каждом `separator`:
```javascript
let sentence = "I like JavaScript";
let words = sentence.split(" ");
console.log(words);
// prints [ 'I', 'like', 'JavaScript' ]
```
Противоположность — метод массива `join(separator)`, который склеивает части обратно в строку:
```javascript
console.log(words.join("-"));
// prints I-like-JavaScript
```

# --instructions--

Разбейте `csv` по каждой запятой и выведите, сколько цветов он содержит

# --seed--

```javascript
let csv = "red,green,blue";
let colors = csv.[/]([/]);
console.log(colors.[/]);
```

# --answers--

- split
- ","
- " "
- join
- length
- size

# --solutions--

```javascript
let csv = "red,green,blue";
let colors = csv.split(",");
console.log(colors.length);
```

# --output--

3
