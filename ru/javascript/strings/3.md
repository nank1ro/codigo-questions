---
language: javascript
exerciseType: 2
---

# --description--

У строк есть много встроенных **методов**. Два самых простых меняют регистр каждой буквы:
```javascript
let word = "Hello";
console.log(word.toUpperCase());
// prints HELLO
console.log(word.toLowerCase());
// prints hello
```
Оба метода не принимают аргументов, поэтому не забывайте про скобки.

# --instructions--

Выведите `name` в верхнем регистре, а затем в нижнем

# --seed--

```javascript
let name = "Ada";
console.log(name.[/]());
console.log(name.[/]());
```

# --answers--

- toUpperCase
- toLowerCase
- upper
- lower
- capitalize

# --solutions--

```javascript
let name = "Ada";
console.log(name.toUpperCase());
console.log(name.toLowerCase());
```

# --output--

ADA
ada
