---
language: javascript
exerciseType: 2
---

# --description--

Метод `slice(start, end)` извлекает часть строки, от индекса `start` до индекса `end` (не включая его):
```javascript
let word = "JavaScript";
console.log(word.slice(0, 4));
// prints Java
console.log(word.slice(4));
// prints Script
```
Если опустить `end`, срез идёт до конца строки.
Отрицательный индекс отсчитывается с конца: `word.slice(-3)` — это `"ipt"`.
Метод `substring(start, end)` работает так же, но не принимает отрицательные индексы.

# --instructions--

Используйте `slice()`, чтобы вывести первые четыре символа `word`

# --seed--

```javascript
let word = "JavaScript";
console.log(word.[/](0, [/]));
```

# --answers--

- slice
- 4
- 3
- 5
- splice
- cut

# --solutions--

```javascript
let word = "JavaScript";
console.log(word.slice(0, 4));
```

# --output--

Java
