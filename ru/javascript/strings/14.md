---
language: javascript
exerciseType: 1
---

# --description--

Метод `padStart(targetLength, padString)` добавляет `padString` в **начало** строки, пока она не достигнет `targetLength` символов. `padEnd()` делает то же самое в конце:
```javascript
console.log("7".padStart(3, "0"));
// prints 007
console.log("Tea".padEnd(6, "."));
// prints Tea...
```
Если строка уже достаточно длинная, она возвращается без изменений.
У чисел нет строковых методов, поэтому сначала преобразуйте их с помощью `String(number)`.

# --instructions--

Напишите функцию `twoDigits(number)`, которая возвращает `number` в виде строки минимум из двух символов, дополненной слева символом `"0"`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function twoDigits(number) {

}
```

# --asserts--

`twoDigits(5)` должна возвращать `"05"`.

```javascript
tryCatch(twoDigits(5) === "05");
```

`twoDigits(12)` должна возвращать `"12"`.

```javascript
tryCatch(twoDigits(12) === "12");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function twoDigits(number) {
  return String(number).padStart(2, "0");
}
```
