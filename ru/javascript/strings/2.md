---
language: javascript
exerciseType: 1
---

# --description--

У каждого символа в строке есть **индекс**, начиная с `0`.
Прочитать отдельный символ можно с помощью квадратных скобок или метода `charAt()`:
```javascript
let word = "hello";
console.log(word[0]);
// prints h
console.log(word.charAt(1));
// prints e
```
Последний символ находится по индексу `length - 1`:
```javascript
console.log(word[word.length - 1]);
// prints o
```

# --instructions--

Сохраните первый символ `word` в переменной `first`, а последний символ — в переменной `last`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
let word = "coding";
```

# --asserts--

`first` должна быть равна `"c"`.

```javascript
tryCatch(first === "c");
```

`last` должна быть равна `"g"`.

```javascript
tryCatch(last === "g");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
let word = "coding";
let first = word[0];
let last = word[word.length - 1];
```
