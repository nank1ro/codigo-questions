---
language: javascript
exerciseType: 1
---

# --description--

У каждой функции есть метод `bind`, который возвращает **новую** функцию с заранее зафиксированными значениями. Его первый аргумент становится `this` новой функции; остальные аргументы ставятся перед теми, с которыми новую функцию вызовут (**частичное применение**):
```javascript
function multiply(a, b) {
  return a * b;
}
const double = multiply.bind(null, 2);
console.log(double(21));
// prints 42
```
Фиксация `this` важна для методов. Когда метод скопирован из своего объекта и вызывается сам по себе, `this` больше не ссылается на объект, поэтому `this.name` становится `undefined`. `bind` привязывает его к объекту:
```javascript
const user = {
  name: "Ana",
  hello() { return `Hi ${this.name}`; },
};
const loose = user.hello;
console.log(loose());
// prints Hi undefined
const bound = user.hello.bind(user);
console.log(bound());
// prints Hi Ana
```
Исходная функция никогда не меняется: `bind` всегда создаёт новую, чьё `name` — это исходное имя с приставкой `bound `.

# --instructions--

Функция `power` и объект `counter` уже даны. Сохрани в константе `square` версию `power`, привязанную с `this`, равным `null`, и `exponent`, зафиксированным как `2`, а в константе `tick` — версию `counter.increment`, привязанную к `counter`, чтобы она продолжала работать при самостоятельном вызове

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = 
const tick = 
```

# --asserts--

`square(3)` должна вернуть `9`.

```javascript
tryCatch(square(3) === 9);
```

`square(12)` должна вернуть `144`.

```javascript
tryCatch(square(12) === 144);
```

`square` должна быть создана через `bind`, поэтому её имя — `"bound power"`.

```javascript
tryCatch(square.name === "bound power");
```

`tick` должна быть новой функцией, созданной через `bind`, а не самим `counter.increment`.

```javascript
tryCatch(tick !== counter.increment && tick.name === "bound increment");
```

Два самостоятельных вызова `tick()` должны вернуть `1`, а затем `2`, и `counter.count` должен быть равен `2`.

```javascript
tryCatch(tick() === 1 && tick() === 2 && counter.count === 2);
```

`tick` должна работать и тогда, когда её сохранили в другую переменную и вызвали оттуда.

```javascript
tryCatch((() => { const alone = tick; return alone() === 3; })());
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = power.bind(null, 2);
const tick = counter.increment.bind(counter);
```
