---
language: javascript
exerciseType: 4
---

# --description--

Настоящие программы объединяют эти методы в **конвейер**: отфильтруй нужные элементы, преобразуй их в нужные значения и сверни их в результат. Хранение промежуточных массивов в константах делает каждый шаг читаемым и позволяет использовать их повторно:
```javascript
const adults = people.filter((p) => p.age >= 18);
const names = adults.map((p) => p.name);
const totalAge = adults.reduce((sum, p) => sum + p.age, 0);
```
`names.join(", ")` превращает массив строк в одну строку, элементы которой разделены запятой и пробелом.

# --instructions--

Расставь строки так, чтобы оставить только товары корзины с положительным `qty`, собрать их названия, сложить для них `price * qty` и вывести названия, соединённые через `", "`, и итог в одной строке. Перечисли товары в порядке pen, book, bag и объяви `inStock`, `names` и `total` именно в этом порядке

# --answers--

```javascript
const cart = [
```

```javascript
  { name: "pen", price: 2, qty: 3 },
```

```javascript
  { name: "book", price: 12, qty: 1 },
```

```javascript
  { name: "bag", price: 30, qty: 0 },
```

```javascript
];
```

```javascript
const inStock = cart.filter((item) => item.qty > 0);
```

```javascript
const names = inStock.map((item) => item.name);
```

```javascript
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
```

```javascript
console.log(names.join(", "), total);
```

# --solutions--

```javascript
const cart = [
  { name: "pen", price: 2, qty: 3 },
  { name: "book", price: 12, qty: 1 },
  { name: "bag", price: 30, qty: 0 },
];
const inStock = cart.filter((item) => item.qty > 0);
const names = inStock.map((item) => item.name);
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
console.log(names.join(", "), total);
```

# --output--

pen, book 18
