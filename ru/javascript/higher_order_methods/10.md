---
language: javascript
exerciseType: 2
---

# --description--

Так как функции — это значения, можно написать функцию высшего порядка, которая **объединяет** две функции в новую. `compose(f, g)` возвращает функцию, применяющую сначала `g`, а затем `f` к результату, как в математической записи *f(g(x))*:
```javascript
const compose = (f, g) => (x) => f(g(x));
const trim = (s) => s.trim();
const shout = (s) => s.toUpperCase();
const clean = compose(shout, trim);
console.log(clean("  hi  "));
// prints HI
```
Порядок важен: `compose(f, g)` сначала выполняет `g`, потом `f`. Построение программ из склеенных таким образом маленьких функций называется **композицией функций**.

# --instructions--

Дополни `compose` так, чтобы она возвращала функцию, применяющую сначала `g`, а затем `f` к результату, после чего выведи `compose(double, inc)(5)` и `compose(inc, double)(5)`, получив вывод `12 11`

# --seed--

```javascript
const compose = (f, g) => (x) => [/]([/](x));
const inc = (n) => n + 1;
const double = (n) => n * 2;
console.log(compose(double, inc)(5), compose(inc, double)(5));
```

# --answers--

- f
- g
- x
- compose

# --solutions--

```javascript
const compose = (f, g) => (x) => f(g(x));
const inc = (n) => n + 1;
const double = (n) => n * 2;
console.log(compose(double, inc)(5), compose(inc, double)(5));
```

# --output--

12 11
