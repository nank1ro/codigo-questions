---
language: javascript
exerciseType: 2
---

# --description--

Аккумулятор `reduce` не обязан быть числом: это может быть строка, массив или объект. Начав с пустого объекта `{}`, можно за один проход что-то посчитать или сгруппировать. Не забудь **вернуть аккумулятор** из колбэка, иначе следующий шаг получит `undefined`:
```javascript
const votes = ["yes", "no", "yes"];
const tally = votes.reduce((acc, vote) => {
  acc[vote] = (acc[vote] ?? 0) + 1;
  return acc;
}, {});
console.log(tally);
// prints { yes: 2, no: 1 }
```
`acc[vote] ?? 0` читает текущий счётчик или `0`, когда такого ключа ещё нет.

# --instructions--

Дополни код так, чтобы `counts` сопоставлял каждой первой букве количество слов, начинающихся с неё, используя `reduce` с пустым объектом в качестве начального аккумулятора и `?? 0` для отсутствующих ключей, получив вывод `{ a: 2, b: 2, c: 1 }`

# --seed--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.[/]((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] [/] 0) + 1;
  return [/];
}, [/]);
console.log(counts);
```

# --answers--

- reduce
- ??
- acc
- {}
- map
- filter
- &&
- word
- 0

# --solutions--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.reduce((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] ?? 0) + 1;
  return acc;
}, {});
console.log(counts);
```

# --output--

{ a: 2, b: 2, c: 1 }
