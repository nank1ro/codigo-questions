---
language: javascript
exerciseType: 2
---

# --description--

Map запоминает порядок, в котором были добавлены пары, и вы можете перебирать его с помощью `for...of`.
Метод `entries()` возвращает каждую пару в виде массива `[key, value]`, который можно сразу распаковать в цикле:
```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
// prints apple: 3
// prints pear: 5
```
Перебор самого Map, `for (const [name, qty] of stock)`, делает ровно то же самое.

# --instructions--

Переберите записи `stock` и выведите каждое имя, за которым следует его количество

# --seed--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [/] of stock.[/]()) {
  console.log(`${name}: ${qty}`);
}
```

# --answers--

- [name, qty]
- [qty, name]
- name, qty
- entries
- pairs
- items

# --solutions--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
```

# --output--

apple: 3
pear: 5
