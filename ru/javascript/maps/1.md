---
language: javascript
exerciseType: 2
---

# --description--

**Map** хранит **пары ключ-значение**: каждое значение сохраняется под ключом, и вы используете этот ключ, чтобы снова найти значение.
Вы создаёте пустой Map с помощью `new Map()`, добавляете пару с помощью `set(key, value)` и читаете значение с помощью `get(key)`:
```javascript
let ages = new Map();
ages.set("Ann", 30);
ages.set("Bob", 25);
console.log(ages.get("Ann"));
// prints 30
```
Вызов `set()` с ключом, который уже существует, заменяет его значение.

# --instructions--

Создайте пустой Map `ages`, сохраните `30` под ключом `"Ann"`, затем выведите значение, сохранённое для `"Ann"`

# --seed--

```javascript
let ages = new [/]();
ages.[/]("Ann", 30);
console.log(ages.[/]("Ann"));
```

# --answers--

- Map
- set
- get
- Object
- add
- put
- read

# --solutions--

```javascript
let ages = new Map();
ages.set("Ann", 30);
console.log(ages.get("Ann"));
```

# --output--

30
