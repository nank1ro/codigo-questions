---
language: javascript
exerciseType: 2
---

# --description--

`delete(key)` возвращает `true`, если пара была удалена, и `false`, если такого ключа не было.
Чтобы удалить **все** пары сразу, вызовите `clear()`:
```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Удалите `"pen"` из `cart`, выведя результат удаления, затем очистите весь Map и выведите, сколько пар осталось

# --seed--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
