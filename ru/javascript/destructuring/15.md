---
language: javascript
exerciseType: 4
---

# --description--

`Object.entries(obj)` превращает объект в массив пар `[key, value]`. Поместите шаблон массива в заголовок цикла `for...of`, и каждая пара будет распакована по мере выполнения цикла:
```javascript
const ages = { ada: 36, bob: 41 };
for (const [name, age] of Object.entries(ages)) {
    console.log(`${name} is ${age}`);
}
// prints ada is 36
// prints bob is 41
```
Это читаемый способ пройти по объекту: ни индекса, ни обращения по ключу — только те два имени, которые вам важны. `Object.keys` и `Object.values` дают только одну из сторон, `Object.entries` даёт обе.

# --instructions--

Расположите элементы так, чтобы получилась программа, которая выводит, сколько видов фруктов есть на складе, а затем по строке на каждую запись

# --answers--

```javascript
const stock = { apples: 3, pears: 5 };
```

```javascript
console.log(`Stock (${Object.keys(stock).length}):`);
```

```javascript
for (const [item, count] of Object.entries(stock)) {
```

```javascript
  console.log(`${item}: ${count}`);
}
```

# --solutions--

```javascript
const stock = { apples: 3, pears: 5 };
console.log(`Stock (${Object.keys(stock).length}):`);
for (const [item, count] of Object.entries(stock)) {
  console.log(`${item}: ${count}`);
}
```

# --output--

Stock (2):
apples: 3
pears: 5
