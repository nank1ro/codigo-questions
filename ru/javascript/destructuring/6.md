---
language: javascript
exerciseType: 2
---

# --description--

Объекты тоже можно деструктурировать, с фигурными скобками вместо квадратных. Здесь позиция не значит ничего: каждое имя сопоставляется с **ключом**, который пишется так же:
```javascript
const user = { name: "Ada", age: 36 };
const { age, name } = user;
console.log(name, age);
// выводит Ada 36
```
Перестановка `age` и `name` в шаблоне ничего не меняет, а ключи, которые шаблон не упоминает, просто остаются в стороне. Имя без соответствующего ключа становится `undefined`.

# --instructions--

Дополните оба шаблона так, чтобы программа вывела `Dune (1965) by Herbert`

# --seed--

```javascript
const book = { title: "Dune", author: "Herbert", year: 1965 };
const { title, [/] } = book;
const { author } = [/];
console.log(`${title} (${year}) by ${author}`);
```

# --answers--

- year
- book
- title
- published
- novel
- 1965

# --solutions--

```javascript
const book = { title: "Dune", author: "Herbert", year: 1965 };
const { title, year } = book;
const { author } = book;
console.log(`${title} (${year}) by ${author}`);
```

# --output--

Dune (1965) by Herbert
