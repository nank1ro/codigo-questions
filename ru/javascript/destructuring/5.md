---
language: javascript
exerciseType: 1
---

# --description--

Шаблон может также стоять слева в обычном присваивании, без `const` или `let` перед ним, и тогда он записывает значения в уже существующие переменные. Это превращает обмен значениями двух переменных в одну строку, без временной переменной:
```javascript
let a = 1;
let b = 2;
[a, b] = [b, a];
console.log(a, b);
// prints 2 1
```
Правая часть строится первой, поэтому оба старых значения уже в безопасности внутри временного массива, когда происходит присваивание. Следите за точкой с запятой на предыдущей строке: иначе строка, начинающаяся с `[`, будет прочитана как обращение по индексу к тому, что шло перед ней.

# --instructions--

Напишите функцию `sortPair`, которая принимает два значения и возвращает массив из двух элементов, где меньший идёт первым, меняя их местами присваиванием с деструктуризацией, когда они стоят в неправильном порядке

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function sortPair(a, b) {
  
}
```

# --asserts--

`sortPair(5, 2)` должна начинаться с `2`.

```javascript
tryCatch(sortPair(5, 2)[0] === 2);
```

`sortPair(5, 2)` должна заканчиваться на `5`.

```javascript
tryCatch(sortPair(5, 2)[1] === 5);
```

Уже отсортированная пара должна остаться как есть.

```javascript
tryCatch(sortPair(1, 9).join(",") === "1,9");
```

Два равных значения должны остаться как есть.

```javascript
tryCatch(sortPair(4, 4).join(",") === "4,4");
```

Отрицательные значения тоже должны работать.

```javascript
tryCatch(sortPair(3, -8).join(",") === "-8,3");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function sortPair(a, b) {
  if (a > b) {
    [a, b] = [b, a];
  }
  return [a, b];
}
```
