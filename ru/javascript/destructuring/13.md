---
language: javascript
exerciseType: 4
---

# --description--

Шаблон может заменить имя параметра в объявлении функции, и тогда распаковка происходит в момент вызова:
```javascript
function area({ width, height }) {
    return width * height;
}
console.log(area({ width: 4, height: 3 }));
// prints 12
```
Внутри тела вообще нет объектной переменной — только `width` и `height`. Вызывающий передаёт один объект, но сигнатура документирует, какие именно ключи читает функция, и ключи могут прийти в любом порядке.

# --instructions--

Расположите элементы так, чтобы получилась программа, которая сначала определяет функцию, а затем выводит площадь прямоугольника

# --answers--

```javascript
function area({ width, height }) {
```

```javascript
  return width * height;
}
```

```javascript
const rect = { width: 4, height: 3 };
```

```javascript
console.log(area(rect));
```

# --solutions--

```javascript
function area({ width, height }) {
  return width * height;
}
const rect = { width: 4, height: 3 };
console.log(area(rect));
```

# --output--

12
