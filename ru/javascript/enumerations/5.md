---
language: javascript
exerciseType: 1
---

# --description--

Поскольку перечисление — это просто объект, обычные объектные методы позволяют его исследовать:
- `Object.keys(Enum)` возвращает массив с **именами** членов
- `Object.values(Enum)` возвращает массив со **значениями** членов
- `Object.entries(Enum)` возвращает массив пар `[имя, значение]`

```javascript
const Color = Object.freeze({ RED: "red", BLUE: "blue" });
console.log(Object.keys(Color));
// prints [ 'RED', 'BLUE' ]
console.log(Object.values(Color));
// prints [ 'red', 'blue' ]
```
Сочетание `Object.values()` с методом массива `includes()` — стандартный способ проверить, является ли произвольное значение, например считанное из пользовательского ввода, допустимым членом:
```javascript
console.log(Object.values(Color).includes("red"));
// prints true
console.log(Object.values(Color).includes("pink"));
// prints false
```

# --instructions--

Напишите функцию `isColor(value)`, которая возвращает `true`, если `value` является одним из значений перечисления `Color`, и `false` в противном случае

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Color = Object.freeze({
  RED: "red",
  GREEN: "green",
  BLUE: "blue",
});

function isColor(value) {

}
```

# --asserts--

`isColor("red")` должна вернуть `true`.

```javascript
tryCatch(isColor("red") === true);
```

`isColor("blue")` должна вернуть `true`.

```javascript
tryCatch(isColor("blue") === true);
```

`isColor("pink")` должна вернуть `false`.

```javascript
tryCatch(isColor("pink") === false);
```

`isColor("RED")` должна вернуть `false`, потому что `"RED"` — это имя члена, а не значение.

```javascript
tryCatch(isColor("RED") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Color = Object.freeze({
  RED: "red",
  GREEN: "green",
  BLUE: "blue",
});

function isColor(value) {
  return Object.values(Color).includes(value);
}
```
