---
language: javascript
exerciseType: 4
---

# --description--

Когда каждому члену нужно несколько данных или собственные методы, роль перечисления может сыграть **класс**. Каждый член — это экземпляр класса, хранящийся в свойстве `static`, то есть свойстве, принадлежащем самому классу, а не каждому экземпляру:
```javascript
class Planet {
  static MERCURY = new Planet("Mercury", 0.4);
  static EARTH = new Planet("Earth", 1);

  constructor(name, gravity) {
    this.name = name;
    this.gravity = gravity;
  }
}
console.log(Planet.EARTH.name);
// prints Earth
```
Вызовите `Object.freeze(Planet)` после класса, чтобы никто не мог добавлять или заменять члены, и заморозьте каждый экземпляр в конструкторе с помощью `Object.freeze(this)`, чтобы сами члены оставались доступными только для чтения.

# --instructions--

Расположите строки по порядку, чтобы объявить перечисление `Coin` на основе класса и вывести центы члена `DIME`.
Объявите `PENNY` перед `DIME`, обе — перед конструктором, и заморозьте класс перед выводом

# --answers--

```javascript
class Coin {
```

```javascript
  static PENNY = new Coin("penny", 1);
```

```javascript
  static DIME = new Coin("dime", 10);
```

```javascript
  constructor(name, cents) { this.name = name; this.cents = cents; }
```

```javascript
}
```

```javascript
Object.freeze(Coin);
```

```javascript
console.log(Coin.DIME.cents);
```

# --solutions--

```javascript
class Coin {
  static PENNY = new Coin("penny", 1);
  static DIME = new Coin("dime", 10);
  constructor(name, cents) { this.name = name; this.cents = cents; }
}
Object.freeze(Coin);
console.log(Coin.DIME.cents);
```

# --output--

10
