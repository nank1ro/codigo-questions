---
language: javascript
exerciseType: 1
---

# --description--

Перечисления естественно сочетаются с оператором `switch`, который сравнивает одно значение со списком меток `case` и выполняет код первой подходящей.
Каждая ветка заканчивается `return` или `break`, а необязательная ветка `default` выполняется, когда ничего не подошло:
```javascript
const Light = Object.freeze({ RED: "red", GREEN: "green" });

function action(light) {
  switch (light) {
    case Light.RED:
      return "stop";
    case Light.GREEN:
      return "go";
    default:
      return "unknown";
  }
}
console.log(action(Light.GREEN));
// prints go
```
Всегда сравнивайте с членами (`Light.RED`), а не с исходными значениями (`"red"`): если значение когда-нибудь изменится, `switch` продолжит работать.

# --instructions--

Напишите функцию `weekend(day)`, которая использует `switch` по перечислению `Day` и возвращает `true` для `Day.SATURDAY` и `Day.SUNDAY`, и `false` для любого другого значения

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {

}
```

# --asserts--

`weekend(Day.SATURDAY)` должна вернуть `true`.

```javascript
tryCatch(weekend(Day.SATURDAY) === true);
```

`weekend(Day.SUNDAY)` должна вернуть `true`.

```javascript
tryCatch(weekend(Day.SUNDAY) === true);
```

`weekend(Day.MONDAY)` должна вернуть `false`.

```javascript
tryCatch(weekend(Day.MONDAY) === false);
```

`weekend("holiday")` должна вернуть `false`, потому что это не член перечисления.

```javascript
tryCatch(weekend("holiday") === false);
```

Функция должна использовать оператор `switch`.

```javascript
tryCatch(/switch\s*\(/.test(weekend.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {
  switch (day) {
    case Day.SATURDAY:
    case Day.SUNDAY:
      return true;
    default:
      return false;
  }
}
```
