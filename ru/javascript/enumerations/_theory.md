**Перечисление** (или *enum*) — это общий тип для небольшой группы связанных фиксированных значений: дни недели, масти колоды карт, возможные состояния заказа.
В отличие от многих языков, в JavaScript **нет** ключевого слова `enum`. Идиоматической заменой служит обычный объект, свойствами которого являются члены перечисления, переданный в `Object.freeze()`, чтобы никто не мог изменить его позже:
```javascript
const Color = Object.freeze({
  RED: "red",
  GREEN: "green",
  BLUE: "blue",
});
console.log(Color.RED);
// prints red
```
По соглашению объект объявляется с помощью `const`, его имя начинается с заглавной буквы, а имена членов записываются в формате `UPPER_CASE`, точно так же, как и другие константы.

---

Значение, хранящееся в каждом члене, выбираете вы сами. **Строки** — самый распространённый выбор, потому что они читаемы при выводе, логировании или сохранении в файл:
```javascript
const Status = Object.freeze({
  ACTIVE: "active",
  DONE: "done",
});
console.log(Status.DONE);
// prints done
```
После заморозки объект также не может получить новые свойства, а `Object.isFrozen(obj)` сообщает, был ли объект заморожен:
```javascript
console.log(Object.isFrozen(Status));
// prints true
```

---

Зачем вообще замораживать объект? Замороженный объект отклоняет любое изменение: присвоение значения существующему члену, добавление нового или удаление какого-либо не имеет никакого эффекта.
То, как проявляется это отклонение, зависит от режима, в котором выполняется ваш код:
- в **нестрогом режиме** (sloppy mode, режим по умолчанию для обычных скриптов) присвоение **молча игнорируется**
- в **строгом режиме** (strict mode, файлы, начинающиеся с `"use strict"`, модули ES и тела классов) оно **выбрасывает** `TypeError`

```javascript
const Size = Object.freeze({ SMALL: "s", LARGE: "l" });
Size.SMALL = "xs";
Size.MEDIUM = "m";
console.log(Size.SMALL);
// prints s
console.log(Size.MEDIUM);
// prints undefined
```
В любом случае перечисление сохраняет заданные вами значения, а это именно то, что нужно от набора констант.

---

Члены также могут хранить **числа**. Числовые значения удобны, когда члены имеют естественный порядок, поскольку их можно сравнивать обычными операторами:
```javascript
const Priority = Object.freeze({
  LOW: 1,
  MEDIUM: 2,
  HIGH: 3,
});
console.log(Priority.HIGH > Priority.LOW);
// prints true
```
Компромисс — читаемость: вывод `Priority.HIGH` показывает `3`, что говорит гораздо меньше, чем сказала бы строка `"high"`.

---

Поскольку перечисление — это просто объект, обычные объектные методы позволяют его исследовать:
- `Object.keys(Enum)` возвращает массив с **именами** членов
- `Object.values(Enum)` возвращает массив со **значениями** членов
- `Object.entries(Enum)` возвращает массив пар `[имя, значение]`

```javascript
const Color = Object.freeze({ RED: "red", BLUE: "blue" });
console.log(Object.keys(Color));
// prints [ 'RED', 'BLUE' ]
console.log(Object.values(Color));
// prints [ 'red', 'blue' ]
```
Сочетание `Object.values()` с методом массива `includes()` — стандартный способ проверить, является ли произвольное значение, например считанное из пользовательского ввода, допустимым членом:
```javascript
console.log(Object.values(Color).includes("red"));
// prints true
console.log(Object.values(Color).includes("pink"));
// prints false
```
