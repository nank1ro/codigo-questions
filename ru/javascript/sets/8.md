---
language: javascript
exerciseType: 2
---

# --description--

`delete(value)` возвращает `true`, если значение было удалено, и `false`, если его не было в set.
Чтобы удалить **все** значения сразу, вызовите `clear()`:
```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Удалите `"pen"` из `cart`, выведя результат удаления, затем очистите весь set и выведите, сколько значений осталось

# --seed--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
