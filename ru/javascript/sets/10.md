---
language: javascript
exerciseType: 1
---

# --description--

Сочетание расширения и `filter()` даёт вам классические операции теории множеств. Каждая из них строит **новую** коллекцию и не изменяет исходные:
- **объединение**, каждое значение, которое есть в `a`, в `b` или в обоих: `new Set([...a, ...b])`
- **пересечение**, только значения, которые есть в **обоих**: `[...a].filter((x) => b.has(x))`
- **разность**, значения из `a`, которых **нет** в `b`: `[...a].filter((x) => !b.has(x))`

```javascript
let a = new Set([1, 2, 3]);
let b = new Set([3, 4]);
console.log([...new Set([...a, ...b])]);
// prints [ 1, 2, 3, 4 ]
console.log([...a].filter((x) => b.has(x)));
// prints [ 3 ]
console.log([...a].filter((x) => !b.has(x)));
// prints [ 1, 2 ]
```
Современные движки JavaScript также предоставляют `a.union(b)`, `a.intersection(b)` и `a.difference(b)` прямо на sets, но версии с расширением и `filter()` работают везде.

# --instructions--

Напишите функцию `union(a, b)`, которая возвращает **новый** set со всеми значениями, найденными в `a` или в `b`, не изменяя ни один из них

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function union(a, b) {

}
```

# --asserts--

`union(new Set([1, 2]), new Set([2, 3]))` должно содержать ровно `1`, `2` и `3`.

```javascript
tryCatch(JSON.stringify([...union(new Set([1, 2]), new Set([2, 3]))]) === '[1,2,3]');
```

Результат должен быть `Set`.

```javascript
tryCatch(union(new Set([1]), new Set([2])) instanceof Set);
```

Объединение двух пустых sets должно иметь `size`, равный `0`.

```javascript
tryCatch(union(new Set(), new Set()).size === 0);
```

Первый set не должен изменяться.

```javascript
let _a = new Set([1]);
union(_a, new Set([2]));
tryCatch(_a.size === 1);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function union(a, b) {
  return new Set([...a, ...b]);
}
```
