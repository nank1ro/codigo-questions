---
language: javascript
exerciseType: 2
---

# --description--

Вы можете построить set сразу, передав массив в `new Set()`. Дубликаты в массиве отбрасываются, поэтому это самый быстрый способ найти отличающиеся значения массива:
```javascript
let nums = [1, 2, 2, 3, 3, 3];
let distinct = new Set(nums);
console.log(distinct.size);
// prints 3
```
Оператор **расширения** `...` работает в обратную сторону и превращает set обратно в массив:
```javascript
let unique = [...distinct];
console.log(unique);
// prints [ 1, 2, 3 ]
```
`Array.from(distinct)` делает то же самое.

# --instructions--

Постройте массив `unique` с отличающимися значениями `nums`, затем выведите, сколько в нём элементов

# --seed--

```javascript
let nums = [1, 2, 2, 3, 3, 3];
let unique = [...new Set([/])];
console.log(unique.[/]);
```

# --answers--

- nums
- [nums]
- ...nums
- length
- size
- count

# --solutions--

```javascript
let nums = [1, 2, 2, 3, 3, 3];
let unique = [...new Set(nums)];
console.log(unique.length);
```

# --output--

3
