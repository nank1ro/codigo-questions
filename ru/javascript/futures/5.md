---
language: javascript
exerciseType: 2
---

# --description--

`await` — не единственный способ прочитать промис. У каждого промиса есть метод **`.then(callback)`**: колбэк получает значение, как только промис выполнен.
```javascript
Promise.resolve(21).then((n) => {
  console.log(n);
  // prints 21
});
```
**`Promise.resolve(value)`** создаёт промис, который уже выполнен со значением `value`; это удобно, когда значение уже есть под рукой, но вернуть нужно промис.

`.then` возвращает **новый** промис, выполненный с тем, что вернул колбэк, поэтому вызовы можно **объединять в цепочку**, где каждый шаг работает с результатом предыдущего:
```javascript
Promise.resolve(21)
  .then((n) => n * 2)
  .then((n) => console.log(n));
// prints 42
```

# --instructions--

Допишите код так, чтобы `double` возвращала промис, уже выполненный со значением `n * 2`, а её результат читался с помощью колбэка, чтобы программа вывела `42`

# --seed--

```javascript
function double(n) {
  return Promise.[/](n * 2);
}

double(21).[/]((result) => {
  console.log(result);
});
```

# --answers--

- resolve
- then
- all
- catch
- await

# --solutions--

```javascript
function double(n) {
  return Promise.resolve(n * 2);
}

double(21).then((result) => {
  console.log(result);
});
```

# --output--

42
