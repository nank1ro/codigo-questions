---
language: javascript
exerciseType: 4
---

# --description--

Разница между **последовательным** и **параллельным** ожиданием решается тем, *где* вы поставите `await`:
```javascript
// sequential: about 300 + 300 = 600 ms
const a = await load("a");
const b = await load("b");

// parallel: about 300 ms
const [a, b] = await Promise.all([load("a"), load("b")]);
```
В первой версии вторая загрузка начинается только после завершения первой, потому что `await` приостанавливает функцию на этой строке. Во второй оба вызова сделаны до того, как что-либо ожидается, поэтому обе загрузки уже идут, пока `Promise.all` ждёт.

Используйте последовательные `await`, только когда вторая задача действительно нуждается в результате первой. Иначе запускайте всё сразу, а ожидайте вместе.

# --instructions--

Отсортируйте элементы в программу, которая определяет `later`, затем `async` функцию `main`, которая выводит `loading`, запускает оба вызова `later` вместе с помощью `Promise.all`, выводит два результата, соединённые пробелом, и в конце выводит `done`. Определите сначала `later`, затем `main`, и вызовите `main()` в последней строке

# --answers--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
```

```javascript
async function main() {
```

```javascript
  console.log("loading");
```

```javascript
  const results = await Promise.all([later("one", 30), later("two", 10)]);
```

```javascript
  console.log(results.join(" "));
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
async function main() {
  console.log("loading");
  const results = await Promise.all([later("one", 30), later("two", 10)]);
  console.log(results.join(" "));
  console.log("done");
}
main();
```

# --output--

loading
ready two
ready one
one two
done
