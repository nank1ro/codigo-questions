---
language: javascript
exerciseType: 4
---

# --description--

`async` функция читается сверху вниз, как любая другая функция: `await` просто приостанавливает её, пока ожидаемый промис не выполнится, а затем выполнение продолжается со следующей строки.
```javascript
async function main() {
  console.log("start");
  const value = await Promise.resolve("data");
  console.log(value);
  console.log("done");
}

main();
// prints start, then data, then done
```
Обратите внимание на последнюю строку: `async` функцию всё ещё нужно **вызвать**. Запись `main` без скобок определяет работу, но никогда её не запускает, и ничего не выводится.

# --instructions--

Отсортируйте элементы в программу, которая определяет `async` функцию `load`, возвращающую `data`, затем `async` функцию `main`, которая выводит `start`, ожидает `load()` в `value`, выводит `value` и в конце выводит `done`. Определите сначала `load`, затем `main`, и вызовите `main()` в последней строке

# --answers--

```javascript
async function load() {
  return "data";
}
```

```javascript
async function main() {
```

```javascript
  console.log("start");
```

```javascript
  const value = await load();
```

```javascript
  console.log(value);
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
async function load() {
  return "data";
}
async function main() {
  console.log("start");
  const value = await load();
  console.log(value);
  console.log("done");
}
main();
```

# --output--

start
data
done
