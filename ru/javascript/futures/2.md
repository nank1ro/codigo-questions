---
language: javascript
exerciseType: 1
---

# --description--

Добавление `async` перед функцией меняет то, что она отдаёт: тело по-прежнему вычисляет обычное значение, но вызывающий код получает промис, обёрнутый вокруг него.
```javascript
function shout(text) {
  return text.toUpperCase();
}
async function shoutLater(text) {
  return text.toUpperCase();
}

console.log(shout("hi"));
// prints HI
console.log(shoutLater("hi"));
// prints Promise { 'HI' }
```
Обе функции содержат одинаковый код; отличается только способ чтения результата. Чтобы `shoutLater("hi")` отдала `"HI"`, её нужно подождать через `await` внутри другой `async` функции.

Пометка функции `async` ничего не стоит, когда ждать нечего, и именно она позволяет использовать `await` внутри неё позже.

# --instructions--

Напишите `async` функцию `greet`, которая принимает строку `name` и возвращает `Hello, `, за которым следуют имя и восклицательный знак, например `Hello, Ada!`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
async function greet(name) {
  
}
```

# --asserts--

Функция `greet` должна быть объявлена с ключевым словом `async`.

```json
{
  "regex": "async\\s+function\\s+greet",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`await greet("Ada")` должно быть `"Hello, Ada!"`.

```javascript
(async () => {
  tryCatch((await greet("Ada")) === "Hello, Ada!");
})().catch(() => tryCatch(false));
```

`await greet("Bob")` должно быть `"Hello, Bob!"`.

```javascript
(async () => {
  tryCatch((await greet("Bob")) === "Hello, Bob!");
})().catch(() => tryCatch(false));
```

`await greet("")` должно быть `"Hello, !"`.

```javascript
(async () => {
  tryCatch((await greet("")) === "Hello, !");
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
async function greet(name) {
  return `Hello, ${name}!`;
}
```
