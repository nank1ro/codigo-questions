---
language: javascript
exerciseType: 1
---

# --description--

Стрелочные функции по-настоящему раскрываются как **колбэки**: функции, передаваемые в качестве аргументов другим функциям. Методы массивов — самый распространённый пример.
`map(callback)` возвращает новый массив с результатом колбэка для каждого элемента, а `filter(callback)` возвращает новый массив только с теми элементами, для которых колбэк возвращает `true`:
```javascript
const numbers = [1, 2, 3, 4];
console.log(numbers.map((n) => n * 10));
// prints [ 10, 20, 30, 40 ]
console.log(numbers.filter((n) => n > 2));
// prints [ 3, 4 ]
```
Оба возвращают новый массив и оставляют исходный нетронутым, поэтому их можно объединять в цепочки: `numbers.filter(...).map(...)`.

# --instructions--

Напишите функцию `evenSquares(numbers)`, которая возвращает новый массив с квадратами чётных чисел из `numbers`, в исходном порядке, объединяя `filter` и `map` в цепочку из стрелочных функций

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function evenSquares(numbers) {

}
```

# --asserts--

`evenSquares([1, 2, 3, 4])` должна вернуть `[4, 16]`.

```javascript
tryCatch(JSON.stringify(evenSquares([1, 2, 3, 4])) === "[4,16]");
```

`evenSquares([6, 5, 0])` должна вернуть `[36, 0]`.

```javascript
tryCatch(JSON.stringify(evenSquares([6, 5, 0])) === "[36,0]");
```

`evenSquares([1, 3, 5])` должна вернуть пустой массив.

```javascript
tryCatch(JSON.stringify(evenSquares([1, 3, 5])) === "[]");
```

Исходный массив не должен изменяться.

```javascript
tryCatch((() => { const input = [2, 3]; evenSquares(input); return JSON.stringify(input) === "[2,3]"; })());
```

Функция должна использовать `filter`, `map` и хотя бы одну стрелочную функцию.

```javascript
tryCatch(/\.filter\(/.test(evenSquares.toString()) && /\.map\(/.test(evenSquares.toString()) && /=>/.test(evenSquares.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function evenSquares(numbers) {
  return numbers.filter((n) => n % 2 === 0).map((n) => n * n);
}
```
