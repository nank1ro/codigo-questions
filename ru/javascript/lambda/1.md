---
language: javascript
exerciseType: 2
---

# --description--

Функции не обязательно иметь имя. **Функциональное выражение** создаёт функцию как значение, которое можно сохранить в переменную и вызывать через неё:
```javascript
const add = function (a, b) {
  return a + b;
};
console.log(add(2, 3));
// prints 5
```
**Стрелочная функция** — более короткий способ записать то же самое: уберите ключевое слово `function` и поставьте «жирную стрелку» `=>` между списком параметров и телом:
```javascript
const add = (a, b) => {
  return a + b;
};
console.log(add(2, 3));
// prints 5
```
Стрелочные функции обычно сохраняют в `const`, чтобы имя нельзя было случайно переназначить, и вызывают их точно так же, как любую другую функцию.

# --instructions--

Дополните стрелочную функцию `add` так, чтобы она возвращала сумму своих двух параметров, а затем выведите результат её вызова с `2` и `3`

# --seed--

```javascript
const add = (a, b) [/] {
  [/] a + b;
};
console.log([/](2, 3));
```

# --answers--

- =>
- return
- add
- ->
- function
- yield
- sum

# --solutions--

```javascript
const add = (a, b) => {
  return a + b;
};
console.log(add(2, 3));
```

# --output--

5
