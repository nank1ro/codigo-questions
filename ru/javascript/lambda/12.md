---
language: javascript
exerciseType: 4
---

# --instructions--

Упорядочьте строки так, чтобы определить `makeCounter`, которая возвращает стрелочную функцию, увеличивающую и возвращающую её приватный `count`, затем создайте счётчик `next` и выведите результат двух вызовов на одной строке

# --answers--

```javascript
const makeCounter = () => {
```

```javascript
  let count = 0;
```

```javascript
  return () => {
```

```javascript
    count += 1;
```

```javascript
    return count;
```

```javascript
  };
```

```javascript
};
```

```javascript
const next = makeCounter();
```

```javascript
console.log(next(), next());
```

# --solutions--

```javascript
const makeCounter = () => {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
};
const next = makeCounter();
console.log(next(), next());
```

# --output--

1 2
