---
language: javascript
exerciseType: 2
---

# --description--

Оборачивание литерала объекта в круглые скобки — стандартный способ создавать объекты однострочной стрелочной функцией, например когда вы превращаете пару значений в запись:
```javascript
const user = (name, age) => ({ name: name, age: age });
console.log(user("Ana", 30).age);
// prints 30
```
Стрелочная функция без параметров начинается с пустой пары круглых скобок `()`:
```javascript
const empty = () => ({});
console.log(empty());
// prints {}
```

# --instructions--

Дополните `point` так, чтобы она возвращала объект со свойствами `x` и `y`, а `origin` — так, чтобы она не принимала параметров, затем выведите `point(3, 4).y` и `origin().x`

# --seed--

```javascript
const point = (x, y) => [/]{ x: x, y: y }[/];
const origin = [/] => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --answers--

- (
- )
- ()
- [
- ]
- function
- this

# --solutions--

```javascript
const point = (x, y) => ({ x: x, y: y });
const origin = () => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --output--

4
0
