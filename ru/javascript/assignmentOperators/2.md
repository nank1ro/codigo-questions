---
language: javascript
exerciseType: 2
---

# --description--

У нас уже есть инициализированная переменная `total`
```javascript
var total = 5;
```
Допустим, мы хотим добавить число `2` к переменной `total`, мы можем написать
```javascript
total = total + 2;
```
Хорошо, это работает! Но есть более короткая запись для того же действия:
```javascript
total += 2;
```
Знак `+=` называется **присваивание сложения**.
Он добавляет значение к значению переменной и присваивает результат этой переменной.

# --instructions--

Добавьте к переменной `num` значение `15`, используя оператор *присваивания сложения*, затем выведите её значение

# --seed--

```javascript
var num = 75;
[/][/][/];
console.log(num);
```

# --answers--

- +
- +=
- =+
- num 
-  15

# --solutions--

```javascript
var num = 75;
num += 15;
console.log(num);
```

# --output--

90
