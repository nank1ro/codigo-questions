---
language: javascript
exerciseType: 2
---

# --description--

Рассмотрим оператор **присваивания остатка** `%=`.
Он вычисляет остаток от деления переменной на значение и присваивает результат этой переменной.
Таким образом, следующие записи абсолютно одинаковы
```javascript
num = num % 5;
// равносильно
num %= 5;
```

# --instructions--

Вычислите остаток от деления `result` на значение `3`, используя оператор *присваивания остатка*, затем выведите его значение

# --seed--

```javascript
var result = 5;
result [/][/];
console.log(result);
```

# --answers--

- % 
- =% 
- %= 
- 3

# --solutions--

```javascript
var result = 5;
result %= 3;
console.log(result);
```

# --output--

2
