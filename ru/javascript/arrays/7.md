---
language: javascript
exerciseType: 2
---

# --description--

В JavaScript мы можем делать срезы массива как угодно!
```javascript
// Берём первые два элемента
listName.slice(0, 2);
// Берём элементы с четвёртого до последнего
listName.slice(3);
```
Если срез массива включает самый первый или последний элемент, индекс для этого элемента можно не указывать

# --instructions--

Разделите массив `animals` ровно пополам на две группы

# --seed--

```javascript
let animals = ["dog", "cat", "frog", "bear", "lion", "butterfly"];
let firstGroup = [/].[/]([/], [/]);
let secondGroup = [/].slice([/]);
console.log(firstGroup);
console.log(secondGroup);
```

# --answers--

- 0
- 3
- 4
- 1
- 3
- ..<
- ...
- animals
- animals
- slice

# --solutions--

```javascript
let animals = ["dog", "cat", "frog", "bear", "lion", "butterfly"];
let firstGroup = animals.slice(0, 3);
let secondGroup = animals.slice(3);
console.log(firstGroup);
console.log(secondGroup);
```

# --output--

["dog", "cat", "frog"]
["bear", "lion", "butterfly"]
