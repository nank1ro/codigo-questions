---
language: javascript
exerciseType: 2
---

# --description--

Как и строки, массивы имеют **длину**.
Длина массива — это количество элементов, которые он содержит

# --instructions--

Дополните код, чтобы вывести длину массива

# --seed--

```javascript
var numbers = [5, 6, 8, 4];
console.log([/][/][/]);
```

# --answers--

- len
- length
- numbers
- count
- .

# --solutions--

```javascript
var numbers = [5, 6, 8, 4];
console.log(numbers.length);
```

# --output--

4
