---
language: c
exerciseType: 4
---

# --description--

У операторов есть **приоритет**, определяющий, что вычисляется первым:
1. сначала применяется `!`
2. затем реляционные сравнения `<`, `>`, `<=`, `>=`
3. затем сравнения на равенство `==`, `!=`
4. затем `&&`
5. затем `||`

Поэтому `a > 0 && a < 10` не нуждается в скобках: оба сравнения вычисляются до `&&`.
А `x == 1 || y == 2 && z == 3` означает `x == 1 || (y == 2 && z == 3)`, потому что `&&` имеет более высокий приоритет, чем `||`.

# --instructions--

Расположите строки в правильном порядке, чтобы сохранить в `inside`, находится ли `a` между `1` и `9`, сохранить его отрицание в `outside` и вывести оба значения через пробел

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int a = 5;
```

```c
    int inside = a > 0 && a < 10;
```

```c
    int outside = !inside;
```

```c
    printf("%d %d\n", inside, outside);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int a = 5;
    int inside = a > 0 && a < 10;
    int outside = !inside;
    printf("%d %d\n", inside, outside);
    return 0;
}
```

# --output--

1 0
