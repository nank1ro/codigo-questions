---
language: c
exerciseType: 2
---

# --description--

Цикл `while` продолжает выполняться, пока его условие не равно нулю, поэтому именно сравнение решает, когда он остановится:
```c
int i = 0;
while (i < 3) {
    printf("%d\n", i);
    i++;
}
// выводит 0, 1, 2
```
Выбор между `<` и `<=` определяет, будет ли включено последнее значение.

# --instructions--

Дополните код так, чтобы цикл выводил числа от `1` до `3`, по одному на строке

# --seed--

```c
int count = [/];
while (count [/] 3) {
    printf("%d\n", count);
    count++;
}
```

# --answers--

- 1
- <=
- 0
- <
- !=
- >

# --solutions--

```c
int count = 1;
while (count <= 3) {
    printf("%d\n", count);
    count++;
}
```

# --output--

1
2
3
