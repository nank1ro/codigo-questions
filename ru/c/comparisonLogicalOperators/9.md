---
language: c
exerciseType: 4
---

# --description--

Вычисление по короткой схеме также пропускает вызовы функций: в `0 && check()` функция `check` никогда не вызывается, поэтому любой её побочный эффект (например, обновление счётчика) не происходит.

# --instructions--

Расположите строки в правильном порядке, чтобы определить счётчик `calls`, функцию `check`, которая увеличивает его и возвращает `1`, и `main`, которая вычисляет `0 && check()` и выводит результат, а затем `calls`. Определите функцию перед `main`

# --answers--

```c
#include <stdio.h>
int calls = 0;
```

```c
int check() {
```

```c
    calls++;
    return 1;
}
```

```c
int main() {
```

```c
    int result = 0 && check();
```

```c
    printf("%d %d\n", result, calls);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int calls = 0;
int check() {
    calls++;
    return 1;
}
int main() {
    int result = 0 && check();
    printf("%d %d\n", result, calls);
    return 0;
}
```

# --output--

0 0
