---
language: c
exerciseType: 2
---

# --description--

Оператор **или** `||` даёт `1`, если хотя бы одна сторона истинна, и `0`, только если обе стороны ложны:
```c
int day = 1;
printf("%d\n", day == 1 || day == 7); // выводит "1"
```
Каждая сторона должна быть полным сравнением: `day == 6 || 7` не означает "6 или 7" (вы поймёте почему позже).

# --instructions--

Дополните код так, чтобы `weekend` было `1`, когда `day` равно `6` или `day` равно `7`, а затем выведите его

# --seed--

```c
int day = 7;
int weekend = day == 6 [/] day == [/];
printf("%d\n", weekend);
```

# --answers--

- ||
- 7
- &&
- 6
- =

# --solutions--

```c
int day = 7;
int weekend = day == 6 || day == 7;
printf("%d\n", weekend);
```

# --output--

1
