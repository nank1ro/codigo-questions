---
language: c
exerciseType: 4
---

# --description--

Параметр типа `bool` можно использовать напрямую как операнд `&&` или `||`, не сравнивая его с `true`: пишите `age >= 18 && citizen`, а не `citizen == true`.

# --instructions--

Расположите строки в правильном порядке, чтобы определить `can_vote`, которая возвращает `true`, если `age` не меньше `18` и `citizen` равно `true`, и `main`, которая выводит результат для `20, true`, а затем для `20, false`. Определите функцию перед `main`

# --answers--

```c
#include <stdio.h>
#include <stdbool.h>
```

```c
bool can_vote(int age, bool citizen) {
```

```c
    return age >= 18 && citizen;
}
```

```c
int main() {
```

```c
    printf("%d\n", can_vote(20, true));
```

```c
    printf("%d\n", can_vote(20, false));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
#include <stdbool.h>
bool can_vote(int age, bool citizen) {
    return age >= 18 && citizen;
}
int main() {
    printf("%d\n", can_vote(20, true));
    printf("%d\n", can_vote(20, false));
    return 0;
}
```

# --output--

1
0
