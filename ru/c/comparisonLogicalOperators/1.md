---
language: c
exerciseType: 2
---

# --description--

**Операторы сравнения** сравнивают два значения и дают результат: `1`, если сравнение верно, и `0`, если нет.
Оператор **равенства** `==` проверяет, совпадают ли два значения, оператор **неравенства** `!=` проверяет, отличаются ли они:
```c
int a = 7, b = 3;
printf("%d\n", a == b);
// выводит "0"
printf("%d\n", a != b);
// выводит "1"
```
Будьте внимательны: `==` (два знака) сравнивает, а одиночный `=` присваивает значение.

# --instructions--

Дополните код, чтобы вывести, равно ли `a` значению `b`, а затем **не** равно ли `a` значению `b`

# --seed--

```c
int a = 7, b = 3;
printf("%d\n", a [/] b);
printf("%d\n", a [/] b);
```

# --answers--

- ==
- !=
- =
- !

# --solutions--

```c
int a = 7, b = 3;
printf("%d\n", a == b);
printf("%d\n", a != b);
```

# --output--

0
1
