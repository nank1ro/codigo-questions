---
language: c
exerciseType: 2
---

# --description--

У операторов есть **приоритет**, определяющий, что вычисляется первым:
1. сначала применяется `!`
2. затем реляционные сравнения `<`, `>`, `<=`, `>=`
3. затем сравнения на равенство `==`, `!=`
4. затем `&&`
5. затем `||`

Поэтому `a > 0 && a < 10` не нуждается в скобках, а `a && b || c` означает `(a && b) || c`, потому что `&&` связывает сильнее, чем `||`. Используйте скобки, чтобы задать другую группировку или просто сделать намерение читаемым.

# --instructions--

Дополните код так, чтобы `without` вычисляло `a && b || c` с приоритетом по умолчанию, а `with` заставляло сначала вычислить `b || c`, затем выведите оба значения

# --seed--

```c
int a = 0, b = 1, c = 1;
int without = a && b [/] c;
int with = a && [/]b || c[/];
printf("%d %d\n", without, with);
```

# --answers--

- ||
- (
- )
- &&
- !

# --solutions--

```c
int a = 0, b = 1, c = 1;
int without = a && b || c;
int with = a && (b || c);
printf("%d %d\n", without, with);
```

# --output--

1 0
