---
language: c
exerciseType: 4
---

# --instructions--

Расположите строки кода так, чтобы программа сравнила `password` с `attempt` и вывела `Access granted`. Объявите `password` перед `attempt` и поместите каждое сообщение внутрь своей ветки

# --answers--

```c
char password[] = "secret";
```

```c
char attempt[] = "secret";
```

```c
if (strcmp(password, attempt) == 0) {
```

```c
    printf("Access granted\n");
```

```c
} else {
```

```c
    printf("Access denied\n");
```

```c
}
```

# --solutions--

```c
char password[] = "secret";
char attempt[] = "secret";
if (strcmp(password, attempt) == 0) {
    printf("Access granted\n");
} else {
    printf("Access denied\n");
}
```

# --output--

Access granted
