---
language: c
exerciseType: 2
---

# --description--

Писать `enum Color` каждый раз многословно. С помощью `typedef` вы даёте перечислению короткое имя типа, а само перечисление может остаться анонимным:
```c
typedef enum { RED, GREEN, BLUE } Color;

Color favorite = BLUE;
```
Новое имя `Color` используется само по себе, без ключевого слова `enum` перед ним.

# --instructions--

Дополните код, чтобы определить анонимное перечисление с константами `OFF` и `ON` под именем типа `State`, а затем объявить `lamp` этого типа

# --seed--

```c
typedef [/] { OFF, ON } State;
[/] lamp = ON;
printf("%d\n", lamp);
```

# --answers--

- enum
- struct
- State
- enum State
- typedef

# --solutions--

```c
typedef enum { OFF, ON } State;
State lamp = ON;
printf("%d\n", lamp);
```

# --output--

1
