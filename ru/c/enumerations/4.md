---
language: c
exerciseType: 2
---

# --description--

Перечисление — это ещё и тип: вы можете объявить переменную этого типа, написав `enum`, а затем имя перечисления, и присвоить ей одну из его констант:
```c
enum Color favorite = GREEN;
```
Поскольку константы — целые числа, переменные enum сравниваются обычными операторами:
```c
if (favorite == GREEN) {
    printf("Green it is\n");
}
```

# --instructions--

Дополните код, чтобы объявить переменную `light` типа `enum Light` со значением `GREEN`, чтобы программа вывела `Go`

# --seed--

```c
enum Light { RED, YELLOW, GREEN };
[/] Light light = [/];
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --answers--

- enum
- struct
- GREEN
- RED
- YELLOW
- "GREEN"

# --solutions--

```c
enum Light { RED, YELLOW, GREEN };
enum Light light = GREEN;
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --output--

Go
