---
language: c
exerciseType: 4
---

# --description--

В C нет встроенного способа получить имя константы перечисления: `printf("%d\n", SUMMER)` выводит `2`, а не `Summer`. Обычное решение — небольшая функция со `switch`, которая возвращает соответствующую строку для каждой константы.

# --instructions--

Расположите строки в правильном порядке, чтобы определить тип `Season`, функцию `season_name`, возвращающую название сезона, и `main`, которая выводит название `SUMMER`. Определите функцию перед `main`, сохраните порядок case таким же, как порядок констант, и поставьте `default` последним

# --answers--

```c
#include <stdio.h>
typedef enum { WINTER, SPRING, SUMMER, AUTUMN } Season;
```

```c
char *season_name(Season season) {
```

```c
    switch (season) {
```

```c
        case WINTER: return "Winter";
```

```c
        case SPRING: return "Spring";
```

```c
        case SUMMER: return "Summer";
```

```c
        default: return "Autumn";
```

```c
    }
}
```

```c
int main() {
```

```c
    printf("%s\n", season_name(SUMMER));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef enum { WINTER, SPRING, SUMMER, AUTUMN } Season;
char *season_name(Season season) {
    switch (season) {
        case WINTER: return "Winter";
        case SPRING: return "Spring";
        case SUMMER: return "Summer";
        default: return "Autumn";
    }
}
int main() {
    printf("%s\n", season_name(SUMMER));
    return 0;
}
```

# --output--

Summer
