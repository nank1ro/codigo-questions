---
language: c
exerciseType: 4
---

# --description--

Когда каждый case в `switch` обрабатывает одну константу, не забывайте про `break` после каждого из них, иначе выполнение провалится к следующему case.
Case `default` не обязателен, если вы охватили все константы перечисления.

# --instructions--

Расположите строки в правильном порядке, чтобы объявить `enum Size`, сохранить `MEDIUM` в `size` и вывести `Medium` с помощью `switch`. Сохраните порядок case таким же, как порядок констант в объявлении

# --answers--

```c
enum Size { SMALL, MEDIUM, LARGE };
```

```c
enum Size size = MEDIUM;
```

```c
switch (size) {
```

```c
    case SMALL: printf("Small\n"); break;
```

```c
    case MEDIUM: printf("Medium\n"); break;
```

```c
    case LARGE: printf("Large\n"); break;
```

```c
}
```

# --solutions--

```c
enum Size { SMALL, MEDIUM, LARGE };
enum Size size = MEDIUM;
switch (size) {
    case SMALL: printf("Small\n"); break;
    case MEDIUM: printf("Medium\n"); break;
    case LARGE: printf("Large\n"); break;
}
```

# --output--

Medium
