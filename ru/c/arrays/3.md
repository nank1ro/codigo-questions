---
language: c
exerciseType: 2
---

# --description--

Индекс массива ведёт себя как любое другое имя переменной! Его можно использовать как для доступа, так и для присвоения значений.
Вы видели, как получить доступ к элементу массива по индексу:
```c
int numbers[4] = {5, 6, 7, 8};
numbers[0]; // Gets the value 5
```
Вот как работает присвоение:
```c
int numbers[4] = {5, 6, 7, 8};
numbers[0] = 1;
printf("%d\n", numbers[0]); // prints the new value 1
```

# --instructions--

Дополните код, чтобы заменить элемент, который в данный момент содержит значение `5`, новым значением `10`

# --seed--

```c
int numbers[/] = {10, 4, 6, 5};

numbers[/] = 10;

for (int i = 0; i < 4; i++) {
    printf("%d\n", [/][i]);
}
```

# --answers--

- [0]
- [1]
- [2]
- [3]
- [4]
- numbers

# --solutions--

```c
int numbers[4] = {10, 4, 6, 5};

numbers[3] = 10;

for (int i = 0; i < 4; i++) {
    printf("%d\n", numbers[i]);
}
```

# --output--

10
4
6
10
