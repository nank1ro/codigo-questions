---
language: c
exerciseType: 4
---

# --description--

Разворот списка поворачивает каждый указатель `next` в обратную сторону на месте с помощью трёх указателей: `prev` — уже развёрнутая часть, `head` — обрабатываемый узел, а `next` — копия остатка списка, сохранённая до изменения связи. На каждом шаге текущий узел направляется обратно на `prev`, затем и `prev`, и `head` продвигаются вперёд:
```c
Node *next = head->next;
head->next = prev;
prev = head;
head = next;
```
Когда `head` достигает `NULL`, все связи перевёрнуты, и `prev` — новая голова.

# --instructions--

Отсортируйте строки так, чтобы определить `reverse`, которая разворачивает список на месте и возвращает его новую голову, и `main`, который выводит развёрнутый список `1, 2, 3`. Определите функцию до `main`

# --answers--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
```

```c
Node *reverse(Node *head) {
    Node *prev = NULL;
```

```c
    while (head != NULL) {
        Node *next = head->next;
```

```c
        head->next = prev;
```

```c
        prev = head;
```

```c
        head = next;
    }
```

```c
    return prev;
}
```

```c
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
```

```c
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
Node *reverse(Node *head) {
    Node *prev = NULL;
    while (head != NULL) {
        Node *next = head->next;
        head->next = prev;
        prev = head;
        head = next;
    }
    return prev;
}
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --output--

3
2
1
