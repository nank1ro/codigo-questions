---
language: c
exerciseType: 3
---

# --instructions--

Почему `true` не является строкой?
```c
bool a = true;
```

# --answers--

- Оно хранится внутри переменной
- Вокруг него нет кавычек, и тип не является char
- Оно начинается с заглавной буквы

# --solutions--

- Вокруг него нет кавычек, и тип не является char

