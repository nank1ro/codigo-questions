---
language: c
exerciseType: 2
---

# --description--

В C мы используем функцию `printf` с выражением для отображения двух или более значений вместе, например:
```c
>>> char c = 'C';
>>> printf("Hello %c!\n", c);
Hello C!
```
`%c` используется для отображения одного символа

# --instructions--

Выведите имя, используя выражение `%s` (используется для отображения _массива символов_ или _строки_)

# --seed--

```c
char c[] = "Robot";
printf("Mr [/]\n", [/]);
```

# --answers--

- +
- f
- -
- =
- %s
- c

# --solutions--

```c
char c[] = "Robot";
printf("Mr %s\n", c);
```

# --output--

Mr. Robot
