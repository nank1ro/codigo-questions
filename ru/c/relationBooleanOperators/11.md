---
language: c
exerciseType: 2
---

# --instructions--

Проверьте, **НЕ** равно ли `word` строке `dog`, выведя булев результат.
Имейте в виду, что `strcmp` возвращает `0` только когда строки **равны**, поэтому не путайте возвращаемый 0 с булевым значением false

# --seed--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat")[/][/];
printf("%d\n", [/]);
```

# --answers--

- word
- "dog"
-  == 
-  = 
- dog
-  != 
- 0
- are_words_equal

# --solutions--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat") == 0;
printf("%d\n", are_words_equal);
```

# --output--

1
