---
language: c
exerciseType: 4
---

# --description--

Указатель на `char` работает как любой другой указатель: он содержит адрес одного символа, а `*p` читает или записывает этот символ:
```c
char grade = 'B';
char *p = &grade;
*p = 'A';
printf("%c\n", grade); // prints "A"
```
Чтение через указатель и запись через него можно свободно смешивать: `*p = *p + 1` превращает `'A'` в `'B'`.

# --instructions--

Расставьте строки по порядку так, чтобы объявить `letter` со значением `'a'`, направить на неё `p`, заменить букву через указатель на `'b'` и вывести `letter`

# --answers--

```c
char letter = 'a';
```

```c
char *p = &letter;
```

```c
*p = 'b';
```

```c
printf("%c\n", letter);
```


# --solutions--

```c
char letter = 'a';
char *p = &letter;
*p = 'b';
printf("%c\n", letter);
```

# --output--

b
