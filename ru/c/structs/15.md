---
language: c
exerciseType: 4
---

# --description--

У `printf` нет спецификатора для целой структуры. Обычное решение — небольшая функция, которая выводит члены в фиксированном формате, чтобы каждая часть программы показывала значение одинаково:
```c
void print_person(Person p) {
    printf("%s (%d)\n", p.name, p.age);
}
```

# --instructions--

Расставьте строки по порядку так, чтобы определить тип `Point`, функцию `print_point`, которая выводит точку как `(x, y)`, и `main`, который выводит точку `(3, 4)`. Определите функцию до `main`

# --answers--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
```

```c
void print_point(Point p) {
```

```c
    printf("(%d, %d)\n", p.x, p.y);
```

```c
}
```

```c
int main() {
```

```c
    Point p = {3, 4};
```

```c
    print_point(p);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
void print_point(Point p) {
    printf("(%d, %d)\n", p.x, p.y);
}
int main() {
    Point p = {3, 4};
    print_point(p);
    return 0;
}
```

# --output--

(3, 4)
