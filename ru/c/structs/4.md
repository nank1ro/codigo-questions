---
language: c
exerciseType: 2
---

# --description--

Писать `struct Point` каждый раз громоздко. С помощью `typedef` вы даёте структуре короткое имя типа, а сама структура может оставаться анонимной:
```c
typedef struct {
    double celsius;
} Temperature;

Temperature t = {21.5};
```
Новое имя `Temperature` используется само по себе, без ключевого слова `struct` перед ним. Это самый распространённый способ объявлять структуры в реальных программах.

# --instructions--

Дополните код так, чтобы определить анонимную структуру с членом `celsius` под именем типа `Temperature`, а затем объявить `t` с этим типом

# --seed--

```c
typedef [/] {
    double celsius;
} Temperature;
[/] t = {21.5};
printf("%.1f\n", t.celsius);
```

# --answers--

- struct
- enum
- typedef
- Temperature
- struct Temperature
- Temperature t

# --solutions--

```c
typedef struct {
    double celsius;
} Temperature;
Temperature t = {21.5};
printf("%.1f\n", t.celsius);
```

# --output--

21.5
