---
language: c
exerciseType: 2
---

# --description--

Через указатель члены доступны с помощью оператора **стрелка** `->` вместо точки:
```c
void grow(Box *b) {
    b->size = b->size * 2;
}
```
`b->size` — это сокращение для `(*b).size`: сначала перейти по указателю, затем взять член. Точка работает только со структурой, стрелка — только с указателем на структуру.
Вызывающий код передаёт адрес своей переменной с помощью `&`, и изменение, сделанное через указатель, видно после вызова.

# --instructions--

Дополните `grow` так, чтобы она удваивала `size` через указатель, и дополните вызов так, чтобы он передавал адрес `box`

# --seed--

```c
#include <stdio.h>

typedef struct { int size; } Box;

void grow(Box *b) {
    b[/]size = b->size * 2;
}

int main() {
    Box box = {5};
    grow([/]box);
    printf("%d\n", box.size);
    return 0;
}
```

# --answers--

- ->
- .
- &
- *
- (*b)

# --solutions--

```c
#include <stdio.h>

typedef struct { int size; } Box;

void grow(Box *b) {
    b->size = b->size * 2;
}

int main() {
    Box box = {5};
    grow(&box);
    printf("%d\n", box.size);
    return 0;
}
```

# --output--

10
