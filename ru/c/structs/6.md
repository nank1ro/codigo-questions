---
language: c
exerciseType: 4
---

# --description--

Структуры можно хранить в массиве, как и любой другой тип. Каждый элемент инициализируется своими фигурными скобками, а цикл обходит их по одному: сначала обращение по индексу, затем оператор точка:
```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};

for (int i = 0; i < 3; i++) {
    printf("%d\n", path[i].y);
}
```

# --instructions--

Расставьте строки по порядку так, чтобы определить тип `Point`, сохранить три точки в `path`, сложить их члены `x` в `sum` и вывести результат. Объявите `path` до `sum`

# --answers--

```c
typedef struct { int x; int y; } Point;
```

```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
```

```c
int sum = 0;
```

```c
for (int i = 0; i < 3; i++) {
```

```c
    sum += path[i].x;
```

```c
}
```

```c
printf("%d\n", sum);
```

# --solutions--

```c
typedef struct { int x; int y; } Point;
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
int sum = 0;
for (int i = 0; i < 3; i++) {
    sum += path[i].x;
}
printf("%d\n", sum);
```

# --output--

6
