---
language: c
exerciseType: 2
---

# --description--

Структура часто хранит текст, который содержится в члене — массиве `char` фиксированного размера:
```c
typedef struct {
    char name[20];
    int age;
} Person;
```
Члену-массиву нельзя присвоить значение с помощью `=` после объявления: `p.name = "Ann"` не компилируется. Скопируйте текст в него с помощью `strcpy` из `string.h`, передав член как приёмник:
```c
Person p;
strcpy(p.name, "Ann");
p.age = 30;
```
Только инициализатор в фигурных скобках при объявлении принимает строку напрямую: `Person p = {"Ann", 30};`.

# --instructions--

Дополните код так, чтобы заменить имя `p` на `"Ann"`, а затем вывести имя и возраст

# --seed--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
[/](p.name, "Ann");
printf("%s is %d\n", p[/]name, p.age);
```

# --answers--

- strcpy
- strcat
- strcmp
- strlen
- .
- ->

# --solutions--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
strcpy(p.name, "Ann");
printf("%s is %d\n", p.name, p.age);
```

# --output--

Ann is 30
