---
language: c
exerciseType: 4
---

# --description--

У каждого бинарного побитового оператора есть форма **составного присваивания**, которая обновляет переменную на месте: `&=`, `|=`, `^=`, `<<=` и `>>=`.
```c
unsigned int x = 12;
x &= 10;  // same as x = x & 10;
x |= 1;   // same as x = x | 1;
x ^= 3;   // same as x = x ^ 3;
x <<= 1;  // same as x = x << 1;
x >>= 2;  // same as x = x >> 2;
```
Они читаются лучше, чем повторение имени переменной, и являются обычным способом изменить биты переменной-флага.

# --instructions--

Отсортируйте строки так, чтобы `flags` сначала был равен `0x01`, затем с помощью `|=` был включён бит со значением `4`, после чего значение было сдвинуто на 2 разряда влево с помощью `<<=`, и наконец выведено

# --answers--

```c
unsigned int flags = 0x01;
```

```c
flags |= 0x04;
```

```c
flags <<= 2;
```

```c
printf("%u\n", flags);
```

# --solutions--

```c
unsigned int flags = 0x01;
flags |= 0x04;
flags <<= 2;
printf("%u\n", flags);
```

# --output--

20
