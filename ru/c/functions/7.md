---
language: c
exerciseType: 3
---

# --instructions--

Какая __проблема__ в этом фрагменте кода?
```c
greet("Daniel");
void greet(char *name) {
    printf("Hello %s!\n", name);
};
```

# --answers--

- Нельзя использовать greet() до её определения
- Нельзя использовать greet() дважды
- greet() ничего не возвращает
- Нельзя использовать name в качестве параметра

# --solutions--

- Нельзя использовать greet() до её определения

