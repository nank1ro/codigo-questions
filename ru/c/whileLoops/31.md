---
language: c
exerciseType: 3
---

# --instructions--

Что делает `counter += 1`?
```c
int counter = 1;

while (counter < 5) {
    printf("%d\n", counter);
    counter += 1;
}
```

# --answers--

- Увеличивает counter на единицу при каждом выполнении блока кода цикла
- Создаёт переменную counter
- Увеличивает counter на два каждый раз
- Увеличивает counter на три каждый раз

# --solutions--

- Увеличивает counter на единицу при каждом выполнении блока кода цикла

