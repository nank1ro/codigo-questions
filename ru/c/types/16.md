---
language: c
exerciseType: 4
---

# --description--

Соберём всё вместе: выбирайте тип исходя из вида значения, сопоставляйте каждый спецификатор `printf` с типом его аргумента и приводите тип, когда вычисление должно происходить в типе, отличном от типа его операндов.

# --instructions--

Расставьте строки по порядку так, чтобы получилась программа, которая сохраняет `7` в `total` и `2` в `count`, вычисляет `total`, делённое на `count`, как `double` в `avg`, и выводит целочисленное деление `total / count`, а затем `avg` с одним знаком после запятой

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int total = 7, count = 2;
```

```c
    double avg = (double) total / count;
```

```c
    printf("%d %.1f\n", total / count, avg);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int total = 7, count = 2;
    double avg = (double) total / count;
    printf("%d %.1f\n", total / count, avg);
    return 0;
}
```

# --output--

3 3.5
