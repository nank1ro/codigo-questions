---
language: dart
exerciseType: 2
---

# --description--

O **operador maior ou igual a** `>=` retorna `true` quando o valor à esquerda é maior que **ou igual a** o valor à direita.

```dart
int score = 50;
bool passed = score >= 50; // true
```

Ao contrário de `>`, este operador também retorna `true` quando ambos os valores são iguais.

# --instructions--

Complete o código para verificar se `7` é maior ou igual a `7` e armazene o resultado em `isAtLeastSeven`.

# --seed--

```dart
void main() {
  bool isAtLeastSeven = 7 [/] 7;
  print(isAtLeastSeven);
}
```

# --answers--

- >=
- >
- <=
- ==

# --solutions--

```dart
void main() {
  bool isAtLeastSeven = 7 >= 7;
  print(isAtLeastSeven);
}
```

# --output--

true
