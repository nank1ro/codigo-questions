---
language: dart
exerciseType: 1
---

# --instructions--

Declare três variáveis `int` de nível superior: `x` com o valor `5`, `y` com o valor `3` e `z` com o valor `0`. Depois declare uma variável `bool` de nível superior chamada `result` que avalia `!(x > 10) || (y < 5 && z == 0)`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` deve ser `true` e do tipo `bool`

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 5;
int y = 3;
int z = 0;
bool result = !(x > 10) || (y < 5 && z == 0);

void main() {
  print(result);
}
```
