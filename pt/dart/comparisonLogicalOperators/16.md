---
language: dart
exerciseType: 1
---

# --instructions--

O **operador NOT** `!` inverte um valor booleano: `!true` torna-se `false` e `!false` torna-se `true`.

Declareum variável `bool` de nível superior chamada `isOffline` com o valor `false`. Depois declare uma variável `bool` de nível superior chamada `isOnline` que armazena a negação de `isOffline` usando o operador `!`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isOnline` deve ser `true` e do tipo `bool`

```dart
  test('test1', () {
    expect(isOnline, true, reason: '--err-t1--');
    expect(isOnline, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isOffline = false;
bool isOnline = !isOffline;

void main() {
  print(isOnline);
}
```
