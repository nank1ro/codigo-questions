---
language: dart
exerciseType: 1
---

# --description--

O **operador AND** `&&` combina duas expressões booleanas. Retorna `true` apenas quando **ambos** os lados são `true`. Se um dos lados for `false`, o resultado é `false`.

```dart
bool a = true;
bool b = true;
bool result = a && b; // true

bool c = true;
bool d = false;
bool result2 = c && d; // false
```

# --instructions--

Declare duas variáveis `int` de nível superior: `age` com o valor `25` e `score` com o valor `80`. Depois declare uma variável `bool` de nível superior chamada `qualifies` que é `true` apenas quando `age` é maior ou igual a `18` E `score` é maior ou igual a `60`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`qualifies` deve ser `true` e do tipo `bool`

```dart
  test('test1', () {
    expect(qualifies, true, reason: '--err-t1--');
    expect(qualifies, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int age = 25;
int score = 80;
bool qualifies = age >= 18 && score >= 60;

void main() {
  print(qualifies);
}
```
