---
language: dart
exerciseType: 2
---

# --description--

O **operador maior que** `>` retorna `true` quando o valor à esquerda é estritamente maior que o valor à direita.

```dart
int a = 8;
int b = 3;
bool result = a > b; // true
```

Se o valor à esquerda for igual ou menor que o da direita, retorna `false`.

# --instructions--

Complete o código para verificar se `15` é maior que `6` e armazene o resultado em `isGreater`.

# --seed--

```dart
void main() {
  bool isGreater = 15 [/] 6;
  print(isGreater);
}
```

# --answers--

- >
- <
- >=
- ==

# --solutions--

```dart
void main() {
  bool isGreater = 15 > 6;
  print(isGreater);
}
```

# --output--

true
