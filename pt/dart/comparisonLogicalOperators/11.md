---
language: dart
exerciseType: 1
---

# --instructions--

Declare duas variáveis `int` de nível superior: `a` com o valor `5` e `b` com o valor `9`. Depois declare uma variável `bool` de nível superior chamada `notEqual` que armazena o resultado da comparação de `a` e `b` com o operador `!=`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`notEqual` deve ser `true` e do tipo `bool`

```dart
  test('test1', () {
    expect(notEqual, true, reason: '--err-t1--');
    expect(notEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 5;
int b = 9;
bool notEqual = a != b;

void main() {
  print(notEqual);
}
```
