---
language: dart
exerciseType: 1
---

# --instructions--

Declare três variáveis `int` de nível superior: `number` com o valor `42`, `min` com o valor `1` e `max` com o valor `100`. Depois declare uma variável `bool` de nível superior chamada `inRange` que é `true` quando `number` é maior ou igual a `min` E menor ou igual a `max`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`inRange` deve ser `true` e do tipo `bool`

```dart
  test('test1', () {
    expect(inRange, true, reason: '--err-t1--');
    expect(inRange, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int number = 42;
int min = 1;
int max = 100;
bool inRange = number >= min && number <= max;

void main() {
  print(inRange);
}
```
