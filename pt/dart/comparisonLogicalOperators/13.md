---
language: dart
exerciseType: 1
---

# --instructions--

Declare duas variáveis `int` de nível superior: `price` com o valor `30` e `budget` com o valor `50`. Depois declare uma variável `bool` de nível superior chamada `canAfford` que armazena o resultado da verificação se `price` é menor que `budget` usando o operador `<`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canAfford` deve ser `true` e do tipo `bool`

```dart
  test('test1', () {
    expect(canAfford, true, reason: '--err-t1--');
    expect(canAfford, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int price = 30;
int budget = 50;
bool canAfford = price < budget;

void main() {
  print(canAfford);
}
```
