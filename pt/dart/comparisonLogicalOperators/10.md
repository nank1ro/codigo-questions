---
language: dart
exerciseType: 1
---

# --instructions--

Declare duas variáveis `int` de nível superior: `x` com o valor `8` e `y` com o valor `8`. Depois declare uma variável `bool` de nível superior chamada `areEqual` que armazena o resultado da comparação de `x` e `y` com o operador `==`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`areEqual` deve ser `true` e do tipo `bool`

```dart
  test('test1', () {
    expect(areEqual, true, reason: '--err-t1--');
    expect(areEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 8;
int y = 8;
bool areEqual = x == y;

void main() {
  print(areEqual);
}
```
