---
language: dart
exerciseType: 1
---

# --instructions--

Escreva uma função `minOf(int a, int b)` que retorna o menor de dois números usando o operador ternário.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`minOf(3, 7)` deve retornar `3`

```dart
  test('test1', () {
    expect(minOf(3, 7), 3, reason: '--err-t1--');
  });
```

`minOf(10, 2)` deve retornar `2`

```dart
  test('test2', () {
    expect(minOf(10, 2), 2, reason: '--err-t2--');
  });
```

`minOf(5, 5)` deve retornar `5`

```dart
  test('test3', () {
    expect(minOf(5, 5), 5, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int minOf(int a, int b) {
  return a < b ? a : b;
}

void main() {
  print(minOf(3, 7));
}
```
