---
language: dart
exerciseType: 2
---

# --description--

O **operador ternário** `condition ? expr1 : expr2` é uma forma compacta de escrever uma expressão simples `if-else`.

```dart
int x = 10;
String label = x > 0 ? 'positive' : 'non-positive';
print(label); // positive
```

Se a condição for `true`, `expr1` é usado; caso contrário, `expr2` é usado.

# --instructions--

Complete a expressão ternária para atribuir `'Even'` a `parity` quando `n` for divisível por `2`, e `'Odd'` caso contrário.

# --seed--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 [/] 'Even' [/] 'Odd';
  print(parity);
}
```

# --answers--

- ?
- :
- ==
- ;

# --solutions--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 ? 'Even' : 'Odd';
  print(parity);
}
```

# --output--

Even
