---
language: dart
exerciseType: 1
---

# --instructions--

Escreva uma função `ageGroup(int age)` que retorna `'child'` para `age < 13`, `'teenager'` para `age < 18`, `'adult'` para `age < 65`, e `'senior'` para `age >= 65`, usando uma cadeia `if-else`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`ageGroup(8)` deve retornar `'child'`

```dart
  test('test1', () {
    expect(ageGroup(8), 'child', reason: '--err-t1--');
  });
```

`ageGroup(15)` deve retornar `'teenager'`

```dart
  test('test2', () {
    expect(ageGroup(15), 'teenager', reason: '--err-t2--');
  });
```

`ageGroup(30)` deve retornar `'adult'`

```dart
  test('test3', () {
    expect(ageGroup(30), 'adult', reason: '--err-t3--');
  });
```

`ageGroup(70)` deve retornar `'senior'`

```dart
  test('test4', () {
    expect(ageGroup(70), 'senior', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String ageGroup(int age) {
  if (age < 13) {
    return 'child';
  } else if (age < 18) {
    return 'teenager';
  } else if (age < 65) {
    return 'adult';
  } else {
    return 'senior';
  }
}

void main() {
  print(ageGroup(30));
}
```
