---
language: dart
exerciseType: 2
---

# --description--

O erro que volta do `Isolate.run` é uma **cópia** do que foi lançado do outro lado, então as verificações usuais continuam funcionando: `catch (e)` lhe dá o objeto, e `e is FormatException` diz que tipo de falha foi.

```dart
try {
  await Isolate.run(() => int.parse('abc'));
} on FormatException {
  print('not a number');
}
```

Em que você não pode confiar é no stack trace apontando para o seu próprio isolate: o erro viajou, a pilha não.

# --instructions--

Complete o código para que o parsing rode em outro isolate e sua falha seja capturada, de modo que o programa imprima `failed`.

# --seed--

```dart
import 'dart:isolate';

Future<void> main() async {
  [/] {
    final n = await Isolate.[/](() => int.parse('abc'));
    print(n);
  } [/] (e) {
    print('failed');
  }
}
```

# --answers--

- try
- run
- catch
- spawn
- finally
- then
- await

# --solutions--

```dart
import 'dart:isolate';

Future<void> main() async {
  try {
    final n = await Isolate.run(() => int.parse('abc'));
    print(n);
  } catch (e) {
    print('failed');
  }
}
```

# --output--

failed
