---
language: dart
exerciseType: 1
---

# --description--

`Isolate.spawn` passa exatamente **uma** mensagem ao ponto de entrada, e o worker geralmente precisa tanto de um `SendPort` para responder quanto de alguns dados para trabalhar. O truque usual é agrupar tudo em uma `List` e desempacotar do outro lado:

```dart
void multiply(List<Object> message) {
  final port = message[0] as SendPort;
  final value = message[1] as int;
  port.send(value * 2);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(multiply, <Object>[receivePort.sendPort, 21]);
  print(await receivePort.first); // 42
}
```

A lista é copiada na ida, então o worker lê seus próprios valores. Um `SendPort` é uma das poucas coisas que não são copiadas, mas compartilhadas: ele continua apontando para a caixa de correio do isolate que o criou, e é exatamente por isso que pode ser usado como endereço de retorno.

# --instructions--

Escreva uma função de nível superior `sendSum` que recebe uma `List<Object>` `message` em que o elemento `0` é um `SendPort` e os elementos `1` e `2` são `int`s, e envia a soma deles à porta.

Depois escreva uma função `addInIsolate` que recebe dois `int`s `a` e `b` e retorna um `Future<int>` que conclui com a soma calculada por um isolate `sendSum` iniciado.

Exemplo:
```dart
print(await addInIsolate(2, 3)); // 5
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendSum` deveria enviar a soma dos dois números da mensagem à porta

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendSum(<Object>[port.sendPort, 4, 6]);
    expect(await port.first, 10, reason: '--err-t1--');
  });
```

`addInIsolate(2, 3)` deveria concluir com `5`

```dart
  test('test2', () async {
    expect(await addInIsolate(2, 3), 5, reason: '--err-t2--');
  });
```

`addInIsolate(10, -4)` deveria concluir com `6` e `addInIsolate` deveria retornar um `Future<int>`

```dart
  test('test3', () async {
    expect(await addInIsolate(10, -4), 6, reason: '--err-t3--');
    expect(addInIsolate(1, 1), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendSum(List<Object> message) {
  final port = message[0] as SendPort;
  final a = message[1] as int;
  final b = message[2] as int;
  port.send(a + b);
}

Future<int> addInIsolate(int a, int b) async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendSum, <Object>[receivePort.sendPort, a, b]);
  return await receivePort.first as int;
}
```
