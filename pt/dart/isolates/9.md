---
language: dart
exerciseType: 2
---

# --description--

`first` lê uma mensagem e fecha a caixa de correio. Uma `ReceivePort` também é uma **`Stream`**, então para ler muitas mensagens você itera sobre ela com `await for`.

O laço nunca termina por si só: a porta fica aberta esperando uma mensagem que pode nunca chegar. Por isso o worker envia um último valor como sinal, geralmente `null`, e o ouvinte reage chamando **`close()`**, que encerra a stream e o laço:

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(countdown, receivePort.sendPort);
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --instructions--

Complete o código para que o isolate iniciado envie `3`, `2`, `1` e depois `null`, e o isolate principal imprima cada número, feche a caixa de correio no `null` e por fim imprima `done`.

# --seed--

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.[/](countdown, receivePort.sendPort);
  await [/] (final message in receivePort) {
    if (message == null) {
      receivePort.[/]();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --answers--

- spawn
- for
- close
- run
- while
- listen
- first

# --solutions--

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(countdown, receivePort.sendPort);
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --output--

3
2
1
done
