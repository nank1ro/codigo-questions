---
language: dart
exerciseType: 1
---

# --description--

Uma exceção lançada dentro de um isolate não pode pular para outro: os dois têm pilhas separadas. O `Isolate.run` fecha essa lacuna para você capturando o erro, copiando-o de volta e fazendo o future retornado falhar com ele. Do seu lado, portanto, ele é um erro assíncrono comum, capturado com `try`/`catch` ao redor do `await`:

```dart
Future<int> parseInIsolate(String text) async {
  try {
    return await Isolate.run(() => int.parse(text));
  } catch (e) {
    return -1;
  }
}
```

O `await` dentro do `try` importa, exatamente como com qualquer outro future: sem ele o future deixaria o bloco `try` inacabado e o `catch` nunca rodaria.

Com `Isolate.spawn` não existe essa ponte. Um erro não capturado mata o isolate iniciado silenciosamente e o pai continua esperando uma mensagem que nunca chegará, o que é mais um motivo para recorrer a `Isolate.run` primeiro.

# --instructions--

Escreva uma função `safeDivide` que recebe dois `int`s `a` e `b` e retorna um `Future<int>`: ela calcula `a ~/ b` em outro isolate com `Isolate.run` e conclui com `-1` se o cálculo lançar uma exceção.

Exemplo:
```dart
print(await safeDivide(10, 2)); // 5
print(await safeDivide(7, 0));  // -1
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeDivide(10, 2)` deveria concluir com `5`

```dart
  test('test1', () async {
    expect(await safeDivide(10, 2), 5, reason: '--err-t1--');
  });
```

`safeDivide(7, 0)` deveria concluir com `-1` em vez de falhar

```dart
  test('test2', () async {
    expect(await safeDivide(7, 0), -1, reason: '--err-t2--');
  });
```

`safeDivide(-9, 3)` deveria concluir com `-3` e `safeDivide` deveria retornar um `Future<int>`

```dart
  test('test3', () async {
    expect(await safeDivide(-9, 3), -3, reason: '--err-t3--');
    expect(safeDivide(4, 2), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<int> safeDivide(int a, int b) async {
  try {
    return await Isolate.run(() => a ~/ b);
  } catch (e) {
    return -1;
  }
}
```
