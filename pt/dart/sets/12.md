---
language: dart
exerciseType: 1
---

# --description--

Converter uma lista em um conjunto é a maneira mais fácil de **remover duplicatas**: toda lista tem um método `.toSet()` que retorna um conjunto com seus elementos únicos, na ordem da primeira aparição. Um conjunto tem um método `.toList()` que faz o caminho inverso, então encadear os dois lhe dá uma lista sem duplicatas:

```dart
var votes = ['a', 'b', 'a', 'c', 'b'];
Set<String> unique = votes.toSet();
print(unique); // {a, b, c}
List<String> cleaned = votes.toSet().toList();
print(cleaned); // [a, b, c]
```

# --instructions--

Dada a lista `votes`, crie uma `List<String>` chamada `cleaned` que contenha seus elementos sem duplicatas, na ordem da primeira aparição.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var votes = ['yes', 'no', 'yes', 'maybe', 'no', 'yes'];
```

# --after-seed--

```dart
  return cleaned;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`cleaned` deve ser `['yes', 'no', 'maybe']`

```dart
  test('test1', () {
    expect(result(), ['yes', 'no', 'maybe'], reason: '--err-t1--');
  });
```

`cleaned` deve ser uma `List`

```dart
  test('test2', () {
    expect(result(), isA<List>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var votes = ['yes', 'no', 'yes', 'maybe', 'no', 'yes'];
List<String> cleaned = votes.toSet().toList();
```
