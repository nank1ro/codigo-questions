---
language: dart
exerciseType: 1
---

# --description--

Conjuntos suportam as operações clássicas de conjuntos. Cada uma retorna um **novo** conjunto e deixa os originais intactos:

- `a.union(b)` contém os elementos que estão em `a` **ou** em `b`
- `a.intersection(b)` contém os elementos que estão em **ambos** `a` e `b`
- `a.difference(b)` contém os elementos de `a` que **não** estão em `b`

```dart
var a = {1, 2, 3};
var b = {2, 3, 4};
print(a.union(b));        // {1, 2, 3, 4}
print(a.intersection(b)); // {2, 3}
print(a.difference(b));   // {1}
```

# --instructions--

Dados os conjuntos `a` e `b`, crie um conjunto chamado `both` com a união deles, um conjunto chamado `common` com a interseção deles, e um conjunto chamado `onlyA` com os elementos de `a` que não estão em `b`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var a = {1, 2, 3, 4};
var b = {3, 4, 5};
```

# --after-seed--

```dart
  return [both, common, onlyA];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`both` deve ser `{1, 2, 3, 4, 5}`

```dart
  test('test1', () {
    expect(result()[0], {1, 2, 3, 4, 5}, reason: '--err-t1--');
  });
```

`common` deve ser `{3, 4}`

```dart
  test('test2', () {
    expect(result()[1], {3, 4}, reason: '--err-t2--');
  });
```

`onlyA` deve ser `{1, 2}`

```dart
  test('test3', () {
    expect(result()[2], {1, 2}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var a = {1, 2, 3, 4};
var b = {3, 4, 5};
var both = a.union(b);
var common = a.intersection(b);
var onlyA = a.difference(b);
```
