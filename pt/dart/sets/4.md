---
language: dart
exerciseType: 2
---

# --description--

O método `.remove(value)` exclui um valor do conjunto. Ele retorna `true` se o valor estava lá e `false` caso contrário:

```dart
var numbers = {1, 2, 3};
print(numbers.remove(2)); // true
print(numbers.remove(9)); // false
print(numbers); // {1, 3}
```

Para excluir todos os elementos de uma vez, use `.clear()`.

# --instructions--

Complete o código para remover o número `2` do conjunto, e depois imprima o conjunto.

# --seed--

```dart
var numbers = {1, 2, 3};
numbers.[/](2);
print(numbers);
```

# --answers--

- remove
- removeAt
- delete
- drop
- clear
- pop

# --solutions--

```dart
var numbers = {1, 2, 3};
numbers.remove(2);
print(numbers);
```

# --output--

{1, 3}
