---
language: dart
exerciseType: 1
---

# --instructions--

Dada a lista `numbers`, armazene em um `int` chamado `uniqueCount` quantos valores diferentes ela contém, e em uma `List<int>` chamada `unique` esses valores sem duplicatas, na ordem da primeira aparição.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var numbers = [4, 8, 4, 15, 8, 16, 4];
```

# --after-seed--

```dart
  return [uniqueCount, unique];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`uniqueCount` deve ser `4`

```dart
  test('test1', () {
    expect(result()[0], 4, reason: '--err-t1--');
  });
```

`unique` deve ser `[4, 8, 15, 16]`

```dart
  test('test2', () {
    expect(result()[1], [4, 8, 15, 16], reason: '--err-t2--');
  });
```

`unique` deve ser uma `List`

```dart
  test('test3', () {
    expect(result()[1], isA<List>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var numbers = [4, 8, 4, 15, 8, 16, 4];
int uniqueCount = numbers.toSet().length;
List<int> unique = numbers.toSet().toList();
```
