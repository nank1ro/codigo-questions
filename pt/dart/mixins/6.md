---
language: dart
exerciseType: 4
---

# --description--

Juntando as peças, um programa que usa um mixin tem três partes: a declaração `mixin`, uma ou mais classes que o adotam com `with`, e o código que chama o membro compartilhado.

```dart
mixin Barker {
  String bark() => 'Woof!';
}

class Dog with Barker {}

void main() {
  print(Dog().bark()); // Woof!
}
```

Declarações de nível superior podem ser escritas em qualquer ordem em Dart, mas ler um arquivo de cima para baixo fica mais fácil quando o mixin vem antes das classes que o usam.

# --instructions--

Ordene os itens para definir um mixin `Purrer` com um método `purr` que retorna `Purr...`, uma classe `Cat` que o usa, e um `main` que imprime o resultado de `purr`. Defina primeiro o mixin, depois a classe, depois o `main`. O programa deve imprimir:

```
Purr...
```

# --answers--

```dart
mixin Purrer {
```

```dart
  String purr() => 'Purr...';
```

```dart
}
```

```dart
class Cat with Purrer {}
```

```dart
void main() {
```

```dart
  print(Cat().purr());
```

```dart
}
```

# --solutions--

```dart
mixin Purrer {
  String purr() => 'Purr...';
}

class Cat with Purrer {}

void main() {
  print(Cat().purr());
}
```

# --output--

Purr...
