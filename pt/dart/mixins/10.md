---
language: dart
exerciseType: 1
---

# --description--

O corpo da classe fica no fim da cadeia, então um membro declarado na classe **sobrescreve** o mesmo membro vindo de qualquer um dos seus mixins. Dentro da sobrescrita, **`super`** alcança a versão que o mixin forneceu:

```dart
mixin Polite {
  String greet() => 'Hello';
}

class Host with Polite {
  @override
  String greet() => '${super.greet()}, welcome!';
}

class Guest with Polite {}

void main() {
  print(Host().greet());  // Hello, welcome!
  print(Guest().greet()); // Hello
}
```

O mixin em si não é alterado: `Guest` ainda recebe o `greet` original. `super.greet()` é o que permite a `Host` construir sobre o comportamento compartilhado em vez de copiá-lo.

# --instructions--

Declare um mixin `Signed` com um método `sign` que retorna `'signed'`.

Declare uma classe `Letter` que usa `Signed` e sobrescreve `sign` de modo que retorne o resultado do mixin seguido de `' by Ada'`, obtido chamando `super`.

Declare uma classe `Note` que usa `Signed` e não sobrescreve nada.

Exemplo:
```dart
print(Letter().sign()); // signed by Ada
print(Note().sign());   // signed
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Letter().sign()` deve ser `'signed by Ada'`

```dart
  test('test1', () {
    expect(Letter().sign(), 'signed by Ada', reason: '--err-t1--');
  });
```

`Note().sign()` deve continuar sendo `'signed'`, porque o mixin não é alterado

```dart
  test('test2', () {
    expect(Note().sign(), 'signed', reason: '--err-t2--');
  });
```

As duas classes devem usar o mixin, e a sobrescrita também deve ser usada através do tipo do mixin

```dart
  test('test3', () {
    final Signed letter = Letter();
    expect(letter.sign(), 'signed by Ada', reason: '--err-t3--');
    expect(Note(), isA<Signed>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Signed {
  String sign() => 'signed';
}

class Letter with Signed {
  @override
  String sign() => '${super.sign()} by Ada';
}

class Note with Signed {}
```
