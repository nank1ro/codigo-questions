---
language: dart
exerciseType: 4
---

# --description--

Um mixin com uma cláusula `on` lê os membros da sua superclasse como se fossem seus, e é isso que o torna um bom lugar para comportamento que decora um tipo existente:

```dart
class Shape {
  String get kind => 'shape';
}

mixin Printable on Shape {
  void show() {
    print('a $kind');
  }
}

class Square extends Shape with Printable {
  @override
  String get kind => 'square';
}
```

`Square` sobrescreve `kind`, e `show` captura a sobrescrita automaticamente: o mixin sempre chama o membro no objeto real.

# --instructions--

Ordene os itens para definir uma classe `Shape` com um getter `kind` que retorna `'shape'`, um mixin `Printable` restrito a `Shape` com um método `show` que imprime `a ` seguido de `kind`, uma classe `Square` que estende `Shape`, usa `Printable` e sobrescreve `kind` com `'square'`, e um `main` que chama `show` em um `Square`. Defina primeiro `Shape`, depois `Printable`, depois `Square`, depois o `main`. O programa deve imprimir:

```
a square
```

# --answers--

```dart
class Shape {
```

```dart
  String get kind => 'shape';
```

```dart
}
```

```dart
mixin Printable on Shape {
```

```dart
  void show() {
    print('a $kind');
  }
```

```dart
}
```

```dart
class Square extends Shape with Printable {
```

```dart
  @override
  String get kind => 'square';
```

```dart
}
```

```dart
void main() {
```

```dart
  Square().show();
```

```dart
}
```

# --solutions--

```dart
class Shape {
  String get kind => 'shape';
}

mixin Printable on Shape {
  void show() {
    print('a $kind');
  }
}

class Square extends Shape with Printable {
  @override
  String get kind => 'square';
}

void main() {
  Square().show();
}
```

# --output--

a square
