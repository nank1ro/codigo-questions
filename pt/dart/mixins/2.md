---
language: dart
exerciseType: 1
---

# --description--

O corpo de um mixin se parece com o corpo de uma classe: métodos, getters e campos, escritos exatamente da mesma forma. A diferença está no que você pode fazer com a própria declaração.

```dart
mixin Timestamped {
  String get stamp => '[log]';

  String format(String text) => '$stamp $text';
}

class Server with Timestamped {}
class Printer with Timestamped {}
```

O nome de um mixin também é um **tipo**, então `Server() is Timestamped` é `true` e uma variável pode ser declarada como `Timestamped t = Server();`. Duas classes sem relação agora compartilham uma mesma implementação sem que uma herde da outra.

# --instructions--

Declare um mixin `Shouter` com um método `shout` que recebe uma `String` `text` e a retorna em maiúsculas seguida de um ponto de exclamação.

Declare uma classe `Speaker` que usa `Shouter` e não adiciona nada próprio.

Exemplo:
```dart
print(Speaker().shout('hi')); // HI!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Speaker().shout('hi')` deve ser `'HI!'`

```dart
  test('test1', () {
    expect(Speaker().shout('hi'), 'HI!', reason: '--err-t1--');
  });
```

`Speaker().shout('Dart rocks')` deve ser `'DART ROCKS!'` e `Speaker().shout('')` deve ser `'!'`

```dart
  test('test2', () {
    expect(Speaker().shout('Dart rocks'), 'DART ROCKS!', reason: '--err-t2--');
    expect(Speaker().shout(''), '!', reason: '--err-t2--');
  });
```

`Speaker` deve obter `shout` do mixin, então um `Speaker` também deve ser um `Shouter`

```dart
  test('test3', () {
    expect(Speaker(), isA<Shouter>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Shouter {
  String shout(String text) => '${text.toUpperCase()}!';
}

class Speaker with Shouter {}
```
