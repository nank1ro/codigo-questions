---
language: dart
exerciseType: 1
---

# --description--

Um mixin pode declarar um membro **sem corpo**. Esse membro é abstrato: o mixin o utiliza, e a classe que adota o mixin precisa fornecê-lo.

```dart
mixin Greeting {
  String get name;                       // no body: the class provides it

  String greet() => 'Hello, $name!';
}

class Person with Greeting {
  @override
  final String name;

  Person(this.name);
}

void main() {
  print(Person('Ada').greet()); // Hello, Ada!
}
```

O mixin traz o comportamento, a classe traz os dados. Um campo na classe, como `final String name;`, é suficiente para satisfazer um getter abstrato de mesmo nome.

# --instructions--

Declare um mixin `Labelled` com:
- um getter abstrato `title` do tipo `String`, declarado sem corpo
- um método `label` que retorna `title` entre colchetes

Declare uma classe `Book` que usa `Labelled`, tem um campo `title` do tipo `final String` e um construtor que o recebe.

Exemplo:
```dart
print(Book('Dune').label()); // [Dune]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Book('Dune').label()` deve ser `'[Dune]'`

```dart
  test('test1', () {
    expect(Book('Dune').label(), '[Dune]', reason: '--err-t1--');
  });
```

`Book('').label()` deve ser `'[]'` e `Book('a b').label()` deve ser `'[a b]'`

```dart
  test('test2', () {
    expect(Book('').label(), '[]', reason: '--err-t2--');
    expect(Book('a b').label(), '[a b]', reason: '--err-t2--');
  });
```

`Book` deve manter seu `title` e ser um `Labelled`

```dart
  test('test3', () {
    final book = Book('Dart');
    expect(book.title, 'Dart', reason: '--err-t3--');
    expect(book, isA<Labelled>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Labelled {
  String get title;

  String label() => '[$title]';
}

class Book with Labelled {
  @override
  final String title;

  Book(this.title);
}
```
