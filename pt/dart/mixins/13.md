---
language: dart
exerciseType: 1
---

# --description--

Quando um mixin tem uma cláusula `on`, ele pode **sobrescrever** um membro desse tipo e chamar **`super`** para alcançar a versão abaixo dele na cadeia:

```dart
class Logger {
  String log(String message) => message;
}

mixin Timestamped on Logger {
  @override
  String log(String message) => '[12:00] ${super.log(message)}';
}

class AppLogger extends Logger with Timestamped {}

void main() {
  print(AppLogger().log('started')); // [12:00] started
}
```

`super.log` não é o `log` do próprio mixin, é o que está abaixo dele, então não há recursão infinita. Empilhe vários mixins assim com `with A, B` e cada um envolve o anterior: a chamada entra primeiro no **último** mixin e desce até a superclasse.

# --instructions--

Declare uma classe `Pipeline` com um método `run` que recebe uma `String` `input` e a retorna inalterada.

Declare um mixin `Trimmed` em `Pipeline` que sobrescreve `run` passando a entrada aparada a `super.run`, e um mixin `Upper` em `Pipeline` que sobrescreve `run` retornando o resultado de `super.run` em maiúsculas.

Declare uma classe `TextPipeline` que estende `Pipeline` e usa `Trimmed` e depois `Upper`, e uma classe `TrimOnly` que estende `Pipeline` e usa apenas `Trimmed`.

Exemplo:
```dart
print(TextPipeline().run('  hi  ')); // HI
print(TrimOnly().run('  hi  '));     // hi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`TextPipeline().run('  hi  ')` deve ser `'HI'`

```dart
  test('test1', () {
    expect(TextPipeline().run('  hi  '), 'HI', reason: '--err-t1--');
    expect(TextPipeline().run('dart'), 'DART', reason: '--err-t1--');
  });
```

`Pipeline().run('  hi  ')` deve continuar retornando a entrada inalterada

```dart
  test('test2', () {
    expect(Pipeline().run('  hi  '), '  hi  ', reason: '--err-t2--');
  });
```

`TrimOnly().run('  hi  ')` deve ser `'hi'`, e cada classe deve usar os mixins de que precisa

```dart
  test('test3', () {
    expect(TrimOnly().run('  hi  '), 'hi', reason: '--err-t3--');
    expect(TrimOnly(), isA<Trimmed>(), reason: '--err-t3--');
    expect(TextPipeline(), isA<Upper>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Pipeline {
  String run(String input) => input;
}

mixin Trimmed on Pipeline {
  @override
  String run(String input) => super.run(input.trim());
}

mixin Upper on Pipeline {
  @override
  String run(String input) => super.run(input).toUpperCase();
}

class TextPipeline extends Pipeline with Trimmed, Upper {}

class TrimOnly extends Pipeline with Trimmed {}
```
