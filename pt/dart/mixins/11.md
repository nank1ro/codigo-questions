---
language: dart
exerciseType: 2
---

# --description--

Alguns comportamentos só fazem sentido por cima de uma classe específica e precisam dos membros dessa classe para fazer seu trabalho. A cláusula **`on`** declara o requisito:

```dart
class Animal {
  String get name => 'animal';
}

mixin Noisy on Animal {
  String shout() => '${name.toUpperCase()}!';
}

class Dog extends Animal with Noisy {
  @override
  String get name => 'dog';
}

void main() {
  print(Dog().shout()); // DOG!
}
```

`on Animal` faz duas coisas: permite que o mixin use os membros de `Animal`, como `name` acima, e restringe quem pode usar o mixin. `class Rock with Noisy {}` é um erro de compilação, porque `Rock` não é um `Animal`.

# --instructions--

Complete o código para que `Described` seja um mixin restrito a `Vehicle`, e `Car` seja um `Vehicle` que o usa.

# --seed--

```dart
class Vehicle {
  int get wheels => 4;
}

mixin Described [/] Vehicle {
  String describe() => 'has $wheels wheels';
}

class Car [/] Vehicle [/] Described {}

void main() {
  print(Car().describe());
}
```

# --answers--

- on
- extends
- with
- mixin
- class
- of

# --solutions--

```dart
class Vehicle {
  int get wheels => 4;
}

mixin Described on Vehicle {
  String describe() => 'has $wheels wheels';
}

class Car extends Vehicle with Described {}

void main() {
  print(Car().describe());
}
```

# --output--

has 4 wheels
