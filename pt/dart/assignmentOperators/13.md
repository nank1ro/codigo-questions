---
language: dart
exerciseType: 1
---

# --instructions--

Declare uma variável `int` `remainder` com valor `29`. Depois use `%=` para atribuir a ela o resto da divisão por `6`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int remainder = 29;
```

# --after-seed--

```dart
  return remainder;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`remainder` deve ser `5`

```dart
  test('test1', () {
    expect(result(), 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 29;
remainder %= 6;
```
