---
language: dart
exerciseType: 1
---

# --instructions--

Declare uma variável `int` de nível superior `n` com valor `2`. Depois, em `main`, aplique os seguintes operadores de atribuição em ordem:
- `n *= 10` (multiplique por 10)
- `n += 5` (adicione 5)
- `n -= 3` (subtraia 3)

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`n` deve ser `22`

```dart
  test('test1', () {
    expect(n, 22, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 2;

void main() {
  n *= 10;
  n += 5;
  n -= 3;
  print(n);
}
```
