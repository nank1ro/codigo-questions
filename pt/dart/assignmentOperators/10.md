---
language: dart
exerciseType: 1
---

# --instructions--

Declare uma variável `int` de nível superior `width` com valor `6`. Depois use `*=` para multiplicá-la por `4`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`width` deve ser `24`

```dart
  test('test1', () {
    expect(width, 24, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int width = 6;

void main() {
  width *= 4;
  print(width);
}
```
