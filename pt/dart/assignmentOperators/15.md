---
language: dart
exerciseType: 1
---

# --instructions--

Uma loja está aplicando um desconto de 20% em um item. Declare uma variável `double` `price` com valor `50.0`. Depois use `*=` para aplicar o desconto multiplicando `price` por `0.8`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
double price = 50.0;
```

# --after-seed--

```dart
  return price;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`price` deve ser `40.0`

```dart
  test('test1', () {
    expect(result(), closeTo(40.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double price = 50.0;
price *= 0.8;
```
