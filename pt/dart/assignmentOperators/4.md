---
language: dart
exerciseType: 2
---

# --description--

O **operador de atribuição de multiplicação** `*=` multiplica uma variável por um valor e armazena o resultado de volta nessa variável.

```dart
int side = 4;
side *= 3; // side agora é 12
```

Esta é uma forma abreviada de `side = side * 3`.

# --instructions--

Complete o código para multiplicar `price` por `3` usando `*=`.

# --seed--

```dart
void main() {
  int price = 7;
  price [/] 3;
  print(price);
}
```

# --answers--

- *=
- =
- +=
- *

# --solutions--

```dart
void main() {
  int price = 7;
  price *= 3;
  print(price);
}
```

# --output--

21
