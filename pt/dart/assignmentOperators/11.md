---
language: dart
exerciseType: 1
---

# --instructions--

Declare uma variável `double` `value` com valor `20.0`. Depois use `/=` para dividi-la por `4`. Note que `/=` sempre produz um `double` em Dart.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
double value = 20.0;
```

# --after-seed--

```dart
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value` deve ser `5.0`

```dart
  test('test1', () {
    expect(result(), closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;
value /= 4;
```
