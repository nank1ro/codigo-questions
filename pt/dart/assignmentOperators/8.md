---
language: dart
exerciseType: 1
---

# --instructions--

Declare uma variável `int` `points` com valor `20`. Depois use `+=` para adicionar `15` a ela.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int points = 20;
```

# --after-seed--

```dart
  return points;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`points` deve ser `35`

```dart
  test('test1', () {
    expect(result(), 35, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int points = 20;
points += 15;
```
