---
language: dart
exerciseType: 2
---

# --description--

O **operador de atribuição de subtração** `-=` subtrai um valor de uma variável e armazena o resultado de volta nessa variável.

```dart
int lives = 3;
lives -= 1; // lives agora é 2
```

Esta é uma forma abreviada de `lives = lives - 1`.

# --instructions--

Complete o código para subtrair `5` da variável `stock` usando `-=`.

# --seed--

```dart
void main() {
  int stock = 20;
  stock [/] 5;
  print(stock);
}
```

# --answers--

- -=
- =
- +=
- -

# --solutions--

```dart
void main() {
  int stock = 20;
  stock -= 5;
  print(stock);
}
```

# --output--

15
