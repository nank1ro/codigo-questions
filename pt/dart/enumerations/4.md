---
language: dart
exerciseType: 1
---

# --description--

Como `values` é uma lista, você pode combiná-la com os métodos de lista que já conhece. Por exemplo, `.map()` com `.name` transforma os valores em uma lista de strings:

```dart
enum Direction { north, south, east, west }

List<String> names = Direction.values.map((d) => d.name).toList();
print(names); // [north, south, east, west]
```

# --instructions--

Declare um enum `Weekday` com os valores `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday` e `sunday`, nesta ordem. Depois escreva uma função `dayNames()` que retorne uma `List<String>` com o nome de cada dia da semana, na ordem de declaração.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Weekday.values.length` deve ser `7`

```dart
  test('test1', () {
    expect(Weekday.values.length, 7, reason: '--err-t1--');
  });
```

`Weekday.values[2]` deve ser `Weekday.wednesday`

```dart
  test('test2', () {
    expect(Weekday.values[2], Weekday.wednesday, reason: '--err-t2--');
  });
```

`dayNames()` deve retornar os sete nomes em ordem

```dart
  test('test3', () {
    expect(dayNames(), ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Weekday { monday, tuesday, wednesday, thursday, friday, saturday, sunday }

List<String> dayNames() {
  return Weekday.values.map((day) => day.name).toList();
}
```
