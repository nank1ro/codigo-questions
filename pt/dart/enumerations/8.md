---
language: dart
exerciseType: 2
---

# --description--

Desde o Dart 3, um `switch` também pode ser usado como uma **expressão** que produz um valor. Cada case é escrito como `padrão => valor` e os cases são separados por vírgulas, sem a palavra-chave `case` e sem `break`:

```dart
enum Direction { north, south, east, west }

String arrow(Direction direction) => switch (direction) {
      Direction.north => '^',
      Direction.south => 'v',
      Direction.east => '>',
      Direction.west => '<',
    };
```

Assim como na forma de instrução, uma expressão switch sobre um enum deve ser exaustiva.

# --instructions--

Complete a expressão switch para que `label` retorne a primeira letra de cada tamanho, e depois imprima o label de `Size.large`.

# --seed--

```dart
enum Size { small, medium, large }

String label(Size size) => [/] (size) {
      Size.small [/] 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --answers--

- switch
- =>
- case
- when
- ->
- :

# --solutions--

```dart
enum Size { small, medium, large }

String label(Size size) => switch (size) {
      Size.small => 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --output--

L
