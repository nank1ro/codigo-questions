---
language: dart
exerciseType: 4
---

# --description--

Percorrer `values` é a maneira habitual de processar todos os valores de um enum. Dentro do laço, o valor atual se comporta como qualquer outro objeto, então você pode ler o seu `index` e o seu `name` e usá-los diretamente em uma interpolação de string:

```dart
enum Direction { north, south, east, west }

for (var direction in Direction.values) {
  print('${direction.index}: ${direction.name}');
}
```

# --instructions--

Ordene os itens para declarar o enum `Level` e imprimir, para cada valor, seu índice e seu nome. Declare o enum antes do `main`. O programa deve imprimir:

```
0: low
1: medium
2: high
```

# --answers--

```dart
enum Level { low, medium, high }
```

```dart
void main() {
```

```dart
  for (var level in Level.values) {
```

```dart
    print('${level.index}: ${level.name}');
```

```dart
  }
}
```

# --solutions--

```dart
enum Level { low, medium, high }
void main() {
  for (var level in Level.values) {
    print('${level.index}: ${level.name}');
  }
}
```

# --output--

0: low
1: medium
2: high
