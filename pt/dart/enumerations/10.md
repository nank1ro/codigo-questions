---
language: dart
exerciseType: 1
---

# --description--

Desde o Dart 2.17, um enum pode declarar **campos** e um **construtor**, assim como uma classe. Isso é chamado de *enum aprimorado*. Cada valor então passa seus próprios argumentos para o construtor:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);
}

print(Planet.mars.moons); // 2
```

Observe as três regras: a lista de valores termina com um **ponto e vírgula** `;`, os campos devem ser `final`, e o construtor deve ser `const`.

# --instructions--

Declare um enum `Coin` com um campo `final int` chamado `cents` e os valores `penny(1)`, `nickel(5)`, `dime(10)` e `quarter(25)`, nesta ordem.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Coin.penny.cents` deve ser `1` e `Coin.nickel.cents` deve ser `5`

```dart
  test('test1', () {
    expect(Coin.penny.cents, 1, reason: '--err-t1--');
    expect(Coin.nickel.cents, 5, reason: '--err-t2--');
  });
```

`Coin.dime.cents` deve ser `10`

```dart
  test('test2', () {
    expect(Coin.dime.cents, 10, reason: '--err-t3--');
  });
```

`Coin.quarter.cents` deve ser `25`

```dart
  test('test3', () {
    expect(Coin.quarter.cents, 25, reason: '--err-t4--');
  });
```

`Coin.values` deve ter 4 valores, com `penny` primeiro

```dart
  test('test4', () {
    expect(Coin.values.length, 4, reason: '--err-t5--');
    expect(Coin.values[0], Coin.penny, reason: '--err-t6--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Coin {
  penny(1),
  nickel(5),
  dime(10),
  quarter(25);

  final int cents;

  const Coin(this.cents);
}
```
