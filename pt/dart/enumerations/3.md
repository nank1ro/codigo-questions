---
language: dart
exerciseType: 2
---

# --description--

Todo enum também tem uma lista constante chamada `values` que contém todos os seus valores na ordem de declaração. Você pode indexá-la como qualquer lista, ler seu `length`, ou percorrê-la com `for-in`:

```dart
enum Direction { north, south, east, west }

print(Direction.values.length); // 4
print(Direction.values[1]);     // Direction.south

for (var direction in Direction.values) {
  print(direction.name);
}
```

# --instructions--

Complete o código para imprimir o nome de `Planet.earth`, seu índice e o número total de planetas.

# --seed--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.[/]);
  print(Planet.earth.[/]);
  print(Planet.[/].length);
}
```

# --answers--

- name
- index
- values
- ordinal
- position
- count

# --solutions--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.name);
  print(Planet.earth.index);
  print(Planet.values.length);
}
```

# --output--

earth
2
3
