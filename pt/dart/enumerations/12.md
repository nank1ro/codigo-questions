---
language: dart
exerciseType: 1
---

# --description--

Um enum aprimorado também pode declarar **métodos** e **getters**. Dentro deles, `this` é o valor atual, então você pode usar diretamente seu `name`, `index` e campos:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);

  bool get hasMoons => moons > 0;

  String describe() => '$name has $moons moon(s)';
}

print(Planet.earth.hasMoons);   // true
print(Planet.mars.describe()); // mars has 2 moon(s)
```

Um enum sem campos ainda pode declarar métodos: a lista de valores então termina com `;` e os membros vêm em seguida.

# --instructions--

Declare um enum `Direction` com os valores `north`, `east`, `south` e `west`, nesta ordem. Adicione um getter `opposite` que retorne o valor do lado oposto (`north` <-> `south`, `east` <-> `west`) e um método `turnRight()` que retorne o valor seguinte, voltando de `west` para `north`.

Dica: combine `index`, `values` e o operador `%`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Direction.north.opposite` deve ser `Direction.south`

```dart
  test('test1', () {
    expect(Direction.north.opposite, Direction.south, reason: '--err-t1--');
  });
```

`Direction.west.opposite` deve ser `Direction.east`

```dart
  test('test2', () {
    expect(Direction.west.opposite, Direction.east, reason: '--err-t2--');
  });
```

`Direction.north.turnRight()` deve ser `Direction.east`

```dart
  test('test3', () {
    expect(Direction.north.turnRight(), Direction.east, reason: '--err-t3--');
  });
```

`Direction.west.turnRight()` deve ser `Direction.north`

```dart
  test('test4', () {
    expect(Direction.west.turnRight(), Direction.north, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Direction {
  north,
  east,
  south,
  west;

  Direction get opposite => Direction.values[(index + 2) % 4];

  Direction turnRight() => Direction.values[(index + 1) % 4];
}
```
