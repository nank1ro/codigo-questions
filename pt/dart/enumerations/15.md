---
language: dart
exerciseType: 1
---

# --description--

Valores de enum são excelentes **chaves de map**: são únicos, fáceis de comparar, e o compilador garante que você use apenas valores reais. Declare o map com o enum como tipo da chave e busque os valores com `[]`:

```dart
enum Direction { north, south, east, west }

Map<Direction, String> arrows = {
  Direction.north: '^',
  Direction.south: 'v',
  Direction.east: '>',
  Direction.west: '<',
};

print(arrows[Direction.east]); // >
```

Como em qualquer map, uma busca retorna `null` quando a chave não existe, então use `??` para fornecer um valor alternativo.

# --instructions--

Dado `enum Size { small, medium, large }`, declare um `Map<Size, int>` de nível superior chamado `prices` em que `small` custa `2`, `medium` custa `3` e `large` custa `4`. Depois escreva uma função `totalPrice(List<Size> order)` que retorne a soma dos preços de cada tamanho em `order`.

# --seed--

```dart
enum Size { small, medium, large }
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`prices[Size.medium]` deve ser `3`

```dart
  test('test1', () {
    expect(prices[Size.medium], 3, reason: '--err-t1--');
  });
```

`totalPrice([Size.small, Size.large])` deve ser `6`

```dart
  test('test2', () {
    expect(totalPrice([Size.small, Size.large]), 6, reason: '--err-t2--');
  });
```

`totalPrice([Size.medium, Size.medium, Size.small])` deve ser `8`

```dart
  test('test3', () {
    expect(totalPrice([Size.medium, Size.medium, Size.small]), 8, reason: '--err-t3--');
  });
```

`totalPrice([])` deve ser `0`

```dart
  test('test4', () {
    expect(totalPrice([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Size { small, medium, large }

Map<Size, int> prices = {
  Size.small: 2,
  Size.medium: 3,
  Size.large: 4,
};

int totalPrice(List<Size> order) {
  int total = 0;
  for (var size in order) {
    total += prices[size] ?? 0;
  }
  return total;
}
```
