---
language: dart
exerciseType: 2
---

# --description--

As listas em Dart são **indexadas a partir de zero**, ou seja, o primeiro elemento está no índice `0`, o segundo no índice `1`, e assim por diante.

```dart
var colors = ['red', 'green', 'blue'];
print(colors[0]); // red
print(colors[2]); // blue
```

O acesso a um elemento por índice é feito com a sintaxe `list[index]`.

# --instructions--

Complete o código para imprimir o segundo elemento da lista.

# --seed--

```dart
var animals = ['cat', 'dog', 'bird'];
print(animals[/]);
```

# --answers--

- [1]
- [0]
- [2]
- .get(1)
- (1)
- [3]

# --solutions--

```dart
var animals = ['cat', 'dog', 'bird'];
print(animals[1]);
```

# --output--

dog
