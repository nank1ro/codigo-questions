---
language: dart
exerciseType: 1
---

# --instructions--

Crie uma `List<int>` vazia. Retorne `true` se ela estiver `.isEmpty`, depois adicione o valor `1` e retorne `true` se ela estiver `.isNotEmpty`. Retorne ambos os booleanos como uma `List<bool>`.

# --before-seed--

```dart
List<bool> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

O primeiro valor é true (estava vazia)

```dart
  test('first is true', () {
    expect(computeValue()[0], true, reason: '--err-t1--');
  });
```

O segundo valor é true (não está vazia após adicionar)

```dart
  test('second is true', () {
    expect(computeValue()[1], true, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<int> nums = [];
  bool wasEmpty = nums.isEmpty;
  nums.add(1);
  bool nowNotEmpty = nums.isNotEmpty;
  List<bool> result = [wasEmpty, nowNotEmpty];
```
