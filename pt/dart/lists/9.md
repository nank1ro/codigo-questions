---
language: dart
exerciseType: 1
---

# --instructions--

Crie uma `List<String>` chamada `colors` contendo `'red'`, `'green'` e `'blue'`. Retorne seu comprimento.

# --before-seed--

```dart
int computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Retorna o comprimento correto

```dart
  test('length is 3', () {
    expect(computeValue(), 3, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<String> colors = ['red', 'green', 'blue'];
  int result = colors.length;
```
