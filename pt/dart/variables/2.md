---
language: dart
exerciseType: 1
---

# --description--

No exemplo anterior, vimos a criação de uma variável:

```dart
int number = 1;
```

Não se deixe enganar pelo símbolo `=`.
Ele não é o símbolo de igualdade como na matemática, mas é conhecido como o __operador de atribuição__ porque atribui um valor à variável.
O sinal de igualdade, por outro lado, é `==`.

# --instructions--

Atribua à variável `x` o valor `5`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x` deve ser igual a `5`.

```dart
  test('test1', () {
    expect(x, 5, reason: '--err-t1--');
  });
```

`x` deve ser do tipo `int`

```dart
  test('test2', () {
    expect(x, TypeMatcher<int>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 5;
```
