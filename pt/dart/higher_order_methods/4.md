---
language: dart
exerciseType: 1
---

# --description--

Alguns métodos de ordem superior respondem a uma pergunta sobre a coleção em vez de construir uma nova. Eles recebem um predicado e retornam um `bool`:

- `any` é `true` quando **pelo menos um** elemento satisfaz o predicado
- `every` é `true` quando **todos** os elementos o satisfazem

```dart
final numbers = [1, 2, 3];
print(numbers.any((n) => n > 2));   // true
print(numbers.every((n) => n > 2)); // false
```

Ambos param assim que a resposta é certa: `any` no primeiro elemento que corresponde, `every` no primeiro que não corresponde.

Em uma coleção vazia `any` é `false` e `every` é `true`: não há elemento para provar o primeiro, e nenhum para quebrar o segundo.

# --instructions--

Escreva duas funções, ambas recebendo uma `List<int>` `numbers`:

- `hasNegative` retorna `true` se pelo menos um elemento for menor que `0`
- `allEven` retorna `true` se todos os elementos forem pares

Exemplo:
```dart
print(hasNegative([1, -2, 3])); // true
print(allEven([2, 4]));         // true
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasNegative([1, -2, 3])` deve ser `true` e `hasNegative([1, 2, 3])` deve ser `false`

```dart
  test('test1', () {
    expect(hasNegative([1, -2, 3]), true, reason: '--err-t1--');
    expect(hasNegative([1, 2, 3]), false, reason: '--err-t1--');
  });
```

`allEven([2, 4, 6])` deve ser `true` e `allEven([2, 5])` deve ser `false`

```dart
  test('test2', () {
    expect(allEven([2, 4, 6]), true, reason: '--err-t2--');
    expect(allEven([2, 5]), false, reason: '--err-t2--');
  });
```

Em uma lista vazia `hasNegative` deve ser `false` e `allEven` deve ser `true`

```dart
  test('test3', () {
    expect(hasNegative(<int>[]), false, reason: '--err-t3--');
    expect(allEven(<int>[]), true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool hasNegative(List<int> numbers) {
  return numbers.any((n) => n < 0);
}

bool allEven(List<int> numbers) {
  return numbers.every((n) => n % 2 == 0);
}
```
