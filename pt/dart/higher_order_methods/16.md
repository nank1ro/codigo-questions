---
language: dart
exerciseType: 1
---

# --description--

Juntos, esses métodos substituem a maior parte dos laços escritos à mão. Um pipeline costuma se ler em três etapas: **selecionar** os elementos com `where`, **transformá-los** com `map`, e depois **combiná-los** com `fold`:

```dart
final prices = [12, 40, 7];
final cheapTotal = prices.where((p) => p < 20).fold(0, (acc, p) => acc + p);
print(cheapTotal); // 19
```

Como `fold` escolhe seu próprio valor inicial, ele também pode encerrar uma cadeia com um tipo que não tem nada a ver com os elementos, como uma `String` crescendo um pedaço de cada vez:

```dart
final words = ['fig', 'kiwi'];
final firstLetters = words.fold('', (acc, w) => acc + w[0]);
print(firstLetters); // fk
```

Cada etapa continua curta e diz o que faz, que é a verdadeira razão para preferi-las a um laço que faz as três coisas ao mesmo tempo.

# --instructions--

Escreva uma função `initials` que recebe uma `List<String>` `names`, ignora as vazias, e retorna uma `String` formada pela primeira letra de cada nome restante, em maiúsculas e na ordem.

Exemplo:
```dart
print(initials(['ada', '', 'linus'])); // AL
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`initials(['ada', '', 'linus'])` deve retornar `'AL'`

```dart
  test('test1', () {
    expect(initials(['ada', '', 'linus']), 'AL', reason: '--err-t1--');
  });
```

As letras devem estar em maiúsculas e na ordem, e nomes já em maiúsculas devem permanecer como estão

```dart
  test('test2', () {
    expect(initials(['grace', 'Bob', 'ada']), 'GBA', reason: '--err-t2--');
    expect(initials(['x']), 'X', reason: '--err-t2--');
  });
```

`initials` deve retornar uma `String` vazia quando não houver nenhum nome não vazio

```dart
  test('test3', () {
    expect(initials(<String>[]), '', reason: '--err-t3--');
    expect(initials(['', '']), '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String initials(List<String> names) {
  return names
      .where((n) => n.isNotEmpty)
      .map((n) => n[0].toUpperCase())
      .fold('', (acc, letter) => acc + letter);
}
```
