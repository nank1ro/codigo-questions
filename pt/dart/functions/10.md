---
language: dart
exerciseType: 1
---

# --instructions--

Escreva uma **função de seta** `square` que receba um parâmetro `int` `n` e retorne `n * n`.

Use a sintaxe `=>` (sem chaves, sem a palavra-chave `return`).

Exemplo:
```dart
print(square(4));  // 16
print(square(5));  // 25
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`square(4)` deve retornar `16`
```dart
  test('test1', () {
    expect(square(4), 16, reason: '--err-t1--');
  });
```

`square(5)` deve retornar `25`
```dart
  test('test2', () {
    expect(square(5), 25, reason: '--err-t2--');
  });
```

`square(0)` deve retornar `0`
```dart
  test('test3', () {
    expect(square(0), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int square(int n) => n * n;
```
