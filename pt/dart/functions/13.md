---
language: dart
exerciseType: 1
---

# --instructions--

Escreva uma função `range` que receba um parâmetro `int` `n` e retorne uma `List<int>` contendo os inteiros de `1` a `n` (inclusive).

Exemplo:
```dart
print(range(3)); // [1, 2, 3]
print(range(5)); // [1, 2, 3, 4, 5]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`range(3)` deve retornar `[1, 2, 3]`
```dart
  test('test1', () {
    expect(range(3), [1, 2, 3], reason: '--err-t1--');
  });
```

`range(5)` deve retornar `[1, 2, 3, 4, 5]`
```dart
  test('test2', () {
    expect(range(5), [1, 2, 3, 4, 5], reason: '--err-t2--');
  });
```

`range(1)` deve retornar `[1]`
```dart
  test('test3', () {
    expect(range(1), [1], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> range(int n) {
  List<int> result = [];
  for (int i = 1; i <= n; i++) {
    result.add(i);
  }
  return result;
}
```
