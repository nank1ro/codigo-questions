---
language: dart
exerciseType: 4
---

# --description--

O tipo depois de `on` pode ser um tipo **parametrizado** como `List<int>`. A extension então se aplica apenas a listas desse tipo de elemento: `[1, 2].total()` funciona, `['a', 'b'].total()` não compila.

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}
```

Dentro da extension, `this` é a lista, então você pode percorrê-la, indexá-la ou chamar `length` como de costume.

# --instructions--

Ordene os itens para declarar uma extension `Totals` em `List<int>` com um método `total` que soma os elementos usando um loop `for`, e depois um `main` que imprime o total de `[3, 4, 5]`. Declare a extension antes do `main`. O programa deve imprimir:

```
12
```

# --answers--

```dart
extension Totals on List<int> {
```

```dart
  int total() {
```

```dart
    var sum = 0;
```

```dart
    for (final n in this) {
```

```dart
      sum += n;
```

```dart
    }
```

```dart
    return sum;
```

```dart
  }
```

```dart
}
```

```dart
void main() {
```

```dart
  print([3, 4, 5].total());
```

```dart
}
```

# --solutions--

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}

void main() {
  print([3, 4, 5].total());
}
```

# --output--

12
