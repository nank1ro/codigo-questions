---
language: dart
exerciseType: 4
---

# --description--

Extensions não são apenas para tipos do SDK: você também pode estender **suas próprias classes**. Isso é útil quando a classe vem de um pacote que você não controla, ou quando você quer manter a classe pequena e adicionar auxiliares opcionais ao lado do código que precisa deles.

```dart
class Circle {
  final double radius;
  Circle(this.radius);
}

extension CircleMath on Circle {
  double get diameter => radius * 2;
}

void main() {
  print(Circle(3).diameter); // 6.0
}
```

A extension enxerga os campos e métodos públicos da classe, exatamente como código escrito fora da classe.

# --instructions--

Ordene os itens para definir uma classe `Rectangle` com `width` e `height`, depois uma extension `RectangleMath` em `Rectangle` com um getter `area`, e depois um `main` que imprime a área de um retângulo de `4` por `2.5`. Defina primeiro a classe, depois a extension, depois o `main`. Dentro da classe, declare ambos os campos antes do construtor. O programa deve imprimir:

```
10.0
```

# --answers--

```dart
class Rectangle {
```

```dart
  final double width;
```

```dart
  final double height;
```

```dart
  Rectangle(this.width, this.height);
```

```dart
}
```

```dart
extension RectangleMath on Rectangle {
```

```dart
  double get area => width * height;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Rectangle(4, 2.5).area);
```

```dart
}
```

# --solutions--

```dart
class Rectangle {
  final double width;
  final double height;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

```dart
class Rectangle {
  final double height;
  final double width;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

# --output--

10.0
