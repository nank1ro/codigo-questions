---
language: dart
exerciseType: 1
---

# --description--

O Dart permite que um tipo defina o que operadores como `+`, `*` ou `==` significam para seus valores, usando um método cujo nome é a palavra-chave `operator` seguida do símbolo. O lado direito do operador é o parâmetro do método:

```dart
extension Scaling on List<int> {
  List<int> operator *(int factor) => map((n) => n * factor).toList();
}

void main() {
  print([1, 2, 3] * 10); // [10, 20, 30]
}
```

Como extensions podem declarar operadores, você pode dar a um tipo existente um novo operador que ele ainda não tem. `String` tem `+` e `*`, mas não `-`, então uma extension pode definir o que `'hello world' - 'o'` significa.

# --instructions--

Escreva uma extension chamada `Removing` em `String` com um operador `-` que recebe um parâmetro `String` `part` e retorna a string sem todas as ocorrências de `part`. Use `replaceAll`.

Exemplo:
```dart
print('hello world' - 'o'); // hell wrld
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`'hello world' - 'o'` deve ser `'hell wrld'`

```dart
  test('test1', () {
    expect('hello world' - 'o', 'hell wrld', reason: '--err-t1--');
  });
```

`'banana' - 'an'` deve ser `'ba'`

```dart
  test('test2', () {
    expect('banana' - 'an', 'ba', reason: '--err-t2--');
  });
```

`'abc' - 'z'` deve ser `'abc'` e `'aaa' - 'a'` deve ser `''`

```dart
  test('test3', () {
    expect('abc' - 'z', 'abc', reason: '--err-t3--');
    expect('aaa' - 'a', '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Removing on String {
  String operator -(String part) => replaceAll(part, '');
}
```
