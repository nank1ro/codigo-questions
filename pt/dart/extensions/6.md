---
language: dart
exerciseType: 2
---

# --description--

Uma extension em `String` não pode ser chamada em uma `String?`: o valor pode ser `null`, e o compilador recusa a chamada. Se você declarar a extension no tipo **nullable** em vez disso, o método pode ser chamado diretamente em uma `String?`, e dentro dela `this` tem tipo `String?`, então você mesmo deve tratar o caso `null`, por exemplo com `??`:

```dart
extension Defaults on int? {
  int orZero() => this ?? 0;
}

void main() {
  int? count = null;
  print(count.orZero()); // 0
  print(5.orZero());     // 5
}
```

Um `int` non-nullable pode ser passado onde um `int?` é esperado, então a extension funciona em ambos.

# --instructions--

Complete a extension para que `orEmpty` possa ser chamado em uma string nullable e retorne a própria string, ou a string vazia quando ela for `null`. O programa deve imprimir `[]` e `[Ada]`.

# --seed--

```dart
extension Defaults on [/] {
  String orEmpty() => this [/] '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --answers--

- String?
- ??
- String
- +
- ?.
- !

# --solutions--

```dart
extension Defaults on String? {
  String orEmpty() => this ?? '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --output--

[]
[Ada]
