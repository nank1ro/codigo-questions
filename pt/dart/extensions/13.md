---
language: dart
exerciseType: 2
---

# --description--

Uma extension genérica pode receber **funções** como parâmetros, exatamente como `where` e `map` fazem. O tipo da função é escrito com o tipo de elemento `T`, então o callback recebe elementos do tipo correto:

```dart
extension Checks<T> on List<T> {
  bool all(bool Function(T) test) {
    for (final item in this) {
      if (!test(item)) return false;
    }
    return true;
  }
}

void main() {
  print([2, 4, 6].all((n) => n.isEven)); // true
}
```

# --instructions--

Complete a extension para que `countWhere` retorne quantos elementos satisfazem a função `test` dada, de modo que o programa imprima `2` e depois `1`.

# --seed--

```dart
extension Counting<T> on List<T> {
  int countWhere([/] Function(T) test) {
    var count = 0;
    for (final item in [/]) {
      if (test(item)) count++;
    }
    return count;
  }
}

void main() {
  print([1, 2, 3, 4].countWhere((n) => n.isEven));
  print(['a', 'bb', 'c'].countWhere((s) => s.length > 1));
}
```

# --answers--

- bool
- this
- int
- void
- T
- list

# --solutions--

```dart
extension Counting<T> on List<T> {
  int countWhere(bool Function(T) test) {
    var count = 0;
    for (final item in this) {
      if (test(item)) count++;
    }
    return count;
  }
}

void main() {
  print([1, 2, 3, 4].countWhere((n) => n.isEven));
  print(['a', 'bb', 'c'].countWhere((s) => s.length > 1));
}
```

# --output--

2
1
