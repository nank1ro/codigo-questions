---
language: dart
exerciseType: 1
---

# --description--

Uma extension também pode declarar **getters**, que são lidos como propriedades, sem parênteses. Dentro de uma extension você pode chamar os membros do próprio tipo diretamente: `this.` é opcional, exatamente como dentro de uma classe.

```dart
extension Sizes on String {
  bool get isLong => length > 10;      // same as this.length
  String get firstChar => this[0];
}

void main() {
  print('Dart'.isLong);           // false
  print('extension'.firstChar);   // e
}
```

Escolha um getter quando o membro apenas **lê** um valor e não recebe parâmetros; escolha um método quando ele faz um trabalho ou precisa de argumentos.

# --instructions--

Escreva uma extension chamada `Blank` em `String` com um getter `isBlank` do tipo `bool` que é `true` quando a string está vazia ou contém apenas espaços. Use `trim()` e `isEmpty`.

Exemplo:
```dart
print('   '.isBlank);  // true
print(' hi '.isBlank); // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`''.isBlank` e `'   '.isBlank` devem ser `true`

```dart
  test('test1', () {
    expect(''.isBlank, true, reason: '--err-t1--');
    expect('   '.isBlank, true, reason: '--err-t1--');
  });
```

`' hi '.isBlank` deve ser `false`

```dart
  test('test2', () {
    expect(' hi '.isBlank, false, reason: '--err-t2--');
    expect('x'.isBlank, false, reason: '--err-t2--');
  });
```

`isBlank` deve ser um `bool`

```dart
  test('test3', () {
    expect('  '.isBlank, isA<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Blank on String {
  bool get isBlank => trim().isEmpty;
}
```
