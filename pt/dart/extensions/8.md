---
language: dart
exerciseType: 1
---

# --description--

Uma extension pode declarar membros **static**. Como em uma classe, eles pertencem à própria extension, não a nenhum valor, e são acessados através do **nome da extension**, não através do tipo que ela estende:

```dart
extension Temperatures on double {
  static const double boiling = 100.0;

  static bool isBoiling(double celsius) => celsius >= boiling;
}

void main() {
  print(Temperatures.boiling);         // 100.0
  print(Temperatures.isBoiling(37.5)); // false
  print(double.boiling);               // error: 'boiling' isn't defined for 'double'
}
```

Membros static são um lugar prático para constantes e funções auxiliares relacionadas ao tipo estendido.

# --instructions--

Escreva uma extension chamada `Dice` em `int` com:
- uma constante static `sides` do tipo `int` com valor `6`
- um método static `isValid` que recebe um parâmetro `int` `value` e retorna `true` quando ele está entre `1` e `sides` (ambos incluídos)

Exemplo:
```dart
print(Dice.sides);      // 6
print(Dice.isValid(7)); // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Dice.sides` deve ser `6`

```dart
  test('test1', () {
    expect(Dice.sides, 6, reason: '--err-t1--');
  });
```

`Dice.isValid(1)` e `Dice.isValid(6)` devem ser `true`

```dart
  test('test2', () {
    expect(Dice.isValid(1), true, reason: '--err-t2--');
    expect(Dice.isValid(6), true, reason: '--err-t2--');
  });
```

`Dice.isValid(0)` e `Dice.isValid(7)` devem ser `false`

```dart
  test('test3', () {
    expect(Dice.isValid(0), false, reason: '--err-t3--');
    expect(Dice.isValid(7), false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Dice on int {
  static const int sides = 6;

  static bool isValid(int value) => value >= 1 && value <= sides;
}
```
