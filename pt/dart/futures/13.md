---
language: dart
exerciseType: 1
---

# --description--

Você não precisa de `Future.wait` para rodar dois futures ao mesmo tempo. Uma função `async` começa a rodar assim que é **chamada**, até o seu primeiro `await`; o future que você recebe de volta é o trabalho já em andamento. O truque, então, é: **chamar primeiro, aguardar depois**:

```dart
Future<int> pages() => Future.delayed(const Duration(milliseconds: 20), () => 300);
Future<int> words() => Future.delayed(const Duration(milliseconds: 20), () => 90000);

Future<double> average() async {
  final p = pages();          // both timers start now
  final w = words();
  return await w / await p;   // waits once, about 20 ms in total
}
```

Compare com `return await words() / await pages();`, onde `pages()` só é chamada depois que `words()` concluiu: mesmo resultado, o dobro do tempo. Prefira a forma concorrente sempre que a segunda chamada não precisar do resultado da primeira.

# --instructions--

A função `slow(value)` retorna um `Future<int>` que conclui com `value` depois de um pequeno atraso, e ela registra `start <value>` quando é chamada e `end <value>` quando conclui na lista `calls`. Escreva uma função `async` `addConcurrently` que recebe dois `int` `a` e `b`, chama `slow(a)` e `slow(b)` de forma que os dois rodem **ao mesmo tempo**, e retorna um `Future<int>` que conclui com a soma dos dois resultados.

Exemplo:
```dart
print(await addConcurrently(2, 3)); // 5
print(calls); // [start 2, start 3, end 2, end 3]
```

# --before-seed--

```dart
final calls = <String>[];

Future<int> slow(int value) async {
  calls.add('start $value');
  await Future.delayed(const Duration(milliseconds: 5));
  calls.add('end $value');
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`addConcurrently(2, 3)` deveria concluir com `5`

```dart
  test('test1', () async {
    calls.clear();
    expect(await addConcurrently(2, 3), 5, reason: '--err-t1--');
  });
```

`addConcurrently(2, 3)` deveria chamar `slow(2)` e `slow(3)` antes que qualquer um conclua, então `calls` deveria ser `[start 2, start 3, end 2, end 3]`

```dart
  test('test2', () async {
    calls.clear();
    await addConcurrently(2, 3);
    expect(calls, ['start 2', 'start 3', 'end 2', 'end 3'], reason: '--err-t2--');
  });
```

`addConcurrently(10, -4)` deveria concluir com `6` e as duas chamadas deveriam começar antes que qualquer uma conclua

```dart
  test('test3', () async {
    calls.clear();
    expect(await addConcurrently(10, -4), 6, reason: '--err-t3--');
    expect(calls, ['start 10', 'start -4', 'end 10', 'end -4'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> addConcurrently(int a, int b) async {
  final first = slow(a);
  final second = slow(b);
  return await first + await second;
}
```
