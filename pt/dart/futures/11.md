---
language: dart
exerciseType: 1
---

# --description--

Quando vários futures não dependem uns dos outros, entregue todos a **`Future.wait`**: ele recebe uma `List<Future<T>>`, deixa que rodem ao mesmo tempo e retorna um único `Future<List<T>>` que conclui quando **todos** terminam. Os resultados mantêm a ordem da lista de entrada, independentemente de qual future terminou primeiro:

```dart
Future<String> slow() => Future.delayed(const Duration(milliseconds: 20), () => 'slow');
Future<String> fast() => Future.delayed(const Duration(milliseconds: 5), () => 'fast');

final results = await Future.wait([slow(), fast()]);
print(results); // [slow, fast]
```

`await slow(); await fast();` leva `25` milissegundos, porque `fast()` só é chamada depois que `slow()` concluiu; `await Future.wait([slow(), fast()])` leva cerca de `20`, a duração do mais longo.

# --instructions--

Escreva uma função `async` `sumOfFutures` que recebe uma `List<Future<int>>` `futures`, espera por todos com `Future.wait` e retorna um `Future<int>` que conclui com a soma dos valores deles. Uma lista vazia dá `0`.

Exemplo:
```dart
final futures = [Future.value(1), Future.delayed(const Duration(milliseconds: 5), () => 2)];
print(await sumOfFutures(futures)); // 3
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumOfFutures` de futures que concluem com `1`, `2` e `3` deveria concluir com `6`

```dart
  test('test1', () async {
    final futures = [
      Future.value(1),
      Future.delayed(const Duration(milliseconds: 5), () => 2),
      Future.value(3),
    ];
    expect(await sumOfFutures(futures), 6, reason: '--err-t1--');
  });
```

`sumOfFutures` de um único future que conclui com `10` deveria concluir com `10`

```dart
  test('test2', () async {
    expect(await sumOfFutures([Future.delayed(const Duration(milliseconds: 5), () => 10)]), 10, reason: '--err-t2--');
  });
```

`sumOfFutures` de uma lista vazia deveria concluir com `0` e `sumOfFutures` deveria retornar um `Future<int>`

```dart
  test('test3', () async {
    expect(await sumOfFutures(<Future<int>>[]), 0, reason: '--err-t3--');
    expect(sumOfFutures([Future.value(4)]), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> sumOfFutures(List<Future<int>> futures) async {
  final values = await Future.wait(futures);
  var total = 0;
  for (final v in values) {
    total += v;
  }
  return total;
}
```
