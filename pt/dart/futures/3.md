---
language: dart
exerciseType: 1
---

# --description--

`Future.value` conclui na hora. Para simular um trabalho que leva tempo, use **`Future.delayed`**: ele recebe uma `Duration` e uma função, espera a duração e então conclui com o que a função retorna:

```dart
Future<String> slowHello() {
  return Future.delayed(const Duration(milliseconds: 10), () => 'hello');
}
```

`Duration` é construída com parâmetros nomeados como `seconds`, `milliseconds` ou `minutes`. Dentro de uma função `async` você também pode aguardar um atraso sozinho, sem valor, apenas para pausar:

```dart
Future<int> slowDouble(int n) async {
  await Future.delayed(const Duration(milliseconds: 10));
  return n * 2;
}
```

Os dois estilos são comuns; o segundo se lê como código sequencial comum.

# --instructions--

Escreva uma função `slowSquare` que recebe um `int` `n` e retorna um `Future<int>` que conclui com `n * n` depois de um atraso de `10` milissegundos.

Exemplo:
```dart
print(await slowSquare(4)); // 16, after 10 ms
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`slowSquare(4)` deveria concluir com `16`

```dart
  test('test1', () async {
    expect(await slowSquare(4), 16, reason: '--err-t1--');
  });
```

`slowSquare(-3)` deveria concluir com `9` e `slowSquare(0)` com `0`

```dart
  test('test2', () async {
    expect(await slowSquare(-3), 9, reason: '--err-t2--');
    expect(await slowSquare(0), 0, reason: '--err-t2--');
  });
```

`slowSquare` deveria retornar um `Future<int>` que leva pelo menos `10` milissegundos para concluir

```dart
  test('test3', () async {
    final future = slowSquare(5);
    expect(future, isA<Future<int>>(), reason: '--err-t3--');
    final watch = Stopwatch()..start();
    await future;
    expect(watch.elapsedMilliseconds, greaterThanOrEqualTo(9), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> slowSquare(int n) {
  return Future.delayed(const Duration(milliseconds: 10), () => n * n);
}
```
