---
language: dart
exerciseType: 2
---

# --description--

Um erro se **propaga** por todo `await` que não o captura. Se `load()` falha, `await load()` dentro de `loadTwice()` lança; como `loadTwice` não tem `try`/`catch`, o próprio future dela falha com o mesmo erro; e assim por diante cadeia acima, até que algum `await` esteja envolvido em um `try`/`catch`:

```dart
Future<int> load() async {
  throw StateError('offline');
}

Future<int> loadTwice() async {
  final n = await load();   // throws here, loadTwice fails too
  return n * 2;             // never runs
}

Future<void> main() async {
  try {
    print(await loadTwice());
  } catch (e) {
    print('failed: $e');    // failed: Bad state: offline
  }
}
```

Isso espelha como as exceções se propagam pelas chamadas síncronas: você as trata uma vez, no nível que sabe o que fazer.

# --instructions--

Complete o código para que `fetch` seja assíncrona e lance, `fetchLength` a aguarde, e `main` capture o erro e imprima `failed: ` seguido dele. O programa deve imprimir `failed: Bad state: no server`.

# --seed--

```dart
Future<String> fetch() [/] {
  throw StateError('no server');
}

Future<int> fetchLength() async {
  final text = [/] fetch();
  return text.length;
}

Future<void> main() async {
  [/] {
    print(await fetchLength());
  } [/] (e) {
    print('failed: $e');
  }
}
```

# --answers--

- async
- await
- try
- catch
- then
- finally
- sync*

# --solutions--

```dart
Future<String> fetch() async {
  throw StateError('no server');
}

Future<int> fetchLength() async {
  final text = await fetch();
  return text.length;
}

Future<void> main() async {
  try {
    print(await fetchLength());
  } catch (e) {
    print('failed: $e');
  }
}
```

# --output--

failed: Bad state: no server
