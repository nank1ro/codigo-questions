---
language: dart
exerciseType: 1
---

# --description--

Marcar uma função como `async` faz duas coisas: permite `await` no corpo e faz a função **retornar um `Future`**. O que você devolve com `return` vira o valor com que o future é concluído, então o tipo de retorno declarado é `Future<T>` mesmo que o corpo devolva um `T` puro:

```dart
Future<String> label(int n) async {
  return 'item $n';
}

Future<void> main() async {
  print(await label(3)); // item 3
}
```

Aqui você não precisa de `Future.value`: a palavra-chave `async` embrulha o valor retornado para você.

# --instructions--

Escreva uma função `async` `greet` que recebe uma `String` `name` e retorna um `Future<String>` que conclui com `Hello, ` seguido do nome e de um ponto de exclamação.

Exemplo:
```dart
print(await greet('Ada')); // Hello, Ada!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greet('Ada')` deveria concluir com `'Hello, Ada!'`

```dart
  test('test1', () async {
    expect(await greet('Ada'), 'Hello, Ada!', reason: '--err-t1--');
  });
```

`greet('Linus')` deveria concluir com `'Hello, Linus!'`

```dart
  test('test2', () async {
    expect(await greet('Linus'), 'Hello, Linus!', reason: '--err-t2--');
  });
```

`greet` deveria retornar um `Future<String>`

```dart
  test('test3', () async {
    expect(greet('Bob'), isA<Future<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<String> greet(String name) async {
  return 'Hello, $name!';
}
```
