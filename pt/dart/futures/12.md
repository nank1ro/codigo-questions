---
language: dart
exerciseType: 4
---

# --description--

`Future.wait` é a ferramenta para "carregar várias coisas e então continuar". O formato típico é: montar a lista de futures, fazer `await Future.wait` nela e então usar a lista resultante:

```dart
Future<int> stock() => Future.delayed(const Duration(milliseconds: 10), () => 8);
Future<int> orders() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<void> main() async {
  final counts = await Future.wait([stock(), orders()]);
  print(counts); // [8, 3]
}
```

`orders()` conclui primeiro, mas a lista ainda segue a ordem das chamadas: `stock()` primeiro, `orders()` em segundo.

# --instructions--

Ordene os itens para definir uma função `one` que conclui com `1` depois de `10` milissegundos, uma função `two` que conclui com `2` depois de `5` milissegundos, e uma `main` `async` que imprime `loading`, aguarda as duas funções com `Future.wait` em `values` e imprime `values`. Defina `one` primeiro, depois `two`, depois `main`. O programa deve imprimir:

```
loading
[1, 2]
```

# --answers--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
```

```dart
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);
```

```dart
Future<void> main() async {
```

```dart
  print('loading');
```

```dart
  final values = await Future.wait([one(), two()]);
```

```dart
  print(values);
```

```dart
}
```

# --solutions--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);

Future<void> main() async {
  print('loading');
  final values = await Future.wait([one(), two()]);
  print(values);
}
```

# --output--

loading
[1, 2]
