---
language: dart
exerciseType: 1
---

# --description--

Um future também pode concluir com um **erro**. Quando uma função `async` lança, a exceção não escapa na hora: ela vira o erro do future retornado. Quem faz `await` nesse future vê o erro lançado no `await`, então ele pode ser tratado com um `try`/`catch` comum:

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s); // throws FormatException for 'abc'
}

Future<int> orZero(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return 0;
  }
}
```

O `await` dentro do `try` é essencial: `return parseLater(s);` entregaria o future ao chamador **sem esperar**, então o erro chegaria depois que o bloco `try` já tivesse terminado e o `catch` nunca rodaria.

# --instructions--

A função `parseLater(s)` retorna um `Future<int>` que conclui com o número escrito em `s`, ou com um erro se `s` não for um número. Escreva uma função `async` `safeParse` que recebe uma `String` `s` e retorna um `Future<int>` que conclui com o número interpretado, ou com `-1` se `parseLater` falhar.

Exemplo:
```dart
print(await safeParse('12'));  // 12
print(await safeParse('abc')); // -1
```

# --before-seed--

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s);
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeParse('12')` deveria concluir com `12`

```dart
  test('test1', () async {
    expect(await safeParse('12'), 12, reason: '--err-t1--');
  });
```

`safeParse('abc')` deveria concluir com `-1` em vez de lançar

```dart
  test('test2', () async {
    expect(await safeParse('abc'), -1, reason: '--err-t2--');
  });
```

`safeParse('-7')` deveria concluir com `-7` e `safeParse('')` com `-1`

```dart
  test('test3', () async {
    expect(await safeParse('-7'), -7, reason: '--err-t3--');
    expect(await safeParse(''), -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> safeParse(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return -1;
  }
}
```
