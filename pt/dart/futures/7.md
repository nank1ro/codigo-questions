---
language: dart
exerciseType: 4
---

# --description--

Quando uma função chega a um `await`, ela **pausa** naquela linha e o resto do programa continua. As linhas depois do `await` só rodam quando o future é concluído. Ler uma função `async` de cima para baixo, portanto, mostra a ordem exata dos seus efeitos:

```dart
Future<int> load() async {
  await Future.delayed(const Duration(milliseconds: 5));
  return 7;
}

Future<void> main() async {
  print('loading');
  final n = await load();
  print('value: $n');
  print('done');
}
// loading
// value: 7
// done
```

# --instructions--

Ordene os itens para definir uma função `async` `fetchName` que espera `10` milissegundos e então retorna `'Ada'`, e uma `main` `async` que imprime `fetching`, depois aguarda `fetchName()` em `name`, depois imprime `Hello, ` seguido do nome e de `!`, e por fim imprime `done`. Defina `fetchName` antes de `main`. O programa deve imprimir:

```
fetching
Hello, Ada!
done
```

# --answers--

```dart
Future<String> fetchName() async {
```

```dart
  await Future.delayed(const Duration(milliseconds: 10));
```

```dart
  return 'Ada';
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  print('fetching');
```

```dart
  final name = await fetchName();
```

```dart
  print('Hello, $name!');
```

```dart
  print('done');
```

```dart
}
```

# --solutions--

```dart
Future<String> fetchName() async {
  await Future.delayed(const Duration(milliseconds: 10));
  return 'Ada';
}

Future<void> main() async {
  print('fetching');
  final name = await fetchName();
  print('Hello, $name!');
  print('done');
}
```

# --output--

fetching
Hello, Ada!
done
