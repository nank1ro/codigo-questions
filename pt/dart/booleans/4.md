---
language: dart
exerciseType: 2
---

# --description--

O __operador de negação__ `!` (também chamado de "não") inverte um valor booleano.
Aplicar `!` a `true` resulta em `false`, e aplicar `!` a `false` resulta em `true`.

```dart
bool isActive = true;
print(!isActive); // false
```

Isso é útil quando você quer verificar o oposto de uma condição.

# --instructions--

Dado que a variável `isBlocked` é definida como `false`, imprima seu valor negado usando `!`.

# --seed--

```dart
bool isBlocked = false;
print([/]isBlocked);
```

# --answers--

- !
- !!
- ~
- not

# --solutions--

```dart
bool isBlocked = false;
print(!isBlocked);
```

# --output--

true
