---
language: dart
exerciseType: 1
---

# --instructions--

Declare uma variável booleana chamada `flag` com o valor `true`, depois declare outra variável booleana chamada `isFlag` e use a palavra-chave `is` para verificar se `flag` é do tipo `bool`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`flag` deve ser igual a `true`.

```dart
  test('test1', () {
    expect(flag, true, reason: '--err-t1--');
  });
```

`isFlag` deve ser `true` (o resultado da verificação `is bool`).

```dart
  test('test2', () {
    expect(isFlag, true, reason: '--err-t2--');
  });
```

`isFlag` deve ser do tipo `bool`.

```dart
  test('test3', () {
    expect(isFlag, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool flag = true;
bool isFlag = flag is bool;
```
