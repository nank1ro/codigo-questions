---
language: dart
exerciseType: 1
---

# --instructions--

Declare uma variável booleana chamada `isHidden` e atribua a ela o valor `false`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHidden` deve ser igual a `false`.

```dart
  test('test1', () {
    expect(isHidden, false, reason: '--err-t1--');
  });
```

`isHidden` deve ser do tipo `bool`.

```dart
  test('test2', () {
    expect(isHidden, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isHidden = false;
```
