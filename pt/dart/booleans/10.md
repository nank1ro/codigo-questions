---
language: dart
exerciseType: 1
---

# --instructions--

O operador `&&` (AND) retorna `true` apenas quando **ambos** os operandos são `true`.

Declare uma variável booleana chamada `result` que combina `true && true` usando o operador `&&`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` deve ser igual a `true`.

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result` deve ser do tipo `bool`.

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = true && true;
```
