---
language: dart
exerciseType: 1
---

# --description--

A última peça é lançar um `Error` de propósito. Uma função que documenta o que aceita deve recusar qualquer outra coisa ruidosamente, e `ArgumentError` é o objeto para isso:

```dart
int setVolume(int level) {
  if (level < 0 || level > 100) {
    throw ArgumentError('level must be between 0 and 100');
  }
  return level;
}
```

A mensagem é acessível como `e.message`, e `toString()` imprime `Invalid argument(s): ` seguido dela.

Isso não contradiz a regra de antes. Lançar um `ArgumentError` é certo, capturar um não é: ele diz ao autor do *chamador* que a chamada em si está errada, e a correção é uma verificação antes da chamada, não um tratamento ao redor dela.

# --instructions--

Escreva uma função `checkAge` que recebe um `int` `age` e o retorna, mas lança `ArgumentError('age must be between 0 and 120')` quando a idade é menor que `0` ou maior que `120`.

Exemplo:
```dart
print(checkAge(30)); // 30
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`checkAge(30)` deve retornar `30`, e as duas extremidades do intervalo devem ser aceitas

```dart
  test('test1', () {
    expect(checkAge(30), 30, reason: '--err-t1--');
    expect(checkAge(0), 0, reason: '--err-t1--');
    expect(checkAge(120), 120, reason: '--err-t1--');
  });
```

`checkAge(-1)` e `checkAge(121)` devem lançar um `ArgumentError`

```dart
  test('test2', () {
    expect(() => checkAge(-1), throwsArgumentError, reason: '--err-t2--');
    expect(() => checkAge(121), throwsArgumentError, reason: '--err-t2--');
  });
```

O erro lançado deve carregar a mensagem `'age must be between 0 and 120'`

```dart
  test('test3', () {
    try {
      checkAge(200);
      fail('--err-t3--');
    } on ArgumentError catch (e) {
      expect(e.message, 'age must be between 0 and 120', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int checkAge(int age) {
  if (age < 0 || age > 120) {
    throw ArgumentError('age must be between 0 and 120');
  }
  return age;
}
```
