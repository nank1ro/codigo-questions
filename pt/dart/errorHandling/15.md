---
language: dart
exerciseType: 1
---

# --description--

Onde o bloco `try` fica decide quanto trabalho uma única falha destrói. Ao redor do laço, o primeiro elemento ruim encerra o lote inteiro; **dentro** do laço, apenas esse elemento se perde e o resto ainda é processado:

```dart
for (final text in texts) {
  try {
    total += int.parse(text);
  } on FormatException {
    continue;
  }
}
```

Esta é a forma cotidiana para importar um arquivo, ler uma lista de configurações ou tratar uma fila de mensagens: uma linha danificada não deve descartar as boas. A regra continua a mesma de antes — mantenha o bloco `try` ao redor da instrução que pode falhar, e nada maior.

# --instructions--

Escreva uma função `sumOf` que recebe uma `List<String>` `texts` e retorna a soma das que descrevem um número inteiro, ignorando as outras.

Use `int.parse` com um `try`/`catch` dentro do laço, para que uma entrada ilegível não pare o resto.

Exemplo:
```dart
print(sumOf(['1', 'x', '3'])); // 4
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumOf(['1', 'x', '3'])` deve retornar `4`

```dart
  test('test1', () {
    expect(sumOf(['1', 'x', '3']), 4, reason: '--err-t1--');
  });
```

`sumOf(['10', '20', '30'])` deve retornar `60`

```dart
  test('test2', () {
    expect(sumOf(['10', '20', '30']), 60, reason: '--err-t2--');
    expect(sumOf(['-5', '5']), 0, reason: '--err-t2--');
  });
```

Uma lista sem número legível, e uma lista vazia, devem dar `0`

```dart
  test('test3', () {
    expect(sumOf(['x', 'y', '']), 0, reason: '--err-t3--');
    expect(sumOf(<String>[]), 0, reason: '--err-t3--');
    expect(sumOf(['x', '7']), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sumOf(List<String> texts) {
  var total = 0;
  for (final text in texts) {
    try {
      total += int.parse(text);
    } on FormatException {
      continue;
    }
  }
  return total;
}
```
