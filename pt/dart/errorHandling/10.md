---
language: dart
exerciseType: 1
---

# --description--

`Exception` é uma interface, então a sua própria classe pode ser uma. Uma exceção personalizada dá à falha um nome que uma cláusula `on` pode selecionar, e campos que um bloco de tratamento pode ler:

```dart
class EmptyCartException implements Exception {
  final String message;

  EmptyCartException(this.message);

  @override
  String toString() => 'EmptyCartException: $message';
}

throw EmptyCartException('nothing to pay for');
```

Três partes valem a pena manter: `implements Exception` para que a classe pertença junto das outras falhas, um campo `final` carregando o detalhe, e um `toString()` sobrescrito para que o relatório de exceção não tratada seja legível. Sem essa sobrescrita, o Dart imprime o nome puro da classe e o detalhe se perde.

# --instructions--

Defina uma classe `InvalidEmailException` que implementa `Exception`, com um campo `final String` `message` definido pelo construtor, e um `toString()` que retorna `'InvalidEmailException: '` seguido da mensagem.

Depois escreva uma função `checkEmail` que recebe uma `String` `email` e a retorna, mas lança `InvalidEmailException('missing @')` quando o email não contém `'@'`.

Exemplo:
```dart
print(checkEmail('ada@dart.dev')); // ada@dart.dev
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`checkEmail('ada@dart.dev')` deve retornar o email sem alterações

```dart
  test('test1', () {
    expect(checkEmail('ada@dart.dev'), 'ada@dart.dev', reason: '--err-t1--');
  });
```

`checkEmail('nope')` deve lançar uma `InvalidEmailException`, que é uma `Exception`

```dart
  test('test2', () {
    expect(() => checkEmail('nope'), throwsA(isA<InvalidEmailException>()), reason: '--err-t2--');
    expect(() => checkEmail('nope'), throwsA(isA<Exception>()), reason: '--err-t2--');
  });
```

O objeto lançado deve carregar a mensagem `'missing @'` e ser impresso como `InvalidEmailException: missing @`

```dart
  test('test3', () {
    try {
      checkEmail('nope');
      fail('--err-t3--');
    } on InvalidEmailException catch (e) {
      expect(e.message, 'missing @', reason: '--err-t3--');
      expect(e.toString(), 'InvalidEmailException: missing @', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class InvalidEmailException implements Exception {
  final String message;

  InvalidEmailException(this.message);

  @override
  String toString() => 'InvalidEmailException: $message';
}

String checkEmail(String email) {
  if (!email.contains('@')) {
    throw InvalidEmailException('missing @');
  }
  return email;
}
```
