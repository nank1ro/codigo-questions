---
language: dart
exerciseType: 1
---

# --description--

`firstWhere` retorna o primeiro elemento que corresponde a um teste. Quando nada corresponde, não há elemento para retornar, então ele lança um `StateError`:

```dart
final words = ['a', 'fg'];
print(words.firstWhere((w) => w.length > 3)); // Bad state: No element
```

Como `int.tryParse`, a biblioteca oferece uma saída. O parâmetro nomeado `orElse` recebe uma função que produz o valor a usar quando nada correspondeu:

```dart
print(words.firstWhere((w) => w.length > 3, orElse: () => 'none')); // none
```

A escolha é a mesma de antes: `orElse` quando "nada correspondeu" é um desfecho comum, a chamada simples quando significaria que os dados estão quebrados e alguém precisa saber.

# --instructions--

Escreva duas funções que recebem uma `List<String>` `words`.

`firstLong` retorna a primeira palavra com mais de três caracteres usando `firstWhere`, ou `'none'` quando não há nenhuma, com `orElse`.

`firstLongStrict` faz o mesmo com `firstWhere`, mas sem `orElse`, então lança um `StateError` quando não há nenhuma.

Exemplo:
```dart
print(firstLong(['a', 'bcde', 'fg'])); // bcde
print(firstLong(['a', 'fg'])); // none
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`firstLong(['a', 'bcde', 'fg'])` deve retornar `'bcde'`

```dart
  test('test1', () {
    expect(firstLong(['a', 'bcde', 'fg']), 'bcde', reason: '--err-t1--');
    expect(firstLong(['long', 'longer']), 'long', reason: '--err-t1--');
  });
```

`firstLong` deve retornar `'none'` quando nenhuma palavra tem mais de três caracteres

```dart
  test('test2', () {
    expect(firstLong(['a', 'fg']), 'none', reason: '--err-t2--');
    expect(firstLong(<String>[]), 'none', reason: '--err-t2--');
    expect(firstLong(['abc']), 'none', reason: '--err-t2--');
  });
```

`firstLongStrict` deve retornar a palavra quando há uma e lançar um `StateError` caso contrário

```dart
  test('test3', () {
    expect(firstLongStrict(['a', 'bcde']), 'bcde', reason: '--err-t3--');
    expect(() => firstLongStrict(['a', 'fg']), throwsStateError, reason: '--err-t3--');
    expect(() => firstLongStrict(<String>[]), throwsStateError, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String firstLong(List<String> words) {
  return words.firstWhere((w) => w.length > 3, orElse: () => 'none');
}

String firstLongStrict(List<String> words) {
  return words.firstWhere((w) => w.length > 3);
}
```
