---
language: dart
exerciseType: 1
---

# --description--

Às vezes um bloco de tratamento não é o lugar certo para se recuperar: você só quer *notar* a falha e deixá-la continuar para o chamador que realmente pode lidar com ela. A palavra-chave `rethrow` faz isso, dentro de um bloco `catch` ou `on ... catch`:

```dart
try {
  return int.parse(text);
} on FormatException {
  log.add('bad input: $text');
  rethrow;
}
```

`rethrow` envia o **mesmo** objeto adiante, então o chamador vê a falha original. Escrever `throw e` em vez disso também funcionaria, mas reinicia a jornada e perde onde a falha aconteceu primeiro.

Um bloco `finally` na mesma instrução ainda roda, mesmo na saída.

# --instructions--

Escreva uma função `parseOrLog` que recebe uma `String` `text` e uma `List<String>` `log`, e retorna `int.parse(text)`.

Quando o texto não for um número inteiro, adicione a string `'bad input: '` seguida do texto a `log`, e então relance a `FormatException` para que o chamador ainda a veja.

Exemplo:
```dart
final log = <String>[];
print(parseOrLog('42', log)); // 42
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parseOrLog('42', log)` deve retornar `42` e deixar o log vazio

```dart
  test('test1', () {
    final log = <String>[];
    expect(parseOrLog('42', log), 42, reason: '--err-t1--');
    expect(log, isEmpty, reason: '--err-t1--');
  });
```

`parseOrLog('oops', log)` deve lançar uma `FormatException`

```dart
  test('test2', () {
    expect(() => parseOrLog('oops', <String>[]), throwsFormatException, reason: '--err-t2--');
  });
```

O log deve coletar uma linha por falha, e valores bons não devem ser registrados

```dart
  test('test3', () {
    final log = <String>[];
    expect(() => parseOrLog('oops', log), throwsFormatException, reason: '--err-t3--');
    expect(log, ['bad input: oops'], reason: '--err-t3--');
    expect(parseOrLog('7', log), 7, reason: '--err-t3--');
    expect(() => parseOrLog('x', log), throwsFormatException, reason: '--err-t3--');
    expect(log, ['bad input: oops', 'bad input: x'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int parseOrLog(String text, List<String> log) {
  try {
    return int.parse(text);
  } on FormatException {
    log.add('bad input: $text');
    rethrow;
  }
}
```
