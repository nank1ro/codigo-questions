---
language: dart
exerciseType: 1
---

# --instructions--
Escreva uma função que retorne o primeiro número negativo encontrado em uma `List<int>`. Use um laço `for` com `break` para parar assim que um número negativo for encontrado. Retorne `0` se não houver número negativo.

# --before-seed--
```dart
int firstNegative(List<int> numbers) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Encontra o primeiro número negativo
```dart
    test('finds first negative', () {
      expect(firstNegative([3, -5, -2, 1]), -5, reason: '--err-t1--');
    });
```

Retorna 0 quando não há negativo
```dart
    test('no negative', () {
      expect(firstNegative([1, 2, 3]), 0, reason: '--err-t2--');
    });
```

O primeiro elemento é negativo
```dart
    test('first is negative', () {
      expect(firstNegative([-7, 4, 2]), -7, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = 0; i < numbers.length; i++) {
    if (numbers[i] < 0) {
      result = numbers[i];
      break;
    }
  }
```
