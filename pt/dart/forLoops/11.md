---
language: dart
exerciseType: 1
---

# --instructions--
Escreva uma função que retorne a quantidade de números pares no intervalo de `start` a `end` (ambos inclusive).

# --before-seed--
```dart
int countEvens(int start, int end) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
O intervalo de 1 a 10 tem 5 pares
```dart
    test('1 to 10', () {
      expect(countEvens(1, 10), 5, reason: '--err-t1--');
    });
```

O intervalo de 2 a 6 tem 3 pares
```dart
    test('2 to 6', () {
      expect(countEvens(2, 6), 3, reason: '--err-t2--');
    });
```

O intervalo de 1 a 1 tem 0 pares
```dart
    test('1 to 1', () {
      expect(countEvens(1, 1), 0, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = start; i <= end; i++) {
    if (i % 2 == 0) result++;
  }
```
