---
language: dart
exerciseType: 2
---

# --description--
O laço `for-in` itera sobre cada elemento de uma coleção sem precisar de um índice:

```dart
List<String> fruits = ['apple', 'banana', 'cherry'];
for (var fruit in fruits) {
  print(fruit);
}
```

Cada iteração atribui o próximo elemento à variável do laço (`fruit` aqui).

# --instructions--
Complete o laço `for-in` que imprime cada número da lista.

# --seed--
```dart
List<int> numbers = [10, 20, 30];
for ([/] n [/] numbers) {
  [/](n);
}
```

# --answers--

- var
- in
- print
- int
- of
- write
- final
- println

# --solutions--
```dart
List<int> numbers = [10, 20, 30];
for (var n in numbers) {
  print(n);
}
```

# --output--
10
20
30
