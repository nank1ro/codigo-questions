---
language: dart
exerciseType: 2
---

# --description--

Dois outros métodos muito comuns que recebem uma função anônima são `map` e `where`:

- `map` transforma cada elemento com a função e retorna os novos valores
- `where` mantém apenas os elementos para os quais a função retorna `true`

Ambos retornam um `Iterable` preguiçoso; chame `toList()` para transformar o resultado em uma `List`:

```dart
var numbers = [1, 2, 3];

var squares = numbers.map((n) => n * n).toList();
print(squares); // [1, 4, 9]

var big = numbers.where((n) => n > 1).toList();
print(big); // [2, 3]
```

# --instructions--

Complete o código para que `doubled` contenha cada número multiplicado por `2` e `evens` contenha apenas os números pares.

# --seed--

```dart
void main() {
  var numbers = [1, 2, 3, 4];
  var doubled = numbers.[/]((n) => n * 2).toList();
  var evens = numbers.[/]((n) => n % 2 == 0).toList();
  print(doubled);
  print(evens);
}
```

# --answers--

- map
- where
- filter
- forEach
- select
- each

# --solutions--

```dart
void main() {
  var numbers = [1, 2, 3, 4];
  var doubled = numbers.map((n) => n * 2).toList();
  var evens = numbers.where((n) => n % 2 == 0).toList();
  print(doubled);
  print(evens);
}
```

# --output--

[2, 4, 6, 8]
[2, 4]
