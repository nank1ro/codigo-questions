---
language: dart
exerciseType: 1
---

# --description--

Funções que recebem e retornam outras funções podem ser combinadas livremente. Um exemplo clássico é a **composição**: construir uma nova função que executa uma função e depois alimenta seu resultado em outra:

```dart
int Function(int) then(int Function(int) first, int Function(int) second) {
  return (n) => second(first(n));
}
```

# --instructions--

Escreva uma função `compose` que recebe dois parâmetros `f` e `g`, ambos do tipo `int Function(int)`, e retorna uma função do tipo `int Function(int)` que aplica `f` ao seu argumento e depois `g` ao resultado.

Exemplo:
```dart
var addThenDouble = compose((n) => n + 1, (n) => n * 2);
print(addThenDouble(3)); // 8
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`compose((n) => n + 1, (n) => n * 2)(3)` deve ser `8`

```dart
  test('test1', () {
    expect(compose((n) => n + 1, (n) => n * 2)(3), 8, reason: '--err-t1--');
  });
```

`compose((n) => n * 2, (n) => n + 1)(3)` deve ser `7`

```dart
  test('test2', () {
    expect(compose((n) => n * 2, (n) => n + 1)(3), 7, reason: '--err-t2--');
  });
```

A função retornada deve ter o tipo `int Function(int)`

```dart
  test('test3', () {
    var negate = compose((n) => -n, (n) => n * n);
    expect(negate, isA<int Function(int)>(), reason: '--err-t3--');
    expect(negate(5), 25, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int Function(int) compose(int Function(int) f, int Function(int) g) {
  return (int n) => g(f(n));
}
```
