---
language: dart
exerciseType: 1
---

# --description--

O `reduce` combina todos os elementos de uma lista em um único valor. Sua função anônima recebe dois parâmetros: o valor **acumulado até agora** e o **próximo elemento**, e retorna o novo valor acumulado. O primeiro elemento é usado como ponto de partida:

```dart
var numbers = [2, 3, 4];

var product = numbers.reduce((total, n) => total * n);
print(product); // 24
```

O `reduce` lança um erro em uma lista vazia, pois não há um primeiro elemento para começar.

# --instructions--

Escreva uma função `longest` que recebe um parâmetro `List<String>` não vazio `words` e retorna a palavra mais longa, usando `reduce` com uma função anônima. Se duas palavras tiverem o mesmo comprimento, mantenha a que vier primeiro.

Exemplo:
```dart
print(longest(['sun', 'planet', 'moon'])); // planet
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`longest(['sun', 'planet', 'moon'])` deve ser `'planet'`

```dart
  test('test1', () {
    expect(longest(['sun', 'planet', 'moon']), 'planet', reason: '--err-t1--');
  });
```

`longest(['a'])` deve ser `'a'`

```dart
  test('test2', () {
    expect(longest(['a']), 'a', reason: '--err-t2--');
  });
```

`longest(['red', 'blue', 'pink'])` deve ser `'blue'`

```dart
  test('test3', () {
    expect(longest(['red', 'blue', 'pink']), 'blue', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String longest(List<String> words) {
  return words.reduce((best, word) => word.length > best.length ? word : best);
}
```
