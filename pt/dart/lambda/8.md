---
language: dart
exerciseType: 1
---

# --description--

Como `map` e `where` retornam um `Iterable`, suas chamadas podem ser **encadeadas** uma após a outra. Cada etapa recebe o resultado da anterior, e `toList()` é chamado uma vez no final:

```dart
var numbers = [1, 2, 3, 4, 5, 6];

var result = numbers.where((n) => n > 3).map((n) => n * 10).toList();
print(result); // [40, 50, 60]
```

# --instructions--

Escreva uma função `oddSquares` que recebe um parâmetro `List<int>` `numbers` e retorna uma `List<int>` com os quadrados apenas dos números ímpares, na ordem original. Use `where` e `map` com funções anônimas.

Exemplo:
```dart
print(oddSquares([1, 2, 3, 4, 5])); // [1, 9, 25]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`oddSquares([1, 2, 3, 4, 5])` deve ser `[1, 9, 25]`

```dart
  test('test1', () {
    expect(oddSquares([1, 2, 3, 4, 5]), [1, 9, 25], reason: '--err-t1--');
  });
```

`oddSquares([2, 4, 6])` deve ser `[]`

```dart
  test('test2', () {
    expect(oddSquares([2, 4, 6]), [], reason: '--err-t2--');
  });
```

`oddSquares([7, 10, 3])` deve ser `[49, 9]`

```dart
  test('test3', () {
    expect(oddSquares([7, 10, 3]), [49, 9], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> oddSquares(List<int> numbers) {
  return numbers.where((n) => n % 2 != 0).map((n) => n * n).toList();
}
```
