---
language: dart
exerciseType: 2
---

# --description--

Uma função também pode **retornar uma função**. O tipo de retorno é então um tipo de função, e o corpo retorna uma função anônima:

```dart
int Function(int) makeAdder(int amount) {
  return (int n) => n + amount;
}

var addTen = makeAdder(10);
print(addTen(5)); // 15
```

Note que a função retornada ainda usa `amount`, um parâmetro de `makeAdder`, mesmo depois que `makeAdder` terminou. Uma função que lembra as variáveis ao seu redor como essa é chamada de **closure**.

# --instructions--

Complete `makeMultiplier` para que ela retorne uma função que multiplica seu argumento por `factor`, e depois chame a função retornada para que o programa imprima `15`.

# --seed--

```dart
int Function(int) makeMultiplier(int factor) {
  return ([/] n) => n [/] factor;
}

void main() {
  var triple = makeMultiplier(3);
  print(triple([/]));
}
```

# --answers--

- int
- *
- 5
- +
- 15
- String

# --solutions--

```dart
int Function(int) makeMultiplier(int factor) {
  return (int n) => n * factor;
}

void main() {
  var triple = makeMultiplier(3);
  print(triple(5));
}
```

# --output--

15
