---
language: dart
exerciseType: 4
---

# --description--

Funções anônimas suportam os mesmos tipos de parâmetros que funções nomeadas: parâmetros **posicionais opcionais** em `[]` e parâmetros **nomeados** em `{}`, ambos com valores padrão:

```dart
var repeat = (String text, [int times = 2]) => text * times;
print(repeat('ab'));    // abab
print(repeat('ab', 3)); // ababab

var describe = ({required String item, int count = 1}) => '$count x $item';
print(describe(item: 'apple'));           // 1 x apple
print(describe(item: 'pear', count: 4));  // 4 x pear
```

# --instructions--

Ordene os itens para armazenar em `greet` uma função anônima com um parâmetro nomeado `name` cujo valor padrão é `'World'`, e depois chame-a primeiro sem argumentos e depois com `name: 'Dart'`. O programa deve imprimir:

```
Hello, World!
Hello, Dart!
```

# --answers--

```dart
void main() {
```

```dart
  var greet = ({String name = 'World'}) {
```

```dart
    print('Hello, $name!');
```

```dart
  };
```

```dart
  greet();
```

```dart
  greet(name: 'Dart');
```

```dart
}
```

# --solutions--

```dart
void main() {
  var greet = ({String name = 'World'}) {
    print('Hello, $name!');
  };
  greet();
  greet(name: 'Dart');
}
```

# --output--

Hello, World!
Hello, Dart!
