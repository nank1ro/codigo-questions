---
language: dart
exerciseType: 1
---

# --description--

Como um tipo de função é um tipo normal, uma função pode receber **outra função como parâmetro**. Dentro do corpo, o parâmetro é chamado como qualquer função:

```dart
int apply(int n, int Function(int) operation) {
  return operation(n);
}

print(apply(5, (n) => n * 2)); // 10
print(apply(5, (n) => n - 1)); // 4
```

Aqui quem chama decide o que `apply` faz passando uma função anônima como segundo argumento.

# --instructions--

Escreva uma função `applyTwice` que recebe um parâmetro `int` `n` e um parâmetro de função `operation` do tipo `int Function(int)`, e retorna o resultado de aplicar `operation` a `n` duas vezes.

Exemplo:
```dart
print(applyTwice(3, (n) => n * 2)); // 12
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`applyTwice(3, (n) => n * 2)` deve ser `12`

```dart
  test('test1', () {
    expect(applyTwice(3, (n) => n * 2), 12, reason: '--err-t1--');
  });
```

`applyTwice(10, (n) => n + 5)` deve ser `20`

```dart
  test('test2', () {
    expect(applyTwice(10, (n) => n + 5), 20, reason: '--err-t2--');
  });
```

`applyTwice(2, (n) => n * n)` deve ser `16`

```dart
  test('test3', () {
    expect(applyTwice(2, (n) => n * n), 16, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int applyTwice(int n, int Function(int) operation) {
  return operation(operation(n));
}
```
