---
language: dart
exerciseType: 1
---

# --description--

Uma closure não apenas lê as variáveis que captura: ela também pode **modificá-las**, e as mudanças são mantidas entre as chamadas. Isso torna possível manter um estado privado sem uma classe:

```dart
int Function() makeTimer() {
  var seconds = 0;
  return () {
    seconds += 10;
    return seconds;
  };
}

var timer = makeTimer();
print(timer()); // 10
print(timer()); // 20
```

Cada chamada a `makeTimer()` cria uma variável `seconds` completamente nova, então dois timers nunca compartilham sua contagem.

# --instructions--

Escreva uma função `makeCounter` sem parâmetros que retorna uma função do tipo `int Function()`. Cada vez que a função retornada for chamada, ela deve incrementar um contador capturado e retorná-lo, começando de `1`.

Exemplo:
```dart
var counter = makeCounter();
print(counter()); // 1
print(counter()); // 2
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

A primeira chamada ao contador deve retornar `1`

```dart
  test('test1', () {
    var counter = makeCounter();
    expect(counter(), 1, reason: '--err-t1--');
  });
```

Três chamadas ao mesmo contador devem retornar `1`, `2` e `3`

```dart
  test('test2', () {
    var counter = makeCounter();
    expect([counter(), counter(), counter()], [1, 2, 3], reason: '--err-t2--');
  });
```

Dois contadores devem ser independentes

```dart
  test('test3', () {
    var a = makeCounter();
    var b = makeCounter();
    a();
    a();
    expect(b(), 1, reason: '--err-t3--');
    expect(a(), 3, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int Function() makeCounter() {
  var count = 0;
  return () {
    count++;
    return count;
  };
}
```
