---
language: dart
exerciseType: 1
---

# --description--

Uma função anônima não precisa ser armazenada em lugar algum: ela pode ser **chamada imediatamente** escrevendo os argumentos logo após sua chave de fechamento. Isso é útil para calcular um valor com algumas variáveis temporárias que não devem vazar para o código ao redor:

```dart
var area = (double radius) {
  var pi = 3.14;
  return pi * radius * radius;
}(2.0);

print(area); // 12.56
```

Aqui `pi` existe apenas dentro da função anônima, e `area` recebe o valor retornado.

# --instructions--

Uma variável `int` `limit` já está definida. Declare uma variável `sum` inicializada invocando imediatamente uma função anônima sem parâmetros que soma todos os inteiros de `1` a `limit` com um loop e retorna o total.

# --before-seed--

```dart
dynamic result(int limit) {
```

# --seed--

```dart
var sum = 
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` deve ser um `int`

```dart
  test('test1', () {
    expect(result(100), isA<int>(), reason: '--err-t1--');
  });
```

`sum` deve ser `5050` quando `limit` for `100`

```dart
  test('test2', () {
    expect(result(100), 5050, reason: '--err-t2--');
  });
```

`sum` deve ser `55` quando `limit` for `10`

```dart
  test('test3', () {
    expect(result(10), 55, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var sum = () {
  var total = 0;
  for (var i = 1; i <= limit; i++) {
    total += i;
  }
  return total;
}();
```
