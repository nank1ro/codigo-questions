---
language: dart
exerciseType: 1
---

# --description--

O método `.substring(start, end)` retorna a parte de uma string do índice `start` até, **sem incluir**, o índice `end`. Se você omitir `end`, ele pega tudo até o final da string:

```dart
var text = 'Hello Dart';
print(text.substring(0, 5));  // Hello
print(text.substring(6));     // Dart
```

# --instructions--

Dada a string `date` no formato `YYYY-MM-DD`, armazene em uma `String` chamada `year` os quatro primeiros caracteres e em uma `String` chamada `day` os dois últimos caracteres.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var date = '2024-06-15';
```

# --after-seed--

```dart
  return [year, day];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`year` deve ser `'2024'`

```dart
  test('test1', () {
    expect(result()[0], '2024', reason: '--err-t1--');
  });
```

`day` deve ser `'15'`

```dart
  test('test2', () {
    expect(result()[1], '15', reason: '--err-t2--');
  });
```

`year` deve ser uma `String`

```dart
  test('test3', () {
    expect(result()[0], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var date = '2024-06-15';
String year = date.substring(0, 4);
String day = date.substring(8);
```
