---
language: dart
exerciseType: 1
---

# --description--

Métodos de string retornam strings, então eles podem ser **encadeados** um após o outro. Combinado com `.split('')`, a propriedade de lista `.reversed` e `.join()`, isso permite inverter uma string em uma única expressão:

```dart
var text = 'Dart';
print(text.split('').reversed.join()); // traD
print(text.toLowerCase().replaceAll('a', '4')); // d4rt
```

Um **palíndromo** é um texto que se lê da mesma forma de frente para trás e de trás para frente, como `level`.

# --instructions--

Escreva uma função `isPalindrome` que recebe um parâmetro `String` chamado `text` e retorna `true` se ele for um palíndromo, ignorando maiúsculas/minúsculas e espaços, ou `false` caso contrário.

Exemplo:
```dart
print(isPalindrome('Level'));            // true
print(isPalindrome('never odd or even')); // true
print(isPalindrome('Dart'));             // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isPalindrome('Level')` deve retornar `true`

```dart
  test('test1', () {
    expect(isPalindrome('Level'), true, reason: '--err-t1--');
  });
```

`isPalindrome('never odd or even')` deve retornar `true`

```dart
  test('test2', () {
    expect(isPalindrome('never odd or even'), true, reason: '--err-t2--');
  });
```

`isPalindrome('Dart')` deve retornar `false`

```dart
  test('test3', () {
    expect(isPalindrome('Dart'), false, reason: '--err-t3--');
  });
```

`isPalindrome('Race car')` deve retornar `true`

```dart
  test('test4', () {
    expect(isPalindrome('Race car'), true, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isPalindrome(String text) {
  var clean = text.toLowerCase().replaceAll(' ', '');
  return clean == clean.split('').reversed.join();
}
```
