---
language: dart
exerciseType: 1
---

# --description--

Internamente, cada caractere de uma string é armazenado como um número, seu **code unit** (um código UTF-16). `.codeUnitAt(index)` fornece o código de um caractere e `.codeUnits` fornece a lista inteira. `String.fromCharCode(code)` faz o oposto, construindo uma string a partir de um código:

```dart
var word = 'AB';
print(word.codeUnitAt(0));         // 65
print(word.codeUnits);             // [65, 66]
print(String.fromCharCode(67));    // C
```

Letras consecutivas têm códigos consecutivos: `'A'` é 65, `'B'` é 66, e assim por diante.

# --instructions--

Dada a string `letter`, armazene em um `int` chamado `code` seu code unit, e em uma `String` chamada `next` o caractere cujo código é `code + 1`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var letter = 'D';
```

# --after-seed--

```dart
  return [code, next];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`code` deve ser `68`

```dart
  test('test1', () {
    expect(result()[0], 68, reason: '--err-t1--');
  });
```

`next` deve ser `'E'`

```dart
  test('test2', () {
    expect(result()[1], 'E', reason: '--err-t2--');
  });
```

`next` deve ser uma `String`

```dart
  test('test3', () {
    expect(result()[1], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var letter = 'D';
int code = letter.codeUnitAt(0);
String next = String.fromCharCode(code + 1);
```
