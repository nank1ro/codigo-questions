---
language: dart
exerciseType: 2
---

# --description--

O método `.replaceAll(from, to)` retorna uma nova string em que **toda** ocorrência de `from` é substituída por `to`. `.replaceFirst(from, to)` substitui apenas a primeira:

```dart
var text = 'a-b-c';
print(text.replaceAll('-', '+'));    // a+b+c
print(text.replaceFirst('-', '+'));  // a+b-c
```

# --instructions--

Complete o código para substituir cada `cats` por `dogs` e imprimir o resultado.

# --seed--

```dart
var text = 'cats here, cats there';
print(text.[/]('cats', [/]));
```

# --answers--

- replaceAll
- 'dogs'
- replaceFirst
- replace
- 'cats'
- dogs

# --solutions--

```dart
var text = 'cats here, cats there';
print(text.replaceAll('cats', 'dogs'));
```

# --output--

dogs here, dogs there
