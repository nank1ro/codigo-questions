---
language: dart
exerciseType: 1
---

# --description--

Duas strings podem ser unidas em uma nova com o operador `+`, chamado **concatenação**:

```dart
var greeting = 'Hello' + ', ' + 'Dart';
print(greeting); // Hello, Dart
```

Dart também une dois **literais** de string escritos um ao lado do outro, sem nenhum operador. Isso é útil para dividir um texto longo em várias linhas:

```dart
var text = 'Hello, '
    'Dart';
print(text); // Hello, Dart
```

Apenas strings podem ser concatenadas com `+`: `'Age: ' + 30` é um erro de compilação, porque `30` é um `int`.

# --instructions--

Dadas as variáveis `first` e `last`, crie uma `String` chamada `full` que contenha o primeiro nome, um espaço e o sobrenome.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var first = 'Ada';
var last = 'Lovelace';
```

# --after-seed--

```dart
  return full;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`full` deve ser `'Ada Lovelace'`

```dart
  test('test1', () {
    expect(result(), 'Ada Lovelace', reason: '--err-t1--');
  });
```

`full` deve ser uma `String`

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var first = 'Ada';
var last = 'Lovelace';
String full = first + ' ' + last;
```
