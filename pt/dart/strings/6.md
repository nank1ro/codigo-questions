---
language: dart
exerciseType: 4
---

# --description--

Textos digitados por um usuário costumam ter espaços extras ao redor. O método `.trim()` retorna uma cópia da string sem espaços em branco no início e no fim (espaços, tabulações e quebras de linha). `.trimLeft()` e `.trimRight()` removem apenas de um dos lados:

```dart
var input = '   hello  ';
print('[${input.trim()}]');      // [hello]
print('[${input.trimLeft()}]');  // [hello  ]
```

# --instructions--

Ordene as linhas de código para remover os espaços ao redor de `input` e imprimir o texto limpo em maiúsculas.

# --answers--

```dart
void main() {
```

```dart
  var input = '  hello  ';
```

```dart
  var clean = input.trim();
```

```dart
  print(clean.toUpperCase());
```

```dart
}
```

# --solutions--

```dart
void main() {
  var input = '  hello  ';
  var clean = input.trim();
  print(clean.toUpperCase());
}
```

# --output--

HELLO
