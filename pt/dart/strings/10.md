---
language: dart
exerciseType: 1
---

# --description--

O método `.split(separator)` corta uma string em uma `List<String>` a cada ocorrência do separador. O oposto é `.join(separator)`, um método de listas que cola os elementos em uma única string:

```dart
var csv = 'a,b,c';
List<String> parts = csv.split(',');
print(parts);            // [a, b, c]
print(parts.join(' - ')); // a - b - c
```

Chamar `.split('')` com um separador vazio retorna uma lista com cada caractere individual.

# --instructions--

Dada a string `csv`, armazene em uma `List<String>` chamada `colors` suas partes separadas pela vírgula, e em uma `String` chamada `joined` essas partes unidas com `' | '`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var csv = 'red,green,blue';
```

# --after-seed--

```dart
  return [colors, joined];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`colors` deve ser `['red', 'green', 'blue']`

```dart
  test('test1', () {
    expect(result()[0], ['red', 'green', 'blue'], reason: '--err-t1--');
  });
```

`joined` deve ser `'red | green | blue'`

```dart
  test('test2', () {
    expect(result()[1], 'red | green | blue', reason: '--err-t2--');
  });
```

`colors` deve ser uma `List`

```dart
  test('test3', () {
    expect(result()[0], isA<List>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var csv = 'red,green,blue';
List<String> colors = csv.split(',');
String joined = colors.join(' | ');
```
