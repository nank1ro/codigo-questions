---
language: dart
exerciseType: 1
---

# --description--

Toda string sabe quantos caracteres possui através de sua propriedade `.length`. Espaços e pontuação também contam como caracteres:

```dart
var word = 'hello';
print(word.length);      // 5
print('a b'.length);     // 3
```

Você pode ler um único caractere com colchetes e seu **índice**, começando em `0`. O resultado é uma `String` de um único caractere:

```dart
print(word[0]);                // h
print(word[word.length - 1]);  // o
```

Ler um índice fora da string (como `word[5]`) gera um erro.

# --instructions--

Dada a string `word`, armazene em um `int` chamado `size` seu número de caracteres, em uma `String` chamada `first` seu primeiro caractere, e em uma `String` chamada `last` seu último caractere.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var word = 'Flutter';
```

# --after-seed--

```dart
  return [size, first, last];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`size` deve ser `7`

```dart
  test('test1', () {
    expect(result()[0], 7, reason: '--err-t1--');
  });
```

`first` deve ser `'F'`

```dart
  test('test2', () {
    expect(result()[1], 'F', reason: '--err-t2--');
  });
```

`last` deve ser `'r'`

```dart
  test('test3', () {
    expect(result()[2], 'r', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var word = 'Flutter';
int size = word.length;
String first = word[0];
String last = word[word.length - 1];
```
