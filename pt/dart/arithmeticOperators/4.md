---
language: dart
exerciseType: 2
---

# --description--

O **operador de divisão** `/` divide um valor por outro. Em Dart, `/` **sempre retorna um `double`**, mesmo quando o resultado é um número inteiro.

```dart
int a = 10;
int b = 4;
double result = a / b; // 2.5

double exact = 10 / 2; // 5.0  (not 5!)
```

Sempre use uma variável `double` para armazenar o resultado de `/`.

# --instructions--

Complete o código para dividir `9` por `4` e armazenar o resultado em `result`.

# --seed--

```dart
void main() {
  double result = 9 [/] 4;
  print(result);
}
```

# --answers--

- /
- *
- +
- -

# --solutions--

```dart
void main() {
  double result = 9 / 4;
  print(result);
}
```

# --output--

2.25
