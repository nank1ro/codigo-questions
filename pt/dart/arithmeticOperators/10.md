---
language: dart
exerciseType: 1
---

# --instructions--

Declare duas variáveis `int` de nível superior: `a` com o valor `12` e `b` com o valor `8`. Então declare uma terceira variável `int` de nível superior chamada `total` que armazena sua soma.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`a` deve ser `12`, `b` deve ser `8`, e `total` deve ser `20`

```dart
  test('test1', () {
    expect(a, 12, reason: '--err-t1--');
    expect(b, 8, reason: '--err-t2--');
    expect(total, 20, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 12;
int b = 8;
int total = a + b;

void main() {
  print(total);
}
```
