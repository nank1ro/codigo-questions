---
language: dart
exerciseType: 1
---

# --instructions--

Uma loja vende `itemCount` itens a `pricePerItem` dólares cada, com um valor `discount` dólar subtraído do total. Declare estas três variáveis `int` de nível superior: `itemCount` = `6`, `pricePerItem` = `15`, `discount` = `10`. Então declare uma variável `int` de nível superior chamada `totalPrice` que calcula o total após o desconto.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`totalPrice` deve ser igual a `80`

```dart
  test('test1', () {
    expect(totalPrice, 80, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int itemCount = 6;
int pricePerItem = 15;
int discount = 10;
int totalPrice = itemCount * pricePerItem - discount;

void main() {
  print(totalPrice);
}
```
