---
language: dart
exerciseType: 2
---

# --description--

O **operador de divisão inteira** `~/` divide dois números e **trunca** o resultado para um inteiro (descarta qualquer parte decimal).

```dart
int a = 10;
int b = 3;
int result = a ~/ b; // 3  (not 3.333...)
```

Use `~/` quando você precisar de um quociente de número inteiro sem resto.

# --instructions--

Complete o código para realizar a divisão inteira de `17` por `5` e armazenar o resultado em `result`.

# --seed--

```dart
void main() {
  int result = 17 [/] 5;
  print(result);
}
```

# --answers--

- ~/
- /
- %
- *

# --solutions--

```dart
void main() {
  int result = 17 ~/ 5;
  print(result);
}
```

# --output--

3
