---
language: dart
exerciseType: 1
---

# --instructions--

Use o operador de módulo `%` para encontrar o resto quando `17` é dividido por `5`. Armazene o resultado em uma variável de nível superior chamada `remainder`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

A variável `remainder` deve ser igual a `2`

```dart
  test('test1', () {
    expect(remainder, 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 17 % 5;

void main() {
  print(remainder);
}
```
