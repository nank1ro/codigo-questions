---
language: dart
exerciseType: 1
---

# --description--

A propriedade `.keys` retorna todas as chaves de um map e `.values` retorna todos os valores, na ordem de inserção. Ambas são `Iterable`s preguiçosas (lazy), então chame `.toList()` quando precisar de uma `List` de verdade:

```dart
var ages = {'Ann': 30, 'Bob': 25};
List<String> names = ages.keys.toList();
List<int> years = ages.values.toList();
print(names); // [Ann, Bob]
print(years); // [30, 25]
```

# --instructions--

Dado o map `scores`, crie uma `List<String>` chamada `subjects` com suas chaves e uma `List<int>` chamada `points` com seus valores.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var scores = {'math': 90, 'art': 75, 'gym': 60};
```

# --after-seed--

```dart
  return [subjects, points];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`subjects` deve ser `['math', 'art', 'gym']`

```dart
  test('test1', () {
    expect(result()[0], ['math', 'art', 'gym'], reason: '--err-t1--');
  });
```

`points` deve ser `[90, 75, 60]`

```dart
  test('test2', () {
    expect(result()[1], [90, 75, 60], reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var scores = {'math': 90, 'art': 75, 'gym': 60};
List<String> subjects = scores.keys.toList();
List<int> points = scores.values.toList();
```
