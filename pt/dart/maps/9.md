---
language: dart
exerciseType: 1
---

# --description--

Um map literal vazio `{}` não tem pares para inferir os tipos, então dê a ele tipos explícitos com `<K, V>{}` ou com uma anotação de tipo:

```dart
var cart = <String, int>{};
Map<String, int> other = {};
```

A propriedade `.isEmpty` é `true` quando um map não tem pares, e `.isNotEmpty` é `true` quando ele tem pelo menos um:

```dart
print(cart.isEmpty); // true
cart['pen'] = 2;
print(cart.isNotEmpty); // true
```

# --instructions--

Crie um `Map<String, int>` vazio chamado `cart`. Armazene se ele `.isEmpty` em um `bool` chamado `wasEmpty`, depois adicione a chave `'pen'` com valor `2` e armazene se ele `.isNotEmpty` em um `bool` chamado `hasItems`.

# --before-seed--

```dart
dynamic result() {
```

# --after-seed--

```dart
  return [wasEmpty, hasItems, cart];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`wasEmpty` deve ser `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasItems` deve ser `true`

```dart
  test('test2', () {
    expect(result()[1], true, reason: '--err-t2--');
  });
```

`cart` deve ser `{'pen': 2}`

```dart
  test('test3', () {
    expect(result()[2], {'pen': 2}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var cart = <String, int>{};
bool wasEmpty = cart.isEmpty;
cart['pen'] = 2;
bool hasItems = cart.isNotEmpty;
```
