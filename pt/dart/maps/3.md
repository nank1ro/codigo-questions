---
language: dart
exerciseType: 2
---

# --description--

Atribuir com `map[key] = value` **adiciona** um novo par, quando a chave ainda não está no map, ou **atualiza** o valor armazenado em uma chave existente:

```dart
var ages = {'Ann': 30};
ages['Bob'] = 25; // adds Bob
ages['Ann'] = 31; // updates Ann
print(ages); // {Ann: 31, Bob: 25}
```

Novas chaves são adicionadas depois das existentes, então um map lembra a ordem de inserção.

# --instructions--

Complete o código para atualizar a contagem de `apple` para `5` e, em seguida, adicionar `pear` com uma contagem de `2`.

# --seed--

```dart
var stock = {'apple': 3};
stock[/] = 5;
stock[/] = 2;
print(stock);
```

# --answers--

- ['apple']
- ['pear']
- .apple
- ('pear')
- 'apple'
- [0]

# --solutions--

```dart
var stock = {'apple': 3};
stock['apple'] = 5;
stock['pear'] = 2;
print(stock);
```

# --output--

{apple: 5, pear: 2}
