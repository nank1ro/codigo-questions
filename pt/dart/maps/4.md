---
language: dart
exerciseType: 2
---

# --description--

O método `.remove(key)` exclui uma chave e seu valor do map. Ele retorna o valor removido, ou `null` se a chave não existia:

```dart
var ages = {'Ann': 30, 'Bob': 25};
int? removed = ages.remove('Ann');
print(removed); // 30
print(ages); // {Bob: 25}
```

A propriedade `.length` retorna o número de pares chave-valor:

```dart
print(ages.length); // 1
```

# --instructions--

Complete o código para remover `Bob` do map e, em seguida, imprimir quantos pares restaram.

# --seed--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.[/]('Bob');
print(ages.[/]);
```

# --answers--

- remove
- length
- delete
- removeAt
- size
- count

# --solutions--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.remove('Bob');
print(ages.length);
```

# --output--

2
