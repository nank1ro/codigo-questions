---
language: dart
exerciseType: 2
---

# --description--

Chamar um método ou ler uma propriedade em `null` causaria uma falha, então o Dart não permite fazer isso em um valor nullable com o ponto normal:

```dart
String? text;
print(text.length); // error: the property 'length' can't be unconditionally accessed
```

O operador de **acesso null-aware** `?.` resolve isso: se o valor for `null` a expressão inteira é `null` e nada mais é avaliado, caso contrário ele funciona como um `.` normal:

```dart
String? text = 'Dart';
print(text?.length); // 4

text = null;
print(text?.length); // null
```

Como o resultado pode ser `null`, seu tipo é nullable: `text?.length` é um `int?`, não um `int`.

# --instructions--

Complete a função `lengthOf` para que ela aceite uma `String` nullable e retorne seu comprimento, ou `null` quando o texto estiver ausente. O programa deve imprimir `4` e depois `null`.

# --seed--

```dart
int? lengthOf([/] text) {
  return text[/]length;
}

void main() {
  print(lengthOf('Dart'));
  print(lengthOf(null));
}
```

# --answers--

- String?
- ?.
- String
- .
- !.
- ??

# --solutions--

```dart
int? lengthOf(String? text) {
  return text?.length;
}

void main() {
  print(lengthOf('Dart'));
  print(lengthOf(null));
}
```

# --output--

4
null
