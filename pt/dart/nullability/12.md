---
language: dart
exerciseType: 4
---

# --description--

A nullability molda a forma como você declara **parâmetros nomeados**. Um parâmetro nomeado com um tipo nullable é opcional: quando quem chama o omite, ele é simplesmente `null`.

```dart
String label({String? title}) => title ?? 'untitled';

print(label());               // untitled
print(label(title: 'Notes')); // Notes
```

Um parâmetro nomeado com um tipo non-nullable e sem valor padrão ficaria sem valor quando omitido, então o Dart exige que você o marque como `required`; quem chama deve então sempre passá-lo:

```dart
String label({required String name, String? title}) { ... }

label(name: 'Ada');               // ok
label(name: 'Ada', title: 'Dr.'); // ok
label(title: 'Dr.');              // error: the named parameter 'name' is required
```

# --instructions--

Ordene os itens para definir uma função `label` com um parâmetro nomeado obrigatório `name` e um parâmetro nomeado opcional nullable `title`, que retorna apenas `name` quando `title` for `null` e `'$title $name'` caso contrário. Defina primeiro a função, depois `main`. O programa deve imprimir:

```
Ada
Countess Lovelace
```

# --answers--

```dart
String label({required String name, String? title}) {
```

```dart
  if (title == null) return name;
```

```dart
  return '$title $name';
```

```dart
}
```

```dart
void main() {
```

```dart
  print(label(name: 'Ada'));
```

```dart
  print(label(name: 'Lovelace', title: 'Countess'));
```

```dart
}
```

# --solutions--

```dart
String label({required String name, String? title}) {
  if (title == null) return name;
  return '$title $name';
}

void main() {
  print(label(name: 'Ada'));
  print(label(name: 'Lovelace', title: 'Countess'));
}
```

# --output--

Ada
Countess Lovelace
