---
language: dart
exerciseType: 1
---

# --description--

Uma variável nullable declarada **sem valor** começa como `null`, então `= null` pode ser omitido:

```dart
int? age;
print(age); // null
```

Uma variável non-nullable não tem esse padrão: o Dart se recusa a compilar qualquer código que a leia antes de um valor ter sido atribuído.

```dart
int count;
print(count); // error: 'count' must be assigned before it can be used
```

# --instructions--

Declare uma variável `String` nullable chamada `email` sem valor, e uma variável `String` chamada `username` com o valor `'ada'`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart

```

# --after-seed--

```dart
  return [email, username];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`email` deve ser `null`

```dart
  test('test1', () {
    expect(result()[0], isNull, reason: '--err-t1--');
  });
```

`username` deve ser `'ada'`

```dart
  test('test2', () {
    expect(result()[1], 'ada', reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String? email;
String username = 'ada';
```
