---
language: dart
exerciseType: 1
---

# --description--

Muitas vezes um valor ausente deve ser substituído por um **padrão**. O operador **if-null** `??` retorna o operando da esquerda quando ele não é `null`, e o da direita caso contrário:

```dart
String? nickname;
print(nickname ?? 'anonymous'); // anonymous

nickname = 'Ada';
print(nickname ?? 'anonymous'); // Ada
```

`??` reage apenas a `null`: uma string vazia `''` ou o número `0` são valores reais, então são mantidos.

`??` combina bem com `?.`, porque `?.` produz um resultado nullable:

```dart
String? text;
print(text?.length ?? 0); // 0
```

# --instructions--

Escreva uma função `greet` que recebe um parâmetro `String` nullable chamado `name` e retorna `'Hello, '` seguido de `name`, ou de `'stranger'` quando `name` for `null`. Use `??`.

Exemplo:
```dart
print(greet('Ada')); // Hello, Ada
print(greet(null));  // Hello, stranger
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greet('Ada')` deve ser `'Hello, Ada'`

```dart
  test('test1', () {
    expect(greet('Ada'), 'Hello, Ada', reason: '--err-t1--');
  });
```

`greet(null)` deve ser `'Hello, stranger'`

```dart
  test('test2', () {
    expect(greet(null), 'Hello, stranger', reason: '--err-t2--');
  });
```

`greet('')` deve ser `'Hello, '`, porque uma string vazia não é `null`

```dart
  test('test3', () {
    expect(greet(''), 'Hello, ', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String greet(String? name) {
  return 'Hello, ${name ?? 'stranger'}';
}
```
