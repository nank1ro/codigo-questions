---
language: dart
exerciseType: 2
---

# --description--

O operador de **atribuição if-null** `??=` atribui um valor a uma variável **somente se** essa variável for atualmente `null`; caso contrário, ele a deixa intacta:

```dart
int? retries;
retries ??= 3;
print(retries); // 3

retries ??= 10;
print(retries); // 3, it already had a value
```

Ele também funciona em entradas de map, que são nullable porque uma chave pode estar ausente:

```dart
var stock = {'apple': 4};
stock['pear'] ??= 1;  // added
stock['apple'] ??= 9; // ignored
print(stock); // {apple: 4, pear: 1}
```

# --instructions--

Complete o código para que o tema salvo e o tamanho de fonte existente sejam mantidos enquanto a margem ausente é adicionada. O programa deve imprimir `dark` e depois `{font: 12, margin: 8}`.

# --seed--

```dart
String? savedTheme() => 'dark';

void main() {
  String? theme = savedTheme();
  var sizes = {'font': 12};
  theme [/] 'light';
  sizes['font'] [/] 14;
  sizes['margin'] ??= 8;
  print(theme);
  print(sizes);
}
```

# --answers--

- ??=
- ??=
- =
- ?=
- =??
- :=

# --solutions--

```dart
String? savedTheme() => 'dark';

void main() {
  String? theme = savedTheme();
  var sizes = {'font': 12};
  theme ??= 'light';
  sizes['font'] ??= 14;
  sizes['margin'] ??= 8;
  print(theme);
  print(sizes);
}
```

# --output--

dark
{font: 12, margin: 8}
