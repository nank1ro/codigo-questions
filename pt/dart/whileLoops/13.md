---
language: dart
exerciseType: 1
---

# --instructions--

Use um laço `while` com `continue` para somar apenas os números **ímpares** de `1` a `10` (inclusive). Retorne a soma.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` deve ser igual a `25`

```dart
  test('test1', () {
    expect(computeValue(), 25, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sum = 0;
int i = 1;
while (i <= 10) {
  if (i % 2 == 0) {
    i++;
    continue;
  }
  sum += i;
  i++;
}
```
