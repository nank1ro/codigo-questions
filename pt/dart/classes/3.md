---
language: dart
exerciseType: 2
---

# --description--

Um **método** é uma função definida dentro de uma classe. Os métodos descrevem o comportamento de um objeto.

```dart
class Circle {
  double radius;
  Circle(this.radius);

  double area() {
    return 3.14 * radius * radius;
  }
}

void main() {
  final c = Circle(5);
  print(c.area()); // 78.5
}
```

Os métodos podem acessar os próprios campos do objeto diretamente pelo nome (ou via `this.fieldName`).

# --instructions--

Complete a classe `Rectangle` para que ela tenha campos `int` `width` e `height`, um construtor e um método `perimeter()` que retorna `2 * (width + height)`. Depois chame o método e imprima o resultado.

# --seed--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.[/]);

  int [/]() {
    return 2 * ([/] + height);
  }
}

void main() {
  final r = Rectangle(4, 6);
  print(r.[/]());
}
```

# --answers--

- height
- perimeter
- width
- area
- this
- 3
- r

# --solutions--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.height);

  int perimeter() {
    return 2 * (width + height);
  }
}

void main() {
  final r = Rectangle(4, 6);
  print(r.perimeter());
}
```

# --output--

20
