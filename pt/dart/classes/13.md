---
language: dart
exerciseType: 1
---

# --instructions--

Crie uma classe pai `Animal` com um campo `String` `name` e um construtor. Depois crie uma classe filha `Dog` que estende `Animal`. `Dog` deve ter um campo `String` adicional `breed` e um construtor que usa `super` para passar `name` para `Animal`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Dog('Rex', 'Labrador').name` deve ser igual a `'Rex'`

```dart
  test('test1', () {
    final dog = Dog('Rex', 'Labrador');
    expect(dog.name, 'Rex', reason: '--err-t1--');
  });
```

`Dog('Rex', 'Labrador').breed` deve ser igual a `'Labrador'`

```dart
  test('test2', () {
    final dog = Dog('Rex', 'Labrador');
    expect(dog.breed, 'Labrador', reason: '--err-t2--');
  });
```

`Dog` deve ser uma instância de `Animal`

```dart
  test('test3', () {
    final dog = Dog('Buddy', 'Poodle');
    expect(dog is Animal, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Dog extends Animal {
  String breed;
  Dog(String name, this.breed) : super(name);
}
```
