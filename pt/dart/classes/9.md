---
language: dart
exerciseType: 2
---

# --description--

Um **setter** é um método especial que permite atribuir um valor enquanto executa uma lógica de validação. Você o define com a palavra-chave `set`:

```dart
class Temperature {
  double _celsius = 0;

  double get celsius => _celsius;

  set celsius(double value) {
    if (value < -273.15) throw ArgumentError('Too cold!');
    _celsius = value;
  }
}
```

O nome do campo geralmente é prefixado com `_` para marcá-lo como privado.

# --instructions--

Complete o setter `age` na classe `Person` que atribui `value` a `_age` apenas quando `value >= 0`.

# --seed--

```dart
class Person {
  int _age = 0;

  int get age => _age;

  [/] age(int value) {
    if (value >= 0) _age = [/];
  }
}
```

# --answers--

- set
- value
- get
- _age
- age
- 0

# --solutions--

```dart
class Person {
  int _age = 0;

  int get age => _age;

  set age(int value) {
    if (value >= 0) _age = value;
  }
}
```
