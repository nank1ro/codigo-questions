---
language: dart
exerciseType: 2
---

# --description--

Uma **classe** é um modelo para criar objetos. Ela agrupa dados relacionados (campos) e comportamentos (métodos) juntos.

Em Dart, você declara uma classe com a palavra-chave `class` seguida do nome da classe (por convenção, PascalCase):

```dart
class Car {
  String brand = 'Toyota';
}
```

Você cria uma **instância** (objeto) da classe assim:

```dart
final car = Car();
print(car.brand); // Toyota
```

# --instructions--

Complete o código para declarar uma classe chamada `Person` com um campo `String` `name` inicializado com `'Alice'`, depois crie uma instância e imprima seu `name`.

# --seed--

```dart
[/] Person {
  String [/] = 'Alice';
}

void main() {
  final p = [/]();
  print(p.[/]);
}
```

# --answers--

- class
- name
- Person
- brand
- age
- void
- p

# --solutions--

```dart
class Person {
  String name = 'Alice';
}

void main() {
  final p = Person();
  print(p.name);
}
```

# --output--

Alice
