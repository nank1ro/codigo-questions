---
language: dart
exerciseType: 1
---

# --instructions--

Crie uma classe `Car` com duas propriedades `String`: `brand` e `model`. Adicione um construtor que defina ambas usando a sintaxe de abreviação.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Criar `Car('Toyota', 'Corolla')` deve definir `brand` como `'Toyota'`

```dart
  test('test1', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.brand, 'Toyota', reason: '--err-t1--');
  });
```

Criar `Car('Toyota', 'Corolla')` deve definir `model` como `'Corolla'`

```dart
  test('test2', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.model, 'Corolla', reason: '--err-t2--');
  });
```

Criar `Car('Honda', 'Civic')` deve definir `brand` como `'Honda'`

```dart
  test('test3', () {
    final car = Car('Honda', 'Civic');
    expect(car.brand, 'Honda', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Car {
  String brand;
  String model;
  Car(this.brand, this.model);
}
```
