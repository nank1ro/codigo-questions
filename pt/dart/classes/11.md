---
language: dart
exerciseType: 2
---

# --description--

Quando o construtor de uma subclasse precisa chamar o construtor da superclasse, use a palavra-chave `super` na **lista de inicialização**:

```dart
class Vehicle {
  String brand;
  Vehicle(this.brand);
}

class Car extends Vehicle {
  int doors;
  Car(String brand, this.doors) : super(brand);
}
```

`super(brand)` encaminha o argumento `brand` para o construtor de `Vehicle`.

# --instructions--

Complete o construtor de `Cat` para que ele chame o construtor da superclasse `Animal` com `name`.

# --seed--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Cat extends Animal {
  Cat(String name) : [/](name);
}
```

# --answers--

- super
- Animal
- parent
- base
- this

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Cat extends Animal {
  Cat(String name) : super(name);
}
```
