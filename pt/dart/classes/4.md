---
language: dart
exerciseType: 2
---

# --description--

Um **getter** é um método especial que parece uma propriedade quando acessado. Use a palavra-chave `get` para definir um:

```dart
class Circle {
  double radius;
  Circle(this.radius);

  double get area => 3.14 * radius * radius;
}

void main() {
  final c = Circle(5);
  print(c.area); // acessado como um campo, não como uma chamada de método
}
```

Os getters são úteis para valores calculados derivados de campos.

# --instructions--

Complete a classe `Square`. Ela tem um campo `int` `side`, um construtor e um getter `area` que retorna `side * side`. Depois acesse o getter e imprima o resultado.

# --seed--

```dart
class Square {
  int side;
  Square(this.side);

  int [/] area => [/] * side;
}

void main() {
  final s = Square(7);
  print(s.[/]);
}
```

# --answers--

- get
- side
- area
- set
- width
- s
- int

# --solutions--

```dart
class Square {
  int side;
  Square(this.side);

  int get area => side * side;
}

void main() {
  final s = Square(7);
  print(s.area);
}
```

# --output--

49
