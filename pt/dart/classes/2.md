---
language: dart
exerciseType: 2
---

# --description--

Um **construtor** é um método especial que é executado quando você cria uma instância de uma classe. Ele é usado para inicializar os campos do objeto.

Em Dart, o construtor mais simples usa a **abreviação** `this.fieldName` para atribuir parâmetros diretamente aos campos:

```dart
class Car {
  String brand;
  Car(this.brand);
}

void main() {
  final car = Car('Toyota');
  print(car.brand); // Toyota
}
```

# --instructions--

Complete a classe `Dog` para que ela tenha um campo `String` `name` e um construtor que o defina usando a sintaxe de abreviação, depois crie um `Dog` chamado `'Rex'` e imprima seu `name`.

# --seed--

```dart
class Dog {
  String [/];
  Dog([/].[/]);
}

void main() {
  final dog = Dog([/]);
  print(dog.name);
}
```

# --answers--

- name
- this
- name
- 'Rex'
- breed
- Dog
- age

# --solutions--

```dart
class Dog {
  String name;
  Dog(this.name);
}

void main() {
  final dog = Dog('Rex');
  print(dog.name);
}
```

# --output--

Rex
