---
language: dart
exerciseType: 1
---

# --instructions--

Crie uma classe `Counter` com um campo `int` `count` inicializado como `0`. Adicione um método `increment()` que aumenta `count` em `1`, e um método `reset()` que define `count` de volta como `0`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Chamar `increment()` três vezes deve fazer `count` ser igual a `3`

```dart
  test('test1', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.increment();
    expect(c.count, 3, reason: '--err-t1--');
  });
```

Chamar `reset()` deve definir `count` de volta como `0`

```dart
  test('test2', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.reset();
    expect(c.count, 0, reason: '--err-t2--');
  });
```

O `count` inicial deve ser `0`

```dart
  test('test3', () {
    final c = Counter();
    expect(c.count, 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Counter {
  int count = 0;

  void increment() {
    count++;
  }

  void reset() {
    count = 0;
  }
}
```
