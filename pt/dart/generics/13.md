---
language: dart
exerciseType: 4
---

# --description--

O mesmo bound funciona igualmente bem para o menor elemento: apenas o sinal da comparação muda. `compareTo` retorna um número negativo quando o receptor vem primeiro, então `item.compareTo(best) < 0` significa "este é o menor".

# --instructions--

Ordene os itens para definir uma função genérica `smallest` com o bound `Comparable<T>` que retorna o menor elemento de uma lista, e um `main` que imprime o menor de `['pear', 'fig', 'kiwi']`. Declare a função antes do `main`. O programa deve imprimir:

```
fig
```

# --answers--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
```

```dart
  var best = items.first;
```

```dart
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
```

```dart
  return best;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(smallest(['pear', 'fig', 'kiwi']));
```

```dart
}
```

# --solutions--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
  return best;
}

void main() {
  print(smallest(['pear', 'fig', 'kiwi']));
}
```

# --output--

fig
