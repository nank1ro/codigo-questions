---
language: dart
exerciseType: 1
---

# --description--

Um `T` sem bound pode ser qualquer coisa, então dentro do corpo você só pode usar o que todo objeto tem. Isto não compila:

```dart
T twice<T>(T value) => value + value; // error: + is not defined for T
```

Um **bound** resolve isso. Escrever `T extends num` diz que "`T` só pode ser um número", e em troca o corpo pode usar tudo que um `num` oferece:

```dart
T twice<T extends num>(T value) => (value + value) as T;

num half<T extends num>(T value) => value / 2;
```

O bound é verificado no ponto de chamada: `half(4)` e `half(2.5)` estão ok, `half('fig')` é um erro de compilação. Um bound é uma promessa nos dois sentidos, argumentos mais restritos por mais poder dentro.

# --instructions--

Escreva uma função genérica `total` com um parâmetro de tipo `T` com o bound `num` que recebe uma `List<T>` `values` e retorna a soma dos elementos como um `num`. Uma lista vazia dá `0`.

Exemplo:
```dart
print(total([1, 2, 3]));  // 6
print(total([1.5, 2.5])); // 4.0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total([1, 2, 3])` deve retornar `6`

```dart
  test('test1', () {
    expect(total([1, 2, 3]), 6, reason: '--err-t1--');
  });
```

`total` também deve funcionar com doubles, então `total([1.5, 2.5])` deve retornar `4.0`, e deve aceitar um argumento de tipo explícito como `total<int>([1, 2])`

```dart
  test('test2', () {
    expect(total([1.5, 2.5]), 4.0, reason: '--err-t2--');
    expect(total([10, 20, 30, 40]), 100, reason: '--err-t2--');
    expect(total<int>([1, 2]), 3, reason: '--err-t2--');
    expect(total<double>([1.5, 1.5]), 3.0, reason: '--err-t2--');
  });
```

`total` deve retornar `0` para uma lista vazia

```dart
  test('test3', () {
    expect(total(<int>[]), 0, reason: '--err-t3--');
    expect(total(<double>[]), 0, reason: '--err-t3--');
    expect(total([-5, 5, 7]), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
num total<T extends num>(List<T> values) {
  num sum = 0;
  for (final value in values) {
    sum += value;
  }
  return sum;
}
```
