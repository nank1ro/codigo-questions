---
language: dart
exerciseType: 1
---

# --description--

`List` não é a única coleção genérica. Um `Set` recebe um argumento de tipo, e um `Map` recebe **dois**: um para as chaves e um para os valores, nessa ordem.

```dart
Set<String> tags = {'new', 'sale'};
Map<String, int> ages = {'Ada': 36, 'Grace': 45};
```

Um literal de coleção vazio não pode ser deduzido a partir do seu conteúdo, então você escreve os argumentos de tipo no próprio literal:

```dart
final counts = <String, int>{};
final seen = <String>{};
counts['fig'] = 3;
```

Uma vez que os tipos são conhecidos, tudo que você tira da coleção já tem o tipo certo: `ages['Ada']` é um `int?`, nunca um valor misterioso.

# --instructions--

Escreva uma função `lengths` que recebe uma `List<String>` `words` e retorna um `Map<String, int>` onde cada palavra é uma chave e seu comprimento é o valor.

Exemplo:
```dart
print(lengths(['fig', 'kiwi'])); // {fig: 3, kiwi: 4}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`lengths(['fig', 'kiwi'])` deve retornar `{fig: 3, kiwi: 4}`

```dart
  test('test1', () {
    expect(lengths(['fig', 'kiwi']), {'fig': 3, 'kiwi': 4}, reason: '--err-t1--');
  });
```

`lengths` deve retornar um `Map<String, int>`

```dart
  test('test2', () {
    expect(lengths(['plum']), isA<Map<String, int>>(), reason: '--err-t2--');
    expect(lengths(['plum']), {'plum': 4}, reason: '--err-t2--');
  });
```

`lengths` deve retornar um mapa vazio para uma lista vazia

```dart
  test('test3', () {
    expect(lengths(<String>[]), <String, int>{}, reason: '--err-t3--');
    expect(lengths(['a', 'bb', 'ccc']), {'a': 1, 'bb': 2, 'ccc': 3}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Map<String, int> lengths(List<String> words) {
  final result = <String, int>{};
  for (final word in words) {
    result[word] = word.length;
  }
  return result;
}
```
