---
language: dart
exerciseType: 1
---

# --description--

Um bound pode mencionar o próprio parâmetro de tipo. `Comparable<T>` é a interface de tudo que sabe se comparar com seus semelhantes, por meio de `compareTo`:

```dart
print('fig'.compareTo('kiwi')); // negativo: fig vem primeiro
print('kiwi'.compareTo('fig')); // positive
print('fig'.compareTo('fig'));  // zero
```

Então `T extends Comparable<T>` se lê como "qualquer tipo que pode ser comparado consigo mesmo", que é exatamente o que uma função de ordenação ou de máximo precisa:

```dart
T maxOf<T extends Comparable<T>>(T a, T b) => a.compareTo(b) >= 0 ? a : b;

print(maxOf('fig', 'kiwi')); // kiwi
```

`String` e `DateTime` a satisfazem diretamente. `int` e `double` implementam `Comparable<num>`, então uma lista de números simplesmente é comparada como `num`.

# --instructions--

Escreva uma função genérica `largest` com um parâmetro de tipo `T` com o bound `Comparable<T>` que recebe uma `List<T>` `items` não vazia e retorna o maior elemento, comparando com `compareTo`.

Exemplo:
```dart
print(largest(['fig', 'kiwi', 'apple'])); // kiwi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest(['fig', 'kiwi', 'apple'])` deve retornar `'kiwi'`

```dart
  test('test1', () {
    expect(largest(['fig', 'kiwi', 'apple']), 'kiwi', reason: '--err-t1--');
  });
```

`largest` deve funcionar com números e com datas também

```dart
  test('test2', () {
    expect(largest([3, 9, 5]), 9, reason: '--err-t2--');
    expect(
      largest([DateTime(2020), DateTime(2024), DateTime(2001)]),
      DateTime(2024),
      reason: '--err-t2--',
    );
  });
```

`largest` deve retornar o único elemento de uma lista de um elemento, o último quando ele for o maior, e deve aceitar um argumento de tipo explícito como `largest<String>(['a', 'z'])`

```dart
  test('test3', () {
    expect(largest<String>(['a', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['only']), 'only', reason: '--err-t3--');
    expect(largest(['a', 'b', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['z', 'b', 'a']), 'z', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
T largest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) > 0) {
      best = item;
    }
  }
  return best;
}
```
