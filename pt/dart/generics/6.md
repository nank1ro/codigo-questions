---
language: dart
exerciseType: 4
---

# --description--

Dentro de uma classe genérica o parâmetro de tipo é visível em todos os lugares: em campos, em parâmetros de construtor, em assinaturas de métodos e em corpos de métodos. Ele é declarado uma vez, ao lado do nome da classe, e todo membro pode usá-lo.

```dart
class Holder<T> {
  final T item;

  Holder(this.item);

  String describe() => 'holding $item';
}
```

Criar o objeto é o que decide o tipo: `Holder<String>('fig')` torna `item` uma `String`, `Holder<int>(3)` o torna um `int`.

# --instructions--

Ordene os itens para definir uma classe genérica `Holder` com um campo `item` do tipo `T`, um construtor, e um método `describe`, e depois um `main` que imprime a descrição de uma `Holder<String>` construída com `'fig'`. Declare a classe antes do `main`. O programa deve imprimir:

```
holding fig
```

# --answers--

```dart
class Holder<T> {
  final T item;
  Holder(this.item);
```

```dart
  String describe() => 'holding $item';
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Holder<String>('fig').describe());
```

```dart
}
```

# --solutions--

```dart
class Holder<T> {
  final T item;
  Holder(this.item);
  String describe() => 'holding $item';
}

void main() {
  print(Holder<String>('fig').describe());
}
```

# --output--

holding fig
