---
language: dart
exerciseType: 1
---

# --description--

Um `typedef` dá um nome a um tipo, e pode receber parâmetros de tipo próprios. O motivo usual é nomear uma família de tipos de função uma única vez em vez de escrevê-la por extenso a cada uso:

```dart
typedef Transform<I, O> = O Function(I input);

final Transform<String, int> length = (word) => word.length;
print(length('kiwi')); // 4
```

`Transform<String, int>` é apenas outra forma de escrever `int Function(String)`, então os dois são intercambiáveis. O ganho é legibilidade: um parâmetro declarado como `Transform<I, O> transform` diz para que a função serve, enquanto `O Function(I)` diz apenas como ela é.

Um typedef genérico e uma função genérica se combinam naturalmente, com os próprios parâmetros de tipo da função preenchendo os do typedef.

# --instructions--

Declare um typedef genérico `Transform` com dois parâmetros de tipo `I` e `O` que nomeia uma função que recebe um `I` e retorna um `O`.

Depois escreva uma função genérica `convertAll` com parâmetros de tipo `I` e `O` que recebe uma `List<I>` `items` e uma `Transform<I, O>` `transform`, e retorna uma `List<O>` contendo cada item convertido, em ordem.

Exemplo:
```dart
print(convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)); // [3, 4]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)` deve retornar `[3, 4]`

```dart
  test('test1', () {
    expect(
      convertAll<String, int>(['fig', 'kiwi'], (w) => w.length),
      [3, 4],
      reason: '--err-t1--',
    );
  });
```

`convertAll` deve retornar uma `List<O>`, então converter ints em strings deve dar uma `List<String>`

```dart
  test('test2', () {
    final result = convertAll<int, String>([1, 2], (n) => 'n$n');
    expect(result, ['n1', 'n2'], reason: '--err-t2--');
    expect(result, isA<List<String>>(), reason: '--err-t2--');
  });
```

O typedef `Transform` deve nomear um tipo de função, e uma lista vazia deve dar uma lista vazia

```dart
  test('test3', () {
    final Transform<int, int> twice = (n) => n * 2;
    expect(convertAll<int, int>([1, 2, 3], twice), [2, 4, 6], reason: '--err-t3--');
    expect(convertAll<String, int>(<String>[], (w) => w.length), <int>[], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
typedef Transform<I, O> = O Function(I input);

List<O> convertAll<I, O>(List<I> items, Transform<I, O> transform) {
  return items.map(transform).toList();
}
```
