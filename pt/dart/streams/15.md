---
language: dart
exerciseType: 4
---

# --description--

Uma stream pode carregar **erros** além de valores. Dentro de um gerador `async*`, um `throw` envia um evento de erro e encerra a stream; um `StreamController` pode enviar um com `addError`.

Do lado de quem consome, um laço `await for` relança o erro no ponto onde o laço está, então você o trata com um `try`/`catch` comum em volta do laço:

```dart
Stream<int> risky() async* {
  yield 1;
  throw StateError('sensor offline');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('caught: $e');
  }
}
// 1
// caught: Bad state: sensor offline
```

Com `listen`, passe em vez disso um callback `onError`: `stream.listen(print, onError: (e) => print('caught: $e'));`

# --instructions--

Ordene os itens para definir uma função `async*` `risky` que emite `1` e depois lança uma `FormatException('bad')`, e um `main` `async` que imprime cada valor de `risky()` dentro de um `try`/`catch`, imprime `error: ` seguido do erro quando o captura, e imprime `finished` depois de todo o bloco `try`/`catch`, fora dele. Defina `risky` antes de `main`. O programa deve imprimir:

```
1
error: FormatException: bad
finished
```

# --answers--

```dart
Stream<int> risky() async* {
```

```dart
  yield 1;
```

```dart
  throw FormatException('bad');
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  try {
```

```dart
    await for (final n in risky()) {
```

```dart
      print(n);
```

```dart
    }
```

```dart
  } catch (e) {
```

```dart
    print('error: $e');
```

```dart
  }
```

```dart
  print('finished');
```

```dart
}
```

# --solutions--

```dart
Stream<int> risky() async* {
  yield 1;
  throw FormatException('bad');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('error: $e');
  }
  print('finished');
}
```

# --output--

1
error: FormatException: bad
finished
