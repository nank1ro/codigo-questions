---
language: dart
exerciseType: 1
---

# --description--

Como `Iterable`, uma `Stream` tem métodos que constroem uma **nova stream** a partir de uma existente:

- `map` transforma cada evento
- `where` mantém apenas os eventos que satisfazem uma condição
- `take` para depois de um dado número de eventos

```dart
final numbers = Stream.fromIterable([1, 2, 3, 4, 5, 6]);
final firstOdds = numbers.where((n) => n.isOdd).take(2);
print(await firstOdds.toList()); // [1, 3]
```

Esses métodos são **preguiçosos**: nada roda até que alguém escute a stream resultante. Eles podem ser encadeados, e a stream de origem nunca é modificada.

# --instructions--

Escreva uma função `evenSquares` que recebe uma `Stream<int>` `source` e retorna uma `Stream<int>` contendo o quadrado de cada valor **par** de `source`, na mesma ordem.

Exemplo:
```dart
final source = Stream.fromIterable([1, 2, 3, 4]);
print(await evenSquares(source).toList()); // [4, 16]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`evenSquares` de uma stream de `1, 2, 3, 4` deveria emitir `4, 16`

```dart
  test('test1', () async {
    expect(await evenSquares(Stream.fromIterable([1, 2, 3, 4])).toList(), [4, 16], reason: '--err-t1--');
  });
```

`evenSquares` de uma stream de `6, 5, 10` deveria emitir `36, 100`

```dart
  test('test2', () async {
    expect(await evenSquares(Stream.fromIterable([6, 5, 10])).toList(), [36, 100], reason: '--err-t2--');
  });
```

`evenSquares` de uma stream de `1, 3` não deveria emitir nada e deveria retornar uma `Stream<int>`

```dart
  test('test3', () async {
    expect(await evenSquares(Stream.fromIterable([1, 3])).toList(), <int>[], reason: '--err-t3--');
    expect(evenSquares(Stream.fromIterable([2])), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> evenSquares(Stream<int> source) {
  return source.where((n) => n.isEven).map((n) => n * n);
}
```
