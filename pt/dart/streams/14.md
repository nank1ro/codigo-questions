---
language: dart
exerciseType: 2
---

# --description--

Um controlador pode criar uma stream broadcast diretamente com o construtor nomeado `StreamController<T>.broadcast()`. Sua `stream` aceita qualquer número de ouvintes, e cada evento é entregue a todos eles, na ordem em que se inscreveram:

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>.broadcast();
  controller.stream.listen((msg) => print('first: $msg'));
  controller.stream.listen((msg) => print('second: $msg'));
  controller.add('hi');
  controller.close();
}
// first: hi
// second: hi
```

Como toda stream broadcast, ela não usa buffer: eventos adicionados antes de um ouvinte se inscrever são perdidos para esse ouvinte.

# --instructions--

Complete o código para criar um controlador broadcast, inscrever dois ouvintes `A` e `B`, enviar os valores `1` e `2`, e depois fechar o controlador, de modo que o programa imprima `A got 1`, `B got 1`, `A got 2`, `B got 2`.

# --seed--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.[/]();
  controller.stream.[/]((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.[/](1);
  controller.add(2);
  controller.[/]();
}
```

# --answers--

- broadcast
- listen
- add
- close
- asBroadcastStream
- subscribe
- sink

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.broadcast();
  controller.stream.listen((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
```

# --output--

A got 1
B got 1
A got 2
B got 2
