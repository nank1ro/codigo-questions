---
language: dart
exerciseType: 3
---

# --description--

Toda stream acaba terminando. Em um gerador `async*`, a stream está **concluída** assim que o corpo da função termina, seja porque chegou ao fim, seja porque encontrou um `return`. Um laço `await for` sobre uma stream concluída sai, e qualquer future de `toList()` se completa.

Uma stream não recomeça nem repete seus valores: uma vez concluída, permanece concluída.

# --instructions--

O que acontece com a stream retornada por uma função `async*` quando o corpo da função termina?

# --answers--

- A stream está concluída, e um laço `await for` sobre ela termina
- A stream recomeça a partir do primeiro `yield`
- A stream continua aberta até que `close()` seja chamado nela
- O último valor emitido é emitido mais uma vez

# --solutions--

- A stream está concluída, e um laço `await for` sobre ela termina
