---
language: dart
exerciseType: 1
---

# --description--

`Stream.fromIterable` precisa de todos os valores de antemão. Para **produzir** valores um de cada vez, escreva um **gerador assíncrono**: uma função cujo corpo é marcado com `async*` e cujo tipo de retorno é `Stream<T>`. Dentro dela, `yield` envia um evento para a stream:

```dart
Stream<int> countTo(int n) async* {
  for (var i = 1; i <= n; i++) {
    yield i;
  }
}
```

O corpo não roda quando você chama `countTo(3)`: ele roda preguiçosamente, à medida que o ouvinte pede valores, e a stream está concluída quando o corpo termina.

Para juntar todos os eventos em uma `List`, chame `toList()`. Ele retorna um `Future<List<T>>`, então você o aguarda com `await`:

```dart
final values = await countTo(3).toList();
print(values); // [1, 2, 3]
```

# --instructions--

Escreva uma função `async*` `countdown` que recebe um `int` `from` e retorna uma `Stream<int>` emitindo os valores de `from` até `1`. Se `from` for `0` ou menor, a stream não emite nada.

Exemplo:
```dart
print(await countdown(3).toList()); // [3, 2, 1]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countdown(3)` deveria emitir `3`, `2`, `1`

```dart
  test('test1', () async {
    expect(await countdown(3).toList(), [3, 2, 1], reason: '--err-t1--');
  });
```

`countdown(1)` deveria emitir apenas `1`

```dart
  test('test2', () async {
    expect(await countdown(1).toList(), [1], reason: '--err-t2--');
  });
```

`countdown(0)` não deveria emitir nada e `countdown` deveria retornar uma `Stream<int>`

```dart
  test('test3', () async {
    expect(await countdown(0).toList(), <int>[], reason: '--err-t3--');
    expect(countdown(2), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> countdown(int from) async* {
  for (var i = from; i >= 1; i--) {
    yield i;
  }
}
```
