---
language: dart
exerciseType: 4
---

# --description--

Um gerador `async*` pode repassar **todos os eventos de outra stream** com `yield*` (yield-star). É como um laço `await for` que emite cada valor, em uma única linha:

```dart
Stream<int> ones() async* {
  yield 1;
  yield 1;
}

Stream<int> sequence() async* {
  yield 0;
  yield* ones();
  yield 2;
}
// sequence() emits 0, 1, 1, 2
```

A stream externa continua com seus próprios `yield` assim que a stream interna está concluída.

# --instructions--

Ordene os itens para definir uma função `async*` `pair` que emite `1` e `2`, uma função `async*` `all` que emite `0`, depois cada valor de `pair()`, depois `3`, e um `main` que imprime cada valor de `all()`. Defina primeiro `pair`, depois `all`, depois `main`. O programa deve imprimir:

```
0
1
2
3
```

# --answers--

```dart
Stream<int> pair() async* {
```

```dart
  yield 1;
```

```dart
  yield 2;
```

```dart
}
```

```dart
Stream<int> all() async* {
```

```dart
  yield 0;
```

```dart
  yield* pair();
```

```dart
  yield 3;
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  await for (final n in all()) {
```

```dart
    print(n);
```

```dart
  }
```

```dart
}
```

# --solutions--

```dart
Stream<int> pair() async* {
  yield 1;
  yield 2;
}

Stream<int> all() async* {
  yield 0;
  yield* pair();
  yield 3;
}

Future<void> main() async {
  await for (final n in all()) {
    print(n);
  }
}
```

# --output--

0
1
2
3
