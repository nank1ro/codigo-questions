---
language: dart
exerciseType: 1
---

# --description--

Além de `toList()`, uma stream oferece outros métodos que **consomem** todos os seus eventos e retornam um único `Future`:

- `first` e `last` se completam com o primeiro ou o último evento
- `length` se completa com o número de eventos
- `join(separator)` se completa com todos os eventos unidos em uma única `String`
- `reduce(combine)` combina os eventos dois a dois em um único valor

```dart
final numbers = Stream.fromIterable([3, 9, 2]);
print(await numbers.reduce((a, b) => a + b)); // 14
```

`reduce` chama `combine` com o resultado obtido até então e o próximo evento. Ele lança uma exceção se a stream estiver vazia, então use-o apenas quando pelo menos um evento for garantido.

# --instructions--

Escreva duas funções:
- `largest`, que recebe uma `Stream<int>` `source` e retorna um `Future<int>` que se completa com o maior valor, usando `reduce`. A stream sempre tem pelo menos um evento.
- `sentence`, que recebe uma `Stream<String>` `words` e retorna um `Future<String>` que se completa com todas as palavras unidas por um único espaço.

Exemplo:
```dart
print(await largest(Stream.fromIterable([3, 9, 2])));        // 9
print(await sentence(Stream.fromIterable(['I', 'am', 'Dart']))); // I am Dart
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest` de uma stream de `3, 9, 2` deveria se completar com `9`

```dart
  test('test1', () async {
    expect(await largest(Stream.fromIterable([3, 9, 2])), 9, reason: '--err-t1--');
  });
```

`largest` de uma stream de `-4, -1, -7` deveria se completar com `-1` e `largest` de uma stream de `5` deveria se completar com `5`

```dart
  test('test2', () async {
    expect(await largest(Stream.fromIterable([-4, -1, -7])), -1, reason: '--err-t2--');
    expect(await largest(Stream.fromIterable([5])), 5, reason: '--err-t2--');
  });
```

`sentence` de uma stream de `'I', 'am', 'Dart'` deveria se completar com `'I am Dart'`

```dart
  test('test3', () async {
    expect(await sentence(Stream.fromIterable(['I', 'am', 'Dart'])), 'I am Dart', reason: '--err-t3--');
    expect(await sentence(Stream.fromIterable(['solo'])), 'solo', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> largest(Stream<int> source) {
  return source.reduce((a, b) => a > b ? a : b);
}

Future<String> sentence(Stream<String> words) {
  return words.join(' ');
}
```
