---
language: dart
exerciseType: 1
---

# --description--

Geradores produzem eventos de dentro de uma função. Quando os eventos vêm de **outro lugar** (um botão, um callback de rede, outro objeto) você precisa de um **`StreamController`**. Ele vive na biblioteca `dart:async`, então o arquivo deve começar com `import 'dart:async';`.

Um controlador é dono de uma stream e permite que você empurre eventos para dentro dela:

```dart
import 'dart:async';

Stream<int> dice() {
  final controller = StreamController<int>();
  controller.add(4);
  controller.add(2);
  controller.close();
  return controller.stream;
}
```

- `add(value)` envia um evento
- `close()` encerra a stream; esquecê-lo significa que os ouvintes esperam para sempre
- `stream` é a `Stream` que os ouvintes consomem

Eventos adicionados antes de alguém escutar são guardados em um buffer, então o código acima é seguro: um ouvinte que chega depois ainda recebe `4` e `2`.

# --instructions--

Escreva uma função `announce` que recebe uma `List<String>` `names` e retorna uma `Stream<String>` construída com um `StreamController`: adicione cada nome em maiúsculas, feche o controlador e retorne sua stream. Lembre-se do import de `dart:async`.

Exemplo:
```dart
print(await announce(['ada', 'linus']).toList()); // [ADA, LINUS]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`announce(['ada', 'linus'])` deveria emitir `'ADA'`, `'LINUS'`

```dart
  test('test1', () async {
    expect(await announce(['ada', 'linus']).toList(), ['ADA', 'LINUS'], reason: '--err-t1--');
  });
```

`announce(['Dart'])` deveria emitir `'DART'`

```dart
  test('test2', () async {
    expect(await announce(['Dart']).toList(), ['DART'], reason: '--err-t2--');
  });
```

`announce([])` não deveria emitir nada e deveria terminar, e `announce` deveria retornar uma `Stream<String>`

```dart
  test('test3', () async {
    expect(await announce([]).toList(), <String>[], reason: '--err-t3--');
    expect(announce(['x']), isA<Stream<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
import 'dart:async';

Stream<String> announce(List<String> names) {
  final controller = StreamController<String>();
  for (final name in names) {
    controller.add(name.toUpperCase());
  }
  controller.close();
  return controller.stream;
}
```
