---
language: dart
exerciseType: 2
---

# --description--

Como `where`, `map` e `take` retornam cada um uma stream, você pode encadeá-los e terminar com `toList()` para obter o resultado como uma lista. Apenas o `toList()` final precisa de um `await`, já que é a única chamada que retorna um `Future`:

```dart
final numbers = Stream.fromIterable([5, 12, 8, 20, 3]);
final big = await numbers.where((n) => n > 6).take(2).toList();
print(big); // [12, 8]
```

# --instructions--

Complete o código para manter apenas as palavras com exatamente `3` letras, parar depois das `2` primeiras delas e juntá-las em uma lista, de modo que o programa imprima `[sun, sky]`.

# --seed--

```dart
Future<void> main() async {
  final words = Stream.fromIterable(['sun', 'moon', 'star', 'sky', 'fog']);
  final short = await words.[/]((w) => w.length == 3).[/](2).[/]();
  print(short);
}
```

# --answers--

- where
- take
- toList
- map
- skip
- first

# --solutions--

```dart
Future<void> main() async {
  final words = Stream.fromIterable(['sun', 'moon', 'star', 'sky', 'fog']);
  final short = await words.where((w) => w.length == 3).take(2).toList();
  print(short);
}
```

# --output--

[sun, sky]
