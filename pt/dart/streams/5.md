---
language: dart
exerciseType: 2
---

# --description--

`await for` pausa a função atual até que a stream esteja concluída. Quando você quer reagir aos eventos **sem esperar**, chame `listen` e passe um callback: ele é invocado uma vez por evento, e o código depois de `listen` roda imediatamente.

```dart
final ticks = Stream.fromIterable([1, 2]);
ticks.listen((tick) => print('tick $tick'));
print('not waiting');
// not waiting
// tick 1
// tick 2
```

`listen` também aceita um parâmetro nomeado `onDone`, uma função sem argumentos chamada quando a stream termina:

```dart
final ticks2 = Stream.fromIterable([1, 2]);
ticks2.listen((tick) => print(tick), onDone: () => print('no more ticks'));
```

# --instructions--

Complete o código para que a stream seja escutada, cada letra seja impressa e `done` seja impresso quando a stream terminar. O programa deve imprimir `listening`, depois `a`, `b` e `done`.

# --seed--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.[/]((letter) => print(letter), [/]: () => print('done'));
  print('listening');
}
```

# --answers--

- listen
- onDone
- onError
- forEach
- await

# --solutions--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.listen((letter) => print(letter), onDone: () => print('done'));
  print('listening');
}
```

# --output--

listening
a
b
done
