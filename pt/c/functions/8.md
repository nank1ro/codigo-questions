---
language: c
exerciseType: 2
---

# --instructions--

Complete a função para imprimir o resultado correto chamando a função `power` com uma base de `4` e um expoente de `5`

# --seed--

```c
const int [/](int [/], int exp) {
    return pow(base, [/]);
}

int main() {
    printf("%d\n", [/](4, [/]));
    return 0;
}
```

# --answers--

- 5
- base
- exponent
- exp
- power
- exponent
- Double
- Int
- power
- result

# --solutions--

```c
const int power(int base, int exp) {
    return pow(base, exp);
}

int main() {
    printf("%d\n", power(4, 5));
    return 0;
}
```

# --output--

1024
