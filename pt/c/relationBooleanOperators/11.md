---
language: c
exerciseType: 2
---

# --instructions--

Verifique se `word` **NAO** e igual a `dog`, imprimindo o resultado booleano.
Esteja ciente de que `strcmp` retorna `0` apenas quando as strings sao **iguais**, entao nao se deixe enganar pensando que o 0 que ele retorna e como o booleano false

# --seed--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat")[/][/];
printf("%d\n", [/]);
```

# --answers--

- word
- "dog"
-  == 
-  = 
- dog
-  != 
- 0
- are_words_equal

# --solutions--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat") == 0;
printf("%d\n", are_words_equal);
```

# --output--

1
