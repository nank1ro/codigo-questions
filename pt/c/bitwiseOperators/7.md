---
language: c
exerciseType: 4
---

# --description--

Cada operador bit a bit binário tem uma forma de **atribuição composta** que atualiza uma variável no próprio lugar: `&=`, `|=`, `^=`, `<<=` e `>>=`.
```c
unsigned int x = 12;
x &= 10;  // é igual a x = x & 10;
x |= 1;   // é igual a x = x | 1;
x ^= 3;   // é igual a x = x ^ 3;
x <<= 1;  // é igual a x = x << 1;
x >>= 2;  // é igual a x = x >> 2;
```
Eles ficam mais legíveis do que repetir o nome da variável e são a maneira usual de alterar os bits de uma variável de flags.

# --instructions--

Ordene as linhas para que `flags` comece em `0x01`, depois receba o bit de valor `4` ligado com `|=`, depois seja deslocado 2 casas para a esquerda com `<<=` e, por fim, seja exibido

# --answers--

```c
unsigned int flags = 0x01;
```

```c
flags |= 0x04;
```

```c
flags <<= 2;
```

```c
printf("%u\n", flags);
```

# --solutions--

```c
unsigned int flags = 0x01;
flags |= 0x04;
flags <<= 2;
printf("%u\n", flags);
```

# --output--

20
