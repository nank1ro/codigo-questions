---
language: c
exerciseType: 4
---

# --description--

Juntando tudo: escolha o tipo a partir do tipo de valor, combine cada especificador do `printf` com o tipo de seu argumento, e faça o cast quando um cálculo precisar acontecer em um tipo diferente do de seus operandos.

# --instructions--

Ordene as linhas para construir um programa que armazene `7` em `total` e `2` em `count`, calcule `total` dividido por `count` como um `double` em `avg`, e imprima a divisão inteira `total / count` seguida de `avg` com uma casa decimal

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int total = 7, count = 2;
```

```c
    double avg = (double) total / count;
```

```c
    printf("%d %.1f\n", total / count, avg);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int total = 7, count = 2;
    double avg = (double) total / count;
    printf("%d %.1f\n", total / count, avg);
    return 0;
}
```

# --output--

3 3.5
