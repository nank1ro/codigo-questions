---
language: c
exerciseType: 4
---

# --description--

`sprintf` funciona como `printf`, mas escreve o texto formatado em um array de caracteres em vez de exibi-lo na tela:
```c
char buffer[30];
sprintf(buffer, "%d items", 3); // buffer is "3 items"
```
O buffer deve ser grande o suficiente para todo o texto e seu `'\0'`.

# --instructions--

Ordene as linhas para construir o texto `Age: 25` dentro de `buffer` e exibi-lo. Declare `buffer` antes de `age`

# --answers--

```c
char buffer[30];
```

```c
int age = 25;
```

```c
sprintf(buffer, "Age: %d", age);
```

```c
printf("%s\n", buffer);
```

# --solutions--

```c
char buffer[30];
int age = 25;
sprintf(buffer, "Age: %d", age);
printf("%s\n", buffer);
```

# --output--

Age: 25
