---
language: c
exerciseType: 2
---

# --description--

O cabeçalho `ctype.h` fornece funções que trabalham com um único caractere.
`toupper(c)` retorna a versão maiúscula de uma letra e `tolower(c)` a versão minúscula; qualquer outro caractere é retornado sem alteração:
```c
char letter = toupper('a'); // 'A'
```

# --instructions--

Exiba `Ab` convertendo `first` para maiúscula e `second` para minúscula

# --seed--

```c
#include <stdio.h>
#include <ctype.h>

int main() {
    char first = 'a';
    char second = 'B';
    printf("%c%c\n", [/](first), [/](second));
    return 0;
}
```

# --answers--

- toupper
- tolower
- upper
- lower
- capitalize

# --solutions--

```c
#include <stdio.h>
#include <ctype.h>

int main() {
    char first = 'a';
    char second = 'B';
    printf("%c%c\n", toupper(first), tolower(second));
    return 0;
}
```

# --output--

Ab
