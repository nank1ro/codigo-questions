---
language: c
exerciseType: 3
---

# --description--

`strcmp` compara as strings caractere por caractere usando seus códigos de caractere.
O resultado é negativo quando a primeira string vem antes da segunda, positivo quando vem depois, e `0` quando são iguais:
```c
strcmp("a", "b"); // negative
strcmp("b", "a"); // positive
```

# --instructions--

O que `strcmp("apple", "banana")` retorna?

# --answers--

- Um número negativo
- 0
- Um número positivo
- A string "apple"

# --solutions--

- Um número negativo
