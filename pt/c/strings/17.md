---
language: c
exerciseType: 4
---

# --instructions--

Ordene as linhas para que o programa compare `password` com `attempt` e exiba `Access granted`. Declare `password` antes de `attempt` e coloque cada mensagem dentro do seu próprio ramo

# --answers--

```c
char password[] = "secret";
```

```c
char attempt[] = "secret";
```

```c
if (strcmp(password, attempt) == 0) {
```

```c
    printf("Access granted\n");
```

```c
} else {
```

```c
    printf("Access denied\n");
```

```c
}
```

# --solutions--

```c
char password[] = "secret";
char attempt[] = "secret";
if (strcmp(password, attempt) == 0) {
    printf("Access granted\n");
} else {
    printf("Access denied\n");
}
```

# --output--

Access granted
