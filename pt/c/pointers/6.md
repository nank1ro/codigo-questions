---
language: c
exerciseType: 4
---

# --description--

Um ponteiro para `char` funciona como qualquer outro ponteiro: ele guarda o endereço de um único caractere, e `*p` lê ou escreve esse caractere:
```c
char grade = 'B';
char *p = &grade;
*p = 'A';
printf("%c\n", grade); // prints "A"
```
Ler por meio de um ponteiro e escrever por meio dele podem ser misturados livremente: `*p = *p + 1` transforma `'A'` em `'B'`.

# --instructions--

Ordene as linhas para declarar `letter` como `'a'`, apontar `p` para ela, substituir a letra por meio do ponteiro com `'b'` e imprimir `letter`

# --answers--

```c
char letter = 'a';
```

```c
char *p = &letter;
```

```c
*p = 'b';
```

```c
printf("%c\n", letter);
```


# --solutions--

```c
char letter = 'a';
char *p = &letter;
*p = 'b';
printf("%c\n", letter);
```

# --output--

b
