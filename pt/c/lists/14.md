---
language: c
exerciseType: 4
---

# --description--

Inverter uma lista vira cada ponteiro `next` para o outro lado, no próprio lugar, com três ponteiros: `prev` é a parte já invertida, `head` o nó sendo processado e `next` uma cópia do resto da lista, salvo antes de o elo ser alterado. A cada passo o nó atual é apontado de volta para `prev`, então tanto `prev` quanto `head` avançam:
```c
Node *next = head->next;
head->next = prev;
prev = head;
head = next;
```
Quando `head` alcança `NULL` todos os elos foram invertidos e `prev` é a nova cabeça.

# --instructions--

Ordene as linhas para definir `reverse`, que inverte uma lista no próprio lugar e retorna sua nova cabeça, e uma `main` que imprime a lista invertida `1, 2, 3`. Defina a função antes da `main`

# --answers--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
```

```c
Node *reverse(Node *head) {
    Node *prev = NULL;
```

```c
    while (head != NULL) {
        Node *next = head->next;
```

```c
        head->next = prev;
```

```c
        prev = head;
```

```c
        head = next;
    }
```

```c
    return prev;
}
```

```c
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
```

```c
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
Node *reverse(Node *head) {
    Node *prev = NULL;
    while (head != NULL) {
        Node *next = head->next;
        head->next = prev;
        prev = head;
        head = next;
    }
    return prev;
}
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --output--

3
2
1
