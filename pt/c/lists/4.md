---
language: c
exerciseType: 2
---

# --description--

Percorrer uma lista, chamado de **percurso**, é um loop que começa na cabeça e segue o `next` até alcançar `NULL`. Um loop `for` o expressa em uma única linha, com um ponteiro como variável do loop:
```c
for (Node *n = head; n != NULL; n = n->next) {
    printf("%d\n", n->value);
}
```
Não há índice: a única forma de alcançar um nó é pelo ponteiro armazenado no nó anterior.

# --instructions--

Complete o loop para que ele comece em `a` e imprima o valor de cada nó da lista

# --seed--

```c
typedef struct Node {
    int value;
    struct Node *next;
} Node;
Node c = {3, NULL};
Node b = {2, &c};
Node a = {1, &b};
for (Node *n = [/]; n [/] NULL; n = [/]) {
    printf("%d\n", n->value);
}
```

# --answers--

- &a
- a
- !=
- ==
- n->next
- n.next

# --solutions--

```c
typedef struct Node {
    int value;
    struct Node *next;
} Node;
Node c = {3, NULL};
Node b = {2, &c};
Node a = {1, &b};
for (Node *n = &a; n != NULL; n = n->next) {
    printf("%d\n", n->value);
}
```

# --output--

1
2
3
