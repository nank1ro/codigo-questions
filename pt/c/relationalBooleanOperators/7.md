---
language: c
exerciseType: 2
---

# --description--

Desde o C99, o cabeçalho `stdbool.h` fornece o tipo `bool` e as constantes `true` (que é `1`) e `false` (que é `0`):
```c
#include <stdbool.h>

bool open = true;
bool full = 3 > 5;
printf("%d %d\n", open, full); // prints "1 0"
```
Um `bool` ainda é um inteiro por baixo: ele é impresso com `%d`, e funciona com `&&`, `||` e `!` exatamente como o resultado de uma comparação. Ele apenas torna a intenção mais clara do que um `int` simples.

# --instructions--

Complete o código para que `open` seja a constante booleana para verdadeiro e `closed` seja sua negação, e depois imprima ambos

# --seed--

```c
bool open = [/];
bool closed = [/]open;
printf("%d %d\n", open, closed);
```

# --answers--

- true
- !
- false
- -
- ~

# --solutions--

```c
bool open = true;
bool closed = !open;
printf("%d %d\n", open, closed);
```

# --output--

1 0
