---
language: c
exerciseType: 4
---

# --description--

A avaliação de curto-circuito também pula chamadas de função: em `0 && check()` a função `check` nunca é chamada, então qualquer efeito colateral que ela tenha (como atualizar um contador) não acontece.

# --instructions--

Ordene as linhas para definir um contador `calls`, uma função `check` que o incrementa e retorna `1`, e uma `main` que calcula `0 && check()` e imprime o resultado seguido de `calls`. Defina a função antes de `main`

# --answers--

```c
#include <stdio.h>
int calls = 0;
```

```c
int check() {
```

```c
    calls++;
    return 1;
}
```

```c
int main() {
```

```c
    int result = 0 && check();
```

```c
    printf("%d %d\n", result, calls);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int calls = 0;
int check() {
    calls++;
    return 1;
}
int main() {
    int result = 0 && check();
    printf("%d %d\n", result, calls);
    return 0;
}
```

# --output--

0 0
