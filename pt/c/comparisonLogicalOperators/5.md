---
language: c
exerciseType: 2
---

# --description--

O operador **ou** `||` dá `1` quando pelo menos um dos lados é verdadeiro, e `0` somente quando ambos os lados são falsos:
```c
int day = 1;
printf("%d\n", day == 1 || day == 7); // imprime "1"
```
Cada lado deve ser uma comparação completa: `day == 6 || 7` não significa "6 ou 7" (você verá o motivo mais adiante).

# --instructions--

Complete o código para que `weekend` seja `1` quando `day` for `6` ou `day` for `7`, depois imprima-o

# --seed--

```c
int day = 7;
int weekend = day == 6 [/] day == [/];
printf("%d\n", weekend);
```

# --answers--

- ||
- 7
- &&
- 6
- =

# --solutions--

```c
int day = 7;
int weekend = day == 6 || day == 7;
printf("%d\n", weekend);
```

# --output--

1
