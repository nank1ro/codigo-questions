---
language: c
exerciseType: 4
---

# --description--

Um parâmetro `bool` pode ser usado diretamente como operando de `&&` ou `||`, sem compará-lo a `true`: escreva `age >= 18 && citizen`, não `citizen == true`.

# --instructions--

Ordene as linhas para definir `can_vote`, que retorna `true` quando `age` é pelo menos `18` e `citizen` é `true`, e uma `main` que imprime o resultado para `20, true` e depois para `20, false`. Defina a função antes de `main`

# --answers--

```c
#include <stdio.h>
#include <stdbool.h>
```

```c
bool can_vote(int age, bool citizen) {
```

```c
    return age >= 18 && citizen;
}
```

```c
int main() {
```

```c
    printf("%d\n", can_vote(20, true));
```

```c
    printf("%d\n", can_vote(20, false));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
#include <stdbool.h>
bool can_vote(int age, bool citizen) {
    return age >= 18 && citizen;
}
int main() {
    printf("%d\n", can_vote(20, true));
    printf("%d\n", can_vote(20, false));
    return 0;
}
```

# --output--

1
0
