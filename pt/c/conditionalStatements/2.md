---
language: c
exerciseType: 2
---

# --description--

Vamos continuar com o exemplo anterior.
```c
bool nice_weather = true;
if (nice_weather) {
    // play outside
}
```
Vimos que a instrucao `if` executa o bloco de codigo apenas se a condicao for `true`.
Outra coisa importante a considerar e representada pelas **chaves** `{}` que indicam um bloco de codigo.

# --instructions--

Corrija a instrucao `if` adicionando as *chaves* `{}`

# --seed--

```c
int time = 8;
[/] (time[/]12) [/]
    printf("It's morning\n");
[/]
```

# --answers--

- if
-  == 
-  = 
- else
-  ? 
-  < 
- {
- }

# --solutions--

```c
int time = 8;
if (time < 12) {
    printf("It's morning\n");
}
```

# --output--

It's morning
