---
language: c
exerciseType: 2
---

# --description--

Escrever `struct Point` toda vez é verboso. Com `typedef` você dá à struct um nome de tipo curto, e a própria struct pode permanecer anônima:
```c
typedef struct {
    double celsius;
} Temperature;

Temperature t = {21.5};
```
O novo nome `Temperature` é usado sozinho, sem a palavra-chave `struct` na frente dele. Essa é a forma mais comum de declarar structs em programas reais.

# --instructions--

Complete o código para definir a struct anônima com o membro `celsius` sob o nome de tipo `Temperature`, depois declare `t` com esse tipo

# --seed--

```c
typedef [/] {
    double celsius;
} Temperature;
[/] t = {21.5};
printf("%.1f\n", t.celsius);
```

# --answers--

- struct
- enum
- typedef
- Temperature
- struct Temperature
- Temperature t

# --solutions--

```c
typedef struct {
    double celsius;
} Temperature;
Temperature t = {21.5};
printf("%.1f\n", t.celsius);
```

# --output--

21.5
