---
language: c
exerciseType: 2
---

# --description--

Através de um ponteiro, os membros são acessados com o operador **seta** `->` em vez do ponto:
```c
void grow(Box *b) {
    b->size = b->size * 2;
}
```
`b->size` é um atalho para `(*b).size`: primeiro siga o ponteiro, depois pegue o membro. O ponto funciona apenas em uma struct, a seta apenas em um ponteiro para uma struct.
Quem chama passa o endereço de sua variável com `&`, e a mudança feita através do ponteiro fica visível após a chamada.

# --instructions--

Complete `grow` para que ela dobre `size` através do ponteiro, e complete a chamada para que ela passe o endereço de `box`

# --seed--

```c
#include <stdio.h>

typedef struct { int size; } Box;

void grow(Box *b) {
    b[/]size = b->size * 2;
}

int main() {
    Box box = {5};
    grow([/]box);
    printf("%d\n", box.size);
    return 0;
}
```

# --answers--

- ->
- .
- &
- *
- (*b)

# --solutions--

```c
#include <stdio.h>

typedef struct { int size; } Box;

void grow(Box *b) {
    b->size = b->size * 2;
}

int main() {
    Box box = {5};
    grow(&box);
    printf("%d\n", box.size);
    return 0;
}
```

# --output--

10
