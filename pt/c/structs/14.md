---
language: c
exerciseType: 2
---

# --description--

Uma struct frequentemente contém texto, armazenado como um membro array de `char` com tamanho fixo:
```c
typedef struct {
    char name[20];
    int age;
} Person;
```
Um membro array não pode ser atribuído com `=` depois da declaração: `p.name = "Ann"` não compila. Copie o texto para ele com `strcpy` de `string.h`, passando o membro como destino:
```c
Person p;
strcpy(p.name, "Ann");
p.age = 30;
```
Apenas um inicializador com chaves na declaração aceita a string diretamente: `Person p = {"Ann", 30};`.

# --instructions--

Complete o código para substituir o nome de `p` por `"Ann"`, depois imprima o nome e a idade

# --seed--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
[/](p.name, "Ann");
printf("%s is %d\n", p[/]name, p.age);
```

# --answers--

- strcpy
- strcat
- strcmp
- strlen
- .
- ->

# --solutions--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
strcpy(p.name, "Ann");
printf("%s is %d\n", p.name, p.age);
```

# --output--

Ann is 30
