---
language: c
exerciseType: 4
---

# --description--

Structs podem ser armazenadas em um array como qualquer outro tipo. Cada elemento é inicializado com suas próprias chaves, e um loop os visita um por um, indexando primeiro e depois usando o ponto:
```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};

for (int i = 0; i < 3; i++) {
    printf("%d\n", path[i].y);
}
```

# --instructions--

Ordene as linhas para definir o tipo `Point`, armazenar três pontos em `path`, somar seus membros `x` em `sum` e imprimi-lo. Declare `path` antes de `sum`

# --answers--

```c
typedef struct { int x; int y; } Point;
```

```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
```

```c
int sum = 0;
```

```c
for (int i = 0; i < 3; i++) {
```

```c
    sum += path[i].x;
```

```c
}
```

```c
printf("%d\n", sum);
```

# --solutions--

```c
typedef struct { int x; int y; } Point;
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
int sum = 0;
for (int i = 0; i < 3; i++) {
    sum += path[i].x;
}
printf("%d\n", sum);
```

# --output--

6
