---
language: c
exerciseType: 4
---

# --description--

`printf` não tem especificador para uma struct inteira. A solução usual é uma pequena função que imprime os membros em um formato fixo, para que cada parte do programa mostre o valor da mesma maneira:
```c
void print_person(Person p) {
    printf("%s (%d)\n", p.name, p.age);
}
```

# --instructions--

Ordene as linhas para definir o tipo `Point`, uma função `print_point` que imprime um ponto como `(x, y)`, e uma `main` que imprime o ponto `(3, 4)`. Defina a função antes de `main`

# --answers--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
```

```c
void print_point(Point p) {
```

```c
    printf("(%d, %d)\n", p.x, p.y);
```

```c
}
```

```c
int main() {
```

```c
    Point p = {3, 4};
```

```c
    print_point(p);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
void print_point(Point p) {
    printf("(%d, %d)\n", p.x, p.y);
}
int main() {
    Point p = {3, 4};
    print_point(p);
    return 0;
}
```

# --output--

(3, 4)
