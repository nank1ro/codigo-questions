---
language: c
exerciseType: 2
---

# --description--

Escrever `enum Color` toda vez é verboso. Com `typedef` você dá à enumeração um nome de tipo curto, e a própria enumeração pode ficar anônima:
```c
typedef enum { RED, GREEN, BLUE } Color;

Color favorite = BLUE;
```
O novo nome `Color` é usado sozinho, sem a palavra-chave `enum` na frente dele.

# --instructions--

Complete o código para definir a enumeração anônima com as constantes `OFF` e `ON` sob o nome de tipo `State`, depois declare `lamp` com esse tipo

# --seed--

```c
typedef [/] { OFF, ON } State;
[/] lamp = ON;
printf("%d\n", lamp);
```

# --answers--

- enum
- struct
- State
- enum State
- typedef

# --solutions--

```c
typedef enum { OFF, ON } State;
State lamp = ON;
printf("%d\n", lamp);
```

# --output--

1
