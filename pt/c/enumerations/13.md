---
language: c
exerciseType: 4
---

# --description--

C não oferece nenhuma forma integrada de obter o nome de uma constante de enumeração: `printf("%d\n", SUMMER)` imprime `2`, não `Summer`. A solução habitual é uma pequena função com um `switch` que retorna a string correspondente a cada constante.

# --instructions--

Ordene as linhas para definir o tipo `Season`, uma função `season_name` que retorna o nome de uma estação, e uma `main` que imprime o nome de `SUMMER`. Defina a função antes de `main`, mantenha os casos na mesma ordem das constantes e coloque `default` por último

# --answers--

```c
#include <stdio.h>
typedef enum { WINTER, SPRING, SUMMER, AUTUMN } Season;
```

```c
char *season_name(Season season) {
```

```c
    switch (season) {
```

```c
        case WINTER: return "Winter";
```

```c
        case SPRING: return "Spring";
```

```c
        case SUMMER: return "Summer";
```

```c
        default: return "Autumn";
```

```c
    }
}
```

```c
int main() {
```

```c
    printf("%s\n", season_name(SUMMER));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef enum { WINTER, SPRING, SUMMER, AUTUMN } Season;
char *season_name(Season season) {
    switch (season) {
        case WINTER: return "Winter";
        case SPRING: return "Spring";
        case SUMMER: return "Summer";
        default: return "Autumn";
    }
}
int main() {
    printf("%s\n", season_name(SUMMER));
    return 0;
}
```

# --output--

Summer
