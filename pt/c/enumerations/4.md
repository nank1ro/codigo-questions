---
language: c
exerciseType: 2
---

# --description--

Uma enumeração também é um tipo: você pode declarar uma variável desse tipo escrevendo `enum` seguido do nome da enumeração, e atribuir a ela uma de suas constantes:
```c
enum Color favorite = GREEN;
```
Como as constantes são inteiros, você compara variáveis enum com os operadores usuais:
```c
if (favorite == GREEN) {
    printf("Green it is\n");
}
```

# --instructions--

Complete o código para declarar a variável `light` do tipo `enum Light` contendo `GREEN`, para que o programa imprima `Go`

# --seed--

```c
enum Light { RED, YELLOW, GREEN };
[/] Light light = [/];
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --answers--

- enum
- struct
- GREEN
- RED
- YELLOW
- "GREEN"

# --solutions--

```c
enum Light { RED, YELLOW, GREEN };
enum Light light = GREEN;
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --output--

Go
