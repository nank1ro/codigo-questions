---
language: python
exerciseType: 1
---

# --instructions--

Use as chaves de abertura `{` e fechamento `}` para inserir o número `500` no espaço vazio da string formatada

# --seed--

```python
print(f"There are  items")
```

# --solutions--

```python
print(f"There are {500} items")
```

# --output--

There are 500 items
