---
language: python
exerciseType: 1
---

# --instructions--

Você tem `60` produtos na `store`, durante o dia você vendeu `50` deles e `bought` `55` novos.
Atualize a quantidade da `store`.

# --seed--

```python
store = 60
sold = 50
bought = 55
# write after this line
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

A quantidade da `store` deve ser igual a 65

```python
    def test_1(self):
        self.assertEqual(store, 65, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
store = 60
sold = 50
bought = 55
store += bought - sold
```
