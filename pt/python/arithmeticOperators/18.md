---
language: python
exerciseType: 1
---

# --instructions--

Você tem `60` produtos na `store`, durante o dia você vendeu `50` deles e `comprou` `55` novos.
Atualize a quantidade de `store`.

# --seed--

```python
store = 60
sold = 50
bought = 55
# escreva após esta linha
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

A quantidade de `store` deve ser igual a 65

```python
    def test_1(self):
        self.assertEqual(store, 65, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
store = 60
sold = 50
bought = 55
store += bought - sold
```
