---
language: python
exerciseType: 1
---

# --description--

Variáveis do tipo string podem ser declaradas usando aspas simples ou duplas:
```python
>>> x = "May"
>>> x = 'May'
```
Ambas são a mesma coisa.

# --instructions--

Declare uma variável chamada `month` com o valor `june`.

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`month` deve ser igual a `"june"`.

```python
    def test_1(self):
        self.assertEqual(month, "june", "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
month = "june"
```
