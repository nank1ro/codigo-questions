---
language: python
exerciseType: 1
---

# --description--

`partial` é útil com funções embutidas que recebem opções. `int(text, base=16)` analisa uma string hexadecimal; fixar a base gera um conversor de um argumento que se encaixa no `map`:
```python
from functools import partial

hex_to_int = partial(int, base=16)
print(hex_to_int("ff"))                      # 255
print(list(map(hex_to_int, ["a", "10"])))    # [10, 16]
```
O objeto partial lembra o que ele envolve: `hex_to_int.func` é `int`, e `hex_to_int.keywords` é `{'base': 16}`.

# --instructions--

Defina duas funções com `partial`: `hex_to_int` que converte uma string hexadecimal em um `int`, e `binary_to_int` que converte uma string binária em um `int`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`hex_to_int("ff")` deve retornar `255`

```python
    def test_1(self):
        self.assertEqual(hex_to_int("ff"), 255, "--err-t1--")
        self.assertEqual(hex_to_int("10"), 16, "--err-t1--")
```

`binary_to_int("1010")` deve retornar `10`

```python
    def test_2(self):
        self.assertEqual(binary_to_int("1010"), 10, "--err-t2--")
        self.assertEqual(binary_to_int("11"), 3, "--err-t2--")
```

Ambas devem ser objetos partial envolvendo `int`

```python
    def test_3(self):
        self.assertIs(hex_to_int.func, int, "--err-t3--")
        self.assertIs(binary_to_int.func, int, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import partial

hex_to_int = partial(int, base=16)
binary_to_int = partial(int, base=2)
```
