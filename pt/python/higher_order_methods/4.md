---
language: python
exerciseType: 1
---

# --description--

A função embutida `filter(func, iterable)` mantém apenas os elementos para os quais `func` retorna um valor verdadeiro. Como `map`, ela retorna um objeto preguiçoso que deve ser convertido em uma lista:
```python
numbers = [4, -2, 7, 0]
print(list(filter(lambda n: n > 0, numbers)))  # [4, 7]
```
A função passada a `filter` é chamada de **predicado**: ela recebe um elemento e responde a uma pergunta de sim/não sobre ele. Passar `None` em vez de uma função mantém os elementos que são verdadeiros por si mesmos, descartando `0`, `""` e `None`.

# --instructions--

Escreva uma função `positives` que recebe uma lista de números e retorna uma nova lista com apenas os números maiores que zero, usando `filter`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `filter`

```json
{
  "regex": "filter\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`positives([3, -1, 0, 5])` deve retornar `[3, 5]`

```python
    def test_1(self):
        self.assertEqual(positives([3, -1, 0, 5]), [3, 5], "--err-t1--")
```

O resultado deve ser uma lista

```python
    def test_2(self):
        self.assertIsInstance(positives([1, 2]), list, "--err-t2--")
        self.assertEqual(positives([1, 2]), [1, 2], "--err-t2--")
```

`positives([-4, 0])` deve retornar uma lista vazia

```python
    def test_3(self):
        self.assertEqual(positives([-4, 0]), [], "--err-t3--")
        self.assertEqual(positives([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def positives(numbers):
    return list(filter(lambda n: n > 0, numbers))
```
