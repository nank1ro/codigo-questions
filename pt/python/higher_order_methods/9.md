---
language: python
exerciseType: 1
---

# --description--

`reduce(func, iterable, initial)` do módulo `functools` dobra uma sequência em um **único valor**. Ela chama `func` com o resultado até o momento e o próximo elemento, começando de `initial`:
```python
from functools import reduce

total = reduce(lambda acc, n: acc + n, [1, 2, 3], 0)
print(total)  # 6
```
Os passos são `0 + 1`, depois `1 + 2`, depois `3 + 3`. Quando `initial` é omitido, o primeiro elemento é usado como valor inicial, mas então uma sequência vazia levanta um `TypeError`, então forneça um valor inicial sempre que a sequência puder estar vazia.

# --instructions--

Escreva uma função `product` que recebe uma lista de números e retorna todos eles multiplicados entre si, usando `reduce`; o produto de uma lista vazia deve ser `1`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `reduce`

```json
{
  "regex": "reduce\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`product([2, 3, 4])` deve retornar `24`

```python
    def test_1(self):
        self.assertEqual(product([2, 3, 4]), 24, "--err-t1--")
```

`product([5])` deve retornar `5`

```python
    def test_2(self):
        self.assertEqual(product([5]), 5, "--err-t2--")
```

`product([])` deve retornar `1`

```python
    def test_3(self):
        self.assertEqual(product([]), 1, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import reduce

def product(numbers):
    return reduce(lambda acc, n: acc * n, numbers, 1)
```
