---
language: python
exerciseType: 1
---

# --description--

Passar uma função como argumento permite que quem chama decida **o quê** fazer, enquanto a função de ordem superior decide **quantas vezes** ou **sobre o quê**. O parâmetro função pode ser chamado quantas vezes for necessário, e o seu resultado pode ser passado de volta para ele:
```python
def repeat(func, value, times):
    for _ in range(times):
        value = func(value)
    return value

def add_one(n):
    return n + 1

print(repeat(add_one, 0, 3))  # 3
```
Qualquer chamável funciona: uma função `def`, uma função embutida como `len` ou uma `lambda`.

# --instructions--

Escreva uma função `apply_twice` que recebe uma função `func` e um `value`, e retorna o resultado de aplicar `func` a `value` duas vezes seguidas

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`apply_twice(lambda x: x * 2, 3)` deve retornar `12`

```python
    def test_1(self):
        self.assertEqual(apply_twice(lambda x: x * 2, 3), 12, "--err-t1--")
```

`apply_twice(lambda s: s + "!", "hi")` deve retornar `"hi!!"`

```python
    def test_2(self):
        self.assertEqual(apply_twice(lambda s: s + "!", "hi"), "hi!!", "--err-t2--")
```

A segunda chamada deve receber o resultado da primeira

```python
    def test_3(self):
        seen = []
        def record(x):
            seen.append(x)
            return x + 10
        self.assertEqual(apply_twice(record, 0), 20, "--err-t3--")
        self.assertEqual(seen, [0, 10], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def apply_twice(func, value):
    return func(func(value))
```
