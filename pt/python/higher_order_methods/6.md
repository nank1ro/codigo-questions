---
language: python
exerciseType: 4
---

# --description--

A função `key` pode escolher **qualquer parte** de um elemento. Para uma lista de tuplas, `lambda s: s[1]` ordena pelo segundo item de cada tupla; para uma lista de dicionários, `lambda d: d["age"]` ordena por um valor:
```python
pairs = [("b", 2), ("a", 3), ("c", 1)]
print(sorted(pairs, key=lambda p: p[1]))  # [('c', 1), ('b', 2), ('a', 3)]
```
`min` e `max` aceitam o mesmo parâmetro `key`, então `max(pairs, key=lambda p: p[1])` retorna `('a', 3)`: a tupla inteira, não apenas o número.

# --instructions--

Ordene os itens para imprimir as pontuações da mais alta para a mais baixa, uma por linha como `name: score`

# --answers--

```python
scores = [("Ada", 92), ("Linus", 85), ("Grace", 97)]
```

```python
ranked = sorted(scores, key=lambda s: s[1], reverse=True)
```

```python
for name, score in ranked:
```

```python
    print(f"{name}: {score}")
```

# --solutions--

```python
scores = [("Ada", 92), ("Linus", 85), ("Grace", 97)]
ranked = sorted(scores, key=lambda s: s[1], reverse=True)
for name, score in ranked:
    print(f"{name}: {score}")
```

# --output--

Grace: 97
Ada: 92
Linus: 85
