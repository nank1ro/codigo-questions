---
language: python
exerciseType: 1
---

# --description--

Uma função também pode **retornar** uma função. Defina uma função interna com `def` e retorne-a sem chamá-la:
```python
def make_greeter(greeting):
    def greet(name):
        return greeting + ", " + name
    return greet

hello = make_greeter("Hello")
print(hello("Ada"))  # Hello, Ada
```
A função interna continua usando `greeting` mesmo depois que `make_greeter` terminou: ela **lembra** as variáveis do escopo onde foi criada. Uma função assim é chamada de **closure**. Cada chamada a `make_greeter` cria uma nova closure independente com o seu próprio `greeting`.

# --instructions--

Escreva uma função `make_multiplier` que recebe um número `factor` e retorna uma função que recebe um número e retorna o número multiplicado por `factor`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`make_multiplier(2)` deve retornar uma função

```python
    def test_1(self):
        self.assertTrue(callable(make_multiplier(2)), "--err-t1--")
```

`make_multiplier(2)(5)` deve retornar `10`

```python
    def test_2(self):
        double = make_multiplier(2)
        self.assertEqual(double(5), 10, "--err-t2--")
        self.assertEqual(double(0), 0, "--err-t2--")
```

Cada multiplicador deve lembrar o seu próprio factor

```python
    def test_3(self):
        double = make_multiplier(2)
        triple = make_multiplier(3)
        self.assertEqual(triple(4), 12, "--err-t3--")
        self.assertEqual(double(4), 8, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def make_multiplier(factor):
    def multiply(number):
        return number * factor
    return multiply
```
