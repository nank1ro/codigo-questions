---
language: python
exerciseType: 1
---

# --description--

Um decorator que aceita apenas um argumento tem pouca utilidade. Para envolver **qualquer** função, o wrapper coleta cada argumento posicional em `*args` e cada argumento nomeado em `**kwargs`, e os encaminha sem alterações:
```python
def uppercase(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs).upper()
    return wrapper

@uppercase
def greet(name, punctuation="!"):
    return "hello " + name + punctuation

print(greet("ada"))                    # HELLO ADA!
print(greet("ada", punctuation="?"))   # HELLO ADA?
```
Dentro do wrapper, `args` é uma tupla e `kwargs` um dicionário; o `*` e o `**` na chamada os desempacotam de volta em argumentos separados.

# --instructions--

Escreva um decorator `exclaim` que retorna um wrapper que aceita quaisquer argumentos, chama a função envolvida com eles e retorna o seu resultado com `"!"` acrescentado; depois use-o para decorar uma função `greet` que recebe um `name` e retorna `"hello <name>"`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve aplicar o decorator com `@exclaim`

```json
{
  "regex": "^@exclaim\\s*$",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greet("ada")` deve retornar `"hello ada!"`

```python
    def test_1(self):
        self.assertEqual(greet("ada"), "hello ada!", "--err-t1--")
```

`exclaim` deve funcionar com uma função de dois argumentos

```python
    def test_2(self):
        joined = exclaim(lambda a, b: a + " " + b)
        self.assertEqual(joined("good", "day"), "good day!", "--err-t2--")
```

`exclaim` deve encaminhar argumentos nomeados

```python
    def test_3(self):
        tag = exclaim(lambda word, times=1: word * times)
        self.assertEqual(tag("hi", times=2), "hihi!", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def exclaim(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs) + "!"
    return wrapper

@exclaim
def greet(name):
    return "hello " + name
```
