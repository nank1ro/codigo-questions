---
language: python
exerciseType: 1
---

# --description--

`pop()` remove **um elemento arbitrário** do conjunto e o retorna.
Como um conjunto não tem ordem, você não pode escolher qual elemento é removido; chamar `pop()` em um conjunto vazio gera um `KeyError`:
```python
tickets = {101, 102, 103}
picked = tickets.pop()
print(len(tickets))  # 2
```
`clear()` remove **todos** os elementos, deixando um conjunto vazio:
```python
tickets.clear()
print(len(tickets))  # 0
```

# --instructions--

Remova um elemento de `tickets` com `pop()` e armazene-o na variável `picked`, depois esvazie o conjunto `drafts` com `clear()`

# --seed--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `pop()`

```json
{
  "regex": "\\.pop\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar `clear()`

```json
{
  "regex": "\\.clear\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`picked` deve ser um dos tickets originais

```python
    def test_1(self):
        self.assertIn(picked, {101, 102, 103}, "--err-t1--")
```

`tickets` deve conter 2 elementos e não conter mais `picked`

```python
    def test_2(self):
        self.assertEqual(len(tickets), 2, "--err-t2--")
        self.assertNotIn(picked, tickets, "--err-t2--")
```

`drafts` deve estar vazio

```python
    def test_3(self):
        self.assertEqual(drafts, set(), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
picked = tickets.pop()
drafts.clear()
```
