---
language: python
exerciseType: 1
---

# --description--

Dois conjuntos são **iguais** quando contêm os mesmos elementos, seja qual for a ordem em que foram escritos:
```python
print({1, 2, 3} == {3, 1, 2})  # True
```
Comparar o tamanho de uma coleção com o tamanho de seu conjunto é uma forma rápida de detectar duplicatas: se o conjunto for **menor**, algum valor apareceu mais de uma vez:
```python
emails = ["a@x.com", "b@x.com", "a@x.com"]
print(len(set(emails)) < len(emails))  # True
```
O método de lista `count(value)` diz quantas vezes um valor aparece, o que ajuda a descobrir *quais* valores estão duplicados.

# --instructions--

Armazene em `has_duplicates` se `emails` contém algum endereço repetido, comparando o tamanho da lista com o tamanho de seu conjunto, depois armazene em `duplicates` o conjunto dos endereços que aparecem mais de uma vez, usando uma compreensão de conjunto e `count()`

# --seed--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `set()`

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar uma compreensão de conjunto

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_duplicates` deve ser `True`

```python
    def test_1(self):
        self.assertEqual(has_duplicates, True, "--err-t1--")
```

`duplicates` deve ser igual a `{'ann@x.com', 'bob@x.com'}`

```python
    def test_2(self):
        self.assertEqual(duplicates, {"ann@x.com", "bob@x.com"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
has_duplicates = len(set(emails)) < len(emails)
duplicates = {e for e in emails if emails.count(e) > 1}
```
