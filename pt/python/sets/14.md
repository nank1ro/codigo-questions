---
language: python
exerciseType: 1
---

# --description--

Uma **compreensão de conjunto** constrói um conjunto em uma única expressão, com a mesma sintaxe de uma list comprehension, mas com chaves:
```python
nums = [1, 2, 2, 3]
squares = {n * n for n in nums}
print(squares)  # {1, 4, 9}
```
Um `if` opcional filtra os elementos, e duplicatas produzidas pela expressão são descartadas automaticamente:
```python
evens = {n for n in range(10) if n % 2 == 0}
print(evens)  # {0, 2, 4, 6, 8}
```

# --instructions--

Armazene em `lowered` o conjunto das palavras de `words` convertidas para minúsculas, usando uma compreensão de conjunto, depois em `long_words` o conjunto dos elementos de `lowered` com mais de 5 caracteres, usando uma compreensão de conjunto com `if`

# --seed--

```python
words = ["Apple", "banana", "APPLE", "Cherry", "banana"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar uma compreensão de conjunto

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`lowered` deve ser igual a `{'apple', 'banana', 'cherry'}`

```python
    def test_1(self):
        self.assertEqual(lowered, {"apple", "banana", "cherry"}, "--err-t1--")
```

`long_words` deve ser igual a `{'banana', 'cherry'}`

```python
    def test_2(self):
        self.assertEqual(long_words, {"banana", "cherry"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
words = ["Apple", "banana", "APPLE", "Cherry", "banana"]
lowered = {w.lower() for w in words}
long_words = {w for w in lowered if len(w) > 5}
```
