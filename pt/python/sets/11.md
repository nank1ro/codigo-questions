---
language: python
exerciseType: 4
---

# --description--

Conjuntos também podem ser **comparados** entre si.
`a.issubset(b)`, ou `a <= b`, é `True` quando todo elemento de `a` também está em `b`.
`a.issuperset(b)`, ou `a >= b`, é `True` quando `a` contém todo elemento de `b`.
`a.isdisjoint(b)` é `True` quando os dois conjuntos não têm **nenhum** elemento em comum:
```python
small = {1, 2}
big = {1, 2, 3}
print(small <= big)              # True
print(big.issuperset(small))     # True
print(small.isdisjoint({8, 9}))  # True
```

# --instructions--

Ordene os itens para construir `palette` a partir de `primary`, depois imprima, nesta ordem: se `primary` é subconjunto de `palette`, se `palette` é subconjunto de `primary`, e se `primary` e `{"black"}` são disjuntos

# --answers--

```python
primary = {"red", "blue", "yellow"}
```

```python
palette = primary | {"green"}
```

```python
print(primary <= palette)
```

```python
print(palette.issubset(primary))
```

```python
print(primary.isdisjoint({"black"}))
```

# --solutions--

```python
primary = {"red", "blue", "yellow"}
palette = primary | {"green"}
print(primary <= palette)
print(palette.issubset(primary))
print(primary.isdisjoint({"black"}))
```

# --output--

True
False
True
