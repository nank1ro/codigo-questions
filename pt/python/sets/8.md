---
language: python
exerciseType: 2
---

# --description--

A **interseção** de dois conjuntos contém apenas os elementos presentes em **ambos**.
Use o operador `&` ou o método `intersection()`:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a & b)               # {2, 3}
print(a.intersection(b))   # {2, 3}
```
Se os conjuntos não têm nada em comum, o resultado é um conjunto vazio.

# --instructions--

Complete o código para armazenar em `common` os elementos presentes em `a` e `b`, para que o programa imprima `[3, 4]`

# --seed--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a [/] b
print(sorted(common))
```

# --answers--

- &
- |
- -
- ^
- and

# --solutions--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a & b
print(sorted(common))
```

# --output--

[3, 4]
