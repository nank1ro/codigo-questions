---
language: python
exerciseType: 1
---

# --description--

Um conjunto só pode conter elementos **hasheáveis**, ou seja, valores que não podem mudar: números, strings, `True`/`False` e **tuplas**.
Tentar adicionar uma lista, um dicionário ou outro conjunto gera um `TypeError`:
```python
points = set()
points.add((1, 2))  # ok, a tuple
points.add([1, 2])  # TypeError: unhashable type: 'list'
```
Conjuntos de tuplas são úteis para acompanhar pares únicos, como coordenadas ou registros (name, age):
```python
visits = [(1, 2), (1, 2), (3, 4)]
print(len(set(visits)))  # 2
```

# --instructions--

Armazene em `points` o conjunto das coordenadas distintas em `visits`, adicione a ele o ponto `(0, 0)`, depois armazene em `count` quantos pontos distintos ele contém

# --seed--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `add()`

```json
{
  "regex": "\\.add\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`points` deve ser igual a `{(0, 0), (1, 2), (3, 4), (5, 6)}`

```python
    def test_1(self):
        self.assertEqual(points, {(0, 0), (1, 2), (3, 4), (5, 6)}, "--err-t1--")
```

`count` deve ser igual a `4`

```python
    def test_2(self):
        self.assertEqual(count, 4, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
points = set(visits)
points.add((0, 0))
count = len(points)
```
