---
language: python
exerciseType: 1
---

# --description--

A **união** de dois conjuntos é um novo conjunto com os elementos de **ambos**, sem duplicatas.
Use o operador `|` ou o método `union()`:
```python
a = {1, 2}
b = {2, 3}
print(a | b)        # {1, 2, 3}
print(a.union(b))   # {1, 2, 3}
```
Nem `a` nem `b` são modificados: operações de conjunto sempre retornam um novo conjunto.

# --instructions--

Armazene em `skills` a união de `frontend` e `backend` usando o operador `|`, depois armazene em `total` quantas habilidades distintas ela contém

# --seed--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar o operador `|`

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`skills` deve ser igual a `{'html', 'css', 'js', 'python', 'sql'}`

```python
    def test_1(self):
        self.assertEqual(skills, {"html", "css", "js", "python", "sql"}, "--err-t1--")
```

`total` deve ser igual a `5`

```python
    def test_2(self):
        self.assertEqual(total, 5, "--err-t2--")
```

`frontend` não deve ser modificado

```python
    def test_3(self):
        self.assertEqual(frontend, {"html", "css", "js"}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
skills = frontend | backend
total = len(skills)
```
