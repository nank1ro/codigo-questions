---
language: python
exerciseType: 1
---

# --description--

A função embutida `set()` constrói um conjunto a partir de qualquer coleção, por exemplo uma lista ou uma string.
Como um conjunto mantém cada valor apenas uma vez, esta é a forma clássica de **remover duplicatas**:
```python
nums = [1, 2, 2, 3]
unique = set(nums)
print(len(unique))  # 3
```
Para verificar se um valor está presente, use o operador `in`, que retorna `True` ou `False`:
```python
print(2 in unique)  # True
print(9 in unique)  # False
```
Verificações de pertencimento em um conjunto são muito rápidas, mesmo com milhares de elementos.

# --instructions--

Armazene na variável `unique` um conjunto construído a partir de `nums` usando `set()`, e em `has_three` se o valor `3` está em `unique`, usando `in`

# --seed--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `set()`

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar o operador `in`

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`unique` deve ser igual a `{1, 2, 3, 4}`

```python
    def test_1(self):
        self.assertEqual(unique, {1, 2, 3, 4}, "--err-t1--")
```

`has_three` deve ser `True`

```python
    def test_2(self):
        self.assertEqual(has_three, True, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
unique = set(nums)
has_three = 3 in unique
```
