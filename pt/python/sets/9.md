---
language: python
exerciseType: 1
---

# --description--

A **diferença** `a - b` contém os elementos de `a` que **não** estão em `b`.
A ordem importa: `a - b` e `b - a` costumam ser diferentes:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a - b)  # {1}
print(b - a)  # {4}
```
A **diferença simétrica** `a ^ b` contém os elementos que estão em **exatamente um** dos dois conjuntos:
```python
print(a ^ b)  # {1, 4}
```
As formas em método são `difference()` e `symmetric_difference()`.

# --instructions--

Armazene em `only_ann` os hobbies de `ann` que `bob` não tem, usando `-`, e em `not_shared` os hobbies que apenas um deles tem, usando `^`

# --seed--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar o operador `-`

```json
{
  "regex": "\\s-\\s",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar o operador `^`

```json
{
  "regex": "\\^",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`only_ann` deve ser igual a `{'tennis', 'yoga'}`

```python
    def test_1(self):
        self.assertEqual(only_ann, {"tennis", "yoga"}, "--err-t1--")
```

`not_shared` deve ser igual a `{'tennis', 'yoga', 'swimming'}`

```python
    def test_2(self):
        self.assertEqual(not_shared, {"tennis", "yoga", "swimming"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
only_ann = ann - bob
not_shared = ann ^ bob
```
