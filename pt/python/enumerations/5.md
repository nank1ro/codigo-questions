---
language: python
exerciseType: 1
---

# --description--

Você pode obter um membro a partir do seu **valor** chamando a classe como uma função, ou a partir do seu **nome** usando colchetes:
```python
print(Color(2))        # Color.GREEN
print(Color["BLUE"])   # Color.BLUE
```
Ambas são úteis quando o valor ou o nome vem de fora do programa, como um arquivo ou a entrada do usuário.
Se nada corresponder, `Color(9)` gera um `ValueError` e `Color["PINK"]` gera um `KeyError`.

# --instructions--

Armazene em `by_value` o membro de `Status` cujo valor é `code`, chamando `Status(...)`, e em `by_name` o membro cujo nome é `label`, usando `Status[...]`

# --seed--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve chamar `Status(...)` com `code`

```json
{
  "regex": "Status\\(\\s*code\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar `Status[...]` com `label`

```json
{
  "regex": "Status\\[\\s*label\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`by_value` deve ser `Status.DONE`

```python
    def test_1(self):
        self.assertIs(by_value, Status.DONE, "--err-t1--")
```

`by_name` deve ser `Status.FAILED`

```python
    def test_2(self):
        self.assertIs(by_name, Status.FAILED, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
by_value = Status(code)
by_name = Status[label]
```
