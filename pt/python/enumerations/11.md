---
language: python
exerciseType: 1
---

# --description--

Uma `Flag` é uma enum cujos membros podem ser **combinados**: um valor pode conter vários membros ao mesmo tempo, como um conjunto de opções.
Declare seus membros com `auto()`, que para uma `Flag` atribui potências de dois (`1`, `2`, `4`, ...), para que cada combinação tenha um valor distinto:
```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```
Use `|` para combinar membros, `in` para verificar se um membro faz parte de uma combinação e `value` para ver o número resultante:
```python
full = Permission.READ | Permission.WRITE | Permission.EXECUTE
print(Permission.WRITE in full)  # True
print(full.value)                # 7
print(full)                      # Permission.READ|WRITE|EXECUTE
```

# --instructions--

Armazene em `editor` a combinação de `Permission.READ` e `Permission.WRITE` usando `|`, em `can_write` se `Permission.WRITE` está em `editor`, e em `bits` o valor de `editor`

# --seed--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar o operador `|`

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar o operador `in`

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`editor` deve ser igual a `Permission.READ | Permission.WRITE`

```python
    def test_1(self):
        self.assertEqual(editor, Permission.READ | Permission.WRITE, "--err-t1--")
```

`can_write` deve ser `True`

```python
    def test_2(self):
        self.assertEqual(can_write, True, "--err-t2--")
```

`bits` deve ser igual a `3`

```python
    def test_3(self):
        self.assertEqual(bits, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()

editor = Permission.READ | Permission.WRITE
can_write = Permission.WRITE in editor
bits = editor.value
```
