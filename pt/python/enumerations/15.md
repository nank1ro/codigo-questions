---
language: python
exerciseType: 4
---

# --description--

Iterar sobre a classe e procurar membros pelo nome funcionam bem juntas quando você processa dados vindos de fora, como linhas de log ou um arquivo.
Uma dictionary comprehension sobre a classe prepara uma entrada por membro, depois `Level[name]` converte cada string recebida no membro correspondente:
```python
counts = {level: 0 for level in Level}
counts[Level["INFO"]] += 1
```
Como a enum mantém a ordem de declaração, iterar sobre `counts` depois dá os membros nessa mesma ordem.

# --instructions--

Ordene os itens para declarar `Level` com valores automáticos, preparar um contador para cada membro, contar os nomes na lista e depois imprimir o nome de cada membro com sua contagem

# --answers--

```python
from enum import Enum, auto
```

```python
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
```

```python
counts = {level: 0 for level in Level}
```

```python
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
```

```python
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --solutions--

```python
from enum import Enum, auto
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
counts = {level: 0 for level in Level}
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --output--

DEBUG: 0
INFO: 2
ERROR: 1
