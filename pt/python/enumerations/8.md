---
language: python
exerciseType: 3
---

# --description--

`StrEnum` (disponível desde o Python 3.11) é o equivalente em strings de `IntEnum`: seus membros também são strings, iguais ao seu valor.
Isso os torna convenientes em qualquer lugar onde strings simples sejam esperadas, como chaves de configuração ou parâmetros de API:
```python
from enum import StrEnum

class Mode(StrEnum):
    LIGHT = "light"
    DARK = "dark"

print(Mode.DARK == "dark")  # True
print(Mode.DARK.upper())    # DARK
```
Ao contrário de um `Enum` simples, converter um membro de `StrEnum` em texto com `str()` ou dentro de uma f-string dá o seu **valor**, não `Mode.DARK`.

# --instructions--

O que este código imprime?
```python
from enum import StrEnum

class Mode(StrEnum):
    LIGHT = "light"
    DARK = "dark"

print(f"theme: {Mode.DARK}")
```

# --answers--

- `theme: dark`, porque um membro de `StrEnum` é formatado como seu valor
- `theme: Mode.DARK`, porque uma f-string sempre mostra os nomes da classe e do membro
- `theme: DARK`, porque uma f-string mostra o nome do membro
- `theme: <Mode.DARK: 'dark'>`, porque uma f-string usa o `repr()` do membro

# --solutions--

- `theme: dark`, porque um membro de `StrEnum` é formatado como seu valor
