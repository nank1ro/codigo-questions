---
language: python
exerciseType: 1
---

# --description--

Uma enum é uma classe, então pode ter **métodos** e **propriedades** como qualquer outra classe.
Dentro deles, `self` é o membro no qual o método foi chamado, então você pode olhar `self.name`, `self.value` ou comparar `self` com outros membros:
```python
from enum import Enum

class Light(Enum):
    RED = 1
    GREEN = 2

    def can_go(self):
        return self is Light.GREEN

    @property
    def label(self):
        return self.name.lower()

print(Light.GREEN.can_go())  # True
print(Light.RED.label)       # red
```
Qualquer valor simples atribuído no corpo da classe se torna um membro, enquanto funções e propriedades nunca se tornam, não importa onde apareçam.

# --instructions--

Adicione a `Season` um método `is_warm()` que retorna `True` apenas para `SPRING` e `SUMMER`, e uma propriedade `label` que retorna o nome do membro em minúsculas

# --seed--

```python
from enum import Enum

class Season(Enum):
    WINTER = 1
    SPRING = 2
    SUMMER = 3
    AUTUMN = 4
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve definir o método `is_warm(self)`

```json
{
  "regex": "def is_warm\\(self\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar o decorador `@property`

```json
{
  "regex": "@property",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Season.SUMMER.is_warm()` deve ser `True`

```python
    def test_1(self):
        self.assertEqual(Season.SUMMER.is_warm(), True, "--err-t1--")
        self.assertEqual(Season.SPRING.is_warm(), True, "--err-t1--")
```

`Season.WINTER.is_warm()` deve ser `False`

```python
    def test_2(self):
        self.assertEqual(Season.WINTER.is_warm(), False, "--err-t2--")
        self.assertEqual(Season.AUTUMN.is_warm(), False, "--err-t2--")
```

`Season.AUTUMN.label` deve ser igual a `"autumn"`

```python
    def test_3(self):
        self.assertEqual(Season.AUTUMN.label, "autumn", "--err-t3--")
        self.assertEqual(Season.WINTER.label, "winter", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Season(Enum):
    WINTER = 1
    SPRING = 2
    SUMMER = 3
    AUTUMN = 4

    def is_warm(self):
        return self in (Season.SPRING, Season.SUMMER)

    @property
    def label(self):
        return self.name.lower()
```
