---
language: python
exerciseType: 4
---

# --description--

Uma classe enum é **iterável**: um loop `for` sobre a classe visita cada membro, na ordem em que foram declarados:
```python
for color in Color:
    print(color.name, color.value)
```
`len()` retorna quantos membros a enum tem, e `list(Color)` constrói uma lista com eles:
```python
print(len(Color))    # 3
print(list(Color))   # [<Color.RED: 1>, <Color.GREEN: 2>, <Color.BLUE: 3>]
```
Dentro de uma lista, os membros são mostrados com seu `repr()`, que inclui o valor entre colchetes angulares.

# --instructions--

Ordene os itens para declarar a enumeração `Weekday`, imprimir quantos membros ela tem e depois imprimir o nome e o valor de cada membro, um por linha

# --answers--

```python
from enum import Enum
```

```python
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
```

```python
print(len(Weekday))
```

```python
for day in Weekday:
```

```python
    print(day.name, day.value)
```

# --solutions--

```python
from enum import Enum
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
print(len(Weekday))
for day in Weekday:
    print(day.name, day.value)
```

# --output--

3
MON 1
TUE 2
WED 3
