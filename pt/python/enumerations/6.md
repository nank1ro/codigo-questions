---
language: python
exerciseType: 2
---

# --description--

Muitas vezes os valores exatos não importam: você só precisa que os membros sejam distintos.
Nesse caso você pode deixar o Python escolher os valores com `auto()`, também importado do módulo `enum`.
Ele atribui `1` ao primeiro membro e depois vai contando:
```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    SOUTH = auto()
    EAST = auto()

print(Direction.EAST.value)  # 3
```

# --instructions--

Complete o código para importar `auto` e imprimir o valor do membro `LARGE` de `Size`

# --seed--

```python
from enum import Enum, [/]

class Size(Enum):
    SMALL = auto()
    MEDIUM = auto()
    LARGE = auto()

print(Size.LARGE.[/])
```

# --answers--

- auto
- value
- name
- Enum
- unique
- index

# --solutions--

```python
from enum import Enum, auto

class Size(Enum):
    SMALL = auto()
    MEDIUM = auto()
    LARGE = auto()

print(Size.LARGE.value)
```

# --output--

3
