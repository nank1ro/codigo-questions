---
language: python
exerciseType: 4
---

# --description--

Enums combinam naturalmente com a instrução `match` (disponível desde o Python 3.10), que compara um valor com uma série de padrões `case` e executa o primeiro que corresponde:
```python
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
```
Sempre escreva o membro com sua classe, como `Light.RED`: um nome solto como `case RED:` não compararia nada, apenas capturaria o valor em uma nova variável `RED` e corresponderia a tudo.
O curinga `case _:` é o padrão e deve vir por último, porque qualquer padrão depois dele nunca poderia ser alcançado.

# --instructions--

Ordene os itens para declarar a enumeração `Light`, definir `action()` com o caso `RED` primeiro, depois o caso `GREEN`, depois o padrão, e imprimir a ação para `Light.YELLOW`

# --answers--

```python
from enum import Enum
```

```python
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
```

```python
def action(light):
```

```python
    match light:
```

```python
        case Light.RED:
            return "Stop"
```

```python
        case Light.GREEN:
            return "Go"
```

```python
        case _:
            return "Slow down"
```

```python
print(action(Light.YELLOW))
```

# --solutions--

```python
from enum import Enum
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
print(action(Light.YELLOW))
```

# --output--

Slow down
