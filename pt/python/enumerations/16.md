---
language: python
exerciseType: 1
---

# --description--

Além de métodos regulares, uma enum pode definir **métodos de classe** com `@classmethod`. Eles recebem a própria classe da enum como `cls`, então são o lugar certo para formas alternativas de encontrar um membro:
```python
class Color(Enum):
    RED = auto()
    BLUE = auto()

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]

print(Color.parse(" blue "))  # Color.BLUE
```
Junto com `auto()`, métodos, propriedades e consultas, isso permite construir enums que carregam seu próprio comportamento.

# --instructions--

Declare uma enumeração `Direction` com os membros `NORTH`, `EAST`, `SOUTH` e `WEST`, nessa ordem, usando `auto()`.
Adicione um método `opposite()` que retorna o membro do lado oposto (`NORTH` e `SOUTH`, `EAST` e `WEST`), e um método de classe `parse(text)` que retorna o membro cujo nome corresponde a `text`, ignorando maiúsculas e minúsculas e espaços ao redor

# --seed--

```python
from enum import Enum, auto
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `auto()`

```json
{
  "regex": "auto\\(\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar o decorador `@classmethod`

```json
{
  "regex": "@classmethod",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Direction` deve ter os membros `NORTH`, `EAST`, `SOUTH` e `WEST` com valores de `1` a `4`

```python
    def test_1(self):
        self.assertEqual([d.name for d in Direction], ["NORTH", "EAST", "SOUTH", "WEST"], "--err-t1--")
        self.assertEqual([d.value for d in Direction], [1, 2, 3, 4], "--err-t1--")
```

`Direction.NORTH.opposite()` deve ser `Direction.SOUTH` e `Direction.WEST.opposite()` deve ser `Direction.EAST`

```python
    def test_2(self):
        self.assertIs(Direction.NORTH.opposite(), Direction.SOUTH, "--err-t2--")
        self.assertIs(Direction.SOUTH.opposite(), Direction.NORTH, "--err-t2--")
        self.assertIs(Direction.EAST.opposite(), Direction.WEST, "--err-t2--")
        self.assertIs(Direction.WEST.opposite(), Direction.EAST, "--err-t2--")
```

`Direction.parse(" west ")` deve ser `Direction.WEST`

```python
    def test_3(self):
        self.assertIs(Direction.parse(" west "), Direction.WEST, "--err-t3--")
        self.assertIs(Direction.parse("North"), Direction.NORTH, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    EAST = auto()
    SOUTH = auto()
    WEST = auto()

    def opposite(self):
        opposites = {
            Direction.NORTH: Direction.SOUTH,
            Direction.SOUTH: Direction.NORTH,
            Direction.EAST: Direction.WEST,
            Direction.WEST: Direction.EAST,
        }
        return opposites[self]

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]
```
