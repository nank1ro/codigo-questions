---
language: python
exerciseType: 1
---

# --description--

O Python procura por `#` apenas no código, nunca dentro de uma **string**. Entre aspas, o `#` é um caractere comum:
```python
print("black is #000000")  # a hex colour
```
O primeiro `#` faz parte do texto, o segundo inicia um comentário de verdade. O mesmo vale para `"""` dentro de um comentário `#`: ali ele é apenas três caracteres de aspas, e não inicia nada.

# --instructions--

Escreva uma função `label(name)` que retorne `name` com um `#` na frente, de modo que `label("python")` retorne `"#python"`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`label("python")` deve retornar `"#python"`

```python
    def test_1(self):
        self.assertEqual(label("python"), "#python", "--err-t1--")
```

`label("ai")` deve retornar `"#ai"`

```python
    def test_2(self):
        self.assertEqual(label("ai"), "#ai", "--err-t2--")
```

`label("")` deve retornar `"#"`

```python
    def test_3(self):
        self.assertEqual(label(""), "#", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def label(name):
    """Returns name with a # in front of it."""
    return "#" + name
```
