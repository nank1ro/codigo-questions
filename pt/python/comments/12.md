---
language: python
exerciseType: 4
---

# --description--

A docstring precisa ser a **primeira linha do corpo**, acima de todas as outras instruções. Uma string escrita depois do `return`, ou em qualquer outro lugar do corpo, é apenas uma string: `__doc__` continua `None` e nenhuma ferramenta jamais a mostrará.

# --instructions--

Ordene os itens para construir um programa que define `to_seconds` com sua docstring como a primeira linha do corpo, e depois imprime `to_seconds(2)`

# --answers--

```python
def to_seconds(minutes):
```

```python
    """Returns the number of seconds in the given minutes."""
```

```python
    return minutes * 60
```

```python
print(to_seconds(2))
```

# --solutions--

```python
def to_seconds(minutes):
    """Returns the number of seconds in the given minutes."""
    return minutes * 60
print(to_seconds(2))
```

# --output--

120
