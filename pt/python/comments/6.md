---
language: python
exerciseType: 1
---

# --description--

Quando uma string é a **primeira instrução** dentro de uma função, o Python a trata como a documentação dessa função. Ela se chama **docstring**:
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"
```
Por convenção, uma docstring é escrita entre aspas duplas triplas, `"""`, mesmo quando cabe em uma linha, para que possa crescer depois sem trocar as aspas.

Escreva o resumo na terceira pessoa, como se descrevesse a função: "Returns...", "Adds...", "Checks...". A docstring deve vir antes de qualquer outra instrução no corpo, caso contrário é apenas uma string comum.

# --instructions--

Escreva uma função `double(n)` que retorne `n` multiplicado por dois, e dê a ela uma docstring de uma linha

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`double` deve ter uma docstring como sua primeira instrução

```python
    def test_1(self):
        self.assertIsNotNone(double.__doc__, "--err-t1--")
```

`double(21)` deve retornar `42`

```python
    def test_2(self):
        self.assertEqual(double(21), 42, "--err-t2--")
```

`double(-3)` deve retornar `-6` e `double(0)` deve retornar `0`

```python
    def test_3(self):
        self.assertEqual(double(-3), -6, "--err-t3--")
        self.assertEqual(double(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def double(n):
    """Returns n multiplied by two."""
    return n * 2
```
