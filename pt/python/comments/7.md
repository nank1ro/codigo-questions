---
language: python
exerciseType: 2
---

# --description--

Uma docstring não é descartada: o Python a guarda no atributo `__doc__` da função, para que o programa possa ler a própria documentação enquanto roda:
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"

print(greet.__doc__)  # Retorna a saudação para name.
```
Quando uma função não tem docstring, `__doc__` é `None`. É isso que `help(greet)` imprime, e o que um editor mostra quando você passa o mouse sobre o nome.

# --instructions--

Complete o código para que `greet` receba uma docstring e o programa a imprima

# --seed--

```python
def greet(name):
    [/]Returns a greeting for name.[/]
    return "Hi, " + name + "!"

print(greet.[/])
```

# --answers--

- """
- """
- __doc__
- __name__
- #

# --solutions--

```python
def greet(name):
    """Returns a greeting for name."""
    return "Hi, " + name + "!"

print(greet.__doc__)
```

# --output--

Returns a greeting for name.
