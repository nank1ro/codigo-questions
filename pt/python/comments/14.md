---
language: python
exerciseType: 1
---

# --description--

Um comentário colocado acima de uma função para dizer o que a função faz está no lugar errado. A docstring é o lugar para isso: ela está anexada à função, o `help()` a encontra e os editores a mostram, enquanto um comentário `#` acima do `def` é invisível para todos eles.

```python
# soma a e b
def add(a, b):
    return a + b
```
Mover a mesma frase uma linha para baixo, entre aspas triplas, a transforma em documentação de verdade:
```python
def add(a, b):
    """Returns the sum of a and b."""
    return a + b
```

# --instructions--

Remova os dois comentários `#` acima de `subtract` e documente a função com uma docstring em vez disso

# --seed--

```python
# subtract
# esta função subtrai b de a
def subtract(a, b):
    return a - b
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Os comentários `#` acima de `subtract` devem ser removidos

```json
{
  "regex": "#\\s*this function subtracts",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

O comentário `# subtract` também deve ser removido

```json
{
  "regex": "#\\s*subtract",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`subtract` deve ter uma docstring

```python
    def test_1(self):
        self.assertIsNotNone(subtract.__doc__, "--err-t1--")
```

`subtract(5, 3)` deve retornar `2`

```python
    def test_2(self):
        self.assertEqual(subtract(5, 3), 2, "--err-t2--")
```

`subtract(0, 4)` deve retornar `-4`

```python
    def test_3(self):
        self.assertEqual(subtract(0, 4), -4, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def subtract(a, b):
    """Returns b subtracted from a."""
    return a - b
```
