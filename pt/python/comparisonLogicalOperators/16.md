---
language: python
exerciseType: 1
---

# --description--

`any(iterable)` retorna `True` se pelo menos um elemento for verdadeiro e `all(iterable)` se todos forem (`all([])` é `True`). Combinados com uma expressão geradora, expressam uma condição sobre uma coleção inteira, e fazem curto-circuito como `or` e `and`:
```python
print(any(ch.isdigit() for ch in "abc1"))  # True
print(all(ch.isalpha() for ch in "ab1"))   # False
```

# --instructions--

Escreva uma função `is_valid_password(password)` que retorne `True` apenas quando a string tiver entre `8` e `20` caracteres inclusive, contiver pelo menos um dígito e não contiver espaços, e `False` caso contrário

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`"secret123"` é válida, e senhas de exatamente `8` ou `20` caracteres também são

```python
    def test_1(self):
        self.assertIs(is_valid_password("secret123"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefg1"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefghij123456789x"), True, "--err-t1--")
```

Senhas curtas demais ou longas demais não são válidas

```python
    def test_2(self):
        self.assertIs(is_valid_password("short1"), False, "--err-t2--")
        self.assertIs(is_valid_password("abcdefghij1234567890x"), False, "--err-t2--")
```

Uma senha sem dígitos ou com um espaço não é válida

```python
    def test_3(self):
        self.assertIs(is_valid_password("nodigitshere"), False, "--err-t3--")
        self.assertIs(is_valid_password("pass word1"), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_valid_password(password):
    return (
        8 <= len(password) <= 20
        and any(ch.isdigit() for ch in password)
        and " " not in password
    )
```
