---
language: python
exerciseType: 2
---

# --description--

Todo valor tem um **valor de verdade**. `bool(value)` retorna `False` para `0`, `0.0`, `None`, a string vazia `""` e contêineres vazios como `[]`, `{}` e `set()`; qualquer outro valor é verdadeiro, incluindo `"0"` e `[0]`:
```python
print(bool(0), bool(""), bool(None))   # False False False
print(bool(42), bool("0"), bool([0]))  # True True True
```
`if`, `while`, `and`, `or` e `not` usam essa regra, então `if items:` verifica que a lista não está vazia e `not name` verifica que a string está vazia; não é preciso escrever `len(items) > 0` nem `name == ""`.

# --instructions--

Complete o código para que a primeira linha exiba o valor de verdade de `cart`, `name` e `count`, e o `if` exiba `empty cart` quando a lista estiver vazia, produzindo a saída `False True False`, `empty cart`

# --seed--

```python
cart = []
name = "Ada"
count = 0
print([/](cart), bool(name), bool(count))
if [/] cart:
    print("empty cart")
```

# --answers--

- bool
- not
- len
- str
- is

# --solutions--

```python
cart = []
name = "Ada"
count = 0
print(bool(cart), bool(name), bool(count))
if not cart:
    print("empty cart")
```

# --output--

False True False
empty cart
