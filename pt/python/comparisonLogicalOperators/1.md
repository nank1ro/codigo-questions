---
language: python
exerciseType: 2
---

# --description--

Os operadores de comparação comparam dois valores e retornam um **booleano**, `True` ou `False`: `==` igual, `!=` diferente, `<` menor que, `>` maior que, `<=` menor ou igual, `>=` maior ou igual:
```python
age = 18
print(age == 18)  # True
print(age != 18)  # False
print(age < 18)   # False
print(age >= 18)  # True
```
O resultado pode ser armazenado numa variável ou exibido diretamente. Um único `=` é uma atribuição, não uma comparação.

# --instructions--

Complete o código para que as três linhas verifiquem se `score` é igual a `75`, menor que `100` e maior que `50`, produzindo a saída `True`, `True`, `True`

# --seed--

```python
score = 75
print(score [/] 75)
print(score [/] 100)
print(score [/] 50)
```

# --answers--

- ==
- <
- >
- =
- !==

# --solutions--

```python
score = 75
print(score == 75)
print(score < 100)
print(score > 50)
```

# --output--

True
True
True
