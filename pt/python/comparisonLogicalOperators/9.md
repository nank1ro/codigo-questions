---
language: python
exerciseType: 4
---

# --description--

Os operadores lógicos são de **curto-circuito**: `and` para assim que um operando é falso e `or` assim que um é verdadeiro, porque o resultado já é conhecido. Os operandos restantes nunca são avaliados, então se forem chamadas de função elas não são executadas:
```python
def check(n):
    print("checking", n)
    return n > 0

check(-1) and check(5)  # prints only "checking -1"
check(2) or check(-3)   # prints only "checking 2"
```

# --instructions--

Ordene os itens para definir `is_positive`, calcular `first` com `and` e depois `second` com `or`, e exibir os dois resultados, produzindo a saída `checking -1`, `checking 2`, `False True`

# --answers--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
```

```python
first = is_positive(-1) and is_positive(5)
```

```python
second = is_positive(2) or is_positive(-3)
```

```python
print(first, second)
```

# --solutions--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
first = is_positive(-1) and is_positive(5)
second = is_positive(2) or is_positive(-3)
print(first, second)
```

# --output--

checking -1
checking 2
False True
