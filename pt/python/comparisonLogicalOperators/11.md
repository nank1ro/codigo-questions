---
language: python
exerciseType: 1
---

# --description--

A avaliação em curto-circuito é uma forma segura de **proteger** uma operação que falharia com alguns valores. Em `word is not None and len(word) < 4`, `len(word)` só é executado quando `word` não é `None`, então a chamada nunca lança um erro. A proteção deve vir primeiro:
```python
def is_short(word):
    return word is not None and len(word) < 4

print(is_short(None))   # False
print(is_short("cat"))  # True
```
Tenha em mente que `and` retorna um operando: `word and len(word) < 4` dá `None` para `None` e `""` para a string vazia, não `False`. Proteja com uma comparação de verdade quando for necessário um booleano.

# --instructions--

Escreva uma função `first_is_zero(numbers)` que retorne `True` quando a lista `numbers` não estiver vazia e seu primeiro elemento for `0`, e `False` caso contrário. O resultado deve ser um booleano mesmo para uma lista vazia

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_is_zero([0, 5])` deve retornar `True`

```python
    def test_1(self):
        self.assertIs(first_is_zero([0, 5]), True, "--err-t1--")
        self.assertIs(first_is_zero([0]), True, "--err-t1--")
```

`first_is_zero([3, 0])` deve retornar `False`

```python
    def test_2(self):
        self.assertIs(first_is_zero([3, 0]), False, "--err-t2--")
```

`first_is_zero([])` deve retornar `False` sem lançar um erro

```python
    def test_3(self):
        self.assertIs(first_is_zero([]), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def first_is_zero(numbers):
    return len(numbers) > 0 and numbers[0] == 0
```
