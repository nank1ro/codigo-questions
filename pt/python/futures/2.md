---
language: python
exerciseType: 1
---

# --description--

Uma corrotina pode receber parâmetros e `return` um valor exatamente como uma função normal. O valor não está disponível quando o objeto de corrotina é criado, mas apenas quando a corrotina já foi executada. `asyncio.run` retorna aquilo que a corrotina retornou:
```python
import asyncio

async def double(n):
    return n * 2

result = asyncio.run(double(21))
print(result)  # 42
```
Tudo o que você sabe sobre funções continua valendo dentro de `async def`: variáveis locais, condições, laços e múltiplas instruções `return`.

# --instructions--

Escreva uma função corrotina `add(a, b)` com `async def` que retorne a soma de `a` e `b`

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`add` deve ser uma função corrotina

```python
    def test_1(self):
        self.assertTrue(inspect.iscoroutinefunction(add), "--err-t1--")
```

Executar `add(1, 3)` deve retornar `4`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(add(1, 3)), 4, "--err-t2--")
```

Executar `add(200, 210)` deve retornar `410`

```python
    def test_3(self):
        self.assertEqual(asyncio.run(add(200, 210)), 410, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
async def add(a, b):
    return a + b
```
