---
language: python
exerciseType: 2
---

# --description--

Uma tarefa continua rodando haja ou não alguém esperando por ela. Crie-a cedo, faça outro trabalho e faça `await` dela apenas no ponto em que o seu resultado for necessário: a espera é mais curta porque parte da tarefa já rodou em segundo plano. Uma tarefa também pode ser inspecionada com `task.done()`, que retorna `True` assim que ela termina.

# --instructions--

Complete o código para que `compute()` seja agendada como uma tarefa, `"waiting"` seja exibido enquanto ela roda, e a tarefa seja então aguardada para obter o seu valor, produzindo a saída `waiting`, `42`

# --seed--

```python
import asyncio

async def compute():
    await asyncio.sleep(0.01)
    return 42

async def main():
    task = asyncio.[/](compute())
    print("waiting")
    value = [/] task
    print(value)

asyncio.run(main())
```

# --answers--

- create_task
- await
- gather
- run
- sleep
- result

# --solutions--

```python
import asyncio

async def compute():
    await asyncio.sleep(0.01)
    return 42

async def main():
    task = asyncio.create_task(compute())
    print("waiting")
    value = await task
    print(value)

asyncio.run(main())
```

# --output--

waiting
42
