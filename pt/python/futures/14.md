---
language: python
exerciseType: 2
---

# --description--

`asyncio.wait_for(awaitable, timeout)` aguarda algo, mas desiste depois de `timeout` segundos: a operação é cancelada e um `TimeoutError` é lançado, que pode ser capturado como qualquer exceção:
```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())  # timed out
```
Com um timeout de `0.1`, o mesmo código exibiria `done`. `asyncio.TimeoutError` é outro nome para o `TimeoutError` embutido.

# --instructions--

Complete o código para que `slow()` seja aguardada com um timeout de `0.01` segundos e o timeout seja capturado, produzindo a saída `timed out`

# --seed--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.[/](slow(), [/]=0.01)
        print(result)
    except [/]:
        print("timed out")

asyncio.run(main())
```

# --answers--

- wait_for
- timeout
- TimeoutError
- gather
- delay
- ValueError

# --solutions--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())
```

# --output--

timed out
