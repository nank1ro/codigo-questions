---
language: python
exerciseType: 4
---

# --description--

Tratar uma corrotina que falha segue o padrão síncrono: a corrotina lança uma exceção, quem chama envolve o `await` num `try`/`except` e decide o que fazer com o objeto de erro, por exemplo exibir a sua mensagem com `print(e)`.

# --instructions--

Ordene os itens para construir um programa que comece com o import, defina `load` antes de `main`, exiba o resultado de carregar `"config"`, depois capture o erro lançado ao carregar um nome vazio e exiba a sua mensagem, produzindo a saída `loaded config`, `empty name`

# --answers--

```python
import asyncio
```

```python
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
```

```python
async def main():
    print(await load("config"))
```

```python
    try:
        await load("")
```

```python
    except ValueError as e:
        print(e)
```

```python
asyncio.run(main())
```

# --solutions--

```python
import asyncio
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
async def main():
    print(await load("config"))
    try:
        await load("")
    except ValueError as e:
        print(e)
asyncio.run(main())
```

# --output--

loaded config
empty name
