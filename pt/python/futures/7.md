---
language: python
exerciseType: 1
---

# --description--

A concorrência compensa quando o número de operações não é fixo. Construa os objetos de corrotina numa list comprehension, desempacote-os no `gather` e aguarde o lote inteiro de uma vez:
```python
import asyncio

async def fetch(url):
    await asyncio.sleep(0.02)
    return len(url)

async def fetch_all(urls):
    return await asyncio.gather(*[fetch(u) for u in urls])

print(asyncio.run(fetch_all(["a.com", "bb.com"])))  # [5, 6]
```
Dez urls ainda levam cerca de 20 milissegundos no total em vez de 200, e os resultados permanecem na ordem das urls.

# --instructions--

Escreva uma função corrotina `fetch(item_id)` que espere `0.02` segundos e retorne `item_id * 10`, e uma função corrotina `fetch_all(ids)` que rode `fetch` para cada id **concorrentemente** com `asyncio.gather` e retorne a lista de resultados

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch(3)` deve retornar `30`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch(3)), 30, "--err-t1--")
```

`fetch_all([1, 2, 3])` deve retornar `[10, 20, 30]` e `fetch_all([])` deve retornar `[]`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(fetch_all([1, 2, 3])), [10, 20, 30], "--err-t2--")
        self.assertEqual(asyncio.run(fetch_all([])), [], "--err-t2--")
```

`fetch_all` com cinco ids deve executá-los concorrentemente, levando bem menos de `0.1` segundos

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(fetch_all([1, 2, 3, 4, 5]))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [10, 20, 30, 40, 50], "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch(item_id):
    await asyncio.sleep(0.02)
    return item_id * 10

async def fetch_all(ids):
    return await asyncio.gather(*[fetch(i) for i in ids])
```
