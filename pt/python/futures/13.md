---
language: python
exerciseType: 3
---

# --description--

Quando uma das corrotinas passadas ao `gather` lança uma exceção, ela se propaga para a linha `await asyncio.gather(...)` e os resultados das outras são perdidos, embora continuem rodando. Passar `return_exceptions=True` muda isso: o `gather` nunca lança, e o objeto de exceção toma o lugar do resultado ausente na lista:
```python
import asyncio

async def ok():
    return 1

async def fail():
    raise ValueError("bad")

async def main():
    results = await asyncio.gather(ok(), fail(), return_exceptions=True)
    print(results)  # [1, ValueError('bad')]

asyncio.run(main())
```
Cada elemento pode então ser verificado com `isinstance(result, Exception)` para separar as falhas dos valores.

# --instructions--

O que este código exibe?
```python
import asyncio

async def ok():
    return 1

async def fail():
    raise ValueError("bad")

async def main():
    results = await asyncio.gather(ok(), fail(), return_exceptions=True)
    print(len(results), isinstance(results[1], ValueError))

asyncio.run(main())
```

# --answers--

- `2 True`, porque com `return_exceptions=True` a exceção é retornada na lista em vez de ser lançada
- Um `ValueError`, porque uma exceção em qualquer corrotina sempre se propaga para fora do `gather`
- `1 False`, porque a corrotina que falhou é descartada dos resultados
- `2 False`, porque o `gather` substitui os resultados que falharam por `None`

# --solutions--

- `2 True`, porque com `return_exceptions=True` a exceção é retornada na lista em vez de ser lançada
