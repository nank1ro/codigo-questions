---
language: python
exerciseType: 1
---

# --description--

Corrotinas chamam umas às outras com `await`. Uma corrotina pode aguardar qualquer outra corrotina, receber o seu valor de retorno e continuar, exatamente como uma cadeia de chamadas de função:
```python
import asyncio

async def fetch_price(item):
    await asyncio.sleep(0.01)  # simulates a slow lookup
    return 10

async def total(item, quantity):
    price = await fetch_price(item)
    return price * quantity

print(asyncio.run(total("pen", 3)))  # 30
```
Apenas a corrotina mais externa passa por `asyncio.run`; cada uma interna é alcançada com `await`. Se `total` fosse um `def` comum, ela não poderia usar `await` de forma alguma: a palavra-chave `async` se espalha para cada função da cadeia que precisa esperar.

# --instructions--

Escreva uma função corrotina `fetch_user(user_id)` que espere `0.01` segundos e retorne a string `"user"` seguida do id, e uma função corrotina `greet_user(user_id)` que aguarde `fetch_user` e retorne `"Hello, "` seguido do nome buscado

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch_user(1)` deve retornar `"user1"` e `fetch_user(42)` deve retornar `"user42"`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch_user(1)), "user1", "--err-t1--")
        self.assertEqual(asyncio.run(fetch_user(42)), "user42", "--err-t1--")
```

`greet_user(1)` deve retornar `"Hello, user1"`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(greet_user(1)), "Hello, user1", "--err-t2--")
```

Tanto `fetch_user` quanto `greet_user` devem ser funções corrotinas

```python
    def test_3(self):
        self.assertTrue(inspect.iscoroutinefunction(fetch_user), "--err-t3--")
        self.assertTrue(inspect.iscoroutinefunction(greet_user), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch_user(user_id):
    await asyncio.sleep(0.01)
    return f"user{user_id}"

async def greet_user(user_id):
    name = await fetch_user(user_id)
    return f"Hello, {name}"
```
