---
language: python
exerciseType: 1
---

# --description--

O bloco `try` para na **primeira** instrução que levanta uma exceção; as linhas seguintes a ela são puladas e o controle passa para o bloco `except`. Nada no bloco `try` é desfeito, então mantenha-o o mais curto possível:
```python
def half(n):
    try:
        return 10 / n
    except ZeroDivisionError:
        return "undefined"

print(half(2))  # 5.0
print(half(0))  # undefined
```
Um `return` dentro de `except` funciona como qualquer outro `return`, o que torna `try`/`except` uma forma natural de devolver um valor alternativo em vez de quebrar.

# --instructions--

Escreva uma função `safe_divide(a, b)` que retorna `a / b`, ou a string `"undefined"` quando `b` for `0`, capturando `ZeroDivisionError`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_divide(9, 3)` deve retornar `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_divide(9, 3), 3.0, "--err-t1--")
```

`safe_divide(1, 0)` deve retornar `"undefined"`

```python
    def test_2(self):
        self.assertEqual(safe_divide(1, 0), "undefined", "--err-t2--")
```

`safe_divide(-5, 2)` deve retornar `-2.5`

```python
    def test_3(self):
        self.assertEqual(safe_divide(-5, 2), -2.5, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "undefined"
```
