---
language: python
exerciseType: 2
---

# --description--

Um bloco `try` pode ser seguido por **várias** cláusulas `except`, cada uma tratando uma falha diferente com uma recuperação diferente. O Python compara a exceção levantada contra elas de cima para baixo e executa a **primeira** que corresponder; as outras são puladas:
```python
try:
    value = 100 / int(text)
except ValueError:
    print("not a number")
except ZeroDivisionError:
    print("cannot divide by zero")
```
Como a primeira correspondência vence, a ordem importa quando os tipos são relacionados: uma cláusula para um tipo geral colocada acima de uma cláusula para um tipo mais específico sempre venceria, deixando a cláusula específica inalcançável.

# --instructions--

Complete o código para que texto ilegível e um divisor zero sejam relatados separadamente, produzindo a saída `2.0 not a number cannot divide by zero`

# --seed--

```python
def describe(text):
    try:
        return 10 / int(text)
    except [/]:
        return "not a number"
    except [/]:
        return "cannot divide by zero"

print(describe("5"), describe("abc"), describe("0"))
```

# --answers--

- ValueError
- ZeroDivisionError
- TypeError
- KeyError

# --solutions--

```python
def describe(text):
    try:
        return 10 / int(text)
    except ValueError:
        return "not a number"
    except ZeroDivisionError:
        return "cannot divide by zero"

print(describe("5"), describe("abc"), describe("0"))
```

# --output--

2.0 not a number cannot divide by zero
