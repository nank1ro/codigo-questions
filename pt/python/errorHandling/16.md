---
language: python
exerciseType: 1
---

# --description--

Quando nenhum tipo embutido serve, defina o seu próprio criando uma subclasse de `Exception`. Um corpo vazio geralmente basta — o nome é a mensagem para o leitor:
```python
class ConfigError(Exception):
    pass
```
Ela se comporta como qualquer outra exceção: `raise ConfigError("bad port")`, e `except ConfigError:` a captura.

Traduzir uma falha de baixo nível para o seu próprio tipo é comum, e o erro original não deve se perder no processo. `raise NewError(...) from original` os **encadeia**: ele armazena `original` no atributo `__cause__` da nova exceção, e o traceback mostra ambos sob *The above exception was the direct cause of the following exception*:
```python
try:
    port = int(text)
except ValueError as e:
    raise ConfigError("bad port") from e
```
Sem `from e` os dois ainda ficam vinculados implicitamente, mas o `from` diz em voz alta que o primeiro erro causou o segundo.

# --instructions--

Defina uma classe de exceção `ConfigError` que seja subclasse de `Exception`, e escreva uma função `read_port(text)` que retorna `int(text)` e, quando isso levanta `ValueError`, levanta `ConfigError("bad port")` encadeada dela com `raise ... from`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`read_port("8080")` deve retornar `8080`, e `ConfigError` deve ser subclasse de `Exception`

```python
    def test_1(self):
        self.assertEqual(read_port("8080"), 8080, "--err-t1--")
        self.assertTrue(issubclass(ConfigError, Exception), "--err-t1--")
```

`read_port("abc")` deve levantar um `ConfigError` com a mensagem `"bad port"`

```python
    def test_2(self):
        with self.assertRaises(ConfigError, msg="--err-t2--") as ctx:
            read_port("abc")
        self.assertEqual(str(ctx.exception), "bad port", "--err-t2--")
```

o `ConfigError` levantado deve ser encadeado do `ValueError` original

```python
    def test_3(self):
        with self.assertRaises(ConfigError, msg="--err-t3--") as ctx:
            read_port("abc")
        self.assertIsInstance(ctx.exception.__cause__, ValueError, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
class ConfigError(Exception):
    pass

def read_port(text):
    try:
        return int(text)
    except ValueError as e:
        raise ConfigError("bad port") from e
```
