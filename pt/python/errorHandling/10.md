---
language: python
exerciseType: 4
---

# --description--

Um bloco `try` pode ser seguido por um bloco `else`, que roda **apenas quando o bloco `try` terminou sem levantar exceção**:
```python
try:
    number = int(text)
except ValueError:
    print("not a number")
else:
    print(number * 2)
```
Colocar `print(number * 2)` dentro do bloco `try` também funcionaria, mas então um `ValueError` levantado pela própria impressão seria confundido com uma falha de conversão. O `else` mantém o bloco `try` reduzido à única instrução protegida, e guarda tudo o que deve acontecer em caso de sucesso.

# --instructions--

Ordene os itens para construir um programa que define `parse` e então o chama com `"21"` e com `"abc"`, nessa ordem, imprimindo o número dobrado em caso de sucesso e uma mensagem em caso de falha

# --answers--

```python
def parse(text):
```

```python
    try:
        number = int(text)
```

```python
    except ValueError:
        print(f"{text} is not a number")
```

```python
    else:
        print(number * 2)
```

```python
parse("21")
```

```python
parse("abc")
```

# --solutions--

```python
def parse(text):
    try:
        number = int(text)
    except ValueError:
        print(f"{text} is not a number")
    else:
        print(number * 2)
parse("21")
parse("abc")
```

# --output--

42
abc is not a number
