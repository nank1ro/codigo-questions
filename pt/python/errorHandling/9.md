---
language: python
exerciseType: 1
---

# --description--

Uma exceção é um objeto, e `as` o vincula a um nome para que o manipulador possa examiná-lo:
```python
try:
    int("abc")
except ValueError as e:
    print(e)                   # invalid literal for int() with base 10: 'abc'
    print(type(e).__name__)    # ValueError
```
`str(e)` — que é o que `print(e)` e um slot de f-string usam — retorna a mensagem com que a exceção foi construída, e `type(e).__name__` retorna o nome da classe como texto. O nome vinculado por `as` só existe dentro do bloco `except`; o Python o apaga quando o bloco termina.

# --instructions--

Escreva uma função `error_label(text)` que retorna `int(text)`, e quando isso levanta `ValueError` retorna uma string formada pelo nome da classe da exceção, `": "`, e a mensagem da exceção, por exemplo `"ValueError: ..."`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`error_label("8")` deve retornar `8`

```python
    def test_1(self):
        self.assertEqual(error_label("8"), 8, "--err-t1--")
```

`error_label("abc")` deve começar com o nome da classe

```python
    def test_2(self):
        self.assertTrue(error_label("abc").startswith("ValueError: "), "--err-t2--")
```

`error_label("abc")` também deve conter a mensagem, que menciona `abc`

```python
    def test_3(self):
        self.assertIn("abc", error_label("abc")[12:], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def error_label(text):
    try:
        return int(text)
    except ValueError as e:
        return f"{type(e).__name__}: {e}"
```
