---
language: python
exerciseType: 1
---

# --description--

`None` é um valor que existe por conta própria e significa "nada aqui". É o que uma função retorna quando não tem uma instrução `return`, e é um placeholder comum para um valor que ainda não é conhecido.
Como existe apenas um `None`, verifique-o com `is`, não com `==`:
```python
result = None
if result is None:
    print("no result yet")
```
Todo tipo tem um atributo `__name__` com o seu nome como uma string, o que é útil para mensagens:
```python
print(type(3).__name__)     # int
print(type(None).__name__)  # NoneType
```

# --instructions--

Escreva uma função `describe` que recebe um valor e retorna a string `"nothing"` quando o valor é `None`, e o nome do seu tipo caso contrário

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve comparar com `is None`

```json
{
  "regex": "is\\s+None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar `__name__`

```json
{
  "regex": "__name__",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe(None)` deve retornar `"nothing"`

```python
    def test_1(self):
        self.assertEqual(describe(None), "nothing", "--err-t1--")
```

`describe(5)` deve retornar `"int"` e `describe(2.0)` deve retornar `"float"`

```python
    def test_2(self):
        self.assertEqual(describe(5), "int", "--err-t2--")
        self.assertEqual(describe(2.0), "float", "--err-t2--")
```

`describe("a")` deve retornar `"str"` e `describe(True)` deve retornar `"bool"`

```python
    def test_3(self):
        self.assertEqual(describe("a"), "str", "--err-t3--")
        self.assertEqual(describe(True), "bool", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe(value):
    if value is None:
        return "nothing"
    return type(value).__name__
```
