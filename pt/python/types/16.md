---
language: python
exerciseType: 1
---

# --description--

Texto vindo de fora é sempre um `str`, e cabe ao seu programa descobrir qual tipo ele realmente contém.
Uma abordagem comum é tentar a conversão **mais estrita** primeiro, e recorrer à seguinte quando ela gerar um `ValueError`:
```python
try:
    value = int(text)
except ValueError:
    value = float(text)
```
Aninhar um segundo `try` dentro do bloco `except` permite recorrer mais uma vez, por exemplo para manter o texto como está.

# --instructions--

Escreva uma função `parse_value` que recebe uma string e a retorna como um `int` se for um número inteiro, como um `float` se for um número decimal, e inalterada caso contrário

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve capturar `ValueError` com `except`

```json
{
  "regex": "except\\s+ValueError",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_value("42")` deve retornar o `int` `42`

```python
    def test_1(self):
        self.assertEqual(parse_value("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_value("42"), int, "--err-t1--")
        self.assertEqual(parse_value("-8"), -8, "--err-t1--")
```

`parse_value("3.5")` deve retornar o `float` `3.5`

```python
    def test_2(self):
        self.assertEqual(parse_value("3.5"), 3.5, "--err-t2--")
        self.assertIsInstance(parse_value("3.5"), float, "--err-t2--")
```

`parse_value("abc")` deve retornar `"abc"`

```python
    def test_3(self):
        self.assertEqual(parse_value("abc"), "abc", "--err-t3--")
        self.assertEqual(parse_value(""), "", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_value(text):
    try:
        return int(text)
    except ValueError:
        try:
            return float(text)
        except ValueError:
            return text
```
