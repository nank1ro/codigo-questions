---
language: python
exerciseType: 1
---

# --description--

Você já conhece os operadores aritméticos. O que importa aqui é o **tipo do resultado**.
Combinar um `int` com um `float` resulta em um `float`, mesmo quando a parte decimal é zero:
```python
print(3 + 0.5)   # 3.5
print(2 * 1.0)   # 2.0
```
A **divisão verdadeira** `/` sempre retorna um `float`, mesmo quando os números se dividem exatamente:
```python
print(10 / 5)    # 2.0
print(7 / 2)     # 3.5
```
A **divisão inteira** `//` arredonda o resultado para baixo até o número inteiro mais próximo (então `-7 // 2` é `-4`) e retorna um `int` quando ambos os operandos são inteiros. Junto com o resto `%`, ela divide uma quantidade em partes inteiras:
```python
print(7 // 2)    # 3
print(7 % 2)     # 1
```

# --instructions--

Armazene em `each` o resultado de dividir `total` por `people` com `/`, em `whole` a mesma divisão com `//` e em `rest` o resto

# --seed--

```python
total = 17
people = 5
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `//`

```json
{
  "regex": "//",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`each` deve ser o float `3.4`

```python
    def test_1(self):
        self.assertIsInstance(each, float, "--err-t1--")
        self.assertEqual(each, 3.4, "--err-t1--")
```

`whole` deve ser o inteiro `3`

```python
    def test_2(self):
        self.assertIsInstance(whole, int, "--err-t2--")
        self.assertEqual(whole, 3, "--err-t2--")
```

`rest` deve ser `2`

```python
    def test_3(self):
        self.assertEqual(rest, 2, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
total = 17
people = 5

each = total / people
whole = total // people
rest = total % people
```
