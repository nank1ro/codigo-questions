---
language: python
exerciseType: 2
---

# --description--

`bool` é uma **subclasse** de `int`: `True` se comporta como `1` e `False` como `0` onde quer que um número seja esperado:
```python
print(True + 1)     # 2
print(True == 1)    # True
print(False * 10)   # 0
```
`sum()` soma os itens de uma lista, então somar uma lista de booleanos **conta** quantos são `True`:
```python
answers = [True, False, True]
print(sum(answers))  # 2
```
Por causa da relação de subclasse, `isinstance(True, int)` retorna `True`, enquanto `type(True)` ainda é `bool`.

# --instructions--

Complete o código para que ele conte as respostas corretas, some `1` a `True` e verifique que `True` é uma instância de `int`, imprimindo `3`, `2` e `True`

# --seed--

```python
answers = [True, False, True, True]
correct = [/](answers)
print(correct)
print(True [/] 1)
print(isinstance(True, [/]))
```

# --answers--

- sum
- +
- int
- len
- ==
- str

# --solutions--

```python
answers = [True, False, True, True]
correct = sum(answers)
print(correct)
print(True + 1)
print(isinstance(True, int))
```

# --output--

3
2
True
