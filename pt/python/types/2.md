---
language: python
exerciseType: 1
---

# --description--

`type()` retorna a classe de um valor, então você pode compará-la com um nome de classe usando `is`:
```python
age = 30
print(type(age) is int)  # True
```
Na maioria das vezes, porém, você só quer saber **se** um valor é de um certo tipo. Essa é a função de `isinstance(value, cls)`, que retorna `True` ou `False`:
```python
print(isinstance(age, int))    # True
print(isinstance(age, str))    # False
```
O segundo argumento também pode ser uma **tupla** de classes: o resultado é `True` se o valor pertencer a qualquer uma delas:
```python
print(isinstance(2.5, (int, float)))  # True
```

# --instructions--

Armazene em `age_type` o tipo de `age`, usando `type()`, em `is_text` se `name` é um `str` e em `is_number` se `price` é um `int` ou um `float`, usando `isinstance()`

# --seed--

```python
age = 30
price = 9.99
name = "Ada"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `type()`

```json
{
  "regex": "type\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar `isinstance()`

```json
{
  "regex": "isinstance\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`age_type` deve ser `int`

```python
    def test_1(self):
        self.assertIs(age_type, int, "--err-t1--")
```

`is_text` deve ser `True`

```python
    def test_2(self):
        self.assertIs(is_text, True, "--err-t2--")
```

`is_number` deve ser `True`

```python
    def test_3(self):
        self.assertIs(is_number, True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
age = 30
price = 9.99
name = "Ada"

age_type = type(age)
is_text = isinstance(name, str)
is_number = isinstance(price, (int, float))
```
