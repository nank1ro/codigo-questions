---
language: python
exerciseType: 2
---

# --description--

A tomada de decisao e necessaria quando queremos executar um codigo apenas se uma determinada condicao for satisfeita.
Vamos supor que queremos brincar la fora apenas se o tempo estiver bom.
Em programacao, podemos salvar uma variavel booleana `nice_weather` e executar a acao de brincar la fora `if` (se) essa variavel for `True`, assim:
```python
nice_weather = True
if (nice_weather):
    # play outside
```

# --instructions--

Crie uma instrucao `if` verificando se `time` e menor que `12`, entao no bloco de codigo imprima a string `"It's morning"`

# --seed--

```python
time = 8
[/] (time < 12):
    print("It's morning")
```

# --answers--

- if
- ==
- =
- else
- ?

# --solutions--

```python
time = 8
if (time < 12):
    print("It's morning")
```

# --output--

It's morning
