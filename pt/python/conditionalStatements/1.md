---
language: python
exerciseType: 2
---

# --description--

A tomada de decisão é necessária quando queremos executar um código apenas se uma determinada condição for satisfeita.
Vamos supor que queremos brincar lá fora apenas se o tempo estiver bom.
Em programação, podemos salvar uma variável booleana `nice_weather` e executar a ação de brincar lá fora `if` (se) essa variável for `True`, assim:
```python
nice_weather = True
if (nice_weather):
    # brincar lá fora
```

# --instructions--

Crie uma instrução `if` verificando se `time` é menor que `12`, então no bloco de código imprima a string `"It's morning"`

# --seed--

```python
time = 8
[/] (time < 12):
    print("It's morning")
```

# --answers--

- if
- ==
- =
- else
- ?

# --solutions--

```python
time = 8
if (time < 12):
    print("It's morning")
```

# --output--

It's morning
