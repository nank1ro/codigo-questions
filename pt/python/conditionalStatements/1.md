---
language: python
exerciseType: 2
---

# --description--

A tomada de decisao e necessaria quando queremos executar codigo somente se uma determinada condicao for satisfeita.
Vamos supor que queremos brincar fora somente se o clima estiver bom.
Na programacao, podemos salvar uma variavel booleana `nice_weather` e realizar a acao de brincar fora `if` esta variavel for `True`, como:
```python
nice_weather = True
if (nice_weather):
    # play outside
```

# --instructions--

Crie uma instrucao `if` verificando se o `time` e menor que `12`, entao no bloco de codigo imprima a string `"It's morning"`

# --seed--

```python
time = 8
[/] (time < 12):
    print("It's morning")
```

# --answers--

- if
- ==
- =
- else
- ?

# --solutions--

```python
time = 8
if (time < 12):
    print("It's morning")
```

# --output--

It's morning
