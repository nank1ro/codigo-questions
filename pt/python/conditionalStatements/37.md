---
language: python
exerciseType: 2
---

# --description--

O operador `not` inverte um valor booleano: `not True` é `False`, e `not False` é `True`.
```python
is_online = False
print(not is_online)  # True
```

# --instructions--

Preencha os espacos em branco para executar o bloco de codigo

# --seed--

```python
is_morning = False
is_afternoon = not is_morning
if ([/]):
    print("Good afternoon!")
```

# --answers--

- is_morning
- is_afternoon
- "is_morning"
- "is_afternoon"

# --solutions--

```python
is_morning = False
is_afternoon = not is_morning
if (is_afternoon):
    print("Good afternoon!")
```

# --output--

Good afternoon!
