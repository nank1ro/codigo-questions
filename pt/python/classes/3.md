---
language: python
exerciseType: 2
---

# --description--

Vamos substituir `pass` por outra coisa.
O método `__init__()` é necessário para classes, e é usado para __inicializar__ os objetos que ele cria.
`__init__()` sempre leva pelo menos um argumento, `self`, que se refere ao objeto sendo criado.
Você pode pensar em `__init__()` como o método que inicia cada objeto que a classe cria.

# --instructions--

Defina um método `__init__()` para sua classe `Animal`.
Passe para ele o argumento `self` por enquanto.
Finalmente, coloque o `pass` no bloco de código da definição `__init__()`

# --seed--

```python
class Animal:
    def [/]([/]):
        [/]
```

# --answers--

- __init__
- self
- object
- pass

# --solutions--

```python
class Animal:
    def __init__(self):
        pass
```
