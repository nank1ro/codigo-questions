---
language: python
exerciseType: 2
---

# --description--

Pode haver momentos em que queremos converter nossa tupla em uma lista.
Para fazer isso, podemos usar a funcao `list()`

# --instructions--

Use a funcao `list()` para converter a tupla em uma lista

# --seed--

```python
tuple1 = ("dog", "bear", "cat")
list1 = [/]
list1.append("frog")
print(list1)
```

# --answers--

- list[tuple1]
- list(tuple1)

# --solutions--

```python
tuple1 = ("dog", "bear", "cat")
list1 = list(tuple1)
list1.append("frog")
print(list1)
```

# --output--

['dog', 'bear', 'cat', 'frog']
