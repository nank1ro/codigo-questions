---
language: python
exerciseType: 1
---

# --description--

`re.sub(pattern, replacement, text)` retorna uma nova string na qual cada correspondência foi substituída. Strings são imutáveis, então o texto original permanece intacto:
```python
import re

print(re.sub(r"\d", "#", "call 555 now"))     # call ### now
print(re.sub(r"\s+", " ", "too    many gaps"))  # too many gaps
```

A substituição pode se referir aos grupos de captura com `\1`, `\2`, ... (ou `\g<name>` para um grupo nomeado), o que torna reordenar texto uma tarefa de uma linha. A substituição também é uma raw string, pela mesma razão da barra invertida:
```python
import re

print(re.sub(r"(\w+), (\w+)", r"\2 \1", "Lovelace, Ada"))  # Ada Lovelace
```
Um argumento `count` limita quantas correspondências são substituídas: `re.sub(r"\d", "#", "1 2 3", count=1)` resulta em `# 2 3`.

# --instructions--

Escreva uma função `mask_digits(text)` que substitui cada dígito por `#`, e uma função `swap_names(text)` que transforma um par `surname, name` em `name surname` usando uma referência inversa

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`mask_digits` deve substituir cada dígito

```python
    def test_1(self):
        self.assertEqual(mask_digits("call 555 now"), "call ### now", "--err-t1--")
        self.assertEqual(mask_digits("no digits"), "no digits", "--err-t1--")
```

`swap_names` deve colocar o nome antes do sobrenome

```python
    def test_2(self):
        self.assertEqual(swap_names("Lovelace, Ada"), "Ada Lovelace", "--err-t2--")
        self.assertEqual(swap_names("Turing, Alan"), "Alan Turing", "--err-t2--")
```

`swap_names` deve deixar um texto sem vírgula inalterado

```python
    def test_3(self):
        self.assertEqual(swap_names("Ada Lovelace"), "Ada Lovelace", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def mask_digits(text):
    return re.sub(r"\d", "#", text)

def swap_names(text):
    return re.sub(r"(\w+), (\w+)", r"\2 \1", text)
```
