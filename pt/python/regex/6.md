---
language: python
exerciseType: 1
---

# --description--

Contar parênteses para encontrar o grupo `3` cansa rápido. Um grupo pode receber um nome com `(?P<name>...)` e depois ser lido com `match.group("name")`:
```python
import re

match = re.search(r"(?P<hour>\d\d):(?P<minute>\d\d)", "starts at 09:30")
print(match.group("hour"))    # 09
print(match.group("minute"))  # 30
print(match.groupdict())      # {'hour': '09', 'minute': '30'}
```
`match.groupdict()` retorna todos os grupos nomeados como um dicionário. Grupos nomeados mantêm seu número também, então `match.group(1)` ainda funciona.

O exemplo também usa um **quantificador** com chaves: `\d{2}` significa exatamente dois dígitos, `\d{2,4}` significa entre dois e quatro, e `\d{2,}` significa dois ou mais. Eles são a versão precisa de `+` (um ou mais), `*` (zero ou mais) e `?` (zero ou um).

# --instructions--

Escreva uma função `parse_date(text)` que encontra uma data escrita como quatro dígitos, um hífen, dois dígitos, um hífen e dois dígitos, e retorna um dicionário com as chaves `year`, `month` e `day`, ou `None` quando não há tal data

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`parse_date` deve ler as três partes da data

```python
    def test_1(self):
        self.assertEqual(parse_date("due 2026-09-13 ok"), {"year": "2026", "month": "09", "day": "13"}, "--err-t1--")
```

`parse_date` deve funcionar quando a data é o texto inteiro

```python
    def test_2(self):
        self.assertEqual(parse_date("1999-12-31"), {"year": "1999", "month": "12", "day": "31"}, "--err-t2--")
```

`parse_date` deve retornar `None` quando não há data

```python
    def test_3(self):
        self.assertIsNone(parse_date("no date here"), "--err-t3--")
        self.assertIsNone(parse_date("26-09-13"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def parse_date(text):
    match = re.search(r"(?P<year>\d{4})-(?P<month>\d{2})-(?P<day>\d{2})", text)
    if match is None:
        return None
    return match.groupdict()
```
