---
language: python
exerciseType: 1
---

# --description--

Quando a posição ou os grupos de cada correspondência são necessários, `re.finditer(pattern, text)` é a chamada certa: ela percorre o texto e produz um **objeto de correspondência** para cada correspondência, uma de cada vez:
```python
import re

for match in re.finditer(r"\d+", "a1 bb22"):
    print(match.group(), match.start())
# 1 1
# 22 5
```
`finditer` produz um iterador, não uma lista, então ele pode ser usado em um loop `for` ou em uma comprehension. Enquanto `findall` dá apenas o texto, `finditer` dá tudo o que um objeto de correspondência sabe.

# --instructions--

Escreva uma função `word_positions(text)` que retorna uma lista de tuplas, uma para cada sequência de letras no texto, contendo a palavra e o índice onde ela começa

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`word_positions("hi there")` deve retornar `[("hi", 0), ("there", 3)]`

```python
    def test_1(self):
        self.assertEqual(word_positions("hi there"), [("hi", 0), ("there", 3)], "--err-t1--")
```

Dígitos e símbolos não devem fazer parte de uma palavra

```python
    def test_2(self):
        self.assertEqual(word_positions("a1b"), [("a", 0), ("b", 2)], "--err-t2--")
        self.assertEqual(word_positions("Ada, Bob"), [("Ada", 0), ("Bob", 5)], "--err-t2--")
```

Um texto sem letras deve retornar uma lista vazia

```python
    def test_3(self):
        self.assertEqual(word_positions("123 456"), [], "--err-t3--")
        self.assertEqual(word_positions(""), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def word_positions(text):
    return [(match.group(), match.start()) for match in re.finditer(r"[a-zA-Z]+", text)]
```
