---
language: python
exerciseType: 1
---

# --description--

`re.search` varre o texto inteiro, mas `re.match` só tenta o padrão no **começo**:
```python
import re

print(re.search(r"\d+", "order 42"))  # <re.Match object; span=(6, 8), match='42'>
print(re.match(r"\d+", "order 42"))   # None
print(re.match(r"\d+", "42 orders"))  # <re.Match object; span=(0, 2), match='42'>
```

Ambas retornam `None` quando nada corresponde, e um objeto de correspondência é sempre truthy, então a maneira usual de perguntar "houve correspondência?" é um simples `if`:
```python
if re.search(r"\d", text):
    print("there is a digit")
```
Quando um `True` ou `False` real é necessário, compare com `is not None` ou envolva a chamada em `bool(...)`.

# --instructions--

Escreva uma função `has_digit(text)` que retorna `True` quando o texto contém um dígito em qualquer lugar, e uma função `starts_with_digit(text)` que retorna `True` apenas quando o texto começa com um dígito

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`has_digit` deve encontrar um dígito em qualquer lugar do texto

```python
    def test_1(self):
        self.assertEqual(has_digit("order 42"), True, "--err-t1--")
        self.assertEqual(has_digit("7up"), True, "--err-t1--")
```

`has_digit` deve retornar `False` quando não há dígito

```python
    def test_2(self):
        self.assertEqual(has_digit("no numbers here"), False, "--err-t2--")
        self.assertEqual(has_digit(""), False, "--err-t2--")
```

`starts_with_digit` deve olhar apenas o começo do texto

```python
    def test_3(self):
        self.assertEqual(starts_with_digit("42 orders"), True, "--err-t3--")
        self.assertEqual(starts_with_digit("order 42"), False, "--err-t3--")
        self.assertEqual(starts_with_digit(""), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def has_digit(text):
    return re.search(r"\d", text) is not None

def starts_with_digit(text):
    return re.match(r"\d", text) is not None
```
