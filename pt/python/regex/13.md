---
language: python
exerciseType: 1
---

# --description--

`str.split` só consegue cortar em um separador fixo. `re.split(pattern, text)` corta em qualquer coisa que o padrão descreva, que é o que uma entrada bagunçada geralmente precisa:
```python
import re

print("a, b;c".split(","))            # ['a', ' b;c']
print(re.split(r"[,;]", "a, b;c"))    # ['a', ' b', 'c']
print(re.split(r"[,;\s]+", "a, b;c")) # ['a', 'b', 'c']
```
Escrever o separador como `[,;\s]+` faz com que toda uma sequência de vírgulas, ponto e vírgula e espaços conte como um único corte, em vez de deixar strings vazias entre eles.

Um argumento `maxsplit` para depois de um determinado número de cortes, deixando o resto do texto no último elemento: `re.split(r"\s+", "a b c", maxsplit=1)` resulta em `['a', 'b c']`.

# --instructions--

Escreva uma função `split_fields(line)` que retorna a lista de campos na linha, tratando qualquer sequência de vírgulas, ponto e vírgula e espaços como um único separador

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`split_fields` deve cortar em vírgulas, ponto e vírgula e espaços

```python
    def test_1(self):
        self.assertEqual(split_fields("a, b;c"), ["a", "b", "c"], "--err-t1--")
        self.assertEqual(split_fields("red green blue"), ["red", "green", "blue"], "--err-t1--")
```

Uma sequência de separadores deve produzir um único corte

```python
    def test_2(self):
        self.assertEqual(split_fields("x ; ; y"), ["x", "y"], "--err-t2--")
        self.assertEqual(split_fields("1,,2"), ["1", "2"], "--err-t2--")
```

Uma linha sem separadores deve retornar um único campo

```python
    def test_3(self):
        self.assertEqual(split_fields("solo"), ["solo"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def split_fields(line):
    return re.split(r"[,;\s]+", line)
```
