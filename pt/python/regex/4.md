---
language: python
exerciseType: 1
---

# --description--

Um objeto de correspondência carrega mais do que o texto correspondido. Além de `.group()`, ele oferece a posição da correspondência dentro da string original:
```python
import re

match = re.search(r"\d+", "order 42 shipped")
print(match.group())  # 42
print(match.start())  # 6
print(match.end())    # 8
print(match.span())   # (6, 8)
```
`.start()` é o índice do primeiro caractere correspondido, `.end()` é o índice logo após o último, e `.span()` retorna ambos como uma tupla. Isso significa que `text[match.start():match.end()]` é sempre igual a `match.group()`.

Como `re.search` pode retornar `None`, ler `.group()` direto levanta um `AttributeError` quando nada correspondeu; verifique o resultado primeiro.

# --instructions--

Escreva uma função `first_number(text)` que retorna uma tupla com a primeira sequência de dígitos, seu índice inicial e seu índice final, ou `None` quando o texto não contém dígito

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_number("order 42 shipped")` deve retornar `("42", 6, 8)`

```python
    def test_1(self):
        self.assertEqual(first_number("order 42 shipped"), ("42", 6, 8), "--err-t1--")
```

A correspondência deve começar no índice `0` quando o texto começa com dígitos

```python
    def test_2(self):
        self.assertEqual(first_number("7up"), ("7", 0, 1), "--err-t2--")
        self.assertEqual(first_number("100 items"), ("100", 0, 3), "--err-t2--")
```

`first_number` deve retornar `None` quando não há dígito

```python
    def test_3(self):
        self.assertIsNone(first_number("no digits here"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def first_number(text):
    match = re.search(r"\d+", text)
    if match is None:
        return None
    return (match.group(), match.start(), match.end())
```
