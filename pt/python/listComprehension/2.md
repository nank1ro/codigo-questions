---
language: python
exerciseType: 1
---

# --description--

A expressão à esquerda pode ser qualquer coisa que produza um valor: um cálculo, uma chamada de função, uma chamada de método.
A variável do loop pode ter qualquer nome que você quiser, e ela existe apenas dentro dos colchetes:
```python
prices = [10, 20]
with_tax = [price * 1.2 for price in prices]
print(with_tax)  # [12.0, 24.0]
```

# --instructions--

Armazene na variável `squares` a lista dos quadrados de `nums`, usando uma list comprehension

# --seed--

```python
nums = [1, 2, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar uma list comprehension

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`squares` deve ser igual a `[1, 4, 9, 16]`

```python
    def test_1(self):
        self.assertEqual(squares, [1, 4, 9, 16], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 3, 4]
squares = [n * n for n in nums]
```
