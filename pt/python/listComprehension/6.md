---
language: python
exerciseType: 1
---

# --description--

Comprehensions são ótimas para **transformar strings**.
Chame um método de string em cada item, ou construa uma nova string com uma f-string:
```python
names = ["ana", "bob"]
greetings = [f"Hi {name.capitalize()}" for name in names]
print(greetings)  # ['Hi Ana', 'Hi Bob']
```

# --instructions--

Armazene na variável `shout` uma lista com cada nome de `names` em maiúsculas seguido de `!`, usando uma list comprehension

# --seed--

```python
names = ["ana", "bob", "cy"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar uma list comprehension

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`shout` deve ser igual a `['ANA!', 'BOB!', 'CY!']`

```python
    def test_1(self):
        self.assertEqual(shout, ["ANA!", "BOB!", "CY!"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
names = ["ana", "bob", "cy"]
shout = [name.upper() + "!" for name in names]
```
