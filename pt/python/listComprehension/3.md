---
language: python
exerciseType: 2
---

# --description--

Uma comprehension também pode **filtrar** os itens.
Adicione uma condição `if` depois da parte `for`: apenas os itens para os quais a condição é `True` acabam na nova lista:
```python
nums = [5, 12, 8, 20]
big = [n for n in nums if n > 10]
print(big)  # [12, 20]
```
Isso é o mesmo que um loop com um `if` dentro, e substitui o `filter()` com uma lambda de forma mais legível.

# --instructions--

Complete a comprehension para manter apenas os números pares, para que o programa imprima `[2, 4]`

# --seed--

```python
nums = [1, 2, 3, 4]
evens = [n for n in nums [/] n % 2 [/] 0]
print(evens)
```

# --answers--

- if
- ==
- !=
- when
- while

# --solutions--

```python
nums = [1, 2, 3, 4]
evens = [n for n in nums if n % 2 == 0]
print(evens)
```

# --output--

[2, 4]
