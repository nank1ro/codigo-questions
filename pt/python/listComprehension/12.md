---
language: python
exerciseType: 4
---

# --description--

O `for` interno pode usar a variável do externo.
Esta é a forma clássica de **achatar** uma lista de listas em uma única lista:
```python
matrix = [[1, 2], [3, 4]]
flat = [n for row in matrix for n in row]
print(flat)  # [1, 2, 3, 4]
```
A função `sum()` então soma todos os números de uma lista.

# --instructions--

Ordene os itens para achatar `matrix` e imprimir a lista achatada junto com a soma de seus números

# --answers--

```python
matrix = [[1, 2], [3, 4]]
```

```python
flat = [n for row in matrix for n in row]
```

```python
total = sum(flat)
```

```python
print(flat, total)
```

# --solutions--

```python
matrix = [[1, 2], [3, 4]]
flat = [n for row in matrix for n in row]
total = sum(flat)
print(flat, total)
```

# --output--

[1, 2, 3, 4] 10
