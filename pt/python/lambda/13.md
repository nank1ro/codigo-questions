---
language: python
exerciseType: 4
---

# --description--

`max()` e `min()` também aceitam uma função `key`, assim como `sorted()`:
```python
words = ["hi", "hello", "hey"]
print(max(words, key=lambda w: len(w)))  # hello
```

# --instructions--

Ordene os itens para imprimir os nomes em ordem alfabética ignorando maiúsculas e minúsculas, e depois o nome mais longo

# --answers--

```python
names = ["bob", "Alice", "Charlie"]
```

```python
ordered = sorted(names, key=lambda s: s.lower())
```

```python
longest = max(ordered, key=lambda s: len(s))
```

```python
print(ordered)
print(longest)
```

# --solutions--

```python
names = ["bob", "Alice", "Charlie"]
ordered = sorted(names, key=lambda s: s.lower())
longest = max(ordered, key=lambda s: len(s))
print(ordered)
print(longest)
```

# --output--

['Alice', 'bob', 'Charlie']
Charlie
