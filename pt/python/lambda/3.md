---
language: python
exerciseType: 2
---

# --description--

Uma lambda pode receber **mais de um parâmetro**.
Separe-os com vírgulas, exatamente como em uma função `def`:
```python
power = lambda base, exp: base ** exp
print(power(2, 3))  # 8
```

# --instructions--

Complete a lambda para que ela receba os parâmetros `a` e `b` e retorne a soma deles

# --seed--

```python
add = lambda a[/] b[/] a + b
print(add(3, 4))
```

# --answers--

- ,
- :
- ;
- ->

# --solutions--

```python
add = lambda a, b: a + b
print(add(3, 4))
```

# --output--

7
