---
language: python
exerciseType: 1
---

# --description--

**Quando você deve preferir `def`?**
Uma lambda é ótima para uma função curta e descartável passada como argumento.
Se a lógica precisar de um nome, de várias linhas, de uma docstring, ou for reutilizada em muitos lugares, uma função `def` é mais clara.
Um último truque: `sorted()` também aceita `reverse=True` para obter os maiores valores primeiro:
```python
print(sorted([3, 1, 2], reverse=True))  # [3, 2, 1]
```

# --instructions--

Armazene em `ranking` a lista `students` ordenada da maior para a menor `grade`, depois armazene em `names` a lista dos nomes em `ranking`, usando `sorted()` e `map()` com lambdas

# --seed--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar uma expressão `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar `map()`

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ranking` deve começar com o estudante `Cy`

```python
    def test_1(self):
        self.assertEqual(ranking[0]["name"], "Cy", "--err-t1--")
```

`names` deve ser igual a `['Cy', 'Ana', 'Bo']`

```python
    def test_2(self):
        self.assertEqual(names, ["Cy", "Ana", "Bo"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
ranking = sorted(students, key=lambda s: s["grade"], reverse=True)
names = list(map(lambda s: s["name"], ranking))
```
