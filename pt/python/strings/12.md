---
language: python
exerciseType: 4
---

# --description--

`replace(old, new)` retorna uma cópia da string onde **toda** ocorrência de `old` é substituída por `new`:
```python
text = "a-b-c"
print(text.replace("-", "+"))  # a+b+c
```
Como as strings são imutáveis, lembre-se de armazenar o resultado se quiser mantê-lo.

# --instructions--

Ordene os itens para substituir todo `cats` por `dogs` em `msg` e imprimir o resultado

# --answers--

```python
msg = "I like cats, cats are great"
```

```python
new_msg = msg.replace("cats", "dogs")
```

```python
print(new_msg)
```

# --solutions--

```python
msg = "I like cats, cats are great"
new_msg = msg.replace("cats", "dogs")
print(new_msg)
```

# --output--

I like dogs, dogs are great
