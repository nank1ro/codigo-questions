---
language: python
exerciseType: 4
---

# --description--

Como todo método de string retorna uma nova string, você pode **encadear** métodos um após o outro.
Cada chamada trabalha sobre o resultado da anterior:
```python
text = "  Hello World  "
print(text.strip().lower())  # hello world
```
Um fatiamento também aceita um terceiro valor, o **passo**.
O passo `-1` percorre a string de trás para frente, que é o truque clássico para invertê-la:
```python
print("abc"[::-1])  # cba
```

# --instructions--

Ordene os itens para limpar `word`, invertê-la e imprimir se a palavra limpa é um palíndromo (lê-se igual de trás para frente)

# --answers--

```python
word = "  Level  "
```

```python
clean = word.strip().lower()
```

```python
reversed_word = clean[::-1]
```

```python
print(clean == reversed_word)
```

# --solutions--

```python
word = "  Level  "
clean = word.strip().lower()
reversed_word = clean[::-1]
print(clean == reversed_word)
```

# --output--

True
