---
language: python
exerciseType: 1
---

# --description--

Um **slug** é uma versão de um título amigável para URLs: minúsculas, sem espaços ao redor, e palavras separadas por traços, como `hello-world`.
Construir um é apenas um encadeamento dos métodos que você aprendeu: `strip()`, `lower()` e `replace()`.

# --instructions--

Escreva uma função `slugify(title)` que retorna `title` sem espaços ao redor, em minúsculas e com cada espaço substituído por `-`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `replace()`

```json
{
  "regex": "\\.replace\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`slugify("Hello World")` deve retornar `"hello-world"`

```python
    def test_1(self):
        self.assertEqual(slugify("Hello World"), "hello-world", "--err-t1--")
```

`slugify("  Learn Python Strings  ")` deve retornar `"learn-python-strings"`

```python
    def test_2(self):
        self.assertEqual(slugify("  Learn Python Strings  "), "learn-python-strings", "--err-t2--")
```

`slugify("codigo")` deve retornar `"codigo"`

```python
    def test_3(self):
        self.assertEqual(slugify("codigo"), "codigo", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def slugify(title):
    return title.strip().lower().replace(" ", "-")
```
