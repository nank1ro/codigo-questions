---
language: python
exerciseType: 1
---

# --description--

`find(sub)` retorna o **índice** da primeira ocorrência de `sub`, ou `-1` se não for encontrado:
```python
text = "hello world"
print(text.find("o"))  # 4
print(text.find("z"))  # -1
```
`count(sub)` retorna **quantas vezes** `sub` aparece:
```python
print(text.count("o"))   # 2
print(text.count("ll"))  # 1
```

# --instructions--

Armazene em `position` o índice do primeiro `o` em `text`, em `missing` o resultado de procurar `z`, e em `total` quantos `o` o texto contém

# --seed--

```python
text = "hello world"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `find()`

```json
{
  "regex": "\\.find\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar `count()`

```json
{
  "regex": "\\.count\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`position` deve ser igual a `4`

```python
    def test_1(self):
        self.assertEqual(position, 4, "--err-t1--")
```

`missing` deve ser igual a `-1`

```python
    def test_2(self):
        self.assertEqual(missing, -1, "--err-t2--")
```

`total` deve ser igual a `2`

```python
    def test_3(self):
        self.assertEqual(total, 2, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
text = "hello world"
position = text.find("o")
missing = text.find("z")
total = text.count("o")
```
