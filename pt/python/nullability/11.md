---
language: python
exerciseType: 2
---

# --description--

Uma lista pode conter `None` junto a valores reais, por exemplo leituras que falharam ou respostas que foram puladas. A maioria das operações não o aceita: `sum([8, None])` gera um `TypeError`.
Filtre os valores `None` com uma list comprehension cuja condição é `is not None`:
```python
readings = [3, None, 5]
valid = [r for r in readings if r is not None]
print(valid)  # [3, 5]
```
Usar `if r` em vez disso também descartaria cada `0`, então seja explícito quando zero é uma leitura válida.

# --instructions--

Complete o código para que `valid` contenha apenas as pontuações que não são `None`, depois imprima o total, produzindo a saída `[8, 0, 9]`, `3`, `17`

# --seed--

```python
scores = [8, None, 0, None, 9]
valid = [s for s in scores if s [/] None]
print(valid)
print(len(valid))
print([/](valid))
```

# --answers--

- is not
- sum
- is
- max
- len
- and

# --solutions--

```python
scores = [8, None, 0, None, 9]
valid = [s for s in scores if s is not None]
print(valid)
print(len(valid))
print(sum(valid))
```

# --output--

[8, 0, 9]
3
17
