---
language: python
exerciseType: 1
---

# --description--

Um parâmetro pode ter um **valor padrão**, usado quando quem chama omite o argumento. `None` é o padrão usual para "não fornecido":
```python
def greet(name=None):
    if name is None:
        name = "stranger"
    print("Hello, " + name)

greet()       # Hello, stranger
greet("Ada")  # Hello, Ada
```
Isso importa para listas e dicionários. Um valor padrão é avaliado **uma vez**, quando a função é definida, então `def add(item, items=[])` compartilha a mesma lista em toda chamada que omite `items`, e os itens se acumulam. A correção é usar `None` como padrão e criar uma lista nova dentro da função:
```python
def add(item, items=None):
    if items is None:
        items = []
    items.append(item)
    return items
```

# --instructions--

Escreva uma função `add_tag` que recebe uma string `tag` e uma lista opcional `tags`; ela deve adicionar `tag` a `tags` e retornar a lista, criando uma nova lista quando `tags` não é fornecida

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

O valor padrão de `tags` deve ser `None`

```json
{
  "regex": "tags\\s*=\\s*None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`add_tag("new")` deve retornar `["new"]`

```python
    def test_1(self):
        self.assertEqual(add_tag("new"), ["new"], "--err-t1--")
```

Duas chamadas sem `tags` devem retornar listas independentes

```python
    def test_2(self):
        first = add_tag("a")
        second = add_tag("b")
        self.assertEqual(first, ["a"], "--err-t2--")
        self.assertEqual(second, ["b"], "--err-t2--")
```

`add_tag("c", ["a", "b"])` deve retornar `["a", "b", "c"]`

```python
    def test_3(self):
        existing = ["a", "b"]
        self.assertEqual(add_tag("c", existing), ["a", "b", "c"], "--err-t3--")
        self.assertIs(add_tag("d", existing), existing, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def add_tag(tag, tags=None):
    if tags is None:
        tags = []
    tags.append(tag)
    return tags
```
