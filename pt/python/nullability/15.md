---
language: python
exerciseType: 1
---

# --instructions--

Escreva uma função `oldest` que recebe uma lista de dicionários com uma chave `"name"` e uma chave opcional `"age"`, que pode estar ausente ou ser `None`; ela deve retornar o nome da pessoa com a maior idade, ignorando pessoas sem idade, e `None` quando ninguém tem idade

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `get`

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve verificar com `is None` ou `is not None`

```json
{
  "regex": "is\\s+(not\\s+)?None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

O mais velho entre Ada (36), Linus (28) e Grace (45) deve ser `"Grace"`

```python
    def test_1(self):
        people = [{"name": "Ada", "age": 36}, {"name": "Linus", "age": 28}, {"name": "Grace", "age": 45}]
        self.assertEqual(oldest(people), "Grace", "--err-t1--")
```

Pessoas com idade ausente ou `None` devem ser ignoradas

```python
    def test_2(self):
        people = [{"name": "Tim"}, {"name": "Ada", "age": 36}, {"name": "Bob", "age": None}]
        self.assertEqual(oldest(people), "Ada", "--err-t2--")
```

O resultado deve ser `None` quando ninguém tem idade, e uma idade de `0` deve contar

```python
    def test_3(self):
        self.assertIsNone(oldest([{"name": "Tim"}, {"name": "Bob", "age": None}]), "--err-t3--")
        self.assertIsNone(oldest([]), "--err-t3--")
        self.assertEqual(oldest([{"name": "Baby", "age": 0}, {"name": "Tim"}]), "Baby", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def oldest(people):
    best_name = None
    best_age = None
    for person in people:
        age = person.get("age")
        if age is None:
            continue
        if best_age is None or age > best_age:
            best_age = age
            best_name = person["name"]
    return best_name
```
