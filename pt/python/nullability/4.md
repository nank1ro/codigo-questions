---
language: python
exerciseType: 2
---

# --description--

`None` conta como **falso** em uma condição, então `if not value:` é `True` quando `value` é `None`. É tentador usá-lo como uma verificação de `None`, mas o mesmo teste também é `True` para `0`, `""`, `[]` e todos os outros valores vazios:
```python
count = 0
if not count:
    print("missing?")   # printed, but 0 is a real value!
```
Quando "nenhum valor" e "valor vazio" devem ser tratados de forma diferente, verifique `is None` primeiro, depois a truthiness:
```python
if count is None:
    print("no count yet")
elif not count:
    print("count is zero")
```
Use `if not value:` apenas quando você realmente quiser tratar `None` e os valores vazios da mesma forma.

# --instructions--

Complete a função para que ela retorne `missing` para `None`, `empty` para valores vazios e `present` caso contrário, produzindo a saída `missing`, `empty`, `present`

# --seed--

```python
def describe(value):
    if value [/] None:
        return "missing"
    if [/] value:
        return "empty"
    return "present"

print(describe(None))
print(describe(0))
print(describe("hi"))
```

# --answers--

- is
- not
- is not
- in
- and

# --solutions--

```python
def describe(value):
    if value is None:
        return "missing"
    if not value:
        return "empty"
    return "present"

print(describe(None))
print(describe(0))
print(describe("hi"))
```

# --output--

missing
empty
present
