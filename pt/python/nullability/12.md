---
language: python
exerciseType: 1
---

# --description--

Uma busca seguida de uma verificação de `None` geralmente precisa de duas linhas: uma para armazenar o resultado, outra para testá-lo. O operador de **expressão de atribuição** `:=`, apelidado de *walrus*, atribui um valor **dentro** de uma expressão, então ambos os passos cabem no `if`:
```python
ages = {"Ada": 36}
if (age := ages.get("Ada")) is not None:
    print(age + 1)  # 37
```
Os parênteses são obrigatórios: sem eles, o `:=` tentaria atribuir a comparação inteira. Depois do `if`, `age` permanece disponível como qualquer outra variável.

# --instructions--

Escreva uma função `describe_user` que recebe um dicionário `users` que mapeia nomes para idades e um `name`; ela deve retornar `"<name> is <age>"` quando o nome está presente, e `"unknown user"` caso contrário, usando o operador walrus para armazenar o resultado de `get`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar o operador `:=`

```json
{
  "regex": ":=",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar `get`

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe_user({"Ada": 36}, "Ada")` deve retornar `"Ada is 36"`

```python
    def test_1(self):
        self.assertEqual(describe_user({"Ada": 36}, "Ada"), "Ada is 36", "--err-t1--")
```

`describe_user({"Ada": 36}, "Grace")` deve retornar `"unknown user"`

```python
    def test_2(self):
        self.assertEqual(describe_user({"Ada": 36}, "Grace"), "unknown user", "--err-t2--")
```

Uma idade de `0` ainda deve contar como presente

```python
    def test_3(self):
        self.assertEqual(describe_user({"Tim": 0}, "Tim"), "Tim is 0", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe_user(users, name):
    if (age := users.get(name)) is not None:
        return f"{name} is {age}"
    return "unknown user"
```
