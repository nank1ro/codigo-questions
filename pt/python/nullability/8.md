---
language: python
exerciseType: 4
---

# --description--

Funções que podem receber `None` geralmente começam com uma **guarda**: um `if` que retorna cedo quando não há nada com que trabalhar. O restante da função pode então presumir que o valor está presente, sem aninhar tudo dentro de um `else`:
```python
def shout(text):
    if text is None:
        return None
    return text.upper() + "!"
```
As guardas vêm primeiro, na ordem em que as verificações devem acontecer: você não pode chamar `text.split()` antes de saber que `text` não é `None`.

# --instructions--

Ordene os itens para definir `first_word`, que retorna `None` quando `text` é `None` ou não tem palavras, e a primeira palavra caso contrário; mantenha as guardas antes do código que depende delas

# --answers--

```python
def first_word(text):
```

```python
    if text is None:
        return None
```

```python
    words = text.split()
```

```python
    if len(words) == 0:
        return None
```

```python
    return words[0]
```

```python
print(first_word("hello world"))
print(first_word(None))
print(first_word("   "))
```

# --solutions--

```python
def first_word(text):
    if text is None:
        return None
    words = text.split()
    if len(words) == 0:
        return None
    return words[0]
print(first_word("hello world"))
print(first_word(None))
print(first_word("   "))
```

# --output--

hello
None
None
