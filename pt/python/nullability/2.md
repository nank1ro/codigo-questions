---
language: python
exerciseType: 1
---

# --description--

Toda chamada de função produz um valor, mesmo quando a função parece não retornar nada. Uma função **sem** instrução `return`, ou com um `return` vazio, devolve `None`:
```python
def say_hello():
    print("hello")

result = say_hello()
print(result)  # None
```
É por isso que chamar `print(my_list.append(3))` mostra `None`: `append` modifica a lista no lugar e não retorna nada.
Uma função que apenas executa uma ação (imprimir, salvar, modificar uma lista) geralmente retorna `None`, enquanto uma função que calcula algo deve devolvê-lo explicitamente com `return`.

# --instructions--

Escreva uma função `store` que recebe um valor e o adiciona à lista `saved`, sem retornar nada

# --seed--

```python
saved = []

def store(value):
    pass
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve usar `append`

```json
{
  "regex": "\\.append\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`store("a")` deve retornar `None`

```python
    def test_1(self):
        self.assertIsNone(store("a"), "--err-t1--")
```

Depois de `store("b")` a lista `saved` deve terminar com `"b"`

```python
    def test_2(self):
        store("b")
        self.assertEqual(saved[-1], "b", "--err-t2--")
```

Todo valor armazenado deve ser mantido, em ordem

```python
    def test_3(self):
        before = len(saved)
        store(1)
        store(2)
        self.assertEqual(saved[before:], [1, 2], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
saved = []

def store(value):
    saved.append(value)
```
