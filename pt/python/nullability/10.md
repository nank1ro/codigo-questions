---
language: python
exerciseType: 1
---

# --description--

Quando `0` ou `""` devem ser mantidos e apenas `None` substituído, o fallback precisa de uma verificação explícita com `is None`. A forma compacta é a **expressão condicional**, `a if condition else b`, que avalia para `a` quando a condição é verdadeira e para `b` caso contrário:
```python
timeout = 0
seconds = timeout if timeout is not None else 30
print(seconds)  # 0, not 30
```

# --instructions--

Escreva uma função `port_or_default` que recebe `port` e o retorna quando ele não é `None`, e `8080` caso contrário; o valor `0` deve ser retornado como está

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Você deve verificar com `is not None` ou `is None`

```json
{
  "regex": "is\\s+(not\\s+)?None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`port_or_default(3000)` deve retornar `3000`

```python
    def test_1(self):
        self.assertEqual(port_or_default(3000), 3000, "--err-t1--")
```

`port_or_default(None)` deve retornar `8080`

```python
    def test_2(self):
        self.assertEqual(port_or_default(None), 8080, "--err-t2--")
```

`port_or_default(0)` deve retornar `0`

```python
    def test_3(self):
        self.assertEqual(port_or_default(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def port_or_default(port):
    return port if port is not None else 8080
```
