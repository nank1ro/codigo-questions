---
language: python
exerciseType: 3
---

# --description--

O operador `or` não retorna `True` ou `False`: ele retorna seu operando à **esquerda** quando este é verdadeiro, e seu operando à **direita** caso contrário. Isso dá uma forma de uma linha para fornecer um fallback:
```python
name = None
print(name or "anonymous")  # anonymous
name = "Ada"
print(name or "anonymous")  # Ada
```
A pegadinha é que o `or` olha a truthiness, não o `None`: `0`, `""` e `[]` também são substituídos pelo fallback. Use `x or fallback` apenas quando todo valor vazio também deve se tornar o fallback.

# --instructions--

O que este código imprime?
```python
def label(count):
    return count or "unknown"

print(label(0), label(None), label(5))
```

# --answers--

- `unknown unknown 5`, porque `0` é falso, então também é substituído pelo fallback
- `0 unknown 5`, porque apenas `None` é substituído pelo fallback
- `False unknown 5`, porque o `or` converte `0` para um booleano
- `True True True`, porque o `or` sempre retorna um booleano

# --solutions--

- `unknown unknown 5`, porque `0` é falso, então também é substituído pelo fallback
