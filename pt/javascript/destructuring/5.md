---
language: javascript
exerciseType: 1
---

# --description--

Um padrão também pode ficar à esquerda de uma atribuição simples, sem `const` ou `let` na frente, e então ele escreve em variáveis que já existem. Isso transforma a troca de dois valores em uma única linha, sem variável temporária:
```javascript
let a = 1;
let b = 2;
[a, b] = [b, a];
console.log(a, b);
// prints 2 1
```
O lado direito é construído primeiro, então ambos os valores antigos já estão seguros dentro do array temporário quando a atribuição acontece. Atenção ao ponto e vírgula na linha anterior: uma linha que começa com `[` seria lida como um índice do que veio antes.

# --instructions--

Escreva uma função `sortPair` que recebe dois valores e retorna um array de dois elementos com o menor primeiro, trocando-os com uma atribuição por desestruturação quando estiverem na ordem errada

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function sortPair(a, b) {
  
}
```

# --asserts--

`sortPair(5, 2)` deve começar com `2`.

```javascript
tryCatch(sortPair(5, 2)[0] === 2);
```

`sortPair(5, 2)` deve terminar com `5`.

```javascript
tryCatch(sortPair(5, 2)[1] === 5);
```

Um par já ordenado deve permanecer como está.

```javascript
tryCatch(sortPair(1, 9).join(",") === "1,9");
```

Dois valores iguais devem permanecer como estão.

```javascript
tryCatch(sortPair(4, 4).join(",") === "4,4");
```

Valores negativos também devem funcionar.

```javascript
tryCatch(sortPair(3, -8).join(",") === "-8,3");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function sortPair(a, b) {
  if (a > b) {
    [a, b] = [b, a];
  }
  return [a, b];
}
```
