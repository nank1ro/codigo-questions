---
language: javascript
exerciseType: 1
---

# --description--

A desestruturação é mais útil bem onde um array chega: um argumento de função, ou o resultado de uma chamada. Em vez de manter o array por perto e indexá-lo em todo lugar, você o desempacota uma vez e dá às partes nomes reais:
```javascript
function middle(range) {
    const [start, end] = range;
    return (start + end) / 2;
}
console.log(middle([0, 10]));
// prints 5
```
Nada é copiado ou alterado no array original, o padrão apenas lê a partir dele.

# --instructions--

Escreva uma função `label` que recebe um array de dois elementos contendo um nome e uma pontuação, e retorna a string `"name: score"`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function label(entry) {
  
}
```

# --asserts--

`label(["Ada", 90])` deve retornar `"Ada: 90"`.

```javascript
tryCatch(label(["Ada", 90]) === "Ada: 90");
```

`label(["Bob", 0])` deve retornar `"Bob: 0"`.

```javascript
tryCatch(label(["Bob", 0]) === "Bob: 0");
```

Uma pontuação negativa deve ser mantida como está.

```javascript
tryCatch(label(["Cy", -3]) === "Cy: -3");
```

`label(["Dee", 100])` deve retornar `"Dee: 100"`.

```javascript
tryCatch(label(["Dee", 100]) === "Dee: 100");
```

Elementos extras devem ser ignorados.

```javascript
tryCatch(label(["Eve", 55, "extra"]) === "Eve: 55");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function label(entry) {
  const [name, score] = entry;
  return `${name}: ${score}`;
}
```
