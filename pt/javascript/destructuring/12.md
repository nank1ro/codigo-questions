---
language: javascript
exerciseType: 1
---

# --description--

Padrões de objeto também têm um rest, e nele ele coleta todas as chaves que o padrão não mencionou em um novo objeto:
```javascript
const user = { id: 1, name: "Ada", city: "London" };
const { id, ...profile } = user;
console.log(profile);
// imprime { name: 'Ada', city: 'London' }
```
Esta é a forma mais curta de construir uma cópia de um objeto sem uma de suas chaves: o original nunca é tocado, e o objeto rest é um novo contendo os valores restantes.

# --instructions--

Escreva uma função `withoutPassword` que retorna uma cópia do objeto dado sem sua chave `password`, deixando o objeto original inalterado

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function withoutPassword(user) {
  
}
```

# --asserts--

A chave `password` deve desaparecer.

```javascript
tryCatch(withoutPassword({ name: "Ada", password: "x" }).password === undefined);
```

As outras chaves devem sobreviver.

```javascript
tryCatch(withoutPassword({ name: "Ada", password: "x" }).name === "Ada");
```

Todas as outras chaves devem sobreviver.

```javascript
tryCatch(Object.keys(withoutPassword({ name: "Ada", password: "x", city: "London" })).length === 2);
```

O objeto original não deve ser alterado.

```javascript
const original = { name: "Bob", password: "y" };
withoutPassword(original);
tryCatch(original.password === "y");
```

Um objeto com apenas uma senha deve dar um objeto vazio.

```javascript
tryCatch(Object.keys(withoutPassword({ password: "z" })).length === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function withoutPassword(user) {
  const { password, ...rest } = user;
  return rest;
}
```
