---
language: javascript
exerciseType: 1
---

# --description--

Todas as funções têm um método `bind` que devolve uma **nova** função com algumas coisas fixadas antecipadamente. O seu primeiro argumento passa a ser o `this` da nova função; os restantes argumentos são colocados à frente daqueles com que a nova função for chamada (uma **aplicação parcial**):
```javascript
function multiply(a, b) {
  return a * b;
}
const double = multiply.bind(null, 2);
console.log(double(21));
// prints 42
```
Fixar `this` é importante nos métodos. Quando um método é copiado para fora do seu objeto e chamado sozinho, `this` deixa de se referir ao objeto, por isso `this.name` passa a ser `undefined`. `bind` fixa-o ao objeto:
```javascript
const user = {
  name: "Ana",
  hello() { return `Hi ${this.name}`; },
};
const loose = user.hello;
console.log(loose());
// prints Hi undefined
const bound = user.hello.bind(user);
console.log(bound());
// prints Hi Ana
```
A função original nunca é alterada: `bind` constrói sempre uma nova, cujo `name` é o nome original precedido de `bound `.

# --instructions--

A função `power` e o objeto `counter` são dados. Guarda na constante `square` uma versão de `power` ligada com `this` definido como `null` e `exponent` fixado em `2`, e na constante `tick` uma versão de `counter.increment` ligada a `counter`, para que continue a funcionar quando chamada sozinha

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = 
const tick = 
```

# --asserts--

`square(3)` deve devolver `9`.

```javascript
tryCatch(square(3) === 9);
```

`square(12)` deve devolver `144`.

```javascript
tryCatch(square(12) === 144);
```

`square` deve ser criada com `bind`, por isso o seu nome é `"bound power"`.

```javascript
tryCatch(square.name === "bound power");
```

`tick` deve ser uma nova função criada com `bind`, não o próprio `counter.increment`.

```javascript
tryCatch(tick !== counter.increment && tick.name === "bound increment");
```

Chamar `tick()` duas vezes sozinho deve devolver `1` e depois `2`, e `counter.count` deve ser `2`.

```javascript
tryCatch(tick() === 1 && tick() === 2 && counter.count === 2);
```

`tick` deve continuar a funcionar quando é guardada noutra variável e chamada a partir daí.

```javascript
tryCatch((() => { const alone = tick; return alone() === 3; })());
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = power.bind(null, 2);
const tick = counter.increment.bind(counter);
```
