---
language: javascript
exerciseType: 2
---

# --description--

O acumulador de `reduce` não tem de ser um número: pode ser uma string, um array ou um objeto. A partir de um objeto vazio `{}` podes contar ou agrupar coisas numa só passagem. Lembra-te de **devolver o acumulador** a partir do callback, caso contrário o passo seguinte recebe `undefined`:
```javascript
const votes = ["yes", "no", "yes"];
const tally = votes.reduce((acc, vote) => {
  acc[vote] = (acc[vote] ?? 0) + 1;
  return acc;
}, {});
console.log(tally);
// prints { yes: 2, no: 1 }
```
`acc[vote] ?? 0` lê a contagem atual, ou `0` quando essa chave ainda não existe.

# --instructions--

Completa o código para que `counts` associe cada primeira letra ao número de palavras que começam por ela, usando `reduce` com um objeto vazio como acumulador inicial e `?? 0` para as chaves em falta, produzindo a saída `{ a: 2, b: 2, c: 1 }`

# --seed--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.[/]((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] [/] 0) + 1;
  return [/];
}, [/]);
console.log(counts);
```

# --answers--

- reduce
- ??
- acc
- {}
- map
- filter
- &&
- word
- 0

# --solutions--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.reduce((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] ?? 0) + 1;
  return acc;
}, {});
console.log(counts);
```

# --output--

{ a: 2, b: 2, c: 1 }
