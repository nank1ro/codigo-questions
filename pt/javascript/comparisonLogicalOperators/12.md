---
language: javascript
exerciseType: 2
---

# --description--

Quando os dois lados são strings, o `>` as compara caractere por caractere na ordem do código, e não pelo tamanho, então `"b" > "a"` é `true` e `"apple" > "ant"` também é `true`.

# --instructions--

Verifique se `num` e **maior que** `7`, imprimindo o resultado booleano

# --seed--

```javascript
var num = 8;
console.log([/][/][/]);
```

# --answers--

- num 
- 'num' 
- == 
- > 
- num 
- >= 
- 7

# --solutions--

```javascript
var num = 8;
console.log(num > 7);
```

# --output--

true
