---
language: javascript
exerciseType: 1
---

# --description--

Quando uma filha define um método que o pai já tem, a versão da filha vence. Isso é chamado de **sobrescrita**:
```javascript
class Animal {
    speak() {
        return "some sound";
    }
}
class Dog extends Animal {
    speak() {
        return "Woof";
    }
}
console.log(new Dog().speak());
// imprime Woof
```
A sobrescrita não apaga a versão do pai, apenas a esconde. Dentro do método da filha, `super.methodName(...)` ainda alcança a versão do pai, o que permite estender o comportamento do pai em vez de substituí-lo:
```javascript
class Puppy extends Dog {
    speak() {
        return super.speak() + "!";
    }
}
console.log(new Puppy().speak());
// imprime Woof!
```
Note a diferença: `super(...)` chama o **construtor** do pai, `super.name(...)` chama um **método** do pai.

# --instructions--

Escreva uma classe `LoudGreeter` que estenda `Greeter` e sobrescreva `greet` para que retorne a saudação do pai em maiúsculas seguida de um ponto de exclamação

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Greeter {
  constructor(name) {
    this.name = name;
  }
  greet() {
    return `Hello ${this.name}`;
  }
}

class LoudGreeter {
  
}
```

# --asserts--

`new LoudGreeter("Ann").greet()` deve retornar `"HELLO ANN!"`.

```javascript
tryCatch(new LoudGreeter("Ann").greet() === "HELLO ANN!");
```

`new LoudGreeter("bob").greet()` deve retornar `"HELLO BOB!"`.

```javascript
tryCatch(new LoudGreeter("bob").greet() === "HELLO BOB!");
```

O pai deve permanecer intacto.

```javascript
tryCatch(new Greeter("Ann").greet() === "Hello Ann");
```

`LoudGreeter` deve reutilizar o método do pai por meio de `super`.

```json
{
  "regex": "super\\.greet\\s*\\(\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Greeter {
  constructor(name) {
    this.name = name;
  }
  greet() {
    return `Hello ${this.name}`;
  }
}

class LoudGreeter extends Greeter {
  greet() {
    return super.greet().toUpperCase() + "!";
  }
}
```
