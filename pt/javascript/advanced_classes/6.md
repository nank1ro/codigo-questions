---
language: javascript
exerciseType: 4
---

# --description--

A ordem importa quando as classes herdam umas das outras. Uma declaração `class` **não** sofre hoisting da forma como uma `function` sofre: o nome só existe a partir da linha em que a classe é escrita em diante. Portanto, uma classe filha precisa aparecer *depois* do pai que ela estende, caso contrário a cláusula `extends` falha com um `ReferenceError`.

# --instructions--

Ordene os itens em um programa funcional. Lembre-se de que a classe pai deve ser definida antes da filha

# --answers--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
```

```javascript
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
```

```javascript
console.log(new Car().describe());
```

# --solutions--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
console.log(new Car().describe());
```

# --output--

a vehicle with four wheels
