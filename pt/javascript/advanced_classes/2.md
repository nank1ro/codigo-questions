---
language: javascript
exerciseType: 1
---

# --description--

Uma classe filha não precisa redefinir nada que o pai já forneça. Os métodos também são herdados, então uma instância da filha pode chamá-los como se fossem seus:
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
    speak() {
        return `${this.name} makes a sound`;
    }
}
class Dog extends Animal {}
console.log(new Dog("Max").speak());
// imprime Max makes a sound
```
Quando uma filha declara o seu próprio construtor, chamar `super(...)` nele é **obrigatório**: sem isso o objeto nunca é inicializado e o JavaScript lança um `ReferenceError`. Uma filha sem construtor algum não tem problema, porque o JavaScript escreve um que repassa todos os argumentos ao pai.

# --instructions--

Escreva uma classe `Cat` que estenda `Animal`, receba `name` e `lives`, passe `name` ao construtor do pai e armazene `lives` na instância

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Cat {
  
}
```

# --asserts--

`new Cat("Kitty", 9).name` deve ser `"Kitty"`.

```javascript
tryCatch(new Cat("Kitty", 9).name === "Kitty");
```

`new Cat("Kitty", 9).lives` deve ser `9`.

```javascript
tryCatch(new Cat("Kitty", 9).lives === 9);
```

`Cat` deve estender `Animal`.

```javascript
tryCatch(new Cat("Kitty", 9) instanceof Animal);
```

`Cat` deve herdar `speak` de `Animal`.

```javascript
tryCatch(new Cat("Kitty", 9).speak() === "Kitty makes a sound");
```

Um segundo gato deve manter os seus próprios valores.

```javascript
tryCatch(new Cat("Tom", 7).lives === 7);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Cat extends Animal {
  constructor(name, lives) {
    super(name);
    this.lives = lives;
  }
}
```
