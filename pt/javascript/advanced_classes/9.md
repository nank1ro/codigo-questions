---
language: javascript
exerciseType: 4
---

# --description--

Um uso muito comum de um método estático é uma **fábrica**: um método que constrói uma instância a partir de algum outro formato de dados e a retorna. Ele mantém o `new` em um só lugar e dá à construção um nome que diz o que ela faz:
```javascript
class Duration {
    constructor(seconds) {
        this.seconds = seconds;
    }
    static fromMinutes(minutes) {
        return new Duration(minutes * 60);
    }
}
console.log(Duration.fromMinutes(2).seconds);
// imprime 120
```
Uma fábrica pode ser chamada antes que qualquer instância exista, o que um método normal não poderia.

# --instructions--

Ordene os itens em um programa funcional que constrói um ponto a partir de um array e imprime as suas coordenadas

# --answers--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
```

```javascript
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
```

```javascript
const p = Point.fromArray([3, 4]);
```

```javascript
console.log(`${p.x},${p.y}`);
```

# --solutions--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
const p = Point.fromArray([3, 4]);
console.log(`${p.x},${p.y}`);
```

# --output--

3,4
