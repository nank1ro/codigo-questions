---
language: javascript
exerciseType: 2
---

# --description--

Nas funções podemos adicionar um _comentário opcional_ que explica o que a função faz:
```javascript
// Imprime 'Hello World' no console.
function helloWorld() {
    console.log("Hello, World!");
}
```
Podemos usar `//` para um comentário de uma linha e `/** */` para um comentário de múltiplas linhas

# --instructions--

Adicione um comentário válido à função

# --seed--

```javascript
[/][/] Returns the product between n1 and n2 numbers;
const product = (n1, n2) => n1 * n2;
```

# --answers--

- /
- /
- /
- \
- \
- \
- /*
- /**

# --solutions--

```javascript
// Returns the product between n1 and n2 numbers;
const product = (n1, n2) => n1 * n2;
```
