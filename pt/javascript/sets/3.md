---
language: javascript
exerciseType: 3
---

# --description--

`add()` retorna o próprio set, então várias chamadas podem ser encadeadas:
```javascript
let letters = new Set();
letters.add("a").add("b");
```
Encadeando ou não, um valor que já está presente nunca é adicionado uma segunda vez, então `size` conta cada valor distinto apenas uma vez.

# --instructions--

O que este código imprime?
```javascript
let letters = new Set();
letters.add("a").add("b").add("a");
console.log(letters.size);
```

# --answers--

- 2
- 3
- 1
- Um erro, porque "a" já está no set

# --solutions--

- 2
