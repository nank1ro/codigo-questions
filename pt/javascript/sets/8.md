---
language: javascript
exerciseType: 2
---

# --description--

`delete(value)` retorna `true` quando o valor foi removido e `false` quando ele não estava no set.
Para remover **todos** os valores de uma vez, chame `clear()`:
```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Remova `"pen"` de `cart` imprimindo o resultado da remoção, depois esvazie o set inteiro e imprima quantos valores restaram

# --seed--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
