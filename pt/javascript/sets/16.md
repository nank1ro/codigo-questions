---
language: javascript
exerciseType: 1
---

# --instructions--

Escreva uma função `firstRepeated(items)` que retorna o primeiro valor do array `items` que aparece pela segunda vez, ou `undefined` se todos os valores forem únicos

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function firstRepeated(items) {

}
```

# --asserts--

`firstRepeated([3, 1, 4, 1, 5, 3])` deve retornar `1`, porque `1` se repete antes de `3`.

```javascript
tryCatch(firstRepeated([3, 1, 4, 1, 5, 3]) === 1);
```

`firstRepeated(["a", "b", "a"])` deve retornar `"a"`.

```javascript
tryCatch(firstRepeated(["a", "b", "a"]) === "a");
```

`firstRepeated([1, 2, 3])` deve retornar `undefined`.

```javascript
tryCatch(firstRepeated([1, 2, 3]) === undefined);
```

`firstRepeated([])` deve retornar `undefined`.

```javascript
tryCatch(firstRepeated([]) === undefined);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function firstRepeated(items) {
  let seen = new Set();
  for (const item of items) {
    if (seen.has(item)) {
      return item;
    }
    seen.add(item);
  }
  return undefined;
}
```
