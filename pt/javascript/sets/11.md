---
language: javascript
exerciseType: 2
---

# --instructions--

Construa o array `common` com os números que estão tanto em `a` quanto em `b`, depois imprima-o

# --seed--

```javascript
let a = new Set([1, 2, 3]);
let b = new Set([2, 3, 4]);
let common = [...a].[/]((x) => b.[/](x));
console.log(common);
```

# --answers--

- filter
- has
- map
- find
- includes
- contains

# --solutions--

```javascript
let a = new Set([1, 2, 3]);
let b = new Set([2, 3, 4]);
let common = [...a].filter((x) => b.has(x));
console.log(common);
```

# --output--

[ 2, 3 ]
