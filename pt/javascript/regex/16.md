---
language: javascript
exerciseType: 1
---

# --description--

Uma última flag completa o conjunto. Por padrão `^` e `$` significam o início e o fim da **string inteira**, então um padrão ancorado com `^` só pode corresponder no comecinho, mesmo quando o texto tem várias linhas.

A flag **`m`** (multiline) muda isso: `^` e `$` então também correspondem logo depois e logo antes de cada quebra de linha, então cada linha é ancorada por si mesma:
```javascript
const text = "note a\nb\nnote c";
console.log(text.match(/^note.*/g));
// imprime [ 'note a' ]
console.log(text.match(/^note.*/gm));
// imprime [ 'note a', 'note c' ]
```
Dois detalhes importam aqui. Por padrão o `.` não corresponde a uma quebra de linha (apenas a flag `s` muda isso), então `.*` para no fim da linha por si só. E `match` com a flag `g` retorna `null`, não um array vazio, quando nada corresponde, então combine-o com `?? []` quando você promete retornar um array.

# --instructions--

Escreva uma função `todoLines` que recebe uma string `text` e retorna um array com cada linha que começa com `todo`, ignorando maiúsculas e minúsculas. Retorne um array vazio quando não houver nenhuma

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function todoLines(text) {
  
}
```

# --asserts--

`todoLines("todo a\nb\nTODO c")` deve retornar ambas as linhas de todo.

```javascript
tryCatch(JSON.stringify(todoLines("todo a\nb\nTODO c")) === '["todo a","TODO c"]');
```

`todoLines("nothing here")` deve retornar um array vazio.

```javascript
tryCatch(JSON.stringify(todoLines("nothing here")) === '[]');
```

`todoLines("Todo write tests")` deve retornar essa linha.

```javascript
tryCatch(JSON.stringify(todoLines("Todo write tests")) === '["Todo write tests"]');
```

`todoLines("a todo b")` deve retornar um array vazio, porque a linha não começa com todo.

```javascript
tryCatch(JSON.stringify(todoLines("a todo b")) === '[]');
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function todoLines(text) {
  return text.match(/^todo.*/gim) ?? [];
}
```
