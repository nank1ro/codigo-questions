---
language: javascript
exerciseType: 1
---

# --description--

Como `match` retorna `null` quando o padrão está ausente, ler `match[0]` direto lança `TypeError: Cannot read properties of null`. Proteja-o:
```javascript
function firstWord(text) {
  const match = text.match(/[a-z]+/);
  if (match === null) {
    return "";
  }
  return match[0];
}
```
O operador de coalescência nula escreve a mesma proteção em uma linha, porque `match?.[0]` é `undefined` quando `match` é `null`:
```javascript
return text.match(/[a-z]+/)?.[0] ?? "";
```

# --instructions--

Escreva uma função `firstNumber` que recebe uma string `text` e retorna a primeira sequência de dígitos que ela contém como string, ou uma string vazia quando não há nenhuma

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function firstNumber(text) {
  
}
```

# --asserts--

`firstNumber("order 42 shipped")` deve retornar `42`.

```javascript
tryCatch(firstNumber("order 42 shipped") === "42");
```

`firstNumber("a1b22")` deve retornar `1`.

```javascript
tryCatch(firstNumber("a1b22") === "1");
```

`firstNumber("no digits here")` deve retornar uma string vazia.

```javascript
tryCatch(firstNumber("no digits here") === "");
```

`firstNumber("2026 and 2027")` deve retornar `2026`.

```javascript
tryCatch(firstNumber("2026 and 2027") === "2026");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function firstNumber(text) {
  return text.match(/\d+/)?.[0] ?? "";
}
```
