---
language: javascript
exerciseType: 2
---

# --description--

`delete(key)` retorna `true` quando um par foi removido e `false` quando a chave não existia.
Para remover **todos** os pares de uma vez, chame `clear()`:
```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Remova `"pen"` de `cart` imprimindo o resultado da remoção, depois esvazie o map inteiro e imprima quantos pares restam

# --seed--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
