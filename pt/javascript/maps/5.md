---
language: javascript
exerciseType: 2
---

# --description--

Um map se lembra da ordem em que os pares foram adicionados, e você pode percorrê-lo com `for...of`.
O método `entries()` fornece cada par como um array `[key, value]`, que você pode desestruturar diretamente no loop:
```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
// prints apple: 3
// prints pear: 5
```
Percorrer o map diretamente, `for (const [name, qty] of stock)`, faz exatamente a mesma coisa.

# --instructions--

Percorra as entradas de `stock` e imprima cada nome seguido de sua quantidade

# --seed--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [/] of stock.[/]()) {
  console.log(`${name}: ${qty}`);
}
```

# --answers--

- [name, qty]
- [qty, name]
- name, qty
- entries
- pairs
- items

# --solutions--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
```

# --output--

apple: 3
pear: 5
