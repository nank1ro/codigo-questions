---
language: javascript
exerciseType: 2
---

# --description--

Um **Map** armazena **pares chave-valor**: cada valor é salvo sob uma chave, e você usa essa chave para encontrar o valor novamente.
Você cria um map vazio com `new Map()`, adiciona um par com `set(key, value)` e lê um valor com `get(key)`:
```javascript
let ages = new Map();
ages.set("Ann", 30);
ages.set("Bob", 25);
console.log(ages.get("Ann"));
// prints 30
```
Chamar `set()` com uma chave que já existe substitui seu valor.

# --instructions--

Crie um map vazio `ages`, armazene `30` sob a chave `"Ann"`, depois imprima o valor armazenado para `"Ann"`

# --seed--

```javascript
let ages = new [/]();
ages.[/]("Ann", 30);
console.log(ages.[/]("Ann"));
```

# --answers--

- Map
- set
- get
- Object
- add
- put
- read

# --solutions--

```javascript
let ages = new Map();
ages.set("Ann", 30);
console.log(ages.get("Ann"));
```

# --output--

30
