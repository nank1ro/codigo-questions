---
language: javascript
exerciseType: 2
---

# --description--

O método `split(separator)` divide uma string em um **array** de pedaços, cortando em cada `separator`:
```javascript
let sentence = "I like JavaScript";
let words = sentence.split(" ");
console.log(words);
// prints [ 'I', 'like', 'JavaScript' ]
```
O oposto é o método de array `join(separator)`, que cola os pedaços de volta em uma string:
```javascript
console.log(words.join("-"));
// prints I-like-JavaScript
```

# --instructions--

Divida `csv` em cada vírgula e imprima quantas cores ele contém

# --seed--

```javascript
let csv = "red,green,blue";
let colors = csv.[/]([/]);
console.log(colors.[/]);
```

# --answers--

- split
- ","
- " "
- join
- length
- size

# --solutions--

```javascript
let csv = "red,green,blue";
let colors = csv.split(",");
console.log(colors.length);
```

# --output--

3
