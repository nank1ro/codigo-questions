---
language: javascript
exerciseType: 2
---

# --description--

Strings possuem muitos **métodos** integrados. Dois dos mais simples alteram a caixa de todas as letras:
```javascript
let word = "Hello";
console.log(word.toUpperCase());
// prints HELLO
console.log(word.toLowerCase());
// prints hello
```
Ambos os métodos não recebem argumentos, então não esqueça os parênteses.

# --instructions--

Imprima `name` em maiúsculas e depois em minúsculas

# --seed--

```javascript
let name = "Ada";
console.log(name.[/]());
console.log(name.[/]());
```

# --answers--

- toUpperCase
- toLowerCase
- upper
- lower
- capitalize

# --solutions--

```javascript
let name = "Ada";
console.log(name.toUpperCase());
console.log(name.toLowerCase());
```

# --output--

ADA
ada
