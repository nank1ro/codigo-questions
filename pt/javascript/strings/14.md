---
language: javascript
exerciseType: 1
---

# --description--

O método `padStart(targetLength, padString)` adiciona `padString` ao **início** da string até que ela atinja `targetLength` caracteres. `padEnd()` faz o mesmo no final:
```javascript
console.log("7".padStart(3, "0"));
// prints 007
console.log("Tea".padEnd(6, "."));
// prints Tea...
```
Se a string já for longa o suficiente, ela é retornada sem alterações.
Números não têm métodos de string, então converta-os primeiro com `String(number)`.

# --instructions--

Escreva uma função `twoDigits(number)` que retorna `number` como uma string de pelo menos dois caracteres, preenchida à esquerda com `"0"`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function twoDigits(number) {

}
```

# --asserts--

`twoDigits(5)` deve retornar `"05"`.

```javascript
tryCatch(twoDigits(5) === "05");
```

`twoDigits(12)` deve retornar `"12"`.

```javascript
tryCatch(twoDigits(12) === "12");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function twoDigits(number) {
  return String(number).padStart(2, "0");
}
```
