---
language: javascript
exerciseType: 4
---

# --instructions--

Ordene as linhas para imprimir a extensão de `file` em maiúsculas

# --answers--

```javascript
let file = "report.pdf";
```

```javascript
let extension = file.slice(-3);
```

```javascript
console.log(extension.toUpperCase());
```

# --solutions--

```javascript
let file = "report.pdf";
let extension = file.slice(-3);
console.log(extension.toUpperCase());
```

# --output--

PDF
