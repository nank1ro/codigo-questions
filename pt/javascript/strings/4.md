---
language: javascript
exerciseType: 1
---

# --description--

Para verificar se uma string contém outra string, use estes métodos, que retornam um booleano:
- `includes(text)` é `true` se `text` aparecer em qualquer lugar
- `startsWith(text)` é `true` se a string começar com `text`
- `endsWith(text)` é `true` se a string terminar com `text`

```javascript
let file = "photo.png";
console.log(file.includes("."));
// prints true
console.log(file.startsWith("ph"));
// prints true
console.log(file.endsWith(".jpg"));
// prints false
```
A comparação diferencia maiúsculas de minúsculas: `"Hello".includes("h")` é `false`.

# --instructions--

Escreva uma função `isImage(fileName)` que retorna `true` quando `fileName` termina com `".png"` e `false` caso contrário

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isImage(fileName) {

}
```

# --asserts--

`isImage("cat.png")` deve retornar `true`.

```javascript
tryCatch(isImage("cat.png") === true);
```

`isImage("cat.jpg")` deve retornar `false`.

```javascript
tryCatch(isImage("cat.jpg") === false);
```

`isImage("png.txt")` deve retornar `false`.

```javascript
tryCatch(isImage("png.txt") === false);
```

`isImage("a.png.txt")` deve retornar `false`.

```javascript
tryCatch(isImage("a.png.txt") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isImage(fileName) {
  return fileName.endsWith(".png");
}
```
