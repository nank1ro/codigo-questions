---
language: javascript
exerciseType: 2
---

# --description--

Você não precisa armazenar uma arrow function para chamá-la. Envolva-a em parênteses e adicione a lista de argumentos logo em seguida: esta é uma arrow function **imediatamente invocada**:
```javascript
const doubled = ((n) => n * 2)(5);
console.log(doubled);
// prints 10
```
Os parênteses ao redor da arrow são obrigatórios: sem eles, `(n) => n * 2(5)` tentaria chamar o número `2`. Funções imediatamente invocadas são úteis para calcular um valor com algumas variáveis temporárias que não devem vazar para o resto do programa.

# --instructions--

Complete o código para que `message` receba o resultado de uma arrow function imediatamente invocada que retorna `"ready"`, e depois imprima `message` e o resultado de chamar imediatamente `(n) => n * 3` com `4`

# --seed--

```javascript
const message = [/]() => "ready"[/]();
console.log(message);
console.log([/](n) => n * 3)(4));
```

# --answers--

- (
- )
- (
- [
- ]
- {
- }

# --solutions--

```javascript
const message = (() => "ready")();
console.log(message);
console.log(((n) => n * 3)(4));
```

# --output--

ready
12
