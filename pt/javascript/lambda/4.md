---
language: javascript
exerciseType: 2
---

# --description--

Envolver o objeto literal em parênteses é a forma padrão de construir objetos com uma arrow function de uma linha, por exemplo quando você transforma um par de valores em um registro:
```javascript
const user = (name, age) => ({ name: name, age: age });
console.log(user("Ana", 30).age);
// prints 30
```
Uma arrow function sem parâmetros começa com um par vazio de parênteses `()`:
```javascript
const empty = () => ({});
console.log(empty());
// prints {}
```

# --instructions--

Complete `point` para que ele retorne um objeto com as propriedades `x` e `y`, e `origin` para que ele não receba parâmetros, e depois imprima `point(3, 4).y` e `origin().x`

# --seed--

```javascript
const point = (x, y) => [/]{ x: x, y: y }[/];
const origin = [/] => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --answers--

- (
- )
- ()
- [
- ]
- function
- this

# --solutions--

```javascript
const point = (x, y) => ({ x: x, y: y });
const origin = () => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --output--

4
0
