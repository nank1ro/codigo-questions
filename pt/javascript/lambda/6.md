---
language: javascript
exerciseType: 4
---

# --description--

Dois outros métodos de array recebem um callback.
`forEach(callback)` chama o callback uma vez por elemento e não retorna nada; use-o para efeitos colaterais, como imprimir.
`reduce(callback, initialValue)` reduz o array a um único valor: o callback recebe o valor acumulado até o momento e o elemento atual, e retorna o novo valor acumulado:
```javascript
const numbers = [1, 2, 3];
numbers.forEach((n) => console.log(n));
// prints 1, 2 and 3 on three lines
const total = numbers.reduce((sum, n) => sum + n, 0);
console.log(total);
// prints 6
```

# --instructions--

Ordene as linhas para calcular o total de `prices` com `reduce` logo após declarar o array, imprima cada preço em sua própria linha com `forEach`, e depois imprima o total

# --answers--

```javascript
const prices = [4, 6, 10];
```

```javascript
const total = prices.reduce((sum, p) => sum + p, 0);
```

```javascript
prices.forEach((p) => console.log(p));
```

```javascript
console.log(total);
```

# --solutions--

```javascript
const prices = [4, 6, 10];
const total = prices.reduce((sum, p) => sum + p, 0);
prices.forEach((p) => console.log(p));
console.log(total);
```

# --output--

4
6
10
20
