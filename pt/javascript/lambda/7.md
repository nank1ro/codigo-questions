---
language: javascript
exerciseType: 2
---

# --description--

`sort(compare)` ordena um array no próprio lugar usando um callback que recebe dois elementos e retorna um número negativo quando o primeiro deve vir primeiro, um número positivo quando o segundo deve vir primeiro, ou `0` quando são iguais. Para números, `(a, b) => a - b` ordena em ordem crescente e `(a, b) => b - a` em ordem decrescente.
`find(callback)` retorna o primeiro elemento para o qual o callback retorna `true`, ou `undefined` se não houver nenhum:
```javascript
const scores = [50, 90, 70];
scores.sort((a, b) => a - b);
console.log(scores);
// prints [ 50, 70, 90 ]
console.log(scores.find((s) => s > 60));
// prints 70
```

# --instructions--

Ordene `ages` em ordem crescente, depois encontre a primeira idade estritamente maior que `21` e imprima tanto o array ordenado quanto essa idade

# --seed--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a [/] b);
const adult = ages.find((age) => age [/] 21);
console.log(ages);
console.log(adult);
```

# --answers--

- -
- >
- +
- <
- ===
- *

# --solutions--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a - b);
const adult = ages.find((age) => age > 21);
console.log(ages);
console.log(adult);
```

# --output--

[ 18, 25, 31 ]
25
