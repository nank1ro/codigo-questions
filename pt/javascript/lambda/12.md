---
language: javascript
exerciseType: 4
---

# --instructions--

Ordene as linhas para definir `makeCounter`, que retorna uma arrow function que incrementa e retorna o seu `count` privado, depois crie o contador `next` e imprima o resultado de duas chamadas na mesma linha

# --answers--

```javascript
const makeCounter = () => {
```

```javascript
  let count = 0;
```

```javascript
  return () => {
```

```javascript
    count += 1;
```

```javascript
    return count;
```

```javascript
  };
```

```javascript
};
```

```javascript
const next = makeCounter();
```

```javascript
console.log(next(), next());
```

# --solutions--

```javascript
const makeCounter = () => {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
};
const next = makeCounter();
console.log(next(), next());
```

# --output--

1 2
