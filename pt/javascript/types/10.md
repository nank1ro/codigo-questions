---
language: javascript
exerciseType: 1
---

# --description--

Quando o JavaScript precisa de um boolean, por exemplo em uma condição `if` ou em `Boolean(value)`, ele converte o valor. Apenas oito valores se tornam `false`; eles são chamados de **falsy**:
`false`, `0`, `-0`, `0n`, `""`, `null`, `undefined` e `NaN`.
**Todo o resto é truthy**, incluindo alguns valores que parecem vazios:
```javascript
console.log(Boolean(0), Boolean(""), Boolean(NaN));
// prints false false false
console.log(Boolean("0"), Boolean("false"), Boolean([]), Boolean({}));
// prints true true true true
```
`"0"` é uma string não vazia, então é truthy; um array vazio é um objeto, então também é truthy.

# --instructions--

Escreva uma função `countTruthy(values)` que retorna quantos elementos do array `values` são truthy

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function countTruthy(values) {

}
```

# --asserts--

`countTruthy([1, "a", true])` deve retornar `3`.

```javascript
tryCatch(countTruthy([1, "a", true]) === 3);
```

`countTruthy([0, "", null, undefined, NaN, false])` deve retornar `0`.

```javascript
tryCatch(countTruthy([0, "", null, undefined, NaN, false]) === 0);
```

`countTruthy(["0", "false", [], {}])` deve retornar `4`.

```javascript
tryCatch(countTruthy(["0", "false", [], {}]) === 4);
```

`countTruthy([0n, -0, " "])` deve retornar `1`, porque apenas a string com espaço é truthy.

```javascript
tryCatch(countTruthy([0n, -0, " "]) === 1);
```

`countTruthy([])` deve retornar `0`.

```javascript
tryCatch(countTruthy([]) === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function countTruthy(values) {
  let count = 0;
  for (const value of values) {
    if (value) {
      count++;
    }
  }
  return count;
}
```
