---
language: javascript
exerciseType: 1
---

# --description--

Como os números são doubles, alguns decimais não podem ser armazenados exatamente e pequenos erros aparecem:
```javascript
console.log(0.1 + 0.2);
// prints 0.30000000000000004
console.log(0.1 + 0.2 === 0.3);
// prints false
```
O método `toFixed(digits)` arredonda um número para `digits` casas decimais, mas retorna uma **string**, o que é bom para exibição e errado para outras contas:
```javascript
const price = (0.1 + 0.2).toFixed(2);
console.log(price, typeof price);
// prints 0.30 string
```
Para obter um **número** arredondado, converta o resultado de volta com `Number()`:
```javascript
console.log(Number((0.1 + 0.2).toFixed(1)));
// prints 0.3
```

# --instructions--

Escreva uma função `roundTo(value, decimals)` que retorna `value` arredondado para `decimals` casas decimais como um **número**, usando `toFixed` e convertendo o resultado de volta

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function roundTo(value, decimals) {

}
```

# --asserts--

`roundTo(3.14159, 2)` deve retornar `3.14`.

```javascript
tryCatch(roundTo(3.14159, 2) === 3.14);
```

`roundTo(0.1 + 0.2, 1)` deve retornar `0.3`.

```javascript
tryCatch(roundTo(0.1 + 0.2, 1) === 0.3);
```

`roundTo(2.5, 0)` deve retornar `3`.

```javascript
tryCatch(roundTo(2.5, 0) === 3);
```

`roundTo(10, 2)` deve retornar `10`.

```javascript
tryCatch(roundTo(10, 2) === 10);
```

O resultado deve ser um número, não uma string.

```javascript
tryCatch(typeof roundTo(1.005, 1) === "number");
```

A função deve usar `toFixed`.

```javascript
tryCatch(/toFixed\(/.test(roundTo.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function roundTo(value, decimals) {
  return Number(value.toFixed(decimals));
}
```
