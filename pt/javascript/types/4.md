---
language: javascript
exerciseType: 2
---

# --description--

Você pode converter um valor para outro tipo **explicitamente** chamando o tipo como uma função:
- `Number(value)` converte para um número: `Number("42")` é `42`
- `String(value)` converte para uma string: `String(42)` é `"42"`
- `Boolean(value)` converte para um boolean: `Boolean("")` é `false`

O resultado é um valor completamente novo; o original não é alterado:
```javascript
const input = "7";
const count = Number(input) + 1;
console.log(count, typeof count);
// prints 8 number
console.log(String(count) + "!");
// prints 8!
```
Converter explicitamente deixa sua intenção visível: quem lê `Number(input)` sabe que `input` era texto.

# --instructions--

Complete o código para que `count` seja `input` convertido para número mais `1`, `label` seja `count` convertido para string e repetido duas vezes, e `flag` seja a string vazia convertida para boolean, produzindo a saída `43 4343 false`

# --seed--

```javascript
const input = "42";
const count = [/](input) + 1;
const label = [/](count).repeat(2);
const flag = [/]("");
console.log(count, label, flag);
```

# --answers--

- Number
- String
- Boolean
- Int
- Bool
- toString

# --solutions--

```javascript
const input = "42";
const count = Number(input) + 1;
const label = String(count).repeat(2);
const flag = Boolean("");
console.log(count, label, flag);
```

# --output--

43 4343 false
