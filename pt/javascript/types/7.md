---
language: javascript
exerciseType: 1
---

# --description--

JavaScript também converte **implicitamente**, e o operador `+` é onde isso mais causa problemas. Se qualquer um dos lados for uma string, o `+` **concatena** e o outro lado é convertido para uma string:
```javascript
console.log("5" + 3);
// prints 53
console.log(1 + 2 + "3");
// prints 33, because 1 + 2 is computed first
```
Todos os outros operadores aritméticos convertem os dois lados para **números**:
```javascript
console.log("6" - 2, "3" * "4");
// prints 4 12
```
Portanto, somar valores que vêm de texto (entrada do usuário, arquivos, URLs) pode construir silenciosamente uma string em vez de uma soma. Converta com `Number()` antes de somar para ficar seguro.

# --instructions--

Escreva uma função `total(prices)` que retorna a soma numérica dos elementos do array `prices`, onde cada elemento é um número ou uma string contendo um número, como `"2.5"`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function total(prices) {

}
```

# --asserts--

`total([1, 2, 3])` deve retornar `6`.

```javascript
tryCatch(total([1, 2, 3]) === 6);
```

`total(["1", 2])` deve retornar o número `3`, não a string `"12"`.

```javascript
tryCatch(total(["1", 2]) === 3);
```

`total(["2.5", "0.5"])` deve retornar `3`.

```javascript
tryCatch(total(["2.5", "0.5"]) === 3);
```

`total(["10"])` deve retornar o número `10`.

```javascript
tryCatch(total(["10"]) === 10 && typeof total(["10"]) === "number");
```

`total([])` deve retornar `0`.

```javascript
tryCatch(total([]) === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function total(prices) {
  let sum = 0;
  for (const price of prices) {
    sum += Number(price);
  }
  return sum;
}
```
