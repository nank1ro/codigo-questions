---
language: javascript
exerciseType: 1
---

# --description--

Texto vindo de formulários, arquivos ou URLs é sempre uma string, mesmo quando representa um número ou um boolean. Convertê-lo de volta para o tipo certo combina o que você já viu: compare com `"true"` e `"false"` para booleans, e tente `Number()` para números, lembrando que `Number("")` é `0` e que `Number.isNaN` informa quando a conversão falhou:
```javascript
console.log(Number("3.5"), Number(""), Number("12px"));
// prints 3.5 0 NaN
```
Quando nada corresponde, mantenha a string como está.

# --instructions--

Escreva uma função `parseValue(text)` que retorna `true` ou `false` quando `text` é `"true"` ou `"false"`, um número quando o `text` inteiro é um número, e `text` sem alterações em todos os outros casos, incluindo a string vazia

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function parseValue(text) {

}
```

# --asserts--

`parseValue("42")` deve retornar o número `42`.

```javascript
tryCatch(parseValue("42") === 42);
```

`parseValue("3.5")` deve retornar `3.5`.

```javascript
tryCatch(parseValue("3.5") === 3.5);
```

`parseValue("true")` e `parseValue("false")` devem retornar os booleans `true` e `false`.

```javascript
tryCatch(parseValue("true") === true && parseValue("false") === false);
```

`parseValue("Ana")` deve retornar `"Ana"`.

```javascript
tryCatch(parseValue("Ana") === "Ana");
```

`parseValue("12px")` deve retornar `"12px"`, porque o texto inteiro não é um número.

```javascript
tryCatch(parseValue("12px") === "12px");
```

`parseValue("")` deve retornar `""`, não `0`.

```javascript
tryCatch(parseValue("") === "");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function parseValue(text) {
  if (text === "true") {
    return true;
  }
  if (text === "false") {
    return false;
  }
  if (text !== "" && !Number.isNaN(Number(text))) {
    return Number(text);
  }
  return text;
}
```
