---
language: javascript
exerciseType: 1
---

# --description--

`Number()` é rigoroso: a string inteira deve ser um número, caso contrário o resultado é `NaN` ("Not a Number"):
```javascript
console.log(Number("12px"));
// prints NaN
```
`parseInt()` e `parseFloat()` são mais tolerantes: eles leem dígitos a partir do início da string, ignoram espaços à esquerda e param no primeiro caractere que não faz parte de um número. `parseInt` mantém apenas a parte inteira:
```javascript
console.log(parseInt("12px"), parseFloat("1.5kg"));
// prints 12 1.5
console.log(parseInt("3.9em"), parseInt("-4px"));
// prints 3 -4
```
Quando a string não começa com algo que possa iniciar um número (um sinal opcional, seguido de um dígito), eles também retornam `NaN`:
```javascript
console.log(parseInt("auto"));
// prints NaN
```
`NaN` é o único valor que não é igual a si mesmo, então `x === NaN` é sempre `false`; para detectá-lo use `Number.isNaN(x)`.

# --instructions--

Escreva uma função `toPixels(text)` que retorna o número inteiro no início de `text` (como `12` para `"12px"`), ou `0` quando `text` não começa com um número, usando `parseInt`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function toPixels(text) {

}
```

# --asserts--

`toPixels("12px")` deve retornar `12`.

```javascript
tryCatch(toPixels("12px") === 12);
```

`toPixels("3.9em")` deve retornar `3`.

```javascript
tryCatch(toPixels("3.9em") === 3);
```

`toPixels("-4px")` deve retornar `-4`.

```javascript
tryCatch(toPixels("-4px") === -4);
```

`toPixels("auto")` deve retornar `0`.

```javascript
tryCatch(toPixels("auto") === 0);
```

`toPixels("")` deve retornar `0`.

```javascript
tryCatch(toPixels("") === 0);
```

A função deve usar `parseInt`.

```javascript
tryCatch(/parseInt\(/.test(toPixels.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function toPixels(text) {
  const value = parseInt(text);
  if (Number.isNaN(value)) {
    return 0;
  }
  return value;
}
```
