---
language: javascript
exerciseType: 2
---

# --description--

Os membros também podem conter **números**. Valores numéricos são úteis quando os membros têm uma ordem natural, pois é possível compará-los com os operadores usuais:
```javascript
const Priority = Object.freeze({
  LOW: 1,
  MEDIUM: 2,
  HIGH: 3,
});
console.log(Priority.HIGH > Priority.LOW);
// prints true
```
A contrapartida é a legibilidade: imprimir `Priority.HIGH` mostra `3`, o que informa muito menos do que a string `"high"` informaria.

# --instructions--

Declare a enumeração numérica congelada `Level` com `EASY` igual a `1`, `NORMAL` igual a `2` e `HARD` igual a `3`, depois imprima se `HARD` é maior que `EASY` e a diferença entre `HARD` e `EASY`

# --seed--

```javascript
const Level = Object.freeze({
  EASY: [/],
  NORMAL: 2,
  HARD: [/],
});
console.log(Level.HARD [/] Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --answers--

- 1
- 3
- >
- 0
- <
- ===

# --solutions--

```javascript
const Level = Object.freeze({
  EASY: 1,
  NORMAL: 2,
  HARD: 3,
});
console.log(Level.HARD > Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --output--

true
2
