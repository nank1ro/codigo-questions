---
language: javascript
exerciseType: 4
---

# --description--

A ordem dentro de um comentário JSDoc é sempre a mesma: primeiro o resumo, depois um `@param` por parâmetro na ordem em que são declarados, e por último `@returns`. A abertura `/**` e o fechamento ` */` envolvem tudo, e o comentário fica diretamente acima da função que descreve:
```javascript
/**
 * Returns the number of seconds in the given minutes.
 * @param {number} minutes a whole number of minutes
 * @returns {number} minutes multiplied by sixty
 */
function toSeconds(minutes) {
  return minutes * 60;
}
```

# --instructions--

Ordene os itens para documentar `twice` com um resumo, uma linha `@param` e uma linha `@returns`, nessa ordem e diretamente acima da função, e depois imprima `twice(21)` após definir a função

# --answers--

```javascript
/**
```

```javascript
 * Returns the double of a number.
```

```javascript
 * @param {number} n the number to double
```

```javascript
 * @returns {number} n multiplied by two
```

```javascript
 */
```

```javascript
function twice(n) {
  return n * 2;
}
```

```javascript
console.log(twice(21));
```

# --solutions--

```javascript
/**
 * Returns the double of a number.
 * @param {number} n the number to double
 * @returns {number} n multiplied by two
 */
function twice(n) {
  return n * 2;
}
console.log(twice(21));
```

# --output--

42
