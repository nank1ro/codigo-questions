---
language: javascript
exerciseType: 2
---

# --description--

Vamos continuar com o exemplo anterior.
```javascript
var niceWeather = true;
if (niceWeather) {
    // brincar fora
}
```
Vimos que a instrução `if` executa o bloco de código apenas se a condição for `true`.
Outra coisa importante a considerar é representada pelas **chaves** `{}` que indicam um bloco de código.

# --instructions--

Corrija a instrução `if` adicionando as *chaves* `{}`

# --seed--

```javascript
var time = 8;
if (time < 12) [/]
    console.log("It's morning");
[/]
```

# --answers--

- if
- ==
- =
- else
- ?
- {
- }

# --solutions--

```javascript
var time = 8;
if (time < 12) {
    console.log("It's morning");
}
```

# --output--

It's morning
