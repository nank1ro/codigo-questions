---
language: javascript
exerciseType: 2
---

# --description--

O _operador de coalescência nula_ `a ?? b` desembrulha um opcional `a` se ele contiver um valor, ou retorna um valor padrão `b` se `a` é `nil`.
A expressão `a` é sempre de um tipo opcional.
A expressão `b` deve corresponder ao tipo que está armazenado dentro de a.
O operador de coalescência nula é abreviação para o código abaixo:
```javascript
a != nil ? a! : b;
```

# --instructions--

Use o _operador de coalescência nula_

# --seed--

```javascript
let name; // name is null
let greet = "Hello ";
greet += name[/]"World";
console.log(greet);
```

# --answers--

-  if
-  ==
-  =
-  else
-  ??
-  ?

# --solutions--

```javascript
let name; // name is null
let greet = "Hello ";
greet += name ?? "World";
console.log(greet);
```

# --output--

Hello World
