---
language: javascript
exerciseType: 2
---

# --description--

Acabamos de ver como executar um bloco de código se uma condição ocorrer, agora vamos ver como executar outro bloco de código se a primeira condição falhar.
Vamos brincar fora se o clima estiver bom; caso contrário, ficamos em casa.
Em JavaScript podemos usar a instrução `else` (senão), como:
```javascript
var niceWeather = true;
if (niceWeather) {
    // brincar fora
} else {
    // ficar em casa
}
```

# --instructions--

Crie uma instrução `else` e no bloco de código imprima a string `"It's afternoon"` (É tarde)

# --seed--

```javascript
var time = 15;
if (time < 12) {
    console.log("It's morning");
} [/][/]
    console.log("It's afternoon");
}
```

# --answers--

- It's afternoon
- 12
- else
-  <
-  >
- :
- ;
-  {

# --solutions--

```javascript
var time = 15;
if (time < 12) {
    console.log("It's morning");
} else {
    console.log("It's afternoon");
}
```

# --output--

It's afternoon
