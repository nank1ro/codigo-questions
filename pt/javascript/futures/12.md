---
language: javascript
exerciseType: 1
---

# --description--

`Promise.all` funciona com um array de qualquer tamanho, incluindo um vazio: aguardar `Promise.all([])` devolve um array vazio imediatamente. Isso torna seguro passar uma lista construída em tempo de execução, sem um caso especial para "não há nada a esperar".
```javascript
const values = await Promise.all(items);
console.log(values.length === items.length);
// prints true
```
O array que ela devolve sempre tem exatamente tantos elementos quanto o array que recebeu, nas mesmas posições, então ele pode ser percorrido como qualquer outro array.

# --instructions--

Escreva uma função `async` `sumAll` que recebe um array de promises de números, aguarda todos eles juntos com `Promise.all` e retorna a soma deles. Um array vazio deve dar `0`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
async function sumAll(promises) {
  
}
```

# --asserts--

`await sumAll([])` deve ser `0`.

```javascript
(async () => {
  tryCatch((await sumAll([])) === 0);
})().catch(() => tryCatch(false));
```

`await sumAll([Promise.resolve(1), Promise.resolve(2), Promise.resolve(3)])` deve ser `6`.

```javascript
(async () => {
  const promises = [Promise.resolve(1), Promise.resolve(2), Promise.resolve(3)];
  tryCatch((await sumAll(promises)) === 6);
})().catch(() => tryCatch(false));
```

`await sumAll([Promise.resolve(-4), Promise.resolve(4)])` deve ser `0`.

```javascript
(async () => {
  const promises = [Promise.resolve(-4), Promise.resolve(4)];
  tryCatch((await sumAll(promises)) === 0);
})().catch(() => tryCatch(false));
```

`sumAll` também deve funcionar com promises que levam quantidades diferentes de tempo.

```javascript
(async () => {
  const later = (n, ms) => new Promise((resolve) => setTimeout(() => resolve(n), ms));
  tryCatch((await sumAll([later(10, 30), later(5, 5)])) === 15);
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
async function sumAll(promises) {
  const values = await Promise.all(promises);
  let total = 0;
  for (const value of values) {
    total += value;
  }
  return total;
}
```
