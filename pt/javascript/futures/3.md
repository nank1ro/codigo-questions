---
language: javascript
exerciseType: 1
---

# --description--

Quando o valor realmente chega mais tarde, você constrói a promise você mesmo com **`new Promise`**. Ela recebe uma função, que por sua vez recebe um callback **`resolve`**: chame `resolve(value)` quando o valor estiver pronto e a promise será cumprida com ele.
```javascript
const soon = new Promise((resolve) => {
  setTimeout(() => resolve("done"), 1000);
});
```
`setTimeout(callback, ms)` agenda o `callback` para rodar após `ms` milissegundos e retorna imediatamente, então nada fica bloqueado enquanto isso.

A função passada para `new Promise` roda imediatamente, mas a promise permanece **pending** até que `resolve` seja chamado. Aguardá-la com `await` dá o valor:
```javascript
async function main() {
  console.log(await soon);
  // prints done, about one second later
}

main();
```

# --instructions--

Escreva uma função `delay` que recebe um número de milissegundos `ms` e um `value`, e retorna uma promise cumprida com `value` após `ms` milissegundos. Use `new Promise` e `setTimeout`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function delay(ms, value) {
  
}
```

# --asserts--

`delay(10, "ok")` deve retornar uma promise.

```javascript
(async () => {
  tryCatch(delay(10, "ok") instanceof Promise);
})().catch(() => tryCatch(false));
```

`await delay(0, 7)` deve ser `7`.

```javascript
(async () => {
  tryCatch((await delay(0, 7)) === 7);
})().catch(() => tryCatch(false));
```

`await delay(10, "ok")` deve ser `"ok"`.

```javascript
(async () => {
  tryCatch((await delay(10, "ok")) === "ok");
})().catch(() => tryCatch(false));
```

`await delay(60, "x")` deve levar pelo menos `50` milissegundos.

```javascript
(async () => {
  const start = Date.now();
  await delay(60, "x");
  tryCatch(Date.now() - start >= 50);
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function delay(ms, value) {
  return new Promise((resolve) => {
    setTimeout(() => resolve(value), ms);
  });
}
```
