---
language: javascript
exerciseType: 4
---

# --description--

Uma função `async` é lida de cima para baixo como qualquer outra função: `await` simplesmente a pausa até que a promise aguardada seja cumprida, então a execução continua na linha seguinte.
```javascript
async function main() {
  console.log("start");
  const value = await Promise.resolve("data");
  console.log(value);
  console.log("done");
}

main();
// prints start, then data, then done
```
Note a última linha: uma função `async` ainda precisa ser **chamada**. Escrever `main` sem os parênteses define o trabalho mas nunca o inicia, e nada é impresso.

# --instructions--

Ordene os itens em um programa que define uma função `async` `load` que retorna `data`, depois uma função `async` `main` que imprime `start`, aguarda `load()` em `value`, imprime `value` e por fim imprime `done`. Defina `load` primeiro, depois `main`, e chame `main()` na última linha

# --answers--

```javascript
async function load() {
  return "data";
}
```

```javascript
async function main() {
```

```javascript
  console.log("start");
```

```javascript
  const value = await load();
```

```javascript
  console.log(value);
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
async function load() {
  return "data";
}
async function main() {
  console.log("start");
  const value = await load();
  console.log(value);
  console.log("done");
}
main();
```

# --output--

start
data
done
