---
language: javascript
exerciseType: 2
---

# --description--

Já temos uma variável inicializada `total`
```javascript
var total = 5;
```
Digamos que queremos adicionar o número `2` à variável `total`, podemos escrever
```javascript
total = total + 2;
```
Ok, funciona! Mas existe uma versão mais curta para fazer a mesma coisa:
```javascript
total += 2;
```
O sinal `+=` é chamado de **atribuição com adição**.
Ele adiciona um valor ao valor da variável e atribui o resultado a essa variável.

# --instructions--

Adicione a variavel `num` o valor `15` usando o operador de *atribuicao com adicao*, depois imprima seu valor

# --seed--

```javascript
var num = 75;
[/][/][/];
console.log(num);
```

# --answers--

- +
- +=
- =+
- num 
-  15

# --solutions--

```javascript
var num = 75;
num += 15;
console.log(num);
```

# --output--

90
