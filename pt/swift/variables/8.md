---
language: swift
exerciseType: 1
---

# --description--

Em Swift, variaveis do tipo string so podem ser declaradas usando aspas duplas:
```swift
let x = "May"
```

# --instructions--

Declare uma variavel chamada `month` com o valor de `june`.

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`month` deve ser igual a `"june"`.

```swift
    func test1() {
        XCTAssertEqual(month, "june", "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
let month = "june"
```
