---
language: swift
exerciseType: 1
---

# --description--

Variaveis sao chamadas assim porque o valor que armazenam pode mudar.
Podemos atualizar `x` usando `=` e dando a ela um novo valor.
```swift
var x = 1
print(x) // imprime 1
x = 2
print(x) // imprime 2
```

# --instructions--

Atualize o valor da variavel `x` com o numero `10`.

# --seed--

```swift
var x = 5
// escreva apos esta linha
```

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`x` deve ser igual a `10`.

```swift
    func test1() {
        XCTAssertEqual(x, 10, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
var x = 5
x = 10
```
