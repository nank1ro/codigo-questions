---
language: swift
exerciseType: 1
---

# --description--

Nos tambem podemos dar as variaveis os valores de outras variaveis. Aqui, podemos dar a variavel `y` o valor de `x`
```swift
var x = 5
var y = x
print(y) // imprime 5
```

# --instructions--

Crie uma nova variavel chamada `y` e atribua a ela o valor da variavel `x`.

# --seed--

```swift
var x = 1
// escreva apos esta linha
```

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`y` deve ser igual a `x`.

```swift
    func test1() {
        XCTAssertEqual(x, y, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
var x = 1
var y = x
```
