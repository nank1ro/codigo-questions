---
language: swift
exerciseType: 2
---

# --description--

Uma `enumeração` define um tipo comum para um grupo de valores relacionados e permite que você trabalhe com esses valores de forma type-safe no seu código.
Declaramos enumerações usando a palavra-chave `enum`:
```swift
enum Colors {
    case blue
    case red
    case green
}
```
Os valores definidos em uma enumeração (como `blue`, `red` e `green`) são seus _casos de enumeração_.
Usamos a palavra-chave `case` para introduzir novos casos de enumeração.

# --instructions--

Declare uma enumeração válida

# --seed--

```swift
[/] Week {
    case monday
    case tuesday
    [/] wednesday
    case thursday
    case friday
    case [/]
    case sunday
}
```

# --answers--

- enum
- enumeration
- saturday
- "saturday"
- case
- "case"

# --solutions--

```swift
enum Week {
    case monday
    case tuesday
    case wednesday
    case thursday
    case friday
    case saturday
    case sunday
}
```
