---
language: swift
exerciseType: 4
---

# --description--

`Int`, `String`, arrays e structs são **value types**: um método não pode alterar o valor no qual é chamado, a menos que esteja marcado como `mutating`. Extensions também podem adicionar métodos `mutating`:
```swift
extension Int {
    mutating func increment() {
        self += 1
    }
}
var count = 1
count.increment()
print(count) // 2
```
Dentro de um método `mutating` você pode atribuir a `self`. O valor deve ser armazenado em um `var`: chamar `increment()` em uma constante `let` é um erro de compilação.

# --instructions--

Ordene os itens para definir primeiro uma extension de `Int` com um método `mutating` `double()`, depois declarar uma variável `n` com o valor `4`, duplicá-la e imprimi-la

# --answers--

```swift
extension Int {
```

```swift
    mutating func double() { self *= 2 }
```

```swift
}
```

```swift
var n = 4
```

```swift
n.double()
```

```swift
print(n)
```

# --solutions--

```swift
extension Int {
    mutating func double() { self *= 2 }
}
var n = 4
n.double()
print(n)
```

# --output--

8
