---
language: swift
exerciseType: 1
---

# --description--

Um método `mutating` pode receber parâmetros como qualquer outro método, e pode substituir `self` inteiramente em vez de atualizá-lo no lugar:
```swift
extension Int {
    mutating func reset(to value: Int) {
        self = value
    }
}
var score = 42
score.reset(to: 0)
print(score) // 0
```

# --instructions--

Escreva uma extension de `Int` com um método `mutating` `clamp(to max: Int)` que substitua o número por `max` quando o número for maior que `max`, e deixe-o inalterado caso contrário

Exemplo de chamada do método:
```swift
var speed = 15
speed.clamp(to: 10)
print(speed)
// prints 10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Você deve usar uma extension de `Int`

```json
{
  "regex": "extension\\s+Int",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

O método deve ser marcado como `mutating`

```json
{
  "regex": "mutating\\s+func\\s+clamp",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`15` limitado a `10` deve se tornar `10`

```swift
var _a = 15
_a.clamp(to: 10)
tryCatch(_a == 10)
```

`3` limitado a `10` deve permanecer `3`

```swift
var _b = 3
_b.clamp(to: 10)
tryCatch(_b == 3)
```

`10` limitado a `10` deve permanecer `10`

```swift
var _c = 10
_c.clamp(to: 10)
tryCatch(_c == 10)
```

`-5` limitado a `0` deve permanecer `-5`

```swift
var _d = -5
_d.clamp(to: 0)
tryCatch(_d == -5)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension Int {
    mutating func clamp(to max: Int) {
        if self > max {
            self = max
        }
    }
}
```
