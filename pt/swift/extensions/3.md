---
language: swift
exerciseType: 3
---

# --description--

Uma extension não cria um novo tipo e não copia o antigo: ela adiciona membros ao próprio tipo, então todo valor existente e futuro desse tipo os recebe.
É por isso que extensions são tão úteis com tipos que você não pode editar, como os da biblioteca padrão ou de um framework: você não pode abrir o arquivo onde `Int` é definido, mas pode estendê-lo a partir de qualquer arquivo do seu programa.
```swift
extension Int {
    func isDivisible(by other: Int) -> Bool {
        return self % other == 0
    }
}
let n = 12
print(n.isDivisible(by: 4)) // true
```

# --instructions--

Qual destas afirmações sobre extensions é verdadeira?

# --answers--

- Você pode adicionar um método a `Int` mesmo que não possa editar o código-fonte dele
- Uma extension cria um novo tipo que é uma cópia do original
- Um método adicionado em uma extension só pode ser chamado dentro dessa extension
- Uma extension deve ser escrita no mesmo arquivo onde o tipo é definido

# --solutions--

- Você pode adicionar um método a `Int` mesmo que não possa editar o código-fonte dele
