---
language: swift
exerciseType: 2
---

# --description--

Além de métodos, uma extension pode adicionar **propriedades computadas**: propriedades que não armazenam um valor, mas o calculam toda vez que são lidas.
Uma propriedade computada é declarada com `var`, uma anotação de tipo e um corpo entre chaves que retorna o valor:
```swift
extension Int {
    var isNegative: Bool {
        return self < 0
    }
}
print((-3).isNegative) // true
print(7.isNegative)    // false
```
Ela é lida como qualquer propriedade, sem parênteses: `7.isNegative`, e não `7.isNegative()`.

# --instructions--

Complete a extension para que `isEven` seja uma propriedade computada que indica se o número é par, de modo que o programa imprima `true` e `false`

# --seed--

```swift
extension Int {
    [/] isEven: [/] {
        return self % 2 == 0
    }
}
print(4.isEven)
print(7.isEven)
```

# --answers--

- var
- Bool
- let
- func
- Int

# --solutions--

```swift
extension Int {
    var isEven: Bool {
        return self % 2 == 0
    }
}
print(4.isEven)
print(7.isEven)
```

# --output--

true
false
