---
language: swift
exerciseType: 1
---

# --description--

Propriedades computadas em extensions também são uma escolha natural para `String`. O método `reversed()` retorna os caracteres em ordem inversa, e `String(...)` os transforma de volta em uma string:
```swift
extension String {
    var backwards: String {
        return String(reversed())
    }
}
print("swift".backwards) // tfiws
```
Assim como nos métodos, `reversed()` dentro da extension significa `self.reversed()`.

# --instructions--

Escreva uma extension de `String` com uma propriedade computada `isPalindrome` do tipo `Bool`, que seja `true` quando a string lê-se da mesma forma de frente e de trás. A comparação diferencia maiúsculas de minúsculas

Exemplo de uso:
```swift
print("level".isPalindrome)
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Você deve usar uma extension de `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`"level".isPalindrome` deve ser `true`

```swift
tryCatch("level".isPalindrome == true)
```

`"swift".isPalindrome` deve ser `false`

```swift
tryCatch("swift".isPalindrome == false)
```

`"Anna".isPalindrome` deve ser `false` (a comparação diferencia maiúsculas de minúsculas)

```swift
tryCatch("Anna".isPalindrome == false)
```

`"".isPalindrome` deve ser `true`

```swift
tryCatch("".isPalindrome == true)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    var isPalindrome: Bool {
        return self == String(reversed())
    }
}
```
