---
language: swift
exerciseType: 1
---

# --description--

`Array` é um tipo genérico: `[Int]` e `[String]` são ambos arrays, com um tipo **`Element`** diferente. Uma extension de `Array` se aplica a todos eles, o que é um problema quando o novo membro só faz sentido para alguns elementos: você não pode somar números que são strings.
Uma cláusula `where` restringe a extension aos arrays cujo `Element` é de um tipo dado:
```swift
extension Array where Element == Int {
    var largest: Int {
        var result = Int.min
        for number in self {
            if number > result {
                result = number
            }
        }
        return result
    }
}
print([3, 9, 2].largest) // 9
```
`[3, 9, 2].largest` funciona, enquanto `["a", "b"].largest` é um erro de compilação: a propriedade não existe em `[String]`.

# --instructions--

Escreva uma extension de `Array` restrita a `Element == Int` com um método `sum()` que retorne a soma dos elementos. Não use `reduce`

Exemplo de chamada do método:
```swift
print([1, 2, 3].sum())
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Você deve usar uma extension de `Array` com uma cláusula `where Element == Int`

```json
{
  "regex": "extension\\s+Array\\s+where\\s+Element\\s*==\\s*Int",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você não deve usar `reduce`

```json
{
  "regex": "reduce",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`[1, 2, 3].sum()` deve retornar `6`

```swift
tryCatch([1, 2, 3].sum() == 6)
```

`[10, -4].sum()` deve retornar `6`

```swift
tryCatch([10, -4].sum() == 6)
```

`[7].sum()` deve retornar `7`

```swift
tryCatch([7].sum() == 7)
```

A soma de um array vazio deve ser `0`

```swift
let _empty: [Int] = []
tryCatch(_empty.sum() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension Array where Element == Int {
    func sum() -> Int {
        var total = 0
        for number in self {
            total += number
        }
        return total
    }
}
```
