---
language: swift
exerciseType: 1
---

# --description--

Extensions não são apenas para tipos de outras pessoas. Uma forma comum de organizar o seu próprio código é manter as propriedades armazenadas no corpo da struct ou classe e adicionar o comportamento em uma ou mais extensions, cada uma agrupando membros relacionados:
```swift
struct Circle {
    var radius: Double
}
extension Circle {
    var diameter: Double {
        return radius * 2
    }
    func grown(by amount: Double) -> Circle {
        return Circle(radius: radius + amount)
    }
}
```
Os membros adicionados em uma extension podem usar as propriedades armazenadas diretamente, exatamente como se tivessem sido escritos dentro do tipo.

# --instructions--

A struct `Rectangle` é dada. Escreva uma extension de `Rectangle` com:
- uma propriedade computada `area` do tipo `Double`, o produto de `width` e `height`
- um método `scaled(by factor: Double)` que retorne um novo `Rectangle` com ambos os lados multiplicados por `factor`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Rectangle {
    var width: Double
    var height: Double
}
```

# --asserts--

Você deve usar uma extension de `Rectangle`

```json
{
  "regex": "extension\\s+Rectangle",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

A área de um retângulo de `2` por `3` deve ser `6.0`

```swift
tryCatch(Rectangle(width: 2, height: 3).area == 6.0)
```

A área de um retângulo de `1.5` por `4` deve ser `6.0`

```swift
tryCatch(Rectangle(width: 1.5, height: 4).area == 6.0)
```

Um retângulo de `2` por `3` escalado por `2` deve ser `4` por `6`

```swift
let _r = Rectangle(width: 2, height: 3).scaled(by: 2)
tryCatch(_r.width == 4.0 && _r.height == 6.0)
```

Escalar não deve alterar o retângulo original

```swift
let _o = Rectangle(width: 2, height: 3)
let _s = _o.scaled(by: 0.5)
tryCatch(_o.width == 2.0 && _o.height == 3.0 && _s.area == 1.5)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Rectangle {
    var width: Double
    var height: Double
}
extension Rectangle {
    var area: Double {
        return width * height
    }
    func scaled(by factor: Double) -> Rectangle {
        return Rectangle(width: width * factor, height: height * factor)
    }
}
```
