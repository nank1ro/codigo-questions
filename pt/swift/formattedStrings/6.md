---
language: swift
exerciseType: 1
---

# --description--

Outra flag é `0`: em vez de espaços, o campo é preenchido com zeros à esquerda. É assim que você obtém números como `007` ou `00042`:
```swift
print(String(format: "%05d", 42))  // 00042
print(String(format: "%03d", 7))   // 007
print(String(format: "%03d", 1234)) // 1234
```
Como com os espaços, um valor mais longo que a largura nunca é cortado.

# --instructions--

Escreva uma função `ticket` que recebe um inteiro sem rótulo de argumento e retorna `T-` seguido do número preenchido com zeros até a largura `5`, usando `String(format:)`

Exemplo de chamada da função:
```swift
print(ticket(42))
// prints T-00042
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func ticket() {

}
```

# --asserts--

A função deve usar `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ticket(42)` deve retornar `"T-00042"`

```swift
tryCatch(ticket(42) == "T-00042")
```

`ticket(7)` deve retornar `"T-00007"`

```swift
tryCatch(ticket(7) == "T-00007")
```

`ticket(12345)` deve retornar `"T-12345"`

```swift
tryCatch(ticket(12345) == "T-12345")
```

`ticket(123456)` deve retornar `"T-123456"`

```swift
tryCatch(ticket(123456) == "T-123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func ticket(_ n: Int) -> String {
    return String(format: "T-%05d", n)
}
```
