---
language: swift
exerciseType: 1
---

# --description--

Um número entre `%` e a letra define a **largura mínima** do campo. Se o valor for mais curto, espaços são adicionados à esquerda para que fique **alinhado à direita**; se for mais longo, nada é cortado:
```swift
print(String(format: "%5d|", 42))    //    42|
print(String(format: "%5d|", 12345)) // 12345|
```
Largura e precisão se combinam: `%8.2f` significa "pelo menos 8 caracteres de largura, com 2 casas decimais":
```swift
print(String(format: "%8.2f|", 3.14159)) //     3.14|
```
Larguras fixas são o que alinham as colunas de uma tabela.

# --instructions--

Escreva uma função `rightAligned` que recebe um inteiro sem rótulo de argumento e o retorna como uma string alinhada à direita em um campo de largura `5`, usando `String(format:)`

Exemplo de chamada da função:
```swift
print(rightAligned(42) + "|")
// prints    42|
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func rightAligned() {

}
```

# --asserts--

A função deve usar `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`rightAligned(42)` deve retornar `"   42"`

```swift
tryCatch(rightAligned(42) == "   42")
```

`rightAligned(7)` deve retornar `"    7"`

```swift
tryCatch(rightAligned(7) == "    7")
```

`rightAligned(12345)` deve retornar `"12345"`

```swift
tryCatch(rightAligned(12345) == "12345")
```

`rightAligned(123456)` deve retornar `"123456"` (nada é cortado)

```swift
tryCatch(rightAligned(123456) == "123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func rightAligned(_ n: Int) -> String {
    return String(format: "%5d", n)
}
```
