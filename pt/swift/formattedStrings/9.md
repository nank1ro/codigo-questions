---
language: swift
exerciseType: 1
---

# --description--

Para inserir uma `String` em uma string de formato, use o especificador `%@`:
```swift
let name = "Ada"
let age = 36
print(String(format: "%@ is %d years old", name, age)) // Ada is 36 years old
```
`%@` aceita uma `String` do Swift diretamente. Não use `%s` com uma string do Swift: esse especificador espera uma string C e imprime lixo ou faz o programa travar.

# --instructions--

Escreva uma função `greeting` que recebe um nome (`String`) e uma idade (`Int`), ambos sem rótulos de argumento, e retorna `<name> is <age> years old` usando `String(format:)` com `%@` e `%d`

Exemplo de chamada da função:
```swift
print(greeting("Ada", 36))
// prints Ada is 36 years old
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func greeting() {

}
```

# --asserts--

A função deve usar `String(format:` com `%@`

```json
{
  "regex": "String\\(format:[^\\n]*%@",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greeting("Ada", 36)` deve retornar `"Ada is 36 years old"`

```swift
tryCatch(greeting("Ada", 36) == "Ada is 36 years old")
```

`greeting("Linus", 54)` deve retornar `"Linus is 54 years old"`

```swift
tryCatch(greeting("Linus", 54) == "Linus is 54 years old")
```

`greeting("Bo", 7)` deve retornar `"Bo is 7 years old"`

```swift
tryCatch(greeting("Bo", 7) == "Bo is 7 years old")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greeting(_ name: String, _ age: Int) -> String {
    return String(format: "%@ is %d years old", name, age)
}
```
