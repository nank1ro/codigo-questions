---
language: swift
exerciseType: 1
---

# --description--

A primeira linha de um comentário de documentação é o **resumo**: uma frase curta que diz o que a função faz. Escreva-a na terceira pessoa, como se estivesse descrevendo a função: "Retorna...", "Soma...", "Verifica...".
```swift
/// Returns `true` when `n` is divisible by two.
func isEven(_ n: Int) -> Bool {
    return n % 2 == 0
}
```
O comentário deve ficar logo acima da declaração, sem linha em branco entre eles, caso contrário o Xcode não o vincula à função.

# --instructions--

Adicione um comentário de documentação `///` com um resumo de uma linha diretamente acima de `square`, por exemplo `/// Returns `n` multiplied by itself.`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func square(_ n: Int) -> Int {
    return n * n
}
```

# --asserts--

Um comentário `///` deve ser colocado diretamente acima de `func square`

```json
{
  "regex": "///\\s*\\S[^\\n]*\\n\\s*func square",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`square(3)` deve retornar `9`

```swift
tryCatch(square(3) == 9)
```

`square(-5)` deve retornar `25`

```swift
tryCatch(square(-5) == 25)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
/// Returns `n` multiplied by itself.
func square(_ n: Int) -> Int {
    return n * n
}
```
