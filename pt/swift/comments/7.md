---
language: swift
exerciseType: 1
---

# --description--

Para comentar várias linhas de uma vez, envolva-as em um único comentário em bloco em vez de adicionar `//` a cada linha:
```swift
var total = 100
/*
total = total - 30
total = total - 20
*/
print(total) // prints 100
```
Graças ao aninhamento, isso funciona mesmo quando uma dessas linhas já contém um comentário `/* */`.

# --instructions--

Comente as duas linhas que alteram `total` com um único comentário em bloco, para que `total` permaneça `100`. Mantenha o comentário interno `/* base */` como está

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
var total = 100
total = total /* base */ - 30
total = total - 20
```

# --asserts--

As duas linhas devem estar envolvidas por um comentário em bloco que ainda contenha `/* base */`

```json
{
  "regex": "/\\*[\\s\\S]*total = total /\\* base \\*/ - 30[\\s\\S]*total = total - 20[\\s\\S]*\\*/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`total` deve ser igual a `100`

```swift
tryCatch(total == 100)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
var total = 100
/*
total = total /* base */ - 30
total = total - 20
*/
```
