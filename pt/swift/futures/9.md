---
language: swift
exerciseType: 1
---

# --description--

`async let` está vinculado ao escopo em que é escrito. Para iniciar um trabalho concorrente e manter uma referência a ele, use uma **`Task`**. A closure passada para `Task { }` roda por conta própria, e a tarefa pode ser armazenada, passada adiante ou retornada:
```swift
let job = Task {
    return await double(21)
}
```
O resultado é lido mais tarde com **`.value`**, que é aguardado com `await`:
```swift
print(await job.value)
// prints 42
```
O tipo da referência diz o que ela produz e o que ela pode lançar: `Task<Int, Never>` é uma tarefa que retorna um `Int` e nunca lança erros. Ao contrário de `async let`, uma `Task` pode ser criada a partir de código comum, não assíncrono.

# --instructions--

Escreva uma função `startDoubling` que recebe um inteiro sem rótulo de argumento e retorna uma `Task<Int, Never>` que produz o dobro desse número

Exemplo de chamada da função:
```swift
print(await startDoubling(4).value)
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func startDoubling() {

}
```

# --asserts--

A função deve criar uma `Task`

```json
{
  "regex": "Task\\s*\\{",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`startDoubling(4).value` deve ser `8`

```swift
tryCatch(await startDoubling(4).value == 8)
```

`startDoubling(0).value` deve ser `0`

```swift
tryCatch(await startDoubling(0).value == 0)
```

`startDoubling(-6).value` deve ser `-12`

```swift
tryCatch(await startDoubling(-6).value == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func startDoubling(_ n: Int) -> Task<Int, Never> {
    return Task {
        return n * 2
    }
}
```
