---
language: swift
exerciseType: 1
---

# --description--

Uma função assíncrona ainda é uma função comum: ela pode receber parâmetros e retornar um valor de qualquer tipo. Apenas duas coisas mudam, a palavra-chave `async` na assinatura e o `await` em cada local de chamada:
```swift
func price(of quantity: Int) async -> Double {
    return Double(quantity) * 2.5
}

let total = await price(of: 4)
print(total)
// prints 10.0
```
O valor retornado é um `Double` normal, não um wrapper: depois que o `await` termina, você trabalha com ele exatamente como de costume.

# --instructions--

Escreva uma função `double` que recebe um inteiro sem rótulo de argumento, é assíncrona e retorna o dobro desse número

Exemplo de chamada da função:
```swift
print(await double(4))
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func double() {

}
```

# --asserts--

A função deve estar marcada como `async`

```json
{
  "regex": "func\\s+double\\s*\\([^)]*\\)\\s*async",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`double(4)` deve retornar `8`

```swift
tryCatch(await double(4) == 8)
```

`double(0)` deve retornar `0`

```swift
tryCatch(await double(0) == 0)
```

`double(-7)` deve retornar `-14`

```swift
tryCatch(await double(-7) == -14)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func double(_ n: Int) async -> Int {
    return n * 2
}
```
