---
language: swift
exerciseType: 4
---

# --description--

Várias chamadas `await` escritas uma após a outra rodam **sequencialmente**: a segunda chamada nem sequer começa até que a primeira tenha retornado. O código é lido de cima para baixo, exatamente como um código comum:
```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}

let a = await step("A")
print(a)
let b = await step("B")
print(b)
// start A
// done A
// start B
// done B
```
É isso que você quer quando a segunda chamada precisa do resultado da primeira. Quando as chamadas são independentes, esperar por uma antes de iniciar a outra é tempo desperdiçado, e os próximos exercícios mostram como evitar isso.

# --instructions--

Ordene as linhas para exibir a saída abaixo, definindo `step` antes do código de nível superior
```
start A
done A
start B
done B
```

# --answers--

```swift
func step(_ name: String) async -> String {
```

```swift
    print("start \(name)")
```

```swift
    return "done \(name)"
```

```swift
}
```

```swift
let a = await step("A")
```

```swift
print(a)
```

```swift
let b = await step("B")
```

```swift
print(b)
```

# --solutions--

```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}
let a = await step("A")
print(a)
let b = await step("B")
print(b)
```

# --output--

start A
done A
start B
done B
