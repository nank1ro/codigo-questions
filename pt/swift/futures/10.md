---
language: swift
exerciseType: 1
---

# --description--

`Task.sleep` pausa a tarefa atual por um tempo sem bloquear mais nada. Ela pode ser interrompida, então é uma chamada assíncrona que lança erros e precisa de `try await`. A duração é informada com auxiliares como `.seconds`, `.milliseconds` ou `.nanoseconds`:
```swift
func slowGreeting() async throws -> String {
    try await Task.sleep(for: .milliseconds(50))
    return "hello"
}
```
Essa é a maneira padrão de simular um trabalho lento em um exemplo, em vez de uma chamada de rede real. Note que ela não congela o programa: enquanto uma tarefa dorme, as outras continuam executando.

# --instructions--

Escreva uma função `delayed` que recebe um inteiro sem rótulo de argumento, é assíncrona, pode lançar erros, espera `50` milissegundos com `Task.sleep` e então retorna o número multiplicado por `3`

Exemplo de chamada da função:
```swift
print(try await delayed(2))
// imprime 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func delayed() {

}
```

# --asserts--

A função deve estar marcada como `async throws`

```json
{
  "regex": "func\\s+delayed\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

A função deve usar `Task.sleep`

```json
{
  "regex": "Task\\.sleep",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`delayed(2)` deve retornar `6`

```swift
tryCatch((try? await delayed(2)) == 6)
```

`delayed(0)` deve retornar `0`

```swift
tryCatch((try? await delayed(0)) == 0)
```

`delayed(-4)` deve retornar `-12`

```swift
tryCatch((try? await delayed(-4)) == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func delayed(_ n: Int) async throws -> Int {
    try await Task.sleep(for: .milliseconds(50))
    return n * 3
}
```
