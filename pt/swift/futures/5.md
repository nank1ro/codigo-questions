---
language: swift
exerciseType: 2
---

# --description--

Quando você não se importa com o *motivo* pelo qual a chamada falhou, `try?` é mais curto do que um bloco `do`. Ele transforma uma chamada que lança erros em um **opcional**: o valor em caso de sucesso, `nil` em caso de falha. Combinado com `await`, escreve-se `try? await`:
```swift
let value = try? await parse("42")  // Optional(42)
let broken = try? await parse("x")  // nil
```
Como o resultado é opcional, ele se encaixa diretamente em um `if let`:
```swift
if let value = try? await parse("x") {
    print(value)
} else {
    print("not a number")
}
```
Use `try? await` para um fallback rápido, e `do` / `catch` quando o erro em si importa.

# --instructions--

Complete o código para que a chamada que falha produza `nil` e o programa exiba `not a number`

# --seed--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = [/] await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --answers--

- try?
- try
- try!
- await
- throws

# --solutions--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = try? await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --output--

not a number
