---
language: swift
exerciseType: 1
---

# --description--

`async let` funciona quando você sabe quantas chamadas existem enquanto escreve o código. Para uma lista cujo tamanho só é conhecido em tempo de execução, use um **grupo de tarefas**.

`withTaskGroup(of:)` abre um grupo, `addTask` inicia uma tarefa filha por item, e o grupo é então lido com `for await`, que entrega os resultados à medida que terminam:
```swift
let total = await withTaskGroup(of: Int.self) { group in
    for n in numbers {
        group.addTask {
            return await square(n)
        }
    }
    var sum = 0
    for await value in group {
        sum += value
    }
    return sum
}
```
`of: Int.self` declara o que cada tarefa filha retorna. Toda a chamada `withTaskGroup` é uma única expressão, então ela precisa de um único `await` na frente, e ela não retorna até que cada tarefa filha tenha terminado.

# --instructions--

Escreva uma função `sumOfSquares` que recebe um array de inteiros sem rótulo de argumento, é assíncrona e retorna a soma de `square` aplicada a cada elemento, calculando-os com `withTaskGroup`

Exemplo de chamada da função:
```swift
print(await sumOfSquares([1, 2, 3]))
// imprime 14
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func square(_ n: Int) async -> Int {
    return n * n
}

func sumOfSquares() {

}
```

# --asserts--

A função deve usar `withTaskGroup`

```json
{
  "regex": "withTaskGroup",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`sumOfSquares([1, 2, 3])` deve retornar `14`

```swift
tryCatch(await sumOfSquares([1, 2, 3]) == 14)
```

`sumOfSquares([4])` deve retornar `16`

```swift
tryCatch(await sumOfSquares([4]) == 16)
```

`sumOfSquares([])` deve retornar `0`

```swift
tryCatch(await sumOfSquares([]) == 0)
```

`sumOfSquares([-2, 5])` deve retornar `29`

```swift
tryCatch(await sumOfSquares([-2, 5]) == 29)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func square(_ n: Int) async -> Int {
    return n * n
}

func sumOfSquares(_ numbers: [Int]) async -> Int {
    return await withTaskGroup(of: Int.self) { group in
        for n in numbers {
            group.addTask {
                return await square(n)
            }
        }
        var total = 0
        for await value in group {
            total += value
        }
        return total
    }
}
```
