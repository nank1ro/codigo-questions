---
language: swift
exerciseType: 1
---

# --description--

Um trabalho que acontece ao longo do tempo frequentemente falha: um servidor está fora do ar, um arquivo está ausente, a entrada está errada. Tal função é marcada como **`async throws`** e é chamada com **`try await`**:
```swift
enum LoadError: Error {
    case missing
}

func load(_ name: String) async throws -> String {
    if name.isEmpty {
        throw LoadError.missing
    }
    return "file: \(name)"
}
```
Para tratar o erro, você envolve a chamada em um bloco `do` e o captura:
```swift
do {
    let text = try await load("")
    print(text)
} catch {
    print("could not load")
}
// imprime could not load
```
A ordem das palavras-chave é fixa: `try` vem primeiro, depois `await`.

# --instructions--

Escreva uma função `parse` que recebe uma string sem rótulo de argumento, é assíncrona, pode lançar erros e retorna a string convertida em `Int`. Se a string não for um número, lance `InputError.notANumber`

Exemplo de chamada da função:
```swift
print(try await parse("7"))
// imprime 7
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum InputError: Error {
    case notANumber
}

func parse() {

}
```

# --asserts--

A função deve estar marcada como `async throws`

```json
{
  "regex": "func\\s+parse\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse("7")` deve retornar `7`

```swift
do {
    let value = try await parse("7")
    tryCatch(value == 7)
} catch {
    tryCatch(false)
}
```

`parse("-3")` deve retornar `-3`

```swift
do {
    let value = try await parse("-3")
    tryCatch(value == -3)
} catch {
    tryCatch(false)
}
```

`parse("abc")` deve lançar erro

```swift
do {
    _ = try await parse("abc")
    tryCatch(false)
} catch {
    tryCatch(true)
}
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}
```
