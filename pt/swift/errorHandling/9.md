---
language: swift
exerciseType: 1
---

# --description--

Como o `try?` produz um opcional, o operador de coalescência nula `??` termina o trabalho fornecendo um fallback:
```swift
let port = (try? readPort(text)) ?? 8080
```
Os parênteses importam. Sem eles, o `try?` tentaria cobrir a expressão inteira incluindo o `??`, e o compilador pede que você seja explícito sobre onde a chamada que falha termina.

Leia a linha como uma frase: *use a porta que conseguimos ler, caso contrário 8080*. Duas linhas de `do`/`catch` viram uma quando a recuperação realmente é apenas um valor padrão.

# --instructions--

Complete `portOrDefault` para que retorne a porta lida de `text`, ou `8080` quando `readPort` lançar um erro

Exemplo de chamada da função:
```swift
print(portOrDefault("abc"))
// imprime 8080
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum ConfigError: Error {
    case notANumber
}

func readPort(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ConfigError.notANumber
    }
    return value
}

func portOrDefault(_ text: String) -> Int {
    
}
```

# --asserts--

`portOrDefault("3000")` deve retornar `3000`

```swift
tryCatch(portOrDefault("3000") == 3000)
```

`portOrDefault("80")` deve retornar `80`

```swift
tryCatch(portOrDefault("80") == 80)
```

`portOrDefault("abc")` deve usar o fallback `8080`

```swift
tryCatch(portOrDefault("abc") == 8080)
```

`portOrDefault("")` deve usar o fallback `8080`

```swift
tryCatch(portOrDefault("") == 8080)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum ConfigError: Error {
    case notANumber
}

func readPort(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ConfigError.notANumber
    }
    return value
}

func portOrDefault(_ text: String) -> Int {
    return (try? readPort(text)) ?? 8080
}
```
