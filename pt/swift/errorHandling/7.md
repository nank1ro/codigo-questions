---
language: swift
exerciseType: 1
---

# --description--

Um validador fica mais legível quando as rejeições vêm primeiro e o trabalho real fica sem indentação no final. O `guard` é feito para isso: ele declara a condição que deve valer, e seu bloco `else` executa quando ela não vale.
```swift
func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
O bloco `else` de um `guard` precisa sair do escopo atual, e `throw` é uma das maneiras de fazer isso, ao lado de `return`, `break` e `continue`. Vários `guard`s empilhados no topo de uma função parecem uma lista das regras que a entrada precisa satisfazer.

# --instructions--

Complete `priceFor` com um `guard` que lance `OrderError.notPositive` a menos que `quantity` seja maior que `0`

Exemplo de chamada da função:
```swift
do {
    print(try priceFor(4))
} catch {
    print("invalid")
}
// imprime 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    
    return quantity * 3
}
```

# --before-asserts--

```swift
func _price(_ quantity: Int) -> Int {
    do {
        return try priceFor(quantity)
    } catch OrderError.notPositive {
        return -1
    } catch {
        return -2
    }
}
```

# --asserts--

`priceFor(4)` deve retornar `12`

```swift
tryCatch(_price(4) == 12)
```

`priceFor(1)` deve retornar `3`

```swift
tryCatch(_price(1) == 3)
```

`priceFor(0)` deve lançar `notPositive`

```swift
tryCatch(_price(0) == -1)
```

`priceFor(-5)` deve lançar `notPositive`

```swift
tryCatch(_price(-5) == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
