---
language: swift
exerciseType: 1
---

# --instructions--

Escreva um validador usando tudo deste capítulo:
- dê a `FormError` um segundo caso `tooLong` carregando um `maximum` do tipo `Int`
- `validate` lança `FormError.empty` quando o nome está vazio, lança `FormError.tooLong(maximum: 10)` quando ele tem mais de 10 caracteres e, caso contrário, retorna o nome
- `safeName` retorna o nome validado, ou `"guest"` quando `validate` lança um erro

Exemplo de chamada da função:
```swift
print(safeName(""))
// imprime guest
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum FormError: Error {
    case empty
}

func validate(_ name: String) throws -> String {
    
}

func safeName(_ name: String) -> String {
    
}
```

# --before-asserts--

```swift
func _kind(_ name: String) -> String {
    do {
        return try validate(name)
    } catch FormError.empty {
        return "empty"
    } catch FormError.tooLong(let maximum) {
        return "max \(maximum)"
    } catch {
        return "other"
    }
}
```

# --asserts--

`validate("Ada")` deve retornar `Ada`

```swift
tryCatch(_kind("Ada") == "Ada")
```

`validate("")` deve lançar `empty`

```swift
tryCatch(_kind("") == "empty")
```

`validate("12345678901")` deve lançar `tooLong` carregando `10`

```swift
tryCatch(_kind("12345678901") == "max 10")
```

um nome de exatamente 10 caracteres deve ser aceito

```swift
tryCatch(_kind("1234567890") == "1234567890")
```

`safeName("")` deve retornar `guest`

```swift
tryCatch(safeName("") == "guest")
```

`safeName("Ada")` deve retornar `Ada`

```swift
tryCatch(safeName("Ada") == "Ada")
```

`safeName("12345678901")` deve retornar `guest`

```swift
tryCatch(safeName("12345678901") == "guest")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum FormError: Error {
    case empty
    case tooLong(maximum: Int)
}

func validate(_ name: String) throws -> String {
    guard !name.isEmpty else {
        throw FormError.empty
    }
    guard name.count <= 10 else {
        throw FormError.tooLong(maximum: 10)
    }
    return name
}

func safeName(_ name: String) -> String {
    return (try? validate(name)) ?? "guest"
}
```
