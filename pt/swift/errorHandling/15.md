---
language: swift
exerciseType: 4
---

# --description--

Onde o bloco `do` fica decide quanto custa uma única falha. Coloque-o **dentro** do loop e cada item recebe sua própria tentativa, então um valor ruim é pulado e o resto continua rodando:
```swift
for age in [4, -1, 7] {
    do {
        print(try label(age))
    } catch {
        print("skipped")
    }
}
```
Envolver o loop inteiro em um único `do`, em vez disso, pararia no primeiro erro e nunca chegaria a `7`. Nenhuma das opções está errada — é a diferença entre *um item ruim* e *desistir*.

# --instructions--

Ordene os itens para imprimir as linhas abaixo, declarando o tipo de erro primeiro e a função antes do loop
```
age 4
skipped
age 7
```

# --answers--

```swift
enum AgeError: Error {
    case negative
}
```

```swift
func label(_ age: Int) throws -> String {
    if age < 0 {
        throw AgeError.negative
    }
    return "age \(age)"
}
```

```swift
for age in [4, -1, 7] {
    do {
```

```swift
        print(try label(age))
```

```swift
    } catch {
        print("skipped")
    }
}
```

# --solutions--

```swift
enum AgeError: Error {
    case negative
}
func label(_ age: Int) throws -> String {
    if age < 0 {
        throw AgeError.negative
    }
    return "age \(age)"
}
for age in [4, -1, 7] {
    do {
        print(try label(age))
    } catch {
        print("skipped")
    }
}
```

# --output--

age 4
skipped
age 7
