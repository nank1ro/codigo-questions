---
language: swift
exerciseType: 1
---

# --description--

Uma função que lança erros ainda pode retornar um valor. A palavra-chave `throws` fica entre a lista de parâmetros e a seta de retorno:
```swift
func square(_ n: Int) throws -> Int {
    if n < 0 {
        throw SquareError.negative
    }
    return n * n
}
```
Lendo em voz alta: *square recebe um `Int`, pode lançar um erro e retorna um `Int`*.

No local da chamada o valor só existe quando nada foi lançado, então a atribuição fica dentro do bloco `do`:
```swift
do {
    let result = try square(4)
    print(result) // 16
} catch {
    print("failed")
}
```
O `try` não é um enfeite opcional: o compilador rejeita a chamada sem ele, então um leitor sempre vê quais linhas podem falhar.

# --instructions--

Marque `half` como capaz de lançar erros, faça-a lançar `MathError.notEven` quando `n` for ímpar e, caso contrário, retorne `n / 2`

Exemplo de chamada da função:
```swift
do {
    print(try half(10))
} catch {
    print("odd")
}
// imprime 5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum MathError: Error {
    case notEven
}

func half(_ n: Int) -> Int {
    
}
```

# --before-asserts--

```swift
func _half(_ n: Int) -> Int {
    do {
        return try half(n)
    } catch {
        return -999
    }
}
```

# --asserts--

`half(10)` deve retornar `5`

```swift
tryCatch(_half(10) == 5)
```

`half(0)` deve retornar `0`

```swift
tryCatch(_half(0) == 0)
```

`half(7)` deve lançar um erro

```swift
tryCatch(_half(7) == -999)
```

`half(-4)` deve retornar `-2`

```swift
tryCatch(_half(-4) == -2)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum MathError: Error {
    case notEven
}

func half(_ n: Int) throws -> Int {
    if n % 2 != 0 {
        throw MathError.notEven
    }
    return n / 2
}
```
