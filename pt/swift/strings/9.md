---
language: swift
exerciseType: 1
---

# --description--

Trabalhar com índices é verboso, então o Swift oferece atalhos para os casos mais comuns:
- `first` e `last` retornam o primeiro e o último caractere como um `Character?` opcional (`nil` para uma string vazia)
- `prefix(n)` retorna os primeiros `n` caracteres e `suffix(n)` os últimos `n`
```swift
let word = "Swift"
print(word.first!)     // S
print(word.prefix(2))  // Sw
print(word.suffix(3))  // ift
```
`prefix` e `suffix` retornam uma `Substring`, uma visão sobre o texto original. Para armazená-la como uma `String` de verdade, envolva-a em `String(...)`. Se `n` for maior que `count`, você simplesmente obtém a string inteira.

# --instructions--

Escreva uma função `firstTwo` que recebe uma `String` (omita o rótulo do argumento) e retorna seus dois primeiros caracteres como uma `String`, e uma função `lastTwo` que retorna seus dois últimos caracteres como uma `String`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`firstTwo("Swift")` deve retornar `"Sw"`

```swift
tryCatch(firstTwo("Swift") == "Sw")
```

`firstTwo("a")` deve retornar `"a"`

```swift
tryCatch(firstTwo("a") == "a")
```

`lastTwo("Swift")` deve retornar `"ft"`

```swift
tryCatch(lastTwo("Swift") == "ft")
```

`lastTwo("")` deve retornar `""`

```swift
tryCatch(lastTwo("") == "")
```

`firstTwo` deve retornar uma `String`

```swift
tryCatch(type(of: firstTwo("Swift")) == String.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstTwo(_ text: String) -> String {
    return String(text.prefix(2))
}

func lastTwo(_ text: String) -> String {
    return String(text.suffix(2))
}
```
