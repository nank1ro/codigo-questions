---
language: swift
exerciseType: 1
---

# --description--

A **interpolação de strings** insere o valor de uma expressão dentro de um literal de string. Envolva a expressão em `\()`:
```swift
let name = "Ada"
let age = 36
print("\(name) is \(age) years old") // Ada is 36 years old
```
Qualquer tipo pode ser interpolado: números, booleanos e outras strings são todos convertidos em texto automaticamente.

# --instructions--

Escreva uma função `greet` que recebe uma `String` (omita o rótulo do argumento) e retorna o texto `Hello, <name>!`, onde `<name>` é o argumento.

Exemplo de chamada da função:
```swift
print(greet("Ada"))
// prints Hello, Ada!
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`greet("Ada")` deve retornar `"Hello, Ada!"`

```swift
tryCatch(greet("Ada") == "Hello, Ada!")
```

`greet("Swift")` deve retornar `"Hello, Swift!"`

```swift
tryCatch(greet("Swift") == "Hello, Swift!")
```

`greet("")` deve retornar `"Hello, !"`

```swift
tryCatch(greet("") == "Hello, !")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greet(_ name: String) -> String {
    return "Hello, \(name)!"
}
```
