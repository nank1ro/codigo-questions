---
language: swift
exerciseType: 1
---

# --description--

Uma `String` é uma coleção de valores `Character`. Um `Character` é uma única letra, dígito, símbolo ou espaço, e é escrito com as mesmas aspas duplas de uma string, então você precisa de uma anotação de tipo para obter um:
```swift
let letter: Character = "a"
let text = "abc"
print(text.count) // 3
```
Verificar `isEmpty` é preferível a comparar `count` com `0`: fica mais claro e não precisa contar todos os caracteres.

# --instructions--

Escreva uma função `length` que recebe uma `String` (omita o rótulo do argumento) e retorna seu número de caracteres, e uma função `hasText` que recebe uma `String` (omita o rótulo do argumento) e retorna `true` somente quando a string não está vazia

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`length("Swift")` deve retornar `5`

```swift
tryCatch(length("Swift") == 5)
```

`length("a b c")` deve retornar `5`

```swift
tryCatch(length("a b c") == 5)
```

`length("")` deve retornar `0`

```swift
tryCatch(length("") == 0)
```

`hasText("x")` deve retornar `true`

```swift
tryCatch(hasText("x") == true)
```

`hasText("")` deve retornar `false`

```swift
tryCatch(hasText("") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func length(_ text: String) -> Int {
    return text.count
}

func hasText(_ text: String) -> Bool {
    return !text.isEmpty
}
```
