---
language: swift
exerciseType: 2
---

# --description--

Duas strings podem ser unidas com o operador `+`, que produz uma nova string:
```swift
let full = "Hello" + " " + "world" // Hello world
```
Para adicionar texto ao final de uma variável de string existente, use `+=`. A variável deve ser declarada com `var`, porque seu valor muda:
```swift
var log = "Start"
log += "..."
print(log) // Start...
```

# --instructions--

Declare uma variável `message` contendo `Hello`, acrescente `, world` a ela com o operador de atribuição composta, depois imprima-a

# --seed--

```swift
[/] message = "Hello"
message [/] ", world"
print(message)
```

# --answers--

- var
- let
- +=
- +
- =
- -=

# --solutions--

```swift
var message = "Hello"
message += ", world"
print(message)
```

# --output--

Hello, world
