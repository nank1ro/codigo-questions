---
language: swift
exerciseType: 2
---

# --description--

**Dicionários** são similares a arrays e tuplas, mas você acessa valores procurando uma *chave* em vez de um índice
Uma chave pode ser qualquer string ou número.
Dicionários são delimitados por colchetes, assim:
```swift
var dictionaryName: [String: Int] = ["key1": 1, "key2": 2, "key3": 3]
```
Este é um dicionário chamado `dictionaryName` com três *pares chave-valor*.
A chave `key1` aponta para o valor `1`, `key2` para `2`, e assim por diante.

# --instructions--

Complete o código para criar um dicionário válido

# --seed--

```swift
var user: [String: [/]] = [/]"first_name": "John", "last_name": "Hood", "age": 30[/]
if let firstName = user[[/]] {
    print("The first name is \(firstName)")
}
```

# --answers--

- Any
- String
- Int
- [
- {
- }
- ]
- "first_name"
- first_name

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let firstName = user["first_name"] {
    print("The first name is \(firstName)")
}
```

# --output--

The first name is John
