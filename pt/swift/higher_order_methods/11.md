---
language: swift
exerciseType: 2
---

# --description--

`zip` emparelha os elementos de duas sequências posição por posição, produzindo uma sequência de tuplas. Ela para no fim da mais curta:
```swift
let names = ["Ann", "Bob"]
let ages = [31, 27, 99]
let pairs = zip(names, ages).map { "\($0) is \($1)" }
print(pairs) // ["Ann is 31", "Bob is 27"]
```
Dentro da closure `$0` é o elemento da primeira sequência e `$1` o da segunda. `zip` é uma função livre, não um método: você escreve `zip(a, b)`, não `a.zip(b)`.

# --instructions--

Complete o código para emparelhar cada cidade com sua temperatura e manter apenas as cidades mais quentes que `20` graus, de forma que o programa imprima `["Rome", "Cairo"]`

# --seed--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = [/](cities, temps).[/] { $1 > 20 }.map { $0.0 }
print(warm)
```

# --answers--

- zip
- filter
- map
- enumerated
- compactMap

# --solutions--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = zip(cities, temps).filter { $1 > 20 }.map { $0.0 }
print(warm)
```

# --output--

["Rome", "Cairo"]
