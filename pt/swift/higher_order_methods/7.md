---
language: swift
exerciseType: 1
---

# --description--

Funções de ordem superior funcionam em qualquer array, incluindo arrays de suas próprias structs. Encadear `filter` e depois `map` é a forma usual de selecionar alguns elementos e extrair um valor de cada um:
```swift
struct Book {
    var title: String
    var pages: Int
}
let books = [Book(title: "Dune", pages: 412), Book(title: "Haiku", pages: 40)]
let long = books.filter { $0.pages > 100 }.map { $0.title }
print(long) // ["Dune"]
```
Fazer na ordem oposta, `map` e depois `filter`, perderia a propriedade `pages` antes que a verificação pudesse usá-la.

# --instructions--

A struct `Product` é fornecida. Escreva uma função `cheapNames` que recebe um array de `Product` (omita o rótulo do argumento) e retorna os nomes dos produtos cujo `price` está abaixo de `10`, na ordem original, encadeando `filter` e `map`

Exemplo de chamada da função:
```swift
print(cheapNames([Product(name: "Pen", price: 2.5), Product(name: "Bag", price: 30)]))
// prints ["Pen"]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Product {
    var name: String
    var price: Double
}
```

# --asserts--

Você deve usar `filter`

```json
{
  "regex": "\\.filter",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você deve usar `map`

```json
{
  "regex": "\\.map",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`cheapNames` deve retornar `["Pen"]` para uma caneta a `2.5` e uma bolsa a `30`

```swift
tryCatch(cheapNames([Product(name: "Pen", price: 2.5), Product(name: "Bag", price: 30)]) == ["Pen"])
```

`cheapNames` deve manter a ordem original

```swift
tryCatch(cheapNames([Product(name: "Tea", price: 4), Product(name: "Cup", price: 9.99), Product(name: "Pot", price: 25)]) == ["Tea", "Cup"])
```

Um produto com preço exatamente `10` deve ser excluído

```swift
tryCatch(cheapNames([Product(name: "Cap", price: 10)]) == [])
```

`cheapNames([])` deve retornar `[]`

```swift
tryCatch(cheapNames([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Product {
    var name: String
    var price: Double
}
func cheapNames(_ products: [Product]) -> [String] {
    return products.filter { $0.price < 10 }.map { $0.name }
}
```
