---
language: swift
exerciseType: 1
---

# --description--

Algumas funções de ordem superior respondem a uma pergunta sobre a coleção em vez de transformá-la. Todas elas recebem uma closure que retorna um `Bool`:
- `first(where:)` retorna o primeiro elemento que satisfaz a closure, ou `nil` se não houver nenhum
- `contains(where:)` retorna `true` se pelo menos um elemento a satisfaz
- `allSatisfy` retorna `true` se todos os elementos a satisfazem

```swift
let nums = [4, 9, 16]
print(nums.first(where: { $0 > 5 }))  // Optional(9)
print(nums.contains(where: { $0 > 5 })) // true
print(nums.allSatisfy { $0 > 5 })       // false
```
Ao contrário de `filter`, `first(where:)` para na primeira correspondência e não constrói um novo array.

# --instructions--

Escreva uma função `firstLong` que recebe um array de `String` e um `Int` chamado `minLength` (omita os dois rótulos de argumento) e retorna a primeira string com pelo menos `minLength` caracteres, ou `nil` se não houver nenhuma, usando `first(where:)`

Exemplo de chamada da função:
```swift
print(firstLong(["hi", "hello", "hey"], 4))
// prints Optional("hello")
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Você deve usar `first(where:)`

```json
{
  "regex": "\\.first\\s*\\(\\s*where",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstLong(["hi", "hello", "hey"], 4)` deve retornar `"hello"`

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 4) == "hello")
```

`firstLong(["hi", "hello", "hey"], 2)` deve retornar `"hi"`

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 2) == "hi")
```

`firstLong(["hi", "hey"], 4)` deve retornar `nil`

```swift
tryCatch(firstLong(["hi", "hey"], 4) == nil)
```

`firstLong([], 1)` deve retornar `nil`

```swift
tryCatch(firstLong([], 1) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstLong(_ words: [String], _ minLength: Int) -> String? {
    return words.first(where: { $0.count >= minLength })
}
```
