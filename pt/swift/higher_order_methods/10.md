---
language: swift
exerciseType: 1
---

# --description--

`enumerated()` transforma um array em uma sequência de pares `(offset, element)`, de modo que uma closure possa usar a posição de cada elemento junto com seu valor:
```swift
let steps = ["mix", "bake"]
let numbered = steps.enumerated().map { pair in
    "\(pair.offset + 1). \(pair.element)"
}
print(numbered) // ["1. mix", "2. bake"]
```
Como cada par é uma tupla, a closure também pode desconstruí-lo: `.map { (i, step) in "\(i + 1). \(step)" }`.

# --instructions--

Escreva uma função `evenPositions` que recebe um array de `String` (omita o rótulo do argumento) e retorna os elementos encontrados nos índices pares (0, 2, 4, ...), usando `enumerated()`, `filter` e `map`

Exemplo de chamada da função:
```swift
print(evenPositions(["a", "b", "c", "d", "e"]))
// prints ["a", "c", "e"]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Você deve usar `enumerated()`

```json
{
  "regex": "\\.enumerated\\s*\\(\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você não deve usar um loop `for`

```json
{
  "regex": "\\bfor\\b",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`evenPositions(["a", "b", "c", "d", "e"])` deve retornar `["a", "c", "e"]`

```swift
tryCatch(evenPositions(["a", "b", "c", "d", "e"]) == ["a", "c", "e"])
```

`evenPositions(["x", "y"])` deve retornar `["x"]`

```swift
tryCatch(evenPositions(["x", "y"]) == ["x"])
```

`evenPositions([])` deve retornar `[]`

```swift
tryCatch(evenPositions([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func evenPositions(_ items: [String]) -> [String] {
    return items.enumerated().filter { $0.offset % 2 == 0 }.map { $0.element }
}
```
