---
language: swift
exerciseType: 1
---

# --description--

`map` e `filter` em um array são **eager**: cada um processa o array inteiro e constrói um novo antes que o próximo passo seja executado. Em uma coleção grande, ou quando você só precisa do primeiro resultado, isso é trabalho desperdiçado.
A propriedade `lazy` retorna uma visão cujas operações rodam apenas quando um elemento é realmente solicitado, um elemento por vez através de toda a cadeia:
```swift
let firstBig = (1...1000).lazy.map { $0 * $0 }.first { $0 > 50 }
print(firstBig!) // 64
```
Aqui apenas `1, 2, ..., 8` são elevados ao quadrado: `first(where:)` pede elementos até que um satisfaça a condição, e a cadeia para aí. Sem `lazy`, `map` elevaria todos os 1000 números ao quadrado primeiro.

# --instructions--

Escreva uma função `firstCubeAbove` que recebe um `Int` chamado `limit` (omita o rótulo do argumento) e retorna o menor cubo (`n * n * n` para `n` começando em `1`) maior que `limit`, usando `lazy` e `map` no intervalo `1...limit + 1` e `first(where:)`

Exemplo de chamada da função:
```swift
print(firstCubeAbove(10))
// prints 27
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Você deve usar `lazy`

```json
{
  "regex": "\\.lazy",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Você não deve usar um loop

```json
{
  "regex": "\\b(for|while)\\b",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`firstCubeAbove(10)` deve retornar `27`

```swift
tryCatch(firstCubeAbove(10) == 27)
```

`firstCubeAbove(27)` deve retornar `64`

```swift
tryCatch(firstCubeAbove(27) == 64)
```

`firstCubeAbove(0)` deve retornar `1`

```swift
tryCatch(firstCubeAbove(0) == 1)
```

`firstCubeAbove(1000000)` deve retornar `1030301`

```swift
tryCatch(firstCubeAbove(1000000) == 1030301)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstCubeAbove(_ limit: Int) -> Int {
    return (1...limit + 1).lazy.map { $0 * $0 * $0 }.first(where: { $0 > limit })!
}
```
