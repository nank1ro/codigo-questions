---
language: swift
exerciseType: 4
---

# --description--

Vincular vários opcionais em um único `if let` mantém o código plano: um único bloco `else` cobre todos os valores ausentes.

# --instructions--

Ordene os itens para imprimir `Area: 20` quando tanto `width` quanto `height` tiverem um valor, e `Missing size` caso contrário.
Declare `width` antes de `height`

# --answers--

```swift
let width: Int? = 4
```

```swift
let height: Int? = 5
```

```swift
if let w = width, let h = height {
```

```swift
    print("Area: \(w * h)")
```

```swift
} else {
    print("Missing size")
}
```

# --solutions--

```swift
let width: Int? = 4
let height: Int? = 5
if let w = width, let h = height {
    print("Area: \(w * h)")
} else {
    print("Missing size")
}
```

# --output--

Area: 20
