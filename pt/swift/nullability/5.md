---
language: swift
exerciseType: 1
---

# --description--

Verificar se é `nil` e depois fazer o desempacotamento forçado é verboso. O Swift oferece a **vinculação opcional** com `if let`, que desempacota o opcional e armazena o valor em uma nova constante em uma única etapa:
```swift
let score: Int? = 10
if let value = score {
    print("Score: \(value)") // value é um Int, não um Int?
} else {
    print("No score")
}
```
O corpo do `if` só executa quando o opcional contém um valor; dentro dele `value` é um `Int` comum e não precisa de `!`.

# --instructions--

Escreva uma função `describe` que recebe um `Int?` (omita o rótulo do argumento) e retorna `"Value: "` seguido do número (por exemplo, `"Value: 5"` para `5`) quando o opcional contém um valor, ou `"No value"` quando ele é `nil`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`describe(5)` deve retornar `"Value: 5"`

```swift
tryCatch(describe(5) == "Value: 5")
```

`describe(42)` deve retornar `"Value: 42"`

```swift
tryCatch(describe(42) == "Value: 42")
```

`describe(nil)` deve retornar `"No value"`

```swift
tryCatch(describe(nil) == "No value")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func describe(_ number: Int?) -> String {
    if let value = number {
        return "Value: \(value)"
    } else {
        return "No value"
    }
}
```
