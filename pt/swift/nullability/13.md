---
language: swift
exerciseType: 1
---

# --description--

Um único `if let` ou `guard let` pode desempacotar vários opcionais de uma vez: basta separar as vinculações com vírgulas.
O corpo só executa se todos os opcionais tiverem um valor:
```swift
let first: String? = "Ada"
let last: String? = "Lovelace"
if let first = first, let last = last {
    print("\(first) \(last)")
}
```
Você também pode acrescentar uma condição booleana após as vinculações, como `if let n = number, n > 0`.

# --instructions--

Escreva uma função `add` que recebe dois valores `Int?` (omita os rótulos dos argumentos) e retorna a soma deles como um `Int?`, ou `nil` se algum deles estiver ausente

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`add(2, 3)` deve retornar `5`

```swift
tryCatch(add(2, 3) == 5)
```

`add(nil, 3)` deve retornar `nil`

```swift
tryCatch(add(nil, 3) == nil)
```

`add(2, nil)` deve retornar `nil`

```swift
tryCatch(add(2, nil) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func add(_ a: Int?, _ b: Int?) -> Int? {
    guard let a = a, let b = b else { return nil }
    return a + b
}
```
