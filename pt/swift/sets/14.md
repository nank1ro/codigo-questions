---
language: swift
exerciseType: 2
---

# --description--

Sets e arrays se convertem facilmente um no outro.
Passar um array para `Set(...)` constrói um set a partir de seus elementos, que é a forma mais rápida de **remover duplicatas**:
```swift
let votes = [3, 1, 3, 2, 1]
let unique = Set(votes) // {1, 2, 3} in some order
```
Passar um set para `Array(...)` devolve um array, mas como um set não tem ordem, os elementos saem em uma sequência imprevisível.
Por isso, quando você precisa de um resultado ordenado, geralmente chama `sorted()` no set, que já retorna um array:
```swift
let ordered = unique.sorted() // [1, 2, 3]
```

# --instructions--

Remova as duplicatas de `votes`, depois imprima quantos valores distintos existem e os valores distintos em ordem crescente

# --seed--

```swift
let votes = [3, 1, 3, 2, 1]
let unique = [/](votes)
print(unique.[/])
print(unique.sorted())
```

# --answers--

- Set
- Array
- count
- length
- size

# --solutions--

```swift
let votes = [3, 1, 3, 2, 1]
let unique = Set(votes)
print(unique.count)
print(unique.sorted())
```

# --output--

3
[1, 2, 3]
