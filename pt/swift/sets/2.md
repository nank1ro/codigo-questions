---
language: swift
exerciseType: 1
---

# --description--

Assim como os arrays, os sets podem ser constantes (`let`) ou variáveis (`var`). Apenas um set `var` pode ser alterado depois de criado.
Para criar um set vazio você chama o inicializador do tipo, porque um literal vazio `[]` sozinho não diria ao Swift qual tipo de elemento usar:
```swift
var visited = Set<String>()
print(visited.isEmpty) // true
```
A propriedade `isEmpty` é `true` quando o set não tem elementos, exatamente como nos arrays.

# --instructions--

Declare um set constante de strings chamado `colors` contendo `"red"`, `"green"` e `"blue"`, e um set variável vazio de inteiros chamado `scores`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`colors` deve conter exatamente `"red"`, `"green"` e `"blue"`

```swift
tryCatch(colors == ["red", "green", "blue"])
```

`colors` deve ser um `Set<String>`

```swift
tryCatch(type(of: colors) == Set<String>.self)
```

`scores` deve estar vazio

```swift
tryCatch(scores.isEmpty)
```

`scores` deve ser um `Set<Int>`

```swift
tryCatch(type(of: scores) == Set<Int>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let colors: Set<String> = ["red", "green", "blue"]
var scores = Set<Int>()
```
