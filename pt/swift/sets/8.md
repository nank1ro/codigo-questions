---
language: swift
exerciseType: 4
---

# --description--

Sets suportam as operações clássicas da teoria dos conjuntos. Cada uma retorna um **novo** set e deixa os originais inalterados:
- `a.union(b)` contém todo elemento que está em `a`, em `b`, ou em ambos
- `a.intersection(b)` contém apenas os elementos que estão em **ambos** `a` e `b`
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.union(b).sorted())        // [1, 2, 3, 4]
print(a.intersection(b).sorted()) // [3]
```

# --instructions--

Ordene os itens para imprimir todo número que aparece em `odds` ou em `primes`, em ordem crescente.
Declare `odds` antes de `primes`

# --answers--

```swift
let odds: Set<Int> = [1, 3, 5]
```

```swift
let primes: Set<Int> = [2, 3, 5]
```

```swift
let all = odds.union(primes)
```

```swift
print(all.sorted())
```

# --solutions--

```swift
let odds: Set<Int> = [1, 3, 5]
let primes: Set<Int> = [2, 3, 5]
let all = odds.union(primes)
print(all.sorted())
```

# --output--

[1, 2, 3, 5]
