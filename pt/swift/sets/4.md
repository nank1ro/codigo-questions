---
language: swift
exerciseType: 2
---

# --description--

Para verificar se um valor está em um set, use o método `contains(_:)`, que retorna um `Bool`:
```swift
let primes: Set<Int> = [2, 3, 5, 7]
print(primes.contains(5)) // true
print(primes.contains(6)) // false
```
Essa verificação é muito rápida em um set, mesmo com milhares de elementos, o que é uma das principais razões para preferir um set a um array em testes de pertencimento.

# --instructions--

Imprima `Welcome` se `role` for um dos papéis permitidos, caso contrário imprima `Access denied`

# --seed--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed[/]([/]) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --answers--

- .contains
- .has
- .includes
- role
- "role"
- allowed

# --solutions--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed.contains(role) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --output--

Welcome
