---
language: swift
exerciseType: 1
---

# --description--

Uma **propriedade computada** parece uma propriedade, mas se comporta como um método: ela não armazena nada, calcula seu valor toda vez que você a lê. Você escreve o tipo e, em seguida, um bloco de código que retorna o valor:
```swift
struct Rectangle {
    var width: Int
    var height: Int

    var area: Int {
        return width * height
    }
}

let r = Rectangle(width: 3, height: 4)
print(r.area) // 12, no parentheses
```
Propriedades computadas não fazem parte do inicializador membro a membro, já que não há nada a armazenar. Use uma quando o valor for derivado dos outros, e um método quando o trabalho precisar de parâmetros.

# --instructions--

Adicione a `Temperature` uma propriedade computada `fahrenheit` do tipo `Double`, que retorna o valor em celsius multiplicado por `9`, dividido por `5`, mais `32`

Exemplo de uso:
```swift
print(Temperature(celsius: 100).fahrenheit)
// prints 212.0
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Temperature {
    var celsius: Double

}
```

# --asserts--

`100` graus celsius devem ser `212` fahrenheit

```swift
tryCatch(Temperature(celsius: 100).fahrenheit == 212)
```

`0` graus celsius devem ser `32` fahrenheit

```swift
tryCatch(Temperature(celsius: 0).fahrenheit == 32)
```

`-40` graus celsius devem ser `-40` fahrenheit

```swift
tryCatch(Temperature(celsius: -40).fahrenheit == -40)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Temperature {
    var celsius: Double

    var fahrenheit: Double {
        return celsius * 9 / 5 + 32
    }
}
```
