---
language: swift
exerciseType: 1
---

# --description--

Uma struct é um tipo como qualquer outro, então pode ser armazenada em um array, um dicionário ou um conjunto, e toda ferramenta que você já conhece continua funcionando sobre ela:
```swift
struct Item {
    var name: String
    var price: Int
}

let items = [Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]

for item in items {
    print(item.name)
}

let names = items.map { $0.name }
let total = items.reduce(0) { $0 + $1.price }
print(total) // 10
```
Lembre-se de que o array contém *cópias*: ler `items[0]` em uma variável e alterá-la não afeta o array.

# --instructions--

Escreva uma função `totalPrice` que recebe um array de `Item` sem rótulo de argumento e retorna a soma do `price` de cada item, ou `0` quando o array está vazio

Exemplo de chamada da função:
```swift
print(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]))
// prints 10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice() {

}
```

# --asserts--

Dois itens de `3` e `7` devem totalizar `10`

```swift
tryCatch(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]) == 10)
```

Um único item deve totalizar o próprio preço

```swift
tryCatch(totalPrice([Item(name: "Pen", price: 2)]) == 2)
```

Um array vazio deve totalizar `0`

```swift
tryCatch(totalPrice([]) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice(_ items: [Item]) -> Int {
    return items.reduce(0) { $0 + $1.price }
}
```
