---
language: swift
exerciseType: 1
---

# --description--

Você nunca escreveu o código que constrói um `Book`, ainda assim `Book(title: "Swift", pages: 120)` funcionou. O Swift o escreve para você: toda struct recebe de graça um **inicializador membro a membro**, um inicializador cujos parâmetros são suas propriedades armazenadas, na ordem em que são declaradas, cada um usando o nome da propriedade como seu rótulo de argumento:
```swift
struct Point {
    var x: Int
    var y: Int
}

let p = Point(x: 3, y: 4)
print(p.x) // 3
```
Classes não recebem isso de graça, o que é uma das razões pelas quais structs são a forma mais rápida de modelar um valor.

# --instructions--

Escreva uma função `makePoint` que recebe dois inteiros sem rótulo de argumento e retorna um `Point` construído com o inicializador membro a membro

Exemplo de chamada da função:
```swift
print(makePoint(3, 4).x)
// prints 3
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Point {
    var x: Int
    var y: Int
}

func makePoint() {

}
```

# --asserts--

`makePoint(3, 4)` deve ter `x` igual a `3`

```swift
tryCatch(makePoint(3, 4).x == 3)
```

`makePoint(3, 4)` deve ter `y` igual a `4`

```swift
tryCatch(makePoint(3, 4).y == 4)
```

`makePoint(0, -5)` deve ter `y` igual a `-5`

```swift
tryCatch(makePoint(0, -5).y == -5)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Point {
    var x: Int
    var y: Int
}

func makePoint(_ x: Int, _ y: Int) -> Point {
    return Point(x: x, y: y)
}
```
