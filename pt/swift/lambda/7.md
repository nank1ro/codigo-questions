---
language: swift
exerciseType: 1
---

# --description--

`filter` mantém apenas os elementos para os quais a closure retorna `true`. A closure recebe um elemento e deve retornar um `Bool`:
```swift
let nums = [5, 12, 8, 20]
let big = nums.filter { $0 > 10 }
print(big) // [12, 20]
```
Os elementos mantêm sua ordem original, e o resultado é um novo array do mesmo tipo de elemento.

# --instructions--

Escreva uma função `evens` que recebe um array de `Int` (omita o rótulo do argumento) e retorna um novo array apenas com os números pares, usando `filter`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Você deve usar `filter`

```json
{
  "regex": "\\.filter",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`evens([1, 2, 3, 4, 5, 6])` deve retornar `[2, 4, 6]`

```swift
tryCatch(evens([1, 2, 3, 4, 5, 6]) == [2, 4, 6])
```

`evens([7, 9])` deve retornar `[]`

```swift
tryCatch(evens([7, 9]) == [])
```

`evens([0, -2, 3])` deve retornar `[0, -2]`

```swift
tryCatch(evens([0, -2, 3]) == [0, -2])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func evens(_ nums: [Int]) -> [Int] {
    return nums.filter { $0 % 2 == 0 }
}
```
