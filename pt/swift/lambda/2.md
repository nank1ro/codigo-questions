---
language: swift
exerciseType: 1
---

# --description--

Escrever todos os tipos dentro da closure geralmente é desnecessário. Quando a constante tem um **tipo de função** explícito, o Swift infere os tipos dos parâmetros e do retorno, então você só precisa listar os nomes dos parâmetros antes do `in`:
```swift
let triple: (Int) -> Int = { n in
    return n * 3
}
```
O tipo `(Int) -> Int` se lê como "uma função que recebe um `Int` e retorna um `Int`".
Quando o corpo é uma única expressão, a palavra-chave `return` também pode ser omitida, isso é chamado de **retorno implícito**:
```swift
let triple: (Int) -> Int = { n in n * 3 }
print(triple(4)) // 12
```

# --instructions--

Armazene na constante `square` uma closure do tipo `(Int) -> Int` que retorna seu argumento multiplicado por si mesmo

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Você deve usar uma expressão de closure, não uma `func`

```json
{
  "regex": "func\\s+square",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`square(4)` deve retornar `16`

```swift
tryCatch(square(4) == 16)
```

`square(-3)` deve retornar `9`

```swift
tryCatch(square(-3) == 9)
```

`square(0)` deve retornar `0`

```swift
tryCatch(square(0) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let square: (Int) -> Int = { n in n * n }
```
