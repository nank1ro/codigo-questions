---
language: swift
exerciseType: 2
---

# --description--

Uma closure pode usar variáveis declaradas fora do seu corpo. Ela as **captura**: a variável continua existindo enquanto a closure existir, mesmo depois que a função que a declarou tiver retornado.
Isso permite que uma função construa uma closure com seu próprio estado privado:
```swift
func makeCounter() -> () -> Int {
    var count = 0
    return {
        count += 1
        return count
    }
}
```
`() -> Int` é o tipo de uma closure sem parâmetros que retorna um `Int`. Cada chamada à closure retornada incrementa o mesmo `count` capturado:
```swift
let counter = makeCounter()
print(counter()) // 1
print(counter()) // 2
```

# --instructions--

Complete `makeCounter` para que ela retorne uma closure sem parâmetros que incrementa e retorna o `count` capturado, de forma que o programa imprima `1`, `2` e `3` em três linhas

# --seed--

```swift
func makeCounter() -> [/] {
    [/] count = 0
    return {
        count += 1
        return count
    }
}
let counter = makeCounter()
print(counter())
print(counter())
print(counter())
```

# --answers--

- () -> Int
- var
- let
- Int
- () -> Void

# --solutions--

```swift
func makeCounter() -> () -> Int {
    var count = 0
    return {
        count += 1
        return count
    }
}
let counter = makeCounter()
print(counter())
print(counter())
print(counter())
```

# --output--

1
2
3
