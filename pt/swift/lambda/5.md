---
language: swift
exerciseType: 1
---

# --description--

Quando uma closure é o **último** argumento de uma função, você pode escrevê-la depois do parêntese de fechamento da chamada. Essa é a sintaxe de **trailing closure**:
```swift
print(apply(5) { $0 + 1 }) // 6
```
Se a closure for o único argumento, os parênteses podem ser omitidos completamente:
```swift
func run(_ task: () -> Int) -> Int {
    return task()
}
print(run { 42 }) // 42
```
As duas formas chamam exatamente a mesma função, a sintaxe trailing só é mais fácil de ler quando a closure é longa.

# --instructions--

Escreva uma função `applyTwice` que recebe um `Int` e uma closure do tipo `(Int) -> Int` (omita os dois rótulos de argumento) e retorna o resultado de aplicar a closure ao número duas vezes

Exemplo de chamada da função:
```swift
print(applyTwice(3) { $0 * 2 })
// prints 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`applyTwice(3) { $0 * 2 }` deve retornar `12`

```swift
tryCatch(applyTwice(3) { $0 * 2 } == 12)
```

`applyTwice(1) { $0 + 10 }` deve retornar `21`

```swift
tryCatch(applyTwice(1) { $0 + 10 } == 21)
```

`applyTwice(2, { $0 * $0 })` deve retornar `16`

```swift
tryCatch(applyTwice(2, { $0 * $0 }) == 16)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func applyTwice(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(operation(n))
}
```
