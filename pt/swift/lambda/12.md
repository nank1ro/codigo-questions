---
language: swift
exerciseType: 1
---

# --description--

Retornar uma closure é uma forma prática de construir funções personalizadas. Os parâmetros da função externa são capturados pela closure que ela retorna:
```swift
func makeAdder(_ amount: Int) -> (Int) -> Int {
    return { $0 + amount }
}
let addFive = makeAdder(5)
print(addFive(10)) // 15
```
O tipo de retorno `(Int) -> Int` descreve a closure, e a abreviação `$0` se refere ao argumento dessa closure, não de `makeAdder`.

# --instructions--

Escreva uma função `makeMultiplier` que recebe um `Int` chamado `factor` (omita o rótulo do argumento) e retorna uma closure do tipo `(Int) -> Int` que multiplica seu argumento por `factor`

Exemplo de chamada da função:
```swift
let double = makeMultiplier(2)
print(double(10))
// prints 20
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`makeMultiplier(2)(10)` deve retornar `20`

```swift
tryCatch(makeMultiplier(2)(10) == 20)
```

`makeMultiplier(3)(4)` deve retornar `12`

```swift
tryCatch(makeMultiplier(3)(4) == 12)
```

`makeMultiplier(0)(99)` deve retornar `0`

```swift
tryCatch(makeMultiplier(0)(99) == 0)
```

Uma closure retornada por `makeMultiplier(5)` deve continuar funcionando entre chamadas

```swift
let _timesFive = makeMultiplier(5)
tryCatch(_timesFive(1) == 5 && _timesFive(6) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func makeMultiplier(_ factor: Int) -> (Int) -> Int {
    return { $0 * factor }
}
```
