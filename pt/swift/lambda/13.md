---
language: swift
exerciseType: 4
---

# --description--

Uma closure armazenada em uma constante pode ser passada sempre que uma closure for esperada como argumento, usando o rótulo de argumento do parâmetro:
```swift
let ascending = { (a: Int, b: Int) -> Bool in a < b }
print([3, 1, 2].sorted(by: ascending)) // [1, 2, 3]
```

# --instructions--

Ordene os itens para armazenar em `byLength` uma closure que ordena duas strings da mais curta para a mais longa, depois imprima `words` ordenado com ela.
Declare `words` antes de `byLength`

# --answers--

```swift
let words = ["kiwi", "fig", "banana"]
```

```swift
let byLength = { (a: String, b: String) -> Bool in
```

```swift
    a.count < b.count
```

```swift
}
```

```swift
print(words.sorted(by: byLength))
```

# --solutions--

```swift
let words = ["kiwi", "fig", "banana"]
let byLength = { (a: String, b: String) -> Bool in
    a.count < b.count
}
print(words.sorted(by: byLength))
```

# --output--

["fig", "kiwi", "banana"]
