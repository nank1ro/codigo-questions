---
language: swift
exerciseType: 1
---

# --description--

Por padrão, uma closure passada para uma função só pode ser usada enquanto essa função estiver em execução. Se a função armazenar a closure ou retornar outra closure que a use, a closure **escapa** da função, e seu parâmetro precisa ser marcado com `@escaping`:
```swift
func twice(_ task: @escaping () -> Int) -> () -> Int {
    return { task() * 2 }
}
let answer = twice { 21 }
print(answer()) // 42
```
Sem `@escaping` o compilador reporta um erro, porque a closure retornada usaria `task` depois que `twice` já tivesse terminado.

# --instructions--

Escreva uma função `compose` que recebe duas closures do tipo `(Int) -> Int` (omita os dois rótulos de argumento) e retorna uma closure que aplica a primeira e depois a segunda ao seu argumento

Exemplo de chamada da função:
```swift
let addThenDouble = compose({ $0 + 1 }, { $0 * 2 })
print(addThenDouble(3))
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`compose({ $0 + 1 }, { $0 * 2 })(3)` deve retornar `8`

```swift
tryCatch(compose({ $0 + 1 }, { $0 * 2 })(3) == 8)
```

`compose({ $0 * 2 }, { $0 + 1 })(3)` deve retornar `7`

```swift
tryCatch(compose({ $0 * 2 }, { $0 + 1 })(3) == 7)
```

`compose({ $0 }, { $0 })(9)` deve retornar `9`

```swift
tryCatch(compose({ $0 }, { $0 })(9) == 9)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func compose(_ first: @escaping (Int) -> Int, _ second: @escaping (Int) -> Int) -> (Int) -> Int {
    return { second(first($0)) }
}
```
