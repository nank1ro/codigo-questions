---
language: swift
exerciseType: 2
---

# --description--

Como closures são valores, uma função pode aceitar uma como parâmetro. O tipo do parâmetro é apenas o tipo de função:
```swift
func apply(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(n)
}
print(apply(5, { $0 + 1 })) // 6
```
A função `apply` não sabe o que `operation` faz, ela só sabe que recebe um `Int` e retorna um `Int`, e a chama como qualquer outra função.

# --instructions--

Complete a função `apply` para que seu segundo parâmetro seja uma closure que recebe um `Int` e retorna um `Int`, e a função retorne o resultado de chamar essa closure com `n`, de forma que o programa imprima `12`

# --seed--

```swift
func apply(_ n: Int, _ operation: [/]) -> Int {
    return [/]
}
print(apply(6, { $0 * 2 }))
```

# --answers--

- (Int) -> Int
- operation(n)
- Int
- (Int) -> Void
- operation
- n

# --solutions--

```swift
func apply(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(n)
}
print(apply(6, { $0 * 2 }))
```

# --output--

12
