---
language: swift
exerciseType: 1
---

# --instructions--

Crie uma nova variável `revenue` e atribua a ela a diferença entre `earnings` e `costs`, depois imprima seu valor

# --seed--

```swift
var earnings = 2000
var costs = 750
```

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`revenue` deve ser igual à diferença entre `earnings` e `costs`.

```swift
    func test1() {
        XCTAssertEqual(revenue, earnings - costs, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
var earnings = 2000
var costs = 750
var revenue = earnings - costs;
```

# --output--

1250
