---
language: swift
exerciseType: 2
---

# --description--

Como você viu nas lições de strings, iterar sobre uma `String` fornece um `Character` por vez. Um `Character` não é uma `String`, então para usá-lo como texto você o converte com `String(c)`.
Quando o caractere é um dígito, a propriedade `wholeNumberValue` fornece seu valor numérico como um `Int?`: ele é `nil` para caracteres que não são dígitos.
```swift
for c in "a1" {
    print(c.wholeNumberValue)
}
// nil
// Optional(1)
```

# --instructions--

Complete o loop para que ele some os dígitos da string, de modo que o programa imprima `8`

# --seed--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.[/] {
        sum [/] digit
    }
}
print(sum)
```

# --answers--

- wholeNumberValue
- +=
- asciiValue
- isNumber
- -=
- =

# --solutions--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.wholeNumberValue {
        sum += digit
    }
}
print(sum)
```

# --output--

8
