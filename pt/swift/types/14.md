---
language: swift
exerciseType: 1
---

# --description--

Como `Int(text)` e `Double(text)` retornam `nil` em caso de falha, comparar o resultado com `nil` informa se um texto é um número desse tipo:
```swift
print(Int("42") != nil)     // true
print(Double("4.2") != nil) // true
print(Double("42") != nil)  // true, a whole number is also a valid Double
```
Note a última linha: todo texto aceito pelo `Int` também é aceito pelo `Double`, então verifique primeiro o `Int` quando quiser distingui-los.

# --instructions--

Escreva uma função `classify` que recebe uma `String` (omita o rótulo do argumento) e retorna `"Int"` se o texto for um número inteiro válido, `"Double"` se for um número decimal válido, e `"String"` caso contrário

Exemplo de chamada da função:
```swift
print(classify("3.5"))
// prints Double
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`classify("42")` deve retornar `"Int"`

```swift
tryCatch(classify("42") == "Int")
```

`classify("-7")` deve retornar `"Int"`

```swift
tryCatch(classify("-7") == "Int")
```

`classify("3.5")` deve retornar `"Double"`

```swift
tryCatch(classify("3.5") == "Double")
```

`classify("hello")` deve retornar `"String"`

```swift
tryCatch(classify("hello") == "String")
```

`classify("")` deve retornar `"String"`

```swift
tryCatch(classify("") == "String")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func classify(_ text: String) -> String {
    if Int(text) != nil {
        return "Int"
    }
    if Double(text) != nil {
        return "Double"
    }
    return "String"
}
```
