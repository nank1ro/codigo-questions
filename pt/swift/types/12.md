---
language: swift
exerciseType: 1
---

# --description--

`Int.max` e `Int.min` são úteis como valores iniciais quando você procura um extremo: qualquer número é menor que `Int.max`, então ele é um valor inicial seguro para "o menor visto até agora":
```swift
var smallest = Int.max
for number in [8, 3, 5] {
    if number < smallest {
        smallest = number
    }
}
print(smallest) // 3
```

# --instructions--

Escreva uma função `smallest` que recebe um array de `Int` (omita o rótulo do argumento) e retorna seu menor elemento, ou `Int.max` quando o array está vazio. Não use o método `min()`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Você não deve usar `min()`

```json
{
  "regex": "\\.min\\(",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`smallest([8, 3, 5])` deve retornar `3`

```swift
tryCatch(smallest([8, 3, 5]) == 3)
```

`smallest([-5, 5])` deve retornar `-5`

```swift
tryCatch(smallest([-5, 5]) == -5)
```

`smallest([Int.min, 0])` deve retornar `Int.min`

```swift
tryCatch(smallest([Int.min, 0]) == Int.min)
```

`smallest([])` deve retornar `Int.max`

```swift
tryCatch(smallest([]) == Int.max)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func smallest(_ numbers: [Int]) -> Int {
    var result = Int.max
    for number in numbers {
        if number < result {
            result = number
        }
    }
    return result
}
```
