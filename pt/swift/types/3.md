---
language: swift
exerciseType: 1
---

# --description--

A função `type(of:)` retorna o tipo de um valor, o que é útil para verificar o que o Swift inferiu:
```swift
print(type(of: 42))    // Int
print(type(of: 2.5))   // Double
print(type(of: "hi"))  // String
```
Quando você quer um tipo diferente do inferido, adicione uma anotação. Um literal de número inteiro pode ser armazenado em uma constante `Double` ou `Float`, e um literal de um caractere em uma constante `Character`:
```swift
let ratio: Double = 3       // 3.0, not an Int
let half: Float = 0.5
let initial: Character = "S"
print(type(of: ratio))      // Double
```

# --instructions--

Declare três constantes:
- `ratio`, um `Double` que armazena o literal de número inteiro `3`
- `half`, um `Float` que armazena `0.5`
- `initial`, um `Character` que armazena `"S"`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`ratio` deve ser um `Double` igual a `3.0`

```swift
tryCatch(type(of: ratio) == Double.self && ratio == 3.0)
```

`half` deve ser um `Float` igual a `0.5`

```swift
tryCatch(type(of: half) == Float.self && half == 0.5)
```

`initial` deve ser um `Character` igual a `"S"`

```swift
tryCatch(type(of: initial) == Character.self && initial == "S")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let ratio: Double = 3
let half: Float = 0.5
let initial: Character = "S"
```
