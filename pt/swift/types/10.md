---
language: swift
exerciseType: 4
---

# --description--

Um **alias de tipo** dá a um tipo existente um novo nome, com a palavra-chave `typealias`:
```swift
typealias Score = Int
let best: Score = 100
print(best + 1) // 101
```
`Score` e `Int` são o mesmo tipo, então eles se combinam livremente. Um alias não adiciona nenhuma segurança: ele apenas torna o código mais legível quando um tipo comum tem um significado específico no seu programa.

# --instructions--

Ordene os itens para definir `Celsius` como um alias de `Double` na primeira linha, converter o ponto de ebulição da água para Fahrenheit e imprimi-lo

# --answers--

```swift
typealias Celsius = Double
```

```swift
let boiling: Celsius = 100
```

```swift
let fahrenheit = boiling * 9 / 5 + 32
```

```swift
print(fahrenheit)
```

# --solutions--

```swift
typealias Celsius = Double
let boiling: Celsius = 100
let fahrenheit = boiling * 9 / 5 + 32
print(fahrenheit)
```

# --output--

212.0
