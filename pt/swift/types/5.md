---
language: swift
exerciseType: 1
---

# --description--

`Int(x)` não arredonda: ele **trunca**, simplesmente descartando a parte decimal, então `Int(3.99)` é `3` e `Int(-3.99)` é `-3`.
Para arredondar para o número inteiro mais próximo, chame `rounded()` no `Double` primeiro e depois converta:
```swift
let x = 3.99
print(Int(x))            // 3
print(Int(x.rounded()))  // 4
```
Valores intermediários como `2.5` são arredondados para longe do zero: `2.5` se torna `3.0` e `-2.5` se torna `-3.0`.

# --instructions--

Escreva uma função `roundedAverage` que recebe dois `Int` (omita os rótulos dos argumentos) e retorna a média deles arredondada para o número inteiro mais próximo, como um `Int`

Exemplo de chamada da função:
```swift
print(roundedAverage(3, 4))
// prints 4
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`roundedAverage(3, 4)` deve retornar `4` (a média é `3.5`)

```swift
tryCatch(roundedAverage(3, 4) == 4)
```

`roundedAverage(2, 4)` deve retornar `3`

```swift
tryCatch(roundedAverage(2, 4) == 3)
```

`roundedAverage(10, 15)` deve retornar `13` (a média é `12.5`)

```swift
tryCatch(roundedAverage(10, 15) == 13)
```

`roundedAverage(-3, -4)` deve retornar `-4`

```swift
tryCatch(roundedAverage(-3, -4) == -4)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func roundedAverage(_ a: Int, _ b: Int) -> Int {
    let average = Double(a + b) / 2
    return Int(average.rounded())
}
```
