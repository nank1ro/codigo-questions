---
language: swift
exerciseType: 1
---

# --description--

Quando uma função precisa retornar um resultado decimal calculado a partir de números inteiros, converta os operandos para `Double` antes de dividir e declare o tipo de retorno como `Double`:
```swift
func ratio(_ part: Int, _ total: Int) -> Double {
    return Double(part) / Double(total)
}
print(ratio(1, 4)) // 0.25
```
Lembre-se de que o `count` de um array também é um `Int`, então ele precisa da mesma conversão.

# --instructions--

Escreva uma função `average` que recebe um array de `Int` (omita o rótulo do argumento) e retorna a média dos elementos como um `Double`. O array nunca está vazio

Exemplo de chamada da função:
```swift
print(average([1, 2]))
// prints 1.5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`average([1, 2])` deve retornar `1.5`

```swift
tryCatch(average([1, 2]) == 1.5)
```

`average([3, 4, 5, 6])` deve retornar `4.5`

```swift
tryCatch(average([3, 4, 5, 6]) == 4.5)
```

`average([10])` deve retornar `10.0`

```swift
tryCatch(average([10]) == 10.0)
```

`average([1, 1, 2])` deve retornar um `Double`

```swift
tryCatch(type(of: average([1, 1, 2])) == Double.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func average(_ numbers: [Int]) -> Double {
    var sum = 0
    for number in numbers {
        sum += number
    }
    return Double(sum) / Double(numbers.count)
}
```
