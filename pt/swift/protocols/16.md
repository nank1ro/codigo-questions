---
language: swift
exerciseType: 4
---

# --instructions--

Ordene os itens para imprimir `[G]`, declarando o protocolo primeiro, depois sua extension, depois o enum, depois o `print`

# --answers--

```swift
protocol Badge {
    var symbol: String { get }
}
```

```swift
extension Badge {
    func banner() -> String { return "[\(symbol)]" }
}
```

```swift
enum Rank: Badge {
    case gold

    var symbol: String { return "G" }
}
```

```swift
print(Rank.gold.banner())
```

# --solutions--

```swift
protocol Badge {
    var symbol: String { get }
}
extension Badge {
    func banner() -> String { return "[\(symbol)]" }
}
enum Rank: Badge {
    case gold

    var symbol: String { return "G" }
}
print(Rank.gold.banner())
```

# --output--

[G]
