---
language: swift
exerciseType: 1
---

# --description--

Um nome de protocolo sozinho não é um tipo, é uma restrição, então o Swift pede que você diga qual de duas coisas você quer dizer.

`some Shape` significa *um tipo em conformidade específico*, fixo em tempo de compilação. Quem chama nunca descobre qual é, mas é sempre o mesmo:
```swift
func unitSquare() -> some Shape {
    return Square(side: 1)
}
```
`any Shape` significa *uma caixa que pode conter qualquer tipo em conformidade*, e dois valores desse tipo podem conter tipos diferentes. Você precisa dele sempre que o tipo concreto puder variar, como dentro de um array misto:
```swift
let shapes: [any Shape] = [Square(side: 2), Rect(width: 2, height: 3)]
```
Ambos permitem chamar os requisitos do protocolo. Prefira `some` quando um único tipo for suficiente, porque não custa nada em tempo de execução; recorra a `any` quando realmente precisar misturar tipos.

# --instructions--

Escreva uma função `totalArea` que recebe um array de `any Shape` sem rótulo de argumento e retorna a soma de suas áreas

Exemplo de chamada da função:
```swift
print(totalArea([Square(side: 2), Rect(width: 2, height: 3)]))
// prints 10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Shape {
    func area() -> Int
}

struct Square: Shape {
    var side: Int

    func area() -> Int {
        return side * side
    }
}

struct Rect: Shape {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}

func unitSquare() -> some Shape {
    return Square(side: 1)
}

func totalArea() {

}
```

# --asserts--

Um quadrado de lado `2` e um retângulo de `2` por `3` devem totalizar `10`

```swift
tryCatch(totalArea([Square(side: 2), Rect(width: 2, height: 3)]) == 10)
```

Um array vazio deve totalizar `0`

```swift
tryCatch(totalArea([]) == 0)
```

Um único retângulo de `5` por `4` deve totalizar `20`

```swift
tryCatch(totalArea([Rect(width: 5, height: 4)]) == 20)
```

Apenas o quadrado unitário deve totalizar `1`

```swift
tryCatch(totalArea([unitSquare()]) == 1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Shape {
    func area() -> Int
}

struct Square: Shape {
    var side: Int

    func area() -> Int {
        return side * side
    }
}

struct Rect: Shape {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}

func unitSquare() -> some Shape {
    return Square(side: 1)
}

func totalArea(_ shapes: [any Shape]) -> Int {
    var total = 0
    for shape in shapes {
        total += shape.area()
    }
    return total
}
```
