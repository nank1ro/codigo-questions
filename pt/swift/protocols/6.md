---
language: swift
exerciseType: 4
---

# --description--

Um protocolo geralmente reúne mais de um requisito, e um tipo em conformidade deve satisfazer todos eles:
```swift
protocol Vehicle {
    var wheels: Int { get }
    func move() -> String
}

struct Bike: Vehicle {
    var wheels = 2

    func move() -> String {
        return "pedalling"
    }
}
```
A ordem dos requisitos dentro das chaves não importa, nem a ordem em que o tipo em conformidade os fornece: o compilador só verifica que nada está faltando.

# --instructions--

Ordene os itens para imprimir `Moo`, declarando o protocolo primeiro, depois a struct, depois a instância antes do `print`

# --answers--

```swift
protocol Animal {
    func sound() -> String
}
```

```swift
struct Cow: Animal {
    func sound() -> String { return "Moo" }
}
```

```swift
let cow = Cow()
```

```swift
print(cow.sound())
```

# --solutions--

```swift
protocol Animal {
    func sound() -> String
}
struct Cow: Animal {
    func sound() -> String { return "Moo" }
}
let cow = Cow()
print(cow.sound())
```

# --output--

Moo
