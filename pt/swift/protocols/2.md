---
language: swift
exerciseType: 1
---

# --description--

Um protocolo também pode exigir **métodos**. Você escreve a assinatura — nome, parâmetros e tipo de retorno — e para por aí, sem corpo:
```swift
protocol Greeter {
    func greet() -> String
}
```
O tipo em conformidade deve declarar um método com exatamente essa assinatura, e ele fornece o corpo:
```swift
struct Robot: Greeter {
    func greet() -> String {
        return "BEEP"
    }
}

print(Robot().greet()) // BEEP
```
Se algo diferir — o nome, um tipo de parâmetro, o tipo de retorno — o tipo não está em conformidade, e o compilador lhe diz qual requisito está faltando.

# --instructions--

Faça `Friendly` conformar-se a `Greeter` adicionando o método `greet()` exigido, que retorna o nome entre `Hello, ` e `!`

Exemplo de uso:
```swift
print(Friendly(name: "Ada").greet())
// imprime Hello, Ada!
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Greeter {
    func greet() -> String
}

struct Friendly {
    var name: String
}
```

# --before-asserts--

```swift
func _asGreeter(_ g: Greeter) -> String {
    return g.greet()
}
```

# --asserts--

`Friendly(name: "Ada")` deve cumprimentar com `Hello, Ada!`

```swift
tryCatch(_asGreeter(Friendly(name: "Ada")) == "Hello, Ada!")
```

`Friendly(name: "Bo")` deve cumprimentar com `Hello, Bo!`

```swift
tryCatch(_asGreeter(Friendly(name: "Bo")) == "Hello, Bo!")
```

Um nome vazio deve cumprimentar com `Hello, !`

```swift
tryCatch(_asGreeter(Friendly(name: "")) == "Hello, !")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Greeter {
    func greet() -> String
}

struct Friendly: Greeter {
    var name: String

    func greet() -> String {
        return "Hello, \(name)!"
    }
}
```
