---
language: swift
exerciseType: 1
---

# --description--

A biblioteca padrão é construída a partir de protocolos, e seus próprios tipos podem adotá-los.

`Equatable` dá a um tipo o operador `==`. Para uma struct cujas propriedades armazenadas são todas `Equatable`, declarar a conformidade é suficiente — o Swift escreve `==` para você:
```swift
struct Point: Equatable {
    var x: Int
    var y: Int
}

print(Point(x: 1, y: 2) == Point(x: 1, y: 2)) // true
```
`Comparable` herda de `Equatable` e adiciona ordenação. Você implementa um único operador, `<`, escrito como uma `static func` que recebe os dois valores, e ganha de graça `>`, `<=`, `>=`, além de `sorted()`, `min()` e `max()` nas coleções:
```swift
struct Version: Comparable {
    var number: Int

    static func < (lhs: Version, rhs: Version) -> Bool {
        return lhs.number < rhs.number
    }
}
```

# --instructions--

Faça `Weight` conformar-se a `Comparable`, ordenando por `grams`

Exemplo de uso:
```swift
print(Weight(grams: 100) < Weight(grams: 250))
// imprime true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Weight {
    var grams: Int
}
```

# --asserts--

`100` gramas devem ser mais leves que `250` gramas

```swift
tryCatch(Weight(grams: 100) < Weight(grams: 250))
```

`500` gramas devem ser mais pesados que `250` gramas

```swift
tryCatch(Weight(grams: 500) > Weight(grams: 250))
```

Dois pesos de `80` gramas devem ser iguais

```swift
tryCatch(Weight(grams: 80) == Weight(grams: 80))
```

O mais pesado entre `3` e `1` gramas deve ser `3` gramas

```swift
tryCatch([Weight(grams: 3), Weight(grams: 1)].max() == Weight(grams: 3))
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Weight: Comparable {
    var grams: Int

    static func < (lhs: Weight, rhs: Weight) -> Bool {
        return lhs.grams < rhs.grams
    }
}
```
