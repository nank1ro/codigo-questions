---
language: swift
exerciseType: 1
---

# --description--

Um protocolo pode se basear em outro. Escrever o nome de um protocolo depois de dois-pontos faz o novo protocolo **herdar** cada requisito do antigo:
```swift
protocol Named {
    var name: String { get }
}

protocol Aged: Named {
    var age: Int { get }
}
```
Um tipo em conformidade com `Aged` deve fornecer `age` *e* `name`, e conta como um tipo `Named` em todos os lugares. Um protocolo pode herdar de vários protocolos de uma vez, separados por vírgulas.

# --instructions--

Declare um protocolo `Timed` que herda de `Named` e adiciona uma propriedade `seconds` legível do tipo `Int`, depois faça `Track` conformar-se a `Timed`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Named {
    var name: String { get }
}

struct Track {
    var name: String
    var seconds: Int
}
```

# --before-asserts--

```swift
extension Timed {
    func label() -> String {
        return "\(name) \(seconds)"
    }
}

func _name(_ n: Named) -> String {
    return n.name
}
```

# --asserts--

Uma faixa chamada `Intro` de `90` segundos deve ser rotulada como `Intro 90`

```swift
tryCatch(Track(name: "Intro", seconds: 90).label() == "Intro 90")
```

Uma faixa chamada `Outro` de `5` segundos deve ser rotulada como `Outro 5`

```swift
tryCatch(Track(name: "Outro", seconds: 5).label() == "Outro 5")
```

Uma faixa chamada `Gap` de `0` segundos deve ser rotulada como `Gap 0`

```swift
tryCatch(Track(name: "Gap", seconds: 0).label() == "Gap 0")
```

Um valor `Timed` deve ser utilizável como um valor `Named`

```swift
tryCatch(_name(Track(name: "Intro", seconds: 90)) == "Intro")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Named {
    var name: String { get }
}

protocol Timed: Named {
    var seconds: Int { get }
}

struct Track: Timed {
    var name: String
    var seconds: Int
}
```
