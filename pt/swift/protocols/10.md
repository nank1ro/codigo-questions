---
language: swift
exerciseType: 4
---

# --description--

Uma extension de protocolo também pode adicionar membros que o protocolo nunca listou como requisitos. São conveniências extras, disponíveis em todo tipo em conformidade:
```swift
protocol Sized {
    var count: Int { get }
}

extension Sized {
    var isEmpty: Bool {
        return count == 0
    }
}
```
`isEmpty` não é um requisito, então um tipo em conformidade não precisa fornecê-lo — ele simplesmente o recebe.

# --instructions--

Ordene os itens para imprimir `HEY`, declarando o protocolo primeiro, depois sua extension, depois a struct, depois o `print`

# --answers--

```swift
protocol Loud {
    var text: String { get }
}
```

```swift
extension Loud {
    func shout() -> String { return text.uppercased() }
}
```

```swift
struct Message: Loud {
    var text: String
}
```

```swift
print(Message(text: "hey").shout())
```

# --solutions--

```swift
protocol Loud {
    var text: String { get }
}
extension Loud {
    func shout() -> String { return text.uppercased() }
}
struct Message: Loud {
    var text: String
}
print(Message(text: "hey").shout())
```

# --output--

HEY
