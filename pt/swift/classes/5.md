---
language: swift
exerciseType: 2
---

# --description--

Quando uma classe possui suas próprias funções, essas funções são chamadas de __métodos__.

# --instructions--

Adicione um método, `description`, à sua classe `Animal`.
Usando duas instruções `print` separadas, ele deve imprimir o `gender` e `legs` do animal no qual é chamado.
Em seguida, chame o método `description`.

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4

    [/][/]() {
        if let gender = self.gender { print(gender) }
        print([/].legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.[/]
```

# --answers--

-  description
- func
- description()
- self
- description[]
- function
- method

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4

    func description() {
        if let gender = self.gender { print(gender) }
        print(self.legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.description()
```

# --output--

female
4
