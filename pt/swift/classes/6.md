---
language: swift
exerciseType: 2
---

# --description--

Quando uma classe tem suas proprias funcoes, essas funcoes sao chamadas de __metodos__.

# --instructions--

Adicione um metodo, `description`, a sua classe `Animal`.
Usando duas declaracoes `print` separadas, ele deve imprimir o `gender` e `legs` do animal no qual e chamado.
Em seguida, chame o metodo `description`.

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4

    [/][/]() {
        if let gender = self.gender { print(gender) }
        print([/].legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.[/]
```

# --answers--

-  description
- func
- description()
- self
- description[]
- function
- method

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4

    func description() {
        if let gender = self.gender { print(gender) }
        print(self.legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.description()
```

# --output--

female
4
