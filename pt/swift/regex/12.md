---
language: swift
exerciseType: 1
---

# --description--

`replacing(_:with:)` troca cada correspondência por um texto fixo e retorna uma nova `String`, deixando o original intacto:
```swift
let text = "a1 b22"
print(text.replacing(#/\d+/#, with: "#")) // a# b#
print(text)                               // a1 b22
```
Observe que `\d+` substitui uma sequência inteira de dígitos por um único `#`, enquanto `\d` substituiria um dígito por vez. O padrão decide quanto desaparece.

# --instructions--

Escreva uma função `mask` que recebe um texto (`String`) sem rótulo de argumento e o retorna com cada dígito individual substituído por um `#`

Exemplo de chamada da função:
```swift
print(mask("a1 b22"))
// imprime a# b##
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func mask() {

}
```

# --asserts--

A função deve usar `replacing(`

```json
{
  "regex": "replacing\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`mask("a1 b22")` deve retornar `"a# b##"`

```swift
tryCatch(mask("a1 b22") == "a# b##")
```

`mask("2024")` deve retornar `"####"`

```swift
tryCatch(mask("2024") == "####")
```

`mask("abc")` deve retornar `"abc"`

```swift
tryCatch(mask("abc") == "abc")
```

`mask("")` deve retornar `""`

```swift
tryCatch(mask("") == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func mask(_ text: String) -> String {
    return text.replacing(#/\d/#, with: "#")
}
```
