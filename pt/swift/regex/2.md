---
language: swift
exerciseType: 1
---

# --description--

Alguns atalhos cobrem a maioria dos padrões. Cada um corresponde a exatamente **um** caractere:
- `\d` é um dígito
- `\w` é uma letra, um dígito ou um underscore
- `\s` é um espaço, uma tabulação ou uma quebra de linha
- `.` é qualquer caractere único

Para corresponder a mais de um caractere, adicione um **quantificador** logo após o padrão:
- `+` significa um ou mais
- `*` significa zero ou mais
- `?` significa zero ou um

Então `\w+` é uma palavra, `\s*` é um espaçamento opcional, e `\d?` é um dígito opcional:
```swift
print("hello world".contains(#/\w+\s\w+/#)) // true
print("hello".contains(#/\w+\s\w+/#))       // false
```
Um caractere sem significado especial simplesmente corresponde a si mesmo, então `#/cat/#` corresponde às três letras `cat`.

# --instructions--

Escreva uma função `hasDigit` que recebe um texto (`String`) sem rótulo de argumento e retorna `true` quando o texto contém pelo menos um dígito, usando uma regex

Exemplo de chamada da função:
```swift
print(hasDigit("abc1"))
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func hasDigit() {

}
```

# --asserts--

A função deve usar um literal de regex escrito com `#/`

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`hasDigit("abc1")` deve retornar `true`

```swift
tryCatch(hasDigit("abc1") == true)
```

`hasDigit("7")` deve retornar `true`

```swift
tryCatch(hasDigit("7") == true)
```

`hasDigit("abc")` deve retornar `false`

```swift
tryCatch(hasDigit("abc") == false)
```

`hasDigit("")` deve retornar `false`

```swift
tryCatch(hasDigit("") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func hasDigit(_ text: String) -> Bool {
    return text.contains(#/\d/#)
}
```
