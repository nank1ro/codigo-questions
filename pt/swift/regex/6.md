---
language: swift
exerciseType: 1
---

# --description--

`m.0` não é uma `String`, mas uma `Substring`: uma visão sobre o texto original, não uma cópia. Ela é exibida exatamente como uma string, mas onde uma `String` é exigida você precisa convertê-la:
```swift
let text = "order 42"
if let m = text.firstMatch(of: #/\d+/#) {
    let found: String = String(m.0)
    print(found) // 42
}
```
Os inicializadores de números aceitam uma `Substring` diretamente, então `Int(m.0)` funciona sem o desvio.

# --instructions--

Escreva uma função `firstNumber` que recebe um texto (`String`) sem rótulo de argumento e retorna a primeira sequência de dígitos como uma `String`, ou `"none"` quando o texto não contém nenhum dígito

Exemplo de chamada da função:
```swift
print(firstNumber("order 42 today 7"))
// prints 42
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func firstNumber() {

}
```

# --asserts--

A função deve usar `firstMatch(of:`

```json
{
  "regex": "firstMatch\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstNumber("order 42 today 7")` deve retornar `"42"`

```swift
tryCatch(firstNumber("order 42 today 7") == "42")
```

`firstNumber("a1")` deve retornar `"1"`

```swift
tryCatch(firstNumber("a1") == "1")
```

`firstNumber("100")` deve retornar `"100"`

```swift
tryCatch(firstNumber("100") == "100")
```

`firstNumber("abc")` deve retornar `"none"`

```swift
tryCatch(firstNumber("abc") == "none")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstNumber(_ text: String) -> String {
    if let m = text.firstMatch(of: #/\d+/#) {
        return String(m.0)
    }
    return "none"
}
```
