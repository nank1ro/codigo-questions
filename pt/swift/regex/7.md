---
language: swift
exerciseType: 4
---

# --description--

`matches(of:)` retorna **todas** as correspondências em vez da primeira, como um array. O array nunca é `nil`: quando nada corresponde ele é simplesmente vazio, então ele pode ser percorrido ou transformado imediatamente:
```swift
let text = "a1 b22"
print(text.matches(of: #/\d+/#).count) // 2
```
Cada elemento é uma correspondência, então `$0.0` dentro de um `map` é o texto correspondido:
```swift
let found = text.matches(of: #/\d+/#).map { String($0.0) }
print(found) // ["1", "22"]
```

# --instructions--

Ordene os itens para construir um programa que reúne cada sequência de dígitos de `text` em `found` e a exibe

# --answers--

```swift
let text = "a1 b22 c333"
```

```swift
let pattern = #/\d+/#
```

```swift
let found = text.matches(of: pattern).map { String($0.0) }
```

```swift
print(found)
```

# --solutions--

```swift
let text = "a1 b22 c333"
let pattern = #/\d+/#
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

```swift
let pattern = #/\d+/#
let text = "a1 b22 c333"
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

# --output--

["1", "22", "333"]
