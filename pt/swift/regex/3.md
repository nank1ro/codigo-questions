---
language: swift
exerciseType: 3
---

# --description--

Por padrão, um padrão pode corresponder em qualquer lugar dentro do texto. **Âncoras** o vinculam a uma posição em vez disso:
- `^` significa "o início do texto"
- `$` significa "o fim do texto"

```swift
print("swift".contains(#/^sw/#))  // true, o texto começa com sw
print("myswift".contains(#/^sw/#)) // false, sw não está no início
print("swift".contains(#/ft$/#))  // true, o texto termina com ft
```
Âncoras correspondem a uma posição, não a um caractere, então elas não acrescentam nada ao conteúdo da correspondência.

# --instructions--

O que este código imprime?
```swift
let pattern = #/^a\w+/#
print("abc".contains(pattern), "xabc".contains(pattern))
```

# --answers--

- `true false`
- `true true`
- `false true`
- `false false`

# --solutions--

- `true false`
