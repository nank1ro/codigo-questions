---
language: swift
exerciseType: 4
---

# --description--

Validar um texto inteiro com `^` e `$` funciona, mas `wholeMatch(of:)` diz isso diretamente: ele retorna uma correspondência somente quando o padrão cobre o texto do primeiro ao último caractere, e `nil` caso contrário:
```swift
print("1a2b".wholeMatch(of: #/[0-9a-f]+/#) != nil) // true
print("1z".wholeMatch(of: #/[0-9a-f]+/#) != nil)   // false
```
Use `firstMatch(of:)` para encontrar algo dentro de um texto, e `wholeMatch(of:)` para verificar que um texto tem uma forma exata.

# --instructions--

Ordene os itens para construir um programa que define `isHex` antes do loop e exibe o resultado para cada valor de `values`

# --answers--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
```

```swift
let values = ["1a2b", "1z", "ff"]
```

```swift
for value in values {
    print(isHex(value))
}
```

# --solutions--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
let values = ["1a2b", "1z", "ff"]
for value in values {
    print(isHex(value))
}
```

```swift
let values = ["1a2b", "1z", "ff"]
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
for value in values {
    print(isHex(value))
}
```

# --output--

true
false
true
