---
language: swift
exerciseType: 1
---

# --description--

Como `Int(_:)` aceita uma `Substring`, transformar o texto encontrado em números é um único passo. `compactMap` é útil aqui: ele descarta os valores que retornam `nil`:
```swift
let text = "a1 b22"
let numbers = text.matches(of: #/\d+/#).compactMap { Int($0.0) }
print(numbers) // [1, 22]
```
Use `map` quando todos os elementos convertem, `compactMap` quando alguns podem falhar.

# --instructions--

Escreva uma função `numbers` que recebe um texto (`String`) sem rótulo de argumento e retorna cada sequência de dígitos que ele contém como um array de `Int`, na ordem em que aparecem

Exemplo de chamada da função:
```swift
print(numbers("a1 b22 c333"))
// prints [1, 22, 333]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func numbers() {

}
```

# --asserts--

A função deve usar `matches(of:`

```json
{
  "regex": "matches\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`numbers("a1 b22 c333")` deve retornar `[1, 22, 333]`

```swift
tryCatch(numbers("a1 b22 c333") == [1, 22, 333])
```

`numbers("7")` deve retornar `[7]`

```swift
tryCatch(numbers("7") == [7])
```

`numbers("abc")` deve retornar `[]`

```swift
tryCatch(numbers("abc") == [])
```

`numbers("10-20")` deve retornar `[10, 20]`

```swift
tryCatch(numbers("10-20") == [10, 20])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func numbers(_ text: String) -> [Int] {
    return text.matches(of: #/\d+/#).compactMap { Int($0.0) }
}
```
