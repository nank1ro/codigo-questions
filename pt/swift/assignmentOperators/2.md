---
language: swift
exerciseType: 2
---

# --description--

Já temos uma variável inicializada `total`
```swift
var total = 5
```
Digamos que queremos adicionar o número `2` à variável `total`, podemos escrever
```swift
total = total + 2
```
OK, funciona! Mas há uma versão mais curta para fazer a mesma coisa:
```swift
total += 2
```
O sinal `+=` é chamado de **atribuição de adição**.
Adiciona um valor ao valor da variável e atribui o resultado a essa variável.

# --instructions--

Adicione à variável `num` o valor `15` usando o operador de *atribuição de adição*, depois imprima seu valor

# --seed--

```swift
var num = 75
[/][/][/]
print(num)
```

# --answers--

- +
- +=
- =+
- num 
-  15

# --solutions--

```swift
var num = 75
num += 15
print(num)
```

# --output--

90
