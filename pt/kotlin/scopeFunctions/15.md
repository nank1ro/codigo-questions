---
language: kotlin
exerciseType: 3
---

# --instructions--

O que este programa imprime?

```kotlin
fun main() {
    val words = mutableListOf("a")
    val first = words.apply { add("b") }
    val second = words.run { size }
    println("$first $second")
}
```

# --answers--

- `[a, b] 2`, porque `apply` retorna a lista e `run` retorna o resultado do bloco
- `true 2`, porque `apply` retorna o resultado de `add`
- `[a, b] [a, b]`, porque ambos retornam a lista
- `2 [a, b]`, porque `apply` retorna o tamanho e `run` retorna a lista

# --solutions--

- `[a, b] 2`, porque `apply` retorna a lista e `run` retorna o resultado do bloco
