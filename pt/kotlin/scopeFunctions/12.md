---
language: kotlin
exerciseType: 4
---

# --instructions--

Você consegue ordenar estes itens para que o programa imprima a lista depois que o novo elemento for adicionado e, depois, o seu tamanho?

# --answers--

```kotlin
val words = mutableListOf("fig")
```

```kotlin
val count = words
```

```kotlin
    .apply { add("kiwi") }
```

```kotlin
    .also { println("now: $it") }
```

```kotlin
    .size
```

```kotlin
println("size: $count")
```

# --solutions--

```kotlin
val words = mutableListOf("fig")
val count = words
    .apply { add("kiwi") }
    .also { println("now: $it") }
    .size
println("size: $count")
```

# --output--

now: [fig, kiwi]
size: 2
