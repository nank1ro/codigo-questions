---
language: kotlin
exerciseType: 1
---

# --description--

Quando o bloco precisa do objeto como **argumento** de outra coisa, `also` fica mais legível que `apply`: `it` pode ser entregue diretamente, enquanto `this` teria de ser escrito por extenso.
```kotlin
val names = mutableListOf<String>()
val user = "ada".also { names.add(it) }
println(user)  // ada
println(names) // [ada]
```
O valor da expressão continua sendo `"ada"`: `also` apenas o observa passar.

# --instructions--

Declare um `val log = mutableListOf<String>()` no nível superior.

Depois escreva uma função `record(word: String): String` que retorne `word` em maiúsculas e, com `also`, adicione esse mesmo valor em maiúsculas a `log` antes de retorná-lo.

Exemplo: após `record("hi")` a função retorna `HI` e `log` contém `HI`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`log` deve começar vazio

```kotlin
    tryCatch(log.isEmpty())
```

`record("hi")` deve retornar `HI`

```kotlin
    tryCatch(record("hi") == "HI")
```

Após a primeira chamada `log` deve conter apenas `HI`

```kotlin
    tryCatch(log == listOf("HI"))
```

Uma segunda chamada deve acrescentar a `log` sem perder o primeiro valor

```kotlin
    record("bye")
    tryCatch(log == listOf("HI", "BYE"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
val log = mutableListOf<String>()

fun record(word: String): String {
    return word.uppercase().also { log.add(it) }
}
```
