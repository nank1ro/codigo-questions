---
language: kotlin
exerciseType: 1
---

# --description--

`apply` não se limita a objetos que você acabou de criar: funciona sobre qualquer objeto, e como devolve o próprio objeto você pode usar a expressão inteira onde quer que o objeto seja esperado.
```kotlin
val box = Box()
box.apply { label = "tools" }        // changes box and returns it
println(listOf(Box().apply { label = "nails" }).size) // 1
```
O bloco é um bloco de código normal, então pode conter quantas instruções você precisar.

# --instructions--

Crie uma classe `Profile` com duas propriedades, `var name: String` começando em `""` e `var age: Int` começando em `0`.

Depois escreva uma função `makeProfile(n: String, a: Int): Profile` que cria um `Profile()` e usa `apply` para definir `name` como `n` e `age` como `a`, retornando o objeto configurado.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

Um novo `Profile()` deve começar com nome vazio e idade `0`

```kotlin
    val fresh = Profile()
    tryCatch(fresh.name == "" && fresh.age == 0)
```

`makeProfile("Ada", 36)` deve ter `name` igual a `Ada`

```kotlin
    tryCatch(makeProfile("Ada", 36).name == "Ada")
```

`makeProfile("Ada", 36)` deve ter `age` igual a `36`

```kotlin
    tryCatch(makeProfile("Ada", 36).age == 36)
```

`makeProfile("Bob", 7)` deve ter `name` igual a `Bob` e `age` igual a `7`

```kotlin
    val bob = makeProfile("Bob", 7)
    tryCatch(bob.name == "Bob" && bob.age == 7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Profile {
    var name: String = ""
    var age: Int = 0
}

fun makeProfile(n: String, a: Int): Profile {
    return Profile().apply {
        name = n
        age = a
    }
}
```
