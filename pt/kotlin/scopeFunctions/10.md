---
language: kotlin
exerciseType: 3
---

# --description--

`with` faz o mesmo trabalho que `run`, mas **não** é uma extensão: o objeto é passado como o primeiro argumento em vez de ser o receptor de uma chamada com ponto.
```kotlin
val text = with(StringBuilder()) {
    append("Hello")
    append(", world")
    toString()
}
println(text) // Hello, world
```
Dentro do bloco o objeto é `this` e a chamada retorna a última expressão, exatamente como `run`. Prefira `with` quando você já tem um objeto não nulo e quer agrupar várias chamadas sobre ele; prefira `run` quando o objeto sai de uma cadeia ou pode precisar de uma chamada segura (`obj?.run { ... }`).

# --instructions--

O que este programa imprime?

```kotlin
class Point(val x: Int, val y: Int)

fun main() {
    val point = Point(2, 5)
    val result = with(point) {
        x + y
    }
    println(result)
}
```

# --answers--

- `7`, porque `with` retorna o resultado da última expressão do seu bloco
- `2`, porque `with` retorna a primeira propriedade do objeto
- O objeto `point`, porque `with` retorna o objeto que recebeu
- Nada: não compila, `with` deve ser chamado como `point.with { ... }`

# --solutions--

- `7`, porque `with` retorna o resultado da última expressão do seu bloco
