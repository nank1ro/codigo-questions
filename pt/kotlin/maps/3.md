---
language: kotlin
exerciseType: 2
---

# --description--

Quando uma chave pode estar ausente, `getOrDefault` permite escolher o valor a ser usado em vez de `null`:
```kotlin
val ages = mapOf("Alice" to 30)
println(ages.getOrDefault("Alice", 0)) // 30
println(ages.getOrDefault("Zoe", 0))   // 0
```
O primeiro argumento é a chave, o segundo é o valor padrão retornado quando a chave não é encontrada.

# --instructions--

Complete o código para imprimir o estoque de `"kiwi"`, usando `0` quando a fruta não estiver no map

# --seed--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.[/]("kiwi", [/]))
```

# --answers--

- getOrDefault
- get
- getOrNull
- 0
- null
- "kiwi"

# --solutions--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.getOrDefault("kiwi", 0))
```

# --output--

0
