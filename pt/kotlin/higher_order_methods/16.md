---
language: kotlin
exerciseType: 1
---

# --description--

Um `Map` pode ser processado como uma lista de entradas: `filter` e `map` funcionam diretamente no mapa e recebem cada entrada com `.key` e `.value`. `filter` em um mapa retorna um mapa, enquanto `map` retorna uma lista. Métodos de ordenação como `sortedBy` não são definidos em um mapa: passe primeiro por `scores.entries`, que é uma coleção das entradas:
```kotlin
val scores = mapOf("Ann" to 90, "Bob" to 72)
println(scores.filter { it.value > 80 })              // {Ann=90}
println(scores.entries.sortedBy { it.value }.map { it.key }) // [Bob, Ann]
```
`scores.entries` é o conjunto de todas as entradas; `scores.keys` e `scores.values` dão apenas um dos lados.

# --instructions--

Escreva uma função `topScorers(scores: Map<String, Int>, n: Int): List<String>` que retorne os nomes das `n` pessoas com a maior pontuação, da maior para a menor, encadeando `sortedByDescending`, `take` e `map`. Se `n` for maior que o mapa, retorne todos os nomes.

Exemplo: `topScorers(mapOf("Ann" to 90, "Bob" to 72, "Cy" to 85), 2)` retorna `[Ann, Cy]`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`topScorers(mapOf("Ann" to 90, "Bob" to 72, "Cy" to 85), 2)` deve retornar `[Ann, Cy]`

```kotlin
    tryCatch(topScorers(mapOf("Ann" to 90, "Bob" to 72, "Cy" to 85), 2) == listOf("Ann", "Cy"))
```

`topScorers(mapOf("Ann" to 90, "Bob" to 72, "Cy" to 85), 5)` deve retornar `[Ann, Cy, Bob]`

```kotlin
    tryCatch(topScorers(mapOf("Ann" to 90, "Bob" to 72, "Cy" to 85), 5) == listOf("Ann", "Cy", "Bob"))
```

`topScorers(mapOf("Ann" to 90, "Bob" to 72), 0)` deve retornar uma lista vazia

```kotlin
    tryCatch(topScorers(mapOf("Ann" to 90, "Bob" to 72), 0) == listOf<String>())
```

`topScorers(mapOf("Zed" to 10, "Yan" to 20), 1)` deve retornar `[Yan]`

```kotlin
    tryCatch(topScorers(mapOf("Zed" to 10, "Yan" to 20), 1) == listOf("Yan"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun topScorers(scores: Map<String, Int>, n: Int): List<String> {
    return scores.entries.sortedByDescending { it.value }.take(n).map { it.key }
}
```
