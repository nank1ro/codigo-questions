---
language: kotlin
exerciseType: 2
---

# --description--

Em uma cadeia, o tipo de `it` muda a cada passo: depois de `filter` em uma `List<String>` você ainda tem strings, mas depois de `map { it.length }` você tem uma `List<Int>`, então a próxima lambda vê números.
```kotlin
val words = listOf("kiwi", "fig")
println(words.map { it.length }.filter { it > 3 }) // [4]
```
Cada passo retorna uma lista **nova** e nunca toca na anterior, então uma cadeia pode ser dividida em valores intermediários nomeados sem mudar o resultado.

# --instructions--

Complete o código para que `result` contenha as frutas que começam com `a`, em maiúsculas, de modo que o programa imprima `[APPLE, AVOCADO]`

# --seed--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.[/] { it.startsWith("a") }.[/] { it.[/] }
println(result)
```

# --answers--

- filter
- map
- uppercase()
- forEach
- count
- lowercase()
- length

# --solutions--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.filter { it.startsWith("a") }.map { it.uppercase() }
println(result)
```

# --output--

[APPLE, AVOCADO]
