---
language: kotlin
exerciseType: 1
---

# --description--

Lançar e capturar não é gratuito, e para as conversões comuns o Kotlin oferece uma variante mais barata que simplesmente retorna `null` em vez de lançar: `toIntOrNull()`, `toDoubleOrNull()`, `toLongOrNull()`.

```kotlin
println("42".toIntOrNull())  // 42
println("abc".toIntOrNull()) // null
```
Combinado com o operador elvis `?:`, que fornece um substituto quando o valor à sua esquerda é `null`, todo o `try`/`catch` colapsa em uma linha:
```kotlin
val n = "abc".toIntOrNull() ?: 0
println(n) // 0
```
Use `try`/`catch` quando a falha for genuinamente excepcional; use `toIntOrNull()` quando uma entrada inválida é esperada.

# --instructions--

Escreva uma função `safeSum(a: String, b: String): Int` que retorna a soma dos dois números, contando qualquer texto que não é um número como `0`. Use `toIntOrNull()` e o operador elvis, não `try`/`catch`.

Exemplo de chamada da função:
```kotlin
println(safeSum("2", "x"))
// imprime 2
```

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --seed--

```kotlin
fun safeSum(a: String, b: String): Int {
    
}
```

# --asserts--

`safeSum("2", "3")` deve retornar `5`

```kotlin
    tryCatch(safeSum("2", "3") == 5)
```

`safeSum("2", "x")` deve retornar `2`

```kotlin
    tryCatch(safeSum("2", "x") == 2)
```

`safeSum("a", "b")` deve retornar `0`

```kotlin
    tryCatch(safeSum("a", "b") == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun safeSum(a: String, b: String): Int {
    return (a.toIntOrNull() ?: 0) + (b.toIntOrNull() ?: 0)
}
```
