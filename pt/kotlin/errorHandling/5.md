---
language: kotlin
exerciseType: 2
---

# --description--

Um bloco `finally` pode ser adicionado no final. Ele executa **independente do que aconteça**: depois de um `try` bem-sucedido, depois de um `catch` ter recuperado, e até mesmo quando a exceção não é capturada de forma alguma.

```kotlin
try {
    println("reading")
    println("abc".toInt())
} catch (e: NumberFormatException) {
    println("bad number")
} finally {
    println("closing")
}
```
```
reading
bad number
closing
```
Isso faz dele o lugar para limpeza que não deve ser ignorada, como fechar um arquivo. Um `try` precisa de pelo menos um `catch` ou um `finally`, mas pode ter ambos.

# --instructions--

Complete o código para que a conversão inválida seja capturada e a linha de limpeza sempre seja executada.

# --seed--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } [/] (e: NumberFormatException) {
        println("bad number")
    } [/] {
        println("closing")
    }
}
```

# --answers--

- catch
- finally
- try
- throw
- else
- do

# --solutions--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } catch (e: NumberFormatException) {
        println("bad number")
    } finally {
        println("closing")
    }
}
```

# --output--

bad number
closing
