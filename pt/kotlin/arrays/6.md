---
language: kotlin
exerciseType: 2
---

# --description--

Imprimir um array diretamente não mostra seus elementos, imprime algo como `[Ljava.lang.String;@1b6d3586`.
Use `joinToString` para construir uma string legível, opcionalmente com um separador customizado (o padrão é `", "`), ou `contentToString` para obter os elementos entre colchetes:
```kotlin
val nums = intArrayOf(1, 2, 3)
println(nums.joinToString())      // 1, 2, 3
println(nums.joinToString("-"))   // 1-2-3
println(nums.contentToString())   // [1, 2, 3]
```

# --instructions--

Complete o código para imprimir as frutas separadas por `" - "`, depois o array inteiro entre colchetes

# --seed--

```kotlin
val fruits = arrayOf("apple", "kiwi", "plum")
println(fruits.[/]([/]))
println(fruits.[/]())
```

# --answers--

- joinToString
- contentToString
- toString
- join
- " - "
- "-"

# --solutions--

```kotlin
val fruits = arrayOf("apple", "kiwi", "plum")
println(fruits.joinToString(" - "))
println(fruits.contentToString())
```

# --output--

apple - kiwi - plum
[apple, kiwi, plum]
