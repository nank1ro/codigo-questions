---
language: kotlin
exerciseType: 2
---

# --description--

Um **array** armazena um número fixo de valores do mesmo tipo sob um único nome de variável.
Você cria um com `arrayOf`, lê um elemento com colchetes e um **índice** que começa em `0`, e obtém o número de elementos com `size`:
```kotlin
val colors = arrayOf("red", "green", "blue")
println(colors[0])   // red
println(colors.size) // 3
```
O último elemento está no índice `size - 1`.

# --instructions--

Complete o código para criar o array com `arrayOf`, imprimir seu número de elementos e depois seu último elemento

# --seed--

```kotlin
val numbers = [/](3, 5, 7)
println(numbers.[/])
println(numbers[[/]])
```

# --answers--

- arrayOf
- Array
- size
- length
- count
- 2
- 3

# --solutions--

```kotlin
val numbers = arrayOf(3, 5, 7)
println(numbers.size)
println(numbers[2])
```

# --output--

3
7
