---
language: kotlin
exerciseType: 2
---

# --description--

Todo array tem duas propriedades úteis para trabalhar com índices:
- `indices` é o intervalo de índices válidos, de `0` até o último
- `lastIndex` é o índice do último elemento, ou seja, `size - 1`
```kotlin
val letters = arrayOf("x", "y")
println(letters.lastIndex) // 1
for (i in letters.indices) {
    println(letters[i])
}
// prints x, then y
```

# --instructions--

Complete o código para imprimir o último índice de `letters`, e depois cada índice seguido de seu elemento

# --seed--

```kotlin
val letters = arrayOf("a", "b", "c")
println(letters.[/])
for (i in letters.[/]) {
    println("$i: ${letters[i]}")
}
```

# --answers--

- lastIndex
- indices
- size
- length
- last

# --solutions--

```kotlin
val letters = arrayOf("a", "b", "c")
println(letters.lastIndex)
for (i in letters.indices) {
    println("$i: ${letters[i]}")
}
```

# --output--

2
0: a
1: b
2: c
