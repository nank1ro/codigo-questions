---
language: kotlin
exerciseType: 4
---

# --description--

Uma função de extensão pode ser declarada com um **receptor nullable**, então ela pode ser chamada mesmo em um valor `null`. Dentro dela, `this` é nullable e deve ser verificado:
```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val none: String? = null
println(none.orDash()) // -
```
Note que nenhum `?.` é necessário no ponto de chamada: a própria função trata o caso `null`.
A biblioteca padrão usa esse truque em `isNullOrEmpty()` e `orEmpty()`, que podem ser chamadas com segurança em qualquer `String?`.

# --instructions--

Você consegue ordenar estas linhas para definir a função de extensão `orDash` e imprimir `-` para um nome ausente, depois `kotlin` para um presente? Defina a função primeiro

# --answers--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
```

```kotlin
val a: String? = null
val b: String? = "kotlin"
```

```kotlin
println(a.orDash())
```

```kotlin
println(b.orDash())
```

# --solutions--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val a: String? = null
val b: String? = "kotlin"
println(a.orDash())
println(b.orDash())
```

# --output--

-
kotlin
