---
language: kotlin
exerciseType: 4
---

# --description--

Cadeias de chamadas seguras brilham com objetos aninhados, onde qualquer nível pode estar ausente:
```kotlin
class Address(val city: String?)
class User(val address: Address?)

val user = User(Address("Rome"))
println(user.address?.city ?: "unknown") // Rome
val nobody = User(null)
println(nobody.address?.city ?: "unknown") // unknown
```
Cada `?.` protege o passo seguinte, e o `?:` final fornece o padrão.

# --instructions--

Você consegue ordenar estas linhas para imprimir `unknown` para um usuário sem endereço? Defina as classes primeiro, `Address` antes de `User`

# --answers--

```kotlin
class Address(val city: String?)
```

```kotlin
class User(val address: Address?)
```

```kotlin
val bob = User(null)
```

```kotlin
println(bob.address?.city ?: "unknown")
```

# --solutions--

```kotlin
class Address(val city: String?)
class User(val address: Address?)
val bob = User(null)
println(bob.address?.city ?: "unknown")
```

# --output--

unknown
