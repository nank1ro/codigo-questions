---
language: kotlin
exerciseType: 2
---

# --description--

Coleções também podem conter elementos nullable: uma `List<Int?>` pode conter entradas `null`, enquanto uma `List<Int>` nunca contém.
O `filterNotNull()` retorna uma nova lista com as entradas `null` removidas, e o tipo de seus elementos se torna não nulo, então você pode usar os elementos livremente:
```kotlin
val scores: List<Int?> = listOf(10, null, 20)
val valid = scores.filterNotNull() // List<Int> [10, 20]
println(valid.sum())               // 30
```

# --instructions--

Complete o código para declarar uma lista que pode conter números `null`, depois remova as entradas `null` e imprima os valores restantes e sua soma

# --seed--

```kotlin
val values: List<[/]> = listOf(3, null, 5, null)
val present = values.[/]()
println(present)
println(present.sum())
```

# --answers--

- Int?
- Int
- filterNotNull
- filter
- mapNotNull
- requireNotNull

# --solutions--

```kotlin
val values: List<Int?> = listOf(3, null, 5, null)
val present = values.filterNotNull()
println(present)
println(present.sum())
```

# --output--

[3, 5]
8
