---
language: kotlin
exerciseType: 2
---

# --description--

Toda classe enum expõe uma propriedade `entries`: uma lista de todas as suas entradas na ordem de declaração. É útil para iterar:
```kotlin
for (direction in Direction.entries) {
    println(direction.name)
}
// NORTH
// SOUTH
// EAST
// WEST
```
Por ser uma lista, `entries` também suporta `size` e indexação, por exemplo `Direction.entries[0]` é `NORTH`.

Código mais antigo usa a função `values()` em vez disso, que retorna um array; `entries` é a escolha recomendada desde o Kotlin 1.9.

# --instructions--

Complete o laço para que ele imprima o ordinal e o nome de cada entrada de `Size`, separados por um espaço

# --seed--

```kotlin
enum class Size {
    SMALL, MEDIUM, LARGE
}

fun main() {
    for (size in Size.[/]) {
        println("${size.[/]} ${size.[/]}")
    }
}
```

# --answers--

- entries
- cases
- list
- ordinal
- name
- index
- value

# --solutions--

```kotlin
enum class Size {
    SMALL, MEDIUM, LARGE
}

fun main() {
    for (size in Size.entries) {
        println("${size.ordinal} ${size.name}")
    }
}
```

# --output--

0 SMALL
1 MEDIUM
2 LARGE
