---
language: kotlin
exerciseType: 1
---

# --description--

Para ir de uma `String` de volta a uma entrada, use a função `valueOf`. Ela procura a entrada cujo `name` corresponde exatamente:
```kotlin
val direction = Direction.valueOf("EAST")
println(direction == Direction.EAST) // true
```
A correspondência diferencia maiúsculas de minúsculas: `Direction.valueOf("east")` lança uma `IllegalArgumentException` porque nenhuma entrada tem esse nome.

# --instructions--

Declare uma classe enum `Status` com as entradas `ACTIVE` e `INACTIVE`. Depois escreva uma função `statusOf(text: String)` que retorna o `Status` correspondente a `text`, independente de maiúsculas ou minúsculas (assim `"active"`, `"Active"` e `"ACTIVE"` resultam todos em `Status.ACTIVE`).

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`statusOf("ACTIVE")` deve retornar `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("ACTIVE") == Status.ACTIVE)
```

`statusOf("inactive")` deve retornar `Status.INACTIVE`

```kotlin
    tryCatch(statusOf("inactive") == Status.INACTIVE)
```

`statusOf("Active")` deve retornar `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("Active") == Status.ACTIVE)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
enum class Status {
    ACTIVE, INACTIVE
}

fun statusOf(text: String): Status {
    return Status.valueOf(text.uppercase())
}
```
