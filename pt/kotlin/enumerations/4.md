---
language: kotlin
exerciseType: 2
---

# --description--

Uma classe enum pode ter um **construtor**, assim como uma classe comum. Cada entrada então passa seus próprios argumentos, e os valores são armazenados em propriedades:
```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
println(Planet.MARS.moons) // 2
```
As propriedades do construtor geralmente são declaradas com `val`, já que os dados de uma entrada não devem mudar.

# --instructions--

Complete o código para que toda `Coin` armazene seu valor em centavos, e então imprima o valor de uma `PENNY` (que vale `1` centavo)

# --seed--

```kotlin
enum class Coin([/] cents: Int) {
    PENNY([/]),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.[/])
}
```

# --answers--

- val
- fun
- const
- 1
- "1"
- cents
- name
- ordinal

# --solutions--

```kotlin
enum class Coin(val cents: Int) {
    PENNY(1),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.cents)
}
```

# --output--

1
