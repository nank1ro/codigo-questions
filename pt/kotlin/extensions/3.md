---
language: kotlin
exerciseType: 1
---

# --description--

Uma função de extensão pode receber parâmetros como qualquer outra função. O receptor fica à esquerda do ponto e os parâmetros vão entre os parênteses:
```kotlin
fun Int.isDivisibleBy(other: Int): Boolean {
    return this % other == 0
}
println(12.isDivisibleBy(4)) // true
println(12.isDivisibleBy(5)) // false
```
O tipo antes do ponto é um tipo normal, então você pode estender `List<Int>`, `Double` ou uma classe que você mesmo escreveu da mesma forma.

# --instructions--

Escreva uma função de extensão `isBetween(low: Int, high: Int)` em `Int` que retorne `true` quando o receptor for maior ou igual a `low` e menor ou igual a `high`.

Exemplo: `5.isBetween(1, 10)` retorna `true`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`5.isBetween(1, 10)` deve retornar `true`

```kotlin
    tryCatch(5.isBetween(1, 10) == true)
```

`15.isBetween(1, 10)` deve retornar `false`

```kotlin
    tryCatch(15.isBetween(1, 10) == false)
```

`10.isBetween(1, 10)` deve retornar `true`

```kotlin
    tryCatch(10.isBetween(1, 10) == true)
```

`0.isBetween(1, 10)` deve retornar `false`

```kotlin
    tryCatch(0.isBetween(1, 10) == false)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun Int.isBetween(low: Int, high: Int): Boolean {
    return this >= low && this <= high
}
```
