---
language: kotlin
exerciseType: 2
---

# --instructions--

Complete o código para adicionar um `companion object` dentro de `AppConfig` que contenha uma propriedade `val` chamada `version` igual a `"1.0"`.

# --seed--

```kotlin
class AppConfig {
    [/] [/] {
        val version = "1.0"
    }
}
```

# --answers--

- companion
- object
- class
- static
- init
- val

# --solutions--

```kotlin
class AppConfig {
    companion object {
        val version = "1.0"
    }
}
```
