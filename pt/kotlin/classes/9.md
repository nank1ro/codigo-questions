---
language: kotlin
exerciseType: 2
---

# --instructions--

Complete o código para declarar uma `data class` chamada `Color` com propriedades `val` chamadas `red`, `green` e `blue` do tipo `Int`.

# --seed--

```kotlin
[/] [/] Color(val red: Int, val green: Int, val blue: Int)
```

# --answers--

- data
- class
- object
- open
- abstract
- sealed

# --solutions--

```kotlin
data class Color(val red: Int, val green: Int, val blue: Int)
```
