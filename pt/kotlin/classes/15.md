---
language: kotlin
exerciseType: 4
---

# --instructions--

Você consegue ordenar estas linhas para definir uma classe `open` chamada `Shape` com um método `open` chamado `area()`, uma subclasse `Square` que o substitui, e imprimir a área?

# --answers--

```kotlin
open class Shape {
```

```kotlin
    open fun area(): Int = 0
```

```kotlin
}
```

```kotlin
class Square(val side: Int) : Shape() {
```

```kotlin
    override fun area(): Int = side * side
```

```kotlin
}
```

```kotlin
fun main() {
```

```kotlin
    println(Square(4).area())
```

```kotlin
}
```

# --solutions--

```kotlin
open class Shape {
    open fun area(): Int = 0
}
class Square(val side: Int) : Shape() {
    override fun area(): Int = side * side
}
fun main() {
    println(Square(4).area())
}
```

# --output--

16
