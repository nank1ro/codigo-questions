---
language: kotlin
exerciseType: 2
---

# --description--

Uma `String` é uma sequência de caracteres escrita entre aspas duplas.
A propriedade `length` informa quantos caracteres uma string contém, incluindo espaços:
```kotlin
val greeting = "Hello world"
println(greeting.length) // 11
println("".length)       // 0
```

# --instructions--

Complete o código para imprimir o número de caracteres em `word`

# --seed--

```kotlin
val word = "Kotlin"
println(word.[/])
```

# --answers--

- length
- size
- len
- length()
- count

# --solutions--

```kotlin
val word = "Kotlin"
println(word.length)
```

# --output--

6
