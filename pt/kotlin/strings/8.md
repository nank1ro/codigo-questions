---
language: kotlin
exerciseType: 4
---

# --description--

Como `split` retorna uma `List`, você pode percorrer seus elementos como qualquer outra lista:
```kotlin
for (part in "a-b".split("-")) {
    println(part)
}
// prints a, then b
```

# --instructions--

Você consegue ordenar estas linhas para imprimir cada palavra de `sentence` em maiúsculas, uma por linha?

# --answers--

```kotlin
val sentence = "the quick brown fox"
```

```kotlin
val words = sentence.split(" ")
```

```kotlin
for (word in words) {
```

```kotlin
    println(word.uppercase())
```

```kotlin
}
```

# --solutions--

```kotlin
val sentence = "the quick brown fox"
val words = sentence.split(" ")
for (word in words) {
    println(word.uppercase())
}
```

# --output--

THE
QUICK
BROWN
FOX
