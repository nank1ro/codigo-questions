---
language: kotlin
exerciseType: 1
---

# --description--

`repeat(n)` retorna a string concatenada `n` vezes:
```kotlin
println("ab".repeat(3)) // ababab
```
`padStart(width, char)` adiciona `char` no início até que a string atinja `width` caracteres; `padEnd` os adiciona no final.
Se a string já for longa o suficiente, ela é retornada sem alterações:
```kotlin
println("7".padStart(3, '0'))   // 007
println("hi".padEnd(5, '.'))    // hi...
println("1234".padStart(2, '0')) // 1234
```
Números não são strings: chame `toString()` primeiro, como em `42.toString().padStart(4, '0')`.

# --instructions--

Escreva uma função `padNumber(n: Int, width: Int): String` que retorna `n` como uma string, preenchida com `'0'` no início até ter `width` caracteres.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`padNumber(7, 3)` deve retornar `"007"`

```kotlin
    tryCatch(padNumber(7, 3) == "007")
```

`padNumber(42, 5)` deve retornar `"00042"`

```kotlin
    tryCatch(padNumber(42, 5) == "00042")
```

`padNumber(1234, 2)` deve retornar `"1234"`

```kotlin
    tryCatch(padNumber(1234, 2) == "1234")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun padNumber(n: Int, width: Int): String {
    return n.toString().padStart(width, '0')
}
```
