---
language: kotlin
exerciseType: 2
---

# --description--

`split` divide uma string em uma `List` de pedaços em torno de um separador, enquanto `joinToString` faz o oposto: ela une os elementos de uma coleção em uma única string com o separador que você escolher:
```kotlin
val csv = "a,b,c"
val parts = csv.split(",")
println(parts)                  // [a, b, c]
println(parts.joinToString("/")) // a/b/c
```

# --instructions--

Complete o código para dividir `csv` na vírgula e imprimir as cores separadas por `" - "`

# --seed--

```kotlin
val csv = "red,green,blue"
val colors = csv.[/](",")
println(colors.[/](" - "))
```

# --answers--

- split
- joinToString
- join
- separate
- substring

# --solutions--

```kotlin
val csv = "red,green,blue"
val colors = csv.split(",")
println(colors.joinToString(" - "))
```

# --output--

red - green - blue
