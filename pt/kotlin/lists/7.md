---
language: kotlin
exerciseType: 2
---

# --description--

Uma lista mutável não precisa ter um tamanho fixo.
Você pode adicionar itens ao final de uma lista quando quiser!
Para adicionar um item a uma lista mutável, usamos a função `add` ou o atalho `+=`:
```kotlin
val letters = mutableListOf("a", "b")
letters.add("c")
println(letters)
// prints [a, b, c]
```

# --instructions--

Complete o código para adicionar mais três itens à lista `numbers`, na seguinte ordem: `6`, depois `8` e finalmente `9`

# --seed--

```kotlin
val numbers = mutableListOf(1, 3, 9)
numbers.[/]([/])
numbers.add([/])
[/].add(9)
println(numbers)
```

# --answers--

- append
- add
- 6
- 8
- numbers

# --solutions--

```kotlin
val numbers = mutableListOf(1, 3, 9)
numbers.add(6)
numbers.add(8)
numbers.add(9)
println(numbers)
```

# --output--

[1, 3, 9, 6, 8, 9]
