---
language: kotlin
exerciseType: 2
---

# --description--

Acabamos de ver como executar um bloco de código se uma condição ocorrer, agora vamos ver como executar outro bloco de código se a primeira condição falhar.
Vamos brincar ao ar livre se o clima estiver bom; caso contrário, ficamos em casa.
Em Kotlin podemos usar a instrução `else`, assim:
```kotlin
var niceWeather = true
if (niceWeather) {
    // brincar lá fora
} else {
    // ficar em casa
}
```

# --instructions--

Crie uma instrução `else` e no bloco de código imprima a string `"It's afternoon"`

# --seed--

```kotlin
var time = 15
if (time < 12) {
    println("It's morning")
} [/][/]
    println("It's afternoon")
}
```

# --answers--

- It's afternoon
- 12
- else
-  < 
-  > 
- :
- ;
-  {

# --solutions--

```kotlin
var time = 15
if (time < 12) {
    println("It's morning")
} else {
    println("It's afternoon")
}
```

# --output--

It's afternoon
