---
language: kotlin
exerciseType: 1
---

# --description--

Um `companion object` é o singleton que pertence a uma classe. Além de constantes, o seu trabalho natural é armazenar **funções de fábrica**: funções que verificam ou transformam a entrada antes de construir uma instância, e que podem retornar `null` quando a entrada não faz sentido.

Marcar o construtor como `private` obriga todo chamador a passar pela fábrica:

```kotlin
class Age private constructor(val years: Int) {
    companion object {
        fun of(years: Int): Age? = if (years >= 0) Age(years) else null
    }
}

println(Age.of(30)?.years) // 30
println(Age.of(-1))        // null
```

O companion é chamado no nome da classe, `Age.of(...)`, e ele pode acessar o construtor privado porque vive dentro da classe.

# --instructions--

Declare uma classe `Temperature` com um construtor **privado** que recebe uma propriedade `val` `celsius` do tipo `Int`.

Adicione um companion object com uma função de fábrica `fromCelsius(value: Int): Temperature?` que retorna um `Temperature` quando `value` é pelo menos `-273`, e `null` caso contrário.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`Temperature.fromCelsius(20)` deve ter `20` graus

```kotlin
    tryCatch(Temperature.fromCelsius(20)?.celsius == 20)
```

`Temperature.fromCelsius(-273)` deve ser aceito

```kotlin
    tryCatch(Temperature.fromCelsius(-273)?.celsius == -273)
```

`Temperature.fromCelsius(-300)` deve retornar `null`

```kotlin
    tryCatch(Temperature.fromCelsius(-300) == null)
```

`Temperature.fromCelsius(0)` deve ser aceito

```kotlin
    tryCatch(Temperature.fromCelsius(0)?.celsius == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Temperature private constructor(val celsius: Int) {
    companion object {
        fun fromCelsius(value: Int): Temperature? {
            return if (value >= -273) Temperature(value) else null
        }
    }
}
```
