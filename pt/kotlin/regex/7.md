---
language: kotlin
exerciseType: 1
---

# --description--

`replace` reescreve o texto: ele retorna uma string **nova** onde cada correspondência é trocada pela substituição, deixando o original intacto.
```kotlin
println(Regex("""\d+""").replace("a1 b22", "#")) // a# b#
```
Na string de substituição, `$1`, `$2`, ... representam os grupos capturados daquela correspondência, então você pode reordenar ou reutilizar as partes que correspondeu:
```kotlin
val name = Regex("""(\w+) (\w+)""")
println(name.replace("Ann Lee", "$2 $1")) // Lee Ann
```
`$0` é a correspondência inteira. Se você precisar de um `$` literal na substituição, escape-o como `\$`.
`replace` reescreve **todas** as correspondências, então quando apenas uma string completa deve ser reescrita, ancore o padrão com as **âncoras** `^` (início do texto) e `$` (fim do texto):
```kotlin
println(Regex("""^\w+$""").replace("one two", "x")) // one two, nothing is replaced
```

# --instructions--

Escreva uma função `lastNameFirst(name: String): String` que transforma `"Ann Lee"` em `"Lee, Ann"`, usando `replace` com referências a grupos. Quando `name` não for duas palavras separadas por um espaço, retorne-o inalterado.

Exemplo: `lastNameFirst("Ann Lee")` retorna `Lee, Ann`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`lastNameFirst("Ann Lee")` deve retornar `Lee, Ann`

```kotlin
    tryCatch(lastNameFirst("Ann Lee") == "Lee, Ann")
```

`lastNameFirst("Marco Rossi")` deve retornar `Rossi, Marco`

```kotlin
    tryCatch(lastNameFirst("Marco Rossi") == "Rossi, Marco")
```

`lastNameFirst("Cher")` deve retornar `Cher`

```kotlin
    tryCatch(lastNameFirst("Cher") == "Cher")
```

`lastNameFirst("")` deve retornar uma string vazia

```kotlin
    tryCatch(lastNameFirst("") == "")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun lastNameFirst(name: String): String {
    return Regex("""^(\w+) (\w+)$""").replace(name, "$2, $1")
}
```
