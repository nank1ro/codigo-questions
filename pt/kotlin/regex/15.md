---
language: kotlin
exerciseType: 4
---

# --description--

Como `findAll` dá uma sequência, os métodos de agregação que você já conhece funcionam em correspondências também: `sumOf`, `maxOfOrNull`, `filter`, `sortedBy`. Extrair números de texto livre é um trabalho de duas etapas: correspondê-los e depois converter o texto em um número.
```kotlin
val total = Regex("""\d+""").findAll("2 apples, 3 pears").sumOf { it.value.toInt() }
println(total) // 5
```

# --instructions--

Você consegue ordenar estes itens para imprimir `total: 17`, somando todos os números do texto?

# --answers--

```kotlin
val text = "ann scored 8 and bob scored 9"
```

```kotlin
val numbers = Regex("""\d+""").findAll(text)
```

```kotlin
val total = numbers.sumOf { it.value.toInt() }
```

```kotlin
println("total: $total")
```

# --solutions--

```kotlin
val text = "ann scored 8 and bob scored 9"
val numbers = Regex("""\d+""").findAll(text)
val total = numbers.sumOf { it.value.toInt() }
println("total: $total")
```

# --output--

total: 17
