---
language: kotlin
exerciseType: 1
---

# --description--

Um comentário não precisa de uma linha própria: ele pode vir depois do código na mesma linha. Este é um **comentário de fim de linha**, e é um bom lugar para uma nota curta sobre aquela instrução específica:
```kotlin
val retries = 3 // give up after three attempts
```
Tudo, de `//` até o fim da linha, é ignorado, enquanto o código antes dele roda como de costume.

# --instructions--

Adicione um comentário de fim de linha depois de `val seats = 4` explicando o valor, por exemplo `// seats per table`

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --seed--

```kotlin
val seats = 4
```

# --asserts--

A linha `val seats = 4` deve terminar com um comentário `//`

```json
{
  "regex": "val seats = 4[ \\t]*//[ \\t]*\\S",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`seats` ainda deve ser igual a `4`

```kotlin
    tryCatch(seats == 4)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
val seats = 4 // seats per table
```
