---
language: kotlin
exerciseType: 4
---

# --description--

Depois do resumo, um comentário de documentação pode descrever os parâmetros e o valor retornado com **tags KDoc**, que sempre começam com `@`:
```kotlin
/**
 * Returns the number of seconds in the given minutes.
 * @param minutes a whole number of minutes, never negative
 * @return [minutes] multiplied by sixty
 */
fun toSeconds(minutes: Int): Int {
    return minutes * 60
}
```
`@param` é seguido pelo nome do parâmetro e depois pela sua descrição; há um `@param` por parâmetro. `@return` descreve o valor que a função devolve. A ordem é sempre a mesma: primeiro o resumo, depois as tags `@param`, depois `@return`.

# --instructions--

Ordene os itens para documentar `twice` com um resumo, depois uma tag `@param`, depois uma tag `@return`, tudo dentro de um único comentário de documentação colocado diretamente acima da função, e depois imprima `twice(21)`

# --answers--

```kotlin
/**
 * Returns the double of a number.
```

```kotlin
 * @param n the number to double
```

```kotlin
 * @return [n] multiplied by two
```

```kotlin
 */
```

```kotlin
fun twice(n: Int): Int {
    return n * 2
}
```

```kotlin
println(twice(21))
```

# --solutions--

```kotlin
/**
 * Returns the double of a number.
 * @param n the number to double
 * @return [n] multiplied by two
 */
fun twice(n: Int): Int {
    return n * 2
}
println(twice(21))
```

# --output--

42
