---
language: kotlin
exerciseType: 2
---

# --description--

Uma função com mais de um parâmetro recebe uma tag `@param` para cada um deles, escritas na mesma ordem dos parâmetros:
```kotlin
/**
 * Returns the area of a rectangle.
 * @param width the horizontal side, in centimetres
 * @param height the vertical side, in centimetres
 * @return the product of [width] and [height]
 */
fun area(width: Int, height: Int): Int {
    return width * height
}
```
Uma tag ainda é apenas um comentário: se você renomear um parâmetro e esquecer a tag, nada quebra, mas a documentação começa a mentir. Atualize o KDoc junto com a assinatura.

# --instructions--

Complete a documentação de `price`: a primeira tag deve descrever o parâmetro `quantity`, a segunda o parâmetro `unitPrice`, e a terceira o valor que a função retorna

# --seed--

```kotlin
/**
 * Returns the total price of an order.
 * [/] quantity how many items were ordered
 * [/] unitPrice the price of a single item
 * [/] the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --answers--

- @param
- @param
- @return
- @throws
- @property

# --solutions--

```kotlin
/**
 * Returns the total price of an order.
 * @param quantity how many items were ordered
 * @param unitPrice the price of a single item
 * @return the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --output--

15
