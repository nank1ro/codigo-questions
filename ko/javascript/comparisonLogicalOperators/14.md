---
language: javascript
exerciseType: 2
---

# --description--

`NaN`이 관련된 비교는 반대되는 것끼리도 항상 `false`를 반환합니다: `NaN < 3`과 `NaN >= 3` 모두 `false`이므로, `<`가 실패했다고 해서 항상 왼쪽 값이 더 크다는 뜻은 아닙니다.

# --instructions--

`num`이 `3`보다 **작은지** 확인하고 불리언 결과를 출력하세요

# --seed--

```javascript
var num = 5;
console.log([/][/][/]);
```

# --answers--

- num 
- 'num' 
- == 
- < 
- num 
- <= 
- >= 
- 3

# --solutions--

```javascript
var num = 5;
console.log(num < 3);
```

# --output--

false
