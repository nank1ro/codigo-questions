---
language: javascript
exerciseType: 2
---

# --description--

한쪽이 문자열이고 다른 쪽이 숫자일 때, `<=`는 먼저 문자열을 숫자로 변환합니다. 그래서 `"7" <= 8`은 `true`입니다.

# --instructions--

`num`이 `8` **이하인지** 확인하고 불리언 결과를 출력하세요

# --seed--

```javascript
var num = 7;
console.log([/][/][/]);
```

# --answers--

- num
- 'num'
-  <= 
-  = 
-  >= 
- 8

# --solutions--

```javascript
var num = 7;
console.log(num <= 8);
```

# --output--

true
