---
language: javascript
exerciseType: 4
---

# --description--

`test`는 맞는지 아닌지만 알려줍니다. 일치한 텍스트 그 자체를 얻으려면 문자열에 대해 **`match`**를 호출하세요:
```javascript
const match = "order 42 shipped".match(/\d+/);
```
아무것도 일치하지 않으면 `match`는 `null`을 반환합니다. 일치하는 것이 있으면 배열과 비슷한 결과를 반환합니다:
- `match[0]`은 일치한 텍스트입니다
- `match.index`는 일치가 시작되는 위치입니다
- `match.input`은 검색한 전체 문자열입니다

```javascript
console.log(match[0]);
// prints 42
console.log(match.index);
// prints 6
```
결과가 `null`일 수 있으므로 `match[0]`을 읽기 전에 확인하세요.

# --instructions--

텍스트의 첫 번째 숫자 연속과 그것이 시작되는 위치를 출력하는 프로그램이 되도록 항목을 정렬하세요. 텍스트를 먼저 선언하고 그다음 일치 결과를 선언하세요

# --answers--

```javascript
const text = "order 42 shipped";
```

```javascript
const match = text.match(/\d+/);
```

```javascript
console.log(match[0]);
```

```javascript
console.log(match.index);
```

# --solutions--

```javascript
const text = "order 42 shipped";
const match = text.match(/\d+/);
console.log(match[0]);
console.log(match.index);
```

# --output--

42
6
