---
language: javascript
exerciseType: 1
---

# --description--

필요한 것만 캡처하세요. 그룹은 조각을 다시 읽는 방법일 뿐만 아니라, 패턴의 어느 부분이 중요한지 읽는 이에게 알려 줍니다. 분만 원하는 시각 패턴에서는 분만 그룹으로 감싸고 나머지는 그룹 없이 두세요:
```javascript
const minutes = "at 14:35:02".match(/\d{2}:(\d{2}):\d{2}/)?.[1];
console.log(minutes);
// 35 출력
```
패턴 전체는 여전히 일치해야 하므로 시와 초도 여전히 필요합니다. 단지 캡처되지 않을 뿐입니다. 그룹이 적으면 `match[1]`, `match[2]` 등을 읽을 때 짚어 둘 번호도 적어집니다.

# --instructions--

문자열 `date`를 받아 그 월을 문자열로 반환하는 함수 `monthOf`를 작성하세요. `date`에는 `YYYY-MM-DD`로 쓰인 날짜가 다른 텍스트에 둘러싸인 채 포함될 수 있고, 그런 날짜가 없으면 빈 문자열을 반환합니다. 예를 들어 `monthOf("2026-09-12")`는 `09`를 반환합니다

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function monthOf(date) {
  
}
```

# --asserts--

`monthOf("2026-09-12")`는 `09`를 반환해야 합니다.

```javascript
tryCatch(monthOf("2026-09-12") === "09");
```

`monthOf("born on 1999-12-31")`는 `12`를 반환해야 합니다.

```javascript
tryCatch(monthOf("born on 1999-12-31") === "12");
```

`monthOf("no date")`는 빈 문자열을 반환해야 합니다.

```javascript
tryCatch(monthOf("no date") === "");
```

`monthOf("12-31-1999")`는 빈 문자열을 반환해야 합니다.

```javascript
tryCatch(monthOf("12-31-1999") === "");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function monthOf(date) {
  return date.match(/\d{4}-(\d{2})-\d{2}/)?.[1] ?? "";
}
```
