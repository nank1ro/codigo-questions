---
language: javascript
exerciseType: 4
---

# --description--

치환은 문자열일 필요가 없습니다. **함수**를 넘기면 일치마다 한 번씩 호출되고, 그것이 반환하는 것은 무엇이든 그 일치 자리에 삽입됩니다.

함수는 전체 일치를 먼저 받고, 그다음 각 캡처 그룹을 받습니다:
```javascript
console.log("hello world".replace(/\w+/g, (word) => word.length));
// 5 5 출력

console.log("ann lee".replace(/(\w)(\w*)/g, (whole, first, rest) => first.toUpperCase() + rest));
// Ann Lee 출력
```
이것이 일치한 텍스트로부터 치환을 계산하는 유일한 방법이며, `$1`만으로는 할 수 없는 일입니다.

# --instructions--

모든 숫자를 그 두 배로 바꾼 텍스트를 출력하는 프로그램이 되도록 항목을 정렬하세요. 텍스트를 먼저 선언하고 그다음 함수를 선언하세요

# --answers--

```javascript
const text = "a1 b2 c3";
```

```javascript
function double(digit) {
```

```javascript
  return Number(digit) * 2;
```

```javascript
}
```

```javascript
console.log(text.replace(/\d/g, double));
```

# --solutions--

```javascript
const text = "a1 b2 c3";
function double(digit) {
  return Number(digit) * 2;
}
console.log(text.replace(/\d/g, double));
```

# --output--

a2 b4 c6
