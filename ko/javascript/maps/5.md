---
language: javascript
exerciseType: 2
---

# --description--

맵은 쌍이 추가된 순서를 기억하며, `for...of`로 반복할 수 있습니다.
`entries()` 메서드는 각 쌍을 `[key, value]` 배열로 제공하므로, 루프 안에서 바로 구조 분해할 수 있습니다:
```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
// prints apple: 3
// prints pear: 5
```
맵을 직접 반복하는 `for (const [name, qty] of stock)`도 정확히 같은 동작을 합니다.

# --instructions--

`stock`의 엔트리를 반복하며 각 이름과 그 수량을 출력하세요

# --seed--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [/] of stock.[/]()) {
  console.log(`${name}: ${qty}`);
}
```

# --answers--

- [name, qty]
- [qty, name]
- name, qty
- entries
- pairs
- items

# --solutions--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
```

# --output--

apple: 3
pear: 5
