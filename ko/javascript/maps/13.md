---
language: javascript
exerciseType: 2
---

# --description--

`delete(key)`는 쌍이 제거되었을 때 `true`를, 키가 없었을 때 `false`를 반환합니다.
**모든** 쌍을 한 번에 제거하려면 `clear()`를 호출하세요:
```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

`cart`에서 `"pen"`을 제거하며 그 결과를 출력하고, 맵 전체를 비운 다음 남은 쌍의 개수를 출력하세요

# --seed--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
