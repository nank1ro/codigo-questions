---
language: javascript
exerciseType: 2
---

# --description--

`split(separator)` 메서드는 `separator`가 나타날 때마다 잘라서 문자열을 조각들의 **배열**로 나눕니다:
```javascript
let sentence = "I like JavaScript";
let words = sentence.split(" ");
console.log(words);
// prints [ 'I', 'like', 'JavaScript' ]
```
그 반대는 배열 메서드 `join(separator)`로, 조각들을 다시 하나의 문자열로 이어붙입니다:
```javascript
console.log(words.join("-"));
// prints I-like-JavaScript
```

# --instructions--

`csv`를 쉼표마다 분할하고 포함된 색상의 개수를 출력하세요

# --seed--

```javascript
let csv = "red,green,blue";
let colors = csv.[/]([/]);
console.log(colors.[/]);
```

# --answers--

- split
- ","
- " "
- join
- length
- size

# --solutions--

```javascript
let csv = "red,green,blue";
let colors = csv.split(",");
console.log(colors.length);
```

# --output--

3
