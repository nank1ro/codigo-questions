---
language: javascript
exerciseType: 2
---

# --description--

문자열에는 다양한 내장 **메서드**가 있습니다. 그중 가장 간단한 두 가지는 모든 글자의 대소문자를 바꿉니다:
```javascript
let word = "Hello";
console.log(word.toUpperCase());
// prints HELLO
console.log(word.toLowerCase());
// prints hello
```
두 메서드 모두 인자를 받지 않으므로 괄호를 잊지 마세요.

# --instructions--

`name`을 대문자로, 그다음 소문자로 출력하세요

# --seed--

```javascript
let name = "Ada";
console.log(name.[/]());
console.log(name.[/]());
```

# --answers--

- toUpperCase
- toLowerCase
- upper
- lower
- capitalize

# --solutions--

```javascript
let name = "Ada";
console.log(name.toUpperCase());
console.log(name.toLowerCase());
```

# --output--

ADA
ada
