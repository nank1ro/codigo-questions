---
language: javascript
exerciseType: 4
---

# --instructions--

줄을 정렬하여 영수증 한 줄을 출력하세요: `item`을 점으로 8자가 될 때까지 채우고, 이어서 가격을 오른쪽 정렬로 6자로 표시하여 출력합니다

# --answers--

```javascript
let item = "Tea";
```

```javascript
let row = item.padEnd(8, ".");
```

```javascript
row = row + "2.50".padStart(6);
```

```javascript
console.log(row);
```

# --solutions--

```javascript
let item = "Tea";
let row = item.padEnd(8, ".");
row = row + "2.50".padStart(6);
console.log(row);
```

# --output--

Tea.....  2.50
