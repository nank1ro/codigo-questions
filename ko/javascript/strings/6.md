---
language: javascript
exerciseType: 2
---

# --description--

`slice(start, end)` 메서드는 인덱스 `start`부터 인덱스 `end` 직전까지(포함하지 않음)의 문자열 조각을 추출합니다:
```javascript
let word = "JavaScript";
console.log(word.slice(0, 4));
// prints Java
console.log(word.slice(4));
// prints Script
```
`end`를 생략하면 문자열 끝까지 잘라냅니다.
음수 인덱스는 끝에서부터 셉니다: `word.slice(-3)`은 `"ipt"`입니다.
`substring(start, end)` 메서드도 동일하게 동작하지만 음수 인덱스는 받지 않습니다.

# --instructions--

`slice()`를 사용해 `word`의 처음 네 글자를 출력하세요

# --seed--

```javascript
let word = "JavaScript";
console.log(word.[/](0, [/]));
```

# --answers--

- slice
- 4
- 3
- 5
- splice
- cut

# --solutions--

```javascript
let word = "JavaScript";
console.log(word.slice(0, 4));
```

# --output--

Java
