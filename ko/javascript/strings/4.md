---
language: javascript
exerciseType: 1
---

# --description--

문자열이 다른 문자열을 포함하는지 확인하려면 다음 메서드를 사용하세요. 모두 불리언 값을 반환합니다:
- `includes(text)`는 `text`가 어디든 나타나면 `true`
- `startsWith(text)`는 문자열이 `text`로 시작하면 `true`
- `endsWith(text)`는 문자열이 `text`로 끝나면 `true`

```javascript
let file = "photo.png";
console.log(file.includes("."));
// prints true
console.log(file.startsWith("ph"));
// prints true
console.log(file.endsWith(".jpg"));
// prints false
```
비교는 대소문자를 구분합니다: `"Hello".includes("h")`는 `false`입니다.

# --instructions--

`fileName`이 `".png"`로 끝나면 `true`, 그렇지 않으면 `false`를 반환하는 함수 `isImage(fileName)`을 작성하세요

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isImage(fileName) {

}
```

# --asserts--

`isImage("cat.png")`는 `true`를 반환해야 합니다.

```javascript
tryCatch(isImage("cat.png") === true);
```

`isImage("cat.jpg")`는 `false`를 반환해야 합니다.

```javascript
tryCatch(isImage("cat.jpg") === false);
```

`isImage("png.txt")`는 `false`를 반환해야 합니다.

```javascript
tryCatch(isImage("png.txt") === false);
```

`isImage("a.png.txt")`는 `false`를 반환해야 합니다.

```javascript
tryCatch(isImage("a.png.txt") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isImage(fileName) {
  return fileName.endsWith(".png");
}
```
