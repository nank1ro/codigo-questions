---
language: javascript
exerciseType: 4
---

# --description--

각 멤버가 여러 데이터나 자신만의 메서드를 필요로 할 때는 **클래스**가 열거형의 역할을 할 수 있습니다. 모든 멤버는 그 클래스의 인스턴스이며, `static` 프로퍼티, 즉 각 인스턴스가 아니라 클래스 자체에 속하는 프로퍼티에 저장됩니다:
```javascript
class Planet {
  static MERCURY = new Planet("Mercury", 0.4);
  static EARTH = new Planet("Earth", 1);

  constructor(name, gravity) {
    this.name = name;
    this.gravity = gravity;
  }
}
console.log(Planet.EARTH.name);
// prints Earth
```
클래스 뒤에 `Object.freeze(Planet)`을 호출해 아무도 멤버를 추가하거나 교체하지 못하게 막고, 생성자 안에서 `Object.freeze(this)`로 각 인스턴스를 동결하여 멤버 자체를 읽기 전용으로 유지하세요.

# --instructions--

줄을 정렬하여 클래스 기반 열거형 `Coin`을 선언하고 `DIME` 멤버의 센트 값을 출력하세요.
`PENNY`는 `DIME`보다 먼저, 둘 다 생성자보다 먼저 선언하고, 출력하기 전에 클래스를 동결하세요

# --answers--

```javascript
class Coin {
```

```javascript
  static PENNY = new Coin("penny", 1);
```

```javascript
  static DIME = new Coin("dime", 10);
```

```javascript
  constructor(name, cents) { this.name = name; this.cents = cents; }
```

```javascript
}
```

```javascript
Object.freeze(Coin);
```

```javascript
console.log(Coin.DIME.cents);
```

# --solutions--

```javascript
class Coin {
  static PENNY = new Coin("penny", 1);
  static DIME = new Coin("dime", 10);
  constructor(name, cents) { this.name = name; this.cents = cents; }
}
Object.freeze(Coin);
console.log(Coin.DIME.cents);
```

# --output--

10
