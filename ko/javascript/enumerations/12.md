---
language: javascript
exerciseType: 2
---

# --description--

열거형 값은 예를 들어 모든 멤버를 라벨이나 가격에 대응시키기 위해, 다른 객체의 **키**로 자주 사용됩니다. 객체 리터럴 안에서 키를 대괄호 `[ ]`로 감싸면 표현식이 평가되고 그 결과가 키로 사용됩니다(**계산된 키**). 이는 문자열 멤버와 심볼 멤버 모두에서 동작합니다:
```javascript
const Status = Object.freeze({ ACTIVE: "active", DONE: "done" });
const labels = {
  [Status.ACTIVE]: "In progress",
  [Status.DONE]: "Completed",
};
console.log(labels[Status.DONE]);
// prints Completed
```
대괄호가 없다면 `Status.DONE: "Completed"`는 구문 오류가 되고, `"Status.DONE"`은 그냥 문자열 키가 됩니다.

# --instructions--

열거형 멤버를 키로 사용하여 `Size.SMALL`을 `3`에, `Size.LARGE`를 `5`에 대응시키는 객체 `prices`를 만든 다음, 라지 사이즈의 가격을 출력하세요

# --seed--

```javascript
const Size = Object.freeze({ SMALL: "small", LARGE: "large" });
const prices = {
  [/]: 3,
  [/]: 5,
};
console.log(prices[/]);
```

# --answers--

- [Size.SMALL]
- [Size.LARGE]
- [Size.LARGE]
- Size.SMALL
- Size.LARGE
- .LARGE
- (Size.LARGE)

# --solutions--

```javascript
const Size = Object.freeze({ SMALL: "small", LARGE: "large" });
const prices = {
  [Size.SMALL]: 3,
  [Size.LARGE]: 5,
};
console.log(prices[Size.LARGE]);
```

# --output--

5
