---
language: javascript
exerciseType: 1
---

# --description--

각 멤버에 저장할 값은 자유롭게 선택할 수 있습니다. **문자열**은 출력하거나 로그로 남기거나 파일에 저장할 때 읽기 쉽기 때문에 가장 흔한 선택입니다:
```javascript
const Status = Object.freeze({
  ACTIVE: "active",
  DONE: "done",
});
console.log(Status.DONE);
// prints done
```
한 번 동결된 객체는 새로운 프로퍼티도 받을 수 없으며, `Object.isFrozen(obj)`로 객체가 동결되었는지 확인할 수 있습니다:
```javascript
console.log(Object.isFrozen(Status));
// prints true
```

# --instructions--

`UP`, `DOWN`, `LEFT`, `RIGHT` 멤버가 각각 소문자로 된 자기 이름(`"up"`, `"down"`, `"left"`, `"right"`)을 가지는 동결된 열거형 `Direction`을 선언하세요

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Direction = 
```

# --asserts--

`Direction.UP`은 `"up"`이어야 합니다.

```javascript
tryCatch(Direction.UP === "up");
```

`Direction.DOWN`은 `"down"`이어야 합니다.

```javascript
tryCatch(Direction.DOWN === "down");
```

`Direction.LEFT`는 `"left"`, `Direction.RIGHT`는 `"right"`여야 합니다.

```javascript
tryCatch(Direction.LEFT === "left" && Direction.RIGHT === "right");
```

`Direction`은 정확히 4개의 멤버를 가져야 합니다.

```javascript
tryCatch(Object.keys(Direction).length === 4);
```

`Direction`은 동결되어 있어야 합니다.

```javascript
tryCatch(Object.isFrozen(Direction) === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Direction = Object.freeze({
  UP: "up",
  DOWN: "down",
  LEFT: "left",
  RIGHT: "right",
});
```
