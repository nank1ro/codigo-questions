---
language: javascript
exerciseType: 2
---

# --description--

**열거형**(*enum*)은 요일, 카드의 무늬, 주문의 상태처럼 관련된 값들의 작고 고정된 그룹을 위한 공통 타입입니다.
다른 많은 언어와 달리 JavaScript에는 `enum` 키워드가 **없습니다**. 관용적인 대체 방법은 멤버를 프로퍼티로 갖는 일반 객체를 `Object.freeze()`에 전달하여 이후에 아무도 변경할 수 없게 만드는 것입니다:
```javascript
const Color = Object.freeze({
  RED: "red",
  GREEN: "green",
  BLUE: "blue",
});
console.log(Color.RED);
// prints red
```
관례상 이 객체는 `const`로 선언하고, 이름은 대문자로 시작하며, 멤버 이름은 다른 상수와 마찬가지로 `UPPER_CASE`로 작성합니다.

# --instructions--

`SPRING`과 `SUMMER` 멤버가 각각 문자열 `"spring"`과 `"summer"`를 가지는 동결된 열거형 `Season`을 선언한 다음, `SUMMER`의 값을 출력하세요

# --seed--

```javascript
const Season = Object.[/]({
  SPRING: "spring",
  [/]: "summer",
});
console.log(Season.[/]);
```

# --answers--

- freeze
- SUMMER
- SUMMER
- seal
- lock
- summer
- Summer

# --solutions--

```javascript
const Season = Object.freeze({
  SPRING: "spring",
  SUMMER: "summer",
});
console.log(Season.SUMMER);
```

# --output--

summer
