---
language: javascript
exerciseType: 1
---

# --description--

열거형은 `switch`문과 자연스럽게 어울립니다. `switch`문은 하나의 값을 여러 `case` 레이블 목록과 비교하여 처음으로 일치하는 것의 코드를 실행합니다.
각 분기는 `return` 또는 `break`로 끝나며, 선택적인 `default` 분기는 어느 것도 일치하지 않을 때 실행됩니다:
```javascript
const Light = Object.freeze({ RED: "red", GREEN: "green" });

function action(light) {
  switch (light) {
    case Light.RED:
      return "stop";
    case Light.GREEN:
      return "go";
    default:
      return "unknown";
  }
}
console.log(action(Light.GREEN));
// prints go
```
항상 원시 값(`"red"`)이 아니라 멤버(`Light.RED`)와 비교하세요. 그러면 값이 나중에 바뀌더라도 `switch`가 계속 동작합니다.

# --instructions--

`Day` 열거형에 대해 `switch`를 사용하여 `Day.SATURDAY`와 `Day.SUNDAY`에 대해서는 `true`를, 그 외의 값에 대해서는 `false`를 반환하는 함수 `weekend(day)`를 작성하세요

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {

}
```

# --asserts--

`weekend(Day.SATURDAY)`는 `true`를 반환해야 합니다.

```javascript
tryCatch(weekend(Day.SATURDAY) === true);
```

`weekend(Day.SUNDAY)`는 `true`를 반환해야 합니다.

```javascript
tryCatch(weekend(Day.SUNDAY) === true);
```

`weekend(Day.MONDAY)`는 `false`를 반환해야 합니다.

```javascript
tryCatch(weekend(Day.MONDAY) === false);
```

`weekend("holiday")`는 `false`를 반환해야 합니다. 멤버가 아니기 때문입니다.

```javascript
tryCatch(weekend("holiday") === false);
```

이 함수는 `switch`문을 사용해야 합니다.

```javascript
tryCatch(/switch\s*\(/.test(weekend.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {
  switch (day) {
    case Day.SATURDAY:
    case Day.SUNDAY:
      return true;
    default:
      return false;
  }
}
```
