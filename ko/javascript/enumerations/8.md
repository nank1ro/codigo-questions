---
language: javascript
exerciseType: 2
---

# --description--

값에서 멤버 이름으로 되돌아가는 것을 **역방향 조회**라고 합니다. `Object.keys()`로 이름들을 순회하며, 배열 메서드 `find()`를 사용해 값이 일치하는 첫 번째 이름을 고릅니다. `find()`는 콜백이 `true`가 되는 첫 번째 요소를 반환합니다(없으면 `undefined`를 반환합니다):
```javascript
const Priority = Object.freeze({ LOW: 1, HIGH: 3 });
let name = Object.keys(Priority).find((key) => Priority[key] === 3);
console.log(name);
// prints HIGH
```
`Priority[key]`는 변수 `key`에 저장된 이름을 가진 멤버를 읽습니다. 이는 어떤 객체에서든 사용하는 것과 같은 대괄호 표기법입니다.

# --instructions--

값이 `2`인 `Priority` 멤버의 이름을 찾아 출력하세요

# --seed--

```javascript
const Priority = Object.freeze({ LOW: 1, MEDIUM: 2, HIGH: 3 });
let name = Object.[/](Priority).[/]((key) => Priority[/] === 2);
console.log(name);
```

# --answers--

- keys
- find
- [key]
- values
- filter
- .key
- (key)

# --solutions--

```javascript
const Priority = Object.freeze({ LOW: 1, MEDIUM: 2, HIGH: 3 });
let name = Object.keys(Priority).find((key) => Priority[key] === 2);
console.log(name);
```

# --output--

MEDIUM
