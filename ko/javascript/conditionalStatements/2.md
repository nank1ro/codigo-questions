---
language: javascript
exerciseType: 2
---

# --description--

이전 예제를 계속 살펴봅시다.
```javascript
var niceWeather = true;
if (niceWeather) {
    // play outside
}
```
`if` 문은 조건이 `true`일 때만 코드 블록을 실행한다는 것을 확인했습니다.
또 하나 중요한 것은 코드 블록을 나타내는 **중괄호** `{}`입니다.

# --instructions--

*중괄호* `{}`를 추가하여 `if` 문을 수정하세요

# --seed--

```javascript
var time = 8;
if (time < 12) [/]
    console.log("It's morning");
[/]
```

# --answers--

- if
- ==
- =
- else
- ?
- {
- }

# --solutions--

```javascript
var time = 8;
if (time < 12) {
    console.log("It's morning");
}
```

# --output--

It's morning
