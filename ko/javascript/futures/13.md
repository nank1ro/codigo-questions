---
language: javascript
exerciseType: 4
---

# --description--

**순차적** 대기와 **병렬** 대기의 차이는 `await`를 *어디에* 두느냐로 결정됩니다:
```javascript
// sequential: about 300 + 300 = 600 ms
const a = await load("a");
const b = await load("b");

// parallel: about 300 ms
const [a, b] = await Promise.all([load("a"), load("b")]);
```
첫 번째 버전에서는 두 번째 다운로드가 첫 번째가 끝나야 시작됩니다. `await`가 그 줄에서 함수를 멈추기 때문입니다. 두 번째에서는 아무것도 await하기 전에 두 호출을 모두 하므로, `Promise.all`이 기다리는 동안 두 다운로드는 이미 실행 중입니다.

두 번째 작업이 정말로 첫 번째의 결과를 필요로 할 때만 순차적인 await를 사용하세요. 그렇지 않다면 모두 먼저 시작한 다음 함께 await하세요.

# --instructions--

줄을 정렬해서 `later`와, `loading`을 출력하고 두 `later` 호출을 `Promise.all`로 함께 시작해 두 결과를 공백으로 연결해 출력한 다음 마지막으로 `done`을 출력하는 `async` 함수 `main`이 있는 프로그램을 만드세요. `later`를 먼저, 그다음 `main`을 정의하고 마지막 줄에서 `main()`을 호출하세요

# --answers--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
```

```javascript
async function main() {
```

```javascript
  console.log("loading");
```

```javascript
  const results = await Promise.all([later("one", 30), later("two", 10)]);
```

```javascript
  console.log(results.join(" "));
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
async function main() {
  console.log("loading");
  const results = await Promise.all([later("one", 30), later("two", 10)]);
  console.log(results.join(" "));
  console.log("done");
}
main();
```

# --output--

loading
ready two
ready one
one two
done
