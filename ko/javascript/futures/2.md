---
language: javascript
exerciseType: 1
---

# --description--

함수 앞에 `async`를 붙이면 함수가 돌려주는 것이 달라집니다: 본문은 여전히 평범한 값을 계산하지만, 호출자는 그 값을 감싼 프로미스를 받습니다.
```javascript
function shout(text) {
  return text.toUpperCase();
}
async function shoutLater(text) {
  return text.toUpperCase();
}

console.log(shout("hi"));
// prints HI
console.log(shoutLater("hi"));
// prints Promise { 'HI' }
```
두 함수는 같은 코드를 담고 있습니다. 결과를 읽는 방식만 다를 뿐입니다. `shoutLater("hi")`가 `"HI"`를 돌려주게 하려면 다른 `async` 함수 안에서 await해야 합니다.

함수에 `async`를 붙이는 것은 기다릴 것이 없을 때는 아무 비용도 들지 않으며, 나중에 그 안에서 `await`를 쓸 수 있게 해주는 장치입니다.

# --instructions--

문자열 `name`을 받아 `Hello, ` 다음에 이름과 느낌표를 이어 붙인 값(예: `Hello, Ada!`)을 반환하는 `async` 함수 `greet`를 작성하세요

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
async function greet(name) {
  
}
```

# --asserts--

`greet`는 `async` 키워드로 선언되어야 합니다

```json
{
  "regex": "async\\s+function\\s+greet",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`await greet("Ada")`는 `"Hello, Ada!"`여야 합니다

```javascript
(async () => {
  tryCatch((await greet("Ada")) === "Hello, Ada!");
})().catch(() => tryCatch(false));
```

`await greet("Bob")`은 `"Hello, Bob!"`여야 합니다

```javascript
(async () => {
  tryCatch((await greet("Bob")) === "Hello, Bob!");
})().catch(() => tryCatch(false));
```

`await greet("")`는 `"Hello, !"`여야 합니다

```javascript
(async () => {
  tryCatch((await greet("")) === "Hello, !");
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
async function greet(name) {
  return `Hello, ${name}!`;
}
```
