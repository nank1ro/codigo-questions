---
language: javascript
exerciseType: 4
---

# --description--

`async` 함수는 다른 함수와 마찬가지로 위에서 아래로 읽힙니다: `await`는 대기 중인 프로미스가 이행될 때까지 함수를 잠시 멈출 뿐이고, 그다음 줄에서 실행이 계속됩니다.
```javascript
async function main() {
  console.log("start");
  const value = await Promise.resolve("data");
  console.log(value);
  console.log("done");
}

main();
// prints start, then data, then done
```
마지막 줄에 주목하세요: `async` 함수도 여전히 **호출**되어야 합니다. 괄호 없이 `main`만 쓰면 작업을 정의할 뿐 결코 시작하지 않고, 아무것도 출력되지 않습니다.

# --instructions--

줄을 정렬해서 `data`를 반환하는 `async` 함수 `load`와, `start`를 출력하고 `load()`를 await해 `value`에 담은 뒤 `value`를 출력하고 마지막으로 `done`을 출력하는 `async` 함수 `main`이 있는 프로그램을 만드세요. `load`를 먼저, 그다음 `main`을 정의하고 마지막 줄에서 `main()`을 호출하세요

# --answers--

```javascript
async function load() {
  return "data";
}
```

```javascript
async function main() {
```

```javascript
  console.log("start");
```

```javascript
  const value = await load();
```

```javascript
  console.log(value);
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
async function load() {
  return "data";
}
async function main() {
  console.log("start");
  const value = await load();
  console.log(value);
  console.log("done");
}
main();
```

# --output--

start
data
done
