---
language: javascript
exerciseType: 1
---

# --description--

`FIXME`는 `TODO`와 다릅니다. 코드는 이미 존재하지만 잘못되었다고 알려져 있습니다. 좋은 `FIXME`는 버그가 무엇인지 말해 주고, 가능하다면 그것을 보여 주는 예도 덧붙여, 다음 사람이 빠르게 고칠 수 있게 합니다. `TODO`와 마찬가지로 버그를 고치면 표식은 지우되, 여전히 사실인 JSDoc 주석은 그대로 두세요.

# --instructions--

`average`의 `FIXME` 주석이 설명하는 버그를 고치고(괄호를 추가해 나눗셈보다 합이 먼저 계산되게 하세요), 그다음 `FIXME` 주석을 지우세요. JSDoc 주석은 그대로 두세요

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
/**
 * Returns the average of two numbers.
 * @param {number} a the first number
 * @param {number} b the second number
 * @returns {number} the sum of a and b divided by two
 */
function average(a, b) {
  // FIXME: divides before adding, so average(4, 6) gives 7 instead of 5
  return a + b / 2;
}
```

# --asserts--

The `FIXME` comment must be removed.

```json
{
  "regex": "FIXME",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

The JSDoc comment must be kept.

```json
{
  "regex": "/\\*\\*[\\s\\S]*Returns the average of two numbers[\\s\\S]*\\*/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`average(4, 6)` must return `5`.

```javascript
tryCatch(average(4, 6) === 5);
```

`average(3, 4)` must return `3.5`.

```javascript
tryCatch(average(3, 4) === 3.5);
```

`average(0, 0)` must return `0`.

```javascript
tryCatch(average(0, 0) === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
/**
 * Returns the average of two numbers.
 * @param {number} a the first number
 * @param {number} b the second number
 * @returns {number} the sum of a and b divided by two
 */
function average(a, b) {
  return (a + b) / 2;
}
```
