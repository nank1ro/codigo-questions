---
language: javascript
exerciseType: 4
---

# --description--

JSDoc 주석 안의 순서는 언제나 같습니다. 먼저 요약, 그다음 선언된 순서대로 매개변수마다 `@param` 하나, 마지막에 `@returns`입니다. 여는 `/**`와 닫는 ` */`가 전체를 감싸고, 주석은 자신이 설명하는 함수 바로 위에 놓입니다:
```javascript
/**
 * Returns the number of seconds in the given minutes.
 * @param {number} minutes a whole number of minutes
 * @returns {number} minutes multiplied by sixty
 */
function toSeconds(minutes) {
  return minutes * 60;
}
```

# --instructions--

요약, `@param` 줄, `@returns` 줄 순서로 `twice`를 문서화하고 함수 바로 위에 놓이도록, 그리고 함수를 정의한 뒤 `twice(21)`을 출력하도록 항목을 정렬하세요

# --answers--

```javascript
/**
```

```javascript
 * Returns the double of a number.
```

```javascript
 * @param {number} n the number to double
```

```javascript
 * @returns {number} n multiplied by two
```

```javascript
 */
```

```javascript
function twice(n) {
  return n * 2;
}
```

```javascript
console.log(twice(21));
```

# --solutions--

```javascript
/**
 * Returns the double of a number.
 * @param {number} n the number to double
 * @returns {number} n multiplied by two
 */
function twice(n) {
  return n * 2;
}
console.log(twice(21));
```

# --output--

42
