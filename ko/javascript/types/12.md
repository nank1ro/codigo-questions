---
language: javascript
exerciseType: 4
---

# --description--

JavaScript에는 정수와 소수를 위한 단일 `number` 타입이 있습니다: 모든 숫자는 64비트 부동 소수점 값(*double*)입니다. 따라서 `5`와 `5.0`은 같은 값이며, 별도의 정수 타입은 없습니다:
```javascript
console.log(5 === 5.0, 10 / 2);
// prints true 5
```
숫자에 소수 부분이 없는지 묻으려면 `Number.isInteger`를 사용하세요:
```javascript
console.log(Number.isInteger(5.0), Number.isInteger(3.5));
// prints true false
```
템플릿 리터럴은 보간된 값을 `String()`과 같은 규칙으로 문자열로 변환하므로, `${5.0}`은 `"5.0"`이 아니라 `"5"`가 됩니다.

# --instructions--

`n`을 2로 나누고 결과가 정수인지 말하는 문장을 반환하는 `half`를 정의한 다음, `10`과 `7`에 대한 결과를 출력하도록 줄을 정렬하세요. 함수를 먼저 정의하세요

# --answers--

```javascript
function half(n) {
```

```javascript
  const result = n / 2;
```

```javascript
  if (Number.isInteger(result)) {
```

```javascript
    return `${result} is whole`;
```

```javascript
  }
```

```javascript
  return `${result} is not whole`;
```

```javascript
}
```

```javascript
console.log(half(10));
```

```javascript
console.log(half(7));
```

# --solutions--

```javascript
function half(n) {
  const result = n / 2;
  if (Number.isInteger(result)) {
    return `${result} is whole`;
  }
  return `${result} is not whole`;
}
console.log(half(10));
console.log(half(7));
```

# --output--

5 is whole
3.5 is not whole
