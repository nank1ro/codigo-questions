---
language: javascript
exerciseType: 2
---

# --description--

"이것이 `NaN`인가?"를 묻는 방법은 두 가지가 있으며, 두 방법은 서로 다른 질문에 답합니다.
오래된 전역 함수 `isNaN(value)`는 먼저 `value`를 숫자로 **변환한 다음** 검사합니다. 따라서 전혀 `NaN`이 아니더라도 숫자가 될 수 없는 모든 것에 대해 `true`를 말합니다:
```javascript
console.log(isNaN("hello"));
// prints true, because Number("hello") is NaN
console.log(isNaN("42"));
// prints false, because Number("42") is 42
```
`Number.isNaN(value)`는 변환하지 **않습니다**: `value`가 실제로 숫자 `NaN`일 때만 `true`입니다:
```javascript
console.log(Number.isNaN("hello"));
// prints false, a string is not NaN
console.log(Number.isNaN(Number("hello")));
// prints true
```
`Number.isNaN`을 선호하고, 변환이 실패했는지 알고 싶다면 먼저 변환하세요.

# --instructions--

첫 번째 줄은 전역 `isNaN`을 사용하고, 두 번째 줄은 문자열 자체에 `Number.isNaN`을 사용하고, 세 번째 줄은 검사하기 전에 `value`를 숫자로 변환하여 출력이 `true`, `false`, `true`가 되도록 코드를 완성하세요

# --seed--

```javascript
const value = "hello";
console.log([/](value));
console.log([/](value));
console.log(Number.isNaN([/]));
```

# --answers--

- isNaN
- Number.isNaN
- Number(value)
- String(value)
- value
- isNumber

# --solutions--

```javascript
const value = "hello";
console.log(isNaN(value));
console.log(Number.isNaN(value));
console.log(Number.isNaN(Number(value)));
```

# --output--

true
false
true
