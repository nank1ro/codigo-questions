---
language: javascript
exerciseType: 2
---

# --description--

JavaScript의 모든 값에는 **타입**이 있습니다. 일곱 가지 **원시** 타입이 있습니다:
- 모든 숫자를 위한 `number`, 예를 들어 `42` 또는 `3.14`
- 텍스트를 위한 `string`, 예를 들어 `"Ana"`
- `true`와 `false`를 위한 `boolean`
- 한 번도 제공되지 않은 값을 위한 `undefined`
- 의도적으로 비어 있는 값을 위한 `null`
- `9007199254740993n`처럼 모든 크기의 정수를 위한 `bigint`
- `Symbol()`로 만든 고유한 식별자를 위한 `symbol`

그 외의 모든 것(배열, 함수, `{}`로 만든 객체, 날짜 등)은 `object`입니다.
`typeof` 연산자는 값의 타입을 문자열로 알려줍니다:
```javascript
console.log(typeof 42, typeof "Ana", typeof true);
// prints number string boolean
let city;
console.log(typeof city);
// prints undefined
```

# --instructions--

`age`가 숫자 `42`를, `name`이 문자열 `"Ana"`를, `active`가 불리언 `true`를 가지고 `city`는 값 없이 선언되어 출력이 `number string boolean undefined`가 되도록 코드를 완성하세요

# --seed--

```javascript
const age = [/];
const name = [/];
const active = [/];
let [/];
console.log(typeof age, typeof name, typeof active, typeof city);
```

# --answers--

- 42
- "Ana"
- true
- city
- null
- 42n
- city = null

# --solutions--

```javascript
const age = 42;
const name = "Ana";
const active = true;
let city;
console.log(typeof age, typeof name, typeof active, typeof city);
```

# --output--

number string boolean undefined
