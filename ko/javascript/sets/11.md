---
language: javascript
exerciseType: 2
---

# --instructions--

`a`와 `b` 둘 다에 있는 숫자들로 배열 `common`을 만들고, 그것을 출력하세요

# --seed--

```javascript
let a = new Set([1, 2, 3]);
let b = new Set([2, 3, 4]);
let common = [...a].[/]((x) => b.[/](x));
console.log(common);
```

# --answers--

- filter
- has
- map
- find
- includes
- contains

# --solutions--

```javascript
let a = new Set([1, 2, 3]);
let b = new Set([2, 3, 4]);
let common = [...a].filter((x) => b.has(x));
console.log(common);
```

# --output--

[ 2, 3 ]
