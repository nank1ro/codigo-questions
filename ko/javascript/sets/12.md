---
language: javascript
exerciseType: 4
---

# --instructions--

`all`에는 있지만 `done`에는 없는 이름들을 담은 배열을 출력하도록 줄을 정렬하세요.
`done`보다 먼저 `all`을 선언하세요

# --answers--

```javascript
const all = new Set(["ann", "bob", "cy"]);
```

```javascript
const done = new Set(["bob"]);
```

```javascript
const pending = [...all].filter((name) => !done.has(name));
```

```javascript
console.log(pending);
```

# --solutions--

```javascript
const all = new Set(["ann", "bob", "cy"]);
const done = new Set(["bob"]);
const pending = [...all].filter((name) => !done.has(name));
console.log(pending);
```

# --output--

[ 'ann', 'cy' ]
