---
language: javascript
exerciseType: 2
---

# --description--

`delete(value)`는 값이 제거되었으면 `true`를, 세트에 없었으면 `false`를 반환합니다.
**모든** 값을 한 번에 제거하려면 `clear()`를 호출하세요:
```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

`cart`에서 `"pen"`을 제거하면서 그 결과를 출력하고, 그런 다음 세트 전체를 비우고 남아있는 값의 개수를 출력하세요

# --seed--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
