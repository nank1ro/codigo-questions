---
language: javascript
exerciseType: 2
---

# --description--

배열을 `new Set()`에 전달하면 한 번에 세트를 만들 수 있습니다. 배열 안의 중복은 제거되므로, 이는 배열의 고유한 값을 찾는 가장 빠른 방법입니다:
```javascript
let nums = [1, 2, 2, 3, 3, 3];
let distinct = new Set(nums);
console.log(distinct.size);
// prints 3
```
**스프레드** 연산자 `...`는 반대로 세트를 다시 배열로 바꿔줍니다:
```javascript
let unique = [...distinct];
console.log(unique);
// prints [ 1, 2, 3 ]
```
`Array.from(distinct)`도 같은 일을 합니다.

# --instructions--

`nums`의 고유한 값들로 배열 `unique`를 만들고, 그 요소 개수를 출력하세요

# --seed--

```javascript
let nums = [1, 2, 2, 3, 3, 3];
let unique = [...new Set([/])];
console.log(unique.[/]);
```

# --answers--

- nums
- [nums]
- ...nums
- length
- size
- count

# --solutions--

```javascript
let nums = [1, 2, 2, 3, 3, 3];
let unique = [...new Set(nums)];
console.log(unique.length);
```

# --output--

3
