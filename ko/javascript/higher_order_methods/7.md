---
language: javascript
exerciseType: 4
---

# --description--

비교 함수는 요소의 어떤 부분이든 볼 수 있으므로, 객체 배열은 그 속성 하나를 비교하는 것만으로 그 속성 기준으로 정렬됩니다:
```javascript
const items = [{ name: "b", size: 3 }, { name: "a", size: 1 }];
const bySize = [...items].sort((x, y) => x.size - y.size);
console.log(bySize.map((item) => item.name));
// prints [ 'a', 'b' ]
```
복사본을 정렬하면 `items`는 원래 순서 그대로 남습니다.

# --instructions--

줄을 정렬해서 `players`의 복사본을 `score` 오름차순으로 정렬하고, 그 순서대로 이름을 `map`으로 출력한 다음, `players`의 첫 요소 이름을 출력해 변경되지 않았음을 보이세요. `ranked`를 `names`보다 먼저 선언하세요

# --answers--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
```

```javascript
const ranked = [...players].sort((a, b) => a.score - b.score);
```

```javascript
const names = ranked.map((p) => p.name);
```

```javascript
console.log(names);
```

```javascript
console.log(players[0].name);
```

# --solutions--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
const ranked = [...players].sort((a, b) => a.score - b.score);
const names = ranked.map((p) => p.name);
console.log(names);
console.log(players[0].name);
```

# --output--

[ 'Leo', 'Zoe', 'Mia' ]
Zoe
