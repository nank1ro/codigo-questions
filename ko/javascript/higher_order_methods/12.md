---
language: javascript
exerciseType: 2
---

# --description--

`reduce`의 누적자는 숫자일 필요가 없습니다. 문자열, 배열, 객체일 수도 있습니다. 빈 객체 `{}`에서 시작하면 한 번의 순회로 세거나 묶을 수 있습니다. 콜백에서 반드시 **누적자를 반환**하세요. 그러지 않으면 다음 단계가 `undefined`를 받습니다:
```javascript
const votes = ["yes", "no", "yes"];
const tally = votes.reduce((acc, vote) => {
  acc[vote] = (acc[vote] ?? 0) + 1;
  return acc;
}, {});
console.log(tally);
// prints { yes: 2, no: 1 }
```
`acc[vote] ?? 0`은 현재 개수를 읽고, 그 키가 아직 없으면 `0`을 읽습니다.

# --instructions--

`counts`가 각 첫 글자에 그 글자로 시작하는 단어의 개수를 대응시키도록 코드를 완성하세요. `reduce`를 사용하고, 빈 객체를 시작 누적자로 삼으며, 없는 키에는 `?? 0`을 사용해 출력 `{ a: 2, b: 2, c: 1 }`이 나오게 하세요

# --seed--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.[/]((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] [/] 0) + 1;
  return [/];
}, [/]);
console.log(counts);
```

# --answers--

- reduce
- ??
- acc
- {}
- map
- filter
- &&
- word
- 0

# --solutions--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.reduce((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] ?? 0) + 1;
  return acc;
}, {});
console.log(counts);
```

# --output--

{ a: 2, b: 2, c: 1 }
