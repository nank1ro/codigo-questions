---
language: javascript
exerciseType: 1
---

# --description--

모든 함수에는 일부를 미리 고정한 **새** 함수를 반환하는 `bind` 메서드가 있습니다. 첫 번째 인자는 새 함수의 `this`가 되고, 나머지 인자는 새 함수를 호출할 때 넘기는 인자들 앞에 놓입니다(**부분 적용**):
```javascript
function multiply(a, b) {
  return a * b;
}
const double = multiply.bind(null, 2);
console.log(double(21));
// prints 42
```
`this`를 고정하는 것은 메서드에서 중요합니다. 메서드를 객체 밖으로 꺼내 단독으로 호출하면 `this`는 더 이상 그 객체를 가리키지 않으므로 `this.name`은 `undefined`가 됩니다. `bind`는 그것을 객체에 묶어 둡니다:
```javascript
const user = {
  name: "Ana",
  hello() { return `Hi ${this.name}`; },
};
const loose = user.hello;
console.log(loose());
// prints Hi undefined
const bound = user.hello.bind(user);
console.log(bound());
// prints Hi Ana
```
원래 함수는 절대 변경되지 않습니다. `bind`는 항상 새 함수를 만들며, 그 `name`은 원래 이름 앞에 `bound `가 붙은 것입니다.

# --instructions--

함수 `power`와 객체 `counter`가 주어져 있습니다. 상수 `square`에는 `this`를 `null`로 하고 `exponent`를 `2`로 고정해 묶은 `power`를 저장하고, 상수 `tick`에는 `counter`에 묶은 `counter.increment`를 저장해 단독으로 호출해도 계속 동작하게 하세요

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = 
const tick = 
```

# --asserts--

`square(3)`은 `9`를 반환해야 합니다.

```javascript
tryCatch(square(3) === 9);
```

`square(12)`는 `144`를 반환해야 합니다.

```javascript
tryCatch(square(12) === 144);
```

`square`는 `bind`로 만들어져야 하므로 그 이름은 `"bound power"`입니다.

```javascript
tryCatch(square.name === "bound power");
```

`tick`은 `bind`로 만든 새 함수여야 하며 `counter.increment` 자체여서는 안 됩니다.

```javascript
tryCatch(tick !== counter.increment && tick.name === "bound increment");
```

`tick()`을 단독으로 두 번 호출하면 `1`과 그다음 `2`를 반환해야 하고, `counter.count`는 `2`여야 합니다.

```javascript
tryCatch(tick() === 1 && tick() === 2 && counter.count === 2);
```

`tick`은 다른 변수에 저장해 거기서 호출해도 계속 동작해야 합니다.

```javascript
tryCatch((() => { const alone = tick; return alone() === 3; })());
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = power.bind(null, 2);
const tick = counter.increment.bind(counter);
```
