---
language: javascript
exerciseType: 4
---

# --description--

실제 프로그램은 이 메서드들을 **파이프라인**으로 결합합니다. 관심 있는 요소를 걸러내고, 필요한 값으로 변환하고, 하나의 결과로 축약합니다. 중간 배열을 상수에 담아 두면 각 단계가 읽기 쉬워지고 재사용할 수도 있습니다:
```javascript
const adults = people.filter((p) => p.age >= 18);
const names = adults.map((p) => p.name);
const totalAge = adults.reduce((sum, p) => sum + p.age, 0);
```
`names.join(", ")`은 문자열 배열을 요소가 쉼표와 공백으로 구분된 하나의 문자열로 바꿉니다.

# --instructions--

줄을 정렬해서 장바구니에서 `qty`가 양수인 항목만 남기고, 그 이름을 모으고, 그 항목들의 `price * qty`를 더한 뒤, `", "`로 이어 붙인 이름과 합계를 한 줄로 출력하세요. 항목은 pen, book, bag 순으로 나열하고 `inStock`, `names`, `total`을 이 순서로 선언하세요

# --answers--

```javascript
const cart = [
```

```javascript
  { name: "pen", price: 2, qty: 3 },
```

```javascript
  { name: "book", price: 12, qty: 1 },
```

```javascript
  { name: "bag", price: 30, qty: 0 },
```

```javascript
];
```

```javascript
const inStock = cart.filter((item) => item.qty > 0);
```

```javascript
const names = inStock.map((item) => item.name);
```

```javascript
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
```

```javascript
console.log(names.join(", "), total);
```

# --solutions--

```javascript
const cart = [
  { name: "pen", price: 2, qty: 3 },
  { name: "book", price: 12, qty: 1 },
  { name: "bag", price: 30, qty: 0 },
];
const inStock = cart.filter((item) => item.qty > 0);
const names = inStock.map((item) => item.name);
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
console.log(names.join(", "), total);
```

# --output--

pen, book 18
