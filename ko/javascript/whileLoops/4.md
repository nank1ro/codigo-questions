---
language: javascript
exerciseType: 2
---

# --description--

코드 블록 내에서 `while` 반복문을 멈추려면 `counter` 변수를 증가시켜야 합니다.

# --instructions--

코드 블록 내에서 `counter` 변수를 증가시키세요

# --seed--

```javascript
var counter = 3;
while (counter < 5) {
    console.log(counter);
    [/][/][/][/];
}
```

# --answers--

- counter
-  +
- = 
- -
- *
- 1

# --solutions--

```javascript
var counter = 3;
while (counter < 5) {
    console.log(counter);
    counter += 1;
}
```

# --output--

3
4
