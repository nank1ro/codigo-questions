---
language: javascript
exerciseType: 1
---

# --description--

자식이 부모가 이미 가진 메서드를 정의하면 자식 버전이 이깁니다. 이것을 **재정의(오버라이딩)**라고 합니다:
```javascript
class Animal {
    speak() {
        return "some sound";
    }
}
class Dog extends Animal {
    speak() {
        return "Woof";
    }
}
console.log(new Dog().speak());
// Woof 출력
```
재정의는 부모 버전을 삭제하지 않고 숨길 뿐입니다. 자식 메서드 안에서 `super.methodName(...)`는 여전히 부모 버전에 도달할 수 있으므로, 부모 동작을 대체하는 대신 확장할 수 있습니다:
```javascript
class Puppy extends Dog {
    speak() {
        return super.speak() + "!";
    }
}
console.log(new Puppy().speak());
// Woof! 출력
```
차이에 주의하세요. `super(...)`는 부모 **생성자**를 호출하고, `super.name(...)`는 부모 **메서드**를 호출합니다.

# --instructions--

`Greeter`를 상속하는 클래스 `LoudGreeter`를 작성하고, `greet`를 재정의하여 부모의 인사말을 대문자로 바꾸고 뒤에 느낌표를 붙여 반환하도록 하세요

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Greeter {
  constructor(name) {
    this.name = name;
  }
  greet() {
    return `Hello ${this.name}`;
  }
}

class LoudGreeter {
  
}
```

# --asserts--

`new LoudGreeter("Ann").greet()`는 `"HELLO ANN!"`을 반환해야 합니다

```javascript
tryCatch(new LoudGreeter("Ann").greet() === "HELLO ANN!");
```

`new LoudGreeter("bob").greet()`는 `"HELLO BOB!"`을 반환해야 합니다

```javascript
tryCatch(new LoudGreeter("bob").greet() === "HELLO BOB!");
```

부모 클래스는 그대로 유지되어야 합니다

```javascript
tryCatch(new Greeter("Ann").greet() === "Hello Ann");
```

`LoudGreeter`는 `super`를 통해 부모 메서드를 재사용해야 합니다

```json
{
  "regex": "super\\.greet\\s*\\(\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Greeter {
  constructor(name) {
    this.name = name;
  }
  greet() {
    return `Hello ${this.name}`;
  }
}

class LoudGreeter extends Greeter {
  greet() {
    return super.greet().toUpperCase() + "!";
  }
}
```
