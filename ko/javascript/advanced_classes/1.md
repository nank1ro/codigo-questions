---
language: javascript
exerciseType: 2
---

# --description--

이미 `extends`가 한 클래스를 다른 클래스의 자식으로 만든다는 것은 알고 있습니다. 이 키워드가 실제로 제공하는 것은 **상속**입니다. 자식 클래스는 부모의 모든 프로퍼티와 메서드를 공짜로 물려받고, 그 위에 자신만의 것을 추가할 수 있습니다.

상속을 유용하게 만드는 요소는 바로 **`super`**입니다. 자식 생성자 안에서 `super(...)`는 부모 생성자를 호출하므로, 부모가 자신이 소유한 객체의 부분을 설정할 수 있습니다:
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
```
여기서 `super(name)`은 `name`을 `Animal`에 넘겨 저장하게 하고, `Dog`는 `breed`만 신경 쓰면 됩니다.

# --instructions--

`Dog`가 `Animal`을 상속하고 `name`을 부모 생성자에 전달하도록 코드를 완성하세요

# --seed--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog [/] Animal {
    constructor(name, breed) {
        [/](name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --answers--

- extends
- inherits
- super
- parent
- new Animal

# --solutions--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --output--

Max is a Beagle
