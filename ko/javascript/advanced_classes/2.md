---
language: javascript
exerciseType: 1
---

# --description--

자식 클래스는 부모가 이미 제공하는 것을 다시 정의할 필요가 없습니다. 메서드도 상속되므로, 자식의 인스턴스는 자기 것인 것처럼 메서드를 호출할 수 있습니다:
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
    speak() {
        return `${this.name} makes a sound`;
    }
}
class Dog extends Animal {}
console.log(new Dog("Max").speak());
// Max makes a sound 출력
```
자식이 자신의 생성자를 선언하면, 그 안에서 `super(...)`를 호출하는 것은 **필수**입니다. 호출하지 않으면 객체가 초기화되지 않은 채로 남아 JavaScript가 `ReferenceError`를 던집니다. 생성자가 전혀 없는 자식은 괜찮습니다. JavaScript가 모든 인자를 부모에게 전달하는 생성자를 작성해 주기 때문입니다.

# --instructions--

`Animal`을 상속하는 클래스 `Cat`을 작성하세요. `name`과 `lives`를 받고, `name`을 부모 생성자에 전달하고, `lives`를 인스턴스에 저장해야 합니다

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Cat {
  
}
```

# --asserts--

`new Cat("Kitty", 9).name`은 `"Kitty"`이어야 합니다

```javascript
tryCatch(new Cat("Kitty", 9).name === "Kitty");
```

`new Cat("Kitty", 9).lives`는 `9`이어야 합니다

```javascript
tryCatch(new Cat("Kitty", 9).lives === 9);
```

`Cat`은 `Animal`을 상속해야 합니다

```javascript
tryCatch(new Cat("Kitty", 9) instanceof Animal);
```

`Cat`은 `Animal`에서 `speak`를 상속해야 합니다

```javascript
tryCatch(new Cat("Kitty", 9).speak() === "Kitty makes a sound");
```

두 번째 고양이는 자기 자신의 값을 유지해야 합니다

```javascript
tryCatch(new Cat("Tom", 7).lives === 7);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Cat extends Animal {
  constructor(name, lives) {
    super(name);
    this.lives = lives;
  }
}
```
