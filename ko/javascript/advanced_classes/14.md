---
language: javascript
exerciseType: 4
---

# --description--

객체를 출력하면 보통 쓸모없는 결과가 나옵니다. JavaScript가 문자열이 필요한데 객체를 받으면 객체의 **`toString`** 메서드를 호출하며, 기본 구현은 `[object Object]`를 반환합니다. 직접 정의하면 그것을 대체합니다:
```javascript
class Money {
    constructor(amount) {
        this.amount = amount;
    }
    toString() {
        return `$${this.amount}`;
    }
}
console.log(`${new Money(7)}`);
// $7 출력
```
같은 메서드는 문자열 연결과 `String(value)`에서도 사용됩니다. 적절한 **숫자** 변환도 원한다면 `[Symbol.toPrimitive](hint)`를 정의하세요. 이것은 `"string"`, `"number"` 또는 `"default"`를 받고 무엇을 반환할지 결정하며, 존재하면 `toString`보다 우선합니다.

# --instructions--

템플릿 리터럴로 가격을 출력하는 동작하는 프로그램이 되도록 항목을 정렬하세요

# --answers--

```javascript
class Money {
  constructor(amount) {
    this.amount = amount;
  }
```

```javascript
  toString() {
    return `$${this.amount}`;
  }
}
```

```javascript
const price = new Money(7);
```

```javascript
console.log(`Total: ${price}`);
```

# --solutions--

```javascript
class Money {
  constructor(amount) {
    this.amount = amount;
  }
  toString() {
    return `$${this.amount}`;
  }
}
const price = new Money(7);
console.log(`Total: ${price}`);
```

# --output--

Total: $7
