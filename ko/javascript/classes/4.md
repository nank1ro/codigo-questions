---
language: javascript
exerciseType: 2
---

# --description--

클래스를 정의하는 것만으로는 객체가 생성되지 않습니다.
객체를 생성하려면 클래스의 __인스턴스__를 만들어야 합니다.
JavaScript에서 클래스의 새 인스턴스를 만들 때는 항상 클래스 이름 앞에 `new` 키워드를 사용합니다.
매개변수에 기본값을 할당하려면 생성자의 매개변수 이름 목록에서 지정하면 됩니다.

# --instructions--

`legs` 매개변수에 기본값으로 `4`를 할당하세요.
`Animal` 클래스의 인스턴스 `dog`을 만드세요

# --seed--

```javascript
class Animal {
    constructor(gender, legs[/][/]) {
        this.gender = gender;
        this.legs = legs;
    }
}
let [/] = [/] Animal("male");
console.log(dog);
```

# --answers--

- dog
- dog
- gender
- Animal
- ()
- animal
- []
-  = 
- 4
- new

# --solutions--

```javascript
class Animal {
    constructor(gender, legs = 4) {
        this.gender = gender;
        this.legs = legs;
    }
}
let dog = new Animal("male");
console.log(dog);
```

# --output--

Animal {
    gender: "male",
    legs: 4
}
