---
language: javascript
exerciseType: 4
---

# --instructions--

자신만의 `count`를 증가시키고 반환하는 화살표 함수를 반환하는 `makeCounter`를 정의하고, 카운터 `next`를 만들어 두 번 호출한 결과를 같은 줄에 출력하도록 줄을 정렬하세요

# --answers--

```javascript
const makeCounter = () => {
```

```javascript
  let count = 0;
```

```javascript
  return () => {
```

```javascript
    count += 1;
```

```javascript
    return count;
```

```javascript
  };
```

```javascript
};
```

```javascript
const next = makeCounter();
```

```javascript
console.log(next(), next());
```

# --solutions--

```javascript
const makeCounter = () => {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
};
const next = makeCounter();
console.log(next(), next());
```

# --output--

1 2
