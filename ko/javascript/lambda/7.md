---
language: javascript
exerciseType: 2
---

# --description--

`sort(compare)`는 두 요소를 받아 첫 번째가 먼저 와야 하면 음수를, 두 번째가 먼저 와야 하면 양수를, 같으면 `0`을 반환하는 콜백을 사용해 배열을 제자리에서 정렬합니다. 숫자의 경우 `(a, b) => a - b`는 오름차순으로, `(a, b) => b - a`는 내림차순으로 정렬합니다.
`find(callback)`은 콜백이 `true`를 반환하는 첫 번째 요소를 반환하며, 없으면 `undefined`를 반환합니다:
```javascript
const scores = [50, 90, 70];
scores.sort((a, b) => a - b);
console.log(scores);
// prints [ 50, 70, 90 ]
console.log(scores.find((s) => s > 60));
// prints 70
```

# --instructions--

`ages`를 오름차순으로 정렬한 다음 `21`보다 큰 첫 번째 나이를 찾고, 정렬된 배열과 그 나이를 모두 출력하세요

# --seed--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a [/] b);
const adult = ages.find((age) => age [/] 21);
console.log(ages);
console.log(adult);
```

# --answers--

- -
- >
- +
- <
- ===
- *

# --solutions--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a - b);
const adult = ages.find((age) => age > 21);
console.log(ages);
console.log(adult);
```

# --output--

[ 18, 25, 31 ]
25
