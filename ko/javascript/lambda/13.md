---
language: javascript
exerciseType: 2
---

# --description--

화살표 함수를 저장하지 않고도 호출할 수 있습니다. 괄호로 감싸고 바로 뒤에 인수 목록을 붙이면 **즉시 호출되는** 화살표 함수가 됩니다:
```javascript
const doubled = ((n) => n * 2)(5);
console.log(doubled);
// prints 10
```
화살표 주위의 괄호는 필수입니다. 괄호가 없으면 `(n) => n * 2(5)`는 숫자 `2`를 호출하려고 합니다. 즉시 호출되는 함수는 프로그램의 나머지 부분으로 새어 나가면 안 되는 몇 개의 임시 변수로 값을 계산할 때 유용합니다.

# --instructions--

`message`가 `"ready"`를 반환하는 즉시 호출되는 화살표 함수의 결과를 받도록 코드를 완성한 다음, `message`와 `(n) => n * 3`을 `4`로 즉시 호출한 결과를 출력하세요

# --seed--

```javascript
const message = [/]() => "ready"[/]();
console.log(message);
console.log([/](n) => n * 3)(4));
```

# --answers--

- (
- )
- (
- [
- ]
- {
- }

# --solutions--

```javascript
const message = (() => "ready")();
console.log(message);
console.log(((n) => n * 3)(4));
```

# --output--

ready
12
