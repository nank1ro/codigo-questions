---
language: javascript
exerciseType: 4
---

# --description--

`Object.entries(obj)`는 객체를 `[key, value]` 쌍의 배열로 바꿉니다. `for...of` 루프의 머리에 배열 패턴을 놓으면, 루프가 도는 동안 매 쌍이 풀어헤쳐집니다:
```javascript
const ages = { ada: 36, bob: 41 };
for (const [name, age] of Object.entries(ages)) {
    console.log(`${name} is ${age}`);
}
// prints ada is 36
// prints bob is 41
```
이것이 객체를 순회하는 읽기 좋은 방법입니다: 인덱스도, 조회도 없이 관심 있는 두 이름만 있습니다. `Object.keys`와 `Object.values`는 각각 한쪽만 주고, `Object.entries`는 둘 다 줍니다.

# --instructions--

재고에 있는 과일 종류가 몇 가지인지 출력한 다음 항목마다 한 줄씩 출력하도록 항목을 정렬하세요

# --answers--

```javascript
const stock = { apples: 3, pears: 5 };
```

```javascript
console.log(`Stock (${Object.keys(stock).length}):`);
```

```javascript
for (const [item, count] of Object.entries(stock)) {
```

```javascript
  console.log(`${item}: ${count}`);
}
```

# --solutions--

```javascript
const stock = { apples: 3, pears: 5 };
console.log(`Stock (${Object.keys(stock).length}):`);
for (const [item, count] of Object.entries(stock)) {
  console.log(`${item}: ${count}`);
}
```

# --output--

Stock (2):
apples: 3
pears: 5
