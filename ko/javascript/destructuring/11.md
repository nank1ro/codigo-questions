---
language: javascript
exerciseType: 2
---

# --description--

배열의 머리를 꺼내고 나머지 꼬리를 유지하는 일은 워낙 흔한 필요라서 패턴에만 있는 전용 문법이 있습니다. 마지막 이름 앞의 점 세 개는 그 이름을 **나머지 요소(rest element)**로 만들고, 남은 모든 요소를 완전히 새로운 배열로 모읍니다:
```javascript
const queue = ["a", "b", "c"];
const [next, ...waiting] = queue;
console.log(next, waiting);
// a [ 'b', 'c' ] 출력
```
나머지 요소는 패턴에서 맨 마지막에 와야 하며 기본값을 가질 수 없습니다: 남은 것이 없으면 단순히 빈 배열이 됩니다.

# --instructions--

`best`가 첫 번째 점수가 되고 `others`가 나머지를 모아 출력 `10 3`이 나오도록 패턴을 완성하세요

# --seed--

```javascript
const scores = [10, 20, 30, 40];
const [best, [/][/]] = scores;
console.log(best, others.length);
```

# --answers--

- ...
- others
- ..
- rest
- *
- &

# --solutions--

```javascript
const scores = [10, 20, 30, 40];
const [best, ...others] = scores;
console.log(best, others.length);
```

# --output--

10 3
