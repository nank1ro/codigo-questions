---
language: javascript
exerciseType: 4
---

# --description--

객체 패턴은 키와 같은 이름으로 변수를 만드는데, 키가 의미를 알 수 없거나 이미 사용 중인 이름이라면 곤란합니다. `key: newName`처럼 작성하면 변수의 **이름을 바꿀(renames)** 수 있습니다:
```javascript
const row = { n: "Ada", y: 1815 };
const { n: name, y: born } = row;
console.log(name, born);
// prints Ada 1815
```
"`n`을 가져와서 `name`이라고 부른다"고 읽으면 됩니다. 콜론은 타입을 선언하는 것이 아니며, `n` 자체는 결코 변수로 만들어지지 않고 `name`만 만들어집니다. 이름이 바뀐 변수에도 그 뒤에 기본값을 쓸 수 있습니다: `{ n: name = "unknown" }`.

# --instructions--

응답의 짧은 키의 이름을 바꾸고 `Ada from London`을 출력하도록 항목을 정렬하세요

# --answers--

```javascript
const response = { n: "Ada", c: "London" };
```

```javascript
const { n: name, c: city } = response;
```

```javascript
const line = `${name} from ${city}`;
```

```javascript
console.log(line);
```

# --solutions--

```javascript
const response = { n: "Ada", c: "London" };
const { n: name, c: city } = response;
const line = `${name} from ${city}`;
console.log(line);
```

# --output--

Ada from London
