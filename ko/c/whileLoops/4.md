---
language: c
exerciseType: 2
---

# --description--

코드 블록 안에서 `while` 반복문을 멈추기 위해 `counter` 변수를 증가시킵니다.

# --instructions--

코드 블록 안에서 `counter` 변수를 증가시키세요

# --seed--

```c
int counter = 3;
while (counter < 5) {
    printf("%d\n", [/]);
    [/][/];
}
```

# --answers--

- counter
-  +
- = 
- -
- *
- 1
- counter
- ++

# --solutions--

```c
int counter = 3;
while (counter < 5) {
    printf("%d\n", counter);
    counter++;
}
```

# --output--

3
4
