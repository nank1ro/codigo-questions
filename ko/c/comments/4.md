---
language: c
exerciseType: 3
---

# --description--

블록 주석은 **중첩되지 않습니다**. 컴파일러는 앞에 `/*`가 몇 개 있든 상관없이 가장 먼저 만난 `*/`에서 `/*` 주석을 끝냅니다:
```c
/* outer /* inner */ still code */
```
여기서 주석은 `inner` 바로 뒤에서 끝나므로, `still code */`는 코드로 컴파일되어 오류가 발생합니다.

이것은 이미 `/* */` 주석이 포함된 블록을 주석 처리하고 싶을 때 문제가 됩니다: 안쪽의 `*/`가 바깥쪽 주석을 너무 일찍 닫아 버리기 때문입니다. 이 경우에는 대신 각 줄 앞에 `//`를 붙이세요.

# --instructions--

이 코드를 컴파일하면 어떻게 됩니까?
```c
/* start
   /* nested */
   printf("hidden\n");
*/
printf("done\n");
```

# --answers--

- 컴파일 오류: 주석이 첫 번째 `*/`에서 끝나 `printf` 뒤에 짝이 없는 `*/`가 남습니다
- `done`을 출력합니다
- `hidden`과 `done`을 출력합니다
- 모든 것이 바깥쪽 주석 안에 있기 때문에 아무것도 출력하지 않습니다

# --solutions--

- 컴파일 오류: 주석이 첫 번째 `*/`에서 끝나 `printf` 뒤에 짝이 없는 `*/`가 남습니다
