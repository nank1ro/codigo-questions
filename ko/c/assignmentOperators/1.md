---
language: c
exerciseType: 2
---

# --description--

변수에 값을 할당하려면 `=` 기호를 사용할 수 있다는 것을 이미 배웠습니다. 예를 들면:
```c
int a = 5;
```

# --instructions--

변수 `num`에 값 `20`을 할당한 후, 그 값을 출력하세요

# --seed--

```c
int num [/][/];
printf("%d\n", num);
```

# --answers--

- = 
- / 
- * 
- 20

# --solutions--

```c
int num = 20;
printf("%d\n", num);
```

# --output--

20
