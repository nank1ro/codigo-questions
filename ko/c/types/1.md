---
language: c
exerciseType: 2
---

# --description--

C는 **정적 타입** 언어입니다: 모든 변수는 타입과 함께 선언되며, 이 타입이 저장할 수 있는 값의 종류와 사용하는 메모리 양을 결정합니다.
가장 많이 사용하는 세 가지 타입은 다음과 같습니다:
- `int` : `30`이나 `-4`처럼 정수
- `double` : `1.75`처럼 소수 부분이 있는 수
- `char` : `'A'`처럼 작은따옴표로 감싼 문자 하나

각 타입에는 고유한 `printf` **서식 지정자**가 있습니다: `%d`는 `int`를, `%f`는 `double`을(기본적으로 소수점 이하 6자리), `%c`는 `char`를 출력합니다:
```c
int age = 30;
double height = 1.75;
char initial = 'A';
printf("%d %f %c\n", age, height, initial);
// "30 1.750000 A" 출력
```
타입에 맞지 않는 지정자를 사용하면 이상한 값이 출력되므로 항상 맞춰서 사용하세요.

# --instructions--

`age`, `height`, `initial`을 공백으로 구분해 출력하도록 서식 문자열을 완성하세요

# --seed--

```c
int age = 30;
double height = 1.75;
char initial = 'A';
printf("%[/] %[/] %[/]\n", age, height, initial);
```

# --answers--

- d
- f
- c
- s
- p

# --solutions--

```c
int age = 30;
double height = 1.75;
char initial = 'A';
printf("%d %f %c\n", age, height, initial);
```

# --output--

30 1.750000 A
