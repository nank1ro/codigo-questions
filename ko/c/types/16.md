---
language: c
exerciseType: 4
---

# --description--

종합하기: 값의 종류에 따라 타입을 선택하고, 모든 `printf` 지정자를 그 인자의 타입에 맞추며, 계산이 피연산자와 다른 타입으로 이루어져야 할 때 캐스팅하세요.

# --instructions--

`total`에 `7`을, `count`에 `2`를 저장하고, `total`을 `count`로 나눈 값을 `double`로 `avg`에 계산한 다음, 정수 나눗셈 `total / count`와 이어서 `avg`를 소수점 이하 1자리로 출력하는 프로그램을 만들도록 줄을 정렬하세요

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int total = 7, count = 2;
```

```c
    double avg = (double) total / count;
```

```c
    printf("%d %.1f\n", total / count, avg);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int total = 7, count = 2;
    double avg = (double) total / count;
    printf("%d %.1f\n", total / count, avg);
    return 0;
}
```

# --output--

3 3.5
