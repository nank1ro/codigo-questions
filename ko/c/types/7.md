---
language: c
exerciseType: 2
---

# --description--

두 `int` 변수로부터 부동소수점 나눗셈을 얻는 표준적인 방법은 캐스트입니다: 나누기 전에 **한 피연산자**를 `double`로 캐스팅하세요.
```c
int total = 7, count = 2;
double avg = (double) total / count; // 3.5
```
`(double) (total / count)`처럼 결과 전체를 캐스팅하는 것은 흔한 실수입니다: 그 시점에는 이미 정수 나눗셈이 일어나 소수 부분이 사라진 뒤이기 때문입니다.

# --instructions--

`avg`가 `total`을 `count`로 나눈 값을 소수 부분까지 담도록 코드를 완성한 다음, 소수점 이하 1자리로 출력하세요

# --seed--

```c
int total = 7, count = 2;
double avg = [/] total / count;
printf("[/]\n", avg);
```

# --answers--

- (double)
- (int)
- (long)
- %.1f
- %f
- %d

# --solutions--

```c
int total = 7, count = 2;
double avg = (double) total / count;
printf("%.1f\n", avg);
```

# --output--

3.5
