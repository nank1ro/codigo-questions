---
language: c
exerciseType: 2
---

# --description--

종합해 봅시다: 리스트는 종종 배열로부터 만들어집니다. 앞쪽에 추가하면 순서가 뒤바뀌므로, 배열은 마지막 요소부터 첫 번째 요소까지 **거꾸로** 순회하고, 첫 번째 요소가 헤드에 오게 됩니다. 그다음 프로그램은 순회로 리스트를 출력하고 노드 하나씩 해제하여 모든 `malloc`이 `free`와 짝을 이루도록 합니다.

# --instructions--

`push_front`와 마지막 반복문을 완성하여 프로그램이 배열로 리스트 `10, 20, 30`을 만들고, 출력하고, 모든 노드를 해제하도록 하세요

# --seed--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = [/];
    return [/];
}

int main() {
    int values[3] = {10, 20, 30};
    Node *head = NULL;
    for (int i = 2; i >= 0; i--) {
        head = push_front(head, values[i]);
    }
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        Node *next = head->next;
        [/](head);
        head = next;
    }
    return 0;
}
```

# --answers--

- head
- node
- NULL
- free
- values[i]

# --solutions--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

int main() {
    int values[3] = {10, 20, 30};
    Node *head = NULL;
    for (int i = 2; i >= 0; i--) {
        head = push_front(head, values[i]);
    }
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        Node *next = head->next;
        free(head);
        head = next;
    }
    return 0;
}
```

# --output--

10
20
30
