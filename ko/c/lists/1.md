---
language: c
exerciseType: 2
---

# --description--

**연결 리스트**는 메모리 곳곳에 흩어진 개별 **노드**들에 값을 저장합니다: 각 노드는 값과 다음 노드에 대한 포인터를 담고 있으며, 마지막 노드는 `NULL`을 가리킵니다. 그 안의 포인터는 선언 중인 타입 자체를 참조하므로, 구조체는 자기 자신을 이름으로 부르기 위한 **태그**가 필요합니다. 중괄호 안에서는 `typedef` 이름이 아직 존재하지 않습니다:
```c
typedef struct Node {
    int value;
    struct Node *next;
} Node;
```
노드는 한 노드의 주소를 다른 노드의 `next`에 저장하여 연결하며, 포인터가 가리키는 노드의 멤버는 화살표로 접근합니다:
```c
Node second = {2, NULL};
Node first = {1, &second};
printf("%d\n", first.next->value); // prints "2"
```

# --instructions--

`next` 멤버의 선언을 완성하고, `first`를 `second`에 연결한 뒤 두 값을 모두 출력하도록 코드를 완성하세요

# --seed--

```c
typedef struct Node {
    int value;
    [/] *next;
} Node;
Node second = {2, NULL};
Node first = {1, [/]};
printf("%d %d\n", first.value, first.next->value);
```

# --answers--

- struct Node
- Node
- &second
- second
- NULL
- first

# --solutions--

```c
typedef struct Node {
    int value;
    struct Node *next;
} Node;
Node second = {2, NULL};
Node first = {1, &second};
printf("%d %d\n", first.value, first.next->value);
```

# --output--

1 2
