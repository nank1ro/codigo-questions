---
language: c
exerciseType: 2
---

# --description--

첫 번째 노드를 제거하는 것은 `push_front`의 거울상입니다: 두 번째 노드의 주소를 저장하고, 첫 번째 노드를 해제한 뒤, 저장한 주소를 새 헤드로 반환합니다. 호출자는 그 결과를 자신의 헤드 변수에 다시 저장합니다:
```c
Node *next = head->next;
free(head);
return next;
```
헤드가 `NULL`이 될 때까지 pop하면 호출 한 번에 노드 하나씩 리스트 전체를 해제합니다.

# --instructions--

`pop_front`를 완성하여 첫 번째 노드를 해제하고 두 번째 노드를 새 헤드로 반환하도록 하세요

# --seed--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

Node *pop_front(Node *head) {
    Node *next = [/];
    [/](head);
    return [/];
}

int main() {
    Node *head = NULL;
    head = push_front(head, 3);
    head = push_front(head, 2);
    head = push_front(head, 1);
    head = pop_front(head);
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        head = pop_front(head);
    }
    return 0;
}
```

# --answers--

- head->next
- free
- next
- head
- NULL

# --solutions--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

Node *pop_front(Node *head) {
    Node *next = head->next;
    free(head);
    return next;
}

int main() {
    Node *head = NULL;
    head = push_front(head, 3);
    head = push_front(head, 2);
    head = push_front(head, 1);
    head = pop_front(head);
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        head = pop_front(head);
    }
    return 0;
}
```

# --output--

2
3
