---
language: c
exerciseType: 4
---

# --description--

`sprintf`는 `printf`와 똑같이 동작하지만, 화면에 쓰는 대신 형식화된 텍스트를 **버퍼**라고 부르는 `char` 배열에 쓰고 그 뒤에 종료 문자 `'\0'`을 붙입니다:
```c
char buffer[32];
sprintf(buffer, "%d-%d", 3, 7);
printf("%s\n", buffer); // prints "3-7"
```
버퍼는 호출 전에 선언해야 하고 전체 텍스트에 종료 문자까지 담을 만큼 커야 합니다. 그렇지 않으면 `sprintf`가 버퍼 끝을 넘어서 씁니다.

# --instructions--

버퍼를 선언하고, `sprintf`로 그 안에 `50%`를 쓰고, 버퍼를 출력하도록 줄을 정렬하세요

# --answers--

```c
char buffer[32];
```

```c
sprintf(buffer, "%d%%", 50);
```

```c
printf("%s\n", buffer);
```

# --solutions--

```c
char buffer[32];
sprintf(buffer, "%d%%", 50);
printf("%s\n", buffer);
```

# --output--

50%
