---
language: c
exerciseType: 4
---

# --description--

`char`에 대한 포인터도 다른 포인터와 똑같이 동작합니다: 문자 하나의 주소를 담고, `*p`로 그 문자를 읽거나 씁니다:
```c
char grade = 'B';
char *p = &grade;
*p = 'A';
printf("%c\n", grade); // prints "A"
```
포인터를 통한 읽기와 쓰기는 자유롭게 섞을 수 있습니다: `*p = *p + 1`은 `'A'`를 `'B'`로 바꿉니다.

# --instructions--

`letter`를 `'a'`로 선언하고, `p`가 그것을 가리키게 한 다음, 포인터를 통해 문자를 `'b'`로 바꾸고 `letter`를 출력하도록 줄을 정렬하세요

# --answers--

```c
char letter = 'a';
```

```c
char *p = &letter;
```

```c
*p = 'b';
```

```c
printf("%c\n", letter);
```


# --solutions--

```c
char letter = 'a';
char *p = &letter;
*p = 'b';
printf("%c\n", letter);
```

# --output--

b
