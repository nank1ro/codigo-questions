---
language: c
exerciseType: 2
---

# --description--

C의 불리언 리터럴은 `true`와 `false`입니다: 소문자로 쓰고 따옴표 없이 사용하며, 언어에 내장된 것이 아니라 `<stdbool.h>`에서 정의됩니다. `True`라고 쓰면 조건으로 컴파일되지 않습니다 — 소문자 형태만 동작하며, 값이 `true`일 때 블록이 실행됩니다.

# --instructions--

코드 블록이 실행되도록 빈칸을 채우세요

# --seed--

```c
if ([/]) {
    printf("Run Code\n");
}
```

# --answers--

- True
- False
- true
- false
- "True"
- "False"

# --solutions--

```c
if (true) {
    printf("Run Code\n");
}
```

# --output--

Run Code
