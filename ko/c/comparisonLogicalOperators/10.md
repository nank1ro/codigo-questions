---
language: c
exerciseType: 4
---

# --description--

연산자에는 무엇을 먼저 계산할지 정하는 **우선순위**가 있습니다:
1. `!`가 먼저 적용됩니다
2. 그다음 관계 비교 `<`, `>`, `<=`, `>=`
3. 그다음 동등 비교 `==`, `!=`
4. 그다음 `&&`
5. 마지막으로 `||`

따라서 `a > 0 && a < 10`에는 괄호가 필요 없습니다. 두 비교 모두 `&&`보다 먼저 계산되기 때문입니다.
또한 `x == 1 || y == 2 && z == 3`은 `x == 1 || (y == 2 && z == 3)`을 의미합니다. `&&`가 `||`보다 결합력이 강하기 때문입니다.

# --instructions--

`a`가 `1`과 `9` 사이에 있는지를 `inside`에 저장하고, 그 부정을 `outside`에 저장한 뒤, 두 값을 공백으로 구분해 출력하도록 줄을 정렬하세요

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int a = 5;
```

```c
    int inside = a > 0 && a < 10;
```

```c
    int outside = !inside;
```

```c
    printf("%d %d\n", inside, outside);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int a = 5;
    int inside = a > 0 && a < 10;
    int outside = !inside;
    printf("%d %d\n", inside, outside);
    return 0;
}
```

# --output--

1 0
