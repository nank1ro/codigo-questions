---
language: c
exerciseType: 4
---

# --description--

`bool` 매개변수는 `true`와 비교하지 않고도 `&&`나 `||`의 피연산자로 바로 사용할 수 있습니다. `citizen == true`가 아니라 `age >= 18 && citizen`처럼 쓰세요.

# --instructions--

`age`가 `18` 이상이고 `citizen`이 `true`이면 `true`를 반환하는 `can_vote`와, `20, true`인 경우와 `20, false`인 경우의 결과를 출력하는 `main`을 정의하도록 줄을 정렬하세요. 함수는 `main`보다 먼저 정의하세요

# --answers--

```c
#include <stdio.h>
#include <stdbool.h>
```

```c
bool can_vote(int age, bool citizen) {
```

```c
    return age >= 18 && citizen;
}
```

```c
int main() {
```

```c
    printf("%d\n", can_vote(20, true));
```

```c
    printf("%d\n", can_vote(20, false));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
#include <stdbool.h>
bool can_vote(int age, bool citizen) {
    return age >= 18 && citizen;
}
int main() {
    printf("%d\n", can_vote(20, true));
    printf("%d\n", can_vote(20, false));
    return 0;
}
```

# --output--

1
0
