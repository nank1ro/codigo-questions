---
language: c
exerciseType: 3
---

# --description--

C에서 흔한 실수는 `==`를 써야 할 곳에 `=`를 쓰는 것입니다. 대입은 대입된 값을 값으로 갖는 식이므로, 이 코드는 그래도 컴파일됩니다:
```c
int x = 5;
if (x = 0) { ... } // x에 0을 대입하고, 조건은 0(거짓)이 된다
if (x = 3) { ... } // x에 3을 대입하고, 조건은 3(참)이 된다
```
대부분의 컴파일러는 이에 대해 경고를 하므로, 조건이 이상하게 동작할 때는 경고 메시지를 확인하세요.

# --instructions--

다음 코드의 출력은 무엇입니까?
```c
int x = 5;
if (x = 0) {
    printf("zero");
} else {
    printf("not zero");
}
```

# --answers--

- not zero
- zero
- 컴파일 오류
- 5

# --solutions--

- not zero
