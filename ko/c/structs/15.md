---
language: c
exerciseType: 4
---

# --description--

`printf`에는 구조체 전체를 위한 서식 지정자가 없습니다. 보통의 해결책은 멤버들을 고정된 형식으로 출력하는 작은 함수를 만드는 것으로, 프로그램의 모든 부분이 같은 방식으로 값을 보여주게 합니다:
```c
void print_person(Person p) {
    printf("%s (%d)\n", p.name, p.age);
}
```

# --instructions--

`Point` 타입과, 점을 `(x, y)`로 출력하는 함수 `print_point`, 그리고 점 `(3, 4)`를 출력하는 `main`을 정의하도록 줄을 정렬하세요. 함수를 `main`보다 먼저 정의하세요

# --answers--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
```

```c
void print_point(Point p) {
```

```c
    printf("(%d, %d)\n", p.x, p.y);
```

```c
}
```

```c
int main() {
```

```c
    Point p = {3, 4};
```

```c
    print_point(p);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
void print_point(Point p) {
    printf("(%d, %d)\n", p.x, p.y);
}
int main() {
    Point p = {3, 4};
    print_point(p);
    return 0;
}
```

# --output--

(3, 4)
