---
language: c
exerciseType: 4
---

# --instructions--

프로그램이 `password`와 `attempt`를 비교하여 `Access granted`를 출력하도록 줄을 정렬하세요. `password`를 `attempt`보다 먼저 선언하고, 각 메시지를 해당하는 분기 안에 넣으세요

# --answers--

```c
char password[] = "secret";
```

```c
char attempt[] = "secret";
```

```c
if (strcmp(password, attempt) == 0) {
```

```c
    printf("Access granted\n");
```

```c
} else {
```

```c
    printf("Access denied\n");
```

```c
}
```

# --solutions--

```c
char password[] = "secret";
char attempt[] = "secret";
if (strcmp(password, attempt) == 0) {
    printf("Access granted\n");
} else {
    printf("Access denied\n");
}
```

# --output--

Access granted
