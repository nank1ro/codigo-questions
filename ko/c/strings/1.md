---
language: c
exerciseType: 2
---

# --description--

C에는 전용 문자열 타입이 없습니다. **문자열**은 특수 문자인 **널 종료 문자** `'\0'`로 끝나는 `char` 배열입니다.
문자열을 만드는 가장 쉬운 방법은 큰따옴표로 감싼 문자열 리터럴을 사용하는 것입니다:
```c
char name[] = "Codigo";
```
컴파일러가 문자 수를 세어 끝에 자동으로 `'\0'`을 추가해 줍니다.
문자열을 출력하려면 `%s` 플레이스홀더를 사용합니다:
```c
printf("%s\n", name);
// prints "Codigo"
```

# --instructions--

텍스트 `Codigo`를 `char` 배열 `name`에 저장하고 출력하도록 코드를 완성하세요

# --seed--

```c
char [/][] = [/];
printf("[/]\n", name);
```

# --answers--

- name
- "name"
- "Codigo"
- 'Codigo'
- %s
- %c

# --solutions--

```c
char name[] = "Codigo";
printf("%s\n", name);
```

# --output--

Codigo
