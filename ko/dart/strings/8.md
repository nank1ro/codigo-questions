---
language: dart
exerciseType: 1
---

# --description--

문자열 안을 검색할 수 있는 여러 메서드가 있습니다:

- `.contains(other)`는 `other`가 문자열 어디든 나타나면 `true`를 반환합니다
- `.startsWith(other)`와 `.endsWith(other)`는 시작과 끝을 확인합니다
- `.indexOf(other)`는 처음 나타난 위치의 인덱스를 반환하며, 찾지 못하면 `-1`을 반환합니다

```dart
var file = 'report.pdf';
print(file.contains('port'));    // true
print(file.startsWith('rep'));   // true
print(file.endsWith('.txt'));    // false
print(file.indexOf('.'));        // 6
print(file.indexOf('x'));        // -1
```

이들은 모두 대소문자를 구분합니다. `'Dart'.contains('dart')`는 `false`입니다.

# --instructions--

문자열 `email`이 주어졌을 때, `@` 문자가 포함되어 있는지를 `bool`형 `hasAt`에, 그 문자의 인덱스를 `int`형 `atIndex`에, 이메일이 `example.com`으로 끝나는지를 `bool`형 `isExample`에 저장하세요.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var email = 'ada@example.com';
```

# --after-seed--

```dart
  return [hasAt, atIndex, isExample];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasAt`는 `true`여야 합니다

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`atIndex`는 `3`이어야 합니다

```dart
  test('test2', () {
    expect(result()[1], 3, reason: '--err-t2--');
  });
```

`isExample`는 `true`여야 합니다

```dart
  test('test3', () {
    expect(result()[2], true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var email = 'ada@example.com';
bool hasAt = email.contains('@');
int atIndex = email.indexOf('@');
bool isExample = email.endsWith('example.com');
```
