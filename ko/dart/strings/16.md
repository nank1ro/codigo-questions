---
language: dart
exerciseType: 1
---

# --description--

문자열은 불변이기 때문에, 반복문에서 `+=`로 긴 텍스트를 만들면 매 단계마다 새로운 문자열이 생성됩니다. **StringBuffer**는 텍스트 조각을 효율적으로 모아두었다가, 필요할 때만 최종 문자열을 만들어냅니다:

- `.write(value)`는 값을 추가합니다(어떤 타입이든 텍스트로 변환됩니다)
- `.writeln(value)`는 값을 추가한 뒤 줄바꿈을 붙입니다
- `.toString()`는 지금까지 만든 문자열을 반환합니다

```dart
var buffer = StringBuffer();
buffer.write('Hello');
buffer.write(', ');
buffer.writeln('Dart!');
buffer.write(42);
print(buffer.toString()); // Hello, Dart!\n42
```

# --instructions--

리스트 `items`가 주어졌을 때, `StringBuffer`를 사용해 각 항목이 자기 줄에, `1`부터 시작하는 번호와 마침표가 앞에 붙은(예: `1. milk`) `String`형 `menu`를 만드세요. 모든 줄은 줄바꿈으로 끝나야 합니다(`.writeln()`을 사용하세요).

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var items = ['milk', 'eggs', 'bread'];
```

# --after-seed--

```dart
  return menu;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`menu`는 `'1. milk\n2. eggs\n3. bread\n'`여야 합니다

```dart
  test('test1', () {
    expect(result(), '1. milk\n2. eggs\n3. bread\n', reason: '--err-t1--');
  });
```

`menu`는 `String`이어야 합니다

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var items = ['milk', 'eggs', 'bread'];
var buffer = StringBuffer();
for (var i = 0; i < items.length; i++) {
  buffer.writeln('${i + 1}. ${items[i]}');
}
String menu = buffer.toString();
```
