---
language: dart
exerciseType: 1
---

# --description--

`.split(separator)` 메서드는 구분자가 나타날 때마다 문자열을 잘라서 `List<String>`으로 만듭니다. 그 반대는 리스트의 메서드인 `.join(separator)`로, 요소들을 하나의 문자열로 이어붙입니다:

```dart
var csv = 'a,b,c';
List<String> parts = csv.split(',');
print(parts);            // [a, b, c]
print(parts.join(' - ')); // a - b - c
```

빈 구분자로 `.split('')`을 호출하면 문자 하나하나로 이루어진 리스트를 얻습니다.

# --instructions--

문자열 `csv`가 주어졌을 때, 쉼표로 나눈 부분들을 `List<String>`형 `colors`에, 그 부분들을 `' | '`로 이어붙인 것을 `String`형 `joined`에 저장하세요.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var csv = 'red,green,blue';
```

# --after-seed--

```dart
  return [colors, joined];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`colors`는 `['red', 'green', 'blue']`여야 합니다

```dart
  test('test1', () {
    expect(result()[0], ['red', 'green', 'blue'], reason: '--err-t1--');
  });
```

`joined`는 `'red | green | blue'`여야 합니다

```dart
  test('test2', () {
    expect(result()[1], 'red | green | blue', reason: '--err-t2--');
  });
```

`colors`는 `List`여야 합니다

```dart
  test('test3', () {
    expect(result()[0], isA<List>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var csv = 'red,green,blue';
List<String> colors = csv.split(',');
String joined = colors.join(' | ');
```
