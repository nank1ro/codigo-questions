---
language: dart
exerciseType: 1
---

# --description--

모든 문자열은 `.length` 프로퍼티를 통해 자신이 담고 있는 문자 수를 알고 있습니다. 공백과 문장 부호도 문자로 셉니다:

```dart
var word = 'hello';
print(word.length);      // 5
print('a b'.length);     // 3
```

대괄호와 `0`부터 시작하는 **인덱스**를 사용해 문자 하나를 읽을 수 있습니다. 결과는 한 글자짜리 `String`입니다:

```dart
print(word[0]);                // h
print(word[word.length - 1]);  // o
```

문자열 범위를 벗어난 인덱스(예: `word[5]`)를 읽으면 오류가 발생합니다.

# --instructions--

문자열 `word`가 주어졌을 때, 그 문자 수를 `int`형 `size`에, 첫 번째 문자를 `String`형 `first`에, 마지막 문자를 `String`형 `last`에 저장하세요.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var word = 'Flutter';
```

# --after-seed--

```dart
  return [size, first, last];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`size`는 `7`이어야 합니다

```dart
  test('test1', () {
    expect(result()[0], 7, reason: '--err-t1--');
  });
```

`first`는 `'F'`여야 합니다

```dart
  test('test2', () {
    expect(result()[1], 'F', reason: '--err-t2--');
  });
```

`last`는 `'r'`이어야 합니다

```dart
  test('test3', () {
    expect(result()[2], 'r', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var word = 'Flutter';
int size = word.length;
String first = word[0];
String last = word[word.length - 1];
```
