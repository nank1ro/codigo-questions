---
language: dart
exerciseType: 4
---

# --description--

함수가 `await`를 만나면 그 줄에서 **멈추고**, 프로그램의 나머지는 계속 진행됩니다. `await` 뒤의 줄들은 future가 완료된 뒤에야 실행됩니다. 그래서 `async` 함수를 위에서 아래로 읽으면 그 효과가 일어나는 정확한 순서를 알 수 있습니다:

```dart
Future<int> load() async {
  await Future.delayed(const Duration(milliseconds: 5));
  return 7;
}

Future<void> main() async {
  print('loading');
  final n = await load();
  print('value: $n');
  print('done');
}
// loading
// value: 7
// done
```

# --instructions--

항목을 정렬해 `10` 밀리초를 기다린 뒤 `'Ada'`를 반환하는 `async` 함수 `fetchName`과, `fetching`을 출력하고 `fetchName()`을 await 해서 `name`에 담은 뒤 `Hello, ` 뒤에 이름과 `!`를 붙여 출력하고 마지막으로 `done`을 출력하는 `async` `main`을 정의하세요. `fetchName`을 `main`보다 먼저 정의하세요. 프로그램은 다음을 출력해야 합니다:

```
fetching
Hello, Ada!
done
```

# --answers--

```dart
Future<String> fetchName() async {
```

```dart
  await Future.delayed(const Duration(milliseconds: 10));
```

```dart
  return 'Ada';
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  print('fetching');
```

```dart
  final name = await fetchName();
```

```dart
  print('Hello, $name!');
```

```dart
  print('done');
```

```dart
}
```

# --solutions--

```dart
Future<String> fetchName() async {
  await Future.delayed(const Duration(milliseconds: 10));
  return 'Ada';
}

Future<void> main() async {
  print('fetching');
  final name = await fetchName();
  print('Hello, $name!');
  print('done');
}
```

# --output--

fetching
Hello, Ada!
done
