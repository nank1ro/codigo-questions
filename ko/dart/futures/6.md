---
language: dart
exerciseType: 1
---

# --description--

`async` 함수 안에서 `await`는 비동기 단계를 평범한 순차 코드처럼 쓸 수 있게 해 줍니다. 각 `await`는 자신의 future를 기다리고, 다음 줄은 값이 준비된 뒤에야 실행됩니다:

```dart
Future<int> width() => Future.delayed(const Duration(milliseconds: 5), () => 4);
Future<int> height() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<int> area() async {
  final w = await width();
  final h = await height();
  return w * h; // 12
}
```

`await`는 식 안에서 바로 쓸 수도 있습니다. `return await width() * await height();`도 같은 결과를 줍니다.

# --instructions--

함수 `price(item)`은 `item`의 가격이 담긴 `Future<int>`를 반환합니다. `await`를 사용해 `'apple'`의 가격과 `'milk'`의 가격을 얻고, 그 합을 `total`에 저장하세요.

# --before-seed--

```dart
const prices = {'apple': 3, 'bread': 5, 'milk': 2};

Future<int> price(String item) {
  return Future.delayed(const Duration(milliseconds: 5), () => prices[item]!);
}

Future<dynamic> result() async {
```

# --seed--

```dart
var total = 0;
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total`은 `5`여야 합니다

```dart
  test('test1', () async {
    expect(await result(), 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var total = 0;
total = await price('apple') + await price('milk');
```
