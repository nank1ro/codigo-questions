---
language: dart
exerciseType: 1
---

# --description--

`Future.value`는 곧바로 완료됩니다. 시간이 걸리는 작업을 흉내 내려면 **`Future.delayed`**를 사용하세요. `Duration`과 함수를 받아 그 시간만큼 기다린 뒤, 함수가 반환한 값으로 완료됩니다:

```dart
Future<String> slowHello() {
  return Future.delayed(const Duration(milliseconds: 10), () => 'hello');
}
```

`Duration`은 `seconds`, `milliseconds`, `minutes` 같은 이름 있는 매개변수로 만듭니다. `async` 함수 안에서는 값 없이 지연만 await 해서 잠시 멈출 수도 있습니다:

```dart
Future<int> slowDouble(int n) async {
  await Future.delayed(const Duration(milliseconds: 10));
  return n * 2;
}
```

두 방식 모두 흔합니다. 두 번째는 평범한 순차 코드처럼 읽힙니다.

# --instructions--

`int` `n`을 받아 `10` 밀리초의 지연 뒤에 `n * n`으로 완료되는 `Future<int>`를 반환하는 함수 `slowSquare`를 작성하세요.

예시:
```dart
print(await slowSquare(4)); // 16, after 10 ms
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`slowSquare(4)`는 `16`으로 완료되어야 합니다

```dart
  test('test1', () async {
    expect(await slowSquare(4), 16, reason: '--err-t1--');
  });
```

`slowSquare(-3)`은 `9`로, `slowSquare(0)`은 `0`으로 완료되어야 합니다

```dart
  test('test2', () async {
    expect(await slowSquare(-3), 9, reason: '--err-t2--');
    expect(await slowSquare(0), 0, reason: '--err-t2--');
  });
```

`slowSquare`는 완료까지 최소 `10` 밀리초가 걸리는 `Future<int>`를 반환해야 합니다

```dart
  test('test3', () async {
    final future = slowSquare(5);
    expect(future, isA<Future<int>>(), reason: '--err-t3--');
    final watch = Stopwatch()..start();
    await future;
    expect(watch.elapsedMilliseconds, greaterThanOrEqualTo(9), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> slowSquare(int n) {
  return Future.delayed(const Duration(milliseconds: 10), () => n * n);
}
```
