---
language: dart
exerciseType: 4
---

# --description--

`Future.wait`는 "여러 개를 불러온 다음 계속하기"를 위한 도구입니다. 전형적인 형태는 future 리스트를 만들고, 거기에 `await Future.wait`를 적용한 뒤, 나온 리스트를 사용하는 것입니다:

```dart
Future<int> stock() => Future.delayed(const Duration(milliseconds: 10), () => 8);
Future<int> orders() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<void> main() async {
  final counts = await Future.wait([stock(), orders()]);
  print(counts); // [8, 3]
}
```

`orders()`가 먼저 완료되지만, 리스트는 여전히 호출 순서를 따릅니다. `stock()`이 첫 번째, `orders()`가 두 번째입니다.

# --instructions--

항목을 정렬해 `10` 밀리초 뒤 `1`로 완료되는 함수 `one`, `5` 밀리초 뒤 `2`로 완료되는 함수 `two`, 그리고 `loading`을 출력하고 `Future.wait`로 두 함수를 기다려 `values`에 담은 뒤 `values`를 출력하는 `async` `main`을 정의하세요. `one`을 먼저, 그다음 `two`, 그다음 `main`을 정의하세요. 프로그램은 다음을 출력해야 합니다:

```
loading
[1, 2]
```

# --answers--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
```

```dart
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);
```

```dart
Future<void> main() async {
```

```dart
  print('loading');
```

```dart
  final values = await Future.wait([one(), two()]);
```

```dart
  print(values);
```

```dart
}
```

# --solutions--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);

Future<void> main() async {
  print('loading');
  final values = await Future.wait([one(), two()]);
  print(values);
}
```

# --output--

loading
[1, 2]
