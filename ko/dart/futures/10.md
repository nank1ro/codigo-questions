---
language: dart
exerciseType: 2
---

# --description--

콜백을 쓸 때 오류는 `then`의 짝인 **`catchError`**가 처리합니다. 둘 다 새로운 future를 반환하므로 보통 이어 붙여서 씁니다. future가 성공하면 `then`이 값을 받고, 실패하면 `catchError`가 오류를 받으며, 두 콜백 중 하나만 실행됩니다:

```dart
Future<String> download() async {
  throw StateError('no network');
}

void main() {
  download()
      .then((data) => print('data: $data'))
      .catchError((e) => print('error: $e'));
  print('requested');
}
// requested
// error: Bad state: no network
```

`then` 뒤에 놓인 `catchError`는 `then` 콜백 안에서 던져진 오류도 잡습니다. `then`과 마찬가지로 체인 뒤의 코드가 먼저 실행됩니다. 콜백은 현재 코드가 끝난 뒤에야 호출되기 때문입니다.

# --instructions--

다운로드가 성공하면 `data: ` 뒤에 데이터를, 실패하면 `error: ` 뒤에 오류를 출력하도록 체인을 완성하세요. 프로그램은 `requested`를 출력한 다음 `error: Bad state: no network`를 출력해야 합니다.

# --seed--

```dart
Future<String> download() async {
  throw StateError('no network');
}

void main() {
  download()
      .[/]((data) => print('data: $data'))
      .[/]((e) => print('error: $e'));
  print('requested');
}
```

# --answers--

- then
- catchError
- catch
- onError
- await
- whenComplete

# --solutions--

```dart
Future<String> download() async {
  throw StateError('no network');
}

void main() {
  download()
      .then((data) => print('data: $data'))
      .catchError((e) => print('error: $e'));
  print('requested');
}
```

# --output--

requested
error: Bad state: no network
