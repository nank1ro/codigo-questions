---
language: dart
exerciseType: 2
---

# --description--

오류는 그것을 잡지 않는 모든 `await`를 거쳐 **전파**됩니다. `load()`가 실패하면 `loadTwice()` 안의 `await load()`가 예외를 던지고, `loadTwice`에는 `try`/`catch`가 없으므로 그 future도 같은 오류로 실패합니다. 이런 식으로 어떤 `await`가 `try`/`catch`로 감싸질 때까지 사슬을 따라 올라갑니다:

```dart
Future<int> load() async {
  throw StateError('offline');
}

Future<int> loadTwice() async {
  final n = await load();   // throws here, loadTwice fails too
  return n * 2;             // never runs
}

Future<void> main() async {
  try {
    print(await loadTwice());
  } catch (e) {
    print('failed: $e');    // failed: Bad state: offline
  }
}
```

이는 동기 호출에서 예외가 전파되는 방식과 같습니다. 무엇을 해야 할지 아는 계층에서 한 번만 처리하면 됩니다.

# --instructions--

`fetch`가 비동기이면서 예외를 던지고, `fetchLength`가 그것을 await 하며, `main`이 오류를 잡아 `failed: ` 뒤에 그것을 붙여 출력하도록 코드를 완성하세요. 프로그램은 `failed: Bad state: no server`를 출력해야 합니다.

# --seed--

```dart
Future<String> fetch() [/] {
  throw StateError('no server');
}

Future<int> fetchLength() async {
  final text = [/] fetch();
  return text.length;
}

Future<void> main() async {
  [/] {
    print(await fetchLength());
  } [/] (e) {
    print('failed: $e');
  }
}
```

# --answers--

- async
- await
- try
- catch
- then
- finally
- sync*

# --solutions--

```dart
Future<String> fetch() async {
  throw StateError('no server');
}

Future<int> fetchLength() async {
  final text = await fetch();
  return text.length;
}

Future<void> main() async {
  try {
    print(await fetchLength());
  } catch (e) {
    print('failed: $e');
  }
}
```

# --output--

failed: Bad state: no server
