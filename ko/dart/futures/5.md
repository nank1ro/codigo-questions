---
language: dart
exerciseType: 2
---

# --description--

future를 쓰는 방법이 `await`만 있는 것은 아닙니다. **`then`**으로 **콜백**을 등록할 수도 있습니다. 넘긴 함수는 future가 완료되면 그 값과 함께 호출됩니다. `await`와 달리 `then`은 현재 함수를 멈추지 **않으므로**, 그 뒤의 코드가 먼저 실행됩니다:

```dart
Future<int> fetchNumber() => Future.value(42);

void main() {
  fetchNumber().then((n) => print('got $n'));
  print('waiting');
}
// waiting
// got 42
```

`Future.value`로 만든 future조차 현재 코드가 끝난 뒤에야 값을 전달합니다. 그래서 `waiting`이 먼저 출력됩니다. `then`은 `async`든 아니든 어떤 함수에서나 동작합니다.

# --instructions--

`fetchNumber`가 `42`로 완료된 future를 반환하고, `main`이 `got ` 뒤에 값을 붙여 출력하는 콜백을 등록하도록 코드를 완성하세요. 프로그램은 `waiting`을 출력한 다음 `got 42`를 출력해야 합니다.

# --seed--

```dart
Future<int> fetchNumber() => Future.[/](42);

void main() {
  fetchNumber().[/]((n) => print('got $n'));
  print('waiting');
}
```

# --answers--

- value
- then
- await
- catchError
- delayed
- listen

# --solutions--

```dart
Future<int> fetchNumber() => Future.value(42);

void main() {
  fetchNumber().then((n) => print('got $n'));
  print('waiting');
}
```

# --output--

waiting
got 42
