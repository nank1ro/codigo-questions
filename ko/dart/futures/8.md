---
language: dart
exerciseType: 1
---

# --description--

future는 **오류**로 완료될 수도 있습니다. `async` 함수가 예외를 던지면 예외가 곧바로 빠져나가지 않고, 반환된 future의 오류가 됩니다. 그 future를 `await` 하는 쪽은 `await` 지점에서 오류가 던져진 것으로 보게 되므로, 평범한 `try`/`catch`로 처리할 수 있습니다:

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s); // throws FormatException for 'abc'
}

Future<int> orZero(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return 0;
  }
}
```

`try` 안의 `await`가 핵심입니다. `return parseLater(s);`는 future를 **기다리지 않고** 호출자에게 넘겨 버리므로, 오류는 `try` 블록이 이미 끝난 뒤에 도착하고 `catch`는 절대 실행되지 않습니다.

# --instructions--

함수 `parseLater(s)`는 `s`에 적힌 숫자로 완료되는 `Future<int>`를 반환하고, `s`가 숫자가 아니면 오류로 완료됩니다. `String` `s`를 받아 해석한 숫자로 완료되거나, `parseLater`가 실패하면 `-1`로 완료되는 `Future<int>`를 반환하는 `async` 함수 `safeParse`를 작성하세요.

예시:
```dart
print(await safeParse('12'));  // 12
print(await safeParse('abc')); // -1
```

# --before-seed--

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s);
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeParse('12')`는 `12`로 완료되어야 합니다

```dart
  test('test1', () async {
    expect(await safeParse('12'), 12, reason: '--err-t1--');
  });
```

`safeParse('abc')`는 예외를 던지는 대신 `-1`로 완료되어야 합니다

```dart
  test('test2', () async {
    expect(await safeParse('abc'), -1, reason: '--err-t2--');
  });
```

`safeParse('-7')`은 `-7`로, `safeParse('')`는 `-1`로 완료되어야 합니다

```dart
  test('test3', () async {
    expect(await safeParse('-7'), -7, reason: '--err-t3--');
    expect(await safeParse(''), -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> safeParse(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return -1;
  }
}
```
