---
language: dart
exerciseType: 1
---

# --description--

함수에 `async`를 붙이면 두 가지 일이 일어납니다. 본문 안에서 `await`를 쓸 수 있게 되고, 함수가 **`Future`를 반환**하게 됩니다. `return`으로 돌려주는 값이 곧 future가 완료되는 값이 되므로, 본문이 순수한 `T`를 반환해도 선언하는 반환 타입은 `Future<T>`입니다:

```dart
Future<String> label(int n) async {
  return 'item $n';
}

Future<void> main() async {
  print(await label(3)); // item 3
}
```

여기서는 `Future.value`가 필요 없습니다. `async` 키워드가 반환값을 대신 감싸 줍니다.

# --instructions--

`String` `name`을 받아 `Hello, ` 뒤에 이름과 느낌표를 붙인 값으로 완료되는 `Future<String>`을 반환하는 `async` 함수 `greet`을 작성하세요.

예시:
```dart
print(await greet('Ada')); // Hello, Ada!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greet('Ada')`는 `'Hello, Ada!'`로 완료되어야 합니다

```dart
  test('test1', () async {
    expect(await greet('Ada'), 'Hello, Ada!', reason: '--err-t1--');
  });
```

`greet('Linus')`는 `'Hello, Linus!'`로 완료되어야 합니다

```dart
  test('test2', () async {
    expect(await greet('Linus'), 'Hello, Linus!', reason: '--err-t2--');
  });
```

`greet`은 `Future<String>`을 반환해야 합니다

```dart
  test('test3', () async {
    expect(greet('Bob'), isA<Future<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<String> greet(String name) async {
  return 'Hello, $name!';
}
```
