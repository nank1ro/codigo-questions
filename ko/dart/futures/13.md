---
language: dart
exerciseType: 1
---

# --description--

두 future를 동시에 실행하는 데 `Future.wait`가 꼭 필요한 것은 아닙니다. `async` 함수는 **호출되는** 순간부터 첫 `await`까지 실행되기 시작합니다. 돌려받는 future는 이미 진행 중인 작업입니다. 그래서 요령은 **먼저 호출하고, 나중에 await 하기**입니다:

```dart
Future<int> pages() => Future.delayed(const Duration(milliseconds: 20), () => 300);
Future<int> words() => Future.delayed(const Duration(milliseconds: 20), () => 90000);

Future<double> average() async {
  final p = pages();          // both timers start now
  final w = words();
  return await w / await p;   // waits once, about 20 ms in total
}
```

`return await words() / await pages();`와 비교해 보세요. 여기서는 `pages()`가 `words()`가 완료된 뒤에야 호출됩니다. 결과는 같지만 시간은 두 배입니다. 두 번째 호출이 첫 번째의 결과를 필요로 하지 않는다면 언제나 동시 실행 형태를 택하세요.

# --instructions--

함수 `slow(value)`는 짧은 지연 뒤 `value`로 완료되는 `Future<int>`를 반환하며, 호출될 때 `start <value>`를, 완료될 때 `end <value>`를 리스트 `calls`에 기록합니다. 두 개의 `int` `a`와 `b`를 받아 `slow(a)`와 `slow(b)`가 **동시에** 실행되도록 호출하고, 두 결과의 합으로 완료되는 `Future<int>`를 반환하는 `async` 함수 `addConcurrently`를 작성하세요.

예시:
```dart
print(await addConcurrently(2, 3)); // 5
print(calls); // [start 2, start 3, end 2, end 3]
```

# --before-seed--

```dart
final calls = <String>[];

Future<int> slow(int value) async {
  calls.add('start $value');
  await Future.delayed(const Duration(milliseconds: 5));
  calls.add('end $value');
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`addConcurrently(2, 3)`은 `5`로 완료되어야 합니다

```dart
  test('test1', () async {
    calls.clear();
    expect(await addConcurrently(2, 3), 5, reason: '--err-t1--');
  });
```

`addConcurrently(2, 3)`은 둘 중 어느 하나가 완료되기 전에 `slow(2)`와 `slow(3)`을 호출해야 하므로, `calls`는 `[start 2, start 3, end 2, end 3]`이어야 합니다

```dart
  test('test2', () async {
    calls.clear();
    await addConcurrently(2, 3);
    expect(calls, ['start 2', 'start 3', 'end 2', 'end 3'], reason: '--err-t2--');
  });
```

`addConcurrently(10, -4)`는 `6`으로 완료되어야 하고, 둘 중 어느 하나가 완료되기 전에 두 호출이 모두 시작되어야 합니다

```dart
  test('test3', () async {
    calls.clear();
    expect(await addConcurrently(10, -4), 6, reason: '--err-t3--');
    expect(calls, ['start 10', 'start -4', 'end 10', 'end -4'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> addConcurrently(int a, int b) async {
  final first = slow(a);
  final second = slow(b);
  return await first + await second;
}
```
