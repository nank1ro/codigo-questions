---
language: dart
exerciseType: 1
---

# --instructions--

변수 `number`의 값을 `25`로 업데이트하세요

# --before-seed--

```dart
dynamic number() {
```

# --seed--

```dart
int number = 1;
```

# --after-seed--

```dart
  return number;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`number`는 `25`와 같아야 합니다.

```dart
    test('test1', () {
      expect(number(), 25, reason: '--err-t1--');
    });
```

`number`는 `int` 타입이어야 합니다

```dart
    test('test2', () {
      expect(number(), TypeMatcher<int>(), reason: '--err-t2--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int number = 1;
number = 25;
```
