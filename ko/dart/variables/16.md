---
language: dart
exerciseType: 1
---

# --instructions--

변수 `x`를 정수와 소수를 모두 지원하도록 숫자 `20`으로 초기화하세요.
변수를 숫자 `3.14159`로 업데이트하세요.

# --before-seed--

```dart
dynamic number() {
```

# --after-seed--

```dart
  return x;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x`는 `3.14159`와 같아야 합니다.

```dart
  test('test1', () {
    expect(number(), 3.14159, reason: '--err-t1--');
  });
```

`x`는 `num` 타입이어야 합니다

```dart
  test('test2', () {
    expect(number(), TypeMatcher<num>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
num x = 20;
x = 3.14159;
```
