---
language: dart
exerciseType: 1
---

# --description--

클래스는 **여러 믹스인을 한 번에** 사용할 수 있으며, `with` 뒤에 쉼표로 나열합니다. Dart는 이들을 **왼쪽에서 오른쪽으로** 적용하면서 각 믹스인을 이전 믹스인 위에 쌓습니다. 따라서 두 믹스인이 같은 멤버를 선언하면 목록에서 **마지막** 믹스인이 이깁니다:

```dart
mixin A {
  String who() => 'A';
}

mixin B {
  String who() => 'B';
}

class First with A, B {}
class Second with B, A {}

void main() {
  print(First().who());  // B, the last mixin in the list
  print(Second().who()); // A, the last mixin in the list
}
```

이 쌓임을 **선형화(linearization)**라고 합니다. `with A, B`는 `Object` → `A` → `B` → 클래스 자체의 체인을 만듭니다.

# --instructions--

`'quiet'`를 반환하는 메서드 `volume`을 가진 믹스인 `Quiet`와, `'loud'`를 반환하는 메서드 `volume`을 가진 믹스인 `Loud`를 선언하세요.

두 믹스인을 모두 사용하여 `Speaker().volume()`이 `'loud'`가 되는 클래스 `Speaker`와, 두 믹스인을 모두 사용하여 `Whisperer().volume()`이 `'quiet'`가 되는 클래스 `Whisperer`를 선언하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Speaker().volume()`은 `'loud'`이어야 합니다

```dart
  test('test1', () {
    expect(Speaker().volume(), 'loud', reason: '--err-t1--');
  });
```

`Whisperer().volume()`은 `'quiet'`이어야 합니다

```dart
  test('test2', () {
    expect(Whisperer().volume(), 'quiet', reason: '--err-t2--');
  });
```

두 클래스는 모두 두 믹스인을 사용해야 합니다

```dart
  test('test3', () {
    expect(Speaker(), isA<Quiet>(), reason: '--err-t3--');
    expect(Speaker(), isA<Loud>(), reason: '--err-t3--');
    expect(Whisperer(), isA<Quiet>(), reason: '--err-t3--');
    expect(Whisperer(), isA<Loud>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Quiet {
  String volume() => 'quiet';
}

mixin Loud {
  String volume() => 'loud';
}

class Speaker with Quiet, Loud {}

class Whisperer with Loud, Quiet {}
```
