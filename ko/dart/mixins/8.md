---
language: dart
exerciseType: 2
---

# --description--

마지막 믹스인이 이기기 때문에, `with` 목록의 순서는 스타일의 세부 사항이 아니라 클래스 의미의 일부입니다. 순서를 바꾸면 객체가 최종적으로 갖게 되는 구현이 달라집니다:

```dart
mixin Plain {
  String format(String text) => text;
}

mixin Starred {
  String format(String text) => '*$text*';
}

class Fancy with Plain, Starred {}  // format comes from Starred
class Simple with Starred, Plain {} // format comes from Plain
```

하나의 믹스인만 선언하는 멤버는 경쟁하지 않습니다. 순서와 상관없이 사용할 수 있습니다. `with X, Y`를 "`X`에서 시작해 `Y`가 이를 재정의하게 한다"고 읽으면 됩니다.

# --instructions--

`Message`가 각 믹스인을 한 번씩 사용하고, `format`이 텍스트를 그대로 두며, `frame`을 사용할 수 있도록 `with` 목록을 완성하세요.

# --seed--

```dart
mixin Plain {
  String format(String text) => text;
}

mixin Starred {
  String format(String text) => '*$text*';

  String frame(String text) => '($text)';
}

class Message with [/], [/] {}

void main() {
  print(Message().format('hi'));
  print(Message().frame('hi'));
}
```

# --answers--

- Starred
- Plain
- Message
- format
- frame

# --solutions--

```dart
mixin Plain {
  String format(String text) => text;
}

mixin Starred {
  String format(String text) => '*$text*';

  String frame(String text) => '($text)';
}

class Message with Starred, Plain {}

void main() {
  print(Message().format('hi'));
  print(Message().frame('hi'));
}
```

# --output--

hi
(hi)
