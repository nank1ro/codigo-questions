---
language: dart
exerciseType: 1
---

# --description--

맵이 특정 키를 가지고 있는지 확인하려면 `.containsKey(key)`를 사용합니다. 특정 값을 가진 쌍이 있는지 확인하려면 `.containsValue(value)`를 사용합니다. 둘 다 `bool`을 반환합니다:

```dart
var ages = {'Ann': 30, 'Bob': 25};
print(ages.containsKey('Ann')); // true
print(ages.containsKey('Zed')); // false
print(ages.containsValue(25)); // true
```

# --instructions--

맵 `ages`가 주어졌을 때, 키 `'Ann'`을 포함하는지를 `bool` `hasAnn`에, 값 `99`를 포함하는지를 `bool` `has99`에 저장하세요.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var ages = {'Ann': 30, 'Bob': 25};
```

# --after-seed--

```dart
  return [hasAnn, has99];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasAnn`은 `true`여야 합니다

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`has99`는 `false`여야 합니다

```dart
  test('test2', () {
    expect(result()[1], false, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var ages = {'Ann': 30, 'Bob': 25};
bool hasAnn = ages.containsKey('Ann');
bool has99 = ages.containsValue(99);
```
