---
language: dart
exerciseType: 1
---

# --instructions--

`&&`, `||`, `!`를 결합하여 결과를 만드세요.

이 정확한 식을 사용하여 불리언 변수 `result`를 선언하세요: `!false && (true || false)`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result`는 `true`와 같아야 합니다.

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result`는 `bool` 타입이어야 합니다.

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = !false && (true || false);
```
