---
language: dart
exerciseType: 2
---

# --description--

불리언 변수는 값 `false`도 가질 수 있습니다.
어떤 것이 사실이 아닐 때는 `false`를 사용하세요.

```dart
bool isLoggedIn = false;
print(isLoggedIn); // false
```

`true`처럼 `false`도 소문자로 작성되어야 합니다.

# --instructions--

`isExpired`이라는 불리언 변수를 선언하고 값 `false`를 할당한 후 출력하세요.

# --seed--

```dart
[/] [/] = [/];
print([/]);
```

# --answers--

- bool
- isExpired
- false
- true
- print
- String

# --solutions--

```dart
bool isExpired = false;
print(isExpired);
```

# --output--

false
