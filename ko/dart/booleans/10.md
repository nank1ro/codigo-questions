---
language: dart
exerciseType: 1
---

# --instructions--

`&&` 연산자(AND)는 **두 피연산자 모두** `true`일 때만 `true`를 반환합니다.

`&&` 연산자를 사용하여 `true && true`를 결합하는 불리언 변수 `result`를 선언하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result`는 `true`와 같아야 합니다.

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result`는 `bool` 타입이어야 합니다.

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = true && true;
```
