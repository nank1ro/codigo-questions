---
language: dart
exerciseType: 1
---

# --instructions--

값이 `true`인 `flag`이라는 불리언 변수를 선언한 후, 다른 불리언 변수 `isFlag`를 선언하고 `is` 키워드를 사용하여 `flag`가 `bool` 타입인지 확인하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`flag`는 `true`와 같아야 합니다.

```dart
  test('test1', () {
    expect(flag, true, reason: '--err-t1--');
  });
```

`isFlag`는 `true`여야 합니다 (`is bool` 검사의 결과).

```dart
  test('test2', () {
    expect(isFlag, true, reason: '--err-t2--');
  });
```

`isFlag`는 `bool` 타입이어야 합니다.

```dart
  test('test3', () {
    expect(isFlag, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool flag = true;
bool isFlag = flag is bool;
```
