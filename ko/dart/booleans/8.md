---
language: dart
exerciseType: 1
---

# --instructions--

`isEmpty`이라는 불리언 변수를 선언하고 값 `false`를 할당하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isEmpty`는 `false`와 같아야 합니다.

```dart
  test('test1', () {
    expect(isEmpty, false, reason: '--err-t1--');
  });
```

`isEmpty`는 `bool` 타입이어야 합니다.

```dart
  test('test2', () {
    expect(isEmpty, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isEmpty = false;
```
