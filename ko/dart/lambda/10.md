---
language: dart
exerciseType: 1
---

# --description--

`reduce`는 리스트의 모든 요소를 하나의 값으로 결합합니다. 이 익명 함수는 두 개의 매개변수, 즉 **지금까지 누적된 값**과 **다음 요소**를 받아 새로운 누적 값을 반환합니다. 첫 번째 요소가 시작점으로 사용됩니다:

```dart
var numbers = [2, 3, 4];

var product = numbers.reduce((total, n) => total * n);
print(product); // 24
```

`reduce`는 시작할 첫 번째 요소가 없기 때문에 빈 리스트에서는 오류를 던집니다.

# --instructions--

익명 함수와 함께 `reduce`를 사용하여, 비어 있지 않은 `List<String>` 매개변수 `words`를 받아 가장 긴 단어를 반환하는 함수 `longest`를 작성하세요. 두 단어의 길이가 같다면 먼저 나오는 단어를 유지하세요.

예시:
```dart
print(longest(['sun', 'planet', 'moon'])); // planet
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`longest(['sun', 'planet', 'moon'])`은 `'planet'`이어야 합니다

```dart
  test('test1', () {
    expect(longest(['sun', 'planet', 'moon']), 'planet', reason: '--err-t1--');
  });
```

`longest(['a'])`는 `'a'`여야 합니다

```dart
  test('test2', () {
    expect(longest(['a']), 'a', reason: '--err-t2--');
  });
```

`longest(['red', 'blue', 'pink'])`은 `'blue'`여야 합니다

```dart
  test('test3', () {
    expect(longest(['red', 'blue', 'pink']), 'blue', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String longest(List<String> words) {
  return words.reduce((best, word) => word.length > best.length ? word : best);
}
```
