---
language: dart
exerciseType: 4
---

# --description--

익명 함수는 이름 있는 함수와 같은 종류의 매개변수를 지원합니다. `[]`의 **선택적 위치** 매개변수와 `{}`의 **이름 있는** 매개변수이며, 둘 다 기본값을 가질 수 있습니다:

```dart
var repeat = (String text, [int times = 2]) => text * times;
print(repeat('ab'));    // abab
print(repeat('ab', 3)); // ababab

var describe = ({required String item, int count = 1}) => '$count x $item';
print(describe(item: 'apple'));           // 1 x apple
print(describe(item: 'pear', count: 4));  // 4 x pear
```

# --instructions--

항목을 정렬하여 기본값이 `'World'`인 이름 있는 매개변수 `name`을 가지는 익명 함수를 `greet`에 저장한 다음, 먼저 인자 없이, 그다음에는 `name: 'Dart'`로 호출하세요. 프로그램은 다음을 출력해야 합니다:

```
Hello, World!
Hello, Dart!
```

# --answers--

```dart
void main() {
```

```dart
  var greet = ({String name = 'World'}) {
```

```dart
    print('Hello, $name!');
```

```dart
  };
```

```dart
  greet();
```

```dart
  greet(name: 'Dart');
```

```dart
}
```

# --solutions--

```dart
void main() {
  var greet = ({String name = 'World'}) {
    print('Hello, $name!');
  };
  greet();
  greet(name: 'Dart');
}
```

# --output--

Hello, World!
Hello, Dart!
