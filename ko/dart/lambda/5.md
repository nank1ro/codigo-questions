---
language: dart
exerciseType: 1
---

# --description--

함수 타입도 일반 타입이기 때문에, 함수는 **다른 함수를 매개변수로** 받을 수 있습니다. 본문 안에서 이 매개변수는 다른 함수처럼 호출합니다:

```dart
int apply(int n, int Function(int) operation) {
  return operation(n);
}

print(apply(5, (n) => n * 2)); // 10
print(apply(5, (n) => n - 1)); // 4
```

여기서 호출자는 두 번째 인자로 익명 함수를 전달함으로써 `apply`가 무엇을 할지 결정합니다.

# --instructions--

`int` 매개변수 `n`과 `int Function(int)` 타입의 함수 매개변수 `operation`을 받아, `operation`을 `n`에 두 번 적용한 결과를 반환하는 함수 `applyTwice`를 작성하세요.

예시:
```dart
print(applyTwice(3, (n) => n * 2)); // 12
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`applyTwice(3, (n) => n * 2)`는 `12`여야 합니다

```dart
  test('test1', () {
    expect(applyTwice(3, (n) => n * 2), 12, reason: '--err-t1--');
  });
```

`applyTwice(10, (n) => n + 5)`는 `20`이어야 합니다

```dart
  test('test2', () {
    expect(applyTwice(10, (n) => n + 5), 20, reason: '--err-t2--');
  });
```

`applyTwice(2, (n) => n * n)`는 `16`이어야 합니다

```dart
  test('test3', () {
    expect(applyTwice(2, (n) => n * n), 16, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int applyTwice(int n, int Function(int) operation) {
  return operation(operation(n));
}
```
