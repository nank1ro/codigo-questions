---
language: dart
exerciseType: 1
---

# --description--

`map`과 `where`는 둘 다 `Iterable`을 반환하므로, 이들의 호출은 서로 연속해서 **연결**할 수 있습니다. 각 단계는 이전 단계의 결과를 받으며, `toList()`는 마지막에 한 번 호출합니다:

```dart
var numbers = [1, 2, 3, 4, 5, 6];

var result = numbers.where((n) => n > 3).map((n) => n * 10).toList();
print(result); // [40, 50, 60]
```

# --instructions--

`List<int>` 매개변수 `numbers`를 받아 원래 순서대로 홀수의 제곱만 담은 `List<int>`를 반환하는 함수 `oddSquares`를 작성하세요. 익명 함수와 함께 `where`와 `map`을 사용하세요.

예시:
```dart
print(oddSquares([1, 2, 3, 4, 5])); // [1, 9, 25]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`oddSquares([1, 2, 3, 4, 5])`는 `[1, 9, 25]`여야 합니다

```dart
  test('test1', () {
    expect(oddSquares([1, 2, 3, 4, 5]), [1, 9, 25], reason: '--err-t1--');
  });
```

`oddSquares([2, 4, 6])`는 `[]`여야 합니다

```dart
  test('test2', () {
    expect(oddSquares([2, 4, 6]), [], reason: '--err-t2--');
  });
```

`oddSquares([7, 10, 3])`는 `[49, 9]`여야 합니다

```dart
  test('test3', () {
    expect(oddSquares([7, 10, 3]), [49, 9], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> oddSquares(List<int> numbers) {
  return numbers.where((n) => n % 2 != 0).map((n) => n * n).toList();
}
```
