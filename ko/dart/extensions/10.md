---
language: dart
exerciseType: 1
---

# --description--

Dart에서는 타입이 `+`, `*`, `==` 같은 연산자가 자신의 값들에 대해 무엇을 의미하는지 정의할 수 있습니다. 이를 위해 키워드 `operator` 뒤에 기호를 붙인 이름의 메서드를 사용합니다. 연산자의 오른쪽 항이 메서드의 매개변수입니다:

```dart
extension Scaling on List<int> {
  List<int> operator *(int factor) => map((n) => n * factor).toList();
}

void main() {
  print([1, 2, 3] * 10); // [10, 20, 30]
}
```

확장은 연산자를 선언할 수 있으므로, 기존 타입에 아직 없는 새 연산자를 줄 수 있습니다. `String`은 `+`와 `*`는 가지고 있지만 `-`는 없으므로, 확장이 `'hello world' - 'o'`가 무엇을 의미하는지 정의할 수 있습니다.

# --instructions--

`String` 매개변수 `part`를 받아 `part`가 나타나는 모든 부분을 제거한 문자열을 반환하는 연산자 `-`를 가진, `String`에 대한 `Removing`이라는 확장을 작성하세요. `replaceAll`을 사용하세요.

예:
```dart
print('hello world' - 'o'); // hell wrld
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`'hello world' - 'o'`는 `'hell wrld'`이어야 합니다

```dart
  test('test1', () {
    expect('hello world' - 'o', 'hell wrld', reason: '--err-t1--');
  });
```

`'banana' - 'an'`은 `'ba'`이어야 합니다

```dart
  test('test2', () {
    expect('banana' - 'an', 'ba', reason: '--err-t2--');
  });
```

`'abc' - 'z'`는 `'abc'`, `'aaa' - 'a'`는 `''`이어야 합니다

```dart
  test('test3', () {
    expect('abc' - 'z', 'abc', reason: '--err-t3--');
    expect('aaa' - 'a', '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Removing on String {
  String operator -(String part) => replaceAll(part, '');
}
```
