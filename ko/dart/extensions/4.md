---
language: dart
exerciseType: 4
---

# --description--

`on` 뒤의 타입은 `List<int>` 같은 **매개변수화된** 타입일 수 있습니다. 그러면 확장은 그 요소 타입의 리스트에만 적용됩니다: `[1, 2].total()`은 동작하지만 `['a', 'b'].total()`은 컴파일되지 않습니다.

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}
```

확장 내부에서 `this`는 그 리스트이므로, 평소처럼 순회하거나 인덱스로 접근하거나 `length`를 호출할 수 있습니다.

# --instructions--

항목을 정렬해서 `for` 반복문으로 요소들을 더하는 메서드 `total`을 가진 `List<int>`에 대한 확장 `Totals`를 선언하고, `[3, 4, 5]`의 합계를 출력하는 `main`을 선언하세요. 확장은 `main` 앞에 선언하세요. 프로그램은 다음을 출력해야 합니다:

```
12
```

# --answers--

```dart
extension Totals on List<int> {
```

```dart
  int total() {
```

```dart
    var sum = 0;
```

```dart
    for (final n in this) {
```

```dart
      sum += n;
```

```dart
    }
```

```dart
    return sum;
```

```dart
  }
```

```dart
}
```

```dart
void main() {
```

```dart
  print([3, 4, 5].total());
```

```dart
}
```

# --solutions--

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}

void main() {
  print([3, 4, 5].total());
}
```

# --output--

12
