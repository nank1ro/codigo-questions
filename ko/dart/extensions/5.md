---
language: dart
exerciseType: 1
---

# --description--

`List<int>`에 대한 확장은 `List<String>`에 사용할 수 없습니다. **모든** 요소 타입에 동작하는 확장 하나를 작성하려면, 확장에 **타입 매개변수**를 주고(이름 뒤 꺾쇠 괄호 사이에 씁니다) `on` 타입에 그것을 사용하세요:

```dart
extension Firsts<T> on List<T> {
  T get firstOrLast => length > 1 ? this[0] : this[length - 1];
}
```

`T`는 "요소 타입이 무엇이든"에 해당하는 자리표시자입니다: `List<int>`에서는 `int`가 되고, `List<String>`에서는 `String`이 되므로, 위 게터는 그에 따라 `int`나 `String`을 반환합니다. 컴파일러가 호출할 때마다 `T`를 채워 넣어 줍니다.

```dart
print([7, 8, 9].firstOrLast); // 7
print(['a', 'b'].firstOrLast); // a
```

# --instructions--

`List<T>`에 대한, 타입 매개변수 `T`를 가진 `Seconds`라는 확장을 작성하세요. 이 확장에는 인덱스 `1`의 요소를 반환하고 리스트에 요소가 두 개 미만이면 `null`을 반환하는 `T?` 타입의 게터 `secondOrNull`이 있습니다.

예:
```dart
print([10, 20, 30].secondOrNull); // 20
print(['solo'].secondOrNull);     // null
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`[10, 20, 30].secondOrNull`은 `20`이어야 합니다

```dart
  test('test1', () {
    expect([10, 20, 30].secondOrNull, 20, reason: '--err-t1--');
  });
```

`['a', 'b'].secondOrNull`은 `'b'`이어야 합니다

```dart
  test('test2', () {
    expect(['a', 'b'].secondOrNull, 'b', reason: '--err-t2--');
  });
```

`['solo'].secondOrNull`과 `<int>[].secondOrNull`은 `null`이어야 합니다

```dart
  test('test3', () {
    expect(['solo'].secondOrNull, null, reason: '--err-t3--');
    expect(<int>[].secondOrNull, null, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Seconds<T> on List<T> {
  T? get secondOrNull => length > 1 ? this[1] : null;
}
```
