---
language: dart
exerciseType: 1
---

# --description--

확장은 **static** 멤버를 선언할 수 있습니다. 클래스에서와 마찬가지로 static 멤버는 어떤 값이 아니라 확장 자체에 속하며, 확장하는 타입이 아니라 **확장의 이름**을 통해 접근합니다:

```dart
extension Temperatures on double {
  static const double boiling = 100.0;

  static bool isBoiling(double celsius) => celsius >= boiling;
}

void main() {
  print(Temperatures.boiling);         // 100.0
  print(Temperatures.isBoiling(37.5)); // false
  print(double.boiling);               // error: 'boiling' isn't defined for 'double'
}
```

static 멤버는 확장하는 타입과 관련된 상수와 헬퍼 함수를 두기에 편리한 곳입니다.

# --instructions--

`int`에 대한 `Dice`라는 확장을 작성하세요:
- 값이 `6`인 `int` 타입의 static 상수 `sides`
- `int` 매개변수 `value`를 받아 그 값이 `1`부터 `sides` 사이(양쪽 끝 포함)에 있을 때 `true`를 반환하는 static 메서드 `isValid`

예:
```dart
print(Dice.sides);      // 6
print(Dice.isValid(7)); // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Dice.sides`는 `6`이어야 합니다

```dart
  test('test1', () {
    expect(Dice.sides, 6, reason: '--err-t1--');
  });
```

`Dice.isValid(1)`과 `Dice.isValid(6)`은 `true`이어야 합니다

```dart
  test('test2', () {
    expect(Dice.isValid(1), true, reason: '--err-t2--');
    expect(Dice.isValid(6), true, reason: '--err-t2--');
  });
```

`Dice.isValid(0)`과 `Dice.isValid(7)`은 `false`이어야 합니다

```dart
  test('test3', () {
    expect(Dice.isValid(0), false, reason: '--err-t3--');
    expect(Dice.isValid(7), false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Dice on int {
  static const int sides = 6;

  static bool isValid(int value) => value >= 1 && value <= sides;
}
```
