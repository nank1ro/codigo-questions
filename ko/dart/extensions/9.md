---
language: dart
exerciseType: 4
---

# --description--

확장은 SDK 타입만을 위한 것이 아닙니다: **여러분이 직접 작성한 클래스**도 확장할 수 있습니다. 이는 클래스가 여러분이 제어하지 않는 패키지에 있을 때, 또는 클래스를 작게 유지하면서 필요한 코드 곁에 선택적인 헬퍼를 추가하고 싶을 때 유용합니다.

```dart
class Circle {
  final double radius;
  Circle(this.radius);
}

extension CircleMath on Circle {
  double get diameter => radius * 2;
}

void main() {
  print(Circle(3).diameter); // 6.0
}
```

확장은 클래스 바깥에 작성한 코드와 똑같이 클래스의 public 필드와 메서드를 볼 수 있습니다.

# --instructions--

항목을 정렬해서 `width`와 `height`를 가진 클래스 `Rectangle`, 그다음에 게터 `area`를 가진 `Rectangle`에 대한 확장 `RectangleMath`, 그다음에 `4` x `2.5` 직사각형의 넓이를 출력하는 `main`을 정의하세요. 클래스를 먼저 정의하고, 그다음에 확장, 그다음에 `main`을 정의하세요. 클래스 안에서는 생성자 앞에 두 필드를 모두 선언하세요. 프로그램은 다음을 출력해야 합니다:

```
10.0
```

# --answers--

```dart
class Rectangle {
```

```dart
  final double width;
```

```dart
  final double height;
```

```dart
  Rectangle(this.width, this.height);
```

```dart
}
```

```dart
extension RectangleMath on Rectangle {
```

```dart
  double get area => width * height;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Rectangle(4, 2.5).area);
```

```dart
}
```

# --solutions--

```dart
class Rectangle {
  final double width;
  final double height;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

```dart
class Rectangle {
  final double height;
  final double width;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

# --output--

10.0
