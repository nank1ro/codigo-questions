---
language: dart
exerciseType: 3
---

# --description--

확장이 타입이 **이미 가지고 있는** 멤버를 선언하면 어떻게 될까요? 타입 자체의 멤버가 항상 이깁니다: 확장 멤버는 타입 자체에 그 이름의 멤버가 없을 때만 고려됩니다. 확장 멤버는 조용히 무시되며, 오류도 오버라이드도 없습니다.

```dart
extension Shorter on String {
  int get length => 0;
}

void main() {
  print('four'.length); // 4, String's own length is used
}
```

따라서 확장은 멤버를 추가하고 빈틈을 메울 수 있지만, 기존 멤버가 동작하는 방식을 **바꿀** 수는 없습니다.

# --instructions--

이 프로그램은 무엇을 출력합니까?

```dart
extension Loud on String {
  String toUpperCase() => 'LOUD';
}

void main() {
  print('hi'.toUpperCase());
}
```

# --answers--

- `HI`
- `LOUD`
- 아무것도 출력하지 않습니다. `toUpperCase`가 이미 정의되어 있어 컴파일되지 않습니다
- 아무것도 출력하지 않습니다. 런타임에 오류가 발생합니다

# --solutions--

- `HI`
