---
language: dart
exerciseType: 1
---

# --description--

확장은 **게터(getter)**도 선언할 수 있으며, 게터는 괄호 없이 프로퍼티처럼 읽습니다. 확장 내부에서는 타입 자체의 멤버를 바로 호출할 수 있습니다: 클래스 내부와 마찬가지로 `this.`는 생략할 수 있습니다.

```dart
extension Sizes on String {
  bool get isLong => length > 10;      // same as this.length
  String get firstChar => this[0];
}

void main() {
  print('Dart'.isLong);           // false
  print('extension'.firstChar);   // e
}
```

멤버가 값을 **읽기만** 하고 매개변수를 받지 않는다면 게터를, 작업을 수행하거나 인수가 필요하다면 메서드를 선택하세요.

# --instructions--

문자열이 비어 있거나 공백만 포함할 때 `true`가 되는 `bool` 타입의 게터 `isBlank`를 가진, `String`에 대한 `Blank`라는 확장을 작성하세요. `trim()`과 `isEmpty`를 사용하세요.

예:
```dart
print('   '.isBlank);  // true
print(' hi '.isBlank); // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`''.isBlank`와 `'   '.isBlank`는 `true`이어야 합니다

```dart
  test('test1', () {
    expect(''.isBlank, true, reason: '--err-t1--');
    expect('   '.isBlank, true, reason: '--err-t1--');
  });
```

`' hi '.isBlank`는 `false`이어야 합니다

```dart
  test('test2', () {
    expect(' hi '.isBlank, false, reason: '--err-t2--');
    expect('x'.isBlank, false, reason: '--err-t2--');
  });
```

`isBlank`는 `bool`이어야 합니다

```dart
  test('test3', () {
    expect('  '.isBlank, isA<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Blank on String {
  bool get isBlank => trim().isEmpty;
}
```
