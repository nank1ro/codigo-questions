---
language: dart
exerciseType: 2
---

# --description--

`String`에 대한 확장은 `String?`에 호출할 수 없습니다: 값이 `null`일 수 있고 컴파일러가 호출을 거부하기 때문입니다. 대신 확장을 **널 가능** 타입에 선언하면 `String?`에 메서드를 바로 호출할 수 있고, 그 안에서 `this`는 `String?` 타입이므로 `null`인 경우를 직접 처리해야 합니다. 예를 들어 `??`로 처리할 수 있습니다:

```dart
extension Defaults on int? {
  int orZero() => this ?? 0;
}

void main() {
  int? count = null;
  print(count.orZero()); // 0
  print(5.orZero());     // 5
}
```

널 불가능 `int`는 `int?`가 기대되는 곳에 전달될 수 있으므로, 이 확장은 두 경우 모두 동작합니다.

# --instructions--

`orEmpty`를 널 가능 문자열에 호출할 수 있고, 문자열 자체를 반환하거나 `null`일 때 빈 문자열을 반환하도록 확장을 완성하세요. 프로그램은 `[]`와 `[Ada]`를 출력해야 합니다.

# --seed--

```dart
extension Defaults on [/] {
  String orEmpty() => this [/] '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --answers--

- String?
- ??
- String
- +
- ?.
- !

# --solutions--

```dart
extension Defaults on String? {
  String orEmpty() => this ?? '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --output--

[]
[Ada]
