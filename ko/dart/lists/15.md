---
language: dart
exerciseType: 1
---

# --instructions--

리스트 `[3, 7, 2, 9, 4]`이 주어졌을 때, `for-in` 반복문을 사용하여 모든 요소를 합산하세요. 합계를 반환하세요.

# --before-seed--

```dart
int computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

합계는 25

```dart
  test('sum is 25', () {
    expect(computeValue(), 25, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var numbers = [3, 7, 2, 9, 4];
  int result = 0;
  for (var n in numbers) {
    result += n;
  }
```
