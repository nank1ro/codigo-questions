---
language: dart
exerciseType: 1
---

# --instructions--

리스트 `['apple', 'banana', 'cherry', 'date']`이 주어졌을 때, `.indexOf()`를 사용하여 `'cherry'`의 인덱스를 찾으세요. 인덱스를 반환하세요.

# --before-seed--

```dart
int computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

'cherry'의 인덱스는 2

```dart
  test('indexOf cherry is 2', () {
    expect(computeValue(), 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var fruits = ['apple', 'banana', 'cherry', 'date'];
  int result = fruits.indexOf('cherry');
```
