---
language: dart
exerciseType: 1
---

# --instructions--

리스트 `[5, 10, 15, 20]`이 주어졌을 때, `15`가 포함되어 있으면 `true`, 그렇지 않으면 `false`를 반환하세요. `.contains()`를 사용하세요.

# --before-seed--

```dart
bool computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

15에 대해 true를 반환한다

```dart
  test('contains 15', () {
    expect(computeValue(), true, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var numbers = [5, 10, 15, 20];
  bool result = numbers.contains(15);
```
