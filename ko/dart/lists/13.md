---
language: dart
exerciseType: 1
---

# --instructions--

리스트 `[100, 200, 300, 400, 500]`이 주어졌을 때, 인덱스 `0`이 `.first` 요소이고 인덱스 `1`이 `.last` 요소인 `List<int>`를 반환하세요.

# --before-seed--

```dart
List<int> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

첫 번째 요소는 100

```dart
  test('first is 100', () {
    expect(computeValue()[0], 100, reason: '--err-t1--');
  });
```

마지막 요소는 500

```dart
  test('last is 500', () {
    expect(computeValue()[1], 500, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var nums = [100, 200, 300, 400, 500];
  List<int> result = [nums.first, nums.last];
```
