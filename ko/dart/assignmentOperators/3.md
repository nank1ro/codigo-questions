---
language: dart
exerciseType: 2
---

# --description--

**빼기 대입 연산자** `-=`는 변수에서 값을 빼고 결과를 그 변수에 저장합니다.

```dart
int lives = 3;
lives -= 1; // lives는 이제 2
```

이는 `lives = lives - 1`의 약자입니다.

# --instructions--

코드를 완성하여 `-=`를 사용하여 변수 `stock`에서 `5`를 빼세요.

# --seed--

```dart
void main() {
  int stock = 20;
  stock [/] 5;
  print(stock);
}
```

# --answers--

- -=
- =
- +=
- -

# --solutions--

```dart
void main() {
  int stock = 20;
  stock -= 5;
  print(stock);
}
```

# --output--

15
