---
language: dart
exerciseType: 3
---

# --instructions--

`x`가 현재 `10`일 때, `x += 5`는 무엇을 합니까?

# --answers--

- `x`를 `15`로 설정합니다
- `x`를 `5`로 설정합니다
- `x`를 `10`으로 설정합니다
- `x`를 `50`으로 설정합니다

# --solutions--

- `x`를 `15`로 설정합니다
