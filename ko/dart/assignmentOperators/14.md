---
language: dart
exerciseType: 1
---

# --instructions--

값이 `2`인 `int` 변수 `n`을 선언합니다. 그런 다음 `main`에서 다음 대입 연산자를 순서대로 적용합니다:
- `n *= 10` (10으로 곱하기)
- `n += 5` (5 추가)
- `n -= 3` (3 빼기)

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int n = 2;
```

# --after-seed--

```dart
  return n;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`n`은 `22`여야 합니다

```dart
  test('test1', () {
    expect(result(), 22, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 2;
n *= 10;
n += 5;
n -= 3;
```
