---
language: dart
exerciseType: 1
---

# --instructions--

값이 `29`인 최상위 수준의 `int` 변수 `remainder`를 선언합니다. 그런 다음 `%=`를 사용하여 `6`으로 나눈 나머지를 할당합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`remainder`는 `5`여야 합니다

```dart
  test('test1', () {
    expect(remainder, 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 29;

void main() {
  remainder %= 6;
  print(remainder);
}
```
