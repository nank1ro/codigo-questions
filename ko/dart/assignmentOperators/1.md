---
language: dart
exerciseType: 2
---

# --description--

**대입 연산자** `=`는 값을 변수에 저장합니다.

```dart
int x = 5;
String name = 'Alice';
```

일단 할당되면 변수는 다시 할당될 때까지 그 값을 유지합니다.

# --instructions--

코드를 완성하여 변수 `answer`에 값 `42`를 할당하세요.

# --seed--

```dart
void main() {
  int answer [/] 42;
  print(answer);
}
```

# --answers--

- =
- ==
- +=
- =>

# --solutions--

```dart
void main() {
  int answer = 42;
  print(answer);
}
```

# --output--

42
