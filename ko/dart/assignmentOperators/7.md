---
language: dart
exerciseType: 3
---

# --instructions--

`x`가 `9`일 때, `x %= 4`는 무엇을 합니까?

# --answers--

- `x`를 `9 / 4`의 나머지로 설정합니다. 이는 `1`입니다
- `x`를 `9 / 4`의 결과로 설정합니다. 이는 `2.25`입니다
- `x`를 `4`로 설정합니다
- `x`를 `36`으로 설정합니다

# --solutions--

- `x`를 `9 / 4`의 나머지로 설정합니다. 이는 `1`입니다
