---
language: dart
exerciseType: 1
---

# --instructions--

값이 `100`인 `int` 변수 `fuel`을 선언합니다. 그런 다음 `-=`를 사용하여 `35`를 뺍니다.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int fuel = 100;
```

# --after-seed--

```dart
  return fuel;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`fuel`은 `65`여야 합니다

```dart
  test('test1', () {
    expect(result(), 65, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int fuel = 100;
fuel -= 35;
```
