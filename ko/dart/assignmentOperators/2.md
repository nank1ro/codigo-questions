---
language: dart
exerciseType: 2
---

# --description--

**더하기 대입 연산자** `+=`는 변수에 값을 더하고 결과를 그 변수에 저장합니다.

```dart
int score = 10;
score += 5; // score는 이제 15
```

이는 `score = score + 5`의 약자입니다.

# --instructions--

코드를 완성하여 `+=`를 사용하여 변수 `total`에 `8`을 추가하세요.

# --seed--

```dart
void main() {
  int total = 12;
  total [/] 8;
  print(total);
}
```

# --answers--

- +=
- =
- -=
- +

# --solutions--

```dart
void main() {
  int total = 12;
  total += 8;
  print(total);
}
```

# --output--

20
