---
language: dart
exerciseType: 1
---

# --instructions--

값이 `6`인 최상위 수준의 `int` 변수 `width`를 선언합니다. 그런 다음 `*=`를 사용하여 `4`를 곱합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`width`는 `24`여야 합니다

```dart
  test('test1', () {
    expect(width, 24, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int width = 6;

void main() {
  width *= 4;
  print(width);
}
```
