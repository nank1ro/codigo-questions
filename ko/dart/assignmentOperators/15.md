---
language: dart
exerciseType: 1
---

# --instructions--

상점이 상품에 20% 할인을 적용하고 있습니다. 값이 `50.0`인 최상위 수준의 `double` 변수 `price`를 선언합니다. 그런 다음 `*=`를 사용하여 `price`에 `0.8`을 곱하여 할인을 적용합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`price`는 `40.0`이어야 합니다

```dart
  test('test1', () {
    expect(price, closeTo(40.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double price = 50.0;

void main() {
  price *= 0.8;
  print(price);
}
```
