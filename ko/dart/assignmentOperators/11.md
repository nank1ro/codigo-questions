---
language: dart
exerciseType: 1
---

# --instructions--

값이 `20.0`인 최상위 수준의 `double` 변수 `value`를 선언합니다. 그런 다음 `/=`를 사용하여 `4`로 나눕니다. Dart에서 `/=`는 항상 `double`을 생성함을 참고합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value`는 `5.0`이어야 합니다

```dart
  test('test1', () {
    expect(value, closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;

void main() {
  value /= 4;
  print(value);
}
```
