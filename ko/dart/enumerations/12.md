---
language: dart
exerciseType: 1
---

# --description--

확장 enum은 **메서드**와 **게터**도 선언할 수 있습니다. 그 안에서는 `this`가 현재 값이므로, 그 값의 `name`, `index`, 필드를 바로 사용할 수 있습니다:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);

  bool get hasMoons => moons > 0;

  String describe() => '$name has $moons moon(s)';
}

print(Planet.earth.hasMoons);   // true
print(Planet.mars.describe()); // mars has 2 moon(s)
```

필드가 없는 enum도 메서드를 선언할 수 있습니다. 이 경우 값 목록은 `;`으로 끝나고 그 뒤에 멤버가 이어집니다.

# --instructions--

값 `north`, `east`, `south`, `west`를 이 순서로 갖는 enum `Direction`을 선언하세요. 반대쪽 값을 반환하는 게터 `opposite`(`north`<->`south`, `east`<->`west`)와, 다음 값을 반환하는 메서드 `turnRight()`(`west`에서 `north`로 순환)를 추가하세요.

힌트: `index`, `values`, `%` 연산자를 조합하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Direction.north.opposite`는 `Direction.south`여야 합니다

```dart
  test('test1', () {
    expect(Direction.north.opposite, Direction.south, reason: '--err-t1--');
  });
```

`Direction.west.opposite`는 `Direction.east`여야 합니다

```dart
  test('test2', () {
    expect(Direction.west.opposite, Direction.east, reason: '--err-t2--');
  });
```

`Direction.north.turnRight()`는 `Direction.east`여야 합니다

```dart
  test('test3', () {
    expect(Direction.north.turnRight(), Direction.east, reason: '--err-t3--');
  });
```

`Direction.west.turnRight()`는 `Direction.north`여야 합니다

```dart
  test('test4', () {
    expect(Direction.west.turnRight(), Direction.north, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Direction {
  north,
  east,
  south,
  west;

  Direction get opposite => Direction.values[(index + 2) % 4];

  Direction turnRight() => Direction.values[(index + 1) % 4];
}
```
