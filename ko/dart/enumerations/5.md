---
language: dart
exerciseType: 4
---

# --description--

`values`를 순회하는 것은 열거형의 모든 값을 처리하는 일반적인 방법입니다. 반복문 안에서 현재 값은 다른 객체와 똑같이 동작하므로, `index`와 `name`을 읽어 문자열 보간에 바로 사용할 수 있습니다:

```dart
enum Direction { north, south, east, west }

for (var direction in Direction.values) {
  print('${direction.index}: ${direction.name}');
}
```

# --instructions--

항목을 정렬하여 enum `Level`을 선언하고, 각 값에 대해 인덱스와 이름을 출력하세요. enum은 `main`보다 앞에 선언하세요. 프로그램은 다음을 출력해야 합니다:

```
0: low
1: medium
2: high
```

# --answers--

```dart
enum Level { low, medium, high }
```

```dart
void main() {
```

```dart
  for (var level in Level.values) {
```

```dart
    print('${level.index}: ${level.name}');
```

```dart
  }
}
```

# --solutions--

```dart
enum Level { low, medium, high }
void main() {
  for (var level in Level.values) {
    print('${level.index}: ${level.name}');
  }
}
```

# --output--

0: low
1: medium
2: high
