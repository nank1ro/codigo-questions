---
language: dart
exerciseType: 1
---

# --description--

enum은 `implements` 키워드로 **인터페이스를 구현**할 수 있습니다. 그러면 enum은 인터페이스가 선언한 모든 멤버를 제공하기로 약속하며, 그 값들은 해당 인터페이스 타입이 필요한 곳이라면 어디서든 사용할 수 있습니다:

```dart
abstract class Describable {
  String describe();
}

enum Animal implements Describable {
  dog,
  cat;

  @override
  String describe() => 'I am a $name';
}

Describable pet = Animal.cat;
print(pet.describe()); // I am a cat
```

인터페이스에 선언된 게터는 게터로 구현하거나, 같은 이름의 `final` 필드로 구현할 수 있습니다.

# --instructions--

`Priced`를 구현하는 enum `Ticket`을 선언하세요. `final int` 필드 `price`와 값 `child(5)`, `adult(12)`, `senior(8)`을 이 순서로 가지도록 하세요. 그런 다음, 모든 항목의 가격 합계를 반환하는 함수 `total(List<Priced> items)`를 작성하세요.

# --seed--

```dart
abstract class Priced {
  int get price;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Ticket.adult.price`는 `12`여야 합니다

```dart
  test('test1', () {
    expect(Ticket.adult.price, 12, reason: '--err-t1--');
  });
```

`Ticket`은 `Priced`로 사용할 수 있어야 합니다

```dart
  test('test2', () {
    Priced item = Ticket.senior;
    expect(item.price, 8, reason: '--err-t2--');
  });
```

`total([Ticket.child, Ticket.adult, Ticket.senior])`는 `25`여야 합니다

```dart
  test('test3', () {
    expect(total([Ticket.child, Ticket.adult, Ticket.senior]), 25, reason: '--err-t3--');
  });
```

`total([])`는 `0`이어야 합니다

```dart
  test('test4', () {
    expect(total([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
abstract class Priced {
  int get price;
}

enum Ticket implements Priced {
  child(5),
  adult(12),
  senior(8);

  @override
  final int price;

  const Ticket(this.price);
}

int total(List<Priced> items) {
  int sum = 0;
  for (var item in items) {
    sum += item.price;
  }
  return sum;
}
```
