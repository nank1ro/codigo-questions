---
language: dart
exerciseType: 2
---

# --description--

모든 enum은 선언 순서대로 모든 값을 담고 있는 `values`라는 상수 리스트도 가지고 있습니다. 일반 리스트처럼 인덱스로 접근하거나, `length`를 읽거나, `for-in`으로 반복할 수 있습니다:

```dart
enum Direction { north, south, east, west }

print(Direction.values.length); // 4
print(Direction.values[1]);     // Direction.south

for (var direction in Direction.values) {
  print(direction.name);
}
```

# --instructions--

코드를 완성하여 `Planet.earth`의 이름, 인덱스, 행성의 총 개수를 출력하세요.

# --seed--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.[/]);
  print(Planet.earth.[/]);
  print(Planet.[/].length);
}
```

# --answers--

- name
- index
- values
- ordinal
- position
- count

# --solutions--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.name);
  print(Planet.earth.index);
  print(Planet.values.length);
}
```

# --output--

earth
2
3
