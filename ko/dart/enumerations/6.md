---
language: dart
exerciseType: 3
---

# --description--

각 enum 값은 정확히 하나만 존재하므로, 같은 값을 가리키는 두 참조는 항상 같습니다. `==`와 `!=`로 비교하세요:

```dart
enum Direction { north, south, east, west }

var heading = Direction.north;
print(heading == Direction.north); // true
print(heading != Direction.south); // true
print(Direction.values[0] == heading); // true
```

# --instructions--

`enum Size { small, medium, large }`가 주어졌을 때, `print(Size.medium == Size.values[1]);`은 무엇을 출력합니까?

# --answers--

- true
- false
- 1
- 컴파일 오류가 발생한다

# --solutions--

- true
