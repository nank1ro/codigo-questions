---
language: dart
exerciseType: 1
---

# --description--

`values`는 리스트이므로 이미 알고 있는 리스트 메서드와 결합해서 사용할 수 있습니다. 예를 들어 `.map()`과 `.name`을 함께 사용하면 값들을 문자열 리스트로 변환할 수 있습니다:

```dart
enum Direction { north, south, east, west }

List<String> names = Direction.values.map((d) => d.name).toList();
print(names); // [north, south, east, west]
```

# --instructions--

값 `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`를 이 순서로 갖는 enum `Weekday`를 선언하세요. 그런 다음, 선언 순서대로 모든 요일의 이름을 담은 `List<String>`을 반환하는 함수 `dayNames()`를 작성하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Weekday.values.length`는 `7`이어야 합니다

```dart
  test('test1', () {
    expect(Weekday.values.length, 7, reason: '--err-t1--');
  });
```

`Weekday.values[2]`는 `Weekday.wednesday`여야 합니다

```dart
  test('test2', () {
    expect(Weekday.values[2], Weekday.wednesday, reason: '--err-t2--');
  });
```

`dayNames()`는 7개의 이름을 순서대로 반환해야 합니다

```dart
  test('test3', () {
    expect(dayNames(), ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Weekday { monday, tuesday, wednesday, thursday, friday, saturday, sunday }

List<String> dayNames() {
  return Weekday.values.map((day) => day.name).toList();
}
```
