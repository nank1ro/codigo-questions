---
language: dart
exerciseType: 1
---

# --description--

Dart 2.17부터 enum은 클래스처럼 **필드**와 **생성자**를 선언할 수 있습니다. 이를 *확장 enum*이라고 합니다. 각 값은 자신만의 인자를 생성자에 전달합니다:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);
}

print(Planet.mars.moons); // 2
```

세 가지 규칙에 주목하세요: 값 목록은 **세미콜론** `;`으로 끝나야 하고, 필드는 `final`이어야 하며, 생성자는 `const`여야 합니다.

# --instructions--

`final int` 필드 `cents`와 값 `penny(1)`, `nickel(5)`, `dime(10)`, `quarter(25)`를 이 순서로 갖는 enum `Coin`을 선언하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Coin.penny.cents`는 `1`, `Coin.nickel.cents`는 `5`여야 합니다

```dart
  test('test1', () {
    expect(Coin.penny.cents, 1, reason: '--err-t1--');
    expect(Coin.nickel.cents, 5, reason: '--err-t2--');
  });
```

`Coin.dime.cents`는 `10`이어야 합니다

```dart
  test('test2', () {
    expect(Coin.dime.cents, 10, reason: '--err-t3--');
  });
```

`Coin.quarter.cents`는 `25`여야 합니다

```dart
  test('test3', () {
    expect(Coin.quarter.cents, 25, reason: '--err-t4--');
  });
```

`Coin.values`는 4개의 값을 가지고 `penny`가 첫 번째여야 합니다

```dart
  test('test4', () {
    expect(Coin.values.length, 4, reason: '--err-t5--');
    expect(Coin.values[0], Coin.penny, reason: '--err-t6--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Coin {
  penny(1),
  nickel(5),
  dime(10),
  quarter(25);

  final int cents;

  const Coin(this.cents);
}
```
