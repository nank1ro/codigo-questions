---
language: dart
exerciseType: 1
---

# --description--

enum 값은 **맵의 키**로 아주 훌륭합니다: 유일하고, 비교하기 쉬우며, 컴파일러가 실제 값만 사용되는지 확인해 줍니다. enum을 키 타입으로 하는 맵을 선언하고 `[]`로 값을 조회하세요:

```dart
enum Direction { north, south, east, west }

Map<Direction, String> arrows = {
  Direction.north: '^',
  Direction.south: 'v',
  Direction.east: '>',
  Direction.west: '<',
};

print(arrows[Direction.east]); // >
```

다른 맵과 마찬가지로, 키가 없으면 조회 결과는 `null`이므로 `??`로 대체 값을 제공하세요.

# --instructions--

`enum Size { small, medium, large }`가 주어졌을 때, `small`은 `2`, `medium`은 `3`, `large`는 `4`의 가격을 갖는 최상위 `Map<Size, int>` `prices`를 선언하세요. 그런 다음, `order`에 있는 모든 사이즈의 가격 합계를 반환하는 함수 `totalPrice(List<Size> order)`를 작성하세요.

# --seed--

```dart
enum Size { small, medium, large }
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`prices[Size.medium]`는 `3`이어야 합니다

```dart
  test('test1', () {
    expect(prices[Size.medium], 3, reason: '--err-t1--');
  });
```

`totalPrice([Size.small, Size.large])`는 `6`이어야 합니다

```dart
  test('test2', () {
    expect(totalPrice([Size.small, Size.large]), 6, reason: '--err-t2--');
  });
```

`totalPrice([Size.medium, Size.medium, Size.small])`는 `8`이어야 합니다

```dart
  test('test3', () {
    expect(totalPrice([Size.medium, Size.medium, Size.small]), 8, reason: '--err-t3--');
  });
```

`totalPrice([])`는 `0`이어야 합니다

```dart
  test('test4', () {
    expect(totalPrice([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Size { small, medium, large }

Map<Size, int> prices = {
  Size.small: 2,
  Size.medium: 3,
  Size.large: 4,
};

int totalPrice(List<Size> order) {
  int total = 0;
  for (var size in order) {
    total += prices[size] ?? 0;
  }
  return total;
}
```
