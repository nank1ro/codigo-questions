---
language: dart
exerciseType: 2
---

# --description--

**클래스**는 객체를 만들기 위한 청사진입니다. 관련 데이터(필드)와 동작(메서드)을 함께 묶습니다.

Dart에서는 `class` 키워드 뒤에 클래스 이름(관례적으로 PascalCase)을 써서 클래스를 선언합니다:

```dart
class Car {
  String brand = 'Toyota';
}
```

클래스의 **인스턴스**(객체)는 다음과 같이 생성합니다:

```dart
final car = Car();
print(car.brand); // Toyota
```

# --instructions--

`String` 필드 `name`을 `'Alice'`로 초기화한 `Person` 클래스를 선언하고, 인스턴스를 생성하여 `name`을 출력하는 코드를 완성하세요.

# --seed--

```dart
[/] Person {
  String [/] = 'Alice';
}

void main() {
  final p = [/]();
  print(p.[/]);
}
```

# --answers--

- class
- name
- Person
- brand
- age
- void
- p

# --solutions--

```dart
class Person {
  String name = 'Alice';
}

void main() {
  final p = Person();
  print(p.name);
}
```

# --output--

Alice
