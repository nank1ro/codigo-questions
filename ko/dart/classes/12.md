---
language: dart
exerciseType: 1
---

# --description--

**메서드 오버라이딩**을 사용하면 서브클래스가 슈퍼클래스에 이미 존재하는 메서드를 자신만의 구현으로 제공할 수 있습니다. `@override` 애너테이션을 사용합니다:

```dart
class Shape {
  double area() => 0;
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() => 3.14159 * radius * radius;
}
```

`@override` 애너테이션은 슈퍼클래스의 메서드를 의도적으로 대체하고 있다는 것을 Dart(그리고 다른 개발자)에게 알려줍니다.

# --instructions--

`String` `'...'`을 반환하는 메서드 `sound()`를 가진 클래스 `Animal`을 만드세요. 그다음 `sound()`를 오버라이드하여 `'Woof'`를 반환하는 서브클래스 `Dog`를 만드세요. `sound()`를 오버라이드하여 `'Meow'`를 반환하는 서브클래스 `Cat`도 추가하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Animal.sound()`가 `'...'`을 반환한다

```dart
    test('animal sound', () {
      expect(Animal().sound(), '...', reason: '--err-t1--');
    });
```

`Dog.sound()`가 `'Woof'`를 반환한다

```dart
    test('dog sound', () {
      Animal dog = Dog();
      expect(dog.sound(), 'Woof', reason: '--err-t2--');
    });
```

`Cat.sound()`가 `'Meow'`를 반환한다

```dart
    test('cat sound', () {
      Animal cat = Cat();
      expect(cat.sound(), 'Meow', reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String sound() => '...';
}

class Dog extends Animal {
  @override
  String sound() => 'Woof';
}

class Cat extends Animal {
  @override
  String sound() => 'Meow';
}
```
