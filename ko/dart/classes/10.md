---
language: dart
exerciseType: 1
---

# --instructions--

`0`으로 초기화된 `int` 필드 `count`를 가진 `Counter` 클래스를 만드세요. `count`를 `1` 증가시키는 메서드 `increment()`와 `count`를 다시 `0`으로 설정하는 메서드 `reset()`을 추가하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`increment()`를 세 번 호출하면 `count`가 `3`이 되어야 함

```dart
  test('test1', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.increment();
    expect(c.count, 3, reason: '--err-t1--');
  });
```

`reset()`을 호출하면 `count`가 다시 `0`이 되어야 함

```dart
  test('test2', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.reset();
    expect(c.count, 0, reason: '--err-t2--');
  });
```

초기 `count`는 `0`이어야 함

```dart
  test('test3', () {
    final c = Counter();
    expect(c.count, 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Counter {
  int count = 0;

  void increment() {
    count++;
  }

  void reset() {
    count = 0;
  }
}
```
