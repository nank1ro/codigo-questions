---
language: dart
exerciseType: 1
---

# --instructions--

두 개의 `int` 프로퍼티 `x`와 `y`를 가진 `Point` 클래스를 만드세요. 두 프로퍼티를 모두 설정하는 생성자를 추가하세요. 또한 `x`와 `y` 모두 `0`으로 설정하는 명명된 생성자 `Point.origin()`도 추가하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Point(3, 5)`는 `x`를 `3`으로, `y`를 `5`로 설정해야 함

```dart
  test('test1', () {
    final p = Point(3, 5);
    expect(p.x, 3, reason: '--err-t1--');
    expect(p.y, 5, reason: '--err-t1--');
  });
```

`Point.origin()`은 `x`와 `y` 모두 `0`으로 설정해야 함

```dart
  test('test2', () {
    final p = Point.origin();
    expect(p.x, 0, reason: '--err-t2--');
    expect(p.y, 0, reason: '--err-t2--');
  });
```

`Point(-1, 4)`는 `x`를 `-1`로 설정해야 함

```dart
  test('test3', () {
    final p = Point(-1, 4);
    expect(p.x, -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Point {
  int x;
  int y;
  Point(this.x, this.y);
  Point.origin() : x = 0, y = 0;
}
```
