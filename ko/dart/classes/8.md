---
language: dart
exerciseType: 1
---

# --instructions--

두 개의 `String` 프로퍼티 `brand`와 `model`을 가진 `Car` 클래스를 만드세요. 축약 문법을 사용하여 두 프로퍼티를 모두 설정하는 생성자를 추가하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Car('Toyota', 'Corolla')`를 생성하면 `brand`가 `'Toyota'`로 설정되어야 함

```dart
  test('test1', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.brand, 'Toyota', reason: '--err-t1--');
  });
```

`Car('Toyota', 'Corolla')`를 생성하면 `model`이 `'Corolla'`로 설정되어야 함

```dart
  test('test2', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.model, 'Corolla', reason: '--err-t2--');
  });
```

`Car('Honda', 'Civic')`를 생성하면 `brand`가 `'Honda'`로 설정되어야 함

```dart
  test('test3', () {
    final car = Car('Honda', 'Civic');
    expect(car.brand, 'Honda', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Car {
  String brand;
  String model;
  Car(this.brand, this.model);
}
```
