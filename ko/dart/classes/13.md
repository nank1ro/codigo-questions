---
language: dart
exerciseType: 2
---

# --description--

**추상 클래스**는 직접 인스턴스화할 수 없는 클래스입니다. 서브클래스가 반드시 구현해야 하는 메서드, 즉 계약을 정의하는 기반으로 사용됩니다. 본문을 생략하여 추상 메서드를 표시합니다:

```dart
abstract class Shape {
  double area(); // abstract method — no body
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() => 3.14159 * radius * radius;
}
```

`Shape()`를 직접 인스턴스화하려고 하면 오류가 발생합니다.

# --instructions--

추상 메서드 `fuelType`을 가진 `Vehicle`의 추상 클래스 정의를 완성하세요.

# --seed--

```dart
[/] class Vehicle {
  String fuelType();
}
```

# --answers--

- abstract
- Abstract
- interface
- base
- virtual

# --solutions--

```dart
abstract class Vehicle {
  String fuelType();
}
```
