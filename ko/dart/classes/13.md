---
language: dart
exerciseType: 1
---

# --instructions--

`String` 필드 `name`과 생성자를 가진 부모 클래스 `Animal`을 만드세요. 그런 다음 `Animal`을 상속하는 자식 클래스 `Dog`를 만드세요. `Dog`는 추가적인 `String` 필드 `breed`와 `super`를 사용하여 `name`을 `Animal`에 전달하는 생성자를 가져야 합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Dog('Rex', 'Labrador').name`은 `'Rex'`와 같아야 함

```dart
  test('test1', () {
    final dog = Dog('Rex', 'Labrador');
    expect(dog.name, 'Rex', reason: '--err-t1--');
  });
```

`Dog('Rex', 'Labrador').breed`는 `'Labrador'`와 같아야 함

```dart
  test('test2', () {
    final dog = Dog('Rex', 'Labrador');
    expect(dog.breed, 'Labrador', reason: '--err-t2--');
  });
```

`Dog`는 `Animal`의 인스턴스여야 함

```dart
  test('test3', () {
    final dog = Dog('Buddy', 'Poodle');
    expect(dog is Animal, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Dog extends Animal {
  String breed;
  Dog(String name, this.breed) : super(name);
}
```
