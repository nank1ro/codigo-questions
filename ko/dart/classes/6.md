---
language: dart
exerciseType: 1
---

# --description--

`this`는 클래스의 **현재 인스턴스**, 즉 메서드가 호출된 객체를 가리킵니다. 메서드 안에서 이를 사용해 객체 자신의 필드에 접근할 수 있습니다:

```dart
class Circle {
  double radius;

  Circle(this.radius);

  double diameter() {
    return this.radius * 2;
  }
}
```

여기서 `this.radius`는 `diameter()`가 호출된 circle의 `radius` 필드를 읽습니다. 같은 이름의 다른 변수가 없으면 `this.`는 생략할 수 있습니다: `radius * 2`도 동일하게 동작합니다.

# --instructions--

생성자 `Rectangle(this.width, this.height)`로 설정되는, `double` 타입의 필드 `width`와 `height`를 가진 클래스 `Rectangle`을 만드세요. `this.width * this.height`를 반환하는 메서드 `area()`를 추가하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Rectangle`의 넓이가 올바르다

```dart
    test('area', () {
      var r = Rectangle(4.0, 5.0);
      expect(r.area(), 20.0, reason: '--err-t1--');
    });
```

`width`와 `height`가 올바르게 저장된다

```dart
    test('fields', () {
      var r = Rectangle(3.0, 7.0);
      expect(r.width, 3.0, reason: '--err-t2--');
      expect(r.height, 7.0, reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Rectangle {
  double width;
  double height;

  Rectangle(this.width, this.height);

  double area() {
    return this.width * this.height;
  }
}
```
