---
language: dart
exerciseType: 2
---

# --description--

**메서드**는 클래스 내부에 정의된 함수입니다. 메서드는 객체의 동작을 나타냅니다.

```dart
class Circle {
  double radius;
  Circle(this.radius);

  double area() {
    return 3.14 * radius * radius;
  }
}

void main() {
  final c = Circle(5);
  print(c.area()); // 78.5
}
```

메서드는 객체 자신의 필드에 이름으로 직접(또는 `this.fieldName`으로) 접근할 수 있습니다.

# --instructions--

`int` 필드 `width`와 `height`, 생성자, 그리고 `2 * (width + height)`를 반환하는 메서드 `perimeter()`를 갖는 `Rectangle` 클래스를 완성하세요. 그런 다음 메서드를 호출하고 결과를 출력하세요.

# --seed--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.[/]);

  int [/]() {
    return 2 * ([/] + height);
  }
}

void main() {
  final r = Rectangle(4, 6);
  print(r.[/]());
}
```

# --answers--

- height
- perimeter
- width
- area
- this
- 3
- r

# --solutions--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.height);

  int perimeter() {
    return 2 * (width + height);
  }
}

void main() {
  final r = Rectangle(4, 6);
  print(r.perimeter());
}
```

# --output--

20
