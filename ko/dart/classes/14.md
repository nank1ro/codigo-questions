---
language: dart
exerciseType: 2
---

# --description--

**정적 멤버(static member)**는 특정 인스턴스가 아니라 클래스 자체에 속합니다. `static` 키워드로 선언하며, 클래스에 직접 접근합니다:

```dart
class MathHelper {
  static const double pi = 3.14159;

  static double circleArea(double r) => pi * r * r;
}

void main() {
  // access without creating an object:
  print(MathHelper.pi);
  print(MathHelper.circleArea(5));
}
```

정적 필드와 메서드는 모든 인스턴스가 공유합니다.

# --instructions--

`a + b`를 반환하는 `Calculator` 클래스의 정적 메서드 `add`를 완성하세요.

# --seed--

```dart
class Calculator {
  [/] int add(int a, int b) => a [/] b;
}
```

# --answers--

- static
- +
- -
- void
- final
- *

# --solutions--

```dart
class Calculator {
  static int add(int a, int b) => a + b;
}
```
