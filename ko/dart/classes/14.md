---
language: dart
exerciseType: 1
---

# --instructions--

추상 메서드 `double area()`를 가진 `abstract` 클래스 `Shape`를 만드세요. 그런 다음 `Shape`를 상속하는 `Circle` 클래스를 만드세요. `Circle`은 `double` 필드 `radius`, 생성자를 가지고, `area()`를 오버라이드하여 `3.14 * radius * radius`를 반환해야 합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Circle(5).area()`는 `78.5`와 같아야 함

```dart
  test('test1', () {
    final c = Circle(5);
    expect(c.area(), 78.5, reason: '--err-t1--');
  });
```

`Circle(10).area()`는 `314.0`과 같아야 함

```dart
  test('test2', () {
    final c = Circle(10);
    expect(c.area(), 314.0, reason: '--err-t2--');
  });
```

`Circle`은 `Shape`의 인스턴스여야 함

```dart
  test('test3', () {
    final c = Circle(3);
    expect(c is Shape, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
abstract class Shape {
  double area();
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() {
    return 3.14 * radius * radius;
  }
}
```
