---
language: dart
exerciseType: 4
---

# --description--

`StreamController`는 흔히 같은 자리에서 만들어지고 소비됩니다. `listen`으로 `controller.stream`을 구독한 다음, `add`로 이벤트를 추가하고 `close`로 컨트롤러를 닫습니다. `listen`은 기다리지 않으므로 이벤트는 현재 코드가 끝난 뒤에 전달되지만, 언제나 추가된 순서대로 전달됩니다:

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>();
  controller.stream.listen((n) => print('got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
// got 1
// got 2
```

# --instructions--

항목을 정렬해서 `dart:async`를 import하고, `StreamController<String>`을 만들고, 그 스트림을 구독해 각 값을 출력하고, `hello` 그다음 `world`를 추가하고, 마지막으로 컨트롤러를 닫으세요. 값을 추가하기 전에 `listen`으로 구독합니다. 프로그램은 다음을 출력해야 합니다:

```
hello
world
```

# --answers--

```dart
import 'dart:async';
```

```dart
void main() {
```

```dart
  final controller = StreamController<String>();
```

```dart
  controller.stream.listen((word) => print(word));
```

```dart
  controller.add('hello');
```

```dart
  controller.add('world');
```

```dart
  controller.close();
```

```dart
}
```

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>();
  controller.stream.listen((word) => print(word));
  controller.add('hello');
  controller.add('world');
  controller.close();
}
```

# --output--

hello
world
