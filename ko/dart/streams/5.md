---
language: dart
exerciseType: 2
---

# --description--

`await for`는 스트림이 완료될 때까지 현재 함수를 멈춥니다. **기다리지 않고** 이벤트에 반응하고 싶을 때는 `listen`을 호출하고 콜백을 넘기세요. 콜백은 이벤트마다 한 번씩 호출되고, `listen` 다음 코드는 곧바로 실행됩니다.

```dart
final ticks = Stream.fromIterable([1, 2]);
ticks.listen((tick) => print('tick $tick'));
print('not waiting');
// not waiting
// tick 1
// tick 2
```

`listen`은 이름 있는 매개변수 `onDone`도 받습니다. 스트림이 끝날 때 호출되는, 인자가 없는 함수입니다:

```dart
final ticks2 = Stream.fromIterable([1, 2]);
ticks2.listen((tick) => print(tick), onDone: () => print('no more ticks'));
```

# --instructions--

스트림을 구독하고, 각 글자를 출력하고, 스트림이 끝날 때 `done`을 출력하도록 코드를 완성하세요. 프로그램은 `listening`, 그다음 `a`, `b`, `done`을 출력해야 합니다.

# --seed--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.[/]((letter) => print(letter), [/]: () => print('done'));
  print('listening');
}
```

# --answers--

- listen
- onDone
- onError
- forEach
- await

# --solutions--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.listen((letter) => print(letter), onDone: () => print('done'));
  print('listening');
}
```

# --output--

listening
a
b
done
