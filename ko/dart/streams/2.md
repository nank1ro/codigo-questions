---
language: dart
exerciseType: 1
---

# --description--

`Stream.fromIterable`은 모든 값을 미리 필요로 합니다. 값을 하나씩 **생산**하려면 **비동기 제너레이터**를 작성하세요. 본문이 `async*`로 표시되고 반환 타입이 `Stream<T>`인 함수입니다. 그 안에서 `yield`가 스트림에 이벤트 하나를 보냅니다:

```dart
Stream<int> countTo(int n) async* {
  for (var i = 1; i <= n; i++) {
    yield i;
  }
}
```

`countTo(3)`을 호출해도 본문은 실행되지 않습니다. 리스너가 값을 요청하는 대로 지연 실행되며, 본문이 끝나면 스트림은 완료됩니다.

모든 이벤트를 `List`로 모으려면 `toList()`를 호출하세요. 이는 `Future<List<T>>`를 반환하므로 `await`로 기다립니다:

```dart
final values = await countTo(3).toList();
print(values); // [1, 2, 3]
```

# --instructions--

`int` `from`을 받아 `from`부터 `1`까지 내려가는 값을 내보내는 `Stream<int>`를 반환하는 `async*` 함수 `countdown`을 작성하세요. `from`이 `0` 이하이면 스트림은 아무것도 내보내지 않습니다.

예시:
```dart
print(await countdown(3).toList()); // [3, 2, 1]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countdown(3)`은 `3`, `2`, `1`을 내보내야 합니다

```dart
  test('test1', () async {
    expect(await countdown(3).toList(), [3, 2, 1], reason: '--err-t1--');
  });
```

`countdown(1)`은 `1`만 내보내야 합니다

```dart
  test('test2', () async {
    expect(await countdown(1).toList(), [1], reason: '--err-t2--');
  });
```

`countdown(0)`은 아무것도 내보내지 않아야 하고 `countdown`은 `Stream<int>`를 반환해야 합니다

```dart
  test('test3', () async {
    expect(await countdown(0).toList(), <int>[], reason: '--err-t3--');
    expect(countdown(2), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> countdown(int from) async* {
  for (var i = from; i >= 1; i--) {
    yield i;
  }
}
```
