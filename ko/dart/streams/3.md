---
language: dart
exerciseType: 1
---

# --description--

`await for` 루프는 출력하는 것 이상을 할 수 있습니다. 루프 앞에서 선언한 변수를 갱신할 수 있죠. 스트림을 소비해 결과를 계산하는 함수는 `async`로 표시해야 하며, 그 결과의 `Future`를 반환합니다:

```dart
Future<int> count(Stream<String> words) async {
  var total = 0;
  await for (final word in words) {
    total += word.length;
  }
  return total;
}
```

이 함수는 스트림이 완료된 뒤에야 `return`에 도달하므로, 호출자는 future를 기다릴 때 최종 값을 받습니다:

```dart
print(await count(Stream.fromIterable(['hi', 'dart']))); // 6
```

# --instructions--

함수 `scores()`는 `Stream<int>`를 반환합니다. `scores()`에 대한 `await for` 루프를 사용해 모든 값을 `total`에 더하세요.

# --before-seed--

```dart
Stream<int> scores() => Stream.fromIterable([4, 7, 1, 9]);

Future<dynamic> result() async {
```

# --seed--

```dart
var total = 0;
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total`은 `21`이어야 합니다

```dart
  test('test1', () async {
    expect(await result(), 21, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var total = 0;
await for (final score in scores()) {
  total += score;
}
```
