---
language: dart
exerciseType: 1
---

# --description--

`toList()` 외에도 스트림에는 모든 이벤트를 **소비**하고 하나의 `Future`를 반환하는 메서드들이 있습니다:

- `first`와 `last`는 첫 번째 또는 마지막 이벤트로 완료됩니다
- `length`는 이벤트 개수로 완료됩니다
- `join(separator)`는 모든 이벤트를 하나의 `String`으로 이어 붙인 값으로 완료됩니다
- `reduce(combine)`은 이벤트를 두 개씩 묶어 하나의 값으로 만듭니다

```dart
final numbers = Stream.fromIterable([3, 9, 2]);
print(await numbers.reduce((a, b) => a + b)); // 14
```

`reduce`는 지금까지의 결과와 다음 이벤트를 `combine`에 전달합니다. 스트림이 비어 있으면 예외를 던지므로, 이벤트가 최소 하나 보장될 때만 사용하세요.

# --instructions--

두 함수를 작성하세요:
- `largest`: `Stream<int>` `source`를 받아 `reduce`를 사용해 가장 큰 값으로 완료되는 `Future<int>`를 반환합니다. 스트림에는 항상 최소 하나의 이벤트가 있습니다.
- `sentence`: `Stream<String>` `words`를 받아 모든 단어를 공백 하나로 이어 붙인 값으로 완료되는 `Future<String>`을 반환합니다.

예시:
```dart
print(await largest(Stream.fromIterable([3, 9, 2])));        // 9
print(await sentence(Stream.fromIterable(['I', 'am', 'Dart']))); // I am Dart
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`3, 9, 2` 스트림에 대한 `largest`는 `9`로 완료되어야 합니다

```dart
  test('test1', () async {
    expect(await largest(Stream.fromIterable([3, 9, 2])), 9, reason: '--err-t1--');
  });
```

`-4, -1, -7` 스트림에 대한 `largest`는 `-1`로, `5` 스트림에 대한 `largest`는 `5`로 완료되어야 합니다

```dart
  test('test2', () async {
    expect(await largest(Stream.fromIterable([-4, -1, -7])), -1, reason: '--err-t2--');
    expect(await largest(Stream.fromIterable([5])), 5, reason: '--err-t2--');
  });
```

`'I', 'am', 'Dart'` 스트림에 대한 `sentence`는 `'I am Dart'`로 완료되어야 합니다

```dart
  test('test3', () async {
    expect(await sentence(Stream.fromIterable(['I', 'am', 'Dart'])), 'I am Dart', reason: '--err-t3--');
    expect(await sentence(Stream.fromIterable(['solo'])), 'solo', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> largest(Stream<int> source) {
  return source.reduce((a, b) => a > b ? a : b);
}

Future<String> sentence(Stream<String> words) {
  return words.join(' ');
}
```
