---
language: dart
exerciseType: 1
---

# --description--

`Iterable`처럼 `Stream`에도 기존 스트림으로부터 **새 스트림**을 만드는 메서드가 있습니다:

- `map`은 각 이벤트를 변환합니다
- `where`는 조건을 만족하는 이벤트만 남깁니다
- `take`는 주어진 개수의 이벤트 후에 멈춥니다

```dart
final numbers = Stream.fromIterable([1, 2, 3, 4, 5, 6]);
final firstOdds = numbers.where((n) => n.isOdd).take(2);
print(await firstOdds.toList()); // [1, 3]
```

이 메서드들은 **지연 평가**됩니다. 누군가 결과 스트림을 구독하기 전까지는 아무것도 실행되지 않습니다. 체이닝할 수 있고, 원본 스트림은 절대 변경되지 않습니다.

# --instructions--

`Stream<int>` `source`를 받아 `source`의 각 **짝수** 값의 제곱을 같은 순서로 담은 `Stream<int>`를 반환하는 함수 `evenSquares`를 작성하세요.

예시:
```dart
final source = Stream.fromIterable([1, 2, 3, 4]);
print(await evenSquares(source).toList()); // [4, 16]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`1, 2, 3, 4` 스트림에 대한 `evenSquares`는 `4, 16`을 내보내야 합니다

```dart
  test('test1', () async {
    expect(await evenSquares(Stream.fromIterable([1, 2, 3, 4])).toList(), [4, 16], reason: '--err-t1--');
  });
```

`6, 5, 10` 스트림에 대한 `evenSquares`는 `36, 100`을 내보내야 합니다

```dart
  test('test2', () async {
    expect(await evenSquares(Stream.fromIterable([6, 5, 10])).toList(), [36, 100], reason: '--err-t2--');
  });
```

`1, 3` 스트림에 대한 `evenSquares`는 아무것도 내보내지 않아야 하고 `Stream<int>`를 반환해야 합니다

```dart
  test('test3', () async {
    expect(await evenSquares(Stream.fromIterable([1, 3])).toList(), <int>[], reason: '--err-t3--');
    expect(evenSquares(Stream.fromIterable([2])), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> evenSquares(Stream<int> source) {
  return source.where((n) => n.isEven).map((n) => n * n);
}
```
