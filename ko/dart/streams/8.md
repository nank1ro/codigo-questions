---
language: dart
exerciseType: 2
---

# --description--

`where`, `map`, `take`가 각각 스트림을 반환하므로, 이들을 체이닝하고 마지막에 `toList()`를 붙여 결과를 리스트로 얻을 수 있습니다. `Future`를 반환하는 유일한 호출이 마지막 `toList()`이므로, `await`가 필요한 곳도 거기뿐입니다:

```dart
final numbers = Stream.fromIterable([5, 12, 8, 20, 3]);
final big = await numbers.where((n) => n > 6).take(2).toList();
print(big); // [12, 8]
```

# --instructions--

정확히 `3`글자인 단어만 남기고, 그중 처음 `2`개 뒤에 멈추고, 리스트로 모으도록 코드를 완성해서 프로그램이 `[sun, sky]`를 출력하게 하세요.

# --seed--

```dart
Future<void> main() async {
  final words = Stream.fromIterable(['sun', 'moon', 'star', 'sky', 'fog']);
  final short = await words.[/]((w) => w.length == 3).[/](2).[/]();
  print(short);
}
```

# --answers--

- where
- take
- toList
- map
- skip
- first

# --solutions--

```dart
Future<void> main() async {
  final words = Stream.fromIterable(['sun', 'moon', 'star', 'sky', 'fog']);
  final short = await words.where((w) => w.length == 3).take(2).toList();
  print(short);
}
```

# --output--

[sun, sky]
