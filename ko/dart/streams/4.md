---
language: dart
exerciseType: 3
---

# --description--

모든 스트림은 결국 끝납니다. `async*` 제너레이터의 경우, 함수 본문이 끝나는 순간 스트림은 **완료**됩니다. 끝까지 도달했든 `return`을 만났든 마찬가지입니다. 완료된 스트림에 대한 `await for` 루프는 빠져나오고, `toList()`의 future는 모두 완료됩니다.

스트림은 다시 시작하거나 값을 반복하지 않습니다. 한 번 완료되면 계속 완료된 상태로 남습니다.

# --instructions--

`async*` 함수가 반환한 스트림은 함수 본문이 끝나면 어떻게 되나요?

# --answers--

- 스트림이 완료되고, 그에 대한 `await for` 루프가 끝난다
- 스트림이 첫 번째 `yield`부터 다시 시작한다
- 스트림에 `close()`가 호출될 때까지 열린 상태로 남는다
- 마지막으로 내보낸 값이 한 번 더 내보내진다

# --solutions--

- 스트림이 완료되고, 그에 대한 `await for` 루프가 끝난다
