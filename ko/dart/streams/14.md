---
language: dart
exerciseType: 2
---

# --description--

컨트롤러는 이름 있는 생성자 `StreamController<T>.broadcast()`로 브로드캐스트 스트림을 바로 만들 수 있습니다. 그 `stream`은 리스너를 몇 개든 받아들이고, 각 이벤트는 구독한 순서대로 그들 모두에게 전달됩니다:

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>.broadcast();
  controller.stream.listen((msg) => print('first: $msg'));
  controller.stream.listen((msg) => print('second: $msg'));
  controller.add('hi');
  controller.close();
}
// first: hi
// second: hi
```

모든 브로드캐스트 스트림처럼 버퍼링하지 않습니다. 리스너가 구독하기 전에 추가된 이벤트는 그 리스너에게는 사라집니다.

# --instructions--

브로드캐스트 컨트롤러를 만들고, 두 리스너 `A`와 `B`를 구독시키고, 값 `1`과 `2`를 보낸 뒤 컨트롤러를 닫도록 코드를 완성해서, 프로그램이 `A got 1`, `B got 1`, `A got 2`, `B got 2`를 출력하게 하세요.

# --seed--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.[/]();
  controller.stream.[/]((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.[/](1);
  controller.add(2);
  controller.[/]();
}
```

# --answers--

- broadcast
- listen
- add
- close
- asBroadcastStream
- subscribe
- sink

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.broadcast();
  controller.stream.listen((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
```

# --output--

A got 1
B got 1
A got 2
B got 2
