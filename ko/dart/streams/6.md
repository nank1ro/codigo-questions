---
language: dart
exerciseType: 4
---

# --description--

`async*` 제너레이터는 `yield*`(yield-star)로 **다른 스트림의 모든 이벤트**를 전달할 수 있습니다. 각 값을 yield하는 `await for` 루프를 한 줄로 쓴 것과 같습니다:

```dart
Stream<int> ones() async* {
  yield 1;
  yield 1;
}

Stream<int> sequence() async* {
  yield 0;
  yield* ones();
  yield 2;
}
// sequence() emits 0, 1, 1, 2
```

내부 스트림이 완료되면 바깥 스트림은 자신의 `yield`를 이어갑니다.

# --instructions--

항목을 정렬해서 `1`과 `2`를 내보내는 `async*` 함수 `pair`, `0`에 이어 `pair()`의 모든 값, 그다음 `3`을 내보내는 `async*` 함수 `all`, 그리고 `all()`의 각 값을 출력하는 `main`을 정의하세요. `pair`를 먼저, 그다음 `all`, 그다음 `main`을 정의합니다. 프로그램은 다음을 출력해야 합니다:

```
0
1
2
3
```

# --answers--

```dart
Stream<int> pair() async* {
```

```dart
  yield 1;
```

```dart
  yield 2;
```

```dart
}
```

```dart
Stream<int> all() async* {
```

```dart
  yield 0;
```

```dart
  yield* pair();
```

```dart
  yield 3;
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  await for (final n in all()) {
```

```dart
    print(n);
```

```dart
  }
```

```dart
}
```

# --solutions--

```dart
Stream<int> pair() async* {
  yield 1;
  yield 2;
}

Stream<int> all() async* {
  yield 0;
  yield* pair();
  yield 3;
}

Future<void> main() async {
  await for (final n in all()) {
    print(n);
  }
}
```

# --output--

0
1
2
3
