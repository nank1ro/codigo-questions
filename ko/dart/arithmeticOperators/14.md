---
language: dart
exerciseType: 1
---

# --instructions--

두 최상위 `int` 변수를 선언합니다: 값 `29`의 `total`과 값 `4`의 `perGroup`. 그런 다음 `29`에 `4`의 완전한 그룹이 몇 개나 맞는지를 저장하는 최상위 `int` 변수 `groups`를 선언합니다(정수 나눗셈 `~/` 사용).

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`groups`는 `7`과 같아야 합니다

```dart
  test('test1', () {
    expect(groups, 7, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 29;
int perGroup = 4;
int groups = total ~/ perGroup;

void main() {
  print(groups);
}
```
