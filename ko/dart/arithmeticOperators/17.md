---
language: dart
exerciseType: 1
---

# --instructions--

가게가 `itemCount`개의 상품을 `pricePerItem` 달러에 판매하며 총액에서 `discount` 달러를 뺍니다. 이 3개의 최상위 `int` 변수를 선언합니다: `itemCount` = `6`, `pricePerItem` = `15`, `discount` = `10`. 그런 다음 할인 후 총액을 계산하는 최상위 `int` 변수 `totalPrice`를 선언합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`totalPrice`는 `80`과 같아야 합니다

```dart
  test('test1', () {
    expect(totalPrice, 80, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int itemCount = 6;
int pricePerItem = 15;
int discount = 10;
int totalPrice = itemCount * pricePerItem - discount;

void main() {
  print(totalPrice);
}
```
