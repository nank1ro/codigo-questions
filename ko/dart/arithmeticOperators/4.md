---
language: dart
exerciseType: 2
---

# --description--

**나눗셈 연산자** `/`는 한 값을 다른 값으로 나눕니다. Dart에서는 `/`가 결과가 정수인 경우에도 **항상 `double`을 반환합니다**.

```dart
int a = 10;
int b = 4;
double result = a / b; // 2.5

double exact = 10 / 2; // 5.0  (not 5!)
```

`/`의 결과를 저장하려면 항상 `double` 변수를 사용하십시오.

# --instructions--

`9`를 `4`로 나누는 코드를 완성하고 결과를 `result`에 저장합니다.

# --seed--

```dart
void main() {
  double result = 9 [/] 4;
  print(result);
}
```

# --answers--

- /
- *
- +
- -

# --solutions--

```dart
void main() {
  double result = 9 / 4;
  print(result);
}
```

# --output--

2.25
