---
language: dart
exerciseType: 1
---

# --instructions--

최상위 `double` 변수 `result`를 선언하여 `7`을 `2`로 나눈 결과를 저장합니다. Dart에서 `/`는 항상 `double`을 반환한다는 것을 기억하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result`는 값 `3.5`의 `double`이어야 합니다

```dart
  test('test1', () {
    expect(result, isA<double>(), reason: '--err-t1--');
    expect(result, closeTo(3.5, 0.001), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double result = 7 / 2;

void main() {
  print(result);
}
```
