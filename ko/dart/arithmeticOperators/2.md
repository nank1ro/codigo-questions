---
language: dart
exerciseType: 2
---

# --description--

**뺄셈 연산자** `-`는 한 값에서 다른 값을 뺍니다.

```dart
int a = 10;
int b = 4;
int diff = a - b; // 6
```

두 번째 피연산자가 첫 번째보다 크면 결과가 음수일 수 있습니다.

# --instructions--

`20`에서 `9`를 빼는 코드를 완성하고 결과를 `diff`에 저장합니다.

# --seed--

```dart
void main() {
  int diff = 20 [/] 9;
  print(diff);
}
```

# --answers--

- -
- +
- *
- /

# --solutions--

```dart
void main() {
  int diff = 20 - 9;
  print(diff);
}
```

# --output--

11
