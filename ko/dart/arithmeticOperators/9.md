---
language: dart
exerciseType: 1
---

# --instructions--

모듈로 연산자 `%`를 사용하여 `17`을 `5`로 나눈 나머지를 구합니다. 결과를 `remainder`라는 최상위 변수에 저장합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

변수 `remainder`는 `2`와 같아야 합니다

```dart
  test('test1', () {
    expect(remainder, 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 17 % 5;

void main() {
  print(remainder);
}
```
