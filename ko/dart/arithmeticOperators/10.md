---
language: dart
exerciseType: 1
---

# --instructions--

두 최상위 `int` 변수를 선언합니다: 값 `12`의 `a`와 값 `8`의 `b`. 그런 다음 합계를 저장하는 세 번째 최상위 `int` 변수 `total`을 선언합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`a`는 `12`, `b`는 `8`, `total`은 `20`이어야 합니다

```dart
  test('test1', () {
    expect(a, 12, reason: '--err-t1--');
    expect(b, 8, reason: '--err-t2--');
    expect(total, 20, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 12;
int b = 8;
int total = a + b;

void main() {
  print(total);
}
```
