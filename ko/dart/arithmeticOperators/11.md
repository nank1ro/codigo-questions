---
language: dart
exerciseType: 1
---

# --instructions--

두 최상위 `int` 변수를 선언합니다: 값 `25`의 `x`와 값 `9`의 `y`. 그런 다음 `y`를 `x`에서 뺀 결과를 저장하는 최상위 `int` 변수 `diff`를 선언합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`diff`는 `16`과 같아야 합니다

```dart
  test('test1', () {
    expect(diff, 16, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 25;
int y = 9;
int diff = x - y;

void main() {
  print(diff);
}
```
