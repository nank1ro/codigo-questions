---
language: dart
exerciseType: 1
---

# --instructions--
`List<int>`에서 홀수만 포함하는 리스트를 반환하는 함수를 작성하세요. 짝수를 건너뛰기 위해 `continue`가 있는 `for` 반복문을 사용하세요.

# --before-seed--
```dart
List<int> filterOdds(List<int> numbers) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
짝수를 걸러낸다
```dart
    test('filters evens', () {
      expect(filterOdds([1, 2, 3, 4, 5]), [1, 3, 5], reason: '--err-t1--');
    });
```

모두 짝수이면 빈 리스트를 반환한다
```dart
    test('all even', () {
      expect(filterOdds([2, 4, 6]), [], reason: '--err-t2--');
    });
```

모두 홀수이면 같은 값들을 반환한다
```dart
    test('all odd', () {
      expect(filterOdds([1, 3, 5]), [1, 3, 5], reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  List<int> result = [];
  for (int i = 0; i < numbers.length; i++) {
    if (numbers[i] % 2 == 0) continue;
    result.add(numbers[i]);
  }
```
