---
language: dart
exerciseType: 1
---

# --instructions--
`for` 반복문을 사용하여 음이 아닌 정수 `n`의 팩토리얼을 계산하는 함수를 작성하세요. 0의 팩토리얼은 1입니다.

# --before-seed--
```dart
int factorial(int n) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
5의 팩토리얼은 120
```dart
    test('5! = 120', () {
      expect(factorial(5), 120, reason: '--err-t1--');
    });
```

0의 팩토리얼은 1
```dart
    test('0! = 1', () {
      expect(factorial(0), 1, reason: '--err-t2--');
    });
```

1의 팩토리얼은 1
```dart
    test('1! = 1', () {
      expect(factorial(1), 1, reason: '--err-t3--');
    });
```

6의 팩토리얼은 720
```dart
    test('6! = 720', () {
      expect(factorial(6), 720, reason: '--err-t4--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 1;
  for (int i = 2; i <= n; i++) {
    result *= i;
  }
```
