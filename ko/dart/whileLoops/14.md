---
language: dart
exerciseType: 1
---

# --instructions--

`while` 반복문을 사용하여 `10`에서 `1`까지(포함) 카운트다운하세요. 수행된 반복 횟수를 반환하세요.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return iterations;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`iterations`는 `10`과 같아야 합니다

```dart
  test('test1', () {
    expect(computeValue(), 10, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 10;
int iterations = 0;
while (n >= 1) {
  iterations++;
  n--;
}
```
