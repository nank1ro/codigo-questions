---
language: dart
exerciseType: 1
---

# --instructions--

`while` 반복문을 사용하여 `1`부터 `10`까지(포함)의 숫자 합계를 계산하고 결과를 반환하세요.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum`은 `55`와 같아야 합니다

```dart
  test('test1', () {
    expect(computeValue(), 55, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sum = 0;
int i = 1;
while (i <= 10) {
  sum += i;
  i++;
}
```
