---
language: dart
exerciseType: 1
---

# --instructions--

`do-while` 반복문을 사용하여 `0`에서 시작하는 변수 `total`에 `total`이 `12` 이상이 될 때까지 `3`을 계속 더하세요. `total`을 반환하세요.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total`은 `12`와 같아야 합니다

```dart
  test('test1', () {
    expect(computeValue(), 12, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 0;
do {
  total += 3;
} while (total < 12);
```
