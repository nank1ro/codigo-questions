---
language: dart
exerciseType: 1
---

# --instructions--

`product = 1`에서 시작하여 `while` 반복문을 사용해 `product`가 `100` 이상이 될 때까지 `2`를 계속 곱하세요. `product`를 반환하세요.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return product;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`product`는 `128`과 같아야 합니다

```dart
  test('test1', () {
    expect(computeValue(), 128, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int product = 1;
while (product < 100) {
  product *= 2;
}
```
