---
language: dart
exerciseType: 3
---

# --description--

타입 승격은 외부에서 바뀔 수 있는 클래스 **필드**에는 **동작하지 않습니다**. 검사와 사용 사이에 다른 코드(서브클래스에서 오버라이드된 게터, 다른 메서드)가 그것을 다시 `null`로 설정할 수 있기 때문입니다:

```dart
class Box {
  int? value;

  int doubled() {
    if (value != null) {
      return value * 2; // error: 'value' can't be unconditionally accessed
    }
    return 0;
  }
}
```

표준적인 해결책은 필드를 **지역 변수**에 복사하는 것입니다. 지역 변수는 승격됩니다:

```dart
int doubled() {
  final v = value;
  if (v != null) {
    return v * 2;
  }
  return 0;
}
```

# --instructions--

설명에 있는 `doubled`의 첫 번째 버전이 컴파일되지 않는 이유는 무엇인가요?

# --answers--

- `value`는 필드이므로 널 검사로 승격되지 않는다. 먼저 지역 변수에 복사해야 한다
- `int?` 값은 `null`이 아니어도 곱할 수 없다
- `if` 문에 `else` 분기가 없다
- 클래스의 필드는 널 가능 타입을 가질 수 없다

# --solutions--

- `value`는 필드이므로 널 검사로 승격되지 않는다. 먼저 지역 변수에 복사해야 한다
