---
language: dart
exerciseType: 1
---

# --description--

실제 데이터에는 빈틈이 가득합니다. 비워 둔 양식 항목, 파일에서 빠진 열, 숫자라고 하기 어려운 문자열처럼 말이죠. 이 장의 도구들은 그런 데이터를 다루기 위해 자연스럽게 결합됩니다. 없는 요소를 버리려면 `nonNulls`, 안전하게 변환하려면 `int.tryParse`, 변환하지 못한 것을 처리하려면 `??`나 `if` 검사를 사용합니다.

# --instructions--

`List<String?>` 매개변수 `prices`를 받아 `int`로 변환할 수 있는 값들의 합을 반환하는 함수 `total`을 작성하세요. `null` 항목과 올바른 정수가 아닌 문자열은 모두 무시합니다.

예시:
```dart
print(total(['3', null, 'abc', '4'])); // 7
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total(['3', null, 'abc', '4'])`는 `7`이어야 합니다

```dart
  test('test1', () {
    expect(total(['3', null, 'abc', '4']), 7, reason: '--err-t1--');
  });
```

`total([null, null])`은 `0`이어야 합니다

```dart
  test('test2', () {
    expect(total([null, null]), 0, reason: '--err-t2--');
  });
```

`total(['10', '', '-2', '5'])`는 `13`이어야 합니다

```dart
  test('test3', () {
    expect(total(['10', '', '-2', '5']), 13, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total(List<String?> prices) {
  var sum = 0;
  for (var price in prices.nonNulls) {
    var value = int.tryParse(price);
    if (value != null) {
      sum += value;
    }
  }
  return sum;
}
```
