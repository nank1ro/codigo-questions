---
language: dart
exerciseType: 4
---

# --description--

두 개의 연산자에도 널 인식 변형이 있습니다.

**널 인식 캐스케이드** `?..`는 객체가 `null`이 아닐 때만 캐스케이드 연산의 연쇄를 실행하고, 그렇지 않으면 전부 건너뜁니다:

```dart
List<int>? numbers;
numbers?..add(1)..add(2); // nothing happens, numbers is still null
```

**널 인식 스프레드** `...?`는 널 가능 컬렉션의 요소를 리터럴에 넣고, 컬렉션이 `null`이면 아무것도 추가하지 않습니다:

```dart
List<int>? extra;
print([0, ...?extra]); // [0]

extra = [1, 2];
print([0, ...?extra]); // [0, 1, 2]
```

`?`가 없으면 `List<int>?`에 대한 `...extra`는 컴파일 오류가 됩니다.

# --instructions--

항목을 정렬해서 `?..`로 널 가능 리스트에 `1`과 `2`를 추가하는 함수 `fill`을 정의하고, 그것을 `main`에서 사용하세요. 함수를 먼저 정의하고 그다음 `main`을 정의하세요. 프로그램은 다음을 출력해야 합니다:

```
[0]
[1, 2, 3]
```

# --answers--

```dart
void fill(List<int>? target) {
```

```dart
  target?..add(1)..add(2);
```

```dart
}
```

```dart
void main() {
```

```dart
  List<int>? numbers;
```

```dart
  print([0, ...?numbers]);
```

```dart
  numbers = [];
```

```dart
  fill(numbers);
```

```dart
  print([...?numbers, 3]);
```

```dart
}
```

# --solutions--

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  print([0, ...?numbers]);
  numbers = [];
  fill(numbers);
  print([...?numbers, 3]);
}
```

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  numbers = [];
  print([0, ...?numbers]);
  fill(numbers);
  print([...?numbers, 3]);
}
```

# --output--

[0]
[1, 2, 3]
