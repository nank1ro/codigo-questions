---
language: dart
exerciseType: 1
---

# --description--

널 가능 값을 `if`로 확인하는 것은 `!`보다 안전하며, Dart는 그에 대해 보상해 줍니다. `if (x != null)` 같은 검사 뒤에는 컴파일러가 블록 안에서 `x`가 `null`일 수 없음을 알기 때문에, 그곳에서 `x`를 널 불가능으로 취급합니다. 이를 **타입 승격**이라고 합니다:

```dart
int twice(int? n) {
  if (n != null) {
    return n * 2; // here n is an int, no ! needed
  }
  return 0;
}
```

이른 반환 뒤에도 승격이 동작합니다:

```dart
int twice(int? n) {
  if (n == null) return 0;
  return n * 2; // n is an int from here on
}
```

승격은 **지역 변수와 매개변수**에 적용됩니다. 이들의 값은 검사와 사용 사이에 몰래 바뀔 수 없기 때문입니다.

# --instructions--

널 가능 `int` 매개변수 `n`을 받아 정수 나눗셈 `~/`로 `n`을 `2`로 나눈 값을 반환하고, `n`이 `null`이면 `0`을 반환하는 함수 `half`를 작성하세요. `!`가 아니라 `if` 널 검사를 사용하세요.

예시:
```dart
print(half(10));   // 5
print(half(null)); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`half(10)`은 `5`여야 합니다

```dart
  test('test1', () {
    expect(half(10), 5, reason: '--err-t1--');
  });
```

`half(7)`은 `3`이어야 합니다

```dart
  test('test2', () {
    expect(half(7), 3, reason: '--err-t2--');
  });
```

`half(null)`은 `0`이어야 합니다

```dart
  test('test3', () {
    expect(half(null), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int half(int? n) {
  if (n != null) {
    return n ~/ 2;
  }
  return 0;
}
```
