---
language: dart
exerciseType: 4
---

# --description--

널 가능성은 **이름 있는 매개변수**를 선언하는 방식에도 영향을 줍니다. 널 가능 타입의 이름 있는 매개변수는 선택적입니다. 호출자가 생략하면 그냥 `null`이 됩니다.

```dart
String label({String? title}) => title ?? 'untitled';

print(label());               // untitled
print(label(title: 'Notes')); // Notes
```

널 불가능 타입이면서 기본값도 없는 이름 있는 매개변수는 생략되면 값이 없게 되므로, Dart는 그것을 `required`로 표시하도록 요구합니다. 그러면 호출자는 항상 그것을 전달해야 합니다:

```dart
String label({required String name, String? title}) { ... }

label(name: 'Ada');               // ok
label(name: 'Ada', title: 'Dr.'); // ok
label(title: 'Dr.');              // error: the named parameter 'name' is required
```

# --instructions--

항목을 정렬해서 필수 이름 있는 매개변수 `name`과 선택적이며 널 가능한 이름 있는 매개변수 `title`을 가진 함수 `label`을 정의하세요. `title`이 `null`이면 `name`만, 그렇지 않으면 `'$title $name'`을 반환합니다. 함수를 먼저 정의하고 그다음 `main`을 정의하세요. 프로그램은 다음을 출력해야 합니다:

```
Ada
Countess Lovelace
```

# --answers--

```dart
String label({required String name, String? title}) {
```

```dart
  if (title == null) return name;
```

```dart
  return '$title $name';
```

```dart
}
```

```dart
void main() {
```

```dart
  print(label(name: 'Ada'));
```

```dart
  print(label(name: 'Lovelace', title: 'Countess'));
```

```dart
}
```

# --solutions--

```dart
String label({required String name, String? title}) {
  if (title == null) return name;
  return '$title $name';
}

void main() {
  print(label(name: 'Ada'));
  print(label(name: 'Lovelace', title: 'Countess'));
}
```

# --output--

Ada
Countess Lovelace
