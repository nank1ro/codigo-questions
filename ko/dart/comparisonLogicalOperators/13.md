---
language: dart
exerciseType: 1
---

# --instructions--

2개의 최상위 레벨 `int` 변수를 선언합니다: 값 `30`을 가진 `price`와 값 `50`을 가진 `budget`. 그런 다음 `<` 연산자를 사용하여 `price`가 `budget`보다 작은지 확인한 결과를 저장하는 `canAfford`이라는 최상위 레벨 `bool` 변수를 선언합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canAfford`는 `true`이고 `bool` 타입이어야 합니다

```dart
  test('test1', () {
    expect(canAfford, true, reason: '--err-t1--');
    expect(canAfford, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int price = 30;
int budget = 50;
bool canAfford = price < budget;

void main() {
  print(canAfford);
}
```
