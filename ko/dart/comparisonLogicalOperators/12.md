---
language: dart
exerciseType: 1
---

# --instructions--

2개의 최상위 레벨 `int` 변수를 선언합니다: 값 `20`을 가진 `high`와 값 `12`를 가진 `low`. 그런 다음 `>` 연산자를 사용하여 `high`가 `low`보다 큰지 확인한 결과를 저장하는 `isHigher`이라는 최상위 레벨 `bool` 변수를 선언합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHigher`은 `true`이고 `bool` 타입이어야 합니다

```dart
  test('test1', () {
    expect(isHigher, true, reason: '--err-t1--');
    expect(isHigher, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int high = 20;
int low = 12;
bool isHigher = high > low;

void main() {
  print(isHigher);
}
```
