---
language: dart
exerciseType: 1
---

# --instructions--

2개의 최상위 레벨 `int` 변수를 선언합니다: 값 `8`을 가진 `x`와 값 `8`을 가진 `y`. 그런 다음 `==` 연산자로 `x`와 `y`를 비교한 결과를 저장하는 `areEqual`이라는 최상위 레벨 `bool` 변수를 선언합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`areEqual`은 `true`이고 `bool` 타입이어야 합니다

```dart
  test('test1', () {
    expect(areEqual, true, reason: '--err-t1--');
    expect(areEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 8;
int y = 8;
bool areEqual = x == y;

void main() {
  print(areEqual);
}
```
