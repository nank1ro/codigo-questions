---
language: dart
exerciseType: 1
---

# --instructions--

**NOT 연산자** `!` 는 부울 값을 반전시킵니다: `!true`는 `false`가 되고 `!false`는 `true`가 됩니다.

값 `false`를 가진 `isOffline`이라는 최상위 레벨 `bool` 변수를 선언합니다. 그런 다음 `!` 연산자를 사용하여 `isOffline`의 부정을 저장하는 `isOnline`이라는 최상위 레벨 `bool` 변수를 선언합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isOnline`은 `true`이고 `bool` 타입이어야 합니다

```dart
  test('test1', () {
    expect(isOnline, true, reason: '--err-t1--');
    expect(isOnline, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isOffline = false;
bool isOnline = !isOffline;

void main() {
  print(isOnline);
}
```
