---
language: dart
exerciseType: 1
---

# --description--

**AND 연산자** `&&` 는 두 개의 부울 식을 결합합니다. **두 쪽 모두** `true`일 때만 `true`를 반환합니다. 어느 한쪽이라도 `false`이면 결과는 `false`입니다.

```dart
bool a = true;
bool b = true;
bool result = a && b; // true

bool c = true;
bool d = false;
bool result2 = c && d; // false
```

# --instructions--

2개의 최상위 레벨 `int` 변수를 선언합니다: 값 `25`를 가진 `age`와 값 `80`을 가진 `score`. 그런 다음 `age`가 18 이상 AND `score`가 60 이상일 때만 `true`인 `qualifies`라는 최상위 레벨 `bool` 변수를 선언합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`qualifies`는 `true`이고 `bool` 타입이어야 합니다

```dart
  test('test1', () {
    expect(qualifies, true, reason: '--err-t1--');
    expect(qualifies, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int age = 25;
int score = 80;
bool qualifies = age >= 18 && score >= 60;

void main() {
  print(qualifies);
}
```
