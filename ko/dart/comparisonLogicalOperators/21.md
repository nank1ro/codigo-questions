---
language: dart
exerciseType: 4
---

# --instructions--

코드 줄을 정렬하여 온도 값을 선언하고, `&&`를 사용하여 `0`에서 `100` 범위(포함) 내에 있는지 확인하고, 결과를 `isValid`에 저장하고, 인쇄합니다. 출력은 `true`이어야 합니다.

# --answers--

```dart
void main() {
```

```dart
  int temperature = 37;
```

```dart
  bool isValid = temperature >= 0 && temperature <= 100;
```

```dart
  print(isValid);
```

```dart
}
```

# --solutions--

```dart
void main() {
  int temperature = 37;
  bool isValid = temperature >= 0 && temperature <= 100;
  print(isValid);
}
```

# --output--

true
