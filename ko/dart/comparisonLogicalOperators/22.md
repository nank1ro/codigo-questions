---
language: dart
exerciseType: 4
---

# --instructions--

코드 줄을 정렬하여 두 개의 부울 변수를 선언하고, 표시된 대로 `||` 및 `&&`로 결합하고, 결과를 `outcome`에 저장하고, 인쇄합니다. 출력은 `true`이어야 합니다.

# --answers--

```dart
void main() {
```

```dart
  bool hasPass = false;
```

```dart
  bool isAdmin = true;
```

```dart
  bool outcome = hasPass || isAdmin && !hasPass;
```

```dart
  print(outcome);
```

```dart
}
```

# --solutions--

```dart
void main() {
  bool hasPass = false;
  bool isAdmin = true;
  bool outcome = hasPass || isAdmin && !hasPass;
  print(outcome);
}
```

# --output--

true
