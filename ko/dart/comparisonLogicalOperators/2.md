---
language: dart
exerciseType: 2
---

# --description--

**다름 연산자** `!=` 는 두 값이 **다르면** `true`, 같으면 `false`를 반환합니다.

```dart
int x = 3;
int y = 7;
bool result = x != y; // true
```

`==` 의 반대입니다.

# --instructions--

코드를 완성하여 `4`가 `9`와 다른지 확인하고 결과를 `isDifferent`에 저장합니다.

# --seed--

```dart
void main() {
  bool isDifferent = 4 [/] 9;
  print(isDifferent);
}
```

# --answers--

- !=
- ==
- !
- <>

# --solutions--

```dart
void main() {
  bool isDifferent = 4 != 9;
  print(isDifferent);
}
```

# --output--

true
