---
language: dart
exerciseType: 1
---

# --instructions--

3개의 최상위 레벨 `int` 변수를 선언합니다: 값 `5`를 가진 `x`, 값 `3`을 가진 `y`, 값 `0`을 가진 `z`. 그런 다음 `!(x > 10) || (y < 5 && z == 0)`을 평가하는 `result`라는 최상위 레벨 `bool` 변수를 선언합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result`는 `true`이고 `bool` 타입이어야 합니다

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 5;
int y = 3;
int z = 0;
bool result = !(x > 10) || (y < 5 && z == 0);

void main() {
  print(result);
}
```
