---
language: dart
exerciseType: 1
---

# --instructions--

2개의 최상위 레벨 `int` 변수를 선언합니다: 값 `5`를 가진 `a`와 값 `9`를 가진 `b`. 그런 다음 `!=` 연산자로 `a`와 `b`를 비교한 결과를 저장하는 `notEqual`이라는 최상위 레벨 `bool` 변수를 선언합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`notEqual`은 `true`이고 `bool` 타입이어야 합니다

```dart
  test('test1', () {
    expect(notEqual, true, reason: '--err-t1--');
    expect(notEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 5;
int b = 9;
bool notEqual = a != b;

void main() {
  print(notEqual);
}
```
