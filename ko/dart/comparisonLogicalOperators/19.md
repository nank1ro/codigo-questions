---
language: dart
exerciseType: 1
---

# --instructions--

3개의 최상위 레벨 `int` 변수를 선언합니다: 값 `42`를 가진 `number`, 값 `1`을 가진 `min`, 값 `100`을 가진 `max`. 그런 다음 `number`가 `min` 이상 AND `max` 이하일 때 `true`인 `inRange`라는 최상위 레벨 `bool` 변수를 선언합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`inRange`는 `true`이고 `bool` 타입이어야 합니다

```dart
  test('test1', () {
    expect(inRange, true, reason: '--err-t1--');
    expect(inRange, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int number = 42;
int min = 1;
int max = 100;
bool inRange = number >= min && number <= max;

void main() {
  print(inRange);
}
```
