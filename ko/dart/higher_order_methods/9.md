---
language: dart
exerciseType: 1
---

# --description--

`map`에 주는 함수가 각 요소에 대해 컬렉션을 반환하면, 컬렉션의 시퀀스가 만들어집니다. **`expand`**도 같은 일을 하지만 그다음 모든 것을 하나의 평평한 시퀀스로 합칩니다:

```dart
final numbers = [1, 2];
print(numbers.map((n) => [n, -n]).toList());    // [[1, -1], [2, -2]]
print(numbers.expand((n) => [n, -n]).toList()); // [1, -1, 2, -2]
```

순서는 유지됩니다: 첫 번째 요소가 만들어낸 모든 것이 먼저 오고, 그다음 두 번째 요소가 만들어낸 모든 것이 오는 식입니다.

반환된 컬렉션은 어떤 크기든 가질 수 있으므로, `expand`는 처음보다 **더 많거나 적은** 요소를 만들어내는 방법이기도 합니다: 어떤 요소에 대해 빈 리스트를 반환하면 그 요소는 그저 사라집니다.

```dart
print(['a b', 'c'].expand((s) => s.split(' ')).toList()); // [a, b, c]
```

# --instructions--

`expand`를 사용하여 `List<int>` `numbers`를 받아 모든 요소가 두 번 연속 나타나는 새로운 `List<int>`를 반환하는 함수 `doubleUp`을 작성하세요.

예시:
```dart
print(doubleUp([1, 2])); // [1, 1, 2, 2]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`doubleUp([1, 2])`는 `[1, 1, 2, 2]`를 반환해야 합니다

```dart
  test('test1', () {
    expect(doubleUp([1, 2]), [1, 1, 2, 2], reason: '--err-t1--');
  });
```

`doubleUp`은 원래 순서를 유지하고 `List<int>`를 반환해야 합니다

```dart
  test('test2', () {
    expect(doubleUp([5, 0, -3]), [5, 5, 0, 0, -3, -3], reason: '--err-t2--');
    expect(doubleUp([7]), isA<List<int>>(), reason: '--err-t2--');
  });
```

빈 리스트가 주어지면 `doubleUp`은 빈 리스트를 반환해야 하며, 받은 리스트를 변경해서는 안 됩니다

```dart
  test('test3', () {
    expect(doubleUp(<int>[]), <int>[], reason: '--err-t3--');
    final original = [4, 6];
    doubleUp(original);
    expect(original, [4, 6], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> doubleUp(List<int> numbers) {
  return numbers.expand((n) => [n, n]).toList();
}
```
