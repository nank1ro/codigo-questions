---
language: dart
exerciseType: 1
---

# --description--

일부 고차 메서드는 새 컬렉션을 만드는 대신 컬렉션에 대한 질문에 답합니다. 술어를 받아 `bool`을 반환합니다:

- **최소한 하나의** 요소라도 술어를 만족하면 `any`는 `true`입니다
- **모든** 요소가 술어를 만족하면 `every`는 `true`입니다

```dart
final numbers = [1, 2, 3];
print(numbers.any((n) => n > 2));   // true
print(numbers.every((n) => n > 2)); // false
```

둘 다 답이 확실해지는 즉시 멈춥니다: `any`는 일치하는 첫 번째 요소에서, `every`는 일치하지 않는 첫 번째 요소에서 멈춥니다.

빈 컬렉션에서 `any`는 `false`이고 `every`는 `true`입니다: 첫 번째를 참으로 만들 요소가 없고, 두 번째를 깨뜨릴 요소도 없습니다.

# --instructions--

함수 두 개를 작성하세요. 둘 다 `List<int>` `numbers`를 받습니다:

- `hasNegative`는 최소한 하나의 요소라도 `0`보다 작으면 `true`를 반환합니다
- `allEven`은 모든 요소가 짝수이면 `true`를 반환합니다

예시:
```dart
print(hasNegative([1, -2, 3])); // true
print(allEven([2, 4]));         // true
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasNegative([1, -2, 3])`는 `true`여야 하고 `hasNegative([1, 2, 3])`는 `false`여야 합니다

```dart
  test('test1', () {
    expect(hasNegative([1, -2, 3]), true, reason: '--err-t1--');
    expect(hasNegative([1, 2, 3]), false, reason: '--err-t1--');
  });
```

`allEven([2, 4, 6])`는 `true`여야 하고 `allEven([2, 5])`는 `false`여야 합니다

```dart
  test('test2', () {
    expect(allEven([2, 4, 6]), true, reason: '--err-t2--');
    expect(allEven([2, 5]), false, reason: '--err-t2--');
  });
```

빈 리스트에서 `hasNegative`는 `false`여야 하고 `allEven`은 `true`여야 합니다

```dart
  test('test3', () {
    expect(hasNegative(<int>[]), false, reason: '--err-t3--');
    expect(allEven(<int>[]), true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool hasNegative(List<int> numbers) {
  return numbers.any((n) => n < 0);
}

bool allEven(List<int> numbers) {
  return numbers.every((n) => n % 2 == 0);
}
```
