---
language: dart
exerciseType: 4
---

# --description--

생성된 isolate는 항상 같은 네 단계를 따릅니다. 우편함을 열고, 주소와 함께 워커를 생성하고, 답을 기다리고, 그것을 사용합니다.

```dart
import 'dart:isolate';

void worker(SendPort port) {
  port.send('done');
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

`Isolate.spawn` 앞의 `await`는 isolate가 작업을 끝내기를 기다리는 것이 아니라 *시작*되기를 기다립니다. 결과는 나중에 포트를 통해 도착합니다.

# --instructions--

코드 라인을 정렬하여 `7 * 6`을 main isolate로 다시 보내는 `worker` isolate를 생성하고, main isolate가 그것을 출력하는 프로그램을 만드세요. import를 가장 먼저 놓고 `main` 전에 `worker`를 정의하세요. 프로그램은 `42`를 출력해야 합니다.

# --answers--

```dart
import 'dart:isolate';
```

```dart
void worker(SendPort port) {
```

```dart
  port.send(7 * 6);
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  final receivePort = ReceivePort();
```

```dart
  await Isolate.spawn(worker, receivePort.sendPort);
```

```dart
  print(await receivePort.first);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
void worker(SendPort port) {
  port.send(7 * 6);
}
Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

# --output--

42
