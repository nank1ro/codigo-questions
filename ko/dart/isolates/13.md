---
language: dart
exerciseType: 2
---

# --description--

`Isolate.run`에서 돌아오는 에러는 반대쪽에서 던져진 것의 **복사본**이므로, 평소의 확인 방법이 그대로 동작합니다. `catch (e)`는 객체를 주고, `e is FormatException`은 어떤 종류의 실패였는지 알려 줍니다.

```dart
try {
  await Isolate.run(() => int.parse('abc'));
} on FormatException {
  print('not a number');
}
```

의존할 수 없는 것은 자신의 isolate를 가리키는 스택 트레이스입니다. 에러는 이동했지만 스택은 그렇지 않습니다.

# --instructions--

파싱이 다른 isolate에서 실행되고 그 실패가 잡히도록 코드를 완성해서, 프로그램이 `failed`를 출력하게 하세요.

# --seed--

```dart
import 'dart:isolate';

Future<void> main() async {
  [/] {
    final n = await Isolate.[/](() => int.parse('abc'));
    print(n);
  } [/] (e) {
    print('failed');
  }
}
```

# --answers--

- try
- run
- catch
- spawn
- finally
- then
- await

# --solutions--

```dart
import 'dart:isolate';

Future<void> main() async {
  try {
    final n = await Isolate.run(() => int.parse('abc'));
    print(n);
  } catch (e) {
    print('failed');
  }
}
```

# --output--

failed
