---
language: dart
exerciseType: 1
---

# --description--

각 `Isolate.run`은 자신만의 isolate를 시작하므로, 그중 여러 개는 실제로 기계가 가진 코어 수만큼 같은 순간에 실행됩니다. 패턴은 여러분이 future에서 이미 알고 있는 것입니다. 모든 계산을 먼저 시작한 다음, `Future.wait`로 그 전부를 기다립니다. 결과는 입력의 순서를 유지합니다.

```dart
Future<List<int>> doubleAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * 2)));
}
```

isolate를 시작하는 것은 공짜가 아닙니다. 메모리와 몇 밀리초가 듭니다. 하나의 긴 계산을 몇 개의 isolate에 나누는 것은 값어치가 있지만, 천 개의 사소한 덧셈을 천 개의 isolate에 보내는 것은 그렇지 않습니다.

# --instructions--

`List<int>` `numbers`를 받아 각 숫자의 제곱으로, 같은 순서로 완료되는 `Future<List<int>>`를 반환하는 함수 `squareAll`을 작성하세요. 모든 제곱은 자신만의 isolate에서 계산합니다.

예시:
```dart
print(await squareAll([1, 2, 3])); // [1, 4, 9]
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`squareAll([1, 2, 3])`은 `[1, 4, 9]`로 완료되어야 합니다

```dart
  test('test1', () async {
    expect(await squareAll([1, 2, 3]), [1, 4, 9], reason: '--err-t1--');
  });
```

`squareAll([-4, 5])`는 입력의 순서를 유지하며 `[16, 25]`로 완료되어야 합니다

```dart
  test('test2', () async {
    expect(await squareAll([-4, 5]), [16, 25], reason: '--err-t2--');
  });
```

`squareAll([])`은 빈 리스트로 완료되어야 하고 `squareAll`은 `Future<List<int>>`를 반환해야 합니다

```dart
  test('test3', () async {
    expect(await squareAll(<int>[]), <int>[], reason: '--err-t3--');
    expect(squareAll([2]), isA<Future<List<int>>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<List<int>> squareAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * n)));
}
```
