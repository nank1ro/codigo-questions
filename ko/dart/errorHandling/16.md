---
language: dart
exerciseType: 1
---

# --description--

마지막 조각은 일부러 `Error`를 던지는 것입니다. 무엇을 받아들이는지 문서화한 함수는 다른 것을 큰 소리로 거부해야 하고, `ArgumentError`가 그 역할의 객체입니다:

```dart
int setVolume(int level) {
  if (level < 0 || level > 100) {
    throw ArgumentError('level must be between 0 and 100');
  }
  return level;
}
```

메시지는 `e.message`로 접근할 수 있고, `toString()`은 `Invalid argument(s): ` 뒤에 그것을 붙여 출력합니다.

이것은 앞서의 규칙과 모순되지 않습니다. `ArgumentError`를 던지는 것은 옳고, 그것을 잡는 것은 아닙니다: 그것은 *호출자의* 작성자에게 호출 자체가 잘못되었다고 말해 주는 것이고, 해결은 그 주위의 핸들러가 아니라 호출 전의 검사입니다.

# --instructions--

`int` 타입의 `age`를 받아 그것을 반환하지만, 나이가 `0` 미만이거나 `120` 초과이면 `ArgumentError('age must be between 0 and 120')`을 던지는 함수 `checkAge`를 작성하세요.

예:
```dart
print(checkAge(30)); // 30
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`checkAge(30)`은 `30`을 반환해야 하고, 범위의 양 끝도 받아들여져야 합니다

```dart
  test('test1', () {
    expect(checkAge(30), 30, reason: '--err-t1--');
    expect(checkAge(0), 0, reason: '--err-t1--');
    expect(checkAge(120), 120, reason: '--err-t1--');
  });
```

`checkAge(-1)`과 `checkAge(121)`은 `ArgumentError`를 던져야 합니다

```dart
  test('test2', () {
    expect(() => checkAge(-1), throwsArgumentError, reason: '--err-t2--');
    expect(() => checkAge(121), throwsArgumentError, reason: '--err-t2--');
  });
```

던져진 오류는 메시지 `'age must be between 0 and 120'`을 담아야 합니다

```dart
  test('test3', () {
    try {
      checkAge(200);
      fail('--err-t3--');
    } on ArgumentError catch (e) {
      expect(e.message, 'age must be between 0 and 120', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int checkAge(int age) {
  if (age < 0 || age > 120) {
    throw ArgumentError('age must be between 0 and 120');
  }
  return age;
}
```
