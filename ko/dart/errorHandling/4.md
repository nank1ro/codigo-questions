---
language: dart
exerciseType: 1
---

# --description--

하나의 `try` 블록 뒤에는 **여러 개의** 절을 둘 수 있고, 각각은 서로 다른 실패에서 복구합니다. Dart는 던져진 객체를 위에서부터 아래로 각 절과 비교하여, 일치하는 **첫 번째** 절을 실행합니다:

```dart
try {
  return names[int.parse(text)];
} on FormatException {
  return 'not a number';
} on RangeError {
  return 'out of range';
} catch (e) {
  return 'unknown problem';
}
```

존재하지 않는 인덱스로 리스트를 읽으면 **`RangeError`**가 던져지므로, 그 한 줄의 두 실패는 서로 다른 답을 받습니다.

첫 번째로 일치한 절이 이기므로 순서가 중요합니다: 더 구체적인 절 위에 일반적인 타입의 절을 놓으면 항상 그것이 이겨서 구체적인 절에는 도달할 수 없게 됩니다. 구체적인 절을 먼저 쓰고, 안전망을 원한다면 맨눈의 `catch`를 마지막에 쓰세요.

위의 `on RangeError` 절은 여러 절을 어떻게 나열하는지 보여 주기 위해서만 있는 것입니다. `RangeError`는 프로그램이 통제할 수 없는 상황이 아니라 코드의 실수를 알리는 것이고, 그런 실패는 잡는 대신 막아야 하는 이유는 뒤의 연습문제에서 설명합니다.

# --instructions--

`List<int>` 타입의 `scores`와 `String` 타입의 `text`를 받아, `text`가 나타내는 위치에 있는 `scores`의 요소를 `'score '` 뒤에 붙여 반환하는 함수 `lookup`을 작성하세요.

`text`가 정수가 아니면 `'not a number'`를, 위치가 존재하지 않으면 `'out of range'`를, 그 외의 경우에는 `'unknown problem'`을 반환하세요.

예:
```dart
print(lookup([10, 20, 30], '1')); // score 20
print(lookup([10, 20, 30], 'two')); // not a number
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`lookup([10, 20, 30], '1')`는 `'score 20'`을 반환해야 합니다

```dart
  test('test1', () {
    expect(lookup([10, 20, 30], '1'), 'score 20', reason: '--err-t1--');
    expect(lookup([10, 20, 30], '0'), 'score 10', reason: '--err-t1--');
  });
```

`lookup([10, 20, 30], 'two')`는 `'not a number'`를 반환해야 합니다

```dart
  test('test2', () {
    expect(lookup([10, 20, 30], 'two'), 'not a number', reason: '--err-t2--');
    expect(lookup([10, 20, 30], ''), 'not a number', reason: '--err-t2--');
  });
```

`lookup([10, 20, 30], '7')`은 `'out of range'`를 반환해야 합니다

```dart
  test('test3', () {
    expect(lookup([10, 20, 30], '7'), 'out of range', reason: '--err-t3--');
    expect(lookup([10, 20, 30], '-1'), 'out of range', reason: '--err-t3--');
    expect(lookup(<int>[], '0'), 'out of range', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String lookup(List<int> scores, String text) {
  try {
    return 'score ${scores[int.parse(text)]}';
  } on FormatException {
    return 'not a number';
  } on RangeError {
    return 'out of range';
  } catch (e) {
    return 'unknown problem';
  }
}
```
