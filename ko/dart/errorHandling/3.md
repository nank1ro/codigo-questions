---
language: dart
exerciseType: 2
---

# --description--

맨눈의 `catch`는 모든 것을 받아들이므로, 계획하지 못한 실패까지도 감춰 버립니다. 정확히 한 종류만 처리하려면 `on` 절에 그 타입의 이름을 씁니다:

```dart
try {
  return int.parse(text);
} on FormatException catch (e) {
  return -1;
}
```

텍스트가 정수가 아닐 때 `int.parse`는 **`FormatException`**을 던지므로, 입력을 읽을 때는 그 타입의 이름을 쓰는 것이 맞습니다. `on` 절은 그 타입과 그 하위 타입에만 일치하고 그 외의 것에는 아무것도 일치하지 않습니다: 다른 실패는 계속 바깥쪽으로 전파되어 여전히 모습을 드러내고, 그것을 위한 것이 아니었던 복구에 삼켜지지 않습니다.

# --instructions--

읽을 수 없는 텍스트가 `-1`을 내고 `FormatException`만 처리되도록 `readAge`를 완성하세요.

# --seed--

```dart
int readAge(String text) {
  try {
    return int.parse(text);
  } [/] FormatException [/] (e) {
    return -1;
  }
}

void main() {
  print(readAge('30'));
  print(readAge('old'));
}
```

# --answers--

- on
- catch
- try
- finally
- throw

# --solutions--

```dart
int readAge(String text) {
  try {
    return int.parse(text);
  } on FormatException catch (e) {
    return -1;
  }
}

void main() {
  print(readAge('30'));
  print(readAge('old'));
}
```

# --output--

30
-1
