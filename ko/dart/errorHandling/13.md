---
language: dart
exerciseType: 1
---

# --description--

`firstWhere`는 검사에 일치하는 첫 번째 요소를 반환합니다. 아무것도 일치하지 않으면 반환할 요소가 없으므로 `StateError`를 던집니다:

```dart
final words = ['a', 'fg'];
print(words.firstWhere((w) => w.length > 3)); // Bad state: No element
```

`int.tryParse`처럼 라이브러리는 출구를 제공합니다. 이름 있는 매개변수 `orElse`는 아무것도 일치하지 않았을 때 쓸 값을 만들어 내는 함수를 받습니다:

```dart
print(words.firstWhere((w) => w.length > 3, orElse: () => 'none')); // none
```

선택은 앞서와 같습니다: "아무것도 일치하지 않음"이 평범한 결과라면 `orElse`, 그것이 데이터가 망가졌다는 뜻이고 누군가 들어야 한다면 맨눈의 호출입니다.

# --instructions--

`List<String>` 타입의 `words`를 받는 두 함수를 작성하세요.

`firstLong`은 `firstWhere`를 `orElse`와 함께 써서 세 글자보다 긴 첫 번째 단어를 반환하고, 없으면 `'none'`을 반환합니다.

`firstLongStrict`는 `orElse` 없이 `firstWhere`로 같은 일을 하므로, 없을 때 `StateError`를 던집니다.

예:
```dart
print(firstLong(['a', 'bcde', 'fg'])); // bcde
print(firstLong(['a', 'fg'])); // none
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`firstLong(['a', 'bcde', 'fg'])`는 `'bcde'`를 반환해야 합니다

```dart
  test('test1', () {
    expect(firstLong(['a', 'bcde', 'fg']), 'bcde', reason: '--err-t1--');
    expect(firstLong(['long', 'longer']), 'long', reason: '--err-t1--');
  });
```

세 글자보다 긴 단어가 없을 때 `firstLong`은 `'none'`을 반환해야 합니다

```dart
  test('test2', () {
    expect(firstLong(['a', 'fg']), 'none', reason: '--err-t2--');
    expect(firstLong(<String>[]), 'none', reason: '--err-t2--');
    expect(firstLong(['abc']), 'none', reason: '--err-t2--');
  });
```

`firstLongStrict`는 단어가 있으면 그 단어를 반환하고, 없으면 `StateError`를 던져야 합니다

```dart
  test('test3', () {
    expect(firstLongStrict(['a', 'bcde']), 'bcde', reason: '--err-t3--');
    expect(() => firstLongStrict(['a', 'fg']), throwsStateError, reason: '--err-t3--');
    expect(() => firstLongStrict(<String>[]), throwsStateError, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String firstLong(List<String> words) {
  return words.firstWhere((w) => w.length > 3, orElse: () => 'none');
}

String firstLongStrict(List<String> words) {
  return words.firstWhere((w) => w.length > 3);
}
```
