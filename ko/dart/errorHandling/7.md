---
language: dart
exerciseType: 1
---

# --description--

여러분의 코드도 라이브러리와 똑같은 방식으로 예외를 던집니다. `Exception('message')`는 짧은 설명을 담은 평범한 예외를 만들고, `throw`가 그것을 보냅니다:

```dart
if (amount > balance) {
  throw Exception('insufficient funds');
}
```

메시지는 사라지지 않습니다: `toString()`은 `Exception`이라는 단어, 콜론, 메시지를 한데 모으는데, 이는 처리되지 않은 예외 보고서가 출력하는 것과 정확히 같습니다.

```dart
print(Exception('insufficient funds')); // Exception: insufficient funds
```

던지는 것은 `-1` 같은 만들어 낸 값을 반환하는 것보다 낫습니다: 호출자가 그것을 확인하는 것을 잊을 수 없고, 이유가 그 값과 함께 전달됩니다.

# --instructions--

두 `int` 값 `balance`와 `amount`를 받아 남은 잔액을 반환하는 함수 `withdraw`를 작성하세요.

`amount`가 `balance`보다 크면 그 대신 `Exception('insufficient funds')`를 던지세요.

예:
```dart
print(withdraw(100, 30)); // 70
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`withdraw(100, 30)`은 `70`을 반환해야 합니다

```dart
  test('test1', () {
    expect(withdraw(100, 30), 70, reason: '--err-t1--');
    expect(withdraw(50, 50), 0, reason: '--err-t1--');
  });
```

`withdraw(100, 300)`은 `Exception`을 던져야 합니다

```dart
  test('test2', () {
    expect(() => withdraw(100, 300), throwsA(isA<Exception>()), reason: '--err-t2--');
  });
```

던져진 예외는 `Exception: insufficient funds`로 출력되어야 합니다

```dart
  test('test3', () {
    try {
      withdraw(0, 1);
      fail('--err-t3--');
    } catch (e) {
      expect(e.toString(), 'Exception: insufficient funds', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int withdraw(int balance, int amount) {
  if (amount > balance) {
    throw Exception('insufficient funds');
  }
  return balance - amount;
}
```
