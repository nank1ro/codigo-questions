---
language: dart
exerciseType: 1
---

# --instructions--

`if-else` 문을 사용하여 `n`이 `2`로 나누어 떨어질 때 `'even'`을 반환하고, 그렇지 않으면 `'odd'`를 반환하는 함수 `parity(int n)`을 작성하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parity(4)`는 `'even'`을 반환해야 한다

```dart
  test('test1', () {
    expect(parity(4), 'even', reason: '--err-t1--');
  });
```

`parity(7)`는 `'odd'`를 반환해야 한다

```dart
  test('test2', () {
    expect(parity(7), 'odd', reason: '--err-t2--');
  });
```

`parity(0)`는 `'even'`을 반환해야 한다

```dart
  test('test3', () {
    expect(parity(0), 'even', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String parity(int n) {
  if (n % 2 == 0) {
    return 'even';
  } else {
    return 'odd';
  }
}

void main() {
  print(parity(4));
}
```
