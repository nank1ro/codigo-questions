---
language: dart
exerciseType: 1
---

# --instructions--

`isHot`이라는 최상위 `bool` 변수를 선언하세요. `if` 문을 사용하여 최상위 `int` 변수 `temp`(`35`로 설정됨)가 `30`보다 클 때 `isHot`에 `true`를 대입하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`temp`는 `35`여야 한다

```dart
  test('test1', () {
    expect(temp, 35, reason: '--err-t1--');
  });
```

`isHot`은 `true`여야 한다

```dart
  test('test2', () {
    expect(isHot, true, reason: '--err-t2--');
    expect(isHot, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int temp = 35;
bool isHot = false;

void main() {
  if (temp > 30) {
    isHot = true;
  }
  print(isHot);
}
```
