---
language: dart
exerciseType: 1
---

# --instructions--

`isHot`이라는 `bool` 변수를 선언하세요. `if` 문을 사용하여 `int` 변수 `temp`(`35`로 설정됨)가 `30`보다 클 때 `isHot`에 `true`를 대입하세요.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int temp = 35;
bool isHot = false;
```

# --after-seed--

```dart
  return isHot;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHot`은 `true`여야 한다

```dart
  test('test1', () {
    expect(result(), true, reason: '--err-t1--');
    expect(result(), TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int temp = 35;
bool isHot = false;
if (temp > 30) {
  isHot = true;
}
```
