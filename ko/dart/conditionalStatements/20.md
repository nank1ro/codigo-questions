---
language: dart
exerciseType: 1
---

# --instructions--

`if-else` 체인을 사용하여 `age < 13`일 때 `'child'`, `age < 18`일 때 `'teenager'`, `age < 65`일 때 `'adult'`, `age >= 65`일 때 `'senior'`를 반환하는 함수 `ageGroup(int age)`를 작성하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`ageGroup(8)`은 `'child'`를 반환해야 한다

```dart
  test('test1', () {
    expect(ageGroup(8), 'child', reason: '--err-t1--');
  });
```

`ageGroup(15)`는 `'teenager'`를 반환해야 한다

```dart
  test('test2', () {
    expect(ageGroup(15), 'teenager', reason: '--err-t2--');
  });
```

`ageGroup(30)`은 `'adult'`를 반환해야 한다

```dart
  test('test3', () {
    expect(ageGroup(30), 'adult', reason: '--err-t3--');
  });
```

`ageGroup(70)`은 `'senior'`를 반환해야 한다

```dart
  test('test4', () {
    expect(ageGroup(70), 'senior', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String ageGroup(int age) {
  if (age < 13) {
    return 'child';
  } else if (age < 18) {
    return 'teenager';
  } else if (age < 65) {
    return 'adult';
  } else {
    return 'senior';
  }
}

void main() {
  print(ageGroup(30));
}
```
