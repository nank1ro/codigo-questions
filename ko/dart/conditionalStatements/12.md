---
language: dart
exerciseType: 1
---

# --instructions--

`if-else`를 사용하여 `n`이 `0`보다 클 때 `'positive'`를 반환하고, 그렇지 않으면 `'non-positive'`를 반환하는 함수 `describe(int n)`을 작성하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`describe(10)`은 `'positive'`를 반환해야 한다

```dart
  test('test1', () {
    expect(describe(10), 'positive', reason: '--err-t1--');
  });
```

`describe(0)`은 `'non-positive'`를 반환해야 한다

```dart
  test('test2', () {
    expect(describe(0), 'non-positive', reason: '--err-t2--');
  });
```

`describe(-5)`은 `'non-positive'`를 반환해야 한다

```dart
  test('test3', () {
    expect(describe(-5), 'non-positive', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String describe(int n) {
  if (n > 0) {
    return 'positive';
  } else {
    return 'non-positive';
  }
}

void main() {
  print(describe(10));
}
```
