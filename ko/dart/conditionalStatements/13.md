---
language: dart
exerciseType: 1
---

# --instructions--

`else if` 체인을 사용하여 `score >= 90`일 때 `'A'`, `score >= 70`일 때 `'B'`, `score >= 50`일 때 `'C'`, 그렇지 않으면 `'F'`를 반환하는 함수 `classify(int score)`를 작성하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`classify(95)`는 `'A'`를 반환해야 한다

```dart
  test('test1', () {
    expect(classify(95), 'A', reason: '--err-t1--');
  });
```

`classify(75)`는 `'B'`를 반환해야 한다

```dart
  test('test2', () {
    expect(classify(75), 'B', reason: '--err-t2--');
  });
```

`classify(55)`는 `'C'`를 반환해야 한다

```dart
  test('test3', () {
    expect(classify(55), 'C', reason: '--err-t3--');
  });
```

`classify(30)`는 `'F'`를 반환해야 한다

```dart
  test('test4', () {
    expect(classify(30), 'F', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String classify(int score) {
  if (score >= 90) {
    return 'A';
  } else if (score >= 70) {
    return 'B';
  } else if (score >= 50) {
    return 'C';
  } else {
    return 'F';
  }
}

void main() {
  print(classify(95));
}
```
