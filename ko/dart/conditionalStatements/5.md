---
language: dart
exerciseType: 2
---

# --description--

**삼항 연산자** `condition ? expr1 : expr2`는 간단한 `if-else` 표현식을 간결하게 작성하는 방법입니다.

```dart
int x = 10;
String label = x > 0 ? 'positive' : 'non-positive';
print(label); // positive
```

조건이 `true`이면 `expr1`이 사용되고, 그렇지 않으면 `expr2`가 사용됩니다.

# --instructions--

`n`이 `2`로 나누어 떨어질 때 `parity`에 `'Even'`을 대입하고, 그렇지 않으면 `'Odd'`를 대입하도록 삼항 표현식을 완성하세요.

# --seed--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 [/] 'Even' [/] 'Odd';
  print(parity);
}
```

# --answers--

- ?
- :
- ==
- ;

# --solutions--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 ? 'Even' : 'Odd';
  print(parity);
}
```

# --output--

Even
