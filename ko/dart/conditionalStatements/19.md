---
language: dart
exerciseType: 1
---

# --instructions--

`else if` 체인을 사용하여 문자 등급을 반환하는 함수 `letterGrade(int score)`를 작성하세요: `score >= 90`일 때 `'A'`, `score >= 80`일 때 `'B'`, `score >= 70`일 때 `'C'`, `score >= 60`일 때 `'D'`, `60` 미만일 때 `'F'`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`letterGrade(92)`는 `'A'`를 반환해야 한다

```dart
  test('test1', () {
    expect(letterGrade(92), 'A', reason: '--err-t1--');
  });
```

`letterGrade(85)`는 `'B'`를 반환해야 한다

```dart
  test('test2', () {
    expect(letterGrade(85), 'B', reason: '--err-t2--');
  });
```

`letterGrade(73)`는 `'C'`를 반환해야 한다

```dart
  test('test3', () {
    expect(letterGrade(73), 'C', reason: '--err-t3--');
  });
```

`letterGrade(61)`는 `'D'`를 반환해야 한다

```dart
  test('test4', () {
    expect(letterGrade(61), 'D', reason: '--err-t4--');
  });
```

`letterGrade(45)`는 `'F'`를 반환해야 한다

```dart
  test('test5', () {
    expect(letterGrade(45), 'F', reason: '--err-t5--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String letterGrade(int score) {
  if (score >= 90) {
    return 'A';
  } else if (score >= 80) {
    return 'B';
  } else if (score >= 70) {
    return 'C';
  } else if (score >= 60) {
    return 'D';
  } else {
    return 'F';
  }
}

void main() {
  print(letterGrade(85));
}
```
