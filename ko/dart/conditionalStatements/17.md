---
language: dart
exerciseType: 1
---

# --instructions--

`default` 절이 있는 `switch` 문을 사용하여 `'green'`에는 `'Go'`, `'yellow'`에는 `'Slow down'`, `'red'`에는 `'Stop'`, 그 외의 값에는 `'Invalid'`를 반환하는 함수 `trafficLight(String color)`를 작성하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`trafficLight('green')`은 `'Go'`를 반환해야 한다

```dart
  test('test1', () {
    expect(trafficLight('green'), 'Go', reason: '--err-t1--');
  });
```

`trafficLight('yellow')`은 `'Slow down'`을 반환해야 한다

```dart
  test('test2', () {
    expect(trafficLight('yellow'), 'Slow down', reason: '--err-t2--');
  });
```

`trafficLight('red')`은 `'Stop'`을 반환해야 한다

```dart
  test('test3', () {
    expect(trafficLight('red'), 'Stop', reason: '--err-t3--');
  });
```

`trafficLight('blue')`은 `'Invalid'`를 반환해야 한다

```dart
  test('test4', () {
    expect(trafficLight('blue'), 'Invalid', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String trafficLight(String color) {
  switch (color) {
    case 'green':
      return 'Go';
    case 'yellow':
      return 'Slow down';
    case 'red':
      return 'Stop';
    default:
      return 'Invalid';
  }
}

void main() {
  print(trafficLight('green'));
}
```
