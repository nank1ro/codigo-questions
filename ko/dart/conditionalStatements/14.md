---
language: dart
exerciseType: 1
---

# --instructions--

중첩된 `if` 문을 사용하여 `age >= 18` **이고** `isCitizen`이 `true`일 때만 `true`를 반환하는 함수 `canVote(int age, bool isCitizen)`를 작성하세요.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canVote(20, true)`는 `true`를 반환해야 한다

```dart
  test('test1', () {
    expect(canVote(20, true), true, reason: '--err-t1--');
  });
```

`canVote(16, true)`는 `false`를 반환해야 한다

```dart
  test('test2', () {
    expect(canVote(16, true), false, reason: '--err-t2--');
  });
```

`canVote(25, false)`는 `false`를 반환해야 한다

```dart
  test('test3', () {
    expect(canVote(25, false), false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool canVote(int age, bool isCitizen) {
  if (age >= 18) {
    if (isCitizen) {
      return true;
    }
  }
  return false;
}

void main() {
  print(canVote(20, true));
}
```
