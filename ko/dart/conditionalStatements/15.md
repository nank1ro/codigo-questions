---
language: dart
exerciseType: 1
---

# --instructions--

삼항 연산자를 사용하여 `n`의 절댓값을 반환하는 함수 `absoluteValue(int n)`을 작성하세요. `n`이 음수가 아닐 때 `n`을 반환하고, 그렇지 않으면 `-n`을 반환해야 합니다.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`absoluteValue(7)`는 `7`을 반환해야 한다

```dart
  test('test1', () {
    expect(absoluteValue(7), 7, reason: '--err-t1--');
  });
```

`absoluteValue(-4)`는 `4`를 반환해야 한다

```dart
  test('test2', () {
    expect(absoluteValue(-4), 4, reason: '--err-t2--');
  });
```

`absoluteValue(0)`는 `0`을 반환해야 한다

```dart
  test('test3', () {
    expect(absoluteValue(0), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int absoluteValue(int n) {
  return n >= 0 ? n : -n;
}

void main() {
  print(absoluteValue(-4));
}
```
