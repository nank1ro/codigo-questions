---
language: dart
exerciseType: 1
---

# --description--

경계는 타입 매개변수 자신을 언급할 수도 있습니다. `Comparable<T>`는 `compareTo`를 통해 자신과 같은 종류와 자신을 비교하는 방법을 아는 모든 것의 인터페이스입니다:

```dart
print('fig'.compareTo('kiwi')); // negative: fig comes first
print('kiwi'.compareTo('fig')); // positive
print('fig'.compareTo('fig'));  // zero
```

따라서 `T extends Comparable<T>`는 "자기 자신과 비교될 수 있는 어떤 타입"으로 읽으며, 이것이 정렬이나 최댓값 함수에 정확히 필요한 것입니다:

```dart
T maxOf<T extends Comparable<T>>(T a, T b) => a.compareTo(b) >= 0 ? a : b;

print(maxOf('fig', 'kiwi')); // kiwi
```

`String`과 `DateTime`은 둘 다 이를 직접 만족합니다. `int`와 `double`은 `Comparable<num>`을 구현하므로, 숫자 리스트는 단순히 `num`으로 비교됩니다.

# --instructions--

`Comparable<T>`로 경계 지어진 타입 매개변수 `T`를 가지고, 비어 있지 않은 `List<T>` 타입의 `items`를 받아 `compareTo`로 비교하여 가장 큰 요소를 반환하는 제네릭 함수 `largest`를 작성하세요.

예:
```dart
print(largest(['fig', 'kiwi', 'apple'])); // kiwi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest(['fig', 'kiwi', 'apple'])`는 `'kiwi'`를 반환해야 합니다

```dart
  test('test1', () {
    expect(largest(['fig', 'kiwi', 'apple']), 'kiwi', reason: '--err-t1--');
  });
```

`largest`는 숫자와 날짜로도 동작해야 합니다

```dart
  test('test2', () {
    expect(largest([3, 9, 5]), 9, reason: '--err-t2--');
    expect(
      largest([DateTime(2020), DateTime(2024), DateTime(2001)]),
      DateTime(2024),
      reason: '--err-t2--',
    );
  });
```

`largest`는 요소가 하나뿐인 리스트의 유일한 요소를 반환해야 하고, 마지막 요소가 가장 클 때도 가장 큰 요소를 반환해야 하며, `largest<String>(['a', 'z'])`처럼 명시적인 타입 인수도 받아들여야 합니다

```dart
  test('test3', () {
    expect(largest<String>(['a', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['only']), 'only', reason: '--err-t3--');
    expect(largest(['a', 'b', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['z', 'b', 'a']), 'z', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
T largest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) > 0) {
      best = item;
    }
  }
  return best;
}
```
