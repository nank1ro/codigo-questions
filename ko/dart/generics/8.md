---
language: dart
exerciseType: 2
---

# --description--

견고한 널 안전성에서 물음표는 서로 다른 두 곳에 위치할 수 있고, 위치마다 의미가 다릅니다:

```dart
Box<int?> a = Box(null); // a box that exists and holds a nullable int
Box<int>? b = null;      // no box at all, but if there is one it holds an int
```

`Box<int?>`에서는 **타입 인수**가 널 가능하므로 `a.value`의 타입은 `int?`이고 `null`일 수 있으며, `a` 자체는 항상 존재합니다. `Box<int>?`에서는 **변수**가 널 가능하므로 `b`는 `null`일 수 있고, 안의 값에 접근하려면 `b?.value`나 `b!.value`가 필요합니다.

일반 `T`는 `T extends Object?`를 의미하므로, `Box<int?>`처럼 널 가능한 타입 인수도 완전히 합법적입니다.

# --instructions--

`holdsNull`이 `null`을 담는 박스가 되고, `missing`이 아무 박스도 담지 않을 수 있는 변수가 되도록 코드를 완성하세요.

# --seed--

```dart
class Box<T> {
  final T value;
  Box(this.value);
}

void main() {
  final holdsNull = Box<[/]>(null);
  Box<int>[/] missing = null;
  print('value: ${holdsNull.value}');
  print('box: $missing');
}
```

# --answers--

- int?
- ?
- int
- !
- ??

# --solutions--

```dart
class Box<T> {
  final T value;
  Box(this.value);
}

void main() {
  final holdsNull = Box<int?>(null);
  Box<int>? missing = null;
  print('value: ${holdsNull.value}');
  print('box: $missing');
}
```

# --output--

value: null
box: null
