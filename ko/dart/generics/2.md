---
language: dart
exerciseType: 1
---

# --description--

`List`만 제네릭 컬렉션인 것은 아닙니다. `Set`은 타입 인수를 하나 받고, `Map`은 **두 개**를 받습니다: 하나는 키용, 하나는 값용이며 그 순서대로입니다.

```dart
Set<String> tags = {'new', 'sale'};
Map<String, int> ages = {'Ada': 36, 'Grace': 45};
```

빈 컬렉션 리터럴은 내용물에서 타입을 알아낼 수 없으므로, 리터럴 자체에 타입 인수를 씁니다:

```dart
final counts = <String, int>{};
final seen = <String>{};
counts['fig'] = 3;
```

타입이 한번 알려지면 컬렉션에서 꺼내는 모든 값은 이미 올바른 타입을 가집니다: `ages['Ada']`는 `int?`이지 알 수 없는 값이 아닙니다.

# --instructions--

`List<String>` 타입의 `words`를 받아 모든 단어가 키이고 그 길이가 값인 `Map<String, int>`를 반환하는 함수 `lengths`를 작성하세요.

예:
```dart
print(lengths(['fig', 'kiwi'])); // {fig: 3, kiwi: 4}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`lengths(['fig', 'kiwi'])`는 `{fig: 3, kiwi: 4}`를 반환해야 합니다

```dart
  test('test1', () {
    expect(lengths(['fig', 'kiwi']), {'fig': 3, 'kiwi': 4}, reason: '--err-t1--');
  });
```

`lengths`는 `Map<String, int>`를 반환해야 합니다

```dart
  test('test2', () {
    expect(lengths(['plum']), isA<Map<String, int>>(), reason: '--err-t2--');
    expect(lengths(['plum']), {'plum': 4}, reason: '--err-t2--');
  });
```

`lengths`는 빈 리스트에 대해 빈 맵을 반환해야 합니다

```dart
  test('test3', () {
    expect(lengths(<String>[]), <String, int>{}, reason: '--err-t3--');
    expect(lengths(['a', 'bb', 'ccc']), {'a': 1, 'bb': 2, 'ccc': 3}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Map<String, int> lengths(List<String> words) {
  final result = <String, int>{};
  for (final word in words) {
    result[word] = word.length;
  }
  return result;
}
```
