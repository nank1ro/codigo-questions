---
language: dart
exerciseType: 1
---

# --description--

`typedef`는 타입에 이름을 붙이며, 자체 타입 매개변수를 가질 수도 있습니다. 보통의 이유는 매번 풀어 쓰는 대신 함수 타입의 계열에 한 번만 이름을 붙이기 위함입니다:

```dart
typedef Transform<I, O> = O Function(I input);

final Transform<String, int> length = (word) => word.length;
print(length('kiwi')); // 4
```

`Transform<String, int>`는 `int Function(String)`을 쓰는 또 다른 방법일 뿐이므로 둘은 서로 바꿔 쓸 수 있습니다. 얻는 것은 가독성입니다: `Transform<I, O> transform`으로 선언된 매개변수는 함수의 용도를 말해주지만, `O Function(I)`는 생김새만 말해줍니다.

제네릭 typedef와 제네릭 함수는 자연스럽게 결합됩니다. 함수 자신의 타입 매개변수가 typedef의 타입 매개변수를 채웁니다.

# --instructions--

`I`를 받아 `O`를 반환하는 함수에 이름을 붙이는, 타입 매개변수 `I`와 `O`를 가진 제네릭 typedef `Transform`을 선언하세요.

그다음, 타입 매개변수 `I`와 `O`를 가지고 `List<I>` 타입의 `items`와 `Transform<I, O>` 타입의 `transform`을 받아 변환된 모든 항목을 순서대로 담는 `List<O>`를 반환하는 제네릭 함수 `convertAll`을 작성하세요.

예:
```dart
print(convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)); // [3, 4]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)`는 `[3, 4]`를 반환해야 합니다

```dart
  test('test1', () {
    expect(
      convertAll<String, int>(['fig', 'kiwi'], (w) => w.length),
      [3, 4],
      reason: '--err-t1--',
    );
  });
```

`convertAll`은 `List<O>`를 반환해야 하므로, int를 문자열로 변환하면 `List<String>`이 되어야 합니다

```dart
  test('test2', () {
    final result = convertAll<int, String>([1, 2], (n) => 'n$n');
    expect(result, ['n1', 'n2'], reason: '--err-t2--');
    expect(result, isA<List<String>>(), reason: '--err-t2--');
  });
```

typedef `Transform`은 함수 타입에 이름을 붙여야 하고, 빈 리스트는 빈 리스트를 반환해야 합니다

```dart
  test('test3', () {
    final Transform<int, int> twice = (n) => n * 2;
    expect(convertAll<int, int>([1, 2, 3], twice), [2, 4, 6], reason: '--err-t3--');
    expect(convertAll<String, int>(<String>[], (w) => w.length), <int>[], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
typedef Transform<I, O> = O Function(I input);

List<O> convertAll<I, O>(List<I> items, Transform<I, O> transform) {
  return items.map(transform).toList();
}
```
