---
language: dart
exerciseType: 1
---

# --description--

경계가 없는 `T`는 무엇이든 될 수 있으므로, 본문 안에서는 모든 객체가 가진 것만 사용할 수 있습니다. 다음은 컴파일되지 않습니다:

```dart
T twice<T>(T value) => value + value; // error: + is not defined for T
```

**경계(bound)**가 이를 해결합니다. `T extends num`은 "`T`는 숫자만 될 수 있다"라고 말하고, 그 대가로 본문은 `num`이 제공하는 모든 것을 사용할 수 있습니다:

```dart
T twice<T extends num>(T value) => (value + value) as T;

num half<T extends num>(T value) => value / 2;
```

경계는 호출 지점에서 검사됩니다: `half(4)`와 `half(2.5)`는 괜찮지만, `half('fig')`는 컴파일 시점 오류입니다. 경계는 양방향의 약속입니다. 더 좁은 인수를 주는 대신 그 안에서 더 많은 기능을 얻습니다.

# --instructions--

`num`으로 경계 지어진 타입 매개변수 `T`를 가지고, `List<T>` 타입의 `values`를 받아 요소들의 합을 `num`으로 반환하는 제네릭 함수 `total`을 작성하세요. 빈 리스트는 `0`을 반환합니다.

예:
```dart
print(total([1, 2, 3]));  // 6
print(total([1.5, 2.5])); // 4.0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total([1, 2, 3])`은 `6`을 반환해야 합니다

```dart
  test('test1', () {
    expect(total([1, 2, 3]), 6, reason: '--err-t1--');
  });
```

`total`은 double로도 동작해야 하므로 `total([1.5, 2.5])`는 `4.0`을 반환해야 하고, `total<int>([1, 2])`처럼 명시적인 타입 인수도 받아들여야 합니다

```dart
  test('test2', () {
    expect(total([1.5, 2.5]), 4.0, reason: '--err-t2--');
    expect(total([10, 20, 30, 40]), 100, reason: '--err-t2--');
    expect(total<int>([1, 2]), 3, reason: '--err-t2--');
    expect(total<double>([1.5, 1.5]), 3.0, reason: '--err-t2--');
  });
```

`total`은 빈 리스트에 대해 `0`을 반환해야 합니다

```dart
  test('test3', () {
    expect(total(<int>[]), 0, reason: '--err-t3--');
    expect(total(<double>[]), 0, reason: '--err-t3--');
    expect(total([-5, 5, 7]), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
num total<T extends num>(List<T> values) {
  num sum = 0;
  for (final value in values) {
    sum += value;
  }
  return sum;
}
```
