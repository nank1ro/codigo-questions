---
language: dart
exerciseType: 1
---

# --instructions--

**기본값**이 `"World"`인 `String` 매개변수 `name`을 받아 문자열 `"Hello, <name>!"`을 반환하는 함수 `greetUser`를 작성하세요.

예시:
```dart
print(greetUser());          // Hello, World!
print(greetUser("Alice"));   // Hello, Alice!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greetUser()`는 `"Hello, World!"`를 반환해야 합니다
```dart
  test('test1', () {
    expect(greetUser(), "Hello, World!", reason: '--err-t1--');
  });
```

`greetUser("Alice")`는 `"Hello, Alice!"`를 반환해야 합니다
```dart
  test('test2', () {
    expect(greetUser("Alice"), "Hello, Alice!", reason: '--err-t2--');
  });
```

`greetUser("Bob")`는 `"Hello, Bob!"`를 반환해야 합니다
```dart
  test('test3', () {
    expect(greetUser("Bob"), "Hello, Bob!", reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String greetUser([String name = "World"]) {
  return "Hello, $name!";
}
```
