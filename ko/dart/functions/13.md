---
language: dart
exerciseType: 1
---

# --instructions--

`int` 매개변수 `n`을 받아 `1`부터 `n`까지의 정수(포함)를 담은 `List<int>`를 반환하는 함수 `range`를 작성하세요.

예시:
```dart
print(range(3)); // [1, 2, 3]
print(range(5)); // [1, 2, 3, 4, 5]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`range(3)`은 `[1, 2, 3]`을 반환해야 합니다
```dart
  test('test1', () {
    expect(range(3), [1, 2, 3], reason: '--err-t1--');
  });
```

`range(5)`는 `[1, 2, 3, 4, 5]`를 반환해야 합니다
```dart
  test('test2', () {
    expect(range(5), [1, 2, 3, 4, 5], reason: '--err-t2--');
  });
```

`range(1)`은 `[1]`을 반환해야 합니다
```dart
  test('test3', () {
    expect(range(1), [1], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> range(int n) {
  List<int> result = [];
  for (int i = 1; i <= n; i++) {
    result.add(i);
  }
  return result;
}
```
