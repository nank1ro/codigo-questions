---
language: dart
exerciseType: 1
---

# --instructions--

`int` 매개변수 `n`을 받아 `n`이 짝수이면 `true`, 홀수이면 `false`를 반환하는 함수 `isEven`을 작성하세요.

예시:
```dart
print(isEven(4));  // true
print(isEven(7));  // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isEven(4)`는 `true`를 반환해야 합니다
```dart
  test('test1', () {
    expect(isEven(4), true, reason: '--err-t1--');
  });
```

`isEven(7)`은 `false`를 반환해야 합니다
```dart
  test('test2', () {
    expect(isEven(7), false, reason: '--err-t2--');
  });
```

`isEven(0)`은 `true`를 반환해야 합니다
```dart
  test('test3', () {
    expect(isEven(0), true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isEven(int n) {
  return n % 2 == 0;
}
```
