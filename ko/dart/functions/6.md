---
language: dart
exerciseType: 3
---

# --instructions--

Dart 함수에서 `void` 반환 타입은 무엇을 의미합니까?

```dart
void sayHi() {
  print("Hi!");
}
```

# --answers--

- 함수가 숫자 0을 반환한다
- 함수가 아무 값도 반환하지 않는다
- 함수가 빈 문자열을 반환한다
- 함수가 모든 타입을 반환할 수 있다

# --solutions--

- 함수가 아무 값도 반환하지 않는다
