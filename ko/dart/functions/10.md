---
language: dart
exerciseType: 1
---

# --instructions--

`int` 매개변수 `n`을 받아 `n * n`을 반환하는 **화살표 함수** `square`를 작성하세요.

`=>` 구문을 사용하세요 (중괄호 없음, `return` 키워드 없음).

예시:
```dart
print(square(4));  // 16
print(square(5));  // 25
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`square(4)`는 `16`을 반환해야 합니다
```dart
  test('test1', () {
    expect(square(4), 16, reason: '--err-t1--');
  });
```

`square(5)`는 `25`를 반환해야 합니다
```dart
  test('test2', () {
    expect(square(5), 25, reason: '--err-t2--');
  });
```

`square(0)`은 `0`을 반환해야 합니다
```dart
  test('test3', () {
    expect(square(0), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int square(int n) => n * n;
```
