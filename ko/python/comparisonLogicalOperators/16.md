---
language: python
exerciseType: 1
---

# --description--

`any(iterable)`은 최소한 하나의 요소가 참이면 `True`를 반환하고 `all(iterable)`은 모든 요소가 참이면 `True`를 반환합니다 (`all([])`은 `True`입니다). 제너레이터 표현식과 결합하면 컬렉션 전체에 대한 조건을 표현할 수 있으며, `or`와 `and`처럼 단락 평가를 합니다:
```python
print(any(ch.isdigit() for ch in "abc1"))  # True
print(all(ch.isalpha() for ch in "ab1"))   # False
```

# --instructions--

문자열의 길이가 `8`자 이상 `20`자 이하이고 숫자를 최소 하나 포함하며 공백을 포함하지 않을 때만 `True`를 반환하고 그 외에는 `False`를 반환하는 함수 `is_valid_password(password)`를 작성하세요

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`"secret123"`은 유효하며 정확히 `8`자 또는 `20`자인 비밀번호도 유효합니다

```python
    def test_1(self):
        self.assertIs(is_valid_password("secret123"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefg1"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefghij123456789x"), True, "--err-t1--")
```

너무 짧거나 너무 긴 비밀번호는 유효하지 않습니다

```python
    def test_2(self):
        self.assertIs(is_valid_password("short1"), False, "--err-t2--")
        self.assertIs(is_valid_password("abcdefghij1234567890x"), False, "--err-t2--")
```

숫자가 없거나 공백이 있는 비밀번호는 유효하지 않습니다

```python
    def test_3(self):
        self.assertIs(is_valid_password("nodigitshere"), False, "--err-t3--")
        self.assertIs(is_valid_password("pass word1"), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_valid_password(password):
    return (
        8 <= len(password) <= 20
        and any(ch.isdigit() for ch in password)
        and " " not in password
    )
```
