---
language: python
exerciseType: 3
---

# --instructions--

이 코드는 무엇을 출력하나요?
```python
a = [1, 2]
b = [1, 2]
c = a
c.append(3)
print(a == b, a is c, a)
```

# --answers--

- `False True [1, 2, 3]`, `c`는 `a`와 같은 리스트의 다른 이름이므로 `c`를 통해 추가하면 `a`도 바뀌어 `b`와 달라지기 때문입니다
- `True True [1, 2]`, `c = a`가 리스트를 복사하므로 `a`는 변하지 않기 때문입니다
- `True False [1, 2, 3]`, `is`가 값을 비교하고 `==`가 정체성을 비교하기 때문입니다
- `False False [1, 2, 3]`, `append`가 새 리스트를 만들어 `c`가 더 이상 `a`를 가리키지 않기 때문입니다

# --solutions--

- `False True [1, 2, 3]`, `c`는 `a`와 같은 리스트의 다른 이름이므로 `c`를 통해 추가하면 `a`도 바뀌어 `b`와 달라지기 때문입니다
