---
language: python
exerciseType: 4
---

# --description--

논리 연산자는 **단락 평가**를 합니다. 결과가 이미 정해지므로 `and`는 피연산자가 거짓이 되는 즉시, `or`는 참이 되는 즉시 멈춥니다. 남은 피연산자는 평가되지 않으므로 함수 호출이라면 실행되지 않습니다:
```python
def check(n):
    print("checking", n)
    return n > 0

check(-1) and check(5)  # prints only "checking -1"
check(2) or check(-3)   # prints only "checking 2"
```

# --instructions--

항목을 정렬해 `is_positive`를 정의하고 `and`로 `first`를, 이어서 `or`로 `second`를 계산한 뒤 두 결과를 출력해 출력 `checking -1`, `checking 2`, `False True`가 나오도록 하세요

# --answers--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
```

```python
first = is_positive(-1) and is_positive(5)
```

```python
second = is_positive(2) or is_positive(-3)
```

```python
print(first, second)
```

# --solutions--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
first = is_positive(-1) and is_positive(5)
second = is_positive(2) or is_positive(-3)
print(first, second)
```

# --output--

checking -1
checking 2
False True
