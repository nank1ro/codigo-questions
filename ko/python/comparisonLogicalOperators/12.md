---
language: python
exerciseType: 2
---

# --description--

`in` 연산자는 **포함 여부**를 확인합니다. 어떤 요소가 리스트, 튜플, 집합에 있는지, 부분 문자열이 문자열에 있는지, 키가 딕셔너리에 있는지를 확인합니다. `not in`은 그 부정입니다:
```python
fruits = ["apple", "pear"]
print("pear" in fruits)          # True
print("kiwi" not in fruits)      # True
print("ex" in "text")            # True
print("age" in {"name": "Ada"})  # False, only keys are checked
```
둘 다 불리언을 반환하고 영어처럼 읽히므로, 반복문을 쓰는 대신 포함 여부를 확인하는 선호되는 방법입니다.

# --instructions--

`"pear"`가 리스트에 있는지, `"py"`가 `"happy"`의 부분 문자열인지, `"kiwi"`가 리스트에 없는지를 확인해 출력 `True True`, `True`가 나오도록 코드를 완성하세요

# --seed--

```python
fruits = ["apple", "pear"]
print("pear" [/] fruits, "py" [/] "happy")
print("kiwi" [/] [/] fruits)
```

# --answers--

- in
- in
- in
- not
- ==
- and

# --solutions--

```python
fruits = ["apple", "pear"]
print("pear" in fruits, "py" in "happy")
print("kiwi" not in fruits)
```

# --output--

True True
True
