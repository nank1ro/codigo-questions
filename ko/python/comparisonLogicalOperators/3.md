---
language: python
exerciseType: 2
---

# --description--

비교는 **연결**할 수 있습니다. `1 < x < 10`은 `x`가 `1`보다 크고 **그리고** `10`보다 작은지를 확인하며, `1 < x and x < 10`과 정확히 같지만 `x`는 한 번만 평가됩니다:
```python
x = 5
print(1 < x < 10)   # True
print(1 < x <= 5)   # True
print(10 < x < 20)  # False
```
어떤 비교 연산자든 연결할 수 있고 각 연산자는 양옆의 두 값에 적용됩니다. `a < b == c`는 `a < b and b == c`를 뜻합니다. 연결을 범위 `low < x < high`처럼 읽는 것이 가장 흔한 사용법입니다.

# --instructions--

`x`가 `1`과 `10` 사이에 엄격히 있는지 확인해 출력 `True`, `False`가 나오도록 연결 비교를 완성하세요

# --seed--

```python
x = 5
print(1 [/] x [/] 10)
print(10 < x < 20)
```

# --answers--

- <
- <
- >
- >=
- ==

# --solutions--

```python
x = 5
print(1 < x < 10)
print(10 < x < 20)
```

# --output--

True
False
