---
language: python
exerciseType: 2
---

# --description--

Python의 모든 값에는 그것이 어떤 종류의 데이터이고 무엇을 할 수 있는지 알려주는 **타입**이 있습니다.
기본 내장 타입은 다음과 같습니다:
- `int`, `42`나 `-3` 같은 정수
- `float`, `3.5` 같은 소수 부분이 있는 수
- `str`, `"hello"` 같은 텍스트
- `bool`, `True`와 `False` 두 값 중 하나
- `NoneType`, "값 없음"을 뜻하는 특별한 값 `None`의 타입

내장 함수 `type()`은 값의 타입을 반환합니다. 이를 출력하면 클래스 이름이 표시됩니다:
```python
print(type(42))     # <class 'int'>
print(type("hi"))   # <class 'str'>
```
`NoneType`을 직접 쓸 일은 없습니다: `type(None)`이 그것을 반환하지만, 그 이름은 다른 네 가지와 달리 내장 이름이 아닙니다.

# --instructions--

`int`, `float`, `str`, `bool` 타입을 이 순서대로 출력하도록 코드를 완성하세요

# --seed--

```python
print(type([/]))
print(type([/]))
print(type([/]))
print(type([/]))
```

# --answers--

- 42
- 3.5
- "hi"
- True
- None
- int

# --solutions--

```python
print(type(42))
print(type(3.5))
print(type("hi"))
print(type(True))
```

# --output--

<class 'int'>
<class 'float'>
<class 'str'>
<class 'bool'>
