---
language: python
exerciseType: 2
---

# --description--

`re.sub`에 주는 치환 값은 **함수**일 수도 있습니다. 이 함수는 매치마다 한 번씩 호출되어 매치 객체를 받고, 그 자리에 넣을 문자열을 반환해야 합니다. 이렇게 하면 치환 결과가 매치된 내용에 따라 달라질 수 있습니다:
```python
import re

def shout(match):
    return match.group().upper()

print(re.sub(r"[a-z]+", shout, "one two"))  # ONE TWO
```
함수는 괄호 없이 이름으로 전달합니다. `shout(match)`라고 쓰면 `re.sub`에 건네주는 대신 즉시 호출해 버립니다.

# --instructions--

텍스트에 있는 모든 숫자가 그 두 배 값으로 치환되도록 코드를 완성하여 `a2 b44`가 출력되게 하세요

# --seed--

```python
import re

def double(match):
    return str(int(match.group()) * 2)

print(re.[/](r"\d+", [/], "a1 b22"))
```

# --answers--

- sub
- double
- findall
- double(match)
- split

# --solutions--

```python
import re

def double(match):
    return str(int(match.group()) * 2)

print(re.sub(r"\d+", double, "a1 b22"))
```

# --output--

a2 b44
