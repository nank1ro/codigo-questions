---
language: python
exerciseType: 1
---

# --description--

모든 매치의 위치나 그룹이 필요할 때는 `re.finditer(pattern, text)`가 알맞은 선택입니다. 텍스트를 훑으면서 매치마다 **매치 객체**를 하나씩 내어 줍니다:
```python
import re

for match in re.finditer(r"\d+", "a1 bb22"):
    print(match.group(), match.start())
# 1 1
# 22 5
```
`finditer`는 리스트가 아니라 이터레이터를 만들므로 `for` 반복문이나 컴프리헨션에서 쓸 수 있습니다. `findall`이 텍스트만 주는 반면, `finditer`는 매치 객체가 아는 모든 것을 줍니다.

# --instructions--

텍스트에 있는 각 글자 묶음마다 단어와 그 단어가 시작하는 인덱스를 담은 튜플의 리스트를 반환하는 함수 `word_positions(text)`를 작성하세요

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`word_positions("hi there")`는 `[("hi", 0), ("there", 3)]`을 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(word_positions("hi there"), [("hi", 0), ("there", 3)], "--err-t1--")
```

숫자와 기호는 단어의 일부가 되면 안 됩니다

```python
    def test_2(self):
        self.assertEqual(word_positions("a1b"), [("a", 0), ("b", 2)], "--err-t2--")
        self.assertEqual(word_positions("Ada, Bob"), [("Ada", 0), ("Bob", 5)], "--err-t2--")
```

글자가 없는 텍스트는 빈 리스트를 반환해야 합니다

```python
    def test_3(self):
        self.assertEqual(word_positions("123 456"), [], "--err-t3--")
        self.assertEqual(word_positions(""), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def word_positions(text):
    return [(match.group(), match.start()) for match in re.finditer(r"[a-zA-Z]+", text)]
```
