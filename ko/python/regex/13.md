---
language: python
exerciseType: 1
---

# --description--

`str.split`은 고정된 구분자로만 자를 수 있습니다. `re.split(pattern, text)`는 패턴이 설명하는 무엇으로든 자를 수 있는데, 지저분한 입력에는 보통 이런 것이 필요합니다:
```python
import re

print("a, b;c".split(","))            # ['a', ' b;c']
print(re.split(r"[,;]", "a, b;c"))    # ['a', ' b', 'c']
print(re.split(r"[,;\s]+", "a, b;c")) # ['a', 'b', 'c']
```
구분자를 `[,;\s]+`로 쓰면 쉼표, 세미콜론, 공백이 연달아 나오는 묶음 전체가 한 번의 자르기로 취급되어, 그 사이에 빈 문자열이 남지 않습니다.

`maxsplit` 인자는 정해진 횟수만큼 자른 뒤 멈추고 나머지 텍스트를 마지막 요소에 남깁니다. `re.split(r"\s+", "a b c", maxsplit=1)`은 `['a', 'b c']`를 줍니다.

# --instructions--

쉼표, 세미콜론, 공백이 연달아 나오는 묶음을 하나의 구분자로 취급하여 줄에 있는 필드들의 리스트를 반환하는 함수 `split_fields(line)`를 작성하세요

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`split_fields`는 쉼표, 세미콜론, 공백에서 잘라야 합니다

```python
    def test_1(self):
        self.assertEqual(split_fields("a, b;c"), ["a", "b", "c"], "--err-t1--")
        self.assertEqual(split_fields("red green blue"), ["red", "green", "blue"], "--err-t1--")
```

연달아 나오는 구분자는 한 번의 자르기만 만들어야 합니다

```python
    def test_2(self):
        self.assertEqual(split_fields("x ; ; y"), ["x", "y"], "--err-t2--")
        self.assertEqual(split_fields("1,,2"), ["1", "2"], "--err-t2--")
```

구분자가 없는 줄은 필드 하나만 반환해야 합니다

```python
    def test_3(self):
        self.assertEqual(split_fields("solo"), ["solo"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def split_fields(line):
    return re.split(r"[,;\s]+", line)
```
