---
language: python
exerciseType: 1
---

# --description--

`re.search`는 텍스트 전체를 훑지만, `re.match`는 패턴을 **맨 앞**에서만 시도합니다:
```python
import re

print(re.search(r"\d+", "order 42"))  # <re.Match object; span=(6, 8), match='42'>
print(re.match(r"\d+", "order 42"))   # None
print(re.match(r"\d+", "42 orders"))  # <re.Match object; span=(0, 2), match='42'>
```

둘 다 매치되는 것이 없으면 `None`을 반환하고, 매치 객체는 언제나 참으로 평가되므로, "매치되었는가?"를 묻는 일반적인 방법은 단순한 `if`입니다:
```python
if re.search(r"\d", text):
    print("there is a digit")
```
실제 `True`나 `False`가 필요할 때는 `is not None`과 비교하거나 호출을 `bool(...)`로 감싸세요.

# --instructions--

텍스트 어디에든 숫자가 있으면 `True`를 반환하는 함수 `has_digit(text)`와, 텍스트가 숫자로 시작할 때만 `True`를 반환하는 함수 `starts_with_digit(text)`를 작성하세요

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`has_digit`는 텍스트 어디에서든 숫자를 찾아야 합니다

```python
    def test_1(self):
        self.assertEqual(has_digit("order 42"), True, "--err-t1--")
        self.assertEqual(has_digit("7up"), True, "--err-t1--")
```

숫자가 없으면 `has_digit`는 `False`를 반환해야 합니다

```python
    def test_2(self):
        self.assertEqual(has_digit("no numbers here"), False, "--err-t2--")
        self.assertEqual(has_digit(""), False, "--err-t2--")
```

`starts_with_digit`는 텍스트의 시작 부분만 살펴야 합니다

```python
    def test_3(self):
        self.assertEqual(starts_with_digit("42 orders"), True, "--err-t3--")
        self.assertEqual(starts_with_digit("order 42"), False, "--err-t3--")
        self.assertEqual(starts_with_digit(""), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def has_digit(text):
    return re.search(r"\d", text) is not None

def starts_with_digit(text):
    return re.match(r"\d", text) is not None
```
