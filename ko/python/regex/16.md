---
language: python
exerciseType: 1
---

# --instructions--

하나 이상의 글자, `=` 기호, 하나 이상의 숫자로 쓰인 설정과 일치하고 두 부분이 각각 `key`와 `value`라는 이름 붙은 그룹에 담기는 컴파일된 패턴 `PAIR_RE`를 정의하세요. 그런 다음 텍스트에서 찾은 모든 키를 그 값의 `int`에 대응시킨 딕셔너리를 반환하는 함수 `parse_settings(text)`를 작성하세요

# --before-asserts--

```python
import re
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`PAIR_RE`는 `key`와 `value`라는 이름 붙은 그룹을 가진 컴파일된 패턴이어야 합니다

```python
    def test_1(self):
        self.assertIsInstance(PAIR_RE, re.Pattern, "--err-t1--")
        self.assertEqual(PAIR_RE.search("width=10").groupdict(), {"key": "width", "value": "10"}, "--err-t1--")
```

`parse_settings`는 모든 설정을 정수 값으로 반환해야 합니다

```python
    def test_2(self):
        self.assertEqual(parse_settings("width=10 height=20"), {"width": 10, "height": 20}, "--err-t2--")
        self.assertEqual(parse_settings("size=3; depth=7"), {"size": 3, "depth": 7}, "--err-t2--")
```

설정이 없는 텍스트는 빈 딕셔너리를 반환해야 합니다

```python
    def test_3(self):
        self.assertEqual(parse_settings("nothing to read"), {}, "--err-t3--")
        self.assertEqual(parse_settings(""), {}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

PAIR_RE = re.compile(r"(?P<key>[a-zA-Z]+)=(?P<value>\d+)")

def parse_settings(text):
    settings = {}
    for match in PAIR_RE.finditer(text):
        settings[match.group("key")] = int(match.group("value"))
    return settings
```
