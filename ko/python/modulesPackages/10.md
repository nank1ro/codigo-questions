---
language: python
exerciseType: 4
---

# --instructions--

이 스크립트가 `random`에서 `choice()` 함수를 사용하도록 만들 수 있습니까?

# --answers--

```python
from random import choice
```

```python
names = ['Shane','Keanu', 'Khia']
```

```python
name = choice(names)
```

```python
print(f'Hello {name}')
```

# --solutions--

```python
from random import choice
names = ['Shane','Keanu', 'Khia']
name = choice(names)
print(f'Hello {name}')
```
