---
language: python
exerciseType: 2
---

# --description--

키를 사용하여 딕셔너리 값에 접근하는 것은 인덱스를 사용하여 리스트 값에 접근하는 것과 같습니다:
```python
user['age']
# user 딕셔너리에서 age 값을 가져옵니다
```

# --instructions--

키 `last_name`의 값을 출력하세요

# --seed--

```python
user = {"first_name": "John", "last_name": "Hood", "age": 30}
print([/][/][/][/])
```

# --answers--

- user
- [
- ]
- last_name
- "last_name"

# --solutions--

```python
user = {"first_name": "John", "last_name": "Hood", "age": 30}
print(user["last_name"])
```

# --output--

Hood
