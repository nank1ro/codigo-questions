---
language: python
exerciseType: 1
---

# --description--

집합은 **해시 가능한** 요소, 즉 변경할 수 없는 값만 담을 수 있습니다: 숫자, 문자열, `True`/`False`, 그리고 **튜플**입니다.
리스트, 딕셔너리, 또는 다른 집합을 추가하려고 하면 `TypeError`가 발생합니다:
```python
points = set()
points.add((1, 2))  # ok, a tuple
points.add([1, 2])  # TypeError: unhashable type: 'list'
```
튜플의 집합은 좌표나 (name, age) 같은 고유한 쌍을 관리하는 데 편리합니다:
```python
visits = [(1, 2), (1, 2), (3, 4)]
print(len(set(visits)))  # 2
```

# --instructions--

`visits`에 있는 서로 다른 좌표들의 집합을 `points`에 저장하고, 여기에 점 `(0, 0)`을 추가한 다음, 그 안에 서로 다른 점이 몇 개인지 `count`에 저장하세요

# --seed--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`add()`를 사용해야 합니다

```json
{
  "regex": "\\.add\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`points`는 `{(0, 0), (1, 2), (3, 4), (5, 6)}`과 같아야 합니다

```python
    def test_1(self):
        self.assertEqual(points, {(0, 0), (1, 2), (3, 4), (5, 6)}, "--err-t1--")
```

`count`는 `4`와 같아야 합니다

```python
    def test_2(self):
        self.assertEqual(count, 4, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
points = set(visits)
points.add((0, 0))
count = len(points)
```
