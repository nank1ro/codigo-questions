---
language: python
exerciseType: 1
---

# --description--

`pop()`은 집합에서 **임의의 요소**를 제거하고 그것을 반환합니다.
집합에는 순서가 없으므로 어떤 요소가 제거될지 선택할 수 없습니다. 빈 집합에서 `pop()`을 호출하면 `KeyError`가 발생합니다:
```python
tickets = {101, 102, 103}
picked = tickets.pop()
print(len(tickets))  # 2
```
`clear()`는 **모든** 요소를 제거하여 빈 집합을 남깁니다:
```python
tickets.clear()
print(len(tickets))  # 0
```

# --instructions--

`pop()`으로 `tickets`에서 요소 하나를 제거하여 변수 `picked`에 저장한 다음, `clear()`로 집합 `drafts`를 비우세요

# --seed--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`pop()`을 사용해야 합니다

```json
{
  "regex": "\\.pop\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`clear()`를 사용해야 합니다

```json
{
  "regex": "\\.clear\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`picked`는 원래 tickets 중 하나여야 합니다

```python
    def test_1(self):
        self.assertIn(picked, {101, 102, 103}, "--err-t1--")
```

`tickets`는 요소 2개를 포함해야 하며 더 이상 `picked`를 포함하면 안 됩니다

```python
    def test_2(self):
        self.assertEqual(len(tickets), 2, "--err-t2--")
        self.assertNotIn(picked, tickets, "--err-t2--")
```

`drafts`는 비어 있어야 합니다

```python
    def test_3(self):
        self.assertEqual(drafts, set(), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
picked = tickets.pop()
drafts.clear()
```
