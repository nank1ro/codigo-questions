---
language: python
exerciseType: 4
---

# --description--

리스트와 정확히 똑같이, `for`로 집합을 순회할 수 있습니다:
```python
for color in {"red", "blue"}:
    print(color)
```
집합은 순서가 없으므로 요소는 **어떤 순서로든** 나올 수 있으며, 그 순서는 실행할 때마다 바뀔 수도 있습니다.
예측 가능한 순서가 필요하면 집합을 `sorted()`에 전달하세요. 이는 요소를 정렬한 **리스트**를 반환합니다:
```python
for color in sorted({"red", "blue"}):
    print(color)  # blue, then red
```

# --instructions--

항목을 정렬하여 `words`의 서로 다른 단어들을 알파벳 순으로 한 번씩 출력하세요

# --answers--

```python
words = ["pear", "apple", "pear", "fig"]
```

```python
unique = set(words)
```

```python
for word in sorted(unique):
```

```python
    print(word)
```

# --solutions--

```python
words = ["pear", "apple", "pear", "fig"]
unique = set(words)
for word in sorted(unique):
    print(word)
```

# --output--

apple
fig
pear
