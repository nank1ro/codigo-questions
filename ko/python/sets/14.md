---
language: python
exerciseType: 1
---

# --description--

**집합 컴프리헨션(set comprehension)**은 리스트 컴프리헨션과 같은 문법이지만 중괄호를 사용해, 하나의 표현식으로 집합을 만듭니다:
```python
nums = [1, 2, 2, 3]
squares = {n * n for n in nums}
print(squares)  # {1, 4, 9}
```
선택적인 `if`로 요소를 걸러낼 수 있으며, 표현식으로 인해 생기는 중복은 자동으로 제거됩니다:
```python
evens = {n for n in range(10) if n % 2 == 0}
print(evens)  # {0, 2, 4, 6, 8}
```

# --instructions--

집합 컴프리헨션을 사용하여 `words`의 단어들을 소문자로 바꾼 집합을 `lowered`에 저장하고, 그다음 `if`가 있는 집합 컴프리헨션으로 `lowered`의 요소 중 5글자를 초과하는 것들을 `long_words`에 저장하세요

# --seed--

```python
words = ["Apple", "banana", "APPLE", "Cherry", "banana"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

집합 컴프리헨션을 사용해야 합니다

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`lowered`는 `{'apple', 'banana', 'cherry'}`와 같아야 합니다

```python
    def test_1(self):
        self.assertEqual(lowered, {"apple", "banana", "cherry"}, "--err-t1--")
```

`long_words`는 `{'banana', 'cherry'}`와 같아야 합니다

```python
    def test_2(self):
        self.assertEqual(long_words, {"banana", "cherry"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
words = ["Apple", "banana", "APPLE", "Cherry", "banana"]
lowered = {w.lower() for w in words}
long_words = {w for w in lowered if len(w) > 5}
```
