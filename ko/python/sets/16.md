---
language: python
exerciseType: 1
---

# --description--

두 집합은 작성된 순서와 상관없이 같은 요소를 담고 있으면 **같습니다**:
```python
print({1, 2, 3} == {3, 1, 2})  # True
```
컬렉션의 길이와 그 집합의 길이를 비교하는 것은 중복을 빠르게 발견하는 방법입니다: 집합이 **더 작다면**, 어떤 값이 두 번 이상 나온 것입니다:
```python
emails = ["a@x.com", "b@x.com", "a@x.com"]
print(len(set(emails)) < len(emails))  # True
```
리스트 메서드 `count(value)`는 어떤 값이 몇 번 나오는지 알려주므로, *어떤* 값이 중복되었는지 찾는 데 도움이 됩니다.

# --instructions--

리스트의 길이와 그 집합의 길이를 비교하여 `emails`에 반복된 주소가 있는지를 `has_duplicates`에 저장하고, 그다음 집합 컴프리헨션과 `count()`를 사용해 두 번 이상 나오는 주소들의 집합을 `duplicates`에 저장하세요

# --seed--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`set()`을 사용해야 합니다

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

집합 컴프리헨션을 사용해야 합니다

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_duplicates`는 `True`여야 합니다

```python
    def test_1(self):
        self.assertEqual(has_duplicates, True, "--err-t1--")
```

`duplicates`는 `{'ann@x.com', 'bob@x.com'}`과 같아야 합니다

```python
    def test_2(self):
        self.assertEqual(duplicates, {"ann@x.com", "bob@x.com"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
has_duplicates = len(set(emails)) < len(emails)
duplicates = {e for e in emails if emails.count(e) > 1}
```
