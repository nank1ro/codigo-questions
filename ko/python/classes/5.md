---
language: python
exerciseType: 2
---

# --description--

`__init__()`의 첫 번째 인수는 인스턴스 객체를 참조하는 데 사용되며, 관례적으로 이 인수를 `self`라고 합니다.
추가 인수(예: 동물의 `gender`와 `legs`)를 추가하고 `__init__()`의 본문에서 각각을 `self.gender`와 `self.legs`에 할당하면, `Animal` 클래스의 인스턴스 객체를 생성할 때 각 인스턴스에 gender와 legs를 지정해야 하며, 이 값들은 생성한 특정 인스턴스와 연결됩니다

# --instructions--

`self.gender`와 `self.legs`를 사용하여 각 객체에 gender와 legs를 할당하세요

# --seed--

```python
class Animal:
    def __init__(self, gender, legs):
        [/].[/] = gender
        self.legs = [/]
```

# --answers--

- self
- gender
- =
- legs
- .
- ;

# --solutions--

```python
class Animal:
    def __init__(self, gender, legs):
        self.gender = gender
        self.legs = legs
```
