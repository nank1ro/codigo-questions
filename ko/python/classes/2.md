---
language: python
exerciseType: 2
---

# --description--

기본적인 클래스는 `class` 키워드와 클래스 이름으로만 구성됩니다. 예를 들면:
```python
class ClassName:
    pass
```

# --instructions--

`Animal`이라는 클래스를 만드세요.
지금은 클래스의 코드 블록에 `pass` 키워드를 사용하세요.
(`pass`는 아무것도 하지 않지만, Python이 표현식을 기대하는 코드 영역에서 자리 표시자로 유용합니다)

# --seed--

```python
[/][/]:
    [/]
```

# --answers--

- class 
- Class 
- Animal
- 'Animal'
- Animal()
- pass
- return

# --solutions--

```python
class Animal:
    pass
```
