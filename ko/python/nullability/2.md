---
language: python
exerciseType: 1
---

# --description--

모든 함수 호출은 값을 만들어냅니다. 함수가 아무것도 반환하지 않는 것처럼 보일 때도 마찬가지입니다. `return` 문이 **없는** 함수, 또는 그냥 `return`만 있는 함수는 `None`을 돌려줍니다:
```python
def say_hello():
    print("hello")

result = say_hello()
print(result)  # None
```
그래서 `print(my_list.append(3))`를 호출하면 `None`이 출력됩니다: `append`는 리스트를 제자리에서 변경하고 아무것도 반환하지 않습니다.
동작만 수행하는 함수(출력, 저장, 리스트 변경)는 보통 `None`을 반환하는 반면, 무언가를 계산하는 함수는 반드시 명시적으로 `return`해야 합니다.

# --instructions--

값을 받아 리스트 `saved`에 추가하고 아무것도 반환하지 않는 함수 `store`를 작성하세요

# --seed--

```python
saved = []

def store(value):
    pass
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`append`를 사용해야 합니다

```json
{
  "regex": "\\.append\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`store("a")`는 `None`을 반환해야 합니다

```python
    def test_1(self):
        self.assertIsNone(store("a"), "--err-t1--")
```

`store("b")` 후에는 리스트 `saved`의 마지막이 `"b"`여야 합니다

```python
    def test_2(self):
        store("b")
        self.assertEqual(saved[-1], "b", "--err-t2--")
```

저장된 모든 값은 순서대로 유지되어야 합니다

```python
    def test_3(self):
        before = len(saved)
        store(1)
        store(2)
        self.assertEqual(saved[before:], [1, 2], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
saved = []

def store(value):
    saved.append(value)
```
