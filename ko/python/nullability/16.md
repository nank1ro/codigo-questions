---
language: python
exerciseType: 1
---

# --instructions--

함수 `get_path`를 작성하세요. 딕셔너리 `data`, `keys` 리스트, 선택적 `default`(제공되지 않으면 `None`)를 받습니다; 키를 한 단계씩 따라가며 찾은 값을 반환해야 하고, 어떤 키가 없거나 `None`을 담고 있거나 단계가 딕셔너리가 아니면 `default`를 반환해야 합니다; `0`과 `""` 같은 값은 그대로 반환되어야 합니다

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`default`의 기본값은 `None`이어야 합니다

```json
{
  "regex": "default\\s*=\\s*None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`get_path({"address": {"city": "Rome"}}, ["address", "city"])`는 `"Rome"`을 반환해야 합니다

```python
    def test_1(self):
        data = {"address": {"city": "Rome"}}
        self.assertEqual(get_path(data, ["address", "city"]), "Rome", "--err-t1--")
        self.assertEqual(get_path(data, ["address"]), {"city": "Rome"}, "--err-t1--")
```

없는 키, `None`, 딕셔너리가 아닌 단계는 기본값을 내야 합니다

```python
    def test_2(self):
        data = {"address": None, "name": "Ada"}
        self.assertIsNone(get_path(data, ["address", "city"]), "--err-t2--")
        self.assertEqual(get_path(data, ["phone", "home"], "n/a"), "n/a", "--err-t2--")
        self.assertEqual(get_path(data, ["name", "first"], "n/a"), "n/a", "--err-t2--")
```

`0`과 `""`는 그대로 반환되어야 합니다

```python
    def test_3(self):
        data = {"stats": {"count": 0, "note": ""}}
        self.assertEqual(get_path(data, ["stats", "count"], -1), 0, "--err-t3--")
        self.assertEqual(get_path(data, ["stats", "note"], "none"), "", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def get_path(data, keys, default=None):
    current = data
    for key in keys:
        if not isinstance(current, dict):
            return default
        current = current.get(key)
        if current is None:
            return default
    return current
```
