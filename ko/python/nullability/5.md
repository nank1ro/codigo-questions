---
language: python
exerciseType: 1
---

# --description--

매개변수는 **기본값**을 가질 수 있으며, 이는 호출자가 인자를 생략할 때 사용됩니다. "제공되지 않음"의 기본값으로는 `None`이 보통 쓰입니다:
```python
def greet(name=None):
    if name is None:
        name = "stranger"
    print("Hello, " + name)

greet()       # Hello, stranger
greet("Ada")  # Hello, Ada
```
이것이 리스트와 딕셔너리에서 문제가 됩니다. 기본값은 함수가 정의될 때 **한 번만** 평가되므로, `def add(item, items=[])`는 `items`를 생략하는 모든 호출에서 같은 리스트를 공유하고 항목이 쌓입니다. 해결책은 `None`을 기본값으로 두고 함수 안에서 새 리스트를 만드는 것입니다:
```python
def add(item, items=None):
    if items is None:
        items = []
    items.append(item)
    return items
```

# --instructions--

문자열 `tag`와 선택적 리스트 `tags`를 받는 함수 `add_tag`를 작성하세요; `tag`를 `tags`에 추가하고 리스트를 반환해야 하며, `tags`가 제공되지 않으면 새 리스트를 만들어야 합니다

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`tags`의 기본값은 `None`이어야 합니다

```json
{
  "regex": "tags\\s*=\\s*None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`add_tag("new")`는 `["new"]`를 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(add_tag("new"), ["new"], "--err-t1--")
```

`tags` 없이 두 번 호출하면 독립적인 리스트를 반환해야 합니다

```python
    def test_2(self):
        first = add_tag("a")
        second = add_tag("b")
        self.assertEqual(first, ["a"], "--err-t2--")
        self.assertEqual(second, ["b"], "--err-t2--")
```

`add_tag("c", ["a", "b"])`는 `["a", "b", "c"]`를 반환해야 합니다

```python
    def test_3(self):
        existing = ["a", "b"]
        self.assertEqual(add_tag("c", existing), ["a", "b", "c"], "--err-t3--")
        self.assertIs(add_tag("d", existing), existing, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def add_tag(tag, tags=None):
    if tags is None:
        tags = []
    tags.append(tag)
    return tags
```
