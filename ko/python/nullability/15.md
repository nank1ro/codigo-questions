---
language: python
exerciseType: 1
---

# --instructions--

함수 `oldest`를 작성하세요. `"name"` 키와 선택적 `"age"` 키를 가진 딕셔너리의 리스트를 받으며, `"age"`는 없거나 `None`일 수 있습니다; 나이가 가장 많은 사람의 이름을 반환해야 하고, 나이가 없는 사람은 무시하며, 아무도 나이가 없으면 `None`을 반환해야 합니다

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`get`을 사용해야 합니다

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`is None` 또는 `is not None`으로 검사해야 합니다

```json
{
  "regex": "is\\s+(not\\s+)?None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Ada(36), Linus(28), Grace(45) 중 가장 나이가 많은 사람은 `"Grace"`여야 합니다

```python
    def test_1(self):
        people = [{"name": "Ada", "age": 36}, {"name": "Linus", "age": 28}, {"name": "Grace", "age": 45}]
        self.assertEqual(oldest(people), "Grace", "--err-t1--")
```

나이가 없거나 `None`인 사람은 무시되어야 합니다

```python
    def test_2(self):
        people = [{"name": "Tim"}, {"name": "Ada", "age": 36}, {"name": "Bob", "age": None}]
        self.assertEqual(oldest(people), "Ada", "--err-t2--")
```

아무도 나이가 없으면 결과는 `None`이어야 하고, `0`인 나이도 유효해야 합니다

```python
    def test_3(self):
        self.assertIsNone(oldest([{"name": "Tim"}, {"name": "Bob", "age": None}]), "--err-t3--")
        self.assertIsNone(oldest([]), "--err-t3--")
        self.assertEqual(oldest([{"name": "Baby", "age": 0}, {"name": "Tim"}]), "Baby", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def oldest(people):
    best_name = None
    best_age = None
    for person in people:
        age = person.get("age")
        if age is None:
            continue
        if best_age is None or age > best_age:
            best_age = age
            best_name = person["name"]
    return best_name
```
