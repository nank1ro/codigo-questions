---
language: python
exerciseType: 2
---

# --description--

딕셔너리에서 `[]`로 없는 키를 읽으면 `KeyError`가 발생합니다. `get` 메서드가 안전한 대안입니다: 키가 있으면 값을 반환하고 없으면 `None`을 반환합니다:
```python
ages = {"Ada": 36}
print(ages.get("Ada"))    # 36
print(ages.get("Grace"))  # None
```
`get`은 두 번째 인자를 받습니다. 키가 없을 때 `None` **대신에** 반환할 값입니다:
```python
print(ages.get("Grace", 0))  # 0
```
이것이 일상적인 코드에서 `None`이 나타나는 가장 흔한 방식입니다: 아무것도 찾지 못한 조회입니다.

# --instructions--

`Ada`의 나이를 출력하고, 없는 `Grace`에 대해서는 `None`을, 기본값을 사용한 `Grace`에 대해서는 `-1`을 출력하도록 코드를 완성하여 출력 `36`, `None`, `-1`, `36`을 만들어보세요

# --seed--

```python
ages = {"Ada": 36}
print(ages.get([/]))
print(ages.get("Grace"))
print(ages.get("Grace", [/]))
print(ages.get("Ada", -1))
```

# --answers--

- "Ada"
- "Grace"
- -1
- None
- 0

# --solutions--

```python
ages = {"Ada": 36}
print(ages.get("Ada"))
print(ages.get("Grace"))
print(ages.get("Grace", -1))
print(ages.get("Ada", -1))
```

# --output--

36
None
-1
36
