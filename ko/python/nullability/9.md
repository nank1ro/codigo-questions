---
language: python
exerciseType: 3
---

# --description--

`or` 연산자는 `True`나 `False`를 반환하지 않습니다: 왼쪽 피연산자가 참이면 **왼쪽** 피연산자를, 그렇지 않으면 **오른쪽** 피연산자를 반환합니다. 이를 통해 한 줄로 폴백을 제공할 수 있습니다:
```python
name = None
print(name or "anonymous")  # anonymous
name = "Ada"
print(name or "anonymous")  # Ada
```
문제는 `or`가 `None`이 아니라 truthiness를 본다는 것입니다: `0`, `""`, `[]`도 폴백으로 대체됩니다. 모든 빈 값이 폴백이 되어도 괜찮을 때만 `x or fallback`을 사용하세요.

# --instructions--

이 코드는 무엇을 출력할까요?
```python
def label(count):
    return count or "unknown"

print(label(0), label(None), label(5))
```

# --answers--

- `unknown unknown 5`, `0`은 거짓이므로 폴백으로 대체되기 때문입니다
- `0 unknown 5`, `None`만 폴백으로 대체되기 때문입니다
- `False unknown 5`, `or`가 `0`을 불리언으로 변환하기 때문입니다
- `True True True`, `or`는 항상 불리언을 반환하기 때문입니다

# --solutions--

- `unknown unknown 5`, `0`은 거짓이므로 폴백으로 대체되기 때문입니다
