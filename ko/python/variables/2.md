---
language: python
exerciseType: 1
---

# --description--

변수는 저장된 값을 변경할 수 있기 때문에 이렇게 불립니다.
`=`를 사용하여 새로운 값을 지정함으로써 `x`를 업데이트할 수 있습니다.
```python
>>> x = 1
>>> print(x)
1
>>> x = 2
>>> print(x)
2
```

# --instructions--

변수 `x`의 값을 숫자 `10`으로 업데이트하세요.

# --seed--

```python
x = 5
# write after this line
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`x`는 `10`과 같아야 합니다.

```python
    def test_1(self):
        self.assertEqual(x, 10, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
x = 5
x = 10
```
