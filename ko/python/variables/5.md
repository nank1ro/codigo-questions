---
language: python
exerciseType: 1
---

# --description--

문자열 변수는 작은따옴표 또는 큰따옴표를 사용하여 선언할 수 있습니다:
```python
>>> x = "May"
>>> x = 'May'
```
둘 다 같은 것입니다.

# --instructions--

`month`라는 변수를 선언하고 값을 `june`으로 설정하세요.

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`month`는 `"june"`과 같아야 합니다.

```python
    def test_1(self):
        self.assertEqual(month, "june", "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
month = "june"
```
