---
language: python
exerciseType: 1
---

# --description--

다른 변수의 값을 변수에 할당할 수도 있습니다. 여기서 `y` 변수에 `x`의 값을 할당할 수 있습니다
```python
>>> x = 5
>>> y = x
>>> print(y)
5
```

# --instructions--

`y`라는 새 변수를 만들고 변수 `x`의 값을 할당하세요.

# --seed--

```python
x = 1
# write after this line
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`y`는 `x`와 같아야 합니다.

```python
    def test_1(self):
        self.assertEqual(x, y, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
x = 1
y = x
```
