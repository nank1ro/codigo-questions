---
language: python
exerciseType: 1
---

# --description--

변수를 업데이트하면 이전 값을 잊어버립니다. 여기서 `x` 변수를 두 번 출력하여 값이 어떻게 업데이트되는지 확인할 수 있습니다.
```python
>>> x = 5
>>> print(x)
5
>>> x = 10
>>> print(x)
10
```

# --instructions--

변수 `x`의 값을 문자열 `"updated"`로 업데이트하세요.

# --seed--

```python
x = 1
# write after this line
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`x`는 `"updated"`와 같아야 합니다.

```python
    def test_1(self):
        self.assertEqual(x, "updated", "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
x = 1
x = "updated"
```
