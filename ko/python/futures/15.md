---
language: python
exerciseType: 4
---

# --description--

실패하는 코루틴을 다루는 방법은 동기 패턴을 따릅니다: 코루틴이 예외를 발생시키면, 호출자는 `await`를 `try`/`except`로 감싸고 오류 객체를 어떻게 처리할지 결정합니다. 예를 들어 `print(e)`로 메시지를 출력하는 식입니다.

# --instructions--

항목을 정렬하여 import로 시작하고, `main` 전에 `load`를 정의하고, `"config"`를 로드한 결과를 출력한 다음, 빈 이름을 로드할 때 발생한 오류를 잡아 그 메시지를 출력하는 프로그램을 만들어 `loaded config`, `empty name`이 출력되게 하세요

# --answers--

```python
import asyncio
```

```python
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
```

```python
async def main():
    print(await load("config"))
```

```python
    try:
        await load("")
```

```python
    except ValueError as e:
        print(e)
```

```python
asyncio.run(main())
```

# --solutions--

```python
import asyncio
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
async def main():
    print(await load("config"))
    try:
        await load("")
    except ValueError as e:
        print(e)
asyncio.run(main())
```

# --output--

loaded config
empty name
