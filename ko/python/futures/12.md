---
language: python
exerciseType: 1
---

# --description--

코루틴 안에서 발생한 예외는 코루틴 객체를 만든 곳에 나타나지 않습니다: 예외는 그것을 실행하는 `await`에서 발생하고, 가장 바깥쪽 코루틴의 경우 `asyncio.run`에서 발생합니다. 따라서 `try`/`except`는 **`await`**를 감싸야 합니다:
```python
import asyncio

async def load(path):
    await asyncio.sleep(0.01)
    if path == "":
        raise FileNotFoundError("empty path")
    return "contents"

async def safe_load(path):
    try:
        return await load(path)
    except FileNotFoundError:
        return ""

print(asyncio.run(safe_load("")))  # prints an empty line
```
아무도 잡지 않은 예외는 모든 `await`를 타고 `asyncio.run`까지 전파되어 동기 코드에서 다시 발생합니다. 보통의 호출 스택과 똑같습니다.

# --instructions--

`0.01`초를 기다린 뒤 `a / b`를 반환하는 코루틴 함수 `divide(a, b)`와, `divide(a, b)`를 await해 그 결과를 반환하되 `ZeroDivisionError`가 발생하면 `None`을 반환하는 코루틴 함수 `safe_divide(a, b)`를 작성하세요

# --before-asserts--

```python
import asyncio
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`divide(10, 4)`는 `2.5`를 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(asyncio.run(divide(10, 4)), 2.5, "--err-t1--")
```

`divide(1, 0)`을 실행하면 `ZeroDivisionError`가 발생해야 합니다

```python
    def test_2(self):
        with self.assertRaises(ZeroDivisionError, msg="--err-t2--"):
            asyncio.run(divide(1, 0))
```

`safe_divide(10, 2)`는 `5.0`을, `safe_divide(1, 0)`은 `None`을 반환해야 합니다

```python
    def test_3(self):
        self.assertEqual(asyncio.run(safe_divide(10, 2)), 5.0, "--err-t3--")
        self.assertIsNone(asyncio.run(safe_divide(1, 0)), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def divide(a, b):
    await asyncio.sleep(0.01)
    return a / b

async def safe_divide(a, b):
    try:
        return await divide(a, b)
    except ZeroDivisionError:
        return None
```
