---
language: python
exerciseType: 1
---

# --description--

`asyncio.create_task(coro)`는 코루틴을 **태스크**로 감싸 백그라운드에서 실행되도록 예약합니다. `await`와 달리 즉시 반환되므로, 태스크가 실행되는 동안 현재 코루틴도 계속 작업할 수 있습니다. 태스크는 실제로 현재 코루틴이 다음 `await`에서 일시 중지될 때 시작됩니다. 나중에 태스크를 await하면 그 결과를 얻습니다:
```python
import asyncio

async def background():
    print("task started")
    await asyncio.sleep(0.01)
    return "task done"

async def main():
    task = asyncio.create_task(background())
    print("main continues")
    print(await task)

asyncio.run(main())
```
`background`는 `main`이 await할 때 비로소 시작되므로, 이 코드는 먼저 `main continues`를 출력하고 그다음 `task started`와 `task done`을 출력합니다.

# --instructions--

리스트 `log`에 `"start"`를 추가하고 `0.01`초를 기다린 뒤 `"done"`을 추가하는 코루틴 함수 `background(log)`와, `background(log)`를 `asyncio.create_task`로 예약하고 `log`에 `"main"`을 추가한 다음 태스크를 await하고 `log`를 반환하는 코루틴 함수 `main(log)`를 작성하세요

# --before-asserts--

```python
import asyncio
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`background([])`는 `None`을 반환하고 로그를 `["start", "done"]`으로 남겨야 합니다

```python
    def test_1(self):
        log = []
        self.assertIsNone(asyncio.run(background(log)), "--err-t1--")
        self.assertEqual(log, ["start", "done"], "--err-t1--")
```

`main([])`은 `["main", "start", "done"]`을 반환해야 합니다. 태스크는 `main`이 await할 때만 시작되기 때문입니다

```python
    def test_2(self):
        self.assertEqual(asyncio.run(main([])), ["main", "start", "done"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def background(log):
    log.append("start")
    await asyncio.sleep(0.01)
    log.append("done")

async def main(log):
    task = asyncio.create_task(background(log))
    log.append("main")
    await task
    return log
```
