---
language: python
exerciseType: 2
---

# --description--

`asyncio.wait_for(awaitable, timeout)`은 무언가를 await하다가 `timeout`초가 지나면 포기합니다: 작업은 취소되고 `TimeoutError`가 발생하며, 이는 다른 예외처럼 잡을 수 있습니다:
```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())  # timed out
```
타임아웃이 `0.1`이라면 같은 코드는 `done`을 출력합니다. `asyncio.TimeoutError`는 내장 `TimeoutError`의 다른 이름입니다.

# --instructions--

`slow()`가 `0.01`초 타임아웃으로 await되고 타임아웃이 잡혀 `timed out`이 출력되도록 코드를 완성하세요

# --seed--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.[/](slow(), [/]=0.01)
        print(result)
    except [/]:
        print("timed out")

asyncio.run(main())
```

# --answers--

- wait_for
- timeout
- TimeoutError
- gather
- delay
- ValueError

# --solutions--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())
```

# --output--

timed out
