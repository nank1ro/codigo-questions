---
language: python
exerciseType: 4
---

# --description--

작은 비동기 프로그램은 정해진 형태를 따릅니다: `asyncio`를 import하고, 코루틴 함수들을 정의하고, 이들을 await하는 `main` 코루틴을 정의한 다음, 마지막에 `asyncio.run(main())`을 한 번 호출합니다.

# --instructions--

항목을 정렬하여 import로 시작하고, `main` 전에 `download`를 정의하고, 두 개의 다운로드를 `asyncio.gather`로 동시에 실행한 뒤 결과를 출력하는 프로그램을 만들어 `['report.pdf', 'data.csv']`가 출력되게 하세요

# --answers--

```python
import asyncio
```

```python
async def download(name):
    await asyncio.sleep(0.01)
    return name
```

```python
async def main():
    files = await asyncio.gather(download("report.pdf"), download("data.csv"))
    print(files)
```

```python
asyncio.run(main())
```

# --solutions--

```python
import asyncio
async def download(name):
    await asyncio.sleep(0.01)
    return name
async def main():
    files = await asyncio.gather(download("report.pdf"), download("data.csv"))
    print(files)
asyncio.run(main())
```

# --output--

['report.pdf', 'data.csv']
