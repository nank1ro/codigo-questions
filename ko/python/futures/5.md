---
language: python
exerciseType: 1
---

# --description--

코루틴은 `await`로 서로를 호출합니다. 코루틴은 다른 어떤 코루틴이든 await하고 그 반환값을 받아 계속 진행할 수 있습니다. 함수 호출의 연쇄와 똑같습니다:
```python
import asyncio

async def fetch_price(item):
    await asyncio.sleep(0.01)  # simulates a slow lookup
    return 10

async def total(item, quantity):
    price = await fetch_price(item)
    return price * quantity

print(asyncio.run(total("pen", 3)))  # 30
```
`asyncio.run`을 거치는 것은 가장 바깥쪽 코루틴뿐이며, 안쪽 코루틴은 모두 `await`로 도달합니다. 만약 `total`이 평범한 `def`라면 `await`를 전혀 사용할 수 없습니다: `async` 키워드는 기다려야 하는 연쇄상의 모든 함수로 번져나갑니다.

# --instructions--

`0.01`초를 기다린 뒤 id를 뒤에 붙인 문자열 `"user"`를 반환하는 코루틴 함수 `fetch_user(user_id)`와, `fetch_user`를 await한 뒤 가져온 이름 앞에 `"Hello, "`를 붙여 반환하는 코루틴 함수 `greet_user(user_id)`를 작성하세요

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch_user(1)`은 `"user1"`을, `fetch_user(42)`는 `"user42"`를 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch_user(1)), "user1", "--err-t1--")
        self.assertEqual(asyncio.run(fetch_user(42)), "user42", "--err-t1--")
```

`greet_user(1)`은 `"Hello, user1"`을 반환해야 합니다

```python
    def test_2(self):
        self.assertEqual(asyncio.run(greet_user(1)), "Hello, user1", "--err-t2--")
```

`fetch_user`와 `greet_user`는 모두 코루틴 함수여야 합니다

```python
    def test_3(self):
        self.assertTrue(inspect.iscoroutinefunction(fetch_user), "--err-t3--")
        self.assertTrue(inspect.iscoroutinefunction(greet_user), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch_user(user_id):
    await asyncio.sleep(0.01)
    return f"user{user_id}"

async def greet_user(user_id):
    name = await fetch_user(user_id)
    return f"Hello, {name}"
```
