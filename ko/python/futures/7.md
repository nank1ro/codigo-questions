---
language: python
exerciseType: 1
---

# --description--

연산 개수가 정해져 있지 않을 때 동시성이 빛을 발합니다. 리스트 컴프리헨션으로 코루틴 객체를 만들고, `gather`에 언패킹해서 전달한 뒤 한꺼번에 await하세요:
```python
import asyncio

async def fetch(url):
    await asyncio.sleep(0.02)
    return len(url)

async def fetch_all(urls):
    return await asyncio.gather(*[fetch(u) for u in urls])

print(asyncio.run(fetch_all(["a.com", "bb.com"])))  # [5, 6]
```
열 개의 url도 200밀리초 대신 총 약 20밀리초가 걸리며, 결과는 url 순서를 그대로 유지합니다.

# --instructions--

`0.02`초를 기다린 뒤 `item_id * 10`을 반환하는 코루틴 함수 `fetch(item_id)`와, 모든 id에 대해 `fetch`를 `asyncio.gather`로 **동시에** 실행하고 결과 리스트를 반환하는 코루틴 함수 `fetch_all(ids)`를 작성하세요

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch(3)`은 `30`을 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch(3)), 30, "--err-t1--")
```

`fetch_all([1, 2, 3])`은 `[10, 20, 30]`을, `fetch_all([])`은 `[]`를 반환해야 합니다

```python
    def test_2(self):
        self.assertEqual(asyncio.run(fetch_all([1, 2, 3])), [10, 20, 30], "--err-t2--")
        self.assertEqual(asyncio.run(fetch_all([])), [], "--err-t2--")
```

다섯 개의 id로 `fetch_all`을 호출하면 동시에 실행되어 `0.1`초보다 훨씬 짧게 걸려야 합니다

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(fetch_all([1, 2, 3, 4, 5]))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [10, 20, 30, 40, 50], "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch(item_id):
    await asyncio.sleep(0.02)
    return item_id * 10

async def fetch_all(ids):
    return await asyncio.gather(*[fetch(i) for i in ids])
```
