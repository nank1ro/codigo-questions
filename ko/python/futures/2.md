---
language: python
exerciseType: 1
---

# --description--

코루틴은 일반 함수와 똑같이 매개변수를 받고 `return`으로 값을 반환할 수 있습니다. 이 값은 코루틴 객체가 만들어질 때가 아니라 코루틴이 실행된 후에야 얻을 수 있습니다. `asyncio.run`은 코루틴이 반환한 값을 그대로 반환합니다:
```python
import asyncio

async def double(n):
    return n * 2

result = asyncio.run(double(21))
print(result)  # 42
```
함수에 대해 아는 모든 것은 `async def` 안에서도 그대로 적용됩니다: 지역 변수, 조건문, 반복문, 여러 개의 `return` 문 등이 그렇습니다.

# --instructions--

`async def`로 `a`와 `b`의 합을 반환하는 코루틴 함수 `add(a, b)`를 작성하세요

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`add`는 코루틴 함수여야 합니다

```python
    def test_1(self):
        self.assertTrue(inspect.iscoroutinefunction(add), "--err-t1--")
```

`add(1, 3)`을 실행하면 `4`가 반환되어야 합니다

```python
    def test_2(self):
        self.assertEqual(asyncio.run(add(1, 3)), 4, "--err-t2--")
```

`add(200, 210)`을 실행하면 `410`이 반환되어야 합니다

```python
    def test_3(self):
        self.assertEqual(asyncio.run(add(200, 210)), 410, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
async def add(a, b):
    return a + b
```
