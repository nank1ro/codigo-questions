---
language: python
exerciseType: 1
---

# --description--

`await`는 현재 코루틴을 일시 중지했다가 기다리던 작업이 끝나면 그 결과를 돌려줍니다. 코루틴이 일시 중지된 동안 이벤트 루프는 다른 코루틴을 자유롭게 실행할 수 있습니다. `await`는 `async def` 안에서만 사용할 수 있습니다.

`asyncio.sleep(seconds)`는 가장 간단한 awaitable입니다: 루프를 막지 않으면서 주어진 시간만큼 기다립니다. `time.sleep`과 달리 반드시 await해야 합니다:
```python
import asyncio

async def countdown():
    print("ready")
    await asyncio.sleep(0.05)
    print("go")

asyncio.run(countdown())
```
이 프로그램은 `ready`를 출력하고, 50밀리초를 기다린 뒤 `go`를 출력합니다. `await` 없이 `asyncio.sleep(0.05)`라고 쓰면 코루틴 객체만 만들어질 뿐 실행되지 않으므로 아무런 대기도 일어나지 않습니다.

# --instructions--

`asyncio.sleep`으로 `delay`초만큼 기다린 뒤 `value`를 반환하는 코루틴 함수 `wait_and_return(value, delay)`를 작성하세요

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`wait_and_return("ok", 0.01)`을 실행하면 `"ok"`가 반환되어야 합니다

```python
    def test_1(self):
        self.assertEqual(asyncio.run(wait_and_return("ok", 0.01)), "ok", "--err-t1--")
```

`wait_and_return(7, 0.02)`을 실행하면 `7`이 반환되고 최소 `0.02`초가 걸려야 합니다

```python
    def test_2(self):
        start = time.perf_counter()
        result = asyncio.run(wait_and_return(7, 0.02))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, 7, "--err-t2--")
        self.assertGreaterEqual(elapsed, 0.018, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def wait_and_return(value, delay):
    await asyncio.sleep(delay)
    return value
```
