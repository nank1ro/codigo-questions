---
language: python
exerciseType: 2
---

# --description--

태스크는 기다려 주는 사람이 있든 없든 계속 실행됩니다. 미리 만들어 두고 다른 작업을 하다가, 결과가 필요한 지점에서만 `await`하세요: 태스크의 일부가 이미 백그라운드에서 실행되었으므로 기다림이 짧아집니다. 태스크는 `task.done()`으로 확인할 수도 있는데, 이는 끝나면 `True`를 반환합니다.

# --instructions--

`compute()`가 태스크로 예약되고, 실행되는 동안 `"waiting"`이 출력되며, 그다음 태스크를 await해 값을 얻어 `waiting`, `42`가 출력되도록 코드를 완성하세요

# --seed--

```python
import asyncio

async def compute():
    await asyncio.sleep(0.01)
    return 42

async def main():
    task = asyncio.[/](compute())
    print("waiting")
    value = [/] task
    print(value)

asyncio.run(main())
```

# --answers--

- create_task
- await
- gather
- run
- sleep
- result

# --solutions--

```python
import asyncio

async def compute():
    await asyncio.sleep(0.01)
    return 42

async def main():
    task = asyncio.create_task(compute())
    print("waiting")
    value = await task
    print(value)

asyncio.run(main())
```

# --output--

waiting
42
