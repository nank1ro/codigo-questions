---
language: python
exerciseType: 2
---

# --description--

튜플을 리스트로 변환하고 싶을 때가 있습니다.
이를 위해 `list()` 함수를 사용할 수 있습니다

# --instructions--

`list()` 함수를 사용하여 튜플을 리스트로 변환하세요

# --seed--

```python
tuple1 = ("dog", "bear", "cat")
list1 = [/]
list1.append("frog")
print(list1)
```

# --answers--

- list[tuple1]
- list(tuple1)

# --solutions--

```python
tuple1 = ("dog", "bear", "cat")
list1 = list(tuple1)
list1.append("frog")
print(list1)
```

# --output--

['dog', 'bear', 'cat', 'frog']
