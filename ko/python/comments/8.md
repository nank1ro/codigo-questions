---
language: python
exerciseType: 4
---

# --description--

파일도 문서화할 수 있습니다. 어떤 import나 정의보다도 앞선 **파일의 맨 첫 번째 문**으로 작성된 문자열은 **모듈 docstring**입니다: 파일 전체가 무엇을 위한 것인지 말해 줍니다.
```python
"""Tools for working with rectangles."""

def area(width, height):
    """Returns the area of a rectangle."""
    return width * height
```
첫 번째 문만 해당됩니다. 주석은 그 위에 놓일 수 있지만, 그 사이에 실제 코드가 하나라도 들어가면 문자열은 다시 아무 쓸모없는 평범한 문자열로 돌아갑니다.

# --instructions--

항목들을 정렬하여 맨 첫 줄에 모듈 docstring으로 시작하고, 이어서 `area`를 정의하고, 마지막으로 `area(3, 4)`를 출력하는 프로그램을 만드세요

# --answers--

```python
"""Tools for working with rectangles."""
```

```python
def area(width, height):
    """Returns the area of a rectangle."""
    return width * height
```

```python
print(area(3, 4))
```

# --solutions--

```python
"""Tools for working with rectangles."""
def area(width, height):
    """Returns the area of a rectangle."""
    return width * height
print(area(3, 4))
```

# --output--

12
