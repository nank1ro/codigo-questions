---
language: python
exerciseType: 2
---

# --description--

클래스도 같은 방식으로 동작합니다: 클래스 본문의 첫 번째 문으로 놓인 문자열은 그 클래스의 docstring이며, `__doc__`에 저장됩니다:
```python
class Point:
    """A point on a grid."""

    def __init__(self, x, y):
        self.x = x
        self.y = y

print(Point.__doc__)  # A point on a grid.
```
클래스 안의 각 메서드도 자기만의 docstring을 가질 수 있으며, `Point.__init__.__doc__`으로 읽을 수 있습니다. 따라서 docstring이 허용되는 세 곳은 모듈의 맨 위, 클래스의 맨 위, 함수의 맨 위입니다.

# --instructions--

`Point`가 docstring을 갖고 프로그램이 그것을 출력하도록 코드를 완성하세요

# --seed--

```python
class Point:
    [/]A point on a grid.[/]

    def __init__(self, x, y):
        self.x = x
        self.y = y

print(Point.[/])
```

# --answers--

- """
- """
- __doc__
- __name__
- #

# --solutions--

```python
class Point:
    """A point on a grid."""

    def __init__(self, x, y):
        self.x = x
        self.y = y

print(Point.__doc__)
```

# --output--

A point on a grid.
