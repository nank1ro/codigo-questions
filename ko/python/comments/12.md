---
language: python
exerciseType: 4
---

# --description--

docstring은 다른 모든 문보다 위인 **본문의 첫 번째 줄**이어야 합니다. `return` 뒤에 쓰인 문자열이나 본문의 다른 어디에 있든 문자열은 그저 문자열일 뿐입니다: `__doc__`은 `None`으로 남고, 어떤 도구도 그것을 보여 주지 않습니다.

# --instructions--

항목들을 정렬하여 docstring을 본문의 첫 번째 줄로 갖는 `to_seconds`를 정의하고, 이어서 `to_seconds(2)`를 출력하는 프로그램을 만드세요

# --answers--

```python
def to_seconds(minutes):
```

```python
    """Returns the number of seconds in the given minutes."""
```

```python
    return minutes * 60
```

```python
print(to_seconds(2))
```

# --solutions--

```python
def to_seconds(minutes):
    """Returns the number of seconds in the given minutes."""
    return minutes * 60
print(to_seconds(2))
```

# --output--

120
