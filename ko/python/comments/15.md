---
language: python
exerciseType: 1
---

# --description--

Python은 코드 안에서만 `#`을 찾으며, **문자열** 안에서는 찾지 않습니다. 따옴표 사이에서 `#`은 그저 평범한 문자입니다:
```python
print("black is #000000")  # a hex colour
```
첫 번째 `#`은 텍스트의 일부이고, 두 번째 것이 진짜 주석을 시작합니다. `#` 주석 안의 `"""`도 마찬가지입니다: 거기서는 그저 따옴표 문자 세 개일 뿐이며, 아무것도 시작하지 않습니다.

# --instructions--

`name` 앞에 `#`을 붙여 반환하는 함수 `label(name)`을 작성하세요. 그러면 `label("python")`은 `"#python"`을 반환합니다

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`label("python")`은 `"#python"`을 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(label("python"), "#python", "--err-t1--")
```

`label("ai")`는 `"#ai"`를 반환해야 합니다

```python
    def test_2(self):
        self.assertEqual(label("ai"), "#ai", "--err-t2--")
```

`label("")`은 `"#"`을 반환해야 합니다

```python
    def test_3(self):
        self.assertEqual(label(""), "#", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def label(name):
    """Returns name with a # in front of it."""
    return "#" + name
```
