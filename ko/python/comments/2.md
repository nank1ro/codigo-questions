---
language: python
exerciseType: 1
---

# --description--

주석은 꼭 자기 줄을 가질 필요가 없습니다: 같은 줄에서 코드 뒤에 붙을 수 있습니다. 이것을 **인라인 주석**이라고 하며, 그 문장에 대한 짧은 메모를 남기기 좋은 자리입니다:
```python
retries = 3  # 세 번 시도한 후 포기
```
`#`부터 줄의 끝까지는 모두 무시되고, 그 앞의 코드는 평소처럼 실행됩니다.

Python의 스타일 가이드인 **PEP 8**은 여기에 약간의 공백을 요구합니다: 코드와 `#` 사이에는 **공백 두 칸**, `#` 뒤에는 **공백 한 칸**을 두세요. 자기 줄에 있는 주석에는 `#` 뒤의 공백만 필요합니다.

# --instructions--

`seats = 4` 뒤에 값을 설명하는 인라인 주석을 추가하세요. 예를 들어 `# seats per table`처럼요. `#` 앞에는 공백 두 칸을 남기세요

# --seed--

```python
seats = 4
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`seats = 4` 줄은 `#` 주석으로 끝나야 하며, `#` 앞에는 공백 두 칸, `#` 뒤에는 공백 한 칸이 있어야 합니다

```json
{
  "regex": "seats = 4[ \\t]{2,}#[ \\t]\\S",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`seats`는 여전히 `4`와 같아야 합니다

```python
    def test_1(self):
        self.assertEqual(seats, 4, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
seats = 4  # 테이블당 좌석 수
```
