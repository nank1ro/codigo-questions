---
language: python
exerciseType: 2
---

# --description--

docstring은 버려지지 않습니다: Python은 그것을 함수의 `__doc__` 속성에 저장하므로, 프로그램이 실행되는 동안 자기 문서를 읽을 수 있습니다:
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"

print(greet.__doc__)  # Returns the greeting for name.
```
함수에 docstring이 없으면 `__doc__`은 `None`입니다. `help(greet)`가 출력하는 내용이자, 이름 위에 마우스를 올렸을 때 에디터가 보여 주는 내용입니다.

# --instructions--

`greet`가 docstring을 갖고 프로그램이 그것을 출력하도록 코드를 완성하세요

# --seed--

```python
def greet(name):
    [/]Returns a greeting for name.[/]
    return "Hi, " + name + "!"

print(greet.[/])
```

# --answers--

- """
- """
- __doc__
- __name__
- #

# --solutions--

```python
def greet(name):
    """Returns a greeting for name."""
    return "Hi, " + name + "!"

print(greet.__doc__)
```

# --output--

Returns a greeting for name.
