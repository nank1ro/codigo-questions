---
language: python
exerciseType: 2
---

# --description--

같은 방식이 딕셔너리에도 적용됩니다: **딕셔너리 컴프리헨션**은 중괄호와 `key: value` 표현식을 사용합니다.
```python
names = ["ana", "bob"]
lengths = {name: len(name) for name in names}
print(lengths)  # {'ana': 3, 'bob': 3}
```

# --instructions--

프로그램이 `{1: 1, 2: 4, 3: 9}`를 출력하도록, 각 숫자를 그 제곱값에 매핑하는 딕셔너리 컴프리헨션으로 코드를 완성하세요

# --seed--

```python
squares = [/]n: n * n for n in range(1, 4)[/]
print(squares)
```

# --answers--

- {
- }
- [
- ]
- (

# --solutions--

```python
squares = {n: n * n for n in range(1, 4)}
print(squares)
```

# --output--

{1: 1, 2: 4, 3: 9}
