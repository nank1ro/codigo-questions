---
language: python
exerciseType: 1
---

# --description--

왼쪽의 표현식은 값을 만들어내는 것이라면 무엇이든 될 수 있습니다: 계산, 함수 호출, 메서드 호출 등입니다.
반복 변수는 원하는 이름을 가질 수 있으며, 이는 대괄호 안에서만 존재합니다.
```python
prices = [10, 20]
with_tax = [price * 1.2 for price in prices]
print(with_tax)  # [12.0, 24.0]
```

# --instructions--

리스트 컴프리헨션을 사용하여 `nums`의 각 값을 제곱한 리스트를 변수 `squares`에 저장하세요

# --seed--

```python
nums = [1, 2, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

리스트 컴프리헨션을 사용해야 합니다

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`squares`는 `[1, 4, 9, 16]`과 같아야 합니다

```python
    def test_1(self):
        self.assertEqual(squares, [1, 4, 9, 16], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 3, 4]
squares = [n * n for n in nums]
```
