---
language: python
exerciseType: 4
---

# --description--

컴프리헨션 안에서는 그 전에 정의된 변수를, 예를 들어 `range()`의 상한값으로 사용할 수 있습니다.

# --instructions--

`limit`보다 작은 7의 배수를 출력하도록 항목을 정렬하세요

# --answers--

```python
limit = 20
```

```python
multiples = [n for n in range(1, limit) if n % 7 == 0]
```

```python
print(multiples)
```

# --solutions--

```python
limit = 20
multiples = [n for n in range(1, limit) if n % 7 == 0]
print(multiples)
```

# --output--

[7, 14]
