---
language: python
exerciseType: 1
---

# --description--

인수를 하나만 받는 데코레이터는 별로 쓸모가 없습니다. **어떤** 함수든 감싸려면 wrapper가 모든 위치 인수를 `*args`로, 모든 키워드 인수를 `**kwargs`로 모아 변경 없이 전달합니다:
```python
def uppercase(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs).upper()
    return wrapper

@uppercase
def greet(name, punctuation="!"):
    return "hello " + name + punctuation

print(greet("ada"))                    # HELLO ADA!
print(greet("ada", punctuation="?"))   # HELLO ADA?
```
wrapper 안에서 `args`는 튜플이고 `kwargs`는 딕셔너리입니다; 호출 시의 `*`와 `**`는 이들을 다시 개별 인수로 풀어줍니다.

# --instructions--

어떤 인수든 받아 감싸진 함수를 그 인수들로 호출하고 그 결과에 `"!"`를 붙여 반환하는 wrapper를 반환하는 데코레이터 `exclaim`을 작성하세요; 그다음 `name`을 받아 `"hello <name>"`을 반환하는 함수 `greet`를 데코레이트하는 데 사용하세요

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

데코레이터를 `@exclaim`으로 적용해야 합니다

```json
{
  "regex": "^@exclaim\\s*$",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greet("ada")`는 `"hello ada!"`를 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(greet("ada"), "hello ada!", "--err-t1--")
```

`exclaim`은 두 인수를 받는 함수와도 동작해야 합니다

```python
    def test_2(self):
        joined = exclaim(lambda a, b: a + " " + b)
        self.assertEqual(joined("good", "day"), "good day!", "--err-t2--")
```

`exclaim`은 키워드 인수를 전달해야 합니다

```python
    def test_3(self):
        tag = exclaim(lambda word, times=1: word * times)
        self.assertEqual(tag("hi", times=2), "hihi!", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def exclaim(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs) + "!"
    return wrapper

@exclaim
def greet(name):
    return "hello " + name
```
