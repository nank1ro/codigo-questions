---
language: python
exerciseType: 1
---

# --description--

내장 `filter(func, iterable)`은 `func`가 참인 값을 반환하는 요소만 남깁니다. `map`처럼 지연 객체를 반환하므로 리스트로 바꿔야 합니다:
```python
numbers = [4, -2, 7, 0]
print(list(filter(lambda n: n > 0, numbers)))  # [4, 7]
```
`filter`에 전달되는 함수를 **술어(predicate)**라고 합니다: 하나의 요소를 받아 그에 대한 예/아니오 질문에 답합니다. 함수 대신 `None`을 전달하면 스스로 참인 요소만 남고 `0`, `""`와 `None`은 버려집니다.

# --instructions--

`filter`를 사용하여 숫자 리스트를 받고 0보다 큰 숫자만 들어 있는 새 리스트를 반환하는 함수 `positives`를 작성하세요

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`filter`를 사용해야 합니다

```json
{
  "regex": "filter\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`positives([3, -1, 0, 5])`는 `[3, 5]`를 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(positives([3, -1, 0, 5]), [3, 5], "--err-t1--")
```

결과는 리스트여야 합니다

```python
    def test_2(self):
        self.assertIsInstance(positives([1, 2]), list, "--err-t2--")
        self.assertEqual(positives([1, 2]), [1, 2], "--err-t2--")
```

`positives([-4, 0])`는 빈 리스트를 반환해야 합니다

```python
    def test_3(self):
        self.assertEqual(positives([-4, 0]), [], "--err-t3--")
        self.assertEqual(positives([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def positives(numbers):
    return list(filter(lambda n: n > 0, numbers))
```
