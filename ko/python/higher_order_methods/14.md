---
language: python
exerciseType: 2
---

# --description--

`any(iterable)`은 **적어도 하나의** 요소가 참이면 `True`를 반환하고, `all(iterable)`은 **모든** 요소가 참이면 `True`를 반환합니다. 둘은 **제너레이터 표현식**과 자연스럽게 어울립니다: 대괄호 없이 쓴 리스트 컴프리헨션으로, 리스트를 만드는 대신 값을 한 번에 하나씩 만들어 냅니다:
```python
ages = [21, 34, 17]
print(any(age >= 18 for age in ages))  # True
print(all(age >= 18 for age in ages))  # False
```
값이 지연되어 만들어지기 때문에, `any`는 첫 번째 `True`에서, `all`은 첫 번째 `False`에서 나머지를 평가하지 않고 멈춥니다. `sum`도 제너레이터 표현식을 받습니다: `sum(1 for age in ages if age >= 18)`은 어른의 수를 셉니다.

# --instructions--

어떤 나이가 어른인지, 모든 나이가 어른인지, 나이가 어른인 경우가 몇 개인지 출력하도록 코드를 완성하여 `True`, `False`, `2`가 출력되게 하세요

# --seed--

```python
ages = [21, 34, 17]
print([/](age >= 18 for age in ages))
print([/](age >= 18 for age in ages))
print([/](1 for age in ages if age >= 18))
```

# --answers--

- any
- all
- sum
- len
- filter

# --solutions--

```python
ages = [21, 34, 17]
print(any(age >= 18 for age in ages))
print(all(age >= 18 for age in ages))
print(sum(1 for age in ages if age >= 18))
```

# --output--

True
False
2
