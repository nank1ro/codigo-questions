---
language: python
exerciseType: 2
---

# --description--

Python에서 함수는 숫자나 문자열처럼 **값**입니다. 변수에 저장하거나, 리스트에 넣거나, 다른 함수에 전달할 수 있습니다. 호출하는 것은 오직 괄호입니다: `shout`는 함수 그 자체이고, `shout("hi")`는 그 결과입니다:
```python
def shout(text):
    return text.upper() + "!"

say = shout
print(say("hi"))  # HI!
```
다른 함수를 매개변수로 받거나 함수를 반환하는 함수를 **고차 함수**라고 합니다. 그 안에서 매개변수는 다른 함수와 마찬가지로 괄호를 붙여 호출됩니다:
```python
def apply(func, value):
    return func(value)

print(apply(shout, "bye"))  # BYE!
print(apply(len, "bye"))    # 3
```

# --instructions--

`apply`가 전달받은 함수를 호출하고, `say`는 호출하지 않고 `shout`를 참조하도록 코드를 완성하여 `HI!`, `3`이 출력되게 하세요

# --seed--

```python
def shout(text):
    return text.upper() + "!"

def apply(func, value):
    return [/](value)

say = [/]
print(say("hi"))
print(apply(len, "bye"))
```

# --answers--

- func
- shout
- shout()
- value
- print

# --solutions--

```python
def shout(text):
    return text.upper() + "!"

def apply(func, value):
    return func(value)

say = shout
print(say("hi"))
print(apply(len, "bye"))
```

# --output--

HI!
3
