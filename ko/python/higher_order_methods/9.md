---
language: python
exerciseType: 1
---

# --description--

`functools` 모듈의 `reduce(func, iterable, initial)`은 시퀀스를 **하나의 값**으로 접습니다. `initial`에서 시작하여 지금까지의 결과와 다음 요소로 `func`를 호출합니다:
```python
from functools import reduce

total = reduce(lambda acc, n: acc + n, [1, 2, 3], 0)
print(total)  # 6
```
단계는 `0 + 1`, 그다음 `1 + 2`, 그다음 `3 + 3`입니다. `initial`이 생략되면 첫 번째 요소가 시작 값으로 사용되지만, 그 경우 빈 시퀀스는 `TypeError`를 발생시키므로 시퀀스가 비어 있을 수 있다면 항상 초기 값을 주세요.

# --instructions--

`reduce`를 사용하여 숫자 리스트를 받아 모두 곱한 값을 반환하는 함수 `product`를 작성하세요; 빈 리스트의 곱은 `1`이어야 합니다

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`reduce`를 사용해야 합니다

```json
{
  "regex": "reduce\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`product([2, 3, 4])`는 `24`를 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(product([2, 3, 4]), 24, "--err-t1--")
```

`product([5])`는 `5`를 반환해야 합니다

```python
    def test_2(self):
        self.assertEqual(product([5]), 5, "--err-t2--")
```

`product([])`는 `1`을 반환해야 합니다

```python
    def test_3(self):
        self.assertEqual(product([]), 1, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import reduce

def product(numbers):
    return reduce(lambda acc, n: acc * n, numbers, 1)
```
