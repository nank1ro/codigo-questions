---
language: python
exerciseType: 1
---

# --description--

`partial`은 옵션을 받는 내장 함수와 함께 쓰면 유용합니다. `int(text, base=16)`은 16진수 문자열을 파싱합니다; 밑을 고정하면 `map`에 딱 맞는 인수 하나짜리 변환기가 됩니다:
```python
from functools import partial

hex_to_int = partial(int, base=16)
print(hex_to_int("ff"))                      # 255
print(list(map(hex_to_int, ["a", "10"])))    # [10, 16]
```
partial 객체는 감싸고 있는 것을 기억합니다: `hex_to_int.func`는 `int`이고, `hex_to_int.keywords`는 `{'base': 16}`입니다.

# --instructions--

`partial`로 두 함수를 정의하세요: 16진수 문자열을 `int`로 변환하는 `hex_to_int`와 2진수 문자열을 `int`로 변환하는 `binary_to_int`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`hex_to_int("ff")`는 `255`를 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(hex_to_int("ff"), 255, "--err-t1--")
        self.assertEqual(hex_to_int("10"), 16, "--err-t1--")
```

`binary_to_int("1010")`는 `10`을 반환해야 합니다

```python
    def test_2(self):
        self.assertEqual(binary_to_int("1010"), 10, "--err-t2--")
        self.assertEqual(binary_to_int("11"), 3, "--err-t2--")
```

둘 다 `int`를 감싸는 partial 객체여야 합니다

```python
    def test_3(self):
        self.assertIs(hex_to_int.func, int, "--err-t3--")
        self.assertIs(binary_to_int.func, int, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import partial

hex_to_int = partial(int, base=16)
binary_to_int = partial(int, base=2)
```
