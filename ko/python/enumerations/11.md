---
language: python
exerciseType: 1
---

# --description--

`Flag`는 멤버들을 **조합**할 수 있는 열거형입니다: 하나의 값이 옵션 집합처럼 여러 멤버를 한 번에 담을 수 있습니다.
멤버는 `auto()`로 선언하세요. `Flag`에서 `auto()`는 2의 거듭제곱(`1`, `2`, `4`, ...)을 할당하므로 모든 조합이 고유한 값을 가집니다:
```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```
`|`로 멤버를 조합하고, `in`으로 멤버가 조합에 포함되어 있는지 확인하고, `value`로 결과 숫자를 확인하세요:
```python
full = Permission.READ | Permission.WRITE | Permission.EXECUTE
print(Permission.WRITE in full)  # True
print(full.value)                # 7
print(full)                      # Permission.READ|WRITE|EXECUTE
```

# --instructions--

`|`를 사용하여 `Permission.READ`와 `Permission.WRITE`의 조합을 `editor`에 저장하고, `Permission.WRITE`가 `editor`에 있는지를 `can_write`에, `editor`의 값을 `bits`에 저장하세요

# --seed--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`|` 연산자를 사용해야 합니다

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`in` 연산자를 사용해야 합니다

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`editor`는 `Permission.READ | Permission.WRITE`와 같아야 합니다

```python
    def test_1(self):
        self.assertEqual(editor, Permission.READ | Permission.WRITE, "--err-t1--")
```

`can_write`는 `True`여야 합니다

```python
    def test_2(self):
        self.assertEqual(can_write, True, "--err-t2--")
```

`bits`는 `3`과 같아야 합니다

```python
    def test_3(self):
        self.assertEqual(bits, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()

editor = Permission.READ | Permission.WRITE
can_write = Permission.WRITE in editor
bits = editor.value
```
