---
language: python
exerciseType: 1
---

# --description--

열거형의 모든 멤버는 두 개의 속성을 가집니다: 클래스에 작성한 식별자인 `name`과, 여러분이 할당한 값인 `value`입니다:
```python
from enum import Enum

class Color(Enum):
    RED = 1
    GREEN = 2

print(Color.GREEN.name)   # GREEN
print(Color.GREEN.value)  # 2
```
값은 정수뿐만 아니라 어떤 타입이든 될 수 있습니다: 문자열, 튜플, 실수가 흔한 선택입니다.
멤버는 일반 객체이므로 변수에 저장해 두었다가 나중에 그 속성을 읽을 수 있습니다:
```python
chosen = Color.RED
print(chosen.value)  # 1
```

# --instructions--

`home`에는 멤버 `Planet.EARTH`를, `label`에는 그 이름을, `position`에는 그 값을 저장하세요

# --seed--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`.name`을 사용해야 합니다

```json
{
  "regex": "\\.name\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`.value`를 사용해야 합니다

```json
{
  "regex": "\\.value\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`home`은 `Planet.EARTH`여야 합니다

```python
    def test_1(self):
        self.assertIs(home, Planet.EARTH, "--err-t1--")
```

`label`은 `"EARTH"`와 같아야 합니다

```python
    def test_2(self):
        self.assertEqual(label, "EARTH", "--err-t2--")
```

`position`은 `3`과 같아야 합니다

```python
    def test_3(self):
        self.assertEqual(position, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3

home = Planet.EARTH
label = home.name
position = home.value
```
