---
language: python
exerciseType: 4
---

# --description--

열거형 클래스는 **반복 가능**합니다: 클래스에 대한 `for` 반복문은 선언된 순서대로 모든 멤버를 방문합니다:
```python
for color in Color:
    print(color.name, color.value)
```
`len()`은 열거형이 가진 멤버 수를 반환하고, `list(Color)`는 멤버들의 리스트를 만듭니다:
```python
print(len(Color))    # 3
print(list(Color))   # [<Color.RED: 1>, <Color.GREEN: 2>, <Color.BLUE: 3>]
```
리스트 안에서는 멤버가 `repr()`로 표시되며, 여기에는 꺾쇠 괄호 사이에 값이 포함됩니다.

# --instructions--

항목을 정렬하여 열거형 `Weekday`를 선언하고, 멤버 수를 출력한 다음, 각 멤버의 이름과 값을 한 줄에 하나씩 출력하세요

# --answers--

```python
from enum import Enum
```

```python
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
```

```python
print(len(Weekday))
```

```python
for day in Weekday:
```

```python
    print(day.name, day.value)
```

# --solutions--

```python
from enum import Enum
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
print(len(Weekday))
for day in Weekday:
    print(day.name, day.value)
```

# --output--

3
MON 1
TUE 2
WED 3
