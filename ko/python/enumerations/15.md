---
language: python
exerciseType: 4
---

# --description--

로그 줄이나 파일처럼 외부에서 들어오는 데이터를 처리할 때는 클래스를 반복하는 것과 이름으로 멤버를 조회하는 것이 잘 어울립니다.
클래스에 대한 딕셔너리 컴프리헨션이 멤버마다 하나의 항목을 준비하고, `Level[name]`이 들어오는 각 문자열을 일치하는 멤버로 변환합니다:
```python
counts = {level: 0 for level in Level}
counts[Level["INFO"]] += 1
```
열거형은 선언 순서를 유지하므로, 나중에 `counts`를 반복하면 그와 같은 순서로 멤버를 얻습니다.

# --instructions--

항목을 정렬하여 자동 값을 가진 `Level`을 선언하고, 모든 멤버의 카운터를 준비하고, 리스트의 이름을 세고, 각 멤버 이름과 그 개수를 출력하세요

# --answers--

```python
from enum import Enum, auto
```

```python
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
```

```python
counts = {level: 0 for level in Level}
```

```python
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
```

```python
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --solutions--

```python
from enum import Enum, auto
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
counts = {level: 0 for level in Level}
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --output--

DEBUG: 0
INFO: 2
ERROR: 1
