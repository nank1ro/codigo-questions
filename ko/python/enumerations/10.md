---
language: python
exerciseType: 2
---

# --description--

두 멤버가 같은 값을 가지면 두 번째 멤버는 새로운 멤버가 아니라 첫 번째 멤버의 **별칭(alias)**입니다:
```python
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # alias of ACTIVE

print(Status.ENABLED)  # Status.ACTIVE
print(len(Status))     # 1
```
별칭은 반복할 때 건너뛰어지며 `len()`으로 세지 않습니다.
보통 중복된 값은 실수입니다. `enum`에서 임포트하는 `unique` 데코레이터는 별칭을 가진 열거형이 선언되는 즉시 Python이 `ValueError`를 발생시키게 합니다:
```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # ValueError: duplicate values found
```

# --instructions--

코드를 완성하여 `unique` 데코레이터를 임포트하고 `Status`에 적용하여 중복된 값이 거부되도록 한 다음, 멤버 수를 출력하세요

# --seed--

```python
from enum import Enum, [/]

[/]
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --answers--

- unique
- @unique
- auto
- @auto
- @property
- @classmethod

# --solutions--

```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --output--

2
