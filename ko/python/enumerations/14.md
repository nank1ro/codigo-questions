---
language: python
exerciseType: 2
---

# --description--

멤버 값은 **튜플**일 수 있으며, 이를 통해 각 멤버에 여러 조각의 데이터를 붙일 수 있습니다.
열거형이 `__init__` 메서드를 정의하면 Python은 각 멤버마다 한 번씩 호출하면서 튜플을 매개변수로 언패킹하므로, 각 조각을 자신만의 속성에 저장할 수 있습니다:
```python
from enum import Enum

class Planet(Enum):
    EARTH = (5.97, 6371)
    MARS = (0.64, 3390)

    def __init__(self, mass, radius):
        self.mass = mass
        self.radius = radius

print(Planet.MARS.radius)  # 3390
print(Planet.MARS.value)   # (0.64, 3390)
```
멤버의 `value`는 전체 튜플 그대로입니다.

# --instructions--

코드를 완성하여 `__init__`에서 각 멤버의 튜플을 언패킹하고 두 번째 요소를 속성 `metal`에 저장한 다음, `Coin.PENNY`의 금속을 출력하세요

# --seed--

```python
from enum import Enum

class Coin(Enum):
    PENNY = (1, "copper")
    DIME = (10, "silver")

    def [/](self, cents, metal):
        self.cents = cents
        self.[/] = metal

print(Coin.DIME.cents)
print(Coin.PENNY.[/])
```

# --answers--

- __init__
- metal
- metal
- __new__
- value
- name
- cents

# --solutions--

```python
from enum import Enum

class Coin(Enum):
    PENNY = (1, "copper")
    DIME = (10, "silver")

    def __init__(self, cents, metal):
        self.cents = cents
        self.metal = metal

print(Coin.DIME.cents)
print(Coin.PENNY.metal)
```

# --output--

10
copper
