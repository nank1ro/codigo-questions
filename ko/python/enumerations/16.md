---
language: python
exerciseType: 1
---

# --description--

일반 메서드 외에도 열거형은 `@classmethod`로 **클래스 메서드**를 정의할 수 있습니다. 클래스 메서드는 열거형 클래스 자신을 `cls`로 받으므로, 멤버를 찾는 대체적인 방법을 담기에 알맞은 자리입니다:
```python
class Color(Enum):
    RED = auto()
    BLUE = auto()

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]

print(Color.parse(" blue "))  # Color.BLUE
```
`auto()`와 메서드, 프로퍼티, 조회가 함께 어우러져 자신만의 동작을 지니는 열거형을 만들 수 있습니다.

# --instructions--

`auto()`를 사용하여 멤버 `NORTH`, `EAST`, `SOUTH`, `WEST`를 이 순서대로 가진 열거형 `Direction`을 선언하세요.
반대쪽 멤버를 반환하는 메서드 `opposite()`(`NORTH`와 `SOUTH`, `EAST`와 `WEST`)와, 대소문자와 앞뒤 공백을 무시하고 이름이 `text`와 일치하는 멤버를 반환하는 클래스 메서드 `parse(text)`를 추가하세요

# --seed--

```python
from enum import Enum, auto
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`auto()`를 사용해야 합니다

```json
{
  "regex": "auto\\(\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`@classmethod` 데코레이터를 사용해야 합니다

```json
{
  "regex": "@classmethod",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Direction`은 `1`부터 `4`까지의 값을 가진 멤버 `NORTH`, `EAST`, `SOUTH`, `WEST`를 가져야 합니다

```python
    def test_1(self):
        self.assertEqual([d.name for d in Direction], ["NORTH", "EAST", "SOUTH", "WEST"], "--err-t1--")
        self.assertEqual([d.value for d in Direction], [1, 2, 3, 4], "--err-t1--")
```

`Direction.NORTH.opposite()`은 `Direction.SOUTH`여야 하고 `Direction.WEST.opposite()`는 `Direction.EAST`여야 합니다

```python
    def test_2(self):
        self.assertIs(Direction.NORTH.opposite(), Direction.SOUTH, "--err-t2--")
        self.assertIs(Direction.SOUTH.opposite(), Direction.NORTH, "--err-t2--")
        self.assertIs(Direction.EAST.opposite(), Direction.WEST, "--err-t2--")
        self.assertIs(Direction.WEST.opposite(), Direction.EAST, "--err-t2--")
```

`Direction.parse(" west ")`는 `Direction.WEST`여야 합니다

```python
    def test_3(self):
        self.assertIs(Direction.parse(" west "), Direction.WEST, "--err-t3--")
        self.assertIs(Direction.parse("North"), Direction.NORTH, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    EAST = auto()
    SOUTH = auto()
    WEST = auto()

    def opposite(self):
        opposites = {
            Direction.NORTH: Direction.SOUTH,
            Direction.SOUTH: Direction.NORTH,
            Direction.EAST: Direction.WEST,
            Direction.WEST: Direction.EAST,
        }
        return opposites[self]

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]
```
