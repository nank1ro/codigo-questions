---
language: python
exerciseType: 1
---

# --description--

일반 `Enum` 멤버는 `<`로 비교하거나 숫자에 더할 수 없습니다.
멤버들이 순서가 필요한 **수준**을 나타낼 때는 대신 `IntEnum`을 상속하세요: 멤버들이 정수이기도 하므로 비교, 산술, 정렬을 지원합니다:
```python
from enum import IntEnum

class Priority(IntEnum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

print(Priority.HIGH > Priority.LOW)  # True
print(Priority.LOW + 1)              # 2
print(int(Priority.MEDIUM))          # 2
```
`IntEnum` 멤버는 자신의 정수 값과 같기도 합니다: `Priority.LOW == 1`은 `True`입니다.

# --instructions--

`is_urgent`에는 `Priority.HIGH`가 `Priority.MEDIUM`보다 큰지를 저장하고, `ordered`에는 `sorted()`로 정렬한 리스트 `tasks`를, `score`에는 `Priority.LOW`와 `Priority.HIGH`의 합을 저장하세요

# --seed--

```python
from enum import IntEnum

class Priority(IntEnum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

tasks = [Priority.HIGH, Priority.LOW, Priority.MEDIUM]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`sorted()`를 사용해야 합니다

```json
{
  "regex": "sorted\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`is_urgent`는 `True`여야 합니다

```python
    def test_1(self):
        self.assertEqual(is_urgent, True, "--err-t1--")
```

`ordered`는 `[Priority.LOW, Priority.MEDIUM, Priority.HIGH]`와 같아야 합니다

```python
    def test_2(self):
        self.assertEqual(ordered, [Priority.LOW, Priority.MEDIUM, Priority.HIGH], "--err-t2--")
```

`score`는 `4`와 같아야 합니다

```python
    def test_3(self):
        self.assertEqual(score, 4, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import IntEnum

class Priority(IntEnum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

tasks = [Priority.HIGH, Priority.LOW, Priority.MEDIUM]
is_urgent = Priority.HIGH > Priority.MEDIUM
ordered = sorted(tasks)
score = Priority.LOW + Priority.HIGH
```
