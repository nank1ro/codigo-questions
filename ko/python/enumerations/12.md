---
language: python
exerciseType: 4
---

# --description--

열거형은 `match` 문(Python 3.10부터 사용 가능)과 자연스럽게 어울립니다. `match` 문은 값을 일련의 `case` 패턴과 비교하여 일치하는 첫 번째 패턴을 실행합니다:
```python
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
```
항상 `Light.RED`처럼 멤버를 클래스와 함께 작성하세요: `case RED:` 같은 맨 이름은 아무것도 비교하지 않고, 그저 값을 새 변수 `RED`에 담아 모든 것에 일치시킬 뿐입니다.
와일드카드 `case _:`는 기본값이며 마지막에 와야 합니다. 그 뒤에 오는 패턴은 절대 도달할 수 없기 때문입니다.

# --instructions--

항목을 정렬하여 열거형 `Light`를 선언하고, `RED` case를 먼저, 그다음 `GREEN` case, 마지막에 기본값을 넣어 `action()`을 정의한 다음, `Light.YELLOW`의 동작을 출력하세요

# --answers--

```python
from enum import Enum
```

```python
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
```

```python
def action(light):
```

```python
    match light:
```

```python
        case Light.RED:
            return "Stop"
```

```python
        case Light.GREEN:
            return "Go"
```

```python
        case _:
            return "Slow down"
```

```python
print(action(Light.YELLOW))
```

# --solutions--

```python
from enum import Enum
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
print(action(Light.YELLOW))
```

# --output--

Slow down
