---
language: python
exerciseType: 1
---

# --description--

`finally` 블록은 **무슨 일이 있어도** 실행됩니다: `try` 블록이 문제없이 끝난 후에도, `except` 블록 후에도, 아무도 잡지 않은 예외가 바깥쪽으로 전파되는 중에도, `try`나 `except` 블록이 `return`을 실행할 때에도 마찬가지입니다:
```python
def read(text):
    try:
        return int(text)
    except ValueError:
        return 0
    finally:
        print("done")
```
두 경로 모두 값이 함수를 떠나기 전에 `done`을 출력합니다. 이 보장이 `finally`의 존재 이유입니다: 파일 닫기, 락 해제, 설정 복원 같은 일이죠. 전체 모양은 `try` / `except` / `else` / `finally`입니다; `try`에는 최소한 하나의 `except`나 `finally`가 필요하고, `else`는 `except`와 함께 있을 때만 동작합니다.

# --instructions--

빈 리스트에서 시작하여 `int(text)`를 추가하고, `int(text)`가 `ValueError`를 발생시키면 대신 문자열 `"bad"`를 추가하며, `finally` 블록에서는 항상 `"done"`을 추가한 뒤 그 리스트를 반환하는 함수 `record(text)`를 작성하세요

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`record("5")`는 `[5, "done"]`을 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(record("5"), [5, "done"], "--err-t1--")
```

`record("x")`는 `["bad", "done"]`을 반환해야 합니다

```python
    def test_2(self):
        self.assertEqual(record("x"), ["bad", "done"], "--err-t2--")
```

각 호출은 새 리스트에서 시작해야 합니다

```python
    def test_3(self):
        record("1")
        self.assertEqual(record("2"), [2, "done"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def record(text):
    log = []
    try:
        log.append(int(text))
    except ValueError:
        log.append("bad")
    finally:
        log.append("done")
    return log
```
