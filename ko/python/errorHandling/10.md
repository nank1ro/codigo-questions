---
language: python
exerciseType: 4
---

# --description--

`try` 블록 뒤에는 `else` 블록이 올 수 있으며, 이것은 **`try` 블록이 예외 없이 끝났을 때만** 실행됩니다:
```python
try:
    number = int(text)
except ValueError:
    print("not a number")
else:
    print(number * 2)
```
`print(number * 2)`를 `try` 블록 안에 넣어도 동작은 하지만, 그렇게 하면 출력 자체가 발생시킨 `ValueError`가 변환 실패로 오인될 수 있습니다. `else`는 `try` 블록을 보호 대상인 단 하나의 문장으로 유지하고, 성공했을 때 일어나야 할 모든 일을 담습니다.

# --instructions--

항목들을 정렬하여 `parse`를 정의한 다음 `"21"`과 `"abc"`로 순서대로 호출하고, 성공하면 두 배가 된 숫자를, 실패하면 메시지를 출력하는 프로그램을 만드세요

# --answers--

```python
def parse(text):
```

```python
    try:
        number = int(text)
```

```python
    except ValueError:
        print(f"{text} is not a number")
```

```python
    else:
        print(number * 2)
```

```python
parse("21")
```

```python
parse("abc")
```

# --solutions--

```python
def parse(text):
    try:
        number = int(text)
    except ValueError:
        print(f"{text} is not a number")
    else:
        print(number * 2)
parse("21")
parse("abc")
```

# --output--

42
abc is not a number
