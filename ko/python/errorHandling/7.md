---
language: python
exerciseType: 1
---

# --description--

여러 실패가 **같은** 복구를 필요로 할 때는, 블록을 반복하는 것보다 하나의 절에 튜플로 나열하는 쪽이 더 간결합니다:
```python
try:
    value = int(text) / divisor
except (ValueError, ZeroDivisionError):
    value = 0
```
괄호는 필수입니다: `except ValueError, ZeroDivisionError:`는 Python 3에서 문법 오류입니다. 튜플은 여전히 명시적인 타입 목록입니다.

# --instructions--

`a / b`를 반환하고, 나눗셈이 `ZeroDivisionError` 또는 `TypeError`를 발생시키면 `None`을 반환하는 함수 `safe_ratio(a, b)`를 작성하세요. 두 타입을 튜플로 담은 하나의 `except` 절을 사용하세요

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_ratio(9, 3)`은 `3.0`을 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(safe_ratio(9, 3), 3.0, "--err-t1--")
```

`safe_ratio(1, 0)`은 `None`을 반환해야 합니다

```python
    def test_2(self):
        self.assertIsNone(safe_ratio(1, 0), "--err-t2--")
```

`safe_ratio(1, "x")`는 `None`을 반환해야 합니다

```python
    def test_3(self):
        self.assertIsNone(safe_ratio(1, "x"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_ratio(a, b):
    try:
        return a / b
    except (ZeroDivisionError, TypeError):
        return None
```
