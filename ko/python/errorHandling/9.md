---
language: python
exerciseType: 1
---

# --description--

예외는 객체이며, `as`는 그 객체를 이름에 묶어서 핸들러가 들여다볼 수 있게 해줍니다:
```python
try:
    int("abc")
except ValueError as e:
    print(e)                   # invalid literal for int() with base 10: 'abc'
    print(type(e).__name__)    # ValueError
```
`str(e)` — `print(e)`와 f-string 슬롯이 사용하는 것입니다 — 는 예외가 만들어질 때 담긴 메시지를 주고, `type(e).__name__`은 클래스 이름을 텍스트로 줍니다. `as`가 묶은 이름은 `except` 블록 안에서만 존재하며, 블록이 끝나면 Python이 그 이름을 삭제합니다.

# --instructions--

`int(text)`를 반환하고, 그것이 `ValueError`를 발생시킬 때는 예외 클래스 이름, `": "`, 예외 메시지로 이루어진 문자열(예: `"ValueError: ..."`)을 반환하는 함수 `error_label(text)`를 작성하세요

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`error_label("8")`은 `8`을 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(error_label("8"), 8, "--err-t1--")
```

`error_label("abc")`는 클래스 이름으로 시작해야 합니다

```python
    def test_2(self):
        self.assertTrue(error_label("abc").startswith("ValueError: "), "--err-t2--")
```

`error_label("abc")`는 `abc`를 언급하는 메시지도 포함해야 합니다

```python
    def test_3(self):
        self.assertIn("abc", error_label("abc")[12:], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def error_label(text):
    try:
        return int(text)
    except ValueError as e:
        return f"{type(e).__name__}: {e}"
```
