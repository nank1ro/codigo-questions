---
language: python
exerciseType: 1
---

# --description--

`try` 블록은 예외를 발생시키는 **첫 번째** 문장에서 멈춥니다; 그 뒤의 줄들은 건너뛰고 제어는 `except` 블록으로 이동합니다. `try` 블록 안의 어떤 것도 되돌려지지 않으므로, 가능한 한 짧게 유지하세요:
```python
def half(n):
    try:
        return 10 / n
    except ZeroDivisionError:
        return "undefined"

print(half(2))  # 5.0
print(half(0))  # undefined
```
`except` 안의 `return`은 다른 `return`과 똑같이 동작하므로, `try`/`except`는 프로그램을 강종하는 대신 대체 값을 돌려주는 자연스러운 방법이 됩니다.

# --instructions--

`a / b`를 반환하고, `b`가 `0`일 때는 `ZeroDivisionError`를 잡아 문자열 `"undefined"`를 반환하는 함수 `safe_divide(a, b)`를 작성하세요

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_divide(9, 3)`은 `3.0`을 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(safe_divide(9, 3), 3.0, "--err-t1--")
```

`safe_divide(1, 0)`은 `"undefined"`를 반환해야 합니다

```python
    def test_2(self):
        self.assertEqual(safe_divide(1, 0), "undefined", "--err-t2--")
```

`safe_divide(-5, 2)`는 `-2.5`를 반환해야 합니다

```python
    def test_3(self):
        self.assertEqual(safe_divide(-5, 2), -2.5, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "undefined"
```
