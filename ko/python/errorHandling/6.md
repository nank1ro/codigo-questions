---
language: python
exerciseType: 2
---

# --description--

하나의 `try` 블록 뒤에는 **여러 개의** `except` 절이 올 수 있으며, 각 절은 서로 다른 실패를 서로 다른 복구 방법으로 처리합니다. Python은 발생한 예외를 위에서부터 아래로 비교하여 일치하는 **첫 번째** 절을 실행하고, 나머지는 건너뜁니다:
```python
try:
    value = 100 / int(text)
except ValueError:
    print("not a number")
except ZeroDivisionError:
    print("cannot divide by zero")
```
첫 번째 일치가 이기므로, 타입들이 서로 관련되어 있을 때는 순서가 중요합니다: 일반 타입의 절이 더 구체적인 타입의 절 위에 놓이면 항상 일반 절이 이겨서 구체적인 절에는 도달할 수 없게 됩니다.

# --instructions--

읽을 수 없는 텍스트와 0 나눗수가 각각 따로 보고되도록 코드를 완성하여 출력 `2.0 not a number cannot divide by zero`를 만들어보세요

# --seed--

```python
def describe(text):
    try:
        return 10 / int(text)
    except [/]:
        return "not a number"
    except [/]:
        return "cannot divide by zero"

print(describe("5"), describe("abc"), describe("0"))
```

# --answers--

- ValueError
- ZeroDivisionError
- TypeError
- KeyError

# --solutions--

```python
def describe(text):
    try:
        return 10 / int(text)
    except ValueError:
        return "not a number"
    except ZeroDivisionError:
        return "cannot divide by zero"

print(describe("5"), describe("abc"), describe("0"))
```

# --output--

2.0 not a number cannot divide by zero
