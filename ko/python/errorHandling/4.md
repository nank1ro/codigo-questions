---
language: python
exerciseType: 2
---

# --description--

`except` 절은 이름을 명시한 타입과 그 하위 클래스만 잡습니다. 이것이 핵심입니다: 그 밖의 모든 것은 계속 바깥쪽으로 전파되므로, 예상하지 못한 버그는 삼켜지는 대신 여전히 트레이스백으로 나타납니다.

`int(text)`는 텍스트가 정수를 나타내지 않을 때 **`ValueError`**를 발생시키므로, 사용자 입력을 읽을 때 명시할 타입은 바로 이것입니다:
```python
def to_int(text):
    try:
        return int(text)
    except ValueError:
        return 0

print(to_int("12"))    # 12
print(to_int("oops"))  # 0
```
여기서 `ValueError`를 명시하는 것은 형식이 아니라 결정입니다: `int(None)`은 `TypeError`를 발생시키는데, 이 함수는 일부러 그것을 **잡지 않습니다**. `None`을 전달하는 것은 프로그래밍 실수이며 드러나야 하기 때문입니다.

# --instructions--

정수가 아닌 텍스트가 `0`으로 변환되도록 코드를 완성하여 출력 `12 0`을 만들어보세요

# --seed--

```python
def to_int(text):
    try:
        return [/](text)
    except [/]:
        return 0

print(to_int("12"), to_int("oops"))
```

# --answers--

- int
- ValueError
- float
- TypeError
- str

# --solutions--

```python
def to_int(text):
    try:
        return int(text)
    except ValueError:
        return 0

print(to_int("12"), to_int("oops"))
```

# --output--

12 0
