---
language: python
exerciseType: 1
---

# --instructions--

여는 `{`와 닫는 `}` 중괄호를 사용하여 포맷 문자열의 빈 공간에 숫자 `500`을 삽입하세요

# --seed--

```python
print(f"There are  items")
```

# --solutions--

```python
print(f"There are {500} items")
```

# --output--

There are 500 items
