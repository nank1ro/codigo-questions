---
language: python
exerciseType: 1
---

# --instructions--

`store`에 `60`개의 상품이 있습니다. 하루 동안 `50`개를 판매하고 `55`개를 새로 구매(`bought`)했습니다.
`store` 수량을 업데이트하세요.

# --seed--

```python
store = 60
sold = 50
bought = 55
# write after this line
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`store` 수량은 65와 같아야 합니다

```python
    def test_1(self):
        self.assertEqual(store, 65, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
store = 60
sold = 50
bought = 55
store += bought - sold
```
