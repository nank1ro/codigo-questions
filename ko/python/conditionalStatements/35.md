---
language: python
exerciseType: 2
---

# --description--

콜론은 `if` 줄에서 없어서는 안 될 유일한 요소입니다: 조건을 마무리하고 블록을 엽니다. Python에서 조건을 감싸는 괄호는 선택 사항이므로, `if True:`와 `if (True):`는 동일하게 동작합니다.

# --instructions--

코드 블록이 실행되도록 빈칸을 채우세요

# --seed--

```python
[/][/][/][/][/]
    print("Hello User!")
```

# --answers--

- True
- False
- "True"
- "False"
- :
- ;
- if 
- else 
- elif 
- (
- )

# --solutions--

```python
if (True):
    print("Hello User!")
```

# --output--

Hello User!
