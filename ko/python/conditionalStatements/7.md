---
language: python
exerciseType: 2
---

# --description--

모든 조건문은 이를 시작하는 `if` 키워드가 필요합니다. 이 키워드가 바로 아래 블록이 조건이 성립할 때만 실행된다는 것을 Python에 알려줍니다.

# --instructions--

빈칸을 채워서 조건문을 만드세요

# --seed--

```python
[/] (True):
    print("It's True!")
```

# --answers--

- if
- else
- elif
- elseif

# --solutions--

```python
if (True):
    print("It's True!")
```

# --output--

It's True!
