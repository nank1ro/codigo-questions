---
language: python
exerciseType: 2
---

# --description--

`if` 아래의 블록은 절대 비어 있을 수 없습니다: 콜론 뒤에 들여쓰기된 줄이 없으면 Python은 `IndentationError`를 발생시킵니다. 아직 실행할 내용이 없을 때는 보통 `pass`를 자리표시자로 사용합니다.

# --instructions--

코드 블록이 실행되도록 빈칸을 채우세요

# --seed--

```python
[/][/]
    [/]
```

# --answers--

- if (True)
- if (False)
- if ("True")
- if ("False")
- if
- else
- elif
- :
- ;
- print("Hello!")

# --solutions--

```python
if (True):
    print("Hello!")
```

# --output--

Hello!
