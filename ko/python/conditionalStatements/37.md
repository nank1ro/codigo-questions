---
language: python
exerciseType: 2
---

# --description--

`not` 연산자는 불리언 값을 뒤집습니다: `not True`는 `False`이고, `not False`는 `True`입니다.
```python
is_online = False
print(not is_online)  # True
```

# --instructions--

코드 블록이 실행되도록 빈칸을 채우세요

# --seed--

```python
is_morning = False
is_afternoon = not is_morning
if ([/]):
    print("Good afternoon!")
```

# --answers--

- is_morning
- is_afternoon
- "is_morning"
- "is_afternoon"

# --solutions--

```python
is_morning = False
is_afternoon = not is_morning
if (is_afternoon):
    print("Good afternoon!")
```

# --output--

Good afternoon!
