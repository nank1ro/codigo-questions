---
language: python
exerciseType: 1
---

# --description--

**그렇다면 언제 `def`를 사용해야 할까요?**
lambda는 인자로 전달되는 짧고 일회성인 함수에 적합합니다.
로직에 이름, 여러 줄, docstring이 필요하거나 여러 곳에서 재사용된다면, `def` 함수가 더 명확합니다.
마지막으로 한 가지 팁: `sorted()`는 `reverse=True`도 받을 수 있어서 가장 큰 값부터 얻을 수 있습니다.
```python
print(sorted([3, 1, 2], reverse=True))  # [3, 2, 1]
```

# --instructions--

`ranking`에 `grade`가 높은 순에서 낮은 순으로 정렬한 리스트 `students`를 저장하고, 그다음 `names`에 `ranking`의 이름 리스트를 `sorted()`와 `map()`을 lambda와 함께 사용하여 저장하세요

# --seed--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`lambda` 표현식을 사용해야 합니다

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`map()`을 사용해야 합니다

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ranking`은 학생 `Cy`로 시작해야 합니다

```python
    def test_1(self):
        self.assertEqual(ranking[0]["name"], "Cy", "--err-t1--")
```

`names`는 `['Cy', 'Ana', 'Bo']`와 같아야 합니다

```python
    def test_2(self):
        self.assertEqual(names, ["Cy", "Ana", "Bo"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
ranking = sorted(students, key=lambda s: s["grade"], reverse=True)
names = list(map(lambda s: s["name"], ranking))
```
