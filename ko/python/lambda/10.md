---
language: python
exerciseType: 2
---

# --description--

`filter()`는 함수가 `True`를 반환하는 항목만 남깁니다.
```python
nums = [5, 12, 8, 20]
big = list(filter(lambda n: n > 10, nums))
print(big)  # [12, 20]
```

# --instructions--

짝수만 남도록 코드를 완성하여 프로그램이 `[2, 4]`를 출력하게 하세요

# --seed--

```python
nums = [1, 2, 3, 4]
evens = list([/](lambda n: n % 2 [/] 0, nums))
print(evens)
```

# --answers--

- filter
- map
- sorted
- ==
- !=

# --solutions--

```python
nums = [1, 2, 3, 4]
evens = list(filter(lambda n: n % 2 == 0, nums))
print(evens)
```

# --output--

[2, 4]
