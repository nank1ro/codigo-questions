---
language: python
exerciseType: 1
---

# --description--

lambda의 매개변수도 **기본값**을 지원합니다.
```python
greet = lambda name="World": f"Hello, {name}!"
print(greet())       # Hello, World!
print(greet("Ana"))  # Hello, Ana!
```

# --instructions--

변수 `multiply`에 `n`과 기본값이 `2`인 두 번째 매개변수 `factor`를 받아, `n`에 `factor`를 곱한 값을 반환하는 lambda를 저장하세요

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`lambda` 표현식을 사용해야 합니다

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`multiply(5)`는 `10`을 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(multiply(5), 10, "--err-t1--")
```

`multiply(5, 3)`는 `15`를 반환해야 합니다

```python
    def test_2(self):
        self.assertEqual(multiply(5, 3), 15, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
multiply = lambda n, factor=2: n * factor
```
