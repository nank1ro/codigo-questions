---
language: python
exerciseType: 4
---

# --description--

`max()`와 `min()`도 `sorted()`와 마찬가지로 `key` 함수를 받을 수 있습니다.
```python
words = ["hi", "hello", "hey"]
print(max(words, key=lambda w: len(w)))  # hello
```

# --instructions--

항목을 정렬하여 대소문자를 무시한 알파벳 순서로 이름을 출력한 다음, 가장 긴 이름을 출력하세요

# --answers--

```python
names = ["bob", "Alice", "Charlie"]
```

```python
ordered = sorted(names, key=lambda s: s.lower())
```

```python
longest = max(ordered, key=lambda s: len(s))
```

```python
print(ordered)
print(longest)
```

# --solutions--

```python
names = ["bob", "Alice", "Charlie"]
ordered = sorted(names, key=lambda s: s.lower())
longest = max(ordered, key=lambda s: len(s))
print(ordered)
print(longest)
```

# --output--

['Alice', 'bob', 'Charlie']
Charlie
