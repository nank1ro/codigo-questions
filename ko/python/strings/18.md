---
language: python
exerciseType: 1
---

# --description--

**슬러그(slug)**는 제목을 URL에 적합한 형태로 만든 것입니다: 소문자로, 앞뒤 공백 없이, 단어는 대시로 구분됩니다. 예: `hello-world`.
슬러그를 만드는 것은 지금까지 배운 메서드 `strip()`, `lower()`, `replace()`를 연결하는 것일 뿐입니다.

# --instructions--

`title`의 앞뒤 공백을 제거하고, 소문자로 바꾸고, 모든 공백을 `-`로 바꾼 값을 반환하는 함수 `slugify(title)`을 작성하세요

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`replace()`를 사용해야 합니다

```json
{
  "regex": "\\.replace\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`slugify("Hello World")`는 `"hello-world"`를 반환해야 합니다

```python
    def test_1(self):
        self.assertEqual(slugify("Hello World"), "hello-world", "--err-t1--")
```

`slugify("  Learn Python Strings  ")`는 `"learn-python-strings"`를 반환해야 합니다

```python
    def test_2(self):
        self.assertEqual(slugify("  Learn Python Strings  "), "learn-python-strings", "--err-t2--")
```

`slugify("codigo")`는 `"codigo"`를 반환해야 합니다

```python
    def test_3(self):
        self.assertEqual(slugify("codigo"), "codigo", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def slugify(title):
    return title.strip().lower().replace(" ", "-")
```
