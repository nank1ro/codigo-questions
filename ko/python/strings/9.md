---
language: python
exerciseType: 3
---

# --description--

문자열은 **불변(immutable)**입니다: 한 번 만들어지면 그 문자들을 바꿀 수 없습니다.
인덱스에 대입하면 `TypeError`가 발생합니다:
```python
word = "dog"
word[1] = "i"  # TypeError: 'str' object does not support item assignment
```
문자열을 "바꾸려면" 예를 들어 슬라이스와 연결을 사용해 새 문자열을 만들고, 그것을 변수에 저장해야 합니다:
```python
word = word[0] + "i" + word[2:]
print(word)  # dig
```

# --instructions--

이 코드를 실행하면 어떻게 됩니까?
```python
word = "cat"
word[0] = "b"
print(word)
```

# --answers--

- `TypeError`: 문자열은 불변이므로, 문자를 제자리에서 교체할 수 없다
- `bat`을 출력한다
- `b`를 출력한다
- `cat`을 출력하며, 대입은 조용히 무시된다

# --solutions--

- `TypeError`: 문자열은 불변이므로, 문자를 제자리에서 교체할 수 없다
