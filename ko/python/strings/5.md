---
language: python
exerciseType: 1
---

# --description--

**슬라이스(slice)**는 문자열의 일부를 추출합니다.
대괄호 안에 `[start:end]`를 적으면, `start` 위치의 문자는 포함되고 `end` 위치의 문자는 **제외**됩니다:
```python
word = "python"
print(word[0:3])  # pyt
print(word[2:4])  # th
```
`start`를 생략하면 처음부터, `end`를 생략하면 끝까지 슬라이스할 수 있습니다:
```python
print(word[:2])  # py
print(word[2:])  # thon
```
슬라이싱은 에러를 발생시키지 않습니다: `end`가 길이보다 크면 그냥 마지막 문자에서 멈춥니다.

# --instructions--

슬라이스를 사용하여 `word`의 처음 4개 문자를 변수 `first`에, 나머지 문자를 모두 변수 `rest`에 저장하세요

# --seed--

```python
word = "notebook"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

슬라이스 `[:]`를 사용해야 합니다

```json
{
  "regex": "\\[[^\\]]*:[^\\]]*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`first`는 `"note"`와 같아야 합니다

```python
    def test_1(self):
        self.assertEqual(first, "note", "--err-t1--")
```

`rest`는 `"book"`과 같아야 합니다

```python
    def test_2(self):
        self.assertEqual(rest, "book", "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
word = "notebook"
first = word[:4]
rest = word[4:]
```
