---
language: python
exerciseType: 2
---

# --instructions--

밑이 `4`이고 지수가 `5`인 `power` 함수를 호출하여 올바른 결과를 출력하도록 함수를 완성하세요

# --seed--

```python
def power([/], [/]):
    result = base ** exponent
    print(f"{base} to the power of {exponent} is {result}")

power([/], [/])
```

# --answers--

- 4
- 5
- base
- exponent
- exp
- power

# --solutions--

```python
def power(base, exponent):
    result = base ** exponent
    print(f"{base} to the power of {exponent} is {result}")

power(4, 5)
```

# --output--

4 to the power of 5 is 1024
