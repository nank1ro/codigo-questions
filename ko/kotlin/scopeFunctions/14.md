---
language: kotlin
exerciseType: 2
---

# --instructions--

개의 설명이 `with` 블록 안에서 만들어지고 프로그램이 그것을 출력하도록 코드를 완성하세요

# --seed--

```kotlin
class Dog(val name: String, val age: Int)

fun main() {
    val dog = Dog("Rex", 4)
    val info = [/](dog) {
        name + " is " + [/]
    }
    println(info)
}
```

# --answers--

- with
- age
- run
- also
- apply
- name

# --solutions--

```kotlin
class Dog(val name: String, val age: Int)

fun main() {
    val dog = Dog("Rex", 4)
    val info = with(dog) {
        name + " is " + age
    }
    println(info)
}
```

# --output--

Rex is 4
