---
language: kotlin
exerciseType: 4
---

# --instructions--

프로그램이 새 요소가 추가된 후의 리스트를 출력한 다음 그 크기를 출력하도록 이 항목들을 올바른 순서로 정렬할 수 있습니까?

# --answers--

```kotlin
val words = mutableListOf("fig")
```

```kotlin
val count = words
```

```kotlin
    .apply { add("kiwi") }
```

```kotlin
    .also { println("now: $it") }
```

```kotlin
    .size
```

```kotlin
println("size: $count")
```

# --solutions--

```kotlin
val words = mutableListOf("fig")
val count = words
    .apply { add("kiwi") }
    .also { println("now: $it") }
    .size
println("size: $count")
```

# --output--

now: [fig, kiwi]
size: 2
