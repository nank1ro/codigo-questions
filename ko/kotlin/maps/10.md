---
language: kotlin
exerciseType: 4
---

# --instructions--

프로그램이 `{Ben=15}`를 출력하도록 이 줄들을 올바른 순서로 정렬할 수 있습니까?

# --answers--

```kotlin
val scores = mutableMapOf("Ann" to 10)
```

```kotlin
scores["Ben"] = scores.getOrDefault("Ann", 0) + 5
```

```kotlin
scores.remove("Ann")
```

```kotlin
println(scores)
```

# --solutions--

```kotlin
val scores = mutableMapOf("Ann" to 10)
scores["Ben"] = scores.getOrDefault("Ann", 0) + 5
scores.remove("Ann")
println(scores)
```

# --output--

{Ben=15}
