---
language: kotlin
exerciseType: 2
---

# --description--

`map`은 map의 각 항목을 새로운 요소로 변환합니다. `filter`와 달리 결과는 `Map`이 아니라 `List`입니다:
```kotlin
val ages = mapOf("Alice" to 30, "Bob" to 25)
val labels = ages.map { (name, age) -> "$name: $age" }
println(labels) // [Alice: 30, Bob: 25]
```

# --instructions--

`ages`의 각 항목마다 `"name is age"` 형식의 문자열을 하나씩 담은 리스트를 만들도록 코드를 완성하세요

# --seed--

```kotlin
val ages = mapOf("Ann" to 30, "Ben" to 25)
val labels = ages.[/] { ([/], [/]) -> "$name is $age" }
println(labels)
```

# --answers--

- map
- filter
- forEach
- name
- age
- key
- value

# --solutions--

```kotlin
val ages = mapOf("Ann" to 30, "Ben" to 25)
val labels = ages.map { (name, age) -> "$name is $age" }
println(labels)
```

# --output--

[Ann is 30, Ben is 25]
