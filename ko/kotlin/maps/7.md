---
language: kotlin
exerciseType: 2
---

# --description--

`keys` 프로퍼티는 map의 모든 키를 `Set`으로 반환하고, `values`는 모든 값을 컬렉션으로 반환합니다:
```kotlin
val capitals = mapOf("Italy" to "Rome", "France" to "Paris")
println(capitals.keys)   // [Italy, France]
println(capitals.values) // [Rome, Paris]
```
둘 다 항목이 삽입된 순서를 유지합니다.

# --instructions--

map의 키를 먼저, 그다음 값을 출력하도록 코드를 완성하세요

# --seed--

```kotlin
val ages = mapOf("Alice" to 30, "Bob" to 25)
println(ages.[/])
println(ages.[/])
```

# --answers--

- keys
- values
- entries
- size
- items

# --solutions--

```kotlin
val ages = mapOf("Alice" to 30, "Bob" to 25)
println(ages.keys)
println(ages.values)
```

# --output--

[Alice, Bob]
[30, 25]
