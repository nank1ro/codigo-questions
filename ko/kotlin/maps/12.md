---
language: kotlin
exerciseType: 4
---

# --instructions--

각 과일이 몇 번 등장하는지 세어 `{apple=2, pear=1}`을 출력하기 위해 이 줄들을 올바른 순서로 정렬할 수 있습니까? `counts`보다 앞에 `fruits`를 선언하세요.

# --answers--

```kotlin
val fruits = listOf("apple", "pear", "apple")
```

```kotlin
val counts = mutableMapOf<String, Int>()
```

```kotlin
for (fruit in fruits) {
```

```kotlin
    counts[fruit] = counts.getOrDefault(fruit, 0) + 1
```

```kotlin
}
```

```kotlin
println(counts)
```

# --solutions--

```kotlin
val fruits = listOf("apple", "pear", "apple")
val counts = mutableMapOf<String, Int>()
for (fruit in fruits) {
    counts[fruit] = counts.getOrDefault(fruit, 0) + 1
}
println(counts)
```

# --output--

{apple=2, pear=1}
