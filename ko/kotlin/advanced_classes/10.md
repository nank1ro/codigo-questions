---
language: kotlin
exerciseType: 2
---

# --instructions--

`ofKilometres`가 킬로미터로부터 `Distance`를 만드는 `Distance`의 팩토리 함수가 되도록 코드를 완성하세요

# --seed--

```kotlin
class Distance private constructor(val meters: Int) {
    [/] object {
        fun ofKilometres(km: Int) = [/](km * 1000)
    }
}

fun main() {
    println(Distance.ofKilometres(3).meters)
}
```

# --answers--

- companion
- Distance
- data
- inner
- Int
- meters

# --solutions--

```kotlin
class Distance private constructor(val meters: Int) {
    companion object {
        fun ofKilometres(km: Int) = Distance(km * 1000)
    }
}

fun main() {
    println(Distance.ofKilometres(3).meters)
}
```

# --output--

3000
