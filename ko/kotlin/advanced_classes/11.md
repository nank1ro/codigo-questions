---
language: kotlin
exerciseType: 1
---

# --description--

`interface`는 타입이 무엇을 할 수 있는지 나열합니다. 그 멤버들은 기본적으로 추상이지만, 인터페이스는 **기본 구현(default implementation)**, 즉 구현하는 모든 클래스가 공짜로 상속하고 재정의할 수 있는 본문을 제공할 수도 있습니다:

```kotlin
interface Greeter {
    val name: String              // abstract, 클래스가 반드시 제공해야 함
    fun greet(): String = "Hi, $name"  // 기본 구현
}

class Person(override val name: String) : Greeter

class Robot(override val name: String) : Greeter {
    override fun greet(): String = "BEEP $name"
}

println(Person("Ann").greet()) // Hi, Ann
println(Robot("R2").greet())   // BEEP R2
```

인터페이스는 상태를 저장할 수 없으므로(백킹 필드가 없음), 추상 프로퍼티는 보통 생성자에서 `override val`로 클래스가 구현해야 합니다. 클래스와 달리, 타입은 원하는 만큼 많은 인터페이스를 구현할 수 있습니다.

# --instructions--

`String` 타입의 추상 `val` 프로퍼티 `name`과, `name`이 `"Ann"`일 때 기본 구현이 `"Hello, Ann!"`을 반환하는 메서드 `greet(): String`을 가진 인터페이스 `Greeter`를 선언하세요.

그다음 `Greeter`를 구현하고 생성자에서 `name`을 받는 클래스 `Person`과, 마찬가지로 `Greeter`를 구현하지만 `name`이 `"Bo"`일 때 `"Ahoy, Bo!"`를 반환하도록 `greet()`을 재정의하는 클래스 `Pirate`를 선언하세요.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`Person("Ann").greet()`는 `"Hello, Ann!"`을 반환해야 합니다

```kotlin
    tryCatch(Person("Ann").greet() == "Hello, Ann!")
```

`Person("Cy").name`은 `"Cy"`를 반환해야 합니다

```kotlin
    tryCatch(Person("Cy").name == "Cy")
```

`Pirate("Bo").greet()`은 `"Ahoy, Bo!"`를 반환해야 합니다

```kotlin
    tryCatch(Pirate("Bo").greet() == "Ahoy, Bo!")
```

두 클래스 모두 `Greeter`로 사용할 수 있어야 합니다

```kotlin
    val greeters: List<Greeter> = listOf(Person("Ann"), Pirate("Bo"))
    tryCatch(greeters.map { it.greet() } == listOf("Hello, Ann!", "Ahoy, Bo!"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
interface Greeter {
    val name: String
    fun greet(): String = "Hello, $name!"
}

class Person(override val name: String) : Greeter

class Pirate(override val name: String) : Greeter {
    override fun greet(): String = "Ahoy, $name!"
}
```
