---
language: kotlin
exerciseType: 1
---

# --description--

주 생성자의 각 프로퍼티에 대해 데이터 클래스는 `componentN()` 함수도 생성합니다: 첫 번째 프로퍼티에는 `component1()`, 두 번째에는 `component2()` 등입니다.

이 함수들은 **구조 분해 선언(destructuring declarations)**을 가능하게 합니다. 객체를 한 줄에서 여러 변수로 풀어냅니다:

```kotlin
data class Point(val x: Int, val y: Int)

val point = Point(3, 7)
val (x, y) = point
println(x)               // 3
println(point.component2()) // 7
```

변수의 순서는 이름이 아닌 프로퍼티의 순서를 따릅니다. 필요 없는 프로퍼티는 `_`로 건너뛸 수 있습니다:

```kotlin
val (_, onlyY) = point
```

# --instructions--

`Int` 타입의 `val` 프로퍼티 `x`와 `y`를 가진 데이터 클래스 `Point`를 선언하세요.

그다음 구조 분해 선언을 사용하여 두 프로퍼티를 읽고 `"x=3, y=7"` 형태로 반환하는 함수 `describe(point: Point): String`를 작성하세요.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`describe(Point(3, 7))`는 `"x=3, y=7"`을 반환해야 합니다

```kotlin
    tryCatch(describe(Point(3, 7)) == "x=3, y=7")
```

`describe(Point(-4, 0))`은 `"x=-4, y=0"`을 반환해야 합니다

```kotlin
    tryCatch(describe(Point(-4, 0)) == "x=-4, y=0")
```

`Point`는 `component1()`과 `component2()`를 생성해야 합니다

```kotlin
    val point = Point(1, 2)
    tryCatch(point.component1() == 1 && point.component2() == 2)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
data class Point(val x: Int, val y: Int)

fun describe(point: Point): String {
    val (x, y) = point
    return "x=$x, y=$y"
}
```
