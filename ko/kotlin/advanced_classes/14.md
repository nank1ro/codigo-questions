---
language: kotlin
exerciseType: 2
---

# --description--

다른 클래스 안에 선언된 클래스는 기본적으로 **중첩(nested)**됩니다. 내부 클래스는 외부 인스턴스에 대해 아무것도 모르며, 외부 클래스 이름으로 생성합니다:

```kotlin
class Outer {
    class Nested {
        fun hello() = "hi"
    }
}

println(Outer.Nested().hello()) // hi
```

`inner` 키워드를 추가하면 상황이 달라집니다: `inner` 클래스는 외부 인스턴스에 대한 참조를 가지므로 외부 프로퍼티를 읽을 수 있고, **인스턴스로부터** 생성합니다:

```kotlin
class Counter(val step: Int) {
    inner class Doubler {
        fun value() = step * 2
    }
}

println(Counter(5).Doubler().value()) // 10
```

`inner` 클래스 안에서 `this`는 내부 객체입니다. 외부 객체가 명시적으로 필요하면 `this@Counter`를 사용하세요.

# --instructions--

`Badge`가 자신이 속한 `Outer` 인스턴스의 `title`을 읽을 수 있도록 코드를 완성하세요

# --seed--

```kotlin
class Outer(val title: String) {
    [/] class Badge {
        fun render(): String = "[${[/]}]"
    }
}

fun main() {
    println(Outer("Codigo").Badge().render())
}
```

# --answers--

- inner
- title
- nested
- open
- this
- render

# --solutions--

```kotlin
class Outer(val title: String) {
    inner class Badge {
        fun render(): String = "[${title}]"
    }
}

fun main() {
    println(Outer("Codigo").Badge().render())
}
```

# --output--

[Codigo]
