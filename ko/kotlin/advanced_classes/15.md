---
language: kotlin
exerciseType: 3
---

# --instructions--

이 코드에서는 어떤 일이 발생합니까?

```kotlin
class Outer(val name: String) {
    class Nested {
        fun show(): String = name
    }
}
```

# --answers--

- 컴파일되지 않습니다: 중첩 클래스는 외부 멤버를 볼 수 없으므로, `Nested`는 `inner`여야 합니다
- 컴파일되고, `show()`는 외부 인스턴스의 `name`을 반환합니다
- 컴파일되고, `show()`는 항상 빈 문자열을 반환합니다
- 컴파일되지 않습니다: Kotlin은 클래스 안에 클래스를 허용하지 않습니다

# --solutions--

- 컴파일되지 않습니다: 중첩 클래스는 외부 멤버를 볼 수 없으므로, `Nested`는 `inner`여야 합니다
