---
language: kotlin
exerciseType: 1
---

# --description--

`sealed` 클래스는 **닫힌(closed)** 선택 집합을 기술합니다: 같은 패키지와 모듈에 작성된 하위 클래스만 허용되므로, 컴파일러는 그 모든 것을 알 수 있습니다.

```kotlin
sealed class Shape
data class Circle(val radius: Int) : Shape()
data class Square(val side: Int) : Shape()
```

그 대가는 **모든 경우를 다루는(exhaustive) `when`**입니다: sealed 타입에 대해 분기하고 모든 하위 클래스를 다루면 `else` 분기를 생략할 수 있습니다. 나중에 새 하위 클래스를 추가하면, 컴파일러가 조용히 `else`를 택하는 대신 업데이트를 잊은 모든 `when`을 알려줍니다.

```kotlin
fun name(shape: Shape): String = when (shape) {
    is Circle -> "circle"
    is Square -> "square"
}
```

`is Circle` 이후에는 값이 스마트 캐스트되므로, 그 분기 안에서는 수동 캐스트 없이도 `shape.radius`를 사용할 수 있습니다.

# --instructions--

`Int` 타입의 `val` 프로퍼티 `side`를 가진 `Square`와, `Int` 타입의 `val` 프로퍼티 `width`와 `height`를 가진 `Rectangle`이라는 두 데이터 클래스 하위 클래스를 가진 sealed 클래스 `Shape`를 선언하세요.

그다음 `else` 분기 없이 모든 경우를 다루는 `when`을 사용하여 도형의 넓이를 반환하는 함수 `area(shape: Shape): Int`를 작성하세요.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`area(Square(3))`은 `9`를 반환해야 합니다

```kotlin
    tryCatch(area(Square(3)) == 9)
```

`area(Rectangle(2, 5))`는 `10`을 반환해야 합니다

```kotlin
    tryCatch(area(Rectangle(2, 5)) == 10)
```

`area(Square(0))`은 `0`을 반환해야 합니다

```kotlin
    tryCatch(area(Square(0)) == 0)
```

`Square`와 `Rectangle`은 `Shape`의 하위 클래스여야 합니다

```kotlin
    val shapes: List<Shape> = listOf(Square(4), Rectangle(3, 3))
    tryCatch(shapes.map { area(it) } == listOf(16, 9))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
sealed class Shape
data class Square(val side: Int) : Shape()
data class Rectangle(val width: Int, val height: Int) : Shape()

fun area(shape: Shape): Int = when (shape) {
    is Square -> shape.side * shape.side
    is Rectangle -> shape.width * shape.height
}
```
