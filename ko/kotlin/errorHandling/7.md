---
language: kotlin
exerciseType: 2
---

# --description--

`try`가 표현식이기 때문에 값이 기대되는 어디에서든 사용할 수 있습니다 — `=`로 작성된 함수의 전체 본문으로도 사용할 수 있습니다:
```kotlin
fun length(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    -1
}
```
두 블록은 모두 같은 타입의 값을 산출해야 하며, 여기서는 `Int`입니다. 대체 값을 `catch` 블록의 마지막 표현식으로 쓰세요; 어느 블록 안에도 `return`은 없습니다.

# --instructions--

`text`에 담긴 숫자를 돌려주고, 텍스트가 숫자가 아니면 `0`을 돌려주도록 함수를 완성하세요.

# --seed--

```kotlin
fun size(text: String): Int = [/] {
    text.toInt()
} [/] (e: NumberFormatException) {
    [/]
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --answers--

- try
- catch
- 0
- finally
- throw
- 1

# --solutions--

```kotlin
fun size(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    0
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --output--

15
0
