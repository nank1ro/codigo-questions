---
language: kotlin
exerciseType: 2
---

# --description--

프로그램을 계속 살아 있게 하려면 위험한 명령문을 `try` 블록 안에 넣고 복구 방법을 `catch` 블록에 기술합니다:
```kotlin
try {
    println("abc".toInt())
} catch (e: Exception) {
    println("cannot read that number")
}
println("still running")
```
Kotlin은 `try` 블록을 실행합니다; 그 안의 명령문이 던지는 즉시 블록의 나머지 부분은 건너뛰어지고 제어가 `catch` 블록으로 이동합니다. 괄호 안의 이름 — 여기서는 `e` — 는 예외 객체이고, `Exception`은 잡히는 타입입니다.

`catch` 블록이 끝나면 프로그램은 전체 `try`/`catch` 뒤의 줄에서 정상적으로 계속됩니다.

# --instructions--

실패한 변환이 잡히고 프로그램이 계속 실행되도록 코드를 완성하세요.

# --seed--

```kotlin
fun main() {
    val text = "abc"
    [/] {
        println(text.toInt())
    } [/] (e: Exception) {
        println("not a number")
    }
    println("done")
}
```

# --answers--

- try
- catch
- finally
- throw
- if
- else

# --solutions--

```kotlin
fun main() {
    val text = "abc"
    try {
        println(text.toInt())
    } catch (e: Exception) {
        println("not a number")
    }
    println("done")
}
```

# --output--

not a number
done
