---
language: kotlin
exerciseType: 2
---

# --description--

`finally` 블록을 마지막에 추가할 수 있습니다. 이 블록은 **무슨 일이 있어도** 실행됩니다: `try`가 성공한 후에도, `catch`가 복구한 후에도, 심지어 예외가 전혀 잡히지 않을 때에도 실행됩니다.

```kotlin
try {
    println("reading")
    println("abc".toInt())
} catch (e: NumberFormatException) {
    println("bad number")
} finally {
    println("closing")
}
```
```
reading
bad number
closing
```
그래서 파일 닫기처럼 건너뛰어서는 안 되는 정리 작업을 넣는 장소가 됩니다. `try`에는 최소한 `catch`나 `finally` 중 하나가 필요하지만, 둘 다 가질 수도 있습니다.

# --instructions--

잘못된 변환이 잡히고 정리 줄이 항상 실행되도록 코드를 완성하세요.

# --seed--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } [/] (e: NumberFormatException) {
        println("bad number")
    } [/] {
        println("closing")
    }
}
```

# --answers--

- catch
- finally
- try
- throw
- else
- do

# --solutions--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } catch (e: NumberFormatException) {
        println("bad number")
    } finally {
        println("closing")
    }
}
```

# --output--

bad number
closing
