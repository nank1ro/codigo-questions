---
language: kotlin
exerciseType: 2
---

# --instructions--

`clean`을 소문자로 출력하고, 이어서 `@` 문자의 인덱스를 출력하도록 코드를 완성하세요

# --seed--

```kotlin
val email = "  User@Example.com "
val clean = email.trim()
println(clean.[/]())
println(clean.[/]("@"))
```

# --answers--

- lowercase
- uppercase
- indexOf
- contains
- trim
- length

# --solutions--

```kotlin
val email = "  User@Example.com "
val clean = email.trim()
println(clean.lowercase())
println(clean.indexOf("@"))
```

# --output--

user@example.com
4
