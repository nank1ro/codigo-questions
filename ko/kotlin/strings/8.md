---
language: kotlin
exerciseType: 4
---

# --description--

`split`은 `List`를 반환하므로 다른 리스트와 마찬가지로 요소를 순회할 수 있습니다:
```kotlin
for (part in "a-b".split("-")) {
    println(part)
}
// prints a, then b
```

# --instructions--

`sentence`의 각 단어를 대문자로 한 줄씩 출력하도록 이 줄들을 정렬할 수 있나요?

# --answers--

```kotlin
val sentence = "the quick brown fox"
```

```kotlin
val words = sentence.split(" ")
```

```kotlin
for (word in words) {
```

```kotlin
    println(word.uppercase())
```

```kotlin
}
```

# --solutions--

```kotlin
val sentence = "the quick brown fox"
val words = sentence.split(" ")
for (word in words) {
    println(word.uppercase())
}
```

# --output--

THE
QUICK
BROWN
FOX
