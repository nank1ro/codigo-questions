---
language: kotlin
exerciseType: 2
---

# --description--

`split`은 구분자를 기준으로 문자열을 `List` 조각으로 나누고, `joinToString`은 그 반대로 컬렉션의 요소들을 원하는 구분자로 하나의 문자열로 이어 붙입니다:
```kotlin
val csv = "a,b,c"
val parts = csv.split(",")
println(parts)                  // [a, b, c]
println(parts.joinToString("/")) // a/b/c
```

# --instructions--

`csv`를 쉼표로 분할하고, 색상을 `" - "`로 구분해 출력하도록 코드를 완성하세요

# --seed--

```kotlin
val csv = "red,green,blue"
val colors = csv.[/](",")
println(colors.[/](" - "))
```

# --answers--

- split
- joinToString
- join
- separate
- substring

# --solutions--

```kotlin
val csv = "red,green,blue"
val colors = csv.split(",")
println(colors.joinToString(" - "))
```

# --output--

red - green - blue
