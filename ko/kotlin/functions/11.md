---
language: kotlin
exerciseType: 2
---

# --description--

단일 표현식 함수는 블록 본문 대신 `=`를 사용합니다:
```kotlin
fun triple(n: Int) = n * 3
```
이것은 다음의 약어입니다:
```kotlin
fun triple(n: Int): Int {
    return n * 3
}
```

# --instructions--

두 정수를 더하는 단일 표현식 함수를 완성하세요.

# --seed--

```kotlin
fun add(a: Int, b: Int) [/] a [/] b
fun main() {
    println(add(3, 4))
}
```

# --answers--

- =
- +
- -
- *
- return
- ->

# --solutions--

```kotlin
fun add(a: Int, b: Int) = a + b
fun main() {
    println(add(3, 4))
}
```

# --output--

7
