---
language: kotlin
exerciseType: 1
---

# --description--

고차 함수를 직접 작성할 수도 있습니다: 함수 타입을 가진 매개변수는 그 모양의 람다라면 무엇이든 받아들이며, 함수 안에서는 일반 함수처럼 호출합니다.
```kotlin
fun repeatTwice(text: String, transform: (String) -> String): String {
    return transform(transform(text))
}
println(repeatTwice("a", { it + "!" })) // a!!
println(repeatTwice("a") { it + "!" })  // same, with a trailing lambda
```
함수 매개변수를 **마지막**에 두어야 호출자가 trailing 람다 문법을 사용할 수 있습니다.

# --instructions--

`f`를 `n`에 적용하고, 그 결과에 다시 `f`를 적용하여 반환하는 함수 `applyTwice(n: Int, f: (Int) -> Int): Int`를 작성하세요.

예: `applyTwice(3) { it + 1 }`은 `5`를 반환합니다.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`applyTwice(3) { it + 1 }`은 `5`를 반환해야 합니다

```kotlin
    tryCatch(applyTwice(3) { it + 1 } == 5)
```

`applyTwice(2) { it * it }`는 `16`을 반환해야 합니다

```kotlin
    tryCatch(applyTwice(2) { it * it } == 16)
```

`applyTwice(10, { it - 3 })`은 `4`를 반환해야 합니다

```kotlin
    tryCatch(applyTwice(10, { it - 3 }) == 4)
```

`applyTwice(7) { it }`은 `7`을 반환해야 합니다

```kotlin
    tryCatch(applyTwice(7) { it } == 7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun applyTwice(n: Int, f: (Int) -> Int): Int {
    return f(f(n))
}
```
