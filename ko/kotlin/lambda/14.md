---
language: kotlin
exerciseType: 4
---

# --description--

**익명 함수**는 `fun`으로 선언하지만 이름이 없는 함수입니다. 함수 값을 만드는 또 다른 방법입니다:
```kotlin
val square = fun(x: Int): Int {
    return x * x
}
println(square(4)) // 16
```
람다와 달리 반환 타입을 명시적으로 선언할 수 있고, 값을 만들 때 `return`을 사용합니다.
익명 함수와 람다는 서로 바꿔 쓸 수 있습니다: 둘 다 `map`, `filter` 또는 함수 타입을 받는 어떤 함수에도 전달할 수 있습니다.

# --instructions--

`numbers`를 익명 함수 `square`로 매핑하여 `[1, 4, 9]`를 출력하도록 이 줄들을 정렬할 수 있습니까?

# --answers--

```kotlin
val square = fun(x: Int): Int {
    return x * x
}
```

```kotlin
val numbers = listOf(1, 2, 3)
```

```kotlin
println(numbers.map(square))
```

# --solutions--

```kotlin
val square = fun(x: Int): Int {
    return x * x
}
val numbers = listOf(1, 2, 3)
println(numbers.map(square))
```

```kotlin
val numbers = listOf(1, 2, 3)
val square = fun(x: Int): Int {
    return x * x
}
println(numbers.map(square))
```

# --output--

[1, 4, 9]
