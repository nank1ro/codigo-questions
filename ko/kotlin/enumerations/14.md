---
language: kotlin
exerciseType: 4
---

# --instructions--

enum `Planet`을 선언하고, 각 항목의 이름과 moons 개수를 출력하도록 항목들을 정렬하세요. 먼저 enum을 선언하고, 그다음 `main`을 배치하세요. 프로그램은 다음과 같이 출력해야 합니다:

```
MERCURY: 0
EARTH: 1
MARS: 2
```

# --answers--

```kotlin
enum class Planet(val moons: Int) {
```

```kotlin
    MERCURY(0),
```

```kotlin
    EARTH(1),
```

```kotlin
    MARS(2)
```

```kotlin
}
```

```kotlin
fun main() {
    for (planet in Planet.entries) {
```

```kotlin
        println("${planet.name}: ${planet.moons}")
```

```kotlin
    }
}
```

# --solutions--

```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
fun main() {
    for (planet in Planet.entries) {
        println("${planet.name}: ${planet.moons}")
    }
}
```

# --output--

MERCURY: 0
EARTH: 1
MARS: 2
