---
language: kotlin
exerciseType: 2
---

# --description--

`Any`는 계층의 꼭대기에 있는 타입입니다: 모든 Kotlin 값은 `Any`이므로, `Any` 타입의 변수는 `Int`, `String`, `Boolean` 등 무엇이든 담을 수 있습니다.
실제로 무엇이 담겨 있는지 알아보려면 `is` 연산자를 사용하세요. 이 연산자는 값이 그 타입일 때 `true`를 반환합니다:
```kotlin
val value: Any = 42
println(value is Int)    // true
println(value is String) // false
```
검사를 통과하면 컴파일러가 값을 **스마트 캐스트**합니다: `if` 안(또는 `when` 분기 안)에서는 변환 없이 그 타입으로 사용할 수 있습니다:
```kotlin
if (value is Int) println(value + 1) // 43, value is an Int here
when (value) {
    is String -> println(value.length)
    is Boolean -> println(!value)
}
```

# --instructions--

`when`을 완성하여 `describe`가 `Int`에는 수에 1을 더한 값을, `String`에는 텍스트의 길이를 반환하도록 하세요. 프로그램이 세 줄에 걸쳐 `number 42`, `text of length 2`, `unknown`을 출력하도록 하세요

# --seed--

```kotlin
fun describe(value: Any): String {
    return when (value) {
        [/] Int -> "number ${value + 1}"
        is [/] -> "text of length ${value.length}"
        else -> "unknown"
    }
}
println(describe(41))
println(describe("hi"))
println(describe(true))
```

# --answers--

- is
- String
- as
- Any
- Char

# --solutions--

```kotlin
fun describe(value: Any): String {
    return when (value) {
        is Int -> "number ${value + 1}"
        is String -> "text of length ${value.length}"
        else -> "unknown"
    }
}
println(describe(41))
println(describe("hi"))
println(describe(true))
```

# --output--

number 42
text of length 2
unknown
