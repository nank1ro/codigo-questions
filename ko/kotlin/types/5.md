---
language: kotlin
exerciseType: 2
---

# --description--

피연산자의 타입이 나눗셈의 동작 방식을 결정합니다. 둘 다 `Int`일 때 `/` 연산자는 **정수 나눗셈**을 수행합니다: 결과는 `Int`이고 나머지는 버려집니다.
피연산자 중 하나라도 `Double`이면 `/`는 부동소수점 나눗셈을 수행하고 소수 부분을 유지합니다:
```kotlin
println(7 / 2)              // 3
println(7.0 / 2)            // 3.5
val slices = 7
println(slices.toDouble() / 2) // 3.5
```
따라서 두 `Int` 변수로 소수 결과를 얻으려면 나누기 **전에** 최소 하나를 변환해야 합니다: `(7 / 2).toDouble()`은 `3.0`인데, 이는 정수 나눗셈이 이미 일어났기 때문입니다.

# --instructions--

`slices`를 `Double`로 변환해 두 번째 나눗셈이 소수 부분을 유지하도록 완성하여, 프로그램이 `3`과 `3.5`를 출력하도록 하세요

# --seed--

```kotlin
val slices = 7
val people = 2
println(slices / people)
println(slices[/] / people)
```

# --answers--

- .toDouble()
- .toInt()
- .toLong()
- .toString()

# --solutions--

```kotlin
val slices = 7
val people = 2
println(slices / people)
println(slices.toDouble() / people)
```

# --output--

3
3.5
