---
language: kotlin
exerciseType: 4
---

# --description--

요약 다음에는 문서화 주석이 항상 `@`로 시작하는 **KDoc 태그**로 매개변수와 반환값을 설명할 수 있습니다:
```kotlin
/**
 * Returns the number of seconds in the given minutes.
 * @param minutes a whole number of minutes, never negative
 * @return [minutes] multiplied by sixty
 */
fun toSeconds(minutes: Int): Int {
    return minutes * 60
}
```
`@param` 뒤에는 매개변수의 이름과 그 설명이 이어집니다; 매개변수마다 `@param`이 하나씩 있습니다. `@return`은 함수가 돌려주는 값을 설명합니다. 순서는 항상 같습니다: 먼저 요약, 그다음 `@param` 태그, 그다음 `@return`입니다.

# --instructions--

요약, 그다음 `@param` 태그, 그다음 `@return` 태그를 함수 바로 위의 하나의 문서화 주석 안에 넣어 `twice`를 문서화하고, 이어서 `twice(21)`을 출력하는 프로그램을 만들기 위해 항목들을 정렬하세요

# --answers--

```kotlin
/**
 * Returns the double of a number.
```

```kotlin
 * @param n the number to double
```

```kotlin
 * @return [n] multiplied by two
```

```kotlin
 */
```

```kotlin
fun twice(n: Int): Int {
    return n * 2
}
```

```kotlin
println(twice(21))
```

# --solutions--

```kotlin
/**
 * Returns the double of a number.
 * @param n the number to double
 * @return [n] multiplied by two
 */
fun twice(n: Int): Int {
    return n * 2
}
println(twice(21))
```

# --output--

42
