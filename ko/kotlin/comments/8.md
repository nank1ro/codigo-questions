---
language: kotlin
exerciseType: 4
---

# --description--

블록 주석의 흔한 용도는 **헤더 주석(header comment)**입니다: 함수 바로 위에 놓이는 짧은 블록으로, 함수가 하는 일과 매개변수의 의미를 설명합니다.
```kotlin
/*
Returns the number of seconds in the given minutes.
minutes is a whole number, never negative.
*/
fun toSeconds(minutes: Int): Int {
    return minutes * 60
}
```
`toSeconds`를 호출하는 사람은 이제 본문 대신 헤더를 읽으면 됩니다. 헤더는 함수 바로 옆에 두어 함께 업데이트되도록 하세요.

# --instructions--

헤더 주석이 바로 위에 놓여 문서화된 `area`를 정의하고, 이어서 `area(3, 4)`를 출력하는 프로그램을 만들기 위해 항목들을 정렬하세요

# --answers--

```kotlin
/*
```

```kotlin
Returns the area of a rectangle.
```

```kotlin
*/
```

```kotlin
fun area(w: Int, h: Int): Int {
    return w * h
}
```

```kotlin
println(area(3, 4))
```

# --solutions--

```kotlin
/*
Returns the area of a rectangle.
*/
fun area(w: Int, h: Int): Int {
    return w * h
}
println(area(3, 4))
```

# --output--

12
