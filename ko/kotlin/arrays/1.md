---
language: kotlin
exerciseType: 2
---

# --description--

**배열**은 같은 타입의 값을 정해진 개수만큼 하나의 변수 이름 아래에 저장합니다.
`arrayOf`로 배열을 생성하고, 대괄호와 `0`부터 시작하는 **인덱스**로 요소를 읽으며, `size`로 요소의 개수를 얻습니다:
```kotlin
val colors = arrayOf("red", "green", "blue")
println(colors[0])   // red
println(colors.size) // 3
```
마지막 요소는 인덱스 `size - 1`에 있습니다.

# --instructions--

`arrayOf`로 배열을 생성하고, 요소의 개수를 출력한 다음, 마지막 요소를 출력하도록 코드를 완성하세요

# --seed--

```kotlin
val numbers = [/](3, 5, 7)
println(numbers.[/])
println(numbers[[/]])
```

# --answers--

- arrayOf
- Array
- size
- length
- count
- 2
- 3

# --solutions--

```kotlin
val numbers = arrayOf(3, 5, 7)
println(numbers.size)
println(numbers[2])
```

# --output--

3
7
