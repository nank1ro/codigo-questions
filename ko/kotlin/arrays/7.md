---
language: kotlin
exerciseType: 4
---

# --description--

배열은 **제자리에서** 정렬하거나, 새로 정렬된 컬렉션으로 복사할 수 있습니다:
- `sort()`와 `sortDescending()`은 배열 자체를 재정렬하며 아무것도 반환하지 않습니다
- `reverse()`는 배열 자체의 순서를 뒤집습니다
- `sorted()`, `sortedDescending()`, `reversed()`는 배열을 그대로 두고 새로운 `List`를 반환합니다
```kotlin
val nums = intArrayOf(3, 1, 2)
nums.sort()                 // nums is now [1, 2, 3]
println(nums.reversed())    // [3, 2, 1], nums is still [1, 2, 3]
```

# --instructions--

점수를 오름차순으로, 그다음 내림차순으로 출력하도록 이 줄들을 정렬할 수 있습니까?

# --answers--

```kotlin
val scores = intArrayOf(40, 90, 70)
```

```kotlin
scores.sort()
```

```kotlin
println(scores.joinToString())
```

```kotlin
scores.reverse()
```

```kotlin
println(scores.joinToString())
```

# --solutions--

```kotlin
val scores = intArrayOf(40, 90, 70)
scores.sort()
println(scores.joinToString())
scores.reverse()
println(scores.joinToString())
```

# --output--

40, 70, 90
90, 70, 40
