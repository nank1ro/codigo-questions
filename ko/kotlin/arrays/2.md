---
language: kotlin
exerciseType: 1
---

# --description--

`arrayOf(1, 2, 3)`은 각 요소가 박싱된 객체인 `Array<Int>`를 생성합니다.
기본 타입을 위해 Kotlin은 `IntArray`, `DoubleArray`, `BooleanArray`와 같은 전용의 더 효율적인 타입을 제공합니다:
```kotlin
val a = intArrayOf(1, 2, 3)   // IntArray
val b = doubleArrayOf(1.5, 2.5) // DoubleArray
```
각 인덱스를 받는 **init** 람다로 정해진 크기의 배열을 만들거나, 0으로 채워진 `IntArray`를 만들 수도 있습니다:
```kotlin
val doubles = Array(3) { i -> i * 2 } // [0, 2, 4]
val zeros = IntArray(3)               // [0, 0, 0]
val evens = IntArray(4) { it * 2 }    // [0, 2, 4, 6]
```

# --instructions--

인덱스 `i`의 요소가 `i * i`인 `n`개 요소의 `IntArray`를 반환하는 함수 `squares(n: Int): IntArray`를 작성하세요.

예: `squares(4)`는 `[0, 1, 4, 9]`를 반환합니다.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`squares(4)`는 `[0, 1, 4, 9]`를 반환해야 합니다

```kotlin
    tryCatch(squares(4).contentEquals(intArrayOf(0, 1, 4, 9)))
```

`squares(1)`은 `[0]`을 반환해야 합니다

```kotlin
    tryCatch(squares(1).contentEquals(intArrayOf(0)))
```

`squares(0)`은 빈 배열을 반환해야 합니다

```kotlin
    tryCatch(squares(0).size == 0)
```

`squares(6)`은 `IntArray`여야 합니다

```kotlin
    tryCatch(squares(6) is IntArray && squares(6)[5] == 25)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun squares(n: Int): IntArray {
    return IntArray(n) { it * it }
}
```
