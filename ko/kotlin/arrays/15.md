---
language: kotlin
exerciseType: 4
---

# --instructions--

`0`, `1`, `2`, `3`의 제곱을 만들고, 그것들을 라벨로 바꾼 다음, 각 라벨을 인덱스와 함께 출력하도록 이 줄들을 정렬할 수 있습니까?

# --answers--

```kotlin
val squares = Array(4) { it * it }
```

```kotlin
val labels = squares.map { "sq=$it" }
```

```kotlin
for ((i, label) in labels.withIndex()) {
```

```kotlin
    println("$i $label")
```

```kotlin
}
```

# --solutions--

```kotlin
val squares = Array(4) { it * it }
val labels = squares.map { "sq=$it" }
for ((i, label) in labels.withIndex()) {
    println("$i $label")
}
```

# --output--

0 sq=0
1 sq=1
2 sq=4
3 sq=9
