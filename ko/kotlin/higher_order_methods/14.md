---
language: kotlin
exerciseType: 2
---

# --description--

체인 안에서 `it`의 타입은 단계마다 바뀝니다. `List<String>`에 `filter`를 적용한 뒤에는 여전히 문자열이지만, `map { it.length }` 뒤에는 `List<Int>`가 되므로 다음 람다는 숫자를 봅니다.
```kotlin
val words = listOf("kiwi", "fig")
println(words.map { it.length }.filter { it > 3 }) // [4]
```
모든 단계는 **새** 리스트를 반환하고 이전 리스트는 결코 건드리지 않으므로, 체인을 이름 붙인 중간 값들로 나누어도 결과는 달라지지 않습니다.

# --instructions--

`result`가 `a`로 시작하는 과일들을 대문자로 담아 프로그램이 `[APPLE, AVOCADO]`를 출력하도록 코드를 완성하세요

# --seed--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.[/] { it.startsWith("a") }.[/] { it.[/] }
println(result)
```

# --answers--

- filter
- map
- uppercase()
- forEach
- count
- lowercase()
- length

# --solutions--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.filter { it.startsWith("a") }.map { it.uppercase() }
println(result)
```

# --output--

[APPLE, AVOCADO]
