---
language: kotlin
exerciseType: 2
---

# --description--

`Set`을 `List`로 변환하려면 `toList()` 함수를 사용할 수 있습니다

# --instructions--

`Set`을 `List`로 변환하도록 코드를 완성하세요

# --seed--

```kotlin
val uniqueNumbers = setOf(1, 2, 3, 3)
var numbers = [/][/][/]
numbers += 4
println(numbers)
```

# --answers--

- uniqueNumbers
- .
- toList()
- toList
- distinct()
- asList()

# --solutions--

```kotlin
val uniqueNumbers = setOf(1, 2, 3, 3)
var numbers = uniqueNumbers.toList()
numbers += 4
println(numbers)
```

# --output--

[1, 2, 3, 4]
