---
language: kotlin
exerciseType: 2
---

# --description--

리시버 타입은 **널 허용**일 수 있습니다. `String?`에 대한 확장은 `null`을 담고 있을 수 있는 변수에서도 호출할 수 있고, 함수 안의 `this`는 `String?`이므로 `null` 경우는 직접 처리합니다. 보통 널 가능성 수업에서 만난 엘비스 연산자 `?:`를 사용합니다.
```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val name: String? = null
println(name.orDash())  // -
println("Ada".orDash()) // Ada
```
`null` 값에서 `name.orDash()`를 호출하는 것은 안전합니다. 함수 자체가 `null` 리시버를 받아들이므로 `?.`가 필요 없습니다.

# --instructions--

널 허용 `String`에서 호출할 수 있고 리시버가 `null`일 때 `"-"`를 반환하도록 확장을 완성하여, 프로그램이 `-`와 `Ada`를 두 줄에 걸쳐 출력하도록 하세요

# --seed--

```kotlin
fun String[/].orDash(): String = [/] ?: "-"
val name: String? = null
println(name.orDash())
println("Ada".orDash())
```

# --answers--

- ?
- this
- !!
- it
- name
- ?.

# --solutions--

```kotlin
fun String?.orDash(): String = this ?: "-"
val name: String? = null
println(name.orDash())
println("Ada".orDash())
```

# --output--

-
Ada
