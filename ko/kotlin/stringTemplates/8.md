---
language: kotlin
exerciseType: 2
---

# --description--

`${}` 안에 조건을 넣을 수도 있습니다. 예를 들면:
```kotlin
println("${if (true) "Correct" else "Wrong"}")
// prints Correct
```

# --instructions--

`ERROR`를 출력하세요

# --seed--

```kotlin
println("${if ([/]) "OK" else [/]}")
```

# --answers--

- false
- true
- "ERROR"
- ERROR

# --solutions--

```kotlin
println("${if (false) "OK" else "ERROR"}")
```

# --output--

ERROR
