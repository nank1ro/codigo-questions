---
language: kotlin
exerciseType: 4
---

# --description--

안전 호출 연쇄는 어느 단계든 없을 수 있는 중첩된 객체에서 빛을 발합니다:
```kotlin
class Address(val city: String?)
class User(val address: Address?)

val user = User(Address("Rome"))
println(user.address?.city ?: "unknown") // Rome
val nobody = User(null)
println(nobody.address?.city ?: "unknown") // unknown
```
각 `?.`는 다음 단계를 보호하고, 마지막 `?:`는 기본값을 제공합니다.

# --instructions--

주소가 없는 사용자에 대해 `unknown`을 출력하도록 이 줄들을 올바른 순서로 정렬할 수 있습니까? 클래스를 먼저 정의하고, `User`보다 `Address`를 앞에 두세요

# --answers--

```kotlin
class Address(val city: String?)
```

```kotlin
class User(val address: Address?)
```

```kotlin
val bob = User(null)
```

```kotlin
println(bob.address?.city ?: "unknown")
```

# --solutions--

```kotlin
class Address(val city: String?)
class User(val address: Address?)
val bob = User(null)
println(bob.address?.city ?: "unknown")
```

# --output--

unknown
