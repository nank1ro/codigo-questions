---
language: kotlin
exerciseType: 1
---

# --description--

현실적인 패턴은 보통 이 모든 것을 한꺼번에 섞습니다. 필요한 부분을 담아 둘 그룹, 문자 그대로의 점을 위한 이스케이프한 `\.`, 그리고 그 주변으로 텍스트를 다시 조립하는 람다 대체입니다.
```kotlin
val email = Regex("""(\w+)@(\w+\.\w+)""")
println(email.replace("ping ann@mail.com now") { m -> "<" + m.groupValues[2] + ">" })
// ping <mail.com> now
```
패턴은 일이 허락하는 한 최대한 단순하게 유지하세요. 모든 올바른 이메일 주소를 묘사하려는 패턴은 읽을 수 없지만, `\w+@\w+\.\w+`면 문장에서 주소를 찾기에 충분합니다.

# --instructions--

모든 주소의 `@` 앞 이름을 `***`로 대체하고 도메인은 유지하는 함수 `redactEmails(text: String): String`을 작성하세요. 주소는 단어 문자의 연속, `@`, 단어 문자의 연속, 점, 단어 문자의 연속입니다.

예: `redactEmails("write to ann@mail.com")`는 `write to ***@mail.com`을 반환합니다.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`redactEmails("write to ann@mail.com")`는 `write to ***@mail.com`을 반환해야 합니다

```kotlin
    tryCatch(redactEmails("write to ann@mail.com") == "write to ***@mail.com")
```

`redactEmails("bob@site.org and cy@site.org")`는 두 주소를 모두 가려야 합니다

```kotlin
    tryCatch(redactEmails("bob@site.org and cy@site.org") == "***@site.org and ***@site.org")
```

`redactEmails("no address here")`는 텍스트를 그대로 반환해야 합니다

```kotlin
    tryCatch(redactEmails("no address here") == "no address here")
```

`redactEmails("")`는 빈 문자열을 반환해야 합니다

```kotlin
    tryCatch(redactEmails("") == "")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun redactEmails(text: String): String {
    return Regex("""(\w+)@(\w+\.\w+)""").replace(text) { m -> "***@" + m.groupValues[2] }
}
```
