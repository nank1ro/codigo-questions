---
language: kotlin
exerciseType: 2
---

# --instructions--

`Int` 타입의 `val` 프로퍼티 `red`, `green`, `blue`를 가진 `Color`라는 이름의 `data class`를 선언하는 코드를 완성하세요.

# --seed--

```kotlin
[/] [/] Color(val red: Int, val green: Int, val blue: Int)
```

# --answers--

- data
- class
- object
- open
- abstract
- sealed

# --solutions--

```kotlin
data class Color(val red: Int, val green: Int, val blue: Int)
```
