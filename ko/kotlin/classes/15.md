---
language: kotlin
exerciseType: 4
---

# --instructions--

`open` 메서드 `area()`를 가진 `open` 클래스 `Shape`와 이를 재정의하는 서브클래스 `Square`를 정의하고 넓이를 출력하기 위해 이 줄들을 올바른 순서로 정렬할 수 있습니까?

# --answers--

```kotlin
open class Shape {
```

```kotlin
    open fun area(): Int = 0
```

```kotlin
}
```

```kotlin
class Square(val side: Int) : Shape() {
```

```kotlin
    override fun area(): Int = side * side
```

```kotlin
}
```

```kotlin
fun main() {
```

```kotlin
    println(Square(4).area())
```

```kotlin
}
```

# --solutions--

```kotlin
open class Shape {
    open fun area(): Int = 0
}
class Square(val side: Int) : Shape() {
    override fun area(): Int = side * side
}
fun main() {
    println(Square(4).area())
}
```

# --output--

16
