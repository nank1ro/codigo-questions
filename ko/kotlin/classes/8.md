---
language: kotlin
exerciseType: 2
---

# --instructions--

`String` 타입의 `val` 프로퍼티 `title`을 가진 주 생성자를 가진 `Book`이라는 이름의 `class`를 정의하는 코드를 완성하세요.

# --seed--

```kotlin
[/] Book([/] title: String)
```

# --answers--

- class
- val
- var
- fun
- object
- data

# --solutions--

```kotlin
class Book(val title: String)
```
