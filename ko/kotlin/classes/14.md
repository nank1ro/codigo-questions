---
language: kotlin
exerciseType: 4
---

# --instructions--

`data class` `Student`를 정의하고 그 `toString()`을 출력하기 위해 이 줄들을 올바른 순서로 정렬할 수 있습니까?

# --answers--

```kotlin
data class Student(val name: String, val grade: Int)
```

```kotlin
fun main() {
```

```kotlin
    val s = Student("Alice", 90)
```

```kotlin
    println(s.toString())
```

```kotlin
}
```

# --solutions--

```kotlin
data class Student(val name: String, val grade: Int)
fun main() {
    val s = Student("Alice", 90)
    println(s.toString())
}
```

# --output--

Student(name=Alice, grade=90)
