---
language: kotlin
exerciseType: 2
---

# --instructions--

`Cat`이 `Animal`을 상속하고 `speak()` 메서드를 재정의하여 `"Meow"`를 반환하도록 코드를 완성하세요.

# --seed--

```kotlin
open class Animal(val name: String) {
    open fun speak(): String = "..."
}

class Cat(name: String) [/] Animal(name) {
    [/] fun speak(): String = "Meow"
}
```

# --answers--

- :
- override
- extends
- open
- super
- implements

# --solutions--

```kotlin
open class Animal(val name: String) {
    open fun speak(): String = "..."
}

class Cat(name: String) : Animal(name) {
    override fun speak(): String = "Meow"
}
```
