---
language: kotlin
exerciseType: 2
---

# --instructions--

`"1.0"`과 같은 `val` 프로퍼티 `version`을 가진 `companion object`를 `AppConfig` 안에 추가하는 코드를 완성하세요.

# --seed--

```kotlin
class AppConfig {
    [/] [/] {
        val version = "1.0"
    }
}
```

# --answers--

- companion
- object
- class
- static
- init
- val

# --solutions--

```kotlin
class AppConfig {
    companion object {
        val version = "1.0"
    }
}
```
