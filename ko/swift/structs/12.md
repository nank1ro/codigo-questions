---
language: swift
exerciseType: 4
---

# --instructions--

구조체를 먼저 선언하고 인스턴스를 `print` 앞에 만들어 아래를 출력하도록 항목을 정렬하세요

# --answers--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int
}
```

```swift
let price = Money(amount: 12)
```

```swift
print("\(Money.currency) \(price.amount)")
```

# --solutions--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int
}
let price = Money(amount: 12)
print("\(Money.currency) \(price.amount)")
```

# --output--

EUR 12
