---
language: swift
exerciseType: 1
---

# --description--

구조체는 다른 타입과 마찬가지의 타입이므로 배열, 딕셔너리, 세트에 저장할 수 있고, 이미 알고 있는 모든 도구가 그대로 동작합니다:
```swift
struct Item {
    var name: String
    var price: Int
}

let items = [Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]

for item in items {
    print(item.name)
}

let names = items.map { $0.name }
let total = items.reduce(0) { $0 + $1.price }
print(total) // 10
```
배열은 *복사본*을 담는다는 점을 기억하세요: `items[0]`를 변수로 읽어 와 변경해도 배열은 바뀌지 않습니다.

# --instructions--

인자 레이블 없이 `Item`의 배열을 받아 모든 항목의 `price` 합계를 반환하고, 배열이 비어 있으면 `0`을 반환하는 함수 `totalPrice`를 작성하세요

함수 호출 예시:
```swift
print(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]))
// prints 10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice() {

}
```

# --asserts--

`3`과 `7`인 두 항목의 합계는 `10`이어야 합니다

```swift
tryCatch(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]) == 10)
```

단일 항목의 합계는 그 항목 자신의 가격이어야 합니다

```swift
tryCatch(totalPrice([Item(name: "Pen", price: 2)]) == 2)
```

빈 배열의 합계는 `0`이어야 합니다

```swift
tryCatch(totalPrice([]) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice(_ items: [Item]) -> Int {
    return items.reduce(0) { $0 + $1.price }
}
```
