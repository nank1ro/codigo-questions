---
language: swift
exerciseType: 4
---

# --instructions--

구조체를 먼저 선언하고 인스턴스를 `print` 앞에 만들어 `12`를 출력하도록 항목을 정렬하세요

# --answers--

```swift
struct Rectangle {
    var width: Int
    var height: Int
    func area() -> Int {
        return width * height
    }
}
```

```swift
let r = Rectangle(width: 3, height: 4)
```

```swift
print(r.area())
```

# --solutions--

```swift
struct Rectangle {
    var width: Int
    var height: Int
    func area() -> Int {
        return width * height
    }
}
let r = Rectangle(width: 3, height: 4)
print(r.area())
```

# --output--

12
