---
language: swift
exerciseType: 2
---

# --description--

저장 프로퍼티는 선언된 바로 그 자리에 **기본값**을 가질 수 있습니다. Swift는 그 값으로 타입을 추론하므로 타입 어노테이션을 생략할 수 있습니다:
```swift
struct Counter {
    var label: String
    var value = 0
}
```
멤버별 이니셜라이저는 기본값이 있는 모든 프로퍼티를 선택적 인자로 바꿉니다: 값을 전달하면 기본값을 덮어쓰고, 생략하면 기본값이 유지됩니다.
```swift
let a = Counter(label: "clicks")
print(a.value) // 0

let b = Counter(label: "clicks", value: 7)
print(b.value) // 7
```

# --instructions--

`value`가 `0`으로 시작하고 `value`를 전달하지 않고 `c`가 생성되도록 코드를 완성하세요

# --seed--

```swift
struct Counter {
    var label: String
    var value = [/]
}

let c = Counter([/]: "clicks")
print(c.label)
print(c.value)
```

# --answers--

- 0
- label
- 1
- value
- Counter
- name

# --solutions--

```swift
struct Counter {
    var label: String
    var value = 0
}

let c = Counter(label: "clicks")
print(c.label)
print(c.value)
```

# --output--

clicks
0
