---
language: swift
exerciseType: 2
---

# --description--

멤버별 이니셜라이저가 원하는 방식으로 타입을 만들어 주지 않는다면, 직접 **이니셜라이저**를 작성하면 됩니다. `init`으로 선언하고 원하는 매개변수를 받으며, 끝나기 전에 모든 저장 프로퍼티에 값을 부여해야 합니다. 그 안에서 `self`는 생성 중인 인스턴스입니다:
```swift
struct Square {
    var side: Int

    init(_ side: Int) {
        self.side = side
    }
}

let s = Square(5)
print(s.side) // 5
```
구조체의 중괄호 안에 `init`을 작성하면 멤버별 이니셜라이저를 대체하므로, 이후로는 `Square(side: 5)`가 더 이상 존재하지 않습니다.

# --instructions--

`Square(5)`가 `side`에 `5`를 저장하도록 이니셜라이저를 완성하세요

# --seed--

```swift
struct Square {
    var side: Int

    [/](_ side: [/]) {
        [/].side = side
    }
}

let s = Square(5)
print(s.side)
```

# --answers--

- init
- Int
- self
- func
- String
- this

# --solutions--

```swift
struct Square {
    var side: Int

    init(_ side: Int) {
        self.side = side
    }
}

let s = Square(5)
print(s.side)
```

# --output--

5
