---
language: swift
exerciseType: 4
---

# --description--

요약 다음에는 Xcode가 인식하는 특별한 마크다운 목록 항목으로 매개변수와 반환 값을 설명할 수 있습니다:
```swift
/// Returns the number of seconds in the given minutes.
/// - Parameter minutes: a whole number of minutes, never negative
/// - Returns: `minutes` multiplied by sixty
func toSeconds(_ minutes: Int) -> Int {
    return minutes * 60
}
```
순서는 항상 같습니다: 먼저 요약, 그다음 각 매개변수마다 `- Parameter 이름:`, 그리고 `- Returns:`.

# --instructions--

항목을 정렬하여 `twice`를 요약, `Parameter` 줄, `Returns` 줄 순서로 함수 바로 위에 문서화한 뒤 `twice(21)`을 출력하세요

# --answers--

```swift
/// Returns the double of a number.
```

```swift
/// - Parameter n: the number to double
```

```swift
/// - Returns: `n` multiplied by two
```

```swift
func twice(_ n: Int) -> Int {
    return n * 2
}
```

```swift
print(twice(21))
```

# --solutions--

```swift
/// Returns the double of a number.
/// - Parameter n: the number to double
/// - Returns: `n` multiplied by two
func twice(_ n: Int) -> Int {
    return n * 2
}
print(twice(21))
```

# --output--

42
