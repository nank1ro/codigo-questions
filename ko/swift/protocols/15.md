---
language: swift
exerciseType: 1
---

# --description--

프로토콜 이름 자체는 타입이 아니라 제약이므로, Swift는 두 가지 중 무엇을 뜻하는지 밝혀 달라고 요구합니다.

`some Shape`는 *컴파일 시점에 정해지는 하나의 특정한 준수 타입*을 뜻합니다. 호출하는 쪽은 그것이 무엇인지 절대 알 수 없지만, 언제나 같은 타입입니다:
```swift
func unitSquare() -> some Shape {
    return Square(side: 1)
}
```
`any Shape`는 *준수하는 어떤 타입이든 담을 수 있는 상자*를 뜻하며, 그 타입의 두 값이 서로 다른 타입을 담고 있을 수도 있습니다. 혼합 배열 안처럼 구체 타입이 달라질 수 있는 곳에서는 이것이 필요합니다:
```swift
let shapes: [any Shape] = [Square(side: 2), Rect(width: 2, height: 3)]
```
둘 다 프로토콜의 요구사항을 호출할 수 있게 해 줍니다. 하나의 타입으로 충분하다면 실행 시점 비용이 없는 `some`을 택하고, 정말로 타입을 섞어야 할 때 `any`를 쓰세요.

# --instructions--

인자 레이블 없이 `any Shape`의 배열을 받아 그 넓이의 합을 반환하는 함수 `totalArea`를 작성하세요

함수 호출 예시:
```swift
print(totalArea([Square(side: 2), Rect(width: 2, height: 3)]))
// 10을 출력
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Shape {
    func area() -> Int
}

struct Square: Shape {
    var side: Int

    func area() -> Int {
        return side * side
    }
}

struct Rect: Shape {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}

func unitSquare() -> some Shape {
    return Square(side: 1)
}

func totalArea() {

}
```

# --asserts--

변이 `2`인 정사각형과 `2` 곱하기 `3` 직사각형의 합은 `10`이어야 합니다

```swift
tryCatch(totalArea([Square(side: 2), Rect(width: 2, height: 3)]) == 10)
```

빈 배열의 합은 `0`이어야 합니다

```swift
tryCatch(totalArea([]) == 0)
```

`5` 곱하기 `4` 직사각형 하나의 합은 `20`이어야 합니다

```swift
tryCatch(totalArea([Rect(width: 5, height: 4)]) == 20)
```

단위 정사각형 하나만의 합은 `1`이어야 합니다

```swift
tryCatch(totalArea([unitSquare()]) == 1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Shape {
    func area() -> Int
}

struct Square: Shape {
    var side: Int

    func area() -> Int {
        return side * side
    }
}

struct Rect: Shape {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}

func unitSquare() -> some Shape {
    return Square(side: 1)
}

func totalArea(_ shapes: [any Shape]) -> Int {
    var total = 0
    for shape in shapes {
        total += shape.area()
    }
    return total
}
```
