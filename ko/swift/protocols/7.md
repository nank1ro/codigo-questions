---
language: swift
exerciseType: 1
---

# --description--

구조체는 값 타입이므로, 저장 프로퍼티 중 하나를 바꾸는 메서드에는 `mutating`을 표시해야 합니다. 그 메서드가 프로토콜 요구사항이라면 프로토콜에도 그렇게 적어야 합니다:
```swift
protocol Togglable {
    mutating func toggle()
}

struct Light: Togglable {
    var isOn = false

    mutating func toggle() {
        isOn = !isOn
    }
}

var lamp = Light()
lamp.toggle()
print(lamp.isOn) // true
```
프로토콜에 `mutating`이 없다면 구조체는 그 요구사항을 결코 만족시킬 수 없습니다. 클래스는 참조 타입이라 이 키워드가 전혀 필요 없습니다: 클래스는 평범한 메서드로 `mutating` 요구사항을 충족합니다. mutating 메서드를 호출하려면 `var`가 필요합니다 — `let`에서 호출하면 컴파일 오류입니다.

# --instructions--

`points`를 `0`으로 설정하는 메서드 `reset()`으로 `Score`가 `Resettable`을 준수하게 만드세요

사용 예시:
```swift
var score = Score(points: 10)
score.reset()
print(score.points)
// 0을 출력
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Resettable {
    mutating func reset()
}

struct Score {
    var points: Int
}
```

# --before-asserts--

```swift
func _reset<T: Resettable>(_ v: inout T) {
    v.reset()
}
```

# --asserts--

`10`점은 리셋 후 `0`이어야 합니다

```swift
var _s1 = Score(points: 10)
_reset(&_s1)
tryCatch(_s1.points == 0)
```

`-4`점은 리셋 후 `0`이어야 합니다

```swift
var _s2 = Score(points: -4)
_reset(&_s2)
tryCatch(_s2.points == 0)
```

`0`점은 리셋 후에도 `0`이어야 합니다

```swift
var _s3 = Score(points: 0)
_reset(&_s3)
tryCatch(_s3.points == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Resettable {
    mutating func reset()
}

struct Score: Resettable {
    var points: Int

    mutating func reset() {
        points = 0
    }
}
```
