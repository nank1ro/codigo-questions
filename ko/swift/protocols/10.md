---
language: swift
exerciseType: 4
---

# --description--

프로토콜 익스텐션은 프로토콜이 요구사항으로 나열하지 않은 멤버도 추가할 수 있습니다. 그것들은 준수하는 모든 타입에서 쓸 수 있는 추가 편의 기능입니다:
```swift
protocol Sized {
    var count: Int { get }
}

extension Sized {
    var isEmpty: Bool {
        return count == 0
    }
}
```
`isEmpty`는 요구사항이 아니므로 준수하는 타입이 제공할 필요가 없습니다 — 그냥 얻게 됩니다.

# --instructions--

`HEY`를 출력하도록 항목을 정렬하세요. 프로토콜을 먼저, 그다음 그 익스텐션, 그다음 구조체, 마지막에 `print`를 놓으세요

# --answers--

```swift
protocol Loud {
    var text: String { get }
}
```

```swift
extension Loud {
    func shout() -> String { return text.uppercased() }
}
```

```swift
struct Message: Loud {
    var text: String
}
```

```swift
print(Message(text: "hey").shout())
```

# --solutions--

```swift
protocol Loud {
    var text: String { get }
}
extension Loud {
    func shout() -> String { return text.uppercased() }
}
struct Message: Loud {
    var text: String
}
print(Message(text: "hey").shout())
```

# --output--

HEY
