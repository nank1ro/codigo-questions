---
language: swift
exerciseType: 1
---

# --description--

표준 라이브러리는 프로토콜로 만들어져 있으며, 여러분이 만든 타입도 그 프로토콜들을 채택할 수 있습니다.

`Equatable`은 타입에 `==` 연산자를 부여합니다. 저장 프로퍼티가 모두 `Equatable`인 구조체라면 준수를 선언하는 것만으로 충분합니다 — Swift가 `==`를 대신 작성해 줍니다:
```swift
struct Point: Equatable {
    var x: Int
    var y: Int
}

print(Point(x: 1, y: 2) == Point(x: 1, y: 2)) // true
```
`Comparable`은 `Equatable`을 상속하고 순서를 추가합니다. 두 값을 받는 `static func`로 작성하는 `<` 연산자 하나만 구현하면, `>`, `<=`, `>=`와 컬렉션의 `sorted()`, `min()`, `max()`를 공짜로 얻습니다:
```swift
struct Version: Comparable {
    var number: Int

    static func < (lhs: Version, rhs: Version) -> Bool {
        return lhs.number < rhs.number
    }
}
```

# --instructions--

`grams`를 기준으로 순서를 정하도록 `Weight`가 `Comparable`을 준수하게 만드세요

사용 예시:
```swift
print(Weight(grams: 100) < Weight(grams: 250))
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Weight {
    var grams: Int
}
```

# --asserts--

`100`그램은 `250`그램보다 가벼워야 합니다

```swift
tryCatch(Weight(grams: 100) < Weight(grams: 250))
```

`500`그램은 `250`그램보다 무거워야 합니다

```swift
tryCatch(Weight(grams: 500) > Weight(grams: 250))
```

`80`그램인 두 무게는 같아야 합니다

```swift
tryCatch(Weight(grams: 80) == Weight(grams: 80))
```

`3`그램과 `1`그램 중 가장 무거운 것은 `3`그램이어야 합니다

```swift
tryCatch([Weight(grams: 3), Weight(grams: 1)].max() == Weight(grams: 3))
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Weight: Comparable {
    var grams: Int

    static func < (lhs: Weight, rhs: Weight) -> Bool {
        return lhs.grams < rhs.grams
    }
}
```
