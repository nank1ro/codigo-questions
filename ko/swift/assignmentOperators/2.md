---
language: swift
exerciseType: 2
---

# --description--

이미 초기화된 변수 `total`이 있습니다
```swift
var total = 5
```
변수 `total`에 숫자 `2`를 더하고 싶다면, 다음과 같이 작성할 수 있습니다
```swift
total = total + 2
```
네, 잘 작동합니다! 하지만 같은 작업을 수행하는 더 짧은 방법이 있습니다:
```swift
total += 2
```
`+=` 기호는 **덧셈 대입**이라고 합니다.
이 연산자는 변수의 값에 값을 더한 후 그 결과를 해당 변수에 할당합니다.

# --instructions--

*덧셈 대입* 연산자를 사용하여 변수 `num`에 값 `15`를 더한 다음, 그 값을 출력하세요

# --seed--

```swift
var num = 75
[/][/][/]
print(num)
```

# --answers--

- +
- +=
- =+
- num 
-  15

# --solutions--

```swift
var num = 75
num += 15
print(num)
```

# --output--

90
