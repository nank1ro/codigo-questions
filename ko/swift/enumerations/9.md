---
language: swift
exerciseType: 1
---

# --instructions--

열거형을 반복할 수 있도록 적절한 타입을 추가하세요

# --seed--

```swift
enum Gender {
    case male, female
}
```

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`Gender`는 반복 가능해야 합니다.

```swift
    func test1() {
        XCTAssertEqual(Gender.allCases.count, 2, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
enum Gender: CaseIterable {
    case male, female
}
```
