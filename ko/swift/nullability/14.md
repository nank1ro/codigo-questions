---
language: swift
exerciseType: 4
---

# --description--

여러 옵셔널을 하나의 `if let`에 바인딩하면 코드가 평탄하게 유지됩니다: 하나의 `else` 분기가 값이 없는 모든 경우를 처리합니다.

# --instructions--

`width`와 `height` 둘 다 값이 있으면 `Area: 20`을, 그렇지 않으면 `Missing size`를 출력하도록 항목을 정렬하세요.
`width`를 `height`보다 먼저 선언하세요

# --answers--

```swift
let width: Int? = 4
```

```swift
let height: Int? = 5
```

```swift
if let w = width, let h = height {
```

```swift
    print("Area: \(w * h)")
```

```swift
} else {
    print("Missing size")
}
```

# --solutions--

```swift
let width: Int? = 4
let height: Int? = 5
if let w = width, let h = height {
    print("Area: \(w * h)")
} else {
    print("Missing size")
}
```

# --output--

Area: 20
