---
language: swift
exerciseType: 2
---

# --description--

실패할 수 있는 변환은 `guard let`과 자연스럽게 어울립니다: 변환하고, 결과가 `nil`이면 중단한 뒤, 일반 값으로 작업합니다.

# --instructions--

텍스트를 `Int`로 변환해 그 두 배 값을 반환하고, 변환이 실패하면 `nil`을 반환하도록 함수를 완성하세요.
그런 다음 결과를 출력하되, `0`을 대체 값으로 사용하세요

# --seed--

```swift
func doubled(_ text: String) -> Int? {
    guard let number = [/](text) else { return [/] }
    return number * 2
}
print(doubled("21") [/] 0)
print(doubled("x") ?? 0)
```

# --answers--

- Int
- nil
- ??
- Int?
- String
- !

# --solutions--

```swift
func doubled(_ text: String) -> Int? {
    guard let number = Int(text) else { return nil }
    return number * 2
}
print(doubled("21") ?? 0)
print(doubled("x") ?? 0)
```

# --output--

42
0
