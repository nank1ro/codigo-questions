---
language: swift
exerciseType: 4
---

# --description--

상수에 저장된 클로저는 매개변수의 인자 레이블을 사용해 클로저 인수가 필요한 곳이라면 어디든 전달할 수 있습니다:
```swift
let ascending = { (a: Int, b: Int) -> Bool in a < b }
print([3, 1, 2].sorted(by: ascending)) // [1, 2, 3]
```

# --instructions--

항목을 정렬하여 두 문자열을 짧은 것부터 긴 것 순으로 정렬하는 클로저를 `byLength`에 저장한 뒤, 이를 사용해 정렬한 `words`를 출력하세요.
`words`를 `byLength`보다 먼저 선언하세요

# --answers--

```swift
let words = ["kiwi", "fig", "banana"]
```

```swift
let byLength = { (a: String, b: String) -> Bool in
```

```swift
    a.count < b.count
```

```swift
}
```

```swift
print(words.sorted(by: byLength))
```

# --solutions--

```swift
let words = ["kiwi", "fig", "banana"]
let byLength = { (a: String, b: String) -> Bool in
    a.count < b.count
}
print(words.sorted(by: byLength))
```

# --output--

["fig", "kiwi", "banana"]
