---
language: swift
exerciseType: 1
---

# --description--

기본적으로 함수에 전달된 클로저는 그 함수가 실행되는 동안에만 사용될 수 있습니다. 함수가 클로저를 저장하거나 그것을 사용하는 다른 클로저를 반환하면, 그 클로저는 함수를 **탈출**한다고 하며, 해당 매개변수에는 `@escaping`을 표시해야 합니다:
```swift
func twice(_ task: @escaping () -> Int) -> () -> Int {
    return { task() * 2 }
}
let answer = twice { 21 }
print(answer()) // 42
```
`@escaping`이 없으면 컴파일러가 오류를 보고합니다. 반환된 클로저가 `twice`가 끝난 뒤에 `task`를 사용하게 되기 때문입니다.

# --instructions--

`(Int) -> Int` 타입의 클로저 두 개를 받아(두 인자 레이블 모두 생략) 첫 번째 클로저를 적용한 다음 그 결과에 두 번째 클로저를 적용하는 클로저를 반환하는 함수 `compose`를 작성하세요

함수 호출 예시:
```swift
let addThenDouble = compose({ $0 + 1 }, { $0 * 2 })
print(addThenDouble(3))
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`compose({ $0 + 1 }, { $0 * 2 })(3)`은 `8`을 반환해야 합니다

```swift
tryCatch(compose({ $0 + 1 }, { $0 * 2 })(3) == 8)
```

`compose({ $0 * 2 }, { $0 + 1 })(3)`은 `7`을 반환해야 합니다

```swift
tryCatch(compose({ $0 * 2 }, { $0 + 1 })(3) == 7)
```

`compose({ $0 }, { $0 })(9)`는 `9`를 반환해야 합니다

```swift
tryCatch(compose({ $0 }, { $0 })(9) == 9)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func compose(_ first: @escaping (Int) -> Int, _ second: @escaping (Int) -> Int) -> (Int) -> Int {
    return { second(first($0)) }
}
```
