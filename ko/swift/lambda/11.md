---
language: swift
exerciseType: 2
---

# --description--

클로저는 자신의 본문 밖에서 선언된 변수를 사용할 수 있습니다. 클로저는 그 변수를 **캡처**합니다. 즉, 그 변수를 선언한 함수가 이미 반환되었더라도, 클로저가 존재하는 한 그 변수는 계속 살아 있습니다.
이를 통해 함수는 자신만의 프라이빗한 상태를 가진 클로저를 만들 수 있습니다:
```swift
func makeCounter() -> () -> Int {
    var count = 0
    return {
        count += 1
        return count
    }
}
```
`() -> Int`는 매개변수가 없고 `Int`를 반환하는 클로저의 타입입니다. 반환된 클로저를 호출할 때마다 캡처된 같은 `count`가 증가합니다:
```swift
let counter = makeCounter()
print(counter()) // 1
print(counter()) // 2
```

# --instructions--

`makeCounter`를 완성하여 매개변수가 없고 캡처한 `count`를 증가시켜 반환하는 클로저를 반환하도록 하여, 프로그램이 세 줄에 걸쳐 `1`, `2`, `3`을 출력하도록 하세요

# --seed--

```swift
func makeCounter() -> [/] {
    [/] count = 0
    return {
        count += 1
        return count
    }
}
let counter = makeCounter()
print(counter())
print(counter())
print(counter())
```

# --answers--

- () -> Int
- var
- let
- Int
- () -> Void

# --solutions--

```swift
func makeCounter() -> () -> Int {
    var count = 0
    return {
        count += 1
        return count
    }
}
let counter = makeCounter()
print(counter())
print(counter())
print(counter())
```

# --output--

1
2
3
