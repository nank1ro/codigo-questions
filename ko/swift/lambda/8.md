---
language: swift
exerciseType: 2
---

# --description--

`reduce`는 모든 요소를 하나의 값으로 합칩니다. 초깃값과, 두 개의 인수(지금까지 누적된 값과 현재 요소)를 받는 클로저를 인수로 받습니다. 클로저는 새로운 누적값을 반환합니다:
```swift
let nums = [1, 2, 3, 4]
let product = nums.reduce(1) { $0 * $1 }
print(product) // 24
```
여기서 `$0`은 `1`에서 시작해 `1 * 1`, `1 * 2`, `2 * 3`, 그리고 마지막으로 `6 * 4`가 됩니다.
`map`, `filter`, `reduce`는 모두 값을 반환하므로 `nums.filter { $0 > 1 }.map { $0 * 10 }`처럼 연결할 수 있습니다.

# --instructions--

`reduce`를 사용해 `nums`의 모든 숫자를 `0`부터 더하도록 코드를 완성하여, 프로그램이 `10`을 출력하도록 하세요

# --seed--

```swift
let nums = [1, 2, 3, 4]
let total = nums.[/]([/]) { $0 + $1 }
print(total)
```

# --answers--

- reduce
- 0
- map
- filter
- 1

# --solutions--

```swift
let nums = [1, 2, 3, 4]
let total = nums.reduce(0) { $0 + $1 }
print(total)
```

# --output--

10
