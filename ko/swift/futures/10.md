---
language: swift
exerciseType: 1
---

# --description--

`Task.sleep`은 다른 것을 막지 않으면서 현재 작업을 잠시 멈춥니다. 중단될 수 있으므로 오류를 던지는 비동기 호출이며 `try await`가 필요합니다. 지속 시간은 `.seconds`, `.milliseconds`, `.nanoseconds` 같은 헬퍼로 지정합니다:
```swift
func slowGreeting() async throws -> String {
    try await Task.sleep(for: .milliseconds(50))
    return "hello"
}
```
이것은 실제 네트워크 호출 대신 예제에서 느린 작업을 시뮬레이션하는 표준 방법입니다. 프로그램을 얼리지 않는다는 점에 주의하세요: 한 작업이 잠들어 있는 동안에도 다른 작업들은 계속 실행됩니다.

# --instructions--

정수를 받아(인자 레이블 생략) `Task.sleep`으로 `50`밀리초 기다린 뒤 그 수에 `3`을 곱해 반환하는, 오류를 던질 수 있는 비동기 함수 `delayed`를 작성하세요

함수 호출 예시:
```swift
print(try await delayed(2))
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func delayed() {

}
```

# --asserts--

함수는 `async throws`로 표시되어야 합니다

```json
{
  "regex": "func\\s+delayed\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

함수는 `Task.sleep`을 사용해야 합니다

```json
{
  "regex": "Task\\.sleep",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`delayed(2)`는 `6`을 반환해야 합니다

```swift
tryCatch((try? await delayed(2)) == 6)
```

`delayed(0)`은 `0`을 반환해야 합니다

```swift
tryCatch((try? await delayed(0)) == 0)
```

`delayed(-4)`는 `-12`를 반환해야 합니다

```swift
tryCatch((try? await delayed(-4)) == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func delayed(_ n: Int) async throws -> Int {
    try await Task.sleep(for: .milliseconds(50))
    return n * 3
}
```
