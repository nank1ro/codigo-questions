---
language: swift
exerciseType: 3
---

# --description--

비동기 함수는 보통 서로를 쌓아 만듭니다. `async` 함수 안에서는 다른 `async` 함수를 `await`할 수 있으며, 그 결과는 평범한 값처럼 사용합니다:
```swift
func base() async -> Int {
    return 10
}

func withBonus() async -> Int {
    let value = await base()
    return value + 5
}

print(await withBonus())
// prints 15
```
`await`는 비동기 컨텍스트 안에서만 허용됩니다: `async` 함수 또는 스크립트의 최상위 레벨입니다. 평범한, `async`가 아닌 함수는 아무것도 `await`할 수 없습니다.

# --instructions--

이 코드는 출력에 무엇을 표시합니까?
```swift
func first() async -> Int {
    return 10
}

func second() async -> Int {
    let base = await first()
    return base + 5
}

print(await second())
```

# --answers--

- `15`
- `10`
- `5`
- 아무것도 표시되지 않습니다, `second`가 await되지 않기 때문입니다

# --solutions--

- `15`
