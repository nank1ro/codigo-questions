---
language: swift
exerciseType: 1
---

# --description--

`async let`은 쓰인 스코프에 묶여 있습니다. 동시 작업을 시작하면서 그 핸들을 유지하려면 **`Task`**를 사용하세요. `Task { }`에 전달된 클로저는 자체적으로 실행되며, 작업은 저장되거나 전달되거나 반환될 수 있습니다:
```swift
let job = Task {
    return await double(21)
}
```
결과는 나중에 **`.value`**로 읽으며, 이는 await됩니다:
```swift
print(await job.value)
// prints 42
```
핸들의 타입은 무엇을 만들어내고 무엇을 던질 수 있는지 말해줍니다: `Task<Int, Never>`는 `Int`를 반환하고 결코 오류를 던지지 않는 작업입니다. `async let`과 달리 `Task`는 평범한, 비동기가 아닌 코드에서 만들 수 있습니다.

# --instructions--

정수를 받아(인자 레이블 생략) 그 수의 두 배를 만들어내는 `Task<Int, Never>`를 반환하는 함수 `startDoubling`을 작성하세요

함수 호출 예시:
```swift
print(await startDoubling(4).value)
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func startDoubling() {

}
```

# --asserts--

함수는 `Task`를 생성해야 합니다

```json
{
  "regex": "Task\\s*\\{",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`startDoubling(4).value`는 `8`이어야 합니다

```swift
tryCatch(await startDoubling(4).value == 8)
```

`startDoubling(0).value`는 `0`이어야 합니다

```swift
tryCatch(await startDoubling(0).value == 0)
```

`startDoubling(-6).value`는 `-12`이어야 합니다

```swift
tryCatch(await startDoubling(-6).value == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func startDoubling(_ n: Int) -> Task<Int, Never> {
    return Task {
        return n * 2
    }
}
```
