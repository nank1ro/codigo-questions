---
language: swift
exerciseType: 1
---

# --description--

두 개의 독립적인 호출을 동시에 실행하려면 **`async let`**으로 선언하세요. 작업은 즉시 시작되고, 프로그램은 기다리지 않고 계속 진행됩니다:
```swift
async let left = step("A")
async let right = step("B")
```
값은 아직 준비되지 않았으므로 바인딩을 바로 쓸 수 없습니다: 최종적으로 필요한 지점에서 `await`해야 합니다. 표현식 앞의 `await` 하나가 그 안의 모든 `async let`을 처리합니다:
```swift
let both = await left + right
```
각 호출이 1초 걸린다면 순차 버전은 2초가 필요하지만 `async let` 버전은 두 호출이 겹치기 때문에 약 1초면 충분합니다.

# --instructions--

두 정수를 받아(인자 레이블 모두 생략) `async let`으로 두 호출을 동시에 시작하여 각각에 `slowValue`를 적용한 합을 반환하는 비동기 함수 `total`을 작성하세요

함수 호출 예시:
```swift
print(await total(1, 2))
// prints 30
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total() {

}
```

# --asserts--

함수는 `async let`을 사용해야 합니다

```json
{
  "regex": "async\\s+let",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`total(1, 2)`는 `30`을 반환해야 합니다

```swift
tryCatch(await total(1, 2) == 30)
```

`total(0, 0)`은 `0`을 반환해야 합니다

```swift
tryCatch(await total(0, 0) == 0)
```

`total(5, -2)`는 `30`을 반환해야 합니다

```swift
tryCatch(await total(5, -2) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total(_ a: Int, _ b: Int) async -> Int {
    async let first = slowValue(a)
    async let second = slowValue(b)
    return await first + second
}
```
