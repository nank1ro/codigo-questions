---
language: swift
exerciseType: 2
---

# --description--

호출이 *왜* 실패했는지 신경 쓰지 않을 때는 `try?`가 `do` 블록보다 짧습니다. `try?`는 오류를 던지는 호출을 **옵셔널**로 바꿉니다: 성공하면 값, 실패하면 `nil`입니다. `await`와 결합하면 `try? await`라고 씁니다:
```swift
let value = try? await parse("42")  // Optional(42)
let broken = try? await parse("x")  // nil
```
결과가 옵셔널이므로 `if let`에 그대로 들어맞습니다:
```swift
if let value = try? await parse("x") {
    print(value)
} else {
    print("not a number")
}
```
빠른 폴백에는 `try? await`를, 오류 자체가 중요할 때는 `do` / `catch`를 사용하세요.

# --instructions--

실패한 호출이 `nil`을 만들어내고 프로그램이 `not a number`를 출력하도록 코드를 완성하세요

# --seed--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = [/] await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --answers--

- try?
- try
- try!
- await
- throws

# --solutions--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = try? await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --output--

not a number
