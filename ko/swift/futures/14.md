---
language: swift
exerciseType: 4
---

# --instructions--

항목을 정렬하여 작업 그룹에서 `[1, 2, 3]`의 모든 숫자를 두 배로 만들고 그 합을 출력하는 프로그램을 만드세요
```
12
```

# --answers--

```swift
let total = await withTaskGroup(of: Int.self) { group in
```

```swift
    for n in [1, 2, 3] { group.addTask { n * 2 } }
```

```swift
    var sum = 0
```

```swift
    for await value in group { sum += value }
```

```swift
    return sum
```

```swift
}
```

```swift
print(total)
```

# --solutions--

```swift
let total = await withTaskGroup(of: Int.self) { group in
    for n in [1, 2, 3] { group.addTask { n * 2 } }
    var sum = 0
    for await value in group { sum += value }
    return sum
}
print(total)
```

```swift
let total = await withTaskGroup(of: Int.self) { group in
    var sum = 0
    for n in [1, 2, 3] { group.addTask { n * 2 } }
    for await value in group { sum += value }
    return sum
}
print(total)
```

# --output--

12
