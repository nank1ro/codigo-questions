---
language: swift
exerciseType: 4
---

# --description--

set은 집합론의 고전적인 연산을 지원합니다. 각 연산은 **새로운** set을 반환하며 원본은 변경되지 않습니다:
- `a.union(b)`(합집합)는 `a`, `b`, 또는 둘 다에 있는 모든 요소를 포함합니다
- `a.intersection(b)`(교집합)는 `a`와 `b` **둘 다**에 있는 요소만 포함합니다
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.union(b).sorted())        // [1, 2, 3, 4]
print(a.intersection(b).sorted()) // [3]
```

# --instructions--

`odds`나 `primes`에 나타나는 모든 숫자를 오름차순으로 출력하도록 항목을 정렬하세요.
`primes`보다 먼저 `odds`를 선언하세요

# --answers--

```swift
let odds: Set<Int> = [1, 3, 5]
```

```swift
let primes: Set<Int> = [2, 3, 5]
```

```swift
let all = odds.union(primes)
```

```swift
print(all.sorted())
```

# --solutions--

```swift
let odds: Set<Int> = [1, 3, 5]
let primes: Set<Int> = [2, 3, 5]
let all = odds.union(primes)
print(all.sorted())
```

# --output--

[1, 2, 3, 5]
