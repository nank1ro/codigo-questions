---
language: swift
exerciseType: 4
---

# --description--

set끼리 서로 비교할 수도 있습니다. 이 메서드들은 `Bool`을 반환합니다:
- `a.isSubset(of: b)`(부분집합)는 `a`의 모든 요소가 `b`에도 있을 때 `true`입니다
- `a.isSuperset(of: b)`(상위집합)는 `a`가 `b`의 모든 요소를 포함할 때 `true`입니다
- `a.isDisjoint(with: b)`(서로소)는 `a`와 `b`에 공통 요소가 없을 때 `true`입니다
```swift
let small: Set<Int> = [1, 2]
let big: Set<Int> = [1, 2, 3]
print(small.isSubset(of: big))   // true
print(big.isSuperset(of: small)) // true
print(small.isDisjoint(with: big)) // false
```

# --instructions--

먼저 `basket`의 모든 과일이 `fruits`에도 있는지 확인하고, 그다음 두 set에 공통 요소가 전혀 없는지 확인하도록 항목을 정렬하세요.
`basket`보다 먼저 `fruits`를 선언하세요

# --answers--

```swift
let fruits: Set<String> = ["apple", "pear", "plum"]
```

```swift
let basket: Set<String> = ["pear", "plum"]
```

```swift
print(basket.isSubset(of: fruits))
```

```swift
print(fruits.isDisjoint(with: basket))
```

# --solutions--

```swift
let fruits: Set<String> = ["apple", "pear", "plum"]
let basket: Set<String> = ["pear", "plum"]
print(basket.isSubset(of: fruits))
print(fruits.isDisjoint(with: basket))
```

# --output--

true
false
