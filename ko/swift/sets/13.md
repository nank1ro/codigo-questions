---
language: swift
exerciseType: 1
---

# --instructions--

두 개의 `Set<String>`을 받아서 (인자 레이블은 생략) `Bool`을 반환하는 두 함수를 작성하세요:
- `canAccess`는 필요한 권한과 부여된 권한을 받아, 필요한 권한이 모두 부여되었을 때만 `true`를 반환합니다
- `noOverlap`은 두 set에 공통된 권한이 없을 때 `true`를 반환합니다

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`canAccess(["read"], ["read", "write"])`는 `true`를 반환해야 합니다

```swift
tryCatch(canAccess(["read"], ["read", "write"]) == true)
```

`canAccess(["read", "delete"], ["read", "write"])`는 `false`를 반환해야 합니다

```swift
tryCatch(canAccess(["read", "delete"], ["read", "write"]) == false)
```

`canAccess([], ["read"])`는 `true`를 반환해야 합니다

```swift
tryCatch(canAccess([], ["read"]) == true)
```

`noOverlap(["read"], ["write"])`는 `true`를 반환해야 합니다

```swift
tryCatch(noOverlap(["read"], ["write"]) == true)
```

`noOverlap(["read", "write"], ["write"])`는 `false`를 반환해야 합니다

```swift
tryCatch(noOverlap(["read", "write"], ["write"]) == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func canAccess(_ required: Set<String>, _ granted: Set<String>) -> Bool {
    return required.isSubset(of: granted)
}

func noOverlap(_ first: Set<String>, _ second: Set<String>) -> Bool {
    return first.isDisjoint(with: second)
}
```
