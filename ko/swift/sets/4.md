---
language: swift
exerciseType: 2
---

# --description--

값이 set에 있는지 확인하려면 `Bool`을 반환하는 `contains(_:)` 메서드를 사용합니다:
```swift
let primes: Set<Int> = [2, 3, 5, 7]
print(primes.contains(5)) // true
print(primes.contains(6)) // false
```
이 확인은 요소가 수천 개라도 set에서는 매우 빠릅니다. 이것이 멤버십 확인 시 배열보다 set을 선호하는 주된 이유 중 하나입니다.

# --instructions--

`role`이 허용된 역할 중 하나이면 `Welcome`을, 그렇지 않으면 `Access denied`를 출력하세요

# --seed--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed[/]([/]) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --answers--

- .contains
- .has
- .includes
- role
- "role"
- allowed

# --solutions--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed.contains(role) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --output--

Welcome
