---
language: swift
exerciseType: 2
---

# --description--

때때로 배열의 일부분만 접근하고 싶을 수 있습니다.
다음 코드를 살펴보세요:
```swift
let numbers = [1, 2, 3, 4]
let slice = numbers[1...2]
print(slice)
// [2, 3]을 출력합니다
```
먼저 `numbers`라는 배열을 생성합니다.
그런 다음 배열의 일부분을 가져와 slice 배열에 저장합니다.
배열 이름 뒤에 포함할 인덱스를 정의하여 이를 수행합니다: `numbers[1...2]`.
Swift에서는 `...`를 사용하여 마지막 인덱스를 포함할 수 있지만, `..<`를 사용하여 마지막 인덱스를 제외할 수도 있습니다

# --instructions--

`numbers`에서 가운데 두 숫자만 포함하는 `middle`이라는 배열을 생성하세요

# --seed--

```swift
let numbers = [10, 5, 5, 5, 3, 2, 1, 0]
let middle = [/][[/][/][/]]
print(middle)
```

# --answers--

- numbers
- 2
- :
- 3
- -1
- 6
- 3
- 5
- ...
- ..<

# --solutions--

```swift
let numbers = [10, 5, 5, 5, 3, 2, 1, 0]
let middle = numbers[3..<5]
print(middle)
```

# --output--

[5, 3]
