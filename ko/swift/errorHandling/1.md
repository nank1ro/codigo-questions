---
language: swift
exerciseType: 2
---

# --description--

Swift에서 오류는 크래시가 아니라 **값**입니다. `Error` 프로토콜을 준수하면 어떤 타입이든 오류가 될 수 있는데, 무엇이 잘못될 수 있는지를 케이스 이름으로 정확히 표현할 수 있어서 보통 열거형을 씁니다:
```swift
enum LoginError: Error {
    case wrongPassword
}
```
실패할 수 있는 함수에는 `throws`를 표시하고, 실패는 `throw`로 알립니다:
```swift
func login(_ password: String) throws {
    if password != "swift" {
        throw LoginError.wrongPassword
    }
    print("welcome")
}
```
그런 함수를 호출하려면 `try`가 필요하고, 그 호출은 `do` 블록 안에 있어야 하며, 그 뒤에는 실패했을 때 무엇을 할지 적는 `catch` 블록이 따라옵니다:
```swift
do {
    try login("hunter2")
} catch {
    print("login failed")
}
// login failed
```
`throw`가 실행되면 `do` 블록의 나머지는 건너뛰고 `catch`가 이어받습니다. 크래시는 일어나지 않습니다. 프로그램은 `catch` 뒤로 계속 진행됩니다.

# --instructions--

`TempError`가 오류 타입이 되고, 너무 추울 때 `check`가 오류를 던지며, 그 실패가 처리되어 `too cold`를 출력하도록 코드를 완성하세요

# --seed--

```swift
enum TempError: [/] {
    case tooCold
}

func check(_ degrees: Int) [/] {
    if degrees < 10 {
        [/] TempError.tooCold
    }
    print("fine")
}

do {
    [/] check(4)
} [/] {
    print("too cold")
}
```

# --answers--

- Error
- throws
- throw
- try
- catch
- Exception
- finally
- raise

# --solutions--

```swift
enum TempError: Error {
    case tooCold
}

func check(_ degrees: Int) throws {
    if degrees < 10 {
        throw TempError.tooCold
    }
    print("fine")
}

do {
    try check(4)
} catch {
    print("too cold")
}
```

# --output--

too cold
