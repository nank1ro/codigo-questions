---
language: swift
exerciseType: 2
---

# --description--

때로는 무언가가 *왜* 실패했는지는 중요하지 않고, 실패했다는 사실만 알면 됩니다. `try?`는 스로우하는 호출을 **옵셔널**로 바꿉니다. 성공하면 값이고, 오류를 던지면 `nil`입니다.
```swift
enum ParseError: Error {
    case notANumber
}

func toInt(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ParseError.notANumber
    }
    return value
}

if let number = try? toInt("42") {
    print(number) // 42
}
```
`do`도 `catch`도 없습니다. 실패가 이미 익숙한 옵셔널 언래핑 안으로 접혀 들어갑니다. 대가는 오류 값이 버려진다는 점이므로, 정말로 보고할 것이 없을 때만 `try?`를 쓰세요.

# --instructions--

성공한 호출은 언래핑되고 실패한 호출은 `nil`로 인식되어 `42` 다음에 `not a number`를 출력하도록 코드를 완성하세요

# --seed--

```swift
enum ParseError: Error {
    case notANumber
}

func toInt(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ParseError.notANumber
    }
    return value
}

let good = [/] toInt("42")
if let number = [/] {
    print(number)
}
if (try? toInt("abc")) == [/] {
    print("not a number")
}
```

# --answers--

- try?
- good
- nil
- try
- bad
- 0

# --solutions--

```swift
enum ParseError: Error {
    case notANumber
}

func toInt(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ParseError.notANumber
    }
    return value
}

let good = try? toInt("42")
if let number = good {
    print(number)
}
if (try? toInt("abc")) == nil {
    print("not a number")
}
```

# --output--

42
not a number
