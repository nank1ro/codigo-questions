---
language: swift
exerciseType: 3
---

# --description--

순서가 중요합니다. Swift는 던져진 값을 작성된 순서대로 각 `catch` 패턴과 비교하고 처음 일치하는 곳에서 멈추므로, 패턴 없는 `catch`를 맨 위에 두면 그 아래 모든 것을 삼켜 버립니다. 구체적인 케이스를 위에, 모두 잡는 `catch`를 아래에 두세요.

어떤 패턴에도 맞지 않는 오류는 무시되지 않습니다. 마지막의 패턴 없는 `catch`로 갑니다.

# --instructions--

이 코드는 무엇을 출력할까요?
```swift
enum ValidationError: Error {
    case empty
    case tooShort
}

func check() throws {
    throw ValidationError.tooShort
}

do {
    try check()
} catch ValidationError.empty {
    print("empty")
} catch {
    print("other")
}
```

# --answers--

- `other`
- `empty`
- `empty` 다음에 `other`
- 아무것도 출력하지 않습니다. 프로그램이 처리되지 않은 오류로 멈춥니다

# --solutions--

- `other`

