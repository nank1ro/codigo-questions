---
language: swift
exerciseType: 1
---

# --description--

몇 가지 축약 표현만으로 대부분의 패턴을 다룰 수 있습니다. 각각은 정확히 **한** 문자와 일치합니다:
- `\d`는 숫자 한 개입니다
- `\w`는 글자, 숫자 또는 밑줄 한 개입니다
- `\s`는 공백, 탭 또는 줄바꿈 한 개입니다
- `.`는 임의의 문자 한 개입니다

한 개보다 많은 문자와 일치시키려면 패턴 바로 뒤에 **수량자(quantifier)**를 붙입니다:
- `+`는 하나 이상을 뜻합니다
- `*`는 0개 이상을 뜻합니다
- `?`는 0개 또는 1개를 뜻합니다

따라서 `\w+`는 단어, `\s*`는 있어도 없어도 되는 공백, `\d?`는 있어도 없어도 되는 숫자입니다:
```swift
print("hello world".contains(#/\w+\s\w+/#)) // true
print("hello".contains(#/\w+\s\w+/#))       // false
```
특별한 의미가 없는 문자는 자기 자신과 일치할 뿐이므로, `#/cat/#`는 세 글자 `cat`과 일치합니다.

# --instructions--

정규 표현식을 사용하여, 텍스트(`String`)를 받는(인수 레이블은 생략) 함수 `hasDigit`를 작성하세요. 이 함수는 텍스트에 숫자가 하나 이상 있으면 `true`를 반환합니다

함수 호출 예시:
```swift
print(hasDigit("abc1"))
// true를 출력
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func hasDigit() {

}
```

# --asserts--

함수는 `#/`로 쓴 정규 표현식 리터럴을 사용해야 합니다

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`hasDigit("abc1")`는 `true`를 반환해야 합니다

```swift
tryCatch(hasDigit("abc1") == true)
```

`hasDigit("7")`는 `true`를 반환해야 합니다

```swift
tryCatch(hasDigit("7") == true)
```

`hasDigit("abc")`는 `false`를 반환해야 합니다

```swift
tryCatch(hasDigit("abc") == false)
```

`hasDigit("")`는 `false`를 반환해야 합니다

```swift
tryCatch(hasDigit("") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func hasDigit(_ text: String) -> Bool {
    return text.contains(#/\d/#)
}
```
