---
language: swift
exerciseType: 1
---

# --description--

`m.0`은 `String`이 아니라 `Substring`입니다: 원본 텍스트를 들여다보는 뷰이지 복사본이 아닙니다. 문자열과 똑같이 출력되지만, `String`이 필요한 곳에서는 변환해야 합니다:
```swift
let text = "order 42"
if let m = text.firstMatch(of: #/\d+/#) {
    let found: String = String(m.0)
    print(found) // 42
}
```
숫자 이니셜라이저는 `Substring`을 바로 받아들이므로, `Int(m.0)`는 우회 없이 동작합니다.

# --instructions--

텍스트(`String`)를 받는(인수 레이블은 생략) 함수 `firstNumber`를 작성하세요. 이 함수는 첫 번째 숫자 묶음을 `String`으로 반환하고, 텍스트에 숫자가 없으면 `"none"`을 반환합니다

함수 호출 예시:
```swift
print(firstNumber("order 42 today 7"))
// 42를 출력
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func firstNumber() {

}
```

# --asserts--

함수는 `firstMatch(of:`를 사용해야 합니다

```json
{
  "regex": "firstMatch\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstNumber("order 42 today 7")`는 `"42"`를 반환해야 합니다

```swift
tryCatch(firstNumber("order 42 today 7") == "42")
```

`firstNumber("a1")`는 `"1"`을 반환해야 합니다

```swift
tryCatch(firstNumber("a1") == "1")
```

`firstNumber("100")`는 `"100"`을 반환해야 합니다

```swift
tryCatch(firstNumber("100") == "100")
```

`firstNumber("abc")`는 `"none"`을 반환해야 합니다

```swift
tryCatch(firstNumber("abc") == "none")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstNumber(_ text: String) -> String {
    if let m = text.firstMatch(of: #/\d+/#) {
        return String(m.0)
    }
    return "none"
}
```
