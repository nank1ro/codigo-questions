---
language: swift
exerciseType: 1
---

# --description--

축약 표현이 맞지 않을 때는 받아들일 문자를 대괄호 사이에 나열합니다. `[abc]`는 `a` 한 개, `b` 한 개 또는 `c` 한 개와 일치하고, 대시는 범위를 씁니다:
```swift
print("f".contains(#/[a-f]/#))  // true
print("Z".contains(#/[A-Z]/#))  // true
print("5".contains(#/[0-9a-f]/#)) // true
```
중괄호 안의 숫자는 앞의 패턴이 정확히 몇 번 반복되는지 말해줍니다: `{3}`은 세 번, `{2,4}`는 두 번에서 네 번 사이를 뜻합니다:
```swift
print("aaa".contains(#/^a{3}$/#))  // true
print("aa".contains(#/^a{3}$/#))   // false
```
패턴을 `^`와 `$`로 감싸고 개수를 붙이는 것은 텍스트 전체가 주어진 모양을 갖는지 검사하는 일반적인 방법입니다.

# --instructions--

정규 표현식을 사용하여, 텍스트(`String`)를 받는(인수 레이블은 생략) 함수 `isCode`를 작성하세요. 이 함수는 텍스트 전체가 정확히 대문자 세 개일 때만 `true`를 반환합니다

함수 호출 예시:
```swift
print(isCode("ABC"))
// true를 출력
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func isCode() {

}
```

# --asserts--

함수는 `#/`로 쓴 정규 표현식 리터럴을 사용해야 합니다

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`isCode("ABC")`는 `true`를 반환해야 합니다

```swift
tryCatch(isCode("ABC") == true)
```

`isCode("XYZ")`는 `true`를 반환해야 합니다

```swift
tryCatch(isCode("XYZ") == true)
```

`isCode("AB")`는 `false`를 반환해야 합니다

```swift
tryCatch(isCode("AB") == false)
```

`isCode("ABCD")`는 `false`를 반환해야 합니다

```swift
tryCatch(isCode("ABCD") == false)
```

`isCode("abc")`는 `false`를 반환해야 합니다

```swift
tryCatch(isCode("abc") == false)
```

`isCode("A1C")`는 `false`를 반환해야 합니다

```swift
tryCatch(isCode("A1C") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func isCode(_ text: String) -> Bool {
    return text.contains(#/^[A-Z]{3}$/#)
}
```
