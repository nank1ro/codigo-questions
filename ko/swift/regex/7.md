---
language: swift
exerciseType: 4
---

# --description--

`matches(of:)`는 첫 번째 대신 **모든** 매치를 배열로 반환합니다. 이 배열은 절대 `nil`이 아닙니다: 아무것도 일치하지 않으면 그저 비어 있을 뿐이므로, 바로 반복하거나 변환할 수 있습니다:
```swift
let text = "a1 b22"
print(text.matches(of: #/\d+/#).count) // 2
```
각 요소는 매치이므로, `map` 안에서 `$0.0`은 매치된 텍스트입니다:
```swift
let found = text.matches(of: #/\d+/#).map { String($0.0) }
print(found) // ["1", "22"]
```

# --instructions--

`text`의 모든 숫자 묶음을 `found`에 모아 출력하는 프로그램을 만들도록 항목을 정렬하세요

# --answers--

```swift
let text = "a1 b22 c333"
```

```swift
let pattern = #/\d+/#
```

```swift
let found = text.matches(of: pattern).map { String($0.0) }
```

```swift
print(found)
```

# --solutions--

```swift
let text = "a1 b22 c333"
let pattern = #/\d+/#
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

```swift
let pattern = #/\d+/#
let text = "a1 b22 c333"
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

# --output--

["1", "22", "333"]
