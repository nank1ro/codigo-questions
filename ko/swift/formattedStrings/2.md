---
language: swift
exerciseType: 1
---

# --description--

두 번째 도구는 **Foundation** 프레임워크에 속한 `String(format:)`이므로, 파일은 `import Foundation`으로 시작해야 합니다.

이 메서드는 삽입할 값들 앞에 **포맷 문자열**을 받습니다. 포맷 문자열 안에서 `%`로 시작하는 **지정자**는 각 값이 어디에 들어가고 어떻게 표현되는지를 나타냅니다. 정수의 지정자는 `%d`입니다:
```swift
import Foundation

let count = 7
let text = String(format: "Item %d", count)
print(text) // Item 7
```
`String(format:)`은 일반 `String`을 반환하므로, 출력하거나 저장하거나 함수에서 반환할 수 있습니다.

# --instructions--

`String(format:)`을 `%d`와 함께 사용하여, 정수를 받아(인자 레이블 생략) `Item ` 뒤에 숫자가 붙은 텍스트를 반환하는 함수 `label`을 작성하세요

함수 호출 예시:
```swift
print(label(7))
// prints Item 7
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func label() {

}
```

# --asserts--

함수는 `String(format:`을 사용해야 합니다

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`label(7)`은 `"Item 7"`을 반환해야 합니다

```swift
tryCatch(label(7) == "Item 7")
```

`label(42)`는 `"Item 42"`를 반환해야 합니다

```swift
tryCatch(label(42) == "Item 42")
```

`label(0)`은 `"Item 0"`을 반환해야 합니다

```swift
tryCatch(label(0) == "Item 0")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func label(_ n: Int) -> String {
    return String(format: "Item %d", n)
}
```
