---
language: swift
exerciseType: 4
---

# --description--

두 도구를 합쳐 테이블을 출력해 봅시다: `padding`은 각 행의 텍스트를 왼쪽 정렬하고, `String(format:)`은 숫자를 고정된 너비와 정밀도로 오른쪽 정렬합니다:
```swift
import Foundation

let items = [("Tea", 2.5), ("Cake", 12.0)]
for (name, price) in items {
    let label = name.padding(toLength: 6, withPad: ".", startingAt: 0)
    print(label + String(format: "%6.2f", price))
}
// Tea...  2.50
// Cake.. 12.00
```
모든 행의 너비가 같기 때문에 소수점이 같은 열에 맞춰집니다.

# --instructions--

아래 테이블을 출력하도록 항목을 정렬하세요. `import Foundation`으로 시작하고 출력하기 전에 `label`을 계산합니다
```
Tea...  2.50
Cake.. 12.00
```

# --answers--

```swift
import Foundation
```

```swift
let items = [("Tea", 2.5), ("Cake", 12.0)]
```

```swift
for (name, price) in items {
```

```swift
    let label = name.padding(toLength: 6, withPad: ".", startingAt: 0)
```

```swift
    print(label + String(format: "%6.2f", price))
```

```swift
}
```

# --solutions--

```swift
import Foundation
let items = [("Tea", 2.5), ("Cake", 12.0)]
for (name, price) in items {
    let label = name.padding(toLength: 6, withPad: ".", startingAt: 0)
    print(label + String(format: "%6.2f", price))
}
```

# --output--

Tea...  2.50
Cake.. 12.00
