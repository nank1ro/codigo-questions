---
language: swift
exerciseType: 2
---

# --description--

클래스를 정의하는 것만으로는 객체가 생성되지 않습니다.
객체를 생성하려면 클래스의 __인스턴스__를 만들어야 합니다

# --instructions--

`dog`이라는 이름으로 `Animal` 클래스의 인스턴스를 만드세요

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4
}
let [/] = [/][/]
[/].[/] = "male"

print(dog.gender!)
print(dog.legs)
```

# --answers--

- dog
- dog
- gender
- Animal
- ()
- animal
- []

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4
}
let dog = Animal()
dog.gender = "male"

print(dog.gender!)
print(dog.legs)
```

# --output--

male
4
