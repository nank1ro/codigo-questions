---
language: swift
exerciseType: 4
---

# --instructions--

`Dog`을 `Animal`의 서브클래스로 만들 수 있습니까?
`legs`는 `Animal` 클래스 안에, `breed`는 `Dog` 클래스 안에 있어야 합니다

# --answers--

```swift
class Animal {
    var legs = 4
}
```

```swift
class Dog: Animal {
    var breed: String?
}
```

```swift
let max = Dog()
```

```swift
max.breed = "Chihuahua"
```

```swift
print(max.breed!)
```

# --solutions--

```swift
class Animal {
    var legs = 4
}
class Dog: Animal {
    var breed: String?
}
let max = Dog()
max.breed = "Chihuahua"
print(max.breed!)
```

# --output--

Chihuahua
