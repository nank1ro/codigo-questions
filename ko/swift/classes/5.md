---
language: swift
exerciseType: 2
---

# --description--

클래스가 자체 함수를 가지고 있을 때, 그 함수들을 __메서드__라고 합니다.

# --instructions--

`Animal` 클래스에 `description` 메서드를 추가하세요.
두 개의 별도 `print` 문을 사용하여, 호출된 동물의 `gender`와 `legs`를 출력해야 합니다.
그런 다음 `description` 메서드를 호출하세요.

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4

    [/][/]() {
        if let gender = self.gender { print(gender) }
        print([/].legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.[/]
```

# --answers--

-  description
- func
- description()
- self
- description[]
- function
- method

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4

    func description() {
        if let gender = self.gender { print(gender) }
        print(self.legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.description()
```

# --output--

female
4
