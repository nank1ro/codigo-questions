---
language: swift
exerciseType: 2
---

# --description--

의사 결정은 특정 조건이 충족될 때만 코드를 실행하고자 할 때 필요합니다.
날씨가 좋을 때만 밖에서 놀고 싶다고 가정해 봅시다.
프로그래밍에서는 불리언 변수 `niceWeather`를 저장하고, 이 변수가 `true`인 `if` 밖에서 노는 행동을 수행할 수 있습니다:
```swift
var niceWeather = true
if niceWeather {
    // play outside
}
```

# --instructions--

`time`이 `12`보다 작은지 확인하는 `if` 문을 만들고, 코드 블록에서 문자열 `"It's morning"`을 출력하세요

# --seed--

```swift
var time = 8
[/] time < 12 {
    print("It's morning")
}
```

# --answers--

- if
- ==
- =
- else
- ?

# --solutions--

```swift
var time = 8
if time < 12 {
    print("It's morning")
}
```

# --output--

It's morning
