---
language: swift
exerciseType: 1
---

# --description--

`flatMap`은 **배열**을 반환하는 클로저를 위한 것입니다: 배열의 배열을 만드는 대신, 반환된 모든 배열을 하나의 평평한 결과로 합칩니다:
```swift
let teams = [["Ann", "Bob"], ["Cid"]]
print(teams.map { $0 })     // [["Ann", "Bob"], ["Cid"]]
print(teams.flatMap { $0 }) // ["Ann", "Bob", "Cid"]
```
클로저는 평탄화되기 전에 각 내부 배열을 변환할 수도 있습니다. 예를 들어 `teams.flatMap { $0.reversed() }`는 `["Bob", "Ann", "Cid"]`를 반환합니다.

# --instructions--

`flatMap`을 사용하여 `Int` 배열의 배열을 받아(인자 레이블 생략) 모든 숫자를 순서대로 담은 하나의 배열을 반환하는 함수 `flatten`을 작성하세요

함수 호출 예시:
```swift
print(flatten([[1, 2], [3]]))
// prints [1, 2, 3]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`flatMap`을 사용해야 합니다

```json
{
  "regex": "\\.flatMap",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`flatten([[1, 2], [3]])`은 `[1, 2, 3]`을 반환해야 합니다

```swift
tryCatch(flatten([[1, 2], [3]]) == [1, 2, 3])
```

`flatten([[5], [], [6, 7]])`은 `[5, 6, 7]`을 반환해야 합니다

```swift
tryCatch(flatten([[5], [], [6, 7]]) == [5, 6, 7])
```

`flatten([])`은 `[]`을 반환해야 합니다

```swift
tryCatch(flatten([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func flatten(_ lists: [[Int]]) -> [Int] {
    return lists.flatMap { $0 }
}
```
