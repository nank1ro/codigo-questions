---
language: swift
exerciseType: 4
---

# --description--

`contains(where:)`와 `allSatisfy`는 플래그 변수를 사용하는 반복문의 흔한 패턴을 대체합니다. 두 함수 모두 답을 알 수 있게 되는 즉시 멈춥니다: `contains(where:)`는 첫 번째 일치에서, `allSatisfy`는 클로저를 만족하지 않는 첫 번째 요소에서 멈춥니다.
```swift
let ages = [15, 22, 40]
let anyMinor = ages.contains { $0 < 18 }  // true
let allAdults = ages.allSatisfy { $0 >= 18 } // false
```
`contains { ... }`는 `contains(where:)`의 후행 클로저 형태이며, 특정 값을 찾는 `contains(_:)`와 혼동해서는 안 됩니다.

# --instructions--

모든 점수가 `60` 이상인지, 그리고 `100`인 점수가 있는지 검사한 뒤 두 결과를 한 줄에 출력하도록 항목들을 정렬하세요
`scores`를 먼저 선언하고 출력은 마지막에 하세요

# --answers--

```swift
let scores = [72, 100, 64]
```

```swift
let allPassed = scores.allSatisfy { $0 >= 60 }
```

```swift
let anyPerfect = scores.contains { $0 == 100 }
```

```swift
print("\(allPassed) \(anyPerfect)")
```

# --solutions--

```swift
let scores = [72, 100, 64]
let allPassed = scores.allSatisfy { $0 >= 60 }
let anyPerfect = scores.contains { $0 == 100 }
print("\(allPassed) \(anyPerfect)")
```

```swift
let scores = [72, 100, 64]
let anyPerfect = scores.contains { $0 == 100 }
let allPassed = scores.allSatisfy { $0 >= 60 }
print("\(allPassed) \(anyPerfect)")
```

# --output--

true true
