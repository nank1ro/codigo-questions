---
language: swift
exerciseType: 2
---

# --description--

클로저가 하나의 프로퍼티만 읽을 때는 대신 **키 경로**를 전달할 수 있습니다: `\.name`은 "요소의 `name` 프로퍼티"를 뜻하며, `map(\.name)`은 `map { $0.name }`과 같습니다.
프로퍼티로 정렬할 때는 일반적인 두 인자 클로저를 사용하여 두 요소에서 그 프로퍼티를 비교합니다:
```swift
struct City {
    var name: String
    var population: Int
}
let cities = [City(name: "Oslo", population: 700), City(name: "Rome", population: 2800)]
let byPopulation = cities.sorted { $0.population > $1.population }
print(byPopulation.map(\.name)) // ["Rome", "Oslo"]
```

# --instructions--

`score`가 높은 플레이어부터 낮은 플레이어 순으로 정렬하고 키 경로로 이름을 출력하도록 코드를 완성하여, 프로그램이 `["Mia", "Leo", "Zoe"]`를 출력하도록 하세요

# --seed--

```swift
struct Player {
    var name: String
    var score: Int
}
let players = [Player(name: "Leo", score: 40), Player(name: "Mia", score: 75), Player(name: "Zoe", score: 12)]
let ranking = players.sorted { $0.score [/] $1.score }
print(ranking.map([/]))
```

# --answers--

- >
- \.name
- <
- \.score
- $0.name

# --solutions--

```swift
struct Player {
    var name: String
    var score: Int
}
let players = [Player(name: "Leo", score: 40), Player(name: "Mia", score: 75), Player(name: "Zoe", score: 12)]
let ranking = players.sorted { $0.score > $1.score }
print(ranking.map(\.name))
```

# --output--

["Mia", "Leo", "Zoe"]
