---
language: swift
exerciseType: 1
---

# --description--

`Dictionary(grouping:by:)`는 컬렉션을 배열들의 딕셔너리로 나눕니다. 클로저는 각 요소의 **키**를 계산하고, 같은 키를 가진 모든 요소는 같은 배열에 들어갑니다:
```swift
let words = ["apple", "bee", "avocado"]
let byInitial = Dictionary(grouping: words, by: { $0.first! })
print(byInitial["a"]!) // ["apple", "avocado"]
```
`mapValues`는 키는 그대로 두면서 딕셔너리의 모든 값을 변환하므로, 그룹화 다음의 자연스러운 다음 단계입니다:
```swift
let sizes = byInitial.mapValues { $0.count }
print(sizes["a"]!) // 2
```

# --instructions--

`Dictionary(grouping:by:)`와 `mapValues`를 사용하여 `String` 배열을 받아(인자 레이블 생략) 각 단어 길이를 그 길이를 가진 단어의 개수에 대응시키는 `[Int: Int]` 타입의 딕셔너리를 반환하는 함수 `countByLength`를 작성하세요

함수 호출 예시:
```swift
print(countByLength(["hi", "yo", "hey"])[2]!)
// prints 2
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`Dictionary(grouping:by:)`를 사용해야 합니다

```json
{
  "regex": "Dictionary\\s*\\(\\s*grouping",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`mapValues`를 사용해야 합니다

```json
{
  "regex": "\\.mapValues",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`countByLength(["hi", "yo", "hey"])`는 `[2: 2, 3: 1]`을 반환해야 합니다

```swift
tryCatch(countByLength(["hi", "yo", "hey"]) == [2: 2, 3: 1])
```

`countByLength(["a", "bb", "cc", "dd"])`는 `[1: 1, 2: 3]`을 반환해야 합니다

```swift
tryCatch(countByLength(["a", "bb", "cc", "dd"]) == [1: 1, 2: 3])
```

`countByLength([])`는 빈 딕셔너리를 반환해야 합니다

```swift
tryCatch(countByLength([]) == [:])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func countByLength(_ words: [String]) -> [Int: Int] {
    return Dictionary(grouping: words, by: { $0.count }).mapValues { $0.count }
}
```
