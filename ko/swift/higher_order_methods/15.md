---
language: swift
exerciseType: 1
---

# --description--

고차 함수를 직접 작성할 수도 있습니다. 클로저를 받아 그것으로 만든 **새 클로저를 반환**하는 함수는 흔한 패턴입니다: 반환된 클로저는 원래 클로저를 캡처하므로, 매개변수에는 `@escaping`을 표시해야 합니다.
예를 들어 `negate`는 술어를 그 반대로 바꾸어 `filter`에 전달할 수 있게 해 줍니다:
```swift
func negate(_ predicate: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { !predicate($0) }
}
let isEven: (Int) -> Bool = { $0 % 2 == 0 }
print([1, 2, 3, 4].filter(negate(isEven))) // [1, 3]
```
`filter(negate(isEven))`는 후행 클로저 문법 없이 클로저를 일반 인수로 전달한다는 점에 유의하세요.

# --instructions--

`(Int) -> Bool` 타입의 클로저 두 개를 받아(두 인자 레이블 모두 생략) 같은 인수에 대해 둘 다 `true`일 때만 `true`인 같은 타입의 클로저를 반환하는 함수 `both`를 작성하세요

함수 호출 예시:
```swift
let smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
print([4, 12, 7, 8].filter(smallEven))
// prints [4, 8]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`both({ $0 < 10 }, { $0 % 2 == 0 })`은 `4`에 대해 `true`여야 합니다

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(4) == true)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })`은 `12`에 대해 `false`여야 합니다

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(12) == false)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })`은 `7`에 대해 `false`여야 합니다

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(7) == false)
```

반환된 클로저는 `filter`와 함께 작동해야 합니다

```swift
let _smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
tryCatch([4, 12, 7, 8].filter(_smallEven) == [4, 8])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func both(_ first: @escaping (Int) -> Bool, _ second: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { first($0) && second($0) }
}
```
