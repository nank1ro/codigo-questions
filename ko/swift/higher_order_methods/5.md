---
language: swift
exerciseType: 1
---

# --description--

일부 고차 함수는 컬렉션을 변환하는 대신 컬렉션에 대한 질문에 답합니다. 이들은 모두 `Bool`을 반환하는 클로저를 받습니다:
- `first(where:)`는 클로저를 만족하는 첫 번째 요소를 반환하고, 없으면 `nil`을 반환합니다
- `contains(where:)`는 최소 한 개의 요소가 클로저를 만족하면 `true`를 반환합니다
- `allSatisfy`는 모든 요소가 클로저를 만족하면 `true`를 반환합니다

```swift
let nums = [4, 9, 16]
print(nums.first(where: { $0 > 5 }))  // Optional(9)
print(nums.contains(where: { $0 > 5 })) // true
print(nums.allSatisfy { $0 > 5 })       // false
```
`filter`와 달리 `first(where:)`는 첫 번째 일치에서 멈추고 새 배열을 만들지 않습니다.

# --instructions--

`first(where:)`를 사용하여 `String` 배열과 `minLength`라는 `Int`를 받아(두 인자 레이블 모두 생략) `minLength`자 이상인 첫 번째 문자열을 반환하고, 없으면 `nil`을 반환하는 함수 `firstLong`을 작성하세요

함수 호출 예시:
```swift
print(firstLong(["hi", "hello", "hey"], 4))
// prints Optional("hello")
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`first(where:)`를 사용해야 합니다

```json
{
  "regex": "\\.first\\s*\\(\\s*where",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstLong(["hi", "hello", "hey"], 4)`는 `"hello"`를 반환해야 합니다

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 4) == "hello")
```

`firstLong(["hi", "hello", "hey"], 2)`는 `"hi"`를 반환해야 합니다

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 2) == "hi")
```

`firstLong(["hi", "hey"], 4)`는 `nil`을 반환해야 합니다

```swift
tryCatch(firstLong(["hi", "hey"], 4) == nil)
```

`firstLong([], 1)`은 `nil`을 반환해야 합니다

```swift
tryCatch(firstLong([], 1) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstLong(_ words: [String], _ minLength: Int) -> String? {
    return words.first(where: { $0.count >= minLength })
}
```
