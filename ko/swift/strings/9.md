---
language: swift
exerciseType: 1
---

# --description--

인덱스를 다루는 것은 장황하므로, Swift는 가장 흔한 경우를 위한 지름길을 제공합니다:
- `first`와 `last`는 첫 번째와 마지막 문자를 옵셔널 `Character?`로 반환합니다(빈 문자열이면 `nil`)
- `prefix(n)`은 처음 `n`개의 문자를, `suffix(n)`은 마지막 `n`개의 문자를 반환합니다
```swift
let word = "Swift"
print(word.first!)     // S
print(word.prefix(2))  // Sw
print(word.suffix(3))  // ift
```
`prefix`와 `suffix`는 원본 텍스트에 대한 뷰인 `Substring`을 반환합니다. 실제 `String`으로 저장하려면 `String(...)`으로 감싸세요. `n`이 `count`보다 크면 그냥 전체 문자열을 얻습니다.

# --instructions--

`String`을 받아(인수 레이블은 생략) 처음 두 문자를 `String`으로 반환하는 함수 `firstTwo`와, 마지막 두 문자를 `String`으로 반환하는 함수 `lastTwo`를 작성하세요

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`firstTwo("Swift")`는 `"Sw"`를 반환해야 합니다

```swift
tryCatch(firstTwo("Swift") == "Sw")
```

`firstTwo("a")`는 `"a"`를 반환해야 합니다

```swift
tryCatch(firstTwo("a") == "a")
```

`lastTwo("Swift")`는 `"ft"`를 반환해야 합니다

```swift
tryCatch(lastTwo("Swift") == "ft")
```

`lastTwo("")`는 `""`를 반환해야 합니다

```swift
tryCatch(lastTwo("") == "")
```

`firstTwo`는 `String`을 반환해야 합니다

```swift
tryCatch(type(of: firstTwo("Swift")) == String.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstTwo(_ text: String) -> String {
    return String(text.prefix(2))
}

func lastTwo(_ text: String) -> String {
    return String(text.suffix(2))
}
```
