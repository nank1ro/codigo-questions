---
language: swift
exerciseType: 1
---

# --description--

Foundation 프레임워크는 다양한 추가 문자열 메서드를 제공합니다. 가장 유용한 것 중 하나가 `replacingOccurrences(of:with:)`로, 첫 번째 텍스트의 모든 출현을 두 번째 텍스트로 바꾼 새 문자열을 반환합니다:
```swift
import Foundation

let path = "a/b/c"
print(path.replacingOccurrences(of: "/", with: "-")) // a-b-c
```
파일 맨 위에서 `import Foundation`을 잊지 마세요. 그렇지 않으면 이 메서드를 사용할 수 없습니다. 메서드 호출은 체이닝할 수 있으므로, `text.lowercased().replacingOccurrences(of: " ", with: "_")`도 유효합니다.

# --instructions--

`String`을 받아(인수 레이블은 생략) 소문자로 바꾸고 모든 공백을 밑줄 `_`로 바꾸어 반환하는 함수 `snakeCase`를 작성하세요

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`snakeCase("Hello World")`는 `"hello_world"`를 반환해야 합니다

```swift
tryCatch(snakeCase("Hello World") == "hello_world")
```

`snakeCase("My First Post")`는 `"my_first_post"`를 반환해야 합니다

```swift
tryCatch(snakeCase("My First Post") == "my_first_post")
```

`snakeCase("swift")`는 `"swift"`를 반환해야 합니다

```swift
tryCatch(snakeCase("swift") == "swift")
```

`snakeCase("")`는 `""`를 반환해야 합니다

```swift
tryCatch(snakeCase("") == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func snakeCase(_ text: String) -> String {
    return text.lowercased().replacingOccurrences(of: " ", with: "_")
}
```
