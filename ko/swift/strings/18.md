---
language: swift
exerciseType: 4
---

# --description--

`Character`는 `String`이 아니므로 `+`로 문자열에 직접 결합할 수 없습니다. 먼저 `String(...)`으로 변환하세요:
```swift
let letter: Character = "a"
let text = String(letter) + "bc" // abc
```
이를 `for`-`in` 루프와 결합하면, 예를 들어 새 문자를 지금까지 모은 것들 앞에 두는 식으로, 한 번에 한 문자씩 문자열을 다시 만들 수 있습니다.

# --instructions--

`swift`를 뒤집어서 출력하도록, 결과를 한 번에 한 문자씩 만들면서 항목을 정렬하세요.
`word`를 `reversed`보다 먼저 선언하세요

# --answers--

```swift
let word = "swift"
```

```swift
var reversed = ""
```

```swift
for letter in word {
```

```swift
    reversed = String(letter) + reversed
```

```swift
}
```

```swift
print(reversed)
```

# --solutions--

```swift
let word = "swift"
var reversed = ""
for letter in word {
    reversed = String(letter) + reversed
}
print(reversed)
```

# --output--

tfiws
