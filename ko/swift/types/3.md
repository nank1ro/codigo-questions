---
language: swift
exerciseType: 1
---

# --description--

`type(of:)` 함수는 값의 타입을 반환하며, Swift가 무엇을 추론했는지 확인할 때 유용합니다:
```swift
print(type(of: 42))    // Int
print(type(of: 2.5))   // Double
print(type(of: "hi"))  // String
```
추론된 타입과 다른 타입을 원하면 어노테이션을 추가합니다. 정수 리터럴은 `Double`이나 `Float` 상수에 저장할 수 있고, 한 문자 리터럴은 `Character` 상수에 저장할 수 있습니다:
```swift
let ratio: Double = 3       // 3.0, not an Int
let half: Float = 0.5
let initial: Character = "S"
print(type(of: ratio))      // Double
```

# --instructions--

세 개의 상수를 선언하세요:
- `ratio`: 정수 리터럴 `3`을 담는 `Double`
- `half`: `0.5`를 담는 `Float`
- `initial`: `"S"`를 담는 `Character`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`ratio`는 `3.0`과 같은 `Double` 값이어야 합니다

```swift
tryCatch(type(of: ratio) == Double.self && ratio == 3.0)
```

`half`는 `0.5`와 같은 `Float` 값이어야 합니다

```swift
tryCatch(type(of: half) == Float.self && half == 0.5)
```

`initial`은 `"S"`와 같은 `Character` 값이어야 합니다

```swift
tryCatch(type(of: initial) == Character.self && initial == "S")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let ratio: Double = 3
let half: Float = 0.5
let initial: Character = "S"
```
