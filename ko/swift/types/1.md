---
language: swift
exerciseType: 2
---

# --description--

Swift의 모든 값에는 **타입**이 있으며, 타입은 컴파일러에게 그 값이 어떤 종류의 데이터인지, 무엇을 할 수 있는지 알려줍니다.
기본 타입은 다음과 같습니다:
- `Int`: `42`나 `-7` 같은 정수
- `Double`: `3.14`처럼 소수 부분이 있는 수
- `String`: `"Hello"` 같은 텍스트 조각
- `Character`: `"a"` 같은 하나의 문자
- `Bool`: `true` 또는 `false`

상수나 변수의 타입은 **타입 어노테이션**, 즉 이름 뒤에 콜론과 타입 이름을 붙여서 지정할 수 있습니다:
```swift
let age: Int = 36
let name: String = "Ada"
```
한 타입의 값은 다른 타입의 상수에 저장할 수 없습니다: `let age: Int = "36"`은 컴파일 오류입니다.

# --instructions--

타입 어노테이션을 완성하여 프로그램이 컴파일되고 `Ada, 36, 1.68, true`를 출력하도록 하세요

# --seed--

```swift
let name: [/] = "Ada"
let age: [/] = 36
let height: [/] = 1.68
let isAdmin: [/] = true
print("\(name), \(age), \(height), \(isAdmin)")
```

# --answers--

- String
- Int
- Double
- Bool
- Character

# --solutions--

```swift
let name: String = "Ada"
let age: Int = 36
let height: Double = 1.68
let isAdmin: Bool = true
print("\(name), \(age), \(height), \(isAdmin)")
```

# --output--

Ada, 36, 1.68, true
