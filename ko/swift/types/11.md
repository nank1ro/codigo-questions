---
language: swift
exerciseType: 3
---

# --description--

`Int`는 64비트를 사용하므로 고정된 범위의 수만 표현할 수 있습니다. 가장 큰 값과 가장 작은 값은 `Int.max`와 `Int.min`로 제공됩니다:
```swift
print(Int.max) // 9223372036854775807
print(Int.min) // -9223372036854775808
```
이 한계를 넘어서는 것을 **오버플로**라고 합니다. 다른 많은 언어와 달리, Swift는 조용히 범위의 반대쪽 끝으로 되돌아가지 않습니다: 오버플로가 발생한 연산은 프로그램을 멈추는 **런타임 오류**입니다.

# --instructions--

이 코드를 실행하면 어떻게 될까요?
```swift
func next(_ n: Int) -> Int {
    return n + 1
}
print(next(Int.max))
```

# --answers--

- 프로그램이 오버플로 오류로 크래시됩니다
- -9223372036854775808을 출력합니다
- 0을 출력합니다
- 9223372036854775807을 출력합니다

# --solutions--

- 프로그램이 오버플로 오류로 크래시됩니다
