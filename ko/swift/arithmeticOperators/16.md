---
language: swift
exerciseType: 1
---

# --instructions--

새로운 변수 `revenue`를 생성하고 `earnings`과 `costs`의 차이를 대입한 후, 그 값을 출력하세요

# --seed--

```swift
var earnings = 2000
var costs = 750
```

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`revenue`는 `earnings`과 `costs`의 차이와 같아야 합니다.

```swift
    func test1() {
        XCTAssertEqual(revenue, earnings - costs, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
var earnings = 2000
var costs = 750
var revenue = earnings - costs;
```

# --output--

1250
