---
language: swift
exerciseType: 1
---

# --description--
`Array`는 제네릭 타입입니다: `[Int]`와 `[String]`은 둘 다 배열이지만 **`Element`** 타입이 다릅니다. `Array`의 확장은 이 모두에 적용되는데, 새 멤버가 일부 요소에만 의미가 있을 때는 문제가 됩니다: 문자열인 숫자를 더할 수는 없습니다.
`where` 절은 확장을 `Element`가 주어진 타입인 배열로 제한합니다:
```swift
extension Array where Element == Int {
    var largest: Int {
        var result = Int.min
        for number in self {
            if number > result {
                result = number
            }
        }
        return result
    }
}
print([3, 9, 2].largest) // 9
```
`[3, 9, 2].largest`는 동작하지만, `["a", "b"].largest`는 컴파일 오류입니다: 그 프로퍼티는 `[String]`에 존재하지 않습니다.

# --instructions--
`Element == Int`로 제한된 `Array`의 확장을 작성하고, 요소들의 합을 반환하는 메서드 `sum()`을 만드세요. `reduce`는 사용하지 마세요

메서드 호출 예시:
```swift
print([1, 2, 3].sum())
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--
`where Element == Int` 절을 가진 `Array`의 확장을 사용해야 합니다

```json
{
  "regex": "extension\\s+Array\\s+where\\s+Element\\s*==\\s*Int",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`reduce`를 사용하면 안 됩니다

```json
{
  "regex": "reduce",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`[1, 2, 3].sum()`은 `6`을 반환해야 합니다

```swift
tryCatch([1, 2, 3].sum() == 6)
```

`[10, -4].sum()`은 `6`을 반환해야 합니다

```swift
tryCatch([10, -4].sum() == 6)
```

`[7].sum()`은 `7`을 반환해야 합니다

```swift
tryCatch([7].sum() == 7)
```

빈 배열의 합은 `0`이어야 합니다

```swift
let _empty: [Int] = []
tryCatch(_empty.sum() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension Array where Element == Int {
    func sum() -> Int {
        var total = 0
        for number in self {
            total += number
        }
        return total
    }
}
```
