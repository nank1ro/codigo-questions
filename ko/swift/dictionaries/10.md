---
language: swift
exerciseType: 2
---

# --description--

딕셔너리에 값을 __추가__하거나 __변경__하기 위해 `updateValue(_:forKey:)` 메서드를 사용할 수도 있습니다

# --instructions--

딕셔너리에서 `age` 키의 값을 `20`으로 업데이트하세요

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let [/] = [/][/][/]([/], forKey: [/]) {
    print("The old age was \(oldAge)")
}
print(user)
```

# --answers--

- update
- add
- insert
- user
- .
- updateValue
- 20
- "age"
- oldAge

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let oldAge = user.updateValue(20, forKey: "age") {
    print("The old age was \(oldAge)")
}
print(user)
```

# --output--

The old age was 30
["age": 20, "first_name": "John", "last_name": "Hood"]
