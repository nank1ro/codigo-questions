---
language: swift
exerciseType: 2
---

# --description--

프로그래밍에서 코드 블록을 반복해야 하는 경우가 자주 있습니다. 예를 들어:
```swift
print("2 seconds")
print("3 seconds")
print("4 seconds")
print("5 seconds")
```
이 코드는 다음과 같은 출력을 생성합니다:
```swift
2 seconds
3 seconds
4 seconds
5 seconds
```
긴 구문의 경우 코드를 작성하는 데 많은 시간이 걸리지만, 다행히 반복문을 사용할 수 있습니다.
위와 동일한 출력을 얻기 위해 `while` 반복문을 배워봅시다.
```swift
var count = 2
while count <= 5 {
    print("\(count) seconds")
    count += 1
}
```
먼저 초기값 `2`를 할당하는 변수 `count`를 생성했습니다.
그런 다음 조건 `count <= 5`가 `true`인 동안 코드 블록을 실행하는 `while` 구문을 사용했습니다.
코드 블록 내부에서 `count += 1` 줄을 추가하는 것을 **잊지 마세요**.
이 줄은 `count` 값을 증가시키며, 그렇지 않으면 반복문이 무한히 실행됩니다

# --instructions--

`while` 반복문으로 코드 블록을 반복하기 위해 빈칸을 채우세요

# --seed--

```swift
[/] true {
    print("repeat")
}
```

# --answers--

- while
- "while"
- for
- loop

# --solutions--

```swift
while true {
    print("repeat")
}
```

# --output--

repeat
repeat
repeat
repeat
...
