---
language: dart
exerciseType: 1
---

# --instructions--

Usa un bucle `while` con `continue` para sumar solo los números **impares** del `1` al `10` (inclusive). Devuelve la suma.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` debe ser igual a `25`

```dart
  test('test1', () {
    expect(computeValue(), 25, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sum = 0;
int i = 1;
while (i <= 10) {
  if (i % 2 == 0) {
    i++;
    continue;
  }
  sum += i;
  i++;
}
```
