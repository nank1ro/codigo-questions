---
language: dart
exerciseType: 1
---

# --instructions--

Usa un bucle `while` para calcular el factorial de `6` (es decir, `6!`). Devuelve el resultado.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return factorial;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`factorial` debe ser igual a `720`

```dart
  test('test1', () {
    expect(computeValue(), 720, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int factorial = 1;
int i = 1;
while (i <= 6) {
  factorial *= i;
  i++;
}
```
