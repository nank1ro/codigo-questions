---
language: dart
exerciseType: 1
---

# --instructions--

Usa un bucle `while` para contar hacia atrás desde `10` hasta `1` (inclusive). Devuelve el número de iteraciones realizadas.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return iterations;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`iterations` debe ser igual a `10`

```dart
  test('test1', () {
    expect(computeValue(), 10, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 10;
int iterations = 0;
while (n >= 1) {
  iterations++;
  n--;
}
```
