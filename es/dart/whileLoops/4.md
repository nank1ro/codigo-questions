---
language: dart
exerciseType: 2
---

# --description--

La condición de un bucle `while` puede incluir cualquier expresión booleana. En cuanto la condición se evalúa como `false`, el bucle termina.

```dart
int n = 100;
while (n > 1) {
  n ~/= 2;
}
print(n); // 0
```

Aquí, `n` se divide a la mitad en cada iteración usando la división entera (`~/`).

# --instructions--

Completa el bucle `while` que sigue dividiendo `n` a la mitad (usando `~/= 2`) mientras sea mayor que `1`, y luego imprime el resultado.

# --seed--

```dart
void main() {
  int n = 64;
  while (n [/] 1) {
    n [/] 2;
  }
  print(n);
}
```

# --answers--

- >
- <
- ~/=
- /=
- *=

# --solutions--

```dart
void main() {
  int n = 64;
  while (n > 1) {
    n ~/= 2;
  }
  print(n);
}
```

# --output--

1
