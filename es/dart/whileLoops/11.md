---
language: dart
exerciseType: 1
---

# --instructions--

Usa un bucle `do-while` para sumar `3` a una variable `total` que comienza en `0`, hasta que `total` alcance o supere `12`. Devuelve `total`.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` debe ser igual a `12`

```dart
  test('test1', () {
    expect(computeValue(), 12, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 0;
do {
  total += 3;
} while (total < 12);
```
