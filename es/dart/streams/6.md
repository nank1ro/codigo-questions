---
language: dart
exerciseType: 4
---

# --description--

Un generador `async*` puede reenviar **todos los eventos de otro stream** con `yield*` (yield-star). Es como un bucle `await for` que emite cada valor, en una sola línea:

```dart
Stream<int> ones() async* {
  yield 1;
  yield 1;
}

Stream<int> sequence() async* {
  yield 0;
  yield* ones();
  yield 2;
}
// sequence() emits 0, 1, 1, 2
```

El stream exterior continúa con sus propios `yield` una vez que el stream interior está terminado.

# --instructions--

Ordena los elementos para definir una función `async*` `pair` que emita `1` y `2`, una función `async*` `all` que emita `0`, luego cada valor de `pair()`, luego `3`, y un `main` que imprima cada valor de `all()`. Define primero `pair`, luego `all`, luego `main`. El programa debe imprimir:

```
0
1
2
3
```

# --answers--

```dart
Stream<int> pair() async* {
```

```dart
  yield 1;
```

```dart
  yield 2;
```

```dart
}
```

```dart
Stream<int> all() async* {
```

```dart
  yield 0;
```

```dart
  yield* pair();
```

```dart
  yield 3;
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  await for (final n in all()) {
```

```dart
    print(n);
```

```dart
  }
```

```dart
}
```

# --solutions--

```dart
Stream<int> pair() async* {
  yield 1;
  yield 2;
}

Stream<int> all() async* {
  yield 0;
  yield* pair();
  yield 3;
}

Future<void> main() async {
  await for (final n in all()) {
    print(n);
  }
}
```

# --output--

0
1
2
3
