---
language: dart
exerciseType: 1
---

# --description--

Los generadores producen eventos desde dentro de una función. Cuando los eventos vienen de **otro sitio** (un botón, una callback de red, otro objeto) necesitas un **`StreamController`**. Vive en la biblioteca `dart:async`, así que el archivo debe empezar con `import 'dart:async';`.

Un controlador es dueño de un stream y te deja empujar eventos dentro de él:

```dart
import 'dart:async';

Stream<int> dice() {
  final controller = StreamController<int>();
  controller.add(4);
  controller.add(2);
  controller.close();
  return controller.stream;
}
```

- `add(value)` envía un evento
- `close()` termina el stream; olvidarlo significa que los oyentes esperan para siempre
- `stream` es el `Stream` que consumen los oyentes

Los eventos añadidos antes de que alguien escuche se guardan en un búfer, así que el código de arriba es seguro: un oyente que llega más tarde recibe igualmente `4` y `2`.

# --instructions--

Escribe una función `announce` que reciba una `List<String>` `names` y devuelva un `Stream<String>` construido con un `StreamController`: añade cada nombre en mayúsculas, cierra el controlador y devuelve su stream. Recuerda el import de `dart:async`.

Ejemplo:
```dart
print(await announce(['ada', 'linus']).toList()); // [ADA, LINUS]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`announce(['ada', 'linus'])` debería emitir `'ADA'`, `'LINUS'`

```dart
  test('test1', () async {
    expect(await announce(['ada', 'linus']).toList(), ['ADA', 'LINUS'], reason: '--err-t1--');
  });
```

`announce(['Dart'])` debería emitir `'DART'`

```dart
  test('test2', () async {
    expect(await announce(['Dart']).toList(), ['DART'], reason: '--err-t2--');
  });
```

`announce([])` no debería emitir nada y debería acabar, y `announce` debería devolver un `Stream<String>`

```dart
  test('test3', () async {
    expect(await announce([]).toList(), <String>[], reason: '--err-t3--');
    expect(announce(['x']), isA<Stream<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
import 'dart:async';

Stream<String> announce(List<String> names) {
  final controller = StreamController<String>();
  for (final name in names) {
    controller.add(name.toUpperCase());
  }
  controller.close();
  return controller.stream;
}
```
