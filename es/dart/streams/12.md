---
language: dart
exerciseType: 4
---

# --description--

Un `StreamController` se suele crear y consumir en el mismo sitio: te suscribes a `controller.stream` con `listen`, luego añades eventos con `add` y cierras el controlador con `close`. Como `listen` no espera, los eventos se entregan después de que acabe el código actual, pero siempre en el orden en que se añadieron:

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>();
  controller.stream.listen((n) => print('got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
// got 1
// got 2
```

# --instructions--

Ordena los elementos para importar `dart:async`, crear un `StreamController<String>`, suscribirte a su stream imprimiendo cada valor, añadir `hello` y luego `world`, y por último cerrar el controlador. Suscríbete con `listen` antes de añadir ningún valor. El programa debe imprimir:

```
hello
world
```

# --answers--

```dart
import 'dart:async';
```

```dart
void main() {
```

```dart
  final controller = StreamController<String>();
```

```dart
  controller.stream.listen((word) => print(word));
```

```dart
  controller.add('hello');
```

```dart
  controller.add('world');
```

```dart
  controller.close();
```

```dart
}
```

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>();
  controller.stream.listen((word) => print(word));
  controller.add('hello');
  controller.add('world');
  controller.close();
}
```

# --output--

hello
world
