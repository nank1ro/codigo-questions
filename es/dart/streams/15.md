---
language: dart
exerciseType: 4
---

# --description--

Un stream puede llevar **errores** además de valores. Dentro de un generador `async*`, un `throw` envía un evento de error y termina el stream; un `StreamController` puede enviar uno con `addError`.

Del lado del consumo, un bucle `await for` relanza el error donde está el bucle, así que lo manejas con un `try`/`catch` normal alrededor del bucle:

```dart
Stream<int> risky() async* {
  yield 1;
  throw StateError('sensor offline');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('caught: $e');
  }
}
// 1
// caught: Bad state: sensor offline
```

Con `listen`, pasa en su lugar una callback `onError`: `stream.listen(print, onError: (e) => print('caught: $e'));`

# --instructions--

Ordena los elementos para definir una función `async*` `risky` que emita `1` y luego lance una `FormatException('bad')`, y un `main` `async` que imprima cada valor de `risky()` dentro de un `try`/`catch`, imprima `error: ` seguido del error cuando lo capture, e imprima `finished` después de todo el bloque `try`/`catch`, fuera de él. Define `risky` antes de `main`. El programa debe imprimir:

```
1
error: FormatException: bad
finished
```

# --answers--

```dart
Stream<int> risky() async* {
```

```dart
  yield 1;
```

```dart
  throw FormatException('bad');
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  try {
```

```dart
    await for (final n in risky()) {
```

```dart
      print(n);
```

```dart
    }
```

```dart
  } catch (e) {
```

```dart
    print('error: $e');
```

```dart
  }
```

```dart
  print('finished');
```

```dart
}
```

# --solutions--

```dart
Stream<int> risky() async* {
  yield 1;
  throw FormatException('bad');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('error: $e');
  }
  print('finished');
}
```

# --output--

1
error: FormatException: bad
finished
