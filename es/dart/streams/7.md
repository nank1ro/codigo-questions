---
language: dart
exerciseType: 1
---

# --description--

Como `Iterable`, un `Stream` tiene métodos que construyen un **nuevo stream** a partir de uno existente:

- `map` transforma cada evento
- `where` conserva solo los eventos que cumplen una condición
- `take` se detiene después de un número dado de eventos

```dart
final numbers = Stream.fromIterable([1, 2, 3, 4, 5, 6]);
final firstOdds = numbers.where((n) => n.isOdd).take(2);
print(await firstOdds.toList()); // [1, 3]
```

Estos métodos son **perezosos**: no se ejecuta nada hasta que alguien escucha el stream resultante. Se pueden encadenar, y el stream de origen nunca se modifica.

# --instructions--

Escribe una función `evenSquares` que reciba un `Stream<int>` `source` y devuelva un `Stream<int>` con el cuadrado de cada valor **par** de `source`, en el mismo orden.

Ejemplo:
```dart
final source = Stream.fromIterable([1, 2, 3, 4]);
print(await evenSquares(source).toList()); // [4, 16]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`evenSquares` de un stream de `1, 2, 3, 4` debería emitir `4, 16`

```dart
  test('test1', () async {
    expect(await evenSquares(Stream.fromIterable([1, 2, 3, 4])).toList(), [4, 16], reason: '--err-t1--');
  });
```

`evenSquares` de un stream de `6, 5, 10` debería emitir `36, 100`

```dart
  test('test2', () async {
    expect(await evenSquares(Stream.fromIterable([6, 5, 10])).toList(), [36, 100], reason: '--err-t2--');
  });
```

`evenSquares` de un stream de `1, 3` no debería emitir nada y debería devolver un `Stream<int>`

```dart
  test('test3', () async {
    expect(await evenSquares(Stream.fromIterable([1, 3])).toList(), <int>[], reason: '--err-t3--');
    expect(evenSquares(Stream.fromIterable([2])), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> evenSquares(Stream<int> source) {
  return source.where((n) => n.isEven).map((n) => n * n);
}
```
