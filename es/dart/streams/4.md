---
language: dart
exerciseType: 3
---

# --description--

Todo stream acaba terminando. En un generador `async*`, el stream está **terminado** en cuanto acaba el cuerpo de la función, tanto si llegó al final como si encontró un `return`. Un bucle `await for` sobre un stream terminado sale, y cualquier future de `toList()` se completa.

Un stream no se reinicia ni repite sus valores: una vez terminado, sigue terminado.

# --instructions--

¿Qué le pasa al stream devuelto por una función `async*` cuando acaba el cuerpo de la función?

# --answers--

- El stream está terminado y un bucle `await for` sobre él acaba
- El stream vuelve a empezar desde el primer `yield`
- El stream sigue abierto hasta que se llame a `close()` sobre él
- El último valor emitido se emite una vez más

# --solutions--

- El stream está terminado y un bucle `await for` sobre él acaba
