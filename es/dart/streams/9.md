---
language: dart
exerciseType: 1
---

# --description--

Además de `toList()`, un stream ofrece otros métodos que **consumen** todos sus eventos y devuelven un único `Future`:

- `first` y `last` se completan con el primer o el último evento
- `length` se completa con el número de eventos
- `join(separator)` se completa con todos los eventos unidos en un solo `String`
- `reduce(combine)` combina los eventos de dos en dos en un único valor

```dart
final numbers = Stream.fromIterable([3, 9, 2]);
print(await numbers.reduce((a, b) => a + b)); // 14
```

`reduce` llama a `combine` con el resultado acumulado y el siguiente evento. Lanza una excepción si el stream está vacío, así que úsalo solo cuando se garantice al menos un evento.

# --instructions--

Escribe dos funciones:
- `largest`, que recibe un `Stream<int>` `source` y devuelve un `Future<int>` que se completa con el valor más grande, usando `reduce`. El stream siempre tiene al menos un evento.
- `sentence`, que recibe un `Stream<String>` `words` y devuelve un `Future<String>` que se completa con todas las palabras unidas por un solo espacio.

Ejemplo:
```dart
print(await largest(Stream.fromIterable([3, 9, 2])));        // 9
print(await sentence(Stream.fromIterable(['I', 'am', 'Dart']))); // I am Dart
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest` de un stream de `3, 9, 2` debería completarse con `9`

```dart
  test('test1', () async {
    expect(await largest(Stream.fromIterable([3, 9, 2])), 9, reason: '--err-t1--');
  });
```

`largest` de un stream de `-4, -1, -7` debería completarse con `-1` y `largest` de un stream de `5` debería completarse con `5`

```dart
  test('test2', () async {
    expect(await largest(Stream.fromIterable([-4, -1, -7])), -1, reason: '--err-t2--');
    expect(await largest(Stream.fromIterable([5])), 5, reason: '--err-t2--');
  });
```

`sentence` de un stream de `'I', 'am', 'Dart'` debería completarse con `'I am Dart'`

```dart
  test('test3', () async {
    expect(await sentence(Stream.fromIterable(['I', 'am', 'Dart'])), 'I am Dart', reason: '--err-t3--');
    expect(await sentence(Stream.fromIterable(['solo'])), 'solo', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> largest(Stream<int> source) {
  return source.reduce((a, b) => a > b ? a : b);
}

Future<String> sentence(Stream<String> words) {
  return words.join(' ');
}
```
