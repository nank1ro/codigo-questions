---
language: dart
exerciseType: 2
---

# --description--

Un error se **propaga** por cada `await` que no lo captura. Si `load()` falla, `await load()` dentro de `loadTwice()` lanza; como `loadTwice` no tiene `try`/`catch`, su propio future falla con el mismo error; y así hacia arriba en la cadena, hasta que algún `await` esté envuelto en un `try`/`catch`:

```dart
Future<int> load() async {
  throw StateError('offline');
}

Future<int> loadTwice() async {
  final n = await load();   // throws here, loadTwice fails too
  return n * 2;             // never runs
}

Future<void> main() async {
  try {
    print(await loadTwice());
  } catch (e) {
    print('failed: $e');    // failed: Bad state: offline
  }
}
```

Esto refleja cómo se propagan las excepciones en las llamadas síncronas: las manejas una vez, en el nivel que sabe qué hacer.

# --instructions--

Completa el código para que `fetch` sea asíncrona y lance, `fetchLength` la espere, y `main` capture el error e imprima `failed: ` seguido de él. El programa debe imprimir `failed: Bad state: no server`.

# --seed--

```dart
Future<String> fetch() [/] {
  throw StateError('no server');
}

Future<int> fetchLength() async {
  final text = [/] fetch();
  return text.length;
}

Future<void> main() async {
  [/] {
    print(await fetchLength());
  } [/] (e) {
    print('failed: $e');
  }
}
```

# --answers--

- async
- await
- try
- catch
- then
- finally
- sync*

# --solutions--

```dart
Future<String> fetch() async {
  throw StateError('no server');
}

Future<int> fetchLength() async {
  final text = await fetch();
  return text.length;
}

Future<void> main() async {
  try {
    print(await fetchLength());
  } catch (e) {
    print('failed: $e');
  }
}
```

# --output--

failed: Bad state: no server
