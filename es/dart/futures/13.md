---
language: dart
exerciseType: 1
---

# --description--

No necesitas `Future.wait` para ejecutar dos futures a la vez. Una función `async` empieza a ejecutarse en cuanto se la **llama**, hasta su primer `await`; el future que recibes es el trabajo ya en marcha. El truco es, por tanto: **llamar primero, esperar después**:

```dart
Future<int> pages() => Future.delayed(const Duration(milliseconds: 20), () => 300);
Future<int> words() => Future.delayed(const Duration(milliseconds: 20), () => 90000);

Future<double> average() async {
  final p = pages();          // both timers start now
  final w = words();
  return await w / await p;   // waits once, about 20 ms in total
}
```

Compáralo con `return await words() / await pages();`, donde `pages()` solo se llama después de que `words()` se haya completado: mismo resultado, el doble de tiempo. Prefiere la forma concurrente siempre que la segunda llamada no necesite el resultado de la primera.

# --instructions--

La función `slow(value)` devuelve un `Future<int>` que se completa con `value` tras un breve retardo, y registra `start <value>` cuando se la llama y `end <value>` cuando se completa en la lista `calls`. Escribe una función `async` `addConcurrently` que reciba dos `int` `a` y `b`, llame a `slow(a)` y `slow(b)` de forma que ambos se ejecuten **a la vez**, y devuelva un `Future<int>` que se complete con la suma de los dos resultados.

Ejemplo:
```dart
print(await addConcurrently(2, 3)); // 5
print(calls); // [start 2, start 3, end 2, end 3]
```

# --before-seed--

```dart
final calls = <String>[];

Future<int> slow(int value) async {
  calls.add('start $value');
  await Future.delayed(const Duration(milliseconds: 5));
  calls.add('end $value');
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`addConcurrently(2, 3)` debería completarse con `5`

```dart
  test('test1', () async {
    calls.clear();
    expect(await addConcurrently(2, 3), 5, reason: '--err-t1--');
  });
```

`addConcurrently(2, 3)` debería llamar a `slow(2)` y `slow(3)` antes de que cualquiera se complete, así que `calls` debería ser `[start 2, start 3, end 2, end 3]`

```dart
  test('test2', () async {
    calls.clear();
    await addConcurrently(2, 3);
    expect(calls, ['start 2', 'start 3', 'end 2', 'end 3'], reason: '--err-t2--');
  });
```

`addConcurrently(10, -4)` debería completarse con `6` y ambas llamadas deberían empezar antes de que cualquiera se complete

```dart
  test('test3', () async {
    calls.clear();
    expect(await addConcurrently(10, -4), 6, reason: '--err-t3--');
    expect(calls, ['start 10', 'start -4', 'end 10', 'end -4'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> addConcurrently(int a, int b) async {
  final first = slow(a);
  final second = slow(b);
  return await first + await second;
}
```
