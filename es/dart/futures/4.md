---
language: dart
exerciseType: 3
---

# --description--

Ten claras las dos caras de un future:

- una función `async` **declara** `Future<T>` y **devuelve** un `T` sin envolver: el envoltorio es automático
- quien hace `await` sobre un `Future<T>` **recibe** un `T` sin envolver: el desenvoltorio es automático

```dart
Future<int> count() async {
  return 3;                 // int, envuelto en Future<int>
}

Future<void> main() async {
  int n = await count();    // Future<int>, desenvuelto a int
  Future<int> f = count();  // sin await: sigue siendo un Future<int>
}
```

Escribir `int count() async` es un error: una función `async` debe declarar un tipo de retorno `Future` (o `void`).

# --instructions--

¿Qué tipo de retorno debe declarar una función `async` cuyo cuerpo termina con `return 3;`?

# --answers--

- `Future<int>`
- `int`
- `async int`
- `Stream<int>`

# --solutions--

- `Future<int>`
