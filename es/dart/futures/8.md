---
language: dart
exerciseType: 1
---

# --description--

Un future también puede completarse con un **error**. Cuando una función `async` lanza, la excepción no escapa de inmediato: se convierte en el error del future devuelto. Quien haga `await` sobre ese future ve el error lanzado en el `await`, así que puede manejarse con un `try`/`catch` normal:

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s); // throws FormatException for 'abc'
}

Future<int> orZero(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return 0;
  }
}
```

El `await` dentro del `try` es esencial: `return parseLater(s);` entregaría el future al llamador **sin esperar**, así que el error llegaría cuando el bloque `try` ya hubiera terminado y el `catch` nunca se ejecutaría.

# --instructions--

La función `parseLater(s)` devuelve un `Future<int>` que se completa con el número escrito en `s`, o con un error si `s` no es un número. Escribe una función `async` `safeParse` que reciba un `String` `s` y devuelva un `Future<int>` que se complete con el número interpretado, o con `-1` si `parseLater` falla.

Ejemplo:
```dart
print(await safeParse('12'));  // 12
print(await safeParse('abc')); // -1
```

# --before-seed--

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s);
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeParse('12')` debería completarse con `12`

```dart
  test('test1', () async {
    expect(await safeParse('12'), 12, reason: '--err-t1--');
  });
```

`safeParse('abc')` debería completarse con `-1` en lugar de lanzar

```dart
  test('test2', () async {
    expect(await safeParse('abc'), -1, reason: '--err-t2--');
  });
```

`safeParse('-7')` debería completarse con `-7` y `safeParse('')` con `-1`

```dart
  test('test3', () async {
    expect(await safeParse('-7'), -7, reason: '--err-t3--');
    expect(await safeParse(''), -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> safeParse(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return -1;
  }
}
```
