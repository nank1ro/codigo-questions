---
language: dart
exerciseType: 1
---

# --description--

Cuando varios futures no dependen entre sí, pásalos todos a **`Future.wait`**: recibe una `List<Future<T>>`, los deja ejecutarse a la vez y devuelve un único `Future<List<T>>` que se completa cuando **todos** han terminado. Los resultados mantienen el orden de la lista de entrada, sin importar cuál terminó antes:

```dart
Future<String> slow() => Future.delayed(const Duration(milliseconds: 20), () => 'slow');
Future<String> fast() => Future.delayed(const Duration(milliseconds: 5), () => 'fast');

final results = await Future.wait([slow(), fast()]);
print(results); // [slow, fast]
```

`await slow(); await fast();` tarda `25` milisegundos, porque `fast()` solo se llama cuando `slow()` se ha completado; `await Future.wait([slow(), fast()])` tarda unos `20`, la duración del más largo.

# --instructions--

Escribe una función `async` `sumOfFutures` que reciba una `List<Future<int>>` `futures`, espere a todos con `Future.wait` y devuelva un `Future<int>` que se complete con la suma de sus valores. Una lista vacía da `0`.

Ejemplo:
```dart
final futures = [Future.value(1), Future.delayed(const Duration(milliseconds: 5), () => 2)];
print(await sumOfFutures(futures)); // 3
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumOfFutures` de futures que se completan con `1`, `2` y `3` debería completarse con `6`

```dart
  test('test1', () async {
    final futures = [
      Future.value(1),
      Future.delayed(const Duration(milliseconds: 5), () => 2),
      Future.value(3),
    ];
    expect(await sumOfFutures(futures), 6, reason: '--err-t1--');
  });
```

`sumOfFutures` de un único future que se completa con `10` debería completarse con `10`

```dart
  test('test2', () async {
    expect(await sumOfFutures([Future.delayed(const Duration(milliseconds: 5), () => 10)]), 10, reason: '--err-t2--');
  });
```

`sumOfFutures` de una lista vacía debería completarse con `0` y `sumOfFutures` debería devolver un `Future<int>`

```dart
  test('test3', () async {
    expect(await sumOfFutures(<Future<int>>[]), 0, reason: '--err-t3--');
    expect(sumOfFutures([Future.value(4)]), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> sumOfFutures(List<Future<int>> futures) async {
  final values = await Future.wait(futures);
  var total = 0;
  for (final v in values) {
    total += v;
  }
  return total;
}
```
