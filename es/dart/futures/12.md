---
language: dart
exerciseType: 4
---

# --description--

`Future.wait` es la herramienta para "cargar varias cosas y luego continuar". La forma típica es: construir la lista de futures, hacer `await Future.wait` sobre ella y luego usar la lista resultante:

```dart
Future<int> stock() => Future.delayed(const Duration(milliseconds: 10), () => 8);
Future<int> orders() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<void> main() async {
  final counts = await Future.wait([stock(), orders()]);
  print(counts); // [8, 3]
}
```

`orders()` se completa antes, pero la lista sigue el orden de las llamadas: `stock()` primero, `orders()` segundo.

# --instructions--

Ordena los elementos para definir una función `one` que se complete con `1` tras `10` milisegundos, una función `two` que se complete con `2` tras `5` milisegundos, y una `main` `async` que imprima `loading`, espere ambas funciones con `Future.wait` en `values` e imprima `values`. Define `one` primero, luego `two`, luego `main`. El programa debe imprimir:

```
loading
[1, 2]
```

# --answers--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
```

```dart
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);
```

```dart
Future<void> main() async {
```

```dart
  print('loading');
```

```dart
  final values = await Future.wait([one(), two()]);
```

```dart
  print(values);
```

```dart
}
```

# --solutions--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);

Future<void> main() async {
  print('loading');
  final values = await Future.wait([one(), two()]);
  print(values);
}
```

# --output--

loading
[1, 2]
