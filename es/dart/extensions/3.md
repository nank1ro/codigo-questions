---
language: dart
exerciseType: 1
---

# --description--

Una extensión también puede declarar **getters**, que se leen como propiedades, sin paréntesis. Dentro de una extensión puedes llamar directamente a los miembros propios del tipo: `this.` es opcional, exactamente igual que dentro de una clase.

```dart
extension Sizes on String {
  bool get isLong => length > 10;      // same as this.length
  String get firstChar => this[0];
}

void main() {
  print('Dart'.isLong);           // false
  print('extension'.firstChar);   // e
}
```

Elige un getter cuando el miembro solo **lee** un valor y no recibe parámetros; elige un método cuando realiza trabajo o necesita argumentos.

# --instructions--

Escribe una extensión llamada `Blank` sobre `String` con un getter `isBlank` de tipo `bool` que sea `true` cuando la cadena esté vacía o contenga solo espacios. Usa `trim()` y `isEmpty`.

Ejemplo:
```dart
print('   '.isBlank);  // true
print(' hi '.isBlank); // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`''.isBlank` y `'   '.isBlank` deben ser `true`

```dart
  test('test1', () {
    expect(''.isBlank, true, reason: '--err-t1--');
    expect('   '.isBlank, true, reason: '--err-t1--');
  });
```

`' hi '.isBlank` debe ser `false`

```dart
  test('test2', () {
    expect(' hi '.isBlank, false, reason: '--err-t2--');
    expect('x'.isBlank, false, reason: '--err-t2--');
  });
```

`isBlank` debe ser un `bool`

```dart
  test('test3', () {
    expect('  '.isBlank, isA<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Blank on String {
  bool get isBlank => trim().isEmpty;
}
```
