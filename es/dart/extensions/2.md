---
language: dart
exerciseType: 1
---

# --description--

Las extensiones funcionan con cualquier tipo, incluidos los números. Esta extensión da a cada `int` un método que lo duplica:

```dart
extension Doubling on int {
  int doubled() => this * 2;
}
```

Como la extensión se aplica al **tipo**, puedes llamar al método sobre una variable o directamente sobre un **literal**. Los literales negativos necesitan paréntesis, de lo contrario el punto se lee antes que el signo menos:

```dart
var n = 21;
print(n.doubled());    // 42
print(4.doubled());    // 8
print((-3).doubled()); // -6
```

# --instructions--

Escribe una extensión llamada `Squaring` sobre `int` con un método `squared` que no reciba parámetros y devuelva el número multiplicado por sí mismo.

Ejemplo:
```dart
print(5.squared()); // 25
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`5.squared()` debe ser `25`

```dart
  test('test1', () {
    expect(5.squared(), 25, reason: '--err-t1--');
  });
```

`(-3).squared()` debe ser `9`

```dart
  test('test2', () {
    expect((-3).squared(), 9, reason: '--err-t2--');
  });
```

`0.squared()` debe ser `0` y el resultado debe ser un `int`

```dart
  test('test3', () {
    expect(0.squared(), 0, reason: '--err-t3--');
    expect(12.squared(), isA<int>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Squaring on int {
  int squared() => this * this;
}
```
