---
language: dart
exerciseType: 2
---

# --description--

Una extensión sobre `String` no puede llamarse sobre un `String?`: el valor podría ser `null` y el compilador rechaza la llamada. Si en cambio declaras la extensión sobre el tipo **nullable**, el método puede llamarse directamente sobre un `String?` y, dentro de él, `this` tiene tipo `String?`, así que debes manejar tú mismo el caso `null`, por ejemplo con `??`:

```dart
extension Defaults on int? {
  int orZero() => this ?? 0;
}

void main() {
  int? count = null;
  print(count.orZero()); // 0
  print(5.orZero());     // 5
}
```

Un `int` no nullable puede pasarse donde se espera un `int?`, así que la extensión funciona con ambos.

# --instructions--

Completa la extensión para que `orEmpty` pueda llamarse sobre una cadena nullable y devuelva la propia cadena, o la cadena vacía cuando es `null`. El programa debe imprimir `[]` y `[Ada]`.

# --seed--

```dart
extension Defaults on [/] {
  String orEmpty() => this [/] '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --answers--

- String?
- ??
- String
- +
- ?.
- !

# --solutions--

```dart
extension Defaults on String? {
  String orEmpty() => this ?? '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --output--

[]
[Ada]
