---
language: dart
exerciseType: 4
---

# --description--

El tipo después de `on` puede ser un tipo **parametrizado** como `List<int>`. La extensión se aplica entonces solo a listas de ese tipo de elementos: `[1, 2].total()` funciona, `['a', 'b'].total()` no compila.

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}
```

Dentro de la extensión, `this` es la lista, así que puedes recorrerla, indexarla o llamar a `length` como de costumbre.

# --instructions--

Ordena los elementos para declarar una extensión `Totals` sobre `List<int>` con un método `total` que sume los elementos usando un bucle `for`, y después un `main` que imprima el total de `[3, 4, 5]`. Declara la extensión antes de `main`. El programa debe imprimir:

```
12
```

# --answers--

```dart
extension Totals on List<int> {
```

```dart
  int total() {
```

```dart
    var sum = 0;
```

```dart
    for (final n in this) {
```

```dart
      sum += n;
```

```dart
    }
```

```dart
    return sum;
```

```dart
  }
```

```dart
}
```

```dart
void main() {
```

```dart
  print([3, 4, 5].total());
```

```dart
}
```

# --solutions--

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}

void main() {
  print([3, 4, 5].total());
}
```

# --output--

12
