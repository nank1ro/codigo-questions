---
language: dart
exerciseType: 4
---

# --description--

Las extensiones no son solo para tipos del SDK: también puedes extender **tus propias clases**. Esto es útil cuando la clase viene de un paquete que no controlas, o cuando quieres mantener la clase pequeña y añadir ayudantes opcionales junto al código que los necesita.

```dart
class Circle {
  final double radius;
  Circle(this.radius);
}

extension CircleMath on Circle {
  double get diameter => radius * 2;
}

void main() {
  print(Circle(3).diameter); // 6.0
}
```

La extensión ve los campos y métodos públicos de la clase, exactamente igual que el código escrito fuera de la clase.

# --instructions--

Ordena los elementos para definir una clase `Rectangle` con `width` y `height`, después una extensión `RectangleMath` sobre `Rectangle` con un getter `area`, y después un `main` que imprima el área de un rectángulo de `4` por `2.5`. Define primero la clase, luego la extensión y después `main`. Dentro de la clase, declara ambos campos antes del constructor. El programa debe imprimir:

```
10.0
```

# --answers--

```dart
class Rectangle {
```

```dart
  final double width;
```

```dart
  final double height;
```

```dart
  Rectangle(this.width, this.height);
```

```dart
}
```

```dart
extension RectangleMath on Rectangle {
```

```dart
  double get area => width * height;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Rectangle(4, 2.5).area);
```

```dart
}
```

# --solutions--

```dart
class Rectangle {
  final double width;
  final double height;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

```dart
class Rectangle {
  final double height;
  final double width;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

# --output--

10.0
