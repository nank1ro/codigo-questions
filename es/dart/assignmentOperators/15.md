---
language: dart
exerciseType: 1
---

# --instructions--

Una tienda está aplicando un descuento del 20% a un artículo. Declara una variable `double` de nivel superior `price` con valor `50.0`. Luego usa `*=` para aplicar el descuento multiplicando `price` por `0.8`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`price` debería ser `40.0`

```dart
  test('test1', () {
    expect(price, closeTo(40.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double price = 50.0;

void main() {
  price *= 0.8;
  print(price);
}
```
