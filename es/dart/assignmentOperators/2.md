---
language: dart
exerciseType: 2
---

# --description--

El **operador de asignación de adición** `+=` suma un valor a una variable y almacena el resultado de nuevo en esa variable.

```dart
int score = 10;
score += 5; // score is now 15
```

Esta es una abreviatura para `score = score + 5`.

# --instructions--

Completa el código para agregar `8` a la variable `total` usando `+=`.

# --seed--

```dart
void main() {
  int total = 12;
  total [/] 8;
  print(total);
}
```

# --answers--

- +=
- =
- -=
- +

# --solutions--

```dart
void main() {
  int total = 12;
  total += 8;
  print(total);
}
```

# --output--

20
