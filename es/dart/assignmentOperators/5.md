---
language: dart
exerciseType: 3
---

# --description--

Los operadores de asignación combinan una operación aritmética con una asignación en un solo paso.

Por ejemplo, `x += 3` es una abreviatura para `x = x + 3`.

# --instructions--

¿Qué hace `x += 5` cuando `x` es actualmente `10`?

# --answers--

- Establece `x` a `15`
- Establece `x` a `5`
- Establece `x` a `10`
- Establece `x` a `50`

# --solutions--

- Establece `x` a `15`
