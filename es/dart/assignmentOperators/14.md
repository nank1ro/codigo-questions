---
language: dart
exerciseType: 1
---

# --instructions--

Declara una variable `int` `n` con valor `2`. Luego, en `main`, aplica los siguientes operadores de asignación en orden:
- `n *= 10` (multiplicar por 10)
- `n += 5` (agregar 5)
- `n -= 3` (restar 3)

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int n = 2;
```

# --after-seed--

```dart
  return n;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`n` debería ser `22`

```dart
  test('test1', () {
    expect(result(), 22, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 2;
n *= 10;
n += 5;
n -= 3;
```
