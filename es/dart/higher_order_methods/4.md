---
language: dart
exerciseType: 1
---

# --description--

Algunos métodos de orden superior responden una pregunta sobre la colección en lugar de construir una nueva. Toman un predicado y devuelven un `bool`:

- `any` es `true` cuando **al menos un** elemento satisface el predicado
- `every` es `true` cuando **todos** los elementos lo satisfacen

```dart
final numbers = [1, 2, 3];
print(numbers.any((n) => n > 2));   // true
print(numbers.every((n) => n > 2)); // false
```

Ambos se detienen en cuanto la respuesta es segura: `any` en el primer elemento que coincide, `every` en el primero que no.

Sobre una colección vacía `any` es `false` y `every` es `true`: no hay ningún elemento que pruebe la primera, y ninguno que rompa la segunda.

# --instructions--

Escribe dos funciones, ambas toman una `List<int>` `numbers`:

- `hasNegative` devuelve `true` si al menos un elemento es menor que `0`
- `allEven` devuelve `true` si todos los elementos son pares

Ejemplo:
```dart
print(hasNegative([1, -2, 3])); // true
print(allEven([2, 4]));         // true
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasNegative([1, -2, 3])` debería ser `true` y `hasNegative([1, 2, 3])` debería ser `false`

```dart
  test('test1', () {
    expect(hasNegative([1, -2, 3]), true, reason: '--err-t1--');
    expect(hasNegative([1, 2, 3]), false, reason: '--err-t1--');
  });
```

`allEven([2, 4, 6])` debería ser `true` y `allEven([2, 5])` debería ser `false`

```dart
  test('test2', () {
    expect(allEven([2, 4, 6]), true, reason: '--err-t2--');
    expect(allEven([2, 5]), false, reason: '--err-t2--');
  });
```

En una lista vacía `hasNegative` debería ser `false` y `allEven` debería ser `true`

```dart
  test('test3', () {
    expect(hasNegative(<int>[]), false, reason: '--err-t3--');
    expect(allEven(<int>[]), true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool hasNegative(List<int> numbers) {
  return numbers.any((n) => n < 0);
}

bool allEven(List<int> numbers) {
  return numbers.every((n) => n % 2 == 0);
}
```
