---
language: dart
exerciseType: 1
---

# --description--

Cuando la función que le das a `map` devuelve una colección para cada elemento, acabas con una secuencia de colecciones. **`expand`** hace el mismo trabajo pero luego une todas en una única secuencia plana:

```dart
final numbers = [1, 2];
print(numbers.map((n) => [n, -n]).toList());    // [[1, -1], [2, -2]]
print(numbers.expand((n) => [n, -n]).toList()); // [1, -1, 2, -2]
```

El orden se conserva: todo lo producido por el primer elemento va primero, luego todo lo producido por el segundo, y así sucesivamente.

Como la colección devuelta puede tener cualquier tamaño, `expand` también es la forma de producir **más o menos** elementos de los que tenías al principio: devolver una lista vacía para un elemento simplemente lo descarta.

```dart
print(['a b', 'c'].expand((s) => s.split(' ')).toList()); // [a, b, c]
```

# --instructions--

Escribe una función `doubleUp` que tome una `List<int>` `numbers` y devuelva una nueva `List<int>` donde cada elemento aparece dos veces seguidas, usando `expand`.

Ejemplo:
```dart
print(doubleUp([1, 2])); // [1, 1, 2, 2]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`doubleUp([1, 2])` debería devolver `[1, 1, 2, 2]`

```dart
  test('test1', () {
    expect(doubleUp([1, 2]), [1, 1, 2, 2], reason: '--err-t1--');
  });
```

`doubleUp` debería mantener el orden original y devolver una `List<int>`

```dart
  test('test2', () {
    expect(doubleUp([5, 0, -3]), [5, 5, 0, 0, -3, -3], reason: '--err-t2--');
    expect(doubleUp([7]), isA<List<int>>(), reason: '--err-t2--');
  });
```

`doubleUp` debería devolver una lista vacía para una lista vacía, y no debe cambiar la lista que recibe

```dart
  test('test3', () {
    expect(doubleUp(<int>[]), <int>[], reason: '--err-t3--');
    final original = [4, 6];
    doubleUp(original);
    expect(original, [4, 6], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> doubleUp(List<int> numbers) {
  return numbers.expand((n) => [n, n]).toList();
}
```
