---
language: dart
exerciseType: 1
---

# --description--

Cada uno de estos métodos devuelve un `Iterable`, y cada `Iterable` tiene otra vez los mismos métodos. Eso es lo que permite **encadenarlos**: un cálculo completo se lee como un pipeline de izquierda a derecha, cada paso trabajando sobre lo que produjo el anterior.

```dart
final words = ['kiwi', 'fig', 'banana', 'date'];
print(words.where((w) => w.length == 4).map((w) => w.toUpperCase()).toList());
// [KIWI, DATE]
```

Solo el último paso necesita `toList()`: llamarlo en medio construiría una lista que nadie guarda.

El tipo cambia a lo largo de la cadena, y también lo que recibe la siguiente función: después de `where` sobre una `List<String>` todavía tienes cadenas, pero después de `map((w) => w.length)` el siguiente paso ve números.

Como cada paso es perezoso, el orden importa para el trabajo realizado, no solo para el resultado: filtrar primero con `where` significa que `map` se llama sobre menos elementos.

# --instructions--

Escribe una función `shortUpper` que tome una `List<String>` `words` y devuelva una nueva `List<String>` con las palabras de como máximo `4` caracteres, en mayúsculas, en su orden original.

Ejemplo:
```dart
print(shortUpper(['kiwi', 'banana', 'fig'])); // [KIWI, FIG]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shortUpper(['kiwi', 'banana', 'fig'])` debería devolver `['KIWI', 'FIG']`

```dart
  test('test1', () {
    expect(shortUpper(['kiwi', 'banana', 'fig']), ['KIWI', 'FIG'],
        reason: '--err-t1--');
  });
```

Las palabras de exactamente `4` caracteres deberían conservarse, y las más largas descartarse

```dart
  test('test2', () {
    expect(shortUpper(['date', 'grape']), ['DATE'], reason: '--err-t2--');
    expect(shortUpper(['melon']), <String>[], reason: '--err-t2--');
  });
```

`shortUpper` debería devolver una `List<String>` y no debe cambiar la lista que recibe

```dart
  test('test3', () {
    expect(shortUpper(['a']), isA<List<String>>(), reason: '--err-t3--');
    final original = ['fig', 'banana'];
    shortUpper(original);
    expect(original, ['fig', 'banana'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<String> shortUpper(List<String> words) {
  return words.where((w) => w.length <= 4).map((w) => w.toUpperCase()).toList();
}
```
