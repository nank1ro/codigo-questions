---
language: dart
exerciseType: 1
---

# --description--

En conjunto, estos métodos reemplazan la mayoría de los bucles escritos a mano. Un pipeline suele leerse en tres etapas: **selecciona** los elementos con `where`, **transfórmalos** con `map`, y luego **combínalos** con `fold`:

```dart
final prices = [12, 40, 7];
final cheapTotal = prices.where((p) => p < 20).fold(0, (acc, p) => acc + p);
print(cheapTotal); // 19
```

Como `fold` elige su propio valor inicial, también puede terminar una cadena con un tipo que no tiene nada que ver con los elementos, como una `String` que crece una pieza a la vez:

```dart
final words = ['fig', 'kiwi'];
final firstLetters = words.fold('', (acc, w) => acc + w[0]);
print(firstLetters); // fk
```

Cada etapa se mantiene corta y dice lo que hace, que es la razón real para preferirlos sobre un bucle que hace las tres cosas a la vez.

# --instructions--

Escribe una función `initials` que tome una `List<String>` `names`, ignore las vacías, y devuelva una `String` formada por la primera letra de cada nombre restante, en mayúsculas y en orden.

Ejemplo:
```dart
print(initials(['ada', '', 'linus'])); // AL
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`initials(['ada', '', 'linus'])` debería devolver `'AL'`

```dart
  test('test1', () {
    expect(initials(['ada', '', 'linus']), 'AL', reason: '--err-t1--');
  });
```

Las letras deberían estar en mayúsculas y en orden, y los nombres que ya están en mayúsculas deberían quedarse como están

```dart
  test('test2', () {
    expect(initials(['grace', 'Bob', 'ada']), 'GBA', reason: '--err-t2--');
    expect(initials(['x']), 'X', reason: '--err-t2--');
  });
```

`initials` debería devolver una `String` vacía cuando no haya ningún nombre no vacío

```dart
  test('test3', () {
    expect(initials(<String>[]), '', reason: '--err-t3--');
    expect(initials(['', '']), '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String initials(List<String> names) {
  return names
      .where((n) => n.isNotEmpty)
      .map((n) => n[0].toUpperCase())
      .fold('', (acc, letter) => acc + letter);
}
```
