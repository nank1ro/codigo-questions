---
language: dart
exerciseType: 1
---

# --instructions--

Inicializa la variable `x` con el número `1`, luego actualízala con el valor `"hello"` sin encontrar ningún error.

# --before-seed--

```dart
dynamic value() {
```

# --after-seed--

```dart
  return x;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x` debe ser igual a `"hello"`.

```dart
  test('test1', () {
    expect(value(), "hello", reason: '--err-t1--');
  });
```

`x` debe ser de tipo `dynamic`

```dart
  test('test2', () {
    expect(value(), TypeMatcher<dynamic>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
dynamic x = 1;
x = "hello";
```
