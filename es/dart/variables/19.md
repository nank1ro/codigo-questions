---
language: dart
exerciseType: 1
---

# --instructions--

Inicializa la variable `x` con el número `1`, luego actualízala con el valor `"hola"` sin encontrar ningún error.

# --before-seed--

```dart
dynamic valor() {
```

# --after-seed--

```dart
  return x;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x` debe ser igual a `"hola"`.

```dart
  test('test1', () {
    expect(valor(), "hola", reason: '--err-t1--');
  });
```

`x` debe ser de tipo `dynamic`

```dart
  test('test2', () {
    expect(valor(), TypeMatcher<dynamic>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
dynamic x = 1;
x = "hola";
```
