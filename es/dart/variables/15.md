---
language: dart
exerciseType: 1
---

# --instructions--

Inicializa la variable `x` con el número `20` de modo que admita tanto enteros como decimales.
Actualiza la variable con el número `3.14159`.

# --before-seed--

```dart
dynamic numero() {
```

# --after-seed--

```dart
  return x;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x` debe ser igual a `3.14159`.

```dart
  test('test1', () {
    expect(numero(), 3.14159, reason: '--err-t1--');
  });
```

`x` debe ser de tipo `num`

```dart
  test('test2', () {
    expect(numero(), TypeMatcher<num>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
num x = 20;
x = 3.14159;
```
