---
language: dart
exerciseType: 1
---

# --description--

El tipo `int` permite almacenar números enteros.
Para guardar números decimales en su lugar, podemos usar el tipo `double`:

```dart
double pi = 3.14159;
```

Este ejemplo es similar al anterior. Sin embargo, esta vez, la variable es de tipo `double`, un tipo que permite almacenar números decimales con alta precisión.


# --instructions--

Asigna a la variable `x` el valor `6.28`.

# --before-seed--

```dart
dynamic number() {
```

# --after-seed--

```dart
  return x;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x` debe ser igual a `6.28`.

```dart
  test('test1', () {
    expect(number(), 6.28, reason: '--err-t1--');
  });
```

`x` debe ser de tipo `double`

```dart
    test('test2', () {
      expect(number(), TypeMatcher<double>(), reason: '--err-t2--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double x = 6.28;
```
