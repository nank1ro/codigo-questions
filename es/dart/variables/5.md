---
language: dart
exerciseType: 1
---

# --description--

Dart admite la __inferencia de tipos__.
No es necesario indicar el tipo de una variable ya que Dart puede inferirlo.
La palabra clave `var` le dice a Dart que use el tipo más apropiado.

```dart
var numero = 5;
```

No es necesario decirle a Dart que el número `5` es de tipo `int`.
Dart infiere el tipo y hace que `numero` sea de tipo `int`.

# --instructions--

Inicializa la variable `x` con el número `3.14159` y deja que Dart infiera el tipo.

# --before-seed--

```dart
dynamic numero() {
```

# --after-seed--

```dart
  return x;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x` debe ser igual a `3.14159`.

```dart
  test('test1', () {
    expect(numero(), 3.14159, reason: '--err-t1--');
  });
```

`x` debe ser de tipo `double`

```dart
  test('test2', () {
    expect(numero(), TypeMatcher<double>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var x = 3.14159;
```
