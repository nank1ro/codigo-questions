---
language: dart
exerciseType: 2
---

# --description--

El **operador ternario** `condition ? expr1 : expr2` es una forma compacta de escribir una expresión `if-else` simple.

```dart
int x = 10;
String label = x > 0 ? 'positive' : 'non-positive';
print(label); // positive
```

Si la condición es `true`, se usa `expr1`; de lo contrario, se usa `expr2`.

# --instructions--

Completa la expresión ternaria para asignar `'Even'` a `parity` cuando `n` sea divisible por `2`, y `'Odd'` en caso contrario.

# --seed--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 [/] 'Even' [/] 'Odd';
  print(parity);
}
```

# --answers--

- ?
- :
- ==
- ;

# --solutions--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 ? 'Even' : 'Odd';
  print(parity);
}
```

# --output--

Even
