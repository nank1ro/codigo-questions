---
language: dart
exerciseType: 1
---

# --instructions--

Escribe una función `trafficLight(String color)` que devuelva `'Go'` para `'green'`, `'Slow down'` para `'yellow'`, `'Stop'` para `'red'`, e `'Invalid'` para cualquier otro valor, usando una sentencia `switch` con una cláusula `default`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`trafficLight('green')` debe devolver `'Go'`

```dart
  test('test1', () {
    expect(trafficLight('green'), 'Go', reason: '--err-t1--');
  });
```

`trafficLight('yellow')` debe devolver `'Slow down'`

```dart
  test('test2', () {
    expect(trafficLight('yellow'), 'Slow down', reason: '--err-t2--');
  });
```

`trafficLight('red')` debe devolver `'Stop'`

```dart
  test('test3', () {
    expect(trafficLight('red'), 'Stop', reason: '--err-t3--');
  });
```

`trafficLight('blue')` debe devolver `'Invalid'`

```dart
  test('test4', () {
    expect(trafficLight('blue'), 'Invalid', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String trafficLight(String color) {
  switch (color) {
    case 'green':
      return 'Go';
    case 'yellow':
      return 'Slow down';
    case 'red':
      return 'Stop';
    default:
      return 'Invalid';
  }
}

void main() {
  print(trafficLight('green'));
}
```
