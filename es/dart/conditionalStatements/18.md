---
language: dart
exerciseType: 1
---

# --instructions--

Escribe una función `parity(int n)` que devuelva `'even'` cuando `n` sea divisible por `2`, y `'odd'` en caso contrario, usando una sentencia `if-else`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parity(4)` debe devolver `'even'`

```dart
  test('test1', () {
    expect(parity(4), 'even', reason: '--err-t1--');
  });
```

`parity(7)` debe devolver `'odd'`

```dart
  test('test2', () {
    expect(parity(7), 'odd', reason: '--err-t2--');
  });
```

`parity(0)` debe devolver `'even'`

```dart
  test('test3', () {
    expect(parity(0), 'even', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String parity(int n) {
  if (n % 2 == 0) {
    return 'even';
  } else {
    return 'odd';
  }
}

void main() {
  print(parity(4));
}
```
