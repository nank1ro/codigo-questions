---
language: dart
exerciseType: 2
---

# --description--

Las funciones pueden aceptar **parámetros** — valores que se pasan al llamar a la función. Los parámetros se declaran dentro de los paréntesis con su tipo y nombre:

```dart
int square(int n) {
  return n * n;
}

void main() {
  print(square(4)); // prints 16
}
```

Cada parámetro tiene un tipo (`int`) y un nombre (`n`). Los parámetros múltiples se separan por comas.

# --instructions--

Completa la función `multiply` que toma dos parámetros `int` `a` y `b` y devuelve su producto.

# --seed--

```dart
int multiply([/] a, [/] b) {
  return a [/] b;
}

void main() {
  print(multiply(3, 4));
}
```

# --answers--

- int
- int
- *
- +
- String
- double

# --solutions--

```dart
int multiply(int a, int b) {
  return a * b;
}

void main() {
  print(multiply(3, 4));
}
```

# --output--

12
