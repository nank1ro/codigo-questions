---
language: dart
exerciseType: 1
---

# --instructions--

Escribe una función `fullName` que tome dos **parámetros con nombre** `firstName` y `lastName` (ambos `required String`) y devuelva el nombre completo como una sola cadena con un espacio entre ellos.

Ejemplo:
```dart
print(fullName(firstName: "Jane", lastName: "Doe")); // Jane Doe
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`fullName(firstName: "Jane", lastName: "Doe")` debe devolver `"Jane Doe"`
```dart
  test('test1', () {
    expect(fullName(firstName: "Jane", lastName: "Doe"), "Jane Doe", reason: '--err-t1--');
  });
```

`fullName(firstName: "John", lastName: "Smith")` debe devolver `"John Smith"`
```dart
  test('test2', () {
    expect(fullName(firstName: "John", lastName: "Smith"), "John Smith", reason: '--err-t2--');
  });
```

`fullName(firstName: "A", lastName: "B")` debe devolver `"A B"`
```dart
  test('test3', () {
    expect(fullName(firstName: "A", lastName: "B"), "A B", reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String fullName({required String firstName, required String lastName}) {
  return "$firstName $lastName";
}
```
