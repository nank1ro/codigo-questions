---
language: dart
exerciseType: 1
---

# --description--

Para comprobar si un valor está en un Set usa `.contains(value)`, que devuelve un `bool`. La propiedad `.isEmpty` es `true` cuando el Set no tiene elementos, y `.isNotEmpty` cuando tiene al menos uno:

```dart
var seen = {'x', 'y'};
print(seen.contains('x')); // true
print(seen.contains('z')); // false
print(seen.isEmpty);       // false
print(seen.isNotEmpty);    // true
```

# --instructions--

Dado el Set `seen`, guarda en un `bool` llamado `hasX` si contiene `'x'`, en un `bool` llamado `hasZ` si contiene `'z'`, y en un `bool` llamado `empty` si está vacío.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var seen = {'x', 'y'};
```

# --after-seed--

```dart
  return [hasX, hasZ, empty];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasX` debe ser `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasZ` debe ser `false`

```dart
  test('test2', () {
    expect(result()[1], false, reason: '--err-t2--');
  });
```

`empty` debe ser `false`

```dart
  test('test3', () {
    expect(result()[2], false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var seen = {'x', 'y'};
bool hasX = seen.contains('x');
bool hasZ = seen.contains('z');
bool empty = seen.isEmpty;
```
