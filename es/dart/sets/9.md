---
language: dart
exerciseType: 1
---

# --description--

Los Sets admiten las operaciones clásicas de conjuntos. Cada una devuelve un Set **nuevo** y deja los originales sin modificar:

- `a.union(b)` contiene los elementos que están en `a` **o** en `b`
- `a.intersection(b)` contiene los elementos que están en **ambos**, `a` y `b`
- `a.difference(b)` contiene los elementos de `a` que **no** están en `b`

```dart
var a = {1, 2, 3};
var b = {2, 3, 4};
print(a.union(b));        // {1, 2, 3, 4}
print(a.intersection(b)); // {2, 3}
print(a.difference(b));   // {1}
```

# --instructions--

Dados los Sets `a` y `b`, crea un Set llamado `both` con su unión, un Set llamado `common` con su intersección, y un Set llamado `onlyA` con los elementos de `a` que no están en `b`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var a = {1, 2, 3, 4};
var b = {3, 4, 5};
```

# --after-seed--

```dart
  return [both, common, onlyA];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`both` debe ser `{1, 2, 3, 4, 5}`

```dart
  test('test1', () {
    expect(result()[0], {1, 2, 3, 4, 5}, reason: '--err-t1--');
  });
```

`common` debe ser `{3, 4}`

```dart
  test('test2', () {
    expect(result()[1], {3, 4}, reason: '--err-t2--');
  });
```

`onlyA` debe ser `{1, 2}`

```dart
  test('test3', () {
    expect(result()[2], {1, 2}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var a = {1, 2, 3, 4};
var b = {3, 4, 5};
var both = a.union(b);
var common = a.intersection(b);
var onlyA = a.difference(b);
```
