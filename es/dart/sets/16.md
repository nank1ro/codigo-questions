---
language: dart
exerciseType: 1
---

# --instructions--

Dada la lista `words`, construye un `Set<String>` llamado `duplicates` que contenga solo las palabras que aparecen **más de una vez** en la lista. Pista: `.add()` devuelve `false` cuando el valor ya está en el Set.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var words = ['sun', 'moon', 'sun', 'star', 'moon', 'sun'];
```

# --after-seed--

```dart
  return duplicates;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`duplicates` debe ser `{'sun', 'moon'}`

```dart
  test('test1', () {
    expect(result(), {'sun', 'moon'}, reason: '--err-t1--');
  });
```

`duplicates` debe ser un `Set`

```dart
  test('test2', () {
    expect(result(), isA<Set>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var words = ['sun', 'moon', 'sun', 'star', 'moon', 'sun'];
var seen = <String>{};
var duplicates = <String>{};
for (var word in words) {
  if (!seen.add(word)) {
    duplicates.add(word);
  }
}
```
