---
language: dart
exerciseType: 1
---

# --description--

Convertir una lista en un Set es la forma más fácil de **eliminar duplicados**: toda lista tiene un método `.toSet()` que devuelve un Set con sus elementos únicos, en el orden de su primera aparición. Un Set tiene un método `.toList()` que hace el camino inverso, así que encadenar ambos te da una lista sin duplicados:

```dart
var votes = ['a', 'b', 'a', 'c', 'b'];
Set<String> unique = votes.toSet();
print(unique); // {a, b, c}
List<String> cleaned = votes.toSet().toList();
print(cleaned); // [a, b, c]
```

# --instructions--

Dada la lista `votes`, crea una `List<String>` llamada `cleaned` que contenga sus elementos sin duplicados, en el orden de su primera aparición.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var votes = ['yes', 'no', 'yes', 'maybe', 'no', 'yes'];
```

# --after-seed--

```dart
  return cleaned;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`cleaned` debe ser `['yes', 'no', 'maybe']`

```dart
  test('test1', () {
    expect(result(), ['yes', 'no', 'maybe'], reason: '--err-t1--');
  });
```

`cleaned` debe ser una `List`

```dart
  test('test2', () {
    expect(result(), isA<List>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var votes = ['yes', 'no', 'yes', 'maybe', 'no', 'yes'];
List<String> cleaned = votes.toSet().toList();
```
