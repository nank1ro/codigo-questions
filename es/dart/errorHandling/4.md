---
language: dart
exerciseType: 1
---

# --description--

Un bloque `try` puede ir seguido de **varias** cláusulas, cada una recuperándose de un fallo distinto. Dart compara el objeto lanzado con ellas de arriba abajo y ejecuta la **primera** que coincide:

```dart
try {
  return names[int.parse(text)];
} on FormatException {
  return 'not a number';
} on RangeError {
  return 'out of range';
} catch (e) {
  return 'unknown problem';
}
```

Leer una lista con un índice que no existe lanza un **`RangeError`**, así que los dos fallos de esa misma línea obtienen respuestas distintas.

Como gana la primera coincidencia, el orden importa: una cláusula de un tipo general colocada encima de una más específica ganaría siempre, dejando la cláusula específica inalcanzable. Escribe primero las cláusulas específicas y, si quieres una red de seguridad, un `catch` a secas al final.

La cláusula `on RangeError` de arriba está aquí solo para mostrar cómo se ordenan varias cláusulas. `RangeError` señala un error en el código en lugar de una condición que el programa no pudo controlar, y un ejercicio posterior explica por qué semejante fallo debe prevenirse en lugar de capturarse.

# --instructions--

Escribe una función `lookup` que reciba una `List<int>` `scores` y un `String` `text`, y devuelva `'score '` seguido del elemento de `scores` en la posición que describe `text`.

Devuelve `'not a number'` cuando `text` no sea un número entero, `'out of range'` cuando la posición no exista y `'unknown problem'` para cualquier otra cosa.

Ejemplo:
```dart
print(lookup([10, 20, 30], '1')); // score 20
print(lookup([10, 20, 30], 'two')); // not a number
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`lookup([10, 20, 30], '1')` debería devolver `'score 20'`

```dart
  test('test1', () {
    expect(lookup([10, 20, 30], '1'), 'score 20', reason: '--err-t1--');
    expect(lookup([10, 20, 30], '0'), 'score 10', reason: '--err-t1--');
  });
```

`lookup([10, 20, 30], 'two')` debería devolver `'not a number'`

```dart
  test('test2', () {
    expect(lookup([10, 20, 30], 'two'), 'not a number', reason: '--err-t2--');
    expect(lookup([10, 20, 30], ''), 'not a number', reason: '--err-t2--');
  });
```

`lookup([10, 20, 30], '7')` debería devolver `'out of range'`

```dart
  test('test3', () {
    expect(lookup([10, 20, 30], '7'), 'out of range', reason: '--err-t3--');
    expect(lookup([10, 20, 30], '-1'), 'out of range', reason: '--err-t3--');
    expect(lookup(<int>[], '0'), 'out of range', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String lookup(List<int> scores, String text) {
  try {
    return 'score ${scores[int.parse(text)]}';
  } on FormatException {
    return 'not a number';
  } on RangeError {
    return 'out of range';
  } catch (e) {
    return 'unknown problem';
  }
}
```
