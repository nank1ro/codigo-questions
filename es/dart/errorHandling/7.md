---
language: dart
exerciseType: 1
---

# --description--

Tu propio código lanza igual que lo hace la biblioteca. `Exception('message')` construye una excepción sencilla que lleva una breve explicación, y `throw` la pone en camino:

```dart
if (amount > balance) {
  throw Exception('insufficient funds');
}
```

El mensaje no se pierde: `toString()` junta la palabra `Exception`, dos puntos y el mensaje, que es exactamente lo que imprime el informe de excepción no controlada.

```dart
print(Exception('insufficient funds')); // Exception: insufficient funds
```

Lanzar es mejor que devolver un valor inventado como `-1`: el llamador no puede olvidarse de mirarlo, y el motivo viaja con él.

# --instructions--

Escribe una función `withdraw` que reciba dos valores `int` `balance` y `amount` y devuelva el saldo restante.

Cuando `amount` sea mayor que `balance`, lanza en su lugar `Exception('insufficient funds')`.

Ejemplo:
```dart
print(withdraw(100, 30)); // 70
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`withdraw(100, 30)` debería devolver `70`

```dart
  test('test1', () {
    expect(withdraw(100, 30), 70, reason: '--err-t1--');
    expect(withdraw(50, 50), 0, reason: '--err-t1--');
  });
```

`withdraw(100, 300)` debería lanzar una `Exception`

```dart
  test('test2', () {
    expect(() => withdraw(100, 300), throwsA(isA<Exception>()), reason: '--err-t2--');
  });
```

La excepción lanzada debería imprimirse como `Exception: insufficient funds`

```dart
  test('test3', () {
    try {
      withdraw(0, 1);
      fail('--err-t3--');
    } catch (e) {
      expect(e.toString(), 'Exception: insufficient funds', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int withdraw(int balance, int amount) {
  if (amount > balance) {
    throw Exception('insufficient funds');
  }
  return balance - amount;
}
```
