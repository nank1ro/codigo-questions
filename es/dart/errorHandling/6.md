---
language: dart
exerciseType: 4
---

# --description--

Un tercer bloque puede ir después de los gestores. `finally` se ejecuta **en todos los casos**: después de que el bloque `try` termine con normalidad, después de que un gestor se recupere, y también cuando nada coincide y el fallo sigue viajando hacia fuera.

```dart
try {
  return 'parsed ${int.parse(text)}';
} on FormatException {
  return 'failed';
} finally {
  print('done');
}
```

Se ejecuta incluso antes de que un `return` entregue su valor, por eso el mensaje de arriba se imprime antes de que el llamador vea el resultado. Eso hace de `finally` el lugar para el trabajo que debe ocurrir de cualquier forma, como cerrar lo que abriste.

# --instructions--

Ordena los elementos para definir una función `attempt` que devuelva `'parsed '` seguido del número, o `'failed'` cuando el texto no es un número, e imprima `done` en un bloque `finally` en ambos casos. Define la función antes que `main`. El programa debe imprimir:

```
done
parsed 7
done
failed
```

# --answers--

```dart
String attempt(String text) {
```

```dart
  try {
    return 'parsed ${int.parse(text)}';
```

```dart
  } on FormatException {
    return 'failed';
```

```dart
  } finally {
    print('done');
  }
}
```

```dart
void main() {
```

```dart
  print(attempt('7'));
```

```dart
  print(attempt('x'));
```

```dart
}
```

# --solutions--

```dart
String attempt(String text) {
  try {
    return 'parsed ${int.parse(text)}';
  } on FormatException {
    return 'failed';
  } finally {
    print('done');
  }
}

void main() {
  print(attempt('7'));
  print(attempt('x'));
}
```

# --output--

done
parsed 7
done
failed
