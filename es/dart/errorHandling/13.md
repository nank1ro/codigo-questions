---
language: dart
exerciseType: 1
---

# --description--

`firstWhere` devuelve el primer elemento que cumple una condición. Cuando nada la cumple no hay elemento que devolver, así que lanza un `StateError`:

```dart
final words = ['a', 'fg'];
print(words.firstWhere((w) => w.length > 3)); // Bad state: No element
```

Como `int.tryParse`, la biblioteca ofrece una salida. El parámetro con nombre `orElse` recibe una función que produce el valor a usar cuando nada coincidió:

```dart
print(words.firstWhere((w) => w.length > 3, orElse: () => 'none')); // none
```

La elección es la misma que antes: `orElse` cuando "nada coincidió" es un desenlace ordinario, la llamada a secas cuando significaría que los datos están rotos y alguien debe enterarse.

# --instructions--

Escribe dos funciones que reciban una `List<String>` `words`.

`firstLong` devuelve la primera palabra de más de tres caracteres usando `firstWhere`, o `'none'` cuando no hay ninguna, con `orElse`.

`firstLongStrict` hace lo mismo con `firstWhere`, pero sin `orElse`, así que lanza un `StateError` cuando no hay ninguna.

Ejemplo:
```dart
print(firstLong(['a', 'bcde', 'fg'])); // bcde
print(firstLong(['a', 'fg'])); // none
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`firstLong(['a', 'bcde', 'fg'])` debería devolver `'bcde'`

```dart
  test('test1', () {
    expect(firstLong(['a', 'bcde', 'fg']), 'bcde', reason: '--err-t1--');
    expect(firstLong(['long', 'longer']), 'long', reason: '--err-t1--');
  });
```

`firstLong` debería devolver `'none'` cuando ninguna palabra tiene más de tres caracteres

```dart
  test('test2', () {
    expect(firstLong(['a', 'fg']), 'none', reason: '--err-t2--');
    expect(firstLong(<String>[]), 'none', reason: '--err-t2--');
    expect(firstLong(['abc']), 'none', reason: '--err-t2--');
  });
```

`firstLongStrict` debería devolver la palabra cuando hay una y lanzar un `StateError` en caso contrario

```dart
  test('test3', () {
    expect(firstLongStrict(['a', 'bcde']), 'bcde', reason: '--err-t3--');
    expect(() => firstLongStrict(['a', 'fg']), throwsStateError, reason: '--err-t3--');
    expect(() => firstLongStrict(<String>[]), throwsStateError, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String firstLong(List<String> words) {
  return words.firstWhere((w) => w.length > 3, orElse: () => 'none');
}

String firstLongStrict(List<String> words) {
  return words.firstWhere((w) => w.length > 3);
}
```
