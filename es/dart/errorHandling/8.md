---
language: dart
exerciseType: 1
---

# --description--

A veces un gestor no es el lugar adecuado para recuperarse: solo quieres *notar* el fallo y dejar que continúe hacia el llamador que de verdad puede ocuparse de él. La palabra clave `rethrow` hace eso, dentro de un bloque `catch` o `on ... catch`:

```dart
try {
  return int.parse(text);
} on FormatException {
  log.add('bad input: $text');
  rethrow;
}
```

`rethrow` envía hacia fuera el **mismo** objeto, así que el llamador ve el fallo original. Escribir `throw e` en su lugar también funcionaría, pero reinicia el viaje y pierde dónde ocurrió el fallo por primera vez.

Un bloque `finally` en la misma sentencia sigue ejecutándose, incluso en el camino hacia fuera.

# --instructions--

Escribe una función `parseOrLog` que reciba un `String` `text` y una `List<String>` `log`, y devuelva `int.parse(text)`.

Cuando el texto no sea un número entero, añade a `log` la cadena `'bad input: '` seguida del texto, y después relanza la `FormatException` para que el llamador siga viéndola.

Ejemplo:
```dart
final log = <String>[];
print(parseOrLog('42', log)); // 42
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parseOrLog('42', log)` debería devolver `42` y dejar el log vacío

```dart
  test('test1', () {
    final log = <String>[];
    expect(parseOrLog('42', log), 42, reason: '--err-t1--');
    expect(log, isEmpty, reason: '--err-t1--');
  });
```

`parseOrLog('oops', log)` debería lanzar una `FormatException`

```dart
  test('test2', () {
    expect(() => parseOrLog('oops', <String>[]), throwsFormatException, reason: '--err-t2--');
  });
```

El log debería recoger una línea por cada fallo, y los valores buenos no deberían quedar registrados

```dart
  test('test3', () {
    final log = <String>[];
    expect(() => parseOrLog('oops', log), throwsFormatException, reason: '--err-t3--');
    expect(log, ['bad input: oops'], reason: '--err-t3--');
    expect(parseOrLog('7', log), 7, reason: '--err-t3--');
    expect(() => parseOrLog('x', log), throwsFormatException, reason: '--err-t3--');
    expect(log, ['bad input: oops', 'bad input: x'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int parseOrLog(String text, List<String> log) {
  try {
    return int.parse(text);
  } on FormatException {
    log.add('bad input: $text');
    rethrow;
  }
}
```
