---
language: dart
exerciseType: 1
---

# --description--

La última pieza es lanzar un `Error` a propósito. Una función que documenta qué acepta debe rechazar cualquier otra cosa de forma sonora, y `ArgumentError` es el objeto para ello:

```dart
int setVolume(int level) {
  if (level < 0 || level > 100) {
    throw ArgumentError('level must be between 0 and 100');
  }
  return level;
}
```

El mensaje es accesible como `e.message`, y `toString()` imprime `Invalid argument(s): ` seguido de él.

Esto no contradice la regla de antes. Lanzar un `ArgumentError` es correcto, capturarlo no lo es: le dice al autor del *llamador* que la llamada en sí está mal, y la solución es una comprobación antes de la llamada, no un gestor alrededor de ella.

# --instructions--

Escribe una función `checkAge` que reciba un `int` `age` y lo devuelva, pero que lance `ArgumentError('age must be between 0 and 120')` cuando la edad sea menor que `0` o mayor que `120`.

Ejemplo:
```dart
print(checkAge(30)); // 30
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`checkAge(30)` debería devolver `30`, y los dos extremos del rango deberían aceptarse

```dart
  test('test1', () {
    expect(checkAge(30), 30, reason: '--err-t1--');
    expect(checkAge(0), 0, reason: '--err-t1--');
    expect(checkAge(120), 120, reason: '--err-t1--');
  });
```

`checkAge(-1)` y `checkAge(121)` deberían lanzar un `ArgumentError`

```dart
  test('test2', () {
    expect(() => checkAge(-1), throwsArgumentError, reason: '--err-t2--');
    expect(() => checkAge(121), throwsArgumentError, reason: '--err-t2--');
  });
```

El error lanzado debería llevar el mensaje `'age must be between 0 and 120'`

```dart
  test('test3', () {
    try {
      checkAge(200);
      fail('--err-t3--');
    } on ArgumentError catch (e) {
      expect(e.message, 'age must be between 0 and 120', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int checkAge(int age) {
  if (age < 0 || age > 120) {
    throw ArgumentError('age must be between 0 and 120');
  }
  return age;
}
```
