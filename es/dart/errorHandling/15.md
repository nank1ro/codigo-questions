---
language: dart
exerciseType: 1
---

# --description--

Dónde se coloca el bloque `try` decide cuánto trabajo destruye un solo fallo. Alrededor de un bucle, el primer elemento defectuoso termina el lote entero; **dentro** del bucle, solo ese elemento se pierde y el resto sigue procesándose:

```dart
for (final text in texts) {
  try {
    total += int.parse(text);
  } on FormatException {
    continue;
  }
}
```

Esta es la forma cotidiana de importar un archivo, leer una lista de ajustes o gestionar una cola de mensajes: una fila dañada no debería desechar las buenas. La regla sigue siendo la misma de antes — mantén el bloque `try` alrededor de la sentencia que puede fallar, y no más grande.

# --instructions--

Escribe una función `sumOf` que reciba una `List<String>` `texts` y devuelva la suma de las que describen un número entero, ignorando las demás.

Usa `int.parse` con un `try`/`catch` dentro del bucle, para que una entrada ilegible no detenga al resto.

Ejemplo:
```dart
print(sumOf(['1', 'x', '3'])); // 4
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumOf(['1', 'x', '3'])` debería devolver `4`

```dart
  test('test1', () {
    expect(sumOf(['1', 'x', '3']), 4, reason: '--err-t1--');
  });
```

`sumOf(['10', '20', '30'])` debería devolver `60`

```dart
  test('test2', () {
    expect(sumOf(['10', '20', '30']), 60, reason: '--err-t2--');
    expect(sumOf(['-5', '5']), 0, reason: '--err-t2--');
  });
```

Una lista sin ningún número legible, y una lista vacía, deben dar ambas `0`

```dart
  test('test3', () {
    expect(sumOf(['x', 'y', '']), 0, reason: '--err-t3--');
    expect(sumOf(<String>[]), 0, reason: '--err-t3--');
    expect(sumOf(['x', '7']), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sumOf(List<String> texts) {
  var total = 0;
  for (final text in texts) {
    try {
      total += int.parse(text);
    } on FormatException {
      continue;
    }
  }
  return total;
}
```
