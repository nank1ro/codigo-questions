---
language: dart
exerciseType: 1
---

# --instructions--

Declara dos variables `int` de nivel superior: `x` con el valor `8` e `y` con el valor `8`. Luego declara una variable `bool` de nivel superior llamada `areEqual` que almacena el resultado de comparar `x` e `y` con el operador `==`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`areEqual` debe ser `true` y de tipo `bool`

```dart
  test('test1', () {
    expect(areEqual, true, reason: '--err-t1--');
    expect(areEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 8;
int y = 8;
bool areEqual = x == y;

void main() {
  print(areEqual);
}
```
