---
language: dart
exerciseType: 1
---

# --instructions--

Declara tres variables `int` de nivel superior: `number` con el valor `42`, `min` con el valor `1` y `max` con el valor `100`. Luego declara una variable `bool` de nivel superior llamada `inRange` que es `true` cuando `number` es mayor que o igual a `min` Y menor que o igual a `max`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`inRange` debe ser `true` y de tipo `bool`

```dart
  test('test1', () {
    expect(inRange, true, reason: '--err-t1--');
    expect(inRange, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int number = 42;
int min = 1;
int max = 100;
bool inRange = number >= min && number <= max;

void main() {
  print(inRange);
}
```
