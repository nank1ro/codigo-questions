---
language: dart
exerciseType: 1
---

# --instructions--

Declara dos variables `int` de nivel superior: `high` con el valor `20` y `low` con el valor `12`. Luego declara una variable `bool` de nivel superior llamada `isHigher` que almacene el resultado de comprobar si `high` es mayor que `low` usando el operador `>`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHigher` debe ser `true` y de tipo `bool`

```dart
  test('test1', () {
    expect(isHigher, true, reason: '--err-t1--');
    expect(isHigher, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int high = 20;
int low = 12;
bool isHigher = high > low;

void main() {
  print(isHigher);
}
```
