---
language: dart
exerciseType: 1
---

# --instructions--

El **operador NOT** `!` invierte un valor booleano: `!true` se convierte en `false` y `!false` se convierte en `true`.

Declara una variable `bool` de nivel superior llamada `isOffline` con el valor `false`. Luego declara una variable `bool` de nivel superior llamada `isOnline` que almacena la negación de `isOffline` usando el operador `!`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isOnline` debe ser `true` y de tipo `bool`

```dart
  test('test1', () {
    expect(isOnline, true, reason: '--err-t1--');
    expect(isOnline, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isOffline = false;
bool isOnline = !isOffline;

void main() {
  print(isOnline);
}
```
