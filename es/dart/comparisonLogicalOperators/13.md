---
language: dart
exerciseType: 1
---

# --instructions--

Declara dos variables `int` de nivel superior: `price` con el valor `30` y `budget` con el valor `50`. Luego declara una variable `bool` de nivel superior llamada `canAfford` que almacene el resultado de comprobar si `price` es menor que `budget` usando el operador `<`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canAfford` debe ser `true` y de tipo `bool`

```dart
  test('test1', () {
    expect(canAfford, true, reason: '--err-t1--');
    expect(canAfford, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int price = 30;
int budget = 50;
bool canAfford = price < budget;

void main() {
  print(canAfford);
}
```
