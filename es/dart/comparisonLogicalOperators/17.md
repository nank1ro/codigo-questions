---
language: dart
exerciseType: 1
---

# --instructions--

Declara una variable `int` de nivel superior llamada `age` con el valor `35`. Luego declara una variable `bool` de nivel superior llamada `isWorkingAge` que es `true` cuando `age` es mayor que o igual a `18` Y menor que o igual a `65`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isWorkingAge` debe ser `true` y de tipo `bool`

```dart
  test('test1', () {
    expect(isWorkingAge, true, reason: '--err-t1--');
    expect(isWorkingAge, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int age = 35;
bool isWorkingAge = age >= 18 && age <= 65;

void main() {
  print(isWorkingAge);
}
```
