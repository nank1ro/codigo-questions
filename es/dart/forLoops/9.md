---
language: dart
exerciseType: 1
---

# --instructions--
Escribe una función que devuelva la suma de todos los enteros desde 1 hasta `n` (inclusive) usando un bucle `for`.

# --before-seed--
```dart
int sumTo(int n) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
La suma de 1 a 5 es 15
```dart
    test('sum 1..5', () {
      expect(sumTo(5), 15, reason: '--err-t1--');
    });
```

La suma de 1 a 1 es 1
```dart
    test('sum 1..1', () {
      expect(sumTo(1), 1, reason: '--err-t2--');
    });
```

La suma de 1 a 10 es 55
```dart
    test('sum 1..10', () {
      expect(sumTo(10), 55, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = 1; i <= n; i++) {
    result += i;
  }
```
