---
language: dart
exerciseType: 1
---

# --instructions--
Escribe una función que reciba una `List<int>` y devuelva una nueva lista donde cada elemento esté multiplicado por 2.

# --before-seed--
```dart
List<int> doubleElements(List<int> items) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Duplica cada elemento
```dart
    test('doubles elements', () {
      expect(doubleElements([1, 2, 3]), [2, 4, 6], reason: '--err-t1--');
    });
```

Una lista vacía devuelve una lista vacía
```dart
    test('empty list', () {
      expect(doubleElements([]), [], reason: '--err-t2--');
    });
```

Lista con un único elemento
```dart
    test('single element', () {
      expect(doubleElements([5]), [10], reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  List<int> result = [];
  for (var item in items) {
    result.add(item * 2);
  }
```
