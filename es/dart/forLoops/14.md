---
language: dart
exerciseType: 1
---

# --instructions--
Escribe una función que devuelva una lista con solo los números impares de una `List<int>`. Usa un bucle `for` con `continue` para saltar los números pares.

# --before-seed--
```dart
List<int> filterOdds(List<int> numbers) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Filtra los números pares
```dart
    test('filters evens', () {
      expect(filterOdds([1, 2, 3, 4, 5]), [1, 3, 5], reason: '--err-t1--');
    });
```

Todos los números pares devuelven una lista vacía
```dart
    test('all even', () {
      expect(filterOdds([2, 4, 6]), [], reason: '--err-t2--');
    });
```

Todos los números impares devuelven los mismos valores
```dart
    test('all odd', () {
      expect(filterOdds([1, 3, 5]), [1, 3, 5], reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  List<int> result = [];
  for (int i = 0; i < numbers.length; i++) {
    if (numbers[i] % 2 == 0) continue;
    result.add(numbers[i]);
  }
```
