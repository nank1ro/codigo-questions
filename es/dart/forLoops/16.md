---
language: dart
exerciseType: 1
---

# --instructions--
Escribe una función que calcule el factorial de un entero no negativo `n` usando un bucle `for`. El factorial de 0 es 1.

# --before-seed--
```dart
int factorial(int n) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
El factorial de 5 es 120
```dart
    test('5! = 120', () {
      expect(factorial(5), 120, reason: '--err-t1--');
    });
```

El factorial de 0 es 1
```dart
    test('0! = 1', () {
      expect(factorial(0), 1, reason: '--err-t2--');
    });
```

El factorial de 1 es 1
```dart
    test('1! = 1', () {
      expect(factorial(1), 1, reason: '--err-t3--');
    });
```

El factorial de 6 es 720
```dart
    test('6! = 720', () {
      expect(factorial(6), 720, reason: '--err-t4--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 1;
  for (int i = 2; i <= n; i++) {
    result *= i;
  }
```
