---
language: dart
exerciseType: 1
---

# --instructions--
Escribe una función que devuelva el valor en la posición (`row`, `col`) de una tabla de multiplicar. Usa bucles `for` anidados donde el bucle externo itera sobre las filas y el interno sobre las columnas (ambos con índice 1). Devuelve el producto en la posición indicada.

# --before-seed--
```dart
int multiplicationCell(int row, int col) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Fila 3, columna 4 es igual a 12
```dart
    test('3x4 = 12', () {
      expect(multiplicationCell(3, 4), 12, reason: '--err-t1--');
    });
```

Fila 1, columna 1 es igual a 1
```dart
    test('1x1 = 1', () {
      expect(multiplicationCell(1, 1), 1, reason: '--err-t2--');
    });
```

Fila 7, columna 8 es igual a 56
```dart
    test('7x8 = 56', () {
      expect(multiplicationCell(7, 8), 56, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int r = 1; r <= row; r++) {
    for (int c = 1; c <= col; c++) {
      if (r == row && c == col) {
        result = r * c;
      }
    }
  }
```
