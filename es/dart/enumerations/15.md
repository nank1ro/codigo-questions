---
language: dart
exerciseType: 1
---

# --description--

Los valores de un enum son excelentes **claves de mapa**: son únicos, fáciles de comparar, y el compilador comprueba que solo uses valores reales. Declara el mapa con el enum como tipo de clave y busca los valores con `[]`:

```dart
enum Direction { north, south, east, west }

Map<Direction, String> arrows = {
  Direction.north: '^',
  Direction.south: 'v',
  Direction.east: '>',
  Direction.west: '<',
};

print(arrows[Direction.east]); // >
```

Como con cualquier mapa, una búsqueda devuelve `null` cuando falta la clave, así que usa `??` para proporcionar un valor alternativo.

# --instructions--

Dado `enum Size { small, medium, large }`, declara un `Map<Size, int>` de nivel superior llamado `prices` donde `small` cuesta `2`, `medium` cuesta `3` y `large` cuesta `4`. Luego escribe una función `totalPrice(List<Size> order)` que devuelva la suma de los precios de cada tamaño en `order`.

# --seed--

```dart
enum Size { small, medium, large }
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`prices[Size.medium]` debe ser `3`

```dart
  test('test1', () {
    expect(prices[Size.medium], 3, reason: '--err-t1--');
  });
```

`totalPrice([Size.small, Size.large])` debe ser `6`

```dart
  test('test2', () {
    expect(totalPrice([Size.small, Size.large]), 6, reason: '--err-t2--');
  });
```

`totalPrice([Size.medium, Size.medium, Size.small])` debe ser `8`

```dart
  test('test3', () {
    expect(totalPrice([Size.medium, Size.medium, Size.small]), 8, reason: '--err-t3--');
  });
```

`totalPrice([])` debe ser `0`

```dart
  test('test4', () {
    expect(totalPrice([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Size { small, medium, large }

Map<Size, int> prices = {
  Size.small: 2,
  Size.medium: 3,
  Size.large: 4,
};

int totalPrice(List<Size> order) {
  int total = 0;
  for (var size in order) {
    total += prices[size] ?? 0;
  }
  return total;
}
```
