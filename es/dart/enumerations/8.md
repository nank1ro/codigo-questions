---
language: dart
exerciseType: 2
---

# --description--

Desde Dart 3, un `switch` también puede usarse como una **expresión** que produce un valor. Cada caso se escribe como `pattern => value` y los casos se separan por comas, sin la palabra clave `case` y sin `break`:

```dart
enum Direction { north, south, east, west }

String arrow(Direction direction) => switch (direction) {
      Direction.north => '^',
      Direction.south => 'v',
      Direction.east => '>',
      Direction.west => '<',
    };
```

Al igual que la forma de sentencia, una expresión switch sobre un enum debe ser exhaustiva.

# --instructions--

Completa la expresión switch para que `label` devuelva la primera letra de cada tamaño, y luego imprime la etiqueta de `Size.large`.

# --seed--

```dart
enum Size { small, medium, large }

String label(Size size) => [/] (size) {
      Size.small [/] 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --answers--

- switch
- =>
- case
- when
- ->
- :

# --solutions--

```dart
enum Size { small, medium, large }

String label(Size size) => switch (size) {
      Size.small => 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --output--

L
