---
language: dart
exerciseType: 1
---

# --description--

Un enum puede **implementar una interfaz** con la palabra clave `implements`. El enum promete entonces proporcionar cada miembro que declara la interfaz, y sus valores pueden usarse en cualquier lugar donde se espere ese tipo de interfaz:

```dart
abstract class Describable {
  String describe();
}

enum Animal implements Describable {
  dog,
  cat;

  @override
  String describe() => 'I am a $name';
}

Describable pet = Animal.cat;
print(pet.describe()); // I am a cat
```

Un getter declarado en la interfaz puede implementarse tanto con un getter como con un campo `final` del mismo nombre.

# --instructions--

Declara un enum `Ticket` que implemente `Priced`, con un campo `final int` llamado `price` y los valores `child(5)`, `adult(12)` y `senior(8)`, en este orden. Luego escribe una función `total(List<Priced> items)` que devuelva la suma de los precios de todos los elementos.

# --seed--

```dart
abstract class Priced {
  int get price;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Ticket.adult.price` debe ser `12`

```dart
  test('test1', () {
    expect(Ticket.adult.price, 12, reason: '--err-t1--');
  });
```

Un `Ticket` puede usarse como un `Priced`

```dart
  test('test2', () {
    Priced item = Ticket.senior;
    expect(item.price, 8, reason: '--err-t2--');
  });
```

`total([Ticket.child, Ticket.adult, Ticket.senior])` debe ser `25`

```dart
  test('test3', () {
    expect(total([Ticket.child, Ticket.adult, Ticket.senior]), 25, reason: '--err-t3--');
  });
```

`total([])` debe ser `0`

```dart
  test('test4', () {
    expect(total([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
abstract class Priced {
  int get price;
}

enum Ticket implements Priced {
  child(5),
  adult(12),
  senior(8);

  @override
  final int price;

  const Ticket(this.price);
}

int total(List<Priced> items) {
  int sum = 0;
  for (var item in items) {
    sum += item.price;
  }
  return sum;
}
```
