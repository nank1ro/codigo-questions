---
language: dart
exerciseType: 1
---

# --description--

Como `values` es una lista, puedes combinarla con los métodos de lista que ya conoces. Por ejemplo, `.map()` junto con `.name` convierte los valores en una lista de strings:

```dart
enum Direction { north, south, east, west }

List<String> names = Direction.values.map((d) => d.name).toList();
print(names); // [north, south, east, west]
```

# --instructions--

Declara un enum `Weekday` con los valores `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday` y `sunday`, en este orden. Luego escribe una función `dayNames()` que devuelva una `List<String>` con el nombre de cada día de la semana, en el orden de declaración.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Weekday.values.length` debe ser `7`

```dart
  test('test1', () {
    expect(Weekday.values.length, 7, reason: '--err-t1--');
  });
```

`Weekday.values[2]` debe ser `Weekday.wednesday`

```dart
  test('test2', () {
    expect(Weekday.values[2], Weekday.wednesday, reason: '--err-t2--');
  });
```

`dayNames()` debe devolver los siete nombres en orden

```dart
  test('test3', () {
    expect(dayNames(), ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Weekday { monday, tuesday, wednesday, thursday, friday, saturday, sunday }

List<String> dayNames() {
  return Weekday.values.map((day) => day.name).toList();
}
```
