---
language: dart
exerciseType: 1
---

# --description--

Desde Dart 2.17, un enum puede declarar **campos** y un **constructor**, igual que una clase. Esto se llama un *enum mejorado*. Cada valor pasa entonces sus propios argumentos al constructor:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);
}

print(Planet.mars.moons); // 2
```

Fíjate en las tres reglas: la lista de valores termina con un **punto y coma** `;`, los campos deben ser `final`, y el constructor debe ser `const`.

# --instructions--

Declara un enum `Coin` con un campo `final int` llamado `cents` y los valores `penny(1)`, `nickel(5)`, `dime(10)` y `quarter(25)`, en este orden.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Coin.penny.cents` debe ser `1` y `Coin.nickel.cents` debe ser `5`

```dart
  test('test1', () {
    expect(Coin.penny.cents, 1, reason: '--err-t1--');
    expect(Coin.nickel.cents, 5, reason: '--err-t2--');
  });
```

`Coin.dime.cents` debe ser `10`

```dart
  test('test2', () {
    expect(Coin.dime.cents, 10, reason: '--err-t3--');
  });
```

`Coin.quarter.cents` debe ser `25`

```dart
  test('test3', () {
    expect(Coin.quarter.cents, 25, reason: '--err-t4--');
  });
```

`Coin.values` debe tener 4 valores, con `penny` en primer lugar

```dart
  test('test4', () {
    expect(Coin.values.length, 4, reason: '--err-t5--');
    expect(Coin.values[0], Coin.penny, reason: '--err-t6--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Coin {
  penny(1),
  nickel(5),
  dime(10),
  quarter(25);

  final int cents;

  const Coin(this.cents);
}
```
