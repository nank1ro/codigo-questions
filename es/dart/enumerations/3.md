---
language: dart
exerciseType: 2
---

# --description--

Cada enum también tiene una lista constante llamada `values` que contiene todos sus valores en el orden de declaración. Puedes indexarla como cualquier lista, leer su `length` o recorrerla con `for-in`:

```dart
enum Direction { north, south, east, west }

print(Direction.values.length); // 4
print(Direction.values[1]);     // Direction.south

for (var direction in Direction.values) {
  print(direction.name);
}
```

# --instructions--

Completa el código para imprimir el nombre de `Planet.earth`, su índice y el número total de planetas.

# --seed--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.[/]);
  print(Planet.earth.[/]);
  print(Planet.[/].length);
}
```

# --answers--

- name
- index
- values
- ordinal
- position
- count

# --solutions--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.name);
  print(Planet.earth.index);
  print(Planet.values.length);
}
```

# --output--

earth
2
3
