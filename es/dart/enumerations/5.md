---
language: dart
exerciseType: 4
---

# --description--

Recorrer `values` es la forma habitual de procesar todos los valores de un enum. Dentro del bucle, el valor actual se comporta como cualquier otro objeto, así que puedes leer su `index` y su `name` y usarlos directamente en una interpolación de cadena:

```dart
enum Direction { north, south, east, west }

for (var direction in Direction.values) {
  print('${direction.index}: ${direction.name}');
}
```

# --instructions--

Ordena los elementos para declarar el enum `Level` e imprimir, para cada valor, su índice y su nombre. Declara el enum antes de `main`. El programa debe imprimir:

```
0: low
1: medium
2: high
```

# --answers--

```dart
enum Level { low, medium, high }
```

```dart
void main() {
```

```dart
  for (var level in Level.values) {
```

```dart
    print('${level.index}: ${level.name}');
```

```dart
  }
}
```

# --solutions--

```dart
enum Level { low, medium, high }
void main() {
  for (var level in Level.values) {
    print('${level.index}: ${level.name}');
  }
}
```

# --output--

0: low
1: medium
2: high
