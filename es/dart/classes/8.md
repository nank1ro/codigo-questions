---
language: dart
exerciseType: 1
---

# --instructions--

Crea una clase `Car` con dos propiedades `String`: `brand` y `model`. Agrega un constructor que establezca ambas usando la sintaxis abreviada.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Crear `Car('Toyota', 'Corolla')` debería establecer `brand` en `'Toyota'`

```dart
  test('test1', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.brand, 'Toyota', reason: '--err-t1--');
  });
```

Crear `Car('Toyota', 'Corolla')` debería establecer `model` en `'Corolla'`

```dart
  test('test2', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.model, 'Corolla', reason: '--err-t2--');
  });
```

Crear `Car('Honda', 'Civic')` debería establecer `brand` en `'Honda'`

```dart
  test('test3', () {
    final car = Car('Honda', 'Civic');
    expect(car.brand, 'Honda', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Car {
  String brand;
  String model;
  Car(this.brand, this.model);
}
```
