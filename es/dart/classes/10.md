---
language: dart
exerciseType: 1
---

# --instructions--

Crea una clase `Counter` con un campo `int` `count` inicializado en `0`. Agrega un método `increment()` que aumenta `count` en `1`, y un método `reset()` que restablece `count` a `0`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Llamar a `increment()` tres veces debería hacer que `count` sea igual a `3`

```dart
  test('test1', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.increment();
    expect(c.count, 3, reason: '--err-t1--');
  });
```

Llamar a `reset()` debería restablecer `count` a `0`

```dart
  test('test2', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.reset();
    expect(c.count, 0, reason: '--err-t2--');
  });
```

El `count` inicial debería ser `0`

```dart
  test('test3', () {
    final c = Counter();
    expect(c.count, 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Counter {
  int count = 0;

  void increment() {
    count++;
  }

  void reset() {
    count = 0;
  }
}
```
