---
language: dart
exerciseType: 2
---

# --description--

Una **clase** es una plantilla para crear objetos. Agrupa datos relacionados (campos) y comportamiento (métodos) juntos.

En Dart, declaras una clase con la palabra clave `class` seguida del nombre de la clase (por convención, PascalCase):

```dart
class Car {
  String brand = 'Toyota';
}
```

Creas una **instancia** (objeto) de la clase así:

```dart
final car = Car();
print(car.brand); // Toyota
```

# --instructions--

Completa el código para declarar una clase llamada `Person` con un campo `String` `name` inicializado en `'Alice'`, luego crea una instancia e imprime su `name`.

# --seed--

```dart
[/] Person {
  String [/] = 'Alice';
}

void main() {
  final p = [/]();
  print(p.[/]);
}
```

# --answers--

- class
- name
- Person
- brand
- age
- void
- p

# --solutions--

```dart
class Person {
  String name = 'Alice';
}

void main() {
  final p = Person();
  print(p.name);
}
```

# --output--

Alice
