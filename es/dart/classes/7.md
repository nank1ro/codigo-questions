---
language: dart
exerciseType: 2
---

# --description--

Dart admite **constructores con nombre**, que te permiten definir formas adicionales de crear un objeto. Los constructores con nombre se escriben como `ClassName.constructorName`:

```dart
class Point {
  double x;
  double y;

  Point(this.x, this.y);

  Point.origin()
      : x = 0,
        y = 0;
}
```

La parte después de los dos puntos es la **lista de inicialización**: asigna los campos antes de que se ejecute el cuerpo del constructor. Luego puedes crear un objeto en el origen con: `var p = Point.origin();`

# --instructions--

Completa el constructor con nombre `zero` de la clase `Counter` que establece `count` en `0`.

# --seed--

```dart
class Counter {
  int count;

  Counter(this.count);

  Counter.[/]() : count = [/];
}
```

# --answers--

- zero
- 0
- count
- init
- 1
- reset
- start

# --solutions--

```dart
class Counter {
  int count;

  Counter(this.count);

  Counter.zero() : count = 0;
}
```
