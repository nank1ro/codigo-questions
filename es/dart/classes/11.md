---
language: dart
exerciseType: 1
---

# --instructions--

Crea una clase `Rectangle` con campos `int` `width` y `height` y un constructor que los establezca. Agrega un getter `area` que devuelva `width * height`, y un getter `perimeter` que devuelva `2 * (width + height)`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Rectangle(4, 5).area` debería ser igual a `20`

```dart
  test('test1', () {
    final r = Rectangle(4, 5);
    expect(r.area, 20, reason: '--err-t1--');
  });
```

`Rectangle(4, 5).perimeter` debería ser igual a `18`

```dart
  test('test2', () {
    final r = Rectangle(4, 5);
    expect(r.perimeter, 18, reason: '--err-t2--');
  });
```

`Rectangle(3, 3).area` debería ser igual a `9`

```dart
  test('test3', () {
    final r = Rectangle(3, 3);
    expect(r.area, 9, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.height);

  int get area => width * height;
  int get perimeter => 2 * (width + height);
}
```
