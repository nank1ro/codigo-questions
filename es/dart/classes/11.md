---
language: dart
exerciseType: 2
---

# --description--

Cuando el constructor de una subclase necesita llamar al constructor de la superclase, usa la palabra clave `super` en la **lista de inicialización**:

```dart
class Vehicle {
  String brand;
  Vehicle(this.brand);
}

class Car extends Vehicle {
  int doors;
  Car(String brand, this.doors) : super(brand);
}
```

`super(brand)` reenvía el argumento `brand` al constructor de `Vehicle`.

# --instructions--

Completa el constructor de `Cat` para que llame al constructor de la superclase `Animal` con `name`.

# --seed--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Cat extends Animal {
  Cat(String name) : [/](name);
}
```

# --answers--

- super
- Animal
- parent
- base
- this

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Cat extends Animal {
  Cat(String name) : super(name);
}
```
