---
language: dart
exerciseType: 1
---

# --instructions--

Crea una clase `Point` con dos propiedades `int` `x` e `y`. Agrega un constructor que establezca ambas. También agrega un constructor nombrado `Point.origin()` que establezca tanto `x` como `y` en `0`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Point(3, 5)` debería establecer `x` en `3` e `y` en `5`

```dart
  test('test1', () {
    final p = Point(3, 5);
    expect(p.x, 3, reason: '--err-t1--');
    expect(p.y, 5, reason: '--err-t1--');
  });
```

`Point.origin()` debería establecer tanto `x` como `y` en `0`

```dart
  test('test2', () {
    final p = Point.origin();
    expect(p.x, 0, reason: '--err-t2--');
    expect(p.y, 0, reason: '--err-t2--');
  });
```

`Point(-1, 4)` debería establecer `x` en `-1`

```dart
  test('test3', () {
    final p = Point(-1, 4);
    expect(p.x, -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Point {
  int x;
  int y;
  Point(this.x, this.y);
  Point.origin() : x = 0, y = 0;
}
```
