---
language: dart
exerciseType: 2
---

# --description--

Un **setter** es un método especial que te permite asignar un valor mientras ejecuta lógica de validación. Se define con la palabra clave `set`:

```dart
class Temperature {
  double _celsius = 0;

  double get celsius => _celsius;

  set celsius(double value) {
    if (value < -273.15) throw ArgumentError('Too cold!');
    _celsius = value;
  }
}
```

El nombre del campo suele llevar el prefijo `_` para marcarlo como privado.

# --instructions--

Completa el setter `age` en la clase `Person` que asigna `value` a `_age` solo cuando `value >= 0`.

# --seed--

```dart
class Person {
  int _age = 0;

  int get age => _age;

  [/] age(int value) {
    if (value >= 0) _age = [/];
  }
}
```

# --answers--

- set
- value
- get
- _age
- age
- 0

# --solutions--

```dart
class Person {
  int _age = 0;

  int get age => _age;

  set age(int value) {
    if (value >= 0) _age = value;
  }
}
```
