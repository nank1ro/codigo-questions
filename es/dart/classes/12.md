---
language: dart
exerciseType: 1
---

# --description--

La **sobrescritura de métodos** permite que una subclase proporcione su propia implementación de un método que ya existe en la superclase. Usa la anotación `@override`:

```dart
class Shape {
  double area() => 0;
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() => 3.14159 * radius * radius;
}
```

La anotación `@override` le indica a Dart (y a otros desarrolladores) que estás reemplazando intencionalmente el método de la superclase.

# --instructions--

Crea una clase `Animal` con un método `sound()` que devuelva el `String` `'...'`. Luego crea una subclase `Dog` que sobrescriba `sound()` para devolver `'Woof'`. Añade otra subclase `Cat` que sobrescriba `sound()` para devolver `'Meow'`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Animal.sound()` devuelve `'...'`

```dart
    test('animal sound', () {
      expect(Animal().sound(), '...', reason: '--err-t1--');
    });
```

`Dog.sound()` devuelve `'Woof'`

```dart
    test('dog sound', () {
      Animal dog = Dog();
      expect(dog.sound(), 'Woof', reason: '--err-t2--');
    });
```

`Cat.sound()` devuelve `'Meow'`

```dart
    test('cat sound', () {
      Animal cat = Cat();
      expect(cat.sound(), 'Meow', reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String sound() => '...';
}

class Dog extends Animal {
  @override
  String sound() => 'Woof';
}

class Cat extends Animal {
  @override
  String sound() => 'Meow';
}
```
