---
language: dart
exerciseType: 2
---

# --description--

Nullability also applies to the **elements** of a collection. A `List<int>` never contains `null`, while a `List<int?>` may:

```dart
List<int?> scores = [7, null, 9];
```

Note the difference with `List<int>?`, which is a list that may itself be missing but, when present, holds only real numbers.

To get rid of the `null` elements, `nonNulls` returns an `Iterable` with only the present values, typed without `?`:

```dart
var present = scores.nonNulls.toList(); // List<int>
print(present); // [7, 9]
```

`whereType<int>()` does the same and also works when the list mixes several types.

# --instructions--

Complete the code to declare a list of nullable `int` values and then keep only the non-null ones with `nonNulls`. The program must print `[7, 9]` and then `2`.

# --seed--

```dart
void main() {
  List<[/]> scores = [7, null, 9, null];
  var present = scores.[/].toList();
  print(present);
  print(present.length);
}
```

# --answers--

- int?
- nonNulls
- int
- notNull
- nonNull
- compact

# --solutions--

```dart
void main() {
  List<int?> scores = [7, null, 9, null];
  var present = scores.nonNulls.toList();
  print(present);
  print(present.length);
}
```

# --output--

[7, 9]
2
