---
language: dart
exerciseType: 1
---

# --description--

Una variable nullable declarada **sin valor** empieza siendo `null`, por lo que se puede omitir `= null`:

```dart
int? age;
print(age); // null
```

Una variable no nula no tiene ese valor por defecto: Dart se niega a compilar cualquier código que la lea antes de que se le haya asignado un valor.

```dart
int count;
print(count); // error: 'count' must be assigned before it can be used
```

# --instructions--

Declara una variable `email` de tipo `String` nullable sin valor, y una variable `username` de tipo `String` con el valor `'ada'`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart

```

# --after-seed--

```dart
  return [email, username];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`email` debe ser `null`

```dart
  test('test1', () {
    expect(result()[0], isNull, reason: '--err-t1--');
  });
```

`username` debe ser `'ada'`

```dart
  test('test2', () {
    expect(result()[1], 'ada', reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String? email;
String username = 'ada';
```
