---
language: dart
exerciseType: 4
---

# --description--

La nulabilidad determina cómo declaras los **parámetros nombrados**. Un parámetro nombrado con un tipo nullable es opcional: cuando quien lo llama lo omite, simplemente es `null`.

```dart
String label({String? title}) => title ?? 'untitled';

print(label());               // untitled
print(label(title: 'Notes')); // Notes
```

Un parámetro nombrado con un tipo no nullable y sin valor por defecto no tendría ningún valor si se omitiera, así que Dart te exige marcarlo `required`; quien llama debe entonces pasarlo siempre:

```dart
String label({required String name, String? title}) { ... }

label(name: 'Ada');               // ok
label(name: 'Ada', title: 'Dr.'); // ok
label(title: 'Dr.');              // error: the named parameter 'name' is required
```

# --instructions--

Ordena los elementos para definir una función `label` con un parámetro nombrado requerido `name` y un parámetro nombrado opcional y nullable `title`, devolviendo `name` solo cuando `title` es `null` y `'$title $name'` en caso contrario. Define la función primero, y luego `main`. El programa debe imprimir:

```
Ada
Countess Lovelace
```

# --answers--

```dart
String label({required String name, String? title}) {
```

```dart
  if (title == null) return name;
```

```dart
  return '$title $name';
```

```dart
}
```

```dart
void main() {
```

```dart
  print(label(name: 'Ada'));
```

```dart
  print(label(name: 'Lovelace', title: 'Countess'));
```

```dart
}
```

# --solutions--

```dart
String label({required String name, String? title}) {
  if (title == null) return name;
  return '$title $name';
}

void main() {
  print(label(name: 'Ada'));
  print(label(name: 'Lovelace', title: 'Countess'));
}
```

# --output--

Ada
Countess Lovelace
