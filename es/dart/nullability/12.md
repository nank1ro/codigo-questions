---
language: dart
exerciseType: 4
---

# --description--

Nullability shapes how you declare **named parameters**. A named parameter with a nullable type is optional: when the caller omits it, it is simply `null`.

```dart
String label({String? title}) => title ?? 'untitled';

print(label());               // untitled
print(label(title: 'Notes')); // Notes
```

A named parameter with a non-nullable type and no default value would have no value when omitted, so Dart requires you to mark it `required`; the caller must then always pass it:

```dart
String label({required String name, String? title}) { ... }

label(name: 'Ada');               // ok
label(name: 'Ada', title: 'Dr.'); // ok
label(title: 'Dr.');              // error: the named parameter 'name' is required
```

# --instructions--

Sort the items to define a function `label` with a required named parameter `name` and an optional nullable named parameter `title`, returning `name` alone when `title` is `null` and `'$title $name'` otherwise. Define the function first, then `main`. The program must print:

```
Ada
Countess Lovelace
```

# --answers--

```dart
String label({required String name, String? title}) {
```

```dart
  if (title == null) return name;
```

```dart
  return '$title $name';
```

```dart
}
```

```dart
void main() {
```

```dart
  print(label(name: 'Ada'));
```

```dart
  print(label(name: 'Lovelace', title: 'Countess'));
```

```dart
}
```

# --solutions--

```dart
String label({required String name, String? title}) {
  if (title == null) return name;
  return '$title $name';
}

void main() {
  print(label(name: 'Ada'));
  print(label(name: 'Lovelace', title: 'Countess'));
}
```

# --output--

Ada
Countess Lovelace
