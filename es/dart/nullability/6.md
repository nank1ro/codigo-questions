---
language: dart
exerciseType: 2
---

# --description--

El operador de **asignación if-null** `??=` asigna un valor a una variable **solo si** esa variable es actualmente `null`; en caso contrario la deja intacta:

```dart
int? retries;
retries ??= 3;
print(retries); // 3

retries ??= 10;
print(retries); // 3, it already had a value
```

También funciona con las entradas de un mapa, que son nullable porque una clave puede faltar:

```dart
var stock = {'apple': 4};
stock['pear'] ??= 1;  // added
stock['apple'] ??= 9; // ignored
print(stock); // {apple: 4, pear: 1}
```

# --instructions--

Completa el código de modo que se mantengan el tema guardado y el tamaño de fuente existente, mientras se añade el margen que falta. El programa debe imprimir `dark` y luego `{font: 12, margin: 8}`.

# --seed--

```dart
String? savedTheme() => 'dark';

void main() {
  String? theme = savedTheme();
  var sizes = {'font': 12};
  theme [/] 'light';
  sizes['font'] [/] 14;
  sizes['margin'] ??= 8;
  print(theme);
  print(sizes);
}
```

# --answers--

- ??=
- ??=
- =
- ?=
- =??
- :=

# --solutions--

```dart
String? savedTheme() => 'dark';

void main() {
  String? theme = savedTheme();
  var sizes = {'font': 12};
  theme ??= 'light';
  sizes['font'] ??= 14;
  sizes['margin'] ??= 8;
  print(theme);
  print(sizes);
}
```

# --output--

dark
{font: 12, margin: 8}
