---
language: dart
exerciseType: 1
---

# --description--

A veces **tú** sabes que un valor nullable no es `null` en un cierto punto, aunque el compilador no pueda saberlo. El operador de **aserción nula** (*null assertion*) `!` convierte un `String?` en un `String` al prometer que el valor está presente:

```dart
String? text = 'Dart';
String sure = text!;
print(sure.length); // 4
```

Ten cuidado: `!` mueve la comprobación del tiempo de compilación al tiempo de ejecución. Si el valor **es** `null`, el programa lanza un error y se detiene:

```dart
String? text;
print(text!.length); // Null check operator used on a null value
```

Usa `!` con moderación, y solo cuando un `null` allí sería un error de todos modos.

# --instructions--

Escribe una función `shout` que reciba un parámetro `text` de tipo `String` nullable y lo devuelva en mayúsculas seguido de `!`. Usa el operador `!`, de modo que la función lance un error cuando `text` sea `null`.

Ejemplo:
```dart
print(shout('hello')); // HELLO!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shout('hello')` debe ser `'HELLO!'`

```dart
  test('test1', () {
    expect(shout('hello'), 'HELLO!', reason: '--err-t1--');
  });
```

`shout('Dart')` debe ser `'DART!'`

```dart
  test('test2', () {
    expect(shout('Dart'), 'DART!', reason: '--err-t2--');
  });
```

`shout(null)` debe lanzar un error

```dart
  test('test3', () {
    expect(() => shout(null), throwsA(anything), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String shout(String? text) {
  return '${text!.toUpperCase()}!';
}
```
