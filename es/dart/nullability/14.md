---
language: dart
exerciseType: 1
---

# --description--

Muchas funciones de biblioteca usan `null` para indicar que algo **no se pudo hacer**. Convertir una cadena en un número es el ejemplo clásico: `int.parse` lanza un `FormatException` cuando el texto no es un número, mientras que `int.tryParse` devuelve `null` en su lugar y te deja decidir qué hacer:

```dart
print(int.tryParse('42'));  // 42
print(int.tryParse('4x2')); // null
print(int.tryParse(''));    // null
```

El tipo de retorno de `int.tryParse` es `int?`, así que todo lo que has aprendido se aplica: `??` para un valor por defecto, `?.` para encadenar, y una comprobación `if` para promocionar. `double.tryParse` funciona de la misma manera.

# --instructions--

Escribe una función `parseOrZero` que reciba un parámetro `text` de tipo `String` y devuelva el entero que contiene, o `0` cuando el texto no sea un entero válido. Usa `int.tryParse`.

Ejemplo:
```dart
print(parseOrZero('42'));  // 42
print(parseOrZero('abc')); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parseOrZero('42')` should be `42`

```dart
  test('test1', () {
    expect(parseOrZero('42'), 42, reason: '--err-t1--');
  });
```

`parseOrZero('-5')` should be `-5`

```dart
  test('test2', () {
    expect(parseOrZero('-5'), -5, reason: '--err-t2--');
  });
```

`parseOrZero('abc')` and `parseOrZero('')` should be `0`

```dart
  test('test3', () {
    expect(parseOrZero('abc'), 0, reason: '--err-t3--');
    expect(parseOrZero(''), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int parseOrZero(String text) {
  return int.tryParse(text) ?? 0;
}
```
