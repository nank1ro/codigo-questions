---
language: dart
exerciseType: 4
---

# --description--

Dos operadores más tienen una variante null-aware.

La **cascada null-aware** `?..` ejecuta una cadena de operaciones en cascada solo cuando el objeto no es `null`, y las omite todas en caso contrario:

```dart
List<int>? numbers;
numbers?..add(1)..add(2); // no pasa nada, numbers sigue siendo null
```

El **spread null-aware** `...?` inserta los elementos de una colección nullable en un literal, sin añadir nada cuando la colección es `null`:

```dart
List<int>? extra;
print([0, ...?extra]); // [0]

extra = [1, 2];
print([0, ...?extra]); // [0, 1, 2]
```

Sin el `?`, `...extra` sobre una `List<int>?` sería un error de compilación.

# --instructions--

Ordena los elementos para definir una función `fill` que añada `1` y `2` a una lista nullable con `?..`, y luego úsala en `main`. Define la función primero, y luego `main`. El programa debe imprimir:

```
[0]
[1, 2, 3]
```

# --answers--

```dart
void fill(List<int>? target) {
```

```dart
  target?..add(1)..add(2);
```

```dart
}
```

```dart
void main() {
```

```dart
  List<int>? numbers;
```

```dart
  print([0, ...?numbers]);
```

```dart
  numbers = [];
```

```dart
  fill(numbers);
```

```dart
  print([...?numbers, 3]);
```

```dart
}
```

# --solutions--

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  print([0, ...?numbers]);
  numbers = [];
  fill(numbers);
  print([...?numbers, 3]);
}
```

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  numbers = [];
  print([0, ...?numbers]);
  fill(numbers);
  print([...?numbers, 3]);
}
```

# --output--

[0]
[1, 2, 3]
