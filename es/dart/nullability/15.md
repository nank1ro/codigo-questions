---
language: dart
exerciseType: 4
---

# --description--

Two more operators have a null-aware variant.

The **null-aware cascade** `?..` runs a chain of cascade operations only when the object is not `null`, and skips them all otherwise:

```dart
List<int>? numbers;
numbers?..add(1)..add(2); // nothing happens, numbers is still null
```

The **null-aware spread** `...?` inserts the elements of a nullable collection into a literal, adding nothing when the collection is `null`:

```dart
List<int>? extra;
print([0, ...?extra]); // [0]

extra = [1, 2];
print([0, ...?extra]); // [0, 1, 2]
```

Without the `?`, `...extra` on a `List<int>?` would be a compile error.

# --instructions--

Sort the items to define a function `fill` that adds `1` and `2` to a nullable list with `?..`, then use it in `main`. Define the function first, then `main`. The program must print:

```
[0]
[1, 2, 3]
```

# --answers--

```dart
void fill(List<int>? target) {
```

```dart
  target?..add(1)..add(2);
```

```dart
}
```

```dart
void main() {
```

```dart
  List<int>? numbers;
```

```dart
  print([0, ...?numbers]);
```

```dart
  numbers = [];
```

```dart
  fill(numbers);
```

```dart
  print([...?numbers, 3]);
```

```dart
}
```

# --solutions--

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  print([0, ...?numbers]);
  numbers = [];
  fill(numbers);
  print([...?numbers, 3]);
}
```

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  numbers = [];
  print([0, ...?numbers]);
  fill(numbers);
  print([...?numbers, 3]);
}
```

# --output--

[0]
[1, 2, 3]
