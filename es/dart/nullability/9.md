---
language: dart
exerciseType: 1
---

# --description--

Comprobar un valor nullable con `if` es más seguro que `!`, y Dart te lo recompensa. Después de una comprobación como `if (x != null)`, el compilador sabe que `x` no puede ser `null` dentro del bloque, por lo que trata `x` como no nulo allí. Esto se llama **promoción de tipos** (*type promotion*):

```dart
int twice(int? n) {
  if (n != null) {
    return n * 2; // here n is an int, no ! needed
  }
  return 0;
}
```

La promoción también funciona después de un retorno anticipado:

```dart
int twice(int? n) {
  if (n == null) return 0;
  return n * 2; // n is an int from here on
}
```

La promoción se aplica a las **variables locales y los parámetros**, cuyo valor no puede cambiar a tus espaldas entre la comprobación y el uso.

# --instructions--

Escribe una función `half` que reciba un parámetro `n` de tipo `int` nullable y devuelva `n` dividido entre `2` con la división entera `~/`, o `0` cuando `n` es `null`. Usa una comprobación de `null` con `if`, no `!`.

Ejemplo:
```dart
print(half(10));   // 5
print(half(null)); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`half(10)` debe ser `5`

```dart
  test('test1', () {
    expect(half(10), 5, reason: '--err-t1--');
  });
```

`half(7)` debe ser `3`

```dart
  test('test2', () {
    expect(half(7), 3, reason: '--err-t2--');
  });
```

`half(null)` debe ser `0`

```dart
  test('test3', () {
    expect(half(null), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int half(int? n) {
  if (n != null) {
    return n ~/ 2;
  }
  return 0;
}
```
