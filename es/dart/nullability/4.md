---
language: dart
exerciseType: 1
---

# --description--

A menudo un valor faltante debe reemplazarse por un valor **por defecto**. El operador **if-null** `??` devuelve el operando izquierdo cuando no es `null`, y el operando derecho en caso contrario:

```dart
String? nickname;
print(nickname ?? 'anonymous'); // anonymous

nickname = 'Ada';
print(nickname ?? 'anonymous'); // Ada
```

`??` solo reacciona ante `null`: una cadena vacía `''` o el número `0` son valores reales, por lo que se mantienen.

`??` combina bien con `?.`, porque `?.` produce un resultado nullable:

```dart
String? text;
print(text?.length ?? 0); // 0
```

# --instructions--

Escribe una función `greet` que reciba un parámetro `name` de tipo `String` nullable y devuelva `'Hello, '` seguido de `name`, o de `'stranger'` cuando `name` es `null`. Usa `??`.

Ejemplo:
```dart
print(greet('Ada')); // Hello, Ada
print(greet(null));  // Hello, stranger
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greet('Ada')` debe ser `'Hello, Ada'`

```dart
  test('test1', () {
    expect(greet('Ada'), 'Hello, Ada', reason: '--err-t1--');
  });
```

`greet(null)` debe ser `'Hello, stranger'`

```dart
  test('test2', () {
    expect(greet(null), 'Hello, stranger', reason: '--err-t2--');
  });
```

`greet('')` debe ser `'Hello, '`, porque una cadena vacía no es `null`

```dart
  test('test3', () {
    expect(greet(''), 'Hello, ', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String greet(String? name) {
  return 'Hello, ${name ?? 'stranger'}';
}
```
