---
language: dart
exerciseType: 1
---

# --instructions--

Declara dos variables `int` de nivel superior: `a` con el valor `12` y `b` con el valor `8`. Luego declara una tercera variable `int` de nivel superior llamada `total` que almacena su suma.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`a` debe ser `12`, `b` debe ser `8` y `total` debe ser `20`

```dart
  test('test1', () {
    expect(a, 12, reason: '--err-t1--');
    expect(b, 8, reason: '--err-t2--');
    expect(total, 20, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 12;
int b = 8;
int total = a + b;

void main() {
  print(total);
}
```
