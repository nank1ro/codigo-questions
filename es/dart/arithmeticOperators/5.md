---
language: dart
exerciseType: 2
---

# --description--

El **operador de división entera** `~/` divide dos números y **trunca** el resultado a un entero (descarta cualquier parte decimal).

```dart
int a = 10;
int b = 3;
int result = a ~/ b; // 3  (not 3.333...)
```

Usa `~/` cuando necesites un cociente de número entero sin residuo.

# --instructions--

Completa el código para realizar la división entera de `17` entre `5` y almacena el resultado en `result`.

# --seed--

```dart
void main() {
  int result = 17 [/] 5;
  print(result);
}
```

# --answers--

- ~/
- /
- %
- *

# --solutions--

```dart
void main() {
  int result = 17 ~/ 5;
  print(result);
}
```

# --output--

3
