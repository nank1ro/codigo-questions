---
language: dart
exerciseType: 1
---

# --instructions--

Declara tres variables `int` de nivel superior: `a` con el valor `10`, `b` con el valor `3` y `c` con el valor `2`. Luego declara:
- `sum` = `a + b`
- `product` = `b * c`
- `combined` = `sum - product`

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` debe ser `13`, `product` debe ser `6` y `combined` debe ser `7`

```dart
  test('test1', () {
    expect(sum, 13, reason: '--err-t1--');
    expect(product, 6, reason: '--err-t2--');
    expect(combined, 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 10;
int b = 3;
int c = 2;
int sum = a + b;
int product = b * c;
int combined = sum - product;

void main() {
  print(combined);
}
```
