---
language: dart
exerciseType: 1
---

# --instructions--

Declara dos variables `int` de nivel superior: `total` con el valor `29` y `perGroup` con el valor `4`. Luego declara una variable `int` de nivel superior llamada `groups` que almacena cuántos grupos completos de `4` caben en `29` (usa división entera `~/`).

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`groups` debe ser igual a `7`

```dart
  test('test1', () {
    expect(groups, 7, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 29;
int perGroup = 4;
int groups = total ~/ perGroup;

void main() {
  print(groups);
}
```
