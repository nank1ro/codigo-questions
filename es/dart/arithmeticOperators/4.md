---
language: dart
exerciseType: 2
---

# --description--

El **operador de división** `/` divide un valor entre otro. En Dart, `/` **siempre devuelve un `double`**, incluso cuando el resultado es un número entero.

```dart
int a = 10;
int b = 4;
double result = a / b; // 2.5

double exact = 10 / 2; // 5.0  (not 5!)
```

Siempre usa una variable `double` para almacenar el resultado de `/`.

# --instructions--

Completa el código para dividir `9` entre `4` y almacena el resultado en `result`.

# --seed--

```dart
void main() {
  double result = 9 [/] 4;
  print(result);
}
```

# --answers--

- /
- *
- +
- -

# --solutions--

```dart
void main() {
  double result = 9 / 4;
  print(result);
}
```

# --output--

2.25
