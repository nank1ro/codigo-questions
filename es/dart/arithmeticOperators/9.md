---
language: dart
exerciseType: 1
---

# --instructions--

Usa el operador módulo `%` para encontrar el residuo cuando `17` se divide entre `5`. Almacena el resultado en una variable de nivel superior llamada `remainder`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

La variable `remainder` debe ser igual a `2`

```dart
  test('test1', () {
    expect(remainder, 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 17 % 5;

void main() {
  print(remainder);
}
```
