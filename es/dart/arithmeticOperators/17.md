---
language: dart
exerciseType: 1
---

# --instructions--

Una tienda vende `itemCount` artículos a `pricePerItem` dólares cada uno, con una cantidad de `discount` dólares restada del total. Declara estas tres variables `int` de nivel superior: `itemCount` = `6`, `pricePerItem` = `15`, `discount` = `10`. Luego declara una variable `int` de nivel superior llamada `totalPrice` que calcula el total después del descuento.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`totalPrice` debe ser igual a `80`

```dart
  test('test1', () {
    expect(totalPrice, 80, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int itemCount = 6;
int pricePerItem = 15;
int discount = 10;
int totalPrice = itemCount * pricePerItem - discount;

void main() {
  print(totalPrice);
}
```
