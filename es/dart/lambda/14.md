---
language: dart
exerciseType: 4
---

# --description--

Las funciones anónimas admiten los mismos tipos de parámetros que las funciones con nombre: parámetros **posicionales opcionales** entre `[]` y parámetros **con nombre** entre `{}`, ambos con valores por defecto:

```dart
var repeat = (String text, [int times = 2]) => text * times;
print(repeat('ab'));    // abab
print(repeat('ab', 3)); // ababab

var describe = ({required String item, int count = 1}) => '$count x $item';
print(describe(item: 'apple'));           // 1 x apple
print(describe(item: 'pear', count: 4));  // 4 x pear
```

# --instructions--

Ordena los elementos para guardar en `greet` una función anónima con un parámetro con nombre `name` cuyo valor por defecto es `'World'`, y luego llámala primero sin argumentos y después con `name: 'Dart'`. El programa debe imprimir:

```
Hello, World!
Hello, Dart!
```

# --answers--

```dart
void main() {
```

```dart
  var greet = ({String name = 'World'}) {
```

```dart
    print('Hello, $name!');
```

```dart
  };
```

```dart
  greet();
```

```dart
  greet(name: 'Dart');
```

```dart
}
```

# --solutions--

```dart
void main() {
  var greet = ({String name = 'World'}) {
    print('Hello, $name!');
  };
  greet();
  greet(name: 'Dart');
}
```

# --output--

Hello, World!
Hello, Dart!
