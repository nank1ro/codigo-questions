---
language: dart
exerciseType: 1
---

# --description--

Como un tipo de función es un tipo normal, una función puede tomar **otra función como parámetro**. Dentro del cuerpo, el parámetro se llama como cualquier función:

```dart
int apply(int n, int Function(int) operation) {
  return operation(n);
}

print(apply(5, (n) => n * 2)); // 10
print(apply(5, (n) => n - 1)); // 4
```

Aquí quien llama decide qué hace `apply` pasando una función anónima como segundo argumento.

# --instructions--

Escribe una función `applyTwice` que tome un parámetro `int` `n` y un parámetro de función `operation` de tipo `int Function(int)`, y devuelva el resultado de aplicar `operation` a `n` dos veces.

Ejemplo:
```dart
print(applyTwice(3, (n) => n * 2)); // 12
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`applyTwice(3, (n) => n * 2)` debería ser `12`

```dart
  test('test1', () {
    expect(applyTwice(3, (n) => n * 2), 12, reason: '--err-t1--');
  });
```

`applyTwice(10, (n) => n + 5)` debería ser `20`

```dart
  test('test2', () {
    expect(applyTwice(10, (n) => n + 5), 20, reason: '--err-t2--');
  });
```

`applyTwice(2, (n) => n * n)` debería ser `16`

```dart
  test('test3', () {
    expect(applyTwice(2, (n) => n * n), 16, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int applyTwice(int n, int Function(int) operation) {
  return operation(operation(n));
}
```
