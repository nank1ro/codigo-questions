---
language: dart
exerciseType: 2
---

# --description--

Cuando el cuerpo es una sola expresión, una función anónima puede usar la **sintaxis flecha** `=>`, igual que una función con nombre. La flecha reemplaza las llaves y la palabra clave `return`:

```dart
var add = (int a, int b) => a + b;

print(add(2, 3)); // 5
```

Esta forma corta es con diferencia la manera más común de escribir funciones anónimas en Dart.

# --instructions--

Completa el código para guardar en `triple` una función flecha que devuelve su argumento multiplicado por `3`, de modo que el programa imprima `12`.

# --seed--

```dart
void main() {
  var triple = (int n) [/] n [/] 3;
  print(triple(4));
}
```

# --answers--

- =>
- *
- ->
- +
- return
- {

# --solutions--

```dart
void main() {
  var triple = (int n) => n * 3;
  print(triple(4));
}
```

# --output--

12
