---
language: dart
exerciseType: 1
---

# --description--

Una función anónima no tiene que guardarse en ningún sitio: puede **llamarse inmediatamente** escribiendo los argumentos justo después de su llave de cierre. Esto es útil para calcular un valor con unas pocas variables temporales que no deben filtrarse en el código circundante:

```dart
var area = (double radius) {
  var pi = 3.14;
  return pi * radius * radius;
}(2.0);

print(area); // 12.56
```

Aquí `pi` existe solo dentro de la función anónima, y `area` recibe el valor devuelto.

# --instructions--

Una variable `int` `limit` ya está definida. Declara una variable `sum` inicializada invocando inmediatamente una función anónima sin parámetros que suma todos los enteros desde `1` hasta `limit` con un bucle y devuelve el total.

# --before-seed--

```dart
dynamic result(int limit) {
```

# --seed--

```dart
var sum = 
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` debería ser un `int`

```dart
  test('test1', () {
    expect(result(100), isA<int>(), reason: '--err-t1--');
  });
```

`sum` debería ser `5050` cuando `limit` es `100`

```dart
  test('test2', () {
    expect(result(100), 5050, reason: '--err-t2--');
  });
```

`sum` debería ser `55` cuando `limit` es `10`

```dart
  test('test3', () {
    expect(result(10), 55, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var sum = () {
  var total = 0;
  for (var i = 1; i <= limit; i++) {
    total += i;
  }
  return total;
}();
```
