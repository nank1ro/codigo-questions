---
language: dart
exerciseType: 1
---

# --description--

Un closure no solo lee las variables que captura: también puede **modificarlas**, y los cambios se conservan entre llamadas. Esto permite mantener un estado privado sin una clase:

```dart
int Function() makeTimer() {
  var seconds = 0;
  return () {
    seconds += 10;
    return seconds;
  };
}

var timer = makeTimer();
print(timer()); // 10
print(timer()); // 20
```

Cada llamada a `makeTimer()` crea una variable `seconds` completamente nueva, así que dos temporizadores nunca comparten su contador.

# --instructions--

Escribe una función `makeCounter` sin parámetros que devuelva una función de tipo `int Function()`. Cada vez que la función devuelta se llame debe incrementar un contador capturado y devolverlo, empezando desde `1`.

Ejemplo:
```dart
var counter = makeCounter();
print(counter()); // 1
print(counter()); // 2
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

La primera llamada al contador debería devolver `1`

```dart
  test('test1', () {
    var counter = makeCounter();
    expect(counter(), 1, reason: '--err-t1--');
  });
```

Tres llamadas al mismo contador deberían devolver `1`, `2` y `3`

```dart
  test('test2', () {
    var counter = makeCounter();
    expect([counter(), counter(), counter()], [1, 2, 3], reason: '--err-t2--');
  });
```

Dos contadores deberían ser independientes

```dart
  test('test3', () {
    var a = makeCounter();
    var b = makeCounter();
    a();
    a();
    expect(b(), 1, reason: '--err-t3--');
    expect(a(), 3, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int Function() makeCounter() {
  var count = 0;
  return () {
    count++;
    return count;
  };
}
```
