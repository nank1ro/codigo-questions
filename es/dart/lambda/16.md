---
language: dart
exerciseType: 1
---

# --description--

Las funciones que toman y devuelven otras funciones pueden combinarse libremente. Un ejemplo clásico es la **composición**: construir una nueva función que ejecuta una función y luego pasa su resultado a otra:

```dart
int Function(int) then(int Function(int) first, int Function(int) second) {
  return (n) => second(first(n));
}
```

# --instructions--

Escribe una función `compose` que tome dos parámetros `f` y `g`, ambos de tipo `int Function(int)`, y devuelva una función de tipo `int Function(int)` que aplique `f` a su argumento y luego `g` al resultado.

Ejemplo:
```dart
var addThenDouble = compose((n) => n + 1, (n) => n * 2);
print(addThenDouble(3)); // 8
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`compose((n) => n + 1, (n) => n * 2)(3)` debería ser `8`

```dart
  test('test1', () {
    expect(compose((n) => n + 1, (n) => n * 2)(3), 8, reason: '--err-t1--');
  });
```

`compose((n) => n * 2, (n) => n + 1)(3)` debería ser `7`

```dart
  test('test2', () {
    expect(compose((n) => n * 2, (n) => n + 1)(3), 7, reason: '--err-t2--');
  });
```

La función devuelta debería tener tipo `int Function(int)`

```dart
  test('test3', () {
    var negate = compose((n) => -n, (n) => n * n);
    expect(negate, isA<int Function(int)>(), reason: '--err-t3--');
    expect(negate(5), 25, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int Function(int) compose(int Function(int) f, int Function(int) g) {
  return (int n) => g(f(n));
}
```
