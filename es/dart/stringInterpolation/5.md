---
language: dart
exerciseType: 3
---

# --description--

Si lo que sigue al signo `$` no es reconocible como un identificador de programa, Dart producirá un error de compilación: el `$` dentro de una cadena siempre debe ir seguido de un identificador o de una expresión entre llaves (`${}`).

# --instructions--

¿Qué sucede cuando intentas ejecutar este código?

```dart
void main() {
    print("$5 friends");
}
```

# --answers--

- No compila porque `$5` no es una interpolación de cadena válida
- Imprime `$5 friends`
- Imprime `5 friends`
- No imprime nada

# --solutions--

- No compila porque `$5` no es una interpolación de cadena válida
