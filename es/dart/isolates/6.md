---
language: dart
exerciseType: 1
---

# --description--

La función que se le da a `Isolate.spawn` se llama el **punto de entrada**. Debe ser una función de nivel superior (o estática) que reciba exactamente un parámetro: el mensaje que `Isolate.spawn` le pasa.

```dart
void reportAnswer(SendPort port) {
  port.send(42);
}
```

Los mensajes que llegan a un `ReceivePort` tienen el tipo estático `dynamic`, porque cualquier valor pudo haber sido enviado. Cuando sabes qué envía el otro isolate, hazle un cast:

```dart
final receivePort = ReceivePort();
await Isolate.spawn(reportAnswer, receivePort.sendPort);
final answer = await receivePort.first as int;
```

# --instructions--

Escribe una función de nivel superior `sendGreeting` que reciba un `SendPort` `port` y envíe la cadena `hello from the isolate` a ese puerto.

Luego escribe una función `greetFromIsolate` que devuelva un `Future<String>`: crea un `ReceivePort`, inicia `sendGreeting` con su `sendPort` y se completa con el primer mensaje recibido.

Ejemplo:
```dart
print(await greetFromIsolate()); // hello from the isolate
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendGreeting` debería enviar `'hello from the isolate'` al puerto que recibe

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendGreeting(port.sendPort);
    expect(await port.first, 'hello from the isolate', reason: '--err-t1--');
  });
```

`greetFromIsolate()` debería completarse con `'hello from the isolate'`

```dart
  test('test2', () async {
    expect(await greetFromIsolate(), 'hello from the isolate', reason: '--err-t2--');
  });
```

`greetFromIsolate` debería devolver un `Future<String>` y debería funcionar al llamarla dos veces

```dart
  test('test3', () async {
    expect(greetFromIsolate(), isA<Future<String>>(), reason: '--err-t3--');
    expect(await greetFromIsolate(), 'hello from the isolate', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendGreeting(SendPort port) {
  port.send('hello from the isolate');
}

Future<String> greetFromIsolate() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendGreeting, receivePort.sendPort);
  return await receivePort.first as String;
}
```
