---
language: dart
exerciseType: 1
---

# --description--

`Isolate.spawn` pasa exactamente **un** mensaje al punto de entrada, y el worker normalmente necesita tanto un `SendPort` para responder como algunos datos con los que trabajar. El truco habitual es agrupar todo en una `List` y desempaquetarlo del otro lado:

```dart
void multiply(List<Object> message) {
  final port = message[0] as SendPort;
  final value = message[1] as int;
  port.send(value * 2);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(multiply, <Object>[receivePort.sendPort, 21]);
  print(await receivePort.first); // 42
}
```

La lista se copia a la ida, así que el worker lee sus propios valores. Un `SendPort` es una de las pocas cosas que no se copian sino que se comparten: sigue apuntando al buzón del isolate que lo creó, que es justamente por eso que puede usarse como dirección de respuesta.

# --instructions--

Escribe una función de nivel superior `sendSum` que reciba una `List<Object>` `message` donde el elemento `0` es un `SendPort` y los elementos `1` y `2` son `int`, y envíe su suma al puerto.

Luego escribe una función `addInIsolate` que reciba dos `int` `a` y `b` y devuelva un `Future<int>` que se complete con la suma calculada por un isolate `sendSum` iniciado.

Ejemplo:
```dart
print(await addInIsolate(2, 3)); // 5
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendSum` debería enviar la suma de los dos números del mensaje al puerto

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendSum(<Object>[port.sendPort, 4, 6]);
    expect(await port.first, 10, reason: '--err-t1--');
  });
```

`addInIsolate(2, 3)` debería completarse con `5`

```dart
  test('test2', () async {
    expect(await addInIsolate(2, 3), 5, reason: '--err-t2--');
  });
```

`addInIsolate(10, -4)` debería completarse con `6` y `addInIsolate` debería devolver un `Future<int>`

```dart
  test('test3', () async {
    expect(await addInIsolate(10, -4), 6, reason: '--err-t3--');
    expect(addInIsolate(1, 1), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendSum(List<Object> message) {
  final port = message[0] as SendPort;
  final a = message[1] as int;
  final b = message[2] as int;
  port.send(a + b);
}

Future<int> addInIsolate(int a, int b) async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendSum, <Object>[receivePort.sendPort, a, b]);
  return await receivePort.first as int;
}
```
