---
language: dart
exerciseType: 1
---

# --description--

Cada `Isolate.run` inicia su propio isolate, así que varios de ellos realmente se ejecutan en el mismo instante, en tantos núcleos como tenga la máquina. El patrón es el que ya conoces de los futures: inicia primero cada cálculo y luego espera todos con `Future.wait`, que conserva los resultados en el orden de la entrada.

```dart
Future<List<int>> doubleAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * 2)));
}
```

Iniciar un isolate no es gratis: cuesta memoria y unos pocos milisegundos. Dividir un cálculo largo entre un puñado de isolates vale la pena; enviar mil sumas triviales a mil isolates no.

# --instructions--

Escribe una función `squareAll` que reciba una `List<int>` `numbers` y devuelva un `Future<List<int>>` que se complete con el cuadrado de cada número, en el mismo orden, con cada cuadrado calculado en su propio isolate.

Ejemplo:
```dart
print(await squareAll([1, 2, 3])); // [1, 4, 9]
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`squareAll([1, 2, 3])` debería completarse con `[1, 4, 9]`

```dart
  test('test1', () async {
    expect(await squareAll([1, 2, 3]), [1, 4, 9], reason: '--err-t1--');
  });
```

`squareAll([-4, 5])` debería completarse con `[16, 25]`, conservando el orden de la entrada

```dart
  test('test2', () async {
    expect(await squareAll([-4, 5]), [16, 25], reason: '--err-t2--');
  });
```

`squareAll([])` debería completarse con una lista vacía y `squareAll` debería devolver un `Future<List<int>>`

```dart
  test('test3', () async {
    expect(await squareAll(<int>[]), <int>[], reason: '--err-t3--');
    expect(squareAll([2]), isA<Future<List<int>>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<List<int>> squareAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * n)));
}
```
