---
language: dart
exerciseType: 2
---

# --description--

`first` lee un mensaje y cierra el buzón. Un `ReceivePort` también es un **`Stream`**, así que para leer muchos mensajes lo recorres con `await for`.

El bucle nunca termina por sí solo: el puerto queda abierto esperando un mensaje que quizás nunca llegue. Por eso el worker envía un último valor como señal, a menudo `null`, y quien escucha reacciona llamando a **`close()`**, que termina el stream y el bucle:

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(countdown, receivePort.sendPort);
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --instructions--

Completa el código para que el isolate iniciado envíe `3`, `2`, `1` y luego `null`, y el isolate principal imprima cada número, cierre el buzón con `null` y finalmente imprima `done`.

# --seed--

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.[/](countdown, receivePort.sendPort);
  await [/] (final message in receivePort) {
    if (message == null) {
      receivePort.[/]();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --answers--

- spawn
- for
- close
- run
- while
- listen
- first

# --solutions--

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(countdown, receivePort.sendPort);
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --output--

3
2
1
done
