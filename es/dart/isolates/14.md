---
language: dart
exerciseType: 4
---

# --description--

En conjunto, un programa con `Isolate.run` se lee como código secuencial ordinario: la línea anterior a la llamada se ejecuta en el isolate principal, el cálculo se ejecuta en otro lado, y la línea posterior al `await` vuelve a ejecutarse en el isolate principal con el resultado en la mano.

```dart
import 'dart:isolate';

int twice(int n) => n * 2;

Future<void> main() async {
  print('start');
  final result = await Isolate.run(() => twice(4));
  print(result);
}
// start
// 8
```

# --instructions--

Ordena los elementos para construir un programa que imprima `start`, eleve `9` al cuadrado en otro isolate con `Isolate.run` e imprima el resultado. Pon el import primero y define `square` antes de `main`. El programa debe imprimir:

```
start
81
```

# --answers--

```dart
import 'dart:isolate';
```

```dart
int square(int n) => n * n;
```

```dart
Future<void> main() async {
```

```dart
  print('start');
```

```dart
  final result = await Isolate.run(() => square(9));
```

```dart
  print(result);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
int square(int n) => n * n;
Future<void> main() async {
  print('start');
  final result = await Isolate.run(() => square(9));
  print(result);
}
```

# --output--

start
81
