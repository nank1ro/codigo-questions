---
language: dart
exerciseType: 1
---

# --description--

Un `typedef` da un nombre a un tipo, y puede recibir parámetros de tipo propios. La razón habitual es nombrar una familia de tipos de función una sola vez en lugar de deletrearla en cada uso:

```dart
typedef Transform<I, O> = O Function(I input);

final Transform<String, int> length = (word) => word.length;
print(length('kiwi')); // 4
```

`Transform<String, int>` es solo otra forma de escribir `int Function(String)`, así que ambos son intercambiables. La ganancia es legibilidad: un parámetro declarado como `Transform<I, O> transform` dice para qué sirve la función, mientras que `O Function(I)` solo dice cómo es.

Un typedef genérico y una función genérica se combinan de forma natural, con los parámetros de tipo de la propia función rellenando los del typedef.

# --instructions--

Declara un typedef genérico `Transform` con dos parámetros de tipo `I` y `O` que nombre una función que recibe un `I` y devuelve un `O`.

Luego escribe una función genérica `convertAll` con parámetros de tipo `I` y `O` que tome una `List<I>` `items` y una `Transform<I, O>` `transform`, y devuelva una `List<O>` con cada elemento convertido, en orden.

Ejemplo:
```dart
print(convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)); // [3, 4]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)` debería devolver `[3, 4]`

```dart
  test('test1', () {
    expect(
      convertAll<String, int>(['fig', 'kiwi'], (w) => w.length),
      [3, 4],
      reason: '--err-t1--',
    );
  });
```

`convertAll` debería devolver una `List<O>`, así que convertir ints a cadenas debería dar una `List<String>`

```dart
  test('test2', () {
    final result = convertAll<int, String>([1, 2], (n) => 'n$n');
    expect(result, ['n1', 'n2'], reason: '--err-t2--');
    expect(result, isA<List<String>>(), reason: '--err-t2--');
  });
```

El typedef `Transform` debería nombrar un tipo de función, y una lista vacía debería dar una lista vacía

```dart
  test('test3', () {
    final Transform<int, int> twice = (n) => n * 2;
    expect(convertAll<int, int>([1, 2, 3], twice), [2, 4, 6], reason: '--err-t3--');
    expect(convertAll<String, int>(<String>[], (w) => w.length), <int>[], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
typedef Transform<I, O> = O Function(I input);

List<O> convertAll<I, O>(List<I> items, Transform<I, O> transform) {
  return items.map(transform).toList();
}
```
