---
language: dart
exerciseType: 1
---

# --description--

Una cota puede mencionar al propio parámetro de tipo. `Comparable<T>` es la interfaz de todo lo que sabe compararse consigo mismo con otros de su mismo tipo, mediante `compareTo`:

```dart
print('fig'.compareTo('kiwi')); // negativo: fig va primero
print('kiwi'.compareTo('fig')); // positivo
print('fig'.compareTo('fig'));  // cero
```

Así que `T extends Comparable<T>` se lee como "cualquier tipo que pueda compararse consigo mismo", que es justo lo que necesita una función de ordenamiento o de máximo:

```dart
T maxOf<T extends Comparable<T>>(T a, T b) => a.compareTo(b) >= 0 ? a : b;

print(maxOf('fig', 'kiwi')); // kiwi
```

`String` y `DateTime` la satisfacen directamente. `int` y `double` implementan `Comparable<num>`, así que una lista de números simplemente se compara como `num`.

# --instructions--

Escribe una función genérica `largest` con un parámetro de tipo `T` acotado por `Comparable<T>` que tome una `List<T>` `items` no vacía y devuelva el elemento mayor, comparando con `compareTo`.

Ejemplo:
```dart
print(largest(['fig', 'kiwi', 'apple'])); // kiwi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest(['fig', 'kiwi', 'apple'])` debería devolver `'kiwi'`

```dart
  test('test1', () {
    expect(largest(['fig', 'kiwi', 'apple']), 'kiwi', reason: '--err-t1--');
  });
```

`largest` debería funcionar también con números y con fechas

```dart
  test('test2', () {
    expect(largest([3, 9, 5]), 9, reason: '--err-t2--');
    expect(
      largest([DateTime(2020), DateTime(2024), DateTime(2001)]),
      DateTime(2024),
      reason: '--err-t2--',
    );
  });
```

`largest` debería devolver el único elemento de una lista de un elemento, el último cuando es el mayor, y debería aceptar un argumento de tipo explícito como `largest<String>(['a', 'z'])`

```dart
  test('test3', () {
    expect(largest<String>(['a', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['only']), 'only', reason: '--err-t3--');
    expect(largest(['a', 'b', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['z', 'b', 'a']), 'z', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
T largest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) > 0) {
      best = item;
    }
  }
  return best;
}
```
