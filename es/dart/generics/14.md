---
language: dart
exerciseType: 1
---

# --description--

Una clase genérica puede tener constructores con nombre y constructores **factory** como cualquier otra clase, y el parámetro de tipo está disponible dentro de ellos. Un constructor factory no crea el objeto por sí mismo: ejecuta un cuerpo y devuelve uno, lo que le permite elegir, reutilizar o construir la instancia de la manera que quiera.

```dart
class Box<T> {
  final T value;

  Box(this.value);

  factory Box.first(List<T> items) => Box(items.first);
}

final b = Box<int>.first([5, 6]);
print(b.value); // 5
```

El argumento de tipo va en la clase, no en el nombre del constructor: `Box<int>.first(...)`. Dentro del factory, `<T>[]` es una `List<T>` vacía real, así que un factory es el lugar natural para construir un valor por defecto para un tipo que aún no conoces.

# --instructions--

Declara una clase genérica `Stack` con un parámetro de tipo `T`, un campo `final` `items` de tipo `List<T>` asignado por el constructor normal, un constructor **factory** `Stack.empty()` que construya un `Stack` con una lista vacía y un getter `top` de tipo `T?` que devuelva el último elemento, o `null` cuando la pila esté vacía.

Ejemplo:
```dart
print(Stack<int>([1, 2, 3]).top);  // 3
print(Stack<String>.empty().top);  // null
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Stack<int>([1, 2, 3]).top` debería ser `3`

```dart
  test('test1', () {
    final stack = Stack<int>([1, 2, 3]);
    expect(stack.top, 3, reason: '--err-t1--');
    expect(stack.items, [1, 2, 3], reason: '--err-t1--');
  });
```

`Stack.empty()` debería construir una pila vacía del tipo correcto, cuyo `top` sea `null`

```dart
  test('test2', () {
    final stack = Stack<String>.empty();
    expect(stack, isA<Stack<String>>(), reason: '--err-t2--');
    expect(stack.items, <String>[], reason: '--err-t2--');
    expect(stack.top, null, reason: '--err-t2--');
  });
```

La lista construida por `Stack.empty()` debería seguir siendo utilizable

```dart
  test('test3', () {
    final stack = Stack<int>.empty();
    stack.items.add(4);
    stack.items.add(9);
    expect(stack.top, 9, reason: '--err-t3--');
    expect(Stack<bool>([true]).top, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Stack<T> {
  final List<T> items;

  Stack(this.items);

  factory Stack.empty() => Stack(<T>[]);

  T? get top => items.isEmpty ? null : items.last;
}
```
