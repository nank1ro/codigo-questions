---
language: dart
exerciseType: 2
---

# --description--

El método `.replaceAll(from, to)` devuelve una nueva cadena donde **cada** aparición de `from` se reemplaza por `to`. `.replaceFirst(from, to)` reemplaza solo la primera:

```dart
var text = 'a-b-c';
print(text.replaceAll('-', '+'));    // a+b+c
print(text.replaceFirst('-', '+'));  // a+b-c
```

# --instructions--

Completa el código para reemplazar cada `cats` por `dogs` e imprimir el resultado.

# --seed--

```dart
var text = 'cats here, cats there';
print(text.[/]('cats', [/]));
```

# --answers--

- replaceAll
- 'dogs'
- replaceFirst
- replace
- 'cats'
- dogs

# --solutions--

```dart
var text = 'cats here, cats there';
print(text.replaceAll('cats', 'dogs'));
```

# --output--

dogs here, dogs there
