---
language: dart
exerciseType: 1
---

# --description--

El método `.substring(start, end)` devuelve la parte de una cadena desde el índice `start` hasta, **sin incluirlo**, el índice `end`. Si omites `end`, toma todo hasta el final de la cadena:

```dart
var text = 'Hello Dart';
print(text.substring(0, 5));  // Hello
print(text.substring(6));     // Dart
```

# --instructions--

Dada la cadena `date` en el formato `YYYY-MM-DD`, guarda en un `String` llamado `year` los primeros cuatro caracteres y en un `String` llamado `day` los últimos dos caracteres.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var date = '2024-06-15';
```

# --after-seed--

```dart
  return [year, day];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`year` debería ser `'2024'`

```dart
  test('test1', () {
    expect(result()[0], '2024', reason: '--err-t1--');
  });
```

`day` debería ser `'15'`

```dart
  test('test2', () {
    expect(result()[1], '15', reason: '--err-t2--');
  });
```

`year` debería ser un `String`

```dart
  test('test3', () {
    expect(result()[0], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var date = '2024-06-15';
String year = date.substring(0, 4);
String day = date.substring(8);
```
