---
language: dart
exerciseType: 1
---

# --description--

Varios métodos te permiten buscar dentro de una cadena:

- `.contains(other)` devuelve `true` si `other` aparece en cualquier parte de la cadena
- `.startsWith(other)` y `.endsWith(other)` comprueban el inicio y el final
- `.indexOf(other)` devuelve el índice de la primera aparición, o `-1` si no se encuentra

```dart
var file = 'report.pdf';
print(file.contains('port'));    // true
print(file.startsWith('rep'));   // true
print(file.endsWith('.txt'));    // false
print(file.indexOf('.'));        // 6
print(file.indexOf('x'));        // -1
```

Todos son sensibles a mayúsculas y minúsculas: `'Dart'.contains('dart')` es `false`.

# --instructions--

Dada la cadena `email`, guarda en un `bool` llamado `hasAt` si contiene el carácter `@`, en un `int` llamado `atIndex` el índice de ese carácter, y en un `bool` llamado `isExample` si el email termina con `example.com`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var email = 'ada@example.com';
```

# --after-seed--

```dart
  return [hasAt, atIndex, isExample];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasAt` debería ser `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`atIndex` debería ser `3`

```dart
  test('test2', () {
    expect(result()[1], 3, reason: '--err-t2--');
  });
```

`isExample` debería ser `true`

```dart
  test('test3', () {
    expect(result()[2], true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var email = 'ada@example.com';
bool hasAt = email.contains('@');
int atIndex = email.indexOf('@');
bool isExample = email.endsWith('example.com');
```
