---
language: dart
exerciseType: 1
---

# --description--

El método `.split(separator)` corta una cadena en una `List<String>` en cada aparición del separador. Lo opuesto es `.join(separator)`, un método de las listas que une los elementos en una sola cadena:

```dart
var csv = 'a,b,c';
List<String> parts = csv.split(',');
print(parts);            // [a, b, c]
print(parts.join(' - ')); // a - b - c
```

Llamar a `.split('')` con un separador vacío te da una lista con cada carácter individual.

# --instructions--

Dada la cadena `csv`, guarda en una `List<String>` llamada `colors` sus partes separadas por la coma, y en un `String` llamado `joined` esas partes unidas con `' | '`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var csv = 'red,green,blue';
```

# --after-seed--

```dart
  return [colors, joined];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`colors` debería ser `['red', 'green', 'blue']`

```dart
  test('test1', () {
    expect(result()[0], ['red', 'green', 'blue'], reason: '--err-t1--');
  });
```

`joined` debería ser `'red | green | blue'`

```dart
  test('test2', () {
    expect(result()[1], 'red | green | blue', reason: '--err-t2--');
  });
```

`colors` debería ser una `List`

```dart
  test('test3', () {
    expect(result()[0], isA<List>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var csv = 'red,green,blue';
List<String> colors = csv.split(',');
String joined = colors.join(' | ');
```
