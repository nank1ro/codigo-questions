---
language: dart
exerciseType: 1
---

# --description--

Dos cadenas se pueden unir en una nueva con el operador `+`, llamado **concatenación**:

```dart
var greeting = 'Hello' + ', ' + 'Dart';
print(greeting); // Hello, Dart
```

Dart también une dos **literales** de cadena que están escritos uno junto al otro, sin ningún operador. Esto es útil para dividir un texto largo en varias líneas:

```dart
var text = 'Hello, '
    'Dart';
print(text); // Hello, Dart
```

Solo las cadenas se pueden concatenar con `+`: `'Age: ' + 30` es un error de compilación, porque `30` es un `int`.

# --instructions--

Dadas las variables `first` y `last`, crea un `String` llamado `full` que contenga el nombre, un espacio y el apellido.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var first = 'Ada';
var last = 'Lovelace';
```

# --after-seed--

```dart
  return full;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`full` debería ser `'Ada Lovelace'`

```dart
  test('test1', () {
    expect(result(), 'Ada Lovelace', reason: '--err-t1--');
  });
```

`full` debería ser un `String`

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var first = 'Ada';
var last = 'Lovelace';
String full = first + ' ' + last;
```
