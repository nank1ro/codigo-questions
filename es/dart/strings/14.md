---
language: dart
exerciseType: 1
---

# --description--

Por debajo, cada carácter de una cadena se almacena como un número, su **code unit** (un código UTF-16). `.codeUnitAt(index)` da el código de un carácter y `.codeUnits` la lista completa. `String.fromCharCode(code)` hace lo contrario, construyendo una cadena a partir de un código:

```dart
var word = 'AB';
print(word.codeUnitAt(0));         // 65
print(word.codeUnits);             // [65, 66]
print(String.fromCharCode(67));    // C
```

Las letras consecutivas tienen códigos consecutivos: `'A'` es 65, `'B'` es 66, y así sucesivamente.

# --instructions--

Dada la cadena `letter`, guarda en un `int` llamado `code` su code unit, y en un `String` llamado `next` el carácter cuyo código es `code + 1`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var letter = 'D';
```

# --after-seed--

```dart
  return [code, next];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`code` debería ser `68`

```dart
  test('test1', () {
    expect(result()[0], 68, reason: '--err-t1--');
  });
```

`next` debería ser `'E'`

```dart
  test('test2', () {
    expect(result()[1], 'E', reason: '--err-t2--');
  });
```

`next` debería ser un `String`

```dart
  test('test3', () {
    expect(result()[1], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var letter = 'D';
int code = letter.codeUnitAt(0);
String next = String.fromCharCode(code + 1);
```
