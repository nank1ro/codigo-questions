---
language: dart
exerciseType: 1
---

# --description--

Toda cadena conoce cuántos caracteres tiene a través de su propiedad `.length`. Los espacios y la puntuación también cuentan como caracteres:

```dart
var word = 'hello';
print(word.length);      // 5
print('a b'.length);     // 3
```

Puedes leer un único carácter con corchetes y su **índice**, empezando desde `0`. El resultado es un `String` de un solo carácter:

```dart
print(word[0]);                // h
print(word[word.length - 1]);  // o
```

Leer un índice fuera de la cadena (como `word[5]`) lanza un error.

# --instructions--

Dada la cadena `word`, guarda en un `int` llamado `size` su número de caracteres, en un `String` llamado `first` su primer carácter, y en un `String` llamado `last` su último carácter.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var word = 'Flutter';
```

# --after-seed--

```dart
  return [size, first, last];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`size` debería ser `7`

```dart
  test('test1', () {
    expect(result()[0], 7, reason: '--err-t1--');
  });
```

`first` debería ser `'F'`

```dart
  test('test2', () {
    expect(result()[1], 'F', reason: '--err-t2--');
  });
```

`last` debería ser `'r'`

```dart
  test('test3', () {
    expect(result()[2], 'r', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var word = 'Flutter';
int size = word.length;
String first = word[0];
String last = word[word.length - 1];
```
