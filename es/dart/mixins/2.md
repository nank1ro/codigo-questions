---
language: dart
exerciseType: 1
---

# --description--

El cuerpo de un mixin se parece al cuerpo de una clase: métodos, getters y campos, escritos exactamente igual. La diferencia está en lo que puedes hacer con la declaración en sí.

```dart
mixin Timestamped {
  String get stamp => '[log]';

  String format(String text) => '$stamp $text';
}

class Server with Timestamped {}
class Printer with Timestamped {}
```

El nombre de un mixin también es un **tipo**, así que `Server() is Timestamped` es `true` y una variable puede declararse como `Timestamped t = Server();`. Dos clases sin relación comparten ahora una misma implementación sin que ninguna herede de la otra.

# --instructions--

Declara un mixin `Shouter` con un método `shout` que reciba un `String` `text` y lo devuelva en mayúsculas seguido de un signo de exclamación.

Declara una clase `Speaker` que use `Shouter` y no añada nada propio.

Ejemplo:
```dart
print(Speaker().shout('hi')); // HI!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Speaker().shout('hi')` debería ser `'HI!'`

```dart
  test('test1', () {
    expect(Speaker().shout('hi'), 'HI!', reason: '--err-t1--');
  });
```

`Speaker().shout('Dart rocks')` debería ser `'DART ROCKS!'` y `Speaker().shout('')` debería ser `'!'`

```dart
  test('test2', () {
    expect(Speaker().shout('Dart rocks'), 'DART ROCKS!', reason: '--err-t2--');
    expect(Speaker().shout(''), '!', reason: '--err-t2--');
  });
```

`Speaker` debería obtener `shout` del mixin, así que un `Speaker` también debería ser un `Shouter`

```dart
  test('test3', () {
    expect(Speaker(), isA<Shouter>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Shouter {
  String shout(String text) => '${text.toUpperCase()}!';
}

class Speaker with Shouter {}
```
