---
language: dart
exerciseType: 4
---

# --description--

Un mixin con una cláusula `on` lee los miembros de su superclase como si fueran suyos, y eso es lo que lo convierte en un buen sitio para un comportamiento que decora un tipo existente:

```dart
class Shape {
  String get kind => 'shape';
}

mixin Printable on Shape {
  void show() {
    print('a $kind');
  }
}

class Square extends Shape with Printable {
  @override
  String get kind => 'square';
}
```

`Square` sobrescribe `kind`, y `show` recoge la sobrescritura automáticamente: el mixin siempre llama al miembro sobre el objeto real.

# --instructions--

Ordena los elementos para definir una clase `Shape` con un getter `kind` que devuelva `'shape'`, un mixin `Printable` restringido a `Shape` con un método `show` que imprima `a ` seguido de `kind`, una clase `Square` que extienda `Shape`, use `Printable` y sobrescriba `kind` con `'square'`, y una `main` que llame a `show` sobre un `Square`. Define `Shape` primero, luego `Printable`, luego `Square`, luego `main`. El programa debe imprimir:

```
a square
```

# --answers--

```dart
class Shape {
```

```dart
  String get kind => 'shape';
```

```dart
}
```

```dart
mixin Printable on Shape {
```

```dart
  void show() {
    print('a $kind');
  }
```

```dart
}
```

```dart
class Square extends Shape with Printable {
```

```dart
  @override
  String get kind => 'square';
```

```dart
}
```

```dart
void main() {
```

```dart
  Square().show();
```

```dart
}
```

# --solutions--

```dart
class Shape {
  String get kind => 'shape';
}

mixin Printable on Shape {
  void show() {
    print('a $kind');
  }
}

class Square extends Shape with Printable {
  @override
  String get kind => 'square';
}

void main() {
  Square().show();
}
```

# --output--

a square
