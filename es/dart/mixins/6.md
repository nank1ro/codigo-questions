---
language: dart
exerciseType: 4
---

# --description--

Juntando las piezas, un programa que usa un mixin tiene tres partes: la declaración `mixin`, una o más clases que lo incorporan `with`, y el código que llama al miembro compartido.

```dart
mixin Barker {
  String bark() => 'Woof!';
}

class Dog with Barker {}

void main() {
  print(Dog().bark()); // Woof!
}
```

Las declaraciones de nivel superior pueden escribirse en cualquier orden en Dart, pero leer un archivo de arriba abajo es más fácil cuando el mixin va antes de las clases que lo usan.

# --instructions--

Ordena los elementos para definir un mixin `Purrer` con un método `purr` que devuelva `Purr...`, una clase `Cat` que lo use, y una `main` que imprima el resultado de `purr`. Define el mixin primero, luego la clase, luego `main`. El programa debe imprimir:

```
Purr...
```

# --answers--

```dart
mixin Purrer {
```

```dart
  String purr() => 'Purr...';
```

```dart
}
```

```dart
class Cat with Purrer {}
```

```dart
void main() {
```

```dart
  print(Cat().purr());
```

```dart
}
```

# --solutions--

```dart
mixin Purrer {
  String purr() => 'Purr...';
}

class Cat with Purrer {}

void main() {
  print(Cat().purr());
}
```

# --output--

Purr...
