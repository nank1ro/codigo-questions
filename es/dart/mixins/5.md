---
language: dart
exerciseType: 1
---

# --description--

Un mixin puede declarar un miembro **sin cuerpo**. Ese miembro es abstracto: el mixin lo usa, y la clase que incorpora el mixin tiene que proporcionarlo.

```dart
mixin Greeting {
  String get name;                       // no body: the class provides it

  String greet() => 'Hello, $name!';
}

class Person with Greeting {
  @override
  final String name;

  Person(this.name);
}

void main() {
  print(Person('Ada').greet()); // Hello, Ada!
}
```

El mixin aporta el comportamiento, la clase aporta los datos. Un campo en la clase, como `final String name;`, es suficiente para satisfacer un getter abstracto del mismo nombre.

# --instructions--

Declara un mixin `Labelled` con:
- un getter abstracto `title` de tipo `String`, declarado sin cuerpo
- un método `label` que devuelva `title` entre corchetes

Declara una clase `Book` que use `Labelled`, tenga un campo `final String` `title` y un constructor que lo reciba.

Ejemplo:
```dart
print(Book('Dune').label()); // [Dune]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Book('Dune').label()` debería ser `'[Dune]'`

```dart
  test('test1', () {
    expect(Book('Dune').label(), '[Dune]', reason: '--err-t1--');
  });
```

`Book('').label()` debería ser `'[]'` y `Book('a b').label()` debería ser `'[a b]'`

```dart
  test('test2', () {
    expect(Book('').label(), '[]', reason: '--err-t2--');
    expect(Book('a b').label(), '[a b]', reason: '--err-t2--');
  });
```

`Book` debería conservar su `title` y ser un `Labelled`

```dart
  test('test3', () {
    final book = Book('Dart');
    expect(book.title, 'Dart', reason: '--err-t3--');
    expect(book, isA<Labelled>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Labelled {
  String get title;

  String label() => '[$title]';
}

class Book with Labelled {
  @override
  final String title;

  Book(this.title);
}
```
