---
language: dart
exerciseType: 1
---

# --description--

El cuerpo de la clase está al final mismo de la cadena, así que un miembro declarado en la clase **sobrescribe** el mismo miembro que venga de cualquiera de sus mixins. Dentro de la sobrescritura, **`super`** alcanza la versión que el mixin proporcionó:

```dart
mixin Polite {
  String greet() => 'Hello';
}

class Host with Polite {
  @override
  String greet() => '${super.greet()}, welcome!';
}

class Guest with Polite {}

void main() {
  print(Host().greet());  // Hello, welcome!
  print(Guest().greet()); // Hello
}
```

El mixin en sí queda intacto: `Guest` sigue obteniendo el `greet` original. `super.greet()` es lo que permite a `Host` construir sobre el comportamiento compartido en lugar de copiarlo.

# --instructions--

Declara un mixin `Signed` con un método `sign` que devuelva `'signed'`.

Declara una clase `Letter` que use `Signed` y sobrescriba `sign` de modo que devuelva el resultado del mixin seguido de `' by Ada'`, obtenido llamando a `super`.

Declara una clase `Note` que use `Signed` y no sobrescriba nada.

Ejemplo:
```dart
print(Letter().sign()); // signed by Ada
print(Note().sign());   // signed
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Letter().sign()` debería ser `'signed by Ada'`

```dart
  test('test1', () {
    expect(Letter().sign(), 'signed by Ada', reason: '--err-t1--');
  });
```

`Note().sign()` debería seguir siendo `'signed'`, porque el mixin no cambia

```dart
  test('test2', () {
    expect(Note().sign(), 'signed', reason: '--err-t2--');
  });
```

Ambas clases deberían usar el mixin, y la sobrescritura debería usarse también a través del tipo del mixin

```dart
  test('test3', () {
    final Signed letter = Letter();
    expect(letter.sign(), 'signed by Ada', reason: '--err-t3--');
    expect(Note(), isA<Signed>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Signed {
  String sign() => 'signed';
}

class Letter with Signed {
  @override
  String sign() => '${super.sign()} by Ada';
}

class Note with Signed {}
```
