---
language: dart
exerciseType: 1
---

# --description--

Una vez que un mixin tiene una cláusula `on`, puede **sobrescribir** un miembro de ese tipo y llamar a **`super`** para alcanzar la versión que está debajo de él en la cadena:

```dart
class Logger {
  String log(String message) => message;
}

mixin Timestamped on Logger {
  @override
  String log(String message) => '[12:00] ${super.log(message)}';
}

class AppLogger extends Logger with Timestamped {}

void main() {
  print(AppLogger().log('started')); // [12:00] started
}
```

`super.log` no es el `log` del propio mixin, es el que está debajo de él, así que no hay recursión infinita. Apila varios mixins así con `with A, B` y cada uno envuelve al anterior: la llamada entra primero en el **último** mixin y baja hasta la superclase.

# --instructions--

Declara una clase `Pipeline` con un método `run` que reciba un `String` `input` y lo devuelva sin cambios.

Declara un mixin `Trimmed` sobre `Pipeline` que sobrescriba `run` pasando la entrada recortada a `super.run`, y un mixin `Upper` sobre `Pipeline` que sobrescriba `run` devolviendo el resultado de `super.run` en mayúsculas.

Declara una clase `TextPipeline` que extienda `Pipeline` y use `Trimmed` y luego `Upper`, y una clase `TrimOnly` que extienda `Pipeline` y use solo `Trimmed`.

Ejemplo:
```dart
print(TextPipeline().run('  hi  ')); // HI
print(TrimOnly().run('  hi  '));     // hi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`TextPipeline().run('  hi  ')` debería ser `'HI'`

```dart
  test('test1', () {
    expect(TextPipeline().run('  hi  '), 'HI', reason: '--err-t1--');
    expect(TextPipeline().run('dart'), 'DART', reason: '--err-t1--');
  });
```

`Pipeline().run('  hi  ')` debería seguir devolviendo la entrada sin cambios

```dart
  test('test2', () {
    expect(Pipeline().run('  hi  '), '  hi  ', reason: '--err-t2--');
  });
```

`TrimOnly().run('  hi  ')` debería ser `'hi'`, y cada clase debería usar los mixins que necesita

```dart
  test('test3', () {
    expect(TrimOnly().run('  hi  '), 'hi', reason: '--err-t3--');
    expect(TrimOnly(), isA<Trimmed>(), reason: '--err-t3--');
    expect(TextPipeline(), isA<Upper>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Pipeline {
  String run(String input) => input;
}

mixin Trimmed on Pipeline {
  @override
  String run(String input) => super.run(input.trim());
}

mixin Upper on Pipeline {
  @override
  String run(String input) => super.run(input).toUpperCase();
}

class TextPipeline extends Pipeline with Trimmed, Upper {}

class TrimOnly extends Pipeline with Trimmed {}
```
