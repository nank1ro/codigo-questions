---
language: dart
exerciseType: 1
---

# --description--

El método `.update(key, update)` reemplaza el valor de una clave existente. El segundo argumento es una función que recibe el valor actual y devuelve el nuevo. Si la clave falta, `.update()` lanza un error, a menos que pases una función `ifAbsent` que produzca el valor inicial:

```dart
var stock = {'apple': 3};
stock.update('apple', (n) => n + 1); // apple pasa a 4
stock.update('kiwi', (n) => n + 1, ifAbsent: () => 1); // kiwi se agrega con 1
print(stock); // {apple: 4, kiwi: 1}
```

# --instructions--

Dado el mapa `stock`, usa `.update()` para duplicar el valor de `'apple'`, luego usa `.update()` con `ifAbsent` sobre la clave `'kiwi'` para que se agregue con el valor `5`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var stock = {'apple': 3, 'pear': 1};
```

# --after-seed--

```dart
  return stock;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`stock` debería ser `{'apple': 6, 'pear': 1, 'kiwi': 5}`

```dart
  test('test1', () {
    expect(result(), {'apple': 6, 'pear': 1, 'kiwi': 5}, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var stock = {'apple': 3, 'pear': 1};
stock.update('apple', (n) => n * 2);
stock.update('kiwi', (n) => n * 2, ifAbsent: () => 5);
```
