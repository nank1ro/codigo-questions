---
language: dart
exerciseType: 2
---

# --description--

El método `.remove(key)` elimina una clave y su valor del mapa. Devuelve el valor eliminado, o `null` si la clave no estaba:

```dart
var ages = {'Ann': 30, 'Bob': 25};
int? removed = ages.remove('Ann');
print(removed); // 30
print(ages); // {Bob: 25}
```

La propiedad `.length` devuelve el número de pares clave-valor:

```dart
print(ages.length); // 1
```

# --instructions--

Completa el código para eliminar a `Bob` del mapa y luego imprimir cuántos pares quedan.

# --seed--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.[/]('Bob');
print(ages.[/]);
```

# --answers--

- remove
- length
- delete
- removeAt
- size
- count

# --solutions--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.remove('Bob');
print(ages.length);
```

# --output--

2
