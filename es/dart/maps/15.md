---
language: dart
exerciseType: 1
---

# --description--

El método `.putIfAbsent(key, ifAbsent)` agrega un par **solo si** la clave todavía no está en el mapa. El segundo argumento es una función que produce el valor. Si la clave ya existe, el mapa queda intacto. En ambos casos se devuelve el valor que ahora está almacenado bajo la clave:

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99); // Ann ya está, nada cambia
ages.putIfAbsent('Bob', () => 25); // Bob se agrega
print(ages); // {Ann: 30, Bob: 25}
```

# --instructions--

Dado el mapa `ages`, usa `.putIfAbsent()` dos veces: primero para la clave `'Ann'` con el valor `99`, luego para la clave `'Bob'` con el valor `25`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var ages = {'Ann': 30};
```

# --after-seed--

```dart
  return ages;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`ages` debería ser `{'Ann': 30, 'Bob': 25}`

```dart
  test('test1', () {
    expect(result(), {'Ann': 30, 'Bob': 25}, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99);
ages.putIfAbsent('Bob', () => 25);
```
