---
language: dart
exerciseType: 2
---

# --description--

Una asignación con `map[key] = value` **agrega** un nuevo par, cuando la clave todavía no está en el mapa, o **actualiza** el valor almacenado bajo una clave existente:

```dart
var ages = {'Ann': 30};
ages['Bob'] = 25; // agrega Bob
ages['Ann'] = 31; // actualiza Ann
print(ages); // {Ann: 31, Bob: 25}
```

Las claves nuevas se añaden después de las existentes, así que un mapa recuerda el orden de inserción.

# --instructions--

Completa el código para actualizar la cantidad de `apple` a `5` y luego agregar `pear` con una cantidad de `2`.

# --seed--

```dart
var stock = {'apple': 3};
stock[/] = 5;
stock[/] = 2;
print(stock);
```

# --answers--

- ['apple']
- ['pear']
- .apple
- ('pear')
- 'apple'
- [0]

# --solutions--

```dart
var stock = {'apple': 3};
stock['apple'] = 5;
stock['pear'] = 2;
print(stock);
```

# --output--

{apple: 5, pear: 2}
