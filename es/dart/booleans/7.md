---
language: dart
exerciseType: 1
---

# --instructions--

Declara una variable booleana llamada `isReady` y asígnable el valor `true`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isReady` debe ser igual a `true`.

```dart
  test('test1', () {
    expect(isReady, true, reason: '--err-t1--');
  });
```

`isReady` debe ser de tipo `bool`.

```dart
  test('test2', () {
    expect(isReady, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isReady = true;
```
