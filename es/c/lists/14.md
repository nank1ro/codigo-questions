---
language: c
exerciseType: 4
---

# --description--

Invertir una lista gira cada puntero `next`, en el sitio, con tres punteros: `prev` es la parte ya invertida, `head` el nodo que se está procesando y `next` una copia del resto de la lista, guardada antes de cambiar el enlace. En cada paso el nodo actual se apunta hacia `prev`, y luego tanto `prev` como `head` avanzan:
```c
Node *next = head->next;
head->next = prev;
prev = head;
head = next;
```
Cuando `head` llega a `NULL` todos los enlaces se han girado y `prev` es la nueva cabeza.

# --instructions--

Ordena las líneas para definir `reverse`, que invierte una lista en el sitio y devuelve su nueva cabeza, y un `main` que imprime la lista invertida `1, 2, 3`. Define la función antes de `main`

# --answers--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
```

```c
Node *reverse(Node *head) {
    Node *prev = NULL;
```

```c
    while (head != NULL) {
        Node *next = head->next;
```

```c
        head->next = prev;
```

```c
        prev = head;
```

```c
        head = next;
    }
```

```c
    return prev;
}
```

```c
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
```

```c
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
Node *reverse(Node *head) {
    Node *prev = NULL;
    while (head != NULL) {
        Node *next = head->next;
        head->next = prev;
        prev = head;
        head = next;
    }
    return prev;
}
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --output--

3
2
1
