---
language: c
exerciseType: 2
---

# --description--

Quitar el primer nodo es el espejo de `push_front`: guarda la dirección del segundo nodo, libera el primero y devuelve la dirección guardada como la nueva cabeza. Quien llama guarda el resultado de nuevo en su variable de cabeza:
```c
Node *next = head->next;
free(head);
return next;
```
Hacer pop hasta que la cabeza sea `NULL` libera toda la lista, un nodo por llamada.

# --instructions--

Completa `pop_front` para que libere el primer nodo y devuelva el segundo como la nueva cabeza

# --seed--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

Node *pop_front(Node *head) {
    Node *next = [/];
    [/](head);
    return [/];
}

int main() {
    Node *head = NULL;
    head = push_front(head, 3);
    head = push_front(head, 2);
    head = push_front(head, 1);
    head = pop_front(head);
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        head = pop_front(head);
    }
    return 0;
}
```

# --answers--

- head->next
- free
- next
- head
- NULL

# --solutions--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

Node *pop_front(Node *head) {
    Node *next = head->next;
    free(head);
    return next;
}

int main() {
    Node *head = NULL;
    head = push_front(head, 3);
    head = push_front(head, 2);
    head = push_front(head, 1);
    head = pop_front(head);
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        head = pop_front(head);
    }
    return 0;
}
```

# --output--

2
3
