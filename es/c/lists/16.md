---
language: c
exerciseType: 2
---

# --description--

Todo junto: una lista a menudo se construye a partir de un array. Insertar al principio invierte el orden, así que el array se recorre hacia **atrás**, desde el último elemento al primero, y el primer elemento acaba en la cabeza. El programa luego imprime la lista con un recorrido y la libera nodo a nodo, de modo que cada `malloc` vaya acompañado de un `free`.

# --instructions--

Completa `push_front` y el bucle final para que el programa construya la lista `10, 20, 30` a partir del array, la imprima y libere cada nodo

# --seed--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = [/];
    return [/];
}

int main() {
    int values[3] = {10, 20, 30};
    Node *head = NULL;
    for (int i = 2; i >= 0; i--) {
        head = push_front(head, values[i]);
    }
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        Node *next = head->next;
        [/](head);
        head = next;
    }
    return 0;
}
```

# --answers--

- head
- node
- NULL
- free
- values[i]

# --solutions--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

int main() {
    int values[3] = {10, 20, 30};
    Node *head = NULL;
    for (int i = 2; i >= 0; i--) {
        head = push_front(head, values[i]);
    }
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        Node *next = head->next;
        free(head);
        head = next;
    }
    return 0;
}
```

# --output--

10
20
30
