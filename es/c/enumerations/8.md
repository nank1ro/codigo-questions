---
language: c
exerciseType: 2
---

# --description--

Escribir `enum Color` cada vez es tedioso. Con `typedef` le das a la enumeración un nombre de tipo corto, y la enumeración en sí puede permanecer anónima:
```c
typedef enum { RED, GREEN, BLUE } Color;

Color favorite = BLUE;
```
El nuevo nombre `Color` se usa por sí solo, sin la palabra clave `enum` delante.

# --instructions--

Completa el código para definir la enumeración anónima con las constantes `OFF` y `ON` bajo el nombre de tipo `State`, y luego declara `lamp` con ese tipo

# --seed--

```c
typedef [/] { OFF, ON } State;
[/] lamp = ON;
printf("%d\n", lamp);
```

# --answers--

- enum
- struct
- State
- enum State
- typedef

# --solutions--

```c
typedef enum { OFF, ON } State;
State lamp = ON;
printf("%d\n", lamp);
```

# --output--

1
