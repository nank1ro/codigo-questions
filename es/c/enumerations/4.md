---
language: c
exerciseType: 2
---

# --description--

Una enumeración también es un tipo: puedes declarar una variable de ese tipo escribiendo `enum` seguido del nombre de la enumeración, y asignarle una de sus constantes:
```c
enum Color favorite = GREEN;
```
Como las constantes son enteros, comparas variables de tipo enumeración con los operadores habituales:
```c
if (favorite == GREEN) {
    printf("Green it is\n");
}
```

# --instructions--

Completa el código para declarar la variable `light` de tipo `enum Light` con el valor `GREEN`, de modo que el programa imprima `Go`

# --seed--

```c
enum Light { RED, YELLOW, GREEN };
[/] Light light = [/];
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --answers--

- enum
- struct
- GREEN
- RED
- YELLOW
- "GREEN"

# --solutions--

```c
enum Light { RED, YELLOW, GREEN };
enum Light light = GREEN;
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --output--

Go
