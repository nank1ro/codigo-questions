---
language: c
exerciseType: 4
---

# --description--

Un parámetro `bool` se puede usar directamente como operando de `&&` o `||`, sin compararlo con `true`: escribe `age >= 18 && citizen`, no `citizen == true`.

# --instructions--

Ordena las líneas para definir `can_vote`, que devuelve `true` cuando `age` es al menos `18` y `citizen` es `true`, y una `main` que imprime el resultado para `20, true` y luego para `20, false`. Define la función antes de `main`

# --answers--

```c
#include <stdio.h>
#include <stdbool.h>
```

```c
bool can_vote(int age, bool citizen) {
```

```c
    return age >= 18 && citizen;
}
```

```c
int main() {
```

```c
    printf("%d\n", can_vote(20, true));
```

```c
    printf("%d\n", can_vote(20, false));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
#include <stdbool.h>
bool can_vote(int age, bool citizen) {
    return age >= 18 && citizen;
}
int main() {
    printf("%d\n", can_vote(20, true));
    printf("%d\n", can_vote(20, false));
    return 0;
}
```

# --output--

1
0
