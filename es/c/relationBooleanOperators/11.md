---
language: c
exerciseType: 2
---

# --instructions--

Verifica si `word` **NO** es igual a `dog`, imprimiendo el resultado booleano.
Ten en cuenta que `strcmp` devuelve `0` solo cuando las cadenas son **iguales**, así que no te confundas pensando que el 0 que devuelve es como el booleano falso

# --seed--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat")[/][/];
printf("%d\n", [/]);
```

# --answers--

- word
- "dog"
-  == 
-  = 
- dog
-  != 
- 0
- are_words_equal

# --solutions--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat") == 0;
printf("%d\n", are_words_equal);
```

# --output--

1
