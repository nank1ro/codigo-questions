---
language: c
exerciseType: 4
---

# --description--

Cada operador bit a bit binario tiene una forma de **asignación compuesta** que actualiza una variable en el sitio: `&=`, `|=`, `^=`, `<<=` y `>>=`.
```c
unsigned int x = 12;
x &= 10;  // same as x = x & 10;
x |= 1;   // same as x = x | 1;
x ^= 3;   // same as x = x ^ 3;
x <<= 1;  // same as x = x << 1;
x >>= 2;  // same as x = x >> 2;
```
Se leen mejor que repetir el nombre de la variable y son la forma habitual de cambiar los bits de una variable de flags.

# --instructions--

Ordena las líneas para que `flags` empiece en `0x01`, luego se encienda el bit que vale `4` con `|=`, después se desplace 2 posiciones a la izquierda con `<<=` y finalmente se imprima

# --answers--

```c
unsigned int flags = 0x01;
```

```c
flags |= 0x04;
```

```c
flags <<= 2;
```

```c
printf("%u\n", flags);
```

# --solutions--

```c
unsigned int flags = 0x01;
flags |= 0x04;
flags <<= 2;
printf("%u\n", flags);
```

# --output--

20
