---
language: c
exerciseType: 4
---

# --description--

Los structs pueden guardarse en un array como cualquier otro tipo. Cada elemento se inicializa con sus propias llaves, y un bucle los recorre uno a uno, indexando primero y usando el punto después:
```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};

for (int i = 0; i < 3; i++) {
    printf("%d\n", path[i].y);
}
```

# --instructions--

Ordena las líneas para definir el tipo `Point`, guardar tres puntos en `path`, sumar sus miembros `x` en `sum` e imprimirlo. Declara `path` antes que `sum`

# --answers--

```c
typedef struct { int x; int y; } Point;
```

```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
```

```c
int sum = 0;
```

```c
for (int i = 0; i < 3; i++) {
```

```c
    sum += path[i].x;
```

```c
}
```

```c
printf("%d\n", sum);
```

# --solutions--

```c
typedef struct { int x; int y; } Point;
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
int sum = 0;
for (int i = 0; i < 3; i++) {
    sum += path[i].x;
}
printf("%d\n", sum);
```

# --output--

6
