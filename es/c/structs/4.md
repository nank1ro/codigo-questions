---
language: c
exerciseType: 2
---

# --description--

Escribir `struct Point` cada vez es verboso. Con `typedef` le das al struct un nombre de tipo corto, y el struct en sí puede quedarse anónimo:
```c
typedef struct {
    double celsius;
} Temperature;

Temperature t = {21.5};
```
El nuevo nombre `Temperature` se usa por sí solo, sin la palabra clave `struct` delante. Es la forma más habitual de declarar structs en programas reales.

# --instructions--

Completa el código para definir el struct anónimo con el miembro `celsius` bajo el nombre de tipo `Temperature`, y luego declara `t` con ese tipo

# --seed--

```c
typedef [/] {
    double celsius;
} Temperature;
[/] t = {21.5};
printf("%.1f\n", t.celsius);
```

# --answers--

- struct
- enum
- typedef
- Temperature
- struct Temperature
- Temperature t

# --solutions--

```c
typedef struct {
    double celsius;
} Temperature;
Temperature t = {21.5};
printf("%.1f\n", t.celsius);
```

# --output--

21.5
