---
language: c
exerciseType: 4
---

# --description--

`printf` no tiene ningún especificador para un struct entero. La solución habitual es una función pequeña que imprime los miembros con un formato fijo, para que todo el programa muestre el valor de la misma manera:
```c
void print_person(Person p) {
    printf("%s (%d)\n", p.name, p.age);
}
```

# --instructions--

Ordena las líneas para definir el tipo `Point`, una función `print_point` que imprima un punto como `(x, y)` y un `main` que imprima el punto `(3, 4)`. Define la función antes de `main`

# --answers--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
```

```c
void print_point(Point p) {
```

```c
    printf("(%d, %d)\n", p.x, p.y);
```

```c
}
```

```c
int main() {
```

```c
    Point p = {3, 4};
```

```c
    print_point(p);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
void print_point(Point p) {
    printf("(%d, %d)\n", p.x, p.y);
}
int main() {
    Point p = {3, 4};
    print_point(p);
    return 0;
}
```

# --output--

(3, 4)
