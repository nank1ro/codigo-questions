---
language: c
exerciseType: 2
---

# --description--

Veamos el operador **asignación de división** `/=`.
Divide la variable por un valor y asigna el resultado a esa variable.
Entonces los siguientes son exactamente lo mismo
```c
num = num / 5;
// es igual a
num /= 5;
```

# --instructions--

Calcula el cociente de `result` dividiendo su valor por `5` usando el operador *asignación de división*, luego imprime su valor

# --seed--

```c
int result = 50;
[/][/][/];
printf("%d\n", result);
```

# --answers--

-  / 
-  /= 
-  =/ 
- 5
- 25
- result

# --solutions--

```c
int result = 50;
result /= 5;
printf("%d\n", result);
```

# --output--

10
