---
language: c
exerciseType: 4
---

# --description--

No encadenes comparaciones como en matemáticas. `1 <= x <= 10` compila, pero se evalúa como `(1 <= x) <= 10`: la primera comparación da `0` o `1`, y eso se compara después con `10`, así que toda la expresión es siempre `1`.
```c
int x = 20;
printf("%d\n", 1 <= x <= 10);       // prints "1" (wrong!)
printf("%d\n", 1 <= x && x <= 10);  // prints "0"
```
Escribe siempre ambas comparaciones explícitamente y únelas con `&&`.

# --instructions--

Ordena las líneas para construir un programa que almacene la comparación encadenada en `chained`, la comprobación de rango correcta en `correct` e imprima ambos. Define `chained` antes de `correct`

# --answers--

```c
#include <stdio.h>

int main() {
```

```c
    int x = 20;
```

```c
    int chained = 1 <= x <= 10;
```

```c
    int correct = 1 <= x && x <= 10;
```

```c
    printf("%d %d\n", chained, correct);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>

int main() {
    int x = 20;
    int chained = 1 <= x <= 10;
    int correct = 1 <= x && x <= 10;
    printf("%d %d\n", chained, correct);
    return 0;
}
```

# --output--

1 0
