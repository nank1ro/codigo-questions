---
language: c
exerciseType: 2
---

# --description--

Desde C99 el encabezado `stdbool.h` proporciona el tipo `bool` y las constantes `true` (que es `1`) y `false` (que es `0`):
```c
#include <stdbool.h>

bool open = true;
bool full = 3 > 5;
printf("%d %d\n", open, full); // prints "1 0"
```
Un `bool` sigue siendo un entero por debajo: se imprime con `%d`, y funciona con `&&`, `||` y `!` exactamente como el resultado de una comparación. Solo deja la intención más clara que un `int` normal.

# --instructions--

Completa el código para que `open` sea la constante booleana para verdadero y `closed` sea su negación, y luego imprime ambos

# --seed--

```c
bool open = [/];
bool closed = [/]open;
printf("%d %d\n", open, closed);
```

# --answers--

- true
- !
- false
- -
- ~

# --solutions--

```c
bool open = true;
bool closed = !open;
printf("%d %d\n", open, closed);
```

# --output--

1 0
