---
language: c
exerciseType: 4
---

# --description--

Uniendo todo: elige el tipo según la clase de valor, haz que cada especificador de `printf` coincida con el tipo de su argumento, y convierte cuando un cálculo deba realizarse en un tipo distinto al de sus operandos.

# --instructions--

Ordena las líneas para construir un programa que almacene `7` en `total` y `2` en `count`, calcule `total` dividido entre `count` como un `double` en `avg`, e imprima la división entera `total / count` seguida de `avg` con un decimal

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int total = 7, count = 2;
```

```c
    double avg = (double) total / count;
```

```c
    printf("%d %.1f\n", total / count, avg);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int total = 7, count = 2;
    double avg = (double) total / count;
    printf("%d %.1f\n", total / count, avg);
    return 0;
}
```

# --output--

3 3.5
