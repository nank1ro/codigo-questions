---
language: c
exerciseType: 4
---

# --instructions--

Ordena las líneas para que el programa compare `password` con `attempt` e imprima `Access granted`. Declara `password` antes que `attempt` y coloca cada mensaje dentro de su propia rama

# --answers--

```c
char password[] = "secret";
```

```c
char attempt[] = "secret";
```

```c
if (strcmp(password, attempt) == 0) {
```

```c
    printf("Access granted\n");
```

```c
} else {
```

```c
    printf("Access denied\n");
```

```c
}
```

# --solutions--

```c
char password[] = "secret";
char attempt[] = "secret";
if (strcmp(password, attempt) == 0) {
    printf("Access granted\n");
} else {
    printf("Access denied\n");
}
```

# --output--

Access granted
