---
language: kotlin
exerciseType: 4
---

# --description--

Como un `Result` es un valor ordinario, se puede guardar en un `val` y consultarse tantas veces como quieras:

```kotlin
val result = runCatching { "abc".toInt() }

println(result.getOrElse { 0 })  // 0
println(result.getOrNull())      // null
println(result.isSuccess)        // false
```
Un `try`/`catch` no puede hacer esto. Ahí el resultado se maneja una sola vez, en el momento del fallo, y luego desaparece. Un `Result` conserva el fallo, así que el código que reacciona a él no tiene que estar en el lugar donde ocurrió.

# --instructions--

Ordena los elementos para que el programa almacene el resultado de la conversión, luego imprima el valor recuperado, después el resultado crudo y por último si el intento tuvo éxito.

# --answers--

```kotlin
val result = runCatching { "abc".toInt() }
```

```kotlin
val value = result.getOrElse { 0 }
```

```kotlin
println("value: $value")
```

```kotlin
println("raw: ${result.getOrNull()}")
```

```kotlin
println("success: ${result.isSuccess}")
```

# --solutions--

```kotlin
val result = runCatching { "abc".toInt() }
val value = result.getOrElse { 0 }
println("value: $value")
println("raw: ${result.getOrNull()}")
println("success: ${result.isSuccess}")
```

# --output--

value: 0
raw: null
success: false
