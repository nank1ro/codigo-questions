---
language: kotlin
exerciseType: 2
---

# --description--

Un bloque `finally` puede añadirse al final. Se ejecuta **pase lo que pase**: después de un `try` exitoso, después de que un `catch` se haya recuperado, e incluso cuando la excepción no se captura en absoluto.

```kotlin
try {
    println("reading")
    println("abc".toInt())
} catch (e: NumberFormatException) {
    println("bad number")
} finally {
    println("closing")
}
```
```
reading
bad number
closing
```
Eso lo convierte en el lugar para la limpieza que no debe omitirse, como cerrar un archivo. Un `try` necesita al menos un `catch` o un `finally`, pero puede tener ambos.

# --instructions--

Completa el código para que la conversión errónea se capture y la línea de limpieza siempre se ejecute.

# --seed--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } [/] (e: NumberFormatException) {
        println("bad number")
    } [/] {
        println("closing")
    }
}
```

# --answers--

- catch
- finally
- try
- throw
- else
- do

# --solutions--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } catch (e: NumberFormatException) {
        println("bad number")
    } finally {
        println("closing")
    }
}
```

# --output--

bad number
closing
