---
language: kotlin
exerciseType: 2
---

# --description--

Como `try` es una expresión, puede usarse en cualquier lugar donde se espera un valor — incluido como cuerpo completo de una función escrita con `=`:
```kotlin
fun length(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    -1
}
```
Ambos bloques deben producir un valor del mismo tipo, aquí `Int`. Escribe el valor de reserva como la última expresión del bloque `catch`; no hay ningún `return` dentro de ninguno de los bloques.

# --instructions--

Completa la función para que devuelva el número en `text`, y `0` cuando el texto no es un número.

# --seed--

```kotlin
fun size(text: String): Int = [/] {
    text.toInt()
} [/] (e: NumberFormatException) {
    [/]
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --answers--

- try
- catch
- 0
- finally
- throw
- 1

# --solutions--

```kotlin
fun size(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    0
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --output--

15
0
