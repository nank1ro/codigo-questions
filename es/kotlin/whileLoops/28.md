---
language: kotlin
exerciseType: 3
---

# --instructions--

¿Cómo sabemos que este bucle no se detiene?
```kotlin
var isLoading = true

while (isLoading) {
    println("Loading...")
    isLoading = true
}
```

# --answers--

- Porque la variable no se actualiza a falso dentro del bloque de código
- Porque todos los bucles while no se detienen
- Porque la variable se actualiza a falso

# --solutions--

- Porque la variable no se actualiza a falso dentro del bloque de código
