---
language: kotlin
exerciseType: 1
---

# --description--

Un `Int` puede albergar números enteros hasta unos dos mil millones, más precisamente hasta `Int.MAX_VALUE`, que es `2147483647`.
Un literal de número entero demasiado grande para un `Int` se infiere automáticamente como `Long`, y puedes forzar un `Long` para cualquier literal con el sufijo `L`:
```kotlin
val big = 3000000000  // Long, too big for an Int
val small = 3L        // Long, thanks to the suffix
```
De la misma manera el sufijo `f` convierte un literal decimal en un `Float`: `val ratio = 0.5f`.
Los números largos son difíciles de leer, así que Kotlin te permite colocar guiones bajos `_` en cualquier lugar entre los dígitos; el compilador los ignora:
```kotlin
val population = 8_000_000_000L
val million = 1_000_000
println(million) // 1000000
```

# --instructions--

Declara una variable `Long` llamada `distance` igual a 12 mil millones, escrita con guiones bajos como `12_000_000_000`, y una variable `Float` llamada `ratio` igual a `0.75`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`distance` debe ser igual a `12000000000`

```kotlin
    tryCatch(distance == 12000000000L)
```

`distance` debe ser un `Long`

```kotlin
    tryCatch(distance::class.simpleName == "Long")
```

`ratio` debe ser igual a `0.75`

```kotlin
    tryCatch(ratio == 0.75f)
```

`ratio` debe ser un `Float`

```kotlin
    tryCatch(ratio::class.simpleName == "Float")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
val distance = 12_000_000_000L
val ratio = 0.75f
```
