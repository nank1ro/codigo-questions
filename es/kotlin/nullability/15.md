---
language: kotlin
exerciseType: 4
---

# --description--

Una función de extensión puede declararse sobre un **receptor nullable**, así que se puede llamar incluso sobre un valor `null`. Dentro, `this` es nullable y debe comprobarse:
```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val none: String? = null
println(none.orDash()) // -
```
Ten en cuenta que no hace falta `?.` en el punto de llamada: la propia función gestiona el caso `null`.
La biblioteca estándar usa este truco en `isNullOrEmpty()` y `orEmpty()`, que se pueden llamar con seguridad sobre cualquier `String?`.

# --instructions--

¿Puedes ordenar estas líneas para definir la función de extensión `orDash` e imprimir `-` para un nombre faltante, y después `kotlin` para uno presente? Define la función primero

# --answers--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
```

```kotlin
val a: String? = null
val b: String? = "kotlin"
```

```kotlin
println(a.orDash())
```

```kotlin
println(b.orDash())
```

# --solutions--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val a: String? = null
val b: String? = "kotlin"
println(a.orDash())
println(b.orDash())
```

# --output--

-
kotlin
