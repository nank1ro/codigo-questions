---
language: kotlin
exerciseType: 1
---

# --description--

Cuando un `null` significa que quien llamó cometió un error, falla cuanto antes con `requireNotNull`.
Devuelve el valor como no nulo cuando está presente, y lanza un `IllegalArgumentException` cuando es `null`, con un mensaje opcional:
```kotlin
fun greet(name: String?): String {
    val safeName = requireNotNull(name) { "name is required" }
    return "Hello, $safeName"
}
greet(null) // IllegalArgumentException: name is required
```
Después de la llamada, el compilador también aplica una conversión inteligente (smart cast) al propio `name` como `String`, así que `name.length` está permitido a partir de esa línea.
A diferencia de `!!`, el fallo lleva un mensaje claro e indica que el *argumento* era incorrecto.

# --instructions--

Escribe una función `safeUpper(text: String?): String` que devuelva `text` en mayúsculas. Cuando `text` es `null` debe lanzar un `IllegalArgumentException` con el mensaje `text is required`, usando `requireNotNull`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`safeUpper("abc")` debe devolver `"ABC"`

```kotlin
    tryCatch(safeUpper("abc") == "ABC")
```

`safeUpper("Kotlin")` debe devolver `"KOTLIN"`

```kotlin
    tryCatch(safeUpper("Kotlin") == "KOTLIN")
```

`safeUpper(null)` debe lanzar un `IllegalArgumentException`

```kotlin
    var threw = false
    try {
        safeUpper(null)
    } catch (e: IllegalArgumentException) {
        threw = true
    }
    tryCatch(threw)
```

El mensaje de la excepción debe ser `text is required`

```kotlin
    var message = ""
    try {
        safeUpper(null)
    } catch (e: IllegalArgumentException) {
        message = e.message ?: ""
    }
    tryCatch(message == "text is required")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun safeUpper(text: String?): String {
    val value = requireNotNull(text) { "text is required" }
    return value.uppercase()
}
```
