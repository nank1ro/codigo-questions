---
language: kotlin
exerciseType: 2
---

# --description--

La actividad más común de `Set` es probar la pertenencia usando `in` o `contains()`

```kotlin
val numbers = setOf(1, 2, 3)
println(2 in numbers) // prints true
println(numbers.contains(5)) // prints false
```

Como puedes ver arriba, `in` y `contains` devuelven un `Bool` diciendo si el elemento pasado está presente en el conjunto

# --instructions--

Completa el código para comprobar si el menú contiene `pizza`, en caso afirmativo come la pizza, de lo contrario come el primer plato del menú.

# --seed--

```kotlin
val dishes = setOf("rice", "noodles", "pizza")
var selectedDish =  dishes.first();
if ([/][/][/]) {
    selectedDish = "[/]"
}
println("I eat [/]")
```

# --answers--

- "pizza"
- pizza
- contains
-  in 
- dishes
- selectedDish
- pizza
- $selectedDish

# --solutions--

```kotlin
val dishes = setOf("rice", "noodles", "pizza")
var selectedDish =  dishes.first();
if ("pizza" in dishes) {
    selectedDish = "pizza"
}
println("I eat $selectedDish")
```

# --output--

I eat pizza
