---
language: kotlin
exerciseType: 1
---

# --description--

Una función de extensión con exactamente **un** parámetro se puede marcar como `infix`. Una función infix se puede llamar sin el punto ni los paréntesis, con el receptor a la izquierda y el argumento a la derecha, lo que se lee casi como una frase:
```kotlin
infix fun Int.percentOf(total: Int): Int = total * this / 100

println(20 percentOf 50)   // 10
println(20.percentOf(50))  // 10, the normal call still works
```
Kotlin también usa esto para algunas funciones integradas: `1 to "one"` construye un `Pair`, y `1 until 5` construye un rango.

# --instructions--

Escribe una función de extensión infix `toThe(exponent: Int)` sobre `Int` que devuelva el receptor elevado a la potencia dada, de modo que `2 toThe 10` sea `1024`. El exponente nunca es negativo, y cualquier número elevado a `0` es `1`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`2 toThe 10` debe devolver `1024`

```kotlin
    tryCatch((2 toThe 10) == 1024)
```

`3 toThe 3` debe devolver `27`

```kotlin
    tryCatch((3 toThe 3) == 27)
```

`5 toThe 0` debe devolver `1`

```kotlin
    tryCatch((5 toThe 0) == 1)
```

`7.toThe(1)` debe devolver `7`

```kotlin
    tryCatch(7.toThe(1) == 7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
infix fun Int.toThe(exponent: Int): Int {
    var result = 1
    repeat(exponent) { result *= this }
    return result
}
```
