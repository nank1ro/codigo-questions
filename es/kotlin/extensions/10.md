---
language: kotlin
exerciseType: 1
---

# --description--

Una extensión puede trabajar sobre toda una familia de tipos a la vez gracias a un **parámetro de tipo**: un marcador de posición para un tipo, declarado entre corchetes angulares justo después de `fun`, que Kotlin rellena en cada llamada. Esto hace que la extensión sea **genérica**:
```kotlin
fun <T> List<T>.second(): T {
    return this[1]
}
println(listOf(1, 2, 3).second())      // 2
println(listOf("a", "b").second())     // b
```
Con `listOf(1, 2, 3)` el marcador `T` es `Int`, con `listOf("a", "b")` es `String`, así que la misma función devuelve el tipo correcto cada vez.

# --instructions--

Escribe una función de extensión genérica `penultimate()` sobre `List<T>` que devuelva el penúltimo elemento de la lista. La lista siempre tiene al menos dos elementos.

Ejemplo: `listOf(1, 2, 3).penultimate()` devuelve `2`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`listOf(1, 2, 3).penultimate()` debe devolver `2`

```kotlin
    tryCatch(listOf(1, 2, 3).penultimate() == 2)
```

`listOf("a", "b").penultimate()` debe devolver `"a"`

```kotlin
    tryCatch(listOf("a", "b").penultimate() == "a")
```

`listOf(true, false, true, false).penultimate()` debe devolver `true`

```kotlin
    tryCatch(listOf(true, false, true, false).penultimate() == true)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun <T> List<T>.penultimate(): T {
    return this[size - 2]
}
```
