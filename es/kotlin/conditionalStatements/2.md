---
language: kotlin
exerciseType: 2
---

# --description--

Continuemos con el ejemplo anterior.
```kotlin
var niceWeather = true
if (niceWeather) {
    // play outside
}
```
Hemos visto que la sentencia `if` ejecuta el bloque de código solo si la condición es `true`.
Otra cosa importante a considerar está representada por los **llaves** `{}` que indican un bloque de código.

# --instructions--

Corrige la sentencia `if` añadiendo las *llaves* `{}`

# --seed--

```kotlin
var time = 8
if (time < 12) [/]
    println("It's morning")
[/]
```

# --answers--

- if
- ==
- =
- else
- ?
- {
- }

# --solutions--

```kotlin
var time = 8
if (time < 12) {
    println("It's morning")
}
```

# --output--

It's morning
