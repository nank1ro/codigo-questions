---
language: kotlin
exerciseType: 1
---

# --description--

Una `interface` enumera lo que un tipo puede hacer. Sus miembros son abstractos por defecto, pero una interfaz también puede incluir una **implementación por defecto**, un cuerpo que toda clase que la implemente hereda gratis y puede sobrescribir:

```kotlin
interface Greeter {
    val name: String              // abstracto, la clase debe proporcionarlo
    fun greet(): String = "Hi, $name"  // implementación por defecto
}

class Person(override val name: String) : Greeter

class Robot(override val name: String) : Greeter {
    override fun greet(): String = "BEEP $name"
}

println(Person("Ann").greet()) // Hi, Ann
println(Robot("R2").greet())   // BEEP R2
```

Una interfaz no puede almacenar estado (no tiene campos de respaldo), así que una propiedad abstracta debe ser implementada por la clase, normalmente con `override val` en el constructor. A diferencia de una clase, un tipo puede implementar tantas interfaces como quiera.

# --instructions--

Declara una interfaz `Greeter` con una propiedad abstracta `val` `name` de tipo `String` y un método `greet(): String` cuya implementación por defecto devuelva `"Hello, Ann!"` para un `name` igual a `"Ann"`.

Después declara una clase `Person` que implemente `Greeter` y reciba `name` en su constructor, y una clase `Pirate` que también implemente `Greeter` pero que sobrescriba `greet()` para devolver `"Ahoy, Bo!"` para un `name` igual a `"Bo"`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`Person("Ann").greet()` debe devolver `"Hello, Ann!"`

```kotlin
    tryCatch(Person("Ann").greet() == "Hello, Ann!")
```

`Person("Cy").name` debe devolver `"Cy"`

```kotlin
    tryCatch(Person("Cy").name == "Cy")
```

`Pirate("Bo").greet()` debe devolver `"Ahoy, Bo!"`

```kotlin
    tryCatch(Pirate("Bo").greet() == "Ahoy, Bo!")
```

Ambas clases deben poder usarse como un `Greeter`

```kotlin
    val greeters: List<Greeter> = listOf(Person("Ann"), Pirate("Bo"))
    tryCatch(greeters.map { it.greet() } == listOf("Hello, Ann!", "Ahoy, Bo!"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
interface Greeter {
    val name: String
    fun greet(): String = "Hello, $name!"
}

class Person(override val name: String) : Greeter

class Pirate(override val name: String) : Greeter {
    override fun greet(): String = "Ahoy, $name!"
}
```
