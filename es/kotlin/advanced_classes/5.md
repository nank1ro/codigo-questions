---
language: kotlin
exerciseType: 1
---

# --description--

Una clase `sealed` describe un conjunto **cerrado** de alternativas: solo se permiten las subclases escritas en el mismo paquete y módulo, de modo que el compilador conoce todas ellas.

```kotlin
sealed class Shape
data class Circle(val radius: Int) : Shape()
data class Square(val side: Int) : Shape()
```

La ventaja es el **`when` exhaustivo**: cuando haces un `when` sobre un tipo sealed y cubres todas las subclases, puedes omitir la rama `else`. Si añades una nueva subclase más adelante, el compilador avisa de cada `when` que hayas olvidado actualizar, en lugar de tomar el `else` en silencio.

```kotlin
fun name(shape: Shape): String = when (shape) {
    is Circle -> "circle"
    is Square -> "square"
}
```

Después de `is Circle` el valor se convierte automáticamente mediante smart cast, así que `shape.radius` está disponible dentro de esa rama sin ninguna conversión manual.

# --instructions--

Declara una clase sealed `Shape` con dos subclases data class: `Square`, con una propiedad `val` `side` de tipo `Int`, y `Rectangle`, con propiedades `val` `width` y `height` de tipo `Int`.

Después escribe una función `area(shape: Shape): Int` que devuelva el área de la figura, usando un `when` exhaustivo sin rama `else`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`area(Square(3))` debe devolver `9`

```kotlin
    tryCatch(area(Square(3)) == 9)
```

`area(Rectangle(2, 5))` debe devolver `10`

```kotlin
    tryCatch(area(Rectangle(2, 5)) == 10)
```

`area(Square(0))` debe devolver `0`

```kotlin
    tryCatch(area(Square(0)) == 0)
```

`Square` y `Rectangle` deben ser subclases de `Shape`

```kotlin
    val shapes: List<Shape> = listOf(Square(4), Rectangle(3, 3))
    tryCatch(shapes.map { area(it) } == listOf(16, 9))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
sealed class Shape
data class Square(val side: Int) : Shape()
data class Rectangle(val width: Int, val height: Int) : Shape()

fun area(shape: Shape): Int = when (shape) {
    is Square -> shape.side * shape.side
    is Rectangle -> shape.width * shape.height
}
```
