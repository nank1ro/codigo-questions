---
language: kotlin
exerciseType: 4
---

# --description--

Las piezas de este tema suelen combinarse: una `enum class` cuyas entradas llevan sus propias propiedades modela un conjunto fijo de etiquetas, mientras que una `data class` transporta los datos que las acompañan.

```kotlin
enum class Speed(val surcharge: Int) {
    STANDARD(0),
    EXPRESS(15)
}

data class Order(val total: Int, val speed: Speed)

val order = Order(100, Speed.EXPRESS)
println(order.total + order.speed.surcharge) // 115
```

# --instructions--

Ordena los elementos para imprimir el coste total del pedido. Declara primero el enum, después la data class, y luego `main`.

# --answers--

```kotlin
enum class Shipping(val surcharge: Int) { STANDARD(0), EXPRESS(15) }
```

```kotlin
data class Order(val total: Int, val shipping: Shipping)
```

```kotlin
fun main() {
```

```kotlin
    val order = Order(100, Shipping.EXPRESS)
```

```kotlin
    println(order.total + order.shipping.surcharge)
```

```kotlin
}
```

# --solutions--

```kotlin
enum class Shipping(val surcharge: Int) { STANDARD(0), EXPRESS(15) }
data class Order(val total: Int, val shipping: Shipping)
fun main() {
    val order = Order(100, Shipping.EXPRESS)
    println(order.total + order.shipping.surcharge)
}
```

# --output--

115
