---
language: kotlin
exerciseType: 1
---

# --description--

Por cada propiedad del constructor primario, una data class también genera una función `componentN()`: `component1()` para la primera propiedad, `component2()` para la segunda, y así sucesivamente.

Esas funciones hacen posibles las **declaraciones de desestructuración**, donde desempaquetas un objeto en varias variables en una sola línea:

```kotlin
data class Point(val x: Int, val y: Int)

val point = Point(3, 7)
val (x, y) = point
println(x)               // 3
println(point.component2()) // 7
```

El orden de las variables sigue el orden de las propiedades, no sus nombres. Usa `_` para omitir la que no necesites:

```kotlin
val (_, onlyY) = point
```

# --instructions--

Declara una data class `Point` con propiedades `val` `x` e `y` de tipo `Int`.

Después escribe una función `describe(point: Point): String` que use una declaración de desestructuración para leer ambas propiedades y las devuelva con el formato `"x=3, y=7"`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`describe(Point(3, 7))` debe devolver `"x=3, y=7"`

```kotlin
    tryCatch(describe(Point(3, 7)) == "x=3, y=7")
```

`describe(Point(-4, 0))` debe devolver `"x=-4, y=0"`

```kotlin
    tryCatch(describe(Point(-4, 0)) == "x=-4, y=0")
```

`Point` debe generar `component1()` y `component2()`

```kotlin
    val point = Point(1, 2)
    tryCatch(point.component1() == 1 && point.component2() == 2)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
data class Point(val x: Int, val y: Int)

fun describe(point: Point): String {
    val (x, y) = point
    return "x=$x, y=$y"
}
```
