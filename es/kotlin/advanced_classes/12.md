---
language: kotlin
exerciseType: 3
---

# --instructions--

¿Qué ocurre con este programa?

```kotlin
interface Movable {
    fun move(): String = "moving"
}

class Robot : Movable

fun main() {
    println(Robot().move())
}
```

# --answers--

- Imprime `moving`, porque `Robot` hereda la implementación por defecto
- No compila: `Robot` debe sobrescribir `move()`
- No compila: una interfaz no puede contener un cuerpo de función
- Imprime `null`

# --solutions--

- Imprime `moving`, porque `Robot` hereda la implementación por defecto
