---
language: kotlin
exerciseType: 2
---

# --description--

Un `String` es una secuencia de caracteres escrita entre comillas dobles.
La propiedad `length` indica cuántos caracteres contiene una cadena, espacios incluidos:
```kotlin
val greeting = "Hello world"
println(greeting.length) // 11
println("".length)       // 0
```

# --instructions--

Completa el código para imprimir el número de caracteres de `word`

# --seed--

```kotlin
val word = "Kotlin"
println(word.[/])
```

# --answers--

- length
- size
- len
- length()
- count

# --solutions--

```kotlin
val word = "Kotlin"
println(word.length)
```

# --output--

6
