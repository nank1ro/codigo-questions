---
language: kotlin
exerciseType: 4
---

# --description--

Como `split` devuelve una `List`, puedes recorrer sus elementos como en cualquier otra lista:
```kotlin
for (part in "a-b".split("-")) {
    println(part)
}
// prints a, then b
```

# --instructions--

¿Puedes ordenar estas líneas para imprimir cada palabra de `sentence` en mayúsculas, una por línea?

# --answers--

```kotlin
val sentence = "the quick brown fox"
```

```kotlin
val words = sentence.split(" ")
```

```kotlin
for (word in words) {
```

```kotlin
    println(word.uppercase())
```

```kotlin
}
```

# --solutions--

```kotlin
val sentence = "the quick brown fox"
val words = sentence.split(" ")
for (word in words) {
    println(word.uppercase())
}
```

# --output--

THE
QUICK
BROWN
FOX
