---
language: kotlin
exerciseType: 3
---

# --instructions--

¿Qué imprime este programa?

```kotlin
fun main() {
    val words = mutableListOf("a")
    val first = words.apply { add("b") }
    val second = words.run { size }
    println("$first $second")
}
```

# --answers--

- `[a, b] 2`, porque `apply` devuelve la lista y `run` devuelve el resultado del bloque
- `true 2`, porque `apply` devuelve el resultado de `add`
- `[a, b] [a, b]`, porque ambos devuelven la lista
- `2 [a, b]`, porque `apply` devuelve el tamaño y `run` devuelve la lista

# --solutions--

- `[a, b] 2`, porque `apply` devuelve la lista y `run` devuelve el resultado del bloque
