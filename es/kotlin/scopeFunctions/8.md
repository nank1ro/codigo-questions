---
language: kotlin
exerciseType: 1
---

# --description--

Cuando el bloque necesita el objeto como **argumento** de otra cosa, `also` se lee mejor que `apply`: `it` puede pasarse directamente, mientras que `this` tendría que escribirse explícitamente.
```kotlin
val names = mutableListOf<String>()
val user = "ada".also { names.add(it) }
println(user)  // ada
println(names) // [ada]
```
El valor de la expresión sigue siendo `"ada"`: `also` solo lo observa pasar.

# --instructions--

Declara un `val log = mutableListOf<String>()` de nivel superior.

Después escribe una función `record(word: String): String` que devuelva `word` en mayúsculas y que, con `also`, añada ese mismo valor en mayúsculas a `log` antes de devolverlo.

Ejemplo: después de `record("hi")` la función devuelve `HI` y `log` contiene `HI`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`log` debe empezar vacío

```kotlin
    tryCatch(log.isEmpty())
```

`record("hi")` debe devolver `HI`

```kotlin
    tryCatch(record("hi") == "HI")
```

Después de la primera llamada `log` debe contener solo `HI`

```kotlin
    tryCatch(log == listOf("HI"))
```

Una segunda llamada debe añadir a `log` sin perder el primer valor

```kotlin
    record("bye")
    tryCatch(log == listOf("HI", "BYE"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
val log = mutableListOf<String>()

fun record(word: String): String {
    return word.uppercase().also { log.add(it) }
}
```
