---
language: kotlin
exerciseType: 1
---

# --description--

Para leer un valor, indexas el mapa con su clave, usando corchetes o la función `get`:
```kotlin
val ages = mapOf("Alice" to 30, "Bob" to 25)
println(ages["Alice"])    // 30
println(ages.get("Bob"))  // 25
```

Si la clave no está presente, el resultado es `null`, por lo que el tipo de `ages["Alice"]` es `Int?`, no `Int`:
```kotlin
println(ages["Zoe"]) // null
```

# --instructions--

Escribe una función `priceOf(prices: Map<String, Int>, item: String): Int?` que devuelva el precio almacenado para `item`, o `null` si el artículo no está en el mapa.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

El precio de `"apple"` en `{apple=2, bread=3}` debe ser `2`

```kotlin
    val prices = mapOf("apple" to 2, "bread" to 3)
    tryCatch(priceOf(prices, "apple") == 2)
```

El precio de `"bread"` debe ser `3`

```kotlin
    tryCatch(priceOf(prices, "bread") == 3)
```

El precio de un artículo ausente debe ser `null`

```kotlin
    tryCatch(priceOf(prices, "kiwi") == null)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun priceOf(prices: Map<String, Int>, item: String): Int? {
    return prices[item]
}
```
