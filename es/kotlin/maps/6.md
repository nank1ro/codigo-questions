---
language: kotlin
exerciseType: 1
---

# --description--

Para comprobar si una clave existe usa `containsKey` o el operador `in`; para comprobar si un valor existe usa `containsValue`:
```kotlin
val ages = mapOf("Alice" to 30, "Bob" to 25)
println(ages.containsKey("Alice")) // true
println("Zoe" in ages)             // false
println(ages.containsValue(25))    // true
```

# --instructions--

Dado un mapa de nombres de usuario a direcciones de correo, escribe dos funciones:
- `isRegistered(users: Map<String, String>, username: String): Boolean` que devuelva `true` si `username` es una clave del mapa
- `isEmailTaken(users: Map<String, String>, email: String): Boolean` que devuelva `true` si `email` es uno de los valores

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`isRegistered` debe devolver `true` para un nombre de usuario existente

```kotlin
    val users = mapOf("ann" to "ann@mail.com", "ben" to "ben@mail.com")
    tryCatch(isRegistered(users, "ann") == true)
```

`isRegistered` debe devolver `false` para un nombre de usuario ausente

```kotlin
    tryCatch(isRegistered(users, "zoe") == false)
```

`isEmailTaken` debe devolver `true` para un correo ya presente en el mapa

```kotlin
    tryCatch(isEmailTaken(users, "ben@mail.com") == true)
```

`isEmailTaken` debe devolver `false` para un correo no utilizado

```kotlin
    tryCatch(isEmailTaken(users, "zoe@mail.com") == false)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun isRegistered(users: Map<String, String>, username: String): Boolean {
    return users.containsKey(username)
}

fun isEmailTaken(users: Map<String, String>, email: String): Boolean {
    return users.containsValue(email)
}
```
