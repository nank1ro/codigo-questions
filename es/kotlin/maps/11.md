---
language: kotlin
exerciseType: 1
---

# --instructions--

Escribe una función `countWords(words: List<String>): Map<String, Int>` que devuelva un mapa emparejando cada palabra distinta con el número de veces que aparece en la lista.

Ejemplo de llamada a la función:
```kotlin
println(countWords(listOf("a", "b", "a")))
// prints {a=2, b=1}
```

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`countWords(listOf("a", "b", "a"))` debe devolver `{a=2, b=1}`

```kotlin
    tryCatch(countWords(listOf("a", "b", "a")) == mapOf("a" to 2, "b" to 1))
```

`countWords(listOf("hi", "hi", "hi"))` debe devolver `{hi=3}`

```kotlin
    tryCatch(countWords(listOf("hi", "hi", "hi")) == mapOf("hi" to 3))
```

`countWords` de una lista vacía debe devolver un mapa vacío

```kotlin
    tryCatch(countWords(listOf()) == mapOf<String, Int>())
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun countWords(words: List<String>): Map<String, Int> {
    val counts = mutableMapOf<String, Int>()
    for (word in words) {
        counts[word] = counts.getOrDefault(word, 0) + 1
    }
    return counts
}
```
