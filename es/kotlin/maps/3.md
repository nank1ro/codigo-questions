---
language: kotlin
exerciseType: 2
---

# --description--

Cuando una clave podría estar ausente, `getOrDefault` te permite elegir el valor que se usará en lugar de `null`:
```kotlin
val ages = mapOf("Alice" to 30)
println(ages.getOrDefault("Alice", 0)) // 30
println(ages.getOrDefault("Zoe", 0))   // 0
```
El primer argumento es la clave, el segundo es el valor por defecto devuelto cuando la clave no se encuentra.

# --instructions--

Completa el código para imprimir el stock de `"kiwi"`, usando `0` cuando la fruta no está en el mapa

# --seed--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.[/]("kiwi", [/]))
```

# --answers--

- getOrDefault
- get
- getOrNull
- 0
- null
- "kiwi"

# --solutions--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.getOrDefault("kiwi", 0))
```

# --output--

0
