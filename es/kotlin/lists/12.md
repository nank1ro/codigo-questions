---
language: kotlin
exerciseType: 2
---

# --description--

En Kotlin podemos recorrer una lista de una forma muy simple usando las palabras clave `for..in`:
```kotlin
val numbers = listOf(1, 2, 3)
for (num in numbers) {
    println(num)
}
// prints 1, 2, 3
```
Un nombre de variable sigue a la palabra clave `for`, se le asignará el valor de cada elemento de la lista a su vez.

# --instructions--

Imprime cada elemento de la lista multiplicándolo por `2`

# --seed--

```kotlin
val numbers = listOf(2, 4, 6)
[/]([/][/][/]) {
    println([/][/][/])
}
```

# --answers--

- for 
- num
-  in 
- numbers
- num
-  * 
-  ** 
- 2

# --solutions--

```kotlin
val numbers = listOf(2, 4, 6)
for (num in numbers) {
    println(num * 2)
}
```

# --output--

4
8
12
