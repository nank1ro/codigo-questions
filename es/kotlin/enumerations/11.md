---
language: kotlin
exerciseType: 4
---

# --instructions--

Ordena los elementos para imprimir `Slow down`. Declara primero el enum, luego la función `action`, después `main`, y mantén las ramas `when` en el mismo orden que las entradas del enum.

# --answers--

```kotlin
enum class Light {
    RED, YELLOW, GREEN
}
```

```kotlin
fun action(light: Light): String = when (light) {
```

```kotlin
    Light.RED -> "Stop"
```

```kotlin
    Light.YELLOW -> "Slow down"
```

```kotlin
    Light.GREEN -> "Go"
```

```kotlin
}
```

```kotlin
fun main() {
    println(action(Light.YELLOW))
}
```

# --solutions--

```kotlin
enum class Light {
    RED, YELLOW, GREEN
}
fun action(light: Light): String = when (light) {
    Light.RED -> "Stop"
    Light.YELLOW -> "Slow down"
    Light.GREEN -> "Go"
}
fun main() {
    println(action(Light.YELLOW))
}
```

# --output--

Slow down
