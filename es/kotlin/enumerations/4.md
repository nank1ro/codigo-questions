---
language: kotlin
exerciseType: 2
---

# --description--

Un enum puede tener un **constructor**, igual que una clase normal. Cada entrada pasa entonces sus propios argumentos, y los valores se almacenan en propiedades:
```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
println(Planet.MARS.moons) // 2
```
Las propiedades del constructor normalmente se declaran con `val`, ya que los datos de una entrada no están pensados para cambiar.

# --instructions--

Completa el código para que cada `Coin` almacene su valor en centavos, luego imprime el valor de un `PENNY` (que vale `1` centavo)

# --seed--

```kotlin
enum class Coin([/] cents: Int) {
    PENNY([/]),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.[/])
}
```

# --answers--

- val
- fun
- const
- 1
- "1"
- cents
- name
- ordinal

# --solutions--

```kotlin
enum class Coin(val cents: Int) {
    PENNY(1),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.cents)
}
```

# --output--

1
