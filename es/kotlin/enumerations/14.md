---
language: kotlin
exerciseType: 4
---

# --instructions--

Ordena los elementos para declarar el enum `Planet` e imprimir, para cada entrada, su nombre y su número de lunas. Declara primero el enum, luego `main`. El programa debe imprimir:

```
MERCURY: 0
EARTH: 1
MARS: 2
```

# --answers--

```kotlin
enum class Planet(val moons: Int) {
```

```kotlin
    MERCURY(0),
```

```kotlin
    EARTH(1),
```

```kotlin
    MARS(2)
```

```kotlin
}
```

```kotlin
fun main() {
    for (planet in Planet.entries) {
```

```kotlin
        println("${planet.name}: ${planet.moons}")
```

```kotlin
    }
}
```

# --solutions--

```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
fun main() {
    for (planet in Planet.entries) {
        println("${planet.name}: ${planet.moons}")
    }
}
```

# --output--

MERCURY: 0
EARTH: 1
MARS: 2
