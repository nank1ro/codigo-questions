---
language: kotlin
exerciseType: 1
---

# --description--

Para pasar de un `String` de vuelta a una entrada, usa la función `valueOf`. Busca la entrada cuyo `name` coincide exactamente:
```kotlin
val direction = Direction.valueOf("EAST")
println(direction == Direction.EAST) // true
```
La coincidencia distingue mayúsculas de minúsculas: `Direction.valueOf("east")` lanza una `IllegalArgumentException` porque ninguna entrada tiene ese nombre.

# --instructions--

Declara un enum `Status` con las entradas `ACTIVE` e `INACTIVE`. Luego escribe una función `statusOf(text: String)` que devuelva el `Status` que coincide con `text`, sin importar mayúsculas o minúsculas (de modo que `"active"`, `"Active"` y `"ACTIVE"` den todos `Status.ACTIVE`).

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`statusOf("ACTIVE")` debe devolver `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("ACTIVE") == Status.ACTIVE)
```

`statusOf("inactive")` debe devolver `Status.INACTIVE`

```kotlin
    tryCatch(statusOf("inactive") == Status.INACTIVE)
```

`statusOf("Active")` debe devolver `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("Active") == Status.ACTIVE)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
enum class Status {
    ACTIVE, INACTIVE
}

fun statusOf(text: String): Status {
    return Status.valueOf(text.uppercase())
}
```
