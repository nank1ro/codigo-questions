---
language: kotlin
exerciseType: 1
---

# --description--

`zip` empareja los elementos de dos listas posición por posición. Sin lambda devuelve una lista de valores `Pair`, cuyas mitades se leen con `.first` y `.second`; con una lambda, los dos elementos de cada posición se le pasan y los resultados se recogen en una lista:
```kotlin
val names = listOf("Ann", "Bob")
val ages = listOf(31, 25)
println(names.zip(ages))                            // [(Ann, 31), (Bob, 25)]
println(names.zip(ages) { name, age -> "$name:$age" }) // [Ann:31, Bob:25]
```
El resultado es tan largo como la **más corta** de las dos listas: los elementos sobrantes de la más larga se ignoran.

# --instructions--

Escribe una función `dotProduct(a: List<Int>, b: List<Int>): Int` que multiplique los elementos de `a` y `b` posición por posición y devuelva la suma de los productos, usando `zip`. Los elementos sobrantes de la lista más larga se ignoran.

Ejemplo: `dotProduct(listOf(1, 2, 3), listOf(4, 5, 6))` devuelve `32`, es decir `1*4 + 2*5 + 3*6`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`dotProduct(listOf(1, 2, 3), listOf(4, 5, 6))` debe devolver `32`

```kotlin
    tryCatch(dotProduct(listOf(1, 2, 3), listOf(4, 5, 6)) == 32)
```

`dotProduct(listOf(1, 2, 3), listOf(10, 10))` debe devolver `30`, ignorando el `3` sobrante

```kotlin
    tryCatch(dotProduct(listOf(1, 2, 3), listOf(10, 10)) == 30)
```

`dotProduct(listOf(2), listOf(-3, 100))` debe devolver `-6`

```kotlin
    tryCatch(dotProduct(listOf(2), listOf(-3, 100)) == -6)
```

`dotProduct(listOf(), listOf(1, 2))` debe devolver `0`

```kotlin
    tryCatch(dotProduct(listOf(), listOf(1, 2)) == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun dotProduct(a: List<Int>, b: List<Int>): Int {
    return a.zip(b) { x, y -> x * y }.sum()
}
```
