---
language: kotlin
exerciseType: 2
---

# --description--

Cada array tiene dos propiedades útiles para trabajar con índices:
- `indices` es el rango de índices válidos, de `0` al último
- `lastIndex` es el índice del último elemento, es decir, `size - 1`
```kotlin
val letters = arrayOf("x", "y")
println(letters.lastIndex) // 1
for (i in letters.indices) {
    println(letters[i])
}
// prints x, then y
```

# --instructions--

Completa el código para imprimir el último índice de `letters`, y después cada índice seguido de su elemento

# --seed--

```kotlin
val letters = arrayOf("a", "b", "c")
println(letters.[/])
for (i in letters.[/]) {
    println("$i: ${letters[i]}")
}
```

# --answers--

- lastIndex
- indices
- size
- length
- last

# --solutions--

```kotlin
val letters = arrayOf("a", "b", "c")
println(letters.lastIndex)
for (i in letters.indices) {
    println("$i: ${letters[i]}")
}
```

# --output--

2
0: a
1: b
2: c
