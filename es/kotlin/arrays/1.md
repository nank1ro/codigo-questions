---
language: kotlin
exerciseType: 2
---

# --description--

Un **array** almacena un número fijo de valores del mismo tipo bajo un único nombre de variable.
Creas uno con `arrayOf`, lees un elemento con corchetes y un **índice** que empieza en `0`, y obtienes el número de elementos con `size`:
```kotlin
val colors = arrayOf("red", "green", "blue")
println(colors[0])   // red
println(colors.size) // 3
```
El último elemento está en el índice `size - 1`.

# --instructions--

Completa el código para crear el array con `arrayOf`, imprimir su número de elementos y después su último elemento

# --seed--

```kotlin
val numbers = [/](3, 5, 7)
println(numbers.[/])
println(numbers[[/]])
```

# --answers--

- arrayOf
- Array
- size
- length
- count
- 2
- 3

# --solutions--

```kotlin
val numbers = arrayOf(3, 5, 7)
println(numbers.size)
println(numbers[2])
```

# --output--

3
7
