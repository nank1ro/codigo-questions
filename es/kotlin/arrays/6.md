---
language: kotlin
exerciseType: 2
---

# --description--

Imprimir un array directamente no muestra sus elementos, imprime algo como `[Ljava.lang.String;@1b6d3586`.
Usa `joinToString` para construir una cadena legible, opcionalmente con un separador personalizado (el predeterminado es `", "`), o `contentToString` para obtener los elementos entre corchetes:
```kotlin
val nums = intArrayOf(1, 2, 3)
println(nums.joinToString())      // 1, 2, 3
println(nums.joinToString("-"))   // 1-2-3
println(nums.contentToString())   // [1, 2, 3]
```

# --instructions--

Completa el código para imprimir las frutas separadas por `" - "`, y después el array completo entre corchetes

# --seed--

```kotlin
val fruits = arrayOf("apple", "kiwi", "plum")
println(fruits.[/]([/]))
println(fruits.[/]())
```

# --answers--

- joinToString
- contentToString
- toString
- join
- " - "
- "-"

# --solutions--

```kotlin
val fruits = arrayOf("apple", "kiwi", "plum")
println(fruits.joinToString(" - "))
println(fruits.contentToString())
```

# --output--

apple - kiwi - plum
[apple, kiwi, plum]
