---
language: kotlin
exerciseType: 1
---

# --description--

Los arrays numéricos vienen con funciones de agregación:
```kotlin
val nums = intArrayOf(3, 9, 1)
println(nums.sum())     // 13
println(nums.max())     // 9
println(nums.min())     // 1
println(nums.average()) // 4.333333333333333
println(nums.count())   // 3
```
`max()` y `min()` lanzan una excepción con un array vacío, usa `maxOrNull()` y `minOrNull()` cuando el array pueda estar vacío.

# --instructions--

Escribe una función `range(numbers: IntArray): Int` que devuelva la diferencia entre el elemento más grande y el más pequeño de `numbers`. Puedes asumir que el array nunca está vacío.

Ejemplo: `range(intArrayOf(3, 9, 1))` devuelve `8`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`range(intArrayOf(3, 9, 1))` debe devolver `8`

```kotlin
    tryCatch(range(intArrayOf(3, 9, 1)) == 8)
```

`range(intArrayOf(5))` debe devolver `0`

```kotlin
    tryCatch(range(intArrayOf(5)) == 0)
```

`range(intArrayOf(-2, 4, 0))` debe devolver `6`

```kotlin
    tryCatch(range(intArrayOf(-2, 4, 0)) == 6)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun range(numbers: IntArray): Int {
    return numbers.max() - numbers.min()
}
```
