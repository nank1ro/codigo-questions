---
language: kotlin
exerciseType: 2
---

# --description--

Una función con más de un parámetro recibe una etiqueta `@param` por cada uno de ellos, escritas en el mismo orden que los parámetros:
```kotlin
/**
 * Returns the area of a rectangle.
 * @param width the horizontal side, in centimetres
 * @param height the vertical side, in centimetres
 * @return the product of [width] and [height]
 */
fun area(width: Int, height: Int): Int {
    return width * height
}
```
Una etiqueta sigue siendo solo un comentario: si renombras un parámetro y olvidas la etiqueta, no se rompe nada, pero la documentación empieza a mentir. Actualiza el KDoc junto con la firma.

# --instructions--

Completa la documentación de `price`: la primera etiqueta debe describir el parámetro `quantity`, la segunda el parámetro `unitPrice`, y la tercera el valor que devuelve la función

# --seed--

```kotlin
/**
 * Returns the total price of an order.
 * [/] quantity how many items were ordered
 * [/] unitPrice the price of a single item
 * [/] the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --answers--

- @param
- @param
- @return
- @throws
- @property

# --solutions--

```kotlin
/**
 * Returns the total price of an order.
 * @param quantity how many items were ordered
 * @param unitPrice the price of a single item
 * @return the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --output--

15
