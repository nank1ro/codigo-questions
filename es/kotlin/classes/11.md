---
language: kotlin
exerciseType: 2
---

# --instructions--

Completa el código para agregar un `companion object` dentro de `AppConfig` que tenga una propiedad `val` llamada `version` igual a `"1.0"`.

# --seed--

```kotlin
class AppConfig {
    [/] [/] {
        val version = "1.0"
    }
}
```

# --answers--

- companion
- object
- class
- static
- init
- val

# --solutions--

```kotlin
class AppConfig {
    companion object {
        val version = "1.0"
    }
}
```
