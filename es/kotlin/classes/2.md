---
language: kotlin
exerciseType: 1
---

# --description--

Las clases pueden tener múltiples propiedades en su **constructor primario**. Cada propiedad se declara con `val` (solo lectura) o `var` (mutable) directamente en la lista de parámetros del constructor:

```kotlin
class Person(val name: String, var age: Int)
```

La palabra clave `val` hace que `name` sea de solo lectura después de la creación, mientras que `var` hace que `age` sea mutable — se puede cambiar después:

```kotlin
val p = Person("Alice", 30)
p.age = 31  // permitido porque age es var
```

# --instructions--

Crea una clase `Person` con una propiedad `val` llamada `name` de tipo `String` y una propiedad `var` llamada `age` de tipo `Int`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

Una `Person("Alice", 30)` debe tener `name` igual a `"Alice"`

```kotlin
    val p = Person("Alice", 30)
    tryCatch(p.name == "Alice")
```

Una `Person("Alice", 30)` debe tener `age` igual a `30`

```kotlin
    tryCatch(p.age == 30)
```

Cambiar `age` a `31` debe actualizar la propiedad

```kotlin
    p.age = 31
    tryCatch(p.age == 31)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Person(val name: String, var age: Int)
```
