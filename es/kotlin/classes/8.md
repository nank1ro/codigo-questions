---
language: kotlin
exerciseType: 2
---

# --instructions--

Completa el código para definir una `class` llamada `Book` con un constructor primario que tenga una propiedad `val` llamada `title` de tipo `String`.

# --seed--

```kotlin
[/] Book([/] title: String)
```

# --answers--

- class
- val
- var
- fun
- object
- data

# --solutions--

```kotlin
class Book(val title: String)
```
