---
language: javascript
exerciseType: 2
---

# --description--

En las funciones podemos añadir un _comentario opcional_ que explique qué hace la función:
```javascript
// Prints 'Hello World' to the console.
function helloWorld() {
    console.log("Hello, World!");
}
```
Podemos usar `//` para un comentario de una línea y `/** */` para un comentario de varias líneas

# --instructions--

Añade un comentario válido a la función

# --seed--

```javascript
[/][/] Returns the product between n1 and n2 numbers;
const product = (n1, n2) => n1 * n2;
```

# --answers--

- /
- /
- /
- \
- \
- \
- /*
- /**

# --solutions--

```javascript
// Returns the product between n1 and n2 numbers;
const product = (n1, n2) => n1 * n2;
```
