---
language: javascript
exerciseType: 4
---

# --description--

Dos métodos de array más toman un callback.
`forEach(callback)` llama al callback una vez por elemento y no devuelve nada; úsalo para efectos secundarios como imprimir.
`reduce(callback, initialValue)` pliega el array en un único valor: el callback recibe el valor acumulado hasta ahora y el elemento actual, y devuelve el nuevo valor acumulado:
```javascript
const numbers = [1, 2, 3];
numbers.forEach((n) => console.log(n));
// imprime 1, 2 y 3 en tres líneas
const total = numbers.reduce((sum, n) => sum + n, 0);
console.log(total);
// imprime 6
```

# --instructions--

Ordena las líneas para calcular el total de `prices` con `reduce` justo después de declarar el array, imprime cada precio en su propia línea con `forEach`, luego imprime el total

# --answers--

```javascript
const prices = [4, 6, 10];
```

```javascript
const total = prices.reduce((sum, p) => sum + p, 0);
```

```javascript
prices.forEach((p) => console.log(p));
```

```javascript
console.log(total);
```

# --solutions--

```javascript
const prices = [4, 6, 10];
const total = prices.reduce((sum, p) => sum + p, 0);
prices.forEach((p) => console.log(p));
console.log(total);
```

# --output--

4
6
10
20
