---
language: javascript
exerciseType: 4
---

# --instructions--

Ordena las líneas para definir `makeCounter`, que devuelve una función flecha que incrementa y devuelve su `count` privado, luego crea el contador `next` e imprime el resultado de dos llamadas en la misma línea

# --answers--

```javascript
const makeCounter = () => {
```

```javascript
  let count = 0;
```

```javascript
  return () => {
```

```javascript
    count += 1;
```

```javascript
    return count;
```

```javascript
  };
```

```javascript
};
```

```javascript
const next = makeCounter();
```

```javascript
console.log(next(), next());
```

# --solutions--

```javascript
const makeCounter = () => {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
};
const next = makeCounter();
console.log(next(), next());
```

# --output--

1 2
