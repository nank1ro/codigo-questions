---
language: javascript
exerciseType: 2
---

# --description--

`sort(compare)` ordena un array en el lugar usando un callback que recibe dos elementos y devuelve un número negativo cuando el primero debe ir primero, un número positivo cuando el segundo debe ir primero, o `0` cuando son iguales. Para números, `(a, b) => a - b` ordena en orden ascendente y `(a, b) => b - a` en orden descendente.
`find(callback)` devuelve el primer elemento para el que el callback devuelve `true`, o `undefined` si no hay ninguno:
```javascript
const scores = [50, 90, 70];
scores.sort((a, b) => a - b);
console.log(scores);
// prints [ 50, 70, 90 ]
console.log(scores.find((s) => s > 60));
// prints 70
```

# --instructions--

Ordena `ages` en orden ascendente, luego encuentra la primera edad estrictamente mayor que `21` e imprime tanto el array ordenado como esa edad

# --seed--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a [/] b);
const adult = ages.find((age) => age [/] 21);
console.log(ages);
console.log(adult);
```

# --answers--

- -
- >
- +
- <
- ===
- *

# --solutions--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a - b);
const adult = ages.find((age) => age > 21);
console.log(ages);
console.log(adult);
```

# --output--

[ 18, 25, 31 ]
25
