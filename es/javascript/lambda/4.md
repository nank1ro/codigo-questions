---
language: javascript
exerciseType: 2
---

# --description--

Rodear el objeto literal con paréntesis es la forma estándar de construir objetos con una función flecha de una sola línea, por ejemplo cuando conviertes un par de valores en un registro:
```javascript
const user = (name, age) => ({ name: name, age: age });
console.log(user("Ana", 30).age);
// prints 30
```
Una función flecha sin parámetros empieza con un par vacío de paréntesis `()`:
```javascript
const empty = () => ({});
console.log(empty());
// prints {}
```

# --instructions--

Completa `point` para que devuelva un objeto con las propiedades `x` e `y`, y `origin` para que no tome parámetros, luego imprime `point(3, 4).y` y `origin().x`

# --seed--

```javascript
const point = (x, y) => [/]{ x: x, y: y }[/];
const origin = [/] => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --answers--

- (
- )
- ()
- [
- ]
- function
- this

# --solutions--

```javascript
const point = (x, y) => ({ x: x, y: y });
const origin = () => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --output--

4
0
