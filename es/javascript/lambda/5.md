---
language: javascript
exerciseType: 1
---

# --description--

Las funciones flecha brillan de verdad como **callbacks**: funciones pasadas como argumentos a otras funciones. Los métodos de array son el ejemplo más común.
`map(callback)` devuelve un nuevo array con el resultado del callback para cada elemento, y `filter(callback)` devuelve un nuevo array con solo los elementos para los que el callback devuelve `true`:
```javascript
const numbers = [1, 2, 3, 4];
console.log(numbers.map((n) => n * 10));
// prints [ 10, 20, 30, 40 ]
console.log(numbers.filter((n) => n > 2));
// prints [ 3, 4 ]
```
Ambos devuelven un nuevo array y dejan el original intacto, así que puedes encadenarlos: `numbers.filter(...).map(...)`.

# --instructions--

Escribe una función `evenSquares(numbers)` que devuelva un nuevo array con los cuadrados de los números pares de `numbers`, en el orden original, encadenando `filter` y `map` con funciones flecha

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function evenSquares(numbers) {

}
```

# --asserts--

`evenSquares([1, 2, 3, 4])` debe devolver `[4, 16]`.

```javascript
tryCatch(JSON.stringify(evenSquares([1, 2, 3, 4])) === "[4,16]");
```

`evenSquares([6, 5, 0])` debe devolver `[36, 0]`.

```javascript
tryCatch(JSON.stringify(evenSquares([6, 5, 0])) === "[36,0]");
```

`evenSquares([1, 3, 5])` debe devolver un array vacío.

```javascript
tryCatch(JSON.stringify(evenSquares([1, 3, 5])) === "[]");
```

El array original no debe modificarse.

```javascript
tryCatch((() => { const input = [2, 3]; evenSquares(input); return JSON.stringify(input) === "[2,3]"; })());
```

La función debe usar `filter`, `map` y al menos una función flecha.

```javascript
tryCatch(/\.filter\(/.test(evenSquares.toString()) && /\.map\(/.test(evenSquares.toString()) && /=>/.test(evenSquares.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function evenSquares(numbers) {
  return numbers.filter((n) => n % 2 === 0).map((n) => n * n);
}
```
