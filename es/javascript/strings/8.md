---
language: javascript
exerciseType: 4
---

# --instructions--

Ordena las líneas para mostrar la extensión de `file` en mayúsculas

# --answers--

```javascript
let file = "report.pdf";
```

```javascript
let extension = file.slice(-3);
```

```javascript
console.log(extension.toUpperCase());
```

# --solutions--

```javascript
let file = "report.pdf";
let extension = file.slice(-3);
console.log(extension.toUpperCase());
```

# --output--

PDF
