---
language: javascript
exerciseType: 1
---

# --description--

Cada carácter de una cadena tiene un **índice**, empezando desde `0`.
Puedes leer un carácter individual con corchetes o con el método `charAt()`:
```javascript
let word = "hello";
console.log(word[0]);
// prints h
console.log(word.charAt(1));
// prints e
```
El último carácter está en el índice `length - 1`:
```javascript
console.log(word[word.length - 1]);
// prints o
```

# --instructions--

Guarda el primer carácter de `word` en una variable `first` y el último carácter en una variable `last`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
let word = "coding";
```

# --asserts--

`first` debe ser igual a `"c"`.

```javascript
tryCatch(first === "c");
```

`last` debe ser igual a `"g"`.

```javascript
tryCatch(last === "g");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
let word = "coding";
let first = word[0];
let last = word[word.length - 1];
```
