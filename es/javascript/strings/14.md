---
language: javascript
exerciseType: 1
---

# --description--

El método `padStart(targetLength, padString)` añade `padString` al **inicio** de la cadena hasta alcanzar `targetLength` caracteres. `padEnd()` hace lo mismo al final:
```javascript
console.log("7".padStart(3, "0"));
// prints 007
console.log("Tea".padEnd(6, "."));
// prints Tea...
```
Si la cadena ya es lo bastante larga, se devuelve sin cambios.
Los números no tienen métodos de cadena, así que conviértelos primero con `String(number)`.

# --instructions--

Escribe una función `twoDigits(number)` que devuelva `number` como una cadena de al menos dos caracteres, rellenada a la izquierda con `"0"`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function twoDigits(number) {

}
```

# --asserts--

`twoDigits(5)` debe devolver `"05"`.

```javascript
tryCatch(twoDigits(5) === "05");
```

`twoDigits(12)` debe devolver `"12"`.

```javascript
tryCatch(twoDigits(12) === "12");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function twoDigits(number) {
  return String(number).padStart(2, "0");
}
```
