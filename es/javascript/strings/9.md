---
language: javascript
exerciseType: 2
---

# --description--

El método `split(separator)` divide una cadena en un **array** de partes, cortando en cada `separator`:
```javascript
let sentence = "I like JavaScript";
let words = sentence.split(" ");
console.log(words);
// prints [ 'I', 'like', 'JavaScript' ]
```
Lo contrario es el método de array `join(separator)`, que une las partes de nuevo en una cadena:
```javascript
console.log(words.join("-"));
// prints I-like-JavaScript
```

# --instructions--

Divide `csv` en cada coma y muestra cuántos colores contiene

# --seed--

```javascript
let csv = "red,green,blue";
let colors = csv.[/]([/]);
console.log(colors.[/]);
```

# --answers--

- split
- ","
- " "
- join
- length
- size

# --solutions--

```javascript
let csv = "red,green,blue";
let colors = csv.split(",");
console.log(colors.length);
```

# --output--

3
