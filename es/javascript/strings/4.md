---
language: javascript
exerciseType: 1
---

# --description--

Para comprobar si una cadena contiene otra cadena, usa estos métodos, que devuelven todos un booleano:
- `includes(text)` es `true` si `text` aparece en cualquier parte
- `startsWith(text)` es `true` si la cadena empieza con `text`
- `endsWith(text)` es `true` si la cadena termina con `text`

```javascript
let file = "photo.png";
console.log(file.includes("."));
// prints true
console.log(file.startsWith("ph"));
// prints true
console.log(file.endsWith(".jpg"));
// prints false
```
La comparación distingue mayúsculas de minúsculas: `"Hello".includes("h")` es `false`.

# --instructions--

Escribe una función `isImage(fileName)` que devuelva `true` cuando `fileName` termine en `".png"` y `false` en caso contrario

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isImage(fileName) {

}
```

# --asserts--

`isImage("cat.png")` debe devolver `true`.

```javascript
tryCatch(isImage("cat.png") === true);
```

`isImage("cat.jpg")` debe devolver `false`.

```javascript
tryCatch(isImage("cat.jpg") === false);
```

`isImage("png.txt")` debe devolver `false`.

```javascript
tryCatch(isImage("png.txt") === false);
```

`isImage("a.png.txt")` debe devolver `false`.

```javascript
tryCatch(isImage("a.png.txt") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isImage(fileName) {
  return fileName.endsWith(".png");
}
```
