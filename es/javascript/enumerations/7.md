---
language: javascript
exerciseType: 1
---

# --description--

Las enumeraciones combinan naturalmente con la sentencia `switch`, que compara un valor con una lista de etiquetas `case` y ejecuta el código de la primera que coincida.
Cada rama termina con `return` o `break`, y la rama opcional `default` se ejecuta cuando nada coincide:
```javascript
const Light = Object.freeze({ RED: "red", GREEN: "green" });

function action(light) {
  switch (light) {
    case Light.RED:
      return "stop";
    case Light.GREEN:
      return "go";
    default:
      return "unknown";
  }
}
console.log(action(Light.GREEN));
// prints go
```
Compara siempre con los miembros (`Light.RED`), nunca con los valores en bruto (`"red"`): si el valor cambia alguna vez, el `switch` sigue funcionando.

# --instructions--

Escribe una función `weekend(day)` que use un `switch` sobre la enumeración `Day` y devuelva `true` para `Day.SATURDAY` y `Day.SUNDAY`, y `false` para cualquier otro valor

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {

}
```

# --asserts--

`weekend(Day.SATURDAY)` debe devolver `true`.

```javascript
tryCatch(weekend(Day.SATURDAY) === true);
```

`weekend(Day.SUNDAY)` debe devolver `true`.

```javascript
tryCatch(weekend(Day.SUNDAY) === true);
```

`weekend(Day.MONDAY)` debe devolver `false`.

```javascript
tryCatch(weekend(Day.MONDAY) === false);
```

`weekend("holiday")` debe devolver `false`, porque no es un miembro.

```javascript
tryCatch(weekend("holiday") === false);
```

La función debe usar una sentencia `switch`.

```javascript
tryCatch(/switch\s*\(/.test(weekend.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {
  switch (day) {
    case Day.SATURDAY:
    case Day.SUNDAY:
      return true;
    default:
      return false;
  }
}
```
