---
language: javascript
exerciseType: 2
---

# --description--

Los miembros también pueden contener **números**. Los valores numéricos son útiles cuando los miembros tienen un orden natural, porque puedes compararlos con los operadores habituales:
```javascript
const Priority = Object.freeze({
  LOW: 1,
  MEDIUM: 2,
  HIGH: 3,
});
console.log(Priority.HIGH > Priority.LOW);
// prints true
```
La contrapartida es la legibilidad: imprimir `Priority.HIGH` muestra `3`, lo que te dice mucho menos que la cadena `"high"`.

# --instructions--

Declara la enumeración numérica congelada `Level` con `EASY` igual a `1`, `NORMAL` igual a `2` y `HARD` igual a `3`, y luego imprime si `HARD` es mayor que `EASY` y la diferencia entre `HARD` y `EASY`

# --seed--

```javascript
const Level = Object.freeze({
  EASY: [/],
  NORMAL: 2,
  HARD: [/],
});
console.log(Level.HARD [/] Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --answers--

- 1
- 3
- >
- 0
- <
- ===

# --solutions--

```javascript
const Level = Object.freeze({
  EASY: 1,
  NORMAL: 2,
  HARD: 3,
});
console.log(Level.HARD > Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --output--

true
2
