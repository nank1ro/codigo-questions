---
language: javascript
exerciseType: 1
---

# --description--

Captura solo lo que necesitas. Un grupo no es solo una forma de leer una pieza después; también le dice al lector qué parte del patrón importa. En un patrón de hora donde solo quieres los minutos, agrupa únicamente los minutos y deja el resto sin agrupar:
```javascript
const minutes = "at 14:35:02".match(/\d{2}:(\d{2}):\d{2}/)?.[1];
console.log(minutes);
// prints 35
```
Todo el patrón sigue teniendo que coincidir, así que las horas y los segundos siguen siendo obligatorios; simplemente no se capturan. Menos grupos significan menos números que recordar cuando lees `match[1]`, `match[2]` y así sucesivamente.

# --instructions--

Escribe una función `monthOf` que reciba una cadena `date` que puede contener una fecha escrita como `YYYY-MM-DD`, posiblemente rodeada de otro texto, y devuelva su mes como una cadena, o una cadena vacía cuando `date` no contenga tal fecha. Por ejemplo, `monthOf("2026-09-12")` devuelve `09`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function monthOf(date) {
  
}
```

# --asserts--

`monthOf("2026-09-12")` debe devolver `09`.

```javascript
tryCatch(monthOf("2026-09-12") === "09");
```

`monthOf("born on 1999-12-31")` debe devolver `12`.

```javascript
tryCatch(monthOf("born on 1999-12-31") === "12");
```

`monthOf("no date")` debe devolver una cadena vacía.

```javascript
tryCatch(monthOf("no date") === "");
```

`monthOf("12-31-1999")` debe devolver una cadena vacía.

```javascript
tryCatch(monthOf("12-31-1999") === "");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function monthOf(date) {
  return date.match(/\d{4}-(\d{2})-\d{2}/)?.[1] ?? "";
}
```
