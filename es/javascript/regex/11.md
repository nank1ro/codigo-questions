---
language: javascript
exerciseType: 1
---

# --description--

La bandera `g` te da cada cadena coincidente, pero descarta los grupos. Cuando necesitas los grupos de *todas* las coincidencias, usa **`matchAll`**. Devuelve un iterador de objetos de coincidencia completos, cada uno exactamente igual al resultado de un `match` normal:
```javascript
const text = "a=1;b=2";
for (const match of text.matchAll(/(?<key>\w+)=(?<value>\w+)/g)) {
  console.log(`${match.groups.key} -> ${match.groups.value}`);
}
// imprime a -> 1
// imprime b -> 2
```
`matchAll` requiere la bandera `g`; sin ella, lanza un `TypeError`. Como devuelve un iterador, extiéndelo con `[...text.matchAll(pattern)]` cuando quieras un array real, y ten en cuenta que no produce nada en absoluto cuando el patrón nunca coincide.

# --instructions--

Escribe una función `parsePairs` que reciba una cadena `text`, encuentre cada par `key=value` en ella, donde las claves y los valores son rachas de caracteres de palabra, y devuelva un objeto que asocie cada clave con su valor. Por ejemplo, `parsePairs("host=local;port=80")` devuelve `{ host: "local", port: "80" }`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function parsePairs(text) {
  
}
```

# --asserts--

`parsePairs("host=local;port=80")` debe devolver un objeto con `host` y `port`.

```javascript
tryCatch(JSON.stringify(parsePairs("host=local;port=80")) === '{"host":"local","port":"80"}');
```

`parsePairs("a=1")` debe devolver un objeto con solo `a`.

```javascript
tryCatch(JSON.stringify(parsePairs("a=1")) === '{"a":"1"}');
```

`parsePairs("nothing here")` debe devolver un objeto vacío.

```javascript
tryCatch(JSON.stringify(parsePairs("nothing here")) === '{}');
```

`parsePairs("x=1 y=2 z=3")` debe devolver los tres pares.

```javascript
tryCatch(JSON.stringify(parsePairs("x=1 y=2 z=3")) === '{"x":"1","y":"2","z":"3"}');
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function parsePairs(text) {
  const pairs = {};
  for (const match of text.matchAll(/(?<key>\w+)=(?<value>\w+)/g)) {
    pairs[match.groups.key] = match.groups.value;
  }
  return pairs;
}
```
