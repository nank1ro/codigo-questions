---
language: javascript
exerciseType: 2
---

# --description--

`delete(value)` devuelve `true` cuando el valor fue eliminado y `false` cuando no estaba en el set.
Para eliminar **todos** los valores de una vez, llama a `clear()`:
```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Elimina `"pen"` de `cart` imprimiendo el resultado de la eliminación, luego vacía todo el set e imprime cuántos valores quedan

# --seed--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
