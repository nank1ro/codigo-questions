---
language: javascript
exerciseType: 2
---

# --description--

Ya tenemos una variable `total` inicializada
```javascript
var total = 5;
```
Digamos que queremos añadir el número `2` a la variable `total`, podemos escribir
```javascript
total = total + 2;
```
¡Bien, funciona! Pero hay una versión más corta para hacer lo mismo:
```javascript
total += 2;
```
El signo `+=` se llama **asignación de suma**.
Suma un valor al valor de la variable y asigna el resultado a esa variable.

# --instructions--

Añade a la variable `num` el valor `15` usando el operador de *asignación de suma*, luego imprime su valor

# --seed--

```javascript
var num = 75;
[/][/][/];
console.log(num);
```

# --answers--

- +
- +=
- =+
- num 
-  15

# --solutions--

```javascript
var num = 75;
num += 15;
console.log(num);
```

# --output--

90
