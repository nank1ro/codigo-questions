---
language: javascript
exerciseType: 1
---

# --description--

El operador **`instanceof`** pregunta si un objeto fue construido a partir de una clase, o de cualquier clase que herede de ella:
```javascript
class Animal {}
class Dog extends Animal {}
const max = new Dog();
console.log(max instanceof Dog);    // true
console.log(max instanceof Animal); // true, Dog extends Animal
```
JavaScript no tiene una palabra clave `abstract`, pero la misma idea se escribe a mano: una clase base define la forma y cada método que una hija *debe* proporcionar simplemente lanza un error:
```javascript
class Shape {
    area() {
        throw new Error("area() must be implemented");
    }
}
```
Una hija que olvida sobrescribir `area` falla estrepitosamente la primera vez que se usa, en lugar de devolver silenciosamente `undefined`.

# --instructions--

Escribe una clase `Shape` cuyo método `area` lance un `Error`, y una clase `Square` que la extienda, reciba un `side` y sobrescriba `area` para devolver el área del cuadrado

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Shape {
  
}

class Square {
  
}
```

# --asserts--

`new Square(3).area()` debe devolver `9`.

```javascript
tryCatch(new Square(3).area() === 9);
```

`new Square(5).area()` debe devolver `25`.

```javascript
tryCatch(new Square(5).area() === 25);
```

Un cuadrado debe ser un `Shape`.

```javascript
tryCatch(new Square(3) instanceof Shape === true);
```

Llamar a `area` en la clase base debe lanzar un error.

```javascript
let _threw = false;
try {
  new Shape().area();
} catch (e) {
  _threw = true;
}
tryCatch(_threw === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Shape {
  area() {
    throw new Error("area() must be implemented");
  }
}

class Square extends Shape {
  constructor(side) {
    super();
    this.side = side;
  }
  area() {
    return this.side * this.side;
  }
}
```
