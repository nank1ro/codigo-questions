---
language: javascript
exerciseType: 1
---

# --description--

Una clase hija no tiene que redefinir nada de lo que el padre ya proporciona. Los métodos también se heredan, así que una instancia de la hija puede llamarlos como si fueran suyos:
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
    speak() {
        return `${this.name} makes a sound`;
    }
}
class Dog extends Animal {}
console.log(new Dog("Max").speak());
// prints Max makes a sound
```
Cuando una hija declara su propio constructor, llamar a `super(...)` dentro de él es **obligatorio**: sin él el objeto nunca se inicializa y JavaScript lanza un `ReferenceError`. Una hija sin constructor alguno está bien, porque JavaScript escribe uno que reenvía cada argumento al padre.

# --instructions--

Escribe una clase `Cat` que extienda `Animal`, reciba `name` y `lives`, le pase `name` al constructor del padre y almacene `lives` en la instancia

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Cat {
  
}
```

# --asserts--

`new Cat("Kitty", 9).name` debe ser `"Kitty"`.

```javascript
tryCatch(new Cat("Kitty", 9).name === "Kitty");
```

`new Cat("Kitty", 9).lives` debe ser `9`.

```javascript
tryCatch(new Cat("Kitty", 9).lives === 9);
```

`Cat` debe extender `Animal`.

```javascript
tryCatch(new Cat("Kitty", 9) instanceof Animal);
```

`Cat` debe heredar `speak` de `Animal`.

```javascript
tryCatch(new Cat("Kitty", 9).speak() === "Kitty makes a sound");
```

Un segundo gato debe conservar sus propios valores.

```javascript
tryCatch(new Cat("Tom", 7).lives === 7);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Cat extends Animal {
  constructor(name, lives) {
    super(name);
    this.lives = lives;
  }
}
```
