---
language: javascript
exerciseType: 4
---

# --description--

Imprimir un objeto suele dar algo poco útil. Siempre que JavaScript necesita un string y obtiene un objeto en su lugar, llama al método **`toString`** del objeto, y el predeterminado devuelve `[object Object]`. Definir el tuyo propio reemplaza eso:
```javascript
class Money {
    constructor(amount) {
        this.amount = amount;
    }
    toString() {
        return `$${this.amount}`;
    }
}
console.log(`${new Money(7)}`);
// imprime $7
```
El mismo método lo usan la concatenación de strings y `String(value)`. Si además quieres un **número** razonable, define `[Symbol.toPrimitive](hint)`, que recibe `"string"`, `"number"` o `"default"` y decide qué devolver; cuando existe gana sobre `toString`.

# --instructions--

Ordena los elementos para formar un programa funcional que imprima un precio a través de un literal de plantilla

# --answers--

```javascript
class Money {
  constructor(amount) {
    this.amount = amount;
  }
```

```javascript
  toString() {
    return `$${this.amount}`;
  }
}
```

```javascript
const price = new Money(7);
```

```javascript
console.log(`Total: ${price}`);
```

# --solutions--

```javascript
class Money {
  constructor(amount) {
    this.amount = amount;
  }
  toString() {
    return `$${this.amount}`;
  }
}
const price = new Money(7);
console.log(`Total: ${price}`);
```

# --output--

Total: $7
