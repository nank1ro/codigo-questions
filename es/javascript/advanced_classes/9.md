---
language: javascript
exerciseType: 4
---

# --description--

Un uso muy común de un método estático es una **fábrica**: un método que construye una instancia a partir de otra forma de datos y la devuelve. Mantiene `new` en un solo lugar y le da a la construcción un nombre que dice lo que hace:
```javascript
class Duration {
    constructor(seconds) {
        this.seconds = seconds;
    }
    static fromMinutes(minutes) {
        return new Duration(minutes * 60);
    }
}
console.log(Duration.fromMinutes(2).seconds);
// prints 120
```
Una fábrica puede llamarse antes de que exista cualquier instancia, algo que un método normal no puede hacer.

# --instructions--

Ordena los elementos para formar un programa funcional que construya un punto a partir de un array e imprima sus coordenadas

# --answers--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
```

```javascript
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
```

```javascript
const p = Point.fromArray([3, 4]);
```

```javascript
console.log(`${p.x},${p.y}`);
```

# --solutions--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
const p = Point.fromArray([3, 4]);
console.log(`${p.x},${p.y}`);
```

# --output--

3,4
