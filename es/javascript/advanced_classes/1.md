---
language: javascript
exerciseType: 2
---

# --description--

Ya sabes que `extends` convierte una clase en hija de otra. Lo que esa palabra clave realmente te da es **herencia**: la hija obtiene gratis cada propiedad y método del padre, y puede añadir los suyos propios encima.

La pieza que hace útil la herencia es **`super`**. Dentro del constructor de una clase hija, `super(...)` llama al constructor del padre, de modo que el padre puede configurar la parte del objeto que le pertenece:
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
```
Aquí `super(name)` le pasa `name` a `Animal`, que lo almacena, y `Dog` solo tiene que preocuparse de `breed`.

# --instructions--

Completa el código para que `Dog` herede de `Animal` y le pase `name` al constructor del padre

# --seed--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog [/] Animal {
    constructor(name, breed) {
        [/](name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --answers--

- extends
- inherits
- super
- parent
- new Animal

# --solutions--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --output--

Max is a Beagle
