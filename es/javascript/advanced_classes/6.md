---
language: javascript
exerciseType: 4
---

# --description--

El orden importa cuando las clases heredan entre sí. Una declaración `class` **no** se eleva como lo hace una `function`: el nombre solo existe desde la línea donde la clase está escrita en adelante. Así que una clase hija tiene que aparecer *después* del padre que extiende, de lo contrario la cláusula `extends` falla con un `ReferenceError`.

# --instructions--

Ordena los elementos para formar un programa funcional. Recuerda que la clase padre debe definirse antes que la hija

# --answers--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
```

```javascript
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
```

```javascript
console.log(new Car().describe());
```

# --solutions--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
console.log(new Car().describe());
```

# --output--

a vehicle with four wheels
