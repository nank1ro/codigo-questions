---
language: javascript
exerciseType: 2
---

# --description--

Al igual que los strings, los arrays tienen una **longitud**.
La longitud de un array es el número de elementos que contiene

# --instructions--

Completa el código para imprimir la longitud del array

# --seed--

```javascript
var numbers = [5, 6, 8, 4];
console.log([/][/][/]);
```

# --answers--

- len
- length
- numbers
- count
- .

# --solutions--

```javascript
var numbers = [5, 6, 8, 4];
console.log(numbers.length);
```

# --output--

4
