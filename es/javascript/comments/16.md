---
language: javascript
exerciseType: 1
---

# --description--

Un `FIXME` es distinto de un `TODO`: el código ya existe, pero se sabe que está mal. Un buen `FIXME` dice cuál es el error y, cuando es posible, da un ejemplo que lo muestra, para que la siguiente persona pueda corregirlo rápido. Igual que con `TODO`, borra la marca en cuanto el error esté corregido, pero conserva el comentario JSDoc, que sigue siendo cierto.

# --instructions--

Corrige el error descrito por el comentario `FIXME` en `average` (añade los paréntesis para que la suma se calcule antes de la división), y después elimina el comentario `FIXME`. Conserva el comentario JSDoc

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
/**
 * Returns the average of two numbers.
 * @param {number} a the first number
 * @param {number} b the second number
 * @returns {number} the sum of a and b divided by two
 */
function average(a, b) {
  // FIXME: divides before adding, so average(4, 6) gives 7 instead of 5
  return a + b / 2;
}
```

# --asserts--

The `FIXME` comment must be removed.

```json
{
  "regex": "FIXME",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

The JSDoc comment must be kept.

```json
{
  "regex": "/\\*\\*[\\s\\S]*Returns the average of two numbers[\\s\\S]*\\*/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`average(4, 6)` must return `5`.

```javascript
tryCatch(average(4, 6) === 5);
```

`average(3, 4)` must return `3.5`.

```javascript
tryCatch(average(3, 4) === 3.5);
```

`average(0, 0)` must return `0`.

```javascript
tryCatch(average(0, 0) === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
/**
 * Returns the average of two numbers.
 * @param {number} a the first number
 * @param {number} b the second number
 * @returns {number} the sum of a and b divided by two
 */
function average(a, b) {
  return (a + b) / 2;
}
```
