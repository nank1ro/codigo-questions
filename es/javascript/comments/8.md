---
language: javascript
exerciseType: 4
---

# --description--

Para comentar varias líneas de una vez, envuélvelas en un único comentario de bloque en lugar de añadir `//` a cada línea:
```javascript
let total = 100;
/*
total = total - 30;
total = total - 20;
*/
console.log(total); // prints 100
```
Como las líneas de dentro del bloque se ignoran, `total` nunca cambia. Recuerda que esto solo funciona si ninguna de esas líneas contiene un `*/`.

# --instructions--

Ordena los elementos para que `total` se declare primero, las dos líneas que la modifican queden envueltas en un único comentario de bloque y el valor se imprima al final, produciendo la salida `100`

# --answers--

```javascript
let total = 100;
```

```javascript
/*
```

```javascript
total = total - 30;
```

```javascript
total = total - 20;
```

```javascript
*/
```

```javascript
console.log(total);
```

# --solutions--

```javascript
let total = 100;
/*
total = total - 30;
total = total - 20;
*/
console.log(total);
```

```javascript
let total = 100;
/*
total = total - 20;
total = total - 30;
*/
console.log(total);
```

# --output--

100
