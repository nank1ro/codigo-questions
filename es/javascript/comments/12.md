---
language: javascript
exerciseType: 4
---

# --description--

El orden dentro de un comentario JSDoc es siempre el mismo: primero el resumen, después un `@param` por cada parámetro en el orden en que están declarados, y al final `@returns`. La apertura `/**` y el cierre ` */` lo envuelven todo, y el comentario se sitúa directamente encima de la función que describe:
```javascript
/**
 * Returns the number of seconds in the given minutes.
 * @param {number} minutes a whole number of minutes
 * @returns {number} minutes multiplied by sixty
 */
function toSeconds(minutes) {
  return minutes * 60;
}
```

# --instructions--

Ordena los elementos para documentar `twice` con un resumen, una línea `@param` y una línea `@returns`, en este orden y directamente encima de la función, y después imprime `twice(21)` tras definir la función

# --answers--

```javascript
/**
```

```javascript
 * Returns the double of a number.
```

```javascript
 * @param {number} n the number to double
```

```javascript
 * @returns {number} n multiplied by two
```

```javascript
 */
```

```javascript
function twice(n) {
  return n * 2;
}
```

```javascript
console.log(twice(21));
```

# --solutions--

```javascript
/**
 * Returns the double of a number.
 * @param {number} n the number to double
 * @returns {number} n multiplied by two
 */
function twice(n) {
  return n * 2;
}
console.log(twice(21));
```

# --output--

42
