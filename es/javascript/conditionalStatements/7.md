---
language: javascript
exerciseType: 2
---

# --description--

El operador condicional ternario es un operador especial con tres partes, que toma la forma `question ? answer1 : answer2`.
Es un atajo para evaluar una de dos expresiones basándose en si `question` es true o false.
Si `question` es true, evalúa `answer1` y devuelve su valor; de lo contrario, evalúa `answer2` y devuelve su valor.
```javascript
let a = 10, b = 20, c = 0;
if (a < b) {
    c = a;
} else {
    c = b;
}
console.log(c);
// prints 10
```
El código abreviado para el código anterior es:
```javascript
let a = 10, b = 20, c = 0;
c = a < b ? a : b;
console.log(c);
// prints 10
```
`c` se establece igual a `a`, porque la condición `a < b` era true

# --instructions--

Crea un operador ternario válido imprimiendo `"It's morning"` si `time` es menor que `12`, de lo contrario `"It's afternoon"`

# --seed--

```javascript
var time = 15;
console.log(time < 12[/]"It's morning"[/]"It's afternoon");
```

# --answers--

- if
- ==
- =
-  ? 
-  : 
- else

# --solutions--

```javascript
var time = 15;
console.log(time < 12 ? "It's morning" : "It's afternoon");
```

# --output--

It's afternoon
