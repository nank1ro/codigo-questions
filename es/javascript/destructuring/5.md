---
language: javascript
exerciseType: 1
---

# --description--

Un patrón también puede quedar a la izquierda de una asignación normal, sin `const` ni `let` delante, y entonces escribe en variables que ya existen. Eso convierte el intercambio de dos valores en una sola línea, sin variable temporal:
```javascript
let a = 1;
let b = 2;
[a, b] = [b, a];
console.log(a, b);
// imprime 2 1
```
El lado derecho se construye primero, así que ambos valores antiguos ya están a salvo dentro del array temporal cuando ocurre la asignación. Ojo al punto y coma de la línea anterior: de lo contrario, una línea que empieza con `[` se leería como un índice de lo que venga antes.

# --instructions--

Escribe una función `sortPair` que reciba dos valores y devuelva un array de dos elementos con el menor primero, intercambiándolos con una asignación por destructuración cuando estén en el orden incorrecto

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function sortPair(a, b) {
  
}
```

# --asserts--

`sortPair(5, 2)` debe empezar con `2`.

```javascript
tryCatch(sortPair(5, 2)[0] === 2);
```

`sortPair(5, 2)` debe terminar con `5`.

```javascript
tryCatch(sortPair(5, 2)[1] === 5);
```

Un par ya ordenado debe quedarse tal cual.

```javascript
tryCatch(sortPair(1, 9).join(",") === "1,9");
```

Dos valores iguales deben quedarse tal cual.

```javascript
tryCatch(sortPair(4, 4).join(",") === "4,4");
```

Los valores negativos también deben funcionar.

```javascript
tryCatch(sortPair(3, -8).join(",") === "-8,3");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function sortPair(a, b) {
  if (a > b) {
    [a, b] = [b, a];
  }
  return [a, b];
}
```
