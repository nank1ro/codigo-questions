---
language: javascript
exerciseType: 1
---

# --description--

Toda función tiene un método `bind` que devuelve una función **nueva** con algunas cosas fijadas de antemano. Su primer argumento pasa a ser el `this` de la nueva función; los argumentos restantes se colocan delante de aquellos con los que se llame a la nueva función (una **aplicación parcial**):
```javascript
function multiply(a, b) {
  return a * b;
}
const double = multiply.bind(null, 2);
console.log(double(21));
// prints 42
```
Fijar `this` importa en los métodos. Cuando un método se copia fuera de su objeto y se llama por su cuenta, `this` ya no se refiere al objeto, así que `this.name` pasa a ser `undefined`. `bind` lo fija al objeto:
```javascript
const user = {
  name: "Ana",
  hello() { return `Hi ${this.name}`; },
};
const loose = user.hello;
console.log(loose());
// prints Hi undefined
const bound = user.hello.bind(user);
console.log(bound());
// prints Hi Ana
```
La función original nunca cambia: `bind` siempre construye una nueva, cuyo `name` es el nombre original precedido por `bound `.

# --instructions--

La función `power` y el objeto `counter` ya están dados. Guarda en la constante `square` una versión de `power` ligada con `this` puesto a `null` y `exponent` fijado a `2`, y en la constante `tick` una versión de `counter.increment` ligada a `counter`, de modo que siga funcionando al llamarla por su cuenta

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = 
const tick = 
```

# --asserts--

`square(3)` debe devolver `9`.

```javascript
tryCatch(square(3) === 9);
```

`square(12)` debe devolver `144`.

```javascript
tryCatch(square(12) === 144);
```

`square` debe crearse con `bind`, de modo que su nombre sea `"bound power"`.

```javascript
tryCatch(square.name === "bound power");
```

`tick` debe ser una función nueva creada con `bind`, no `counter.increment` en sí.

```javascript
tryCatch(tick !== counter.increment && tick.name === "bound increment");
```

Llamar a `tick()` dos veces por su cuenta debe devolver `1` y luego `2`, y `counter.count` debe ser `2`.

```javascript
tryCatch(tick() === 1 && tick() === 2 && counter.count === 2);
```

`tick` debe seguir funcionando cuando se guarda en otra variable y se llama desde ahí.

```javascript
tryCatch((() => { const alone = tick; return alone() === 3; })());
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = power.bind(null, 2);
const tick = counter.increment.bind(counter);
```
