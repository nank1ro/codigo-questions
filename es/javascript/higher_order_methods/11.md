---
language: javascript
exerciseType: 1
---

# --description--

Una función que devuelve una función también es la forma natural de **adaptar** un callback. Supón que tienes un predicado y necesitas el contrario para `filter`: en vez de reescribirlo, envuélvelo:
```javascript
const isLong = (word) => word.length > 4;
const isShort = (word) => !isLong(word);
console.log(["tree", "forest"].filter(isShort));
// prints [ 'tree' ]
```
Un `not(predicate)` general haría esto para cualquier predicado: devuelve una nueva función que llama a `predicate` con el mismo argumento e invierte el resultado con `!`. Los predicados de `filter`, `find`, `some` y `every` reciben el elemento como primer argumento, así que el envoltorio solo necesita reenviar ese valor.

# --instructions--

Escribe una función `not(predicate)` que devuelva un nuevo predicado que devuelva `true` exactamente cuando `predicate` devuelve `false` para el mismo argumento

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function not(predicate) {

}
```

# --asserts--

`not((n) => n % 2 === 0)(3)` debe devolver `true`.

```javascript
tryCatch(not((n) => n % 2 === 0)(3) === true);
```

`not((n) => n % 2 === 0)(4)` debe devolver `false`.

```javascript
tryCatch(not((n) => n % 2 === 0)(4) === false);
```

`[1, 2, 3, 4].filter(not(isEven))` debe devolver `[1, 3]`.

```javascript
tryCatch((() => { const isEven = (n) => n % 2 === 0; return JSON.stringify([1, 2, 3, 4].filter(not(isEven))) === "[1,3]"; })());
```

`["a", "", "b"].filter(not((s) => s === ""))` debe devolver `["a", "b"]`.

```javascript
tryCatch(JSON.stringify(["a", "", "b"].filter(not((s) => s === ""))) === '["a","b"]');
```

Negar dos veces debe devolver la respuesta original: `not(not(isEven))(4)` debe devolver `true`.

```javascript
tryCatch(not(not((n) => n % 2 === 0))(4) === true);
```

`not` debe devolver una función.

```javascript
tryCatch(typeof not((n) => n > 0) === "function");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function not(predicate) {
  return (value) => !predicate(value);
}
```
