---
language: javascript
exerciseType: 4
---

# --description--

Los programas reales combinan estos métodos en una **tubería**: filtra los elementos que te interesan, mapéalos a los valores que necesitas y redúcelos a un resultado. Guardar los arrays intermedios en constantes hace legible cada paso y permite reutilizarlos:
```javascript
const adults = people.filter((p) => p.age >= 18);
const names = adults.map((p) => p.name);
const totalAge = adults.reduce((sum, p) => sum + p.age, 0);
```
`names.join(", ")` convierte un array de cadenas en una sola cadena con los elementos separados por una coma y un espacio.

# --instructions--

Ordena las líneas para quedarte solo con los artículos del carrito con `qty` positiva, recoger sus nombres, sumar `price * qty` para ellos e imprimir los nombres unidos por `", "` y el total en una sola línea. Lista los artículos en el orden pen, book, bag y declara `inStock`, `names` y `total` en ese orden

# --answers--

```javascript
const cart = [
```

```javascript
  { name: "pen", price: 2, qty: 3 },
```

```javascript
  { name: "book", price: 12, qty: 1 },
```

```javascript
  { name: "bag", price: 30, qty: 0 },
```

```javascript
];
```

```javascript
const inStock = cart.filter((item) => item.qty > 0);
```

```javascript
const names = inStock.map((item) => item.name);
```

```javascript
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
```

```javascript
console.log(names.join(", "), total);
```

# --solutions--

```javascript
const cart = [
  { name: "pen", price: 2, qty: 3 },
  { name: "book", price: 12, qty: 1 },
  { name: "bag", price: 30, qty: 0 },
];
const inStock = cart.filter((item) => item.qty > 0);
const names = inStock.map((item) => item.name);
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
console.log(names.join(", "), total);
```

# --output--

pen, book 18
