---
language: javascript
exerciseType: 4
---

# --description--

El comparador puede mirar cualquier parte de los elementos, así que un array de objetos se ordena por una de sus propiedades simplemente comparando esa propiedad:
```javascript
const items = [{ name: "b", size: 3 }, { name: "a", size: 1 }];
const bySize = [...items].sort((x, y) => x.size - y.size);
console.log(bySize.map((item) => item.name));
// prints [ 'a', 'b' ]
```
Ordenar la copia deja `items` en su orden original.

# --instructions--

Ordena las líneas para ordenar una copia de `players` por `score` ascendente, imprimir sus nombres en ese orden con `map`, y luego imprimir el nombre del primer elemento de `players` para mostrar que no cambió. Declara `ranked` antes que `names`

# --answers--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
```

```javascript
const ranked = [...players].sort((a, b) => a.score - b.score);
```

```javascript
const names = ranked.map((p) => p.name);
```

```javascript
console.log(names);
```

```javascript
console.log(players[0].name);
```

# --solutions--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
const ranked = [...players].sort((a, b) => a.score - b.score);
const names = ranked.map((p) => p.name);
console.log(names);
console.log(players[0].name);
```

# --output--

[ 'Leo', 'Zoe', 'Mia' ]
Zoe
