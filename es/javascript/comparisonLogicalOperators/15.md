---
language: javascript
exerciseType: 2
---

# --description--

Continuemos con el operador de comparación **menor que o igual a** `<=`.
Devuelve un **booleano** (`true` o `false`) indicando si una expresión es menor que o igual a la otra, por ejemplo:
```javascript
console.log(2 <= 2);
// imprime true
console.log(3 <= 2);
// imprime false
console.log(3 <= 4);
// imprime true
```

# --instructions--

Imprime un booleano verificando si `num` es menor que o igual a `8`

# --seed--

```javascript
var num = 7;
console.log([/][/][/]);
```

# --answers--

- num
- 'num'
-  <= 
-  = 
-  >= 
- 8

# --solutions--

```javascript
var num = 7;
console.log(num <= 8);
```

# --output--

true
