---
language: javascript
exerciseType: 2
---

# --description--

Dentro del bloque de código, para detener el bucle `while`, incrementamos la variable `counter`.

# --instructions--

Incrementa la variable `counter` dentro del bloque de código

# --seed--

```javascript
var counter = 3;
while (counter < 5) {
    console.log(counter);
    [/][/][/][/];
}
```

# --answers--

- counter
-  +
- = 
- -
- *
- 1

# --solutions--

```javascript
var counter = 3;
while (counter < 5) {
    console.log(counter);
    counter += 1;
}
```

# --output--

3
4
