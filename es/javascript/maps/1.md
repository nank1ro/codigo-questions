---
language: javascript
exerciseType: 2
---

# --description--

Un **Map** almacena **pares clave-valor**: cada valor se guarda bajo una clave, y usas esa clave para volver a encontrar el valor.
Creas un map vacío con `new Map()`, agregas un par con `set(key, value)` y lees un valor con `get(key)`:
```javascript
let ages = new Map();
ages.set("Ann", 30);
ages.set("Bob", 25);
console.log(ages.get("Ann"));
// prints 30
```
Llamar a `set()` con una clave que ya existe reemplaza su valor.

# --instructions--

Crea un map vacío `ages`, guarda `30` bajo la clave `"Ann"`, y luego imprime el valor guardado para `"Ann"`

# --seed--

```javascript
let ages = new [/]();
ages.[/]("Ann", 30);
console.log(ages.[/]("Ann"));
```

# --answers--

- Map
- set
- get
- Object
- add
- put
- read

# --solutions--

```javascript
let ages = new Map();
ages.set("Ann", 30);
console.log(ages.get("Ann"));
```

# --output--

30
