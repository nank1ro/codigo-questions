---
language: javascript
exerciseType: 2
---

# --description--

`delete(key)` devuelve `true` cuando se eliminó un par y `false` cuando la clave no estaba.
Para eliminar **todos** los pares de una vez, llama a `clear()`:
```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Elimina `"pen"` de `cart` imprimiendo el resultado de la eliminación, luego vacía todo el map e imprime cuántos pares quedan

# --seed--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
