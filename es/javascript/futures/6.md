---
language: javascript
exerciseType: 4
---

# --description--

Una función `async` se lee de arriba abajo como cualquier otra función: `await` simplemente la pausa hasta que la promesa esperada se cumple, y luego la ejecución continúa en la línea siguiente.
```javascript
async function main() {
  console.log("start");
  const value = await Promise.resolve("data");
  console.log(value);
  console.log("done");
}

main();
// prints start, then data, then done
```
Fíjate en la última línea: una función `async` igual hay que **llamarla**. Escribir `main` sin los paréntesis define el trabajo pero nunca lo inicia, y no se imprime nada.

# --instructions--

Ordena los elementos en un programa que defina una función `async` llamada `load` que devuelva `data`, luego una función `async` llamada `main` que imprima `start`, espere `load()` en `value`, imprima `value` y finalmente imprima `done`. Define `load` primero, luego `main`, y llama a `main()` en la última línea

# --answers--

```javascript
async function load() {
  return "data";
}
```

```javascript
async function main() {
```

```javascript
  console.log("start");
```

```javascript
  const value = await load();
```

```javascript
  console.log(value);
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
async function load() {
  return "data";
}
async function main() {
  console.log("start");
  const value = await load();
  console.log(value);
  console.log("done");
}
main();
```

# --output--

start
data
done
