---
language: javascript
exerciseType: 1
---

# --description--

`Promise.all` funciona con un array de cualquier longitud, incluido uno vacío: esperar `Promise.all([])` devuelve un array vacío de inmediato. Eso hace que sea seguro pasar una lista construida en tiempo de ejecución, sin un caso especial para "nada que esperar".
```javascript
const values = await Promise.all(items);
console.log(values.length === items.length);
// prints true
```
El array que devuelve siempre tiene exactamente tantos elementos como el array que recibió, en las mismas posiciones, así que se puede recorrer como cualquier otro array.

# --instructions--

Escribe una función `async` llamada `sumAll` que reciba un array de promesas de números, espere todas juntas con `Promise.all` y devuelva su suma. Un array vacío debe dar `0`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
async function sumAll(promises) {
  
}
```

# --asserts--

`await sumAll([])` debe ser `0`.

```javascript
(async () => {
  tryCatch((await sumAll([])) === 0);
})().catch(() => tryCatch(false));
```

`await sumAll([Promise.resolve(1), Promise.resolve(2), Promise.resolve(3)])` debe ser `6`.

```javascript
(async () => {
  const promises = [Promise.resolve(1), Promise.resolve(2), Promise.resolve(3)];
  tryCatch((await sumAll(promises)) === 6);
})().catch(() => tryCatch(false));
```

`await sumAll([Promise.resolve(-4), Promise.resolve(4)])` debe ser `0`.

```javascript
(async () => {
  const promises = [Promise.resolve(-4), Promise.resolve(4)];
  tryCatch((await sumAll(promises)) === 0);
})().catch(() => tryCatch(false));
```

`sumAll` también debe funcionar con promesas que tardan cantidades de tiempo distintas.

```javascript
(async () => {
  const later = (n, ms) => new Promise((resolve) => setTimeout(() => resolve(n), ms));
  tryCatch((await sumAll([later(10, 30), later(5, 5)])) === 15);
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
async function sumAll(promises) {
  const values = await Promise.all(promises);
  let total = 0;
  for (const value of values) {
    total += value;
  }
  return total;
}
```
