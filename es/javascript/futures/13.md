---
language: javascript
exerciseType: 4
---

# --description--

La diferencia entre esperar de forma **secuencial** y de forma **paralela** la decide *dónde* colocas `await`:
```javascript
// sequential: about 300 + 300 = 600 ms
const a = await load("a");
const b = await load("b");

// parallel: about 300 ms
const [a, b] = await Promise.all([load("a"), load("b")]);
```
En la primera versión la segunda descarga solo comienza cuando la primera ha terminado, porque `await` pausa la función en esa línea. En la segunda, ambas llamadas se hacen antes de esperar nada, así que las dos descargas ya se están ejecutando mientras `Promise.all` espera.

Usa `await` secuenciales solo cuando la segunda tarea realmente necesite el resultado de la primera. Si no, inicia todo primero y espera todo junto.

# --instructions--

Ordena los elementos en un programa que defina `later`, luego una función `async` llamada `main` que imprima `loading`, inicie ambas llamadas a `later` juntas con `Promise.all`, imprima los dos resultados unidos por un espacio y finalmente imprima `done`. Define `later` primero, luego `main`, y llama a `main()` en la última línea

# --answers--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
```

```javascript
async function main() {
```

```javascript
  console.log("loading");
```

```javascript
  const results = await Promise.all([later("one", 30), later("two", 10)]);
```

```javascript
  console.log(results.join(" "));
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
async function main() {
  console.log("loading");
  const results = await Promise.all([later("one", 30), later("two", 10)]);
  console.log(results.join(" "));
  console.log("done");
}
main();
```

# --output--

loading
ready two
ready one
one two
done
