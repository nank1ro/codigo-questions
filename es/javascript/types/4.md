---
language: javascript
exerciseType: 2
---

# --description--

Puedes convertir un valor a otro tipo **explícitamente** llamando al tipo como una función:
- `Number(value)` convierte a un número: `Number("42")` es `42`
- `String(value)` convierte a una cadena: `String(42)` es `"42"`
- `Boolean(value)` convierte a un booleano: `Boolean("")` es `false`

El resultado es un valor completamente nuevo; el original no se cambia:
```javascript
const input = "7";
const count = Number(input) + 1;
console.log(count, typeof count);
// prints 8 number
console.log(String(count) + "!");
// prints 8!
```
Convertir explícitamente hace visible tu intención: quien lee `Number(input)` sabe que `input` era texto.

# --instructions--

Completa el código para que `count` sea `input` convertido a número más `1`, `label` sea `count` convertido a cadena y repetido dos veces, y `flag` sea la cadena vacía convertida a booleano, produciendo la salida `43 4343 false`

# --seed--

```javascript
const input = "42";
const count = [/](input) + 1;
const label = [/](count).repeat(2);
const flag = [/]("");
console.log(count, label, flag);
```

# --answers--

- Number
- String
- Boolean
- Int
- Bool
- toString

# --solutions--

```javascript
const input = "42";
const count = Number(input) + 1;
const label = String(count).repeat(2);
const flag = Boolean("");
console.log(count, label, flag);
```

# --output--

43 4343 false
