---
language: javascript
exerciseType: 4
---

# --description--

JavaScript tiene un único tipo `number` para enteros y decimales: cada número es un valor de punto flotante de 64 bits (un *double*). Así que `5` y `5.0` son el mismo valor, y no hay un tipo entero separado:
```javascript
console.log(5 === 5.0, 10 / 2);
// prints true 5
```
Para preguntar si un número no tiene parte fraccionaria, usa `Number.isInteger`:
```javascript
console.log(Number.isInteger(5.0), Number.isInteger(3.5));
// prints true false
```
Los literales de plantilla convierten el valor interpolado a una cadena con las mismas reglas que `String()`, así que `${5.0}` se vuelve `"5"`, no `"5.0"`.

# --instructions--

Ordena las líneas para definir `half`, que divide `n` entre dos y devuelve una frase que dice si el resultado es entero, y luego imprime el resultado para `10` y para `7`. Define primero la función

# --answers--

```javascript
function half(n) {
```

```javascript
  const result = n / 2;
```

```javascript
  if (Number.isInteger(result)) {
```

```javascript
    return `${result} is whole`;
```

```javascript
  }
```

```javascript
  return `${result} is not whole`;
```

```javascript
}
```

```javascript
console.log(half(10));
```

```javascript
console.log(half(7));
```

# --solutions--

```javascript
function half(n) {
  const result = n / 2;
  if (Number.isInteger(result)) {
    return `${result} is whole`;
  }
  return `${result} is not whole`;
}
console.log(half(10));
console.log(half(7));
```

# --output--

5 is whole
3.5 is not whole
