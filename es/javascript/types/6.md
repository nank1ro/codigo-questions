---
language: javascript
exerciseType: 2
---

# --description--

Hay dos formas de preguntar "¿es esto `NaN`?", y responden a preguntas diferentes.
La antigua función global `isNaN(value)` primero **convierte** `value` a un número y luego comprueba. Así que dice `true` para cualquier cosa que no pueda convertirse en un número, aunque no sea `NaN` en absoluto:
```javascript
console.log(isNaN("hello"));
// imprime true, porque Number("hello") es NaN
console.log(isNaN("42"));
// imprime false, porque Number("42") es 42
```
`Number.isNaN(value)` **no** convierte: es `true` solo cuando `value` es realmente el número `NaN`:
```javascript
console.log(Number.isNaN("hello"));
// imprime false, una cadena no es NaN
console.log(Number.isNaN(Number("hello")));
// imprime true
```
Prefiere `Number.isNaN`, y convierte primero si quieres saber si una conversión falló.

# --instructions--

Completa el código para que la primera línea use la función global `isNaN`, la segunda use `Number.isNaN` sobre la cadena misma, y la tercera convierta `value` a un número antes de comprobar, produciendo la salida `true`, `false`, `true`

# --seed--

```javascript
const value = "hello";
console.log([/](value));
console.log([/](value));
console.log(Number.isNaN([/]));
```

# --answers--

- isNaN
- Number.isNaN
- Number(value)
- String(value)
- value
- isNumber

# --solutions--

```javascript
const value = "hello";
console.log(isNaN(value));
console.log(Number.isNaN(value));
console.log(Number.isNaN(Number(value)));
```

# --output--

true
false
true
