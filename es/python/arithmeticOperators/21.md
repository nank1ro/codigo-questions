---
language: python
exerciseType: 1
---

# --instructions--

Tienes `60` productos en la `store`, durante el día vendiste `50` de ellos y `compraste` `55` nuevos.
Actualiza la cantidad de `store`.

# --seed--

```python
store = 60
sold = 50
bought = 55
# write after this line
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

La cantidad de `store` debe ser igual a 65

```python
    def test_1(self):
        self.assertEqual(store, 65, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
store = 60
sold = 50
bought = 55
store += bought - sold
```
