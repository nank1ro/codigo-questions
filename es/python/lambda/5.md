---
language: python
exerciseType: 1
---

# --description--

Los parámetros de una lambda también admiten **valores por defecto**:
```python
greet = lambda name="World": f"Hello, {name}!"
print(greet())       # Hello, World!
print(greet("Ana"))  # Hello, Ana!
```

# --instructions--

Guarda en la variable `multiply` una lambda que tome `n` y un segundo parámetro `factor` con valor por defecto `2`, y retorne `n` multiplicado por `factor`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar una expresión `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`multiply(5)` debe retornar `10`

```python
    def test_1(self):
        self.assertEqual(multiply(5), 10, "--err-t1--")
```

`multiply(5, 3)` debe retornar `15`

```python
    def test_2(self):
        self.assertEqual(multiply(5, 3), 15, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
multiply = lambda n, factor=2: n * factor
```
