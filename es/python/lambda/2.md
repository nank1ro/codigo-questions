---
language: python
exerciseType: 1
---

# --description--

Observa que el cuerpo de la lambda no tiene la palabra clave `return`.
El cuerpo es una **expresión única**, y su valor se retorna automáticamente:
```python
square = lambda n: n * n
print(square(3))  # 9
```

# --instructions--

Guarda en la variable `square` una lambda que tome un parámetro `n` y retorne `n` multiplicado por sí mismo

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar una expresión `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`square(4)` debe retornar `16`

```python
    def test_1(self):
        self.assertEqual(square(4), 16, "--err-t1--")
```

`square(7)` debe retornar `49`

```python
    def test_2(self):
        self.assertEqual(square(7), 49, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
square = lambda n: n * n
```
