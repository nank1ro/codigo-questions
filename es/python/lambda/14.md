---
language: python
exerciseType: 1
---

# --description--

**¿Cuándo deberías preferir `def`?**
Una lambda es ideal para una función corta y desechable pasada como argumento.
Si la lógica necesita un nombre, varias líneas, un docstring, o se reutiliza en muchos lugares, una función `def` es más clara.
Un último truco: `sorted()` también acepta `reverse=True` para obtener primero los valores más grandes:
```python
print(sorted([3, 1, 2], reverse=True))  # [3, 2, 1]
```

# --instructions--

Guarda en `ranking` la lista `students` ordenada de mayor a menor `grade`, luego guarda en `names` la lista de los nombres de `ranking`, usando `sorted()` y `map()` con lambdas

# --seed--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar una expresión `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Debes usar `map()`

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ranking` debe empezar con el estudiante `Cy`

```python
    def test_1(self):
        self.assertEqual(ranking[0]["name"], "Cy", "--err-t1--")
```

`names` debe ser igual a `['Cy', 'Ana', 'Bo']`

```python
    def test_2(self):
        self.assertEqual(names, ["Cy", "Ana", "Bo"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
ranking = sorted(students, key=lambda s: s["grade"], reverse=True)
names = list(map(lambda s: s["name"], ranking))
```
