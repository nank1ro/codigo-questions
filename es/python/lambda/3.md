---
language: python
exerciseType: 2
---

# --description--

Una lambda puede tomar **más de un parámetro**.
Sepáralos con comas, igual que en una función `def`:
```python
power = lambda base, exp: base ** exp
print(power(2, 3))  # 8
```

# --instructions--

Completa la lambda para que tome los parámetros `a` y `b` y retorne su suma

# --seed--

```python
add = lambda a[/] b[/] a + b
print(add(3, 4))
```

# --answers--

- ,
- :
- ;
- ->

# --solutions--

```python
add = lambda a, b: a + b
print(add(3, 4))
```

# --output--

7
