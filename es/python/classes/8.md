---
language: python
exerciseType: 2
---

# --description--

Cuando una clase tiene sus propias funciones, esas funciones se llaman __métodos__. Ya has visto uno de esos métodos: `__init__()`.
¡Pero también puedes definir tus propios métodos!

# --instructions--

Agrega un método, `descripcion`, a tu clase `Animal`.
Usando dos declaraciones `print` separadas, debe imprimir el `genero` y las `patas` del animal en el que se llama.
Luego llama al método `descripcion`.

# --seed--

```python
class Animal:
    def __init__(self, genero, patas):
        self.genero = genero
        self.patas = patas
    def [/]([/]):
        [/]([/][/][/])
        [/]([/][/][/])

perro = Animal("hembra", 5)
perro.descripcion()
```

# --answers--

- descripcion
- self
- Animal
- print
- print
- self
- self
- genero
- patas
- .
- .

# --solutions--

```python
class Animal:
    def __init__(self, genero, patas):
        self.genero = genero
        self.patas = patas
    def descripcion(self):
        print(self.genero)
        print(self.patas)

perro = Animal("hembra", 5)
perro.descripcion()
```

# --output--

hembra
5
