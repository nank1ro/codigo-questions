---
language: python
exerciseType: 1
---

# --description--

Cuando ningún tipo integrado encaja, define el tuyo heredando de `Exception`. Un cuerpo vacío suele bastar — el nombre es el mensaje para quien lee:
```python
class ConfigError(Exception):
    pass
```
Se comporta como cualquier otra excepción: `raise ConfigError("bad port")`, y `except ConfigError:` la captura.

Traducir un fallo de bajo nivel a tu propio tipo es habitual, y el error original no debería perderse en el proceso. `raise NewError(...) from original` los **encadena**: guarda `original` en el atributo `__cause__` de la nueva excepción, y el traceback muestra ambos bajo *The above exception was the direct cause of the following exception*:
```python
try:
    port = int(text)
except ValueError as e:
    raise ConfigError("bad port") from e
```
Sin `from e` los dos siguen quedando enlazados de forma implícita, pero `from` dice en voz alta que el primer error causó el segundo.

# --instructions--

Define una clase de excepción `ConfigError` que herede de `Exception`, y escribe una función `read_port(text)` que devuelva `int(text)` y, cuando eso lance `ValueError`, lance `ConfigError("bad port")` encadenado desde ella con `raise ... from`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`read_port("8080")` debe devolver `8080`, y `ConfigError` debe heredar de `Exception`

```python
    def test_1(self):
        self.assertEqual(read_port("8080"), 8080, "--err-t1--")
        self.assertTrue(issubclass(ConfigError, Exception), "--err-t1--")
```

`read_port("abc")` debe lanzar un `ConfigError` con el mensaje `"bad port"`

```python
    def test_2(self):
        with self.assertRaises(ConfigError, msg="--err-t2--") as ctx:
            read_port("abc")
        self.assertEqual(str(ctx.exception), "bad port", "--err-t2--")
```

el `ConfigError` lanzado debe estar encadenado desde el `ValueError` original

```python
    def test_3(self):
        with self.assertRaises(ConfigError, msg="--err-t3--") as ctx:
            read_port("abc")
        self.assertIsInstance(ctx.exception.__cause__, ValueError, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
class ConfigError(Exception):
    pass

def read_port(text):
    try:
        return int(text)
    except ValueError as e:
        raise ConfigError("bad port") from e
```
