---
language: python
exerciseType: 1
---

# --description--

Una excepción es un objeto, y `as` la asocia a un nombre para que el manejador pueda examinarla:
```python
try:
    int("abc")
except ValueError as e:
    print(e)                   # invalid literal for int() with base 10: 'abc'
    print(type(e).__name__)    # ValueError
```
`str(e)` — que es lo que usan `print(e)` y una ranura de f-string — da el mensaje con el que se construyó la excepción, y `type(e).__name__` da el nombre de la clase como texto. El nombre asociado por `as` solo existe dentro del bloque `except`; Python lo elimina cuando el bloque termina.

# --instructions--

Escribe una función `error_label(text)` que devuelva `int(text)`, y cuando eso lance `ValueError` devuelva una cadena formada por el nombre de la clase de la excepción, `": "` y el mensaje de la excepción, por ejemplo `"ValueError: ..."`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`error_label("8")` debe devolver `8`

```python
    def test_1(self):
        self.assertEqual(error_label("8"), 8, "--err-t1--")
```

`error_label("abc")` debe empezar por el nombre de la clase

```python
    def test_2(self):
        self.assertTrue(error_label("abc").startswith("ValueError: "), "--err-t2--")
```

`error_label("abc")` también debe contener el mensaje, que menciona `abc`

```python
    def test_3(self):
        self.assertIn("abc", error_label("abc")[12:], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def error_label(text):
    try:
        return int(text)
    except ValueError as e:
        return f"{type(e).__name__}: {e}"
```
