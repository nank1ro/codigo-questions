---
language: python
exerciseType: 2
---

# --description--

Un bloque `try` puede ir seguido de **varias** cláusulas `except`, cada una manejando un fallo diferente con una recuperación diferente. Python compara la excepción lanzada contra ellas de arriba a abajo y ejecuta la **primera** que coincide; las demás se omiten:
```python
try:
    value = 100 / int(text)
except ValueError:
    print("not a number")
except ZeroDivisionError:
    print("cannot divide by zero")
```
Como la primera coincidencia gana, el orden importa cuando los tipos están relacionados: una cláusula para un tipo general colocada encima de una cláusula para uno más específico ganaría siempre, dejando la cláusula específica inalcanzable.

# --instructions--

Completa el código para que el texto ilegible y un divisor cero se informen por separado, produciendo la salida `2.0 not a number cannot divide by zero`

# --seed--

```python
def describe(text):
    try:
        return 10 / int(text)
    except [/]:
        return "not a number"
    except [/]:
        return "cannot divide by zero"

print(describe("5"), describe("abc"), describe("0"))
```

# --answers--

- ValueError
- ZeroDivisionError
- TypeError
- KeyError

# --solutions--

```python
def describe(text):
    try:
        return 10 / int(text)
    except ValueError:
        return "not a number"
    except ZeroDivisionError:
        return "cannot divide by zero"

print(describe("5"), describe("abc"), describe("0"))
```

# --output--

2.0 not a number cannot divide by zero
