---
language: python
exerciseType: 1
---

# --description--

El bloque `try` se detiene en la **primera** instrucción que lanza; las líneas posteriores se omiten y el control pasa al bloque `except`. Nada dentro del bloque `try` se deshace, así que mantenlo tan corto como sea posible:
```python
def half(n):
    try:
        return 10 / n
    except ZeroDivisionError:
        return "undefined"

print(half(2))  # 5.0
print(half(0))  # undefined
```
Un `return` dentro de `except` funciona como cualquier otro `return`, lo que hace de `try`/`except` una forma natural de devolver un valor de respaldo en lugar de fallar.

# --instructions--

Escribe una función `safe_divide(a, b)` que devuelva `a / b`, o la cadena `"undefined"` cuando `b` sea `0`, capturando `ZeroDivisionError`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_divide(9, 3)` debe devolver `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_divide(9, 3), 3.0, "--err-t1--")
```

`safe_divide(1, 0)` debe devolver `"undefined"`

```python
    def test_2(self):
        self.assertEqual(safe_divide(1, 0), "undefined", "--err-t2--")
```

`safe_divide(-5, 2)` debe devolver `-2.5`

```python
    def test_3(self):
        self.assertEqual(safe_divide(-5, 2), -2.5, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "undefined"
```
