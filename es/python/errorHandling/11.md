---
language: python
exerciseType: 1
---

# --description--

Un bloque `finally` se ejecuta **pase lo que pase**: tras un bloque `try` limpio, tras un bloque `except`, incluso mientras una excepción que nadie capturó viaja hacia fuera, e incluso cuando el bloque `try` o `except` ejecuta un `return`:
```python
def read(text):
    try:
        return int(text)
    except ValueError:
        return 0
    finally:
        print("done")
```
Ambos caminos imprimen `done` antes de que el valor salga de la función. Esa garantía es para lo que sirve `finally`: cerrar un fichero, liberar un bloqueo, restaurar un ajuste. La forma completa es `try` / `except` / `else` / `finally`; un `try` necesita al menos un `except` o un `finally`, y `else` solo funciona junto a un `except`.

# --instructions--

Escribe una función `record(text)` que parta de una lista vacía, le añada `int(text)`, añada en su lugar la cadena `"bad"` cuando `int(text)` lance `ValueError`, añada siempre `"done"` en un bloque `finally`, y devuelva la lista

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`record("5")` debe devolver `[5, "done"]`

```python
    def test_1(self):
        self.assertEqual(record("5"), [5, "done"], "--err-t1--")
```

`record("x")` debe devolver `["bad", "done"]`

```python
    def test_2(self):
        self.assertEqual(record("x"), ["bad", "done"], "--err-t2--")
```

cada llamada debe partir de una lista nueva

```python
    def test_3(self):
        record("1")
        self.assertEqual(record("2"), [2, "done"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def record(text):
    log = []
    try:
        log.append(int(text))
    except ValueError:
        log.append("bad")
    finally:
        log.append("done")
    return log
```
