---
language: python
exerciseType: 1
---

# --description--

También podemos dar a las variables los valores de otras variables. Aquí, podemos darle a la variable `y` el valor de `x`
```python
>>> x = 5
>>> y = x
>>> print(y)
5
```

# --instructions--

Crea una nueva variable llamada `y` y asígnale el valor de la variable `x`.

# --seed--

```python
x = 1
# escribe después de esta línea
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`y` debe ser igual a `x`.

```python
    def test_1(self):
        self.assertEqual(x, y, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
x = 1
y = x
```
