---
language: python
exerciseType: 3
---

# --instructions--

¿Cómo verificamos si una lista está vacía?

# --answers--

- if len(nombre_lista) == 0:
- if nombre_lista == 0:
- if nombre_lista.length == 0:
- if nombre_lista[0] == False:
- if not nombre_lista:

# --solutions--

- if not nombre_lista:
- if len(nombre_lista) == 0:
