---
language: python
exerciseType: 2
---

# --description--

Puede haber momentos en los que queramos cambiar nuestra tupla a una lista.
Para hacer esto, podemos usar la función `list()`

# --instructions--

Usa la función `list()` para convertir la tupla a una lista

# --seed--

```python
tuple1 = ("dog", "bear", "cat")
list1 = [/]
list1.append("frog")
print(list1)
```

# --answers--

- list[tuple1]
- list(tuple1)

# --solutions--

```python
tuple1 = ("dog", "bear", "cat")
list1 = list(tuple1)
list1.append("frog")
print(list1)
```

# --output--

['dog', 'bear', 'cat', 'frog']
