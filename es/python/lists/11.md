---
language: python
exerciseType: 2
---

# --description--

Puedes dividir una cadena exactamente igual que una lista. De hecho, puedes pensar en las cadenas como listas de caracteres: cada carácter es un elemento secuencial en la lista, comenzando desde el índice `0`.
```python
nombre_lista[:2]
# Toma los dos primeros elementos
nombre_lista[3:]
# Toma desde el cuarto hasta los últimos elementos
```
Si tu porción de lista incluye el primer o último elemento de una lista (o una cadena), no es necesario incluir el índice de ese elemento.

# --instructions--

- Asigna a `nombre` una porción de `nombre_completo` desde el índice `0` hasta *pero sin incluir* el índice `4`.
- Asigna a `apellido` una porción de `nombre_completo` desde el índice `4` hasta el final de la cadena

# --seed--

```python
nombre_completo = "Sam Smith"
nombre = [/][/]
apellido = [/][/]
print(nombre)
print(apellido)
```

# --answers--

- nombre_completo
- [:4]
- [:3]
- nombre_completo
- [4:]
- [4:2]

# --solutions--

```python
nombre_completo = "Sam Smith"
nombre = nombre_completo[:3]
apellido = nombre_completo[4:]
print(nombre)
print(apellido)
```

# --output--

Sam
Smith
