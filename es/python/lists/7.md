---
language: python
exerciseType: 2
---

# --description--

¡Puedes dividir una cadena exactamente como una lista! De hecho, puedes pensar en las cadenas como listas de caracteres: cada carácter es un elemento secuencial en la lista, comenzando desde el índice `0`.
```python
list_name[:2]
# Grabs the first two items
list_name[3:]
# Grabs the fourth through last items
```
Si tu porción de lista incluye el primer o último elemento en una lista (o una cadena), el índice de ese elemento no tiene que incluirse.

# --instructions--

- Asigna a `first_name` una porción de `full_name` desde el índice `0` hasta *pero sin incluir* el índice `4`.
- Asigna a `last_name` una porción de `full_name` desde el índice `4` hasta el final de la cadena

# --seed--

```python
full_name = "Sam Smith"
first_name = [/][/]
last_name = [/][/]
print(first_name)
print(last_name)
```

# --answers--

- full_name
- [:4]
- [:3]
- full_name
- [4:]
- [4:2]

# --solutions--

```python
full_name = "Sam Smith"
first_name = full_name[:3]
last_name = full_name[4:]
print(first_name)
print(last_name)
```

# --output--

Sam
Smith
