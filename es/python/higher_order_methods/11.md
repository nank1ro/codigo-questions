---
language: python
exerciseType: 1
---

# --description--

`partial` resulta útil con funciones integradas que aceptan opciones. `int(text, base=16)` interpreta una cadena hexadecimal; fijar la base da un convertidor de un argumento que encaja con `map`:
```python
from functools import partial

hex_to_int = partial(int, base=16)
print(hex_to_int("ff"))                      # 255
print(list(map(hex_to_int, ["a", "10"])))    # [10, 16]
```
El objeto partial recuerda lo que envuelve: `hex_to_int.func` es `int`, y `hex_to_int.keywords` es `{'base': 16}`.

# --instructions--

Define dos funciones con `partial`: `hex_to_int` que convierta una cadena hexadecimal en un `int`, y `binary_to_int` que convierta una cadena binaria en un `int`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`hex_to_int("ff")` debe retornar `255`

```python
    def test_1(self):
        self.assertEqual(hex_to_int("ff"), 255, "--err-t1--")
        self.assertEqual(hex_to_int("10"), 16, "--err-t1--")
```

`binary_to_int("1010")` debe retornar `10`

```python
    def test_2(self):
        self.assertEqual(binary_to_int("1010"), 10, "--err-t2--")
        self.assertEqual(binary_to_int("11"), 3, "--err-t2--")
```

Ambos deben ser objetos partial que envuelvan `int`

```python
    def test_3(self):
        self.assertIs(hex_to_int.func, int, "--err-t3--")
        self.assertIs(binary_to_int.func, int, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import partial

hex_to_int = partial(int, base=16)
binary_to_int = partial(int, base=2)
```
