---
language: python
exerciseType: 1
---

# --description--

Pasar una función como argumento permite que quien llama decida **qué** hacer, mientras que la función de orden superior decide **cuántas veces** o **sobre qué**. El parámetro función puede llamarse tantas veces como sea necesario, y su resultado puede volver a pasársele:
```python
def repeat(func, value, times):
    for _ in range(times):
        value = func(value)
    return value

def add_one(n):
    return n + 1

print(repeat(add_one, 0, 3))  # 3
```
Cualquier callable sirve: una función `def`, una función integrada como `len` o una `lambda`.

# --instructions--

Escribe una función `apply_twice` que tome una función `func` y un `value`, y retorne el resultado de aplicar `func` a `value` dos veces seguidas

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`apply_twice(lambda x: x * 2, 3)` debe retornar `12`

```python
    def test_1(self):
        self.assertEqual(apply_twice(lambda x: x * 2, 3), 12, "--err-t1--")
```

`apply_twice(lambda s: s + "!", "hi")` debe retornar `"hi!!"`

```python
    def test_2(self):
        self.assertEqual(apply_twice(lambda s: s + "!", "hi"), "hi!!", "--err-t2--")
```

La segunda llamada debe recibir el resultado de la primera

```python
    def test_3(self):
        seen = []
        def record(x):
            seen.append(x)
            return x + 10
        self.assertEqual(apply_twice(record, 0), 20, "--err-t3--")
        self.assertEqual(seen, [0, 10], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def apply_twice(func, value):
    return func(func(value))
```
