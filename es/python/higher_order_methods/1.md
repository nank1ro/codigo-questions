---
language: python
exerciseType: 2
---

# --description--

En Python una función es un **valor**, igual que un número o una cadena. Puedes guardarla en una variable, meterla en una lista o pasarla a otra función. Solo los paréntesis la llaman: `shout` es la función en sí, `shout("hi")` es su resultado:
```python
def shout(text):
    return text.upper() + "!"

say = shout
print(say("hi"))  # HI!
```
Una función que recibe otra función como parámetro, o que retorna una, se llama **función de orden superior**. Dentro de ella, el parámetro se llama con paréntesis como cualquier otra función:
```python
def apply(func, value):
    return func(value)

print(apply(shout, "bye"))  # BYE!
print(apply(len, "bye"))    # 3
```

# --instructions--

Completa el código para que `apply` llame a la función que recibe, y `say` se refiera a `shout` sin llamarla, produciendo la salida `HI!`, `3`

# --seed--

```python
def shout(text):
    return text.upper() + "!"

def apply(func, value):
    return [/](value)

say = [/]
print(say("hi"))
print(apply(len, "bye"))
```

# --answers--

- func
- shout
- shout()
- value
- print

# --solutions--

```python
def shout(text):
    return text.upper() + "!"

def apply(func, value):
    return func(value)

say = shout
print(say("hi"))
print(apply(len, "bye"))
```

# --output--

HI!
3
