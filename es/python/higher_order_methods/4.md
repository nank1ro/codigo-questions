---
language: python
exerciseType: 1
---

# --description--

La función integrada `filter(func, iterable)` conserva solo los elementos para los que `func` retorna un valor verdadero. Como `map`, retorna un objeto perezoso que debe convertirse en lista:
```python
numbers = [4, -2, 7, 0]
print(list(filter(lambda n: n > 0, numbers)))  # [4, 7]
```
La función que se pasa a `filter` se llama **predicado**: toma un elemento y responde una pregunta de sí/no sobre él. Pasar `None` en lugar de una función conserva los elementos que son verdaderos por sí mismos, descartando `0`, `""` y `None`.

# --instructions--

Escribe una función `positives` que tome una lista de números y retorne una nueva lista con solo los números mayores que cero, usando `filter`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `filter`

```json
{
  "regex": "filter\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`positives([3, -1, 0, 5])` debe retornar `[3, 5]`

```python
    def test_1(self):
        self.assertEqual(positives([3, -1, 0, 5]), [3, 5], "--err-t1--")
```

El resultado debe ser una lista

```python
    def test_2(self):
        self.assertIsInstance(positives([1, 2]), list, "--err-t2--")
        self.assertEqual(positives([1, 2]), [1, 2], "--err-t2--")
```

`positives([-4, 0])` debe retornar una lista vacía

```python
    def test_3(self):
        self.assertEqual(positives([-4, 0]), [], "--err-t3--")
        self.assertEqual(positives([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def positives(numbers):
    return list(filter(lambda n: n > 0, numbers))
```
