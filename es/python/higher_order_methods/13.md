---
language: python
exerciseType: 1
---

# --description--

Un decorador que solo acepta un argumento es de poca utilidad. Para envolver **cualquier** función, el wrapper recoge cada argumento posicional en `*args` y cada argumento de palabra clave en `**kwargs`, y los reenvía sin cambios:
```python
def uppercase(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs).upper()
    return wrapper

@uppercase
def greet(name, punctuation="!"):
    return "hello " + name + punctuation

print(greet("ada"))                    # HELLO ADA!
print(greet("ada", punctuation="?"))   # HELLO ADA?
```
Dentro del wrapper, `args` es una tupla y `kwargs` un diccionario; el `*` y el `**` en la llamada los desempaquetan de nuevo en argumentos separados.

# --instructions--

Escribe un decorador `exclaim` que retorne un wrapper que acepte cualquier argumento, llame a la función envuelta con ellos y retorne su resultado con `"!"` añadido; luego úsalo para decorar una función `greet` que tome un `name` y retorne `"hello <name>"`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes aplicar el decorador con `@exclaim`

```json
{
  "regex": "^@exclaim\\s*$",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greet("ada")` debe retornar `"hello ada!"`

```python
    def test_1(self):
        self.assertEqual(greet("ada"), "hello ada!", "--err-t1--")
```

`exclaim` debe funcionar con una función de dos argumentos

```python
    def test_2(self):
        joined = exclaim(lambda a, b: a + " " + b)
        self.assertEqual(joined("good", "day"), "good day!", "--err-t2--")
```

`exclaim` debe reenviar los argumentos de palabra clave

```python
    def test_3(self):
        tag = exclaim(lambda word, times=1: word * times)
        self.assertEqual(tag("hi", times=2), "hihi!", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def exclaim(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs) + "!"
    return wrapper

@exclaim
def greet(name):
    return "hello " + name
```
