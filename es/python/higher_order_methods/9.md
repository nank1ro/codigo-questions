---
language: python
exerciseType: 1
---

# --description--

`reduce(func, iterable, initial)` del módulo `functools` pliega una secuencia en un **único valor**. Llama a `func` con el resultado hasta el momento y el siguiente elemento, empezando por `initial`:
```python
from functools import reduce

total = reduce(lambda acc, n: acc + n, [1, 2, 3], 0)
print(total)  # 6
```
Los pasos son `0 + 1`, luego `1 + 2`, luego `3 + 3`. Cuando se omite `initial`, el primer elemento se usa como valor inicial, pero entonces una secuencia vacía lanza un `TypeError`, así que da un valor inicial siempre que la secuencia pueda estar vacía.

# --instructions--

Escribe una función `product` que tome una lista de números y los retorne a todos multiplicados entre sí, usando `reduce`; el producto de una lista vacía debe ser `1`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `reduce`

```json
{
  "regex": "reduce\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`product([2, 3, 4])` debe retornar `24`

```python
    def test_1(self):
        self.assertEqual(product([2, 3, 4]), 24, "--err-t1--")
```

`product([5])` debe retornar `5`

```python
    def test_2(self):
        self.assertEqual(product([5]), 5, "--err-t2--")
```

`product([])` debe retornar `1`

```python
    def test_3(self):
        self.assertEqual(product([]), 1, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import reduce

def product(numbers):
    return reduce(lambda acc, n: acc * n, numbers, 1)
```
