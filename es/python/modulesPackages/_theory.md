Un __módulo__ es un archivo que puede contener cualquier tipo de código Python.
Los módulos nos permiten usar código una y otra vez.

---

Un __módulo__ es un archivo que puede contener cualquier tipo de código Python.
Los módulos nos permiten usar código una y otra vez.

---

También podemos importar solo las funciones específicas que necesitamos de un módulo usando las palabras clave `from` e `import`

---

También podemos renombrar una función al importarla usando la palabra clave `as`
