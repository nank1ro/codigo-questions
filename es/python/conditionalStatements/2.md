---
language: python
exerciseType: 2
---

# --description--

Acabamos de ver cómo ejecutar un bloque de código si se cumple una condición, ahora veamos cómo ejecutar otro bloque de código si la primera condición falla.
Vamos a jugar afuera si el clima es agradable; de lo contrario, nos quedamos en casa.
En Python podemos usar la sentencia `else`, como:
```python
buen_clima = True
if (buen_clima):
    # jugar afuera
else:
    # quedarse en casa
```

# --instructions--

Crea una sentencia `else` y en el bloque de código imprime la cadena `"Es de tarde"`

# --seed--

```python
hora = 15
if (hora < 12):
    print("Es de mañana")
[/][/]
    print("Es de tarde")
```

# --answers--

- Es de tarde
- 12
- else
-  < 
-  > 
- :
- ;

# --solutions--

```python
hora = 15
if (hora < 12):
    print("Es de mañana")
else:
    print("Es de tarde")
```

# --output--

Es de tarde
