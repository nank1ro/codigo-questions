---
language: python
exerciseType: 2
---

# --description--

Se requiere tomar decisiones cuando queremos ejecutar código solo si se cumple cierta condición.
Supongamos que queremos jugar afuera solo si el clima es agradable.
En programación, podemos guardar una variable booleana `nice_weather` y realizar la acción de jugar afuera `if` esta variable es `True`, como:
```python
nice_weather = True
if (nice_weather):
    # play outside
```

# --instructions--

Crea una instrucción `if` que verifique si `time` es menor que `12`, luego en el bloque de código imprime la cadena `"It's morning"`

# --seed--

```python
time = 8
[/] (time < 12):
    print("It's morning")
```

# --answers--

- if
- ==
- =
- else
- ?

# --solutions--

```python
time = 8
if (time < 12):
    print("It's morning")
```

# --output--

It's morning
