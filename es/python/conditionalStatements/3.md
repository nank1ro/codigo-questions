---
language: python
exerciseType: 2
---

# --description--

Supongamos que tenemos otra condición para verificar, como en este ejemplo:
```python
numero = 3
if (numero == 2):
    print("el número es 2")
elif (numero == 3):
    print("el número es 3")
else:
    print("hacer otra cosa")
```
y la salida de este código es `el número es 3`.
Primero, verifiquemos si el número es igual a 2, esto es falso.
Así que pasemos a la segunda sentencia y verifiquemos si `numero` es igual a 3, siendo verdadero ejecutamos el siguiente bloque de código imprimiendo `el número es 3`

# --instructions--

Crea una sentencia `elif` que verifique si `hora` es igual a `12` y, en el bloque de código, imprime la cadena `"Es mediodía"`

# --seed--

```python
hora = 12
if (hora < 12):
    print("Es de mañana")
[/]([/][/][/])[/]
    print("[/]")
else:
    print("Es de tarde")
```

# --answers--

- Es mediodía
- 12
- elif 
-  < 
-  == 
- :
- ;
- hora

# --solutions--

```python
hora = 12
if (hora < 12):
    print("Es de mañana")
elif (hora == 12):
    print("Es mediodía")
else:
    print("Es de tarde")
```

# --output--

Es mediodía
