---
language: python
exerciseType: 2
---

# --description--

Debido a que los diccionarios son mutables, se pueden cambiar de muchas formas. Los elementos se pueden eliminar de un diccionario con el comando `del`:
```python
del dict_name[key_name]
```
eliminará la clave `key_name` y su valor asociado del diccionario.

# --instructions--

Elimina la clave `age` del diccionario

# --seed--

```python
user = {"first_name": "John", "last_name": "Hood", "age": 30}
[/][/][/]
print(user)
```

# --answers--

- del 
- delete
- clear
- pop
- user
- ["age"]

# --solutions--

```python
user = {"first_name": "John", "last_name": "Hood", "age": 30}
del user["age"]
print(user)
```

# --output--

{'first_name': 'John', 'last_name': 'Hood'}
