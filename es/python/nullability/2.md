---
language: python
exerciseType: 1
---

# --description--

Cada llamada a una función produce un valor, incluso cuando la función parece no devolver nada. Una función **sin** instrucción `return`, o con un `return` sin valor, devuelve `None`:
```python
def say_hello():
    print("hello")

result = say_hello()
print(result)  # None
```
Por esto al llamar `print(my_list.append(3))` se muestra `None`: `append` modifica la lista en su lugar y no devuelve nada.
Una función que solo realiza una acción (imprimir, guardar, modificar una lista) normalmente devuelve `None`, mientras que una función que calcula algo debe devolverlo explícitamente con `return`.

# --instructions--

Escribe una función `store` que reciba un valor y lo añada a la lista `saved`, sin devolver nada

# --seed--

```python
saved = []

def store(value):
    pass
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `append`

```json
{
  "regex": "\\.append\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`store("a")` debe devolver `None`

```python
    def test_1(self):
        self.assertIsNone(store("a"), "--err-t1--")
```

Tras `store("b")` la lista `saved` debe terminar con `"b"`

```python
    def test_2(self):
        store("b")
        self.assertEqual(saved[-1], "b", "--err-t2--")
```

Cada valor guardado debe conservarse, en orden

```python
    def test_3(self):
        before = len(saved)
        store(1)
        store(2)
        self.assertEqual(saved[before:], [1, 2], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
saved = []

def store(value):
    saved.append(value)
```
