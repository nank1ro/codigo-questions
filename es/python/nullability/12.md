---
language: python
exerciseType: 1
---

# --description--

Una búsqueda seguida de una comprobación de `None` normalmente necesita dos líneas: una para guardar el resultado y otra para probarlo. El operador de **expresión de asignación** `:=`, apodado el *walrus*, asigna un valor **dentro** de una expresión, así que ambos pasos caben en el `if`:
```python
ages = {"Ada": 36}
if (age := ages.get("Ada")) is not None:
    print(age + 1)  # 37
```
Los paréntesis son obligatorios: sin ellos, `:=` intentaría asignar toda la comparación. Después del `if`, `age` sigue disponible como cualquier otra variable.

# --instructions--

Escribe una función `describe_user` que reciba un diccionario `users` que mapea nombres a edades y un `name`; debe devolver `"<name> is <age>"` cuando el nombre está presente, y `"unknown user"` en caso contrario, usando el operador walrus para guardar el resultado de `get`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar el operador `:=`

```json
{
  "regex": ":=",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Debes usar `get`

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe_user({"Ada": 36}, "Ada")` debe devolver `"Ada is 36"`

```python
    def test_1(self):
        self.assertEqual(describe_user({"Ada": 36}, "Ada"), "Ada is 36", "--err-t1--")
```

`describe_user({"Ada": 36}, "Grace")` debe devolver `"unknown user"`

```python
    def test_2(self):
        self.assertEqual(describe_user({"Ada": 36}, "Grace"), "unknown user", "--err-t2--")
```

Una edad de `0` debe seguir contando como presente

```python
    def test_3(self):
        self.assertEqual(describe_user({"Tim": 0}, "Tim"), "Tim is 0", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe_user(users, name):
    if (age := users.get(name)) is not None:
        return f"{name} is {age}"
    return "unknown user"
```
