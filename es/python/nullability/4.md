---
language: python
exerciseType: 2
---

# --description--

`None` cuenta como **falso** en una condición, así que `if not value:` es `True` cuando `value` es `None`. Es tentador usarlo como comprobación de `None`, pero la misma prueba también es `True` para `0`, `""`, `[]` y cualquier otro valor vacío:
```python
count = 0
if not count:
    print("missing?")   # printed, but 0 is a real value!
```
Cuando "sin valor" y "valor vacío" deben tratarse de forma distinta, comprueba primero `is None` y después la veracidad:
```python
if count is None:
    print("no count yet")
elif not count:
    print("count is zero")
```
Usa `if not value:` solo cuando de verdad quieras tratar `None` y los valores vacíos de la misma manera.

# --instructions--

Completa la función para que devuelva `missing` para `None`, `empty` para los valores vacíos y `present` en caso contrario, produciendo la salida `missing`, `empty`, `present`

# --seed--

```python
def describe(value):
    if value [/] None:
        return "missing"
    if [/] value:
        return "empty"
    return "present"

print(describe(None))
print(describe(0))
print(describe("hi"))
```

# --answers--

- is
- not
- is not
- in
- and

# --solutions--

```python
def describe(value):
    if value is None:
        return "missing"
    if not value:
        return "empty"
    return "present"

print(describe(None))
print(describe(0))
print(describe("hi"))
```

# --output--

missing
empty
present
