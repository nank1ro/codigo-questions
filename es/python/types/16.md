---
language: python
exerciseType: 1
---

# --description--

El texto que viene de fuera siempre es una `str`, y depende de tu programa averiguar qué tipo contiene realmente.
Un enfoque común es probar primero la conversión más **estricta**, y recurrir a la siguiente cuando lanza un `ValueError`:
```python
try:
    value = int(text)
except ValueError:
    value = float(text)
```
Anidar un segundo `try` dentro del bloque `except` te permite recurrir una vez más, por ejemplo para conservar el texto tal cual.

# --instructions--

Escribe una función `parse_value` que reciba una cadena y la devuelva como un `int` si es un número entero, como un `float` si es un número decimal, y sin cambios en caso contrario

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes capturar `ValueError` con `except`

```json
{
  "regex": "except\\s+ValueError",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_value("42")` debe devolver el `int` `42`

```python
    def test_1(self):
        self.assertEqual(parse_value("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_value("42"), int, "--err-t1--")
        self.assertEqual(parse_value("-8"), -8, "--err-t1--")
```

`parse_value("3.5")` debe devolver el `float` `3.5`

```python
    def test_2(self):
        self.assertEqual(parse_value("3.5"), 3.5, "--err-t2--")
        self.assertIsInstance(parse_value("3.5"), float, "--err-t2--")
```

`parse_value("abc")` debe devolver `"abc"`

```python
    def test_3(self):
        self.assertEqual(parse_value("abc"), "abc", "--err-t3--")
        self.assertEqual(parse_value(""), "", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_value(text):
    try:
        return int(text)
    except ValueError:
        try:
            return float(text)
        except ValueError:
            return text
```
