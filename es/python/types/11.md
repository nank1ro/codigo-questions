---
language: python
exerciseType: 1
---

# --description--

El formato solo cambia cómo se muestra un número. Para obtener un **valor** redondeado usa la función integrada `round()`:
```python
print(round(3.14159, 2))  # 3.14
print(round(2.71828, 1))  # 2.7
```
Con un solo argumento `round()` redondea al número entero más cercano y devuelve un `int`; con un número de decimales devuelve un `float`:
```python
print(round(3.7))     # 4
print(round(3.7, 0))  # 4.0
```
Ten en cuenta que los valores exactamente a medio camino entre dos números se redondean al **par**: `round(2.5)` es `2` y `round(3.5)` es `4`.

# --instructions--

Escribe una función `average` que reciba una lista de números y devuelva su promedio redondeado a dos decimales

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `round()`

```json
{
  "regex": "round\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`average([1, 2, 3, 4])` debe devolver `2.5`

```python
    def test_1(self):
        self.assertEqual(average([1, 2, 3, 4]), 2.5, "--err-t1--")
```

`average([1, 2, 2])` debe devolver `1.67`

```python
    def test_2(self):
        self.assertEqual(average([1, 2, 2]), 1.67, "--err-t2--")
        self.assertEqual(average([1, 1, 1, 2]), 1.25, "--err-t2--")
```

`average([10])` debe devolver el float `10.0`

```python
    def test_3(self):
        self.assertIsInstance(average([10]), float, "--err-t3--")
        self.assertEqual(average([10]), 10.0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def average(numbers):
    return round(sum(numbers) / len(numbers), 2)
```
