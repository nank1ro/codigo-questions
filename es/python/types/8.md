---
language: python
exerciseType: 2
---

# --description--

`bool` es una **subclase** de `int`: `True` se comporta como `1` y `False` como `0` dondequiera que se espera un número:
```python
print(True + 1)     # 2
print(True == 1)    # True
print(False * 10)   # 0
```
`sum()` suma los elementos de una lista, así que sumar una lista de booleanos **cuenta** cuántos son `True`:
```python
answers = [True, False, True]
print(sum(answers))  # 2
```
Debido a la relación de subclase, `isinstance(True, int)` devuelve `True`, mientras que `type(True)` sigue siendo `bool`.

# --instructions--

Completa el código para que cuente las respuestas correctas, sume `1` a `True` y compruebe que `True` es una instancia de `int`, imprimiendo `3`, `2` y `True`

# --seed--

```python
answers = [True, False, True, True]
correct = [/](answers)
print(correct)
print(True [/] 1)
print(isinstance(True, [/]))
```

# --answers--

- sum
- +
- int
- len
- ==
- str

# --solutions--

```python
answers = [True, False, True, True]
correct = sum(answers)
print(correct)
print(True + 1)
print(isinstance(True, int))
```

# --output--

3
2
True
