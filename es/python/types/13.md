---
language: python
exerciseType: 1
---

# --description--

A diferencia de muchos lenguajes, los enteros de Python **no tienen tamaño máximo**: un `int` crece para contener tantos dígitos como sean necesarios, así que los cálculos grandes siguen siendo exactos:
```python
print(3 ** 100)  # 515377520732011331036461129765621272702107522001
```
Un `float`, por otro lado, conserva solo unos 15 dígitos significativos, así que la misma potencia como float pierde precisión:
```python
print(3.0 ** 100)  # 5.153775207320113e+47
```
Como `str()` funciona con cualquier `int`, una forma rápida de contar los dígitos de un número es medir la longitud de su texto.

# --instructions--

Guarda en `big` el valor de `2` elevado a `200`, luego escribe una función `digits` que reciba un `int` positivo y devuelva cuántos dígitos tiene

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `str()`

```json
{
  "regex": "str\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`big` debe ser `2 ** 200`

```python
    def test_1(self):
        self.assertEqual(big, 2 ** 200, "--err-t1--")
        self.assertIsInstance(big, int, "--err-t1--")
```

`digits(big)` debe devolver `61`

```python
    def test_2(self):
        self.assertEqual(digits(big), 61, "--err-t2--")
```

`digits(7)` debe devolver `1` y `digits(10 ** 50)` debe devolver `51`

```python
    def test_3(self):
        self.assertEqual(digits(7), 1, "--err-t3--")
        self.assertEqual(digits(10 ** 50), 51, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
big = 2 ** 200

def digits(number):
    return len(str(number))
```
