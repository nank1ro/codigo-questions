---
language: python
exerciseType: 1
---

# --description--

Ya conoces los operadores aritméticos. Lo que importa aquí es el **tipo del resultado**.
Combinar un `int` con un `float` da un `float`, incluso cuando la parte decimal es cero:
```python
print(3 + 0.5)   # 3.5
print(2 * 1.0)   # 2.0
```
La **división verdadera** `/` siempre devuelve un `float`, incluso cuando los números se dividen exactamente:
```python
print(10 / 5)    # 2.0
print(7 / 2)     # 3.5
```
La **división entera** `//` redondea el resultado hacia abajo al número entero más cercano (así `-7 // 2` es `-4`) y devuelve un `int` cuando ambos operandos son enteros. Junto con el resto `%` divide una cantidad en partes enteras:
```python
print(7 // 2)    # 3
print(7 % 2)     # 1
```

# --instructions--

Guarda en `each` el resultado de dividir `total` entre `people` con `/`, en `whole` la misma división con `//` y en `rest` el resto

# --seed--

```python
total = 17
people = 5
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `//`

```json
{
  "regex": "//",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`each` debe ser el float `3.4`

```python
    def test_1(self):
        self.assertIsInstance(each, float, "--err-t1--")
        self.assertEqual(each, 3.4, "--err-t1--")
```

`whole` debe ser el entero `3`

```python
    def test_2(self):
        self.assertIsInstance(whole, int, "--err-t2--")
        self.assertEqual(whole, 3, "--err-t2--")
```

`rest` debe ser `2`

```python
    def test_3(self):
        self.assertEqual(rest, 2, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
total = 17
people = 5

each = total / people
whole = total // people
rest = total % people
```
