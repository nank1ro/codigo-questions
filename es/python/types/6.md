---
language: python
exerciseType: 1
---

# --description--

Una conversión puede fallar. `int("abc")` no puede producir un número, así que lanza un `ValueError` y el programa se detiene:
```python
int("abc")   # ValueError: invalid literal for int() with base 10: 'abc'
int("3.5")   # ValueError as well: "3.5" is not a whole number
```
Para mantener el programa en ejecución puedes capturar el error con `try` / `except`: el código del bloque `try` se ejecuta, y si lanza el error indicado, el bloque `except` se ejecuta en su lugar:
```python
try:
    number = int(text)
except ValueError:
    number = 0
```
Cuando la conversión tiene éxito, el bloque `except` se omite.

# --instructions--

Escribe una función `to_int` que reciba una cadena y la devuelva convertida a un `int`, o `None` cuando la conversión lanza un `ValueError`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes capturar `ValueError` con `except`

```json
{
  "regex": "except\\s+ValueError",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`to_int("42")` debe devolver `42`

```python
    def test_1(self):
        self.assertEqual(to_int("42"), 42, "--err-t1--")
        self.assertEqual(to_int("-7"), -7, "--err-t1--")
```

`to_int("abc")` debe devolver `None`

```python
    def test_2(self):
        self.assertIsNone(to_int("abc"), "--err-t2--")
```

`to_int("3.5")` debe devolver `None`

```python
    def test_3(self):
        self.assertIsNone(to_int("3.5"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def to_int(text):
    try:
        return int(text)
    except ValueError:
        return None
```
