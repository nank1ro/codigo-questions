---
language: python
exerciseType: 1
---

# --instructions--

Usa llaves de apertura `{` y cierre `}` para insertar el número `500` en el espacio vacío de la cadena formateada

# --seed--

```python
print(f"There are  items")
```

# --solutions--

```python
print(f"There are {500} items")
```

# --output--

There are 500 items
