---
language: python
exerciseType: 1
---

# --description--

El operador `in` comprueba si una cadena **contiene** otra.
Devuelve `True` o `False`, así que encaja de forma natural dentro de un `if`:
```python
email = "ada@mail.com"
print("@" in email)      # True
print("xyz" in email)    # False
if " " in email:
    print("No spaces allowed")
```
`not in` hace la comprobación contraria.

# --instructions--

Escribe una función `has_space(text)` que devuelva `True` si `text` contiene al menos un espacio, `False` en caso contrario, usando el operador `in`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar el operador `in`

```json
{
  "regex": "\" \" in |' ' in ",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_space("hello world")` debe devolver `True`

```python
    def test_1(self):
        self.assertEqual(has_space("hello world"), True, "--err-t1--")
```

`has_space("hello")` debe devolver `False`

```python
    def test_2(self):
        self.assertEqual(has_space("hello"), False, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def has_space(text):
    return " " in text
```
