---
language: python
exerciseType: 1
---

# --description--

Un **slice** (corte) extrae una parte de una cadena.
Escribe `[start:end]` entre corchetes: el carácter en `start` se incluye, el que está en `end` se **excluye**:
```python
word = "python"
print(word[0:3])  # pyt
print(word[2:4])  # th
```
Puedes omitir `start` para cortar desde el principio, o `end` para cortar hasta el final:
```python
print(word[:2])  # py
print(word[2:])  # thon
```
Un slice nunca provoca un error: un `end` mayor que la longitud simplemente se detiene en el último carácter.

# --instructions--

Guarda en la variable `first` los primeros 4 caracteres de `word` y en `rest` todos los restantes, usando slices

# --seed--

```python
word = "notebook"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar un slice `[:]`

```json
{
  "regex": "\\[[^\\]]*:[^\\]]*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`first` debe ser igual a `"note"`

```python
    def test_1(self):
        self.assertEqual(first, "note", "--err-t1--")
```

`rest` debe ser igual a `"book"`

```python
    def test_2(self):
        self.assertEqual(rest, "book", "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
word = "notebook"
first = word[:4]
rest = word[4:]
```
