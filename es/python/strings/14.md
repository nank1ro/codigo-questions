---
language: python
exerciseType: 1
---

# --description--

`startswith(prefix)` y `endswith(suffix)` devuelven `True` o `False` según cómo empieza o termina la cadena:
```python
url = "https://codigo.dev"
print(url.startswith("https"))  # True
print(url.endswith(".com"))     # False
```
Son la forma habitual de comprobar extensiones de archivo, protocolos o prefijos.

# --instructions--

Escribe una función `is_python_file(name)` que devuelva `True` si `name` termina en `.py`, y una función `is_comment(line)` que devuelva `True` si `line` empieza con `#`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `endswith()`

```json
{
  "regex": "\\.endswith\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Debes usar `startswith()`

```json
{
  "regex": "\\.startswith\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`is_python_file("main.py")` debe devolver `True`

```python
    def test_1(self):
        self.assertEqual(is_python_file("main.py"), True, "--err-t1--")
```

`is_python_file("notes.txt")` debe devolver `False`

```python
    def test_2(self):
        self.assertEqual(is_python_file("notes.txt"), False, "--err-t2--")
```

`is_comment("# todo")` debe devolver `True`

```python
    def test_3(self):
        self.assertEqual(is_comment("# todo"), True, "--err-t3--")
```

`is_comment("x = 1  # todo")` debe devolver `False`

```python
    def test_4(self):
        self.assertEqual(is_comment("x = 1  # todo"), False, "--err-t4--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_python_file(name):
    return name.endswith(".py")

def is_comment(line):
    return line.startswith("#")
```
