---
language: python
exerciseType: 4
---

# --description--

`replace(old, new)` devuelve una copia de la cadena en la que **todas** las apariciones de `old` se reemplazan por `new`:
```python
text = "a-b-c"
print(text.replace("-", "+"))  # a+b+c
```
Como las cadenas son inmutables, recuerda guardar el resultado si quieres conservarlo.

# --instructions--

Ordena los elementos para reemplazar cada `cats` por `dogs` en `msg` e imprimir el resultado

# --answers--

```python
msg = "I like cats, cats are great"
```

```python
new_msg = msg.replace("cats", "dogs")
```

```python
print(new_msg)
```

# --solutions--

```python
msg = "I like cats, cats are great"
new_msg = msg.replace("cats", "dogs")
print(new_msg)
```

# --output--

I like dogs, dogs are great
