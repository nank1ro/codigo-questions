---
language: python
exerciseType: 1
---

# --description--

Un **slug** es una versión de un título apta para URLs: en minúsculas, sin espacios alrededor, y con las palabras separadas por guiones, como `hello-world`.
Construir uno es solo una cadena de los métodos que ya has aprendido: `strip()`, `lower()` y `replace()`.

# --instructions--

Escribe una función `slugify(title)` que devuelva `title` sin espacios alrededor, en minúsculas y con cada espacio reemplazado por `-`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `replace()`

```json
{
  "regex": "\\.replace\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`slugify("Hello World")` debe devolver `"hello-world"`

```python
    def test_1(self):
        self.assertEqual(slugify("Hello World"), "hello-world", "--err-t1--")
```

`slugify("  Learn Python Strings  ")` debe devolver `"learn-python-strings"`

```python
    def test_2(self):
        self.assertEqual(slugify("  Learn Python Strings  "), "learn-python-strings", "--err-t2--")
```

`slugify("codigo")` debe devolver `"codigo"`

```python
    def test_3(self):
        self.assertEqual(slugify("codigo"), "codigo", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def slugify(title):
    return title.strip().lower().replace(" ", "-")
```
