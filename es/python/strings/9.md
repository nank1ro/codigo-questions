---
language: python
exerciseType: 3
---

# --description--

Las cadenas son **inmutables**: una vez creadas, sus caracteres no se pueden cambiar.
Asignar a un índice provoca un `TypeError`:
```python
word = "dog"
word[1] = "i"  # TypeError: 'str' object does not support item assignment
```
Para "cambiar" una cadena, construyes una nueva, por ejemplo con slices y concatenación, y la guardas en la variable:
```python
word = word[0] + "i" + word[2:]
print(word)  # dig
```

# --instructions--

¿Qué ocurre cuando se ejecuta este código?
```python
word = "cat"
word[0] = "b"
print(word)
```

# --answers--

- Un `TypeError`: las cadenas son inmutables, un carácter no se puede reemplazar en el sitio
- Imprime `bat`
- Imprime `b`
- Imprime `cat`, la asignación se ignora silenciosamente

# --solutions--

- Un `TypeError`: las cadenas son inmutables, un carácter no se puede reemplazar en el sitio
