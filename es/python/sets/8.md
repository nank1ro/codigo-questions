---
language: python
exerciseType: 2
---

# --description--

La **intersección** de dos sets contiene solo los elementos presentes en **ambos**.
Usa el operador `&` o el método `intersection()`:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a & b)               # {2, 3}
print(a.intersection(b))   # {2, 3}
```
Si los sets no tienen nada en común, el resultado es un set vacío.

# --instructions--

Completa el código para guardar en `common` los elementos presentes tanto en `a` como en `b`, de modo que el programa imprima `[3, 4]`

# --seed--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a [/] b
print(sorted(common))
```

# --answers--

- &
- |
- -
- ^
- and

# --solutions--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a & b
print(sorted(common))
```

# --output--

[3, 4]
