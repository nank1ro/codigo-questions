---
language: python
exerciseType: 1
---

# --description--

Dos sets son **iguales** cuando contienen los mismos elementos, sin importar el orden en que fueron escritos:
```python
print({1, 2, 3} == {3, 1, 2})  # True
```
Comparar la longitud de una colección con la longitud de su set es una forma rápida de detectar duplicados: si el set es **más pequeño**, algún valor apareció más de una vez:
```python
emails = ["a@x.com", "b@x.com", "a@x.com"]
print(len(set(emails)) < len(emails))  # True
```
El método de lista `count(value)` indica cuántas veces aparece un valor, lo que ayuda a encontrar *cuáles* valores están duplicados.

# --instructions--

Guarda en `has_duplicates` si `emails` contiene alguna dirección repetida, comparando la longitud de la lista con la longitud de su set, luego guarda en `duplicates` el set de las direcciones que aparecen más de una vez, usando una set comprehension y `count()`

# --seed--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `set()`

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Debes usar una set comprehension

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_duplicates` debe ser `True`

```python
    def test_1(self):
        self.assertEqual(has_duplicates, True, "--err-t1--")
```

`duplicates` debe ser igual a `{'ann@x.com', 'bob@x.com'}`

```python
    def test_2(self):
        self.assertEqual(duplicates, {"ann@x.com", "bob@x.com"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
has_duplicates = len(set(emails)) < len(emails)
duplicates = {e for e in emails if emails.count(e) > 1}
```
