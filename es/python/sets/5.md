---
language: python
exerciseType: 1
---

# --description--

`pop()` elimina **un elemento arbitrario** del set y lo devuelve.
Como un set no tiene orden, no puedes elegir qué elemento se elimina; llamar a `pop()` en un set vacío lanza un `KeyError`:
```python
tickets = {101, 102, 103}
picked = tickets.pop()
print(len(tickets))  # 2
```
`clear()` elimina **todos** los elementos, dejando un set vacío:
```python
tickets.clear()
print(len(tickets))  # 0
```

# --instructions--

Quita un elemento de `tickets` con `pop()` y guárdalo en la variable `picked`, luego vacía el set `drafts` con `clear()`

# --seed--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `pop()`

```json
{
  "regex": "\\.pop\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Debes usar `clear()`

```json
{
  "regex": "\\.clear\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`picked` debe ser uno de los tickets originales

```python
    def test_1(self):
        self.assertIn(picked, {101, 102, 103}, "--err-t1--")
```

`tickets` debe contener 2 elementos y ya no debe contener `picked`

```python
    def test_2(self):
        self.assertEqual(len(tickets), 2, "--err-t2--")
        self.assertNotIn(picked, tickets, "--err-t2--")
```

`drafts` debe estar vacío

```python
    def test_3(self):
        self.assertEqual(drafts, set(), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
picked = tickets.pop()
drafts.clear()
```
