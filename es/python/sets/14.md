---
language: python
exerciseType: 1
---

# --description--

Una **set comprehension** construye un set en una sola expresión, con la misma sintaxis que una list comprehension pero con llaves:
```python
nums = [1, 2, 2, 3]
squares = {n * n for n in nums}
print(squares)  # {1, 4, 9}
```
Un `if` opcional filtra los elementos, y los duplicados producidos por la expresión se descartan automáticamente:
```python
evens = {n for n in range(10) if n % 2 == 0}
print(evens)  # {0, 2, 4, 6, 8}
```

# --instructions--

Guarda en `lowered` el set de las palabras de `words` convertidas a minúsculas, usando una set comprehension, y luego en `long_words` el set de los elementos de `lowered` con más de 5 caracteres, usando una set comprehension con `if`

# --seed--

```python
words = ["Apple", "banana", "APPLE", "Cherry", "banana"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar una set comprehension

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`lowered` debe ser igual a `{'apple', 'banana', 'cherry'}`

```python
    def test_1(self):
        self.assertEqual(lowered, {"apple", "banana", "cherry"}, "--err-t1--")
```

`long_words` debe ser igual a `{'banana', 'cherry'}`

```python
    def test_2(self):
        self.assertEqual(long_words, {"banana", "cherry"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
words = ["Apple", "banana", "APPLE", "Cherry", "banana"]
lowered = {w.lower() for w in words}
long_words = {w for w in lowered if len(w) > 5}
```
