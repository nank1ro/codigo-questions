---
language: python
exerciseType: 1
---

# --description--

La función incorporada `set()` construye un set a partir de cualquier colección, por ejemplo una lista o una cadena.
Como un set conserva cada valor una sola vez, esta es la forma clásica de **eliminar duplicados**:
```python
nums = [1, 2, 2, 3]
unique = set(nums)
print(len(unique))  # 3
```
Para comprobar si un valor está presente usa el operador `in`, que devuelve `True` o `False`:
```python
print(2 in unique)  # True
print(9 in unique)  # False
```
Las comprobaciones de pertenencia en un set son muy rápidas, incluso con miles de elementos.

# --instructions--

Guarda en la variable `unique` un set construido a partir de `nums` usando `set()`, y en `has_three` si el valor `3` está en `unique`, usando `in`

# --seed--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `set()`

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Debes usar el operador `in`

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`unique` debe ser igual a `{1, 2, 3, 4}`

```python
    def test_1(self):
        self.assertEqual(unique, {1, 2, 3, 4}, "--err-t1--")
```

`has_three` debe ser `True`

```python
    def test_2(self):
        self.assertEqual(has_three, True, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
unique = set(nums)
has_three = 3 in unique
```
