---
language: python
exerciseType: 1
---

# --description--

La **diferencia** `a - b` contiene los elementos de `a` que **no** están en `b`.
El orden importa: `a - b` y `b - a` suelen ser diferentes:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a - b)  # {1}
print(b - a)  # {4}
```
La **diferencia simétrica** `a ^ b` contiene los elementos que están en **exactamente uno** de los dos sets:
```python
print(a ^ b)  # {1, 4}
```
Las formas de método son `difference()` y `symmetric_difference()`.

# --instructions--

Guarda en `only_ann` los hobbies de `ann` que `bob` no tiene, usando `-`, y en `not_shared` los hobbies que solo uno de ellos tiene, usando `^`

# --seed--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar el operador `-`

```json
{
  "regex": "\\s-\\s",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Debes usar el operador `^`

```json
{
  "regex": "\\^",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`only_ann` debe ser igual a `{'tennis', 'yoga'}`

```python
    def test_1(self):
        self.assertEqual(only_ann, {"tennis", "yoga"}, "--err-t1--")
```

`not_shared` debe ser igual a `{'tennis', 'yoga', 'swimming'}`

```python
    def test_2(self):
        self.assertEqual(not_shared, {"tennis", "yoga", "swimming"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
only_ann = ann - bob
not_shared = ann ^ bob
```
