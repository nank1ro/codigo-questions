---
language: python
exerciseType: 1
---

# --description--

La **unión** de dos sets es un nuevo set con los elementos de **ambos**, sin duplicados.
Usa el operador `|` o el método `union()`:
```python
a = {1, 2}
b = {2, 3}
print(a | b)        # {1, 2, 3}
print(a.union(b))   # {1, 2, 3}
```
Ni `a` ni `b` se modifican: las operaciones de sets siempre devuelven un set nuevo.

# --instructions--

Guarda en `skills` la unión de `frontend` y `backend` usando el operador `|`, y guarda en `total` cuántos skills distintos contiene

# --seed--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar el operador `|`

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`skills` debe ser igual a `{'html', 'css', 'js', 'python', 'sql'}`

```python
    def test_1(self):
        self.assertEqual(skills, {"html", "css", "js", "python", "sql"}, "--err-t1--")
```

`total` debe ser igual a `5`

```python
    def test_2(self):
        self.assertEqual(total, 5, "--err-t2--")
```

`frontend` no debe modificarse

```python
    def test_3(self):
        self.assertEqual(frontend, {"html", "css", "js"}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
skills = frontend | backend
total = len(skills)
```
