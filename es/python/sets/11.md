---
language: python
exerciseType: 4
---

# --description--

Los sets también se pueden **comparar** entre sí.
`a.issubset(b)`, o `a <= b`, es `True` cuando cada elemento de `a` también está en `b`.
`a.issuperset(b)`, o `a >= b`, es `True` cuando `a` contiene cada elemento de `b`.
`a.isdisjoint(b)` es `True` cuando los dos sets no tienen **ningún** elemento en común:
```python
small = {1, 2}
big = {1, 2, 3}
print(small <= big)              # True
print(big.issuperset(small))     # True
print(small.isdisjoint({8, 9}))  # True
```

# --instructions--

Ordena los elementos para construir `palette` a partir de `primary`, luego imprime, en este orden: si `primary` es un subconjunto de `palette`, si `palette` es un subconjunto de `primary`, y si `primary` y `{"black"}` son disjuntos

# --answers--

```python
primary = {"red", "blue", "yellow"}
```

```python
palette = primary | {"green"}
```

```python
print(primary <= palette)
```

```python
print(palette.issubset(primary))
```

```python
print(primary.isdisjoint({"black"}))
```

# --solutions--

```python
primary = {"red", "blue", "yellow"}
palette = primary | {"green"}
print(primary <= palette)
print(palette.issubset(primary))
print(primary.isdisjoint({"black"}))
```

# --output--

True
False
True
