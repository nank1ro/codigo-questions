---
language: python
exerciseType: 1
---

# --description--

Un set solo puede contener elementos **hasheables**, es decir, valores que no pueden cambiar: números, cadenas, `True`/`False` y **tuplas**.
Intentar agregar una lista, un diccionario u otro set lanza un `TypeError`:
```python
points = set()
points.add((1, 2))  # ok, una tupla
points.add([1, 2])  # TypeError: unhashable type: 'list'
```
Los sets de tuplas son útiles para llevar el registro de pares únicos, como coordenadas o registros (nombre, edad):
```python
visits = [(1, 2), (1, 2), (3, 4)]
print(len(set(visits)))  # 2
```

# --instructions--

Guarda en `points` el set de las coordenadas distintas de `visits`, agrégale el punto `(0, 0)`, y luego guarda en `count` cuántos puntos distintos contiene

# --seed--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `add()`

```json
{
  "regex": "\\.add\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`points` debe ser igual a `{(0, 0), (1, 2), (3, 4), (5, 6)}`

```python
    def test_1(self):
        self.assertEqual(points, {(0, 0), (1, 2), (3, 4), (5, 6)}, "--err-t1--")
```

`count` debe ser igual a `4`

```python
    def test_2(self):
        self.assertEqual(count, 4, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
points = set(visits)
points.add((0, 0))
count = len(points)
```
