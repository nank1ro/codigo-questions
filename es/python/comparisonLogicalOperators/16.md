---
language: python
exerciseType: 1
---

# --description--

`any(iterable)` devuelve `True` si al menos un elemento es verdadero y `all(iterable)` si lo son todos (`all([])` es `True`). Combinados con una expresión generadora expresan una condición sobre toda una colección, y hacen cortocircuito como `or` y `and`:
```python
print(any(ch.isdigit() for ch in "abc1"))  # True
print(all(ch.isalpha() for ch in "ab1"))   # False
```

# --instructions--

Escribe una función `is_valid_password(password)` que devuelva `True` solo cuando la cadena tenga entre `8` y `20` caracteres inclusive, contenga al menos un dígito y no contenga espacios, y `False` en caso contrario

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`"secret123"` es válida, y también lo son las contraseñas de exactamente `8` o `20` caracteres

```python
    def test_1(self):
        self.assertIs(is_valid_password("secret123"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefg1"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefghij123456789x"), True, "--err-t1--")
```

Las contraseñas demasiado cortas o demasiado largas no son válidas

```python
    def test_2(self):
        self.assertIs(is_valid_password("short1"), False, "--err-t2--")
        self.assertIs(is_valid_password("abcdefghij1234567890x"), False, "--err-t2--")
```

Una contraseña sin dígitos o con un espacio no es válida

```python
    def test_3(self):
        self.assertIs(is_valid_password("nodigitshere"), False, "--err-t3--")
        self.assertIs(is_valid_password("pass word1"), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_valid_password(password):
    return (
        8 <= len(password) <= 20
        and any(ch.isdigit() for ch in password)
        and " " not in password
    )
```
