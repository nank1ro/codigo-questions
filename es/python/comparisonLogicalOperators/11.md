---
language: python
exerciseType: 1
---

# --description--

La evaluación en cortocircuito es una forma segura de **proteger** una operación que fallaría con algunos valores. En `word is not None and len(word) < 4`, `len(word)` se ejecuta solo cuando `word` no es `None`, así que la llamada nunca lanza un error. La protección debe ir primero:
```python
def is_short(word):
    return word is not None and len(word) < 4

print(is_short(None))   # False
print(is_short("cat"))  # True
```
Ten en cuenta que `and` devuelve un operando: `word and len(word) < 4` da `None` para `None` y `""` para la cadena vacía, no `False`. Protege con una comparación real cuando se necesite un booleano.

# --instructions--

Escribe una función `first_is_zero(numbers)` que devuelva `True` cuando la lista `numbers` no esté vacía y su primer elemento sea `0`, y `False` en caso contrario. El resultado debe ser un booleano incluso para una lista vacía

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_is_zero([0, 5])` debe devolver `True`

```python
    def test_1(self):
        self.assertIs(first_is_zero([0, 5]), True, "--err-t1--")
        self.assertIs(first_is_zero([0]), True, "--err-t1--")
```

`first_is_zero([3, 0])` debe devolver `False`

```python
    def test_2(self):
        self.assertIs(first_is_zero([3, 0]), False, "--err-t2--")
```

`first_is_zero([])` debe devolver `False` sin lanzar un error

```python
    def test_3(self):
        self.assertIs(first_is_zero([]), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def first_is_zero(numbers):
    return len(numbers) > 0 and numbers[0] == 0
```
