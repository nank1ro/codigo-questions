---
language: python
exerciseType: 1
---

# --description--

Los operadores lógicos combinan booleanos. `and` es `True` solo cuando ambos lados son `True`, `or` cuando al menos uno lo es, y `not` invierte un único valor:
```python
age = 20
member = False
print(age >= 18 and member)  # False
print(age >= 18 or member)   # True
print(not member)            # True
```
Las comparaciones tienen mayor precedencia que los operadores lógicos, así que `age >= 18 and member` no necesita paréntesis. Los paréntesis son necesarios para agrupar un `or` dentro de un `and`: `a and (b or c)`.

# --instructions--

Escribe una función `is_leap(year)` que devuelva `True` cuando `year` sea divisible por `4` pero no por `100`, salvo que también sea divisible por `400`, y `False` en caso contrario

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`2024` y `2000` son años bisiestos

```python
    def test_1(self):
        self.assertIs(is_leap(2024), True, "--err-t1--")
        self.assertIs(is_leap(2000), True, "--err-t1--")
```

`2023` no es un año bisiesto

```python
    def test_2(self):
        self.assertIs(is_leap(2023), False, "--err-t2--")
```

`1900` es divisible por `100` pero no por `400`, así que no es un año bisiesto

```python
    def test_3(self):
        self.assertIs(is_leap(1900), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_leap(year):
    return year % 4 == 0 and (year % 100 != 0 or year % 400 == 0)
```
