---
language: python
exerciseType: 2
---

# --description--

Las comparaciones se pueden **encadenar**: `1 < x < 10` comprueba que `x` sea mayor que `1` **y** menor que `10`, exactamente como `1 < x and x < 10`, pero `x` se evalúa una sola vez:
```python
x = 5
print(1 < x < 10)   # True
print(1 < x <= 5)   # True
print(10 < x < 20)  # False
```
Cualquier operador de comparación se puede encadenar y cada uno se aplica a sus dos vecinos: `a < b == c` significa `a < b and b == c`. Leer una cadena como un rango, `low < x < high`, es el uso más común.

# --instructions--

Completa la comparación encadenada para que compruebe que `x` está estrictamente entre `1` y `10`, produciendo la salida `True`, `False`

# --seed--

```python
x = 5
print(1 [/] x [/] 10)
print(10 < x < 20)
```

# --answers--

- <
- <
- >
- >=
- ==

# --solutions--

```python
x = 5
print(1 < x < 10)
print(10 < x < 20)
```

# --output--

True
False
