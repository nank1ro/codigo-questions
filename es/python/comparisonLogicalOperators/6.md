---
language: python
exerciseType: 2
---

# --description--

Todo valor tiene un **valor de verdad**. `bool(value)` devuelve `False` para `0`, `0.0`, `None`, la cadena vacía `""` y los contenedores vacíos como `[]`, `{}` y `set()`; cualquier otro valor es verdadero, incluidos `"0"` y `[0]`:
```python
print(bool(0), bool(""), bool(None))   # False False False
print(bool(42), bool("0"), bool([0]))  # True True True
```
`if`, `while`, `and`, `or` y `not` usan esta regla, así que `if items:` comprueba que la lista no esté vacía y `not name` comprueba que la cadena esté vacía; no hace falta escribir `len(items) > 0` ni `name == ""`.

# --instructions--

Completa el código para que la primera línea muestre el valor de verdad de `cart`, `name` y `count`, y el `if` muestre `empty cart` cuando la lista esté vacía, produciendo la salida `False True False`, `empty cart`

# --seed--

```python
cart = []
name = "Ada"
count = 0
print([/](cart), bool(name), bool(count))
if [/] cart:
    print("empty cart")
```

# --answers--

- bool
- not
- len
- str
- is

# --solutions--

```python
cart = []
name = "Ada"
count = 0
print(bool(cart), bool(name), bool(count))
if not cart:
    print("empty cart")
```

# --output--

False True False
empty cart
