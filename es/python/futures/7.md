---
language: python
exerciseType: 1
---

# --description--

La concurrencia rinde cuando la cantidad de operaciones no es fija. Construye los objetos corrutina en una list comprehension, desempaquétalos en `gather` y espera con `await` todo el lote de una vez:
```python
import asyncio

async def fetch(url):
    await asyncio.sleep(0.02)
    return len(url)

async def fetch_all(urls):
    return await asyncio.gather(*[fetch(u) for u in urls])

print(asyncio.run(fetch_all(["a.com", "bb.com"])))  # [5, 6]
```
Diez urls siguen tomando unos 20 milisegundos en total en lugar de 200, y los resultados se mantienen en el orden de las urls.

# --instructions--

Escribe una función corrutina `fetch(item_id)` que espere `0.02` segundos y retorne `item_id * 10`, y una función corrutina `fetch_all(ids)` que ejecute `fetch` para cada id de forma **concurrente** con `asyncio.gather` y retorne la lista de resultados

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch(3)` debe retornar `30`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch(3)), 30, "--err-t1--")
```

`fetch_all([1, 2, 3])` debe retornar `[10, 20, 30]` y `fetch_all([])` debe retornar `[]`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(fetch_all([1, 2, 3])), [10, 20, 30], "--err-t2--")
        self.assertEqual(asyncio.run(fetch_all([])), [], "--err-t2--")
```

`fetch_all` con cinco ids debe ejecutarlos de forma concurrente, tomando bastante menos de `0.1` segundos

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(fetch_all([1, 2, 3, 4, 5]))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [10, 20, 30, 40, 50], "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch(item_id):
    await asyncio.sleep(0.02)
    return item_id * 10

async def fetch_all(ids):
    return await asyncio.gather(*[fetch(i) for i in ids])
```
