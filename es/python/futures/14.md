---
language: python
exerciseType: 2
---

# --description--

`asyncio.wait_for(awaitable, timeout)` espera algo con `await` pero se rinde después de `timeout` segundos: la operación se cancela y se lanza un `TimeoutError`, que puede capturarse como cualquier excepción:
```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())  # timed out
```
Con un timeout de `0.1` el mismo código imprimiría `done`. `asyncio.TimeoutError` es otro nombre para el `TimeoutError` integrado.

# --instructions--

Completa el código para que `slow()` se espere con `await` con un timeout de `0.01` segundos y el timeout se capture, produciendo la salida `timed out`

# --seed--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.[/](slow(), [/]=0.01)
        print(result)
    except [/]:
        print("timed out")

asyncio.run(main())
```

# --answers--

- wait_for
- timeout
- TimeoutError
- gather
- delay
- ValueError

# --solutions--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())
```

# --output--

timed out
