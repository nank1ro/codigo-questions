---
language: python
exerciseType: 1
---

# --description--

Una excepción lanzada dentro de una corrutina no aparece donde se creó el objeto corrutina: se lanza en el `await` que la ejecuta, o por `asyncio.run` en el caso de la más externa. `try`/`except` debe por lo tanto envolver el **`await`**:
```python
import asyncio

async def load(path):
    await asyncio.sleep(0.01)
    if path == "":
        raise FileNotFoundError("empty path")
    return "contents"

async def safe_load(path):
    try:
        return await load(path)
    except FileNotFoundError:
        return ""

print(asyncio.run(safe_load("")))  # prints an empty line
```
Una excepción que nadie captura se propaga a través de cada `await` hasta `asyncio.run`, que vuelve a lanzarla en el código síncrono, exactamente como una pila de llamadas normal.

# --instructions--

Escribe una función corrutina `divide(a, b)` que espere `0.01` segundos y retorne `a / b`, y una función corrutina `safe_divide(a, b)` que espere con `await` a `divide(a, b)` y retorne su resultado, o `None` si se lanza un `ZeroDivisionError`

# --before-asserts--

```python
import asyncio
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`divide(10, 4)` debe retornar `2.5`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(divide(10, 4)), 2.5, "--err-t1--")
```

`divide(1, 0)` debe lanzar un `ZeroDivisionError` al ejecutarse

```python
    def test_2(self):
        with self.assertRaises(ZeroDivisionError, msg="--err-t2--"):
            asyncio.run(divide(1, 0))
```

`safe_divide(10, 2)` debe retornar `5.0` y `safe_divide(1, 0)` debe retornar `None`

```python
    def test_3(self):
        self.assertEqual(asyncio.run(safe_divide(10, 2)), 5.0, "--err-t3--")
        self.assertIsNone(asyncio.run(safe_divide(1, 0)), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def divide(a, b):
    await asyncio.sleep(0.01)
    return a / b

async def safe_divide(a, b):
    try:
        return await divide(a, b)
    except ZeroDivisionError:
        return None
```
