---
language: python
exerciseType: 1
---

# --description--

Las piezas se combinan de forma natural. Para ejecutar muchas operaciones de manera concurrente, cada una con su propio límite de tiempo, envuelve cada una en una pequeña corrutina que aplique `wait_for` y capture el `TimeoutError`, y luego haz `gather` de los envoltorios:
```python
async def guarded(coro, limit):
    try:
        return await asyncio.wait_for(coro, timeout=limit)
    except TimeoutError:
        return None
```
`gather(*[guarded(job(x), limit) for x in items])` retorna entonces un valor para cada trabajo que terminó a tiempo y `None` para cada uno que no, en el orden original, y todo el lote toma como máximo unos `limit` segundos.

# --instructions--

Escribe una función corrutina `job(delay)` que espere `delay` segundos y retorne `delay`, y una función corrutina `run_jobs(delays, limit)` que ejecute un `job` para cada delay de forma **concurrente**, limitando cada uno a `limit` segundos con `asyncio.wait_for`, y retorne una lista con el delay de cada trabajo que terminó a tiempo y `None` para cada trabajo que agotó el timeout, en el orden original

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`job(0.01)` debe retornar `0.01`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(job(0.01)), 0.01, "--err-t1--")
```

`run_jobs([0.01, 0.06, 0.02], 0.04)` debe retornar `[0.01, None, 0.02]` y `run_jobs([], 0.01)` debe retornar `[]`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(run_jobs([0.01, 0.06, 0.02], 0.04)), [0.01, None, 0.02], "--err-t2--")
        self.assertEqual(asyncio.run(run_jobs([], 0.01)), [], "--err-t2--")
```

Cinco trabajos de `0.02` segundos con un límite de `0.05` deben terminar todos y ejecutarse de forma concurrente, tomando bastante menos de `0.1` segundos

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(run_jobs([0.02] * 5, 0.05))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [0.02] * 5, "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def job(delay):
    await asyncio.sleep(delay)
    return delay

async def guarded(delay, limit):
    try:
        return await asyncio.wait_for(job(delay), timeout=limit)
    except TimeoutError:
        return None

async def run_jobs(delays, limit):
    return await asyncio.gather(*[guarded(d, limit) for d in delays])
```
