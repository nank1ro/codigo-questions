---
language: python
exerciseType: 4
---

# --description--

Manejar una corrutina que falla sigue el patrón síncrono: la corrutina lanza una excepción, quien llama envuelve el `await` en `try`/`except` y decide qué hacer con el objeto de error, por ejemplo imprimir su mensaje con `print(e)`.

# --instructions--

Ordena los elementos para construir un programa que empiece con el import, defina `load` antes de `main`, imprima el resultado de cargar `"config"` y luego capture el error lanzado al cargar un nombre vacío e imprima su mensaje, produciendo la salida `loaded config`, `empty name`

# --answers--

```python
import asyncio
```

```python
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
```

```python
async def main():
    print(await load("config"))
```

```python
    try:
        await load("")
```

```python
    except ValueError as e:
        print(e)
```

```python
asyncio.run(main())
```

# --solutions--

```python
import asyncio
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
async def main():
    print(await load("config"))
    try:
        await load("")
    except ValueError as e:
        print(e)
asyncio.run(main())
```

# --output--

loaded config
empty name
