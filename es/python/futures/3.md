---
language: python
exerciseType: 1
---

# --description--

`await` pausa la corrutina actual hasta que la operación esperada se completa y entonces entrega su resultado. Mientras la corrutina está pausada, el bucle de eventos queda libre para ejecutar otras corrutinas. `await` solo está permitido dentro de un `async def`.

`asyncio.sleep(seconds)` es el awaitable más simple: espera el tiempo indicado sin bloquear el bucle. A diferencia de `time.sleep`, debe esperarse con `await`:
```python
import asyncio

async def countdown():
    print("ready")
    await asyncio.sleep(0.05)
    print("go")

asyncio.run(countdown())
```
El programa imprime `ready`, espera 50 milisegundos e imprime `go`. Escribir `asyncio.sleep(0.05)` sin `await` crea el objeto corrutina pero nunca lo ejecuta, así que no ocurre ninguna espera.

# --instructions--

Escribe una función corrutina `wait_and_return(value, delay)` que espere `delay` segundos con `asyncio.sleep` y luego retorne `value`

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`wait_and_return("ok", 0.01)` debe retornar `"ok"`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(wait_and_return("ok", 0.01)), "ok", "--err-t1--")
```

`wait_and_return(7, 0.02)` debe retornar `7` y tomar al menos `0.02` segundos

```python
    def test_2(self):
        start = time.perf_counter()
        result = asyncio.run(wait_and_return(7, 0.02))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, 7, "--err-t2--")
        self.assertGreaterEqual(elapsed, 0.018, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def wait_and_return(value, delay):
    await asyncio.sleep(delay)
    return value
```
