---
language: python
exerciseType: 1
---

# --description--

Las corrutinas se llaman entre sí con `await`. Una corrutina puede esperar con `await` a cualquier otra corrutina, recibir su valor de retorno y continuar, exactamente como una cadena de llamadas a funciones:
```python
import asyncio

async def fetch_price(item):
    await asyncio.sleep(0.01)  # simulates a slow lookup
    return 10

async def total(item, quantity):
    price = await fetch_price(item)
    return price * quantity

print(asyncio.run(total("pen", 3)))  # 30
```
Solo la corrutina más externa pasa por `asyncio.run`; cada una de las internas se alcanza con `await`. Si `total` fuera un `def` normal, no podría usar `await` en absoluto: la palabra clave `async` se propaga a cada función de la cadena que necesita esperar.

# --instructions--

Escribe una función corrutina `fetch_user(user_id)` que espere `0.01` segundos y retorne la cadena `"user"` seguida del id, y una función corrutina `greet_user(user_id)` que espere con `await` a `fetch_user` y retorne `"Hello, "` seguido del nombre obtenido

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch_user(1)` debe retornar `"user1"` y `fetch_user(42)` debe retornar `"user42"`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch_user(1)), "user1", "--err-t1--")
        self.assertEqual(asyncio.run(fetch_user(42)), "user42", "--err-t1--")
```

`greet_user(1)` debe retornar `"Hello, user1"`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(greet_user(1)), "Hello, user1", "--err-t2--")
```

Tanto `fetch_user` como `greet_user` deben ser funciones corrutina

```python
    def test_3(self):
        self.assertTrue(inspect.iscoroutinefunction(fetch_user), "--err-t3--")
        self.assertTrue(inspect.iscoroutinefunction(greet_user), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch_user(user_id):
    await asyncio.sleep(0.01)
    return f"user{user_id}"

async def greet_user(user_id):
    name = await fetch_user(user_id)
    return f"Hello, {name}"
```
