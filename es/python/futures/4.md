---
language: python
exerciseType: 3
---

# --description--

Llamar a una función corrutina no es suficiente para ejecutarla. La llamada solo construye un objeto corrutina; el cuerpo se ejecuta cuando ese objeto se espera con `await` o se pasa a `asyncio.run`:
```python
async def hello():
    print("hi")

hello()  # nothing is printed
```
Python incluso lo advierte: `RuntimeWarning: coroutine 'hello' was never awaited`. Un `await` olvidado es el error asíncrono más común: el código parece llamado pero nunca se ejecuta, y cualquier variable que debería contener su resultado contiene en su lugar un objeto corrutina.

# --instructions--

¿Qué imprime este código?
```python
import asyncio

async def hello():
    print("hi")
    return 1

async def main():
    result = hello()
    print("main")

asyncio.run(main())
```

# --answers--

- Solo `main`, porque `hello()` crea un objeto corrutina que nunca se espera con `await`, así que su cuerpo no se ejecuta
- `hi` y luego `main`, porque llamar a una función corrutina la ejecuta inmediatamente
- `main` y luego `hi`, porque la corrutina se ejecuta cuando `main` retorna
- Un `TypeError`, porque una corrutina no puede asignarse a una variable

# --solutions--

- Solo `main`, porque `hello()` crea un objeto corrutina que nunca se espera con `await`, así que su cuerpo no se ejecuta
