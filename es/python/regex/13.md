---
language: python
exerciseType: 1
---

# --description--

`str.split` solo puede cortar por un separador fijo. `re.split(pattern, text)` corta por cualquier cosa que el patrón describa, que es lo que suele necesitar una entrada desordenada:
```python
import re

print("a, b;c".split(","))            # ['a', ' b;c']
print(re.split(r"[,;]", "a, b;c"))    # ['a', ' b', 'c']
print(re.split(r"[,;\s]+", "a, b;c")) # ['a', 'b', 'c']
```
Escribir el separador como `[,;\s]+` hace que toda una secuencia de comas, puntos y comas y espacios cuente como un solo corte, en lugar de dejar cadenas vacías entre ellas.

Un argumento `maxsplit` se detiene tras un número dado de cortes, dejando el resto del texto en el último elemento: `re.split(r"\s+", "a b c", maxsplit=1)` da `['a', 'b c']`.

# --instructions--

Escribe una función `split_fields(line)` que devuelva la lista de campos de la línea, tratando cualquier secuencia de comas, puntos y comas y espacios como un único separador

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`split_fields` debe cortar por comas, puntos y comas y espacios

```python
    def test_1(self):
        self.assertEqual(split_fields("a, b;c"), ["a", "b", "c"], "--err-t1--")
        self.assertEqual(split_fields("red green blue"), ["red", "green", "blue"], "--err-t1--")
```

Una secuencia de separadores debe producir un solo corte

```python
    def test_2(self):
        self.assertEqual(split_fields("x ; ; y"), ["x", "y"], "--err-t2--")
        self.assertEqual(split_fields("1,,2"), ["1", "2"], "--err-t2--")
```

Una línea sin separadores debe devolver un solo campo

```python
    def test_3(self):
        self.assertEqual(split_fields("solo"), ["solo"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def split_fields(line):
    return re.split(r"[,;\s]+", line)
```
