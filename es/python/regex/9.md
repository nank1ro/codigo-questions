---
language: python
exerciseType: 1
---

# --description--

Cuando se necesitan la posición o los grupos de cada coincidencia, `re.finditer(pattern, text)` es la llamada adecuada: recorre el texto y produce un **objeto de coincidencia** por cada coincidencia, de una en una:
```python
import re

for match in re.finditer(r"\d+", "a1 bb22"):
    print(match.group(), match.start())
# 1 1
# 22 5
```
`finditer` produce un iterador, no una lista, así que puede usarse en un bucle `for` o en una comprensión. Donde `findall` da solo el texto, `finditer` da todo lo que un objeto de coincidencia sabe.

# --instructions--

Escribe una función `word_positions(text)` que devuelva una lista de tuplas, una por cada secuencia de letras en el texto, con la palabra y el índice donde comienza

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`word_positions("hi there")` debe devolver `[("hi", 0), ("there", 3)]`

```python
    def test_1(self):
        self.assertEqual(word_positions("hi there"), [("hi", 0), ("there", 3)], "--err-t1--")
```

Los dígitos y los símbolos no deben formar parte de una palabra

```python
    def test_2(self):
        self.assertEqual(word_positions("a1b"), [("a", 0), ("b", 2)], "--err-t2--")
        self.assertEqual(word_positions("Ada, Bob"), [("Ada", 0), ("Bob", 5)], "--err-t2--")
```

Un texto sin letras debe devolver una lista vacía

```python
    def test_3(self):
        self.assertEqual(word_positions("123 456"), [], "--err-t3--")
        self.assertEqual(word_positions(""), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def word_positions(text):
    return [(match.group(), match.start()) for match in re.finditer(r"[a-zA-Z]+", text)]
```
