---
language: python
exerciseType: 1
---

# --description--

Un objeto de coincidencia lleva más que el texto coincidente. Además de `.group()` ofrece la posición de la coincidencia dentro de la cadena original:
```python
import re

match = re.search(r"\d+", "order 42 shipped")
print(match.group())  # 42
print(match.start())  # 6
print(match.end())    # 8
print(match.span())   # (6, 8)
```
`.start()` es el índice del primer carácter coincidente, `.end()` es el índice justo después del último, y `.span()` devuelve ambos como una tupla. Eso significa que `text[match.start():match.end()]` siempre es igual a `match.group()`.

Como `re.search` puede devolver `None`, leer `.group()` directamente provoca un `AttributeError` cuando nada coincidió; comprueba primero el resultado.

# --instructions--

Escribe una función `first_number(text)` que devuelva una tupla con la primera secuencia de dígitos, su índice inicial y su índice final, o `None` cuando el texto no contiene ningún dígito

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_number("order 42 shipped")` debe devolver `("42", 6, 8)`

```python
    def test_1(self):
        self.assertEqual(first_number("order 42 shipped"), ("42", 6, 8), "--err-t1--")
```

La coincidencia debe empezar en el índice `0` cuando el texto comienza con dígitos

```python
    def test_2(self):
        self.assertEqual(first_number("7up"), ("7", 0, 1), "--err-t2--")
        self.assertEqual(first_number("100 items"), ("100", 0, 3), "--err-t2--")
```

`first_number` debe devolver `None` cuando no hay ningún dígito

```python
    def test_3(self):
        self.assertIsNone(first_number("no digits here"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def first_number(text):
    match = re.search(r"\d+", text)
    if match is None:
        return None
    return (match.group(), match.start(), match.end())
```
