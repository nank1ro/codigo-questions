---
language: python
exerciseType: 1
---

# --description--

Contar paréntesis para encontrar el grupo `3` se vuelve tedioso rápidamente. A un grupo se le puede dar un nombre con `(?P<name>...)` y luego leerlo con `match.group("name")`:
```python
import re

match = re.search(r"(?P<hour>\d\d):(?P<minute>\d\d)", "starts at 09:30")
print(match.group("hour"))    # 09
print(match.group("minute"))  # 30
print(match.groupdict())      # {'hour': '09', 'minute': '30'}
```
`match.groupdict()` devuelve todos los grupos con nombre como un diccionario. Los grupos con nombre conservan también su número, así que `match.group(1)` sigue funcionando.

El ejemplo usa además un **cuantificador** con llaves: `\d{2}` significa exactamente dos dígitos, `\d{2,4}` significa entre dos y cuatro, y `\d{2,}` significa dos o más. Son la versión precisa de `+` (uno o más), `*` (cero o más) y `?` (cero o uno).

# --instructions--

Escribe una función `parse_date(text)` que encuentre una fecha escrita como cuatro dígitos, un guion, dos dígitos, un guion y dos dígitos, y devuelva un diccionario con las claves `year`, `month` y `day`, o `None` cuando no hay tal fecha

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`parse_date` debe leer las tres partes de la fecha

```python
    def test_1(self):
        self.assertEqual(parse_date("due 2026-09-13 ok"), {"year": "2026", "month": "09", "day": "13"}, "--err-t1--")
```

`parse_date` debe funcionar cuando la fecha es todo el texto

```python
    def test_2(self):
        self.assertEqual(parse_date("1999-12-31"), {"year": "1999", "month": "12", "day": "31"}, "--err-t2--")
```

`parse_date` debe devolver `None` cuando no hay ninguna fecha

```python
    def test_3(self):
        self.assertIsNone(parse_date("no date here"), "--err-t3--")
        self.assertIsNone(parse_date("26-09-13"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def parse_date(text):
    match = re.search(r"(?P<year>\d{4})-(?P<month>\d{2})-(?P<day>\d{2})", text)
    if match is None:
        return None
    return match.groupdict()
```
