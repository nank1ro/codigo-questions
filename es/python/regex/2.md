---
language: python
exerciseType: 1
---

# --description--

`re.search` escanea todo el texto, pero `re.match` solo prueba el patrón al **mismo principio**:
```python
import re

print(re.search(r"\d+", "order 42"))  # <re.Match object; span=(6, 8), match='42'>
print(re.match(r"\d+", "order 42"))   # None
print(re.match(r"\d+", "42 orders"))  # <re.Match object; span=(0, 2), match='42'>
```

Ambas devuelven `None` cuando nada coincide, y un objeto de coincidencia siempre es verdadero, así que la forma habitual de preguntar "¿coincidió?" es un simple `if`:
```python
if re.search(r"\d", text):
    print("there is a digit")
```
Cuando se necesita un `True` o un `False` real, compara con `is not None` o envuelve la llamada en `bool(...)`.

# --instructions--

Escribe una función `has_digit(text)` que devuelva `True` cuando el texto contenga un dígito en cualquier parte, y una función `starts_with_digit(text)` que devuelva `True` solo cuando el texto comience con un dígito

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`has_digit` debe encontrar un dígito en cualquier parte del texto

```python
    def test_1(self):
        self.assertEqual(has_digit("order 42"), True, "--err-t1--")
        self.assertEqual(has_digit("7up"), True, "--err-t1--")
```

`has_digit` debe devolver `False` cuando no hay ningún dígito

```python
    def test_2(self):
        self.assertEqual(has_digit("no numbers here"), False, "--err-t2--")
        self.assertEqual(has_digit(""), False, "--err-t2--")
```

`starts_with_digit` debe mirar solo el principio del texto

```python
    def test_3(self):
        self.assertEqual(starts_with_digit("42 orders"), True, "--err-t3--")
        self.assertEqual(starts_with_digit("order 42"), False, "--err-t3--")
        self.assertEqual(starts_with_digit(""), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def has_digit(text):
    return re.search(r"\d", text) is not None

def starts_with_digit(text):
    return re.match(r"\d", text) is not None
```
