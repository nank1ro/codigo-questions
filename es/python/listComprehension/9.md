---
language: python
exerciseType: 1
---

# --description--

Las dos condiciones pueden combinarse en la misma comprehension: un `if-else` para elegir el valor, y un `if` de filtro al final para omitir algunos elementos.
```python
nums = [1, 2, 3, 4]
result = ["big" if n > 2 else "small" for n in nums if n != 3]
print(result)  # ['small', 'small', 'big']
```

# --instructions--

Guarda en la variable `clipped` una copia de `nums` en la que cada número negativo se reemplace por `0`, usando una list comprehension con una expresión `if-else`

# --seed--

```python
nums = [4, -2, 7, -5]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar una list comprehension con una expresión `if-else`

```json
{
  "regex": "\\[.* if .* else .* for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`clipped` debe ser igual a `[4, 0, 7, 0]`

```python
    def test_1(self):
        self.assertEqual(clipped, [4, 0, 7, 0], "--err-t1--")
```

`nums` no debe modificarse

```python
    def test_2(self):
        self.assertEqual(nums, [4, -2, 7, -5], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [4, -2, 7, -5]
clipped = [n if n >= 0 else 0 for n in nums]
```
