---
language: python
exerciseType: 1
---

# --description--

**¿Cuándo deberías usar una comprehension?**
Es perfecta cuando el resultado es una lista (o un dict, o un set) y la lógica cabe en una línea legible: una transformación simple, un filtro opcional.
Si necesitas varias sentencias, más de dos `for` anidados, o la línea se vuelve difícil de leer, escribe en su lugar un bucle `for` simple: el código será más largo, pero más claro.
Una comprehension también reemplaza la mayoría de los usos de `map()` y `filter()` con lambdas:
```python
doubled = list(map(lambda n: n * 2, nums))
doubled = [n * 2 for n in nums]  # same result, easier to read
```

# --instructions--

Define una función `initials(names)` que retorne una lista con la primera letra de cada nombre en `names` en mayúscula, omitiendo las cadenas vacías. Usa una list comprehension

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar una list comprehension

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`initials(["ana", "bob"])` debe retornar `['A', 'B']`

```python
    def test_1(self):
        self.assertEqual(initials(["ana", "bob"]), ["A", "B"], "--err-t1--")
```

`initials(["", "cy", ""])` debe retornar `['C']`

```python
    def test_2(self):
        self.assertEqual(initials(["", "cy", ""]), ["C"], "--err-t2--")
```

`initials([])` debe retornar `[]`

```python
    def test_3(self):
        self.assertEqual(initials([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def initials(names):
    return [name[0].upper() for name in names if name != ""]
```
