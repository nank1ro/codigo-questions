---
language: python
exerciseType: 4
---

# --description--

El `for` interno puede usar la variable del externo.
Esta es la forma clásica de **aplanar** una lista de listas en una sola lista:
```python
matrix = [[1, 2], [3, 4]]
flat = [n for row in matrix for n in row]
print(flat)  # [1, 2, 3, 4]
```
La función `sum()` luego suma todos los números de una lista.

# --instructions--

Ordena los elementos para aplanar `matrix` e imprimir la lista aplanada junto con la suma de sus números

# --answers--

```python
matrix = [[1, 2], [3, 4]]
```

```python
flat = [n for row in matrix for n in row]
```

```python
total = sum(flat)
```

```python
print(flat, total)
```

# --solutions--

```python
matrix = [[1, 2], [3, 4]]
flat = [n for row in matrix for n in row]
total = sum(flat)
print(flat, total)
```

# --output--

[1, 2, 3, 4] 10
