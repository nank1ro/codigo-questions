---
language: python
exerciseType: 1
---

# --description--

Los miembros de un enum son **hasheables**, por lo que pueden usarse como claves de diccionario y como elementos de un set.
Un diccionario con claves de un enum es una forma limpia de adjuntar datos a cada miembro, y buscarlo con un miembro es más seguro que usar una cadena simple que podría escribirse mal:
```python
class Size(Enum):
    SMALL = 1
    LARGE = 2

prices = {Size.SMALL: 2.5, Size.LARGE: 4.5}
print(prices[Size.LARGE])  # 4.5
```
Como los miembros pueden aparecer en cualquier colección, todo lo que sabes sobre listas, sets y comprehensions también funciona con ellos:
```python
order = [Size.LARGE, Size.SMALL, Size.LARGE]
print(len(set(order)))  # 2
```

# --instructions--

Guarda en `large_price` el precio de `Size.LARGE` leído de `prices`, en `total` la suma de los precios de los tamaños en `order`, y en `sizes` el set de los tamaños distintos en `order`

# --seed--

```python
from enum import Enum

class Size(Enum):
    SMALL = 1
    MEDIUM = 2
    LARGE = 3

prices = {Size.SMALL: 2.5, Size.MEDIUM: 3.5, Size.LARGE: 4.5}
order = [Size.LARGE, Size.SMALL, Size.LARGE]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes leer `prices` con `Size.LARGE` como clave

```json
{
  "regex": "prices\\[\\s*Size\\.LARGE\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`large_price` debe ser igual a `4.5`

```python
    def test_1(self):
        self.assertEqual(large_price, 4.5, "--err-t1--")
```

`total` debe ser igual a `11.5`

```python
    def test_2(self):
        self.assertEqual(total, 11.5, "--err-t2--")
```

`sizes` debe ser igual a `{Size.SMALL, Size.LARGE}`

```python
    def test_3(self):
        self.assertEqual(sizes, {Size.SMALL, Size.LARGE}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Size(Enum):
    SMALL = 1
    MEDIUM = 2
    LARGE = 3

prices = {Size.SMALL: 2.5, Size.MEDIUM: 3.5, Size.LARGE: 4.5}
order = [Size.LARGE, Size.SMALL, Size.LARGE]
large_price = prices[Size.LARGE]
total = sum(prices[size] for size in order)
sizes = set(order)
```
