---
language: python
exerciseType: 4
---

# --description--

Los enums se combinan naturalmente con la declaración `match` (disponible desde Python 3.10), que compara un valor con una serie de patrones `case` y ejecuta el primero que coincide:
```python
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
```
Escribe siempre el miembro con su clase, como `Light.RED`: un nombre suelto como `case RED:` no compararía nada, solo capturaría el valor en una nueva variable `RED` y coincidiría con todo.
El comodín `case _:` es el predeterminado y debe ir al final, porque cualquier patrón después de él nunca podría alcanzarse.

# --instructions--

Ordena los elementos para declarar el enum `Light`, definir `action()` con el caso `RED` primero, luego el caso `GREEN`, luego el predeterminado, e imprimir la acción para `Light.YELLOW`

# --answers--

```python
from enum import Enum
```

```python
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
```

```python
def action(light):
```

```python
    match light:
```

```python
        case Light.RED:
            return "Stop"
```

```python
        case Light.GREEN:
            return "Go"
```

```python
        case _:
            return "Slow down"
```

```python
print(action(Light.YELLOW))
```

# --solutions--

```python
from enum import Enum
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
print(action(Light.YELLOW))
```

# --output--

Slow down
