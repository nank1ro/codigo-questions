---
language: python
exerciseType: 1
---

# --description--

Cada miembro de un enum tiene dos atributos: `name`, el identificador que escribiste en la clase, y `value`, el valor que le asignaste:
```python
from enum import Enum

class Color(Enum):
    RED = 1
    GREEN = 2

print(Color.GREEN.name)   # GREEN
print(Color.GREEN.value)  # 2
```
El valor puede ser de cualquier tipo, no solo un entero: cadenas, tuplas y floats son opciones habituales.
Un miembro es un objeto normal, por lo que puedes guardarlo en una variable y leer sus atributos más adelante:
```python
chosen = Color.RED
print(chosen.value)  # 1
```

# --instructions--

Guarda en `home` el miembro `Planet.EARTH`, en `label` su nombre y en `position` su valor

# --seed--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `.name`

```json
{
  "regex": "\\.name\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Debes usar `.value`

```json
{
  "regex": "\\.value\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`home` debe ser `Planet.EARTH`

```python
    def test_1(self):
        self.assertIs(home, Planet.EARTH, "--err-t1--")
```

`label` debe ser igual a `"EARTH"`

```python
    def test_2(self):
        self.assertEqual(label, "EARTH", "--err-t2--")
```

`position` debe ser igual a `3`

```python
    def test_3(self):
        self.assertEqual(position, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3

home = Planet.EARTH
label = home.name
position = home.value
```
