---
language: python
exerciseType: 2
---

# --description--

Si dos miembros comparten el mismo valor, el segundo no es un miembro nuevo sino un **alias** del primero:
```python
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # alias of ACTIVE

print(Status.ENABLED)  # Status.ACTIVE
print(len(Status))     # 1
```
Los alias se omiten al iterar y no se cuentan con `len()`.
Normalmente un valor duplicado es un error. El decorador `unique`, importado de `enum`, hace que Python lance un `ValueError` tan pronto como se declara un enum con alias:
```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # ValueError: duplicate values found
```

# --instructions--

Completa el código para importar el decorador `unique` y aplicarlo a `Status`, de modo que los valores duplicados se rechacen, y luego imprimir el número de miembros

# --seed--

```python
from enum import Enum, [/]

[/]
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --answers--

- unique
- @unique
- auto
- @auto
- @property
- @classmethod

# --solutions--

```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --output--

2
