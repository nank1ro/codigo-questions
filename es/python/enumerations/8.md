---
language: python
exerciseType: 3
---

# --description--

`StrEnum` (disponible desde Python 3.11) es la contraparte de cadenas de `IntEnum`: sus miembros también son cadenas, iguales a su valor.
Esto los hace convenientes dondequiera que se esperan cadenas simples, como claves de configuración o parámetros de una API:
```python
from enum import StrEnum

class Mode(StrEnum):
    LIGHT = "light"
    DARK = "dark"

print(Mode.DARK == "dark")  # True
print(Mode.DARK.upper())    # DARK
```
A diferencia de un `Enum` normal, convertir un miembro de `StrEnum` a texto con `str()` o dentro de una f-string da su **valor**, no `Mode.DARK`.

# --instructions--

¿Qué imprime este código?
```python
from enum import StrEnum

class Mode(StrEnum):
    LIGHT = "light"
    DARK = "dark"

print(f"theme: {Mode.DARK}")
```

# --answers--

- `theme: dark`, porque un miembro de `StrEnum` se formatea como su valor
- `theme: Mode.DARK`, porque una f-string siempre muestra los nombres de la clase y del miembro
- `theme: DARK`, porque una f-string muestra el nombre del miembro
- `theme: <Mode.DARK: 'dark'>`, porque una f-string usa el `repr()` del miembro

# --solutions--

- `theme: dark`, porque un miembro de `StrEnum` se formatea como su valor
