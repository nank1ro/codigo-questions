---
language: python
exerciseType: 2
---

# --description--

El valor de un miembro puede ser una **tupla**, lo que te permite adjuntar varios datos a cada miembro.
Si el enum define un método `__init__`, Python lo llama una vez por miembro, desempaquetando la tupla en sus parámetros, por lo que puedes guardar cada dato en su propio atributo:
```python
from enum import Enum

class Planet(Enum):
    EARTH = (5.97, 6371)
    MARS = (0.64, 3390)

    def __init__(self, mass, radius):
        self.mass = mass
        self.radius = radius

print(Planet.MARS.radius)  # 3390
print(Planet.MARS.value)   # (0.64, 3390)
```
El `value` del miembro sigue siendo la tupla completa.

# --instructions--

Completa el código para desempaquetar la tupla de cada miembro en `__init__`, guardando el segundo elemento en un atributo `metal`, y luego imprimir el metal de `Coin.PENNY`

# --seed--

```python
from enum import Enum

class Coin(Enum):
    PENNY = (1, "copper")
    DIME = (10, "silver")

    def [/](self, cents, metal):
        self.cents = cents
        self.[/] = metal

print(Coin.DIME.cents)
print(Coin.PENNY.[/])
```

# --answers--

- __init__
- metal
- metal
- __new__
- value
- name
- cents

# --solutions--

```python
from enum import Enum

class Coin(Enum):
    PENNY = (1, "copper")
    DIME = (10, "silver")

    def __init__(self, cents, metal):
        self.cents = cents
        self.metal = metal

print(Coin.DIME.cents)
print(Coin.PENNY.metal)
```

# --output--

10
copper
