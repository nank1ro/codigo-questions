---
language: python
exerciseType: 4
---

# --description--

Una clase enum es **iterable**: un bucle `for` sobre la clase visita cada miembro, en el orden en que fueron declarados:
```python
for color in Color:
    print(color.name, color.value)
```
`len()` devuelve cuántos miembros tiene el enum, y `list(Color)` construye una lista con ellos:
```python
print(len(Color))    # 3
print(list(Color))   # [<Color.RED: 1>, <Color.GREEN: 2>, <Color.BLUE: 3>]
```
Dentro de una lista, los miembros se muestran con su `repr()`, que incluye el valor entre corchetes angulares.

# --instructions--

Ordena los elementos para declarar el enum `Weekday`, imprimir cuántos miembros tiene y luego imprimir el nombre y el valor de cada miembro, uno por línea

# --answers--

```python
from enum import Enum
```

```python
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
```

```python
print(len(Weekday))
```

```python
for day in Weekday:
```

```python
    print(day.name, day.value)
```

# --solutions--

```python
from enum import Enum
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
print(len(Weekday))
for day in Weekday:
    print(day.name, day.value)
```

# --output--

3
MON 1
TUE 2
WED 3
