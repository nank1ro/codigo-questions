---
language: python
exerciseType: 1
---

# --description--

Además de los métodos normales, un enum puede definir **métodos de clase** con `@classmethod`. Reciben la propia clase del enum como `cls`, por lo que son el lugar adecuado para formas alternativas de encontrar un miembro:
```python
class Color(Enum):
    RED = auto()
    BLUE = auto()

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]

print(Color.parse(" blue "))  # Color.BLUE
```
Junto con `auto()`, los métodos, las propiedades y las búsquedas, esto te permite construir enums que llevan su propio comportamiento.

# --instructions--

Declara un enum `Direction` con los miembros `NORTH`, `EAST`, `SOUTH` y `WEST`, en este orden, usando `auto()`.
Agrega un método `opposite()` que devuelva el miembro del lado opuesto (`NORTH` y `SOUTH`, `EAST` y `WEST`), y un método de clase `parse(text)` que devuelva el miembro cuyo nombre coincide con `text`, ignorando mayúsculas y minúsculas y los espacios alrededor

# --seed--

```python
from enum import Enum, auto
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Debes usar `auto()`

```json
{
  "regex": "auto\\(\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Debes usar el decorador `@classmethod`

```json
{
  "regex": "@classmethod",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Direction` debe tener los miembros `NORTH`, `EAST`, `SOUTH` y `WEST` con valores de `1` a `4`

```python
    def test_1(self):
        self.assertEqual([d.name for d in Direction], ["NORTH", "EAST", "SOUTH", "WEST"], "--err-t1--")
        self.assertEqual([d.value for d in Direction], [1, 2, 3, 4], "--err-t1--")
```

`Direction.NORTH.opposite()` debe ser `Direction.SOUTH` y `Direction.WEST.opposite()` debe ser `Direction.EAST`

```python
    def test_2(self):
        self.assertIs(Direction.NORTH.opposite(), Direction.SOUTH, "--err-t2--")
        self.assertIs(Direction.SOUTH.opposite(), Direction.NORTH, "--err-t2--")
        self.assertIs(Direction.EAST.opposite(), Direction.WEST, "--err-t2--")
        self.assertIs(Direction.WEST.opposite(), Direction.EAST, "--err-t2--")
```

`Direction.parse(" west ")` debe ser `Direction.WEST`

```python
    def test_3(self):
        self.assertIs(Direction.parse(" west "), Direction.WEST, "--err-t3--")
        self.assertIs(Direction.parse("North"), Direction.NORTH, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    EAST = auto()
    SOUTH = auto()
    WEST = auto()

    def opposite(self):
        opposites = {
            Direction.NORTH: Direction.SOUTH,
            Direction.SOUTH: Direction.NORTH,
            Direction.EAST: Direction.WEST,
            Direction.WEST: Direction.EAST,
        }
        return opposites[self]

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]
```
