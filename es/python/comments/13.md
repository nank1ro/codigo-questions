---
language: python
exerciseType: 1
---

# --description--

Algunos comentarios siguen una convención que los editores entienden. Los **marcadores** más comunes son:
- `# TODO: ...` señala algo que todavía necesita escribirse
- `# FIXME: ...` señala código que se sabe que está mal y debe corregirse

```python
limit = 10
# TODO: lee el límite desde la configuración
```
Para Python son comentarios ordinarios; los editores los recopilan en un panel dedicado, así que el trabajo pendiente es fácil de encontrar. Un `TODO` suele ir junto a un marcador de posición que mantiene el programa en funcionamiento hasta que se escribe el código real.

Cuando termines el trabajo, sustituye el marcador de posición y borra el marcador en el mismo cambio, para que el comentario nunca mienta sobre el estado del código.

# --instructions--

Completa el trabajo descrito por el comentario `TODO` en `is_even`, y luego elimina el comentario ya que el trabajo está hecho

# --seed--

```python
def is_even(n):
    # TODO: devuelve True cuando n es par, False en caso contrario
    return False
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

El comentario `TODO` debe eliminarse

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`is_even(4)` debe devolver `True`

```python
    def test_1(self):
        self.assertEqual(is_even(4), True, "--err-t1--")
```

`is_even(7)` debe devolver `False`

```python
    def test_2(self):
        self.assertEqual(is_even(7), False, "--err-t2--")
```

`is_even(0)` debe devolver `True`

```python
    def test_3(self):
        self.assertEqual(is_even(0), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_even(n):
    return n % 2 == 0
```
