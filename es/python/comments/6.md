---
language: python
exerciseType: 1
---

# --description--

Cuando una cadena es la **primera instrucción** dentro de una función, Python la trata como la documentación de esa función. Se llama **docstring**:
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"
```
Por convención, una docstring se escribe entre triples comillas dobles, `"""`, incluso cuando cabe en una línea, para que pueda crecer después sin cambiar las comillas.

Escribe el resumen en tercera persona, como si describieras la función: "Returns...", "Adds...", "Checks...". La docstring debe ir antes de cualquier otra instrucción en el cuerpo, de lo contrario es solo una cadena ordinaria.

# --instructions--

Escribe una función `double(n)` que devuelva `n` multiplicado por dos, y dale una docstring de una línea

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`double` debe tener una docstring como su primera instrucción

```python
    def test_1(self):
        self.assertIsNotNone(double.__doc__, "--err-t1--")
```

`double(21)` debe devolver `42`

```python
    def test_2(self):
        self.assertEqual(double(21), 42, "--err-t2--")
```

`double(-3)` debe devolver `-6` y `double(0)` debe devolver `0`

```python
    def test_3(self):
        self.assertEqual(double(-3), -6, "--err-t3--")
        self.assertEqual(double(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def double(n):
    """Returns n multiplied by two."""
    return n * 2
```
