---
language: python
exerciseType: 1
---

# --description--

Python busca `#` solo en el código, nunca dentro de una **cadena**. Entre comillas, `#` es un carácter ordinario:
```python
print("black is #000000")  # a hex colour
```
El primer `#` es parte del texto, el segundo inicia un comentario real. Lo mismo ocurre con `"""` dentro de un comentario `#`: ahí es solo tres caracteres de comilla, y no inicia nada.

# --instructions--

Escribe una función `label(name)` que devuelva `name` con un `#` delante, de modo que `label("python")` devuelva `"#python"`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`label("python")` debe devolver `"#python"`

```python
    def test_1(self):
        self.assertEqual(label("python"), "#python", "--err-t1--")
```

`label("ai")` debe devolver `"#ai"`

```python
    def test_2(self):
        self.assertEqual(label("ai"), "#ai", "--err-t2--")
```

`label("")` debe devolver `"#"`

```python
    def test_3(self):
        self.assertEqual(label(""), "#", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def label(name):
    """Returns name with a # in front of it."""
    return "#" + name
```
