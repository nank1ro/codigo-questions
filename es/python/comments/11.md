---
language: python
exerciseType: 1
---

# --description--

Cuando una línea no basta, una docstring crece hasta un formato fijo, descrito en **PEP 257**: un resumen de una línea, una línea en blanco, luego los detalles, y el `"""` de cierre en una línea propia.
```python
def to_seconds(minutes):
    """Returns the number of seconds in the given minutes.

    minutes is a whole number and is never negative.
    """
    return minutes * 60
```
La línea en blanco importa: las herramientas muestran la primera línea por sí sola, como descripción corta, y guardan el resto para quien quiera leer más.

# --instructions--

Dale a `area` una docstring formada por un resumen de una línea, una línea en blanco, una línea de detalle, y el `"""` de cierre en su propia línea

# --seed--

```python
def area(width, height):
    return width * height
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`area` debe tener una docstring

```python
    def test_1(self):
        self.assertIsNotNone(area.__doc__, "--err-t1--")
```

La docstring debe empezar con una línea de resumen seguida de una línea en blanco y más texto

```python
    def test_2(self):
        lines = area.__doc__.split("\n")
        self.assertTrue(len(lines) >= 3, "--err-t2--")
        self.assertNotEqual(lines[0].strip(), "", "--err-t2--")
        self.assertEqual(lines[1].strip(), "", "--err-t2--")
        self.assertNotEqual(lines[2].strip(), "", "--err-t2--")
        self.assertEqual(lines[-1].strip(), "", "--err-t2--")
```

`area(3, 4)` debe devolver `12` y `area(0, 5)` debe devolver `0`

```python
    def test_3(self):
        self.assertEqual(area(3, 4), 12, "--err-t3--")
        self.assertEqual(area(0, 5), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def area(width, height):
    """Returns the area of a rectangle.

    Both sides are whole numbers of centimetres.
    """
    return width * height
```
