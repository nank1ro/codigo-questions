---
language: python
exerciseType: 1
---

# --description--

Un comentario colocado encima de una función para decir qué hace la función está en el lugar equivocado. La docstring es el lugar para eso: se adjunta a la función, `help()` la encuentra y los editores la muestran, mientras que un comentario `#` encima del `def` es invisible para todos ellos.

```python
# adds a and b
def add(a, b):
    return a + b
```
Mover la misma frase una línea abajo, entre triples comillas, la convierte en documentación real:
```python
def add(a, b):
    """Returns the sum of a and b."""
    return a + b
```

# --instructions--

Elimina los dos comentarios `#` encima de `subtract` y documenta la función con una docstring en su lugar

# --seed--

```python
# subtract
# this function subtracts b from a
def subtract(a, b):
    return a - b
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Los comentarios `#` encima de `subtract` deben eliminarse

```json
{
  "regex": "#\\s*this function subtracts",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

El comentario `# subtract` también debe eliminarse

```json
{
  "regex": "#\\s*subtract",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`subtract` debe tener una docstring

```python
    def test_1(self):
        self.assertIsNotNone(subtract.__doc__, "--err-t1--")
```

`subtract(5, 3)` debe devolver `2`

```python
    def test_2(self):
        self.assertEqual(subtract(5, 3), 2, "--err-t2--")
```

`subtract(0, 4)` debe devolver `-4`

```python
    def test_3(self):
        self.assertEqual(subtract(0, 4), -4, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def subtract(a, b):
    """Returns b subtracted from a."""
    return a - b
```
