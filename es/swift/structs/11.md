---
language: swift
exerciseType: 1
---

# --description--

Algunos valores pertenecen al tipo mismo en lugar de a una instancia concreta: un código de moneda, un valor por defecto compartido, una fábrica que construye un caso común. Márcalos `static` y léelos a través del nombre del tipo:
```swift
struct Money {
    static let currency = "EUR"
    var amount: Int

    static func zero() -> Money {
        return Money(amount: 0)
    }
}

print(Money.currency)     // EUR
print(Money.zero().amount) // 0
```
Aquí `currency` se declara con `let` porque nunca cambia, así que es una constante compartida por todo el programa. `Money.currency` funciona sin crear ni un solo `Money`, mientras que `amount` necesita una instancia.

# --instructions--

Agrega a `Money` una constante estática `currency` igual a `"EUR"` y un método estático `zero` que no tome parámetros y devuelva un `Money` con `amount` igual a `0`

Ejemplo de uso:
```swift
print(Money.currency)
// prints EUR
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Money {
    var amount: Int

}
```

# --asserts--

`currency` debe ser una constante estática

```json
{
  "regex": "static\\s+let\\s+currency",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Money.currency` debe ser `"EUR"`

```swift
tryCatch(Money.currency == "EUR")
```

`Money.zero()` debe tener un `amount` de `0`

```swift
tryCatch(Money.zero().amount == 0)
```

El inicializador miembro debe seguir funcionando

```swift
tryCatch(Money(amount: 7).amount == 7)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int

    static func zero() -> Money {
        return Money(amount: 0)
    }
}
```
