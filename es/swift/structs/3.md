---
language: swift
exerciseType: 2
---

# --description--

A una propiedad almacenada se le puede dar un **valor por defecto** justo donde se declara. Swift infiere su tipo a partir de ese valor, así que puedes omitir la anotación de tipo:
```swift
struct Counter {
    var label: String
    var value = 0
}
```
El inicializador miembro convierte cada propiedad con valor por defecto en un argumento opcional: pásalo para sobrescribir el valor por defecto, omítelo para conservarlo.
```swift
let a = Counter(label: "clicks")
print(a.value) // 0

let b = Counter(label: "clicks", value: 7)
print(b.value) // 7
```

# --instructions--

Completa el código para que `value` empiece en `0` y `c` se cree sin pasar `value`

# --seed--

```swift
struct Counter {
    var label: String
    var value = [/]
}

let c = Counter([/]: "clicks")
print(c.label)
print(c.value)
```

# --answers--

- 0
- label
- 1
- value
- Counter
- name

# --solutions--

```swift
struct Counter {
    var label: String
    var value = 0
}

let c = Counter(label: "clicks")
print(c.label)
print(c.value)
```

# --output--

clicks
0
