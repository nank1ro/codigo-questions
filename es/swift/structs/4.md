---
language: swift
exerciseType: 1
---

# --description--

Una estructura también puede contener **métodos**: funciones escritas dentro de las llaves que trabajan sobre la instancia en la que se llaman. Dentro de un método usas los nombres de las propiedades directamente, sin ningún prefijo:
```swift
struct Rectangle {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}

let r = Rectangle(width: 3, height: 4)
print(r.area()) // 12
```
Si un parámetro del método oculta el nombre de una propiedad, escribe `self.width` para referirte a la propiedad de la instancia.

# --instructions--

Agrega un método `area` a `Rectangle` que no tome parámetros y devuelva el ancho multiplicado por la altura

Ejemplo de llamada a la función:
```swift
print(Rectangle(width: 3, height: 4).area())
// prints 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Rectangle {
    var width: Int
    var height: Int

}
```

# --asserts--

Un rectángulo de `3` por `4` debe tener un área de `12`

```swift
tryCatch(Rectangle(width: 3, height: 4).area() == 12)
```

Un rectángulo de `1` por `1` debe tener un área de `1`

```swift
tryCatch(Rectangle(width: 1, height: 1).area() == 1)
```

Un rectángulo de `0` por `7` debe tener un área de `0`

```swift
tryCatch(Rectangle(width: 0, height: 7).area() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Rectangle {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}
```
