---
language: swift
exerciseType: 1
---

# --description--

Una estructura es un tipo como cualquier otro, así que puede almacenarse en un array, un diccionario o un conjunto, y cada herramienta que ya conoces sigue funcionando con ella:
```swift
struct Item {
    var name: String
    var price: Int
}

let items = [Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]

for item in items {
    print(item.name)
}

let names = items.map { $0.name }
let total = items.reduce(0) { $0 + $1.price }
print(total) // 10
```
Recuerda que el array contiene *copias*: leer `items[0]` en una variable y cambiarla no toca el array.

# --instructions--

Escribe una función `totalPrice` que tome un array de `Item` sin etiqueta de argumento y devuelva la suma del `price` de cada elemento, o `0` cuando el array esté vacío

Ejemplo de llamada a la función:
```swift
print(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]))
// prints 10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice() {

}
```

# --asserts--

Dos elementos de `3` y `7` deben totalizar `10`

```swift
tryCatch(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]) == 10)
```

Un solo elemento debe totalizar su propio precio

```swift
tryCatch(totalPrice([Item(name: "Pen", price: 2)]) == 2)
```

Un array vacío debe totalizar `0`

```swift
tryCatch(totalPrice([]) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice(_ items: [Item]) -> Int {
    return items.reduce(0) { $0 + $1.price }
}
```
