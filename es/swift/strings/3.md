---
language: swift
exerciseType: 2
---

# --description--

Dos cadenas se pueden unir con el operador `+`, que produce una nueva cadena:
```swift
let full = "Hello" + " " + "world" // Hello world
```
Para añadir texto al final de una variable de cadena existente usa `+=`. La variable debe declararse con `var`, porque su valor cambia:
```swift
var log = "Start"
log += "..."
print(log) // Start...
```

# --instructions--

Declara una variable `message` con el valor `Hello`, añádele `, world` con el operador de asignación compuesta, y luego imprímela

# --seed--

```swift
[/] message = "Hello"
message [/] ", world"
print(message)
```

# --answers--

- var
- let
- +=
- +
- =
- -=

# --solutions--

```swift
var message = "Hello"
message += ", world"
print(message)
```

# --output--

Hello, world
