---
language: swift
exerciseType: 1
---

# --description--

Trabajar con índices es tedioso, así que Swift ofrece atajos para los casos más comunes:
- `first` y `last` devuelven el primer y el último carácter como un `Character?` opcional (`nil` para una cadena vacía)
- `prefix(n)` devuelve los primeros `n` caracteres y `suffix(n)` los últimos `n`
```swift
let word = "Swift"
print(word.first!)     // S
print(word.prefix(2))  // Sw
print(word.suffix(3))  // ift
```
`prefix` y `suffix` devuelven una `Substring`, una vista sobre el texto original. Para guardarla como un `String` real, envuélvela en `String(...)`. Si `n` es mayor que `count`, simplemente obtienes la cadena completa.

# --instructions--

Escribe una función `firstTwo` que reciba un `String` (omite la etiqueta de argumento) y devuelva sus dos primeros caracteres como un `String`, y una función `lastTwo` que devuelva sus dos últimos caracteres como un `String`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`firstTwo("Swift")` debe devolver `"Sw"`

```swift
tryCatch(firstTwo("Swift") == "Sw")
```

`firstTwo("a")` debe devolver `"a"`

```swift
tryCatch(firstTwo("a") == "a")
```

`lastTwo("Swift")` debe devolver `"ft"`

```swift
tryCatch(lastTwo("Swift") == "ft")
```

`lastTwo("")` debe devolver `""`

```swift
tryCatch(lastTwo("") == "")
```

`firstTwo` debe devolver un `String`

```swift
tryCatch(type(of: firstTwo("Swift")) == String.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstTwo(_ text: String) -> String {
    return String(text.prefix(2))
}

func lastTwo(_ text: String) -> String {
    return String(text.suffix(2))
}
```
