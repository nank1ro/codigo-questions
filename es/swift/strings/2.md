---
language: swift
exerciseType: 1
---

# --description--

La **interpolación de cadenas** inserta el valor de una expresión dentro de un literal de cadena. Envuelve la expresión en `\()`:
```swift
let name = "Ada"
let age = 36
print("\(name) is \(age) years old") // Ada is 36 years old
```
Se puede interpolar cualquier tipo: números, booleanos y otras cadenas se convierten automáticamente en texto.

# --instructions--

Escribe una función `greet` que reciba un `String` (omite la etiqueta de argumento) y devuelva el texto `Hello, <name>!`, donde `<name>` es el argumento.

Ejemplo de llamada a la función:
```swift
print(greet("Ada"))
// prints Hello, Ada!
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`greet("Ada")` debe devolver `"Hello, Ada!"`

```swift
tryCatch(greet("Ada") == "Hello, Ada!")
```

`greet("Swift")` debe devolver `"Hello, Swift!"`

```swift
tryCatch(greet("Swift") == "Hello, Swift!")
```

`greet("")` debe devolver `"Hello, !"`

```swift
tryCatch(greet("") == "Hello, !")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greet(_ name: String) -> String {
    return "Hello, \(name)!"
}
```
