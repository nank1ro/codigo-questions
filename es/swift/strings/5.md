---
language: swift
exerciseType: 1
---

# --description--

Un `String` es una colección de valores `Character`. Un `Character` es una sola letra, dígito, símbolo o espacio, y se escribe con las mismas comillas dobles que una cadena, por lo que necesitas una anotación de tipo para obtener uno:
```swift
let letter: Character = "a"
let text = "abc"
print(text.count) // 3
```
Comprobar `isEmpty` es preferible a comparar `count` con `0`: se lee mejor y no necesita contar cada carácter.

# --instructions--

Escribe una función `length` que reciba un `String` (omite la etiqueta de argumento) y devuelva su número de caracteres, y una función `hasText` que reciba un `String` (omite la etiqueta de argumento) y devuelva `true` solo cuando la cadena no esté vacía

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`length("Swift")` debe devolver `5`

```swift
tryCatch(length("Swift") == 5)
```

`length("a b c")` debe devolver `5`

```swift
tryCatch(length("a b c") == 5)
```

`length("")` debe devolver `0`

```swift
tryCatch(length("") == 0)
```

`hasText("x")` debe devolver `true`

```swift
tryCatch(hasText("x") == true)
```

`hasText("")` debe devolver `false`

```swift
tryCatch(hasText("") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func length(_ text: String) -> Int {
    return text.count
}

func hasText(_ text: String) -> Bool {
    return !text.isEmpty
}
```
