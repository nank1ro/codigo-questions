---
language: swift
exerciseType: 1
---

# --description--

El framework Foundation añade muchos métodos adicionales para cadenas. Uno de los más útiles es `replacingOccurrences(of:with:)`, que devuelve una nueva cadena en la que cada aparición del primer texto se reemplaza por el segundo:
```swift
import Foundation

let path = "a/b/c"
print(path.replacingOccurrences(of: "/", with: "-")) // a-b-c
```
Recuerda hacer `import Foundation` al principio del archivo, de lo contrario el método no está disponible. Las llamadas a métodos se pueden encadenar, así que `text.lowercased().replacingOccurrences(of: " ", with: "_")` es válido.

# --instructions--

Escribe una función `snakeCase` que reciba un `String` (omite la etiqueta de argumento) y lo devuelva en minúsculas con cada espacio reemplazado por un guion bajo `_`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`snakeCase("Hello World")` debe devolver `"hello_world"`

```swift
tryCatch(snakeCase("Hello World") == "hello_world")
```

`snakeCase("My First Post")` debe devolver `"my_first_post"`

```swift
tryCatch(snakeCase("My First Post") == "my_first_post")
```

`snakeCase("swift")` debe devolver `"swift"`

```swift
tryCatch(snakeCase("swift") == "swift")
```

`snakeCase("")` debe devolver `""`

```swift
tryCatch(snakeCase("") == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func snakeCase(_ text: String) -> String {
    return text.lowercased().replacingOccurrences(of: " ", with: "_")
}
```
