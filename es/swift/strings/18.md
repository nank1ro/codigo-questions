---
language: swift
exerciseType: 4
---

# --description--

Un `Character` no es un `String`, así que no se puede unir directamente a una cadena con `+`. Conviértelo primero con `String(...)`:
```swift
let letter: Character = "a"
let text = String(letter) + "bc" // abc
```
Combinar esto con un bucle `for`-`in` te permite reconstruir una cadena carácter a carácter, por ejemplo colocando cada nuevo carácter delante de los ya recopilados.

# --instructions--

Ordena los elementos para imprimir `swift` invertido, construyendo el resultado carácter a carácter.
Declara `word` antes de `reversed`

# --answers--

```swift
let word = "swift"
```

```swift
var reversed = ""
```

```swift
for letter in word {
```

```swift
    reversed = String(letter) + reversed
```

```swift
}
```

```swift
print(reversed)
```

# --solutions--

```swift
let word = "swift"
var reversed = ""
for letter in word {
    reversed = String(letter) + reversed
}
print(reversed)
```

# --output--

tfiws
