---
language: swift
exerciseType: 2
---

# --description--

cuando un clase tiene su propio functions, esos functions son llamo __methods__.

# --instructions--

Anade un metodo, `description`, un your `Animal` clase.
usando dos separate `Imprime` sentencias, lo deberia Imprime out el `gender` y `legs` de el animal it's llamo on.
entonces llamar `description` metodo.

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4

    [/][/]() {
        if let gender = self.gender { print(gender) }
        print([/].legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.[/]
```

# --answers--

-  description
- func
- description()
- self
- description[]
- funcion
- metodo

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4

    func description() {
        if let gender = self.gender { print(gender) }
        print(self.legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.description()
```

# --output--

female
4
