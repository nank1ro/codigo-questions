---
language: swift
exerciseType: 2
---

# --description--

Puedes acceder a las propiedades de una instancia usando _sintaxis de punto_.
En sintaxis de punto, escribes el nombre de la propiedad inmediatamente después del nombre de la instancia, separado por un punto `.`, sin espacios:
```swift
someInstance.someProperty
```
Usando la misma sintaxis también podemos actualizar el valor de una propiedad

# --instructions--

Asigna a la instancia `Animal`, el género `female`.
Imprime el género de la instancia `Animal`

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4
}
let dog = [/]()
[/][/]gender = "female"
print(dog.[/])
```

# --answers--

- class 
- Dog
- Animal
- Dog
- ()
- Dog()
- dog
- .
- gender

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4
}
let dog = Animal()
dog.gender = "female"
print(dog.gender)
```

# --output--

female
