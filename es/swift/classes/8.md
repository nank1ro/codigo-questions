---
language: swift
exerciseType: 2
---

# --description--

Una ventaja de las estructuras es que tienen un inicializador miembro generado automáticamente, que puedes usar para inicializar las propiedades miembro de nuevas instancias de estructura.
Los valores iniciales para las propiedades de la nueva instancia se pueden pasar al inicializador miembro por nombre

# --instructions--

Crea una instancia de `Animal` inicializando todos los parámetros

# --seed--

```swift
[/] Animal {
    var gender: String
    var legs: Int
}
let dog = Animal([/]: "male", legs: [/])
print(dog.gender)
print(dog.legs)
```

# --answers--

- struct
- class
- gender
- 4

# --solutions--

```swift
struct Animal {
    var gender: String
    var legs: Int
}
let dog = Animal(gender: "male", legs: 4)
print(dog.gender)
print(dog.legs)
```

# --output--

male
4
