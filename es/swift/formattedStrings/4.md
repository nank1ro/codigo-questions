---
language: swift
exerciseType: 1
---

# --description--

Un número entre `%` y la letra establece el **ancho mínimo** del campo. Si el valor es más corto, se añaden espacios a la izquierda para que quede **alineado a la derecha**; si es más largo, no se corta nada:
```swift
print(String(format: "%5d|", 42))    //    42|
print(String(format: "%5d|", 12345)) // 12345|
```
Ancho y precisión se combinan: `%8.2f` significa "al menos 8 caracteres de ancho, con 2 decimales":
```swift
print(String(format: "%8.2f|", 3.14159)) //     3.14|
```
Los anchos fijos son lo que alinea las columnas de una tabla.

# --instructions--

Escribe una función `rightAligned` que reciba un entero (omite la etiqueta de argumento) y lo devuelva como una cadena alineada a la derecha en un campo de ancho `5`, usando `String(format:)`

Ejemplo de llamada a la función:
```swift
print(rightAligned(42) + "|")
// prints    42|
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func rightAligned() {

}
```

# --asserts--

La función debe usar `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`rightAligned(42)` debe devolver `"   42"`

```swift
tryCatch(rightAligned(42) == "   42")
```

`rightAligned(7)` debe devolver `"    7"`

```swift
tryCatch(rightAligned(7) == "    7")
```

`rightAligned(12345)` debe devolver `"12345"`

```swift
tryCatch(rightAligned(12345) == "12345")
```

`rightAligned(123456)` debe devolver `"123456"` (no se corta nada)

```swift
tryCatch(rightAligned(123456) == "123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func rightAligned(_ n: Int) -> String {
    return String(format: "%5d", n)
}
```
