---
language: swift
exerciseType: 1
---

# --description--

La segunda herramienta es `String(format:)`, que proviene del framework **Foundation**, así que el archivo debe empezar con `import Foundation`.

Recibe una **cadena de formato** seguida de los valores a insertar. Dentro de la cadena de formato, un **especificador** que empieza con `%` marca dónde va cada valor y cómo se escribe. El especificador para un entero es `%d`:
```swift
import Foundation

let count = 7
let text = String(format: "Item %d", count)
print(text) // Item 7
```
`String(format:)` devuelve un `String` normal, así que puedes imprimirlo, almacenarlo o devolverlo desde una función.

# --instructions--

Escribe una función `label` que reciba un entero (omite la etiqueta de argumento) y devuelva el texto `Item ` seguido del número, usando `String(format:)` con `%d`

Ejemplo de llamada a la función:
```swift
print(label(7))
// prints Item 7
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func label() {

}
```

# --asserts--

La función debe usar `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`label(7)` debe devolver `"Item 7"`

```swift
tryCatch(label(7) == "Item 7")
```

`label(42)` debe devolver `"Item 42"`

```swift
tryCatch(label(42) == "Item 42")
```

`label(0)` debe devolver `"Item 0"`

```swift
tryCatch(label(0) == "Item 0")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func label(_ n: Int) -> String {
    return String(format: "Item %d", n)
}
```
