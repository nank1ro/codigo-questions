---
language: swift
exerciseType: 1
---

# --description--

Para insertar un `String` en una cadena de formato, usa el especificador `%@`:
```swift
let name = "Ada"
let age = 36
print(String(format: "%@ is %d years old", name, age)) // Ada is 36 years old
```
`%@` acepta un `String` de Swift directamente. No uses `%s` con una cadena de Swift: ese especificador espera una cadena de C e imprime basura o se bloquea.

# --instructions--

Escribe una función `greeting` que reciba un nombre (`String`) y una edad (`Int`), ambos sin etiqueta de argumento, y devuelva `<name> is <age> years old` usando `String(format:)` con `%@` y `%d`

Ejemplo de llamada a la función:
```swift
print(greeting("Ada", 36))
// prints Ada is 36 years old
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func greeting() {

}
```

# --asserts--

La función debe usar `String(format:` con `%@`

```json
{
  "regex": "String\\(format:[^\\n]*%@",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greeting("Ada", 36)` debe devolver `"Ada is 36 years old"`

```swift
tryCatch(greeting("Ada", 36) == "Ada is 36 years old")
```

`greeting("Linus", 54)` debe devolver `"Linus is 54 years old"`

```swift
tryCatch(greeting("Linus", 54) == "Linus is 54 years old")
```

`greeting("Bo", 7)` debe devolver `"Bo is 7 years old"`

```swift
tryCatch(greeting("Bo", 7) == "Bo is 7 years old")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greeting(_ name: String, _ age: Int) -> String {
    return String(format: "%@ is %d years old", name, age)
}
```
