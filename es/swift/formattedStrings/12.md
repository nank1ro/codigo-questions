---
language: swift
exerciseType: 1
---

# --description--

Las banderas de ancho solo funcionan dentro de `String(format:)`. Para rellenar una cadena por tu cuenta, construye los espacios con `String(repeating:count:)` y únelos al texto:
```swift
let text = "7"
let spaces = String(repeating: " ", count: 4 - text.count)
print(spaces + text + "|") //    7|
```
Si el texto ya es más largo que el ancho, `4 - text.count` se vuelve negativo y `String(repeating:count:)` se bloquea. Protégelo con `max(0, ...)`, así el conteo nunca baja de cero y el texto largo se queda como está.

# --instructions--

Escribe una función `leftPad` que reciba un texto (`String`) y un ancho (`Int`), ambos sin etiqueta de argumento, y devuelva el texto alineado a la derecha en ese ancho añadiendo espacios a la izquierda. Un texto más largo que el ancho debe devolverse sin cambios

Ejemplo de llamada a la función:
```swift
print(leftPad("7", 4) + "|")
// prints    7|
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func leftPad() {

}
```

# --asserts--

`leftPad("7", 4)` debe devolver `"   7"`

```swift
tryCatch(leftPad("7", 4) == "   7")
```

`leftPad("ab", 5)` debe devolver `"   ab"`

```swift
tryCatch(leftPad("ab", 5) == "   ab")
```

`leftPad("abcd", 4)` debe devolver `"abcd"`

```swift
tryCatch(leftPad("abcd", 4) == "abcd")
```

`leftPad("hello", 3)` debe devolver `"hello"` (no se corta nada)

```swift
tryCatch(leftPad("hello", 3) == "hello")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func leftPad(_ text: String, _ width: Int) -> String {
    let spaces = String(repeating: " ", count: max(0, width - text.count))
    return spaces + text
}
```
