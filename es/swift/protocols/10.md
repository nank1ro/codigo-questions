---
language: swift
exerciseType: 4
---

# --description--

A protocol extension can also add members the protocol never listed as requirements. They are extra conveniences, available on every conforming type:
```swift
protocol Sized {
    var count: Int { get }
}

extension Sized {
    var isEmpty: Bool {
        return count == 0
    }
}
```
`isEmpty` is not a requirement, so a conforming type does not have to provide it — it simply gets it.

# --instructions--

Sort the items to print `HEY`, declaring the protocol first, then its extension, then the struct, then the `print`

# --answers--

```swift
protocol Loud {
    var text: String { get }
}
```

```swift
extension Loud {
    func shout() -> String { return text.uppercased() }
}
```

```swift
struct Message: Loud {
    var text: String
}
```

```swift
print(Message(text: "hey").shout())
```

# --solutions--

```swift
protocol Loud {
    var text: String { get }
}
extension Loud {
    func shout() -> String { return text.uppercased() }
}
struct Message: Loud {
    var text: String
}
print(Message(text: "hey").shout())
```

# --output--

HEY
