---
language: swift
exerciseType: 4
---

# --description--

Una extensión de protocolo también puede añadir miembros que el protocolo nunca listó como requisitos. Son comodidades extra, disponibles en todo tipo que conforme:
```swift
protocol Sized {
    var count: Int { get }
}

extension Sized {
    var isEmpty: Bool {
        return count == 0
    }
}
```
`isEmpty` no es un requisito, así que un tipo que conforma no tiene que proporcionarlo —simplemente lo obtiene.

# --instructions--

Ordena los elementos para imprimir `HEY`, declarando primero el protocolo, luego su extensión, luego el struct, y luego el `print`

# --answers--

```swift
protocol Loud {
    var text: String { get }
}
```

```swift
extension Loud {
    func shout() -> String { return text.uppercased() }
}
```

```swift
struct Message: Loud {
    var text: String
}
```

```swift
print(Message(text: "hey").shout())
```

# --solutions--

```swift
protocol Loud {
    var text: String { get }
}
extension Loud {
    func shout() -> String { return text.uppercased() }
}
struct Message: Loud {
    var text: String
}
print(Message(text: "hey").shout())
```

# --output--

HEY
