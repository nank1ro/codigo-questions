---
language: swift
exerciseType: 4
---

# --instructions--

Ordena los elementos para imprimir `[G]`, declarando primero el protocolo, luego su extensión, luego el enum, y luego el `print`

# --answers--

```swift
protocol Badge {
    var symbol: String { get }
}
```

```swift
extension Badge {
    func banner() -> String { return "[\(symbol)]" }
}
```

```swift
enum Rank: Badge {
    case gold

    var symbol: String { return "G" }
}
```

```swift
print(Rank.gold.banner())
```

# --solutions--

```swift
protocol Badge {
    var symbol: String { get }
}
extension Badge {
    func banner() -> String { return "[\(symbol)]" }
}
enum Rank: Badge {
    case gold

    var symbol: String { return "G" }
}
print(Rank.gold.banner())
```

# --output--

[G]
