---
language: swift
exerciseType: 1
---

# --description--

La biblioteca estándar está construida a partir de protocolos, y tus propios tipos pueden adoptarlos.

`Equatable` le da a un tipo el operador `==`. Para un struct cuyas propiedades almacenadas son todas `Equatable`, basta con declarar la conformidad —Swift escribe `==` por ti:
```swift
struct Point: Equatable {
    var x: Int
    var y: Int
}

print(Point(x: 1, y: 2) == Point(x: 1, y: 2)) // true
```
`Comparable` hereda de `Equatable` y añade ordenación. Implementas un único operador, `<`, escrito como una `static func` que toma los dos valores, y obtienes `>`, `<=`, `>=`, además de `sorted()`, `min()` y `max()` sobre colecciones gratis:
```swift
struct Version: Comparable {
    var number: Int

    static func < (lhs: Version, rhs: Version) -> Bool {
        return lhs.number < rhs.number
    }
}
```

# --instructions--

Haz que `Weight` conforme a `Comparable`, ordenando por `grams`

Ejemplo de uso:
```swift
print(Weight(grams: 100) < Weight(grams: 250))
// imprime true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Weight {
    var grams: Int
}
```

# --asserts--

`100` grams must be lighter than `250` grams

```swift
tryCatch(Weight(grams: 100) < Weight(grams: 250))
```

`500` grams must be heavier than `250` grams

```swift
tryCatch(Weight(grams: 500) > Weight(grams: 250))
```

Two weights of `80` grams must be equal

```swift
tryCatch(Weight(grams: 80) == Weight(grams: 80))
```

The heaviest of `3` and `1` grams must be `3` grams

```swift
tryCatch([Weight(grams: 3), Weight(grams: 1)].max() == Weight(grams: 3))
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Weight: Comparable {
    var grams: Int

    static func < (lhs: Weight, rhs: Weight) -> Bool {
        return lhs.grams < rhs.grams
    }
}
```
