---
language: swift
exerciseType: 2
---

# --description--

La conformidad no tiene que declararse junto al tipo. Una **extensión** puede añadirla más tarde, lo que mantiene la propia declaración del tipo centrada en sus datos:
```swift
protocol Resettable {
    mutating func reset()
}

struct Timer {
    var seconds = 0
}

extension Timer: Resettable {
    mutating func reset() {
        seconds = 0
    }
}
```
Esto también funciona con tipos que no escribiste tú: puedes hacer que un tipo de la biblioteca estándar conforme a uno de tus protocolos sin tocar su código fuente.

# --instructions--

Completa el código para que `increment()` sea un requisito mutating y `Clicks` adopte `Counter` en una extensión

# --seed--

```swift
protocol Counter {
    [/] func increment()
}

struct Clicks {
    var count = 0
}

[/] Clicks: Counter {
    mutating func increment() {
        count [/] 1
    }
}

var c = Clicks()
c.increment()
c.increment()
print(c.count)
```

# --answers--

- mutating
- extension
- +=
- static
- struct
- -=

# --solutions--

```swift
protocol Counter {
    mutating func increment()
}

struct Clicks {
    var count = 0
}

extension Clicks: Counter {
    mutating func increment() {
        count += 1
    }
}

var c = Clicks()
c.increment()
c.increment()
print(c.count)
```

# --output--

2
