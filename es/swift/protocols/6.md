---
language: swift
exerciseType: 4
---

# --description--

Un protocolo normalmente reúne más de un requisito, y un tipo que conforma debe satisfacerlos todos:
```swift
protocol Vehicle {
    var wheels: Int { get }
    func move() -> String
}

struct Bike: Vehicle {
    var wheels = 2

    func move() -> String {
        return "pedalling"
    }
}
```
El orden de los requisitos dentro de las llaves no importa, y tampoco importa el orden en que el tipo que conforma los proporciona: el compilador solo comprueba que no falte nada.

# --instructions--

Ordena los elementos para imprimir `Moo`, declarando primero el protocolo, luego el struct, y luego la instancia antes del `print`

# --answers--

```swift
protocol Animal {
    func sound() -> String
}
```

```swift
struct Cow: Animal {
    func sound() -> String { return "Moo" }
}
```

```swift
let cow = Cow()
```

```swift
print(cow.sound())
```

# --solutions--

```swift
protocol Animal {
    func sound() -> String
}
struct Cow: Animal {
    func sound() -> String { return "Moo" }
}
let cow = Cow()
print(cow.sound())
```

# --output--

Moo
