---
language: swift
exerciseType: 1
---

# --instructions--

Tienes `60` productos en el `store`, durante el día vendiste `50` de ellos y `compraste` `55` nuevos.
Actualiza la cantidad del `store`.

# --seed--

```swift
var store = 60
var sold = 50
var bought = 55
// escribe después de esta línea
```

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`store` debe ser igual a `65`.

```swift
    func test1() {
        XCTAssertEqual(store, 65, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
var store = 60
var sold = 50
var bought = 55
store += bought - sold;
```
