---
language: swift
exerciseType: 1
---

# --description--

Un `FIXME` es diferente de un `TODO`: el código ya existe, pero se sabe que está mal. Un buen `FIXME` dice cuál es el error y, cuando es posible, da un ejemplo que lo muestra, para que la siguiente persona pueda corregirlo rápidamente. Igual que con `TODO`, borra el marcador una vez corregido el error, pero conserva el comentario de documentación, que sigue siendo cierto.

# --instructions--

Corrige el error descrito por el comentario `FIXME` en `average` (añade los paréntesis para que la suma se calcule antes de la división) y luego elimina el comentario `FIXME`. Conserva el comentario de documentación `///`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
/// Returns the average of `a` and `b`, rounded down.
func average(_ a: Int, _ b: Int) -> Int {
    // FIXME: divides before adding, so average(4, 6) gives 7 instead of 5
    return a + b / 2
}
```

# --asserts--

El comentario `FIXME` debe eliminarse

```json
{
  "regex": "FIXME",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

El comentario de documentación `///` debe conservarse

```json
{
  "regex": "/// Returns the average",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`average(4, 6)` debe devolver `5`

```swift
tryCatch(average(4, 6) == 5)
```

`average(10, 20)` debe devolver `15`

```swift
tryCatch(average(10, 20) == 15)
```

`average(3, 4)` debe devolver `3`

```swift
tryCatch(average(3, 4) == 3)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
/// Returns the average of `a` and `b`, rounded down.
func average(_ a: Int, _ b: Int) -> Int {
    return (a + b) / 2
}
```
