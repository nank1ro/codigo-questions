---
language: swift
exerciseType: 1
---

# --description--

A diferencia de `//`, que se detiene al final de la línea, un comentario `/*` solo se detiene en el `*/`. Si olvidas cerrarlo, el compilador trata todo el código siguiente como parte del comentario y reporta un error:
```swift
let width = 10 /* in centimetres
print(width) // still inside the comment: error, the comment is never closed
```
Tanto `//` como `/* */` funcionan como comentarios al final de la línea, pero con `/*` asegúrate siempre de que el `*/` está presente.

# --instructions--

`twice` no compila porque el comentario al final de la primera línea nunca se cierra, así que se traga el `return`. Cierra el comentario al final de esa misma línea

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func twice(_ n: Int) -> Int {
    let result = n * 2 /* twice the input
    return result
}
```

# --asserts--

El comentario debe cerrarse en la misma línea

```json
{
  "regex": "/\\* twice the input \\*/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`twice(4)` debe devolver `8`

```swift
tryCatch(twice(4) == 8)
```

`twice(-3)` debe devolver `-6`

```swift
tryCatch(twice(-3) == -6)
```

`twice(0)` debe devolver `0`

```swift
tryCatch(twice(0) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func twice(_ n: Int) -> Int {
    let result = n * 2 /* twice the input */
    return result
}
```
