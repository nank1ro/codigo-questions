---
language: swift
exerciseType: 4
---

# --description--

Después del resumen, un comentario de documentación puede describir los parámetros y el valor de retorno con elementos de lista especiales de Markdown que Xcode reconoce:
```swift
/// Devuelve el número de segundos en los minutos dados.
/// - Parameter minutes: un número entero de minutos, nunca negativo
/// - Returns: `minutes` multiplicado por sesenta
func toSeconds(_ minutes: Int) -> Int {
    return minutes * 60
}
```
El orden es siempre el mismo: primero el resumen, después `- Parameter nombre:` para cada parámetro y por último `- Returns:`.

# --instructions--

Ordena los elementos para documentar `twice` con un resumen, una línea `Parameter` y una línea `Returns`, en este orden y justo encima de la función, y luego imprime `twice(21)`

# --answers--

```swift
/// Devuelve el doble de un número.
```

```swift
/// - Parameter n: el número a duplicar
```

```swift
/// - Returns: `n` multiplicado por dos
```

```swift
func twice(_ n: Int) -> Int {
    return n * 2
}
```

```swift
print(twice(21))
```

# --solutions--

```swift
/// Devuelve el doble de un número.
/// - Parameter n: el número a duplicar
/// - Returns: `n` multiplicado por dos
func twice(_ n: Int) -> Int {
    return n * 2
}
print(twice(21))
```

# --output--

42
