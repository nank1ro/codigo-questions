---
language: swift
exerciseType: 1
---

# --description--

Para comentar varias líneas a la vez, envuélvelas en un único comentario de bloque en lugar de añadir `//` a cada línea:
```swift
var total = 100
/*
total = total - 30
total = total - 20
*/
print(total) // prints 100
```
Gracias al anidamiento, esto funciona incluso cuando una de esas líneas ya contiene un comentario `/* */`.

# --instructions--

Comenta las dos líneas que cambian `total` con un único comentario de bloque, de modo que `total` se quede en `100`. Deja el comentario interior `/* base */` tal como está

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
var total = 100
total = total /* base */ - 30
total = total - 20
```

# --asserts--

Las dos líneas deben quedar envueltas en un comentario de bloque que aún contenga `/* base */`

```json
{
  "regex": "/\\*[\\s\\S]*total = total /\\* base \\*/ - 30[\\s\\S]*total = total - 20[\\s\\S]*\\*/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`total` debe ser igual a `100`

```swift
tryCatch(total == 100)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
var total = 100
/*
total = total /* base */ - 30
total = total - 20
*/
```
