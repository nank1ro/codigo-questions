---
language: swift
exerciseType: 1
---

# --description--

Devolver un cierre es una forma práctica de construir funciones personalizadas. Los parámetros de la función externa son capturados por el cierre que devuelve:
```swift
func makeAdder(_ amount: Int) -> (Int) -> Int {
    return { $0 + amount }
}
let addFive = makeAdder(5)
print(addFive(10)) // 15
```
El tipo de retorno `(Int) -> Int` describe el cierre, y la forma abreviada `$0` se refiere al argumento de ese cierre, no al de `makeAdder`.

# --instructions--

Escribe una función `makeMultiplier` que reciba un `Int` llamado `factor` (omite la etiqueta del argumento) y devuelva un cierre de tipo `(Int) -> Int` que multiplique su argumento por `factor`

Ejemplo de llamada a la función:
```swift
let double = makeMultiplier(2)
print(double(10))
// prints 20
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`makeMultiplier(2)(10)` debe devolver `20`

```swift
tryCatch(makeMultiplier(2)(10) == 20)
```

`makeMultiplier(3)(4)` debe devolver `12`

```swift
tryCatch(makeMultiplier(3)(4) == 12)
```

`makeMultiplier(0)(99)` debe devolver `0`

```swift
tryCatch(makeMultiplier(0)(99) == 0)
```

Un cierre devuelto por `makeMultiplier(5)` debe seguir funcionando entre llamadas

```swift
let _timesFive = makeMultiplier(5)
tryCatch(_timesFive(1) == 5 && _timesFive(6) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func makeMultiplier(_ factor: Int) -> (Int) -> Int {
    return { $0 * factor }
}
```
