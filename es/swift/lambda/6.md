---
language: swift
exerciseType: 4
---

# --description--

Los cierres brillan con los métodos de array que reciben uno como argumento. `map` llama al cierre en cada elemento y devuelve un nuevo array con los resultados:
```swift
let nums = [1, 2, 3]
let doubled = nums.map { $0 * 2 }
print(doubled) // [2, 4, 6]
```
El array original no cambia. Como `map` recibe un único cierre como argumento, la sintaxis de cierre final es la forma habitual de llamarlo.

# --instructions--

Ordena los elementos para construir un array con los cuadrados de `[1, 2, 3, 4]` usando `map`, e imprímelo

# --answers--

```swift
let nums = [1, 2, 3, 4]
```

```swift
let squares = nums.map { $0 * $0 }
```

```swift
print(squares)
```

# --solutions--

```swift
let nums = [1, 2, 3, 4]
let squares = nums.map { $0 * $0 }
print(squares)
```

# --output--

[1, 4, 9, 16]
