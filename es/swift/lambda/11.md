---
language: swift
exerciseType: 2
---

# --description--

Un cierre puede usar variables declaradas fuera de su cuerpo. Las **captura**: la variable sigue existiendo mientras el cierre exista, incluso después de que la función que la declaró haya retornado.
Esto permite que una función construya un cierre con su propio estado privado:
```swift
func makeCounter() -> () -> Int {
    var count = 0
    return {
        count += 1
        return count
    }
}
```
`() -> Int` es el tipo de un cierre sin parámetros que devuelve un `Int`. Cada llamada al cierre devuelto incrementa el mismo `count` capturado:
```swift
let counter = makeCounter()
print(counter()) // 1
print(counter()) // 2
```

# --instructions--

Completa `makeCounter` para que devuelva un cierre sin parámetros que incremente y devuelva el `count` capturado, de modo que el programa imprima `1`, `2` y `3` en tres líneas

# --seed--

```swift
func makeCounter() -> [/] {
    [/] count = 0
    return {
        count += 1
        return count
    }
}
let counter = makeCounter()
print(counter())
print(counter())
print(counter())
```

# --answers--

- () -> Int
- var
- let
- Int
- () -> Void

# --solutions--

```swift
func makeCounter() -> () -> Int {
    var count = 0
    return {
        count += 1
        return count
    }
}
let counter = makeCounter()
print(counter())
print(counter())
print(counter())
```

# --output--

1
2
3
