---
language: swift
exerciseType: 1
---

# --description--

`filter` conserva solo los elementos para los que el cierre devuelve `true`. El cierre recibe un elemento y debe devolver un `Bool`:
```swift
let nums = [5, 12, 8, 20]
let big = nums.filter { $0 > 10 }
print(big) // [12, 20]
```
Los elementos conservan su orden original, y el resultado es un nuevo array del mismo tipo de elemento.

# --instructions--

Escribe una función `evens` que reciba un array de `Int` (omite la etiqueta del argumento) y devuelva un nuevo array solo con los números pares, usando `filter`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Debes usar `filter`

```json
{
  "regex": "\\.filter",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`evens([1, 2, 3, 4, 5, 6])` debe devolver `[2, 4, 6]`

```swift
tryCatch(evens([1, 2, 3, 4, 5, 6]) == [2, 4, 6])
```

`evens([7, 9])` debe devolver `[]`

```swift
tryCatch(evens([7, 9]) == [])
```

`evens([0, -2, 3])` debe devolver `[0, -2]`

```swift
tryCatch(evens([0, -2, 3]) == [0, -2])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func evens(_ nums: [Int]) -> [Int] {
    return nums.filter { $0 % 2 == 0 }
}
```
