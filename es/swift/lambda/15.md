---
language: swift
exerciseType: 1
---

# --description--

Por defecto, un cierre pasado a una función solo debe usarse mientras esa función se ejecuta. Si la función almacena el cierre o devuelve otro cierre que lo usa, el cierre **escapa** de la función, y su parámetro debe marcarse con `@escaping`:
```swift
func twice(_ task: @escaping () -> Int) -> () -> Int {
    return { task() * 2 }
}
let answer = twice { 21 }
print(answer()) // 42
```
Sin `@escaping` el compilador informa un error, porque el cierre devuelto usaría `task` después de que `twice` haya terminado.

# --instructions--

Escribe una función `compose` que reciba dos cierres de tipo `(Int) -> Int` (omite ambas etiquetas de argumento) y devuelva un cierre que aplique el primero y luego el segundo a su argumento

Ejemplo de llamada a la función:
```swift
let addThenDouble = compose({ $0 + 1 }, { $0 * 2 })
print(addThenDouble(3))
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`compose({ $0 + 1 }, { $0 * 2 })(3)` debe devolver `8`

```swift
tryCatch(compose({ $0 + 1 }, { $0 * 2 })(3) == 8)
```

`compose({ $0 * 2 }, { $0 + 1 })(3)` debe devolver `7`

```swift
tryCatch(compose({ $0 * 2 }, { $0 + 1 })(3) == 7)
```

`compose({ $0 }, { $0 })(9)` debe devolver `9`

```swift
tryCatch(compose({ $0 }, { $0 })(9) == 9)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func compose(_ first: @escaping (Int) -> Int, _ second: @escaping (Int) -> Int) -> (Int) -> Int {
    return { second(first($0)) }
}
```
