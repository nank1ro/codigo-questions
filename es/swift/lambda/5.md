---
language: swift
exerciseType: 1
---

# --description--

Cuando un cierre es el **último** argumento de una función, puedes escribirlo después del paréntesis de cierre de la llamada. Esta es la sintaxis de **cierre final**:
```swift
print(apply(5) { $0 + 1 }) // 6
```
Si el cierre es el único argumento, los paréntesis se pueden omitir por completo:
```swift
func run(_ task: () -> Int) -> Int {
    return task()
}
print(run { 42 }) // 42
```
Ambas formas llaman exactamente a la misma función, la sintaxis final solo es más fácil de leer cuando el cierre es largo.

# --instructions--

Escribe una función `applyTwice` que reciba un `Int` y un cierre de tipo `(Int) -> Int` (omite ambas etiquetas de argumento) y devuelva el resultado de aplicar el cierre al número dos veces

Ejemplo de llamada a la función:
```swift
print(applyTwice(3) { $0 * 2 })
// prints 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`applyTwice(3) { $0 * 2 }` debe devolver `12`

```swift
tryCatch(applyTwice(3) { $0 * 2 } == 12)
```

`applyTwice(1) { $0 + 10 }` debe devolver `21`

```swift
tryCatch(applyTwice(1) { $0 + 10 } == 21)
```

`applyTwice(2, { $0 * $0 })` debe devolver `16`

```swift
tryCatch(applyTwice(2, { $0 * $0 }) == 16)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func applyTwice(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(operation(n))
}
```
