---
language: swift
exerciseType: 2
---

# --description--

Como los cierres son valores, una función puede aceptar uno como parámetro. El tipo del parámetro es simplemente el tipo de función:
```swift
func apply(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(n)
}
print(apply(5, { $0 + 1 })) // 6
```
La función `apply` no sabe qué hace `operation`, solo sabe que recibe un `Int` y devuelve un `Int`, y la llama como a cualquier otra función.

# --instructions--

Completa la función `apply` para que su segundo parámetro sea un cierre que reciba un `Int` y devuelva un `Int`, y para que la función devuelva el resultado de llamar a ese cierre con `n`, de modo que el programa imprima `12`

# --seed--

```swift
func apply(_ n: Int, _ operation: [/]) -> Int {
    return [/]
}
print(apply(6, { $0 * 2 }))
```

# --answers--

- (Int) -> Int
- operation(n)
- Int
- (Int) -> Void
- operation
- n

# --solutions--

```swift
func apply(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(n)
}
print(apply(6, { $0 * 2 }))
```

# --output--

12
