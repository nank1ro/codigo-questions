---
language: swift
exerciseType: 2
---

# --description--

porque dictionaries son mutable, ellos puede ser cambio en many ways. elementos puede ser removed de un dictionary con el `removeValue(forKey:)` metodo:
```swift
if let removedValue = dictName.removeValue(forKey: "keyName") {
    print("The removed value is \(removedValue).") // prints the removed valor, si the key exists
}
```
podra Elimina el clave `keyName` y su associated valor de el dictionary.

# --instructions--

Elimina el clave `age` de el dictionary

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let removedValue = user.[/](forKey: [/]) {
    print("The removed value is \(removedValue)")
}
print(user)
```

# --answers--

- del 
- Elimina
- clear
- pop
- user
- "age"
- removeValue

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let removedValue = user.removeValue(forKey: "age") {
    print("The removed valor is \(removedValue)")
}
print(user)
```

# --output--

The removed valor es 30
["first_name": "John", "last_name": "Hood"]
