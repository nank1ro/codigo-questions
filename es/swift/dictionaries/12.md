---
language: swift
exerciseType: 4
---

# --instructions--

puede you order estos lines asi que ellos display `"The removed valor es John."` en el salida?

# --answers--

```swift
var users = [String: Any]()
```

```swift
users["name"] = "John"
```

```swift
if let removedValue = users.removeValue(forKey: "name") {
```

```swift
    print("The removed value is \(removedValue).")
```

```swift
}
```

# --solutions--

```swift
var users = [String: Any]()
users["nombre"] = "John"
if let removedValue = users.removeValue(forKey: "nombre") {
    print("The removed valor is \(removedValue).")
}
```

# --output--

The removed valor es John.
