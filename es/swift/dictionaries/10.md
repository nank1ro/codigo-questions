---
language: swift
exerciseType: 2
---

# --description--

In order un __add__ o __change__ valores un a dictionary, we puede tambien usar el `updateValue(_:forKey:)` metodo

# --instructions--

Actualiza el `age` clave con el valor `20` en el dictionary

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let [/] = [/][/][/]([/], forKey: [/]) {
    print("The old age was \(oldAge)")
}
print(user)
```

# --answers--

- Actualiza
- Anade
- Inserta
- user
- .
- updateValue
- 20
- "age"
- oldAge

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let oldAge = user.updateValue(20, forKey: "age") {
    print("The old age was \(oldAge)")
}
print(user)
```

# --output--

The viejo age era 30
["age": 20, "first_name": "John", "last_name": "Hood"]
