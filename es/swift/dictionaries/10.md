---
language: swift
exerciseType: 2
---

# --description--

Para __agregar__ o __cambiar__ valores a un diccionario, también podemos usar el método `updateValue(_:forKey:)`

# --instructions--

Actualiza la clave `age` con el valor `20` en el diccionario

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let [/] = [/][/][/]([/], forKey: [/]) {
    print("The old age was \(oldAge)")
}
print(user)
```

# --answers--

- update
- add
- insert
- user
- .
- updateValue
- 20
- "age"
- oldAge

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let oldAge = user.updateValue(20, forKey: "age") {
    print("The old age was \(oldAge)")
}
print(user)
```

# --output--

The old age was 30
["age": 20, "first_name": "John", "last_name": "Hood"]
