---
language: swift
exerciseType: 2
---

# --description--

Accessing dictionary valores por clave es solo like accessing arreglo valores por indice:
```swift
// obtiene the age valor de the user dictionary
user['age']
```

# --instructions--

Imprime out el valor de el clave `last_name`

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let lastName = [/][/][/][/] {
    print("The last name is [/]")
}
```

# --answers--

- user
- [
- ]
- last_name
- "last_name"
- \(lastName)

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let lastName = user["last_name"] {
    print("The last nombre is \(lastName)")
}
```

# --output--

el ultimo nombre es Hood
