---
language: swift
exerciseType: 1
---

# --description--

Una función que recibe un cierre tiene un problema: no puede saber si el cierre que le entregan lanzará un error. Marcar la función como `throws` obligaría a quien la llama a escribir `try`, incluso a quien pasa un cierre inofensivo. `rethrows` dice *yo lanzo solo si el cierre que me diste lanza*:
```swift
func applyTwice(_ value: Int, _ transform: (Int) throws -> Int) rethrows -> Int {
    return try transform(transform(value))
}
```
Dentro del cuerpo igual escribes `try`, porque la llamada de verdad podría fallar. En el punto de llamada el compilador mira el cierre que pasaste:
```swift
let doubled = applyTwice(3, { (n: Int) -> Int in n * 2 }) // no se necesita try
```
La biblioteca estándar usa esto por todas partes — `map`, `filter` y `sorted(by:)` son todas `rethrows` — razón por la que nunca escribes `try` delante de un `map` ordinario.

# --instructions--

Completa `applyAll` para que acepte un cierre que lanza errores, lance un error solo cuando ese cierre lo lance, y devuelva el arreglo de valores transformados

Ejemplo de llamada a la función:
```swift
print(applyAll([1, 2], { (n: Int) -> Int in n + 1 }))
// imprime [2, 3]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func applyAll(_ numbers: [Int], _ transform: (Int) -> Int) -> [Int] {
    
}
```

# --before-asserts--

```swift
enum _TestError: Error {
    case boom
}

func _propagates() -> Bool {
    do {
        _ = try applyAll([1, 2], { (n: Int) -> Int in throw _TestError.boom })
        return false
    } catch {
        return true
    }
}
```

# --asserts--

duplicar `1, 2, 3` debe dar `2, 4, 6` sin ningún `try`

```swift
let _doubled: [Int] = applyAll([1, 2, 3], { (n: Int) -> Int in n * 2 })
tryCatch(_doubled == [2, 4, 6])
```

un arreglo vacío debe dar un arreglo vacío

```swift
let _empty: [Int] = applyAll([], { (n: Int) -> Int in n * 2 })
tryCatch(_empty.isEmpty)
```

sumar uno a `5` debe dar `6`

```swift
let _plus: [Int] = applyAll([5], { (n: Int) -> Int in n + 1 })
tryCatch(_plus == [6])
```

un cierre que lanza un error debe hacer que `applyAll` lance un error

```swift
tryCatch(_propagates())
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func applyAll(_ numbers: [Int], _ transform: (Int) throws -> Int) rethrows -> [Int] {
    var result: [Int] = []
    for number in numbers {
        result.append(try transform(number))
    }
    return result
}
```
