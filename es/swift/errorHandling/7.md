---
language: swift
exerciseType: 1
---

# --description--

Un validador se lee mejor cuando los rechazos van primero y el trabajo real queda sin indentar abajo. `guard` está hecho para eso: enuncia la condición que debe cumplirse, y su bloque `else` se ejecuta cuando no se cumple.
```swift
func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
El bloque `else` de un `guard` tiene que abandonar el ámbito actual, y `throw` es una de las formas de hacerlo, junto con `return`, `break` y `continue`. Varios `guard` apilados al principio de una función se leen como la lista de reglas que la entrada debe satisfacer.

# --instructions--

Completa `priceFor` con un `guard` que lance `OrderError.notPositive` a menos que `quantity` sea mayor que `0`

Ejemplo de llamada a la función:
```swift
do {
    print(try priceFor(4))
} catch {
    print("invalid")
}
// imprime 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    
    return quantity * 3
}
```

# --before-asserts--

```swift
func _price(_ quantity: Int) -> Int {
    do {
        return try priceFor(quantity)
    } catch OrderError.notPositive {
        return -1
    } catch {
        return -2
    }
}
```

# --asserts--

`priceFor(4)` debe devolver `12`

```swift
tryCatch(_price(4) == 12)
```

`priceFor(1)` debe devolver `3`

```swift
tryCatch(_price(1) == 3)
```

`priceFor(0)` debe lanzar `notPositive`

```swift
tryCatch(_price(0) == -1)
```

`priceFor(-5)` debe lanzar `notPositive`

```swift
tryCatch(_price(-5) == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
