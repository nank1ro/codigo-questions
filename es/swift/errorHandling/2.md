---
language: swift
exerciseType: 1
---

# --description--

Una función que lanza errores puede seguir devolviendo un valor. La palabra clave `throws` va entre la lista de parámetros y la flecha de retorno:
```swift
func square(_ n: Int) throws -> Int {
    if n < 0 {
        throw SquareError.negative
    }
    return n * n
}
```
Leyéndolo en voz alta: *square recibe un `Int`, puede lanzar errores y devuelve un `Int`*.

En el punto de llamada el valor solo existe cuando no se lanzó nada, así que la asignación vive dentro del bloque `do`:
```swift
do {
    let result = try square(4)
    print(result) // 16
} catch {
    print("failed")
}
```
El `try` no es un adorno opcional: el compilador rechaza la llamada sin él, así que quien lee siempre ve qué líneas pueden fallar.

# --instructions--

Marca `half` para que lance errores, haz que lance `MathError.notEven` cuando `n` sea impar y, en caso contrario, devuelva `n / 2`

Ejemplo de llamada a la función:
```swift
do {
    print(try half(10))
} catch {
    print("odd")
}
// imprime 5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum MathError: Error {
    case notEven
}

func half(_ n: Int) -> Int {
    
}
```

# --before-asserts--

```swift
func _half(_ n: Int) -> Int {
    do {
        return try half(n)
    } catch {
        return -999
    }
}
```

# --asserts--

`half(10)` debe devolver `5`

```swift
tryCatch(_half(10) == 5)
```

`half(0)` debe devolver `0`

```swift
tryCatch(_half(0) == 0)
```

`half(7)` debe lanzar un error

```swift
tryCatch(_half(7) == -999)
```

`half(-4)` debe devolver `-2`

```swift
tryCatch(_half(-4) == -2)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum MathError: Error {
    case notEven
}

func half(_ n: Int) throws -> Int {
    if n % 2 != 0 {
        throw MathError.notEven
    }
    return n / 2
}
```
