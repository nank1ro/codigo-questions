---
language: swift
exerciseType: 1
---

# --description--

Cuando una función lanza un error, todo lo que va después del `throw` se omite — incluida la línea que se suponía que cerraba el archivo o liberaba el bloqueo. `defer` lo resuelve: registra un bloque ahora y lo ejecuta cuando termina el ámbito actual, ocurra como ocurra.
```swift
func load() throws {
    print("open")
    defer { print("close") }
    throw FileError.missing
}
```
La llamada imprime `open`, luego `close`, y solo entonces el error viaja hacia quien llamó. Si la función hubiera devuelto con normalidad, `close` igual se habría impreso — de eso se trata. Pon la limpieza justo al lado de la preparación y deja de preocuparte por qué salida toma el código.

# --instructions--

Añade un bloque `defer` dentro de `run` que añada `"cleanup"` a `log`, de modo que ocurra tanto cuando `run` devuelve con normalidad como cuando lanza un error

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum TaskError: Error {
    case failed
}

func steps(_ shouldFail: Bool) -> [String] {
    var log: [String] = []
    func run() throws {
        log.append("start")
        
        if shouldFail {
            throw TaskError.failed
        }
        log.append("done")
    }
    do {
        try run()
    } catch {
        log.append("caught")
    }
    return log
}
```

# --asserts--

en el camino con éxito el log debe ser `start`, `done`, `cleanup`

```swift
let _ok: [String] = steps(false)
tryCatch(_ok == ["start", "done", "cleanup"])
```

en el camino que lanza un error el log debe ser `start`, `cleanup`, `caught`

```swift
let _bad: [String] = steps(true)
tryCatch(_bad == ["start", "cleanup", "caught"])
```

la limpieza debe ejecutarse en ambos caminos

```swift
tryCatch(steps(false).contains("cleanup") && steps(true).contains("cleanup"))
```

la limpieza debe ejecutarse exactamente una vez en el camino que lanza un error

```swift
let _once: [String] = steps(true)
tryCatch(_once.filter { $0 == "cleanup" }.count == 1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum TaskError: Error {
    case failed
}

func steps(_ shouldFail: Bool) -> [String] {
    var log: [String] = []
    func run() throws {
        log.append("start")
        defer { log.append("cleanup") }
        if shouldFail {
            throw TaskError.failed
        }
        log.append("done")
    }
    do {
        try run()
    } catch {
        log.append("caught")
    }
    return log
}
```
