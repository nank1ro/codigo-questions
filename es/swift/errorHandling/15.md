---
language: swift
exerciseType: 4
---

# --description--

Dónde se sitúa el bloque `do` decide cuánto cuesta un solo fallo. Ponlo **dentro** del bucle y cada elemento obtiene su propio intento, así un valor malo se omite y el resto sigue ejecutándose:
```swift
for age in [4, -1, 7] {
    do {
        print(try label(age))
    } catch {
        print("skipped")
    }
}
```
Envolver todo el bucle en un solo `do` en cambio se detendría en el primer error y nunca llegaría a `7`. Ninguna opción está mal — es la diferencia entre *un elemento malo* y *rendirse*.

# --instructions--

Ordena los elementos para imprimir las líneas de abajo, declarando primero el tipo de error y la función antes del bucle
```
age 4
skipped
age 7
```

# --answers--

```swift
enum AgeError: Error {
    case negative
}
```

```swift
func label(_ age: Int) throws -> String {
    if age < 0 {
        throw AgeError.negative
    }
    return "age \(age)"
}
```

```swift
for age in [4, -1, 7] {
    do {
```

```swift
        print(try label(age))
```

```swift
    } catch {
        print("skipped")
    }
}
```

# --solutions--

```swift
enum AgeError: Error {
    case negative
}
func label(_ age: Int) throws -> String {
    if age < 0 {
        throw AgeError.negative
    }
    return "age \(age)"
}
for age in [4, -1, 7] {
    do {
        print(try label(age))
    } catch {
        print("skipped")
    }
}
```

# --output--

age 4
skipped
age 7
