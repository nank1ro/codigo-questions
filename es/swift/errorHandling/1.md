---
language: swift
exerciseType: 2
---

# --description--

En Swift un error es un **valor**, no un fallo. Cualquier tipo puede actuar como tal adoptando el protocolo `Error`, y un enum es la opción habitual porque sus casos nombran exactamente lo que puede salir mal:
```swift
enum LoginError: Error {
    case wrongPassword
}
```
Una función que puede fallar se marca con `throws`, e informa del fallo con `throw`:
```swift
func login(_ password: String) throws {
    if password != "swift" {
        throw LoginError.wrongPassword
    }
    print("welcome")
}
```
Llamar a semejante función necesita `try`, y la llamada debe vivir dentro de un bloque `do` seguido de un bloque `catch` que diga qué hacer cuando falla:
```swift
do {
    try login("hunter2")
} catch {
    print("login failed")
}
// login failed
```
Cuando se ejecuta `throw`, el resto del bloque `do` se omite y `catch` toma el control. Nada se interrumpe: el programa continúa después del `catch`.

# --instructions--

Completa el código para que `TempError` sea un tipo de error, `check` lance un error cuando haga demasiado frío y el fallo se maneje, imprimiendo `too cold`

# --seed--

```swift
enum TempError: [/] {
    case tooCold
}

func check(_ degrees: Int) [/] {
    if degrees < 10 {
        [/] TempError.tooCold
    }
    print("fine")
}

do {
    [/] check(4)
} [/] {
    print("too cold")
}
```

# --answers--

- Error
- throws
- throw
- try
- catch
- Exception
- finally
- raise

# --solutions--

```swift
enum TempError: Error {
    case tooCold
}

func check(_ degrees: Int) throws {
    if degrees < 10 {
        throw TempError.tooCold
    }
    print("fine")
}

do {
    try check(4)
} catch {
    print("too cold")
}
```

# --output--

too cold
