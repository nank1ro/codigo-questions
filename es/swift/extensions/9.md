---
language: swift
exerciseType: 2
---

# --description--

Una extensión puede añadir **inicializadores** nuevos a un tipo. Para un struct este es el mejor lugar para ponerlos: un `init` escrito dentro del cuerpo del struct reemplaza el inicializador miembro a miembro automático, mientras que uno añadido en una extensión lo conserva.
El inicializador nuevo normalmente delega en uno existente con `self.init(...)`:
```swift
struct Size {
    var width: Double
    var height: Double
}
extension Size {
    init(square side: Double) {
        self.init(width: side, height: side)
    }
}
let a = Size(square: 3)          // from the extension
let b = Size(width: 2, height: 5) // memberwise, still available
```

# --instructions--

Completa la extensión para que `Point(both: 2.5)` cree un punto con `x` e `y` iguales a `2.5`, de modo que el programa imprima `2.5 2.5` y `1.0 2.0`

# --seed--

```swift
struct Point {
    var x: Double
    var y: Double
}
extension Point {
    [/](both: Double) {
        [/](x: both, y: both)
    }
}
let p = Point(both: 2.5)
let q = Point(x: 1, y: 2)
print(p.x, p.y)
print(q.x, q.y)
```

# --answers--

- init
- self.init
- func
- Point
- self

# --solutions--

```swift
struct Point {
    var x: Double
    var y: Double
}
extension Point {
    init(both: Double) {
        self.init(x: both, y: both)
    }
}
let p = Point(both: 2.5)
let q = Point(x: 1, y: 2)
print(p.x, p.y)
print(q.x, q.y)
```

# --output--

2.5 2.5
1.0 2.0
