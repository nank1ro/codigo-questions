---
language: swift
exerciseType: 1
---

# --description--

Los métodos estáticos en las extensiones son un buen hogar para pequeñas funciones de fábrica que construyen un valor del tipo. `String(repeating:count:)` es el inicializador estándar que repite un fragmento de texto un número de veces:
```swift
extension String {
    static func dashes(_ count: Int) -> String {
        return String(repeating: "-", count: count)
    }
}
print(String.dashes(4)) // ----
```

# --instructions--

Escribe una extensión de `String` con un método estático `stars(_ count: Int)` que devuelva una cadena hecha de `count` asteriscos `*`

Ejemplo de llamada al método:
```swift
print(String.stars(3))
// prints ***
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Debes usar una extensión de `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

El método debe ser `static`

```json
{
  "regex": "static\\s+func\\s+stars",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`String.stars(3)` debe devolver `"***"`

```swift
tryCatch(String.stars(3) == "***")
```

`String.stars(1)` debe devolver `"*"`

```swift
tryCatch(String.stars(1) == "*")
```

`String.stars(0)` debe devolver una cadena vacía

```swift
tryCatch(String.stars(0) == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    static func stars(_ count: Int) -> String {
        return String(repeating: "*", count: count)
    }
}
```
