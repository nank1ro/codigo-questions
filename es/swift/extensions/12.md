---
language: swift
exerciseType: 1
---

# --description--

`Array` es un tipo genérico: `[Int]` y `[String]` son ambos arrays, con un tipo **`Element`** distinto. Una extensión de `Array` se aplica a todos ellos, lo cual es un problema cuando el miembro nuevo solo tiene sentido para algunos elementos: no puedes sumar números que son cadenas.
Una cláusula `where` restringe la extensión a los arrays cuyo `Element` es de un tipo dado:
```swift
extension Array where Element == Int {
    var largest: Int {
        var result = Int.min
        for number in self {
            if number > result {
                result = number
            }
        }
        return result
    }
}
print([3, 9, 2].largest) // 9
```
`[3, 9, 2].largest` funciona, mientras que `["a", "b"].largest` es un error de compilación: la propiedad no existe en `[String]`.

# --instructions--

Escribe una extensión de `Array` restringida a `Element == Int` con un método `sum()` que devuelva la suma de los elementos. No uses `reduce`

Ejemplo de llamada al método:
```swift
print([1, 2, 3].sum())
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Debes usar una extensión de `Array` con una cláusula `where Element == Int`

```json
{
  "regex": "extension\\s+Array\\s+where\\s+Element\\s*==\\s*Int",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

No debes usar `reduce`

```json
{
  "regex": "reduce",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`[1, 2, 3].sum()` debe devolver `6`

```swift
tryCatch([1, 2, 3].sum() == 6)
```

`[10, -4].sum()` debe devolver `6`

```swift
tryCatch([10, -4].sum() == 6)
```

`[7].sum()` debe devolver `7`

```swift
tryCatch([7].sum() == 7)
```

La suma de un array vacío debe ser `0`

```swift
let _empty: [Int] = []
tryCatch(_empty.sum() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension Array where Element == Int {
    func sum() -> Int {
        var total = 0
        for number in self {
            total += number
        }
        return total
    }
}
```
