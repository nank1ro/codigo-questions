---
language: swift
exerciseType: 1
---

# --description--

Las propiedades calculadas también encajan de forma natural en las extensiones de `String`. El método `reversed()` devuelve los caracteres en orden inverso, y `String(...)` los convierte de nuevo en una cadena:
```swift
extension String {
    var backwards: String {
        return String(reversed())
    }
}
print("swift".backwards) // tfiws
```
Como con los métodos, `reversed()` dentro de la extensión significa `self.reversed()`.

# --instructions--

Escribe una extensión de `String` con una propiedad calculada `isPalindrome` de tipo `Bool`, que sea `true` cuando la cadena se lee igual de izquierda a derecha que al revés. La comparación distingue mayúsculas de minúsculas

Ejemplo de uso:
```swift
print("level".isPalindrome)
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Debes usar una extensión de `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`"level".isPalindrome` debe ser `true`

```swift
tryCatch("level".isPalindrome == true)
```

`"swift".isPalindrome` debe ser `false`

```swift
tryCatch("swift".isPalindrome == false)
```

`"Anna".isPalindrome` debe ser `false` (la comparación distingue mayúsculas de minúsculas)

```swift
tryCatch("Anna".isPalindrome == false)
```

`"".isPalindrome` debe ser `true`

```swift
tryCatch("".isPalindrome == true)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    var isPalindrome: Bool {
        return self == String(reversed())
    }
}
```
