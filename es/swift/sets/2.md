---
language: swift
exerciseType: 1
---

# --description--

Al igual que los arrays, los sets pueden ser constantes (`let`) o variables (`var`). Solo un set `var` puede modificarse después de ser creado.
Para crear un set vacío, llamas al inicializador del tipo, porque un literal vacío `[]` por sí solo no le indicaría a Swift qué tipo de elemento usar:
```swift
var visited = Set<String>()
print(visited.isEmpty) // true
```
La propiedad `isEmpty` es `true` cuando el set no tiene elementos, igual que con los arrays.

# --instructions--

Declara un set constante de cadenas llamado `colors` que contenga `"red"`, `"green"` y `"blue"`, y un set variable vacío de enteros llamado `scores`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`colors` debe contener exactamente `"red"`, `"green"` y `"blue"`

```swift
tryCatch(colors == ["red", "green", "blue"])
```

`colors` debe ser un `Set<String>`

```swift
tryCatch(type(of: colors) == Set<String>.self)
```

`scores` debe estar vacío

```swift
tryCatch(scores.isEmpty)
```

`scores` debe ser un `Set<Int>`

```swift
tryCatch(type(of: scores) == Set<Int>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let colors: Set<String> = ["red", "green", "blue"]
var scores = Set<Int>()
```
