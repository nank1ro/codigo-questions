---
language: swift
exerciseType: 2
---

# --description--

Los sets y los arrays se convierten fácilmente entre sí.
Pasar un array a `Set(...)` crea un set a partir de sus elementos, lo cual es la forma más rápida de **eliminar duplicados**:
```swift
let votes = [3, 1, 3, 2, 1]
let unique = Set(votes) // {1, 2, 3} in some order
```
Pasar un set a `Array(...)` devuelve un array, pero como un set no tiene orden, los elementos salen en una secuencia impredecible.
Por eso, cuando necesitas un resultado ordenado, normalmente llamas a `sorted()` sobre el set, que ya devuelve un array:
```swift
let ordered = unique.sorted() // [1, 2, 3]
```

# --instructions--

Elimina los duplicados de `votes`, luego imprime cuántos valores distintos hay y los valores distintos en orden ascendente

# --seed--

```swift
let votes = [3, 1, 3, 2, 1]
let unique = [/](votes)
print(unique.[/])
print(unique.sorted())
```

# --answers--

- Set
- Array
- count
- length
- size

# --solutions--

```swift
let votes = [3, 1, 3, 2, 1]
let unique = Set(votes)
print(unique.count)
print(unique.sorted())
```

# --output--

3
[1, 2, 3]
