---
language: swift
exerciseType: 2
---

# --instructions--

Imprime, en orden ascendente, los días en los que no hay reunión

# --seed--

```swift
let weekdays: Set<String> = ["mon", "tue", "wed"]
let meetings: Set<String> = ["tue", "sat"]
let free = weekdays.[/](meetings)
print(free.sorted())
```

# --answers--

- subtracting
- subtract
- intersection
- union
- symmetricDifference

# --solutions--

```swift
let weekdays: Set<String> = ["mon", "tue", "wed"]
let meetings: Set<String> = ["tue", "sat"]
let free = weekdays.subtracting(meetings)
print(free.sorted())
```

# --output--

["mon", "wed"]
