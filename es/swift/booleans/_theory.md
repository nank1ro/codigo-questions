Swift tiene un tipo Boolean básico, llamado `Bool`.
Los valores booleanos se denominan lógicos porque solo pueden ser verdaderos o falsos.
Puedes evaluar cualquier expresión en Swift y obtener una de dos respuestas, `true` o `false`.

---

We puede almacenar el booleano valor `verdadero` en un variable solo like un numero o un cadena.

---

 The opposite value of `true` is `false` 
