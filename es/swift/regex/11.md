---
language: swift
exerciseType: 2
---

# --description--

Contar paréntesis se vuelve frágil en cuanto un patrón crece. Dale al grupo un **nombre** en su lugar, escribiendo `?<nombre>` justo después de su paréntesis de apertura, y léelo como una propiedad de la coincidencia:
```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>\d+)/#) {
    print(m.key)   // size
    print(m.value) // 10
}
```
Los grupos con nombre siguen numerados, así que `m.1` sigue funcionando, pero `m.key` dice qué contiene y sobrevive a un cambio en el patrón.

# --instructions--

Completa el código para que el grupo de valor coincida con dígitos y la clave se imprima antes que él, produciendo la salida `size 10`

# --seed--

```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>[/]+)/#) {
    print(m.[/], m.value)
}
```

# --answers--

- \d
- \s
- [A-Z]
- key
- name
- 0

# --solutions--

```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>\d+)/#) {
    print(m.key, m.value)
}
```

# --output--

size 10
