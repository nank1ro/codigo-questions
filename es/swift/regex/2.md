---
language: swift
exerciseType: 1
---

# --description--

Unas pocas abreviaturas cubren la mayoría de los patrones. Cada una coincide exactamente con **un** carácter:
- `\d` es un dígito
- `\w` es una letra, un dígito o un guion bajo
- `\s` es un espacio, un tabulador o un salto de línea
- `.` es cualquier carácter suelto

Para coincidir con más de un carácter, añade un **cuantificador** justo después del patrón:
- `+` significa uno o más
- `*` significa cero o más
- `?` significa cero o uno

Así que `\w+` es una palabra, `\s*` es un espaciado opcional y `\d?` es un dígito opcional:
```swift
print("hello world".contains(#/\w+\s\w+/#)) // true
print("hello".contains(#/\w+\s\w+/#))       // false
```
Un carácter sin significado especial simplemente se representa a sí mismo, así que `#/cat/#` coincide con las tres letras `cat`.

# --instructions--

Escribe una función `hasDigit` que reciba un texto (`String`) sin etiqueta de argumento y devuelva `true` cuando el texto contenga al menos un dígito, usando una regex

Ejemplo de llamada a la función:
```swift
print(hasDigit("abc1"))
// imprime true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func hasDigit() {

}
```

# --asserts--

La función debe usar un literal de regex escrito con `#/`

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`hasDigit("abc1")` debe devolver `true`

```swift
tryCatch(hasDigit("abc1") == true)
```

`hasDigit("7")` debe devolver `true`

```swift
tryCatch(hasDigit("7") == true)
```

`hasDigit("abc")` debe devolver `false`

```swift
tryCatch(hasDigit("abc") == false)
```

`hasDigit("")` debe devolver `false`

```swift
tryCatch(hasDigit("") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func hasDigit(_ text: String) -> Bool {
    return text.contains(#/\d/#)
}
```
