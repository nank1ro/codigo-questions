---
language: swift
exerciseType: 1
---

# --description--

`m.0` no es un `String` sino un `Substring`: una vista del texto original, no una copia. Se imprime exactamente como una cadena, pero donde se requiere un `String` tienes que convertirlo:
```swift
let text = "order 42"
if let m = text.firstMatch(of: #/\d+/#) {
    let found: String = String(m.0)
    print(found) // 42
}
```
Los inicializadores de números aceptan un `Substring` directamente, así que `Int(m.0)` funciona sin el rodeo.

# --instructions--

Escribe una función `firstNumber` que reciba un texto (`String`) sin etiqueta de argumento y devuelva la primera serie de dígitos como un `String`, o `"none"` cuando el texto no contenga ningún dígito

Ejemplo de llamada a la función:
```swift
print(firstNumber("order 42 today 7"))
// imprime 42
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func firstNumber() {

}
```

# --asserts--

La función debe usar `firstMatch(of:`

```json
{
  "regex": "firstMatch\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstNumber("order 42 today 7")` debe devolver `"42"`

```swift
tryCatch(firstNumber("order 42 today 7") == "42")
```

`firstNumber("a1")` debe devolver `"1"`

```swift
tryCatch(firstNumber("a1") == "1")
```

`firstNumber("100")` debe devolver `"100"`

```swift
tryCatch(firstNumber("100") == "100")
```

`firstNumber("abc")` debe devolver `"none"`

```swift
tryCatch(firstNumber("abc") == "none")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstNumber(_ text: String) -> String {
    if let m = text.firstMatch(of: #/\d+/#) {
        return String(m.0)
    }
    return "none"
}
```
