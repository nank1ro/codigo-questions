---
language: swift
exerciseType: 4
---

# --description--

`matches(of:)` devuelve **todas** las coincidencias en lugar de la primera, como un array. El array nunca es `nil`: cuando nada coincide simplemente está vacío, así que se puede recorrer o transformar directamente:
```swift
let text = "a1 b22"
print(text.matches(of: #/\d+/#).count) // 2
```
Cada elemento es una coincidencia, así que `$0.0` dentro de un `map` es el texto coincidente:
```swift
let found = text.matches(of: #/\d+/#).map { String($0.0) }
print(found) // ["1", "22"]
```

# --instructions--

Ordena los elementos para construir un programa que reúna cada serie de dígitos de `text` en `found` y lo imprima

# --answers--

```swift
let text = "a1 b22 c333"
```

```swift
let pattern = #/\d+/#
```

```swift
let found = text.matches(of: pattern).map { String($0.0) }
```

```swift
print(found)
```

# --solutions--

```swift
let text = "a1 b22 c333"
let pattern = #/\d+/#
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

```swift
let pattern = #/\d+/#
let text = "a1 b22 c333"
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

# --output--

["1", "22", "333"]
