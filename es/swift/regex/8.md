---
language: swift
exerciseType: 1
---

# --description--

Como `Int(_:)` acepta un `Substring`, convertir el texto encontrado en números es un solo paso. `compactMap` resulta útil aquí: descarta los valores que vuelven como `nil`:
```swift
let text = "a1 b22"
let numbers = text.matches(of: #/\d+/#).compactMap { Int($0.0) }
print(numbers) // [1, 22]
```
Usa `map` cuando todos los elementos se conviertan, `compactMap` cuando alguno pueda fallar.

# --instructions--

Escribe una función `numbers` que reciba un texto (`String`) sin etiqueta de argumento y devuelva cada serie de dígitos que contenga como un array de `Int`, en el orden en que aparecen

Ejemplo de llamada a la función:
```swift
print(numbers("a1 b22 c333"))
// prints [1, 22, 333]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func numbers() {

}
```

# --asserts--

La función debe usar `matches(of:`

```json
{
  "regex": "matches\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`numbers("a1 b22 c333")` debe devolver `[1, 22, 333]`

```swift
tryCatch(numbers("a1 b22 c333") == [1, 22, 333])
```

`numbers("7")` debe devolver `[7]`

```swift
tryCatch(numbers("7") == [7])
```

`numbers("abc")` debe devolver `[]`

```swift
tryCatch(numbers("abc") == [])
```

`numbers("10-20")` debe devolver `[10, 20]`

```swift
tryCatch(numbers("10-20") == [10, 20])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func numbers(_ text: String) -> [Int] {
    return text.matches(of: #/\d+/#).compactMap { Int($0.0) }
}
```
