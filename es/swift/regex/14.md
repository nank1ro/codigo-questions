---
language: swift
exerciseType: 4
---

# --description--

Validar un texto completo con `^` y `$` funciona, pero `wholeMatch(of:)` lo dice directamente: devuelve una coincidencia solo cuando el patrón cubre el texto desde el primer carácter hasta el último, y `nil` en caso contrario:
```swift
print("1a2b".wholeMatch(of: #/[0-9a-f]+/#) != nil) // true
print("1z".wholeMatch(of: #/[0-9a-f]+/#) != nil)   // false
```
Usa `firstMatch(of:)` para encontrar algo dentro de un texto, y `wholeMatch(of:)` para comprobar que un texto tiene una forma exacta.

# --instructions--

Ordena los elementos para construir un programa que defina `isHex` antes del bucle e imprima el resultado para cada valor de `values`

# --answers--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
```

```swift
let values = ["1a2b", "1z", "ff"]
```

```swift
for value in values {
    print(isHex(value))
}
```

# --solutions--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
let values = ["1a2b", "1z", "ff"]
for value in values {
    print(isHex(value))
}
```

```swift
let values = ["1a2b", "1z", "ff"]
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
for value in values {
    print(isHex(value))
}
```

# --output--

true
false
true
