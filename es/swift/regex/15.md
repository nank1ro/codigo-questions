---
language: swift
exerciseType: 1
---

# --description--

Un literal `#/ ... /#` es fijo al compilar. Cuando el patrón solo se conoce en tiempo de ejecución, por ejemplo porque lo escribió un usuario, constrúyelo con `Regex(_:)`:
```swift
let regex = try Regex("[0-9]+")
print("abc123".contains(regex)) // true
```
Este inicializador **lanza** errores: un patrón inválido como `"["` solo se descubre mientras el programa se ejecuta, así que la llamada necesita `try`, y el error debe manejarse con `do`/`catch` (o `try?`) o propagarse marcando la función que lo rodea como `throws`, como hace este ejercicio. Una regex construida así no tiene propiedades numeradas conocidas en tiempo de compilación, pero `contains`, `matches(of:)` y `replacing` funcionan exactamente igual que antes.

# --instructions--

Escribe una función `countMatches` que reciba un texto (`String`) y un patrón (`String`), ambos sin etiquetas de argumento, esté marcada como `throws` y devuelva cuántas veces se encuentra el patrón en el texto. Un patrón inválido debe dejar salir el error

Ejemplo de llamada a la función:
```swift
print(try countMatches("a1 b22", "[0-9]+"))
// imprime 2
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func countMatches() {

}
```

# --asserts--

La función debe construir la regex con `try Regex(`

```json
{
  "regex": "try\\s+Regex\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`countMatches("a1 b22", "[0-9]+")` debe devolver `2`

```swift
tryCatch((try? countMatches("a1 b22", "[0-9]+")) == 2)
```

`countMatches("abc", "[0-9]+")` debe devolver `0`

```swift
tryCatch((try? countMatches("abc", "[0-9]+")) == 0)
```

`countMatches("hello world", "o")` debe devolver `2`

```swift
tryCatch((try? countMatches("hello world", "o")) == 2)
```

`countMatches("abc", "[")` debe lanzar un error

```swift
tryCatch((try? countMatches("abc", "[")) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func countMatches(_ text: String, _ pattern: String) throws -> Int {
    let regex = try Regex(pattern)
    return text.matches(of: regex).count
}
```
