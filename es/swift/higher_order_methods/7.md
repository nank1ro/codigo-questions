---
language: swift
exerciseType: 1
---

# --description--

Las funciones de orden superior funcionan con cualquier array, incluidos los arrays de tus propios structs. Encadenar `filter` y luego `map` es la forma habitual de seleccionar algunos elementos y extraer un valor de cada uno:
```swift
struct Book {
    var title: String
    var pages: Int
}
let books = [Book(title: "Dune", pages: 412), Book(title: "Haiku", pages: 40)]
let long = books.filter { $0.pages > 100 }.map { $0.title }
print(long) // ["Dune"]
```
Hacerlo en el orden contrario, `map` y luego `filter`, perdería la propiedad `pages` antes de que la comprobación pudiera usarla.

# --instructions--

El struct `Product` está dado. Escribe una función `cheapNames` que reciba un array de `Product` (omite la etiqueta del argumento) y devuelva los nombres de los productos cuyo `price` sea inferior a `10`, en el orden original, encadenando `filter` y `map`

Ejemplo de llamada a la función:
```swift
print(cheapNames([Product(name: "Pen", price: 2.5), Product(name: "Bag", price: 30)]))
// prints ["Pen"]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Product {
    var name: String
    var price: Double
}
```

# --asserts--

Debes usar `filter`

```json
{
  "regex": "\\.filter",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Debes usar `map`

```json
{
  "regex": "\\.map",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`cheapNames` debe devolver `["Pen"]` para un bolígrafo a `2.5` y una bolsa a `30`

```swift
tryCatch(cheapNames([Product(name: "Pen", price: 2.5), Product(name: "Bag", price: 30)]) == ["Pen"])
```

`cheapNames` debe mantener el orden original

```swift
tryCatch(cheapNames([Product(name: "Tea", price: 4), Product(name: "Cup", price: 9.99), Product(name: "Pot", price: 25)]) == ["Tea", "Cup"])
```

Un producto con un precio exactamente de `10` debe excluirse

```swift
tryCatch(cheapNames([Product(name: "Cap", price: 10)]) == [])
```

`cheapNames([])` debe devolver `[]`

```swift
tryCatch(cheapNames([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Product {
    var name: String
    var price: Double
}
func cheapNames(_ products: [Product]) -> [String] {
    return products.filter { $0.price < 10 }.map { $0.name }
}
```
