---
language: swift
exerciseType: 1
---

# --description--

`flatMap` es para los cierres que devuelven un **array**: en lugar de construir un array de arrays, une todos los arrays devueltos en un único resultado plano:
```swift
let teams = [["Ann", "Bob"], ["Cid"]]
print(teams.map { $0 })     // [["Ann", "Bob"], ["Cid"]]
print(teams.flatMap { $0 }) // ["Ann", "Bob", "Cid"]
```
El cierre también puede transformar cada array interno antes de aplanarlo, por ejemplo `teams.flatMap { $0.reversed() }` da `["Bob", "Ann", "Cid"]`.

# --instructions--

Escribe una función `flatten` que reciba un array de arrays de `Int` (omite la etiqueta del argumento) y devuelva un único array con todos los números en orden, usando `flatMap`

Ejemplo de llamada a la función:
```swift
print(flatten([[1, 2], [3]]))
// prints [1, 2, 3]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Debes usar `flatMap`

```json
{
  "regex": "\\.flatMap",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`flatten([[1, 2], [3]])` debe devolver `[1, 2, 3]`

```swift
tryCatch(flatten([[1, 2], [3]]) == [1, 2, 3])
```

`flatten([[5], [], [6, 7]])` debe devolver `[5, 6, 7]`

```swift
tryCatch(flatten([[5], [], [6, 7]]) == [5, 6, 7])
```

`flatten([])` debe devolver `[]`

```swift
tryCatch(flatten([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func flatten(_ lists: [[Int]]) -> [Int] {
    return lists.flatMap { $0 }
}
```
