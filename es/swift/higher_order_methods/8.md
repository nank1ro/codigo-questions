---
language: swift
exerciseType: 2
---

# --description--

Cuando un cierre solo lee una propiedad, puedes pasar en su lugar un **key path**: `\.name` significa "la propiedad `name` del elemento", y `map(\.name)` es lo mismo que `map { $0.name }`.
Ordenar por una propiedad usa el cierre habitual de dos argumentos, comparando esa propiedad en ambos elementos:
```swift
struct City {
    var name: String
    var population: Int
}
let cities = [City(name: "Oslo", population: 700), City(name: "Rome", population: 2800)]
let byPopulation = cities.sorted { $0.population > $1.population }
print(byPopulation.map(\.name)) // ["Rome", "Oslo"]
```

# --instructions--

Completa el código para ordenar los jugadores de la puntuación más alta a la más baja e imprimir sus nombres con un key path, de modo que el programa imprima `["Mia", "Leo", "Zoe"]`

# --seed--

```swift
struct Player {
    var name: String
    var score: Int
}
let players = [Player(name: "Leo", score: 40), Player(name: "Mia", score: 75), Player(name: "Zoe", score: 12)]
let ranking = players.sorted { $0.score [/] $1.score }
print(ranking.map([/]))
```

# --answers--

- >
- \.name
- <
- \.score
- $0.name

# --solutions--

```swift
struct Player {
    var name: String
    var score: Int
}
let players = [Player(name: "Leo", score: 40), Player(name: "Mia", score: 75), Player(name: "Zoe", score: 12)]
let ranking = players.sorted { $0.score > $1.score }
print(ranking.map(\.name))
```

# --output--

["Mia", "Leo", "Zoe"]
