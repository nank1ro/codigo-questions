---
language: swift
exerciseType: 4
---

# --description--

`contains(where:)` y `allSatisfy` reemplazan el patrón común de un bucle con una variable de bandera. Ambos se detienen en cuanto se conoce la respuesta: `contains(where:)` en la primera coincidencia, `allSatisfy` en el primer elemento que falla.
```swift
let ages = [15, 22, 40]
let anyMinor = ages.contains { $0 < 18 }  // true
let allAdults = ages.allSatisfy { $0 >= 18 } // false
```
`contains { ... }` es la forma de cierre final de `contains(where:)`, que no debe confundirse con `contains(_:)`, que busca un valor específico.

# --instructions--

Ordena los elementos para comprobar si todas las puntuaciones son al menos `60` y si alguna puntuación es exactamente `100`, y luego imprime ambos resultados en una línea.
Declara `scores` primero e imprime al final

# --answers--

```swift
let scores = [72, 100, 64]
```

```swift
let allPassed = scores.allSatisfy { $0 >= 60 }
```

```swift
let anyPerfect = scores.contains { $0 == 100 }
```

```swift
print("\(allPassed) \(anyPerfect)")
```

# --solutions--

```swift
let scores = [72, 100, 64]
let allPassed = scores.allSatisfy { $0 >= 60 }
let anyPerfect = scores.contains { $0 == 100 }
print("\(allPassed) \(anyPerfect)")
```

```swift
let scores = [72, 100, 64]
let anyPerfect = scores.contains { $0 == 100 }
let allPassed = scores.allSatisfy { $0 >= 60 }
print("\(allPassed) \(anyPerfect)")
```

# --output--

true true
