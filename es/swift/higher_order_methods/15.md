---
language: swift
exerciseType: 1
---

# --description--

Puedes escribir tus propias funciones de orden superior. Una función que recibe un cierre y **devuelve un nuevo cierre** construido a partir de él es un patrón común: el cierre devuelto captura el original, así que el parámetro debe ser `@escaping`.
Por ejemplo, `negate` convierte un predicado en su opuesto, listo para pasarse a `filter`:
```swift
func negate(_ predicate: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { !predicate($0) }
}
let isEven: (Int) -> Bool = { $0 % 2 == 0 }
print([1, 2, 3, 4].filter(negate(isEven))) // [1, 3]
```
Ten en cuenta que `filter(negate(isEven))` pasa el cierre como un argumento normal, sin la sintaxis de cierre final.

# --instructions--

Escribe una función `both` que reciba dos cierres de tipo `(Int) -> Bool` (omite ambas etiquetas de argumento) y devuelva un cierre del mismo tipo que sea `true` solo cuando ambos sean `true` para el mismo argumento

Ejemplo de llamada a la función:
```swift
let smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
print([4, 12, 7, 8].filter(smallEven))
// prints [4, 8]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`both({ $0 < 10 }, { $0 % 2 == 0 })` debe ser `true` para `4`

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(4) == true)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })` debe ser `false` para `12`

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(12) == false)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })` debe ser `false` para `7`

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(7) == false)
```

El cierre devuelto debe funcionar con `filter`

```swift
let _smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
tryCatch([4, 12, 7, 8].filter(_smallEven) == [4, 8])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func both(_ first: @escaping (Int) -> Bool, _ second: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { first($0) && second($0) }
}
```
