---
language: swift
exerciseType: 1
---

# --description--

Algunas funciones de orden superior responden una pregunta sobre la colección en lugar de transformarla. Todas reciben un cierre que devuelve un `Bool`:
- `first(where:)` devuelve el primer elemento que satisface el cierre, o `nil` si no hay ninguno
- `contains(where:)` devuelve `true` si al menos un elemento lo satisface
- `allSatisfy` devuelve `true` si todos los elementos lo satisfacen

```swift
let nums = [4, 9, 16]
print(nums.first(where: { $0 > 5 }))  // Optional(9)
print(nums.contains(where: { $0 > 5 })) // true
print(nums.allSatisfy { $0 > 5 })       // false
```
A diferencia de `filter`, `first(where:)` se detiene en la primera coincidencia y no construye un nuevo array.

# --instructions--

Escribe una función `firstLong` que reciba un array de `String` y un `Int` llamado `minLength` (omite ambas etiquetas de argumento) y devuelva la primera cadena con al menos `minLength` caracteres, o `nil` si no hay ninguna, usando `first(where:)`

Ejemplo de llamada a la función:
```swift
print(firstLong(["hi", "hello", "hey"], 4))
// prints Optional("hello")
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Debes usar `first(where:)`

```json
{
  "regex": "\\.first\\s*\\(\\s*where",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstLong(["hi", "hello", "hey"], 4)` debe devolver `"hello"`

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 4) == "hello")
```

`firstLong(["hi", "hello", "hey"], 2)` debe devolver `"hi"`

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 2) == "hi")
```

`firstLong(["hi", "hey"], 4)` debe devolver `nil`

```swift
tryCatch(firstLong(["hi", "hey"], 4) == nil)
```

`firstLong([], 1)` debe devolver `nil`

```swift
tryCatch(firstLong([], 1) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstLong(_ words: [String], _ minLength: Int) -> String? {
    return words.first(where: { $0.count >= minLength })
}
```
