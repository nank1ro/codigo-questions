---
language: swift
exerciseType: 1
---

# --description--

`map` y `filter` sobre un array son **ansiosos**: cada uno procesa todo el array y construye uno nuevo antes de que se ejecute el siguiente paso. En una colección grande, o cuando solo necesitas el primer resultado, eso es trabajo desperdiciado.
La propiedad `lazy` devuelve una vista cuyas operaciones se ejecutan solo cuando se solicita realmente un elemento, de uno en uno a través de toda la cadena:
```swift
let firstBig = (1...1000).lazy.map { $0 * $0 }.first { $0 > 50 }
print(firstBig!) // 64
```
Aquí solo `1, 2, ..., 8` se elevan al cuadrado: `first(where:)` pide elementos hasta que uno satisface la condición, y la cadena se detiene ahí. Sin `lazy`, `map` elevaría al cuadrado los 1000 números primero.

# --instructions--

Escribe una función `firstCubeAbove` que reciba un `Int` llamado `limit` (omite la etiqueta del argumento) y devuelva el cubo más pequeño (`n * n * n` para `n` empezando desde `1`) mayor que `limit`, usando `lazy` y `map` sobre el rango `1...limit + 1` y `first(where:)`

Ejemplo de llamada a la función:
```swift
print(firstCubeAbove(10))
// prints 27
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Debes usar `lazy`

```json
{
  "regex": "\\.lazy",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

No debes usar un bucle

```json
{
  "regex": "\\b(for|while)\\b",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`firstCubeAbove(10)` debe devolver `27`

```swift
tryCatch(firstCubeAbove(10) == 27)
```

`firstCubeAbove(27)` debe devolver `64`

```swift
tryCatch(firstCubeAbove(27) == 64)
```

`firstCubeAbove(0)` debe devolver `1`

```swift
tryCatch(firstCubeAbove(0) == 1)
```

`firstCubeAbove(1000000)` debe devolver `1030301`

```swift
tryCatch(firstCubeAbove(1000000) == 1030301)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstCubeAbove(_ limit: Int) -> Int {
    return (1...limit + 1).lazy.map { $0 * $0 * $0 }.first(where: { $0 > limit })!
}
```
