---
language: swift
exerciseType: 2
---

# --description--

`zip` empareja los elementos de dos secuencias posición por posición, produciendo una secuencia de tuplas. Se detiene al final de la más corta:
```swift
let names = ["Ann", "Bob"]
let ages = [31, 27, 99]
let pairs = zip(names, ages).map { "\($0) is \($1)" }
print(pairs) // ["Ann is 31", "Bob is 27"]
```
Dentro del cierre `$0` es el elemento de la primera secuencia y `$1` el de la segunda. `zip` es una función libre, no un método: escribes `zip(a, b)`, no `a.zip(b)`.

# --instructions--

Completa el código para emparejar cada ciudad con su temperatura y conservar solo las ciudades con más de `20` grados, de modo que el programa imprima `["Rome", "Cairo"]`

# --seed--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = [/](cities, temps).[/] { $1 > 20 }.map { $0.0 }
print(warm)
```

# --answers--

- zip
- filter
- map
- enumerated
- compactMap

# --solutions--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = zip(cities, temps).filter { $1 > 20 }.map { $0.0 }
print(warm)
```

# --output--

["Rome", "Cairo"]
