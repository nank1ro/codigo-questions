---
language: swift
exerciseType: 1
---

# --description--

`Dictionary(grouping:by:)` divide una colección en un diccionario de arrays. El cierre calcula la **clave** de cada elemento, y todos los elementos con la misma clave acaban en el mismo array:
```swift
let words = ["apple", "bee", "avocado"]
let byInitial = Dictionary(grouping: words, by: { $0.first! })
print(byInitial["a"]!) // ["apple", "avocado"]
```
`mapValues` transforma cada valor de un diccionario manteniendo las claves, así que es el siguiente paso natural después de agrupar:
```swift
let sizes = byInitial.mapValues { $0.count }
print(sizes["a"]!) // 2
```

# --instructions--

Escribe una función `countByLength` que reciba un array de `String` (omite la etiqueta del argumento) y devuelva un diccionario de tipo `[Int: Int]` que asocie cada longitud de palabra con cuántas palabras tienen esa longitud, usando `Dictionary(grouping:by:)` y `mapValues`

Ejemplo de llamada a la función:
```swift
print(countByLength(["hi", "yo", "hey"])[2]!)
// prints 2
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Debes usar `Dictionary(grouping:by:)`

```json
{
  "regex": "Dictionary\\s*\\(\\s*grouping",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Debes usar `mapValues`

```json
{
  "regex": "\\.mapValues",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`countByLength(["hi", "yo", "hey"])` debe devolver `[2: 2, 3: 1]`

```swift
tryCatch(countByLength(["hi", "yo", "hey"]) == [2: 2, 3: 1])
```

`countByLength(["a", "bb", "cc", "dd"])` debe devolver `[1: 1, 2: 3]`

```swift
tryCatch(countByLength(["a", "bb", "cc", "dd"]) == [1: 1, 2: 3])
```

`countByLength([])` debe devolver un diccionario vacío

```swift
tryCatch(countByLength([]) == [:])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func countByLength(_ words: [String]) -> [Int: Int] {
    return Dictionary(grouping: words, by: { $0.count }).mapValues { $0.count }
}
```
