---
language: swift
exerciseType: 2
---

# --description--

En Swift podemos recorrer un array de una manera muy simple usando las palabras clave `for..in`:
```swift
var numbers = [1, 2, 3]
for num in numbers {
    print(num)
}
// prints 1, 2, 3
```
Un nombre de variable sigue a la palabra clave `for`, se le asignará el valor de cada elemento del array a su vez.

# --instructions--

Imprime cada elemento del array multiplicándolo por `2`

# --seed--

```swift
var numbers = [2, 4, 6]
[/][/][/][/]{
    print([/][/][/])
}
```

# --answers--

- for 
- num 
- in 
- numbers 
- num 
- * 
- ** 
- 2

# --solutions--

```swift
var numbers = [2, 4, 6]
for num in numbers {
    print(num * 2)
}
```

# --output--

4
8
12
