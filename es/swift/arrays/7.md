---
language: swift
exerciseType: 2
---

# --description--

In Swift we puede slice un arreglo como we querer!
```swift
// Grabs el primero two elementos
listName[..<2]
// Grabs the fourth through ultimo elementos
listName[3...]
```
si your arreglo slice incluye el muy primero o ultimo elemento en un arreglo, el indice para ese elemento doesn't tiene un ser included

# --instructions--

Divide el `animals` exactly en half within el dos groups

# --seed--

```swift
let animals = ["dog", "cat", "frog", "bear", "lion", "butterfly"]
let firstGroup = [/][[/][/]]
let secondGroup = [/][[/][/]]
print(firstGroup)
print(secondGroup)
```

# --answers--

- 3
- 4
- 1
- 3
- ..<
- ...
- animals
- animals

# --solutions--

```swift
let animals = ["dog", "cat", "frog", "bear", "lion", "butterfly"]
let firstGroup = animals[..<3]
let secondGroup = animals[3...]
print(firstGroup)
print(secondGroup)
```

# --output--

["dog", "cat", "frog"]
["bear", "lion", "butterfly"]
