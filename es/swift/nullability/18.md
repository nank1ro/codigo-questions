---
language: swift
exerciseType: 1
---

# --description--

A veces quieres transformar el valor dentro de un opcional y mantener el resultado opcional, sin desenvolver y volver a envolver a mano.
Los opcionales tienen un método `map`: aplica la clausura al valor si existe alguno, y devuelve `nil` en caso contrario.
```swift
let score: Int? = 10
let doubled = score.map { $0 * 2 } // Int? que contiene 20
let missing: Int? = nil
let stillMissing = missing.map { $0 * 2 } // nil
```
Combinado con una conversión que puede fallar, forma un pipeline compacto: `Int(text).map { $0 + 1 }`.

# --instructions--

Escribe una función `squareText` que reciba un `String` (omite la etiqueta del argumento), lo convierta a `Int` y devuelva su cuadrado como `Int?`, o `nil` cuando la conversión falle.
Usa `map`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`squareText("4")` debe devolver `16`

```swift
tryCatch(squareText("4") == 16)
```

`squareText("12")` debe devolver `144`

```swift
tryCatch(squareText("12") == 144)
```

`squareText("x")` debe devolver `nil`

```swift
tryCatch(squareText("x") == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func squareText(_ text: String) -> Int? {
    return Int(text).map { $0 * $0 }
}
```
