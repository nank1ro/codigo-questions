---
language: swift
exerciseType: 4
---

# --description--

Enlazar varios opcionales en un solo `if let` mantiene el código plano: una única rama `else` cubre cualquier valor faltante.

# --instructions--

Ordena los elementos para imprimir `Area: 20` cuando tanto `width` como `height` tengan un valor, y `Missing size` en caso contrario.
Declara `width` antes que `height`

# --answers--

```swift
let width: Int? = 4
```

```swift
let height: Int? = 5
```

```swift
if let w = width, let h = height {
```

```swift
    print("Area: \(w * h)")
```

```swift
} else {
    print("Missing size")
}
```

# --solutions--

```swift
let width: Int? = 4
let height: Int? = 5
if let w = width, let h = height {
    print("Area: \(w * h)")
} else {
    print("Missing size")
}
```

# --output--

Area: 20
