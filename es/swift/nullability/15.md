---
language: swift
exerciseType: 1
---

# --description--

Muchas operaciones pueden fallar, y Swift informa del fallo devolviendo un opcional.
Convertir texto en un número es el ejemplo clásico: `Int("42")` devuelve un `Int?` que contiene `42`, mientras que `Int("abc")` devuelve `nil`.
`Int("3.5")` también es `nil`, porque el texto no es un número entero; usa `Double("3.5")` para decimales.
```swift
let typed = "42"
if let number = Int(typed) {
    print(number + 1) // 43
}
```
Otros ejemplos son `array.first` (`nil` para un array vacío) y `dictionary[key]` (`nil` cuando falta la clave).

# --instructions--

Escribe una función `parseAge` que reciba un `String` (omite la etiqueta del argumento) y lo devuelva convertido a `Int`, o `-1` cuando el texto no sea un número entero válido

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`parseAge("42")` debe devolver `42`

```swift
tryCatch(parseAge("42") == 42)
```

`parseAge("abc")` debe devolver `-1`

```swift
tryCatch(parseAge("abc") == -1)
```

`parseAge("3.5")` debe devolver `-1`

```swift
tryCatch(parseAge("3.5") == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func parseAge(_ text: String) -> Int {
    return Int(text) ?? -1
}
```
