---
language: swift
exerciseType: 1
---

# --description--

Comprobar `nil` y luego desenvolver forzadamente es tedioso. Swift ofrece el **enlace opcional** con `if let`, que desenvuelve el opcional y guarda el valor en una nueva constante en un solo paso:
```swift
let score: Int? = 10
if let value = score {
    print("Score: \(value)") // value es un Int, no un Int?
} else {
    print("No score")
}
```
El cuerpo del `if` solo se ejecuta cuando el opcional contiene un valor; dentro de él `value` es un `Int` simple y no necesita `!`.

# --instructions--

Escribe una función `describe` que reciba un `Int?` (omite la etiqueta del argumento) y devuelva `"Value: "` seguido del número (por ejemplo `"Value: 5"` para `5`) cuando el opcional contenga un valor, o `"No value"` cuando sea `nil`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`describe(5)` debe devolver `"Value: 5"`

```swift
tryCatch(describe(5) == "Value: 5")
```

`describe(42)` debe devolver `"Value: 42"`

```swift
tryCatch(describe(42) == "Value: 42")
```

`describe(nil)` debe devolver `"No value"`

```swift
tryCatch(describe(nil) == "No value")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func describe(_ number: Int?) -> String {
    if let value = number {
        return "Value: \(value)"
    } else {
        return "No value"
    }
}
```
