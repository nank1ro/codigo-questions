---
language: swift
exerciseType: 4
---

# --description--

El desenvolvimiento forzado solo es seguro cuando ya has comprobado que el opcional no es `nil`:
```swift
if score != nil {
    print(score! * 2)
}
```

# --instructions--

Ordena los elementos para que `price` se duplique y se imprima solo después de comprobar que no es `nil`

# --answers--

```swift
let price: Double? = 9.5
```

```swift
if price != nil {
```

```swift
    print(price! * 2)
```

```swift
}
```

# --solutions--

```swift
let price: Double? = 9.5
if price != nil {
    print(price! * 2)
}
```

# --output--

19.0
