---
language: swift
exerciseType: 1
---

# --description--

Un solo `if let` o `guard let` puede desenvolver varios opcionales a la vez: separa los enlaces con comas.
El cuerpo se ejecuta solo si todos los opcionales tienen un valor:
```swift
let first: String? = "Ada"
let last: String? = "Lovelace"
if let first = first, let last = last {
    print("\(first) \(last)")
}
```
También puedes añadir una condición booleana después de los enlaces, como `if let n = number, n > 0`.

# --instructions--

Escribe una función `add` que reciba dos valores `Int?` (omite las etiquetas de los argumentos) y devuelva su suma como `Int?`, o `nil` si falta alguno de ellos

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`add(2, 3)` debe devolver `5`

```swift
tryCatch(add(2, 3) == 5)
```

`add(nil, 3)` debe devolver `nil`

```swift
tryCatch(add(nil, 3) == nil)
```

`add(2, nil)` debe devolver `nil`

```swift
tryCatch(add(2, nil) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func add(_ a: Int?, _ b: Int?) -> Int? {
    guard let a = a, let b = b else { return nil }
    return a + b
}
```
