---
language: swift
exerciseType: 1
---

# --description--

El trabajo que ocurre a lo largo del tiempo a menudo falla: un servidor está caído, falta un archivo, la entrada es incorrecta. Esa función se marca como **`async throws`**, y se llama con **`try await`**:
```swift
enum LoadError: Error {
    case missing
}

func load(_ name: String) async throws -> String {
    if name.isEmpty {
        throw LoadError.missing
    }
    return "file: \(name)"
}
```
Para manejar el error envuelves la llamada en un bloque `do` y la capturas:
```swift
do {
    let text = try await load("")
    print(text)
} catch {
    print("could not load")
}
// imprime could not load
```
El orden de las palabras clave es fijo: `try` va primero, luego `await`.

# --instructions--

Escribe una función `parse` que reciba una cadena (omite la etiqueta de argumento), sea asíncrona, pueda lanzar errores y devuelva la cadena convertida a `Int`. Si la cadena no es un número, lanza `InputError.notANumber`

Ejemplo de llamada a la función:
```swift
print(try await parse("7"))
// imprime 7
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum InputError: Error {
    case notANumber
}

func parse() {

}
```

# --asserts--

La función debe estar marcada como `async throws`

```json
{
  "regex": "func\\s+parse\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse("7")` debe devolver `7`

```swift
do {
    let value = try await parse("7")
    tryCatch(value == 7)
} catch {
    tryCatch(false)
}
```

`parse("-3")` debe devolver `-3`

```swift
do {
    let value = try await parse("-3")
    tryCatch(value == -3)
} catch {
    tryCatch(false)
}
```

`parse("abc")` debe lanzar un error

```swift
do {
    _ = try await parse("abc")
    tryCatch(false)
} catch {
    tryCatch(true)
}
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}
```
