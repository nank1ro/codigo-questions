---
language: swift
exerciseType: 1
---

# --description--

`async let` funciona cuando sabes cuántas llamadas hay mientras escribes el código. Para una lista cuyo tamaño solo se conoce en tiempo de ejecución, usa un **grupo de tareas**.

`withTaskGroup(of:)` abre un grupo, `addTask` inicia una tarea hija por elemento, y el grupo se lee después con `for await`, que entrega los resultados a medida que terminan:
```swift
let total = await withTaskGroup(of: Int.self) { group in
    for n in numbers {
        group.addTask {
            return await square(n)
        }
    }
    var sum = 0
    for await value in group {
        sum += value
    }
    return sum
}
```
`of: Int.self` declara lo que devuelve cada tarea hija. Toda la llamada a `withTaskGroup` es una expresión, así que necesita un único `await` delante, y no devuelve hasta que cada tarea hija ha terminado.

# --instructions--

Escribe una función `sumOfSquares` que reciba un array de enteros (omite la etiqueta de argumento), sea asíncrona y devuelva la suma de `square` aplicado a cada elemento, calculándolos con `withTaskGroup`

Ejemplo de llamada a la función:
```swift
print(await sumOfSquares([1, 2, 3]))
// prints 14
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func square(_ n: Int) async -> Int {
    return n * n
}

func sumOfSquares() {

}
```

# --asserts--

La función debe usar `withTaskGroup`

```json
{
  "regex": "withTaskGroup",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`sumOfSquares([1, 2, 3])` debe devolver `14`

```swift
tryCatch(await sumOfSquares([1, 2, 3]) == 14)
```

`sumOfSquares([4])` debe devolver `16`

```swift
tryCatch(await sumOfSquares([4]) == 16)
```

`sumOfSquares([])` debe devolver `0`

```swift
tryCatch(await sumOfSquares([]) == 0)
```

`sumOfSquares([-2, 5])` debe devolver `29`

```swift
tryCatch(await sumOfSquares([-2, 5]) == 29)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func square(_ n: Int) async -> Int {
    return n * n
}

func sumOfSquares(_ numbers: [Int]) async -> Int {
    return await withTaskGroup(of: Int.self) { group in
        for n in numbers {
            group.addTask {
                return await square(n)
            }
        }
        var total = 0
        for await value in group {
            total += value
        }
        return total
    }
}
```
