---
language: swift
exerciseType: 4
---

# --description--

Varias llamadas con `await` escritas una tras otra se ejecutan **secuencialmente**: la segunda llamada ni siquiera empieza hasta que la primera ha terminado. El código se lee de arriba abajo, exactamente como el código ordinario:
```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}

let a = await step("A")
print(a)
let b = await step("B")
print(b)
// start A
// done A
// start B
// done B
```
Esto es lo que quieres cuando la segunda llamada necesita el resultado de la primera. Cuando las llamadas son independientes, esperar una antes de empezar la otra es tiempo perdido, y los siguientes ejercicios muestran cómo evitarlo.

# --instructions--

Ordena los elementos para imprimir la salida de abajo, definiendo `step` antes del código de nivel superior
```
start A
done A
start B
done B
```

# --answers--

```swift
func step(_ name: String) async -> String {
```

```swift
    print("start \(name)")
```

```swift
    return "done \(name)"
```

```swift
}
```

```swift
let a = await step("A")
```

```swift
print(a)
```

```swift
let b = await step("B")
```

```swift
print(b)
```

# --solutions--

```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}
let a = await step("A")
print(a)
let b = await step("B")
print(b)
```

# --output--

start A
done A
start B
done B
