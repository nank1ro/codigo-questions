---
language: swift
exerciseType: 2
---

# --description--

Cuando no te importa *por qué* falló la llamada, `try?` es más corto que un bloque `do`. Convierte una llamada que lanza errores en un **opcional**: el valor si hay éxito, `nil` si falla. Combinado con `await` se escribe `try? await`:
```swift
let value = try? await parse("42")  // Optional(42)
let broken = try? await parse("x")  // nil
```
Como el resultado es opcional, encaja directamente en un `if let`:
```swift
if let value = try? await parse("x") {
    print(value)
} else {
    print("not a number")
}
```
Usa `try? await` para un respaldo rápido, y `do` / `catch` cuando el error en sí importa.

# --instructions--

Completa el código para que la llamada fallida produzca `nil` y el programa imprima `not a number`

# --seed--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = [/] await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --answers--

- try?
- try
- try!
- await
- throws

# --solutions--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = try? await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --output--

not a number
