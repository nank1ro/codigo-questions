---
language: swift
exerciseType: 1
---

# --description--

Una función asíncrona sigue siendo una función ordinaria: puede recibir parámetros y devolver un valor de cualquier tipo. Solo cambian dos cosas, la palabra clave `async` en la firma y el `await` en cada llamada:
```swift
func price(of quantity: Int) async -> Double {
    return Double(quantity) * 2.5
}

let total = await price(of: 4)
print(total)
// prints 10.0
```
El valor devuelto es un `Double` normal, no un envoltorio: cuando `await` ha terminado, trabajas con él exactamente como siempre.

# --instructions--

Escribe una función `double` que reciba un entero (omite la etiqueta de argumento), sea asíncrona y devuelva el doble de ese número

Ejemplo de llamada a la función:
```swift
print(await double(4))
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func double() {

}
```

# --asserts--

La función debe estar marcada como `async`

```json
{
  "regex": "func\\s+double\\s*\\([^)]*\\)\\s*async",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`double(4)` debe devolver `8`

```swift
tryCatch(await double(4) == 8)
```

`double(0)` debe devolver `0`

```swift
tryCatch(await double(0) == 0)
```

`double(-7)` debe devolver `-14`

```swift
tryCatch(await double(-7) == -14)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func double(_ n: Int) async -> Int {
    return n * 2
}
```
