---
language: swift
exerciseType: 1
---

# --description--

`async let` está atado al ámbito donde se escribe. Para iniciar trabajo concurrente y conservar un manejador de él, usa una **`Task`**. El cierre pasado a `Task { }` se ejecuta por su cuenta, y la tarea puede almacenarse, pasarse o devolverse:
```swift
let job = Task {
    return await double(21)
}
```
El resultado se lee más tarde con **`.value`**, al que se espera con `await`:
```swift
print(await job.value)
// imprime 42
```
El tipo del manejador dice qué produce y qué puede lanzar: `Task<Int, Never>` es una tarea que devuelve un `Int` y nunca lanza errores. A diferencia de `async let`, una `Task` puede crearse desde código ordinario, que no sea asíncrono.

# --instructions--

Escribe una función `startDoubling` que reciba un entero (omite la etiqueta de argumento) y devuelva una `Task<Int, Never>` que produzca el doble de ese número

Ejemplo de llamada a la función:
```swift
print(await startDoubling(4).value)
// imprime 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func startDoubling() {

}
```

# --asserts--

La función debe crear una `Task`

```json
{
  "regex": "Task\\s*\\{",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`startDoubling(4).value` debe ser `8`

```swift
tryCatch(await startDoubling(4).value == 8)
```

`startDoubling(0).value` debe ser `0`

```swift
tryCatch(await startDoubling(0).value == 0)
```

`startDoubling(-6).value` debe ser `-12`

```swift
tryCatch(await startDoubling(-6).value == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func startDoubling(_ n: Int) -> Task<Int, Never> {
    return Task {
        return n * 2
    }
}
```
