---
language: swift
exerciseType: 1
---

# --description--

`Task.sleep` pausa la tarea actual durante un tiempo sin bloquear nada más. Puede interrumpirse, así que es una llamada asíncrona que lanza errores y necesita `try await`. La duración se indica con ayudantes como `.seconds`, `.milliseconds` o `.nanoseconds`:
```swift
func slowGreeting() async throws -> String {
    try await Task.sleep(for: .milliseconds(50))
    return "hello"
}
```
Es la forma estándar de simular trabajo lento en un ejemplo, en lugar de una llamada de red real. Ten en cuenta que no congela el programa: mientras una tarea duerme, las demás siguen ejecutándose.

# --instructions--

Escribe una función `delayed` que reciba un entero (omite la etiqueta de argumento), sea asíncrona, pueda lanzar errores, espere `50` milisegundos con `Task.sleep` y luego devuelva el número multiplicado por `3`

Ejemplo de llamada a la función:
```swift
print(try await delayed(2))
// imprime 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func delayed() {

}
```

# --asserts--

La función debe estar marcada como `async throws`

```json
{
  "regex": "func\\s+delayed\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

La función debe usar `Task.sleep`

```json
{
  "regex": "Task\\.sleep",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`delayed(2)` debe devolver `6`

```swift
tryCatch((try? await delayed(2)) == 6)
```

`delayed(0)` debe devolver `0`

```swift
tryCatch((try? await delayed(0)) == 0)
```

`delayed(-4)` debe devolver `-12`

```swift
tryCatch((try? await delayed(-4)) == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func delayed(_ n: Int) async throws -> Int {
    try await Task.sleep(for: .milliseconds(50))
    return n * 3
}
```
