---
language: swift
exerciseType: 2
---

# --description--

Como viste en las lecciones de cadenas, iterar sobre una `String` te da un `Character` a la vez. Un `Character` no es una `String`, así que para usarlo como texto lo conviertes con `String(c)`.
Cuando el carácter es un dígito, la propiedad `wholeNumberValue` te da su valor numérico como un `Int?`: es `nil` para los caracteres que no son dígitos.
```swift
for c in "a1" {
    print(c.wholeNumberValue)
}
// nil
// Optional(1)
```

# --instructions--

Completa el bucle para que sume los dígitos de la cadena, de modo que el programa imprima `8`

# --seed--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.[/] {
        sum [/] digit
    }
}
print(sum)
```

# --answers--

- wholeNumberValue
- +=
- asciiValue
- isNumber
- -=
- =

# --solutions--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.wholeNumberValue {
        sum += digit
    }
}
print(sum)
```

# --output--

8
