---
language: swift
exerciseType: 4
---

# --description--

Un **alias de tipo** le da un nuevo nombre a un tipo existente, con la palabra clave `typealias`:
```swift
typealias Score = Int
let best: Score = 100
print(best + 1) // 101
```
`Score` e `Int` son el mismo tipo, así que se mezclan libremente. Un alias no añade ninguna seguridad: solo hace que el código se lea mejor cuando un tipo sencillo tiene un significado específico en tu programa.

# --instructions--

Ordena los elementos para definir `Celsius` como un alias de `Double` en la primera línea, convertir el punto de ebullición del agua a Fahrenheit e imprimirlo

# --answers--

```swift
typealias Celsius = Double
```

```swift
let boiling: Celsius = 100
```

```swift
let fahrenheit = boiling * 9 / 5 + 32
```

```swift
print(fahrenheit)
```

# --solutions--

```swift
typealias Celsius = Double
let boiling: Celsius = 100
let fahrenheit = boiling * 9 / 5 + 32
print(fahrenheit)
```

# --output--

212.0
