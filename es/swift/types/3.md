---
language: swift
exerciseType: 1
---

# --description--

La función `type(of:)` devuelve el tipo de un valor, lo que resulta útil para comprobar qué infirió Swift:
```swift
print(type(of: 42))    // Int
print(type(of: 2.5))   // Double
print(type(of: "hi"))  // String
```
Cuando quieres un tipo distinto del inferido, añade una anotación. Un literal de número entero se puede almacenar en una constante `Double` o `Float`, y un literal de un carácter en una constante `Character`:
```swift
let ratio: Double = 3       // 3.0, not an Int
let half: Float = 0.5
let initial: Character = "S"
print(type(of: ratio))      // Double
```

# --instructions--

Declara tres constantes:
- `ratio`, un `Double` que contenga el literal entero `3`
- `half`, un `Float` que contenga `0.5`
- `initial`, un `Character` que contenga `"S"`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`ratio` debe ser un `Double` igual a `3.0`

```swift
tryCatch(type(of: ratio) == Double.self && ratio == 3.0)
```

`half` debe ser un `Float` igual a `0.5`

```swift
tryCatch(type(of: half) == Float.self && half == 0.5)
```

`initial` debe ser un `Character` igual a `"S"`

```swift
tryCatch(type(of: initial) == Character.self && initial == "S")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let ratio: Double = 3
let half: Float = 0.5
let initial: Character = "S"
```
