---
language: swift
exerciseType: 1
---

# --description--

Como `Int(text)` y `Double(text)` devuelven `nil` en caso de fallo, comparar el resultado con `nil` te dice si un texto es un número de ese tipo:
```swift
print(Int("42") != nil)     // true
print(Double("4.2") != nil) // true
print(Double("42") != nil)  // true, a whole number is also a valid Double
```
Fíjate en la última línea: todo texto aceptado por `Int` también es aceptado por `Double`, así que comprueba primero `Int` cuando quieras distinguirlos.

# --instructions--

Escribe una función `classify` que reciba una `String` (omite la etiqueta de argumento) y devuelva `"Int"` si el texto es un número entero válido, `"Double"` si es un número decimal válido, y `"String"` en cualquier otro caso

Ejemplo de llamada a la función:
```swift
print(classify("3.5"))
// prints Double
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`classify("42")` debe devolver `"Int"`

```swift
tryCatch(classify("42") == "Int")
```

`classify("-7")` debe devolver `"Int"`

```swift
tryCatch(classify("-7") == "Int")
```

`classify("3.5")` debe devolver `"Double"`

```swift
tryCatch(classify("3.5") == "Double")
```

`classify("hello")` debe devolver `"String"`

```swift
tryCatch(classify("hello") == "String")
```

`classify("")` debe devolver `"String"`

```swift
tryCatch(classify("") == "String")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func classify(_ text: String) -> String {
    if Int(text) != nil {
        return "Int"
    }
    if Double(text) != nil {
        return "Double"
    }
    return "String"
}
```
