---
language: swift
exerciseType: 1
---

# --description--

Cuando una función debe devolver un resultado decimal calculado a partir de números enteros, convierte los operandos a `Double` antes de dividir y declara el tipo de retorno como `Double`:
```swift
func ratio(_ part: Int, _ total: Int) -> Double {
    return Double(part) / Double(total)
}
print(ratio(1, 4)) // 0.25
```
Recuerda que el `count` de un array también es un `Int`, así que necesita la misma conversión.

# --instructions--

Escribe una función `average` que reciba un array de `Int` (omite la etiqueta de argumento) y devuelva el promedio de sus elementos como `Double`. El array nunca está vacío

Ejemplo de llamada a la función:
```swift
print(average([1, 2]))
// prints 1.5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`average([1, 2])` debe devolver `1.5`

```swift
tryCatch(average([1, 2]) == 1.5)
```

`average([3, 4, 5, 6])` debe devolver `4.5`

```swift
tryCatch(average([3, 4, 5, 6]) == 4.5)
```

`average([10])` debe devolver `10.0`

```swift
tryCatch(average([10]) == 10.0)
```

`average([1, 1, 2])` debe devolver un `Double`

```swift
tryCatch(type(of: average([1, 1, 2])) == Double.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func average(_ numbers: [Int]) -> Double {
    var sum = 0
    for number in numbers {
        sum += number
    }
    return Double(sum) / Double(numbers.count)
}
```
