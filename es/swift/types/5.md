---
language: swift
exerciseType: 1
---

# --description--

`Int(x)` no redondea: **trunca**, simplemente descartando la parte decimal, así que `Int(3.99)` es `3` e `Int(-3.99)` es `-3`.
Para redondear al número entero más cercano, llama primero a `rounded()` sobre el `Double` y luego convierte:
```swift
let x = 3.99
print(Int(x))            // 3
print(Int(x.rounded()))  // 4
```
Los valores intermedios como `2.5` se redondean lejos del cero: `2.5` se convierte en `3.0` y `-2.5` en `-3.0`.

# --instructions--

Escribe una función `roundedAverage` que reciba dos `Int` (omite las etiquetas de argumento) y devuelva su promedio redondeado al número entero más cercano, como `Int`

Ejemplo de llamada a la función:
```swift
print(roundedAverage(3, 4))
// prints 4
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`roundedAverage(3, 4)` debe devolver `4` (el promedio es `3.5`)

```swift
tryCatch(roundedAverage(3, 4) == 4)
```

`roundedAverage(2, 4)` debe devolver `3`

```swift
tryCatch(roundedAverage(2, 4) == 3)
```

`roundedAverage(10, 15)` debe devolver `13` (el promedio es `12.5`)

```swift
tryCatch(roundedAverage(10, 15) == 13)
```

`roundedAverage(-3, -4)` debe devolver `-4`

```swift
tryCatch(roundedAverage(-3, -4) == -4)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func roundedAverage(_ a: Int, _ b: Int) -> Int {
    let average = Double(a + b) / 2
    return Int(average.rounded())
}
```
