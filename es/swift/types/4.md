---
language: swift
exerciseType: 2
---

# --description--

Swift nunca convierte entre tipos numéricos por sí solo: sumar un `Int` a un `Double` es un error de compilación, aunque ambos sean números.
```swift
let apples = 3
let price = 1.5
let total = apples * price // error: Int and Double can't be mixed
```
Para combinarlos creas un nuevo valor del tipo que necesitas, pasando el valor al inicializador del tipo:
```swift
let total = Double(apples) * price // 4.5
```
Funciona igual a la inversa: `Int(4.5)` produce un `Int`, conservando solo la parte entera del número.

# --instructions--

Completa las conversiones para que `total` sea el coste de las manzanas como `Double` y `whole` su parte entera como `Int`, de modo que el programa imprima `4.5` y `4`

# --seed--

```swift
let apples = 3
let price = 1.5
let total = [/](apples) * price
let whole = [/](total)
print(total)
print(whole)
```

# --answers--

- Double
- Int
- Float
- String
- Character

# --solutions--

```swift
let apples = 3
let price = 1.5
let total = Double(apples) * price
let whole = Int(total)
print(total)
print(whole)
```

# --output--

4.5
4
