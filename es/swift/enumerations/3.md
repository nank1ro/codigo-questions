---
language: swift
exerciseType: 2
---

# --description--

Podemos coincidir con valores de enumeración individuales con una declaración `switch`:
```swift
let color = Colors.red
switch color {
    case .blue:
        print("Blue")
    case .red:
        print("Red")
    case .green:
        print("Green")
}
// prints "Red"
```
Tenga en cuenta que si no necesita proporcionar un `case` para cada caso de enumeración, puede proporcionar un caso `default` para cubrir cualquier caso que no se trate explícitamente

# --instructions--

Completa la declaración `switch` para cubrir todos los casos

# --seed--

```swift
let currentDay = Week.monday
[/][/] {
    case .saturday, .[/]:
        print("Weekend")
    [/]:
        print("Working day")
}
```

# --answers--

- switch 
- currentDay
- sunday
- default

# --solutions--

```swift
let currentDay = Week.monday
switch currentDay {
    case .saturday, .sunday:
        print("Weekend")
    default:
        print("Working day")
}
```

# --output--

Working day
