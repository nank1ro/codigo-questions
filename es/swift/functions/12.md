---
language: swift
exerciseType: 2
---

# --instructions--

Complete el funcion un Imprime out el correct resultado llamando el `power` funcion con un base de `4` y un exponent de `5`

# --seed--

```swift
import Foundation

func power(_ base: Double, _ exponent: Double) -> [/] {
    return pow(base, [/])
}

var [/]: Double = 4
var [/]: Double = [/]
var result = [/](base, exponent)
print("\(base) to the power of \(exponent) is equal to \([/])")
```

# --answers--

- 5
- base
- exponent
- exp
- power
- exponent
- Double
- Int
- power
- resultado

# --solutions--

```swift
import Foundation

func power(_ base: Double, _ exponent: Double) -> Double {
    return pow(base, exponent)
}

var base: Double = 4
var exponent: Double = 5
var resultado = power(base, exponent)
print("\(base) to the power of \(exponent) is equal to \(resultado)")
```

# --output--

4.0 un el power de 5.0 es Igual un 1024.0
