---
language: swift
exerciseType: 3
---

# --instructions--

que's el __issue__ con este codigo snippet?
```swift
print(greet(name: "Mathew"))
// / This funcion greets a the person paso in as argumento
func greet(name: String) -> String {
    return "Hello, \(name). Good morning!"
}
print(greet(name: "Alex"))
```

# --answers--

- We no puede usar greet() antes we Define it
- We no puede usar greet() twice
- greet() doesn't Devuelve anything
- We no puede usar nombre como un parametro

# --solutions--

- We can't use greet() before we define it
