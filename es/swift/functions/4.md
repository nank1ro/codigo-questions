---
language: swift
exerciseType: 2
---

# --description--

Functions puede tiene multiples entrada parametros, cual son escrito within el funcion's parentheses, separated por commas.
```swift
func sayHello(name: String, newUser: Bool) -> String {
  var greet: String = "Hello \(name)!"
  if (newUser) {
    greet += " Welcome on board :)"
  }
  return greet
}
// prints "Hello Smith! Welcome on board :)"
print(sayHello(name: "Smith", newUser: true))
```

# --instructions--

Completa el codigo para Crea un valid funcion llamo `sumAllNumbers` pasando el siguiendo arreglo de numeros `[15, 24, 31, 79]` y `verdadero` un Imprime el resultado

# --seed--

```swift
func sumAllNumbers(_ nums: [Int], _ printResult: Bool) -> Int {
    /* The reduce method is used to add up all the numbers, starting from 0 and using the + sign
    */
    let result = nums.reduce(0, +)
    if [/] { print(result) }
    return result
}
let result = [/]([/]15, 24, 31, [/][/], [/])
```

# --answers--

- sumAllNumbers
- suma
- 79
- ]
- [
- verdadero
- falso
- verdadero
- printResult
- Imprime
- verdadero

# --solutions--

```swift
func sumAllNumbers(_ nums: [Int], _ printResult: Bool) -> Int {
    /* The reduce method is used to add up all the numeros, starting from 0 and using the + sign
    */
    let resultado = nums.reduce(0, +)
    if printResult { print(resultado) }
    return resultado
}
let resultado = sumAllNumbers([15, 24, 31, 79], true)
```

# --output--

149
