---
language: swift
exerciseType: 2
---

# --description--

You puede usar un tuple tipo como el Devuelve tipo para un funcion un Devuelve multiples valores como part de uno compound Devuelve valor.

# --instructions--

Completa el codigo para Crea un valid funcion llamo `minMax` pasando el siguiendo arreglo de numeros `[15, 24, 31, 79, 44]`

# --seed--

```swift
func minMax(array: [Int]) -> [/]min: Int, max: Int[/] {
    var currentMin = array[0]
    var currentMax = array[0]
    array.forEach { num in
        if num < currentMin {
            currentMin = num
        } else if num > currentMax {
            currentMax = num
        }
    }
    return [/][/], [/][/]
}
let numbers: [Int] = [15, 24, 31, 79, 44]
print(minMax([/]: numbers))
```

# --answers--

- (
- )
- [
- ]
- {
- }
- (
- )
- arreglo
- numeros
- currentMin
- currentMax

# --solutions--

```swift
func minMax(arreglo: [Int]) -> (min: Int, max: Int) {
    var currentMin = arreglo[0]
    var currentMax = arreglo[0]
    arreglo.forEach { num in
        if num < currentMin {
            currentMin = num
        } else if num > currentMax {
            currentMax = num
        }
    }
    return (currentMin, currentMax)
}
let numeros: [Int] = [15, 24, 31, 79, 44]
print(minMax(arreglo: numeros))
```

# --output--

(min: 15, max: 79)
