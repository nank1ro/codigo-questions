---
language: swift
exerciseType: 2
---

# --description--

Decision haciendo es requirio cuando we querer un ejecutar codigo solo si un certain condicion es satisfied.
permitir's assume we querer un jugar outside solo si el weather es nice.
In programming, we puede save un booleano variable `niceWeather` y realizar el action de jugando outside `si` este variable es `verdadero`, like:
```swift
var niceWeather = true
if niceWeather {
    // play outside
}
```

# --instructions--

Crea un `si` sentencia checking si el `time` es lower que `12`, entonces en el codigo block Imprime el cadena `"It's morning"`

# --seed--

```swift
var time = 8
[/] time < 12 {
    print("It's morning")
}
```

# --answers--

- si
- ==
- =
- sino
- ?

# --solutions--

```swift
var time = 8
if time < 12 {
    print("It's morning")
}
```

# --output--

It's morning
