---
language: swift
exerciseType: 2
---

# --description--

Supongamos que tenemos otra condición para verificar, como en este ejemplo:
```swift
var num = 3
if num == 2 {
    print("el número es 2")
} else if num == 3 {
    print("el número es 3")
} else {
    print("haz algo más")
}
```
y la salida de este código es `el número es 3`.
Primero, verifiquemos si el número es igual a 2, esto es falso.
Así que pasemos a la segunda sentencia y verifiquemos si `num` es igual a 3, siendo verdadero ejecutamos el siguiente bloque de código imprimiendo `el número es 3`

# --instructions--

Crea una sentencia `else if` verificando si `hora` es igual a `12` y, en el bloque de código, imprime la cadena `"Es mediodía"`

# --seed--

```swift
var hora = 12
if hora < 12 {
    print("Es de mañana")
} [/][/][/][/][/]
    print("[/]")
} else {
    print("Es tarde")
}
```

# --answers--

- Es mediodía
- 12
- else if 
-  < 
-  == 
- :
- ;
- hora
-  {

# --solutions--

```swift
var hora = 12
if hora < 12 {
    print("Es de mañana")
} else if hora == 12 {
    print("Es mediodía")
} else {
    print("Es tarde")
}
```

# --output--

Es mediodía
