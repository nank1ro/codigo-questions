---
language: swift
exerciseType: 2
---

# --description--

Continuemos con el previous ejemplo.
```swift
var niceWeather = true
if niceWeather {
    // play outside
}
```
We've visto ese el `si` sentencia executes el block de codigo solo si el condicion es `verdadero`.
Another importante thing un considerar es represented por el **curly brackets** `{}` cual indicate un codigo block.

# --instructions--

Fix el `si` sentencia anadiendo el *curly brackets* `{}`

# --seed--

```swift
var time = 8
if time < 12 [/]
    print("It's morning")
[/]
```

# --answers--

- si
- ==
- =
- sino
- ?
- {
- }

# --solutions--

```swift
var time = 8
if time < 12 {
    print("It's morning")
}
```

# --output--

It's morning
