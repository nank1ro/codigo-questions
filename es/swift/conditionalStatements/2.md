---
language: swift
exerciseType: 2
---

# --description--

Continuemos con el ejemplo anterior.
```swift
var niceWeather = true
if niceWeather {
    // play outside
}
```
Hemos visto que la instrucción `if` ejecuta el bloque de código solo si la condición es `true`.
Otra cosa importante a considerar es representada por los **corchetes ondulantes** `{}` que indican un bloque de código.

# --instructions--

Corrige la instrucción `if` añadiendo los *corchetes ondulantes* `{}`

# --seed--

```swift
var time = 8
if time < 12 [/]
    print("It's morning")
[/]
```

# --answers--

- if
- ==
- =
- else
- ?
- {
- }

# --solutions--

```swift
var time = 8
if time < 12 {
    print("It's morning")
}
```

# --output--

It's morning
