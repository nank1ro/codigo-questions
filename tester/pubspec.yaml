name: tester
description: Local runner that compiles and tests exercises via codecompiler.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.0
  dotenv: ^4.2.0
  glob: ^2.1.0
  http: ^1.2.0
  path: ^1.9.0
  parser:
    path: ../parser

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
