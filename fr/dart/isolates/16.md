---
language: dart
exerciseType: 1
---

# --description--

Chaque `Isolate.run` démarre son propre isolate, donc plusieurs d'entre eux s'exécutent vraiment au même instant, sur autant de cœurs que la machine en possède. Le schéma est celui que tu connais déjà avec les futures : démarre chaque calcul d'abord, puis attends-les tous avec `Future.wait`, qui garde les résultats dans l'ordre de l'entrée.

```dart
Future<List<int>> doubleAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * 2)));
}
```

Démarrer un isolate n'est pas gratuit : cela coûte de la mémoire et quelques millisecondes. Répartir un long calcul sur une poignée d'isolates paie, envoyer mille additions triviales à mille isolates ne paie pas.

# --instructions--

Écris une fonction `squareAll` qui prend une `List<int>` `numbers` et renvoie un `Future<List<int>>` se terminant avec le carré de chaque nombre, dans le même ordre, chaque carré étant calculé sur son propre isolate.

Exemple :
```dart
print(await squareAll([1, 2, 3])); // [1, 4, 9]
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`squareAll([1, 2, 3])` devrait se terminer par `[1, 4, 9]`

```dart
  test('test1', () async {
    expect(await squareAll([1, 2, 3]), [1, 4, 9], reason: '--err-t1--');
  });
```

`squareAll([-4, 5])` devrait se terminer par `[16, 25]`, en gardant l'ordre de l'entrée

```dart
  test('test2', () async {
    expect(await squareAll([-4, 5]), [16, 25], reason: '--err-t2--');
  });
```

`squareAll([])` devrait se terminer par une liste vide et `squareAll` devrait renvoyer un `Future<List<int>>`

```dart
  test('test3', () async {
    expect(await squareAll(<int>[]), <int>[], reason: '--err-t3--');
    expect(squareAll([2]), isA<Future<List<int>>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<List<int>> squareAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * n)));
}
```
