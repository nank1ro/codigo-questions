---
language: dart
exerciseType: 1
---

# --description--

Ramasser toute une série de messages suit une seule recette : une liste vide avant la boucle, un `add` par vrai message, et `close()` sur le signal qui met fin au stream. Une fois le port fermé, le `await for` se termine et la fonction peut renvoyer :

```dart
Future<List<int>> collect(ReceivePort port) async {
  final values = <int>[];
  await for (final message in port) {
    if (message == null) {
      port.close();
    } else {
      values.add(message as int);
    }
  }
  return values;
}
```

Les messages gardent l'ordre dans lequel ils ont été envoyés, donc la liste que tu construis reflète pas à pas le travail de l'autre isolate.

# --instructions--

Écris une fonction de premier niveau `sendWords` qui prend une `List<Object>` `message` où l'élément `0` est un `SendPort` et l'élément `1` est une `List<String>` : envoie chaque mot au port, puis envoie `null` pour signaler la fin.

Puis écris une fonction `collectWords` qui prend une `List<String>` `words` et renvoie un `Future<List<String>>` se terminant avec tous les mots reçus d'un isolate `sendWords` démarré pour l'occasion.

Exemple :
```dart
print(await collectWords(['a', 'b'])); // [a, b]
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendWords` devrait envoyer chaque mot puis `null`

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendWords(<Object>[port.sendPort, <String>['a', 'b']]);
    final received = <dynamic>[];
    await for (final message in port) {
      received.add(message);
      if (message == null) port.close();
    }
    expect(received, ['a', 'b', null], reason: '--err-t1--');
  });
```

`collectWords(['hello', 'isolate'])` devrait se terminer par `['hello', 'isolate']`

```dart
  test('test2', () async {
    expect(await collectWords(['hello', 'isolate']), ['hello', 'isolate'], reason: '--err-t2--');
  });
```

`collectWords([])` devrait se terminer par une liste vide et `collectWords` devrait renvoyer un `Future<List<String>>`

```dart
  test('test3', () async {
    expect(await collectWords(<String>[]), <String>[], reason: '--err-t3--');
    expect(collectWords(['x']), isA<Future<List<String>>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendWords(List<Object> message) {
  final port = message[0] as SendPort;
  final words = message[1] as List<String>;
  for (final word in words) {
    port.send(word);
  }
  port.send(null);
}

Future<List<String>> collectWords(List<String> words) async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendWords, <Object>[receivePort.sendPort, words]);
  final collected = <String>[];
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      collected.add(message as String);
    }
  }
  return collected;
}
```
