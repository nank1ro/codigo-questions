---
language: dart
exerciseType: 1
---

# --description--

Une exception levée dans un isolate ne peut pas sauter vers un autre : les deux ont des piles séparées. `Isolate.run` comble ce fossé pour toi en attrapant l'erreur, en la recopiant et en faisant échouer le future renvoyé avec elle. De ton côté, c'est donc une erreur asynchrone ordinaire, attrapée avec `try`/`catch` autour du `await` :

```dart
Future<int> parseInIsolate(String text) async {
  try {
    return await Isolate.run(() => int.parse(text));
  } catch (e) {
    return -1;
  }
}
```

Le `await` dans le `try` compte, exactement comme pour n'importe quel autre future : sans lui, le future quitterait le bloc `try` sans être terminé et le `catch` ne s'exécuterait jamais.

Avec `Isolate.spawn`, il n'y a pas un tel pont. Une erreur non attrapée tue l'isolate démarré en silence et le parent continue d'attendre un message qui n'arrivera jamais, ce qui est une raison de plus de penser d'abord à `Isolate.run`.

# --instructions--

Écris une fonction `safeDivide` qui prend deux `int` `a` et `b` et renvoie un `Future<int>` : elle calcule `a ~/ b` sur un autre isolate avec `Isolate.run` et se termine avec `-1` si le calcul lève une exception.

Exemple :
```dart
print(await safeDivide(10, 2)); // 5
print(await safeDivide(7, 0));  // -1
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeDivide(10, 2)` devrait se terminer par `5`

```dart
  test('test1', () async {
    expect(await safeDivide(10, 2), 5, reason: '--err-t1--');
  });
```

`safeDivide(7, 0)` devrait se terminer par `-1` au lieu d'échouer

```dart
  test('test2', () async {
    expect(await safeDivide(7, 0), -1, reason: '--err-t2--');
  });
```

`safeDivide(-9, 3)` devrait se terminer par `-3` et `safeDivide` devrait renvoyer un `Future<int>`

```dart
  test('test3', () async {
    expect(await safeDivide(-9, 3), -3, reason: '--err-t3--');
    expect(safeDivide(4, 2), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<int> safeDivide(int a, int b) async {
  try {
    return await Isolate.run(() => a ~/ b);
  } catch (e) {
    return -1;
  }
}
```
