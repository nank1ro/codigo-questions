---
language: dart
exerciseType: 1
---

# --description--

`Isolate.spawn` passe exactement **un** message au point d'entrée, et le worker a généralement besoin à la fois d'un `SendPort` pour répondre et de données à traiter. L'astuce habituelle est de tout regrouper dans une seule `List` et de la déballer de l'autre côté :

```dart
void multiply(List<Object> message) {
  final port = message[0] as SendPort;
  final value = message[1] as int;
  port.send(value * 2);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(multiply, <Object>[receivePort.sendPort, 21]);
  print(await receivePort.first); // 42
}
```

La liste est copiée à l'aller, donc le worker lit ses propres valeurs. Un `SendPort` est l'une des rares choses qui n'est pas copiée mais partagée : il continue de pointer vers la boîte aux lettres de l'isolate qui l'a créé, et c'est exactement pourquoi il peut servir d'adresse de retour.

# --instructions--

Écris une fonction de premier niveau `sendSum` qui prend une `List<Object>` `message` où l'élément `0` est un `SendPort` et les éléments `1` et `2` sont des `int`, et envoie leur somme au port.

Puis écris une fonction `addInIsolate` qui prend deux `int` `a` et `b` et renvoie un `Future<int>` se terminant avec la somme calculée par un isolate `sendSum` démarré pour l'occasion.

Exemple :
```dart
print(await addInIsolate(2, 3)); // 5
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendSum` devrait envoyer la somme des deux nombres du message au port

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendSum(<Object>[port.sendPort, 4, 6]);
    expect(await port.first, 10, reason: '--err-t1--');
  });
```

`addInIsolate(2, 3)` devrait se terminer par `5`

```dart
  test('test2', () async {
    expect(await addInIsolate(2, 3), 5, reason: '--err-t2--');
  });
```

`addInIsolate(10, -4)` devrait se terminer par `6` et `addInIsolate` devrait renvoyer un `Future<int>`

```dart
  test('test3', () async {
    expect(await addInIsolate(10, -4), 6, reason: '--err-t3--');
    expect(addInIsolate(1, 1), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendSum(List<Object> message) {
  final port = message[0] as SendPort;
  final a = message[1] as int;
  final b = message[2] as int;
  port.send(a + b);
}

Future<int> addInIsolate(int a, int b) async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendSum, <Object>[receivePort.sendPort, a, b]);
  return await receivePort.first as int;
}
```
