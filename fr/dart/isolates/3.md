---
language: dart
exerciseType: 1
---

# --description--

Seules les **données** sont copiées entre isolates ; pas le **code**. Chaque isolate d'un programme peut déjà voir toutes les fonctions de premier niveau et toutes les classes de ce programme, donc le calcul confié à `Isolate.run` peut librement les appeler :

```dart
int slowLength(String text) => text.length;

Future<int> lengthInIsolate(String text) {
  return Isolate.run(() => slowLength(text));
}
```

Ce qui voyage, c'est le `text` capturé à l'aller et l'`int` résultant au retour, chacun une copie. Le schéma est toujours le même : garde la fonction lourde où elle est, et enveloppe l'**appel** dans `Isolate.run`.

# --instructions--

La fonction `wordCount(text)` compte les mots d'une chaîne. Écris une fonction `countWords` qui prend une `String` `text` et renvoie un `Future<int>` se terminant avec `wordCount(text)`, calculé sur un autre isolate avec `Isolate.run`.

Exemple :
```dart
print(await countWords('hello isolate world')); // 3
```

# --before-seed--

```dart
import 'dart:isolate';

int wordCount(String text) {
  return text.split(' ').where((w) => w.isNotEmpty).length;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countWords('hello isolate world')` devrait se terminer par `3`

```dart
  test('test1', () async {
    expect(await countWords('hello isolate world'), 3, reason: '--err-t1--');
  });
```

`countWords('Dart')` devrait se terminer par `1` et `countWords('')` par `0`

```dart
  test('test2', () async {
    expect(await countWords('Dart'), 1, reason: '--err-t2--');
    expect(await countWords(''), 0, reason: '--err-t2--');
  });
```

`countWords('  a  b ')` devrait se terminer par `2` et `countWords` devrait renvoyer un `Future<int>`

```dart
  test('test3', () async {
    expect(await countWords('  a  b '), 2, reason: '--err-t3--');
    expect(countWords('one two'), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<int> countWords(String text) {
  return Isolate.run(() => wordCount(text));
}
```
