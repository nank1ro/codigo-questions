---
language: dart
exerciseType: 1
---

# --description--

La fonction donnée à `Isolate.spawn` s'appelle le **point d'entrée**. Elle doit être une fonction de premier niveau (ou statique) qui prend exactement un paramètre : le message que `Isolate.spawn` lui passe.

```dart
void reportAnswer(SendPort port) {
  port.send(42);
}
```

Les messages qui arrivent dans un `ReceivePort` ont le type statique `dynamic`, car n'importe quelle valeur pouvait avoir été envoyée. Quand tu sais ce que l'autre isolate envoie, fais un cast :

```dart
final receivePort = ReceivePort();
await Isolate.spawn(reportAnswer, receivePort.sendPort);
final answer = await receivePort.first as int;
```

# --instructions--

Écris une fonction de premier niveau `sendGreeting` qui prend un `SendPort` `port` et lui envoie la chaîne `hello from the isolate`.

Puis écris une fonction `greetFromIsolate` qui renvoie un `Future<String>` : elle crée un `ReceivePort`, démarre `sendGreeting` avec son `sendPort` et se termine avec le premier message reçu.

Exemple :
```dart
print(await greetFromIsolate()); // hello from the isolate
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendGreeting` devrait envoyer `'hello from the isolate'` au port qu'elle reçoit

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendGreeting(port.sendPort);
    expect(await port.first, 'hello from the isolate', reason: '--err-t1--');
  });
```

`greetFromIsolate()` devrait se terminer par `'hello from the isolate'`

```dart
  test('test2', () async {
    expect(await greetFromIsolate(), 'hello from the isolate', reason: '--err-t2--');
  });
```

`greetFromIsolate` devrait renvoyer un `Future<String>` et devrait fonctionner quand on l'appelle deux fois

```dart
  test('test3', () async {
    expect(greetFromIsolate(), isA<Future<String>>(), reason: '--err-t3--');
    expect(await greetFromIsolate(), 'hello from the isolate', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendGreeting(SendPort port) {
  port.send('hello from the isolate');
}

Future<String> greetFromIsolate() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendGreeting, receivePort.sendPort);
  return await receivePort.first as String;
}
```
