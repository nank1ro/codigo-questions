---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez deux variables `int` de niveau supérieur : `x` avec la valeur `25` et `y` avec la valeur `9`. Puis déclarez une variable `int` de niveau supérieur nommée `diff` qui stocke le résultat de la soustraction de `y` de `x`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`diff` doit être égal à `16`

```dart
  test('test1', () {
    expect(diff, 16, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 25;
int y = 9;
int diff = x - y;

void main() {
  print(diff);
}
```
