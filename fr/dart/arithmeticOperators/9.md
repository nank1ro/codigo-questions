---
language: dart
exerciseType: 1
---

# --instructions--

Utilisez l'opérateur modulo `%` pour trouver le reste lorsque `17` est divisé par `5`. Stockez le résultat dans une variable de niveau supérieur nommée `remainder`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

La variable `remainder` doit être égale à `2`

```dart
  test('test1', () {
    expect(remainder, 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 17 % 5;

void main() {
  print(remainder);
}
```
