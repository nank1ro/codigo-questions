---
language: dart
exerciseType: 2
---

# --description--

L'**opérateur de division** `/` divise une valeur par une autre. En Dart, `/` **renvoie toujours un `double`**, même lorsque le résultat est un nombre entier.

```dart
int a = 10;
int b = 4;
double result = a / b; // 2.5

double exact = 10 / 2; // 5.0  (not 5!)
```

Utilisez toujours une variable `double` pour stocker le résultat de `/`.

# --instructions--

Complétez le code pour diviser `9` par `4` et stocker le résultat dans `result`.

# --seed--

```dart
void main() {
  double result = 9 [/] 4;
  print(result);
}
```

# --answers--

- /
- *
- +
- -

# --solutions--

```dart
void main() {
  double result = 9 / 4;
  print(result);
}
```

# --output--

2.25
