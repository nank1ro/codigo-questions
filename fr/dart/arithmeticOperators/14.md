---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez deux variables `int` de niveau supérieur : `total` avec la valeur `29` et `perGroup` avec la valeur `4`. Puis déclarez une variable `int` de niveau supérieur nommée `groups` qui stocke combien de groupes complets de `4` rentrent dans `29` (utilisez la division entière `~/`).

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`groups` doit être égal à `7`

```dart
  test('test1', () {
    expect(groups, 7, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 29;
int perGroup = 4;
int groups = total ~/ perGroup;

void main() {
  print(groups);
}
```
