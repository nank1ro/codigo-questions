---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez une variable `double` de niveau supérieur nommée `result` qui stocke le résultat de la division de `7` par `2`. Rappelez-vous que `/` en Dart retourne toujours un `double`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` doit être un `double` avec la valeur `3.5`

```dart
  test('test1', () {
    expect(result, isA<double>(), reason: '--err-t1--');
    expect(result, closeTo(3.5, 0.001), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double result = 7 / 2;

void main() {
  print(result);
}
```
