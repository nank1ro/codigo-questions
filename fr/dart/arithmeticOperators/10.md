---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez deux variables `int` de niveau supérieur : `a` avec la valeur `12` et `b` avec la valeur `8`. Puis déclarez une troisième variable `int` de niveau supérieur nommée `total` qui stocke leur somme.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`a` doit être `12`, `b` doit être `8` et `total` doit être `20`

```dart
  test('test1', () {
    expect(a, 12, reason: '--err-t1--');
    expect(b, 8, reason: '--err-t2--');
    expect(total, 20, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 12;
int b = 8;
int total = a + b;

void main() {
  print(total);
}
```
