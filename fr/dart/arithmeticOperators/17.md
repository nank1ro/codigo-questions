---
language: dart
exerciseType: 1
---

# --instructions--

Un magasin vend `itemCount` articles à `pricePerItem` dollars chacun, avec un montant de `discount` dollars soustrait du total. Déclarez ces trois variables `int` de niveau supérieur : `itemCount` = `6`, `pricePerItem` = `15`, `discount` = `10`. Puis déclarez une variable `int` de niveau supérieur nommée `totalPrice` qui calcule le total après la remise.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`totalPrice` doit être égal à `80`

```dart
  test('test1', () {
    expect(totalPrice, 80, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int itemCount = 6;
int pricePerItem = 15;
int discount = 10;
int totalPrice = itemCount * pricePerItem - discount;

void main() {
  print(totalPrice);
}
```
