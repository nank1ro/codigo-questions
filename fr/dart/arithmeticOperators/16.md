---
language: dart
exerciseType: 1
---

# --description--

Dart suit l'**ordre standard des opérations** (priorité) : la multiplication et la division sont évaluées avant l'addition et la soustraction.

```dart
int result = 2 + 3 * 4; // 14, not 20
```

Utilisez des parenthèses pour changer l'ordre :

```dart
int result = (2 + 3) * 4; // 20
```

# --instructions--

Déclarez une variable `int` de niveau supérieur nommée `result` qui évalue `3 + 4 * 2 - 1`. Laissez Dart appliquer sa priorité d'opérateur par défaut (n'ajoutez pas de parenthèses).

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` doit être égal à `10`

```dart
  test('test1', () {
    expect(result, 10, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int result = 3 + 4 * 2 - 1;

void main() {
  print(result);
}
```
