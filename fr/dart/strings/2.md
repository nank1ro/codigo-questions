---
language: dart
exerciseType: 1
---

# --description--

Deux chaînes peuvent être jointes en une nouvelle avec l'opérateur `+`, appelé **concaténation** :

```dart
var greeting = 'Hello' + ', ' + 'Dart';
print(greeting); // Hello, Dart
```

Dart joint également deux **littéraux** de chaîne écrits l'un à côté de l'autre, sans aucun opérateur. C'est pratique pour répartir un long texte sur plusieurs lignes :

```dart
var text = 'Hello, '
    'Dart';
print(text); // Hello, Dart
```

Seules les chaînes peuvent être concaténées avec `+` : `'Age: ' + 30` est une erreur de compilation, car `30` est un `int`.

# --instructions--

Étant donné les variables `first` et `last`, créez un `String` nommé `full` qui contient le prénom, un espace, et le nom de famille.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var first = 'Ada';
var last = 'Lovelace';
```

# --after-seed--

```dart
  return full;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`full` devrait être `'Ada Lovelace'`

```dart
  test('test1', () {
    expect(result(), 'Ada Lovelace', reason: '--err-t1--');
  });
```

`full` devrait être un `String`

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var first = 'Ada';
var last = 'Lovelace';
String full = first + ' ' + last;
```
