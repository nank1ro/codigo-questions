---
language: dart
exerciseType: 1
---

# --description--

Comme les chaînes sont immuables, construire un long texte avec `+=` dans une boucle crée une nouvelle chaîne à chaque étape. Un **StringBuffer** rassemble des morceaux de texte efficacement et ne produit la chaîne finale que lorsque vous la demandez :

- `.write(value)` ajoute une valeur (tout type est converti en texte)
- `.writeln(value)` ajoute la valeur suivie d'un retour à la ligne
- `.toString()` renvoie la chaîne construite jusqu'à présent

```dart
var buffer = StringBuffer();
buffer.write('Hello');
buffer.write(', ');
buffer.writeln('Dart!');
buffer.write(42);
print(buffer.toString()); // Hello, Dart!\n42
```

# --instructions--

Étant donné la liste `items`, utilisez un `StringBuffer` pour construire un `String` nommé `menu` où chaque élément est sur sa propre ligne, précédé de sa position à partir de `1` et d'un point, comme `1. milk`. Chaque ligne doit se terminer par un retour à la ligne (utilisez `.writeln()`).

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var items = ['milk', 'eggs', 'bread'];
```

# --after-seed--

```dart
  return menu;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`menu` devrait être `'1. milk\n2. eggs\n3. bread\n'`

```dart
  test('test1', () {
    expect(result(), '1. milk\n2. eggs\n3. bread\n', reason: '--err-t1--');
  });
```

`menu` devrait être un `String`

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var items = ['milk', 'eggs', 'bread'];
var buffer = StringBuffer();
for (var i = 0; i < items.length; i++) {
  buffer.writeln('${i + 1}. ${items[i]}');
}
String menu = buffer.toString();
```
