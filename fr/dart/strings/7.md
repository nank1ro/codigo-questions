---
language: dart
exerciseType: 1
---

# --description--

La méthode `.substring(start, end)` renvoie la partie d'une chaîne allant de l'index `start` jusqu'à, **sans l'inclure**, l'index `end`. Si vous omettez `end`, elle prend tout jusqu'à la fin de la chaîne :

```dart
var text = 'Hello Dart';
print(text.substring(0, 5));  // Hello
print(text.substring(6));     // Dart
```

# --instructions--

Étant donné la chaîne `date` au format `YYYY-MM-DD`, stockez dans un `String` nommé `year` les quatre premiers caractères et dans un `String` nommé `day` les deux derniers caractères.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var date = '2024-06-15';
```

# --after-seed--

```dart
  return [year, day];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`year` devrait être `'2024'`

```dart
  test('test1', () {
    expect(result()[0], '2024', reason: '--err-t1--');
  });
```

`day` devrait être `'15'`

```dart
  test('test2', () {
    expect(result()[1], '15', reason: '--err-t2--');
  });
```

`year` devrait être un `String`

```dart
  test('test3', () {
    expect(result()[0], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var date = '2024-06-15';
String year = date.substring(0, 4);
String day = date.substring(8);
```
