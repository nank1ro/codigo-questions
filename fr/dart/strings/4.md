---
language: dart
exerciseType: 1
---

# --description--

Chaque chaîne connaît le nombre de caractères qu'elle contient grâce à sa propriété `.length`. Les espaces et la ponctuation comptent aussi comme des caractères :

```dart
var word = 'hello';
print(word.length);      // 5
print('a b'.length);     // 3
```

Vous pouvez lire un seul caractère avec des crochets et son **index**, en commençant à `0`. Le résultat est un `String` d'un seul caractère :

```dart
print(word[0]);                // h
print(word[word.length - 1]);  // o
```

Lire un index en dehors de la chaîne (comme `word[5]`) lève une erreur.

# --instructions--

Étant donné la chaîne `word`, stockez dans un `int` nommé `size` son nombre de caractères, dans un `String` nommé `first` son premier caractère, et dans un `String` nommé `last` son dernier caractère.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var word = 'Flutter';
```

# --after-seed--

```dart
  return [size, first, last];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`size` devrait être `7`

```dart
  test('test1', () {
    expect(result()[0], 7, reason: '--err-t1--');
  });
```

`first` devrait être `'F'`

```dart
  test('test2', () {
    expect(result()[1], 'F', reason: '--err-t2--');
  });
```

`last` devrait être `'r'`

```dart
  test('test3', () {
    expect(result()[2], 'r', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var word = 'Flutter';
int size = word.length;
String first = word[0];
String last = word[word.length - 1];
```
