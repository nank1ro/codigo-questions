---
language: dart
exerciseType: 1
---

# --description--

Les méthodes de chaîne renvoient des chaînes, elles peuvent donc être **chaînées** les unes après les autres. Combiné avec `.split('')`, la propriété de liste `.reversed` et `.join()`, cela permet d'inverser une chaîne en une seule expression :

```dart
var text = 'Dart';
print(text.split('').reversed.join()); // traD
print(text.toLowerCase().replaceAll('a', '4')); // d4rt
```

Un **palindrome** est un texte qui se lit de la même façon dans les deux sens, comme `level`.

# --instructions--

Écrivez une fonction `isPalindrome` qui prend un paramètre `String` nommé `text` et renvoie `true` s'il s'agit d'un palindrome, en ignorant la casse et les espaces, ou `false` sinon.

Exemple :
```dart
print(isPalindrome('Level'));            // true
print(isPalindrome('never odd or even')); // true
print(isPalindrome('Dart'));             // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isPalindrome('Level')` doit renvoyer `true`

```dart
  test('test1', () {
    expect(isPalindrome('Level'), true, reason: '--err-t1--');
  });
```

`isPalindrome('never odd or even')` doit renvoyer `true`

```dart
  test('test2', () {
    expect(isPalindrome('never odd or even'), true, reason: '--err-t2--');
  });
```

`isPalindrome('Dart')` doit renvoyer `false`

```dart
  test('test3', () {
    expect(isPalindrome('Dart'), false, reason: '--err-t3--');
  });
```

`isPalindrome('Race car')` doit renvoyer `true`

```dart
  test('test4', () {
    expect(isPalindrome('Race car'), true, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isPalindrome(String text) {
  var clean = text.toLowerCase().replaceAll(' ', '');
  return clean == clean.split('').reversed.join();
}
```
