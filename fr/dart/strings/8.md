---
language: dart
exerciseType: 1
---

# --description--

Plusieurs méthodes permettent de rechercher à l'intérieur d'une chaîne :

- `.contains(other)` renvoie `true` si `other` apparaît n'importe où dans la chaîne
- `.startsWith(other)` et `.endsWith(other)` vérifient le début et la fin
- `.indexOf(other)` renvoie l'index de la première occurrence, ou `-1` si elle n'est pas trouvée

```dart
var file = 'report.pdf';
print(file.contains('port'));    // true
print(file.startsWith('rep'));   // true
print(file.endsWith('.txt'));    // false
print(file.indexOf('.'));        // 6
print(file.indexOf('x'));        // -1
```

Toutes sont sensibles à la casse : `'Dart'.contains('dart')` vaut `false`.

# --instructions--

Étant donné la chaîne `email`, stockez dans un `bool` nommé `hasAt` si elle contient le caractère `@`, dans un `int` nommé `atIndex` l'index de ce caractère, et dans un `bool` nommé `isExample` si l'email se termine par `example.com`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var email = 'ada@example.com';
```

# --after-seed--

```dart
  return [hasAt, atIndex, isExample];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasAt` devrait être `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`atIndex` devrait être `3`

```dart
  test('test2', () {
    expect(result()[1], 3, reason: '--err-t2--');
  });
```

`isExample` devrait être `true`

```dart
  test('test3', () {
    expect(result()[2], true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var email = 'ada@example.com';
bool hasAt = email.contains('@');
int atIndex = email.indexOf('@');
bool isExample = email.endsWith('example.com');
```
