---
language: dart
exerciseType: 1
---

# --description--

Les données réelles sont pleines de trous : un champ de formulaire laissé vide, une colonne absente d'un fichier, une chaîne qui n'est pas tout à fait un nombre. Les outils de ce chapitre se combinent naturellement pour les gérer : `nonNulls` pour supprimer les éléments manquants, `int.tryParse` pour convertir en toute sécurité, `??` ou une vérification `if` pour traiter ce qui n'a pas pu être converti.

# --instructions--

Écrivez une fonction `total` qui prend un paramètre `List<String?>` `prices` et retourne la somme des valeurs qui peuvent être converties en un `int`, en ignorant à la fois les entrées `null` et les chaînes qui ne sont pas des entiers valides.

Exemple :
```dart
print(total(['3', null, 'abc', '4'])); // 7
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total(['3', null, 'abc', '4'])` doit être `7`

```dart
  test('test1', () {
    expect(total(['3', null, 'abc', '4']), 7, reason: '--err-t1--');
  });
```

`total([null, null])` doit être `0`

```dart
  test('test2', () {
    expect(total([null, null]), 0, reason: '--err-t2--');
  });
```

`total(['10', '', '-2', '5'])` doit être `13`

```dart
  test('test3', () {
    expect(total(['10', '', '-2', '5']), 13, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total(List<String?> prices) {
  var sum = 0;
  for (var price in prices.nonNulls) {
    var value = int.tryParse(price);
    if (value != null) {
      sum += value;
    }
  }
  return sum;
}
```
