---
language: dart
exerciseType: 1
---

# --description--

Parfois, **vous** savez qu'une valeur nullable n'est pas `null` à un certain point, même si le compilateur ne peut pas le deviner. L'opérateur d'**assertion non nulle** `!` transforme une `String?` en une `String` en promettant que la valeur est présente :

```dart
String? text = 'Dart';
String sure = text!;
print(sure.length); // 4
```

Attention : `!` déplace la vérification du moment de la compilation vers le moment de l'exécution. Si la valeur **est** `null`, le programme lève une erreur et s'arrête :

```dart
String? text;
print(text!.length); // Null check operator used on a null value
```

Utilisez `!` avec parcimonie, et seulement quand un `null` à cet endroit serait de toute façon un bug.

# --instructions--

Écrivez une fonction `shout` qui prend un paramètre `String` nullable `text` et le retourne en majuscules suivi de `!`. Utilisez l'opérateur `!`, afin que la fonction lève une erreur quand `text` est `null`.

Exemple :
```dart
print(shout('hello')); // HELLO!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shout('hello')` doit être `'HELLO!'`

```dart
  test('test1', () {
    expect(shout('hello'), 'HELLO!', reason: '--err-t1--');
  });
```

`shout('Dart')` doit être `'DART!'`

```dart
  test('test2', () {
    expect(shout('Dart'), 'DART!', reason: '--err-t2--');
  });
```

`shout(null)` doit lever une erreur

```dart
  test('test3', () {
    expect(() => shout(null), throwsA(anything), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String shout(String? text) {
  return '${text!.toUpperCase()}!';
}
```
