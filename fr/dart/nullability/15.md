---
language: dart
exerciseType: 4
---

# --description--

Deux autres opérateurs possèdent une variante null-aware.

La **cascade null-aware** `?..` exécute une chaîne d'opérations en cascade seulement quand l'objet n'est pas `null`, et les saute toutes sinon :

```dart
List<int>? numbers;
numbers?..add(1)..add(2); // nothing happens, numbers is still null
```

Le **spread null-aware** `...?` insère les éléments d'une collection nullable dans un littéral, sans rien ajouter quand la collection est `null` :

```dart
List<int>? extra;
print([0, ...?extra]); // [0]

extra = [1, 2];
print([0, ...?extra]); // [0, 1, 2]
```

Sans le `?`, `...extra` sur une `List<int>?` serait une erreur de compilation.

# --instructions--

Triez les éléments pour définir une fonction `fill` qui ajoute `1` et `2` à une liste nullable avec `?..`, puis utilisez-la dans `main`. Définissez d'abord la fonction, puis `main`. Le programme doit afficher :

```
[0]
[1, 2, 3]
```

# --answers--

```dart
void fill(List<int>? target) {
```

```dart
  target?..add(1)..add(2);
```

```dart
}
```

```dart
void main() {
```

```dart
  List<int>? numbers;
```

```dart
  print([0, ...?numbers]);
```

```dart
  numbers = [];
```

```dart
  fill(numbers);
```

```dart
  print([...?numbers, 3]);
```

```dart
}
```

# --solutions--

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  print([0, ...?numbers]);
  numbers = [];
  fill(numbers);
  print([...?numbers, 3]);
}
```

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  numbers = [];
  print([0, ...?numbers]);
  fill(numbers);
  print([...?numbers, 3]);
}
```

# --output--

[0]
[1, 2, 3]
