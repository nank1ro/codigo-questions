---
language: dart
exerciseType: 1
---

# --description--

Vérifier une valeur nullable avec `if` est plus sûr que `!`, et Dart vous le rend bien. Après une vérification comme `if (x != null)`, le compilateur sait que `x` ne peut pas être `null` à l'intérieur du bloc, donc il traite `x` comme non nullable à cet endroit. Cela s'appelle la **promotion de type** :

```dart
int twice(int? n) {
  if (n != null) {
    return n * 2; // here n is an int, no ! needed
  }
  return 0;
}
```

La promotion fonctionne aussi après un retour anticipé :

```dart
int twice(int? n) {
  if (n == null) return 0;
  return n * 2; // n is an int from here on
}
```

La promotion s'applique aux **variables locales et aux paramètres**, dont la valeur ne peut pas changer à votre insu entre la vérification et l'utilisation.

# --instructions--

Écrivez une fonction `half` qui prend un paramètre `int` nullable `n` et retourne `n` divisé par `2` avec la division entière `~/`, ou `0` quand `n` est `null`. Utilisez une vérification `if` de `null`, pas `!`.

Exemple :
```dart
print(half(10));   // 5
print(half(null)); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`half(10)` doit être `5`

```dart
  test('test1', () {
    expect(half(10), 5, reason: '--err-t1--');
  });
```

`half(7)` doit être `3`

```dart
  test('test2', () {
    expect(half(7), 3, reason: '--err-t2--');
  });
```

`half(null)` doit être `0`

```dart
  test('test3', () {
    expect(half(null), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int half(int? n) {
  if (n != null) {
    return n ~/ 2;
  }
  return 0;
}
```
