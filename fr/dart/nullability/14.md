---
language: dart
exerciseType: 1
---

# --description--

Beaucoup de fonctions de bibliothèque utilisent `null` pour signaler que quelque chose **n'a pas pu être fait**. Convertir une chaîne en nombre est l'exemple classique : `int.parse` lève une `FormatException` quand le texte n'est pas un nombre, tandis que `int.tryParse` retourne `null` à la place et vous laisse décider quoi faire :

```dart
print(int.tryParse('42'));  // 42
print(int.tryParse('4x2')); // null
print(int.tryParse(''));    // null
```

Le type de retour de `int.tryParse` est `int?`, donc tout ce que vous avez appris s'applique : `??` pour une valeur par défaut, `?.` pour chaîner, et une vérification `if` pour promouvoir. `double.tryParse` fonctionne de la même manière.

# --instructions--

Écrivez une fonction `parseOrZero` qui prend un paramètre `String` `text` et retourne l'entier qu'il contient, ou `0` quand le texte n'est pas un entier valide. Utilisez `int.tryParse`.

Exemple :
```dart
print(parseOrZero('42'));  // 42
print(parseOrZero('abc')); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parseOrZero('42')` doit être `42`

```dart
  test('test1', () {
    expect(parseOrZero('42'), 42, reason: '--err-t1--');
  });
```

`parseOrZero('-5')` doit être `-5`

```dart
  test('test2', () {
    expect(parseOrZero('-5'), -5, reason: '--err-t2--');
  });
```

`parseOrZero('abc')` et `parseOrZero('')` doivent être `0`

```dart
  test('test3', () {
    expect(parseOrZero('abc'), 0, reason: '--err-t3--');
    expect(parseOrZero(''), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int parseOrZero(String text) {
  return int.tryParse(text) ?? 0;
}
```
