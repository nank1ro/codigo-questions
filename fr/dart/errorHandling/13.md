---
language: dart
exerciseType: 1
---

# --description--

`firstWhere` retourne le premier élément correspondant à un test. Quand rien ne correspond, il n'y a pas d'élément à retourner, alors il lève une `StateError` :

```dart
final words = ['a', 'fg'];
print(words.firstWhere((w) => w.length > 3)); // Bad state: No element
```

Comme `int.tryParse`, la bibliothèque propose une porte de sortie. Le paramètre nommé `orElse` prend une fonction qui produit la valeur à utiliser quand rien ne correspondait :

```dart
print(words.firstWhere((w) => w.length > 3, orElse: () => 'none')); // none
```

Le choix est le même qu'auparavant : `orElse` quand « rien ne correspond » est un résultat ordinaire, l'appel seul quand cela signifierait que les données sont corrompues et que quelqu'un doit l'apprendre.

# --instructions--

Écrivez deux fonctions qui prennent une `List<String>` `words`.

`firstLong` retourne le premier mot de plus de trois caractères avec `firstWhere`, ou `'none'` quand il n'y en a pas, avec `orElse`.

`firstLongStrict` fait la même chose avec `firstWhere`, mais sans `orElse`, si bien qu'il lève une `StateError` quand il n'y en a pas.

Exemple :
```dart
print(firstLong(['a', 'bcde', 'fg'])); // bcde
print(firstLong(['a', 'fg'])); // none
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`firstLong(['a', 'bcde', 'fg'])` devrait retourner `'bcde'`

```dart
  test('test1', () {
    expect(firstLong(['a', 'bcde', 'fg']), 'bcde', reason: '--err-t1--');
    expect(firstLong(['long', 'longer']), 'long', reason: '--err-t1--');
  });
```

`firstLong` devrait retourner `'none'` quand aucun mot ne fait plus de trois caractères

```dart
  test('test2', () {
    expect(firstLong(['a', 'fg']), 'none', reason: '--err-t2--');
    expect(firstLong(<String>[]), 'none', reason: '--err-t2--');
    expect(firstLong(['abc']), 'none', reason: '--err-t2--');
  });
```

`firstLongStrict` devrait retourner le mot quand il y en a un et lever une `StateError` sinon

```dart
  test('test3', () {
    expect(firstLongStrict(['a', 'bcde']), 'bcde', reason: '--err-t3--');
    expect(() => firstLongStrict(['a', 'fg']), throwsStateError, reason: '--err-t3--');
    expect(() => firstLongStrict(<String>[]), throwsStateError, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String firstLong(List<String> words) {
  return words.firstWhere((w) => w.length > 3, orElse: () => 'none');
}

String firstLongStrict(List<String> words) {
  return words.firstWhere((w) => w.length > 3);
}
```
