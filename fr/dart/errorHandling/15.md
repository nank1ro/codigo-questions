---
language: dart
exerciseType: 1
---

# --description--

L'endroit où se trouve le bloc `try` décide de la quantité de travail qu'un seul échec détruit. Autour d'une boucle, le premier élément incorrect met fin à tout le lot ; **à l'intérieur** de la boucle, seul cet élément est perdu et le reste est quand même traité :

```dart
for (final text in texts) {
  try {
    total += int.parse(text);
  } on FormatException {
    continue;
  }
}
```

C'est la forme du quotidien pour importer un fichier, lire une liste de réglages ou traiter une file de messages : une ligne endommagée ne devrait pas jeter les bonnes. La règle reste la même qu'auparavant — gardez le bloc `try` autour de l'instruction qui peut échouer, et pas plus grand.

# --instructions--

Écrivez une fonction `sumOf` qui prend une `List<String>` `texts` et retourne la somme de celles qui décrivent un nombre entier, en ignorant les autres.

Utilisez `int.parse` avec un `try`/`catch` à l'intérieur de la boucle, pour qu'une entrée illisible n'arrête pas le reste.

Exemple :
```dart
print(sumOf(['1', 'x', '3'])); // 4
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumOf(['1', 'x', '3'])` devrait retourner `4`

```dart
  test('test1', () {
    expect(sumOf(['1', 'x', '3']), 4, reason: '--err-t1--');
  });
```

`sumOf(['10', '20', '30'])` devrait retourner `60`

```dart
  test('test2', () {
    expect(sumOf(['10', '20', '30']), 60, reason: '--err-t2--');
    expect(sumOf(['-5', '5']), 0, reason: '--err-t2--');
  });
```

Une liste sans nombre lisible, et une liste vide, devraient toutes deux donner `0`

```dart
  test('test3', () {
    expect(sumOf(['x', 'y', '']), 0, reason: '--err-t3--');
    expect(sumOf(<String>[]), 0, reason: '--err-t3--');
    expect(sumOf(['x', '7']), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sumOf(List<String> texts) {
  var total = 0;
  for (final text in texts) {
    try {
      total += int.parse(text);
    } on FormatException {
      continue;
    }
  }
  return total;
}
```
