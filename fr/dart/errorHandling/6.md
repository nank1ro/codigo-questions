---
language: dart
exerciseType: 4
---

# --description--

Un troisième bloc peut suivre les gestionnaires. `finally` s'exécute **dans tous les cas** : après que le bloc `try` s'est terminé normalement, après qu'un gestionnaire a rattrapé l'échec, et aussi quand rien ne correspondait et que l'échec continue de voyager vers l'extérieur.

```dart
try {
  return 'parsed ${int.parse(text)}';
} on FormatException {
  return 'failed';
} finally {
  print('done');
}
```

Il s'exécute même avant qu'un `return` ne rende sa valeur, c'est pourquoi le message ci-dessus est affiché avant que l'appelant ne voie le résultat. Cela fait de `finally` l'endroit du travail qui doit avoir lieu dans tous les cas, comme fermer ce que vous avez ouvert.

# --instructions--

Triez les éléments pour définir une fonction `attempt` qui retourne `'parsed '` suivi du nombre, ou `'failed'` quand le texte n'est pas un nombre, et affiche `done` dans un bloc `finally` dans tous les cas. Définissez la fonction avant `main`. Le programme doit afficher :

```
done
parsed 7
done
failed
```

# --answers--

```dart
String attempt(String text) {
```

```dart
  try {
    return 'parsed ${int.parse(text)}';
```

```dart
  } on FormatException {
    return 'failed';
```

```dart
  } finally {
    print('done');
  }
}
```

```dart
void main() {
```

```dart
  print(attempt('7'));
```

```dart
  print(attempt('x'));
```

```dart
}
```

# --solutions--

```dart
String attempt(String text) {
  try {
    return 'parsed ${int.parse(text)}';
  } on FormatException {
    return 'failed';
  } finally {
    print('done');
  }
}

void main() {
  print(attempt('7'));
  print(attempt('x'));
}
```

# --output--

done
parsed 7
done
failed
