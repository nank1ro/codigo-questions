---
language: dart
exerciseType: 1
---

# --description--

Le corps de la classe se trouve tout à la fin de la chaîne, donc un membre déclaré dans la classe **redéfinit** le même membre venant de n'importe lequel de ses mixins. À l'intérieur de la redéfinition, **`super`** atteint la version fournie par le mixin :

```dart
mixin Polite {
  String greet() => 'Hello';
}

class Host with Polite {
  @override
  String greet() => '${super.greet()}, welcome!';
}

class Guest with Polite {}

void main() {
  print(Host().greet());  // Hello, welcome!
  print(Guest().greet()); // Hello
}
```

Le mixin lui-même reste intact : `Guest` obtient toujours le `greet` d'origine. `super.greet()` est ce qui permet à `Host` de s'appuyer sur le comportement partagé au lieu de le copier.

# --instructions--

Déclarez un mixin `Signed` avec une méthode `sign` retournant `'signed'`.

Déclarez une classe `Letter` qui utilise `Signed` et redéfinit `sign` pour qu'elle retourne le résultat du mixin suivi de `' by Ada'`, obtenu en appelant `super`.

Déclarez une classe `Note` qui utilise `Signed` et ne redéfinit rien.

Exemple :
```dart
print(Letter().sign()); // signed by Ada
print(Note().sign());   // signed
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Letter().sign()` doit valoir `'signed by Ada'`

```dart
  test('test1', () {
    expect(Letter().sign(), 'signed by Ada', reason: '--err-t1--');
  });
```

`Note().sign()` doit toujours valoir `'signed'`, car le mixin est inchangé

```dart
  test('test2', () {
    expect(Note().sign(), 'signed', reason: '--err-t2--');
  });
```

Les deux classes doivent utiliser le mixin, et la redéfinition doit aussi être utilisée à travers le type du mixin

```dart
  test('test3', () {
    final Signed letter = Letter();
    expect(letter.sign(), 'signed by Ada', reason: '--err-t3--');
    expect(Note(), isA<Signed>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Signed {
  String sign() => 'signed';
}

class Letter with Signed {
  @override
  String sign() => '${super.sign()} by Ada';
}

class Note with Signed {}
```
