---
language: dart
exerciseType: 2
---

# --description--

Les listes en Dart sont **indexées à partir de zéro**, ce qui signifie que le premier élément est à l'index `0`, le second à l'index `1`, et ainsi de suite.

```dart
var colors = ['red', 'green', 'blue'];
print(colors[0]); // red
print(colors[2]); // blue
```

L'accès à un élément par index se fait avec la syntaxe `list[index]`.

# --instructions--

Complétez le code pour afficher le deuxième élément de la liste.

# --seed--

```dart
var animals = ['cat', 'dog', 'bird'];
print(animals[/]);
```

# --answers--

- [1]
- [0]
- [2]
- .get(1)
- (1)
- [3]

# --solutions--

```dart
var animals = ['cat', 'dog', 'bird'];
print(animals[1]);
```

# --output--

dog
