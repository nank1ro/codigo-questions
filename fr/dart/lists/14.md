---
language: dart
exerciseType: 1
---

# --description--

La propriété `.isEmpty` vaut `true` lorsqu'une liste n'a aucun élément, et `.isNotEmpty` vaut `true` lorsqu'elle en a au moins un.

```dart
var colors = <String>[];
print(colors.isEmpty); // true
colors.add('red');
print(colors.isNotEmpty); // true
```

# --instructions--

Créez une `List<int>` vide. Stockez dans un booléen si elle est `.isEmpty`, puis ajoutez la valeur `1` et stockez dans un second booléen si elle est `.isNotEmpty`. Retournez les deux booléens, dans cet ordre, sous forme de `List<bool>`.

# --before-seed--

```dart
List<bool> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

La première valeur est true (était vide)

```dart
  test('first is true', () {
    expect(computeValue()[0], true, reason: '--err-t1--');
  });
```

La deuxième valeur est true (n'est plus vide après l'ajout)

```dart
  test('second is true', () {
    expect(computeValue()[1], true, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<int> nums = [];
  bool wasEmpty = nums.isEmpty;
  nums.add(1);
  bool nowNotEmpty = nums.isNotEmpty;
  List<bool> result = [wasEmpty, nowNotEmpty];
```
