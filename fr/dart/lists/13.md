---
language: dart
exerciseType: 1
---

# --description--

Les propriétés `.first` et `.last` vous donnent le premier et le dernier élément d'une liste sans utiliser d'index.

```dart
var colors = ['red', 'green', 'blue'];
print(colors.first); // red
print(colors.last); // blue
```

Les deux lèvent une erreur si la liste est vide.

# --instructions--

Étant donné la liste `[100, 200, 300, 400, 500]`, retournez une `List<int>` où l'index `0` est l'élément `.first` et l'index `1` est l'élément `.last`.

# --before-seed--

```dart
List<int> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Le premier élément est 100

```dart
  test('first is 100', () {
    expect(computeValue()[0], 100, reason: '--err-t1--');
  });
```

Le dernier élément est 500

```dart
  test('last is 500', () {
    expect(computeValue()[1], 500, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var nums = [100, 200, 300, 400, 500];
  List<int> result = [nums.first, nums.last];
```
