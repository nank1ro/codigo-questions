---
language: dart
exerciseType: 1
---

# --instructions--

Commencez avec une `List<int>` contenant `[1, 2, 3]`. Ajoutez la valeur `4` avec `.add()`. Retournez la liste.

# --before-seed--

```dart
List<int> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

La liste contient l'élément ajouté

```dart
  test('list is [1, 2, 3, 4]', () {
    expect(computeValue(), [1, 2, 3, 4], reason: '--err-t1--');
  });
```

La longueur est 4

```dart
  test('length is 4', () {
    expect(computeValue().length, 4, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<int> result = [1, 2, 3];
  result.add(4);
```
