---
language: dart
exerciseType: 1
---

# --instructions--
Écrivez une fonction qui retourne la somme de tous les entiers de 1 à `n` (inclus) à l'aide d'une boucle `for`.

# --before-seed--
```dart
int sumTo(int n) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
La somme de 1 à 5 est égale à 15
```dart
    test('sum 1..5', () {
      expect(sumTo(5), 15, reason: '--err-t1--');
    });
```

La somme de 1 à 1 est égale à 1
```dart
    test('sum 1..1', () {
      expect(sumTo(1), 1, reason: '--err-t2--');
    });
```

La somme de 1 à 10 est égale à 55
```dart
    test('sum 1..10', () {
      expect(sumTo(10), 55, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = 1; i <= n; i++) {
    result += i;
  }
```
