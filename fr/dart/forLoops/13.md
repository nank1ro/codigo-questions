---
language: dart
exerciseType: 1
---

# --instructions--
Écrivez une fonction qui retourne le premier nombre négatif trouvé dans une `List<int>`. Utilisez une boucle `for` avec `break` pour s'arrêter dès qu'un nombre négatif est trouvé. Retournez `0` s'il n'y a pas de nombre négatif.

# --before-seed--
```dart
int firstNegative(List<int> numbers) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Trouve le premier nombre négatif
```dart
    test('finds first negative', () {
      expect(firstNegative([3, -5, -2, 1]), -5, reason: '--err-t1--');
    });
```

Retourne 0 quand il n'y a pas de nombre négatif
```dart
    test('no negative', () {
      expect(firstNegative([1, 2, 3]), 0, reason: '--err-t2--');
    });
```

Le premier élément est négatif
```dart
    test('first is negative', () {
      expect(firstNegative([-7, 4, 2]), -7, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = 0; i < numbers.length; i++) {
    if (numbers[i] < 0) {
      result = numbers[i];
      break;
    }
  }
```
