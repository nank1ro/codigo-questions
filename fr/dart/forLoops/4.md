---
language: dart
exerciseType: 2
---

# --description--
La boucle `for-in` itère sur chaque élément d'une collection sans avoir besoin d'un index :

```dart
List<String> fruits = ['apple', 'banana', 'cherry'];
for (var fruit in fruits) {
  print(fruit);
}
```

À chaque itération, l'élément suivant est assigné à la variable de boucle (`fruit` ici).

# --instructions--
Complétez la boucle `for-in` qui affiche chaque nombre de la liste.

# --seed--
```dart
List<int> numbers = [10, 20, 30];
for ([/] n [/] numbers) {
  [/](n);
}
```

# --answers--

- var
- in
- print
- int
- of
- write
- final
- println

# --solutions--
```dart
List<int> numbers = [10, 20, 30];
for (var n in numbers) {
  print(n);
}
```

# --output--
10
20
30
