---
language: dart
exerciseType: 1
---

# --instructions--
Écrivez une fonction qui retourne le nombre de chiffres pairs dans la plage de `start` à `end` (les deux inclus).

# --before-seed--
```dart
int countEvens(int start, int end) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
La plage de 1 à 10 contient 5 nombres pairs
```dart
    test('1 to 10', () {
      expect(countEvens(1, 10), 5, reason: '--err-t1--');
    });
```

La plage de 2 à 6 contient 3 nombres pairs
```dart
    test('2 to 6', () {
      expect(countEvens(2, 6), 3, reason: '--err-t2--');
    });
```

La plage de 1 à 1 contient 0 nombre pair
```dart
    test('1 to 1', () {
      expect(countEvens(1, 1), 0, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = start; i <= end; i++) {
    if (i % 2 == 0) result++;
  }
```
