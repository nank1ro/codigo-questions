---
language: dart
exerciseType: 1
---

# --instructions--
Écrivez une fonction qui prend une `List<int>` et retourne une nouvelle liste où chaque élément est multiplié par 2.

# --before-seed--
```dart
List<int> doubleElements(List<int> items) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Double chaque élément
```dart
    test('doubles elements', () {
      expect(doubleElements([1, 2, 3]), [2, 4, 6], reason: '--err-t1--');
    });
```

Une liste vide retourne une liste vide
```dart
    test('empty list', () {
      expect(doubleElements([]), [], reason: '--err-t2--');
    });
```

Liste avec un seul élément
```dart
    test('single element', () {
      expect(doubleElements([5]), [10], reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  List<int> result = [];
  for (var item in items) {
    result.add(item * 2);
  }
```
