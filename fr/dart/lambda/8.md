---
language: dart
exerciseType: 1
---

# --description--

Comme `map` et `where` retournent tous deux un `Iterable`, leurs appels peuvent être **chaînés** l'un après l'autre. Chaque étape reçoit le résultat de la précédente, et `toList()` est appelé une fois à la fin :

```dart
var numbers = [1, 2, 3, 4, 5, 6];

var result = numbers.where((n) => n > 3).map((n) => n * 10).toList();
print(result); // [40, 50, 60]
```

# --instructions--

Écrivez une fonction `oddSquares` qui prend un paramètre `List<int>` `numbers` et retourne une `List<int>` avec les carrés des nombres impairs uniquement, dans l'ordre d'origine. Utilisez `where` et `map` avec des fonctions anonymes.

Exemple :
```dart
print(oddSquares([1, 2, 3, 4, 5])); // [1, 9, 25]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`oddSquares([1, 2, 3, 4, 5])` devrait être `[1, 9, 25]`

```dart
  test('test1', () {
    expect(oddSquares([1, 2, 3, 4, 5]), [1, 9, 25], reason: '--err-t1--');
  });
```

`oddSquares([2, 4, 6])` devrait être `[]`

```dart
  test('test2', () {
    expect(oddSquares([2, 4, 6]), [], reason: '--err-t2--');
  });
```

`oddSquares([7, 10, 3])` devrait être `[49, 9]`

```dart
  test('test3', () {
    expect(oddSquares([7, 10, 3]), [49, 9], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> oddSquares(List<int> numbers) {
  return numbers.where((n) => n % 2 != 0).map((n) => n * n).toList();
}
```
