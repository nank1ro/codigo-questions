---
language: dart
exerciseType: 4
---

# --description--

Les fonctions anonymes prennent en charge les mêmes types de paramètres que les fonctions nommées : les paramètres **positionnels optionnels** entre `[]` et les paramètres **nommés** entre `{}`, tous deux avec des valeurs par défaut :

```dart
var repeat = (String text, [int times = 2]) => text * times;
print(repeat('ab'));    // abab
print(repeat('ab', 3)); // ababab

var describe = ({required String item, int count = 1}) => '$count x $item';
print(describe(item: 'apple'));           // 1 x apple
print(describe(item: 'pear', count: 4));  // 4 x pear
```

# --instructions--

Triez les éléments pour stocker dans `greet` une fonction anonyme avec un paramètre nommé `name` dont la valeur par défaut est `'World'`, puis appelez-la d'abord sans arguments, puis avec `name: 'Dart'`. Le programme doit afficher :

```
Hello, World!
Hello, Dart!
```

# --answers--

```dart
void main() {
```

```dart
  var greet = ({String name = 'World'}) {
```

```dart
    print('Hello, $name!');
```

```dart
  };
```

```dart
  greet();
```

```dart
  greet(name: 'Dart');
```

```dart
}
```

# --solutions--

```dart
void main() {
  var greet = ({String name = 'World'}) {
    print('Hello, $name!');
  };
  greet();
  greet(name: 'Dart');
}
```

# --output--

Hello, World!
Hello, Dart!
