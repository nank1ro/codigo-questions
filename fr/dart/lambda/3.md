---
language: dart
exerciseType: 2
---

# --description--

Lorsque le corps est une seule expression, une fonction anonyme peut utiliser la **syntaxe fléchée** `=>`, tout comme une fonction nommée. La flèche remplace les accolades et le mot-clé `return` :

```dart
var add = (int a, int b) => a + b;

print(add(2, 3)); // 5
```

Cette forme courte est de loin la façon la plus courante d'écrire des fonctions anonymes en Dart.

# --instructions--

Complétez le code pour stocker dans `triple` une fonction fléchée qui retourne son argument multiplié par `3`, afin que le programme affiche `12`.

# --seed--

```dart
void main() {
  var triple = (int n) [/] n [/] 3;
  print(triple(4));
}
```

# --answers--

- =>
- *
- ->
- +
- return
- {

# --solutions--

```dart
void main() {
  var triple = (int n) => n * 3;
  print(triple(4));
}
```

# --output--

12
