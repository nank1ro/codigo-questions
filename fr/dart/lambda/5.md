---
language: dart
exerciseType: 1
---

# --description--

Comme un type de fonction est un type normal, une fonction peut prendre **une autre fonction en paramètre**. Dans le corps, le paramètre est appelé comme n'importe quelle fonction :

```dart
int apply(int n, int Function(int) operation) {
  return operation(n);
}

print(apply(5, (n) => n * 2)); // 10
print(apply(5, (n) => n - 1)); // 4
```

Ici, l'appelant décide de ce que fait `apply` en passant une fonction anonyme comme second argument.

# --instructions--

Écrivez une fonction `applyTwice` qui prend un paramètre `int` `n` et un paramètre de fonction `operation` de type `int Function(int)`, et retourne le résultat de l'application de `operation` à `n` deux fois.

Exemple :
```dart
print(applyTwice(3, (n) => n * 2)); // 12
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`applyTwice(3, (n) => n * 2)` devrait être `12`

```dart
  test('test1', () {
    expect(applyTwice(3, (n) => n * 2), 12, reason: '--err-t1--');
  });
```

`applyTwice(10, (n) => n + 5)` devrait être `20`

```dart
  test('test2', () {
    expect(applyTwice(10, (n) => n + 5), 20, reason: '--err-t2--');
  });
```

`applyTwice(2, (n) => n * n)` devrait être `16`

```dart
  test('test3', () {
    expect(applyTwice(2, (n) => n * n), 16, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int applyTwice(int n, int Function(int) operation) {
  return operation(operation(n));
}
```
