---
language: dart
exerciseType: 1
---

# --description--

Une fonction anonyme n'a pas besoin d'être stockée quelque part : elle peut être **appelée immédiatement** en écrivant les arguments juste après son accolade fermante. C'est pratique pour calculer une valeur avec quelques variables temporaires qui ne doivent pas fuiter dans le code environnant :

```dart
var area = (double radius) {
  var pi = 3.14;
  return pi * radius * radius;
}(2.0);

print(area); // 12.56
```

Ici, `pi` n'existe qu'à l'intérieur de la fonction anonyme, et `area` reçoit la valeur retournée.

# --instructions--

Une variable `int` `limit` est déjà définie. Déclarez une variable `sum` initialisée en invoquant immédiatement une fonction anonyme sans paramètres qui additionne tous les entiers de `1` à `limit` avec une boucle et retourne le total.

# --before-seed--

```dart
dynamic result(int limit) {
```

# --seed--

```dart
var sum = 
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` devrait être un `int`

```dart
  test('test1', () {
    expect(result(100), isA<int>(), reason: '--err-t1--');
  });
```

`sum` devrait être `5050` lorsque `limit` vaut `100`

```dart
  test('test2', () {
    expect(result(100), 5050, reason: '--err-t2--');
  });
```

`sum` devrait être `55` lorsque `limit` vaut `10`

```dart
  test('test3', () {
    expect(result(10), 55, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var sum = () {
  var total = 0;
  for (var i = 1; i <= limit; i++) {
    total += i;
  }
  return total;
}();
```
