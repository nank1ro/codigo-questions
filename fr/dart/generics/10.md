---
language: dart
exerciseType: 1
---

# --description--

Un `T` sans borne pourrait être n'importe quoi, donc à l'intérieur du corps vous ne pouvez utiliser que ce que tout objet possède. Ceci ne compile pas :

```dart
T twice<T>(T value) => value + value; // error: + is not defined for T
```

Une **borne** règle le problème. Écrire `T extends num` dit que « `T` ne peut être qu'un nombre », et en échange le corps peut utiliser tout ce qu'un `num` offre :

```dart
T twice<T extends num>(T value) => (value + value) as T;

num half<T extends num>(T value) => value / 2;
```

La borne est vérifiée au point d'appel : `half(4)` et `half(2.5)` passent, `half('fig')` est une erreur de compilation. Une borne est une promesse dans les deux sens, des arguments plus restreints contre plus de puissance à l'intérieur.

# --instructions--

Écrivez une fonction générique `total` avec un paramètre de type `T` borné par `num` qui prend une `List<T>` `values` et retourne la somme des éléments sous forme de `num`. Une liste vide donne `0`.

Exemple :
```dart
print(total([1, 2, 3]));  // 6
print(total([1.5, 2.5])); // 4.0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total([1, 2, 3])` devrait retourner `6`

```dart
  test('test1', () {
    expect(total([1, 2, 3]), 6, reason: '--err-t1--');
  });
```

`total` devrait aussi fonctionner avec des doubles, donc `total([1.5, 2.5])` devrait retourner `4.0`, et elle devrait accepter un argument de type explicite comme `total<int>([1, 2])`

```dart
  test('test2', () {
    expect(total([1.5, 2.5]), 4.0, reason: '--err-t2--');
    expect(total([10, 20, 30, 40]), 100, reason: '--err-t2--');
    expect(total<int>([1, 2]), 3, reason: '--err-t2--');
    expect(total<double>([1.5, 1.5]), 3.0, reason: '--err-t2--');
  });
```

`total` devrait retourner `0` pour une liste vide

```dart
  test('test3', () {
    expect(total(<int>[]), 0, reason: '--err-t3--');
    expect(total(<double>[]), 0, reason: '--err-t3--');
    expect(total([-5, 5, 7]), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
num total<T extends num>(List<T> values) {
  num sum = 0;
  for (final value in values) {
    sum += value;
  }
  return sum;
}
```
