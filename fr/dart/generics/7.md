---
language: dart
exerciseType: 1
---

# --description--

Une classe peut déclarer plusieurs paramètres de type, séparés par des virgules. `Map<K, V>` est l'exemple intégré : un type pour les clés, un pour les valeurs.

```dart
class Entry<K, V> {
  final K key;
  final V value;

  Entry(this.key, this.value);
}

final e = Entry<String, int>('age', 30);
```

L'**ordre** fait partie du type : `Entry<String, int>` et `Entry<int, String>` sont des types sans lien, et une valeur de l'un ne peut pas être affectée à l'autre. Les paramètres de type peuvent aussi être réordonnés dans un type de retour, ce qui permet à une méthode de rendre une version inversée de l'objet :

```dart
Entry<V, K> get flipped => Entry(value, key);
```

# --instructions--

Déclarez une classe générique `Pair` avec deux paramètres de type `A` et `B`, un champ `final` `first` de type `A` et un champ `final` `second` de type `B`, tous deux définis par le constructeur, et un getter `swapped` qui retourne une `Pair<B, A>` contenant les deux mêmes valeurs dans l'ordre opposé.

Exemple :
```dart
final p = Pair<String, int>('age', 30);
print(p.first);         // age
print(p.swapped.first); // 30
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Pair<String, int>('age', 30)` devrait avoir `first` égal à `'age'` et `second` égal à `30`

```dart
  test('test1', () {
    final pair = Pair<String, int>('age', 30);
    expect(pair.first, 'age', reason: '--err-t1--');
    expect(pair.second, 30, reason: '--err-t1--');
  });
```

`swapped` devrait retourner une `Pair<int, String>` avec les valeurs échangées

```dart
  test('test2', () {
    final swapped = Pair<String, int>('age', 30).swapped;
    expect(swapped, isA<Pair<int, String>>(), reason: '--err-t2--');
    expect(swapped.first, 30, reason: '--err-t2--');
    expect(swapped.second, 'age', reason: '--err-t2--');
  });
```

`Pair` devrait fonctionner avec deux types quelconques, et échanger deux fois devrait redonner les valeurs d'origine

```dart
  test('test3', () {
    final pair = Pair<bool, String>(true, 'yes');
    expect(pair.swapped.first, 'yes', reason: '--err-t3--');
    expect(pair.swapped.second, true, reason: '--err-t3--');
    expect(pair.swapped.swapped, isA<Pair<bool, String>>(), reason: '--err-t3--');
    expect(pair.swapped.swapped.first, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Pair<A, B> {
  final A first;
  final B second;

  Pair(this.first, this.second);

  Pair<B, A> get swapped => Pair(second, first);
}
```
