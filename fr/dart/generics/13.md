---
language: dart
exerciseType: 4
---

# --description--

La même borne fonctionne tout aussi bien pour le plus petit élément : seul le signe de la comparaison change. `compareTo` retourne un nombre négatif quand le récepteur vient en premier, donc `item.compareTo(best) < 0` signifie que celui-ci est plus petit.

# --instructions--

Triez les éléments pour définir une fonction générique `smallest` bornée par `Comparable<T>` qui retourne le plus petit élément d'une liste, et un `main` qui affiche le plus petit de `['pear', 'fig', 'kiwi']`. Définissez la fonction avant `main`. Le programme doit afficher :

```
fig
```

# --answers--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
```

```dart
  var best = items.first;
```

```dart
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
```

```dart
  return best;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(smallest(['pear', 'fig', 'kiwi']));
```

```dart
}
```

# --solutions--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
  return best;
}

void main() {
  print(smallest(['pear', 'fig', 'kiwi']));
}
```

# --output--

fig
