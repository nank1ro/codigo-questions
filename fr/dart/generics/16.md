---
language: dart
exerciseType: 1
---

# --description--

Un `typedef` donne un nom à un type, et il peut prendre ses propres paramètres de type. La raison habituelle est de nommer une famille de types de fonctions une bonne fois pour toutes au lieu de l'énoncer à chaque utilisation :

```dart
typedef Transform<I, O> = O Function(I input);

final Transform<String, int> length = (word) => word.length;
print(length('kiwi')); // 4
```

`Transform<String, int>` n'est qu'une autre façon d'écrire `int Function(String)`, les deux sont donc interchangeables. Le gain, c'est la lisibilité : un paramètre déclaré `Transform<I, O> transform` dit à quoi la fonction sert, tandis que `O Function(I)` dit seulement à quoi elle ressemble.

Un typedef générique et une fonction générique se combinent naturellement, les paramètres de type de la fonction remplissant ceux du typedef.

# --instructions--

Déclarez un typedef générique `Transform` avec deux paramètres de type `I` et `O` qui nomme une fonction prenant un `I` et retournant un `O`.

Écrivez ensuite une fonction générique `convertAll` avec les paramètres de type `I` et `O` qui prend une `List<I>` `items` et une `Transform<I, O>` `transform`, et retourne une `List<O>` contenant chaque élément converti, dans l'ordre.

Exemple :
```dart
print(convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)); // [3, 4]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)` devrait retourner `[3, 4]`

```dart
  test('test1', () {
    expect(
      convertAll<String, int>(['fig', 'kiwi'], (w) => w.length),
      [3, 4],
      reason: '--err-t1--',
    );
  });
```

`convertAll` devrait retourner une `List<O>`, donc convertir des ints en chaînes devrait donner une `List<String>`

```dart
  test('test2', () {
    final result = convertAll<int, String>([1, 2], (n) => 'n$n');
    expect(result, ['n1', 'n2'], reason: '--err-t2--');
    expect(result, isA<List<String>>(), reason: '--err-t2--');
  });
```

Le typedef `Transform` devrait nommer un type de fonction, et une liste vide devrait donner une liste vide

```dart
  test('test3', () {
    final Transform<int, int> twice = (n) => n * 2;
    expect(convertAll<int, int>([1, 2, 3], twice), [2, 4, 6], reason: '--err-t3--');
    expect(convertAll<String, int>(<String>[], (w) => w.length), <int>[], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
typedef Transform<I, O> = O Function(I input);

List<O> convertAll<I, O>(List<I> items, Transform<I, O> transform) {
  return items.map(transform).toList();
}
```
