---
language: dart
exerciseType: 1
---

# --description--

Une borne peut mentionner le paramètre de type lui-même. `Comparable<T>` est l'interface de tout ce qui sait se comparer à ses semblables, via `compareTo` :

```dart
print('fig'.compareTo('kiwi')); // négatif : fig arrive en premier
print('kiwi'.compareTo('fig')); // positive
print('fig'.compareTo('fig'));  // zero
```

Ainsi, `T extends Comparable<T>` se lit « tout type qui peut être comparé à lui-même », exactement ce dont une fonction de tri ou de maximum a besoin :

```dart
T maxOf<T extends Comparable<T>>(T a, T b) => a.compareTo(b) >= 0 ? a : b;

print(maxOf('fig', 'kiwi')); // kiwi
```

`String` et `DateTime` la satisfont directement. `int` et `double` implémentent `Comparable<num>`, donc une liste de nombres est simplement comparée comme des `num`.

# --instructions--

Écrivez une fonction générique `largest` avec un paramètre de type `T` borné par `Comparable<T>` qui prend une `List<T>` `items` non vide et retourne le plus grand élément, en comparant avec `compareTo`.

Exemple :
```dart
print(largest(['fig', 'kiwi', 'apple'])); // kiwi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest(['fig', 'kiwi', 'apple'])` devrait retourner `'kiwi'`

```dart
  test('test1', () {
    expect(largest(['fig', 'kiwi', 'apple']), 'kiwi', reason: '--err-t1--');
  });
```

`largest` devrait fonctionner avec des nombres et avec des dates aussi

```dart
  test('test2', () {
    expect(largest([3, 9, 5]), 9, reason: '--err-t2--');
    expect(
      largest([DateTime(2020), DateTime(2024), DateTime(2001)]),
      DateTime(2024),
      reason: '--err-t2--',
    );
  });
```

`largest` devrait retourner le seul élément d'une liste à un élément, le dernier quand c'est le plus grand, et elle devrait accepter un argument de type explicite comme `largest<String>(['a', 'z'])`

```dart
  test('test3', () {
    expect(largest<String>(['a', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['only']), 'only', reason: '--err-t3--');
    expect(largest(['a', 'b', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['z', 'b', 'a']), 'z', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
T largest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) > 0) {
      best = item;
    }
  }
  return best;
}
```
