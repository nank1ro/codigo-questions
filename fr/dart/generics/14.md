---
language: dart
exerciseType: 1
---

# --description--

Une classe générique peut avoir des constructeurs nommés et **factory** comme n'importe quelle autre classe, et le paramètre de type y est disponible. Un constructeur factory ne crée pas l'objet lui-même : il exécute un corps et en retourne un, ce qui lui permet de choisir, réutiliser ou construire l'instance comme bon lui semble.

```dart
class Box<T> {
  final T value;

  Box(this.value);

  factory Box.first(List<T> items) => Box(items.first);
}

final b = Box<int>.first([5, 6]);
print(b.value); // 5
```

L'argument de type se place sur la classe, pas sur le nom du constructeur : `Box<int>.first(...)`. À l'intérieur de la factory, `<T>[]` est une véritable `List<T>` vide, donc une factory est l'endroit tout indiqué pour construire une valeur par défaut pour un type que vous ne connaissez pas encore.

# --instructions--

Déclarez une classe générique `Stack` avec un paramètre de type `T`, un champ `final` `items` de type `List<T>` défini par le constructeur normal, un constructeur **factory** `Stack.empty()` qui construit une `Stack` avec une liste vide, et un getter `top` de type `T?` retournant le dernier élément, ou `null` quand la pile est vide.

Exemple :
```dart
print(Stack<int>([1, 2, 3]).top);  // 3
print(Stack<String>.empty().top);  // null
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Stack<int>([1, 2, 3]).top` devrait valoir `3`

```dart
  test('test1', () {
    final stack = Stack<int>([1, 2, 3]);
    expect(stack.top, 3, reason: '--err-t1--');
    expect(stack.items, [1, 2, 3], reason: '--err-t1--');
  });
```

`Stack.empty()` devrait construire une pile vide du bon type, dont `top` vaut `null`

```dart
  test('test2', () {
    final stack = Stack<String>.empty();
    expect(stack, isA<Stack<String>>(), reason: '--err-t2--');
    expect(stack.items, <String>[], reason: '--err-t2--');
    expect(stack.top, null, reason: '--err-t2--');
  });
```

La liste construite par `Stack.empty()` devrait rester utilisable

```dart
  test('test3', () {
    final stack = Stack<int>.empty();
    stack.items.add(4);
    stack.items.add(9);
    expect(stack.top, 9, reason: '--err-t3--');
    expect(Stack<bool>([true]).top, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Stack<T> {
  final List<T> items;

  Stack(this.items);

  factory Stack.empty() => Stack(<T>[]);

  T? get top => items.isEmpty ? null : items.last;
}
```
