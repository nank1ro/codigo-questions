---
language: dart
exerciseType: 4
---

# --description--

À l'intérieur d'une classe générique, le paramètre de type est visible partout : dans les champs, dans les paramètres du constructeur, dans les signatures des méthodes et dans leurs corps. Il est déclaré une seule fois, à côté du nom de la classe, et chaque membre peut l'utiliser.

```dart
class Holder<T> {
  final T item;

  Holder(this.item);

  String describe() => 'holding $item';
}
```

C'est la création de l'objet qui décide du type : `Holder<String>('fig')` fait de `item` un `String`, `Holder<int>(3)` en fait un `int`.

# --instructions--

Triez les éléments pour définir une classe générique `Holder` avec un champ `item` de type `T`, un constructeur, et une méthode `describe`, puis un `main` qui affiche la description d'une `Holder<String>` construite avec `'fig'`. Définissez la classe avant `main`. Le programme doit afficher :

```
holding fig
```

# --answers--

```dart
class Holder<T> {
  final T item;
  Holder(this.item);
```

```dart
  String describe() => 'holding $item';
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Holder<String>('fig').describe());
```

```dart
}
```

# --solutions--

```dart
class Holder<T> {
  final T item;
  Holder(this.item);
  String describe() => 'holding $item';
}

void main() {
  print(Holder<String>('fig').describe());
}
```

# --output--

holding fig
