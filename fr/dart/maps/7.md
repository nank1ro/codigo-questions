---
language: dart
exerciseType: 1
---

# --description--

La propriété `.keys` donne toutes les clés d'une Map et `.values` donne toutes les valeurs, dans l'ordre d'insertion. Ce sont des `Iterable`s paresseux, donc appelez `.toList()` quand vous avez besoin d'une vraie `List` :

```dart
var ages = {'Ann': 30, 'Bob': 25};
List<String> names = ages.keys.toList();
List<int> years = ages.values.toList();
print(names); // [Ann, Bob]
print(years); // [30, 25]
```

# --instructions--

Pour la Map `scores`, créez une `List<String>` nommée `subjects` avec ses clés et une `List<int>` nommée `points` avec ses valeurs.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var scores = {'math': 90, 'art': 75, 'gym': 60};
```

# --after-seed--

```dart
  return [subjects, points];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`subjects` devrait être `['math', 'art', 'gym']`

```dart
  test('test1', () {
    expect(result()[0], ['math', 'art', 'gym'], reason: '--err-t1--');
  });
```

`points` devrait être `[90, 75, 60]`

```dart
  test('test2', () {
    expect(result()[1], [90, 75, 60], reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var scores = {'math': 90, 'art': 75, 'gym': 60};
List<String> subjects = scores.keys.toList();
List<int> points = scores.values.toList();
```
