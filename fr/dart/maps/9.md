---
language: dart
exerciseType: 1
---

# --description--

Une Map littérale vide `{}` n'a aucune paire permettant d'inférer les types, donnez-lui donc des types explicites avec `<K, V>{}` ou avec une annotation de type :

```dart
var cart = <String, int>{};
Map<String, int> other = {};
```

La propriété `.isEmpty` vaut `true` quand une Map n'a aucune paire, et `.isNotEmpty` vaut `true` quand elle en a au moins une :

```dart
print(cart.isEmpty); // true
cart['pen'] = 2;
print(cart.isNotEmpty); // true
```

# --instructions--

Créez une `Map<String, int>` vide nommée `cart`. Stockez si elle est `.isEmpty` dans un `bool` nommé `wasEmpty`, puis ajoutez la clé `'pen'` avec la valeur `2` et stockez si elle est `.isNotEmpty` dans un `bool` nommé `hasItems`.

# --before-seed--

```dart
dynamic result() {
```

# --after-seed--

```dart
  return [wasEmpty, hasItems, cart];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`wasEmpty` devrait être `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasItems` devrait être `true`

```dart
  test('test2', () {
    expect(result()[1], true, reason: '--err-t2--');
  });
```

`cart` devrait être `{'pen': 2}`

```dart
  test('test3', () {
    expect(result()[2], {'pen': 2}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var cart = <String, int>{};
bool wasEmpty = cart.isEmpty;
cart['pen'] = 2;
bool hasItems = cart.isNotEmpty;
```
