---
language: dart
exerciseType: 1
---

# --description--

Une Map n'est pas un `Iterable`, vous ne pouvez donc pas la parcourir directement avec `for-in`. Parcourez plutôt `.entries` : chaque élément est un `MapEntry` avec une `.key` et une `.value` :

```dart
var ages = {'Ann': 30, 'Bob': 25};
for (var entry in ages.entries) {
  print('${entry.key}: ${entry.value}');
}
// Ann: 30
// Bob: 25
```

# --instructions--

Pour la Map `ages`, parcourez ses `.entries` avec `for-in` et ajoutez à une `List<String>` nommée `lines` une chaîne par paire de la forme `'Ann is 30'`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
```

# --after-seed--

```dart
  return lines;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`lines` devrait être `['Ann is 30', 'Bob is 25', 'Cid is 41']`

```dart
  test('test1', () {
    expect(result(), ['Ann is 30', 'Bob is 25', 'Cid is 41'], reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
List<String> lines = [];
for (var entry in ages.entries) {
  lines.add('${entry.key} is ${entry.value}');
}
```
