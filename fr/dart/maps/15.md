---
language: dart
exerciseType: 1
---

# --description--

La méthode `.putIfAbsent(key, ifAbsent)` ajoute une paire **seulement si** la clé n'est pas encore dans la Map. Le deuxième argument est une fonction qui produit la valeur. Si la clé existe déjà, la Map reste inchangée. Dans les deux cas, la valeur désormais stockée sous la clé est renvoyée :

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99); // Ann est déjà présente, rien ne change
ages.putIfAbsent('Bob', () => 25); // Bob est ajouté
print(ages); // {Ann: 30, Bob: 25}
```

# --instructions--

Pour la Map `ages`, utilisez `.putIfAbsent()` deux fois : d'abord pour la clé `'Ann'` avec la valeur `99`, puis pour la clé `'Bob'` avec la valeur `25`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var ages = {'Ann': 30};
```

# --after-seed--

```dart
  return ages;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`ages` devrait être `{'Ann': 30, 'Bob': 25}`

```dart
  test('test1', () {
    expect(result(), {'Ann': 30, 'Bob': 25}, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99);
ages.putIfAbsent('Bob', () => 25);
```
