---
language: dart
exerciseType: 4
---

# --description--

Un stream peut transporter des **erreurs** aussi bien que des valeurs. À l'intérieur d'un générateur `async*`, un `throw` envoie un événement d'erreur et termine le stream ; un `StreamController` peut en envoyer un avec `addError`.

Du côté consommation, une boucle `await for` relance l'erreur à l'endroit de la boucle, tu la gères donc avec un `try`/`catch` ordinaire autour de la boucle :

```dart
Stream<int> risky() async* {
  yield 1;
  throw StateError('sensor offline');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('caught: $e');
  }
}
// 1
// caught: Bad state: sensor offline
```

Avec `listen`, passe plutôt une callback `onError` : `stream.listen(print, onError: (e) => print('caught: $e'));`

# --instructions--

Trie les éléments pour définir une fonction `async*` `risky` qui émet `1` puis lève une `FormatException('bad')`, et un `main` `async` qui affiche chaque valeur de `risky()` dans un `try`/`catch`, affiche `error: ` suivi de l'erreur quand il l'attrape, et affiche `finished` après tout le bloc `try`/`catch`, en dehors de celui-ci. Définis `risky` avant `main`. Le programme doit afficher :

```
1
error: FormatException: bad
finished
```

# --answers--

```dart
Stream<int> risky() async* {
```

```dart
  yield 1;
```

```dart
  throw FormatException('bad');
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  try {
```

```dart
    await for (final n in risky()) {
```

```dart
      print(n);
```

```dart
    }
```

```dart
  } catch (e) {
```

```dart
    print('error: $e');
```

```dart
  }
```

```dart
  print('finished');
```

```dart
}
```

# --solutions--

```dart
Stream<int> risky() async* {
  yield 1;
  throw FormatException('bad');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('error: $e');
  }
  print('finished');
}
```

# --output--

1
error: FormatException: bad
finished
