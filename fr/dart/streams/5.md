---
language: dart
exerciseType: 2
---

# --description--

`await for` met en pause la fonction courante jusqu'à ce que le stream soit terminé. Quand tu veux réagir aux événements **sans attendre**, appelle `listen` et passe une callback : elle est invoquée une fois par événement, et le code qui suit `listen` s'exécute tout de suite.

```dart
final ticks = Stream.fromIterable([1, 2]);
ticks.listen((tick) => print('tick $tick'));
print('not waiting');
// not waiting
// tick 1
// tick 2
```

`listen` accepte aussi un paramètre nommé `onDone`, une fonction sans arguments appelée quand le stream se termine :

```dart
final ticks2 = Stream.fromIterable([1, 2]);
ticks2.listen((tick) => print(tick), onDone: () => print('no more ticks'));
```

# --instructions--

Complète le code pour que le stream soit écouté, que chaque lettre soit affichée, et que `done` soit affiché quand le stream se termine. Le programme doit afficher `listening`, puis `a`, `b` et `done`.

# --seed--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.[/]((letter) => print(letter), [/]: () => print('done'));
  print('listening');
}
```

# --answers--

- listen
- onDone
- onError
- forEach
- await

# --solutions--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.listen((letter) => print(letter), onDone: () => print('done'));
  print('listening');
}
```

# --output--

listening
a
b
done
