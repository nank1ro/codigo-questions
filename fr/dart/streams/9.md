---
language: dart
exerciseType: 1
---

# --description--

En plus de `toList()`, un stream propose d'autres méthodes qui **consomment** tous ses événements et renvoient un seul `Future` :

- `first` et `last` se complètent avec le premier ou le dernier événement
- `length` se complète avec le nombre d'événements
- `join(separator)` se complète avec tous les événements joints en une seule `String`
- `reduce(combine)` combine les événements deux par deux en une seule valeur

```dart
final numbers = Stream.fromIterable([3, 9, 2]);
print(await numbers.reduce((a, b) => a + b)); // 14
```

`reduce` appelle `combine` avec le résultat obtenu jusque-là et l'événement suivant. Elle lève une exception si le stream est vide, ne l'utilise donc que lorsqu'au moins un événement est garanti.

# --instructions--

Écris deux fonctions :
- `largest`, qui prend un `Stream<int>` `source` et renvoie un `Future<int>` se complétant avec la plus grande valeur, en utilisant `reduce`. Le stream a toujours au moins un événement.
- `sentence`, qui prend un `Stream<String>` `words` et renvoie un `Future<String>` se complétant avec tous les mots joints par une seule espace.

Exemple :
```dart
print(await largest(Stream.fromIterable([3, 9, 2])));        // 9
print(await sentence(Stream.fromIterable(['I', 'am', 'Dart']))); // I am Dart
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest` d'un stream de `3, 9, 2` devrait se compléter avec `9`

```dart
  test('test1', () async {
    expect(await largest(Stream.fromIterable([3, 9, 2])), 9, reason: '--err-t1--');
  });
```

`largest` d'un stream de `-4, -1, -7` devrait se compléter avec `-1` et `largest` d'un stream de `5` devrait se compléter avec `5`

```dart
  test('test2', () async {
    expect(await largest(Stream.fromIterable([-4, -1, -7])), -1, reason: '--err-t2--');
    expect(await largest(Stream.fromIterable([5])), 5, reason: '--err-t2--');
  });
```

`sentence` d'un stream de `'I', 'am', 'Dart'` devrait se compléter avec `'I am Dart'`

```dart
  test('test3', () async {
    expect(await sentence(Stream.fromIterable(['I', 'am', 'Dart'])), 'I am Dart', reason: '--err-t3--');
    expect(await sentence(Stream.fromIterable(['solo'])), 'solo', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> largest(Stream<int> source) {
  return source.reduce((a, b) => a > b ? a : b);
}

Future<String> sentence(Stream<String> words) {
  return words.join(' ');
}
```
