---
language: dart
exerciseType: 1
---

# --description--

Les générateurs produisent des événements depuis l'intérieur d'une fonction. Quand les événements viennent **d'ailleurs** (un bouton, une callback réseau, un autre objet), il te faut un **`StreamController`**. Il vit dans la bibliothèque `dart:async`, le fichier doit donc commencer par `import 'dart:async';`.

Un contrôleur possède un stream et te permet d'y pousser des événements :

```dart
import 'dart:async';

Stream<int> dice() {
  final controller = StreamController<int>();
  controller.add(4);
  controller.add(2);
  controller.close();
  return controller.stream;
}
```

- `add(value)` envoie un événement
- `close()` termine le stream ; l'oublier signifie que les auditeurs attendent pour toujours
- `stream` est le `Stream` que les auditeurs consomment

Les événements ajoutés avant que quiconque écoute sont conservés dans un tampon, le code ci-dessus est donc sûr : un auditeur qui arrive plus tard reçoit quand même `4` et `2`.

# --instructions--

Écris une fonction `announce` qui prend une `List<String>` `names` et renvoie un `Stream<String>` construit avec un `StreamController` : ajoute chaque nom en majuscules, ferme le contrôleur et renvoie son stream. N'oublie pas l'import de `dart:async`.

Exemple :
```dart
print(await announce(['ada', 'linus']).toList()); // [ADA, LINUS]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`announce(['ada', 'linus'])` devrait émettre `'ADA'`, `'LINUS'`

```dart
  test('test1', () async {
    expect(await announce(['ada', 'linus']).toList(), ['ADA', 'LINUS'], reason: '--err-t1--');
  });
```

`announce(['Dart'])` devrait émettre `'DART'`

```dart
  test('test2', () async {
    expect(await announce(['Dart']).toList(), ['DART'], reason: '--err-t2--');
  });
```

`announce([])` ne devrait rien émettre et devrait se terminer, et `announce` devrait renvoyer un `Stream<String>`

```dart
  test('test3', () async {
    expect(await announce([]).toList(), <String>[], reason: '--err-t3--');
    expect(announce(['x']), isA<Stream<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
import 'dart:async';

Stream<String> announce(List<String> names) {
  final controller = StreamController<String>();
  for (final name in names) {
    controller.add(name.toUpperCase());
  }
  controller.close();
  return controller.stream;
}
```
