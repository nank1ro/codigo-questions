---
language: dart
exerciseType: 2
---

# --description--

Un contrôleur peut créer directement un stream broadcast avec le constructeur nommé `StreamController<T>.broadcast()`. Son `stream` accepte n'importe quel nombre d'auditeurs, et chaque événement est livré à tous, dans l'ordre où ils se sont abonnés :

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>.broadcast();
  controller.stream.listen((msg) => print('first: $msg'));
  controller.stream.listen((msg) => print('second: $msg'));
  controller.add('hi');
  controller.close();
}
// first: hi
// second: hi
```

Comme tout stream broadcast, il n'utilise pas de tampon : les événements ajoutés avant qu'un auditeur s'abonne sont perdus pour cet auditeur.

# --instructions--

Complète le code pour créer un contrôleur broadcast, abonner deux auditeurs `A` et `B`, envoyer les valeurs `1` et `2`, puis fermer le contrôleur, afin que le programme affiche `A got 1`, `B got 1`, `A got 2`, `B got 2`.

# --seed--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.[/]();
  controller.stream.[/]((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.[/](1);
  controller.add(2);
  controller.[/]();
}
```

# --answers--

- broadcast
- listen
- add
- close
- asBroadcastStream
- subscribe
- sink

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.broadcast();
  controller.stream.listen((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
```

# --output--

A got 1
B got 1
A got 2
B got 2
