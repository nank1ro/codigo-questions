---
language: dart
exerciseType: 1
---

# --description--

`Stream.fromIterable` a besoin de toutes les valeurs d'avance. Pour **produire** les valeurs une à une, écris un **générateur asynchrone** : une fonction dont le corps est marqué `async*` et dont le type de retour est `Stream<T>`. À l'intérieur, `yield` envoie un événement au stream :

```dart
Stream<int> countTo(int n) async* {
  for (var i = 1; i <= n; i++) {
    yield i;
  }
}
```

Le corps ne s'exécute pas quand tu appelles `countTo(3)` : il s'exécute paresseusement, au fur et à mesure que l'auditeur demande des valeurs, et le stream est terminé quand le corps se termine.

Pour rassembler tous les événements dans une `List`, appelle `toList()`. Elle renvoie un `Future<List<T>>`, donc tu l'attends avec `await` :

```dart
final values = await countTo(3).toList();
print(values); // [1, 2, 3]
```

# --instructions--

Écris une fonction `async*` `countdown` qui prend un `int` `from` et renvoie un `Stream<int>` émettant les valeurs de `from` jusqu'à `1`. Si `from` vaut `0` ou moins, le stream n'émet rien.

Exemple :
```dart
print(await countdown(3).toList()); // [3, 2, 1]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countdown(3)` devrait émettre `3`, `2`, `1`

```dart
  test('test1', () async {
    expect(await countdown(3).toList(), [3, 2, 1], reason: '--err-t1--');
  });
```

`countdown(1)` devrait émettre seulement `1`

```dart
  test('test2', () async {
    expect(await countdown(1).toList(), [1], reason: '--err-t2--');
  });
```

`countdown(0)` ne devrait rien émettre et `countdown` devrait renvoyer un `Stream<int>`

```dart
  test('test3', () async {
    expect(await countdown(0).toList(), <int>[], reason: '--err-t3--');
    expect(countdown(2), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> countdown(int from) async* {
  for (var i = from; i >= 1; i--) {
    yield i;
  }
}
```
