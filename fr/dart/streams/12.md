---
language: dart
exerciseType: 4
---

# --description--

Un `StreamController` est souvent créé et consommé au même endroit : tu t'abonnes à `controller.stream` avec `listen`, puis tu ajoutes des événements avec `add` et tu fermes le contrôleur avec `close`. Comme `listen` n'attend pas, les événements sont livrés après la fin du code courant, mais toujours dans l'ordre où ils ont été ajoutés :

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>();
  controller.stream.listen((n) => print('got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
// got 1
// got 2
```

# --instructions--

Trie les éléments pour importer `dart:async`, créer un `StreamController<String>`, t'abonner à son stream en affichant chaque valeur, ajouter `hello` puis `world`, et enfin fermer le contrôleur. Abonne-toi avec `listen` avant d'ajouter la moindre valeur. Le programme doit afficher :

```
hello
world
```

# --answers--

```dart
import 'dart:async';
```

```dart
void main() {
```

```dart
  final controller = StreamController<String>();
```

```dart
  controller.stream.listen((word) => print(word));
```

```dart
  controller.add('hello');
```

```dart
  controller.add('world');
```

```dart
  controller.close();
```

```dart
}
```

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>();
  controller.stream.listen((word) => print(word));
  controller.add('hello');
  controller.add('world');
  controller.close();
}
```

# --output--

hello
world
