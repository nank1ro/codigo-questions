---
language: dart
exerciseType: 1
---

# --description--

Comme `Iterable`, un `Stream` possède des méthodes qui construisent un **nouveau stream** à partir d'un stream existant :

- `map` transforme chaque événement
- `where` ne garde que les événements qui satisfont une condition
- `take` s'arrête après un nombre donné d'événements

```dart
final numbers = Stream.fromIterable([1, 2, 3, 4, 5, 6]);
final firstOdds = numbers.where((n) => n.isOdd).take(2);
print(await firstOdds.toList()); // [1, 3]
```

Ces méthodes sont **paresseuses** : rien ne s'exécute tant que personne n'écoute le stream résultant. Elles peuvent être chaînées, et le stream source n'est jamais modifié.

# --instructions--

Écris une fonction `evenSquares` qui prend un `Stream<int>` `source` et renvoie un `Stream<int>` contenant le carré de chaque valeur **paire** de `source`, dans le même ordre.

Exemple :
```dart
final source = Stream.fromIterable([1, 2, 3, 4]);
print(await evenSquares(source).toList()); // [4, 16]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`evenSquares` d'un stream de `1, 2, 3, 4` devrait émettre `4, 16`

```dart
  test('test1', () async {
    expect(await evenSquares(Stream.fromIterable([1, 2, 3, 4])).toList(), [4, 16], reason: '--err-t1--');
  });
```

`evenSquares` d'un stream de `6, 5, 10` devrait émettre `36, 100`

```dart
  test('test2', () async {
    expect(await evenSquares(Stream.fromIterable([6, 5, 10])).toList(), [36, 100], reason: '--err-t2--');
  });
```

`evenSquares` d'un stream de `1, 3` ne devrait rien émettre et devrait renvoyer un `Stream<int>`

```dart
  test('test3', () async {
    expect(await evenSquares(Stream.fromIterable([1, 3])).toList(), <int>[], reason: '--err-t3--');
    expect(evenSquares(Stream.fromIterable([2])), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> evenSquares(Stream<int> source) {
  return source.where((n) => n.isEven).map((n) => n * n);
}
```
