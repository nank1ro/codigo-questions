---
language: dart
exerciseType: 3
---

# --description--

Tout stream finit par se terminer. Pour un générateur `async*`, le stream est **terminé** dès que le corps de la fonction se termine, qu'il soit arrivé à la fin ou qu'il ait rencontré un `return`. Une boucle `await for` sur un stream terminé s'arrête, et tout future de `toList()` se complète.

Un stream ne redémarre pas et ne répète pas ses valeurs : une fois terminé, il reste terminé.

# --instructions--

Qu'arrive-t-il au stream renvoyé par une fonction `async*` quand le corps de la fonction se termine ?

# --answers--

- Le stream est terminé, et une boucle `await for` dessus s'arrête
- Le stream repart depuis le premier `yield`
- Le stream reste ouvert jusqu'à ce que `close()` soit appelé dessus
- La dernière valeur émise est émise une fois de plus

# --solutions--

- Le stream est terminé, et une boucle `await for` dessus s'arrête
