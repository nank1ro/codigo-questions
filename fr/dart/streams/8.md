---
language: dart
exerciseType: 2
---

# --description--

Comme `where`, `map` et `take` renvoient chacune un stream, tu peux les chaîner et terminer par `toList()` pour obtenir le résultat sous forme de liste. Seul le `toList()` final a besoin d'un `await`, puisque c'est le seul appel qui renvoie un `Future` :

```dart
final numbers = Stream.fromIterable([5, 12, 8, 20, 3]);
final big = await numbers.where((n) => n > 6).take(2).toList();
print(big); // [12, 8]
```

# --instructions--

Complète le code pour ne garder que les mots de exactement `3` lettres, t'arrêter après les `2` premiers d'entre eux, et les rassembler dans une liste, afin que le programme affiche `[sun, sky]`.

# --seed--

```dart
Future<void> main() async {
  final words = Stream.fromIterable(['sun', 'moon', 'star', 'sky', 'fog']);
  final short = await words.[/]((w) => w.length == 3).[/](2).[/]();
  print(short);
}
```

# --answers--

- where
- take
- toList
- map
- skip
- first

# --solutions--

```dart
Future<void> main() async {
  final words = Stream.fromIterable(['sun', 'moon', 'star', 'sky', 'fog']);
  final short = await words.where((w) => w.length == 3).take(2).toList();
  print(short);
}
```

# --output--

[sun, sky]
