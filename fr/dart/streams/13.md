---
language: dart
exerciseType: 1
---

# --description--

Les streams vus jusqu'ici sont à **abonnement unique** : ils n'autorisent qu'un seul auditeur. Appeler `listen`, `await for` ou n'importe quelle méthode de consommation une deuxième fois lève une `StateError` ("Stream has already been listened to").

Pour partager un stream entre plusieurs auditeurs, convertis-le en stream **broadcast** avec `asBroadcastStream()` :

```dart
final shared = Stream.fromIterable([1, 2, 3]).asBroadcastStream();
final total = shared.reduce((a, b) => a + b);
final count = shared.length;
print(await total); // 6
print(await count); // 3
```

Un stream broadcast n'utilise pas de tampon : un auditeur ne reçoit que les événements émis **après** son abonnement. Dans l'exemple, les deux auditeurs s'abonnent avant le premier `await`, donc tous deux reçoivent chaque événement.

# --instructions--

Écris une fonction `sumAndCount` qui prend un `Stream<int>` `source` et renvoie un `Future<List<int>>` se complétant avec une liste de deux éléments : la somme des valeurs et le nombre de valeurs. Convertis `source` en stream broadcast, lance les deux appels de consommation **avant** d'en attendre l'un ou l'autre, puis attends-les. Le stream a toujours au moins un événement.

Exemple :
```dart
print(await sumAndCount(Stream.fromIterable([1, 2, 3]))); // [6, 3]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumAndCount` d'un stream de `1, 2, 3` devrait se compléter avec `[6, 3]`

```dart
  test('test1', () async {
    expect(await sumAndCount(Stream.fromIterable([1, 2, 3])), [6, 3], reason: '--err-t1--');
  });
```

`sumAndCount` d'un stream de `10, -4` devrait se compléter avec `[6, 2]`

```dart
  test('test2', () async {
    expect(await sumAndCount(Stream.fromIterable([10, -4])), [6, 2], reason: '--err-t2--');
  });
```

`sumAndCount` d'un stream de `7` devrait se compléter avec `[7, 1]`

```dart
  test('test3', () async {
    expect(await sumAndCount(Stream.fromIterable([7])), [7, 1], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<List<int>> sumAndCount(Stream<int> source) async {
  final shared = source.asBroadcastStream();
  final sum = shared.reduce((a, b) => a + b);
  final count = shared.length;
  return [await sum, await count];
}
```
