---
language: dart
exerciseType: 1
---

# --instructions--

Écrivez une fonction `fullName` qui prend deux **paramètres nommés** `firstName` et `lastName` (tous deux `required String`) et retourne le nom complet en une seule chaîne avec un espace entre eux.

Exemple :
```dart
print(fullName(firstName: "Jane", lastName: "Doe")); // Jane Doe
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`fullName(firstName: "Jane", lastName: "Doe")` doit retourner `"Jane Doe"`
```dart
  test('test1', () {
    expect(fullName(firstName: "Jane", lastName: "Doe"), "Jane Doe", reason: '--err-t1--');
  });
```

`fullName(firstName: "John", lastName: "Smith")` doit retourner `"John Smith"`
```dart
  test('test2', () {
    expect(fullName(firstName: "John", lastName: "Smith"), "John Smith", reason: '--err-t2--');
  });
```

`fullName(firstName: "A", lastName: "B")` doit retourner `"A B"`
```dart
  test('test3', () {
    expect(fullName(firstName: "A", lastName: "B"), "A B", reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String fullName({required String firstName, required String lastName}) {
  return "$firstName $lastName";
}
```
