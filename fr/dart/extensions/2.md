---
language: dart
exerciseType: 1
---

# --description--

Les extensions fonctionnent sur n'importe quel type, y compris les nombres. Cette extension donne à chaque `int` une méthode qui le double :

```dart
extension Doubling on int {
  int doubled() => this * 2;
}
```

Comme l'extension s'applique au **type**, vous pouvez appeler la méthode sur une variable ou directement sur un **littéral**. Les littéraux négatifs ont besoin de parenthèses, sinon le point est lu avant le signe moins :

```dart
var n = 21;
print(n.doubled());    // 42
print(4.doubled());    // 8
print((-3).doubled()); // -6
```

# --instructions--

Écrivez une extension nommée `Squaring` sur `int` avec une méthode `squared` qui ne prend aucun paramètre et retourne le nombre multiplié par lui-même.

Exemple :
```dart
print(5.squared()); // 25
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`5.squared()` doit être `25`

```dart
  test('test1', () {
    expect(5.squared(), 25, reason: '--err-t1--');
  });
```

`(-3).squared()` doit être `9`

```dart
  test('test2', () {
    expect((-3).squared(), 9, reason: '--err-t2--');
  });
```

`0.squared()` doit être `0` et le résultat doit être un `int`

```dart
  test('test3', () {
    expect(0.squared(), 0, reason: '--err-t3--');
    expect(12.squared(), isA<int>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Squaring on int {
  int squared() => this * this;
}
```
