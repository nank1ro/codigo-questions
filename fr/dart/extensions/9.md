---
language: dart
exerciseType: 4
---

# --description--

Les extensions ne sont pas réservées aux types du SDK : vous pouvez aussi étendre **vos propres classes**. C'est utile lorsque la classe provient d'un paquet que vous ne contrôlez pas, ou lorsque vous voulez garder la classe petite et ajouter des aides optionnelles à côté du code qui en a besoin.

```dart
class Circle {
  final double radius;
  Circle(this.radius);
}

extension CircleMath on Circle {
  double get diameter => radius * 2;
}

void main() {
  print(Circle(3).diameter); // 6.0
}
```

L'extension voit les champs et les méthodes publics de la classe, exactement comme du code écrit à l'extérieur de la classe.

# --instructions--

Triez les éléments pour définir une classe `Rectangle` avec `width` et `height`, puis une extension `RectangleMath` sur `Rectangle` avec un getter `area`, puis un `main` qui affiche l'aire d'un rectangle de `4` par `2.5`. Définissez d'abord la classe, puis l'extension, puis `main`. À l'intérieur de la classe, déclarez les deux champs avant le constructeur. Le programme doit afficher :

```
10.0
```

# --answers--

```dart
class Rectangle {
```

```dart
  final double width;
```

```dart
  final double height;
```

```dart
  Rectangle(this.width, this.height);
```

```dart
}
```

```dart
extension RectangleMath on Rectangle {
```

```dart
  double get area => width * height;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Rectangle(4, 2.5).area);
```

```dart
}
```

# --solutions--

```dart
class Rectangle {
  final double width;
  final double height;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

```dart
class Rectangle {
  final double height;
  final double width;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

# --output--

10.0
