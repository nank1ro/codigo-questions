---
language: dart
exerciseType: 2
---

# --description--

Une extension sur `String` ne peut pas être appelée sur une `String?` : la valeur pourrait être `null`, et le compilateur refuse l'appel. Si vous déclarez plutôt l'extension sur le type **nullable**, la méthode peut être appelée directement sur une `String?`, et à l'intérieur `this` a le type `String?`, vous devez donc gérer vous-même le cas `null`, par exemple avec `??` :

```dart
extension Defaults on int? {
  int orZero() => this ?? 0;
}

void main() {
  int? count = null;
  print(count.orZero()); // 0
  print(5.orZero());     // 5
}
```

Un `int` non nullable peut être passé là où un `int?` est attendu, l'extension fonctionne donc sur les deux.

# --instructions--

Complétez l'extension pour que `orEmpty` puisse être appelé sur une chaîne nullable et retourne la chaîne elle-même, ou la chaîne vide quand elle est `null`. Le programme doit afficher `[]` et `[Ada]`.

# --seed--

```dart
extension Defaults on [/] {
  String orEmpty() => this [/] '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --answers--

- String?
- ??
- String
- +
- ?.
- !

# --solutions--

```dart
extension Defaults on String? {
  String orEmpty() => this ?? '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --output--

[]
[Ada]
