---
language: dart
exerciseType: 1
---

# --description--

Une extension sur `List<int>` ne peut pas être utilisée sur une `List<String>`. Pour écrire une extension qui fonctionne pour **tous** les types d'éléments, donnez à l'extension un **paramètre de type**, écrit entre chevrons après son nom, et utilisez-le dans le type du `on` :

```dart
extension Firsts<T> on List<T> {
  T get firstOrLast => length > 1 ? this[0] : this[length - 1];
}
```

`T` est un espace réservé pour « quel que soit le type de l'élément » : sur une `List<int>` il devient `int`, sur une `List<String>` il devient `String`, si bien que le getter ci-dessus retourne un `int` ou une `String` en conséquence. Le compilateur remplit `T` pour vous à chaque appel.

```dart
print([7, 8, 9].firstOrLast); // 7
print(['a', 'b'].firstOrLast); // a
```

# --instructions--

Écrivez une extension nommée `Seconds` avec un paramètre de type `T` sur `List<T>`, avec un getter `secondOrNull` de type `T?` qui retourne l'élément à l'index `1`, ou `null` quand la liste contient moins de deux éléments.

Exemple :
```dart
print([10, 20, 30].secondOrNull); // 20
print(['solo'].secondOrNull);     // null
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`[10, 20, 30].secondOrNull` doit être `20`

```dart
  test('test1', () {
    expect([10, 20, 30].secondOrNull, 20, reason: '--err-t1--');
  });
```

`['a', 'b'].secondOrNull` doit être `'b'`

```dart
  test('test2', () {
    expect(['a', 'b'].secondOrNull, 'b', reason: '--err-t2--');
  });
```

`['solo'].secondOrNull` et `<int>[].secondOrNull` doivent être `null`

```dart
  test('test3', () {
    expect(['solo'].secondOrNull, null, reason: '--err-t3--');
    expect(<int>[].secondOrNull, null, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Seconds<T> on List<T> {
  T? get secondOrNull => length > 1 ? this[1] : null;
}
```
