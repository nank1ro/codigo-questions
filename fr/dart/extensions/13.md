---
language: dart
exerciseType: 2
---

# --description--

Une extension générique peut prendre des **fonctions** en paramètres, exactement comme le font `where` et `map`. Le type de la fonction s'écrit avec le type d'élément `T`, si bien que le callback reçoit des éléments du bon type :

```dart
extension Checks<T> on List<T> {
  bool all(bool Function(T) test) {
    for (final item in this) {
      if (!test(item)) return false;
    }
    return true;
  }
}

void main() {
  print([2, 4, 6].all((n) => n.isEven)); // true
}
```

# --instructions--

Complétez l'extension pour que `countWhere` retourne combien d'éléments satisfont la fonction `test` donnée, afin que le programme affiche `2` puis `1`.

# --seed--

```dart
extension Counting<T> on List<T> {
  int countWhere([/] Function(T) test) {
    var count = 0;
    for (final item in [/]) {
      if (test(item)) count++;
    }
    return count;
  }
}

void main() {
  print([1, 2, 3, 4].countWhere((n) => n.isEven));
  print(['a', 'bb', 'c'].countWhere((s) => s.length > 1));
}
```

# --answers--

- bool
- this
- int
- void
- T
- list

# --solutions--

```dart
extension Counting<T> on List<T> {
  int countWhere(bool Function(T) test) {
    var count = 0;
    for (final item in this) {
      if (test(item)) count++;
    }
    return count;
  }
}

void main() {
  print([1, 2, 3, 4].countWhere((n) => n.isEven));
  print(['a', 'bb', 'c'].countWhere((s) => s.length > 1));
}
```

# --output--

2
1
