---
language: dart
exerciseType: 1
---

# --description--

Dart permet à un type de définir la signification d'opérateurs comme `+`, `*` ou `==` pour ses valeurs, au moyen d'une méthode dont le nom est le mot-clé `operator` suivi du symbole. Le membre de droite de l'opérateur est le paramètre de la méthode :

```dart
extension Scaling on List<int> {
  List<int> operator *(int factor) => map((n) => n * factor).toList();
}

void main() {
  print([1, 2, 3] * 10); // [10, 20, 30]
}
```

Comme les extensions peuvent déclarer des opérateurs, vous pouvez donner à un type existant un nouvel opérateur qu'il n'a pas déjà. `String` possède `+` et `*`, mais pas `-`, une extension peut donc définir ce que signifie `'hello world' - 'o'`.

# --instructions--

Écrivez une extension nommée `Removing` sur `String` avec un opérateur `-` qui prend un paramètre `String` `part` et retourne la chaîne privée de toutes les occurrences de `part`. Utilisez `replaceAll`.

Exemple :
```dart
print('hello world' - 'o'); // hell wrld
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`'hello world' - 'o'` doit être `'hell wrld'`

```dart
  test('test1', () {
    expect('hello world' - 'o', 'hell wrld', reason: '--err-t1--');
  });
```

`'banana' - 'an'` doit être `'ba'`

```dart
  test('test2', () {
    expect('banana' - 'an', 'ba', reason: '--err-t2--');
  });
```

`'abc' - 'z'` doit être `'abc'` et `'aaa' - 'a'` doit être `''`

```dart
  test('test3', () {
    expect('abc' - 'z', 'abc', reason: '--err-t3--');
    expect('aaa' - 'a', '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Removing on String {
  String operator -(String part) => replaceAll(part, '');
}
```
