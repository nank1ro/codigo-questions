---
language: dart
exerciseType: 2
---

# --description--

Dart prend en charge les **constructeurs nommés**, qui permettent de définir d'autres façons de créer un objet. Les constructeurs nommés s'écrivent `ClassName.constructorName` :

```dart
class Point {
  double x;
  double y;

  Point(this.x, this.y);

  Point.origin()
      : x = 0,
        y = 0;
}
```

La partie après les deux-points est la **liste d'initialisation** : elle affecte les champs avant l'exécution du corps du constructeur. On peut alors créer un objet à l'origine avec : `var p = Point.origin();`

# --instructions--

Complétez le constructeur nommé `zero` de la classe `Counter` qui définit `count` à `0`.

# --seed--

```dart
class Counter {
  int count;

  Counter(this.count);

  Counter.[/]() : count = [/];
}
```

# --answers--

- zero
- 0
- count
- init
- 1
- reset
- start

# --solutions--

```dart
class Counter {
  int count;

  Counter(this.count);

  Counter.zero() : count = 0;
}
```
