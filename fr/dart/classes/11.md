---
language: dart
exerciseType: 2
---

# --description--

Lorsque le constructeur d'une sous-classe doit appeler le constructeur de la superclasse, utilisez le mot-clé `super` dans la **liste d'initialisation** :

```dart
class Vehicle {
  String brand;
  Vehicle(this.brand);
}

class Car extends Vehicle {
  int doors;
  Car(String brand, this.doors) : super(brand);
}
```

`super(brand)` transmet l'argument `brand` au constructeur de `Vehicle`.

# --instructions--

Complétez le constructeur de `Cat` pour qu'il appelle le constructeur de la superclasse `Animal` avec `name`.

# --seed--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Cat extends Animal {
  Cat(String name) : [/](name);
}
```

# --answers--

- super
- Animal
- parent
- base
- this

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Cat extends Animal {
  Cat(String name) : super(name);
}
```
