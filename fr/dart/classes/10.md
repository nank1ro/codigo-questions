---
language: dart
exerciseType: 1
---

# --instructions--

Créez une classe `Counter` avec un champ `int` `count` initialisé à `0`. Ajoutez une méthode `increment()` qui augmente `count` de `1`, et une méthode `reset()` qui remet `count` à `0`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Appeler `increment()` trois fois devrait rendre `count` égal à `3`

```dart
  test('test1', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.increment();
    expect(c.count, 3, reason: '--err-t1--');
  });
```

Appeler `reset()` devrait remettre `count` à `0`

```dart
  test('test2', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.reset();
    expect(c.count, 0, reason: '--err-t2--');
  });
```

Le `count` initial devrait être `0`

```dart
  test('test3', () {
    final c = Counter();
    expect(c.count, 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Counter {
  int count = 0;

  void increment() {
    count++;
  }

  void reset() {
    count = 0;
  }
}
```
