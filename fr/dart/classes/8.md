---
language: dart
exerciseType: 1
---

# --instructions--

Créez une classe `Car` avec deux propriétés `String` : `brand` et `model`. Ajoutez un constructeur qui les définit toutes les deux avec la syntaxe abrégée.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Créer `Car('Toyota', 'Corolla')` devrait définir `brand` à `'Toyota'`

```dart
  test('test1', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.brand, 'Toyota', reason: '--err-t1--');
  });
```

Créer `Car('Toyota', 'Corolla')` devrait définir `model` à `'Corolla'`

```dart
  test('test2', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.model, 'Corolla', reason: '--err-t2--');
  });
```

Créer `Car('Honda', 'Civic')` devrait définir `brand` à `'Honda'`

```dart
  test('test3', () {
    final car = Car('Honda', 'Civic');
    expect(car.brand, 'Honda', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Car {
  String brand;
  String model;
  Car(this.brand, this.model);
}
```
