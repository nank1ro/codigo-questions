---
language: dart
exerciseType: 2
---

# --description--

Une **classe** est un modèle pour créer des objets. Elle regroupe des données (champs) et des comportements (méthodes) liés.

En Dart, vous déclarez une classe avec le mot-clé `class` suivi du nom de la classe (par convention, en PascalCase) :

```dart
class Car {
  String brand = 'Toyota';
}
```

Vous créez une **instance** (objet) de la classe ainsi :

```dart
final car = Car();
print(car.brand); // Toyota
```

# --instructions--

Complétez le code pour déclarer une classe nommée `Person` avec un champ `String` `name` initialisé à `'Alice'`, puis créez une instance et affichez son `name`.

# --seed--

```dart
[/] Person {
  String [/] = 'Alice';
}

void main() {
  final p = [/]();
  print(p.[/]);
}
```

# --answers--

- class
- name
- Person
- brand
- age
- void
- p

# --solutions--

```dart
class Person {
  String name = 'Alice';
}

void main() {
  final p = Person();
  print(p.name);
}
```

# --output--

Alice
