---
language: dart
exerciseType: 2
---

# --description--

Un **getter** est une méthode spéciale qui ressemble à une propriété lorsqu'on y accède. Utilisez le mot-clé `get` pour en définir un :

```dart
class Circle {
  double radius;
  Circle(this.radius);

  double get area => 3.14 * radius * radius;
}

void main() {
  final c = Circle(5);
  print(c.area); // accédé comme un champ, pas un appel de méthode
}
```

Les getters sont utiles pour les valeurs calculées dérivées des champs.

# --instructions--

Complétez la classe `Square`. Elle a un champ `int` `side`, un constructeur et un getter `area` qui retourne `side * side`. Accédez ensuite au getter et affichez le résultat.

# --seed--

```dart
class Square {
  int side;
  Square(this.side);

  int [/] area => [/] * side;
}

void main() {
  final s = Square(7);
  print(s.[/]);
}
```

# --answers--

- get
- side
- area
- set
- width
- s
- int

# --solutions--

```dart
class Square {
  int side;
  Square(this.side);

  int get area => side * side;
}

void main() {
  final s = Square(7);
  print(s.area);
}
```

# --output--

49
