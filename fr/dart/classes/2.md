---
language: dart
exerciseType: 2
---

# --description--

Un **constructeur** est une méthode spéciale qui s'exécute lorsque vous créez une instance d'une classe. Il est utilisé pour initialiser les champs de l'objet.

En Dart, le constructeur le plus simple utilise la **syntaxe abrégée** `this.nomChamp` pour assigner des paramètres directement aux champs :

```dart
class Car {
  String brand;
  Car(this.brand);
}

void main() {
  final car = Car('Toyota');
  print(car.brand); // Toyota
}
```

# --instructions--

Complétez la classe `Dog` pour qu'elle ait un champ `String` `name` et un constructeur qui le définit avec la syntaxe abrégée, puis créez un `Dog` nommé `'Rex'` et affichez son `name`.

# --seed--

```dart
class Dog {
  String [/];
  Dog([/].[/]);
}

void main() {
  final dog = Dog([/]);
  print(dog.name);
}
```

# --answers--

- name
- this
- name
- 'Rex'
- breed
- Dog
- age

# --solutions--

```dart
class Dog {
  String name;
  Dog(this.name);
}

void main() {
  final dog = Dog('Rex');
  print(dog.name);
}
```

# --output--

Rex
