---
language: dart
exerciseType: 1
---

# --description--

La **redéfinition de méthode** permet à une sous-classe de fournir sa propre implémentation d'une méthode qui existe déjà dans la superclasse. Utilisez l'annotation `@override` :

```dart
class Shape {
  double area() => 0;
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() => 3.14159 * radius * radius;
}
```

L'annotation `@override` indique à Dart (et aux autres développeurs) que vous remplacez intentionnellement la méthode de la superclasse.

# --instructions--

Créez une classe `Animal` avec une méthode `sound()` qui renvoie le `String` `'...'`. Créez ensuite une sous-classe `Dog` qui redéfinit `sound()` pour renvoyer `'Woof'`. Ajoutez une autre sous-classe `Cat` qui redéfinit `sound()` pour renvoyer `'Meow'`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Animal.sound()` renvoie `'...'`

```dart
    test('animal sound', () {
      expect(Animal().sound(), '...', reason: '--err-t1--');
    });
```

`Dog.sound()` renvoie `'Woof'`

```dart
    test('dog sound', () {
      Animal dog = Dog();
      expect(dog.sound(), 'Woof', reason: '--err-t2--');
    });
```

`Cat.sound()` renvoie `'Meow'`

```dart
    test('cat sound', () {
      Animal cat = Cat();
      expect(cat.sound(), 'Meow', reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String sound() => '...';
}

class Dog extends Animal {
  @override
  String sound() => 'Woof';
}

class Cat extends Animal {
  @override
  String sound() => 'Meow';
}
```
