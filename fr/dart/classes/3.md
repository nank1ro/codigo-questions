---
language: dart
exerciseType: 2
---

# --description--

Une **méthode** est une fonction définie à l'intérieur d'une classe. Les méthodes décrivent le comportement d'un objet.

```dart
class Circle {
  double radius;
  Circle(this.radius);

  double area() {
    return 3.14 * radius * radius;
  }
}

void main() {
  final c = Circle(5);
  print(c.area()); // 78.5
}
```

Les méthodes peuvent accéder directement aux champs de l'objet par leur nom (ou via `this.nomChamp`).

# --instructions--

Complétez la classe `Rectangle` pour qu'elle ait des champs `int` `width` et `height`, un constructeur et une méthode `perimeter()` qui retourne `2 * (width + height)`. Appelez ensuite la méthode et affichez le résultat.

# --seed--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.[/]);

  int [/]() {
    return 2 * ([/] + height);
  }
}

void main() {
  final r = Rectangle(4, 6);
  print(r.[/]());
}
```

# --answers--

- height
- perimeter
- width
- area
- this
- 3
- r

# --solutions--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.height);

  int perimeter() {
    return 2 * (width + height);
  }
}

void main() {
  final r = Rectangle(4, 6);
  print(r.perimeter());
}
```

# --output--

20
