---
language: dart
exerciseType: 1
---

# --description--

Les valeurs d'un enum font d'excellentes **clés de map** : elles sont uniques, faciles à comparer, et le compilateur vérifie que vous n'utilisez que des valeurs réelles. Déclarez la map avec l'enum comme type de clé et recherchez les valeurs avec `[]` :

```dart
enum Direction { north, south, east, west }

Map<Direction, String> arrows = {
  Direction.north: '^',
  Direction.south: 'v',
  Direction.east: '>',
  Direction.west: '<',
};

print(arrows[Direction.east]); // >
```

Comme pour toute map, une recherche renvoie `null` quand la clé est absente, utilisez donc `??` pour fournir une valeur de repli.

# --instructions--

Avec `enum Size { small, medium, large }`, déclarez une `Map<Size, int>` de niveau supérieur nommée `prices` où `small` coûte `2`, `medium` coûte `3` et `large` coûte `4`. Écrivez ensuite une fonction `totalPrice(List<Size> order)` qui renvoie la somme des prix de chaque taille dans `order`.

# --seed--

```dart
enum Size { small, medium, large }
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`prices[Size.medium]` devrait être `3`

```dart
  test('test1', () {
    expect(prices[Size.medium], 3, reason: '--err-t1--');
  });
```

`totalPrice([Size.small, Size.large])` devrait être `6`

```dart
  test('test2', () {
    expect(totalPrice([Size.small, Size.large]), 6, reason: '--err-t2--');
  });
```

`totalPrice([Size.medium, Size.medium, Size.small])` devrait être `8`

```dart
  test('test3', () {
    expect(totalPrice([Size.medium, Size.medium, Size.small]), 8, reason: '--err-t3--');
  });
```

`totalPrice([])` devrait être `0`

```dart
  test('test4', () {
    expect(totalPrice([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Size { small, medium, large }

Map<Size, int> prices = {
  Size.small: 2,
  Size.medium: 3,
  Size.large: 4,
};

int totalPrice(List<Size> order) {
  int total = 0;
  for (var size in order) {
    total += prices[size] ?? 0;
  }
  return total;
}
```
