---
language: dart
exerciseType: 2
---

# --description--

Chaque enum possède aussi une liste constante appelée `values` qui contient toutes ses valeurs dans l'ordre de déclaration. Vous pouvez l'indexer comme n'importe quelle liste, lire sa `length` ou la parcourir avec `for-in` :

```dart
enum Direction { north, south, east, west }

print(Direction.values.length); // 4
print(Direction.values[1]);     // Direction.south

for (var direction in Direction.values) {
  print(direction.name);
}
```

# --instructions--

Complétez le code pour afficher le nom de `Planet.earth`, son index et le nombre total de planètes.

# --seed--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.[/]);
  print(Planet.earth.[/]);
  print(Planet.[/].length);
}
```

# --answers--

- name
- index
- values
- ordinal
- position
- count

# --solutions--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.name);
  print(Planet.earth.index);
  print(Planet.values.length);
}
```

# --output--

earth
2
3
