---
language: dart
exerciseType: 2
---

# --description--

Depuis Dart 3, un `switch` peut aussi être utilisé comme une **expression** qui produit une valeur. Chaque cas s'écrit comme `pattern => value` et les cas sont séparés par des virgules, sans le mot-clé `case` et sans `break` :

```dart
enum Direction { north, south, east, west }

String arrow(Direction direction) => switch (direction) {
      Direction.north => '^',
      Direction.south => 'v',
      Direction.east => '>',
      Direction.west => '<',
    };
```

Comme la forme instruction, une expression switch sur un enum doit être exhaustive.

# --instructions--

Complétez l'expression switch pour que `label` retourne la première lettre de chaque taille, puis affichez l'étiquette de `Size.large`.

# --seed--

```dart
enum Size { small, medium, large }

String label(Size size) => [/] (size) {
      Size.small [/] 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --answers--

- switch
- =>
- case
- when
- ->
- :

# --solutions--

```dart
enum Size { small, medium, large }

String label(Size size) => switch (size) {
      Size.small => 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --output--

L
