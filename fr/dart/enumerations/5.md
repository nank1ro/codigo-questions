---
language: dart
exerciseType: 4
---

# --description--

Parcourir `values` est la manière habituelle de traiter chaque valeur d'un enum. Dans la boucle, la valeur courante se comporte comme n'importe quel autre objet : tu peux donc lire son `index` et son `name` et les utiliser directement dans une interpolation de chaîne :

```dart
enum Direction { north, south, east, west }

for (var direction in Direction.values) {
  print('${direction.index}: ${direction.name}');
}
```

# --instructions--

Triez les éléments pour déclarer l'enum `Level` et afficher, pour chaque valeur, son index et son nom. Déclarez l'enum avant `main`. Le programme doit afficher :

```
0: low
1: medium
2: high
```

# --answers--

```dart
enum Level { low, medium, high }
```

```dart
void main() {
```

```dart
  for (var level in Level.values) {
```

```dart
    print('${level.index}: ${level.name}');
```

```dart
  }
}
```

# --solutions--

```dart
enum Level { low, medium, high }
void main() {
  for (var level in Level.values) {
    print('${level.index}: ${level.name}');
  }
}
```

# --output--

0: low
1: medium
2: high
