---
language: dart
exerciseType: 1
---

# --description--

Comme `values` est une liste, vous pouvez la combiner avec les méthodes de liste que vous connaissez déjà. Par exemple, `.map()` avec `.name` transforme les valeurs en une liste de chaînes :

```dart
enum Direction { north, south, east, west }

List<String> names = Direction.values.map((d) => d.name).toList();
print(names); // [north, south, east, west]
```

# --instructions--

Déclarez un enum `Weekday` avec les valeurs `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday` et `sunday`, dans cet ordre. Écrivez ensuite une fonction `dayNames()` qui renvoie une `List<String>` avec le nom de chaque jour de la semaine, dans l'ordre de déclaration.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Weekday.values.length` devrait être `7`

```dart
  test('test1', () {
    expect(Weekday.values.length, 7, reason: '--err-t1--');
  });
```

`Weekday.values[2]` devrait être `Weekday.wednesday`

```dart
  test('test2', () {
    expect(Weekday.values[2], Weekday.wednesday, reason: '--err-t2--');
  });
```

`dayNames()` doit retourner les sept noms dans l'ordre

```dart
  test('test3', () {
    expect(dayNames(), ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Weekday { monday, tuesday, wednesday, thursday, friday, saturday, sunday }

List<String> dayNames() {
  return Weekday.values.map((day) => day.name).toList();
}
```
