---
language: dart
exerciseType: 1
---

# --description--

Depuis Dart 2.17, un enum peut déclarer des **champs** et un **constructeur**, tout comme une classe. On appelle cela un *enum enrichi*. Chaque valeur transmet alors ses propres arguments au constructeur :

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);
}

print(Planet.mars.moons); // 2
```

Remarquez les trois règles : la liste des valeurs se termine par un **point-virgule** `;`, les champs doivent être `final`, et le constructeur doit être `const`.

# --instructions--

Déclarez un enum `Coin` avec un champ `final int` nommé `cents` et les valeurs `penny(1)`, `nickel(5)`, `dime(10)` et `quarter(25)`, dans cet ordre.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Coin.penny.cents` devrait être `1` et `Coin.nickel.cents` devrait être `5`

```dart
  test('test1', () {
    expect(Coin.penny.cents, 1, reason: '--err-t1--');
    expect(Coin.nickel.cents, 5, reason: '--err-t2--');
  });
```

`Coin.dime.cents` devrait être `10`

```dart
  test('test2', () {
    expect(Coin.dime.cents, 10, reason: '--err-t3--');
  });
```

`Coin.quarter.cents` devrait être `25`

```dart
  test('test3', () {
    expect(Coin.quarter.cents, 25, reason: '--err-t4--');
  });
```

`Coin.values` devrait avoir 4 valeurs, avec `penny` en premier

```dart
  test('test4', () {
    expect(Coin.values.length, 4, reason: '--err-t5--');
    expect(Coin.values[0], Coin.penny, reason: '--err-t6--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Coin {
  penny(1),
  nickel(5),
  dime(10),
  quarter(25);

  final int cents;

  const Coin(this.cents);
}
```
