---
language: dart
exerciseType: 1
---

# --instructions--

Déclare une variable `bool` de niveau supérieur nommée `isHot`. Assigne-lui `true` lorsque la variable `int` de niveau supérieur `temp` (définie à `35`) est supérieure à `30`, en utilisant une instruction `if`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`temp` doit être égal à `35`

```dart
  test('test1', () {
    expect(temp, 35, reason: '--err-t1--');
  });
```

`isHot` doit être `true`

```dart
  test('test2', () {
    expect(isHot, true, reason: '--err-t2--');
    expect(isHot, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int temp = 35;
bool isHot = false;

void main() {
  if (temp > 30) {
    isHot = true;
  }
  print(isHot);
}
```
