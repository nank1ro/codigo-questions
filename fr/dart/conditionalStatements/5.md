---
language: dart
exerciseType: 2
---

# --description--

L'**opérateur ternaire** `condition ? expr1 : expr2` est une façon compacte d'écrire une expression `if-else` simple.

```dart
int x = 10;
String label = x > 0 ? 'positive' : 'non-positive';
print(label); // positive
```

Si la condition est `true`, `expr1` est utilisé ; sinon, `expr2` est utilisé.

# --instructions--

Complète l'expression ternaire pour assigner `'Even'` à `parity` lorsque `n` est divisible par `2`, et `'Odd'` sinon.

# --seed--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 [/] 'Even' [/] 'Odd';
  print(parity);
}
```

# --answers--

- ?
- :
- ==
- ;

# --solutions--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 ? 'Even' : 'Odd';
  print(parity);
}
```

# --output--

Even
