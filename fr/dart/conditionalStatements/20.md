---
language: dart
exerciseType: 1
---

# --instructions--

Écris une fonction `ageGroup(int age)` qui renvoie `'child'` pour `age < 13`, `'teenager'` pour `age < 18`, `'adult'` pour `age < 65`, et `'senior'` pour `age >= 65`, en utilisant une chaîne `if-else`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`ageGroup(8)` doit renvoyer `'child'`

```dart
  test('test1', () {
    expect(ageGroup(8), 'child', reason: '--err-t1--');
  });
```

`ageGroup(15)` doit renvoyer `'teenager'`

```dart
  test('test2', () {
    expect(ageGroup(15), 'teenager', reason: '--err-t2--');
  });
```

`ageGroup(30)` doit renvoyer `'adult'`

```dart
  test('test3', () {
    expect(ageGroup(30), 'adult', reason: '--err-t3--');
  });
```

`ageGroup(70)` doit renvoyer `'senior'`

```dart
  test('test4', () {
    expect(ageGroup(70), 'senior', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String ageGroup(int age) {
  if (age < 13) {
    return 'child';
  } else if (age < 18) {
    return 'teenager';
  } else if (age < 65) {
    return 'adult';
  } else {
    return 'senior';
  }
}

void main() {
  print(ageGroup(30));
}
```
