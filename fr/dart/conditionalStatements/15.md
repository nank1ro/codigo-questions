---
language: dart
exerciseType: 1
---

# --instructions--

Écris une fonction `absoluteValue(int n)` qui renvoie la valeur absolue de `n` en utilisant l'opérateur ternaire. La fonction doit renvoyer `n` lorsqu'il est non négatif, et `-n` sinon.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`absoluteValue(7)` doit renvoyer `7`

```dart
  test('test1', () {
    expect(absoluteValue(7), 7, reason: '--err-t1--');
  });
```

`absoluteValue(-4)` doit renvoyer `4`

```dart
  test('test2', () {
    expect(absoluteValue(-4), 4, reason: '--err-t2--');
  });
```

`absoluteValue(0)` doit renvoyer `0`

```dart
  test('test3', () {
    expect(absoluteValue(0), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int absoluteValue(int n) {
  return n >= 0 ? n : -n;
}

void main() {
  print(absoluteValue(-4));
}
```
