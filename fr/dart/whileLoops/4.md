---
language: dart
exerciseType: 2
---

# --description--

La condition d'une boucle `while` peut contenir n'importe quelle expression booléenne. Dès que la condition est évaluée à `false`, la boucle se termine.

```dart
int n = 100;
while (n > 1) {
  n ~/= 2;
}
print(n); // 0
```

Ici, `n` est divisé par deux à chaque itération en utilisant la division entière (`~/`).

# --instructions--

Complétez la boucle `while` qui divise `n` par deux (en utilisant `~/= 2`) tant qu'il est supérieur à `1`, puis affiche le résultat.

# --seed--

```dart
void main() {
  int n = 64;
  while (n [/] 1) {
    n [/] 2;
  }
  print(n);
}
```

# --answers--

- >
- <
- ~/=
- /=
- *=

# --solutions--

```dart
void main() {
  int n = 64;
  while (n > 1) {
    n ~/= 2;
  }
  print(n);
}
```

# --output--

1
