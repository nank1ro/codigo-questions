---
language: dart
exerciseType: 2
---

# --description--

Une variable compteur contrôle le nombre de fois qu'une boucle `while` s'exécute.

```dart
int count = 0;
while (count < 5) {
  count++;
}
print(count); // 5
```

Le compteur commence à `0`, et la boucle l'incrémente à chaque itération jusqu'à ce que `count < 5` soit `false`.

# --instructions--

Complétez la boucle qui compte de `1` à `5` et affiche chaque valeur.

# --seed--

```dart
void main() {
  int count = [/];
  while (count [/] 5) {
    print(count);
    [/];
  }
}
```

# --answers--

- 1
- 0
- <=
- <
- count++
- count--
- count += 2

# --solutions--

```dart
void main() {
  int count = 1;
  while (count <= 5) {
    print(count);
    count++;
  }
}
```

# --output--

1
2
3
4
5
