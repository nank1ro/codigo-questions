---
language: dart
exerciseType: 1
---

# --instructions--

Utilisez une boucle `do-while` pour ajouter `3` à une variable `total` commençant à `0`, jusqu'à ce que `total` atteigne ou dépasse `12`. Retournez `total`.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` doit être égal à `12`

```dart
  test('test1', () {
    expect(computeValue(), 12, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 0;
do {
  total += 3;
} while (total < 12);
```
