---
language: dart
exerciseType: 1
---

# --instructions--

Commencez avec `product = 1` et utilisez une boucle `while` pour multiplier répétitivement par `2` jusqu'à ce que `product` soit supérieur ou égal à `100`. Retournez `product`.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return product;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`product` doit être égal à `128`

```dart
  test('test1', () {
    expect(computeValue(), 128, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int product = 1;
while (product < 100) {
  product *= 2;
}
```
