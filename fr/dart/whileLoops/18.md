---
language: dart
exerciseType: 1
---

# --instructions--

Utilisez une boucle `while` pour calculer la factorielle de `6` (c'est-à-dire `6!`). Retournez le résultat.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return factorial;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`factorial` doit être égal à `720`

```dart
  test('test1', () {
    expect(computeValue(), 720, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int factorial = 1;
int i = 1;
while (i <= 6) {
  factorial *= i;
  i++;
}
```
