---
language: dart
exerciseType: 1
---

# --instructions--

Écrivez une boucle `while` qui compte de `1` à `5` et retourne la valeur finale du compteur.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return counter;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`counter` doit être égal à `5`

```dart
  test('test1', () {
    expect(computeValue(), 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int counter = 1;
while (counter < 5) {
  counter++;
}
```
