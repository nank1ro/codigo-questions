---
language: dart
exerciseType: 1
---

# --instructions--

Utilisez une boucle `while` pour calculer la somme de tous les nombres **pairs** de `2` à `20` (inclus). Retournez la somme.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` doit être égal à `110`

```dart
  test('test1', () {
    expect(computeValue(), 110, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sum = 0;
int i = 2;
while (i <= 20) {
  sum += i;
  i += 2;
}
```
