---
language: dart
exerciseType: 1
---

# --description--

Certaines méthodes d'ordre supérieur répondent à une question sur la collection au lieu d'en construire une nouvelle. Elles prennent un prédicat et retournent un `bool` :

- `any` vaut `true` quand **au moins un** élément satisfait le prédicat
- `every` vaut `true` quand **tous** les éléments le satisfont

```dart
final numbers = [1, 2, 3];
print(numbers.any((n) => n > 2));   // true
print(numbers.every((n) => n > 2)); // false
```

Toutes deux s'arrêtent dès que la réponse est certaine : `any` au premier élément qui correspond, `every` au premier qui ne correspond pas.

Sur une collection vide, `any` vaut `false` et `every` vaut `true` : il n'y a aucun élément pour prouver la première, et aucun pour casser la seconde.

# --instructions--

Écrivez deux fonctions, prenant toutes deux une `List<int>` `numbers` :

- `hasNegative` retourne `true` si au moins un élément est inférieur à `0`
- `allEven` retourne `true` si chaque élément est pair

Exemple :
```dart
print(hasNegative([1, -2, 3])); // true
print(allEven([2, 4]));         // true
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasNegative([1, -2, 3])` devrait être `true` et `hasNegative([1, 2, 3])` devrait être `false`

```dart
  test('test1', () {
    expect(hasNegative([1, -2, 3]), true, reason: '--err-t1--');
    expect(hasNegative([1, 2, 3]), false, reason: '--err-t1--');
  });
```

`allEven([2, 4, 6])` devrait être `true` et `allEven([2, 5])` devrait être `false`

```dart
  test('test2', () {
    expect(allEven([2, 4, 6]), true, reason: '--err-t2--');
    expect(allEven([2, 5]), false, reason: '--err-t2--');
  });
```

Sur une liste vide, `hasNegative` devrait être `false` et `allEven` devrait être `true`

```dart
  test('test3', () {
    expect(hasNegative(<int>[]), false, reason: '--err-t3--');
    expect(allEven(<int>[]), true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool hasNegative(List<int> numbers) {
  return numbers.any((n) => n < 0);
}

bool allEven(List<int> numbers) {
  return numbers.every((n) => n % 2 == 0);
}
```
