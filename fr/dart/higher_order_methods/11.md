---
language: dart
exerciseType: 1
---

# --description--

Dart n'a pas de méthode `sorted`. `sort` appartient à `List`, elle réordonne la liste **sur place** et ne retourne rien :

```dart
final numbers = [3, 1, 2];
numbers.sort();
print(numbers); // [1, 2, 3]
```

Comme elle retourne `void`, vous ne pouvez pas du tout utiliser le résultat : `final sorted = numbers.sort();` donne une valeur que le compilateur refuse de vous laisser lire. L'idiome pour une **copie** ordonnée est `toList()` suivi de la cascade `..sort()` : `toList()` fait la copie, et `..` exécute `sort` dessus tout en rendant la copie elle-même.

```dart
final numbers = [3, 1, 2];
final sorted = numbers.toList()..sort();
print(sorted);  // [1, 2, 3]
print(numbers); // [3, 1, 2], untouched
```

`sort` accepte aussi un **comparateur** : une fonction de deux éléments retournant un nombre négatif quand le premier vient avant le second, `0` quand ils sont égaux, et un nombre positif sinon. `compareTo` produit exactement cela, donc trier selon n'importe quelle clé tient en une ligne :

```dart
final words = ['kiwi', 'fig', 'banana'];
print(words.toList()..sort((a, b) => a.length.compareTo(b.length)));
// [fig, kiwi, banana]
```

# --instructions--

Écrivez une fonction `byLength` qui prend une `List<String>` `words` et retourne une **nouvelle** liste avec les mêmes mots ordonnés du plus court au plus long, en laissant `words` intacte.

Exemple :
```dart
print(byLength(['kiwi', 'fig', 'banana'])); // [fig, kiwi, banana]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`byLength(['kiwi', 'fig', 'banana'])` devrait retourner `['fig', 'kiwi', 'banana']`

```dart
  test('test1', () {
    expect(byLength(['kiwi', 'fig', 'banana']), ['fig', 'kiwi', 'banana'],
        reason: '--err-t1--');
  });
```

`byLength` devrait retourner une `List<String>` et fonctionner sur une liste vide

```dart
  test('test2', () {
    expect(byLength(['bb', 'a']), isA<List<String>>(), reason: '--err-t2--');
    expect(byLength(['bb', 'a']), ['a', 'bb'], reason: '--err-t2--');
    expect(byLength(<String>[]), <String>[], reason: '--err-t2--');
  });
```

`byLength` ne doit pas modifier la liste qu'il reçoit

```dart
  test('test3', () {
    final original = ['kiwi', 'fig', 'banana'];
    final result = byLength(original);
    expect(original, ['kiwi', 'fig', 'banana'], reason: '--err-t3--');
    expect(result, ['fig', 'kiwi', 'banana'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<String> byLength(List<String> words) {
  return words.toList()..sort((a, b) => a.length.compareTo(b.length));
}
```
