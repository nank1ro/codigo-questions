---
language: dart
exerciseType: 1
---

# --instructions--

Étant donné la liste `words`, construisez un `Set<String>` nommé `duplicates` contenant uniquement les mots qui apparaissent **plus d'une fois** dans la liste. Astuce : `.add()` renvoie `false` lorsque la valeur est déjà dans le Set.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var words = ['sun', 'moon', 'sun', 'star', 'moon', 'sun'];
```

# --after-seed--

```dart
  return duplicates;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`duplicates` devrait être `{'sun', 'moon'}`

```dart
  test('test1', () {
    expect(result(), {'sun', 'moon'}, reason: '--err-t1--');
  });
```

`duplicates` devrait être un `Set`

```dart
  test('test2', () {
    expect(result(), isA<Set>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var words = ['sun', 'moon', 'sun', 'star', 'moon', 'sun'];
var seen = <String>{};
var duplicates = <String>{};
for (var word in words) {
  if (!seen.add(word)) {
    duplicates.add(word);
  }
}
```
