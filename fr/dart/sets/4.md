---
language: dart
exerciseType: 2
---

# --description--

La méthode `.remove(value)` supprime une valeur du Set. Elle renvoie `true` si la valeur était présente et `false` sinon :

```dart
var numbers = {1, 2, 3};
print(numbers.remove(2)); // true
print(numbers.remove(9)); // false
print(numbers); // {1, 3}
```

Pour supprimer tous les éléments à la fois, utilisez `.clear()`.

# --instructions--

Complétez le code pour retirer le nombre `2` du Set, puis affichez le Set.

# --seed--

```dart
var numbers = {1, 2, 3};
numbers.[/](2);
print(numbers);
```

# --answers--

- remove
- removeAt
- delete
- drop
- clear
- pop

# --solutions--

```dart
var numbers = {1, 2, 3};
numbers.remove(2);
print(numbers);
```

# --output--

{1, 3}
