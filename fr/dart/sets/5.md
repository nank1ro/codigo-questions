---
language: dart
exerciseType: 1
---

# --description--

Pour vérifier si une valeur est dans un Set, utilisez `.contains(value)`, qui renvoie un `bool`. La propriété `.isEmpty` vaut `true` quand le Set n'a aucun élément, et `.isNotEmpty` quand il en a au moins un :

```dart
var seen = {'x', 'y'};
print(seen.contains('x')); // true
print(seen.contains('z')); // false
print(seen.isEmpty);       // false
print(seen.isNotEmpty);    // true
```

# --instructions--

Étant donné le Set `seen`, stockez dans un `bool` nommé `hasX` s'il contient `'x'`, dans un `bool` nommé `hasZ` s'il contient `'z'`, et dans un `bool` nommé `empty` s'il est vide.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var seen = {'x', 'y'};
```

# --after-seed--

```dart
  return [hasX, hasZ, empty];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasX` devrait être `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasZ` devrait être `false`

```dart
  test('test2', () {
    expect(result()[1], false, reason: '--err-t2--');
  });
```

`empty` devrait être `false`

```dart
  test('test3', () {
    expect(result()[2], false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var seen = {'x', 'y'};
bool hasX = seen.contains('x');
bool hasZ = seen.contains('z');
bool empty = seen.isEmpty;
```
