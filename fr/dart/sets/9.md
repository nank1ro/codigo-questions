---
language: dart
exerciseType: 1
---

# --description--

Les Sets prennent en charge les opérations classiques sur les ensembles. Chacune renvoie un **nouveau** Set et laisse les originaux inchangés :

- `a.union(b)` contient les éléments qui sont dans `a` **ou** dans `b`
- `a.intersection(b)` contient les éléments qui sont à la fois dans `a` **et** dans `b`
- `a.difference(b)` contient les éléments de `a` qui ne sont **pas** dans `b`

```dart
var a = {1, 2, 3};
var b = {2, 3, 4};
print(a.union(b));        // {1, 2, 3, 4}
print(a.intersection(b)); // {2, 3}
print(a.difference(b));   // {1}
```

# --instructions--

Étant donné les Sets `a` et `b`, créez un Set nommé `both` avec leur union, un Set nommé `common` avec leur intersection, et un Set nommé `onlyA` avec les éléments de `a` qui ne sont pas dans `b`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var a = {1, 2, 3, 4};
var b = {3, 4, 5};
```

# --after-seed--

```dart
  return [both, common, onlyA];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`both` devrait être `{1, 2, 3, 4, 5}`

```dart
  test('test1', () {
    expect(result()[0], {1, 2, 3, 4, 5}, reason: '--err-t1--');
  });
```

`common` devrait être `{3, 4}`

```dart
  test('test2', () {
    expect(result()[1], {3, 4}, reason: '--err-t2--');
  });
```

`onlyA` devrait être `{1, 2}`

```dart
  test('test3', () {
    expect(result()[2], {1, 2}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var a = {1, 2, 3, 4};
var b = {3, 4, 5};
var both = a.union(b);
var common = a.intersection(b);
var onlyA = a.difference(b);
```
