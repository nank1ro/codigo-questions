---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez une variable `int` de niveau supérieur `remainder` avec la valeur `29`. Utilisez ensuite `%=` pour lui affecter le reste de la division par `6`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`remainder` devrait être `5`

```dart
  test('test1', () {
    expect(remainder, 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 29;

void main() {
  remainder %= 6;
  print(remainder);
}
```
