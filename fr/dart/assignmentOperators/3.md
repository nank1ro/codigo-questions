---
language: dart
exerciseType: 2
---

# --description--

The **subtraction assignment operator** `-=` soustrait une valeur d'une variable et stocke le résultat dans cette variable.

```dart
int lives = 3;
lives -= 1; // lives is now 2
```

C'est un raccourci pour `lives = lives - 1`.

# --instructions--

Complétez le code pour soustraire `5` de la variable `stock` en utilisant `-=`.

# --seed--

```dart
void main() {
  int stock = 20;
  stock [/] 5;
  print(stock);
}
```

# --answers--

- -=
- =
- +=
- -

# --solutions--

```dart
void main() {
  int stock = 20;
  stock -= 5;
  print(stock);
}
```

# --output--

15
