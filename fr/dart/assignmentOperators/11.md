---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez une variable `double` de niveau supérieur `value` avec la valeur `20.0`. Utilisez ensuite `/=` pour la diviser par `4`. Notez que `/=` produit toujours un `double` en Dart.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value` devrait être `5.0`

```dart
  test('test1', () {
    expect(value, closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;

void main() {
  value /= 4;
  print(value);
}
```
