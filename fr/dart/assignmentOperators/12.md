---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez une variable `int` `apples` avec la valeur `17`. Utilisez ensuite `~/=` pour la diviser entièrement par `5`. L'opérateur `~/=` effectue une division entière et retourne toujours un `int`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int apples = 17;
```

# --after-seed--

```dart
  return apples;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`apples` devrait être `3`

```dart
  test('test1', () {
    expect(result(), 3, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int apples = 17;
apples ~/= 5;
```
