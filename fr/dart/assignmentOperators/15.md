---
language: dart
exerciseType: 1
---

# --instructions--

Un magasin applique une réduction de 20% sur un article. Déclarez une variable `double` `price` avec la valeur `50.0`. Utilisez ensuite `*=` pour appliquer la réduction en multipliant `price` par `0.8`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
double price = 50.0;
```

# --after-seed--

```dart
  return price;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`price` devrait être `40.0`

```dart
  test('test1', () {
    expect(result(), closeTo(40.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double price = 50.0;
price *= 0.8;
```
