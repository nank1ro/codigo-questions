---
language: dart
exerciseType: 3
---

# --description--

Les opérateurs d'affectation combinent une opération arithmétique avec une affectation en une seule étape.

Par exemple, `x += 3` est un raccourci pour `x = x + 3`.

# --instructions--

Que fait `x += 5` quand `x` vaut actuellement `10` ?

# --answers--

- Définit `x` à `15`
- Définit `x` à `5`
- Définit `x` à `10`
- Définit `x` à `50`

# --solutions--

- Définit `x` à `15`
