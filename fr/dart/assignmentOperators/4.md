---
language: dart
exerciseType: 2
---

# --description--

The **multiplication assignment operator** `*=` multiplie une variable par une valeur et stocke le résultat dans cette variable.

```dart
int side = 4;
side *= 3; // side is now 12
```

C'est un raccourci pour `side = side * 3`.

# --instructions--

Complétez le code pour multiplier `price` par `3` en utilisant `*=`.

# --seed--

```dart
void main() {
  int price = 7;
  price [/] 3;
  print(price);
}
```

# --answers--

- *=
- =
- +=
- *

# --solutions--

```dart
void main() {
  int price = 7;
  price *= 3;
  print(price);
}
```

# --output--

21
