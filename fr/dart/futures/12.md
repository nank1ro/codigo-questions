---
language: dart
exerciseType: 4
---

# --description--

`Future.wait` est l'outil pour « charger plusieurs choses, puis continuer ». La forme typique est : construire la liste des futures, faire `await Future.wait` dessus, puis utiliser la liste obtenue :

```dart
Future<int> stock() => Future.delayed(const Duration(milliseconds: 10), () => 8);
Future<int> orders() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<void> main() async {
  final counts = await Future.wait([stock(), orders()]);
  print(counts); // [8, 3]
}
```

`orders()` se termine en premier, mais la liste suit quand même l'ordre des appels : `stock()` en premier, `orders()` en second.

# --instructions--

Trie les éléments pour définir une fonction `one` qui se termine par `1` après `10` millisecondes, une fonction `two` qui se termine par `2` après `5` millisecondes, et une `main` `async` qui affiche `loading`, attend les deux fonctions avec `Future.wait` dans `values` et affiche `values`. Définis `one` en premier, puis `two`, puis `main`. Le programme doit afficher :

```
loading
[1, 2]
```

# --answers--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
```

```dart
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);
```

```dart
Future<void> main() async {
```

```dart
  print('loading');
```

```dart
  final values = await Future.wait([one(), two()]);
```

```dart
  print(values);
```

```dart
}
```

# --solutions--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);

Future<void> main() async {
  print('loading');
  final values = await Future.wait([one(), two()]);
  print(values);
}
```

# --output--

loading
[1, 2]
