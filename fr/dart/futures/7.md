---
language: dart
exerciseType: 4
---

# --description--

Quand une fonction rencontre un `await`, elle se **met en pause** à cette ligne et le reste du programme continue. Les lignes après l'`await` ne s'exécutent qu'une fois le future terminé. Lire une fonction `async` de haut en bas te donne donc l'ordre exact de ses effets :

```dart
Future<int> load() async {
  await Future.delayed(const Duration(milliseconds: 5));
  return 7;
}

Future<void> main() async {
  print('loading');
  final n = await load();
  print('value: $n');
  print('done');
}
// loading
// value: 7
// done
```

# --instructions--

Trie les éléments pour définir une fonction `async` `fetchName` qui attend `10` millisecondes puis renvoie `'Ada'`, et une `main` `async` qui affiche `fetching`, puis attend `fetchName()` dans `name`, puis affiche `Hello, ` suivi du nom et de `!`, et enfin affiche `done`. Définis `fetchName` avant `main`. Le programme doit afficher :

```
fetching
Hello, Ada!
done
```

# --answers--

```dart
Future<String> fetchName() async {
```

```dart
  await Future.delayed(const Duration(milliseconds: 10));
```

```dart
  return 'Ada';
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  print('fetching');
```

```dart
  final name = await fetchName();
```

```dart
  print('Hello, $name!');
```

```dart
  print('done');
```

```dart
}
```

# --solutions--

```dart
Future<String> fetchName() async {
  await Future.delayed(const Duration(milliseconds: 10));
  return 'Ada';
}

Future<void> main() async {
  print('fetching');
  final name = await fetchName();
  print('Hello, $name!');
  print('done');
}
```

# --output--

fetching
Hello, Ada!
done
