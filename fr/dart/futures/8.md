---
language: dart
exerciseType: 1
---

# --description--

Un future peut aussi se terminer par une **erreur**. Quand une fonction `async` lève une exception, celle-ci ne s'échappe pas immédiatement : elle devient l'erreur du future renvoyé. Celui qui fait `await` sur ce future voit l'erreur levée au niveau de l'`await`, elle peut donc être traitée avec un `try`/`catch` ordinaire :

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s); // throws FormatException for 'abc'
}

Future<int> orZero(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return 0;
  }
}
```

L'`await` à l'intérieur du `try` est essentiel : `return parseLater(s);` remettrait le future à l'appelant **sans attendre**, l'erreur arriverait donc alors que le bloc `try` est déjà terminé et le `catch` ne s'exécuterait jamais.

# --instructions--

La fonction `parseLater(s)` renvoie un `Future<int>` qui se termine par le nombre écrit dans `s`, ou par une erreur si `s` n'est pas un nombre. Écris une fonction `async` `safeParse` qui prend un `String` `s` et renvoie un `Future<int>` se terminant par le nombre analysé, ou par `-1` si `parseLater` échoue.

Exemple :
```dart
print(await safeParse('12'));  // 12
print(await safeParse('abc')); // -1
```

# --before-seed--

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s);
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeParse('12')` devrait se terminer par `12`

```dart
  test('test1', () async {
    expect(await safeParse('12'), 12, reason: '--err-t1--');
  });
```

`safeParse('abc')` devrait se terminer par `-1` au lieu de lever une exception

```dart
  test('test2', () async {
    expect(await safeParse('abc'), -1, reason: '--err-t2--');
  });
```

`safeParse('-7')` devrait se terminer par `-7` et `safeParse('')` par `-1`

```dart
  test('test3', () async {
    expect(await safeParse('-7'), -7, reason: '--err-t3--');
    expect(await safeParse(''), -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> safeParse(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return -1;
  }
}
```
