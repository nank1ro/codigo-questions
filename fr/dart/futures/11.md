---
language: dart
exerciseType: 1
---

# --description--

Quand plusieurs futures ne dépendent pas les uns des autres, confie-les tous à **`Future.wait`** : il prend une `List<Future<T>>`, les laisse s'exécuter en même temps et renvoie un unique `Future<List<T>>` qui se termine quand **tous** sont finis. Les résultats conservent l'ordre de la liste d'entrée, quel que soit le future terminé en premier :

```dart
Future<String> slow() => Future.delayed(const Duration(milliseconds: 20), () => 'slow');
Future<String> fast() => Future.delayed(const Duration(milliseconds: 5), () => 'fast');

final results = await Future.wait([slow(), fast()]);
print(results); // [slow, fast]
```

`await slow(); await fast();` prend `25` millisecondes, car `fast()` n'est appelé qu'une fois `slow()` terminé ; `await Future.wait([slow(), fast()])` prend environ `20`, la durée du plus long.

# --instructions--

Écris une fonction `async` `sumOfFutures` qui prend une `List<Future<int>>` `futures`, les attend tous avec `Future.wait` et renvoie un `Future<int>` se terminant par la somme de leurs valeurs. Une liste vide donne `0`.

Exemple :
```dart
final futures = [Future.value(1), Future.delayed(const Duration(milliseconds: 5), () => 2)];
print(await sumOfFutures(futures)); // 3
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumOfFutures` de futures se terminant par `1`, `2` et `3` devrait se terminer par `6`

```dart
  test('test1', () async {
    final futures = [
      Future.value(1),
      Future.delayed(const Duration(milliseconds: 5), () => 2),
      Future.value(3),
    ];
    expect(await sumOfFutures(futures), 6, reason: '--err-t1--');
  });
```

`sumOfFutures` d'un seul future se terminant par `10` devrait se terminer par `10`

```dart
  test('test2', () async {
    expect(await sumOfFutures([Future.delayed(const Duration(milliseconds: 5), () => 10)]), 10, reason: '--err-t2--');
  });
```

`sumOfFutures` d'une liste vide devrait se terminer par `0` et `sumOfFutures` devrait renvoyer un `Future<int>`

```dart
  test('test3', () async {
    expect(await sumOfFutures(<Future<int>>[]), 0, reason: '--err-t3--');
    expect(sumOfFutures([Future.value(4)]), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> sumOfFutures(List<Future<int>> futures) async {
  final values = await Future.wait(futures);
  var total = 0;
  for (final v in values) {
    total += v;
  }
  return total;
}
```
