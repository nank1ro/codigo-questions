---
language: dart
exerciseType: 1
---

# --description--

Tu n'as pas besoin de `Future.wait` pour exécuter deux futures en même temps. Une fonction `async` commence à s'exécuter dès qu'elle est **appelée**, jusqu'à son premier `await` ; le future que tu récupères est le travail déjà en cours. L'astuce est donc : **appeler d'abord, attendre ensuite** :

```dart
Future<int> pages() => Future.delayed(const Duration(milliseconds: 20), () => 300);
Future<int> words() => Future.delayed(const Duration(milliseconds: 20), () => 90000);

Future<double> average() async {
  final p = pages();          // both timers start now
  final w = words();
  return await w / await p;   // waits once, about 20 ms in total
}
```

Compare avec `return await words() / await pages();`, où `pages()` n'est appelé qu'une fois `words()` terminé : même résultat, deux fois plus de temps. Préfère la forme concurrente chaque fois que le second appel n'a pas besoin du résultat du premier.

# --instructions--

La fonction `slow(value)` renvoie un `Future<int>` se terminant par `value` après un court délai, et elle enregistre `start <value>` quand elle est appelée et `end <value>` quand elle se termine dans la liste `calls`. Écris une fonction `async` `addConcurrently` qui prend deux `int` `a` et `b`, appelle `slow(a)` et `slow(b)` de sorte que les deux s'exécutent **en même temps**, et renvoie un `Future<int>` se terminant par la somme des deux résultats.

Exemple :
```dart
print(await addConcurrently(2, 3)); // 5
print(calls); // [start 2, start 3, end 2, end 3]
```

# --before-seed--

```dart
final calls = <String>[];

Future<int> slow(int value) async {
  calls.add('start $value');
  await Future.delayed(const Duration(milliseconds: 5));
  calls.add('end $value');
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`addConcurrently(2, 3)` devrait se terminer par `5`

```dart
  test('test1', () async {
    calls.clear();
    expect(await addConcurrently(2, 3), 5, reason: '--err-t1--');
  });
```

`addConcurrently(2, 3)` devrait appeler `slow(2)` et `slow(3)` avant que l'un des deux ne se termine, donc `calls` devrait valoir `[start 2, start 3, end 2, end 3]`

```dart
  test('test2', () async {
    calls.clear();
    await addConcurrently(2, 3);
    expect(calls, ['start 2', 'start 3', 'end 2', 'end 3'], reason: '--err-t2--');
  });
```

`addConcurrently(10, -4)` devrait se terminer par `6` et les deux appels devraient démarrer avant que l'un des deux ne se termine

```dart
  test('test3', () async {
    calls.clear();
    expect(await addConcurrently(10, -4), 6, reason: '--err-t3--');
    expect(calls, ['start 10', 'start -4', 'end 10', 'end -4'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> addConcurrently(int a, int b) async {
  final first = slow(a);
  final second = slow(b);
  return await first + await second;
}
```
