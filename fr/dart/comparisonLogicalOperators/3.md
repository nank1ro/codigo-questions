---
language: dart
exerciseType: 2
---

# --description--

L'**opérateur supérieur à** `>` renvoie `true` lorsque la valeur de gauche est strictement plus grande que la valeur de droite.

```dart
int a = 8;
int b = 3;
bool result = a > b; // true
```

Si la valeur de gauche est égale ou inférieure à celle de droite, il renvoie `false`.

# --instructions--

Complétez le code pour vérifier si `15` est plus grand que `6` et stockez le résultat dans `isGreater`.

# --seed--

```dart
void main() {
  bool isGreater = 15 [/] 6;
  print(isGreater);
}
```

# --answers--

- >
- <
- >=
- ==

# --solutions--

```dart
void main() {
  bool isGreater = 15 > 6;
  print(isGreater);
}
```

# --output--

true
