---
language: dart
exerciseType: 2
---

# --description--

L'**opérateur inférieur à** `<` renvoie `true` lorsque la valeur de gauche est strictement plus petite que la valeur de droite.

```dart
int a = 2;
int b = 9;
bool result = a < b; // true
```

Si la valeur de gauche est égale ou supérieure à celle de droite, il renvoie `false`.

# --instructions--

Complétez le code pour vérifier si `3` est plus petit que `10` et stockez le résultat dans `isSmaller`.

# --seed--

```dart
void main() {
  bool isSmaller = 3 [/] 10;
  print(isSmaller);
}
```

# --answers--

- <
- >
- <=
- !=

# --solutions--

```dart
void main() {
  bool isSmaller = 3 < 10;
  print(isSmaller);
}
```

# --output--

true
