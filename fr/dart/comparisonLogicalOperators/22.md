---
language: dart
exerciseType: 4
---

# --instructions--

Triez les lignes de code pour déclarer deux variables booléennes, les combiner avec `||` et `&&` comme indiqué, stocker le résultat dans `outcome` et l'imprimer. La sortie doit être `true`.

# --answers--

```dart
void main() {
```

```dart
  bool hasPass = false;
```

```dart
  bool isAdmin = true;
```

```dart
  bool outcome = hasPass || isAdmin && !hasPass;
```

```dart
  print(outcome);
```

```dart
}
```

# --solutions--

```dart
void main() {
  bool hasPass = false;
  bool isAdmin = true;
  bool outcome = hasPass || isAdmin && !hasPass;
  print(outcome);
}
```

# --output--

true
