---
language: dart
exerciseType: 1
---

# --instructions--

Déclare deux variables `int` de premier niveau : `high` avec la valeur `20` et `low` avec la valeur `12`. Déclare ensuite une variable `bool` de premier niveau nommée `isHigher` qui stocke le résultat de la vérification indiquant si `high` est supérieur à `low` à l'aide de l'opérateur `>`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHigher` doit être `true` et de type `bool`

```dart
  test('test1', () {
    expect(isHigher, true, reason: '--err-t1--');
    expect(isHigher, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int high = 20;
int low = 12;
bool isHigher = high > low;

void main() {
  print(isHigher);
}
```
