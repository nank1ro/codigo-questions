---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez une variable `int` de niveau supérieur appelée `age` avec la valeur `35`. Puis déclarez une variable `bool` de niveau supérieur appelée `isWorkingAge` qui est `true` quand `age` est plus grand que ou égal à `18` ET plus petit que ou égal à `65`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isWorkingAge` doit être `true` et de type `bool`

```dart
  test('test1', () {
    expect(isWorkingAge, true, reason: '--err-t1--');
    expect(isWorkingAge, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int age = 35;
bool isWorkingAge = age >= 18 && age <= 65;

void main() {
  print(isWorkingAge);
}
```
