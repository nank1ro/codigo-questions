---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez trois variables `int` de niveau supérieur : `number` avec la valeur `42`, `min` avec la valeur `1` et `max` avec la valeur `100`. Puis déclarez une variable `bool` de niveau supérieur appelée `inRange` qui est `true` quand `number` est plus grand que ou égal à `min` ET plus petit que ou égal à `max`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`inRange` doit être `true` et de type `bool`

```dart
  test('test1', () {
    expect(inRange, true, reason: '--err-t1--');
    expect(inRange, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int number = 42;
int min = 1;
int max = 100;
bool inRange = number >= min && number <= max;

void main() {
  print(inRange);
}
```
