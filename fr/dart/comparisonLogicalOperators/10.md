---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez deux variables `int` de niveau supérieur : `x` avec la valeur `8` et `y` avec la valeur `8`. Puis déclarez une variable `bool` de niveau supérieur appelée `areEqual` qui stocke le résultat de la comparaison de `x` et `y` avec l'opérateur `==`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`areEqual` doit être `true` et de type `bool`

```dart
  test('test1', () {
    expect(areEqual, true, reason: '--err-t1--');
    expect(areEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 8;
int y = 8;
bool areEqual = x == y;

void main() {
  print(areEqual);
}
```
