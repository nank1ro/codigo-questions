---
language: dart
exerciseType: 1
---

# --description--

L'**opérateur ET** `&&` combine deux expressions booléennes. Il renvoie `true` uniquement lorsque **les deux** côtés sont `true`. Si l'un des côtés est `false`, le résultat est `false`.

```dart
bool a = true;
bool b = true;
bool result = a && b; // true

bool c = true;
bool d = false;
bool result2 = c && d; // false
```

# --instructions--

Declare two top-level `int` variables: `age` with the value `25` and `score` with the value `80`. Then declare a top-level `bool` variable named `qualifies` that is `true` only when `age` is greater than or equal to `18` AND `score` is greater than or equal to `60`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`qualifies` must be `true` and of type `bool`

```dart
  test('test1', () {
    expect(qualifies, true, reason: '--err-t1--');
    expect(qualifies, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int age = 25;
int score = 80;
bool qualifies = age >= 18 && score >= 60;

void main() {
  print(qualifies);
}
```
