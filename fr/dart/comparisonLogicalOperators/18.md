---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez trois variables `int` de niveau supérieur : `x` avec la valeur `5`, `y` avec la valeur `3` et `z` avec la valeur `0`. Puis déclarez une variable `bool` de niveau supérieur appelée `result` qui évalue `!(x > 10) || (y < 5 && z == 0)`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` doit être `true` et de type `bool`

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 5;
int y = 3;
int z = 0;
bool result = !(x > 10) || (y < 5 && z == 0);

void main() {
  print(result);
}
```
