---
language: dart
exerciseType: 4
---

# --instructions--

Triez les lignes de code pour déclarer une valeur de température, vérifier si elle est dans la plage `0` à `100` (inclus) en utilisant `&&`, stocker le résultat dans `isValid` et l'imprimer. La sortie doit être `true`.

# --answers--

```dart
void main() {
```

```dart
  int temperature = 37;
```

```dart
  bool isValid = temperature >= 0 && temperature <= 100;
```

```dart
  print(isValid);
```

```dart
}
```

# --solutions--

```dart
void main() {
  int temperature = 37;
  bool isValid = temperature >= 0 && temperature <= 100;
  print(isValid);
}
```

# --output--

true
