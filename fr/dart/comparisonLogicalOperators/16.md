---
language: dart
exerciseType: 1
---

# --instructions--

L'**opérateur NOT** `!` inverse une valeur booléenne : `!true` devient `false` et `!false` devient `true`.

Déclarez une variable `bool` de niveau supérieur appelée `isOffline` avec la valeur `false`. Puis déclarez une variable `bool` de niveau supérieur appelée `isOnline` qui stocke la négation de `isOffline` en utilisant l'opérateur `!`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isOnline` doit être `true` et de type `bool`

```dart
  test('test1', () {
    expect(isOnline, true, reason: '--err-t1--');
    expect(isOnline, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isOffline = false;
bool isOnline = !isOffline;

void main() {
  print(isOnline);
}
```
