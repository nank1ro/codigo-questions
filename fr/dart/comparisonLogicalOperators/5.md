---
language: dart
exerciseType: 2
---

# --description--

L'**opérateur supérieur ou égal à** `>=` retourne `true` quand la valeur de gauche est plus grande que **ou égale à** la valeur de droite.

```dart
int score = 50;
bool passed = score >= 50; // true
```

Contrairement à `>`, cet opérateur retourne aussi `true` quand les deux valeurs sont égales.

# --instructions--

Complétez le code pour vérifier si `7` est plus grand que ou égal à `7` et stockez le résultat dans `isAtLeastSeven`.

# --seed--

```dart
void main() {
  bool isAtLeastSeven = 7 [/] 7;
  print(isAtLeastSeven);
}
```

# --answers--

- >=
- >
- <=
- ==

# --solutions--

```dart
void main() {
  bool isAtLeastSeven = 7 >= 7;
  print(isAtLeastSeven);
}
```

# --output--

true
