---
language: dart
exerciseType: 1
---

# --instructions--

Déclare deux variables `int` de premier niveau : `a` avec la valeur `5` et `b` avec la valeur `9`. Déclare ensuite une variable `bool` de premier niveau nommée `notEqual` qui stocke le résultat de la comparaison de `a` et `b` avec l'opérateur `!=`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`notEqual` doit être `true` et de type `bool`

```dart
  test('test1', () {
    expect(notEqual, true, reason: '--err-t1--');
    expect(notEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 5;
int b = 9;
bool notEqual = a != b;

void main() {
  print(notEqual);
}
```
