---
language: dart
exerciseType: 1
---

# --instructions--

Déclare deux variables `int` de premier niveau : `price` avec la valeur `30` et `budget` avec la valeur `50`. Déclare ensuite une variable `bool` de premier niveau nommée `canAfford` qui stocke le résultat de la vérification indiquant si `price` est inférieur à `budget` à l'aide de l'opérateur `<`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canAfford` doit être `true` et de type `bool`

```dart
  test('test1', () {
    expect(canAfford, true, reason: '--err-t1--');
    expect(canAfford, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int price = 30;
int budget = 50;
bool canAfford = price < budget;

void main() {
  print(canAfford);
}
```
