---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez une variable booléenne appelée `flag` avec la valeur `true`, puis déclarez une autre variable booléenne appelée `isFlag` et utilisez le mot-clé `is` pour vérifier si `flag` est de type `bool`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`flag` doit être égal à `true`.

```dart
  test('test1', () {
    expect(flag, true, reason: '--err-t1--');
  });
```

`isFlag` doit être `true` (le résultat de la vérification `is bool`).

```dart
  test('test2', () {
    expect(isFlag, true, reason: '--err-t2--');
  });
```

`isFlag` doit être de type `bool`.

```dart
  test('test3', () {
    expect(isFlag, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool flag = true;
bool isFlag = flag is bool;
```
