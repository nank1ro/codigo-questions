---
language: dart
exerciseType: 1
---

# --instructions--

Déclarez une variable booléenne appelée `isEmpty` et assignez-lui la valeur `false`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isEmpty` doit être égal à `false`.

```dart
  test('test1', () {
    expect(isEmpty, false, reason: '--err-t1--');
  });
```

`isEmpty` doit être de type `bool`.

```dart
  test('test2', () {
    expect(isEmpty, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isEmpty = false;
```
