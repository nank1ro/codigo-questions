---
language: dart
exerciseType: 2
---

# --description--

L'__opérateur de négation__ `!` (aussi appelé "non") inverse une valeur booléenne.
Appliquer `!` à `true` donne `false`, et appliquer `!` à `false` donne `true`.

```dart
bool isActive = true;
print(!isActive); // false
```

C'est utile quand vous voulez vérifier le contraire d'une condition.

# --instructions--

Étant donné la variable `isBlocked` définie à `false`, affichez sa valeur négée en utilisant `!`.

# --seed--

```dart
bool isBlocked = false;
print([/]isBlocked);
```

# --answers--

- !
- !!
- ~
- not

# --solutions--

```dart
bool isBlocked = false;
print(!isBlocked);
```

# --output--

true
