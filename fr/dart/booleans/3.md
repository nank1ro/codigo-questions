---
language: dart
exerciseType: 2
---

# --description--

Une variable booléenne peut également contenir la valeur `false`.
Utilisez `false` quand quelque chose n'est pas le cas.

```dart
bool isLoggedIn = false;
print(isLoggedIn); // false
```

Comme `true`, `false` doit être écrit en minuscules.

# --instructions--

Déclarez une variable booléenne appelée `isExpired` et assignez-lui la valeur `false`, puis affichez-la.

# --seed--

```dart
[/] [/] = [/];
print([/]);
```

# --answers--

- bool
- isExpired
- false
- true
- print
- String

# --solutions--

```dart
bool isExpired = false;
print(isExpired);
```

# --output--

false
