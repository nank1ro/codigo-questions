---
language: dart
exerciseType: 1
---

# --instructions--

Combinez `&&`, `||` et `!` pour produire un résultat.

Déclarez une variable booléenne appelée `result` en utilisant cette expression exacte : `!false && (true || false)`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` doit être égal à `true`.

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result` doit être de type `bool`.

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = !false && (true || false);
```
