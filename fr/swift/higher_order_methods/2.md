---
language: swift
exerciseType: 1
---

# --description--

`flatMap` est destiné aux fermetures qui renvoient un **tableau** : au lieu de construire un tableau de tableaux, elle réunit tous les tableaux renvoyés en un seul résultat à plat :
```swift
let teams = [["Ann", "Bob"], ["Cid"]]
print(teams.map { $0 })     // [["Ann", "Bob"], ["Cid"]]
print(teams.flatMap { $0 }) // ["Ann", "Bob", "Cid"]
```
La fermeture peut aussi transformer chaque tableau interne avant l'aplanissement, par exemple `teams.flatMap { $0.reversed() }` donne `["Bob", "Ann", "Cid"]`.

# --instructions--

Écris une fonction `flatten` qui prend un tableau de tableaux de `Int` (omets le libellé de l'argument) et renvoie un seul tableau avec tous les nombres dans l'ordre, en utilisant `flatMap`

Exemple d'appel de fonction :
```swift
print(flatten([[1, 2], [3]]))
// prints [1, 2, 3]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Tu dois utiliser `flatMap`

```json
{
  "regex": "\\.flatMap",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`flatten([[1, 2], [3]])` doit renvoyer `[1, 2, 3]`

```swift
tryCatch(flatten([[1, 2], [3]]) == [1, 2, 3])
```

`flatten([[5], [], [6, 7]])` doit renvoyer `[5, 6, 7]`

```swift
tryCatch(flatten([[5], [], [6, 7]]) == [5, 6, 7])
```

`flatten([])` doit renvoyer `[]`

```swift
tryCatch(flatten([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func flatten(_ lists: [[Int]]) -> [Int] {
    return lists.flatMap { $0 }
}
```
