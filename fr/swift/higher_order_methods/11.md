---
language: swift
exerciseType: 2
---

# --description--

`zip` associe les éléments de deux séquences position par position, produisant une séquence de tuples. Elle s'arrête à la fin de la plus courte :
```swift
let names = ["Ann", "Bob"]
let ages = [31, 27, 99]
let pairs = zip(names, ages).map { "\($0) is \($1)" }
print(pairs) // ["Ann is 31", "Bob is 27"]
```
Dans la fermeture, `$0` est l'élément de la première séquence et `$1` celui de la seconde. `zip` est une fonction libre, pas une méthode : tu écris `zip(a, b)`, pas `a.zip(b)`.

# --instructions--

Complète le code pour associer chaque ville à sa température et ne garder que les villes plus chaudes que `20` degrés, de sorte que le programme affiche `["Rome", "Cairo"]`

# --seed--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = [/](cities, temps).[/] { $1 > 20 }.map { $0.0 }
print(warm)
```

# --answers--

- zip
- filter
- map
- enumerated
- compactMap

# --solutions--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = zip(cities, temps).filter { $1 > 20 }.map { $0.0 }
print(warm)
```

# --output--

["Rome", "Cairo"]
