---
language: swift
exerciseType: 1
---

# --description--

`map` et `filter` sur un tableau sont **avides** : chacun traite tout le tableau et en construit un nouveau avant que l'étape suivante ne s'exécute. Sur une grande collection, ou quand seul le premier résultat est nécessaire, c'est du travail gaspillé.
La propriété `lazy` renvoie une vue dont les opérations ne s'exécutent que quand un élément est réellement demandé, un élément à la fois à travers toute la chaîne :
```swift
let firstBig = (1...1000).lazy.map { $0 * $0 }.first { $0 > 50 }
print(firstBig!) // 64
```
Ici seuls `1, 2, ..., 8` sont élevés au carré : `first(where:)` demande des éléments jusqu'à ce que l'un satisfasse la condition, et la chaîne s'arrête là. Sans `lazy`, `map` élèverait d'abord au carré les 1000 nombres.

# --instructions--

Écris une fonction `firstCubeAbove` qui prend un `Int` appelé `limit` (omets le libellé de l'argument) et renvoie le plus petit cube (`n * n * n` pour `n` à partir de `1`) supérieur à `limit`, en utilisant `lazy` et `map` sur l'intervalle `1...limit + 1` et `first(where:)`

Exemple d'appel de fonction :
```swift
print(firstCubeAbove(10))
// prints 27
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Tu dois utiliser `lazy`

```json
{
  "regex": "\\.lazy",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Tu ne dois pas utiliser de boucle

```json
{
  "regex": "\\b(for|while)\\b",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`firstCubeAbove(10)` doit renvoyer `27`

```swift
tryCatch(firstCubeAbove(10) == 27)
```

`firstCubeAbove(27)` doit renvoyer `64`

```swift
tryCatch(firstCubeAbove(27) == 64)
```

`firstCubeAbove(0)` doit renvoyer `1`

```swift
tryCatch(firstCubeAbove(0) == 1)
```

`firstCubeAbove(1000000)` doit renvoyer `1030301`

```swift
tryCatch(firstCubeAbove(1000000) == 1030301)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstCubeAbove(_ limit: Int) -> Int {
    return (1...limit + 1).lazy.map { $0 * $0 * $0 }.first(where: { $0 > limit })!
}
```
