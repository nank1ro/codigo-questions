---
language: swift
exerciseType: 1
---

# --description--

Certaines fonctions d'ordre supérieur répondent à une question sur la collection au lieu de la transformer. Elles prennent toutes une fermeture qui renvoie un `Bool` :
- `first(where:)` renvoie le premier élément qui satisfait la fermeture, ou `nil` s'il n'y en a aucun
- `contains(where:)` renvoie `true` si au moins un élément la satisfait
- `allSatisfy` renvoie `true` si tous les éléments la satisfont

```swift
let nums = [4, 9, 16]
print(nums.first(where: { $0 > 5 }))  // Optional(9)
print(nums.contains(where: { $0 > 5 })) // true
print(nums.allSatisfy { $0 > 5 })       // false
```
Contrairement à `filter`, `first(where:)` s'arrête à la première correspondance et ne construit pas de nouveau tableau.

# --instructions--

Écris une fonction `firstLong` qui prend un tableau de `String` et un `Int` appelé `minLength` (omets les deux libellés d'arguments) et renvoie la première chaîne d'au moins `minLength` caractères, ou `nil` s'il n'y en a aucune, en utilisant `first(where:)`

Exemple d'appel de fonction :
```swift
print(firstLong(["hi", "hello", "hey"], 4))
// prints Optional("hello")
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Tu dois utiliser `first(where:)`

```json
{
  "regex": "\\.first\\s*\\(\\s*where",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstLong(["hi", "hello", "hey"], 4)` doit renvoyer `"hello"`

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 4) == "hello")
```

`firstLong(["hi", "hello", "hey"], 2)` doit renvoyer `"hi"`

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 2) == "hi")
```

`firstLong(["hi", "hey"], 4)` doit renvoyer `nil`

```swift
tryCatch(firstLong(["hi", "hey"], 4) == nil)
```

`firstLong([], 1)` doit renvoyer `nil`

```swift
tryCatch(firstLong([], 1) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstLong(_ words: [String], _ minLength: Int) -> String? {
    return words.first(where: { $0.count >= minLength })
}
```
