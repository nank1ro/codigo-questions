---
language: swift
exerciseType: 1
---

# --description--

Tu peux écrire tes propres fonctions d'ordre supérieur. Une fonction qui prend une fermeture et **renvoie une nouvelle fermeture** construite à partir d'elle est un schéma courant : la fermeture renvoyée capture l'originale, si bien que le paramètre doit être `@escaping`.
Par exemple, `negate` transforme un prédicat en son contraire, prêt à être passé à `filter` :
```swift
func negate(_ predicate: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { !predicate($0) }
}
let isEven: (Int) -> Bool = { $0 % 2 == 0 }
print([1, 2, 3, 4].filter(negate(isEven))) // [1, 3]
```
Note que `filter(negate(isEven))` passe la fermeture comme un argument normal, sans la syntaxe de fermeture en fin d'appel.

# --instructions--

Écris une fonction `both` qui prend deux fermetures de type `(Int) -> Bool` (omets les deux libellés d'arguments) et renvoie une fermeture du même type qui vaut `true` uniquement quand les deux valent `true` pour le même argument

Exemple d'appel de fonction :
```swift
let smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
print([4, 12, 7, 8].filter(smallEven))
// prints [4, 8]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`both({ $0 < 10 }, { $0 % 2 == 0 })` doit valoir `true` pour `4`

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(4) == true)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })` doit valoir `false` pour `12`

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(12) == false)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })` doit valoir `false` pour `7`

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(7) == false)
```

La fermeture renvoyée doit fonctionner avec `filter`

```swift
let _smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
tryCatch([4, 12, 7, 8].filter(_smallEven) == [4, 8])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func both(_ first: @escaping (Int) -> Bool, _ second: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { first($0) && second($0) }
}
```
