---
language: swift
exerciseType: 4
---

# --description--

Après le résumé, un commentaire de documentation peut décrire les paramètres et la valeur de retour avec des éléments de liste Markdown spéciaux que Xcode reconnaît :
```swift
/// Renvoie le nombre de secondes dans les minutes données.
/// - Parameter minutes: un nombre entier de minutes, jamais négatif
/// - Returns: `minutes` multiplié par soixante
func toSeconds(_ minutes: Int) -> Int {
    return minutes * 60
}
```
L'ordre est toujours le même : le résumé d'abord, puis `- Parameter name:` pour chaque paramètre, puis `- Returns:`.

# --instructions--

Trie les éléments pour documenter `twice` avec un résumé, une ligne `Parameter` et une ligne `Returns`, dans cet ordre et directement au-dessus de la fonction, puis affiche `twice(21)`

# --answers--

```swift
/// Renvoie le double d'un nombre.
```

```swift
/// - Parameter n: le nombre à doubler
```

```swift
/// - Returns: `n` multiplié par deux
```

```swift
func twice(_ n: Int) -> Int {
    return n * 2
}
```

```swift
print(twice(21))
```

# --solutions--

```swift
/// Renvoie le double d'un nombre.
/// - Parameter n: le nombre à doubler
/// - Returns: `n` multiplié par deux
func twice(_ n: Int) -> Int {
    return n * 2
}
print(twice(21))
```

# --output--

42
