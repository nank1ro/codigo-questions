---
language: swift
exerciseType: 1
---

# --description--

Pour mettre plusieurs lignes en commentaire d'un coup, enveloppe-les dans un seul commentaire de bloc au lieu d'ajouter `//` à chaque ligne :
```swift
var total = 100
/*
total = total - 30
total = total - 20
*/
print(total) // prints 100
```
Grâce à l'imbrication, cela fonctionne même quand l'une de ces lignes contient déjà un commentaire `/* */`.

# --instructions--

Mets en commentaire les deux lignes qui modifient `total` avec un seul commentaire de bloc, pour que `total` reste `100`. Garde le commentaire interne `/* base */` tel quel

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
var total = 100
total = total /* base */ - 30
total = total - 20
```

# --asserts--

Les deux lignes doivent être enveloppées dans un commentaire de bloc qui contient toujours `/* base */`

```json
{
  "regex": "/\\*[\\s\\S]*total = total /\\* base \\*/ - 30[\\s\\S]*total = total - 20[\\s\\S]*\\*/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`total` doit être égal à `100`

```swift
tryCatch(total == 100)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
var total = 100
/*
total = total /* base */ - 30
total = total - 20
*/
```
