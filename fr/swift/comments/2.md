---
language: swift
exerciseType: 1
---

# --description--

Un commentaire n'a pas besoin de sa propre ligne : il peut suivre le code sur la même ligne. C'est un **commentaire en fin de ligne**, et c'est un bon endroit pour une courte note à propos de cette instruction précise :
```swift
let retries = 3 // give up after three attempts
```
Tout ce qui va de `//` jusqu'à la fin de la ligne est ignoré, tandis que le code qui le précède s'exécute normalement.

# --instructions--

Ajoute un commentaire en fin de ligne après `let seats = 4` qui explique la valeur, par exemple `// seats per table`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
let seats = 4
```

# --asserts--

La ligne `let seats = 4` doit se terminer par un commentaire `//`

```json
{
  "regex": "let seats = 4[ \\t]*//[ \\t]*\\S",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`seats` doit toujours être égal à `4`

```swift
tryCatch(seats == 4)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let seats = 4 // seats per table
```
