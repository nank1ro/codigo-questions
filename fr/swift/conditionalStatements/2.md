---
language: swift
exerciseType: 2
---

# --description--

Continuons avec l'exemple précédent.
```swift
var niceWeather = true
if niceWeather {
    // play outside
}
```
Nous avons vu que l'instruction `if` exécute le bloc de code uniquement si la condition est `true`.
Une autre chose importante à considérer est représentée par les **crochets ondulés** `{}` qui indiquent un bloc de code.

# --instructions--

Réparer l'instruction `if` en ajoutant les *crochets ondulés* `{}`

# --seed--

```swift
var time = 8
if time < 12 [/]
    print("It's morning")
[/]
```

# --answers--

- if
- ==
- =
- else
- ?
- {
- }

# --solutions--

```swift
var time = 8
if time < 12 {
    print("It's morning")
}
```

# --output--

C'est le matin
