---
language: swift
exerciseType: 2
---

# --description--

Quand tu te fiches de savoir *pourquoi* l'appel a échoué, `try?` est plus court qu'un bloc `do`. Il transforme un appel qui peut lever une erreur en **optionnel** : la valeur en cas de succès, `nil` en cas d'échec. Combiné avec `await`, il s'écrit `try? await` :
```swift
let value = try? await parse("42")  // Optional(42)
let broken = try? await parse("x")  // nil
```
Comme le résultat est optionnel, il s'intègre directement dans un `if let` :
```swift
if let value = try? await parse("x") {
    print(value)
} else {
    print("not a number")
}
```
Utilise `try? await` pour une solution de repli rapide, et `do` / `catch` quand l'erreur elle-même compte.

# --instructions--

Complète le code pour que l'appel qui échoue produise `nil` et que le programme affiche `not a number`

# --seed--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = [/] await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --answers--

- try?
- try
- try!
- await
- throws

# --solutions--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = try? await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --output--

not a number
