---
language: swift
exerciseType: 1
---

# --description--

`async let` est lié à la portée où il est écrit. Pour démarrer un travail simultané et garder une référence dessus, utilise une **`Task`**. La fermeture passée à `Task { }` s'exécute de son côté, et la tâche peut être stockée, transmise ou renvoyée :
```swift
let job = Task {
    return await double(21)
}
```
Le résultat se lit plus tard avec **`.value`**, qui est attendu :
```swift
print(await job.value)
// affiche 42
```
Le type de la référence dit ce qu'elle produit et ce qu'elle peut lever : `Task<Int, Never>` est une tâche qui renvoie un `Int` et ne lève jamais d'erreur. Contrairement à `async let`, une `Task` peut être créée depuis du code ordinaire, non asynchrone.

# --instructions--

Écris une fonction `startDoubling` qui prend un entier sans libellé d'argument et renvoie une `Task<Int, Never>` produisant le double de ce nombre

Exemple d'appel de fonction :
```swift
print(await startDoubling(4).value)
// affiche 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func startDoubling() {

}
```

# --asserts--

La fonction doit créer une `Task`

```json
{
  "regex": "Task\\s*\\{",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`startDoubling(4).value` doit valoir `8`

```swift
tryCatch(await startDoubling(4).value == 8)
```

`startDoubling(0).value` doit valoir `0`

```swift
tryCatch(await startDoubling(0).value == 0)
```

`startDoubling(-6).value` doit valoir `-12`

```swift
tryCatch(await startDoubling(-6).value == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func startDoubling(_ n: Int) -> Task<Int, Never> {
    return Task {
        return n * 2
    }
}
```
