---
language: swift
exerciseType: 1
---

# --description--

`Task.sleep` met en pause la tâche courante pendant un moment sans bloquer quoi que ce soit d'autre. Elle peut être interrompue, c'est donc un appel asynchrone qui peut lever une erreur et il nécessite `try await`. La durée est donnée avec des helpers comme `.seconds`, `.milliseconds` ou `.nanoseconds` :
```swift
func slowGreeting() async throws -> String {
    try await Task.sleep(for: .milliseconds(50))
    return "hello"
}
```
C'est la manière standard de simuler un travail lent dans un exemple, au lieu d'un vrai appel réseau. Note qu'il ne fige pas le programme : pendant qu'une tâche dort, les autres continuent de s'exécuter.

# --instructions--

Écris une fonction `delayed` qui prend un entier sans libellé d'argument, est asynchrone, peut lever une erreur, attend `50` millisecondes avec `Task.sleep`, puis renvoie le nombre multiplié par `3`

Exemple d'appel de fonction :
```swift
print(try await delayed(2))
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func delayed() {

}
```

# --asserts--

La fonction doit être marquée `async throws`

```json
{
  "regex": "func\\s+delayed\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

La fonction doit utiliser `Task.sleep`

```json
{
  "regex": "Task\\.sleep",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`delayed(2)` doit renvoyer `6`

```swift
tryCatch((try? await delayed(2)) == 6)
```

`delayed(0)` doit renvoyer `0`

```swift
tryCatch((try? await delayed(0)) == 0)
```

`delayed(-4)` doit renvoyer `-12`

```swift
tryCatch((try? await delayed(-4)) == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func delayed(_ n: Int) async throws -> Int {
    try await Task.sleep(for: .milliseconds(50))
    return n * 3
}
```
