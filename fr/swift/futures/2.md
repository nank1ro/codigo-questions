---
language: swift
exerciseType: 1
---

# --description--

Une fonction asynchrone reste une fonction ordinaire : elle peut prendre des paramètres et renvoyer une valeur de n'importe quel type. Seules deux choses changent, le mot-clé `async` dans la signature et le `await` à chaque site d'appel :
```swift
func price(of quantity: Int) async -> Double {
    return Double(quantity) * 2.5
}

let total = await price(of: 4)
print(total)
// affiche 10.0
```
La valeur renvoyée est un `Double` normal, pas un emballage : une fois que `await` a terminé, tu travailles avec exactement comme d'habitude.

# --instructions--

Écris une fonction `double` qui prend un entier sans libellé d'argument, est asynchrone et renvoie le double de ce nombre

Exemple d'appel de fonction :
```swift
print(await double(4))
// affiche 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func double() {

}
```

# --asserts--

La fonction doit être marquée `async`

```json
{
  "regex": "func\\s+double\\s*\\([^)]*\\)\\s*async",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`double(4)` doit renvoyer `8`

```swift
tryCatch(await double(4) == 8)
```

`double(0)` doit renvoyer `0`

```swift
tryCatch(await double(0) == 0)
```

`double(-7)` doit renvoyer `-14`

```swift
tryCatch(await double(-7) == -14)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func double(_ n: Int) async -> Int {
    return n * 2
}
```
