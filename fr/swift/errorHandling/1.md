---
language: swift
exerciseType: 2
---

# --description--

En Swift, une erreur est une **valeur**, pas un plantage. N'importe quel type peut jouer ce rôle en se conformant au protocole `Error`, et une énumération est le choix habituel parce que ses cas nomment exactement ce qui peut mal tourner :
```swift
enum LoginError: Error {
    case wrongPassword
}
```
Une fonction qui peut échouer est marquée `throws`, et elle signale l'échec avec `throw` :
```swift
func login(_ password: String) throws {
    if password != "swift" {
        throw LoginError.wrongPassword
    }
    print("welcome")
}
```
Appeler une telle fonction exige `try`, et l'appel doit se trouver dans un bloc `do` suivi d'un bloc `catch` qui indique quoi faire en cas d'échec :
```swift
do {
    try login("hunter2")
} catch {
    print("login failed")
}
// affiche login failed
```
Quand `throw` s'exécute, le reste du bloc `do` est ignoré et `catch` prend le relais. Rien ne plante : le programme continue après le `catch`.

# --instructions--

Complète le code pour que `TempError` soit un type d'erreur, que `check` lève une erreur quand il fait trop froid, et que l'échec soit géré, en affichant `too cold`

# --seed--

```swift
enum TempError: [/] {
    case tooCold
}

func check(_ degrees: Int) [/] {
    if degrees < 10 {
        [/] TempError.tooCold
    }
    print("fine")
}

do {
    [/] check(4)
} [/] {
    print("too cold")
}
```

# --answers--

- Error
- throws
- throw
- try
- catch
- Exception
- finally
- raise

# --solutions--

```swift
enum TempError: Error {
    case tooCold
}

func check(_ degrees: Int) throws {
    if degrees < 10 {
        throw TempError.tooCold
    }
    print("fine")
}

do {
    try check(4)
} catch {
    print("too cold")
}
```

# --output--

too cold
