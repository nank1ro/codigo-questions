---
language: swift
exerciseType: 1
---

# --description--

Un cas d'erreur peut transporter des données. Donne au cas des **valeurs associées** et le `throw` les remplit, pour que le gestionnaire apprenne non seulement *ce* qui a échoué mais aussi *de combien* :
```swift
enum ValidationError: Error {
    case tooShort(minimum: Int)
}

throw ValidationError.tooShort(minimum: 8)
```
Le `catch` correspondant lie ces valeurs avec `let` :
```swift
} catch ValidationError.tooShort(let minimum) {
    print("needs at least \(minimum) characters")
}
```
Le nom après `let` se choisit librement ; c'est une nouvelle constante disponible uniquement dans ce bloc `catch`. C'est ainsi qu'une erreur transporte un message utile sans que tu aies à coller des nombres dans des chaînes à l'endroit où l'échec se produit.

# --instructions--

Donne à `tooShort` une valeur associée `minimum` de type `Int`, et fais que `check` lève `PasswordError.tooShort(minimum: 8)` quand le mot de passe a moins de 8 caractères

Exemple d'appel de fonction :
```swift
do {
    try check("abc")
} catch PasswordError.tooShort(let minimum) {
    print(minimum)
}
// affiche 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum PasswordError: Error {
    case tooShort
}

func check(_ password: String) throws {
    
}
```

# --before-asserts--

```swift
func _minimum(_ password: String) -> Int {
    do {
        try check(password)
        return 0
    } catch PasswordError.tooShort(let minimum) {
        return minimum
    } catch {
        return -1
    }
}
```

# --asserts--

`check("abc")` doit lever `tooShort` transportant `8`

```swift
tryCatch(_minimum("abc") == 8)
```

`check("")` doit lever `tooShort` transportant `8`

```swift
tryCatch(_minimum("") == 8)
```

`check("1234567")` doit lever une erreur car 7 caractères ne suffisent pas

```swift
tryCatch(_minimum("1234567") == 8)
```

`check("12345678")` ne doit pas lever d'erreur

```swift
tryCatch(_minimum("12345678") == 0)
```

`check("longenough")` ne doit pas lever d'erreur

```swift
tryCatch(_minimum("longenough") == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum PasswordError: Error {
    case tooShort(minimum: Int)
}

func check(_ password: String) throws {
    if password.count < 8 {
        throw PasswordError.tooShort(minimum: 8)
    }
}
```
