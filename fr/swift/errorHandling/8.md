---
language: swift
exerciseType: 2
---

# --description--

Parfois tu te fiches de savoir *pourquoi* quelque chose a échoué, seulement que ce soit le cas. `try?` transforme un appel qui peut lever une erreur en **optionnel** : la valeur quand il réussit, `nil` quand il lève une erreur.
```swift
enum ParseError: Error {
    case notANumber
}

func toInt(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ParseError.notANumber
    }
    return value
}

if let number = try? toInt("42") {
    print(number) // 42
}
```
Pas de `do`, pas de `catch` : l'échec est replié dans l'optionnel que tu sais déjà déballer. Le prix à payer, c'est que la valeur de l'erreur est jetée, donc n'utilise `try?` que lorsqu'il n'y a vraiment rien à signaler.

# --instructions--

Complète le code pour que l'appel réussi soit déballé et que l'appel en échec soit reconnu comme `nil`, en affichant `42` puis `not a number`

# --seed--

```swift
enum ParseError: Error {
    case notANumber
}

func toInt(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ParseError.notANumber
    }
    return value
}

let good = [/] toInt("42")
if let number = [/] {
    print(number)
}
if (try? toInt("abc")) == [/] {
    print("not a number")
}
```

# --answers--

- try?
- good
- nil
- try
- bad
- 0

# --solutions--

```swift
enum ParseError: Error {
    case notANumber
}

func toInt(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ParseError.notANumber
    }
    return value
}

let good = try? toInt("42")
if let number = good {
    print(number)
}
if (try? toInt("abc")) == nil {
    print("not a number")
}
```

# --output--

42
not a number
