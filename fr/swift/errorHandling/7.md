---
language: swift
exerciseType: 1
---

# --description--

Un validateur se lit mieux quand les rejets viennent en premier et que le vrai travail reste sans indentation en bas. `guard` est fait pour cela : il énonce la condition qui doit être vraie, et son bloc `else` s'exécute quand elle ne l'est pas.
```swift
func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
Le bloc `else` d'un `guard` doit quitter la portée courante, et `throw` est l'une des façons de le faire, aux côtés de `return`, `break` et `continue`. Plusieurs `guard` empilés en haut d'une fonction se lisent comme la liste des règles que l'entrée doit satisfaire.

# --instructions--

Complète `priceFor` avec un `guard` qui lève `OrderError.notPositive` sauf si `quantity` est supérieur à `0`

Exemple d'appel de fonction :
```swift
do {
    print(try priceFor(4))
} catch {
    print("invalid")
}
// affiche 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    
    return quantity * 3
}
```

# --before-asserts--

```swift
func _price(_ quantity: Int) -> Int {
    do {
        return try priceFor(quantity)
    } catch OrderError.notPositive {
        return -1
    } catch {
        return -2
    }
}
```

# --asserts--

`priceFor(4)` doit renvoyer `12`

```swift
tryCatch(_price(4) == 12)
```

`priceFor(1)` doit renvoyer `3`

```swift
tryCatch(_price(1) == 3)
```

`priceFor(0)` doit lever `notPositive`

```swift
tryCatch(_price(0) == -1)
```

`priceFor(-5)` doit lever `notPositive`

```swift
tryCatch(_price(-5) == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
