---
language: swift
exerciseType: 1
---

# --description--

Quand une fonction lève une erreur, tout ce qui suit le `throw` est ignoré — y compris la ligne qui devait fermer le fichier ou libérer le verrou. `defer` résout cela : il enregistre un bloc maintenant et l'exécute quand la portée courante se termine, quelle que soit la façon dont elle se termine.
```swift
func load() throws {
    print("open")
    defer { print("close") }
    throw FileError.missing
}
```
L'appel affiche `open`, puis `close`, et seulement ensuite l'erreur part vers l'appelant. Si la fonction était revenue normalement, `close` se serait quand même affiché — c'est tout l'intérêt. Place le nettoyage juste à côté de la mise en place et cesse de te demander quelle sortie le code emprunte.

# --instructions--

Ajoute un bloc `defer` dans `run` qui ajoute `"cleanup"` à `log`, pour que cela se produise aussi bien quand `run` revient normalement que quand il lève une erreur

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum TaskError: Error {
    case failed
}

func steps(_ shouldFail: Bool) -> [String] {
    var log: [String] = []
    func run() throws {
        log.append("start")
        
        if shouldFail {
            throw TaskError.failed
        }
        log.append("done")
    }
    do {
        try run()
    } catch {
        log.append("caught")
    }
    return log
}
```

# --asserts--

sur le chemin du succès le journal doit être `start`, `done`, `cleanup`

```swift
let _ok: [String] = steps(false)
tryCatch(_ok == ["start", "done", "cleanup"])
```

sur le chemin de l'échec le journal doit être `start`, `cleanup`, `caught`

```swift
let _bad: [String] = steps(true)
tryCatch(_bad == ["start", "cleanup", "caught"])
```

le nettoyage doit s'exécuter sur les deux chemins

```swift
tryCatch(steps(false).contains("cleanup") && steps(true).contains("cleanup"))
```

le nettoyage doit s'exécuter exactement une fois sur le chemin de l'échec

```swift
let _once: [String] = steps(true)
tryCatch(_once.filter { $0 == "cleanup" }.count == 1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum TaskError: Error {
    case failed
}

func steps(_ shouldFail: Bool) -> [String] {
    var log: [String] = []
    func run() throws {
        log.append("start")
        defer { log.append("cleanup") }
        if shouldFail {
            throw TaskError.failed
        }
        log.append("done")
    }
    do {
        try run()
    } catch {
        log.append("caught")
    }
    return log
}
```
