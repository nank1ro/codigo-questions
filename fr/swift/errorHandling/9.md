---
language: swift
exerciseType: 1
---

# --description--

Comme `try?` produit un optionnel, l'opérateur de coalescence `??` termine le travail en fournissant une valeur de repli :
```swift
let port = (try? readPort(text)) ?? 8080
```
Les parenthèses comptent. Sinon, `try?` tenterait de couvrir toute l'expression, `??` compris, et le compilateur te demande d'être explicite sur l'endroit où l'appel qui peut échouer se termine.

Lis la ligne comme une seule phrase : *utilise le port qu'on a réussi à lire, sinon 8080*. Deux lignes de `do`/`catch` se réduisent à une quand la récupération n'est vraiment qu'une valeur par défaut.

# --instructions--

Complète `portOrDefault` pour qu'il renvoie le port lu depuis `text`, ou `8080` quand `readPort` lève une erreur

Exemple d'appel de fonction :
```swift
print(portOrDefault("abc"))
// affiche 8080
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum ConfigError: Error {
    case notANumber
}

func readPort(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ConfigError.notANumber
    }
    return value
}

func portOrDefault(_ text: String) -> Int {
    
}
```

# --asserts--

`portOrDefault("3000")` doit renvoyer `3000`

```swift
tryCatch(portOrDefault("3000") == 3000)
```

`portOrDefault("80")` doit renvoyer `80`

```swift
tryCatch(portOrDefault("80") == 80)
```

`portOrDefault("abc")` doit revenir à `8080`

```swift
tryCatch(portOrDefault("abc") == 8080)
```

`portOrDefault("")` doit revenir à `8080`

```swift
tryCatch(portOrDefault("") == 8080)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum ConfigError: Error {
    case notANumber
}

func readPort(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ConfigError.notANumber
    }
    return value
}

func portOrDefault(_ text: String) -> Int {
    return (try? readPort(text)) ?? 8080
}
```
