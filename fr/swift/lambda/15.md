---
language: swift
exerciseType: 1
---

# --description--

Par défaut, une fermeture passée à une fonction ne doit être utilisée que pendant l'exécution de cette fonction. Si la fonction stocke la fermeture ou renvoie une autre fermeture qui l'utilise, la fermeture **s'échappe** de la fonction, et son paramètre doit être marqué avec `@escaping` :
```swift
func twice(_ task: @escaping () -> Int) -> () -> Int {
    return { task() * 2 }
}
let answer = twice { 21 }
print(answer()) // 42
```
Sans `@escaping`, le compilateur signale une erreur, car la fermeture renvoyée utiliserait `task` après la fin de `twice`.

# --instructions--

Écris une fonction `compose` qui prend deux fermetures de type `(Int) -> Int` (omets les deux libellés d'argument) et renvoie une fermeture qui applique la première puis la seconde à son argument

Exemple d'appel de fonction :
```swift
let addThenDouble = compose({ $0 + 1 }, { $0 * 2 })
print(addThenDouble(3))
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`compose({ $0 + 1 }, { $0 * 2 })(3)` doit renvoyer `8`

```swift
tryCatch(compose({ $0 + 1 }, { $0 * 2 })(3) == 8)
```

`compose({ $0 * 2 }, { $0 + 1 })(3)` doit renvoyer `7`

```swift
tryCatch(compose({ $0 * 2 }, { $0 + 1 })(3) == 7)
```

`compose({ $0 }, { $0 })(9)` doit renvoyer `9`

```swift
tryCatch(compose({ $0 }, { $0 })(9) == 9)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func compose(_ first: @escaping (Int) -> Int, _ second: @escaping (Int) -> Int) -> (Int) -> Int {
    return { second(first($0)) }
}
```
