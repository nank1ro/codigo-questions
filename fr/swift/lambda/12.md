---
language: swift
exerciseType: 1
---

# --description--

Renvoyer une fermeture est un moyen pratique de construire des fonctions personnalisées. Les paramètres de la fonction externe sont capturés par la fermeture qu'elle renvoie :
```swift
func makeAdder(_ amount: Int) -> (Int) -> Int {
    return { $0 + amount }
}
let addFive = makeAdder(5)
print(addFive(10)) // 15
```
Le type de retour `(Int) -> Int` décrit la fermeture, et la forme abrégée `$0` fait référence à l'argument de cette fermeture, pas à celui de `makeAdder`.

# --instructions--

Écris une fonction `makeMultiplier` qui prend un `Int` appelé `factor` (omets le libellé de l'argument) et renvoie une fermeture de type `(Int) -> Int` qui multiplie son argument par `factor`

Exemple d'appel de fonction :
```swift
let double = makeMultiplier(2)
print(double(10))
// prints 20
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`makeMultiplier(2)(10)` doit renvoyer `20`

```swift
tryCatch(makeMultiplier(2)(10) == 20)
```

`makeMultiplier(3)(4)` doit renvoyer `12`

```swift
tryCatch(makeMultiplier(3)(4) == 12)
```

`makeMultiplier(0)(99)` doit renvoyer `0`

```swift
tryCatch(makeMultiplier(0)(99) == 0)
```

Une fermeture renvoyée par `makeMultiplier(5)` doit continuer à fonctionner d'un appel à l'autre

```swift
let _timesFive = makeMultiplier(5)
tryCatch(_timesFive(1) == 5 && _timesFive(6) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func makeMultiplier(_ factor: Int) -> (Int) -> Int {
    return { $0 * factor }
}
```
