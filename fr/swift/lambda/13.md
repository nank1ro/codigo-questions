---
language: swift
exerciseType: 4
---

# --description--

Une fermeture stockée dans une constante peut être passée partout où un argument de type fermeture est attendu, en utilisant le libellé de l'argument du paramètre :
```swift
let ascending = { (a: Int, b: Int) -> Bool in a < b }
print([3, 1, 2].sorted(by: ascending)) // [1, 2, 3]
```

# --instructions--

Trie les éléments pour stocker dans `byLength` une fermeture qui ordonne deux chaînes de la plus courte à la plus longue, puis affiche `words` triés avec elle.
Déclare `words` avant `byLength`

# --answers--

```swift
let words = ["kiwi", "fig", "banana"]
```

```swift
let byLength = { (a: String, b: String) -> Bool in
```

```swift
    a.count < b.count
```

```swift
}
```

```swift
print(words.sorted(by: byLength))
```

# --solutions--

```swift
let words = ["kiwi", "fig", "banana"]
let byLength = { (a: String, b: String) -> Bool in
    a.count < b.count
}
print(words.sorted(by: byLength))
```

# --output--

["fig", "kiwi", "banana"]
