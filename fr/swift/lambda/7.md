---
language: swift
exerciseType: 1
---

# --description--

`filter` ne garde que les éléments pour lesquels la fermeture renvoie `true`. La fermeture reçoit un élément et doit renvoyer un `Bool` :
```swift
let nums = [5, 12, 8, 20]
let big = nums.filter { $0 > 10 }
print(big) // [12, 20]
```
Les éléments conservent leur ordre d'origine, et le résultat est un nouveau tableau du même type d'élément.

# --instructions--

Écris une fonction `evens` qui prend un tableau de `Int` (omets le libellé de l'argument) et renvoie un nouveau tableau contenant uniquement les nombres pairs, en utilisant `filter`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Tu dois utiliser `filter`

```json
{
  "regex": "\\.filter",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`evens([1, 2, 3, 4, 5, 6])` doit renvoyer `[2, 4, 6]`

```swift
tryCatch(evens([1, 2, 3, 4, 5, 6]) == [2, 4, 6])
```

`evens([7, 9])` doit renvoyer `[]`

```swift
tryCatch(evens([7, 9]) == [])
```

`evens([0, -2, 3])` doit renvoyer `[0, -2]`

```swift
tryCatch(evens([0, -2, 3]) == [0, -2])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func evens(_ nums: [Int]) -> [Int] {
    return nums.filter { $0 % 2 == 0 }
}
```
