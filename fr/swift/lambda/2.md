---
language: swift
exerciseType: 1
---

# --description--

Écrire chaque type à l'intérieur de la fermeture est souvent inutile. Lorsque la constante a un **type de fonction** explicite, Swift déduit les types des paramètres et du retour, donc tu n'as qu'à lister les noms des paramètres avant `in` :
```swift
let triple: (Int) -> Int = { n in
    return n * 3
}
```
Le type `(Int) -> Int` se lit « une fonction qui prend un `Int` et renvoie un `Int` ».
Lorsque le corps est une expression unique, le mot-clé `return` peut aussi être omis, on appelle cela un **retour implicite** :
```swift
let triple: (Int) -> Int = { n in n * 3 }
print(triple(4)) // 12
```

# --instructions--

Stocke dans la constante `square` une fermeture de type `(Int) -> Int` qui renvoie son argument multiplié par lui-même

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Tu dois utiliser une expression de fermeture, pas une `func`

```json
{
  "regex": "func\\s+square",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`square(4)` doit renvoyer `16`

```swift
tryCatch(square(4) == 16)
```

`square(-3)` doit renvoyer `9`

```swift
tryCatch(square(-3) == 9)
```

`square(0)` doit renvoyer `0`

```swift
tryCatch(square(0) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let square: (Int) -> Int = { n in n * n }
```
