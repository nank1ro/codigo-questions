---
language: swift
exerciseType: 1
---

# --description--

Quand une fermeture est le **dernier** argument d'une fonction, tu peux l'écrire après la parenthèse fermante de l'appel. C'est la syntaxe de **fermeture finale** :
```swift
print(apply(5) { $0 + 1 }) // 6
```
Si la fermeture est le seul argument, les parenthèses peuvent être complètement omises :
```swift
func run(_ task: () -> Int) -> Int {
    return task()
}
print(run { 42 }) // 42
```
Les deux formes appellent exactement la même fonction, la syntaxe finale est simplement plus facile à lire quand la fermeture est longue.

# --instructions--

Écris une fonction `applyTwice` qui prend un `Int` et une fermeture de type `(Int) -> Int` (omets les deux libellés d'argument) et renvoie le résultat de l'application de la fermeture au nombre deux fois

Exemple d'appel de fonction :
```swift
print(applyTwice(3) { $0 * 2 })
// prints 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`applyTwice(3) { $0 * 2 }` doit renvoyer `12`

```swift
tryCatch(applyTwice(3) { $0 * 2 } == 12)
```

`applyTwice(1) { $0 + 10 }` doit renvoyer `21`

```swift
tryCatch(applyTwice(1) { $0 + 10 } == 21)
```

`applyTwice(2, { $0 * $0 })` doit renvoyer `16`

```swift
tryCatch(applyTwice(2, { $0 * $0 }) == 16)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func applyTwice(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(operation(n))
}
```
