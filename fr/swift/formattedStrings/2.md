---
language: swift
exerciseType: 1
---

# --description--

Le second outil est `String(format:)`, qui vient du framework **Foundation**, donc le fichier doit commencer par `import Foundation`.

Il prend une **chaîne de format** suivie des valeurs à insérer. À l'intérieur de la chaîne de format, un **spécificateur** commençant par `%` marque où va chaque valeur et comment elle est écrite. Le spécificateur pour un entier est `%d` :
```swift
import Foundation

let count = 7
let text = String(format: "Item %d", count)
print(text) // Item 7
```
`String(format:)` renvoie une `String` normale, donc tu peux l'afficher, la stocker ou la renvoyer depuis une fonction.

# --instructions--

Écris une fonction `label` qui prend un entier sans étiquette d'argument et renvoie le texte `Item ` suivi du nombre, en utilisant `String(format:)` avec `%d`

Exemple d'appel de fonction :
```swift
print(label(7))
// prints Item 7
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func label() {

}
```

# --asserts--

La fonction doit utiliser `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`label(7)` doit renvoyer `"Item 7"`

```swift
tryCatch(label(7) == "Item 7")
```

`label(42)` doit renvoyer `"Item 42"`

```swift
tryCatch(label(42) == "Item 42")
```

`label(0)` doit renvoyer `"Item 0"`

```swift
tryCatch(label(0) == "Item 0")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func label(_ n: Int) -> String {
    return String(format: "Item %d", n)
}
```
