---
language: swift
exerciseType: 1
---

# --description--

Pour insérer une `String` dans une chaîne de format, utilise le spécificateur `%@` :
```swift
let name = "Ada"
let age = 36
print(String(format: "%@ is %d years old", name, age)) // Ada is 36 years old
```
`%@` accepte directement une `String` Swift. N'utilise pas `%s` avec une chaîne Swift : ce spécificateur attend une chaîne C et affiche n'importe quoi ou plante.

# --instructions--

Écris une fonction `greeting` qui prend un nom (`String`) et un âge (`Int`), tous deux sans étiquette d'argument, et renvoie `<name> is <age> years old` en utilisant `String(format:)` avec `%@` et `%d`

Exemple d'appel de fonction :
```swift
print(greeting("Ada", 36))
// prints Ada is 36 years old
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func greeting() {

}
```

# --asserts--

La fonction doit utiliser `String(format:` avec `%@`

```json
{
  "regex": "String\\(format:[^\\n]*%@",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greeting("Ada", 36)` doit renvoyer `"Ada is 36 years old"`

```swift
tryCatch(greeting("Ada", 36) == "Ada is 36 years old")
```

`greeting("Linus", 54)` doit renvoyer `"Linus is 54 years old"`

```swift
tryCatch(greeting("Linus", 54) == "Linus is 54 years old")
```

`greeting("Bo", 7)` doit renvoyer `"Bo is 7 years old"`

```swift
tryCatch(greeting("Bo", 7) == "Bo is 7 years old")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greeting(_ name: String, _ age: Int) -> String {
    return String(format: "%@ is %d years old", name, age)
}
```
