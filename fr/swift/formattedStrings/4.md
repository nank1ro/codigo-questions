---
language: swift
exerciseType: 1
---

# --description--

Un nombre entre `%` et la lettre définit la **largeur minimale** du champ. Si la valeur est plus courte, des espaces sont ajoutés à gauche pour qu'elle soit **alignée à droite** ; si elle est plus longue, rien n'est coupé :
```swift
print(String(format: "%5d|", 42))    //    42|
print(String(format: "%5d|", 12345)) // 12345|
```
La largeur et la précision se combinent : `%8.2f` signifie "au moins 8 caractères de large, avec 2 décimales" :
```swift
print(String(format: "%8.2f|", 3.14159)) //     3.14|
```
Ce sont les largeurs fixes qui alignent les colonnes d'un tableau.

# --instructions--

Écris une fonction `rightAligned` qui prend un entier sans étiquette d'argument et le renvoie sous forme de chaîne alignée à droite dans un champ de largeur `5`, en utilisant `String(format:)`

Exemple d'appel de fonction :
```swift
print(rightAligned(42) + "|")
// prints    42|
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func rightAligned() {

}
```

# --asserts--

La fonction doit utiliser `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`rightAligned(42)` doit renvoyer `"   42"`

```swift
tryCatch(rightAligned(42) == "   42")
```

`rightAligned(7)` doit renvoyer `"    7"`

```swift
tryCatch(rightAligned(7) == "    7")
```

`rightAligned(12345)` doit renvoyer `"12345"`

```swift
tryCatch(rightAligned(12345) == "12345")
```

`rightAligned(123456)` doit renvoyer `"123456"` (rien n'est coupé)

```swift
tryCatch(rightAligned(123456) == "123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func rightAligned(_ n: Int) -> String {
    return String(format: "%5d", n)
}
```
