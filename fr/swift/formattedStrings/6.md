---
language: swift
exerciseType: 1
---

# --description--

Un autre drapeau est `0` : au lieu d'espaces, le champ est rempli de zéros à gauche. C'est ainsi que tu obtiens des nombres comme `007` ou `00042` :
```swift
print(String(format: "%05d", 42))  // 00042
print(String(format: "%03d", 7))   // 007
print(String(format: "%03d", 1234)) // 1234
```
Comme avec les espaces, une valeur plus longue que la largeur n'est jamais coupée.

# --instructions--

Écris une fonction `ticket` qui prend un entier sans étiquette d'argument et renvoie `T-` suivi du nombre complété avec des zéros jusqu'à la largeur `5`, en utilisant `String(format:)`

Exemple d'appel de fonction :
```swift
print(ticket(42))
// prints T-00042
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func ticket() {

}
```

# --asserts--

La fonction doit utiliser `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ticket(42)` doit renvoyer `"T-00042"`

```swift
tryCatch(ticket(42) == "T-00042")
```

`ticket(7)` doit renvoyer `"T-00007"`

```swift
tryCatch(ticket(7) == "T-00007")
```

`ticket(12345)` doit renvoyer `"T-12345"`

```swift
tryCatch(ticket(12345) == "T-12345")
```

`ticket(123456)` doit renvoyer `"T-123456"`

```swift
tryCatch(ticket(123456) == "T-123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func ticket(_ n: Int) -> String {
    return String(format: "T-%05d", n)
}
```
