---
language: swift
exerciseType: 2
---

# --description--

Nous pouvons également utiliser la propriété `isEmpty` que nous avons utilisée avec les tableaux pour déterminer si un dictionnaire est vide

# --instructions--

Vérifiez si le dictionnaire `user` est vide

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if ([/][/][/]) {
    print("user dictionary is empty")
} else {
    print("user dictionary is NOT empty")
}
```

# --answers--

- user
- .
- isEmpty
- isEmpty()
- is_empty

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if (user.isEmpty) {
    print("user dictionary is empty")
} else {
    print("user dictionary is NOT empty")
}
```

# --output--

user dictionary is NOT empty
