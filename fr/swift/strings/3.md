---
language: swift
exerciseType: 2
---

# --description--

Deux chaînes peuvent être jointes avec l'opérateur `+`, qui produit une nouvelle chaîne :
```swift
let full = "Hello" + " " + "world" // Hello world
```
Pour ajouter du texte à la fin d'une variable de chaîne existante, utilise `+=`. La variable doit être déclarée avec `var`, car sa valeur change :
```swift
var log = "Start"
log += "..."
print(log) // Start...
```

# --instructions--

Déclare une variable `message` contenant `Hello`, ajoute-lui `, world` avec l'opérateur d'assignation composé, puis affiche-la

# --seed--

```swift
[/] message = "Hello"
message [/] ", world"
print(message)
```

# --answers--

- var
- let
- +=
- +
- =
- -=

# --solutions--

```swift
var message = "Hello"
message += ", world"
print(message)
```

# --output--

Hello, world
