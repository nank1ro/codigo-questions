---
language: swift
exerciseType: 1
---

# --description--

L'**interpolation de chaîne** insère la valeur d'une expression à l'intérieur d'un littéral de chaîne. Enveloppe l'expression dans `\()` :
```swift
let name = "Ada"
let age = 36
print("\(name) is \(age) years old") // Ada is 36 years old
```
N'importe quel type peut être interpolé : les nombres, les booléens et les autres chaînes sont tous automatiquement convertis en texte.

# --instructions--

Écris une fonction `greet` qui prend un `String` (omets l'étiquette d'argument) et renvoie le texte `Hello, <name>!`, où `<name>` est l'argument.

Exemple d'appel de fonction :
```swift
print(greet("Ada"))
// prints Hello, Ada!
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`greet("Ada")` doit retourner `"Hello, Ada!"`

```swift
tryCatch(greet("Ada") == "Hello, Ada!")
```

`greet("Swift")` doit retourner `"Hello, Swift!"`

```swift
tryCatch(greet("Swift") == "Hello, Swift!")
```

`greet("")` doit retourner `"Hello, !"`

```swift
tryCatch(greet("") == "Hello, !")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greet(_ name: String) -> String {
    return "Hello, \(name)!"
}
```
