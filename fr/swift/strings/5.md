---
language: swift
exerciseType: 1
---

# --description--

Une `String` est une collection de valeurs `Character`. Un `Character` est une seule lettre, un chiffre, un symbole ou un espace, et il s'écrit avec les mêmes guillemets doubles qu'une chaîne, donc tu as besoin d'une annotation de type pour en obtenir un :
```swift
let letter: Character = "a"
let text = "abc"
print(text.count) // 3
```
Vérifier `isEmpty` est préférable à comparer `count` à `0` : c'est plus lisible et cela n'a pas besoin de compter chaque caractère.

# --instructions--

Écris une fonction `length` qui prend un `String` (omets l'étiquette d'argument) et renvoie son nombre de caractères, et une fonction `hasText` qui prend un `String` (omets l'étiquette d'argument) et renvoie `true` seulement quand la chaîne n'est pas vide

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`length("Swift")` doit retourner `5`

```swift
tryCatch(length("Swift") == 5)
```

`length("a b c")` doit retourner `5`

```swift
tryCatch(length("a b c") == 5)
```

`length("")` doit retourner `0`

```swift
tryCatch(length("") == 0)
```

`hasText("x")` doit retourner `true`

```swift
tryCatch(hasText("x") == true)
```

`hasText("")` doit retourner `false`

```swift
tryCatch(hasText("") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func length(_ text: String) -> Int {
    return text.count
}

func hasText(_ text: String) -> Bool {
    return !text.isEmpty
}
```
