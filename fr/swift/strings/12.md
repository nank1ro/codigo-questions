---
language: swift
exerciseType: 2
---

# --description--

`uppercased()` et `lowercased()` renvoient une **nouvelle** chaîne avec chaque lettre convertie en majuscule ou en minuscule. La chaîne d'origine n'est pas modifiée :
```swift
let name = "Swift"
print(name.uppercased()) // SWIFT
print(name.lowercased()) // swift
print(name)              // Swift
```
Ce sont toutes les deux des méthodes, donc n'oublie pas les parenthèses.

# --instructions--

Affiche `quiet please` en majuscules, puis affiche `QUIET` en minuscules

# --seed--

```swift
let shout = "quiet please".[/]()
print(shout)
print("QUIET".[/]())
```

# --answers--

- uppercased
- lowercased
- uppercase
- lowercase
- capitalized

# --solutions--

```swift
let shout = "quiet please".uppercased()
print(shout)
print("QUIET".lowercased())
```

# --output--

QUIET PLEASE
quiet
