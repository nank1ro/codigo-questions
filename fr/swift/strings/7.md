---
language: swift
exerciseType: 1
---

# --description--

Puisqu'une chaîne est une collection de caractères, tu peux itérer dessus avec une boucle `for`-`in`. Chaque itération te donne un `Character` :
```swift
for letter in "hey" {
    print(letter)
}
// h
// e
// y
```
Un `Character` peut être comparé avec `==` à un littéral de caractère, donc compter combien de fois un caractère apparaît n'est qu'une boucle avec un compteur.

# --instructions--

Écris une fonction `occurrences` qui prend un `Character` avec l'étiquette d'argument `of` et un `String` avec l'étiquette d'argument `in`, et renvoie combien de fois le caractère apparaît dans la chaîne.

Exemple d'appel de fonction :
```swift
print(occurrences(of: "l", in: "hello"))
// prints 2
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`occurrences(of: "l", in: "hello")` doit retourner `2`

```swift
tryCatch(occurrences(of: "l", in: "hello") == 2)
```

`occurrences(of: " ", in: "a b c")` doit retourner `2`

```swift
tryCatch(occurrences(of: " ", in: "a b c") == 2)
```

`occurrences(of: "z", in: "hello")` doit retourner `0`

```swift
tryCatch(occurrences(of: "z", in: "hello") == 0)
```

`occurrences(of: "a", in: "")` doit retourner `0`

```swift
tryCatch(occurrences(of: "a", in: "") == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func occurrences(of target: Character, in text: String) -> Int {
    var total = 0
    for letter in text {
        if letter == target {
            total += 1
        }
    }
    return total
}
```
