---
language: swift
exerciseType: 4
---

# --description--

Un `Character` n'est pas une `String`, donc il ne peut pas être joint directement à une chaîne avec `+`. Convertis-le d'abord avec `String(...)` :
```swift
let letter: Character = "a"
let text = String(letter) + "bc" // abc
```
Combiner cela avec une boucle `for`-`in` te permet de reconstruire une chaîne caractère par caractère, par exemple en plaçant chaque nouveau caractère devant ceux déjà rassemblés.

# --instructions--

Trie les éléments pour afficher `swift` inversé, en construisant le résultat caractère par caractère.
Déclare `word` avant `reversed`

# --answers--

```swift
let word = "swift"
```

```swift
var reversed = ""
```

```swift
for letter in word {
```

```swift
    reversed = String(letter) + reversed
```

```swift
}
```

```swift
print(reversed)
```

# --solutions--

```swift
let word = "swift"
var reversed = ""
for letter in word {
    reversed = String(letter) + reversed
}
print(reversed)
```

# --output--

tfiws
