---
language: swift
exerciseType: 1
---

# --description--

Travailler avec des index est verbeux, donc Swift propose des raccourcis pour les cas les plus courants :
- `first` et `last` renvoient le premier et le dernier caractère sous forme de `Character?` optionnel (`nil` pour une chaîne vide)
- `prefix(n)` renvoie les `n` premiers caractères et `suffix(n)` les `n` derniers
```swift
let word = "Swift"
print(word.first!)     // S
print(word.prefix(2))  // Sw
print(word.suffix(3))  // ift
```
`prefix` et `suffix` renvoient une `Substring`, une vue sur le texte d'origine. Pour la stocker comme un vrai `String`, enveloppe-la dans `String(...)`. Si `n` est plus grand que `count`, tu obtiens simplement la chaîne entière.

# --instructions--

Écris une fonction `firstTwo` qui prend un `String` (omets l'étiquette d'argument) et renvoie ses deux premiers caractères sous forme de `String`, et une fonction `lastTwo` qui renvoie ses deux derniers caractères sous forme de `String`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`firstTwo("Swift")` doit retourner `"Sw"`

```swift
tryCatch(firstTwo("Swift") == "Sw")
```

`firstTwo("a")` doit retourner `"a"`

```swift
tryCatch(firstTwo("a") == "a")
```

`lastTwo("Swift")` doit retourner `"ft"`

```swift
tryCatch(lastTwo("Swift") == "ft")
```

`lastTwo("")` doit retourner `""`

```swift
tryCatch(lastTwo("") == "")
```

`firstTwo` doit retourner un `String`

```swift
tryCatch(type(of: firstTwo("Swift")) == String.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstTwo(_ text: String) -> String {
    return String(text.prefix(2))
}

func lastTwo(_ text: String) -> String {
    return String(text.suffix(2))
}
```
