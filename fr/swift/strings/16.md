---
language: swift
exerciseType: 1
---

# --description--

Le framework Foundation ajoute de nombreuses méthodes de chaîne supplémentaires. L'une des plus utiles est `replacingOccurrences(of:with:)`, qui renvoie une nouvelle chaîne où chaque occurrence du premier texte est remplacée par le second :
```swift
import Foundation

let path = "a/b/c"
print(path.replacingOccurrences(of: "/", with: "-")) // a-b-c
```
N'oublie pas de faire `import Foundation` en haut du fichier, sinon la méthode n'est pas disponible. Les appels de méthode peuvent être chaînés, donc `text.lowercased().replacingOccurrences(of: " ", with: "_")` est valide.

# --instructions--

Écris une fonction `snakeCase` qui prend un `String` (omets l'étiquette d'argument) et le renvoie en minuscules avec chaque espace remplacé par un underscore `_`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`snakeCase("Hello World")` doit retourner `"hello_world"`

```swift
tryCatch(snakeCase("Hello World") == "hello_world")
```

`snakeCase("My First Post")` doit retourner `"my_first_post"`

```swift
tryCatch(snakeCase("My First Post") == "my_first_post")
```

`snakeCase("swift")` doit retourner `"swift"`

```swift
tryCatch(snakeCase("swift") == "swift")
```

`snakeCase("")` doit retourner `""`

```swift
tryCatch(snakeCase("") == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func snakeCase(_ text: String) -> String {
    return text.lowercased().replacingOccurrences(of: " ", with: "_")
}
```
