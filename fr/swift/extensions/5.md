---
language: swift
exerciseType: 1
---

# --description--

Les propriétés calculées dans les extensions conviennent aussi tout naturellement à `String`. La méthode `reversed()` renvoie les caractères dans l'ordre inverse, et `String(...)` les retransforme en chaîne :
```swift
extension String {
    var backwards: String {
        return String(reversed())
    }
}
print("swift".backwards) // tfiws
```
Comme pour les méthodes, `reversed()` à l'intérieur de l'extension signifie `self.reversed()`.

# --instructions--

Écris une extension de `String` avec une propriété calculée `isPalindrome` de type `Bool`, qui vaut `true` lorsque la chaîne se lit de la même façon dans les deux sens. La comparaison est sensible à la casse

Exemple d'utilisation :
```swift
print("level".isPalindrome)
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Tu dois utiliser une extension de `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`"level".isPalindrome` doit être `true`

```swift
tryCatch("level".isPalindrome == true)
```

`"swift".isPalindrome` doit être `false`

```swift
tryCatch("swift".isPalindrome == false)
```

`"Anna".isPalindrome` doit être `false` (la comparaison est sensible à la casse)

```swift
tryCatch("Anna".isPalindrome == false)
```

`"".isPalindrome` doit être `true`

```swift
tryCatch("".isPalindrome == true)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    var isPalindrome: Bool {
        return self == String(reversed())
    }
}
```
