---
language: swift
exerciseType: 1
---

# --description--

Certaines valeurs appartiennent au type lui-même plutôt qu'à une seule instance : un code de devise, une valeur par défaut partagée, une fabrique qui construit un cas courant. Marque-les `static` et lis-les à travers le nom du type :
```swift
struct Money {
    static let currency = "EUR"
    var amount: Int

    static func zero() -> Money {
        return Money(amount: 0)
    }
}

print(Money.currency)     // EUR
print(Money.zero().amount) // 0
```
Ici `currency` est déclarée avec `let` parce qu'elle ne change jamais, donc c'est une constante partagée par tout le programme. `Money.currency` fonctionne sans créer un seul `Money`, tandis que `amount` a besoin d'une instance.

# --instructions--

Ajoute à `Money` une constante statique `currency` égale à `"EUR"` et une méthode statique `zero` qui ne prend aucun paramètre et renvoie un `Money` avec `amount` égal à `0`

Exemple d'utilisation :
```swift
print(Money.currency)
// prints EUR
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Money {
    var amount: Int

}
```

# --asserts--

`currency` doit être une constante statique

```json
{
  "regex": "static\\s+let\\s+currency",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Money.currency` doit être `"EUR"`

```swift
tryCatch(Money.currency == "EUR")
```

`Money.zero()` doit avoir un `amount` de `0`

```swift
tryCatch(Money.zero().amount == 0)
```

L'initialiseur membre à membre doit toujours fonctionner

```swift
tryCatch(Money(amount: 7).amount == 7)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int

    static func zero() -> Money {
        return Money(amount: 0)
    }
}
```
