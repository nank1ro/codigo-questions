---
language: swift
exerciseType: 1
---

# --description--

Une struct est un type comme les autres, donc elle peut être stockée dans un tableau, un dictionnaire ou un set, et chaque outil que tu connais déjà continue de fonctionner sur elle :
```swift
struct Item {
    var name: String
    var price: Int
}

let items = [Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]

for item in items {
    print(item.name)
}

let names = items.map { $0.name }
let total = items.reduce(0) { $0 + $1.price }
print(total) // 10
```
Rappelle-toi que le tableau contient des *copies* : lire `items[0]` dans une variable et la modifier ne touche pas le tableau.

# --instructions--

Écris une fonction `totalPrice` qui prend un tableau d'`Item` sans libellé d'argument et renvoie la somme des `price` de chaque article, ou `0` quand le tableau est vide

Exemple d'appel de fonction :
```swift
print(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]))
// prints 10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice() {

}
```

# --asserts--

Deux articles à `3` et `7` doivent totaliser `10`

```swift
tryCatch(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]) == 10)
```

Un seul article doit totaliser son propre prix

```swift
tryCatch(totalPrice([Item(name: "Pen", price: 2)]) == 2)
```

Un tableau vide doit totaliser `0`

```swift
tryCatch(totalPrice([]) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice(_ items: [Item]) -> Int {
    return items.reduce(0) { $0 + $1.price }
}
```
