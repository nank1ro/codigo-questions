---
language: swift
exerciseType: 1
---

# --description--

Une struct peut aussi contenir des **méthodes** : des fonctions écrites entre les accolades qui travaillent sur l'instance sur laquelle elles sont appelées. Dans une méthode, tu utilises les noms des propriétés directement, sans aucun préfixe :
```swift
struct Rectangle {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}

let r = Rectangle(width: 3, height: 4)
print(r.area()) // 12
```
Si un paramètre de la méthode masque un nom de propriété, écris `self.width` pour désigner la propriété de l'instance.

# --instructions--

Ajoute une méthode `area` à `Rectangle` qui ne prend aucun paramètre et renvoie la largeur multipliée par la hauteur

Exemple d'appel de fonction :
```swift
print(Rectangle(width: 3, height: 4).area())
// prints 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Rectangle {
    var width: Int
    var height: Int

}
```

# --asserts--

Un rectangle de `3` par `4` doit avoir une aire de `12`

```swift
tryCatch(Rectangle(width: 3, height: 4).area() == 12)
```

Un rectangle de `1` par `1` doit avoir une aire de `1`

```swift
tryCatch(Rectangle(width: 1, height: 1).area() == 1)
```

Un rectangle de `0` par `7` doit avoir une aire de `0`

```swift
tryCatch(Rectangle(width: 0, height: 7).area() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Rectangle {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}
```
