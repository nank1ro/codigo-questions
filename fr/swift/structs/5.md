---
language: swift
exerciseType: 1
---

# --description--

Une **propriété calculée** ressemble à une propriété mais se comporte comme une méthode : elle ne stocke rien, elle calcule sa valeur chaque fois que tu la lis. Tu écris le type, puis un bloc de code qui renvoie la valeur :
```swift
struct Rectangle {
    var width: Int
    var height: Int

    var area: Int {
        return width * height
    }
}

let r = Rectangle(width: 3, height: 4)
print(r.area) // 12, no parentheses
```
Les propriétés calculées ne font pas partie de l'initialiseur membre à membre, puisqu'il n'y a rien à stocker. Utilise-en une quand la valeur est dérivée des autres, et une méthode quand le travail a besoin de paramètres.

# --instructions--

Ajoute une propriété calculée `fahrenheit` de type `Double` à `Temperature`, qui renvoie la valeur celsius multipliée par `9`, divisée par `5`, plus `32`

Exemple d'utilisation :
```swift
print(Temperature(celsius: 100).fahrenheit)
// prints 212.0
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Temperature {
    var celsius: Double

}
```

# --asserts--

`100` degrés celsius doivent valoir `212` fahrenheit

```swift
tryCatch(Temperature(celsius: 100).fahrenheit == 212)
```

`0` degré celsius doit valoir `32` fahrenheit

```swift
tryCatch(Temperature(celsius: 0).fahrenheit == 32)
```

`-40` degrés celsius doivent valoir `-40` fahrenheit

```swift
tryCatch(Temperature(celsius: -40).fahrenheit == -40)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Temperature {
    var celsius: Double

    var fahrenheit: Double {
        return celsius * 9 / 5 + 32
    }
}
```
