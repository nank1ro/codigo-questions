---
language: swift
exerciseType: 2
---

# --description--

Une propriété stockée peut recevoir une **valeur par défaut** dès l'endroit où elle est déclarée. Swift déduit son type de cette valeur, donc tu peux omettre l'annotation de type :
```swift
struct Counter {
    var label: String
    var value = 0
}
```
L'initialiseur membre à membre transforme chaque propriété avec valeur par défaut en argument optionnel : passe-le pour remplacer la valeur par défaut, omets-le pour la conserver.
```swift
let a = Counter(label: "clicks")
print(a.value) // 0

let b = Counter(label: "clicks", value: 7)
print(b.value) // 7
```

# --instructions--

Complète le code pour que `value` commence à `0` et que `c` soit créé sans passer `value`

# --seed--

```swift
struct Counter {
    var label: String
    var value = [/]
}

let c = Counter([/]: "clicks")
print(c.label)
print(c.value)
```

# --answers--

- 0
- label
- 1
- value
- Counter
- name

# --solutions--

```swift
struct Counter {
    var label: String
    var value = 0
}

let c = Counter(label: "clicks")
print(c.label)
print(c.value)
```

# --output--

clicks
0
