---
language: swift
exerciseType: 2
---

# --description--

Quand une classe a ses propres fonctions, ces fonctions s'appellent des **méthodes**.

# --instructions--

Ajoutez une méthode, `description`, à votre classe `Animal`.
En utilisant deux instructions `print` séparées, elle doit afficher le `gender` et les `legs` de l'animal sur lequel elle est appelée.
Ensuite, appelez la méthode `description`.

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4

    [/][/]() {
        if let gender = self.gender { print(gender) }
        print([/].legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.[/]
```

# --answers--

-  description
- func
- description()
- self
- description[]
- function
- method

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4

    func description() {
        if let gender = self.gender { print(gender) }
        print(self.legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.description()
```

# --output--

female
4
