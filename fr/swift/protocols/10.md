---
language: swift
exerciseType: 4
---

# --description--

Une extension de protocole peut aussi ajouter des membres que le protocole n'a jamais listés comme exigences. Ce sont des facilités supplémentaires, disponibles sur tout type conforme :
```swift
protocol Sized {
    var count: Int { get }
}

extension Sized {
    var isEmpty: Bool {
        return count == 0
    }
}
```
`isEmpty` n'est pas une exigence, donc un type conforme n'a pas à la fournir — il l'obtient simplement.

# --instructions--

Trie les éléments pour afficher `HEY`, en déclarant d'abord le protocole, puis son extension, puis la struct, puis le `print`

# --answers--

```swift
protocol Loud {
    var text: String { get }
}
```

```swift
extension Loud {
    func shout() -> String { return text.uppercased() }
}
```

```swift
struct Message: Loud {
    var text: String
}
```

```swift
print(Message(text: "hey").shout())
```

# --solutions--

```swift
protocol Loud {
    var text: String { get }
}
extension Loud {
    func shout() -> String { return text.uppercased() }
}
struct Message: Loud {
    var text: String
}
print(Message(text: "hey").shout())
```

# --output--

HEY
