---
language: swift
exerciseType: 4
---

# --instructions--

Trie les éléments pour afficher `[G]`, en déclarant d'abord le protocole, puis son extension, puis l'énumération, puis le `print`

# --answers--

```swift
protocol Badge {
    var symbol: String { get }
}
```

```swift
extension Badge {
    func banner() -> String { return "[\(symbol)]" }
}
```

```swift
enum Rank: Badge {
    case gold

    var symbol: String { return "G" }
}
```

```swift
print(Rank.gold.banner())
```

# --solutions--

```swift
protocol Badge {
    var symbol: String { get }
}
extension Badge {
    func banner() -> String { return "[\(symbol)]" }
}
enum Rank: Badge {
    case gold

    var symbol: String { return "G" }
}
print(Rank.gold.banner())
```

# --output--

[G]
