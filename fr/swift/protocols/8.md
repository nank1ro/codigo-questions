---
language: swift
exerciseType: 2
---

# --description--

La conformité n'a pas à être déclarée à côté du type. Une **extension** peut l'ajouter plus tard, ce qui garde la déclaration du type elle-même concentrée sur ses données :
```swift
protocol Resettable {
    mutating func reset()
}

struct Timer {
    var seconds = 0
}

extension Timer: Resettable {
    mutating func reset() {
        seconds = 0
    }
}
```
Cela fonctionne aussi pour les types que tu n'as pas écrits : tu peux faire conformer un type de la bibliothèque standard à l'un de tes protocoles sans toucher à son code source.

# --instructions--

Complète le code pour que `increment()` soit une exigence mutante et que `Clicks` adopte `Counter` dans une extension

# --seed--

```swift
protocol Counter {
    [/] func increment()
}

struct Clicks {
    var count = 0
}

[/] Clicks: Counter {
    mutating func increment() {
        count [/] 1
    }
}

var c = Clicks()
c.increment()
c.increment()
print(c.count)
```

# --answers--

- mutating
- extension
- +=
- static
- struct
- -=

# --solutions--

```swift
protocol Counter {
    mutating func increment()
}

struct Clicks {
    var count = 0
}

extension Clicks: Counter {
    mutating func increment() {
        count += 1
    }
}

var c = Clicks()
c.increment()
c.increment()
print(c.count)
```

# --output--

2
