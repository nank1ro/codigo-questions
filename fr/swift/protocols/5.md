---
language: swift
exerciseType: 2
---

# --description--

Une **énumération** peut aussi se conformer. Elle n'a pas de propriétés stockées, donc une exigence de propriété est généralement satisfaite avec une propriété calculée qui fait un `switch` sur les cas :
```swift
protocol Priced {
    var price: Int { get }
}

enum Ticket: Priced {
    case child, adult

    var price: Int {
        switch self {
        case .child: return 5
        case .adult: return 12
        }
    }
}

print(Ticket.adult.price) // 12
```
Structs, classes et énumérations : les trois adoptent les protocoles de la même manière, et le code écrit contre le protocole fonctionne avec chacune d'elles.

# --instructions--

Complète le code pour que `Priced` exige un `price` lisible et que `Drink` s'y conforme

# --seed--

```swift
protocol Priced {
    var price: Int { [/] }
}

enum Drink: [/] {
    case tea, coffee

    var price: Int {
        [/] self {
        case .tea: return 3
        case .coffee: return 5
        }
    }
}

print(Drink.coffee.price)
```

# --answers--

- get
- Priced
- switch
- set
- if
- return

# --solutions--

```swift
protocol Priced {
    var price: Int { get }
}

enum Drink: Priced {
    case tea, coffee

    var price: Int {
        switch self {
        case .tea: return 3
        case .coffee: return 5
        }
    }
}

print(Drink.coffee.price)
```

# --output--

5
