---
language: swift
exerciseType: 1
---

# --description--

Tu peux comparer un optionnel à `nil` en utilisant `==` et `!=`, et tu peux aussi le comparer directement à une valeur simple du type enveloppé :
```swift
var score: Int? = 10
print(score == nil) // false
print(score == 10)  // true
```
Souviens-toi que `Int?` et `Int` sont deux types différents : un `Int?` peut être vide, un `Int` ne l'est jamais.

# --instructions--

Déclare une variable optionnelle `String` `nickname` sans valeur, et une variable optionnelle `Int` `score` contenant `10`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`nickname` doit être `nil`

```swift
tryCatch(nickname == nil)
```

`nickname` doit être un `String` optionnel

```swift
tryCatch(type(of: nickname) == String?.self)
```

`score` doit être égal à `10`

```swift
tryCatch(score == 10)
```

`score` doit être un `Int` optionnel

```swift
tryCatch(type(of: score) == Int?.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
var nickname: String?
var score: Int? = 10
```
