---
language: swift
exerciseType: 4
---

# --description--

L'extraction forcée n'est sûre que lorsque tu as déjà vérifié que l'optionnel n'est pas `nil` :
```swift
if score != nil {
    print(score! * 2)
}
```

# --instructions--

Trie les éléments pour que `price` soit doublé et affiché seulement après avoir vérifié qu'il n'est pas `nil`

# --answers--

```swift
let price: Double? = 9.5
```

```swift
if price != nil {
```

```swift
    print(price! * 2)
```

```swift
}
```

# --solutions--

```swift
let price: Double? = 9.5
if price != nil {
    print(price! * 2)
}
```

# --output--

19.0
