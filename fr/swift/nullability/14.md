---
language: swift
exerciseType: 4
---

# --description--

Lier plusieurs optionnels dans un seul `if let` garde le code plat : une seule branche `else` couvre chaque valeur absente.

# --instructions--

Trie les éléments pour afficher `Area: 20` lorsque `width` et `height` ont tous les deux une valeur, et `Missing size` sinon.
Déclare `width` avant `height`

# --answers--

```swift
let width: Int? = 4
```

```swift
let height: Int? = 5
```

```swift
if let w = width, let h = height {
```

```swift
    print("Area: \(w * h)")
```

```swift
} else {
    print("Missing size")
}
```

# --solutions--

```swift
let width: Int? = 4
let height: Int? = 5
if let w = width, let h = height {
    print("Area: \(w * h)")
} else {
    print("Missing size")
}
```

# --output--

Area: 20
