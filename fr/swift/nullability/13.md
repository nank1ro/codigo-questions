---
language: swift
exerciseType: 1
---

# --description--

Un seul `if let` ou `guard let` peut extraire plusieurs optionnels à la fois : sépare les liaisons par des virgules.
Le corps ne s'exécute que si chaque optionnel a une valeur :
```swift
let first: String? = "Ada"
let last: String? = "Lovelace"
if let first = first, let last = last {
    print("\(first) \(last)")
}
```
Tu peux aussi ajouter une condition booléenne après les liaisons, comme `if let n = number, n > 0`.

# --instructions--

Écris une fonction `add` qui prend deux valeurs `Int?` (omets les libellés des arguments) et renvoie leur somme sous forme de `Int?`, ou `nil` si l'une d'elles est absente

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`add(2, 3)` doit renvoyer `5`

```swift
tryCatch(add(2, 3) == 5)
```

`add(nil, 3)` doit renvoyer `nil`

```swift
tryCatch(add(nil, 3) == nil)
```

`add(2, nil)` doit renvoyer `nil`

```swift
tryCatch(add(2, nil) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func add(_ a: Int?, _ b: Int?) -> Int? {
    guard let a = a, let b = b else { return nil }
    return a + b
}
```
