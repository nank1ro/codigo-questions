---
language: swift
exerciseType: 1
---

# --description--

De nombreuses opérations peuvent échouer, et Swift signale l'échec en renvoyant un optionnel.
Convertir du texte en nombre en est l'exemple classique : `Int("42")` renvoie un `Int?` contenant `42`, tandis que `Int("abc")` renvoie `nil`.
`Int("3.5")` est aussi `nil`, car le texte n'est pas un nombre entier ; utilise `Double("3.5")` pour les décimaux.
```swift
let typed = "42"
if let number = Int(typed) {
    print(number + 1) // 43
}
```
D'autres exemples sont `array.first` (`nil` pour un tableau vide) et `dictionary[key]` (`nil` quand la clé est absente).

# --instructions--

Écris une fonction `parseAge` qui prend un `String` (omets le libellé de l'argument) et le renvoie converti en `Int`, ou `-1` lorsque le texte n'est pas un nombre entier valide

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`parseAge("42")` doit renvoyer `42`

```swift
tryCatch(parseAge("42") == 42)
```

`parseAge("abc")` doit renvoyer `-1`

```swift
tryCatch(parseAge("abc") == -1)
```

`parseAge("3.5")` doit renvoyer `-1`

```swift
tryCatch(parseAge("3.5") == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func parseAge(_ text: String) -> Int {
    return Int(text) ?? -1
}
```
