---
language: swift
exerciseType: 1
---

# --description--

Quand une fonction doit renvoyer un résultat décimal calculé à partir de nombres entiers, convertis les opérandes en `Double` avant de diviser et déclare le type de retour comme `Double` :
```swift
func ratio(_ part: Int, _ total: Int) -> Double {
    return Double(part) / Double(total)
}
print(ratio(1, 4)) // 0.25
```
Souviens-toi que `count` d'un tableau est aussi un `Int`, donc il a besoin de la même conversion.

# --instructions--

Écris une fonction `average` qui prend un tableau de `Int` (omets le label de l'argument) et renvoie la moyenne de ses éléments en tant que `Double`. Le tableau n'est jamais vide

Exemple d'appel de fonction :
```swift
print(average([1, 2]))
// prints 1.5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`average([1, 2])` doit retourner `1.5`

```swift
tryCatch(average([1, 2]) == 1.5)
```

`average([3, 4, 5, 6])` doit retourner `4.5`

```swift
tryCatch(average([3, 4, 5, 6]) == 4.5)
```

`average([10])` doit retourner `10.0`

```swift
tryCatch(average([10]) == 10.0)
```

`average([1, 1, 2])` doit retourner un `Double`

```swift
tryCatch(type(of: average([1, 1, 2])) == Double.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func average(_ numbers: [Int]) -> Double {
    var sum = 0
    for number in numbers {
        sum += number
    }
    return Double(sum) / Double(numbers.count)
}
```
