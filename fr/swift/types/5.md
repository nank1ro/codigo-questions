---
language: swift
exerciseType: 1
---

# --description--

`Int(x)` n'arrondit pas : il **tronque**, en supprimant simplement la partie décimale, donc `Int(3.99)` vaut `3` et `Int(-3.99)` vaut `-3`.
Pour arrondir au nombre entier le plus proche, appelle d'abord `rounded()` sur le `Double` puis convertis :
```swift
let x = 3.99
print(Int(x))            // 3
print(Int(x.rounded()))  // 4
```
Les valeurs à mi-chemin comme `2.5` sont arrondies en s'éloignant de zéro : `2.5` devient `3.0` et `-2.5` devient `-3.0`.

# --instructions--

Écris une fonction `roundedAverage` qui prend deux `Int` (omets les labels des arguments) et renvoie leur moyenne arrondie au nombre entier le plus proche, en tant qu'`Int`

Exemple d'appel de fonction :
```swift
print(roundedAverage(3, 4))
// prints 4
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`roundedAverage(3, 4)` doit retourner `4` (la moyenne est `3.5`)

```swift
tryCatch(roundedAverage(3, 4) == 4)
```

`roundedAverage(2, 4)` doit retourner `3`

```swift
tryCatch(roundedAverage(2, 4) == 3)
```

`roundedAverage(10, 15)` doit retourner `13` (la moyenne est `12.5`)

```swift
tryCatch(roundedAverage(10, 15) == 13)
```

`roundedAverage(-3, -4)` doit retourner `-4`

```swift
tryCatch(roundedAverage(-3, -4) == -4)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func roundedAverage(_ a: Int, _ b: Int) -> Int {
    let average = Double(a + b) / 2
    return Int(average.rounded())
}
```
