---
language: swift
exerciseType: 1
---

# --description--

La fonction `type(of:)` retourne le type d'une valeur, ce qui est pratique pour vérifier ce que Swift a déduit :
```swift
print(type(of: 42))    // Int
print(type(of: 2.5))   // Double
print(type(of: "hi"))  // String
```
Quand tu veux un type différent de celui déduit, ajoute une annotation. Un littéral entier peut être stocké dans une constante `Double` ou `Float`, et un littéral d'un seul caractère dans une constante `Character` :
```swift
let ratio: Double = 3       // 3.0, not an Int
let half: Float = 0.5
let initial: Character = "S"
print(type(of: ratio))      // Double
```

# --instructions--

Déclare trois constantes :
- `ratio`, un `Double` contenant le littéral entier `3`
- `half`, un `Float` contenant `0.5`
- `initial`, un `Character` contenant `"S"`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`ratio` doit être un `Double` égal à `3.0`

```swift
tryCatch(type(of: ratio) == Double.self && ratio == 3.0)
```

`half` doit être un `Float` égal à `0.5`

```swift
tryCatch(type(of: half) == Float.self && half == 0.5)
```

`initial` doit être un `Character` égal à `"S"`

```swift
tryCatch(type(of: initial) == Character.self && initial == "S")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let ratio: Double = 3
let half: Float = 0.5
let initial: Character = "S"
```
