---
language: swift
exerciseType: 1
---

# --description--

Comme `Int(text)` et `Double(text)` retournent `nil` en cas d'échec, comparer le résultat avec `nil` te dit si un texte est un nombre de ce genre :
```swift
print(Int("42") != nil)     // true
print(Double("4.2") != nil) // true
print(Double("42") != nil)  // true, a whole number is also a valid Double
```
Note la dernière ligne : tout texte accepté par `Int` est aussi accepté par `Double`, donc vérifie d'abord `Int` quand tu veux les distinguer.

# --instructions--

Écris une fonction `classify` qui prend une `String` (omets le label de l'argument) et renvoie `"Int"` si le texte est un nombre entier valide, `"Double"` si c'est un nombre décimal valide, et `"String"` sinon

Exemple d'appel de fonction :
```swift
print(classify("3.5"))
// prints Double
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`classify("42")` doit retourner `"Int"`

```swift
tryCatch(classify("42") == "Int")
```

`classify("-7")` doit retourner `"Int"`

```swift
tryCatch(classify("-7") == "Int")
```

`classify("3.5")` doit retourner `"Double"`

```swift
tryCatch(classify("3.5") == "Double")
```

`classify("hello")` doit retourner `"String"`

```swift
tryCatch(classify("hello") == "String")
```

`classify("")` doit retourner `"String"`

```swift
tryCatch(classify("") == "String")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func classify(_ text: String) -> String {
    if Int(text) != nil {
        return "Int"
    }
    if Double(text) != nil {
        return "Double"
    }
    return "String"
}
```
