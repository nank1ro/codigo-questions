---
language: swift
exerciseType: 2
---

# --description--

Swift n'effectue jamais de conversion entre types numériques de lui-même : additionner un `Int` à un `Double` est une erreur de compilation, même si les deux sont des nombres.
```swift
let apples = 3
let price = 1.5
let total = apples * price // error: Int and Double can't be mixed
```
Pour les combiner, tu crées une nouvelle valeur du type dont tu as besoin, en passant la valeur à l'initialiseur du type :
```swift
let total = Double(apples) * price // 4.5
```
Cela fonctionne aussi dans l'autre sens : `Int(4.5)` produit un `Int`, en gardant seulement la partie entière du nombre.

# --instructions--

Complète les conversions afin que `total` soit le coût des pommes en tant que `Double` et que `whole` en soit la partie entière en tant qu'`Int`, de sorte que le programme affiche `4.5` et `4`

# --seed--

```swift
let apples = 3
let price = 1.5
let total = [/](apples) * price
let whole = [/](total)
print(total)
print(whole)
```

# --answers--

- Double
- Int
- Float
- String
- Character

# --solutions--

```swift
let apples = 3
let price = 1.5
let total = Double(apples) * price
let whole = Int(total)
print(total)
print(whole)
```

# --output--

4.5
4
