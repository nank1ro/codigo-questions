---
language: swift
exerciseType: 1
---

# --description--

`Int.max` et `Int.min` sont utiles comme valeurs de départ quand tu cherches un extremum : tout nombre réel est plus petit que `Int.max`, donc c'est une valeur initiale sûre pour « le plus petit vu jusqu'ici » :
```swift
var smallest = Int.max
for number in [8, 3, 5] {
    if number < smallest {
        smallest = number
    }
}
print(smallest) // 3
```

# --instructions--

Écris une fonction `smallest` qui prend un tableau de `Int` (omets le label de l'argument) et renvoie son plus petit élément, ou `Int.max` quand le tableau est vide. N'utilise pas la méthode `min()`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Tu ne dois pas utiliser `min()`

```json
{
  "regex": "\\.min\\(",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`smallest([8, 3, 5])` doit retourner `3`

```swift
tryCatch(smallest([8, 3, 5]) == 3)
```

`smallest([-5, 5])` doit retourner `-5`

```swift
tryCatch(smallest([-5, 5]) == -5)
```

`smallest([Int.min, 0])` doit retourner `Int.min`

```swift
tryCatch(smallest([Int.min, 0]) == Int.min)
```

`smallest([])` doit retourner `Int.max`

```swift
tryCatch(smallest([]) == Int.max)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func smallest(_ numbers: [Int]) -> Int {
    var result = Int.max
    for number in numbers {
        if number < result {
            result = number
        }
    }
    return result
}
```
