---
language: swift
exerciseType: 4
---

# --description--

Les sets prennent en charge les opérations classiques de la théorie des ensembles. Chacune renvoie un **nouveau** set et laisse les originaux inchangés :
- `a.union(b)` contient chaque élément présent dans `a`, dans `b`, ou dans les deux
- `a.intersection(b)` ne contient que les éléments présents à la fois dans `a` et dans `b`
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.union(b).sorted())        // [1, 2, 3, 4]
print(a.intersection(b).sorted()) // [3]
```

# --instructions--

Trie les éléments pour afficher chaque nombre présent dans `odds` ou dans `primes`, dans l'ordre croissant.
Déclare `odds` avant `primes`

# --answers--

```swift
let odds: Set<Int> = [1, 3, 5]
```

```swift
let primes: Set<Int> = [2, 3, 5]
```

```swift
let all = odds.union(primes)
```

```swift
print(all.sorted())
```

# --solutions--

```swift
let odds: Set<Int> = [1, 3, 5]
let primes: Set<Int> = [2, 3, 5]
let all = odds.union(primes)
print(all.sorted())
```

# --output--

[1, 2, 3, 5]
