---
language: swift
exerciseType: 1
---

# --description--

Comme `Int(_:)` accepte une `Substring`, transformer du texte trouvé en nombres se fait en une étape. `compactMap` est pratique ici : elle écarte les valeurs qui reviennent `nil` :
```swift
let text = "a1 b22"
let numbers = text.matches(of: #/\d+/#).compactMap { Int($0.0) }
print(numbers) // [1, 22]
```
Utilise `map` quand chaque élément se convertit, `compactMap` quand certains peuvent échouer.

# --instructions--

Écris une fonction `numbers` qui prend un texte (`String`) sans étiquette d'argument et renvoie chaque suite de chiffres qu'il contient sous forme d'un tableau de `Int`, dans l'ordre où elles apparaissent

Exemple d'appel de fonction :
```swift
print(numbers("a1 b22 c333"))
// affiche [1, 22, 333]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func numbers() {

}
```

# --asserts--

La fonction doit utiliser `matches(of:`

```json
{
  "regex": "matches\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`numbers("a1 b22 c333")` doit renvoyer `[1, 22, 333]`

```swift
tryCatch(numbers("a1 b22 c333") == [1, 22, 333])
```

`numbers("7")` doit renvoyer `[7]`

```swift
tryCatch(numbers("7") == [7])
```

`numbers("abc")` doit renvoyer `[]`

```swift
tryCatch(numbers("abc") == [])
```

`numbers("10-20")` doit renvoyer `[10, 20]`

```swift
tryCatch(numbers("10-20") == [10, 20])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func numbers(_ text: String) -> [Int] {
    return text.matches(of: #/\d+/#).compactMap { Int($0.0) }
}
```
