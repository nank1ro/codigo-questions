---
language: swift
exerciseType: 1
---

# --description--

Quand aucun des raccourcis ne convient, liste les caractères que tu acceptes entre crochets. `[abc]` correspond à un `a`, un `b` ou un `c`, et un tiret écrit un intervalle :
```swift
print("f".contains(#/[a-f]/#))  // true
print("Z".contains(#/[A-Z]/#))  // true
print("5".contains(#/[0-9a-f]/#)) // true
```
Un nombre entre accolades dit exactement combien de fois le motif précédent se répète : `{3}` signifie trois fois, `{2,4}` signifie entre deux et quatre fois :
```swift
print("aaa".contains(#/^a{3}$/#))  // true
print("aa".contains(#/^a{3}$/#))   // false
```
Envelopper un motif dans `^` et `$` avec un compteur est la façon habituelle de vérifier qu'un texte entier a une forme donnée.

# --instructions--

Écris une fonction `isCode` qui prend un texte (`String`) sans étiquette d'argument et renvoie `true` seulement quand le texte entier est exactement trois lettres majuscules, en utilisant une expression régulière

Exemple d'appel de fonction :
```swift
print(isCode("ABC"))
// affiche true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func isCode() {

}
```

# --asserts--

La fonction doit utiliser un littéral d'expression régulière écrit avec `#/`

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`isCode("ABC")` doit renvoyer `true`

```swift
tryCatch(isCode("ABC") == true)
```

`isCode("XYZ")` doit renvoyer `true`

```swift
tryCatch(isCode("XYZ") == true)
```

`isCode("AB")` doit renvoyer `false`

```swift
tryCatch(isCode("AB") == false)
```

`isCode("ABCD")` doit renvoyer `false`

```swift
tryCatch(isCode("ABCD") == false)
```

`isCode("abc")` doit renvoyer `false`

```swift
tryCatch(isCode("abc") == false)
```

`isCode("A1C")` doit renvoyer `false`

```swift
tryCatch(isCode("A1C") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func isCode(_ text: String) -> Bool {
    return text.contains(#/^[A-Z]{3}$/#)
}
```
