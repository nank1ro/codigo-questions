---
language: swift
exerciseType: 4
---

# --description--

`matches(of:)` renvoie **toutes** les correspondances au lieu de la première, sous forme de tableau. Le tableau n'est jamais `nil` : quand rien ne correspond il est simplement vide, donc tu peux le parcourir ou le transformer directement :
```swift
let text = "a1 b22"
print(text.matches(of: #/\d+/#).count) // 2
```
Chaque élément est une correspondance, donc `$0.0` dans un `map` est le texte correspondant :
```swift
let found = text.matches(of: #/\d+/#).map { String($0.0) }
print(found) // ["1", "22"]
```

# --instructions--

Ordonne les éléments pour construire un programme qui rassemble chaque suite de chiffres de `text` dans `found` et l'affiche

# --answers--

```swift
let text = "a1 b22 c333"
```

```swift
let pattern = #/\d+/#
```

```swift
let found = text.matches(of: pattern).map { String($0.0) }
```

```swift
print(found)
```

# --solutions--

```swift
let text = "a1 b22 c333"
let pattern = #/\d+/#
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

```swift
let pattern = #/\d+/#
let text = "a1 b22 c333"
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

# --output--

["1", "22", "333"]
