---
language: swift
exerciseType: 1
---

# --description--

Un littéral `#/ ... /#` est figé à la compilation. Quand le motif ne devient connu qu'à l'exécution, par exemple parce qu'un utilisateur l'a tapé, construis-le avec `Regex(_:)` :
```swift
let regex = try Regex("[0-9]+")
print("abc123".contains(regex)) // true
```
Cet initialiseur **lève** une erreur : un motif invalide comme `"["` n'est découvert que pendant l'exécution du programme, donc l'appel a besoin de `try`, et l'erreur doit soit être gérée avec `do`/`catch` (ou `try?`), soit être propagée en marquant la fonction englobante `throws`, comme le fait cet exercice. Une expression régulière construite de cette façon n'a pas de propriétés numérotées connues à la compilation, mais `contains`, `matches(of:)` et `replacing` fonctionnent exactement comme avant.

# --instructions--

Écris une fonction `countMatches` qui prend un texte (`String`) et un motif (`String`), tous deux sans étiquette d'argument, est marquée `throws`, et renvoie combien de fois le motif est trouvé dans le texte. Un motif invalide doit laisser sortir l'erreur

Exemple d'appel de fonction :
```swift
print(try countMatches("a1 b22", "[0-9]+"))
// prints 2
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func countMatches() {

}
```

# --asserts--

La fonction doit construire l'expression régulière avec `try Regex(`

```json
{
  "regex": "try\\s+Regex\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`countMatches("a1 b22", "[0-9]+")` doit renvoyer `2`

```swift
tryCatch((try? countMatches("a1 b22", "[0-9]+")) == 2)
```

`countMatches("abc", "[0-9]+")` doit renvoyer `0`

```swift
tryCatch((try? countMatches("abc", "[0-9]+")) == 0)
```

`countMatches("hello world", "o")` doit renvoyer `2`

```swift
tryCatch((try? countMatches("hello world", "o")) == 2)
```

`countMatches("abc", "[")` doit lever une erreur

```swift
tryCatch((try? countMatches("abc", "[")) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func countMatches(_ text: String, _ pattern: String) throws -> Int {
    let regex = try Regex(pattern)
    return text.matches(of: regex).count
}
```
