---
language: swift
exerciseType: 4
---

# --description--

Valider un texte entier avec `^` et `$` fonctionne, mais `wholeMatch(of:)` le dit directement : elle renvoie une correspondance seulement quand le motif couvre le texte du premier au dernier caractère, et `nil` sinon :
```swift
print("1a2b".wholeMatch(of: #/[0-9a-f]+/#) != nil) // true
print("1z".wholeMatch(of: #/[0-9a-f]+/#) != nil)   // false
```
Utilise `firstMatch(of:)` pour trouver quelque chose dans un texte, et `wholeMatch(of:)` pour vérifier qu'un texte a une forme exacte.

# --instructions--

Ordonne les éléments pour construire un programme qui définit `isHex` avant la boucle et affiche le résultat pour chaque valeur de `values`

# --answers--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
```

```swift
let values = ["1a2b", "1z", "ff"]
```

```swift
for value in values {
    print(isHex(value))
}
```

# --solutions--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
let values = ["1a2b", "1z", "ff"]
for value in values {
    print(isHex(value))
}
```

```swift
let values = ["1a2b", "1z", "ff"]
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
for value in values {
    print(isHex(value))
}
```

# --output--

true
false
true
