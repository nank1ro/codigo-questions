---
language: swift
exerciseType: 2
---

# --description--

Compter les parenthèses devient fragile dès qu'un motif grandit. Donne plutôt un **nom** à un groupe, en écrivant `?<nom>` juste après sa parenthèse ouvrante, et lis-le comme une propriété de la correspondance :
```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>\d+)/#) {
    print(m.key)   // size
    print(m.value) // 10
}
```
Les groupes nommés restent numérotés, donc `m.1` continue de fonctionner, mais `m.key` dit ce qu'il contient et survit à un changement dans le motif.

# --instructions--

Complète le code pour que le groupe value corresponde à des chiffres et que la clé soit affichée avant lui, produisant la sortie `size 10`

# --seed--

```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>[/]+)/#) {
    print(m.[/], m.value)
}
```

# --answers--

- \d
- \s
- [A-Z]
- key
- name
- 0

# --solutions--

```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>\d+)/#) {
    print(m.key, m.value)
}
```

# --output--

size 10
