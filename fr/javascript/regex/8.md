---
language: javascript
exerciseType: 1
---

# --description--

Ne capture que ce dont tu as besoin. Un groupe n'est pas seulement un moyen de relire un morceau ; il indique aussi au lecteur quelle partie du motif compte. Dans un motif d'heure où tu ne veux que les minutes, groupe les minutes seules et laisse le reste sans groupe :
```javascript
const minutes = "at 14:35:02".match(/\d{2}:(\d{2}):\d{2}/)?.[1];
console.log(minutes);
// prints 35
```
Tout le motif doit quand même correspondre, donc les heures et les secondes restent obligatoires ; elles ne sont simplement pas capturées. Moins de groupes signifient moins de numéros à suivre quand tu lis `match[1]`, `match[2]` et ainsi de suite.

# --instructions--

Écris une fonction `monthOf` qui prend une chaîne `date` qui peut contenir une date écrite sous la forme `YYYY-MM-DD`, éventuellement entourée d'autre texte, et renvoie son mois sous forme de chaîne, ou une chaîne vide quand `date` ne contient pas une telle date. Par exemple `monthOf("2026-09-12")` renvoie `09`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function monthOf(date) {
  
}
```

# --asserts--

`monthOf("2026-09-12")` doit renvoyer `09`.

```javascript
tryCatch(monthOf("2026-09-12") === "09");
```

`monthOf("born on 1999-12-31")` doit renvoyer `12`.

```javascript
tryCatch(monthOf("born on 1999-12-31") === "12");
```

`monthOf("no date")` doit renvoyer une chaîne vide.

```javascript
tryCatch(monthOf("no date") === "");
```

`monthOf("12-31-1999")` doit renvoyer une chaîne vide.

```javascript
tryCatch(monthOf("12-31-1999") === "");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function monthOf(date) {
  return date.match(/\d{4}-(\d{2})-\d{2}/)?.[1] ?? "";
}
```
