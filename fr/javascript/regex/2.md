---
language: javascript
exerciseType: 1
---

# --description--

Les motifs deviennent utiles quand ils décrivent un *type* de caractère au lieu d'un caractère exact. Quelques **séquences d'échappement** couvrent la plupart des besoins :
- `\d` n'importe quel chiffre, de `0` à `9`
- `\w` n'importe quel caractère de mot : une lettre, un chiffre ou `_`
- `\s` n'importe quel espace : une espace, une tabulation, un retour à la ligne

```javascript
console.log(/\d/.test("room 12"));
// prints true
console.log(/\d/.test("lobby"));
// prints false
```
Un **quantificateur** indique combien de fois le morceau précédent peut se répéter. Le plus courant est `+`, qui signifie « un ou plus » :
```javascript
console.log(/\d+/.test("42"));
// prints true
```
Ainsi `/\d/` correspond à un seul chiffre et `/\d+/` correspond à une suite de chiffres. Pour un simple `test` les deux se comportent pareil, car il suffit à chacun qu'un chiffre soit présent.

# --instructions--

Écris une fonction `hasDigit` qui prend une chaîne `text` et renvoie `true` quand `text` contient au moins un chiffre, en utilisant `test`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function hasDigit(text) {
  
}
```

# --asserts--

La méthode `test` doit être utilisée.

```json
{
  "regex": "\\.test\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`hasDigit("room 12")` doit renvoyer `true`.

```javascript
tryCatch(hasDigit("room 12") === true);
```

`hasDigit("lobby")` doit renvoyer `false`.

```javascript
tryCatch(hasDigit("lobby") === false);
```

`hasDigit("7")` doit renvoyer `true`.

```javascript
tryCatch(hasDigit("7") === true);
```

`hasDigit("")` doit renvoyer `false`.

```javascript
tryCatch(hasDigit("") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function hasDigit(text) {
  return /\d/.test(text);
}
```
