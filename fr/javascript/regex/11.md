---
language: javascript
exerciseType: 1
---

# --description--

Le flag `g` te donne chaque chaîne correspondante, mais il jette les groupes. Quand tu as besoin des groupes de *chaque* correspondance, utilise **`matchAll`**. Il renvoie un itérateur d'objets de correspondance complets, chacun exactement comme le résultat d'un `match` simple :
```javascript
const text = "a=1;b=2";
for (const match of text.matchAll(/(?<key>\w+)=(?<value>\w+)/g)) {
  console.log(`${match.groups.key} -> ${match.groups.value}`);
}
// affiche a -> 1
// affiche b -> 2
```
`matchAll` exige le flag `g` ; sans lui, il lève une `TypeError`. Comme il renvoie un itérateur, étale-le avec `[...text.matchAll(pattern)]` quand tu veux un vrai tableau, et note qu'il ne produit rien du tout quand le motif ne correspond jamais.

# --instructions--

Écris une fonction `parsePairs` qui prend une chaîne `text`, y trouve chaque paire `key=value`, où les clés et les valeurs sont des suites de caractères de mot, et renvoie un objet associant chaque clé à sa valeur. Par exemple `parsePairs("host=local;port=80")` renvoie `{ host: "local", port: "80" }`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function parsePairs(text) {
  
}
```

# --asserts--

`parsePairs("host=local;port=80")` doit renvoyer un objet avec `host` et `port`.

```javascript
tryCatch(JSON.stringify(parsePairs("host=local;port=80")) === '{"host":"local","port":"80"}');
```

`parsePairs("a=1")` doit renvoyer un objet avec seulement `a`.

```javascript
tryCatch(JSON.stringify(parsePairs("a=1")) === '{"a":"1"}');
```

`parsePairs("nothing here")` doit renvoyer un objet vide.

```javascript
tryCatch(JSON.stringify(parsePairs("nothing here")) === '{}');
```

`parsePairs("x=1 y=2 z=3")` doit renvoyer les trois paires.

```javascript
tryCatch(JSON.stringify(parsePairs("x=1 y=2 z=3")) === '{"x":"1","y":"2","z":"3"}');
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function parsePairs(text) {
  const pairs = {};
  for (const match of text.matchAll(/(?<key>\w+)=(?<value>\w+)/g)) {
    pairs[match.groups.key] = match.groups.value;
  }
  return pairs;
}
```
