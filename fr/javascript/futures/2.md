---
language: javascript
exerciseType: 1
---

# --description--

Ajouter `async` devant une fonction change ce qu'elle renvoie : le corps calcule toujours une valeur ordinaire, mais l'appelant reçoit une promesse enveloppant cette valeur.
```javascript
function shout(text) {
  return text.toUpperCase();
}
async function shoutLater(text) {
  return text.toUpperCase();
}

console.log(shout("hi"));
// prints HI
console.log(shoutLater("hi"));
// prints Promise { 'HI' }
```
Les deux fonctions contiennent le même code ; seule la façon de lire le résultat diffère. `shoutLater("hi")` doit être attendue avec `await` à l'intérieur d'une autre fonction `async` pour redonner `"HI"`.

Marquer une fonction `async` ne coûte rien quand il n'y a rien à attendre, et c'est ce qui permet d'utiliser `await` à l'intérieur plus tard.

# --instructions--

Écrivez une fonction `async` nommée `greet` qui prend une string `name` et renvoie `Hello, ` suivi du nom et d'un point d'exclamation, par exemple `Hello, Ada!`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
async function greet(name) {
  
}
```

# --asserts--

`greet` doit être déclarée avec le mot-clé `async`.

```json
{
  "regex": "async\\s+function\\s+greet",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`await greet("Ada")` doit être `"Hello, Ada!"`.

```javascript
(async () => {
  tryCatch((await greet("Ada")) === "Hello, Ada!");
})().catch(() => tryCatch(false));
```

`await greet("Bob")` doit être `"Hello, Bob!"`.

```javascript
(async () => {
  tryCatch((await greet("Bob")) === "Hello, Bob!");
})().catch(() => tryCatch(false));
```

`await greet("")` doit être `"Hello, !"`.

```javascript
(async () => {
  tryCatch((await greet("")) === "Hello, !");
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
async function greet(name) {
  return `Hello, ${name}!`;
}
```
