---
language: javascript
exerciseType: 4
---

# --description--

Une fonction `async` se lit de haut en bas comme n'importe quelle autre fonction : `await` la met simplement en pause jusqu'à ce que la promesse attendue soit tenue, puis l'exécution continue à la ligne suivante.
```javascript
async function main() {
  console.log("start");
  const value = await Promise.resolve("data");
  console.log(value);
  console.log("done");
}

main();
// prints start, then data, then done
```
Remarquez la dernière ligne : une fonction `async` doit quand même être **appelée**. Écrire `main` sans les parenthèses définit le travail mais ne le démarre jamais, et rien n'est affiché.

# --instructions--

Ordonnez les éléments pour obtenir un programme qui définit une fonction `async` `load` renvoyant `data`, puis une fonction `async` `main` qui affiche `start`, attend `load()` dans `value`, affiche `value` et affiche enfin `done`. Définissez d'abord `load`, puis `main`, et appelez `main()` sur la dernière ligne

# --answers--

```javascript
async function load() {
  return "data";
}
```

```javascript
async function main() {
```

```javascript
  console.log("start");
```

```javascript
  const value = await load();
```

```javascript
  console.log(value);
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
async function load() {
  return "data";
}
async function main() {
  console.log("start");
  const value = await load();
  console.log(value);
  console.log("done");
}
main();
```

# --output--

start
data
done
