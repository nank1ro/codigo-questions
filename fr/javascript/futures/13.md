---
language: javascript
exerciseType: 4
---

# --description--

La différence entre l'attente **séquentielle** et **parallèle** est décidée par l'*endroit* où vous placez `await` :
```javascript
// sequential: about 300 + 300 = 600 ms
const a = await load("a");
const b = await load("b");

// parallel: about 300 ms
const [a, b] = await Promise.all([load("a"), load("b")]);
```
Dans la première version, le second téléchargement ne démarre qu'une fois le premier terminé, car `await` met la fonction en pause sur cette ligne. Dans la seconde, les deux appels sont faits avant d'attendre quoi que ce soit, donc les deux téléchargements sont déjà en cours pendant que `Promise.all` attend.

Utilisez des `await` séquentiels seulement quand la seconde tâche a vraiment besoin du résultat de la première. Sinon, démarrez tout d'abord et attendez ensemble.

# --instructions--

Ordonnez les éléments pour obtenir un programme qui définit `later`, puis une fonction `async` `main` qui affiche `loading`, démarre les deux appels de `later` ensemble avec `Promise.all`, affiche les deux résultats joints par une espace et affiche enfin `done`. Définissez d'abord `later`, puis `main`, et appelez `main()` sur la dernière ligne

# --answers--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
```

```javascript
async function main() {
```

```javascript
  console.log("loading");
```

```javascript
  const results = await Promise.all([later("one", 30), later("two", 10)]);
```

```javascript
  console.log(results.join(" "));
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
async function main() {
  console.log("loading");
  const results = await Promise.all([later("one", 30), later("two", 10)]);
  console.log(results.join(" "));
  console.log("done");
}
main();
```

# --output--

loading
ready two
ready one
one two
done
