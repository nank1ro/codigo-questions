---
language: javascript
exerciseType: 2
---

# --description--

Les membres peuvent aussi contenir des **nombres**. Les valeurs numériques sont pratiques quand les membres ont un ordre naturel, car vous pouvez les comparer avec les opérateurs habituels :
```javascript
const Priority = Object.freeze({
  LOW: 1,
  MEDIUM: 2,
  HIGH: 3,
});
console.log(Priority.HIGH > Priority.LOW);
// prints true
```
Le compromis est la lisibilité : afficher `Priority.HIGH` montre `3`, ce qui vous en dit beaucoup moins que la chaîne `"high"`.

# --instructions--

Déclarez l'énumération numérique gelée `Level` avec `EASY` égal à `1`, `NORMAL` égal à `2` et `HARD` égal à `3`, puis affichez si `HARD` est supérieur à `EASY` et la différence entre `HARD` et `EASY`

# --seed--

```javascript
const Level = Object.freeze({
  EASY: [/],
  NORMAL: 2,
  HARD: [/],
});
console.log(Level.HARD [/] Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --answers--

- 1
- 3
- >
- 0
- <
- ===

# --solutions--

```javascript
const Level = Object.freeze({
  EASY: 1,
  NORMAL: 2,
  HARD: 3,
});
console.log(Level.HARD > Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --output--

true
2
