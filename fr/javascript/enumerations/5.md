---
language: javascript
exerciseType: 1
---

# --description--

Comme une énumération est simplement un objet, les outils habituels des objets vous permettent de l'inspecter :
- `Object.keys(Enum)` renvoie un tableau avec les **noms** des membres
- `Object.values(Enum)` renvoie un tableau avec les **valeurs** des membres
- `Object.entries(Enum)` renvoie un tableau de paires `[name, value]`

```javascript
const Color = Object.freeze({ RED: "red", BLUE: "blue" });
console.log(Object.keys(Color));
// prints [ 'RED', 'BLUE' ]
console.log(Object.values(Color));
// prints [ 'red', 'blue' ]
```
Combiner `Object.values()` avec la méthode de tableau `includes()` est le moyen standard de vérifier si une valeur arbitraire, par exemple une valeur lue depuis une saisie utilisateur, est un membre valide :
```javascript
console.log(Object.values(Color).includes("red"));
// prints true
console.log(Object.values(Color).includes("pink"));
// prints false
```

# --instructions--

Écrivez une fonction `isColor(value)` qui renvoie `true` si `value` est l'une des valeurs de l'énumération `Color`, et `false` sinon

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Color = Object.freeze({
  RED: "red",
  GREEN: "green",
  BLUE: "blue",
});

function isColor(value) {

}
```

# --asserts--

`isColor("red")` doit renvoyer `true`.

```javascript
tryCatch(isColor("red") === true);
```

`isColor("blue")` doit renvoyer `true`.

```javascript
tryCatch(isColor("blue") === true);
```

`isColor("pink")` doit renvoyer `false`.

```javascript
tryCatch(isColor("pink") === false);
```

`isColor("RED")` doit renvoyer `false`, car `"RED"` est un nom de membre, pas une valeur.

```javascript
tryCatch(isColor("RED") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Color = Object.freeze({
  RED: "red",
  GREEN: "green",
  BLUE: "blue",
});

function isColor(value) {
  return Object.values(Color).includes(value);
}
```
