---
language: javascript
exerciseType: 1
---

# --description--

Une classe enfant n'a pas besoin de redéfinir ce que le parent fournit déjà. Les méthodes sont héritées elles aussi, si bien qu'une instance de l'enfant peut les appeler comme si elles étaient les siennes :
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
    speak() {
        return `${this.name} makes a sound`;
    }
}
class Dog extends Animal {}
console.log(new Dog("Max").speak());
// prints Max makes a sound
```
Quand un enfant déclare son propre constructeur, y appeler `super(...)` est **obligatoire** : sans cela, l'objet n'est jamais initialisé et JavaScript lève une `ReferenceError`. Un enfant sans constructeur du tout, en revanche, ne pose aucun problème, car JavaScript en écrit un qui transmet chaque argument au parent.

# --instructions--

Écrivez une classe `Cat` qui étend `Animal`, prend `name` et `lives`, transmet `name` au constructeur parent et stocke `lives` sur l'instance

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Cat {
  
}
```

# --asserts--

`new Cat("Kitty", 9).name` doit être `"Kitty"`.

```javascript
tryCatch(new Cat("Kitty", 9).name === "Kitty");
```

`new Cat("Kitty", 9).lives` doit être `9`.

```javascript
tryCatch(new Cat("Kitty", 9).lives === 9);
```

`Cat` doit étendre `Animal`.

```javascript
tryCatch(new Cat("Kitty", 9) instanceof Animal);
```

`Cat` doit hériter de la méthode `speak` d'`Animal`.

```javascript
tryCatch(new Cat("Kitty", 9).speak() === "Kitty makes a sound");
```

Un deuxième chat doit conserver ses propres valeurs.

```javascript
tryCatch(new Cat("Tom", 7).lives === 7);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Cat extends Animal {
  constructor(name, lives) {
    super(name);
    this.lives = lives;
  }
}
```
