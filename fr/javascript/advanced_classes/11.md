---
language: javascript
exerciseType: 1
---

# --description--

La vraie valeur d'un setter, c'est qu'il peut refuser. Entre l'assignation et la valeur stockée, vous avez une occasion de vérifier, de borner ou de rejeter ce qui arrive :
```javascript
class Volume {
    constructor(level) {
        this._level = level;
    }
    get level() {
        return this._level;
    }
    set level(value) {
        if (value <= 10) {
            this._level = value;
        }
    }
}
const v = new Volume(3);
v.level = 50;
console.log(v.level);
// affiche 3, le setter a rejeté 50
```
Un getter et un setter de même nom forment une seule propriété, ils ne peuvent donc pas être en même temps un champ normal : la valeur stockée vit sous un autre nom, par convention le même nom précédé d'un underscore.

# --instructions--

Écrivez une classe `Thermostat` dont le constructeur prend une température et la stocke dans `_temperature`, avec un getter `temperature` qui la renvoie et un setter `temperature` qui ignore toute valeur inférieure à `0`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Thermostat {
  
}
```

# --asserts--

Le getter doit renvoyer la valeur donnée au constructeur.

```javascript
tryCatch(new Thermostat(20).temperature === 20);
```

Le setter doit accepter une valeur valide.

```javascript
const t1 = new Thermostat(20);
t1.temperature = 25;
tryCatch(t1.temperature === 25);
```

Le setter doit ignorer une valeur négative.

```javascript
const t2 = new Thermostat(20);
t2.temperature = -5;
tryCatch(t2.temperature === 20);
```

Le setter doit accepter `0`.

```javascript
const t3 = new Thermostat(20);
t3.temperature = 0;
tryCatch(t3.temperature === 0);
```

La valeur doit être stockée dans `_temperature`.

```javascript
tryCatch(new Thermostat(20)._temperature === 20);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Thermostat {
  constructor(temperature) {
    this._temperature = temperature;
  }
  get temperature() {
    return this._temperature;
  }
  set temperature(value) {
    if (value >= 0) {
      this._temperature = value;
    }
  }
}
```
