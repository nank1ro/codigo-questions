---
language: javascript
exerciseType: 4
---

# --description--

Un usage très courant d'une méthode statique est la **factory** : une méthode qui construit une instance à partir d'une autre forme de données et la renvoie. Elle garde `new` à un seul endroit et donne à la construction un nom qui dit ce qu'elle fait :
```javascript
class Duration {
    constructor(seconds) {
        this.seconds = seconds;
    }
    static fromMinutes(minutes) {
        return new Duration(minutes * 60);
    }
}
console.log(Duration.fromMinutes(2).seconds);
// affiche 120
```
Une factory peut être appelée avant même qu'une instance existe, ce qu'une méthode normale ne peut pas faire.

# --instructions--

Triez les éléments pour obtenir un programme fonctionnel qui construit un point à partir d'un tableau et affiche ses coordonnées

# --answers--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
```

```javascript
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
```

```javascript
const p = Point.fromArray([3, 4]);
```

```javascript
console.log(`${p.x},${p.y}`);
```

# --solutions--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
const p = Point.fromArray([3, 4]);
console.log(`${p.x},${p.y}`);
```

# --output--

3,4
