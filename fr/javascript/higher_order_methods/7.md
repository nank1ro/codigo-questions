---
language: javascript
exerciseType: 4
---

# --description--

Le comparateur peut regarder n'importe quelle partie des éléments, donc un tableau d'objets se trie selon l'une de leurs propriétés simplement en comparant cette propriété :
```javascript
const items = [{ name: "b", size: 3 }, { name: "a", size: 1 }];
const bySize = [...items].sort((x, y) => x.size - y.size);
console.log(bySize.map((item) => item.name));
// prints [ 'a', 'b' ]
```
Trier la copie laisse `items` dans son ordre d'origine.

# --instructions--

Trie les lignes pour trier une copie de `players` par `score` croissant, afficher leurs noms dans cet ordre avec `map`, puis afficher le nom du premier élément de `players` pour montrer qu'il n'a pas changé. Déclare `ranked` avant `names`

# --answers--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
```

```javascript
const ranked = [...players].sort((a, b) => a.score - b.score);
```

```javascript
const names = ranked.map((p) => p.name);
```

```javascript
console.log(names);
```

```javascript
console.log(players[0].name);
```

# --solutions--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
const ranked = [...players].sort((a, b) => a.score - b.score);
const names = ranked.map((p) => p.name);
console.log(names);
console.log(players[0].name);
```

# --output--

[ 'Leo', 'Zoe', 'Mia' ]
Zoe
