---
language: javascript
exerciseType: 1
---

# --description--

Une fonction qui renvoie une fonction est aussi la façon naturelle d'**adapter** un callback. Suppose que tu aies un prédicat et qu'il te faille son contraire pour `filter` : au lieu de le réécrire, enveloppe-le :
```javascript
const isLong = (word) => word.length > 4;
const isShort = (word) => !isLong(word);
console.log(["tree", "forest"].filter(isShort));
// prints [ 'tree' ]
```
Un `not(predicate)` générique ferait cela pour n'importe quel prédicat : il renvoie une nouvelle fonction qui appelle `predicate` avec le même argument et inverse le résultat avec `!`. Les prédicats de `filter`, `find`, `some` et `every` reçoivent l'élément en premier argument, l'enveloppe n'a donc qu'à transmettre cette seule valeur.

# --instructions--

Écris une fonction `not(predicate)` qui renvoie un nouveau prédicat renvoyant `true` exactement quand `predicate` renvoie `false` pour le même argument

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function not(predicate) {

}
```

# --asserts--

`not((n) => n % 2 === 0)(3)` doit renvoyer `true`.

```javascript
tryCatch(not((n) => n % 2 === 0)(3) === true);
```

`not((n) => n % 2 === 0)(4)` doit renvoyer `false`.

```javascript
tryCatch(not((n) => n % 2 === 0)(4) === false);
```

`[1, 2, 3, 4].filter(not(isEven))` doit renvoyer `[1, 3]`.

```javascript
tryCatch((() => { const isEven = (n) => n % 2 === 0; return JSON.stringify([1, 2, 3, 4].filter(not(isEven))) === "[1,3]"; })());
```

`["a", "", "b"].filter(not((s) => s === ""))` doit renvoyer `["a", "b"]`.

```javascript
tryCatch(JSON.stringify(["a", "", "b"].filter(not((s) => s === ""))) === '["a","b"]');
```

Nier deux fois doit redonner la réponse d'origine : `not(not(isEven))(4)` doit renvoyer `true`.

```javascript
tryCatch(not(not((n) => n % 2 === 0))(4) === true);
```

`not` doit renvoyer une fonction.

```javascript
tryCatch(typeof not((n) => n > 0) === "function");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function not(predicate) {
  return (value) => !predicate(value);
}
```
