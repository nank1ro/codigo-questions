---
language: javascript
exerciseType: 2
---

# --description--

En JavaScript une fonction est une **valeur** : tu peux la stocker dans une variable, la mettre dans un tableau et la passer à une autre fonction en argument. Une fonction qui reçoit une fonction en argument, ou qui en renvoie une, s'appelle une **fonction d'ordre supérieur**. La fonction passée s'appelle un **callback**, car celui qui la reçoit *la rappelle* quand il en a besoin :
```javascript
function shout(text) {
  return text.toUpperCase() + "!";
}
function twice(fn, value) {
  return fn(fn(value));
}
console.log(twice(shout, "hi"));
// prints HI!!
```
Remarque que `shout` est passée **sans parenthèses** : `twice(shout, "hi")` transmet la fonction elle-même, alors que `twice(shout("hi"), "hi")` appellerait d'abord `shout` et passerait son résultat, la chaîne `"HI!"`, qui ne peut pas être appelée.

# --instructions--

Complète `applyTo` pour qu'elle renvoie le résultat de l'appel de `fn` avec `value`, puis appelle-la avec `double` et avec `negate` (en passant les fonctions elles-mêmes), toutes deux avec `21`, produisant la sortie `42 -21`

# --seed--

```javascript
function double(n) {
  return n * 2;
}
function negate(n) {
  return -n;
}
function applyTo(fn, value) {
  return [/](value);
}
console.log(applyTo([/], 21), applyTo([/], 21));
```

# --answers--

- fn
- double
- negate
- double()
- negate(21)
- callback

# --solutions--

```javascript
function double(n) {
  return n * 2;
}
function negate(n) {
  return -n;
}
function applyTo(fn, value) {
  return fn(value);
}
console.log(applyTo(double, 21), applyTo(negate, 21));
```

# --output--

42 -21
