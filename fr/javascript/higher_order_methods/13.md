---
language: javascript
exerciseType: 1
---

# --description--

Toute fonction possède une méthode `bind` qui renvoie une **nouvelle** fonction avec certaines choses fixées à l'avance. Son premier argument devient le `this` de la nouvelle fonction ; les arguments restants sont placés devant ceux avec lesquels la nouvelle fonction est appelée (une **application partielle**) :
```javascript
function multiply(a, b) {
  return a * b;
}
const double = multiply.bind(null, 2);
console.log(double(21));
// prints 42
```
Fixer `this` est important pour les méthodes. Quand une méthode est copiée hors de son objet et appelée seule, `this` ne désigne plus l'objet, donc `this.name` devient `undefined`. `bind` le verrouille sur l'objet :
```javascript
const user = {
  name: "Ana",
  hello() { return `Hi ${this.name}`; },
};
const loose = user.hello;
console.log(loose());
// prints Hi undefined
const bound = user.hello.bind(user);
console.log(bound());
// prints Hi Ana
```
La fonction d'origine n'est jamais modifiée : `bind` construit toujours une nouvelle fonction, dont le `name` est le nom d'origine précédé de `bound `.

# --instructions--

La fonction `power` et l'objet `counter` sont fournis. Stocke dans la constante `square` une version de `power` liée avec `this` fixé à `null` et `exponent` fixé à `2`, et dans la constante `tick` une version de `counter.increment` liée à `counter`, pour qu'elle continue de fonctionner quand elle est appelée seule

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = 
const tick = 
```

# --asserts--

`square(3)` doit renvoyer `9`.

```javascript
tryCatch(square(3) === 9);
```

`square(12)` doit renvoyer `144`.

```javascript
tryCatch(square(12) === 144);
```

`square` doit être créée avec `bind`, son nom est donc `"bound power"`.

```javascript
tryCatch(square.name === "bound power");
```

`tick` doit être une nouvelle fonction créée avec `bind`, pas `counter.increment` elle-même.

```javascript
tryCatch(tick !== counter.increment && tick.name === "bound increment");
```

Appeler `tick()` deux fois seule doit renvoyer `1` puis `2`, et `counter.count` doit valoir `2`.

```javascript
tryCatch(tick() === 1 && tick() === 2 && counter.count === 2);
```

`tick` doit encore fonctionner quand elle est stockée dans une autre variable et appelée depuis celle-ci.

```javascript
tryCatch((() => { const alone = tick; return alone() === 3; })());
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = power.bind(null, 2);
const tick = counter.increment.bind(counter);
```
