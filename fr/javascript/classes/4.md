---
language: javascript
exerciseType: 2
---

# --description--

La définition d'une classe ne crée pas d'objet.
Pour ce faire, nous devons créer une __instance__ d'une classe.
En JavaScript, pour créer une nouvelle instance d'une classe, nous utilisons toujours le mot-clé `new` avant le nom de la classe.
Si vous voulez assigner une valeur par défaut à un paramètre, faites-le dans la liste des paramètres du constructeur

# --instructions--

Attribuez `4` comme valeur par défaut au paramètre `legs`.
Créez une instance de la classe `Animal` appelée `dog`

# --seed--

```javascript
class Animal {
    constructor(gender, legs[/][/]) {
        this.gender = gender;
        this.legs = legs;
    }
}
let [/] = [/] Animal("male");
console.log(dog);
```

# --answers--

- dog
- dog
- gender
- Animal
- ()
- animal
- []
-  = 
- 4
- new

# --solutions--

```javascript
class Animal {
    constructor(gender, legs = 4) {
        this.gender = gender;
        this.legs = legs;
    }
}
let dog = new Animal("male");
console.log(dog);
```

# --output--

Animal {
    gender: "male",
    legs: 4
}
