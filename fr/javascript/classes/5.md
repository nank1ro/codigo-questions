---
language: javascript
exerciseType: 2
---

# --description--

Lorsqu'une classe a ses propres fonctions, ces fonctions sont appelées des __méthodes__.

# --instructions--

Ajoutez une méthode, `description`, à votre classe `Animal`.
En utilisant deux déclarations d'impression distinctes, elle doit imprimer le `gender` et les `legs` de l'animal sur lequel elle est appelée.
Puis appelez la méthode `description`

# --seed--

```javascript
class Animal {
    constructor(gender, legs = 4) {
        this.gender = gender;
        this.legs = legs;
    }
    [/] {
        console.log([/].[/]);
        console.log([/].legs);
    }
}
let dog = new Animal("male");
dog.description();
```

# --answers--

-  description
- function
- description()
- this
- description[]
- function
- method
- gender
- this

# --solutions--

```javascript
class Animal {
    constructor(gender, legs = 4) {
        this.gender = gender;
        this.legs = legs;
    }
    description() {
        console.log(this.gender);
        console.log(this.legs);
    }
}
let dog = new Animal("male");
dog.description();
```

# --output--

female
4
