---
language: javascript
exerciseType: 4
---

# --description--

L'ordre à l'intérieur d'un commentaire JSDoc est toujours le même : d'abord le résumé, puis un `@param` par paramètre dans l'ordre où ils sont déclarés, puis `@returns` en dernier. L'ouverture `/**` et la fermeture ` */` entourent le tout, et le commentaire se place directement au-dessus de la fonction qu'il décrit :
```javascript
/**
 * Returns the number of seconds in the given minutes.
 * @param {number} minutes a whole number of minutes
 * @returns {number} minutes multiplied by sixty
 */
function toSeconds(minutes) {
  return minutes * 60;
}
```

# --instructions--

Ordonne les éléments pour documenter `twice` avec un résumé, une ligne `@param` et une ligne `@returns`, dans cet ordre et directement au-dessus de la fonction, puis affiche `twice(21)` après avoir défini la fonction

# --answers--

```javascript
/**
```

```javascript
 * Returns the double of a number.
```

```javascript
 * @param {number} n the number to double
```

```javascript
 * @returns {number} n multiplied by two
```

```javascript
 */
```

```javascript
function twice(n) {
  return n * 2;
}
```

```javascript
console.log(twice(21));
```

# --solutions--

```javascript
/**
 * Returns the double of a number.
 * @param {number} n the number to double
 * @returns {number} n multiplied by two
 */
function twice(n) {
  return n * 2;
}
console.log(twice(21));
```

# --output--

42
