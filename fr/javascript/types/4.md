---
language: javascript
exerciseType: 2
---

# --description--

Vous pouvez convertir une valeur vers un autre type **explicitement** en appelant le type comme une fonction :
- `Number(value)` convertit en nombre : `Number("42")` vaut `42`
- `String(value)` convertit en chaîne : `String(42)` vaut `"42"`
- `Boolean(value)` convertit en booléen : `Boolean("")` vaut `false`

Le résultat est une toute nouvelle valeur ; l'originale n'est pas modifiée :
```javascript
const input = "7";
const count = Number(input) + 1;
console.log(count, typeof count);
// prints 8 number
console.log(String(count) + "!");
// prints 8!
```
Convertir explicitement rend votre intention visible : quiconque lit `Number(input)` sait que `input` était du texte.

# --instructions--

Complétez le code pour que `count` soit `input` converti en nombre plus `1`, que `label` soit `count` converti en chaîne et répété deux fois, et que `flag` soit la chaîne vide convertie en booléen, produisant la sortie `43 4343 false`

# --seed--

```javascript
const input = "42";
const count = [/](input) + 1;
const label = [/](count).repeat(2);
const flag = [/]("");
console.log(count, label, flag);
```

# --answers--

- Number
- String
- Boolean
- Int
- Bool
- toString

# --solutions--

```javascript
const input = "42";
const count = Number(input) + 1;
const label = String(count).repeat(2);
const flag = Boolean("");
console.log(count, label, flag);
```

# --output--

43 4343 false
