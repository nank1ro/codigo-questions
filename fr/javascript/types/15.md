---
language: javascript
exerciseType: 1
---

# --description--

Comme `typeof` répond `"object"` pour les tableaux, les objets et `null`, les distinguer nécessite deux vérifications supplémentaires.
`Array.isArray(value)` vaut `true` uniquement pour les tableaux :
```javascript
console.log(Array.isArray([1, 2]), Array.isArray({}));
// prints true false
```
Pour `null`, comparez directement, `value === null`. Les combiner donne une image complète de n'importe quelle valeur :
```javascript
function kind(value) {
  if (value === null) return "null";
  if (Array.isArray(value)) return "array";
  return typeof value;
}
```
Vérifiez d'abord `null` et les tableaux, car le `typeof` simple ne peut pas les distinguer.

# --instructions--

Écrivez une fonction `typeName(value)` qui renvoie `"null"` pour `null`, `"array"` pour les tableaux et le résultat de `typeof` pour toute autre valeur, en utilisant `Array.isArray`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function typeName(value) {

}
```

# --asserts--

`typeName(null)` doit renvoyer `"null"`.

```javascript
tryCatch(typeName(null) === "null");
```

`typeName([1, 2])` et `typeName([])` doivent renvoyer `"array"`.

```javascript
tryCatch(typeName([1, 2]) === "array" && typeName([]) === "array");
```

`typeName({})` doit renvoyer `"object"`.

```javascript
tryCatch(typeName({}) === "object");
```

`typeName(42)`, `typeName("a")` et `typeName(true)` doivent renvoyer `"number"`, `"string"` et `"boolean"`.

```javascript
tryCatch(typeName(42) === "number" && typeName("a") === "string" && typeName(true) === "boolean");
```

`typeName(undefined)`, `typeName(10n)` et `typeName(Symbol())` doivent renvoyer `"undefined"`, `"bigint"` et `"symbol"`.

```javascript
tryCatch(typeName(undefined) === "undefined" && typeName(10n) === "bigint" && typeName(Symbol()) === "symbol");
```

`typeName(function () {})` doit renvoyer `"function"`.

```javascript
tryCatch(typeName(function () {}) === "function");
```

La fonction doit utiliser `Array.isArray`.

```javascript
tryCatch(/Array\.isArray\(/.test(typeName.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function typeName(value) {
  if (value === null) {
    return "null";
  }
  if (Array.isArray(value)) {
    return "array";
  }
  return typeof value;
}
```
