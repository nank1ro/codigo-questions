---
language: javascript
exerciseType: 4
---

# --description--

JavaScript possède un seul type `number` pour les nombres entiers et décimaux : chaque nombre est une valeur flottante 64 bits (un *double*). Donc `5` et `5.0` sont la même valeur, et il n'y a pas de type entier séparé :
```javascript
console.log(5 === 5.0, 10 / 2);
// prints true 5
```
Pour savoir si un nombre n'a pas de partie fractionnaire, utilisez `Number.isInteger` :
```javascript
console.log(Number.isInteger(5.0), Number.isInteger(3.5));
// prints true false
```
Les chaînes de modèle convertissent la valeur interpolée en chaîne avec les mêmes règles que `String()`, donc `${5.0}` devient `"5"`, pas `"5.0"`.

# --instructions--

Ordonnez les lignes pour définir `half`, qui divise `n` par deux et renvoie une phrase disant si le résultat est entier, puis affichez le résultat pour `10` et pour `7`. Définissez d'abord la fonction

# --answers--

```javascript
function half(n) {
```

```javascript
  const result = n / 2;
```

```javascript
  if (Number.isInteger(result)) {
```

```javascript
    return `${result} is whole`;
```

```javascript
  }
```

```javascript
  return `${result} is not whole`;
```

```javascript
}
```

```javascript
console.log(half(10));
```

```javascript
console.log(half(7));
```

# --solutions--

```javascript
function half(n) {
  const result = n / 2;
  if (Number.isInteger(result)) {
    return `${result} is whole`;
  }
  return `${result} is not whole`;
}
console.log(half(10));
console.log(half(7));
```

# --output--

5 is whole
3.5 is not whole
