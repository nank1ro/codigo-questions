---
language: javascript
exerciseType: 1
---

# --description--

Quand JavaScript a besoin d'un booléen, par exemple dans une condition `if` ou dans `Boolean(value)`, il convertit la valeur. Seules huit valeurs deviennent `false` ; elles sont appelées **falsy** :
`false`, `0`, `-0`, `0n`, `""`, `null`, `undefined` et `NaN`.
**Tout le reste est truthy**, y compris certaines valeurs qui semblent vides :
```javascript
console.log(Boolean(0), Boolean(""), Boolean(NaN));
// prints false false false
console.log(Boolean("0"), Boolean("false"), Boolean([]), Boolean({}));
// prints true true true true
```
`"0"` est une chaîne non vide, donc elle est truthy ; un tableau vide est un objet, donc il est truthy aussi.

# --instructions--

Écrivez une fonction `countTruthy(values)` qui renvoie combien d'éléments du tableau `values` sont truthy

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function countTruthy(values) {

}
```

# --asserts--

`countTruthy([1, "a", true])` doit renvoyer `3`.

```javascript
tryCatch(countTruthy([1, "a", true]) === 3);
```

`countTruthy([0, "", null, undefined, NaN, false])` doit renvoyer `0`.

```javascript
tryCatch(countTruthy([0, "", null, undefined, NaN, false]) === 0);
```

`countTruthy(["0", "false", [], {}])` doit renvoyer `4`.

```javascript
tryCatch(countTruthy(["0", "false", [], {}]) === 4);
```

`countTruthy([0n, -0, " "])` doit renvoyer `1`, car seule la chaîne contenant une espace est truthy.

```javascript
tryCatch(countTruthy([0n, -0, " "]) === 1);
```

`countTruthy([])` doit renvoyer `0`.

```javascript
tryCatch(countTruthy([]) === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function countTruthy(values) {
  let count = 0;
  for (const value of values) {
    if (value) {
      count++;
    }
  }
  return count;
}
```
