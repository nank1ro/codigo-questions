---
language: javascript
exerciseType: 2
---

# --description--

Une **Map** stocke des **paires clé-valeur** : chaque valeur est enregistrée sous une clé, et vous utilisez cette clé pour retrouver la valeur.
Vous créez une map vide avec `new Map()`, ajoutez une paire avec `set(key, value)` et lisez une valeur avec `get(key)` :
```javascript
let ages = new Map();
ages.set("Ann", 30);
ages.set("Bob", 25);
console.log(ages.get("Ann"));
// prints 30
```
Appeler `set()` avec une clé qui existe déjà remplace sa valeur.

# --instructions--

Créez une map vide `ages`, stockez `30` sous la clé `"Ann"`, puis affichez la valeur stockée pour `"Ann"`

# --seed--

```javascript
let ages = new [/]();
ages.[/]("Ann", 30);
console.log(ages.[/]("Ann"));
```

# --answers--

- Map
- set
- get
- Object
- add
- put
- read

# --solutions--

```javascript
let ages = new Map();
ages.set("Ann", 30);
console.log(ages.get("Ann"));
```

# --output--

30
