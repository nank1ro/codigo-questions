---
language: javascript
exerciseType: 2
---

# --description--

`delete(key)` renvoie `true` quand une paire a été supprimée et `false` quand la clé n'était pas présente.
Pour supprimer **toutes** les paires d'un coup, appelez `clear()` :
```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Retirez `"pen"` de `cart` en affichant le résultat de la suppression, puis videz complètement la map et affichez combien de paires restent

# --seed--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
