---
language: javascript
exerciseType: 1
---

# --description--

La déstructuration est la plus utile au moment même où un tableau arrive : un argument de fonction, ou le résultat d'un appel. Au lieu de garder le tableau sous la main et de l'indexer partout, vous le déballez une seule fois et donnez de vrais noms à ses parties :
```javascript
function middle(range) {
    const [start, end] = range;
    return (start + end) / 2;
}
console.log(middle([0, 10]));
// affiche 5
```
Rien n'est copié ni modifié dans le tableau d'origine, le motif ne fait qu'y lire.

# --instructions--

Écrivez une fonction `label` qui prend un tableau à deux éléments contenant un nom et un score, et renvoie la chaîne `"name: score"`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function label(entry) {
  
}
```

# --asserts--

`label(["Ada", 90])` doit renvoyer `"Ada: 90"`.

```javascript
tryCatch(label(["Ada", 90]) === "Ada: 90");
```

`label(["Bob", 0])` doit renvoyer `"Bob: 0"`.

```javascript
tryCatch(label(["Bob", 0]) === "Bob: 0");
```

Un score négatif doit être conservé tel quel.

```javascript
tryCatch(label(["Cy", -3]) === "Cy: -3");
```

`label(["Dee", 100])` doit renvoyer `"Dee: 100"`.

```javascript
tryCatch(label(["Dee", 100]) === "Dee: 100");
```

Les éléments en trop doivent être ignorés.

```javascript
tryCatch(label(["Eve", 55, "extra"]) === "Eve: 55");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function label(entry) {
  const [name, score] = entry;
  return `${name}: ${score}`;
}
```
