---
language: javascript
exerciseType: 2
---

# --description--

Les objets peuvent aussi être déstructurés, avec des accolades au lieu de crochets. Ici la position ne signifie rien : chaque nom est confronté à la **clé** qui s'écrit de la même façon :
```javascript
const user = { name: "Ada", age: 36 };
const { age, name } = user;
console.log(name, age);
// affiche Ada 36
```
Permuter `age` et `name` dans le motif ne change rien, et les clés que le motif ne mentionne pas sont simplement laissées de côté. Un nom sans clé correspondante devient `undefined`.

# --instructions--

Complétez les deux motifs pour que le programme affiche `Dune (1965) by Herbert`

# --seed--

```javascript
const book = { title: "Dune", author: "Herbert", year: 1965 };
const { title, [/] } = book;
const { author } = [/];
console.log(`${title} (${year}) by ${author}`);
```

# --answers--

- year
- book
- title
- published
- novel
- 1965

# --solutions--

```javascript
const book = { title: "Dune", author: "Herbert", year: 1965 };
const { title, year } = book;
const { author } = book;
console.log(`${title} (${year}) by ${author}`);
```

# --output--

Dune (1965) by Herbert
