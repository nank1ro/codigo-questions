---
language: javascript
exerciseType: 2
---

# --description--

Tout comme dans l'addition assignment, nous avons le **decrement assignment** `-=`.
La fonctionnalité est la même, la seule différence est qu'elle effectue la soustraction.
Donc les éléments suivants sont exactement les mêmes
```javascript
var num = num - 5;
// is equal to
num -= 5;
```

# --instructions--

Soustrayez de la variable `result` la valeur `15` en utilisant l'opérateur *decrement assignment*, puis imprimez sa valeur

# --seed--

```javascript
var result = 75;
result [/][/];
console.log(result);
```

# --answers--

- - 
- -= 
- =- 
- 50
- 15

# --solutions--

```javascript
var result = 75;
result -= 15;
console.log(result);
```

# --output--

60
