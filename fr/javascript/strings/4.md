---
language: javascript
exerciseType: 1
---

# --description--

Pour vérifier si une chaîne contient une autre chaîne, utilise ces méthodes, qui renvoient toutes un booléen :
- `includes(text)` vaut `true` si `text` apparaît n'importe où
- `startsWith(text)` vaut `true` si la chaîne commence par `text`
- `endsWith(text)` vaut `true` si la chaîne se termine par `text`

```javascript
let file = "photo.png";
console.log(file.includes("."));
// prints true
console.log(file.startsWith("ph"));
// prints true
console.log(file.endsWith(".jpg"));
// prints false
```
La comparaison est sensible à la casse : `"Hello".includes("h")` vaut `false`.

# --instructions--

Écris une fonction `isImage(fileName)` qui renvoie `true` quand `fileName` se termine par `".png"`, et `false` sinon

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isImage(fileName) {

}
```

# --asserts--

`isImage("cat.png")` doit renvoyer `true`.

```javascript
tryCatch(isImage("cat.png") === true);
```

`isImage("cat.jpg")` doit renvoyer `false`.

```javascript
tryCatch(isImage("cat.jpg") === false);
```

`isImage("png.txt")` doit renvoyer `false`.

```javascript
tryCatch(isImage("png.txt") === false);
```

`isImage("a.png.txt")` doit renvoyer `false`.

```javascript
tryCatch(isImage("a.png.txt") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isImage(fileName) {
  return fileName.endsWith(".png");
}
```
