---
language: javascript
exerciseType: 1
---

# --description--

La méthode `padStart(targetLength, padString)` ajoute `padString` au **début** de la chaîne jusqu'à atteindre `targetLength` caractères. `padEnd()` fait la même chose à la fin :
```javascript
console.log("7".padStart(3, "0"));
// prints 007
console.log("Tea".padEnd(6, "."));
// prints Tea...
```
Si la chaîne est déjà assez longue, elle est renvoyée inchangée.
Les nombres n'ont pas de méthodes de chaîne, alors convertis-les d'abord avec `String(number)`.

# --instructions--

Écris une fonction `twoDigits(number)` qui renvoie `number` sous forme de chaîne d'au moins deux caractères, complétée à gauche par `"0"`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function twoDigits(number) {

}
```

# --asserts--

`twoDigits(5)` doit renvoyer `"05"`.

```javascript
tryCatch(twoDigits(5) === "05");
```

`twoDigits(12)` doit renvoyer `"12"`.

```javascript
tryCatch(twoDigits(12) === "12");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function twoDigits(number) {
  return String(number).padStart(2, "0");
}
```
