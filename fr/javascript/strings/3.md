---
language: javascript
exerciseType: 2
---

# --description--

Les chaînes disposent de nombreuses **méthodes** intégrées. Deux des plus simples changent la casse de chaque lettre :
```javascript
let word = "Hello";
console.log(word.toUpperCase());
// prints HELLO
console.log(word.toLowerCase());
// prints hello
```
Ces deux méthodes ne prennent aucun argument, alors n'oublie pas les parenthèses.

# --instructions--

Affiche `name` en majuscules, puis en minuscules

# --seed--

```javascript
let name = "Ada";
console.log(name.[/]());
console.log(name.[/]());
```

# --answers--

- toUpperCase
- toLowerCase
- upper
- lower
- capitalize

# --solutions--

```javascript
let name = "Ada";
console.log(name.toUpperCase());
console.log(name.toLowerCase());
```

# --output--

ADA
ada
