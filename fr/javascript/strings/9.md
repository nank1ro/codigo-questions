---
language: javascript
exerciseType: 2
---

# --description--

La méthode `split(separator)` découpe une chaîne en un **tableau** de morceaux, en coupant à chaque `separator` :
```javascript
let sentence = "I like JavaScript";
let words = sentence.split(" ");
console.log(words);
// prints [ 'I', 'like', 'JavaScript' ]
```
L'opération inverse est la méthode de tableau `join(separator)`, qui recolle les morceaux en une chaîne :
```javascript
console.log(words.join("-"));
// prints I-like-JavaScript
```

# --instructions--

Découpe `csv` à chaque virgule et affiche combien de couleurs il contient

# --seed--

```javascript
let csv = "red,green,blue";
let colors = csv.[/]([/]);
console.log(colors.[/]);
```

# --answers--

- split
- ","
- " "
- join
- length
- size

# --solutions--

```javascript
let csv = "red,green,blue";
let colors = csv.split(",");
console.log(colors.length);
```

# --output--

3
