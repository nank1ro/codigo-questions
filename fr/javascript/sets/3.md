---
language: javascript
exerciseType: 3
---

# --description--

`add()` renvoie le set lui-même, donc plusieurs appels peuvent être chaînés :
```javascript
let letters = new Set();
letters.add("a").add("b");
```
Que vous chaîniez ou non, une valeur déjà présente n'est jamais ajoutée une seconde fois, donc `size` compte chaque valeur distincte une seule fois.

# --instructions--

Qu'affiche ce code ?
```javascript
let letters = new Set();
letters.add("a").add("b").add("a");
console.log(letters.size);
```

# --answers--

- 2
- 3
- 1
- Une erreur, car "a" est déjà dans le set

# --solutions--

- 2
