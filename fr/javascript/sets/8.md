---
language: javascript
exerciseType: 2
---

# --description--

`delete(value)` renvoie `true` quand la valeur a été retirée et `false` quand elle n'était pas dans le set.
Pour retirer **toutes** les valeurs d'un coup, appelez `clear()` :
```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Retirez `"pen"` de `cart` en affichant le résultat du retrait, puis videz tout le set et affichez combien de valeurs restent

# --seed--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
