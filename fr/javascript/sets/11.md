---
language: javascript
exerciseType: 2
---

# --instructions--

Construisez le tableau `common` avec les nombres qui sont à la fois dans `a` et dans `b`, puis affichez-le

# --seed--

```javascript
let a = new Set([1, 2, 3]);
let b = new Set([2, 3, 4]);
let common = [...a].[/]((x) => b.[/](x));
console.log(common);
```

# --answers--

- filter
- has
- map
- find
- includes
- contains

# --solutions--

```javascript
let a = new Set([1, 2, 3]);
let b = new Set([2, 3, 4]);
let common = [...a].filter((x) => b.has(x));
console.log(common);
```

# --output--

[ 2, 3 ]
