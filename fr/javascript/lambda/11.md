---
language: javascript
exerciseType: 1
---

# --description--

Puisqu'une fonction est une valeur, une fonction fléchée peut **renvoyer une autre fonction fléchée**. Chaîner deux flèches est une manière compacte d'écrire une fonction qui construit des fonctions :
```javascript
const makeAdder = (amount) => (n) => n + amount;
const addTen = makeAdder(10);
console.log(addTen(5));
// prints 15
console.log(makeAdder(1)(5));
// prints 6
```
Lisez-la de gauche à droite : `makeAdder` prend `amount` et renvoie `(n) => n + amount`, une fonction fléchée qui capture `amount` à travers une fermeture. `makeAdder(1)(5)` appelle immédiatement la fonction renvoyée.

# --instructions--

Stockez dans la constante `makeMultiplier` une fonction fléchée qui prend un `factor` et qui renvoie une fonction fléchée qui multiplie son argument par `factor`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const makeMultiplier = 
```

# --asserts--

`makeMultiplier(3)` doit renvoyer une fonction.

```javascript
tryCatch(typeof makeMultiplier(3) === "function");
```

`makeMultiplier(3)(4)` doit renvoyer `12`.

```javascript
tryCatch(makeMultiplier(3)(4) === 12);
```

La fonction renvoyée par `makeMultiplier(2)` doit renvoyer `20` lorsqu'elle est appelée avec `10`.

```javascript
tryCatch((() => { const twice = makeMultiplier(2); return twice(10) === 20; })());
```

Deux multiplicateurs doivent être indépendants : `makeMultiplier(5)(2)` doit être `10` et `makeMultiplier(0)(2)` doit être `0`.

```javascript
tryCatch(makeMultiplier(5)(2) === 10 && makeMultiplier(0)(2) === 0);
```

`makeMultiplier` et la fonction qu'elle renvoie doivent toutes deux être des fonctions fléchées.

```javascript
tryCatch(!/function/.test(makeMultiplier.toString()) && /=>/.test(makeMultiplier(3).toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const makeMultiplier = (factor) => (n) => n * factor;
```
