---
language: javascript
exerciseType: 4
---

# --description--

Deux autres méthodes d'array prennent un callback.
`forEach(callback)` appelle le callback une fois par élément et ne renvoie rien ; utilisez-le pour des effets de bord tels que l'affichage.
`reduce(callback, initialValue)` réduit l'array en une seule valeur : le callback reçoit la valeur accumulée jusqu'à présent et l'élément courant, et renvoie la nouvelle valeur accumulée :
```javascript
const numbers = [1, 2, 3];
numbers.forEach((n) => console.log(n));
// prints 1, 2 and 3 on three lines
const total = numbers.reduce((sum, n) => sum + n, 0);
console.log(total);
// prints 6
```

# --instructions--

Triez les lignes pour calculer le total de `prices` avec `reduce` juste après la déclaration de l'array, affichez chaque prix sur sa propre ligne avec `forEach`, puis affichez le total

# --answers--

```javascript
const prices = [4, 6, 10];
```

```javascript
const total = prices.reduce((sum, p) => sum + p, 0);
```

```javascript
prices.forEach((p) => console.log(p));
```

```javascript
console.log(total);
```

# --solutions--

```javascript
const prices = [4, 6, 10];
const total = prices.reduce((sum, p) => sum + p, 0);
prices.forEach((p) => console.log(p));
console.log(total);
```

# --output--

4
6
10
20
