---
language: javascript
exerciseType: 4
---

# --instructions--

Triez les lignes pour définir `makeCounter`, qui renvoie une fonction fléchée qui incrémente et renvoie son `count` privé, puis créez le compteur `next` et affichez le résultat de deux appels sur la même ligne

# --answers--

```javascript
const makeCounter = () => {
```

```javascript
  let count = 0;
```

```javascript
  return () => {
```

```javascript
    count += 1;
```

```javascript
    return count;
```

```javascript
  };
```

```javascript
};
```

```javascript
const next = makeCounter();
```

```javascript
console.log(next(), next());
```

# --solutions--

```javascript
const makeCounter = () => {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
};
const next = makeCounter();
console.log(next(), next());
```

# --output--

1 2
