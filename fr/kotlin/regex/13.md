---
language: kotlin
exerciseType: 1
---

# --description--

`findAll` et les groupes transforment ensemble une ligne de texte en données structurées. Chaque `MatchResult` de la séquence porte ses propres `groupValues`, donc une seule chaîne peut construire une liste, une map ou un total :
```kotlin
val pair = Regex("""(\w+)=(\w+)""")
val config = pair.findAll("host=local;port=80").associate { it.groupValues[1] to it.groupValues[2] }
println(config) // {host=local, port=80}
```
`associate` construit une map à partir des paires `key to value` renvoyées par la lambda. Quand un motif a un nombre fixe de groupes, `destructured` vous permet de les déballer dans des variables nommées au lieu de les lire par indice :
```kotlin
val (key, value) = pair.find("host=local")!!.destructured
println(key)   // host
println(value) // local
```

# --instructions--

Écrivez une fonction `parseConfig(text: String): Map<String, String>` qui lit chaque paire `key=value` de `text`, où les clés et les valeurs sont des suites de caractères de mot, et les renvoie sous forme de map.

Exemple : `parseConfig("host=local;port=80")` renvoie `{host=local, port=80}`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`parseConfig("host=local;port=80")` doit retourner `{host=local, port=80}`

```kotlin
    tryCatch(parseConfig("host=local;port=80") == mapOf("host" to "local", "port" to "80"))
```

`parseConfig("mode=dark")` doit retourner `{mode=dark}`

```kotlin
    tryCatch(parseConfig("mode=dark") == mapOf("mode" to "dark"))
```

`parseConfig("a=1, b=2, c=3")` doit retourner trois entrées

```kotlin
    tryCatch(parseConfig("a=1, b=2, c=3") == mapOf("a" to "1", "b" to "2", "c" to "3"))
```

`parseConfig("nothing here")` doit retourner une map vide

```kotlin
    tryCatch(parseConfig("nothing here") == mapOf<String, String>())
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun parseConfig(text: String): Map<String, String> {
    return Regex("""(\w+)=(\w+)""").findAll(text).associate { it.groupValues[1] to it.groupValues[2] }
}
```
