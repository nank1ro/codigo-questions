---
language: kotlin
exerciseType: 2
---

# --description--

Une fonction avec plus d'un paramètre reçoit une balise `@param` pour chacun d'eux, écrites dans le même ordre que les paramètres :
```kotlin
/**
 * Returns the area of a rectangle.
 * @param width the horizontal side, in centimetres
 * @param height the vertical side, in centimetres
 * @return the product of [width] and [height]
 */
fun area(width: Int, height: Int): Int {
    return width * height
}
```
Une balise n'est toujours qu'un commentaire : si vous renommez un paramètre et que vous oubliez la balise, rien ne casse, mais la documentation se met à mentir. Mettez à jour le KDoc en même temps que la signature.

# --instructions--

Complétez la documentation de `price` : la première balise doit décrire le paramètre `quantity`, la deuxième le paramètre `unitPrice`, et la troisième la valeur que la fonction renvoie

# --seed--

```kotlin
/**
 * Returns the total price of an order.
 * [/] quantity how many items were ordered
 * [/] unitPrice the price of a single item
 * [/] the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --answers--

- @param
- @param
- @return
- @throws
- @property

# --solutions--

```kotlin
/**
 * Returns the total price of an order.
 * @param quantity how many items were ordered
 * @param unitPrice the price of a single item
 * @return the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --output--

15
