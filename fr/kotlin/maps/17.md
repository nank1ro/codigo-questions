---
language: kotlin
exerciseType: 2
---

# --description--

`map` transforme chaque entrée d'une map en un nouvel élément. Contrairement à `filter`, le résultat est une `List`, pas une `Map` :
```kotlin
val ages = mapOf("Alice" to 30, "Bob" to 25)
val labels = ages.map { (name, age) -> "$name: $age" }
println(labels) // [Alice: 30, Bob: 25]
```

# --instructions--

Complétez le code pour construire une liste de chaînes au format `"name is age"`, une pour chaque entrée de `ages`

# --seed--

```kotlin
val ages = mapOf("Ann" to 30, "Ben" to 25)
val labels = ages.[/] { ([/], [/]) -> "$name is $age" }
println(labels)
```

# --answers--

- map
- filter
- forEach
- name
- age
- key
- value

# --solutions--

```kotlin
val ages = mapOf("Ann" to 30, "Ben" to 25)
val labels = ages.map { (name, age) -> "$name is $age" }
println(labels)
```

# --output--

[Ann is 30, Ben is 25]
