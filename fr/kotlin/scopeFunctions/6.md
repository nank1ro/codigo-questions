---
language: kotlin
exerciseType: 1
---

# --description--

`apply` ne se limite pas aux objets que vous venez de créer : il fonctionne sur n'importe quel objet et, comme il redonne l'objet, vous pouvez utiliser l'expression entière partout où l'objet est attendu.
```kotlin
val box = Box()
box.apply { label = "tools" }        // modifie box et le renvoie
println(listOf(Box().apply { label = "nails" }).size) // 1
```
Le bloc est un bloc de code ordinaire, il peut donc contenir autant d'instructions que nécessaire.

# --instructions--

Créez une classe `Profile` avec deux propriétés, `var name: String` initialisée à `""` et `var age: Int` initialisée à `0`.

Écrivez ensuite une fonction `makeProfile(n: String, a: Int): Profile` qui crée un `Profile()` et utilise `apply` pour affecter `n` à `name` et `a` à `age`, en retournant l'objet configuré.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

Un nouveau `Profile()` doit démarrer avec un nom vide et un âge de `0`

```kotlin
    val fresh = Profile()
    tryCatch(fresh.name == "" && fresh.age == 0)
```

`makeProfile("Ada", 36)` doit avoir `name` égal à `Ada`

```kotlin
    tryCatch(makeProfile("Ada", 36).name == "Ada")
```

`makeProfile("Ada", 36)` doit avoir `age` égal à `36`

```kotlin
    tryCatch(makeProfile("Ada", 36).age == 36)
```

`makeProfile("Bob", 7)` doit avoir `name` égal à `Bob` et `age` égal à `7`

```kotlin
    val bob = makeProfile("Bob", 7)
    tryCatch(bob.name == "Bob" && bob.age == 7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Profile {
    var name: String = ""
    var age: Int = 0
}

fun makeProfile(n: String, a: Int): Profile {
    return Profile().apply {
        name = n
        age = a
    }
}
```
