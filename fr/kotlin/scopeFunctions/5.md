---
language: kotlin
exerciseType: 2
---

# --description--

`apply` avance sur les deux axes à la fois : dans son bloc, l'objet est le récepteur `this` (ses membres peuvent donc être utilisés **sans aucun préfixe**), et l'appel retourne **l'objet lui-même**, et non le résultat du bloc.

Cette combinaison fait de `apply` l'outil idéal pour **configurer** un objet juste au moment où vous le créez :
```kotlin
class Server {
    var host = "localhost"
    var port = 80
}

val server = Server().apply {
    host = "example.com"
    port = 8080
}
println("${server.host}:${server.port}") // example.com:8080
```
`host` et `port` dans le bloc sont `this.host` et `this.port` ; comme `apply` redonne le `Server` configuré, celui-ci peut être affecté immédiatement.

# --instructions--

Complétez le code pour que la nouvelle `Box` soit configurée avec `apply` et que le programme affiche son étiquette et son poids.

# --seed--

```kotlin
class Box {
    var label = "empty"
    var weight = 0
}

fun main() {
    val box = Box().[/] {
        label = "books"
        [/] = 12
    }
    println("${box.label} ${box.weight}")
}
```

# --answers--

- apply
- weight
- let
- also
- run
- size

# --solutions--

```kotlin
class Box {
    var label = "empty"
    var weight = 0
}

fun main() {
    val box = Box().apply {
        label = "books"
        weight = 12
    }
    println("${box.label} ${box.weight}")
}
```

# --output--

books 12
