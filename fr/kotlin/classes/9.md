---
language: kotlin
exerciseType: 2
---

# --instructions--

Complétez le code pour déclarer une `data class` nommée `Color` avec des propriétés `val` nommées `red`, `green` et `blue` de type `Int`.

# --seed--

```kotlin
[/] [/] Color(val red: Int, val green: Int, val blue: Int)
```

# --answers--

- data
- class
- object
- open
- abstract
- sealed

# --solutions--

```kotlin
data class Color(val red: Int, val green: Int, val blue: Int)
```
