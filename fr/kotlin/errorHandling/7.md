---
language: kotlin
exerciseType: 2
---

# --description--

Comme `try` est une expression, il peut être utilisé partout où une valeur est attendue — notamment comme corps entier d'une fonction écrite avec `=` :
```kotlin
fun length(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    -1
}
```
Les deux blocs doivent produire une valeur du même type, ici `Int`. Écrivez la valeur de repli comme dernière expression du bloc `catch` ; il n'y a pas de `return` dans l'un ou l'autre bloc.

# --instructions--

Complétez la fonction pour qu'elle restitue le nombre contenu dans `text`, et `0` lorsque le texte n'est pas un nombre.

# --seed--

```kotlin
fun size(text: String): Int = [/] {
    text.toInt()
} [/] (e: NumberFormatException) {
    [/]
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --answers--

- try
- catch
- 0
- finally
- throw
- 1

# --solutions--

```kotlin
fun size(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    0
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --output--

15
0
