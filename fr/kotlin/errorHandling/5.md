---
language: kotlin
exerciseType: 2
---

# --description--

Un bloc `finally` peut être ajouté à la fin. Il s'exécute **quoi qu'il arrive** : après un `try` réussi, après qu'un `catch` a récupéré, et même lorsque l'exception n'est pas interceptée du tout.

```kotlin
try {
    println("reading")
    println("abc".toInt())
} catch (e: NumberFormatException) {
    println("bad number")
} finally {
    println("closing")
}
```
```
reading
bad number
closing
```
C'est donc l'endroit pour le nettoyage qui ne doit pas être sauté, comme la fermeture d'un fichier. Un `try` a besoin d'au moins un `catch` ou un `finally`, mais il peut avoir les deux.

# --instructions--

Complétez le code pour que la conversion incorrecte soit interceptée et que la ligne de nettoyage s'exécute toujours.

# --seed--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } [/] (e: NumberFormatException) {
        println("bad number")
    } [/] {
        println("closing")
    }
}
```

# --answers--

- catch
- finally
- try
- throw
- else
- do

# --solutions--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } catch (e: NumberFormatException) {
        println("bad number")
    } finally {
        println("closing")
    }
}
```

# --output--

bad number
closing
