---
language: kotlin
exerciseType: 4
---

# --instructions--

Ordonnez les éléments pour que le programme stocke l'issue de la conversion, puis affiche la valeur récupérée, puis le résultat brut, puis indique si la tentative a réussi.

# --answers--

```kotlin
val result = runCatching { "abc".toInt() }
```

```kotlin
val value = result.getOrElse { 0 }
```

```kotlin
println("value: $value")
```

```kotlin
println("raw: ${result.getOrNull()}")
```

```kotlin
println("success: ${result.isSuccess}")
```

# --solutions--

```kotlin
val result = runCatching { "abc".toInt() }
val value = result.getOrElse { 0 }
println("value: $value")
println("raw: ${result.getOrNull()}")
println("success: ${result.isSuccess}")
```

# --output--

value: 0
raw: null
success: false
