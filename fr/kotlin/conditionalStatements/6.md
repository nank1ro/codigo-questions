---
language: kotlin
exerciseType: 2
---

# --description--

On peut aussi imbriquer une instruction conditionnelle (`if`, `else if` ou `else`) à l'intérieur d'une autre instruction conditionnelle, pour créer une structure plus complexe.
```kotlin
var num = 4
if (num < 3) {
    println("the number is lower than 3")
} else {
    if (num == 3) {
        println("the number is 3")
    } else if (num == 4) {
        println("the number is 4")
    } else {
        println("the number is greather than 4")
    }
}
```
et la sortie de ce code est `the number is 4`.

# --instructions--

Modifions le programme de l'heure de la journée, en imbriquant une instruction conditionnelle à l'intérieur de l'instruction `else`.
L'instruction conditionnelle imbriquée doit vérifier si `time` est *inférieur* à `18` et dans le bloc de code doit afficher `"It's afternoon"`.
Si la condition précédente est `false`, affichez `"It's evening"`

# --seed--

```kotlin
var time = 19
if (time > 0 && time < 12) {
    println("It's morning")
} else if (time == 12) {
    println("It's midday")
} else if (time == 0) {
    println("It's midnight")
} else {
    [/]([/][/][/]) {
        println("It's afternoon")
    } [/] {
        println("It's evening")
    }
}
```

# --answers--

- 6
- elif 
-  < 
-  == 
- :
- ;
- time
- else if:
- elseif:
- if 
- else

# --solutions--

```kotlin
var time = 19
if (time > 0 && time < 12) {
    println("It's morning")
} else if (time == 12) {
    println("It's midday")
} else if (time == 0) {
    println("It's midnight")
} else {
    if (time < 6) {
        println("It's afternoon")
    } else {
        println("It's evening")
    }
}
```

# --output--

It's evening
