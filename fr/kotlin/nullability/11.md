---
language: kotlin
exerciseType: 2
---

# --description--

Les collections peuvent aussi contenir des éléments nullables : une `List<Int?>` peut contenir des entrées `null`, tandis qu'une `List<Int>` n'en contient jamais.
`filterNotNull()` retourne une nouvelle liste dont les entrées `null` sont retirées, et le type de ses éléments devient non null, si bien que vous pouvez utiliser les éléments librement :
```kotlin
val scores: List<Int?> = listOf(10, null, 20)
val valid = scores.filterNotNull() // List<Int> [10, 20]
println(valid.sum())               // 30
```

# --instructions--

Complétez le code pour déclarer une liste pouvant contenir des nombres `null`, puis retirez les entrées `null` et affichez les valeurs restantes et leur somme

# --seed--

```kotlin
val values: List<[/]> = listOf(3, null, 5, null)
val present = values.[/]()
println(present)
println(present.sum())
```

# --answers--

- Int?
- Int
- filterNotNull
- filter
- mapNotNull
- requireNotNull

# --solutions--

```kotlin
val values: List<Int?> = listOf(3, null, 5, null)
val present = values.filterNotNull()
println(present)
println(present.sum())
```

# --output--

[3, 5]
8
