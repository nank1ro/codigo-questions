---
language: kotlin
exerciseType: 4
---

# --description--

Une fonction d'extension peut être déclarée sur un **récepteur nullable**, si bien qu'elle peut être appelée même sur une valeur `null`. À l'intérieur, `this` est nullable et doit être vérifié :
```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val none: String? = null
println(none.orDash()) // -
```
Notez qu'aucun `?.` n'est nécessaire au site d'appel : la fonction gère elle-même le cas `null`.
La bibliothèque standard utilise cette astuce dans `isNullOrEmpty()` et `orEmpty()`, qui peuvent être appelées sans risque sur n'importe quel `String?`.

# --instructions--

Pouvez-vous ordonner ces lignes pour définir la fonction d'extension `orDash` et afficher `-` pour un nom absent, puis `kotlin` pour un nom présent ? Définissez d'abord la fonction

# --answers--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
```

```kotlin
val a: String? = null
val b: String? = "kotlin"
```

```kotlin
println(a.orDash())
```

```kotlin
println(b.orDash())
```

# --solutions--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val a: String? = null
val b: String? = "kotlin"
println(a.orDash())
println(b.orDash())
```

# --output--

-
kotlin
