---
language: kotlin
exerciseType: 1
---

# --description--

Une classe `sealed` décrit un ensemble **fermé** d'alternatives : seules les sous-classes écrites dans le même package et le même module sont autorisées, ainsi le compilateur connaît chacune d'elles.

```kotlin
sealed class Shape
data class Circle(val radius: Int) : Shape()
data class Square(val side: Int) : Shape()
```

Le bénéfice, c'est le `when` **exhaustif** : quand vous branchez sur un type sealed et couvrez chaque sous-classe, vous pouvez omettre la branche `else`. Ajoutez une nouvelle sous-classe plus tard et le compilateur signale chaque `when` que vous avez oublié de mettre à jour, au lieu de prendre silencieusement le `else`.

```kotlin
fun name(shape: Shape): String = when (shape) {
    is Circle -> "circle"
    is Square -> "square"
}
```

Après `is Circle`, la valeur subit un smart cast, donc `shape.radius` est disponible dans cette branche sans aucun cast manuel.

# --instructions--

Déclarez une classe sealed `Shape` avec deux sous-classes data class : `Square`, avec une propriété `val` `side` de type `Int`, et `Rectangle`, avec des propriétés `val` `width` et `height` de type `Int`.

Ensuite, écrivez une fonction `area(shape: Shape): Int` qui retourne l'aire de la forme, en utilisant un `when` exhaustif sans branche `else`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`area(Square(3))` doit retourner `9`

```kotlin
    tryCatch(area(Square(3)) == 9)
```

`area(Rectangle(2, 5))` doit retourner `10`

```kotlin
    tryCatch(area(Rectangle(2, 5)) == 10)
```

`area(Square(0))` doit retourner `0`

```kotlin
    tryCatch(area(Square(0)) == 0)
```

`Square` et `Rectangle` doivent être des sous-classes de `Shape`

```kotlin
    val shapes: List<Shape> = listOf(Square(4), Rectangle(3, 3))
    tryCatch(shapes.map { area(it) } == listOf(16, 9))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
sealed class Shape
data class Square(val side: Int) : Shape()
data class Rectangle(val width: Int, val height: Int) : Shape()

fun area(shape: Shape): Int = when (shape) {
    is Square -> shape.side * shape.side
    is Rectangle -> shape.width * shape.height
}
```
