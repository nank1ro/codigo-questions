---
language: kotlin
exerciseType: 2
---

# --instructions--

Complétez le code pour que `Payment` soit une hiérarchie fermée et que le `when` couvre les deux sous-classes sans branche `else`

# --seed--

```kotlin
[/] class Payment
data class Card(val digits: String) : Payment()
data class Transfer(val iban: String) : Payment()

fun label(payment: Payment): String = when (payment) {
    is Card -> "card ${payment.digits}"
    [/] Transfer -> "transfer ${payment.iban}"
}

fun main() {
    println(label(Transfer("IT60X")))
}
```

# --answers--

- sealed
- is
- open
- abstract
- data
- in

# --solutions--

```kotlin
sealed class Payment
data class Card(val digits: String) : Payment()
data class Transfer(val iban: String) : Payment()

fun label(payment: Payment): String = when (payment) {
    is Card -> "card ${payment.digits}"
    is Transfer -> "transfer ${payment.iban}"
}

fun main() {
    println(label(Transfer("IT60X")))
}
```

# --output--

transfer IT60X
