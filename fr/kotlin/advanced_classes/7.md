---
language: kotlin
exerciseType: 1
---

# --description--

Parfois, vous avez besoin d'exactement **une** instance de quelque chose : un logger, un registre, une configuration d'application. Remplacer `class` par `object` déclare ce singleton pour vous :

```kotlin
object Registry {
    var size = 0
    fun add() {
        size++
    }
}

Registry.add()
println(Registry.size) // 1
```

L'instance est créée la première fois que vous la touchez, et vous utilisez le nom lui-même, il n'y a pas d'appel `Registry()` ni de constructeur. Un `object` peut contenir des propriétés, des méthodes, des blocs `init`, et il peut implémenter des interfaces ou étendre une classe.

# --instructions--

Déclarez un `object` nommé `Logger` avec une propriété `var` `count` de type `Int` initialisée à `0`, et une méthode `log(message: String): String` qui ajoute `1` à `count` et retourne le message préfixé par le nouveau compteur, sous la forme `"[1] hello"`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`Logger.count` doit commencer à `0`

```kotlin
    tryCatch(Logger.count == 0)
```

Le premier appel doit retourner `"[1] hello"`

```kotlin
    tryCatch(Logger.log("hello") == "[1] hello")
```

Le deuxième appel doit retourner `"[2] bye"`, car l'état est partagé

```kotlin
    tryCatch(Logger.log("bye") == "[2] bye")
```

`Logger.count` doit valoir `2` après les deux appels

```kotlin
    tryCatch(Logger.count == 2)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
object Logger {
    var count: Int = 0

    fun log(message: String): String {
        count++
        return "[$count] $message"
    }
}
```
