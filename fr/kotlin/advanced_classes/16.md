---
language: kotlin
exerciseType: 4
---

# --description--

Les pièces de ce sujet se combinent habituellement : une `enum class` dont les entrées portent leurs propres propriétés modélise un ensemble fixe de balises, tandis qu'une `data class` transporte la charge utile qui les accompagne.

```kotlin
enum class Speed(val surcharge: Int) {
    STANDARD(0),
    EXPRESS(15)
}

data class Order(val total: Int, val speed: Speed)

val order = Order(100, Speed.EXPRESS)
println(order.total + order.speed.surcharge) // 115
```

# --instructions--

Ordonnez les éléments pour afficher le coût total de la commande. Déclarez l'enum d'abord, puis la data class, puis `main`.

# --answers--

```kotlin
enum class Shipping(val surcharge: Int) { STANDARD(0), EXPRESS(15) }
```

```kotlin
data class Order(val total: Int, val shipping: Shipping)
```

```kotlin
fun main() {
```

```kotlin
    val order = Order(100, Shipping.EXPRESS)
```

```kotlin
    println(order.total + order.shipping.surcharge)
```

```kotlin
}
```

# --solutions--

```kotlin
enum class Shipping(val surcharge: Int) { STANDARD(0), EXPRESS(15) }
data class Order(val total: Int, val shipping: Shipping)
fun main() {
    val order = Order(100, Shipping.EXPRESS)
    println(order.total + order.shipping.surcharge)
}
```

# --output--

115
