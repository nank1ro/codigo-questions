---
language: kotlin
exerciseType: 2
---

# --description--

Il existe deux types de `Set`s en Kotlin :

- `Set` ne peut pas être modifié après sa création.
- `MutableSet` peut être modifié après sa création, ce qui signifie que vous pouvez ajouter, supprimer ou mettre à jour ses éléments.

```kotlin
val numbers = setOf(1, 2, 3)
numbers.add(4) // [1]
```
__[1]__ lève une erreur car les `Set`s sont _en lecture seule_.

Pour créer un ensemble modifiable, utilisez le mot-clé `mutableSetOf`
```kotlin
val numbers = mutableSetOf(1, 2, 3)
numbers.add(4)
println(numbers)
// affiche [1, 2, 3, 4]
```

# --instructions--

Complétez le code pour créer un ensemble modifiable valide

# --seed--

```kotlin
val [/] = [/]<String>[/]"apple", "banana"[/]
fruits.add("mango")
println(fruits)
```

# --answers--

- fruits
- "fruits"
- [
- ]
- (
- )
- listOf
- setOf
- mutableListOf
- mutableSetOf

# --solutions--

```kotlin
val fruits = mutableSetOf<String>("apple", "banana")
fruits.add("mango")
println(fruits)
```

# --output--

[apple, banana, mango]
