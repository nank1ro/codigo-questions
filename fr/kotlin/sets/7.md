---
language: kotlin
exerciseType: 2
---

# --description--

Pour convertir un `Set` en `List`, nous pouvons utiliser la fonction `toList()`

# --instructions--

Complétez le code pour convertir le `Set` en `List`

# --seed--

```kotlin
val uniqueNumbers = setOf(1, 2, 3, 3)
var numbers = [/][/][/]
numbers += 4
println(numbers)
```

# --answers--

- uniqueNumbers
- .
- toList()
- toList
- distinct()
- asList()

# --solutions--

```kotlin
val uniqueNumbers = setOf(1, 2, 3, 3)
var numbers = uniqueNumbers.toList()
numbers += 4
println(numbers)
```

# --output--

[1, 2, 3, 4]
