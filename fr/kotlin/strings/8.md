---
language: kotlin
exerciseType: 4
---

# --description--

Comme `split` retourne une `List`, vous pouvez parcourir ses éléments comme n'importe quelle autre liste :
```kotlin
for (part in "a-b".split("-")) {
    println(part)
}
// prints a, then b
```

# --instructions--

Pouvez-vous ordonner ces lignes pour afficher chaque mot de `sentence` en majuscules, un par ligne ?

# --answers--

```kotlin
val sentence = "the quick brown fox"
```

```kotlin
val words = sentence.split(" ")
```

```kotlin
for (word in words) {
```

```kotlin
    println(word.uppercase())
```

```kotlin
}
```

# --solutions--

```kotlin
val sentence = "the quick brown fox"
val words = sentence.split(" ")
for (word in words) {
    println(word.uppercase())
}
```

# --output--

THE
QUICK
BROWN
FOX
