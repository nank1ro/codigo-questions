---
language: kotlin
exerciseType: 2
---

# --description--

`uppercase()` retourne une copie de la chaîne avec toutes les lettres en majuscules, `lowercase()` fait l'inverse.
La chaîne d'origine n'est pas modifiée :
```kotlin
val name = "Kotlin"
println(name.uppercase()) // KOTLIN
println(name.lowercase()) // kotlin
println(name)             // Kotlin
```

# --instructions--

Complétez le code pour afficher `word` en majuscules, puis `shout` en minuscules

# --seed--

```kotlin
val word = "quiet"
val shout = "LOUD"
println(word.[/]())
println(shout.[/]())
```

# --answers--

- uppercase
- lowercase
- upper
- lower
- titlecase

# --solutions--

```kotlin
val word = "quiet"
val shout = "LOUD"
println(word.uppercase())
println(shout.lowercase())
```

# --output--

QUIET
loud
