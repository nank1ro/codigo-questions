---
language: kotlin
exerciseType: 1
---

# --description--

`repeat(n)` retourne la chaîne concaténée `n` fois :
```kotlin
println("ab".repeat(3)) // ababab
```
`padStart(width, char)` ajoute `char` au début jusqu'à ce que la chaîne atteigne `width` caractères ; `padEnd` les ajoute à la fin.
Si la chaîne est déjà assez longue, elle est retournée inchangée :
```kotlin
println("7".padStart(3, '0'))   // 007
println("hi".padEnd(5, '.'))    // hi...
println("1234".padStart(2, '0')) // 1234
```
Les nombres ne sont pas des chaînes : appelez d'abord `toString()`, comme dans `42.toString().padStart(4, '0')`.

# --instructions--

Écrivez une fonction `padNumber(n: Int, width: Int): String` qui retourne `n` sous forme de chaîne, complétée par `'0'` au début jusqu'à atteindre `width` caractères.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`padNumber(7, 3)` doit retourner `"007"`

```kotlin
    tryCatch(padNumber(7, 3) == "007")
```

`padNumber(42, 5)` doit retourner `"00042"`

```kotlin
    tryCatch(padNumber(42, 5) == "00042")
```

`padNumber(1234, 2)` doit retourner `"1234"`

```kotlin
    tryCatch(padNumber(1234, 2) == "1234")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun padNumber(n: Int, width: Int): String {
    return n.toString().padStart(width, '0')
}
```
