---
language: kotlin
exerciseType: 2
---

# --description--

`split` découpe une chaîne en une `List` de morceaux autour d'un séparateur, tandis que `joinToString` fait l'inverse : elle assemble les éléments d'une collection en une seule chaîne avec le séparateur de votre choix :
```kotlin
val csv = "a,b,c"
val parts = csv.split(",")
println(parts)                  // [a, b, c]
println(parts.joinToString("/")) // a/b/c
```

# --instructions--

Complétez le code pour découper `csv` sur la virgule et afficher les couleurs séparées par `" - "`

# --seed--

```kotlin
val csv = "red,green,blue"
val colors = csv.[/](",")
println(colors.[/](" - "))
```

# --answers--

- split
- joinToString
- join
- separate
- substring

# --solutions--

```kotlin
val csv = "red,green,blue"
val colors = csv.split(",")
println(colors.joinToString(" - "))
```

# --output--

red - green - blue
