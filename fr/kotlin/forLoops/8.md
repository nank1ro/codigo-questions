---
language: kotlin
exerciseType: 2
---

# --description--

En Kotlin, nous pouvons utiliser le `for-in` également pour les collections itérables en appelant la closure donnée sur chaque élément :
```kotlin
// this is a list, we'll see about that soon
val numbers = listOf(2, 4, 6, 8, 10)
for (num in numbers) {
    println(num)
}
// prints (2, 4, 6, 8, 10)
```

# --instructions--

Complétez le code afin d'afficher les nombres suivants `(3, 6, 9, 12, 15)` en utilisant une boucle `for-in`

# --seed--

```kotlin
val numbers = listOf(3, 6, 9, 12, 15)
[/] (num[/]numbers) {
    println([/])
}
```

# --answers--

- for
-  in 
- numbers
- forEach
- num
- it

# --solutions--

```kotlin
val numbers = listOf(3, 6, 9, 12, 15)
for (num in numbers) {
    println(num)
}
```

# --output--

3
6
9
12
15
