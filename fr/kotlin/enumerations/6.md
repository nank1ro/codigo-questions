---
language: kotlin
exerciseType: 2
---

# --description--

Chaque enum expose une propriété `entries` : une liste de toutes ses entrées dans l'ordre de déclaration. Elle est pratique pour itérer :
```kotlin
for (direction in Direction.entries) {
    println(direction.name)
}
// NORTH
// SOUTH
// EAST
// WEST
```
Étant une liste, `entries` prend aussi en charge `size` et l'indexation, par exemple `Direction.entries[0]` vaut `NORTH`.

Le code plus ancien utilise à la place la fonction `values()`, qui retourne un tableau ; `entries` est le choix recommandé depuis Kotlin 1.9.

# --instructions--

Complétez la boucle pour qu'elle affiche l'ordinal et le nom de chaque entrée de `Size`, séparés par un espace

# --seed--

```kotlin
enum class Size {
    SMALL, MEDIUM, LARGE
}

fun main() {
    for (size in Size.[/]) {
        println("${size.[/]} ${size.[/]}")
    }
}
```

# --answers--

- entries
- cases
- list
- ordinal
- name
- index
- value

# --solutions--

```kotlin
enum class Size {
    SMALL, MEDIUM, LARGE
}

fun main() {
    for (size in Size.entries) {
        println("${size.ordinal} ${size.name}")
    }
}
```

# --output--

0 SMALL
1 MEDIUM
2 LARGE
