---
language: kotlin
exerciseType: 1
---

# --description--

Pour passer d'un `String` à une entrée, utilisez la fonction `valueOf`. Elle recherche l'entrée dont le `name` correspond exactement :
```kotlin
val direction = Direction.valueOf("EAST")
println(direction == Direction.EAST) // true
```
La correspondance est sensible à la casse : `Direction.valueOf("east")` lève une `IllegalArgumentException` car aucune entrée ne porte ce nom.

# --instructions--

Déclarez un enum `Status` avec les entrées `ACTIVE` et `INACTIVE`. Écrivez ensuite une fonction `statusOf(text: String)` qui retourne le `Status` correspondant à `text`, quelle que soit la casse (ainsi `"active"`, `"Active"` et `"ACTIVE"` donnent tous `Status.ACTIVE`).

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`statusOf("ACTIVE")` doit retourner `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("ACTIVE") == Status.ACTIVE)
```

`statusOf("inactive")` doit retourner `Status.INACTIVE`

```kotlin
    tryCatch(statusOf("inactive") == Status.INACTIVE)
```

`statusOf("Active")` doit retourner `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("Active") == Status.ACTIVE)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
enum class Status {
    ACTIVE, INACTIVE
}

fun statusOf(text: String): Status {
    return Status.valueOf(text.uppercase())
}
```
