---
language: kotlin
exerciseType: 4
---

# --instructions--

Triez les éléments pour déclarer l'enum `Planet` et afficher, pour chaque entrée, son nom et son nombre de lunes. Déclarez d'abord l'enum, puis `main`. Le programme doit afficher :

```
MERCURY: 0
EARTH: 1
MARS: 2
```

# --answers--

```kotlin
enum class Planet(val moons: Int) {
```

```kotlin
    MERCURY(0),
```

```kotlin
    EARTH(1),
```

```kotlin
    MARS(2)
```

```kotlin
}
```

```kotlin
fun main() {
    for (planet in Planet.entries) {
```

```kotlin
        println("${planet.name}: ${planet.moons}")
```

```kotlin
    }
}
```

# --solutions--

```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
fun main() {
    for (planet in Planet.entries) {
        println("${planet.name}: ${planet.moons}")
    }
}
```

# --output--

MERCURY: 0
EARTH: 1
MARS: 2
