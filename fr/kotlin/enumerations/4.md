---
language: kotlin
exerciseType: 2
---

# --description--

Un enum peut avoir un **constructeur**, comme une classe classique. Chaque entrée passe alors ses propres arguments, et les valeurs sont stockées dans des propriétés :
```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
println(Planet.MARS.moons) // 2
```
Les propriétés du constructeur sont généralement déclarées avec `val`, car les données d'une entrée ne sont pas censées changer.

# --instructions--

Complétez le code pour que chaque `Coin` stocke sa valeur en centimes, puis affichez la valeur d'un `PENNY` (qui vaut `1` centime)

# --seed--

```kotlin
enum class Coin([/] cents: Int) {
    PENNY([/]),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.[/])
}
```

# --answers--

- val
- fun
- const
- 1
- "1"
- cents
- name
- ordinal

# --solutions--

```kotlin
enum class Coin(val cents: Int) {
    PENNY(1),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.cents)
}
```

# --output--

1
