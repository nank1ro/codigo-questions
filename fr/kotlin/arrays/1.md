---
language: kotlin
exerciseType: 2
---

# --description--

Un **tableau** stocke un nombre fixe de valeurs du même type sous un seul nom de variable.
Vous en créez un avec `arrayOf`, lisez un élément avec des crochets et un **index** commençant à `0`, et obtenez le nombre d'éléments avec `size` :
```kotlin
val colors = arrayOf("red", "green", "blue")
println(colors[0])   // red
println(colors.size) // 3
```
Le dernier élément se trouve à l'index `size - 1`.

# --instructions--

Complétez le code afin de créer le tableau avec `arrayOf`, d'afficher son nombre d'éléments, puis son dernier élément

# --seed--

```kotlin
val numbers = [/](3, 5, 7)
println(numbers.[/])
println(numbers[[/]])
```

# --answers--

- arrayOf
- Array
- size
- length
- count
- 2
- 3

# --solutions--

```kotlin
val numbers = arrayOf(3, 5, 7)
println(numbers.size)
println(numbers[2])
```

# --output--

3
7
