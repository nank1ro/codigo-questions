---
language: kotlin
exerciseType: 2
---

# --description--

En Kotlin, nous pouvons boucler à travers une liste d'une manière très simple en utilisant les mots-clés `for..in` :
```kotlin
val numbers = listOf(1, 2, 3)
for (num in numbers) {
    println(num)
}
// affiche 1, 2, 3
```
Un nom de variable suit le mot-clé `for`, il sera assigné à la valeur de chaque élément de la liste à tour de rôle.

# --instructions--

Affiche chaque élément de la liste en le multipliant par `2`

# --seed--

```kotlin
val numbers = listOf(2, 4, 6)
[/]([/][/][/]) {
    println([/][/][/])
}
```

# --answers--

- for 
- num
-  in 
- numbers
- num
-  * 
-  ** 
- 2

# --solutions--

```kotlin
val numbers = listOf(2, 4, 6)
for (num in numbers) {
    println(num * 2)
}
```

# --output--

4
8
12
