---
language: kotlin
exerciseType: 2
---

# --description--

Comme nous l'avons vu dans l'exemple précédent, nous pouvons ajouter des éléments un par un en utilisant la fonction `add`.
Mais si nous devons ajouter tous les éléments d'une autre liste à la fois, nous pouvons simplement utiliser la fonction `addAll`, ou le raccourci `+=` :
```kotlin
val letters = mutableListOf("a", "b")
val newLetters = listOf("c", "d", "e")
letters.addAll(newLetters)
println(letters)
// affiche [a, b, c, d, e]
```

# --instructions--

Complétez le code pour ajouter trois éléments de plus à la liste `numbers`, dans l'ordre suivant `5`, puis `12` et finalement `9`

# --seed--

```kotlin
val numbers = mutableListOf(4, 2, 24)
val newNumbers = listOf([/], [/], [/])
numbers.[/](newNumbers)
println(numbers)
```

# --answers--

- append
- appendAll
- add
- 5
- 12
- 9
- numbers
- newNumbers
- addAll

# --solutions--

```kotlin
val numbers = mutableListOf(4, 2, 24)
val newNumbers = listOf(5, 12, 9)
numbers.addAll(newNumbers)
println(numbers)
```

# --output--

[4, 2, 24, 5, 12, 9]
