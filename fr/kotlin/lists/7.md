---
language: kotlin
exerciseType: 2
---

# --description--

Une liste mutable n'a pas besoin d'avoir une longueur fixe.
Vous pouvez ajouter des éléments à la fin d'une liste à tout moment !
Pour ajouter un élément à une liste mutable, nous utilisons la fonction `add` ou le raccourci `+=` :
```kotlin
val letters = mutableListOf("a", "b")
letters.add("c")
println(letters)
// affiche [a, b, c]
```

# --instructions--

Complétez le code pour ajouter trois éléments de plus à la liste `numbers`, dans l'ordre suivant `6`, puis `8` et finalement `9`

# --seed--

```kotlin
val numbers = mutableListOf(1, 3, 9)
numbers.[/]([/])
numbers.add([/])
[/].add(9)
println(numbers)
```

# --answers--

- append
- add
- 6
- 8
- numbers

# --solutions--

```kotlin
val numbers = mutableListOf(1, 3, 9)
numbers.add(6)
numbers.add(8)
numbers.add(9)
println(numbers)
```

# --output--

[1, 3, 9, 6, 8, 9]
