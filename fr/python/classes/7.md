---
language: python
exerciseType: 2
---

# --description--

Quand une classe a ses propres fonctions, ces fonctions sont appelées __méthodes__. Vous avez déjà vu une telle méthode: `__init__()`.
Mais vous pouvez aussi définir vos propres méthodes!

# --instructions--

Ajoutez une méthode, `description`, à votre classe `Animal`.
En utilisant deux déclarations `print` séparées, elle doit imprimer le `gender` et les `legs` de l'animal sur lequel elle est appelée.
Ensuite, appelez la méthode `description`.

# --seed--

```python
class Animal:
    def __init__(self, gender, legs):
        self.gender = gender
        self.legs = legs
    def [/]([/]):
        [/]([/][/][/])
        [/]([/][/][/])

dog = Animal("female", 5)
dog.description()
```

# --answers--

- description
- self
- Animal
- print
- print
- self
- self
- gender
- legs
- .
- .

# --solutions--

```python
class Animal:
    def __init__(self, gender, legs):
        self.gender = gender
        self.legs = legs
    def description(self):
        print(self.gender)
        print(self.legs)

dog = Animal("female", 5)
dog.description()
```

# --output--

female
5
