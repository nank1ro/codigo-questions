---
language: python
exerciseType: 1
---

# --description--

Les variables de chaîne peuvent être déclarées soit en utilisant des guillemets simples, soit des guillemets doubles :
```python
>>> x = "May"
>>> x = 'May'
```
Both are the same thing.

# --instructions--

Déclarez une variable nommée `month` avec la valeur de `june`.

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`month` must be equal to `"june"`.

```python
    def test_1(self):
        self.assertEqual(month, "june", "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
month = "june"
```
