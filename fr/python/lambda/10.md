---
language: python
exerciseType: 2
---

# --description--

`filter()` ne conserve que les éléments pour lesquels la fonction retourne `True` :
```python
nums = [5, 12, 8, 20]
big = list(filter(lambda n: n > 10, nums))
print(big)  # [12, 20]
```

# --instructions--

Complétez le code pour ne conserver que les nombres pairs, de sorte que le programme affiche `[2, 4]`

# --seed--

```python
nums = [1, 2, 3, 4]
evens = list([/](lambda n: n % 2 [/] 0, nums))
print(evens)
```

# --answers--

- filter
- map
- sorted
- ==
- !=

# --solutions--

```python
nums = [1, 2, 3, 4]
evens = list(filter(lambda n: n % 2 == 0, nums))
print(evens)
```

# --output--

[2, 4]
