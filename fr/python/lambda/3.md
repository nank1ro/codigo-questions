---
language: python
exerciseType: 2
---

# --description--

Une lambda peut prendre **plus d'un paramètre**.
Séparez-les par des virgules, exactement comme dans une fonction `def` :
```python
power = lambda base, exp: base ** exp
print(power(2, 3))  # 8
```

# --instructions--

Complétez la lambda pour qu'elle prenne les paramètres `a` et `b` et retourne leur somme

# --seed--

```python
add = lambda a[/] b[/] a + b
print(add(3, 4))
```

# --answers--

- ,
- :
- ;
- ->

# --solutions--

```python
add = lambda a, b: a + b
print(add(3, 4))
```

# --output--

7
