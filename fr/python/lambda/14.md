---
language: python
exerciseType: 1
---

# --description--

**Quand devriez-vous préférer `def` ?**
Une lambda est idéale pour une fonction courte et jetable passée en argument.
Si la logique a besoin d'un nom, de plusieurs lignes, d'une docstring, ou est réutilisée à de nombreux endroits, une fonction `def` est plus claire.
Une dernière astuce : `sorted()` accepte aussi `reverse=True` pour obtenir d'abord les plus grandes valeurs :
```python
print(sorted([3, 1, 2], reverse=True))  # [3, 2, 1]
```

# --instructions--

Stockez dans `ranking` la liste `students` triée de la `grade` la plus haute à la plus basse, puis stockez dans `names` la liste des noms de `ranking`, en utilisant `sorted()` et `map()` avec des lambdas

# --seed--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser une expression `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser `map()`

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ranking` doit commencer par l'étudiant `Cy`

```python
    def test_1(self):
        self.assertEqual(ranking[0]["name"], "Cy", "--err-t1--")
```

`names` doit être égal à `['Cy', 'Ana', 'Bo']`

```python
    def test_2(self):
        self.assertEqual(names, ["Cy", "Ana", "Bo"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
ranking = sorted(students, key=lambda s: s["grade"], reverse=True)
names = list(map(lambda s: s["name"], ranking))
```
