---
language: python
exerciseType: 1
---

# --description--

Les lambdas brillent vraiment en tant qu'**arguments d'autres fonctions**.
`sorted()` accepte un paramètre `key` : une fonction appelée sur chaque élément, et son résultat décide de l'ordre.
Une lambda est parfaitement adaptée :
```python
words = ["banana", "kiwi", "apple"]
print(sorted(words, key=lambda w: len(w)))
# ['kiwi', 'apple', 'banana']
```

# --instructions--

Stockez dans la variable `by_length` la liste `words` triée du mot le plus court au plus long, en utilisant `sorted()` avec une lambda comme `key`

# --seed--

```python
words = ["elephant", "cat", "tiger", "ox"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser une expression `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`by_length` doit être égal à `['ox', 'cat', 'tiger', 'elephant']`

```python
    def test_1(self):
        self.assertEqual(by_length, ["ox", "cat", "tiger", "elephant"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
words = ["elephant", "cat", "tiger", "ox"]
by_length = sorted(words, key=lambda w: len(w))
```
