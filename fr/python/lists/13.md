---
language: python
exerciseType: 2
---

# --description--

De même, nous pouvons convertir une liste en tuple

# --instructions--

Utilisez la fonction `tuple()` pour convertir la liste en tuple

# --seed--

```python
list1 = ["dog", "bear", "cat"]
tuple1 = [/]
print(tuple1)
```

# --answers--

- tuple[list1]
- tuple(list1)

# --solutions--

```python
list1 = ["dog", "bear", "cat"]
tuple1 = tuple(list1)
print(tuple1)
```

# --output--

('dog', 'bear', 'cat')
