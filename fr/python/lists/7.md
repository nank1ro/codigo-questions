---
language: python
exerciseType: 2
---

# --description--

Vous pouvez découper une chaîne exactement comme une liste ! En fait, vous pouvez penser aux chaînes comme des listes de caractères : chaque caractère est un élément séquentiel dans la liste, à partir de l'indice `0`.
```python
list_name[:2]
# Grabs the first two items
list_name[3:]
# Grabs the fourth through last items
```
Si votre tranche de liste inclut le tout premier ou le dernier élément d'une liste (ou d'une chaîne), l'indice de cet élément ne doit pas être inclus.

# --instructions--

- Assignez à `first_name` une tranche de `full_name` de l'indice `0` jusqu'à *mais sans inclure* l'indice `4`.
- Assignez à `last_name` une tranche de `full_name` de l'indice `4` jusqu'à la fin de la chaîne

# --seed--

```python
full_name = "Sam Smith"
first_name = [/][/]
last_name = [/][/]
print(first_name)
print(last_name)
```

# --answers--

- full_name
- [:4]
- [:3]
- full_name
- [4:]
- [4:2]

# --solutions--

```python
full_name = "Sam Smith"
first_name = full_name[:3]
last_name = full_name[4:]
print(first_name)
print(last_name)
```

# --output--

Sam
Smith
