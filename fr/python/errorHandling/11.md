---
language: python
exerciseType: 1
---

# --description--

Un bloc `finally` s'exécute **quoi qu'il arrive** : après un bloc `try` sans incident, après un bloc `except`, même pendant qu'une exception que personne n'a interceptée se propage, et même quand le bloc `try` ou `except` exécute un `return` :
```python
def read(text):
    try:
        return int(text)
    except ValueError:
        return 0
    finally:
        print("done")
```
Les deux chemins affichent `done` avant que la valeur ne quitte la fonction. Cette garantie est la raison d'être de `finally` : fermer un fichier, libérer un verrou, restaurer un paramètre. La forme complète est `try` / `except` / `else` / `finally` ; un `try` a besoin d'au moins un `except` ou d'un `finally`, et `else` ne fonctionne qu'accompagné d'un `except`.

# --instructions--

Écrivez une fonction `record(text)` qui part d'une liste vide, y ajoute `int(text)`, ajoute la chaîne `"bad"` à la place quand `int(text)` lève une `ValueError`, ajoute toujours `"done"` dans un bloc `finally`, et renvoie la liste

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`record("5")` doit renvoyer `[5, "done"]`

```python
    def test_1(self):
        self.assertEqual(record("5"), [5, "done"], "--err-t1--")
```

`record("x")` doit renvoyer `["bad", "done"]`

```python
    def test_2(self):
        self.assertEqual(record("x"), ["bad", "done"], "--err-t2--")
```

chaque appel doit partir d'une liste vierge

```python
    def test_3(self):
        record("1")
        self.assertEqual(record("2"), [2, "done"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def record(text):
    log = []
    try:
        log.append(int(text))
    except ValueError:
        log.append("bad")
    finally:
        log.append("done")
    return log
```
