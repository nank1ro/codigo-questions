---
language: python
exerciseType: 1
---

# --description--

Quand plusieurs défaillances méritent la **même** récupération, les lister sous forme de tuple dans une seule clause est plus court que de répéter le bloc :
```python
try:
    value = int(text) / divisor
except (ValueError, ZeroDivisionError):
    value = 0
```
Les parenthèses sont obligatoires : `except ValueError, ZeroDivisionError:` est une erreur de syntaxe en Python 3. Un tuple reste une liste explicite de types.

# --instructions--

Écrivez une fonction `safe_ratio(a, b)` qui renvoie `a / b`, et renvoie `None` quand la division lève une `ZeroDivisionError` ou une `TypeError`, en utilisant une seule clause `except` avec un tuple des deux types

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_ratio(9, 3)` doit renvoyer `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_ratio(9, 3), 3.0, "--err-t1--")
```

`safe_ratio(1, 0)` doit renvoyer `None`

```python
    def test_2(self):
        self.assertIsNone(safe_ratio(1, 0), "--err-t2--")
```

`safe_ratio(1, "x")` doit renvoyer `None`

```python
    def test_3(self):
        self.assertIsNone(safe_ratio(1, "x"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_ratio(a, b):
    try:
        return a / b
    except (ZeroDivisionError, TypeError):
        return None
```
