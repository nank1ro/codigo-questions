---
language: python
exerciseType: 1
---

# --description--

Le bloc `try` s'arrête à la **première** instruction qui lève une exception ; les lignes qui suivent sont ignorées et le contrôle passe au bloc `except`. Rien dans le bloc `try` n'est annulé, gardez-le donc aussi court que possible :
```python
def half(n):
    try:
        return 10 / n
    except ZeroDivisionError:
        return "undefined"

print(half(2))  # 5.0
print(half(0))  # undefined
```
Un `return` à l'intérieur d'un `except` fonctionne comme n'importe quel autre `return`, ce qui fait de `try`/`except` un moyen naturel de renvoyer une valeur de secours au lieu de planter.

# --instructions--

Écrivez une fonction `safe_divide(a, b)` qui renvoie `a / b`, ou la chaîne `"undefined"` quand `b` vaut `0`, en interceptant `ZeroDivisionError`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_divide(9, 3)` doit renvoyer `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_divide(9, 3), 3.0, "--err-t1--")
```

`safe_divide(1, 0)` doit renvoyer `"undefined"`

```python
    def test_2(self):
        self.assertEqual(safe_divide(1, 0), "undefined", "--err-t2--")
```

`safe_divide(-5, 2)` doit renvoyer `-2.5`

```python
    def test_3(self):
        self.assertEqual(safe_divide(-5, 2), -2.5, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "undefined"
```
