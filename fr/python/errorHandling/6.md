---
language: python
exerciseType: 2
---

# --description--

Un bloc `try` peut être suivi de **plusieurs** clauses `except`, chacune gérant une défaillance différente avec une récupération différente. Python compare l'exception levée à ces clauses de haut en bas et exécute la **première** qui correspond ; les autres sont ignorées :
```python
try:
    value = 100 / int(text)
except ValueError:
    print("not a number")
except ZeroDivisionError:
    print("cannot divide by zero")
```
Comme la première correspondance gagne, l'ordre compte quand les types sont liés : une clause pour un type général placée au-dessus d'une clause pour un type plus spécifique gagnerait toujours, rendant la clause spécifique inaccessible.

# --instructions--

Complétez le code pour qu'un texte illisible et un diviseur zéro soient signalés séparément, de sorte à afficher `2.0 not a number cannot divide by zero`

# --seed--

```python
def describe(text):
    try:
        return 10 / int(text)
    except [/]:
        return "not a number"
    except [/]:
        return "cannot divide by zero"

print(describe("5"), describe("abc"), describe("0"))
```

# --answers--

- ValueError
- ZeroDivisionError
- TypeError
- KeyError

# --solutions--

```python
def describe(text):
    try:
        return 10 / int(text)
    except ValueError:
        return "not a number"
    except ZeroDivisionError:
        return "cannot divide by zero"

print(describe("5"), describe("abc"), describe("0"))
```

# --output--

2.0 not a number cannot divide by zero
