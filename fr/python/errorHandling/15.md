---
language: python
exerciseType: 4
---

# --description--

Parfois, un gestionnaire doit réagir à une défaillance sans en prendre la responsabilité : l'enregistrer, la compter, fermer quelque chose — puis laisser l'appelant s'en occuper. Un `raise` seul à l'intérieur d'un bloc `except` **relance** l'exception en cours de traitement, avec son type, son message et sa traceback d'origine intacts :
```python
try:
    value = int(text)
except ValueError:
    print("could not read the value")
    raise
```
Écrire `raise ValueError(...)` à la place construirait une nouvelle exception et jetterait la traceback d'origine, qui est exactement l'information dont vous avez besoin pour déboguer.

# --instructions--

Ordonnez les éléments pour construire un programme qui définit `load`, affiche le résultat du chargement de `"5"`, puis l'appelle avec `"abc"` dans un bloc `try` qui signale la défaillance, produisant un message de `load` puis un de l'appelant

# --answers--

```python
def load(text):
```

```python
    try:
        return int(text)
```

```python
    except ValueError:
        print(f"could not read {text}")
        raise
```

```python
print(load("5"))
```

```python
try:
    load("abc")
except ValueError:
    print("handled by the caller")
```

# --solutions--

```python
def load(text):
    try:
        return int(text)
    except ValueError:
        print(f"could not read {text}")
        raise
print(load("5"))
try:
    load("abc")
except ValueError:
    print("handled by the caller")
```

# --output--

5
could not read abc
handled by the caller
