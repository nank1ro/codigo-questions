---
language: python
exerciseType: 1
---

# --description--

Une exception est un objet, et `as` la lie à un nom pour que le gestionnaire puisse l'examiner :
```python
try:
    int("abc")
except ValueError as e:
    print(e)                   # invalid literal for int() with base 10: 'abc'
    print(type(e).__name__)    # ValueError
```
`str(e)` — ce qu'utilisent `print(e)` et un emplacement de f-string — donne le message avec lequel l'exception a été construite, et `type(e).__name__` donne le nom de la classe sous forme de texte. Le nom lié par `as` n'existe qu'à l'intérieur du bloc `except` ; Python le supprime quand le bloc se termine.

# --instructions--

Écrivez une fonction `error_label(text)` qui renvoie `int(text)`, et quand cela lève une `ValueError` renvoie une chaîne composée du nom de la classe de l'exception, de `": "` et du message de l'exception, par exemple `"ValueError: ..."`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`error_label("8")` doit renvoyer `8`

```python
    def test_1(self):
        self.assertEqual(error_label("8"), 8, "--err-t1--")
```

`error_label("abc")` doit commencer par le nom de la classe

```python
    def test_2(self):
        self.assertTrue(error_label("abc").startswith("ValueError: "), "--err-t2--")
```

`error_label("abc")` doit aussi contenir le message, qui mentionne `abc`

```python
    def test_3(self):
        self.assertIn("abc", error_label("abc")[12:], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def error_label(text):
    try:
        return int(text)
    except ValueError as e:
        return f"{type(e).__name__}: {e}"
```
