---
language: python
exerciseType: 1
---

# --description--

Quand une ligne ne suffit pas, une docstring prend une disposition fixe, décrite dans la **PEP 257** : un résumé d'une ligne, une ligne vide, puis les détails, et le `"""` de fermeture sur une ligne à part.
```python
def to_seconds(minutes):
    """Returns the number of seconds in the given minutes.

    minutes is a whole number and is never negative.
    """
    return minutes * 60
```
La ligne vide compte : les outils affichent la première ligne seule, comme une courte description, et gardent le reste pour celui qui veut en lire plus.

# --instructions--

Donnez à `area` une docstring composée d'un résumé d'une ligne, d'une ligne vide, d'une ligne de détail, et du `"""` de fermeture sur sa propre ligne

# --seed--

```python
def area(width, height):
    return width * height
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`area` doit avoir une docstring

```python
    def test_1(self):
        self.assertIsNotNone(area.__doc__, "--err-t1--")
```

La docstring doit commencer par une ligne de résumé, suivie d'une ligne vide puis d'un autre texte

```python
    def test_2(self):
        lines = area.__doc__.split("\n")
        self.assertTrue(len(lines) >= 3, "--err-t2--")
        self.assertNotEqual(lines[0].strip(), "", "--err-t2--")
        self.assertEqual(lines[1].strip(), "", "--err-t2--")
        self.assertNotEqual(lines[2].strip(), "", "--err-t2--")
        self.assertEqual(lines[-1].strip(), "", "--err-t2--")
```

`area(3, 4)` doit renvoyer `12` et `area(0, 5)` doit renvoyer `0`

```python
    def test_3(self):
        self.assertEqual(area(3, 4), 12, "--err-t3--")
        self.assertEqual(area(0, 5), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def area(width, height):
    """Returns the area of a rectangle.

    Both sides are whole numbers of centimetres.
    """
    return width * height
```
