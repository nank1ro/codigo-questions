---
language: python
exerciseType: 1
---

# --description--

Certains commentaires suivent une convention que les éditeurs comprennent. Les **marqueurs** les plus courants sont :
- `# TODO: ...` signale quelque chose qui doit encore être écrit
- `# FIXME: ...` signale du code que l'on sait faux et qui doit être corrigé

```python
limit = 10
# TODO: read the limit from the settings
```
Pour Python, ce sont des commentaires ordinaires ; les éditeurs les rassemblent dans un panneau dédié, donc le travail en attente est facile à trouver. Un `TODO` se trouve généralement à côté d'un espace réservé qui garde le programme en marche jusqu'à ce que le vrai code soit écrit.

Quand vous terminez le travail, remplacez l'espace réservé et supprimez le marqueur dans le même changement, pour que le commentaire ne mente jamais sur l'état du code.

# --instructions--

Terminez le travail décrit par le commentaire `TODO` dans `is_even`, puis retirez le commentaire puisque le travail est fait

# --seed--

```python
def is_even(n):
    # TODO: return True when n is even, False otherwise
    return False
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Le commentaire `TODO` doit être retiré

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`is_even(4)` doit renvoyer `True`

```python
    def test_1(self):
        self.assertEqual(is_even(4), True, "--err-t1--")
```

`is_even(7)` doit renvoyer `False`

```python
    def test_2(self):
        self.assertEqual(is_even(7), False, "--err-t2--")
```

`is_even(0)` doit renvoyer `True`

```python
    def test_3(self):
        self.assertEqual(is_even(0), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_even(n):
    return n % 2 == 0
```
