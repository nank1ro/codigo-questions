---
language: python
exerciseType: 2
---

# --description--

Une docstring n'est pas jetée : Python la stocke dans l'attribut `__doc__` de la fonction, pour que le programme puisse lire sa propre documentation pendant qu'il s'exécute :
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"

print(greet.__doc__)  # Renvoie la salutation pour name.
```
Quand une fonction n'a pas de docstring, `__doc__` vaut `None`. C'est ce que `help(greet)` affiche, et ce qu'un éditeur montre quand vous survolez le nom.

# --instructions--

Complétez le code pour que `greet` ait une docstring et que le programme l'affiche

# --seed--

```python
def greet(name):
    [/]Returns a greeting for name.[/]
    return "Hi, " + name + "!"

print(greet.[/])
```

# --answers--

- """
- """
- __doc__
- __name__
- #

# --solutions--

```python
def greet(name):
    """Returns a greeting for name."""
    return "Hi, " + name + "!"

print(greet.__doc__)
```

# --output--

Returns a greeting for name.
