---
language: python
exerciseType: 1
---

# --description--

Python ne cherche le `#` que dans le code, jamais à l'intérieur d'une **chaîne**. Entre guillemets, `#` est un caractère ordinaire :
```python
print("black is #000000")  # a hex colour
```
Le premier `#` fait partie du texte, le second commence un vrai commentaire. Il en va de même pour `"""` à l'intérieur d'un commentaire `#` : là, ce ne sont que trois caractères guillemets, et cela ne commence rien.

# --instructions--

Écrivez une fonction `label(name)` qui renvoie `name` avec un `#` devant, ainsi `label("python")` renvoie `"#python"`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`label("python")` doit renvoyer `"#python"`

```python
    def test_1(self):
        self.assertEqual(label("python"), "#python", "--err-t1--")
```

`label("ai")` doit renvoyer `"#ai"`

```python
    def test_2(self):
        self.assertEqual(label("ai"), "#ai", "--err-t2--")
```

`label("")` doit renvoyer `"#"`

```python
    def test_3(self):
        self.assertEqual(label(""), "#", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def label(name):
    """Returns name with a # in front of it."""
    return "#" + name
```
