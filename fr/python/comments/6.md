---
language: python
exerciseType: 1
---

# --description--

Quand une chaîne est la **première instruction** à l'intérieur d'une fonction, Python la traite comme la documentation de cette fonction. Elle s'appelle une **docstring** :
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"
```
Par convention, une docstring s'écrit entre guillemets doubles triples, `"""`, même quand elle tient sur une ligne, pour qu'elle puisse grandir plus tard sans changer les guillemets.

Écrivez le résumé à la troisième personne, comme si vous décriviez la fonction : « Returns... », « Adds... », « Checks... ». La docstring doit venir avant toute autre instruction dans le corps, sinon ce n'est qu'une chaîne ordinaire.

# --instructions--

Écrivez une fonction `double(n)` qui renvoie `n` multiplié par deux, et donnez-lui une docstring d'une ligne

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`double` doit avoir une docstring comme première instruction

```python
    def test_1(self):
        self.assertIsNotNone(double.__doc__, "--err-t1--")
```

`double(21)` doit renvoyer `42`

```python
    def test_2(self):
        self.assertEqual(double(21), 42, "--err-t2--")
```

`double(-3)` doit renvoyer `-6` et `double(0)` doit renvoyer `0`

```python
    def test_3(self):
        self.assertEqual(double(-3), -6, "--err-t3--")
        self.assertEqual(double(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def double(n):
    """Returns n multiplied by two."""
    return n * 2
```
