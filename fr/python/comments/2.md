---
language: python
exerciseType: 1
---

# --description--

Un commentaire n'a pas besoin de sa propre ligne : il peut suivre le code sur la même ligne. C'est un **commentaire en ligne**, et c'est un bon endroit pour une courte note sur cette instruction précise :
```python
retries = 3  # give up after three attempts
```
Tout ce qui va de `#` jusqu'à la fin de la ligne est ignoré, tandis que le code avant lui s'exécute normalement.

Le guide de style de Python, **PEP 8**, demande un peu d'espacement ici : au moins **deux espaces** entre le code et le `#`, et **une espace** après le `#`. Un commentaire sur sa propre ligne n'a besoin que de l'espace après le `#`.

# --instructions--

Ajoutez un commentaire en ligne après `seats = 4` qui explique la valeur, par exemple `# seats per table`. Laissez deux espaces avant le `#`

# --seed--

```python
seats = 4
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

La ligne `seats = 4` doit se terminer par un commentaire `#`, avec deux espaces avant le `#` et une espace après

```json
{
  "regex": "seats = 4[ \\t]{2,}#[ \\t]\\S",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`seats` doit toujours être égal à `4`

```python
    def test_1(self):
        self.assertEqual(seats, 4, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
seats = 4  # seats per table
```
