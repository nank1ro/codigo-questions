---
language: python
exerciseType: 1
---

# --description--

Un objet de correspondance transporte plus que le texte qui a correspondu. Outre `.group()`, il offre la position de la correspondance dans la chaîne d'origine :
```python
import re

match = re.search(r"\d+", "order 42 shipped")
print(match.group())  # 42
print(match.start())  # 6
print(match.end())    # 8
print(match.span())   # (6, 8)
```
`.start()` est l'index du premier caractère correspondant, `.end()` l'index juste après le dernier et `.span()` renvoie les deux sous forme de tuple. Cela signifie que `text[match.start():match.end()]` est toujours égal à `match.group()`.

Comme `re.search` peut renvoyer `None`, lire `.group()` immédiatement lève une `AttributeError` quand rien ne correspond ; vérifiez d'abord le résultat.

# --instructions--

Écrivez une fonction `first_number(text)` qui renvoie un tuple avec la première suite de chiffres, son index de début et son index de fin, ou `None` quand le texte ne contient aucun chiffre

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_number("order 42 shipped")` doit renvoyer `("42", 6, 8)`

```python
    def test_1(self):
        self.assertEqual(first_number("order 42 shipped"), ("42", 6, 8), "--err-t1--")
```

La correspondance doit commencer à l'index `0` quand le texte commence par des chiffres

```python
    def test_2(self):
        self.assertEqual(first_number("7up"), ("7", 0, 1), "--err-t2--")
        self.assertEqual(first_number("100 items"), ("100", 0, 3), "--err-t2--")
```

`first_number` doit renvoyer `None` quand il n'y a pas de chiffre

```python
    def test_3(self):
        self.assertIsNone(first_number("no digits here"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def first_number(text):
    match = re.search(r"\d+", text)
    if match is None:
        return None
    return (match.group(), match.start(), match.end())
```
