---
language: python
exerciseType: 1
---

# --description--

`re.search` parcourt tout le texte, mais `re.match` n'essaie le motif qu'au **tout début** :
```python
import re

print(re.search(r"\d+", "order 42"))  # <re.Match object; span=(6, 8), match='42'>
print(re.match(r"\d+", "order 42"))   # None
print(re.match(r"\d+", "42 orders"))  # <re.Match object; span=(0, 2), match='42'>
```

Toutes deux renvoient `None` quand rien ne correspond, et un objet de correspondance est toujours truthy, donc la façon habituelle de demander « y a-t-il eu une correspondance ? » est un simple `if` :
```python
if re.search(r"\d", text):
    print("there is a digit")
```
Quand un véritable `True` ou `False` est nécessaire, comparez avec `is not None` ou enveloppez l'appel dans `bool(...)`.

# --instructions--

Écrivez une fonction `has_digit(text)` qui renvoie `True` quand le texte contient un chiffre n'importe où, et une fonction `starts_with_digit(text)` qui renvoie `True` seulement quand le texte commence par un chiffre

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`has_digit` doit trouver un chiffre n'importe où dans le texte

```python
    def test_1(self):
        self.assertEqual(has_digit("order 42"), True, "--err-t1--")
        self.assertEqual(has_digit("7up"), True, "--err-t1--")
```

`has_digit` doit renvoyer `False` quand il n'y a pas de chiffre

```python
    def test_2(self):
        self.assertEqual(has_digit("no numbers here"), False, "--err-t2--")
        self.assertEqual(has_digit(""), False, "--err-t2--")
```

`starts_with_digit` ne doit regarder que le début du texte

```python
    def test_3(self):
        self.assertEqual(starts_with_digit("42 orders"), True, "--err-t3--")
        self.assertEqual(starts_with_digit("order 42"), False, "--err-t3--")
        self.assertEqual(starts_with_digit(""), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def has_digit(text):
    return re.search(r"\d", text) is not None

def starts_with_digit(text):
    return re.match(r"\d", text) is not None
```
