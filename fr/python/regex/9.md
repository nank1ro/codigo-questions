---
language: python
exerciseType: 1
---

# --description--

Quand la position ou les groupes de chaque correspondance sont nécessaires, `re.finditer(pattern, text)` est le bon appel : il parcourt le texte et produit un **objet de correspondance** pour chaque correspondance, un à la fois :
```python
import re

for match in re.finditer(r"\d+", "a1 bb22"):
    print(match.group(), match.start())
# 1 1
# 22 5
```
`finditer` produit un itérateur, pas une liste, donc il peut être utilisé dans une boucle `for` ou dans une compréhension. Là où `findall` ne donne que le texte, `finditer` donne tout ce qu'un objet de correspondance sait.

# --instructions--

Écrivez une fonction `word_positions(text)` qui renvoie une liste de tuples, un pour chaque suite de lettres dans le texte, contenant le mot et l'index où il commence

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`word_positions("hi there")` doit renvoyer `[("hi", 0), ("there", 3)]`

```python
    def test_1(self):
        self.assertEqual(word_positions("hi there"), [("hi", 0), ("there", 3)], "--err-t1--")
```

Les chiffres et les symboles ne doivent pas faire partie d'un mot

```python
    def test_2(self):
        self.assertEqual(word_positions("a1b"), [("a", 0), ("b", 2)], "--err-t2--")
        self.assertEqual(word_positions("Ada, Bob"), [("Ada", 0), ("Bob", 5)], "--err-t2--")
```

Un texte sans lettres doit renvoyer une liste vide

```python
    def test_3(self):
        self.assertEqual(word_positions("123 456"), [], "--err-t3--")
        self.assertEqual(word_positions(""), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def word_positions(text):
    return [(match.group(), match.start()) for match in re.finditer(r"[a-zA-Z]+", text)]
```
