---
language: python
exerciseType: 3
---

# --description--

Il existe un troisième point d'entrée, `re.fullmatch`, qui ne réussit que lorsque le motif couvre le texte **entier**, du premier au dernier caractère. C'est l'outil tout indiqué pour la validation :
```python
import re

print(bool(re.fullmatch(r"\d+", "2026")))    # True
print(bool(re.fullmatch(r"\d+", "2026!")))   # False
```

Ainsi, les trois fonctions ne diffèrent que par l'endroit où le motif est autorisé à se trouver : `re.match` au début du texte, `re.search` n'importe où dans le texte et `re.fullmatch` sur la totalité du texte.

# --instructions--

Qu'affiche ce code ?
```python
import re

print(bool(re.match(r"\d+", "12abc")), bool(re.search(r"\d+", "12abc")), bool(re.fullmatch(r"\d+", "12abc")))
```

# --answers--

- `True True False`, car les chiffres sont au début et à l'intérieur du texte, mais ils ne couvrent pas tout le texte
- `True True True`, car les trois fonctions réussissent dès que le motif est trouvé quelque part
- `False True False`, car `re.match` exige que le motif couvre tout le texte
- `True False False`, car `re.search` ne regarde qu'après le premier caractère

# --solutions--

- `True True False`, car les chiffres sont au début et à l'intérieur du texte, mais ils ne couvrent pas tout le texte
