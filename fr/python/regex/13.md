---
language: python
exerciseType: 1
---

# --description--

`str.split` ne peut couper que sur un séparateur fixe. `re.split(pattern, text)` coupe sur tout ce que décrit le motif, ce dont l'entrée désordonnée a généralement besoin :
```python
import re

print("a, b;c".split(","))            # ['a', ' b;c']
print(re.split(r"[,;]", "a, b;c"))    # ['a', ' b', 'c']
print(re.split(r"[,;\s]+", "a, b;c")) # ['a', 'b', 'c']
```
Écrire le séparateur sous la forme `[,;\s]+` fait qu'une suite entière de virgules, points-virgules et espaces compte pour une seule coupe, au lieu de laisser des chaînes vides entre elles.

Un argument `maxsplit` s'arrête après un nombre donné de coupes, en laissant le reste du texte dans le dernier élément : `re.split(r"\s+", "a b c", maxsplit=1)` donne `['a', 'b c']`.

# --instructions--

Écrivez une fonction `split_fields(line)` qui renvoie la liste des champs de la ligne, en traitant toute suite de virgules, points-virgules et espaces comme un séparateur unique

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`split_fields` doit couper sur les virgules, les points-virgules et les espaces

```python
    def test_1(self):
        self.assertEqual(split_fields("a, b;c"), ["a", "b", "c"], "--err-t1--")
        self.assertEqual(split_fields("red green blue"), ["red", "green", "blue"], "--err-t1--")
```

Une suite de séparateurs doit produire une seule coupe

```python
    def test_2(self):
        self.assertEqual(split_fields("x ; ; y"), ["x", "y"], "--err-t2--")
        self.assertEqual(split_fields("1,,2"), ["1", "2"], "--err-t2--")
```

Une ligne sans séparateurs doit renvoyer un seul champ

```python
    def test_3(self):
        self.assertEqual(split_fields("solo"), ["solo"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def split_fields(line):
    return re.split(r"[,;\s]+", line)
```
