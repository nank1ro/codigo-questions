---
language: python
exerciseType: 1
---

# --description--

Les comprehensions sont excellentes pour **transformer des chaînes**.
Appelez une méthode de chaîne sur chaque élément, ou construisez une nouvelle chaîne avec une f-string :
```python
names = ["ana", "bob"]
greetings = [f"Hi {name.capitalize()}" for name in names]
print(greetings)  # ['Hi Ana', 'Hi Bob']
```

# --instructions--

Stockez dans la variable `shout` une liste avec chaque nom de `names` en majuscules suivi de `!`, à l'aide d'une list comprehension

# --seed--

```python
names = ["ana", "bob", "cy"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser une list comprehension

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`shout` doit être égal à `['ANA!', 'BOB!', 'CY!']`

```python
    def test_1(self):
        self.assertEqual(shout, ["ANA!", "BOB!", "CY!"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
names = ["ana", "bob", "cy"]
shout = [name.upper() + "!" for name in names]
```
