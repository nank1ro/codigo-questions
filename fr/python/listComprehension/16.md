---
language: python
exerciseType: 1
---

# --description--

**Quand devriez-vous utiliser une comprehension ?**
Elle est parfaite quand le résultat est une liste (ou un dict, ou un set) et que la logique tient sur une ligne lisible : une transformation simple, un filtre optionnel.
Si vous avez besoin de plusieurs instructions, de plus de deux `for` imbriqués, ou que la ligne devient difficile à lire, écrivez plutôt une simple boucle `for` : le code sera plus long, mais plus clair.
Une comprehension remplace aussi la plupart des usages de `map()` et `filter()` avec des lambdas :
```python
doubled = list(map(lambda n: n * 2, nums))
doubled = [n * 2 for n in nums]  # same result, easier to read
```

# --instructions--

Définissez une fonction `initials(names)` qui retourne une liste avec la première lettre de chaque nom de `names` en majuscule, en ignorant les chaînes vides. Utilisez une list comprehension

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser une list comprehension

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`initials(["ana", "bob"])` doit retourner `['A', 'B']`

```python
    def test_1(self):
        self.assertEqual(initials(["ana", "bob"]), ["A", "B"], "--err-t1--")
```

`initials(["", "cy", ""])` doit retourner `['C']`

```python
    def test_2(self):
        self.assertEqual(initials(["", "cy", ""]), ["C"], "--err-t2--")
```

`initials([])` doit retourner `[]`

```python
    def test_3(self):
        self.assertEqual(initials([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def initials(names):
    return [name[0].upper() for name in names if name != ""]
```
