---
language: python
exerciseType: 1
---

# --description--

Une comprehension peut avoir **plus d'un `for`**.
Ils fonctionnent comme des boucles imbriquées : le premier `for` est la boucle externe, le second est la boucle interne.
```python
pairs = [a + b for a in ["x", "y"] for b in ["1", "2"]]
print(pairs)  # ['x1', 'x2', 'y1', 'y2']
```

# --instructions--

Stockez dans la variable `combos` la liste de toutes les combinaisons de `colors` et `sizes` sous la forme `f"{color} {size}"`, à l'aide d'une list comprehension avec deux parties `for`

# --seed--

```python
colors = ["red", "blue"]
sizes = ["S", "M"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser une list comprehension avec deux parties `for`

```json
{
  "regex": "\\[.* for .* in .* for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`combos` doit être égal à `['red S', 'red M', 'blue S', 'blue M']`

```python
    def test_1(self):
        self.assertEqual(combos, ["red S", "red M", "blue S", "blue M"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
colors = ["red", "blue"]
sizes = ["S", "M"]
combos = [f"{color} {size}" for color in colors for size in sizes]
```
