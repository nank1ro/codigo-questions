---
language: python
exerciseType: 1
---

# --description--

Une enum est une classe, elle peut donc avoir des **méthodes** et des **propriétés** comme n'importe quelle autre classe.
À l'intérieur, `self` est le membre sur lequel la méthode a été appelée, vous pouvez donc consulter `self.name`, `self.value` ou comparer `self` avec d'autres membres :
```python
from enum import Enum

class Light(Enum):
    RED = 1
    GREEN = 2

    def can_go(self):
        return self is Light.GREEN

    @property
    def label(self):
        return self.name.lower()

print(Light.GREEN.can_go())  # True
print(Light.RED.label)       # red
```
Toute valeur simple assignée dans le corps de la classe devient un membre, tandis que les fonctions et les propriétés n'en deviennent jamais, peu importe où elles apparaissent.

# --instructions--

Ajoutez à `Season` une méthode `is_warm()` qui renvoie `True` seulement pour `SPRING` et `SUMMER`, et une propriété `label` qui renvoie le nom du membre en minuscules

# --seed--

```python
from enum import Enum

class Season(Enum):
    WINTER = 1
    SPRING = 2
    SUMMER = 3
    AUTUMN = 4
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez définir la méthode `is_warm(self)`

```json
{
  "regex": "def is_warm\\(self\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser le décorateur `@property`

```json
{
  "regex": "@property",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Season.SUMMER.is_warm()` doit être `True`

```python
    def test_1(self):
        self.assertEqual(Season.SUMMER.is_warm(), True, "--err-t1--")
        self.assertEqual(Season.SPRING.is_warm(), True, "--err-t1--")
```

`Season.WINTER.is_warm()` doit être `False`

```python
    def test_2(self):
        self.assertEqual(Season.WINTER.is_warm(), False, "--err-t2--")
        self.assertEqual(Season.AUTUMN.is_warm(), False, "--err-t2--")
```

`Season.AUTUMN.label` doit être égal à `"autumn"`

```python
    def test_3(self):
        self.assertEqual(Season.AUTUMN.label, "autumn", "--err-t3--")
        self.assertEqual(Season.WINTER.label, "winter", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Season(Enum):
    WINTER = 1
    SPRING = 2
    SUMMER = 3
    AUTUMN = 4

    def is_warm(self):
        return self in (Season.SPRING, Season.SUMMER)

    @property
    def label(self):
        return self.name.lower()
```
