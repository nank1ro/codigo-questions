---
language: python
exerciseType: 4
---

# --description--

Une classe enum est **itérable** : une boucle `for` sur la classe visite chaque membre, dans l'ordre où ils ont été déclarés :
```python
for color in Color:
    print(color.name, color.value)
```
`len()` renvoie le nombre de membres de l'enum, et `list(Color)` construit une liste de ces membres :
```python
print(len(Color))    # 3
print(list(Color))   # [<Color.RED: 1>, <Color.GREEN: 2>, <Color.BLUE: 3>]
```
Dans une liste, les membres sont affichés avec leur `repr()`, qui inclut la valeur entre chevrons.

# --instructions--

Ordonnez les éléments pour déclarer l'enum `Weekday`, affichez combien de membres elle contient, puis affichez le nom et la valeur de chaque membre, un par ligne

# --answers--

```python
from enum import Enum
```

```python
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
```

```python
print(len(Weekday))
```

```python
for day in Weekday:
```

```python
    print(day.name, day.value)
```

# --solutions--

```python
from enum import Enum
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
print(len(Weekday))
for day in Weekday:
    print(day.name, day.value)
```

# --output--

3
MON 1
TUE 2
WED 3
