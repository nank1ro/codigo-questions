---
language: python
exerciseType: 1
---

# --description--

Les membres d'une enum sont **hachables**, ils peuvent donc servir de clés de dictionnaire et d'éléments de set.
Un dictionnaire indexé par une enum est une façon propre d'attacher des données à chaque membre, et le consulter avec un membre est plus sûr que d'utiliser une chaîne brute qui pourrait être mal orthographiée :
```python
class Size(Enum):
    SMALL = 1
    LARGE = 2

prices = {Size.SMALL: 2.5, Size.LARGE: 4.5}
print(prices[Size.LARGE])  # 4.5
```
Comme les membres peuvent apparaître dans n'importe quelle collection, tout ce que vous savez sur les listes, les sets et les compréhensions fonctionne aussi avec eux :
```python
order = [Size.LARGE, Size.SMALL, Size.LARGE]
print(len(set(order)))  # 2
```

# --instructions--

Stockez dans `large_price` le prix de `Size.LARGE` lu dans `prices`, dans `total` la somme des prix des tailles dans `order`, et dans `sizes` le set des tailles distinctes dans `order`

# --seed--

```python
from enum import Enum

class Size(Enum):
    SMALL = 1
    MEDIUM = 2
    LARGE = 3

prices = {Size.SMALL: 2.5, Size.MEDIUM: 3.5, Size.LARGE: 4.5}
order = [Size.LARGE, Size.SMALL, Size.LARGE]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez lire `prices` avec `Size.LARGE` comme clé

```json
{
  "regex": "prices\\[\\s*Size\\.LARGE\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`large_price` doit être égal à `4.5`

```python
    def test_1(self):
        self.assertEqual(large_price, 4.5, "--err-t1--")
```

`total` doit être égal à `11.5`

```python
    def test_2(self):
        self.assertEqual(total, 11.5, "--err-t2--")
```

`sizes` doit être égal à `{Size.SMALL, Size.LARGE}`

```python
    def test_3(self):
        self.assertEqual(sizes, {Size.SMALL, Size.LARGE}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Size(Enum):
    SMALL = 1
    MEDIUM = 2
    LARGE = 3

prices = {Size.SMALL: 2.5, Size.MEDIUM: 3.5, Size.LARGE: 4.5}
order = [Size.LARGE, Size.SMALL, Size.LARGE]
large_price = prices[Size.LARGE]
total = sum(prices[size] for size in order)
sizes = set(order)
```
