---
language: python
exerciseType: 2
---

# --description--

Souvent, les valeurs exactes n'ont pas d'importance : vous avez seulement besoin que les membres soient distincts.
Dans ce cas, vous pouvez laisser Python choisir les valeurs avec `auto()`, également importé depuis le module `enum`.
Il assigne `1` au premier membre puis compte à partir de là :
```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    SOUTH = auto()
    EAST = auto()

print(Direction.EAST.value)  # 3
```

# --instructions--

Complétez le code pour importer `auto` et afficher la valeur du membre `LARGE` de `Size`

# --seed--

```python
from enum import Enum, [/]

class Size(Enum):
    SMALL = auto()
    MEDIUM = auto()
    LARGE = auto()

print(Size.LARGE.[/])
```

# --answers--

- auto
- value
- name
- Enum
- unique
- index

# --solutions--

```python
from enum import Enum, auto

class Size(Enum):
    SMALL = auto()
    MEDIUM = auto()
    LARGE = auto()

print(Size.LARGE.value)
```

# --output--

3
