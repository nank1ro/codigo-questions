---
language: python
exerciseType: 1
---

# --description--

Chaque membre d'une enum a deux attributs : `name`, l'identifiant que vous avez écrit dans la classe, et `value`, la valeur que vous lui avez assignée :
```python
from enum import Enum

class Color(Enum):
    RED = 1
    GREEN = 2

print(Color.GREEN.name)   # GREEN
print(Color.GREEN.value)  # 2
```
La valeur peut être de n'importe quel type, pas seulement un entier : des chaînes, des tuples et des flottants sont des choix courants.
Un membre est un objet normal, vous pouvez donc le stocker dans une variable et lire ses attributs plus tard :
```python
chosen = Color.RED
print(chosen.value)  # 1
```

# --instructions--

Stockez dans `home` le membre `Planet.EARTH`, dans `label` son nom et dans `position` sa valeur

# --seed--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser `.name`

```json
{
  "regex": "\\.name\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser `.value`

```json
{
  "regex": "\\.value\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`home` doit être `Planet.EARTH`

```python
    def test_1(self):
        self.assertIs(home, Planet.EARTH, "--err-t1--")
```

`label` doit être égal à `"EARTH"`

```python
    def test_2(self):
        self.assertEqual(label, "EARTH", "--err-t2--")
```

`position` doit être égal à `3`

```python
    def test_3(self):
        self.assertEqual(position, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3

home = Planet.EARTH
label = home.name
position = home.value
```
