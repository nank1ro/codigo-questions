---
language: python
exerciseType: 1
---

# --description--

Vous pouvez obtenir un membre à partir de sa **valeur** en appelant la classe comme une fonction, ou à partir de son **nom** en utilisant les crochets :
```python
print(Color(2))        # Color.GREEN
print(Color["BLUE"])   # Color.BLUE
```
Les deux sont pratiques quand la valeur ou le nom vient de l'extérieur du programme, comme un fichier ou une saisie utilisateur.
Si rien ne correspond, `Color(9)` lève une `ValueError` et `Color["PINK"]` lève une `KeyError`.

# --instructions--

Stockez dans `by_value` le membre de `Status` dont la valeur est `code`, en appelant `Status(...)`, et dans `by_name` le membre dont le nom est `label`, en utilisant `Status[...]`

# --seed--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez appeler `Status(...)` avec `code`

```json
{
  "regex": "Status\\(\\s*code\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser `Status[...]` avec `label`

```json
{
  "regex": "Status\\[\\s*label\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`by_value` doit être `Status.DONE`

```python
    def test_1(self):
        self.assertIs(by_value, Status.DONE, "--err-t1--")
```

`by_name` doit être `Status.FAILED`

```python
    def test_2(self):
        self.assertIs(by_name, Status.FAILED, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
by_value = Status(code)
by_name = Status[label]
```
