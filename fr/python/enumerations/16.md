---
language: python
exerciseType: 1
---

# --description--

En plus des méthodes classiques, une enum peut définir des **méthodes de classe** avec `@classmethod`. Elles reçoivent la classe enum elle-même comme `cls`, elles sont donc l'endroit idéal pour des façons alternatives de trouver un membre :
```python
class Color(Enum):
    RED = auto()
    BLUE = auto()

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]

print(Color.parse(" blue "))  # Color.BLUE
```
Avec `auto()`, les méthodes, les propriétés et les recherches, cela vous permet de construire des enums qui portent leur propre comportement.

# --instructions--

Déclarez une enum `Direction` avec les membres `NORTH`, `EAST`, `SOUTH` et `WEST`, dans cet ordre, en utilisant `auto()`.
Ajoutez une méthode `opposite()` qui renvoie le membre du côté opposé (`NORTH` et `SOUTH`, `EAST` et `WEST`), et une méthode de classe `parse(text)` qui renvoie le membre dont le nom correspond à `text`, en ignorant la casse et les espaces autour

# --seed--

```python
from enum import Enum, auto
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser `auto()`

```json
{
  "regex": "auto\\(\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser le décorateur `@classmethod`

```json
{
  "regex": "@classmethod",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Direction` doit avoir les membres `NORTH`, `EAST`, `SOUTH` et `WEST` avec des valeurs de `1` à `4`

```python
    def test_1(self):
        self.assertEqual([d.name for d in Direction], ["NORTH", "EAST", "SOUTH", "WEST"], "--err-t1--")
        self.assertEqual([d.value for d in Direction], [1, 2, 3, 4], "--err-t1--")
```

`Direction.NORTH.opposite()` doit être `Direction.SOUTH` et `Direction.WEST.opposite()` doit être `Direction.EAST`

```python
    def test_2(self):
        self.assertIs(Direction.NORTH.opposite(), Direction.SOUTH, "--err-t2--")
        self.assertIs(Direction.SOUTH.opposite(), Direction.NORTH, "--err-t2--")
        self.assertIs(Direction.EAST.opposite(), Direction.WEST, "--err-t2--")
        self.assertIs(Direction.WEST.opposite(), Direction.EAST, "--err-t2--")
```

`Direction.parse(" west ")` doit être `Direction.WEST`

```python
    def test_3(self):
        self.assertIs(Direction.parse(" west "), Direction.WEST, "--err-t3--")
        self.assertIs(Direction.parse("North"), Direction.NORTH, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    EAST = auto()
    SOUTH = auto()
    WEST = auto()

    def opposite(self):
        opposites = {
            Direction.NORTH: Direction.SOUTH,
            Direction.SOUTH: Direction.NORTH,
            Direction.EAST: Direction.WEST,
            Direction.WEST: Direction.EAST,
        }
        return opposites[self]

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]
```
