---
language: python
exerciseType: 1
---

# --description--

Un `Flag` est une enum dont les membres peuvent être **combinés** : une valeur peut contenir plusieurs membres à la fois, comme un ensemble d'options.
Déclarez ses membres avec `auto()`, qui pour un `Flag` assigne des puissances de deux (`1`, `2`, `4`, ...), afin que chaque combinaison ait une valeur distincte :
```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```
Utilisez `|` pour combiner des membres, `in` pour vérifier si un membre fait partie d'une combinaison et `value` pour voir le nombre qui en résulte :
```python
full = Permission.READ | Permission.WRITE | Permission.EXECUTE
print(Permission.WRITE in full)  # True
print(full.value)                # 7
print(full)                      # Permission.READ|WRITE|EXECUTE
```

# --instructions--

Stockez dans `editor` la combinaison de `Permission.READ` et `Permission.WRITE` en utilisant `|`, dans `can_write` si `Permission.WRITE` est dans `editor`, et dans `bits` la valeur de `editor`

# --seed--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser l'opérateur `|`

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser l'opérateur `in`

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`editor` doit être égal à `Permission.READ | Permission.WRITE`

```python
    def test_1(self):
        self.assertEqual(editor, Permission.READ | Permission.WRITE, "--err-t1--")
```

`can_write` doit être `True`

```python
    def test_2(self):
        self.assertEqual(can_write, True, "--err-t2--")
```

`bits` doit être égal à `3`

```python
    def test_3(self):
        self.assertEqual(bits, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()

editor = Permission.READ | Permission.WRITE
can_write = Permission.WRITE in editor
bits = editor.value
```
