---
language: python
exerciseType: 2
---

# --description--

Si deux membres partagent la même valeur, le second n'est pas un nouveau membre mais un **alias** du premier :
```python
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # alias of ACTIVE

print(Status.ENABLED)  # Status.ACTIVE
print(len(Status))     # 1
```
Les alias sont ignorés lors de l'itération et ne sont pas comptés par `len()`.
Habituellement, une valeur dupliquée est une erreur. Le décorateur `unique`, importé depuis `enum`, fait lever à Python une `ValueError` dès qu'une enum avec des alias est déclarée :
```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # ValueError: duplicate values found
```

# --instructions--

Complétez le code pour importer le décorateur `unique` et l'appliquer à `Status`, afin que les valeurs dupliquées soient rejetées, puis affichez le nombre de membres

# --seed--

```python
from enum import Enum, [/]

[/]
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --answers--

- unique
- @unique
- auto
- @auto
- @property
- @classmethod

# --solutions--

```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --output--

2
