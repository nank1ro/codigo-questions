---
language: python
exerciseType: 2
---

# --description--

`None` compte comme **faux** dans une condition, donc `if not value:` est `True` quand `value` est `None`. Il est tentant de l'utiliser comme test de `None`, mais le même test est aussi `True` pour `0`, `""`, `[]` et toutes les autres valeurs vides :
```python
count = 0
if not count:
    print("missing?")   # printed, but 0 is a real value!
```
Quand « aucune valeur » et « valeur vide » doivent être traitées différemment, vérifiez d'abord `is None`, puis la véracité :
```python
if count is None:
    print("no count yet")
elif not count:
    print("count is zero")
```
Utilisez `if not value:` seulement quand vous voulez vraiment traiter `None` et les valeurs vides de la même manière.

# --instructions--

Complétez la fonction pour qu'elle renvoie `missing` pour `None`, `empty` pour les valeurs vides et `present` sinon, de sorte à afficher `missing`, `empty`, `present`

# --seed--

```python
def describe(value):
    if value [/] None:
        return "missing"
    if [/] value:
        return "empty"
    return "present"

print(describe(None))
print(describe(0))
print(describe("hi"))
```

# --answers--

- is
- not
- is not
- in
- and

# --solutions--

```python
def describe(value):
    if value is None:
        return "missing"
    if not value:
        return "empty"
    return "present"

print(describe(None))
print(describe(0))
print(describe("hi"))
```

# --output--

missing
empty
present
