---
language: python
exerciseType: 1
---

# --description--

Quand `0` ou `""` doivent être conservés et que seul `None` doit être remplacé, la valeur de repli a besoin d'une vérification explicite `is None`. La forme compacte est l'**expression conditionnelle**, `a if condition else b`, qui vaut `a` quand la condition est vraie et `b` sinon :
```python
timeout = 0
seconds = timeout if timeout is not None else 30
print(seconds)  # 0, not 30
```

# --instructions--

Écrivez une fonction `port_or_default` qui prend `port` et le renvoie quand il n'est pas `None`, et `8080` sinon ; la valeur `0` doit être renvoyée telle quelle

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez vérifier avec `is not None` ou `is None`

```json
{
  "regex": "is\\s+(not\\s+)?None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`port_or_default(3000)` doit renvoyer `3000`

```python
    def test_1(self):
        self.assertEqual(port_or_default(3000), 3000, "--err-t1--")
```

`port_or_default(None)` doit renvoyer `8080`

```python
    def test_2(self):
        self.assertEqual(port_or_default(None), 8080, "--err-t2--")
```

`port_or_default(0)` doit renvoyer `0`

```python
    def test_3(self):
        self.assertEqual(port_or_default(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def port_or_default(port):
    return port if port is not None else 8080
```
