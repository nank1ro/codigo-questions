---
language: python
exerciseType: 1
---

# --description--

Une recherche suivie d'une vérification de `None` nécessite généralement deux lignes : une pour stocker le résultat, une pour le tester. L'opérateur d'**expression d'affectation** `:=`, surnommé le *walrus*, affecte une valeur **à l'intérieur** d'une expression, donc les deux étapes tiennent dans le `if` :
```python
ages = {"Ada": 36}
if (age := ages.get("Ada")) is not None:
    print(age + 1)  # 37
```
Les parenthèses sont obligatoires : sans elles, `:=` essaierait d'affecter toute la comparaison. Après le `if`, `age` reste disponible comme n'importe quelle autre variable.

# --instructions--

Écrivez une fonction `describe_user` qui prend un dictionnaire `users` associant des noms à des âges et un `name` ; elle doit renvoyer `"<name> is <age>"` quand le nom est présent, et `"unknown user"` sinon, en utilisant l'opérateur walrus pour stocker le résultat de `get`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser l'opérateur `:=`

```json
{
  "regex": ":=",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser `get`

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe_user({"Ada": 36}, "Ada")` doit renvoyer `"Ada is 36"`

```python
    def test_1(self):
        self.assertEqual(describe_user({"Ada": 36}, "Ada"), "Ada is 36", "--err-t1--")
```

`describe_user({"Ada": 36}, "Grace")` doit renvoyer `"unknown user"`

```python
    def test_2(self):
        self.assertEqual(describe_user({"Ada": 36}, "Grace"), "unknown user", "--err-t2--")
```

Un âge de `0` doit quand même compter comme présent

```python
    def test_3(self):
        self.assertEqual(describe_user({"Tim": 0}, "Tim"), "Tim is 0", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe_user(users, name):
    if (age := users.get(name)) is not None:
        return f"{name} is {age}"
    return "unknown user"
```
