---
language: python
exerciseType: 1
---

# --description--

Une fonction qui renvoie un nombre **ou** `None` devrait le dire dans sa signature. Une **indication de type** (type hint) est une annotation qui documente le type attendu : `name: str` pour un paramètre et `-> int` pour la valeur de retour. Python n'impose pas les indications de type, mais les éditeurs et les lecteurs s'y fient :
```python
def parse_age(text: str) -> int | None:
    if text.isdigit():
        return int(text)
    return None
```
`int | None` se lit « un `int` ou `None` ». L'ancienne écriture `Optional[int]` du module `typing` signifie exactement la même chose, et vous la rencontrerez encore dans du code existant.
Chaque fois que vous voyez `| None` dans une signature, pensez à vérifier le résultat avant de l'utiliser.

# --instructions--

Écrivez une fonction `parse_age` avec une indication de type qui prend une chaîne et la renvoie convertie en `int` quand elle contient seulement des chiffres, et `None` sinon

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Le type de retour doit être indiqué comme `int | None`

```json
{
  "regex": "->\\s*(int\\s*\\|\\s*None|None\\s*\\|\\s*int|Optional\\[int\\])",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_age("42")` doit renvoyer `42`

```python
    def test_1(self):
        self.assertEqual(parse_age("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_age("42"), int, "--err-t1--")
```

`parse_age("abc")` doit renvoyer `None`

```python
    def test_2(self):
        self.assertIsNone(parse_age("abc"), "--err-t2--")
        self.assertIsNone(parse_age("4a"), "--err-t2--")
```

`parse_age("")` doit renvoyer `None`

```python
    def test_3(self):
        self.assertIsNone(parse_age(""), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_age(text: str) -> int | None:
    if text.isdigit():
        return int(text)
    return None
```
