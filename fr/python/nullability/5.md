---
language: python
exerciseType: 1
---

# --description--

Un paramètre peut avoir une **valeur par défaut**, utilisée quand l'appelant omet l'argument. `None` est la valeur par défaut habituelle pour « non fourni » :
```python
def greet(name=None):
    if name is None:
        name = "stranger"
    print("Hello, " + name)

greet()       # Hello, stranger
greet("Ada")  # Hello, Ada
```
Cela compte pour les listes et les dictionnaires. Une valeur par défaut est évaluée **une seule fois**, quand la fonction est définie, donc `def add(item, items=[])` partage la même liste entre tous les appels qui omettent `items`, et les éléments s'accumulent. La solution est de mettre `None` par défaut et de créer une nouvelle liste dans la fonction :
```python
def add(item, items=None):
    if items is None:
        items = []
    items.append(item)
    return items
```

# --instructions--

Écrivez une fonction `add_tag` qui prend une chaîne `tag` et une liste facultative `tags` ; elle doit ajouter `tag` à `tags` et renvoyer la liste, en créant une nouvelle liste quand `tags` n'est pas fournie

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

La valeur par défaut de `tags` doit être `None`

```json
{
  "regex": "tags\\s*=\\s*None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`add_tag("new")` doit renvoyer `["new"]`

```python
    def test_1(self):
        self.assertEqual(add_tag("new"), ["new"], "--err-t1--")
```

Deux appels sans `tags` doivent renvoyer des listes indépendantes

```python
    def test_2(self):
        first = add_tag("a")
        second = add_tag("b")
        self.assertEqual(first, ["a"], "--err-t2--")
        self.assertEqual(second, ["b"], "--err-t2--")
```

`add_tag("c", ["a", "b"])` doit renvoyer `["a", "b", "c"]`

```python
    def test_3(self):
        existing = ["a", "b"]
        self.assertEqual(add_tag("c", existing), ["a", "b", "c"], "--err-t3--")
        self.assertIs(add_tag("d", existing), existing, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def add_tag(tag, tags=None):
    if tags is None:
        tags = []
    tags.append(tag)
    return tags
```
