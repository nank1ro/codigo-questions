---
language: python
exerciseType: 1
---

# --description--

Un décorateur qui n'accepte qu'un seul argument est peu utile. Pour envelopper **n'importe quelle** fonction, le wrapper collecte tous les arguments positionnels dans `*args` et tous les arguments nommés dans `**kwargs`, et les transmet sans les modifier :
```python
def uppercase(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs).upper()
    return wrapper

@uppercase
def greet(name, punctuation="!"):
    return "hello " + name + punctuation

print(greet("ada"))                    # HELLO ADA!
print(greet("ada", punctuation="?"))   # HELLO ADA?
```
Dans le wrapper, `args` est un tuple et `kwargs` un dictionnaire ; le `*` et le `**` dans l'appel les redéploient en arguments séparés.

# --instructions--

Écrivez un décorateur `exclaim` qui renvoie un wrapper acceptant n'importe quels arguments, appelant la fonction enveloppée avec ceux-ci et renvoyant son résultat suivi de `"!"` ; puis utilisez-le pour décorer une fonction `greet` qui prend un `name` et renvoie `"hello <name>"`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez appliquer le décorateur avec `@exclaim`

```json
{
  "regex": "^@exclaim\\s*$",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greet("ada")` doit renvoyer `"hello ada!"`

```python
    def test_1(self):
        self.assertEqual(greet("ada"), "hello ada!", "--err-t1--")
```

`exclaim` doit fonctionner avec une fonction à deux arguments

```python
    def test_2(self):
        joined = exclaim(lambda a, b: a + " " + b)
        self.assertEqual(joined("good", "day"), "good day!", "--err-t2--")
```

`exclaim` doit transmettre les arguments nommés

```python
    def test_3(self):
        tag = exclaim(lambda word, times=1: word * times)
        self.assertEqual(tag("hi", times=2), "hihi!", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def exclaim(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs) + "!"
    return wrapper

@exclaim
def greet(name):
    return "hello " + name
```
