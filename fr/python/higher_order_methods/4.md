---
language: python
exerciseType: 1
---

# --description--

La fonction native `filter(func, iterable)` ne garde que les éléments pour lesquels `func` renvoie une valeur vraie. Comme `map`, elle renvoie un objet paresseux qui doit être transformé en liste :
```python
numbers = [4, -2, 7, 0]
print(list(filter(lambda n: n > 0, numbers)))  # [4, 7]
```
La fonction passée à `filter` est appelée un **prédicat** : elle prend un élément et répond à une question par oui ou non à son sujet. Passer `None` au lieu d'une fonction garde les éléments qui sont vrais par eux-mêmes, en écartant `0`, `""` et `None`.

# --instructions--

Écrivez une fonction `positives` qui prend une liste de nombres et renvoie une nouvelle liste contenant uniquement les nombres supérieurs à zéro, en utilisant `filter`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser `filter`

```json
{
  "regex": "filter\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`positives([3, -1, 0, 5])` doit renvoyer `[3, 5]`

```python
    def test_1(self):
        self.assertEqual(positives([3, -1, 0, 5]), [3, 5], "--err-t1--")
```

Le résultat doit être une liste

```python
    def test_2(self):
        self.assertIsInstance(positives([1, 2]), list, "--err-t2--")
        self.assertEqual(positives([1, 2]), [1, 2], "--err-t2--")
```

`positives([-4, 0])` doit renvoyer une liste vide

```python
    def test_3(self):
        self.assertEqual(positives([-4, 0]), [], "--err-t3--")
        self.assertEqual(positives([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def positives(numbers):
    return list(filter(lambda n: n > 0, numbers))
```
