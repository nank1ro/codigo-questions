---
language: python
exerciseType: 1
---

# --description--

`reduce(func, iterable, initial)` du module `functools` réduit une séquence en une **valeur unique**. Elle appelle `func` avec le résultat obtenu jusque-là et l'élément suivant, en partant de `initial` :
```python
from functools import reduce

total = reduce(lambda acc, n: acc + n, [1, 2, 3], 0)
print(total)  # 6
```
Les étapes sont `0 + 1`, puis `1 + 2`, puis `3 + 3`. Quand `initial` est omis, le premier élément sert de valeur de départ, mais une séquence vide lève alors une `TypeError` ; donnez donc une valeur initiale dès que la séquence peut être vide.

# --instructions--

Écrivez une fonction `product` qui prend une liste de nombres et renvoie leur produit, en utilisant `reduce` ; le produit d'une liste vide doit être `1`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser `reduce`

```json
{
  "regex": "reduce\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`product([2, 3, 4])` doit renvoyer `24`

```python
    def test_1(self):
        self.assertEqual(product([2, 3, 4]), 24, "--err-t1--")
```

`product([5])` doit renvoyer `5`

```python
    def test_2(self):
        self.assertEqual(product([5]), 5, "--err-t2--")
```

`product([])` doit renvoyer `1`

```python
    def test_3(self):
        self.assertEqual(product([]), 1, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import reduce

def product(numbers):
    return reduce(lambda acc, n: acc * n, numbers, 1)
```
