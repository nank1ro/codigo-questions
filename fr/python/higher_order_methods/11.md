---
language: python
exerciseType: 1
---

# --description--

`partial` est pratique avec les fonctions natives qui acceptent des options. `int(text, base=16)` analyse une chaîne hexadécimale ; fixer la base donne un convertisseur à un seul argument qui convient à `map` :
```python
from functools import partial

hex_to_int = partial(int, base=16)
print(hex_to_int("ff"))                      # 255
print(list(map(hex_to_int, ["a", "10"])))    # [10, 16]
```
L'objet partial se souvient de ce qu'il enveloppe : `hex_to_int.func` est `int`, et `hex_to_int.keywords` est `{'base': 16}`.

# --instructions--

Définissez deux fonctions avec `partial` : `hex_to_int` qui convertit une chaîne hexadécimale en `int`, et `binary_to_int` qui convertit une chaîne binaire en `int`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`hex_to_int("ff")` doit renvoyer `255`

```python
    def test_1(self):
        self.assertEqual(hex_to_int("ff"), 255, "--err-t1--")
        self.assertEqual(hex_to_int("10"), 16, "--err-t1--")
```

`binary_to_int("1010")` doit renvoyer `10`

```python
    def test_2(self):
        self.assertEqual(binary_to_int("1010"), 10, "--err-t2--")
        self.assertEqual(binary_to_int("11"), 3, "--err-t2--")
```

Les deux doivent être des objets partial enveloppant `int`

```python
    def test_3(self):
        self.assertIs(hex_to_int.func, int, "--err-t3--")
        self.assertIs(binary_to_int.func, int, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import partial

hex_to_int = partial(int, base=16)
binary_to_int = partial(int, base=2)
```
