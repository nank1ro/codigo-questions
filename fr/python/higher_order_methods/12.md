---
language: python
exerciseType: 4
---

# --description--

Un **décorateur** est une fonction d'ordre supérieur qui prend une fonction et en renvoie une nouvelle qui l'enveloppe, généralement pour ajouter un comportement avant ou après l'appel d'origine :
```python
def announce(func):
    def wrapper(name):
        print("calling " + func.__name__)
        return func(name)
    return wrapper
```
`func.__name__` est le nom sous lequel la fonction a été définie. Appliquer le décorateur n'est qu'un appel : `greet = announce(greet)`. La syntaxe `@` placée sur la ligne **au-dessus** d'un `def` fait exactement cela :
```python
@announce
def greet(name):
    return "Hello, " + name
```
Le décorateur doit être défini avant d'être utilisé avec `@`, car le remplacement a lieu dès que le `def` s'exécute.

# --instructions--

Ordonnez les éléments pour définir le décorateur `announce`, l'appliquer à `greet` et afficher le résultat de `greet("Ada")`

# --answers--

```python
def announce(func):
```

```python
    def wrapper(name):
        print("calling " + func.__name__)
        return func(name)
```

```python
    return wrapper
```

```python
@announce
def greet(name):
    return "Hello, " + name
```

```python
print(greet("Ada"))
```

# --solutions--

```python
def announce(func):
    def wrapper(name):
        print("calling " + func.__name__)
        return func(name)
    return wrapper
@announce
def greet(name):
    return "Hello, " + name
print(greet("Ada"))
```

# --output--

calling greet
Hello, Ada
