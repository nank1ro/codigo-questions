---
language: python
exerciseType: 1
---

# --description--

Passer une fonction en argument laisse à l'appelant le choix de **quoi** faire, tandis que la fonction d'ordre supérieur décide **combien de fois** ou **sur quoi**. Le paramètre fonction peut être appelé autant de fois que nécessaire, et son résultat peut lui être redonné en entrée :
```python
def repeat(func, value, times):
    for _ in range(times):
        value = func(value)
    return value

def add_one(n):
    return n + 1

print(repeat(add_one, 0, 3))  # 3
```
N'importe quel appelable convient : une fonction `def`, une fonction native comme `len` ou une `lambda`.

# --instructions--

Écrivez une fonction `apply_twice` qui prend une fonction `func` et une `value`, et renvoie le résultat de l'application de `func` à `value` deux fois de suite

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`apply_twice(lambda x: x * 2, 3)` doit renvoyer `12`

```python
    def test_1(self):
        self.assertEqual(apply_twice(lambda x: x * 2, 3), 12, "--err-t1--")
```

`apply_twice(lambda s: s + "!", "hi")` doit renvoyer `"hi!!"`

```python
    def test_2(self):
        self.assertEqual(apply_twice(lambda s: s + "!", "hi"), "hi!!", "--err-t2--")
```

Le second appel doit recevoir le résultat du premier

```python
    def test_3(self):
        seen = []
        def record(x):
            seen.append(x)
            return x + 10
        self.assertEqual(apply_twice(record, 0), 20, "--err-t3--")
        self.assertEqual(seen, [0, 10], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def apply_twice(func, value):
    return func(func(value))
```
