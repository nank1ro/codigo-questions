---
language: python
exerciseType: 1
---

# --instructions--

Utilisez les accolades d'ouverture `{` et de fermeture `}` pour insérer le nombre `500` dans l'espace vide de la chaîne formatée

# --seed--

```python
print(f"There are  items")
```

# --solutions--

```python
print(f"There are {500} items")
```

# --output--

There are 500 items
