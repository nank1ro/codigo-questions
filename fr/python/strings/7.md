---
language: python
exerciseType: 1
---

# --description--

L'opérateur `in` vérifie si une chaîne en **contient** une autre.
Il renvoie `True` ou `False`, donc il s'intègre naturellement dans un `if` :
```python
email = "ada@mail.com"
print("@" in email)      # True
print("xyz" in email)    # False
if " " in email:
    print("No spaces allowed")
```
`not in` effectue la vérification inverse.

# --instructions--

Écrivez une fonction `has_space(text)` qui renvoie `True` si `text` contient au moins un espace, `False` sinon, en utilisant l'opérateur `in`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser l'opérateur `in`

```json
{
  "regex": "\" \" in |' ' in ",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_space("hello world")` doit renvoyer `True`

```python
    def test_1(self):
        self.assertEqual(has_space("hello world"), True, "--err-t1--")
```

`has_space("hello")` doit renvoyer `False`

```python
    def test_2(self):
        self.assertEqual(has_space("hello"), False, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def has_space(text):
    return " " in text
```
