---
language: python
exerciseType: 1
---

# --description--

Un **slug** est une version d'un titre adaptée aux URL : en minuscules, sans espaces autour, et avec les mots séparés par des tirets, comme `hello-world`.
En construire un n'est qu'un enchaînement des méthodes que vous avez apprises : `strip()`, `lower()` et `replace()`.

# --instructions--

Écrivez une fonction `slugify(title)` qui renvoie `title` sans espaces autour, en minuscules et avec chaque espace remplacé par `-`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser `replace()`

```json
{
  "regex": "\\.replace\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`slugify("Hello World")` doit renvoyer `"hello-world"`

```python
    def test_1(self):
        self.assertEqual(slugify("Hello World"), "hello-world", "--err-t1--")
```

`slugify("  Learn Python Strings  ")` doit renvoyer `"learn-python-strings"`

```python
    def test_2(self):
        self.assertEqual(slugify("  Learn Python Strings  "), "learn-python-strings", "--err-t2--")
```

`slugify("codigo")` doit renvoyer `"codigo"`

```python
    def test_3(self):
        self.assertEqual(slugify("codigo"), "codigo", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def slugify(title):
    return title.strip().lower().replace(" ", "-")
```
