---
language: python
exerciseType: 1
---

# --description--

Une **slice** (tranche) extrait une partie d'une chaîne.
Écrivez `[start:end]` entre crochets : le caractère à `start` est inclus, celui à `end` est **exclu** :
```python
word = "python"
print(word[0:3])  # pyt
print(word[2:4])  # th
```
Vous pouvez omettre `start` pour découper depuis le début, ou `end` pour découper jusqu'à la fin :
```python
print(word[:2])  # py
print(word[2:])  # thon
```
Le découpage ne déclenche jamais d'erreur : un `end` supérieur à la longueur s'arrête simplement au dernier caractère.

# --instructions--

Stockez dans la variable `first` les 4 premiers caractères de `word` et dans `rest` tous les restants, en utilisant des slices

# --seed--

```python
word = "notebook"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser une slice `[:]`

```json
{
  "regex": "\\[[^\\]]*:[^\\]]*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`first` doit être égal à `"note"`

```python
    def test_1(self):
        self.assertEqual(first, "note", "--err-t1--")
```

`rest` doit être égal à `"book"`

```python
    def test_2(self):
        self.assertEqual(rest, "book", "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
word = "notebook"
first = word[:4]
rest = word[4:]
```
