---
language: python
exerciseType: 1
---

# --description--

Les chaînes disposent de nombreuses **méthodes** intégrées : des fonctions appelées avec un point après la chaîne.
`upper()` renvoie le texte en majuscules, `lower()` en minuscules :
```python
word = "Python"
print(word.upper())  # PYTHON
print(word.lower())  # python
print(word)          # Python
```
Remarquez que les méthodes **renvoient une nouvelle chaîne** : la `word` d'origine n'est pas modifiée.
`lower()` est souvent utilisé pour comparer des textes sans tenir compte de la casse : `"Yes".lower() == "yes"`.

# --instructions--

Stockez dans `shout` la version en majuscules de `word` et dans `quiet` la version en minuscules

# --seed--

```python
word = "Python"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`shout` doit être égal à `"PYTHON"`

```python
    def test_1(self):
        self.assertEqual(shout, "PYTHON", "--err-t1--")
```

`quiet` doit être égal à `"python"`

```python
    def test_2(self):
        self.assertEqual(quiet, "python", "--err-t2--")
```

`word` doit toujours être égal à `"Python"`

```python
    def test_3(self):
        self.assertEqual(word, "Python", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
word = "Python"
shout = word.upper()
quiet = word.lower()
```
