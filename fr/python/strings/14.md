---
language: python
exerciseType: 1
---

# --description--

`startswith(prefix)` et `endswith(suffix)` renvoient `True` ou `False` selon la façon dont la chaîne commence ou se termine :
```python
url = "https://codigo.dev"
print(url.startswith("https"))  # True
print(url.endswith(".com"))     # False
```
C'est la façon habituelle de vérifier des extensions de fichiers, des protocoles ou des préfixes.

# --instructions--

Écrivez une fonction `is_python_file(name)` qui renvoie `True` si `name` se termine par `.py`, et une fonction `is_comment(line)` qui renvoie `True` si `line` commence par `#`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser `endswith()`

```json
{
  "regex": "\\.endswith\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser `startswith()`

```json
{
  "regex": "\\.startswith\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`is_python_file("main.py")` doit renvoyer `True`

```python
    def test_1(self):
        self.assertEqual(is_python_file("main.py"), True, "--err-t1--")
```

`is_python_file("notes.txt")` doit renvoyer `False`

```python
    def test_2(self):
        self.assertEqual(is_python_file("notes.txt"), False, "--err-t2--")
```

`is_comment("# todo")` doit renvoyer `True`

```python
    def test_3(self):
        self.assertEqual(is_comment("# todo"), True, "--err-t3--")
```

`is_comment("x = 1  # todo")` doit renvoyer `False`

```python
    def test_4(self):
        self.assertEqual(is_comment("x = 1  # todo"), False, "--err-t4--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_python_file(name):
    return name.endswith(".py")

def is_comment(line):
    return line.startswith("#")
```
