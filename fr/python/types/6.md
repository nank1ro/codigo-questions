---
language: python
exerciseType: 1
---

# --description--

Une conversion peut échouer. `int("abc")` ne peut pas produire de nombre, donc elle lève une `ValueError` et le programme s'arrête :
```python
int("abc")   # ValueError: invalid literal for int() with base 10: 'abc'
int("3.5")   # ValueError as well: "3.5" is not a whole number
```
Pour que le programme continue, vous pouvez intercepter l'erreur avec `try` / `except` : le code du bloc `try` s'exécute, et s'il lève l'erreur nommée, c'est le bloc `except` qui s'exécute à la place :
```python
try:
    number = int(text)
except ValueError:
    number = 0
```
Quand la conversion réussit, le bloc `except` est ignoré.

# --instructions--

Écrivez une fonction `to_int` qui prend une chaîne et la renvoie convertie en `int`, ou `None` quand la conversion lève une `ValueError`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez intercepter `ValueError` avec `except`

```json
{
  "regex": "except\\s+ValueError",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`to_int("42")` doit renvoyer `42`

```python
    def test_1(self):
        self.assertEqual(to_int("42"), 42, "--err-t1--")
        self.assertEqual(to_int("-7"), -7, "--err-t1--")
```

`to_int("abc")` doit renvoyer `None`

```python
    def test_2(self):
        self.assertIsNone(to_int("abc"), "--err-t2--")
```

`to_int("3.5")` doit renvoyer `None`

```python
    def test_3(self):
        self.assertIsNone(to_int("3.5"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def to_int(text):
    try:
        return int(text)
    except ValueError:
        return None
```
