---
language: python
exerciseType: 1
---

# --description--

`type()` renvoie la classe d'une valeur, vous pouvez donc la comparer à un nom de classe avec `is` :
```python
age = 30
print(type(age) is int)  # True
```
La plupart du temps, cependant, vous voulez seulement savoir **si** une valeur est d'un certain type. C'est le rôle de `isinstance(value, cls)`, qui renvoie `True` ou `False` :
```python
print(isinstance(age, int))    # True
print(isinstance(age, str))    # False
```
Le second argument peut aussi être un **tuple** de classes : le résultat est `True` si la valeur appartient à l'un d'eux :
```python
print(isinstance(2.5, (int, float)))  # True
```

# --instructions--

Stockez dans `age_type` le type de `age`, avec `type()`, dans `is_text` si `name` est une `str` et dans `is_number` si `price` est un `int` ou un `float`, avec `isinstance()`

# --seed--

```python
age = 30
price = 9.99
name = "Ada"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser `type()`

```json
{
  "regex": "type\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser `isinstance()`

```json
{
  "regex": "isinstance\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`age_type` doit être égal à `int`

```python
    def test_1(self):
        self.assertIs(age_type, int, "--err-t1--")
```

`is_text` doit être égal à `True`

```python
    def test_2(self):
        self.assertIs(is_text, True, "--err-t2--")
```

`is_number` doit être égal à `True`

```python
    def test_3(self):
        self.assertIs(is_number, True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
age = 30
price = 9.99
name = "Ada"

age_type = type(age)
is_text = isinstance(name, str)
is_number = isinstance(price, (int, float))
```
