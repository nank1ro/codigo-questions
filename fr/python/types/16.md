---
language: python
exerciseType: 1
---

# --description--

Le texte provenant de l'extérieur est toujours une `str`, et c'est à votre programme de déterminer quel type il contient réellement.
Une approche courante consiste à essayer d'abord la conversion la plus **stricte**, puis à revenir à la suivante quand elle lève une `ValueError` :
```python
try:
    value = int(text)
except ValueError:
    value = float(text)
```
Imbriquer un second `try` dans le bloc `except` permet de revenir une fois de plus, par exemple pour garder le texte tel quel.

# --instructions--

Écrivez une fonction `parse_value` qui prend une chaîne et la renvoie comme un `int` si c'est un nombre entier, comme un `float` si c'est un nombre décimal, et inchangée sinon

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez intercepter `ValueError` avec `except`

```json
{
  "regex": "except\\s+ValueError",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_value("42")` doit renvoyer l'`int` `42`

```python
    def test_1(self):
        self.assertEqual(parse_value("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_value("42"), int, "--err-t1--")
        self.assertEqual(parse_value("-8"), -8, "--err-t1--")
```

`parse_value("3.5")` doit renvoyer le `float` `3.5`

```python
    def test_2(self):
        self.assertEqual(parse_value("3.5"), 3.5, "--err-t2--")
        self.assertIsInstance(parse_value("3.5"), float, "--err-t2--")
```

`parse_value("abc")` doit renvoyer `"abc"`

```python
    def test_3(self):
        self.assertEqual(parse_value("abc"), "abc", "--err-t3--")
        self.assertEqual(parse_value(""), "", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_value(text):
    try:
        return int(text)
    except ValueError:
        try:
            return float(text)
        except ValueError:
            return text
```
