---
language: python
exerciseType: 2
---

# --description--

`bool` est une **sous-classe** de `int` : `True` se comporte comme `1` et `False` comme `0` partout où un nombre est attendu :
```python
print(True + 1)     # 2
print(True == 1)    # True
print(False * 10)   # 0
```
`sum()` additionne les éléments d'une liste, donc sommer une liste de booléens **compte** combien valent `True` :
```python
answers = [True, False, True]
print(sum(answers))  # 2
```
À cause de cette relation de sous-classe, `isinstance(True, int)` renvoie `True`, tandis que `type(True)` reste `bool`.

# --instructions--

Complétez le code pour qu'il compte les bonnes réponses, ajoute `1` à `True` et vérifie que `True` est une instance de `int`, en affichant `3`, `2` et `True`

# --seed--

```python
answers = [True, False, True, True]
correct = [/](answers)
print(correct)
print(True [/] 1)
print(isinstance(True, [/]))
```

# --answers--

- sum
- +
- int
- len
- ==
- str

# --solutions--

```python
answers = [True, False, True, True]
correct = sum(answers)
print(correct)
print(True + 1)
print(isinstance(True, int))
```

# --output--

3
2
True
