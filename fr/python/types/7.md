---
language: python
exerciseType: 3
---

# --description--

Chaque valeur peut être interprétée comme un booléen. `bool()` convertit une valeur en `True` ou `False`, et la même règle est appliquée quand une valeur est utilisée directement dans un `if`.
Les valeurs qui comptent comme **fausses** sont celles qui sont « vides » :
- le nombre `0` (et `0.0`)
- la chaîne vide `""`
- les collections vides comme `[]`, `{}`, `()` et `set()`
- `None`

Toute valeur non vide est **vraie**, y compris les nombres négatifs et les chaînes qui ne font que paraître vides, comme `"0"` ou `" "` :
```python
print(bool(0))     # False
print(bool(-1))    # True
print(bool(""))    # False
print(bool("0"))   # True
```
C'est pourquoi `if name:` est une façon courante de vérifier qu'une chaîne n'est pas vide.

# --instructions--

Qu'affiche ce code ?
```python
print(bool(0), bool("0"), bool([]), bool(None))
```

# --answers--

- `False True False False`, parce que `"0"` est une chaîne non vide
- `False False False False`, parce qu'elles représentent toutes zéro ou rien
- `True True False False`, parce que les nombres sont toujours vrais
- Une `TypeError`, parce que `None` ne peut pas être converti en booléen

# --solutions--

- `False True False False`, parce que `"0"` est une chaîne non vide
