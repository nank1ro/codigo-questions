---
language: python
exerciseType: 2
---

# --description--

Les conversions peuvent être combinées. `int("3.7")` échoue, mais `float("3.7")` fonctionne, et `int()` d'un `float` supprime la partie décimale :
```python
number = float("3.7")   # 3.7
print(int(number))      # 3
```
`bool()` suit les règles de véracité : `bool("")` vaut `False`, et notez que `bool("False")` vaut `True`, car c'est une chaîne non vide.

# --instructions--

Complétez le code pour convertir `text` en nombre, afficher son double, puis afficher le nombre converti en `int` et la valeur booléenne d'une chaîne vide, de sorte à afficher `7.4`, `3` et `False`

# --seed--

```python
text = "3.7"
number = [/](text)
print(number * 2)
print([/](number))
print([/](""))
```

# --answers--

- float
- int
- bool
- str
- len

# --solutions--

```python
text = "3.7"
number = float(text)
print(number * 2)
print(int(number))
print(bool(""))
```

# --output--

7.4
3
False
