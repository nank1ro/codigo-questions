---
language: python
exerciseType: 1
---

# --description--

Contrairement à beaucoup de langages, les entiers de Python n'ont **pas de taille maximale** : un `int` grandit pour contenir autant de chiffres que nécessaire, donc les grands calculs restent exacts :
```python
print(3 ** 100)  # 515377520732011331036461129765621272702107522001
```
Un `float`, en revanche, ne garde qu'environ 15 chiffres significatifs, donc la même puissance calculée en float perd en précision :
```python
print(3.0 ** 100)  # 5.153775207320113e+47
```
Comme `str()` fonctionne sur n'importe quel `int`, un moyen rapide de compter les chiffres d'un nombre est de mesurer la longueur de son texte.

# --instructions--

Stockez dans `big` la valeur de `2` à la puissance `200`, puis écrivez une fonction `digits` qui prend un `int` positif et renvoie son nombre de chiffres

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser `str()`

```json
{
  "regex": "str\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`big` doit être égal à `2 ** 200`

```python
    def test_1(self):
        self.assertEqual(big, 2 ** 200, "--err-t1--")
        self.assertIsInstance(big, int, "--err-t1--")
```

`digits(big)` doit renvoyer `61`

```python
    def test_2(self):
        self.assertEqual(digits(big), 61, "--err-t2--")
```

`digits(7)` doit renvoyer `1` et `digits(10 ** 50)` doit renvoyer `51`

```python
    def test_3(self):
        self.assertEqual(digits(7), 1, "--err-t3--")
        self.assertEqual(digits(10 ** 50), 51, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
big = 2 ** 200

def digits(number):
    return len(str(number))
```
