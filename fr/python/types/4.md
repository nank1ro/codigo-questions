---
language: python
exerciseType: 1
---

# --description--

Vous connaissez déjà les opérateurs arithmétiques. Ce qui compte ici, c'est le **type du résultat**.
Combiner un `int` avec un `float` donne un `float`, même quand la partie décimale est nulle :
```python
print(3 + 0.5)   # 3.5
print(2 * 1.0)   # 2.0
```
La **division réelle** `/` renvoie toujours un `float`, même quand les nombres se divisent exactement :
```python
print(10 / 5)    # 2.0
print(7 / 2)     # 3.5
```
La **division entière** `//` arrondit le résultat à l'entier inférieur (ainsi `-7 // 2` vaut `-4`) et renvoie un `int` quand les deux opérandes sont des entiers. Avec le reste `%`, elle découpe une quantité en parties entières :
```python
print(7 // 2)    # 3
print(7 % 2)     # 1
```

# --instructions--

Stockez dans `each` le résultat de la division de `total` par `people` avec `/`, dans `whole` la même division avec `//` et dans `rest` le reste

# --seed--

```python
total = 17
people = 5
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser `//`

```json
{
  "regex": "//",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`each` doit être le float `3.4`

```python
    def test_1(self):
        self.assertIsInstance(each, float, "--err-t1--")
        self.assertEqual(each, 3.4, "--err-t1--")
```

`whole` doit être l'entier `3`

```python
    def test_2(self):
        self.assertIsInstance(whole, int, "--err-t2--")
        self.assertEqual(whole, 3, "--err-t2--")
```

`rest` doit être égal à `2`

```python
    def test_3(self):
        self.assertEqual(rest, 2, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
total = 17
people = 5

each = total / people
whole = total // people
rest = total % people
```
