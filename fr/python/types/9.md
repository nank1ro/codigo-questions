---
language: python
exerciseType: 1
---

# --description--

`None` est une valeur à part qui signifie « rien ici ». C'est ce que renvoie une fonction qui n'a pas d'instruction `return`, et c'est un espace réservé courant pour une valeur encore inconnue.
Comme il n'existe qu'un seul `None`, testez-le avec `is`, pas avec `==` :
```python
result = None
if result is None:
    print("no result yet")
```
Chaque type possède un attribut `__name__` avec son nom sous forme de chaîne, ce qui est pratique pour les messages :
```python
print(type(3).__name__)     # int
print(type(None).__name__)  # NoneType
```

# --instructions--

Écrivez une fonction `describe` qui prend une valeur et renvoie la chaîne `"nothing"` quand la valeur est `None`, et le nom de son type sinon

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez comparer avec `is None`

```json
{
  "regex": "is\\s+None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser `__name__`

```json
{
  "regex": "__name__",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe(None)` doit renvoyer `"nothing"`

```python
    def test_1(self):
        self.assertEqual(describe(None), "nothing", "--err-t1--")
```

`describe(5)` doit renvoyer `"int"` et `describe(2.0)` doit renvoyer `"float"`

```python
    def test_2(self):
        self.assertEqual(describe(5), "int", "--err-t2--")
        self.assertEqual(describe(2.0), "float", "--err-t2--")
```

`describe("a")` doit renvoyer `"str"` et `describe(True)` doit renvoyer `"bool"`

```python
    def test_3(self):
        self.assertEqual(describe("a"), "str", "--err-t3--")
        self.assertEqual(describe(True), "bool", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe(value):
    if value is None:
        return "nothing"
    return type(value).__name__
```
