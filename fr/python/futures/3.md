---
language: python
exerciseType: 1
---

# --description--

`await` met en pause la coroutine courante jusqu'à ce que l'opération attendue se termine, puis donne son résultat. Pendant que la coroutine est en pause, la boucle d'événements est libre d'exécuter d'autres coroutines. `await` n'est autorisé qu'à l'intérieur d'un `async def`.

`asyncio.sleep(seconds)` est l'objet awaitable le plus simple : il attend pendant la durée indiquée sans bloquer la boucle. Contrairement à `time.sleep`, il doit être attendu avec `await` :
```python
import asyncio

async def countdown():
    print("ready")
    await asyncio.sleep(0.05)
    print("go")

asyncio.run(countdown())
```
Le programme affiche `ready`, attend 50 millisecondes puis affiche `go`. Écrire `asyncio.sleep(0.05)` sans `await` crée l'objet coroutine mais ne l'exécute jamais, donc aucune attente n'a lieu.

# --instructions--

Écrivez une fonction coroutine `wait_and_return(value, delay)` qui attend `delay` secondes avec `asyncio.sleep` puis renvoie `value`

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`wait_and_return("ok", 0.01)` doit renvoyer `"ok"`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(wait_and_return("ok", 0.01)), "ok", "--err-t1--")
```

`wait_and_return(7, 0.02)` doit renvoyer `7` et prendre au moins `0.02` secondes

```python
    def test_2(self):
        start = time.perf_counter()
        result = asyncio.run(wait_and_return(7, 0.02))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, 7, "--err-t2--")
        self.assertGreaterEqual(elapsed, 0.018, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def wait_and_return(value, delay):
    await asyncio.sleep(delay)
    return value
```
