---
language: python
exerciseType: 1
---

# --description--

Les pièces se combinent naturellement. Pour exécuter beaucoup d'opérations de manière concurrente, chacune avec sa propre limite de temps, enveloppez chacune dans une petite coroutine qui applique `wait_for` et intercepte la `TimeoutError`, puis passez les enveloppes à `gather` :
```python
async def guarded(coro, limit):
    try:
        return await asyncio.wait_for(coro, timeout=limit)
    except TimeoutError:
        return None
```
`gather(*[guarded(job(x), limit) for x in items])` renvoie alors une valeur pour chaque tâche terminée à temps et `None` pour chacune de celles qui ne l'ont pas été, dans l'ordre d'origine, et le lot entier prend au plus environ `limit` secondes.

# --instructions--

Écrivez une fonction coroutine `job(delay)` qui attend `delay` secondes et renvoie `delay`, et une fonction coroutine `run_jobs(delays, limit)` qui exécute un `job` pour chaque delay **de manière concurrente**, chacun limité à `limit` secondes avec `asyncio.wait_for`, et renvoie une liste avec le delay de chaque tâche terminée à temps et `None` pour chaque tâche ayant dépassé le timeout, dans l'ordre d'origine

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`job(0.01)` doit renvoyer `0.01`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(job(0.01)), 0.01, "--err-t1--")
```

`run_jobs([0.01, 0.06, 0.02], 0.04)` doit renvoyer `[0.01, None, 0.02]` et `run_jobs([], 0.01)` doit renvoyer `[]`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(run_jobs([0.01, 0.06, 0.02], 0.04)), [0.01, None, 0.02], "--err-t2--")
        self.assertEqual(asyncio.run(run_jobs([], 0.01)), [], "--err-t2--")
```

Cinq tâches de `0.02` secondes avec une limite de `0.05` doivent toutes se terminer et s'exécuter de manière concurrente, en prenant bien moins de `0.1` secondes

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(run_jobs([0.02] * 5, 0.05))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [0.02] * 5, "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def job(delay):
    await asyncio.sleep(delay)
    return delay

async def guarded(delay, limit):
    try:
        return await asyncio.wait_for(job(delay), timeout=limit)
    except TimeoutError:
        return None

async def run_jobs(delays, limit):
    return await asyncio.gather(*[guarded(d, limit) for d in delays])
```
