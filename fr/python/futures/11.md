---
language: python
exerciseType: 2
---

# --description--

Une tâche continue de s'exécuter que quelqu'un l'attende ou non. Créez-la tôt, faites autre chose, et attendez-la avec `await` seulement au moment où son résultat est nécessaire : l'attente est plus courte car une partie de la tâche a déjà été exécutée en arrière-plan. Une tâche peut aussi être inspectée avec `task.done()`, qui renvoie `True` une fois qu'elle est terminée.

# --instructions--

Complétez le code pour que `compute()` soit planifiée comme une tâche, que `"waiting"` soit affiché pendant qu'elle s'exécute, et que la tâche soit ensuite attendue avec `await` pour obtenir sa valeur, produisant la sortie `waiting`, `42`

# --seed--

```python
import asyncio

async def compute():
    await asyncio.sleep(0.01)
    return 42

async def main():
    task = asyncio.[/](compute())
    print("waiting")
    value = [/] task
    print(value)

asyncio.run(main())
```

# --answers--

- create_task
- await
- gather
- run
- sleep
- result

# --solutions--

```python
import asyncio

async def compute():
    await asyncio.sleep(0.01)
    return 42

async def main():
    task = asyncio.create_task(compute())
    print("waiting")
    value = await task
    print(value)

asyncio.run(main())
```

# --output--

waiting
42
