---
language: python
exerciseType: 1
---

# --description--

Les coroutines s'appellent entre elles avec `await`. Une coroutine peut attendre n'importe quelle autre coroutine, recevoir sa valeur de retour et continuer, exactement comme une chaîne d'appels de fonctions :
```python
import asyncio

async def fetch_price(item):
    await asyncio.sleep(0.01)  # simulates a slow lookup
    return 10

async def total(item, quantity):
    price = await fetch_price(item)
    return price * quantity

print(asyncio.run(total("pen", 3)))  # 30
```
Seule la coroutine la plus externe passe par `asyncio.run` ; toutes les internes sont atteintes avec `await`. Si `total` était un `def` classique, elle ne pourrait pas utiliser `await` du tout : le mot-clé `async` se propage à chaque fonction de la chaîne qui a besoin d'attendre.

# --instructions--

Écrivez une fonction coroutine `fetch_user(user_id)` qui attend `0.01` secondes et renvoie la chaîne `"user"` suivie de l'id, et une fonction coroutine `greet_user(user_id)` qui attend `fetch_user` avec `await` et renvoie `"Hello, "` suivi du nom récupéré

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch_user(1)` doit renvoyer `"user1"` et `fetch_user(42)` doit renvoyer `"user42"`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch_user(1)), "user1", "--err-t1--")
        self.assertEqual(asyncio.run(fetch_user(42)), "user42", "--err-t1--")
```

`greet_user(1)` doit renvoyer `"Hello, user1"`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(greet_user(1)), "Hello, user1", "--err-t2--")
```

`fetch_user` et `greet_user` doivent toutes deux être des fonctions coroutine

```python
    def test_3(self):
        self.assertTrue(inspect.iscoroutinefunction(fetch_user), "--err-t3--")
        self.assertTrue(inspect.iscoroutinefunction(greet_user), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch_user(user_id):
    await asyncio.sleep(0.01)
    return f"user{user_id}"

async def greet_user(user_id):
    name = await fetch_user(user_id)
    return f"Hello, {name}"
```
