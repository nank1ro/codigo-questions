---
language: python
exerciseType: 1
---

# --description--

La concurrence est payante lorsque le nombre d'opérations n'est pas fixe. Construisez les objets coroutine dans une list comprehension, décompressez-les dans `gather` et attendez-les tous avec `await` en une seule fois :
```python
import asyncio

async def fetch(url):
    await asyncio.sleep(0.02)
    return len(url)

async def fetch_all(urls):
    return await asyncio.gather(*[fetch(u) for u in urls])

print(asyncio.run(fetch_all(["a.com", "bb.com"])))  # [5, 6]
```
Dix urls prennent toujours environ 20 millisecondes au total au lieu de 200, et les résultats restent dans l'ordre des urls.

# --instructions--

Écrivez une fonction coroutine `fetch(item_id)` qui attend `0.02` secondes et renvoie `item_id * 10`, et une fonction coroutine `fetch_all(ids)` qui exécute `fetch` pour chaque id **de manière concurrente** avec `asyncio.gather` et renvoie la liste des résultats

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch(3)` doit renvoyer `30`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch(3)), 30, "--err-t1--")
```

`fetch_all([1, 2, 3])` doit renvoyer `[10, 20, 30]` et `fetch_all([])` doit renvoyer `[]`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(fetch_all([1, 2, 3])), [10, 20, 30], "--err-t2--")
        self.assertEqual(asyncio.run(fetch_all([])), [], "--err-t2--")
```

`fetch_all` avec cinq ids doit les exécuter de manière concurrente, en prenant bien moins de `0.1` secondes

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(fetch_all([1, 2, 3, 4, 5]))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [10, 20, 30, 40, 50], "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch(item_id):
    await asyncio.sleep(0.02)
    return item_id * 10

async def fetch_all(ids):
    return await asyncio.gather(*[fetch(i) for i in ids])
```
