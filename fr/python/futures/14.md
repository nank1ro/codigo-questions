---
language: python
exerciseType: 2
---

# --description--

`asyncio.wait_for(awaitable, timeout)` attend quelque chose avec `await` mais abandonne après `timeout` secondes : l'opération est annulée et une `TimeoutError` est levée, qui peut être interceptée comme n'importe quelle exception :
```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())  # timed out
```
Avec un timeout de `0.1`, le même code afficherait `done`. `asyncio.TimeoutError` est un autre nom de la `TimeoutError` native.

# --instructions--

Complétez le code pour que `slow()` soit attendue avec `await` et un timeout de `0.01` secondes et que le timeout soit intercepté, produisant la sortie `timed out`

# --seed--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.[/](slow(), [/]=0.01)
        print(result)
    except [/]:
        print("timed out")

asyncio.run(main())
```

# --answers--

- wait_for
- timeout
- TimeoutError
- gather
- delay
- ValueError

# --solutions--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())
```

# --output--

timed out
