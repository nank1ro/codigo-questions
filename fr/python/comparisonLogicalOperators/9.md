---
language: python
exerciseType: 4
---

# --description--

Les opérateurs logiques sont à **court-circuit** : `and` s'arrête dès qu'un opérande est faux et `or` dès qu'un opérande est vrai, car le résultat est déjà connu. Les opérandes restants ne sont jamais évalués, donc s'il s'agit d'appels de fonction ils ne s'exécutent pas :
```python
def check(n):
    print("checking", n)
    return n > 0

check(-1) and check(5)  # prints only "checking -1"
check(2) or check(-3)   # prints only "checking 2"
```

# --instructions--

Triez les éléments pour définir `is_positive`, calculer `first` avec `and` puis `second` avec `or`, et afficher les deux résultats, produisant la sortie `checking -1`, `checking 2`, `False True`

# --answers--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
```

```python
first = is_positive(-1) and is_positive(5)
```

```python
second = is_positive(2) or is_positive(-3)
```

```python
print(first, second)
```

# --solutions--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
first = is_positive(-1) and is_positive(5)
second = is_positive(2) or is_positive(-3)
print(first, second)
```

# --output--

checking -1
checking 2
False True
