---
language: python
exerciseType: 1
---

# --description--

Les opérateurs logiques combinent des booléens. `and` vaut `True` seulement quand les deux côtés valent `True`, `or` quand au moins un côté l'est, et `not` inverse une seule valeur :
```python
age = 20
member = False
print(age >= 18 and member)  # False
print(age >= 18 or member)   # True
print(not member)            # True
```
Les comparaisons sont prioritaires sur les opérateurs logiques, donc `age >= 18 and member` n'a pas besoin de parenthèses. Les parenthèses sont nécessaires pour regrouper un `or` à l'intérieur d'un `and` : `a and (b or c)`.

# --instructions--

Écrivez une fonction `is_leap(year)` qui renvoie `True` lorsque `year` est divisible par `4` mais pas par `100`, sauf s'il est aussi divisible par `400`, et `False` sinon

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`2024` et `2000` sont des années bissextiles

```python
    def test_1(self):
        self.assertIs(is_leap(2024), True, "--err-t1--")
        self.assertIs(is_leap(2000), True, "--err-t1--")
```

`2023` n'est pas une année bissextile

```python
    def test_2(self):
        self.assertIs(is_leap(2023), False, "--err-t2--")
```

`1900` est divisible par `100` mais pas par `400`, ce n'est donc pas une année bissextile

```python
    def test_3(self):
        self.assertIs(is_leap(1900), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_leap(year):
    return year % 4 == 0 and (year % 100 != 0 or year % 400 == 0)
```
