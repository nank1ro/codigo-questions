---
language: python
exerciseType: 2
---

# --description--

Les opérateurs de comparaison comparent deux valeurs et renvoient un **booléen**, `True` ou `False` : `==` égal, `!=` différent, `<` inférieur à, `>` supérieur à, `<=` inférieur ou égal, `>=` supérieur ou égal :
```python
age = 18
print(age == 18)  # True
print(age != 18)  # False
print(age < 18)   # False
print(age >= 18)  # True
```
Le résultat peut être stocké dans une variable ou affiché directement. Un simple `=` est une affectation, pas une comparaison.

# --instructions--

Complétez le code pour que les trois lignes vérifient si `score` est égal à `75`, inférieur à `100` et supérieur à `50`, produisant la sortie `True`, `True`, `True`

# --seed--

```python
score = 75
print(score [/] 75)
print(score [/] 100)
print(score [/] 50)
```

# --answers--

- ==
- <
- >
- =
- !==

# --solutions--

```python
score = 75
print(score == 75)
print(score < 100)
print(score > 50)
```

# --output--

True
True
True
