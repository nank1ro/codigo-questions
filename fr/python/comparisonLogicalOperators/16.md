---
language: python
exerciseType: 1
---

# --description--

`any(iterable)` renvoie `True` si au moins un élément est vrai et `all(iterable)` si tous le sont (`all([])` vaut `True`). Combinés à une expression génératrice, ils expriment une condition sur toute une collection, et ils court-circuitent comme `or` et `and` :
```python
print(any(ch.isdigit() for ch in "abc1"))  # True
print(all(ch.isalpha() for ch in "ab1"))   # False
```

# --instructions--

Écrivez une fonction `is_valid_password(password)` qui renvoie `True` uniquement lorsque la chaîne compte entre `8` et `20` caractères inclus, contient au moins un chiffre et ne contient aucun espace, et `False` sinon

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`"secret123"` est valide, et les mots de passe d'exactement `8` ou `20` caractères le sont aussi

```python
    def test_1(self):
        self.assertIs(is_valid_password("secret123"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefg1"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefghij123456789x"), True, "--err-t1--")
```

Les mots de passe trop courts ou trop longs ne sont pas valides

```python
    def test_2(self):
        self.assertIs(is_valid_password("short1"), False, "--err-t2--")
        self.assertIs(is_valid_password("abcdefghij1234567890x"), False, "--err-t2--")
```

Un mot de passe sans chiffre ou avec un espace n'est pas valide

```python
    def test_3(self):
        self.assertIs(is_valid_password("nodigitshere"), False, "--err-t3--")
        self.assertIs(is_valid_password("pass word1"), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_valid_password(password):
    return (
        8 <= len(password) <= 20
        and any(ch.isdigit() for ch in password)
        and " " not in password
    )
```
