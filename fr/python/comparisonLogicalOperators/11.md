---
language: python
exerciseType: 1
---

# --description--

L'évaluation en court-circuit est une manière sûre de **protéger** une opération qui échouerait sur certaines valeurs. Dans `word is not None and len(word) < 4`, `len(word)` ne s'exécute que lorsque `word` n'est pas `None`, donc l'appel ne lève jamais d'erreur. La protection doit venir en premier :
```python
def is_short(word):
    return word is not None and len(word) < 4

print(is_short(None))   # False
print(is_short("cat"))  # True
```
Gardez à l'esprit que `and` renvoie un opérande : `word and len(word) < 4` donne `None` pour `None` et `""` pour la chaîne vide, pas `False`. Protégez avec une vraie comparaison lorsqu'un booléen est requis.

# --instructions--

Écrivez une fonction `first_is_zero(numbers)` qui renvoie `True` lorsque la liste `numbers` n'est pas vide et que son premier élément vaut `0`, et `False` sinon. Le résultat doit être un booléen même pour une liste vide

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_is_zero([0, 5])` doit renvoyer `True`

```python
    def test_1(self):
        self.assertIs(first_is_zero([0, 5]), True, "--err-t1--")
        self.assertIs(first_is_zero([0]), True, "--err-t1--")
```

`first_is_zero([3, 0])` doit renvoyer `False`

```python
    def test_2(self):
        self.assertIs(first_is_zero([3, 0]), False, "--err-t2--")
```

`first_is_zero([])` doit renvoyer `False` sans lever d'erreur

```python
    def test_3(self):
        self.assertIs(first_is_zero([]), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def first_is_zero(numbers):
    return len(numbers) > 0 and numbers[0] == 0
```
