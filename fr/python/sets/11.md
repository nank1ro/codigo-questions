---
language: python
exerciseType: 4
---

# --description--

Les sets peuvent aussi être **comparés** entre eux.
`a.issubset(b)`, ou `a <= b`, vaut `True` quand chaque élément de `a` est aussi dans `b`.
`a.issuperset(b)`, ou `a >= b`, vaut `True` quand `a` contient chaque élément de `b`.
`a.isdisjoint(b)` vaut `True` quand les deux sets n'ont **aucun** élément en commun :
```python
small = {1, 2}
big = {1, 2, 3}
print(small <= big)              # True
print(big.issuperset(small))     # True
print(small.isdisjoint({8, 9}))  # True
```

# --instructions--

Triez les éléments pour construire `palette` à partir de `primary`, puis affichez, dans cet ordre : si `primary` est un sous-ensemble de `palette`, si `palette` est un sous-ensemble de `primary`, et si `primary` et `{"black"}` sont disjoints

# --answers--

```python
primary = {"red", "blue", "yellow"}
```

```python
palette = primary | {"green"}
```

```python
print(primary <= palette)
```

```python
print(palette.issubset(primary))
```

```python
print(primary.isdisjoint({"black"}))
```

# --solutions--

```python
primary = {"red", "blue", "yellow"}
palette = primary | {"green"}
print(primary <= palette)
print(palette.issubset(primary))
print(primary.isdisjoint({"black"}))
```

# --output--

True
False
True
