---
language: python
exerciseType: 1
---

# --description--

La fonction native `set()` construit un set à partir de n'importe quelle collection, par exemple une liste ou une chaîne.
Comme un set ne garde chaque valeur qu'une seule fois, c'est la méthode classique pour **supprimer les doublons** :
```python
nums = [1, 2, 2, 3]
unique = set(nums)
print(len(unique))  # 3
```
Pour vérifier si une valeur est présente, utilisez l'opérateur `in`, qui renvoie `True` ou `False` :
```python
print(2 in unique)  # True
print(9 in unique)  # False
```
Les vérifications d'appartenance sur un set sont très rapides, même avec des milliers d'éléments.

# --instructions--

Stockez dans la variable `unique` un set construit à partir de `nums` avec `set()`, et dans `has_three` si la valeur `3` est dans `unique`, avec `in`

# --seed--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser `set()`

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser l'opérateur `in`

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`unique` doit être égal à `{1, 2, 3, 4}`

```python
    def test_1(self):
        self.assertEqual(unique, {1, 2, 3, 4}, "--err-t1--")
```

`has_three` doit être `True`

```python
    def test_2(self):
        self.assertEqual(has_three, True, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
unique = set(nums)
has_three = 3 in unique
```
