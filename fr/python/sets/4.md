---
language: python
exerciseType: 2
---

# --description--

Les sets sont **mutables** : vous pouvez ajouter et retirer des éléments après leur création.
`add(value)` insère une valeur ; ajouter une valeur déjà présente ne change rien :
```python
letters = {"a", "b"}
letters.add("c")
letters.add("a")
print(len(letters))  # 3
```
Il existe deux façons de retirer un élément :
- `remove(value)` le supprime, mais lève une `KeyError` si la valeur n'est pas dans le set
- `discard(value)` le supprime si présent et ne fait **rien** sinon, sans erreur
```python
letters.remove("a")
letters.discard("z")  # "z" n'y est pas, mais pas d'erreur
letters.remove("z")   # KeyError: 'z'
```

# --instructions--

Complétez le code pour ajouter `"c"` à `letters`, puis retirer `"z"` (qui n'est pas dans le set) sans lever d'erreur, puis affichez combien d'éléments contient le set

# --seed--

```python
letters = {"a", "b"}
letters.[/]("c")
letters.[/]("z")
print([/](letters))
```

# --answers--

- add
- discard
- len
- remove
- append
- size

# --solutions--

```python
letters = {"a", "b"}
letters.add("c")
letters.discard("z")
print(len(letters))
```

# --output--

3
