---
language: python
exerciseType: 1
---

# --description--

La **différence** `a - b` contient les éléments de `a` qui ne sont **pas** dans `b`.
L'ordre compte : `a - b` et `b - a` sont généralement différents :
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a - b)  # {1}
print(b - a)  # {4}
```
La **différence symétrique** `a ^ b` contient les éléments présents dans **exactement un** des deux sets :
```python
print(a ^ b)  # {1, 4}
```
Les formes méthode sont `difference()` et `symmetric_difference()`.

# --instructions--

Stockez dans `only_ann` les loisirs d'`ann` que `bob` n'a pas, avec `-`, et dans `not_shared` les loisirs que seul l'un des deux a, avec `^`

# --seed--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser l'opérateur `-`

```json
{
  "regex": "\\s-\\s",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser l'opérateur `^`

```json
{
  "regex": "\\^",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`only_ann` doit être égal à `{'tennis', 'yoga'}`

```python
    def test_1(self):
        self.assertEqual(only_ann, {"tennis", "yoga"}, "--err-t1--")
```

`not_shared` doit être égal à `{'tennis', 'yoga', 'swimming'}`

```python
    def test_2(self):
        self.assertEqual(not_shared, {"tennis", "yoga", "swimming"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
only_ann = ann - bob
not_shared = ann ^ bob
```
