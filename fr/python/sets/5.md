---
language: python
exerciseType: 1
---

# --description--

`pop()` retire **un élément arbitraire** du set et le renvoie.
Comme un set n'a pas d'ordre, vous ne pouvez pas choisir quel élément est retiré ; appeler `pop()` sur un set vide lève une `KeyError` :
```python
tickets = {101, 102, 103}
picked = tickets.pop()
print(len(tickets))  # 2
```
`clear()` retire **tous** les éléments, laissant un set vide :
```python
tickets.clear()
print(len(tickets))  # 0
```

# --instructions--

Retirez un élément de `tickets` avec `pop()` et stockez-le dans la variable `picked`, puis videz le set `drafts` avec `clear()`

# --seed--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser `pop()`

```json
{
  "regex": "\\.pop\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser `clear()`

```json
{
  "regex": "\\.clear\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`picked` doit être l'un des tickets d'origine

```python
    def test_1(self):
        self.assertIn(picked, {101, 102, 103}, "--err-t1--")
```

`tickets` doit contenir 2 éléments et ne plus contenir `picked`

```python
    def test_2(self):
        self.assertEqual(len(tickets), 2, "--err-t2--")
        self.assertNotIn(picked, tickets, "--err-t2--")
```

`drafts` doit être vide

```python
    def test_3(self):
        self.assertEqual(drafts, set(), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
picked = tickets.pop()
drafts.clear()
```
