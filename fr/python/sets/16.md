---
language: python
exerciseType: 1
---

# --description--

Deux sets sont **égaux** quand ils contiennent les mêmes éléments, quel que soit l'ordre dans lequel ils ont été écrits :
```python
print({1, 2, 3} == {3, 1, 2})  # True
```
Comparer la longueur d'une collection avec la longueur de son set est un moyen rapide de détecter les doublons : si le set est **plus petit**, une valeur est apparue plus d'une fois :
```python
emails = ["a@x.com", "b@x.com", "a@x.com"]
print(len(set(emails)) < len(emails))  # True
```
La méthode de liste `count(value)` indique combien de fois une valeur apparaît, ce qui aide à trouver *quelles* valeurs sont dupliquées.

# --instructions--

Stockez dans `has_duplicates` si `emails` contient une adresse répétée, en comparant la longueur de la liste avec la longueur de son set, puis stockez dans `duplicates` le set des adresses qui apparaissent plus d'une fois, avec une set comprehension et `count()`

# --seed--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser `set()`

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Vous devez utiliser une set comprehension

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_duplicates` doit être `True`

```python
    def test_1(self):
        self.assertEqual(has_duplicates, True, "--err-t1--")
```

`duplicates` doit être égal à `{'ann@x.com', 'bob@x.com'}`

```python
    def test_2(self):
        self.assertEqual(duplicates, {"ann@x.com", "bob@x.com"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
has_duplicates = len(set(emails)) < len(emails)
duplicates = {e for e in emails if emails.count(e) > 1}
```
