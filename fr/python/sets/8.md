---
language: python
exerciseType: 2
---

# --description--

L'**intersection** de deux sets contient seulement les éléments présents dans **les deux**.
Utilisez l'opérateur `&` ou la méthode `intersection()` :
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a & b)               # {2, 3}
print(a.intersection(b))   # {2, 3}
```
Si les sets n'ont rien en commun, le résultat est un set vide.

# --instructions--

Complétez le code pour stocker dans `common` les éléments présents à la fois dans `a` et dans `b`, de sorte que le programme affiche `[3, 4]`

# --seed--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a [/] b
print(sorted(common))
```

# --answers--

- &
- |
- -
- ^
- and

# --solutions--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a & b
print(sorted(common))
```

# --output--

[3, 4]
