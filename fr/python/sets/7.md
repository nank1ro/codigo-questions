---
language: python
exerciseType: 1
---

# --description--

L'**union** de deux sets est un nouveau set avec les éléments des **deux**, sans doublons.
Utilisez l'opérateur `|` ou la méthode `union()` :
```python
a = {1, 2}
b = {2, 3}
print(a | b)        # {1, 2, 3}
print(a.union(b))   # {1, 2, 3}
```
Ni `a` ni `b` n'est modifié : les opérations sur les sets renvoient toujours un nouveau set.

# --instructions--

Stockez dans `skills` l'union de `frontend` et `backend` avec l'opérateur `|`, puis stockez dans `total` le nombre de compétences distinctes qu'elle contient

# --seed--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Vous devez utiliser l'opérateur `|`

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`skills` doit être égal à `{'html', 'css', 'js', 'python', 'sql'}`

```python
    def test_1(self):
        self.assertEqual(skills, {"html", "css", "js", "python", "sql"}, "--err-t1--")
```

`total` doit être égal à `5`

```python
    def test_2(self):
        self.assertEqual(total, 5, "--err-t2--")
```

`frontend` ne doit pas être modifié

```python
    def test_3(self):
        self.assertEqual(frontend, {"html", "css", "js"}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
skills = frontend | backend
total = len(skills)
```
