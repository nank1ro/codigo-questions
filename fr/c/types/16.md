---
language: c
exerciseType: 4
---

# --description--

Pour tout assembler : choisissez le type en fonction de la nature de la valeur, faites correspondre chaque spécificateur `printf` au type de son argument, et convertissez lorsqu'un calcul doit se faire dans un type différent de celui de ses opérandes.

# --instructions--

Triez les lignes pour construire un programme qui stocke `7` dans `total` et `2` dans `count`, calcule `total` divisé par `count` sous forme de `double` dans `avg`, et affiche la division entière `total / count` suivie de `avg` avec une décimale

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int total = 7, count = 2;
```

```c
    double avg = (double) total / count;
```

```c
    printf("%d %.1f\n", total / count, avg);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int total = 7, count = 2;
    double avg = (double) total / count;
    printf("%d %.1f\n", total / count, avg);
    return 0;
}
```

# --output--

3 3.5
