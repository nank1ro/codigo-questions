---
language: c
exerciseType: 2
---

# --description--

Nous pouvons également imbriquer une déclaration conditionnelle (`if`, `else if` ou `else`) à l'intérieur d'une autre déclaration conditionnelle, pour créer une structure plus complexe.
```c
int num = 4;
if (num < 3) {
    printf("the number is lower than 3\n");
} else {
    if (num == 3) {
        printf("the number is 3\n");
  } else if (num == 4) {
        printf("the number is 4\n");
  } else {
        printf("the number is greather than 4\n");
  }
}
```
et la sortie de ce code est `the number is 4`.

# --instructions--

Modifions le programme de l'heure de la journée, en imbriquant une déclaration conditionnelle dans la déclaration `else`.
La déclaration conditionnelle imbriquée devrait vérifier si `time` est *inférieur* à `18` et dans le bloc de code devrait imprimer `"It's afternoon"`.
Si la condition précédente est `false`, imprimez `"It's evening"`

# --seed--

```c
int time = 19;
if (time > 0 && time < 12) {
    printf("It's morning\n");
} else if (time == 12) {
    printf("It's midday\n");
} else if (time == 0) {
    printf("It's midning\n");
} else {
[/]([/][/][/]) {
        printf("It's afternoon\n");
    }[/]{
    printf("It's evening\n");
    }
}
```

# --answers--

- 18
-  else if 
-  < 
-  == 
- :
- ;
- time
-  else if {
-  elseif 
-  if 
-  else 
-  else {

# --solutions--

```c
int time = 19;
if (time > 0 && time < 12) {
    printf("It's morning\n");
} else if (time == 12) {
    printf("It's midday\n");
} else if (time == 0) {
    printf("It's midning\n");
} else {
 if (time < 18) {
        printf("It's afternoon\n");
    } else {
    printf("It's evening\n");
    }
}
```

# --output--

It's evening
