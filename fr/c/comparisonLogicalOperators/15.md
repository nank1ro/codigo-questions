---
language: c
exerciseType: 2
---

# --description--

Une boucle `while` continue de s'exécuter tant que sa condition est non nulle, donc une comparaison décide quand elle s'arrête :
```c
int i = 0;
while (i < 3) {
    printf("%d\n", i);
    i++;
}
// affiche 0, 1, 2
```
Choisir entre `<` et `<=` change si la dernière valeur est incluse.

# --instructions--

Complétez le code pour que la boucle affiche les nombres de `1` à `3`, un par ligne

# --seed--

```c
int count = [/];
while (count [/] 3) {
    printf("%d\n", count);
    count++;
}
```

# --answers--

- 1
- <=
- 0
- <
- !=
- >

# --solutions--

```c
int count = 1;
while (count <= 3) {
    printf("%d\n", count);
    count++;
}
```

# --output--

1
2
3
