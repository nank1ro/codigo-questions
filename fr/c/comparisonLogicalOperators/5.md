---
language: c
exerciseType: 2
---

# --description--

L'opérateur **ou** `||` donne `1` quand au moins un côté est vrai, et `0` seulement quand les deux côtés sont faux :
```c
int day = 1;
printf("%d\n", day == 1 || day == 7); // affiche "1"
```
Chaque côté doit être une comparaison complète : `day == 6 || 7` ne signifie pas "6 ou 7" (vous verrez pourquoi plus tard).

# --instructions--

Complétez le code pour que `weekend` vaille `1` quand `day` est `6` ou que `day` est `7`, puis affichez-le

# --seed--

```c
int day = 7;
int weekend = day == 6 [/] day == [/];
printf("%d\n", weekend);
```

# --answers--

- ||
- 7
- &&
- 6
- =

# --solutions--

```c
int day = 7;
int weekend = day == 6 || day == 7;
printf("%d\n", weekend);
```

# --output--

1
