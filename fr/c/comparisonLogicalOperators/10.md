---
language: c
exerciseType: 4
---

# --description--

Les opérateurs ont une **priorité** qui décide de ce qui est calculé en premier :
1. `!` est appliqué en premier
2. puis les comparaisons relationnelles `<`, `>`, `<=`, `>=`
3. puis les comparaisons d'égalité `==`, `!=`
4. puis `&&`
5. puis `||`

Ainsi, `a > 0 && a < 10` n'a pas besoin de parenthèses : les deux comparaisons sont calculées avant `&&`.
Et `x == 1 || y == 2 && z == 3` signifie `x == 1 || (y == 2 && z == 3)`, car `&&` est plus prioritaire que `||`.

# --instructions--

Triez les lignes pour stocker dans `inside` si `a` est compris entre `1` et `9`, stocker sa négation dans `outside` et afficher les deux valeurs séparées par un espace

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int a = 5;
```

```c
    int inside = a > 0 && a < 10;
```

```c
    int outside = !inside;
```

```c
    printf("%d %d\n", inside, outside);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int a = 5;
    int inside = a > 0 && a < 10;
    int outside = !inside;
    printf("%d %d\n", inside, outside);
    return 0;
}
```

# --output--

1 0
