---
language: c
exerciseType: 4
---

# --description--

C n'offre aucun moyen intégré d'obtenir le nom d'une constante d'énumération : `printf("%d\n", SUMMER)` affiche `2`, pas `Summer`. La solution habituelle est une petite fonction avec un `switch` qui retourne la chaîne correspondant à chaque constante.

# --instructions--

Triez les lignes pour définir le type `Season`, une fonction `season_name` qui retourne le nom d'une saison, et une `main` qui affiche le nom de `SUMMER`. Définissez la fonction avant `main`, gardez les cas dans le même ordre que les constantes et mettez `default` en dernier

# --answers--

```c
#include <stdio.h>
typedef enum { WINTER, SPRING, SUMMER, AUTUMN } Season;
```

```c
char *season_name(Season season) {
```

```c
    switch (season) {
```

```c
        case WINTER: return "Winter";
```

```c
        case SPRING: return "Spring";
```

```c
        case SUMMER: return "Summer";
```

```c
        default: return "Autumn";
```

```c
    }
}
```

```c
int main() {
```

```c
    printf("%s\n", season_name(SUMMER));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef enum { WINTER, SPRING, SUMMER, AUTUMN } Season;
char *season_name(Season season) {
    switch (season) {
        case WINTER: return "Winter";
        case SPRING: return "Spring";
        case SUMMER: return "Summer";
        default: return "Autumn";
    }
}
int main() {
    printf("%s\n", season_name(SUMMER));
    return 0;
}
```

# --output--

Summer
