---
language: c
exerciseType: 2
---

# --description--

Une énumération est aussi un type : vous pouvez déclarer une variable de ce type en écrivant `enum` suivi du nom de l'énumération, et lui assigner une de ses constantes :
```c
enum Color favorite = GREEN;
```
Comme les constantes sont des entiers, vous comparez les variables d'énumération avec les opérateurs habituels :
```c
if (favorite == GREEN) {
    printf("Green it is\n");
}
```

# --instructions--

Complétez le code pour déclarer la variable `light` de type `enum Light` contenant `GREEN`, afin que le programme affiche `Go`

# --seed--

```c
enum Light { RED, YELLOW, GREEN };
[/] Light light = [/];
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --answers--

- enum
- struct
- GREEN
- RED
- YELLOW
- "GREEN"

# --solutions--

```c
enum Light { RED, YELLOW, GREEN };
enum Light light = GREEN;
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --output--

Go
