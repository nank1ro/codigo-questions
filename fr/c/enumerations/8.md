---
language: c
exerciseType: 2
---

# --description--

Écrire `enum Color` à chaque fois est fastidieux. Avec `typedef`, vous donnez à l'énumération un nom de type court, et l'énumération elle-même peut rester anonyme :
```c
typedef enum { RED, GREEN, BLUE } Color;

Color favorite = BLUE;
```
Le nouveau nom `Color` est utilisé seul, sans le mot-clé `enum` devant.

# --instructions--

Complétez le code pour définir l'énumération anonyme avec les constantes `OFF` et `ON` sous le nom de type `State`, puis déclarez `lamp` avec ce type

# --seed--

```c
typedef [/] { OFF, ON } State;
[/] lamp = ON;
printf("%d\n", lamp);
```

# --answers--

- enum
- struct
- State
- enum State
- typedef

# --solutions--

```c
typedef enum { OFF, ON } State;
State lamp = ON;
printf("%d\n", lamp);
```

# --output--

1
