---
language: c
exerciseType: 4
---

# --description--

N'enchaînez pas les comparaisons comme en mathématiques. `1 <= x <= 10` compile, mais il est évalué comme `(1 <= x) <= 10` : la première comparaison donne `0` ou `1`, et cette valeur est ensuite comparée à `10`, donc l'expression entière vaut toujours `1`.
```c
int x = 20;
printf("%d\n", 1 <= x <= 10);       // prints "1" (wrong!)
printf("%d\n", 1 <= x && x <= 10);  // prints "0"
```
Écrivez toujours les deux comparaisons explicitement et joignez-les avec `&&`.

# --instructions--

Triez les lignes pour construire un programme qui stocke la comparaison enchaînée dans `chained`, la vérification d'intervalle correcte dans `correct` et affiche les deux. Définissez `chained` avant `correct`

# --answers--

```c
#include <stdio.h>

int main() {
```

```c
    int x = 20;
```

```c
    int chained = 1 <= x <= 10;
```

```c
    int correct = 1 <= x && x <= 10;
```

```c
    printf("%d %d\n", chained, correct);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>

int main() {
    int x = 20;
    int chained = 1 <= x <= 10;
    int correct = 1 <= x && x <= 10;
    printf("%d %d\n", chained, correct);
    return 0;
}
```

# --output--

1 0
