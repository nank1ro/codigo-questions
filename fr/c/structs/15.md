---
language: c
exerciseType: 4
---

# --description--

`printf` n'a pas de spécificateur pour une struct entière. La solution habituelle est une petite fonction qui affiche les membres dans un format fixe, pour que chaque partie du programme montre la valeur de la même façon :
```c
void print_person(Person p) {
    printf("%s (%d)\n", p.name, p.age);
}
```

# --instructions--

Remets les lignes dans l'ordre pour définir le type `Point`, une fonction `print_point` qui affiche un point sous la forme `(x, y)` et un `main` qui affiche le point `(3, 4)`. Définis la fonction avant `main`

# --answers--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
```

```c
void print_point(Point p) {
```

```c
    printf("(%d, %d)\n", p.x, p.y);
```

```c
}
```

```c
int main() {
```

```c
    Point p = {3, 4};
```

```c
    print_point(p);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
void print_point(Point p) {
    printf("(%d, %d)\n", p.x, p.y);
}
int main() {
    Point p = {3, 4};
    print_point(p);
    return 0;
}
```

# --output--

(3, 4)
