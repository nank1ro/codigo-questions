---
language: c
exerciseType: 2
---

# --description--

Une struct contient souvent du texte, stocké dans un membre tableau de `char` de taille fixe :
```c
typedef struct {
    char name[20];
    int age;
} Person;
```
Un membre tableau ne peut pas être affecté avec `=` après la déclaration : `p.name = "Ann"` ne compile pas. Copie le texte dedans avec `strcpy` de `string.h`, en passant le membre comme destination :
```c
Person p;
strcpy(p.name, "Ann");
p.age = 30;
```
Seul un initialiseur entre accolades à la déclaration accepte la chaîne directement : `Person p = {"Ann", 30};`.

# --instructions--

Complète le code pour remplacer le nom de `p` par `"Ann"`, puis affiche le nom et l'âge

# --seed--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
[/](p.name, "Ann");
printf("%s is %d\n", p[/]name, p.age);
```

# --answers--

- strcpy
- strcat
- strcmp
- strlen
- .
- ->

# --solutions--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
strcpy(p.name, "Ann");
printf("%s is %d\n", p.name, p.age);
```

# --output--

Ann is 30
