---
language: c
exerciseType: 2
---

# --description--

Écrire `struct Point` à chaque fois est verbeux. Avec `typedef`, tu donnes à la struct un nom de type court, et la struct elle-même peut rester anonyme :
```c
typedef struct {
    double celsius;
} Temperature;

Temperature t = {21.5};
```
Le nouveau nom `Temperature` s'utilise seul, sans le mot-clé `struct` devant. C'est la façon la plus courante de déclarer des structs dans les vrais programmes.

# --instructions--

Complète le code pour définir la struct anonyme avec le membre `celsius` sous le nom de type `Temperature`, puis déclare `t` avec ce type

# --seed--

```c
typedef [/] {
    double celsius;
} Temperature;
[/] t = {21.5};
printf("%.1f\n", t.celsius);
```

# --answers--

- struct
- enum
- typedef
- Temperature
- struct Temperature
- Temperature t

# --solutions--

```c
typedef struct {
    double celsius;
} Temperature;
Temperature t = {21.5};
printf("%.1f\n", t.celsius);
```

# --output--

21.5
