---
language: c
exerciseType: 4
---

# --description--

Les structs peuvent être stockées dans un tableau comme n'importe quel autre type. Chaque élément s'initialise avec ses propres accolades, et une boucle les parcourt une par une, en indexant d'abord puis en utilisant le point :
```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};

for (int i = 0; i < 3; i++) {
    printf("%d\n", path[i].y);
}
```

# --instructions--

Remets les lignes dans l'ordre pour définir le type `Point`, stocker trois points dans `path`, additionner leurs membres `x` dans `sum` et l'afficher. Déclare `path` avant `sum`

# --answers--

```c
typedef struct { int x; int y; } Point;
```

```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
```

```c
int sum = 0;
```

```c
for (int i = 0; i < 3; i++) {
```

```c
    sum += path[i].x;
```

```c
}
```

```c
printf("%d\n", sum);
```

# --solutions--

```c
typedef struct { int x; int y; } Point;
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
int sum = 0;
for (int i = 0; i < 3; i++) {
    sum += path[i].x;
}
printf("%d\n", sum);
```

# --output--

6
