---
language: c
exerciseType: 4
---

# --description--

Inverser une liste retourne chaque pointeur `next`, sur place, avec trois pointeurs : `prev` est la partie déjà inversée, `head` le nœud en cours de traitement et `next` une copie du reste de la liste, sauvegardée avant que le lien ne soit modifié. À chaque étape, le nœud courant est fait pointer vers `prev`, puis `prev` et `head` avancent ensemble :
```c
Node *next = head->next;
head->next = prev;
prev = head;
head = next;
```
Lorsque `head` atteint `NULL`, chaque lien a été retourné et `prev` est la nouvelle tête.

# --instructions--

Trie les lignes pour définir `reverse`, qui inverse une liste sur place et renvoie sa nouvelle tête, et un `main` qui affiche la liste inversée `1, 2, 3`. Définis la fonction avant `main`

# --answers--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
```

```c
Node *reverse(Node *head) {
    Node *prev = NULL;
```

```c
    while (head != NULL) {
        Node *next = head->next;
```

```c
        head->next = prev;
```

```c
        prev = head;
```

```c
        head = next;
    }
```

```c
    return prev;
}
```

```c
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
```

```c
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
Node *reverse(Node *head) {
    Node *prev = NULL;
    while (head != NULL) {
        Node *next = head->next;
        head->next = prev;
        prev = head;
        head = next;
    }
    return prev;
}
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --output--

3
2
1
