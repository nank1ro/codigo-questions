---
language: c
exerciseType: 4
---

# --instructions--

Triez les lignes pour que le programme compare `password` avec `attempt` et affiche `Access granted`. Déclarez `password` avant `attempt` et placez chaque message dans sa propre branche

# --answers--

```c
char password[] = "secret";
```

```c
char attempt[] = "secret";
```

```c
if (strcmp(password, attempt) == 0) {
```

```c
    printf("Access granted\n");
```

```c
} else {
```

```c
    printf("Access denied\n");
```

```c
}
```

# --solutions--

```c
char password[] = "secret";
char attempt[] = "secret";
if (strcmp(password, attempt) == 0) {
    printf("Access granted\n");
} else {
    printf("Access denied\n");
}
```

# --output--

Access granted
