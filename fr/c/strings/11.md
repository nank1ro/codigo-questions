---
language: c
exerciseType: 2
---

# --description--

L'en-tête `ctype.h` fournit des fonctions qui travaillent sur un seul caractère.
`toupper(c)` retourne la version majuscule d'une lettre et `tolower(c)` la version minuscule ; tout autre caractère est retourné inchangé :
```c
char letter = toupper('a'); // 'A'
```

# --instructions--

Affichez `Ab` en convertissant `first` en majuscule et `second` en minuscule

# --seed--

```c
#include <stdio.h>
#include <ctype.h>

int main() {
    char first = 'a';
    char second = 'B';
    printf("%c%c\n", [/](first), [/](second));
    return 0;
}
```

# --answers--

- toupper
- tolower
- upper
- lower
- capitalize

# --solutions--

```c
#include <stdio.h>
#include <ctype.h>

int main() {
    char first = 'a';
    char second = 'B';
    printf("%c%c\n", toupper(first), tolower(second));
    return 0;
}
```

# --output--

Ab
