---
language: c
exerciseType: 2
---

# --description--

Tout comme dans l'addition assignment, nous avons le **decrement assignment** `-=`.
La fonctionnalité est la même, la seule différence est qu'il effectue la soustraction.
Donc les éléments suivants sont exactement les mêmes
```c
num = num - 5;
// is equal to
num -= 5;
```

# --instructions--

Soustraire à la variable `result` la valeur `15` en utilisant l'opérateur *decrement assignment*, puis afficher sa valeur

# --seed--

```c
int result = 75;
result[/][/];
printf("%d\n", [/]);
```

# --answers--

-  - 
-  -= 
-  =- 
- 50
- 15
- result

# --solutions--

```c
int result = 75;
result -= 15;
printf("%d\n", result);
```

# --output--

60
