---
language: c
exerciseType: 4
---

# --description--

Chaque opérateur bit à bit binaire possède une forme d'**affectation composée** qui met à jour une variable sur place: `&=`, `|=`, `^=`, `<<=` et `>>=`.
```c
unsigned int x = 12;
x &= 10;  // same as x = x & 10;
x |= 1;   // same as x = x | 1;
x ^= 3;   // same as x = x ^ 3;
x <<= 1;  // same as x = x << 1;
x >>= 2;  // same as x = x >> 2;
```
Elles se lisent mieux que de répéter le nom de la variable et constituent la façon habituelle de modifier les bits d'une variable de flags.

# --instructions--

Triez les lignes pour que `flags` commence à `0x01`, que le bit valant `4` soit ensuite activé avec `|=`, que la valeur soit ensuite décalée de 2 rangs vers la gauche avec `<<=` et qu'elle soit enfin affichée

# --answers--

```c
unsigned int flags = 0x01;
```

```c
flags |= 0x04;
```

```c
flags <<= 2;
```

```c
printf("%u\n", flags);
```

# --solutions--

```c
unsigned int flags = 0x01;
flags |= 0x04;
flags <<= 2;
printf("%u\n", flags);
```

# --output--

20
