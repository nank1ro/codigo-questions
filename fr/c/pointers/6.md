---
language: c
exerciseType: 4
---

# --description--

Un pointeur vers `char` fonctionne comme n'importe quel autre pointeur : il contient l'adresse d'un seul caractère, et `*p` lit ou écrit ce caractère :
```c
char grade = 'B';
char *p = &grade;
*p = 'A';
printf("%c\n", grade); // prints "A"
```
La lecture via un pointeur et l'écriture via celui-ci peuvent être mélangées librement : `*p = *p + 1` transforme `'A'` en `'B'`.

# --instructions--

Remets les lignes dans l'ordre pour déclarer `letter` comme `'a'`, faire pointer `p` vers elle, remplacer la lettre via le pointeur par `'b'` et afficher `letter`

# --answers--

```c
char letter = 'a';
```

```c
char *p = &letter;
```

```c
*p = 'b';
```

```c
printf("%c\n", letter);
```


# --solutions--

```c
char letter = 'a';
char *p = &letter;
*p = 'b';
printf("%c\n", letter);
```

# --output--

b
