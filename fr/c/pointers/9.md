---
language: c
exerciseType: 4
---

# --description--

Comme `p + 1` désigne l'élément suivant, `p++` fait avancer le pointeur lui-même vers l'élément suivant. Une boucle peut parcourir un tableau en faisant avancer un pointeur au lieu d'un indice :
```c
int *p = values;
for (int i = 0; i < 3; i++) {
    printf("%d\n", *p);
    p++;
}
```
Chaque tour affiche l'élément vers lequel `p` pointe, puis déplace `p` d'un élément vers l'avant.

# --instructions--

Remets les lignes dans l'ordre pour stocker `1`, `2` et `3` dans `values`, faire pointer `p` vers son premier élément et afficher chaque élément en faisant avancer le pointeur

# --answers--

```c
int values[3] = {1, 2, 3};
```

```c
int *p = values;
```

```c
for (int i = 0; i < 3; i++) {
```

```c
    printf("%d\n", *p);
```

```c
    p++;
```

```c
}
```


# --solutions--

```c
int values[3] = {1, 2, 3};
int *p = values;
for (int i = 0; i < 3; i++) {
    printf("%d\n", *p);
    p++;
}
```

# --output--

1
2
3
