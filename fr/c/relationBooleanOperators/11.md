---
language: c
exerciseType: 2
---

# --instructions--

Vérifiez si `word` n'est **PAS** égal à `dog`, en imprimant le résultat booléen.
Soyez conscient que `strcmp` retourne `0` seulement quand les chaînes sont **égales**, donc ne vous trompez pas en pensant que le 0 qu'il retourne est comme le booléen faux

# --seed--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat")[/][/];
printf("%d\n", [/]);
```

# --answers--

- word
- "dog"
-  == 
-  = 
- dog
-  != 
- 0
- are_words_equal

# --solutions--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat") == 0;
printf("%d\n", are_words_equal);
```

# --output--

1
