---
language: javascript
exerciseType: 4
---

# --description--

L'ordine dentro un commento JSDoc è sempre lo stesso: prima il riepilogo, poi un `@param` per ogni parametro nell'ordine in cui sono dichiarati, e infine `@returns`. L'apertura `/**` e la chiusura ` */` racchiudono tutto, e il commento sta direttamente sopra la funzione che descrive:
```javascript
/**
 * Returns the number of seconds in the given minutes.
 * @param {number} minutes a whole number of minutes
 * @returns {number} minutes multiplied by sixty
 */
function toSeconds(minutes) {
  return minutes * 60;
}
```

# --instructions--

Ordina gli elementi per documentare `twice` con un riepilogo, una riga `@param` e una riga `@returns`, in questo ordine e direttamente sopra la funzione, poi stampa `twice(21)` dopo aver definito la funzione

# --answers--

```javascript
/**
```

```javascript
 * Returns the double of a number.
```

```javascript
 * @param {number} n the number to double
```

```javascript
 * @returns {number} n multiplied by two
```

```javascript
 */
```

```javascript
function twice(n) {
  return n * 2;
}
```

```javascript
console.log(twice(21));
```

# --solutions--

```javascript
/**
 * Returns the double of a number.
 * @param {number} n the number to double
 * @returns {number} n multiplied by two
 */
function twice(n) {
  return n * 2;
}
console.log(twice(21));
```

# --output--

42
