<section id="options">
{
"language": "javascript",
"type": "runCode",
"showRun": false
}
</section>

<section id="description">
Possiamo anche dare alle variabili i valori di altre variabili.
Qui, possiamo dare alla variabile `y` il valore di` x`
```
var x = 5;
var y = x;
console.log(y); // stampa 5
```
</section>
<section id="instructions">
Crea una nuova variabile chiamata `y` e assegnale il valore della variabile` x`.
</section>
<section id="seed">
var x = 1;
// scrivi dopo questa linea
</section>
<section id="tests">
{
    "tests": [
        {
            "type": "assert",
            "assert":"console.assert(y == x, \"y deve essere uguale a x\")"
        }
    ]
}
</section>