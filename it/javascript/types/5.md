---
language: javascript
exerciseType: 1
---

# --description--

`Number()` è rigoroso: l'intera stringa deve essere un numero, altrimenti il risultato è `NaN` ("Not a Number"):
```javascript
console.log(Number("12px"));
// prints NaN
```
`parseInt()` e `parseFloat()` sono più tolleranti: leggono le cifre dall'inizio della stringa, ignorano gli spazi iniziali e si fermano al primo carattere che non fa parte di un numero. `parseInt` conserva solo la parte intera:
```javascript
console.log(parseInt("12px"), parseFloat("1.5kg"));
// prints 12 1.5
console.log(parseInt("3.9em"), parseInt("-4px"));
// prints 3 -4
```
Quando la stringa non inizia con qualcosa che può iniziare un numero (un segno opzionale, poi una cifra), restituiscono anch'esse `NaN`:
```javascript
console.log(parseInt("auto"));
// prints NaN
```
`NaN` è l'unico valore che non è uguale a se stesso, quindi `x === NaN` è sempre `false`; per rilevarlo usa `Number.isNaN(x)`.

# --instructions--

Scrivi una funzione `toPixels(text)` che restituisca il numero intero all'inizio di `text` (come `12` per `"12px"`), oppure `0` quando `text` non inizia con un numero, usando `parseInt`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function toPixels(text) {

}
```

# --asserts--

`toPixels("12px")` deve restituire `12`.

```javascript
tryCatch(toPixels("12px") === 12);
```

`toPixels("3.9em")` deve restituire `3`.

```javascript
tryCatch(toPixels("3.9em") === 3);
```

`toPixels("-4px")` deve restituire `-4`.

```javascript
tryCatch(toPixels("-4px") === -4);
```

`toPixels("auto")` deve restituire `0`.

```javascript
tryCatch(toPixels("auto") === 0);
```

`toPixels("")` deve restituire `0`.

```javascript
tryCatch(toPixels("") === 0);
```

La funzione deve usare `parseInt`.

```javascript
tryCatch(/parseInt\(/.test(toPixels.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function toPixels(text) {
  const value = parseInt(text);
  if (Number.isNaN(value)) {
    return 0;
  }
  return value;
}
```
