---
language: javascript
exerciseType: 1
---

# --description--

Quando JavaScript ha bisogno di un booleano, per esempio in una condizione `if` o in `Boolean(value)`, converte il valore. Solo otto valori diventano `false`; sono chiamati **falsy**:
`false`, `0`, `-0`, `0n`, `""`, `null`, `undefined` e `NaN`.
**Tutto il resto è truthy**, compresi alcuni valori che sembrano vuoti:
```javascript
console.log(Boolean(0), Boolean(""), Boolean(NaN));
// prints false false false
console.log(Boolean("0"), Boolean("false"), Boolean([]), Boolean({}));
// prints true true true true
```
`"0"` è una stringa non vuota, quindi è truthy; un array vuoto è un oggetto, quindi è truthy anch'esso.

# --instructions--

Scrivi una funzione `countTruthy(values)` che restituisca quanti elementi dell'array `values` sono truthy

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function countTruthy(values) {

}
```

# --asserts--

`countTruthy([1, "a", true])` deve restituire `3`.

```javascript
tryCatch(countTruthy([1, "a", true]) === 3);
```

`countTruthy([0, "", null, undefined, NaN, false])` deve restituire `0`.

```javascript
tryCatch(countTruthy([0, "", null, undefined, NaN, false]) === 0);
```

`countTruthy(["0", "false", [], {}])` deve restituire `4`.

```javascript
tryCatch(countTruthy(["0", "false", [], {}]) === 4);
```

`countTruthy([0n, -0, " "])` deve restituire `1`, perché solo la stringa con lo spazio è truthy.

```javascript
tryCatch(countTruthy([0n, -0, " "]) === 1);
```

`countTruthy([])` deve restituire `0`.

```javascript
tryCatch(countTruthy([]) === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function countTruthy(values) {
  let count = 0;
  for (const value of values) {
    if (value) {
      count++;
    }
  }
  return count;
}
```
