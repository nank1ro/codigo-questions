---
language: javascript
exerciseType: 2
---

# --description--

Puoi convertire un valore in un altro tipo **esplicitamente** chiamando il tipo come una funzione:
- `Number(value)` converte in un numero: `Number("42")` è `42`
- `String(value)` converte in una stringa: `String(42)` è `"42"`
- `Boolean(value)` converte in un booleano: `Boolean("")` è `false`

Il risultato è un valore completamente nuovo; l'originale non viene modificato:
```javascript
const input = "7";
const count = Number(input) + 1;
console.log(count, typeof count);
// prints 8 number
console.log(String(count) + "!");
// prints 8!
```
Convertire esplicitamente rende visibile la tua intenzione: chi legge `Number(input)` sa che `input` era testo.

# --instructions--

Completa il codice in modo che `count` sia `input` convertito in un numero più `1`, `label` sia `count` convertito in una stringa e ripetuto due volte, e `flag` sia la stringa vuota convertita in un booleano, producendo l'output `43 4343 false`

# --seed--

```javascript
const input = "42";
const count = [/](input) + 1;
const label = [/](count).repeat(2);
const flag = [/]("");
console.log(count, label, flag);
```

# --answers--

- Number
- String
- Boolean
- Int
- Bool
- toString

# --solutions--

```javascript
const input = "42";
const count = Number(input) + 1;
const label = String(count).repeat(2);
const flag = Boolean("");
console.log(count, label, flag);
```

# --output--

43 4343 false
