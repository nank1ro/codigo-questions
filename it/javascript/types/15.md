---
language: javascript
exerciseType: 1
---

# --description--

Poiché `typeof` risponde `"object"` per array, oggetti e `null`, distinguerli richiede due controlli extra.
`Array.isArray(value)` è `true` solo per gli array:
```javascript
console.log(Array.isArray([1, 2]), Array.isArray({}));
// prints true false
```
Per `null` confronta direttamente, `value === null`. Combinandli si ottiene un quadro completo di qualsiasi valore:
```javascript
function kind(value) {
  if (value === null) return "null";
  if (Array.isArray(value)) return "array";
  return typeof value;
}
```
Controlla prima `null` e gli array, perché il semplice `typeof` non può distinguerli.

# --instructions--

Scrivi una funzione `typeName(value)` che restituisca `"null"` per `null`, `"array"` per gli array e il risultato di `typeof` per ogni altro valore, usando `Array.isArray`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function typeName(value) {

}
```

# --asserts--

`typeName(null)` deve restituire `"null"`.

```javascript
tryCatch(typeName(null) === "null");
```

`typeName([1, 2])` e `typeName([])` devono restituire `"array"`.

```javascript
tryCatch(typeName([1, 2]) === "array" && typeName([]) === "array");
```

`typeName({})` deve restituire `"object"`.

```javascript
tryCatch(typeName({}) === "object");
```

`typeName(42)`, `typeName("a")` e `typeName(true)` devono restituire `"number"`, `"string"` e `"boolean"`.

```javascript
tryCatch(typeName(42) === "number" && typeName("a") === "string" && typeName(true) === "boolean");
```

`typeName(undefined)`, `typeName(10n)` e `typeName(Symbol())` devono restituire `"undefined"`, `"bigint"` e `"symbol"`.

```javascript
tryCatch(typeName(undefined) === "undefined" && typeName(10n) === "bigint" && typeName(Symbol()) === "symbol");
```

`typeName(function () {})` deve restituire `"function"`.

```javascript
tryCatch(typeName(function () {}) === "function");
```

La funzione deve usare `Array.isArray`.

```javascript
tryCatch(/Array\.isArray\(/.test(typeName.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function typeName(value) {
  if (value === null) {
    return "null";
  }
  if (Array.isArray(value)) {
    return "array";
  }
  return typeof value;
}
```
