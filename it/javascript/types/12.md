---
language: javascript
exerciseType: 4
---

# --description--

JavaScript ha un unico tipo `number` per interi e decimali: ogni numero è un valore in virgola mobile a 64 bit (un *double*). Quindi `5` e `5.0` sono lo stesso valore, e non esiste un tipo intero separato:
```javascript
console.log(5 === 5.0, 10 / 2);
// prints true 5
```
Per chiedere se un numero non ha parte frazionaria, usa `Number.isInteger`:
```javascript
console.log(Number.isInteger(5.0), Number.isInteger(3.5));
// prints true false
```
Le stringhe template convertono il valore interpolato in una stringa con le stesse regole di `String()`, quindi `${5.0}` diventa `"5"`, non `"5.0"`.

# --instructions--

Ordina le righe per definire `half`, che divide `n` per due e restituisce una frase che dice se il risultato è intero, poi stampa il risultato per `10` e per `7`. Definisci prima la funzione

# --answers--

```javascript
function half(n) {
```

```javascript
  const result = n / 2;
```

```javascript
  if (Number.isInteger(result)) {
```

```javascript
    return `${result} is whole`;
```

```javascript
  }
```

```javascript
  return `${result} is not whole`;
```

```javascript
}
```

```javascript
console.log(half(10));
```

```javascript
console.log(half(7));
```

# --solutions--

```javascript
function half(n) {
  const result = n / 2;
  if (Number.isInteger(result)) {
    return `${result} is whole`;
  }
  return `${result} is not whole`;
}
console.log(half(10));
console.log(half(7));
```

# --output--

5 is whole
3.5 is not whole
