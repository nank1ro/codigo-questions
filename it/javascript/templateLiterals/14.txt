<section id="options">
{
"language": "javascript",
"type": "answerButtons"
}
</section>
<section id="instructions">
Cosa c'è di sbagliato in questo codice?
```
console.log(`$(10} amici`);
```
</section>
<section id="answerButtons">
{
	"buttons": ["L'interpolazione di stringa deve essere tra ${}","Il valore all'interno di ${} non è un numero","Alla stringa mancano le sue ``","Il valore all'interno di ${} deve essere un nome di variabile"],
	"correct": "L'interpolazione di stringa deve essere tra ${}"
}
</section>