---
language: javascript
exerciseType: 1
---

# --description--

Per verificare se una stringa ne contiene un'altra, usa questi metodi, che restituiscono tutti un booleano:
- `includes(text)` è `true` se `text` compare da qualche parte
- `startsWith(text)` è `true` se la stringa inizia con `text`
- `endsWith(text)` è `true` se la stringa termina con `text`

```javascript
let file = "photo.png";
console.log(file.includes("."));
// prints true
console.log(file.startsWith("ph"));
// prints true
console.log(file.endsWith(".jpg"));
// prints false
```
Il confronto distingue tra maiuscole e minuscole: `"Hello".includes("h")` è `false`.

# --instructions--

Scrivi una funzione `isImage(fileName)` che restituisca `true` quando `fileName` termina con `".png"` e `false` altrimenti

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isImage(fileName) {

}
```

# --asserts--

`isImage("cat.png")` deve restituire `true`.

```javascript
tryCatch(isImage("cat.png") === true);
```

`isImage("cat.jpg")` deve restituire `false`.

```javascript
tryCatch(isImage("cat.jpg") === false);
```

`isImage("png.txt")` deve restituire `false`.

```javascript
tryCatch(isImage("png.txt") === false);
```

`isImage("a.png.txt")` deve restituire `false`.

```javascript
tryCatch(isImage("a.png.txt") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isImage(fileName) {
  return fileName.endsWith(".png");
}
```
