---
language: javascript
exerciseType: 2
---

# --description--

Le stringhe hanno molti **metodi** integrati. Due dei più semplici cambiano il maiuscolo/minuscolo di ogni lettera:
```javascript
let word = "Hello";
console.log(word.toUpperCase());
// prints HELLO
console.log(word.toLowerCase());
// prints hello
```
Entrambi i metodi non richiedono argomenti, quindi ricordati le parentesi.

# --instructions--

Stampa `name` in maiuscolo, poi in minuscolo

# --seed--

```javascript
let name = "Ada";
console.log(name.[/]());
console.log(name.[/]());
```

# --answers--

- toUpperCase
- toLowerCase
- upper
- lower
- capitalize

# --solutions--

```javascript
let name = "Ada";
console.log(name.toUpperCase());
console.log(name.toLowerCase());
```

# --output--

ADA
ada
