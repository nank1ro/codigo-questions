---
language: javascript
exerciseType: 1
---

# --description--

L'operatore **`instanceof`** chiede se un oggetto è stato costruito a partire da una classe, o da qualsiasi classe che erediti da essa:
```javascript
class Animal {}
class Dog extends Animal {}
const max = new Dog();
console.log(max instanceof Dog);    // true
console.log(max instanceof Animal); // true, Dog extends Animal
```
JavaScript non ha una parola chiave `abstract`, ma la stessa idea si scrive a mano: una classe base definisce la forma ed ogni metodo che una figlia *deve* fornire semplicemente lancia un errore:
```javascript
class Shape {
    area() {
        throw new Error("area() must be implemented");
    }
}
```
Una figlia che dimentica di fare l'override di `area` fallisce rumorosamente la prima volta che viene usata, invece di restituire silenziosamente `undefined`.

# --instructions--

Scrivi una classe `Shape` il cui metodo `area` lancia un `Error`, e una classe `Square` che la estende, riceve un `side` e sovrascrive `area` per restituire l'area del quadrato

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Shape {
  
}

class Square {
  
}
```

# --asserts--

`new Square(3).area()` deve restituire `9`.

```javascript
tryCatch(new Square(3).area() === 9);
```

`new Square(5).area()` deve restituire `25`.

```javascript
tryCatch(new Square(5).area() === 25);
```

Un quadrato deve essere uno `Shape`.

```javascript
tryCatch(new Square(3) instanceof Shape === true);
```

Chiamare `area` sulla classe base deve lanciare un errore.

```javascript
let _threw = false;
try {
  new Shape().area();
} catch (e) {
  _threw = true;
}
tryCatch(_threw === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Shape {
  area() {
    throw new Error("area() must be implemented");
  }
}

class Square extends Shape {
  constructor(side) {
    super();
    this.side = side;
  }
  area() {
    return this.side * this.side;
  }
}
```
