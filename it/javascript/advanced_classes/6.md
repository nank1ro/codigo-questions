---
language: javascript
exerciseType: 4
---

# --description--

L'ordine conta quando le classi ereditano l'una dall'altra. Una dichiarazione `class` **non** viene sollevata (hoisted) come avviene per una `function`: il nome esiste solo dalla riga in cui la classe è scritta in poi. Quindi una classe figlia deve comparire *dopo* il genitore che estende, altrimenti la clausola `extends` fallisce con un `ReferenceError`.

# --instructions--

Ordina gli elementi in un programma funzionante. Ricorda che la classe genitore deve essere definita prima della figlia

# --answers--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
```

```javascript
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
```

```javascript
console.log(new Car().describe());
```

# --solutions--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
console.log(new Car().describe());
```

# --output--

a vehicle with four wheels
