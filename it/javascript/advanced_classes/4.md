---
language: javascript
exerciseType: 1
---

# --description--

Quando una classe figlia definisce un metodo che il genitore ha già, vince la versione della figlia. Questo si chiama **override** (sovrascrittura):
```javascript
class Animal {
    speak() {
        return "some sound";
    }
}
class Dog extends Animal {
    speak() {
        return "Woof";
    }
}
console.log(new Dog().speak());
// stampa Woof
```
L'override non elimina la versione del genitore, la nasconde soltanto. Dentro il metodo della figlia, `super.methodName(...)` raggiunge comunque quella del genitore, il che ti permette di estendere il comportamento del genitore invece di sostituirlo:
```javascript
class Puppy extends Dog {
    speak() {
        return super.speak() + "!";
    }
}
console.log(new Puppy().speak());
// stampa Woof!
```
Nota la differenza: `super(...)` chiama il **costruttore** del genitore, `super.name(...)` chiama un **metodo** del genitore.

# --instructions--

Scrivi una classe `LoudGreeter` che estende `Greeter` e sovrascrive `greet` in modo che restituisca il saluto del genitore in maiuscolo seguito da un punto esclamativo

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Greeter {
  constructor(name) {
    this.name = name;
  }
  greet() {
    return `Hello ${this.name}`;
  }
}

class LoudGreeter {
  
}
```

# --asserts--

`new LoudGreeter("Ann").greet()` deve restituire `"HELLO ANN!"`.

```javascript
tryCatch(new LoudGreeter("Ann").greet() === "HELLO ANN!");
```

`new LoudGreeter("bob").greet()` deve restituire `"HELLO BOB!"`.

```javascript
tryCatch(new LoudGreeter("bob").greet() === "HELLO BOB!");
```

Il genitore deve restare intoccato.

```javascript
tryCatch(new Greeter("Ann").greet() === "Hello Ann");
```

`LoudGreeter` deve riutilizzare il metodo del genitore tramite `super`.

```json
{
  "regex": "super\\.greet\\s*\\(\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Greeter {
  constructor(name) {
    this.name = name;
  }
  greet() {
    return `Hello ${this.name}`;
  }
}

class LoudGreeter extends Greeter {
  greet() {
    return super.greet().toUpperCase() + "!";
  }
}
```
