---
language: javascript
exerciseType: 2
---

# --description--

Sai già che `extends` rende una classe figlia di un'altra. Ciò che quella parola chiave ti dà davvero è l'**ereditarietà**: la figlia riceve gratuitamente ogni proprietà e metodo del genitore, e può aggiungerne di propri sopra.

Il pezzo che rende utile l'ereditarietà è **`super`**. Dentro il costruttore di una classe figlia, `super(...)` chiama il costruttore del genitore, così il genitore può impostare la parte dell'oggetto che gli appartiene:
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
```
Qui `super(name)` passa `name` ad `Animal`, che lo memorizza, e `Dog` deve preoccuparsi solo di `breed`.

# --instructions--

Completa il codice in modo che `Dog` erediti da `Animal` e passi `name` al costruttore del genitore

# --seed--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog [/] Animal {
    constructor(name, breed) {
        [/](name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --answers--

- extends
- inherits
- super
- parent
- new Animal

# --solutions--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --output--

Max is a Beagle
