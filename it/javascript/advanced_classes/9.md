---
language: javascript
exerciseType: 4
---

# --description--

Un uso molto comune di un metodo statico è la **factory** (fabbrica): un metodo che costruisce un'istanza a partire da un'altra forma di dati e la restituisce. Mantiene `new` in un unico posto e dà alla costruzione un nome che dice cosa fa:
```javascript
class Duration {
    constructor(seconds) {
        this.seconds = seconds;
    }
    static fromMinutes(minutes) {
        return new Duration(minutes * 60);
    }
}
console.log(Duration.fromMinutes(2).seconds);
// prints 120
```
Una factory può essere chiamata prima che esista qualsiasi istanza, cosa che un metodo normale non potrebbe fare.

# --instructions--

Ordina gli elementi in un programma funzionante che costruisce un punto a partire da un array e stampa le sue coordinate

# --answers--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
```

```javascript
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
```

```javascript
const p = Point.fromArray([3, 4]);
```

```javascript
console.log(`${p.x},${p.y}`);
```

# --solutions--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
const p = Point.fromArray([3, 4]);
console.log(`${p.x},${p.y}`);
```

# --output--

3,4
