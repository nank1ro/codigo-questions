---
language: javascript
exerciseType: 1
---

# --description--

Per riscrivere **ogni** corrispondenza invece della prima hai due opzioni:
```javascript
console.log("a1 b2".replace(/\d/g, "#"));
// prints a# b#
console.log("a1 b2".replaceAll(/\d/g, "#"));
// prints a# b#
```
**`replaceAll`** è il più chiaro dei due, e accetta anche una semplice stringa come pattern. Quando gli dai una regex, quella regex **deve** portare il flag `g`, altrimenti lancia un `TypeError`; è esattamente ciò che previene il bug silenzioso di usare `replace` e sistemare solo la prima corrispondenza.

# --instructions--

Scrivi una funzione `redact` che prende una stringa `text` e la restituisce con ogni sequenza di cifre sostituita da un singolo `#`, usando `replaceAll`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function redact(text) {
  
}
```

# --asserts--

Il metodo `replaceAll` deve essere usato.

```json
{
  "regex": "\\.replaceAll\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`redact("call 123 or 45")` deve restituire `call # or #`.

```javascript
tryCatch(redact("call 123 or 45") === "call # or #");
```

`redact("no digits")` deve restituire il testo invariato.

```javascript
tryCatch(redact("no digits") === "no digits");
```

`redact("2026")` deve restituire `#`.

```javascript
tryCatch(redact("2026") === "#");
```

`redact("a1b2c3")` deve restituire `a#b#c#`.

```javascript
tryCatch(redact("a1b2c3") === "a#b#c#");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function redact(text) {
  return text.replaceAll(/\d+/g, "#");
}
```
