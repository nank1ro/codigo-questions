---
language: javascript
exerciseType: 1
---

# --description--

I pattern diventano utili quando descrivono un *tipo* di carattere invece di un singolo carattere esatto. Poche **sequenze di escape** coprono la maggior parte delle esigenze:
- `\d` una cifra qualsiasi, da `0` a `9`
- `\w` un carattere di parola qualsiasi: una lettera, una cifra o `_`
- `\s` uno spazio bianco qualsiasi: uno spazio, una tabulazione, un ritorno a capo

```javascript
console.log(/\d/.test("room 12"));
// prints true
console.log(/\d/.test("lobby"));
// prints false
```
Un **quantificatore** dice quante volte può ripetersi il pezzo precedente. Il più comune è `+`, che significa "uno o più":
```javascript
console.log(/\d+/.test("42"));
// prints true
```
Quindi `/\d/` corrisponde a una singola cifra e `/\d+/` corrisponde a una sequenza di cifre. Per un semplice `test` i due si comportano allo stesso modo, perché a entrambi basta che sia presente una cifra.

# --instructions--

Scrivi una funzione `hasDigit` che prende una stringa `text` e restituisce `true` quando `text` contiene almeno una cifra, usando `test`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function hasDigit(text) {
  
}
```

# --asserts--

Il metodo `test` deve essere usato.

```json
{
  "regex": "\\.test\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`hasDigit("room 12")` deve restituire `true`.

```javascript
tryCatch(hasDigit("room 12") === true);
```

`hasDigit("lobby")` deve restituire `false`.

```javascript
tryCatch(hasDigit("lobby") === false);
```

`hasDigit("7")` deve restituire `true`.

```javascript
tryCatch(hasDigit("7") === true);
```

`hasDigit("")` deve restituire `false`.

```javascript
tryCatch(hasDigit("") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function hasDigit(text) {
  return /\d/.test(text);
}
```
