---
language: javascript
exerciseType: 2
---

# --description--

`delete(key)` restituisce `true` quando una coppia è stata rimossa e `false` quando la chiave non era presente.
Per rimuovere **tutte** le coppie in una volta, chiama `clear()`:
```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Rimuovi `"pen"` da `cart` stampando il risultato della rimozione, poi svuota l'intera map e stampa quante coppie rimangono

# --seed--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
