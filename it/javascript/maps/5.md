---
language: javascript
exerciseType: 2
---

# --description--

Una map ricorda l'ordine in cui le coppie sono state aggiunte, e puoi scorrerla con `for...of`.
Il metodo `entries()` fornisce ogni coppia come un array `[key, value]`, che puoi destrutturare direttamente nel ciclo:
```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
// prints apple: 3
// prints pear: 5
```
Scorrere direttamente la map, `for (const [name, qty] of stock)`, fa esattamente la stessa cosa.

# --instructions--

Scorri le voci di `stock` e stampa ogni nome seguito dalla sua quantità

# --seed--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [/] of stock.[/]()) {
  console.log(`${name}: ${qty}`);
}
```

# --answers--

- [name, qty]
- [qty, name]
- name, qty
- entries
- pairs
- items

# --solutions--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
```

# --output--

apple: 3
pear: 5
