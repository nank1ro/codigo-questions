---
language: javascript
exerciseType: 1
---

# --description--

Il valore memorizzato in ciascun membro dipende da te. Le **stringhe** sono la scelta più comune perché sono leggibili quando vengono stampate, registrate o salvate in un file:
```javascript
const Status = Object.freeze({
  ACTIVE: "active",
  DONE: "done",
});
console.log(Status.DONE);
// prints done
```
Una volta congelato, l'oggetto non può nemmeno ricevere nuove proprietà, e `Object.isFrozen(obj)` ti dice se un oggetto è stato congelato:
```javascript
console.log(Object.isFrozen(Status));
// prints true
```

# --instructions--

Dichiara l'enumerazione congelata `Direction` con i membri `UP`, `DOWN`, `LEFT` e `RIGHT`, ciascuno con il proprio nome in minuscolo (`"up"`, `"down"`, `"left"`, `"right"`)

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Direction = 
```

# --asserts--

`Direction.UP` deve essere `"up"`.

```javascript
tryCatch(Direction.UP === "up");
```

`Direction.DOWN` deve essere `"down"`.

```javascript
tryCatch(Direction.DOWN === "down");
```

`Direction.LEFT` deve essere `"left"` e `Direction.RIGHT` deve essere `"right"`.

```javascript
tryCatch(Direction.LEFT === "left" && Direction.RIGHT === "right");
```

`Direction` deve avere esattamente quattro membri.

```javascript
tryCatch(Object.keys(Direction).length === 4);
```

`Direction` deve essere congelato.

```javascript
tryCatch(Object.isFrozen(Direction) === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Direction = Object.freeze({
  UP: "up",
  DOWN: "down",
  LEFT: "left",
  RIGHT: "right",
});
```
