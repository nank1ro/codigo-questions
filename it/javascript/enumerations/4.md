---
language: javascript
exerciseType: 2
---

# --description--

I membri possono anche contenere **numeri**. I valori numerici sono utili quando i membri hanno un ordine naturale, perché puoi confrontarli con gli operatori consueti:
```javascript
const Priority = Object.freeze({
  LOW: 1,
  MEDIUM: 2,
  HIGH: 3,
});
console.log(Priority.HIGH > Priority.LOW);
// prints true
```
Il compromesso è la leggibilità: stampare `Priority.HIGH` mostra `3`, che ti dice molto meno di quanto farebbe la stringa `"high"`.

# --instructions--

Dichiara l'enumerazione numerica congelata `Level` con `EASY` uguale a `1`, `NORMAL` uguale a `2` e `HARD` uguale a `3`, poi stampa se `HARD` è maggiore di `EASY` e la differenza tra `HARD` e `EASY`

# --seed--

```javascript
const Level = Object.freeze({
  EASY: [/],
  NORMAL: 2,
  HARD: [/],
});
console.log(Level.HARD [/] Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --answers--

- 1
- 3
- >
- 0
- <
- ===

# --solutions--

```javascript
const Level = Object.freeze({
  EASY: 1,
  NORMAL: 2,
  HARD: 3,
});
console.log(Level.HARD > Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --output--

true
2
