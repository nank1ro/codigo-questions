---
language: javascript
exerciseType: 1
---

# --description--

Le enumerazioni si abbinano naturalmente all'istruzione `switch`, che confronta un valore con un elenco di etichette `case` ed esegue il codice della prima corrispondente.
Ogni ramo termina con `return` o `break`, e il ramo opzionale `default` viene eseguito quando nulla corrisponde:
```javascript
const Light = Object.freeze({ RED: "red", GREEN: "green" });

function action(light) {
  switch (light) {
    case Light.RED:
      return "stop";
    case Light.GREEN:
      return "go";
    default:
      return "unknown";
  }
}
console.log(action(Light.GREEN));
// prints go
```
Confronta sempre con i membri (`Light.RED`), mai con i valori grezzi (`"red"`): se il valore cambia in futuro, lo `switch` continua a funzionare.

# --instructions--

Scrivi una funzione `weekend(day)` che usi uno `switch` sull'enumerazione `Day` e restituisca `true` per `Day.SATURDAY` e `Day.SUNDAY`, e `false` per qualsiasi altro valore

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {

}
```

# --asserts--

`weekend(Day.SATURDAY)` deve restituire `true`.

```javascript
tryCatch(weekend(Day.SATURDAY) === true);
```

`weekend(Day.SUNDAY)` deve restituire `true`.

```javascript
tryCatch(weekend(Day.SUNDAY) === true);
```

`weekend(Day.MONDAY)` deve restituire `false`.

```javascript
tryCatch(weekend(Day.MONDAY) === false);
```

`weekend("holiday")` deve restituire `false`, perché non è un membro.

```javascript
tryCatch(weekend("holiday") === false);
```

La funzione deve usare un'istruzione `switch`.

```javascript
tryCatch(/switch\s*\(/.test(weekend.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {
  switch (day) {
    case Day.SATURDAY:
    case Day.SUNDAY:
      return true;
    default:
      return false;
  }
}
```
