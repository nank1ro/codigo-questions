---
language: javascript
exerciseType: 4
---

# --description--

Una funzione `async` si legge dall'alto verso il basso come qualsiasi altra funzione: `await` semplicemente la mette in pausa finché la promessa attesa viene adempiuta, poi l'esecuzione continua alla riga successiva.
```javascript
async function main() {
  console.log("start");
  const value = await Promise.resolve("data");
  console.log(value);
  console.log("done");
}

main();
// prints start, then data, then done
```
Notate l'ultima riga: una funzione `async` deve comunque essere **chiamata**. Scrivere `main` senza le parentesi definisce il lavoro ma non lo avvia mai, e non viene stampato nulla.

# --instructions--

Ordina gli elementi in un programma che definisce una funzione `async` `load` che restituisce `data`, poi una funzione `async` `main` che stampa `start`, aspetta `load()` in `value`, stampa `value` e infine stampa `done`. Definisci prima `load`, poi `main`, e chiama `main()` sull'ultima riga

# --answers--

```javascript
async function load() {
  return "data";
}
```

```javascript
async function main() {
```

```javascript
  console.log("start");
```

```javascript
  const value = await load();
```

```javascript
  console.log(value);
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
async function load() {
  return "data";
}
async function main() {
  console.log("start");
  const value = await load();
  console.log(value);
  console.log("done");
}
main();
```

# --output--

start
data
done
