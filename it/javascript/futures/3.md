---
language: javascript
exerciseType: 1
---

# --description--

Quando il valore arriva davvero più tardi, costruisci la promessa da te con **`new Promise`**. Prende una funzione, che riceve una callback **`resolve`**: chiama `resolve(value)` quando il valore è pronto, e la promessa viene adempiuta con esso.
```javascript
const soon = new Promise((resolve) => {
  setTimeout(() => resolve("done"), 1000);
});
```
`setTimeout(callback, ms)` pianifica l'esecuzione di `callback` dopo `ms` millisecondi e restituisce subito, quindi nel frattempo non viene bloccato nulla.

La funzione passata a `new Promise` viene eseguita subito, ma la promessa resta **in sospeso** finché `resolve` non viene chiamato. Fare `await` su di essa dà il valore:
```javascript
async function main() {
  console.log(await soon);
  // prints done, about one second later
}

main();
```

# --instructions--

Scrivi una funzione `delay` che prende un numero di millisecondi `ms` e un `value`, e restituisce una promessa adempiuta con `value` dopo `ms` millisecondi. Usa `new Promise` e `setTimeout`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function delay(ms, value) {
  
}
```

# --asserts--

`delay(10, "ok")` deve restituire una promessa.

```javascript
(async () => {
  tryCatch(delay(10, "ok") instanceof Promise);
})().catch(() => tryCatch(false));
```

`await delay(0, 7)` deve essere `7`.

```javascript
(async () => {
  tryCatch((await delay(0, 7)) === 7);
})().catch(() => tryCatch(false));
```

`await delay(10, "ok")` deve essere `"ok"`.

```javascript
(async () => {
  tryCatch((await delay(10, "ok")) === "ok");
})().catch(() => tryCatch(false));
```

`await delay(60, "x")` deve richiedere almeno `50` millisecondi.

```javascript
(async () => {
  const start = Date.now();
  await delay(60, "x");
  tryCatch(Date.now() - start >= 50);
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function delay(ms, value) {
  return new Promise((resolve) => {
    setTimeout(() => resolve(value), ms);
  });
}
```
