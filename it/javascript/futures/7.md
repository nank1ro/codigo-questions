---
language: javascript
exerciseType: 1
---

# --description--

Anche il lavoro asincrono può fallire. La funzione data a `new Promise` riceve una seconda callback, **`reject`**: chiama `reject(error)` e la promessa diventa rifiutata invece che adempiuta.
```javascript
function readAge(age) {
  return new Promise((resolve, reject) => {
    if (age >= 0) {
      resolve(age);
    } else {
      reject(new Error("negative age"));
    }
  });
}
```
Rifiuta sempre con un oggetto `Error`: porta con sé un `message` e uno stack trace, cosa che una stringa nuda non fa.

Un rifiuto si legge con **`.catch(callback)`**, l'immagine speculare di `.then`. **`Promise.reject(error)`** costruisce una promessa che è già rifiutata, proprio come `Promise.resolve` ne costruisce una adempiuta:
```javascript
readAge(-1).catch((error) => {
  console.log(error.message);
  // prints negative age
});
```
Chiamare `resolve` e `reject` entrambi, o due volte, non cambia nulla: conta solo la prima chiamata.

# --instructions--

Scrivi una funzione `checkAge` che prende un numero `age` e restituisce una promessa adempiuta con `age` quando `age` è almeno `18`, e rifiutata con `new Error("too young")` altrimenti. Usa `new Promise`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function checkAge(age) {
  
}
```

# --asserts--

`await checkAge(20)` deve essere `20`.

```javascript
(async () => {
  tryCatch((await checkAge(20)) === 20);
})().catch(() => tryCatch(false));
```

`await checkAge(18)` deve essere `18`, perché `18` è abbastanza grande.

```javascript
(async () => {
  tryCatch((await checkAge(18)) === 18);
})().catch(() => tryCatch(false));
```

`checkAge(10)` deve essere rifiutata con il messaggio `"too young"`.

```javascript
(async () => {
  let message = "";
  await checkAge(10).catch((error) => { message = error.message; });
  tryCatch(message === "too young");
})().catch(() => tryCatch(false));
```

`checkAge(0)` deve essere rifiutata con un oggetto `Error`, non con una stringa.

```javascript
(async () => {
  let reason = null;
  await checkAge(0).catch((error) => { reason = error; });
  tryCatch(reason instanceof Error);
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function checkAge(age) {
  return new Promise((resolve, reject) => {
    if (age >= 18) {
      resolve(age);
    } else {
      reject(new Error("too young"));
    }
  });
}
```
