---
language: javascript
exerciseType: 1
---

# --description--

A common use of `try` / `catch` around `await` is to replace a failure with a sensible default, so the caller never has to deal with the error:
```javascript
async function sizeOf(path) {
  try {
    return await measure(path);
  } catch (error) {
    return 0;
  }
}
```
Keep the `await` in front of `measure(path)` even though the value is returned straight away. Without it the promise leaves the function without ever passing through the `try` block, and a rejection would escape the `catch`.

# --instructions--

The function `parseLater` returns a promise fulfilled with the number written in `text`, or rejected when `text` is not a number. Write an `async` function `safeParse` that returns that number, or `-1` when `parseLater` fails. Use `try` and `catch`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function parseLater(text) {
  return new Promise((resolve, reject) => {
    const value = Number(text);
    if (Number.isNaN(value)) {
      reject(new Error(`not a number: ${text}`));
    } else {
      resolve(value);
    }
  });
}

async function safeParse(text) {
  
}
```

# --asserts--

`await safeParse("12")` must be `12`.

```javascript
(async () => {
  tryCatch((await safeParse("12")) === 12);
})().catch(() => tryCatch(false));
```

`await safeParse("-7.5")` must be `-7.5`.

```javascript
(async () => {
  tryCatch((await safeParse("-7.5")) === -7.5);
})().catch(() => tryCatch(false));
```

`await safeParse("abc")` must be `-1`.

```javascript
(async () => {
  tryCatch((await safeParse("abc")) === -1);
})().catch(() => tryCatch(false));
```

`safeParse("oops")` must be fulfilled, never rejected.

```javascript
(async () => {
  let ok = false;
  await safeParse("oops").then(() => { ok = true; }, () => { ok = false; });
  tryCatch(ok);
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function parseLater(text) {
  return new Promise((resolve, reject) => {
    const value = Number(text);
    if (Number.isNaN(value)) {
      reject(new Error(`not a number: ${text}`));
    } else {
      resolve(value);
    }
  });
}

async function safeParse(text) {
  try {
    return await parseLater(text);
  } catch (error) {
    return -1;
  }
}
```
