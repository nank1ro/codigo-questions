---
language: javascript
exerciseType: 1
---

# --description--

Aggiungere `async` davanti a una funzione cambia ciò che restituisce: il corpo calcola ancora un valore ordinario, ma chi chiama riceve una promessa che lo avvolge.
```javascript
function shout(text) {
  return text.toUpperCase();
}
async function shoutLater(text) {
  return text.toUpperCase();
}

console.log(shout("hi"));
// prints HI
console.log(shoutLater("hi"));
// prints Promise { 'HI' }
```
Le due funzioni contengono lo stesso codice; cambia solo il modo di leggere il risultato. `shoutLater("hi")` deve essere oggetto di `await` dentro un'altra funzione `async` per restituire `"HI"`.

Contrassegnare una funzione `async` non costa nulla quando non c'è niente da aspettare, ed è ciò che ti permette di usare `await` al suo interno in seguito.

# --instructions--

Scrivi una funzione `async` `greet` che prende una stringa `name` e restituisce `Hello, ` seguito dal nome e da un punto esclamativo, per esempio `Hello, Ada!`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
async function greet(name) {
  
}
```

# --asserts--

`greet` deve essere dichiarata con la parola chiave `async`.

```json
{
  "regex": "async\\s+function\\s+greet",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`await greet("Ada")` deve essere `"Hello, Ada!"`.

```javascript
(async () => {
  tryCatch((await greet("Ada")) === "Hello, Ada!");
})().catch(() => tryCatch(false));
```

`await greet("Bob")` deve essere `"Hello, Bob!"`.

```javascript
(async () => {
  tryCatch((await greet("Bob")) === "Hello, Bob!");
})().catch(() => tryCatch(false));
```

`await greet("")` deve essere `"Hello, !"`.

```javascript
(async () => {
  tryCatch((await greet("")) === "Hello, !");
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
async function greet(name) {
  return `Hello, ${name}!`;
}
```
