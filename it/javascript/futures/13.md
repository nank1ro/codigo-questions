---
language: javascript
exerciseType: 4
---

# --description--

La differenza tra attesa **sequenziale** e **parallela** dipende da *dove* metti `await`:
```javascript
// sequenziale: circa 300 + 300 = 600 ms
const a = await load("a");
const b = await load("b");

// parallela: circa 300 ms
const [a, b] = await Promise.all([load("a"), load("b")]);
```
Nella prima versione il secondo download parte solo quando il primo è finito, perché `await` mette in pausa la funzione a quella riga. Nella seconda, entrambe le chiamate vengono fatte prima che qualcosa venga atteso, quindi entrambi i download sono già in esecuzione mentre `Promise.all` aspetta.

Usa gli `await` sequenziali solo quando il secondo compito ha davvero bisogno del risultato del primo. Altrimenti avvia tutto per primo e aspetta insieme.

# --instructions--

Ordina gli elementi in un programma che definisce `later`, poi una funzione `async` `main` che stampa `loading`, avvia entrambe le chiamate a `later` insieme con `Promise.all`, stampa i due risultati uniti da uno spazio e infine stampa `done`. Definisci prima `later`, poi `main`, e chiama `main()` sull'ultima riga

# --answers--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
```

```javascript
async function main() {
```

```javascript
  console.log("loading");
```

```javascript
  const results = await Promise.all([later("one", 30), later("two", 10)]);
```

```javascript
  console.log(results.join(" "));
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
async function main() {
  console.log("loading");
  const results = await Promise.all([later("one", 30), later("two", 10)]);
  console.log(results.join(" "));
  console.log("done");
}
main();
```

# --output--

loading
ready two
ready one
one two
done
