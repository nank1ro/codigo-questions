---
language: javascript
exerciseType: 1
---

# --description--

Un pattern può anche stare alla sinistra di un assegnamento normale, senza `const` o `let` davanti, e allora scrive in variabili che esistono già. Questo trasforma lo scambio di due valori in una riga sola, senza variabile temporanea:
```javascript
let a = 1;
let b = 2;
[a, b] = [b, a];
console.log(a, b);
// stampa 2 1
```
Il lato destro viene costruito prima, quindi entrambi i vecchi valori sono già al sicuro nell'array temporaneo quando avviene l'assegnamento. Attento al punto e virgola nella riga precedente: altrimenti una riga che inizia con `[` verrebbe letta come un indice di ciò che la precede.

# --instructions--

Scrivi una funzione `sortPair` che prende due valori e restituisce un array di due elementi con il più piccolo per primo, scambiandoli con un assegnamento di destructuring quando sono nell'ordine sbagliato

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function sortPair(a, b) {
  
}
```

# --asserts--

`sortPair(5, 2)` deve iniziare con `2`.

```javascript
tryCatch(sortPair(5, 2)[0] === 2);
```

`sortPair(5, 2)` deve finire con `5`.

```javascript
tryCatch(sortPair(5, 2)[1] === 5);
```

Una coppia già ordinata deve restare com'è.

```javascript
tryCatch(sortPair(1, 9).join(",") === "1,9");
```

Due valori uguali devono restare come sono.

```javascript
tryCatch(sortPair(4, 4).join(",") === "4,4");
```

Anche i valori negativi devono funzionare.

```javascript
tryCatch(sortPair(3, -8).join(",") === "-8,3");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function sortPair(a, b) {
  if (a > b) {
    [a, b] = [b, a];
  }
  return [a, b];
}
```
