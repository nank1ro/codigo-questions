---
language: javascript
exerciseType: 1
---

# --description--

Anche i pattern di oggetto hanno un rest, e lì raccoglie in un nuovo oggetto tutte le chiavi che il pattern non ha citato:
```javascript
const user = { id: 1, name: "Ada", city: "London" };
const { id, ...profile } = user;
console.log(profile);
// stampa { name: 'Ada', city: 'London' }
```
È il modo più breve per costruire una copia di un oggetto senza una delle sue chiavi: l'originale non viene mai toccato e l'oggetto rest è nuovo e contiene i valori rimanenti.

# --instructions--

Scrivi una funzione `withoutPassword` che restituisce una copia dell'oggetto ricevuto senza la sua chiave `password`, lasciando invariato l'oggetto originale

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function withoutPassword(user) {
  
}
```

# --asserts--

La chiave `password` deve sparire.

```javascript
tryCatch(withoutPassword({ name: "Ada", password: "x" }).password === undefined);
```

Le altre chiavi devono sopravvivere.

```javascript
tryCatch(withoutPassword({ name: "Ada", password: "x" }).name === "Ada");
```

Tutte le altre chiavi devono sopravvivere.

```javascript
tryCatch(Object.keys(withoutPassword({ name: "Ada", password: "x", city: "London" })).length === 2);
```

L'oggetto originale non deve essere modificato.

```javascript
const original = { name: "Bob", password: "y" };
withoutPassword(original);
tryCatch(original.password === "y");
```

Un oggetto con la sola password deve dare un oggetto vuoto.

```javascript
tryCatch(Object.keys(withoutPassword({ password: "z" })).length === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function withoutPassword(user) {
  const { password, ...rest } = user;
  return rest;
}
```
