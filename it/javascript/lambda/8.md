---
language: javascript
exerciseType: 1
---

# --description--

I parametri delle funzioni freccia supportano le stesse funzionalità dei parametri delle funzioni normali.
Un **valore predefinito** viene usato quando l'argomento è omesso o è `undefined`:
```javascript
const greet = (name = "World") => `Hello, ${name}!`;
console.log(greet());
// prints Hello, World!
console.log(greet("Ana"));
// prints Hello, Ana!
```
Nota che un parametro con un valore predefinito ha sempre bisogno delle parentesi, anche quando è l'unico: `name = "World" => ...` è un errore di sintassi.

# --instructions--

Memorizza nella costante `greet` una funzione freccia con un parametro `name` che ha come valore predefinito `"World"` e restituisce la stringa `Hello, <name>!`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const greet = 
```

# --asserts--

`greet()` deve restituire `"Hello, World!"`.

```javascript
tryCatch(greet() === "Hello, World!");
```

`greet("Ana")` deve restituire `"Hello, Ana!"`.

```javascript
tryCatch(greet("Ana") === "Hello, Ana!");
```

`greet(undefined)` deve usare anch'esso il valore predefinito.

```javascript
tryCatch(greet(undefined) === "Hello, World!");
```

`greet` deve essere una funzione freccia.

```javascript
tryCatch(typeof greet === "function" && !/function/.test(greet.toString()));
```

Il valore predefinito deve essere dichiarato nella lista dei parametri.

```javascript
tryCatch(/name\s*=\s*["'`]World["'`]/.test(greet.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const greet = (name = "World") => `Hello, ${name}!`;
```
