---
language: javascript
exerciseType: 1
---

# --description--

Le funzioni freccia diventano più corte in due casi comuni.
Quando il corpo è una **singola espressione**, puoi eliminare le parentesi graffe e la parola chiave `return`: il valore dell'espressione viene restituito automaticamente (un **return implicito**):
```javascript
const add = (a, b) => a + b;
console.log(add(2, 3));
// prints 5
```
Quando c'è **esattamente un parametro**, puoi eliminare anche le parentesi tonde attorno ad esso:
```javascript
const double = n => n * 2;
console.log(double(4));
// prints 8
```
Con zero parametri o con due o più, le parentesi sono obbligatorie: `() => 42` e `(a, b) => a + b`.

# --instructions--

Memorizza nella costante `square` una funzione freccia che restituisce il suo argomento moltiplicato per sé stesso, usando un return implicito (senza parentesi graffe e senza `return`)

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const square = 
```

# --asserts--

`square(3)` deve restituire `9`.

```javascript
tryCatch(square(3) === 9);
```

`square(-4)` deve restituire `16`.

```javascript
tryCatch(square(-4) === 16);
```

`square(0)` deve restituire `0`.

```javascript
tryCatch(square(0) === 0);
```

`square` deve essere una funzione freccia, non una `function`.

```javascript
tryCatch(typeof square === "function" && !/function/.test(square.toString()));
```

`square` deve usare un return implicito.

```javascript
tryCatch(!/return/.test(square.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const square = n => n * n;
```
