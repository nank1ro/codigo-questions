---
language: javascript
exerciseType: 4
---

# --description--

Altri due metodi degli array accettano un callback.
`forEach(callback)` chiama il callback una volta per ogni elemento e non restituisce nulla; usalo per effetti collaterali come la stampa.
`reduce(callback, initialValue)` riduce l'array a un singolo valore: il callback riceve il valore accumulato fino a quel momento e l'elemento corrente, e restituisce il nuovo valore accumulato:
```javascript
const numbers = [1, 2, 3];
numbers.forEach((n) => console.log(n));
// prints 1, 2 and 3 on three lines
const total = numbers.reduce((sum, n) => sum + n, 0);
console.log(total);
// prints 6
```

# --instructions--

Ordina le righe per calcolare il totale di `prices` con `reduce` subito dopo aver dichiarato l'array, stampare ogni prezzo su una riga con `forEach`, e poi stampare il totale

# --answers--

```javascript
const prices = [4, 6, 10];
```

```javascript
const total = prices.reduce((sum, p) => sum + p, 0);
```

```javascript
prices.forEach((p) => console.log(p));
```

```javascript
console.log(total);
```

# --solutions--

```javascript
const prices = [4, 6, 10];
const total = prices.reduce((sum, p) => sum + p, 0);
prices.forEach((p) => console.log(p));
console.log(total);
```

# --output--

4
6
10
20
