---
language: javascript
exerciseType: 4
---

# --instructions--

Ordina le righe per definire `makeCounter`, che restituisce una funzione freccia che incrementa e restituisce il suo `count` privato, poi crea il contatore `next` e stampa il risultato di due chiamate sulla stessa riga

# --answers--

```javascript
const makeCounter = () => {
```

```javascript
  let count = 0;
```

```javascript
  return () => {
```

```javascript
    count += 1;
```

```javascript
    return count;
```

```javascript
  };
```

```javascript
};
```

```javascript
const next = makeCounter();
```

```javascript
console.log(next(), next());
```

# --solutions--

```javascript
const makeCounter = () => {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
};
const next = makeCounter();
console.log(next(), next());
```

# --output--

1 2
