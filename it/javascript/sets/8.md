---
language: javascript
exerciseType: 2
---

# --description--

`delete(value)` restituisce `true` quando il valore è stato rimosso e `false` quando non era nel set.
Per rimuovere **tutti** i valori in una volta, chiama `clear()`:
```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Rimuovi `"pen"` da `cart` stampando il risultato della rimozione, poi svuota l'intero set e stampa quanti valori rimangono

# --seed--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
