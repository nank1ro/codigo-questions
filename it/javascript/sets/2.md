---
language: javascript
exerciseType: 1
---

# --description--

Aggiungere un valore già presente nel set non fa **nulla**: i duplicati vengono semplicemente ignorati.
Altri due elementi essenziali:
- `delete(value)` rimuove il valore dal set
- `size` è il numero di valori memorizzati (una proprietà, quindi senza parentesi)

```javascript
let tags = new Set();
tags.add("js");
tags.add("css");
tags.add("js");
console.log(tags.size);
// prints 2
tags.delete("css");
console.log(tags.size);
// prints 1
```

# --instructions--

Aggiungi `"html"` a `tags`, rimuovi `"css"`, poi memorizza in `hasCss` se `tags` contiene ancora `"css"` e in `count` quanti valori contiene

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
let tags = new Set();
tags.add("js");
tags.add("css");
tags.add("js");
```

# --asserts--

`hasCss` deve essere `false`.

```javascript
tryCatch(hasCss === false);
```

`count` deve essere `2`.

```javascript
tryCatch(count === 2);
```

`tags` deve contenere `"html"`.

```javascript
tryCatch(tags.has("html") === true);
```

`tags` non deve contenere `"css"`.

```javascript
tryCatch(tags.has("css") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
let tags = new Set();
tags.add("js");
tags.add("css");
tags.add("js");
tags.add("html");
tags.delete("css");
let hasCss = tags.has("css");
let count = tags.size;
```
