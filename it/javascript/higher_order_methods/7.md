---
language: javascript
exerciseType: 4
---

# --description--

Il comparatore può guardare qualsiasi parte degli elementi, quindi un array di oggetti si ordina in base a una delle loro proprietà semplicemente confrontando quella proprietà:
```javascript
const items = [{ name: "b", size: 3 }, { name: "a", size: 1 }];
const bySize = [...items].sort((x, y) => x.size - y.size);
console.log(bySize.map((item) => item.name));
// prints [ 'a', 'b' ]
```
Ordinare la copia lascia `items` nel suo ordine originale.

# --instructions--

Ordina le righe per ordinare una copia di `players` per `score` crescente, stampare i loro nomi in quell'ordine con `map`, poi stampare il nome del primo elemento di `players` per mostrare che non è stato modificato. Dichiara `ranked` prima di `names`

# --answers--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
```

```javascript
const ranked = [...players].sort((a, b) => a.score - b.score);
```

```javascript
const names = ranked.map((p) => p.name);
```

```javascript
console.log(names);
```

```javascript
console.log(players[0].name);
```

# --solutions--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
const ranked = [...players].sort((a, b) => a.score - b.score);
const names = ranked.map((p) => p.name);
console.log(names);
console.log(players[0].name);
```

# --output--

[ 'Leo', 'Zoe', 'Mia' ]
Zoe
