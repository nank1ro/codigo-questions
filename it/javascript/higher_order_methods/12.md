---
language: javascript
exerciseType: 2
---

# --description--

L'accumulatore di `reduce` non deve essere per forza un numero: può essere una stringa, un array o un oggetto. Partendo da un oggetto vuoto `{}` puoi contare o raggruppare cose in un solo passaggio. Ricorda di **restituire l'accumulatore** dalla callback, altrimenti il passo successivo riceve `undefined`:
```javascript
const votes = ["yes", "no", "yes"];
const tally = votes.reduce((acc, vote) => {
  acc[vote] = (acc[vote] ?? 0) + 1;
  return acc;
}, {});
console.log(tally);
// prints { yes: 2, no: 1 }
```
`acc[vote] ?? 0` legge il conteggio attuale, o `0` quando quella chiave non esiste ancora.

# --instructions--

Completa il codice in modo che `counts` associ ogni lettera iniziale a quante parole cominciano con essa, usando `reduce` con un oggetto vuoto come accumulatore iniziale e `?? 0` per le chiavi mancanti, producendo l'output `{ a: 2, b: 2, c: 1 }`

# --seed--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.[/]((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] [/] 0) + 1;
  return [/];
}, [/]);
console.log(counts);
```

# --answers--

- reduce
- ??
- acc
- {}
- map
- filter
- &&
- word
- 0

# --solutions--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.reduce((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] ?? 0) + 1;
  return acc;
}, {});
console.log(counts);
```

# --output--

{ a: 2, b: 2, c: 1 }
