---
language: javascript
exerciseType: 1
---

# --description--

Ogni funzione ha un metodo `bind` che restituisce una **nuova** funzione con alcune cose fissate in anticipo. Il suo primo argomento diventa il `this` della nuova funzione; gli argomenti restanti vengono messi davanti a quelli con cui la nuova funzione viene chiamata (un'**applicazione parziale**):
```javascript
function multiply(a, b) {
  return a * b;
}
const double = multiply.bind(null, 2);
console.log(double(21));
// prints 42
```
Fissare `this` è importante per i metodi. Quando un metodo viene copiato fuori dal suo oggetto e chiamato da solo, `this` non si riferisce più all'oggetto, quindi `this.name` diventa `undefined`. `bind` lo blocca sull'oggetto:
```javascript
const user = {
  name: "Ana",
  hello() { return `Hi ${this.name}`; },
};
const loose = user.hello;
console.log(loose());
// prints Hi undefined
const bound = user.hello.bind(user);
console.log(bound());
// prints Hi Ana
```
La funzione originale non viene mai modificata: `bind` costruisce sempre una nuova funzione, il cui `name` è il nome originale preceduto da `bound `.

# --instructions--

La funzione `power` e l'oggetto `counter` sono forniti. Salva nella costante `square` una versione di `power` legata con `this` impostato a `null` ed `exponent` fissato a `2`, e nella costante `tick` una versione di `counter.increment` legata a `counter`, in modo che continui a funzionare quando viene chiamata da sola

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = 
const tick = 
```

# --asserts--

`square(3)` deve restituire `9`.

```javascript
tryCatch(square(3) === 9);
```

`square(12)` deve restituire `144`.

```javascript
tryCatch(square(12) === 144);
```

`square` deve essere creata con `bind`, quindi il suo nome è `"bound power"`.

```javascript
tryCatch(square.name === "bound power");
```

`tick` deve essere una nuova funzione creata con `bind`, non `counter.increment` stesso.

```javascript
tryCatch(tick !== counter.increment && tick.name === "bound increment");
```

Chiamare `tick()` due volte da solo deve restituire `1` e poi `2`, e `counter.count` deve essere `2`.

```javascript
tryCatch(tick() === 1 && tick() === 2 && counter.count === 2);
```

`tick` deve continuare a funzionare quando viene salvata in un'altra variabile e chiamata da lì.

```javascript
tryCatch((() => { const alone = tick; return alone() === 3; })());
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = power.bind(null, 2);
const tick = counter.increment.bind(counter);
```
