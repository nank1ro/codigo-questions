---
language: javascript
exerciseType: 1
---

# --description--

Tutto si unisce in `groupBy(items, keyFn)`: una funzione di ordine superiore che riceve una callback che decide la **chiave di gruppo** di ogni elemento e restituisce un oggetto che associa ogni chiave all'array degli elementi con quella chiave. `reduce` con un accumulatore oggetto fa tutto il lavoro:
```javascript
const byInitial = groupBy(["hi", "yo", "hey"], (w) => w[0]);
console.log(byInitial);
// prints { h: [ 'hi', 'hey' ], y: [ 'yo' ] }
```
Per ogni elemento, calcola la chiave, crea l'array per quella chiave se non esiste ancora (`acc[key] ?? []`), inserisci l'elemento e restituisci l'accumulatore. Poiché è il chiamante a scegliere `keyFn`, la stessa funzione raggruppa parole per iniziale, persone per città o numeri per parità.

# --instructions--

Scrivi una funzione `groupBy(items, keyFn)` che restituisce un oggetto le cui chiavi sono i risultati di `keyFn(item)` e i cui valori sono array degli elementi con quella chiave, nell'ordine originale, usando `reduce`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function groupBy(items, keyFn) {

}
```

# --asserts--

`groupBy(["hi", "yo", "hey"], (w) => w[0])` deve restituire `{ h: ["hi", "hey"], y: ["yo"] }`.

```javascript
tryCatch(JSON.stringify(groupBy(["hi", "yo", "hey"], (w) => w[0])) === '{"h":["hi","hey"],"y":["yo"]}');
```

Raggruppare `[1, 2, 3, 4]` per parità deve restituire `{ odd: [1, 3], even: [2, 4] }`.

```javascript
tryCatch(JSON.stringify(groupBy([1, 2, 3, 4], (n) => (n % 2 === 0 ? "even" : "odd"))) === '{"odd":[1,3],"even":[2,4]}');
```

Raggruppare le persone per `city` deve mantenere gli oggetti interi, nell'ordine originale.

```javascript
tryCatch((() => { const people = [{ name: "Ana", city: "Rome" }, { name: "Bo", city: "Oslo" }, { name: "Cy", city: "Rome" }]; const groups = groupBy(people, (p) => p.city); return groups.Rome.map((p) => p.name).join(",") === "Ana,Cy" && groups.Oslo.length === 1 && groups.Oslo[0] === people[1]; })());
```

`groupBy([], (x) => x)` deve restituire un oggetto vuoto.

```javascript
tryCatch(JSON.stringify(groupBy([], (x) => x)) === "{}");
```

L'array originale non deve essere modificato.

```javascript
tryCatch((() => { const input = ["a", "b"]; groupBy(input, (s) => s); return JSON.stringify(input) === '["a","b"]'; })());
```

La funzione deve usare `reduce`.

```javascript
tryCatch(/\.reduce\(/.test(groupBy.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function groupBy(items, keyFn) {
  return items.reduce((acc, item) => {
    const key = keyFn(item);
    acc[key] = acc[key] ?? [];
    acc[key].push(item);
    return acc;
  }, {});
}
```
