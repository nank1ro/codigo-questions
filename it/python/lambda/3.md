---
language: python
exerciseType: 2
---

# --description--

Una lambda può prendere **più di un parametro**.
Separali con virgole, esattamente come in una funzione `def`:
```python
power = lambda base, exp: base ** exp
print(power(2, 3))  # 8
```

# --instructions--

Completa la lambda in modo che prenda i parametri `a` e `b` e restituisca la loro somma

# --seed--

```python
add = lambda a[/] b[/] a + b
print(add(3, 4))
```

# --answers--

- ,
- :
- ;
- ->

# --solutions--

```python
add = lambda a, b: a + b
print(add(3, 4))
```

# --output--

7
