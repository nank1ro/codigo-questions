---
language: python
exerciseType: 1
---

# --description--

Le lambda non si limitano alle funzioni integrate: **le tue funzioni** possono prendere una funzione come parametro e chiamarla.
```python
def apply(func, value):
    return func(value)

print(apply(lambda x: x * 10, 3))  # 30
```

# --instructions--

Definisci una funzione `apply_twice(func, value)` che applichi `func` a `value` due volte e restituisca il risultato.
Poi salva in `result` il valore di `apply_twice` chiamata con una lambda che somma `3`, e il valore `10`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare un'espressione `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`apply_twice(lambda x: x * 2, 5)` deve restituire `20`

```python
    def test_1(self):
        self.assertEqual(apply_twice(lambda x: x * 2, 5), 20, "--err-t1--")
```

`apply_twice(lambda s: s + "!", "hi")` deve restituire `hi!!`

```python
    def test_2(self):
        self.assertEqual(apply_twice(lambda s: s + "!", "hi"), "hi!!", "--err-t2--")
```

`result` deve essere uguale a `16`

```python
    def test_3(self):
        self.assertEqual(result, 16, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def apply_twice(func, value):
    return func(func(value))

result = apply_twice(lambda x: x + 3, 10)
```
