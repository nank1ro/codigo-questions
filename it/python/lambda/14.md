---
language: python
exerciseType: 1
---

# --description--

**Quando dovresti preferire `def`?**
Una lambda è ottima per una funzione breve e usa e getta passata come argomento.
Se la logica ha bisogno di un nome, di più righe, di una docstring, o viene riutilizzata in molti punti, una funzione `def` è più chiara.
Un ultimo trucco: `sorted()` accetta anche `reverse=True` per ottenere prima i valori più grandi:
```python
print(sorted([3, 1, 2], reverse=True))  # [3, 2, 1]
```

# --instructions--

Salva in `ranking` la lista `students` ordinata dal `grade` più alto al più basso, poi salva in `names` la lista dei nomi in `ranking`, usando `sorted()` e `map()` con lambda

# --seed--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare un'espressione `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Devi usare `map()`

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ranking` deve iniziare con lo studente `Cy`

```python
    def test_1(self):
        self.assertEqual(ranking[0]["name"], "Cy", "--err-t1--")
```

`names` deve essere uguale a `['Cy', 'Ana', 'Bo']`

```python
    def test_2(self):
        self.assertEqual(names, ["Cy", "Ana", "Bo"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
ranking = sorted(students, key=lambda s: s["grade"], reverse=True)
names = list(map(lambda s: s["name"], ranking))
```
