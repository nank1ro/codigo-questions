---
language: python
exerciseType: 2
---

# --description--

`filter()` mantiene solo gli elementi per cui la funzione restituisce `True`:
```python
nums = [5, 12, 8, 20]
big = list(filter(lambda n: n > 10, nums))
print(big)  # [12, 20]
```

# --instructions--

Completa il codice per mantenere solo i numeri pari, in modo che il programma stampi `[2, 4]`

# --seed--

```python
nums = [1, 2, 3, 4]
evens = list([/](lambda n: n % 2 [/] 0, nums))
print(evens)
```

# --answers--

- filter
- map
- sorted
- ==
- !=

# --solutions--

```python
nums = [1, 2, 3, 4]
evens = list(filter(lambda n: n % 2 == 0, nums))
print(evens)
```

# --output--

[2, 4]
