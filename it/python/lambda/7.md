---
language: python
exerciseType: 1
---

# --description--

Le lambda danno il meglio di sé come **argomenti di altre funzioni**.
`sorted()` accetta un parametro `key`: una funzione chiamata su ogni elemento, il cui risultato decide l'ordine.
Una lambda è perfetta per questo:
```python
words = ["banana", "kiwi", "apple"]
print(sorted(words, key=lambda w: len(w)))
# ['kiwi', 'apple', 'banana']
```

# --instructions--

Salva nella variabile `by_length` la lista `words` ordinata dalla parola più corta alla più lunga, usando `sorted()` con una lambda come `key`

# --seed--

```python
words = ["elephant", "cat", "tiger", "ox"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare un'espressione `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`by_length` deve essere uguale a `['ox', 'cat', 'tiger', 'elephant']`

```python
    def test_1(self):
        self.assertEqual(by_length, ["ox", "cat", "tiger", "elephant"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
words = ["elephant", "cat", "tiger", "ox"]
by_length = sorted(words, key=lambda w: len(w))
```
