---
language: python
exerciseType: 4
---

# --description--

Anche `max()` e `min()` accettano una funzione `key`, proprio come `sorted()`:
```python
words = ["hi", "hello", "hey"]
print(max(words, key=lambda w: len(w)))  # hello
```

# --instructions--

Ordina gli elementi per stampare i nomi in ordine alfabetico ignorando le maiuscole, e poi il nome più lungo

# --answers--

```python
names = ["bob", "Alice", "Charlie"]
```

```python
ordered = sorted(names, key=lambda s: s.lower())
```

```python
longest = max(ordered, key=lambda s: len(s))
```

```python
print(ordered)
print(longest)
```

# --solutions--

```python
names = ["bob", "Alice", "Charlie"]
ordered = sorted(names, key=lambda s: s.lower())
longest = max(ordered, key=lambda s: len(s))
print(ordered)
print(longest)
```

# --output--

['Alice', 'bob', 'Charlie']
Charlie
