---
language: python
exerciseType: 1
---

# --description--

Nota che il corpo della lambda non ha la parola chiave `return`.
Il corpo è una **singola espressione**, e il suo valore viene restituito automaticamente:
```python
square = lambda n: n * n
print(square(3))  # 9
```

# --instructions--

Salva nella variabile `square` una lambda che prende un parametro `n` e restituisce `n` moltiplicato per se stesso

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare un'espressione `lambda`

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`square(4)` deve restituire `16`

```python
    def test_1(self):
        self.assertEqual(square(4), 16, "--err-t1--")
```

`square(7)` deve restituire `49`

```python
    def test_2(self):
        self.assertEqual(square(7), 49, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
square = lambda n: n * n
```
