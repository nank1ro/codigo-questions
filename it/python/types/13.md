---
language: python
exerciseType: 1
---

# --description--

A differenza di molti linguaggi, gli interi di Python **non hanno una dimensione massima**: un `int` cresce per contenere tutte le cifre necessarie, quindi i grandi calcoli restano esatti:
```python
print(3 ** 100)  # 515377520732011331036461129765621272702107522001
```
Un `float`, invece, conserva solo circa 15 cifre significative, quindi la stessa potenza come float perde precisione:
```python
print(3.0 ** 100)  # 5.153775207320113e+47
```
Siccome `str()` funziona su qualsiasi `int`, un modo rapido per contare le cifre di un numero è misurare la lunghezza del suo testo.

# --instructions--

Salva in `big` il valore di `2` elevato a `200`, poi scrivi una funzione `digits` che prende un `int` positivo e restituisce quante cifre ha

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare `str()`

```json
{
  "regex": "str\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`big` deve essere `2 ** 200`

```python
    def test_1(self):
        self.assertEqual(big, 2 ** 200, "--err-t1--")
        self.assertIsInstance(big, int, "--err-t1--")
```

`digits(big)` deve restituire `61`

```python
    def test_2(self):
        self.assertEqual(digits(big), 61, "--err-t2--")
```

`digits(7)` deve restituire `1` e `digits(10 ** 50)` deve restituire `51`

```python
    def test_3(self):
        self.assertEqual(digits(7), 1, "--err-t3--")
        self.assertEqual(digits(10 ** 50), 51, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
big = 2 ** 200

def digits(number):
    return len(str(number))
```
