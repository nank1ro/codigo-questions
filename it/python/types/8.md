---
language: python
exerciseType: 2
---

# --description--

`bool` è una **sottoclasse** di `int`: `True` si comporta come `1` e `False` come `0` ovunque sia atteso un numero:
```python
print(True + 1)     # 2
print(True == 1)    # True
print(False * 10)   # 0
```
`sum()` somma gli elementi di una lista, quindi sommare una lista di booleani **conta** quanti sono `True`:
```python
answers = [True, False, True]
print(sum(answers))  # 2
```
A causa della relazione di sottoclasse, `isinstance(True, int)` restituisce `True`, mentre `type(True)` resta `bool`.

# --instructions--

Completa il codice in modo che conti le risposte corrette, aggiunga `1` a `True` e verifichi che `True` sia un'istanza di `int`, stampando `3`, `2` e `True`

# --seed--

```python
answers = [True, False, True, True]
correct = [/](answers)
print(correct)
print(True [/] 1)
print(isinstance(True, [/]))
```

# --answers--

- sum
- +
- int
- len
- ==
- str

# --solutions--

```python
answers = [True, False, True, True]
correct = sum(answers)
print(correct)
print(True + 1)
print(isinstance(True, int))
```

# --output--

3
2
True
