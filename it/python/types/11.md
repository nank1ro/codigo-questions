---
language: python
exerciseType: 1
---

# --description--

La formattazione cambia solo il modo in cui un numero viene visualizzato. Per ottenere un **valore** arrotondato usa la funzione integrata `round()`:
```python
print(round(3.14159, 2))  # 3.14
print(round(2.71828, 1))  # 2.7
```
Con un solo argomento `round()` arrotonda al numero intero più vicino e restituisce un `int`; con un numero di decimali restituisce un `float`:
```python
print(round(3.7))     # 4
print(round(3.7, 0))  # 4.0
```
Nota che i valori esattamente a metà strada tra due numeri vengono arrotondati a quello **pari**: `round(2.5)` è `2` e `round(3.5)` è `4`.

# --instructions--

Scrivi una funzione `average` che prende una lista di numeri e restituisce la loro media arrotondata a due decimali

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare `round()`

```json
{
  "regex": "round\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`average([1, 2, 3, 4])` deve restituire `2.5`

```python
    def test_1(self):
        self.assertEqual(average([1, 2, 3, 4]), 2.5, "--err-t1--")
```

`average([1, 2, 2])` deve restituire `1.67`

```python
    def test_2(self):
        self.assertEqual(average([1, 2, 2]), 1.67, "--err-t2--")
        self.assertEqual(average([1, 1, 1, 2]), 1.25, "--err-t2--")
```

`average([10])` deve restituire il float `10.0`

```python
    def test_3(self):
        self.assertIsInstance(average([10]), float, "--err-t3--")
        self.assertEqual(average([10]), 10.0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def average(numbers):
    return round(sum(numbers) / len(numbers), 2)
```
