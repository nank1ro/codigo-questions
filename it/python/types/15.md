---
language: python
exerciseType: 2
---

# --description--

Le conversioni possono essere combinate. `int("3.7")` fallisce, ma `float("3.7")` funziona, e `int()` di un `float` scarta la parte decimale:
```python
number = float("3.7")   # 3.7
print(int(number))      # 3
```
`bool()` segue le regole di verità: `bool("")` è `False`, e nota che `bool("False")` è `True`, perché è una stringa non vuota.

# --instructions--

Completa il codice per convertire `text` in un numero, stamparne il doppio, poi stampare il numero convertito in un `int` e il valore booleano di una stringa vuota, in modo che stampi `7.4`, `3` e `False`

# --seed--

```python
text = "3.7"
number = [/](text)
print(number * 2)
print([/](number))
print([/](""))
```

# --answers--

- float
- int
- bool
- str
- len

# --solutions--

```python
text = "3.7"
number = float(text)
print(number * 2)
print(int(number))
print(bool(""))
```

# --output--

7.4
3
False
