---
language: python
exerciseType: 4
---

# --description--

I valori non cambiano tipo da soli: per trasformare un valore in un altro tipo chiami il nome del tipo come una funzione. Questo si chiama **conversione** (o *casting*):
```python
print(int("42"))      # 42
print(float("3.5"))   # 3.5
print(str(7))         # '7'
print(int(3.9))       # 3
```
`int()` e `float()` leggono numeri scritti come testo, che è ciò che ottieni dall'input dell'utente o dai file. `str()` trasforma qualsiasi cosa in testo, così può essere unita con `+` ad altre stringhe.
Nota che `int(3.9)` non arrotonda: scarta la parte decimale.

# --instructions--

Ordina gli elementi per convertire `age` in un numero, aggiungergli `1` e stampare il risultato unito al testo `Next year: `

# --answers--

```python
age = "30"
```

```python
years = int(age)
```

```python
next_year = years + 1
```

```python
print("Next year: " + str(next_year))
```

# --solutions--

```python
age = "30"
years = int(age)
next_year = years + 1
print("Next year: " + str(next_year))
```

# --output--

Next year: 31
