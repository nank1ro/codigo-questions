---
language: python
exerciseType: 1
---

# --description--

Il testo che arriva dall'esterno è sempre una `str`, e spetta al tuo programma capire quale tipo contiene davvero.
Un approccio comune è provare prima la conversione **più rigorosa**, e ripiegare sulla successiva quando solleva un `ValueError`:
```python
try:
    value = int(text)
except ValueError:
    value = float(text)
```
Annidare un secondo `try` dentro il blocco `except` ti permette di ripiegare ancora una volta, ad esempio per mantenere il testo com'è.

# --instructions--

Scrivi una funzione `parse_value` che prende una stringa e la restituisce come un `int` se è un numero intero, come un `float` se è un numero decimale, e invariata altrimenti

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi intercettare `ValueError` con `except`

```json
{
  "regex": "except\\s+ValueError",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_value("42")` deve restituire l'`int` `42`

```python
    def test_1(self):
        self.assertEqual(parse_value("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_value("42"), int, "--err-t1--")
        self.assertEqual(parse_value("-8"), -8, "--err-t1--")
```

`parse_value("3.5")` deve restituire il `float` `3.5`

```python
    def test_2(self):
        self.assertEqual(parse_value("3.5"), 3.5, "--err-t2--")
        self.assertIsInstance(parse_value("3.5"), float, "--err-t2--")
```

`parse_value("abc")` deve restituire `"abc"`

```python
    def test_3(self):
        self.assertEqual(parse_value("abc"), "abc", "--err-t3--")
        self.assertEqual(parse_value(""), "", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_value(text):
    try:
        return int(text)
    except ValueError:
        try:
            return float(text)
        except ValueError:
            return text
```
