---
language: python
exerciseType: 1
---

# --description--

`None` è un valore a sé che significa "qui non c'è niente". È ciò che una funzione restituisce quando non ha un'istruzione `return`, ed è un segnaposto comune per un valore non ancora noto.
Siccome esiste un solo `None`, controllalo con `is`, non con `==`:
```python
result = None
if result is None:
    print("no result yet")
```
Ogni tipo ha un attributo `__name__` con il suo nome come stringa, comodo per i messaggi:
```python
print(type(3).__name__)     # int
print(type(None).__name__)  # NoneType
```

# --instructions--

Scrivi una funzione `describe` che prende un valore e restituisce la stringa `"nothing"` quando il valore è `None`, e il nome del suo tipo altrimenti

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi confrontare con `is None`

```json
{
  "regex": "is\\s+None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Devi usare `__name__`

```json
{
  "regex": "__name__",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe(None)` deve restituire `"nothing"`

```python
    def test_1(self):
        self.assertEqual(describe(None), "nothing", "--err-t1--")
```

`describe(5)` deve restituire `"int"` e `describe(2.0)` deve restituire `"float"`

```python
    def test_2(self):
        self.assertEqual(describe(5), "int", "--err-t2--")
        self.assertEqual(describe(2.0), "float", "--err-t2--")
```

`describe("a")` deve restituire `"str"` e `describe(True)` deve restituire `"bool"`

```python
    def test_3(self):
        self.assertEqual(describe("a"), "str", "--err-t3--")
        self.assertEqual(describe(True), "bool", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe(value):
    if value is None:
        return "nothing"
    return type(value).__name__
```
