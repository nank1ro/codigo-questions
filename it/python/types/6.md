---
language: python
exerciseType: 1
---

# --description--

Una conversione può fallire. `int("abc")` non può produrre un numero, quindi solleva un `ValueError` e il programma si ferma:
```python
int("abc")   # ValueError: invalid literal for int() with base 10: 'abc'
int("3.5")   # ValueError as well: "3.5" is not a whole number
```
Per far continuare il programma puoi intercettare l'errore con `try` / `except`: il codice nel blocco `try` viene eseguito, e se solleva l'errore indicato viene eseguito invece il blocco `except`:
```python
try:
    number = int(text)
except ValueError:
    number = 0
```
Quando la conversione riesce il blocco `except` viene saltato.

# --instructions--

Scrivi una funzione `to_int` che prende una stringa e la restituisce convertita in un `int`, oppure `None` quando la conversione solleva un `ValueError`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi intercettare `ValueError` con `except`

```json
{
  "regex": "except\\s+ValueError",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`to_int("42")` deve restituire `42`

```python
    def test_1(self):
        self.assertEqual(to_int("42"), 42, "--err-t1--")
        self.assertEqual(to_int("-7"), -7, "--err-t1--")
```

`to_int("abc")` deve restituire `None`

```python
    def test_2(self):
        self.assertIsNone(to_int("abc"), "--err-t2--")
```

`to_int("3.5")` deve restituire `None`

```python
    def test_3(self):
        self.assertIsNone(to_int("3.5"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def to_int(text):
    try:
        return int(text)
    except ValueError:
        return None
```
