---
language: python
exerciseType: 1
---

# --description--

Ogni membro di un enum ha due attributi: `name`, l'identificatore che hai scritto nella classe, e `value`, il valore che gli hai assegnato:
```python
from enum import Enum

class Color(Enum):
    RED = 1
    GREEN = 2

print(Color.GREEN.name)   # GREEN
print(Color.GREEN.value)  # 2
```
Il valore può essere di qualsiasi tipo, non solo un intero: stringhe, tuple e float sono scelte comuni.
Un membro è un oggetto normale, quindi puoi memorizzarlo in una variabile e leggere i suoi attributi in seguito:
```python
chosen = Color.RED
print(chosen.value)  # 1
```

# --instructions--

Memorizza in `home` il membro `Planet.EARTH`, in `label` il suo nome e in `position` il suo valore

# --seed--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare `.name`

```json
{
  "regex": "\\.name\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Devi usare `.value`

```json
{
  "regex": "\\.value\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`home` deve essere `Planet.EARTH`

```python
    def test_1(self):
        self.assertIs(home, Planet.EARTH, "--err-t1--")
```

`label` deve essere uguale a `"EARTH"`

```python
    def test_2(self):
        self.assertEqual(label, "EARTH", "--err-t2--")
```

`position` deve essere uguale a `3`

```python
    def test_3(self):
        self.assertEqual(position, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3

home = Planet.EARTH
label = home.name
position = home.value
```
