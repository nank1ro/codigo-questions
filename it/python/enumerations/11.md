---
language: python
exerciseType: 1
---

# --description--

Un `Flag` è un enum i cui membri possono essere **combinati**: un valore può contenere più membri contemporaneamente, come un insieme di opzioni.
Dichiara i suoi membri con `auto()`, che per un `Flag` assegna potenze di due (`1`, `2`, `4`, ...), così ogni combinazione ha un valore distinto:
```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```
Usa `|` per combinare i membri, `in` per verificare se un membro fa parte di una combinazione e `value` per vedere il numero risultante:
```python
full = Permission.READ | Permission.WRITE | Permission.EXECUTE
print(Permission.WRITE in full)  # True
print(full.value)                # 7
print(full)                      # Permission.READ|WRITE|EXECUTE
```

# --instructions--

Memorizza in `editor` la combinazione di `Permission.READ` e `Permission.WRITE` usando `|`, in `can_write` se `Permission.WRITE` è in `editor`, e in `bits` il valore di `editor`

# --seed--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare l'operatore `|`

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Devi usare l'operatore `in`

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`editor` deve essere uguale a `Permission.READ | Permission.WRITE`

```python
    def test_1(self):
        self.assertEqual(editor, Permission.READ | Permission.WRITE, "--err-t1--")
```

`can_write` deve essere `True`

```python
    def test_2(self):
        self.assertEqual(can_write, True, "--err-t2--")
```

`bits` deve essere uguale a `3`

```python
    def test_3(self):
        self.assertEqual(bits, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()

editor = Permission.READ | Permission.WRITE
can_write = Permission.WRITE in editor
bits = editor.value
```
