---
language: python
exerciseType: 1
---

# --description--

Oltre ai metodi normali, un enum può definire **metodi di classe** con `@classmethod`. Ricevono la classe enum stessa come `cls`, quindi sono il posto giusto per modi alternativi di trovare un membro:
```python
class Color(Enum):
    RED = auto()
    BLUE = auto()

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]

print(Color.parse(" blue "))  # Color.BLUE
```
Insieme a `auto()`, metodi, proprietà e ricerche, questo ti permette di costruire enum che incapsulano il proprio comportamento.

# --instructions--

Dichiara un enum `Direction` con i membri `NORTH`, `EAST`, `SOUTH` e `WEST`, in questo ordine, usando `auto()`.
Aggiungi un metodo `opposite()` che restituisce il membro sul lato opposto (`NORTH` e `SOUTH`, `EAST` e `WEST`), e un metodo di classe `parse(text)` che restituisce il membro il cui nome corrisponde a `text`, ignorando maiuscole e minuscole e gli spazi ai lati

# --seed--

```python
from enum import Enum, auto
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare `auto()`

```json
{
  "regex": "auto\\(\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Devi usare il decoratore `@classmethod`

```json
{
  "regex": "@classmethod",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Direction` deve avere i membri `NORTH`, `EAST`, `SOUTH` e `WEST` con valori da `1` a `4`

```python
    def test_1(self):
        self.assertEqual([d.name for d in Direction], ["NORTH", "EAST", "SOUTH", "WEST"], "--err-t1--")
        self.assertEqual([d.value for d in Direction], [1, 2, 3, 4], "--err-t1--")
```

`Direction.NORTH.opposite()` deve essere `Direction.SOUTH` e `Direction.WEST.opposite()` deve essere `Direction.EAST`

```python
    def test_2(self):
        self.assertIs(Direction.NORTH.opposite(), Direction.SOUTH, "--err-t2--")
        self.assertIs(Direction.SOUTH.opposite(), Direction.NORTH, "--err-t2--")
        self.assertIs(Direction.EAST.opposite(), Direction.WEST, "--err-t2--")
        self.assertIs(Direction.WEST.opposite(), Direction.EAST, "--err-t2--")
```

`Direction.parse(" west ")` deve essere `Direction.WEST`

```python
    def test_3(self):
        self.assertIs(Direction.parse(" west "), Direction.WEST, "--err-t3--")
        self.assertIs(Direction.parse("North"), Direction.NORTH, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    EAST = auto()
    SOUTH = auto()
    WEST = auto()

    def opposite(self):
        opposites = {
            Direction.NORTH: Direction.SOUTH,
            Direction.SOUTH: Direction.NORTH,
            Direction.EAST: Direction.WEST,
            Direction.WEST: Direction.EAST,
        }
        return opposites[self]

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]
```
