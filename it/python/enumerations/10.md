---
language: python
exerciseType: 2
---

# --description--

Se due membri condividono lo stesso valore, il secondo non è un nuovo membro ma un **alias** del primo:
```python
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # alias of ACTIVE

print(Status.ENABLED)  # Status.ACTIVE
print(len(Status))     # 1
```
Gli alias vengono saltati durante l'iterazione e non sono contati da `len()`.
Di solito un valore duplicato è un errore. Il decoratore `unique`, importato da `enum`, fa sì che Python sollevi un `ValueError` appena viene dichiarato un enum con alias:
```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # ValueError: duplicate values found
```

# --instructions--

Completa il codice per importare il decoratore `unique` e applicarlo a `Status`, in modo che i valori duplicati vengano rifiutati, poi stampare il numero di membri

# --seed--

```python
from enum import Enum, [/]

[/]
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --answers--

- unique
- @unique
- auto
- @auto
- @property
- @classmethod

# --solutions--

```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --output--

2
