---
language: python
exerciseType: 2
---

# --description--

Spesso i valori esatti non contano: ti serve solo che i membri siano distinti.
In quel caso puoi lasciare che sia Python a scegliere i valori con `auto()`, importato anch'esso dal modulo `enum`.
Assegna `1` al primo membro e poi conta a salire:
```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    SOUTH = auto()
    EAST = auto()

print(Direction.EAST.value)  # 3
```

# --instructions--

Completa il codice per importare `auto` e stampare il valore del membro `LARGE` di `Size`

# --seed--

```python
from enum import Enum, [/]

class Size(Enum):
    SMALL = auto()
    MEDIUM = auto()
    LARGE = auto()

print(Size.LARGE.[/])
```

# --answers--

- auto
- value
- name
- Enum
- unique
- index

# --solutions--

```python
from enum import Enum, auto

class Size(Enum):
    SMALL = auto()
    MEDIUM = auto()
    LARGE = auto()

print(Size.LARGE.value)
```

# --output--

3
