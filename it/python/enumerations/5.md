---
language: python
exerciseType: 1
---

# --description--

Puoi ottenere un membro a partire dal suo **valore** chiamando la classe come una funzione, o dal suo **nome** usando le parentesi quadre:
```python
print(Color(2))        # Color.GREEN
print(Color["BLUE"])   # Color.BLUE
```
Entrambi sono comodi quando il valore o il nome proviene dall'esterno del programma, come un file o un input dell'utente.
Se non c'è corrispondenza, `Color(9)` solleva un `ValueError` e `Color["PINK"]` solleva un `KeyError`.

# --instructions--

Memorizza in `by_value` il membro di `Status` il cui valore è `code`, chiamando `Status(...)`, e in `by_name` il membro il cui nome è `label`, usando `Status[...]`

# --seed--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi chiamare `Status(...)` con `code`

```json
{
  "regex": "Status\\(\\s*code\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Devi usare `Status[...]` con `label`

```json
{
  "regex": "Status\\[\\s*label\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`by_value` deve essere `Status.DONE`

```python
    def test_1(self):
        self.assertIs(by_value, Status.DONE, "--err-t1--")
```

`by_name` deve essere `Status.FAILED`

```python
    def test_2(self):
        self.assertIs(by_name, Status.FAILED, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
by_value = Status(code)
by_name = Status[label]
```
