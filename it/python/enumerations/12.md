---
language: python
exerciseType: 4
---

# --description--

Gli enum si abbinano naturalmente all'istruzione `match` (disponibile da Python 3.10), che confronta un valore con una serie di pattern `case` ed esegue il primo che corrisponde:
```python
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
```
Scrivi sempre il membro con la sua classe, come `Light.RED`: un nome da solo come `case RED:` non confronterebbe nulla, catturerebbe soltanto il valore in una nuova variabile `RED` e corrisponderebbe a tutto.
Il jolly `case _:` è il caso predefinito e deve venire per ultimo, perché qualsiasi pattern dopo di esso non potrebbe mai essere raggiunto.

# --instructions--

Ordina gli elementi per dichiarare l'enum `Light`, definire `action()` con il caso `RED` per primo, poi il caso `GREEN`, poi il caso predefinito, e stampare l'azione per `Light.YELLOW`

# --answers--

```python
from enum import Enum
```

```python
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
```

```python
def action(light):
```

```python
    match light:
```

```python
        case Light.RED:
            return "Stop"
```

```python
        case Light.GREEN:
            return "Go"
```

```python
        case _:
            return "Slow down"
```

```python
print(action(Light.YELLOW))
```

# --solutions--

```python
from enum import Enum
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
print(action(Light.YELLOW))
```

# --output--

Slow down
