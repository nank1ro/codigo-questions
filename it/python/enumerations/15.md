---
language: python
exerciseType: 4
---

# --description--

Iterare sulla classe e cercare i membri per nome funzionano bene insieme quando elabori dati provenienti dall'esterno, come righe di log o un file.
Una dictionary comprehension sulla classe prepara una voce per membro, poi `Level[name]` converte ogni stringa in arrivo nel membro corrispondente:
```python
counts = {level: 0 for level in Level}
counts[Level["INFO"]] += 1
```
Poiché l'enum conserva l'ordine di dichiarazione, iterare su `counts` in seguito restituisce i membri nello stesso ordine.

# --instructions--

Ordina gli elementi per dichiarare `Level` con valori automatici, preparare un contatore per ogni membro, contare i nomi nella lista, poi stampare il nome di ogni membro con il suo conteggio

# --answers--

```python
from enum import Enum, auto
```

```python
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
```

```python
counts = {level: 0 for level in Level}
```

```python
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
```

```python
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --solutions--

```python
from enum import Enum, auto
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
counts = {level: 0 for level in Level}
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --output--

DEBUG: 0
INFO: 2
ERROR: 1
