---
language: python
exerciseType: 3
---

# --description--

Le stringhe sono **immutabili**: una volta create, i loro caratteri non possono essere modificati.
Assegnare a un indice genera un `TypeError`:
```python
word = "dog"
word[1] = "i"  # TypeError: 'str' object does not support item assignment
```
Per "cambiare" una stringa ne costruisci una nuova, per esempio con slice e concatenazione, e la salvi nella variabile:
```python
word = word[0] + "i" + word[2:]
print(word)  # dig
```

# --instructions--

Cosa succede quando questo codice viene eseguito?
```python
word = "cat"
word[0] = "b"
print(word)
```

# --answers--

- Un `TypeError`: le stringhe sono immutabili, un carattere non può essere sostituito sul posto
- Stampa `bat`
- Stampa `b`
- Stampa `cat`, l'assegnazione viene ignorata silenziosamente

# --solutions--

- Un `TypeError`: le stringhe sono immutabili, un carattere non può essere sostituito sul posto
