---
language: python
exerciseType: 4
---

# --description--

Dato che ogni metodo delle stringhe restituisce una nuova stringa, puoi **concatenare** i metodi uno dopo l'altro.
Ogni chiamata lavora sul risultato della precedente:
```python
text = "  Hello World  "
print(text.strip().lower())  # hello world
```
Una slice accetta anche un terzo valore, il **passo** (step).
Il passo `-1` percorre la stringa all'indietro, il trucco classico per invertirla:
```python
print("abc"[::-1])  # cba
```

# --instructions--

Ordina gli elementi per ripulire `word`, invertirla e stampare se la parola ripulita è un palindromo (si legge uguale al contrario)

# --answers--

```python
word = "  Level  "
```

```python
clean = word.strip().lower()
```

```python
reversed_word = clean[::-1]
```

```python
print(clean == reversed_word)
```

# --solutions--

```python
word = "  Level  "
clean = word.strip().lower()
reversed_word = clean[::-1]
print(clean == reversed_word)
```

# --output--

True
