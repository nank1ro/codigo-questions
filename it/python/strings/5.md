---
language: python
exerciseType: 1
---

# --description--

Una **slice** estrae una parte di una stringa.
Scrivi `[start:end]` tra parentesi quadre: il carattere in posizione `start` è incluso, quello in posizione `end` è **escluso**:
```python
word = "python"
print(word[0:3])  # pyt
print(word[2:4])  # th
```
Puoi omettere `start` per tagliare dall'inizio, oppure `end` per tagliare fino alla fine:
```python
print(word[:2])  # py
print(word[2:])  # thon
```
Lo slicing non genera mai un errore: un `end` più grande della lunghezza si ferma semplicemente all'ultimo carattere.

# --instructions--

Salva nella variabile `first` i primi 4 caratteri di `word` e in `rest` tutti quelli restanti, usando le slice

# --seed--

```python
word = "notebook"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare una slice `[:]`

```json
{
  "regex": "\\[[^\\]]*:[^\\]]*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`first` deve essere uguale a `"note"`

```python
    def test_1(self):
        self.assertEqual(first, "note", "--err-t1--")
```

`rest` deve essere uguale a `"book"`

```python
    def test_2(self):
        self.assertEqual(rest, "book", "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
word = "notebook"
first = word[:4]
rest = word[4:]
```
