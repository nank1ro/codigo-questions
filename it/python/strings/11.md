---
language: python
exerciseType: 1
---

# --description--

`split()` suddivide una stringa in una **lista** di pezzi.
Senza argomenti divide sugli spazi bianchi; con un argomento divide su quel separatore:
```python
print("a b  c".split())      # ['a', 'b', 'c']
print("2024-01-31".split("-"))  # ['2024', '01', '31']
```
`join()` fa il contrario: incolla gli elementi di una lista in un'unica stringa.
Si chiama sul **separatore**, e la lista è l'argomento:
```python
words = ["one", "two", "three"]
print(", ".join(words))  # one, two, three
print("".join(words))    # onetwothree
```

# --instructions--

Salva in `words` la lista delle parole di `sentence`, poi salva in `joined` quelle stesse parole incollate insieme con un `-` tra loro

# --seed--

```python
sentence = "the quick brown fox"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare `split()`

```json
{
  "regex": "\\.split\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Devi usare `join()`

```json
{
  "regex": "\\.join\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`words` deve essere uguale a `['the', 'quick', 'brown', 'fox']`

```python
    def test_1(self):
        self.assertEqual(words, ["the", "quick", "brown", "fox"], "--err-t1--")
```

`joined` deve essere uguale a `"the-quick-brown-fox"`

```python
    def test_2(self):
        self.assertEqual(joined, "the-quick-brown-fox", "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
sentence = "the quick brown fox"
words = sentence.split()
joined = "-".join(words)
```
