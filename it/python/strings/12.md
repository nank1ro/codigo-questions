---
language: python
exerciseType: 4
---

# --description--

`replace(old, new)` restituisce una copia della stringa in cui **ogni** occorrenza di `old` è sostituita da `new`:
```python
text = "a-b-c"
print(text.replace("-", "+"))  # a+b+c
```
Dato che le stringhe sono immutabili, ricordati di salvare il risultato se vuoi conservarlo.

# --instructions--

Ordina gli elementi per sostituire ogni `cats` con `dogs` in `msg` e stampare il risultato

# --answers--

```python
msg = "I like cats, cats are great"
```

```python
new_msg = msg.replace("cats", "dogs")
```

```python
print(new_msg)
```

# --solutions--

```python
msg = "I like cats, cats are great"
new_msg = msg.replace("cats", "dogs")
print(new_msg)
```

# --output--

I like dogs, dogs are great
