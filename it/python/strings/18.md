---
language: python
exerciseType: 1
---

# --description--

Uno **slug** è una versione di un titolo adatta agli URL: minuscola, senza spazi intorno, e con le parole separate da trattini, come `hello-world`.
Costruirne uno è solo una concatenazione dei metodi che hai già imparato: `strip()`, `lower()` e `replace()`.

# --instructions--

Scrivi una funzione `slugify(title)` che restituisca `title` senza spazi intorno, in minuscolo e con ogni spazio sostituito da `-`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare `replace()`

```json
{
  "regex": "\\.replace\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`slugify("Hello World")` deve restituire `"hello-world"`

```python
    def test_1(self):
        self.assertEqual(slugify("Hello World"), "hello-world", "--err-t1--")
```

`slugify("  Learn Python Strings  ")` deve restituire `"learn-python-strings"`

```python
    def test_2(self):
        self.assertEqual(slugify("  Learn Python Strings  "), "learn-python-strings", "--err-t2--")
```

`slugify("codigo")` deve restituire `"codigo"`

```python
    def test_3(self):
        self.assertEqual(slugify("codigo"), "codigo", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def slugify(title):
    return title.strip().lower().replace(" ", "-")
```
