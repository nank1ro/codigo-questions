---
language: python
exerciseType: 1
---

# --description--

Poiché `if value:` applica già il valore di verità, confrontare con `== True` o `== False` è inutile e può persino essere sbagliato: `2 == True` è `False`, eppure `2` è vero. Verifica il valore stesso:
```python
values = [0, 1, "", "a", None, [], [0]]
for value in values:
    if value:
        print(value)  # 1, a, [0]
```

# --instructions--

Scrivi una funzione `count_truthy(values)` che restituisce quanti elementi della lista `values` sono veri

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`count_truthy([0, 1, "", "a", None, [], [0]])` deve restituire `3`

```python
    def test_1(self):
        self.assertEqual(count_truthy([0, 1, "", "a", None, [], [0]]), 3, "--err-t1--")
```

`count_truthy([])` deve restituire `0`

```python
    def test_2(self):
        self.assertEqual(count_truthy([]), 0, "--err-t2--")
```

Anche i valori che sono veri senza essere `True` devono essere contati

```python
    def test_3(self):
        self.assertEqual(count_truthy([2, "no", [None], 0.0, True]), 4, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def count_truthy(values):
    count = 0
    for value in values:
        if value:
            count += 1
    return count
```
