---
language: python
exerciseType: 1
---

# --description--

La valutazione a corto circuito è un modo sicuro per **proteggere** un'operazione che fallirebbe su alcuni valori. In `word is not None and len(word) < 4`, `len(word)` viene eseguito solo quando `word` non è `None`, quindi la chiamata non solleva mai un errore. La protezione deve venire per prima:
```python
def is_short(word):
    return word is not None and len(word) < 4

print(is_short(None))   # False
print(is_short("cat"))  # True
```
Tieni presente che `and` restituisce un operando: `word and len(word) < 4` dà `None` per `None` e `""` per la stringa vuota, non `False`. Proteggi con un confronto vero e proprio quando serve un booleano.

# --instructions--

Scrivi una funzione `first_is_zero(numbers)` che restituisce `True` quando la lista `numbers` non è vuota e il suo primo elemento è `0`, e `False` altrimenti. Il risultato deve essere un booleano anche per una lista vuota

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_is_zero([0, 5])` deve restituire `True`

```python
    def test_1(self):
        self.assertIs(first_is_zero([0, 5]), True, "--err-t1--")
        self.assertIs(first_is_zero([0]), True, "--err-t1--")
```

`first_is_zero([3, 0])` deve restituire `False`

```python
    def test_2(self):
        self.assertIs(first_is_zero([3, 0]), False, "--err-t2--")
```

`first_is_zero([])` deve restituire `False` senza sollevare errori

```python
    def test_3(self):
        self.assertIs(first_is_zero([]), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def first_is_zero(numbers):
    return len(numbers) > 0 and numbers[0] == 0
```
