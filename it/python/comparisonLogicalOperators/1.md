---
language: python
exerciseType: 2
---

# --description--

Gli operatori di confronto confrontano due valori e restituiscono un **booleano**, `True` o `False`: `==` uguale, `!=` diverso, `<` minore di, `>` maggiore di, `<=` minore o uguale, `>=` maggiore o uguale:
```python
age = 18
print(age == 18)  # True
print(age != 18)  # False
print(age < 18)   # False
print(age >= 18)  # True
```
Il risultato può essere memorizzato in una variabile o stampato direttamente. Un singolo `=` è un'assegnazione, non un confronto.

# --instructions--

Completa il codice in modo che le tre righe verifichino se `score` è uguale a `75`, minore di `100` e maggiore di `50`, producendo l'output `True`, `True`, `True`

# --seed--

```python
score = 75
print(score [/] 75)
print(score [/] 100)
print(score [/] 50)
```

# --answers--

- ==
- <
- >
- =
- !==

# --solutions--

```python
score = 75
print(score == 75)
print(score < 100)
print(score > 50)
```

# --output--

True
True
True
