---
language: python
exerciseType: 1
---

# --description--

`any(iterable)` restituisce `True` se almeno un elemento è vero e `all(iterable)` se lo sono tutti (`all([])` è `True`). Combinati con una generator expression esprimono una condizione su un'intera collezione, e vanno in corto circuito come `or` e `and`:
```python
print(any(ch.isdigit() for ch in "abc1"))  # True
print(all(ch.isalpha() for ch in "ab1"))   # False
```

# --instructions--

Scrivi una funzione `is_valid_password(password)` che restituisce `True` solo quando la stringa ha tra `8` e `20` caratteri inclusi, contiene almeno una cifra e non contiene spazi, e `False` altrimenti

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`"secret123"` è valida, e lo sono anche le password di esattamente `8` o `20` caratteri

```python
    def test_1(self):
        self.assertIs(is_valid_password("secret123"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefg1"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefghij123456789x"), True, "--err-t1--")
```

Le password troppo corte o troppo lunghe non sono valide

```python
    def test_2(self):
        self.assertIs(is_valid_password("short1"), False, "--err-t2--")
        self.assertIs(is_valid_password("abcdefghij1234567890x"), False, "--err-t2--")
```

Una password senza cifre o con uno spazio non è valida

```python
    def test_3(self):
        self.assertIs(is_valid_password("nodigitshere"), False, "--err-t3--")
        self.assertIs(is_valid_password("pass word1"), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_valid_password(password):
    return (
        8 <= len(password) <= 20
        and any(ch.isdigit() for ch in password)
        and " " not in password
    )
```
