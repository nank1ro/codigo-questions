---
language: python
exerciseType: 3
---

# --description--

`and` e `or` non restituiscono sempre `True` o `False`: restituiscono uno dei loro **operandi**. `a and b` restituisce `a` se è falso, altrimenti `b`; `a or b` restituisce `a` se è vero, altrimenti `b`:
```python
print(0 and "x")       # 0
print(3 and "x")       # x
print("" or "none")    # none
print("hi" or "none")  # hi
```
Il risultato è vero o falso esattamente quando lo è l'intera espressione, ed è per questo che `if a and b:` funziona comunque. Un uso comune è il valore predefinito: `name = user_input or "guest"`.

# --instructions--

Cosa stampa questo codice?
```python
print(0 or 5, 3 and 0, "x" and "y")
```

# --answers--

- `5 0 y`, perché `or` restituisce il primo operando vero e `and` il primo falso, oppure l'ultimo operando quando non ce n'è nessuno
- `True False True`, perché gli operatori logici producono sempre un booleano
- `0 3 x`, perché `and` e `or` restituiscono sempre il loro primo operando
- `5 3 y`, perché entrambi gli operatori restituiscono l'operando vero

# --solutions--

- `5 0 y`, perché `or` restituisce il primo operando vero e `and` il primo falso, oppure l'ultimo operando quando non ce n'è nessuno
