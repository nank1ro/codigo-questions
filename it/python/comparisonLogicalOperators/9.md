---
language: python
exerciseType: 4
---

# --description--

Gli operatori logici sono a **corto circuito**: `and` si ferma appena un operando è falso e `or` appena uno è vero, perché il risultato è già noto. Gli operandi rimanenti non vengono mai valutati, quindi se sono chiamate di funzione non vengono eseguite:
```python
def check(n):
    print("checking", n)
    return n > 0

check(-1) and check(5)  # prints only "checking -1"
check(2) or check(-3)   # prints only "checking 2"
```

# --instructions--

Ordina gli elementi per definire `is_positive`, calcolare `first` con `and` e poi `second` con `or`, e stampare entrambi i risultati, producendo l'output `checking -1`, `checking 2`, `False True`

# --answers--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
```

```python
first = is_positive(-1) and is_positive(5)
```

```python
second = is_positive(2) or is_positive(-3)
```

```python
print(first, second)
```

# --solutions--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
first = is_positive(-1) and is_positive(5)
second = is_positive(2) or is_positive(-3)
print(first, second)
```

# --output--

checking -1
checking 2
False True
