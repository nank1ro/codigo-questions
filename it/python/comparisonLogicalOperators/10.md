---
language: python
exerciseType: 1
---

# --description--

`==` confronta i **valori**; `is` confronta l'**identità**, cioè se entrambi i nomi si riferiscono allo stesso identico oggetto. Due liste uguali costruite separatamente sono `==` ma non `is`:
```python
a = [1, 2]
b = [1, 2]
c = a
print(a == b, a is b, a is c)  # True False True
```
`is` è pensato per i singleton come `None`, `True` e `False`: scrivi `value is None` o `value is not None`, mai `value == None`, perché una classe può definire `==` in modo che restituisca qualsiasi cosa. Usare `is` con numeri o stringhe non è affidabile e Python lo segnala.

# --instructions--

Scrivi una funzione `is_missing(value)` che restituisce `True` solo quando `value` è `None`, e `False` per qualsiasi altro valore, compresi quelli falsi come `0`, `""` e `[]`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`is_missing(None)` deve restituire `True`

```python
    def test_1(self):
        self.assertIs(is_missing(None), True, "--err-t1--")
```

I valori falsi non sono `None`

```python
    def test_2(self):
        self.assertIs(is_missing(0), False, "--err-t2--")
        self.assertIs(is_missing(""), False, "--err-t2--")
        self.assertIs(is_missing([]), False, "--err-t2--")
        self.assertIs(is_missing(False), False, "--err-t2--")
```

Un oggetto il cui `==` dichiara di essere uguale a tutto non è comunque `None`, quindi il controllo deve usare l'identità

```python
    def test_3(self):
        class Anything:
            def __eq__(self, other):
                return True
        self.assertIs(is_missing(Anything()), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_missing(value):
    return value is None
```
