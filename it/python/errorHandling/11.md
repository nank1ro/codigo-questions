---
language: python
exerciseType: 1
---

# --description--

Un blocco `finally` viene eseguito **qualunque cosa accada**: dopo un blocco `try` andato a buon fine, dopo un blocco `except`, anche mentre un'eccezione che nessuno ha catturato viaggia verso l'esterno, e anche quando il blocco `try` o `except` esegue un `return`:
```python
def read(text):
    try:
        return int(text)
    except ValueError:
        return 0
    finally:
        print("done")
```
Entrambi i percorsi stampano `done` prima che il valore esca dalla funzione. Quella garanzia è lo scopo di `finally`: chiudere un file, rilasciare un lock, ripristinare un'impostazione. La forma completa è `try` / `except` / `else` / `finally`; un `try` ha bisogno di almeno un `except` o di un `finally`, e `else` funziona solo insieme a un `except`.

# --instructions--

Scrivi una funzione `record(text)` che parta da una lista vuota, vi aggiunga `int(text)`, aggiunga invece la stringa `"bad"` quando `int(text)` solleva `ValueError`, aggiunga sempre `"done"` in un blocco `finally`, e restituisca la lista

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`record("5")` deve restituire `[5, "done"]`

```python
    def test_1(self):
        self.assertEqual(record("5"), [5, "done"], "--err-t1--")
```

`record("x")` deve restituire `["bad", "done"]`

```python
    def test_2(self):
        self.assertEqual(record("x"), ["bad", "done"], "--err-t2--")
```

ogni chiamata deve partire da una lista nuova

```python
    def test_3(self):
        record("1")
        self.assertEqual(record("2"), [2, "done"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def record(text):
    log = []
    try:
        log.append(int(text))
    except ValueError:
        log.append("bad")
    finally:
        log.append("done")
    return log
```
