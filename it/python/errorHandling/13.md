---
language: python
exerciseType: 1
---

# --description--

Una manciata di eccezioni integrate copre la maggior parte dei fallimenti quotidiani:

| Eccezione | Sollevata quando | Esempio |
|---|---|---|
| `ValueError` | il tipo è giusto ma il valore è impossibile | `int("abc")` |
| `TypeError` | il tipo stesso è sbagliato | `"x" + 1` |
| `ZeroDivisionError` | una divisione o un modulo ha divisore zero | `1 / 0` |
| `KeyError` | un dizionario non ha quella chiave | `{"a": 1}["b"]` |
| `IndexError` | un indice di sequenza è fuori intervallo | `[1, 2][5]` |

Ricorrere a una di queste invece di inventare un tipo nuovo mantiene i tuoi errori leggibili per chiunque conosca Python.

# --instructions--

Scrivi una funzione `get_item(values, index)` che restituisca `values[index]`, sollevi un `ValueError` con il messaggio `"index must be positive"` quando `index` è negativo, e restituisca `None` quando l'indice è fuori intervallo

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`get_item([1, 2, 3], 1)` deve restituire `2`

```python
    def test_1(self):
        self.assertEqual(get_item([1, 2, 3], 1), 2, "--err-t1--")
```

`get_item([1, 2, 3], 9)` deve restituire `None`

```python
    def test_2(self):
        self.assertIsNone(get_item([1, 2, 3], 9), "--err-t2--")
```

`get_item([1, 2, 3], -1)` deve sollevare un `ValueError` che porta il messaggio

```python
    def test_3(self):
        with self.assertRaises(ValueError, msg="--err-t3--") as ctx:
            get_item([1, 2, 3], -1)
        self.assertEqual(str(ctx.exception), "index must be positive", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def get_item(values, index):
    if index < 0:
        raise ValueError("index must be positive")
    try:
        return values[index]
    except IndexError:
        return None
```
