---
language: python
exerciseType: 1
---

# --description--

Quando più fallimenti meritano lo **stesso** recupero, elencarli come una tupla in una sola clausola è più breve che ripetere il blocco:
```python
try:
    value = int(text) / divisor
except (ValueError, ZeroDivisionError):
    value = 0
```
Le parentesi sono obbligatorie: `except ValueError, ZeroDivisionError:` è un errore di sintassi in Python 3. Una tupla resta comunque un elenco esplicito di tipi.

# --instructions--

Scrivi una funzione `safe_ratio(a, b)` che restituisca `a / b`, e restituisca `None` quando la divisione solleva `ZeroDivisionError` o `TypeError`, usando una sola clausola `except` con una tupla di entrambi i tipi

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_ratio(9, 3)` deve restituire `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_ratio(9, 3), 3.0, "--err-t1--")
```

`safe_ratio(1, 0)` deve restituire `None`

```python
    def test_2(self):
        self.assertIsNone(safe_ratio(1, 0), "--err-t2--")
```

`safe_ratio(1, "x")` deve restituire `None`

```python
    def test_3(self):
        self.assertIsNone(safe_ratio(1, "x"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_ratio(a, b):
    try:
        return a / b
    except (ZeroDivisionError, TypeError):
        return None
```
