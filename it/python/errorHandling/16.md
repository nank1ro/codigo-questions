---
language: python
exerciseType: 1
---

# --description--

Quando nessun tipo integrato è adatto, definisci il tuo estendendo `Exception`. Un corpo vuoto di solito basta — il nome è il messaggio per chi legge:
```python
class ConfigError(Exception):
    pass
```
Si comporta come qualsiasi altra eccezione: `raise ConfigError("bad port")`, e `except ConfigError:` la cattura.

Tradurre un fallimento di basso livello nel proprio tipo è comune, e l'errore originale non dovrebbe andare perso nel processo. `raise NewError(...) from original` li **concatena**: memorizza `original` nell'attributo `__cause__` della nuova eccezione, e il traceback mostra entrambi sotto *The above exception was the direct cause of the following exception*:
```python
try:
    port = int(text)
except ValueError as e:
    raise ConfigError("bad port") from e
```
Senza `from e` i due restano comunque collegati implicitamente, ma `from` dice ad alta voce che il primo errore ha causato il secondo.

# --instructions--

Definisci una classe di eccezione `ConfigError` che estende `Exception`, e scrivi una funzione `read_port(text)` che restituisca `int(text)` e, quando questo solleva `ValueError`, sollevi `ConfigError("bad port")` concatenato da essa con `raise ... from`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`read_port("8080")` deve restituire `8080`, e `ConfigError` deve estendere `Exception`

```python
    def test_1(self):
        self.assertEqual(read_port("8080"), 8080, "--err-t1--")
        self.assertTrue(issubclass(ConfigError, Exception), "--err-t1--")
```

`read_port("abc")` deve sollevare un `ConfigError` con il messaggio `"bad port"`

```python
    def test_2(self):
        with self.assertRaises(ConfigError, msg="--err-t2--") as ctx:
            read_port("abc")
        self.assertEqual(str(ctx.exception), "bad port", "--err-t2--")
```

il `ConfigError` sollevato deve essere concatenato dal `ValueError` originale

```python
    def test_3(self):
        with self.assertRaises(ConfigError, msg="--err-t3--") as ctx:
            read_port("abc")
        self.assertIsInstance(ctx.exception.__cause__, ValueError, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
class ConfigError(Exception):
    pass

def read_port(text):
    try:
        return int(text)
    except ValueError as e:
        raise ConfigError("bad port") from e
```
