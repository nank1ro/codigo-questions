---
language: python
exerciseType: 4
---

# --description--

A volte un gestore deve reagire a un fallimento senza assumersene la responsabilità: registrarlo, contarlo, chiudere qualcosa — e poi lasciare che se ne occupi chi chiama. Un `raise` da solo dentro un blocco `except` **risolleva** l'eccezione che si sta gestendo, con tipo, messaggio e traceback originali intatti:
```python
try:
    value = int(text)
except ValueError:
    print("could not read the value")
    raise
```
Scrivere invece `raise ValueError(...)` costruirebbe una nuova eccezione e butterebbe via il traceback originale, che è esattamente l'informazione che ti serve durante il debug.

# --instructions--

Ordina gli elementi per costruire un programma che definisca `load`, stampi il risultato del caricamento di `"5"`, poi lo chiami con `"abc"` dentro un blocco `try` che segnala il fallimento, producendo un messaggio da `load` e poi uno da chi chiama

# --answers--

```python
def load(text):
```

```python
    try:
        return int(text)
```

```python
    except ValueError:
        print(f"could not read {text}")
        raise
```

```python
print(load("5"))
```

```python
try:
    load("abc")
except ValueError:
    print("handled by the caller")
```

# --solutions--

```python
def load(text):
    try:
        return int(text)
    except ValueError:
        print(f"could not read {text}")
        raise
print(load("5"))
try:
    load("abc")
except ValueError:
    print("handled by the caller")
```

# --output--

5
could not read abc
handled by the caller
