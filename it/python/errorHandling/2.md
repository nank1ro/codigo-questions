---
language: python
exerciseType: 1
---

# --description--

Il blocco `try` si ferma alla **prima** istruzione che solleva un'eccezione; le righe successive vengono saltate e il controllo passa al blocco `except`. Nulla di ciò che è stato fatto nel blocco `try` viene annullato, quindi tienilo il più corto possibile:
```python
def half(n):
    try:
        return 10 / n
    except ZeroDivisionError:
        return "undefined"

print(half(2))  # 5.0
print(half(0))  # undefined
```
Un `return` dentro `except` funziona come qualsiasi altro `return`, il che rende `try`/`except` un modo naturale per restituire un valore di ripiego invece di andare in crash.

# --instructions--

Scrivi una funzione `safe_divide(a, b)` che restituisca `a / b`, oppure la stringa `"undefined"` quando `b` è `0`, catturando `ZeroDivisionError`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_divide(9, 3)` deve restituire `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_divide(9, 3), 3.0, "--err-t1--")
```

`safe_divide(1, 0)` deve restituire `"undefined"`

```python
    def test_2(self):
        self.assertEqual(safe_divide(1, 0), "undefined", "--err-t2--")
```

`safe_divide(-5, 2)` deve restituire `-2.5`

```python
    def test_3(self):
        self.assertEqual(safe_divide(-5, 2), -2.5, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "undefined"
```
