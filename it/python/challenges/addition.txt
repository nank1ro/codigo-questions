<section id="options">
{
"language": "python",
"type": "runCode",
"showRun": true
}
</section>

<section id="description">
Dati due number interi `num1` e `num2`, scrivi un programma per sommare questi due numeri
</section>

<section id="instructions">
Scrivi una funzione che restituisca la somma tra i due numeri
</section>

<section id="seed">
def somma():
	pass
</section>

<section id="test">
import unittest

from addition import somma

class AdditionTest(unittest.TestCase):
    def test_addition1(self):
        self.assertEqual(somma(1, 3), 4)

    def test_addition2(self):
        self.assertEqual(somma(200, 210), 410)

    def test_addition3(self):
        self.assertEqual(somma(15, 35), 50)


if __name__ == "__main__":
    unittest.main()
</section>