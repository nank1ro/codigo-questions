---
language: python
exerciseType: 1
---

# --description--

Le comprehension sono ottime per **trasformare le stringhe**.
Chiama un metodo stringa su ogni elemento, oppure costruisci una nuova stringa con una f-string:
```python
names = ["ana", "bob"]
greetings = [f"Hi {name.capitalize()}" for name in names]
print(greetings)  # ['Hi Ana', 'Hi Bob']
```

# --instructions--

Salva nella variabile `shout` una lista con ogni nome di `names` in maiuscolo seguito da `!`, usando una list comprehension

# --seed--

```python
names = ["ana", "bob", "cy"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare una list comprehension

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`shout` deve essere uguale a `['ANA!', 'BOB!', 'CY!']`

```python
    def test_1(self):
        self.assertEqual(shout, ["ANA!", "BOB!", "CY!"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
names = ["ana", "bob", "cy"]
shout = [name.upper() + "!" for name in names]
```
