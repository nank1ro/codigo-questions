---
language: python
exerciseType: 1
---

# --description--

Le due condizioni possono essere combinate nella stessa comprehension: un `if-else` per scegliere il valore, e un filtro `if` alla fine per saltare alcuni elementi.
```python
nums = [1, 2, 3, 4]
result = ["big" if n > 2 else "small" for n in nums if n != 3]
print(result)  # ['small', 'small', 'big']
```

# --instructions--

Salva nella variabile `clipped` una copia di `nums` in cui ogni numero negativo viene sostituito da `0`, usando una list comprehension con un'espressione `if-else`

# --seed--

```python
nums = [4, -2, 7, -5]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare una list comprehension con un'espressione `if-else`

```json
{
  "regex": "\\[.* if .* else .* for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`clipped` deve essere uguale a `[4, 0, 7, 0]`

```python
    def test_1(self):
        self.assertEqual(clipped, [4, 0, 7, 0], "--err-t1--")
```

`nums` non deve essere modificato

```python
    def test_2(self):
        self.assertEqual(nums, [4, -2, 7, -5], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [4, -2, 7, -5]
clipped = [n if n >= 0 else 0 for n in nums]
```
