---
language: python
exerciseType: 4
---

# --description--

Il `for` interno può usare la variabile di quello esterno.
Questo è il modo classico per **appiattire** una lista di liste in un'unica lista:
```python
matrix = [[1, 2], [3, 4]]
flat = [n for row in matrix for n in row]
print(flat)  # [1, 2, 3, 4]
```
La funzione `sum()` poi somma tutti i numeri di una lista.

# --instructions--

Ordina gli elementi per appiattire `matrix` e stampare la lista appiattita insieme alla somma dei suoi numeri

# --answers--

```python
matrix = [[1, 2], [3, 4]]
```

```python
flat = [n for row in matrix for n in row]
```

```python
total = sum(flat)
```

```python
print(flat, total)
```

# --solutions--

```python
matrix = [[1, 2], [3, 4]]
flat = [n for row in matrix for n in row]
total = sum(flat)
print(flat, total)
```

# --output--

[1, 2, 3, 4] 10
