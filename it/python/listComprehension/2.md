---
language: python
exerciseType: 1
---

# --description--

L'espressione a sinistra può essere qualsiasi cosa che produca un valore: un calcolo, una chiamata a funzione, una chiamata a metodo.
La variabile del ciclo può avere qualsiasi nome tu voglia, ed esiste solo all'interno delle parentesi quadre:
```python
prices = [10, 20]
with_tax = [price * 1.2 for price in prices]
print(with_tax)  # [12.0, 24.0]
```

# --instructions--

Salva nella variabile `squares` la lista dei quadrati di `nums`, usando una list comprehension

# --seed--

```python
nums = [1, 2, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare una list comprehension

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`squares` deve essere uguale a `[1, 4, 9, 16]`

```python
    def test_1(self):
        self.assertEqual(squares, [1, 4, 9, 16], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 3, 4]
squares = [n * n for n in nums]
```
