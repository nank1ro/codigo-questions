---
language: python
exerciseType: 1
---

# --description--

Due set sono **uguali** quando contengono gli stessi elementi, indipendentemente dall'ordine in cui sono stati scritti:
```python
print({1, 2, 3} == {3, 1, 2})  # True
```
Confrontare la lunghezza di una collezione con la lunghezza del suo set è un modo rapido per rilevare i duplicati: se il set è **più piccolo**, qualche valore è comparso più di una volta:
```python
emails = ["a@x.com", "b@x.com", "a@x.com"]
print(len(set(emails)) < len(emails))  # True
```
Il metodo delle liste `count(value)` indica quante volte un valore compare, il che aiuta a trovare *quali* valori sono duplicati.

# --instructions--

Memorizza in `has_duplicates` se `emails` contiene qualche indirizzo ripetuto, confrontando la lunghezza della lista con la lunghezza del suo set, poi memorizza in `duplicates` il set degli indirizzi che compaiono più di una volta, usando una set comprehension e `count()`

# --seed--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare `set()`

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Devi usare una set comprehension

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_duplicates` deve essere `True`

```python
    def test_1(self):
        self.assertEqual(has_duplicates, True, "--err-t1--")
```

`duplicates` deve essere uguale a `{'ann@x.com', 'bob@x.com'}`

```python
    def test_2(self):
        self.assertEqual(duplicates, {"ann@x.com", "bob@x.com"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
has_duplicates = len(set(emails)) < len(emails)
duplicates = {e for e in emails if emails.count(e) > 1}
```
