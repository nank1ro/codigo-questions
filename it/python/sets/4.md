---
language: python
exerciseType: 2
---

# --description--

I set sono **mutabili**: puoi aggiungere e rimuovere elementi dopo averli creati.
`add(value)` inserisce un valore; aggiungerne uno già presente non cambia nulla:
```python
letters = {"a", "b"}
letters.add("c")
letters.add("a")
print(len(letters))  # 3
```
Ci sono due modi per rimuovere un elemento:
- `remove(value)` lo elimina, ma solleva un `KeyError` se il valore non è nel set
- `discard(value)` lo elimina se presente e **non fa nulla** altrimenti, senza errori
```python
letters.remove("a")
letters.discard("z")  # "z" non c'è, ma nessun errore
letters.remove("z")   # KeyError: 'z'
```

# --instructions--

Completa il codice per aggiungere `"c"` a `letters`, poi rimuovere `"z"` (che non è nel set) senza sollevare un errore, quindi stampa quanti elementi contiene il set

# --seed--

```python
letters = {"a", "b"}
letters.[/]("c")
letters.[/]("z")
print([/](letters))
```

# --answers--

- add
- discard
- len
- remove
- append
- size

# --solutions--

```python
letters = {"a", "b"}
letters.add("c")
letters.discard("z")
print(len(letters))
```

# --output--

3
