---
language: python
exerciseType: 1
---

# --description--

La **differenza** `a - b` contiene gli elementi di `a` che **non** sono in `b`.
L'ordine conta: `a - b` e `b - a` sono di solito diversi:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a - b)  # {1}
print(b - a)  # {4}
```
La **differenza simmetrica** `a ^ b` contiene gli elementi presenti in **esattamente uno** dei due set:
```python
print(a ^ b)  # {1, 4}
```
Le forme metodo sono `difference()` e `symmetric_difference()`.

# --instructions--

Memorizza in `only_ann` gli hobby di `ann` che `bob` non ha, usando `-`, e in `not_shared` gli hobby che ha solo uno dei due, usando `^`

# --seed--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare l'operatore `-`

```json
{
  "regex": "\\s-\\s",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Devi usare l'operatore `^`

```json
{
  "regex": "\\^",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`only_ann` deve essere uguale a `{'tennis', 'yoga'}`

```python
    def test_1(self):
        self.assertEqual(only_ann, {"tennis", "yoga"}, "--err-t1--")
```

`not_shared` deve essere uguale a `{'tennis', 'yoga', 'swimming'}`

```python
    def test_2(self):
        self.assertEqual(not_shared, {"tennis", "yoga", "swimming"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
only_ann = ann - bob
not_shared = ann ^ bob
```
