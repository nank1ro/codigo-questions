---
language: python
exerciseType: 4
---

# --description--

I set possono anche essere **confrontati** tra loro.
`a.issubset(b)`, oppure `a <= b`, è `True` quando ogni elemento di `a` è anche in `b`.
`a.issuperset(b)`, oppure `a >= b`, è `True` quando `a` contiene ogni elemento di `b`.
`a.isdisjoint(b)` è `True` quando i due set non hanno **nessun** elemento in comune:
```python
small = {1, 2}
big = {1, 2, 3}
print(small <= big)              # True
print(big.issuperset(small))     # True
print(small.isdisjoint({8, 9}))  # True
```

# --instructions--

Ordina gli elementi per costruire `palette` a partire da `primary`, poi stampa, in quest'ordine: se `primary` è un sottoinsieme di `palette`, se `palette` è un sottoinsieme di `primary`, e se `primary` e `{"black"}` sono disgiunti

# --answers--

```python
primary = {"red", "blue", "yellow"}
```

```python
palette = primary | {"green"}
```

```python
print(primary <= palette)
```

```python
print(palette.issubset(primary))
```

```python
print(primary.isdisjoint({"black"}))
```

# --solutions--

```python
primary = {"red", "blue", "yellow"}
palette = primary | {"green"}
print(primary <= palette)
print(palette.issubset(primary))
print(primary.isdisjoint({"black"}))
```

# --output--

True
False
True
