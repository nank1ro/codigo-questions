---
language: python
exerciseType: 1
---

# --description--

Una **set comprehension** costruisce un set in un'unica espressione, con la stessa sintassi di una list comprehension ma con le parentesi graffe:
```python
nums = [1, 2, 2, 3]
squares = {n * n for n in nums}
print(squares)  # {1, 4, 9}
```
Un `if` opzionale filtra gli elementi, e i duplicati prodotti dall'espressione vengono scartati automaticamente:
```python
evens = {n for n in range(10) if n % 2 == 0}
print(evens)  # {0, 2, 4, 6, 8}
```

# --instructions--

Memorizza in `lowered` il set delle parole di `words` convertite in minuscolo, usando una set comprehension, poi in `long_words` il set degli elementi di `lowered` con più di 5 caratteri, usando una set comprehension con `if`

# --seed--

```python
words = ["Apple", "banana", "APPLE", "Cherry", "banana"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare una set comprehension

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`lowered` deve essere uguale a `{'apple', 'banana', 'cherry'}`

```python
    def test_1(self):
        self.assertEqual(lowered, {"apple", "banana", "cherry"}, "--err-t1--")
```

`long_words` deve essere uguale a `{'banana', 'cherry'}`

```python
    def test_2(self):
        self.assertEqual(long_words, {"banana", "cherry"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
words = ["Apple", "banana", "APPLE", "Cherry", "banana"]
lowered = {w.lower() for w in words}
long_words = {w for w in lowered if len(w) > 5}
```
