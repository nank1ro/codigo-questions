---
language: python
exerciseType: 1
---

# --description--

Un set può contenere solo elementi **hashabili**, cioè valori che non possono cambiare: numeri, stringhe, `True`/`False` e **tuple**.
Provare ad aggiungere una lista, un dizionario o un altro set solleva un `TypeError`:
```python
points = set()
points.add((1, 2))  # ok, una tupla
points.add([1, 2])  # TypeError: unhashable type: 'list'
```
I set di tuple sono comodi per tenere traccia di coppie uniche, come coordinate o record (nome, età):
```python
visits = [(1, 2), (1, 2), (3, 4)]
print(len(set(visits)))  # 2
```

# --instructions--

Memorizza in `points` il set delle coordinate distinte in `visits`, aggiungici il punto `(0, 0)`, poi memorizza in `count` quanti punti distinti contiene

# --seed--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare `add()`

```json
{
  "regex": "\\.add\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`points` deve essere uguale a `{(0, 0), (1, 2), (3, 4), (5, 6)}`

```python
    def test_1(self):
        self.assertEqual(points, {(0, 0), (1, 2), (3, 4), (5, 6)}, "--err-t1--")
```

`count` deve essere uguale a `4`

```python
    def test_2(self):
        self.assertEqual(count, 4, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
points = set(visits)
points.add((0, 0))
count = len(points)
```
