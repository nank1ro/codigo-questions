---
language: python
exerciseType: 2
---

# --description--

L'**intersezione** di due set contiene solo gli elementi presenti in **entrambi**.
Usa l'operatore `&` o il metodo `intersection()`:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a & b)               # {2, 3}
print(a.intersection(b))   # {2, 3}
```
Se i set non hanno nulla in comune, il risultato è un set vuoto.

# --instructions--

Completa il codice per memorizzare in `common` gli elementi presenti sia in `a` che in `b`, in modo che il programma stampi `[3, 4]`

# --seed--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a [/] b
print(sorted(common))
```

# --answers--

- &
- |
- -
- ^
- and

# --solutions--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a & b
print(sorted(common))
```

# --output--

[3, 4]
