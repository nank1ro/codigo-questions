---
language: python
exerciseType: 1
---

# --description--

`pop()` rimuove **un elemento arbitrario** dal set e lo restituisce.
Poiché un set non ha un ordine, non puoi scegliere quale elemento viene rimosso; chiamare `pop()` su un set vuoto solleva un `KeyError`:
```python
tickets = {101, 102, 103}
picked = tickets.pop()
print(len(tickets))  # 2
```
`clear()` rimuove **tutti** gli elementi, lasciando un set vuoto:
```python
tickets.clear()
print(len(tickets))  # 0
```

# --instructions--

Rimuovi un elemento da `tickets` con `pop()` e memorizzalo nella variabile `picked`, poi svuota il set `drafts` con `clear()`

# --seed--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare `pop()`

```json
{
  "regex": "\\.pop\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Devi usare `clear()`

```json
{
  "regex": "\\.clear\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`picked` deve essere uno dei ticket originali

```python
    def test_1(self):
        self.assertIn(picked, {101, 102, 103}, "--err-t1--")
```

`tickets` deve contenere 2 elementi e non deve più contenere `picked`

```python
    def test_2(self):
        self.assertEqual(len(tickets), 2, "--err-t2--")
        self.assertNotIn(picked, tickets, "--err-t2--")
```

`drafts` deve essere vuoto

```python
    def test_3(self):
        self.assertEqual(drafts, set(), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
picked = tickets.pop()
drafts.clear()
```
