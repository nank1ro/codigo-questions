---
language: python
exerciseType: 1
---

# --description--

Alcuni commenti seguono una convenzione che gli editor capiscono. I **marcatori** più comuni sono:
- `# TODO: ...` segnala qualcosa che deve ancora essere scritto
- `# FIXME: ...` segnala del codice notoriamente sbagliato che deve essere corretto

```python
limit = 10
# TODO: read the limit from the settings
```
Per Python sono commenti ordinari; gli editor li raccolgono in un pannello dedicato, così il lavoro in sospeso è facile da trovare. Un `TODO` di solito sta accanto a un segnaposto che mantiene il programma in esecuzione finché il vero codice non viene scritto.

Quando finisci il lavoro, sostituisci il segnaposto ed elimina il marcatore nella stessa modifica, così il commento non mente mai sullo stato del codice.

# --instructions--

Completa il lavoro descritto dal commento `TODO` in `is_even`, poi rimuovi il commento visto che il lavoro è fatto

# --seed--

```python
def is_even(n):
    # TODO: return True when n is even, False otherwise
    return False
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Il commento `TODO` deve essere rimosso

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`is_even(4)` deve restituire `True`

```python
    def test_1(self):
        self.assertEqual(is_even(4), True, "--err-t1--")
```

`is_even(7)` deve restituire `False`

```python
    def test_2(self):
        self.assertEqual(is_even(7), False, "--err-t2--")
```

`is_even(0)` deve restituire `True`

```python
    def test_3(self):
        self.assertEqual(is_even(0), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_even(n):
    return n % 2 == 0
```
