---
language: python
exerciseType: 1
---

# --description--

Quando una riga non basta, una docstring cresce fino a diventare un layout fisso, descritto nella **PEP 257**: un riassunto di una riga, una riga vuota, poi i dettagli, e le virgolette `"""` di chiusura su una riga a sé.
```python
def to_seconds(minutes):
    """Returns the number of seconds in the given minutes.

    minutes is a whole number and is never negative.
    """
    return minutes * 60
```
La riga vuota è importante: gli strumenti mostrano la prima riga da sola, come breve descrizione, e conservano il resto per chi vuole leggere di più.

# --instructions--

Dai ad `area` una docstring fatta di un riassunto di una riga, una riga vuota, una riga di dettaglio, e le virgolette `"""` di chiusura su una riga a sé

# --seed--

```python
def area(width, height):
    return width * height
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`area` deve avere una docstring

```python
    def test_1(self):
        self.assertIsNotNone(area.__doc__, "--err-t1--")
```

La docstring deve iniziare con una riga di riassunto seguita da una riga vuota e da altro testo

```python
    def test_2(self):
        lines = area.__doc__.split("\n")
        self.assertTrue(len(lines) >= 3, "--err-t2--")
        self.assertNotEqual(lines[0].strip(), "", "--err-t2--")
        self.assertEqual(lines[1].strip(), "", "--err-t2--")
        self.assertNotEqual(lines[2].strip(), "", "--err-t2--")
        self.assertEqual(lines[-1].strip(), "", "--err-t2--")
```

`area(3, 4)` deve restituire `12` e `area(0, 5)` deve restituire `0`

```python
    def test_3(self):
        self.assertEqual(area(3, 4), 12, "--err-t3--")
        self.assertEqual(area(0, 5), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def area(width, height):
    """Returns the area of a rectangle.

    Both sides are whole numbers of centimetres.
    """
    return width * height
```
