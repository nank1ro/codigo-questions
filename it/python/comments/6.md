---
language: python
exerciseType: 1
---

# --description--

Quando una stringa è la **prima istruzione** dentro una funzione, Python la tratta come la documentazione di quella funzione. Si chiama **docstring**:
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"
```
Per convenzione una docstring si scrive tra triple virgolette doppie, `"""`, anche quando sta su una riga, così può crescere in seguito senza dover cambiare le virgolette.

Scrivi il riassunto in terza persona, come se descrivessi la funzione: "Restituisce...", "Somma...", "Controlla...". La docstring deve venire prima di qualsiasi altra istruzione nel corpo, altrimenti è solo una stringa ordinaria.

# --instructions--

Scrivi una funzione `double(n)` che restituisce `n` moltiplicato per due, e dale una docstring di una riga

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`double` deve avere una docstring come prima istruzione

```python
    def test_1(self):
        self.assertIsNotNone(double.__doc__, "--err-t1--")
```

`double(21)` deve restituire `42`

```python
    def test_2(self):
        self.assertEqual(double(21), 42, "--err-t2--")
```

`double(-3)` deve restituire `-6` e `double(0)` deve restituire `0`

```python
    def test_3(self):
        self.assertEqual(double(-3), -6, "--err-t3--")
        self.assertEqual(double(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def double(n):
    """Returns n multiplied by two."""
    return n * 2
```
