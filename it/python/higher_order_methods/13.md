---
language: python
exerciseType: 1
---

# --description--

Un decoratore che accetta un solo argomento è di poca utilità. Per avvolgere **qualsiasi** funzione, il wrapper raccoglie ogni argomento posizionale in `*args` e ogni argomento per parola chiave in `**kwargs`, e li inoltra invariati:
```python
def uppercase(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs).upper()
    return wrapper

@uppercase
def greet(name, punctuation="!"):
    return "hello " + name + punctuation

print(greet("ada"))                    # HELLO ADA!
print(greet("ada", punctuation="?"))   # HELLO ADA?
```
All'interno del wrapper, `args` è una tupla e `kwargs` un dizionario; `*` e `**` nella chiamata li spacchettano di nuovo in argomenti separati.

# --instructions--

Scrivi un decoratore `exclaim` che restituisce un wrapper che accetta qualsiasi argomento, chiama la funzione avvolta con essi e restituisce il suo risultato con `"!"` aggiunto in coda; poi usalo per decorare una funzione `greet` che prende un `name` e restituisce `"hello <name>"`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi applicare il decoratore con `@exclaim`

```json
{
  "regex": "^@exclaim\\s*$",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greet("ada")` deve restituire `"hello ada!"`

```python
    def test_1(self):
        self.assertEqual(greet("ada"), "hello ada!", "--err-t1--")
```

`exclaim` deve funzionare con una funzione di due argomenti

```python
    def test_2(self):
        joined = exclaim(lambda a, b: a + " " + b)
        self.assertEqual(joined("good", "day"), "good day!", "--err-t2--")
```

`exclaim` deve inoltrare gli argomenti per parola chiave

```python
    def test_3(self):
        tag = exclaim(lambda word, times=1: word * times)
        self.assertEqual(tag("hi", times=2), "hihi!", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def exclaim(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs) + "!"
    return wrapper

@exclaim
def greet(name):
    return "hello " + name
```
