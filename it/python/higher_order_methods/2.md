---
language: python
exerciseType: 1
---

# --description--

Passare una funzione come argomento permette a chi chiama di decidere **cosa** fare, mentre la funzione di ordine superiore decide **quante volte** o **su cosa**. Il parametro funzione può essere chiamato quante volte serve, e il suo risultato può essere reimmesso nella funzione:
```python
def repeat(func, value, times):
    for _ in range(times):
        value = func(value)
    return value

def add_one(n):
    return n + 1

print(repeat(add_one, 0, 3))  # 3
```
Va bene qualsiasi callable: una funzione `def`, un built-in come `len` o una `lambda`.

# --instructions--

Scrivi una funzione `apply_twice` che prende una funzione `func` e un `value`, e restituisce il risultato dell'applicazione di `func` a `value` due volte di fila

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`apply_twice(lambda x: x * 2, 3)` deve restituire `12`

```python
    def test_1(self):
        self.assertEqual(apply_twice(lambda x: x * 2, 3), 12, "--err-t1--")
```

`apply_twice(lambda s: s + "!", "hi")` deve restituire `"hi!!"`

```python
    def test_2(self):
        self.assertEqual(apply_twice(lambda s: s + "!", "hi"), "hi!!", "--err-t2--")
```

La seconda chiamata deve ricevere il risultato della prima

```python
    def test_3(self):
        seen = []
        def record(x):
            seen.append(x)
            return x + 10
        self.assertEqual(apply_twice(record, 0), 20, "--err-t3--")
        self.assertEqual(seen, [0, 10], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def apply_twice(func, value):
    return func(func(value))
```
