---
language: python
exerciseType: 1
---

# --description--

Il built-in `filter(func, iterable)` conserva solo gli elementi per cui `func` restituisce un valore vero. Come `map`, restituisce un oggetto pigro che deve essere trasformato in una lista:
```python
numbers = [4, -2, 7, 0]
print(list(filter(lambda n: n > 0, numbers)))  # [4, 7]
```
La funzione passata a `filter` è chiamata **predicato**: prende un elemento e risponde a una domanda sì/no su di esso. Passare `None` al posto di una funzione conserva gli elementi veri di per sé, scartando `0`, `""` e `None`.

# --instructions--

Scrivi una funzione `positives` che prende una lista di numeri e restituisce una nuova lista con solo i numeri maggiori di zero, usando `filter`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare `filter`

```json
{
  "regex": "filter\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`positives([3, -1, 0, 5])` deve restituire `[3, 5]`

```python
    def test_1(self):
        self.assertEqual(positives([3, -1, 0, 5]), [3, 5], "--err-t1--")
```

Il risultato deve essere una lista

```python
    def test_2(self):
        self.assertIsInstance(positives([1, 2]), list, "--err-t2--")
        self.assertEqual(positives([1, 2]), [1, 2], "--err-t2--")
```

`positives([-4, 0])` deve restituire una lista vuota

```python
    def test_3(self):
        self.assertEqual(positives([-4, 0]), [], "--err-t3--")
        self.assertEqual(positives([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def positives(numbers):
    return list(filter(lambda n: n > 0, numbers))
```
