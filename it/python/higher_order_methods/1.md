---
language: python
exerciseType: 2
---

# --description--

In Python una funzione è un **valore**, proprio come un numero o una stringa. Puoi salvarla in una variabile, metterla in una lista o passarla a un'altra funzione. Sono solo le parentesi a chiamarla: `shout` è la funzione stessa, `shout("hi")` è il suo risultato:
```python
def shout(text):
    return text.upper() + "!"

say = shout
print(say("hi"))  # HI!
```
Una funzione che riceve un'altra funzione come parametro, o che ne restituisce una, è chiamata **funzione di ordine superiore**. Al suo interno, il parametro viene chiamato con le parentesi come qualsiasi altra funzione:
```python
def apply(func, value):
    return func(value)

print(apply(shout, "bye"))  # BYE!
print(apply(len, "bye"))    # 3
```

# --instructions--

Completa il codice in modo che `apply` chiami la funzione che riceve, e che `say` si riferisca a `shout` senza chiamarla, producendo l'output `HI!`, `3`

# --seed--

```python
def shout(text):
    return text.upper() + "!"

def apply(func, value):
    return [/](value)

say = [/]
print(say("hi"))
print(apply(len, "bye"))
```

# --answers--

- func
- shout
- shout()
- value
- print

# --solutions--

```python
def shout(text):
    return text.upper() + "!"

def apply(func, value):
    return func(value)

say = shout
print(say("hi"))
print(apply(len, "bye"))
```

# --output--

HI!
3
