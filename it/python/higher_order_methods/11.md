---
language: python
exerciseType: 1
---

# --description--

`partial` è comodo con i built-in che accettano opzioni. `int(text, base=16)` analizza una stringa esadecimale; fissare la base dà un convertitore a un solo argomento, adatto a `map`:
```python
from functools import partial

hex_to_int = partial(int, base=16)
print(hex_to_int("ff"))                      # 255
print(list(map(hex_to_int, ["a", "10"])))    # [10, 16]
```
L'oggetto partial ricorda cosa avvolge: `hex_to_int.func` è `int`, e `hex_to_int.keywords` è `{'base': 16}`.

# --instructions--

Definisci due funzioni con `partial`: `hex_to_int` che converte una stringa esadecimale in un `int`, e `binary_to_int` che converte una stringa binaria in un `int`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`hex_to_int("ff")` deve restituire `255`

```python
    def test_1(self):
        self.assertEqual(hex_to_int("ff"), 255, "--err-t1--")
        self.assertEqual(hex_to_int("10"), 16, "--err-t1--")
```

`binary_to_int("1010")` deve restituire `10`

```python
    def test_2(self):
        self.assertEqual(binary_to_int("1010"), 10, "--err-t2--")
        self.assertEqual(binary_to_int("11"), 3, "--err-t2--")
```

Entrambe devono essere oggetti partial che avvolgono `int`

```python
    def test_3(self):
        self.assertIs(hex_to_int.func, int, "--err-t3--")
        self.assertIs(binary_to_int.func, int, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import partial

hex_to_int = partial(int, base=16)
binary_to_int = partial(int, base=2)
```
