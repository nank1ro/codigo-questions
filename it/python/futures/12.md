---
language: python
exerciseType: 1
---

# --description--

Un'eccezione sollevata dentro una coroutine non appare nel punto in cui l'oggetto coroutine è stato creato: viene sollevata dall'`await` che la esegue, oppure da `asyncio.run` per quella più esterna. `try`/`except` deve quindi avvolgere **l'`await`**:
```python
import asyncio

async def load(path):
    await asyncio.sleep(0.01)
    if path == "":
        raise FileNotFoundError("empty path")
    return "contents"

async def safe_load(path):
    try:
        return await load(path)
    except FileNotFoundError:
        return ""

print(asyncio.run(safe_load("")))  # prints an empty line
```
Un'eccezione che nessuno cattura si propaga attraverso ogni `await` fino a `asyncio.run`, che la rilancia nel codice sincrono, esattamente come un normale stack di chiamate.

# --instructions--

Scrivi una funzione coroutine `divide(a, b)` che attende `0.01` secondi e restituisce `a / b`, e una funzione coroutine `safe_divide(a, b)` che attende `divide(a, b)` e ne restituisce il risultato, oppure `None` se viene sollevata una `ZeroDivisionError`

# --before-asserts--

```python
import asyncio
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`divide(10, 4)` deve restituire `2.5`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(divide(10, 4)), 2.5, "--err-t1--")
```

`divide(1, 0)` deve sollevare una `ZeroDivisionError` quando viene eseguita

```python
    def test_2(self):
        with self.assertRaises(ZeroDivisionError, msg="--err-t2--"):
            asyncio.run(divide(1, 0))
```

`safe_divide(10, 2)` deve restituire `5.0` e `safe_divide(1, 0)` deve restituire `None`

```python
    def test_3(self):
        self.assertEqual(asyncio.run(safe_divide(10, 2)), 5.0, "--err-t3--")
        self.assertIsNone(asyncio.run(safe_divide(1, 0)), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def divide(a, b):
    await asyncio.sleep(0.01)
    return a / b

async def safe_divide(a, b):
    try:
        return await divide(a, b)
    except ZeroDivisionError:
        return None
```
