---
language: python
exerciseType: 2
---

# --description--

`asyncio.wait_for(awaitable, timeout)` attende qualcosa ma rinuncia dopo `timeout` secondi: l'operazione viene annullata e viene sollevata una `TimeoutError`, che può essere catturata come qualsiasi eccezione:
```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())  # timed out
```
Con un timeout di `0.1` lo stesso codice stamperebbe `done`. `asyncio.TimeoutError` è un altro nome per la `TimeoutError` integrata.

# --instructions--

Completa il codice in modo che `slow()` venga attesa con un timeout di `0.01` secondi e che il timeout venga catturato, producendo l'output `timed out`

# --seed--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.[/](slow(), [/]=0.01)
        print(result)
    except [/]:
        print("timed out")

asyncio.run(main())
```

# --answers--

- wait_for
- timeout
- TimeoutError
- gather
- delay
- ValueError

# --solutions--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())
```

# --output--

timed out
