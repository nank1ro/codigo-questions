---
language: python
exerciseType: 1
---

# --description--

Le coroutine si chiamano tra loro con `await`. Una coroutine può attendere qualsiasi altra coroutine, riceverne il valore di ritorno e continuare, esattamente come una catena di chiamate di funzione:
```python
import asyncio

async def fetch_price(item):
    await asyncio.sleep(0.01)  # simulates a slow lookup
    return 10

async def total(item, quantity):
    price = await fetch_price(item)
    return price * quantity

print(asyncio.run(total("pen", 3)))  # 30
```
Solo la coroutine più esterna passa attraverso `asyncio.run`; ogni coroutine interna viene raggiunta con `await`. Se `total` fosse un `def` normale, non potrebbe usare `await` affatto: la parola chiave `async` si estende a ogni funzione della catena che deve attendere.

# --instructions--

Scrivi una funzione coroutine `fetch_user(user_id)` che attende `0.01` secondi e restituisce la stringa `"user"` seguita dall'id, e una funzione coroutine `greet_user(user_id)` che attende `fetch_user` e restituisce `"Hello, "` seguito dal nome ottenuto

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch_user(1)` deve restituire `"user1"` e `fetch_user(42)` deve restituire `"user42"`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch_user(1)), "user1", "--err-t1--")
        self.assertEqual(asyncio.run(fetch_user(42)), "user42", "--err-t1--")
```

`greet_user(1)` deve restituire `"Hello, user1"`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(greet_user(1)), "Hello, user1", "--err-t2--")
```

Sia `fetch_user` che `greet_user` devono essere funzioni coroutine

```python
    def test_3(self):
        self.assertTrue(inspect.iscoroutinefunction(fetch_user), "--err-t3--")
        self.assertTrue(inspect.iscoroutinefunction(greet_user), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch_user(user_id):
    await asyncio.sleep(0.01)
    return f"user{user_id}"

async def greet_user(user_id):
    name = await fetch_user(user_id)
    return f"Hello, {name}"
```
