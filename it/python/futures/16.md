---
language: python
exerciseType: 1
---

# --description--

I pezzi si combinano naturalmente. Per eseguire molte operazioni in modo concorrente, ciascuna con il proprio limite di tempo, avvolgi ognuna in una piccola coroutine che applica `wait_for` e cattura la `TimeoutError`, poi passa i wrapper a `gather`:
```python
async def guarded(coro, limit):
    try:
        return await asyncio.wait_for(coro, timeout=limit)
    except TimeoutError:
        return None
```
`gather(*[guarded(job(x), limit) for x in items])` restituisce quindi un valore per ogni job terminato in tempo e `None` per ciascuno che non ci è riuscito, nell'ordine originale, e l'intero blocco richiede al massimo circa `limit` secondi.

# --instructions--

Scrivi una funzione coroutine `job(delay)` che attende `delay` secondi e restituisce `delay`, e una funzione coroutine `run_jobs(delays, limit)` che esegue un `job` per ogni delay **in modo concorrente**, ognuno limitato a `limit` secondi con `asyncio.wait_for`, e restituisce una lista con il delay di ogni job terminato in tempo e `None` per ogni job andato in timeout, nell'ordine originale

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`job(0.01)` deve restituire `0.01`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(job(0.01)), 0.01, "--err-t1--")
```

`run_jobs([0.01, 0.06, 0.02], 0.04)` deve restituire `[0.01, None, 0.02]` e `run_jobs([], 0.01)` deve restituire `[]`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(run_jobs([0.01, 0.06, 0.02], 0.04)), [0.01, None, 0.02], "--err-t2--")
        self.assertEqual(asyncio.run(run_jobs([], 0.01)), [], "--err-t2--")
```

Cinque job di `0.02` secondi con un limite di `0.05` devono tutti terminare ed essere eseguiti in modo concorrente, impiegando ben meno di `0.1` secondi

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(run_jobs([0.02] * 5, 0.05))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [0.02] * 5, "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def job(delay):
    await asyncio.sleep(delay)
    return delay

async def guarded(delay, limit):
    try:
        return await asyncio.wait_for(job(delay), timeout=limit)
    except TimeoutError:
        return None

async def run_jobs(delays, limit):
    return await asyncio.gather(*[guarded(d, limit) for d in delays])
```
