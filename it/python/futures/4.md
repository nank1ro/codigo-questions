---
language: python
exerciseType: 3
---

# --description--

Chiamare una funzione coroutine non basta per eseguirla. La chiamata costruisce soltanto un oggetto coroutine; il corpo viene eseguito quando quell'oggetto viene atteso con `await` o passato a `asyncio.run`:
```python
async def hello():
    print("hi")

hello()  # nothing is printed
```
Python lo segnala persino con un avviso: `RuntimeWarning: coroutine 'hello' was never awaited`. Un `await` dimenticato è il bug asincrono più comune: il codice sembra chiamato ma non viene mai eseguito, e ogni variabile che dovrebbe contenere il suo risultato contiene invece un oggetto coroutine.

# --instructions--

Cosa stampa questo codice?
```python
import asyncio

async def hello():
    print("hi")
    return 1

async def main():
    result = hello()
    print("main")

asyncio.run(main())
```

# --answers--

- Solo `main`, perché `hello()` crea un oggetto coroutine che non viene mai atteso, quindi il suo corpo non viene eseguito
- `hi` poi `main`, perché chiamare una funzione coroutine la esegue subito
- `main` poi `hi`, perché la coroutine viene eseguita quando `main` restituisce
- Un `TypeError`, perché una coroutine non può essere assegnata a una variabile

# --solutions--

- Solo `main`, perché `hello()` crea un oggetto coroutine che non viene mai atteso, quindi il suo corpo non viene eseguito
