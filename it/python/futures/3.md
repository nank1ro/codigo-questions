---
language: python
exerciseType: 1
---

# --description--

`await` mette in pausa la coroutine corrente finché l'operazione attesa non è completata, poi ne restituisce il risultato. Mentre la coroutine è in pausa, l'event loop è libero di eseguire altre coroutine. `await` è consentito solo dentro un `async def`.

`asyncio.sleep(seconds)` è l'awaitable più semplice: attende per il tempo indicato senza bloccare il loop. A differenza di `time.sleep`, deve essere atteso con `await`:
```python
import asyncio

async def countdown():
    print("ready")
    await asyncio.sleep(0.05)
    print("go")

asyncio.run(countdown())
```
Il programma stampa `ready`, attende 50 millisecondi e stampa `go`. Scrivere `asyncio.sleep(0.05)` senza `await` crea l'oggetto coroutine ma non lo esegue mai, quindi non avviene alcuna attesa.

# --instructions--

Scrivi una funzione coroutine `wait_and_return(value, delay)` che attende `delay` secondi con `asyncio.sleep` e poi restituisce `value`

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`wait_and_return("ok", 0.01)` deve restituire `"ok"`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(wait_and_return("ok", 0.01)), "ok", "--err-t1--")
```

`wait_and_return(7, 0.02)` deve restituire `7` e richiedere almeno `0.02` secondi

```python
    def test_2(self):
        start = time.perf_counter()
        result = asyncio.run(wait_and_return(7, 0.02))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, 7, "--err-t2--")
        self.assertGreaterEqual(elapsed, 0.018, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def wait_and_return(value, delay):
    await asyncio.sleep(delay)
    return value
```
