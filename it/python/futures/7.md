---
language: python
exerciseType: 1
---

# --description--

La concorrenza dà i suoi frutti quando il numero di operazioni non è fisso. Costruisci gli oggetti coroutine in una list comprehension, scompattali in `gather` e attendi l'intero blocco in una volta sola:
```python
import asyncio

async def fetch(url):
    await asyncio.sleep(0.02)
    return len(url)

async def fetch_all(urls):
    return await asyncio.gather(*[fetch(u) for u in urls])

print(asyncio.run(fetch_all(["a.com", "bb.com"])))  # [5, 6]
```
Dieci url richiedono comunque circa 20 millisecondi in totale invece di 200, e i risultati restano nell'ordine degli url.

# --instructions--

Scrivi una funzione coroutine `fetch(item_id)` che attende `0.02` secondi e restituisce `item_id * 10`, e una funzione coroutine `fetch_all(ids)` che esegue `fetch` per ogni id **in modo concorrente** con `asyncio.gather` e restituisce la lista dei risultati

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch(3)` deve restituire `30`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch(3)), 30, "--err-t1--")
```

`fetch_all([1, 2, 3])` deve restituire `[10, 20, 30]` e `fetch_all([])` deve restituire `[]`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(fetch_all([1, 2, 3])), [10, 20, 30], "--err-t2--")
        self.assertEqual(asyncio.run(fetch_all([])), [], "--err-t2--")
```

`fetch_all` con cinque id deve eseguirli in modo concorrente, impiegando ben meno di `0.1` secondi

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(fetch_all([1, 2, 3, 4, 5]))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [10, 20, 30, 40, 50], "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch(item_id):
    await asyncio.sleep(0.02)
    return item_id * 10

async def fetch_all(ids):
    return await asyncio.gather(*[fetch(i) for i in ids])
```
