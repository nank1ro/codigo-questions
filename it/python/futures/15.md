---
language: python
exerciseType: 4
---

# --description--

Gestire una coroutine che fallisce segue lo schema sincrono: la coroutine solleva, chi la chiama avvolge l'`await` in `try`/`except` e decide cosa fare dell'oggetto errore, ad esempio stamparne il messaggio con `print(e)`.

# --instructions--

Ordina gli elementi per costruire un programma che inizia con l'import, definisce `load` prima di `main`, stampa il risultato del caricamento di `"config"`, poi cattura l'errore sollevato dal caricamento di un nome vuoto e ne stampa il messaggio, producendo l'output `loaded config`, `empty name`

# --answers--

```python
import asyncio
```

```python
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
```

```python
async def main():
    print(await load("config"))
```

```python
    try:
        await load("")
```

```python
    except ValueError as e:
        print(e)
```

```python
asyncio.run(main())
```

# --solutions--

```python
import asyncio
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
async def main():
    print(await load("config"))
    try:
        await load("")
    except ValueError as e:
        print(e)
asyncio.run(main())
```

# --output--

loaded config
empty name
