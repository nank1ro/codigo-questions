---
language: python
exerciseType: 1
---

# --description--

Una coroutine può ricevere parametri e restituire un valore con `return` esattamente come una funzione normale. Il valore non è disponibile quando l'oggetto coroutine viene creato, ma solo una volta che la coroutine è stata eseguita. `asyncio.run` restituisce ciò che la coroutine ha restituito:
```python
import asyncio

async def double(n):
    return n * 2

result = asyncio.run(double(21))
print(result)  # 42
```
Tutto ciò che sai sulle funzioni vale anche dentro `async def`: variabili locali, condizioni, cicli e più istruzioni `return`.

# --instructions--

Scrivi una funzione coroutine `add(a, b)` con `async def` che restituisce la somma di `a` e `b`

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`add` deve essere una funzione coroutine

```python
    def test_1(self):
        self.assertTrue(inspect.iscoroutinefunction(add), "--err-t1--")
```

Eseguire `add(1, 3)` deve restituire `4`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(add(1, 3)), 4, "--err-t2--")
```

Eseguire `add(200, 210)` deve restituire `410`

```python
    def test_3(self):
        self.assertEqual(asyncio.run(add(200, 210)), 410, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
async def add(a, b):
    return a + b
```
