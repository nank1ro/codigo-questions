---
language: python
exerciseType: 3
---

# --description--

Quando una delle coroutine passate a `gather` solleva un'eccezione, l'eccezione si propaga alla riga `await asyncio.gather(...)` e i risultati delle altre vanno perduti, sebbene le coroutine continuino a essere eseguite. Passare `return_exceptions=True` cambia le cose: `gather` non solleva mai, e l'oggetto eccezione prende il posto del risultato mancante nella lista:
```python
import asyncio

async def ok():
    return 1

async def fail():
    raise ValueError("bad")

async def main():
    results = await asyncio.gather(ok(), fail(), return_exceptions=True)
    print(results)  # [1, ValueError('bad')]

asyncio.run(main())
```
Ogni elemento può quindi essere controllato con `isinstance(result, Exception)` per separare i fallimenti dai valori.

# --instructions--

Cosa stampa questo codice?
```python
import asyncio

async def ok():
    return 1

async def fail():
    raise ValueError("bad")

async def main():
    results = await asyncio.gather(ok(), fail(), return_exceptions=True)
    print(len(results), isinstance(results[1], ValueError))

asyncio.run(main())
```

# --answers--

- `2 True`, perché con `return_exceptions=True` l'eccezione viene restituita nella lista invece di essere sollevata
- Un `ValueError`, perché un'eccezione in una qualsiasi coroutine si propaga sempre fuori da `gather`
- `1 False`, perché la coroutine fallita viene scartata dai risultati
- `2 False`, perché `gather` sostituisce i risultati falliti con `None`

# --solutions--

- `2 True`, perché con `return_exceptions=True` l'eccezione viene restituita nella lista invece di essere sollevata
