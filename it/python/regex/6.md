---
language: python
exerciseType: 1
---

# --description--

Contare le parentesi per trovare il gruppo `3` stanca in fretta. A un gruppo si può dare un nome con `(?P<name>...)` e poi leggerlo con `match.group("name")`:
```python
import re

match = re.search(r"(?P<hour>\d\d):(?P<minute>\d\d)", "starts at 09:30")
print(match.group("hour"))    # 09
print(match.group("minute"))  # 30
print(match.groupdict())      # {'hour': '09', 'minute': '30'}
```
`match.groupdict()` restituisce tutti i gruppi con nome in un dizionario. I gruppi con nome mantengono anche il loro numero, quindi `match.group(1)` continua a funzionare.

L'esempio usa anche un **quantificatore** con le graffe: `\d{2}` significa esattamente due cifre, `\d{2,4}` significa da due a quattro, e `\d{2,}` significa due o più. Sono la versione precisa di `+` (una o più), `*` (zero o più) e `?` (zero o una).

# --instructions--

Scrivi una funzione `parse_date(text)` che trova una data scritta come quattro cifre, un trattino, due cifre, un trattino e due cifre, e restituisce un dizionario con le chiavi `year`, `month` e `day`, oppure `None` quando non c'è una data di questo tipo

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`parse_date` deve leggere le tre parti della data

```python
    def test_1(self):
        self.assertEqual(parse_date("due 2026-09-13 ok"), {"year": "2026", "month": "09", "day": "13"}, "--err-t1--")
```

`parse_date` deve funzionare quando la data è tutto il testo

```python
    def test_2(self):
        self.assertEqual(parse_date("1999-12-31"), {"year": "1999", "month": "12", "day": "31"}, "--err-t2--")
```

`parse_date` deve restituire `None` quando non c'è nessuna data

```python
    def test_3(self):
        self.assertIsNone(parse_date("no date here"), "--err-t3--")
        self.assertIsNone(parse_date("26-09-13"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def parse_date(text):
    match = re.search(r"(?P<year>\d{4})-(?P<month>\d{2})-(?P<day>\d{2})", text)
    if match is None:
        return None
    return match.groupdict()
```
