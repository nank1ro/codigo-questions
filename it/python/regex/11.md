---
language: python
exerciseType: 1
---

# --description--

`re.sub(pattern, replacement, text)` restituisce una nuova stringa in cui ogni corrispondenza è stata sostituita. Le stringhe sono immutabili, quindi il testo originale resta intatto:
```python
import re

print(re.sub(r"\d", "#", "call 555 now"))     # call ### now
print(re.sub(r"\s+", " ", "too    many gaps"))  # too many gaps
```

La sostituzione può riferirsi ai gruppi di cattura con `\1`, `\2`, ... (o `\g<name>` per un gruppo con nome), il che rende il riordino del testo una riga di codice sola. Anche la sostituzione è una raw string, per lo stesso motivo del backslash:
```python
import re

print(re.sub(r"(\w+), (\w+)", r"\2 \1", "Lovelace, Ada"))  # Ada Lovelace
```
Un argomento `count` limita quante corrispondenze vengono sostituite: `re.sub(r"\d", "#", "1 2 3", count=1)` dà `# 2 3`.

# --instructions--

Scrivi una funzione `mask_digits(text)` che sostituisce ogni cifra con `#`, e una funzione `swap_names(text)` che trasforma una coppia `cognome, nome` in `nome cognome` usando un backreference

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`mask_digits` deve sostituire ogni cifra

```python
    def test_1(self):
        self.assertEqual(mask_digits("call 555 now"), "call ### now", "--err-t1--")
        self.assertEqual(mask_digits("no digits"), "no digits", "--err-t1--")
```

`swap_names` deve mettere il nome prima del cognome

```python
    def test_2(self):
        self.assertEqual(swap_names("Lovelace, Ada"), "Ada Lovelace", "--err-t2--")
        self.assertEqual(swap_names("Turing, Alan"), "Alan Turing", "--err-t2--")
```

`swap_names` deve lasciare invariato un testo senza virgola

```python
    def test_3(self):
        self.assertEqual(swap_names("Ada Lovelace"), "Ada Lovelace", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def mask_digits(text):
    return re.sub(r"\d", "#", text)

def swap_names(text):
    return re.sub(r"(\w+), (\w+)", r"\2 \1", text)
```
