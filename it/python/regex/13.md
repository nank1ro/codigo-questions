---
language: python
exerciseType: 1
---

# --description--

`str.split` può tagliare solo su un separatore fisso. `re.split(pattern, text)` taglia su qualsiasi cosa descriva il pattern, che è ciò che input disordinati di solito richiedono:
```python
import re

print("a, b;c".split(","))            # ['a', ' b;c']
print(re.split(r"[,;]", "a, b;c"))    # ['a', ' b', 'c']
print(re.split(r"[,;\s]+", "a, b;c")) # ['a', 'b', 'c']
```
Scrivere il separatore come `[,;\s]+` fa sì che un'intera sequenza di virgole, punti e virgola e spazi conti come un solo taglio, invece di lasciare stringhe vuote tra loro.

Un argomento `maxsplit` si ferma dopo un dato numero di tagli, lasciando il resto del testo nell'ultimo elemento: `re.split(r"\s+", "a b c", maxsplit=1)` dà `['a', 'b c']`.

# --instructions--

Scrivi una funzione `split_fields(line)` che restituisce la lista dei campi della riga, trattando qualsiasi sequenza di virgole, punti e virgola e spazi come un unico separatore

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`split_fields` deve tagliare su virgole, punti e virgola e spazi

```python
    def test_1(self):
        self.assertEqual(split_fields("a, b;c"), ["a", "b", "c"], "--err-t1--")
        self.assertEqual(split_fields("red green blue"), ["red", "green", "blue"], "--err-t1--")
```

Una sequenza di separatori deve produrre un solo taglio

```python
    def test_2(self):
        self.assertEqual(split_fields("x ; ; y"), ["x", "y"], "--err-t2--")
        self.assertEqual(split_fields("1,,2"), ["1", "2"], "--err-t2--")
```

Una riga senza separatori deve restituire un solo campo

```python
    def test_3(self):
        self.assertEqual(split_fields("solo"), ["solo"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def split_fields(line):
    return re.split(r"[,;\s]+", line)
```
