---
language: python
exerciseType: 1
---

# --description--

Quando servono la posizione o i gruppi di ogni corrispondenza, `re.finditer(pattern, text)` è la chiamata giusta: percorre il testo e produce un **oggetto match** per ogni corrispondenza, una alla volta:
```python
import re

for match in re.finditer(r"\d+", "a1 bb22"):
    print(match.group(), match.start())
# 1 1
# 22 5
```
`finditer` produce un iteratore, non una lista, quindi può essere usato in un ciclo `for` o in una comprehension. Dove `findall` dà soltanto il testo, `finditer` dà tutto ciò che un oggetto match sa.

# --instructions--

Scrivi una funzione `word_positions(text)` che restituisce una lista di tuple, una per ogni sequenza di lettere nel testo, contenenti la parola e l'indice in cui inizia

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`word_positions("hi there")` deve restituire `[("hi", 0), ("there", 3)]`

```python
    def test_1(self):
        self.assertEqual(word_positions("hi there"), [("hi", 0), ("there", 3)], "--err-t1--")
```

Cifre e simboli non devono far parte di una parola

```python
    def test_2(self):
        self.assertEqual(word_positions("a1b"), [("a", 0), ("b", 2)], "--err-t2--")
        self.assertEqual(word_positions("Ada, Bob"), [("Ada", 0), ("Bob", 5)], "--err-t2--")
```

Un testo senza lettere deve restituire una lista vuota

```python
    def test_3(self):
        self.assertEqual(word_positions("123 456"), [], "--err-t3--")
        self.assertEqual(word_positions(""), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def word_positions(text):
    return [(match.group(), match.start()) for match in re.finditer(r"[a-zA-Z]+", text)]
```
