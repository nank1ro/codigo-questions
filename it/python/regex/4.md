---
language: python
exerciseType: 1
---

# --description--

Un oggetto match trasporta più del testo trovato. Oltre a `.group()` offre la posizione della corrispondenza all'interno della stringa originale:
```python
import re

match = re.search(r"\d+", "order 42 shipped")
print(match.group())  # 42
print(match.start())  # 6
print(match.end())    # 8
print(match.span())   # (6, 8)
```
`.start()` è l'indice del primo carattere trovato, `.end()` è l'indice subito dopo l'ultimo, e `.span()` li restituisce entrambi come tupla. Ciò significa che `text[match.start():match.end()]` è sempre uguale a `match.group()`.

Poiché `re.search` può restituire `None`, leggere `.group()` subito dopo solleva un `AttributeError` quando non c'è stata corrispondenza; controlla prima il risultato.

# --instructions--

Scrivi una funzione `first_number(text)` che restituisce una tupla con la prima sequenza di cifre, il suo indice di inizio e il suo indice di fine, oppure `None` quando il testo non contiene cifre

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_number("order 42 shipped")` deve restituire `("42", 6, 8)`

```python
    def test_1(self):
        self.assertEqual(first_number("order 42 shipped"), ("42", 6, 8), "--err-t1--")
```

La corrispondenza deve iniziare all'indice `0` quando il testo inizia con cifre

```python
    def test_2(self):
        self.assertEqual(first_number("7up"), ("7", 0, 1), "--err-t2--")
        self.assertEqual(first_number("100 items"), ("100", 0, 3), "--err-t2--")
```

`first_number` deve restituire `None` quando non c'è nessuna cifra

```python
    def test_3(self):
        self.assertIsNone(first_number("no digits here"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def first_number(text):
    match = re.search(r"\d+", text)
    if match is None:
        return None
    return (match.group(), match.start(), match.end())
```
