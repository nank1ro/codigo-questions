---
language: python
exerciseType: 1
---

# --description--

Un parametro può avere un **valore predefinito**, usato quando chi chiama omette l'argomento. `None` è il valore predefinito usuale per "non fornito":
```python
def greet(name=None):
    if name is None:
        name = "stranger"
    print("Hello, " + name)

greet()       # Hello, stranger
greet("Ada")  # Hello, Ada
```
Questo conta per liste e dizionari. Un valore predefinito viene valutato **una sola volta**, quando la funzione è definita, quindi `def add(item, items=[])` condivide la stessa lista in ogni chiamata che omette `items`, e gli elementi si accumulano. La soluzione è usare `None` come predefinito e creare una lista nuova all'interno della funzione:
```python
def add(item, items=None):
    if items is None:
        items = []
    items.append(item)
    return items
```

# --instructions--

Scrivi una funzione `add_tag` che prenda una stringa `tag` e una lista opzionale `tags`; deve aggiungere `tag` a `tags` e restituire la lista, creando una nuova lista quando `tags` non è fornita

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Il valore predefinito di `tags` deve essere `None`

```json
{
  "regex": "tags\\s*=\\s*None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`add_tag("new")` deve restituire `["new"]`

```python
    def test_1(self):
        self.assertEqual(add_tag("new"), ["new"], "--err-t1--")
```

Due chiamate senza `tags` devono restituire liste indipendenti

```python
    def test_2(self):
        first = add_tag("a")
        second = add_tag("b")
        self.assertEqual(first, ["a"], "--err-t2--")
        self.assertEqual(second, ["b"], "--err-t2--")
```

`add_tag("c", ["a", "b"])` deve restituire `["a", "b", "c"]`

```python
    def test_3(self):
        existing = ["a", "b"]
        self.assertEqual(add_tag("c", existing), ["a", "b", "c"], "--err-t3--")
        self.assertIs(add_tag("d", existing), existing, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def add_tag(tag, tags=None):
    if tags is None:
        tags = []
    tags.append(tag)
    return tags
```
