---
language: python
exerciseType: 1
---

# --description--

Quando `0` o `""` devono essere conservati e solo `None` sostituito, il fallback richiede un controllo esplicito `is None`. La forma compatta è l'**espressione condizionale**, `a if condition else b`, che vale `a` quando la condizione è vera e `b` altrimenti:
```python
timeout = 0
seconds = timeout if timeout is not None else 30
print(seconds)  # 0, not 30
```

# --instructions--

Scrivi una funzione `port_or_default` che prenda `port` e lo restituisca quando non è `None`, e `8080` altrimenti; il valore `0` deve essere restituito tale e quale

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi controllare con `is not None` o `is None`

```json
{
  "regex": "is\\s+(not\\s+)?None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`port_or_default(3000)` deve restituire `3000`

```python
    def test_1(self):
        self.assertEqual(port_or_default(3000), 3000, "--err-t1--")
```

`port_or_default(None)` deve restituire `8080`

```python
    def test_2(self):
        self.assertEqual(port_or_default(None), 8080, "--err-t2--")
```

`port_or_default(0)` deve restituire `0`

```python
    def test_3(self):
        self.assertEqual(port_or_default(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def port_or_default(port):
    return port if port is not None else 8080
```
