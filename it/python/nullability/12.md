---
language: python
exerciseType: 1
---

# --description--

Una ricerca seguita da un controllo su `None` di solito richiede due righe: una per salvare il risultato, una per testarlo. L'operatore di **espressione di assegnazione** `:=`, soprannominato *walrus*, assegna un valore **dentro** un'espressione, così entrambi i passi stanno nell'`if`:
```python
ages = {"Ada": 36}
if (age := ages.get("Ada")) is not None:
    print(age + 1)  # 37
```
Le parentesi sono obbligatorie: senza di esse, `:=` tenterebbe di assegnare l'intero confronto. Dopo l'`if`, `age` resta disponibile come qualsiasi altra variabile.

# --instructions--

Scrivi una funzione `describe_user` che prenda un dizionario `users` che mappa nomi a età e un `name`; deve restituire `"<name> is <age>"` quando il nome è presente, e `"unknown user"` altrimenti, usando l'operatore walrus per salvare il risultato di `get`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare l'operatore `:=`

```json
{
  "regex": ":=",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Devi usare `get`

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe_user({"Ada": 36}, "Ada")` deve restituire `"Ada is 36"`

```python
    def test_1(self):
        self.assertEqual(describe_user({"Ada": 36}, "Ada"), "Ada is 36", "--err-t1--")
```

`describe_user({"Ada": 36}, "Grace")` deve restituire `"unknown user"`

```python
    def test_2(self):
        self.assertEqual(describe_user({"Ada": 36}, "Grace"), "unknown user", "--err-t2--")
```

Un'età di `0` deve comunque contare come presente

```python
    def test_3(self):
        self.assertEqual(describe_user({"Tim": 0}, "Tim"), "Tim is 0", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe_user(users, name):
    if (age := users.get(name)) is not None:
        return f"{name} is {age}"
    return "unknown user"
```
