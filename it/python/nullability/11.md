---
language: python
exerciseType: 2
---

# --description--

Una lista può contenere `None` accanto a valori reali, per esempio letture fallite o risposte saltate. La maggior parte delle operazioni non lo accetta: `sum([8, None])` solleva un `TypeError`.
Elimina i valori `None` con una list comprehension la cui condizione è `is not None`:
```python
readings = [3, None, 5]
valid = [r for r in readings if r is not None]
print(valid)  # [3, 5]
```
Usare `if r` al suo posto eliminerebbe anche ogni `0`, quindi sii esplicito quando zero è una lettura valida.

# --instructions--

Completa il codice in modo che `valid` contenga solo i punteggi che non sono `None`, poi stampi il totale, producendo l'output `[8, 0, 9]`, `3`, `17`

# --seed--

```python
scores = [8, None, 0, None, 9]
valid = [s for s in scores if s [/] None]
print(valid)
print(len(valid))
print([/](valid))
```

# --answers--

- is not
- sum
- is
- max
- len
- and

# --solutions--

```python
scores = [8, None, 0, None, 9]
valid = [s for s in scores if s is not None]
print(valid)
print(len(valid))
print(sum(valid))
```

# --output--

[8, 0, 9]
3
17
