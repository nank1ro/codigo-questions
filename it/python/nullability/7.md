---
language: python
exerciseType: 1
---

# --description--

Una funzione che restituisce un numero **o** `None` dovrebbe dirlo nella sua firma. Un **type hint** è un'annotazione che documenta il tipo atteso: `name: str` per un parametro e `-> int` per il valore restituito. Python non impone i type hint, ma editor e lettori vi si affidano:
```python
def parse_age(text: str) -> int | None:
    if text.isdigit():
        return int(text)
    return None
```
`int | None` si legge "un `int` oppure `None`". La grafia più vecchia `Optional[int]` del modulo `typing` significa esattamente la stessa cosa, e la incontrerai ancora nel codice esistente.
Ogni volta che vedi `| None` in una firma, ricorda di controllare il risultato prima di usarlo.

# --instructions--

Scrivi una funzione `parse_age` con un type hint che prenda una stringa e la restituisca convertita in un `int` quando contiene solo cifre, e `None` altrimenti

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Il tipo di ritorno deve essere annotato come `int | None`

```json
{
  "regex": "->\\s*(int\\s*\\|\\s*None|None\\s*\\|\\s*int|Optional\\[int\\])",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_age("42")` deve restituire `42`

```python
    def test_1(self):
        self.assertEqual(parse_age("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_age("42"), int, "--err-t1--")
```

`parse_age("abc")` deve restituire `None`

```python
    def test_2(self):
        self.assertIsNone(parse_age("abc"), "--err-t2--")
        self.assertIsNone(parse_age("4a"), "--err-t2--")
```

`parse_age("")` deve restituire `None`

```python
    def test_3(self):
        self.assertIsNone(parse_age(""), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_age(text: str) -> int | None:
    if text.isdigit():
        return int(text)
    return None
```
