---
language: python
exerciseType: 1
---

# --description--

Ogni chiamata di funzione produce un valore, anche quando la funzione non sembra restituire nulla. Una funzione **senza** istruzione `return`, o con un `return` nudo, restituisce `None`:
```python
def say_hello():
    print("hello")

result = say_hello()
print(result)  # None
```
Per questo chiamare `print(my_list.append(3))` mostra `None`: `append` modifica la lista sul posto e non restituisce nulla.
Una funzione che esegue solo un'azione (stampare, salvare, modificare una lista) di solito restituisce `None`, mentre una funzione che calcola qualcosa deve restituirla esplicitamente con `return`.

# --instructions--

Scrivi una funzione `store` che prenda un valore e lo aggiunga alla lista `saved`, senza restituire nulla

# --seed--

```python
saved = []

def store(value):
    pass
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Devi usare `append`

```json
{
  "regex": "\\.append\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`store("a")` deve restituire `None`

```python
    def test_1(self):
        self.assertIsNone(store("a"), "--err-t1--")
```

Dopo `store("b")` la lista `saved` deve terminare con `"b"`

```python
    def test_2(self):
        store("b")
        self.assertEqual(saved[-1], "b", "--err-t2--")
```

Ogni valore salvato deve essere conservato, nell'ordine

```python
    def test_3(self):
        before = len(saved)
        store(1)
        store(2)
        self.assertEqual(saved[before:], [1, 2], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
saved = []

def store(value):
    saved.append(value)
```
