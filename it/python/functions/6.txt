<section id="options">
{
"language": "python",
"type": "answerButtons"
}
</section>
<section id="instructions">
Qual e' il __problema__ di questo frammento di codice?
```
saluta('Mathew')

def saluta(nome):
    """Questa funzione saluta la persona che riceve come parametro"""
    print(f"Ciao, {nome}. Buongiorno!")

saluta('Alex')
```
</section>
<section id="answerButtons">
{
	"buttons": ["Non possiamo usare la funzione saluta() prima di averla definita","Non possiamo usare la funzione saluta() due volte","saluta() non restituisce alcun valore","Non possiamo usare nome come parametro"],
	"correct": "Non possiamo usare la funzione saluta() prima di averla definita"
}
</section>
