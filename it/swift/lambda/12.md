---
language: swift
exerciseType: 1
---

# --description--

Restituire una closure è un modo pratico per costruire funzioni personalizzate. I parametri della funzione esterna vengono catturati dalla closure che restituisce:
```swift
func makeAdder(_ amount: Int) -> (Int) -> Int {
    return { $0 + amount }
}
let addFive = makeAdder(5)
print(addFive(10)) // 15
```
Il tipo di ritorno `(Int) -> Int` descrive la closure, e la forma abbreviata `$0` si riferisce all'argomento di quella closure, non a quello di `makeAdder`.

# --instructions--

Scrivi una funzione `makeMultiplier` che accetta un `Int` chiamato `factor` (ometti la label dell'argomento) e restituisce una closure di tipo `(Int) -> Int` che moltiplica il suo argomento per `factor`

Esempio di chiamata alla funzione:
```swift
let double = makeMultiplier(2)
print(double(10))
// prints 20
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`makeMultiplier(2)(10)` deve restituire `20`

```swift
tryCatch(makeMultiplier(2)(10) == 20)
```

`makeMultiplier(3)(4)` deve restituire `12`

```swift
tryCatch(makeMultiplier(3)(4) == 12)
```

`makeMultiplier(0)(99)` deve restituire `0`

```swift
tryCatch(makeMultiplier(0)(99) == 0)
```

Una closure restituita da `makeMultiplier(5)` deve continuare a funzionare tra le chiamate

```swift
let _timesFive = makeMultiplier(5)
tryCatch(_timesFive(1) == 5 && _timesFive(6) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func makeMultiplier(_ factor: Int) -> (Int) -> Int {
    return { $0 * factor }
}
```
