---
language: swift
exerciseType: 1
---

# --description--

`filter` mantiene solo gli elementi per cui la closure restituisce `true`. La closure riceve un elemento e deve restituire un `Bool`:
```swift
let nums = [5, 12, 8, 20]
let big = nums.filter { $0 > 10 }
print(big) // [12, 20]
```
Gli elementi mantengono il loro ordine originale, e il risultato è un nuovo array dello stesso tipo di elemento.

# --instructions--

Scrivi una funzione `evens` che accetta un array di `Int` (ometti la label dell'argomento) e restituisce un nuovo array con solo i numeri pari, usando `filter`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Devi usare `filter`

```json
{
  "regex": "\\.filter",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`evens([1, 2, 3, 4, 5, 6])` deve restituire `[2, 4, 6]`

```swift
tryCatch(evens([1, 2, 3, 4, 5, 6]) == [2, 4, 6])
```

`evens([7, 9])` deve restituire `[]`

```swift
tryCatch(evens([7, 9]) == [])
```

`evens([0, -2, 3])` deve restituire `[0, -2]`

```swift
tryCatch(evens([0, -2, 3]) == [0, -2])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func evens(_ nums: [Int]) -> [Int] {
    return nums.filter { $0 % 2 == 0 }
}
```
