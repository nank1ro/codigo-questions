---
language: swift
exerciseType: 4
---

# --description--

Una closure memorizzata in una costante può essere passata ovunque sia previsto un argomento di tipo closure, usando la label dell'argomento del parametro:
```swift
let ascending = { (a: Int, b: Int) -> Bool in a < b }
print([3, 1, 2].sorted(by: ascending)) // [1, 2, 3]
```

# --instructions--

Ordina gli elementi per memorizzare in `byLength` una closure che ordini due stringhe dalla più corta alla più lunga, poi stampa `words` ordinato con essa.
Dichiara `words` prima di `byLength`

# --answers--

```swift
let words = ["kiwi", "fig", "banana"]
```

```swift
let byLength = { (a: String, b: String) -> Bool in
```

```swift
    a.count < b.count
```

```swift
}
```

```swift
print(words.sorted(by: byLength))
```

# --solutions--

```swift
let words = ["kiwi", "fig", "banana"]
let byLength = { (a: String, b: String) -> Bool in
    a.count < b.count
}
print(words.sorted(by: byLength))
```

# --output--

["fig", "kiwi", "banana"]
