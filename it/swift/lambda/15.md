---
language: swift
exerciseType: 1
---

# --description--

Per impostazione predefinita, una closure passata a una funzione deve essere usata solo mentre quella funzione è in esecuzione. Se la funzione memorizza la closure o restituisce un'altra closure che la usa, la closure **sfugge** dalla funzione, e il suo parametro deve essere contrassegnato con `@escaping`:
```swift
func twice(_ task: @escaping () -> Int) -> () -> Int {
    return { task() * 2 }
}
let answer = twice { 21 }
print(answer()) // 42
```
Senza `@escaping` il compilatore segnala un errore, perché la closure restituita userebbe `task` dopo che `twice` è terminata.

# --instructions--

Scrivi una funzione `compose` che accetta due closure di tipo `(Int) -> Int` (ometti entrambe le label degli argomenti) e restituisce una closure che applica prima la prima e poi la seconda al suo argomento

Esempio di chiamata alla funzione:
```swift
let addThenDouble = compose({ $0 + 1 }, { $0 * 2 })
print(addThenDouble(3))
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`compose({ $0 + 1 }, { $0 * 2 })(3)` deve restituire `8`

```swift
tryCatch(compose({ $0 + 1 }, { $0 * 2 })(3) == 8)
```

`compose({ $0 * 2 }, { $0 + 1 })(3)` deve restituire `7`

```swift
tryCatch(compose({ $0 * 2 }, { $0 + 1 })(3) == 7)
```

`compose({ $0 }, { $0 })(9)` deve restituire `9`

```swift
tryCatch(compose({ $0 }, { $0 })(9) == 9)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func compose(_ first: @escaping (Int) -> Int, _ second: @escaping (Int) -> Int) -> (Int) -> Int {
    return { second(first($0)) }
}
```
