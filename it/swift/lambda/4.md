---
language: swift
exerciseType: 2
---

# --description--

Poiché le closure sono valori, una funzione può accettarne una come parametro. Il tipo del parametro è semplicemente il tipo di funzione:
```swift
func apply(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(n)
}
print(apply(5, { $0 + 1 })) // 6
```
La funzione `apply` non sa cosa fa `operation`, sa solo che accetta un `Int` e restituisce un `Int`, e la richiama come qualsiasi altra funzione.

# --instructions--

Completa la funzione `apply` in modo che il suo secondo parametro sia una closure che accetta un `Int` e restituisce un `Int`, e che la funzione restituisca il risultato della chiamata di quella closure su `n`, così che il programma stampi `12`

# --seed--

```swift
func apply(_ n: Int, _ operation: [/]) -> Int {
    return [/]
}
print(apply(6, { $0 * 2 }))
```

# --answers--

- (Int) -> Int
- operation(n)
- Int
- (Int) -> Void
- operation
- n

# --solutions--

```swift
func apply(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(n)
}
print(apply(6, { $0 * 2 }))
```

# --output--

12
