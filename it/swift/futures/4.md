---
language: swift
exerciseType: 1
---

# --description--

Il lavoro che si svolge nel tempo spesso fallisce: un server è giù, un file manca, l'input è sbagliato. Una funzione del genere è marcata **`async throws`** e si chiama con **`try await`**:
```swift
enum LoadError: Error {
    case missing
}

func load(_ name: String) async throws -> String {
    if name.isEmpty {
        throw LoadError.missing
    }
    return "file: \(name)"
}
```
Per gestire l'errore racchiudi la chiamata in un blocco `do` e la intercetti:
```swift
do {
    let text = try await load("")
    print(text)
} catch {
    print("could not load")
}
// prints could not load
```
L'ordine delle parole chiave è fisso: prima viene `try`, poi `await`.

# --instructions--

Scrivi una funzione `parse` che accetta una stringa senza label dell'argomento, è asincrona, può lanciare errori e restituisce la stringa convertita in un `Int`. Se la stringa non è un numero, lancia `InputError.notANumber`

Esempio di chiamata alla funzione:
```swift
print(try await parse("7"))
// prints 7
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum InputError: Error {
    case notANumber
}

func parse() {

}
```

# --asserts--

La funzione deve essere marcata `async throws`

```json
{
  "regex": "func\\s+parse\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse("7")` deve restituire `7`

```swift
do {
    let value = try await parse("7")
    tryCatch(value == 7)
} catch {
    tryCatch(false)
}
```

`parse("-3")` deve restituire `-3`

```swift
do {
    let value = try await parse("-3")
    tryCatch(value == -3)
} catch {
    tryCatch(false)
}
```

`parse("abc")` deve lanciare un errore

```swift
do {
    _ = try await parse("abc")
    tryCatch(false)
} catch {
    tryCatch(true)
}
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}
```
