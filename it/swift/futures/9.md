---
language: swift
exerciseType: 1
---

# --description--

`async let` è legato allo scope in cui è scritto. Per avviare un lavoro concorrente e tenerne il controllo, usa un **`Task`**. La closure passata a `Task { }` gira per conto suo e il task può essere memorizzato, passato in giro o restituito:
```swift
let job = Task {
    return await double(21)
}
```
Il risultato si legge più tardi con **`.value`**, che viene atteso:
```swift
print(await job.value)
// prints 42
```
Il tipo dell'handle dice cosa produce e cosa può lanciare: `Task<Int, Never>` è un task che restituisce un `Int` e non lancia mai errori. A differenza di `async let`, un `Task` può essere creato anche da codice ordinario, non asincrono.

# --instructions--

Scrivi una funzione `startDoubling` che accetta un intero senza label dell'argomento e restituisce un `Task<Int, Never>` che produce il doppio di quel numero

Esempio di chiamata alla funzione:
```swift
print(await startDoubling(4).value)
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func startDoubling() {

}
```

# --asserts--

La funzione deve creare un `Task`

```json
{
  "regex": "Task\\s*\\{",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`startDoubling(4).value` deve essere `8`

```swift
tryCatch(await startDoubling(4).value == 8)
```

`startDoubling(0).value` deve essere `0`

```swift
tryCatch(await startDoubling(0).value == 0)
```

`startDoubling(-6).value` deve essere `-12`

```swift
tryCatch(await startDoubling(-6).value == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func startDoubling(_ n: Int) -> Task<Int, Never> {
    return Task {
        return n * 2
    }
}
```
