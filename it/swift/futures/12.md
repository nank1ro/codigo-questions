---
language: swift
exerciseType: 1
---

# --description--

`async let` funziona quando sai quante chiamate ci siano mentre scrivi il codice. Per una lista la cui dimensione è nota solo a run time, usa un **task group**.

`withTaskGroup(of:)` apre un gruppo, `addTask` avvia un task figlio per ogni elemento e il gruppo poi si legge con `for await`, che consegna i risultati man mano che finiscono:
```swift
let total = await withTaskGroup(of: Int.self) { group in
    for n in numbers {
        group.addTask {
            return await square(n)
        }
    }
    var sum = 0
    for await value in group {
        sum += value
    }
    return sum
}
```
`of: Int.self` dichiara cosa restituisce ogni task figlio. L'intera chiamata `withTaskGroup` è una sola espressione, quindi richiede un singolo `await` davanti e non restituisce nulla finché ogni task figlio non è finito.

# --instructions--

Scrivi una funzione `sumOfSquares` che accetta un array di interi senza label dell'argomento, è asincrona e restituisce la somma di `square` applicato a ogni elemento, calcolandoli con `withTaskGroup`

Esempio di chiamata alla funzione:
```swift
print(await sumOfSquares([1, 2, 3]))
// stampa 14
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func square(_ n: Int) async -> Int {
    return n * n
}

func sumOfSquares() {

}
```

# --asserts--

La funzione deve usare `withTaskGroup`

```json
{
  "regex": "withTaskGroup",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`sumOfSquares([1, 2, 3])` deve restituire `14`

```swift
tryCatch(await sumOfSquares([1, 2, 3]) == 14)
```

`sumOfSquares([4])` deve restituire `16`

```swift
tryCatch(await sumOfSquares([4]) == 16)
```

`sumOfSquares([])` deve restituire `0`

```swift
tryCatch(await sumOfSquares([]) == 0)
```

`sumOfSquares([-2, 5])` deve restituire `29`

```swift
tryCatch(await sumOfSquares([-2, 5]) == 29)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func square(_ n: Int) async -> Int {
    return n * n
}

func sumOfSquares(_ numbers: [Int]) async -> Int {
    return await withTaskGroup(of: Int.self) { group in
        for n in numbers {
            group.addTask {
                return await square(n)
            }
        }
        var total = 0
        for await value in group {
            total += value
        }
        return total
    }
}
```
