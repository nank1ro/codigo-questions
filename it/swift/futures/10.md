---
language: swift
exerciseType: 1
---

# --description--

`Task.sleep` mette in pausa il task corrente per un po' senza bloccare nient'altro. Può essere interrotto, quindi è una chiamata asincrona che può lanciare errori e richiede `try await`. La durata si indica con helper come `.seconds`, `.milliseconds` o `.nanoseconds`:
```swift
func slowGreeting() async throws -> String {
    try await Task.sleep(for: .milliseconds(50))
    return "hello"
}
```
Questo è il modo standard per simulare un lavoro lento in un esempio, al posto di una vera chiamata di rete. Nota che non blocca il programma: mentre un task dorme, gli altri continuano a girare.

# --instructions--

Scrivi una funzione `delayed` che accetta un intero senza label dell'argomento, è asincrona, può lanciare errori, attende `50` millisecondi con `Task.sleep` e poi restituisce il numero moltiplicato per `3`

Esempio di chiamata alla funzione:
```swift
print(try await delayed(2))
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func delayed() {

}
```

# --asserts--

La funzione deve essere marcata `async throws`

```json
{
  "regex": "func\\s+delayed\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

La funzione deve usare `Task.sleep`

```json
{
  "regex": "Task\\.sleep",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`delayed(2)` deve restituire `6`

```swift
tryCatch((try? await delayed(2)) == 6)
```

`delayed(0)` deve restituire `0`

```swift
tryCatch((try? await delayed(0)) == 0)
```

`delayed(-4)` deve restituire `-12`

```swift
tryCatch((try? await delayed(-4)) == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func delayed(_ n: Int) async throws -> Int {
    try await Task.sleep(for: .milliseconds(50))
    return n * 3
}
```
