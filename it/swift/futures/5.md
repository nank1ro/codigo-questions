---
language: swift
exerciseType: 2
---

# --description--

Quando non ti interessa *perché* la chiamata è fallita, `try?` è più corto di un blocco `do`. Trasforma una chiamata che può lanciare errori in un **opzionale**: il valore in caso di successo, `nil` in caso di fallimento. Combinato con `await` si scrive `try? await`:
```swift
let value = try? await parse("42")  // Optional(42)
let broken = try? await parse("x")  // nil
```
Poiché il risultato è opzionale, entra direttamente in un `if let`:
```swift
if let value = try? await parse("x") {
    print(value)
} else {
    print("not a number")
}
```
Usa `try? await` per un ripiego veloce, e `do` / `catch` quando l'errore in sé è importante.

# --instructions--

Completa il codice in modo che la chiamata fallita produca `nil` e il programma stampi `not a number`

# --seed--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = [/] await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --answers--

- try?
- try
- try!
- await
- throws

# --solutions--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = try? await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --output--

not a number
