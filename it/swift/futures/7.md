---
language: swift
exerciseType: 1
---

# --description--

Per eseguire due chiamate indipendenti allo stesso tempo, dichiarale con **`async let`**. Il lavoro inizia subito e il programma continua senza aspettare:
```swift
async let left = step("A")
async let right = step("B")
```
Il valore non c'è ancora, quindi non puoi usare il binding direttamente: devi fare `await` nel punto in cui finalmente ti serve. Un solo `await` davanti all'espressione copre ogni `async let` al suo interno:
```swift
let both = await left + right
```
Se ogni chiamata impiega un secondo, la versione sequenziale richiede due secondi mentre la versione con `async let` ne richiede circa uno, perché le due chiamate si sovrappongono.

# --instructions--

Scrivi una funzione `total` che accetta due interi senza label degli argomenti, è asincrona e restituisce la somma di `slowValue` applicato a entrambi, avviando le due chiamate in concorrenza con `async let`

Esempio di chiamata alla funzione:
```swift
print(await total(1, 2))
// stampa 30
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total() {

}
```

# --asserts--

La funzione deve usare `async let`

```json
{
  "regex": "async\\s+let",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`total(1, 2)` deve restituire `30`

```swift
tryCatch(await total(1, 2) == 30)
```

`total(0, 0)` deve restituire `0`

```swift
tryCatch(await total(0, 0) == 0)
```

`total(5, -2)` deve restituire `30`

```swift
tryCatch(await total(5, -2) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total(_ a: Int, _ b: Int) async -> Int {
    async let first = slowValue(a)
    async let second = slowValue(b)
    return await first + second
}
```
