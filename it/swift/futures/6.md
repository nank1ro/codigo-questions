---
language: swift
exerciseType: 4
---

# --description--

Diverse chiamate con `await` scritte una dopo l'altra vengono eseguite in modo **sequenziale**: la seconda chiamata non inizia nemmeno finché la prima non ha restituito il risultato. Il codice si legge dall'alto verso il basso, esattamente come il codice ordinario:
```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}

let a = await step("A")
print(a)
let b = await step("B")
print(b)
// start A
// done A
// start B
// done B
```
È quello che vuoi quando la seconda chiamata ha bisogno del risultato della prima. Quando le chiamate sono indipendenti, aspettarne una prima di iniziare l'altra è tempo sprecato, e i prossimi esercizi mostrano come evitarlo.

# --instructions--

Ordina le righe per stampare l'output qui sotto, definendo `step` prima del codice al top level
```
start A
done A
start B
done B
```

# --answers--

```swift
func step(_ name: String) async -> String {
```

```swift
    print("start \(name)")
```

```swift
    return "done \(name)"
```

```swift
}
```

```swift
let a = await step("A")
```

```swift
print(a)
```

```swift
let b = await step("B")
```

```swift
print(b)
```

# --solutions--

```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}
let a = await step("A")
print(a)
let b = await step("B")
print(b)
```

# --output--

start A
done A
start B
done B
