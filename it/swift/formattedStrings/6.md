---
language: swift
exerciseType: 1
---

# --description--

Un altro flag è `0`: invece che con spazi, il campo viene riempito con zeri a sinistra. È così che ottieni numeri come `007` o `00042`:
```swift
print(String(format: "%05d", 42))  // 00042
print(String(format: "%03d", 7))   // 007
print(String(format: "%03d", 1234)) // 1234
```
Come con gli spazi, un valore più lungo della larghezza non viene mai tagliato.

# --instructions--

Scrivi una funzione `ticket` che prende un intero senza etichetta dell'argomento e restituisce `T-` seguito dal numero riempito con zeri fino alla larghezza `5`, usando `String(format:)`

Esempio di chiamata di funzione:
```swift
print(ticket(42))
// prints T-00042
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func ticket() {

}
```

# --asserts--

La funzione deve usare `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ticket(42)` deve restituire `"T-00042"`

```swift
tryCatch(ticket(42) == "T-00042")
```

`ticket(7)` deve restituire `"T-00007"`

```swift
tryCatch(ticket(7) == "T-00007")
```

`ticket(12345)` deve restituire `"T-12345"`

```swift
tryCatch(ticket(12345) == "T-12345")
```

`ticket(123456)` deve restituire `"T-123456"`

```swift
tryCatch(ticket(123456) == "T-123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func ticket(_ n: Int) -> String {
    return String(format: "T-%05d", n)
}
```
