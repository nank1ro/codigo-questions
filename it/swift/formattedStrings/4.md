---
language: swift
exerciseType: 1
---

# --description--

Un numero tra `%` e la lettera imposta la **larghezza minima** del campo. Se il valore è più corto, vengono aggiunti spazi a sinistra così che risulti **allineato a destra**; se è più lungo, non viene tagliato nulla:
```swift
print(String(format: "%5d|", 42))    //    42|
print(String(format: "%5d|", 12345)) // 12345|
```
Larghezza e precisione si combinano: `%8.2f` significa "largo almeno 8 caratteri, con 2 decimali":
```swift
print(String(format: "%8.2f|", 3.14159)) //     3.14|
```
Le larghezze fisse sono ciò che allinea le colonne di una tabella.

# --instructions--

Scrivi una funzione `rightAligned` che prende un intero senza etichetta dell'argomento e lo restituisce come stringa allineata a destra in un campo di larghezza `5`, usando `String(format:)`

Esempio di chiamata di funzione:
```swift
print(rightAligned(42) + "|")
// prints    42|
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func rightAligned() {

}
```

# --asserts--

La funzione deve usare `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`rightAligned(42)` deve restituire `"   42"`

```swift
tryCatch(rightAligned(42) == "   42")
```

`rightAligned(7)` deve restituire `"    7"`

```swift
tryCatch(rightAligned(7) == "    7")
```

`rightAligned(12345)` deve restituire `"12345"`

```swift
tryCatch(rightAligned(12345) == "12345")
```

`rightAligned(123456)` deve restituire `"123456"` (non viene tagliato nulla)

```swift
tryCatch(rightAligned(123456) == "123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func rightAligned(_ n: Int) -> String {
    return String(format: "%5d", n)
}
```
