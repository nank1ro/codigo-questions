---
language: swift
exerciseType: 1
---

# --description--

Il secondo strumento è `String(format:)`, che proviene dal framework **Foundation**, quindi il file deve iniziare con `import Foundation`.

Prende una **format string** seguita dai valori da inserire. Dentro la format string, uno **specificatore** che inizia con `%` indica dove va ogni valore e come viene scritto. Lo specificatore per un intero è `%d`:
```swift
import Foundation

let count = 7
let text = String(format: "Item %d", count)
print(text) // Item 7
```
`String(format:)` restituisce una normale `String`, quindi puoi stamparla, memorizzarla o restituirla da una funzione.

# --instructions--

Scrivi una funzione `label` che prende un intero senza etichetta dell'argomento e restituisce il testo `Item ` seguito dal numero, usando `String(format:)` con `%d`

Esempio di chiamata di funzione:
```swift
print(label(7))
// prints Item 7
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func label() {

}
```

# --asserts--

La funzione deve usare `String(format:`

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`label(7)` deve restituire `"Item 7"`

```swift
tryCatch(label(7) == "Item 7")
```

`label(42)` deve restituire `"Item 42"`

```swift
tryCatch(label(42) == "Item 42")
```

`label(0)` deve restituire `"Item 0"`

```swift
tryCatch(label(0) == "Item 0")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func label(_ n: Int) -> String {
    return String(format: "Item %d", n)
}
```
