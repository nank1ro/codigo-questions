---
language: swift
exerciseType: 1
---

# --description--

Puoi scrivere le tue funzioni di ordine superiore. Una funzione che prende una closure e **restituisce una nuova closure** costruita a partire da essa è un pattern comune: la closure restituita cattura quella originale, quindi il parametro deve essere `@escaping`.
Per esempio, `negate` trasforma un predicato nel suo opposto, pronto per essere passato a `filter`:
```swift
func negate(_ predicate: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { !predicate($0) }
}
let isEven: (Int) -> Bool = { $0 % 2 == 0 }
print([1, 2, 3, 4].filter(negate(isEven))) // [1, 3]
```
Nota che `filter(negate(isEven))` passa la closure come argomento normale, senza la sintassi della trailing closure.

# --instructions--

Scrivi una funzione `both` che prenda due closure di tipo `(Int) -> Bool` (ometti entrambe le etichette degli argomenti) e restituisca una closure dello stesso tipo che è `true` solo quando entrambe sono `true` per lo stesso argomento

Esempio di chiamata della funzione:
```swift
let smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
print([4, 12, 7, 8].filter(smallEven))
// prints [4, 8]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`both({ $0 < 10 }, { $0 % 2 == 0 })` deve essere `true` per `4`

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(4) == true)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })` deve essere `false` per `12`

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(12) == false)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })` deve essere `false` per `7`

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(7) == false)
```

La closure restituita deve funzionare con `filter`

```swift
let _smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
tryCatch([4, 12, 7, 8].filter(_smallEven) == [4, 8])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func both(_ first: @escaping (Int) -> Bool, _ second: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { first($0) && second($0) }
}
```
