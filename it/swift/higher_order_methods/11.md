---
language: swift
exerciseType: 2
---

# --description--

`zip` accoppia gli elementi di due sequenze posizione per posizione, producendo una sequenza di tuple. Si ferma alla fine della più corta:
```swift
let names = ["Ann", "Bob"]
let ages = [31, 27, 99]
let pairs = zip(names, ages).map { "\($0) is \($1)" }
print(pairs) // ["Ann is 31", "Bob is 27"]
```
Dentro la closure `$0` è l'elemento della prima sequenza e `$1` quello della seconda. `zip` è una funzione libera, non un metodo: si scrive `zip(a, b)`, non `a.zip(b)`.

# --instructions--

Completa il codice per abbinare ogni città alla sua temperatura e mantenere solo le città più calde di `20` gradi, in modo che il programma stampi `["Rome", "Cairo"]`

# --seed--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = [/](cities, temps).[/] { $1 > 20 }.map { $0.0 }
print(warm)
```

# --answers--

- zip
- filter
- map
- enumerated
- compactMap

# --solutions--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = zip(cities, temps).filter { $1 > 20 }.map { $0.0 }
print(warm)
```

# --output--

["Rome", "Cairo"]
