---
language: swift
exerciseType: 1
---

# --description--

Alcune funzioni di ordine superiore rispondono a una domanda sulla collezione invece di trasformarla. Tutte prendono una closure che restituisce un `Bool`:
- `first(where:)` restituisce il primo elemento che soddisfa la closure, oppure `nil` se non c'è nessuno
- `contains(where:)` restituisce `true` se almeno un elemento la soddisfa
- `allSatisfy` restituisce `true` se ogni elemento la soddisfa

```swift
let nums = [4, 9, 16]
print(nums.first(where: { $0 > 5 }))  // Optional(9)
print(nums.contains(where: { $0 > 5 })) // true
print(nums.allSatisfy { $0 > 5 })       // false
```
A differenza di `filter`, `first(where:)` si ferma alla prima corrispondenza e non costruisce un nuovo array.

# --instructions--

Scrivi una funzione `firstLong` che prenda un array di `String` e un `Int` chiamato `minLength` (ometti entrambe le etichette degli argomenti) e restituisca la prima stringa con almeno `minLength` caratteri, oppure `nil` se non c'è nessuna, usando `first(where:)`

Esempio di chiamata della funzione:
```swift
print(firstLong(["hi", "hello", "hey"], 4))
// prints Optional("hello")
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Devi usare `first(where:)`

```json
{
  "regex": "\\.first\\s*\\(\\s*where",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstLong(["hi", "hello", "hey"], 4)` deve restituire `"hello"`

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 4) == "hello")
```

`firstLong(["hi", "hello", "hey"], 2)` deve restituire `"hi"`

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 2) == "hi")
```

`firstLong(["hi", "hey"], 4)` deve restituire `nil`

```swift
tryCatch(firstLong(["hi", "hey"], 4) == nil)
```

`firstLong([], 1)` deve restituire `nil`

```swift
tryCatch(firstLong([], 1) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstLong(_ words: [String], _ minLength: Int) -> String? {
    return words.first(where: { $0.count >= minLength })
}
```
