---
language: swift
exerciseType: 4
---

# --description--

`contains(where:)` e `allSatisfy` sostituiscono il pattern comune di un ciclo con una variabile flag. Entrambi si fermano appena la risposta è nota: `contains(where:)` alla prima corrispondenza, `allSatisfy` al primo elemento che fallisce.
```swift
let ages = [15, 22, 40]
let anyMinor = ages.contains { $0 < 18 }  // true
let allAdults = ages.allSatisfy { $0 >= 18 } // false
```
`contains { ... }` è la forma con trailing closure di `contains(where:)`, da non confondere con `contains(_:)`, che cerca un valore specifico.

# --instructions--

Ordina gli elementi per controllare se ogni punteggio è almeno `60` e se qualche punteggio è esattamente `100`, poi stampa entrambi i risultati su una riga.
Dichiara prima `scores` e stampa per ultimo

# --answers--

```swift
let scores = [72, 100, 64]
```

```swift
let allPassed = scores.allSatisfy { $0 >= 60 }
```

```swift
let anyPerfect = scores.contains { $0 == 100 }
```

```swift
print("\(allPassed) \(anyPerfect)")
```

# --solutions--

```swift
let scores = [72, 100, 64]
let allPassed = scores.allSatisfy { $0 >= 60 }
let anyPerfect = scores.contains { $0 == 100 }
print("\(allPassed) \(anyPerfect)")
```

```swift
let scores = [72, 100, 64]
let anyPerfect = scores.contains { $0 == 100 }
let allPassed = scores.allSatisfy { $0 >= 60 }
print("\(allPassed) \(anyPerfect)")
```

# --output--

true true
