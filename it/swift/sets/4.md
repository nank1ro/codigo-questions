---
language: swift
exerciseType: 2
---

# --description--

Per verificare se un valore è presente in un set usa il metodo `contains(_:)`, che restituisce un `Bool`:
```swift
let primes: Set<Int> = [2, 3, 5, 7]
print(primes.contains(5)) // true
print(primes.contains(6)) // false
```
Questo controllo è molto veloce su un set, anche con migliaia di elementi, ed è uno dei motivi principali per preferire un set a un array per i test di appartenenza.

# --instructions--

Stampa `Welcome` se `role` è uno dei ruoli consentiti, altrimenti stampa `Access denied`

# --seed--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed[/]([/]) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --answers--

- .contains
- .has
- .includes
- role
- "role"
- allowed

# --solutions--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed.contains(role) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --output--

Welcome
