---
language: swift
exerciseType: 1
---

# --description--

Altre due operazioni completano la famiglia:
- `a.subtracting(b)` contiene gli elementi di `a` che **non** sono in `b`
- `a.symmetricDifference(b)` contiene gli elementi presenti in `a` o in `b`, ma **non in entrambi**
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.subtracting(b).sorted())          // [1, 2]
print(a.symmetricDifference(b).sorted())  // [1, 2, 4]
```
A differenza di `union` e `intersection`, `subtracting` non è simmetrica: `a.subtracting(b)` e `b.subtracting(a)` sono di solito diversi.

# --instructions--

Scrivi due funzioni che ricevono ciascuna due `Set<String>` (omettendo le etichette degli argomenti):
- `common` restituisce il set dei valori presenti in entrambi i set
- `onlyInFirst` restituisce il set dei valori presenti nel primo set ma non nel secondo

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`common(["a", "b", "c"], ["b", "c", "d"])` deve restituire `["b", "c"]`

```swift
tryCatch(common(["a", "b", "c"], ["b", "c", "d"]) == ["b", "c"])
```

`common(["a"], ["b"])` deve restituire un set vuoto

```swift
tryCatch(common(["a"], ["b"]).isEmpty)
```

`onlyInFirst(["a", "b", "c"], ["b", "c", "d"])` deve restituire `["a"]`

```swift
tryCatch(onlyInFirst(["a", "b", "c"], ["b", "c", "d"]) == ["a"])
```

`onlyInFirst(["x", "y"], ["z"])` deve restituire `["x", "y"]`

```swift
tryCatch(onlyInFirst(["x", "y"], ["z"]) == ["x", "y"])
```

Entrambe le funzioni devono restituire un `Set<String>`

```swift
tryCatch(type(of: common(["a"], ["a"])) == Set<String>.self && type(of: onlyInFirst(["a"], ["a"])) == Set<String>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func common(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.intersection(second)
}

func onlyInFirst(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.subtracting(second)
}
```
