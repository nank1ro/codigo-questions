---
language: swift
exerciseType: 1
---

# --description--

Come gli array, i set possono essere costanti (`let`) o variabili (`var`). Solo un set `var` può essere modificato dopo la creazione.
Per creare un set vuoto chiami l'inizializzatore del tipo, perché un literal vuoto `[]` da solo non direbbe a Swift quale tipo di elemento usare:
```swift
var visited = Set<String>()
print(visited.isEmpty) // true
```
La proprietà `isEmpty` è `true` quando il set non ha elementi, esattamente come con gli array.

# --instructions--

Dichiara un set costante di stringhe chiamato `colors` contenente `"red"`, `"green"` e `"blue"`, e un set variabile vuoto di interi chiamato `scores`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`colors` deve contenere esattamente `"red"`, `"green"` e `"blue"`

```swift
tryCatch(colors == ["red", "green", "blue"])
```

`colors` deve essere un `Set<String>`

```swift
tryCatch(type(of: colors) == Set<String>.self)
```

`scores` deve essere vuoto

```swift
tryCatch(scores.isEmpty)
```

`scores` deve essere un `Set<Int>`

```swift
tryCatch(type(of: scores) == Set<Int>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let colors: Set<String> = ["red", "green", "blue"]
var scores = Set<Int>()
```
