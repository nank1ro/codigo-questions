---
language: swift
exerciseType: 4
---

# --description--

`matches(of:)` restituisce **tutte** le corrispondenze invece della prima, come array. L'array non è mai `nil`: quando non corrisponde nulla è semplicemente vuoto, quindi puoi scorrerlo o trasformarlo subito:
```swift
let text = "a1 b22"
print(text.matches(of: #/\d+/#).count) // 2
```
Ogni elemento è una corrispondenza, quindi `$0.0` dentro una `map` è il testo corrispondente:
```swift
let found = text.matches(of: #/\d+/#).map { String($0.0) }
print(found) // ["1", "22"]
```

# --instructions--

Ordina gli elementi per costruire un programma che raccoglie ogni sequenza di cifre di `text` in `found` e la stampa

# --answers--

```swift
let text = "a1 b22 c333"
```

```swift
let pattern = #/\d+/#
```

```swift
let found = text.matches(of: pattern).map { String($0.0) }
```

```swift
print(found)
```

# --solutions--

```swift
let text = "a1 b22 c333"
let pattern = #/\d+/#
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

```swift
let pattern = #/\d+/#
let text = "a1 b22 c333"
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

# --output--

["1", "22", "333"]
