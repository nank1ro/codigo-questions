---
language: swift
exerciseType: 4
---

# --description--

Validare un intero testo con `^` e `$` funziona, ma `wholeMatch(of:)` lo dice direttamente: restituisce una corrispondenza solo quando il pattern copre il testo dal primo carattere all'ultimo, e `nil` altrimenti:
```swift
print("1a2b".wholeMatch(of: #/[0-9a-f]+/#) != nil) // true
print("1z".wholeMatch(of: #/[0-9a-f]+/#) != nil)   // false
```
Usa `firstMatch(of:)` per trovare qualcosa dentro un testo, e `wholeMatch(of:)` per controllare che un testo abbia una forma precisa.

# --instructions--

Ordina gli elementi per costruire un programma che definisce `isHex` prima del ciclo e stampa il risultato per ogni valore di `values`

# --answers--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
```

```swift
let values = ["1a2b", "1z", "ff"]
```

```swift
for value in values {
    print(isHex(value))
}
```

# --solutions--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
let values = ["1a2b", "1z", "ff"]
for value in values {
    print(isHex(value))
}
```

```swift
let values = ["1a2b", "1z", "ff"]
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
for value in values {
    print(isHex(value))
}
```

# --output--

true
false
true
