---
language: swift
exerciseType: 1
---

# --description--

Quando nessuna delle scorciatoie va bene, elenca tra parentesi quadre i caratteri che accetti. `[abc]` corrisponde a una `a`, a una `b` o a una `c`, e un trattino scrive un intervallo:
```swift
print("f".contains(#/[a-f]/#))  // true
print("Z".contains(#/[A-Z]/#))  // true
print("5".contains(#/[0-9a-f]/#)) // true
```
Un numero tra graffe dice esattamente quante volte si ripete il pattern precedente: `{3}` significa tre volte, `{2,4}` significa tra due e quattro volte:
```swift
print("aaa".contains(#/^a{3}$/#))  // true
print("aa".contains(#/^a{3}$/#))   // false
```
Racchiudere un pattern tra `^` e `$` con un conteggio è il modo usuale per controllare che un intero testo abbia una forma assegnata.

# --instructions--

Scrivi una funzione `isCode` che accetta un testo (`String`) senza label dell'argomento e restituisce `true` solo quando l'intero testo è esattamente tre lettere maiuscole, usando una regex

Esempio di chiamata alla funzione:
```swift
print(isCode("ABC"))
// stampa true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func isCode() {

}
```

# --asserts--

La funzione deve usare un literal di regex scritto con `#/`

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`isCode("ABC")` deve restituire `true`

```swift
tryCatch(isCode("ABC") == true)
```

`isCode("XYZ")` deve restituire `true`

```swift
tryCatch(isCode("XYZ") == true)
```

`isCode("AB")` deve restituire `false`

```swift
tryCatch(isCode("AB") == false)
```

`isCode("ABCD")` deve restituire `false`

```swift
tryCatch(isCode("ABCD") == false)
```

`isCode("abc")` deve restituire `false`

```swift
tryCatch(isCode("abc") == false)
```

`isCode("A1C")` deve restituire `false`

```swift
tryCatch(isCode("A1C") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func isCode(_ text: String) -> Bool {
    return text.contains(#/^[A-Z]{3}$/#)
}
```
