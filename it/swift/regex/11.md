---
language: swift
exerciseType: 2
---

# --description--

Contare le parentesi diventa fragile appena un pattern cresce. Dai invece un **nome** a un gruppo, scrivendo `?<nome>` subito dopo la sua parentesi di apertura, e legalo come proprietà della corrispondenza:
```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>\d+)/#) {
    print(m.key)   // size
    print(m.value) // 10
}
```
I gruppi con nome restano numerati, quindi `m.1` continua a funzionare, ma `m.key` dice cosa contiene e sopravvive a un cambio del pattern.

# --instructions--

Completa il codice in modo che il gruppo value corrisponda a cifre e la chiave sia stampata prima di esso, producendo l'output `size 10`

# --seed--

```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>[/]+)/#) {
    print(m.[/], m.value)
}
```

# --answers--

- \d
- \s
- [A-Z]
- key
- name
- 0

# --solutions--

```swift
let text = "size=10"
if let m = text.firstMatch(of: #/(?<key>[a-z]+)=(?<value>\d+)/#) {
    print(m.key, m.value)
}
```

# --output--

size 10
