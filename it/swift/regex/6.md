---
language: swift
exerciseType: 1
---

# --description--

`m.0` non è una `String` ma una `Substring`: una vista sul testo originale, non una copia. Si stampa esattamente come una stringa, ma dove è richiesta una `String` devi convertirla:
```swift
let text = "order 42"
if let m = text.firstMatch(of: #/\d+/#) {
    let found: String = String(m.0)
    print(found) // 42
}
```
Gli inizializzatori dei numeri accettano direttamente una `Substring`, quindi `Int(m.0)` funziona senza la deviazione.

# --instructions--

Scrivi una funzione `firstNumber` che accetta un testo (`String`) senza label dell'argomento e restituisce la prima sequenza di cifre come `String`, oppure `"none"` quando il testo non contiene cifre

Esempio di chiamata alla funzione:
```swift
print(firstNumber("order 42 today 7"))
// stampa 42
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func firstNumber() {

}
```

# --asserts--

La funzione deve usare `firstMatch(of:`

```json
{
  "regex": "firstMatch\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstNumber("order 42 today 7")` deve restituire `"42"`

```swift
tryCatch(firstNumber("order 42 today 7") == "42")
```

`firstNumber("a1")` deve restituire `"1"`

```swift
tryCatch(firstNumber("a1") == "1")
```

`firstNumber("100")` deve restituire `"100"`

```swift
tryCatch(firstNumber("100") == "100")
```

`firstNumber("abc")` deve restituire `"none"`

```swift
tryCatch(firstNumber("abc") == "none")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstNumber(_ text: String) -> String {
    if let m = text.firstMatch(of: #/\d+/#) {
        return String(m.0)
    }
    return "none"
}
```
