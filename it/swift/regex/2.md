---
language: swift
exerciseType: 1
---

# --description--

Poche scorciatoie coprono la maggior parte dei pattern. Ognuna corrisponde esattamente a **un** carattere:
- `\d` è una cifra
- `\w` è una lettera, una cifra o un underscore
- `\s` è uno spazio, una tabulazione o un'a capo
- `.` è un carattere qualsiasi

Per corrispondere a più di un carattere, aggiungi un **quantificatore** subito dopo il pattern:
- `+` significa uno o più
- `*` significa zero o più
- `?` significa zero o uno

Quindi `\w+` è una parola, `\s*` è una spaziatura opzionale e `\d?` è una cifra opzionale:
```swift
print("hello world".contains(#/\w+\s\w+/#)) // true
print("hello".contains(#/\w+\s\w+/#))       // false
```
Un carattere senza significato speciale corrisponde semplicemente a se stesso, quindi `#/cat/#` corrisponde alle tre lettere `cat`.

# --instructions--

Scrivi una funzione `hasDigit` che accetta un testo (`String`) senza label dell'argomento e restituisce `true` quando il testo contiene almeno una cifra, usando una regex

Esempio di chiamata alla funzione:
```swift
print(hasDigit("abc1"))
// stampa true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func hasDigit() {

}
```

# --asserts--

La funzione deve usare un literal di regex scritto con `#/`

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`hasDigit("abc1")` deve restituire `true`

```swift
tryCatch(hasDigit("abc1") == true)
```

`hasDigit("7")` deve restituire `true`

```swift
tryCatch(hasDigit("7") == true)
```

`hasDigit("abc")` deve restituire `false`

```swift
tryCatch(hasDigit("abc") == false)
```

`hasDigit("")` deve restituire `false`

```swift
tryCatch(hasDigit("") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func hasDigit(_ text: String) -> Bool {
    return text.contains(#/\d/#)
}
```
