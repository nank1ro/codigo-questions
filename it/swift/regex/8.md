---
language: swift
exerciseType: 1
---

# --description--

Poiché `Int(_:)` accetta una `Substring`, trasformare il testo trovato in numeri è un passaggio solo. `compactMap` è comodo qui: scarta i valori che tornano `nil`:
```swift
let text = "a1 b22"
let numbers = text.matches(of: #/\d+/#).compactMap { Int($0.0) }
print(numbers) // [1, 22]
```
Usa `map` quando ogni elemento si converte, `compactMap` quando alcuni possono fallire.

# --instructions--

Scrivi una funzione `numbers` che accetta un testo (`String`) senza label dell'argomento e restituisce ogni sequenza di cifre che contiene come array di `Int`, nell'ordine in cui appaiono

Esempio di chiamata alla funzione:
```swift
print(numbers("a1 b22 c333"))
// prints [1, 22, 333]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func numbers() {

}
```

# --asserts--

La funzione deve usare `matches(of:`

```json
{
  "regex": "matches\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`numbers("a1 b22 c333")` deve restituire `[1, 22, 333]`

```swift
tryCatch(numbers("a1 b22 c333") == [1, 22, 333])
```

`numbers("7")` deve restituire `[7]`

```swift
tryCatch(numbers("7") == [7])
```

`numbers("abc")` deve restituire `[]`

```swift
tryCatch(numbers("abc") == [])
```

`numbers("10-20")` deve restituire `[10, 20]`

```swift
tryCatch(numbers("10-20") == [10, 20])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func numbers(_ text: String) -> [Int] {
    return text.matches(of: #/\d+/#).compactMap { Int($0.0) }
}
```
