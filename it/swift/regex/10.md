---
language: swift
exerciseType: 1
---

# --description--

Un pattern può contenere diversi gruppi. Sono numerati da sinistra a destra in base alla loro parentesi di apertura, quindi il secondo è `m.2`, il terzo `m.3`, e così via:
```swift
let text = "size=10"
if let m = text.firstMatch(of: #/([a-z]+)=(\d+)/#) {
    print(m.1, m.2) // size 10
}
```
`m.0` resta sempre l'intera corrispondenza, qualunque sia il numero di gruppi.

# --instructions--

Scrivi una funzione `setting` che accetta un testo (`String`) senza label dell'argomento, trova un'impostazione scritta come lettere minuscole, un segno `=` e cifre, e restituisce il nome e il valore uniti da due punti, oppure `"none"` quando il testo non contiene una simile impostazione

Esempio di chiamata alla funzione:
```swift
print(setting("size=10"))
// prints size:10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func setting() {

}
```

# --asserts--

La funzione deve usare un literal di regex scritto con `#/`

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`setting("size=10")` deve restituire `"size:10"`

```swift
tryCatch(setting("size=10") == "size:10")
```

`setting("use width=800 here")` deve restituire `"width:800"`

```swift
tryCatch(setting("use width=800 here") == "width:800")
```

`setting("a=1 b=2")` deve restituire `"a:1"`

```swift
tryCatch(setting("a=1 b=2") == "a:1")
```

`setting("nothing here")` deve restituire `"none"`

```swift
tryCatch(setting("nothing here") == "none")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func setting(_ text: String) -> String {
    if let m = text.firstMatch(of: #/([a-z]+)=(\d+)/#) {
        return "\(m.1):\(m.2)"
    }
    return "none"
}
```
