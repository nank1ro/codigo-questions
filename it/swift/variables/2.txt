<section id="options">
{
"language": "swift",
"type": "runCode",
"showRun": false
}
</section>

<section id="description">
Le variabili vengono chiamate in questo modo perche' il valore che memorizzano può cambiare.
Possiamo aggiornare `x` usando `=` assegnando un nuovo valore:
```
>>> var x = 1
>>> print(x)
1
>>> x = 2
>>> print(x)
2
```
</section>
<section id="instructions">
Aggiorna il valore della variabile `x` con il numero `10`
</section>
<section id="seed">
var x = 5
// scrivi dopo questa linea
</section>
<section id="tests">
{
    "tests": [
        {
            "type": "assert",
            "assert":"assert(x == 10, \"x deve essere uguale a 10\")"
        }
    ]
}
</section>