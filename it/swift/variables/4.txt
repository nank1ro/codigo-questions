<section id="options">
{
"language": "swift",
"type": "runCode",
"showRun": false
}
</section>

<section id="description">
Quando aggiorniamo una variabile, essa dimentica il suo valore precedente.
Qui stampiamo due volte la variabile `x` e vediamo come cambia il suo valore
```
var x = 5
print(x) // prints 5
x = 10
print(x) // prints 10
```
</section>
<section id="instructions">
Aggiorna il valore della variabile `x` con il numero `3`.
</section>
<section id="seed">
var x = 1
// scrivi dopo questa linea
</section>
<section id="tests">
{
    "tests": [
        {
            "type": "assert",
            "assert":"assert(x == 3, \"x deve essere uguale a 3\")"
        }
    ]
}
</section>
