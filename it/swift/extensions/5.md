---
language: swift
exerciseType: 1
---

# --description--
Anche per `String` le proprietà calcolate nelle extension sono una scelta naturale. Il metodo `reversed()` restituisce i caratteri in ordine inverso, e `String(...)` li riporta a una stringa:
```swift
extension String {
    var backwards: String {
        return String(reversed())
    }
}
print("swift".backwards) // tfiws
```
Come per i metodi, `reversed()` dentro l'extension significa `self.reversed()`.

# --instructions--
Scrivi un'extension di `String` con una proprietà calcolata `isPalindrome` di tipo `Bool`, che sia `true` quando la stringa si legge uguale da sinistra e da destra. Il confronto distingue maiuscole e minuscole

Esempio di uso:
```swift
print("level".isPalindrome)
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--
Devi usare un'extension di `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`"level".isPalindrome` deve essere `true`

```swift
tryCatch("level".isPalindrome == true)
```

`"swift".isPalindrome` deve essere `false`

```swift
tryCatch("swift".isPalindrome == false)
```

`"Anna".isPalindrome` deve essere `false` (il confronto distingue maiuscole e minuscole)

```swift
tryCatch("Anna".isPalindrome == false)
```

`"".isPalindrome` deve essere `true`

```swift
tryCatch("".isPalindrome == true)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    var isPalindrome: Bool {
        return self == String(reversed())
    }
}
```
