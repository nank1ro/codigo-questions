---
language: swift
exerciseType: 1
---

# --description--
Le extension funzionano su qualsiasi tipo, anche su quelli di cui non hai il codice sorgente. `String` viene dalla libreria standard, ma puoi comunque darle nuovi metodi:
```swift
extension String {
    func whisper() -> String {
        return self.lowercased() + "..."
    }
}
print("HELLO".whisper()) // hello...
```
Dentro un'extension puoi omettere `self.` quando chiami altri membri del tipo: `lowercased()` da solo significa `self.lowercased()`.

# --instructions--
Scrivi un'extension di `String` con un metodo `shout()` che restituisca la stringa in maiuscolo seguita da un punto esclamativo `!`

Esempio di chiamata del metodo:
```swift
print("hi".shout())
// prints HI!
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--
Devi usare un'extension di `String`

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`"hi".shout()` deve restituire `"HI!"`

```swift
tryCatch("hi".shout() == "HI!")
```

`"Swift is fun".shout()` deve restituire `"SWIFT IS FUN!"`

```swift
tryCatch("Swift is fun".shout() == "SWIFT IS FUN!")
```

`"".shout()` deve restituire `"!"`

```swift
tryCatch("".shout() == "!")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    func shout() -> String {
        return uppercased() + "!"
    }
}
```
