---
language: swift
exerciseType: 1
---

# --description--
Le extension non servono solo per i tipi di altri. Un modo diffuso di organizzare il tuo codice è tenere le proprietà memorizzate nel corpo della struct o della classe e aggiungere il comportamento in una o più extension, ognuna che raggruppa membri correlati:
```swift
struct Circle {
    var radius: Double
}
extension Circle {
    var diameter: Double {
        return radius * 2
    }
    func grown(by amount: Double) -> Circle {
        return Circle(radius: radius + amount)
    }
}
```
I membri aggiunti in un'extension possono usare direttamente le proprietà memorizzate, esattamente come se fossero scritti dentro il tipo.

# --instructions--
La struct `Rectangle` è già data. Scrivi un'extension di `Rectangle` con:
- una proprietà calcolata `area` di tipo `Double`, il prodotto di `width` e `height`
- un metodo `scaled(by factor: Double)` che restituisca un nuovo `Rectangle` con entrambi i lati moltiplicati per `factor`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Rectangle {
    var width: Double
    var height: Double
}
```

# --asserts--
Devi usare un'extension di `Rectangle`

```json
{
  "regex": "extension\\s+Rectangle",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

L'area di un rettangolo `2` per `3` deve essere `6.0`

```swift
tryCatch(Rectangle(width: 2, height: 3).area == 6.0)
```

L'area di un rettangolo `1.5` per `4` deve essere `6.0`

```swift
tryCatch(Rectangle(width: 1.5, height: 4).area == 6.0)
```

Un rettangolo `2` per `3` scalato di `2` deve essere `4` per `6`

```swift
let _r = Rectangle(width: 2, height: 3).scaled(by: 2)
tryCatch(_r.width == 4.0 && _r.height == 6.0)
```

La scalatura non deve cambiare il rettangolo originale

```swift
let _o = Rectangle(width: 2, height: 3)
let _s = _o.scaled(by: 0.5)
tryCatch(_o.width == 2.0 && _o.height == 3.0 && _s.area == 1.5)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Rectangle {
    var width: Double
    var height: Double
}
extension Rectangle {
    var area: Double {
        return width * height
    }
    func scaled(by factor: Double) -> Rectangle {
        return Rectangle(width: width * factor, height: height * factor)
    }
}
```
