---
language: swift
exerciseType: 1
---

# --description--
`Array` è un tipo generico: `[Int]` e `[String]` sono entrambi array, con un tipo **`Element`** diverso. Un'extension di `Array` si applica a tutti, il che è un problema quando il nuovo membro ha senso solo per alcuni elementi: non puoi sommare numeri che sono stringhe.
Una clausola `where` limita l'extension agli array il cui `Element` è un tipo dato:
```swift
extension Array where Element == Int {
    var largest: Int {
        var result = Int.min
        for number in self {
            if number > result {
                result = number
            }
        }
        return result
    }
}
print([3, 9, 2].largest) // 9
```
`[3, 9, 2].largest` funziona, mentre `["a", "b"].largest` è un errore di compilazione: la proprietà non esiste su `[String]`.

# --instructions--
Scrivi un'extension di `Array` limitata a `Element == Int` con un metodo `sum()` che restituisca la somma degli elementi. Non usare `reduce`

Esempio di chiamata del metodo:
```swift
print([1, 2, 3].sum())
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--
Devi usare un'extension di `Array` con una clausola `where Element == Int`

```json
{
  "regex": "extension\\s+Array\\s+where\\s+Element\\s*==\\s*Int",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Non devi usare `reduce`

```json
{
  "regex": "reduce",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`[1, 2, 3].sum()` deve restituire `6`

```swift
tryCatch([1, 2, 3].sum() == 6)
```

`[10, -4].sum()` deve restituire `6`

```swift
tryCatch([10, -4].sum() == 6)
```

`[7].sum()` deve restituire `7`

```swift
tryCatch([7].sum() == 7)
```

La somma di un array vuoto deve essere `0`

```swift
let _empty: [Int] = []
tryCatch(_empty.sum() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension Array where Element == Int {
    func sum() -> Int {
        var total = 0
        for number in self {
            total += number
        }
        return total
    }
}
```
