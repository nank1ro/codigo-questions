---
language: swift
exerciseType: 4
---

# --description--
`Int`, `String`, gli array e le struct sono **tipi valore**: un metodo non può cambiare il valore su cui è chiamato a meno che non sia marcato `mutating`. Anche le extension possono aggiungere metodi mutating:
```swift
extension Int {
    mutating func increment() {
        self += 1
    }
}
var count = 1
count.increment()
print(count) // 2
```
Dentro un metodo mutating puoi assegnare a `self`. Il valore deve essere memorizzato in una `var`: chiamare `increment()` su una costante `let` è un errore di compilazione.

# --instructions--
Ordina gli elementi per definire prima un'extension di `Int` con un metodo mutating `double()`, poi dichiarare una variabile `n` con il valore `4`, raddoppiarla e stamparla

# --answers--

```swift
extension Int {
```

```swift
    mutating func double() { self *= 2 }
```

```swift
}
```

```swift
var n = 4
```

```swift
n.double()
```

```swift
print(n)
```

# --solutions--

```swift
extension Int {
    mutating func double() { self *= 2 }
}
var n = 4
n.double()
print(n)
```

# --output--

8
