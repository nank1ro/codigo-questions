---
language: swift
exerciseType: 1
---

# --description--

I protocolli non sono limitati alle struct. Una **classe** si conforma esattamente allo stesso modo, elencando il protocollo dopo i due punti:
```swift
protocol Openable {
    func open() -> String
}

class Door: Openable {
    func open() -> String {
        return "creak"
    }
}
```
Se la classe eredita anche da un'altra classe, la superclasse viene prima nella lista e i protocolli la seguono. Un tipo può adottare più protocolli contemporaneamente, separati da virgole.

# --instructions--

Fai in modo che la classe `Box` si conformi a `Sized`, restituendo il numero di oggetti che contiene

Esempio di utilizzo:
```swift
print(Box(items: ["pen", "key"]).size())
// prints 2
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Sized {
    func size() -> Int
}

class Box {
    var items: [String]

    init(items: [String]) {
        self.items = items
    }
}
```

# --before-asserts--

```swift
func _asSized(_ s: Sized) -> Int {
    return s.size()
}
```

# --asserts--

Una scatola con `pen` e `key` deve avere dimensione `2`

```swift
tryCatch(_asSized(Box(items: ["pen", "key"])) == 2)
```

Una scatola vuota deve avere dimensione `0`

```swift
tryCatch(_asSized(Box(items: [])) == 0)
```

Una scatola con tre oggetti deve avere dimensione `3`

```swift
tryCatch(_asSized(Box(items: ["a", "b", "c"])) == 3)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Sized {
    func size() -> Int
}

class Box: Sized {
    var items: [String]

    init(items: [String]) {
        self.items = items
    }

    func size() -> Int {
        return items.count
    }
}
```
