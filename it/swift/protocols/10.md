---
language: swift
exerciseType: 4
---

# --description--

L'extension di un protocollo può anche aggiungere membri che il protocollo non ha mai elencato come requisiti. Sono comodità extra, disponibili su ogni tipo che si conforma:
```swift
protocol Sized {
    var count: Int { get }
}

extension Sized {
    var isEmpty: Bool {
        return count == 0
    }
}
```
`isEmpty` non è un requisito, quindi un tipo che si conforma non deve fornirla — la riceve semplicemente.

# --instructions--

Ordina gli elementi per stampare `HEY`, dichiarando prima il protocollo, poi la sua extension, poi la struct, poi la `print`

# --answers--

```swift
protocol Loud {
    var text: String { get }
}
```

```swift
extension Loud {
    func shout() -> String { return text.uppercased() }
}
```

```swift
struct Message: Loud {
    var text: String
}
```

```swift
print(Message(text: "hey").shout())
```

# --solutions--

```swift
protocol Loud {
    var text: String { get }
}
extension Loud {
    func shout() -> String { return text.uppercased() }
}
struct Message: Loud {
    var text: String
}
print(Message(text: "hey").shout())
```

# --output--

HEY
