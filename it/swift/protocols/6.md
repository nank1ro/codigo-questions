---
language: swift
exerciseType: 4
---

# --description--

Un protocollo di solito raccoglie più di un requisito, e un tipo che si conforma deve soddisfarli tutti:
```swift
protocol Vehicle {
    var wheels: Int { get }
    func move() -> String
}

struct Bike: Vehicle {
    var wheels = 2

    func move() -> String {
        return "pedalling"
    }
}
```
L'ordine dei requisiti tra le parentesi graffe non conta, e nemmeno l'ordine in cui il tipo che si conforma li fornisce: il compilatore controlla solo che non manchi nulla.

# --instructions--

Ordina gli elementi per stampare `Moo`, dichiarando prima il protocollo, poi la struct, poi l'istanza prima della `print`

# --answers--

```swift
protocol Animal {
    func sound() -> String
}
```

```swift
struct Cow: Animal {
    func sound() -> String { return "Moo" }
}
```

```swift
let cow = Cow()
```

```swift
print(cow.sound())
```

# --solutions--

```swift
protocol Animal {
    func sound() -> String
}
struct Cow: Animal {
    func sound() -> String { return "Moo" }
}
let cow = Cow()
print(cow.sound())
```

# --output--

Moo
