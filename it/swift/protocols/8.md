---
language: swift
exerciseType: 2
---

# --description--

La conformità non deve essere dichiarata accanto al tipo. Un'**extension** può aggiungerla in seguito, così la dichiarazione del tipo resta concentrata sui suoi dati:
```swift
protocol Resettable {
    mutating func reset()
}

struct Timer {
    var seconds = 0
}

extension Timer: Resettable {
    mutating func reset() {
        seconds = 0
    }
}
```
Funziona anche per i tipi che non hai scritto tu: puoi fare in modo che un tipo della libreria standard si conformi a uno dei tuoi protocolli senza toccarne il codice sorgente.

# --instructions--

Completa il codice in modo che `increment()` sia un requisito mutating e che `Clicks` adotti `Counter` in un'extension

# --seed--

```swift
protocol Counter {
    [/] func increment()
}

struct Clicks {
    var count = 0
}

[/] Clicks: Counter {
    mutating func increment() {
        count [/] 1
    }
}

var c = Clicks()
c.increment()
c.increment()
print(c.count)
```

# --answers--

- mutating
- extension
- +=
- static
- struct
- -=

# --solutions--

```swift
protocol Counter {
    mutating func increment()
}

struct Clicks {
    var count = 0
}

extension Clicks: Counter {
    mutating func increment() {
        count += 1
    }
}

var c = Clicks()
c.increment()
c.increment()
print(c.count)
```

# --output--

2
