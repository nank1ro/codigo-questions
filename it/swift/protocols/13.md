---
language: swift
exerciseType: 1
---

# --description--

La libreria standard è costruita con i protocolli, e i tuoi tipi possono adottarli.

`Equatable` dà a un tipo l'operatore `==`. Per una struct le cui proprietà memorizzate sono tutte `Equatable`, dichiarare la conformità basta — Swift scrive `==` per te:
```swift
struct Point: Equatable {
    var x: Int
    var y: Int
}

print(Point(x: 1, y: 2) == Point(x: 1, y: 2)) // true
```
`Comparable` eredita da `Equatable` e aggiunge l'ordinamento. Implementi un solo operatore, `<`, scritto come una `static func` che prende i due valori, e ottieni `>`, `<=`, `>=`, più `sorted()`, `min()` e `max()` sulle collezioni gratis:
```swift
struct Version: Comparable {
    var number: Int

    static func < (lhs: Version, rhs: Version) -> Bool {
        return lhs.number < rhs.number
    }
}
```

# --instructions--

Fai in modo che `Weight` si conformi a `Comparable`, ordinando per `grams`

Esempio di utilizzo:
```swift
print(Weight(grams: 100) < Weight(grams: 250))
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Weight {
    var grams: Int
}
```

# --asserts--

`100` grammi devono essere più leggeri di `250` grammi

```swift
tryCatch(Weight(grams: 100) < Weight(grams: 250))
```

`500` grammi devono essere più pesanti di `250` grammi

```swift
tryCatch(Weight(grams: 500) > Weight(grams: 250))
```

Due pesi di `80` grammi devono essere uguali

```swift
tryCatch(Weight(grams: 80) == Weight(grams: 80))
```

Il più pesante tra `3` e `1` grammi deve essere `3` grammi

```swift
tryCatch([Weight(grams: 3), Weight(grams: 1)].max() == Weight(grams: 3))
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Weight: Comparable {
    var grams: Int

    static func < (lhs: Weight, rhs: Weight) -> Bool {
        return lhs.grams < rhs.grams
    }
}
```
