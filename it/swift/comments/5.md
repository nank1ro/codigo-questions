---
language: swift
exerciseType: 1
---

# --description--

A differenza di `//`, che si ferma alla fine della riga, un commento `/*` si ferma solo al `*/`. Se dimentichi di chiuderlo, il compilatore tratta tutto il codice seguente come parte del commento e segnala un errore:
```swift
let width = 10 /* in centimetres
print(width) // still inside the comment: error, the comment is never closed
```
Sia `//` che `/* */` funzionano come commenti in coda, ma con `/*` assicurati sempre che ci sia il `*/`.

# --instructions--

`twice` non compila perché il commento in coda sulla prima riga non viene mai chiuso, quindi ingoia il `return`. Chiudi il commento alla fine di quella stessa riga

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func twice(_ n: Int) -> Int {
    let result = n * 2 /* twice the input
    return result
}
```

# --asserts--

Il commento deve essere chiuso sulla stessa riga

```json
{
  "regex": "/\\* twice the input \\*/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`twice(4)` deve restituire `8`

```swift
tryCatch(twice(4) == 8)
```

`twice(-3)` deve restituire `-6`

```swift
tryCatch(twice(-3) == -6)
```

`twice(0)` deve restituire `0`

```swift
tryCatch(twice(0) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func twice(_ n: Int) -> Int {
    let result = n * 2 /* twice the input */
    return result
}
```
