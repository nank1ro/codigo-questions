---
language: swift
exerciseType: 1
---

# --description--

Un `FIXME` è diverso da un `TODO`: il codice esiste già, ma si sa che è sbagliato. Un buon `FIXME` dice qual è il bug e, quando possibile, dà un esempio che lo mostra, così la persona successiva può correggerlo in fretta. Come per il `TODO`, cancella il marcatore una volta corretto il bug, ma tieni il commento di documentazione, che è ancora vero.

# --instructions--

Correggi il bug descritto dal commento `FIXME` in `average` (aggiungi le parentesi così la somma viene calcolata prima della divisione), poi rimuovi il commento `FIXME`. Tieni il commento di documentazione `///`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
/// Returns the average of `a` and `b`, rounded down.
func average(_ a: Int, _ b: Int) -> Int {
    // FIXME: divides before adding, so average(4, 6) gives 7 instead of 5
    return a + b / 2
}
```

# --asserts--

The `FIXME` comment must be removed

```json
{
  "regex": "FIXME",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

The `///` documentation comment must be kept

```json
{
  "regex": "/// Returns the average",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`average(4, 6)` must return `5`

```swift
tryCatch(average(4, 6) == 5)
```

`average(10, 20)` must return `15`

```swift
tryCatch(average(10, 20) == 15)
```

`average(3, 4)` must return `3`

```swift
tryCatch(average(3, 4) == 3)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
/// Returns the average of `a` and `b`, rounded down.
func average(_ a: Int, _ b: Int) -> Int {
    return (a + b) / 2
}
```
