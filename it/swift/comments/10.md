---
language: swift
exerciseType: 1
---

# --description--

La prima riga di un commento di documentazione è il **riassunto**: una frase breve che dice cosa fa la funzione. Scrivila alla terza persona, come se descrivessi la funzione: "Returns...", "Adds...", "Checks...".
```swift
/// Returns `true` when `n` is divisible by two.
func isEven(_ n: Int) -> Bool {
    return n % 2 == 0
}
```
Il commento deve stare subito sopra la dichiarazione, senza righe vuote in mezzo, altrimenti Xcode non lo associa alla funzione.

# --instructions--

Aggiungi un commento di documentazione `///` con un riassunto di una riga subito sopra `square`, per esempio `/// Returns `n` multiplied by itself.`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func square(_ n: Int) -> Int {
    return n * n
}
```

# --asserts--

A `///` comment must be placed directly above `func square`

```json
{
  "regex": "///\\s*\\S[^\\n]*\\n\\s*func square",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`square(3)` must return `9`

```swift
tryCatch(square(3) == 9)
```

`square(-5)` must return `25`

```swift
tryCatch(square(-5) == 25)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
/// Returns `n` multiplied by itself.
func square(_ n: Int) -> Int {
    return n * n
}
```
