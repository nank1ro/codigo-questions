---
language: swift
exerciseType: 4
---

# --description--

Dopo il riassunto, un commento di documentazione può descrivere i parametri e il valore restituito con speciali elementi di elenco Markdown che Xcode riconosce:
```swift
/// Restituisce il numero di secondi nei minuti indicati.
/// - Parameter minutes: un numero intero di minuti, mai negativo
/// - Returns: `minutes` moltiplicato per sessanta
func toSeconds(_ minutes: Int) -> Int {
    return minutes * 60
}
```
L'ordine è sempre lo stesso: prima il riassunto, poi `- Parameter nome:` per ogni parametro, poi `- Returns:`.

# --instructions--

Ordina gli elementi per documentare `twice` con un riassunto, una riga `Parameter` e una riga `Returns`, in quest'ordine e subito sopra la funzione, poi stampa `twice(21)`

# --answers--

```swift
/// Restituisce il doppio di un numero.
```

```swift
/// - Parameter n: il numero da raddoppiare
```

```swift
/// - Returns: `n` moltiplicato per due
```

```swift
func twice(_ n: Int) -> Int {
    return n * 2
}
```

```swift
print(twice(21))
```

# --solutions--

```swift
/// Restituisce il doppio di un numero.
/// - Parameter n: il numero da raddoppiare
/// - Returns: `n` moltiplicato per due
func twice(_ n: Int) -> Int {
    return n * 2
}
print(twice(21))
```

# --output--

42
