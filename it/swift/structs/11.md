---
language: swift
exerciseType: 1
---

# --description--

Alcuni valori appartengono al tipo stesso piuttosto che a una singola istanza: un codice valuta, un valore predefinito condiviso, una factory che costruisce un caso comune. Contrassegnali `static` e leggili tramite il nome del tipo:
```swift
struct Money {
    static let currency = "EUR"
    var amount: Int

    static func zero() -> Money {
        return Money(amount: 0)
    }
}

print(Money.currency)     // EUR
print(Money.zero().amount) // 0
```
Qui `currency` è dichiarata con `let` perché non cambia mai, quindi è una costante condivisa dall'intero programma. `Money.currency` funziona senza creare nemmeno una `Money`, mentre `amount` richiede un'istanza.

# --instructions--

Aggiungi a `Money` una costante statica `currency` uguale a `"EUR"` e un metodo statico `zero` che non prende parametri e restituisce una `Money` con `amount` uguale a `0`

Esempio di utilizzo:
```swift
print(Money.currency)
// prints EUR
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Money {
    var amount: Int

}
```

# --asserts--

`currency` deve essere una costante statica

```json
{
  "regex": "static\\s+let\\s+currency",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Money.currency` deve essere `"EUR"`

```swift
tryCatch(Money.currency == "EUR")
```

`Money.zero()` deve avere un `amount` di `0`

```swift
tryCatch(Money.zero().amount == 0)
```

L'inizializzatore membro per membro deve ancora funzionare

```swift
tryCatch(Money(amount: 7).amount == 7)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int

    static func zero() -> Money {
        return Money(amount: 0)
    }
}
```
