---
language: swift
exerciseType: 2
---

# --description--

Una proprietà memorizzata può ricevere un **valore predefinito** proprio dove viene dichiarata. Swift ne deduce il tipo da quel valore, quindi puoi omettere l'annotazione del tipo:
```swift
struct Counter {
    var label: String
    var value = 0
}
```
L'inizializzatore membro per membro trasforma ogni proprietà con valore predefinito in un argomento facoltativo: passalo per sostituire il valore predefinito, omettilo per conservarlo.
```swift
let a = Counter(label: "clicks")
print(a.value) // 0

let b = Counter(label: "clicks", value: 7)
print(b.value) // 7
```

# --instructions--

Completa il codice in modo che `value` parta da `0` e `c` venga creata senza passare `value`

# --seed--

```swift
struct Counter {
    var label: String
    var value = [/]
}

let c = Counter([/]: "clicks")
print(c.label)
print(c.value)
```

# --answers--

- 0
- label
- 1
- value
- Counter
- name

# --solutions--

```swift
struct Counter {
    var label: String
    var value = 0
}

let c = Counter(label: "clicks")
print(c.label)
print(c.value)
```

# --output--

clicks
0
