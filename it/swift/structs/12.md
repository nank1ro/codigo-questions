---
language: swift
exerciseType: 4
---

# --instructions--

Ordina gli elementi per stampare `EUR 12`, dichiarando prima la struct e l'istanza prima della `print`

# --answers--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int
}
```

```swift
let price = Money(amount: 12)
```

```swift
print("\(Money.currency) \(price.amount)")
```

# --solutions--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int
}
let price = Money(amount: 12)
print("\(Money.currency) \(price.amount)")
```

# --output--

EUR 12
