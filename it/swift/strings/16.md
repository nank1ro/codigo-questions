---
language: swift
exerciseType: 1
---

# --description--

Il framework Foundation aggiunge molti metodi aggiuntivi per le stringhe. Uno dei più utili è `replacingOccurrences(of:with:)`, che restituisce una nuova stringa in cui ogni occorrenza del primo testo viene sostituita dal secondo:
```swift
import Foundation

let path = "a/b/c"
print(path.replacingOccurrences(of: "/", with: "-")) // a-b-c
```
Ricordati di scrivere `import Foundation` in cima al file, altrimenti il metodo non è disponibile. Le chiamate ai metodi possono essere concatenate, quindi `text.lowercased().replacingOccurrences(of: " ", with: "_")` è valido.

# --instructions--

Scrivi una funzione `snakeCase` che accetta una `String` (ometti la label dell'argomento) e la restituisce in minuscolo con ogni spazio sostituito da un underscore `_`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`snakeCase("Hello World")` deve restituire `"hello_world"`

```swift
tryCatch(snakeCase("Hello World") == "hello_world")
```

`snakeCase("My First Post")` deve restituire `"my_first_post"`

```swift
tryCatch(snakeCase("My First Post") == "my_first_post")
```

`snakeCase("swift")` deve restituire `"swift"`

```swift
tryCatch(snakeCase("swift") == "swift")
```

`snakeCase("")` deve restituire `""`

```swift
tryCatch(snakeCase("") == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func snakeCase(_ text: String) -> String {
    return text.lowercased().replacingOccurrences(of: " ", with: "_")
}
```
