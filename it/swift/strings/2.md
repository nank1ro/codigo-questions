---
language: swift
exerciseType: 1
---

# --description--

L'**interpolazione di stringhe** inserisce il valore di un'espressione all'interno di un literal di stringa. Racchiudi l'espressione tra `\()`:
```swift
let name = "Ada"
let age = 36
print("\(name) is \(age) years old") // Ada is 36 years old
```
Qualsiasi tipo può essere interpolato: numeri, booleani e altre stringhe vengono tutti convertiti automaticamente in testo.

# --instructions--

Scrivi una funzione `greet` che accetta una `String` (ometti la label dell'argomento) e restituisce il testo `Hello, <name>!`, dove `<name>` è l'argomento.

Esempio di chiamata alla funzione:
```swift
print(greet("Ada"))
// prints Hello, Ada!
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`greet("Ada")` deve restituire `"Hello, Ada!"`

```swift
tryCatch(greet("Ada") == "Hello, Ada!")
```

`greet("Swift")` deve restituire `"Hello, Swift!"`

```swift
tryCatch(greet("Swift") == "Hello, Swift!")
```

`greet("")` deve restituire `"Hello, !"`

```swift
tryCatch(greet("") == "Hello, !")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greet(_ name: String) -> String {
    return "Hello, \(name)!"
}
```
