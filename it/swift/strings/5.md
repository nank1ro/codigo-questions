---
language: swift
exerciseType: 1
---

# --description--

Una `String` è una collezione di valori `Character`. Un `Character` è una singola lettera, cifra, simbolo o spazio, e si scrive con le stesse virgolette doppie di una stringa, quindi serve un'annotazione di tipo per ottenerne uno:
```swift
let letter: Character = "a"
let text = "abc"
print(text.count) // 3
```
Controllare `isEmpty` è preferibile a confrontare `count` con `0`: si legge meglio e non deve contare ogni carattere.

# --instructions--

Scrivi una funzione `length` che accetta una `String` (ometti la label dell'argomento) e restituisce il suo numero di caratteri, e una funzione `hasText` che accetta una `String` (ometti la label dell'argomento) e restituisce `true` solo quando la stringa non è vuota

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`length("Swift")` deve restituire `5`

```swift
tryCatch(length("Swift") == 5)
```

`length("a b c")` deve restituire `5`

```swift
tryCatch(length("a b c") == 5)
```

`length("")` deve restituire `0`

```swift
tryCatch(length("") == 0)
```

`hasText("x")` deve restituire `true`

```swift
tryCatch(hasText("x") == true)
```

`hasText("")` deve restituire `false`

```swift
tryCatch(hasText("") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func length(_ text: String) -> Int {
    return text.count
}

func hasText(_ text: String) -> Bool {
    return !text.isEmpty
}
```
