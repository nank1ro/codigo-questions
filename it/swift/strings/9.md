---
language: swift
exerciseType: 1
---

# --description--

Lavorare con gli indici è prolisso, quindi Swift offre delle scorciatoie per i casi più comuni:
- `first` e `last` restituiscono il primo e l'ultimo carattere come `Character?` opzionale (`nil` per una stringa vuota)
- `prefix(n)` restituisce i primi `n` caratteri e `suffix(n)` gli ultimi `n`
```swift
let word = "Swift"
print(word.first!)     // S
print(word.prefix(2))  // Sw
print(word.suffix(3))  // ift
```
`prefix` e `suffix` restituiscono una `Substring`, una vista sul testo originale. Per salvarla come una vera `String`, racchiudila in `String(...)`. Se `n` è più grande di `count`, ottieni semplicemente l'intera stringa.

# --instructions--

Scrivi una funzione `firstTwo` che accetta una `String` (ometti la label dell'argomento) e restituisce i suoi primi due caratteri come `String`, e una funzione `lastTwo` che restituisce i suoi ultimi due caratteri come `String`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`firstTwo("Swift")` deve restituire `"Sw"`

```swift
tryCatch(firstTwo("Swift") == "Sw")
```

`firstTwo("a")` deve restituire `"a"`

```swift
tryCatch(firstTwo("a") == "a")
```

`lastTwo("Swift")` deve restituire `"ft"`

```swift
tryCatch(lastTwo("Swift") == "ft")
```

`lastTwo("")` deve restituire `""`

```swift
tryCatch(lastTwo("") == "")
```

`firstTwo` deve restituire una `String`

```swift
tryCatch(type(of: firstTwo("Swift")) == String.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstTwo(_ text: String) -> String {
    return String(text.prefix(2))
}

func lastTwo(_ text: String) -> String {
    return String(text.suffix(2))
}
```
