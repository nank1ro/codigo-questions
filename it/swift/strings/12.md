---
language: swift
exerciseType: 2
---

# --description--

`uppercased()` e `lowercased()` restituiscono una **nuova** stringa con ogni lettera convertita in maiuscolo o minuscolo. La stringa originale non viene modificata:
```swift
let name = "Swift"
print(name.uppercased()) // SWIFT
print(name.lowercased()) // swift
print(name)              // Swift
```
Entrambi sono metodi, quindi non dimenticare le parentesi.

# --instructions--

Stampa `quiet please` in maiuscolo, poi stampa `QUIET` in minuscolo

# --seed--

```swift
let shout = "quiet please".[/]()
print(shout)
print("QUIET".[/]())
```

# --answers--

- uppercased
- lowercased
- uppercase
- lowercase
- capitalized

# --solutions--

```swift
let shout = "quiet please".uppercased()
print(shout)
print("QUIET".lowercased())
```

# --output--

QUIET PLEASE
quiet
