---
language: swift
exerciseType: 4
---

# --description--

Un `Character` non è una `String`, quindi non può essere unito direttamente a una stringa con `+`. Convertilo prima con `String(...)`:
```swift
let letter: Character = "a"
let text = String(letter) + "bc" // abc
```
Combinare questo con un ciclo `for`-`in` ti permette di ricostruire una stringa un carattere alla volta, ad esempio mettendo ogni nuovo carattere davanti a quelli raccolti finora.

# --instructions--

Ordina gli elementi per stampare `swift` invertito, costruendo il risultato un carattere alla volta.
Dichiara `word` prima di `reversed`

# --answers--

```swift
let word = "swift"
```

```swift
var reversed = ""
```

```swift
for letter in word {
```

```swift
    reversed = String(letter) + reversed
```

```swift
}
```

```swift
print(reversed)
```

# --solutions--

```swift
let word = "swift"
var reversed = ""
for letter in word {
    reversed = String(letter) + reversed
}
print(reversed)
```

# --output--

tfiws
