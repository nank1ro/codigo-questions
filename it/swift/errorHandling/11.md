---
language: swift
exerciseType: 1
---

# --description--

Quando una funzione lancia un errore, tutto ciò che viene dopo il `throw` viene saltato — inclusa la riga che avrebbe dovuto chiudere il file o rilasciare il lock. `defer` risolve il problema: registra un blocco ora e lo esegue quando lo scope corrente finisce, in qualunque modo finisca.
```swift
func load() throws {
    print("open")
    defer { print("close") }
    throw FileError.missing
}
```
La chiamata stampa `open`, poi `close`, e solo allora l'errore prosegue verso il chiamante. Se la funzione fosse restituita normalmente, `close` sarebbe comunque stato stampato — questo è il punto. Metti la pulizia subito accanto alla configurazione e smetti di preoccuparti di quale uscita prende il codice.

# --instructions--

Aggiungi un blocco `defer` dentro `run` che aggiunga `"cleanup"` a `log`, così avviene sia quando `run` restituisce normalmente sia quando lancia un errore

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum TaskError: Error {
    case failed
}

func steps(_ shouldFail: Bool) -> [String] {
    var log: [String] = []
    func run() throws {
        log.append("start")
        
        if shouldFail {
            throw TaskError.failed
        }
        log.append("done")
    }
    do {
        try run()
    } catch {
        log.append("caught")
    }
    return log
}
```

# --asserts--

sul percorso riuscito il log deve essere `start`, `done`, `cleanup`

```swift
let _ok: [String] = steps(false)
tryCatch(_ok == ["start", "done", "cleanup"])
```

sul percorso con errore il log deve essere `start`, `cleanup`, `caught`

```swift
let _bad: [String] = steps(true)
tryCatch(_bad == ["start", "cleanup", "caught"])
```

la pulizia deve essere eseguita su entrambi i percorsi

```swift
tryCatch(steps(false).contains("cleanup") && steps(true).contains("cleanup"))
```

la pulizia deve essere eseguita esattamente una volta sul percorso con errore

```swift
let _once: [String] = steps(true)
tryCatch(_once.filter { $0 == "cleanup" }.count == 1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum TaskError: Error {
    case failed
}

func steps(_ shouldFail: Bool) -> [String] {
    var log: [String] = []
    func run() throws {
        log.append("start")
        defer { log.append("cleanup") }
        if shouldFail {
            throw TaskError.failed
        }
        log.append("done")
    }
    do {
        try run()
    } catch {
        log.append("caught")
    }
    return log
}
```
