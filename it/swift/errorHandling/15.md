---
language: swift
exerciseType: 4
---

# --description--

Dove si trova il blocco `do` decide quanto costa un singolo fallimento. Mettilo **dentro** il ciclo e ogni elemento ottiene il proprio tentativo, così un valore sbagliato viene saltato e il resto continua comunque:
```swift
for age in [4, -1, 7] {
    do {
        print(try label(age))
    } catch {
        print("skipped")
    }
}
```
Avvolgere invece l'intero ciclo in un unico `do` si fermerebbe al primo errore e non raggiungerebbe mai `7`. Nessuna delle due è sbagliata — è la differenza tra *un elemento sbagliato* e *arrendersi*.

# --instructions--

Ordina gli elementi per stampare le righe qui sotto, dichiarando prima il tipo di errore e la funzione prima del ciclo
```
age 4
skipped
age 7
```

# --answers--

```swift
enum AgeError: Error {
    case negative
}
```

```swift
func label(_ age: Int) throws -> String {
    if age < 0 {
        throw AgeError.negative
    }
    return "age \(age)"
}
```

```swift
for age in [4, -1, 7] {
    do {
```

```swift
        print(try label(age))
```

```swift
    } catch {
        print("skipped")
    }
}
```

# --solutions--

```swift
enum AgeError: Error {
    case negative
}
func label(_ age: Int) throws -> String {
    if age < 0 {
        throw AgeError.negative
    }
    return "age \(age)"
}
for age in [4, -1, 7] {
    do {
        print(try label(age))
    } catch {
        print("skipped")
    }
}
```

# --output--

age 4
skipped
age 7
