---
language: swift
exerciseType: 1
---

# --description--

Controllare `nil` e poi scompattare forzatamente è prolisso. Swift offre il **binding opzionale** con `if let`, che scompatta l'opzionale e salva il valore in una nuova costante in un solo passaggio:
```swift
let score: Int? = 10
if let value = score {
    print("Score: \(value)") // value è un Int, non un Int?
} else {
    print("No score")
}
```
Il corpo dell'`if` viene eseguito solo quando l'opzionale contiene un valore; al suo interno `value` è un semplice `Int` e non serve `!`.

# --instructions--

Scrivi una funzione `describe` che accetta un `Int?` (ometti la label dell'argomento) e restituisce `"Value: "` seguito dal numero (ad esempio `"Value: 5"` per `5`) quando l'opzionale contiene un valore, oppure `"No value"` quando è `nil`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`describe(5)` deve restituire `"Value: 5"`

```swift
tryCatch(describe(5) == "Value: 5")
```

`describe(42)` deve restituire `"Value: 42"`

```swift
tryCatch(describe(42) == "Value: 42")
```

`describe(nil)` deve restituire `"No value"`

```swift
tryCatch(describe(nil) == "No value")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func describe(_ number: Int?) -> String {
    if let value = number {
        return "Value: \(value)"
    } else {
        return "No value"
    }
}
```
