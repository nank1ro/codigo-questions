---
language: swift
exerciseType: 4
---

# --description--

Legare più opzionali in un unico `if let` mantiene il codice piatto: un solo ramo `else` copre ogni valore assente.

# --instructions--

Ordina gli elementi per stampare `Area: 20` quando sia `width` sia `height` hanno un valore, e `Missing size` altrimenti.
Dichiara `width` prima di `height`

# --answers--

```swift
let width: Int? = 4
```

```swift
let height: Int? = 5
```

```swift
if let w = width, let h = height {
```

```swift
    print("Area: \(w * h)")
```

```swift
} else {
    print("Missing size")
}
```

# --solutions--

```swift
let width: Int? = 4
let height: Int? = 5
if let w = width, let h = height {
    print("Area: \(w * h)")
} else {
    print("Missing size")
}
```

# --output--

Area: 20
