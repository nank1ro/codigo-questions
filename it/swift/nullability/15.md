---
language: swift
exerciseType: 1
---

# --description--

Molte operazioni possono fallire, e Swift segnala il fallimento restituendo un opzionale.
Convertire del testo in un numero è l'esempio classico: `Int("42")` restituisce un `Int?` che contiene `42`, mentre `Int("abc")` restituisce `nil`.
Anche `Int("3.5")` è `nil`, perché il testo non è un numero intero; usa `Double("3.5")` per i decimali.
```swift
let typed = "42"
if let number = Int(typed) {
    print(number + 1) // 43
}
```
Altri esempi sono `array.first` (`nil` per un array vuoto) e `dictionary[key]` (`nil` quando la chiave manca).

# --instructions--

Scrivi una funzione `parseAge` che accetta una `String` (ometti la label dell'argomento) e la restituisce convertita in `Int`, oppure `-1` quando il testo non è un numero intero valido

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`parseAge("42")` deve restituire `42`

```swift
tryCatch(parseAge("42") == 42)
```

`parseAge("abc")` deve restituire `-1`

```swift
tryCatch(parseAge("abc") == -1)
```

`parseAge("3.5")` deve restituire `-1`

```swift
tryCatch(parseAge("3.5") == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func parseAge(_ text: String) -> Int {
    return Int(text) ?? -1
}
```
