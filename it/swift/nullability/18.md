---
language: swift
exerciseType: 1
---

# --description--

A volte vuoi trasformare il valore all'interno di un opzionale e mantenere il risultato opzionale, senza scompattare e reimpacchettare a mano.
Gli opzionali hanno un metodo `map`: applica la closure al valore se ce n'è uno, e restituisce `nil` altrimenti.
```swift
let score: Int? = 10
let doubled = score.map { $0 * 2 } // Int? che contiene 20
let missing: Int? = nil
let stillMissing = missing.map { $0 * 2 } // nil
```
Combinato con una conversione che può fallire crea una pipeline compatta: `Int(text).map { $0 + 1 }`.

# --instructions--

Scrivi una funzione `squareText` che accetta una `String` (ometti la label dell'argomento), la converte in `Int` e restituisce il suo quadrato come `Int?`, oppure `nil` quando la conversione fallisce.
Usa `map`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`squareText("4")` deve restituire `16`

```swift
tryCatch(squareText("4") == 16)
```

`squareText("12")` deve restituire `144`

```swift
tryCatch(squareText("12") == 144)
```

`squareText("x")` deve restituire `nil`

```swift
tryCatch(squareText("x") == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func squareText(_ text: String) -> Int? {
    return Int(text).map { $0 * $0 }
}
```
