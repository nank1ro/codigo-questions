---
language: swift
exerciseType: 1
---

# --description--

La funzione `type(of:)` restituisce il tipo di un valore, utile per controllare cosa ha dedotto Swift:
```swift
print(type(of: 42))    // Int
print(type(of: 2.5))   // Double
print(type(of: "hi"))  // String
```
Quando vuoi un tipo diverso da quello dedotto, aggiungi un'annotazione. Un literal numero intero può essere memorizzato in una costante `Double` o `Float`, e un literal di un solo carattere in una costante `Character`:
```swift
let ratio: Double = 3       // 3.0, not an Int
let half: Float = 0.5
let initial: Character = "S"
print(type(of: ratio))      // Double
```

# --instructions--

Dichiara tre costanti:
- `ratio`, un `Double` che contiene il literal numero intero `3`
- `half`, un `Float` che contiene `0.5`
- `initial`, un `Character` che contiene `"S"`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`ratio` deve essere un `Double` uguale a `3.0`

```swift
tryCatch(type(of: ratio) == Double.self && ratio == 3.0)
```

`half` deve essere un `Float` uguale a `0.5`

```swift
tryCatch(type(of: half) == Float.self && half == 0.5)
```

`initial` deve essere un `Character` uguale a `"S"`

```swift
tryCatch(type(of: initial) == Character.self && initial == "S")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let ratio: Double = 3
let half: Float = 0.5
let initial: Character = "S"
```
