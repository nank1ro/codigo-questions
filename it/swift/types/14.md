---
language: swift
exerciseType: 1
---

# --description--

Poiché `Int(text)` e `Double(text)` restituiscono `nil` in caso di fallimento, confrontare il risultato con `nil` ti dice se un testo è un numero di quel tipo:
```swift
print(Int("42") != nil)     // true
print(Double("4.2") != nil) // true
print(Double("42") != nil)  // true, a whole number is also a valid Double
```
Nota l'ultima riga: ogni testo accettato da `Int` è accettato anche da `Double`, quindi controlla prima `Int` quando vuoi distinguerli.

# --instructions--

Scrivi una funzione `classify` che accetta una `String` (ometti la label dell'argomento) e restituisce `"Int"` se il testo è un numero intero valido, `"Double"` se è un numero decimale valido, e `"String"` altrimenti

Esempio di chiamata della funzione:
```swift
print(classify("3.5"))
// prints Double
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`classify("42")` deve restituire `"Int"`

```swift
tryCatch(classify("42") == "Int")
```

`classify("-7")` deve restituire `"Int"`

```swift
tryCatch(classify("-7") == "Int")
```

`classify("3.5")` deve restituire `"Double"`

```swift
tryCatch(classify("3.5") == "Double")
```

`classify("hello")` deve restituire `"String"`

```swift
tryCatch(classify("hello") == "String")
```

`classify("")` deve restituire `"String"`

```swift
tryCatch(classify("") == "String")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func classify(_ text: String) -> String {
    if Int(text) != nil {
        return "Int"
    }
    if Double(text) != nil {
        return "Double"
    }
    return "String"
}
```
