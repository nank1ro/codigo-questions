---
language: swift
exerciseType: 1
---

# --description--

Quando una funzione deve restituire un risultato decimale calcolato da numeri interi, converti gli operandi in `Double` prima di dividere e dichiara il tipo di ritorno come `Double`:
```swift
func ratio(_ part: Int, _ total: Int) -> Double {
    return Double(part) / Double(total)
}
print(ratio(1, 4)) // 0.25
```
Ricorda che `count` di un array è a sua volta un `Int`, quindi richiede la stessa conversione.

# --instructions--

Scrivi una funzione `average` che accetta un array di `Int` (ometti la label dell'argomento) e restituisce la media dei suoi elementi come `Double`. L'array non è mai vuoto

Esempio di chiamata della funzione:
```swift
print(average([1, 2]))
// prints 1.5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`average([1, 2])` deve restituire `1.5`

```swift
tryCatch(average([1, 2]) == 1.5)
```

`average([3, 4, 5, 6])` deve restituire `4.5`

```swift
tryCatch(average([3, 4, 5, 6]) == 4.5)
```

`average([10])` deve restituire `10.0`

```swift
tryCatch(average([10]) == 10.0)
```

`average([1, 1, 2])` deve restituire un `Double`

```swift
tryCatch(type(of: average([1, 1, 2])) == Double.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func average(_ numbers: [Int]) -> Double {
    var sum = 0
    for number in numbers {
        sum += number
    }
    return Double(sum) / Double(numbers.count)
}
```
