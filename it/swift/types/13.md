---
language: swift
exerciseType: 2
---

# --description--

Come hai visto nelle lezioni sulle stringhe, iterare su una `String` ti dà un `Character` alla volta. Un `Character` non è una `String`, quindi per usarlo come testo lo converti con `String(c)`.
Quando il carattere è una cifra, la proprietà `wholeNumberValue` ti dà il suo valore numerico come `Int?`: è `nil` per i caratteri che non sono cifre.
```swift
for c in "a1" {
    print(c.wholeNumberValue)
}
// nil
// Optional(1)
```

# --instructions--

Completa il ciclo in modo che sommi le cifre della stringa, in modo che il programma stampi `8`

# --seed--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.[/] {
        sum [/] digit
    }
}
print(sum)
```

# --answers--

- wholeNumberValue
- +=
- asciiValue
- isNumber
- -=
- =

# --solutions--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.wholeNumberValue {
        sum += digit
    }
}
print(sum)
```

# --output--

8
