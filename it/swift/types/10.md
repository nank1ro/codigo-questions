---
language: swift
exerciseType: 4
---

# --description--

Un **alias di tipo** dà a un tipo esistente un nuovo nome, con la parola chiave `typealias`:
```swift
typealias Score = Int
let best: Score = 100
print(best + 1) // 101
```
`Score` e `Int` sono lo stesso tipo, quindi si mescolano liberamente. Un alias non aggiunge alcuna sicurezza: rende solo il codice più leggibile quando un tipo comune ha un significato specifico nel tuo programma.

# --instructions--

Ordina gli elementi per definire `Celsius` come alias di `Double` sulla prima riga, converti il punto di ebollizione dell'acqua in Fahrenheit e stampalo

# --answers--

```swift
typealias Celsius = Double
```

```swift
let boiling: Celsius = 100
```

```swift
let fahrenheit = boiling * 9 / 5 + 32
```

```swift
print(fahrenheit)
```

# --solutions--

```swift
typealias Celsius = Double
let boiling: Celsius = 100
let fahrenheit = boiling * 9 / 5 + 32
print(fahrenheit)
```

# --output--

212.0
