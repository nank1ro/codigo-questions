---
language: dart
exerciseType: 1
---

# --description--

Messi insieme, questi metodi sostituiscono la maggior parte dei cicli scritti a mano. Una pipeline di solito si legge in tre fasi: **seleziona** gli elementi con `where`, **trasformali** con `map`, poi **combinali** con `fold`:

```dart
final prices = [12, 40, 7];
final cheapTotal = prices.where((p) => p < 20).fold(0, (acc, p) => acc + p);
print(cheapTotal); // 19
```

Poiché `fold` sceglie il proprio valore iniziale, può anche concludere una catena con un tipo che non c'entra nulla con gli elementi, come una `String` cresciuta un pezzo alla volta:

```dart
final words = ['fig', 'kiwi'];
final firstLetters = words.fold('', (acc, w) => acc + w[0]);
print(firstLetters); // fk
```

Ogni fase resta breve e dice cosa fa, ed è questa la vera ragione per preferirli a un ciclo che fa tutte e tre le cose insieme.

# --instructions--

Scrivi una funzione `initials` che prende una `List<String>` `names`, ignora quelle vuote, e restituisce una `String` fatta della prima lettera di ogni nome rimanente, in maiuscolo e in ordine.

Esempio:
```dart
print(initials(['ada', '', 'linus'])); // AL
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`initials(['ada', '', 'linus'])` dovrebbe restituire `'AL'`

```dart
  test('test1', () {
    expect(initials(['ada', '', 'linus']), 'AL', reason: '--err-t1--');
  });
```

Le lettere dovrebbero essere in maiuscolo e in ordine, e i nomi già in maiuscolo dovrebbero restare come sono

```dart
  test('test2', () {
    expect(initials(['grace', 'Bob', 'ada']), 'GBA', reason: '--err-t2--');
    expect(initials(['x']), 'X', reason: '--err-t2--');
  });
```

`initials` dovrebbe restituire una `String` vuota quando non c'è alcun nome non vuoto

```dart
  test('test3', () {
    expect(initials(<String>[]), '', reason: '--err-t3--');
    expect(initials(['', '']), '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String initials(List<String> names) {
  return names
      .where((n) => n.isNotEmpty)
      .map((n) => n[0].toUpperCase())
      .fold('', (acc, letter) => acc + letter);
}
```
