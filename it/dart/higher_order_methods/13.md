---
language: dart
exerciseType: 1
---

# --description--

Ognuno di questi metodi restituisce un `Iterable`, e ogni `Iterable` ha di nuovo gli stessi metodi. È questo che permette di **incatenarli**: un'intera computazione si legge come una pipeline da sinistra a destra, con ogni passo che lavora su ciò che il precedente ha prodotto.

```dart
final words = ['kiwi', 'fig', 'banana', 'date'];
print(words.where((w) => w.length == 4).map((w) => w.toUpperCase()).toList());
// [KIWI, DATE]
```

Solo l'ultimo passo ha bisogno di `toList()`: chiamarla a metà costruirebbe una lista che nessuno conserva.

Il tipo cambia lungo la catena, e cambia anche ciò che la funzione successiva riceve: dopo `where` su una `List<String>` hai ancora stringhe, ma dopo `map((w) => w.length)` il passo successivo vede numeri.

Poiché ogni passo è lazy, l'ordine conta per il lavoro svolto, non solo per il risultato: filtrare prima con `where` significa che `map` viene chiamata su meno elementi.

# --instructions--

Scrivi una funzione `shortUpper` che prende una `List<String>` `words` e restituisce una nuova `List<String>` con le parole di al massimo `4` caratteri, in maiuscolo, nel loro ordine originale.

Esempio:
```dart
print(shortUpper(['kiwi', 'banana', 'fig'])); // [KIWI, FIG]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shortUpper(['kiwi', 'banana', 'fig'])` dovrebbe restituire `['KIWI', 'FIG']`

```dart
  test('test1', () {
    expect(shortUpper(['kiwi', 'banana', 'fig']), ['KIWI', 'FIG'],
        reason: '--err-t1--');
  });
```

Le parole di esattamente `4` caratteri dovrebbero essere mantenute, quelle più lunghe scartate

```dart
  test('test2', () {
    expect(shortUpper(['date', 'grape']), ['DATE'], reason: '--err-t2--');
    expect(shortUpper(['melon']), <String>[], reason: '--err-t2--');
  });
```

`shortUpper` dovrebbe restituire una `List<String>` e non deve modificare la lista che riceve

```dart
  test('test3', () {
    expect(shortUpper(['a']), isA<List<String>>(), reason: '--err-t3--');
    final original = ['fig', 'banana'];
    shortUpper(original);
    expect(original, ['fig', 'banana'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<String> shortUpper(List<String> words) {
  return words.where((w) => w.length <= 4).map((w) => w.toUpperCase()).toList();
}
```
