---
language: dart
exerciseType: 1
---

# --description--

`reduce` è il parente più snello di `fold`. Non prende alcun valore iniziale: il **primo elemento** è l'accumulatore di partenza, e la funzione viene eseguita per ogni elemento rimanente:

```dart
final numbers = [1, 2, 3, 4];
print(numbers.reduce((a, b) => a + b)); // 10
```

Poiché non c'è un valore iniziale, il risultato ha sempre lo **stesso tipo degli elementi**, e chiamare `reduce` su una collezione vuota lancia una `StateError`: non c'è un primo elemento da cui partire. `fold` non ha questo problema, ed è per questo che è la scelta predefinita più sicura.

`reduce` dà il meglio di sé quando cerchi un elemento tra molti, come il più grande:

```dart
print(numbers.reduce((a, b) => a > b ? a : b)); // 4
```

# --instructions--

Scrivi una funzione `largest` che prende una `List<int>` `numbers` non vuota e restituisce l'elemento più grande, usando `reduce`.

Esempio:
```dart
print(largest([3, 9, 4])); // 9
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest([3, 9, 4])` dovrebbe restituire `9`

```dart
  test('test1', () {
    expect(largest([3, 9, 4]), 9, reason: '--err-t1--');
  });
```

`largest` dovrebbe funzionare quando l'elemento più grande è il primo o l'ultimo, e su un singolo elemento

```dart
  test('test2', () {
    expect(largest([10, 2, 5]), 10, reason: '--err-t2--');
    expect(largest([1, 2, 8]), 8, reason: '--err-t2--');
    expect(largest([7]), 7, reason: '--err-t2--');
  });
```

`largest` dovrebbe funzionare con i numeri negativi e non deve modificare la lista che riceve

```dart
  test('test3', () {
    expect(largest([-4, -1, -9]), -1, reason: '--err-t3--');
    final original = [2, 5, 1];
    largest(original);
    expect(original, [2, 5, 1], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int largest(List<int> numbers) {
  return numbers.reduce((a, b) => a > b ? a : b);
}
```
