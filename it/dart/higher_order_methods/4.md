---
language: dart
exerciseType: 1
---

# --description--

Alcuni metodi di ordine superiore rispondono a una domanda sulla collezione invece di costruirne una nuova. Prendono un predicato e restituiscono un `bool`:

- `any` è `true` quando **almeno un** elemento soddisfa il predicato
- `every` è `true` quando **tutti** gli elementi lo soddisfano

```dart
final numbers = [1, 2, 3];
print(numbers.any((n) => n > 2));   // true
print(numbers.every((n) => n > 2)); // false
```

Entrambi si fermano appena la risposta è certa: `any` al primo elemento che corrisponde, `every` al primo che non corrisponde.

Su una collezione vuota `any` è `false` e `every` è `true`: non c'è alcun elemento che dimostri il primo, e nessuno che rompa il secondo.

# --instructions--

Scrivi due funzioni, entrambe con un parametro `List<int>` `numbers`:

- `hasNegative` restituisce `true` se almeno un elemento è minore di `0`
- `allEven` restituisce `true` se ogni elemento è pari

Esempio:
```dart
print(hasNegative([1, -2, 3])); // true
print(allEven([2, 4]));         // true
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasNegative([1, -2, 3])` dovrebbe essere `true` e `hasNegative([1, 2, 3])` dovrebbe essere `false`

```dart
  test('test1', () {
    expect(hasNegative([1, -2, 3]), true, reason: '--err-t1--');
    expect(hasNegative([1, 2, 3]), false, reason: '--err-t1--');
  });
```

`allEven([2, 4, 6])` dovrebbe essere `true` e `allEven([2, 5])` dovrebbe essere `false`

```dart
  test('test2', () {
    expect(allEven([2, 4, 6]), true, reason: '--err-t2--');
    expect(allEven([2, 5]), false, reason: '--err-t2--');
  });
```

Su una lista vuota `hasNegative` dovrebbe essere `false` e `allEven` dovrebbe essere `true`

```dart
  test('test3', () {
    expect(hasNegative(<int>[]), false, reason: '--err-t3--');
    expect(allEven(<int>[]), true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool hasNegative(List<int> numbers) {
  return numbers.any((n) => n < 0);
}

bool allEven(List<int> numbers) {
  return numbers.every((n) => n % 2 == 0);
}
```
