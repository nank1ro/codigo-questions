---
language: dart
exerciseType: 1
---

# --description--

Diversi metodi ti permettono di cercare all'interno di una stringa:

- `.contains(other)` restituisce `true` se `other` appare da qualche parte nella stringa
- `.startsWith(other)` e `.endsWith(other)` controllano l'inizio e la fine
- `.indexOf(other)` restituisce l'indice della prima occorrenza, o `-1` se non viene trovata

```dart
var file = 'report.pdf';
print(file.contains('port'));    // true
print(file.startsWith('rep'));   // true
print(file.endsWith('.txt'));    // false
print(file.indexOf('.'));        // 6
print(file.indexOf('x'));        // -1
```

Sono tutti case-sensitive: `'Dart'.contains('dart')` è `false`.

# --instructions--

Data la stringa `email`, memorizza in un `bool` chiamato `hasAt` se contiene il carattere `@`, in un `int` chiamato `atIndex` l'indice di quel carattere, e in un `bool` chiamato `isExample` se l'email termina con `example.com`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var email = 'ada@example.com';
```

# --after-seed--

```dart
  return [hasAt, atIndex, isExample];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasAt` dovrebbe essere `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`atIndex` dovrebbe essere `3`

```dart
  test('test2', () {
    expect(result()[1], 3, reason: '--err-t2--');
  });
```

`isExample` dovrebbe essere `true`

```dart
  test('test3', () {
    expect(result()[2], true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var email = 'ada@example.com';
bool hasAt = email.contains('@');
int atIndex = email.indexOf('@');
bool isExample = email.endsWith('example.com');
```
