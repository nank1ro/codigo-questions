---
language: dart
exerciseType: 4
---

# --description--

Il testo digitato da un utente spesso ha spazi extra intorno. Il metodo `.trim()` restituisce una copia della stringa senza gli spazi bianchi iniziali e finali (spazi, tab e a capo). `.trimLeft()` e `.trimRight()` li rimuovono solo da un lato:

```dart
var input = '   hello  ';
print('[${input.trim()}]');      // [hello]
print('[${input.trimLeft()}]');  // [hello  ]
```

# --instructions--

Ordina le righe di codice per rimuovere gli spazi che circondano `input` e stampare il testo pulito in maiuscolo.

# --answers--

```dart
void main() {
```

```dart
  var input = '  hello  ';
```

```dart
  var clean = input.trim();
```

```dart
  print(clean.toUpperCase());
```

```dart
}
```

# --solutions--

```dart
void main() {
  var input = '  hello  ';
  var clean = input.trim();
  print(clean.toUpperCase());
}
```

# --output--

HELLO
