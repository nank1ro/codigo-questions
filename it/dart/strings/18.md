---
language: dart
exerciseType: 1
---

# --description--

I metodi delle stringhe restituiscono stringhe, quindi possono essere **concatenati** uno dopo l'altro. Combinato con `.split('')`, la proprietà `.reversed` delle liste e `.join()`, questo ti permette di invertire una stringa in un'unica espressione:

```dart
var text = 'Dart';
print(text.split('').reversed.join()); // traD
print(text.toLowerCase().replaceAll('a', '4')); // d4rt
```

Un **palindromo** è un testo che si legge allo stesso modo in avanti e indietro, come `level`.

# --instructions--

Scrivi una funzione `isPalindrome` che prende un parametro `String` chiamato `text` e restituisce `true` se è un palindromo, ignorando maiuscole/minuscole e spazi, altrimenti `false`.

Esempio:
```dart
print(isPalindrome('Level'));            // true
print(isPalindrome('never odd or even')); // true
print(isPalindrome('Dart'));             // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isPalindrome('Level')` deve restituire `true`

```dart
  test('test1', () {
    expect(isPalindrome('Level'), true, reason: '--err-t1--');
  });
```

`isPalindrome('never odd or even')` deve restituire `true`

```dart
  test('test2', () {
    expect(isPalindrome('never odd or even'), true, reason: '--err-t2--');
  });
```

`isPalindrome('Dart')` deve restituire `false`

```dart
  test('test3', () {
    expect(isPalindrome('Dart'), false, reason: '--err-t3--');
  });
```

`isPalindrome('Race car')` deve restituire `true`

```dart
  test('test4', () {
    expect(isPalindrome('Race car'), true, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isPalindrome(String text) {
  var clean = text.toLowerCase().replaceAll(' ', '');
  return clean == clean.split('').reversed.join();
}
```
