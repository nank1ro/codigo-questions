---
language: dart
exerciseType: 1
---

# --description--

Il metodo `.substring(start, end)` restituisce la parte di una stringa dall'indice `start` fino, **senza includerlo**, all'indice `end`. Se ometti `end`, prende tutto fino alla fine della stringa:

```dart
var text = 'Hello Dart';
print(text.substring(0, 5));  // Hello
print(text.substring(6));     // Dart
```

# --instructions--

Data la stringa `date` nel formato `YYYY-MM-DD`, memorizza in una `String` chiamata `year` i primi quattro caratteri e in una `String` chiamata `day` gli ultimi due caratteri.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var date = '2024-06-15';
```

# --after-seed--

```dart
  return [year, day];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`year` dovrebbe essere `'2024'`

```dart
  test('test1', () {
    expect(result()[0], '2024', reason: '--err-t1--');
  });
```

`day` dovrebbe essere `'15'`

```dart
  test('test2', () {
    expect(result()[1], '15', reason: '--err-t2--');
  });
```

`year` dovrebbe essere una `String`

```dart
  test('test3', () {
    expect(result()[0], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var date = '2024-06-15';
String year = date.substring(0, 4);
String day = date.substring(8);
```
