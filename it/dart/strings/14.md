---
language: dart
exerciseType: 1
---

# --description--

Sotto il cofano, ogni carattere di una stringa è memorizzato come un numero, il suo **code unit** (un codice UTF-16). `.codeUnitAt(index)` restituisce il codice di un carattere e `.codeUnits` l'intera lista. `String.fromCharCode(code)` fa il contrario, costruendo una stringa a partire da un codice:

```dart
var word = 'AB';
print(word.codeUnitAt(0));         // 65
print(word.codeUnits);             // [65, 66]
print(String.fromCharCode(67));    // C
```

Lettere consecutive hanno codici consecutivi: `'A'` è 65, `'B'` è 66, e così via.

# --instructions--

Data la stringa `letter`, memorizza in un `int` chiamato `code` il suo code unit, e in una `String` chiamata `next` il carattere il cui codice è `code + 1`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var letter = 'D';
```

# --after-seed--

```dart
  return [code, next];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`code` dovrebbe essere `68`

```dart
  test('test1', () {
    expect(result()[0], 68, reason: '--err-t1--');
  });
```

`next` dovrebbe essere `'E'`

```dart
  test('test2', () {
    expect(result()[1], 'E', reason: '--err-t2--');
  });
```

`next` dovrebbe essere una `String`

```dart
  test('test3', () {
    expect(result()[1], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var letter = 'D';
int code = letter.codeUnitAt(0);
String next = String.fromCharCode(code + 1);
```
