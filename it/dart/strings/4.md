---
language: dart
exerciseType: 1
---

# --description--

Ogni stringa conosce quanti caratteri contiene tramite la sua proprietà `.length`. Anche gli spazi e la punteggiatura contano come caratteri:

```dart
var word = 'hello';
print(word.length);      // 5
print('a b'.length);     // 3
```

Puoi leggere un singolo carattere con le parentesi quadre e il suo **indice**, a partire da `0`. Il risultato è una `String` di un solo carattere:

```dart
print(word[0]);                // h
print(word[word.length - 1]);  // o
```

Leggere un indice fuori dalla stringa (come `word[5]`) genera un errore.

# --instructions--

Data la stringa `word`, memorizza in un `int` chiamato `size` il suo numero di caratteri, in una `String` chiamata `first` il suo primo carattere, e in una `String` chiamata `last` il suo ultimo carattere.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var word = 'Flutter';
```

# --after-seed--

```dart
  return [size, first, last];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`size` dovrebbe essere `7`

```dart
  test('test1', () {
    expect(result()[0], 7, reason: '--err-t1--');
  });
```

`first` dovrebbe essere `'F'`

```dart
  test('test2', () {
    expect(result()[1], 'F', reason: '--err-t2--');
  });
```

`last` dovrebbe essere `'r'`

```dart
  test('test3', () {
    expect(result()[2], 'r', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var word = 'Flutter';
int size = word.length;
String first = word[0];
String last = word[word.length - 1];
```
