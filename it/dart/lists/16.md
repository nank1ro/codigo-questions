---
language: dart
exerciseType: 1
---

# --description--

Il metodo `.indexOf(value)` restituisce l'indice del primo elemento uguale a `value`. Se il valore non è presente nella lista, restituisce `-1`.

```dart
var colors = ['red', 'green', 'blue'];
print(colors.indexOf('green')); // 1
print(colors.indexOf('pink')); // -1
```

# --instructions--

Data la lista `['apple', 'banana', 'cherry', 'date']`, usa `.indexOf()` per trovare l'indice di `'cherry'`. Restituisci l'indice.

# --before-seed--

```dart
int computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

L'indice di 'cherry' è 2

```dart
  test('indexOf cherry is 2', () {
    expect(computeValue(), 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var fruits = ['apple', 'banana', 'cherry', 'date'];
  int result = fruits.indexOf('cherry');
```
