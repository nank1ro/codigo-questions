---
language: dart
exerciseType: 2
---

# --description--

Le liste in Dart sono **indicizzate a partire da zero**, il che significa che il primo elemento si trova all'indice `0`, il secondo all'indice `1`, e così via.

```dart
var colors = ['red', 'green', 'blue'];
print(colors[0]); // red
print(colors[2]); // blue
```

L'accesso a un elemento tramite indice avviene con la sintassi `list[index]`.

# --instructions--

Completa il codice per stampare il secondo elemento della lista.

# --seed--

```dart
var animals = ['cat', 'dog', 'bird'];
print(animals[/]);
```

# --answers--

- [1]
- [0]
- [2]
- .get(1)
- (1)
- [3]

# --solutions--

```dart
var animals = ['cat', 'dog', 'bird'];
print(animals[1]);
```

# --output--

dog
