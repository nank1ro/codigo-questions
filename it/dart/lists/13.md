---
language: dart
exerciseType: 1
---

# --instructions--

Data la lista `[100, 200, 300, 400, 500]`, restituisci una `List<int>` dove l'indice `0` è l'elemento `.first` e l'indice `1` è l'elemento `.last`.

# --before-seed--

```dart
List<int> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Il primo elemento è 100

```dart
  test('first is 100', () {
    expect(computeValue()[0], 100, reason: '--err-t1--');
  });
```

L'ultimo elemento è 500

```dart
  test('last is 500', () {
    expect(computeValue()[1], 500, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var nums = [100, 200, 300, 400, 500];
  List<int> result = [nums.first, nums.last];
```
