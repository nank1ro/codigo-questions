---
language: dart
exerciseType: 1
---

# --instructions--

Data la lista `[5, 10, 15, 20]`, restituisci `true` se contiene `15`, altrimenti restituisci `false`. Usa `.contains()`.

# --before-seed--

```dart
bool computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Restituisce true per 15

```dart
  test('contains 15', () {
    expect(computeValue(), true, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var numbers = [5, 10, 15, 20];
  bool result = numbers.contains(15);
```
