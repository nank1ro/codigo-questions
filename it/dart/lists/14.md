---
language: dart
exerciseType: 1
---

# --instructions--

Crea una `List<int>` vuota. Restituisci `true` se è `.isEmpty`, poi aggiungi il valore `1` e restituisci `true` se è `.isNotEmpty`. Restituisci entrambi i booleani come `List<bool>`.

# --before-seed--

```dart
List<bool> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Il primo valore è true (era vuota)

```dart
  test('first is true', () {
    expect(computeValue()[0], true, reason: '--err-t1--');
  });
```

Il secondo valore è true (non è vuota dopo l'aggiunta)

```dart
  test('second is true', () {
    expect(computeValue()[1], true, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<int> nums = [];
  bool wasEmpty = nums.isEmpty;
  nums.add(1);
  bool nowNotEmpty = nums.isNotEmpty;
  List<bool> result = [wasEmpty, nowNotEmpty];
```
