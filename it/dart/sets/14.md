---
language: dart
exerciseType: 1
---

# --instructions--

Data la lista `numbers`, memorizza in un `int` chiamato `uniqueCount` quanti valori diversi contiene, e in una `List<int>` chiamata `unique` quei valori senza duplicati, nell'ordine della loro prima comparsa.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var numbers = [4, 8, 4, 15, 8, 16, 4];
```

# --after-seed--

```dart
  return [uniqueCount, unique];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`uniqueCount` dovrebbe essere `4`

```dart
  test('test1', () {
    expect(result()[0], 4, reason: '--err-t1--');
  });
```

`unique` dovrebbe essere `[4, 8, 15, 16]`

```dart
  test('test2', () {
    expect(result()[1], [4, 8, 15, 16], reason: '--err-t2--');
  });
```

`unique` dovrebbe essere una `List`

```dart
  test('test3', () {
    expect(result()[1], isA<List>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var numbers = [4, 8, 4, 15, 8, 16, 4];
int uniqueCount = numbers.toSet().length;
List<int> unique = numbers.toSet().toList();
```
