---
language: dart
exerciseType: 1
---

# --description--

Il metodo `.add(value)` inserisce un singolo valore. Restituisce `true` se il valore è stato aggiunto e `false` se era già presente nel Set, nel qual caso non cambia nulla. Il metodo `.addAll(iterable)` inserisce ogni elemento di una lista o di un altro Set, saltando anch'esso quelli già presenti:

```dart
var tags = {'dart'};
tags.add('web');    // true
tags.add('dart');   // false, already there
tags.addAll(['web', 'mobile']);
print(tags); // {dart, web, mobile}
```

Un letterale `{}` vuoto è una **mappa**, non un Set. Per creare un Set vuoto, assegnagli un tipo:

```dart
var empty = <String>{};
Set<int> other = {};
```

# --instructions--

Dato il Set `tags`, aggiungi `'web'` con `.add()`, poi aggiungi tutti gli elementi della lista `['web', 'mobile']` con `.addAll()`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var tags = {'dart'};
```

# --after-seed--

```dart
  return tags;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`tags` dovrebbe essere `{'dart', 'web', 'mobile'}`

```dart
  test('test1', () {
    expect(result(), {'dart', 'web', 'mobile'}, reason: '--err-t1--');
  });
```

`tags` dovrebbe avere 3 elementi

```dart
  test('test2', () {
    expect(result().length, 3, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var tags = {'dart'};
tags.add('web');
tags.addAll(['web', 'mobile']);
```
