---
language: dart
exerciseType: 2
---

# --description--

Il metodo `.remove(value)` elimina un valore dal Set. Restituisce `true` se il valore era presente e `false` in caso contrario:

```dart
var numbers = {1, 2, 3};
print(numbers.remove(2)); // true
print(numbers.remove(9)); // false
print(numbers); // {1, 3}
```

Per eliminare tutti gli elementi in una volta, usa `.clear()`.

# --instructions--

Completa il codice per rimuovere il numero `2` dal Set, e poi stampa il Set.

# --seed--

```dart
var numbers = {1, 2, 3};
numbers.[/](2);
print(numbers);
```

# --answers--

- remove
- removeAt
- delete
- drop
- clear
- pop

# --solutions--

```dart
var numbers = {1, 2, 3};
numbers.remove(2);
print(numbers);
```

# --output--

{1, 3}
