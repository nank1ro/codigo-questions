---
language: dart
exerciseType: 1
---

# --description--

I Set supportano le classiche operazioni sugli insiemi. Ognuna restituisce un **nuovo** Set e lascia inalterati gli originali:

- `a.union(b)` contiene gli elementi che sono in `a` **o** in `b`
- `a.intersection(b)` contiene gli elementi che sono presenti **sia** in `a` **che** in `b`
- `a.difference(b)` contiene gli elementi di `a` che **non** sono in `b`

```dart
var a = {1, 2, 3};
var b = {2, 3, 4};
print(a.union(b));        // {1, 2, 3, 4}
print(a.intersection(b)); // {2, 3}
print(a.difference(b));   // {1}
```

# --instructions--

Dati i Set `a` e `b`, crea un Set chiamato `both` con la loro unione, un Set chiamato `common` con la loro intersezione, e un Set chiamato `onlyA` con gli elementi di `a` che non sono in `b`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var a = {1, 2, 3, 4};
var b = {3, 4, 5};
```

# --after-seed--

```dart
  return [both, common, onlyA];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`both` dovrebbe essere `{1, 2, 3, 4, 5}`

```dart
  test('test1', () {
    expect(result()[0], {1, 2, 3, 4, 5}, reason: '--err-t1--');
  });
```

`common` dovrebbe essere `{3, 4}`

```dart
  test('test2', () {
    expect(result()[1], {3, 4}, reason: '--err-t2--');
  });
```

`onlyA` dovrebbe essere `{1, 2}`

```dart
  test('test3', () {
    expect(result()[2], {1, 2}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var a = {1, 2, 3, 4};
var b = {3, 4, 5};
var both = a.union(b);
var common = a.intersection(b);
var onlyA = a.difference(b);
```
