---
language: dart
exerciseType: 1
---

# --description--

Convertire una lista in un Set è il modo più semplice per **rimuovere i duplicati**: ogni lista ha un metodo `.toSet()` che restituisce un Set con i suoi elementi unici, nell'ordine della loro prima comparsa. Un Set ha un metodo `.toList()` che fa il percorso inverso, quindi concatenare i due ti dà una lista senza duplicati:

```dart
var votes = ['a', 'b', 'a', 'c', 'b'];
Set<String> unique = votes.toSet();
print(unique); // {a, b, c}
List<String> cleaned = votes.toSet().toList();
print(cleaned); // [a, b, c]
```

# --instructions--

Data la lista `votes`, crea una `List<String>` chiamata `cleaned` che contenga i suoi elementi senza duplicati, nell'ordine della loro prima comparsa.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var votes = ['yes', 'no', 'yes', 'maybe', 'no', 'yes'];
```

# --after-seed--

```dart
  return cleaned;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`cleaned` dovrebbe essere `['yes', 'no', 'maybe']`

```dart
  test('test1', () {
    expect(result(), ['yes', 'no', 'maybe'], reason: '--err-t1--');
  });
```

`cleaned` dovrebbe essere una `List`

```dart
  test('test2', () {
    expect(result(), isA<List>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var votes = ['yes', 'no', 'yes', 'maybe', 'no', 'yes'];
List<String> cleaned = votes.toSet().toList();
```
