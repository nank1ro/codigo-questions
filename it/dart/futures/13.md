---
language: dart
exerciseType: 1
---

# --description--

Non serve `Future.wait` per far girare due future insieme. Una funzione `async` inizia a essere eseguita appena viene **chiamata**, fino al suo primo `await`; il future che ricevi è il lavoro già in corso. Il trucco quindi è: **prima chiama, poi attendi**:

```dart
Future<int> pages() => Future.delayed(const Duration(milliseconds: 20), () => 300);
Future<int> words() => Future.delayed(const Duration(milliseconds: 20), () => 90000);

Future<double> average() async {
  final p = pages();          // both timers start now
  final w = words();
  return await w / await p;   // waits once, about 20 ms in total
}
```

Confronta con `return await words() / await pages();`, dove `pages()` viene chiamata solo dopo che `words()` si è completata: stesso risultato, il doppio del tempo. Preferisci la forma concorrente ogni volta che la seconda chiamata non ha bisogno del risultato della prima.

# --instructions--

La funzione `slow(value)` restituisce un `Future<int>` che si completa con `value` dopo un breve ritardo, e registra `start <value>` quando viene chiamata e `end <value>` quando si completa nella lista `calls`. Scrivi una funzione `async` `addConcurrently` che prende due `int` `a` e `b`, chiama `slow(a)` e `slow(b)` in modo che girino **insieme**, e restituisce un `Future<int>` che si completa con la somma dei due risultati.

Esempio:
```dart
print(await addConcurrently(2, 3)); // 5
print(calls); // [start 2, start 3, end 2, end 3]
```

# --before-seed--

```dart
final calls = <String>[];

Future<int> slow(int value) async {
  calls.add('start $value');
  await Future.delayed(const Duration(milliseconds: 5));
  calls.add('end $value');
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`addConcurrently(2, 3)` dovrebbe completarsi con `5`

```dart
  test('test1', () async {
    calls.clear();
    expect(await addConcurrently(2, 3), 5, reason: '--err-t1--');
  });
```

`addConcurrently(2, 3)` dovrebbe chiamare `slow(2)` e `slow(3)` prima che una delle due si completi, quindi `calls` dovrebbe essere `[start 2, start 3, end 2, end 3]`

```dart
  test('test2', () async {
    calls.clear();
    await addConcurrently(2, 3);
    expect(calls, ['start 2', 'start 3', 'end 2', 'end 3'], reason: '--err-t2--');
  });
```

`addConcurrently(10, -4)` dovrebbe completarsi con `6` ed entrambe le chiamate dovrebbero partire prima che una delle due si completi

```dart
  test('test3', () async {
    calls.clear();
    expect(await addConcurrently(10, -4), 6, reason: '--err-t3--');
    expect(calls, ['start 10', 'start -4', 'end 10', 'end -4'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> addConcurrently(int a, int b) async {
  final first = slow(a);
  final second = slow(b);
  return await first + await second;
}
```
