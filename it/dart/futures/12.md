---
language: dart
exerciseType: 4
---

# --description--

`Future.wait` è lo strumento per "carica più cose, poi continua". La forma tipica è: costruire la lista dei future, farci `await Future.wait` sopra, poi usare la lista risultante:

```dart
Future<int> stock() => Future.delayed(const Duration(milliseconds: 10), () => 8);
Future<int> orders() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<void> main() async {
  final counts = await Future.wait([stock(), orders()]);
  print(counts); // [8, 3]
}
```

`orders()` si completa per prima, ma la lista segue comunque l'ordine delle chiamate: `stock()` per prima, `orders()` per seconda.

# --instructions--

Ordina gli elementi per definire una funzione `one` che si completa con `1` dopo `10` millisecondi, una funzione `two` che si completa con `2` dopo `5` millisecondi, e una `main` `async` che stampa `loading`, attende entrambe le funzioni con `Future.wait` in `values` e stampa `values`. Definisci prima `one`, poi `two`, poi `main`. Il programma deve stampare:

```
loading
[1, 2]
```

# --answers--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
```

```dart
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);
```

```dart
Future<void> main() async {
```

```dart
  print('loading');
```

```dart
  final values = await Future.wait([one(), two()]);
```

```dart
  print(values);
```

```dart
}
```

# --solutions--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);

Future<void> main() async {
  print('loading');
  final values = await Future.wait([one(), two()]);
  print(values);
}
```

# --output--

loading
[1, 2]
