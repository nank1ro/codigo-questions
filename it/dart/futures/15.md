---
language: dart
exerciseType: 3
---

# --description--

`Future.wait` segue la stessa regola: se **uno qualsiasi** dei future fallisce, il future combinato si completa con quell'errore e `await Future.wait(...)` lancia. Non ottieni mai una lista parziale dei valori riusciti. Per tenere gli altri, gestisci l'errore dentro ogni singolo future, per esempio con `catchError`, prima di passarlo a `Future.wait`.

```dart
Future<int> ok() => Future.value(1);
Future<int> bad() async => throw StateError('nope');

Future<void> main() async {
  try {
    await Future.wait([ok(), bad()]);
  } catch (e) {
    print('failed: $e'); // failed: Bad state: nope
  }
}
```

# --instructions--

Uno dei future passati a `Future.wait` si completa con un errore. Cosa fa `await Future.wait([...])`?

# --answers--

- Lancia quell'errore, quindi l'`await` deve stare dentro un `try`/`catch` per gestirlo
- Restituisce una lista in cui il future fallito è sostituito da `null`
- Restituisce una lista con i soli valori dei future che hanno avuto successo
- Non si completa mai, perché il future fallito viene ritentato all'infinito

# --solutions--

- Lancia quell'errore, quindi l'`await` deve stare dentro un `try`/`catch` per gestirlo
