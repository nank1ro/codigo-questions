---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara due variabili `int` di livello superiore: `x` con il valore `25` e `y` con il valore `9`. Poi dichiara una variabile `int` di livello superiore denominata `diff` che memorizza il risultato della sottrazione di `y` da `x`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`diff` deve essere uguale a `16`

```dart
  test('test1', () {
    expect(diff, 16, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 25;
int y = 9;
int diff = x - y;

void main() {
  print(diff);
}
```
