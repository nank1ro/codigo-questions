---
language: dart
exerciseType: 2
---

# --description--

L'**operatore di divisione** `/` divide un valore per un altro. In Dart, `/` **restituisce sempre un `double`**, anche quando il risultato è un numero intero.

```dart
int a = 10;
int b = 4;
double result = a / b; // 2.5

double exact = 10 / 2; // 5.0  (not 5!)
```

Usa sempre una variabile `double` per memorizzare il risultato di `/`.

# --instructions--

Completa il codice per dividere `9` per `4` e memorizzare il risultato in `result`.

# --seed--

```dart
void main() {
  double result = 9 [/] 4;
  print(result);
}
```

# --answers--

- /
- *
- +
- -

# --solutions--

```dart
void main() {
  double result = 9 / 4;
  print(result);
}
```

# --output--

2.25
