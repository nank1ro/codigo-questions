---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara due variabili `int` di livello superiore: `a` con il valore `12` e `b` con il valore `8`. Poi dichiara una terza variabile `int` di livello superiore denominata `total` che memorizza la loro somma.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`a` deve essere `12`, `b` deve essere `8` e `total` deve essere `20`

```dart
  test('test1', () {
    expect(a, 12, reason: '--err-t1--');
    expect(b, 8, reason: '--err-t2--');
    expect(total, 20, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 12;
int b = 8;
int total = a + b;

void main() {
  print(total);
}
```
