---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara una variabile `double` di livello superiore denominata `result` che memorizza il risultato della divisione di `7` per `2`. Ricorda che `/` in Dart restituisce sempre un `double`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` deve essere un `double` con il valore `3.5`

```dart
  test('test1', () {
    expect(result, isA<double>(), reason: '--err-t1--');
    expect(result, closeTo(3.5, 0.001), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double result = 7 / 2;

void main() {
  print(result);
}
```
