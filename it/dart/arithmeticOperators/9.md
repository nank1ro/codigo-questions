---
language: dart
exerciseType: 1
---

# --instructions--

Usa l'operatore modulo `%` per trovare il resto quando `17` viene diviso per `5`. Memorizza il risultato in una variabile di livello superiore denominata `remainder`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

La variabile `remainder` deve essere uguale a `2`

```dart
  test('test1', () {
    expect(remainder, 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 17 % 5;

void main() {
  print(remainder);
}
```
