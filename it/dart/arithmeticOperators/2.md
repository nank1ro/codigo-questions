---
language: dart
exerciseType: 2
---

# --description--

L'**operatore di sottrazione** `-` sottrae un valore da un altro.

```dart
int a = 10;
int b = 4;
int diff = a - b; // 6
```

Il risultato può essere negativo se il secondo operando è più grande del primo.

# --instructions--

Completa il codice per sottrarre `9` da `20` e memorizzare il risultato in `diff`.

# --seed--

```dart
void main() {
  int diff = 20 [/] 9;
  print(diff);
}
```

# --answers--

- -
- +
- *
- /

# --solutions--

```dart
void main() {
  int diff = 20 - 9;
  print(diff);
}
```

# --output--

11
