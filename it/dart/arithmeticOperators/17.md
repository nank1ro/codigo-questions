---
language: dart
exerciseType: 1
---

# --instructions--

Un negozio vende `itemCount` articoli a `pricePerItem` dollari ciascuno, con un importo di `discount` dollari sottratto dal totale. Dichiara queste tre variabili `int` di livello superiore: `itemCount` = `6`, `pricePerItem` = `15`, `discount` = `10`. Poi dichiara una variabile `int` di livello superiore denominata `totalPrice` che calcola il totale dopo lo sconto.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`totalPrice` deve essere uguale a `80`

```dart
  test('test1', () {
    expect(totalPrice, 80, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int itemCount = 6;
int pricePerItem = 15;
int discount = 10;
int totalPrice = itemCount * pricePerItem - discount;

void main() {
  print(totalPrice);
}
```
