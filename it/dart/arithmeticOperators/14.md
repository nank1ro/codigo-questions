---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara due variabili `int` di livello superiore: `total` con il valore `29` e `perGroup` con il valore `4`. Poi dichiara una variabile `int` di livello superiore denominata `groups` che memorizza quanti gruppi completi di `4` si adattano a `29` (usa divisione intera `~/`).

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`groups` deve essere uguale a `7`

```dart
  test('test1', () {
    expect(groups, 7, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 29;
int perGroup = 4;
int groups = total ~/ perGroup;

void main() {
  print(groups);
}
```
