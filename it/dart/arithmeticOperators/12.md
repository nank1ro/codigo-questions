---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara due variabili `int` di livello superiore: `width` con il valore `7` e `height` con il valore `9`. Poi dichiara una variabile `int` di livello superiore denominata `area` che memorizza il loro prodotto.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`area` deve essere uguale a `63`

```dart
  test('test1', () {
    expect(area, 63, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int width = 7;
int height = 9;
int area = width * height;

void main() {
  print(area);
}
```
