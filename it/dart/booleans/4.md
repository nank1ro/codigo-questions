---
language: dart
exerciseType: 2
---

# --description--

L'__operatore di negazione__ `!` (anche detto "not") inverte un valore booleano.
Applicare `!` a `true` dà `false`, e applicare `!` a `false` dà `true`.

```dart
bool isActive = true;
print(!isActive); // false
```

Questo è utile quando vuoi verificare l'opposto di una condizione.

# --instructions--

Data la variabile `isBlocked` impostata a `false`, stampa il suo valore negato usando `!`.

# --seed--

```dart
bool isBlocked = false;
print([/]isBlocked);
```

# --answers--

- !
- !!
- ~
- not

# --solutions--

```dart
bool isBlocked = false;
print(!isBlocked);
```

# --output--

true
