---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara una variabile booleana chiamata `flag` con il valore `true`, quindi dichiara un'altra variabile booleana chiamata `isFlag` e usa la parola chiave `is` per verificare se `flag` è di tipo `bool`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`flag` deve essere uguale a `true`.

```dart
  test('test1', () {
    expect(flag, true, reason: '--err-t1--');
  });
```

`isFlag` deve essere `true` (il risultato del controllo `is bool`).

```dart
  test('test2', () {
    expect(isFlag, true, reason: '--err-t2--');
  });
```

`isFlag` deve essere di tipo `bool`.

```dart
  test('test3', () {
    expect(isFlag, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool flag = true;
bool isFlag = flag is bool;
```
