---
language: dart
exerciseType: 1
---

# --instructions--

L'operatore `&&` (AND) restituisce `true` solo quando **entrambi** gli operandi sono `true`.

Dichiara una variabile booleana chiamata `result` che combina `true && true` usando l'operatore `&&`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` deve essere uguale a `true`.

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result` deve essere di tipo `bool`.

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = true && true;
```
