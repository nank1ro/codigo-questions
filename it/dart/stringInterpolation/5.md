---
language: dart
exerciseType: 3
---

# --description--

Se ciò che segue il segno `$` non è riconoscibile come un identificatore di programma, Dart genererà un errore di compilazione: il `$` in una stringa deve sempre essere seguito da un identificatore o da un'espressione tra parentesi graffe (`${}`).

# --instructions--

Cosa succede quando provi a eseguire questo codice?

```dart
void main() {
    print("$5 amici");
}
```

# --answers--

- Non si compila perché `$5` è un'interpolazione di stringa non valida
- Stampa `$5 amici`
- Stampa `5 amici`
- Non stampa nulla

# --solutions--

- Non si compila perché `$5` è un'interpolazione di stringa non valida
