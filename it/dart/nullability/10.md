---
language: dart
exerciseType: 3
---

# --description--

La promozione di tipo **non** funziona su un **campo** di una classe che può essere modificato dall'esterno, perché tra il controllo e l'uso un altro pezzo di codice (un getter ridefinito in una sottoclasse, un altro metodo) potrebbe reimpostarlo a `null`:

```dart
class Box {
  int? value;

  int doubled() {
    if (value != null) {
      return value * 2; // error: 'value' can't be unconditionally accessed
    }
    return 0;
  }
}
```

La soluzione standard è copiare il campo in una **variabile locale**, che invece viene promossa:

```dart
int doubled() {
  final v = value;
  if (v != null) {
    return v * 2;
  }
  return 0;
}
```

# --instructions--

Perché la prima versione di `doubled` nella descrizione non compila?

# --answers--

- `value` è un campo, quindi il controllo del null non lo promuove: copialo prima in una variabile locale
- I valori `int?` non possono essere moltiplicati, anche quando non sono `null`
- All'istruzione `if` manca un ramo `else`
- I campi di una classe non possono avere un tipo nullable

# --solutions--

- `value` è un campo, quindi il controllo del null non lo promuove: copialo prima in una variabile locale
