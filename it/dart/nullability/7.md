---
language: dart
exerciseType: 1
---

# --description--

A volte sei **tu** a sapere che un valore nullable non è `null` in un certo punto, anche se il compilatore non può saperlo. L'operatore di **asserzione non null** `!` trasforma una `String?` in una `String` promettendo che il valore è presente:

```dart
String? text = 'Dart';
String sure = text!;
print(sure.length); // 4
```

Fai attenzione: `!` sposta il controllo dal tempo di compilazione al tempo di esecuzione. Se il valore **è** `null`, il programma genera un errore e si ferma:

```dart
String? text;
print(text!.length); // Null check operator used on a null value
```

Usa `!` con parsimonia, e solo quando un `null` lì sarebbe comunque un bug.

# --instructions--

Scrivi una funzione `shout` che prende un parametro `String` nullable `text` e lo restituisce in maiuscolo seguito da `!`. Usa l'operatore `!`, così che la funzione generi un errore quando `text` è `null`.

Esempio:
```dart
print(shout('hello')); // HELLO!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shout('hello')` deve essere `'HELLO!'`

```dart
  test('test1', () {
    expect(shout('hello'), 'HELLO!', reason: '--err-t1--');
  });
```

`shout('Dart')` deve essere `'DART!'`

```dart
  test('test2', () {
    expect(shout('Dart'), 'DART!', reason: '--err-t2--');
  });
```

`shout(null)` deve lanciare un errore

```dart
  test('test3', () {
    expect(() => shout(null), throwsA(anything), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String shout(String? text) {
  return '${text!.toUpperCase()}!';
}
```
