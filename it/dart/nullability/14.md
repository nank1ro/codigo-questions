---
language: dart
exerciseType: 1
---

# --description--

Molte funzioni delle librerie usano `null` per segnalare che qualcosa **non ha potuto essere fatto**. Convertire una stringa in un numero è l'esempio classico: `int.parse` lancia una `FormatException` quando il testo non è un numero, mentre `int.tryParse` restituisce `null` e ti lascia decidere cosa fare:

```dart
print(int.tryParse('42'));  // 42
print(int.tryParse('4x2')); // null
print(int.tryParse(''));    // null
```

Il tipo di ritorno di `int.tryParse` è `int?`, quindi tutto ciò che hai imparato si applica: `??` per un valore predefinito, `?.` per concatenare, e un controllo `if` per promuovere. `double.tryParse` funziona allo stesso modo.

# --instructions--

Scrivi una funzione `parseOrZero` che prende un parametro `String` `text` e restituisce l'intero che contiene, oppure `0` quando il testo non è un intero valido. Usa `int.tryParse`.

Esempio:
```dart
print(parseOrZero('42'));  // 42
print(parseOrZero('abc')); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parseOrZero('42')` deve essere `42`

```dart
  test('test1', () {
    expect(parseOrZero('42'), 42, reason: '--err-t1--');
  });
```

`parseOrZero('-5')` deve essere `-5`

```dart
  test('test2', () {
    expect(parseOrZero('-5'), -5, reason: '--err-t2--');
  });
```

`parseOrZero('abc')` e `parseOrZero('')` devono essere `0`

```dart
  test('test3', () {
    expect(parseOrZero('abc'), 0, reason: '--err-t3--');
    expect(parseOrZero(''), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int parseOrZero(String text) {
  return int.tryParse(text) ?? 0;
}
```
