---
language: dart
exerciseType: 1
---

# --description--

Una variabile nullable dichiarata **senza un valore** inizia come `null`, quindi `= null` può essere omesso:

```dart
int? age;
print(age); // null
```

Una variabile non nullable non ha tale valore predefinito: Dart rifiuta di compilare qualunque codice che la legga prima che le sia stato assegnato un valore.

```dart
int count;
print(count); // error: 'count' must be assigned before it can be used
```

# --instructions--

Dichiara una variabile `String` nullable `email` senza un valore, e una variabile `String` `username` con il valore `'ada'`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart

```

# --after-seed--

```dart
  return [email, username];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`email` deve essere `null`

```dart
  test('test1', () {
    expect(result()[0], isNull, reason: '--err-t1--');
  });
```

`username` deve essere `'ada'`

```dart
  test('test2', () {
    expect(result()[1], 'ada', reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String? email;
String username = 'ada';
```
