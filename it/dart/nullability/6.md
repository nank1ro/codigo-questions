---
language: dart
exerciseType: 2
---

# --description--

L'operatore di **assegnazione if-null** `??=` assegna un valore a una variabile **solo se** quella variabile è attualmente `null`; altrimenti la lascia invariata:

```dart
int? retries;
retries ??= 3;
print(retries); // 3

retries ??= 10;
print(retries); // 3, it already had a value
```

Funziona anche sulle voci di una mappa, che sono nullable perché una chiave potrebbe mancare:

```dart
var stock = {'apple': 4};
stock['pear'] ??= 1;  // added
stock['apple'] ??= 9; // ignored
print(stock); // {apple: 4, pear: 1}
```

# --instructions--

Completa il codice in modo che il tema salvato e la dimensione del carattere esistente siano mantenuti, mentre il margine mancante viene aggiunto. Il programma deve stampare `dark` e poi `{font: 12, margin: 8}`.

# --seed--

```dart
String? savedTheme() => 'dark';

void main() {
  String? theme = savedTheme();
  var sizes = {'font': 12};
  theme [/] 'light';
  sizes['font'] [/] 14;
  sizes['margin'] ??= 8;
  print(theme);
  print(sizes);
}
```

# --answers--

- ??=
- ??=
- =
- ?=
- =??
- :=

# --solutions--

```dart
String? savedTheme() => 'dark';

void main() {
  String? theme = savedTheme();
  var sizes = {'font': 12};
  theme ??= 'light';
  sizes['font'] ??= 14;
  sizes['margin'] ??= 8;
  print(theme);
  print(sizes);
}
```

# --output--

dark
{font: 12, margin: 8}
