---
language: dart
exerciseType: 4
---

# --description--

Altri due operatori hanno una variante null-aware.

La **cascata null-aware** `?..` esegue una catena di operazioni a cascata solo quando l'oggetto non è `null`, e le salta tutte altrimenti:

```dart
List<int>? numbers;
numbers?..add(1)..add(2); // nothing happens, numbers is still null
```

Lo **spread null-aware** `...?` inserisce gli elementi di una collezione nullable in un letterale, senza aggiungere nulla quando la collezione è `null`:

```dart
List<int>? extra;
print([0, ...?extra]); // [0]

extra = [1, 2];
print([0, ...?extra]); // [0, 1, 2]
```

Senza il `?`, `...extra` su una `List<int>?` sarebbe un errore di compilazione.

# --instructions--

Ordina gli elementi per definire una funzione `fill` che aggiunge `1` e `2` a una lista nullable con `?..`, poi usala in `main`. Definisci prima la funzione, poi `main`. Il programma deve stampare:

```
[0]
[1, 2, 3]
```

# --answers--

```dart
void fill(List<int>? target) {
```

```dart
  target?..add(1)..add(2);
```

```dart
}
```

```dart
void main() {
```

```dart
  List<int>? numbers;
```

```dart
  print([0, ...?numbers]);
```

```dart
  numbers = [];
```

```dart
  fill(numbers);
```

```dart
  print([...?numbers, 3]);
```

```dart
}
```

# --solutions--

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  print([0, ...?numbers]);
  numbers = [];
  fill(numbers);
  print([...?numbers, 3]);
}
```

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  numbers = [];
  print([0, ...?numbers]);
  fill(numbers);
  print([...?numbers, 3]);
}
```

# --output--

[0]
[1, 2, 3]
