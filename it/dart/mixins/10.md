---
language: dart
exerciseType: 1
---

# --description--

Il corpo della classe si trova proprio alla fine della catena, quindi un membro dichiarato nella classe fa **override** dello stesso membro proveniente da qualunque suo mixin. All'interno dell'override, **`super`** raggiunge la versione fornita dal mixin:

```dart
mixin Polite {
  String greet() => 'Hello';
}

class Host with Polite {
  @override
  String greet() => '${super.greet()}, welcome!';
}

class Guest with Polite {}

void main() {
  print(Host().greet());  // Hello, welcome!
  print(Guest().greet()); // Hello
}
```

Il mixin stesso rimane intatto: `Guest` ottiene comunque il `greet` originale. `super.greet()` è ciò che permette a `Host` di costruire sul comportamento condiviso invece di copiarlo.

# --instructions--

Dichiara un mixin `Signed` con un metodo `sign` che restituisce `'signed'`.

Dichiara una classe `Letter` che utilizza `Signed` e fa override di `sign` così che restituisca il risultato del mixin seguito da `' by Ada'`, ottenuto chiamando `super`.

Dichiara una classe `Note` che utilizza `Signed` e non fa override di nulla.

Esempio:
```dart
print(Letter().sign()); // signed by Ada
print(Note().sign());   // signed
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Letter().sign()` deve essere `'signed by Ada'`

```dart
  test('test1', () {
    expect(Letter().sign(), 'signed by Ada', reason: '--err-t1--');
  });
```

`Note().sign()` deve essere ancora `'signed'`, perché il mixin è invariato

```dart
  test('test2', () {
    expect(Note().sign(), 'signed', reason: '--err-t2--');
  });
```

Entrambe le classi devono utilizzare il mixin, e l'override deve essere usato anche attraverso il tipo del mixin

```dart
  test('test3', () {
    final Signed letter = Letter();
    expect(letter.sign(), 'signed by Ada', reason: '--err-t3--');
    expect(Note(), isA<Signed>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Signed {
  String sign() => 'signed';
}

class Letter with Signed {
  @override
  String sign() => '${super.sign()} by Ada';
}

class Note with Signed {}
```
