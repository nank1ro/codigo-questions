---
language: dart
exerciseType: 1
---

# --description--

Il corpo di un mixin assomiglia al corpo di una classe: metodi, getter e campi, scritti esattamente allo stesso modo. La differenza sta in ciò che puoi fare con la dichiarazione stessa.

```dart
mixin Timestamped {
  String get stamp => '[log]';

  String format(String text) => '$stamp $text';
}

class Server with Timestamped {}
class Printer with Timestamped {}
```

Il nome di un mixin è anche un **tipo**, quindi `Server() is Timestamped` è `true` e una variabile può essere dichiarata come `Timestamped t = Server();`. Due classi non correlate condividono ora un'unica implementazione senza che alcuna delle due eredi dall'altra.

# --instructions--

Dichiara un mixin `Shouter` con un metodo `shout` che accetta una `String` `text` e la restituisce in maiuscolo seguita da un punto esclamativo.

Dichiara una classe `Speaker` che utilizza `Shouter` e non aggiunge nulla di proprio.

Esempio:
```dart
print(Speaker().shout('hi')); // HI!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Speaker().shout('hi')` deve essere `'HI!'`

```dart
  test('test1', () {
    expect(Speaker().shout('hi'), 'HI!', reason: '--err-t1--');
  });
```

`Speaker().shout('Dart rocks')` deve essere `'DART ROCKS!'` e `Speaker().shout('')` deve essere `'!'`

```dart
  test('test2', () {
    expect(Speaker().shout('Dart rocks'), 'DART ROCKS!', reason: '--err-t2--');
    expect(Speaker().shout(''), '!', reason: '--err-t2--');
  });
```

`Speaker` deve ottenere `shout` dal mixin, quindi un `Speaker` deve essere anche un `Shouter`

```dart
  test('test3', () {
    expect(Speaker(), isA<Shouter>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Shouter {
  String shout(String text) => '${text.toUpperCase()}!';
}

class Speaker with Shouter {}
```
