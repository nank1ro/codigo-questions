---
language: dart
exerciseType: 1
---

# --description--

Un mixin non è limitato ai metodi: può dichiarare anche **campi**, e ogni oggetto di ogni classe che utilizza il mixin ne ottiene una copia propria.

```dart
mixin Counter {
  int count = 0;

  void increment() {
    count++;
  }
}

class Clicker with Counter {}

void main() {
  final a = Clicker();
  final b = Clicker();
  a.increment();
  a.increment();
  print(a.count); // 2
  print(b.count); // 0, b has its own count
}
```

Questo è ciò che rende un mixin più di un'interfaccia: porta con sé sia i dati sia il codice che li elabora.

# --instructions--

Dichiara un mixin `Scored` con:
- un campo `int` `score` che parte da `0`
- un metodo `addPoints` che accetta un `int` `points` e lo aggiunge a `score`

Dichiara una classe `Player` che utilizza `Scored` e non aggiunge nulla di proprio.

Esempio:
```dart
final p = Player();
p.addPoints(3);
print(p.score); // 3
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Un nuovo `Player` deve avere un `score` di `0`, e `addPoints(3)` deve portarlo a `3`

```dart
  test('test1', () {
    final p = Player();
    expect(p.score, 0, reason: '--err-t1--');
    p.addPoints(3);
    expect(p.score, 3, reason: '--err-t1--');
  });
```

Le chiamate ad `addPoints` devono sommarsi

```dart
  test('test2', () {
    final p = Player();
    p.addPoints(3);
    p.addPoints(4);
    p.addPoints(10);
    expect(p.score, 17, reason: '--err-t2--');
  });
```

Ogni `Player` deve avere il proprio `score`, e un `Player` deve essere un `Scored`

```dart
  test('test3', () {
    final a = Player();
    final b = Player();
    a.addPoints(5);
    expect(a.score, 5, reason: '--err-t3--');
    expect(b.score, 0, reason: '--err-t3--');
    expect(a, isA<Scored>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Scored {
  int score = 0;

  void addPoints(int points) {
    score += points;
  }
}

class Player with Scored {}
```
