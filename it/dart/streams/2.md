---
language: dart
exerciseType: 1
---

# --description--

`Stream.fromIterable` ha bisogno di tutti i valori in anticipo. Per **produrre** i valori uno alla volta, scrivi un **generatore asincrono**: una funzione il cui corpo è marcato `async*` e il cui tipo di ritorno è `Stream<T>`. Al suo interno, `yield` invia un evento allo stream:

```dart
Stream<int> countTo(int n) async* {
  for (var i = 1; i <= n; i++) {
    yield i;
  }
}
```

Il corpo non viene eseguito quando chiami `countTo(3)`: viene eseguito pigramente, man mano che l'ascoltatore chiede valori, e lo stream è concluso quando il corpo termina.

Per raccogliere ogni evento in una `List`, chiama `toList()`. Restituisce un `Future<List<T>>`, quindi lo devi attendere con `await`:

```dart
final values = await countTo(3).toList();
print(values); // [1, 2, 3]
```

# --instructions--

Scrivi una funzione `async*` `countdown` che prende un `int` `from` e restituisce uno `Stream<int>` che emette i valori da `from` fino a `1`. Se `from` è `0` o minore, lo stream non emette nulla.

Esempio:
```dart
print(await countdown(3).toList()); // [3, 2, 1]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countdown(3)` dovrebbe emettere `3`, `2`, `1`

```dart
  test('test1', () async {
    expect(await countdown(3).toList(), [3, 2, 1], reason: '--err-t1--');
  });
```

`countdown(1)` dovrebbe emettere solo `1`

```dart
  test('test2', () async {
    expect(await countdown(1).toList(), [1], reason: '--err-t2--');
  });
```

`countdown(0)` non dovrebbe emettere nulla e `countdown` dovrebbe restituire uno `Stream<int>`

```dart
  test('test3', () async {
    expect(await countdown(0).toList(), <int>[], reason: '--err-t3--');
    expect(countdown(2), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> countdown(int from) async* {
  for (var i = from; i >= 1; i--) {
    yield i;
  }
}
```
