---
language: dart
exerciseType: 1
---

# --description--

Gli stream visti finora sono a **sottoscrizione singola**: permettono esattamente un ascoltatore. Chiamare `listen`, `await for` o un qualsiasi metodo di consumo una seconda volta lancia uno `StateError` ("Stream has already been listened to").

Per condividere uno stream tra più ascoltatori, convertilo in uno stream **broadcast** con `asBroadcastStream()`:

```dart
final shared = Stream.fromIterable([1, 2, 3]).asBroadcastStream();
final total = shared.reduce((a, b) => a + b);
final count = shared.length;
print(await total); // 6
print(await count); // 3
```

Uno stream broadcast non usa buffer: un ascoltatore riceve solo gli eventi emessi **dopo** la sua iscrizione. Nell'esempio entrambi gli ascoltatori si iscrivono prima del primo `await`, quindi entrambi ricevono ogni evento.

# --instructions--

Scrivi una funzione `sumAndCount` che prende uno `Stream<int>` `source` e restituisce un `Future<List<int>>` che si completa con una lista di due elementi: la somma dei valori e il numero di valori. Converti `source` in uno stream broadcast, avvia entrambe le chiamate di consumo **prima** di attenderne una qualsiasi, poi attendile. Lo stream ha sempre almeno un evento.

Esempio:
```dart
print(await sumAndCount(Stream.fromIterable([1, 2, 3]))); // [6, 3]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumAndCount` di uno stream di `1, 2, 3` dovrebbe completarsi con `[6, 3]`

```dart
  test('test1', () async {
    expect(await sumAndCount(Stream.fromIterable([1, 2, 3])), [6, 3], reason: '--err-t1--');
  });
```

`sumAndCount` di uno stream di `10, -4` dovrebbe completarsi con `[6, 2]`

```dart
  test('test2', () async {
    expect(await sumAndCount(Stream.fromIterable([10, -4])), [6, 2], reason: '--err-t2--');
  });
```

`sumAndCount` di uno stream di `7` dovrebbe completarsi con `[7, 1]`

```dart
  test('test3', () async {
    expect(await sumAndCount(Stream.fromIterable([7])), [7, 1], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<List<int>> sumAndCount(Stream<int> source) async {
  final shared = source.asBroadcastStream();
  final sum = shared.reduce((a, b) => a + b);
  final count = shared.length;
  return [await sum, await count];
}
```
