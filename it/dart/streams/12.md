---
language: dart
exerciseType: 4
---

# --description--

Uno `StreamController` viene spesso creato e consumato nello stesso posto: ti iscrivi a `controller.stream` con `listen`, poi aggiungi eventi con `add` e chiudi il controller con `close`. Poiché `listen` non aspetta, gli eventi vengono consegnati dopo che il codice corrente è terminato, ma sempre nell'ordine in cui sono stati aggiunti:

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>();
  controller.stream.listen((n) => print('got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
// got 1
// got 2
```

# --instructions--

Ordina gli elementi per importare `dart:async`, creare uno `StreamController<String>`, iscriverti al suo stream stampando ogni valore, aggiungere `hello` e poi `world`, e infine chiudere il controller. Iscriviti con `listen` prima di aggiungere qualsiasi valore. Il programma deve stampare:

```
hello
world
```

# --answers--

```dart
import 'dart:async';
```

```dart
void main() {
```

```dart
  final controller = StreamController<String>();
```

```dart
  controller.stream.listen((word) => print(word));
```

```dart
  controller.add('hello');
```

```dart
  controller.add('world');
```

```dart
  controller.close();
```

```dart
}
```

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>();
  controller.stream.listen((word) => print(word));
  controller.add('hello');
  controller.add('world');
  controller.close();
}
```

# --output--

hello
world
