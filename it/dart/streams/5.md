---
language: dart
exerciseType: 2
---

# --description--

`await for` mette in pausa la funzione corrente finché lo stream non è concluso. Quando vuoi reagire agli eventi **senza aspettare**, chiama `listen` e passa una callback: viene invocata una volta per evento, e il codice dopo `listen` viene eseguito subito.

```dart
final ticks = Stream.fromIterable([1, 2]);
ticks.listen((tick) => print('tick $tick'));
print('not waiting');
// not waiting
// tick 1
// tick 2
```

`listen` accetta anche un parametro con nome `onDone`, una funzione senza argomenti chiamata quando lo stream finisce:

```dart
final ticks2 = Stream.fromIterable([1, 2]);
ticks2.listen((tick) => print(tick), onDone: () => print('no more ticks'));
```

# --instructions--

Completa il codice in modo che lo stream venga ascoltato, ogni lettera venga stampata e `done` venga stampato quando lo stream finisce. Il programma deve stampare `listening`, poi `a`, `b` e `done`.

# --seed--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.[/]((letter) => print(letter), [/]: () => print('done'));
  print('listening');
}
```

# --answers--

- listen
- onDone
- onError
- forEach
- await

# --solutions--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.listen((letter) => print(letter), onDone: () => print('done'));
  print('listening');
}
```

# --output--

listening
a
b
done
