---
language: dart
exerciseType: 1
---

# --description--

Come `Iterable`, uno `Stream` ha metodi che costruiscono un **nuovo stream** a partire da uno esistente:

- `map` trasforma ogni evento
- `where` mantiene solo gli eventi che soddisfano una condizione
- `take` si ferma dopo un dato numero di eventi

```dart
final numbers = Stream.fromIterable([1, 2, 3, 4, 5, 6]);
final firstOdds = numbers.where((n) => n.isOdd).take(2);
print(await firstOdds.toList()); // [1, 3]
```

Questi metodi sono **pigri**: non viene eseguito nulla finché qualcuno non ascolta lo stream risultante. Possono essere concatenati, e lo stream di partenza non viene mai modificato.

# --instructions--

Scrivi una funzione `evenSquares` che prende uno `Stream<int>` `source` e restituisce uno `Stream<int>` contenente il quadrato di ogni valore **pari** di `source`, nello stesso ordine.

Esempio:
```dart
final source = Stream.fromIterable([1, 2, 3, 4]);
print(await evenSquares(source).toList()); // [4, 16]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`evenSquares` di uno stream di `1, 2, 3, 4` dovrebbe emettere `4, 16`

```dart
  test('test1', () async {
    expect(await evenSquares(Stream.fromIterable([1, 2, 3, 4])).toList(), [4, 16], reason: '--err-t1--');
  });
```

`evenSquares` di uno stream di `6, 5, 10` dovrebbe emettere `36, 100`

```dart
  test('test2', () async {
    expect(await evenSquares(Stream.fromIterable([6, 5, 10])).toList(), [36, 100], reason: '--err-t2--');
  });
```

`evenSquares` di uno stream di `1, 3` non dovrebbe emettere nulla e dovrebbe restituire uno `Stream<int>`

```dart
  test('test3', () async {
    expect(await evenSquares(Stream.fromIterable([1, 3])).toList(), <int>[], reason: '--err-t3--');
    expect(evenSquares(Stream.fromIterable([2])), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> evenSquares(Stream<int> source) {
  return source.where((n) => n.isEven).map((n) => n * n);
}
```
