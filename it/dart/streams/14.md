---
language: dart
exerciseType: 2
---

# --description--

Un controller può creare direttamente uno stream broadcast con il costruttore con nome `StreamController<T>.broadcast()`. Il suo `stream` accetta un numero qualsiasi di ascoltatori, e ogni evento viene consegnato a tutti loro, nell'ordine in cui si sono iscritti:

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>.broadcast();
  controller.stream.listen((msg) => print('first: $msg'));
  controller.stream.listen((msg) => print('second: $msg'));
  controller.add('hi');
  controller.close();
}
// first: hi
// second: hi
```

Come ogni stream broadcast, non usa buffer: gli eventi aggiunti prima che un ascoltatore si iscriva sono persi per quell'ascoltatore.

# --instructions--

Completa il codice per creare un controller broadcast, iscrivere due ascoltatori `A` e `B`, inviare i valori `1` e `2`, poi chiudere il controller, in modo che il programma stampi `A got 1`, `B got 1`, `A got 2`, `B got 2`.

# --seed--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.[/]();
  controller.stream.[/]((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.[/](1);
  controller.add(2);
  controller.[/]();
}
```

# --answers--

- broadcast
- listen
- add
- close
- asBroadcastStream
- subscribe
- sink

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.broadcast();
  controller.stream.listen((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
```

# --output--

A got 1
B got 1
A got 2
B got 2
