---
language: dart
exerciseType: 4
---

# --description--

Uno stream può trasportare **errori** oltre ai valori. Dentro un generatore `async*`, un `throw` invia un evento di errore e conclude lo stream; uno `StreamController` può inviarne uno con `addError`.

Dal lato di chi consuma, un ciclo `await for` rilancia l'errore nel punto in cui si trova il ciclo, quindi lo gestisci con un normale `try`/`catch` attorno al ciclo:

```dart
Stream<int> risky() async* {
  yield 1;
  throw StateError('sensor offline');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('caught: $e');
  }
}
// 1
// caught: Bad state: sensor offline
```

Con `listen`, passa invece una callback `onError`: `stream.listen(print, onError: (e) => print('caught: $e'));`

# --instructions--

Ordina gli elementi per definire una funzione `async*` `risky` che emette `1` e poi lancia una `FormatException('bad')`, e un `main` `async` che stampa ogni valore di `risky()` dentro un `try`/`catch`, stampa `error: ` seguito dall'errore quando lo cattura, e stampa `finished` dopo l'intero blocco `try`/`catch`, al suo esterno. Definisci `risky` prima di `main`. Il programma deve stampare:

```
1
error: FormatException: bad
finished
```

# --answers--

```dart
Stream<int> risky() async* {
```

```dart
  yield 1;
```

```dart
  throw FormatException('bad');
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  try {
```

```dart
    await for (final n in risky()) {
```

```dart
      print(n);
```

```dart
    }
```

```dart
  } catch (e) {
```

```dart
    print('error: $e');
```

```dart
  }
```

```dart
  print('finished');
```

```dart
}
```

# --solutions--

```dart
Stream<int> risky() async* {
  yield 1;
  throw FormatException('bad');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('error: $e');
  }
  print('finished');
}
```

# --output--

1
error: FormatException: bad
finished
