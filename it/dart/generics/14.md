---
language: dart
exerciseType: 1
---

# --description--

Una classe generica può avere costruttori denominati e **factory** come qualsiasi altra classe, e il parametro di tipo è disponibile al loro interno. Un costruttore factory non crea l'oggetto in prima persona: esegue un corpo e ne restituisce uno, il che gli permette di scegliere, riutilizzare o costruire l'istanza nel modo che preferisce.

```dart
class Box<T> {
  final T value;

  Box(this.value);

  factory Box.first(List<T> items) => Box(items.first);
}

final b = Box<int>.first([5, 6]);
print(b.value); // 5
```

L'argomento di tipo va sulla classe, non sul nome del costruttore: `Box<int>.first(...)`. Dentro la factory, `<T>[]` è una vera `List<T>` vuota, quindi una factory è il posto naturale per costruire un valore predefinito per un tipo che non conosci ancora.

# --instructions--

Dichiara una classe generica `Stack` con un parametro di tipo `T`, un campo `final` `items` di tipo `List<T>` impostato dal costruttore normale, un costruttore **factory** `Stack.empty()` che costruisce una `Stack` con una lista vuota, e un getter `top` di tipo `T?` che restituisce l'ultimo elemento, oppure `null` quando la stack è vuota.

Esempio:
```dart
print(Stack<int>([1, 2, 3]).top);  // 3
print(Stack<String>.empty().top);  // null
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Stack<int>([1, 2, 3]).top` deve essere `3`

```dart
  test('test1', () {
    final stack = Stack<int>([1, 2, 3]);
    expect(stack.top, 3, reason: '--err-t1--');
    expect(stack.items, [1, 2, 3], reason: '--err-t1--');
  });
```

`Stack.empty()` deve costruire una stack vuota del tipo giusto, il cui `top` è `null`

```dart
  test('test2', () {
    final stack = Stack<String>.empty();
    expect(stack, isA<Stack<String>>(), reason: '--err-t2--');
    expect(stack.items, <String>[], reason: '--err-t2--');
    expect(stack.top, null, reason: '--err-t2--');
  });
```

La lista costruita da `Stack.empty()` deve essere comunque utilizzabile

```dart
  test('test3', () {
    final stack = Stack<int>.empty();
    stack.items.add(4);
    stack.items.add(9);
    expect(stack.top, 9, reason: '--err-t3--');
    expect(Stack<bool>([true]).top, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Stack<T> {
  final List<T> items;

  Stack(this.items);

  factory Stack.empty() => Stack(<T>[]);

  T? get top => items.isEmpty ? null : items.last;
}
```
