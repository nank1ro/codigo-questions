---
language: dart
exerciseType: 1
---

# --description--

Un `typedef` dà un nome a un tipo, e può avere parametri di tipo propri. Il motivo usuale è nominare una volta sola una famiglia di tipi funzione invece di scriverla per esteso a ogni uso:

```dart
typedef Transform<I, O> = O Function(I input);

final Transform<String, int> length = (word) => word.length;
print(length('kiwi')); // 4
```

`Transform<String, int>` è solo un altro modo di scrivere `int Function(String)`, quindi i due sono intercambiabili. Il guadagno è la leggibilità: un parametro dichiarato come `Transform<I, O> transform` dice a cosa serve la funzione, mentre `O Function(I)` dice solo come è fatta.

Un typedef generico e una funzione generica si combinano naturalmente, con i parametri di tipo della funzione che riempiono quelli del typedef.

# --instructions--

Dichiara un typedef generico `Transform` con due parametri di tipo `I` e `O` che nomina una funzione che prende un `I` e restituisce un `O`.

Poi scrivi una funzione generica `convertAll` con parametri di tipo `I` e `O` che prende una `List<I>` `items` e una `Transform<I, O>` `transform`, e restituisce una `List<O>` contenente ogni elemento convertito, in ordine.

Esempio:
```dart
print(convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)); // [3, 4]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)` deve restituire `[3, 4]`

```dart
  test('test1', () {
    expect(
      convertAll<String, int>(['fig', 'kiwi'], (w) => w.length),
      [3, 4],
      reason: '--err-t1--',
    );
  });
```

`convertAll` deve restituire una `List<O>`, quindi convertire degli int in stringhe deve dare una `List<String>`

```dart
  test('test2', () {
    final result = convertAll<int, String>([1, 2], (n) => 'n$n');
    expect(result, ['n1', 'n2'], reason: '--err-t2--');
    expect(result, isA<List<String>>(), reason: '--err-t2--');
  });
```

Il typedef `Transform` deve nominare un tipo funzione, e una lista vuota deve dare una lista vuota

```dart
  test('test3', () {
    final Transform<int, int> twice = (n) => n * 2;
    expect(convertAll<int, int>([1, 2, 3], twice), [2, 4, 6], reason: '--err-t3--');
    expect(convertAll<String, int>(<String>[], (w) => w.length), <int>[], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
typedef Transform<I, O> = O Function(I input);

List<O> convertAll<I, O>(List<I> items, Transform<I, O> transform) {
  return items.map(transform).toList();
}
```
