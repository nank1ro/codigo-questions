---
language: dart
exerciseType: 1
---

# --description--

Una `T` senza vincolo potrebbe essere qualsiasi cosa, quindi dentro il corpo puoi usare solo ciò che ogni oggetto ha. Questo non compila:

```dart
T twice<T>(T value) => value + value; // error: + is not defined for T
```

Un **vincolo** risolve il problema. Scrivere `T extends num` dice che «`T` può essere solo un numero», e in cambio il corpo può usare tutto ciò che un `num` offre:

```dart
T twice<T extends num>(T value) => (value + value) as T;

num half<T extends num>(T value) => value / 2;
```

Il vincolo viene controllato nel punto di chiamata: `half(4)` e `half(2.5)` vanno bene, `half('fig')` è un errore di compilazione. Un vincolo è una promessa in entrambe le direzioni: argomenti più ristretti in cambio di più potenza dentro.

# --instructions--

Scrivi una funzione generica `total` con un parametro di tipo `T` vincolato a `num` che prende una `List<T>` `values` e restituisce la somma degli elementi come `num`. Una lista vuota dà `0`.

Esempio:
```dart
print(total([1, 2, 3]));  // 6
print(total([1.5, 2.5])); // 4.0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total([1, 2, 3])` deve restituire `6`

```dart
  test('test1', () {
    expect(total([1, 2, 3]), 6, reason: '--err-t1--');
  });
```

`total` deve funzionare anche con i double, quindi `total([1.5, 2.5])` deve restituire `4.0`, e deve accettare un argomento di tipo esplicito come `total<int>([1, 2])`

```dart
  test('test2', () {
    expect(total([1.5, 2.5]), 4.0, reason: '--err-t2--');
    expect(total([10, 20, 30, 40]), 100, reason: '--err-t2--');
    expect(total<int>([1, 2]), 3, reason: '--err-t2--');
    expect(total<double>([1.5, 1.5]), 3.0, reason: '--err-t2--');
  });
```

`total` deve restituire `0` per una lista vuota

```dart
  test('test3', () {
    expect(total(<int>[]), 0, reason: '--err-t3--');
    expect(total(<double>[]), 0, reason: '--err-t3--');
    expect(total([-5, 5, 7]), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
num total<T extends num>(List<T> values) {
  num sum = 0;
  for (final value in values) {
    sum += value;
  }
  return sum;
}
```
