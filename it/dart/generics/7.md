---
language: dart
exerciseType: 1
---

# --description--

Una classe può dichiarare più di un parametro di tipo, separati da virgole. `Map<K, V>` è l'esempio integrato: un tipo per le chiavi, uno per i valori.

```dart
class Entry<K, V> {
  final K key;
  final V value;

  Entry(this.key, this.value);
}

final e = Entry<String, int>('age', 30);
```

L'**ordine** fa parte del tipo: `Entry<String, int>` e `Entry<int, String>` sono tipi non correlati, e un valore di uno non può essere assegnato all'altro. I parametri di tipo possono anche essere riordinati in un tipo di ritorno, ed è così che un metodo può restituire una versione invertita dell'oggetto:

```dart
Entry<V, K> get flipped => Entry(value, key);
```

# --instructions--

Dichiara una classe generica `Pair` con due parametri di tipo `A` e `B`, un campo `final` `first` di tipo `A` e un campo `final` `second` di tipo `B`, entrambi impostati dal costruttore, e un getter `swapped` che restituisce una `Pair<B, A>` che contiene gli stessi due valori in ordine opposto.

Esempio:
```dart
final p = Pair<String, int>('age', 30);
print(p.first);         // age
print(p.swapped.first); // 30
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Pair<String, int>('age', 30)` deve avere `first` `'age'` e `second` `30`

```dart
  test('test1', () {
    final pair = Pair<String, int>('age', 30);
    expect(pair.first, 'age', reason: '--err-t1--');
    expect(pair.second, 30, reason: '--err-t1--');
  });
```

`swapped` deve restituire una `Pair<int, String>` con i valori scambiati

```dart
  test('test2', () {
    final swapped = Pair<String, int>('age', 30).swapped;
    expect(swapped, isA<Pair<int, String>>(), reason: '--err-t2--');
    expect(swapped.first, 30, reason: '--err-t2--');
    expect(swapped.second, 'age', reason: '--err-t2--');
  });
```

`Pair` deve funzionare con qualsiasi coppia di tipi, e scambiare due volte deve restituire i valori originali

```dart
  test('test3', () {
    final pair = Pair<bool, String>(true, 'yes');
    expect(pair.swapped.first, 'yes', reason: '--err-t3--');
    expect(pair.swapped.second, true, reason: '--err-t3--');
    expect(pair.swapped.swapped, isA<Pair<bool, String>>(), reason: '--err-t3--');
    expect(pair.swapped.swapped.first, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Pair<A, B> {
  final A first;
  final B second;

  Pair(this.first, this.second);

  Pair<B, A> get swapped => Pair(second, first);
}
```
