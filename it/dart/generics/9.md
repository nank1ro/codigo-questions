---
language: dart
exerciseType: 3
---

# --description--

La differenza conta appena usi il valore. Su una `Box<int?>` raggiungi il campo normalmente e poi gestisci il `null` al suo interno, mentre su una `Box<int>?` devi prima superare la scatola mancante:

```dart
Box<int?> a = Box(null);
print(a.value ?? 0); // 0, la scatola c’è, il suo contenuto è null

Box<int>? b = null;
print(b?.value ?? 0); // 0, manca proprio la scatola
```

Scrivere `b.value` su una `Box<int>?` non compila affatto: Dart si rifiuta di leggere un campo di qualcosa che potrebbe non esistere.

# --instructions--

Dati `Box<int?> a = Box(null);` e `Box<int>? b = null;`, quale affermazione è vera?

# --answers--

- `a` è una scatola reale il cui `value` è `null`, mentre `b` non è affatto una scatola
- Entrambe le dichiarazioni significano la stessa cosa, il `?` può essere scritto su entrambi i lati
- `a` non compila, perché un argomento di tipo non può mai essere nullable
- `b` è una scatola che contiene `null`, mentre `a` non è affatto una scatola

# --solutions--

- `a` è una scatola reale il cui `value` è `null`, mentre `b` non è affatto una scatola
