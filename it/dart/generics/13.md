---
language: dart
exerciseType: 4
---

# --description--

Lo stesso vincolo funziona altrettanto bene per l'elemento più piccolo: cambia solo il segno del confronto. `compareTo` restituisce un numero negativo quando il ricevente viene prima, quindi `item.compareTo(best) < 0` significa «questo è più piccolo».

# --instructions--

Ordina gli elementi per definire una funzione generica `smallest` vincolata a `Comparable<T>` che restituisce l'elemento più piccolo di una lista, e un `main` che stampi il più piccolo di `['pear', 'fig', 'kiwi']`. Definisci la funzione prima di `main`. Il programma deve stampare:

```
fig
```

# --answers--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
```

```dart
  var best = items.first;
```

```dart
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
```

```dart
  return best;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(smallest(['pear', 'fig', 'kiwi']));
```

```dart
}
```

# --solutions--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
  return best;
}

void main() {
  print(smallest(['pear', 'fig', 'kiwi']));
}
```

# --output--

fig
