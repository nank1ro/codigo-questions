---
language: dart
exerciseType: 2
---

# --description--

Con la null safety solida il punto interrogativo può trovarsi in due posti diversi, e significano due cose diverse:

```dart
Box<int?> a = Box(null); // a box that exists and holds a nullable int
Box<int>? b = null;      // no box at all, but if there is one it holds an int
```

In `Box<int?>` l'**argomento di tipo** è nullable, quindi `a.value` ha tipo `int?` e può essere `null`, mentre `a` in sé è sempre presente. In `Box<int>?` è la **variabile** a essere nullable, quindi `b` può essere `null` e ti serve `b?.value` o `b!.value` per raggiungere il suo interno.

Una `T` semplice significa `T extends Object?`, quindi un argomento di tipo nullable come `Box<int?>` è perfettamente legale.

# --instructions--

Completa il codice così che `holdsNull` sia una scatola che contiene `null`, e `missing` sia una variabile che può non contenere alcuna scatola.

# --seed--

```dart
class Box<T> {
  final T value;
  Box(this.value);
}

void main() {
  final holdsNull = Box<[/]>(null);
  Box<int>[/] missing = null;
  print('value: ${holdsNull.value}');
  print('box: $missing');
}
```

# --answers--

- int?
- ?
- int
- !
- ??

# --solutions--

```dart
class Box<T> {
  final T value;
  Box(this.value);
}

void main() {
  final holdsNull = Box<int?>(null);
  Box<int>? missing = null;
  print('value: ${holdsNull.value}');
  print('box: $missing');
}
```

# --output--

value: null
box: null
