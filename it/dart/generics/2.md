---
language: dart
exerciseType: 1
---

# --description--

`List` non è l'unica collezione generica. Una `Set` accetta un argomento di tipo, e una `Map` ne accetta **due**: uno per le chiavi e uno per i valori, in quest'ordine.

```dart
Set<String> tags = {'new', 'sale'};
Map<String, int> ages = {'Ada': 36, 'Grace': 45};
```

Un letterale di collezione vuoto non può essere dedotto dal suo contenuto, quindi scrivi gli argomenti di tipo sul letterale stesso:

```dart
final counts = <String, int>{};
final seen = <String>{};
counts['fig'] = 3;
```

Una volta che i tipi sono noti, tutto ciò che estrai dalla collezione ha già il tipo giusto: `ages['Ada']` è un `int?`, mai un valore misterioso.

# --instructions--

Scrivi una funzione `lengths` che prende una `List<String>` `words` e restituisce una `Map<String, int>` in cui ogni parola è una chiave e la sua lunghezza è il valore.

Esempio:
```dart
print(lengths(['fig', 'kiwi'])); // {fig: 3, kiwi: 4}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`lengths(['fig', 'kiwi'])` deve restituire `{fig: 3, kiwi: 4}`

```dart
  test('test1', () {
    expect(lengths(['fig', 'kiwi']), {'fig': 3, 'kiwi': 4}, reason: '--err-t1--');
  });
```

`lengths` deve restituire una `Map<String, int>`

```dart
  test('test2', () {
    expect(lengths(['plum']), isA<Map<String, int>>(), reason: '--err-t2--');
    expect(lengths(['plum']), {'plum': 4}, reason: '--err-t2--');
  });
```

`lengths` deve restituire una mappa vuota per una lista vuota

```dart
  test('test3', () {
    expect(lengths(<String>[]), <String, int>{}, reason: '--err-t3--');
    expect(lengths(['a', 'bb', 'ccc']), {'a': 1, 'bb': 2, 'ccc': 3}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Map<String, int> lengths(List<String> words) {
  final result = <String, int>{};
  for (final word in words) {
    result[word] = word.length;
  }
  return result;
}
```
