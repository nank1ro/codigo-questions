---
language: dart
exerciseType: 1
---

# --description--

Un vincolo può menzionare il parametro di tipo stesso. `Comparable<T>` è l'interfaccia di tutto ciò che sa confrontarsi con i propri simili, tramite `compareTo`:

```dart
print('fig'.compareTo('kiwi')); // negativo: fig viene prima
print('kiwi'.compareTo('fig')); // positive
print('fig'.compareTo('fig'));  // zero
```

Quindi `T extends Comparable<T>` si legge come «qualsiasi tipo che può essere confrontato con se stesso», che è esattamente ciò di cui ha bisogno una funzione di ordinamento o di massimo:

```dart
T maxOf<T extends Comparable<T>>(T a, T b) => a.compareTo(b) >= 0 ? a : b;

print(maxOf('fig', 'kiwi')); // kiwi
```

`String` e `DateTime` la soddisfano entrambe direttamente. `int` e `double` implementano `Comparable<num>`, quindi una lista di numeri viene semplicemente confrontata come `num`.

# --instructions--

Scrivi una funzione generica `largest` con un parametro di tipo `T` vincolato a `Comparable<T>` che prende una `List<T>` `items` non vuota e restituisce l'elemento più grande, confrontando con `compareTo`.

Esempio:
```dart
print(largest(['fig', 'kiwi', 'apple'])); // kiwi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest(['fig', 'kiwi', 'apple'])` deve restituire `'kiwi'`

```dart
  test('test1', () {
    expect(largest(['fig', 'kiwi', 'apple']), 'kiwi', reason: '--err-t1--');
  });
```

`largest` deve funzionare con i numeri e anche con le date

```dart
  test('test2', () {
    expect(largest([3, 9, 5]), 9, reason: '--err-t2--');
    expect(
      largest([DateTime(2020), DateTime(2024), DateTime(2001)]),
      DateTime(2024),
      reason: '--err-t2--',
    );
  });
```

`largest` deve restituire l'unico elemento di una lista con un solo elemento, l'ultimo quando è il più grande, e deve accettare un argomento di tipo esplicito come `largest<String>(['a', 'z'])`

```dart
  test('test3', () {
    expect(largest<String>(['a', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['only']), 'only', reason: '--err-t3--');
    expect(largest(['a', 'b', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['z', 'b', 'a']), 'z', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
T largest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) > 0) {
      best = item;
    }
  }
  return best;
}
```
