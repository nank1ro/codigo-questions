---
language: dart
exerciseType: 1
---

# --description--

Un **costruttore factory** usa la parola chiave `factory` e permette di controllare la creazione degli oggetti — ad esempio restituendo un'istanza memorizzata nella cache o un sottotipo:

```dart
class Logger {
  static final Logger _instance = Logger._internal();

  factory Logger() => _instance;

  Logger._internal();
}
```

Ogni chiamata a `Logger()` restituisce la stessa istanza (pattern singleton).

# --instructions--

Crea una classe `Counter` con un campo privato `_count` inizializzato a `0`. Aggiungi un metodo di istanza `increment()` che aumenta `_count` di `1`, e un getter `count` che restituisce `_count`. Usa un costruttore `factory` `Counter.start()` che restituisce sempre un nuovo `Counter` inizializzato con `_count = 0` (usando un costruttore privato con nome `Counter._internal()`).

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Il conteggio iniziale è 0

```dart
    test('initial count', () {
      var c = Counter.start();
      expect(c.count, 0, reason: '--err-t1--');
    });
```

Increment aumenta il conteggio di 1

```dart
    test('increment', () {
      var c = Counter.start();
      c.increment();
      expect(c.count, 1, reason: '--err-t2--');
    });
```

Più increment funzionano correttamente

```dart
    test('multiple increments', () {
      var c = Counter.start();
      c.increment();
      c.increment();
      c.increment();
      expect(c.count, 3, reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Counter {
  int _count = 0;

  Counter._internal();

  factory Counter.start() => Counter._internal();

  void increment() {
    _count++;
  }

  int get count => _count;
}
```
