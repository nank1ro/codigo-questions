---
language: dart
exerciseType: 2
---

# --description--

L'**ereditarietà** permette a una classe di riutilizzare i campi e i metodi di un'altra classe. Usa la parola chiave `extends` per creare una sottoclasse (classe figlia):

```dart
class Animal {
  String name;
  Animal(this.name);

  void speak() {
    print('...');
  }
}

class Cat extends Animal {
  Cat(String name) : super(name);

  @override
  void speak() {
    print('Meow');
  }
}
```

La classe figlia chiama il costruttore genitore con `super(...)`. L'annotazione `@override` indica che il metodo sostituisce la versione del genitore.

# --instructions--

Completa il codice in modo che `Cat` estenda `Animal`, chiami il costruttore genitore e sovrascriva `speak()` per stampare `'Meow'`.

# --seed--

```dart
class Animal {
  String name;
  Animal(this.name);

  void speak() {
    print('...');
  }
}

class Cat [/] Animal {
  Cat(String name) : [/](name);

  @[/]
  void speak() {
    print('Meow');
  }
}

void main() {
  final cat = Cat('Whiskers');
  cat.[/]();
}
```

# --answers--

- extends
- super
- override
- speak
- implements
- this
- Cat

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);

  void speak() {
    print('...');
  }
}

class Cat extends Animal {
  Cat(String name) : super(name);

  @override
  void speak() {
    print('Meow');
  }
}

void main() {
  final cat = Cat('Whiskers');
  cat.speak();
}
```

# --output--

Meow
