---
language: dart
exerciseType: 2
---

# --description--

Un **metodo** è una funzione definita all'interno di una classe. I metodi descrivono il comportamento di un oggetto:

```dart
class Animal {
  String name;

  Animal(this.name);

  void speak() {
    print('$name makes a sound.');
  }
}
```

Si chiama un metodo su un oggetto usando la notazione col punto: `dog.speak()`.

# --instructions--

Completa il metodo `greet` all'interno della classe `Person` in modo che stampi `'Hello, I am $name'`.

# --seed--

```dart
class Person {
  String name;

  Person(this.name);

  void [/]() {
    print('Hello, I am $name');
  }
}
```

# --answers--

- greet
- Greet
- hello
- method
- speak

# --solutions--

```dart
class Person {
  String name;

  Person(this.name);

  void greet() {
    print('Hello, I am $name');
  }
}
```
