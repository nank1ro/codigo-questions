---
language: dart
exerciseType: 1
---

# --instructions--

Crea una classe genitore `Animal` con un campo `String` `name` e un costruttore. Poi crea una classe figlia `Dog` che estende `Animal`. `Dog` deve avere un campo `String` aggiuntivo `breed` e un costruttore che usa `super` per passare il `name` ad `Animal`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Dog('Rex', 'Labrador').name` dovrebbe essere uguale a `'Rex'`

```dart
  test('test1', () {
    final dog = Dog('Rex', 'Labrador');
    expect(dog.name, 'Rex', reason: '--err-t1--');
  });
```

`Dog('Rex', 'Labrador').breed` dovrebbe essere uguale a `'Labrador'`

```dart
  test('test2', () {
    final dog = Dog('Rex', 'Labrador');
    expect(dog.breed, 'Labrador', reason: '--err-t2--');
  });
```

`Dog` dovrebbe essere un'istanza di `Animal`

```dart
  test('test3', () {
    final dog = Dog('Buddy', 'Poodle');
    expect(dog is Animal, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Dog extends Animal {
  String breed;
  Dog(String name, this.breed) : super(name);
}
```
