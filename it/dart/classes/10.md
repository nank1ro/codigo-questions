---
language: dart
exerciseType: 1
---

# --instructions--

Crea una classe `Counter` con un campo `int` `count` inizializzato a `0`. Aggiungi un metodo `increment()` che aumenta `count` di `1`, e un metodo `reset()` che reimposta `count` a `0`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Chiamare `increment()` tre volte dovrebbe rendere `count` uguale a `3`

```dart
  test('test1', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.increment();
    expect(c.count, 3, reason: '--err-t1--');
  });
```

Chiamare `reset()` dovrebbe reimpostare `count` a `0`

```dart
  test('test2', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.reset();
    expect(c.count, 0, reason: '--err-t2--');
  });
```

Il `count` iniziale dovrebbe essere `0`

```dart
  test('test3', () {
    final c = Counter();
    expect(c.count, 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Counter {
  int count = 0;

  void increment() {
    count++;
  }

  void reset() {
    count = 0;
  }
}
```
