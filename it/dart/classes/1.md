---
language: dart
exerciseType: 2
---

# --description--

Una **classe** è un modello per creare oggetti. Raggruppa dati correlati (campi) e comportamenti (metodi) insieme.

In Dart, dichiari una classe con la parola chiave `class` seguita dal nome della classe (per convenzione, in PascalCase):

```dart
class Car {
  String brand = 'Toyota';
}
```

Crei un'**istanza** (oggetto) della classe in questo modo:

```dart
final car = Car();
print(car.brand); // Toyota
```

# --instructions--

Completa il codice per dichiarare una classe chiamata `Person` con un campo `String` `name` inizializzato a `'Alice'`, poi crea un'istanza e stampa il suo `name`.

# --seed--

```dart
[/] Person {
  String [/] = 'Alice';
}

void main() {
  final p = [/]();
  print(p.[/]);
}
```

# --answers--

- class
- name
- Person
- brand
- age
- void
- p

# --solutions--

```dart
class Person {
  String name = 'Alice';
}

void main() {
  final p = Person();
  print(p.name);
}
```

# --output--

Alice
