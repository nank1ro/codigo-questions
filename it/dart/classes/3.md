---
language: dart
exerciseType: 2
---

# --description--

Un **metodo** è una funzione definita all'interno di una classe. I metodi descrivono il comportamento di un oggetto.

```dart
class Circle {
  double radius;
  Circle(this.radius);

  double area() {
    return 3.14 * radius * radius;
  }
}

void main() {
  final c = Circle(5);
  print(c.area()); // 78.5
}
```

I metodi possono accedere direttamente ai campi dell'oggetto per nome (o tramite `this.nomeCampo`).

# --instructions--

Completa la classe `Rectangle` in modo che abbia campi `int` `width` e `height`, un costruttore e un metodo `perimeter()` che restituisce `2 * (width + height)`. Poi chiama il metodo e stampa il risultato.

# --seed--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.[/]);

  int [/]() {
    return 2 * ([/] + height);
  }
}

void main() {
  final r = Rectangle(4, 6);
  print(r.[/]());
}
```

# --answers--

- height
- perimeter
- width
- area
- this
- 3
- r

# --solutions--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.height);

  int perimeter() {
    return 2 * (width + height);
  }
}

void main() {
  final r = Rectangle(4, 6);
  print(r.perimeter());
}
```

# --output--

20
