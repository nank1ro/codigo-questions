---
language: dart
exerciseType: 2
---

# --description--

Quando il costruttore di una sottoclasse deve chiamare il costruttore della superclasse, usa la parola chiave `super` nella **lista di inizializzazione**:

```dart
class Vehicle {
  String brand;
  Vehicle(this.brand);
}

class Car extends Vehicle {
  int doors;
  Car(String brand, this.doors) : super(brand);
}
```

`super(brand)` inoltra l'argomento `brand` al costruttore di `Vehicle`.

# --instructions--

Completa il costruttore di `Cat` in modo che chiami il costruttore della superclasse `Animal` con `name`.

# --seed--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Cat extends Animal {
  Cat(String name) : [/](name);
}
```

# --answers--

- super
- Animal
- parent
- base
- this

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Cat extends Animal {
  Cat(String name) : super(name);
}
```
