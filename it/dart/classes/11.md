---
language: dart
exerciseType: 1
---

# --instructions--

Crea una classe `Rectangle` con campi `int` `width` e `height` e un costruttore che li imposta. Aggiungi un getter `area` che restituisce `width * height`, e un getter `perimeter` che restituisce `2 * (width + height)`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Rectangle(4, 5).area` dovrebbe essere uguale a `20`

```dart
  test('test1', () {
    final r = Rectangle(4, 5);
    expect(r.area, 20, reason: '--err-t1--');
  });
```

`Rectangle(4, 5).perimeter` dovrebbe essere uguale a `18`

```dart
  test('test2', () {
    final r = Rectangle(4, 5);
    expect(r.perimeter, 18, reason: '--err-t2--');
  });
```

`Rectangle(3, 3).area` dovrebbe essere uguale a `9`

```dart
  test('test3', () {
    final r = Rectangle(3, 3);
    expect(r.area, 9, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.height);

  int get area => width * height;
  int get perimeter => 2 * (width + height);
}
```
