---
language: dart
exerciseType: 1
---

# --instructions--

Crea una classe `Car` con due proprietà `String`: `brand` e `model`. Aggiungi un costruttore che le imposta entrambe usando la sintassi abbreviata.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Creare `Car('Toyota', 'Corolla')` dovrebbe impostare `brand` a `'Toyota'`

```dart
  test('test1', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.brand, 'Toyota', reason: '--err-t1--');
  });
```

Creare `Car('Toyota', 'Corolla')` dovrebbe impostare `model` a `'Corolla'`

```dart
  test('test2', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.model, 'Corolla', reason: '--err-t2--');
  });
```

Creare `Car('Honda', 'Civic')` dovrebbe impostare `brand` a `'Honda'`

```dart
  test('test3', () {
    final car = Car('Honda', 'Civic');
    expect(car.brand, 'Honda', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Car {
  String brand;
  String model;
  Car(this.brand, this.model);
}
```
