---
language: dart
exerciseType: 1
---

# --instructions--

Crea una classe `abstract` `Shape` con un metodo astratto `double area()`. Poi crea una classe `Circle` che estende `Shape`, ha un campo `double` `radius`, un costruttore e sovrascrive `area()` per restituire `3.14 * radius * radius`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Circle(5).area()` dovrebbe essere uguale a `78.5`

```dart
  test('test1', () {
    final c = Circle(5);
    expect(c.area(), 78.5, reason: '--err-t1--');
  });
```

`Circle(10).area()` dovrebbe essere uguale a `314.0`

```dart
  test('test2', () {
    final c = Circle(10);
    expect(c.area(), 314.0, reason: '--err-t2--');
  });
```

`Circle` dovrebbe essere un'istanza di `Shape`

```dart
  test('test3', () {
    final c = Circle(3);
    expect(c is Shape, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
abstract class Shape {
  double area();
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() {
    return 3.14 * radius * radius;
  }
}
```
