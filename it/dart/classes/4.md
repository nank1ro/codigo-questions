---
language: dart
exerciseType: 3
---

# --description--

Il parametro `this.x` visto nell'esercizio precedente è una forma abbreviata. La forma estesa assegna ogni parametro al proprio campo all'interno del corpo del costruttore (`this.x` è il campo, `x` è il parametro):

```dart
class Point {
  int x = 0;
  int y = 0;

  Point(int x, int y) {
    this.x = x;
    this.y = y;
  }
}
```

La stessa classe può essere scritta come:

```dart
class Point {
  int x;
  int y;

  Point(this.x, this.y);
}
```

Questa forma più breve si chiama **parametri formali di inizializzazione**.

# --instructions--

Quale delle seguenti opzioni usa correttamente la forma abbreviata dei parametri formali di inizializzazione?

# --answers--

- `Point(this.x, this.y);`
- `Point(x, y) { self.x = x; self.y = y; }`
- `Point(x, y) => x + y;`
- `Point.init(x, y);`

# --solutions--

- `Point(this.x, this.y);`
