---
language: dart
exerciseType: 1
---

# --instructions--

Inizia con `product = 1` e usa un ciclo `while` per moltiplicare ripetutamente per `2` finché `product` è maggiore o uguale a `100`. Restituisci `product`.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return product;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`product` deve essere uguale a `128`

```dart
  test('test1', () {
    expect(computeValue(), 128, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int product = 1;
while (product < 100) {
  product *= 2;
}
```
