---
language: dart
exerciseType: 2
---

# --description--

La parola chiave `break` esce immediatamente da un ciclo, indipendentemente dalla condizione del ciclo.

```dart
int i = 0;
while (true) {
  if (i == 3) break;
  print(i);
  i++;
}
```

Senza `break`, il ciclo `while (true)` verrebbe eseguito all'infinito. Qui si ferma quando `i` raggiunge `3`.

# --instructions--

Completa il ciclo che stampa i numeri a partire da `0` e usa `break` per fermarsi quando `i` è uguale a `4`.

# --seed--

```dart
void main() {
  int i = 0;
  while ([/]) {
    if (i == [/]) [/];
    print(i);
    i++;
  }
}
```

# --answers--

- true
- false
- 4
- 3
- break
- continue
- return

# --solutions--

```dart
void main() {
  int i = 0;
  while (true) {
    if (i == 4) break;
    print(i);
    i++;
  }
}
```

# --output--

0
1
2
3
