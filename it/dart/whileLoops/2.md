---
language: dart
exerciseType: 2
---

# --description--

Una variabile contatore controlla quante volte viene eseguito un ciclo `while`.

```dart
int count = 0;
while (count < 5) {
  count++;
}
print(count); // 5
```

Il contatore parte da `0`, e il ciclo lo incrementa a ogni iterazione finché `count < 5` è `false`.

# --instructions--

Completa il ciclo che conta da `1` a `5` e stampa ogni valore.

# --seed--

```dart
void main() {
  int count = [/];
  while (count [/] 5) {
    print(count);
    [/];
  }
}
```

# --answers--

- 1
- 0
- <=
- <
- count++
- count--
- count += 2

# --solutions--

```dart
void main() {
  int count = 1;
  while (count <= 5) {
    print(count);
    count++;
  }
}
```

# --output--

1
2
3
4
5
