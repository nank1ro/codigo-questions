---
language: dart
exerciseType: 1
---

# --instructions--

Usa un ciclo `while` per calcolare la somma dei numeri da `1` a `10` (incluso) e restituisci il risultato.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` deve essere uguale a `55`

```dart
  test('test1', () {
    expect(computeValue(), 55, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sum = 0;
int i = 1;
while (i <= 10) {
  sum += i;
  i++;
}
```
