---
language: dart
exerciseType: 2
---

# --description--

La condizione di un ciclo `while` può contenere qualsiasi espressione booleana. Non appena la condizione restituisce `false`, il ciclo termina.

```dart
int n = 100;
while (n > 1) {
  n ~/= 2;
}
print(n); // 0
```

Qui, `n` viene dimezzato a ogni iterazione usando la divisione intera (`~/`).

# --instructions--

Completa il ciclo `while` che continua a dimezzare `n` (usando `~/= 2`) mentre è maggiore di `1`, poi stampa il risultato.

# --seed--

```dart
void main() {
  int n = 64;
  while (n [/] 1) {
    n [/] 2;
  }
  print(n);
}
```

# --answers--

- >
- <
- ~/=
- /=
- *=

# --solutions--

```dart
void main() {
  int n = 64;
  while (n > 1) {
    n ~/= 2;
  }
  print(n);
}
```

# --output--

1
