---
language: dart
exerciseType: 1
---

# --instructions--

Usa un ciclo `while` per contare a ritroso da `10` a `1` (incluso). Restituisci il numero di iterazioni eseguite.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return iterations;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`iterations` deve essere uguale a `10`

```dart
  test('test1', () {
    expect(computeValue(), 10, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 10;
int iterations = 0;
while (n >= 1) {
  iterations++;
  n--;
}
```
