---
language: dart
exerciseType: 1
---

# --instructions--

Scrivi un ciclo `while` che conta da `1` a `5` e restituisce il valore finale del contatore.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return counter;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`counter` deve essere uguale a `5`

```dart
  test('test1', () {
    expect(computeValue(), 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int counter = 1;
while (counter < 5) {
  counter++;
}
```
