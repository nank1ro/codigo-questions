---
language: dart
exerciseType: 1
---

# --instructions--
Scrivi una funzione che restituisce il valore nella posizione (`row`, `col`) di una tabella di moltiplicazione. Usa cicli `for` annidati dove il ciclo esterno itera sulle righe e quello interno sulle colonne (entrambi con indice 1). Restituisci il prodotto nella posizione indicata.

# --before-seed--
```dart
int multiplicationCell(int row, int col) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Riga 3, colonna 4 è uguale a 12
```dart
    test('3x4 = 12', () {
      expect(multiplicationCell(3, 4), 12, reason: '--err-t1--');
    });
```

Riga 1, colonna 1 è uguale a 1
```dart
    test('1x1 = 1', () {
      expect(multiplicationCell(1, 1), 1, reason: '--err-t2--');
    });
```

Riga 7, colonna 8 è uguale a 56
```dart
    test('7x8 = 56', () {
      expect(multiplicationCell(7, 8), 56, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int r = 1; r <= row; r++) {
    for (int c = 1; c <= col; c++) {
      if (r == row && c == col) {
        result = r * c;
      }
    }
  }
```
