---
language: dart
exerciseType: 1
---

# --instructions--
Scrivi una funzione che riceve una `List<int>` e restituisce una nuova lista in cui ogni elemento è moltiplicato per 2.

# --before-seed--
```dart
List<int> doubleElements(List<int> items) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Raddoppia ogni elemento
```dart
    test('doubles elements', () {
      expect(doubleElements([1, 2, 3]), [2, 4, 6], reason: '--err-t1--');
    });
```

Una lista vuota restituisce una lista vuota
```dart
    test('empty list', () {
      expect(doubleElements([]), [], reason: '--err-t2--');
    });
```

Lista con un solo elemento
```dart
    test('single element', () {
      expect(doubleElements([5]), [10], reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  List<int> result = [];
  for (var item in items) {
    result.add(item * 2);
  }
```
