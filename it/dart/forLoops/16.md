---
language: dart
exerciseType: 1
---

# --instructions--
Scrivi una funzione che calcola il fattoriale di un intero non negativo `n` usando un ciclo `for`. Il fattoriale di 0 è 1.

# --before-seed--
```dart
int factorial(int n) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Il fattoriale di 5 è 120
```dart
    test('5! = 120', () {
      expect(factorial(5), 120, reason: '--err-t1--');
    });
```

Il fattoriale di 0 è 1
```dart
    test('0! = 1', () {
      expect(factorial(0), 1, reason: '--err-t2--');
    });
```

Il fattoriale di 1 è 1
```dart
    test('1! = 1', () {
      expect(factorial(1), 1, reason: '--err-t3--');
    });
```

Il fattoriale di 6 è 720
```dart
    test('6! = 720', () {
      expect(factorial(6), 720, reason: '--err-t4--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 1;
  for (int i = 2; i <= n; i++) {
    result *= i;
  }
```
