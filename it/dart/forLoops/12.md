---
language: dart
exerciseType: 1
---

# --instructions--
Scrivi una funzione che riceve una `List<String>` e restituisce una singola stringa unendo tutti gli elementi con uno spazio tra di loro, usando un ciclo `for-in`.

# --before-seed--
```dart
String joinWords(List<String> words) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Unisce tre parole con spazi
```dart
    test('three words', () {
      expect(joinWords(['hello', 'dart', 'world']), 'hello dart world', reason: '--err-t1--');
    });
```

Una singola parola restituisce quella parola
```dart
    test('single word', () {
      expect(joinWords(['only']), 'only', reason: '--err-t2--');
    });
```

Una lista vuota restituisce una stringa vuota
```dart
    test('empty list', () {
      expect(joinWords([]), '', reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  String result = '';
  for (var word in words) {
    if (result.isEmpty) {
      result = word;
    } else {
      result += ' $word';
    }
  }
```
