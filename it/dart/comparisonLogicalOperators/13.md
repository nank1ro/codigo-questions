---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara due variabili `int` a livello superiore: `price` con il valore `30` e `budget` con il valore `50`. Poi dichiara una variabile `bool` a livello superiore chiamata `canAfford` che memorizza il risultato della verifica se `price` è minore di `budget` usando l'operatore `<`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canAfford` deve essere `true` e di tipo `bool`

```dart
  test('test1', () {
    expect(canAfford, true, reason: '--err-t1--');
    expect(canAfford, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int price = 30;
int budget = 50;
bool canAfford = price < budget;

void main() {
  print(canAfford);
}
```
