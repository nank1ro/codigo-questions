---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara due variabili `int` di livello superiore: `x` con il valore `8` e `y` con il valore `8`. Quindi dichiara una variabile `bool` di livello superiore chiamata `areEqual` che memorizza il risultato del confronto di `x` e `y` con l'operatore `==`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`areEqual` deve essere `true` e di tipo `bool`

```dart
  test('test1', () {
    expect(areEqual, true, reason: '--err-t1--');
    expect(areEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 8;
int y = 8;
bool areEqual = x == y;

void main() {
  print(areEqual);
}
```
