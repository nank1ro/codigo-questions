---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara tre variabili `int` di livello superiore: `x` con il valore `5`, `y` con il valore `3` e `z` con il valore `0`. Quindi dichiara una variabile `bool` di livello superiore chiamata `result` che valuta `!(x > 10) || (y < 5 && z == 0)`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` deve essere `true` e di tipo `bool`

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 5;
int y = 3;
int z = 0;
bool result = !(x > 10) || (y < 5 && z == 0);

void main() {
  print(result);
}
```
