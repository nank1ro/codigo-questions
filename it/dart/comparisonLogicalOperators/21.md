---
language: dart
exerciseType: 4
---

# --instructions--

Ordina le righe di codice per dichiarare un valore di temperatura, verificare se è nell'intervallo da `0` a `100` (inclusi) utilizzando `&&`, memorizzare il risultato in `isValid` e stamparlo. L'output deve essere `true`.

# --answers--

```dart
void main() {
```

```dart
  int temperature = 37;
```

```dart
  bool isValid = temperature >= 0 && temperature <= 100;
```

```dart
  print(isValid);
```

```dart
}
```

# --solutions--

```dart
void main() {
  int temperature = 37;
  bool isValid = temperature >= 0 && temperature <= 100;
  print(isValid);
}
```

# --output--

true
