---
language: dart
exerciseType: 2
---

# --description--

L'**operatore maggiore di o uguale a** `>=` restituisce `true` quando il valore sinistro è più grande che **o uguale a** il valore destro.

```dart
int score = 50;
bool passed = score >= 50; // true
```

A differenza di `>`, questo operatore restituisce anche `true` quando entrambi i valori sono uguali.

# --instructions--

Completa il codice per verificare se `7` è maggiore di o uguale a `7` e memorizza il risultato in `isAtLeastSeven`.

# --seed--

```dart
void main() {
  bool isAtLeastSeven = 7 [/] 7;
  print(isAtLeastSeven);
}
```

# --answers--

- >=
- >
- <=
- ==

# --solutions--

```dart
void main() {
  bool isAtLeastSeven = 7 >= 7;
  print(isAtLeastSeven);
}
```

# --output--

true
