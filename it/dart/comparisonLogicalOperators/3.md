---
language: dart
exerciseType: 2
---

# --description--

L'**operatore maggiore di** `>` restituisce `true` quando il valore a sinistra è strettamente maggiore del valore a destra.

```dart
int a = 8;
int b = 3;
bool result = a > b; // true
```

Se il valore a sinistra è uguale o minore di quello a destra, restituisce `false`.

# --instructions--

Complete the code to check if `15` is greater than `6` and store the result in `isGreater`.

# --seed--

```dart
void main() {
  bool isGreater = 15 [/] 6;
  print(isGreater);
}
```

# --answers--

- >
- <
- >=
- ==

# --solutions--

```dart
void main() {
  bool isGreater = 15 > 6;
  print(isGreater);
}
```

# --output--

true
