---
language: dart
exerciseType: 1
---

# --description--

The **OR operator** `||` combines two boolean expressions. It returns `true` when **at least one** side is `true`. It only returns `false` when **both** sides are `false`.

```dart
bool a = false;
bool b = true;
bool result = a || b; // true

bool c = false;
bool d = false;
bool result2 = c || d; // false
```

# --instructions--

Declare two top-level `bool` variables: `hasTicket` with the value `false` and `isVIP` with the value `true`. Then declare a top-level `bool` variable named `canEnter` that is `true` when either `hasTicket` OR `isVIP` is `true`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canEnter` deve essere `true` e di tipo `bool`

```dart
  test('test1', () {
    expect(canEnter, true, reason: '--err-t1--');
    expect(canEnter, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool hasTicket = false;
bool isVIP = true;
bool canEnter = hasTicket || isVIP;

void main() {
  print(canEnter);
}
```
