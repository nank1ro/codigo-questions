---
language: dart
exerciseType: 1
---

# --description--

L'**operatore OR** `||` combina due espressioni booleane. Restituisce `true` quando **almeno un** lato è `true`. Restituisce `false` solo quando **entrambi** i lati sono `false`.

```dart
bool a = false;
bool b = true;
bool result = a || b; // true

bool c = false;
bool d = false;
bool result2 = c || d; // false
```

# --instructions--

L'**operatore OR** `||` combina due espressioni booleane. Restituisce `true` quando **almeno uno** dei due lati è `true`. Restituisce `false` solo quando **entrambi** i lati sono `false`.

```dart
bool a = false;
bool b = true;
bool result = a || b; // true

bool c = false;
bool d = false;
bool result2 = c || d; // false
```

Dichiara due variabili `bool` a livello superiore: `hasTicket` con il valore `false` e `isVIP` con il valore `true`. Poi dichiara una variabile `bool` a livello superiore chiamata `canEnter` che sia `true` quando `hasTicket` O `isVIP` è `true`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canEnter` deve essere `true` e di tipo `bool`

```dart
  test('test1', () {
    expect(canEnter, true, reason: '--err-t1--');
    expect(canEnter, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool hasTicket = false;
bool isVIP = true;
bool canEnter = hasTicket || isVIP;

void main() {
  print(canEnter);
}
```
