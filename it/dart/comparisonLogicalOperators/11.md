---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara due variabili `int` a livello superiore: `a` con il valore `5` e `b` con il valore `9`. Poi dichiara una variabile `bool` a livello superiore chiamata `notEqual` che memorizza il risultato del confronto tra `a` e `b` con l'operatore `!=`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`notEqual` deve essere `true` e di tipo `bool`

```dart
  test('test1', () {
    expect(notEqual, true, reason: '--err-t1--');
    expect(notEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 5;
int b = 9;
bool notEqual = a != b;

void main() {
  print(notEqual);
}
```
