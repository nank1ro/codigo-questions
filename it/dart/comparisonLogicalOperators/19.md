---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara tre variabili `int` di livello superiore: `number` con il valore `42`, `min` con il valore `1` e `max` con il valore `100`. Quindi dichiara una variabile `bool` di livello superiore chiamata `inRange` che è `true` quando `number` è maggiore di o uguale a `min` E minore di o uguale a `max`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`inRange` deve essere `true` e di tipo `bool`

```dart
  test('test1', () {
    expect(inRange, true, reason: '--err-t1--');
    expect(inRange, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int number = 42;
int min = 1;
int max = 100;
bool inRange = number >= min && number <= max;

void main() {
  print(inRange);
}
```
