---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara due variabili `int` a livello superiore: `high` con il valore `20` e `low` con il valore `12`. Poi dichiara una variabile `bool` a livello superiore chiamata `isHigher` che memorizza il risultato della verifica se `high` è maggiore di `low` usando l'operatore `>`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHigher` deve essere `true` e di tipo `bool`

```dart
  test('test1', () {
    expect(isHigher, true, reason: '--err-t1--');
    expect(isHigher, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int high = 20;
int low = 12;
bool isHigher = high > low;

void main() {
  print(isHigher);
}
```
