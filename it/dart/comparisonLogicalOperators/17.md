---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara una variabile `int` di livello superiore chiamata `age` con il valore `35`. Quindi dichiara una variabile `bool` di livello superiore chiamata `isWorkingAge` che è `true` quando `age` è maggiore di o uguale a `18` E minore di o uguale a `65`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isWorkingAge` deve essere `true` e di tipo `bool`

```dart
  test('test1', () {
    expect(isWorkingAge, true, reason: '--err-t1--');
    expect(isWorkingAge, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int age = 35;
bool isWorkingAge = age >= 18 && age <= 65;

void main() {
  print(isWorkingAge);
}
```
