---
language: dart
exerciseType: 4
---

# --instructions--

Ordina le righe di codice per dichiarare due variabili booleane, combinarle con `||` e `&&` come mostrato, memorizzare il risultato in `outcome` e stamparlo. L'output deve essere `true`.

# --answers--

```dart
void main() {
```

```dart
  bool hasPass = false;
```

```dart
  bool isAdmin = true;
```

```dart
  bool outcome = hasPass || isAdmin && !hasPass;
```

```dart
  print(outcome);
```

```dart
}
```

# --solutions--

```dart
void main() {
  bool hasPass = false;
  bool isAdmin = true;
  bool outcome = hasPass || isAdmin && !hasPass;
  print(outcome);
}
```

# --output--

true
