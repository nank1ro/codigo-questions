---
language: dart
exerciseType: 1
---

# --instructions--

Un negozio sta applicando uno sconto del 20% su un articolo. Dichiara una variabile `double` di livello superiore `price` con valore `50.0`. Quindi usa `*=` per applicare lo sconto moltiplicando `price` per `0.8`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`price` dovrebbe essere `40.0`

```dart
  test('test1', () {
    expect(price, closeTo(40.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double price = 50.0;

void main() {
  price *= 0.8;
  print(price);
}
```
