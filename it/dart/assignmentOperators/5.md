---
language: dart
exerciseType: 3
---

# --description--

Gli operatori di assegnazione combinano un'operazione aritmetica con un'assegnazione in un unico passaggio.

Ad esempio, `x += 3` è un'abbreviazione per `x = x + 3`.

# --instructions--

Cosa fa `x += 5` quando `x` è attualmente `10`?

# --answers--

- Imposta `x` a `15`
- Imposta `x` a `5`
- Imposta `x` a `10`
- Imposta `x` a `50`

# --solutions--

- Imposta `x` a `15`
