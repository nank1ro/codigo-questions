---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara una variabile `int` di livello superiore `points` con valore `20`. Quindi usa `+=` per aggiungere `15`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`points` dovrebbe essere `35`

```dart
  test('test1', () {
    expect(points, 35, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int points = 20;

void main() {
  points += 15;
  print(points);
}
```
