---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara una variabile `int` di livello superiore `n` con valore `2`. Quindi, in `main`, applica i seguenti operatori di assegnazione in ordine:
- `n *= 10` (moltiplicare per 10)
- `n += 5` (aggiungere 5)
- `n -= 3` (sottrarre 3)

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`n` dovrebbe essere `22`

```dart
  test('test1', () {
    expect(n, 22, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 2;

void main() {
  n *= 10;
  n += 5;
  n -= 3;
  print(n);
}
```
