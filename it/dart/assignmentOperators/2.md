---
language: dart
exerciseType: 2
---

# --description--

The **addition assignment operator** `+=` aggiunge un valore a una variabile e memorizza il risultato nella variabile.

```dart
int score = 10;
score += 5; // score is now 15
```

Questo è un'abbreviazione per `score = score + 5`.

# --instructions--

Completa il codice per aggiungere `8` alla variabile `total` usando `+=`.

# --seed--

```dart
void main() {
  int total = 12;
  total [/] 8;
  print(total);
}
```

# --answers--

- +=
- =
- -=
- +

# --solutions--

```dart
void main() {
  int total = 12;
  total += 8;
  print(total);
}
```

# --output--

20
