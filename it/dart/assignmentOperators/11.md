---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara una variabile `double` di livello superiore `value` con valore `20.0`. Quindi usa `/=` per dividerla per `4`. Nota che `/=` produce sempre un `double` in Dart.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value` dovrebbe essere `5.0`

```dart
  test('test1', () {
    expect(value, closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;

void main() {
  value /= 4;
  print(value);
}
```
