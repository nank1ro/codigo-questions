---
language: dart
exerciseType: 2
---

# --description--

Le funzioni possono accettare **parametri** — valori passati quando si chiama la funzione. I parametri sono dichiarati tra le parentesi con il loro tipo e nome:

```dart
int square(int n) {
  return n * n;
}

void main() {
  print(square(4)); // prints 16
}
```

Ogni parametro ha un tipo (`int`) e un nome (`n`). I parametri multipli sono separati da virgole.

# --instructions--

Completa la funzione `multiply` che prende due parametri `int` `a` e `b` e restituisce il loro prodotto.

# --seed--

```dart
int multiply([/] a, [/] b) {
  return a [/] b;
}

void main() {
  print(multiply(3, 4));
}
```

# --answers--

- int
- int
- *
- +
- String
- double

# --solutions--

```dart
int multiply(int a, int b) {
  return a * b;
}

void main() {
  print(multiply(3, 4));
}
```

# --output--

12
