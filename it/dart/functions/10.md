---
language: dart
exerciseType: 1
---

# --instructions--

Scrivi una **funzione freccia** `square` che prende un parametro `int` `n` e restituisce `n * n`.

Usa la sintassi `=>` (senza parentesi graffe, senza la parola chiave `return`).

Esempio:
```dart
print(square(4));  // 16
print(square(5));  // 25
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`square(4)` deve restituire `16`
```dart
  test('test1', () {
    expect(square(4), 16, reason: '--err-t1--');
  });
```

`square(5)` deve restituire `25`
```dart
  test('test2', () {
    expect(square(5), 25, reason: '--err-t2--');
  });
```

`square(0)` deve restituire `0`
```dart
  test('test3', () {
    expect(square(0), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int square(int n) => n * n;
```
