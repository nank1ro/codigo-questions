---
language: dart
exerciseType: 1
---

# --instructions--

Scrivi una funzione `fullName` che prende due **parametri con nome** `firstName` e `lastName` (entrambi `required String`) e restituisce il nome completo come un'unica stringa con uno spazio tra di loro.

Esempio:
```dart
print(fullName(firstName: "Jane", lastName: "Doe")); // Jane Doe
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`fullName(firstName: "Jane", lastName: "Doe")` deve restituire `"Jane Doe"`
```dart
  test('test1', () {
    expect(fullName(firstName: "Jane", lastName: "Doe"), "Jane Doe", reason: '--err-t1--');
  });
```

`fullName(firstName: "John", lastName: "Smith")` deve restituire `"John Smith"`
```dart
  test('test2', () {
    expect(fullName(firstName: "John", lastName: "Smith"), "John Smith", reason: '--err-t2--');
  });
```

`fullName(firstName: "A", lastName: "B")` deve restituire `"A B"`
```dart
  test('test3', () {
    expect(fullName(firstName: "A", lastName: "B"), "A B", reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String fullName({required String firstName, required String lastName}) {
  return "$firstName $lastName";
}
```
