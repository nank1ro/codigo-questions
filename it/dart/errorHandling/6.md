---
language: dart
exerciseType: 4
---

# --description--

Un terzo blocco può seguire i gestori. `finally` viene eseguito **in ogni caso**: dopo che il blocco `try` è terminato normalmente, dopo che un gestore si è ripreso, e anche quando nulla corrisponde e l'errore sta ancora viaggiando verso l'esterno.

```dart
try {
  return 'parsed ${int.parse(text)}';
} on FormatException {
  return 'failed';
} finally {
  print('done');
}
```

Viene eseguito persino prima che un `return` riconsegni il suo valore, ed è per questo che il messaggio qui sopra viene stampato prima che il chiamante veda il risultato. Questo fa di `finally` il posto per il lavoro che deve avvenire in ogni caso, come chiudere ciò che hai aperto.

# --instructions--

Ordina gli elementi per definire una funzione `attempt` che restituisce `'parsed '` seguito dal numero, oppure `'failed'` quando il testo non è un numero, e stampa `done` in un blocco `finally` in ogni caso. Definisci la funzione prima di `main`. Il programma deve stampare:

```
done
parsed 7
done
failed
```

# --answers--

```dart
String attempt(String text) {
```

```dart
  try {
    return 'parsed ${int.parse(text)}';
```

```dart
  } on FormatException {
    return 'failed';
```

```dart
  } finally {
    print('done');
  }
}
```

```dart
void main() {
```

```dart
  print(attempt('7'));
```

```dart
  print(attempt('x'));
```

```dart
}
```

# --solutions--

```dart
String attempt(String text) {
  try {
    return 'parsed ${int.parse(text)}';
  } on FormatException {
    return 'failed';
  } finally {
    print('done');
  }
}

void main() {
  print(attempt('7'));
  print(attempt('x'));
}
```

# --output--

done
parsed 7
done
failed
