---
language: dart
exerciseType: 1
---

# --description--

`Exception` è un'interfaccia, quindi anche la tua classe può esserlo. Un'eccezione personalizzata dà all'errore un nome che una clausola `on` può selezionare, e campi che un gestore può leggere:

```dart
class EmptyCartException implements Exception {
  final String message;

  EmptyCartException(this.message);

  @override
  String toString() => 'EmptyCartException: $message';
}

throw EmptyCartException('nothing to pay for');
```

Tre parti meritano di essere tenute: `implements Exception` così la classe sta insieme agli altri errori, un campo `final` che porta con sé il dettaglio, e un `toString()` sovrascritto così il resoconto dell'eccezione non gestita è leggibile. Senza quella sovrascrittura, Dart stampa il solo nome della classe e il dettaglio va perso.

# --instructions--

Definisci una classe `InvalidEmailException` che implementa `Exception`, con un campo `final String` `message` impostato dal costruttore, e un `toString()` che restituisce `'InvalidEmailException: '` seguito dal messaggio.

Poi scrivi una funzione `checkEmail` che prende una `String` `email` e la restituisce, ma lancia `InvalidEmailException('missing @')` quando l'email non contiene `'@'`.

Esempio:
```dart
print(checkEmail('ada@dart.dev')); // ada@dart.dev
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`checkEmail('ada@dart.dev')` deve restituire l'email invariata

```dart
  test('test1', () {
    expect(checkEmail('ada@dart.dev'), 'ada@dart.dev', reason: '--err-t1--');
  });
```

`checkEmail('nope')` deve lanciare una `InvalidEmailException`, che è una `Exception`

```dart
  test('test2', () {
    expect(() => checkEmail('nope'), throwsA(isA<InvalidEmailException>()), reason: '--err-t2--');
    expect(() => checkEmail('nope'), throwsA(isA<Exception>()), reason: '--err-t2--');
  });
```

L'oggetto lanciato deve portare con sé il messaggio `'missing @'` ed essere stampato come `InvalidEmailException: missing @`

```dart
  test('test3', () {
    try {
      checkEmail('nope');
      fail('--err-t3--');
    } on InvalidEmailException catch (e) {
      expect(e.message, 'missing @', reason: '--err-t3--');
      expect(e.toString(), 'InvalidEmailException: missing @', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class InvalidEmailException implements Exception {
  final String message;

  InvalidEmailException(this.message);

  @override
  String toString() => 'InvalidEmailException: $message';
}

String checkEmail(String email) {
  if (!email.contains('@')) {
    throw InvalidEmailException('missing @');
  }
  return email;
}
```
