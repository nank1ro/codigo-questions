---
language: dart
exerciseType: 1
---

# --description--

`firstWhere` restituisce il primo elemento che soddisfa un test. Quando nulla corrisponde non c'è nessun elemento da restituire, quindi lancia un `StateError`:

```dart
final words = ['a', 'fg'];
print(words.firstWhere((w) => w.length > 3)); // Bad state: No element
```

Come `int.tryParse`, anche la libreria offre una via d'uscita. Il parametro con nome `orElse` prende una funzione che produce il valore da usare quando nulla ha corrisposto:

```dart
print(words.firstWhere((w) => w.length > 3, orElse: () => 'none')); // none
```

La scelta è la stessa di prima: `orElse` quando «niente corrisponde» è un esito ordinario, la chiamata senza `orElse` quando significherebbe che i dati sono corrotti e qualcuno deve sentirlo dire.

# --instructions--

Scrivi due funzioni che prendono una `List<String>` `words`.

`firstLong` restituisce la prima parola più lunga di tre caratteri usando `firstWhere`, oppure `'none'` quando non c'è nessuna, con `orElse`.

`firstLongStrict` fa lo stesso con `firstWhere`, ma senza `orElse`, così lancia un `StateError` quando non c'è nessuna.

Esempio:
```dart
print(firstLong(['a', 'bcde', 'fg'])); // bcde
print(firstLong(['a', 'fg'])); // none
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`firstLong(['a', 'bcde', 'fg'])` deve restituire `'bcde'`

```dart
  test('test1', () {
    expect(firstLong(['a', 'bcde', 'fg']), 'bcde', reason: '--err-t1--');
    expect(firstLong(['long', 'longer']), 'long', reason: '--err-t1--');
  });
```

`firstLong` deve restituire `'none'` quando nessuna parola è più lunga di tre caratteri

```dart
  test('test2', () {
    expect(firstLong(['a', 'fg']), 'none', reason: '--err-t2--');
    expect(firstLong(<String>[]), 'none', reason: '--err-t2--');
    expect(firstLong(['abc']), 'none', reason: '--err-t2--');
  });
```

`firstLongStrict` deve restituire la parola quando c'è e lanciare un `StateError` altrimenti

```dart
  test('test3', () {
    expect(firstLongStrict(['a', 'bcde']), 'bcde', reason: '--err-t3--');
    expect(() => firstLongStrict(['a', 'fg']), throwsStateError, reason: '--err-t3--');
    expect(() => firstLongStrict(<String>[]), throwsStateError, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String firstLong(List<String> words) {
  return words.firstWhere((w) => w.length > 3, orElse: () => 'none');
}

String firstLongStrict(List<String> words) {
  return words.firstWhere((w) => w.length > 3);
}
```
