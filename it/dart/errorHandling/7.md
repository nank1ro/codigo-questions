---
language: dart
exerciseType: 1
---

# --description--

Il tuo codice lancia allo stesso modo in cui lancia la libreria. `Exception('message')` costruisce un'eccezione semplice che porta con sé una breve spiegazione, e `throw` la mette in viaggio:

```dart
if (amount > balance) {
  throw Exception('insufficient funds');
}
```

Il messaggio non va perso: `toString()` mette insieme la parola `Exception`, due punti e il messaggio, che è esattamente ciò che stampa il resoconto dell'eccezione non gestita.

```dart
print(Exception('insufficient funds')); // Exception: insufficient funds
```

Lanciare è meglio che restituire un valore inventato come `-1`: il chiamante non può dimenticarsi di guardarlo, e il motivo viaggia con esso.

# --instructions--

Scrivi una funzione `withdraw` che prende due valori `int` `balance` e `amount` e restituisce il saldo rimanente.

Quando `amount` è maggiore di `balance`, lancia invece `Exception('insufficient funds')`.

Esempio:
```dart
print(withdraw(100, 30)); // 70
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`withdraw(100, 30)` deve restituire `70`

```dart
  test('test1', () {
    expect(withdraw(100, 30), 70, reason: '--err-t1--');
    expect(withdraw(50, 50), 0, reason: '--err-t1--');
  });
```

`withdraw(100, 300)` deve lanciare una `Exception`

```dart
  test('test2', () {
    expect(() => withdraw(100, 300), throwsA(isA<Exception>()), reason: '--err-t2--');
  });
```

L'eccezione lanciata deve essere stampata come `Exception: insufficient funds`

```dart
  test('test3', () {
    try {
      withdraw(0, 1);
      fail('--err-t3--');
    } catch (e) {
      expect(e.toString(), 'Exception: insufficient funds', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int withdraw(int balance, int amount) {
  if (amount > balance) {
    throw Exception('insufficient funds');
  }
  return balance - amount;
}
```
