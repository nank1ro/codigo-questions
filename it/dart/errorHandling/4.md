---
language: dart
exerciseType: 1
---

# --description--

Un blocco `try` può essere seguito da **più** clausole, ognuna delle quali si riprende da un errore diverso. Dart confronta l'oggetto lanciato con esse dall'alto verso il basso ed esegue la **prima** che corrisponde:

```dart
try {
  return names[int.parse(text)];
} on FormatException {
  return 'not a number';
} on RangeError {
  return 'out of range';
} catch (e) {
  return 'unknown problem';
}
```

Leggere una lista con un indice che non esiste lancia un **`RangeError`**, quindi i due errori di quella sola riga ricevono risposte diverse.

Poiché vince la prima corrispondenza, l'ordine conta: una clausola per un tipo generale messa sopra una più specifica vincerebbe sempre, lasciando la clausola specifica irraggiungibile. Scrivi prima le clausole specifiche, e un `catch` senza tipo per ultimo se vuoi una rete di sicurezza.

La clausola `on RangeError` qui sopra è presente solo per mostrare come si ordinano più clausole. Un `RangeError` segnala un errore nel codice più che una condizione che il programma non poteva controllare, e un esercizio successivo spiega perché un simile errore dovrebbe essere prevenuto invece che catturato.

# --instructions--

Scrivi una funzione `lookup` che prende una `List<int>` `scores` e una `String` `text`, e restituisce `'score '` seguito dall'elemento di `scores` nella posizione che `text` descrive.

Restituisci `'not a number'` quando `text` non è un numero intero, `'out of range'` quando la posizione non esiste, e `'unknown problem'` per qualsiasi altra cosa.

Esempio:
```dart
print(lookup([10, 20, 30], '1')); // score 20
print(lookup([10, 20, 30], 'two')); // not a number
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`lookup([10, 20, 30], '1')` deve restituire `'score 20'`

```dart
  test('test1', () {
    expect(lookup([10, 20, 30], '1'), 'score 20', reason: '--err-t1--');
    expect(lookup([10, 20, 30], '0'), 'score 10', reason: '--err-t1--');
  });
```

`lookup([10, 20, 30], 'two')` deve restituire `'not a number'`

```dart
  test('test2', () {
    expect(lookup([10, 20, 30], 'two'), 'not a number', reason: '--err-t2--');
    expect(lookup([10, 20, 30], ''), 'not a number', reason: '--err-t2--');
  });
```

`lookup([10, 20, 30], '7')` deve restituire `'out of range'`

```dart
  test('test3', () {
    expect(lookup([10, 20, 30], '7'), 'out of range', reason: '--err-t3--');
    expect(lookup([10, 20, 30], '-1'), 'out of range', reason: '--err-t3--');
    expect(lookup(<int>[], '0'), 'out of range', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String lookup(List<int> scores, String text) {
  try {
    return 'score ${scores[int.parse(text)]}';
  } on FormatException {
    return 'not a number';
  } on RangeError {
    return 'out of range';
  } catch (e) {
    return 'unknown problem';
  }
}
```
