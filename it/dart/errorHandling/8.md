---
language: dart
exerciseType: 1
---

# --description--

A volte un gestore non è il posto giusto per riprendersi: vuoi solo *accorgerti* dell'errore e lasciarlo proseguire verso il chiamante che può davvero occuparsene. La parola chiave `rethrow` fa proprio questo, dentro un blocco `catch` o `on ... catch`:

```dart
try {
  return int.parse(text);
} on FormatException {
  log.add('bad input: $text');
  rethrow;
}
```

`rethrow` rimanda oltre lo **stesso** oggetto, quindi il chiamante vede l'errore originale. Scrivere `throw e` al suo posto funzionerebbe ugualmente, ma fa ripartire il viaggio e perde il punto in cui l'errore si è verificato la prima volta.

Un blocco `finally` nella stessa istruzione viene comunque eseguito, anche mentre si esce.

# --instructions--

Scrivi una funzione `parseOrLog` che prende una `String` `text` e una `List<String>` `log`, e restituisce `int.parse(text)`.

Quando il testo non è un numero intero, aggiungi a `log` la stringa `'bad input: '` seguita dal testo, poi rilancia la `FormatException` così che il chiamante la veda comunque.

Esempio:
```dart
final log = <String>[];
print(parseOrLog('42', log)); // 42
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parseOrLog('42', log)` deve restituire `42` e lasciare il log vuoto

```dart
  test('test1', () {
    final log = <String>[];
    expect(parseOrLog('42', log), 42, reason: '--err-t1--');
    expect(log, isEmpty, reason: '--err-t1--');
  });
```

`parseOrLog('oops', log)` deve lanciare una `FormatException`

```dart
  test('test2', () {
    expect(() => parseOrLog('oops', <String>[]), throwsFormatException, reason: '--err-t2--');
  });
```

Il log deve raccogliere una riga per ogni errore, e i valori buoni non devono essere registrati

```dart
  test('test3', () {
    final log = <String>[];
    expect(() => parseOrLog('oops', log), throwsFormatException, reason: '--err-t3--');
    expect(log, ['bad input: oops'], reason: '--err-t3--');
    expect(parseOrLog('7', log), 7, reason: '--err-t3--');
    expect(() => parseOrLog('x', log), throwsFormatException, reason: '--err-t3--');
    expect(log, ['bad input: oops', 'bad input: x'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int parseOrLog(String text, List<String> log) {
  try {
    return int.parse(text);
  } on FormatException {
    log.add('bad input: $text');
    rethrow;
  }
}
```
