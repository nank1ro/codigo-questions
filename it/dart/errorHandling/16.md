---
language: dart
exerciseType: 1
---

# --description--

L'ultimo pezzo è lanciare un `Error` di proposito. Una funzione che documenta cosa accetta dovrebbe rifiutare tutto il resto con forza, e `ArgumentError` è l'oggetto fatto per questo:

```dart
int setVolume(int level) {
  if (level < 0 || level > 100) {
    throw ArgumentError('level must be between 0 and 100');
  }
  return level;
}
```

Il messaggio è raggiungibile come `e.message`, e `toString()` stampa `Invalid argument(s): ` seguito dal messaggio.

Questo non contraddice la regola di prima. Lanciare un `ArgumentError` è giusto, catturarne uno no: dice all'autore *del chiamante* che la chiamata stessa è sbagliata, e la correzione è un controllo prima della chiamata, non un gestore attorno ad essa.

# --instructions--

Scrivi una funzione `checkAge` che prende un `int` `age` e lo restituisce, ma lancia `ArgumentError('age must be between 0 and 120')` quando l'età è sotto `0` o sopra `120`.

Esempio:
```dart
print(checkAge(30)); // 30
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`checkAge(30)` deve restituire `30`, e le due estremità dell'intervallo devono essere accettate

```dart
  test('test1', () {
    expect(checkAge(30), 30, reason: '--err-t1--');
    expect(checkAge(0), 0, reason: '--err-t1--');
    expect(checkAge(120), 120, reason: '--err-t1--');
  });
```

`checkAge(-1)` e `checkAge(121)` devono lanciare un `ArgumentError`

```dart
  test('test2', () {
    expect(() => checkAge(-1), throwsArgumentError, reason: '--err-t2--');
    expect(() => checkAge(121), throwsArgumentError, reason: '--err-t2--');
  });
```

L'errore lanciato deve portare con sé il messaggio `'age must be between 0 and 120'`

```dart
  test('test3', () {
    try {
      checkAge(200);
      fail('--err-t3--');
    } on ArgumentError catch (e) {
      expect(e.message, 'age must be between 0 and 120', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int checkAge(int age) {
  if (age < 0 || age > 120) {
    throw ArgumentError('age must be between 0 and 120');
  }
  return age;
}
```
