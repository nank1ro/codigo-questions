---
language: dart
exerciseType: 4
---

# --description--

Un isolate avviato segue sempre gli stessi quattro passi: apri la casella postale, avvia il worker con il suo indirizzo, aspetta la risposta, usala.

```dart
import 'dart:isolate';

void worker(SendPort port) {
  port.send('done');
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

L'`await` davanti a `Isolate.spawn` aspetta che l'isolate *parta*, non che finisca il suo lavoro: il risultato arriva più tardi, attraverso la porta.

# --instructions--

Ordina gli elementi per costruire un programma che avvia un isolate `worker` che rimanda `7 * 6` all'isolate main, che lo stampa. Metti l'import per primo e definisci `worker` prima di `main`. Il programma deve stampare `42`.

# --answers--

```dart
import 'dart:isolate';
```

```dart
void worker(SendPort port) {
```

```dart
  port.send(7 * 6);
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  final receivePort = ReceivePort();
```

```dart
  await Isolate.spawn(worker, receivePort.sendPort);
```

```dart
  print(await receivePort.first);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
void worker(SendPort port) {
  port.send(7 * 6);
}
Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

# --output--

42
