---
language: dart
exerciseType: 2
---

# --description--

`first` legge un messaggio e chiude la casella postale. Una `ReceivePort` è anche uno **`Stream`**, quindi per leggere molti messaggi la scorri con `await for`.

Il ciclo non finisce mai da solo: la porta resta aperta ad aspettare un messaggio che potrebbe non arrivare mai. Il worker invia quindi un ultimo valore come segnale, spesso `null`, e chi lo ascolta reagisce chiamando **`close()`**, che termina lo stream e il ciclo:

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(countdown, receivePort.sendPort);
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --instructions--

Completa il codice in modo che l'isolate avviato invii `3`, `2`, `1` e poi `null`, e che l'isolate main stampi ogni numero, chiuda la casella postale su `null` e alla fine stampi `done`.

# --seed--

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.[/](countdown, receivePort.sendPort);
  await [/] (final message in receivePort) {
    if (message == null) {
      receivePort.[/]();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --answers--

- spawn
- for
- close
- run
- while
- listen
- first

# --solutions--

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(countdown, receivePort.sendPort);
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --output--

3
2
1
done
