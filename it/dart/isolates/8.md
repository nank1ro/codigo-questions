---
language: dart
exerciseType: 1
---

# --description--

`Isolate.spawn` passa esattamente **un** messaggio al punto di ingresso, e il worker di solito ha bisogno sia di una `SendPort` su cui rispondere sia di qualche dato su cui lavorare. Il trucco abituale è impacchettare tutto in una `List` e spacchettarla dall'altra parte:

```dart
void multiply(List<Object> message) {
  final port = message[0] as SendPort;
  final value = message[1] as int;
  port.send(value * 2);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(multiply, <Object>[receivePort.sendPort, 21]);
  print(await receivePort.first); // 42
}
```

La lista viene copiata all'andata, quindi il worker legge i propri valori. Una `SendPort` è una delle poche cose che non viene copiata ma condivisa: continua a puntare alla casella postale dell'isolate che l'ha creata, ed è esattamente per questo che può essere usata come indirizzo di risposta.

# --instructions--

Scrivi una funzione top-level `sendSum` che prende una `List<Object>` `message` in cui l'elemento `0` è una `SendPort` e gli elementi `1` e `2` sono `int`, e ne invia la somma alla porta.

Poi scrivi una funzione `addInIsolate` che prende due `int` `a` e `b` e restituisce un `Future<int>` che si completa con la somma calcolata da un isolate `sendSum` avviato.

Esempio:
```dart
print(await addInIsolate(2, 3)); // 5
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendSum` dovrebbe inviare alla porta la somma dei due numeri del messaggio

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendSum(<Object>[port.sendPort, 4, 6]);
    expect(await port.first, 10, reason: '--err-t1--');
  });
```

`addInIsolate(2, 3)` dovrebbe completarsi con `5`

```dart
  test('test2', () async {
    expect(await addInIsolate(2, 3), 5, reason: '--err-t2--');
  });
```

`addInIsolate(10, -4)` dovrebbe completarsi con `6` e `addInIsolate` dovrebbe restituire un `Future<int>`

```dart
  test('test3', () async {
    expect(await addInIsolate(10, -4), 6, reason: '--err-t3--');
    expect(addInIsolate(1, 1), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendSum(List<Object> message) {
  final port = message[0] as SendPort;
  final a = message[1] as int;
  final b = message[2] as int;
  port.send(a + b);
}

Future<int> addInIsolate(int a, int b) async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendSum, <Object>[receivePort.sendPort, a, b]);
  return await receivePort.first as int;
}
```
