---
language: dart
exerciseType: 1
---

# --description--

Ogni `Isolate.run` avvia il suo isolate, quindi parecchi di loro girano davvero nello stesso istante, su tutti i core di cui la macchina dispone. Lo schema è quello che conosci già dai future: avvia prima ogni calcolo, poi aspettali tutti con `Future.wait`, che mantiene i risultati nell'ordine dell'input.

```dart
Future<List<int>> doubleAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * 2)));
}
```

Avviare un isolate non è gratuito: costa memoria e qualche millisecondo. Spezzare un calcolo lungo su una manciata di isolate ripaga, mandare mille addizioni banali a mille isolate no.

# --instructions--

Scrivi una funzione `squareAll` che prende una `List<int>` `numbers` e restituisce un `Future<List<int>>` che si completa con il quadrato di ogni numero, nello stesso ordine, con ogni quadrato calcolato sul suo isolate.

Esempio:
```dart
print(await squareAll([1, 2, 3])); // [1, 4, 9]
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`squareAll([1, 2, 3])` dovrebbe completarsi con `[1, 4, 9]`

```dart
  test('test1', () async {
    expect(await squareAll([1, 2, 3]), [1, 4, 9], reason: '--err-t1--');
  });
```

`squareAll([-4, 5])` dovrebbe completarsi con `[16, 25]`, mantenendo l'ordine dell'input

```dart
  test('test2', () async {
    expect(await squareAll([-4, 5]), [16, 25], reason: '--err-t2--');
  });
```

`squareAll([])` dovrebbe completarsi con una lista vuota e `squareAll` dovrebbe restituire un `Future<List<int>>`

```dart
  test('test3', () async {
    expect(await squareAll(<int>[]), <int>[], reason: '--err-t3--');
    expect(squareAll([2]), isA<Future<List<int>>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<List<int>> squareAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * n)));
}
```
