---
language: dart
exerciseType: 4
---

# --description--

Messo insieme, un programma con `Isolate.run` si legge come normale codice sequenziale: la riga prima della chiamata gira sull'isolate main, il calcolo gira altrove, e la riga dopo l'`await` torna a girare sull'isolate main con il risultato in mano.

```dart
import 'dart:isolate';

int twice(int n) => n * 2;

Future<void> main() async {
  print('start');
  final result = await Isolate.run(() => twice(4));
  print(result);
}
// start
// 8
```

# --instructions--

Ordina gli elementi per costruire un programma che stampa `start`, eleva al quadrato `9` su un altro isolate con `Isolate.run` e stampa il risultato. Metti l'import per primo e definisci `square` prima di `main`. Il programma deve stampare:

```
start
81
```

# --answers--

```dart
import 'dart:isolate';
```

```dart
int square(int n) => n * n;
```

```dart
Future<void> main() async {
```

```dart
  print('start');
```

```dart
  final result = await Isolate.run(() => square(9));
```

```dart
  print(result);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
int square(int n) => n * n;
Future<void> main() async {
  print('start');
  final result = await Isolate.run(() => square(9));
  print(result);
}
```

# --output--

start
81
