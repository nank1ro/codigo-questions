---
language: dart
exerciseType: 1
---

# --description--

I valori di un enum sono ottime **chiavi di mappa**: sono unici, facili da confrontare, e il compilatore verifica che tu usi solo valori reali. Dichiara la mappa con l'enum come tipo di chiave e cerca i valori con `[]`:

```dart
enum Direction { north, south, east, west }

Map<Direction, String> arrows = {
  Direction.north: '^',
  Direction.south: 'v',
  Direction.east: '>',
  Direction.west: '<',
};

print(arrows[Direction.east]); // >
```

Come con qualsiasi mappa, una ricerca restituisce `null` quando la chiave manca, quindi usa `??` per fornire un valore di riserva.

# --instructions--

Dato `enum Size { small, medium, large }`, dichiara una `Map<Size, int>` di livello superiore chiamata `prices` dove `small` costa `2`, `medium` costa `3` e `large` costa `4`. Scrivi poi una funzione `totalPrice(List<Size> order)` che restituisca la somma dei prezzi di ogni dimensione in `order`.

# --seed--

```dart
enum Size { small, medium, large }
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`prices[Size.medium]` dovrebbe essere `3`

```dart
  test('test1', () {
    expect(prices[Size.medium], 3, reason: '--err-t1--');
  });
```

`totalPrice([Size.small, Size.large])` dovrebbe essere `6`

```dart
  test('test2', () {
    expect(totalPrice([Size.small, Size.large]), 6, reason: '--err-t2--');
  });
```

`totalPrice([Size.medium, Size.medium, Size.small])` dovrebbe essere `8`

```dart
  test('test3', () {
    expect(totalPrice([Size.medium, Size.medium, Size.small]), 8, reason: '--err-t3--');
  });
```

`totalPrice([])` dovrebbe essere `0`

```dart
  test('test4', () {
    expect(totalPrice([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Size { small, medium, large }

Map<Size, int> prices = {
  Size.small: 2,
  Size.medium: 3,
  Size.large: 4,
};

int totalPrice(List<Size> order) {
  int total = 0;
  for (var size in order) {
    total += prices[size] ?? 0;
  }
  return total;
}
```
