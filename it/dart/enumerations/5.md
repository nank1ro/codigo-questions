---
language: dart
exerciseType: 4
---

# --description--

Ciclare su `values` è il modo abituale per elaborare ogni valore di un enum. Dentro al ciclo il valore corrente si comporta come qualsiasi altro oggetto, quindi puoi leggere il suo `index` e il suo `name` e usarli direttamente in un'interpolazione di stringa:

```dart
enum Direction { north, south, east, west }

for (var direction in Direction.values) {
  print('${direction.index}: ${direction.name}');
}
```

# --instructions--

Ordina gli elementi per dichiarare l'enum `Level` e stampare, per ogni valore, il suo indice e il suo nome. Dichiara l'enum prima di `main`. Il programma deve stampare:

```
0: low
1: medium
2: high
```

# --answers--

```dart
enum Level { low, medium, high }
```

```dart
void main() {
```

```dart
  for (var level in Level.values) {
```

```dart
    print('${level.index}: ${level.name}');
```

```dart
  }
}
```

# --solutions--

```dart
enum Level { low, medium, high }
void main() {
  for (var level in Level.values) {
    print('${level.index}: ${level.name}');
  }
}
```

# --output--

0: low
1: medium
2: high
