---
language: dart
exerciseType: 2
---

# --description--

A partire da Dart 3, uno `switch` può essere usato anche come **espressione** che produce un valore. Ogni case è scritto come `pattern => value` e i case sono separati da virgole, senza la parola chiave `case` e senza `break`:

```dart
enum Direction { north, south, east, west }

String arrow(Direction direction) => switch (direction) {
      Direction.north => '^',
      Direction.south => 'v',
      Direction.east => '>',
      Direction.west => '<',
    };
```

Come nella forma istruzione, un'espressione switch su un enum deve essere esaustiva.

# --instructions--

Completa l'espressione switch in modo che `label` restituisca la prima lettera di ogni dimensione, poi stampa l'etichetta di `Size.large`.

# --seed--

```dart
enum Size { small, medium, large }

String label(Size size) => [/] (size) {
      Size.small [/] 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --answers--

- switch
- =>
- case
- when
- ->
- :

# --solutions--

```dart
enum Size { small, medium, large }

String label(Size size) => switch (size) {
      Size.small => 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --output--

L
