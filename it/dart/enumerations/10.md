---
language: dart
exerciseType: 1
---

# --description--

A partire da Dart 2.17, un enum può dichiarare **campi** e un **costruttore**, proprio come una classe. Questo si chiama *enum avanzato*. Ogni valore passa quindi i propri argomenti al costruttore:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);
}

print(Planet.mars.moons); // 2
```

Nota le tre regole: la lista dei valori termina con un **punto e virgola** `;`, i campi devono essere `final`, e il costruttore deve essere `const`.

# --instructions--

Dichiara un enum `Coin` con un campo `final int` chiamato `cents` e i valori `penny(1)`, `nickel(5)`, `dime(10)` e `quarter(25)`, in quest'ordine.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Coin.penny.cents` dovrebbe essere `1` e `Coin.nickel.cents` dovrebbe essere `5`

```dart
  test('test1', () {
    expect(Coin.penny.cents, 1, reason: '--err-t1--');
    expect(Coin.nickel.cents, 5, reason: '--err-t2--');
  });
```

`Coin.dime.cents` dovrebbe essere `10`

```dart
  test('test2', () {
    expect(Coin.dime.cents, 10, reason: '--err-t3--');
  });
```

`Coin.quarter.cents` dovrebbe essere `25`

```dart
  test('test3', () {
    expect(Coin.quarter.cents, 25, reason: '--err-t4--');
  });
```

`Coin.values` dovrebbe avere 4 valori, con `penny` per primo

```dart
  test('test4', () {
    expect(Coin.values.length, 4, reason: '--err-t5--');
    expect(Coin.values[0], Coin.penny, reason: '--err-t6--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Coin {
  penny(1),
  nickel(5),
  dime(10),
  quarter(25);

  final int cents;

  const Coin(this.cents);
}
```
