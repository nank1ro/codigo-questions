---
language: dart
exerciseType: 1
---

# --description--

Un enum può **implementare un'interfaccia** con la parola chiave `implements`. L'enum promette quindi di fornire ogni membro dichiarato dall'interfaccia, e i suoi valori possono essere usati ovunque sia previsto quel tipo di interfaccia:

```dart
abstract class Describable {
  String describe();
}

enum Animal implements Describable {
  dog,
  cat;

  @override
  String describe() => 'I am a $name';
}

Describable pet = Animal.cat;
print(pet.describe()); // I am a cat
```

Un getter dichiarato nell'interfaccia può essere implementato sia con un getter sia con un campo `final` dello stesso nome.

# --instructions--

Dichiara un enum `Ticket` che implementi `Priced`, con un campo `final int` chiamato `price` e i valori `child(5)`, `adult(12)` e `senior(8)`, in quest'ordine. Scrivi poi una funzione `total(List<Priced> items)` che restituisca la somma dei prezzi di tutti gli elementi.

# --seed--

```dart
abstract class Priced {
  int get price;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Ticket.adult.price` dovrebbe essere `12`

```dart
  test('test1', () {
    expect(Ticket.adult.price, 12, reason: '--err-t1--');
  });
```

Un `Ticket` può essere usato come un `Priced`

```dart
  test('test2', () {
    Priced item = Ticket.senior;
    expect(item.price, 8, reason: '--err-t2--');
  });
```

`total([Ticket.child, Ticket.adult, Ticket.senior])` dovrebbe essere `25`

```dart
  test('test3', () {
    expect(total([Ticket.child, Ticket.adult, Ticket.senior]), 25, reason: '--err-t3--');
  });
```

`total([])` dovrebbe essere `0`

```dart
  test('test4', () {
    expect(total([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
abstract class Priced {
  int get price;
}

enum Ticket implements Priced {
  child(5),
  adult(12),
  senior(8);

  @override
  final int price;

  const Ticket(this.price);
}

int total(List<Priced> items) {
  int sum = 0;
  for (var item in items) {
    sum += item.price;
  }
  return sum;
}
```
