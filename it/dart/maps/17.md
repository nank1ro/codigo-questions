---
language: dart
exerciseType: 4
---

# --instructions--

Ordina le righe di codice per contare quante volte appare ogni parola in una lista, usando `.update()` con `ifAbsent`, e stampare la mappa risultante.

# --answers--

```dart
void main() {
```

```dart
  var counts = <String, int>{};
```

```dart
  for (var word in ['a', 'b', 'a']) {
```

```dart
    counts.update(word, (n) => n + 1, ifAbsent: () => 1);
```

```dart
  }
```

```dart
  print(counts);
```

```dart
}
```

# --solutions--

```dart
void main() {
  var counts = <String, int>{};
  for (var word in ['a', 'b', 'a']) {
    counts.update(word, (n) => n + 1, ifAbsent: () => 1);
  }
  print(counts);
}
```

# --output--

{a: 2, b: 1}
