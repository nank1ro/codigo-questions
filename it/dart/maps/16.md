---
language: dart
exerciseType: 1
---

# --description--

Il metodo `.update(key, update)` sostituisce il valore di una chiave esistente. Il secondo argomento è una funzione che riceve il valore attuale e restituisce quello nuovo. Se la chiave manca, `.update()` genera un errore, a meno che tu non passi una funzione `ifAbsent` che produce il valore iniziale:

```dart
var stock = {'apple': 3};
stock.update('apple', (n) => n + 1); // apple diventa 4
stock.update('kiwi', (n) => n + 1, ifAbsent: () => 1); // kiwi viene aggiunto con 1
print(stock); // {apple: 4, kiwi: 1}
```

# --instructions--

Data la mappa `stock`, usa `.update()` per raddoppiare il valore di `'apple'`, poi usa `.update()` con `ifAbsent` sulla chiave `'kiwi'` in modo che venga aggiunta con il valore `5`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var stock = {'apple': 3, 'pear': 1};
```

# --after-seed--

```dart
  return stock;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`stock` dovrebbe essere `{'apple': 6, 'pear': 1, 'kiwi': 5}`

```dart
  test('test1', () {
    expect(result(), {'apple': 6, 'pear': 1, 'kiwi': 5}, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var stock = {'apple': 3, 'pear': 1};
stock.update('apple', (n) => n * 2);
stock.update('kiwi', (n) => n * 2, ifAbsent: () => 5);
```
