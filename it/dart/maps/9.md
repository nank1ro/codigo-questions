---
language: dart
exerciseType: 1
---

# --description--

Una mappa letterale vuota `{}` non ha coppie da cui dedurre i tipi, quindi assegnale tipi espliciti con `<K, V>{}` o con un'annotazione di tipo:

```dart
var cart = <String, int>{};
Map<String, int> other = {};
```

La proprietà `.isEmpty` è `true` quando una mappa non ha coppie, e `.isNotEmpty` è `true` quando ne ha almeno una:

```dart
print(cart.isEmpty); // true
cart['pen'] = 2;
print(cart.isNotEmpty); // true
```

# --instructions--

Crea una `Map<String, int>` vuota chiamata `cart`. Memorizza se è `.isEmpty` in un `bool` chiamato `wasEmpty`, poi aggiungi la chiave `'pen'` con valore `2` e memorizza se è `.isNotEmpty` in un `bool` chiamato `hasItems`.

# --before-seed--

```dart
dynamic result() {
```

# --after-seed--

```dart
  return [wasEmpty, hasItems, cart];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`wasEmpty` dovrebbe essere `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasItems` dovrebbe essere `true`

```dart
  test('test2', () {
    expect(result()[1], true, reason: '--err-t2--');
  });
```

`cart` dovrebbe essere `{'pen': 2}`

```dart
  test('test3', () {
    expect(result()[2], {'pen': 2}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var cart = <String, int>{};
bool wasEmpty = cart.isEmpty;
cart['pen'] = 2;
bool hasItems = cart.isNotEmpty;
```
