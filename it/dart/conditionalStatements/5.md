---
language: dart
exerciseType: 2
---

# --description--

L'**operatore ternario** `condition ? expr1 : expr2` è un modo compatto per scrivere una semplice espressione `if-else`.

```dart
int x = 10;
String label = x > 0 ? 'positive' : 'non-positive';
print(label); // positive
```

Se la condizione è `true`, viene usata `expr1`; altrimenti viene usata `expr2`.

# --instructions--

Completa l'espressione ternaria per assegnare `'Even'` a `parity` quando `n` è divisibile per `2`, e `'Odd'` altrimenti.

# --seed--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 [/] 'Even' [/] 'Odd';
  print(parity);
}
```

# --answers--

- ?
- :
- ==
- ;

# --solutions--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 ? 'Even' : 'Odd';
  print(parity);
}
```

# --output--

Even
