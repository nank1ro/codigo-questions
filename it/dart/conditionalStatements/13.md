---
language: dart
exerciseType: 1
---

# --instructions--

Scrivi una funzione `classify(int score)` che restituisce `'A'` quando `score >= 90`, `'B'` quando `score >= 70`, `'C'` quando `score >= 50`, e `'F'` altrimenti, usando una catena `else if`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`classify(95)` deve restituire `'A'`

```dart
  test('test1', () {
    expect(classify(95), 'A', reason: '--err-t1--');
  });
```

`classify(75)` deve restituire `'B'`

```dart
  test('test2', () {
    expect(classify(75), 'B', reason: '--err-t2--');
  });
```

`classify(55)` deve restituire `'C'`

```dart
  test('test3', () {
    expect(classify(55), 'C', reason: '--err-t3--');
  });
```

`classify(30)` deve restituire `'F'`

```dart
  test('test4', () {
    expect(classify(30), 'F', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String classify(int score) {
  if (score >= 90) {
    return 'A';
  } else if (score >= 70) {
    return 'B';
  } else if (score >= 50) {
    return 'C';
  } else {
    return 'F';
  }
}

void main() {
  print(classify(95));
}
```
