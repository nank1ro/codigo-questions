---
language: dart
exerciseType: 1
---

# --instructions--

Scrivi una funzione `describe(int n)` che restituisce `'positive'` quando `n` è maggiore di `0`, e `'non-positive'` altrimenti, usando `if-else`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`describe(10)` deve restituire `'positive'`

```dart
  test('test1', () {
    expect(describe(10), 'positive', reason: '--err-t1--');
  });
```

`describe(0)` deve restituire `'non-positive'`

```dart
  test('test2', () {
    expect(describe(0), 'non-positive', reason: '--err-t2--');
  });
```

`describe(-5)` deve restituire `'non-positive'`

```dart
  test('test3', () {
    expect(describe(-5), 'non-positive', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String describe(int n) {
  if (n > 0) {
    return 'positive';
  } else {
    return 'non-positive';
  }
}

void main() {
  print(describe(10));
}
```
