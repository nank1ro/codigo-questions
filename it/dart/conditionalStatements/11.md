---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara una variabile `bool` di livello superiore chiamata `isHot`. Assegnale `true` quando la variabile `int` di livello superiore `temp` (impostata a `35`) è maggiore di `30`, usando un'istruzione `if`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`temp` deve essere uguale a `35`

```dart
  test('test1', () {
    expect(temp, 35, reason: '--err-t1--');
  });
```

`isHot` deve essere `true`

```dart
  test('test2', () {
    expect(isHot, true, reason: '--err-t2--');
    expect(isHot, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int temp = 35;
bool isHot = false;

void main() {
  if (temp > 30) {
    isHot = true;
  }
  print(isHot);
}
```
