---
language: dart
exerciseType: 1
---

# --instructions--

Dichiara una variabile `bool` chiamata `isHot`. Assegnale `true` quando la variabile `int` `temp` (impostata a `35`) è maggiore di `30`, usando un'istruzione `if`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int temp = 35;
bool isHot = false;
```

# --after-seed--

```dart
  return isHot;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHot` deve essere `true`

```dart
  test('test1', () {
    expect(result(), true, reason: '--err-t1--');
    expect(result(), TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int temp = 35;
bool isHot = false;
if (temp > 30) {
  isHot = true;
}
```
