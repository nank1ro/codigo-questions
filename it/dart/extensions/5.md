---
language: dart
exerciseType: 1
---

# --description--

Un'estensione su `List<int>` non può essere usata su una `List<String>`. Per scrivere un'estensione che funziona con **qualsiasi** tipo di elemento, dai all'estensione un **parametro di tipo**, scritto tra parentesi angolari dopo il nome, e usalo nel tipo dopo `on`:

```dart
extension Firsts<T> on List<T> {
  T get firstOrLast => length > 1 ? this[0] : this[length - 1];
}
```

`T` è un segnaposto per "qualunque sia il tipo dell'elemento": su una `List<int>` diventa `int`, su una `List<String>` diventa `String`, così il getter qui sopra restituisce un `int` oppure una `String` di conseguenza. Il compilatore riempie `T` per te a ogni chiamata.

```dart
print([7, 8, 9].firstOrLast); // 7
print(['a', 'b'].firstOrLast); // a
```

# --instructions--

Scrivi un'estensione denominata `Seconds` con un parametro di tipo `T` su `List<T>`, con un getter `secondOrNull` di tipo `T?` che restituisce l'elemento all'indice `1`, oppure `null` quando la lista ha meno di due elementi.

Esempio:
```dart
print([10, 20, 30].secondOrNull); // 20
print(['solo'].secondOrNull);     // null
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`[10, 20, 30].secondOrNull` deve essere `20`

```dart
  test('test1', () {
    expect([10, 20, 30].secondOrNull, 20, reason: '--err-t1--');
  });
```

`['a', 'b'].secondOrNull` deve essere `'b'`

```dart
  test('test2', () {
    expect(['a', 'b'].secondOrNull, 'b', reason: '--err-t2--');
  });
```

`['solo'].secondOrNull` e `<int>[].secondOrNull` devono essere `null`

```dart
  test('test3', () {
    expect(['solo'].secondOrNull, null, reason: '--err-t3--');
    expect(<int>[].secondOrNull, null, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Seconds<T> on List<T> {
  T? get secondOrNull => length > 1 ? this[1] : null;
}
```
