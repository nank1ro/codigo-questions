---
language: dart
exerciseType: 2
---

# --description--

Un'estensione su `String` non può essere chiamata su una `String?`: il valore potrebbe essere `null`, e il compilatore rifiuta la chiamata. Se dichiari invece l'estensione sul tipo **nullable**, il metodo può essere chiamato direttamente su una `String?`, e al suo interno `this` ha tipo `String?`, quindi devi gestire tu il caso `null`, per esempio con `??`:

```dart
extension Defaults on int? {
  int orZero() => this ?? 0;
}

void main() {
  int? count = null;
  print(count.orZero()); // 0
  print(5.orZero());     // 5
}
```

Un `int` non nullable può essere passato dove è atteso un `int?`, quindi l'estensione funziona su entrambi.

# --instructions--

Completa l'estensione in modo che `orEmpty` possa essere chiamato su una stringa nullable e restituisca la stringa stessa, oppure la stringa vuota quando è `null`. Il programma deve stampare `[]` e `[Ada]`.

# --seed--

```dart
extension Defaults on [/] {
  String orEmpty() => this [/] '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --answers--

- String?
- ??
- String
- +
- ?.
- !

# --solutions--

```dart
extension Defaults on String? {
  String orEmpty() => this ?? '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --output--

[]
[Ada]
