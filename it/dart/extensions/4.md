---
language: dart
exerciseType: 4
---

# --description--

Il tipo dopo `on` può essere un tipo **parametrizzato** come `List<int>`. L'estensione si applica allora solo alle liste di quel tipo di elemento: `[1, 2].total()` funziona, `['a', 'b'].total()` non compila.

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}
```

All'interno dell'estensione, `this` è la lista, quindi puoi scorrerla con un ciclo, indicizzarla o chiamare `length` come al solito.

# --instructions--

Ordina gli elementi per dichiarare un'estensione `Totals` su `List<int>` con un metodo `total` che somma gli elementi usando un ciclo `for`, poi un `main` che stampa il totale di `[3, 4, 5]`. Dichiara l'estensione prima di `main`. Il programma deve stampare:

```
12
```

# --answers--

```dart
extension Totals on List<int> {
```

```dart
  int total() {
```

```dart
    var sum = 0;
```

```dart
    for (final n in this) {
```

```dart
      sum += n;
```

```dart
    }
```

```dart
    return sum;
```

```dart
  }
```

```dart
}
```

```dart
void main() {
```

```dart
  print([3, 4, 5].total());
```

```dart
}
```

# --solutions--

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}

void main() {
  print([3, 4, 5].total());
}
```

# --output--

12
