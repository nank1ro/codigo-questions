---
language: dart
exerciseType: 4
---

# --description--

Le estensioni non servono solo per i tipi dell'SDK: puoi estendere anche le **tue classi**. È utile quando la classe proviene da un pacchetto che non controlli, o quando vuoi tenere la classe piccola e aggiungere helper opzionali accanto al codice che li usa.

```dart
class Circle {
  final double radius;
  Circle(this.radius);
}

extension CircleMath on Circle {
  double get diameter => radius * 2;
}

void main() {
  print(Circle(3).diameter); // 6.0
}
```

L'estensione vede i campi e i metodi pubblici della classe, esattamente come codice scritto fuori dalla classe.

# --instructions--

Ordina gli elementi per definire una classe `Rectangle` con `width` e `height`, poi un'estensione `RectangleMath` su `Rectangle` con un getter `area`, poi un `main` che stampa l'area di un rettangolo `4` per `2.5`. Definisci prima la classe, poi l'estensione, poi `main`. Dentro la classe, dichiara entrambi i campi prima del costruttore. Il programma deve stampare:

```
10.0
```

# --answers--

```dart
class Rectangle {
```

```dart
  final double width;
```

```dart
  final double height;
```

```dart
  Rectangle(this.width, this.height);
```

```dart
}
```

```dart
extension RectangleMath on Rectangle {
```

```dart
  double get area => width * height;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Rectangle(4, 2.5).area);
```

```dart
}
```

# --solutions--

```dart
class Rectangle {
  final double width;
  final double height;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

```dart
class Rectangle {
  final double height;
  final double width;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

# --output--

10.0
