---
language: dart
exerciseType: 1
---

# --description--

Dart consente a un tipo di definire cosa significano operatori come `+`, `*` o `==` per i suoi valori, usando un metodo il cui nome è la parola chiave `operator` seguita dal simbolo. Il lato destro dell'operatore è il parametro del metodo:

```dart
extension Scaling on List<int> {
  List<int> operator *(int factor) => map((n) => n * factor).toList();
}

void main() {
  print([1, 2, 3] * 10); // [10, 20, 30]
}
```

Poiché le estensioni possono dichiarare operatori, puoi dare a un tipo esistente un operatore che non ha già. `String` ha `+` e `*`, ma non `-`, quindi un'estensione può definire cosa significa `'hello world' - 'o'`.

# --instructions--

Scrivi un'estensione denominata `Removing` su `String` con un operatore `-` che accetta un parametro `String` `part` e restituisce la stringa senza tutte le occorrenze di `part`. Usa `replaceAll`.

Esempio:
```dart
print('hello world' - 'o'); // hell wrld
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`'hello world' - 'o'` deve essere `'hell wrld'`

```dart
  test('test1', () {
    expect('hello world' - 'o', 'hell wrld', reason: '--err-t1--');
  });
```

`'banana' - 'an'` deve essere `'ba'`

```dart
  test('test2', () {
    expect('banana' - 'an', 'ba', reason: '--err-t2--');
  });
```

`'abc' - 'z'` deve essere `'abc'` e `'aaa' - 'a'` deve essere `''`

```dart
  test('test3', () {
    expect('abc' - 'z', 'abc', reason: '--err-t3--');
    expect('aaa' - 'a', '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Removing on String {
  String operator -(String part) => replaceAll(part, '');
}
```
