---
language: dart
exerciseType: 1
---

# --description--

Una closure non si limita a leggere le variabili che cattura: può anche **modificarle**, e le modifiche vengono conservate tra le chiamate. Questo rende possibile mantenere uno stato privato senza una classe:

```dart
int Function() makeTimer() {
  var seconds = 0;
  return () {
    seconds += 10;
    return seconds;
  };
}

var timer = makeTimer();
print(timer()); // 10
print(timer()); // 20
```

Ogni chiamata a `makeTimer()` crea una variabile `seconds` completamente nuova, quindi due timer non condividono mai il loro contatore.

# --instructions--

Scrivi una funzione `makeCounter` senza parametri che restituisce una funzione di tipo `int Function()`. Ogni volta che la funzione restituita viene chiamata deve incrementare un contatore catturato e restituirlo, partendo da `1`.

Esempio:
```dart
var counter = makeCounter();
print(counter()); // 1
print(counter()); // 2
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

La prima chiamata al contatore dovrebbe restituire `1`

```dart
  test('test1', () {
    var counter = makeCounter();
    expect(counter(), 1, reason: '--err-t1--');
  });
```

Tre chiamate allo stesso contatore dovrebbero restituire `1`, `2` e `3`

```dart
  test('test2', () {
    var counter = makeCounter();
    expect([counter(), counter(), counter()], [1, 2, 3], reason: '--err-t2--');
  });
```

Due contatori dovrebbero essere indipendenti

```dart
  test('test3', () {
    var a = makeCounter();
    var b = makeCounter();
    a();
    a();
    expect(b(), 1, reason: '--err-t3--');
    expect(a(), 3, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int Function() makeCounter() {
  var count = 0;
  return () {
    count++;
    return count;
  };
}
```
