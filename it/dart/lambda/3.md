---
language: dart
exerciseType: 2
---

# --description--

Quando il corpo è una singola espressione, una funzione anonima può usare la **sintassi freccia** `=>`, proprio come una funzione con nome. La freccia sostituisce le parentesi graffe e la parola chiave `return`:

```dart
var add = (int a, int b) => a + b;

print(add(2, 3)); // 5
```

Questa forma breve è di gran lunga il modo più comune per scrivere funzioni anonime in Dart.

# --instructions--

Completa il codice per memorizzare in `triple` una funzione freccia che restituisce il suo argomento moltiplicato per `3`, in modo che il programma stampi `12`.

# --seed--

```dart
void main() {
  var triple = (int n) [/] n [/] 3;
  print(triple(4));
}
```

# --answers--

- =>
- *
- ->
- +
- return
- {

# --solutions--

```dart
void main() {
  var triple = (int n) => n * 3;
  print(triple(4));
}
```

# --output--

12
