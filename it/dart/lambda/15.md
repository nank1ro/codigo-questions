---
language: dart
exerciseType: 1
---

# --description--

Una funzione anonima non deve essere memorizzata da nessuna parte: può essere **chiamata immediatamente** scrivendo gli argomenti subito dopo la sua parentesi graffa di chiusura. Questo è utile per calcolare un valore con alcune variabili temporanee che non devono disperdersi nel codice circostante:

```dart
var area = (double radius) {
  var pi = 3.14;
  return pi * radius * radius;
}(2.0);

print(area); // 12.56
```

Qui `pi` esiste solo all'interno della funzione anonima, e `area` riceve il valore restituito.

# --instructions--

Una variabile `int` `limit` è già definita. Dichiara una variabile `sum` inizializzata invocando immediatamente una funzione anonima senza parametri che somma tutti gli interi da `1` a `limit` con un ciclo e restituisce il totale.

# --before-seed--

```dart
dynamic result(int limit) {
```

# --seed--

```dart
var sum = 
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` dovrebbe essere un `int`

```dart
  test('test1', () {
    expect(result(100), isA<int>(), reason: '--err-t1--');
  });
```

`sum` dovrebbe essere `5050` quando `limit` è `100`

```dart
  test('test2', () {
    expect(result(100), 5050, reason: '--err-t2--');
  });
```

`sum` dovrebbe essere `55` quando `limit` è `10`

```dart
  test('test3', () {
    expect(result(10), 55, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var sum = () {
  var total = 0;
  for (var i = 1; i <= limit; i++) {
    total += i;
  }
  return total;
}();
```
