---
language: dart
exerciseType: 1
---

# --description--

Le funzioni che prendono e restituiscono altre funzioni possono essere combinate liberamente. Un classico esempio è la **composizione**: costruire una nuova funzione che esegue una funzione e poi passa il suo risultato a un'altra:

```dart
int Function(int) then(int Function(int) first, int Function(int) second) {
  return (n) => second(first(n));
}
```

# --instructions--

Scrivi una funzione `compose` che prende due parametri `f` e `g`, entrambi di tipo `int Function(int)`, e restituisce una funzione di tipo `int Function(int)` che applica `f` al suo argomento e poi `g` al risultato.

Esempio:
```dart
var addThenDouble = compose((n) => n + 1, (n) => n * 2);
print(addThenDouble(3)); // 8
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`compose((n) => n + 1, (n) => n * 2)(3)` dovrebbe essere `8`

```dart
  test('test1', () {
    expect(compose((n) => n + 1, (n) => n * 2)(3), 8, reason: '--err-t1--');
  });
```

`compose((n) => n * 2, (n) => n + 1)(3)` dovrebbe essere `7`

```dart
  test('test2', () {
    expect(compose((n) => n * 2, (n) => n + 1)(3), 7, reason: '--err-t2--');
  });
```

La funzione restituita dovrebbe avere tipo `int Function(int)`

```dart
  test('test3', () {
    var negate = compose((n) => -n, (n) => n * n);
    expect(negate, isA<int Function(int)>(), reason: '--err-t3--');
    expect(negate(5), 25, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int Function(int) compose(int Function(int) f, int Function(int) g) {
  return (int n) => g(f(n));
}
```
