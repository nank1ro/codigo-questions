---
language: dart
exerciseType: 4
---

# --description--

Le funzioni anonime supportano gli stessi tipi di parametri delle funzioni con nome: parametri **posizionali facoltativi** tra `[]` e parametri **con nome** tra `{}`, entrambi con valori predefiniti:

```dart
var repeat = (String text, [int times = 2]) => text * times;
print(repeat('ab'));    // abab
print(repeat('ab', 3)); // ababab

var describe = ({required String item, int count = 1}) => '$count x $item';
print(describe(item: 'apple'));           // 1 x apple
print(describe(item: 'pear', count: 4));  // 4 x pear
```

# --instructions--

Ordina le righe di codice per memorizzare in `greet` una funzione anonima con un parametro con nome `name` il cui valore predefinito è `'World'`, poi chiamala prima senza argomenti e poi con `name: 'Dart'`. Il programma deve stampare:

```
Hello, World!
Hello, Dart!
```

# --answers--

```dart
void main() {
```

```dart
  var greet = ({String name = 'World'}) {
```

```dart
    print('Hello, $name!');
```

```dart
  };
```

```dart
  greet();
```

```dart
  greet(name: 'Dart');
```

```dart
}
```

# --solutions--

```dart
void main() {
  var greet = ({String name = 'World'}) {
    print('Hello, $name!');
  };
  greet();
  greet(name: 'Dart');
}
```

# --output--

Hello, World!
Hello, Dart!
