---
language: dart
exerciseType: 1
---

# --description--

Poiché un tipo funzione è un tipo normale, una funzione può prendere **un'altra funzione come parametro**. Nel corpo, il parametro viene chiamato come qualsiasi funzione:

```dart
int apply(int n, int Function(int) operation) {
  return operation(n);
}

print(apply(5, (n) => n * 2)); // 10
print(apply(5, (n) => n - 1)); // 4
```

Qui chi chiama decide cosa fa `apply` passando una funzione anonima come secondo argomento.

# --instructions--

Scrivi una funzione `applyTwice` che prende un parametro `int` `n` e un parametro funzione `operation` di tipo `int Function(int)`, e restituisce il risultato dell'applicazione di `operation` a `n` due volte.

Esempio:
```dart
print(applyTwice(3, (n) => n * 2)); // 12
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`applyTwice(3, (n) => n * 2)` dovrebbe essere `12`

```dart
  test('test1', () {
    expect(applyTwice(3, (n) => n * 2), 12, reason: '--err-t1--');
  });
```

`applyTwice(10, (n) => n + 5)` dovrebbe essere `20`

```dart
  test('test2', () {
    expect(applyTwice(10, (n) => n + 5), 20, reason: '--err-t2--');
  });
```

`applyTwice(2, (n) => n * n)` dovrebbe essere `16`

```dart
  test('test3', () {
    expect(applyTwice(2, (n) => n * n), 16, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int applyTwice(int n, int Function(int) operation) {
  return operation(operation(n));
}
```
