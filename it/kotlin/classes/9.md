---
language: kotlin
exerciseType: 2
---

# --instructions--

Completa il codice per dichiarare una `data class` chiamata `Color` con proprietà `val` chiamate `red`, `green` e `blue` di tipo `Int`.

# --seed--

```kotlin
[/] [/] Color(val red: Int, val green: Int, val blue: Int)
```

# --answers--

- data
- class
- object
- open
- abstract
- sealed

# --solutions--

```kotlin
data class Color(val red: Int, val green: Int, val blue: Int)
```
