---
language: kotlin
exerciseType: 2
---

# --instructions--

Completa il codice per fare in modo che `Cat` erediti da `Animal` e sovrascriva il metodo `speak()` per restituire `"Meow"`.

# --seed--

```kotlin
open class Animal(val name: String) {
    open fun speak(): String = "..."
}

class Cat(name: String) [/] Animal(name) {
    [/] fun speak(): String = "Meow"
}
```

# --answers--

- :
- override
- extends
- open
- super
- implements

# --solutions--

```kotlin
open class Animal(val name: String) {
    open fun speak(): String = "..."
}

class Cat(name: String) : Animal(name) {
    override fun speak(): String = "Meow"
}
```
