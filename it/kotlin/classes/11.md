---
language: kotlin
exerciseType: 2
---

# --instructions--

Completa il codice per aggiungere un `companion object` all'interno di `AppConfig` che contenga una proprietà `val` chiamata `version` uguale a `"1.0"`.

# --seed--

```kotlin
class AppConfig {
    [/] [/] {
        val version = "1.0"
    }
}
```

# --answers--

- companion
- object
- class
- static
- init
- val

# --solutions--

```kotlin
class AppConfig {
    companion object {
        val version = "1.0"
    }
}
```
