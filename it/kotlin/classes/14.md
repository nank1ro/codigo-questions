---
language: kotlin
exerciseType: 4
---

# --instructions--

Riesci a ordinare queste righe per definire una `data class` `Student` e stampare il suo `toString()`?

# --answers--

```kotlin
data class Student(val name: String, val grade: Int)
```

```kotlin
fun main() {
```

```kotlin
    val s = Student("Alice", 90)
```

```kotlin
    println(s.toString())
```

```kotlin
}
```

# --solutions--

```kotlin
data class Student(val name: String, val grade: Int)
fun main() {
    val s = Student("Alice", 90)
    println(s.toString())
}
```

# --output--

Student(name=Alice, grade=90)
