---
language: kotlin
exerciseType: 1
---

# --description--

Gli array numerici dispongono di funzioni di aggregazione:
```kotlin
val nums = intArrayOf(3, 9, 1)
println(nums.sum())     // 13
println(nums.max())     // 9
println(nums.min())     // 1
println(nums.average()) // 4.333333333333333
println(nums.count())   // 3
```
`max()` e `min()` lanciano un'eccezione su un array vuoto, usa `maxOrNull()` e `minOrNull()` quando l'array potrebbe essere vuoto.

# --instructions--

Scrivi una funzione `range(numbers: IntArray): Int` che restituisca la differenza tra l'elemento più grande e quello più piccolo di `numbers`. Puoi assumere che l'array non sia mai vuoto.

Esempio: `range(intArrayOf(3, 9, 1))` restituisce `8`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`range(intArrayOf(3, 9, 1))` deve restituire `8`

```kotlin
    tryCatch(range(intArrayOf(3, 9, 1)) == 8)
```

`range(intArrayOf(5))` deve restituire `0`

```kotlin
    tryCatch(range(intArrayOf(5)) == 0)
```

`range(intArrayOf(-2, 4, 0))` deve restituire `6`

```kotlin
    tryCatch(range(intArrayOf(-2, 4, 0)) == 6)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun range(numbers: IntArray): Int {
    return numbers.max() - numbers.min()
}
```
