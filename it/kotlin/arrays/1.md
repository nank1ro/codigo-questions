---
language: kotlin
exerciseType: 2
---

# --description--

Un **array** memorizza un numero fisso di valori dello stesso tipo sotto un unico nome di variabile.
Ne crei uno con `arrayOf`, leggi un elemento con le parentesi quadre e un **indice** che parte da `0`, e ottieni il numero di elementi con `size`:
```kotlin
val colors = arrayOf("red", "green", "blue")
println(colors[0])   // red
println(colors.size) // 3
```
L'ultimo elemento si trova all'indice `size - 1`.

# --instructions--

Completa il codice per creare l'array con `arrayOf`, stampare il suo numero di elementi, poi il suo ultimo elemento

# --seed--

```kotlin
val numbers = [/](3, 5, 7)
println(numbers.[/])
println(numbers[[/]])
```

# --answers--

- arrayOf
- Array
- size
- length
- count
- 2
- 3

# --solutions--

```kotlin
val numbers = arrayOf(3, 5, 7)
println(numbers.size)
println(numbers[2])
```

# --output--

3
7
