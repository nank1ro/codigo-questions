---
language: kotlin
exerciseType: 2
---

# --description--

`uppercase()` restituisce una copia della stringa con ogni lettera in maiuscolo, `lowercase()` fa il contrario.
La stringa originale non viene modificata:
```kotlin
val name = "Kotlin"
println(name.uppercase()) // KOTLIN
println(name.lowercase()) // kotlin
println(name)             // Kotlin
```

# --instructions--

Completa il codice per stampare `word` in maiuscolo e poi `shout` in minuscolo

# --seed--

```kotlin
val word = "quiet"
val shout = "LOUD"
println(word.[/]())
println(shout.[/]())
```

# --answers--

- uppercase
- lowercase
- upper
- lower
- titlecase

# --solutions--

```kotlin
val word = "quiet"
val shout = "LOUD"
println(word.uppercase())
println(shout.lowercase())
```

# --output--

QUIET
loud
