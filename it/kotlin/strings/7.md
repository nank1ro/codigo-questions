---
language: kotlin
exerciseType: 2
---

# --description--

`split` divide una stringa in una `List` di pezzi attorno a un separatore, mentre `joinToString` fa il contrario: unisce gli elementi di una collezione in un'unica stringa con il separatore che scegli:
```kotlin
val csv = "a,b,c"
val parts = csv.split(",")
println(parts)                  // [a, b, c]
println(parts.joinToString("/")) // a/b/c
```

# --instructions--

Completa il codice per dividere `csv` sulla virgola e stampare i colori separati da `" - "`

# --seed--

```kotlin
val csv = "red,green,blue"
val colors = csv.[/](",")
println(colors.[/](" - "))
```

# --answers--

- split
- joinToString
- join
- separate
- substring

# --solutions--

```kotlin
val csv = "red,green,blue"
val colors = csv.split(",")
println(colors.joinToString(" - "))
```

# --output--

red - green - blue
