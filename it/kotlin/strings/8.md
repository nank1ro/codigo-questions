---
language: kotlin
exerciseType: 4
---

# --description--

Dato che `split` restituisce una `List`, puoi scorrere i suoi elementi come qualsiasi altra lista:
```kotlin
for (part in "a-b".split("-")) {
    println(part)
}
// prints a, then b
```

# --instructions--

Riesci a ordinare queste righe per stampare ogni parola di `sentence` in maiuscolo, una per riga?

# --answers--

```kotlin
val sentence = "the quick brown fox"
```

```kotlin
val words = sentence.split(" ")
```

```kotlin
for (word in words) {
```

```kotlin
    println(word.uppercase())
```

```kotlin
}
```

# --solutions--

```kotlin
val sentence = "the quick brown fox"
val words = sentence.split(" ")
for (word in words) {
    println(word.uppercase())
}
```

# --output--

THE
QUICK
BROWN
FOX
