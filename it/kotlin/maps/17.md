---
language: kotlin
exerciseType: 2
---

# --description--

`map` trasforma ogni elemento di una mappa in un nuovo elemento. A differenza di `filter`, il risultato è una `List`, non una `Map`:
```kotlin
val ages = mapOf("Alice" to 30, "Bob" to 25)
val labels = ages.map { (name, age) -> "$name: $age" }
println(labels) // [Alice: 30, Bob: 25]
```

# --instructions--

Completa il codice in modo da costruire una lista di stringhe nella forma `"name is age"`, una per ogni elemento di `ages`

# --seed--

```kotlin
val ages = mapOf("Ann" to 30, "Ben" to 25)
val labels = ages.[/] { ([/], [/]) -> "$name is $age" }
println(labels)
```

# --answers--

- map
- filter
- forEach
- name
- age
- key
- value

# --solutions--

```kotlin
val ages = mapOf("Ann" to 30, "Ben" to 25)
val labels = ages.map { (name, age) -> "$name is $age" }
println(labels)
```

# --output--

[Ann is 30, Ben is 25]
