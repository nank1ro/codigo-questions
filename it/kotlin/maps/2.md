---
language: kotlin
exerciseType: 1
---

# --description--

Per leggere un valore, indicizzi la mappa con la sua chiave, usando le parentesi quadre oppure la funzione `get`:
```kotlin
val ages = mapOf("Alice" to 30, "Bob" to 25)
println(ages["Alice"])    // 30
println(ages.get("Bob"))  // 25
```

Se la chiave non è presente il risultato è `null`, quindi il tipo di `ages["Alice"]` è `Int?`, non `Int`:
```kotlin
println(ages["Zoe"]) // null
```

# --instructions--

Scrivi una funzione `priceOf(prices: Map<String, Int>, item: String): Int?` che restituisca il prezzo memorizzato per `item`, oppure `null` se l'elemento non è nella mappa.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

Il prezzo di `"apple"` in `{apple=2, bread=3}` deve essere `2`

```kotlin
    val prices = mapOf("apple" to 2, "bread" to 3)
    tryCatch(priceOf(prices, "apple") == 2)
```

Il prezzo di `"bread"` deve essere `3`

```kotlin
    tryCatch(priceOf(prices, "bread") == 3)
```

Il prezzo di un elemento mancante deve essere `null`

```kotlin
    tryCatch(priceOf(prices, "kiwi") == null)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun priceOf(prices: Map<String, Int>, item: String): Int? {
    return prices[item]
}
```
