---
language: kotlin
exerciseType: 1
---

# --description--

Una mappa creata con `mapOf` è di sola lettura. Per aggiungere o modificare elementi usa `mutableMapOf`, che restituisce una `MutableMap`:
```kotlin
val ages = mutableMapOf("Alice" to 30)
ages["Bob"] = 25   // adds a new entry
ages["Alice"] = 31 // updates the existing one
println(ages)
// prints {Alice=31, Bob=25}
```
L'assegnazione con `map[key] = value` aggiunge la coppia quando la chiave è nuova e sostituisce il valore quando la chiave esiste già. Puoi anche chiamare `ages.put("Bob", 25)`, che fa la stessa cosa.

# --instructions--

Scrivi una funzione `setGrade(grades: MutableMap<String, Int>, name: String, grade: Int)` che memorizzi `grade` sotto `name`, aggiungendo l'elemento se non esiste o sostituendolo se esiste già.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

Dopo `setGrade(grades, "Ben", 90)` la mappa deve contenere `Ben` con voto `90`

```kotlin
    val grades = mutableMapOf("Ann" to 80)
    setGrade(grades, "Ben", 90)
    tryCatch(grades["Ben"] == 90)
```

Dopo `setGrade(grades, "Ann", 95)` il voto di `Ann` deve essere sostituito con `95`

```kotlin
    setGrade(grades, "Ann", 95)
    tryCatch(grades["Ann"] == 95)
```

La mappa deve essere uguale a `{Ann=95, Ben=90}`

```kotlin
    tryCatch(grades == mapOf("Ann" to 95, "Ben" to 90))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun setGrade(grades: MutableMap<String, Int>, name: String, grade: Int) {
    grades[name] = grade
}
```
