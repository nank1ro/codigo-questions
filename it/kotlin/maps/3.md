---
language: kotlin
exerciseType: 2
---

# --description--

Quando una chiave potrebbe mancare, `getOrDefault` ti permette di scegliere il valore da usare al posto di `null`:
```kotlin
val ages = mapOf("Alice" to 30)
println(ages.getOrDefault("Alice", 0)) // 30
println(ages.getOrDefault("Zoe", 0))   // 0
```
Il primo argomento è la chiave, il secondo è il valore predefinito restituito quando la chiave non viene trovata.

# --instructions--

Completa il codice in modo da stampare la scorta di `"kiwi"`, usando `0` quando il frutto non è nella mappa

# --seed--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.[/]("kiwi", [/]))
```

# --answers--

- getOrDefault
- get
- getOrNull
- 0
- null
- "kiwi"

# --solutions--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.getOrDefault("kiwi", 0))
```

# --output--

0
