---
language: kotlin
exerciseType: 1
---

# --description--

Kotlin supporta i valori di parametri predefiniti. Se un chiamante non fornisce un argomento, viene utilizzato il valore predefinito:
```kotlin
fun greet(name: String = "World") {
    println("Hello, $name!")
}
greet()         // prints Hello, World!
greet("Alice")  // prints Hello, Alice!
```
I valori predefiniti rendono i parametri opzionali, riducendo la necessità di funzioni sovraccaricate.

# --instructions--

Definisci una funzione chiamata `multiply` che prende due parametri `Int`: `a` e `b` con un valore predefinito di `2`. Restituisce `a * b`. Chiama `multiply` con solo l'argomento `5` e memorizza il risultato in una variabile chiamata `result`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`result` deve essere uguale a `10`.

```kotlin
    tryCatch(result == 10)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun multiply(a: Int, b: Int = 2): Int {
    return a * b
}
val result = multiply(5)
```
