---
language: kotlin
exerciseType: 1
---

# --description--

Le funzioni possono avere più parametri di tipi diversi:
```kotlin
fun introduce(name: String, age: Int): String {
    return "My name is $name and I am $age years old."
}
println(introduce("Bob", 30))
// prints My name is Bob and I am 30 years old.
```
Gli argomenti nominati ti permettono di passare valori in qualsiasi ordine usando il nome del parametro:
```kotlin
println(introduce(age = 30, name = "Bob"))
```

# --instructions--

Definisci una funzione chiamata `formatScore` che prende un parametro `String` `player` e un parametro `Int` `score`, e restituisce la stringa `"$player: $score"`. Chiamala con `"Alice"` e `42` usando argomenti nominati (in qualsiasi ordine) e memorizza il risultato in una variabile chiamata `result`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`result` deve essere uguale a `"Alice: 42"`.

```kotlin
    tryCatch(result == "Alice: 42")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun formatScore(player: String, score: Int): String {
    return "$player: $score"
}
val result = formatScore(score = 42, player = "Alice")
```
