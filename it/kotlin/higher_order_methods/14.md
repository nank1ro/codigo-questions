---
language: kotlin
exerciseType: 2
---

# --description--

In una catena, il tipo di `it` cambia a ogni passo: dopo `filter` su una `List<String>` hai ancora delle stringhe, ma dopo `map { it.length }` hai una `List<Int>`, quindi la lambda successiva vede dei numeri.
```kotlin
val words = listOf("kiwi", "fig")
println(words.map { it.length }.filter { it > 3 }) // [4]
```
Ogni passo restituisce una **nuova** lista e non tocca mai la precedente, quindi una catena può essere spezzata in valori intermedi con un nome senza cambiare il risultato.

# --instructions--

Completa il codice in modo che `result` contenga i frutti che iniziano con `a`, in maiuscolo, così che il programma stampi `[APPLE, AVOCADO]`

# --seed--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.[/] { it.startsWith("a") }.[/] { it.[/] }
println(result)
```

# --answers--

- filter
- map
- uppercase()
- forEach
- count
- lowercase()
- length

# --solutions--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.filter { it.startsWith("a") }.map { it.uppercase() }
println(result)
```

# --output--

[APPLE, AVOCADO]
