---
language: kotlin
exerciseType: 1
---

# --instructions--

Scrivi una funzione `sumNumbers(items: List<Any>): Int` che restituisce la somma degli elementi che sono valori `Int`, ignorando gli elementi di qualsiasi altro tipo.

Esempio: `sumNumbers(listOf(1, "a", 2))` restituisce `3`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`sumNumbers(listOf(1, "a", 2))` deve restituire `3`

```kotlin
    tryCatch(sumNumbers(listOf(1, "a", 2)) == 3)
```

`sumNumbers(listOf("x", 'c', true))` deve restituire `0`

```kotlin
    tryCatch(sumNumbers(listOf("x", 'c', true)) == 0)
```

`sumNumbers(listOf(5, 2.5, 5))` deve restituire `10`

```kotlin
    tryCatch(sumNumbers(listOf(5, 2.5, 5)) == 10)
```

`sumNumbers(listOf(4, 6))` deve restituire `10`

```kotlin
    tryCatch(sumNumbers(listOf(4, 6)) == 10)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun sumNumbers(items: List<Any>): Int {
    var total = 0
    for (item in items) {
        if (item is Int) total += item
    }
    return total
}
```
