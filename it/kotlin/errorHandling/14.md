---
language: kotlin
exerciseType: 4
---

# --instructions--

Ordina gli elementi in modo che il programma memorizzi l'esito della conversione, poi stampi il valore recuperato, poi il risultato grezzo, poi se il tentativo è riuscito.

# --answers--

```kotlin
val result = runCatching { "abc".toInt() }
```

```kotlin
val value = result.getOrElse { 0 }
```

```kotlin
println("value: $value")
```

```kotlin
println("raw: ${result.getOrNull()}")
```

```kotlin
println("success: ${result.isSuccess}")
```

# --solutions--

```kotlin
val result = runCatching { "abc".toInt() }
val value = result.getOrElse { 0 }
println("value: $value")
println("raw: ${result.getOrNull()}")
println("success: ${result.isSuccess}")
```

# --output--

value: 0
raw: null
success: false
