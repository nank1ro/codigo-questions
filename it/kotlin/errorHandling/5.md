---
language: kotlin
exerciseType: 2
---

# --description--

Un blocco `finally` può essere aggiunto alla fine. Viene eseguito **qualunque cosa accada**: dopo un `try` riuscito, dopo che un `catch` ha recuperato, e persino quando l'eccezione non viene affatto catturata.

```kotlin
try {
    println("reading")
    println("abc".toInt())
} catch (e: NumberFormatException) {
    println("bad number")
} finally {
    println("closing")
}
```
```
reading
bad number
closing
```
Questo lo rende il posto giusto per le operazioni di pulizia che non devono essere saltate, come chiudere un file. Un `try` ha bisogno almeno di un `catch` o di un `finally`, ma può avere entrambi.

# --instructions--

Completa il codice in modo che la conversione sbagliata venga catturata e la riga di pulizia venga sempre eseguita.

# --seed--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } [/] (e: NumberFormatException) {
        println("bad number")
    } [/] {
        println("closing")
    }
}
```

# --answers--

- catch
- finally
- try
- throw
- else
- do

# --solutions--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } catch (e: NumberFormatException) {
        println("bad number")
    } finally {
        println("closing")
    }
}
```

# --output--

bad number
closing
