---
language: kotlin
exerciseType: 2
---

# --description--

Una funzione con più di un parametro ha un tag `@param` per ognuno di essi, scritti nello stesso ordine dei parametri:
```kotlin
/**
 * Returns the area of a rectangle.
 * @param width the horizontal side, in centimetres
 * @param height the vertical side, in centimetres
 * @return the product of [width] and [height]
 */
fun area(width: Int, height: Int): Int {
    return width * height
}
```
Un tag è comunque solo un commento: se rinomini un parametro e dimentichi il tag, nulla si rompe, ma la documentazione inizia a mentire. Aggiorna il KDoc insieme alla firma.

# --instructions--

Completa la documentazione di `price`: il primo tag deve descrivere il parametro `quantity`, il secondo il parametro `unitPrice` e il terzo il valore che la funzione restituisce

# --seed--

```kotlin
/**
 * Returns the total price of an order.
 * [/] quantity how many items were ordered
 * [/] unitPrice the price of a single item
 * [/] the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --answers--

- @param
- @param
- @return
- @throws
- @property

# --solutions--

```kotlin
/**
 * Returns the total price of an order.
 * @param quantity how many items were ordered
 * @param unitPrice the price of a single item
 * @return the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --output--

15
