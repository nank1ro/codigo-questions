---
language: kotlin
exerciseType: 2
---

# --description--

Una lambda può usare le variabili dichiarate attorno a essa, anche dopo che il codice circostante è terminato. Questo si chiama **closure**: la lambda *cattura* le variabili di cui ha bisogno.
A differenza di molti altri linguaggi, Kotlin permette a una lambda di **modificare** una `var` catturata:
```kotlin
var clicks = 0
val onClick = { clicks++ }
onClick()
onClick()
println(clicks) // 2
```
Ogni chiamata di `onClick` aggiorna la stessa variabile `clicks` che il codice esterno vede.

# --instructions--

Completa la lambda `add` in modo che ogni chiamata aumenti di `n` la variabile catturata `total`, in modo che il programma stampi `12`

# --seed--

```kotlin
var total = 0
val add = { n: Int -> total [/] n }
add(5)
add(7)
println(total)
```

# --answers--

- +=
- +
- =
- -=
- ==

# --solutions--

```kotlin
var total = 0
val add = { n: Int -> total += n }
add(5)
add(7)
println(total)
```

# --output--

12
