---
language: kotlin
exerciseType: 1
---

# --description--

Poiché `let` restituisce il valore della sua ultima espressione, è un modo pratico per **trasformare un valore in qualcos'altro** senza dover dare un nome a una variabile intermedia:
```kotlin
val price = 12
val label = price.let { "$it EUR" }
println(label) // 12 EUR
```
Dentro il blocco puoi usare `it` tutte le volte che ti serve:
```kotlin
println("kiwi".let { "${it.uppercase()} has ${it.length} letters" })
// KIWI has 4 letters
```

# --instructions--

Scrivi una funzione `describe(word: String): String` che restituisce la parola in maiuscolo, seguita da uno spazio e dalla sua lunghezza tra parentesi tonde. Usa `let` così che `word` venga letta solo attraverso `it`.

Esempio: `describe("kiwi")` restituisce `KIWI (4)`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`describe("kiwi")` deve restituire `KIWI (4)`

```kotlin
    tryCatch(describe("kiwi") == "KIWI (4)")
```

`describe("a")` deve restituire `A (1)`

```kotlin
    tryCatch(describe("a") == "A (1)")
```

`describe("")` deve restituire ` (0)`

```kotlin
    tryCatch(describe("") == " (0)")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun describe(word: String): String {
    return word.let { "${it.uppercase()} (${it.length})" }
}
```
