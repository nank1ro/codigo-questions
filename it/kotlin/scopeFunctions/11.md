---
language: kotlin
exerciseType: 1
---

# --description--

Tutte e cinque le scope functions sono ora sul tavolo, e ognuna è solo un punto sui due assi:
- `let` - l'oggetto è `it`, restituisce il risultato del blocco
- `run` - l'oggetto è `this`, restituisce il risultato del blocco
- `with` - l'oggetto è `this` (passato come argomento), restituisce il risultato del blocco
- `apply` - l'oggetto è `this`, restituisce l'oggetto
- `also` - l'oggetto è `it`, restituisce l'oggetto

Scegli la riga che ti serve: `it` si legge meglio quando passi l'oggetto a qualcos'altro, `this` si legge meglio quando tocchi molti dei suoi membri; restituisce il risultato del blocco quando vuoi un nuovo valore, restituisce l'oggetto quando vuoi continuare a lavorare con esso.

# --instructions--

Crea una classe `Point` con due proprietà di sola lettura, `val x: Int` e `val y: Int`, entrambe prese dal costruttore primario.

Poi scrivi una funzione `label(point: Point): String` che usa `with` per restituire le coordinate nella forma `(x, y)`.

Esempio: `label(Point(2, 5))` restituisce `(2, 5)`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`label(Point(2, 5))` deve restituire `(2, 5)`

```kotlin
    tryCatch(label(Point(2, 5)) == "(2, 5)")
```

`label(Point(0, 0))` deve restituire `(0, 0)`

```kotlin
    tryCatch(label(Point(0, 0)) == "(0, 0)")
```

`label(Point(-3, 12))` deve restituire `(-3, 12)`

```kotlin
    tryCatch(label(Point(-3, 12)) == "(-3, 12)")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Point(val x: Int, val y: Int)

fun label(point: Point): String {
    return with(point) {
        "($x, $y)"
    }
}
```
