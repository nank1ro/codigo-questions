---
language: kotlin
exerciseType: 1
---

# --description--

`apply` non è limitato agli oggetti che hai appena creato: funziona su qualsiasi oggetto, e poiché restituisce l'oggetto puoi usare l'intera espressione ovunque ci si aspetti quell'oggetto.
```kotlin
val box = Box()
box.apply { label = "tools" }        // changes box and returns it
println(listOf(Box().apply { label = "nails" }).size) // 1
```
Il blocco è un normale blocco di codice, quindi può contenere quante istruzioni ti servono.

# --instructions--

Crea una classe `Profile` con due proprietà, `var name: String` che parte da `""` e `var age: Int` che parte da `0`.

Poi scrivi una funzione `makeProfile(n: String, a: Int): Profile` che crea un `Profile()` e usa `apply` per impostare `name` a `n` e `age` a `a`, restituendo l'oggetto configurato.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

Un nuovo `Profile()` deve partire con un nome vuoto e età `0`

```kotlin
    val fresh = Profile()
    tryCatch(fresh.name == "" && fresh.age == 0)
```

`makeProfile("Ada", 36)` deve avere `name` uguale a `Ada`

```kotlin
    tryCatch(makeProfile("Ada", 36).name == "Ada")
```

`makeProfile("Ada", 36)` deve avere `age` uguale a `36`

```kotlin
    tryCatch(makeProfile("Ada", 36).age == 36)
```

`makeProfile("Bob", 7)` deve avere `name` uguale a `Bob` e `age` uguale a `7`

```kotlin
    val bob = makeProfile("Bob", 7)
    tryCatch(bob.name == "Bob" && bob.age == 7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Profile {
    var name: String = ""
    var age: Int = 0
}

fun makeProfile(n: String, a: Int): Profile {
    return Profile().apply {
        name = n
        age = a
    }
}
```
