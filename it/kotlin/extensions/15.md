---
language: kotlin
exerciseType: 1
---

# --description--

Una funzione di estensione con esattamente **un** parametro può essere marcata `infix`. Una funzione infix può essere chiamata senza il punto e senza le parentesi, con il ricevente a sinistra e l'argomento a destra, cosa che si legge quasi come una frase:
```kotlin
infix fun Int.percentOf(total: Int): Int = total * this / 100

println(20 percentOf 50)   // 10
println(20.percentOf(50))  // 10, the normal call still works
```
Kotlin la usa anche per alcune funzioni predefinite: `1 to "one"` costruisce una `Pair`, e `1 until 5` costruisce un intervallo.

# --instructions--

Scrivi una funzione di estensione infix `toThe(exponent: Int)` su `Int` che restituisce il ricevente elevato alla potenza data, così che `2 toThe 10` sia `1024`. L'esponente non è mai negativo, e qualsiasi numero elevato a `0` è `1`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`2 toThe 10` deve restituire `1024`

```kotlin
    tryCatch((2 toThe 10) == 1024)
```

`3 toThe 3` deve restituire `27`

```kotlin
    tryCatch((3 toThe 3) == 27)
```

`5 toThe 0` deve restituire `1`

```kotlin
    tryCatch((5 toThe 0) == 1)
```

`7.toThe(1)` deve restituire `7`

```kotlin
    tryCatch(7.toThe(1) == 7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
infix fun Int.toThe(exponent: Int): Int {
    var result = 1
    repeat(exponent) { result *= this }
    return result
}
```
