---
language: kotlin
exerciseType: 1
---

# --description--

Dentro un'estensione con ricevente nullable puoi anche usare la chiamata sicura `this?.` per raggiungere i membri del valore solo quando non è `null`. La libreria standard usa la stessa idea per funzioni come `isNullOrEmpty()`:
```kotlin
fun String?.firstOrQuestion(): Char {
    return this?.firstOrNull() ?: '?'
}
val text: String? = null
println(text.firstOrQuestion())    // ?
println("Kotlin".firstOrQuestion()) // K
```

# --instructions--

Scrivi una funzione di estensione `lengthOrZero()` su `String?` che restituisce la lunghezza del testo, oppure `0` quando il ricevente è `null`.

Esempio: `"Kotlin".lengthOrZero()` restituisce `6`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`"Kotlin".lengthOrZero()` deve restituire `6`

```kotlin
    tryCatch("Kotlin".lengthOrZero() == 6)
```

`"".lengthOrZero()` deve restituire `0`

```kotlin
    tryCatch("".lengthOrZero() == 0)
```

Chiamata su una `String?` che contiene `null`, deve restituire `0`

```kotlin
    val missing: String? = null
    tryCatch(missing.lengthOrZero() == 0)
```

Chiamata su una `String?` che contiene `"hi"`, deve restituire `2`

```kotlin
    val present: String? = "hi"
    tryCatch(present.lengthOrZero() == 2)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun String?.lengthOrZero(): Int {
    return this?.length ?: 0
}
```
