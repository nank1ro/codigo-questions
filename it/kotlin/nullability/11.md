---
language: kotlin
exerciseType: 2
---

# --description--

Anche le collezioni possono contenere elementi nullable: una `List<Int?>` può contenere elementi `null`, mentre una `List<Int>` mai.
`filterNotNull()` restituisce una nuova lista senza gli elementi `null`, e il tipo dei suoi elementi diventa non-null, così puoi usare gli elementi liberamente:
```kotlin
val scores: List<Int?> = listOf(10, null, 20)
val valid = scores.filterNotNull() // List<Int> [10, 20]
println(valid.sum())               // 30
```

# --instructions--

Completa il codice per dichiarare una lista che può contenere numeri `null`, rimuovere gli elementi `null` e stampare i valori rimasti e la loro somma

# --seed--

```kotlin
val values: List<[/]> = listOf(3, null, 5, null)
val present = values.[/]()
println(present)
println(present.sum())
```

# --answers--

- Int?
- Int
- filterNotNull
- filter
- mapNotNull
- requireNotNull

# --solutions--

```kotlin
val values: List<Int?> = listOf(3, null, 5, null)
val present = values.filterNotNull()
println(present)
println(present.sum())
```

# --output--

[3, 5]
8
