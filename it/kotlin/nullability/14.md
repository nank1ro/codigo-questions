---
language: kotlin
exerciseType: 1
---

# --description--

Quando un `null` significa che chi chiama ha commesso un errore, fallisci presto con `requireNotNull`.
Restituisce il valore come non-null quando è presente, e lancia una `IllegalArgumentException` quando è `null`, con un messaggio opzionale:
```kotlin
fun greet(name: String?): String {
    val safeName = requireNotNull(name) { "name is required" }
    return "Hello, $safeName"
}
greet(null) // IllegalArgumentException: name is required
```
Dopo la chiamata il compilatore applica anche uno smart cast a `name` stesso come `String`, quindi `name.length` è consentito da quella riga in poi.
A differenza di `!!`, l'errore porta con sé un messaggio chiaro e indica che è stato sbagliato l'*argomento*.

# --instructions--

Scrivi una funzione `safeUpper(text: String?): String` che restituisca `text` in maiuscolo. Quando `text` è `null` deve lanciare una `IllegalArgumentException` con il messaggio `text is required`, usando `requireNotNull`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`safeUpper("abc")` deve restituire `"ABC"`

```kotlin
    tryCatch(safeUpper("abc") == "ABC")
```

`safeUpper("Kotlin")` deve restituire `"KOTLIN"`

```kotlin
    tryCatch(safeUpper("Kotlin") == "KOTLIN")
```

`safeUpper(null)` deve lanciare una `IllegalArgumentException`

```kotlin
    var threw = false
    try {
        safeUpper(null)
    } catch (e: IllegalArgumentException) {
        threw = true
    }
    tryCatch(threw)
```

Il messaggio dell'eccezione deve essere `text is required`

```kotlin
    var message = ""
    try {
        safeUpper(null)
    } catch (e: IllegalArgumentException) {
        message = e.message ?: ""
    }
    tryCatch(message == "text is required")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun safeUpper(text: String?): String {
    val value = requireNotNull(text) { "text is required" }
    return value.uppercase()
}
```
