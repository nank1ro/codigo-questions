---
language: kotlin
exerciseType: 1
---

# --description--

Una classe `abstract` sta tra un'interfaccia e una classe normale: non può essere istanziata, e mescola membri **astratti**, che non hanno corpo e devono essere sovrascritti, con membri concreti che le sottoclassi ereditano così come sono.

```kotlin
abstract class Vehicle(val name: String) {
    abstract fun wheels(): Int
    fun describe(): String = "$name has ${wheels()} wheels"
}

class Bike(name: String) : Vehicle(name) {
    override fun wheels(): Int = 2
}

println(Bike("BMX").describe()) // BMX has 2 wheels
```

A differenza di un'interfaccia, una classe astratta ha un costruttore e può conservare stato nelle proprietà, ed è per questo che la sottoclasse passa `name` verso l'alto con `: Vehicle(name)`. Una classe può estendere una sola classe, quindi scegli una classe astratta quando le sottoclassi condividono dati, e un'interfaccia quando condividono solo comportamento. I membri astratti sono sovrascrivibili senza aggiungere `open`.

# --instructions--

Dichiara una classe astratta `Employee` con una proprietà `val` `name` di tipo `String` nel costruttore, un metodo astratto `monthlyPay(): Int` e un metodo concreto `yearlyPay(): Int` che restituisce la paga mensile moltiplicata per `12`.

Poi dichiara una classe `Manager` che estende `Employee`, riceve `name` e una proprietà `val` `bonus` di tipo `Int`, e il cui `monthlyPay()` restituisce `5000` più il bonus.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`Manager("Ann", 500).monthlyPay()` must return `5500`

```kotlin
    tryCatch(Manager("Ann", 500).monthlyPay() == 5500)
```

`Manager("Ann", 500).yearlyPay()` must return `66000`

```kotlin
    tryCatch(Manager("Ann", 500).yearlyPay() == 66000)
```

`Manager("Bo", 0).name` must return `"Bo"`

```kotlin
    tryCatch(Manager("Bo", 0).name == "Bo")
```

A `Manager` must be usable as an `Employee`

```kotlin
    val employee: Employee = Manager("Cy", 1000)
    tryCatch(employee.yearlyPay() == 72000)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
abstract class Employee(val name: String) {
    abstract fun monthlyPay(): Int

    fun yearlyPay(): Int = monthlyPay() * 12
}

class Manager(name: String, val bonus: Int) : Employee(name) {
    override fun monthlyPay(): Int = 5000 + bonus
}
```
