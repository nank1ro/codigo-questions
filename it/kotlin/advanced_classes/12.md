---
language: kotlin
exerciseType: 3
---

# --instructions--

Cosa succede con questo programma?

```kotlin
interface Movable {
    fun move(): String = "moving"
}

class Robot : Movable

fun main() {
    println(Robot().move())
}
```

# --answers--

- Stampa `moving`, perché `Robot` eredita l'implementazione di default
- Non compila: `Robot` deve sovrascrivere `move()`
- Non compila: un'interfaccia non può contenere il corpo di una funzione
- Stampa `null`

# --solutions--

- Stampa `moving`, perché `Robot` eredita l'implementazione di default

