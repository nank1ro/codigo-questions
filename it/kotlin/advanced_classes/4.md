---
language: kotlin
exerciseType: 3
---

# --description--

I membri generati considerano solo le proprietà dichiarate nel **costruttore primario**. Una proprietà dichiarata nel **corpo** della classe è una proprietà normale: non fa parte di `toString()`, `equals()`, `hashCode()` o `copy()`.

```kotlin
data class Item(val name: String) {
    var quantity: Int = 0
}

val a = Item("nail")
a.quantity = 5
println(a) // Item(name=nail)
```

È facile dimenticarsene, quindi metti nel costruttore primario tutto ciò che identifica l'oggetto, e tieni nel corpo lo stato derivato o temporaneo.

# --instructions--

Cosa stampa questo programma?

```kotlin
data class Item(val name: String) {
    var quantity: Int = 0
}

fun main() {
    val a = Item("nail")
    a.quantity = 5
    val b = Item("nail")
    b.quantity = 9
    println(a == b)
}
```

# --answers--

- `true`, perché l'`equals()` generato confronta solo `name`
- `false`, perché `quantity` è diverso
- `Item(name=nail, quantity=5)`
- Non compila: una data class non può dichiarare proprietà nel suo corpo

# --solutions--

- `true`, perché l'`equals()` generato confronta solo `name`

