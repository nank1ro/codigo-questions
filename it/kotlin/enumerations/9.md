---
language: kotlin
exerciseType: 2
---

# --description--

Un'**interfaccia** dichiara metodi senza corpo; qualsiasi tipo che la implementa deve fornirli:
```kotlin
interface Greeter {
    fun greet(): String
}
```
Le classi enum possono implementare interfacce. Elenchi l'interfaccia dopo i due punti e contrassegni ogni implementazione con `override`. All'interno del corpo dell'enum, la voce corrente è `this` e le altre voci possono essere referenziate senza il nome della classe:
```kotlin
enum class Language : Greeter {
    ENGLISH, ITALIAN;

    override fun greet(): String = when (this) {
        ENGLISH -> "Hello"
        ITALIAN -> "Ciao"
    }
}
println(Language.ITALIAN.greet()) // Ciao
```

# --instructions--

Completa il codice in modo che `Animal` implementi `Speaker`, poi stampa il verso di un `CAT`

# --seed--

```kotlin
interface Speaker {
    fun sound(): String
}

enum class Animal : [/] {
    DOG, CAT;

    [/] fun sound(): String = when (this) {
        DOG -> "Woof"
        CAT -> "Meow"
    }
}

fun main() {
    println(Animal.[/].sound())
}
```

# --answers--

- Speaker
- Speaker()
- Interface
- override
- open
- abstract
- CAT
- DOG
- "CAT"

# --solutions--

```kotlin
interface Speaker {
    fun sound(): String
}

enum class Animal : Speaker {
    DOG, CAT;

    override fun sound(): String = when (this) {
        DOG -> "Woof"
        CAT -> "Meow"
    }
}

fun main() {
    println(Animal.CAT.sound())
}
```

# --output--

Meow
