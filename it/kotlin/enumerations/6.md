---
language: kotlin
exerciseType: 2
---

# --description--

Ogni classe enum espone una proprietà `entries`: una lista di tutte le sue voci nell'ordine di dichiarazione. È comoda per iterare:
```kotlin
for (direction in Direction.entries) {
    println(direction.name)
}
// NORTH
// SOUTH
// EAST
// WEST
```
Essendo una lista, `entries` supporta anche `size` e l'indicizzazione, ad esempio `Direction.entries[0]` è `NORTH`.

Il codice più vecchio usa invece la funzione `values()`, che restituisce un array; `entries` è la scelta consigliata a partire da Kotlin 1.9.

# --instructions--

Completa il ciclo in modo che stampi l'ordinal e il nome di ogni voce di `Size`, separati da uno spazio

# --seed--

```kotlin
enum class Size {
    SMALL, MEDIUM, LARGE
}

fun main() {
    for (size in Size.[/]) {
        println("${size.[/]} ${size.[/]}")
    }
}
```

# --answers--

- entries
- cases
- list
- ordinal
- name
- index
- value

# --solutions--

```kotlin
enum class Size {
    SMALL, MEDIUM, LARGE
}

fun main() {
    for (size in Size.entries) {
        println("${size.ordinal} ${size.name}")
    }
}
```

# --output--

0 SMALL
1 MEDIUM
2 LARGE
