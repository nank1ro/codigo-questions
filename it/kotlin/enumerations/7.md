---
language: kotlin
exerciseType: 1
---

# --description--

Per passare da una `String` a una voce, usa la funzione `valueOf`. Cerca la voce il cui `name` corrisponde esattamente:
```kotlin
val direction = Direction.valueOf("EAST")
println(direction == Direction.EAST) // true
```
Il confronto distingue maiuscole e minuscole: `Direction.valueOf("east")` lancia una `IllegalArgumentException` perché nessuna voce ha quel nome.

# --instructions--

Dichiara un enum `Status` con le voci `ACTIVE` e `INACTIVE`. Scrivi poi una funzione `statusOf(text: String)` che restituisca lo `Status` corrispondente a `text`, indipendentemente da maiuscole e minuscole (in modo che `"active"`, `"Active"` e `"ACTIVE"` diano tutti `Status.ACTIVE`).

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`statusOf("ACTIVE")` deve restituire `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("ACTIVE") == Status.ACTIVE)
```

`statusOf("inactive")` deve restituire `Status.INACTIVE`

```kotlin
    tryCatch(statusOf("inactive") == Status.INACTIVE)
```

`statusOf("Active")` deve restituire `Status.ACTIVE`

```kotlin
    tryCatch(statusOf("Active") == Status.ACTIVE)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
enum class Status {
    ACTIVE, INACTIVE
}

fun statusOf(text: String): Status {
    return Status.valueOf(text.uppercase())
}
```
