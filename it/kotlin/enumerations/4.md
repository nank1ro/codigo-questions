---
language: kotlin
exerciseType: 2
---

# --description--

Un enum può avere un **costruttore**, proprio come una classe normale. Ogni voce passa allora i propri argomenti, e i valori vengono memorizzati in proprietà:
```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
println(Planet.MARS.moons) // 2
```
Le proprietà del costruttore sono di solito dichiarate con `val`, poiché i dati di una voce non sono pensati per cambiare.

# --instructions--

Completa il codice in modo che ogni `Coin` memorizzi il proprio valore in centesimi, poi stampa il valore di un `PENNY` (che vale `1` centesimo)

# --seed--

```kotlin
enum class Coin([/] cents: Int) {
    PENNY([/]),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.[/])
}
```

# --answers--

- val
- fun
- const
- 1
- "1"
- cents
- name
- ordinal

# --solutions--

```kotlin
enum class Coin(val cents: Int) {
    PENNY(1),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.cents)
}
```

# --output--

1
