---
language: kotlin
exerciseType: 1
---

# --description--

`split` taglia una stringa ovunque il pattern corrisponda e restituisce i pezzi come `List<String>`. A differenza della divisione su una stringa delimitatrice fissa, un separatore regex può descrivere un'intera famiglia di separatori:
```kotlin
val separators = Regex("""[,;]\s*""")
println(separators.split("a, b;c")) // [a, b, c]
```
I separatori trovati non fanno parte del risultato. Se il testo inizia o finisce con un separatore, il pezzo accanto ad esso è vuoto:
```kotlin
println(Regex("""\s+""").split(" a b")) // [, a, b]
```
`split` accetta un `limit` come secondo argomento per fermarsi dopo un dato numero di pezzi, lasciando il resto intatto nell'ultimo.

# --instructions--

Scrivi una funzione `fields(line: String): List<String>` che divide `line` su ogni sequenza di virgole, punti e virgole e spazi, usando `split` con una regex.

Esempio: `fields("a, b;c")` restituisce `[a, b, c]`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`fields("a, b;c")` deve restituire `[a, b, c]`

```kotlin
    tryCatch(fields("a, b;c") == listOf("a", "b", "c"))
```

`fields("one;;two")` deve restituire `[one, two]`

```kotlin
    tryCatch(fields("one;;two") == listOf("one", "two"))
```

`fields("red green blue")` deve restituire `[red, green, blue]`

```kotlin
    tryCatch(fields("red green blue") == listOf("red", "green", "blue"))
```

`fields("solo")` deve restituire `[solo]`

```kotlin
    tryCatch(fields("solo") == listOf("solo"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun fields(line: String): List<String> {
    return Regex("""[,;\s]+""").split(line)
}
```
