---
language: kotlin
exerciseType: 4
---

# --description--

Poiché `findAll` dà una sequenza, i metodi di aggregazione che conosci già funzionano anche sulle corrispondenze: `sumOf`, `maxOfOrNull`, `filter`, `sortedBy`. Estrarre numeri da testo libero è un lavoro in due passi: trovarli, poi convertire il testo in numero.
```kotlin
val total = Regex("""\d+""").findAll("2 apples, 3 pears").sumOf { it.value.toInt() }
println(total) // 5
```

# --instructions--

Riesci a ordinare questi elementi per stampare `total: 17`, sommando ogni numero del testo?

# --answers--

```kotlin
val text = "ann scored 8 and bob scored 9"
```

```kotlin
val numbers = Regex("""\d+""").findAll(text)
```

```kotlin
val total = numbers.sumOf { it.value.toInt() }
```

```kotlin
println("total: $total")
```

# --solutions--

```kotlin
val text = "ann scored 8 and bob scored 9"
val numbers = Regex("""\d+""").findAll(text)
val total = numbers.sumOf { it.value.toInt() }
println("total: $total")
```

# --output--

total: 17
