---
language: c
exerciseType: 4
---

# --description--

`printf` non ha uno specificatore per un'intera struct. La soluzione abituale è una piccola funzione che stampa i membri in un formato fisso, così ogni parte del programma mostra il valore allo stesso modo:
```c
void print_person(Person p) {
    printf("%s (%d)\n", p.name, p.age);
}
```

# --instructions--

Ordina le righe per definire il tipo `Point`, una funzione `print_point` che stampa un punto come `(x, y)` e un `main` che stampa il punto `(3, 4)`. Definisci la funzione prima di `main`

# --answers--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
```

```c
void print_point(Point p) {
```

```c
    printf("(%d, %d)\n", p.x, p.y);
```

```c
}
```

```c
int main() {
```

```c
    Point p = {3, 4};
```

```c
    print_point(p);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
void print_point(Point p) {
    printf("(%d, %d)\n", p.x, p.y);
}
int main() {
    Point p = {3, 4};
    print_point(p);
    return 0;
}
```

# --output--

(3, 4)
