---
language: c
exerciseType: 2
---

# --description--

Scrivere `struct Point` ogni volta è verboso. Con `typedef` dai alla struct un nome di tipo breve, e la struct stessa può restare anonima:
```c
typedef struct {
    double celsius;
} Temperature;

Temperature t = {21.5};
```
Il nuovo nome `Temperature` si usa da solo, senza la parola chiave `struct` davanti. È il modo più comune di dichiarare le struct nei programmi reali.

# --instructions--

Completa il codice per definire la struct anonima con il membro `celsius` sotto il nome di tipo `Temperature`, poi dichiara `t` con quel tipo

# --seed--

```c
typedef [/] {
    double celsius;
} Temperature;
[/] t = {21.5};
printf("%.1f\n", t.celsius);
```

# --answers--

- struct
- enum
- typedef
- Temperature
- struct Temperature
- Temperature t

# --solutions--

```c
typedef struct {
    double celsius;
} Temperature;
Temperature t = {21.5};
printf("%.1f\n", t.celsius);
```

# --output--

21.5
