---
language: c
exerciseType: 4
---

# --description--

Un array non può essere assegnato con `=` dopo la sua dichiarazione:
```c
char copy[20];
copy = "Codigo"; // errore
```
Per copiare una stringa usa `strcpy(destination, source)` da `string.h`.
La destinazione deve essere abbastanza grande da contenere tutti i caratteri più il `'\0'`.

# --instructions--

Ordina le righe per copiare `source` in `copy` e stamparlo. Dichiara `source` prima di `copy`

# --answers--

```c
char source[] = "Codigo";
```

```c
char copy[20];
```

```c
strcpy(copy, source);
```

```c
printf("%s\n", copy);
```

# --solutions--

```c
char source[] = "Codigo";
char copy[20];
strcpy(copy, source);
printf("%s\n", copy);
```

# --output--

Codigo
