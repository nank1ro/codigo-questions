---
language: c
exerciseType: 4
---

# --instructions--

Ordina le righe in modo che il programma confronti `password` con `attempt` e stampi `Access granted`. Dichiara `password` prima di `attempt` e metti ogni messaggio nel proprio ramo

# --answers--

```c
char password[] = "secret";
```

```c
char attempt[] = "secret";
```

```c
if (strcmp(password, attempt) == 0) {
```

```c
    printf("Access granted\n");
```

```c
} else {
```

```c
    printf("Access denied\n");
```

```c
}
```

# --solutions--

```c
char password[] = "secret";
char attempt[] = "secret";
if (strcmp(password, attempt) == 0) {
    printf("Access granted\n");
} else {
    printf("Access denied\n");
}
```

# --output--

Access granted
