---
language: c
exerciseType: 4
---

# --description--

`sprintf` funziona come `printf`, ma scrive il testo formattato in un array di caratteri invece che sullo schermo:
```c
char buffer[30];
sprintf(buffer, "%d items", 3); // buffer è "3 items"
```
Il buffer deve essere abbastanza grande per tutto il testo e il suo `'\0'`.

# --instructions--

Ordina le righe per costruire il testo `Age: 25` dentro `buffer` e stamparlo. Dichiara `buffer` prima di `age`

# --answers--

```c
char buffer[30];
```

```c
int age = 25;
```

```c
sprintf(buffer, "Age: %d", age);
```

```c
printf("%s\n", buffer);
```

# --solutions--

```c
char buffer[30];
int age = 25;
sprintf(buffer, "Age: %d", age);
printf("%s\n", buffer);
```

# --output--

Age: 25
