---
language: c
exerciseType: 2
---

# --description--

L'header `ctype.h` fornisce funzioni che lavorano su un singolo carattere.
`toupper(c)` restituisce la versione maiuscola di una lettera e `tolower(c)` quella minuscola; qualsiasi altro carattere viene restituito invariato:
```c
char letter = toupper('a'); // 'A'
```

# --instructions--

Stampa `Ab` convertendo `first` in maiuscolo e `second` in minuscolo

# --seed--

```c
#include <stdio.h>
#include <ctype.h>

int main() {
    char first = 'a';
    char second = 'B';
    printf("%c%c\n", [/](first), [/](second));
    return 0;
}
```

# --answers--

- toupper
- tolower
- upper
- lower
- capitalize

# --solutions--

```c
#include <stdio.h>
#include <ctype.h>

int main() {
    char first = 'a';
    char second = 'B';
    printf("%c%c\n", toupper(first), tolower(second));
    return 0;
}
```

# --output--

Ab
