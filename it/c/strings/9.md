---
language: c
exerciseType: 3
---

# --description--

`strcmp` confronta le stringhe carattere per carattere usando i loro codici carattere.
Il risultato è negativo quando la prima stringa viene prima della seconda, positivo quando viene dopo, e `0` quando sono uguali:
```c
strcmp("a", "b"); // negativo
strcmp("b", "a"); // positivo
```

# --instructions--

Cosa restituisce `strcmp("apple", "banana")`?

# --answers--

- Un numero negativo
- 0
- Un numero positivo
- La stringa "apple"

# --solutions--

- Un numero negativo
