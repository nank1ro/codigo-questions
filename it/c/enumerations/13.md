---
language: c
exerciseType: 4
---

# --description--

C non offre alcun modo integrato per ottenere il nome di una costante di enumerazione: `printf("%d\n", SUMMER)` stampa `2`, non `Summer`. La soluzione abituale è una piccola funzione con uno `switch` che restituisce la stringa corrispondente a ogni costante.

# --instructions--

Ordina le righe per definire il tipo `Season`, una funzione `season_name` che restituisce il nome di una stagione, e una `main` che stampa il nome di `SUMMER`. Definisci la funzione prima di `main`, mantieni i casi nello stesso ordine delle costanti e metti `default` per ultimo

# --answers--

```c
#include <stdio.h>
typedef enum { WINTER, SPRING, SUMMER, AUTUMN } Season;
```

```c
char *season_name(Season season) {
```

```c
    switch (season) {
```

```c
        case WINTER: return "Winter";
```

```c
        case SPRING: return "Spring";
```

```c
        case SUMMER: return "Summer";
```

```c
        default: return "Autumn";
```

```c
    }
}
```

```c
int main() {
```

```c
    printf("%s\n", season_name(SUMMER));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef enum { WINTER, SPRING, SUMMER, AUTUMN } Season;
char *season_name(Season season) {
    switch (season) {
        case WINTER: return "Winter";
        case SPRING: return "Spring";
        case SUMMER: return "Summer";
        default: return "Autumn";
    }
}
int main() {
    printf("%s\n", season_name(SUMMER));
    return 0;
}
```

# --output--

Summer
