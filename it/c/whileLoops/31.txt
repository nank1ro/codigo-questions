<section id="options">
{
"language": "c",
"type": "answerButtons"
}
</section>
<section id="instructions">
Cosa fa il codice `contatore += 1`?
```
int contatore = 1;

while (contatore < 5) {
    printf("%d\n", contatore);
    contatore += 1;
}
```
</section>
<section id="answerButtons">
{
	"buttons": ["Incrementa il contatore di uno ogni volta che il ciclo esegue il suo blocco di codice", "Crea la variabile contatore", "Incrementa il contatore di due ogni volta", "Incrementa il contatore di tre ogni volta"],
	"correct": "Incrementa il contatore di uno ogni volta che il ciclo esegue il suo blocco di codice"
}
</section>
