---
language: c
exerciseType: 4
---

# --description--

Gli operatori hanno una **precedenza** che decide cosa viene calcolato per primo:
1. `!` viene applicato per primo
2. poi i confronti relazionali `<`, `>`, `<=`, `>=`
3. poi i confronti di uguaglianza `==`, `!=`
4. poi `&&`
5. poi `||`

Quindi `a > 0 && a < 10` non ha bisogno di parentesi: entrambi i confronti vengono calcolati prima di `&&`.
E `x == 1 || y == 2 && z == 3` significa `x == 1 || (y == 2 && z == 3)`, perché `&&` lega più stretto di `||`.

# --instructions--

Ordina le righe per memorizzare in `inside` se `a` è compreso tra `1` e `9`, memorizzare la sua negazione in `outside` e stampare entrambi i valori separati da uno spazio

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int a = 5;
```

```c
    int inside = a > 0 && a < 10;
```

```c
    int outside = !inside;
```

```c
    printf("%d %d\n", inside, outside);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int a = 5;
    int inside = a > 0 && a < 10;
    int outside = !inside;
    printf("%d %d\n", inside, outside);
    return 0;
}
```

# --output--

1 0
