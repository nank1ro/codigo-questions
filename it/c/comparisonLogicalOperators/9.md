---
language: c
exerciseType: 4
---

# --description--

La valutazione a corto circuito salta anche le chiamate di funzione: in `0 && check()` la funzione `check` non viene mai chiamata, quindi qualsiasi effetto collaterale che ha (come aggiornare un contatore) non avviene.

# --instructions--

Ordina le righe per definire un contatore `calls`, una funzione `check` che lo incrementa e restituisce `1`, e una `main` che calcola `0 && check()` e stampa il risultato seguito da `calls`. Definisci la funzione prima di `main`

# --answers--

```c
#include <stdio.h>
int calls = 0;
```

```c
int check() {
```

```c
    calls++;
    return 1;
}
```

```c
int main() {
```

```c
    int result = 0 && check();
```

```c
    printf("%d %d\n", result, calls);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int calls = 0;
int check() {
    calls++;
    return 1;
}
int main() {
    int result = 0 && check();
    printf("%d %d\n", result, calls);
    return 0;
}
```

# --output--

0 0
