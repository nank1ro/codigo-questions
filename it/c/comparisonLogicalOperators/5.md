---
language: c
exerciseType: 2
---

# --description--

L'operatore **or** `||` dà `1` quando almeno un lato è vero, e `0` solo quando entrambi i lati sono falsi:
```c
int day = 1;
printf("%d\n", day == 1 || day == 7); // stampa "1"
```
Ogni lato deve essere un confronto completo: `day == 6 || 7` non significa "6 o 7" (vedrai perché più avanti).

# --instructions--

Completa il codice in modo che `weekend` sia `1` quando `day` è `6` o `day` è `7`, poi stampalo

# --seed--

```c
int day = 7;
int weekend = day == 6 [/] day == [/];
printf("%d\n", weekend);
```

# --answers--

- ||
- 7
- &&
- 6
- =

# --solutions--

```c
int day = 7;
int weekend = day == 6 || day == 7;
printf("%d\n", weekend);
```

# --output--

1
