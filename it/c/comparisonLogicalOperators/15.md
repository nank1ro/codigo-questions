---
language: c
exerciseType: 2
---

# --description--

Un ciclo `while` continua a eseguire finché la sua condizione è diversa da zero, quindi un confronto decide quando si ferma:
```c
int i = 0;
while (i < 3) {
    printf("%d\n", i);
    i++;
}
// stampa 0, 1, 2
```
Scegliere tra `<` e `<=` cambia se l'ultimo valore viene incluso.

# --instructions--

Completa il codice in modo che il ciclo stampi i numeri da `1` a `3`, uno per riga

# --seed--

```c
int count = [/];
while (count [/] 3) {
    printf("%d\n", count);
    count++;
}
```

# --answers--

- 1
- <=
- 0
- <
- !=
- >

# --solutions--

```c
int count = 1;
while (count <= 3) {
    printf("%d\n", count);
    count++;
}
```

# --output--

1
2
3
