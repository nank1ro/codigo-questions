---
language: c
exerciseType: 4
---

# --description--

Un parametro `bool` può essere usato direttamente come operando di `&&` o `||`, senza confrontarlo con `true`: scrivi `age >= 18 && citizen`, non `citizen == true`.

# --instructions--

Ordina le righe per definire `can_vote`, che restituisce `true` quando `age` è almeno `18` e `citizen` è `true`, e una `main` che stampa il risultato per `20, true` e poi per `20, false`. Definisci la funzione prima di `main`

# --answers--

```c
#include <stdio.h>
#include <stdbool.h>
```

```c
bool can_vote(int age, bool citizen) {
```

```c
    return age >= 18 && citizen;
}
```

```c
int main() {
```

```c
    printf("%d\n", can_vote(20, true));
```

```c
    printf("%d\n", can_vote(20, false));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
#include <stdbool.h>
bool can_vote(int age, bool citizen) {
    return age >= 18 && citizen;
}
int main() {
    printf("%d\n", can_vote(20, true));
    printf("%d\n", can_vote(20, false));
    return 0;
}
```

# --output--

1
0
