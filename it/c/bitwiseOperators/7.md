---
language: c
exerciseType: 4
---

# --description--

Ogni operatore bit a bit binario ha una forma di **assegnazione composta** che aggiorna una variabile sul posto: `&=`, `|=`, `^=`, `<<=` e `>>=`.
```c
unsigned int x = 12;
x &= 10;  // equivale a x = x & 10;
x |= 1;   // equivale a x = x | 1;
x ^= 3;   // equivale a x = x ^ 3;
x <<= 1;  // equivale a x = x << 1;
x >>= 2;  // equivale a x = x >> 2;
```
Si leggono meglio che ripetere il nome della variabile e sono il modo usuale per cambiare i bit di una variabile flag.

# --instructions--

Ordina le righe per far partire `flags` da `0x01`, poi accendere con `|=` il bit che vale `4`, poi scorrere di 2 posti a sinistra con `<<=`, e infine stampare il risultato

# --answers--

```c
unsigned int flags = 0x01;
```

```c
flags |= 0x04;
```

```c
flags <<= 2;
```

```c
printf("%u\n", flags);
```

# --solutions--

```c
unsigned int flags = 0x01;
flags |= 0x04;
flags <<= 2;
printf("%u\n", flags);
```

# --output--

20
