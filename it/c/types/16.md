---
language: c
exerciseType: 4
---

# --description--

Mettendo insieme tutto: scegli il tipo in base al tipo di valore, fai corrispondere ogni specificatore di `printf` al tipo del suo argomento, e usa un cast quando un calcolo deve avvenire in un tipo diverso da quello dei suoi operandi.

# --instructions--

Ordina le righe per costruire un programma che memorizzi `7` in `total` e `2` in `count`, calcoli `total` diviso per `count` come `double` in `avg`, e stampi la divisione intera `total / count` seguita da `avg` con un decimale

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int total = 7, count = 2;
```

```c
    double avg = (double) total / count;
```

```c
    printf("%d %.1f\n", total / count, avg);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int total = 7, count = 2;
    double avg = (double) total / count;
    printf("%d %.1f\n", total / count, avg);
    return 0;
}
```

# --output--

3 3.5
