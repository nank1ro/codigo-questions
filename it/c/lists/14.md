---
language: c
exerciseType: 4
---

# --description--

Invertire una lista gira tutti i puntatori `next` al contrario, sul posto, usando tre puntatori: `prev` è la parte già invertita, `head` il nodo in lavorazione e `next` una copia del resto della lista, salvata prima che il collegamento venga cambiato. A ogni passo il nodo corrente viene fatto puntare indietro a `prev`, poi sia `prev` che `head` avanzano:
```c
Node *next = head->next;
head->next = prev;
prev = head;
head = next;
```
Quando `head` raggiunge `NULL` ogni collegamento è stato invertito e `prev` è la nuova testa.

# --instructions--

Ordina le righe per definire `reverse`, che inverte una lista sul posto e restituisce la sua nuova testa, e una `main` che stampa la lista invertita `1, 2, 3`. Definisci la funzione prima di `main`

# --answers--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
```

```c
Node *reverse(Node *head) {
    Node *prev = NULL;
```

```c
    while (head != NULL) {
        Node *next = head->next;
```

```c
        head->next = prev;
```

```c
        prev = head;
```

```c
        head = next;
    }
```

```c
    return prev;
}
```

```c
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
```

```c
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
Node *reverse(Node *head) {
    Node *prev = NULL;
    while (head != NULL) {
        Node *next = head->next;
        head->next = prev;
        prev = head;
        head = next;
    }
    return prev;
}
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --output--

3
2
1
