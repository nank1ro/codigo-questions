---
language: swift
exerciseType: 2
---

# --description--

Wenn eine Klasse ihre eigenen Funktionen hat, werden diese Funktionen __Methoden__ genannt.

# --instructions--

Fügen Sie eine Methode `description` zu Ihrer `Animal`-Klasse hinzu.
Mit zwei separaten `print`-Anweisungen sollte sie das `gender` und die `legs` des Tieres, auf dem sie aufgerufen wird, ausgeben.
Rufen Sie dann die Methode `description` auf.

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4

    [/][/]() {
        if let gender = self.gender { print(gender) }
        print([/].legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.[/]
```

# --answers--

-  description
- func
- description()
- self
- description[]
- function
- method

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4

    func description() {
        if let gender = self.gender { print(gender) }
        print(self.legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.description()
```

# --output--

female
4
