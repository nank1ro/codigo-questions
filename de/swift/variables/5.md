---
language: swift
exerciseType: 1
---

# --description--

In Swift können String-Variablen nur mit Hilfe von doppelten Anführungszeichen deklariert werden:
```swift
let x = "May"
```

# --instructions--

Deklariere eine Variable namens `month` mit dem Wert `june`.

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`month` muss gleich `"june"` sein.

```swift
    func test1() {
        XCTAssertEqual(month, "june", "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
let month = "june"
```
