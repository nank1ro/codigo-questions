---
language: swift
exerciseType: 1
---

# --description--

Da `Int(text)` und `Double(text)` bei einem Fehler `nil` zurückgeben, verrät dir der Vergleich des Ergebnisses mit `nil`, ob ein Text eine Zahl dieser Art ist:
```swift
print(Int("42") != nil)     // true
print(Double("4.2") != nil) // true
print(Double("42") != nil)  // true, a whole number is also a valid Double
```
Beachte die letzte Zeile: jeder von `Int` akzeptierte Text wird auch von `Double` akzeptiert, daher prüfe zuerst auf `Int`, wenn du sie unterscheiden willst.

# --instructions--

Schreibe eine Funktion `classify`, die ein `String` entgegennimmt (lasse das Argumentlabel weg) und `"Int"` zurückgibt, wenn der Text eine gültige Ganzzahl ist, `"Double"`, wenn er eine gültige Dezimalzahl ist, und sonst `"String"`

Beispiel eines Funktionsaufrufs:
```swift
print(classify("3.5"))
// prints Double
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`classify("42")` muss `"Int"` zurückgeben

```swift
tryCatch(classify("42") == "Int")
```

`classify("-7")` muss `"Int"` zurückgeben

```swift
tryCatch(classify("-7") == "Int")
```

`classify("3.5")` muss `"Double"` zurückgeben

```swift
tryCatch(classify("3.5") == "Double")
```

`classify("hello")` muss `"String"` zurückgeben

```swift
tryCatch(classify("hello") == "String")
```

`classify("")` muss `"String"` zurückgeben

```swift
tryCatch(classify("") == "String")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func classify(_ text: String) -> String {
    if Int(text) != nil {
        return "Int"
    }
    if Double(text) != nil {
        return "Double"
    }
    return "String"
}
```
