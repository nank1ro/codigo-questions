---
language: swift
exerciseType: 1
---

# --description--

Bedingte Casts lassen sich natürlich mit `else if` verketten, um mehrere mögliche Typen zu behandeln, und wandeln jeden in den Typ um, den du für das Ergebnis brauchst:
```swift
let item: Any = 2.5
if let number = item as? Int {
    print(Double(number))
} else if let number = item as? Double {
    print(number)
}
```
Ein in `Any` gespeicherter `Int` bleibt ein `Int`: `as? Double` darauf gibt `nil` zurück, weil `as?` den Typ prüft, es wandelt keine Zahlen um.

# --instructions--

Schreibe eine Funktion `sumNumbers`, die ein Array aus `Any` entgegennimmt (lasse das Argumentlabel weg) und die Summe ihrer `Int`- und `Double`-Elemente als `Double` zurückgibt, und die Elemente jedes anderen Typs ignoriert

Beispiel eines Funktionsaufrufs:
```swift
print(sumNumbers([1, 2.5, "x"]))
// prints 3.5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`sumNumbers([1, 2.5, "x"])` muss `3.5` zurückgeben

```swift
tryCatch(sumNumbers([1, 2.5, "x"]) == 3.5)
```

`sumNumbers(["3", 4])` muss `4.0` zurückgeben

```swift
tryCatch(sumNumbers(["3", 4]) == 4.0)
```

`sumNumbers([0.5, 0.25])` muss `0.75` zurückgeben

```swift
tryCatch(sumNumbers([0.5, 0.25]) == 0.75)
```

`sumNumbers(["a", "b"])` muss `0.0` zurückgeben

```swift
tryCatch(sumNumbers(["a", "b"]) == 0.0)
```

`sumNumbers([10])` muss ein `Double` zurückgeben

```swift
tryCatch(type(of: sumNumbers([10])) == Double.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func sumNumbers(_ values: [Any]) -> Double {
    var total = 0.0
    for value in values {
        if let number = value as? Int {
            total += Double(number)
        } else if let number = value as? Double {
            total += number
        }
    }
    return total
}
```
