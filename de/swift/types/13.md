---
language: swift
exerciseType: 2
---

# --description--

Wie du in den Strings-Übungen gesehen hast, gibt dir das Iterieren über einen `String` jeweils ein `Character`. Ein `Character` ist kein `String`, daher wandelst du ihn mit `String(c)` um, um ihn als Text zu verwenden.
Wenn das Zeichen eine Ziffer ist, gibt dir die Eigenschaft `wholeNumberValue` ihren Zahlenwert als `Int?`: sie ist `nil` für Zeichen, die keine Ziffern sind.
```swift
for c in "a1" {
    print(c.wholeNumberValue)
}
// nil
// Optional(1)
```

# --instructions--

Vervollständige die Schleife, sodass sie die Ziffern des Strings aufsummiert, sodass das Programm `8` ausgibt

# --seed--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.[/] {
        sum [/] digit
    }
}
print(sum)
```

# --answers--

- wholeNumberValue
- +=
- asciiValue
- isNumber
- -=
- =

# --solutions--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.wholeNumberValue {
        sum += digit
    }
}
print(sum)
```

# --output--

8
