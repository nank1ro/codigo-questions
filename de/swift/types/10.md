---
language: swift
exerciseType: 4
---

# --description--

Ein **Typalias** gibt einem bestehenden Typ einen neuen Namen, mit dem Schlüsselwort `typealias`:
```swift
typealias Score = Int
let best: Score = 100
print(best + 1) // 101
```
`Score` und `Int` sind derselbe Typ, daher lassen sie sich frei mischen. Ein Alias fügt keine Sicherheit hinzu: Er macht den Code nur besser lesbar, wenn ein einfacher Typ eine bestimmte Bedeutung in deinem Programm hat.

# --instructions--

Sortiere die Elemente, um in der ersten Zeile `Celsius` als Alias von `Double` zu definieren, den Siedepunkt von Wasser in Fahrenheit umzuwandeln und ihn auszugeben

# --answers--

```swift
typealias Celsius = Double
```

```swift
let boiling: Celsius = 100
```

```swift
let fahrenheit = boiling * 9 / 5 + 32
```

```swift
print(fahrenheit)
```

# --solutions--

```swift
typealias Celsius = Double
let boiling: Celsius = 100
let fahrenheit = boiling * 9 / 5 + 32
print(fahrenheit)
```

# --output--

212.0
