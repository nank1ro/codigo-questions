---
language: swift
exerciseType: 1
---

# --description--

Wenn eine Funktion ein Dezimalergebnis aus Ganzzahlen zurückgeben muss, wandle die Operanden vor der Division in `Double` um und deklariere den Rückgabetyp als `Double`:
```swift
func ratio(_ part: Int, _ total: Int) -> Double {
    return Double(part) / Double(total)
}
print(ratio(1, 4)) // 0.25
```
Denke daran, dass `count` eines Arrays ebenfalls ein `Int` ist, daher braucht es dieselbe Umwandlung.

# --instructions--

Schreibe eine Funktion `average`, die ein Array aus `Int` entgegennimmt (lasse das Argumentlabel weg) und den Durchschnitt ihrer Elemente als `Double` zurückgibt. Das Array ist nie leer

Beispiel eines Funktionsaufrufs:
```swift
print(average([1, 2]))
// prints 1.5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`average([1, 2])` muss `1.5` zurückgeben

```swift
tryCatch(average([1, 2]) == 1.5)
```

`average([3, 4, 5, 6])` muss `4.5` zurückgeben

```swift
tryCatch(average([3, 4, 5, 6]) == 4.5)
```

`average([10])` muss `10.0` zurückgeben

```swift
tryCatch(average([10]) == 10.0)
```

`average([1, 1, 2])` muss ein `Double` zurückgeben

```swift
tryCatch(type(of: average([1, 1, 2])) == Double.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func average(_ numbers: [Int]) -> Double {
    var sum = 0
    for number in numbers {
        sum += number
    }
    return Double(sum) / Double(numbers.count)
}
```
