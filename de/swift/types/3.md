---
language: swift
exerciseType: 1
---

# --description--

Die Funktion `type(of:)` gibt den Typ eines Werts zurück, was praktisch ist, um zu prüfen, was Swift abgeleitet hat:
```swift
print(type(of: 42))    // Int
print(type(of: 2.5))   // Double
print(type(of: "hi"))  // String
```
Wenn du einen anderen Typ als den abgeleiteten möchtest, füge eine Annotation hinzu. Ein Ganzzahl-Literal kann in einer `Double`- oder `Float`-Konstante gespeichert werden, und ein Ein-Zeichen-Literal in einer `Character`-Konstante:
```swift
let ratio: Double = 3       // 3.0, not an Int
let half: Float = 0.5
let initial: Character = "S"
print(type(of: ratio))      // Double
```

# --instructions--

Deklariere drei Konstanten:
- `ratio`, ein `Double`, das das Ganzzahl-Literal `3` enthält
- `half`, ein `Float`, das `0.5` enthält
- `initial`, ein `Character`, das `"S"` enthält

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`ratio` muss ein `Double` gleich `3.0` sein

```swift
tryCatch(type(of: ratio) == Double.self && ratio == 3.0)
```

`half` muss ein `Float` gleich `0.5` sein

```swift
tryCatch(type(of: half) == Float.self && half == 0.5)
```

`initial` muss ein `Character` gleich `"S"` sein

```swift
tryCatch(type(of: initial) == Character.self && initial == "S")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let ratio: Double = 3
let half: Float = 0.5
let initial: Character = "S"
```
