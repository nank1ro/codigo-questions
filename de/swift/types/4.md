---
language: swift
exerciseType: 2
---

# --description--

Swift wandelt Zahlentypen nie von selbst um: das Addieren eines `Int` zu einem `Double` ist ein Compilerfehler, obwohl beide Zahlen sind.
```swift
let apples = 3
let price = 1.5
let total = apples * price // error: Int and Double can't be mixed
```
Um sie zu kombinieren, erzeugst du einen neuen Wert des benötigten Typs, indem du den Wert an den Initialisierer des Typs übergibst:
```swift
let total = Double(apples) * price // 4.5
```
Dasselbe funktioniert umgekehrt: `Int(4.5)` erzeugt einen `Int` und behält nur den Ganzzahlanteil der Zahl.

# --instructions--

Vervollständige die Umwandlungen, sodass `total` die Kosten der Äpfel als `Double` und `whole` deren Ganzzahlanteil als `Int` ist, sodass das Programm `4.5` und `4` ausgibt

# --seed--

```swift
let apples = 3
let price = 1.5
let total = [/](apples) * price
let whole = [/](total)
print(total)
print(whole)
```

# --answers--

- Double
- Int
- Float
- String
- Character

# --solutions--

```swift
let apples = 3
let price = 1.5
let total = Double(apples) * price
let whole = Int(total)
print(total)
print(whole)
```

# --output--

4.5
4
