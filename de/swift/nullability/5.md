---
language: swift
exerciseType: 1
---

# --description--

Erst auf `nil` prüfen und dann erzwungen entpacken ist umständlich. Swift bietet **optionale Bindung** mit `if let`, die das Optional in einem einzigen Schritt entpackt und den Wert in einer neuen Konstante speichert:
```swift
let score: Int? = 10
if let value = score {
    print("Score: \(value)") // value ist ein Int, kein Int?
} else {
    print("No score")
}
```
Der Rumpf des `if` wird nur ausgeführt, wenn das Optional einen Wert enthält; darin ist `value` ein einfacher `Int` und benötigt kein `!`.

# --instructions--

Schreibe eine Funktion `describe`, die ein `Int?` entgegennimmt (lasse das Argumentlabel weg) und `"Value: "` gefolgt von der Zahl zurückgibt (zum Beispiel `"Value: 5"` für `5`), wenn das Optional einen Wert enthält, oder `"No value"`, wenn es `nil` ist

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`describe(5)` muss `"Value: 5"` zurückgeben

```swift
tryCatch(describe(5) == "Value: 5")
```

`describe(42)` muss `"Value: 42"` zurückgeben

```swift
tryCatch(describe(42) == "Value: 42")
```

`describe(nil)` muss `"No value"` zurückgeben

```swift
tryCatch(describe(nil) == "No value")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func describe(_ number: Int?) -> String {
    if let value = number {
        return "Value: \(value)"
    } else {
        return "No value"
    }
}
```
