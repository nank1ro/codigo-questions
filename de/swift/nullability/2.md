---
language: swift
exerciseType: 1
---

# --description--

Du kannst ein Optional mit `nil` mithilfe von `==` und `!=` vergleichen, und du kannst es auch direkt mit einem einfachen Wert des zugrunde liegenden Typs vergleichen:
```swift
var score: Int? = 10
print(score == nil) // false
print(score == 10)  // true
```
Denke daran, dass `Int?` und `Int` zwei unterschiedliche Typen sind: ein `Int?` kann leer sein, ein `Int` niemals.

# --instructions--

Deklariere eine optionale `String`-Variable `nickname` ohne Wert und eine optionale `Int`-Variable `score` mit dem Wert `10`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`nickname` muss `nil` sein

```swift
tryCatch(nickname == nil)
```

`nickname` muss ein optionaler `String` sein

```swift
tryCatch(type(of: nickname) == String?.self)
```

`score` muss gleich `10` sein

```swift
tryCatch(score == 10)
```

`score` muss ein optionaler `Int` sein

```swift
tryCatch(type(of: score) == Int?.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
var nickname: String?
var score: Int? = 10
```
