---
language: swift
exerciseType: 4
---

# --description--

Erzwungenes Entpacken ist nur sicher, wenn du bereits geprüft hast, dass das Optional nicht `nil` ist:
```swift
if score != nil {
    print(score! * 2)
}
```

# --instructions--

Sortiere die Elemente so, dass `price` verdoppelt und nur ausgegeben wird, nachdem geprüft wurde, dass es nicht `nil` ist

# --answers--

```swift
let price: Double? = 9.5
```

```swift
if price != nil {
```

```swift
    print(price! * 2)
```

```swift
}
```

# --solutions--

```swift
let price: Double? = 9.5
if price != nil {
    print(price! * 2)
}
```

# --output--

19.0
