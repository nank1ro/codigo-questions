---
language: swift
exerciseType: 1
---

# --description--

Manchmal möchtest du den Wert innerhalb eines Optionals transformieren und das Ergebnis optional halten, ohne von Hand zu entpacken und wieder zu verpacken.
Optionals haben eine `map`-Methode: sie wendet die Closure auf den Wert an, falls einer vorhanden ist, und gibt andernfalls `nil` zurück.
```swift
let score: Int? = 10
let doubled = score.map { $0 * 2 } // Int? mit 20
let missing: Int? = nil
let stillMissing = missing.map { $0 * 2 } // nil
```
Kombiniert mit einer fehlschlagbaren Umwandlung ergibt sich eine kompakte Pipeline: `Int(text).map { $0 + 1 }`.

# --instructions--

Schreibe eine Funktion `squareText`, die einen `String` entgegennimmt (lasse das Argumentlabel weg), ihn in einen `Int` umwandelt und dessen Quadrat als `Int?` zurückgibt, oder `nil`, wenn die Umwandlung fehlschlägt.
Verwende `map`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`squareText("4")` muss `16` zurückgeben

```swift
tryCatch(squareText("4") == 16)
```

`squareText("12")` muss `144` zurückgeben

```swift
tryCatch(squareText("12") == 144)
```

`squareText("x")` muss `nil` zurückgeben

```swift
tryCatch(squareText("x") == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func squareText(_ text: String) -> Int? {
    return Int(text).map { $0 * $0 }
}
```
