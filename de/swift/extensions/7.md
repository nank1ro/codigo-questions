---
language: swift
exerciseType: 4
---

# --description--

`Int`, `String`, Arrays und Structs sind **Werttypen**: Eine Methode kann den Wert, auf dem sie aufgerufen wird, nicht ändern, es sei denn, sie ist als `mutating` markiert. Extensions können auch `mutating`-Methoden hinzufügen:
```swift
extension Int {
    mutating func increment() {
        self += 1
    }
}
var count = 1
count.increment()
print(count) // 2
```
Innerhalb einer `mutating`-Methode kannst du `self` etwas zuweisen. Der Wert muss in einer `var` gespeichert sein: `increment()` auf einer `let`-Konstante aufzurufen ist ein Kompilierfehler.

# --instructions--

Ordne die Elemente so an, dass zuerst eine Extension von `Int` mit einer `mutating`-Methode `double()` definiert wird, dann eine Variable `n` mit dem Wert `4` deklariert wird, sie verdoppelt wird und ausgegeben wird

# --answers--

```swift
extension Int {
```

```swift
    mutating func double() { self *= 2 }
```

```swift
}
```

```swift
var n = 4
```

```swift
n.double()
```

```swift
print(n)
```

# --solutions--

```swift
extension Int {
    mutating func double() { self *= 2 }
}
var n = 4
n.double()
print(n)
```

# --output--

8
