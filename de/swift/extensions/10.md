---
language: swift
exerciseType: 1
---

# --description--

Extensions sind nicht nur für die Typen anderer. Eine übliche Art, deinen eigenen Code zu organisieren, ist, die gespeicherten Eigenschaften im Struct- oder Klassenrumpf zu behalten und das Verhalten in einer oder mehreren Extensions hinzuzufügen, die jeweils verwandte Member gruppieren:
```swift
struct Circle {
    var radius: Double
}
extension Circle {
    var diameter: Double {
        return radius * 2
    }
    func grown(by amount: Double) -> Circle {
        return Circle(radius: radius + amount)
    }
}
```
Member, die in einer Extension hinzugefügt wurden, können die gespeicherten Eigenschaften direkt verwenden, genau so, als wären sie innerhalb des Typs geschrieben.

# --instructions--

Das Struct `Rectangle` ist gegeben. Schreibe eine Extension von `Rectangle` mit:
- einer berechneten Eigenschaft `area` vom Typ `Double`, dem Produkt aus `width` und `height`
- einer Methode `scaled(by factor: Double)`, die ein neues `Rectangle` zurückgibt, dessen beide Seiten mit `factor` multipliziert sind

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Rectangle {
    var width: Double
    var height: Double
}
```

# --asserts--

Du musst eine Extension von `Rectangle` verwenden

```json
{
  "regex": "extension\\s+Rectangle",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Die Fläche eines `2` mal `3` Rechtecks muss `6.0` sein

```swift
tryCatch(Rectangle(width: 2, height: 3).area == 6.0)
```

Die Fläche eines `1.5` mal `4` Rechtecks muss `6.0` sein

```swift
tryCatch(Rectangle(width: 1.5, height: 4).area == 6.0)
```

Ein `2` mal `3` Rechteck, skaliert mit `2`, muss `4` mal `6` sein

```swift
let _r = Rectangle(width: 2, height: 3).scaled(by: 2)
tryCatch(_r.width == 4.0 && _r.height == 6.0)
```

Das Skalieren darf das ursprüngliche Rechteck nicht ändern

```swift
let _o = Rectangle(width: 2, height: 3)
let _s = _o.scaled(by: 0.5)
tryCatch(_o.width == 2.0 && _o.height == 3.0 && _s.area == 1.5)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Rectangle {
    var width: Double
    var height: Double
}
extension Rectangle {
    var area: Double {
        return width * height
    }
    func scaled(by factor: Double) -> Rectangle {
        return Rectangle(width: width * factor, height: height * factor)
    }
}
```
