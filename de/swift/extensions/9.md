---
language: swift
exerciseType: 2
---

# --description--

Eine Extension kann einem Typ neue **Initialisierer** hinzufügen. Bei einem Struct ist das der beste Ort dafür: Ein innerhalb des Struct-Rumpfs geschriebenes `init` ersetzt den automatischen Memberwise-Initialisierer, während ein in einer Extension hinzugefügter ihn beibehält.
Der neue Initialisierer delegiert üblicherweise mit `self.init(...)` an einen bestehenden:
```swift
struct Size {
    var width: Double
    var height: Double
}
extension Size {
    init(square side: Double) {
        self.init(width: side, height: side)
    }
}
let a = Size(square: 3)          // from the extension
let b = Size(width: 2, height: 5) // memberwise, still available
```

# --instructions--

Vervollständige die Extension so, dass `Point(both: 2.5)` einen Punkt mit `x` und `y` gleich `2.5` erstellt, sodass das Programm `2.5 2.5` und `1.0 2.0` ausgibt

# --seed--

```swift
struct Point {
    var x: Double
    var y: Double
}
extension Point {
    [/](both: Double) {
        [/](x: both, y: both)
    }
}
let p = Point(both: 2.5)
let q = Point(x: 1, y: 2)
print(p.x, p.y)
print(q.x, q.y)
```

# --answers--

- init
- self.init
- func
- Point
- self

# --solutions--

```swift
struct Point {
    var x: Double
    var y: Double
}
extension Point {
    init(both: Double) {
        self.init(x: both, y: both)
    }
}
let p = Point(both: 2.5)
let q = Point(x: 1, y: 2)
print(p.x, p.y)
print(q.x, q.y)
```

# --output--

2.5 2.5
1.0 2.0
