---
language: swift
exerciseType: 1
---

# --instructions--

Speichern Sie den Wert `false` in der Variablen `isOn`

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`isOn` muss gleich `false` sein.

```swift
    func test1() {
        XCTAssertEqual(isOn, false, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
let isOn = false;
```
