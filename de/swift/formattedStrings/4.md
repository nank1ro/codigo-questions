---
language: swift
exerciseType: 1
---

# --description--

Eine Zahl zwischen `%` und dem Buchstaben legt die **Mindestbreite** des Feldes fest. Ist der Wert kürzer, werden links Leerzeichen hinzugefügt, sodass er **rechtsbündig** ausgerichtet ist; ist er länger, wird nichts abgeschnitten:
```swift
print(String(format: "%5d|", 42))    //    42|
print(String(format: "%5d|", 12345)) // 12345|
```
Breite und Präzision lassen sich kombinieren: `%8.2f` bedeutet "mindestens 8 Zeichen breit, mit 2 Dezimalstellen":
```swift
print(String(format: "%8.2f|", 3.14159)) //     3.14|
```
Durch feste Breiten werden die Spalten einer Tabelle ausgerichtet.

# --instructions--

Schreibe eine Funktion `rightAligned`, die eine ganze Zahl entgegennimmt (lass das Argumentlabel weg) und sie als rechtsbündig ausgerichteten String in einem Feld der Breite `5` zurückgibt, wobei du `String(format:)` verwendest

Beispiel eines Funktionsaufrufs:
```swift
print(rightAligned(42) + "|")
// prints    42|
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func rightAligned() {

}
```

# --asserts--

Die Funktion muss `String(format:` verwenden

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`rightAligned(42)` muss `"   42"` zurückgeben

```swift
tryCatch(rightAligned(42) == "   42")
```

`rightAligned(7)` muss `"    7"` zurückgeben

```swift
tryCatch(rightAligned(7) == "    7")
```

`rightAligned(12345)` muss `"12345"` zurückgeben

```swift
tryCatch(rightAligned(12345) == "12345")
```

`rightAligned(123456)` muss `"123456"` zurückgeben (nichts wird abgeschnitten)

```swift
tryCatch(rightAligned(123456) == "123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func rightAligned(_ n: Int) -> String {
    return String(format: "%5d", n)
}
```
