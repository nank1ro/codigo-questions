---
language: swift
exerciseType: 1
---

# --description--

Um einen `String` in einen Formatstring einzufügen, verwende den Spezifizierer `%@`:
```swift
let name = "Ada"
let age = 36
print(String(format: "%@ is %d years old", name, age)) // Ada is 36 years old
```
`%@` akzeptiert einen Swift-`String` direkt. Verwende `%s` nicht mit einem Swift-String: Dieser Spezifizierer erwartet einen C-String und gibt Datenmüll aus oder stürzt ab.

# --instructions--

Schreibe eine Funktion `greeting`, die einen Namen (`String`) und ein Alter (`Int`) entgegennimmt, beide ohne Argumentlabel, und `<name> is <age> years old` zurückgibt, wobei du `String(format:)` mit `%@` und `%d` verwendest

Beispiel eines Funktionsaufrufs:
```swift
print(greeting("Ada", 36))
// prints Ada is 36 years old
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func greeting() {

}
```

# --asserts--

Die Funktion muss `String(format:` mit `%@` verwenden

```json
{
  "regex": "String\\(format:[^\\n]*%@",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greeting("Ada", 36)` muss `"Ada is 36 years old"` zurückgeben

```swift
tryCatch(greeting("Ada", 36) == "Ada is 36 years old")
```

`greeting("Linus", 54)` muss `"Linus is 54 years old"` zurückgeben

```swift
tryCatch(greeting("Linus", 54) == "Linus is 54 years old")
```

`greeting("Bo", 7)` muss `"Bo is 7 years old"` zurückgeben

```swift
tryCatch(greeting("Bo", 7) == "Bo is 7 years old")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greeting(_ name: String, _ age: Int) -> String {
    return String(format: "%@ is %d years old", name, age)
}
```
