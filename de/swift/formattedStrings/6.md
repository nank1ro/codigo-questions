---
language: swift
exerciseType: 1
---

# --description--

Eine weitere Flag ist `0`: Statt mit Leerzeichen wird das Feld links mit Nullen gefüllt. So erhält man Zahlen wie `007` oder `00042`:
```swift
print(String(format: "%05d", 42))  // 00042
print(String(format: "%03d", 7))   // 007
print(String(format: "%03d", 1234)) // 1234
```
Wie bei Leerzeichen wird ein Wert, der länger als die Breite ist, nie abgeschnitten.

# --instructions--

Schreibe eine Funktion `ticket`, die eine ganze Zahl entgegennimmt (lass das Argumentlabel weg) und `T-` gefolgt von der Zahl zurückgibt, die mit Nullen auf die Breite `5` aufgefüllt ist, wobei du `String(format:)` verwendest

Beispiel eines Funktionsaufrufs:
```swift
print(ticket(42))
// prints T-00042
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func ticket() {

}
```

# --asserts--

Die Funktion muss `String(format:` verwenden

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ticket(42)` muss `"T-00042"` zurückgeben

```swift
tryCatch(ticket(42) == "T-00042")
```

`ticket(7)` muss `"T-00007"` zurückgeben

```swift
tryCatch(ticket(7) == "T-00007")
```

`ticket(12345)` muss `"T-12345"` zurückgeben

```swift
tryCatch(ticket(12345) == "T-12345")
```

`ticket(123456)` muss `"T-123456"` zurückgeben

```swift
tryCatch(ticket(123456) == "T-123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func ticket(_ n: Int) -> String {
    return String(format: "T-%05d", n)
}
```
