---
language: swift
exerciseType: 1
---

# --description--

Breiten-Flags funktionieren nur innerhalb von `String(format:)`. Um einen einfachen String selbst aufzufüllen, erzeuge die Leerzeichen mit `String(repeating:count:)` und hänge sie an den Text an:
```swift
let text = "7"
let spaces = String(repeating: " ", count: 4 - text.count)
print(spaces + text + "|") //    7|
```
Ist der Text bereits länger als die Breite, wird `4 - text.count` negativ und `String(repeating:count:)` stürzt ab. Sichere es mit `max(0, ...)` ab, damit die Anzahl nie unter null fällt und langer Text unverändert bleibt.

# --instructions--

Schreibe eine Funktion `leftPad`, die einen Text (`String`) und eine Breite (`Int`) entgegennimmt, beide ohne Argumentlabel, und den Text rechtsbündig in dieser Breite zurückgibt, indem links Leerzeichen hinzugefügt werden. Text, der länger als die Breite ist, muss unverändert zurückgegeben werden

Beispiel eines Funktionsaufrufs:
```swift
print(leftPad("7", 4) + "|")
// prints    7|
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func leftPad() {

}
```

# --asserts--

`leftPad("7", 4)` muss `"   7"` zurückgeben

```swift
tryCatch(leftPad("7", 4) == "   7")
```

`leftPad("ab", 5)` muss `"   ab"` zurückgeben

```swift
tryCatch(leftPad("ab", 5) == "   ab")
```

`leftPad("abcd", 4)` muss `"abcd"` zurückgeben

```swift
tryCatch(leftPad("abcd", 4) == "abcd")
```

`leftPad("hello", 3)` muss `"hello"` zurückgeben (nichts wird abgeschnitten)

```swift
tryCatch(leftPad("hello", 3) == "hello")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func leftPad(_ text: String, _ width: Int) -> String {
    let spaces = String(repeating: " ", count: max(0, width - text.count))
    return spaces + text
}
```
