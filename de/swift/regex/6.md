---
language: swift
exerciseType: 1
---

# --description--

`m.0` ist kein `String`, sondern ein `Substring`: ein Blick in den ursprünglichen Text, keine Kopie. Es wird genau wie ein String ausgegeben, aber wo ein `String` verlangt wird, musst du es umwandeln:
```swift
let text = "order 42"
if let m = text.firstMatch(of: #/\d+/#) {
    let found: String = String(m.0)
    print(found) // 42
}
```
Zahleninitialisierer nehmen ein `Substring` direkt an, daher funktioniert `Int(m.0)` ohne den Umweg.

# --instructions--

Schreibe eine Funktion `firstNumber`, die einen Text (`String`) ohne Argumentlabel entgegennimmt und die erste Folge von Ziffern als `String` zurückgibt, oder `"none"`, wenn der Text keine Ziffer enthält

Beispiel für einen Funktionsaufruf:
```swift
print(firstNumber("order 42 today 7"))
// prints 42
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func firstNumber() {

}
```

# --asserts--

Die Funktion muss `firstMatch(of:` verwenden

```json
{
  "regex": "firstMatch\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstNumber("order 42 today 7")` muss `"42"` zurückgeben

```swift
tryCatch(firstNumber("order 42 today 7") == "42")
```

`firstNumber("a1")` muss `"1"` zurückgeben

```swift
tryCatch(firstNumber("a1") == "1")
```

`firstNumber("100")` muss `"100"` zurückgeben

```swift
tryCatch(firstNumber("100") == "100")
```

`firstNumber("abc")` muss `"none"` zurückgeben

```swift
tryCatch(firstNumber("abc") == "none")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstNumber(_ text: String) -> String {
    if let m = text.firstMatch(of: #/\d+/#) {
        return String(m.0)
    }
    return "none"
}
```
