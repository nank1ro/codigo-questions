---
language: swift
exerciseType: 1
---

# --description--

Wenn keine der Kurzschreibweisen passt, liste die Zeichen, die du akzeptierst, in eckigen Klammern auf. `[abc]` trifft auf ein `a`, ein `b` oder ein `c`, und ein Bindestrich schreibt einen Bereich:
```swift
print("f".contains(#/[a-f]/#))  // true
print("Z".contains(#/[A-Z]/#))  // true
print("5".contains(#/[0-9a-f]/#)) // true
```
Eine Zahl in geschweiften Klammern sagt genau, wie oft sich das vorherige Muster wiederholt: `{3}` bedeutet dreimal, `{2,4}` bedeutet zwischen zwei- und viermal:
```swift
print("aaa".contains(#/^a{3}$/#))  // true
print("aa".contains(#/^a{3}$/#))   // false
```
Ein Muster mit `^` und `$` zu umschließen und mit einer Anzahl zu versehen, ist die übliche Methode, um zu prüfen, dass ein ganzer Text eine bestimmte Form hat.

# --instructions--

Schreibe eine Funktion `isCode`, die einen Text (`String`) ohne Argumentlabel entgegennimmt und `true` zurückgibt, nur wenn der ganze Text genau drei Großbuchstaben ist, unter Verwendung eines Regex

Beispiel für einen Funktionsaufruf:
```swift
print(isCode("ABC"))
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func isCode() {

}
```

# --asserts--

Die Funktion muss ein Regex-Literal verwenden, das mit `#/` geschrieben ist

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`isCode("ABC")` muss `true` zurückgeben

```swift
tryCatch(isCode("ABC") == true)
```

`isCode("XYZ")` muss `true` zurückgeben

```swift
tryCatch(isCode("XYZ") == true)
```

`isCode("AB")` muss `false` zurückgeben

```swift
tryCatch(isCode("AB") == false)
```

`isCode("ABCD")` muss `false` zurückgeben

```swift
tryCatch(isCode("ABCD") == false)
```

`isCode("abc")` muss `false` zurückgeben

```swift
tryCatch(isCode("abc") == false)
```

`isCode("A1C")` muss `false` zurückgeben

```swift
tryCatch(isCode("A1C") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func isCode(_ text: String) -> Bool {
    return text.contains(#/^[A-Z]{3}$/#)
}
```
