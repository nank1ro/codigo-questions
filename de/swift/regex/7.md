---
language: swift
exerciseType: 4
---

# --description--

`matches(of:)` gibt **jede** Übereinstimmung statt nur der ersten zurück, als Array. Das Array ist nie `nil`: Wenn nichts passt, ist es einfach leer, sodass du es direkt durchlaufen oder umwandeln kannst:
```swift
let text = "a1 b22"
print(text.matches(of: #/\d+/#).count) // 2
```
Jedes Element ist eine Übereinstimmung, daher ist `$0.0` in einem `map` der gefundene Text:
```swift
let found = text.matches(of: #/\d+/#).map { String($0.0) }
print(found) // ["1", "22"]
```

# --instructions--

Sortiere die Elemente, um ein Programm zu bauen, das jede Folge von Ziffern von `text` in `found` sammelt und sie ausgibt

# --answers--

```swift
let text = "a1 b22 c333"
```

```swift
let pattern = #/\d+/#
```

```swift
let found = text.matches(of: pattern).map { String($0.0) }
```

```swift
print(found)
```

# --solutions--

```swift
let text = "a1 b22 c333"
let pattern = #/\d+/#
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

```swift
let pattern = #/\d+/#
let text = "a1 b22 c333"
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

# --output--

["1", "22", "333"]
