---
language: swift
exerciseType: 4
---

# --description--

Einen ganzen Text mit `^` und `$` zu prüfen funktioniert, aber `wholeMatch(of:)` sagt es direkt: Es gibt nur dann eine Übereinstimmung zurück, wenn das Muster den Text vom ersten bis zum letzten Zeichen abdeckt, und sonst `nil`:
```swift
print("1a2b".wholeMatch(of: #/[0-9a-f]+/#) != nil) // true
print("1z".wholeMatch(of: #/[0-9a-f]+/#) != nil)   // false
```
Verwende `firstMatch(of:)`, um etwas in einem Text zu finden, und `wholeMatch(of:)`, um zu prüfen, dass ein Text genau eine Form hat.

# --instructions--

Sortiere die Elemente, um ein Programm zu bauen, das `isHex` vor der Schleife definiert und das Ergebnis für jeden Wert von `values` ausgibt

# --answers--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
```

```swift
let values = ["1a2b", "1z", "ff"]
```

```swift
for value in values {
    print(isHex(value))
}
```

# --solutions--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
let values = ["1a2b", "1z", "ff"]
for value in values {
    print(isHex(value))
}
```

```swift
let values = ["1a2b", "1z", "ff"]
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
for value in values {
    print(isHex(value))
}
```

# --output--

true
false
true
