---
language: swift
exerciseType: 1
---

# --description--

`replacing(_:with:)` tauscht jede Übereinstimmung gegen einen festen Text und gibt einen neuen `String` zurück, ohne den ursprünglichen zu verändern:
```swift
let text = "a1 b22"
print(text.replacing(#/\d+/#, with: "#")) // a# b#
print(text)                               // a1 b22
```
Beachte, dass `\d+` eine ganze Folge von Ziffern durch ein einzelnes `#` ersetzt, während `\d` eine Ziffer nach der anderen ersetzen würde. Das Muster entscheidet, wie viel verschwindet.

# --instructions--

Schreibe eine Funktion `mask`, die einen Text (`String`) ohne Argumentlabel entgegennimmt und ihn mit jeder einzelnen durch ein `#` ersetzten Ziffer zurückgibt

Beispiel für einen Funktionsaufruf:
```swift
print(mask("a1 b22"))
// gibt a# b## aus
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func mask() {

}
```

# --asserts--

Die Funktion muss `replacing(` verwenden

```json
{
  "regex": "replacing\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`mask("a1 b22")` muss `"a# b##"` zurückgeben

```swift
tryCatch(mask("a1 b22") == "a# b##")
```

`mask("2024")` muss `"####"` zurückgeben

```swift
tryCatch(mask("2024") == "####")
```

`mask("abc")` muss `"abc"` zurückgeben

```swift
tryCatch(mask("abc") == "abc")
```

`mask("")` muss `""` zurückgeben

```swift
tryCatch(mask("") == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func mask(_ text: String) -> String {
    return text.replacing(#/\d/#, with: "#")
}
```
