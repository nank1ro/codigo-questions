---
language: swift
exerciseType: 1
---

# --description--

Weil `Int(_:)` ein `Substring` akzeptiert, ist das Umwandeln von gefundenem Text in Zahlen ein einziger Schritt. `compactMap` ist hier praktisch: Es verwirft die Werte, die `nil` zurückgeben:
```swift
let text = "a1 b22"
let numbers = text.matches(of: #/\d+/#).compactMap { Int($0.0) }
print(numbers) // [1, 22]
```
Verwende `map`, wenn sich jedes Element umwandeln lässt, und `compactMap`, wenn einige fehlschlagen können.

# --instructions--

Schreibe eine Funktion `numbers`, die einen Text (`String`) ohne Argumentlabel entgegennimmt und jede darin enthaltene Folge von Ziffern als Array von `Int` zurückgibt, in der Reihenfolge ihres Auftretens

Beispiel für einen Funktionsaufruf:
```swift
print(numbers("a1 b22 c333"))
// prints [1, 22, 333]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func numbers() {

}
```

# --asserts--

Die Funktion muss `matches(of:` verwenden

```json
{
  "regex": "matches\\(of:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`numbers("a1 b22 c333")` muss `[1, 22, 333]` zurückgeben

```swift
tryCatch(numbers("a1 b22 c333") == [1, 22, 333])
```

`numbers("7")` muss `[7]` zurückgeben

```swift
tryCatch(numbers("7") == [7])
```

`numbers("abc")` muss `[]` zurückgeben

```swift
tryCatch(numbers("abc") == [])
```

`numbers("10-20")` muss `[10, 20]` zurückgeben

```swift
tryCatch(numbers("10-20") == [10, 20])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func numbers(_ text: String) -> [Int] {
    return text.matches(of: #/\d+/#).compactMap { Int($0.0) }
}
```
