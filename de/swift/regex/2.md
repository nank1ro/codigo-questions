---
language: swift
exerciseType: 1
---

# --description--

Ein paar Kurzschreibweisen decken die meisten Muster ab. Jede davon passt auf genau **ein** Zeichen:
- `\d` ist eine Ziffer
- `\w` ist ein Buchstabe, eine Ziffer oder ein Unterstrich
- `\s` ist ein Leerzeichen, ein Tabulator oder ein Zeilenumbruch
- `.` ist ein beliebiges einzelnes Zeichen

Um mehr als ein Zeichen zu treffen, füge direkt nach dem Muster einen **Quantifizierer** hinzu:
- `+` bedeutet eines oder mehr
- `*` bedeutet null oder mehr
- `?` bedeutet null oder eins

Also ist `\w+` ein Wort, `\s*` ein optionaler Abstand und `\d?` eine optionale Ziffer:
```swift
print("hello world".contains(#/\w+\s\w+/#)) // true
print("hello".contains(#/\w+\s\w+/#))       // false
```
Ein Zeichen ohne besondere Bedeutung passt einfach auf sich selbst, daher trifft `#/cat/#` auf die drei Buchstaben `cat`.

# --instructions--

Schreibe eine Funktion `hasDigit`, die einen Text (`String`) ohne Argumentlabel entgegennimmt und `true` zurückgibt, wenn der Text mindestens eine Ziffer enthält, unter Verwendung eines Regex

Beispiel für einen Funktionsaufruf:
```swift
print(hasDigit("abc1"))
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func hasDigit() {

}
```

# --asserts--

Die Funktion muss ein Regex-Literal verwenden, das mit `#/` geschrieben ist

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`hasDigit("abc1")` muss `true` zurückgeben

```swift
tryCatch(hasDigit("abc1") == true)
```

`hasDigit("7")` muss `true` zurückgeben

```swift
tryCatch(hasDigit("7") == true)
```

`hasDigit("abc")` muss `false` zurückgeben

```swift
tryCatch(hasDigit("abc") == false)
```

`hasDigit("")` muss `false` zurückgeben

```swift
tryCatch(hasDigit("") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func hasDigit(_ text: String) -> Bool {
    return text.contains(#/\d/#)
}
```
