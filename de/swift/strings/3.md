---
language: swift
exerciseType: 2
---

# --description--

Zwei Strings können mit dem `+`-Operator verbunden werden, der einen neuen String erzeugt:
```swift
let full = "Hello" + " " + "world" // Hello world
```
Um am Ende einer bestehenden String-Variable Text hinzuzufügen, verwendest du `+=`. Die Variable muss mit `var` deklariert werden, weil sich ihr Wert ändert:
```swift
var log = "Start"
log += "..."
print(log) // Start...
```

# --instructions--

Deklariere eine Variable `message` mit dem Wert `Hello`, hänge mit dem zusammengesetzten Zuweisungsoperator `, world` an und gib sie dann aus

# --seed--

```swift
[/] message = "Hello"
message [/] ", world"
print(message)
```

# --answers--

- var
- let
- +=
- +
- =
- -=

# --solutions--

```swift
var message = "Hello"
message += ", world"
print(message)
```

# --output--

Hello, world
