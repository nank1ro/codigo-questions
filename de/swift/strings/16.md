---
language: swift
exerciseType: 1
---

# --description--

Das Foundation-Framework fügt viele zusätzliche String-Methoden hinzu. Eine der nützlichsten ist `replacingOccurrences(of:with:)`, die einen neuen String zurückgibt, in dem jedes Vorkommen des ersten Textes durch den zweiten ersetzt wird:
```swift
import Foundation

let path = "a/b/c"
print(path.replacingOccurrences(of: "/", with: "-")) // a-b-c
```
Denke daran, am Anfang der Datei `import Foundation` zu schreiben, sonst ist die Methode nicht verfügbar. Methodenaufrufe können verkettet werden, sodass `text.lowercased().replacingOccurrences(of: " ", with: "_")` gültig ist.

# --instructions--

Schreibe eine Funktion `snakeCase`, die einen `String` entgegennimmt (lass das Argumentlabel weg) und ihn in Kleinbuchstaben zurückgibt, wobei jedes Leerzeichen durch einen Unterstrich `_` ersetzt wird

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`snakeCase("Hello World")` muss `"hello_world"` zurückgeben

```swift
tryCatch(snakeCase("Hello World") == "hello_world")
```

`snakeCase("My First Post")` muss `"my_first_post"` zurückgeben

```swift
tryCatch(snakeCase("My First Post") == "my_first_post")
```

`snakeCase("swift")` muss `"swift"` zurückgeben

```swift
tryCatch(snakeCase("swift") == "swift")
```

`snakeCase("")` muss `""` zurückgeben

```swift
tryCatch(snakeCase("") == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func snakeCase(_ text: String) -> String {
    return text.lowercased().replacingOccurrences(of: " ", with: "_")
}
```
