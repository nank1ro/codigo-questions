---
language: swift
exerciseType: 1
---

# --description--

Die Arbeit mit Indizes ist umständlich, daher bietet Swift Abkürzungen für die häufigsten Fälle:
- `first` und `last` geben das erste und letzte Zeichen als optionales `Character?` zurück (`nil` bei einem leeren String)
- `prefix(n)` gibt die ersten `n` Zeichen zurück und `suffix(n)` die letzten `n`
```swift
let word = "Swift"
print(word.first!)     // S
print(word.prefix(2))  // Sw
print(word.suffix(3))  // ift
```
`prefix` und `suffix` geben eine `Substring` zurück, eine Sicht auf den ursprünglichen Text. Um sie als echten `String` zu speichern, umschließe sie mit `String(...)`. Wenn `n` größer als `count` ist, erhältst du einfach den ganzen String.

# --instructions--

Schreibe eine Funktion `firstTwo`, die einen `String` entgegennimmt (lass das Argumentlabel weg) und dessen erste zwei Zeichen als `String` zurückgibt, sowie eine Funktion `lastTwo`, die dessen letzte zwei Zeichen als `String` zurückgibt

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`firstTwo("Swift")` muss `"Sw"` zurückgeben

```swift
tryCatch(firstTwo("Swift") == "Sw")
```

`firstTwo("a")` muss `"a"` zurückgeben

```swift
tryCatch(firstTwo("a") == "a")
```

`lastTwo("Swift")` muss `"ft"` zurückgeben

```swift
tryCatch(lastTwo("Swift") == "ft")
```

`lastTwo("")` muss `""` zurückgeben

```swift
tryCatch(lastTwo("") == "")
```

`firstTwo` muss einen `String` zurückgeben

```swift
tryCatch(type(of: firstTwo("Swift")) == String.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstTwo(_ text: String) -> String {
    return String(text.prefix(2))
}

func lastTwo(_ text: String) -> String {
    return String(text.suffix(2))
}
```
