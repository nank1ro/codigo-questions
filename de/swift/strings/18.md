---
language: swift
exerciseType: 4
---

# --description--

Ein `Character` ist kein `String`, daher kann er nicht direkt mit `+` an einen String angehängt werden. Wandle ihn zuerst mit `String(...)` um:
```swift
let letter: Character = "a"
let text = String(letter) + "bc" // abc
```
Kombiniert mit einer `for`-`in`-Schleife lässt sich damit ein String Zeichen für Zeichen neu aufbauen, zum Beispiel indem jedes neue Zeichen vor die bisher gesammelten gesetzt wird.

# --instructions--

Sortiere die Elemente, um `swift` rückwärts auszugeben, indem du das Ergebnis Zeichen für Zeichen aufbaust.
Deklariere `word` vor `reversed`

# --answers--

```swift
let word = "swift"
```

```swift
var reversed = ""
```

```swift
for letter in word {
```

```swift
    reversed = String(letter) + reversed
```

```swift
}
```

```swift
print(reversed)
```

# --solutions--

```swift
let word = "swift"
var reversed = ""
for letter in word {
    reversed = String(letter) + reversed
}
print(reversed)
```

# --output--

tfiws
