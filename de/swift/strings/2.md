---
language: swift
exerciseType: 1
---

# --description--

Die **String-Interpolation** fügt den Wert eines Ausdrucks in ein String-Literal ein. Setze den Ausdruck in `\()`:
```swift
let name = "Ada"
let age = 36
print("\(name) is \(age) years old") // Ada is 36 years old
```
Jeder Typ kann interpoliert werden: Zahlen, Booleans und andere Strings werden automatisch in Text umgewandelt.

# --instructions--

Schreibe eine Funktion `greet`, die einen `String` entgegennimmt (lass das Argumentlabel weg) und den Text `Hello, <name>!` zurückgibt, wobei `<name>` das Argument ist.

Beispiel für einen Funktionsaufruf:
```swift
print(greet("Ada"))
// prints Hello, Ada!
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`greet("Ada")` muss `"Hello, Ada!"` zurückgeben

```swift
tryCatch(greet("Ada") == "Hello, Ada!")
```

`greet("Swift")` muss `"Hello, Swift!"` zurückgeben

```swift
tryCatch(greet("Swift") == "Hello, Swift!")
```

`greet("")` muss `"Hello, !"` zurückgeben

```swift
tryCatch(greet("") == "Hello, !")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greet(_ name: String) -> String {
    return "Hello, \(name)!"
}
```
