---
language: swift
exerciseType: 1
---

# --description--

Ein `String` ist eine Sammlung von `Character`-Werten. Ein `Character` ist ein einzelner Buchstabe, eine Ziffer, ein Symbol oder ein Leerzeichen, und er wird mit denselben doppelten Anführungszeichen wie ein String geschrieben, daher benötigst du eine Typannotation, um einen zu erhalten:
```swift
let letter: Character = "a"
let text = "abc"
print(text.count) // 3
```
`isEmpty` zu prüfen ist besser, als `count` mit `0` zu vergleichen: Es liest sich besser und muss nicht jedes Zeichen zählen.

# --instructions--

Schreibe eine Funktion `length`, die einen `String` entgegennimmt (lass das Argumentlabel weg) und dessen Anzahl an Zeichen zurückgibt, sowie eine Funktion `hasText`, die einen `String` entgegennimmt (lass das Argumentlabel weg) und nur dann `true` zurückgibt, wenn der String nicht leer ist

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`length("Swift")` muss `5` zurückgeben

```swift
tryCatch(length("Swift") == 5)
```

`length("a b c")` muss `5` zurückgeben

```swift
tryCatch(length("a b c") == 5)
```

`length("")` muss `0` zurückgeben

```swift
tryCatch(length("") == 0)
```

`hasText("x")` muss `true` zurückgeben

```swift
tryCatch(hasText("x") == true)
```

`hasText("")` muss `false` zurückgeben

```swift
tryCatch(hasText("") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func length(_ text: String) -> Int {
    return text.count
}

func hasText(_ text: String) -> Bool {
    return !text.isEmpty
}
```
