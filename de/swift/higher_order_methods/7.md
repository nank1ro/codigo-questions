---
language: swift
exerciseType: 1
---

# --description--

Funktionen höherer Ordnung funktionieren mit jedem Array, auch mit Arrays aus eigenen Structs. `filter` und danach `map` zu verketten ist die übliche Methode, um einige Elemente auszuwählen und aus jedem einen Wert zu extrahieren:
```swift
struct Book {
    var title: String
    var pages: Int
}
let books = [Book(title: "Dune", pages: 412), Book(title: "Haiku", pages: 40)]
let long = books.filter { $0.pages > 100 }.map { $0.title }
print(long) // ["Dune"]
```
In umgekehrter Reihenfolge, erst `map` dann `filter`, würde die `pages`-Eigenschaft verloren gehen, bevor die Prüfung sie verwenden könnte.

# --instructions--

Das Struct `Product` ist vorgegeben. Schreibe eine Funktion `cheapNames`, die ein Array aus `Product` entgegennimmt (lasse das Argumentlabel weg) und die Namen der Produkte zurückgibt, deren `price` unter `10` liegt, in der ursprünglichen Reihenfolge, indem du `filter` und `map` verkettest

Beispiel eines Funktionsaufrufs:
```swift
print(cheapNames([Product(name: "Pen", price: 2.5), Product(name: "Bag", price: 30)]))
// prints ["Pen"]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Product {
    var name: String
    var price: Double
}
```

# --asserts--

Du musst `filter` verwenden

```json
{
  "regex": "\\.filter",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst `map` verwenden

```json
{
  "regex": "\\.map",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`cheapNames` muss `["Pen"]` zurückgeben für einen Stift zum Preis von `2.5` und eine Tasche zum Preis von `30`

```swift
tryCatch(cheapNames([Product(name: "Pen", price: 2.5), Product(name: "Bag", price: 30)]) == ["Pen"])
```

`cheapNames` muss die ursprüngliche Reihenfolge beibehalten

```swift
tryCatch(cheapNames([Product(name: "Tea", price: 4), Product(name: "Cup", price: 9.99), Product(name: "Pot", price: 25)]) == ["Tea", "Cup"])
```

Ein Produkt mit genau dem Preis `10` muss ausgeschlossen werden

```swift
tryCatch(cheapNames([Product(name: "Cap", price: 10)]) == [])
```

`cheapNames([])` muss `[]` zurückgeben

```swift
tryCatch(cheapNames([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Product {
    var name: String
    var price: Double
}
func cheapNames(_ products: [Product]) -> [String] {
    return products.filter { $0.price < 10 }.map { $0.name }
}
```
