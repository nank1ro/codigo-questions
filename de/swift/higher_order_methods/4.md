---
language: swift
exerciseType: 2
---

# --description--

`reduce` baut bei jedem Schritt einen neuen Akkumulationswert auf, was verschwenderisch ist, wenn das Ergebnis ein Array oder ein Wörterbuch ist. `reduce(into:)` übergibt der Closure den Akkumulator als `inout`-Parameter, sodass er direkt vor Ort verändert werden kann, ohne `return`:
```swift
let words = ["a", "b", "a"]
let counts = words.reduce(into: [String: Int]()) { result, word in
    result[word, default: 0] += 1
}
print(counts["a"]!) // 2
```
`[String: Int]()` erzeugt ein leeres Wörterbuch, und `result[word, default: 0]` liest die aktuelle Anzahl oder `0`, wenn der Schlüssel fehlt.

# --instructions--

Vervollständige den Code, um mit `reduce(into:)` zu zählen, wie oft jeder Buchstabe vorkommt, sodass das Programm `3` ausgibt

# --seed--

```swift
let letters = ["x", "y", "x", "x"]
let counts = letters.reduce([/] [String: Int]()) { result, letter in
    result[letter, [/] 0] += 1
}
print(counts["x"]!)
```

# --answers--

- into:
- default:
- 0,
- initial:
- or:

# --solutions--

```swift
let letters = ["x", "y", "x", "x"]
let counts = letters.reduce(into: [String: Int]()) { result, letter in
    result[letter, default: 0] += 1
}
print(counts["x"]!)
```

# --output--

3
