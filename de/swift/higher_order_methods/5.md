---
language: swift
exerciseType: 1
---

# --description--

Einige Funktionen höherer Ordnung beantworten eine Frage über die Sammlung, statt sie zu transformieren. Sie nehmen alle eine Closure entgegen, die einen `Bool` zurückgibt:
- `first(where:)` gibt das erste Element zurück, das die Closure erfüllt, oder `nil`, wenn es keines gibt
- `contains(where:)` gibt `true` zurück, wenn mindestens ein Element sie erfüllt
- `allSatisfy` gibt `true` zurück, wenn jedes Element sie erfüllt

```swift
let nums = [4, 9, 16]
print(nums.first(where: { $0 > 5 }))  // Optional(9)
print(nums.contains(where: { $0 > 5 })) // true
print(nums.allSatisfy { $0 > 5 })       // false
```
Anders als `filter` stoppt `first(where:)` bei der ersten Übereinstimmung und baut kein neues Array auf.

# --instructions--

Schreibe eine Funktion `firstLong`, die ein Array aus `String` und einen `Int` namens `minLength` entgegennimmt (lasse beide Argumentlabel weg) und die erste Zeichenkette mit mindestens `minLength` Zeichen zurückgibt, oder `nil`, wenn es keine gibt, unter Verwendung von `first(where:)`

Beispiel eines Funktionsaufrufs:
```swift
print(firstLong(["hi", "hello", "hey"], 4))
// prints Optional("hello")
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Du musst `first(where:)` verwenden

```json
{
  "regex": "\\.first\\s*\\(\\s*where",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstLong(["hi", "hello", "hey"], 4)` muss `"hello"` zurückgeben

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 4) == "hello")
```

`firstLong(["hi", "hello", "hey"], 2)` muss `"hi"` zurückgeben

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 2) == "hi")
```

`firstLong(["hi", "hey"], 4)` muss `nil` zurückgeben

```swift
tryCatch(firstLong(["hi", "hey"], 4) == nil)
```

`firstLong([], 1)` muss `nil` zurückgeben

```swift
tryCatch(firstLong([], 1) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstLong(_ words: [String], _ minLength: Int) -> String? {
    return words.first(where: { $0.count >= minLength })
}
```
