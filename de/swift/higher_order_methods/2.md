---
language: swift
exerciseType: 1
---

# --description--

`flatMap` ist für Closures, die ein **Array** zurückgeben: Statt ein Array von Arrays zu erzeugen, fügt es alle zurückgegebenen Arrays zu einem einzigen flachen Ergebnis zusammen:
```swift
let teams = [["Ann", "Bob"], ["Cid"]]
print(teams.map { $0 })     // [["Ann", "Bob"], ["Cid"]]
print(teams.flatMap { $0 }) // ["Ann", "Bob", "Cid"]
```
Die Closure kann jedes innere Array auch verändern, bevor es zusammengefügt wird, zum Beispiel gibt `teams.flatMap { $0.reversed() }` `["Bob", "Ann", "Cid"]` zurück.

# --instructions--

Schreibe eine Funktion `flatten`, die ein Array aus Arrays von `Int` entgegennimmt (lasse das Argumentlabel weg) und ein einzelnes Array mit allen Zahlen in der ursprünglichen Reihenfolge zurückgibt, unter Verwendung von `flatMap`

Beispiel eines Funktionsaufrufs:
```swift
print(flatten([[1, 2], [3]]))
// prints [1, 2, 3]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Du musst `flatMap` verwenden

```json
{
  "regex": "\\.flatMap",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`flatten([[1, 2], [3]])` muss `[1, 2, 3]` zurückgeben

```swift
tryCatch(flatten([[1, 2], [3]]) == [1, 2, 3])
```

`flatten([[5], [], [6, 7]])` muss `[5, 6, 7]` zurückgeben

```swift
tryCatch(flatten([[5], [], [6, 7]]) == [5, 6, 7])
```

`flatten([])` muss `[]` zurückgeben

```swift
tryCatch(flatten([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func flatten(_ lists: [[Int]]) -> [Int] {
    return lists.flatMap { $0 }
}
```
