---
language: swift
exerciseType: 1
---

# --description--

`enumerated()` verwandelt ein Array in eine Sequenz von `(offset, element)`-Paaren, sodass eine Closure die Position jedes Elements zusammen mit seinem Wert verwenden kann:
```swift
let steps = ["mix", "bake"]
let numbered = steps.enumerated().map { pair in
    "\(pair.offset + 1). \(pair.element)"
}
print(numbered) // ["1. mix", "2. bake"]
```
Da jedes Paar ein Tupel ist, kann die Closure es auch destrukturieren: `.map { (i, step) in "\(i + 1). \(step)" }`.

# --instructions--

Schreibe eine Funktion `evenPositions`, die ein Array aus `String` entgegennimmt (lasse das Argumentlabel weg) und die Elemente an geraden Indizes (0, 2, 4, ...) zurückgibt, unter Verwendung von `enumerated()`, `filter` und `map`

Beispiel eines Funktionsaufrufs:
```swift
print(evenPositions(["a", "b", "c", "d", "e"]))
// prints ["a", "c", "e"]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Du musst `enumerated()` verwenden

```json
{
  "regex": "\\.enumerated\\s*\\(\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du darfst keine `for`-Schleife verwenden

```json
{
  "regex": "\\bfor\\b",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`evenPositions(["a", "b", "c", "d", "e"])` muss `["a", "c", "e"]` zurückgeben

```swift
tryCatch(evenPositions(["a", "b", "c", "d", "e"]) == ["a", "c", "e"])
```

`evenPositions(["x", "y"])` muss `["x"]` zurückgeben

```swift
tryCatch(evenPositions(["x", "y"]) == ["x"])
```

`evenPositions([])` muss `[]` zurückgeben

```swift
tryCatch(evenPositions([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func evenPositions(_ items: [String]) -> [String] {
    return items.enumerated().filter { $0.offset % 2 == 0 }.map { $0.element }
}
```
