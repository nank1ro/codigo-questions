---
language: swift
exerciseType: 1
---

# --description--

Ein Struct ist ein Typ wie jeder andere, also kann es in einem Array, einem Wörterbuch oder einem Set gespeichert werden, und jedes Werkzeug, das du bereits kennst, funktioniert damit weiter:
```swift
struct Item {
    var name: String
    var price: Int
}

let items = [Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]

for item in items {
    print(item.name)
}

let names = items.map { $0.name }
let total = items.reduce(0) { $0 + $1.price }
print(total) // 10
```
Denke daran, dass das Array *Kopien* enthält: `items[0]` in eine Variable zu lesen und sie zu ändern, berührt das Array nicht.

# --instructions--

Schreibe eine Funktion `totalPrice`, die ein Array von `Item` ohne Argument-Label entgegennimmt und die Summe der `price` jedes Elements zurückgibt, oder `0`, wenn das Array leer ist

Beispiel eines Funktionsaufrufs:
```swift
print(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]))
// prints 10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice() {

}
```

# --asserts--

Zwei Artikel von `3` und `7` müssen zusammen `10` ergeben

```swift
tryCatch(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]) == 10)
```

Ein einzelner Artikel muss seinen eigenen Preis ergeben

```swift
tryCatch(totalPrice([Item(name: "Pen", price: 2)]) == 2)
```

Ein leeres Array muss `0` ergeben

```swift
tryCatch(totalPrice([]) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice(_ items: [Item]) -> Int {
    return items.reduce(0) { $0 + $1.price }
}
```
