---
language: swift
exerciseType: 2
---

# --description--

Einer gespeicherten Eigenschaft kann direkt dort, wo sie deklariert wird, ein **Standardwert** gegeben werden. Swift leitet ihren Typ aus diesem Wert ab, sodass du die Typannotation weglassen kannst:
```swift
struct Counter {
    var label: String
    var value = 0
}
```
Der memberwise initializer macht aus jeder Eigenschaft mit Standardwert ein optionales Argument: übergib es, um den Standardwert zu überschreiben, und lasse es weg, um ihn zu behalten.
```swift
let a = Counter(label: "clicks")
print(a.value) // 0

let b = Counter(label: "clicks", value: 7)
print(b.value) // 7
```

# --instructions--

Vervollständige den Code so, dass `value` bei `0` startet und `c` ohne Übergabe von `value` erstellt wird

# --seed--

```swift
struct Counter {
    var label: String
    var value = [/]
}

let c = Counter([/]: "clicks")
print(c.label)
print(c.value)
```

# --answers--

- 0
- label
- 1
- value
- Counter
- name

# --solutions--

```swift
struct Counter {
    var label: String
    var value = 0
}

let c = Counter(label: "clicks")
print(c.label)
print(c.value)
```

# --output--

clicks
0
