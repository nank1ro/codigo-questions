---
language: swift
exerciseType: 1
---

# --description--

`filter` behält nur die Elemente, für die die Closure `true` zurückgibt. Die Closure erhält ein Element und muss einen `Bool` zurückgeben:
```swift
let nums = [5, 12, 8, 20]
let big = nums.filter { $0 > 10 }
print(big) // [12, 20]
```
Die Elemente behalten ihre ursprüngliche Reihenfolge, und das Ergebnis ist ein neues Array desselben Elementtyps.

# --instructions--

Schreibe eine Funktion `evens`, die ein Array von `Int` entgegennimmt (lasse das Argumentlabel weg) und mithilfe von `filter` ein neues Array nur mit den geraden Zahlen zurückgibt

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Du musst `filter` verwenden

```json
{
  "regex": "\\.filter",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`evens([1, 2, 3, 4, 5, 6])` muss `[2, 4, 6]` zurückgeben

```swift
tryCatch(evens([1, 2, 3, 4, 5, 6]) == [2, 4, 6])
```

`evens([7, 9])` muss `[]` zurückgeben

```swift
tryCatch(evens([7, 9]) == [])
```

`evens([0, -2, 3])` muss `[0, -2]` zurückgeben

```swift
tryCatch(evens([0, -2, 3]) == [0, -2])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func evens(_ nums: [Int]) -> [Int] {
    return nums.filter { $0 % 2 == 0 }
}
```
