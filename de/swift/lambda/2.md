---
language: swift
exerciseType: 1
---

# --description--

Jeden Typ innerhalb der Closure auszuschreiben ist oft unnötig. Wenn die Konstante einen expliziten **Funktionstyp** hat, leitet Swift die Parameter- und Rückgabetypen ab, sodass du nur die Parameternamen vor `in` angeben musst:
```swift
let triple: (Int) -> Int = { n in
    return n * 3
}
```
Der Typ `(Int) -> Int` liest sich als „eine Funktion, die einen `Int` entgegennimmt und einen `Int` zurückgibt".
Wenn der Rumpf ein einzelner Ausdruck ist, kann das Schlüsselwort `return` ebenfalls weggelassen werden, das nennt man eine **implizite Rückgabe**:
```swift
let triple: (Int) -> Int = { n in n * 3 }
print(triple(4)) // 12
```

# --instructions--

Speichere in der Konstante `square` eine Closure vom Typ `(Int) -> Int`, die ihr Argument mit sich selbst multipliziert zurückgibt

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

Du musst einen Closure-Ausdruck verwenden, keine `func`

```json
{
  "regex": "func\\s+square",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`square(4)` muss `16` zurückgeben

```swift
tryCatch(square(4) == 16)
```

`square(-3)` muss `9` zurückgeben

```swift
tryCatch(square(-3) == 9)
```

`square(0)` muss `0` zurückgeben

```swift
tryCatch(square(0) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let square: (Int) -> Int = { n in n * n }
```
