---
language: swift
exerciseType: 1
---

# --description--

Eine Closure zurückzugeben ist eine praktische Möglichkeit, angepasste Funktionen zu erstellen. Die Parameter der äußeren Funktion werden von der Closure erfasst, die sie zurückgibt:
```swift
func makeAdder(_ amount: Int) -> (Int) -> Int {
    return { $0 + amount }
}
let addFive = makeAdder(5)
print(addFive(10)) // 15
```
Der Rückgabetyp `(Int) -> Int` beschreibt die Closure, und die Kurzform `$0` bezieht sich auf das Argument dieser Closure, nicht auf das von `makeAdder`.

# --instructions--

Schreibe eine Funktion `makeMultiplier`, die einen `Int` namens `factor` entgegennimmt (lasse das Argumentlabel weg) und eine Closure vom Typ `(Int) -> Int` zurückgibt, die ihr Argument mit `factor` multipliziert

Beispiel eines Funktionsaufrufs:
```swift
let double = makeMultiplier(2)
print(double(10))
// prints 20
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`makeMultiplier(2)(10)` muss `20` zurückgeben

```swift
tryCatch(makeMultiplier(2)(10) == 20)
```

`makeMultiplier(3)(4)` muss `12` zurückgeben

```swift
tryCatch(makeMultiplier(3)(4) == 12)
```

`makeMultiplier(0)(99)` muss `0` zurückgeben

```swift
tryCatch(makeMultiplier(0)(99) == 0)
```

Eine von `makeMultiplier(5)` zurückgegebene Closure muss über mehrere Aufrufe hinweg weiterfunktionieren

```swift
let _timesFive = makeMultiplier(5)
tryCatch(_timesFive(1) == 5 && _timesFive(6) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func makeMultiplier(_ factor: Int) -> (Int) -> Int {
    return { $0 * factor }
}
```
