---
language: swift
exerciseType: 2
---

# --description--

Da Closures Werte sind, kann eine Funktion eine als Parameter entgegennehmen. Der Parametertyp ist einfach der Funktionstyp:
```swift
func apply(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(n)
}
print(apply(5, { $0 + 1 })) // 6
```
Die Funktion `apply` weiß nicht, was `operation` tut, sie weiß nur, dass sie einen `Int` entgegennimmt und einen `Int` zurückgibt, und ruft sie wie jede andere Funktion auf.

# --instructions--

Vervollständige die Funktion `apply` so, dass ihr zweiter Parameter eine Closure ist, die einen `Int` entgegennimmt und einen `Int` zurückgibt, und die Funktion das Ergebnis des Aufrufs dieser Closure mit `n` zurückgibt, sodass das Programm `12` ausgibt

# --seed--

```swift
func apply(_ n: Int, _ operation: [/]) -> Int {
    return [/]
}
print(apply(6, { $0 * 2 }))
```

# --answers--

- (Int) -> Int
- operation(n)
- Int
- (Int) -> Void
- operation
- n

# --solutions--

```swift
func apply(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(n)
}
print(apply(6, { $0 * 2 }))
```

# --output--

12
