---
language: swift
exerciseType: 2
---

# --description--

Eine Closure kann Variablen verwenden, die außerhalb ihres Rumpfes deklariert wurden. Sie **erfasst** sie: Die Variable bleibt so lange bestehen, wie die Closure existiert, selbst nachdem die Funktion, die sie deklariert hat, zurückgekehrt ist.
Dadurch kann eine Funktion eine Closure mit ihrem eigenen privaten Zustand erstellen:
```swift
func makeCounter() -> () -> Int {
    var count = 0
    return {
        count += 1
        return count
    }
}
```
`() -> Int` ist der Typ einer Closure ohne Parameter, die einen `Int` zurückgibt. Jeder Aufruf der zurückgegebenen Closure erhöht dasselbe erfasste `count`:
```swift
let counter = makeCounter()
print(counter()) // 1
print(counter()) // 2
```

# --instructions--

Vervollständige `makeCounter`, sodass sie eine Closure ohne Parameter zurückgibt, die das erfasste `count` erhöht und zurückgibt, sodass das Programm `1`, `2` und `3` auf drei Zeilen ausgibt

# --seed--

```swift
func makeCounter() -> [/] {
    [/] count = 0
    return {
        count += 1
        return count
    }
}
let counter = makeCounter()
print(counter())
print(counter())
print(counter())
```

# --answers--

- () -> Int
- var
- let
- Int
- () -> Void

# --solutions--

```swift
func makeCounter() -> () -> Int {
    var count = 0
    return {
        count += 1
        return count
    }
}
let counter = makeCounter()
print(counter())
print(counter())
print(counter())
```

# --output--

1
2
3
