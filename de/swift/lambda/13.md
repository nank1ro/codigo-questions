---
language: swift
exerciseType: 4
---

# --description--

Eine in einer Konstante gespeicherte Closure kann überall dort übergeben werden, wo ein Closure-Argument erwartet wird, unter Verwendung des Argumentlabels des Parameters:
```swift
let ascending = { (a: Int, b: Int) -> Bool in a < b }
print([3, 1, 2].sorted(by: ascending)) // [1, 2, 3]
```

# --instructions--

Ordne die Elemente an, um in `byLength` eine Closure zu speichern, die zwei Strings von der kürzesten zur längsten ordnet, und gib dann `words` damit sortiert aus.
Deklariere `words` vor `byLength`

# --answers--

```swift
let words = ["kiwi", "fig", "banana"]
```

```swift
let byLength = { (a: String, b: String) -> Bool in
```

```swift
    a.count < b.count
```

```swift
}
```

```swift
print(words.sorted(by: byLength))
```

# --solutions--

```swift
let words = ["kiwi", "fig", "banana"]
let byLength = { (a: String, b: String) -> Bool in
    a.count < b.count
}
print(words.sorted(by: byLength))
```

# --output--

["fig", "kiwi", "banana"]
