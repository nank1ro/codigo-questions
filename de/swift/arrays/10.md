---
language: swift
exerciseType: 2
---

# --description--

In Swift können wir auf sehr einfache Weise mit den Schlüsselwörtern `for..in` durch ein Array iterieren:
```swift
var numbers = [1, 2, 3]
for num in numbers {
    print(num)
}
// gibt 1, 2, 3 aus
```
Ein Variablenname folgt dem Schlüsselwort `for`, ihm wird der Wert jedes Array-Elements nacheinander zugewiesen.

# --instructions--

Geben Sie jedes Element im Array aus, indem Sie es mit `2` multiplizieren

# --seed--

```swift
var numbers = [2, 4, 6]
[/][/][/][/]{
    print([/][/][/])
}
```

# --answers--

- for 
- num 
- in 
- numbers 
- num 
- * 
- ** 
- 2

# --solutions--

```swift
var numbers = [2, 4, 6]
for num in numbers {
    print(num * 2)
}
```

# --output--

4
8
12
