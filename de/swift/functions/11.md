---
language: swift
exerciseType: 3
---

# --instructions--

Was ist das __Problem__ mit diesem Code-Snippet?
```swift
print(greet(name: "Mathew"))
/// Diese Funktion begrüßt die als Argument übergebene Person
func greet(name: String) -> String {
    return "Hello, \(name). Good morning!"
}
print(greet(name: "Alex"))
```

# --answers--

- Wir können greet() nicht verwenden, bevor wir es definieren
- Wir können greet() nicht zweimal verwenden
- greet() gibt nichts zurück
- Wir können name nicht als Parameter verwenden

# --solutions--

- Wir können greet() nicht verwenden, bevor wir es definieren
