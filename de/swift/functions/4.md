---
language: swift
exerciseType: 2
---

# --description--

Funktionen können mehrere Eingabeparameter haben, die in den Klammern der Funktion stehen und durch Kommas getrennt sind.
```swift
func sayHello(name: String, newUser: Bool) -> String {
  var greet: String = "Hello \(name)!"
  if (newUser) {
    greet += " Welcome on board :)"
  }
  return greet
}
// gibt "Hello Smith! Welcome on board :)" aus
print(sayHello(name: "Smith", newUser: true))
```

# --instructions--

Vervollständigen Sie den Code, um eine gültige Funktion namens `sumAllNumbers` zu erstellen, die das folgende Array von Zahlen `[15, 24, 31, 79]` und `true` übergibt, um das Ergebnis zu drucken

# --seed--

```swift
func sumAllNumbers(_ nums: [Int], _ printResult: Bool) -> Int {
    /* Die reduce-Methode wird verwendet, um alle Zahlen zu addieren, beginnend bei 0 und unter Verwendung des +-Zeichens
    */
    let result = nums.reduce(0, +)
    if [/] { print(result) }
    return result
}
let result = [/]([/]15, 24, 31, [/][/], [/])
```

# --answers--

- sumAllNumbers
- sum
- 79
- ]
- [
- true
- false
- True
- printResult
- print
- true

# --solutions--

```swift
func sumAllNumbers(_ nums: [Int], _ printResult: Bool) -> Int {
    /* Die reduce-Methode wird verwendet, um alle Zahlen zu addieren, beginnend bei 0 und unter Verwendung des +-Zeichens
    */
    let result = nums.reduce(0, +)
    if printResult { print(result) }
    return result
}
let result = sumAllNumbers([15, 24, 31, 79], true)
```

# --output--

149
