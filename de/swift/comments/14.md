---
language: swift
exerciseType: 1
---

# --description--

Ein `TODO` steht meist neben einem Platzhalter, der den Code kompilierbar hält, bis die eigentliche Implementierung geschrieben ist. Wenn du die Arbeit erledigst, ersetze den Platzhalter und entferne den Marker in derselben Änderung, damit der Kommentar nie etwas Falsches über den Zustand des Codes behauptet.

# --instructions--

Erledige die im `TODO`-Kommentar von `isEven` beschriebene Arbeit und entferne den Kommentar anschließend, da die Arbeit getan ist

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func isEven(_ n: Int) -> Bool {
    // TODO: return true when n is even, false otherwise
    return false
}
```

# --asserts--

The `TODO` comment must be removed

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`isEven(4)` must return `true`

```swift
tryCatch(isEven(4) == true)
```

`isEven(7)` must return `false`

```swift
tryCatch(isEven(7) == false)
```

`isEven(0)` must return `true`

```swift
tryCatch(isEven(0) == true)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func isEven(_ n: Int) -> Bool {
    return n % 2 == 0
}
```
