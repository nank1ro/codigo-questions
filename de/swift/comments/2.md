---
language: swift
exerciseType: 1
---

# --description--

Ein Kommentar braucht keine eigene Zeile: Er kann auf derselben Zeile hinter dem Code folgen. Das ist ein **nachstehender Kommentar**, und er ist ein guter Platz für eine kurze Notiz zu genau dieser Anweisung:
```swift
let retries = 3 // give up after three attempts
```
Alles von `//` bis zum Ende der Zeile wird ignoriert, während der Code davor wie üblich ausgeführt wird.

# --instructions--

Füge hinter `let seats = 4` einen nachstehenden Kommentar hinzu, der den Wert erklärt, zum Beispiel `// seats per table`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
let seats = 4
```

# --asserts--

Die Zeile `let seats = 4` muss mit einem `//`-Kommentar enden

```json
{
  "regex": "let seats = 4[ \\t]*//[ \\t]*\\S",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`seats` muss weiterhin gleich `4` sein

```swift
tryCatch(seats == 4)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let seats = 4 // seats per table
```
