---
language: swift
exerciseType: 4
---

# --description--

Nach der Zusammenfassung kann ein Dokumentationskommentar die Parameter und den Rückgabewert mit besonderen Markdown-Listeneinträgen beschreiben, die Xcode erkennt:
```swift
/// Returns the number of seconds in the given minutes.
/// - Parameter minutes: a whole number of minutes, never negative
/// - Returns: `minutes` multiplied by sixty
func toSeconds(_ minutes: Int) -> Int {
    return minutes * 60
}
```
Die Reihenfolge ist immer dieselbe: zuerst die Zusammenfassung, dann `- Parameter name:` für jeden Parameter, dann `- Returns:`.

# --instructions--

Sortiere die Elemente, um `twice` mit einer Zusammenfassung, einer `Parameter`-Zeile und einer `Returns`-Zeile zu dokumentieren, in dieser Reihenfolge und direkt über der Funktion, und gib dann `twice(21)` aus

# --answers--

```swift
/// Returns the double of a number.
```

```swift
/// - Parameter n: the number to double
```

```swift
/// - Returns: `n` multiplied by two
```

```swift
func twice(_ n: Int) -> Int {
    return n * 2
}
```

```swift
print(twice(21))
```

# --solutions--

```swift
/// Returns the double of a number.
/// - Parameter n: the number to double
/// - Returns: `n` multiplied by two
func twice(_ n: Int) -> Int {
    return n * 2
}
print(twice(21))
```

# --output--

42
