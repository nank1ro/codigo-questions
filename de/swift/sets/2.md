---
language: swift
exerciseType: 1
---

# --description--

Wie Arrays können Sets Konstanten (`let`) oder Variablen (`var`) sein. Nur ein `var`-Set kann nach seiner Erstellung geändert werden.
Um ein leeres Set zu erstellen, rufst du den Initialisierer des Typs auf, denn ein leeres Literal `[]` allein würde Swift nicht mitteilen, welchen Elementtyp du verwenden möchtest:
```swift
var visited = Set<String>()
print(visited.isEmpty) // true
```
Die Eigenschaft `isEmpty` ist `true`, wenn das Set keine Elemente enthält, genau wie bei Arrays.

# --instructions--

Deklariere ein konstantes Set von Zeichenketten namens `colors`, das `"red"`, `"green"` und `"blue"` enthält, sowie ein leeres variables Set von Ganzzahlen namens `scores`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`colors` muss genau `"red"`, `"green"` und `"blue"` enthalten

```swift
tryCatch(colors == ["red", "green", "blue"])
```

`colors` muss ein `Set<String>` sein

```swift
tryCatch(type(of: colors) == Set<String>.self)
```

`scores` muss leer sein

```swift
tryCatch(scores.isEmpty)
```

`scores` muss ein `Set<Int>` sein

```swift
tryCatch(type(of: scores) == Set<Int>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let colors: Set<String> = ["red", "green", "blue"]
var scores = Set<Int>()
```
