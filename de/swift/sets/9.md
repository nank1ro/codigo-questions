---
language: swift
exerciseType: 1
---

# --description--

Zwei weitere Operationen vervollständigen die Familie:
- `a.subtracting(b)` enthält die Elemente von `a`, die **nicht** in `b` sind
- `a.symmetricDifference(b)` enthält die Elemente, die in `a` oder in `b` sind, aber **nicht in beiden**
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.subtracting(b).sorted())          // [1, 2]
print(a.symmetricDifference(b).sorted())  // [1, 2, 4]
```
Im Gegensatz zu `union` und `intersection` ist `subtracting` nicht symmetrisch: `a.subtracting(b)` und `b.subtracting(a)` sind normalerweise unterschiedlich.

# --instructions--

Schreibe zwei Funktionen, die jeweils zwei `Set<String>` entgegennehmen (lasse die Argumentlabels weg):
- `common` gibt das Set der Werte zurück, die in beiden Sets vorhanden sind
- `onlyInFirst` gibt das Set der Werte zurück, die im ersten Set, aber nicht im zweiten vorhanden sind

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`common(["a", "b", "c"], ["b", "c", "d"])` muss `["b", "c"]` zurückgeben

```swift
tryCatch(common(["a", "b", "c"], ["b", "c", "d"]) == ["b", "c"])
```

`common(["a"], ["b"])` muss ein leeres Set zurückgeben

```swift
tryCatch(common(["a"], ["b"]).isEmpty)
```

`onlyInFirst(["a", "b", "c"], ["b", "c", "d"])` muss `["a"]` zurückgeben

```swift
tryCatch(onlyInFirst(["a", "b", "c"], ["b", "c", "d"]) == ["a"])
```

`onlyInFirst(["x", "y"], ["z"])` muss `["x", "y"]` zurückgeben

```swift
tryCatch(onlyInFirst(["x", "y"], ["z"]) == ["x", "y"])
```

Beide Funktionen müssen ein `Set<String>` zurückgeben

```swift
tryCatch(type(of: common(["a"], ["a"])) == Set<String>.self && type(of: onlyInFirst(["a"], ["a"])) == Set<String>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func common(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.intersection(second)
}

func onlyInFirst(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.subtracting(second)
}
```
