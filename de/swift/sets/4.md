---
language: swift
exerciseType: 2
---

# --description--

Um zu prüfen, ob ein Wert in einem Set enthalten ist, verwendest du die Methode `contains(_:)`, die einen `Bool` zurückgibt:
```swift
let primes: Set<Int> = [2, 3, 5, 7]
print(primes.contains(5)) // true
print(primes.contains(6)) // false
```
Diese Prüfung ist bei einem Set sehr schnell, selbst bei Tausenden von Elementen, was einer der Hauptgründe ist, für Zugehörigkeitsprüfungen ein Set einem Array vorzuziehen.

# --instructions--

Gib `Welcome` aus, wenn `role` eine der erlaubten Rollen ist, andernfalls gib `Access denied` aus

# --seed--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed[/]([/]) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --answers--

- .contains
- .has
- .includes
- role
- "role"
- allowed

# --solutions--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed.contains(role) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --output--

Welcome
