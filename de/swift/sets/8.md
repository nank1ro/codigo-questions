---
language: swift
exerciseType: 4
---

# --description--

Sets unterstützen die klassischen Operationen der Mengenlehre. Jede gibt ein **neues** Set zurück und lässt die Originale unverändert:
- `a.union(b)` enthält jedes Element, das in `a`, in `b` oder in beiden ist
- `a.intersection(b)` enthält nur die Elemente, die sich in **beiden**, `a` und `b`, befinden
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.union(b).sorted())        // [1, 2, 3, 4]
print(a.intersection(b).sorted()) // [3]
```

# --instructions--

Sortiere die Elemente, um jede Zahl auszugeben, die in `odds` oder in `primes` vorkommt, in aufsteigender Reihenfolge.
Deklariere `odds` vor `primes`

# --answers--

```swift
let odds: Set<Int> = [1, 3, 5]
```

```swift
let primes: Set<Int> = [2, 3, 5]
```

```swift
let all = odds.union(primes)
```

```swift
print(all.sorted())
```

# --solutions--

```swift
let odds: Set<Int> = [1, 3, 5]
let primes: Set<Int> = [2, 3, 5]
let all = odds.union(primes)
print(all.sorted())
```

# --output--

[1, 2, 3, 5]
