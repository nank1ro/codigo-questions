---
language: swift
exerciseType: 2
---

# --description--

Auch ein **Enum** kann konform implementieren. Es hat keine gespeicherten Eigenschaften, daher erfüllt man eine Eigenschaftsanforderung üblicherweise mit einer berechneten Eigenschaft, die über die Fälle schaltet:
```swift
protocol Priced {
    var price: Int { get }
}

enum Ticket: Priced {
    case child, adult

    var price: Int {
        switch self {
        case .child: return 5
        case .adult: return 12
        }
    }
}

print(Ticket.adult.price) // 12
```
Structs, Klassen und Enums: Alle drei nehmen Protokolle auf dieselbe Weise an, und Code, der gegen das Protokoll geschrieben ist, funktioniert mit allen dreien.

# --instructions--

Vervollständige den Code so, dass `Priced` ein lesbares `price` verlangt und `Drink` ihn konform implementiert

# --seed--

```swift
protocol Priced {
    var price: Int { [/] }
}

enum Drink: [/] {
    case tea, coffee

    var price: Int {
        [/] self {
        case .tea: return 3
        case .coffee: return 5
        }
    }
}

print(Drink.coffee.price)
```

# --answers--

- get
- Priced
- switch
- set
- if
- return

# --solutions--

```swift
protocol Priced {
    var price: Int { get }
}

enum Drink: Priced {
    case tea, coffee

    var price: Int {
        switch self {
        case .tea: return 3
        case .coffee: return 5
        }
    }
}

print(Drink.coffee.price)
```

# --output--

5
