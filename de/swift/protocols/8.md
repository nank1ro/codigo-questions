---
language: swift
exerciseType: 2
---

# --description--

Die Konformität muss nicht direkt beim Typ deklariert werden. Eine **Extension** kann sie später hinzufügen, sodass die Deklaration des Typs selbst auf seine Daten fokussiert bleibt:
```swift
protocol Resettable {
    mutating func reset()
}

struct Timer {
    var seconds = 0
}

extension Timer: Resettable {
    mutating func reset() {
        seconds = 0
    }
}
```
Das funktioniert auch bei Typen, die du nicht geschrieben hast: Du kannst einen Standardbibliothekstyp eines deiner Protokolle konform implementieren lassen, ohne seinen Quellcode anzufassen.

# --instructions--

Vervollständige den Code so, dass `increment()` eine mutating-Anforderung ist und `Clicks` `Counter` in einer Extension annimmt

# --seed--

```swift
protocol Counter {
    [/] func increment()
}

struct Clicks {
    var count = 0
}

[/] Clicks: Counter {
    mutating func increment() {
        count [/] 1
    }
}

var c = Clicks()
c.increment()
c.increment()
print(c.count)
```

# --answers--

- mutating
- extension
- +=
- static
- struct
- -=

# --solutions--

```swift
protocol Counter {
    mutating func increment()
}

struct Clicks {
    var count = 0
}

extension Clicks: Counter {
    mutating func increment() {
        count += 1
    }
}

var c = Clicks()
c.increment()
c.increment()
print(c.count)
```

# --output--

2
