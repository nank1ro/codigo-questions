---
language: swift
exerciseType: 1
---

# --description--

Ein Protokollname allein ist kein Typ, sondern eine Einschränkung, daher verlangt Swift von dir zu sagen, welches von zwei Dingen du meinst.

`some Shape` bedeutet *einen bestimmten konformen Typ*, festgelegt zur Kompilierzeit. Der Aufrufer erfährt nie, welcher es ist, aber es ist immer derselbe:
```swift
func unitSquare() -> some Shape {
    return Square(side: 1)
}
```
`any Shape` bedeutet *eine Box, die jeden konformen Typ aufnehmen kann*, und zwei Werte dieses Typs dürfen verschiedene Typen enthalten. Du brauchst ihn, wann immer der konkrete Typ variieren kann, etwa in einem gemischten Array:
```swift
let shapes: [any Shape] = [Square(side: 2), Rect(width: 2, height: 3)]
```
Beide lassen dich die Anforderungen des Protokolls aufrufen. Bevorzuge `some`, wenn ein einziger Typ genügt, weil er zur Laufzeit nichts kostet; greife zu `any`, wenn du wirklich Typen mischen musst.

# --instructions--

Schreibe eine Funktion `totalArea`, die ein Array von `any Shape` ohne Argument-Label nimmt und die Summe ihrer Flächen zurückgibt

Beispiel eines Funktionsaufrufs:
```swift
print(totalArea([Square(side: 2), Rect(width: 2, height: 3)]))
// prints 10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Shape {
    func area() -> Int
}

struct Square: Shape {
    var side: Int

    func area() -> Int {
        return side * side
    }
}

struct Rect: Shape {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}

func unitSquare() -> some Shape {
    return Square(side: 1)
}

func totalArea() {

}
```

# --asserts--

Ein Quadrat mit Seite `2` und ein Rechteck von `2` mal `3` müssen zusammen `10` ergeben

```swift
tryCatch(totalArea([Square(side: 2), Rect(width: 2, height: 3)]) == 10)
```

Ein leeres Array muss zusammen `0` ergeben

```swift
tryCatch(totalArea([]) == 0)
```

Ein einzelnes Rechteck von `5` mal `4` muss zusammen `20` ergeben

```swift
tryCatch(totalArea([Rect(width: 5, height: 4)]) == 20)
```

Das Einheitsquadrat allein muss zusammen `1` ergeben

```swift
tryCatch(totalArea([unitSquare()]) == 1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Shape {
    func area() -> Int
}

struct Square: Shape {
    var side: Int

    func area() -> Int {
        return side * side
    }
}

struct Rect: Shape {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}

func unitSquare() -> some Shape {
    return Square(side: 1)
}

func totalArea(_ shapes: [any Shape]) -> Int {
    var total = 0
    for shape in shapes {
        total += shape.area()
    }
    return total
}
```
