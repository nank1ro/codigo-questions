---
language: swift
exerciseType: 1
---

# --description--

Die Standardbibliothek ist aus Protokollen aufgebaut, und deine eigenen Typen können sie annehmen.

`Equatable` gibt einem Typ den `==`-Operator. Bei einem Struct, dessen gespeicherte Eigenschaften alle `Equatable` sind, genügt es, die Konformität zu deklarieren — Swift schreibt `==` für dich:
```swift
struct Point: Equatable {
    var x: Int
    var y: Int
}

print(Point(x: 1, y: 2) == Point(x: 1, y: 2)) // true
```
`Comparable` erbt von `Equatable` und fügt eine Ordnung hinzu. Du implementierst einen einzigen Operator, `<`, geschrieben als `static func`, die die beiden Werte nimmt, und bekommst `>`, `<=`, `>=` sowie `sorted()`, `min()` und `max()` auf Sammlungen gratis dazu:
```swift
struct Version: Comparable {
    var number: Int

    static func < (lhs: Version, rhs: Version) -> Bool {
        return lhs.number < rhs.number
    }
}
```

# --instructions--

Sorge dafür, dass `Weight` `Comparable` konform implementiert, geordnet nach `grams`

Beispiel einer Verwendung:
```swift
print(Weight(grams: 100) < Weight(grams: 250))
// prints true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Weight {
    var grams: Int
}
```

# --asserts--

`100` Gramm müssen leichter sein als `250` Gramm

```swift
tryCatch(Weight(grams: 100) < Weight(grams: 250))
```

`500` Gramm müssen schwerer sein als `250` Gramm

```swift
tryCatch(Weight(grams: 500) > Weight(grams: 250))
```

Zwei Gewichte von `80` Gramm müssen gleich sein

```swift
tryCatch(Weight(grams: 80) == Weight(grams: 80))
```

Das Schwerste von `3` und `1` Gramm muss `3` Gramm sein

```swift
tryCatch([Weight(grams: 3), Weight(grams: 1)].max() == Weight(grams: 3))
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Weight: Comparable {
    var grams: Int

    static func < (lhs: Weight, rhs: Weight) -> Bool {
        return lhs.grams < rhs.grams
    }
}
```
