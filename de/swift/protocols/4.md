---
language: swift
exerciseType: 1
---

# --description--

Protokolle beschränken sich nicht auf Structs. Eine **Klasse** implementiert auf genau dieselbe Weise konform, indem sie das Protokoll nach einem Doppelpunkt auflistet:
```swift
protocol Openable {
    func open() -> String
}

class Door: Openable {
    func open() -> String {
        return "creak"
    }
}
```
Erbt die Klasse außerdem von einer anderen Klasse, steht die Oberklasse zuerst in der Liste und die Protokolle folgen darauf. Ein Typ kann mehrere Protokolle gleichzeitig annehmen, getrennt durch Kommas.

# --instructions--

Sorge dafür, dass die Klasse `Box` `Sized` konform implementiert und die Anzahl der Elemente zurückgibt, die sie enthält

Beispiel einer Verwendung:
```swift
print(Box(items: ["pen", "key"]).size())
// prints 2
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Sized {
    func size() -> Int
}

class Box {
    var items: [String]

    init(items: [String]) {
        self.items = items
    }
}
```

# --before-asserts--

```swift
func _asSized(_ s: Sized) -> Int {
    return s.size()
}
```

# --asserts--

Eine Box mit `pen` und `key` muss die Größe `2` haben

```swift
tryCatch(_asSized(Box(items: ["pen", "key"])) == 2)
```

Eine leere Box muss die Größe `0` haben

```swift
tryCatch(_asSized(Box(items: [])) == 0)
```

Eine Box mit drei Elementen muss die Größe `3` haben

```swift
tryCatch(_asSized(Box(items: ["a", "b", "c"])) == 3)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Sized {
    func size() -> Int
}

class Box: Sized {
    var items: [String]

    init(items: [String]) {
        self.items = items
    }

    func size() -> Int {
        return items.count
    }
}
```
