---
language: swift
exerciseType: 4
---

# --description--

Eine Protokoll-Extension kann auch Member hinzufügen, die das Protokoll nie als Anforderungen gelistet hat. Sie sind zusätzliche Annehmlichkeiten, die jeder konforme Typ erhält:
```swift
protocol Sized {
    var count: Int { get }
}

extension Sized {
    var isEmpty: Bool {
        return count == 0
    }
}
```
`isEmpty` ist keine Anforderung, also muss ein konformer Typ sie nicht bereitstellen — er bekommt sie einfach.

# --instructions--

Ordne die Elemente so an, dass `HEY` ausgegeben wird, mit dem Protokoll zuerst, dann seiner Extension, dann dem Struct, dann dem `print`

# --answers--

```swift
protocol Loud {
    var text: String { get }
}
```

```swift
extension Loud {
    func shout() -> String { return text.uppercased() }
}
```

```swift
struct Message: Loud {
    var text: String
}
```

```swift
print(Message(text: "hey").shout())
```

# --solutions--

```swift
protocol Loud {
    var text: String { get }
}
extension Loud {
    func shout() -> String { return text.uppercased() }
}
struct Message: Loud {
    var text: String
}
print(Message(text: "hey").shout())
```

# --output--

HEY
