---
language: swift
exerciseType: 2
---

# --description--

In Swift ist ein Fehler ein **Wert**, kein Absturz. Jeder Typ kann einer sein, indem er das `Error`-Protokoll implementiert, und eine Enumeration ist die übliche Wahl, weil ihre Fälle genau benennen, was schiefgehen kann:
```swift
enum LoginError: Error {
    case wrongPassword
}
```
Eine Funktion, die fehlschlagen kann, wird mit `throws` markiert und meldet den Fehlschlag mit `throw`:
```swift
func login(_ password: String) throws {
    if password != "swift" {
        throw LoginError.wrongPassword
    }
    print("welcome")
}
```
Der Aufruf einer solchen Funktion braucht `try`, und der Aufruf muss in einem `do`-Block stehen, auf den ein `catch`-Block folgt, der sagt, was bei einem Fehlschlag zu tun ist:
```swift
do {
    try login("hunter2")
} catch {
    print("login failed")
}
// login failed
```
Wenn `throw` läuft, wird der Rest des `do`-Blocks übersprungen und `catch` übernimmt. Nichts stürzt ab: Das Programm läuft nach dem `catch` weiter.

# --instructions--

Vervollständige den Code so, dass `TempError` ein Fehlertyp ist, `check` wirft, wenn es zu kalt ist, und der Fehlschlag behandelt wird, wobei `too cold` ausgegeben wird

# --seed--

```swift
enum TempError: [/] {
    case tooCold
}

func check(_ degrees: Int) [/] {
    if degrees < 10 {
        [/] TempError.tooCold
    }
    print("fine")
}

do {
    [/] check(4)
} [/] {
    print("too cold")
}
```

# --answers--

- Error
- throws
- throw
- try
- catch
- Exception
- finally
- raise

# --solutions--

```swift
enum TempError: Error {
    case tooCold
}

func check(_ degrees: Int) throws {
    if degrees < 10 {
        throw TempError.tooCold
    }
    print("fine")
}

do {
    try check(4)
} catch {
    print("too cold")
}
```

# --output--

too cold
