---
language: swift
exerciseType: 1
---

# --description--

Ein Validator liest sich am besten, wenn die Zurückweisungen zuerst kommen und die eigentliche Arbeit unausgerückt unten steht. `guard` ist genau dafür gebaut: Er sieht die Bedingung vor, die gelten muss, und sein `else`-Block läuft, wenn sie nicht gilt.
```swift
func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
Der `else`-Block eines `guard` muss den aktuellen Gültigkeitsbereich verlassen, und `throw` ist eine der Möglichkeiten dafür, neben `return`, `break` und `continue`. Mehrere gestapelte `guard`s am Anfang einer Funktion lesen sich wie eine Liste der Regeln, die die Eingabe erfüllen muss.

# --instructions--

Vervollständige `priceFor` mit einem `guard`, der `OrderError.notPositive` wirft, solange `quantity` nicht größer als `0` ist

Beispiel für einen Funktionsaufruf:
```swift
do {
    print(try priceFor(4))
} catch {
    print("invalid")
}
// gibt 12 aus
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    
    return quantity * 3
}
```

# --before-asserts--

```swift
func _price(_ quantity: Int) -> Int {
    do {
        return try priceFor(quantity)
    } catch OrderError.notPositive {
        return -1
    } catch {
        return -2
    }
}
```

# --asserts--

`priceFor(4)` muss `12` zurückgeben

```swift
tryCatch(_price(4) == 12)
```

`priceFor(1)` muss `3` zurückgeben

```swift
tryCatch(_price(1) == 3)
```

`priceFor(0)` muss `notPositive` werfen

```swift
tryCatch(_price(0) == -1)
```

`priceFor(-5)` muss `notPositive` werfen

```swift
tryCatch(_price(-5) == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
