---
language: swift
exerciseType: 1
---

# --description--

Eine Funktion, die eine Closure entgegennimmt, hat ein Problem: Sie kann nicht wissen, ob die ihr übergebene Closure wirft. Die Funktion mit `throws` zu markieren würde jeden Aufrufer zwingen, `try` zu schreiben, sogar die, die eine harmlose Closure übergeben. `rethrows` sagt *ich werfe nur, wenn die Closure, die du mir gegeben hast, wirft*:
```swift
func applyTwice(_ value: Int, _ transform: (Int) throws -> Int) rethrows -> Int {
    return try transform(transform(value))
}
```
Im Körper schreibst du trotzdem `try`, denn der Aufruf kann wirklich fehlschlagen. An der Aufrufstelle schaut der Compiler auf die Closure, die du übergeben hast:
```swift
let doubled = applyTwice(3, { (n: Int) -> Int in n * 2 }) // kein try nötig
```
Die Standardbibliothek nutzt das überall — `map`, `filter` und `sorted(by:)` sind alle `rethrows` — weshalb du nie `try` vor ein gewöhnliches `map` schreibst.

# --instructions--

Vervollständige `applyAll` so, dass es eine werfende Closure entgegennimmt, nur wirft, wenn diese Closure wirft, und das Array der umgewandelten Werte zurückgibt

Beispiel für einen Funktionsaufruf:
```swift
print(applyAll([1, 2], { (n: Int) -> Int in n + 1 }))
// gibt [2, 3] aus
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func applyAll(_ numbers: [Int], _ transform: (Int) -> Int) -> [Int] {
    
}
```

# --before-asserts--

```swift
enum _TestError: Error {
    case boom
}

func _propagates() -> Bool {
    do {
        _ = try applyAll([1, 2], { (n: Int) -> Int in throw _TestError.boom })
        return false
    } catch {
        return true
    }
}
```

# --asserts--

das Verdoppeln von `1, 2, 3` muss `2, 4, 6` ergeben, ganz ohne `try`

```swift
let _doubled: [Int] = applyAll([1, 2, 3], { (n: Int) -> Int in n * 2 })
tryCatch(_doubled == [2, 4, 6])
```

ein leeres Array muss ein leeres Array ergeben

```swift
let _empty: [Int] = applyAll([], { (n: Int) -> Int in n * 2 })
tryCatch(_empty.isEmpty)
```

das Addieren von eins zu `5` muss `6` ergeben

```swift
let _plus: [Int] = applyAll([5], { (n: Int) -> Int in n + 1 })
tryCatch(_plus == [6])
```

eine werfende Closure muss `applyAll` werfen lassen

```swift
tryCatch(_propagates())
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func applyAll(_ numbers: [Int], _ transform: (Int) throws -> Int) rethrows -> [Int] {
    var result: [Int] = []
    for number in numbers {
        result.append(try transform(number))
    }
    return result
}
```
