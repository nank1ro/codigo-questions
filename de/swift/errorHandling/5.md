---
language: swift
exerciseType: 1
---

# --description--

Ein Fehlerfall kann Daten tragen. Gib dem Fall **assoziierte Werte** und das `throw` füllt sie aus, sodass der Handler nicht nur *was* fehlgeschlagen ist erfährt, sondern *um wie viel*:
```swift
enum ValidationError: Error {
    case tooShort(minimum: Int)
}

throw ValidationError.tooShort(minimum: 8)
```
Das passende `catch` bindet diese Werte mit `let`:
```swift
} catch ValidationError.tooShort(let minimum) {
    print("needs at least \(minimum) characters")
}
```
Den Namen nach `let` wählst du selbst; er ist eine neue Konstante, die nur innerhalb dieses `catch`-Blocks verfügbar ist. So trägt ein Fehler eine nützliche Nachricht, ohne dass du Zahlen an der Stelle, an der der Fehlschlag passiert, in Strings einfügen musst.

# --instructions--

Gib `tooShort` einen assoziierten Wert `minimum` vom Typ `Int` und lasse `check` `PasswordError.tooShort(minimum: 8)` werfen, wenn das Passwort weniger als 8 Zeichen hat

Beispiel für einen Funktionsaufruf:
```swift
do {
    try check("abc")
} catch PasswordError.tooShort(let minimum) {
    print(minimum)
}
// gibt 8 aus
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum PasswordError: Error {
    case tooShort
}

func check(_ password: String) throws {
    
}
```

# --before-asserts--

```swift
func _minimum(_ password: String) -> Int {
    do {
        try check(password)
        return 0
    } catch PasswordError.tooShort(let minimum) {
        return minimum
    } catch {
        return -1
    }
}
```

# --asserts--

`check("abc")` muss `tooShort` mit `8` werfen

```swift
tryCatch(_minimum("abc") == 8)
```

`check("")` muss `tooShort` mit `8` werfen

```swift
tryCatch(_minimum("") == 8)
```

`check("1234567")` muss werfen, weil 7 Zeichen nicht reichen

```swift
tryCatch(_minimum("1234567") == 8)
```

`check("12345678")` darf nicht werfen

```swift
tryCatch(_minimum("12345678") == 0)
```

`check("longenough")` darf nicht werfen

```swift
tryCatch(_minimum("longenough") == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum PasswordError: Error {
    case tooShort(minimum: Int)
}

func check(_ password: String) throws {
    if password.count < 8 {
        throw PasswordError.tooShort(minimum: 8)
    }
}
```
