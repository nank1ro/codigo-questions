---
language: swift
exerciseType: 2
---

# --description--

Manchmal interessiert dich nicht *warum* etwas fehlgeschlagen ist, sondern nur, dass es passiert ist. `try?` macht aus einem werfenden Aufruf ein **Optional**: den Wert bei Erfolg, `nil` beim Werfen.
```swift
enum ParseError: Error {
    case notANumber
}

func toInt(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ParseError.notANumber
    }
    return value
}

if let number = try? toInt("42") {
    print(number) // 42
}
```
Kein `do`, kein `catch`: Der Fehlschlag wird in das Optional hineingefaltet, dessen Entpacken du schon kennst. Der Preis dafür ist, dass der Fehlerwert weggeworfen wird, greife also nur zu `try?`, wenn es wirklich nichts zu melden gibt.

# --instructions--

Vervollständige den Code so, dass der erfolgreiche Aufruf entpackt wird und der fehlschlagende als `nil` erkannt wird, wobei `42` und danach `not a number` ausgegeben wird

# --seed--

```swift
enum ParseError: Error {
    case notANumber
}

func toInt(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ParseError.notANumber
    }
    return value
}

let good = [/] toInt("42")
if let number = [/] {
    print(number)
}
if (try? toInt("abc")) == [/] {
    print("not a number")
}
```

# --answers--

- try?
- good
- nil
- try
- bad
- 0

# --solutions--

```swift
enum ParseError: Error {
    case notANumber
}

func toInt(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ParseError.notANumber
    }
    return value
}

let good = try? toInt("42")
if let number = good {
    print(number)
}
if (try? toInt("abc")) == nil {
    print("not a number")
}
```

# --output--

42
not a number
