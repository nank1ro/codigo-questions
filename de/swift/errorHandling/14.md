---
language: swift
exerciseType: 2
---

# --description--

Ein `do`-Block ist nicht auf einen Fehlertyp beschränkt. Jeder Schritt kann auf seine eigene Weise fehlschlagen, und jeder Fehlschlag bekommt sein eigenes `catch`:
```swift
do {
    let text = try load(false)
    let value = try parse(text)
    print(value)
} catch NetworkError.offline {
    print("offline")
} catch ParseError.badFormat {
    print("bad format")
} catch {
    print("unknown")
}
```
Das erste `try`, das wirft, beendet den Block, daher laufen die späteren Schritte nie — der Wert existierte schlicht nie. Das macht diese Form lesbar: Der Happy Path bleibt oben in einer geraden Linie, und jede Art, wie er schiefgehen kann, ist darunter aufgelistet.

# --instructions--

Vervollständige den Code so, dass ein Netzwerkfehler und ein Parsing-Fehler getrennt behandelt werden, wobei `offline` ausgegeben wird

# --seed--

```swift
enum NetworkError: Error {
    case offline
}

enum ParseError: Error {
    case badFormat
}

func load(_ online: Bool) throws -> String {
    guard online else {
        throw NetworkError.offline
    }
    return "raw"
}

func parse(_ text: String) throws -> Int {
    throw ParseError.badFormat
}

do {
    let text = try load(false)
    let value = try parse(text)
    print(value)
} catch [/] {
    print("offline")
} catch [/] {
    print("bad format")
} [/] {
    print("unknown")
}
```

# --answers--

- NetworkError.offline
- ParseError.badFormat
- catch
- NetworkError.badFormat
- ParseError.offline
- finally

# --solutions--

```swift
enum NetworkError: Error {
    case offline
}

enum ParseError: Error {
    case badFormat
}

func load(_ online: Bool) throws -> String {
    guard online else {
        throw NetworkError.offline
    }
    return "raw"
}

func parse(_ text: String) throws -> Int {
    throw ParseError.badFormat
}

do {
    let text = try load(false)
    let value = try parse(text)
    print(value)
} catch NetworkError.offline {
    print("offline")
} catch ParseError.badFormat {
    print("bad format")
} catch {
    print("unknown")
}
```

# --output--

offline
