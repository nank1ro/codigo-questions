---
language: swift
exerciseType: 1
---

# --description--

`async let` funktioniert, wenn du beim Schreiben des Codes weißt, wie viele Aufrufe es gibt. Für eine Liste, deren Größe erst zur Laufzeit bekannt ist, verwendest du eine **Task-Gruppe**.

`withTaskGroup(of:)` öffnet eine Gruppe, `addTask` startet je Element einen Kind-Task, und die Gruppe wird danach mit `for await` gelesen, das die Ergebnisse liefert, sobald sie fertig sind:
```swift
let total = await withTaskGroup(of: Int.self) { group in
    for n in numbers {
        group.addTask {
            return await square(n)
        }
    }
    var sum = 0
    for await value in group {
        sum += value
    }
    return sum
}
```
`of: Int.self` deklariert, was jeder Kind-Task zurückgibt. Der gesamte `withTaskGroup`-Aufruf ist ein einziger Ausdruck, daher braucht er ein einzelnes `await` davor, und er kehrt erst zurück, wenn jeder Kind-Task fertig ist.

# --instructions--

Schreibe eine Funktion `sumOfSquares`, die ein Array aus Ganzzahlen ohne Argumentlabel entgegennimmt, asynchron ist und die Summe von `square` auf jedes Element angewendet zurückgibt, wobei du sie mit `withTaskGroup` berechnest

Beispiel eines Funktionsaufrufs:
```swift
print(await sumOfSquares([1, 2, 3]))
// prints 14
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func square(_ n: Int) async -> Int {
    return n * n
}

func sumOfSquares() {

}
```

# --asserts--

Die Funktion muss `withTaskGroup` verwenden

```json
{
  "regex": "withTaskGroup",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`sumOfSquares([1, 2, 3])` muss `14` zurückgeben

```swift
tryCatch(await sumOfSquares([1, 2, 3]) == 14)
```

`sumOfSquares([4])` muss `16` zurückgeben

```swift
tryCatch(await sumOfSquares([4]) == 16)
```

`sumOfSquares([])` muss `0` zurückgeben

```swift
tryCatch(await sumOfSquares([]) == 0)
```

`sumOfSquares([-2, 5])` muss `29` zurückgeben

```swift
tryCatch(await sumOfSquares([-2, 5]) == 29)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func square(_ n: Int) async -> Int {
    return n * n
}

func sumOfSquares(_ numbers: [Int]) async -> Int {
    return await withTaskGroup(of: Int.self) { group in
        for n in numbers {
            group.addTask {
                return await square(n)
            }
        }
        var total = 0
        for await value in group {
            total += value
        }
        return total
    }
}
```
