---
language: swift
exerciseType: 2
---

# --description--

Wenn es dich nicht interessiert, *warum* der Aufruf fehlgeschlagen ist, ist `try?` kürzer als ein `do`-Block. Es macht aus einem werfenden Aufruf ein **Optional**: den Wert bei Erfolg, `nil` bei Fehlschlag. Zusammen mit `await` schreibt man es als `try? await`:
```swift
let value = try? await parse("42")  // Optional(42)
let broken = try? await parse("x")  // nil
```
Weil das Ergebnis ein Optional ist, passt es direkt in ein `if let`:
```swift
if let value = try? await parse("x") {
    print(value)
} else {
    print("not a number")
}
```
Verwende `try? await` für einen schnellen Ersatz, und `do` / `catch`, wenn der Fehler selbst wichtig ist.

# --instructions--

Vervollständige den Code so, dass der fehlgeschlagene Aufruf `nil` ergibt und das Programm `not a number` ausgibt

# --seed--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = [/] await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --answers--

- try?
- try
- try!
- await
- throws

# --solutions--

```swift
enum InputError: Error {
    case notANumber
}

func parse(_ text: String) async throws -> Int {
    guard let value = Int(text) else {
        throw InputError.notANumber
    }
    return value
}

if let value = try? await parse("abc") {
    print(value)
} else {
    print("not a number")
}
```

# --output--

not a number
