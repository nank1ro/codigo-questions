---
language: swift
exerciseType: 1
---

# --description--

`Task.sleep` hält den aktuellen Task eine Weile an, ohne etwas anderes zu blockieren. Er kann unterbrochen werden, daher ist es ein werfender asynchroner Aufruf und braucht `try await`. Die Dauer wird mit Helfern wie `.seconds`, `.milliseconds` oder `.nanoseconds` angegeben:
```swift
func slowGreeting() async throws -> String {
    try await Task.sleep(for: .milliseconds(50))
    return "hello"
}
```
Das ist die übliche Art, in einem Beispiel langsame Arbeit zu simulieren, anstelle eines echten Netzwerkaufrufs. Beachte, dass das Programm dadurch nicht einfriert: Während ein Task schläft, laufen die anderen weiter.

# --instructions--

Schreibe eine Funktion `delayed`, die eine Ganzzahl ohne Argumentlabel entgegennimmt, asynchron ist, werfen kann, `50` Millisekunden mit `Task.sleep` wartet und dann die Zahl multipliziert mit `3` zurückgibt

Beispiel eines Funktionsaufrufs:
```swift
print(try await delayed(2))
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func delayed() {

}
```

# --asserts--

Die Funktion muss mit `async throws` markiert sein

```json
{
  "regex": "func\\s+delayed\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Die Funktion muss `Task.sleep` verwenden

```json
{
  "regex": "Task\\.sleep",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`delayed(2)` muss `6` zurückgeben

```swift
tryCatch((try? await delayed(2)) == 6)
```

`delayed(0)` muss `0` zurückgeben

```swift
tryCatch((try? await delayed(0)) == 0)
```

`delayed(-4)` muss `-12` zurückgeben

```swift
tryCatch((try? await delayed(-4)) == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func delayed(_ n: Int) async throws -> Int {
    try await Task.sleep(for: .milliseconds(50))
    return n * 3
}
```
