---
language: swift
exerciseType: 1
---

# --description--

Um zwei unabhängige Aufrufe gleichzeitig auszuführen, deklarierst du sie mit **`async let`**. Die Arbeit startet sofort, und das Programm läuft ohne Warten weiter:
```swift
async let left = step("A")
async let right = step("B")
```
Der Wert ist noch nicht da, daher kannst du die Bindung nicht direkt verwenden: Du musst sie mit `await` an der Stelle abwarten, an der du sie schließlich brauchst. Ein einziges `await` vor dem Ausdruck deckt jedes `async let` darin ab:
```swift
let both = await left + right
```
Braucht jeder Aufruf eine Sekunde, benötigt die sequenzielle Version zwei Sekunden, während die `async let`-Version etwa eine braucht, weil sich die beiden Aufrufe überlappen.

# --instructions--

Schreibe eine Funktion `total`, die zwei Ganzzahlen ohne Argumentlabel entgegennimmt, asynchron ist und die Summe von `slowValue` auf beide angewendet zurückgibt, wobei sie die beiden Aufrufe mit `async let` gleichzeitig startet

Beispiel eines Funktionsaufrufs:
```swift
print(await total(1, 2))
// prints 30
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total() {

}
```

# --asserts--

Die Funktion muss `async let` verwenden

```json
{
  "regex": "async\\s+let",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`total(1, 2)` muss `30` zurückgeben

```swift
tryCatch(await total(1, 2) == 30)
```

`total(0, 0)` muss `0` zurückgeben

```swift
tryCatch(await total(0, 0) == 0)
```

`total(5, -2)` muss `30` zurückgeben

```swift
tryCatch(await total(5, -2) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total(_ a: Int, _ b: Int) async -> Int {
    async let first = slowValue(a)
    async let second = slowValue(b)
    return await first + second
}
```
