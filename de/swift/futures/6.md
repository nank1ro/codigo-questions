---
language: swift
exerciseType: 4
---

# --description--

Mehrere `await`-Aufrufe, die nacheinander stehen, laufen **sequenziell**: Der zweite Aufruf startet gar nicht erst, bis der erste zurückgekehrt ist. Der Code liest sich von oben nach unten, genau wie gewöhnlicher Code:
```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}

let a = await step("A")
print(a)
let b = await step("B")
print(b)
// start A
// done A
// start B
// done B
```
Das ist genau das, was du willst, wenn der zweite Aufruf das Ergebnis des ersten braucht. Sind die Aufrufe unabhängig voneinander, ist das Warten auf den einen vor dem Start des anderen verschwendete Zeit, und die nächsten Übungen zeigen, wie du das vermeidest.

# --instructions--

Sortiere die Zeilen, um die folgende Ausgabe zu drucken, wobei du `step` vor dem Code auf der obersten Ebene definierst
```
start A
done A
start B
done B
```

# --answers--

```swift
func step(_ name: String) async -> String {
```

```swift
    print("start \(name)")
```

```swift
    return "done \(name)"
```

```swift
}
```

```swift
let a = await step("A")
```

```swift
print(a)
```

```swift
let b = await step("B")
```

```swift
print(b)
```

# --solutions--

```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}
let a = await step("A")
print(a)
let b = await step("B")
print(b)
```

# --output--

start A
done A
start B
done B
