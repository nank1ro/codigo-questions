---
language: swift
exerciseType: 1
---

# --description--

Eine asynchrone Funktion ist trotzdem eine gewöhnliche Funktion: Sie kann Parameter entgegennehmen und einen Wert eines beliebigen Typs zurückgeben. Nur zwei Dinge ändern sich, das Schlüsselwort `async` in der Signatur und das `await` an jeder Aufrufstelle:
```swift
func price(of quantity: Int) async -> Double {
    return Double(quantity) * 2.5
}

let total = await price(of: 4)
print(total)
// prints 10.0
```
Der zurückgegebene Wert ist ein normales `Double`, kein Wrapper: Sobald `await` abgeschlossen ist, arbeitest du damit genau wie gewohnt.

# --instructions--

Schreibe eine Funktion `double`, die eine Ganzzahl ohne Argumentlabel entgegennimmt, asynchron ist und das Doppelte dieser Zahl zurückgibt

Beispiel eines Funktionsaufrufs:
```swift
print(await double(4))
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func double() {

}
```

# --asserts--

Die Funktion muss mit `async` markiert sein

```json
{
  "regex": "func\\s+double\\s*\\([^)]*\\)\\s*async",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`double(4)` muss `8` zurückgeben

```swift
tryCatch(await double(4) == 8)
```

`double(0)` muss `0` zurückgeben

```swift
tryCatch(await double(0) == 0)
```

`double(-7)` muss `-14` zurückgeben

```swift
tryCatch(await double(-7) == -14)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func double(_ n: Int) async -> Int {
    return n * 2
}
```
