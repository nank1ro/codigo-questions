---
language: swift
exerciseType: 2
---

# --description--

Um Werte zu einem Wörterbuch hinzuzufügen oder zu ändern, können wir auch die Methode `updateValue(_:forKey:)` verwenden

# --instructions--

Aktualisiere den Schlüssel `age` mit dem Wert `20` im Wörterbuch

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let [/] = [/][/][/]([/], forKey: [/]) {
    print("The old age was \(oldAge)")
}
print(user)
```

# --answers--

- update
- add
- insert
- user
- .
- updateValue
- 20
- "age"
- oldAge

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let oldAge = user.updateValue(20, forKey: "age") {
    print("The old age was \(oldAge)")
}
print(user)
```

# --output--

Das alte Alter war 30
["age": 20, "first_name": "John", "last_name": "Hood"]
