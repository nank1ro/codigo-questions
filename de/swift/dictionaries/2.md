---
language: swift
exerciseType: 2
---

# --description--

Der Zugriff auf Wörterbuch-Werte nach Schlüssel ist genauso wie der Zugriff auf Array-Werte nach Index:
```swift
// gets the age value from the user dictionary
user['age']
```

# --instructions--

Gib den Wert des Schlüssels `last_name` aus

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let lastName = [/][/][/][/] {
    print("The last name is [/]")
}
```

# --answers--

- user
- [
- ]
- last_name
- "last_name"
- \(lastName)

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let lastName = user["last_name"] {
    print("The last name is \(lastName)")
}
```

# --output--

Der Nachname ist Hood
