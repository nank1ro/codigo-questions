---
language: swift
exerciseType: 4
---

# --instructions--

Kannst du diese Zeilen so ordnen, dass sie `"The removed value is John."` in der Ausgabe anzeigen?

# --answers--

```swift
var users = [String: Any]()
```

```swift
users["name"] = "John"
```

```swift
if let removedValue = users.removeValue(forKey: "name") {
```

```swift
    print("The removed value is \(removedValue).")
```

```swift
}
```

# --solutions--

```swift
var users = [String: Any]()
users["name"] = "John"
if let removedValue = users.removeValue(forKey: "name") {
    print("The removed value is \(removedValue).")
}
```

# --output--

Der entfernte Wert ist John.
