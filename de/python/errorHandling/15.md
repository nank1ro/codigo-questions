---
language: python
exerciseType: 4
---

# --description--

Manchmal soll ein Handler auf einen Fehler reagieren, ohne die Verantwortung dafür zu übernehmen: ihn mitprotokollieren, ihn mitzählen, etwas schließen — und dann den Aufrufer damit umgehen lassen. Ein `raise` allein innerhalb eines `except`-Blocks **löst die behandelte Ausnahme erneut aus**, mit ihrem ursprünglichen Typ, ihrer Meldung und ihrem Traceback unversehrt:
```python
try:
    value = int(text)
except ValueError:
    print("could not read the value")
    raise
```
Statt dessen `raise ValueError(...)` zu schreiben, würde eine neue Ausnahme bauen und den ursprünglichen Traceback wegwerfen — genau die Information, die du beim Debuggen brauchst.

# --instructions--

Ordne die Elemente zu einem Programm, das `load` definiert, das Ergebnis des Ladens von `"5"` ausgibt und es dann mit `"abc"` innerhalb eines `try`-Blocks aufruft, der den Fehler meldet, sodass zuerst eine Meldung von `load` und dann eine vom Aufrufer ausgegeben wird

# --answers--

```python
def load(text):
```

```python
    try:
        return int(text)
```

```python
    except ValueError:
        print(f"could not read {text}")
        raise
```

```python
print(load("5"))
```

```python
try:
    load("abc")
except ValueError:
    print("handled by the caller")
```

# --solutions--

```python
def load(text):
    try:
        return int(text)
    except ValueError:
        print(f"could not read {text}")
        raise
print(load("5"))
try:
    load("abc")
except ValueError:
    print("handled by the caller")
```

# --output--

5
could not read abc
handled by the caller
