---
language: python
exerciseType: 1
---

# --description--

Wenn mehrere Fehler dieselbe Wiederherstellung verdienen, ist es kürzer, sie als Tupel in einer Klausel aufzulisten, als den Block zu wiederholen:
```python
try:
    value = int(text) / divisor
except (ValueError, ZeroDivisionError):
    value = 0
```
Die Klammern sind erforderlich: `except ValueError, ZeroDivisionError:` ist in Python 3 ein Syntaxfehler. Ein Tupel ist trotzdem eine explizite Liste von Typen.

# --instructions--

Schreibe eine Funktion `safe_ratio(a, b)`, die `a / b` zurückgibt und `None` zurückgibt, wenn die Division einen `ZeroDivisionError` oder `TypeError` auslöst, indem sie eine einzige `except`-Klausel mit einem Tupel beider Typen verwendet

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_ratio(9, 3)` muss `3.0` zurückgeben

```python
    def test_1(self):
        self.assertEqual(safe_ratio(9, 3), 3.0, "--err-t1--")
```

`safe_ratio(1, 0)` muss `None` zurückgeben

```python
    def test_2(self):
        self.assertIsNone(safe_ratio(1, 0), "--err-t2--")
```

`safe_ratio(1, "x")` muss `None` zurückgeben

```python
    def test_3(self):
        self.assertIsNone(safe_ratio(1, "x"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_ratio(a, b):
    try:
        return a / b
    except (ZeroDivisionError, TypeError):
        return None
```
