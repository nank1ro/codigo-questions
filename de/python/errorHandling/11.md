---
language: python
exerciseType: 1
---

# --description--

Ein `finally`-Block läuft **immer, was auch passiert**: nach einem sauberen `try`-Block, nach einem `except`-Block, sogar während eine Ausnahme, die niemand abgefangen hat, nach außen reist, und sogar wenn der `try`- oder `except`-Block ein `return` ausführt:
```python
def read(text):
    try:
        return int(text)
    except ValueError:
        return 0
    finally:
        print("done")
```
Beide Wege geben `done` aus, bevor der Wert die Funktion verlässt. Diese Garantie ist der Zweck von `finally`: eine Datei schließen, eine Sperre freigeben, eine Einstellung zurücksetzen. Die vollständige Form ist `try` / `except` / `else` / `finally`; ein `try` braucht mindestens ein `except` oder ein `finally`, und `else` funktioniert nur zusammen mit einem `except`.

# --instructions--

Schreibe eine Funktion `record(text)`, die mit einer leeren Liste beginnt, `int(text)` an sie anhängt, stattdessen die Zeichenkette `"bad"` anhängt, wenn `int(text)` einen `ValueError` auslöst, immer `"done"` in einem `finally`-Block anhängt und die Liste zurückgibt

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`record("5")` muss `[5, "done"]` zurückgeben

```python
    def test_1(self):
        self.assertEqual(record("5"), [5, "done"], "--err-t1--")
```

`record("x")` muss `["bad", "done"]` zurückgeben

```python
    def test_2(self):
        self.assertEqual(record("x"), ["bad", "done"], "--err-t2--")
```

jeder Aufruf muss mit einer frischen Liste beginnen

```python
    def test_3(self):
        record("1")
        self.assertEqual(record("2"), [2, "done"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def record(text):
    log = []
    try:
        log.append(int(text))
    except ValueError:
        log.append("bad")
    finally:
        log.append("done")
    return log
```
