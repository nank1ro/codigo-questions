---
language: python
exerciseType: 1
---

# --description--

Eine Ausnahme ist ein Objekt, und `as` bindet es an einen Namen, damit der Handler sich es ansehen kann:
```python
try:
    int("abc")
except ValueError as e:
    print(e)                   # invalid literal for int() with base 10: 'abc'
    print(type(e).__name__)    # ValueError
```
`str(e)` — das verwenden auch `print(e)` und ein f-Slot — liefert die Meldung, mit der die Ausnahme erzeugt wurde, und `type(e).__name__` liefert den Klassennamen als Text. Der Name, den `as` bindet, existiert nur innerhalb des `except`-Blocks; Python löscht ihn, wenn der Block endet.

# --instructions--

Schreibe eine Funktion `error_label(text)`, die `int(text)` zurückgibt und, wenn das einen `ValueError` auslöst, eine Zeichenkette zurückgibt, die aus dem Klassennamen der Ausnahme, `": "` und der Ausnahmemeldung besteht, zum Beispiel `"ValueError: ..."`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`error_label("8")` muss `8` zurückgeben

```python
    def test_1(self):
        self.assertEqual(error_label("8"), 8, "--err-t1--")
```

`error_label("abc")` muss mit dem Klassennamen beginnen

```python
    def test_2(self):
        self.assertTrue(error_label("abc").startswith("ValueError: "), "--err-t2--")
```

`error_label("abc")` muss auch die Meldung enthalten, die `abc` erwähnt

```python
    def test_3(self):
        self.assertIn("abc", error_label("abc")[12:], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def error_label(text):
    try:
        return int(text)
    except ValueError as e:
        return f"{type(e).__name__}: {e}"
```
