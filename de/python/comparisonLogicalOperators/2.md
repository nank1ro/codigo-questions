---
language: python
exerciseType: 1
---

# --description--

Vergleichsoperatoren sind nicht auf Zahlen beschränkt. Zeichenketten werden Zeichen für Zeichen über ihre Codepunkte verglichen, sodass `"apple" < "banana"` `True` ist und, weil jeder Großbuchstabe vor den Kleinbuchstaben kommt, `"Zoo" < "apple"` ebenfalls `True` ist. Listen und Tupel werden auf die gleiche Weise Element für Element verglichen:
```python
print("cat" < "dog")       # True
print([1, 2, 3] < [1, 3])  # True
print((1, 2) == (1, 2))    # True
```
Ein Vergleich ist ein Ausdruck, sodass eine Funktion `return a < b` direkt zurückgeben kann, statt ihn in ein `if` zu verpacken.

# --instructions--

Schreibe eine Funktion `comes_first(a, b)`, die `True` zurückgibt, wenn die Zeichenkette `a` vor der Zeichenkette `b` geordnet ist, und `False`, wenn sie gleich sind oder `a` nach `b` kommt

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`comes_first("apple", "banana")` muss `True` zurückgeben

```python
    def test_1(self):
        self.assertIs(comes_first("apple", "banana"), True, "--err-t1--")
```

`comes_first("pear", "apple")` und `comes_first("kiwi", "kiwi")` müssen `False` zurückgeben

```python
    def test_2(self):
        self.assertIs(comes_first("pear", "apple"), False, "--err-t2--")
        self.assertIs(comes_first("kiwi", "kiwi"), False, "--err-t2--")
```

Großbuchstaben kommen vor Kleinbuchstaben, also muss `comes_first("Zoo", "apple")` `True` zurückgeben

```python
    def test_3(self):
        self.assertIs(comes_first("Zoo", "apple"), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def comes_first(a, b):
    return a < b
```
