---
language: python
exerciseType: 1
---

# --description--

Kurzschlussauswertung ist eine sichere Möglichkeit, eine Operation abzusichern (**Guard**), die bei manchen Werten fehlschlagen würde. In `word is not None and len(word) < 4` wird `len(word)` nur ausgeführt, wenn `word` nicht `None` ist, daher löst der Aufruf nie einen Fehler aus. Der Guard muss zuerst kommen:
```python
def is_short(word):
    return word is not None and len(word) < 4

print(is_short(None))   # False
print(is_short("cat"))  # True
```
Denke daran, dass `and` einen Operanden zurückgibt: `word and len(word) < 4` ergibt `None` für `None` und `""` für die leere Zeichenkette, nicht `False`. Wenn ein Boolean verlangt ist, sichere mit einem echten Vergleich ab.

# --instructions--

Schreibe eine Funktion `first_is_zero(numbers)`, die `True` zurückgibt, wenn die Liste `numbers` nicht leer ist und ihr erstes Element `0` ist, und sonst `False`. Das Ergebnis muss auch für eine leere Liste ein Boolean sein

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_is_zero([0, 5])` muss `True` zurückgeben

```python
    def test_1(self):
        self.assertIs(first_is_zero([0, 5]), True, "--err-t1--")
        self.assertIs(first_is_zero([0]), True, "--err-t1--")
```

`first_is_zero([3, 0])` muss `False` zurückgeben

```python
    def test_2(self):
        self.assertIs(first_is_zero([3, 0]), False, "--err-t2--")
```

`first_is_zero([])` muss `False` zurückgeben, ohne einen Fehler auszulösen

```python
    def test_3(self):
        self.assertIs(first_is_zero([]), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def first_is_zero(numbers):
    return len(numbers) > 0 and numbers[0] == 0
```
