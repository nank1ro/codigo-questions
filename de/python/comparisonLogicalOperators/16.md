---
language: python
exerciseType: 1
---

# --description--

`any(iterable)` gibt `True` zurück, wenn mindestens ein Element truthy ist, und `all(iterable)`, wenn jedes Element es ist (`all([])` ist `True`). In Kombination mit einem Generatorausdruck drücken sie eine Bedingung über eine ganze Sammlung aus, und sie schließen kurz wie `or` und `and`:
```python
print(any(ch.isdigit() for ch in "abc1"))  # True
print(all(ch.isalpha() for ch in "ab1"))   # False
```

# --instructions--

Schreibe eine Funktion `is_valid_password(password)`, die `True` zurückgibt, nur wenn die Zeichenkette zwischen `8` und `20` Zeichen (einschließlich) hat, mindestens eine Ziffer enthält und keine Leerzeichen enthält, und sonst `False`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`"secret123"` ist gültig, und ebenso Passwörter mit genau `8` oder `20` Zeichen

```python
    def test_1(self):
        self.assertIs(is_valid_password("secret123"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefg1"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefghij123456789x"), True, "--err-t1--")
```

Zu kurze oder zu lange Passwörter sind nicht gültig

```python
    def test_2(self):
        self.assertIs(is_valid_password("short1"), False, "--err-t2--")
        self.assertIs(is_valid_password("abcdefghij1234567890x"), False, "--err-t2--")
```

Ein Passwort ohne Ziffern oder mit einem Leerzeichen ist nicht gültig

```python
    def test_3(self):
        self.assertIs(is_valid_password("nodigitshere"), False, "--err-t3--")
        self.assertIs(is_valid_password("pass word1"), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_valid_password(password):
    return (
        8 <= len(password) <= 20
        and any(ch.isdigit() for ch in password)
        and " " not in password
    )
```
