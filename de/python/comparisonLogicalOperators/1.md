---
language: python
exerciseType: 2
---

# --description--

Vergleichsoperatoren vergleichen zwei Werte und geben einen **Boolean** zurück, `True` oder `False`: `==` gleich, `!=` ungleich, `<` kleiner als, `>` größer als, `<=` kleiner als oder gleich, `>=` größer als oder gleich:
```python
age = 18
print(age == 18)  # True
print(age != 18)  # False
print(age < 18)   # False
print(age >= 18)  # True
```
Das Ergebnis kann in einer Variablen gespeichert oder direkt ausgegeben werden. Ein einzelnes `=` ist eine Zuweisung, kein Vergleich.

# --instructions--

Vervollständige den Code so, dass die drei Zeilen prüfen, ob `score` gleich `75`, kleiner als `100` und größer als `50` ist, mit der Ausgabe `True`, `True`, `True`

# --seed--

```python
score = 75
print(score [/] 75)
print(score [/] 100)
print(score [/] 50)
```

# --answers--

- ==
- <
- >
- =
- !==

# --solutions--

```python
score = 75
print(score == 75)
print(score < 100)
print(score > 50)
```

# --output--

True
True
True
