---
language: python
exerciseType: 1
---

# --description--

Logische Operatoren kombinieren Booleans. `and` ist `True`, nur wenn beide Seiten `True` sind, `or`, wenn mindestens eine Seite es ist, und `not` kehrt einen einzelnen Wert um:
```python
age = 20
member = False
print(age >= 18 and member)  # False
print(age >= 18 or member)   # True
print(not member)            # True
```
Vergleiche binden stärker als logische Operatoren, daher braucht `age >= 18 and member` keine Klammern. Klammern braucht man, um ein `or` innerhalb eines `and` zu gruppieren: `a and (b or c)`.

# --instructions--

Schreibe eine Funktion `is_leap(year)`, die `True` zurückgibt, wenn `year` durch `4`, aber nicht durch `100` teilbar ist, außer sie ist auch durch `400` teilbar, und sonst `False`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`2024` und `2000` sind Schaltjahre

```python
    def test_1(self):
        self.assertIs(is_leap(2024), True, "--err-t1--")
        self.assertIs(is_leap(2000), True, "--err-t1--")
```

`2023` ist kein Schaltjahr

```python
    def test_2(self):
        self.assertIs(is_leap(2023), False, "--err-t2--")
```

`1900` ist durch `100`, aber nicht durch `400` teilbar, daher ist es kein Schaltjahr

```python
    def test_3(self):
        self.assertIs(is_leap(1900), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_leap(year):
    return year % 4 == 0 and (year % 100 != 0 or year % 400 == 0)
```
