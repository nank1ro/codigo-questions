---
language: python
exerciseType: 4
---

# --description--

Logische Operatoren **schließen kurz**: `and` stoppt, sobald ein Operand falsy ist, und `or`, sobald einer truthy ist, weil das Ergebnis bereits bekannt ist. Die verbleibenden Operanden werden nie ausgewertet, sind es Funktionsaufrufe, werden sie also nicht ausgeführt:
```python
def check(n):
    print("checking", n)
    return n > 0

check(-1) and check(5)  # prints only "checking -1"
check(2) or check(-3)   # prints only "checking 2"
```

# --instructions--

Ordne die Elemente, um `is_positive` zu definieren, `first` mit `and` und danach `second` mit `or` zu berechnen, und gib beide Ergebnisse aus, mit der Ausgabe `checking -1`, `checking 2`, `False True`

# --answers--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
```

```python
first = is_positive(-1) and is_positive(5)
```

```python
second = is_positive(2) or is_positive(-3)
```

```python
print(first, second)
```

# --solutions--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
first = is_positive(-1) and is_positive(5)
second = is_positive(2) or is_positive(-3)
print(first, second)
```

# --output--

checking -1
checking 2
False True
