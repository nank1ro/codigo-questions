---
language: python
exerciseType: 3
---

# --description--

Wenn `not`, `and` und `or` in einem Ausdruck vorkommen, wendet Python zuerst `not` an, dann `and`, dann `or`. `a or b and c` bedeutet also `a or (b and c)`, und `not a == b` bedeutet `not (a == b)`:
```python
x = 6
print(x == 6 or x < 10 and x > 100)  # True: x == 6 or (False)
print(not x == 6)                    # False
```
Wenn eine andere Gruppierung gemeint ist, füge Klammern hinzu; sie machen den Ausdruck auch leichter lesbar.

# --instructions--

Was gibt dieser Code aus?
```python
x = 3
print(x == 3 or x > 10 and x < 0)
```

# --answers--

- `True`, weil `and` vor `or` angewendet wird, sodass der Ausdruck als `x == 3 or (x > 10 and x < 0)` gelesen wird
- `False`, weil die Operatoren von links nach rechts angewendet werden: `(x == 3 or x > 10) and x < 0`
- `False`, weil `x < 0` falsch ist und jede Bedingung im Ausdruck erfüllt sein muss
- Ein `SyntaxError`, weil `and` und `or` ohne Klammern nicht gemischt werden können

# --solutions--

- `True`, weil `and` vor `or` angewendet wird, sodass der Ausdruck als `x == 3 or (x > 10 and x < 0)` gelesen wird
