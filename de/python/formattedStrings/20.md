---
language: python
exerciseType: 1
---

# --instructions--

Verwenden Sie die offnende `{` und die schlieende `}` geschweifte Klammer, um die Zahl `500` in den leeren Platz der formatierten Zeichenkette einzufugen

# --seed--

```python
print(f"There are  items")
```

# --solutions--

```python
print(f"There are {500} items")
```

# --output--

There are 500 items
