---
language: python
exerciseType: 2
---

# --description--

`asyncio.wait_for(awaitable, timeout)` awaited etwas, gibt aber nach `timeout` Sekunden auf: Die Operation wird abgebrochen und ein `TimeoutError` ausgelöst, der wie jede Ausnahme abgefangen werden kann:
```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())  # timed out
```
Mit einem Timeout von `0.1` würde derselbe Code `done` ausgeben. `asyncio.TimeoutError` ist ein anderer Name für die eingebaute `TimeoutError`.

# --instructions--

Vervollständige den Code, sodass `slow()` mit einem Timeout von `0.01` Sekunden awaited wird und der Timeout abgefangen wird, sodass `timed out` ausgegeben wird

# --seed--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.[/](slow(), [/]=0.01)
        print(result)
    except [/]:
        print("timed out")

asyncio.run(main())
```

# --answers--

- wait_for
- timeout
- TimeoutError
- gather
- delay
- ValueError

# --solutions--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())
```

# --output--

timed out
