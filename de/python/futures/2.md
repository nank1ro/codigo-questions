---
language: python
exerciseType: 1
---

# --description--

Eine Coroutine kann Parameter entgegennehmen und genau wie eine normale Funktion einen Wert mit `return` zurückgeben. Der Wert ist nicht verfügbar, wenn das Coroutine-Objekt erzeugt wird, sondern erst, wenn die Coroutine ausgeführt wurde. `asyncio.run` gibt zurück, was die Coroutine zurückgegeben hat:
```python
import asyncio

async def double(n):
    return n * 2

result = asyncio.run(double(21))
print(result)  # 42
```
Alles, was du über Funktionen weißt, gilt auch innerhalb von `async def`: lokale Variablen, Bedingungen, Schleifen und mehrere `return`-Anweisungen.

# --instructions--

Schreibe eine Coroutine-Funktion `add(a, b)` mit `async def`, die die Summe von `a` und `b` zurückgibt

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`add` muss eine Coroutine-Funktion sein

```python
    def test_1(self):
        self.assertTrue(inspect.iscoroutinefunction(add), "--err-t1--")
```

Der Aufruf von `add(1, 3)` muss `4` zurückgeben

```python
    def test_2(self):
        self.assertEqual(asyncio.run(add(1, 3)), 4, "--err-t2--")
```

Der Aufruf von `add(200, 210)` muss `410` zurückgeben

```python
    def test_3(self):
        self.assertEqual(asyncio.run(add(200, 210)), 410, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
async def add(a, b):
    return a + b
```
