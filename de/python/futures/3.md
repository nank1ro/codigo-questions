---
language: python
exerciseType: 1
---

# --description--

`await` pausiert die aktuelle Coroutine, bis der awaited Vorgang abgeschlossen ist, und gibt dann sein Ergebnis zurück. Während die Coroutine pausiert, ist die Event-Loop frei, andere Coroutinen auszuführen. `await` ist nur innerhalb eines `async def` erlaubt.

`asyncio.sleep(seconds)` ist das einfachste Awaitable: Es wartet die angegebene Zeit, ohne die Loop zu blockieren. Anders als `time.sleep` muss es awaited werden:
```python
import asyncio

async def countdown():
    print("ready")
    await asyncio.sleep(0.05)
    print("go")

asyncio.run(countdown())
```
Das Programm gibt `ready` aus, wartet 50 Millisekunden und gibt `go` aus. `asyncio.sleep(0.05)` ohne `await` zu schreiben erzeugt das Coroutine-Objekt, führt es aber nie aus, sodass nicht gewartet wird.

# --instructions--

Schreibe eine Coroutine-Funktion `wait_and_return(value, delay)`, die `delay` Sekunden mit `asyncio.sleep` wartet und dann `value` zurückgibt

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`wait_and_return("ok", 0.01)` muss `"ok"` zurückgeben

```python
    def test_1(self):
        self.assertEqual(asyncio.run(wait_and_return("ok", 0.01)), "ok", "--err-t1--")
```

`wait_and_return(7, 0.02)` muss `7` zurückgeben und mindestens `0.02` Sekunden dauern

```python
    def test_2(self):
        start = time.perf_counter()
        result = asyncio.run(wait_and_return(7, 0.02))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, 7, "--err-t2--")
        self.assertGreaterEqual(elapsed, 0.018, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def wait_and_return(value, delay):
    await asyncio.sleep(delay)
    return value
```
