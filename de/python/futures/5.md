---
language: python
exerciseType: 1
---

# --description--

Coroutinen rufen einander mit `await` auf. Eine Coroutine kann jede andere Coroutine awaiten, ihren Rückgabewert empfangen und weiterlaufen, genau wie eine Kette von Funktionsaufrufen:
```python
import asyncio

async def fetch_price(item):
    await asyncio.sleep(0.01)  # simulates a slow lookup
    return 10

async def total(item, quantity):
    price = await fetch_price(item)
    return price * quantity

print(asyncio.run(total("pen", 3)))  # 30
```
Nur die äußerste Coroutine läuft über `asyncio.run`; jede innere wird mit `await` erreicht. Wäre `total` ein normales `def`, könnte es `await` überhaupt nicht verwenden: Das Schlüsselwort `async` breitet sich auf jede Funktion in der Kette aus, die warten muss.

# --instructions--

Schreibe eine Coroutine-Funktion `fetch_user(user_id)`, die `0.01` Sekunden wartet und die Zeichenkette `"user"` gefolgt von der id zurückgibt, und eine Coroutine-Funktion `greet_user(user_id)`, die `fetch_user` awaited und `"Hello, "` gefolgt vom geholten Namen zurückgibt

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch_user(1)` muss `"user1"` zurückgeben und `fetch_user(42)` muss `"user42"` zurückgeben

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch_user(1)), "user1", "--err-t1--")
        self.assertEqual(asyncio.run(fetch_user(42)), "user42", "--err-t1--")
```

`greet_user(1)` muss `"Hello, user1"` zurückgeben

```python
    def test_2(self):
        self.assertEqual(asyncio.run(greet_user(1)), "Hello, user1", "--err-t2--")
```

Sowohl `fetch_user` als auch `greet_user` müssen Coroutine-Funktionen sein

```python
    def test_3(self):
        self.assertTrue(inspect.iscoroutinefunction(fetch_user), "--err-t3--")
        self.assertTrue(inspect.iscoroutinefunction(greet_user), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch_user(user_id):
    await asyncio.sleep(0.01)
    return f"user{user_id}"

async def greet_user(user_id):
    name = await fetch_user(user_id)
    return f"Hello, {name}"
```
