---
language: python
exerciseType: 1
---

# --description--

Eine innerhalb einer Coroutine ausgelöste Ausnahme tritt nicht dort auf, wo das Coroutine-Objekt erzeugt wurde: Sie wird an dem `await` ausgelöst, das die Coroutine ausführt, oder von `asyncio.run` bei der äußersten. `try`/`except` muss daher das **`await`** umschließen:
```python
import asyncio

async def load(path):
    await asyncio.sleep(0.01)
    if path == "":
        raise FileNotFoundError("empty path")
    return "contents"

async def safe_load(path):
    try:
        return await load(path)
    except FileNotFoundError:
        return ""

print(asyncio.run(safe_load("")))  # prints an empty line
```
Eine Ausnahme, die niemand abfängt, breitet sich durch jedes `await` bis zu `asyncio.run` aus, das sie im synchronen Code erneut auslöst, genau wie ein normaler Aufrufstapel.

# --instructions--

Schreibe eine Coroutine-Funktion `divide(a, b)`, die `0.01` Sekunden wartet und `a / b` zurückgibt, und eine Coroutine-Funktion `safe_divide(a, b)`, die `divide(a, b)` awaited und ihr Ergebnis zurückgibt, oder `None`, wenn ein `ZeroDivisionError` ausgelöst wird

# --before-asserts--

```python
import asyncio
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`divide(10, 4)` muss `2.5` zurückgeben

```python
    def test_1(self):
        self.assertEqual(asyncio.run(divide(10, 4)), 2.5, "--err-t1--")
```

`divide(1, 0)` muss beim Ausführen einen `ZeroDivisionError` auslösen

```python
    def test_2(self):
        with self.assertRaises(ZeroDivisionError, msg="--err-t2--"):
            asyncio.run(divide(1, 0))
```

`safe_divide(10, 2)` muss `5.0` zurückgeben und `safe_divide(1, 0)` muss `None` zurückgeben

```python
    def test_3(self):
        self.assertEqual(asyncio.run(safe_divide(10, 2)), 5.0, "--err-t3--")
        self.assertIsNone(asyncio.run(safe_divide(1, 0)), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def divide(a, b):
    await asyncio.sleep(0.01)
    return a / b

async def safe_divide(a, b):
    try:
        return await divide(a, b)
    except ZeroDivisionError:
        return None
```
