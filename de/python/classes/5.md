---
language: python
exerciseType: 2
---

# --description--

Das erste Argument `__init__()` wird verwendet, um auf das Instanzobjekt zu verweisen, und per Konvention wird dieses Argument `self` genannt.
Wenn Sie zusatzliche Argumente hinzufugen (zum Beispiel ein `gender` und `legs` fur Ihr Tier), indem Sie jedes dieser Argumente im Rumpf von `__init__()` gleich `self.gender` und `self.legs` setzen, wird bewirkt, dass Sie beim Erstellen eines Instanzobjekts Ihrer `Animal`-Klasse jeder Instanz ein Geschlecht und Beine zuweisen mussen, und diese werden mit der bestimmten Instanz verknupft, die Sie erstellen

# --instructions--

Verwenden Sie `self.gender` und `self.legs`, um diese Geschlechter und Beine jedem Objekt zuzuweisen

# --seed--

```python
class Animal:
    def __init__(self, gender, legs):
        [/].[/] = gender
        self.legs = [/]
```

# --answers--

- self
- gender
- =
- legs
- .
- ;

# --solutions--

```python
class Animal:
    def __init__(self, gender, legs):
        self.gender = gender
        self.legs = legs
```
