---
language: python
exerciseType: 2
---

# --description--

Let's replace `pass` with something else.
The method `__init__()` is required for classes, and it's used to __initialize__ the objects it creates.
`__init__()` always takes at least one argument, `self`, that refers to the object being created.
You can think of `__init__()` as the method that boots up each object the class creates.

# --instructions--

Definieren Sie eine `__init__()` Methode für Ihre `Animal` Klasse.
Übergeben Sie ihr das Argument `self` für jetzt.
Fügen Sie schließlich das `pass` in den Codeblock der `__init__()` Definition ein

# --seed--

```python
class Animal:
    def [/]([/]):
        [/]
```

# --answers--

- __init__
- self
- object
- pass

# --solutions--

```python
class Animal:
    def __init__(self):
        pass
```
