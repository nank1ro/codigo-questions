---
language: python
exerciseType: 1
---

# --description--

lambda-Parameter unterstützen auch **Standardwerte**:
```python
greet = lambda name="World": f"Hello, {name}!"
print(greet())       # Hello, World!
print(greet("Ana"))  # Hello, Ana!
```

# --instructions--

Speichere in der Variable `multiply` eine lambda, die `n` und einen zweiten Parameter `factor` mit dem Standardwert `2` nimmt und `n` multipliziert mit `factor` zurückgibt

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst einen `lambda`-Ausdruck verwenden

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`multiply(5)` muss `10` zurückgeben

```python
    def test_1(self):
        self.assertEqual(multiply(5), 10, "--err-t1--")
```

`multiply(5, 3)` muss `15` zurückgeben

```python
    def test_2(self):
        self.assertEqual(multiply(5, 3), 15, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
multiply = lambda n, factor=2: n * factor
```
