---
language: python
exerciseType: 2
---

# --description--

`filter()` behält nur die Elemente, für die die Funktion `True` zurückgibt:
```python
nums = [5, 12, 8, 20]
big = list(filter(lambda n: n > 10, nums))
print(big)  # [12, 20]
```

# --instructions--

Vervollständige den Code, um nur die geraden Zahlen zu behalten, sodass das Programm `[2, 4]` ausgibt

# --seed--

```python
nums = [1, 2, 3, 4]
evens = list([/](lambda n: n % 2 [/] 0, nums))
print(evens)
```

# --answers--

- filter
- map
- sorted
- ==
- !=

# --solutions--

```python
nums = [1, 2, 3, 4]
evens = list(filter(lambda n: n % 2 == 0, nums))
print(evens)
```

# --output--

[2, 4]
