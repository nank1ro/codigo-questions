---
language: python
exerciseType: 2
---

# --description--

Eine lambda kann **mehr als einen Parameter** nehmen.
Trenne sie mit Kommas, genau wie in einer `def`-Funktion:
```python
power = lambda base, exp: base ** exp
print(power(2, 3))  # 8
```

# --instructions--

Vervollständige die lambda so, dass sie die Parameter `a` und `b` nimmt und ihre Summe zurückgibt

# --seed--

```python
add = lambda a[/] b[/] a + b
print(add(3, 4))
```

# --answers--

- ,
- :
- ;
- ->

# --solutions--

```python
add = lambda a, b: a + b
print(add(3, 4))
```

# --output--

7
