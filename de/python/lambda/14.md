---
language: python
exerciseType: 1
---

# --description--

**Wann solltest du `def` bevorzugen?**
Eine lambda ist ideal für eine kurze, einmalig verwendete Funktion, die als Argument übergeben wird.
Wenn die Logik einen Namen, mehrere Zeilen, einen Docstring braucht oder an vielen Stellen wiederverwendet wird, ist eine `def`-Funktion klarer.
Ein letzter Trick: `sorted()` akzeptiert auch `reverse=True`, um zuerst die größten Werte zu erhalten:
```python
print(sorted([3, 1, 2], reverse=True))  # [3, 2, 1]
```

# --instructions--

Speichere in `ranking` die Liste `students`, sortiert vom höchsten zum niedrigsten `grade`, und speichere dann in `names` die Liste der Namen aus `ranking`, mit `sorted()` und `map()` und lambdas

# --seed--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst einen `lambda`-Ausdruck verwenden

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst `map()` verwenden

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ranking` muss mit dem Studenten `Cy` beginnen

```python
    def test_1(self):
        self.assertEqual(ranking[0]["name"], "Cy", "--err-t1--")
```

`names` muss gleich `['Cy', 'Ana', 'Bo']` sein

```python
    def test_2(self):
        self.assertEqual(names, ["Cy", "Ana", "Bo"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
ranking = sorted(students, key=lambda s: s["grade"], reverse=True)
names = list(map(lambda s: s["name"], ranking))
```
