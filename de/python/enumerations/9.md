---
language: python
exerciseType: 1
---

# --description--

Ein Enum ist eine Klasse, daher kann es wie jede andere Klasse **Methoden** und **Properties** haben.
In ihnen ist `self` der Member, auf dem die Methode aufgerufen wurde, du kannst dir also `self.name`, `self.value` ansehen oder `self` mit anderen Membern vergleichen:
```python
from enum import Enum

class Light(Enum):
    RED = 1
    GREEN = 2

    def can_go(self):
        return self is Light.GREEN

    @property
    def label(self):
        return self.name.lower()

print(Light.GREEN.can_go())  # True
print(Light.RED.label)       # red
```
Jeder einfache Wert, der im Klassenkörper zugewiesen wird, wird zu einem Member, während Funktionen und Properties das nie werden, egal wo sie auftauchen.

# --instructions--

Füge zu `Season` eine Methode `is_warm()` hinzu, die nur für `SPRING` und `SUMMER` `True` zurückgibt, und eine Property `label`, die den Member-Namen in Kleinbuchstaben zurückgibt

# --seed--

```python
from enum import Enum

class Season(Enum):
    WINTER = 1
    SPRING = 2
    SUMMER = 3
    AUTUMN = 4
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst die Methode `is_warm(self)` definieren

```json
{
  "regex": "def is_warm\\(self\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst den `@property`-Dekorator verwenden

```json
{
  "regex": "@property",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Season.SUMMER.is_warm()` muss `True` sein

```python
    def test_1(self):
        self.assertEqual(Season.SUMMER.is_warm(), True, "--err-t1--")
        self.assertEqual(Season.SPRING.is_warm(), True, "--err-t1--")
```

`Season.WINTER.is_warm()` muss `False` sein

```python
    def test_2(self):
        self.assertEqual(Season.WINTER.is_warm(), False, "--err-t2--")
        self.assertEqual(Season.AUTUMN.is_warm(), False, "--err-t2--")
```

`Season.AUTUMN.label` muss gleich `"autumn"` sein

```python
    def test_3(self):
        self.assertEqual(Season.AUTUMN.label, "autumn", "--err-t3--")
        self.assertEqual(Season.WINTER.label, "winter", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Season(Enum):
    WINTER = 1
    SPRING = 2
    SUMMER = 3
    AUTUMN = 4

    def is_warm(self):
        return self in (Season.SPRING, Season.SUMMER)

    @property
    def label(self):
        return self.name.lower()
```
