---
language: python
exerciseType: 4
---

# --description--

Eine Enum-Klasse ist **iterierbar**: eine `for`-Schleife über die Klasse besucht jeden Member, in der Reihenfolge, in der sie deklariert wurden:
```python
for color in Color:
    print(color.name, color.value)
```
`len()` liefert, wie viele Member das Enum hat, und `list(Color)` erstellt daraus eine Liste:
```python
print(len(Color))    # 3
print(list(Color))   # [<Color.RED: 1>, <Color.GREEN: 2>, <Color.BLUE: 3>]
```
Innerhalb einer Liste werden Member mit ihrer `repr()` angezeigt, die den Wert zwischen spitzen Klammern enthält.

# --instructions--

Sortiere die Elemente, um das Enum `Weekday` zu deklarieren, gib aus, wie viele Member es hat, und gib dann den Namen und den Wert jedes Members aus, einer pro Zeile

# --answers--

```python
from enum import Enum
```

```python
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
```

```python
print(len(Weekday))
```

```python
for day in Weekday:
```

```python
    print(day.name, day.value)
```

# --solutions--

```python
from enum import Enum
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
print(len(Weekday))
for day in Weekday:
    print(day.name, day.value)
```

# --output--

3
MON 1
TUE 2
WED 3
