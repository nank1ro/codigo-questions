---
language: python
exerciseType: 4
---

# --description--

Enums passen natürlich zur `match`-Anweisung (verfügbar seit Python 3.10), die einen Wert mit einer Reihe von `case`-Mustern vergleicht und den ersten passenden ausführt:
```python
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
```
Schreibe den Member immer mit seiner Klasse, wie `Light.RED`: ein bloßer Name wie `case RED:` würde nichts vergleichen, er würde den Wert nur in eine neue Variable `RED` einfangen und auf alles passen.
Der Platzhalter `case _:` ist der Standardfall und muss zuletzt kommen, denn jedes Muster danach wäre nie erreichbar.

# --instructions--

Sortiere die Elemente, um das Enum `Light` zu deklarieren, definiere `action()` mit dem `RED`-Fall zuerst, dann dem `GREEN`-Fall, dann dem Standardfall, und gib die Aktion für `Light.YELLOW` aus

# --answers--

```python
from enum import Enum
```

```python
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
```

```python
def action(light):
```

```python
    match light:
```

```python
        case Light.RED:
            return "Stop"
```

```python
        case Light.GREEN:
            return "Go"
```

```python
        case _:
            return "Slow down"
```

```python
print(action(Light.YELLOW))
```

# --solutions--

```python
from enum import Enum
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
print(action(Light.YELLOW))
```

# --output--

Slow down
