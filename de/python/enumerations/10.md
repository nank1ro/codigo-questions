---
language: python
exerciseType: 2
---

# --description--

Wenn zwei Member denselben Wert haben, ist der zweite kein neuer Member, sondern ein **Alias** des ersten:
```python
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # alias of ACTIVE

print(Status.ENABLED)  # Status.ACTIVE
print(len(Status))     # 1
```
Aliase werden beim Iterieren übersprungen und von `len()` nicht mitgezählt.
Normalerweise ist ein doppelter Wert ein Fehler. Der `unique`-Dekorator, importiert aus `enum`, bringt Python dazu, einen `ValueError` auszulösen, sobald ein Enum mit Aliasen deklariert wird:
```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # ValueError: duplicate values found
```

# --instructions--

Vervollständige den Code, um den `unique`-Dekorator zu importieren und auf `Status` anzuwenden, sodass doppelte Werte abgelehnt werden, und gib dann die Anzahl der Member aus

# --seed--

```python
from enum import Enum, [/]

[/]
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --answers--

- unique
- @unique
- auto
- @auto
- @property
- @classmethod

# --solutions--

```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --output--

2
