---
language: python
exerciseType: 1
---

# --description--

Neben normalen Methoden kann ein Enum **Klassenmethoden** mit `@classmethod` definieren. Sie erhalten die Enum-Klasse selbst als `cls`, daher sind sie der richtige Ort für alternative Wege, einen Member zu finden:
```python
class Color(Enum):
    RED = auto()
    BLUE = auto()

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]

print(Color.parse(" blue "))  # Color.BLUE
```
Zusammen mit `auto()`, Methoden, Properties und Nachschlägen kannst du damit Enums bauen, die ihr eigenes Verhalten mitbringen.

# --instructions--

Deklariere ein Enum `Direction` mit den Membern `NORTH`, `EAST`, `SOUTH` und `WEST`, in dieser Reihenfolge, mit `auto()`.
Füge eine Methode `opposite()` hinzu, die den Member auf der gegenüberliegenden Seite zurückgibt (`NORTH` und `SOUTH`, `EAST` und `WEST`), und eine Klassenmethode `parse(text)`, die den Member zurückgibt, dessen Name `text` entspricht, ohne Groß-/Kleinschreibung und umgebende Leerzeichen zu beachten

# --seed--

```python
from enum import Enum, auto
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `auto()` verwenden

```json
{
  "regex": "auto\\(\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst den `@classmethod`-Dekorator verwenden

```json
{
  "regex": "@classmethod",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Direction` muss die Member `NORTH`, `EAST`, `SOUTH` und `WEST` mit Werten von `1` bis `4` haben

```python
    def test_1(self):
        self.assertEqual([d.name for d in Direction], ["NORTH", "EAST", "SOUTH", "WEST"], "--err-t1--")
        self.assertEqual([d.value for d in Direction], [1, 2, 3, 4], "--err-t1--")
```

`Direction.NORTH.opposite()` muss `Direction.SOUTH` sein und `Direction.WEST.opposite()` muss `Direction.EAST` sein

```python
    def test_2(self):
        self.assertIs(Direction.NORTH.opposite(), Direction.SOUTH, "--err-t2--")
        self.assertIs(Direction.SOUTH.opposite(), Direction.NORTH, "--err-t2--")
        self.assertIs(Direction.EAST.opposite(), Direction.WEST, "--err-t2--")
        self.assertIs(Direction.WEST.opposite(), Direction.EAST, "--err-t2--")
```

`Direction.parse(" west ")` muss `Direction.WEST` sein

```python
    def test_3(self):
        self.assertIs(Direction.parse(" west "), Direction.WEST, "--err-t3--")
        self.assertIs(Direction.parse("North"), Direction.NORTH, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    EAST = auto()
    SOUTH = auto()
    WEST = auto()

    def opposite(self):
        opposites = {
            Direction.NORTH: Direction.SOUTH,
            Direction.SOUTH: Direction.NORTH,
            Direction.EAST: Direction.WEST,
            Direction.WEST: Direction.EAST,
        }
        return opposites[self]

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]
```
