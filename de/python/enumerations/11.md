---
language: python
exerciseType: 1
---

# --description--

Ein `Flag` ist ein Enum, dessen Member **kombiniert** werden können: ein Wert kann mehrere Member gleichzeitig halten, wie ein Satz von Optionen.
Deklariere seine Member mit `auto()`, das bei einem `Flag` Zweierpotenzen (`1`, `2`, `4`, ...) zuweist, sodass jede Kombination einen eigenen Wert hat:
```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```
Verwende `|`, um Member zu kombinieren, `in`, um zu prüfen, ob ein Member Teil einer Kombination ist, und `value`, um die resultierende Zahl zu sehen:
```python
full = Permission.READ | Permission.WRITE | Permission.EXECUTE
print(Permission.WRITE in full)  # True
print(full.value)                # 7
print(full)                      # Permission.READ|WRITE|EXECUTE
```

# --instructions--

Speichere in `editor` die Kombination aus `Permission.READ` und `Permission.WRITE` mit `|`, in `can_write`, ob `Permission.WRITE` in `editor` enthalten ist, und in `bits` den Wert von `editor`

# --seed--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst den `|`-Operator verwenden

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst den `in`-Operator verwenden

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`editor` muss gleich `Permission.READ | Permission.WRITE` sein

```python
    def test_1(self):
        self.assertEqual(editor, Permission.READ | Permission.WRITE, "--err-t1--")
```

`can_write` muss `True` sein

```python
    def test_2(self):
        self.assertEqual(can_write, True, "--err-t2--")
```

`bits` muss gleich `3` sein

```python
    def test_3(self):
        self.assertEqual(bits, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()

editor = Permission.READ | Permission.WRITE
can_write = Permission.WRITE in editor
bits = editor.value
```
