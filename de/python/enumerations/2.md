---
language: python
exerciseType: 1
---

# --description--

Jeder Member eines Enums hat zwei Attribute: `name`, den Bezeichner, den du in der Klasse geschrieben hast, und `value`, den Wert, den du ihm zugewiesen hast:
```python
from enum import Enum

class Color(Enum):
    RED = 1
    GREEN = 2

print(Color.GREEN.name)   # GREEN
print(Color.GREEN.value)  # 2
```
Der Wert kann einen beliebigen Typ haben, nicht nur eine ganze Zahl: Zeichenketten, Tupel und Floats sind häufige Auswahlmöglichkeiten.
Ein Member ist ein normales Objekt, du kannst ihn also in einer Variable speichern und seine Attribute später lesen:
```python
chosen = Color.RED
print(chosen.value)  # 1
```

# --instructions--

Speichere in `home` den Member `Planet.EARTH`, in `label` seinen Namen und in `position` seinen Wert

# --seed--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `.name` verwenden

```json
{
  "regex": "\\.name\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst `.value` verwenden

```json
{
  "regex": "\\.value\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`home` muss `Planet.EARTH` sein

```python
    def test_1(self):
        self.assertIs(home, Planet.EARTH, "--err-t1--")
```

`label` muss gleich `"EARTH"` sein

```python
    def test_2(self):
        self.assertEqual(label, "EARTH", "--err-t2--")
```

`position` muss gleich `3` sein

```python
    def test_3(self):
        self.assertEqual(position, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3

home = Planet.EARTH
label = home.name
position = home.value
```
