---
language: python
exerciseType: 1
---

# --description--

Jeder Funktionsaufruf erzeugt einen Wert, selbst wenn die Funktion scheinbar nichts zurückgibt. Eine Funktion **ohne** `return`-Anweisung oder mit einem bloßen `return` gibt `None` zurück:
```python
def say_hello():
    print("hello")

result = say_hello()
print(result)  # None
```
Deshalb zeigt der Aufruf von `print(my_list.append(3))` `None`: `append` ändert die Liste an Ort und Stelle und gibt nichts zurück.
Eine Funktion, die nur eine Aktion ausführt (Ausgeben, Speichern, Ändern einer Liste), gibt normalerweise `None` zurück, während eine Funktion, die etwas berechnet, es explizit mit `return` zurückgeben muss.

# --instructions--

Schreibe eine Funktion `store`, die einen Wert nimmt und ihn an die Liste `saved` anhängt, ohne etwas zurückzugeben

# --seed--

```python
saved = []

def store(value):
    pass
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `append` verwenden

```json
{
  "regex": "\\.append\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`store("a")` muss `None` zurückgeben

```python
    def test_1(self):
        self.assertIsNone(store("a"), "--err-t1--")
```

Nach `store("b")` muss die Liste `saved` mit `"b"` enden

```python
    def test_2(self):
        store("b")
        self.assertEqual(saved[-1], "b", "--err-t2--")
```

Jeder gespeicherte Wert muss in der Reihenfolge erhalten bleiben

```python
    def test_3(self):
        before = len(saved)
        store(1)
        store(2)
        self.assertEqual(saved[before:], [1, 2], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
saved = []

def store(value):
    saved.append(value)
```
