---
language: python
exerciseType: 4
---

# --description--

Funktionen, die `None` erhalten können, beginnen oft mit einem **Guard**: einem `if`, das früh zurückkehrt, wenn es nichts zu bearbeiten gibt. Der Rest der Funktion kann dann davon ausgehen, dass der Wert vorhanden ist, ohne alles in einem `else` zu verschachteln:
```python
def shout(text):
    if text is None:
        return None
    return text.upper() + "!"
```
Guards kommen zuerst, in der Reihenfolge, in der die Prüfungen stattfinden müssen: Du kannst `text.split()` nicht aufrufen, bevor du weißt, dass `text` nicht `None` ist.

# --instructions--

Ordne die Elemente, um `first_word` zu definieren, das `None` zurückgibt, wenn `text` `None` ist oder keine Wörter enthält, und sonst das erste Wort; halte die Guards vor dem Code, der von ihnen abhängt

# --answers--

```python
def first_word(text):
```

```python
    if text is None:
        return None
```

```python
    words = text.split()
```

```python
    if len(words) == 0:
        return None
```

```python
    return words[0]
```

```python
print(first_word("hello world"))
print(first_word(None))
print(first_word("   "))
```

# --solutions--

```python
def first_word(text):
    if text is None:
        return None
    words = text.split()
    if len(words) == 0:
        return None
    return words[0]
print(first_word("hello world"))
print(first_word(None))
print(first_word("   "))
```

# --output--

hello
None
None
