---
language: python
exerciseType: 1
---

# --instructions--

Schreibe eine Funktion `oldest`, die eine Liste von Wörterbüchern mit einem `"name"`-Schlüssel und einem optionalen `"age"`-Schlüssel nimmt, der fehlen oder `None` sein kann; sie muss den Namen der Person mit dem höchsten Alter zurückgeben, Personen ohne Alter ignorieren, und `None`, wenn niemand ein Alter hat

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `get` verwenden

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst mit `is None` oder `is not None` prüfen

```json
{
  "regex": "is\\s+(not\\s+)?None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Die älteste von Ada (36), Linus (28) und Grace (45) muss `"Grace"` sein

```python
    def test_1(self):
        people = [{"name": "Ada", "age": 36}, {"name": "Linus", "age": 28}, {"name": "Grace", "age": 45}]
        self.assertEqual(oldest(people), "Grace", "--err-t1--")
```

Personen mit fehlendem oder `None`-Alter müssen ignoriert werden

```python
    def test_2(self):
        people = [{"name": "Tim"}, {"name": "Ada", "age": 36}, {"name": "Bob", "age": None}]
        self.assertEqual(oldest(people), "Ada", "--err-t2--")
```

Das Ergebnis muss `None` sein, wenn niemand ein Alter hat, und ein Alter von `0` muss zählen

```python
    def test_3(self):
        self.assertIsNone(oldest([{"name": "Tim"}, {"name": "Bob", "age": None}]), "--err-t3--")
        self.assertIsNone(oldest([]), "--err-t3--")
        self.assertEqual(oldest([{"name": "Baby", "age": 0}, {"name": "Tim"}]), "Baby", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def oldest(people):
    best_name = None
    best_age = None
    for person in people:
        age = person.get("age")
        if age is None:
            continue
        if best_age is None or age > best_age:
            best_age = age
            best_name = person["name"]
    return best_name
```
