---
language: python
exerciseType: 1
---

# --description--

Eine Suche gefolgt von einer `None`-Prüfung braucht normalerweise zwei Zeilen: eine zum Speichern des Ergebnisses, eine zum Testen. Der Operator für **Zuweisungsausdrücke** `:=`, mit dem Spitznamen *Walrus*, weist einen Wert **innerhalb** eines Ausdrucks zu, sodass beide Schritte in das `if` passen:
```python
ages = {"Ada": 36}
if (age := ages.get("Ada")) is not None:
    print(age + 1)  # 37
```
Die Klammern sind erforderlich: Ohne sie würde `:=` versuchen, den ganzen Vergleich zuzuweisen. Nach dem `if` bleibt `age` wie jede andere Variable verfügbar.

# --instructions--

Schreibe eine Funktion `describe_user`, die ein Wörterbuch `users`, das Namen auf Alter abbildet, und einen `name` nimmt; sie muss `"<name> is <age>"` zurückgeben, wenn der Name vorhanden ist, und sonst `"unknown user"`, wobei sie mit dem Walrus-Operator das Ergebnis von `get` speichert

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst den Operator `:=` verwenden

```json
{
  "regex": ":=",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst `get` verwenden

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe_user({"Ada": 36}, "Ada")` muss `"Ada is 36"` zurückgeben

```python
    def test_1(self):
        self.assertEqual(describe_user({"Ada": 36}, "Ada"), "Ada is 36", "--err-t1--")
```

`describe_user({"Ada": 36}, "Grace")` muss `"unknown user"` zurückgeben

```python
    def test_2(self):
        self.assertEqual(describe_user({"Ada": 36}, "Grace"), "unknown user", "--err-t2--")
```

Ein Alter von `0` muss trotzdem als vorhanden gelten

```python
    def test_3(self):
        self.assertEqual(describe_user({"Tim": 0}, "Tim"), "Tim is 0", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe_user(users, name):
    if (age := users.get(name)) is not None:
        return f"{name} is {age}"
    return "unknown user"
```
