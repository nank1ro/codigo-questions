---
language: python
exerciseType: 1
---

# --description--

Die beiden Bedingungen können in derselben Comprehension kombiniert werden: ein `if-else`, um den Wert zu wählen, und ein Filter-`if` am Ende, um einige Elemente zu überspringen.
```python
nums = [1, 2, 3, 4]
result = ["big" if n > 2 else "small" for n in nums if n != 3]
print(result)  # ['small', 'small', 'big']
```

# --instructions--

Speichere in der Variable `clipped` eine Kopie von `nums`, in der jede negative Zahl durch `0` ersetzt wird, mithilfe einer List Comprehension mit einem `if-else`-Ausdruck

# --seed--

```python
nums = [4, -2, 7, -5]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst eine List Comprehension mit einem `if-else`-Ausdruck verwenden

```json
{
  "regex": "\\[.* if .* else .* for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`clipped` muss gleich `[4, 0, 7, 0]` sein

```python
    def test_1(self):
        self.assertEqual(clipped, [4, 0, 7, 0], "--err-t1--")
```

`nums` darf nicht verändert werden

```python
    def test_2(self):
        self.assertEqual(nums, [4, -2, 7, -5], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [4, -2, 7, -5]
clipped = [n if n >= 0 else 0 for n in nums]
```
