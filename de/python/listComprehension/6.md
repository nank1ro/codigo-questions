---
language: python
exerciseType: 1
---

# --description--

Comprehensions eignen sich hervorragend zum **Transformieren von Strings**.
Rufe eine String-Methode auf jedem Element auf, oder baue einen neuen String mit einem f-String:
```python
names = ["ana", "bob"]
greetings = [f"Hi {name.capitalize()}" for name in names]
print(greetings)  # ['Hi Ana', 'Hi Bob']
```

# --instructions--

Speichere in der Variable `shout` eine Liste mit jedem Namen aus `names` in Großbuchstaben, gefolgt von `!`, mithilfe einer List Comprehension

# --seed--

```python
names = ["ana", "bob", "cy"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst eine List Comprehension verwenden

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`shout` muss gleich `['ANA!', 'BOB!', 'CY!']` sein

```python
    def test_1(self):
        self.assertEqual(shout, ["ANA!", "BOB!", "CY!"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
names = ["ana", "bob", "cy"]
shout = [name.upper() + "!" for name in names]
```
