---
language: python
exerciseType: 1
---

# --description--

Du kannst auch über ein **Dictionary** iterieren.
Mit `.items()` entpackt der `for`-Teil jedes Paar in zwei Variablen, einen Schlüssel und einen Wert:
```python
ages = {"ana": 31, "bob": 25}
lines = [f"{name} is {age}" for name, age in ages.items()]
print(lines)  # ['ana is 31', 'bob is 25']
```

# --instructions--

Speichere in der Variable `expensive` die Liste der Namen der Produkte in `prices`, die mehr als `2` kosten, mithilfe einer List Comprehension über `prices.items()`

# --seed--

```python
prices = {"apple": 1, "steak": 12, "bread": 2, "cheese": 5}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst eine List Comprehension über `.items()` verwenden

```json
{
  "regex": "\\[.* for .* in .*\\.items\\(\\).*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`expensive` muss gleich `['steak', 'cheese']` sein

```python
    def test_1(self):
        self.assertEqual(expensive, ["steak", "cheese"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
prices = {"apple": 1, "steak": 12, "bread": 2, "cheese": 5}
expensive = [name for name, price in prices.items() if price > 2]
```
