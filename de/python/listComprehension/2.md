---
language: python
exerciseType: 1
---

# --description--

Der Ausdruck auf der linken Seite kann alles sein, was einen Wert erzeugt: eine Berechnung, ein Funktionsaufruf, ein Methodenaufruf.
Die Schleifenvariable kann einen beliebigen Namen haben und existiert nur innerhalb der eckigen Klammern:
```python
prices = [10, 20]
with_tax = [price * 1.2 for price in prices]
print(with_tax)  # [12.0, 24.0]
```

# --instructions--

Speichere in der Variable `squares` die Liste der Quadrate von `nums`, mithilfe einer List Comprehension

# --seed--

```python
nums = [1, 2, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst eine List Comprehension verwenden

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`squares` muss gleich `[1, 4, 9, 16]` sein

```python
    def test_1(self):
        self.assertEqual(squares, [1, 4, 9, 16], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 3, 4]
squares = [n * n for n in nums]
```
