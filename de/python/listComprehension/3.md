---
language: python
exerciseType: 2
---

# --description--

Eine Comprehension kann die Elemente auch **filtern**.
Füge nach dem `for`-Teil eine `if`-Bedingung hinzu: nur die Elemente, für die die Bedingung `True` ist, landen in der neuen Liste:
```python
nums = [5, 12, 8, 20]
big = [n for n in nums if n > 10]
print(big)  # [12, 20]
```
Das ist dasselbe wie eine Schleife mit einem `if` darin, und es ersetzt `filter()` mit einer lambda auf lesbarere Weise.

# --instructions--

Vervollständige die Comprehension, sodass nur die geraden Zahlen übrig bleiben und das Programm `[2, 4]` ausgibt

# --seed--

```python
nums = [1, 2, 3, 4]
evens = [n for n in nums [/] n % 2 [/] 0]
print(evens)
```

# --answers--

- if
- ==
- !=
- when
- while

# --solutions--

```python
nums = [1, 2, 3, 4]
evens = [n for n in nums if n % 2 == 0]
print(evens)
```

# --output--

[2, 4]
