---
language: python
exerciseType: 1
---

# --description--

Eine Comprehension kann **mehr als ein `for`** haben.
Sie funktionieren wie verschachtelte Schleifen: das erste `for` ist die äußere Schleife, das zweite die innere.
```python
pairs = [a + b for a in ["x", "y"] for b in ["1", "2"]]
print(pairs)  # ['x1', 'x2', 'y1', 'y2']
```

# --instructions--

Speichere in der Variable `combos` die Liste aller Kombinationen von `colors` und `sizes` als `f"{color} {size}"`, mithilfe einer List Comprehension mit zwei `for`-Teilen

# --seed--

```python
colors = ["red", "blue"]
sizes = ["S", "M"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst eine List Comprehension mit zwei `for`-Teilen verwenden

```json
{
  "regex": "\\[.* for .* in .* for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`combos` muss gleich `['red S', 'red M', 'blue S', 'blue M']` sein

```python
    def test_1(self):
        self.assertEqual(combos, ["red S", "red M", "blue S", "blue M"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
colors = ["red", "blue"]
sizes = ["S", "M"]
combos = [f"{color} {size}" for color in colors for size in sizes]
```
