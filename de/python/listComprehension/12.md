---
language: python
exerciseType: 4
---

# --description--

Das innere `for` kann die Variable des äußeren verwenden.
Das ist der klassische Weg, eine Liste von Listen zu einer einzigen Liste zu **flatten**:
```python
matrix = [[1, 2], [3, 4]]
flat = [n for row in matrix for n in row]
print(flat)  # [1, 2, 3, 4]
```
Die Funktion `sum()` addiert dann alle Zahlen einer Liste.

# --instructions--

Ordne die Elemente, um `matrix` zu flatten und die flache Liste zusammen mit der Summe ihrer Zahlen auszugeben

# --answers--

```python
matrix = [[1, 2], [3, 4]]
```

```python
flat = [n for row in matrix for n in row]
```

```python
total = sum(flat)
```

```python
print(flat, total)
```

# --solutions--

```python
matrix = [[1, 2], [3, 4]]
flat = [n for row in matrix for n in row]
total = sum(flat)
print(flat, total)
```

# --output--

[1, 2, 3, 4] 10
