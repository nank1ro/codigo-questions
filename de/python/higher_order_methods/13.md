---
language: python
exerciseType: 1
---

# --description--

Ein Decorator, der nur ein Argument akzeptiert, ist wenig nützlich. Um **jede** Funktion zu umhüllen, sammelt der Wrapper jedes positionale Argument in `*args` und jedes Schlüsselwortargument in `**kwargs` und reicht sie unverändert weiter:
```python
def uppercase(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs).upper()
    return wrapper

@uppercase
def greet(name, punctuation="!"):
    return "hello " + name + punctuation

print(greet("ada"))                    # HELLO ADA!
print(greet("ada", punctuation="?"))   # HELLO ADA?
```
Im Wrapper ist `args` ein Tupel und `kwargs` ein Wörterbuch; das `*` und `**` im Aufruf entpacken sie wieder in einzelne Argumente.

# --instructions--

Schreibe einen Decorator `exclaim`, der einen Wrapper zurückgibt, der beliebige Argumente akzeptiert, die umhüllte Funktion damit aufruft und ihr Ergebnis mit angehängtem `"!"` zurückgibt; dekoriere damit eine Funktion `greet`, die einen `name` entgegennimmt und `"hello <name>"` zurückgibt

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst den Decorator mit `@exclaim` anwenden

```json
{
  "regex": "^@exclaim\\s*$",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greet("ada")` muss `"hello ada!"` zurückgeben

```python
    def test_1(self):
        self.assertEqual(greet("ada"), "hello ada!", "--err-t1--")
```

`exclaim` muss mit einer Funktion mit zwei Argumenten funktionieren

```python
    def test_2(self):
        joined = exclaim(lambda a, b: a + " " + b)
        self.assertEqual(joined("good", "day"), "good day!", "--err-t2--")
```

`exclaim` muss Schlüsselwortargumente weiterleiten

```python
    def test_3(self):
        tag = exclaim(lambda word, times=1: word * times)
        self.assertEqual(tag("hi", times=2), "hihi!", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def exclaim(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs) + "!"
    return wrapper

@exclaim
def greet(name):
    return "hello " + name
```
