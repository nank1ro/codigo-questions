---
language: python
exerciseType: 1
---

# --description--

Die eingebaute Funktion `filter(func, iterable)` behält nur die Elemente, für die `func` einen wahren Wert zurückgibt. Wie `map` gibt sie ein träges Objekt zurück, das in eine Liste umgewandelt werden muss:
```python
numbers = [4, -2, 7, 0]
print(list(filter(lambda n: n > 0, numbers)))  # [4, 7]
```
Die an `filter` übergebene Funktion heißt **Prädikat**: Sie nimmt ein Element entgegen und beantwortet eine Ja/Nein-Frage dazu. Übergibst du stattdessen `None`, behält `filter` die Elemente, die von sich aus wahr sind, und verwirft `0`, `""` und `None`.

# --instructions--

Schreibe eine Funktion `positives`, die eine Liste von Zahlen entgegennimmt und mit `filter` eine neue Liste zurückgibt, die nur die Zahlen größer als null enthält

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `filter` verwenden

```json
{
  "regex": "filter\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`positives([3, -1, 0, 5])` muss `[3, 5]` zurückgeben

```python
    def test_1(self):
        self.assertEqual(positives([3, -1, 0, 5]), [3, 5], "--err-t1--")
```

Das Ergebnis muss eine Liste sein

```python
    def test_2(self):
        self.assertIsInstance(positives([1, 2]), list, "--err-t2--")
        self.assertEqual(positives([1, 2]), [1, 2], "--err-t2--")
```

`positives([-4, 0])` muss eine leere Liste zurückgeben

```python
    def test_3(self):
        self.assertEqual(positives([-4, 0]), [], "--err-t3--")
        self.assertEqual(positives([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def positives(numbers):
    return list(filter(lambda n: n > 0, numbers))
```
