---
language: python
exerciseType: 1
---

# --description--

`reduce(func, iterable, initial)` aus dem Modul `functools` faltet eine Sequenz zu einem **einzelnen Wert** zusammen. Es ruft `func` mit dem bisherigen Ergebnis und dem nächsten Element auf, beginnend mit `initial`:
```python
from functools import reduce

total = reduce(lambda acc, n: acc + n, [1, 2, 3], 0)
print(total)  # 6
```
Die Schritte sind `0 + 1`, dann `1 + 2`, dann `3 + 3`. Lässt du `initial` weg, wird das erste Element als Startwert verwendet, aber dann wirft eine leere Sequenz einen `TypeError`, gib also einen Anfangswert an, wann immer die Sequenz leer sein kann.

# --instructions--

Schreibe eine Funktion `product`, die eine Liste von Zahlen entgegennimmt und mit `reduce` alle miteinander multipliziert zurückgibt; das Produkt einer leeren Liste muss `1` sein

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `reduce` verwenden

```json
{
  "regex": "reduce\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`product([2, 3, 4])` muss `24` zurückgeben

```python
    def test_1(self):
        self.assertEqual(product([2, 3, 4]), 24, "--err-t1--")
```

`product([5])` muss `5` zurückgeben

```python
    def test_2(self):
        self.assertEqual(product([5]), 5, "--err-t2--")
```

`product([])` muss `1` zurückgeben

```python
    def test_3(self):
        self.assertEqual(product([]), 1, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import reduce

def product(numbers):
    return reduce(lambda acc, n: acc * n, numbers, 1)
```
