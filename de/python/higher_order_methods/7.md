---
language: python
exerciseType: 1
---

# --description--

Eine Funktion kann auch eine Funktion **zurückgeben**. Definiere eine innere Funktion mit `def` und gib sie zurück, ohne sie aufzurufen:
```python
def make_greeter(greeting):
    def greet(name):
        return greeting + ", " + name
    return greet

hello = make_greeter("Hello")
print(hello("Ada"))  # Hello, Ada
```
Die innere Funktion verwendet `greeting` auch dann weiter, wenn `make_greeter` bereits fertig ist: Sie **merkt sich** die Variablen des Gültigkeitsbereichs, in dem sie erstellt wurde. So eine Funktion heißt **Closure**. Jeder Aufruf von `make_greeter` erstellt eine neue, unabhängige Closure mit ihrem eigenen `greeting`.

# --instructions--

Schreibe eine Funktion `make_multiplier`, die eine Zahl `factor` entgegennimmt und eine Funktion zurückgibt, die eine Zahl entgegennimmt und sie mit `factor` multipliziert zurückgibt

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`make_multiplier(2)` muss eine Funktion zurückgeben

```python
    def test_1(self):
        self.assertTrue(callable(make_multiplier(2)), "--err-t1--")
```

`make_multiplier(2)(5)` muss `10` zurückgeben

```python
    def test_2(self):
        double = make_multiplier(2)
        self.assertEqual(double(5), 10, "--err-t2--")
        self.assertEqual(double(0), 0, "--err-t2--")
```

Jeder Multiplikator muss sich seinen eigenen Faktor merken

```python
    def test_3(self):
        double = make_multiplier(2)
        triple = make_multiplier(3)
        self.assertEqual(triple(4), 12, "--err-t3--")
        self.assertEqual(double(4), 8, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def make_multiplier(factor):
    def multiply(number):
        return number * factor
    return multiply
```
