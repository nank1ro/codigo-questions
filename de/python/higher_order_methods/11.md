---
language: python
exerciseType: 1
---

# --description--

`partial` ist praktisch bei eingebauten Funktionen mit Optionen. `int(text, base=16)` zerlegt eine hexadezimale Zeichenkette; legst du die Basis fest, entsteht ein Konverter mit einem Argument, der zu `map` passt:
```python
from functools import partial

hex_to_int = partial(int, base=16)
print(hex_to_int("ff"))                      # 255
print(list(map(hex_to_int, ["a", "10"])))    # [10, 16]
```
Das partial-Objekt merkt sich, was es umhüllt: `hex_to_int.func` ist `int` und `hex_to_int.keywords` ist `{'base': 16}`.

# --instructions--

Definiere zwei Funktionen mit `partial`: `hex_to_int`, das eine hexadezimale Zeichenkette in einen `int` umwandelt, und `binary_to_int`, das eine binäre Zeichenkette in einen `int` umwandelt

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`hex_to_int("ff")` muss `255` zurückgeben

```python
    def test_1(self):
        self.assertEqual(hex_to_int("ff"), 255, "--err-t1--")
        self.assertEqual(hex_to_int("10"), 16, "--err-t1--")
```

`binary_to_int("1010")` muss `10` zurückgeben

```python
    def test_2(self):
        self.assertEqual(binary_to_int("1010"), 10, "--err-t2--")
        self.assertEqual(binary_to_int("11"), 3, "--err-t2--")
```

Beide müssen partial-Objekte sein, die `int` umhüllen

```python
    def test_3(self):
        self.assertIs(hex_to_int.func, int, "--err-t3--")
        self.assertIs(binary_to_int.func, int, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import partial

hex_to_int = partial(int, base=16)
binary_to_int = partial(int, base=2)
```
