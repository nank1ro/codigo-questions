---
language: python
exerciseType: 1
---

# --description--

Eine Funktion als Argument zu übergeben lässt den Aufrufer entscheiden, **was** getan wird, während die Funktion höherer Ordnung entscheidet, **wie oft** oder **auf was**. Der Funktionsparameter kann so oft wie nötig aufgerufen werden, und sein Ergebnis kann ihm wieder zugeführt werden:
```python
def repeat(func, value, times):
    for _ in range(times):
        value = func(value)
    return value

def add_one(n):
    return n + 1

print(repeat(add_one, 0, 3))  # 3
```
Jedes Callable funktioniert: eine `def`-Funktion, eine eingebaute Funktion wie `len` oder eine lambda.

# --instructions--

Schreibe eine Funktion `apply_twice`, die eine Funktion `func` und einen `value` entgegennimmt und das Ergebnis davon zurückgibt, `func` zweimal hintereinander auf `value` anzuwenden

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`apply_twice(lambda x: x * 2, 3)` muss `12` zurückgeben

```python
    def test_1(self):
        self.assertEqual(apply_twice(lambda x: x * 2, 3), 12, "--err-t1--")
```

`apply_twice(lambda s: s + "!", "hi")` muss `"hi!!"` zurückgeben

```python
    def test_2(self):
        self.assertEqual(apply_twice(lambda s: s + "!", "hi"), "hi!!", "--err-t2--")
```

Der zweite Aufruf muss das Ergebnis des ersten erhalten

```python
    def test_3(self):
        seen = []
        def record(x):
            seen.append(x)
            return x + 10
        self.assertEqual(apply_twice(record, 0), 20, "--err-t3--")
        self.assertEqual(seen, [0, 10], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def apply_twice(func, value):
    return func(func(value))
```
