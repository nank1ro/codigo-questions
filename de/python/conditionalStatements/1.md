---
language: python
exerciseType: 2
---

# --description--

Entscheidungsfindung ist erforderlich, wenn wir Code nur dann ausführen wollen, wenn eine bestimmte Bedingung erfüllt ist.
Nehmen wir an, wir wollen nur bei schönem Wetter draußen spielen.
In der Programmierung können wir eine boolesche Variable `nice_weather` speichern und die Aktion des Draußenspielens ausführen, `if` diese Variable `True` ist, wie:
```python
nice_weather = True
if (nice_weather):
    # draußen spielen
```

# --instructions--

Erstellen Sie eine `if`-Anweisung, die überprüft, ob die `time` kleiner als `12` ist, und geben Sie dann im Codeblock die Zeichenkette `"It's morning"` aus

# --seed--

```python
time = 8
[/] (time < 12):
    print("It's morning")
```

# --answers--

- if
- ==
- =
- else
- ?

# --solutions--

```python
time = 8
if (time < 12):
    print("It's morning")
```

# --output--

It's morning
