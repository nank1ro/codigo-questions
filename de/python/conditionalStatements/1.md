---
language: python
exerciseType: 2
---

# --description--

Entscheidungen sind erforderlich, wenn wir Code nur dann ausführen möchten, wenn eine bestimmte Bedingung erfüllt ist.
Nehmen wir an, wir möchten nur draußen spielen, wenn das Wetter schön ist.
In der Programmierung können wir eine boolesche Variable `nice_weather` speichern und die Aktion des Spielens draußen ausführen, wenn diese Variable `True` ist, wie hier:
```python
nice_weather = True
if (nice_weather):
    # play outside
```

# --instructions--

Erstelle eine `if`-Anweisung, die überprüft, ob die `time` kleiner als `12` ist, und gib dann im Code-Block die Zeichenkette `"It's morning"` aus

# --seed--

```python
time = 8
[/] (time < 12):
    print("It's morning")
```

# --answers--

- if
- ==
- =
- else
- ?

# --solutions--

```python
time = 8
if (time < 12):
    print("It's morning")
```

# --output--

It's morning
