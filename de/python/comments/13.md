---
language: python
exerciseType: 1
---

# --description--

Manche Kommentare folgen einer Konvention, die Editoren verstehen. Die häufigsten **Marker** sind:
- `# TODO: ...` markiert etwas, das noch geschrieben werden muss
- `# FIXME: ...` markiert Code, von dem bekannt ist, dass er falsch ist und korrigiert werden muss

```python
limit = 10
# TODO: das Limit aus den Einstellungen lesen
```
Für Python sind sie ganz normale Kommentare; Editoren sammeln sie in einem eigenen Panel ein, damit offene Arbeit leicht zu finden ist. Ein `TODO` steht meist neben einem Platzhalter, der das Programm laufen lässt, bis der echte Code geschrieben ist.

Wenn du die Arbeit abschließt, ersetze den Platzhalter und entferne den Marker in derselben Änderung, damit der Kommentar nie über den Zustand des Codes lügt.

# --instructions--

Vervollständige die Arbeit, die der `TODO`-Kommentar in `is_even` beschreibt, und entferne danach den Kommentar, da die Arbeit erledigt ist

# --seed--

```python
def is_even(n):
    # TODO: True zurückgeben, wenn n gerade ist, sonst False
    return False
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Der `TODO`-Kommentar muss entfernt sein

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`is_even(4)` muss `True` zurückgeben

```python
    def test_1(self):
        self.assertEqual(is_even(4), True, "--err-t1--")
```

`is_even(7)` muss `False` zurückgeben

```python
    def test_2(self):
        self.assertEqual(is_even(7), False, "--err-t2--")
```

`is_even(0)` muss `True` zurückgeben

```python
    def test_3(self):
        self.assertEqual(is_even(0), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_even(n):
    return n % 2 == 0
```
