---
language: python
exerciseType: 1
---

# --description--

Ein Kommentar über einer Funktion, der sagt, was die Funktion tut, ist am falschen Ort. Der Docstring ist der Platz dafür: er hängt an der Funktion, `help()` findet ihn und Editoren zeigen ihn an, während ein `#`-Kommentar über dem `def` für alle unsichtbar bleibt.

```python
# addiert a und b
def add(a, b):
    return a + b
```
Verschiebt man denselben Satz eine Zeile nach unten, zwischen dreifache Anführungszeichen, wird daraus echte Dokumentation:
```python
def add(a, b):
    """Returns the sum of a and b."""
    return a + b
```

# --instructions--

Entferne die beiden `#`-Kommentare über `subtract` und dokumentiere die Funktion stattdessen mit einem Docstring

# --seed--

```python
# subtract
# this function subtracts b from a
def subtract(a, b):
    return a - b
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Die `#`-Kommentare über `subtract` müssen entfernt sein

```json
{
  "regex": "#\\s*this function subtracts",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

Der `# subtract`-Kommentar muss ebenfalls entfernt sein

```json
{
  "regex": "#\\s*subtract",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`subtract` muss einen Docstring haben

```python
    def test_1(self):
        self.assertIsNotNone(subtract.__doc__, "--err-t1--")
```

`subtract(5, 3)` muss `2` zurückgeben

```python
    def test_2(self):
        self.assertEqual(subtract(5, 3), 2, "--err-t2--")
```

`subtract(0, 4)` muss `-4` zurückgeben

```python
    def test_3(self):
        self.assertEqual(subtract(0, 4), -4, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def subtract(a, b):
    """Returns b subtracted from a."""
    return a - b
```
