---
language: python
exerciseType: 1
---

# --description--

Ein Kommentar braucht keine eigene Zeile: er kann dem Code in derselben Zeile folgen. Das ist ein **Inline-Kommentar**, ein guter Platz für eine kurze Notiz zu genau dieser Anweisung:
```python
retries = 3  # nach drei Versuchen aufgeben
```
Alles von `#` bis zum Ende der Zeile wird ignoriert, während der Code davor wie üblich läuft.

Der Styleguide von Python, **PEP 8**, verlangt hier etwas Abstand: mindestens **zwei Leerzeichen** zwischen dem Code und dem `#` und **ein Leerzeichen** nach dem `#`. Ein Kommentar in eigener Zeile braucht nur das Leerzeichen nach dem `#`.

# --instructions--

Füge hinter `seats = 4` einen Inline-Kommentar hinzu, der den Wert erklärt, zum Beispiel `# seats per table`. Lasse zwei Leerzeichen vor dem `#`

# --seed--

```python
seats = 4
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Die Zeile `seats = 4` muss mit einem `#`-Kommentar enden, mit zwei Leerzeichen vor dem `#` und einem danach

```json
{
  "regex": "seats = 4[ \\t]{2,}#[ \\t]\\S",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`seats` muss weiterhin gleich `4` sein

```python
    def test_1(self):
        self.assertEqual(seats, 4, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
seats = 4  # Plätze pro Tisch
```
