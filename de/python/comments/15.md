---
language: python
exerciseType: 1
---

# --description--

Python sucht nach `#` nur im Code, niemals innerhalb einer **Zeichenkette**. Zwischen Anführungszeichen ist `#` ein ganz normales Zeichen:
```python
print("black is #000000")  # a hex colour
```
Das erste `#` ist Teil des Texts, das zweite startet einen echten Kommentar. Dasselbe gilt für `"""` innerhalb eines `#`-Kommentars: dort sind es nur drei Anführungszeichen, und es startet nichts.

# --instructions--

Schreibe eine Funktion `label(name)`, die `name` mit einem `#` davor zurückgibt, sodass `label("python")` `"#python"` zurückgibt

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`label("python")` muss `"#python"` zurückgeben

```python
    def test_1(self):
        self.assertEqual(label("python"), "#python", "--err-t1--")
```

`label("ai")` muss `"#ai"` zurückgeben

```python
    def test_2(self):
        self.assertEqual(label("ai"), "#ai", "--err-t2--")
```

`label("")` muss `"#"` zurückgeben

```python
    def test_3(self):
        self.assertEqual(label(""), "#", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def label(name):
    """Returns name with a # in front of it."""
    return "#" + name
```
