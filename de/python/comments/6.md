---
language: python
exerciseType: 1
---

# --description--

Ist eine Zeichenkette die **erste Anweisung** innerhalb einer Funktion, behandelt Python sie als die Dokumentation dieser Funktion. Sie heißt **Docstring**:
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"
```
Konventionsgemäß wird ein Docstring in dreifachen doppelten Anführungszeichen, `"""`, geschrieben, selbst wenn er in eine Zeile passt, damit er später wachsen kann, ohne die Anführungszeichen zu ändern.

Schreibe die Zusammenfassung in der dritten Person, als würdest du die Funktion beschreiben: "Returns...", "Adds...", "Checks...". Der Docstring muss vor jeder anderen Anweisung im Körper stehen, sonst ist er nur eine gewöhnliche Zeichenkette.

# --instructions--

Schreibe eine Funktion `double(n)`, die `n` mal zwei zurückgibt, und gib ihr einen einzeiligen Docstring

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`double` muss einen Docstring als erste Anweisung haben

```python
    def test_1(self):
        self.assertIsNotNone(double.__doc__, "--err-t1--")
```

`double(21)` muss `42` zurückgeben

```python
    def test_2(self):
        self.assertEqual(double(21), 42, "--err-t2--")
```

`double(-3)` muss `-6` zurückgeben und `double(0)` muss `0` zurückgeben

```python
    def test_3(self):
        self.assertEqual(double(-3), -6, "--err-t3--")
        self.assertEqual(double(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def double(n):
    """Returns n multiplied by two."""
    return n * 2
```
