---
language: python
exerciseType: 1
---

# --description--

Reicht eine Zeile nicht, wächst ein Docstring zu einem festen Layout heran, beschrieben in **PEP 257**: eine einzeilige Zusammenfassung, eine Leerzeile, dann die Details, und das schließende `"""` in einer eigenen Zeile.
```python
def to_seconds(minutes):
    """Returns the number of seconds in the given minutes.

    minutes is a whole number and is never negative.
    """
    return minutes * 60
```
Die Leerzeile ist wichtig: Werkzeuge zeigen die erste Zeile für sich allein an, als kurze Beschreibung, und heben sich den Rest für diejenigen auf, die mehr lesen wollen.

# --instructions--

Gib `area` einen Docstring aus einer einzeiligen Zusammenfassung, einer Leerzeile, einer Detailzeile und dem schließenden `"""` in einer eigenen Zeile

# --seed--

```python
def area(width, height):
    return width * height
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`area` muss einen Docstring haben

```python
    def test_1(self):
        self.assertIsNotNone(area.__doc__, "--err-t1--")
```

Der Docstring muss mit einer Zusammenfassungszeile beginnen, gefolgt von einer Leerzeile und weiterem Text

```python
    def test_2(self):
        lines = area.__doc__.split("\n")
        self.assertTrue(len(lines) >= 3, "--err-t2--")
        self.assertNotEqual(lines[0].strip(), "", "--err-t2--")
        self.assertEqual(lines[1].strip(), "", "--err-t2--")
        self.assertNotEqual(lines[2].strip(), "", "--err-t2--")
        self.assertEqual(lines[-1].strip(), "", "--err-t2--")
```

`area(3, 4)` muss `12` zurückgeben und `area(0, 5)` muss `0` zurückgeben

```python
    def test_3(self):
        self.assertEqual(area(3, 4), 12, "--err-t3--")
        self.assertEqual(area(0, 5), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def area(width, height):
    """Returns the area of a rectangle.

    Both sides are whole numbers of centimetres.
    """
    return width * height
```
