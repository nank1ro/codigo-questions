---
language: python
exerciseType: 1
---

# --description--

`find(sub)` gibt den **Index** des ersten Vorkommens von `sub` zurück, oder `-1`, wenn es nicht gefunden wird:
```python
text = "hello world"
print(text.find("o"))  # 4
print(text.find("z"))  # -1
```
`count(sub)` gibt zurück, **wie oft** `sub` vorkommt:
```python
print(text.count("o"))   # 2
print(text.count("ll"))  # 1
```

# --instructions--

Speichere in `position` den Index des ersten `o` in `text`, in `missing` das Ergebnis der Suche nach `z`, und in `total` wie viele `o` der Text enthält

# --seed--

```python
text = "hello world"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `find()` verwenden

```json
{
  "regex": "\\.find\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst `count()` verwenden

```json
{
  "regex": "\\.count\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`position` muss gleich `4` sein

```python
    def test_1(self):
        self.assertEqual(position, 4, "--err-t1--")
```

`missing` muss gleich `-1` sein

```python
    def test_2(self):
        self.assertEqual(missing, -1, "--err-t2--")
```

`total` muss gleich `2` sein

```python
    def test_3(self):
        self.assertEqual(total, 2, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
text = "hello world"
position = text.find("o")
missing = text.find("z")
total = text.count("o")
```
