---
language: python
exerciseType: 4
---

# --description--

`replace(old, new)` gibt eine Kopie der Zeichenkette zurück, in der **jedes** Vorkommen von `old` durch `new` ersetzt wird:
```python
text = "a-b-c"
print(text.replace("-", "+"))  # a+b+c
```
Da Zeichenketten unveränderlich sind, denk daran, das Ergebnis zu speichern, wenn du es behalten willst.

# --instructions--

Sortiere die Elemente, um jedes `cats` in `msg` durch `dogs` zu ersetzen und das Ergebnis auszugeben

# --answers--

```python
msg = "I like cats, cats are great"
```

```python
new_msg = msg.replace("cats", "dogs")
```

```python
print(new_msg)
```

# --solutions--

```python
msg = "I like cats, cats are great"
new_msg = msg.replace("cats", "dogs")
print(new_msg)
```

# --output--

I like dogs, dogs are great
