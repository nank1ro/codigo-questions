---
language: python
exerciseType: 1
---

# --description--

Ein **Slug** ist eine URL-freundliche Version eines Titels: kleingeschrieben, ohne Leerzeichen darum, und Wörter durch Bindestriche getrennt, wie `hello-world`.
Einen zu erstellen ist nur eine Verkettung der Methoden, die du bereits gelernt hast: `strip()`, `lower()` und `replace()`.

# --instructions--

Schreibe eine Funktion `slugify(title)`, die `title` ohne Leerzeichen darum, in Kleinbuchstaben und mit jedem Leerzeichen durch `-` ersetzt zurückgibt

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `replace()` verwenden

```json
{
  "regex": "\\.replace\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`slugify("Hello World")` muss `"hello-world"` zurückgeben

```python
    def test_1(self):
        self.assertEqual(slugify("Hello World"), "hello-world", "--err-t1--")
```

`slugify("  Learn Python Strings  ")` muss `"learn-python-strings"` zurückgeben

```python
    def test_2(self):
        self.assertEqual(slugify("  Learn Python Strings  "), "learn-python-strings", "--err-t2--")
```

`slugify("codigo")` muss `"codigo"` zurückgeben

```python
    def test_3(self):
        self.assertEqual(slugify("codigo"), "codigo", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def slugify(title):
    return title.strip().lower().replace(" ", "-")
```
