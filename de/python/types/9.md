---
language: python
exerciseType: 1
---

# --description--

`None` ist ein eigener Wert, der "nichts hier" bedeutet. Er ist das, was eine Funktion zurückgibt, wenn sie keine `return`-Anweisung hat, und er ist ein gängiger Platzhalter für einen Wert, der noch nicht bekannt ist.
Da es nur ein einziges `None` gibt, prüfe mit `is` darauf, nicht mit `==`:
```python
result = None
if result is None:
    print("no result yet")
```
Jeder Typ hat ein Attribut `__name__` mit seinem Namen als Zeichenkette, was für Meldungen praktisch ist:
```python
print(type(3).__name__)     # int
print(type(None).__name__)  # NoneType
```

# --instructions--

Schreibe eine Funktion `describe`, die einen Wert entgegennimmt und die Zeichenkette `"nothing"` zurückgibt, wenn der Wert `None` ist, andernfalls den Namen seines Typs

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst mit `is None` vergleichen

```json
{
  "regex": "is\\s+None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst `__name__` verwenden

```json
{
  "regex": "__name__",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe(None)` muss `"nothing"` zurückgeben

```python
    def test_1(self):
        self.assertEqual(describe(None), "nothing", "--err-t1--")
```

`describe(5)` muss `"int"` zurückgeben und `describe(2.0)` muss `"float"` zurückgeben

```python
    def test_2(self):
        self.assertEqual(describe(5), "int", "--err-t2--")
        self.assertEqual(describe(2.0), "float", "--err-t2--")
```

`describe("a")` muss `"str"` zurückgeben und `describe(True)` muss `"bool"` zurückgeben

```python
    def test_3(self):
        self.assertEqual(describe("a"), "str", "--err-t3--")
        self.assertEqual(describe(True), "bool", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe(value):
    if value is None:
        return "nothing"
    return type(value).__name__
```
