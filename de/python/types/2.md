---
language: python
exerciseType: 1
---

# --description--

`type()` gibt die Klasse eines Werts zurück, daher kannst du sie mit einem Klassennamen vergleichen, indem du `is` verwendest:
```python
age = 30
print(type(age) is int)  # True
```
In den meisten Fällen willst du aber nur wissen, **ob** ein Wert einen bestimmten Typ hat. Das ist die Aufgabe von `isinstance(value, cls)`, das `True` oder `False` zurückgibt:
```python
print(isinstance(age, int))    # True
print(isinstance(age, str))    # False
```
Das zweite Argument kann auch ein **Tupel** von Klassen sein: Das Ergebnis ist `True`, wenn der Wert zu einem davon gehört:
```python
print(isinstance(2.5, (int, float)))  # True
```

# --instructions--

Speichere in `age_type` den Typ von `age`, mit `type()`, in `is_text`, ob `name` ein `str` ist, und in `is_number`, ob `price` ein `int` oder ein `float` ist, mit `isinstance()`

# --seed--

```python
age = 30
price = 9.99
name = "Ada"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `type()` verwenden

```json
{
  "regex": "type\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst `isinstance()` verwenden

```json
{
  "regex": "isinstance\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`age_type` muss `int` sein

```python
    def test_1(self):
        self.assertIs(age_type, int, "--err-t1--")
```

`is_text` muss `True` sein

```python
    def test_2(self):
        self.assertIs(is_text, True, "--err-t2--")
```

`is_number` muss `True` sein

```python
    def test_3(self):
        self.assertIs(is_number, True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
age = 30
price = 9.99
name = "Ada"

age_type = type(age)
is_text = isinstance(name, str)
is_number = isinstance(price, (int, float))
```
