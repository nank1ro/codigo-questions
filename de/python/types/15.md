---
language: python
exerciseType: 2
---

# --description--

Umwandlungen können kombiniert werden. `int("3.7")` schlägt fehl, aber `float("3.7")` funktioniert, und `int()` eines `float` verwirft den Dezimalteil:
```python
number = float("3.7")   # 3.7
print(int(number))      # 3
```
`bool()` folgt den Wahrheitsregeln: `bool("")` ist `False`, und beachte, dass `bool("False")` `True` ist, weil es eine nicht-leere Zeichenkette ist.

# --instructions--

Vervollständige den Code, um `text` in eine Zahl umzuwandeln und ihr Doppeltes auszugeben, danach die in ein `int` umgewandelte Zahl und den booleschen Wert einer leeren Zeichenkette auszugeben, sodass `7.4`, `3` und `False` ausgegeben werden

# --seed--

```python
text = "3.7"
number = [/](text)
print(number * 2)
print([/](number))
print([/](""))
```

# --answers--

- float
- int
- bool
- str
- len

# --solutions--

```python
text = "3.7"
number = float(text)
print(number * 2)
print(int(number))
print(bool(""))
```

# --output--

7.4
3
False
