---
language: python
exerciseType: 1
---

# --description--

Du kennst die arithmetischen Operatoren bereits. Was hier zählt, ist der **Typ des Ergebnisses**.
Die Kombination eines `int` mit einem `float` ergibt einen `float`, auch wenn der Dezimalteil null ist:
```python
print(3 + 0.5)   # 3.5
print(2 * 1.0)   # 2.0
```
Die **echte Division** `/` gibt immer einen `float` zurück, selbst wenn die Zahlen exakt teilbar sind:
```python
print(10 / 5)    # 2.0
print(7 / 2)     # 3.5
```
Die **Ganzzahldivision** `//` rundet das Ergebnis auf die nächstkleinere ganze Zahl ab (daher ist `-7 // 2` gleich `-4`) und gibt einen `int` zurück, wenn beide Operanden Ganzzahlen sind. Zusammen mit dem Rest `%` zerlegt sie eine Menge in ganze Teile:
```python
print(7 // 2)    # 3
print(7 % 2)     # 1
```

# --instructions--

Speichere in `each` das Ergebnis der Division von `total` durch `people` mit `/`, in `whole` dieselbe Division mit `//` und in `rest` den Rest

# --seed--

```python
total = 17
people = 5
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `//` verwenden

```json
{
  "regex": "//",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`each` muss die Gleitkommazahl `3.4` sein

```python
    def test_1(self):
        self.assertIsInstance(each, float, "--err-t1--")
        self.assertEqual(each, 3.4, "--err-t1--")
```

`whole` muss die Ganzzahl `3` sein

```python
    def test_2(self):
        self.assertIsInstance(whole, int, "--err-t2--")
        self.assertEqual(whole, 3, "--err-t2--")
```

`rest` muss `2` sein

```python
    def test_3(self):
        self.assertEqual(rest, 2, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
total = 17
people = 5

each = total / people
whole = total // people
rest = total % people
```
