---
language: python
exerciseType: 2
---

# --description--

`bool` ist eine **Unterklasse** von `int`: `True` verhält sich wie `1` und `False` wie `0`, wo immer eine Zahl erwartet wird:
```python
print(True + 1)     # 2
print(True == 1)    # True
print(False * 10)   # 0
```
`sum()` addiert die Elemente einer Liste. Wenn man also eine Liste von booleschen Werten summiert, **zählt** man, wie viele davon `True` sind:
```python
answers = [True, False, True]
print(sum(answers))  # 2
```
Wegen der Unterklasse-Beziehung gibt `isinstance(True, int)` `True` zurück, während `type(True)` weiterhin `bool` ist.

# --instructions--

Vervollständige den Code so, dass er die richtigen Antworten zählt, zu `True` `1` addiert und prüft, ob `True` eine Instanz von `int` ist, und dabei `3`, `2` und `True` ausgibt

# --seed--

```python
answers = [True, False, True, True]
correct = [/](answers)
print(correct)
print(True [/] 1)
print(isinstance(True, [/]))
```

# --answers--

- sum
- +
- int
- len
- ==
- str

# --solutions--

```python
answers = [True, False, True, True]
correct = sum(answers)
print(correct)
print(True + 1)
print(isinstance(True, int))
```

# --output--

3
2
True
