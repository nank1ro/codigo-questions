---
language: python
exerciseType: 1
---

# --description--

Text von außen ist immer eine Zeichenkette (`str`), und dein Programm muss herausfinden, welchen Typ sie wirklich enthält.
Ein gängiger Ansatz ist, zuerst die **strengste** Umwandlung zu versuchen und zur nächsten zurückzufallen, wenn sie einen `ValueError` auslöst:
```python
try:
    value = int(text)
except ValueError:
    value = float(text)
```
Wenn du ein zweites `try` im `except`-Block verschachtelst, kannst du ein weiteres Mal zurückfallen, um zum Beispiel den Text unverändert zu behalten.

# --instructions--

Schreibe eine Funktion `parse_value`, die eine Zeichenkette entgegennimmt und sie als `int` zurückgibt, wenn sie eine ganze Zahl ist, als `float`, wenn sie eine Dezimalzahl ist, und sie andernfalls unverändert zurückgibt

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `ValueError` mit `except` abfangen

```json
{
  "regex": "except\\s+ValueError",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_value("42")` muss den `int` `42` zurückgeben

```python
    def test_1(self):
        self.assertEqual(parse_value("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_value("42"), int, "--err-t1--")
        self.assertEqual(parse_value("-8"), -8, "--err-t1--")
```

`parse_value("3.5")` muss den `float` `3.5` zurückgeben

```python
    def test_2(self):
        self.assertEqual(parse_value("3.5"), 3.5, "--err-t2--")
        self.assertIsInstance(parse_value("3.5"), float, "--err-t2--")
```

`parse_value("abc")` muss `"abc"` zurückgeben

```python
    def test_3(self):
        self.assertEqual(parse_value("abc"), "abc", "--err-t3--")
        self.assertEqual(parse_value(""), "", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_value(text):
    try:
        return int(text)
    except ValueError:
        try:
            return float(text)
        except ValueError:
            return text
```
