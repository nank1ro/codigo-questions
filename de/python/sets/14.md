---
language: python
exerciseType: 1
---

# --description--

Eine **Set Comprehension** erstellt ein Set in einem einzigen Ausdruck, mit derselben Syntax wie eine List Comprehension, aber mit geschweiften Klammern:
```python
nums = [1, 2, 2, 3]
squares = {n * n for n in nums}
print(squares)  # {1, 4, 9}
```
Ein optionales `if` filtert die Elemente, und vom Ausdruck erzeugte Duplikate werden automatisch verworfen:
```python
evens = {n for n in range(10) if n % 2 == 0}
print(evens)  # {0, 2, 4, 6, 8}
```

# --instructions--

Speichere in `lowered` das Set der Wörter von `words`, umgewandelt in Kleinbuchstaben, mit einer Set Comprehension, und dann in `long_words` das Set der Elemente von `lowered` mit mehr als 5 Zeichen, mit einer Set Comprehension mit `if`

# --seed--

```python
words = ["Apple", "banana", "APPLE", "Cherry", "banana"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst eine Set Comprehension verwenden

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`lowered` muss gleich `{'apple', 'banana', 'cherry'}` sein

```python
    def test_1(self):
        self.assertEqual(lowered, {"apple", "banana", "cherry"}, "--err-t1--")
```

`long_words` muss gleich `{'banana', 'cherry'}` sein

```python
    def test_2(self):
        self.assertEqual(long_words, {"banana", "cherry"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
words = ["Apple", "banana", "APPLE", "Cherry", "banana"]
lowered = {w.lower() for w in words}
long_words = {w for w in lowered if len(w) > 5}
```
