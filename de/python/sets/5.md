---
language: python
exerciseType: 1
---

# --description--

`pop()` entfernt **ein beliebiges Element** aus dem Set und gibt es zurück.
Da ein Set keine Reihenfolge hat, kannst du nicht wählen, welches Element entfernt wird; der Aufruf von `pop()` bei einem leeren Set löst einen `KeyError` aus:
```python
tickets = {101, 102, 103}
picked = tickets.pop()
print(len(tickets))  # 2
```
`clear()` entfernt **alle** Elemente und hinterlässt ein leeres Set:
```python
tickets.clear()
print(len(tickets))  # 0
```

# --instructions--

Entferne mit `pop()` ein Element aus `tickets` und speichere es in der Variable `picked`, dann leere das Set `drafts` mit `clear()`

# --seed--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `pop()` verwenden

```json
{
  "regex": "\\.pop\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst `clear()` verwenden

```json
{
  "regex": "\\.clear\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`picked` muss eines der ursprünglichen Tickets sein

```python
    def test_1(self):
        self.assertIn(picked, {101, 102, 103}, "--err-t1--")
```

`tickets` muss 2 Elemente enthalten und darf `picked` nicht mehr enthalten

```python
    def test_2(self):
        self.assertEqual(len(tickets), 2, "--err-t2--")
        self.assertNotIn(picked, tickets, "--err-t2--")
```

`drafts` muss leer sein

```python
    def test_3(self):
        self.assertEqual(drafts, set(), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
picked = tickets.pop()
drafts.clear()
```
