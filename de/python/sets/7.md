---
language: python
exerciseType: 1
---

# --description--

Die **Vereinigung** zweier Sets ist ein neues Set mit den Elementen **beider**, ohne Duplikate.
Verwende den `|`-Operator oder die Methode `union()`:
```python
a = {1, 2}
b = {2, 3}
print(a | b)        # {1, 2, 3}
print(a.union(b))   # {1, 2, 3}
```
Weder `a` noch `b` wird verändert: Mengenoperationen geben immer ein neues Set zurück.

# --instructions--

Speichere in `skills` die Vereinigung von `frontend` und `backend` mit dem `|`-Operator, und speichere in `total`, wie viele unterschiedliche Skills es enthält

# --seed--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst den `|`-Operator verwenden

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`skills` muss gleich `{'html', 'css', 'js', 'python', 'sql'}` sein

```python
    def test_1(self):
        self.assertEqual(skills, {"html", "css", "js", "python", "sql"}, "--err-t1--")
```

`total` muss gleich `5` sein

```python
    def test_2(self):
        self.assertEqual(total, 5, "--err-t2--")
```

`frontend` darf nicht verändert werden

```python
    def test_3(self):
        self.assertEqual(frontend, {"html", "css", "js"}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
skills = frontend | backend
total = len(skills)
```
