---
language: python
exerciseType: 1
---

# --description--

Die **Differenz** `a - b` enthält die Elemente von `a`, die **nicht** in `b` sind.
Die Reihenfolge spielt eine Rolle: `a - b` und `b - a` sind meist unterschiedlich:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a - b)  # {1}
print(b - a)  # {4}
```
Die **symmetrische Differenz** `a ^ b` enthält die Elemente, die in **genau einem** der beiden Sets sind:
```python
print(a ^ b)  # {1, 4}
```
Die Methodenformen sind `difference()` und `symmetric_difference()`.

# --instructions--

Speichere in `only_ann` die Hobbys von `ann`, die `bob` nicht hat, mit `-`, und in `not_shared` die Hobbys, die nur einer von beiden hat, mit `^`

# --seed--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst den `-`-Operator verwenden

```json
{
  "regex": "\\s-\\s",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst den `^`-Operator verwenden

```json
{
  "regex": "\\^",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`only_ann` muss gleich `{'tennis', 'yoga'}` sein

```python
    def test_1(self):
        self.assertEqual(only_ann, {"tennis", "yoga"}, "--err-t1--")
```

`not_shared` muss gleich `{'tennis', 'yoga', 'swimming'}` sein

```python
    def test_2(self):
        self.assertEqual(not_shared, {"tennis", "yoga", "swimming"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
only_ann = ann - bob
not_shared = ann ^ bob
```
