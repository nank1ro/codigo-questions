---
language: python
exerciseType: 2
---

# --description--

Die **Schnittmenge** zweier Sets enthält nur die Elemente, die in **beiden** vorhanden sind.
Verwende den `&`-Operator oder die Methode `intersection()`:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a & b)               # {2, 3}
print(a.intersection(b))   # {2, 3}
```
Wenn die Sets nichts gemeinsam haben, ist das Ergebnis ein leeres Set.

# --instructions--

Vervollständige den Code, um in `common` die Elemente zu speichern, die sowohl in `a` als auch in `b` vorhanden sind, sodass das Programm `[3, 4]` ausgibt

# --seed--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a [/] b
print(sorted(common))
```

# --answers--

- &
- |
- -
- ^
- and

# --solutions--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a & b
print(sorted(common))
```

# --output--

[3, 4]
