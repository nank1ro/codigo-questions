---
language: python
exerciseType: 1
---

# --description--

Die eingebaute Funktion `set()` erstellt ein Set aus einer beliebigen Sammlung, zum Beispiel einer Liste oder einer Zeichenkette.
Da ein Set jeden Wert nur einmal behält, ist dies die klassische Methode, um **Duplikate zu entfernen**:
```python
nums = [1, 2, 2, 3]
unique = set(nums)
print(len(unique))  # 3
```
Um zu prüfen, ob ein Wert vorhanden ist, verwendest du den `in`-Operator, der `True` oder `False` zurückgibt:
```python
print(2 in unique)  # True
print(9 in unique)  # False
```
Mitgliedschaftsprüfungen bei einem Set sind sehr schnell, selbst bei Tausenden von Elementen.

# --instructions--

Speichere in der Variable `unique` ein aus `nums` mit `set()` erstelltes Set und in `has_three`, ob der Wert `3` mit `in` in `unique` enthalten ist

# --seed--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `set()` verwenden

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst den `in`-Operator verwenden

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`unique` muss gleich `{1, 2, 3, 4}` sein

```python
    def test_1(self):
        self.assertEqual(unique, {1, 2, 3, 4}, "--err-t1--")
```

`has_three` muss `True` sein

```python
    def test_2(self):
        self.assertEqual(has_three, True, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
unique = set(nums)
has_three = 3 in unique
```
