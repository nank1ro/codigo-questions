---
language: python
exerciseType: 1
---

# --description--

Zwei Sets sind **gleich**, wenn sie dieselben Elemente enthalten, unabhängig von der Reihenfolge, in der sie geschrieben wurden:
```python
print({1, 2, 3} == {3, 1, 2})  # True
```
Der Vergleich der Länge einer Sammlung mit der Länge ihres Sets ist eine schnelle Methode, um Duplikate zu erkennen: Wenn das Set **kleiner** ist, kam ein Wert mehr als einmal vor:
```python
emails = ["a@x.com", "b@x.com", "a@x.com"]
print(len(set(emails)) < len(emails))  # True
```
Die Listenmethode `count(value)` sagt dir, wie oft ein Wert vorkommt, was hilft herauszufinden, *welche* Werte doppelt sind.

# --instructions--

Speichere in `has_duplicates`, ob `emails` eine wiederholte Adresse enthält, indem du die Länge der Liste mit der Länge ihres Sets vergleichst, und speichere dann in `duplicates` das Set der Adressen, die mehr als einmal vorkommen, mit einer Set Comprehension und `count()`

# --seed--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

Du musst `set()` verwenden

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Du musst eine Set Comprehension verwenden

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_duplicates` muss `True` sein

```python
    def test_1(self):
        self.assertEqual(has_duplicates, True, "--err-t1--")
```

`duplicates` muss gleich `{'ann@x.com', 'bob@x.com'}` sein

```python
    def test_2(self):
        self.assertEqual(duplicates, {"ann@x.com", "bob@x.com"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
has_duplicates = len(set(emails)) < len(emails)
duplicates = {e for e in emails if emails.count(e) > 1}
```
