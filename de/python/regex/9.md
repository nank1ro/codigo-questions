---
language: python
exerciseType: 1
---

# --description--

Wenn die Position oder die Gruppen jedes Treffers gebraucht werden, ist `re.finditer(pattern, text)` der richtige Aufruf: Es geht den Text durch und liefert für jeden Treffer ein **Match-Objekt**, jeweils eines:
```python
import re

for match in re.finditer(r"\d+", "a1 bb22"):
    print(match.group(), match.start())
# 1 1
# 22 5
```
`finditer` erzeugt einen Iterator, keine Liste, daher kann es in einer `for`-Schleife oder in einer Comprehension verwendet werden. Wo `findall` nur den Text liefert, liefert `finditer` alles, was ein Match-Objekt weiß.

# --instructions--

Schreibe eine Funktion `word_positions(text)`, die eine Liste von Tupeln zurückgibt, eines für jede Buchstabenfolge im Text, mit dem Wort und dem Index, an dem es beginnt

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`word_positions("hi there")` muss `[("hi", 0), ("there", 3)]` zurückgeben

```python
    def test_1(self):
        self.assertEqual(word_positions("hi there"), [("hi", 0), ("there", 3)], "--err-t1--")
```

Ziffern und Symbole dürfen nicht Teil eines Worts sein

```python
    def test_2(self):
        self.assertEqual(word_positions("a1b"), [("a", 0), ("b", 2)], "--err-t2--")
        self.assertEqual(word_positions("Ada, Bob"), [("Ada", 0), ("Bob", 5)], "--err-t2--")
```

Ein Text ohne Buchstaben muss eine leere Liste zurückgeben

```python
    def test_3(self):
        self.assertEqual(word_positions("123 456"), [], "--err-t3--")
        self.assertEqual(word_positions(""), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def word_positions(text):
    return [(match.group(), match.start()) for match in re.finditer(r"[a-zA-Z]+", text)]
```
