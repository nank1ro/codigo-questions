---
language: python
exerciseType: 1
---

# --description--

Ein Match-Objekt trägt mehr als den gefundenen Text. Neben `.group()` bietet es die Position des Treffers innerhalb der ursprünglichen Zeichenkette:
```python
import re

match = re.search(r"\d+", "order 42 shipped")
print(match.group())  # 42
print(match.start())  # 6
print(match.end())    # 8
print(match.span())   # (6, 8)
```
`.start()` ist der Index des ersten gefundenen Zeichens, `.end()` ist der Index direkt nach dem letzten, und `.span()` gibt beide als Tupel zurück. Das bedeutet, `text[match.start():match.end()]` ist immer gleich `match.group()`.

Da `re.search` `None` zurückgeben kann, löst das direkte Lesen von `.group()` einen `AttributeError` aus, wenn nichts gepasst hat; prüfe zuerst das Ergebnis.

# --instructions--

Schreibe eine Funktion `first_number(text)`, die ein Tupel mit der ersten Ziffernfolge, ihrem Startindex und ihrem Endindex zurückgibt, oder `None`, wenn der Text keine Ziffer enthält

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_number("order 42 shipped")` muss `("42", 6, 8)` zurückgeben

```python
    def test_1(self):
        self.assertEqual(first_number("order 42 shipped"), ("42", 6, 8), "--err-t1--")
```

Der Treffer muss bei Index `0` beginnen, wenn der Text mit Ziffern beginnt

```python
    def test_2(self):
        self.assertEqual(first_number("7up"), ("7", 0, 1), "--err-t2--")
        self.assertEqual(first_number("100 items"), ("100", 0, 3), "--err-t2--")
```

`first_number` muss `None` zurückgeben, wenn es keine Ziffer gibt

```python
    def test_3(self):
        self.assertIsNone(first_number("no digits here"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def first_number(text):
    match = re.search(r"\d+", text)
    if match is None:
        return None
    return (match.group(), match.start(), match.end())
```
