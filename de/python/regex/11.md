---
language: python
exerciseType: 1
---

# --description--

`re.sub(pattern, replacement, text)` gibt eine neue Zeichenkette zurück, in der jeder Treffer ersetzt wurde. Zeichenketten sind unveränderlich, daher bleibt der ursprüngliche Text unangetastet:
```python
import re

print(re.sub(r"\d", "#", "call 555 now"))     # call ### now
print(re.sub(r"\s+", " ", "too    many gaps"))  # too many gaps
```

Der Ersatz kann mit `\1`, `\2`, ... auf die erfassenden Gruppen zurückverweisen (oder mit `\g<name>` bei einer benannten Gruppe), was das Umordnen von Text zu einer Einzeiler-Angelegenheit macht. Der Ersatz ist aus demselben Backslash-Grund ebenfalls ein Raw-String:
```python
import re

print(re.sub(r"(\w+), (\w+)", r"\2 \1", "Lovelace, Ada"))  # Ada Lovelace
```
Ein `count`-Argument begrenzt, wie viele Treffer ersetzt werden: `re.sub(r"\d", "#", "1 2 3", count=1)` ergibt `# 2 3`.

# --instructions--

Schreibe eine Funktion `mask_digits(text)`, die jede Ziffer durch `#` ersetzt, und eine Funktion `swap_names(text)`, die ein Paar `surname, name` mithilfe einer Rückreferenz in `name surname` umwandelt

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`mask_digits` muss jede Ziffer ersetzen

```python
    def test_1(self):
        self.assertEqual(mask_digits("call 555 now"), "call ### now", "--err-t1--")
        self.assertEqual(mask_digits("no digits"), "no digits", "--err-t1--")
```

`swap_names` muss den Vornamen vor den Nachnamen stellen

```python
    def test_2(self):
        self.assertEqual(swap_names("Lovelace, Ada"), "Ada Lovelace", "--err-t2--")
        self.assertEqual(swap_names("Turing, Alan"), "Alan Turing", "--err-t2--")
```

`swap_names` muss einen Text ohne Komma unverändert lassen

```python
    def test_3(self):
        self.assertEqual(swap_names("Ada Lovelace"), "Ada Lovelace", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def mask_digits(text):
    return re.sub(r"\d", "#", text)

def swap_names(text):
    return re.sub(r"(\w+), (\w+)", r"\2 \1", text)
```
