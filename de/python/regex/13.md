---
language: python
exerciseType: 1
---

# --description--

`str.split` kann nur an einem festen Trennzeichen schneiden. `re.split(pattern, text)` schneidet an allem, was das Muster beschreibt, was chaotische Eingaben üblicherweise brauchen:
```python
import re

print("a, b;c".split(","))            # ['a', ' b;c']
print(re.split(r"[,;]", "a, b;c"))    # ['a', ' b', 'c']
print(re.split(r"[,;\s]+", "a, b;c")) # ['a', 'b', 'c']
```
Das Trennzeichen als `[,;\s]+` zu schreiben lässt eine ganze Folge von Kommas, Semikolons und Leerzeichen als einen einzigen Schnitt zählen, statt leere Zeichenketten zwischen ihnen zu lassen.

Ein `maxsplit`-Argument hält nach einer gegebenen Anzahl von Schnitten an und lässt den Rest des Texts im letzten Element: `re.split(r"\s+", "a b c", maxsplit=1)` ergibt `['a', 'b c']`.

# --instructions--

Schreibe eine Funktion `split_fields(line)`, die die Liste der Felder in der Zeile zurückgibt, wobei jede Folge von Kommas, Semikolons und Leerzeichen als ein einzelnes Trennzeichen behandelt wird

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`split_fields` muss an Kommas, Semikolons und Leerzeichen schneiden

```python
    def test_1(self):
        self.assertEqual(split_fields("a, b;c"), ["a", "b", "c"], "--err-t1--")
        self.assertEqual(split_fields("red green blue"), ["red", "green", "blue"], "--err-t1--")
```

Eine Folge von Trennzeichen muss einen einzigen Schnitt ergeben

```python
    def test_2(self):
        self.assertEqual(split_fields("x ; ; y"), ["x", "y"], "--err-t2--")
        self.assertEqual(split_fields("1,,2"), ["1", "2"], "--err-t2--")
```

Eine Zeile ohne Trennzeichen muss ein einzelnes Feld zurückgeben

```python
    def test_3(self):
        self.assertEqual(split_fields("solo"), ["solo"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def split_fields(line):
    return re.split(r"[,;\s]+", line)
```
