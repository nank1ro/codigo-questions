---
language: python
exerciseType: 1
---

# --description--

Klammern zu zählen, um Gruppe `3` zu finden, wird schnell mühsam. Eine Gruppe kann mit `(?P<name>...)` einen Namen bekommen und dann mit `match.group("name")` gelesen werden:
```python
import re

match = re.search(r"(?P<hour>\d\d):(?P<minute>\d\d)", "starts at 09:30")
print(match.group("hour"))    # 09
print(match.group("minute"))  # 30
print(match.groupdict())      # {'hour': '09', 'minute': '30'}
```
`match.groupdict()` gibt jede benannte Gruppe als Wörterbuch zurück. Benannte Gruppen behalten auch ihre Nummer, daher funktioniert `match.group(1)` weiterhin.

Das Beispiel verwendet auch einen **Quantifizierer** mit geschweiften Klammern: `\d{2}` bedeutet genau zwei Ziffern, `\d{2,4}` bedeutet zwischen zwei und vier, und `\d{2,}` bedeutet zwei oder mehr. Sie sind die präzise Version von `+` (eines oder mehr), `*` (null oder mehr) und `?` (null oder eines).

# --instructions--

Schreibe eine Funktion `parse_date(text)`, die ein Datum findet, das als vier Ziffern, ein Bindestrich, zwei Ziffern, ein Bindestrich und zwei Ziffern geschrieben ist, und ein Wörterbuch mit den Schlüsseln `year`, `month` und `day` zurückgibt, oder `None`, wenn es kein solches Datum gibt

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`parse_date` muss die drei Teile des Datums lesen

```python
    def test_1(self):
        self.assertEqual(parse_date("due 2026-09-13 ok"), {"year": "2026", "month": "09", "day": "13"}, "--err-t1--")
```

`parse_date` muss funktionieren, wenn das Datum der ganze Text ist

```python
    def test_2(self):
        self.assertEqual(parse_date("1999-12-31"), {"year": "1999", "month": "12", "day": "31"}, "--err-t2--")
```

`parse_date` muss `None` zurückgeben, wenn es kein Datum gibt

```python
    def test_3(self):
        self.assertIsNone(parse_date("no date here"), "--err-t3--")
        self.assertIsNone(parse_date("26-09-13"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def parse_date(text):
    match = re.search(r"(?P<year>\d{4})-(?P<month>\d{2})-(?P<day>\d{2})", text)
    if match is None:
        return None
    return match.groupdict()
```
