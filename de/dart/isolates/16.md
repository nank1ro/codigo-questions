---
language: dart
exerciseType: 1
---

# --description--

Jedes `Isolate.run` startet sein eigenes Isolate, daher laufen mehrere davon wirklich zum selben Zeitpunkt, auf so vielen Kernen wie die Maschine hat. Das Muster kennst du bereits von Futures: Starte zuerst jede Berechnung, und warte dann mit `Future.wait` auf alle, was die Ergebnisse in der Reihenfolge der Eingabe behält.

```dart
Future<List<int>> doubleAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * 2)));
}
```

Ein Isolate zu starten ist nicht kostenlos: Es kostet Speicher und ein paar Millisekunden. Eine lange Berechnung auf eine Handvoll Isolates aufzuteilen lohnt sich, tausend triviale Additionen an tausend Isolates zu senden nicht.

# --instructions--

Schreibe eine Funktion `squareAll`, die eine `List<int>` `numbers` entgegennimmt und ein `Future<List<int>>` liefert, das mit dem Quadrat jeder Zahl in derselben Reihenfolge abschließt, wobei jedes Quadrat in seinem eigenen Isolate berechnet wird.

Beispiel:
```dart
print(await squareAll([1, 2, 3])); // [1, 4, 9]
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`squareAll([1, 2, 3])` sollte mit `[1, 4, 9]` abschließen

```dart
  test('test1', () async {
    expect(await squareAll([1, 2, 3]), [1, 4, 9], reason: '--err-t1--');
  });
```

`squareAll([-4, 5])` sollte mit `[16, 25]` abschließen, in der Reihenfolge der Eingabe

```dart
  test('test2', () async {
    expect(await squareAll([-4, 5]), [16, 25], reason: '--err-t2--');
  });
```

`squareAll([])` sollte mit einer leeren Liste abschließen und `squareAll` sollte ein `Future<List<int>>` zurückgeben

```dart
  test('test3', () async {
    expect(await squareAll(<int>[]), <int>[], reason: '--err-t3--');
    expect(squareAll([2]), isA<Future<List<int>>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<List<int>> squareAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * n)));
}
```
