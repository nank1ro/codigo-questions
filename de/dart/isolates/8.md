---
language: dart
exerciseType: 1
---

# --description--

`Isolate.spawn` übergibt genau **eine** Nachricht an den Einstiegspunkt, und der Worker braucht normalerweise sowohl einen `SendPort` zum Antworten als auch einige Daten zum Arbeiten. Der übliche Trick ist, alles in eine `List` zu packen und sie auf der anderen Seite auszupacken:

```dart
void multiply(List<Object> message) {
  final port = message[0] as SendPort;
  final value = message[1] as int;
  port.send(value * 2);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(multiply, <Object>[receivePort.sendPort, 21]);
  print(await receivePort.first); // 42
}
```

Die Liste wird auf dem Hinweg kopiert, daher liest der Worker seine eigenen Werte. Ein `SendPort` ist eines der wenigen Dinge, die nicht kopiert, sondern geteilt werden: Er zeigt weiterhin auf den Briefkasten des Isolates, das ihn erstellt hat, und genau deshalb kann er als Rücksendeadresse verwendet werden.

# --instructions--

Schreibe eine Top-Level-Funktion `sendSum`, die eine `List<Object>` `message` entgegennimmt, bei der Element `0` ein `SendPort` ist und die Elemente `1` und `2` `int`s sind, und ihre Summe an den Port sendet.

Schreibe dann eine Funktion `addInIsolate`, die zwei `int`s `a` und `b` entgegennimmt und ein `Future<int>` liefert, das mit der Summe abschließt, die ein gestartetes `sendSum`-Isolate berechnet.

Beispiel:
```dart
print(await addInIsolate(2, 3)); // 5
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendSum` sollte die Summe der beiden Zahlen der Nachricht an den Port senden

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendSum(<Object>[port.sendPort, 4, 6]);
    expect(await port.first, 10, reason: '--err-t1--');
  });
```

`addInIsolate(2, 3)` sollte mit `5` abschließen

```dart
  test('test2', () async {
    expect(await addInIsolate(2, 3), 5, reason: '--err-t2--');
  });
```

`addInIsolate(10, -4)` sollte mit `6` abschließen und `addInIsolate` sollte ein `Future<int>` zurückgeben

```dart
  test('test3', () async {
    expect(await addInIsolate(10, -4), 6, reason: '--err-t3--');
    expect(addInIsolate(1, 1), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendSum(List<Object> message) {
  final port = message[0] as SendPort;
  final a = message[1] as int;
  final b = message[2] as int;
  port.send(a + b);
}

Future<int> addInIsolate(int a, int b) async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendSum, <Object>[receivePort.sendPort, a, b]);
  return await receivePort.first as int;
}
```
