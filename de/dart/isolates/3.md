---
language: dart
exerciseType: 1
---

# --description--

Zwischen Isolates werden nur **Daten** kopiert, nicht **Code**. Jedes Isolate eines Programms kann bereits alle Top-Level-Funktionen und -Klassen dieses Programms sehen, daher darf die Berechnung, die an `Isolate.run` übergeben wird, sie frei aufrufen:

```dart
int slowLength(String text) => text.length;

Future<int> lengthInIsolate(String text) {
  return Isolate.run(() => slowLength(text));
}
```

Unterwegs ist der eingefangene `text` auf dem Hinweg und das resultierende `int` auf dem Rückweg, jeweils eine Kopie. Das Muster ist immer dasselbe: Lass die schwere Funktion, wo sie ist, und verpacke den **Aufruf** in `Isolate.run`.

# --instructions--

Die Funktion `wordCount(text)` zählt die Wörter eines Strings. Schreibe eine Funktion `countWords`, die einen `String` `text` entgegennimmt und ein `Future<int>` liefert, das mit `wordCount(text)` abschließt, berechnet in einem anderen Isolate mit `Isolate.run`.

Beispiel:
```dart
print(await countWords('hello isolate world')); // 3
```

# --before-seed--

```dart
import 'dart:isolate';

int wordCount(String text) {
  return text.split(' ').where((w) => w.isNotEmpty).length;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countWords('hello isolate world')` sollte mit `3` abschließen

```dart
  test('test1', () async {
    expect(await countWords('hello isolate world'), 3, reason: '--err-t1--');
  });
```

`countWords('Dart')` sollte mit `1` abschließen und `countWords('')` mit `0`

```dart
  test('test2', () async {
    expect(await countWords('Dart'), 1, reason: '--err-t2--');
    expect(await countWords(''), 0, reason: '--err-t2--');
  });
```

`countWords('  a  b ')` sollte mit `2` abschließen und `countWords` sollte ein `Future<int>` zurückgeben

```dart
  test('test3', () async {
    expect(await countWords('  a  b '), 2, reason: '--err-t3--');
    expect(countWords('one two'), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<int> countWords(String text) {
  return Isolate.run(() => wordCount(text));
}
```
