---
language: dart
exerciseType: 4
---

# --description--

Ein gestartetes Isolate folgt immer denselben vier Schritten: den Briefkasten öffnen, den Worker mit seiner Adresse starten, auf die Antwort warten, sie verwenden.

```dart
import 'dart:isolate';

void worker(SendPort port) {
  port.send('done');
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

Das `await` vor `Isolate.spawn` wartet darauf, dass das Isolate *startet*, nicht darauf, dass es seine Arbeit beendet: Das Ergebnis kommt später über den Port.

# --instructions--

Sortiere die Elemente, um ein Programm zu bauen, das ein `worker`-Isolate startet, das `7 * 6` an das Main-Isolate zurücksendet, das es ausgibt. Setze den Import an den Anfang und definiere `worker` vor `main`. Das Programm muss `42` ausgeben.

# --answers--

```dart
import 'dart:isolate';
```

```dart
void worker(SendPort port) {
```

```dart
  port.send(7 * 6);
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  final receivePort = ReceivePort();
```

```dart
  await Isolate.spawn(worker, receivePort.sendPort);
```

```dart
  print(await receivePort.first);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
void worker(SendPort port) {
  port.send(7 * 6);
}
Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

# --output--

42
