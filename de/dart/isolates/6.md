---
language: dart
exerciseType: 1
---

# --description--

Die Funktion, die an `Isolate.spawn` übergeben wird, heißt **Einstiegspunkt**. Sie muss eine Top-Level-Funktion (oder statische Funktion) sein, die genau einen Parameter entgegennimmt: die Nachricht, die `Isolate.spawn` ihr übergibt.

```dart
void reportAnswer(SendPort port) {
  port.send(42);
}
```

Die Nachrichten, die in einem `ReceivePort` ankommen, haben den statischen Typ `dynamic`, weil jeder Wert gesendet worden sein könnte. Wenn du weißt, was das andere Isolate sendet, castest du sie:

```dart
final receivePort = ReceivePort();
await Isolate.spawn(reportAnswer, receivePort.sendPort);
final answer = await receivePort.first as int;
```

# --instructions--

Schreibe eine Top-Level-Funktion `sendGreeting`, die einen `SendPort` `port` entgegennimmt und den String `hello from the isolate` an ihn sendet.

Schreibe dann eine Funktion `greetFromIsolate`, die ein `Future<String>` liefert: Sie erstellt einen `ReceivePort`, startet `sendGreeting` mit seinem `sendPort` und schließt mit der ersten empfangenen Nachricht ab.

Beispiel:
```dart
print(await greetFromIsolate()); // hello from the isolate
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendGreeting` sollte `'hello from the isolate'` an den Port senden, den sie empfängt

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendGreeting(port.sendPort);
    expect(await port.first, 'hello from the isolate', reason: '--err-t1--');
  });
```

`greetFromIsolate()` sollte mit `'hello from the isolate'` abschließen

```dart
  test('test2', () async {
    expect(await greetFromIsolate(), 'hello from the isolate', reason: '--err-t2--');
  });
```

`greetFromIsolate` sollte ein `Future<String>` zurückgeben und sollte funktionieren, wenn sie zweimal aufgerufen wird

```dart
  test('test3', () async {
    expect(greetFromIsolate(), isA<Future<String>>(), reason: '--err-t3--');
    expect(await greetFromIsolate(), 'hello from the isolate', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendGreeting(SendPort port) {
  port.send('hello from the isolate');
}

Future<String> greetFromIsolate() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendGreeting, receivePort.sendPort);
  return await receivePort.first as String;
}
```
