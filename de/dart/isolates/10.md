---
language: dart
exerciseType: 1
---

# --description--

Das Einsammeln einer ganzen Reihe von Nachrichten folgt einem Rezept: eine leere Liste vor der Schleife, ein `add` pro echter Nachricht und `close()` bei dem Signal, das den Stream beendet. Sobald der Port geschlossen ist, endet das `await for`, und die Funktion kann zurückkehren:

```dart
Future<List<int>> collect(ReceivePort port) async {
  final values = <int>[];
  await for (final message in port) {
    if (message == null) {
      port.close();
    } else {
      values.add(message as int);
    }
  }
  return values;
}
```

Nachrichten behalten die Reihenfolge, in der sie gesendet wurden, daher spiegelt die Liste, die du baust, die Arbeit des anderen Isolates Schritt für Schritt wider.

# --instructions--

Schreibe eine Top-Level-Funktion `sendWords`, die eine `List<Object>` `message` entgegennimmt, bei der Element `0` ein `SendPort` ist und Element `1` eine `List<String>`: Sende jedes Wort an den Port und sende dann `null`, um das Ende zu signalisieren.

Schreibe dann eine Funktion `collectWords`, die eine `List<String>` `words` entgegennimmt und ein `Future<List<String>>` liefert, das mit allen Wörtern abschließt, die von einem gestarteten `sendWords`-Isolate empfangen wurden.

Beispiel:
```dart
print(await collectWords(['a', 'b'])); // [a, b]
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendWords` sollte jedes Wort und dann `null` senden

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendWords(<Object>[port.sendPort, <String>['a', 'b']]);
    final received = <dynamic>[];
    await for (final message in port) {
      received.add(message);
      if (message == null) port.close();
    }
    expect(received, ['a', 'b', null], reason: '--err-t1--');
  });
```

`collectWords(['hello', 'isolate'])` sollte mit `['hello', 'isolate']` abschließen

```dart
  test('test2', () async {
    expect(await collectWords(['hello', 'isolate']), ['hello', 'isolate'], reason: '--err-t2--');
  });
```

`collectWords([])` sollte mit einer leeren Liste abschließen und `collectWords` sollte ein `Future<List<String>>` zurückgeben

```dart
  test('test3', () async {
    expect(await collectWords(<String>[]), <String>[], reason: '--err-t3--');
    expect(collectWords(['x']), isA<Future<List<String>>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendWords(List<Object> message) {
  final port = message[0] as SendPort;
  final words = message[1] as List<String>;
  for (final word in words) {
    port.send(word);
  }
  port.send(null);
}

Future<List<String>> collectWords(List<String> words) async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendWords, <Object>[receivePort.sendPort, words]);
  final collected = <String>[];
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      collected.add(message as String);
    }
  }
  return collected;
}
```
