---
language: dart
exerciseType: 1
---

# --description--

Die Methode `.putIfAbsent(key, ifAbsent)` fügt ein Paar **nur dann** hinzu, wenn der Schlüssel noch nicht in der Map ist. Das zweite Argument ist eine Funktion, die den Wert erzeugt. Wenn der Schlüssel bereits existiert, bleibt die Map unverändert. In beiden Fällen wird der jetzt unter dem Schlüssel gespeicherte Wert zurückgegeben:

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99); // Ann ist bereits vorhanden, nichts ändert sich
ages.putIfAbsent('Bob', () => 25); // Bob wird hinzugefügt
print(ages); // {Ann: 30, Bob: 25}
```

# --instructions--

Verwende für die Map `ages` zweimal `.putIfAbsent()`: zuerst für den Schlüssel `'Ann'` mit dem Wert `99`, dann für den Schlüssel `'Bob'` mit dem Wert `25`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var ages = {'Ann': 30};
```

# --after-seed--

```dart
  return ages;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`ages` sollte `{'Ann': 30, 'Bob': 25}` sein

```dart
  test('test1', () {
    expect(result(), {'Ann': 30, 'Bob': 25}, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99);
ages.putIfAbsent('Bob', () => 25);
```
