---
language: dart
exerciseType: 2
---

# --description--

Die Methode `.remove(key)` löscht einen Schlüssel und seinen Wert aus der Map. Sie gibt den entfernten Wert zurück, oder `null`, wenn der Schlüssel nicht vorhanden war:

```dart
var ages = {'Ann': 30, 'Bob': 25};
int? removed = ages.remove('Ann');
print(removed); // 30
print(ages); // {Bob: 25}
```

Die Eigenschaft `.length` gibt die Anzahl der Schlüssel-Wert-Paare zurück:

```dart
print(ages.length); // 1
```

# --instructions--

Vervollständige den Code, um `Bob` aus der Map zu entfernen und dann auszugeben, wie viele Paare übrig sind.

# --seed--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.[/]('Bob');
print(ages.[/]);
```

# --answers--

- remove
- length
- delete
- removeAt
- size
- count

# --solutions--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.remove('Bob');
print(ages.length);
```

# --output--

2
