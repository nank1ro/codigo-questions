---
language: dart
exerciseType: 1
---

# --description--

Die Methode `.update(key, update)` ersetzt den Wert eines vorhandenen Schlüssels. Das zweite Argument ist eine Funktion, die den aktuellen Wert erhält und den neuen zurückgibt. Wenn der Schlüssel fehlt, wirft `.update()` einen Fehler, es sei denn, du übergibst eine `ifAbsent`-Funktion, die den Anfangswert erzeugt:

```dart
var stock = {'apple': 3};
stock.update('apple', (n) => n + 1); // apple wird 4
stock.update('kiwi', (n) => n + 1, ifAbsent: () => 1); // kiwi wird mit 1 hinzugefügt
print(stock); // {apple: 4, kiwi: 1}
```

# --instructions--

Verwende für die Map `stock` `.update()`, um den Wert von `'apple'` zu verdoppeln, und dann `.update()` mit `ifAbsent` für den Schlüssel `'kiwi'`, sodass er mit dem Wert `5` hinzugefügt wird.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var stock = {'apple': 3, 'pear': 1};
```

# --after-seed--

```dart
  return stock;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`stock` sollte `{'apple': 6, 'pear': 1, 'kiwi': 5}` sein

```dart
  test('test1', () {
    expect(result(), {'apple': 6, 'pear': 1, 'kiwi': 5}, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var stock = {'apple': 3, 'pear': 1};
stock.update('apple', (n) => n * 2);
stock.update('kiwi', (n) => n * 2, ifAbsent: () => 5);
```
