---
language: dart
exerciseType: 1
---

# --description--

Die Eigenschaft `.keys` liefert alle Schlüssel einer Map und `.values` liefert alle Werte, in Einfügereihenfolge. Es sind träge `Iterable`s, also rufe `.toList()` auf, wenn du eine echte `List` brauchst:

```dart
var ages = {'Ann': 30, 'Bob': 25};
List<String> names = ages.keys.toList();
List<int> years = ages.values.toList();
print(names); // [Ann, Bob]
print(years); // [30, 25]
```

# --instructions--

Erstelle für die Map `scores` eine `List<String>` namens `subjects` mit ihren Schlüsseln und eine `List<int>` namens `points` mit ihren Werten.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var scores = {'math': 90, 'art': 75, 'gym': 60};
```

# --after-seed--

```dart
  return [subjects, points];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`subjects` sollte `['math', 'art', 'gym']` sein

```dart
  test('test1', () {
    expect(result()[0], ['math', 'art', 'gym'], reason: '--err-t1--');
  });
```

`points` sollte `[90, 75, 60]` sein

```dart
  test('test2', () {
    expect(result()[1], [90, 75, 60], reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var scores = {'math': 90, 'art': 75, 'gym': 60};
List<String> subjects = scores.keys.toList();
List<int> points = scores.values.toList();
```
