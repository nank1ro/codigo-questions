---
language: dart
exerciseType: 4
---

# --description--

Extensions sind nicht nur für SDK-Typen da: Du kannst auch **deine eigenen Klassen** erweitern. Das ist nützlich, wenn die Klasse aus einem Paket stammt, das du nicht kontrollierst, oder wenn du die Klasse klein halten und optionale Helfer neben dem Code hinzufügen möchtest, der sie braucht.

```dart
class Circle {
  final double radius;
  Circle(this.radius);
}

extension CircleMath on Circle {
  double get diameter => radius * 2;
}

void main() {
  print(Circle(3).diameter); // 6.0
}
```

Die Extension sieht die öffentlichen Felder und Methoden der Klasse, genau wie Code, der außerhalb der Klasse geschrieben wurde.

# --instructions--

Sortiere die Elemente, um eine Klasse `Rectangle` mit `width` und `height` zu definieren, dann eine Extension `RectangleMath` auf `Rectangle` mit einem Getter `area` und danach ein `main`, das die Fläche eines Rechtecks von `4` mal `2.5` ausgibt. Definiere zuerst die Klasse, dann die Extension, dann `main`. Deklariere innerhalb der Klasse beide Felder vor dem Konstruktor. Das Programm muss Folgendes ausgeben:

```
10.0
```

# --answers--

```dart
class Rectangle {
```

```dart
  final double width;
```

```dart
  final double height;
```

```dart
  Rectangle(this.width, this.height);
```

```dart
}
```

```dart
extension RectangleMath on Rectangle {
```

```dart
  double get area => width * height;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Rectangle(4, 2.5).area);
```

```dart
}
```

# --solutions--

```dart
class Rectangle {
  final double width;
  final double height;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

```dart
class Rectangle {
  final double height;
  final double width;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

# --output--

10.0
