---
language: dart
exerciseType: 4
---

# --description--

Der Typ nach `on` kann ein **parametrisierter** Typ wie `List<int>` sein. Die Extension gilt dann nur für Listen dieses Elementtyps: `[1, 2].total()` funktioniert, `['a', 'b'].total()` kompiliert nicht.

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}
```

Innerhalb der Extension ist `this` die Liste, sodass du sie wie gewohnt durchlaufen, indizieren oder `length` aufrufen kannst.

# --instructions--

Sortiere die Elemente, um eine Extension `Totals` auf `List<int>` mit einer Methode `total` zu deklarieren, die die Elemente mit einer `for`-Schleife aufsummiert, und danach ein `main`, das die Summe von `[3, 4, 5]` ausgibt. Deklariere die Extension vor `main`. Das Programm muss Folgendes ausgeben:

```
12
```

# --answers--

```dart
extension Totals on List<int> {
```

```dart
  int total() {
```

```dart
    var sum = 0;
```

```dart
    for (final n in this) {
```

```dart
      sum += n;
```

```dart
    }
```

```dart
    return sum;
```

```dart
  }
```

```dart
}
```

```dart
void main() {
```

```dart
  print([3, 4, 5].total());
```

```dart
}
```

# --solutions--

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}

void main() {
  print([3, 4, 5].total());
}
```

# --output--

12
