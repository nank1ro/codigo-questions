---
language: dart
exerciseType: 1
---

# --description--

Dart erlaubt einem Typ, zu definieren, was Operatoren wie `+`, `*` oder `==` für seine Werte bedeuten, und zwar mit einer Methode, deren Name das Schlüsselwort `operator` gefolgt vom Symbol ist. Die rechte Seite des Operators ist der Parameter der Methode:

```dart
extension Scaling on List<int> {
  List<int> operator *(int factor) => map((n) => n * factor).toList();
}

void main() {
  print([1, 2, 3] * 10); // [10, 20, 30]
}
```

Da Extensions Operatoren deklarieren können, kannst du einem vorhandenen Typ einen neuen Operator geben, den er noch nicht hat. `String` hat `+` und `*`, aber kein `-`, sodass eine Extension definieren kann, was `'hello world' - 'o'` bedeutet.

# --instructions--

Schreibe eine Extension namens `Removing` auf `String` mit einem Operator `-`, der einen `String`-Parameter `part` nimmt und den String ohne jedes Vorkommen von `part` zurückgibt. Verwende `replaceAll`.

Beispiel:
```dart
print('hello world' - 'o'); // hell wrld
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`'hello world' - 'o'` sollte `'hell wrld'` sein

```dart
  test('test1', () {
    expect('hello world' - 'o', 'hell wrld', reason: '--err-t1--');
  });
```

`'banana' - 'an'` sollte `'ba'` sein

```dart
  test('test2', () {
    expect('banana' - 'an', 'ba', reason: '--err-t2--');
  });
```

`'abc' - 'z'` sollte `'abc'` sein und `'aaa' - 'a'` sollte `''` sein

```dart
  test('test3', () {
    expect('abc' - 'z', 'abc', reason: '--err-t3--');
    expect('aaa' - 'a', '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Removing on String {
  String operator -(String part) => replaceAll(part, '');
}
```
