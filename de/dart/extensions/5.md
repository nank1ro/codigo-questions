---
language: dart
exerciseType: 1
---

# --description--

Eine Extension auf `List<int>` kann nicht auf einer `List<String>` verwendet werden. Um eine Extension zu schreiben, die mit **jedem** Elementtyp funktioniert, gib der Extension einen **Typparameter**, der in spitzen Klammern nach ihrem Namen steht, und verwende ihn im `on`-Typ:

```dart
extension Firsts<T> on List<T> {
  T get firstOrLast => length > 1 ? this[0] : this[length - 1];
}
```

`T` ist ein Platzhalter für „was auch immer der Elementtyp ist“: Auf einer `List<int>` wird er zu `int`, auf einer `List<String>` zu `String`, sodass der Getter oben entsprechend ein `int` oder ein `String` zurückgibt. Der Compiler setzt `T` bei jedem Aufruf für dich ein.

```dart
print([7, 8, 9].firstOrLast); // 7
print(['a', 'b'].firstOrLast); // a
```

# --instructions--

Schreibe eine Extension namens `Seconds` mit einem Typparameter `T` auf `List<T>` mit einem Getter `secondOrNull` vom Typ `T?`, der das Element an Index `1` zurückgibt, oder `null`, wenn die Liste weniger als zwei Elemente hat.

Beispiel:
```dart
print([10, 20, 30].secondOrNull); // 20
print(['solo'].secondOrNull);     // null
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`[10, 20, 30].secondOrNull` sollte `20` sein

```dart
  test('test1', () {
    expect([10, 20, 30].secondOrNull, 20, reason: '--err-t1--');
  });
```

`['a', 'b'].secondOrNull` sollte `'b'` sein

```dart
  test('test2', () {
    expect(['a', 'b'].secondOrNull, 'b', reason: '--err-t2--');
  });
```

`['solo'].secondOrNull` und `<int>[].secondOrNull` sollten `null` sein

```dart
  test('test3', () {
    expect(['solo'].secondOrNull, null, reason: '--err-t3--');
    expect(<int>[].secondOrNull, null, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Seconds<T> on List<T> {
  T? get secondOrNull => length > 1 ? this[1] : null;
}
```
