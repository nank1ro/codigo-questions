---
language: dart
exerciseType: 1
---

# --instructions--
Schreibe eine Funktion, die den Wert an der Position (`row`, `col`) einer Multiplikationstabelle zurückgibt. Verwende verschachtelte `for`-Schleifen, wobei die äußere Schleife über Zeilen und die innere über Spalten iteriert (beide 1-indiziert). Gib das Produkt an der angegebenen Position zurück.

# --before-seed--
```dart
int multiplicationCell(int row, int col) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Zeile 3, Spalte 4 ergibt 12
```dart
    test('3x4 = 12', () {
      expect(multiplicationCell(3, 4), 12, reason: '--err-t1--');
    });
```

Zeile 1, Spalte 1 ergibt 1
```dart
    test('1x1 = 1', () {
      expect(multiplicationCell(1, 1), 1, reason: '--err-t2--');
    });
```

Zeile 7, Spalte 8 ergibt 56
```dart
    test('7x8 = 56', () {
      expect(multiplicationCell(7, 8), 56, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int r = 1; r <= row; r++) {
    for (int c = 1; c <= col; c++) {
      if (r == row && c == col) {
        result = r * c;
      }
    }
  }
```
