---
language: dart
exerciseType: 1
---

# --instructions--
Schreibe eine Funktion, die die erste negative Zahl in einer `List<int>` zurückgibt. Verwende eine `for`-Schleife mit `break`, um zu stoppen, sobald eine negative Zahl gefunden wurde. Gib `0` zurück, wenn keine negative Zahl vorhanden ist.

# --before-seed--
```dart
int firstNegative(List<int> numbers) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Findet die erste negative Zahl
```dart
    test('finds first negative', () {
      expect(firstNegative([3, -5, -2, 1]), -5, reason: '--err-t1--');
    });
```

Gibt 0 zurück, wenn keine negative Zahl vorhanden ist
```dart
    test('no negative', () {
      expect(firstNegative([1, 2, 3]), 0, reason: '--err-t2--');
    });
```

Das erste Element ist negativ
```dart
    test('first is negative', () {
      expect(firstNegative([-7, 4, 2]), -7, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = 0; i < numbers.length; i++) {
    if (numbers[i] < 0) {
      result = numbers[i];
      break;
    }
  }
```
