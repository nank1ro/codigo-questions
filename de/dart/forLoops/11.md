---
language: dart
exerciseType: 1
---

# --instructions--
Schreibe eine Funktion, die die Anzahl der geraden Zahlen im Bereich von `start` bis `end` (beide einschließlich) zurückgibt.

# --before-seed--
```dart
int countEvens(int start, int end) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Der Bereich 1 bis 10 enthält 5 gerade Zahlen
```dart
    test('1 to 10', () {
      expect(countEvens(1, 10), 5, reason: '--err-t1--');
    });
```

Der Bereich 2 bis 6 enthält 3 gerade Zahlen
```dart
    test('2 to 6', () {
      expect(countEvens(2, 6), 3, reason: '--err-t2--');
    });
```

Der Bereich 1 bis 1 enthält 0 gerade Zahlen
```dart
    test('1 to 1', () {
      expect(countEvens(1, 1), 0, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = start; i <= end; i++) {
    if (i % 2 == 0) result++;
  }
```
