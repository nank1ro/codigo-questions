---
language: dart
exerciseType: 1
---

# --instructions--
Schreibe eine Funktion, die eine `List<String>` entgegennimmt und alle Elemente mit einem Leerzeichen dazwischen zu einer einzigen Zeichenkette verbindet. Verwende dabei eine `for-in`-Schleife.

# --before-seed--
```dart
String joinWords(List<String> words) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Verbindet drei Wörter mit Leerzeichen
```dart
    test('three words', () {
      expect(joinWords(['hello', 'dart', 'world']), 'hello dart world', reason: '--err-t1--');
    });
```

Ein einzelnes Wort wird unverändert zurückgegeben
```dart
    test('single word', () {
      expect(joinWords(['only']), 'only', reason: '--err-t2--');
    });
```

Eine leere Liste gibt eine leere Zeichenkette zurück
```dart
    test('empty list', () {
      expect(joinWords([]), '', reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  String result = '';
  for (var word in words) {
    if (result.isEmpty) {
      result = word;
    } else {
      result += ' $word';
    }
  }
```
