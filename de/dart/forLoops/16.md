---
language: dart
exerciseType: 1
---

# --instructions--
Schreibe eine Funktion, die die Fakultät einer nicht-negativen ganzen Zahl `n` mit einer `for`-Schleife berechnet. Die Fakultät von 0 ist 1.

# --before-seed--
```dart
int factorial(int n) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
Die Fakultät von 5 ist 120
```dart
    test('5! = 120', () {
      expect(factorial(5), 120, reason: '--err-t1--');
    });
```

Die Fakultät von 0 ist 1
```dart
    test('0! = 1', () {
      expect(factorial(0), 1, reason: '--err-t2--');
    });
```

Die Fakultät von 1 ist 1
```dart
    test('1! = 1', () {
      expect(factorial(1), 1, reason: '--err-t3--');
    });
```

Die Fakultät von 6 ist 720
```dart
    test('6! = 720', () {
      expect(factorial(6), 720, reason: '--err-t4--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 1;
  for (int i = 2; i <= n; i++) {
    result *= i;
  }
```
