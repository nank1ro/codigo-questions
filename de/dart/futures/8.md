---
language: dart
exerciseType: 1
---

# --description--

Ein Future kann auch mit einem **Fehler** abschließen. Wenn eine `async`-Funktion wirft, entkommt die Ausnahme nicht sofort: Sie wird zum Fehler des zurückgegebenen Futures. Wer dieses Future **awaitet**, sieht den Fehler beim `await` geworfen, er lässt sich also mit einem gewöhnlichen `try`/`catch` behandeln:

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s); // throws FormatException for 'abc'
}

Future<int> orZero(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return 0;
  }
}
```

Das `await` innerhalb des `try` ist entscheidend: `return parseLater(s);` würde das Future **ohne zu warten** an den Aufrufer weiterreichen, der Fehler käme also an, wenn der `try`-Block schon vorbei ist, und das `catch` würde nie laufen.

# --instructions--

Die Funktion `parseLater(s)` liefert ein `Future<int>`, das mit der in `s` geschriebenen Zahl abschließt, oder mit einem Fehler, falls `s` keine Zahl ist. Schreibe eine `async`-Funktion `safeParse`, die einen `String` `s` entgegennimmt und ein `Future<int>` liefert, das mit der geparsten Zahl abschließt, oder mit `-1`, falls `parseLater` fehlschlägt.

Beispiel:
```dart
print(await safeParse('12'));  // 12
print(await safeParse('abc')); // -1
```

# --before-seed--

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s);
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeParse('12')` sollte mit `12` abschließen

```dart
  test('test1', () async {
    expect(await safeParse('12'), 12, reason: '--err-t1--');
  });
```

`safeParse('abc')` sollte mit `-1` abschließen, statt zu werfen

```dart
  test('test2', () async {
    expect(await safeParse('abc'), -1, reason: '--err-t2--');
  });
```

`safeParse('-7')` sollte mit `-7` abschließen und `safeParse('')` mit `-1`

```dart
  test('test3', () async {
    expect(await safeParse('-7'), -7, reason: '--err-t3--');
    expect(await safeParse(''), -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> safeParse(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return -1;
  }
}
```
