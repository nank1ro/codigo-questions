---
language: dart
exerciseType: 1
---

# --description--

Du brauchst `Future.wait` nicht, um zwei Futures gleichzeitig laufen zu lassen. Eine `async`-Funktion beginnt zu laufen, sobald sie **aufgerufen** wird, bis zu ihrem ersten `await`; das Future, das du zurückbekommst, ist die bereits laufende Arbeit. Der Trick ist also: **zuerst aufrufen, später awaiten**:

```dart
Future<int> pages() => Future.delayed(const Duration(milliseconds: 20), () => 300);
Future<int> words() => Future.delayed(const Duration(milliseconds: 20), () => 90000);

Future<double> average() async {
  final p = pages();          // both timers start now
  final w = words();
  return await w / await p;   // waits once, about 20 ms in total
}
```

Vergleiche mit `return await words() / await pages();`, wo `pages()` erst aufgerufen wird, nachdem `words()` abgeschlossen ist: gleiches Ergebnis, doppelte Zeit. Bevorzuge die nebenläufige Form, wann immer der zweite Aufruf das Ergebnis des ersten nicht braucht.

# --instructions--

Die Funktion `slow(value)` liefert ein `Future<int>`, das nach einer kurzen Verzögerung mit `value` abschließt, und sie trägt `start <value>` bei ihrem Aufruf und `end <value>` bei ihrem Abschluss in die Liste `calls` ein. Schreibe eine `async`-Funktion `addConcurrently`, die zwei `int` `a` und `b` entgegennimmt, `slow(a)` und `slow(b)` so aufruft, dass beide **gleichzeitig** laufen, und ein `Future<int>` liefert, das mit der Summe der beiden Ergebnisse abschließt.

Beispiel:
```dart
print(await addConcurrently(2, 3)); // 5
print(calls); // [start 2, start 3, end 2, end 3]
```

# --before-seed--

```dart
final calls = <String>[];

Future<int> slow(int value) async {
  calls.add('start $value');
  await Future.delayed(const Duration(milliseconds: 5));
  calls.add('end $value');
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`addConcurrently(2, 3)` sollte mit `5` abschließen

```dart
  test('test1', () async {
    calls.clear();
    expect(await addConcurrently(2, 3), 5, reason: '--err-t1--');
  });
```

`addConcurrently(2, 3)` sollte `slow(2)` und `slow(3)` aufrufen, bevor eines von beiden abschließt, `calls` sollte also `[start 2, start 3, end 2, end 3]` sein

```dart
  test('test2', () async {
    calls.clear();
    await addConcurrently(2, 3);
    expect(calls, ['start 2', 'start 3', 'end 2', 'end 3'], reason: '--err-t2--');
  });
```

`addConcurrently(10, -4)` sollte mit `6` abschließen und beide Aufrufe sollten starten, bevor einer von beiden abschließt

```dart
  test('test3', () async {
    calls.clear();
    expect(await addConcurrently(10, -4), 6, reason: '--err-t3--');
    expect(calls, ['start 10', 'start -4', 'end 10', 'end -4'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> addConcurrently(int a, int b) async {
  final first = slow(a);
  final second = slow(b);
  return await first + await second;
}
```
