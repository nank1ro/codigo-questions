---
language: dart
exerciseType: 4
---

# --description--

`Future.wait` ist das Werkzeug für "mehrere Dinge laden, dann weitermachen". Die typische Form ist: die Liste der Futures bauen, darauf `await Future.wait` anwenden und dann die entstandene Liste verwenden:

```dart
Future<int> stock() => Future.delayed(const Duration(milliseconds: 10), () => 8);
Future<int> orders() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<void> main() async {
  final counts = await Future.wait([stock(), orders()]);
  print(counts); // [8, 3]
}
```

`orders()` schließt zuerst ab, aber die Liste folgt trotzdem der Reihenfolge der Aufrufe: `stock()` zuerst, `orders()` als zweites.

# --instructions--

Sortiere die Elemente, um eine Funktion `one` zu definieren, die nach `10` Millisekunden mit `1` abschließt, eine Funktion `two`, die nach `5` Millisekunden mit `2` abschließt, und eine `async`-`main`, die `loading` ausgibt, beide Funktionen mit `Future.wait` in `values` awaitet und `values` ausgibt. Definiere zuerst `one`, dann `two`, dann `main`. Das Programm muss ausgeben:

```
loading
[1, 2]
```

# --answers--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
```

```dart
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);
```

```dart
Future<void> main() async {
```

```dart
  print('loading');
```

```dart
  final values = await Future.wait([one(), two()]);
```

```dart
  print(values);
```

```dart
}
```

# --solutions--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);

Future<void> main() async {
  print('loading');
  final values = await Future.wait([one(), two()]);
  print(values);
}
```

# --output--

loading
[1, 2]
