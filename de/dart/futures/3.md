---
language: dart
exerciseType: 1
---

# --description--

`Future.value` schließt sofort ab. Um Arbeit zu simulieren, die Zeit braucht, verwendest du **`Future.delayed`**: Es nimmt eine `Duration` und eine Funktion entgegen, wartet die Dauer ab und schließt dann mit dem ab, was die Funktion zurückgibt:

```dart
Future<String> slowHello() {
  return Future.delayed(const Duration(milliseconds: 10), () => 'hello');
}
```

`Duration` wird mit benannten Parametern wie `seconds`, `milliseconds` oder `minutes` gebaut. Innerhalb einer `async`-Funktion kannst du auch eine Verzögerung für sich allein awaiten, ohne Wert, nur um zu pausieren:

```dart
Future<int> slowDouble(int n) async {
  await Future.delayed(const Duration(milliseconds: 10));
  return n * 2;
}
```

Beide Stile sind üblich; der zweite liest sich wie gewöhnlicher sequenzieller Code.

# --instructions--

Schreibe eine Funktion `slowSquare`, die ein `int` `n` entgegennimmt und ein `Future<int>` liefert, das nach einer Verzögerung von `10` Millisekunden mit `n * n` abschließt.

Beispiel:
```dart
print(await slowSquare(4)); // 16, after 10 ms
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`slowSquare(4)` sollte mit `16` abschließen

```dart
  test('test1', () async {
    expect(await slowSquare(4), 16, reason: '--err-t1--');
  });
```

`slowSquare(-3)` sollte mit `9` abschließen und `slowSquare(0)` mit `0`

```dart
  test('test2', () async {
    expect(await slowSquare(-3), 9, reason: '--err-t2--');
    expect(await slowSquare(0), 0, reason: '--err-t2--');
  });
```

`slowSquare` sollte ein `Future<int>` zurückgeben, das mindestens `10` Millisekunden bis zum Abschluss braucht

```dart
  test('test3', () async {
    final future = slowSquare(5);
    expect(future, isA<Future<int>>(), reason: '--err-t3--');
    final watch = Stopwatch()..start();
    await future;
    expect(watch.elapsedMilliseconds, greaterThanOrEqualTo(9), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> slowSquare(int n) {
  return Future.delayed(const Duration(milliseconds: 10), () => n * n);
}
```
