---
language: dart
exerciseType: 3
---

# --description--

`Future.wait` folgt derselben Regel: Wenn **irgendeines** der Futures fehlschlägt, schließt das kombinierte Future mit diesem Fehler ab und `await Future.wait(...)` wirft. Du bekommst nie eine Teilliste der erfolgreichen Werte. Um die anderen zu behalten, behandle den Fehler innerhalb jedes einzelnen Futures, zum Beispiel mit `catchError`, bevor du es an `Future.wait` übergibst.

```dart
Future<int> ok() => Future.value(1);
Future<int> bad() async => throw StateError('nope');

Future<void> main() async {
  try {
    await Future.wait([ok(), bad()]);
  } catch (e) {
    print('failed: $e'); // failed: Bad state: nope
  }
}
```

# --instructions--

Eines der an `Future.wait` übergebenen Futures schließt mit einem Fehler ab. Was macht `await Future.wait([...])`?

# --answers--

- Es wirft diesen Fehler, das `await` muss also in einem `try`/`catch` stehen, um ihn zu behandeln
- Es gibt eine Liste zurück, in der das fehlgeschlagene Future durch `null` ersetzt ist
- Es gibt eine Liste zurück, die nur die Werte der erfolgreichen Futures enthält
- Es schließt nie ab, denn das fehlgeschlagene Future wird endlos wiederholt

# --solutions--

- Es wirft diesen Fehler, das `await` muss also in einem `try`/`catch` stehen, um ihn zu behandeln
