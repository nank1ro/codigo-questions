---
language: dart
exerciseType: 1
---

# --description--

Eine Funktion als `async` zu markieren bewirkt zweierlei: Es erlaubt `await` im Rumpf, und es lässt die Funktion **ein `Future` zurückgeben**. Was auch immer du mit `return` zurückgibst, wird zum Wert, mit dem das Future abschließt, der deklarierte Rückgabetyp ist also `Future<T>`, obwohl der Rumpf ein einfaches `T` zurückgibt:

```dart
Future<String> label(int n) async {
  return 'item $n';
}

Future<void> main() async {
  print(await label(3)); // item 3
}
```

`Future.value` brauchst du hier nicht: Das Schlüsselwort `async` verpackt den zurückgegebenen Wert für dich.

# --instructions--

Schreibe eine `async`-Funktion `greet`, die einen `String` `name` entgegennimmt und ein `Future<String>` liefert, das mit `Hello, ` gefolgt vom Namen und einem Ausrufezeichen abschließt.

Beispiel:
```dart
print(await greet('Ada')); // Hello, Ada!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greet('Ada')` sollte mit `'Hello, Ada!'` abschließen

```dart
  test('test1', () async {
    expect(await greet('Ada'), 'Hello, Ada!', reason: '--err-t1--');
  });
```

`greet('Linus')` sollte mit `'Hello, Linus!'` abschließen

```dart
  test('test2', () async {
    expect(await greet('Linus'), 'Hello, Linus!', reason: '--err-t2--');
  });
```

`greet` sollte ein `Future<String>` zurückgeben

```dart
  test('test3', () async {
    expect(greet('Bob'), isA<Future<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<String> greet(String name) async {
  return 'Hello, $name!';
}
```
