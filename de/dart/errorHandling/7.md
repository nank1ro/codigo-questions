---
language: dart
exerciseType: 1
---

# --description--

Dein eigener Code wirft auf dieselbe Weise wie die Bibliothek. `Exception('message')` erzeugt eine simple Ausnahme mit einer kurzen Erklärung, und `throw` schickt sie auf den Weg:

```dart
if (amount > balance) {
  throw Exception('insufficient funds');
}
```

Die Nachricht geht nicht verloren: `toString()` setzt das Wort `Exception`, einen Doppelpunkt und die Nachricht zusammen, und genau das gibt der Bericht über unbehandelte Ausnahmen aus.

```dart
print(Exception('insufficient funds')); // Exception: insufficient funds
```

Werfen ist besser, als einen ausgedachten Wert wie `-1` zurückzugeben: Der Aufrufer kann nicht vergessen hinzusehen, und der Grund reist mit.

# --instructions--

Schreibe eine Funktion `withdraw`, die zwei `int`-Werte `balance` und `amount` entgegennimmt und den verbleibenden Kontostand zurückgibt.

Wenn `amount` größer als `balance` ist, wirf stattdessen `Exception('insufficient funds')`.

Beispiel:
```dart
print(withdraw(100, 30)); // 70
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`withdraw(100, 30)` sollte `70` zurückgeben

```dart
  test('test1', () {
    expect(withdraw(100, 30), 70, reason: '--err-t1--');
    expect(withdraw(50, 50), 0, reason: '--err-t1--');
  });
```

`withdraw(100, 300)` sollte eine `Exception` werfen

```dart
  test('test2', () {
    expect(() => withdraw(100, 300), throwsA(isA<Exception>()), reason: '--err-t2--');
  });
```

Die geworfene Ausnahme sollte als `Exception: insufficient funds` ausgegeben werden

```dart
  test('test3', () {
    try {
      withdraw(0, 1);
      fail('--err-t3--');
    } catch (e) {
      expect(e.toString(), 'Exception: insufficient funds', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int withdraw(int balance, int amount) {
  if (amount > balance) {
    throw Exception('insufficient funds');
  }
  return balance - amount;
}
```
