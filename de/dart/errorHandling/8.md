---
language: dart
exerciseType: 1
---

# --description--

Manchmal ist ein Handler nicht der richtige Ort, um wiederherzustellen: Du willst den Fehler nur *bemerken* und ihn zu dem Aufrufer weiterziehen lassen, der sich tatsächlich darum kümmern kann. Das Schlüsselwort `rethrow` macht das, innerhalb eines `catch`- oder `on ... catch`-Blocks:

```dart
try {
  return int.parse(text);
} on FormatException {
  log.add('bad input: $text');
  rethrow;
}
```

`rethrow` schickt **dasselbe** Objekt weiter, daher sieht der Aufrufer den ursprünglichen Fehler. `throw e` zu schreiben würde auch funktionieren, aber es startet die Reise neu und verliert, wo der Fehler zuerst passiert ist.

Ein `finally`-Block in derselben Anweisung läuft trotzdem, auch auf dem Weg nach draußen.

# --instructions--

Schreibe eine Funktion `parseOrLog`, die einen `String` `text` und eine `List<String>` `log` entgegennimmt und `int.parse(text)` zurückgibt.

Wenn der Text keine ganze Zahl ist, füge den String `'bad input: '` gefolgt von dem Text zu `log` hinzu und wirf die `FormatException` dann erneut, damit der Aufrufer sie weiterhin sieht.

Beispiel:
```dart
final log = <String>[];
print(parseOrLog('42', log)); // 42
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parseOrLog('42', log)` sollte `42` zurückgeben und das Log leer lassen

```dart
  test('test1', () {
    final log = <String>[];
    expect(parseOrLog('42', log), 42, reason: '--err-t1--');
    expect(log, isEmpty, reason: '--err-t1--');
  });
```

`parseOrLog('oops', log)` sollte eine `FormatException` werfen

```dart
  test('test2', () {
    expect(() => parseOrLog('oops', <String>[]), throwsFormatException, reason: '--err-t2--');
  });
```

Das Log sollte eine Zeile pro Fehler sammeln, und gute Werte sollten nicht geloggt werden

```dart
  test('test3', () {
    final log = <String>[];
    expect(() => parseOrLog('oops', log), throwsFormatException, reason: '--err-t3--');
    expect(log, ['bad input: oops'], reason: '--err-t3--');
    expect(parseOrLog('7', log), 7, reason: '--err-t3--');
    expect(() => parseOrLog('x', log), throwsFormatException, reason: '--err-t3--');
    expect(log, ['bad input: oops', 'bad input: x'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int parseOrLog(String text, List<String> log) {
  try {
    return int.parse(text);
  } on FormatException {
    log.add('bad input: $text');
    rethrow;
  }
}
```
