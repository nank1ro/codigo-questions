---
language: dart
exerciseType: 1
---

# --description--

`firstWhere` gibt das erste Element zurück, das einen Test erfüllt. Wenn nichts passt, gibt es kein Element zum Zurückgeben, also wirft es einen `StateError`:

```dart
final words = ['a', 'fg'];
print(words.firstWhere((w) => w.length > 3)); // Bad state: No element
```

Wie `int.tryParse` bietet die Bibliothek einen Ausweg an. Der benannte Parameter `orElse` nimmt eine Funktion entgegen, die den Wert erzeugt, den man verwendet, wenn nichts gepasst hat:

```dart
print(words.firstWhere((w) => w.length > 3, orElse: () => 'none')); // none
```

Die Wahl ist dieselbe wie zuvor: `orElse`, wenn „nichts passte“ ein gewöhnliches Ergebnis ist, der bloße Aufruf, wenn es bedeuten würde, dass die Daten kaputt sind und jemand davon hören muss.

# --instructions--

Schreibe zwei Funktionen, die eine `List<String>` `words` entgegennehmen.

`firstLong` gibt mit `firstWhere` das erste Wort zurück, das länger als drei Zeichen ist, oder `'none'`, wenn es keines gibt, mit `orElse`.

`firstLongStrict` macht dasselbe mit `firstWhere`, aber ohne `orElse`, wirft also einen `StateError`, wenn es keines gibt.

Beispiel:
```dart
print(firstLong(['a', 'bcde', 'fg'])); // bcde
print(firstLong(['a', 'fg'])); // none
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`firstLong(['a', 'bcde', 'fg'])` sollte `'bcde'` zurückgeben

```dart
  test('test1', () {
    expect(firstLong(['a', 'bcde', 'fg']), 'bcde', reason: '--err-t1--');
    expect(firstLong(['long', 'longer']), 'long', reason: '--err-t1--');
  });
```

`firstLong` sollte `'none'` zurückgeben, wenn kein Wort länger als drei Zeichen ist

```dart
  test('test2', () {
    expect(firstLong(['a', 'fg']), 'none', reason: '--err-t2--');
    expect(firstLong(<String>[]), 'none', reason: '--err-t2--');
    expect(firstLong(['abc']), 'none', reason: '--err-t2--');
  });
```

`firstLongStrict` sollte das Wort zurückgeben, wenn es eines gibt, und andernfalls einen `StateError` werfen

```dart
  test('test3', () {
    expect(firstLongStrict(['a', 'bcde']), 'bcde', reason: '--err-t3--');
    expect(() => firstLongStrict(['a', 'fg']), throwsStateError, reason: '--err-t3--');
    expect(() => firstLongStrict(<String>[]), throwsStateError, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String firstLong(List<String> words) {
  return words.firstWhere((w) => w.length > 3, orElse: () => 'none');
}

String firstLongStrict(List<String> words) {
  return words.firstWhere((w) => w.length > 3);
}
```
