---
language: dart
exerciseType: 1
---

# --description--

`Exception` ist ein Interface, also kann deine eigene Klasse eines sein. Eine eigene Ausnahme gibt dem Fehler einen Namen, den eine `on`-Klausel auswählen kann, und Felder, die ein Handler lesen kann:

```dart
class EmptyCartException implements Exception {
  final String message;

  EmptyCartException(this.message);

  @override
  String toString() => 'EmptyCartException: $message';
}

throw EmptyCartException('nothing to pay for');
```

Drei Teile solltest du beibehalten: `implements Exception`, damit die Klasse zu den anderen Fehlern gehört, ein `final`-Feld mit dem Detail und ein überschriebenes `toString()`, damit der Bericht über unbehandelte Ausnahmen lesbar ist. Ohne diese Überschreibung gibt Dart nur den nackten Klassennamen aus, und das Detail geht verloren.

# --instructions--

Definiere eine Klasse `InvalidEmailException`, die `Exception` implementiert, mit einem `final String`-Feld `message`, das der Konstruktor setzt, und einem `toString()`, das `'InvalidEmailException: '` gefolgt von der Nachricht zurückgibt.

Schreibe dann eine Funktion `checkEmail`, die einen `String` `email` entgegennimmt und ihn zurückgibt, aber `InvalidEmailException('missing @')` wirft, wenn die E-Mail kein `'@'` enthält.

Beispiel:
```dart
print(checkEmail('ada@dart.dev')); // ada@dart.dev
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`checkEmail('ada@dart.dev')` sollte die E-Mail unverändert zurückgeben

```dart
  test('test1', () {
    expect(checkEmail('ada@dart.dev'), 'ada@dart.dev', reason: '--err-t1--');
  });
```

`checkEmail('nope')` sollte eine `InvalidEmailException` werfen, die eine `Exception` ist

```dart
  test('test2', () {
    expect(() => checkEmail('nope'), throwsA(isA<InvalidEmailException>()), reason: '--err-t2--');
    expect(() => checkEmail('nope'), throwsA(isA<Exception>()), reason: '--err-t2--');
  });
```

Das geworfene Objekt sollte die Nachricht `'missing @'` tragen und als `InvalidEmailException: missing @` ausgegeben werden

```dart
  test('test3', () {
    try {
      checkEmail('nope');
      fail('--err-t3--');
    } on InvalidEmailException catch (e) {
      expect(e.message, 'missing @', reason: '--err-t3--');
      expect(e.toString(), 'InvalidEmailException: missing @', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class InvalidEmailException implements Exception {
  final String message;

  InvalidEmailException(this.message);

  @override
  String toString() => 'InvalidEmailException: $message';
}

String checkEmail(String email) {
  if (!email.contains('@')) {
    throw InvalidEmailException('missing @');
  }
  return email;
}
```
