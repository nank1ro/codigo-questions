---
language: dart
exerciseType: 1
---

# --description--

Generatoren erzeugen Events innerhalb einer Funktion. Wenn die Events von **woanders** kommen (einem Button, einem Netzwerk-Callback, einem anderen Objekt), brauchst du einen **`StreamController`**. Er lebt in der Bibliothek `dart:async`, die Datei muss also mit `import 'dart:async';` beginnen.

Ein Controller besitzt einen Stream und lässt dich Events hineinschieben:

```dart
import 'dart:async';

Stream<int> dice() {
  final controller = StreamController<int>();
  controller.add(4);
  controller.add(2);
  controller.close();
  return controller.stream;
}
```

- `add(value)` sendet ein Event
- `close()` beendet den Stream; vergisst du es, warten die Zuhörer für immer
- `stream` ist der `Stream`, den die Zuhörer konsumieren

Events, die hinzugefügt werden, bevor jemand zuhört, werden in einem Puffer aufbewahrt, der Code oben ist also sicher: Ein Zuhörer, der später kommt, bekommt trotzdem `4` und `2`.

# --instructions--

Schreibe eine Funktion `announce`, die eine `List<String>` `names` entgegennimmt und einen `Stream<String>` liefert, der mit einem `StreamController` gebaut wird: Füge jeden Namen in Großbuchstaben hinzu, schließe den Controller und gib seinen Stream zurück. Denk an den `dart:async`-Import.

Beispiel:
```dart
print(await announce(['ada', 'linus']).toList()); // [ADA, LINUS]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`announce(['ada', 'linus'])` sollte `'ADA'`, `'LINUS'` senden

```dart
  test('test1', () async {
    expect(await announce(['ada', 'linus']).toList(), ['ADA', 'LINUS'], reason: '--err-t1--');
  });
```

`announce(['Dart'])` sollte `'DART'` senden

```dart
  test('test2', () async {
    expect(await announce(['Dart']).toList(), ['DART'], reason: '--err-t2--');
  });
```

`announce([])` sollte nichts senden und enden, und `announce` sollte einen `Stream<String>` liefern

```dart
  test('test3', () async {
    expect(await announce([]).toList(), <String>[], reason: '--err-t3--');
    expect(announce(['x']), isA<Stream<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
import 'dart:async';

Stream<String> announce(List<String> names) {
  final controller = StreamController<String>();
  for (final name in names) {
    controller.add(name.toUpperCase());
  }
  controller.close();
  return controller.stream;
}
```
