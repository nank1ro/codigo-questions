---
language: dart
exerciseType: 1
---

# --description--

`Stream.fromIterable` braucht alle Werte im Voraus. Um Werte einen nach dem anderen zu **erzeugen**, schreibst du einen **asynchronen Generator**: eine Funktion, deren Rumpf mit `async*` markiert ist und deren Rückgabetyp `Stream<T>` ist. Darin sendet `yield` ein einzelnes Event an den Stream:

```dart
Stream<int> countTo(int n) async* {
  for (var i = 1; i <= n; i++) {
    yield i;
  }
}
```

Der Rumpf läuft nicht, wenn du `countTo(3)` aufrufst: Er läuft träge, während der Zuhörer nach Werten fragt, und der Stream ist beendet, wenn der Rumpf zu Ende ist.

Um alle Events in einer `List` zu sammeln, rufst du `toList()` auf. Es liefert ein `Future<List<T>>`, du musst es also mit `await` abwarten:

```dart
final values = await countTo(3).toList();
print(values); // [1, 2, 3]
```

# --instructions--

Schreibe eine `async*`-Funktion `countdown`, die ein `int` `from` entgegennimmt und einen `Stream<int>` liefert, der die Werte von `from` hinunter bis `1` ausgibt. Ist `from` gleich `0` oder kleiner, sendet der Stream nichts.

Beispiel:
```dart
print(await countdown(3).toList()); // [3, 2, 1]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countdown(3)` sollte `3`, `2`, `1` senden

```dart
  test('test1', () async {
    expect(await countdown(3).toList(), [3, 2, 1], reason: '--err-t1--');
  });
```

`countdown(1)` sollte nur `1` senden

```dart
  test('test2', () async {
    expect(await countdown(1).toList(), [1], reason: '--err-t2--');
  });
```

`countdown(0)` sollte nichts senden und `countdown` sollte einen `Stream<int>` liefern

```dart
  test('test3', () async {
    expect(await countdown(0).toList(), <int>[], reason: '--err-t3--');
    expect(countdown(2), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> countdown(int from) async* {
  for (var i = from; i >= 1; i--) {
    yield i;
  }
}
```
