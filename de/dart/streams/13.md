---
language: dart
exerciseType: 1
---

# --description--

Die bisher gesehenen Streams sind **Single-Subscription-Streams**: Sie erlauben genau einen Zuhörer. Ein zweiter Aufruf von `listen`, `await for` oder einer konsumierenden Methode wirft einen `StateError` ("Stream has already been listened to").

Um einen Stream unter mehreren Zuhörern zu teilen, wandle ihn mit `asBroadcastStream()` in einen **Broadcast**-Stream um:

```dart
final shared = Stream.fromIterable([1, 2, 3]).asBroadcastStream();
final total = shared.reduce((a, b) => a + b);
final count = shared.length;
print(await total); // 6
print(await count); // 3
```

Ein Broadcast-Stream puffert nicht: Ein Zuhörer bekommt nur die Events, die **nach** seinem Abonnement gesendet werden. Im Beispiel abonnieren beide Zuhörer vor dem ersten `await`, also bekommen beide jedes Event.

# --instructions--

Schreibe eine Funktion `sumAndCount`, die einen `Stream<int>` `source` entgegennimmt und ein `Future<List<int>>` liefert, das mit einer Liste aus zwei Elementen abgeschlossen wird: der Summe der Werte und der Anzahl der Werte. Wandle `source` in einen Broadcast-Stream um, starte beide konsumierenden Aufrufe, **bevor** du einen von beiden abwartest, und warte sie dann ab. Der Stream hat immer mindestens ein Event.

Beispiel:
```dart
print(await sumAndCount(Stream.fromIterable([1, 2, 3]))); // [6, 3]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumAndCount` eines Streams aus `1, 2, 3` sollte mit `[6, 3]` abgeschlossen werden

```dart
  test('test1', () async {
    expect(await sumAndCount(Stream.fromIterable([1, 2, 3])), [6, 3], reason: '--err-t1--');
  });
```

`sumAndCount` eines Streams aus `10, -4` sollte mit `[6, 2]` abgeschlossen werden

```dart
  test('test2', () async {
    expect(await sumAndCount(Stream.fromIterable([10, -4])), [6, 2], reason: '--err-t2--');
  });
```

`sumAndCount` eines Streams aus `7` sollte mit `[7, 1]` abgeschlossen werden

```dart
  test('test3', () async {
    expect(await sumAndCount(Stream.fromIterable([7])), [7, 1], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<List<int>> sumAndCount(Stream<int> source) async {
  final shared = source.asBroadcastStream();
  final sum = shared.reduce((a, b) => a + b);
  final count = shared.length;
  return [await sum, await count];
}
```
