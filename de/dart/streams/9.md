---
language: dart
exerciseType: 1
---

# --description--

Neben `toList()` bietet ein Stream weitere Methoden, die alle seine Events **konsumieren** und ein einzelnes `Future` liefern:

- `first` und `last` werden mit dem ersten bzw. letzten Event abgeschlossen
- `length` wird mit der Anzahl der Events abgeschlossen
- `join(separator)` wird mit allen zu einem `String` verbundenen Events abgeschlossen
- `reduce(combine)` verbindet die Events jeweils zu zweit zu einem einzigen Wert

```dart
final numbers = Stream.fromIterable([3, 9, 2]);
print(await numbers.reduce((a, b) => a + b)); // 14
```

`reduce` ruft `combine` mit dem bisherigen Ergebnis und dem nächsten Event auf. Es wirft eine Exception, wenn der Stream leer ist, verwende es also nur, wenn mindestens ein Event garantiert ist.

# --instructions--

Schreibe zwei Funktionen:
- `largest`, die einen `Stream<int>` `source` entgegennimmt und ein `Future<int>` liefert, das mit dem größten Wert abgeschlossen wird, mithilfe von `reduce`. Der Stream hat immer mindestens ein Event.
- `sentence`, die einen `Stream<String>` `words` entgegennimmt und ein `Future<String>` liefert, das mit allen durch ein einzelnes Leerzeichen verbundenen Wörtern abgeschlossen wird.

Beispiel:
```dart
print(await largest(Stream.fromIterable([3, 9, 2])));        // 9
print(await sentence(Stream.fromIterable(['I', 'am', 'Dart']))); // I am Dart
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest` eines Streams aus `3, 9, 2` sollte mit `9` abgeschlossen werden

```dart
  test('test1', () async {
    expect(await largest(Stream.fromIterable([3, 9, 2])), 9, reason: '--err-t1--');
  });
```

`largest` eines Streams aus `-4, -1, -7` sollte mit `-1` abgeschlossen werden und `largest` eines Streams aus `5` sollte mit `5` abgeschlossen werden

```dart
  test('test2', () async {
    expect(await largest(Stream.fromIterable([-4, -1, -7])), -1, reason: '--err-t2--');
    expect(await largest(Stream.fromIterable([5])), 5, reason: '--err-t2--');
  });
```

`sentence` eines Streams aus `'I', 'am', 'Dart'` sollte mit `'I am Dart'` abgeschlossen werden

```dart
  test('test3', () async {
    expect(await sentence(Stream.fromIterable(['I', 'am', 'Dart'])), 'I am Dart', reason: '--err-t3--');
    expect(await sentence(Stream.fromIterable(['solo'])), 'solo', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> largest(Stream<int> source) {
  return source.reduce((a, b) => a > b ? a : b);
}

Future<String> sentence(Stream<String> words) {
  return words.join(' ');
}
```
