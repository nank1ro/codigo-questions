---
language: dart
exerciseType: 2
---

# --description--

`await for` pausiert die aktuelle Funktion, bis der Stream beendet ist. Wenn du auf Events reagieren willst, **ohne zu warten**, rufst du `listen` auf und übergibst einen Callback: Er wird einmal pro Event aufgerufen, und der Code nach `listen` läuft sofort weiter.

```dart
final ticks = Stream.fromIterable([1, 2]);
ticks.listen((tick) => print('tick $tick'));
print('not waiting');
// not waiting
// tick 1
// tick 2
```

`listen` akzeptiert außerdem einen benannten Parameter `onDone`, eine Funktion ohne Argumente, die aufgerufen wird, wenn der Stream endet:

```dart
final ticks2 = Stream.fromIterable([1, 2]);
ticks2.listen((tick) => print(tick), onDone: () => print('no more ticks'));
```

# --instructions--

Vervollständige den Code so, dass der Stream abgehört wird, jeder Buchstabe ausgegeben wird und `done` ausgegeben wird, wenn der Stream endet. Das Programm muss `listening`, dann `a`, `b` und `done` ausgeben.

# --seed--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.[/]((letter) => print(letter), [/]: () => print('done'));
  print('listening');
}
```

# --answers--

- listen
- onDone
- onError
- forEach
- await

# --solutions--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.listen((letter) => print(letter), onDone: () => print('done'));
  print('listening');
}
```

# --output--

listening
a
b
done
