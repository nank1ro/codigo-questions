---
language: dart
exerciseType: 1
---

# --description--

Wie `Iterable` hat auch ein `Stream` Methoden, die aus einem bestehenden Stream einen **neuen Stream** bauen:

- `map` transformiert jedes Event
- `where` behält nur die Events, die eine Bedingung erfüllen
- `take` hört nach einer bestimmten Anzahl von Events auf

```dart
final numbers = Stream.fromIterable([1, 2, 3, 4, 5, 6]);
final firstOdds = numbers.where((n) => n.isOdd).take(2);
print(await firstOdds.toList()); // [1, 3]
```

Diese Methoden sind **träge**: Nichts läuft, bis jemand den entstehenden Stream abhört. Sie lassen sich verketten, und der Quell-Stream wird nie verändert.

# --instructions--

Schreibe eine Funktion `evenSquares`, die einen `Stream<int>` `source` entgegennimmt und einen `Stream<int>` mit dem Quadrat jedes **geraden** Werts von `source` liefert, in derselben Reihenfolge.

Beispiel:
```dart
final source = Stream.fromIterable([1, 2, 3, 4]);
print(await evenSquares(source).toList()); // [4, 16]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`evenSquares` eines Streams aus `1, 2, 3, 4` sollte `4, 16` senden

```dart
  test('test1', () async {
    expect(await evenSquares(Stream.fromIterable([1, 2, 3, 4])).toList(), [4, 16], reason: '--err-t1--');
  });
```

`evenSquares` eines Streams aus `6, 5, 10` sollte `36, 100` senden

```dart
  test('test2', () async {
    expect(await evenSquares(Stream.fromIterable([6, 5, 10])).toList(), [36, 100], reason: '--err-t2--');
  });
```

`evenSquares` eines Streams aus `1, 3` sollte nichts senden und sollte einen `Stream<int>` liefern

```dart
  test('test3', () async {
    expect(await evenSquares(Stream.fromIterable([1, 3])).toList(), <int>[], reason: '--err-t3--');
    expect(evenSquares(Stream.fromIterable([2])), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> evenSquares(Stream<int> source) {
  return source.where((n) => n.isEven).map((n) => n * n);
}
```
