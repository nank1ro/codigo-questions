---
language: dart
exerciseType: 4
---

# --description--

Ein `async*`-Generator kann **jedes Event eines anderen Streams** mit `yield*` (yield-star) weiterreichen. Das ist wie eine `await for`-Schleife, die jeden Wert ausgibt, nur in einer Zeile:

```dart
Stream<int> ones() async* {
  yield 1;
  yield 1;
}

Stream<int> sequence() async* {
  yield 0;
  yield* ones();
  yield 2;
}
// sequence() emits 0, 1, 1, 2
```

Der äußere Stream macht mit seinen eigenen `yield`s weiter, sobald der innere Stream beendet ist.

# --instructions--

Sortiere die Elemente, um eine `async*`-Funktion `pair` zu definieren, die `1` und `2` ausgibt, eine `async*`-Funktion `all`, die `0`, dann jeden Wert von `pair()`, dann `3` ausgibt, und ein `main`, das jeden Wert von `all()` ausgibt. Definiere zuerst `pair`, dann `all`, dann `main`. Das Programm muss Folgendes ausgeben:

```
0
1
2
3
```

# --answers--

```dart
Stream<int> pair() async* {
```

```dart
  yield 1;
```

```dart
  yield 2;
```

```dart
}
```

```dart
Stream<int> all() async* {
```

```dart
  yield 0;
```

```dart
  yield* pair();
```

```dart
  yield 3;
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  await for (final n in all()) {
```

```dart
    print(n);
```

```dart
  }
```

```dart
}
```

# --solutions--

```dart
Stream<int> pair() async* {
  yield 1;
  yield 2;
}

Stream<int> all() async* {
  yield 0;
  yield* pair();
  yield 3;
}

Future<void> main() async {
  await for (final n in all()) {
    print(n);
  }
}
```

# --output--

0
1
2
3
