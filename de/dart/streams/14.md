---
language: dart
exerciseType: 2
---

# --description--

Ein Controller kann mit dem benannten Konstruktor `StreamController<T>.broadcast()` direkt einen Broadcast-Stream erzeugen. Sein `stream` akzeptiert beliebig viele Zuhörer, und jedes Event wird an alle geliefert, in der Reihenfolge ihres Abonnements:

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>.broadcast();
  controller.stream.listen((msg) => print('first: $msg'));
  controller.stream.listen((msg) => print('second: $msg'));
  controller.add('hi');
  controller.close();
}
// first: hi
// second: hi
```

Wie jeder Broadcast-Stream puffert er nicht: Events, die hinzugefügt werden, bevor ein Zuhörer abonniert, sind für diesen Zuhörer verloren.

# --instructions--

Vervollständige den Code, um einen Broadcast-Controller zu erzeugen, zwei Zuhörer `A` und `B` zu abonnieren, die Werte `1` und `2` zu senden und dann den Controller zu schließen, sodass das Programm `A got 1`, `B got 1`, `A got 2`, `B got 2` ausgibt.

# --seed--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.[/]();
  controller.stream.[/]((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.[/](1);
  controller.add(2);
  controller.[/]();
}
```

# --answers--

- broadcast
- listen
- add
- close
- asBroadcastStream
- subscribe
- sink

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.broadcast();
  controller.stream.listen((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
```

# --output--

A got 1
B got 1
A got 2
B got 2
