---
language: dart
exerciseType: 1
---

# --description--

Im Hintergrund wird jedes Zeichen eines Strings als Zahl gespeichert, seine **Code-Unit** (ein UTF-16-Code). `.codeUnitAt(index)` gibt den Code eines Zeichens zurück und `.codeUnits` die ganze Liste. `String.fromCharCode(code)` macht das Gegenteil und baut aus einem Code einen String:

```dart
var word = 'AB';
print(word.codeUnitAt(0));         // 65
print(word.codeUnits);             // [65, 66]
print(String.fromCharCode(67));    // C
```

Aufeinanderfolgende Buchstaben haben aufeinanderfolgende Codes: `'A'` ist 65, `'B'` ist 66, und so weiter.

# --instructions--

Speichere für den String `letter` in einem `int` namens `code` seine Code-Unit, und in einem `String` namens `next` das Zeichen, dessen Code `code + 1` ist.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var letter = 'D';
```

# --after-seed--

```dart
  return [code, next];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`code` sollte `68` sein

```dart
  test('test1', () {
    expect(result()[0], 68, reason: '--err-t1--');
  });
```

`next` sollte `'E'` sein

```dart
  test('test2', () {
    expect(result()[1], 'E', reason: '--err-t2--');
  });
```

`next` sollte ein `String` sein

```dart
  test('test3', () {
    expect(result()[1], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var letter = 'D';
int code = letter.codeUnitAt(0);
String next = String.fromCharCode(code + 1);
```
