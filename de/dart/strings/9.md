---
language: dart
exerciseType: 2
---

# --description--

Die Methode `.replaceAll(from, to)` gibt einen neuen String zurück, in dem **jedes** Vorkommen von `from` durch `to` ersetzt wird. `.replaceFirst(from, to)` ersetzt nur das erste:

```dart
var text = 'a-b-c';
print(text.replaceAll('-', '+'));    // a+b+c
print(text.replaceFirst('-', '+'));  // a+b-c
```

# --instructions--

Vervollständige den Code, um jedes `cats` durch `dogs` zu ersetzen und das Ergebnis auszugeben.

# --seed--

```dart
var text = 'cats here, cats there';
print(text.[/]('cats', [/]));
```

# --answers--

- replaceAll
- 'dogs'
- replaceFirst
- replace
- 'cats'
- dogs

# --solutions--

```dart
var text = 'cats here, cats there';
print(text.replaceAll('cats', 'dogs'));
```

# --output--

dogs here, dogs there
