---
language: dart
exerciseType: 1
---

# --description--

Da Strings unveränderlich sind, erzeugt das Aufbauen eines langen Textes mit `+=` in einer Schleife bei jedem Schritt einen neuen String. Ein **StringBuffer** sammelt Textstücke effizient und erzeugt den endgültigen String erst, wenn du danach fragst:

- `.write(value)` hängt einen Wert an (jeder Typ wird in Text umgewandelt)
- `.writeln(value)` hängt den Wert gefolgt von einem Zeilenumbruch an
- `.toString()` gibt den bisher aufgebauten String zurück

```dart
var buffer = StringBuffer();
buffer.write('Hello');
buffer.write(', ');
buffer.writeln('Dart!');
buffer.write(42);
print(buffer.toString()); // Hello, Dart!\n42
```

# --instructions--

Baue für die Liste `items` mithilfe eines `StringBuffer` einen `String` namens `menu`, bei dem jedes Element auf einer eigenen Zeile steht, mit seiner ab `1` beginnenden Position und einem Punkt davor, wie `1. milk`. Jede Zeile muss mit einem Zeilenumbruch enden (verwende `.writeln()`).

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var items = ['milk', 'eggs', 'bread'];
```

# --after-seed--

```dart
  return menu;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`menu` sollte `'1. milk\n2. eggs\n3. bread\n'` sein

```dart
  test('test1', () {
    expect(result(), '1. milk\n2. eggs\n3. bread\n', reason: '--err-t1--');
  });
```

`menu` sollte ein `String` sein

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var items = ['milk', 'eggs', 'bread'];
var buffer = StringBuffer();
for (var i = 0; i < items.length; i++) {
  buffer.writeln('${i + 1}. ${items[i]}');
}
String menu = buffer.toString();
```
