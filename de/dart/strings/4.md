---
language: dart
exerciseType: 1
---

# --description--

Jeder String kennt über seine `.length`-Eigenschaft, wie viele Zeichen er enthält. Leerzeichen und Satzzeichen zählen ebenfalls als Zeichen:

```dart
var word = 'hello';
print(word.length);      // 5
print('a b'.length);     // 3
```

Du kannst ein einzelnes Zeichen mit eckigen Klammern und seinem **Index** lesen, beginnend bei `0`. Das Ergebnis ist ein einzelnes Zeichen als `String`:

```dart
print(word[0]);                // h
print(word[word.length - 1]);  // o
```

Das Lesen eines Index außerhalb des Strings (wie `word[5]`) wirft einen Fehler.

# --instructions--

Speichere für den String `word` in einem `int` namens `size` seine Anzahl an Zeichen, in einem `String` namens `first` sein erstes Zeichen und in einem `String` namens `last` sein letztes Zeichen.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var word = 'Flutter';
```

# --after-seed--

```dart
  return [size, first, last];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`size` sollte `7` sein

```dart
  test('test1', () {
    expect(result()[0], 7, reason: '--err-t1--');
  });
```

`first` sollte `'F'` sein

```dart
  test('test2', () {
    expect(result()[1], 'F', reason: '--err-t2--');
  });
```

`last` sollte `'r'` sein

```dart
  test('test3', () {
    expect(result()[2], 'r', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var word = 'Flutter';
int size = word.length;
String first = word[0];
String last = word[word.length - 1];
```
