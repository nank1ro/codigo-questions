---
language: dart
exerciseType: 1
---

# --description--

Zwei Strings können mit dem `+`-Operator zu einem neuen zusammengefügt werden, das nennt man **Verkettung**:

```dart
var greeting = 'Hello' + ', ' + 'Dart';
print(greeting); // Hello, Dart
```

Dart fügt außerdem zwei String-**Literale** zusammen, die direkt nebeneinander geschrieben werden, ganz ohne Operator. Das ist praktisch, um einen langen Text auf mehrere Zeilen aufzuteilen:

```dart
var text = 'Hello, '
    'Dart';
print(text); // Hello, Dart
```

Nur Strings können mit `+` verkettet werden: `'Age: ' + 30` ist ein Kompilierfehler, weil `30` ein `int` ist.

# --instructions--

Erstelle mithilfe der Variablen `first` und `last` einen `String` namens `full`, der den Vornamen, ein Leerzeichen und den Nachnamen enthält.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var first = 'Ada';
var last = 'Lovelace';
```

# --after-seed--

```dart
  return full;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`full` sollte `'Ada Lovelace'` sein

```dart
  test('test1', () {
    expect(result(), 'Ada Lovelace', reason: '--err-t1--');
  });
```

`full` sollte ein `String` sein

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var first = 'Ada';
var last = 'Lovelace';
String full = first + ' ' + last;
```
