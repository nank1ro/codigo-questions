---
language: dart
exerciseType: 4
---

# --description--

Von Nutzern eingegebener Text hat oft zusätzliche Leerzeichen drumherum. Die Methode `.trim()` gibt eine Kopie des Strings ohne führende und nachgestellte Leerzeichen (Leerzeichen, Tabs und Zeilenumbrüche) zurück. `.trimLeft()` und `.trimRight()` entfernen sie nur auf einer Seite:

```dart
var input = '   hello  ';
print('[${input.trim()}]');      // [hello]
print('[${input.trimLeft()}]');  // [hello  ]
```

# --instructions--

Sortiere die Codezeilen, um die umgebenden Leerzeichen von `input` zu entfernen und den bereinigten Text in Großbuchstaben auszugeben.

# --answers--

```dart
void main() {
```

```dart
  var input = '  hello  ';
```

```dart
  var clean = input.trim();
```

```dart
  print(clean.toUpperCase());
```

```dart
}
```

# --solutions--

```dart
void main() {
  var input = '  hello  ';
  var clean = input.trim();
  print(clean.toUpperCase());
}
```

# --output--

HELLO
