---
language: dart
exerciseType: 1
---

# --description--

Die Methode `.substring(start, end)` gibt den Teil eines Strings vom Index `start` bis, **aber ohne**, Index `end` zurück. Wenn du `end` weglässt, wird alles bis zum Ende des Strings genommen:

```dart
var text = 'Hello Dart';
print(text.substring(0, 5));  // Hello
print(text.substring(6));     // Dart
```

# --instructions--

Speichere für den String `date` im Format `YYYY-MM-DD` in einem `String` namens `year` die ersten vier Zeichen und in einem `String` namens `day` die letzten zwei Zeichen.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var date = '2024-06-15';
```

# --after-seed--

```dart
  return [year, day];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`year` sollte `'2024'` sein

```dart
  test('test1', () {
    expect(result()[0], '2024', reason: '--err-t1--');
  });
```

`day` sollte `'15'` sein

```dart
  test('test2', () {
    expect(result()[1], '15', reason: '--err-t2--');
  });
```

`year` sollte ein `String` sein

```dart
  test('test3', () {
    expect(result()[0], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var date = '2024-06-15';
String year = date.substring(0, 4);
String day = date.substring(8);
```
