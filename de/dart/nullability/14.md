---
language: dart
exerciseType: 1
---

# --description--

Viele Bibliotheksfunktionen verwenden `null`, um zu melden, dass etwas **nicht getan werden konnte**. Das Umwandeln eines Strings in eine Zahl ist das klassische Beispiel: `int.parse` wirft eine `FormatException`, wenn der Text keine Zahl ist, während `int.tryParse` stattdessen `null` zurückgibt und dir die Entscheidung überlässt, was zu tun ist:

```dart
print(int.tryParse('42'));  // 42
print(int.tryParse('4x2')); // null
print(int.tryParse(''));    // null
```

Der Rückgabetyp von `int.tryParse` ist `int?`, also gilt alles, was du gelernt hast: `??` für einen Standardwert, `?.` zum Verketten und eine `if`-Prüfung für die Promotion. `double.tryParse` funktioniert genauso.

# --instructions--

Schreibe eine Funktion `parseOrZero`, die einen `String`-Parameter `text` entgegennimmt und die darin enthaltene Ganzzahl zurückgibt, oder `0`, wenn der Text keine gültige Ganzzahl ist. Verwende `int.tryParse`.

Example:
```dart
print(parseOrZero('42'));  // 42
print(parseOrZero('abc')); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parseOrZero('42')` sollte `42` sein

```dart
  test('test1', () {
    expect(parseOrZero('42'), 42, reason: '--err-t1--');
  });
```

`parseOrZero('-5')` sollte `-5` sein

```dart
  test('test2', () {
    expect(parseOrZero('-5'), -5, reason: '--err-t2--');
  });
```

`parseOrZero('abc')` und `parseOrZero('')` sollten `0` sein

```dart
  test('test3', () {
    expect(parseOrZero('abc'), 0, reason: '--err-t3--');
    expect(parseOrZero(''), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int parseOrZero(String text) {
  return int.tryParse(text) ?? 0;
}
```
