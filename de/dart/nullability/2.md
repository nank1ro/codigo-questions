---
language: dart
exerciseType: 1
---

# --description--

Eine nullable Variable, die **ohne Wert** deklariert wird, beginnt als `null`, sodass `= null` weggelassen werden kann:

```dart
int? age;
print(age); // null
```

Eine nicht-nullable Variable hat keinen solchen Standardwert: Dart weigert sich, Code zu kompilieren, der sie liest, bevor ein Wert zugewiesen wurde.

```dart
int count;
print(count); // error: 'count' must be assigned before it can be used
```

# --instructions--

Deklariere eine nullable `String`-Variable `email` ohne Wert und eine `String`-Variable `username` mit dem Wert `'ada'`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart

```

# --after-seed--

```dart
  return [email, username];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`email` sollte `null` sein

```dart
  test('test1', () {
    expect(result()[0], isNull, reason: '--err-t1--');
  });
```

`username` sollte `'ada'` sein

```dart
  test('test2', () {
    expect(result()[1], 'ada', reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String? email;
String username = 'ada';
```
