---
language: dart
exerciseType: 4
---

# --description--

Die Nullbarkeit bestimmt, wie du **benannte Parameter** deklarierst. Ein benannter Parameter mit einem nullable Typ ist optional: Lässt der Aufrufer ihn weg, ist er einfach `null`.

```dart
String label({String? title}) => title ?? 'untitled';

print(label());               // untitled
print(label(title: 'Notes')); // Notes
```

Ein benannter Parameter mit einem nicht-nullable Typ und ohne Standardwert hätte beim Weglassen keinen Wert, daher verlangt Dart von dir, ihn als `required` zu markieren; der Aufrufer muss ihn dann immer übergeben:

```dart
String label({required String name, String? title}) { ... }

label(name: 'Ada');               // ok
label(name: 'Ada', title: 'Dr.'); // ok
label(title: 'Dr.');              // error: the named parameter 'name' is required
```

# --instructions--

Sortiere die Elemente, um eine Funktion `label` mit einem erforderlichen benannten Parameter `name` und einem optionalen nullable benannten Parameter `title` zu definieren, die `name` allein zurückgibt, wenn `title` `null` ist, und andernfalls `'$title $name'`. Definiere zuerst die Funktion, dann `main`. Das Programm muss Folgendes ausgeben:

```
Ada
Countess Lovelace
```

# --answers--

```dart
String label({required String name, String? title}) {
```

```dart
  if (title == null) return name;
```

```dart
  return '$title $name';
```

```dart
}
```

```dart
void main() {
```

```dart
  print(label(name: 'Ada'));
```

```dart
  print(label(name: 'Lovelace', title: 'Countess'));
```

```dart
}
```

# --solutions--

```dart
String label({required String name, String? title}) {
  if (title == null) return name;
  return '$title $name';
}

void main() {
  print(label(name: 'Ada'));
  print(label(name: 'Lovelace', title: 'Countess'));
}
```

# --output--

Ada
Countess Lovelace
