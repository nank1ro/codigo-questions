---
language: dart
exerciseType: 1
---

# --description--

Einen nullable Wert mit `if` zu prüfen ist sicherer als `!`, und Dart belohnt dich dafür. Nach einer Prüfung wie `if (x != null)` weiß der Compiler, dass `x` innerhalb des Blocks nicht `null` sein kann, und behandelt `x` dort als nicht-nullable. Das nennt man **Type Promotion**:

```dart
int twice(int? n) {
  if (n != null) {
    return n * 2; // here n is an int, no ! needed
  }
  return 0;
}
```

Die Promotion funktioniert auch nach einem vorzeitigen `return`:

```dart
int twice(int? n) {
  if (n == null) return 0;
  return n * 2; // n is an int from here on
}
```

Die Promotion gilt für **lokale Variablen und Parameter**, deren Wert sich zwischen Prüfung und Verwendung nicht heimlich ändern kann.

# --instructions--

Schreibe eine Funktion `half`, die einen nullable `int`-Parameter `n` entgegennimmt und `n` geteilt durch `2` mit ganzzahliger Division `~/` zurückgibt, oder `0`, wenn `n` `null` ist. Verwende eine `if`-Prüfung auf `null`, nicht `!`.

Example:
```dart
print(half(10));   // 5
print(half(null)); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`half(10)` sollte `5` sein

```dart
  test('test1', () {
    expect(half(10), 5, reason: '--err-t1--');
  });
```

`half(7)` sollte `3` sein

```dart
  test('test2', () {
    expect(half(7), 3, reason: '--err-t2--');
  });
```

`half(null)` sollte `0` sein

```dart
  test('test3', () {
    expect(half(null), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int half(int? n) {
  if (n != null) {
    return n ~/ 2;
  }
  return 0;
}
```
