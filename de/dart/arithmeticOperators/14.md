---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere zwei Top-Level-`int`-Variablen: `total` mit dem Wert `29` und `perGroup` mit dem Wert `4`. Deklariere dann eine Top-Level-`int`-Variable namens `groups`, die speichert, wie viele vollständige Gruppen von `4` in `29` passen (verwende Ganzzahldivision `~/`).

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`groups` sollte gleich `7` sein

```dart
  test('test1', () {
    expect(groups, 7, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 29;
int perGroup = 4;
int groups = total ~/ perGroup;

void main() {
  print(groups);
}
```
