---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere zwei Top-Level-`int`-Variablen: `x` mit dem Wert `25` und `y` mit dem Wert `9`. Deklariere dann eine Top-Level-`int`-Variable namens `diff`, die das Ergebnis der Subtraktion von `y` von `x` speichert.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`diff` sollte gleich `16` sein

```dart
  test('test1', () {
    expect(diff, 16, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 25;
int y = 9;
int diff = x - y;

void main() {
  print(diff);
}
```
