---
language: dart
exerciseType: 1
---

# --instructions--

Ein Shop verkauft `itemCount` Artikel zu je `pricePerItem` Dollar, wobei ein `discount` Betrag vom Gesamtbetrag abgezogen wird. Deklariere diese drei Top-Level-`int`-Variablen: `itemCount` = `6`, `pricePerItem` = `15`, `discount` = `10`. Deklariere dann eine Top-Level-`int`-Variable namens `totalPrice`, die den Gesamtbetrag nach dem Rabatt berechnet.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`totalPrice` sollte gleich `80` sein

```dart
  test('test1', () {
    expect(totalPrice, 80, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int itemCount = 6;
int pricePerItem = 15;
int discount = 10;
int totalPrice = itemCount * pricePerItem - discount;

void main() {
  print(totalPrice);
}
```
