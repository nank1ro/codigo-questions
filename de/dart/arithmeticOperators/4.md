---
language: dart
exerciseType: 2
---

# --description--

Der **Divisionsoperator** `/` teilt einen Wert durch einen anderen. In Dart gibt `/` **immer eine `double` zurück**, auch wenn das Ergebnis eine ganze Zahl ist.

```dart
int a = 10;
int b = 4;
double result = a / b; // 2.5

double exact = 10 / 2; // 5.0  (not 5!)
```

Verwende immer eine `double`-Variable, um das Ergebnis von `/` zu speichern.

# --instructions--

Ergänze den Code, um `9` durch `4` zu dividieren und das Ergebnis in `result` zu speichern.

# --seed--

```dart
void main() {
  double result = 9 [/] 4;
  print(result);
}
```

# --answers--

- /
- *
- +
- -

# --solutions--

```dart
void main() {
  double result = 9 / 4;
  print(result);
}
```

# --output--

2.25
