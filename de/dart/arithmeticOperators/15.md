---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere drei Top-Level-`int`-Variablen: `a` mit dem Wert `10`, `b` mit dem Wert `3` und `c` mit dem Wert `2`. Deklariere dann:
- `sum` = `a + b`
- `product` = `b * c`
- `combined` = `sum - product`

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` sollte `13` sein, `product` sollte `6` sein und `combined` sollte `7` sein

```dart
  test('test1', () {
    expect(sum, 13, reason: '--err-t1--');
    expect(product, 6, reason: '--err-t2--');
    expect(combined, 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 10;
int b = 3;
int c = 2;
int sum = a + b;
int product = b * c;
int combined = sum - product;

void main() {
  print(combined);
}
```
