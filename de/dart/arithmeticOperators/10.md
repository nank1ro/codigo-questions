---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere zwei Top-Level-`int`-Variablen: `a` mit dem Wert `12` und `b` mit dem Wert `8`. Deklariere dann eine dritte Top-Level-`int`-Variable namens `total`, die ihre Summe speichert.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`a` sollte `12` sein, `b` sollte `8` sein und `total` sollte `20` sein

```dart
  test('test1', () {
    expect(a, 12, reason: '--err-t1--');
    expect(b, 8, reason: '--err-t2--');
    expect(total, 20, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 12;
int b = 8;
int total = a + b;

void main() {
  print(total);
}
```
