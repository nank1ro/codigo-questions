---
language: dart
exerciseType: 1
---

# --instructions--

Verwende den Modulo-Operator `%`, um den Rest zu finden, wenn `17` durch `5` geteilt wird. Speichere das Ergebnis in einer Top-Level-Variable namens `remainder`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Die Variable `remainder` sollte gleich `2` sein

```dart
  test('test1', () {
    expect(remainder, 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 17 % 5;

void main() {
  print(remainder);
}
```
