---
language: dart
exerciseType: 2
---

# --description--

Der **Subtraktionsoperator** `-` subtrahiert einen Wert von einem anderen.

```dart
int a = 10;
int b = 4;
int diff = a - b; // 6
```

Das Ergebnis kann negativ sein, wenn der zweite Operand größer als der erste ist.

# --instructions--

Ergänze den Code, um `9` von `20` zu subtrahieren und das Ergebnis in `diff` zu speichern.

# --seed--

```dart
void main() {
  int diff = 20 [/] 9;
  print(diff);
}
```

# --answers--

- -
- +
- *
- /

# --solutions--

```dart
void main() {
  int diff = 20 - 9;
  print(diff);
}
```

# --output--

11
