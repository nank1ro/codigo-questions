---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere eine Top-Level-`double`-Variable namens `result`, die das Ergebnis der Division von `7` durch `2` speichert. Denke daran, dass `/` in Dart immer eine `double` zurückgibt.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` sollte eine `double` mit dem Wert `3.5` sein

```dart
  test('test1', () {
    expect(result, isA<double>(), reason: '--err-t1--');
    expect(result, closeTo(3.5, 0.001), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double result = 7 / 2;

void main() {
  print(result);
}
```
