---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere eine boolesche Variable namens `isEmpty` und weise ihr den Wert `false` zu.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isEmpty` muss gleich `false` sein.

```dart
  test('test1', () {
    expect(isEmpty, false, reason: '--err-t1--');
  });
```

`isEmpty` muss vom Typ `bool` sein.

```dart
  test('test2', () {
    expect(isEmpty, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isEmpty = false;
```
