---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere eine boolesche Variable namens `isHidden` und weise ihr den Wert `false` zu.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHidden` muss gleich `false` sein.

```dart
  test('test1', () {
    expect(isHidden, false, reason: '--err-t1--');
  });
```

`isHidden` muss vom Typ `bool` sein.

```dart
  test('test2', () {
    expect(isHidden, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isHidden = false;
```
