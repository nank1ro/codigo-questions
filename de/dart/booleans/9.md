---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere eine boolesche Variable namens `flag` mit dem Wert `true`, dann deklariere eine weitere boolesche Variable namens `isFlag` und verwende das Schlüsselwort `is`, um zu überprüfen, ob `flag` vom Typ `bool` ist.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`flag` muss gleich `true` sein.

```dart
  test('test1', () {
    expect(flag, true, reason: '--err-t1--');
  });
```

`isFlag` muss `true` sein (das Ergebnis der `is bool` Überprüfung).

```dart
  test('test2', () {
    expect(isFlag, true, reason: '--err-t2--');
  });
```

`isFlag` muss vom Typ `bool` sein.

```dart
  test('test3', () {
    expect(isFlag, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool flag = true;
bool isFlag = flag is bool;
```
