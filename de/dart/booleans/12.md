---
language: dart
exerciseType: 1
---

# --instructions--

Kombiniere `&&`, `||` und `!` um ein Ergebnis zu erhalten.

Deklariere eine boolesche Variable namens `result` mit diesem exakten Ausdruck: `!false && (true || false)`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` muss gleich `true` sein.

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result` muss vom Typ `bool` sein.

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = !false && (true || false);
```
