---
language: dart
exerciseType: 1
---

# --instructions--

Der Operator `||` (ODER) gibt `true` zurück, wenn **mindestens ein** Operand `true` ist.

Deklariere eine boolesche Variable namens `result`, die `false || true` mit dem Operator `||` kombiniert.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` muss gleich `true` sein.

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result` muss vom Typ `bool` sein.

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = false || true;
```
