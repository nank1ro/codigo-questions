---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere eine boolesche Variable namens `isReady` und weise ihr den Wert `true` zu.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isReady` muss gleich `true` sein.

```dart
  test('test1', () {
    expect(isReady, true, reason: '--err-t1--');
  });
```

`isReady` muss vom Typ `bool` sein.

```dart
  test('test2', () {
    expect(isReady, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isReady = true;
```
