---
language: dart
exerciseType: 2
---

# --description--

Eine boolesche Variable kann auch den Wert `false` halten.
Verwende `false`, wenn etwas nicht der Fall ist.

```dart
bool isLoggedIn = false;
print(isLoggedIn); // false
```

Genau wie `true` muss auch `false` in Kleinbuchstaben geschrieben werden.

# --instructions--

Deklariere eine boolesche Variable namens `isExpired` und weise ihr den Wert `false` zu, gib sie dann aus.

# --seed--

```dart
[/] [/] = [/];
print([/]);
```

# --answers--

- bool
- isExpired
- false
- true
- print
- String

# --solutions--

```dart
bool isExpired = false;
print(isExpired);
```

# --output--

false
