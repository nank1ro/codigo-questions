---
language: dart
exerciseType: 1
---

# --description--

Einige Methoden höherer Ordnung beantworten eine Frage über die Collection, statt eine neue zu erstellen. Sie nehmen ein Prädikat entgegen und geben ein `bool` zurück:

- `any` ist `true`, wenn **mindestens ein** Element das Prädikat erfüllt
- `every` ist `true`, wenn **alle** Elemente es erfüllen

```dart
final numbers = [1, 2, 3];
print(numbers.any((n) => n > 2));   // true
print(numbers.every((n) => n > 2)); // false
```

Beide brechen ab, sobald die Antwort feststeht: `any` beim ersten Element, das passt, `every` beim ersten, das nicht passt.

Auf einer leeren Collection ist `any` `false` und `every` ist `true`: Es gibt kein Element, das das erste belegt, und keines, das das zweite widerlegt.

# --instructions--

Schreibe zwei Funktionen, die beide eine `List<int>` `numbers` entgegennimmt:

- `hasNegative` gibt `true` zurück, wenn mindestens ein Element kleiner als `0` ist
- `allEven` gibt `true` zurück, wenn jedes Element gerade ist

Beispiel:
```dart
print(hasNegative([1, -2, 3])); // true
print(allEven([2, 4]));         // true
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasNegative([1, -2, 3])` sollte `true` sein und `hasNegative([1, 2, 3])` sollte `false` sein

```dart
  test('test1', () {
    expect(hasNegative([1, -2, 3]), true, reason: '--err-t1--');
    expect(hasNegative([1, 2, 3]), false, reason: '--err-t1--');
  });
```

`allEven([2, 4, 6])` sollte `true` sein und `allEven([2, 5])` sollte `false` sein

```dart
  test('test2', () {
    expect(allEven([2, 4, 6]), true, reason: '--err-t2--');
    expect(allEven([2, 5]), false, reason: '--err-t2--');
  });
```

Auf einer leeren Liste sollte `hasNegative` `false` sein und `allEven` `true`

```dart
  test('test3', () {
    expect(hasNegative(<int>[]), false, reason: '--err-t3--');
    expect(allEven(<int>[]), true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool hasNegative(List<int> numbers) {
  return numbers.any((n) => n < 0);
}

bool allEven(List<int> numbers) {
  return numbers.every((n) => n % 2 == 0);
}
```
