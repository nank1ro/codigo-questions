---
language: dart
exerciseType: 1
---

# --description--

`where` nimmt eine Funktion, die ein `bool` zurückgibt – ein sogenanntes **Prädikat** – und behält nur die Elemente, für die sie `true` liefert. Die Reihenfolge der übrig bleibenden Elemente ändert sich nie:

```dart
final numbers = [4, -2, 7, 0];
print(numbers.where((n) => n > 0).toList()); // [4, 7]
```

Wie `map` gibt auch `where` ein `Iterable` zurück und verändert die ursprüngliche Collection nie, daher ist `toList()` wieder das, was das Ergebnis in eine `List` verwandelt.

In anderen Sprachen heißt diese Methode `filter`; in Dart heißt sie `where`.

# --instructions--

Schreibe eine Funktion `positives`, die eine `List<int>` `numbers` entgegennimmt und eine neue `List<int>` zurückgibt, die nur die Elemente größer als `0` enthält, in ihrer ursprünglichen Reihenfolge.

Beispiel:
```dart
print(positives([4, -2, 7, 0])); // [4, 7]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`positives([4, -2, 7, 0])` sollte `[4, 7]` zurückgeben

```dart
  test('test1', () {
    expect(positives([4, -2, 7, 0]), [4, 7], reason: '--err-t1--');
  });
```

`positives` sollte die ursprüngliche Reihenfolge beibehalten und eine `List<int>` zurückgeben

```dart
  test('test2', () {
    expect(positives([9, 1, -5, 3]), [9, 1, 3], reason: '--err-t2--');
    expect(positives([1]), isA<List<int>>(), reason: '--err-t2--');
  });
```

`positives` sollte eine leere Liste zurückgeben, wenn nichts positiv ist, und darf die Liste, die sie erhält, nicht verändern

```dart
  test('test3', () {
    expect(positives([-1, -2]), <int>[], reason: '--err-t3--');
    final original = [1, -1, 2];
    positives(original);
    expect(original, [1, -1, 2], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> positives(List<int> numbers) {
  return numbers.where((n) => n > 0).toList();
}
```
