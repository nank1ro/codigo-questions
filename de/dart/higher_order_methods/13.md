---
language: dart
exerciseType: 1
---

# --description--

Jede dieser Methoden gibt ein `Iterable` zurück, und jedes `Iterable` hat dieselben Methoden wieder. Genau das ermöglicht **Verkettung**: Eine ganze Berechnung liest sich als Pipeline von links nach rechts, wobei jeder Schritt mit dem arbeitet, was der vorherige erzeugt hat.

```dart
final words = ['kiwi', 'fig', 'banana', 'date'];
print(words.where((w) => w.length == 4).map((w) => w.toUpperCase()).toList());
// [KIWI, DATE]
```

Nur der letzte Schritt braucht `toList()`: Ruft man es in der Mitte auf, baut man eine Liste, die niemand behält.

Der Typ ändert sich entlang der Kette, und damit auch das, was die nächste Funktion erhält: Nach `where` auf einer `List<String>` hast du immer noch Strings, aber nach `map((w) => w.length)` sieht der nächste Schritt Zahlen.

Da jeder Schritt lazy ist, kommt es bei der erledigten Arbeit auf die Reihenfolge an, nicht nur beim Ergebnis: Erst mit `where` zu filtern bedeutet, dass `map` auf weniger Elementen aufgerufen wird.

# --instructions--

Schreibe eine Funktion `shortUpper`, die eine `List<String>` `words` entgegennimmt und eine neue `List<String>` mit den Wörtern von höchstens `4` Zeichen zurückgibt, in Großbuchstaben, in ihrer ursprünglichen Reihenfolge.

Beispiel:
```dart
print(shortUpper(['kiwi', 'banana', 'fig'])); // [KIWI, FIG]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shortUpper(['kiwi', 'banana', 'fig'])` sollte `['KIWI', 'FIG']` zurückgeben

```dart
  test('test1', () {
    expect(shortUpper(['kiwi', 'banana', 'fig']), ['KIWI', 'FIG'],
        reason: '--err-t1--');
  });
```

Wörter mit genau `4` Zeichen sollten behalten werden, längere verworfen

```dart
  test('test2', () {
    expect(shortUpper(['date', 'grape']), ['DATE'], reason: '--err-t2--');
    expect(shortUpper(['melon']), <String>[], reason: '--err-t2--');
  });
```

`shortUpper` sollte eine `List<String>` zurückgeben und darf die Liste, die es erhält, nicht verändern

```dart
  test('test3', () {
    expect(shortUpper(['a']), isA<List<String>>(), reason: '--err-t3--');
    final original = ['fig', 'banana'];
    shortUpper(original);
    expect(original, ['fig', 'banana'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<String> shortUpper(List<String> words) {
  return words.where((w) => w.length <= 4).map((w) => w.toUpperCase()).toList();
}
```
