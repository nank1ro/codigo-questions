---
language: dart
exerciseType: 1
---

# --description--

`reduce` ist der kürzere Verwandte von `fold`. Es nimmt keinen Startwert: Das **erste Element** ist der Start-Akkumulator, und die Funktion läuft für jedes übrige Element:

```dart
final numbers = [1, 2, 3, 4];
print(numbers.reduce((a, b) => a + b)); // 10
```

Da es keinen Startwert gibt, hat das Ergebnis immer **denselben Typ wie die Elemente**, und der Aufruf von `reduce` auf einer leeren Collection wirft einen `StateError`: Es gibt kein erstes Element als Startpunkt. `fold` hat dieses Problem nicht, weshalb es die sicherere Standardwahl ist.

`reduce` ist am besten, wenn du ein Element unter vielen suchst, zum Beispiel das größte:

```dart
print(numbers.reduce((a, b) => a > b ? a : b)); // 4
```

# --instructions--

Schreibe eine Funktion `largest`, die eine nicht-leere `List<int>` `numbers` entgegennimmt und mit `reduce` das größte Element zurückgibt.

Beispiel:
```dart
print(largest([3, 9, 4])); // 9
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest([3, 9, 4])` sollte `9` zurückgeben

```dart
  test('test1', () {
    expect(largest([3, 9, 4]), 9, reason: '--err-t1--');
  });
```

`largest` sollte funktionieren, wenn das größte Element zuerst oder zuletzt kommt, und bei einem einzelnen Element

```dart
  test('test2', () {
    expect(largest([10, 2, 5]), 10, reason: '--err-t2--');
    expect(largest([1, 2, 8]), 8, reason: '--err-t2--');
    expect(largest([7]), 7, reason: '--err-t2--');
  });
```

`largest` sollte mit negativen Zahlen funktionieren und darf die Liste, die es erhält, nicht verändern

```dart
  test('test3', () {
    expect(largest([-4, -1, -9]), -1, reason: '--err-t3--');
    final original = [2, 5, 1];
    largest(original);
    expect(original, [2, 5, 1], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int largest(List<int> numbers) {
  return numbers.reduce((a, b) => a > b ? a : b);
}
```
