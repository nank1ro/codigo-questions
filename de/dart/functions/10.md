---
language: dart
exerciseType: 1
---

# --instructions--

Schreibe eine **Pfeilfunktion** `square`, die einen `int`-Parameter `n` nimmt und `n * n` zurückgibt.

Verwende die `=>`-Syntax (keine geschweiften Klammern, kein `return`-Schlüsselwort).

Beispiel:
```dart
print(square(4));  // 16
print(square(5));  // 25
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`square(4)` muss `16` zurückgeben
```dart
  test('test1', () {
    expect(square(4), 16, reason: '--err-t1--');
  });
```

`square(5)` muss `25` zurückgeben
```dart
  test('test2', () {
    expect(square(5), 25, reason: '--err-t2--');
  });
```

`square(0)` muss `0` zurückgeben
```dart
  test('test3', () {
    expect(square(0), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int square(int n) => n * n;
```
