---
language: dart
exerciseType: 1
---

# --instructions--

Schreibe eine Funktion `isEven`, die einen `int`-Parameter `n` nimmt und `true` zurückgibt, wenn `n` gerade ist, oder `false` andernfalls.

Beispiel:
```dart
print(isEven(4));  // true
print(isEven(7));  // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isEven(4)` muss `true` zurückgeben
```dart
  test('test1', () {
    expect(isEven(4), true, reason: '--err-t1--');
  });
```

`isEven(7)` muss `false` zurückgeben
```dart
  test('test2', () {
    expect(isEven(7), false, reason: '--err-t2--');
  });
```

`isEven(0)` muss `true` zurückgeben
```dart
  test('test3', () {
    expect(isEven(0), true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isEven(int n) {
  return n % 2 == 0;
}
```
