---
language: dart
exerciseType: 1
---

# --description--

Dart unterstützt außerdem **optionale positionelle Parameter** — Parameter, die in eckige Klammern `[]` eingeschlossen sind. Sie werden anhand ihrer Position übergeben, können vom Aufrufer weggelassen werden und können einen Standardwert haben:

```dart
String greet([String name = "World"]) {
  return "Hello, $name!";
}

void main() {
  print(greet());         // Hello, World!
  print(greet("Alice"));  // Hello, Alice!
}
```

# --instructions--

Schreibe eine Funktion `greetUser`, die einen **optionalen positionellen** `String`-Parameter `name` mit einem **Standardwert** von `"World"` nimmt und die Zeichenkette `"Hello, <name>!"` zurückgibt.

Beispiel:
```dart
print(greetUser());          // Hello, World!
print(greetUser("Alice"));   // Hello, Alice!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greetUser()` muss `"Hello, World!"` zurückgeben
```dart
  test('test1', () {
    expect(greetUser(), "Hello, World!", reason: '--err-t1--');
  });
```

`greetUser("Alice")` muss `"Hello, Alice!"` zurückgeben
```dart
  test('test2', () {
    expect(greetUser("Alice"), "Hello, Alice!", reason: '--err-t2--');
  });
```

`greetUser("Bob")` muss `"Hello, Bob!"` zurückgeben
```dart
  test('test3', () {
    expect(greetUser("Bob"), "Hello, Bob!", reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String greetUser([String name = "World"]) {
  return "Hello, $name!";
}
```
