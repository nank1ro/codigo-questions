---
language: dart
exerciseType: 1
---

# --instructions--

Schreibe eine Funktion `fullName`, die zwei **benannte Parameter** `firstName` und `lastName` (beide `required String`) nimmt und den vollständigen Namen als einzelne Zeichenkette mit einem Leerzeichen dazwischen zurückgibt.

Beispiel:
```dart
print(fullName(firstName: "Jane", lastName: "Doe")); // Jane Doe
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`fullName(firstName: "Jane", lastName: "Doe")` muss `"Jane Doe"` zurückgeben
```dart
  test('test1', () {
    expect(fullName(firstName: "Jane", lastName: "Doe"), "Jane Doe", reason: '--err-t1--');
  });
```

`fullName(firstName: "John", lastName: "Smith")` muss `"John Smith"` zurückgeben
```dart
  test('test2', () {
    expect(fullName(firstName: "John", lastName: "Smith"), "John Smith", reason: '--err-t2--');
  });
```

`fullName(firstName: "A", lastName: "B")` muss `"A B"` zurückgeben
```dart
  test('test3', () {
    expect(fullName(firstName: "A", lastName: "B"), "A B", reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String fullName({required String firstName, required String lastName}) {
  return "$firstName $lastName";
}
```
