---
language: dart
exerciseType: 2
---

# --description--

Das Schlüsselwort `break` beendet eine Schleife sofort, unabhängig von der Schleifenbedingung.

```dart
int i = 0;
while (true) {
  if (i == 3) break;
  print(i);
  i++;
}
```

Ohne `break` würde die `while (true)`-Schleife ewig laufen. Hier hält sie an, wenn `i` den Wert `3` erreicht.

# --instructions--

Vervollständige die Schleife, die Zahlen ab `0` ausgibt und `break` verwendet, um anzuhalten, wenn `i` gleich `4` ist.

# --seed--

```dart
void main() {
  int i = 0;
  while ([/]) {
    if (i == [/]) [/];
    print(i);
    i++;
  }
}
```

# --answers--

- true
- false
- 4
- 3
- break
- continue
- return

# --solutions--

```dart
void main() {
  int i = 0;
  while (true) {
    if (i == 4) break;
    print(i);
    i++;
  }
}
```

# --output--

0
1
2
3
