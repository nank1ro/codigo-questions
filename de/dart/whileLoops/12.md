---
language: dart
exerciseType: 1
---

# --instructions--

Verwende eine `while`-Schleife mit `break`, um das erste Vielfache von `7` zu finden, das größer als `50` ist. Gib diese Zahl zurück.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` muss gleich `56` sein

```dart
  test('test1', () {
    expect(computeValue(), 56, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int result = 0;
int n = 51;
while (true) {
  if (n % 7 == 0) {
    result = n;
    break;
  }
  n++;
}
```
