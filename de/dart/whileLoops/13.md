---
language: dart
exerciseType: 1
---

# --instructions--

Verwende eine `while`-Schleife mit `continue`, um nur die **ungeraden** Zahlen von `1` bis `10` (einschließlich) zu summieren. Gib die Summe zurück.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` muss gleich `25` sein

```dart
  test('test1', () {
    expect(computeValue(), 25, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sum = 0;
int i = 1;
while (i <= 10) {
  if (i % 2 == 0) {
    i++;
    continue;
  }
  sum += i;
  i++;
}
```
