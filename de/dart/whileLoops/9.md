---
language: dart
exerciseType: 1
---

# --instructions--

Schreibe eine `while`-Schleife, die von `1` bis `5` zählt und den endgültigen Zählerwert zurückgibt.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return counter;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`counter` muss gleich `5` sein

```dart
  test('test1', () {
    expect(computeValue(), 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int counter = 1;
while (counter < 5) {
  counter++;
}
```
