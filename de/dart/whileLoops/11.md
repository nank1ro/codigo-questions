---
language: dart
exerciseType: 1
---

# --instructions--

Verwende eine `do-while`-Schleife, um `3` zu einer Variable `total` beginnend bei `0` zu addieren, bis `total` den Wert `12` erreicht oder überschreitet. Gib `total` zurück.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` muss gleich `12` sein

```dart
  test('test1', () {
    expect(computeValue(), 12, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 0;
do {
  total += 3;
} while (total < 12);
```
