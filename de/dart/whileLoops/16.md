---
language: dart
exerciseType: 1
---

# --instructions--

Beginne mit `product = 1` und verwende eine `while`-Schleife, um wiederholt mit `2` zu multiplizieren, bis `product` größer oder gleich `100` ist. Gib `product` zurück.

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return product;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`product` muss gleich `128` sein

```dart
  test('test1', () {
    expect(computeValue(), 128, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int product = 1;
while (product < 100) {
  product *= 2;
}
```
