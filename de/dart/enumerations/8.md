---
language: dart
exerciseType: 2
---

# --description--

Seit Dart 3 kann ein `switch` auch als **Ausdruck** verwendet werden, der einen Wert erzeugt. Jeder Fall wird als `pattern => value` geschrieben, und die Fälle werden durch Kommas getrennt, ohne das Schlüsselwort `case` und ohne `break`:

```dart
enum Direction { north, south, east, west }

String arrow(Direction direction) => switch (direction) {
      Direction.north => '^',
      Direction.south => 'v',
      Direction.east => '>',
      Direction.west => '<',
    };
```

Wie bei der Anweisungsform muss ein switch-Ausdruck über ein Enum erschöpfend sein.

# --instructions--

Vervollständige den switch-Ausdruck, sodass `label` den ersten Buchstaben jeder Größe zurückgibt, und gib dann das Label von `Size.large` aus.

# --seed--

```dart
enum Size { small, medium, large }

String label(Size size) => [/] (size) {
      Size.small [/] 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --answers--

- switch
- =>
- case
- when
- ->
- :

# --solutions--

```dart
enum Size { small, medium, large }

String label(Size size) => switch (size) {
      Size.small => 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --output--

L
