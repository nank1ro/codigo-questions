---
language: dart
exerciseType: 1
---

# --description--

Ein erweitertes Enum kann auch **Methoden** und **Getter** deklarieren. Innerhalb von ihnen ist `this` der aktuelle Wert, sodass du direkt auf seinen `name`, `index` und seine Felder zugreifen kannst:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);

  bool get hasMoons => moons > 0;

  String describe() => '$name has $moons moon(s)';
}

print(Planet.earth.hasMoons);   // true
print(Planet.mars.describe()); // mars has 2 moon(s)
```

Ein Enum ohne Felder kann trotzdem Methoden deklarieren: Die Liste der Werte endet dann mit `;`, gefolgt von den Members.

# --instructions--

Deklariere ein Enum `Direction` mit den Werten `north`, `east`, `south` und `west`, in dieser Reihenfolge. Füge einen Getter `opposite` hinzu, der den Wert auf der gegenüberliegenden Seite zurückgibt (`north` <-> `south`, `east` <-> `west`), und eine Methode `turnRight()`, die den darauffolgenden Wert zurückgibt und dabei von `west` wieder zu `north` umschlägt.

Tipp: Kombiniere `index`, `values` und den `%`-Operator.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Direction.north.opposite` sollte `Direction.south` sein

```dart
  test('test1', () {
    expect(Direction.north.opposite, Direction.south, reason: '--err-t1--');
  });
```

`Direction.west.opposite` sollte `Direction.east` sein

```dart
  test('test2', () {
    expect(Direction.west.opposite, Direction.east, reason: '--err-t2--');
  });
```

`Direction.north.turnRight()` sollte `Direction.east` sein

```dart
  test('test3', () {
    expect(Direction.north.turnRight(), Direction.east, reason: '--err-t3--');
  });
```

`Direction.west.turnRight()` sollte `Direction.north` sein

```dart
  test('test4', () {
    expect(Direction.west.turnRight(), Direction.north, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Direction {
  north,
  east,
  south,
  west;

  Direction get opposite => Direction.values[(index + 2) % 4];

  Direction turnRight() => Direction.values[(index + 1) % 4];
}
```
