---
language: dart
exerciseType: 2
---

# --description--

Jedes Enum hat außerdem eine konstante Liste namens `values`, die alle seine Werte in Deklarationsreihenfolge enthält. Du kannst sie wie jede Liste indizieren, ihre `length` lesen oder mit `for-in` darüber iterieren:

```dart
enum Direction { north, south, east, west }

print(Direction.values.length); // 4
print(Direction.values[1]);     // Direction.south

for (var direction in Direction.values) {
  print(direction.name);
}
```

# --instructions--

Vervollständige den Code, um den Namen von `Planet.earth`, seinen Index und die Gesamtzahl der Planeten auszugeben.

# --seed--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.[/]);
  print(Planet.earth.[/]);
  print(Planet.[/].length);
}
```

# --answers--

- name
- index
- values
- ordinal
- position
- count

# --solutions--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.name);
  print(Planet.earth.index);
  print(Planet.values.length);
}
```

# --output--

earth
2
3
