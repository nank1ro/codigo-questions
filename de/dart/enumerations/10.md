---
language: dart
exerciseType: 1
---

# --description--

Seit Dart 2.17 kann ein Enum **Felder** und einen **Konstruktor** deklarieren, genau wie eine Klasse. Das nennt man ein *erweitertes Enum*. Jeder Wert übergibt dann seine eigenen Argumente an den Konstruktor:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);
}

print(Planet.mars.moons); // 2
```

Beachte die drei Regeln: Die Liste der Werte endet mit einem **Semikolon** `;`, die Felder müssen `final` sein, und der Konstruktor muss `const` sein.

# --instructions--

Deklariere ein Enum `Coin` mit einem `final int`-Feld `cents` und den Werten `penny(1)`, `nickel(5)`, `dime(10)` und `quarter(25)`, in dieser Reihenfolge.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Coin.penny.cents` sollte `1` sein und `Coin.nickel.cents` sollte `5` sein

```dart
  test('test1', () {
    expect(Coin.penny.cents, 1, reason: '--err-t1--');
    expect(Coin.nickel.cents, 5, reason: '--err-t2--');
  });
```

`Coin.dime.cents` sollte `10` sein

```dart
  test('test2', () {
    expect(Coin.dime.cents, 10, reason: '--err-t3--');
  });
```

`Coin.quarter.cents` sollte `25` sein

```dart
  test('test3', () {
    expect(Coin.quarter.cents, 25, reason: '--err-t4--');
  });
```

`Coin.values` sollte 4 Werte haben, wobei `penny` an erster Stelle steht

```dart
  test('test4', () {
    expect(Coin.values.length, 4, reason: '--err-t5--');
    expect(Coin.values[0], Coin.penny, reason: '--err-t6--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Coin {
  penny(1),
  nickel(5),
  dime(10),
  quarter(25);

  final int cents;

  const Coin(this.cents);
}
```
