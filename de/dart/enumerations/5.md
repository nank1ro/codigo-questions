---
language: dart
exerciseType: 4
---

# --description--

Eine Schleife über `values` ist der übliche Weg, um jeden Wert eines Enums zu verarbeiten. In der Schleife verhält sich der aktuelle Wert wie jedes andere Objekt, du kannst also seinen `index` und seinen `name` lesen und sie direkt in einer String-Interpolation verwenden:

```dart
enum Direction { north, south, east, west }

for (var direction in Direction.values) {
  print('${direction.index}: ${direction.name}');
}
```

# --instructions--

Sortiere die Elemente, um das Enum `Level` zu deklarieren und für jeden Wert seinen Index und seinen Namen auszugeben. Deklariere das Enum vor `main`. Das Programm muss Folgendes ausgeben:

```
0: low
1: medium
2: high
```

# --answers--

```dart
enum Level { low, medium, high }
```

```dart
void main() {
```

```dart
  for (var level in Level.values) {
```

```dart
    print('${level.index}: ${level.name}');
```

```dart
  }
}
```

# --solutions--

```dart
enum Level { low, medium, high }
void main() {
  for (var level in Level.values) {
    print('${level.index}: ${level.name}');
  }
}
```

# --output--

0: low
1: medium
2: high
