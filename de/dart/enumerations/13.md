---
language: dart
exerciseType: 4
---

# --instructions--

Sortiere die Elemente, um das erweiterte Enum `Planet` zu deklarieren und für jeden Wert seinen Namen und seine Anzahl an Monden auszugeben. Deklariere das Enum vor `main`, behalte die Werte in der Reihenfolge `mercury`, `earth`, `mars` bei, und deklariere das Feld vor dem Konstruktor. Das Programm muss Folgendes ausgeben:

```
mercury: 0
earth: 1
mars: 2
```

# --answers--

```dart
enum Planet {
```

```dart
  mercury(0),
```

```dart
  earth(1),
```

```dart
  mars(2);
```

```dart
  final int moons;
```

```dart
  const Planet(this.moons);
```

```dart
}
```

```dart
void main() {
  for (var planet in Planet.values) {
```

```dart
    print('${planet.name}: ${planet.moons}');
```

```dart
  }
}
```

# --solutions--

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);
  final int moons;
  const Planet(this.moons);
}
void main() {
  for (var planet in Planet.values) {
    print('${planet.name}: ${planet.moons}');
  }
}
```

# --output--

mercury: 0
earth: 1
mars: 2
