---
language: dart
exerciseType: 1
---

# --description--

Da `values` eine Liste ist, kannst du sie mit den Listenmethoden kombinieren, die du bereits kennst. Zum Beispiel wandelt `.map()` zusammen mit `.name` die Werte in eine Liste von Strings um:

```dart
enum Direction { north, south, east, west }

List<String> names = Direction.values.map((d) => d.name).toList();
print(names); // [north, south, east, west]
```

# --instructions--

Deklariere ein Enum `Weekday` mit den Werten `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday` und `sunday`, in dieser Reihenfolge. Schreibe dann eine Funktion `dayNames()`, die eine `List<String>` mit dem Namen jedes Wochentags in Deklarationsreihenfolge zurückgibt.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Weekday.values.length` sollte `7` sein

```dart
  test('test1', () {
    expect(Weekday.values.length, 7, reason: '--err-t1--');
  });
```

`Weekday.values[2]` sollte `Weekday.wednesday` sein

```dart
  test('test2', () {
    expect(Weekday.values[2], Weekday.wednesday, reason: '--err-t2--');
  });
```

`dayNames()` sollte die sieben Namen in der richtigen Reihenfolge zurückgeben

```dart
  test('test3', () {
    expect(dayNames(), ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Weekday { monday, tuesday, wednesday, thursday, friday, saturday, sunday }

List<String> dayNames() {
  return Weekday.values.map((day) => day.name).toList();
}
```
