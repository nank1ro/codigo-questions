---
language: dart
exerciseType: 3
---

# --description--

Wenn das, was dem `$`-Zeichen folgt, nicht als Programmbezeichner erkennbar ist, erzeugt Dart einen Kompilierungsfehler: das `$` in einem String muss immer von einem Bezeichner oder einem Ausdruck in geschweiften Klammern (`${}`) gefolgt werden.

# --instructions--

Was passiert, wenn du diesen Code ausführst?

```dart
void main() {
    print("$5 friends");
}
```

# --answers--

- Es schlägt beim Kompilieren fehl, weil `$5` keine gültige String-Interpolation ist
- Es gibt `$5 friends` aus
- Es gibt `5 friends` aus
- Es gibt nichts aus

# --solutions--

- Es schlägt beim Kompilieren fehl, weil `$5` keine gültige String-Interpolation ist
