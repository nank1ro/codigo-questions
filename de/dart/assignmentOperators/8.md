---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere eine Top-Level-`int`-Variable `points` mit dem Wert `20`. Verwende dann `+=`, um `15` hinzuzufügen.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`points` sollte `35` sein

```dart
  test('test1', () {
    expect(points, 35, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int points = 20;

void main() {
  points += 15;
  print(points);
}
```
