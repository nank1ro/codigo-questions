---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere eine Top-Level-`int`-Variable `remainder` mit dem Wert `29`. Verwende dann `%=`, um den Rest der Division durch `6` zuzuweisen.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`remainder` sollte `5` sein

```dart
  test('test1', () {
    expect(remainder, 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 29;

void main() {
  remainder %= 6;
  print(remainder);
}
```
