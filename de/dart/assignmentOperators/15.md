---
language: dart
exerciseType: 1
---

# --instructions--

Ein Geschäft wendet einen 20%igen Rabatt auf einen Artikel an. Deklariere eine `double`-Variable `price` mit dem Wert `50.0`. Verwende dann `*=`, um den Rabatt anzuwenden, indem du `price` mit `0.8` multiplizierst.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
double price = 50.0;
```

# --after-seed--

```dart
  return price;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`price` sollte `40.0` sein

```dart
  test('test1', () {
    expect(result(), closeTo(40.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double price = 50.0;
price *= 0.8;
```
