---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere eine Top-Level-`int`-Variable `width` mit dem Wert `6`. Verwende dann `*=`, um sie mit `4` zu multiplizieren.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`width` sollte `24` sein

```dart
  test('test1', () {
    expect(width, 24, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int width = 6;

void main() {
  width *= 4;
  print(width);
}
```
