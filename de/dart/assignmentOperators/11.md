---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere eine `double`-Variable `value` mit dem Wert `20.0`. Verwende dann `/=`, um sie durch `4` zu dividieren. Beachte, dass `/=` in Dart immer ein `double` erzeugt.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
double value = 20.0;
```

# --after-seed--

```dart
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value` sollte `5.0` sein

```dart
  test('test1', () {
    expect(result(), closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;
value /= 4;
```
