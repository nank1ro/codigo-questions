---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere eine Top-Level-`double`-Variable `value` mit dem Wert `20.0`. Verwende dann `/=`, um sie durch `4` zu dividieren. Beachte, dass `/=` in Dart immer ein `double` erzeugt.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value` sollte `5.0` sein

```dart
  test('test1', () {
    expect(value, closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;

void main() {
  value /= 4;
  print(value);
}
```
