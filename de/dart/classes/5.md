---
language: dart
exerciseType: 2
---

# --description--

Eine **Methode** ist eine Funktion, die innerhalb einer Klasse definiert ist. Methoden beschreiben das Verhalten eines Objekts:

```dart
class Animal {
  String name;

  Animal(this.name);

  void speak() {
    print('$name makes a sound.');
  }
}
```

Du rufst eine Methode auf einem Objekt mit Punktnotation auf: `dog.speak()`.

# --instructions--

Vervollständige die Methode `greet` in der Klasse `Person`, sodass sie `'Hello, I am $name'` ausgibt.

# --seed--

```dart
class Person {
  String name;

  Person(this.name);

  void [/]() {
    print('Hello, I am $name');
  }
}
```

# --answers--

- greet
- Greet
- hello
- method
- speak

# --solutions--

```dart
class Person {
  String name;

  Person(this.name);

  void greet() {
    print('Hello, I am $name');
  }
}
```
