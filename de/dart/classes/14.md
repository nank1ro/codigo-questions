---
language: dart
exerciseType: 1
---

# --instructions--

Erstelle eine `abstract`e Klasse `Shape` mit einer abstrakten Methode `double area()`. Erstelle dann eine Klasse `Circle`, die `Shape` erweitert, ein `double`-Feld `radius`, einen Konstruktor hat und `area()` überschreibt, um `3.14 * radius * radius` zurückzugeben.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Circle(5).area()` sollte `78.5` ergeben

```dart
  test('test1', () {
    final c = Circle(5);
    expect(c.area(), 78.5, reason: '--err-t1--');
  });
```

`Circle(10).area()` sollte `314.0` ergeben

```dart
  test('test2', () {
    final c = Circle(10);
    expect(c.area(), 314.0, reason: '--err-t2--');
  });
```

`Circle` sollte eine Instanz von `Shape` sein

```dart
  test('test3', () {
    final c = Circle(3);
    expect(c is Shape, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
abstract class Shape {
  double area();
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() {
    return 3.14 * radius * radius;
  }
}
```
