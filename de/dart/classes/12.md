---
language: dart
exerciseType: 1
---

# --instructions--

Erstelle eine Klasse `Person` mit `String`-Feldern `name` und `int`-Feld `age`, sowie einem Konstruktor. Überschreibe `toString()`, um den String `'Name: <name>, Age: <age>'` zurückzugeben (z.B. `'Name: Alice, Age: 30'`).

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Person('Alice', 30).toString()` sollte `'Name: Alice, Age: 30'` zurückgeben

```dart
  test('test1', () {
    final p = Person('Alice', 30);
    expect(p.toString(), 'Name: Alice, Age: 30', reason: '--err-t1--');
  });
```

`Person('Bob', 25).toString()` sollte `'Name: Bob, Age: 25'` zurückgeben

```dart
  test('test2', () {
    final p = Person('Bob', 25);
    expect(p.toString(), 'Name: Bob, Age: 25', reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Person {
  String name;
  int age;
  Person(this.name, this.age);

  @override
  String toString() {
    return 'Name: $name, Age: $age';
  }
}
```
