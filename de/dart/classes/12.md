---
language: dart
exerciseType: 1
---

# --description--

**Methodenüberschreibung** erlaubt es einer Unterklasse, eine eigene Implementierung einer Methode bereitzustellen, die bereits in der Oberklasse existiert. Verwende die Annotation `@override`:

```dart
class Shape {
  double area() => 0;
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() => 3.14159 * radius * radius;
}
```

Die Annotation `@override` teilt Dart (und anderen Entwicklern) mit, dass du die Methode der Oberklasse absichtlich ersetzt.

# --instructions--

Erstelle eine Klasse `Animal` mit einer Methode `sound()`, die den `String` `'...'` zurückgibt. Erstelle dann eine Unterklasse `Dog`, die `sound()` überschreibt, um `'Woof'` zurückzugeben. Füge eine weitere Unterklasse `Cat` hinzu, die `sound()` überschreibt, um `'Meow'` zurückzugeben.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Animal.sound()` gibt `'...'` zurück

```dart
    test('animal sound', () {
      expect(Animal().sound(), '...', reason: '--err-t1--');
    });
```

`Dog.sound()` gibt `'Woof'` zurück

```dart
    test('dog sound', () {
      Animal dog = Dog();
      expect(dog.sound(), 'Woof', reason: '--err-t2--');
    });
```

`Cat.sound()` gibt `'Meow'` zurück

```dart
    test('cat sound', () {
      Animal cat = Cat();
      expect(cat.sound(), 'Meow', reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String sound() => '...';
}

class Dog extends Animal {
  @override
  String sound() => 'Woof';
}

class Cat extends Animal {
  @override
  String sound() => 'Meow';
}
```
