---
language: dart
exerciseType: 2
---

# --description--

Eine **Klasse** ist eine Vorlage zum Erstellen von Objekten. Sie fasst zusammengehörige Daten (Felder) und Verhalten (Methoden) zusammen.

In Dart deklarierst du eine Klasse mit dem Schlüsselwort `class`, gefolgt vom Klassennamen (konventionell in PascalCase):

```dart
class Car {
  String brand = 'Toyota';
}
```

Du erstellst eine **Instanz** (Objekt) der Klasse so:

```dart
final car = Car();
print(car.brand); // Toyota
```

# --instructions--

Vervollständige den Code, um eine Klasse namens `Person` mit einem `String`-Feld `name`, das mit `'Alice'` initialisiert ist, zu deklarieren, dann erstelle eine Instanz und gib deren `name` aus.

# --seed--

```dart
[/] Person {
  String [/] = 'Alice';
}

void main() {
  final p = [/]();
  print(p.[/]);
}
```

# --answers--

- class
- name
- Person
- brand
- age
- void
- p

# --solutions--

```dart
class Person {
  String name = 'Alice';
}

void main() {
  final p = Person();
  print(p.name);
}
```

# --output--

Alice
