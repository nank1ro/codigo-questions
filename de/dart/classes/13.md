---
language: dart
exerciseType: 1
---

# --instructions--

Erstelle eine Elternklasse `Animal` mit einem `String`-Feld `name` und einem Konstruktor. Erstelle dann eine Kindklasse `Dog`, die `Animal` erweitert. `Dog` soll ein zusätzliches `String`-Feld `breed` und einen Konstruktor haben, der `super` verwendet, um den `name` an `Animal` weiterzugeben.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Dog('Rex', 'Labrador').name` sollte `'Rex'` ergeben

```dart
  test('test1', () {
    final dog = Dog('Rex', 'Labrador');
    expect(dog.name, 'Rex', reason: '--err-t1--');
  });
```

`Dog('Rex', 'Labrador').breed` sollte `'Labrador'` ergeben

```dart
  test('test2', () {
    final dog = Dog('Rex', 'Labrador');
    expect(dog.breed, 'Labrador', reason: '--err-t2--');
  });
```

`Dog` sollte eine Instanz von `Animal` sein

```dart
  test('test3', () {
    final dog = Dog('Buddy', 'Poodle');
    expect(dog is Animal, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Dog extends Animal {
  String breed;
  Dog(String name, this.breed) : super(name);
}
```
