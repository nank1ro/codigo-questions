---
language: dart
exerciseType: 1
---

# --instructions--

Erstelle eine Klasse `Car` mit zwei `String`-Eigenschaften: `brand` und `model`. Füge einen Konstruktor hinzu, der beide mit der Kurzschreibweise setzt.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Das Erstellen von `Car('Toyota', 'Corolla')` sollte `brand` auf `'Toyota'` setzen

```dart
  test('test1', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.brand, 'Toyota', reason: '--err-t1--');
  });
```

Das Erstellen von `Car('Toyota', 'Corolla')` sollte `model` auf `'Corolla'` setzen

```dart
  test('test2', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.model, 'Corolla', reason: '--err-t2--');
  });
```

Das Erstellen von `Car('Honda', 'Civic')` sollte `brand` auf `'Honda'` setzen

```dart
  test('test3', () {
    final car = Car('Honda', 'Civic');
    expect(car.brand, 'Honda', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Car {
  String brand;
  String model;
  Car(this.brand, this.model);
}
```
