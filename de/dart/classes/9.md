---
language: dart
exerciseType: 1
---

# --instructions--

Erstelle eine Klasse `Point` mit zwei `int`-Eigenschaften `x` und `y`. Füge einen Konstruktor hinzu, der beide setzt. Füge außerdem einen benannten Konstruktor `Point.origin()` hinzu, der sowohl `x` als auch `y` auf `0` setzt.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Point(3, 5)` sollte `x` auf `3` und `y` auf `5` setzen

```dart
  test('test1', () {
    final p = Point(3, 5);
    expect(p.x, 3, reason: '--err-t1--');
    expect(p.y, 5, reason: '--err-t1--');
  });
```

`Point.origin()` sollte sowohl `x` als auch `y` auf `0` setzen

```dart
  test('test2', () {
    final p = Point.origin();
    expect(p.x, 0, reason: '--err-t2--');
    expect(p.y, 0, reason: '--err-t2--');
  });
```

`Point(-1, 4)` sollte `x` auf `-1` setzen

```dart
  test('test3', () {
    final p = Point(-1, 4);
    expect(p.x, -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Point {
  int x;
  int y;
  Point(this.x, this.y);
  Point.origin() : x = 0, y = 0;
}
```
