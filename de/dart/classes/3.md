---
language: dart
exerciseType: 2
---

# --description--

Eine **Methode** ist eine Funktion, die innerhalb einer Klasse definiert ist. Methoden beschreiben das Verhalten eines Objekts.

```dart
class Circle {
  double radius;
  Circle(this.radius);

  double area() {
    return 3.14 * radius * radius;
  }
}

void main() {
  final c = Circle(5);
  print(c.area()); // 78.5
}
```

Methoden können direkt über den Namen auf die eigenen Felder des Objekts zugreifen (oder über `this.feldName`).

# --instructions--

Vervollständige die Klasse `Rectangle`, sodass sie `int`-Felder `width` und `height`, einen Konstruktor und eine Methode `perimeter()` hat, die `2 * (width + height)` zurückgibt. Rufe dann die Methode auf und gib das Ergebnis aus.

# --seed--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.[/]);

  int [/]() {
    return 2 * ([/] + height);
  }
}

void main() {
  final r = Rectangle(4, 6);
  print(r.[/]());
}
```

# --answers--

- height
- perimeter
- width
- area
- this
- 3
- r

# --solutions--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.height);

  int perimeter() {
    return 2 * (width + height);
  }
}

void main() {
  final r = Rectangle(4, 6);
  print(r.perimeter());
}
```

# --output--

20
