---
language: dart
exerciseType: 1
---

# --instructions--

Erstelle eine Klasse `Counter` mit einem `int`-Feld `count`, das mit `0` initialisiert ist. Füge eine Methode `increment()` hinzu, die `count` um `1` erhöht, und eine Methode `reset()`, die `count` auf `0` zurücksetzt.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Dreimaliges Aufrufen von `increment()` sollte `count` gleich `3` machen

```dart
  test('test1', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.increment();
    expect(c.count, 3, reason: '--err-t1--');
  });
```

Das Aufrufen von `reset()` sollte `count` auf `0` zurücksetzen

```dart
  test('test2', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.reset();
    expect(c.count, 0, reason: '--err-t2--');
  });
```

Der anfängliche `count` sollte `0` sein

```dart
  test('test3', () {
    final c = Counter();
    expect(c.count, 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Counter {
  int count = 0;

  void increment() {
    count++;
  }

  void reset() {
    count = 0;
  }
}
```
