---
language: dart
exerciseType: 2
---

# --description--

Listen in Dart sind **nullbasiert indiziert**, d. h. das erste Element befindet sich an Index `0`, das zweite an Index `1` usw.

```dart
var colors = ['red', 'green', 'blue'];
print(colors[0]); // red
print(colors[2]); // blue
```

Der Zugriff auf ein Element per Index erfolgt mit der Syntax `list[index]`.

# --instructions--

Vervollständige den Code, um das zweite Element der Liste auszugeben.

# --seed--

```dart
var animals = ['cat', 'dog', 'bird'];
print(animals[/]);
```

# --answers--

- [1]
- [0]
- [2]
- .get(1)
- (1)
- [3]

# --solutions--

```dart
var animals = ['cat', 'dog', 'bird'];
print(animals[1]);
```

# --output--

dog
