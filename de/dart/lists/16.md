---
language: dart
exerciseType: 1
---

# --description--

Die Methode `.indexOf(value)` gibt den Index des ersten Elements zurück, das gleich `value` ist. Wenn der Wert nicht in der Liste enthalten ist, gibt sie `-1` zurück.

```dart
var colors = ['red', 'green', 'blue'];
print(colors.indexOf('green')); // 1
print(colors.indexOf('pink')); // -1
```

# --instructions--

Gegeben die Liste `['apple', 'banana', 'cherry', 'date']`, verwende `.indexOf()`, um den Index von `'cherry'` zu finden. Gib den Index zurück.

# --before-seed--

```dart
int computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Index von 'cherry' ist 2

```dart
  test('indexOf cherry is 2', () {
    expect(computeValue(), 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var fruits = ['apple', 'banana', 'cherry', 'date'];
  int result = fruits.indexOf('cherry');
```
