---
language: dart
exerciseType: 2
---

# --description--

Die Methode `.contains()` prüft, ob eine Liste einen bestimmten Wert enthält. Sie gibt `true` zurück, wenn er gefunden wird, andernfalls `false`:

```dart
var fruits = ['apple', 'mango', 'grape'];
print(fruits.contains('mango'));  // true
print(fruits.contains('orange')); // false
```

Dies ist nützlich für Zugehörigkeitsprüfungen ohne Schleife.

# --instructions--

Vervollständige den Code, um zu prüfen, ob `42` in der Liste enthalten ist, und gib das Ergebnis aus.

# --seed--

```dart
var numbers = [10, 42, 77, 5];
print(numbers.[/](42));
```

# --answers--

- contains
- includes
- has
- find
- exists
- indexOf

# --solutions--

```dart
var numbers = [10, 42, 77, 5];
print(numbers.contains(42));
```

# --output--

true
