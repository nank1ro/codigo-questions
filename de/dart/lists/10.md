---
language: dart
exerciseType: 1
---

# --instructions--

Beginne mit einer `List<int>`, die `[1, 2, 3]` enthält. Füge den Wert `4` mit `.add()` hinzu. Gib die Liste zurück.

# --before-seed--

```dart
List<int> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Liste enthält das hinzugefügte Element

```dart
  test('list is [1, 2, 3, 4]', () {
    expect(computeValue(), [1, 2, 3, 4], reason: '--err-t1--');
  });
```

Länge ist 4

```dart
  test('length is 4', () {
    expect(computeValue().length, 4, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<int> result = [1, 2, 3];
  result.add(4);
```
