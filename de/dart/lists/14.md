---
language: dart
exerciseType: 1
---

# --instructions--

Erstelle eine leere `List<int>`. Gib `true` zurück, wenn sie `.isEmpty` ist, füge dann den Wert `1` hinzu und gib `true` zurück, wenn sie `.isNotEmpty` ist. Gib beide Booleans als `List<bool>` zurück.

# --before-seed--

```dart
List<bool> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Erster Wert ist true (war leer)

```dart
  test('first is true', () {
    expect(computeValue()[0], true, reason: '--err-t1--');
  });
```

Zweiter Wert ist true (ist nach dem Hinzufügen nicht leer)

```dart
  test('second is true', () {
    expect(computeValue()[1], true, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<int> nums = [];
  bool wasEmpty = nums.isEmpty;
  nums.add(1);
  bool nowNotEmpty = nums.isNotEmpty;
  List<bool> result = [wasEmpty, nowNotEmpty];
```
