---
language: dart
exerciseType: 1
---

# --instructions--

Erstelle eine `List<String>` namens `colors`, die `'red'`, `'green'` und `'blue'` enthält. Gib ihre Länge zurück.

# --before-seed--

```dart
int computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

Gibt die korrekte Länge zurück

```dart
  test('length is 3', () {
    expect(computeValue(), 3, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<String> colors = ['red', 'green', 'blue'];
  int result = colors.length;
```
