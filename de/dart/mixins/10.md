---
language: dart
exerciseType: 1
---

# --description--

Der Klassenkörper sitzt ganz am Ende der Kette, daher **überschreibt** ein in der Klasse deklariertes Member dasselbe Member aus jedem seiner Mixins. Innerhalb des Overrides erreicht **`super`** die Version, die das Mixin bereitgestellt hat:

```dart
mixin Polite {
  String greet() => 'Hello';
}

class Host with Polite {
  @override
  String greet() => '${super.greet()}, welcome!';
}

class Guest with Polite {}

void main() {
  print(Host().greet());  // Hello, welcome!
  print(Guest().greet()); // Hello
}
```

Das Mixin selbst bleibt unangetastet: `Guest` bekommt weiterhin das ursprüngliche `greet`. `super.greet()` ermöglicht es `Host`, auf dem gemeinsamen Verhalten aufzubauen, statt es zu kopieren.

# --instructions--

Deklariere ein Mixin `Signed` mit einer Methode `sign`, die `'signed'` zurückgibt.

Deklariere eine Klasse `Letter`, die `Signed` verwendet und `sign` so überschreibt, dass sie das Ergebnis des Mixins gefolgt von `' by Ada'` zurückgibt, erhalten durch den Aufruf von `super`.

Deklariere eine Klasse `Note`, die `Signed` verwendet und nichts überschreibt.

Beispiel:
```dart
print(Letter().sign()); // signed by Ada
print(Note().sign());   // signed
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Letter().sign()` sollte `'signed by Ada'` sein

```dart
  test('test1', () {
    expect(Letter().sign(), 'signed by Ada', reason: '--err-t1--');
  });
```

`Note().sign()` sollte weiterhin `'signed'` sein, denn das Mixin ist unverändert

```dart
  test('test2', () {
    expect(Note().sign(), 'signed', reason: '--err-t2--');
  });
```

Beide Klassen sollten das Mixin verwenden, und der Override sollte auch über den Mixin-Typ verwendet werden

```dart
  test('test3', () {
    final Signed letter = Letter();
    expect(letter.sign(), 'signed by Ada', reason: '--err-t3--');
    expect(Note(), isA<Signed>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Signed {
  String sign() => 'signed';
}

class Letter with Signed {
  @override
  String sign() => '${super.sign()} by Ada';
}

class Note with Signed {}
```
