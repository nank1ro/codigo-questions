---
language: dart
exerciseType: 2
---

# --description--

Manches Verhalten ergibt nur Sinn, wenn es auf einer bestimmten Klasse sitzt, und braucht die Member dieser Klasse, um seine Arbeit zu tun. Die **`on`**-Klausel formuliert diese Anforderung:

```dart
class Animal {
  String get name => 'animal';
}

mixin Noisy on Animal {
  String shout() => '${name.toUpperCase()}!';
}

class Dog extends Animal with Noisy {
  @override
  String get name => 'dog';
}

void main() {
  print(Dog().shout()); // DOG!
}
```

`on Animal` tut zwei Dinge: Es erlaubt dem Mixin, die Member von `Animal` zu verwenden, wie `name` oben, und es beschränkt, wer das Mixin verwenden darf. `class Rock with Noisy {}` ist ein Kompilierfehler, denn `Rock` ist kein `Animal`.

# --instructions--

Vervollständige den Code so, dass `Described` ein Mixin ist, das auf `Vehicle` beschränkt ist, und `Car` ein `Vehicle` ist, das es verwendet.

# --seed--

```dart
class Vehicle {
  int get wheels => 4;
}

mixin Described [/] Vehicle {
  String describe() => 'has $wheels wheels';
}

class Car [/] Vehicle [/] Described {}

void main() {
  print(Car().describe());
}
```

# --answers--

- on
- extends
- with
- mixin
- class
- of

# --solutions--

```dart
class Vehicle {
  int get wheels => 4;
}

mixin Described on Vehicle {
  String describe() => 'has $wheels wheels';
}

class Car extends Vehicle with Described {}

void main() {
  print(Car().describe());
}
```

# --output--

has 4 wheels
