---
language: dart
exerciseType: 1
---

# --description--

Eine Klasse kann **mehrere Mixins auf einmal** verwenden, aufgelistet nach `with` und durch Kommas getrennt. Dart wendet sie **von links nach rechts** an und stapelt jedes auf das vorherige, sodass bei zwei Mixins, die dasselbe Member deklarieren, das **letzte** in der Liste gewinnt:

```dart
mixin A {
  String who() => 'A';
}

mixin B {
  String who() => 'B';
}

class First with A, B {}
class Second with B, A {}

void main() {
  print(First().who());  // B, the last mixin in the list
  print(Second().who()); // A, the last mixin in the list
}
```

Dieses Stapeln nennt man **Linearisierung**: `with A, B` baut die Kette `Object` → `A` → `B` → die Klasse selbst.

# --instructions--

Deklariere ein Mixin `Quiet` mit einer Methode `volume`, die `'quiet'` zurückgibt, und ein Mixin `Loud` mit einer Methode `volume`, die `'loud'` zurückgibt.

Deklariere eine Klasse `Speaker`, die beide Mixins so verwendet, dass `Speaker().volume()` gleich `'loud'` ist, und eine Klasse `Whisperer`, die beide Mixins so verwendet, dass `Whisperer().volume()` gleich `'quiet'` ist.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Speaker().volume()` sollte `'loud'` sein

```dart
  test('test1', () {
    expect(Speaker().volume(), 'loud', reason: '--err-t1--');
  });
```

`Whisperer().volume()` sollte `'quiet'` sein

```dart
  test('test2', () {
    expect(Whisperer().volume(), 'quiet', reason: '--err-t2--');
  });
```

Beide Klassen sollten beide Mixins verwenden

```dart
  test('test3', () {
    expect(Speaker(), isA<Quiet>(), reason: '--err-t3--');
    expect(Speaker(), isA<Loud>(), reason: '--err-t3--');
    expect(Whisperer(), isA<Quiet>(), reason: '--err-t3--');
    expect(Whisperer(), isA<Loud>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Quiet {
  String volume() => 'quiet';
}

mixin Loud {
  String volume() => 'loud';
}

class Speaker with Quiet, Loud {}

class Whisperer with Loud, Quiet {}
```
