---
language: dart
exerciseType: 1
---

# --description--

Der Körper eines Mixins sieht aus wie der Körper einer Klasse: Methoden, Getter und Felder, geschrieben auf genau dieselbe Weise. Der Unterschied liegt darin, was du mit der Deklaration selbst machen kannst.

```dart
mixin Timestamped {
  String get stamp => '[log]';

  String format(String text) => '$stamp $text';
}

class Server with Timestamped {}
class Printer with Timestamped {}
```

Der Name eines Mixins ist auch ein **Typ**, daher ist `Server() is Timestamped` gleich `true`, und eine Variable kann als `Timestamped t = Server();` deklariert werden. Zwei nicht verwandte Klassen teilen sich nun eine Implementierung, ohne dass eine von der anderen erbt.

# --instructions--

Deklariere ein Mixin `Shouter` mit einer Methode `shout`, die einen `String` `text` nimmt und ihn in Großbuchstaben gefolgt von einem Ausrufezeichen zurückgibt.

Deklariere eine Klasse `Speaker`, die `Shouter` verwendet und nichts Eigenes hinzufügt.

Beispiel:
```dart
print(Speaker().shout('hi')); // HI!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Speaker().shout('hi')` sollte `'HI!'` sein

```dart
  test('test1', () {
    expect(Speaker().shout('hi'), 'HI!', reason: '--err-t1--');
  });
```

`Speaker().shout('Dart rocks')` sollte `'DART ROCKS!'` sein und `Speaker().shout('')` sollte `'!'` sein

```dart
  test('test2', () {
    expect(Speaker().shout('Dart rocks'), 'DART ROCKS!', reason: '--err-t2--');
    expect(Speaker().shout(''), '!', reason: '--err-t2--');
  });
```

`Speaker` sollte `shout` aus dem Mixin bekommen, daher sollte ein `Speaker` auch ein `Shouter` sein

```dart
  test('test3', () {
    expect(Speaker(), isA<Shouter>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Shouter {
  String shout(String text) => '${text.toUpperCase()}!';
}

class Speaker with Shouter {}
```
