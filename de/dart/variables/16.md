---
language: dart
exerciseType: 1
---

# --instructions--

Initialisiere die Variable `x` mit der Nummer `20`, so dass sie sowohl Ganzzahlen als auch Dezimalzahlen unterstützt.
Aktualisiere die Variable mit der Nummer `3.14159`.

# --before-seed--

```dart
dynamic number() {
```

# --after-seed--

```dart
  return x;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x` muss gleich `3.14159` sein.

```dart
  test('test1', () {
    expect(number(), 3.14159, reason: '--err-t1--');
  });
```

`x` muss vom Typ `num` sein

```dart
  test('test2', () {
    expect(number(), TypeMatcher<num>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
num x = 20;
x = 3.14159;
```
