---
language: dart
exerciseType: 2
---

# --description--

Die Methode `.remove(value)` löscht einen Wert aus dem Set. Sie gibt `true` zurück, wenn der Wert vorhanden war, und sonst `false`:

```dart
var numbers = {1, 2, 3};
print(numbers.remove(2)); // true
print(numbers.remove(9)); // false
print(numbers); // {1, 3}
```

Um alle Elemente auf einmal zu löschen, verwende `.clear()`.

# --instructions--

Vervollständige den Code, um die Zahl `2` aus dem Set zu entfernen, und gib dann das Set aus.

# --seed--

```dart
var numbers = {1, 2, 3};
numbers.[/](2);
print(numbers);
```

# --answers--

- remove
- removeAt
- delete
- drop
- clear
- pop

# --solutions--

```dart
var numbers = {1, 2, 3};
numbers.remove(2);
print(numbers);
```

# --output--

{1, 3}
