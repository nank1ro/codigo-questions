---
language: dart
exerciseType: 1
---

# --instructions--

Speichere für die Liste `numbers` in einem `int` namens `uniqueCount`, wie viele unterschiedliche Werte sie enthält, und in einer `List<int>` namens `unique` diese Werte ohne Duplikate, in der Reihenfolge ihres ersten Auftretens.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var numbers = [4, 8, 4, 15, 8, 16, 4];
```

# --after-seed--

```dart
  return [uniqueCount, unique];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`uniqueCount` sollte `4` sein

```dart
  test('test1', () {
    expect(result()[0], 4, reason: '--err-t1--');
  });
```

`unique` sollte `[4, 8, 15, 16]` sein

```dart
  test('test2', () {
    expect(result()[1], [4, 8, 15, 16], reason: '--err-t2--');
  });
```

`unique` sollte eine `List` sein

```dart
  test('test3', () {
    expect(result()[1], isA<List>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var numbers = [4, 8, 4, 15, 8, 16, 4];
int uniqueCount = numbers.toSet().length;
List<int> unique = numbers.toSet().toList();
```
