---
language: dart
exerciseType: 1
---

# --description--

Um zu prüfen, ob ein Wert in einem Set enthalten ist, verwende `.contains(value)`, das einen `bool` zurückgibt. Die Eigenschaft `.isEmpty` ist `true`, wenn das Set keine Elemente hat, und `.isNotEmpty`, wenn es mindestens eins hat:

```dart
var seen = {'x', 'y'};
print(seen.contains('x')); // true
print(seen.contains('z')); // false
print(seen.isEmpty);       // false
print(seen.isNotEmpty);    // true
```

# --instructions--

Speichere für das Set `seen` in einem `bool` namens `hasX`, ob es `'x'` enthält, in einem `bool` namens `hasZ`, ob es `'z'` enthält, und in einem `bool` namens `empty`, ob es leer ist.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var seen = {'x', 'y'};
```

# --after-seed--

```dart
  return [hasX, hasZ, empty];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasX` sollte `true` sein

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasZ` sollte `false` sein

```dart
  test('test2', () {
    expect(result()[1], false, reason: '--err-t2--');
  });
```

`empty` sollte `false` sein

```dart
  test('test3', () {
    expect(result()[2], false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var seen = {'x', 'y'};
bool hasX = seen.contains('x');
bool hasZ = seen.contains('z');
bool empty = seen.isEmpty;
```
