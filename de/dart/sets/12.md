---
language: dart
exerciseType: 1
---

# --description--

Eine Liste in ein Set umzuwandeln ist der einfachste Weg, **Duplikate zu entfernen**: Jede Liste hat eine Methode `.toSet()`, die ein Set mit ihren eindeutigen Elementen in der Reihenfolge ihres ersten Auftretens zurückgibt. Ein Set hat eine Methode `.toList()`, die den umgekehrten Weg geht, sodass die Verkettung beider dir eine Liste ohne Duplikate liefert:

```dart
var votes = ['a', 'b', 'a', 'c', 'b'];
Set<String> unique = votes.toSet();
print(unique); // {a, b, c}
List<String> cleaned = votes.toSet().toList();
print(cleaned); // [a, b, c]
```

# --instructions--

Erstelle für die Liste `votes` eine `List<String>` namens `cleaned`, die ihre Elemente ohne Duplikate in der Reihenfolge ihres ersten Auftretens enthält.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var votes = ['yes', 'no', 'yes', 'maybe', 'no', 'yes'];
```

# --after-seed--

```dart
  return cleaned;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`cleaned` sollte `['yes', 'no', 'maybe']` sein

```dart
  test('test1', () {
    expect(result(), ['yes', 'no', 'maybe'], reason: '--err-t1--');
  });
```

`cleaned` sollte eine `List` sein

```dart
  test('test2', () {
    expect(result(), isA<List>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var votes = ['yes', 'no', 'yes', 'maybe', 'no', 'yes'];
List<String> cleaned = votes.toSet().toList();
```
