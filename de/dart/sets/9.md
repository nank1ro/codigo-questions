---
language: dart
exerciseType: 1
---

# --description--

Sets unterstützen die klassischen Mengenoperationen. Jede gibt ein **neues** Set zurück und lässt die Originale unverändert:

- `a.union(b)` enthält die Elemente, die in `a` **oder** in `b` sind
- `a.intersection(b)` enthält die Elemente, die in **beiden**, `a` und `b`, sind
- `a.difference(b)` enthält die Elemente von `a`, die **nicht** in `b` sind

```dart
var a = {1, 2, 3};
var b = {2, 3, 4};
print(a.union(b));        // {1, 2, 3, 4}
print(a.intersection(b)); // {2, 3}
print(a.difference(b));   // {1}
```

# --instructions--

Erstelle für die Sets `a` und `b` ein Set namens `both` mit ihrer Vereinigung, ein Set namens `common` mit ihrer Schnittmenge und ein Set namens `onlyA` mit den Elementen von `a`, die nicht in `b` sind.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var a = {1, 2, 3, 4};
var b = {3, 4, 5};
```

# --after-seed--

```dart
  return [both, common, onlyA];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`both` sollte `{1, 2, 3, 4, 5}` sein

```dart
  test('test1', () {
    expect(result()[0], {1, 2, 3, 4, 5}, reason: '--err-t1--');
  });
```

`common` sollte `{3, 4}` sein

```dart
  test('test2', () {
    expect(result()[1], {3, 4}, reason: '--err-t2--');
  });
```

`onlyA` sollte `{1, 2}` sein

```dart
  test('test3', () {
    expect(result()[2], {1, 2}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var a = {1, 2, 3, 4};
var b = {3, 4, 5};
var both = a.union(b);
var common = a.intersection(b);
var onlyA = a.difference(b);
```
