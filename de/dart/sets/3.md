---
language: dart
exerciseType: 1
---

# --description--

Die Methode `.add(value)` fügt einen einzelnen Wert ein. Sie gibt `true` zurück, wenn der Wert hinzugefügt wurde, und `false`, wenn er bereits im Set war; in diesem Fall ändert sich nichts. Die Methode `.addAll(iterable)` fügt jedes Element einer Liste oder eines anderen Sets ein und überspringt dabei ebenfalls die bereits vorhandenen:

```dart
var tags = {'dart'};
tags.add('web');    // true
tags.add('dart');   // false, already there
tags.addAll(['web', 'mobile']);
print(tags); // {dart, web, mobile}
```

Ein leeres `{}`-Literal ist eine **Map**, kein Set. Um ein leeres Set zu erstellen, gib ihm einen Typ:

```dart
var empty = <String>{};
Set<int> other = {};
```

# --instructions--

Füge dem Set `tags` `'web'` mit `.add()` hinzu und dann alle Elemente der Liste `['web', 'mobile']` mit `.addAll()`.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var tags = {'dart'};
```

# --after-seed--

```dart
  return tags;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`tags` sollte `{'dart', 'web', 'mobile'}` sein

```dart
  test('test1', () {
    expect(result(), {'dart', 'web', 'mobile'}, reason: '--err-t1--');
  });
```

`tags` sollte 3 Elemente haben

```dart
  test('test2', () {
    expect(result().length, 3, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var tags = {'dart'};
tags.add('web');
tags.addAll(['web', 'mobile']);
```
