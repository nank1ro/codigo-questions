---
language: dart
exerciseType: 2
---

# --description--

Wenn der Rumpf ein einzelner Ausdruck ist, kann eine anonyme Funktion die **Pfeilsyntax** `=>` verwenden, genau wie eine benannte Funktion. Der Pfeil ersetzt die geschweiften Klammern und das Schlüsselwort `return`:

```dart
var add = (int a, int b) => a + b;

print(add(2, 3)); // 5
```

Diese Kurzform ist mit Abstand die häufigste Art, anonyme Funktionen in Dart zu schreiben.

# --instructions--

Vervollständige den Code, sodass in `triple` eine Pfeilfunktion gespeichert wird, die ihr Argument multipliziert mit `3` zurückgibt, sodass das Programm `12` ausgibt.

# --seed--

```dart
void main() {
  var triple = (int n) [/] n [/] 3;
  print(triple(4));
}
```

# --answers--

- =>
- *
- ->
- +
- return
- {

# --solutions--

```dart
void main() {
  var triple = (int n) => n * 3;
  print(triple(4));
}
```

# --output--

12
