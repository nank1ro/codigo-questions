---
language: dart
exerciseType: 4
---

# --description--

Anonyme Funktionen unterstützen dieselben Parameterarten wie benannte Funktionen: **optionale positionale** Parameter in `[]` und **benannte** Parameter in `{}`, beide mit Standardwerten:

```dart
var repeat = (String text, [int times = 2]) => text * times;
print(repeat('ab'));    // abab
print(repeat('ab', 3)); // ababab

var describe = ({required String item, int count = 1}) => '$count x $item';
print(describe(item: 'apple'));           // 1 x apple
print(describe(item: 'pear', count: 4));  // 4 x pear
```

# --instructions--

Ordne die Elemente, um in `greet` eine anonyme Funktion mit einem benannten Parameter `name` zu speichern, dessen Standardwert `'World'` ist, und rufe sie zuerst ohne Argumente und dann mit `name: 'Dart'` auf. Das Programm muss Folgendes ausgeben:

```
Hello, World!
Hello, Dart!
```

# --answers--

```dart
void main() {
```

```dart
  var greet = ({String name = 'World'}) {
```

```dart
    print('Hello, $name!');
```

```dart
  };
```

```dart
  greet();
```

```dart
  greet(name: 'Dart');
```

```dart
}
```

# --solutions--

```dart
void main() {
  var greet = ({String name = 'World'}) {
    print('Hello, $name!');
  };
  greet();
  greet(name: 'Dart');
}
```

# --output--

Hello, World!
Hello, Dart!
