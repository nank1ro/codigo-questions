---
language: dart
exerciseType: 1
---

# --description--

Funktionen, die andere Funktionen nehmen und zurückgeben, lassen sich frei kombinieren. Ein klassisches Beispiel ist die **Komposition**: eine neue Funktion bauen, die zuerst eine Funktion ausführt und ihr Ergebnis dann in eine andere einspeist:

```dart
int Function(int) then(int Function(int) first, int Function(int) second) {
  return (n) => second(first(n));
}
```

# --instructions--

Schreibe eine Funktion `compose`, die zwei Parameter `f` und `g` nimmt, beide vom Typ `int Function(int)`, und eine Funktion vom Typ `int Function(int)` zurückgibt, die `f` auf ihr Argument anwendet und dann `g` auf das Ergebnis.

Beispiel:
```dart
var addThenDouble = compose((n) => n + 1, (n) => n * 2);
print(addThenDouble(3)); // 8
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`compose((n) => n + 1, (n) => n * 2)(3)` sollte `8` sein

```dart
  test('test1', () {
    expect(compose((n) => n + 1, (n) => n * 2)(3), 8, reason: '--err-t1--');
  });
```

`compose((n) => n * 2, (n) => n + 1)(3)` sollte `7` sein

```dart
  test('test2', () {
    expect(compose((n) => n * 2, (n) => n + 1)(3), 7, reason: '--err-t2--');
  });
```

Die zurückgegebene Funktion sollte den Typ `int Function(int)` haben

```dart
  test('test3', () {
    var negate = compose((n) => -n, (n) => n * n);
    expect(negate, isA<int Function(int)>(), reason: '--err-t3--');
    expect(negate(5), 25, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int Function(int) compose(int Function(int) f, int Function(int) g) {
  return (int n) => g(f(n));
}
```
