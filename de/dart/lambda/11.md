---
language: dart
exerciseType: 2
---

# --description--

Eine Funktion kann auch **eine Funktion zurückgeben**. Der Rückgabetyp ist dann ein Funktionstyp, und der Rumpf gibt eine anonyme Funktion zurück:

```dart
int Function(int) makeAdder(int amount) {
  return (int n) => n + amount;
}

var addTen = makeAdder(10);
print(addTen(5)); // 15
```

Beachte, dass die zurückgegebene Funktion weiterhin `amount` verwendet, einen Parameter von `makeAdder`, sogar noch nachdem `makeAdder` fertig ist. Eine Funktion, die sich die Variablen um sie herum so merkt, heißt **Closure**.

# --instructions--

Vervollständige `makeMultiplier`, sodass sie eine Funktion zurückgibt, die ihr Argument mit `factor` multipliziert, und rufe dann die zurückgegebene Funktion auf, sodass das Programm `15` ausgibt.

# --seed--

```dart
int Function(int) makeMultiplier(int factor) {
  return ([/] n) => n [/] factor;
}

void main() {
  var triple = makeMultiplier(3);
  print(triple([/]));
}
```

# --answers--

- int
- *
- 5
- +
- 15
- String

# --solutions--

```dart
int Function(int) makeMultiplier(int factor) {
  return (int n) => n * factor;
}

void main() {
  var triple = makeMultiplier(3);
  print(triple(5));
}
```

# --output--

15
