---
language: dart
exerciseType: 1
---

# --description--

Eine anonyme Funktion muss nirgendwo gespeichert werden: Sie kann **sofort aufgerufen** werden, indem man die Argumente direkt hinter ihre schließende geschweifte Klammer schreibt. Das ist praktisch, um einen Wert mit ein paar temporären Variablen zu berechnen, die nicht in den umgebenden Code gelangen sollen:

```dart
var area = (double radius) {
  var pi = 3.14;
  return pi * radius * radius;
}(2.0);

print(area); // 12.56
```

Hier existiert `pi` nur innerhalb der anonymen Funktion, und `area` erhält den zurückgegebenen Wert.

# --instructions--

Eine `int`-Variable `limit` ist bereits definiert. Deklariere eine Variable `sum`, die initialisiert wird, indem sofort eine anonyme Funktion ohne Parameter aufgerufen wird, die alle ganzen Zahlen von `1` bis `limit` mit einer Schleife aufsummiert und die Summe zurückgibt.

# --before-seed--

```dart
dynamic result(int limit) {
```

# --seed--

```dart
var sum = 
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` sollte ein `int` sein

```dart
  test('test1', () {
    expect(result(100), isA<int>(), reason: '--err-t1--');
  });
```

`sum` sollte `5050` sein, wenn `limit` `100` ist

```dart
  test('test2', () {
    expect(result(100), 5050, reason: '--err-t2--');
  });
```

`sum` sollte `55` sein, wenn `limit` `10` ist

```dart
  test('test3', () {
    expect(result(10), 55, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var sum = () {
  var total = 0;
  for (var i = 1; i <= limit; i++) {
    total += i;
  }
  return total;
}();
```
