---
language: dart
exerciseType: 1
---

# --description--

Eine anonyme Funktion kann Parameter haben und mit `return` einen Wert zurückgeben, genau wie eine benannte Funktion. Der Rückgabetyp wird nicht hingeschrieben: Dart **leitet** ihn aus den `return`-Anweisungen im Rumpf ab.

```dart
var add = (int a, int b) {
  return a + b;
};

print(add(2, 3)); // 5
```

# --instructions--

Speichere in der Variable `square` eine anonyme Funktion, die einen `int`-Parameter `n` nimmt und `n` mit sich selbst multipliziert zurückgibt.

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var square = 
```

# --after-seed--

```dart
  return square;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`square` sollte eine Funktion sein

```dart
  test('test1', () {
    expect(result(), isA<Function>(), reason: '--err-t1--');
  });
```

`square(4)` sollte `16` sein

```dart
  test('test2', () {
    expect(result()(4), 16, reason: '--err-t2--');
  });
```

`square(-3)` sollte `9` sein

```dart
  test('test3', () {
    expect(result()(-3), 9, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var square = (int n) {
  return n * n;
};
```
