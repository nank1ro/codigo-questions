---
language: dart
exerciseType: 1
---

# --description--

Da `map` und `where` beide ein `Iterable` zurückgeben, können ihre Aufrufe hintereinander **verkettet** werden. Jeder Schritt erhält das Ergebnis des vorherigen, und `toList()` wird einmal am Ende aufgerufen:

```dart
var numbers = [1, 2, 3, 4, 5, 6];

var result = numbers.where((n) => n > 3).map((n) => n * 10).toList();
print(result); // [40, 50, 60]
```

# --instructions--

Schreibe eine Funktion `oddSquares`, die einen `List<int>`-Parameter `numbers` nimmt und eine `List<int>` mit den Quadraten nur der ungeraden Zahlen in der ursprünglichen Reihenfolge zurückgibt. Verwende `where` und `map` mit anonymen Funktionen.

Beispiel:
```dart
print(oddSquares([1, 2, 3, 4, 5])); // [1, 9, 25]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`oddSquares([1, 2, 3, 4, 5])` sollte `[1, 9, 25]` sein

```dart
  test('test1', () {
    expect(oddSquares([1, 2, 3, 4, 5]), [1, 9, 25], reason: '--err-t1--');
  });
```

`oddSquares([2, 4, 6])` sollte `[]` sein

```dart
  test('test2', () {
    expect(oddSquares([2, 4, 6]), [], reason: '--err-t2--');
  });
```

`oddSquares([7, 10, 3])` sollte `[49, 9]` sein

```dart
  test('test3', () {
    expect(oddSquares([7, 10, 3]), [49, 9], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> oddSquares(List<int> numbers) {
  return numbers.where((n) => n % 2 != 0).map((n) => n * n).toList();
}
```
