---
language: dart
exerciseType: 4
---

# --description--

Dieselbe Bound funktioniert genauso für das kleinste Element: Nur das Vorzeichen des Vergleichs ändert sich. `compareTo` gibt eine negative Zahl zurück, wenn der Empfänger zuerst kommt, also bedeutet `item.compareTo(best) < 0`: „dieses ist kleiner“.

# --instructions--

Sortiere die Elemente, um eine generische Funktion `smallest` zu definieren, die durch `Comparable<T>` beschränkt ist und das kleinste Element einer Liste zurückgibt, dazu ein `main`, das das Kleinste von `['pear', 'fig', 'kiwi']` ausgibt. Definiere die Funktion vor `main`. Das Programm muss ausgeben:

```
fig
```

# --answers--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
```

```dart
  var best = items.first;
```

```dart
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
```

```dart
  return best;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(smallest(['pear', 'fig', 'kiwi']));
```

```dart
}
```

# --solutions--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
  return best;
}

void main() {
  print(smallest(['pear', 'fig', 'kiwi']));
}
```

# --output--

fig
