---
language: dart
exerciseType: 4
---

# --description--

Innerhalb einer generischen Klasse ist der Typparameter überall sichtbar: in Feldern, in Konstruktorparametern, in Methodensignaturen und in Methodenkörpern. Er wird einmal deklariert, neben dem Klassennamen, und jedes Mitglied kann ihn verwenden.

```dart
class Holder<T> {
  final T item;

  Holder(this.item);

  String describe() => 'holding $item';
}
```

Das Erstellen des Objekts entscheidet über den Typ: `Holder<String>('fig')` macht `item` zu einem `String`, `Holder<int>(3)` macht daraus ein `int`.

# --instructions--

Sortiere die Elemente, um eine generische Klasse `Holder` mit einem Feld `item` vom Typ `T`, einem Konstruktor und einer Methode `describe` zu definieren, dazu ein `main`, das die Beschreibung einer mit `'fig'` erstellten `Holder<String>` ausgibt. Definiere die Klasse vor `main`. Das Programm muss ausgeben:

```
holding fig
```

# --answers--

```dart
class Holder<T> {
  final T item;
  Holder(this.item);
```

```dart
  String describe() => 'holding $item';
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Holder<String>('fig').describe());
```

```dart
}
```

# --solutions--

```dart
class Holder<T> {
  final T item;
  Holder(this.item);
  String describe() => 'holding $item';
}

void main() {
  print(Holder<String>('fig').describe());
}
```

# --output--

holding fig
