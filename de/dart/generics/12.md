---
language: dart
exerciseType: 1
---

# --description--

Eine Bound kann den Typparameter selbst erwähnen. `Comparable<T>` ist das Interface von allem, was weiß, wie es sich mit seinesgleichen vergleicht, über `compareTo`:

```dart
print('fig'.compareTo('kiwi')); // negative: fig comes first
print('kiwi'.compareTo('fig')); // positive
print('fig'.compareTo('fig'));  // zero
```

`T extends Comparable<T>` liest sich also als „jeder Typ, der mit sich selbst verglichen werden kann“, was genau das ist, was eine Sortier- oder Maximumfunktion braucht:

```dart
T maxOf<T extends Comparable<T>>(T a, T b) => a.compareTo(b) >= 0 ? a : b;

print(maxOf('fig', 'kiwi')); // kiwi
```

`String` und `DateTime` erfüllen sie beide direkt. `int` und `double` implementieren `Comparable<num>`, also wird eine Liste von Zahlen einfach als `num` verglichen.

# --instructions--

Schreibe eine generische Funktion `largest` mit einem Typparameter `T`, der durch `Comparable<T>` beschränkt ist, die eine nicht-leere `List<T>` `items` entgegennimmt und das größte Element zurückgibt, verglichen mit `compareTo`.

Beispiel:
```dart
print(largest(['fig', 'kiwi', 'apple'])); // kiwi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest(['fig', 'kiwi', 'apple'])` sollte `'kiwi'` zurückgeben

```dart
  test('test1', () {
    expect(largest(['fig', 'kiwi', 'apple']), 'kiwi', reason: '--err-t1--');
  });
```

`largest` sollte auch mit Zahlen und mit Daten funktionieren

```dart
  test('test2', () {
    expect(largest([3, 9, 5]), 9, reason: '--err-t2--');
    expect(
      largest([DateTime(2020), DateTime(2024), DateTime(2001)]),
      DateTime(2024),
      reason: '--err-t2--',
    );
  });
```

`largest` sollte das einzige Element einer Ein-Element-Liste zurückgeben, das letzte, wenn es das größte ist, und sie sollte ein explizites Typargument wie `largest<String>(['a', 'z'])` akzeptieren

```dart
  test('test3', () {
    expect(largest<String>(['a', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['only']), 'only', reason: '--err-t3--');
    expect(largest(['a', 'b', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['z', 'b', 'a']), 'z', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
T largest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) > 0) {
      best = item;
    }
  }
  return best;
}
```
