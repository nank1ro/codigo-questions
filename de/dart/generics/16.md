---
language: dart
exerciseType: 1
---

# --description--

Ein `typedef` gibt einem Typ einen Namen, und es kann eigene Typparameter entgegennehmen. Der übliche Grund: eine Familie von Funktionstypen einmal zu benennen, statt sie bei jeder Verwendung auszuschreiben:

```dart
typedef Transform<I, O> = O Function(I input);

final Transform<String, int> length = (word) => word.length;
print(length('kiwi')); // 4
```

`Transform<String, int>` ist nur eine andere Schreibweise für `int Function(String)`, die beiden sind also austauschbar. Der Gewinn ist Lesbarkeit: Ein als `Transform<I, O> transform` deklarierter Parameter sagt, wofür die Funktion da ist, während `O Function(I)` nur sagt, wie sie aussieht.

Ein generisches typedef und eine generische Funktion lassen sich natürlich kombinieren, wobei die eigenen Typparameter der Funktion die des typedef füllen.

# --instructions--

Deklariere ein generisches typedef `Transform` mit zwei Typparametern `I` und `O`, das eine Funktion benennt, die ein `I` entgegennimmt und ein `O` zurückgibt.

Schreibe dann eine generische Funktion `convertAll` mit Typparametern `I` und `O`, die eine `List<I>` `items` und ein `Transform<I, O>` `transform` entgegennimmt und eine `List<O>` mit jedem konvertierten Element, in Reihenfolge, zurückgibt.

Beispiel:
```dart
print(convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)); // [3, 4]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)` sollte `[3, 4]` zurückgeben

```dart
  test('test1', () {
    expect(
      convertAll<String, int>(['fig', 'kiwi'], (w) => w.length),
      [3, 4],
      reason: '--err-t1--',
    );
  });
```

`convertAll` sollte eine `List<O>` zurückgeben, also sollte die Konvertierung von ints zu Strings eine `List<String>` ergeben

```dart
  test('test2', () {
    final result = convertAll<int, String>([1, 2], (n) => 'n$n');
    expect(result, ['n1', 'n2'], reason: '--err-t2--');
    expect(result, isA<List<String>>(), reason: '--err-t2--');
  });
```

Das typedef `Transform` sollte einen Funktionstyp benennen, und eine leere Liste sollte eine leere Liste ergeben

```dart
  test('test3', () {
    final Transform<int, int> twice = (n) => n * 2;
    expect(convertAll<int, int>([1, 2, 3], twice), [2, 4, 6], reason: '--err-t3--');
    expect(convertAll<String, int>(<String>[], (w) => w.length), <int>[], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
typedef Transform<I, O> = O Function(I input);

List<O> convertAll<I, O>(List<I> items, Transform<I, O> transform) {
  return items.map(transform).toList();
}
```
