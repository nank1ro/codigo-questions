---
language: dart
exerciseType: 1
---

# --instructions--

Schreibe eine Funktion `trafficLight(String color)`, die `'Go'` für `'green'`, `'Slow down'` für `'yellow'`, `'Stop'` für `'red'` und `'Invalid'` für jeden anderen Wert zurückgibt, mithilfe einer `switch`-Anweisung mit einer `default`-Klausel.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`trafficLight('green')` muss `'Go'` zurückgeben

```dart
  test('test1', () {
    expect(trafficLight('green'), 'Go', reason: '--err-t1--');
  });
```

`trafficLight('yellow')` muss `'Slow down'` zurückgeben

```dart
  test('test2', () {
    expect(trafficLight('yellow'), 'Slow down', reason: '--err-t2--');
  });
```

`trafficLight('red')` muss `'Stop'` zurückgeben

```dart
  test('test3', () {
    expect(trafficLight('red'), 'Stop', reason: '--err-t3--');
  });
```

`trafficLight('blue')` muss `'Invalid'` zurückgeben

```dart
  test('test4', () {
    expect(trafficLight('blue'), 'Invalid', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String trafficLight(String color) {
  switch (color) {
    case 'green':
      return 'Go';
    case 'yellow':
      return 'Slow down';
    case 'red':
      return 'Stop';
    default:
      return 'Invalid';
  }
}

void main() {
  print(trafficLight('green'));
}
```
