---
language: dart
exerciseType: 1
---

# --instructions--

Schreibe eine Funktion `absoluteValue(int n)`, die den Absolutwert von `n` mithilfe des ternären Operators zurückgibt. Die Funktion soll `n` zurückgeben, wenn es nicht negativ ist, und `-n` andernfalls.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`absoluteValue(7)` muss `7` zurückgeben

```dart
  test('test1', () {
    expect(absoluteValue(7), 7, reason: '--err-t1--');
  });
```

`absoluteValue(-4)` muss `4` zurückgeben

```dart
  test('test2', () {
    expect(absoluteValue(-4), 4, reason: '--err-t2--');
  });
```

`absoluteValue(0)` muss `0` zurückgeben

```dart
  test('test3', () {
    expect(absoluteValue(0), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int absoluteValue(int n) {
  return n >= 0 ? n : -n;
}

void main() {
  print(absoluteValue(-4));
}
```
