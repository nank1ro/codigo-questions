---
language: dart
exerciseType: 1
---

# --instructions--

Schreibe eine Funktion `describe(int n)`, die `'positive'` zurückgibt, wenn `n` größer als `0` ist, und `'non-positive'` andernfalls, mithilfe von `if-else`.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`describe(10)` muss `'positive'` zurückgeben

```dart
  test('test1', () {
    expect(describe(10), 'positive', reason: '--err-t1--');
  });
```

`describe(0)` muss `'non-positive'` zurückgeben

```dart
  test('test2', () {
    expect(describe(0), 'non-positive', reason: '--err-t2--');
  });
```

`describe(-5)` muss `'non-positive'` zurückgeben

```dart
  test('test3', () {
    expect(describe(-5), 'non-positive', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String describe(int n) {
  if (n > 0) {
    return 'positive';
  } else {
    return 'non-positive';
  }
}

void main() {
  print(describe(10));
}
```
