---
language: dart
exerciseType: 1
---

# --instructions--

Schreibe eine Funktion `parity(int n)`, die `'even'` zurückgibt, wenn `n` durch `2` teilbar ist, und `'odd'` andernfalls, mithilfe einer `if-else`-Anweisung.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parity(4)` muss `'even'` zurückgeben

```dart
  test('test1', () {
    expect(parity(4), 'even', reason: '--err-t1--');
  });
```

`parity(7)` muss `'odd'` zurückgeben

```dart
  test('test2', () {
    expect(parity(7), 'odd', reason: '--err-t2--');
  });
```

`parity(0)` muss `'even'` zurückgeben

```dart
  test('test3', () {
    expect(parity(0), 'even', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String parity(int n) {
  if (n % 2 == 0) {
    return 'even';
  } else {
    return 'odd';
  }
}

void main() {
  print(parity(4));
}
```
