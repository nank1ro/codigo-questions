---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere eine globale `bool`-Variable namens `isHot`. Weise ihr `true` zu, wenn die globale `int`-Variable `temp` (auf `35` gesetzt) größer als `30` ist, mithilfe einer `if`-Anweisung.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`temp` muss gleich `35` sein

```dart
  test('test1', () {
    expect(temp, 35, reason: '--err-t1--');
  });
```

`isHot` muss `true` sein

```dart
  test('test2', () {
    expect(isHot, true, reason: '--err-t2--');
    expect(isHot, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int temp = 35;
bool isHot = false;

void main() {
  if (temp > 30) {
    isHot = true;
  }
  print(isHot);
}
```
