---
language: dart
exerciseType: 2
---

# --description--

Der **ternäre Operator** `condition ? expr1 : expr2` ist eine kompakte Möglichkeit, einen einfachen `if-else`-Ausdruck zu schreiben.

```dart
int x = 10;
String label = x > 0 ? 'positive' : 'non-positive';
print(label); // positive
```

Wenn die Bedingung `true` ist, wird `expr1` verwendet; andernfalls wird `expr2` verwendet.

# --instructions--

Vervollständige den ternären Ausdruck, um `'Even'` zu `parity` zuzuweisen, wenn `n` durch `2` teilbar ist, und `'Odd'` andernfalls.

# --seed--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 [/] 'Even' [/] 'Odd';
  print(parity);
}
```

# --answers--

- ?
- :
- ==
- ;

# --solutions--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 ? 'Even' : 'Odd';
  print(parity);
}
```

# --output--

Even
