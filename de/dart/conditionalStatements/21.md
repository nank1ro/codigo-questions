---
language: dart
exerciseType: 1
---

# --instructions--

Schreibe eine Funktion `minOf(int a, int b)`, die mithilfe des ternären Operators die kleinere von zwei Zahlen zurückgibt.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`minOf(3, 7)` muss `3` zurückgeben

```dart
  test('test1', () {
    expect(minOf(3, 7), 3, reason: '--err-t1--');
  });
```

`minOf(10, 2)` muss `2` zurückgeben

```dart
  test('test2', () {
    expect(minOf(10, 2), 2, reason: '--err-t2--');
  });
```

`minOf(5, 5)` muss `5` zurückgeben

```dart
  test('test3', () {
    expect(minOf(5, 5), 5, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int minOf(int a, int b) {
  return a < b ? a : b;
}

void main() {
  print(minOf(3, 7));
}
```
