---
language: dart
exerciseType: 1
---

# --instructions--

Schreibe eine Funktion `seasonName(int month)`, die die Jahreszeit für eine gegebene Monatszahl mithilfe einer `switch`-Anweisung zurückgibt. Monate `12`, `1`, `2` → `'Winter'`; `3`, `4`, `5` → `'Spring'`; `6`, `7`, `8` → `'Summer'`; `9`, `10`, `11` → `'Autumn'`. Gib `'Unknown'` für jeden anderen Wert zurück.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`seasonName(1)` muss `'Winter'` zurückgeben

```dart
  test('test1', () {
    expect(seasonName(1), 'Winter', reason: '--err-t1--');
  });
```

`seasonName(4)` muss `'Spring'` zurückgeben

```dart
  test('test2', () {
    expect(seasonName(4), 'Spring', reason: '--err-t2--');
  });
```

`seasonName(7)` muss `'Summer'` zurückgeben

```dart
  test('test3', () {
    expect(seasonName(7), 'Summer', reason: '--err-t3--');
  });
```

`seasonName(10)` muss `'Autumn'` zurückgeben

```dart
  test('test4', () {
    expect(seasonName(10), 'Autumn', reason: '--err-t4--');
  });
```

`seasonName(13)` muss `'Unknown'` zurückgeben

```dart
  test('test5', () {
    expect(seasonName(13), 'Unknown', reason: '--err-t5--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String seasonName(int month) {
  switch (month) {
    case 12:
    case 1:
    case 2:
      return 'Winter';
    case 3:
    case 4:
    case 5:
      return 'Spring';
    case 6:
    case 7:
    case 8:
      return 'Summer';
    case 9:
    case 10:
    case 11:
      return 'Autumn';
    default:
      return 'Unknown';
  }
}

void main() {
  print(seasonName(7));
}
```
