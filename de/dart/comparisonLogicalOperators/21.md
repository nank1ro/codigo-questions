---
language: dart
exerciseType: 4
---

# --instructions--

Sortiere die Codezeilen, um einen Temperaturwert zu deklarieren, zu überprüfen, ob er im Bereich `0` bis `100` liegt, unter Verwendung von `&&`, das Ergebnis in `isValid` zu speichern und auszugeben. Die Ausgabe sollte `true` sein.

# --answers--

```dart
void main() {
```

```dart
  int temperature = 37;
```

```dart
  bool isValid = temperature >= 0 && temperature <= 100;
```

```dart
  print(isValid);
```

```dart
}
```

# --solutions--

```dart
void main() {
  int temperature = 37;
  bool isValid = temperature >= 0 && temperature <= 100;
  print(isValid);
}
```

# --output--

true
