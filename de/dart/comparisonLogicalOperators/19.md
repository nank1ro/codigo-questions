---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere drei globale `int`-Variablen: `number` mit dem Wert `42`, `min` mit dem Wert `1` und `max` mit dem Wert `100`. Deklariere dann eine globale `bool`-Variable namens `inRange`, die `true` ist, wenn `number` größer als oder gleich `min` UND kleiner als oder gleich `max` ist.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`inRange` muss `true` sein und vom Typ `bool`

```dart
  test('test1', () {
    expect(inRange, true, reason: '--err-t1--');
    expect(inRange, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int number = 42;
int min = 1;
int max = 100;
bool inRange = number >= min && number <= max;

void main() {
  print(inRange);
}
```
