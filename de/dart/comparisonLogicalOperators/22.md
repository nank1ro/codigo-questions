---
language: dart
exerciseType: 4
---

# --instructions--

Sortiere die Codezeilen, um zwei boolesche Variablen zu deklarieren, sie mit `||` und `&&` wie gezeigt zu kombinieren, das Ergebnis in `outcome` zu speichern und auszugeben. Die Ausgabe sollte `true` sein.

# --answers--

```dart
void main() {
```

```dart
  bool hasPass = false;
```

```dart
  bool isAdmin = true;
```

```dart
  bool outcome = hasPass || isAdmin && !hasPass;
```

```dart
  print(outcome);
```

```dart
}
```

# --solutions--

```dart
void main() {
  bool hasPass = false;
  bool isAdmin = true;
  bool outcome = hasPass || isAdmin && !hasPass;
  print(outcome);
}
```

# --output--

true
