---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere zwei `int`-Variablen auf oberster Ebene: `high` mit dem Wert `20` und `low` mit dem Wert `12`. Deklariere dann eine `bool`-Variable auf oberster Ebene namens `isHigher`, die das Ergebnis der Überprüfung speichert, ob `high` mithilfe des Operators `>` größer als `low` ist.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHigher` muss `true` sein und vom Typ `bool`

```dart
  test('test1', () {
    expect(isHigher, true, reason: '--err-t1--');
    expect(isHigher, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int high = 20;
int low = 12;
bool isHigher = high > low;

void main() {
  print(isHigher);
}
```
