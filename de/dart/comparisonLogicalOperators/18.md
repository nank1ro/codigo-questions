---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere drei globale `int`-Variablen: `x` mit dem Wert `5`, `y` mit dem Wert `3` und `z` mit dem Wert `0`. Deklariere dann eine globale `bool`-Variable namens `result`, die `!(x > 10) || (y < 5 && z == 0)` auswertet.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` muss `true` sein und vom Typ `bool`

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 5;
int y = 3;
int z = 0;
bool result = !(x > 10) || (y < 5 && z == 0);

void main() {
  print(result);
}
```
