---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere eine globale `int`-Variable namens `age` mit dem Wert `35`. Deklariere dann eine globale `bool`-Variable namens `isWorkingAge`, die `true` ist, wenn `age` größer als oder gleich `18` UND kleiner als oder gleich `65` ist.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isWorkingAge` muss `true` sein und vom Typ `bool`

```dart
  test('test1', () {
    expect(isWorkingAge, true, reason: '--err-t1--');
    expect(isWorkingAge, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int age = 35;
bool isWorkingAge = age >= 18 && age <= 65;

void main() {
  print(isWorkingAge);
}
```
