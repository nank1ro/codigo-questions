---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere zwei globale `int`-Variablen: `x` mit dem Wert `8` und `y` mit dem Wert `8`. Deklariere dann eine globale `bool`-Variable namens `areEqual`, die das Ergebnis des Vergleichs von `x` und `y` mit dem `==`-Operator speichert.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`areEqual` muss `true` sein und vom Typ `bool`

```dart
  test('test1', () {
    expect(areEqual, true, reason: '--err-t1--');
    expect(areEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 8;
int y = 8;
bool areEqual = x == y;

void main() {
  print(areEqual);
}
```
