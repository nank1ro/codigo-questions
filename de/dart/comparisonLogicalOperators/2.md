---
language: dart
exerciseType: 2
---

# --description--

Der **Ungleichheitsoperator** `!=` gibt `true` zurück, wenn zwei Werte **unterschiedlich** sind, und `false`, wenn sie gleich sind.

```dart
int x = 3;
int y = 7;
bool result = x != y; // true
```

Er ist das Gegenteil von `==`.

# --instructions--

Vervollständige den Code, um zu überprüfen, ob `4` ungleich `9` ist, und speichere das Ergebnis in `isDifferent`.

# --seed--

```dart
void main() {
  bool isDifferent = 4 [/] 9;
  print(isDifferent);
}
```

# --answers--

- !=
- ==
- !
- <>

# --solutions--

```dart
void main() {
  bool isDifferent = 4 != 9;
  print(isDifferent);
}
```

# --output--

true
