---
language: dart
exerciseType: 1
---

# --instructions--

Declare two top-level `int` variables: `a` with the value `5` and `b` with the value `9`. Then declare a top-level `bool` variable named `notEqual` that stores the result of comparing `a` and `b` with the `!=` operator.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`notEqual` must be `true` and of type `bool`

```dart
  test('test1', () {
    expect(notEqual, true, reason: '--err-t1--');
    expect(notEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 5;
int b = 9;
bool notEqual = a != b;

void main() {
  print(notEqual);
}
```
