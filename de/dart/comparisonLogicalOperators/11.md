---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere zwei `int`-Variablen auf oberster Ebene: `a` mit dem Wert `5` und `b` mit dem Wert `9`. Deklariere dann eine `bool`-Variable auf oberster Ebene namens `notEqual`, die das Ergebnis des Vergleichs von `a` und `b` mit dem Operator `!=` speichert.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`notEqual` muss `true` sein und vom Typ `bool`

```dart
  test('test1', () {
    expect(notEqual, true, reason: '--err-t1--');
    expect(notEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 5;
int b = 9;
bool notEqual = a != b;

void main() {
  print(notEqual);
}
```
