---
language: dart
exerciseType: 2
---

# --description--

Der **Größer-oder-gleich-Operator** `>=` gibt `true` zurück, wenn der linke Wert größer als **oder gleich** dem rechten Wert ist.

```dart
int score = 50;
bool passed = score >= 50; // true
```

Im Gegensatz zu `>` gibt dieser Operator auch `true` zurück, wenn beide Werte gleich sind.

# --instructions--

Vervollständige den Code, um zu überprüfen, ob `7` größer als oder gleich `7` ist, und speichere das Ergebnis in `isAtLeastSeven`.

# --seed--

```dart
void main() {
  bool isAtLeastSeven = 7 [/] 7;
  print(isAtLeastSeven);
}
```

# --answers--

- >=
- >
- <=
- ==

# --solutions--

```dart
void main() {
  bool isAtLeastSeven = 7 >= 7;
  print(isAtLeastSeven);
}
```

# --output--

true
