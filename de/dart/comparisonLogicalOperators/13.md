---
language: dart
exerciseType: 1
---

# --instructions--

Declare two top-level `int` variables: `price` with the value `30` and `budget` with the value `50`. Then declare a top-level `bool` variable named `canAfford` that stores the result of checking whether `price` is less than `budget` using the `<` operator.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canAfford` must be `true` and of type `bool`

```dart
  test('test1', () {
    expect(canAfford, true, reason: '--err-t1--');
    expect(canAfford, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int price = 30;
int budget = 50;
bool canAfford = price < budget;

void main() {
  print(canAfford);
}
```
