---
language: dart
exerciseType: 1
---

# --instructions--

Deklariere zwei `int`-Variablen auf oberster Ebene: `price` mit dem Wert `30` und `budget` mit dem Wert `50`. Deklariere dann eine `bool`-Variable auf oberster Ebene namens `canAfford`, die das Ergebnis der Überprüfung speichert, ob `price` mithilfe des Operators `<` kleiner als `budget` ist.

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canAfford` muss `true` sein und vom Typ `bool`

```dart
  test('test1', () {
    expect(canAfford, true, reason: '--err-t1--');
    expect(canAfford, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int price = 30;
int budget = 50;
bool canAfford = price < budget;

void main() {
  print(canAfford);
}
```
