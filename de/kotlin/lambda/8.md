---
language: kotlin
exerciseType: 1
---

# --description--

Mit `reduce` ist die Form des Ergebnisses dem Lambda überlassen. Jede Operation, die zwei Werte kombiniert, funktioniert: eine Summe, ein Produkt oder das Beibehalten des größeren der beiden.
```kotlin
val numbers = listOf(3, 9, 4)
println(numbers.reduce { acc, n -> if (n > acc) n else acc }) // 9
```
Beachten Sie, dass `reduce` bei einer leeren Liste eine Ausnahme auslöst, weil es kein erstes Element als Startpunkt gibt.

# --instructions--

Schreiben Sie eine Funktion `product(numbers: List<Int>): Int`, die das Produkt aller Zahlen in der Liste zurückgibt, unter Verwendung von `reduce`. Die Liste ist nie leer.

Beispiel: `product(listOf(2, 3, 4))` gibt `24` zurück.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`product(listOf(2, 3, 4))` muss `24` zurückgeben

```kotlin
    tryCatch(product(listOf(2, 3, 4)) == 24)
```

`product(listOf(5))` muss `5` zurückgeben

```kotlin
    tryCatch(product(listOf(5)) == 5)
```

`product(listOf(2, -3, 5))` muss `-30` zurückgeben

```kotlin
    tryCatch(product(listOf(2, -3, 5)) == -30)
```

`product(listOf(7, 0, 9))` muss `0` zurückgeben

```kotlin
    tryCatch(product(listOf(7, 0, 9)) == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun product(numbers: List<Int>): Int {
    return numbers.reduce { acc, n -> acc * n }
}
```
