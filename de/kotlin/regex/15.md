---
language: kotlin
exerciseType: 4
---

# --description--

Weil `findAll` eine Sequenz liefert, funktionieren die aggregierenden Methoden, die du schon kennst, auch auf Treffern: `sumOf`, `maxOfOrNull`, `filter`, `sortedBy`. Zahlen aus freiem Text zu extrahieren ist eine Aufgabe in zwei Schritten: erst die Treffer finden, dann den Text in eine Zahl umwandeln.
```kotlin
val total = Regex("""\d+""").findAll("2 apples, 3 pears").sumOf { it.value.toInt() }
println(total) // 5
```

# --instructions--

Kannst du diese Elemente so ordnen, dass `total: 17` ausgegeben wird, indem jede Zahl im Text aufsummiert wird?

# --answers--

```kotlin
val text = "ann scored 8 and bob scored 9"
```

```kotlin
val numbers = Regex("""\d+""").findAll(text)
```

```kotlin
val total = numbers.sumOf { it.value.toInt() }
```

```kotlin
println("total: $total")
```

# --solutions--

```kotlin
val text = "ann scored 8 and bob scored 9"
val numbers = Regex("""\d+""").findAll(text)
val total = numbers.sumOf { it.value.toInt() }
println("total: $total")
```

# --output--

total: 17
