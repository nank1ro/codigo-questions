---
language: kotlin
exerciseType: 1
---

# --description--

`findAll` und Gruppen verwandeln zusammen eine Textzeile in strukturierte Daten. Jedes `MatchResult` der Sequenz trägt sein eigenes `groupValues`, sodass eine einzige Kette eine Liste, eine Map oder eine Summe bauen kann:
```kotlin
val pair = Regex("""(\w+)=(\w+)""")
val config = pair.findAll("host=local;port=80").associate { it.groupValues[1] to it.groupValues[2] }
println(config) // {host=local, port=80}
```
`associate` baut aus den `key to value`-Paaren, die das Lambda zurückgibt, eine Map. Wenn ein Muster eine feste Anzahl von Gruppen hat, kannst du mit `destructured` die Gruppen in benannte Variablen entpacken, statt sie über den Index zu lesen:
```kotlin
val (key, value) = pair.find("host=local")!!.destructured
println(key)   // host
println(value) // local
```

# --instructions--

Schreibe eine Funktion `parseConfig(text: String): Map<String, String>`, die jedes `key=value`-Paar aus `text` liest, wobei Schlüssel und Werte Folgen von Wortzeichen sind, und sie als Map zurückgibt.

Beispiel: `parseConfig("host=local;port=80")` gibt `{host=local, port=80}` zurück.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`parseConfig("host=local;port=80")` muss `{host=local, port=80}` zurückgeben

```kotlin
    tryCatch(parseConfig("host=local;port=80") == mapOf("host" to "local", "port" to "80"))
```

`parseConfig("mode=dark")` muss `{mode=dark}` zurückgeben

```kotlin
    tryCatch(parseConfig("mode=dark") == mapOf("mode" to "dark"))
```

`parseConfig("a=1, b=2, c=3")` muss drei Einträge zurückgeben

```kotlin
    tryCatch(parseConfig("a=1, b=2, c=3") == mapOf("a" to "1", "b" to "2", "c" to "3"))
```

`parseConfig("nothing here")` muss eine leere Map zurückgeben

```kotlin
    tryCatch(parseConfig("nothing here") == mapOf<String, String>())
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun parseConfig(text: String): Map<String, String> {
    return Regex("""(\w+)=(\w+)""").findAll(text).associate { it.groupValues[1] to it.groupValues[2] }
}
```
