---
language: kotlin
exerciseType: 1
---

# --description--

Runde Klammern in einem Muster erzeugen eine **erfassende Gruppe**: ein Teil des Treffers, den du separat zurücklesen willst. `MatchResult.groupValues` speichert sie, mit dem Index `0` für den ganzen Treffer und `1`, `2`, ... für die Gruppen, von links nach rechts:
```kotlin
val date = Regex("""(\d{4})-(\d{2})-(\d{2})""")
val match = date.find("released 2026-09-12 in Rome")
println(match?.groupValues?.get(0)) // 2026-09-12
println(match?.groupValues?.get(1)) // 2026
println(match?.groupValues?.get(2)) // 09
```
Wenn es überhaupt keinen Treffer gibt, gibt `find` `null` zurück und es gibt nichts zu lesen, daher legt eine Funktion, die eine Gruppe extrahiert, üblicherweise fest, was sie in diesem Fall zurückgibt:
```kotlin
val match = date.find("no date here")
println(match?.groupValues?.get(1) ?: "unknown") // unknown
```

# --instructions--

Schreibe eine Funktion `monthOf(date: String): String`, die den Monat eines Datums im Format `YYYY-MM-DD` zurückgibt, oder eine leere Zeichenkette, wenn `date` kein solches Datum enthält.

Beispiel: `monthOf("2026-09-12")` gibt `09` zurück.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`monthOf("2026-09-12")` muss `09` zurückgeben

```kotlin
    tryCatch(monthOf("2026-09-12") == "09")
```

`monthOf("1999-12-31")` muss `12` zurückgeben

```kotlin
    tryCatch(monthOf("1999-12-31") == "12")
```

`monthOf("due 2024-01-05 sharp")` muss `01` zurückgeben

```kotlin
    tryCatch(monthOf("due 2024-01-05 sharp") == "01")
```

`monthOf("no date")` muss eine leere Zeichenkette zurückgeben

```kotlin
    tryCatch(monthOf("no date") == "")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun monthOf(date: String): String {
    val match = Regex("""(\d{4})-(\d{2})-(\d{2})""").find(date)
    return match?.groupValues?.get(2) ?: ""
}
```
