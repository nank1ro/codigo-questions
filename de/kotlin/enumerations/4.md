---
language: kotlin
exerciseType: 2
---

# --description--

Eine Enum-Klasse kann einen **Konstruktor** haben, genau wie eine gewöhnliche Klasse. Jeder Eintrag übergibt dann seine eigenen Argumente, und die Werte werden in Eigenschaften gespeichert:
```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
println(Planet.MARS.moons) // 2
```
Konstruktor-Eigenschaften werden meist mit `val` deklariert, da sich die Daten eines Eintrags nicht ändern sollen.

# --instructions--

Vervollständige den Code, sodass jede `Coin` ihren Wert in Cent speichert, und gib dann den Wert einer `PENNY` aus (die `1` Cent wert ist)

# --seed--

```kotlin
enum class Coin([/] cents: Int) {
    PENNY([/]),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.[/])
}
```

# --answers--

- val
- fun
- const
- 1
- "1"
- cents
- name
- ordinal

# --solutions--

```kotlin
enum class Coin(val cents: Int) {
    PENNY(1),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.cents)
}
```

# --output--

1
