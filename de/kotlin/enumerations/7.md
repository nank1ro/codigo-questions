---
language: kotlin
exerciseType: 1
---

# --description--

Um von einem `String` zurück zu einem Eintrag zu gelangen, verwende die Funktion `valueOf`. Sie sucht den Eintrag, dessen `name` exakt übereinstimmt:
```kotlin
val direction = Direction.valueOf("EAST")
println(direction == Direction.EAST) // true
```
Der Abgleich unterscheidet zwischen Groß- und Kleinschreibung: `Direction.valueOf("east")` wirft eine `IllegalArgumentException`, weil kein Eintrag diesen Namen hat.

# --instructions--

Deklariere eine Enum-Klasse `Status` mit den Einträgen `ACTIVE` und `INACTIVE`. Schreibe dann eine Funktion `statusOf(text: String)`, die den zu `text` passenden `Status` zurückgibt, unabhängig von der Groß-/Kleinschreibung (sodass `"active"`, `"Active"` und `"ACTIVE"` alle `Status.ACTIVE` ergeben).

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`statusOf("ACTIVE")` muss `Status.ACTIVE` zurückgeben

```kotlin
    tryCatch(statusOf("ACTIVE") == Status.ACTIVE)
```

`statusOf("inactive")` muss `Status.INACTIVE` zurückgeben

```kotlin
    tryCatch(statusOf("inactive") == Status.INACTIVE)
```

`statusOf("Active")` muss `Status.ACTIVE` zurückgeben

```kotlin
    tryCatch(statusOf("Active") == Status.ACTIVE)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
enum class Status {
    ACTIVE, INACTIVE
}

fun statusOf(text: String): Status {
    return Status.valueOf(text.uppercase())
}
```
