---
language: kotlin
exerciseType: 2
---

# --description--

Eine **Schnittstelle** (Interface) deklariert Methoden ohne Rumpf; jeder Typ, der sie implementiert, muss sie bereitstellen:
```kotlin
interface Greeter {
    fun greet(): String
}
```
Enum-Klassen können Schnittstellen implementieren. Du listest die Schnittstelle nach einem Doppelpunkt auf und markierst jede Implementierung mit `override`. Innerhalb des Enum-Rumpfs ist der aktuelle Eintrag `this`, und andere Einträge können ohne den Klassennamen referenziert werden:
```kotlin
enum class Language : Greeter {
    ENGLISH, ITALIAN;

    override fun greet(): String = when (this) {
        ENGLISH -> "Hello"
        ITALIAN -> "Ciao"
    }
}
println(Language.ITALIAN.greet()) // Ciao
```

# --instructions--

Vervollständige den Code, sodass `Animal` `Speaker` implementiert, und gib dann den Laut einer `CAT` aus

# --seed--

```kotlin
interface Speaker {
    fun sound(): String
}

enum class Animal : [/] {
    DOG, CAT;

    [/] fun sound(): String = when (this) {
        DOG -> "Woof"
        CAT -> "Meow"
    }
}

fun main() {
    println(Animal.[/].sound())
}
```

# --answers--

- Speaker
- Speaker()
- Interface
- override
- open
- abstract
- CAT
- DOG
- "CAT"

# --solutions--

```kotlin
interface Speaker {
    fun sound(): String
}

enum class Animal : Speaker {
    DOG, CAT;

    override fun sound(): String = when (this) {
        DOG -> "Woof"
        CAT -> "Meow"
    }
}

fun main() {
    println(Animal.CAT.sound())
}
```

# --output--

Meow
