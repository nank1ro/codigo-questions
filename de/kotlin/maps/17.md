---
language: kotlin
exerciseType: 2
---

# --description--

`map` wandelt jeden Eintrag einer Map in ein neues Element um. Im Gegensatz zu `filter` ist das Ergebnis eine `List`, keine `Map`:
```kotlin
val ages = mapOf("Alice" to 30, "Bob" to 25)
val labels = ages.map { (name, age) -> "$name: $age" }
println(labels) // [Alice: 30, Bob: 25]
```

# --instructions--

Vervollständigen Sie den Code, um eine Liste von Strings der Form `"name is age"` zu erstellen, einen für jeden Eintrag von `ages`

# --seed--

```kotlin
val ages = mapOf("Ann" to 30, "Ben" to 25)
val labels = ages.[/] { ([/], [/]) -> "$name is $age" }
println(labels)
```

# --answers--

- map
- filter
- forEach
- name
- age
- key
- value

# --solutions--

```kotlin
val ages = mapOf("Ann" to 30, "Ben" to 25)
val labels = ages.map { (name, age) -> "$name is $age" }
println(labels)
```

# --output--

[Ann is 30, Ben is 25]
