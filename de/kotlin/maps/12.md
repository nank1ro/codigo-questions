---
language: kotlin
exerciseType: 4
---

# --instructions--

Können Sie diese Zeilen anordnen, um zu zählen, wie oft jede Frucht vorkommt, und `{apple=2, pear=1}` zu drucken? Deklarieren Sie `fruits` vor `counts`.

# --answers--

```kotlin
val fruits = listOf("apple", "pear", "apple")
```

```kotlin
val counts = mutableMapOf<String, Int>()
```

```kotlin
for (fruit in fruits) {
```

```kotlin
    counts[fruit] = counts.getOrDefault(fruit, 0) + 1
```

```kotlin
}
```

```kotlin
println(counts)
```

# --solutions--

```kotlin
val fruits = listOf("apple", "pear", "apple")
val counts = mutableMapOf<String, Int>()
for (fruit in fruits) {
    counts[fruit] = counts.getOrDefault(fruit, 0) + 1
}
println(counts)
```

# --output--

{apple=2, pear=1}
