---
language: kotlin
exerciseType: 2
---

# --description--

Ein Array direkt auszugeben zeigt nicht seine Elemente, sondern etwas wie `[Ljava.lang.String;@1b6d3586`.
Verwende `joinToString`, um eine lesbare Zeichenkette zu erstellen, optional mit einem eigenen Trennzeichen (der Standard ist `", "`), oder `contentToString`, um die Elemente zwischen eckigen Klammern zu erhalten:
```kotlin
val nums = intArrayOf(1, 2, 3)
println(nums.joinToString())      // 1, 2, 3
println(nums.joinToString("-"))   // 1-2-3
println(nums.contentToString())   // [1, 2, 3]
```

# --instructions--

Vervollständige den Code, um die Früchte getrennt durch `" - "` auszugeben, dann das gesamte Array zwischen eckigen Klammern

# --seed--

```kotlin
val fruits = arrayOf("apple", "kiwi", "plum")
println(fruits.[/]([/]))
println(fruits.[/]())
```

# --answers--

- joinToString
- contentToString
- toString
- join
- " - "
- "-"

# --solutions--

```kotlin
val fruits = arrayOf("apple", "kiwi", "plum")
println(fruits.joinToString(" - "))
println(fruits.contentToString())
```

# --output--

apple - kiwi - plum
[apple, kiwi, plum]
