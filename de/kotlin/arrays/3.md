---
language: kotlin
exerciseType: 2
---

# --description--

Jedes Array hat zwei praktische Eigenschaften für die Arbeit mit Indizes:
- `indices` ist der Bereich gültiger Indizes, von `0` bis zum letzten
- `lastIndex` ist der Index des letzten Elements, also `size - 1`
```kotlin
val letters = arrayOf("x", "y")
println(letters.lastIndex) // 1
for (i in letters.indices) {
    println(letters[i])
}
// prints x, then y
```

# --instructions--

Vervollständige den Code, um den letzten Index von `letters` auszugeben, dann jeden Index gefolgt von seinem Element

# --seed--

```kotlin
val letters = arrayOf("a", "b", "c")
println(letters.[/])
for (i in letters.[/]) {
    println("$i: ${letters[i]}")
}
```

# --answers--

- lastIndex
- indices
- size
- length
- last

# --solutions--

```kotlin
val letters = arrayOf("a", "b", "c")
println(letters.lastIndex)
for (i in letters.indices) {
    println("$i: ${letters[i]}")
}
```

# --output--

2
0: a
1: b
2: c
