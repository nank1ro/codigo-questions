---
language: kotlin
exerciseType: 4
---

# --instructions--

Kannst du diese Zeilen ordnen, um die Quadrate von `0`, `1`, `2` und `3` zu bilden, sie in Labels umzuwandeln und jedes Label mit seinem Index auszugeben?

# --answers--

```kotlin
val squares = Array(4) { it * it }
```

```kotlin
val labels = squares.map { "sq=$it" }
```

```kotlin
for ((i, label) in labels.withIndex()) {
```

```kotlin
    println("$i $label")
```

```kotlin
}
```

# --solutions--

```kotlin
val squares = Array(4) { it * it }
val labels = squares.map { "sq=$it" }
for ((i, label) in labels.withIndex()) {
    println("$i $label")
}
```

# --output--

0 sq=0
1 sq=1
2 sq=4
3 sq=9
