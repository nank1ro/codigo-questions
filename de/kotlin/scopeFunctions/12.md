---
language: kotlin
exerciseType: 4
---

# --instructions--

Können Sie diese Elemente so ordnen, dass das Programm die Liste ausgibt, nachdem das neue Element hinzugefügt wurde, und dann ihre Größe?

# --answers--

```kotlin
val words = mutableListOf("fig")
```

```kotlin
val count = words
```

```kotlin
    .apply { add("kiwi") }
```

```kotlin
    .also { println("now: $it") }
```

```kotlin
    .size
```

```kotlin
println("size: $count")
```

# --solutions--

```kotlin
val words = mutableListOf("fig")
val count = words
    .apply { add("kiwi") }
    .also { println("now: $it") }
    .size
println("size: $count")
```

# --output--

now: [fig, kiwi]
size: 2
