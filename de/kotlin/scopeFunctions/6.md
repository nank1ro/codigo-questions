---
language: kotlin
exerciseType: 1
---

# --description--

`apply` ist nicht auf Objekte beschränkt, die Sie gerade erstellt haben: Es funktioniert auf jedem Objekt, und da es das Objekt zurückgibt, können Sie den ganzen Ausdruck überall dort verwenden, wo das Objekt erwartet wird.
```kotlin
val box = Box()
box.apply { label = "tools" }        // changes box and returns it
println(listOf(Box().apply { label = "nails" }).size) // 1
```
Der Block ist ein normaler Codeblock, daher kann er so viele Anweisungen enthalten, wie Sie brauchen.

# --instructions--

Erstellen Sie eine Klasse `Profile` mit zwei Eigenschaften, `var name: String` mit dem Anfangswert `""` und `var age: Int` mit dem Anfangswert `0`.

Schreiben Sie dann eine Funktion `makeProfile(n: String, a: Int): Profile`, die ein `Profile()` erstellt und `apply` verwendet, um `name` auf `n` und `age` auf `a` zu setzen, und das konfigurierte Objekt zurückgibt.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

Ein neues `Profile()` muss mit leerem Namen und Alter `0` starten

```kotlin
    val fresh = Profile()
    tryCatch(fresh.name == "" && fresh.age == 0)
```

`makeProfile("Ada", 36)` muss `name` gleich `Ada` haben

```kotlin
    tryCatch(makeProfile("Ada", 36).name == "Ada")
```

`makeProfile("Ada", 36)` muss `age` gleich `36` haben

```kotlin
    tryCatch(makeProfile("Ada", 36).age == 36)
```

`makeProfile("Bob", 7)` muss `name` gleich `Bob` und `age` gleich `7` haben

```kotlin
    val bob = makeProfile("Bob", 7)
    tryCatch(bob.name == "Bob" && bob.age == 7)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Profile {
    var name: String = ""
    var age: Int = 0
}

fun makeProfile(n: String, a: Int): Profile {
    return Profile().apply {
        name = n
        age = a
    }
}
```
