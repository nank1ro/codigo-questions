---
language: kotlin
exerciseType: 1
---

# --description--

Wenn der Block das Objekt als **Argument** von etwas anderem braucht, liest sich `also` besser als `apply`: `it` kann direkt weitergegeben werden, während `this` ausgeschrieben werden müsste.
```kotlin
val names = mutableListOf<String>()
val user = "ada".also { names.add(it) }
println(user)  // ada
println(names) // [ada]
```
Der Wert des Ausdrucks ist immer noch `"ada"`: `also` sieht nur zu, wie er vorbeizieht.

# --instructions--

Deklarieren Sie ein Top-level-`val log = mutableListOf<String>()`.

Schreiben Sie dann eine Funktion `record(word: String): String`, die `word` in Großbuchstaben zurückgibt und mit `also` denselben Wert in Großbuchstaben zu `log` hinzufügt, bevor sie ihn zurückgibt.

Beispiel: Nach `record("hi")` gibt die Funktion `HI` zurück und `log` enthält `HI`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`log` muss leer starten

```kotlin
    tryCatch(log.isEmpty())
```

`record("hi")` muss `HI` zurückgeben

```kotlin
    tryCatch(record("hi") == "HI")
```

Nach dem ersten Aufruf muss `log` nur `HI` enthalten

```kotlin
    tryCatch(log == listOf("HI"))
```

Ein zweiter Aufruf muss an `log` anhängen, ohne den ersten Wert zu verlieren

```kotlin
    record("bye")
    tryCatch(log == listOf("HI", "BYE"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
val log = mutableListOf<String>()

fun record(word: String): String {
    return word.uppercase().also { log.add(it) }
}
```
