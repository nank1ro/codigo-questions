---
language: kotlin
exerciseType: 4
---

# --description--

Eine Erweiterungsfunktion kann für einen **Nullable-Empfänger** deklariert werden, sodass sie sogar auf einem `null`-Wert aufgerufen werden kann. Innerhalb der Funktion ist `this` nullable und muss geprüft werden:
```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val none: String? = null
println(none.orDash()) // -
```
Beachten Sie, dass beim Aufruf kein `?.` nötig ist: Die Funktion selbst behandelt den `null`-Fall.
Die Standardbibliothek verwendet diesen Trick in `isNullOrEmpty()` und `orEmpty()`, die gefahrlos für jedes `String?` aufgerufen werden können.

# --instructions--

Können Sie diese Zeilen anordnen, um die Erweiterungsfunktion `orDash` zu definieren und `-` für einen fehlenden Namen auszugeben, dann `kotlin` für einen vorhandenen? Definieren Sie zuerst die Funktion

# --answers--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
```

```kotlin
val a: String? = null
val b: String? = "kotlin"
```

```kotlin
println(a.orDash())
```

```kotlin
println(b.orDash())
```

# --solutions--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val a: String? = null
val b: String? = "kotlin"
println(a.orDash())
println(b.orDash())
```

# --output--

-
kotlin
