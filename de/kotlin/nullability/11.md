---
language: kotlin
exerciseType: 2
---

# --description--

Auch Sammlungen können nullable Elemente enthalten: Eine `List<Int?>` kann `null`-Einträge enthalten, während eine `List<Int>` dies nie tut.
`filterNotNull()` gibt eine neue Liste ohne die `null`-Einträge zurück, und ihr Elementtyp wird non-null, sodass Sie die Elemente frei verwenden können:
```kotlin
val scores: List<Int?> = listOf(10, null, 20)
val valid = scores.filterNotNull() // List<Int> [10, 20]
println(valid.sum())               // 30
```

# --instructions--

Vervollständigen Sie den Code, um eine Liste zu deklarieren, die `null`-Zahlen enthalten kann, anschließend die `null`-Einträge zu entfernen und die verbleibenden Werte samt ihrer Summe auszugeben

# --seed--

```kotlin
val values: List<[/]> = listOf(3, null, 5, null)
val present = values.[/]()
println(present)
println(present.sum())
```

# --answers--

- Int?
- Int
- filterNotNull
- filter
- mapNotNull
- requireNotNull

# --solutions--

```kotlin
val values: List<Int?> = listOf(3, null, 5, null)
val present = values.filterNotNull()
println(present)
println(present.sum())
```

# --output--

[3, 5]
8
