---
language: kotlin
exerciseType: 1
---

# --description--

Die rechte Seite von `?:` kann ein beliebiger Ausdruck sein, auch `return`. So lässt sich kompakt aus einer Funktion aussteigen, sobald ein Wert fehlt:
```kotlin
fun firstUpper(text: String?): Char? {
    val first = text?.firstOrNull() ?: return null
    return first.uppercaseChar() // first is a Char here
}
```
Alle Werkzeuge, die Sie kennengelernt haben, lassen sich gut kombinieren: Nullable-Parameter und Rückgabetypen beschreiben, *wo* ein Wert fehlen kann, und `?.`, `?:`, `let`, Smart-Casts und `toIntOrNull` behandeln ihn, ohne dass das Programm jemals abstürzt.

# --instructions--

Schreiben Sie eine Funktion `divide(a: String?, b: String?): Int?`, die beide Zeichenketten in ganze Zahlen umwandelt und `a / b` zurückgibt. Sie muss `null` zurückgeben, wenn eine der Zeichenketten `null` ist, wenn eine keine ganze Zahl ist oder wenn `b` `0` ist.

Beispiel: `divide("10", "2")` gibt `5` zurück, `divide("10", "x")` gibt `null` zurück.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`divide("10", "2")` muss `5` zurückgeben

```kotlin
    tryCatch(divide("10", "2") == 5)
```

`divide("10", "x")` muss `null` zurückgeben

```kotlin
    tryCatch(divide("10", "x") == null)
```

`divide(null, "2")` muss `null` zurückgeben

```kotlin
    tryCatch(divide(null, "2") == null)
```

`divide("9", null)` muss `null` zurückgeben

```kotlin
    tryCatch(divide("9", null) == null)
```

`divide("7", "0")` muss `null` zurückgeben

```kotlin
    tryCatch(divide("7", "0") == null)
```

`divide("-9", "3")` muss `-3` zurückgeben

```kotlin
    tryCatch(divide("-9", "3") == -3)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun divide(a: String?, b: String?): Int? {
    val x = a?.toIntOrNull() ?: return null
    val y = b?.toIntOrNull() ?: return null
    if (y == 0) return null
    return x / y
}
```
