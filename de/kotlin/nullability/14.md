---
language: kotlin
exerciseType: 1
---

# --description--

Wenn ein `null` bedeutet, dass der Aufrufer einen Fehler gemacht hat, brechen Sie früh mit `requireNotNull` ab.
Es gibt den Wert als non-null zurück, wenn er vorhanden ist, und wirft eine `IllegalArgumentException`, wenn er `null` ist, mit einer optionalen Meldung:
```kotlin
fun greet(name: String?): String {
    val safeName = requireNotNull(name) { "name is required" }
    return "Hello, $safeName"
}
greet(null) // IllegalArgumentException: name is required
```
Nach dem Aufruf führt der Compiler auch einen Smart-Cast von `name` selbst zu `String` durch, sodass `name.length` ab dieser Zeile erlaubt ist.
Anders als bei `!!` trägt der Fehler eine klare Meldung und sagt aus, dass das *Argument* falsch war.

# --instructions--

Schreiben Sie eine Funktion `safeUpper(text: String?): String`, die `text` in Großbuchstaben zurückgibt. Wenn `text` `null` ist, muss sie eine `IllegalArgumentException` mit der Meldung `text is required` auslösen, unter Verwendung von `requireNotNull`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`safeUpper("abc")` muss `"ABC"` zurückgeben

```kotlin
    tryCatch(safeUpper("abc") == "ABC")
```

`safeUpper("Kotlin")` muss `"KOTLIN"` zurückgeben

```kotlin
    tryCatch(safeUpper("Kotlin") == "KOTLIN")
```

`safeUpper(null)` muss eine `IllegalArgumentException` auslösen

```kotlin
    var threw = false
    try {
        safeUpper(null)
    } catch (e: IllegalArgumentException) {
        threw = true
    }
    tryCatch(threw)
```

Die Ausnahmemeldung muss `text is required` sein

```kotlin
    var message = ""
    try {
        safeUpper(null)
    } catch (e: IllegalArgumentException) {
        message = e.message ?: ""
    }
    tryCatch(message == "text is required")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun safeUpper(text: String?): String {
    val value = requireNotNull(text) { "text is required" }
    return value.uppercase()
}
```
