---
language: kotlin
exerciseType: 4
---

# --instructions--

Kannst du diese Zeilen in der richtigen Reihenfolge anordnen, um eine `open`-Klasse `Shape` mit einer `open`-Methode `area()`, eine Unterklasse `Square`, die sie überschreibt, und die Ausgabe des Flächeninhalts zu definieren?

# --answers--

```kotlin
open class Shape {
```

```kotlin
    open fun area(): Int = 0
```

```kotlin
}
```

```kotlin
class Square(val side: Int) : Shape() {
```

```kotlin
    override fun area(): Int = side * side
```

```kotlin
}
```

```kotlin
fun main() {
```

```kotlin
    println(Square(4).area())
```

```kotlin
}
```

# --solutions--

```kotlin
open class Shape {
    open fun area(): Int = 0
}
class Square(val side: Int) : Shape() {
    override fun area(): Int = side * side
}
fun main() {
    println(Square(4).area())
}
```

# --output--

16
