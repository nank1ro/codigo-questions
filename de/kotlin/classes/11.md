---
language: kotlin
exerciseType: 2
---

# --instructions--

Vervollständige den Code, um ein `companion object` in `AppConfig` hinzuzufügen, das eine `val`-Eigenschaft `version` mit dem Wert `"1.0"` enthält.

# --seed--

```kotlin
class AppConfig {
    [/] [/] {
        val version = "1.0"
    }
}
```

# --answers--

- companion
- object
- class
- static
- init
- val

# --solutions--

```kotlin
class AppConfig {
    companion object {
        val version = "1.0"
    }
}
```
