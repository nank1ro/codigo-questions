---
language: kotlin
exerciseType: 2
---

# --instructions--

Vervollständige den Code, um eine `data class` namens `Color` mit `val`-Eigenschaften `red`, `green` und `blue` vom Typ `Int` zu deklarieren.

# --seed--

```kotlin
[/] [/] Color(val red: Int, val green: Int, val blue: Int)
```

# --answers--

- data
- class
- object
- open
- abstract
- sealed

# --solutions--

```kotlin
data class Color(val red: Int, val green: Int, val blue: Int)
```
