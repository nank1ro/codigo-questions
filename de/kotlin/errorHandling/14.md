---
language: kotlin
exerciseType: 4
---

# --description--

Weil ein `Result` ein gewöhnlicher Wert ist, kann er in einem `val` gespeichert und beliebig oft befragt werden:

```kotlin
val result = runCatching { "abc".toInt() }

println(result.getOrElse { 0 })  // 0
println(result.getOrNull())      // null
println(result.isSuccess)        // false
```
Ein `try`/`catch` kann das nicht. Dort wird das Ergebnis einmal behandelt, am Ort des Fehlschlags, und ist danach verschwunden. Ein `Result` hält den Fehler fest, sodass der Code, der darauf reagiert, nicht dort stehen muss, wo er passiert ist.

# --instructions--

Ordnen Sie die Elemente so, dass das Programm das Ergebnis der Umwandlung speichert, dann den wiederhergestellten Wert ausgibt, dann das rohe Ergebnis und schließlich, ob der Versuch erfolgreich war.

# --answers--

```kotlin
val result = runCatching { "abc".toInt() }
```

```kotlin
val value = result.getOrElse { 0 }
```

```kotlin
println("value: $value")
```

```kotlin
println("raw: ${result.getOrNull()}")
```

```kotlin
println("success: ${result.isSuccess}")
```

# --solutions--

```kotlin
val result = runCatching { "abc".toInt() }
val value = result.getOrElse { 0 }
println("value: $value")
println("raw: ${result.getOrNull()}")
println("success: ${result.isSuccess}")
```

# --output--

value: 0
raw: null
success: false
