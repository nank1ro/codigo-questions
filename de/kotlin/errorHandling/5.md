---
language: kotlin
exerciseType: 2
---

# --description--

Ein `finally`-Block kann am Ende ergänzt werden. Er wird **unabhängig davon ausgeführt, was passiert**: nach einem erfolgreichen `try`, nachdem ein `catch` die Exception behandelt hat, und sogar dann, wenn die Exception gar nicht abgefangen wird.

```kotlin
try {
    println("reading")
    println("abc".toInt())
} catch (e: NumberFormatException) {
    println("bad number")
} finally {
    println("closing")
}
```
```
reading
bad number
closing
```
Das macht ihn zum Ort für Aufräumarbeiten, die nicht übersprungen werden dürfen, etwa das Schließen einer Datei. Ein `try` braucht mindestens ein `catch` oder ein `finally`, darf aber auch beides haben.

# --instructions--

Vervollständigen Sie den Code, sodass die fehlgeschlagene Umwandlung abgefangen wird und die Aufräumezeile immer ausgeführt wird.

# --seed--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } [/] (e: NumberFormatException) {
        println("bad number")
    } [/] {
        println("closing")
    }
}
```

# --answers--

- catch
- finally
- try
- throw
- else
- do

# --solutions--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } catch (e: NumberFormatException) {
        println("bad number")
    } finally {
        println("closing")
    }
}
```

# --output--

bad number
closing
