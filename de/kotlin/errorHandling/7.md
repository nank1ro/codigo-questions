---
language: kotlin
exerciseType: 2
---

# --description--

Da `try` ein Ausdruck ist, kann es überall dort verwendet werden, wo ein Wert erwartet wird — auch als gesamter Funktionskörper einer mit `=` geschriebenen Funktion:
```kotlin
fun length(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    -1
}
```
Beide Blöcke müssen einen Wert desselben Typs liefern, hier `Int`. Schreiben Sie den Ersatzwert als letzten Ausdruck in den `catch`-Block; in keinem der beiden Blöcke steht ein `return`.

# --instructions--

Vervollständigen Sie die Funktion so, dass sie die Zahl in `text` zurückgibt, und `0`, wenn der Text keine Zahl ist.

# --seed--

```kotlin
fun size(text: String): Int = [/] {
    text.toInt()
} [/] (e: NumberFormatException) {
    [/]
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --answers--

- try
- catch
- 0
- finally
- throw
- 1

# --solutions--

```kotlin
fun size(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    0
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --output--

15
0
