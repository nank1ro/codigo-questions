---
language: kotlin
exerciseType: 1
---

# --description--

Wenn ein Funktionskörper aus einem einzigen Ausdruck besteht, kann man die Einzelausdrucks-Syntax mit `=` verwenden:
```kotlin
fun square(n: Int): Int = n * n
```
Dies ist kompakter als die Blockkörper-Form. Kotlin kann den Rückgabetyp oft ableiten, sodass man auch schreiben kann:
```kotlin
fun square(n: Int) = n * n
```
Einzelausdrucks-Funktionen sind ein gängiges Kotlin-Idiom für einfache Berechnungen.

# --instructions--

Definiere eine Einzelausdrucks-Funktion namens `addThree`, die einen `Int`-Parameter `n` nimmt und `n + 3` zurückgibt. Rufe sie mit `10` auf und speichere das Ergebnis in einer Variable namens `result`.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`result` muss gleich `13` sein.

```kotlin
    tryCatch(result == 13)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun addThree(n: Int) = n + 3
val result = addThree(10)
```
