---
language: kotlin
exerciseType: 4
---

# --description--

Die Bausteine dieses Themas werden üblicherweise kombiniert: Eine `enum class`, deren Einträge eigene Eigenschaften tragen, modelliert eine feste Menge von Tags, während eine `data class` die Nutzdaten trägt, die dazugehören.

```kotlin
enum class Speed(val surcharge: Int) {
    STANDARD(0),
    EXPRESS(15)
}

data class Order(val total: Int, val speed: Speed)

val order = Order(100, Speed.EXPRESS)
println(order.total + order.speed.surcharge) // 115
```

# --instructions--

Ordne die Elemente so, dass die Gesamtkosten der Bestellung ausgegeben werden. Deklariere zuerst das Enum, dann die data class, dann `main`.

# --answers--

```kotlin
enum class Shipping(val surcharge: Int) { STANDARD(0), EXPRESS(15) }
```

```kotlin
data class Order(val total: Int, val shipping: Shipping)
```

```kotlin
fun main() {
```

```kotlin
    val order = Order(100, Shipping.EXPRESS)
```

```kotlin
    println(order.total + order.shipping.surcharge)
```

```kotlin
}
```

# --solutions--

```kotlin
enum class Shipping(val surcharge: Int) { STANDARD(0), EXPRESS(15) }
data class Order(val total: Int, val shipping: Shipping)
fun main() {
    val order = Order(100, Shipping.EXPRESS)
    println(order.total + order.shipping.surcharge)
}
```

# --output--

115
