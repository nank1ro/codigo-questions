---
language: kotlin
exerciseType: 3
---

# --instructions--

Was passiert mit diesem Programm?

```kotlin
interface Movable {
    fun move(): String = "moving"
}

class Robot : Movable

fun main() {
    println(Robot().move())
}
```

# --answers--

- Es gibt `moving` aus, weil `Robot` die Standardimplementierung erbt
- Es kompiliert nicht: `Robot` muss `move()` überschreiben
- Es kompiliert nicht: Ein Interface darf keinen Funktionskörper enthalten
- Es gibt `null` aus

# --solutions--

- Es gibt `moving` aus, weil `Robot` die Standardimplementierung erbt
