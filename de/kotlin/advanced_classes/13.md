---
language: kotlin
exerciseType: 1
---

# --description--

Eine `abstract`-Klasse liegt zwischen einem Interface und einer normalen Klasse: Sie kann nicht instanziiert werden und mischt **abstrakte** Member, die keinen Körper haben und überschrieben werden müssen, mit konkreten, die Unterklassen unverändert erben.

```kotlin
abstract class Vehicle(val name: String) {
    abstract fun wheels(): Int
    fun describe(): String = "$name has ${wheels()} wheels"
}

class Bike(name: String) : Vehicle(name) {
    override fun wheels(): Int = 2
}

println(Bike("BMX").describe()) // BMX has 2 wheels
```

Anders als ein Interface hat eine abstrakte Klasse einen Konstruktor und kann Zustand in Eigenschaften speichern — deshalb übergibt die Unterklasse `name` mit `: Vehicle(name)` nach oben. Eine Klasse kann nur eine andere Klasse erweitern: Greife also zu einer abstrakten Klasse, wenn die Unterklassen Daten teilen, und zu einem Interface, wenn sie nur Verhalten teilen. Abstrakte Member sind überschreibbar, ohne `open` hinzuzufügen.

# --instructions--

Deklariere eine abstract class `Employee` mit einer `val`-Eigenschaft `name` vom Typ `String` im Konstruktor, einer abstrakten Methode `monthlyPay(): Int` und einer konkreten Methode `yearlyPay(): Int`, die den Monatslohn mal `12` zurückgibt.

Deklariere dann eine Klasse `Manager`, die `Employee` erweitert, `name` und eine `val`-Eigenschaft `bonus` vom Typ `Int` entgegennimmt und deren `monthlyPay()` `5000` plus den Bonus zurückgibt.

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`Manager("Ann", 500).monthlyPay()` muss `5500` zurückgeben

```kotlin
    tryCatch(Manager("Ann", 500).monthlyPay() == 5500)
```

`Manager("Ann", 500).yearlyPay()` muss `66000` zurückgeben

```kotlin
    tryCatch(Manager("Ann", 500).yearlyPay() == 66000)
```

`Manager("Bo", 0).name` muss `"Bo"` zurückgeben

```kotlin
    tryCatch(Manager("Bo", 0).name == "Bo")
```

Ein `Manager` muss als `Employee` verwendbar sein

```kotlin
    val employee: Employee = Manager("Cy", 1000)
    tryCatch(employee.yearlyPay() == 72000)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
abstract class Employee(val name: String) {
    abstract fun monthlyPay(): Int

    fun yearlyPay(): Int = monthlyPay() * 12
}

class Manager(name: String, val bonus: Int) : Employee(name) {
    override fun monthlyPay(): Int = 5000 + bonus
}
```
