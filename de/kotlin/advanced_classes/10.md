---
language: kotlin
exerciseType: 2
---

# --instructions--

Vervollständige den Code so, dass `ofKilometres` eine Fabrikfunktion von `Distance` ist, die aus Kilometern eine `Distance` erstellt

# --seed--

```kotlin
class Distance private constructor(val meters: Int) {
    [/] object {
        fun ofKilometres(km: Int) = [/](km * 1000)
    }
}

fun main() {
    println(Distance.ofKilometres(3).meters)
}
```

# --answers--

- companion
- Distance
- data
- inner
- Int
- meters

# --solutions--

```kotlin
class Distance private constructor(val meters: Int) {
    companion object {
        fun ofKilometres(km: Int) = Distance(km * 1000)
    }
}

fun main() {
    println(Distance.ofKilometres(3).meters)
}
```

# --output--

3000
