---
language: kotlin
exerciseType: 2
---

# --description--

Wir können so viele `else if`-Anweisungen hinzufügen, wie wir möchten, es gibt keine Grenzen.
```kotlin
var num = 4
if (num == 2) {
    println("the number is 2")
} else if (num == 3) {
    println("the number is 3")
} else if (num == 4) {
    println("the number is 4")
} else if (num == 5) {
    println("the number is 5")
} else if (num == 6) {
    println("the number is 6")
}
```
und die Ausgabe dieses Codes ist `the number is 4`.

# --instructions--

Fügen Sie eine neue `else if`-Anweisung hinzu, die prüft, ob `time` gleich `0` ist, und geben Sie im Codeblock die Zeichenkette `"It's midnight"` aus

# --seed--

```kotlin
var time = 0
if (time > 0 && time < 12) {
    println("It's morning")
} else if (time == 12) {
    println("It's midday")
} [/]([/][/][/]) {
    println("It's midnight")
} else {
    println("It's afternoon")
}
```

# --answers--

- 0
- else if 
-  < 
-  == 
- :
- ;
- time
- else if 
- elseif
- else

# --solutions--

```kotlin
var time = 0
if (time > 0 && time < 12) {
    println("It's morning")
} else if (time == 12) {
    println("It's midday")
} else if (time == 0) {
    println("It's midnight")
} else {
    println("It's afternoon")
}
```

# --output--

It's midnight
