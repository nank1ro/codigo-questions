---
language: kotlin
exerciseType: 2
---

# --description--

Um eine `Set` in eine `List` zu konvertieren, können wir die Funktion `toList()` verwenden

# --instructions--

Vervollständigen Sie den Code, um die `Set` in eine `List` zu konvertieren

# --seed--

```kotlin
val uniqueNumbers = setOf(1, 2, 3, 3)
var numbers = [/][/][/]
numbers += 4
println(numbers)
```

# --answers--

- uniqueNumbers
- .
- toList()
- toList
- distinct()
- asList()

# --solutions--

```kotlin
val uniqueNumbers = setOf(1, 2, 3, 3)
var numbers = uniqueNumbers.toList()
numbers += 4
println(numbers)
```

# --output--

[1, 2, 3, 4]
