---
language: kotlin
exerciseType: 2
---

# --description--

Eine veränderbare Liste muss keine feste Länge haben.
Sie können jederzeit Elemente am Ende einer Liste hinzufügen!
Um ein Element zu einer veränderbaren Liste hinzuzufügen, verwenden wir die Funktion `add` oder die Abkürzung `+=`:
```kotlin
val letters = mutableListOf("a", "b")
letters.add("c")
println(letters)
// prints [a, b, c]
```
# --instructions--

Vervollständigen Sie den Code, um drei weitere Elemente zur `numbers`-Liste hinzuzufügen, in der folgenden Reihenfolge `6`, dann `8` und schließlich `9`
# --seed--

```kotlin
val numbers = mutableListOf(1, 3, 9)
numbers.[/]([/])
numbers.add([/])
[/].add(9)
println(numbers)
```

# --answers--

- append
- add
- 6
- 8
- numbers

# --solutions--

```kotlin
val numbers = mutableListOf(1, 3, 9)
numbers.add(6)
numbers.add(8)
numbers.add(9)
println(numbers)
```

# --output--

[1, 3, 9, 6, 8, 9]
