---
language: kotlin
exerciseType: 2
---

# --description--

In Kotlin können wir auf sehr einfache Weise durch eine Liste schleifen, indem wir die Schlüsselwörter `for..in` verwenden:
```kotlin
val numbers = listOf(1, 2, 3)
for (num in numbers) {
    println(num)
}
// prints 1, 2, 3 
```
Ein Variablenname folgt dem Schlüsselwort `for`, dem der Wert jedes Listenelements nacheinander zugewiesen wird.
# --instructions--

Drucken Sie jedes Element in der Liste, indem Sie es mit `2` multiplizieren
# --seed--

```kotlin
val numbers = listOf(2, 4, 6)
[/]([/][/][/]) {
    println([/][/][/])
}
```

# --answers--

- for 
- num
-  in 
- numbers
- num
-  * 
-  ** 
- 2

# --solutions--

```kotlin
val numbers = listOf(2, 4, 6)
for (num in numbers) {
    println(num * 2)
}
```

# --output--

4
8
12
