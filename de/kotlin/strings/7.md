---
language: kotlin
exerciseType: 2
---

# --description--

`split` zerlegt eine Zeichenkette anhand eines Trennzeichens in eine `List` von Teilen, während `joinToString` das Gegenteil macht: Es fügt die Elemente einer Collection mit dem gewählten Trennzeichen zu einer einzigen Zeichenkette zusammen:
```kotlin
val csv = "a,b,c"
val parts = csv.split(",")
println(parts)                  // [a, b, c]
println(parts.joinToString("/")) // a/b/c
```

# --instructions--

Vervollständige den Code, um `csv` am Komma aufzuteilen und die Farben getrennt durch `" - "` auszugeben

# --seed--

```kotlin
val csv = "red,green,blue"
val colors = csv.[/](",")
println(colors.[/](" - "))
```

# --answers--

- split
- joinToString
- join
- separate
- substring

# --solutions--

```kotlin
val csv = "red,green,blue"
val colors = csv.split(",")
println(colors.joinToString(" - "))
```

# --output--

red - green - blue
