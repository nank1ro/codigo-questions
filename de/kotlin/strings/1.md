---
language: kotlin
exerciseType: 2
---

# --description--

Ein `String` ist eine Folge von Zeichen, die zwischen doppelten Anführungszeichen steht.
Die Eigenschaft `length` gibt an, wie viele Zeichen eine Zeichenkette enthält, Leerzeichen eingeschlossen:
```kotlin
val greeting = "Hello world"
println(greeting.length) // 11
println("".length)       // 0
```

# --instructions--

Vervollständige den Code, um die Anzahl der Zeichen in `word` auszugeben

# --seed--

```kotlin
val word = "Kotlin"
println(word.[/])
```

# --answers--

- length
- size
- len
- length()
- count

# --solutions--

```kotlin
val word = "Kotlin"
println(word.length)
```

# --output--

6
