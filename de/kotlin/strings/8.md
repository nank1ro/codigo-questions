---
language: kotlin
exerciseType: 4
---

# --description--

Da `split` eine `List` zurückgibt, kannst du wie bei jeder anderen Liste über ihre Elemente iterieren:
```kotlin
for (part in "a-b".split("-")) {
    println(part)
}
// prints a, then b
```

# --instructions--

Ordne diese Zeilen, um jedes Wort von `sentence` in Großbuchstaben, jeweils in einer eigenen Zeile, auszugeben

# --answers--

```kotlin
val sentence = "the quick brown fox"
```

```kotlin
val words = sentence.split(" ")
```

```kotlin
for (word in words) {
```

```kotlin
    println(word.uppercase())
```

```kotlin
}
```

# --solutions--

```kotlin
val sentence = "the quick brown fox"
val words = sentence.split(" ")
for (word in words) {
    println(word.uppercase())
}
```

# --output--

THE
QUICK
BROWN
FOX
