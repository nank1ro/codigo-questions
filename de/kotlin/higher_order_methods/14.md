---
language: kotlin
exerciseType: 2
---

# --description--

In einer Kette ändert sich der Typ von `it` bei jedem Schritt: Nach `filter` auf einer `List<String>` hast du weiterhin Strings, aber nach `map { it.length }` hast du eine `List<Int>`, also sieht das nächste Lambda Zahlen.
```kotlin
val words = listOf("kiwi", "fig")
println(words.map { it.length }.filter { it > 3 }) // [4]
```
Jeder Schritt gibt eine **neue** Liste zurück und rührt die vorherige nie an, also lässt sich eine Kette in benannte Zwischenwerte aufteilen, ohne das Ergebnis zu ändern.

# --instructions--

Vervollständige den Code so, dass `result` die Früchte enthält, die mit `a` beginnen, in Großbuchstaben, damit das Programm `[APPLE, AVOCADO]` ausgibt

# --seed--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.[/] { it.startsWith("a") }.[/] { it.[/] }
println(result)
```

# --answers--

- filter
- map
- uppercase()
- forEach
- count
- lowercase()
- length

# --solutions--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.filter { it.startsWith("a") }.map { it.uppercase() }
println(result)
```

# --output--

[APPLE, AVOCADO]
