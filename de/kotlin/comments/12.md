---
language: kotlin
exerciseType: 2
---

# --description--

Eine Funktion mit mehr als einem Parameter bekommt für jeden davon ein `@param`-Tag, geschrieben in derselben Reihenfolge wie die Parameter:
```kotlin
/**
 * Returns the area of a rectangle.
 * @param width the horizontal side, in centimetres
 * @param height the vertical side, in centimetres
 * @return the product of [width] and [height]
 */
fun area(width: Int, height: Int): Int {
    return width * height
}
```
Ein Tag ist trotzdem nur ein Kommentar: benennst du einen Parameter um und vergisst das Tag, geht nichts kaputt, aber die Dokumentation beginnt zu lügen. Aktualisiere das KDoc zusammen mit der Signatur.

# --instructions--

Vervollständige die Dokumentation von `price`: das erste Tag muss den Parameter `quantity` beschreiben, das zweite den Parameter `unitPrice` und das dritte den Wert, den die Funktion zurückgibt

# --seed--

```kotlin
/**
 * Returns the total price of an order.
 * [/] quantity how many items were ordered
 * [/] unitPrice the price of a single item
 * [/] the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --answers--

- @param
- @param
- @return
- @throws
- @property

# --solutions--

```kotlin
/**
 * Returns the total price of an order.
 * @param quantity how many items were ordered
 * @param unitPrice the price of a single item
 * @return the total price of the order
 */
fun price(quantity: Int, unitPrice: Int): Int {
    return quantity * unitPrice
}
println(price(3, 5))
```

# --output--

15
