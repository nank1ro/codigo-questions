---
language: javascript
exerciseType: 2
---

# --description--

`sort(compare)` sortiert ein Array an Ort und Stelle mit einem Callback, der zwei Elemente erhält und eine negative Zahl zurückgibt, wenn das erste zuerst kommen soll, eine positive Zahl, wenn das zweite zuerst kommen soll, oder `0`, wenn sie gleich sind. Bei Zahlen sortiert `(a, b) => a - b` aufsteigend und `(a, b) => b - a` absteigend.
`find(callback)` gibt das erste Element zurück, für das der Callback `true` zurückgibt, oder `undefined`, wenn es keines gibt:
```javascript
const scores = [50, 90, 70];
scores.sort((a, b) => a - b);
console.log(scores);
// prints [ 50, 70, 90 ]
console.log(scores.find((s) => s > 60));
// prints 70
```

# --instructions--

Sortiere `ages` aufsteigend, finde dann das erste Alter, das streng größer als `21` ist, und gib sowohl das sortierte Array als auch dieses Alter aus

# --seed--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a [/] b);
const adult = ages.find((age) => age [/] 21);
console.log(ages);
console.log(adult);
```

# --answers--

- -
- >
- +
- <
- ===
- *

# --solutions--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a - b);
const adult = ages.find((age) => age > 21);
console.log(ages);
console.log(adult);
```

# --output--

[ 18, 25, 31 ]
25
