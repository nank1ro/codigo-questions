---
language: javascript
exerciseType: 2
---

# --description--

Das Objektliteral in Klammern zu setzen ist der Standardweg, um Objekte mit einer einzeiligen Pfeilfunktion zu erzeugen, zum Beispiel wenn du ein paar Werte in einen Datensatz umwandelst:
```javascript
const user = (name, age) => ({ name: name, age: age });
console.log(user("Ana", 30).age);
// prints 30
```
Eine Pfeilfunktion ohne Parameter beginnt mit einem leeren Paar Klammern `()`:
```javascript
const empty = () => ({});
console.log(empty());
// prints {}
```

# --instructions--

Vervollständige `point`, sodass es ein Objekt mit den Eigenschaften `x` und `y` zurückgibt, und `origin`, sodass es keine Parameter nimmt, und gib dann `point(3, 4).y` und `origin().x` aus

# --seed--

```javascript
const point = (x, y) => [/]{ x: x, y: y }[/];
const origin = [/] => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --answers--

- (
- )
- ()
- [
- ]
- function
- this

# --solutions--

```javascript
const point = (x, y) => ({ x: x, y: y });
const origin = () => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --output--

4
0
