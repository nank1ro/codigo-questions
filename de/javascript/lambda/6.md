---
language: javascript
exerciseType: 4
---

# --description--

Zwei weitere Array-Methoden nehmen einen Callback entgegen.
`forEach(callback)` ruft den Callback einmal pro Element auf und gibt nichts zurück; benutze ihn für Seiteneffekte wie das Ausgeben.
`reduce(callback, initialValue)` faltet das Array zu einem einzelnen Wert zusammen: Der Callback erhält den bisher akkumulierten Wert und das aktuelle Element und gibt den neuen akkumulierten Wert zurück:
```javascript
const numbers = [1, 2, 3];
numbers.forEach((n) => console.log(n));
// prints 1, 2 and 3 on three lines
const total = numbers.reduce((sum, n) => sum + n, 0);
console.log(total);
// prints 6
```

# --instructions--

Sortiere die Zeilen, um mit `reduce` direkt nach der Deklaration des Arrays die Summe von `prices` zu berechnen, gib jeden Preis mit `forEach` in einer eigenen Zeile aus und gib dann die Summe aus

# --answers--

```javascript
const prices = [4, 6, 10];
```

```javascript
const total = prices.reduce((sum, p) => sum + p, 0);
```

```javascript
prices.forEach((p) => console.log(p));
```

```javascript
console.log(total);
```

# --solutions--

```javascript
const prices = [4, 6, 10];
const total = prices.reduce((sum, p) => sum + p, 0);
prices.forEach((p) => console.log(p));
console.log(total);
```

# --output--

4
6
10
20
