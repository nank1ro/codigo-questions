---
language: javascript
exerciseType: 1
---

# --description--

Pfeilfunktionen werden in zwei häufigen Fällen noch kürzer.
Wenn der Körper ein **einzelner Ausdruck** ist, kannst du die geschweiften Klammern und das Schlüsselwort `return` weglassen: Der Wert des Ausdrucks wird automatisch zurückgegeben (ein **implizites return**):
```javascript
const add = (a, b) => a + b;
console.log(add(2, 3));
// prints 5
```
Wenn es **genau einen Parameter** gibt, kannst du auch die Klammern darum weglassen:
```javascript
const double = n => n * 2;
console.log(double(4));
// prints 8
```
Bei null Parametern oder bei zwei oder mehr sind die Klammern erforderlich: `() => 42` und `(a, b) => a + b`.

# --instructions--

Speichere in der Konstante `square` eine Pfeilfunktion, die ihr Argument mit sich selbst multipliziert zurückgibt, mit einem impliziten return (keine geschweiften Klammern und kein `return`)

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const square = 
```

# --asserts--

`square(3)` muss `9` zurückgeben.

```javascript
tryCatch(square(3) === 9);
```

`square(-4)` muss `16` zurückgeben.

```javascript
tryCatch(square(-4) === 16);
```

`square(0)` muss `0` zurückgeben.

```javascript
tryCatch(square(0) === 0);
```

`square` muss eine Pfeilfunktion sein, keine `function`.

```javascript
tryCatch(typeof square === "function" && !/function/.test(square.toString()));
```

`square` muss ein implizites return verwenden.

```javascript
tryCatch(!/return/.test(square.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const square = n => n * n;
```
