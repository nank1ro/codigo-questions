---
language: javascript
exerciseType: 4
---

# --instructions--

Sortiere die Zeilen, um `makeCounter` zu definieren, das eine Pfeilfunktion zurückgibt, die ihr privates `count` erhöht und zurückgibt, erstelle danach den Zähler `next` und gib das Ergebnis von zwei Aufrufen in derselben Zeile aus

# --answers--

```javascript
const makeCounter = () => {
```

```javascript
  let count = 0;
```

```javascript
  return () => {
```

```javascript
    count += 1;
```

```javascript
    return count;
```

```javascript
  };
```

```javascript
};
```

```javascript
const next = makeCounter();
```

```javascript
console.log(next(), next());
```

# --solutions--

```javascript
const makeCounter = () => {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
};
const next = makeCounter();
console.log(next(), next());
```

# --output--

1 2
