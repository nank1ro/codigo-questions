---
language: javascript
exerciseType: 2
---

# --description--

`delete(key)` gibt `true` zurück, wenn ein Paar entfernt wurde, und `false`, wenn der Schlüssel nicht vorhanden war.
Um **alle** Paare auf einmal zu entfernen, rufe `clear()` auf:
```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

Entferne `"pen"` aus `cart` und gib das Ergebnis der Entfernung aus, leere dann die ganze Map und gib aus, wie viele Paare übrig sind

# --seed--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
