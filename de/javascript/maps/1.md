---
language: javascript
exerciseType: 2
---

# --description--

Eine **Map** speichert **Schlüssel-Wert-Paare**: Jeder Wert wird unter einem Schlüssel gespeichert, und du verwendest diesen Schlüssel, um den Wert wiederzufinden.
Du erstellst eine leere Map mit `new Map()`, fügst ein Paar mit `set(key, value)` hinzu und liest einen Wert mit `get(key)`:
```javascript
let ages = new Map();
ages.set("Ann", 30);
ages.set("Bob", 25);
console.log(ages.get("Ann"));
// prints 30
```
Ruft man `set()` mit einem bereits vorhandenen Schlüssel auf, wird dessen Wert ersetzt.

# --instructions--

Erstelle eine leere Map `ages`, speichere `30` unter dem Schlüssel `"Ann"` und gib dann den unter `"Ann"` gespeicherten Wert aus

# --seed--

```javascript
let ages = new [/]();
ages.[/]("Ann", 30);
console.log(ages.[/]("Ann"));
```

# --answers--

- Map
- set
- get
- Object
- add
- put
- read

# --solutions--

```javascript
let ages = new Map();
ages.set("Ann", 30);
console.log(ages.get("Ann"));
```

# --output--

30
