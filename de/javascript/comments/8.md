---
language: javascript
exerciseType: 4
---

# --description--

Um mehrere Zeilen auf einmal auszukommentieren, umschließe sie mit einem einzigen Blockkommentar, statt jeder Zeile ein `//` voranzustellen:
```javascript
let total = 100;
/*
total = total - 30;
total = total - 20;
*/
console.log(total); // prints 100
```
Da die Zeilen im Block ignoriert werden, ändert sich `total` nie. Denk daran, dass das nur funktioniert, wenn keine dieser Zeilen ein `*/` enthält.

# --instructions--

Sortiere die Elemente so, dass `total` zuerst deklariert wird, die zwei Zeilen, die es ändern, in einem einzigen Blockkommentar stehen und der Wert zuletzt ausgegeben wird, was die Ausgabe `100` ergibt

# --answers--

```javascript
let total = 100;
```

```javascript
/*
```

```javascript
total = total - 30;
```

```javascript
total = total - 20;
```

```javascript
*/
```

```javascript
console.log(total);
```

# --solutions--

```javascript
let total = 100;
/*
total = total - 30;
total = total - 20;
*/
console.log(total);
```

```javascript
let total = 100;
/*
total = total - 20;
total = total - 30;
*/
console.log(total);
```

# --output--

100
