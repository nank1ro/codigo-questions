---
language: javascript
exerciseType: 1
---

# --description--

Ein Kommentar braucht keine eigene Zeile: Er kann dem Code in derselben Zeile folgen. Das ist ein **Inline-Kommentar** (oder nachgestellter Kommentar), und er ist ein guter Platz für eine kurze Notiz zu genau dieser Anweisung:
```javascript
const retries = 3; // give up after three attempts
```
Alles von `//` bis zum Zeilenende wird ignoriert, während der Code davor wie gewohnt ausgeführt wird.

# --instructions--

Füge nach `let seats = 4;` einen Inline-Kommentar hinzu, der den Wert erklärt, zum Beispiel `// seats per table`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
let seats = 4;
```

# --asserts--

The line `let seats = 4;` must end with a `//` comment.

```json
{
  "regex": "let seats = 4;[ \\t]*//[ \\t]*\\S",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`seats` must still be equal to `4`.

```javascript
tryCatch(seats === 4);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
let seats = 4; // seats per table
```
