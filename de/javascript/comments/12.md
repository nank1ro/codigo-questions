---
language: javascript
exerciseType: 4
---

# --description--

Die Reihenfolge in einem JSDoc-Kommentar ist immer dieselbe: zuerst die Zusammenfassung, dann ein `@param` pro Parameter in der Reihenfolge ihrer Deklaration, dann zuletzt `@returns`. Das öffnende `/**` und das schließende ` */` umschließen alles, und der Kommentar steht direkt über der Funktion, die er beschreibt:
```javascript
/**
 * Returns the number of seconds in the given minutes.
 * @param {number} minutes a whole number of minutes
 * @returns {number} minutes multiplied by sixty
 */
function toSeconds(minutes) {
  return minutes * 60;
}
```

# --instructions--

Sortiere die Elemente so, dass `twice` mit einer Zusammenfassung, einer `@param`-Zeile und einer `@returns`-Zeile in dieser Reihenfolge und direkt über der Funktion dokumentiert wird, und gib dann `twice(21)` aus, nachdem die Funktion definiert wurde

# --answers--

```javascript
/**
```

```javascript
 * Returns the double of a number.
```

```javascript
 * @param {number} n the number to double
```

```javascript
 * @returns {number} n multiplied by two
```

```javascript
 */
```

```javascript
function twice(n) {
  return n * 2;
}
```

```javascript
console.log(twice(21));
```

# --solutions--

```javascript
/**
 * Returns the double of a number.
 * @param {number} n the number to double
 * @returns {number} n multiplied by two
 */
function twice(n) {
  return n * 2;
}
console.log(twice(21));
```

# --output--

42
