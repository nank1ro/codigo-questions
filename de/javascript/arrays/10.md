---
language: javascript
exerciseType: 2
---

# --description--

In JavaScript können wir auf sehr einfache Weise mit den `for..of`-Schlüsselwörtern durch ein Array schleifen:
```javascript
var numbers = [1, 2, 3];
for (num of numbers) {
    console.log(num);
}
// druckt 1, 2, 3
```
Dem `for`-Schlüsselwort folgt ein Variablenname, dem der Wert jedes Array-Elements nacheinander zugewiesen wird.

# --instructions--

Gebe jedes Element im Array aus, indem du es mit `2` multiplizierst

# --seed--

```javascript
var numbers = [2, 4, 6];
[/]([/][/][/]) {
    console.log([/][/][/]);
}
```

# --answers--

- for 
- num 
- in 
- numbers
- num 
- * 
- ** 
- of 
- 2

# --solutions--

```javascript
var numbers = [2, 4, 6];
for (num of numbers) {
    console.log(num * 2);
}
```

# --output--

4
8
12
