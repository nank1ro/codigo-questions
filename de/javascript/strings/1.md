---
language: javascript
exerciseType: 2
---

# --description--

Ein **String** ist eine Folge von Zeichen in Anführungszeichen, wie `"hello"` oder `'hello'`.
Jeder String hat eine `length`-Eigenschaft, die angibt, wie viele Zeichen er enthält:
```javascript
let greeting = "hello";
console.log(greeting.length);
// prints 5
```
Leerzeichen und Satzzeichen zählen ebenfalls als Zeichen.

# --instructions--

Gib die Anzahl der Zeichen in `word` aus

# --seed--

```javascript
let word = "JavaScript";
console.log(word.[/]);
```

# --answers--

- length
- len
- size
- count

# --solutions--

```javascript
let word = "JavaScript";
console.log(word.length);
```

# --output--

10
