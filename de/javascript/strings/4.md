---
language: javascript
exerciseType: 1
---

# --description--

Um zu prüfen, ob ein String einen anderen String enthält, verwende diese Methoden, die alle einen Boolean zurückgeben:
- `includes(text)` ist `true`, wenn `text` irgendwo vorkommt
- `startsWith(text)` ist `true`, wenn der String mit `text` beginnt
- `endsWith(text)` ist `true`, wenn der String mit `text` endet

```javascript
let file = "photo.png";
console.log(file.includes("."));
// prints true
console.log(file.startsWith("ph"));
// prints true
console.log(file.endsWith(".jpg"));
// prints false
```
Der Vergleich unterscheidet Groß- und Kleinschreibung: `"Hello".includes("h")` ist `false`.

# --instructions--

Schreibe eine Funktion `isImage(fileName)`, die `true` zurückgibt, wenn `fileName` mit `".png"` endet, und andernfalls `false`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isImage(fileName) {

}
```

# --asserts--

`isImage("cat.png")` muss `true` zurückgeben.

```javascript
tryCatch(isImage("cat.png") === true);
```

`isImage("cat.jpg")` muss `false` zurückgeben.

```javascript
tryCatch(isImage("cat.jpg") === false);
```

`isImage("png.txt")` muss `false` zurückgeben.

```javascript
tryCatch(isImage("png.txt") === false);
```

`isImage("a.png.txt")` muss `false` zurückgeben.

```javascript
tryCatch(isImage("a.png.txt") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isImage(fileName) {
  return fileName.endsWith(".png");
}
```
