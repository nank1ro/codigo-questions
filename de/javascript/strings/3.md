---
language: javascript
exerciseType: 2
---

# --description--

Strings bringen viele eingebaute **Methoden** mit. Zwei der einfachsten ändern die Groß-/Kleinschreibung jedes Buchstabens:
```javascript
let word = "Hello";
console.log(word.toUpperCase());
// prints HELLO
console.log(word.toLowerCase());
// prints hello
```
Beide Methoden nehmen keine Argumente entgegen, also vergiss die Klammern nicht.

# --instructions--

Gib `name` in Großbuchstaben aus, dann in Kleinbuchstaben

# --seed--

```javascript
let name = "Ada";
console.log(name.[/]());
console.log(name.[/]());
```

# --answers--

- toUpperCase
- toLowerCase
- upper
- lower
- capitalize

# --solutions--

```javascript
let name = "Ada";
console.log(name.toUpperCase());
console.log(name.toLowerCase());
```

# --output--

ADA
ada
