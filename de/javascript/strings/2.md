---
language: javascript
exerciseType: 1
---

# --description--

Jedes Zeichen in einem String hat einen **Index**, beginnend bei `0`.
Du kannst ein einzelnes Zeichen mit eckigen Klammern oder mit der Methode `charAt()` lesen:
```javascript
let word = "hello";
console.log(word[0]);
// prints h
console.log(word.charAt(1));
// prints e
```
Das letzte Zeichen befindet sich am Index `length - 1`:
```javascript
console.log(word[word.length - 1]);
// prints o
```

# --instructions--

Speichere das erste Zeichen von `word` in einer Variable `first` und das letzte Zeichen in einer Variable `last`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
let word = "coding";
```

# --asserts--

`first` muss gleich `"c"` sein.

```javascript
tryCatch(first === "c");
```

`last` muss gleich `"g"` sein.

```javascript
tryCatch(last === "g");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
let word = "coding";
let first = word[0];
let last = word[word.length - 1];
```
