---
language: javascript
exerciseType: 1
---

# --description--

Die Methode `replace(search, replacement)` gibt einen neuen String zurück, bei dem das **erste** Vorkommen von `search` durch `replacement` ersetzt wird:
```javascript
let text = "red red";
console.log(text.replace("red", "blue"));
// prints blue red
```
Um **jedes** Vorkommen zu ersetzen, verwende `replaceAll()`:
```javascript
console.log(text.replaceAll("red", "blue"));
// prints blue blue
```

# --instructions--

Schreibe eine Funktion `censor(text)`, die `text` zurückgibt, wobei jedes Vorkommen von `"bad"` durch `"***"` ersetzt wird

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function censor(text) {

}
```

# --asserts--

`censor("bad day, bad luck")` muss `"*** day, *** luck"` zurückgeben.

```javascript
tryCatch(censor("bad day, bad luck") === "*** day, *** luck");
```

`censor("good day")` muss `"good day"` zurückgeben.

```javascript
tryCatch(censor("good day") === "good day");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function censor(text) {
  return text.replaceAll("bad", "***");
}
```
