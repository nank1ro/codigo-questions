---
language: javascript
exerciseType: 1
---

# --description--

Die Methode `padStart(targetLength, padString)` fügt `padString` am **Anfang** des Strings hinzu, bis er `targetLength` Zeichen erreicht. `padEnd()` macht dasselbe am Ende:
```javascript
console.log("7".padStart(3, "0"));
// prints 007
console.log("Tea".padEnd(6, "."));
// prints Tea...
```
Wenn der String bereits lang genug ist, wird er unverändert zurückgegeben.
Zahlen haben keine String-Methoden, also wandle sie zuerst mit `String(number)` um.

# --instructions--

Schreibe eine Funktion `twoDigits(number)`, die `number` als String mit mindestens zwei Zeichen zurückgibt, links mit `"0"` aufgefüllt

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function twoDigits(number) {

}
```

# --asserts--

`twoDigits(5)` muss `"05"` zurückgeben.

```javascript
tryCatch(twoDigits(5) === "05");
```

`twoDigits(12)` muss `"12"` zurückgeben.

```javascript
tryCatch(twoDigits(12) === "12");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function twoDigits(number) {
  return String(number).padStart(2, "0");
}
```
