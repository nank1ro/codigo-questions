---
language: javascript
exerciseType: 4
---

# --instructions--

Sortiere die Zeilen, um die Dateiendung von `file` in Großbuchstaben auszugeben

# --answers--

```javascript
let file = "report.pdf";
```

```javascript
let extension = file.slice(-3);
```

```javascript
console.log(extension.toUpperCase());
```

# --solutions--

```javascript
let file = "report.pdf";
let extension = file.slice(-3);
console.log(extension.toUpperCase());
```

# --output--

PDF
