---
language: javascript
exerciseType: 2
---

# --description--

Die Methode `split(separator)` zerlegt einen String in ein **Array** von Teilen und trennt dabei bei jedem `separator`:
```javascript
let sentence = "I like JavaScript";
let words = sentence.split(" ");
console.log(words);
// gibt [ 'I', 'like', 'JavaScript' ] aus
```
Das Gegenteil ist die Array-Methode `join(separator)`, die die Teile wieder zu einem String zusammenfügt:
```javascript
console.log(words.join("-"));
// gibt I-like-JavaScript aus
```

# --instructions--

Teile `csv` bei jedem Komma auf und gib aus, wie viele Farben es enthält

# --seed--

```javascript
let csv = "red,green,blue";
let colors = csv.[/]([/]);
console.log(colors.[/]);
```

# --answers--

- split
- ","
- " "
- join
- length
- size

# --solutions--

```javascript
let csv = "red,green,blue";
let colors = csv.split(",");
console.log(colors.length);
```

# --output--

3
