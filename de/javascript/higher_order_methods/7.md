---
language: javascript
exerciseType: 4
---

# --description--

Die Vergleichsfunktion kann jeden Teil der Elemente betrachten, deshalb wird ein Array von Objekten nach einer ihrer Eigenschaften sortiert, indem einfach diese Eigenschaft verglichen wird:
```javascript
const items = [{ name: "b", size: 3 }, { name: "a", size: 1 }];
const bySize = [...items].sort((x, y) => x.size - y.size);
console.log(bySize.map((item) => item.name));
// prints [ 'a', 'b' ]
```
Das Sortieren der Kopie lässt `items` in seiner ursprünglichen Reihenfolge.

# --instructions--

Sortiere die Zeilen, um eine Kopie von `players` nach aufsteigendem `score` zu sortieren, ihre Namen in dieser Reihenfolge mit `map` auszugeben und dann den Namen des ersten Elements von `players` auszugeben, um zu zeigen, dass es unverändert blieb. Deklariere `ranked` vor `names`

# --answers--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
```

```javascript
const ranked = [...players].sort((a, b) => a.score - b.score);
```

```javascript
const names = ranked.map((p) => p.name);
```

```javascript
console.log(names);
```

```javascript
console.log(players[0].name);
```

# --solutions--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
const ranked = [...players].sort((a, b) => a.score - b.score);
const names = ranked.map((p) => p.name);
console.log(names);
console.log(players[0].name);
```

# --output--

[ 'Leo', 'Zoe', 'Mia' ]
Zoe
