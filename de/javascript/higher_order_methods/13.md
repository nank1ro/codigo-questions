---
language: javascript
exerciseType: 1
---

# --description--

Jede Funktion hat eine Methode `bind`, die eine **neue** Funktion mit vorab festgelegten Dingen zurückgibt. Ihr erstes Argument wird zum `this` der neuen Funktion; die übrigen Argumente werden vor die gestellt, mit denen die neue Funktion aufgerufen wird (eine **partielle Anwendung**):
```javascript
function multiply(a, b) {
  return a * b;
}
const double = multiply.bind(null, 2);
console.log(double(21));
// prints 42
```
`this` festzulegen ist bei Methoden wichtig. Wird eine Methode aus ihrem Objekt herauskopiert und allein aufgerufen, verweist `this` nicht mehr auf das Objekt, sodass `this.name` zu `undefined` wird. `bind` bindet es an das Objekt:
```javascript
const user = {
  name: "Ana",
  hello() { return `Hi ${this.name}`; },
};
const loose = user.hello;
console.log(loose());
// prints Hi undefined
const bound = user.hello.bind(user);
console.log(bound());
// prints Hi Ana
```
Die ursprüngliche Funktion wird nie verändert: `bind` baut immer eine neue, deren `name` der ursprüngliche Name mit vorangestelltem `bound ` ist.

# --instructions--

Die Funktion `power` und das Objekt `counter` sind gegeben. Speichere in der Konstante `square` eine Version von `power`, gebunden mit `this` auf `null` und `exponent` fest auf `2`, und in der Konstante `tick` eine an `counter` gebundene Version von `counter.increment`, sodass sie auch allein aufgerufen weiter funktioniert

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = 
const tick = 
```

# --asserts--

`square(3)` muss `9` zurückgeben.

```javascript
tryCatch(square(3) === 9);
```

`square(12)` muss `144` zurückgeben.

```javascript
tryCatch(square(12) === 144);
```

`square` muss mit `bind` erzeugt werden, sodass sein Name `"bound power"` ist.

```javascript
tryCatch(square.name === "bound power");
```

`tick` muss eine neue, mit `bind` erzeugte Funktion sein, nicht `counter.increment` selbst.

```javascript
tryCatch(tick !== counter.increment && tick.name === "bound increment");
```

`tick()` zweimal allein aufzurufen muss `1` und dann `2` zurückgeben, und `counter.count` muss `2` sein.

```javascript
tryCatch(tick() === 1 && tick() === 2 && counter.count === 2);
```

`tick` muss auch dann funktionieren, wenn es in einer anderen Variablen gespeichert und von dort aufgerufen wird.

```javascript
tryCatch((() => { const alone = tick; return alone() === 3; })());
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = power.bind(null, 2);
const tick = counter.increment.bind(counter);
```
