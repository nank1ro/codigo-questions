---
language: javascript
exerciseType: 1
---

# --description--

Funktionen höherer Ordnung trennen *was mit jedem Element geschehen soll* von *wie die Elemente durchlaufen werden*. Der durchlaufende Teil wird einmal geschrieben, und der Callback entscheidet den Rest:
```javascript
function each(items, action) {
  for (let i = 0; i < items.length; i++) {
    action(items[i]);
  }
}
each(["a", "b"], (letter) => console.log(letter));
// prints a and b on two lines
```
Der Callback erhält ein Element nach dem anderen. Er kann eine inline geschriebene Arrow Function sein, wie oben, oder jede in einer Variablen gespeicherte Funktion. Genau so arbeiten intern die eingebauten Array-Methoden, die du als Nächstes kennenlernst.

# --instructions--

Schreibe eine Funktion `transform(items, fn)`, die ein **neues** Array mit dem Ergebnis von `fn(item)` für jedes Element von `items` in derselben Reihenfolge zurückgibt, mit einer Schleife (benutze nicht `map`)

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function transform(items, fn) {

}
```

# --asserts--

`transform([1, 2, 3], (n) => n * 2)` muss `[2, 4, 6]` zurückgeben.

```javascript
tryCatch(JSON.stringify(transform([1, 2, 3], (n) => n * 2)) === "[2,4,6]");
```

`transform(["a", "b"], (s) => s.toUpperCase())` muss `["A", "B"]` zurückgeben.

```javascript
tryCatch(JSON.stringify(transform(["a", "b"], (s) => s.toUpperCase())) === '["A","B"]');
```

`transform([], (n) => n)` muss ein leeres Array zurückgeben.

```javascript
tryCatch(JSON.stringify(transform([], (n) => n)) === "[]");
```

Das Ergebnis muss ein neues Array sein und das Original darf nicht verändert werden.

```javascript
tryCatch((() => { const input = [1, 2]; const out = transform(input, (n) => n + 1); return out !== input && JSON.stringify(input) === "[1,2]"; })());
```

Die Funktion darf `map` nicht benutzen.

```javascript
tryCatch(!/\.map\(/.test(transform.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function transform(items, fn) {
  const result = [];
  for (const item of items) {
    result.push(fn(item));
  }
  return result;
}
```
