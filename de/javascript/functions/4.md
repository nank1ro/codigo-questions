---
language: javascript
exerciseType: 2
---

# --description--

Funktionen können mehrere Eingabeparameter haben, die in den Klammern der Funktion geschrieben werden und durch Kommas getrennt sind.
```javascript
function sayHello(name, newUser) {
  var greet = `Hello ${name}!`;
  if (newUser) {
    greet += " Welcome on board :)";
  }
  return greet;
}
// prints "Hello Smith! Welcome on board :)"
console.log(sayHello("Smith", true));
```

# --instructions--

Vervollständige den Code, um eine gültige Funktion namens `sumAllNumbers` zu erstellen, die folgende Zahlengruppe `[15, 24, 31, 79]` und `true` übergibt, um das Ergebnis zu drucken

# --seed--

```javascript
const reducer = (accumulator, currentValue) => accumulator + currentValue;

function sumAllNumbers(nums, [/]) {
    let result = nums.reduce(reducer);
    if (printResult) { [/](result); }
    return result;
}

let result = [/]([15, 24, 31, [/]], [/]);
```

# --answers--

- sumAllNumbers
- sum
- 79
- ]
- [
- true
- false
- True
- printResult
- console.log
- true

# --solutions--

```javascript
const reducer = (accumulator, currentValue) => accumulator + currentValue;

function sumAllNumbers(nums, printResult) {
    let result = nums.reduce(reducer);
    if (printResult) { console.log(result); }
    return result;
}

let result = sumAllNumbers([15, 24, 31, 79], true);
```

# --output--

149
