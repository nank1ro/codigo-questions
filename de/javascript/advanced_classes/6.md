---
language: javascript
exerciseType: 4
---

# --description--

Die Reihenfolge spielt eine Rolle, wenn Klassen voneinander erben. Eine `class`-Deklaration wird **nicht** so gehoistet wie eine `function`: Der Name existiert erst ab der Zeile, in der die Klasse geschrieben wird. Eine Kindklasse muss daher *nach* der Elternklasse erscheinen, die sie erweitert, sonst schlägt die `extends`-Klausel mit einem `ReferenceError` fehl.

# --instructions--

Sortiere die Elemente zu einem funktionierenden Programm. Denke daran, dass die Elternklasse vor der Kindklasse definiert sein muss

# --answers--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
```

```javascript
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
```

```javascript
console.log(new Car().describe());
```

# --solutions--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
console.log(new Car().describe());
```

# --output--

a vehicle with four wheels
