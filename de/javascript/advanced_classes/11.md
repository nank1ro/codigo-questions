---
language: javascript
exerciseType: 1
---

# --description--

Der eigentliche Wert eines Setters liegt darin, dass er ablehnen kann. Zwischen der Zuweisung und dem gespeicherten Wert hast du die Gelegenheit zu prüfen, zu begrenzen oder abzulehnen, was ankommt:
```javascript
class Volume {
    constructor(level) {
        this._level = level;
    }
    get level() {
        return this._level;
    }
    set level(value) {
        if (value <= 10) {
            this._level = value;
        }
    }
}
const v = new Volume(3);
v.level = 50;
console.log(v.level);
// gibt 3 aus, der Setter hat 50 abgelehnt
```
Ein Getter und ein Setter mit demselben Namen bilden zusammen eine Eigenschaft, daher können sie nicht zusätzlich ein normales Feld sein: Der gespeicherte Wert lebt unter einem anderen Namen, üblicherweise derselbe Name mit einem vorangestellten Unterstrich.

# --instructions--

Schreibe eine Klasse `Thermostat`, deren Konstruktor eine Temperatur entgegennimmt und sie in `_temperature` speichert, mit einem Getter `temperature`, der sie zurückgibt, und einem Setter `temperature`, der jeden Wert unter `0` ignoriert

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Thermostat {
  
}
```

# --asserts--

Der Getter muss den Wert zurückgeben, der dem Konstruktor übergeben wurde.

```javascript
tryCatch(new Thermostat(20).temperature === 20);
```

Der Setter muss einen gültigen Wert akzeptieren.

```javascript
const t1 = new Thermostat(20);
t1.temperature = 25;
tryCatch(t1.temperature === 25);
```

Der Setter muss einen negativen Wert ignorieren.

```javascript
const t2 = new Thermostat(20);
t2.temperature = -5;
tryCatch(t2.temperature === 20);
```

Der Setter muss `0` akzeptieren.

```javascript
const t3 = new Thermostat(20);
t3.temperature = 0;
tryCatch(t3.temperature === 0);
```

Der Wert muss in `_temperature` gespeichert werden.

```javascript
tryCatch(new Thermostat(20)._temperature === 20);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Thermostat {
  constructor(temperature) {
    this._temperature = temperature;
  }
  get temperature() {
    return this._temperature;
  }
  set temperature(value) {
    if (value >= 0) {
      this._temperature = value;
    }
  }
}
```
