---
language: javascript
exerciseType: 1
---

# --description--

Der Operator **`instanceof`** fragt, ob ein Objekt aus einer Klasse erzeugt wurde, oder aus einer beliebigen Klasse, die von ihr erbt:
```javascript
class Animal {}
class Dog extends Animal {}
const max = new Dog();
console.log(max instanceof Dog);    // true
console.log(max instanceof Animal); // true, Dog extends Animal
```
JavaScript hat kein `abstract`-Schlüsselwort, aber dieselbe Idee schreibt man von Hand: Eine Basisklasse definiert die Form, und jede Methode, die eine Kindklasse *bereitstellen muss*, wirft einfach einen Fehler:
```javascript
class Shape {
    area() {
        throw new Error("area() must be implemented");
    }
}
```
Eine Kindklasse, die vergisst, `area` zu überschreiben, scheitert laut beim ersten Einsatz, statt stillschweigend `undefined` zurückzugeben.

# --instructions--

Schreibe eine Klasse `Shape`, deren Methode `area` einen `Error` wirft, und eine Klasse `Square`, die sie erweitert, eine `side` entgegennimmt und `area` überschreibt, um die Fläche des Quadrats zurückzugeben

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Shape {
  
}

class Square {
  
}
```

# --asserts--

`new Square(3).area()` muss `9` zurückgeben.

```javascript
tryCatch(new Square(3).area() === 9);
```

`new Square(5).area()` muss `25` zurückgeben.

```javascript
tryCatch(new Square(5).area() === 25);
```

Ein Quadrat muss ein `Shape` sein.

```javascript
tryCatch(new Square(3) instanceof Shape === true);
```

Der Aufruf von `area` auf der Basisklasse muss einen Fehler werfen.

```javascript
let _threw = false;
try {
  new Shape().area();
} catch (e) {
  _threw = true;
}
tryCatch(_threw === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Shape {
  area() {
    throw new Error("area() must be implemented");
  }
}

class Square extends Shape {
  constructor(side) {
    super();
    this.side = side;
  }
  area() {
    return this.side * this.side;
  }
}
```
