---
language: javascript
exerciseType: 1
---

# --description--

Wenn eine Kindklasse eine Methode definiert, die die Elternklasse bereits hat, gewinnt die Version der Kindklasse. Das nennt sich **Überschreiben**:
```javascript
class Animal {
    speak() {
        return "some sound";
    }
}
class Dog extends Animal {
    speak() {
        return "Woof";
    }
}
console.log(new Dog().speak());
// prints Woof
```
Beim Überschreiben wird die Version der Elternklasse nicht gelöscht, sie wird nur verdeckt. Innerhalb der Methode der Kindklasse erreichst du sie weiterhin über `super.methodName(...)`, sodass du das Verhalten der Elternklasse erweitern kannst, statt es zu ersetzen:
```javascript
class Puppy extends Dog {
    speak() {
        return super.speak() + "!";
    }
}
console.log(new Puppy().speak());
// prints Woof!
```
Beachte den Unterschied: `super(...)` ruft den **Konstruktor** der Elternklasse auf, `super.name(...)` ruft eine **Methode** der Elternklasse auf.

# --instructions--

Schreibe eine Klasse `LoudGreeter`, die `Greeter` erweitert und `greet` überschreibt, sodass sie die Begrüßung der Elternklasse in Großbuchstaben gefolgt von einem Ausrufezeichen zurückgibt

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Greeter {
  constructor(name) {
    this.name = name;
  }
  greet() {
    return `Hello ${this.name}`;
  }
}

class LoudGreeter {
  
}
```

# --asserts--

`new LoudGreeter("Ann").greet()` muss `"HELLO ANN!"` zurückgeben.

```javascript
tryCatch(new LoudGreeter("Ann").greet() === "HELLO ANN!");
```

`new LoudGreeter("bob").greet()` muss `"HELLO BOB!"` zurückgeben.

```javascript
tryCatch(new LoudGreeter("bob").greet() === "HELLO BOB!");
```

Die Elternklasse muss unverändert bleiben.

```javascript
tryCatch(new Greeter("Ann").greet() === "Hello Ann");
```

`LoudGreeter` muss die Methode der Elternklasse über `super` wiederverwenden.

```json
{
  "regex": "super\\.greet\\s*\\(\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Greeter {
  constructor(name) {
    this.name = name;
  }
  greet() {
    return `Hello ${this.name}`;
  }
}

class LoudGreeter extends Greeter {
  greet() {
    return super.greet().toUpperCase() + "!";
  }
}
```
