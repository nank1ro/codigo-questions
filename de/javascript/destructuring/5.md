---
language: javascript
exerciseType: 1
---

# --description--

Ein Muster kann auch links von einer einfachen Zuweisung stehen, ohne `const` oder `let` davor, und schreibt dann in Variablen, die bereits existieren. Damit wird das Vertauschen zweier Werte zu einer einzigen Zeile, ganz ohne temporäre Variable:
```javascript
let a = 1;
let b = 2;
[a, b] = [b, a];
console.log(a, b);
// gibt 2 1 aus
```
Die rechte Seite wird zuerst aufgebaut, daher sind beide alten Werte beim Zuweisen bereits sicher im temporären Array gespeichert. Achte auf das Semikolon in der Zeile davor: Eine Zeile, die mit `[` beginnt, würde sonst als Index des davor Stehenden gelesen.

# --instructions--

Schreibe eine Funktion `sortPair`, die zwei Werte entgegennimmt und ein zweielementiges Array zurückgibt, in dem der kleinere zuerst steht, und sie dabei per Destrukturierungszuweisung vertauscht, wenn sie in der falschen Reihenfolge sind

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function sortPair(a, b) {
  
}
```

# --asserts--

`sortPair(5, 2)` muss mit `2` beginnen.

```javascript
tryCatch(sortPair(5, 2)[0] === 2);
```

`sortPair(5, 2)` muss mit `5` enden.

```javascript
tryCatch(sortPair(5, 2)[1] === 5);
```

Ein bereits sortiertes Paar muss unverändert bleiben.

```javascript
tryCatch(sortPair(1, 9).join(",") === "1,9");
```

Zwei gleiche Werte müssen unverändert bleiben.

```javascript
tryCatch(sortPair(4, 4).join(",") === "4,4");
```

Negative Werte müssen ebenfalls funktionieren.

```javascript
tryCatch(sortPair(3, -8).join(",") === "-8,3");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function sortPair(a, b) {
  if (a > b) {
    [a, b] = [b, a];
  }
  return [a, b];
}
```
