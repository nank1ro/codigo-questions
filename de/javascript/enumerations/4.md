---
language: javascript
exerciseType: 2
---

# --description--

Mitglieder können auch **Zahlen** enthalten. Numerische Werte sind praktisch, wenn die Mitglieder eine natürliche Reihenfolge haben, weil du sie mit den üblichen Operatoren vergleichen kannst:
```javascript
const Priority = Object.freeze({
  LOW: 1,
  MEDIUM: 2,
  HIGH: 3,
});
console.log(Priority.HIGH > Priority.LOW);
// prints true
```
Der Kompromiss ist die Lesbarkeit: Wenn du `Priority.HIGH` ausgibst, siehst du `3`, was dir viel weniger sagt als der String `"high"`.

# --instructions--

Deklariere die eingefrorene numerische Enumeration `Level` mit `EASY` gleich `1`, `NORMAL` gleich `2` und `HARD` gleich `3`, und gib dann aus, ob `HARD` größer als `EASY` ist, sowie die Differenz zwischen `HARD` und `EASY`

# --seed--

```javascript
const Level = Object.freeze({
  EASY: [/],
  NORMAL: 2,
  HARD: [/],
});
console.log(Level.HARD [/] Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --answers--

- 1
- 3
- >
- 0
- <
- ===

# --solutions--

```javascript
const Level = Object.freeze({
  EASY: 1,
  NORMAL: 2,
  HARD: 3,
});
console.log(Level.HARD > Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --output--

true
2
