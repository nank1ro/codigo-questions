---
language: javascript
exerciseType: 1
---

# --description--

Enumerationen passen natürlich zur `switch`-Anweisung, die einen Wert mit einer Liste von `case`-Marken vergleicht und den Code der ersten passenden ausführt.
Jeder Zweig endet mit `return` oder `break`, und der optionale `default`-Zweig läuft, wenn nichts passt:
```javascript
const Light = Object.freeze({ RED: "red", GREEN: "green" });

function action(light) {
  switch (light) {
    case Light.RED:
      return "stop";
    case Light.GREEN:
      return "go";
    default:
      return "unknown";
  }
}
console.log(action(Light.GREEN));
// prints go
```
Vergleiche immer mit den Mitgliedern (`Light.RED`), niemals mit den rohen Werten (`"red"`): Wenn sich der Wert jemals ändert, funktioniert der `switch` weiterhin.

# --instructions--

Schreibe eine Funktion `weekend(day)`, die einen `switch` auf der Enumeration `Day` verwendet und `true` für `Day.SATURDAY` und `Day.SUNDAY` zurückgibt und `false` für jeden anderen Wert

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {

}
```

# --asserts--

`weekend(Day.SATURDAY)` muss `true` zurückgeben.

```javascript
tryCatch(weekend(Day.SATURDAY) === true);
```

`weekend(Day.SUNDAY)` muss `true` zurückgeben.

```javascript
tryCatch(weekend(Day.SUNDAY) === true);
```

`weekend(Day.MONDAY)` muss `false` zurückgeben.

```javascript
tryCatch(weekend(Day.MONDAY) === false);
```

`weekend("holiday")` muss `false` zurückgeben, weil es kein Mitglied ist.

```javascript
tryCatch(weekend("holiday") === false);
```

Die Funktion muss eine `switch`-Anweisung verwenden.

```javascript
tryCatch(/switch\s*\(/.test(weekend.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {
  switch (day) {
    case Day.SATURDAY:
    case Day.SUNDAY:
      return true;
    default:
      return false;
  }
}
```
