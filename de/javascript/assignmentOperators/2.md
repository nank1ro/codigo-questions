---
language: javascript
exerciseType: 2
---

# --description--

Wir haben bereits eine initialisierte Variable `total`
```javascript
var total = 5;
```
Nehmen wir an, wir möchten die Zahl `2` zur Variablen `total` hinzufügen, wir können schreiben
```javascript
total = total + 2;
```
Okay, es funktioniert! Aber es gibt eine kürzere Version, um dasselbe zu tun:
```javascript
total += 2;
```
Das Zeichen `+=` wird **Additionszuweisung** genannt.
Es addiert einen Wert zum Variablenwert und weist das Ergebnis dieser Variablen zu.

# --instructions--

Addieren Sie zur Variablen `num` den Wert `15` mit dem *Additionszuweisungs*-Operator und geben Sie dann ihren Wert aus

# --seed--

```javascript
var num = 75;
[/][/][/];
console.log(num);
```

# --answers--

- +
- +=
- =+
- num
-  15

# --solutions--

```javascript
var num = 75;
num += 15;
console.log(num);
```

# --output--

90
