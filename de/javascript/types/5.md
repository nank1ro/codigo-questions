---
language: javascript
exerciseType: 1
---

# --description--

`Number()` ist streng: der gesamte String muss eine Zahl sein, sonst ist das Ergebnis `NaN` („Not a Number“):
```javascript
console.log(Number("12px"));
// prints NaN
```
`parseInt()` und `parseFloat()` sind nachgiebiger: sie lesen Ziffern vom Anfang des Strings, überspringen führende Leerzeichen und stoppen beim ersten Zeichen, das nicht Teil einer Zahl ist. `parseInt` behält nur den ganzzahligen Teil:
```javascript
console.log(parseInt("12px"), parseFloat("1.5kg"));
// prints 12 1.5
console.log(parseInt("3.9em"), parseInt("-4px"));
// prints 3 -4
```
Beginnt der String nicht mit etwas, das den Anfang einer Zahl sein kann (ein optionales Vorzeichen, dann eine Ziffer), geben auch sie `NaN` zurück:
```javascript
console.log(parseInt("auto"));
// prints NaN
```
`NaN` ist der einzige Wert, der nicht gleich sich selbst ist, daher ist `x === NaN` immer `false`; um ihn zu erkennen, verwende `Number.isNaN(x)`.

# --instructions--

Schreibe eine Funktion `toPixels(text)`, die die ganze Zahl am Anfang von `text` zurückgibt (wie `12` für `"12px"`), oder `0`, wenn `text` nicht mit einer Zahl beginnt, und dabei `parseInt` verwendet

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function toPixels(text) {

}
```

# --asserts--

`toPixels("12px")` muss `12` zurückgeben.

```javascript
tryCatch(toPixels("12px") === 12);
```

`toPixels("3.9em")` muss `3` zurückgeben.

```javascript
tryCatch(toPixels("3.9em") === 3);
```

`toPixels("-4px")` muss `-4` zurückgeben.

```javascript
tryCatch(toPixels("-4px") === -4);
```

`toPixels("auto")` muss `0` zurückgeben.

```javascript
tryCatch(toPixels("auto") === 0);
```

`toPixels("")` muss `0` zurückgeben.

```javascript
tryCatch(toPixels("") === 0);
```

Die Funktion muss `parseInt` verwenden.

```javascript
tryCatch(/parseInt\(/.test(toPixels.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function toPixels(text) {
  const value = parseInt(text);
  if (Number.isNaN(value)) {
    return 0;
  }
  return value;
}
```
