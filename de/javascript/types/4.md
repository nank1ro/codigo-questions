---
language: javascript
exerciseType: 2
---

# --description--

Du kannst einen Wert **explizit** in einen anderen Typ umwandeln, indem du den Typ als Funktion aufrufst:
- `Number(value)` wandelt in eine Zahl um: `Number("42")` ist `42`
- `String(value)` wandelt in einen String um: `String(42)` ist `"42"`
- `Boolean(value)` wandelt in einen Boolean um: `Boolean("")` ist `false`

Das Ergebnis ist ein ganz neuer Wert; das Original wird nicht geändert:
```javascript
const input = "7";
const count = Number(input) + 1;
console.log(count, typeof count);
// prints 8 number
console.log(String(count) + "!");
// prints 8!
```
Eine explizite Umwandlung macht deine Absicht sichtbar: Wer `Number(input)` liest, weiß, dass `input` Text war.

# --instructions--

Vervollständige den Code, sodass `count` `input` in eine Zahl umgewandelt plus `1` ist, `label` `count` in einen String umgewandelt und zweimal wiederholt ist und `flag` der in einen Boolean umgewandelte leere String ist, wodurch die Ausgabe `43 4343 false` entsteht

# --seed--

```javascript
const input = "42";
const count = [/](input) + 1;
const label = [/](count).repeat(2);
const flag = [/]("");
console.log(count, label, flag);
```

# --answers--

- Number
- String
- Boolean
- Int
- Bool
- toString

# --solutions--

```javascript
const input = "42";
const count = Number(input) + 1;
const label = String(count).repeat(2);
const flag = Boolean("");
console.log(count, label, flag);
```

# --output--

43 4343 false
