---
language: javascript
exerciseType: 1
---

# --description--

Asynchrone Arbeit kann auch fehlschlagen. Die Funktion, der `new Promise` übergeben wird, erhält einen zweiten Callback, **`reject`**: Rufe `reject(error)` auf, und das Promise wird statt erfüllt abgelehnt.
```javascript
function readAge(age) {
  return new Promise((resolve, reject) => {
    if (age >= 0) {
      resolve(age);
    } else {
      reject(new Error("negative age"));
    }
  });
}
```
Lehne immer mit einem `Error`-Objekt ab: Es trägt eine `message` und einen Stack-Trace mit sich, ein bloßer String nicht.

Eine Ablehnung liest man mit **`.catch(callback)`**, dem Spiegelbild von `.then`. **`Promise.reject(error)`** baut ein Promise, das bereits abgelehnt ist, so wie `Promise.resolve` ein erfülltes baut:
```javascript
readAge(-1).catch((error) => {
  console.log(error.message);
  // prints negative age
});
```
Sowohl `resolve` als auch `reject` aufzurufen oder eines davon zweimal ändert nichts: Nur der erste Aufruf zählt.

# --instructions--

Schreibe eine Funktion `checkAge`, die eine Zahl `age` entgegennimmt und ein Promise zurückgibt, das mit `age` erfüllt wird, wenn `age` mindestens `18` ist, und andernfalls mit `new Error("too young")` abgelehnt wird. Verwende `new Promise`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function checkAge(age) {
  
}
```

# --asserts--

`await checkAge(20)` muss `20` sein.

```javascript
(async () => {
  tryCatch((await checkAge(20)) === 20);
})().catch(() => tryCatch(false));
```

`await checkAge(18)` muss `18` sein, weil `18` alt genug ist.

```javascript
(async () => {
  tryCatch((await checkAge(18)) === 18);
})().catch(() => tryCatch(false));
```

`checkAge(10)` muss mit der Meldung `"too young"` abgelehnt werden.

```javascript
(async () => {
  let message = "";
  await checkAge(10).catch((error) => { message = error.message; });
  tryCatch(message === "too young");
})().catch(() => tryCatch(false));
```

`checkAge(0)` muss mit einem `Error`-Objekt abgelehnt werden, nicht mit einem String.

```javascript
(async () => {
  let reason = null;
  await checkAge(0).catch((error) => { reason = error; });
  tryCatch(reason instanceof Error);
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function checkAge(age) {
  return new Promise((resolve, reject) => {
    if (age >= 18) {
      resolve(age);
    } else {
      reject(new Error("too young"));
    }
  });
}
```
