---
language: javascript
exerciseType: 4
---

# --description--

Eine `async`-Funktion liest sich von oben nach unten wie jede andere Funktion auch: `await` pausiert sie einfach, bis das abgewartete Promise erfüllt ist, dann läuft die Ausführung in der nächsten Zeile weiter.
```javascript
async function main() {
  console.log("start");
  const value = await Promise.resolve("data");
  console.log(value);
  console.log("done");
}

main();
// prints start, then data, then done
```
Beachte die letzte Zeile: Eine `async`-Funktion muss immer noch **aufgerufen** werden. `main` ohne die Klammern zu schreiben, definiert die Arbeit, startet sie aber nie, und es wird nichts ausgegeben.

# --instructions--

Sortiere die Elemente so, dass das Programm eine `async`-Funktion `load` definiert, die `data` zurückgibt, dann eine `async`-Funktion `main`, die `start` ausgibt, `load()` in `value` abwartet, `value` ausgibt und schließlich `done` ausgibt. Definiere zuerst `load`, dann `main`, und rufe `main()` in der letzten Zeile auf

# --answers--

```javascript
async function load() {
  return "data";
}
```

```javascript
async function main() {
```

```javascript
  console.log("start");
```

```javascript
  const value = await load();
```

```javascript
  console.log(value);
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
async function load() {
  return "data";
}
async function main() {
  console.log("start");
  const value = await load();
  console.log(value);
  console.log("done");
}
main();
```

# --output--

start
data
done
