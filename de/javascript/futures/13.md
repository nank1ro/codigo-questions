---
language: javascript
exerciseType: 4
---

# --description--

Der Unterschied zwischen **sequenziellem** und **parallelem** Warten entscheidet sich danach, *wo* du `await` setzt:
```javascript
// sequential: about 300 + 300 = 600 ms
const a = await load("a");
const b = await load("b");

// parallel: about 300 ms
const [a, b] = await Promise.all([load("a"), load("b")]);
```
In der ersten Version startet der zweite Download erst, wenn der erste fertig ist, weil `await` die Funktion in dieser Zeile pausiert. In der zweiten werden beide Aufrufe getätigt, bevor irgendetwas abgewartet wird, sodass beide Downloads bereits laufen, während `Promise.all` wartet.

Verwende sequenzielles Warten nur, wenn die zweite Aufgabe wirklich das Ergebnis der ersten braucht. Sonst starte zuerst alles und warte gemeinsam ab.

# --instructions--

Sortiere die Elemente zu einem Programm, das `later` definiert, dann eine `async`-Funktion `main`, die `loading` ausgibt, beide `later`-Aufrufe zusammen mit `Promise.all` startet, die beiden Ergebnisse mit einem Leerzeichen verbunden ausgibt und schließlich `done` ausgibt. Definiere zuerst `later`, dann `main`, und rufe `main()` in der letzten Zeile auf

# --answers--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
```

```javascript
async function main() {
```

```javascript
  console.log("loading");
```

```javascript
  const results = await Promise.all([later("one", 30), later("two", 10)]);
```

```javascript
  console.log(results.join(" "));
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
async function main() {
  console.log("loading");
  const results = await Promise.all([later("one", 30), later("two", 10)]);
  console.log(results.join(" "));
  console.log("done");
}
main();
```

# --output--

loading
ready two
ready one
one two
done
