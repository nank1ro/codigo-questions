---
language: javascript
exerciseType: 4
---

# --instructions--

Ordne die Zeilen, um `"pear"` zum Set hinzuzufügen, jede Frucht auf einer eigenen Zeile auszugeben und dann auszugeben, wie viele Früchte das Set enthält

# --answers--

```javascript
const fruits = new Set(["apple", "kiwi"]);
```

```javascript
fruits.add("pear");
```

```javascript
fruits.forEach((fruit) => {
```

```javascript
  console.log(fruit);
```

```javascript
});
```

```javascript
console.log(fruits.size);
```

# --solutions--

```javascript
const fruits = new Set(["apple", "kiwi"]);
fruits.add("pear");
fruits.forEach((fruit) => {
  console.log(fruit);
});
console.log(fruits.size);
```

# --output--

apple
kiwi
pear
3
