---
language: javascript
exerciseType: 2
---

# --instructions--

Erstelle das Array `common` mit den Zahlen, die sowohl in `a` als auch in `b` sind, und gib es dann aus

# --seed--

```javascript
let a = new Set([1, 2, 3]);
let b = new Set([2, 3, 4]);
let common = [...a].[/]((x) => b.[/](x));
console.log(common);
```

# --answers--

- filter
- has
- map
- find
- includes
- contains

# --solutions--

```javascript
let a = new Set([1, 2, 3]);
let b = new Set([2, 3, 4]);
let common = [...a].filter((x) => b.has(x));
console.log(common);
```

# --output--

[ 2, 3 ]
