---
language: javascript
exerciseType: 1
---

# --description--

Erfasse nur das, was du brauchst. Eine Gruppe ist nicht nur eine Möglichkeit, einen Teil später auszulesen; sie zeigt auch dem Leser, welcher Teil des Musters wichtig ist. In einem Zeitmuster, bei dem du nur die Minuten willst, klammere nur die Minuten und lasse den Rest ungruppiert:
```javascript
const minutes = "at 14:35:02".match(/\d{2}:(\d{2}):\d{2}/)?.[1];
console.log(minutes);
// gibt 35 aus
```
Das ganze Muster muss weiterhin passen, also sind Stunden und Sekunden weiterhin erforderlich; sie werden nur nicht erfasst. Weniger Gruppen bedeuten weniger Nummern, die du beim Lesen von `match[1]`, `match[2]` und so weiter im Kopf behalten musst.

# --instructions--

Schreibe eine Funktion `monthOf`, die einen String `date` nimmt, der ein im Format `YYYY-MM-DD` geschriebenes Datum enthalten kann, möglicherweise umgeben von anderem Text, und seinen Monat als String zurückgibt, oder einen leeren String, wenn `date` kein solches Datum enthält. Zum Beispiel gibt `monthOf("2026-09-12")` `09` zurück

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function monthOf(date) {
  
}
```

# --asserts--

`monthOf("2026-09-12")` muss `09` zurückgeben.

```javascript
tryCatch(monthOf("2026-09-12") === "09");
```

`monthOf("born on 1999-12-31")` muss `12` zurückgeben.

```javascript
tryCatch(monthOf("born on 1999-12-31") === "12");
```

`monthOf("no date")` muss einen leeren String zurückgeben.

```javascript
tryCatch(monthOf("no date") === "");
```

`monthOf("12-31-1999")` muss einen leeren String zurückgeben.

```javascript
tryCatch(monthOf("12-31-1999") === "");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function monthOf(date) {
  return date.match(/\d{4}-(\d{2})-\d{2}/)?.[1] ?? "";
}
```
