---
language: javascript
exerciseType: 1
---

# --description--

Um **jeden** Treffer statt nur den ersten umzuschreiben, hast du zwei Möglichkeiten:
```javascript
console.log("a1 b2".replace(/\d/g, "#"));
// gibt a# b# aus
console.log("a1 b2".replaceAll(/\d/g, "#"));
// gibt a# b# aus
```
**`replaceAll`** ist das klarere von beiden und akzeptiert auch einen einfachen String als Muster. Wenn du ihm ein Regex gibst, muss dieses Regex **unbedingt** das `g`-Flag tragen, sonst wirft es einen `TypeError`; genau das verhindert den stillen Fehler, `replace` zu schreiben und nur den ersten Treffer zu korrigieren.

# --instructions--

Schreibe eine Funktion `redact`, die einen String `text` nimmt und ihn zurückgibt, wobei jeder Ziffernblock durch ein einzelnes `#` ersetzt wird, mithilfe von `replaceAll`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function redact(text) {
  
}
```

# --asserts--

Die Methode `replaceAll` muss verwendet werden.

```json
{
  "regex": "\\.replaceAll\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`redact("call 123 or 45")` muss `call # or #` zurückgeben.

```javascript
tryCatch(redact("call 123 or 45") === "call # or #");
```

`redact("no digits")` muss den Text unverändert zurückgeben.

```javascript
tryCatch(redact("no digits") === "no digits");
```

`redact("2026")` muss `#` zurückgeben.

```javascript
tryCatch(redact("2026") === "#");
```

`redact("a1b2c3")` muss `a#b#c#` zurückgeben.

```javascript
tryCatch(redact("a1b2c3") === "a#b#c#");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function redact(text) {
  return text.replaceAll(/\d+/g, "#");
}
```
