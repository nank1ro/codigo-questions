---
language: javascript
exerciseType: 2
---

# --description--

`if` ist das Schlüsselwort, das eine bedingte Anweisung in JavaScript einleitet. Es gibt hier kein Schlüsselwort `elif` — eine zweite Bedingung wird mit `else if` eingeleitet, geschrieben als zwei getrennte Wörter.

# --instructions--

Füllen Sie die Lücken aus, um eine Bedingungsanweisung zu erstellen

# --seed--

```javascript
[/] (true) {
    console.log("It's True!");
}
```

# --answers--

- if
- else
- elif
- else if

# --solutions--

```javascript
if (true) {
    console.log("It's True!");
}
```

# --output--

It's True!
