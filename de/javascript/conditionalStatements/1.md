---
language: javascript
exerciseType: 2
---

# --description--

Decision making is required when we want to execute code only if a certain condition is satisfied.
Let's assume we want to play outside only if the weather is nice.
In programming, we can save a boolean variable `niceWeather` and perform the action of playing outside `if` this variable is `true`, like:
```javascript
var niceWeather = true;
if (niceWeather) {
    // play outside
}
```

# --instructions--

Erstellen Sie ein `if` statement, das prüft, ob die `time` kleiner als `12` ist, und geben Sie dann im Codeblock den String `"It's morning"` aus

# --seed--

```javascript
var time = 8;
[/] (time < 12) {
    console.log("It's morning");
}
```

# --answers--

- if
- ==
- =
- else
- ?

# --solutions--

```javascript
var time = 8;
if (time < 12) {
    console.log("It's morning");
}
```

# --output--

It's morning
