---
language: javascript
exerciseType: 2
---

# --description--

Entscheidungsfindung ist erforderlich, wenn wir Code nur unter bestimmten Bedingungen ausführen möchten.
Angenommen, wir möchten nur dann nach draußen gehen, wenn das Wetter schön ist.
In der Programmierung können wir eine boolesche Variable `niceWeather` speichern und die Aktion `if` diese Variable `true` ist, wie folgt ausführen:
```javascript
var niceWeather = true;
if (niceWeather) {
    // play outside
}
```

# --instructions--

Erstellen Sie eine `if`-Anweisung, die überprüft, ob `time` kleiner als `12` ist. Geben Sie dann in der Codeblock den String `"It's morning"` aus

# --seed--

```javascript
var time = 8;
[/] (time < 12) {
    console.log("It's morning");
}
```

# --answers--

- if
- ==
- =
- else
- ?

# --solutions--

```javascript
var time = 8;
if (time < 12) {
    console.log("It's morning");
}
```

# --output--

It's morning
