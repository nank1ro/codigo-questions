---
language: c
exerciseType: 4
---

# --description--

C bietet keine eingebaute Möglichkeit, den Namen einer Enum-Konstante zu erhalten: `printf("%d\n", SUMMER)` gibt `2` aus, nicht `Summer`. Die übliche Lösung ist eine kleine Funktion mit einem `switch`, die für jede Konstante die passende Zeichenkette zurückgibt.

# --instructions--

Sortiere die Zeilen, um den Typ `Season` zu definieren, eine Funktion `season_name`, die den Namen einer Jahreszeit zurückgibt, und eine `main`, die den Namen von `SUMMER` ausgibt. Definiere die Funktion vor `main`, halte die Zweige in derselben Reihenfolge wie die Konstanten und setze `default` an letzte Stelle

# --answers--

```c
#include <stdio.h>
typedef enum { WINTER, SPRING, SUMMER, AUTUMN } Season;
```

```c
char *season_name(Season season) {
```

```c
    switch (season) {
```

```c
        case WINTER: return "Winter";
```

```c
        case SPRING: return "Spring";
```

```c
        case SUMMER: return "Summer";
```

```c
        default: return "Autumn";
```

```c
    }
}
```

```c
int main() {
```

```c
    printf("%s\n", season_name(SUMMER));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef enum { WINTER, SPRING, SUMMER, AUTUMN } Season;
char *season_name(Season season) {
    switch (season) {
        case WINTER: return "Winter";
        case SPRING: return "Spring";
        case SUMMER: return "Summer";
        default: return "Autumn";
    }
}
int main() {
    printf("%s\n", season_name(SUMMER));
    return 0;
}
```

# --output--

Summer
