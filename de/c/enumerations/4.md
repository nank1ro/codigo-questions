---
language: c
exerciseType: 2
---

# --description--

Eine Aufzählung ist auch ein Typ: Du kannst eine Variable dieses Typs deklarieren, indem du `enum` gefolgt vom Namen der Aufzählung schreibst, und ihr eine ihrer Konstanten zuweisen:
```c
enum Color favorite = GREEN;
```
Da die Konstanten Ganzzahlen sind, vergleichst du Aufzählungsvariablen mit den üblichen Operatoren:
```c
if (favorite == GREEN) {
    printf("Green it is\n");
}
```

# --instructions--

Vervollständige den Code, um die Variable `light` vom Typ `enum Light` mit dem Wert `GREEN` zu deklarieren, sodass das Programm `Go` ausgibt

# --seed--

```c
enum Light { RED, YELLOW, GREEN };
[/] Light light = [/];
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --answers--

- enum
- struct
- GREEN
- RED
- YELLOW
- "GREEN"

# --solutions--

```c
enum Light { RED, YELLOW, GREEN };
enum Light light = GREEN;
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --output--

Go
