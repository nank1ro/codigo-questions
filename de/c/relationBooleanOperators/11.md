---
language: c
exerciseType: 2
---

# --instructions--

Prüfen Sie, ob `word` **NICHT** gleich `dog` ist, und geben Sie das boolesche Ergebnis aus.
Beachten Sie, dass `strcmp` nur `0` zurückgibt, wenn die Zeichenketten **gleich** sind, also lassen Sie sich nicht täuschen, indem Sie denken, dass die 0, die es zurückgibt, wie der boolesche Wert falsch ist

# --seed--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat")[/][/];
printf("%d\n", [/]);
```

# --answers--

- word
- "dog"
-  == 
-  = 
- dog
-  != 
- 0
- are_words_equal

# --solutions--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat") == 0;
printf("%d\n", are_words_equal);
```

# --output--

1
