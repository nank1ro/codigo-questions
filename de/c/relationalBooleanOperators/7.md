---
language: c
exerciseType: 2
---

# --description--

Seit C99 stellt der Header `stdbool.h` den Typ `bool` und die Konstanten `true` (was `1` ist) und `false` (was `0` ist) bereit:
```c
#include <stdbool.h>

bool open = true;
bool full = 3 > 5;
printf("%d %d\n", open, full); // prints "1 0"
```
Ein `bool` ist darunter weiterhin eine Ganzzahl: Er wird mit `%d` ausgegeben und funktioniert mit `&&`, `||` und `!` genau wie ein Vergleichsergebnis. Er macht die Absicht nur klarer als ein einfaches `int`.

# --instructions--

Vervollständige den Code so, dass `open` die boolesche Konstante für wahr ist und `closed` ihre Negation, und gib beide aus

# --seed--

```c
bool open = [/];
bool closed = [/]open;
printf("%d %d\n", open, closed);
```

# --answers--

- true
- !
- false
- -
- ~

# --solutions--

```c
bool open = true;
bool closed = !open;
printf("%d %d\n", open, closed);
```

# --output--

1 0
