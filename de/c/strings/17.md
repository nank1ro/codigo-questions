---
language: c
exerciseType: 4
---

# --instructions--

Sortiere die Zeilen, sodass das Programm `password` mit `attempt` vergleicht und `Access granted` ausgibt. Deklariere `password` vor `attempt` und setze jede Meldung in ihren eigenen Zweig

# --answers--

```c
char password[] = "secret";
```

```c
char attempt[] = "secret";
```

```c
if (strcmp(password, attempt) == 0) {
```

```c
    printf("Access granted\n");
```

```c
} else {
```

```c
    printf("Access denied\n");
```

```c
}
```

# --solutions--

```c
char password[] = "secret";
char attempt[] = "secret";
if (strcmp(password, attempt) == 0) {
    printf("Access granted\n");
} else {
    printf("Access denied\n");
}
```

# --output--

Access granted
