---
language: c
exerciseType: 4
---

# --description--

`sprintf` funktioniert wie `printf`, schreibt den formatierten Text aber in ein char-Array statt auf den Bildschirm:
```c
char buffer[30];
sprintf(buffer, "%d items", 3); // buffer ist "3 items"
```
Der Puffer muss groß genug für den gesamten Text und sein `'\0'` sein.

# --instructions--

Sortiere die Zeilen, um den Text `Age: 25` in `buffer` zu erstellen und auszugeben. Deklariere `buffer` vor `age`

# --answers--

```c
char buffer[30];
```

```c
int age = 25;
```

```c
sprintf(buffer, "Age: %d", age);
```

```c
printf("%s\n", buffer);
```

# --solutions--

```c
char buffer[30];
int age = 25;
sprintf(buffer, "Age: %d", age);
printf("%s\n", buffer);
```

# --output--

Age: 25
