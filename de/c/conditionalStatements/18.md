---
language: c
exerciseType: 2
---

# --description--

Der Abstand zwischen den Teilen einer `if`-Zeile ist in C frei wählbar: `if(true){` und `if (true) {` sind für den Compiler dieselbe Anweisung, es zählt also nur die Reihenfolge der Teile.

# --instructions--

Fülle die Lücken aus, um den Codeblock auszuführen.

# --seed--

```c
[/][/] {
    printf("I'm ready!\n");
}
```

# --answers--

- (true)
- (false)
- ("true")
- ("false")
- if 
- else 
- else if 
- :
- ;

# --solutions--

```c
if (true) {
    printf("I'm ready!\n");
}
```

# --output--

I'm ready!
