---
language: c
exerciseType: 2
---

# --description--

Lass uns mit dem vorherigen Beispiel fortfahren.
```c
bool nice_weather = true;
if (nice_weather) {
    // play outside
}
```
Wir haben gesehen, dass die `if`-Anweisung den Codeblock nur ausführt, wenn die Bedingung `true` ist.
Ein weiterer wichtiger Punkt sind die **geschwungenen Klammern** `{}`, die einen Codeblock angeben.

# --instructions--

Repariere die `if`-Anweisung, indem du die *geschwungenen Klammern* `{}` hinzufügst.

# --seed--

```c
int time = 8;
[/] (time[/]12) [/]
    printf("It's morning\n");
[/]
```

# --answers--

- if
-  == 
-  = 
- else
-  ? 
-  < 
- {
- }

# --solutions--

```c
int time = 8;
if (time < 12) {
    printf("It's morning\n");
}
```

# --output--

It's morning
