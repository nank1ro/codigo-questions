---
language: c
exerciseType: 2
---

# --description--

Wir können auch eine bedingte Anweisung (`if`, `else if` oder `else`) in einer anderen bedingten Anweisung verschachteln, um eine komplexere Struktur zu schaffen.
```c
int num = 4;
if (num < 3) {
    printf("the number is lower than 3\n");
} else {
    if (num == 3) {
        printf("the number is 3\n");
  } else if (num == 4) {
        printf("the number is 4\n");
  } else {
        printf("the number is greather than 4\n");
  }
}
```
und die Ausgabe dieses Codes ist `the number is 4`.

# --instructions--

Ändern wir das Programm der Tageszeit, indem wir eine bedingte Anweisung in der `else`-Anweisung verschachteln.
Die verschachtelte bedingte Anweisung sollte prüfen, ob `time` *kleiner* als `18` ist, und im Codeblock sollte `"It's afternoon"` ausgegeben werden.
Wenn die vorherige Bedingung `false` ist, gib `"It's evening"` aus.

# --seed--

```c
int time = 19;
if (time > 0 && time < 12) {
    printf("It's morning\n");
} else if (time == 12) {
    printf("It's midday\n");
} else if (time == 0) {
    printf("It's midning\n");
} else {
[/]([/][/][/]) {
        printf("It's afternoon\n");
    }[/]{
    printf("It's evening\n");
    }
}
```

# --answers--

- 18
-  else if 
-  < 
-  == 
- :
- ;
- time
-  else if {
-  elseif 
-  if 
-  else 
-  else {

# --solutions--

```c
int time = 19;
if (time > 0 && time < 12) {
    printf("It's morning\n");
} else if (time == 12) {
    printf("It's midday\n");
} else if (time == 0) {
    printf("It's midning\n");
} else {
 if (time < 18) {
        printf("It's afternoon\n");
    } else {
    printf("It's evening\n");
    }
}
```

# --output--

It's evening
