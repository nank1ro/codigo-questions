---
language: c
exerciseType: 4
---

# --description--

Mehrere kleine Zahlen werden oft in einen größeren Wert gepackt. Um eine davon wieder auszulesen, verschiebe sie zunächst nach unten, sodass sie bei Bit `0` beginnt, und maskiere dann alles oberhalb aus:
```c
unsigned int packed = 0x1234;
printf("%u\n", (packed >> 8) & 0xFF);
// gibt "18" aus, das 0x12-Byte
```
Erst verschieben, dann maskieren – diese Reihenfolge gilt es zu merken: Die Maske beschreibt das Feld immer erst, wenn es unten angekommen ist.

# --instructions--

Ordne die Zeilen so, dass `packed` um 16 Stellen nach rechts verschoben wird, nur die unteren 8 Bits des verschobenen Werts in `third_byte` behalten und das Ergebnis ausgegeben wird

# --answers--

```c
unsigned int packed = 0xDEADBEEF;
```

```c
unsigned int shifted = packed >> 16;
```

```c
unsigned int third_byte = shifted & 0xFF;
```

```c
printf("%u\n", third_byte);
```

# --solutions--

```c
unsigned int packed = 0xDEADBEEF;
unsigned int shifted = packed >> 16;
unsigned int third_byte = shifted & 0xFF;
printf("%u\n", third_byte);
```

# --output--

173
