---
language: c
exerciseType: 2
---

# --instructions--

Vervollständigen Sie die Funktion, um das richtige Ergebnis auszugeben, wenn die `power`-Funktion mit einer Basis von `4` und einem Exponenten von `5` aufgerufen wird

# --seed--

```c
const int [/](int [/], int exp) {
    return pow(base, [/]);
}

int main() {
    printf("%d\n", [/](4, [/]));
    return 0;
}
```

# --answers--

- 5
- base
- exponent
- exp
- power
- exponent
- Double
- Int
- power
- result

# --solutions--

```c
const int power(int base, int exp) {
    return pow(base, exp);
}

int main() {
    printf("%d\n", power(4, 5));
    return 0;
}
```

# --output--

1024
