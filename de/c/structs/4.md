---
language: c
exerciseType: 2
---

# --description--

Jedes Mal `struct Point` zu schreiben ist umständlich. Mit `typedef` gibst du dem struct einen kurzen Typnamen, und das struct selbst kann anonym bleiben:
```c
typedef struct {
    double celsius;
} Temperature;

Temperature t = {21.5};
```
Der neue Name `Temperature` wird für sich allein verwendet, ohne das Schlüsselwort `struct` davor. Das ist die häufigste Art, structs in echten Programmen zu deklarieren.

# --instructions--

Vervollständige den Code, um das anonyme struct mit dem Member `celsius` unter dem Typnamen `Temperature` zu definieren, und deklariere dann `t` mit diesem Typ

# --seed--

```c
typedef [/] {
    double celsius;
} Temperature;
[/] t = {21.5};
printf("%.1f\n", t.celsius);
```

# --answers--

- struct
- enum
- typedef
- Temperature
- struct Temperature
- Temperature t

# --solutions--

```c
typedef struct {
    double celsius;
} Temperature;
Temperature t = {21.5};
printf("%.1f\n", t.celsius);
```

# --output--

21.5
