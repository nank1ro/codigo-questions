---
language: c
exerciseType: 2
---

# --description--

Über einen Zeiger werden die Member mit dem **Pfeil**-Operator `->` statt mit dem Punkt erreicht:
```c
void grow(Box *b) {
    b->size = b->size * 2;
}
```
`b->size` ist eine Kurzform für `(*b).size`: erst dem Zeiger folgen, dann den Member nehmen. Der Punkt funktioniert nur auf einem struct, der Pfeil nur auf einem Zeiger auf ein struct.
Der Aufrufer übergibt die Adresse seiner Variablen mit `&`, und die über den Zeiger vorgenommene Änderung ist nach dem Aufruf sichtbar.

# --instructions--

Vervollständige `grow`, sodass `size` über den Zeiger verdoppelt wird, und vervollständige den Aufruf, sodass er die Adresse von `box` übergibt

# --seed--

```c
#include <stdio.h>

typedef struct { int size; } Box;

void grow(Box *b) {
    b[/]size = b->size * 2;
}

int main() {
    Box box = {5};
    grow([/]box);
    printf("%d\n", box.size);
    return 0;
}
```

# --answers--

- ->
- .
- &
- *
- (*b)

# --solutions--

```c
#include <stdio.h>

typedef struct { int size; } Box;

void grow(Box *b) {
    b->size = b->size * 2;
}

int main() {
    Box box = {5};
    grow(&box);
    printf("%d\n", box.size);
    return 0;
}
```

# --output--

10
