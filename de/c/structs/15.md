---
language: c
exerciseType: 4
---

# --description--

`printf` hat keinen Formatbezeichner für ein ganzes struct. Die übliche Lösung ist eine kleine Funktion, die die Member in einem festen Format ausgibt, damit jeder Teil des Programms den Wert gleich darstellt:
```c
void print_person(Person p) {
    printf("%s (%d)\n", p.name, p.age);
}
```

# --instructions--

Sortiere die Zeilen, um den Typ `Point`, eine Funktion `print_point`, die einen Punkt als `(x, y)` ausgibt, und ein `main` zu definieren, das den Punkt `(3, 4)` ausgibt. Definiere die Funktion vor `main`

# --answers--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
```

```c
void print_point(Point p) {
```

```c
    printf("(%d, %d)\n", p.x, p.y);
```

```c
}
```

```c
int main() {
```

```c
    Point p = {3, 4};
```

```c
    print_point(p);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
void print_point(Point p) {
    printf("(%d, %d)\n", p.x, p.y);
}
int main() {
    Point p = {3, 4};
    print_point(p);
    return 0;
}
```

# --output--

(3, 4)
