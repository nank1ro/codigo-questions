---
language: c
exerciseType: 4
---

# --description--

Structs können wie jeder andere Typ in einem Array gespeichert werden. Jedes Element wird mit eigenen geschweiften Klammern initialisiert, und eine Schleife besucht sie eines nach dem anderen: erst indizieren, dann den Punkt verwenden:
```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};

for (int i = 0; i < 3; i++) {
    printf("%d\n", path[i].y);
}
```

# --instructions--

Sortiere die Zeilen, um den Typ `Point` zu definieren, drei Punkte in `path` zu speichern, ihre `x`-Member in `sum` aufzusummieren und `sum` auszugeben. Deklariere `path` vor `sum`

# --answers--

```c
typedef struct { int x; int y; } Point;
```

```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
```

```c
int sum = 0;
```

```c
for (int i = 0; i < 3; i++) {
```

```c
    sum += path[i].x;
```

```c
}
```

```c
printf("%d\n", sum);
```

# --solutions--

```c
typedef struct { int x; int y; } Point;
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
int sum = 0;
for (int i = 0; i < 3; i++) {
    sum += path[i].x;
}
printf("%d\n", sum);
```

# --output--

6
