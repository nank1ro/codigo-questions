---
language: c
exerciseType: 4
---

# --description--

Alles zusammengefügt: Wähle den Typ entsprechend der Art des Wertes, passe jeden `printf`-Bezeichner zu seinem Argumenttyp, und caste, wenn eine Berechnung in einem anderen Typ als dem seiner Operanden erfolgen muss.

# --instructions--

Ordne die Zeilen so, dass ein Programm entsteht, das `7` in `total` und `2` in `count` speichert, `total` geteilt durch `count` als `double` in `avg` berechnet und die Ganzzahldivision `total / count` gefolgt von `avg` mit einer Dezimalstelle ausgibt

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int total = 7, count = 2;
```

```c
    double avg = (double) total / count;
```

```c
    printf("%d %.1f\n", total / count, avg);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int total = 7, count = 2;
    double avg = (double) total / count;
    printf("%d %.1f\n", total / count, avg);
    return 0;
}
```

# --output--

3 3.5
