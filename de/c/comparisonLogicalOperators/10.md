---
language: c
exerciseType: 4
---

# --description--

Operatoren haben eine **Rangfolge**, die bestimmt, was zuerst berechnet wird:
1. `!` wird zuerst angewendet
2. dann die relationalen Vergleiche `<`, `>`, `<=`, `>=`
3. dann die Gleichheitsvergleiche `==`, `!=`
4. dann `&&`
5. dann `||`

Daher benötigt `a > 0 && a < 10` keine Klammern: Beide Vergleiche werden vor `&&` berechnet.
Und `x == 1 || y == 2 && z == 3` bedeutet `x == 1 || (y == 2 && z == 3)`, weil `&&` stärker bindet als `||`.

# --instructions--

Sortiere die Zeilen, um in `inside` zu speichern, ob `a` zwischen `1` und `9` liegt, seine Negation in `outside` zu speichern und beide Werte durch ein Leerzeichen getrennt auszugeben

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int a = 5;
```

```c
    int inside = a > 0 && a < 10;
```

```c
    int outside = !inside;
```

```c
    printf("%d %d\n", inside, outside);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int a = 5;
    int inside = a > 0 && a < 10;
    int outside = !inside;
    printf("%d %d\n", inside, outside);
    return 0;
}
```

# --output--

1 0
