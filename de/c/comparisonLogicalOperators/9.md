---
language: c
exerciseType: 4
---

# --description--

Kurzschlussauswertung überspringt auch Funktionsaufrufe: In `0 && check()` wird die Funktion `check` nie aufgerufen, sodass ein etwaiger Seiteneffekt (wie das Aktualisieren eines Zählers) nicht eintritt.

# --instructions--

Sortiere die Zeilen, um einen Zähler `calls`, eine Funktion `check`, die ihn erhöht und `1` zurückgibt, und eine `main` zu definieren, die `0 && check()` berechnet und das Ergebnis gefolgt von `calls` ausgibt. Definiere die Funktion vor `main`

# --answers--

```c
#include <stdio.h>
int calls = 0;
```

```c
int check() {
```

```c
    calls++;
    return 1;
}
```

```c
int main() {
```

```c
    int result = 0 && check();
```

```c
    printf("%d %d\n", result, calls);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int calls = 0;
int check() {
    calls++;
    return 1;
}
int main() {
    int result = 0 && check();
    printf("%d %d\n", result, calls);
    return 0;
}
```

# --output--

0 0
