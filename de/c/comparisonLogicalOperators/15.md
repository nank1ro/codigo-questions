---
language: c
exerciseType: 2
---

# --description--

Eine `while`-Schleife läuft weiter, solange ihre Bedingung ungleich null ist, daher entscheidet ein Vergleich, wann sie stoppt:
```c
int i = 0;
while (i < 3) {
    printf("%d\n", i);
    i++;
}
// gibt 0, 1, 2 aus
```
Die Wahl zwischen `<` und `<=` entscheidet, ob der letzte Wert eingeschlossen wird.

# --instructions--

Vervollständige den Code, sodass die Schleife die Zahlen von `1` bis `3` ausgibt, eine pro Zeile

# --seed--

```c
int count = [/];
while (count [/] 3) {
    printf("%d\n", count);
    count++;
}
```

# --answers--

- 1
- <=
- 0
- <
- !=
- >

# --solutions--

```c
int count = 1;
while (count <= 3) {
    printf("%d\n", count);
    count++;
}
```

# --output--

1
2
3
