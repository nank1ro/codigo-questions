---
language: c
exerciseType: 4
---

# --description--

Ein `bool`-Parameter kann direkt als Operand von `&&` oder `||` verwendet werden, ohne ihn mit `true` zu vergleichen: schreibe `age >= 18 && citizen`, nicht `citizen == true`.

# --instructions--

Sortiere die Zeilen, um `can_vote` zu definieren, das `true` zurückgibt, wenn `age` mindestens `18` ist und `citizen` `true` ist, und eine `main`, die das Ergebnis für `20, true` und dann für `20, false` ausgibt. Definiere die Funktion vor `main`

# --answers--

```c
#include <stdio.h>
#include <stdbool.h>
```

```c
bool can_vote(int age, bool citizen) {
```

```c
    return age >= 18 && citizen;
}
```

```c
int main() {
```

```c
    printf("%d\n", can_vote(20, true));
```

```c
    printf("%d\n", can_vote(20, false));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
#include <stdbool.h>
bool can_vote(int age, bool citizen) {
    return age >= 18 && citizen;
}
int main() {
    printf("%d\n", can_vote(20, true));
    printf("%d\n", can_vote(20, false));
    return 0;
}
```

# --output--

1
0
