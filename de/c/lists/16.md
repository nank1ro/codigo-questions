---
language: c
exerciseType: 2
---

# --description--

Zum Zusammensetzen: Eine Liste wird oft aus einem Array aufgebaut. Am Anfang anzuhängen kehrt die Reihenfolge um, daher wird das Array **rückwärts** durchlaufen, vom letzten Element zum ersten, und das erste Element landet am Kopf. Das Programm gibt die Liste dann mit einer Traversierung aus und gibt sie Knoten für Knoten frei, sodass jede `malloc` von einem `free` begleitet wird.

# --instructions--

Vervollständige `push_front` und die letzte Schleife, sodass das Programm die Liste `10, 20, 30` aus dem Array aufbaut, sie ausgibt und jeden Knoten freigibt

# --seed--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = [/];
    return [/];
}

int main() {
    int values[3] = {10, 20, 30};
    Node *head = NULL;
    for (int i = 2; i >= 0; i--) {
        head = push_front(head, values[i]);
    }
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        Node *next = head->next;
        [/](head);
        head = next;
    }
    return 0;
}
```

# --answers--

- head
- node
- NULL
- free
- values[i]

# --solutions--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

int main() {
    int values[3] = {10, 20, 30};
    Node *head = NULL;
    for (int i = 2; i >= 0; i--) {
        head = push_front(head, values[i]);
    }
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        Node *next = head->next;
        free(head);
        head = next;
    }
    return 0;
}
```

# --output--

10
20
30
