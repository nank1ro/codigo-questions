---
language: c
exerciseType: 4
---

# --description--

Das Umkehren einer Liste dreht jeden `next`-Zeiger um, direkt in der Liste, mit drei Zeigern: `prev` ist der bereits umgedrehte Teil, `head` der gerade verarbeitete Knoten und `next` eine Kopie des Rests der Liste, gesichert bevor die Verknüpfung geändert wird. In jedem Schritt zeigt der aktuelle Knoten zurück auf `prev`, dann rücken `prev` und `head` beide vor:
```c
Node *next = head->next;
head->next = prev;
prev = head;
head = next;
```
Wenn `head` `NULL` erreicht, wurde jede Verknüpfung umgedreht, und `prev` ist der neue Kopf.

# --instructions--

Sortiere die Zeilen, um `reverse` zu definieren, das eine Liste an Ort und Stelle umkehrt und ihren neuen Kopf zurückgibt, und ein `main`, das die umgekehrte Liste `1, 2, 3` ausgibt. Definiere die Funktion vor `main`

# --answers--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
```

```c
Node *reverse(Node *head) {
    Node *prev = NULL;
```

```c
    while (head != NULL) {
        Node *next = head->next;
```

```c
        head->next = prev;
```

```c
        prev = head;
```

```c
        head = next;
    }
```

```c
    return prev;
}
```

```c
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
```

```c
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct Node { int value; struct Node *next; } Node;
Node *reverse(Node *head) {
    Node *prev = NULL;
    while (head != NULL) {
        Node *next = head->next;
        head->next = prev;
        prev = head;
        head = next;
    }
    return prev;
}
int main() {
    Node c = {3, NULL};
    Node b = {2, &c};
    Node a = {1, &b};
    for (Node *n = reverse(&a); n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    return 0;
}
```

# --output--

3
2
1
