---
language: c
exerciseType: 2
---

# --description--

Das Entfernen des ersten Knotens ist das Spiegelbild von `push_front`: Sichere die Adresse des zweiten Knotens, gib den ersten frei und gib die gesicherte Adresse als neuen Kopf zurück. Der Aufrufer speichert das Ergebnis zurück in seine Kopf-Variable:
```c
Node *next = head->next;
free(head);
return next;
```
Entfernt man, bis der Kopf `NULL` ist, wird die ganze Liste freigegeben, ein Knoten pro Aufruf.

# --instructions--

Vervollständige `pop_front`, sodass es den ersten Knoten freigibt und den zweiten als neuen Kopf zurückgibt

# --seed--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

Node *pop_front(Node *head) {
    Node *next = [/];
    [/](head);
    return [/];
}

int main() {
    Node *head = NULL;
    head = push_front(head, 3);
    head = push_front(head, 2);
    head = push_front(head, 1);
    head = pop_front(head);
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        head = pop_front(head);
    }
    return 0;
}
```

# --answers--

- head->next
- free
- next
- head
- NULL

# --solutions--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

Node *pop_front(Node *head) {
    Node *next = head->next;
    free(head);
    return next;
}

int main() {
    Node *head = NULL;
    head = push_front(head, 3);
    head = push_front(head, 2);
    head = push_front(head, 1);
    head = pop_front(head);
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        head = pop_front(head);
    }
    return 0;
}
```

# --output--

2
3
