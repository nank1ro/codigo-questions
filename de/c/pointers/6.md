---
language: c
exerciseType: 4
---

# --description--

Ein Zeiger auf `char` funktioniert wie jeder andere Zeiger: Er hält die Adresse eines einzelnen Zeichens, und `*p` liest oder schreibt dieses Zeichen:
```c
char grade = 'B';
char *p = &grade;
*p = 'A';
printf("%c\n", grade); // gibt "A" aus
```
Lesen über einen Zeiger und Schreiben durch ihn lassen sich frei kombinieren: `*p = *p + 1` macht aus `'A'` ein `'B'`.

# --instructions--

Ordne die Zeilen so, dass `letter` als `'a'` deklariert wird, `p` darauf zeigt, der Buchstabe über den Zeiger durch `'b'` ersetzt und `letter` ausgegeben wird

# --answers--

```c
char letter = 'a';
```

```c
char *p = &letter;
```

```c
*p = 'b';
```

```c
printf("%c\n", letter);
```


# --solutions--

```c
char letter = 'a';
char *p = &letter;
*p = 'b';
printf("%c\n", letter);
```

# --output--

b
