---
language: python
exerciseType: 2
---

# --description--

一个非常常见的任务是从已有的列表构建一个新列表。
用 `for` 循环加上 `append()` 需要写好几行：
```python
nums = [1, 2, 3]
doubled = []
for n in nums:
    doubled.append(n * 2)
print(doubled)  # [2, 4, 6]
```
Python 为这个任务提供了一种更简短的形式：**list comprehension**，它用一个表达式就能构建出整个列表：
```python
doubled = [n * 2 for n in nums]
```
语法是 `[表达式 for 元素 in 可迭代对象]`：`for` 部分遍历元素，左边的表达式会针对每个元素求值。
结果是一个全新的列表，和用循环构建出来的完全一样。

# --instructions--

补全 list comprehension，使程序打印出 `[2, 4, 6]`

# --seed--

```python
nums = [1, 2, 3]
doubled = [n [/] 2 [/] n [/] nums]
print(doubled)
```

# --answers--

- *
- for
- in
- +
- of

# --solutions--

```python
nums = [1, 2, 3]
doubled = [n * 2 for n in nums]
print(doubled)
```

# --output--

[2, 4, 6]
