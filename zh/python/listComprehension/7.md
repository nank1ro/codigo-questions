---
language: python
exerciseType: 4
---

# --description--

在 comprehension 内部，你可以使用之前定义的任何变量，例如作为 `range()` 的上限。

# --instructions--

对元素排序，打印出小于 `limit` 的 7 的倍数

# --answers--

```python
limit = 20
```

```python
multiples = [n for n in range(1, limit) if n % 7 == 0]
```

```python
print(multiples)
```

# --solutions--

```python
limit = 20
multiples = [n for n in range(1, limit) if n % 7 == 0]
print(multiples)
```

# --output--

[7, 14]
