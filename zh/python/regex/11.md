---
language: python
exerciseType: 1
---

# --description--

`re.sub(pattern, replacement, text)` 返回一个新字符串，其中每个匹配都已被替换。字符串是不可变的，所以原始文本不会被动过：
```python
import re

print(re.sub(r"\d", "#", "call 555 now"))     # call ### now
print(re.sub(r"\s+", " ", "too    many gaps"))  # too many gaps
```

替换内容可以用 `\1`、`\2`……（命名组则用 `\g<name>`）来引用捕获组，这让重排文本变成一行代码。出于同样的反斜杠原因，替换内容也要写成原始字符串：
```python
import re

print(re.sub(r"(\w+), (\w+)", r"\2 \1", "Lovelace, Ada"))  # Ada Lovelace
```
`count` 参数限制被替换的匹配数量：`re.sub(r"\d", "#", "1 2 3", count=1)` 得到 `# 2 3`。

# --instructions--

编写一个函数 `mask_digits(text)`，把每个数字替换为 `#`；再编写一个函数 `swap_names(text)`，使用反向引用把 `surname, name` 对变成 `name surname`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`mask_digits` 必须替换每个数字

```python
    def test_1(self):
        self.assertEqual(mask_digits("call 555 now"), "call ### now", "--err-t1--")
        self.assertEqual(mask_digits("no digits"), "no digits", "--err-t1--")
```

`swap_names` 必须把名字放在姓氏之前

```python
    def test_2(self):
        self.assertEqual(swap_names("Lovelace, Ada"), "Ada Lovelace", "--err-t2--")
        self.assertEqual(swap_names("Turing, Alan"), "Alan Turing", "--err-t2--")
```

没有逗号的文本必须被 `swap_names` 原样保留

```python
    def test_3(self):
        self.assertEqual(swap_names("Ada Lovelace"), "Ada Lovelace", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def mask_digits(text):
    return re.sub(r"\d", "#", text)

def swap_names(text):
    return re.sub(r"(\w+), (\w+)", r"\2 \1", text)
```
