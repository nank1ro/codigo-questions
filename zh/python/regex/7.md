---
language: python
exerciseType: 4
---

# --description--

方括号定义一个**字符类**：一组字符，在该位置上其中任意一个字符都可以被接受。`[aeiou]` 匹配一个元音字母，`[0-9]` 匹配一个数字，`[a-z]` 匹配一个小写字母。紧跟在左方括号后面的 `^` 会翻转含义，所以 `[^0-9]` 匹配任何*不是*数字的字符。

在字符类之外，`^` 和 `$` 是**锚点**：`^` 把模式固定在文本的开头，`$` 固定在文本的末尾。使用 `re.fullmatch` 时锚点是隐含的，这就是用它做校验可读性更好的原因：
```python
import re

print(bool(re.fullmatch(r"[a-z]{3,8}", "ada")))  # True
print(bool(re.fullmatch(r"[a-z]{3,8}", "Ada")))  # False
```

# --instructions--

排列各项以构建一个程序：以 import 开头，在最后的 `print` 之前定义 `is_valid`，只接受由 3 到 8 个小写字母组成的名字，并打印三个名字的结果，产生输出 `True False False`

# --answers--

```python
import re
```

```python
def is_valid(name):
```

```python
    return re.fullmatch(r"[a-z]{3,8}", name) is not None
```

```python
print(is_valid("ada"), is_valid("Ada"), is_valid("ab"))
```

# --solutions--

```python
import re
def is_valid(name):
    return re.fullmatch(r"[a-z]{3,8}", name) is not None
print(is_valid("ada"), is_valid("Ada"), is_valid("ab"))
```

# --output--

True False False
