---
language: python
exerciseType: 2
---

# --description--

`re.search` 在第一个匹配处就停止。`re.findall(pattern, text)` 则会收集**每一个**匹配，并以字符串列表的形式返回它们：
```python
import re

print(re.findall(r"\d+", "a1 bb22 ccc333"))  # ['1', '22', '333']
print(re.findall(r"\d+", "no digits"))       # []
```
没有匹配时列表为空，因此没有 `None` 需要检查：可以直接对它进行循环，或用 `len(...)` 测量长度。注意 `findall` 返回的是普通字符串而不是匹配对象，所以无法获得位置信息。

# --instructions--

补全代码，使它收集每一段连续的数字，并打印该列表以及找到的数量，产生输出 `['1', '22', '333'] 3`

# --seed--

```python
import re

numbers = re.[/](r"\d+", "a1 bb22 ccc333")
print(numbers, [/](numbers))
```

# --answers--

- findall
- len
- search
- finditer
- sum

# --solutions--

```python
import re

numbers = re.findall(r"\d+", "a1 bb22 ccc333")
print(numbers, len(numbers))
```

# --output--

['1', '22', '333'] 3
