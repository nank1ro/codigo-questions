---
language: python
exerciseType: 1
---

# --description--

`str.split` 只能按固定的分隔符切分。`re.split(pattern, text)` 可以按模式描述的任何内容切分，这正是混乱输入通常需要的：
```python
import re

print("a, b;c".split(","))            # ['a', ' b;c']
print(re.split(r"[,;]", "a, b;c"))    # ['a', ' b', 'c']
print(re.split(r"[,;\s]+", "a, b;c")) # ['a', 'b', 'c']
```
把分隔符写成 `[,;\s]+` 可以让一整串逗号、分号和空格算作一次切分，而不是在它们之间留下空字符串。

`maxsplit` 参数在切分给定的次数后停止，把剩余的文本留在最后一个元素里：`re.split(r"\s+", "a b c", maxsplit=1)` 得到 `['a', 'b c']`。

# --instructions--

编写一个函数 `split_fields(line)`，返回该行中字段的列表，把任何一串逗号、分号和空格都视为单个分隔符

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`split_fields` 必须能按逗号、分号和空格切分

```python
    def test_1(self):
        self.assertEqual(split_fields("a, b;c"), ["a", "b", "c"], "--err-t1--")
        self.assertEqual(split_fields("red green blue"), ["red", "green", "blue"], "--err-t1--")
```

连续的分隔符必须只产生一次切分

```python
    def test_2(self):
        self.assertEqual(split_fields("x ; ; y"), ["x", "y"], "--err-t2--")
        self.assertEqual(split_fields("1,,2"), ["1", "2"], "--err-t2--")
```

没有分隔符的行必须返回单个字段

```python
    def test_3(self):
        self.assertEqual(split_fields("solo"), ["solo"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def split_fields(line):
    return re.split(r"[,;\s]+", line)
```
