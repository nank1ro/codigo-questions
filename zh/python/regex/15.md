---
language: python
exerciseType: 3
---

# --description--

**标志**（flags）会改变模式的应用方式。`re` 中的每个函数都通过 `flags` 参数接受它们，`re.compile` 则把它们存储在模式对象中：
```python
import re

print(re.search(r"cat", "A Cat", flags=re.IGNORECASE).group())  # Cat
title_re = re.compile(r"^#.*", re.MULTILINE)
```
最常用的两个是 `re.IGNORECASE`（让字母无论大小写都能匹配）和 `re.MULTILINE`（让 `^` 和 `$` 匹配每一行的开头和末尾，而不是整个文本）。多个标志用 `|` 组合，例如 `re.IGNORECASE | re.MULTILINE`。

标志只改变匹配规则：返回的文本始终是实际存在的那段文本，保留它原本的大小写。

# --instructions--

这段代码会打印出什么？
```python
import re

print(re.findall(r"cat", "Cat cat CAT", flags=re.IGNORECASE))
```

# --answers--

- `['Cat', 'cat', 'CAT']`，因为该标志让模式可以匹配任意大小写，而匹配到的文本保留它自己的大小写
- `['cat', 'cat', 'cat']`，因为 `re.IGNORECASE` 会先把文本转成小写再匹配
- `['cat']`，因为只有与模式书写完全一致的那一处才会匹配
- `['Cat', 'CAT']`，因为该标志匹配的是那些与模式不同的写法

# --solutions--

- `['Cat', 'cat', 'CAT']`，因为该标志让模式可以匹配任意大小写，而匹配到的文本保留它自己的大小写
