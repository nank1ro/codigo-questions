---
language: python
exerciseType: 2
---

# --description--

一个基本的类只由 `class` 关键字和类名组成，例如：
```python
class ClassName:
    pass
```

# --instructions--

创建一个名为 `Animal` 的类。
现在，在类的代码块中使用 `pass` 关键字。
（`pass` 不执行任何操作，但在 Python 需要表达式的地方，它可以作为占位符使用）

# --seed--

```python
[/][/]:
    [/]
```

# --answers--

- class 
- Class 
- Animal
- 'Animal'
- Animal()
- pass
- return

# --solutions--

```python
class Animal:
    pass
```
