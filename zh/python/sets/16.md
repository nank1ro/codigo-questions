---
language: python
exerciseType: 1
---

# --description--

两个集合**相等**，当它们包含相同的元素时，无论元素写出的顺序如何：
```python
print({1, 2, 3} == {3, 1, 2})  # True
```
将一个集合的长度与其去重后集合的长度进行比较，是检测重复项的一种快速方法：如果集合**更小**，说明有某个值出现了不止一次：
```python
emails = ["a@x.com", "b@x.com", "a@x.com"]
print(len(set(emails)) < len(emails))  # True
```
列表方法 `count(value)` 会告诉你某个值出现了多少次，这有助于找出*哪些*值是重复的。

# --instructions--

通过比较列表长度和其集合长度，将 `emails` 是否包含任何重复地址存储到 `has_duplicates` 中，然后使用集合推导式和 `count()`，将出现超过一次的地址集合存储到 `duplicates` 中

# --seed--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `set()`

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

必须使用集合推导式

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_duplicates` 必须为 `True`

```python
    def test_1(self):
        self.assertEqual(has_duplicates, True, "--err-t1--")
```

`duplicates` 必须等于 `{'ann@x.com', 'bob@x.com'}`

```python
    def test_2(self):
        self.assertEqual(duplicates, {"ann@x.com", "bob@x.com"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
has_duplicates = len(set(emails)) < len(emails)
duplicates = {e for e in emails if emails.count(e) > 1}
```
