---
language: python
exerciseType: 3
---

# --description--

运算符和方法并不完全等价。
运算符 `|`、`&`、`-` 和 `^` 只有在**两个**操作数都是集合时才能工作。
方法 `union()`、`intersection()`、`difference()` 和 `symmetric_difference()` 可以接受**任何可迭代对象**，比如列表或字符串：
```python
a = {1, 2}
print(a.union([2, 3]))  # {1, 2, 3}
print(a | [2, 3])       # TypeError: unsupported operand type(s) for |: 'set' and 'list'
```

# --instructions--

运行这段代码会发生什么？
```python
a = {1, 2}
print(a | [2, 3])
```

# --answers--

- 一个 `TypeError`，因为 `|` 只能在集合之间使用：用 `union()` 将集合与列表合并
- 打印 `{1, 2, 3}`
- 打印 `{1, 2, [2, 3]}`
- 打印 `[1, 2, 2, 3]`

# --solutions--

- 一个 `TypeError`，因为 `|` 只能在集合之间使用：用 `union()` 将集合与列表合并
