---
language: python
exerciseType: 2
---

# --description--

lambda 可以接受**多个参数**。
用逗号分隔它们，和 `def` 函数中完全一样：
```python
power = lambda base, exp: base ** exp
print(power(2, 3))  # 8
```

# --instructions--

补全这个 lambda，使它接受参数 `a` 和 `b` 并返回它们的和

# --seed--

```python
add = lambda a[/] b[/] a + b
print(add(3, 4))
```

# --answers--

- ,
- :
- ;
- ->

# --solutions--

```python
add = lambda a, b: a + b
print(add(3, 4))
```

# --output--

7
