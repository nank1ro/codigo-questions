---
language: python
exerciseType: 2
---

# --description--

你甚至不需要存储 lambda：可以**立即调用它**。
把 lambda 用括号括起来，然后加上参数：
```python
print((lambda x: x + 1)(4))  # 5
```

# --instructions--

补全代码，用参数 `3` 和 `4` 立即调用这个 lambda

# --seed--

```python
print([/]lambda x, y: x * y[/](3, 4))
```

# --answers--

- (
- )
- [
- ]
- {

# --solutions--

```python
print((lambda x, y: x * y)(3, 4))
```

# --output--

12
