---
language: python
exerciseType: 1
---

# --description--

lambda 的参数也支持**默认值**：
```python
greet = lambda name="World": f"Hello, {name}!"
print(greet())       # Hello, World!
print(greet("Ana"))  # Hello, Ana!
```

# --instructions--

将一个接受 `n` 和第二个默认值为 `2` 的参数 `factor`、并返回 `n` 乘以 `factor` 的 lambda 存储到变量 `multiply` 中

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `lambda` 表达式

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`multiply(5)` 必须返回 `10`

```python
    def test_1(self):
        self.assertEqual(multiply(5), 10, "--err-t1--")
```

`multiply(5, 3)` 必须返回 `15`

```python
    def test_2(self):
        self.assertEqual(multiply(5, 3), 15, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
multiply = lambda n, factor=2: n * factor
```
