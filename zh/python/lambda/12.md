---
language: python
exerciseType: 1
---

# --description--

lambda 并不局限于内置函数：**你自己的函数**也可以接受一个函数作为参数并调用它。
```python
def apply(func, value):
    return func(value)

print(apply(lambda x: x * 10, 3))  # 30
```

# --instructions--

定义一个函数 `apply_twice(func, value)`，它把 `func` 应用到 `value` 两次并返回结果。
然后将 `apply_twice` 用一个把 `3` 相加的 lambda 和值 `10` 调用后的结果存储到 `result` 中

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `lambda` 表达式

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`apply_twice(lambda x: x * 2, 5)` 必须返回 `20`

```python
    def test_1(self):
        self.assertEqual(apply_twice(lambda x: x * 2, 5), 20, "--err-t1--")
```

`apply_twice(lambda s: s + "!", "hi")` 必须返回 `hi!!`

```python
    def test_2(self):
        self.assertEqual(apply_twice(lambda s: s + "!", "hi"), "hi!!", "--err-t2--")
```

`result` 必须等于 `16`

```python
    def test_3(self):
        self.assertEqual(result, 16, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def apply_twice(func, value):
    return func(func(value))

result = apply_twice(lambda x: x + 3, 10)
```
