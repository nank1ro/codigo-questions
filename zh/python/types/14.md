---
language: python
exerciseType: 2
---

# --description--

你可以把变量、参数或返回值的预期类型写成**类型提示**：变量和参数在名字后面加冒号，返回值在冒号前面加箭头 `->`：
```python
count: int = 3

def greet(name: str) -> str:
    return "Hi " + name
```
类型提示是给人和编辑器等工具看的**文档**：Python **不会**检查它们。下面这段代码毫无怨言地运行并打印 `hello`：
```python
count: int = "hello"
print(count)
```
提示让预期的类型一目了然，但真正的类型仍然由值决定。

# --instructions--

补全类型提示，使 `double` 接收一个 `int` 并返回一个 `int`，且 `result` 被声明为 `int`

# --seed--

```python
def double(value[/] int) [/] int:
    return value * 2

result[/] int = double(4)
print(result)
print(double(2.5))
```

# --answers--

- :
- ->
- :
- =>
- ::

# --solutions--

```python
def double(value: int) -> int:
    return value * 2

result: int = double(4)
print(result)
print(double(2.5))
```

# --output--

8
5.0
