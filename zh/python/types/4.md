---
language: python
exerciseType: 1
---

# --description--

算术运算符你已经了解了。这里重要的是**结果的类型**。
`int` 与 `float` 相结合会得到 `float`，即使小数部分是零：
```python
print(3 + 0.5)   # 3.5
print(2 * 1.0)   # 2.0
```
**真除法** `/` 总是返回 `float`，即使能整除：
```python
print(10 / 5)    # 2.0
print(7 / 2)     # 3.5
```
**整除** `//` 把结果向下取整到最接近的整数（所以 `-7 // 2` 是 `-4`），并且当两个操作数都是整数时返回 `int`。它和取余 `%` 一起可以把一个数量分成整份：
```python
print(7 // 2)    # 3
print(7 % 2)     # 1
```

# --instructions--

把 `total` 用 `/` 除以 `people` 的结果存入 `each`，用 `//` 做同样的除法存入 `whole`，把余数存入 `rest`

# --seed--

```python
total = 17
people = 5
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `//`

```json
{
  "regex": "//",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`each` 必须是浮点数 `3.4`

```python
    def test_1(self):
        self.assertIsInstance(each, float, "--err-t1--")
        self.assertEqual(each, 3.4, "--err-t1--")
```

`whole` 必须是整数 `3`

```python
    def test_2(self):
        self.assertIsInstance(whole, int, "--err-t2--")
        self.assertEqual(whole, 3, "--err-t2--")
```

`rest` 必须等于 `2`

```python
    def test_3(self):
        self.assertEqual(rest, 2, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
total = 17
people = 5

each = total / people
whole = total // people
rest = total % people
```
