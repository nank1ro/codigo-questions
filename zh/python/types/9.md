---
language: python
exerciseType: 1
---

# --description--

`None` 是一个独立的值，表示"这里什么都没有"。没有 `return` 语句的函数返回的就是它，它也常用作尚未确定的值的占位符。
由于 `None` 只有一个，请用 `is` 而不是 `==` 来检查它：
```python
result = None
if result is None:
    print("no result yet")
```
每种类型都有一个 `__name__` 属性，以字符串形式保存它的名字，这在生成消息时很方便：
```python
print(type(3).__name__)     # int
print(type(None).__name__)  # NoneType
```

# --instructions--

编写一个函数 `describe`，接收一个值，当该值为 `None` 时返回字符串 `"nothing"`，否则返回其类型的名字

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须用 `is None` 进行比较

```json
{
  "regex": "is\\s+None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

必须使用 `__name__`

```json
{
  "regex": "__name__",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe(None)` 必须返回 `"nothing"`

```python
    def test_1(self):
        self.assertEqual(describe(None), "nothing", "--err-t1--")
```

`describe(5)` 必须返回 `"int"`，`describe(2.0)` 必须返回 `"float"`

```python
    def test_2(self):
        self.assertEqual(describe(5), "int", "--err-t2--")
        self.assertEqual(describe(2.0), "float", "--err-t2--")
```

`describe("a")` 必须返回 `"str"`，`describe(True)` 必须返回 `"bool"`

```python
    def test_3(self):
        self.assertEqual(describe("a"), "str", "--err-t3--")
        self.assertEqual(describe(True), "bool", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe(value):
    if value is None:
        return "nothing"
    return type(value).__name__
```
