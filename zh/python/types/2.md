---
language: python
exerciseType: 1
---

# --description--

`type()` 返回一个值的类，因此你可以用 `is` 把它和类名比较：
```python
age = 30
print(type(age) is int)  # True
```
不过大多数情况下，你只想知道一个值**是不是**某个类型。这正是 `isinstance(value, cls)` 的用途，它返回 `True` 或 `False`：
```python
print(isinstance(age, int))    # True
print(isinstance(age, str))    # False
```
第二个参数也可以是一个类的**元组**：只要值属于其中任意一个，结果就是 `True`：
```python
print(isinstance(2.5, (int, float)))  # True
```

# --instructions--

用 `type()` 把 `age` 的类型存入 `age_type`，用 `isinstance()` 把 `name` 是否为 `str` 存入 `is_text`，把 `price` 是否为 `int` 或 `float` 存入 `is_number`

# --seed--

```python
age = 30
price = 9.99
name = "Ada"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `type()`

```json
{
  "regex": "type\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

必须使用 `isinstance()`

```json
{
  "regex": "isinstance\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`age_type` 必须是 `int`

```python
    def test_1(self):
        self.assertIs(age_type, int, "--err-t1--")
```

`is_text` 必须是 `True`

```python
    def test_2(self):
        self.assertIs(is_text, True, "--err-t2--")
```

`is_number` 必须是 `True`

```python
    def test_3(self):
        self.assertIs(is_number, True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
age = 30
price = 9.99
name = "Ada"

age_type = type(age)
is_text = isinstance(name, str)
is_number = isinstance(price, (int, float))
```
