---
language: python
exerciseType: 1
---

# --description--

格式化只改变数字的显示方式。要得到四舍五入后的**值**，请使用内置函数 `round()`：
```python
print(round(3.14159, 2))  # 3.14
print(round(2.71828, 1))  # 2.7
```
只有一个参数时，`round()` 四舍五入到最接近的整数并返回 `int`；带上小数位数时，它返回 `float`：
```python
print(round(3.7))     # 4
print(round(3.7, 0))  # 4.0
```
注意，正好位于两个数中间的值会舍入到**偶数**那一个：`round(2.5)` 是 `2`，而 `round(3.5)` 是 `4`。

# --instructions--

编写一个函数 `average`，接收一个数字列表，返回它们的平均值并保留两位小数

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `round()`

```json
{
  "regex": "round\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`average([1, 2, 3, 4])` 必须返回 `2.5`

```python
    def test_1(self):
        self.assertEqual(average([1, 2, 3, 4]), 2.5, "--err-t1--")
```

`average([1, 2, 2])` 必须返回 `1.67`

```python
    def test_2(self):
        self.assertEqual(average([1, 2, 2]), 1.67, "--err-t2--")
        self.assertEqual(average([1, 1, 1, 2]), 1.25, "--err-t2--")
```

`average([10])` 必须返回浮点数 `10.0`

```python
    def test_3(self):
        self.assertIsInstance(average([10]), float, "--err-t3--")
        self.assertEqual(average([10]), 10.0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def average(numbers):
    return round(sum(numbers) / len(numbers), 2)
```
