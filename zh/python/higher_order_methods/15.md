---
language: python
exerciseType: 4
---

# --instructions--

对各项进行排序，以只保留长度超过三个字母的单词，把它们转换为大写，并按长度排序打印

# --answers--

```python
words = ["pear", "fig", "banana", "kiwi"]
```

```python
long_words = filter(lambda w: len(w) > 3, words)
```

```python
shouted = map(str.upper, long_words)
```

```python
print(sorted(shouted, key=len))
```

# --solutions--

```python
words = ["pear", "fig", "banana", "kiwi"]
long_words = filter(lambda w: len(w) > 3, words)
shouted = map(str.upper, long_words)
print(sorted(shouted, key=len))
```

# --output--

['PEAR', 'KIWI', 'BANANA']
