---
language: python
exerciseType: 2
---

# --description--

内置的 `map(func, iterable)` 对每个元素调用 `func` 并产生结果，每个元素对应一个结果。它返回一个惰性的 *map 对象*，所以要把它包装在 `list()` 中才能看到这些值：
```python
numbers = [1, 2, 3]
print(list(map(lambda n: n * 10, numbers)))  # [10, 20, 30]
```
可以传递任何可调用对象，而不仅仅是 lambda：像 `len` 这样的内置函数，或者从其类中取出的方法，比如 `str.upper`，它把字符串作为自己的第一个参数：
```python
names = ["ada", "linus"]
print(list(map(str.upper, names)))  # ['ADA', 'LINUS']
print(list(map(len, names)))        # [3, 5]
```

# --instructions--

补全代码，使 `upper` 保存大写形式的名称，`lengths` 保存它们的长度，产生输出 `['ADA', 'LINUS']`、`[3, 5]`

# --seed--

```python
names = ["ada", "linus"]
upper = list([/](str.upper, names))
lengths = list(map([/], names))
print(upper)
print(lengths)
```

# --answers--

- map
- len
- filter
- str
- sorted

# --solutions--

```python
names = ["ada", "linus"]
upper = list(map(str.upper, names))
lengths = list(map(len, names))
print(upper)
print(lengths)
```

# --output--

['ADA', 'LINUS']
[3, 5]
