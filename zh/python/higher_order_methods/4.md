---
language: python
exerciseType: 1
---

# --description--

内置的 `filter(func, iterable)` 只保留让 `func` 返回真值的元素。和 `map` 一样，它返回一个惰性对象，必须转换成列表：
```python
numbers = [4, -2, 7, 0]
print(list(filter(lambda n: n > 0, numbers)))  # [4, 7]
```
传递给 `filter` 的函数被称为**谓词**：它接收一个元素，并回答一个关于该元素的是/否问题。传递 `None` 而不是函数时，会保留那些本身为真的元素，丢弃 `0`、`""` 和 `None`。

# --instructions--

使用 `filter` 编写一个函数 `positives`，它接收一个数字列表，并返回一个只包含大于零的数字的新列表

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `filter`

```json
{
  "regex": "filter\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`positives([3, -1, 0, 5])` 必须返回 `[3, 5]`

```python
    def test_1(self):
        self.assertEqual(positives([3, -1, 0, 5]), [3, 5], "--err-t1--")
```

结果必须是一个列表

```python
    def test_2(self):
        self.assertIsInstance(positives([1, 2]), list, "--err-t2--")
        self.assertEqual(positives([1, 2]), [1, 2], "--err-t2--")
```

`positives([-4, 0])` 必须返回一个空列表

```python
    def test_3(self):
        self.assertEqual(positives([-4, 0]), [], "--err-t3--")
        self.assertEqual(positives([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def positives(numbers):
    return list(filter(lambda n: n > 0, numbers))
```
