---
language: python
exerciseType: 1
---

# --description--

`partial` 与接受选项的内置函数搭配使用非常方便。`int(text, base=16)` 解析十六进制字符串；固定进制后就得到一个单参数的转换器，正好适合 `map`：
```python
from functools import partial

hex_to_int = partial(int, base=16)
print(hex_to_int("ff"))                      # 255
print(list(map(hex_to_int, ["a", "10"])))    # [10, 16]
```
partial 对象记住它所包装的内容：`hex_to_int.func` 是 `int`，而 `hex_to_int.keywords` 是 `{'base': 16}`。

# --instructions--

用 `partial` 定义两个函数：把十六进制字符串转换为 `int` 的 `hex_to_int`，以及把二进制字符串转换为 `int` 的 `binary_to_int`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`hex_to_int("ff")` 必须返回 `255`

```python
    def test_1(self):
        self.assertEqual(hex_to_int("ff"), 255, "--err-t1--")
        self.assertEqual(hex_to_int("10"), 16, "--err-t1--")
```

`binary_to_int("1010")` 必须返回 `10`

```python
    def test_2(self):
        self.assertEqual(binary_to_int("1010"), 10, "--err-t2--")
        self.assertEqual(binary_to_int("11"), 3, "--err-t2--")
```

两者都必须是包装 `int` 的 partial 对象

```python
    def test_3(self):
        self.assertIs(hex_to_int.func, int, "--err-t3--")
        self.assertIs(binary_to_int.func, int, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import partial

hex_to_int = partial(int, base=16)
binary_to_int = partial(int, base=2)
```
