---
language: python
exerciseType: 1
---

# --description--

`functools` 模块中的 `reduce(func, iterable, initial)` 把一个序列折叠成一个**单一的值**。它以到目前为止的结果和下一个元素为参数调用 `func`，从 `initial` 开始：
```python
from functools import reduce

total = reduce(lambda acc, n: acc + n, [1, 2, 3], 0)
print(total)  # 6
```
步骤是 `0 + 1`，然后是 `1 + 2`，再是 `3 + 3`。当省略 `initial` 时，第一个元素会被用作起始值，但这样空序列会引发 `TypeError`，所以只要序列可能为空，就应给出一个初始值。

# --instructions--

使用 `reduce` 编写一个函数 `product`，它接收一个数字列表并返回它们全部相乘的结果；空列表的乘积必须是 `1`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `reduce`

```json
{
  "regex": "reduce\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`product([2, 3, 4])` 必须返回 `24`

```python
    def test_1(self):
        self.assertEqual(product([2, 3, 4]), 24, "--err-t1--")
```

`product([5])` 必须返回 `5`

```python
    def test_2(self):
        self.assertEqual(product([5]), 5, "--err-t2--")
```

`product([])` 必须返回 `1`

```python
    def test_3(self):
        self.assertEqual(product([]), 1, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import reduce

def product(numbers):
    return reduce(lambda acc, n: acc * n, numbers, 1)
```
