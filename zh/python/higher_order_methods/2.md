---
language: python
exerciseType: 1
---

# --description--

把函数作为参数传递，让调用者决定做**什么**，而高阶函数决定做**多少次**或在**什么之上**做。函数参数可以按需被调用任意次，其结果还可以再次反馈给它：
```python
def repeat(func, value, times):
    for _ in range(times):
        value = func(value)
    return value

def add_one(n):
    return n + 1

print(repeat(add_one, 0, 3))  # 3
```
任何可调用对象都可以：`def` 函数、像 `len` 这样的内置函数，或者一个 `lambda`。

# --instructions--

编写一个函数 `apply_twice`，它接收一个函数 `func` 和一个 `value`，并返回将 `func` 连续应用到 `value` 两次的结果

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`apply_twice(lambda x: x * 2, 3)` 必须返回 `12`

```python
    def test_1(self):
        self.assertEqual(apply_twice(lambda x: x * 2, 3), 12, "--err-t1--")
```

`apply_twice(lambda s: s + "!", "hi")` 必须返回 `"hi!!"`

```python
    def test_2(self):
        self.assertEqual(apply_twice(lambda s: s + "!", "hi"), "hi!!", "--err-t2--")
```

第二次调用必须接收到第一次调用的结果

```python
    def test_3(self):
        seen = []
        def record(x):
            seen.append(x)
            return x + 10
        self.assertEqual(apply_twice(record, 0), 20, "--err-t3--")
        self.assertEqual(seen, [0, 10], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def apply_twice(func, value):
    return func(func(value))
```
