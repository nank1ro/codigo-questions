---
language: python
exerciseType: 1
---

# --description--

只接受一个参数的装饰器用途有限。要包装**任意**函数，包装器会把每个位置参数收集到 `*args` 中，把每个关键字参数收集到 `**kwargs` 中，并原样转发它们：
```python
def uppercase(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs).upper()
    return wrapper

@uppercase
def greet(name, punctuation="!"):
    return "hello " + name + punctuation

print(greet("ada"))                    # HELLO ADA!
print(greet("ada", punctuation="?"))   # HELLO ADA?
```
在包装器内部，`args` 是一个元组，`kwargs` 是一个字典；调用中的 `*` 和 `**` 把它们重新解包成单独的参数。

# --instructions--

编写一个装饰器 `exclaim`，它返回一个接受任意参数的包装器，用这些参数调用被包装的函数，并返回其结果加上 `"!"` 后的值；然后用它装饰一个函数 `greet`，该函数接收一个 `name` 并返回 `"hello <name>"`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `@exclaim` 应用装饰器

```json
{
  "regex": "^@exclaim\\s*$",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greet("ada")` 必须返回 `"hello ada!"`

```python
    def test_1(self):
        self.assertEqual(greet("ada"), "hello ada!", "--err-t1--")
```

`exclaim` 必须能用于带有两个参数的函数

```python
    def test_2(self):
        joined = exclaim(lambda a, b: a + " " + b)
        self.assertEqual(joined("good", "day"), "good day!", "--err-t2--")
```

`exclaim` 必须转发关键字参数

```python
    def test_3(self):
        tag = exclaim(lambda word, times=1: word * times)
        self.assertEqual(tag("hi", times=2), "hihi!", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def exclaim(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs) + "!"
    return wrapper

@exclaim
def greet(name):
    return "hello " + name
```
