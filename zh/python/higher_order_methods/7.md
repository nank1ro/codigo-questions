---
language: python
exerciseType: 1
---

# --description--

函数也可以**返回**一个函数。用 `def` 定义一个内部函数，并在不调用它的情况下返回它：
```python
def make_greeter(greeting):
    def greet(name):
        return greeting + ", " + name
    return greet

hello = make_greeter("Hello")
print(hello("Ada"))  # Hello, Ada
```
即使 `make_greeter` 已经结束，内部函数仍继续使用 `greeting`：它**记住**了创建它的作用域中的变量。这样的函数被称为**闭包**。每次调用 `make_greeter` 都会创建一个新的、独立的闭包，它拥有自己的 `greeting`。

# --instructions--

编写一个函数 `make_multiplier`，它接收一个数字 `factor`，并返回一个函数，该函数接收一个数字并返回它与 `factor` 相乘的结果

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`make_multiplier(2)` 必须返回一个函数

```python
    def test_1(self):
        self.assertTrue(callable(make_multiplier(2)), "--err-t1--")
```

`make_multiplier(2)(5)` 必须返回 `10`

```python
    def test_2(self):
        double = make_multiplier(2)
        self.assertEqual(double(5), 10, "--err-t2--")
        self.assertEqual(double(0), 0, "--err-t2--")
```

每个乘法器必须记住自己的因子

```python
    def test_3(self):
        double = make_multiplier(2)
        triple = make_multiplier(3)
        self.assertEqual(triple(4), 12, "--err-t3--")
        self.assertEqual(double(4), 8, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def make_multiplier(factor):
    def multiply(number):
        return number * factor
    return multiply
```
