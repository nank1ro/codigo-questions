---
language: python
exerciseType: 1
---

# --description--

短路求值是**保护**某个在部分值上会失败的操作的安全方式。在 `word is not None and len(word) < 4` 中，`len(word)` 只在 `word` 不是 `None` 时执行，所以这次调用永远不会抛出错误。保护条件必须放在前面：
```python
def is_short(word):
    return word is not None and len(word) < 4

print(is_short(None))   # False
print(is_short("cat"))  # True
```
请记住 `and` 返回的是一个操作数：`word and len(word) < 4` 对 `None` 给出 `None`，对空字符串给出 `""`，而不是 `False`。当需要布尔值时，请用真正的比较来做保护。

# --instructions--

编写一个函数 `first_is_zero(numbers)`，当列表 `numbers` 非空且其第一个元素为 `0` 时返回 `True`，否则返回 `False`。即使对空列表，结果也必须是布尔值

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_is_zero([0, 5])` 必须返回 `True`

```python
    def test_1(self):
        self.assertIs(first_is_zero([0, 5]), True, "--err-t1--")
        self.assertIs(first_is_zero([0]), True, "--err-t1--")
```

`first_is_zero([3, 0])` 必须返回 `False`

```python
    def test_2(self):
        self.assertIs(first_is_zero([3, 0]), False, "--err-t2--")
```

`first_is_zero([])` 必须返回 `False` 且不抛出错误

```python
    def test_3(self):
        self.assertIs(first_is_zero([]), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def first_is_zero(numbers):
    return len(numbers) > 0 and numbers[0] == 0
```
