---
language: python
exerciseType: 4
---

# --instructions--

排序这些项，定义 `users` 字典和 `can_login` 函数，该函数用 `name in users` 保护查找，然后输出使用正确密码的 Ada 的结果以及未注册的 Grace 的结果，输出 `True`、`False`

# --answers--

```python
users = {"ada": "secret", "linus": "tux"}
```

```python
def can_login(name, password):
    return name in users and users[name] == password
```

```python
print(can_login("ada", "secret"))
```

```python
print(can_login("grace", "secret"))
```

# --solutions--

```python
users = {"ada": "secret", "linus": "tux"}
def can_login(name, password):
    return name in users and users[name] == password
print(can_login("ada", "secret"))
print(can_login("grace", "secret"))
```

```python
def can_login(name, password):
    return name in users and users[name] == password
users = {"ada": "secret", "linus": "tux"}
print(can_login("ada", "secret"))
print(can_login("grace", "secret"))
```

# --output--

True
False
