---
language: python
exerciseType: 1
---

# --description--

`any(iterable)` 在至少有一个元素为真时返回 `True`，`all(iterable)` 在每个元素都为真时返回 `True`（`all([])` 是 `True`）。与生成器表达式结合，它们可以表达对整个集合的条件，并且像 `or` 和 `and` 一样短路：
```python
print(any(ch.isdigit() for ch in "abc1"))  # True
print(all(ch.isalpha() for ch in "ab1"))   # False
```

# --instructions--

编写一个函数 `is_valid_password(password)`，仅当字符串长度在 `8` 到 `20` 个字符之间（含两端）、至少包含一个数字且不包含空格时返回 `True`，否则返回 `False`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`"secret123"` 是有效的，恰好 `8` 个或 `20` 个字符的密码也是有效的

```python
    def test_1(self):
        self.assertIs(is_valid_password("secret123"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefg1"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefghij123456789x"), True, "--err-t1--")
```

太短或太长的密码是无效的

```python
    def test_2(self):
        self.assertIs(is_valid_password("short1"), False, "--err-t2--")
        self.assertIs(is_valid_password("abcdefghij1234567890x"), False, "--err-t2--")
```

没有数字或含有空格的密码是无效的

```python
    def test_3(self):
        self.assertIs(is_valid_password("nodigitshere"), False, "--err-t3--")
        self.assertIs(is_valid_password("pass word1"), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_valid_password(password):
    return (
        8 <= len(password) <= 20
        and any(ch.isdigit() for ch in password)
        and " " not in password
    )
```
