---
language: python
exerciseType: 2
---

# --description--

你可以像切片列表一样切片字符串！实际上，你可以把字符串看作字符的列表：每个字符是列表中从索引 `0` 开始的连续元素。
```python
list_name[:2]
# Grabs the first two items
list_name[3:]
# Grabs the fourth through last items
```
如果你的列表切片包含列表（或字符串）中的第一个或最后一个元素，则不需要包含该元素的索引。

# --instructions--

- 将 `full_name` 从索引 `0` 到索引 `4`（*不包含*索引 `4`）的切片赋值给 `first_name`。
- 将 `full_name` 从索引 `4` 到字符串末尾的切片赋值给 `last_name`

# --seed--

```python
full_name = "Sam Smith"
first_name = [/][/]
last_name = [/][/]
print(first_name)
print(last_name)
```

# --answers--

- full_name
- [:4]
- [:3]
- full_name
- [4:]
- [4:2]

# --solutions--

```python
full_name = "Sam Smith"
first_name = full_name[:3]
last_name = full_name[4:]
print(first_name)
print(last_name)
```

# --output--

Sam
Smith
