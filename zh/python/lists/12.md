---
language: python
exerciseType: 2
---

# --description--

有时我们可能想将元组转换为列表。
为此，我们可以使用 `list()` 函数

# --instructions--

使用 `list()` 函数将元组转换为列表

# --seed--

```python
tuple1 = ("dog", "bear", "cat")
list1 = [/]
list1.append("frog")
print(list1)
```

# --answers--

- list[tuple1]
- list(tuple1)

# --solutions--

```python
tuple1 = ("dog", "bear", "cat")
list1 = list(tuple1)
list1.append("frog")
print(list1)
```

# --output--

['dog', 'bear', 'cat', 'frog']
