---
language: python
exerciseType: 1
---

# --description--

异常是一个对象，`as` 把它绑定到一个名字上，让处理程序可以查看它：
```python
try:
    int("abc")
except ValueError as e:
    print(e)                   # invalid literal for int() with base 10: 'abc'
    print(type(e).__name__)    # ValueError
```
`str(e)`——`print(e)` 和 f-string 插槽使用的正是它——给出构建异常时所用的消息，而 `type(e).__name__` 以文本形式给出类名。由 `as` 绑定的名字只存在于 `except` 块内部；块结束时 Python 会删除它。

# --instructions--

编写一个函数 `error_label(text)`，它返回 `int(text)`；当引发 `ValueError` 时，返回一个由异常类名、`": "` 和异常消息组成的字符串，例如 `"ValueError: ..."`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`error_label("8")` 必须返回 `8`

```python
    def test_1(self):
        self.assertEqual(error_label("8"), 8, "--err-t1--")
```

`error_label("abc")` 必须以类名开头

```python
    def test_2(self):
        self.assertTrue(error_label("abc").startswith("ValueError: "), "--err-t2--")
```

`error_label("abc")` 必须还包含提到 `abc` 的消息

```python
    def test_3(self):
        self.assertIn("abc", error_label("abc")[12:], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def error_label(text):
    try:
        return int(text)
    except ValueError as e:
        return f"{type(e).__name__}: {e}"
```
