---
language: python
exerciseType: 1
---

# --description--

当多个失败需要**相同的**恢复方式时，在一个子句中用元组把它们列出来，比重复整个块更简洁：
```python
try:
    value = int(text) / divisor
except (ValueError, ZeroDivisionError):
    value = 0
```
括号是必需的：`except ValueError, ZeroDivisionError:` 在 Python 3 中是语法错误。元组仍然是一份显式的类型列表。

# --instructions--

编写一个函数 `safe_ratio(a, b)`，它返回 `a / b`；当除法引发 `ZeroDivisionError` 或 `TypeError` 时返回 `None`，并使用一个带有这两种类型元组的单个 `except` 子句

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_ratio(9, 3)` 必须返回 `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_ratio(9, 3), 3.0, "--err-t1--")
```

`safe_ratio(1, 0)` 必须返回 `None`

```python
    def test_2(self):
        self.assertIsNone(safe_ratio(1, 0), "--err-t2--")
```

`safe_ratio(1, "x")` 必须返回 `None`

```python
    def test_3(self):
        self.assertIsNone(safe_ratio(1, "x"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_ratio(a, b):
    try:
        return a / b
    except (ZeroDivisionError, TypeError):
        return None
```
