---
language: python
exerciseType: 1
---

# --description--

`try` 块会在**第一条**引发异常的语句处停止；它后面的行会被跳过，控制权转移到 `except` 块。`try` 块中已执行的内容不会被撤销，所以要尽量让它保持简短：
```python
def half(n):
    try:
        return 10 / n
    except ZeroDivisionError:
        return "undefined"

print(half(2))  # 5.0
print(half(0))  # undefined
```
`except` 中的 `return` 和其他任何 `return` 一样，这让 `try`/`except` 成为返回一个回退值而不是让程序崩溃的自然方式。

# --instructions--

编写一个函数 `safe_divide(a, b)`，它返回 `a / b`；当 `b` 为 `0` 时捕获 `ZeroDivisionError` 并返回字符串 `"undefined"`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_divide(9, 3)` 必须返回 `3.0`

```python
    def test_1(self):
        self.assertEqual(safe_divide(9, 3), 3.0, "--err-t1--")
```

`safe_divide(1, 0)` 必须返回 `"undefined"`

```python
    def test_2(self):
        self.assertEqual(safe_divide(1, 0), "undefined", "--err-t2--")
```

`safe_divide(-5, 2)` 必须返回 `-2.5`

```python
    def test_3(self):
        self.assertEqual(safe_divide(-5, 2), -2.5, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "undefined"
```
