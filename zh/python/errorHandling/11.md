---
language: python
exerciseType: 1
---

# --description--

`finally` 块**无论如何都会运行**：在没有异常的 `try` 块之后、在 `except` 块之后，即使有一个无人捕获的异常正在向外传播，甚至当 `try` 或 `except` 块执行了 `return` 时，它也会运行：
```python
def read(text):
    try:
        return int(text)
    except ValueError:
        return 0
    finally:
        print("done")
```
两条路径都会在值离开函数之前打印 `done`。这个保证正是 `finally` 的用途：关闭文件、释放锁、恢复设置。完整的形态是 `try` / `except` / `else` / `finally`；`try` 至少需要一个 `except` 或一个 `finally`，而 `else` 只能与 `except` 搭配使用。

# --instructions--

编写一个函数 `record(text)`：从一个空列表开始，把 `int(text)` 追加到其中；当 `int(text)` 引发 `ValueError` 时改为追加字符串 `"bad"`；并在一个 `finally` 块中总是追加 `"done"`，最后返回该列表

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`record("5")` 必须返回 `[5, "done"]`

```python
    def test_1(self):
        self.assertEqual(record("5"), [5, "done"], "--err-t1--")
```

`record("x")` 必须返回 `["bad", "done"]`

```python
    def test_2(self):
        self.assertEqual(record("x"), ["bad", "done"], "--err-t2--")
```

每次调用都必须从一个全新的列表开始

```python
    def test_3(self):
        record("1")
        self.assertEqual(record("2"), [2, "done"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def record(text):
    log = []
    try:
        log.append(int(text))
    except ValueError:
        log.append("bad")
    finally:
        log.append("done")
    return log
```
