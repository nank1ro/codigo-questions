---
language: python
exerciseType: 1
---

# --description--

当没有合适的内置类型时，可以通过继承 `Exception` 来定义自己的异常。空的类体通常就足够了——类名本身就是传达给阅读者的信息：
```python
class ConfigError(Exception):
    pass
```
它的行为和其他任何异常一样：使用 `raise ConfigError("bad port")` 引发它，用 `except ConfigError:` 捕获它。

把低层级的失败转换成自己的类型是很常见的做法，而原始错误不应在这个过程中丢失。`raise NewError(...) from original` 会把它们**链接**起来：它把 `original` 存入新异常的 `__cause__` 属性，回溯信息会在 *The above exception was the direct cause of the following exception* 标题下同时显示两者：
```python
try:
    port = int(text)
except ValueError as e:
    raise ConfigError("bad port") from e
```
如果没有 `from e`，两者仍会隐式地关联，但 `from` 明确说出了第一个错误导致了第二个错误。

# --instructions--

定义一个继承 `Exception` 的异常类 `ConfigError`，并编写一个函数 `read_port(text)`，它返回 `int(text)`；当这引发 `ValueError` 时，用 `raise ... from` 引发由它链接而来的 `ConfigError("bad port")`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`read_port("8080")` 必须返回 `8080`，且 `ConfigError` 必须是 `Exception` 的子类

```python
    def test_1(self):
        self.assertEqual(read_port("8080"), 8080, "--err-t1--")
        self.assertTrue(issubclass(ConfigError, Exception), "--err-t1--")
```

`read_port("abc")` 必须引发一个消息为 `"bad port"` 的 `ConfigError`

```python
    def test_2(self):
        with self.assertRaises(ConfigError, msg="--err-t2--") as ctx:
            read_port("abc")
        self.assertEqual(str(ctx.exception), "bad port", "--err-t2--")
```

被引发的 `ConfigError` 必须链接自原始的 `ValueError`

```python
    def test_3(self):
        with self.assertRaises(ConfigError, msg="--err-t3--") as ctx:
            read_port("abc")
        self.assertIsInstance(ctx.exception.__cause__, ValueError, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
class ConfigError(Exception):
    pass

def read_port(text):
    try:
        return int(text)
    except ValueError as e:
        raise ConfigError("bad port") from e
```
