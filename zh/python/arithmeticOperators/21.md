---
language: python
exerciseType: 1
---

# --instructions--

你的 `store`（商店）里有 `60` 件商品，白天你卖出了 `50` 件，又购入了 `55` 件新商品。
更新 `store` 的数量。

# --seed--

```python
store = 60
sold = 50
bought = 55
# write after this line
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`store` 的数量应等于 65

```python
    def test_1(self):
        self.assertEqual(store, 65, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
store = 60
sold = 50
bought = 55
store += bought - sold
```
