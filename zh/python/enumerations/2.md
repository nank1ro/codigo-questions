---
language: python
exerciseType: 1
---

# --description--

枚举的每个成员都有两个属性：`name`，即你在类中写的标识符；`value`，即你赋给它的值：
```python
from enum import Enum

class Color(Enum):
    RED = 1
    GREEN = 2

print(Color.GREEN.name)   # GREEN
print(Color.GREEN.value)  # 2
```
值可以是任何类型，而不仅限于整数：字符串、元组和浮点数都是常见的选择。
成员是一个普通对象，因此你可以把它存储在变量中，之后再读取它的属性：
```python
chosen = Color.RED
print(chosen.value)  # 1
```

# --instructions--

将成员 `Planet.EARTH` 存储到 `home` 中，将其名称存储到 `label` 中，将其值存储到 `position` 中

# --seed--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `.name`

```json
{
  "regex": "\\.name\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

必须使用 `.value`

```json
{
  "regex": "\\.value\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`home` 必须是 `Planet.EARTH`

```python
    def test_1(self):
        self.assertIs(home, Planet.EARTH, "--err-t1--")
```

`label` 必须等于 `"EARTH"`

```python
    def test_2(self):
        self.assertEqual(label, "EARTH", "--err-t2--")
```

`position` 必须等于 `3`

```python
    def test_3(self):
        self.assertEqual(position, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3

home = Planet.EARTH
label = home.name
position = home.value
```
