---
language: python
exerciseType: 1
---

# --description--

`Flag` 是一种成员可以**组合**的枚举：一个值可以同时包含多个成员，就像一组选项。
用 `auto()` 声明它的成员，对于 `Flag` 来说它会分配 2 的幂（`1`、`2`、`4` 等），因此每种组合都有一个独特的值：
```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```
使用 `|` 组合成员，使用 `in` 检查某个成员是否是组合的一部分，使用 `value` 查看得到的数字：
```python
full = Permission.READ | Permission.WRITE | Permission.EXECUTE
print(Permission.WRITE in full)  # True
print(full.value)                # 7
print(full)                      # Permission.READ|WRITE|EXECUTE
```

# --instructions--

使用 `|` 将 `Permission.READ` 和 `Permission.WRITE` 的组合存储到 `editor` 中，将 `Permission.WRITE` 是否在 `editor` 中存储到 `can_write` 中，并将 `editor` 的值存储到 `bits` 中

# --seed--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `|` 运算符

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

必须使用 `in` 运算符

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`editor` 必须等于 `Permission.READ | Permission.WRITE`

```python
    def test_1(self):
        self.assertEqual(editor, Permission.READ | Permission.WRITE, "--err-t1--")
```

`can_write` 必须是 `True`

```python
    def test_2(self):
        self.assertEqual(can_write, True, "--err-t2--")
```

`bits` 必须等于 `3`

```python
    def test_3(self):
        self.assertEqual(bits, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()

editor = Permission.READ | Permission.WRITE
can_write = Permission.WRITE in editor
bits = editor.value
```
