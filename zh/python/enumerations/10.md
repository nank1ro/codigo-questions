---
language: python
exerciseType: 2
---

# --description--

如果两个成员具有相同的值，第二个成员就不是新成员，而是第一个成员的**别名**：
```python
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # alias of ACTIVE

print(Status.ENABLED)  # Status.ACTIVE
print(len(Status))     # 1
```
迭代时会跳过别名，`len()` 也不会把它们计算在内。
通常重复的值是一个错误。从 `enum` 导入的 `unique` 装饰器让 Python 在声明带别名的枚举时立即抛出 `ValueError`：
```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # ValueError: duplicate values found
```

# --instructions--

补全代码以导入 `unique` 装饰器并将其应用到 `Status` 上，从而拒绝重复的值，然后打印成员的数量

# --seed--

```python
from enum import Enum, [/]

[/]
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --answers--

- unique
- @unique
- auto
- @auto
- @property
- @classmethod

# --solutions--

```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --output--

2
