---
language: python
exerciseType: 1
---

# --description--

除了普通方法之外，枚举还可以用 `@classmethod` 定义**类方法**。它们接收枚举类本身作为 `cls`，因此是把查找成员的其他方式放在这里的合适位置：
```python
class Color(Enum):
    RED = auto()
    BLUE = auto()

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]

print(Color.parse(" blue "))  # Color.BLUE
```
结合 `auto()`、方法、属性和查找，这让你能够构建自带行为的枚举。

# --instructions--

使用 `auto()` 按此顺序声明一个带有成员 `NORTH`、`EAST`、`SOUTH` 和 `WEST` 的枚举 `Direction`。
添加一个方法 `opposite()`，返回相反一侧的成员（`NORTH` 与 `SOUTH`，`EAST` 与 `WEST`）；再添加一个类方法 `parse(text)`，返回名称与 `text` 匹配的成员，忽略大小写和两侧空格

# --seed--

```python
from enum import Enum, auto
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `auto()`

```json
{
  "regex": "auto\\(\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

必须使用 `@classmethod` 装饰器

```json
{
  "regex": "@classmethod",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Direction` 必须拥有成员 `NORTH`、`EAST`、`SOUTH` 和 `WEST`，值从 `1` 到 `4`

```python
    def test_1(self):
        self.assertEqual([d.name for d in Direction], ["NORTH", "EAST", "SOUTH", "WEST"], "--err-t1--")
        self.assertEqual([d.value for d in Direction], [1, 2, 3, 4], "--err-t1--")
```

`Direction.NORTH.opposite()` 必须是 `Direction.SOUTH`，且 `Direction.WEST.opposite()` 必须是 `Direction.EAST`

```python
    def test_2(self):
        self.assertIs(Direction.NORTH.opposite(), Direction.SOUTH, "--err-t2--")
        self.assertIs(Direction.SOUTH.opposite(), Direction.NORTH, "--err-t2--")
        self.assertIs(Direction.EAST.opposite(), Direction.WEST, "--err-t2--")
        self.assertIs(Direction.WEST.opposite(), Direction.EAST, "--err-t2--")
```

`Direction.parse(" west ")` 必须是 `Direction.WEST`

```python
    def test_3(self):
        self.assertIs(Direction.parse(" west "), Direction.WEST, "--err-t3--")
        self.assertIs(Direction.parse("North"), Direction.NORTH, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    EAST = auto()
    SOUTH = auto()
    WEST = auto()

    def opposite(self):
        opposites = {
            Direction.NORTH: Direction.SOUTH,
            Direction.SOUTH: Direction.NORTH,
            Direction.EAST: Direction.WEST,
            Direction.WEST: Direction.EAST,
        }
        return opposites[self]

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]
```
