---
language: python
exerciseType: 1
---

# --description--

你可以像调用函数一样调用类，从**值**出发获取成员；或者使用方括号，从**名称**出发获取成员：
```python
print(Color(2))        # Color.GREEN
print(Color["BLUE"])   # Color.BLUE
```
当值或名称来自程序外部（例如文件或用户输入）时，这两种方式都很方便。
如果没有任何匹配项，`Color(9)` 会抛出 `ValueError`，`Color["PINK"]` 会抛出 `KeyError`。

# --instructions--

将 `Status` 中值为 `code` 的成员存储到 `by_value` 中（调用 `Status(...)`），并将名称为 `label` 的成员存储到 `by_name` 中（使用 `Status[...]`）

# --seed--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须用 `code` 调用 `Status(...)`

```json
{
  "regex": "Status\\(\\s*code\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

必须对 `label` 使用 `Status[...]`

```json
{
  "regex": "Status\\[\\s*label\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`by_value` 必须是 `Status.DONE`

```python
    def test_1(self):
        self.assertIs(by_value, Status.DONE, "--err-t1--")
```

`by_name` 必须是 `Status.FAILED`

```python
    def test_2(self):
        self.assertIs(by_name, Status.FAILED, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
by_value = Status(code)
by_name = Status[label]
```
