---
language: python
exerciseType: 4
---

# --description--

处理失败的协程遵循同步的模式：协程抛出异常，调用者把 `await` 包在 `try`/`except` 中，并决定如何处理这个错误对象，例如用 `print(e)` 打印它的消息。

# --instructions--

排列各项以构建一个程序：以导入开始，在 `main` 之前定义 `load`，打印加载 `"config"` 的结果，然后捕获加载空名称时抛出的错误并打印它的消息，产生输出 `loaded config`、`empty name`

# --answers--

```python
import asyncio
```

```python
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
```

```python
async def main():
    print(await load("config"))
```

```python
    try:
        await load("")
```

```python
    except ValueError as e:
        print(e)
```

```python
asyncio.run(main())
```

# --solutions--

```python
import asyncio
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
async def main():
    print(await load("config"))
    try:
        await load("")
    except ValueError as e:
        print(e)
asyncio.run(main())
```

# --output--

loaded config
empty name
