---
language: python
exerciseType: 1
---

# --description--

`asyncio.create_task(coro)` 把一个协程包装成一个 **Task**，并调度它在后台运行。与 `await` 不同，它立即返回，所以当前协程可以在任务运行期间继续工作。任务实际上在当前协程下一次在 `await` 处暂停时才启动。之后再 await 该任务就能得到它的结果：
```python
import asyncio

async def background():
    print("task started")
    await asyncio.sleep(0.01)
    return "task done"

async def main():
    task = asyncio.create_task(background())
    print("main continues")
    print(await task)

asyncio.run(main())
```
它先打印 `main continues`，因为 `background` 只有在 `main` await 时才启动，然后打印 `task started` 和 `task done`。

# --instructions--

编写一个协程函数 `background(log)`，它把 `"start"` 追加到列表 `log`，等待 `0.01` 秒，然后追加 `"done"`；再编写一个协程函数 `main(log)`，它用 `asyncio.create_task` 调度 `background(log)`，把 `"main"` 追加到 `log`，然后 await 该任务并返回 `log`

# --before-asserts--

```python
import asyncio
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`background([])` 必须返回 `None`，并让日志保持为 `["start", "done"]`

```python
    def test_1(self):
        log = []
        self.assertIsNone(asyncio.run(background(log)), "--err-t1--")
        self.assertEqual(log, ["start", "done"], "--err-t1--")
```

`main([])` 必须返回 `["main", "start", "done"]`，因为任务只在 `main` await 时才启动

```python
    def test_2(self):
        self.assertEqual(asyncio.run(main([])), ["main", "start", "done"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def background(log):
    log.append("start")
    await asyncio.sleep(0.01)
    log.append("done")

async def main(log):
    task = asyncio.create_task(background(log))
    log.append("main")
    await task
    return log
```
