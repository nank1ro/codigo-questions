---
language: python
exerciseType: 2
---

# --description--

`asyncio.wait_for(awaitable, timeout)` 等待某个东西，但在 `timeout` 秒后放弃：操作被取消，并抛出一个 `TimeoutError`，它可以像任何异常一样被捕获：
```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())  # timed out
```
如果超时为 `0.1`，同样的代码会打印 `done`。`asyncio.TimeoutError` 是内置 `TimeoutError` 的另一个名字。

# --instructions--

补全代码，使 `slow()` 以 `0.01` 秒的超时被 await，并且超时被捕获，产生输出 `timed out`

# --seed--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.[/](slow(), [/]=0.01)
        print(result)
    except [/]:
        print("timed out")

asyncio.run(main())
```

# --answers--

- wait_for
- timeout
- TimeoutError
- gather
- delay
- ValueError

# --solutions--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())
```

# --output--

timed out
