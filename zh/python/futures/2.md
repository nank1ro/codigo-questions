---
language: python
exerciseType: 1
---

# --description--

协程可以像普通函数一样接收参数并 `return` 一个值。这个值在协程对象被创建时还不可用，只有协程运行之后才能得到。`asyncio.run` 会返回协程返回的值：
```python
import asyncio

async def double(n):
    return n * 2

result = asyncio.run(double(21))
print(result)  # 42
```
你关于函数所知的一切在 `async def` 内部依然适用：局部变量、条件、循环和多个 `return` 语句。

# --instructions--

用 `async def` 编写一个协程函数 `add(a, b)`，它返回 `a` 与 `b` 的和

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`add` 必须是一个协程函数

```python
    def test_1(self):
        self.assertTrue(inspect.iscoroutinefunction(add), "--err-t1--")
```

运行 `add(1, 3)` 必须返回 `4`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(add(1, 3)), 4, "--err-t2--")
```

运行 `add(200, 210)` 必须返回 `410`

```python
    def test_3(self):
        self.assertEqual(asyncio.run(add(200, 210)), 410, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
async def add(a, b):
    return a + b
```
