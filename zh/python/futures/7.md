---
language: python
exerciseType: 1
---

# --description--

当操作的数量不固定时，并发就派上了用场。用列表推导构建协程对象，把它们解包传给 `gather`，然后一次性 await 整批协程：
```python
import asyncio

async def fetch(url):
    await asyncio.sleep(0.02)
    return len(url)

async def fetch_all(urls):
    return await asyncio.gather(*[fetch(u) for u in urls])

print(asyncio.run(fetch_all(["a.com", "bb.com"])))  # [5, 6]
```
十个 url 总共仍只需约 20 毫秒而不是 200 毫秒，并且结果保持与 url 相同的顺序。

# --instructions--

编写一个协程函数 `fetch(item_id)`，它等待 `0.02` 秒并返回 `item_id * 10`；再编写一个协程函数 `fetch_all(ids)`，它用 `asyncio.gather` 为每个 id **并发**运行 `fetch`，并返回结果列表

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch(3)` 必须返回 `30`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch(3)), 30, "--err-t1--")
```

`fetch_all([1, 2, 3])` 必须返回 `[10, 20, 30]`，`fetch_all([])` 必须返回 `[]`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(fetch_all([1, 2, 3])), [10, 20, 30], "--err-t2--")
        self.assertEqual(asyncio.run(fetch_all([])), [], "--err-t2--")
```

带五个 id 的 `fetch_all` 必须并发运行它们，耗时远低于 `0.1` 秒

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(fetch_all([1, 2, 3, 4, 5]))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [10, 20, 30, 40, 50], "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch(item_id):
    await asyncio.sleep(0.02)
    return item_id * 10

async def fetch_all(ids):
    return await asyncio.gather(*[fetch(i) for i in ids])
```
