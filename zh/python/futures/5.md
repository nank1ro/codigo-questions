---
language: python
exerciseType: 1
---

# --description--

协程之间用 `await` 相互调用。一个协程可以 await 任何其他协程，接收它的返回值并继续执行，就像一条函数调用链：
```python
import asyncio

async def fetch_price(item):
    await asyncio.sleep(0.01)  # simulates a slow lookup
    return 10

async def total(item, quantity):
    price = await fetch_price(item)
    return price * quantity

print(asyncio.run(total("pen", 3)))  # 30
```
只有最外层的协程通过 `asyncio.run` 运行；内部的每一个协程都用 `await` 到达。如果 `total` 是普通的 `def`，它就完全不能使用 `await`：`async` 关键字会蔓延到链上每一个需要等待的函数。

# --instructions--

编写一个协程函数 `fetch_user(user_id)`，它等待 `0.01` 秒并返回后面跟着 id 的字符串 `"user"`；再编写一个协程函数 `greet_user(user_id)`，它 await `fetch_user` 并返回后面跟着所获取名称的 `"Hello, "`

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch_user(1)` 必须返回 `"user1"`，`fetch_user(42)` 必须返回 `"user42"`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch_user(1)), "user1", "--err-t1--")
        self.assertEqual(asyncio.run(fetch_user(42)), "user42", "--err-t1--")
```

`greet_user(1)` 必须返回 `"Hello, user1"`

```python
    def test_2(self):
        self.assertEqual(asyncio.run(greet_user(1)), "Hello, user1", "--err-t2--")
```

`fetch_user` 和 `greet_user` 都必须是协程函数

```python
    def test_3(self):
        self.assertTrue(inspect.iscoroutinefunction(fetch_user), "--err-t3--")
        self.assertTrue(inspect.iscoroutinefunction(greet_user), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch_user(user_id):
    await asyncio.sleep(0.01)
    return f"user{user_id}"

async def greet_user(user_id):
    name = await fetch_user(user_id)
    return f"Hello, {name}"
```
