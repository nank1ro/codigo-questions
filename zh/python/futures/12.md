---
language: python
exerciseType: 1
---

# --description--

在协程内部抛出的异常不会出现在协程对象被创建的地方：它在运行该协程的 `await` 处被抛出，对于最外层的协程则由 `asyncio.run` 抛出。因此 `try`/`except` 必须包裹 **`await`**：
```python
import asyncio

async def load(path):
    await asyncio.sleep(0.01)
    if path == "":
        raise FileNotFoundError("empty path")
    return "contents"

async def safe_load(path):
    try:
        return await load(path)
    except FileNotFoundError:
        return ""

print(asyncio.run(safe_load("")))  # prints an empty line
```
没有人捕获的异常会穿过每一个 `await` 一路传播到 `asyncio.run`，后者在同步代码中把它重新抛出，与普通的调用栈完全一样。

# --instructions--

编写一个协程函数 `divide(a, b)`，它等待 `0.01` 秒并返回 `a / b`；再编写一个协程函数 `safe_divide(a, b)`，它 await `divide(a, b)` 并返回其结果，如果抛出 `ZeroDivisionError` 则返回 `None`

# --before-asserts--

```python
import asyncio
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`divide(10, 4)` 必须返回 `2.5`

```python
    def test_1(self):
        self.assertEqual(asyncio.run(divide(10, 4)), 2.5, "--err-t1--")
```

运行 `divide(1, 0)` 必须抛出 `ZeroDivisionError`

```python
    def test_2(self):
        with self.assertRaises(ZeroDivisionError, msg="--err-t2--"):
            asyncio.run(divide(1, 0))
```

`safe_divide(10, 2)` 必须返回 `5.0`，`safe_divide(1, 0)` 必须返回 `None`

```python
    def test_3(self):
        self.assertEqual(asyncio.run(safe_divide(10, 2)), 5.0, "--err-t3--")
        self.assertIsNone(asyncio.run(safe_divide(1, 0)), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def divide(a, b):
    await asyncio.sleep(0.01)
    return a / b

async def safe_divide(a, b):
    try:
        return await divide(a, b)
    except ZeroDivisionError:
        return None
```
