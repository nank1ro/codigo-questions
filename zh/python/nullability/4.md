---
language: python
exerciseType: 2
---

# --description--

在条件中 `None` 被视为**假**，因此当 `value` 为 `None` 时 `if not value:` 为 `True`。很容易把它当作 `None` 检查来用，但同样的测试对 `0`、`""`、`[]` 以及其他所有空值也是 `True`：
```python
count = 0
if not count:
    print("missing?")   # printed, but 0 is a real value!
```
当"没有值"和"值为空"必须区别对待时，先检查 `is None`，再检查真假性：
```python
if count is None:
    print("no count yet")
elif not count:
    print("count is zero")
```
只有当你确实想把 `None` 和空值同等对待时，才使用 `if not value:`。

# --instructions--

补全该函数，使它对 `None` 返回 `missing`，对空值返回 `empty`，其余情况返回 `present`，从而产生输出 `missing`、`empty`、`present

# --seed--

```python
def describe(value):
    if value [/] None:
        return "missing"
    if [/] value:
        return "empty"
    return "present"

print(describe(None))
print(describe(0))
print(describe("hi"))
```

# --answers--

- is
- not
- is not
- in
- and

# --solutions--

```python
def describe(value):
    if value is None:
        return "missing"
    if not value:
        return "empty"
    return "present"

print(describe(None))
print(describe(0))
print(describe("hi"))
```

# --output--

missing
empty
present
