---
language: python
exerciseType: 1
---

# --description--

返回数字**或** `None` 的函数应该在签名中说明这一点。**类型提示**（type hint）是记录预期类型的注解：参数用 `name: str`，返回值用 `-> int`。Python 不强制执行类型提示，但编辑器和阅读代码的人都依赖它们：
```python
def parse_age(text: str) -> int | None:
    if text.isdigit():
        return int(text)
    return None
```
`int | None` 读作"一个 `int` 或 `None`"。来自 `typing` 模块的旧写法 `Optional[int]` 含义完全相同，你在现有代码中仍会遇到它。
每当在签名中看到 `| None` 时，记得在使用结果之前先做检查。

# --instructions--

编写一个带类型提示的函数 `parse_age`，它接受一个字符串，当字符串只包含数字时返回转换为 `int` 的结果，否则返回 `None`

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

返回类型必须提示为 `int | None`

```json
{
  "regex": "->\\s*(int\\s*\\|\\s*None|None\\s*\\|\\s*int|Optional\\[int\\])",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_age("42")` 必须返回 `42`

```python
    def test_1(self):
        self.assertEqual(parse_age("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_age("42"), int, "--err-t1--")
```

`parse_age("abc")` 必须返回 `None`

```python
    def test_2(self):
        self.assertIsNone(parse_age("abc"), "--err-t2--")
        self.assertIsNone(parse_age("4a"), "--err-t2--")
```

`parse_age("")` 必须返回 `None`

```python
    def test_3(self):
        self.assertIsNone(parse_age(""), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_age(text: str) -> int | None:
    if text.isdigit():
        return int(text)
    return None
```
