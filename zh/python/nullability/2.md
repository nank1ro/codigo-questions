---
language: python
exerciseType: 1
---

# --description--

每次函数调用都会产生一个值，即使函数看起来没有返回任何东西。**没有** `return` 语句的函数，或者只有单独 `return` 的函数，都会返回 `None`：
```python
def say_hello():
    print("hello")

result = say_hello()
print(result)  # None
```
这就是为什么调用 `print(my_list.append(3))` 会显示 `None`：`append` 原地修改列表，不返回任何东西。
只执行动作的函数（打印、保存、修改列表）通常返回 `None`，而计算某个结果的函数必须显式地 `return` 它。

# --instructions--

编写一个函数 `store`，它接受一个值并将其追加到列表 `saved` 中，不返回任何东西

# --seed--

```python
saved = []

def store(value):
    pass
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

必须使用 `append`

```json
{
  "regex": "\\.append\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`store("a")` 必须返回 `None`

```python
    def test_1(self):
        self.assertIsNone(store("a"), "--err-t1--")
```

在 `store("b")` 之后，列表 `saved` 的最后一个元素必须是 `"b"`

```python
    def test_2(self):
        store("b")
        self.assertEqual(saved[-1], "b", "--err-t2--")
```

每个存储的值都必须按顺序保留

```python
    def test_3(self):
        before = len(saved)
        store(1)
        store(2)
        self.assertEqual(saved[before:], [1, 2], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
saved = []

def store(value):
    saved.append(value)
```
