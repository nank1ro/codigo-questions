---
language: python
exerciseType: 2
---

# --description--

让我们看看**除法赋值** `/=` 运算符。
它将变量除以一个值，并将结果赋给该变量。
所以以下两种写法完全相同
```python
num = num / 5
# is equal to
num /= 5
```

# --instructions--

使用*除法赋值*运算符计算 `result` 除以 `5` 的商，然后打印其值

# --seed--

```python
result = 50
result [/][/]
print(result)
```

# --answers--

- / 
- /= 
- =/ 
- 5
- 25

# --solutions--

```python
result = 50
result /= 5
print(result)
```

# --output--

10.0
