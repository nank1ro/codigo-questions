---
language: python
exerciseType: 1
---

# --instructions--

使用左花括号 `{` 和右花括号 `}` 将数字 `500` 插入格式化字符串的空白处

# --seed--

```python
print(f"There are  items")
```

# --solutions--

```python
print(f"There are {500} items")
```

# --output--

There are 500 items
