---
language: python
exerciseType: 1
---

# --description--

当字符串是函数内部的**第一条语句**时，Python 会把它当作该函数的文档。它被称为 **docstring**：
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"
```
按照惯例，docstring 写在三重双引号 `"""` 之间，即使它只有一行也一样，这样以后内容增多时无需更换引号。

摘要要用第三人称书写，就像在描述这个函数一样："Returns..."、"Adds..."、"Checks..."。docstring 必须出现在函数体中其他任何语句之前，否则它只是一个普通字符串。

# --instructions--

编写一个函数 `double(n)`，返回 `n` 乘以二的结果，并为它编写一行 docstring。

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`double` 必须以 docstring 作为它的第一条语句。

```python
    def test_1(self):
        self.assertIsNotNone(double.__doc__, "--err-t1--")
```

`double(21)` 必须返回 `42`。

```python
    def test_2(self):
        self.assertEqual(double(21), 42, "--err-t2--")
```

`double(-3)` 必须返回 `-6`，且 `double(0)` 必须返回 `0`。

```python
    def test_3(self):
        self.assertEqual(double(-3), -6, "--err-t3--")
        self.assertEqual(double(0), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def double(n):
    """Returns n multiplied by two."""
    return n * 2
```
