---
language: python
exerciseType: 1
---

# --description--

当一行写不下时，docstring 会扩展成一种固定的布局，**PEP 257** 对此有描述：一行的摘要、一个空行，然后是详细内容，结束的 `"""` 独占一行。
```python
def to_seconds(minutes):
    """Returns the number of seconds in the given minutes.

    minutes is a whole number and is never negative.
    """
    return minutes * 60
```
这个空行很重要：工具会单独显示第一行作为简短描述，其余内容留给想深入了解的人阅读。

# --instructions--

为 `area` 编写一个 docstring：一行的摘要、一个空行、一行的详细说明，以及独占一行的结束 `"""`。

# --seed--

```python
def area(width, height):
    return width * height
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`area` 必须拥有一个 docstring。

```python
    def test_1(self):
        self.assertIsNotNone(area.__doc__, "--err-t1--")
```

docstring 必须以一行摘要开头，其后是一个空行和更多文本。

```python
    def test_2(self):
        lines = area.__doc__.split("\n")
        self.assertTrue(len(lines) >= 3, "--err-t2--")
        self.assertNotEqual(lines[0].strip(), "", "--err-t2--")
        self.assertEqual(lines[1].strip(), "", "--err-t2--")
        self.assertNotEqual(lines[2].strip(), "", "--err-t2--")
        self.assertEqual(lines[-1].strip(), "", "--err-t2--")
```

`area(3, 4)` 必须返回 `12`，且 `area(0, 5)` 必须返回 `0`。

```python
    def test_3(self):
        self.assertEqual(area(3, 4), 12, "--err-t3--")
        self.assertEqual(area(0, 5), 0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def area(width, height):
    """Returns the area of a rectangle.

    Both sides are whole numbers of centimetres.
    """
    return width * height
```
