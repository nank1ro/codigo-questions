---
language: python
exerciseType: 1
---

# --description--

有些注释遵循编辑器能够识别的约定。最常见的**标记**有：
- `# TODO: ...` 标记还有待编写的内容
- `# FIXME: ...` 标记已知有错、必须修正的代码

```python
limit = 10
# TODO: read the limit from the settings
```
对 Python 来说它们只是普通注释；编辑器会把它们收集到一个专门的面板中，因此待办的工作很容易找到。`TODO` 通常与一个占位实现放在一起，在真正的代码写出来之前，占位实现能让程序继续运行。

完成工作后，请在同一次修改中替换占位实现并删除标记，这样注释就永远不会与代码的实际状态不符。

# --instructions--

完成 `is_even` 中 `TODO` 注释所描述的工作，然后移除该注释，因为工作已经完成。

# --seed--

```python
def is_even(n):
    # TODO: return True when n is even, False otherwise
    return False
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`TODO` 注释必须被移除。

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`is_even(4)` 必须返回 `True`。

```python
    def test_1(self):
        self.assertEqual(is_even(4), True, "--err-t1--")
```

`is_even(7)` 必须返回 `False`。

```python
    def test_2(self):
        self.assertEqual(is_even(7), False, "--err-t2--")
```

`is_even(0)` 必须返回 `True`。

```python
    def test_3(self):
        self.assertEqual(is_even(0), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_even(n):
    return n % 2 == 0
```
