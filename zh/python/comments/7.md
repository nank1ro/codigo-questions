---
language: python
exerciseType: 2
---

# --description--

docstring 并不会被丢弃：Python 会把它存储在函数的 `__doc__` 属性中，因此程序可以在运行时读取自己的文档：
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"

print(greet.__doc__)  # Returns the greeting for name.
```
当函数没有 docstring 时，`__doc__` 为 `None`。`help(greet)` 打印的就是它，鼠标悬停在函数名上时编辑器显示的也是它。

# --instructions--

补全代码，使 `greet` 获得一个 docstring，并让程序把它打印出来

# --seed--

```python
def greet(name):
    [/]Returns a greeting for name.[/]
    return "Hi, " + name + "!"

print(greet.[/])
```

# --answers--

- """
- """
- __doc__
- __name__
- #

# --solutions--

```python
def greet(name):
    """Returns a greeting for name."""
    return "Hi, " + name + "!"

print(greet.__doc__)
```

# --output--

Returns a greeting for name.
