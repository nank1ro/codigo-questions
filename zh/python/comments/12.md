---
language: python
exerciseType: 4
---

# --description--

docstring 必须是**函数体的第一行**，位于其他所有语句之上。写在 `return` 之后或函数体其他任何位置的字符串只是一个普通字符串：`__doc__` 保持为 `None`，任何工具都不会显示它。

# --instructions--

对条目进行排序，构建一个程序：它定义 `to_seconds`，其 docstring 位于函数体的第一行，然后打印 `to_seconds(2)`

# --answers--

```python
def to_seconds(minutes):
```

```python
    """Returns the number of seconds in the given minutes."""
```

```python
    return minutes * 60
```

```python
print(to_seconds(2))
```

# --solutions--

```python
def to_seconds(minutes):
    """Returns the number of seconds in the given minutes."""
    return minutes * 60
print(to_seconds(2))
```

# --output--

120
