---
language: python
exerciseType: 2
---

# --description--

冒号是 `if` 这一行唯一不可或缺的部分：它结束条件，并开启代码块。在 Python 中，条件外的括号是可选的，所以 `if True:` 和 `if (True):` 的行为完全相同。

# --instructions--

填写空白处以执行代码块

# --seed--

```python
[/][/][/][/][/]
    print("Hello User!")
```

# --answers--

- True
- False
- "True"
- "False"
- :
- ;
- if 
- else 
- elif 
- (
- )

# --solutions--

```python
if (True):
    print("Hello User!")
```

# --output--

Hello User!
