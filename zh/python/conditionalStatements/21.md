---
language: python
exerciseType: 2
---

# --description--

当条件是一个变量时，`if` 读取的是该变量此刻保存的值。只需修改上面的赋值语句，就能关闭这个代码块，完全不用改动 `if` 这一行。

# --instructions--

填写空白处以**不**执行代码块

# --seed--

```python
online = [/]
if (online):
    print("Hello User!")
```

# --answers--

- True
- False
- true
- false
- "True"
- "False"

# --solutions--

```python
online = False
if (online):
    print("Hello User!")
```
