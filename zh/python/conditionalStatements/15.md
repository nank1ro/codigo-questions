---
language: python
exerciseType: 2
---

# --description--

`if` 下面的代码块不能为空：如果冒号后面没有缩进的代码行，Python 会抛出 `IndentationError`。当暂时没有内容可执行时，`pass` 是常用的占位语句。

# --instructions--

填写空白处以执行代码块

# --seed--

```python
[/][/]
    [/]
```

# --answers--

- if (True)
- if (False)
- if ("True")
- if ("False")
- if
- else
- elif
- :
- ;
- print("Hello!")

# --solutions--

```python
if (True):
    print("Hello!")
```

# --output--

Hello!
