---
language: python
exerciseType: 2
---

# --description--

`not` 会构建一个新的布尔值，而不是修改它所读取的那个，所以在执行 `is_afternoon = not is_morning` 之后，变量 `is_morning` 仍然保留着原来的值。

# --instructions--

填写空白处以执行代码块

# --seed--

```python
is_morning = True
is_afternoon = not is_morning
if ([/]):
    print("Good morning!")
```

# --answers--

- is_morning
- is_afternoon
- "is_morning"
- "is_afternoon"

# --solutions--

```python
is_morning = True
is_afternoon = not is_morning
if (is_morning):
    print("Good morning!")
```

# --output--

Good morning!
