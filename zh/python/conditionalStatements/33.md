---
language: python
exerciseType: 2
---

# --description--

加了引号的 `"False"` 是一个字符串，而不是布尔值，非空字符串总是被判定为真。只有不加引号的 `False` 才能阻止代码块执行。
```python
print(bool("False"))  # True
```

# --instructions--

填写空白处以**不**执行代码块

# --seed--

```python
if ([/]):
    print("Hello!")
```

# --answers--

- True
- False
- "True"
- "False"

# --solutions--

```python
if (False):
    print("Hello!")
```
