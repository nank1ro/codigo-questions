---
language: python
exerciseType: 2
---

# --description--

每个条件语句都需要用 `if` 关键字引出。正是它告诉 Python：只有当条件成立时，下面的代码块才会执行。

# --instructions--

填写空白处以创建一个条件语句

# --seed--

```python
[/] (True):
    print("It's True!")
```

# --answers--

- if
- else
- elif
- elseif

# --solutions--

```python
if (True):
    print("It's True!")
```

# --output--

It's True!
