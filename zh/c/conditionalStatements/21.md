---
language: c
exerciseType: 2
---

# --description--

`bool` 变量之所以能作为条件，是因为 `if` 只看它此刻所保存的值。当 `online` 中存的是 `false` 时，`if (online)` 的行为就和 `if (false)` 完全一样。

# --instructions--

填写空白处以使代码块**不**被执行

# --seed--

```c
bool online = [/];
if ([/]) {
    printf("Hello User!\n");
}
```

# --answers--

- online
- "online"
- Online
- false
- true

# --solutions--

```c
bool online = false;
if (online) {
    printf("Hello User!\n");
}
```
