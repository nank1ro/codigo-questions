---
language: c
exerciseType: 3
---

# --description--

条件就是 `if` 语句所检查的值：当条件为 `true` 时代码块会执行，为 `false` 时则会被跳过。

# --instructions--

决定代码块是否执行的值是什么？

# --answers--

- 条件
- 字符串
- 整数

# --solutions--

- 条件

