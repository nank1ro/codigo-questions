---
language: c
exerciseType: 2
---

# --description--

C 语言中的 `if` 语句由三部分组成：`if` 关键字、括号中的条件，以及花括号中的代码块。括号是必需的——编译器正是靠它来判断条件在哪里结束。

# --instructions--

填写空白处以执行代码块

# --seed--

```c
[/] ([/]) {
    printf("Run Code\n");
}
```

# --answers--

- True
- False
- true
- false
- "True"
- "False"
- if
- else
- else if

# --solutions--

```c
if (true) {
    printf("Run Code\n");
}
```

# --output--

Run Code
