---
language: c
exerciseType: 2
---

# --description--

现在来实践一下 `if` 语句的语法：关键字、括号中的条件，以及用花括号包裹的代码块。
```c
if (condition) {
    // 条件为 true 时执行
}
```

# --instructions--

填写空白处以创建一个条件语句

# --seed--

```c
[/] (true) {
    printf("It's true!\n");
}
```

# --answers--

- if
- else
- else if
- elseif

# --solutions--

```c
if (true) {
    printf("It's true!\n");
}
```

# --output--

It's true!
