---
language: c
exerciseType: 2
---

# --description--

C 语言中 `if` 语句各部分之间的空格是自由的：对编译器来说，`if(true){` 和 `if (true) {` 是同一条语句，重要的只是各部分的先后顺序。

# --instructions--

填写空白处以执行代码块

# --seed--

```c
[/][/] {
    printf("I'm ready!\n");
}
```

# --answers--

- (true)
- (false)
- ("true")
- ("false")
- if 
- else 
- else if 
- :
- ;

# --solutions--

```c
if (true) {
    printf("I'm ready!\n");
}
```

# --output--

I'm ready!
