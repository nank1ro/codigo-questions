---
language: c
exerciseType: 2
---

# --description--

布尔变量可以直接作为条件使用，不需要额外比较。既然 `online` 已经保存了 `true`，单独写 `if (online)` 就足以执行代码块。

# --instructions--

填写空白处以执行代码块

# --seed--

```c
bool online = true;
if ([/]) {
    printf("Hello User!\n");
}
```

# --answers--

- online
- "online"
- Online
- False

# --solutions--

```c
bool online = true;
if (online) {
    printf("Hello User!\n");
}
```

# --output--

Hello User!
