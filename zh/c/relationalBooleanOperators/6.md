---
language: c
exerciseType: 3
---

# --description--

因为 `!` 把任何非零值变成 `0`，把 `0` 变成 `1`，所以连用两次会把一个值规范化为恰好 `0` 或 `1`：`!!42` 为 `1`，`!!0` 为 `0`。
```c
int count = 3;
printf("%d %d\n", !count, !!count); // prints "0 1"
```
当函数返回任意非零数字而你想要一个干净的 `1` 时，这很有用。

# --instructions--

以下代码的输出是什么？
```c
int n = -5;
printf("%d %d\n", !n, !!n);
```

# --answers--

- 0 1
- 1 0
- 5 -5
- 0 -5

# --solutions--

- 0 1
