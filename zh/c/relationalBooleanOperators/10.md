---
language: c
exerciseType: 3
---

# --description--

短路求值同样会跳过函数调用。在 `1 || check()` 中函数 `check` 永远不会被调用，所以它的任何副作用（比如更新计数器）都不会发生。
```c
int calls = 0;

int check() {
    calls++;
    return 1;
}
```
当 `&&` 或 `||` 右边的函数做了你依赖的事情时，请记住这一点。

# --instructions--

以下代码的输出是什么？
```c
int calls = 0;

int check() {
    calls++;
    return 1;
}

int main() {
    int result = 1 || check();
    printf("%d %d\n", result, calls);
    return 0;
}
```

# --answers--

- 1 0
- 1 1
- 0 0
- 0 1

# --solutions--

- 1 0
