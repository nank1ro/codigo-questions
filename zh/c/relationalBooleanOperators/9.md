---
language: c
exerciseType: 4
---

# --description--

`&&` 和 `||` 使用**短路求值**：一旦结果确定就停止求值。
- 对于 `&&`，如果左边为 `0`，右边永远不会被求值
- 对于 `||`，如果左边非零，右边永远不会被求值

这样左边的检查就能保护右边的危险操作：
```c
int ok = count != 0 && total / count > 2;
```
当 `count` 为 `0` 时，除法永远不会执行，所以程序不会崩溃。

# --instructions--

排序这些代码行，构建一个声明 `total` 和 `count`、用受保护的除法计算 `ok` 并输出它的程序

# --answers--

```c
#include <stdio.h>

int main() {
```

```c
    int total = 10, count = 0;
```

```c
    int ok = count != 0 && total / count > 2;
```

```c
    printf("%d\n", ok);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>

int main() {
    int total = 10, count = 0;
    int ok = count != 0 && total / count > 2;
    printf("%d\n", ok);
    return 0;
}
```

# --output--

0
