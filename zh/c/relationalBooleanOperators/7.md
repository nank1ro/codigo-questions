---
language: c
exerciseType: 2
---

# --description--

从 C99 开始，头文件 `stdbool.h` 提供了类型 `bool` 以及常量 `true`（值为 `1`）和 `false`（值为 `0`）：
```c
#include <stdbool.h>

bool open = true;
bool full = 3 > 5;
printf("%d %d\n", open, full); // prints "1 0"
```
`bool` 底层仍然是整数：它用 `%d` 输出，并且和比较结果一样可以配合 `&&`、`||` 和 `!` 使用。它只是比普通的 `int` 更清楚地表达意图。

# --instructions--

补全代码，使 `open` 为表示真的布尔常量，`closed` 为它的取反，然后输出两者

# --seed--

```c
bool open = [/];
bool closed = [/]open;
printf("%d %d\n", open, closed);
```

# --answers--

- true
- !
- false
- -
- ~

# --solutions--

```c
bool open = true;
bool closed = !open;
printf("%d %d\n", open, closed);
```

# --output--

1 0
