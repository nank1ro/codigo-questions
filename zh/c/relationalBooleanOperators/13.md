---
language: c
exerciseType: 4
---

# --description--

不要像数学里那样连写比较。`1 <= x <= 10` 能编译，但它被解释为 `(1 <= x) <= 10`：第一个比较得到 `0` 或 `1`，然后再和 `10` 比较，所以整个表达式永远为 `1`。
```c
int x = 20;
printf("%d\n", 1 <= x <= 10);       // prints "1" (wrong!)
printf("%d\n", 1 <= x && x <= 10);  // prints "0"
```
始终把两个比较都明确写出来，并用 `&&` 连接。

# --instructions--

排序这些代码行，构建一个把连写比较存入 `chained`、把正确的范围检查存入 `correct` 并输出两者的程序。在 `correct` 之前定义 `chained`

# --answers--

```c
#include <stdio.h>

int main() {
```

```c
    int x = 20;
```

```c
    int chained = 1 <= x <= 10;
```

```c
    int correct = 1 <= x && x <= 10;
```

```c
    printf("%d %d\n", chained, correct);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>

int main() {
    int x = 20;
    int chained = 1 <= x <= 10;
    int correct = 1 <= x && x <= 10;
    printf("%d %d\n", chained, correct);
    return 0;
}
```

# --output--

1 0
