---
language: c
exerciseType: 3
---

# --description--

逻辑运算符不仅适用于 `0` 和 `1`：在 C 中，**任何非零值都视为真**，只有 `0` 视为假。
所以 `5 && 1` 是 `1`，`0 || -3` 是 `1`，`!` 会把任何非零值变成 `0`：
```c
printf("%d\n", !7); // 输出 "0"
printf("%d\n", !0); // 输出 "1"
```
这就是为什么 `day == 6 || 7` 永远为真：单独的 `7` 本身就是一个真值。

# --instructions--

以下代码的输出是什么？
```c
printf("%d\n", !42);
```

# --answers--

- 0
- 1
- -42
- 42

# --solutions--

- 0
