---
language: c
exerciseType: 2
---

# --description--

**比较运算符**比较两个值并给出结果：比较成立时为 `1`，不成立时为 `0`。
**等于**运算符 `==` 检查两个值是否相同，**不等于**运算符 `!=` 检查它们是否不同：
```c
int a = 7, b = 3;
printf("%d\n", a == b);
// 输出 "0"
printf("%d\n", a != b);
// 输出 "1"
```
注意：`==`（两个等号）用于比较，而单个 `=` 用于赋值。

# --instructions--

补全代码，先打印 `a` 是否等于 `b`，然后打印 `a` 是否**不**等于 `b`

# --seed--

```c
int a = 7, b = 3;
printf("%d\n", a [/] b);
printf("%d\n", a [/] b);
```

# --answers--

- ==
- !=
- =
- !

# --solutions--

```c
int a = 7, b = 3;
printf("%d\n", a == b);
printf("%d\n", a != b);
```

# --output--

0
1
