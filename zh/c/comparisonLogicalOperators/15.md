---
language: c
exerciseType: 2
---

# --description--

`while` 循环会持续运行，只要它的条件非零，因此由比较来决定循环何时停止：
```c
int i = 0;
while (i < 3) {
    printf("%d\n", i);
    i++;
}
// 输出 0, 1, 2
```
在 `<` 和 `<=` 之间的选择决定了是否包含最后一个值。

# --instructions--

补全代码，使循环打印从 `1` 到 `3` 的数字，每行一个

# --seed--

```c
int count = [/];
while (count [/] 3) {
    printf("%d\n", count);
    count++;
}
```

# --answers--

- 1
- <=
- 0
- <
- !=
- >

# --solutions--

```c
int count = 1;
while (count <= 3) {
    printf("%d\n", count);
    count++;
}
```

# --output--

1
2
3
