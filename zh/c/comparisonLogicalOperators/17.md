---
language: c
exerciseType: 4
---

# --description--

`bool` 类型的参数可以直接作为 `&&` 或 `||` 的操作数使用，无需将其与 `true` 比较：应写作 `age >= 18 && citizen`，而不是 `citizen == true`。

# --instructions--

排序代码行，定义 `can_vote`，当 `age` 至少为 `18` 且 `citizen` 为 `true` 时返回 `true`，以及一个 `main`，先打印 `20, true` 的结果，再打印 `20, false` 的结果。函数要定义在 `main` 之前

# --answers--

```c
#include <stdio.h>
#include <stdbool.h>
```

```c
bool can_vote(int age, bool citizen) {
```

```c
    return age >= 18 && citizen;
}
```

```c
int main() {
```

```c
    printf("%d\n", can_vote(20, true));
```

```c
    printf("%d\n", can_vote(20, false));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
#include <stdbool.h>
bool can_vote(int age, bool citizen) {
    return age >= 18 && citizen;
}
int main() {
    printf("%d\n", can_vote(20, true));
    printf("%d\n", can_vote(20, false));
    return 0;
}
```

# --output--

1
0
