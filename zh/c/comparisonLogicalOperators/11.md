---
language: c
exerciseType: 2
---

# --description--

`char` 本质上是一个小整数，所以字符可以用同样的运算符进行比较。
使用单引号中的字符字面量进行比较：
```c
char answer = 'y';
printf("%d\n", answer == 'y'); // 输出 "1"
```
双引号会创建一个字符串，不能用这种方式比较。

# --instructions--

补全代码，打印 `grade` 是否等于字符 `B`

# --seed--

```c
char grade = 'B';
printf("%d\n", grade [/] [/]);
```

# --answers--

- ==
- 'B'
- "B"
- =
- 'b'

# --solutions--

```c
char grade = 'B';
printf("%d\n", grade == 'B');
```

# --output--

1
