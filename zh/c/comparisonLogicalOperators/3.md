---
language: c
exerciseType: 3
---

# --description--

在 C 中，比较的结果不是一种特殊类型：它是一个 `int`，其值恰好是 `1`（真）或 `0`（假）。
这意味着你可以像存储其他数字一样把它存入一个 `int` 变量：
```c
int n = 42;
int big = n > 100; // big 为 0
```
输出中不会出现 `true`/`false` 这样的词：`printf("%d", 2 == 2)` 打印的是 `1`。

# --instructions--

以下代码的输出是什么？
```c
int result = 3 <= 3;
printf("%d\n", result);
```

# --answers--

- 1
- 0
- 3
- true

# --solutions--

- 1
