---
language: c
exerciseType: 2
---

# --description--

与加法赋值类似，我们有**减法赋值** `-=`。
功能是一样的，唯一的区别是它执行的是减法运算。
因此，以下两种写法完全相同
```c
num = num - 5;
// is equal to
num -= 5;
```

# --instructions--

使用*减法赋值*运算符从变量 `result` 中减去值 `15`，然后打印它的值

# --seed--

```c
int result = 75;
result[/][/];
printf("%d\n", [/]);
```

# --answers--

-  - 
-  -= 
-  =- 
- 50
- 15
- result

# --solutions--

```c
int result = 75;
result -= 15;
printf("%d\n", result);
```

# --output--

60
