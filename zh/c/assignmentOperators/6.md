---
language: c
exerciseType: 2
---

# --description--

让我们来看看**取模赋值** `%=` 运算符。
它计算变量与一个值的模，并将结果赋给该变量。
因此，以下两种写法完全相同
```c
num = num % 5;
// is equal to
num %= 5;
```

# --instructions--

使用*取模赋值*运算符计算 `result` 与值 `3` 的模，然后打印它的值

# --seed--

```c
int result = 5;
result[/][/];
printf("%d\n", [/]);
```

# --answers--

-  % 
-  =% 
-  %= 
- 3
- result

# --solutions--

```c
int result = 5;
result %= 3;
printf("%d\n", result);
```

# --output--

2
