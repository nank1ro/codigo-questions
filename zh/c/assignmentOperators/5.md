---
language: c
exerciseType: 2
---

# --description--

让我们来看看**除法赋值** `/=` 运算符。
它将变量除以一个值，并将结果赋给该变量。
因此，以下两种写法完全相同
```c
num = num / 5;
// is equal to
num /= 5;
```

# --instructions--

使用*除法赋值*运算符计算 `result` 除以 `5` 的商，然后打印它的值

# --seed--

```c
int result = 50;
[/][/][/];
printf("%d\n", result);
```

# --answers--

-  / 
-  /= 
-  =/ 
- 5
- 25
- result

# --solutions--

```c
int result = 50;
result /= 5;
printf("%d\n", result);
```

# --output--

10
