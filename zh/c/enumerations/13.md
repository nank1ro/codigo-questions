---
language: c
exerciseType: 4
---

# --description--

C 没有内置的方法来获取枚举常量的名称：`printf("%d\n", SUMMER)` 打印的是 `2`，而不是 `Summer`。常见的做法是写一个带 `switch` 的小函数，为每个常量返回对应的字符串。

# --instructions--

排序代码行，定义 `Season` 类型、返回季节名称的函数 `season_name`，以及打印 `SUMMER` 名称的 `main`。函数要定义在 `main` 之前，case 顺序与常量顺序保持一致，并把 `default` 放在最后

# --answers--

```c
#include <stdio.h>
typedef enum { WINTER, SPRING, SUMMER, AUTUMN } Season;
```

```c
char *season_name(Season season) {
```

```c
    switch (season) {
```

```c
        case WINTER: return "Winter";
```

```c
        case SPRING: return "Spring";
```

```c
        case SUMMER: return "Summer";
```

```c
        default: return "Autumn";
```

```c
    }
}
```

```c
int main() {
```

```c
    printf("%s\n", season_name(SUMMER));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef enum { WINTER, SPRING, SUMMER, AUTUMN } Season;
char *season_name(Season season) {
    switch (season) {
        case WINTER: return "Winter";
        case SPRING: return "Spring";
        case SUMMER: return "Summer";
        default: return "Autumn";
    }
}
int main() {
    printf("%s\n", season_name(SUMMER));
    return 0;
}
```

# --output--

Summer
