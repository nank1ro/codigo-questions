---
language: c
exerciseType: 4
---

# --description--

块注释的一个常见用途是**头注释**：放在函数正上方的简短代码块，说明函数做什么、参数的含义以及返回什么。
```c
/*
 * Returns the number of seconds in the given minutes.
 * minutes: a whole number of minutes, never negative
 */
int to_seconds(int minutes) {
    return minutes * 60;
}
```
调用 `to_seconds` 的人现在可以阅读头注释而不是函数体。请把头注释放在函数旁边，以便它们一起更新。

# --instructions--

对各行进行排序，构建一个定义 `max_of` 的程序，它由直接放在其上方、位于 `main` 之前的头注释说明，并打印 `max_of(9, 4)`

# --answers--

```c
#include <stdio.h>
```

```c
/* Returns the larger of a and b */
```

```c
int max_of(int a, int b) {
```

```c
    if (a > b) {
        return a;
    }
```

```c
    return b;
}
```

```c
int main() {
    printf("%d\n", max_of(9, 4));
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
/* Returns the larger of a and b */
int max_of(int a, int b) {
    if (a > b) {
        return a;
    }
    return b;
}
int main() {
    printf("%d\n", max_of(9, 4));
    return 0;
}
```

# --output--

9
