---
language: c
exerciseType: 2
---

# --description--

**注释**是源代码中给人看的文本，而不是给编译器看的。编译器在构建程序之前会把注释丢弃，因此你可以用注释来解释代码的用途、留下提醒或记录某个决定。

最常见的是**单行注释**：从 `//` 到该行末尾的所有内容都会被忽略。
```c
// 问候用户
printf("Hello\n");
```
程序运行时第一行什么也不做；只有 `printf` 会产生输出。

# --instructions--

补全代码，使第一行成为单行注释，并且程序打印 `Hello, world!`

# --seed--

```c
[/] 打印问候语
printf("Hello, world!\n");
```

# --answers--

- //
- /*
- */
- #
- --

# --solutions--

```c
// 打印问候语
printf("Hello, world!\n");
```

# --output--

Hello, world!
