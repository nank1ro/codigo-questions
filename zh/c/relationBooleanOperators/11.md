---
language: c
exerciseType: 2
---

# --instructions--

检查 `word` 是否**不**等于 `dog`，打印布尔结果。
请注意 `strcmp` 仅在字符串**相等**时返回 `0`，所以不要误以为它返回的 0 就像 false 布尔值一样

# --seed--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat")[/][/];
printf("%d\n", [/]);
```

# --answers--

- word
- "dog"
-  == 
-  = 
- dog
-  != 
- 0
- are_words_equal

# --solutions--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat") == 0;
printf("%d\n", are_words_equal);
```

# --output--

1
