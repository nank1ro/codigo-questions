---
language: c
exerciseType: 4
---

# --instructions--

排序代码行，使程序比较 `password` 和 `attempt` 并打印 `Access granted`。在 `attempt` 之前声明 `password`，并将每条消息放在各自的分支内

# --answers--

```c
char password[] = "secret";
```

```c
char attempt[] = "secret";
```

```c
if (strcmp(password, attempt) == 0) {
```

```c
    printf("Access granted\n");
```

```c
} else {
```

```c
    printf("Access denied\n");
```

```c
}
```

# --solutions--

```c
char password[] = "secret";
char attempt[] = "secret";
if (strcmp(password, attempt) == 0) {
    printf("Access granted\n");
} else {
    printf("Access denied\n");
}
```

# --output--

Access granted
