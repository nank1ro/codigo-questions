---
language: c
exerciseType: 1
---

# --description--

Before C23 there was no format specifier that printed a number in binary, and literals like `0b1010` were not standard C either. To show the bits you write the loop yourself: walk from the highest bit down to bit `0` and print `(value >> i) & 1u` each time.
```c
unsigned int value = 5;
for (int i = 3; i >= 0; i--) {
    printf("%u", (value >> i) & 1u);
}
printf("\n");
// 打印 "0101"
```
Shifting the value down by `i` brings bit `i` into the rightmost place, where `& 1u` isolates it.

# --instructions--

Print the 8 bits of `value`, from bit `7` down to bit `0`, on a single line followed by a newline

# --before-seed--

```c
#include <stdio.h>

int main() {
```

# --seed--

```c
unsigned int value = 0xA5;

```

# --after-seed--

```c
    return 0;
}
```

# --solutions--

```c
unsigned int value = 0xA5;
for (int i = 7; i >= 0; i--) {
    printf("%u", (value >> i) & 1u);
}
printf("\n");
```

# --output--

10100101
