---
language: c
exerciseType: 4
---

# --description--

多个较小的数字常常被打包进一个更大的值里。要把其中一个读取回来，先把它右移，使其从第 `0` 位开始，再掩码掉它上面的所有位：
```c
unsigned int packed = 0x1234;
printf("%u\n", (packed >> 8) & 0xFF);
// 打印 "18"，即 0x12 字节
```
先移位、再掩码，这个顺序要记住：掩码描述的始终是字段移到最底部之后的样子。

# --instructions--

排序代码行，把 `packed` 右移 16 位，只保留移位后结果的最低 8 位存入 `third_byte`，并打印它

# --answers--

```c
unsigned int packed = 0xDEADBEEF;
```

```c
unsigned int shifted = packed >> 16;
```

```c
unsigned int third_byte = shifted & 0xFF;
```

```c
printf("%u\n", third_byte);
```

# --solutions--

```c
unsigned int packed = 0xDEADBEEF;
unsigned int shifted = packed >> 16;
unsigned int third_byte = shifted & 0xFF;
printf("%u\n", third_byte);
```

# --output--

173
