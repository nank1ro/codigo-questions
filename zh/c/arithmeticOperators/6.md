---
language: c
exerciseType: 2
---

# --description--

C 语言没有**幂运算**运算符，因此我们需要使用 `math.h` 库中的 `pow()` 函数。
幂运算对应于底数的重复乘法：即**b** 的 *n* 次幂是 *n* 个底数相乘的积：
![exponentiation](https://bit.ly/3zcz6Lt)
```
>>> pow(5, 2);
25
```

# --instructions--

计算数字 5（底数）和 3（指数）的幂，然后打印变量 `result`

# --seed--

```c
int result = [/]([/], [/]);
printf("%d\n", result);
```

# --answers--

- 5
- 3
-  % 
-  * 
- 625
-  ** 
- pow

# --solutions--

```c
int result = pow(5, 3);
printf("%d\n", result);
```

# --output--

125
