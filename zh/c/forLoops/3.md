---
language: c
exerciseType: 2
---

# --description--

我们可以使用 `<` 循环到下一个数字（不包含），或者使用 `<=` 循环到下一个数字（包含）

# --instructions--

补全以下代码中的 `for` 循环，使代码块执行 `3` 次

# --seed--

```c
for (int i = 1; i[/]3; i++) {
        printf("%d\n", i);
}
```

# --answers--

-  ... 
-  ..< 
-  ..> 
-  .< 
-  <= 
-  < 
-  >= 

# --solutions--

```c
for (int i = 1; i <= 3; i++) {
        printf("%d\n", i);
}
```

# --output--

1
2
3
