---
language: c
exerciseType: 4
---

# --description--

综合运用：根据值的种类选择类型，让每个 `printf` 说明符与其参数类型匹配，并在计算需要用与操作数不同的类型进行时使用类型转换。

# --instructions--

对代码行排序，构建一个程序：将 `7` 存入 `total`，将 `2` 存入 `count`，以 `double` 类型计算 `total` 除以 `count` 存入 `avg`，并打印整数除法 `total / count`，接着打印保留一位小数的 `avg`

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int total = 7, count = 2;
```

```c
    double avg = (double) total / count;
```

```c
    printf("%d %.1f\n", total / count, avg);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int total = 7, count = 2;
    double avg = (double) total / count;
    printf("%d %.1f\n", total / count, avg);
    return 0;
}
```

# --output--

3 3.5
