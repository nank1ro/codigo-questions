---
language: c
exerciseType: 4
---

# --description--

`printf` 没有能打印整个结构体的格式说明符。通常的解决办法是写一个小函数，按固定的格式打印各个成员，这样程序的每个部分都能以相同的方式显示这个值：
```c
void print_person(Person p) {
    printf("%s (%d)\n", p.name, p.age);
}
```

# --instructions--

对代码行排序，定义 `Point` 类型、把点打印成 `(x, y)` 的函数 `print_point`，以及打印点 `(3, 4)` 的 `main`。函数要定义在 `main` 之前

# --answers--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
```

```c
void print_point(Point p) {
```

```c
    printf("(%d, %d)\n", p.x, p.y);
```

```c
}
```

```c
int main() {
```

```c
    Point p = {3, 4};
```

```c
    print_point(p);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
void print_point(Point p) {
    printf("(%d, %d)\n", p.x, p.y);
}
int main() {
    Point p = {3, 4};
    print_point(p);
    return 0;
}
```

# --output--

(3, 4)
