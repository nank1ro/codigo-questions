---
language: c
exerciseType: 2
---

# --description--

结构体经常需要保存文本，文本以固定大小的 `char` 数组成员的形式存储：
```c
typedef struct {
    char name[20];
    int age;
} Person;
```
数组成员在声明之后不能用 `=` 赋值：`p.name = "Ann"` 无法通过编译。要用 `string.h` 中的 `strcpy` 把文本复制进去，把该成员作为目标位置：
```c
Person p;
strcpy(p.name, "Ann");
p.age = 30;
```
只有声明处的大括号初始化器能直接接受字符串：`Person p = {"Ann", 30};`。

# --instructions--

补全代码，把 `p` 的名字替换为 `"Ann"`，然后打印名字和年龄

# --seed--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
[/](p.name, "Ann");
printf("%s is %d\n", p[/]name, p.age);
```

# --answers--

- strcpy
- strcat
- strcmp
- strlen
- .
- ->

# --solutions--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
strcpy(p.name, "Ann");
printf("%s is %d\n", p.name, p.age);
```

# --output--

Ann is 30
