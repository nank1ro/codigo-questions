---
language: c
exerciseType: 2
---

# --description--

**链表**（linked list）把值存储在散布于内存中的一个个**节点**（node）里：每个节点持有一个值和一个指向下一个节点的指针，最后一个节点指向 `NULL`。结构体内部的指针引用的正是正在声明的类型本身，因此该结构体需要一个**标签**（tag）来为自身命名；在大括号内部，`typedef` 的名字还不存在：
```c
typedef struct Node {
    int value;
    struct Node *next;
} Node;
```
节点之间通过把一个节点的地址存入另一个节点的 `next` 来链接，被指向节点的成员用箭头来访问：
```c
Node second = {2, NULL};
Node first = {1, &second};
printf("%d\n", first.next->value); // prints "2"
```

# --instructions--

补全 `next` 成员的声明，然后把 `first` 链接到 `second` 并打印这两个值

# --seed--

```c
typedef struct Node {
    int value;
    [/] *next;
} Node;
Node second = {2, NULL};
Node first = {1, [/]};
printf("%d %d\n", first.value, first.next->value);
```

# --answers--

- struct Node
- Node
- &second
- second
- NULL
- first

# --solutions--

```c
typedef struct Node {
    int value;
    struct Node *next;
} Node;
Node second = {2, NULL};
Node first = {1, &second};
printf("%d %d\n", first.value, first.next->value);
```

# --output--

1 2
