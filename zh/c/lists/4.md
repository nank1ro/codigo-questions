---
language: c
exerciseType: 2
---

# --description--

走一遍链表，称为**遍历**（traversal），是一个从头部开始、沿着 `next` 前进直到到达 `NULL` 的循环。`for` 循环可以用一行来表达，循环变量是一个指针：
```c
for (Node *n = head; n != NULL; n = n->next) {
    printf("%d\n", n->value);
}
```
这里没有索引：到达某个节点的唯一途径，就是存储在它前一个节点中的指针。

# --instructions--

补全循环，使其从 `a` 开始并打印链表中每个节点的值

# --seed--

```c
typedef struct Node {
    int value;
    struct Node *next;
} Node;
Node c = {3, NULL};
Node b = {2, &c};
Node a = {1, &b};
for (Node *n = [/]; n [/] NULL; n = [/]) {
    printf("%d\n", n->value);
}
```

# --answers--

- &a
- a
- !=
- ==
- n->next
- n.next

# --solutions--

```c
typedef struct Node {
    int value;
    struct Node *next;
} Node;
Node c = {3, NULL};
Node b = {2, &c};
Node a = {1, &b};
for (Node *n = &a; n != NULL; n = n->next) {
    printf("%d\n", n->value);
}
```

# --output--

1
2
3
