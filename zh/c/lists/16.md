---
language: c
exerciseType: 2
---

# --description--

综合运用：链表常常由数组构建。从前端推入会颠倒顺序，因此要**反向**遍历数组，从最后一个元素到第一个，这样第一个元素最终位于头节点。程序随后用一次遍历打印链表，并逐个节点释放它，让每个 `malloc` 都有对应的 `free`。

# --instructions--

补全 `push_front` 和最后的循环，使程序从数组构建链表 `10, 20, 30`，打印它并释放每个节点

# --seed--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = [/];
    return [/];
}

int main() {
    int values[3] = {10, 20, 30};
    Node *head = NULL;
    for (int i = 2; i >= 0; i--) {
        head = push_front(head, values[i]);
    }
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        Node *next = head->next;
        [/](head);
        head = next;
    }
    return 0;
}
```

# --answers--

- head
- node
- NULL
- free
- values[i]

# --solutions--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

int main() {
    int values[3] = {10, 20, 30};
    Node *head = NULL;
    for (int i = 2; i >= 0; i--) {
        head = push_front(head, values[i]);
    }
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        Node *next = head->next;
        free(head);
        head = next;
    }
    return 0;
}
```

# --output--

10
20
30
