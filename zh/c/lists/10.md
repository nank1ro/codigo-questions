---
language: c
exerciseType: 3
---

# --description--

`free(p)` 之后，变量 `p` 仍然持有旧地址，但它指向的内存已不再属于你：`p` 现在是一个**悬空指针**（dangling pointer）。通过它读取或写入，或者把它再释放一次，都是未定义行为：程序可能打印旧值、打印垃圾值或崩溃，而编译器不会报错。当指针必须在 `free` 之后继续存在时，应紧接着把它设为 `NULL`，这样之后的任何使用都会被 `NULL` 检查拦下。

# --instructions--

以下代码有什么问题？
```c
Node *n = head;
free(n);
printf("%d\n", n->value);
```

# --answers--

- 它读取了一个已被释放的节点，这是未定义行为
- 没有问题：`n` 仍然指向该节点，所以会打印它的值
- 无法编译，因为 `n` 在 `free` 之后被使用
- `free` 需要指针本身的地址，所以应该写成 `free(&n)`

# --solutions--

- 它读取了一个已被释放的节点，这是未定义行为
