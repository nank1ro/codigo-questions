---
language: c
exerciseType: 3
---

# --description--

链表在数组开销大的地方很便宜，反之亦然。无论长度如何，在**前端**添加或移除都只是几次指针赋值，而数组则必须移动每个元素。另一方面，节点在内存中并不连续，因此没有 `list[i]`：要到达第 n 个节点、最后一个节点或求出总长度，只能从头部出发走过中间的每一个节点。经常追加元素的程序通常会保留第二个指针指向最后一个节点，即**尾节点**（tail），以避免这种遍历。

# --instructions--

一个单链表持有 1000 个节点，且只有它的 `head` 已知。以下哪些操作必须访问每一个节点？

# --answers--

- 用 `push_back` 在末尾追加一个节点
- 用 `push_front` 在前端添加一个节点
- 读取第一个节点的值
- 用 `pop_front` 移除第一个节点

# --solutions--

- 用 `push_back` 在末尾追加一个节点
