---
language: c
exerciseType: 3
---

# --description--

链表由一个指向其第一个节点的指针持有，这个节点就是**头节点**（head）。其余每个节点都从头部出发沿着 `next` 依次到达，而且箭头可以串联：`head->next` 是第二个节点，`head->next->next` 是第三个。空链表就是等于 `NULL` 的头节点，最后一个节点的 `next` 也是 `NULL`，因此多跟一步箭头就会对 `NULL` 解引用，使程序崩溃。

# --instructions--

已知上一练习中的 `make_node` 函数，以下代码的输出是什么？
```c
Node *head = make_node(1);
head->next = make_node(2);
head->next->next = make_node(3);
printf("%d\n", head->next->next->value);
```

# --answers--

- 3
- 2
- 1
- 程序会崩溃，因为 `head->next->next` 是 `NULL`

# --solutions--

- 3
