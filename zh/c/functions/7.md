---
language: c
exerciseType: 3
---

# --instructions--

这段代码有什么__问题__？
```c
greet("Daniel");
void greet(char *name) {
    printf("Hello %s!\n", name);
};
```

# --answers--

- 我们不能在定义 greet() 之前使用它
- 我们不能使用 greet() 两次
- greet() 没有返回任何值
- 我们不能使用 name 作为参数

# --solutions--

- 我们不能在定义 greet() 之前使用它

