---
language: swift
exerciseType: 2
---

# --description--

当一个类拥有自己的函数时，这些函数被称为__方法__。

# --instructions--

向你的 `Animal` 类中添加一个 `description` 方法。
使用两个单独的 `print` 语句，它应该打印出被调用的动物的 `gender` 和 `legs`。
然后调用 `description` 方法。

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4

    [/][/]() {
        if let gender = self.gender { print(gender) }
        print([/].legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.[/]
```

# --answers--

-  description
- func
- description()
- self
- description[]
- function
- method

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4

    func description() {
        if let gender = self.gender { print(gender) }
        print(self.legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.description()
```

# --output--

female
4
