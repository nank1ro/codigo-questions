---
language: swift
exerciseType: 2
---

# --description--

扩展可以为类型添加新的**构造器**。对结构体来说，扩展是放置它们的最佳位置：写在结构体主体内的 `init` 会替换自动生成的逐一成员构造器，而在扩展中添加的 `init` 则会保留它。
新的构造器通常通过 `self.init(...)` 委托给已有的构造器：
```swift
struct Size {
    var width: Double
    var height: Double
}
extension Size {
    init(square side: Double) {
        self.init(width: side, height: side)
    }
}
let a = Size(square: 3)          // from the extension
let b = Size(width: 2, height: 5) // memberwise, still available
```

# --instructions--

补全扩展，使 `Point(both: 2.5)` 创建一个 `x` 和 `y` 都等于 `2.5` 的点，并让程序打印 `2.5 2.5` 和 `1.0 2.0`

# --seed--

```swift
struct Point {
    var x: Double
    var y: Double
}
extension Point {
    [/](both: Double) {
        [/](x: both, y: both)
    }
}
let p = Point(both: 2.5)
let q = Point(x: 1, y: 2)
print(p.x, p.y)
print(q.x, q.y)
```

# --answers--

- init
- self.init
- func
- Point
- self

# --solutions--

```swift
struct Point {
    var x: Double
    var y: Double
}
extension Point {
    init(both: Double) {
        self.init(x: both, y: both)
    }
}
let p = Point(both: 2.5)
let q = Point(x: 1, y: 2)
print(p.x, p.y)
print(q.x, q.y)
```

# --output--

2.5 2.5
1.0 2.0
