---
language: swift
exerciseType: 3
---

# --description--

扩展不会创建新类型，也不会复制旧类型：它把成员添加到类型本身，因此该类型的每个现有值和将来的值都能拥有这些成员。
这就是扩展对于你无法编辑的类型（比如来自标准库或框架的类型）如此有用的原因：你打不开定义 `Int` 的那个文件，但可以在程序的任何文件中扩展它。
```swift
extension Int {
    func isDivisible(by other: Int) -> Bool {
        return self % other == 0
    }
}
let n = 12
print(n.isDivisible(by: 4)) // true
```

# --instructions--

关于扩展的下列说法中，哪一项是正确的？

# --answers--

- 即使无法编辑 `Int` 的源代码，你也可以为它添加方法
- 扩展会创建一个新类型，它是原始类型的副本
- 在扩展中添加的方法只能在该扩展内部被调用
- 扩展必须写在定义该类型的同一个文件中

# --solutions--

- 即使无法编辑 `Int` 的源代码，你也可以为它添加方法
