---
language: swift
exerciseType: 1
---

# --description--

`Array` 是一个泛型类型：`[Int]` 和 `[String]` 都是数组，但具有不同的 **`Element`** 类型。`Array` 的扩展对它们全部生效，当新成员只对某些元素有意义时这就是个问题：你无法把字符串当作数字相加。
`where` 子句把扩展限制在 `Element` 为给定类型的那些数组上：
```swift
extension Array where Element == Int {
    var largest: Int {
        var result = Int.min
        for number in self {
            if number > result {
                result = number
            }
        }
        return result
    }
}
print([3, 9, 2].largest) // 9
```
`[3, 9, 2].largest` 可以工作，而 `["a", "b"].largest` 是编译错误：该属性在 `[String]` 上不存在。

# --instructions--

编写一个限制在 `Element == Int` 的 `Array` 扩展，其中包含返回元素之和的方法 `sum()`。不要使用 `reduce`

方法调用示例：
```swift
print([1, 2, 3].sum())
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

你必须使用一个带有 `where Element == Int` 子句的 `Array` 扩展

```json
{
  "regex": "extension\\s+Array\\s+where\\s+Element\\s*==\\s*Int",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

你不得使用 `reduce`

```json
{
  "regex": "reduce",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`[1, 2, 3].sum()` 必须返回 `6`

```swift
tryCatch([1, 2, 3].sum() == 6)
```

`[10, -4].sum()` 必须返回 `6`

```swift
tryCatch([10, -4].sum() == 6)
```

`[7].sum()` 必须返回 `7`

```swift
tryCatch([7].sum() == 7)
```

空数组的和必须为 `0`

```swift
let _empty: [Int] = []
tryCatch(_empty.sum() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension Array where Element == Int {
    func sum() -> Int {
        var total = 0
        for number in self {
            total += number
        }
        return total
    }
}
```
