---
language: swift
exerciseType: 4
---

# --description--

用 `^` 和 `$` 验证整段文本是可行的，但 `wholeMatch(of:)` 把这件事直接说了出来：只有当模式从第一个字符覆盖到最后一个字符时，它才返回一个匹配，否则返回 `nil`：
```swift
print("1a2b".wholeMatch(of: #/[0-9a-f]+/#) != nil) // true
print("1z".wholeMatch(of: #/[0-9a-f]+/#) != nil)   // false
```
在文本内部查找内容用 `firstMatch(of:)`，检查文本是否符合某个确切的形状用 `wholeMatch(of:)`。

# --instructions--

排序各项，构建一个程序，在循环之前定义 `isHex`，并为 `values` 的每个值打印结果

# --answers--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
```

```swift
let values = ["1a2b", "1z", "ff"]
```

```swift
for value in values {
    print(isHex(value))
}
```

# --solutions--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
let values = ["1a2b", "1z", "ff"]
for value in values {
    print(isHex(value))
}
```

```swift
let values = ["1a2b", "1z", "ff"]
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
for value in values {
    print(isHex(value))
}
```

# --output--

true
false
true
