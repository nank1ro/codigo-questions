---
language: swift
exerciseType: 1
---

# --description--

少数几个简写就能覆盖大多数模式。每一个都恰好匹配**一个**字符：
- `\d` 是一个数字
- `\w` 是一个字母、数字或下划线
- `\s` 是一个空格、制表符或换行符
- `.` 是任意单个字符

要匹配多个字符，在模式后面紧跟一个**量词**：
- `+` 表示一个或多个
- `*` 表示零个或多个
- `?` 表示零个或一个

于是 `\w+` 是一个单词，`\s*` 是可选的空白，`\d?` 是一个可选的数字：
```swift
print("hello world".contains(#/\w+\s\w+/#)) // true
print("hello".contains(#/\w+\s\w+/#))       // false
```
没有特殊含义的字符只是匹配它自身，所以 `#/cat/#` 匹配 `cat` 这三个字母。

# --instructions--

编写一个函数 `hasDigit`，接收一个文本（`String`，省略参数标签），使用正则表达式，当文本至少包含一个数字时返回 `true`

函数调用示例：
```swift
print(hasDigit("abc1"))
// 输出 true
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func hasDigit() {

}
```

# --asserts--

函数必须使用以 `#/` 书写的正则表达式字面量

```json
{
  "regex": "#/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`hasDigit("abc1")` 必须返回 `true`

```swift
tryCatch(hasDigit("abc1") == true)
```

`hasDigit("7")` 必须返回 `true`

```swift
tryCatch(hasDigit("7") == true)
```

`hasDigit("abc")` 必须返回 `false`

```swift
tryCatch(hasDigit("abc") == false)
```

`hasDigit("")` 必须返回 `false`

```swift
tryCatch(hasDigit("") == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func hasDigit(_ text: String) -> Bool {
    return text.contains(#/\d/#)
}
```
