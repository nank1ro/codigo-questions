---
language: swift
exerciseType: 1
---

# --description--

当拒绝条件放在最前面、真正的工作留在底部且不缩进时，验证函数读起来最顺畅。`guard` 正是为此而设计的：它陈述必须成立的条件，当条件不成立时运行它的 `else` 代码块。
```swift
func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
`guard` 的 `else` 代码块必须离开当前作用域，`throw` 就是做到这一点的方式之一，与 `return`、`break` 和 `continue` 并列。几个堆叠在函数顶部的 `guard` 读起来就像一份输入必须满足的规则清单。

# --instructions--

用 `guard` 补全 `priceFor`，当 `quantity` 不大于 `0` 时抛出 `OrderError.notPositive`

函数调用示例：
```swift
do {
    print(try priceFor(4))
} catch {
    print("invalid")
}
// 输出 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    
    return quantity * 3
}
```

# --before-asserts--

```swift
func _price(_ quantity: Int) -> Int {
    do {
        return try priceFor(quantity)
    } catch OrderError.notPositive {
        return -1
    } catch {
        return -2
    }
}
```

# --asserts--

`priceFor(4)` 必须返回 `12`

```swift
tryCatch(_price(4) == 12)
```

`priceFor(1)` 必须返回 `3`

```swift
tryCatch(_price(1) == 3)
```

`priceFor(0)` 必须抛出 `notPositive`

```swift
tryCatch(_price(0) == -1)
```

`priceFor(-5)` 必须抛出 `notPositive`

```swift
tryCatch(_price(-5) == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
