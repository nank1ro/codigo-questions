---
language: swift
exerciseType: 1
---

# --description--

转换为小写是忽略大小写比较文本的常用方法：两个仅大小写不同的字符串在都转换为小写后就会相等。

# --instructions--

编写一个函数 `sameWord`，接收两个 `String` 值（省略参数标签），当它们在忽略大小写的情况下相等时返回 `true`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`sameWord("Swift", "swift")` 必须返回 `true`

```swift
tryCatch(sameWord("Swift", "swift") == true)
```

`sameWord("HELLO", "hello")` 必须返回 `true`

```swift
tryCatch(sameWord("HELLO", "hello") == true)
```

`sameWord("Swift", "Swifty")` 必须返回 `false`

```swift
tryCatch(sameWord("Swift", "Swifty") == false)
```

`sameWord("", "")` 必须返回 `true`

```swift
tryCatch(sameWord("", "") == true)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func sameWord(_ a: String, _ b: String) -> Bool {
    return a.lowercased() == b.lowercased()
}
```
