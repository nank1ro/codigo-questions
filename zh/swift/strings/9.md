---
language: swift
exerciseType: 1
---

# --description--

使用索引写法比较冗长，因此 Swift 为最常见的场景提供了简便方式：
- `first` 和 `last` 以可选的 `Character?` 形式返回第一个和最后一个字符（空字符串时为 `nil`）
- `prefix(n)` 返回前 `n` 个字符，`suffix(n)` 返回后 `n` 个字符
```swift
let word = "Swift"
print(word.first!)     // S
print(word.prefix(2))  // Sw
print(word.suffix(3))  // ift
```
`prefix` 和 `suffix` 返回一个 `Substring`，它是对原始文本的一个视图。要把它存储为真正的 `String`，需要用 `String(...)` 包裹。如果 `n` 大于 `count`，你只会得到整个字符串。

# --instructions--

编写一个函数 `firstTwo`，接收一个 `String`（省略参数标签），以 `String` 形式返回它的前两个字符；再编写一个函数 `lastTwo`，以 `String` 形式返回它的后两个字符

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`firstTwo("Swift")` 必须返回 `"Sw"`

```swift
tryCatch(firstTwo("Swift") == "Sw")
```

`firstTwo("a")` 必须返回 `"a"`

```swift
tryCatch(firstTwo("a") == "a")
```

`lastTwo("Swift")` 必须返回 `"ft"`

```swift
tryCatch(lastTwo("Swift") == "ft")
```

`lastTwo("")` 必须返回 `""`

```swift
tryCatch(lastTwo("") == "")
```

`firstTwo` 必须返回一个 `String`

```swift
tryCatch(type(of: firstTwo("Swift")) == String.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstTwo(_ text: String) -> String {
    return String(text.prefix(2))
}

func lastTwo(_ text: String) -> String {
    return String(text.suffix(2))
}
```
