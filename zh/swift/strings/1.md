---
language: swift
exerciseType: 2
---

# --description--

**字符串**是一段文本。在 Swift 中，你在双引号之间书写字符串字面量，它的类型是 `String`：
```swift
let greeting: String = "Hello"
var city = "Rome"
```
和其他值一样，`let` 创建一个不能更改的常量，而 `var` 创建一个可以更改的变量。
Swift 会根据字面量推断出 `String` 类型，因此类型注解是可选的。

# --instructions--

声明一个名为 `language` 的 `String` 类型常量，保存文本 `Swift`，然后打印它

# --seed--

```swift
[/] language: [/] = "Swift"
print(language)
```

# --answers--

- let
- Double
- String
- Character
- Int

# --solutions--

```swift
let language: String = "Swift"
print(language)
```

# --output--

Swift
