---
language: swift
exerciseType: 4
---

# --description--

格式字符串可以包含任意数量的格式说明符。值按相同的顺序跟在后面，用逗号分隔，并且每个值都必须匹配其格式说明符的类型：`Int` 用 `%d`，`Double` 用 `%f`：
```swift
let count = 3
let weight = 4.5
print(String(format: "%d items, %.1f kg", count, weight)) // 3 items, 4.5 kg
```
把 `Double` 传给 `%d`（或把 `Int` 传给 `%f`）可以编译通过，但会打印出一个无意义的数字，所以务必检查格式说明符和值是否一一对应。

# --instructions--

排列这些代码块以打印 `3 boxes, 7.5 kg`，以 `import Foundation` 开头，并根据 `count` 计算 `weight`

# --answers--

```swift
import Foundation
```

```swift
let count = 3
```

```swift
let weight = Double(count) * 2.5
```

```swift
print(String(format: "%d boxes, %.1f kg", count, weight))
```

# --solutions--

```swift
import Foundation
let count = 3
let weight = Double(count) * 2.5
print(String(format: "%d boxes, %.1f kg", count, weight))
```

# --output--

3 boxes, 7.5 kg
