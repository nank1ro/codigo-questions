---
language: swift
exerciseType: 1
---

# --description--

要把 `String` 插入格式字符串，请使用格式说明符 `%@`：
```swift
let name = "Ada"
let age = 36
print(String(format: "%@ is %d years old", name, age)) // Ada is 36 years old
```
`%@` 可以直接接受 Swift 的 `String`。不要对 Swift 字符串使用 `%s`：该格式说明符期望的是 C 字符串，会打印出乱码或导致崩溃。

# --instructions--

编写一个函数 `greeting`，接收一个姓名（`String`）和一个年龄（`Int`），两者都省略参数标签，使用带有 `%@` 和 `%d` 的 `String(format:)` 返回 `<name> is <age> years old`

函数调用示例：
```swift
print(greeting("Ada", 36))
// prints Ada is 36 years old
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func greeting() {

}
```

# --asserts--

函数必须使用带有 `%@` 的 `String(format:`

```json
{
  "regex": "String\\(format:[^\\n]*%@",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greeting("Ada", 36)` 必须返回 `"Ada is 36 years old"`

```swift
tryCatch(greeting("Ada", 36) == "Ada is 36 years old")
```

`greeting("Linus", 54)` 必须返回 `"Linus is 54 years old"`

```swift
tryCatch(greeting("Linus", 54) == "Linus is 54 years old")
```

`greeting("Bo", 7)` 必须返回 `"Bo is 7 years old"`

```swift
tryCatch(greeting("Bo", 7) == "Bo is 7 years old")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greeting(_ name: String, _ age: Int) -> String {
    return String(format: "%@ is %d years old", name, age)
}
```
