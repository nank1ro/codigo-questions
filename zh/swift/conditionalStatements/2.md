---
language: swift
exerciseType: 2
---

# --description--

让我们继续前面的例子。
```swift
var niceWeather = true
if niceWeather {
    // play outside
}
```
我们已经看到 `if` 语句只在条件为 `true` 时才执行代码块。
另一个需要注意的重要事项是**花括号** `{}`，它们表示一个代码块。

# --instructions--

添加*花括号* `{}` 来修复 `if` 语句

# --seed--

```swift
var time = 8
if time < 12 [/]
    print("It's morning")
[/]
```

# --answers--

- if
- ==
- =
- else
- ?
- {
- }

# --solutions--

```swift
var time = 8
if time < 12 {
    print("It's morning")
}
```

# --output--

It's morning
