---
language: swift
exerciseType: 2
---

# --description--

我们还可以在一个条件语句（`if`、`else if` 或 `else`）中嵌套另一个条件语句，以创建更复杂的结构。
```swift
var num = 4
if num < 3 {
    print("the number is lower than 3")
} else {
    if num == 3 {
        print("the number is 3")
    } else if num == 4 {
        print("the number is 4")
    } else {
        print("the number is greather than 4")
    }
}
```
这段代码的输出是 `the number is 4`。

# --instructions--

让我们修改一天中时间段的程序，在 `else` 语句中嵌套一个条件语句。
嵌套的条件语句应检查 `time` 是否*小于* `18`，并在代码块中打印 `"It's afternoon"`。
如果前一个条件为 `false`，则打印 `"It's evening"`

# --seed--

```swift
var time = 19
if time > 0 && time < 12 {
    print("It's morning")
} else if time == 12 {
    print("It's midday")
} else if time == 0 {
    print("It's midnight")
} else {
    [/][/][/][/] {
        print("It's afternoon")
    } [/] {
        print("It's evening")
    }
}
```

# --answers--

- 6
- elif 
-  < 
-  == 
- :
- ;
- time
- else if:
- elseif:
- if 
- else

# --solutions--

```swift
var time = 19
if time > 0 && time < 12 {
    print("It's morning")
} else if time == 12 {
    print("It's midday")
} else if time == 0 {
    print("It's midnight")
} else {
    if time < 6 {
        print("It's afternoon")
    } else {
        print("It's evening")
    }
}
```

# --output--

It's evening
