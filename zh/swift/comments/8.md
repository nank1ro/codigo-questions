---
language: swift
exerciseType: 4
---

# --description--

块注释的一个常见用法是**头部注释**：放在函数正上方的一小段说明，讲清楚这个函数做什么以及它的参数是什么意思。
```swift
/*
Returns the number of seconds in the given minutes.
minutes is a whole number, never negative.
*/
func toSeconds(_ minutes: Int) -> Int {
    return minutes * 60
}
```
调用 `toSeconds` 的人现在可以读头部注释而不必读函数体。把头部注释紧挨着函数放置，它们才会一起被更新。

# --instructions--

排列这些条目，构建一个程序：定义 `area`，并在其正上方用头部注释为它写文档，然后输出 `area(3, 4)`

# --answers--

```swift
/*
```

```swift
Returns the area of a rectangle.
```

```swift
*/
```

```swift
func area(_ w: Int, _ h: Int) -> Int {
    return w * h
}
```

```swift
print(area(3, 4))
```

# --solutions--

```swift
/*
Returns the area of a rectangle.
*/
func area(_ w: Int, _ h: Int) -> Int {
    return w * h
}
print(area(3, 4))
```

# --output--

12
