---
language: swift
exerciseType: 4
---

# --description--

集合之间也可以互相比较。这些方法都返回 `Bool`：
- `a.isSubset(of: b)` 在 `a` 的每个元素都在 `b` 中时为 `true`
- `a.isSuperset(of: b)` 在 `a` 包含 `b` 的每个元素时为 `true`
- `a.isDisjoint(with: b)` 在 `a` 和 `b` 没有共同元素时为 `true`
```swift
let small: Set<Int> = [1, 2]
let big: Set<Int> = [1, 2, 3]
print(small.isSubset(of: big))   // true
print(big.isSuperset(of: small)) // true
print(small.isDisjoint(with: big)) // false
```

# --instructions--

排列这些代码块，先检查 `basket` 中的每种水果是否也在 `fruits` 中，然后检查这两个集合是否完全没有交集。
在 `basket` 之前声明 `fruits`

# --answers--

```swift
let fruits: Set<String> = ["apple", "pear", "plum"]
```

```swift
let basket: Set<String> = ["pear", "plum"]
```

```swift
print(basket.isSubset(of: fruits))
```

```swift
print(fruits.isDisjoint(with: basket))
```

# --solutions--

```swift
let fruits: Set<String> = ["apple", "pear", "plum"]
let basket: Set<String> = ["pear", "plum"]
print(basket.isSubset(of: fruits))
print(fruits.isDisjoint(with: basket))
```

# --output--

true
false
