---
language: swift
exerciseType: 1
---

# --instructions--

编写两个函数，接收两个 `Set<String>`（省略参数标签），并返回一个 `Bool`：
- `canAccess` 接收所需权限和已授予权限，只有当所有所需权限都已被授予时才返回 `true`
- `noOverlap` 在两个集合没有共同权限时返回 `true`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`canAccess(["read"], ["read", "write"])` 必须返回 `true`

```swift
tryCatch(canAccess(["read"], ["read", "write"]) == true)
```

`canAccess(["read", "delete"], ["read", "write"])` 必须返回 `false`

```swift
tryCatch(canAccess(["read", "delete"], ["read", "write"]) == false)
```

`canAccess([], ["read"])` 必须返回 `true`

```swift
tryCatch(canAccess([], ["read"]) == true)
```

`noOverlap(["read"], ["write"])` 必须返回 `true`

```swift
tryCatch(noOverlap(["read"], ["write"]) == true)
```

`noOverlap(["read", "write"], ["write"])` 必须返回 `false`

```swift
tryCatch(noOverlap(["read", "write"], ["write"]) == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func canAccess(_ required: Set<String>, _ granted: Set<String>) -> Bool {
    return required.isSubset(of: granted)
}

func noOverlap(_ first: Set<String>, _ second: Set<String>) -> Bool {
    return first.isDisjoint(with: second)
}
```
