---
language: swift
exerciseType: 1
---

# --instructions--

编写一个函数 `evens`，接收一个 `Set<Int>`（省略参数标签），返回一个只包含集合中偶数的数组，按升序排列

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`evens([1, 2, 3, 4, 5, 6])` 必须返回 `[2, 4, 6]`

```swift
tryCatch(evens([1, 2, 3, 4, 5, 6]) == [2, 4, 6])
```

`evens([10, 4, 8])` 必须返回 `[4, 8, 10]`

```swift
tryCatch(evens([10, 4, 8]) == [4, 8, 10])
```

`evens([7, 9])` 必须返回一个空数组

```swift
tryCatch(evens([7, 9]) == [])
```

该函数必须返回一个整数数组

```swift
tryCatch(type(of: evens([2])) == [Int].self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func evens(_ numbers: Set<Int>) -> [Int] {
    var result: [Int] = []
    for number in numbers.sorted() {
        if number % 2 == 0 {
            result.append(number)
        }
    }
    return result
}
```
