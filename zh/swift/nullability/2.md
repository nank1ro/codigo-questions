---
language: swift
exerciseType: 1
---

# --description--

你可以用 `==` 和 `!=` 把一个可选值和 `nil` 进行比较，也可以直接把它和被包装类型的普通值进行比较：
```swift
var score: Int? = 10
print(score == nil) // false
print(score == 10)  // true
```
记住 `Int?` 和 `Int` 是两种不同的类型：`Int?` 可能为空，`Int` 永远不会为空。

# --instructions--

声明一个没有赋值的可选 `String` 变量 `nickname`，以及一个持有 `10` 的可选 `Int` 变量 `score`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`nickname` 必须为 `nil`

```swift
tryCatch(nickname == nil)
```

`nickname` 必须是可选的 `String`

```swift
tryCatch(type(of: nickname) == String?.self)
```

`score` 必须等于 `10`

```swift
tryCatch(score == 10)
```

`score` 必须是可选的 `Int`

```swift
tryCatch(type(of: score) == Int?.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
var nickname: String?
var score: Int? = 10
```
