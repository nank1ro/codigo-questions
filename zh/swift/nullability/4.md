---
language: swift
exerciseType: 4
---

# --description--

只有在已经确认可选值不是 `nil` 之后，强制解包才是安全的：
```swift
if score != nil {
    print(score! * 2)
}
```

# --instructions--

排列这些项，使得只有在确认 `price` 不是 `nil` 之后，才把它翻倍并打印出来

# --answers--

```swift
let price: Double? = 9.5
```

```swift
if price != nil {
```

```swift
    print(price! * 2)
```

```swift
}
```

# --solutions--

```swift
let price: Double? = 9.5
if price != nil {
    print(price! * 2)
}
```

# --output--

19.0
