---
language: swift
exerciseType: 3
---

# --description--

可选值就像一个盒子：在使用里面的值之前，你必须先把它打开，Swift 把这个过程称为**解包**。
最快的方式是使用感叹号 `!` 进行**强制解包**：
```swift
let score: Int? = 10
print(score! + 5) // 15
```
`!` 告诉 Swift“我确定这里有一个值”。如果你错了，可选值实际上是 `nil`，程序会立即崩溃并报出运行时错误：
```swift
let missing: Int? = nil
print(missing! + 5) // Fatal error: Unexpectedly found nil
```
这就是为什么强制解包被认为是危险的：只有在确定值一定存在时才使用它。

# --instructions--

对一个包含 `nil` 的可选值进行强制解包，会发生什么？

# --answers--

- 程序在运行时崩溃
- 表达式的结果为 0
- 编译器报告错误
- 值被静默地转换为空字符串

# --solutions--

- 程序在运行时崩溃
