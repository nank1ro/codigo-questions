---
language: swift
exerciseType: 2
---

# --description--

有时候一个值就是缺失的：用户没有中间名、搜索没有找到任何结果、文本无法转换成数字。
Swift 用 `nil` 表示缺失的值，但普通变量永远不能持有它：
```swift
var age: Int = nil // error: 'nil' cannot initialize type 'Int'
```
要允许出现缺失的值，你需要在类型后面加上问号 `?` 来声明一个**可选**类型。
`Int?` 可以持有一个 `Int`，也可以持有 `nil`：
```swift
var age: Int? = 30
age = nil // 允许
```
声明时没有赋值的可选变量，其初始值为 `nil`。

# --instructions--

声明一个名为 `middleName` 的可选 `String` 变量，并将它设置为 `nil`

# --seed--

```swift
var middleName: [/] = [/]
print(middleName == nil)
```

# --answers--

- String?
- String
- nil
- null
- None

# --solutions--

```swift
var middleName: String? = nil
print(middleName == nil)
```

# --output--

true
