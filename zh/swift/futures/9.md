---
language: swift
exerciseType: 1
---

# --description--

`async let` 与它所在的作用域绑定在一起。要启动并发工作并保留对它的句柄，请使用 **`Task`**。传给 `Task { }` 的闭包独立运行，任务可以被存储、传递或返回：
```swift
let job = Task {
    return await double(21)
}
```
结果之后用 **`.value`** 读取，读取时需要 await：
```swift
print(await job.value)
// 输出 42
```
句柄的类型说明了它产生什么值以及可能抛出什么错误：`Task<Int, Never>` 是一个返回 `Int` 且永不抛出错误的任务。与 `async let` 不同，`Task` 可以在普通的、非异步的代码中创建。

# --instructions--

编写一个函数 `startDoubling`，接收一个整数（省略参数标签），返回一个 `Task<Int, Never>`，它产生该数字的两倍

函数调用示例：
```swift
print(await startDoubling(4).value)
// 输出 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func startDoubling() {

}
```

# --asserts--

函数必须创建一个 `Task`

```json
{
  "regex": "Task\\s*\\{",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`startDoubling(4).value` 必须是 `8`

```swift
tryCatch(await startDoubling(4).value == 8)
```

`startDoubling(0).value` 必须是 `0`

```swift
tryCatch(await startDoubling(0).value == 0)
```

`startDoubling(-6).value` 必须是 `-12`

```swift
tryCatch(await startDoubling(-6).value == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func startDoubling(_ n: Int) -> Task<Int, Never> {
    return Task {
        return n * 2
    }
}
```
