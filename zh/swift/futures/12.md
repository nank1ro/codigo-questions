---
language: swift
exerciseType: 1
---

# --description--

`async let` 适用于编写代码时就知道调用数量的情况。对于大小只有在运行时才知道的列表，请使用**任务组**。

`withTaskGroup(of:)` 打开一个组，`addTask` 为每个条目启动一个子任务，然后用 `for await` 读取该组，结果一完成就会被送来：
```swift
let total = await withTaskGroup(of: Int.self) { group in
    for n in numbers {
        group.addTask {
            return await square(n)
        }
    }
    var sum = 0
    for await value in group {
        sum += value
    }
    return sum
}
```
`of: Int.self` 声明了每个子任务返回什么。整个 `withTaskGroup` 调用是一个表达式，因此在它前面需要一个 `await`，并且它会等到所有子任务完成后才返回。

# --instructions--

编写一个函数 `sumOfSquares`，接收一个整数数组（省略参数标签），是异步的，用 `withTaskGroup` 对每个元素计算 `square`，并返回这些结果之和

函数调用示例：
```swift
print(await sumOfSquares([1, 2, 3]))
// 输出 14
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func square(_ n: Int) async -> Int {
    return n * n
}

func sumOfSquares() {

}
```

# --asserts--

函数必须使用 `withTaskGroup`

```json
{
  "regex": "withTaskGroup",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`sumOfSquares([1, 2, 3])` 必须返回 `14`

```swift
tryCatch(await sumOfSquares([1, 2, 3]) == 14)
```

`sumOfSquares([4])` 必须返回 `16`

```swift
tryCatch(await sumOfSquares([4]) == 16)
```

`sumOfSquares([])` 必须返回 `0`

```swift
tryCatch(await sumOfSquares([]) == 0)
```

`sumOfSquares([-2, 5])` 必须返回 `29`

```swift
tryCatch(await sumOfSquares([-2, 5]) == 29)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func square(_ n: Int) async -> Int {
    return n * n
}

func sumOfSquares(_ numbers: [Int]) async -> Int {
    return await withTaskGroup(of: Int.self) { group in
        for n in numbers {
            group.addTask {
                return await square(n)
            }
        }
        var total = 0
        for await value in group {
            total += value
        }
        return total
    }
}
```
