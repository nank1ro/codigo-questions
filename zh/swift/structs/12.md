---
language: swift
exerciseType: 4
---

# --instructions--

排序各项以打印 `EUR 12`，先声明结构体，并在 `print` 之前创建实例

# --answers--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int
}
```

```swift
let price = Money(amount: 12)
```

```swift
print("\(Money.currency) \(price.amount)")
```

# --solutions--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int
}
let price = Money(amount: 12)
print("\(Money.currency) \(price.amount)")
```

# --output--

EUR 12
