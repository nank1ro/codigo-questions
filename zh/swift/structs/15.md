---
language: swift
exerciseType: 1
---

# --description--

结构体和其他类型一样，因此它可以存储在数组、字典或集合中，并且你已经掌握的每个工具都能继续作用于它：
```swift
struct Item {
    var name: String
    var price: Int
}

let items = [Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]

for item in items {
    print(item.name)
}

let names = items.map { $0.name }
let total = items.reduce(0) { $0 + $1.price }
print(total) // 10
```
记住数组存储的是*副本*：把 `items[0]` 读入一个变量再修改它，不会触及数组本身。

# --instructions--

编写一个函数 `totalPrice`，它接收一个不带参数标签的 `Item` 数组，并返回每个条目的 `price` 之和；当数组为空时返回 `0`

函数调用示例：
```swift
print(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]))
// prints 10
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice() {

}
```

# --asserts--

两个价格分别为 `3` 和 `7` 的条目，总价必须是 `10`

```swift
tryCatch(totalPrice([Item(name: "Tea", price: 3), Item(name: "Cake", price: 7)]) == 10)
```

单个条目的总价必须是它自己的价格

```swift
tryCatch(totalPrice([Item(name: "Pen", price: 2)]) == 2)
```

空数组的总价必须是 `0`

```swift
tryCatch(totalPrice([]) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Item {
    var name: String
    var price: Int
}

func totalPrice(_ items: [Item]) -> Int {
    return items.reduce(0) { $0 + $1.price }
}
```
