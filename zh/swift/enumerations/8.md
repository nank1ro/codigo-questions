---
language: swift
exerciseType: 1
---

# --instructions--

你有一个名为 `Gender` 的枚举。
添加缺少的成员（`male` 和 `female`）

# --seed--

```swift
enum Gender {
    
}
print(Gender.male)
// print also the other gender
print
```

# --solutions--

```swift
enum Gender {
    case male
    case female
}
print(Gender.male)
// print also the other gender
print(Gender.female)
```

# --output--

male
female
