---
language: swift
exerciseType: 2
---

# --instructions--

你可以通过赋值来指定枚举的值。
为每个枚举成员分配正确的 `Int` 数值

# --seed--

```swift
enum OddNumber: [/] {
    case one = [/]
    case three = 3
    case five[/][/]
}
let number = [/].five
print(number.rawValue)
```

# --answers--

- CaseIterable
- Int
- 1
- 2
-  = 
- 5
- OddNumber
- oddNumber

# --solutions--

```swift
enum OddNumber: Int {
    case one = 1
    case three = 3
    case five = 5
}
let number = OddNumber.five
print(number.rawValue)
```

# --output--

5
