---
language: swift
exerciseType: 4
---

# --description--

协议扩展还可以添加协议从未列为要求的成员。它们是额外的便利功能，每个遵循协议的类型都能使用：
```swift
protocol Sized {
    var count: Int { get }
}

extension Sized {
    var isEmpty: Bool {
        return count == 0
    }
}
```
`isEmpty` 不是一项要求，因此遵循协议的类型不必提供它——它直接获得该成员。

# --instructions--

排列这些代码块以打印 `HEY`：先声明协议，然后是它的扩展，接着是结构体，最后是 `print`

# --answers--

```swift
protocol Loud {
    var text: String { get }
}
```

```swift
extension Loud {
    func shout() -> String { return text.uppercased() }
}
```

```swift
struct Message: Loud {
    var text: String
}
```

```swift
print(Message(text: "hey").shout())
```

# --solutions--

```swift
protocol Loud {
    var text: String { get }
}
extension Loud {
    func shout() -> String { return text.uppercased() }
}
struct Message: Loud {
    var text: String
}
print(Message(text: "hey").shout())
```

# --output--

HEY
