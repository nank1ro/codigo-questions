---
language: swift
exerciseType: 1
---

# --instructions--

创建一个新变量 `revenue`，将 `earnings` 和 `costs` 的差赋值给它，然后打印它的值

# --seed--

```swift
var earnings = 2000
var costs = 750
```

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`revenue` 必须等于 `earnings` 和 `costs` 的差。

```swift
    func test1() {
        XCTAssertEqual(revenue, earnings - costs, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
var earnings = 2000
var costs = 750
var revenue = earnings - costs;
```

# --output--

1250
