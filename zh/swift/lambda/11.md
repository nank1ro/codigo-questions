---
language: swift
exerciseType: 2
---

# --description--

闭包可以使用在其函数体外部声明的变量。它会**捕获**这些变量：只要闭包存在，该变量就会持续存在，即使声明它的函数已经返回。
这让函数可以构建一个拥有自己私有状态的闭包：
```swift
func makeCounter() -> () -> Int {
    var count = 0
    return {
        count += 1
        return count
    }
}
```
`() -> Int` 是一个不接收参数并返回 `Int` 的闭包类型。每次调用返回的闭包都会递增同一个被捕获的 `count`：
```swift
let counter = makeCounter()
print(counter()) // 1
print(counter()) // 2
```

# --instructions--

补全 `makeCounter`，使它返回一个不带参数的闭包，该闭包递增并返回被捕获的 `count`，使程序在三行中打印 `1`、`2` 和 `3`

# --seed--

```swift
func makeCounter() -> [/] {
    [/] count = 0
    return {
        count += 1
        return count
    }
}
let counter = makeCounter()
print(counter())
print(counter())
print(counter())
```

# --answers--

- () -> Int
- var
- let
- Int
- () -> Void

# --solutions--

```swift
func makeCounter() -> () -> Int {
    var count = 0
    return {
        count += 1
        return count
    }
}
let counter = makeCounter()
print(counter())
print(counter())
print(counter())
```

# --output--

1
2
3
