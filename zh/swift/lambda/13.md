---
language: swift
exerciseType: 4
---

# --description--

存储在常量中的闭包可以在任何需要闭包参数的地方传递，使用该参数的参数标签：
```swift
let ascending = { (a: Int, b: Int) -> Bool in a < b }
print([3, 1, 2].sorted(by: ascending)) // [1, 2, 3]
```

# --instructions--

排列这些代码块，把一个按从短到长排序两个字符串的闭包存储到 `byLength` 中，然后打印用它排序后的 `words`。
在 `byLength` 之前声明 `words`

# --answers--

```swift
let words = ["kiwi", "fig", "banana"]
```

```swift
let byLength = { (a: String, b: String) -> Bool in
```

```swift
    a.count < b.count
```

```swift
}
```

```swift
print(words.sorted(by: byLength))
```

# --solutions--

```swift
let words = ["kiwi", "fig", "banana"]
let byLength = { (a: String, b: String) -> Bool in
    a.count < b.count
}
print(words.sorted(by: byLength))
```

# --output--

["fig", "kiwi", "banana"]
