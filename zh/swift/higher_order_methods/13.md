---
language: swift
exerciseType: 1
---

# --description--

`Dictionary(grouping:by:)` 把一个集合拆分成一个由数组组成的字典。闭包计算每个元素的**键**，所有具有相同键的元素最终进入同一个数组：
```swift
let words = ["apple", "bee", "avocado"]
let byInitial = Dictionary(grouping: words, by: { $0.first! })
print(byInitial["a"]!) // ["apple", "avocado"]
```
`mapValues` 在保留键的同时转换字典的每个值，因此它是分组之后的自然下一步：
```swift
let sizes = byInitial.mapValues { $0.count }
print(sizes["a"]!) // 2
```

# --instructions--

编写一个函数 `countByLength`，它接收一个 `String` 数组（省略参数标签），并使用 `Dictionary(grouping:by:)` 和 `mapValues` 返回一个 `[Int: Int]` 类型的字典，把每个单词长度映射到具有该长度的单词数量

函数调用示例：
```swift
print(countByLength(["hi", "yo", "hey"])[2]!)
// prints 2
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

必须使用 `Dictionary(grouping:by:)`

```json
{
  "regex": "Dictionary\\s*\\(\\s*grouping",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

必须使用 `mapValues`

```json
{
  "regex": "\\.mapValues",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`countByLength(["hi", "yo", "hey"])` 必须返回 `[2: 2, 3: 1]`

```swift
tryCatch(countByLength(["hi", "yo", "hey"]) == [2: 2, 3: 1])
```

`countByLength(["a", "bb", "cc", "dd"])` 必须返回 `[1: 1, 2: 3]`

```swift
tryCatch(countByLength(["a", "bb", "cc", "dd"]) == [1: 1, 2: 3])
```

`countByLength([])` 必须返回一个空字典

```swift
tryCatch(countByLength([]) == [:])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func countByLength(_ words: [String]) -> [Int: Int] {
    return Dictionary(grouping: words, by: { $0.count }).mapValues { $0.count }
}
```
