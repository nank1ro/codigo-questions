---
language: swift
exerciseType: 1
---

# --description--

`enumerated()` 把数组转换成一系列 `(offset, element)` 对，因此闭包可以同时使用每个元素的位置和它的值：
```swift
let steps = ["mix", "bake"]
let numbered = steps.enumerated().map { pair in
    "\(pair.offset + 1). \(pair.element)"
}
print(numbered) // ["1. mix", "2. bake"]
```
由于每一对都是一个元组，闭包还可以对它进行解构：`.map { (i, step) in "\(i + 1). \(step)" }`。

# --instructions--

编写一个函数 `evenPositions`，它接收一个 `String` 数组（省略参数标签），并使用 `enumerated()`、`filter` 和 `map` 返回位于偶数索引（0、2、4、……）处的元素

函数调用示例：
```swift
print(evenPositions(["a", "b", "c", "d", "e"]))
// prints ["a", "c", "e"]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

必须使用 `enumerated()`

```json
{
  "regex": "\\.enumerated\\s*\\(\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

不得使用 `for` 循环

```json
{
  "regex": "\\bfor\\b",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`evenPositions(["a", "b", "c", "d", "e"])` 必须返回 `["a", "c", "e"]`

```swift
tryCatch(evenPositions(["a", "b", "c", "d", "e"]) == ["a", "c", "e"])
```

`evenPositions(["x", "y"])` 必须返回 `["x"]`

```swift
tryCatch(evenPositions(["x", "y"]) == ["x"])
```

`evenPositions([])` 必须返回 `[]`

```swift
tryCatch(evenPositions([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func evenPositions(_ items: [String]) -> [String] {
    return items.enumerated().filter { $0.offset % 2 == 0 }.map { $0.element }
}
```
