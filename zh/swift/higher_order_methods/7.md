---
language: swift
exerciseType: 1
---

# --description--

高阶函数可以作用于任何数组，包括你自己结构体的数组。先链接 `filter` 再链接 `map` 是筛选元素并从每个元素中提取值的常用方式：
```swift
struct Book {
    var title: String
    var pages: Int
}
let books = [Book(title: "Dune", pages: 412), Book(title: "Haiku", pages: 40)]
let long = books.filter { $0.pages > 100 }.map { $0.title }
print(long) // ["Dune"]
```
以相反的顺序进行，先 `map` 再 `filter`，会在检查使用 `pages` 属性之前就丢失它。

# --instructions--

结构体 `Product` 已给出。编写一个函数 `cheapNames`，它接收一个 `Product` 数组（省略参数标签），并链接 `filter` 和 `map`，按原始顺序返回 `price` 低于 `10` 的产品名称

函数调用示例：
```swift
print(cheapNames([Product(name: "Pen", price: 2.5), Product(name: "Bag", price: 30)]))
// prints ["Pen"]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Product {
    var name: String
    var price: Double
}
```

# --asserts--

必须使用 `filter`

```json
{
  "regex": "\\.filter",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

必须使用 `map`

```json
{
  "regex": "\\.map",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

对于价格为 `2.5` 的钢笔和价格为 `30` 的包，`cheapNames` 必须返回 `["Pen"]`

```swift
tryCatch(cheapNames([Product(name: "Pen", price: 2.5), Product(name: "Bag", price: 30)]) == ["Pen"])
```

`cheapNames` 必须保持原始顺序

```swift
tryCatch(cheapNames([Product(name: "Tea", price: 4), Product(name: "Cup", price: 9.99), Product(name: "Pot", price: 25)]) == ["Tea", "Cup"])
```

价格恰好为 `10` 的产品必须被排除

```swift
tryCatch(cheapNames([Product(name: "Cap", price: 10)]) == [])
```

`cheapNames([])` 必须返回 `[]`

```swift
tryCatch(cheapNames([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Product {
    var name: String
    var price: Double
}
func cheapNames(_ products: [Product]) -> [String] {
    return products.filter { $0.price < 10 }.map { $0.name }
}
```
