---
language: swift
exerciseType: 1
---

# --description--

有些高阶函数回答关于集合的问题，而不是转换集合。它们都接收一个返回 `Bool` 的闭包：
- `first(where:)` 返回第一个满足闭包的元素，如果没有则返回 `nil`
- `contains(where:)` 如果至少有一个元素满足它，则返回 `true`
- `allSatisfy` 如果所有元素都满足它，则返回 `true`

```swift
let nums = [4, 9, 16]
print(nums.first(where: { $0 > 5 }))  // Optional(9)
print(nums.contains(where: { $0 > 5 })) // true
print(nums.allSatisfy { $0 > 5 })       // false
```
与 `filter` 不同，`first(where:)` 在第一个匹配处停止，并且不会构建新数组。

# --instructions--

编写一个函数 `firstLong`，它接收一个 `String` 数组和一个名为 `minLength` 的 `Int`（省略两个参数标签），并使用 `first(where:)` 返回第一个至少有 `minLength` 个字符的字符串，如果没有则返回 `nil`

函数调用示例：
```swift
print(firstLong(["hi", "hello", "hey"], 4))
// prints Optional("hello")
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

必须使用 `first(where:)`

```json
{
  "regex": "\\.first\\s*\\(\\s*where",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstLong(["hi", "hello", "hey"], 4)` 必须返回 `"hello"`

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 4) == "hello")
```

`firstLong(["hi", "hello", "hey"], 2)` 必须返回 `"hi"`

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 2) == "hi")
```

`firstLong(["hi", "hey"], 4)` 必须返回 `nil`

```swift
tryCatch(firstLong(["hi", "hey"], 4) == nil)
```

`firstLong([], 1)` 必须返回 `nil`

```swift
tryCatch(firstLong([], 1) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstLong(_ words: [String], _ minLength: Int) -> String? {
    return words.first(where: { $0.count >= minLength })
}
```
