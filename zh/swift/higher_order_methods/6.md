---
language: swift
exerciseType: 4
---

# --description--

`contains(where:)` 和 `allSatisfy` 取代了使用循环加标志变量的常见模式。两者都会在答案确定时立即停止：`contains(where:)` 在第一个匹配处停止，`allSatisfy` 在第一个不满足的元素处停止。
```swift
let ages = [15, 22, 40]
let anyMinor = ages.contains { $0 < 18 }  // true
let allAdults = ages.allSatisfy { $0 >= 18 } // false
```
`contains { ... }` 是 `contains(where:)` 的尾随闭包形式，不要与查找特定值的 `contains(_:)` 混淆。

# --instructions--

排列这些代码块，检查每个分数是否至少为 `60`，以及是否有分数恰好为 `100`，然后把两个结果打印在同一行上。
先声明 `scores`，最后打印

# --answers--

```swift
let scores = [72, 100, 64]
```

```swift
let allPassed = scores.allSatisfy { $0 >= 60 }
```

```swift
let anyPerfect = scores.contains { $0 == 100 }
```

```swift
print("\(allPassed) \(anyPerfect)")
```

# --solutions--

```swift
let scores = [72, 100, 64]
let allPassed = scores.allSatisfy { $0 >= 60 }
let anyPerfect = scores.contains { $0 == 100 }
print("\(allPassed) \(anyPerfect)")
```

```swift
let scores = [72, 100, 64]
let anyPerfect = scores.contains { $0 == 100 }
let allPassed = scores.allSatisfy { $0 >= 60 }
print("\(allPassed) \(anyPerfect)")
```

# --output--

true true
