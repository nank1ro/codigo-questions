---
language: swift
exerciseType: 1
---

# --description--

数组上的 `map` 和 `filter` 是**急切**的：每一步都会处理整个数组并构建一个新数组，然后下一步才会运行。在大型集合上，或者当你只需要第一个结果时，这就是浪费的工作。
`lazy` 属性返回一个视图，其操作只有当元素真正被请求时才会运行，整个链条一次处理一个元素：
```swift
let firstBig = (1...1000).lazy.map { $0 * $0 }.first { $0 > 50 }
print(firstBig!) // 64
```
这里只有 `1, 2, ..., 8` 被平方：`first(where:)` 请求元素直到有一个满足条件，链条就在那里停止。如果没有 `lazy`，`map` 会先把全部 1000 个数字都平方。

# --instructions--

编写一个函数 `firstCubeAbove`，它接收一个名为 `limit` 的 `Int`（省略参数标签），并在范围 `1...limit + 1` 上使用 `lazy`、`map` 和 `first(where:)`，返回大于 `limit` 的最小立方数（`n` 从 `1` 开始时的 `n * n * n`）

函数调用示例：
```swift
print(firstCubeAbove(10))
// prints 27
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

必须使用 `lazy`

```json
{
  "regex": "\\.lazy",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

不得使用循环

```json
{
  "regex": "\\b(for|while)\\b",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`firstCubeAbove(10)` 必须返回 `27`

```swift
tryCatch(firstCubeAbove(10) == 27)
```

`firstCubeAbove(27)` 必须返回 `64`

```swift
tryCatch(firstCubeAbove(27) == 64)
```

`firstCubeAbove(0)` 必须返回 `1`

```swift
tryCatch(firstCubeAbove(0) == 1)
```

`firstCubeAbove(1000000)` 必须返回 `1030301`

```swift
tryCatch(firstCubeAbove(1000000) == 1030301)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstCubeAbove(_ limit: Int) -> Int {
    return (1...limit + 1).lazy.map { $0 * $0 * $0 }.first(where: { $0 > limit })!
}
```
