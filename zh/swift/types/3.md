---
language: swift
exerciseType: 1
---

# --description--

`type(of:)` 函数返回一个值的类型，用它来检查 Swift 推断出了什么类型非常方便：
```swift
print(type(of: 42))    // Int
print(type(of: 2.5))   // Double
print(type(of: "hi"))  // String
```
当你想要与推断出的类型不同的类型时，就添加注解。整数字面量可以存储在 `Double` 或 `Float` 常量中，单字符字面量可以存储在 `Character` 常量中：
```swift
let ratio: Double = 3       // 3.0, not an Int
let half: Float = 0.5
let initial: Character = "S"
print(type(of: ratio))      // Double
```

# --instructions--

声明三个常量：
- `ratio`，一个持有整数字面量 `3` 的 `Double`
- `half`，一个持有 `0.5` 的 `Float`
- `initial`，一个持有 `"S"` 的 `Character`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`ratio` 必须是一个等于 `3.0` 的 `Double`

```swift
tryCatch(type(of: ratio) == Double.self && ratio == 3.0)
```

`half` 必须是一个等于 `0.5` 的 `Float`

```swift
tryCatch(type(of: half) == Float.self && half == 0.5)
```

`initial` 必须是一个等于 `"S"` 的 `Character`

```swift
tryCatch(type(of: initial) == Character.self && initial == "S")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let ratio: Double = 3
let half: Float = 0.5
let initial: Character = "S"
```
