---
language: swift
exerciseType: 2
---

# --description--

Swift 从不自动在数字类型之间转换：将 `Int` 与 `Double` 相加是一个编译错误，即使它们都是数字。
```swift
let apples = 3
let price = 1.5
let total = apples * price // error: Int and Double can't be mixed
```
要把它们结合起来，你需要将该值传入类型的初始化器，创建一个所需类型的新值：
```swift
let total = Double(apples) * price // 4.5
```
反过来也一样：`Int(4.5)` 会产生一个 `Int`，只保留数字的整数部分。

# --instructions--

补全转换，使 `total` 是以 `Double` 表示的苹果费用，`whole` 是以 `Int` 表示的它的整数部分，使程序打印 `4.5` 和 `4`

# --seed--

```swift
let apples = 3
let price = 1.5
let total = [/](apples) * price
let whole = [/](total)
print(total)
print(whole)
```

# --answers--

- Double
- Int
- Float
- String
- Character

# --solutions--

```swift
let apples = 3
let price = 1.5
let total = Double(apples) * price
let whole = Int(total)
print(total)
print(whole)
```

# --output--

4.5
4
