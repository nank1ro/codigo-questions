---
language: dart
exerciseType: 1
---

# --instructions--

声明两个顶级 `int` 变量：`total` 的值为 `29`，`perGroup` 的值为 `4`。然后声明一个顶级 `int` 变量，名称为 `groups`，存储 `29` 中有多少个完整的 `4` 组（使用整数除法 `~/`）。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`groups` 应该等于 `7`

```dart
  test('test1', () {
    expect(groups, 7, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 29;
int perGroup = 4;
int groups = total ~/ perGroup;

void main() {
  print(groups);
}
```
