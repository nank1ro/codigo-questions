---
language: dart
exerciseType: 1
---

# --instructions--

一家商店以 `pricePerItem` 美元的价格出售 `itemCount` 件商品，从总额中扣除 `discount` 美元。声明这三个顶级 `int` 变量：`itemCount` = `6`，`pricePerItem` = `15`，`discount` = `10`。然后声明一个顶级 `int` 变量，名称为 `totalPrice`，计算折扣后的总价。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`totalPrice` 应该等于 `80`

```dart
  test('test1', () {
    expect(totalPrice, 80, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int itemCount = 6;
int pricePerItem = 15;
int discount = 10;
int totalPrice = itemCount * pricePerItem - discount;

void main() {
  print(totalPrice);
}
```
