---
language: dart
exerciseType: 3
---

# --instructions--

在 Dart 中，当使用 `+` 运算符处理两个 `int` 值时，它做什么？

# --answers--

- 将它们连接成字符串
- 将它们相加并返回一个 int
- 将它们相加并始终返回 double
- 将第一个数提升为第二个数的幂

# --solutions--

- 将它们相加并返回一个 int
