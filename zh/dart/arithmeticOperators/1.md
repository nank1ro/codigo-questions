---
language: dart
exerciseType: 2
---

# --description--

**加法运算符** `+` 将两个值相加。

```dart
int a = 3;
int b = 4;
int sum = a + b; // 7
```

你可以对 `int` 或 `double` 值使用 `+`。

# --instructions--

完成代码，将 `15` 和 `7` 相加并将结果存储在 `sum` 中。

# --seed--

```dart
void main() {
  int sum = 15 [/] 7;
  print(sum);
}
```

# --answers--

- +
- -
- *
- /

# --solutions--

```dart
void main() {
  int sum = 15 + 7;
  print(sum);
}
```

# --output--

22
