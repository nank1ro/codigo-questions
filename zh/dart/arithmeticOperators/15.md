---
language: dart
exerciseType: 1
---

# --instructions--

声明三个顶级 `int` 变量：`a` 的值为 `10`，`b` 的值为 `3`，`c` 的值为 `2`。然后声明：
- `sum` = `a + b`
- `product` = `b * c`
- `combined` = `sum - product`

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` 应该是 `13`，`product` 应该是 `6`，`combined` 应该是 `7`

```dart
  test('test1', () {
    expect(sum, 13, reason: '--err-t1--');
    expect(product, 6, reason: '--err-t2--');
    expect(combined, 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 10;
int b = 3;
int c = 2;
int sum = a + b;
int product = b * c;
int combined = sum - product;

void main() {
  print(combined);
}
```
