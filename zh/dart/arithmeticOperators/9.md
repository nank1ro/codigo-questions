---
language: dart
exerciseType: 1
---

# --instructions--

使用模运算符 `%` 找到 `17` 除以 `5` 的余数。将结果存储在名为 `remainder` 的顶级变量中。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

变量 `remainder` 应该等于 `2`

```dart
  test('test1', () {
    expect(remainder, 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 17 % 5;

void main() {
  print(remainder);
}
```
