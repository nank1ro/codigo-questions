---
language: dart
exerciseType: 2
---

# --description--

**减法运算符** `-` 从一个值中减去另一个值。

```dart
int a = 10;
int b = 4;
int diff = a - b; // 6
```

如果第二个操作数大于第一个，结果可能为负。

# --instructions--

完成代码，从 `20` 中减去 `9` 并将结果存储在 `diff` 中。

# --seed--

```dart
void main() {
  int diff = 20 [/] 9;
  print(diff);
}
```

# --answers--

- -
- +
- *
- /

# --solutions--

```dart
void main() {
  int diff = 20 - 9;
  print(diff);
}
```

# --output--

11
