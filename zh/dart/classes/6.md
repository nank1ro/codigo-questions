---
language: dart
exerciseType: 1
---

# --description--

`this` 指向类的**当前实例**。它用于区分实例变量和同名参数：

```dart
class Circle {
  double radius;

  Circle(double radius) {
    this.radius = radius;
  }
}
```

此处 `this.radius` 指字段，而 `radius`（不带 `this`）指构造函数参数。

# --instructions--

创建一个 `Rectangle` 类，包含两个 `double` 类型的字段 `width` 和 `height`。添加一个构造函数，接受 `width` 和 `height` 作为参数并通过 `this` 赋值。添加方法 `area()`，返回 `width * height`。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Rectangle` 的面积计算正确

```dart
    test('area', () {
      var r = Rectangle(4.0, 5.0);
      expect(r.area(), 20.0, reason: '--err-t1--');
    });
```

`width` 和 `height` 存储正确

```dart
    test('fields', () {
      var r = Rectangle(3.0, 7.0);
      expect(r.width, 3.0, reason: '--err-t2--');
      expect(r.height, 7.0, reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Rectangle {
  double width;
  double height;

  Rectangle(double width, double height) {
    this.width = width;
    this.height = height;
  }

  double area() {
    return width * height;
  }
}
```
