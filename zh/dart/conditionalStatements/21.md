---
language: dart
exerciseType: 1
---

# --instructions--

编写函数 `minOf(int a, int b)`，使用三元运算符返回两个数中较小的一个。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`minOf(3, 7)` 必须返回 `3`

```dart
  test('test1', () {
    expect(minOf(3, 7), 3, reason: '--err-t1--');
  });
```

`minOf(10, 2)` 必须返回 `2`

```dart
  test('test2', () {
    expect(minOf(10, 2), 2, reason: '--err-t2--');
  });
```

`minOf(5, 5)` 必须返回 `5`

```dart
  test('test3', () {
    expect(minOf(5, 5), 5, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int minOf(int a, int b) {
  return a < b ? a : b;
}

void main() {
  print(minOf(3, 7));
}
```
