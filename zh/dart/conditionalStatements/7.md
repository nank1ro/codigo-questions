---
language: dart
exerciseType: 3
---

# --instructions--

`if` 语句在 Dart 中有什么作用？

# --answers--

- 它始终执行其块内的代码
- 它仅在条件为 `true` 时执行其块
- 它在条件为 `true` 时重复执行其块
- 它定义一个新函数

# --solutions--

- 它仅在条件为 `true` 时执行其块
