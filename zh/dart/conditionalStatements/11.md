---
language: dart
exerciseType: 1
---

# --instructions--

声明一个名为 `isHot` 的顶级 `bool` 变量。当顶级 `int` 变量 `temp`（设为 `35`）大于 `30` 时，使用 `if` 语句将其赋值为 `true`。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`temp` 必须等于 `35`

```dart
  test('test1', () {
    expect(temp, 35, reason: '--err-t1--');
  });
```

`isHot` 必须为 `true`

```dart
  test('test2', () {
    expect(isHot, true, reason: '--err-t2--');
    expect(isHot, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int temp = 35;
bool isHot = false;

void main() {
  if (temp > 30) {
    isHot = true;
  }
  print(isHot);
}
```
