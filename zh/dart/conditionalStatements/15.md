---
language: dart
exerciseType: 1
---

# --instructions--

编写函数 `absoluteValue(int n)`，使用三元运算符返回 `n` 的绝对值。当 `n` 为非负数时返回 `n`，否则返回 `-n`。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`absoluteValue(7)` 必须返回 `7`

```dart
  test('test1', () {
    expect(absoluteValue(7), 7, reason: '--err-t1--');
  });
```

`absoluteValue(-4)` 必须返回 `4`

```dart
  test('test2', () {
    expect(absoluteValue(-4), 4, reason: '--err-t2--');
  });
```

`absoluteValue(0)` 必须返回 `0`

```dart
  test('test3', () {
    expect(absoluteValue(0), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int absoluteValue(int n) {
  return n >= 0 ? n : -n;
}

void main() {
  print(absoluteValue(-4));
}
```
