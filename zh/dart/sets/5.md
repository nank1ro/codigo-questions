---
language: dart
exerciseType: 1
---

# --description--

要检查某个值是否在集合中，使用返回 `bool` 的 `.contains(value)`。当集合没有元素时 `.isEmpty` 为 `true`，当集合至少有一个元素时 `.isNotEmpty` 为 `true`：

```dart
var seen = {'x', 'y'};
print(seen.contains('x')); // true
print(seen.contains('z')); // false
print(seen.isEmpty);       // false
print(seen.isNotEmpty);    // true
```

# --instructions--

给定集合 `seen`，把它是否包含 `'x'` 存入名为 `hasX` 的 `bool`，把它是否包含 `'z'` 存入名为 `hasZ` 的 `bool`，把它是否为空存入名为 `empty` 的 `bool`。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var seen = {'x', 'y'};
```

# --after-seed--

```dart
  return [hasX, hasZ, empty];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasX` 应该是 `true`

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasZ` 应该是 `false`

```dart
  test('test2', () {
    expect(result()[1], false, reason: '--err-t2--');
  });
```

`empty` 应该是 `false`

```dart
  test('test3', () {
    expect(result()[2], false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var seen = {'x', 'y'};
bool hasX = seen.contains('x');
bool hasZ = seen.contains('z');
bool empty = seen.isEmpty;
```
