---
language: dart
exerciseType: 1
---

# --description--

交给 `Isolate.spawn` 的函数被称为**入口点**。它必须是一个顶层（或静态）函数，并且恰好接收一个参数：`Isolate.spawn` 传给它的消息。

```dart
void reportAnswer(SendPort port) {
  port.send(42);
}
```

到达 `ReceivePort` 的消息静态类型是 `dynamic`，因为任何值都可能被发送过来。当你知道另一个 isolate 发送的是什么时，就对它进行类型转换：

```dart
final receivePort = ReceivePort();
await Isolate.spawn(reportAnswer, receivePort.sendPort);
final answer = await receivePort.first as int;
```

# --instructions--

编写一个顶层函数 `sendGreeting`，它接收一个 `SendPort` 类型的 `port`，并向它发送字符串 `hello from the isolate`。

然后编写一个函数 `greetFromIsolate`，它返回一个 `Future<String>`：它创建一个 `ReceivePort`，用它的 `sendPort` spawn `sendGreeting`，并以收到的第一条消息完成。

示例：
```dart
print(await greetFromIsolate()); // hello from the isolate
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendGreeting` 应该向它接收到的端口发送 `'hello from the isolate'`

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendGreeting(port.sendPort);
    expect(await port.first, 'hello from the isolate', reason: '--err-t1--');
  });
```

`greetFromIsolate()` 应该以 `'hello from the isolate'` 完成

```dart
  test('test2', () async {
    expect(await greetFromIsolate(), 'hello from the isolate', reason: '--err-t2--');
  });
```

`greetFromIsolate` 应该返回一个 `Future<String>`，并且被调用两次时都能正常工作

```dart
  test('test3', () async {
    expect(greetFromIsolate(), isA<Future<String>>(), reason: '--err-t3--');
    expect(await greetFromIsolate(), 'hello from the isolate', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendGreeting(SendPort port) {
  port.send('hello from the isolate');
}

Future<String> greetFromIsolate() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendGreeting, receivePort.sendPort);
  return await receivePort.first as String;
}
```
