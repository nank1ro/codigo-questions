---
language: dart
exerciseType: 4
---

# --description--

被 spawn 的 isolate 总是遵循同样的四个步骤：打开邮箱，带着邮箱的地址 spawn 工作单元，等待答案，使用它。

```dart
import 'dart:isolate';

void worker(SendPort port) {
  port.send('done');
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

`Isolate.spawn` 前面的 `await` 等待的是 isolate *启动*，而不是它完成工作：结果稍后通过端口到达。

# --instructions--

排列这些条目，构建一个程序：spawn 一个把 `7 * 6` 发送回主 isolate 的 `worker` isolate，主 isolate 把它打印出来。把 import 放在最前面，并在 `main` 之前定义 `worker`。程序必须打印 `42`。

# --answers--

```dart
import 'dart:isolate';
```

```dart
void worker(SendPort port) {
```

```dart
  port.send(7 * 6);
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  final receivePort = ReceivePort();
```

```dart
  await Isolate.spawn(worker, receivePort.sendPort);
```

```dart
  print(await receivePort.first);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
void worker(SendPort port) {
  port.send(7 * 6);
}
Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

# --output--

42
