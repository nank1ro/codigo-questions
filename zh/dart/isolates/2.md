---
language: dart
exerciseType: 1
---

# --description--

你交给 `Isolate.run` 的函数可以**捕获**它周围的变量。这些值会随函数一起被复制进新 isolate，因此计算可以依赖其调用者：

```dart
Future<int> triple(int n) {
  return Isolate.run(() => n * 3);
}
```

`Isolate.run` 返回一个 `Future`，其值就是函数的返回值，所以 `triple` 可以直接把它返回：当你只是把这个 future 传递出去时，不需要 `async` 也不需要 `await`。

把工作移到另一个 isolate 的意义在于，长时间的计算不会再冻结主 isolate。一个运行一秒钟的循环如果跑在主 isolate 上会阻塞一切；而在 `Isolate.run` 内部它跑在别处，主 isolate 得以继续处理自己的事件。

# --instructions--

编写一个函数 `sumTo`，它接收一个 `int` 类型的 `n`，返回一个 `Future<int>`，以从 `1` 到 `n` 的数字之和完成，且求和使用 `Isolate.run` 在另一个 isolate 上计算。

示例：
```dart
print(await sumTo(4)); // 10
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumTo(4)` 应该以 `10` 完成

```dart
  test('test1', () async {
    expect(await sumTo(4), 10, reason: '--err-t1--');
  });
```

`sumTo(100)` 应该以 `5050` 完成，`sumTo(1)` 以 `1` 完成

```dart
  test('test2', () async {
    expect(await sumTo(100), 5050, reason: '--err-t2--');
    expect(await sumTo(1), 1, reason: '--err-t2--');
  });
```

`sumTo(0)` 应该以 `0` 完成，并且 `sumTo` 应该返回一个 `Future<int>`

```dart
  test('test3', () async {
    expect(await sumTo(0), 0, reason: '--err-t3--');
    expect(sumTo(3), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<int> sumTo(int n) {
  return Isolate.run(() {
    var total = 0;
    for (var i = 1; i <= n; i++) {
      total += i;
    }
    return total;
  });
}
```
