---
language: dart
exerciseType: 1
---

# --description--

在一个 isolate 内部抛出的异常无法跳到另一个 isolate：两者的调用栈是分开的。`Isolate.run` 替你弥合了这个鸿沟：它捕获错误，把错误复制回来，并让返回的 future 以该错误失败。因此在你这一侧，它就是一个普通的异步错误，在 `await` 周围用 `try`/`catch` 捕获：

```dart
Future<int> parseInIsolate(String text) async {
  try {
    return await Isolate.run(() => int.parse(text));
  } catch (e) {
    return -1;
  }
}
```

`try` 里的 `await` 很重要，与任何其他 future 完全一样：没有它，future 会在 `try` 块尚未结束时离开，`catch` 就永远不会运行。

`Isolate.spawn` 没有这样的桥梁。一个未捕获的错误会悄悄杀死被 spawn 的 isolate，而父 isolate 会继续等待一条永远不会到来的消息，这是优先选用 `Isolate.run` 的又一个理由。

# --instructions--

编写一个函数 `safeDivide`，它接收两个 `int` 类型的 `a` 和 `b`，返回一个 `Future<int>`：它用 `Isolate.run` 在另一个 isolate 上计算 `a ~/ b`，如果计算抛出异常，则以 `-1` 完成。

示例：
```dart
print(await safeDivide(10, 2)); // 5
print(await safeDivide(7, 0));  // -1
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeDivide(10, 2)` 应该以 `5` 完成

```dart
  test('test1', () async {
    expect(await safeDivide(10, 2), 5, reason: '--err-t1--');
  });
```

`safeDivide(7, 0)` 应该以 `-1` 完成而不是失败

```dart
  test('test2', () async {
    expect(await safeDivide(7, 0), -1, reason: '--err-t2--');
  });
```

`safeDivide(-9, 3)` 应该以 `-3` 完成，并且 `safeDivide` 应该返回一个 `Future<int>`

```dart
  test('test3', () async {
    expect(await safeDivide(-9, 3), -3, reason: '--err-t3--');
    expect(safeDivide(4, 2), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<int> safeDivide(int a, int b) async {
  try {
    return await Isolate.run(() => a ~/ b);
  } catch (e) {
    return -1;
  }
}
```
