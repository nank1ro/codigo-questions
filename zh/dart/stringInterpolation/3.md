---
language: dart
exerciseType: 2
---

# --description--

字符串插值允许我们显示表达式，例如将字符串与数字相加，而不会产生任何错误。
将表达式放在 `${}` 中会对其求值。
返回值会被转换为字符串并插入到结果字符串中

# --seed--

```dart
print([/]);
```

# --answers--

- "${5} friends"

# --solutions--

```dart
print("${5} friends");
```

# --output--

5 friends
