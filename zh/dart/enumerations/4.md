---
language: dart
exerciseType: 1
---

# --description--

因为 `values` 是一个列表，你可以将它与你已经了解的列表方法结合使用。例如，`.map()` 配合 `.name` 可以把值转换成字符串列表：

```dart
enum Direction { north, south, east, west }

List<String> names = Direction.values.map((d) => d.name).toList();
print(names); // [north, south, east, west]
```

# --instructions--

声明一个枚举 `Weekday`，按此顺序包含值 `monday`、`tuesday`、`wednesday`、`thursday`、`friday`、`saturday` 和 `sunday`。然后编写一个函数 `dayNames()`，返回一个 `List<String>`，按声明顺序包含每个工作日的名称。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Weekday.values.length` 应该是 `7`

```dart
  test('test1', () {
    expect(Weekday.values.length, 7, reason: '--err-t1--');
  });
```

`Weekday.values[2]` 应该是 `Weekday.wednesday`

```dart
  test('test2', () {
    expect(Weekday.values[2], Weekday.wednesday, reason: '--err-t2--');
  });
```

`dayNames()` 必须按顺序返回这七个名称

```dart
  test('test3', () {
    expect(dayNames(), ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Weekday { monday, tuesday, wednesday, thursday, friday, saturday, sunday }

List<String> dayNames() {
  return Weekday.values.map((day) => day.name).toList();
}
```
