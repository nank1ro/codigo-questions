---
language: dart
exerciseType: 2
---

# --description--

从 Dart 3 开始，`switch` 也可以用作能产生值的**表达式**。每个 case 写成 `模式 => 值` 的形式，各个 case 之间用逗号分隔，不需要 `case` 关键字，也不需要 `break`：

```dart
enum Direction { north, south, east, west }

String arrow(Direction direction) => switch (direction) {
      Direction.north => '^',
      Direction.south => 'v',
      Direction.east => '>',
      Direction.west => '<',
    };
```

和语句形式一样，作用于枚举的 switch 表达式也必须是穷举的。

# --instructions--

补全这个 switch 表达式，使 `label` 返回每个尺寸的首字母，然后打印 `Size.large` 的 label。

# --seed--

```dart
enum Size { small, medium, large }

String label(Size size) => [/] (size) {
      Size.small [/] 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --answers--

- switch
- =>
- case
- when
- ->
- :

# --solutions--

```dart
enum Size { small, medium, large }

String label(Size size) => switch (size) {
      Size.small => 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --output--

L
