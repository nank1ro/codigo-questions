---
language: dart
exerciseType: 3
---

# --description--

每个枚举值都只存在一份，因此两个指向同一个值的引用总是相等的。使用 `==` 和 `!=` 比较它们：

```dart
enum Direction { north, south, east, west }

var heading = Direction.north;
print(heading == Direction.north); // true
print(heading != Direction.south); // true
print(Direction.values[0] == heading); // true
```

# --instructions--

给定 `enum Size { small, medium, large }`，`print(Size.medium == Size.values[1]);` 会打印什么？

# --answers--

- true
- false
- 1
- 无法编译

# --solutions--

- true
