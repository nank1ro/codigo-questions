---
language: dart
exerciseType: 1
---

# --instructions--

使用 `while` 循环找出第一个大于 `30` 且同时能被 `3` 和 `5` 整除的数字。返回该数字。

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` 必须等于 `45`

```dart
  test('test1', () {
    expect(computeValue(), 45, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int result = 31;
while (result % 3 != 0 || result % 5 != 0) {
  result++;
}
```
