---
language: dart
exerciseType: 3
---

# --instructions--

与 `while` 循环相比，`do-while` 循环有什么保证？

# --answers--

- 循环体至少执行一次，即使条件最初为 `false`
- 循环体最多执行一次
- 条件在循环体执行前检查
- 除非使用 `break`，否则循环永远运行

# --solutions--

- 循环体至少执行一次，即使条件最初为 `false`
