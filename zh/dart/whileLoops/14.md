---
language: dart
exerciseType: 1
---

# --instructions--

使用 `while` 循环从 `10` 倒数到 `1`（含）。返回执行的迭代次数。

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return iterations;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`iterations` 必须等于 `10`

```dart
  test('test1', () {
    expect(computeValue(), 10, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 10;
int iterations = 0;
while (n >= 1) {
  iterations++;
  n--;
}
```
