---
language: dart
exerciseType: 4
---

# --description--

带 `on` 子句的 mixin 可以把超类的成员当作自己的成员来使用，这正是它适合承载装饰现有类型之行为的原因：

```dart
class Shape {
  String get kind => 'shape';
}

mixin Printable on Shape {
  void show() {
    print('a $kind');
  }
}

class Square extends Shape with Printable {
  @override
  String get kind => 'square';
}
```

`Square` 覆盖了 `kind`，而 `show` 会自动采用覆盖后的版本：mixin 总是在真实对象上调用成员。

# --instructions--

排列这些条目，定义一个类 `Shape`，其中有 getter `kind` 返回 `'shape'`；一个限定于 `Shape` 的 mixin `Printable`，其中有方法 `show`，打印 `a ` 加上 `kind`；一个类 `Square`，继承 `Shape`、使用 `Printable` 并把 `kind` 覆盖为 `'square'`；以及一个在 `Square` 上调用 `show` 的 `main`。先定义 `Shape`，再定义 `Printable`，然后定义 `Square`，最后定义 `main`。程序必须打印：

```
a square
```

# --answers--

```dart
class Shape {
```

```dart
  String get kind => 'shape';
```

```dart
}
```

```dart
mixin Printable on Shape {
```

```dart
  void show() {
    print('a $kind');
  }
```

```dart
}
```

```dart
class Square extends Shape with Printable {
```

```dart
  @override
  String get kind => 'square';
```

```dart
}
```

```dart
void main() {
```

```dart
  Square().show();
```

```dart
}
```

# --solutions--

```dart
class Shape {
  String get kind => 'shape';
}

mixin Printable on Shape {
  void show() {
    print('a $kind');
  }
}

class Square extends Shape with Printable {
  @override
  String get kind => 'square';
}

void main() {
  Square().show();
}
```

# --output--

a square
