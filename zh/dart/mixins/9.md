---
language: dart
exerciseType: 3
---

# --description--

mixin 和 `extends` 可以协同工作。一个类可以既有超类**又有**一列 mixin，而 mixin 总是应用在超类**之上**：

```dart
class Document {
  String header() => 'Document';
}

mixin Timestamped {
  String header() => 'Timestamped';
}

class Report extends Document with Timestamped {}
```

这里的链条是 `Object` → `Document` → `Timestamped` → `Report`。成员查找从链条的末端开始，所以 `Report().header()` 会先找到 `Timestamped` 的版本。在超类和 mixin 中声明同一个成员是完全合法的：这正是 mixin 替换或包装继承行为的方式。

# --instructions--

`class Report extends Document with Timestamped {}`，且 `Document` 和 `Timestamped` 都声明了方法 `header`。`Report().header()` 调用的是哪一个？

# --answers--

- `Timestamped` 的，因为 mixin 应用在超类之上
- `Document` 的，因为超类总是胜过 mixin
- 都不是：在两者中都声明 `header` 是编译错误
- 两者都调用，先后执行，返回拼接后的两个结果

# --solutions--

- `Timestamped` 的，因为 mixin 应用在超类之上
