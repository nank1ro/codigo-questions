---
language: dart
exerciseType: 1
---

# --description--

类主体位于链条的最末端，所以类中声明的成员会**覆盖**来自任何 mixin 的同名成员。在覆盖内部，**`super`** 可以到达 mixin 提供的版本：

```dart
mixin Polite {
  String greet() => 'Hello';
}

class Host with Polite {
  @override
  String greet() => '${super.greet()}, welcome!';
}

class Guest with Polite {}

void main() {
  print(Host().greet());  // Hello, welcome!
  print(Guest().greet()); // Hello
}
```

mixin 本身不受影响：`Guest` 仍然得到原始的 `greet`。`super.greet()` 让 `Host` 在共享行为之上构建，而不是复制它。

# --instructions--

声明一个 mixin `Signed`，其中有方法 `sign` 返回 `'signed'`。

声明一个类 `Letter`，使用 `Signed` 并覆盖 `sign`，通过调用 `super` 得到 mixin 的结果，并在其后加上 `' by Ada'`。

声明一个类 `Note`，使用 `Signed` 且不覆盖任何成员。

示例：
```dart
print(Letter().sign()); // signed by Ada
print(Note().sign());   // signed
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Letter().sign()` 应该是 `'signed by Ada'`

```dart
  test('test1', () {
    expect(Letter().sign(), 'signed by Ada', reason: '--err-t1--');
  });
```

`Note().sign()` 仍然应该是 `'signed'`，因为 mixin 未被改变

```dart
  test('test2', () {
    expect(Note().sign(), 'signed', reason: '--err-t2--');
  });
```

两个类都应该使用该 mixin，并且通过 mixin 类型调用时也应该用到覆盖后的版本

```dart
  test('test3', () {
    final Signed letter = Letter();
    expect(letter.sign(), 'signed by Ada', reason: '--err-t3--');
    expect(Note(), isA<Signed>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Signed {
  String sign() => 'signed';
}

class Letter with Signed {
  @override
  String sign() => '${super.sign()} by Ada';
}

class Note with Signed {}
```
