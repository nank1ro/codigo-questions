---
language: dart
exerciseType: 2
---

# --description--

由于最后一个 mixin 获胜，`with` 列表的顺序是类含义的一部分，而不是风格细节。改变顺序会改变对象最终得到的实现：

```dart
mixin Plain {
  String format(String text) => text;
}

mixin Starred {
  String format(String text) => '*$text*';
}

class Fancy with Plain, Starred {}  // format comes from Starred
class Simple with Starred, Plain {} // format comes from Plain
```

只有一个 mixin 声明的成员永远不存在竞争：无论顺序如何，它们都可用。把 `with X, Y` 读作“从 `X` 开始，然后让 `Y` 覆盖它”。

# --instructions--

补全 `with` 列表，使 `Message` 各使用每个 mixin 一次，它的 `format` 保持文本不变，且 `frame` 可用。

# --seed--

```dart
mixin Plain {
  String format(String text) => text;
}

mixin Starred {
  String format(String text) => '*$text*';

  String frame(String text) => '($text)';
}

class Message with [/], [/] {}

void main() {
  print(Message().format('hi'));
  print(Message().frame('hi'));
}
```

# --answers--

- Starred
- Plain
- Message
- format
- frame

# --solutions--

```dart
mixin Plain {
  String format(String text) => text;
}

mixin Starred {
  String format(String text) => '*$text*';

  String frame(String text) => '($text)';
}

class Message with Starred, Plain {}

void main() {
  print(Message().format('hi'));
  print(Message().frame('hi'));
}
```

# --output--

hi
(hi)
