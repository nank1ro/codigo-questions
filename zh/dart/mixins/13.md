---
language: dart
exerciseType: 1
---

# --description--

一旦 mixin 有了 `on` 子句，它就可以**覆盖**该类型的成员，并调用 **`super`** 到达链条中位于它下方的版本：

```dart
class Logger {
  String log(String message) => message;
}

mixin Timestamped on Logger {
  @override
  String log(String message) => '[12:00] ${super.log(message)}';
}

class AppLogger extends Logger with Timestamped {}

void main() {
  print(AppLogger().log('started')); // [12:00] started
}
```

`super.log` 不是 mixin 自己的 `log`，而是它下方的那一个，因此不会无限递归。用 `with A, B` 叠放多个这样的 mixin 时，每一个都包装前一个：调用先进入**最后**一个 mixin，再向下传递到超类。

# --instructions--

声明一个类 `Pipeline`，其中有方法 `run`，接受一个 `String` 参数 `input` 并原样返回它。

声明一个作用于 `Pipeline` 的 mixin `Trimmed`，它覆盖 `run`，把去除首尾空白后的输入传给 `super.run`；再声明一个作用于 `Pipeline` 的 mixin `Upper`，它覆盖 `run`，把 `super.run` 的结果以大写形式返回。

声明一个类 `TextPipeline`，继承 `Pipeline` 并按先 `Trimmed` 后 `Upper` 的顺序使用它们；再声明一个类 `TrimOnly`，继承 `Pipeline` 且只使用 `Trimmed`。

示例：
```dart
print(TextPipeline().run('  hi  ')); // HI
print(TrimOnly().run('  hi  '));     // hi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`TextPipeline().run('  hi  ')` 应该是 `'HI'`

```dart
  test('test1', () {
    expect(TextPipeline().run('  hi  '), 'HI', reason: '--err-t1--');
    expect(TextPipeline().run('dart'), 'DART', reason: '--err-t1--');
  });
```

`Pipeline().run('  hi  ')` 仍然应该原样返回输入

```dart
  test('test2', () {
    expect(Pipeline().run('  hi  '), '  hi  ', reason: '--err-t2--');
  });
```

`TrimOnly().run('  hi  ')` 应该是 `'hi'`，且每个类都应该使用它需要的 mixin

```dart
  test('test3', () {
    expect(TrimOnly().run('  hi  '), 'hi', reason: '--err-t3--');
    expect(TrimOnly(), isA<Trimmed>(), reason: '--err-t3--');
    expect(TextPipeline(), isA<Upper>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Pipeline {
  String run(String input) => input;
}

mixin Trimmed on Pipeline {
  @override
  String run(String input) => super.run(input.trim());
}

mixin Upper on Pipeline {
  @override
  String run(String input) => super.run(input).toUpperCase();
}

class TextPipeline extends Pipeline with Trimmed, Upper {}

class TrimOnly extends Pipeline with Trimmed {}
```
