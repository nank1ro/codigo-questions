---
language: dart
exerciseType: 1
---

# --description--

mixin 的主体看起来和类的主体一样：方法、getter 和字段，写法完全相同。区别在于你能对声明本身做什么。

```dart
mixin Timestamped {
  String get stamp => '[log]';

  String format(String text) => '$stamp $text';
}

class Server with Timestamped {}
class Printer with Timestamped {}
```

mixin 的名字也是一个**类型**，因此 `Server() is Timestamped` 为 `true`，变量可以声明为 `Timestamped t = Server();`。两个毫无关联的类就这样共享同一份实现，而无须一方继承另一方。

# --instructions--

声明一个 mixin `Shouter`，其中有一个方法 `shout`，接受一个 `String` 参数 `text`，返回全大写的文本并在末尾加上感叹号。

声明一个类 `Speaker`，使用 `Shouter` 且不添加任何自己的成员。

示例：
```dart
print(Speaker().shout('hi')); // HI!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Speaker().shout('hi')` 应该是 `'HI!'`

```dart
  test('test1', () {
    expect(Speaker().shout('hi'), 'HI!', reason: '--err-t1--');
  });
```

`Speaker().shout('Dart rocks')` 应该是 `'DART ROCKS!'`，`Speaker().shout('')` 应该是 `'!'`

```dart
  test('test2', () {
    expect(Speaker().shout('Dart rocks'), 'DART ROCKS!', reason: '--err-t2--');
    expect(Speaker().shout(''), '!', reason: '--err-t2--');
  });
```

`Speaker` 应该从 mixin 获得 `shout`，所以 `Speaker` 也应该是一个 `Shouter`

```dart
  test('test3', () {
    expect(Speaker(), isA<Shouter>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Shouter {
  String shout(String text) => '${text.toUpperCase()}!';
}

class Speaker with Shouter {}
```
