---
language: dart
exerciseType: 1
---

# --instructions--
编写一个函数，返回 `List<int>` 中找到的第一个负数。使用带 `break` 的 `for` 循环在找到负数后立即停止。如果没有负数则返回 `0`。

# --before-seed--
```dart
int firstNegative(List<int> numbers) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
找到第一个负数
```dart
    test('finds first negative', () {
      expect(firstNegative([3, -5, -2, 1]), -5, reason: '--err-t1--');
    });
```

没有负数时返回 0
```dart
    test('no negative', () {
      expect(firstNegative([1, 2, 3]), 0, reason: '--err-t2--');
    });
```

第一个元素为负数
```dart
    test('first is negative', () {
      expect(firstNegative([-7, 4, 2]), -7, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = 0; i < numbers.length; i++) {
    if (numbers[i] < 0) {
      result = numbers[i];
      break;
    }
  }
```
