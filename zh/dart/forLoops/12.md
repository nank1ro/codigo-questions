---
language: dart
exerciseType: 1
---

# --instructions--
编写一个函数，接受 `List<String>` 并使用 `for-in` 循环将所有元素以空格连接，返回一个字符串。

# --before-seed--
```dart
String joinWords(List<String> words) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
用空格连接三个单词
```dart
    test('three words', () {
      expect(joinWords(['hello', 'dart', 'world']), 'hello dart world', reason: '--err-t1--');
    });
```

单个单词返回该单词
```dart
    test('single word', () {
      expect(joinWords(['only']), 'only', reason: '--err-t2--');
    });
```

空列表返回空字符串
```dart
    test('empty list', () {
      expect(joinWords([]), '', reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  String result = '';
  for (var word in words) {
    if (result.isEmpty) {
      result = word;
    } else {
      result += ' $word';
    }
  }
```
