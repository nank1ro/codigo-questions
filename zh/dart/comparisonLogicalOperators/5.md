---
language: dart
exerciseType: 2
---

# --description--

**大于或等于运算符** `>=` 在左值大于**或等于**右值时返回 `true`。

```dart
int score = 50;
bool passed = score >= 50; // true
```

与 `>` 不同，当两个值相等时，此运算符也返回 `true`。

# --instructions--

完成代码以检查 `7` 是否大于或等于 `7`，并将结果存储在 `isAtLeastSeven` 中。

# --seed--

```dart
void main() {
  bool isAtLeastSeven = 7 [/] 7;
  print(isAtLeastSeven);
}
```

# --answers--

- >=
- >
- <=
- ==

# --solutions--

```dart
void main() {
  bool isAtLeastSeven = 7 >= 7;
  print(isAtLeastSeven);
}
```

# --output--

true
