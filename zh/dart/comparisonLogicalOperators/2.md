---
language: dart
exerciseType: 2
---

# --description--

**不等于运算符** `!=` 在两个值**不同**时返回 `true`，在它们相同时返回 `false`。

```dart
int x = 3;
int y = 7;
bool result = x != y; // true
```

它是 `==` 的相反操作。

# --instructions--

完成代码以检查 `4` 是否不等于 `9`，并将结果存储在 `isDifferent` 中。

# --seed--

```dart
void main() {
  bool isDifferent = 4 [/] 9;
  print(isDifferent);
}
```

# --answers--

- !=
- ==
- !
- <>

# --solutions--

```dart
void main() {
  bool isDifferent = 4 != 9;
  print(isDifferent);
}
```

# --output--

true
