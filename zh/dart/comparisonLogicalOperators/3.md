---
language: dart
exerciseType: 2
---

# --description--

**大于运算符** `>` 在左值严格大于右值时返回 `true`。

```dart
int a = 8;
int b = 3;
bool result = a > b; // true
```

如果左值等于或小于右值，则返回 `false`。

# --instructions--

完成代码以检查 `15` 是否大于 `6`，并将结果存储在 `isGreater` 中。

# --seed--

```dart
void main() {
  bool isGreater = 15 [/] 6;
  print(isGreater);
}
```

# --answers--

- >
- <
- >=
- ==

# --solutions--

```dart
void main() {
  bool isGreater = 15 > 6;
  print(isGreater);
}
```

# --output--

true
