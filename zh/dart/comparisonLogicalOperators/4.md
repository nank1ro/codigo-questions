---
language: dart
exerciseType: 2
---

# --description--

**小于运算符** `<` 在左值严格小于右值时返回 `true`。

```dart
int a = 2;
int b = 9;
bool result = a < b; // true
```

如果左值等于或大于右值，则返回 `false`。

# --instructions--

完成代码以检查 `3` 是否小于 `10`，并将结果存储在 `isSmaller` 中。

# --seed--

```dart
void main() {
  bool isSmaller = 3 [/] 10;
  print(isSmaller);
}
```

# --answers--

- <
- >
- <=
- !=

# --solutions--

```dart
void main() {
  bool isSmaller = 3 < 10;
  print(isSmaller);
}
```

# --output--

true
