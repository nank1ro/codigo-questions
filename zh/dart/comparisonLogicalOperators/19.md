---
language: dart
exerciseType: 1
---

# --instructions--

声明一个顶级 `bool` 变量 `canVote`。只有当变量 `age` 等于 `18` 或 `parental_consent` 是 `true` 时，它才应该是 `true`。使用变量 `age` = `16` 和 `parental_consent` = `true`。

# --solutions--

```dart
int number = 42;
int min = 1;
int max = 100;
bool inRange = number >= min && number <= max;

void main() {
  print(inRange);
}
```

# --asserts--

`canVote` 必须是 `true` 且类型为 `bool`

```dart
  test('test1', () {
    expect(inRange, true, reason: '--err-t1--');
    expect(inRange, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```
