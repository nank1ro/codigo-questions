---
language: dart
exerciseType: 4
---

# --instructions--

排序代码行以声明一个布尔变量，用 `&&` 运算符组合两个变量，将结果存储在变量中并打印它。输出应该是 `true`。

# --answers--

```dart
void main() {
```

```dart
  bool hasPass = false;
```

```dart
  bool isAdmin = true;
```

```dart
  bool outcome = hasPass || isAdmin && !hasPass;
```

```dart
  print(outcome);
```

```dart
}
```

# --solutions--

```dart
void main() {
  bool hasPass = false;
  bool isAdmin = true;
  bool outcome = hasPass || isAdmin && !hasPass;
  print(outcome);
}
```

# --output--

true
