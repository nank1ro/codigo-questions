---
language: dart
exerciseType: 4
---

# --instructions--

对代码行进行排序，使用带 `ifAbsent` 的 `.update()` 统计列表中每个单词出现的次数，并打印结果 map。

# --answers--

```dart
void main() {
```

```dart
  var counts = <String, int>{};
```

```dart
  for (var word in ['a', 'b', 'a']) {
```

```dart
    counts.update(word, (n) => n + 1, ifAbsent: () => 1);
```

```dart
  }
```

```dart
  print(counts);
```

```dart
}
```

# --solutions--

```dart
void main() {
  var counts = <String, int>{};
  for (var word in ['a', 'b', 'a']) {
    counts.update(word, (n) => n + 1, ifAbsent: () => 1);
  }
  print(counts);
}
```

# --output--

{a: 2, b: 1}
