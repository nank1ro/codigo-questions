---
language: dart
exerciseType: 1
---

# --instructions--

给定列表 `colors`，构建一个名为 `counts` 的 `Map<String, int>`，其中每个键是一种颜色，值是该颜色在列表中出现的次数。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var colors = ['red', 'blue', 'red', 'green', 'red', 'blue'];
```

# --after-seed--

```dart
  return counts;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`counts` 应该是 `{'red': 3, 'blue': 2, 'green': 1}`

```dart
  test('test1', () {
    expect(result(), {'red': 3, 'blue': 2, 'green': 1}, reason: '--err-t1--');
  });
```

`counts` 应该有 3 个键

```dart
  test('test2', () {
    expect(result().length, 3, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var colors = ['red', 'blue', 'red', 'green', 'red', 'blue'];
var counts = <String, int>{};
for (var color in colors) {
  counts.update(color, (n) => n + 1, ifAbsent: () => 1);
}
```
