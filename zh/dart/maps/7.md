---
language: dart
exerciseType: 1
---

# --description--

`.keys` 属性返回 map 的所有键，`.values` 返回所有值，均按插入顺序排列。它们都是惰性的 `Iterable`，所以需要真正的 `List` 时要调用 `.toList()`：

```dart
var ages = {'Ann': 30, 'Bob': 25};
List<String> names = ages.keys.toList();
List<int> years = ages.values.toList();
print(names); // [Ann, Bob]
print(years); // [30, 25]
```

# --instructions--

给定 map `scores`，用它的键创建一个名为 `subjects` 的 `List<String>`，用它的值创建一个名为 `points` 的 `List<int>`。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var scores = {'math': 90, 'art': 75, 'gym': 60};
```

# --after-seed--

```dart
  return [subjects, points];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`subjects` 应该是 `['math', 'art', 'gym']`

```dart
  test('test1', () {
    expect(result()[0], ['math', 'art', 'gym'], reason: '--err-t1--');
  });
```

`points` 应该是 `[90, 75, 60]`

```dart
  test('test2', () {
    expect(result()[1], [90, 75, 60], reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var scores = {'math': 90, 'art': 75, 'gym': 60};
List<String> subjects = scores.keys.toList();
List<int> points = scores.values.toList();
```
