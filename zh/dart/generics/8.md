---
language: dart
exerciseType: 2
---

# --description--

在健全的空安全下，问号可以出现在两个不同的位置，而它们表示两种不同的含义：

```dart
Box<int?> a = Box(null); // a box that exists and holds a nullable int
Box<int>? b = null;      // no box at all, but if there is one it holds an int
```

在 `Box<int?>` 中，**类型实参**是可空的，所以 `a.value` 的类型是 `int?`，可能是 `null`，而 `a` 本身始终存在。在 `Box<int>?` 中，**变量**是可空的，所以 `b` 可能是 `null`，你需要 `b?.value` 或 `b!.value` 才能访问它的内部。

不带边界的 `T` 意味着 `T extends Object?`，所以像 `Box<int?>` 这样可空的类型实参是完全合法的。

# --instructions--

补全代码，使 `holdsNull` 是一个装着 `null` 的盒子，而 `missing` 是一个可能连盒子都没有的变量。

# --seed--

```dart
class Box<T> {
  final T value;
  Box(this.value);
}

void main() {
  final holdsNull = Box<[/]>(null);
  Box<int>[/] missing = null;
  print('value: ${holdsNull.value}');
  print('box: $missing');
}
```

# --answers--

- int?
- ?
- int
- !
- ??

# --solutions--

```dart
class Box<T> {
  final T value;
  Box(this.value);
}

void main() {
  final holdsNull = Box<int?>(null);
  Box<int>? missing = null;
  print('value: ${holdsNull.value}');
  print('box: $missing');
}
```

# --output--

value: null
box: null
