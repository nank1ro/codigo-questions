---
language: dart
exerciseType: 1
---

# --description--

一个类可以声明多个类型参数，用逗号分隔。`Map<K, V>` 是内置的例子：一个类型对应键，一个类型对应值。

```dart
class Entry<K, V> {
  final K key;
  final V value;

  Entry(this.key, this.value);
}

final e = Entry<String, int>('age', 30);
```

**顺序**是类型的一部分：`Entry<String, int>` 和 `Entry<int, String>` 是互不相关的类型，一个类型的值不能赋给另一个。类型参数还可以在返回类型中重新排列，方法正是借此返回一个调换了顺序的对象版本：

```dart
Entry<V, K> get flipped => Entry(value, key);
```

# --instructions--

声明一个泛型类 `Pair`，它有两个类型参数 `A` 和 `B`、一个 `A` 类型的 `final` 字段 `first` 和一个 `B` 类型的 `final` 字段 `second`，两者都由构造函数设置；还有一个 getter `swapped`，返回一个 `Pair<B, A>`，以相反的顺序保存同样的两个值。

示例：
```dart
final p = Pair<String, int>('age', 30);
print(p.first);         // age
print(p.swapped.first); // 30
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Pair<String, int>('age', 30)` 的 `first` 应该是 `'age'`，`second` 应该是 `30`

```dart
  test('test1', () {
    final pair = Pair<String, int>('age', 30);
    expect(pair.first, 'age', reason: '--err-t1--');
    expect(pair.second, 30, reason: '--err-t1--');
  });
```

`swapped` 应该返回一个 `Pair<int, String>`，其中的值已交换

```dart
  test('test2', () {
    final swapped = Pair<String, int>('age', 30).swapped;
    expect(swapped, isA<Pair<int, String>>(), reason: '--err-t2--');
    expect(swapped.first, 30, reason: '--err-t2--');
    expect(swapped.second, 'age', reason: '--err-t2--');
  });
```

`Pair` 应该能用于任何两个类型，而且交换两次应该得到原来的值

```dart
  test('test3', () {
    final pair = Pair<bool, String>(true, 'yes');
    expect(pair.swapped.first, 'yes', reason: '--err-t3--');
    expect(pair.swapped.second, true, reason: '--err-t3--');
    expect(pair.swapped.swapped, isA<Pair<bool, String>>(), reason: '--err-t3--');
    expect(pair.swapped.swapped.first, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Pair<A, B> {
  final A first;
  final B second;

  Pair(this.first, this.second);

  Pair<B, A> get swapped => Pair(second, first);
}
```
