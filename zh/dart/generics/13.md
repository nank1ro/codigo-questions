---
language: dart
exerciseType: 4
---

# --description--

同样的边界也完全适用于求最小的元素：只是比较结果的符号变了。当接收者排在前面时，`compareTo` 返回负数，所以 `item.compareTo(best) < 0` 表示“这个更小”。

# --instructions--

排列这些条目，定义一个以 `Comparable<T>` 为边界的泛型函数 `smallest`，返回列表中最小的元素；以及一个 `main`，打印 `['pear', 'fig', 'kiwi']` 中最小的一个。先定义函数，再定义 `main`。程序必须打印：

```
fig
```

# --answers--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
```

```dart
  var best = items.first;
```

```dart
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
```

```dart
  return best;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(smallest(['pear', 'fig', 'kiwi']));
```

```dart
}
```

# --solutions--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
  return best;
}

void main() {
  print(smallest(['pear', 'fig', 'kiwi']));
}
```

# --output--

fig
