---
language: dart
exerciseType: 2
---

# --description--

`List` 并不只是存放值，它存放的是**同一种类型**的值。类型写在尖括号里，紧跟在集合名称的后面：

```dart
List<String> names = ['Ada', 'Grace'];
List<int> scores = [10, 20];
```

这里的 `String` 和 `int` 是**类型实参**，而接受类型实参的类型称为**泛型**。列表类只编写一次，`List<String>` 和 `List<int>` 则是由它产生的两个不同的类型。

这样做的好处是编译器知道里面装的是什么：

```dart
names.add(42);          // error: 42 is not a String
print(names.first.toUpperCase()); // 正常：first 是 String
```

# --instructions--

补全代码，使 `names` 成为一个字符串列表，并把 `'Linus'` 添加进去。

# --seed--

```dart
void main() {
  List<[/]> names = ['Ada', 'Grace'];
  names.add([/]);
  print(names);
}
```

# --answers--

- String
- 'Linus'
- int
- 42
- bool

# --solutions--

```dart
void main() {
  List<String> names = ['Ada', 'Grace'];
  names.add('Linus');
  print(names);
}
```

# --output--

[Ada, Grace, Linus]
