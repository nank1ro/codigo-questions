---
language: dart
exerciseType: 1
---

# --description--

边界还可以提到类型参数本身。`Comparable<T>` 是所有懂得如何通过 `compareTo` 与自己同种类型进行比较的东西的接口：

```dart
print('fig'.compareTo('kiwi')); // 负数：fig 排在前面
print('kiwi'.compareTo('fig')); // positive
print('fig'.compareTo('fig'));  // 零
```

所以 `T extends Comparable<T>` 应读作“任何可以与自身比较的类型”，这正是排序或求最大值的函数所需要的：

```dart
T maxOf<T extends Comparable<T>>(T a, T b) => a.compareTo(b) >= 0 ? a : b;

print(maxOf('fig', 'kiwi')); // kiwi
```

`String` 和 `DateTime` 都直接满足它。`int` 和 `double` 实现的是 `Comparable<num>`，所以数字列表直接作为 `num` 来比较。

# --instructions--

编写一个泛型函数 `largest`，类型参数 `T` 以 `Comparable<T>` 为边界，它接受一个非空的 `List<T>` 参数 `items`，用 `compareTo` 进行比较，返回最大的元素。

示例：
```dart
print(largest(['fig', 'kiwi', 'apple'])); // kiwi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest(['fig', 'kiwi', 'apple'])` 应该返回 `'kiwi'`

```dart
  test('test1', () {
    expect(largest(['fig', 'kiwi', 'apple']), 'kiwi', reason: '--err-t1--');
  });
```

`largest` 也应该能处理数字和日期

```dart
  test('test2', () {
    expect(largest([3, 9, 5]), 9, reason: '--err-t2--');
    expect(
      largest([DateTime(2020), DateTime(2024), DateTime(2001)]),
      DateTime(2024),
      reason: '--err-t2--',
    );
  });
```

`largest` 对单元素列表应该返回唯一的元素，当最后一个元素最大时应该返回它，而且它应该接受显式的类型实参，比如 `largest<String>(['a', 'z'])`

```dart
  test('test3', () {
    expect(largest<String>(['a', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['only']), 'only', reason: '--err-t3--');
    expect(largest(['a', 'b', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['z', 'b', 'a']), 'z', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
T largest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) > 0) {
      best = item;
    }
  }
  return best;
}
```
