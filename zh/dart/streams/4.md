---
language: dart
exerciseType: 3
---

# --description--

每个流最终都会结束。对 `async*` 生成器来说，只要函数体执行完毕，流就**结束**了，无论它是跑到了末尾还是遇到了 `return`。对已结束的流的 `await for` 循环会退出，任何 `toList()` 的 future 都会完成。

流不会重新开始，也不会重复它的值：一旦结束，就一直是结束状态。

# --instructions--

当 `async*` 函数的函数体执行完毕时，它返回的流会怎样？

# --answers--

- 流结束了，对它的 `await for` 循环随之结束
- 流从第一个 `yield` 重新开始
- 流会一直开着，直到在它上面调用 `close()`
- 最后发出的值会再发出一次

# --solutions--

- 流结束了，对它的 `await for` 循环随之结束
