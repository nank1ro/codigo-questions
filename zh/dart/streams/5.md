---
language: dart
exerciseType: 2
---

# --description--

`await for` 会暂停当前函数，直到流结束。当你想**不等待**就对事件作出反应时，调用 `listen` 并传入一个回调：它每个事件被调用一次，而 `listen` 之后的代码会立即执行。

```dart
final ticks = Stream.fromIterable([1, 2]);
ticks.listen((tick) => print('tick $tick'));
print('not waiting');
// not waiting
// tick 1
// tick 2
```

`listen` 还接受一个命名参数 `onDone`，那是一个无参函数，在流结束时被调用：

```dart
final ticks2 = Stream.fromIterable([1, 2]);
ticks2.listen((tick) => print(tick), onDone: () => print('no more ticks'));
```

# --instructions--

补全代码，使流被监听、每个字母被打印，并在流结束时打印 `done`。程序必须打印 `listening`，然后是 `a`、`b` 和 `done`。

# --seed--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.[/]((letter) => print(letter), [/]: () => print('done'));
  print('listening');
}
```

# --answers--

- listen
- onDone
- onError
- forEach
- await

# --solutions--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.listen((letter) => print(letter), onDone: () => print('done'));
  print('listening');
}
```

# --output--

listening
a
b
done
