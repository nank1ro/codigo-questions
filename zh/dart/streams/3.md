---
language: dart
exerciseType: 1
---

# --description--

`await for` 循环不只能打印：它还可以更新在循环之前声明的变量。消费一个流并计算结果的函数必须标记为 `async`，并返回该结果的 `Future`：

```dart
Future<int> count(Stream<String> words) async {
  var total = 0;
  await for (final word in words) {
    total += word.length;
  }
  return total;
}
```

函数只有在流结束之后才会到达 `return`，所以调用方在等待这个 future 时才拿到最终的值：

```dart
print(await count(Stream.fromIterable(['hi', 'dart']))); // 6
```

# --instructions--

函数 `scores()` 返回一个 `Stream<int>`。使用对 `scores()` 的 `await for` 循环，把每个值都加到 `total` 上。

# --before-seed--

```dart
Stream<int> scores() => Stream.fromIterable([4, 7, 1, 9]);

Future<dynamic> result() async {
```

# --seed--

```dart
var total = 0;
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` 应该是 `21`

```dart
  test('test1', () async {
    expect(await result(), 21, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var total = 0;
await for (final score in scores()) {
  total += score;
}
```
