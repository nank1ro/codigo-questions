---
language: dart
exerciseType: 1
---

# --description--

除了 `toList()`，流还提供其他**消费**它全部事件并返回单个 `Future` 的方法：

- `first` 和 `last` 以第一个或最后一个事件完成
- `length` 以事件的数量完成
- `join(separator)` 以所有事件连接成的一个 `String` 完成
- `reduce(combine)` 两两合并事件，最终得到一个值

```dart
final numbers = Stream.fromIterable([3, 9, 2]);
print(await numbers.reduce((a, b) => a + b)); // 14
```

`reduce` 会把目前的结果和下一个事件传给 `combine`。如果流是空的，它会抛出异常，所以只在保证至少有一个事件时才使用它。

# --instructions--

编写两个函数：
- `largest`，接收一个 `Stream<int>` `source`，使用 `reduce` 返回一个以最大值完成的 `Future<int>`。流中总是至少有一个事件。
- `sentence`，接收一个 `Stream<String>` `words`，返回一个以所有单词用单个空格连接后的结果完成的 `Future<String>`。

示例：
```dart
print(await largest(Stream.fromIterable([3, 9, 2])));        // 9
print(await sentence(Stream.fromIterable(['I', 'am', 'Dart']))); // I am Dart
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

对 `3, 9, 2` 的流调用 `largest` 应该以 `9` 完成

```dart
  test('test1', () async {
    expect(await largest(Stream.fromIterable([3, 9, 2])), 9, reason: '--err-t1--');
  });
```

对 `-4, -1, -7` 的流调用 `largest` 应该以 `-1` 完成，对 `5` 的流调用 `largest` 应该以 `5` 完成

```dart
  test('test2', () async {
    expect(await largest(Stream.fromIterable([-4, -1, -7])), -1, reason: '--err-t2--');
    expect(await largest(Stream.fromIterable([5])), 5, reason: '--err-t2--');
  });
```

对 `'I', 'am', 'Dart'` 的流调用 `sentence` 应该以 `'I am Dart'` 完成

```dart
  test('test3', () async {
    expect(await sentence(Stream.fromIterable(['I', 'am', 'Dart'])), 'I am Dart', reason: '--err-t3--');
    expect(await sentence(Stream.fromIterable(['solo'])), 'solo', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> largest(Stream<int> source) {
  return source.reduce((a, b) => a > b ? a : b);
}

Future<String> sentence(Stream<String> words) {
  return words.join(' ');
}
```
