---
language: dart
exerciseType: 2
---

# --description--

控制器可以用命名构造函数 `StreamController<T>.broadcast()` 直接创建一个广播流。它的 `stream` 接受任意数量的监听者，每个事件都会按照它们订阅的顺序送达所有监听者：

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>.broadcast();
  controller.stream.listen((msg) => print('first: $msg'));
  controller.stream.listen((msg) => print('second: $msg'));
  controller.add('hi');
  controller.close();
}
// first: hi
// second: hi
```

和每个广播流一样，它不做缓冲：在某个监听者订阅之前添加的事件，对这个监听者来说就丢失了。

# --instructions--

补全代码，创建一个广播控制器，订阅两个监听者 `A` 和 `B`，发送值 `1` 和 `2`，然后关闭控制器，使程序打印 `A got 1`、`B got 1`、`A got 2`、`B got 2`。

# --seed--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.[/]();
  controller.stream.[/]((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.[/](1);
  controller.add(2);
  controller.[/]();
}
```

# --answers--

- broadcast
- listen
- add
- close
- asBroadcastStream
- subscribe
- sink

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.broadcast();
  controller.stream.listen((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
```

# --output--

A got 1
B got 1
A got 2
B got 2
