---
language: dart
exerciseType: 1
---

# --description--

`Stream.fromIterable` 需要事先拿到所有的值。要一个一个地**生产**值，就写一个**异步生成器**：函数体标记为 `async*`、返回类型为 `Stream<T>` 的函数。在它内部，`yield` 向流发送一个事件：

```dart
Stream<int> countTo(int n) async* {
  for (var i = 1; i <= n; i++) {
    yield i;
  }
}
```

调用 `countTo(3)` 时函数体并不会运行：它是惰性执行的，随着监听者索取值而逐步运行，函数体结束时流也就结束了。

要把所有事件收集到一个 `List` 中，调用 `toList()`。它返回 `Future<List<T>>`，所以你要用 `await` 等待它：

```dart
final values = await countTo(3).toList();
print(values); // [1, 2, 3]
```

# --instructions--

编写一个 `async*` 函数 `countdown`，它接收一个 `int` `from`，返回一个 `Stream<int>`，依次发出从 `from` 递减到 `1` 的值。如果 `from` 为 `0` 或更小，流不发出任何值。

示例：
```dart
print(await countdown(3).toList()); // [3, 2, 1]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countdown(3)` 应该发出 `3`、`2`、`1`

```dart
  test('test1', () async {
    expect(await countdown(3).toList(), [3, 2, 1], reason: '--err-t1--');
  });
```

`countdown(1)` 应该只发出 `1`

```dart
  test('test2', () async {
    expect(await countdown(1).toList(), [1], reason: '--err-t2--');
  });
```

`countdown(0)` 不应该发出任何值，并且 `countdown` 应该返回 `Stream<int>`

```dart
  test('test3', () async {
    expect(await countdown(0).toList(), <int>[], reason: '--err-t3--');
    expect(countdown(2), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> countdown(int from) async* {
  for (var i = from; i >= 1; i--) {
    yield i;
  }
}
```
