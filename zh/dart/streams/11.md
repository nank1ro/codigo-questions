---
language: dart
exerciseType: 1
---

# --description--

生成器在一个函数内部产生事件。当事件来自**别处**（一个按钮、一个网络回调、另一个对象）时，你需要一个 **`StreamController`**。它位于 `dart:async` 库中，所以文件必须以 `import 'dart:async';` 开头。

控制器拥有一个流，并让你把事件推入其中：

```dart
import 'dart:async';

Stream<int> dice() {
  final controller = StreamController<int>();
  controller.add(4);
  controller.add(2);
  controller.close();
  return controller.stream;
}
```

- `add(value)` 发送一个事件
- `close()` 结束这个流；忘记它就意味着监听者会一直等下去
- `stream` 就是监听者所消费的那个 `Stream`

在任何人监听之前添加的事件会被保存在缓冲区里，所以上面的代码是安全的：稍后到来的监听者仍然会收到 `4` 和 `2`。

# --instructions--

编写一个函数 `announce`，它接收一个 `List<String>` `names`，返回一个用 `StreamController` 构建的 `Stream<String>`：把每个名字以大写形式添加进去，关闭控制器并返回它的流。记得 `dart:async` 的导入。

示例：
```dart
print(await announce(['ada', 'linus']).toList()); // [ADA, LINUS]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`announce(['ada', 'linus'])` 应该发出 `'ADA'`、`'LINUS'`

```dart
  test('test1', () async {
    expect(await announce(['ada', 'linus']).toList(), ['ADA', 'LINUS'], reason: '--err-t1--');
  });
```

`announce(['Dart'])` 应该发出 `'DART'`

```dart
  test('test2', () async {
    expect(await announce(['Dart']).toList(), ['DART'], reason: '--err-t2--');
  });
```

`announce([])` 不应该发出任何值并且应该结束，并且 `announce` 应该返回 `Stream<String>`

```dart
  test('test3', () async {
    expect(await announce([]).toList(), <String>[], reason: '--err-t3--');
    expect(announce(['x']), isA<Stream<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
import 'dart:async';

Stream<String> announce(List<String> names) {
  final controller = StreamController<String>();
  for (final name in names) {
    controller.add(name.toUpperCase());
  }
  controller.close();
  return controller.stream;
}
```
