---
language: dart
exerciseType: 2
---

# --description--

你已经知道如何声明一个有名字的函数，例如 `void sayHello() { ... }`。Dart 还允许你编写**没有名字**的函数：**匿名函数**。它与命名函数的组成部分相同（括号中的参数和花括号中的函数体），但没有返回类型也没有名字：

```dart
(String name) {
  print('Hello, $name!');
}
```

由于它没有名字，通常的用法是把它存储在一个变量中，然后像调用函数一样调用这个变量：

```dart
var sayHello = (String name) {
  print('Hello, $name!');
};

sayHello('Dart'); // Hello, Dart!
```

注意闭括号后面的 `;`：这个赋值是一条普通的语句。

# --instructions--

补全代码，在 `greet` 中存储一个没有参数、打印 `Hello, anonymous!` 的匿名函数，然后调用它。

# --seed--

```dart
void main() {
  var greet = [/] {
    print('Hello, anonymous!');
  };
  [/];
}
```

# --answers--

- ()
- greet()
- greet
- void
- function
- greet[]

# --solutions--

```dart
void main() {
  var greet = () {
    print('Hello, anonymous!');
  };
  greet();
}
```

# --output--

Hello, anonymous!
