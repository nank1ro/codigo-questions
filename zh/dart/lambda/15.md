---
language: dart
exerciseType: 1
---

# --description--

匿名函数不必存储在任何地方：通过在它的闭括号后面直接写出参数，就可以**立即调用**它。这在计算一个值时非常方便，可以避免一些临时变量泄漏到周围的代码中：

```dart
var area = (double radius) {
  var pi = 3.14;
  return pi * radius * radius;
}(2.0);

print(area); // 12.56
```

这里 `pi` 只存在于匿名函数内部，而 `area` 接收返回的值。

# --instructions--

一个 `int` 变量 `limit` 已经定义好了。声明一个变量 `sum`，通过立即调用一个没有参数的匿名函数来初始化它，该函数用循环把从 `1` 到 `limit` 的所有整数相加并返回总和。

# --before-seed--

```dart
dynamic result(int limit) {
```

# --seed--

```dart
var sum = 
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` 应该是一个 `int`

```dart
  test('test1', () {
    expect(result(100), isA<int>(), reason: '--err-t1--');
  });
```

当 `limit` 为 `100` 时，`sum` 应该是 `5050`

```dart
  test('test2', () {
    expect(result(100), 5050, reason: '--err-t2--');
  });
```

当 `limit` 为 `10` 时，`sum` 应该是 `55`

```dart
  test('test3', () {
    expect(result(10), 55, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var sum = () {
  var total = 0;
  for (var i = 1; i <= limit; i++) {
    total += i;
  }
  return total;
}();
```
