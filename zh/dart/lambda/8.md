---
language: dart
exerciseType: 1
---

# --description--

由于 `map` 和 `where` 都返回 `Iterable`，它们的调用可以一个接一个地**链式**连接。每一步都接收上一步的结果，最后调用一次 `toList()`：

```dart
var numbers = [1, 2, 3, 4, 5, 6];

var result = numbers.where((n) => n > 3).map((n) => n * 10).toList();
print(result); // [40, 50, 60]
```

# --instructions--

编写一个函数 `oddSquares`，它接收一个 `List<int>` 参数 `numbers`，并返回一个 `List<int>`，其中只包含奇数的平方，保持原来的顺序。请配合匿名函数使用 `where` 和 `map`。

示例：
```dart
print(oddSquares([1, 2, 3, 4, 5])); // [1, 9, 25]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`oddSquares([1, 2, 3, 4, 5])` 应该是 `[1, 9, 25]`

```dart
  test('test1', () {
    expect(oddSquares([1, 2, 3, 4, 5]), [1, 9, 25], reason: '--err-t1--');
  });
```

`oddSquares([2, 4, 6])` 应该是 `[]`

```dart
  test('test2', () {
    expect(oddSquares([2, 4, 6]), [], reason: '--err-t2--');
  });
```

`oddSquares([7, 10, 3])` 应该是 `[49, 9]`

```dart
  test('test3', () {
    expect(oddSquares([7, 10, 3]), [49, 9], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> oddSquares(List<int> numbers) {
  return numbers.where((n) => n % 2 != 0).map((n) => n * n).toList();
}
```
