---
language: dart
exerciseType: 1
---

# --description--

在底层，字符串的每个字符都以数字形式存储，即它的**代码单元（code unit）**（一个 UTF-16 编码）。`.codeUnitAt(index)` 返回某个字符的编码，`.codeUnits` 返回整个编码列表。`String.fromCharCode(code)` 则相反，根据编码构建字符串：

```dart
var word = 'AB';
print(word.codeUnitAt(0));         // 65
print(word.codeUnits);             // [65, 66]
print(String.fromCharCode(67));    // C
```

连续的字母有连续的编码：`'A'` 是 65，`'B'` 是 66，以此类推。

# --instructions--

给定字符串 `letter`，将其代码单元存入名为 `code` 的 `int`，并将编码为 `code + 1` 的字符存入名为 `next` 的 `String`。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var letter = 'D';
```

# --after-seed--

```dart
  return [code, next];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`code` 应为 `68`

```dart
  test('test1', () {
    expect(result()[0], 68, reason: '--err-t1--');
  });
```

`next` 应为 `'E'`

```dart
  test('test2', () {
    expect(result()[1], 'E', reason: '--err-t2--');
  });
```

`next` 应为 `String` 类型

```dart
  test('test3', () {
    expect(result()[1], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var letter = 'D';
int code = letter.codeUnitAt(0);
String next = String.fromCharCode(code + 1);
```
