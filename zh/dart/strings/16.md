---
language: dart
exerciseType: 1
---

# --description--

由于字符串是不可变的，在循环中用 `+=` 拼接长文本会在每一步都创建一个新字符串。**StringBuffer** 可以高效地收集文本片段，只有在你需要时才生成最终的字符串：

- `.write(value)` 追加一个值（任何类型都会被转换为文本）
- `.writeln(value)` 追加该值，并在其后加上换行符
- `.toString()` 返回目前为止构建出的字符串

```dart
var buffer = StringBuffer();
buffer.write('Hello');
buffer.write(', ');
buffer.writeln('Dart!');
buffer.write(42);
print(buffer.toString()); // Hello, Dart!\n42
```

# --instructions--

给定列表 `items`，使用 `StringBuffer` 构建一个名为 `menu` 的 `String`，让每个项目独占一行，前面加上从 `1` 开始的序号和一个点，例如 `1. milk`。每一行都必须以换行符结尾（使用 `.writeln()`）。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var items = ['milk', 'eggs', 'bread'];
```

# --after-seed--

```dart
  return menu;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`menu` 应为 `'1. milk\n2. eggs\n3. bread\n'`

```dart
  test('test1', () {
    expect(result(), '1. milk\n2. eggs\n3. bread\n', reason: '--err-t1--');
  });
```

`menu` 应为 `String` 类型

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var items = ['milk', 'eggs', 'bread'];
var buffer = StringBuffer();
for (var i = 0; i < items.length; i++) {
  buffer.writeln('${i + 1}. ${items[i]}');
}
String menu = buffer.toString();
```
