---
language: dart
exerciseType: 1
---

# --description--

每个字符串都能通过它的 `.length` 属性知道自己包含多少个字符。空格和标点符号也算作字符：

```dart
var word = 'hello';
print(word.length);      // 5
print('a b'.length);     // 3
```

你可以用方括号加上**索引**来读取单个字符，索引从 `0` 开始。结果是一个单字符的 `String`：

```dart
print(word[0]);                // h
print(word[word.length - 1]);  // o
```

读取超出字符串范围的索引（比如 `word[5]`）会抛出错误。

# --instructions--

给定字符串 `word`，将其字符数量存入名为 `size` 的 `int`，将其第一个字符存入名为 `first` 的 `String`，将其最后一个字符存入名为 `last` 的 `String`。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var word = 'Flutter';
```

# --after-seed--

```dart
  return [size, first, last];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`size` 应为 `7`

```dart
  test('test1', () {
    expect(result()[0], 7, reason: '--err-t1--');
  });
```

`first` 应为 `'F'`

```dart
  test('test2', () {
    expect(result()[1], 'F', reason: '--err-t2--');
  });
```

`last` 应为 `'r'`

```dart
  test('test3', () {
    expect(result()[2], 'r', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var word = 'Flutter';
int size = word.length;
String first = word[0];
String last = word[word.length - 1];
```
