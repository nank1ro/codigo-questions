---
language: dart
exerciseType: 1
---

# --description--

两个字符串可以用 `+` 运算符连接成一个新的字符串，这称为**拼接（concatenation）**：

```dart
var greeting = 'Hello' + ', ' + 'Dart';
print(greeting); // Hello, Dart
```

Dart 还会将写在一起、不带任何运算符的两个字符串**字面量**自动连接起来。这在把一段长文本拆成多行书写时很方便：

```dart
var text = 'Hello, '
    'Dart';
print(text); // Hello, Dart
```

只有字符串才能用 `+` 拼接：`'Age: ' + 30` 会编译报错，因为 `30` 是 `int` 类型。

# --instructions--

给定变量 `first` 和 `last`，创建一个 `String` 类型的 `full`，包含名字、一个空格和姓氏。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var first = 'Ada';
var last = 'Lovelace';
```

# --after-seed--

```dart
  return full;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`full` 应为 `'Ada Lovelace'`

```dart
  test('test1', () {
    expect(result(), 'Ada Lovelace', reason: '--err-t1--');
  });
```

`full` 应为 `String` 类型

```dart
  test('test2', () {
    expect(result(), isA<String>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var first = 'Ada';
var last = 'Lovelace';
String full = first + ' ' + last;
```
