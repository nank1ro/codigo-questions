---
language: dart
exerciseType: 1
---

# --description--

最后一块拼图是故意抛出一个 `Error`。一个声明了自己接受什么的函数，应该大声拒绝其他任何东西，而 `ArgumentError` 就是为此准备的对象：

```dart
int setVolume(int level) {
  if (level < 0 || level > 100) {
    throw ArgumentError('level must be between 0 and 100');
  }
  return level;
}
```

消息可以通过 `e.message` 访问到，`toString()` 会打印 `Invalid argument(s): ` 加上它。

这与之前的规则并不矛盾。抛出一个 `ArgumentError` 是对的，捕获一个则不是：它是在告诉*调用者*的作者这次调用本身就是错的，正确的修复是在调用之前做检查，而不是在调用外面套一个处理程序。

# --instructions--

编写一个函数 `checkAge`，接受一个 `int` 类型的 `age` 并返回它，但当年龄低于 `0` 或高于 `120` 时抛出 `ArgumentError('age must be between 0 and 120')`。

示例：
```dart
print(checkAge(30)); // 30
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`checkAge(30)` 应该返回 `30`，并且范围的两个端点都应该被接受

```dart
  test('test1', () {
    expect(checkAge(30), 30, reason: '--err-t1--');
    expect(checkAge(0), 0, reason: '--err-t1--');
    expect(checkAge(120), 120, reason: '--err-t1--');
  });
```

`checkAge(-1)` 和 `checkAge(121)` 应该抛出一个 `ArgumentError`

```dart
  test('test2', () {
    expect(() => checkAge(-1), throwsArgumentError, reason: '--err-t2--');
    expect(() => checkAge(121), throwsArgumentError, reason: '--err-t2--');
  });
```

抛出的错误应该携带消息 `'age must be between 0 and 120'`

```dart
  test('test3', () {
    try {
      checkAge(200);
      fail('--err-t3--');
    } on ArgumentError catch (e) {
      expect(e.message, 'age must be between 0 and 120', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int checkAge(int age) {
  if (age < 0 || age > 120) {
    throw ArgumentError('age must be between 0 and 120');
  }
  return age;
}
```
