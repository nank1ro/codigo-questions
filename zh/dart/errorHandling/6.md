---
language: dart
exerciseType: 4
---

# --description--

处理程序后面还可以跟第三个块。`finally` 在**任何情况下**都会运行：`try` 块正常结束之后、某个处理程序恢复之后，以及没有任何子句匹配、失败仍在向外传播时。

```dart
try {
  return 'parsed ${int.parse(text)}';
} on FormatException {
  return 'failed';
} finally {
  print('done');
}
```

它甚至会在 `return` 把值交回给调用者之前运行，这就是为什么上面的消息会在调用者看到结果之前被打印。这使 `finally` 成为放置无论哪种结果都必须执行的工作的地方，比如关闭你打开的东西。

# --instructions--

对条目进行排序，定义一个函数 `attempt`，它返回 `'parsed '` 加上数字，或者当文本不是数字时返回 `'failed'`，并且在 `finally` 块中无论如何都打印 `done`。在 `main` 之前定义该函数。程序必须打印：

```
done
parsed 7
done
failed
```

# --answers--

```dart
String attempt(String text) {
```

```dart
  try {
    return 'parsed ${int.parse(text)}';
```

```dart
  } on FormatException {
    return 'failed';
```

```dart
  } finally {
    print('done');
  }
}
```

```dart
void main() {
```

```dart
  print(attempt('7'));
```

```dart
  print(attempt('x'));
```

```dart
}
```

# --solutions--

```dart
String attempt(String text) {
  try {
    return 'parsed ${int.parse(text)}';
  } on FormatException {
    return 'failed';
  } finally {
    print('done');
  }
}

void main() {
  print(attempt('7'));
  print(attempt('x'));
}
```

# --output--

done
parsed 7
done
failed
