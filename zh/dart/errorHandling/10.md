---
language: dart
exerciseType: 1
---

# --description--

`Exception` 是一个接口，所以你自己的类也可以实现它。自定义异常给失败起了一个 `on` 子句可以选择的名字，还带有处理程序可以读取的字段：

```dart
class EmptyCartException implements Exception {
  final String message;

  EmptyCartException(this.message);

  @override
  String toString() => 'EmptyCartException: $message';
}

throw EmptyCartException('nothing to pay for');
```

有三个部分值得保留：`implements Exception` 让这个类归入其他失败之列，一个携带详细信息的 `final` 字段，以及一个覆盖的 `toString()` 让未处理异常报告可读。没有这个覆盖，Dart 只会打印光秃秃的类名，详细信息就丢失了。

# --instructions--

定义一个实现 `Exception` 的类 `InvalidEmailException`，它有一个由构造函数设置的 `final String` 字段 `message`，以及一个返回 `'InvalidEmailException: '` 加上该消息的 `toString()`。

然后编写一个函数 `checkEmail`，接受一个 `String` 类型的 `email` 并返回它，但当邮箱不包含 `'@'` 时抛出 `InvalidEmailException('missing @')`。

示例：
```dart
print(checkEmail('ada@dart.dev')); // ada@dart.dev
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`checkEmail('ada@dart.dev')` 应该原样返回邮箱

```dart
  test('test1', () {
    expect(checkEmail('ada@dart.dev'), 'ada@dart.dev', reason: '--err-t1--');
  });
```

`checkEmail('nope')` 应该抛出一个 `InvalidEmailException`，它是一个 `Exception`

```dart
  test('test2', () {
    expect(() => checkEmail('nope'), throwsA(isA<InvalidEmailException>()), reason: '--err-t2--');
    expect(() => checkEmail('nope'), throwsA(isA<Exception>()), reason: '--err-t2--');
  });
```

抛出的对象应该携带消息 `'missing @'` 并打印为 `InvalidEmailException: missing @`

```dart
  test('test3', () {
    try {
      checkEmail('nope');
      fail('--err-t3--');
    } on InvalidEmailException catch (e) {
      expect(e.message, 'missing @', reason: '--err-t3--');
      expect(e.toString(), 'InvalidEmailException: missing @', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class InvalidEmailException implements Exception {
  final String message;

  InvalidEmailException(this.message);

  @override
  String toString() => 'InvalidEmailException: $message';
}

String checkEmail(String email) {
  if (!email.contains('@')) {
    throw InvalidEmailException('missing @');
  }
  return email;
}
```
