---
language: dart
exerciseType: 1
---

# --description--

把函数标记为 `async` 会做两件事：允许在函数体里使用 `await`，并让函数**返回一个 `Future`**。你用 `return` 返回的东西会成为该 future 完成时的值，所以即使函数体返回的是普通的 `T`，声明的返回类型也是 `Future<T>`：

```dart
Future<String> label(int n) async {
  return 'item $n';
}

Future<void> main() async {
  print(await label(3)); // item 3
}
```

这里不需要 `Future.value`：`async` 关键字会替你把返回值包装起来。

# --instructions--

编写一个 `async` 函数 `greet`，它接收一个 `String` `name`，返回一个 `Future<String>`，以 `Hello, ` 加上名字再加上一个感叹号完成。

示例：
```dart
print(await greet('Ada')); // Hello, Ada!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greet('Ada')` 应该以 `'Hello, Ada!'` 完成

```dart
  test('test1', () async {
    expect(await greet('Ada'), 'Hello, Ada!', reason: '--err-t1--');
  });
```

`greet('Linus')` 应该以 `'Hello, Linus!'` 完成

```dart
  test('test2', () async {
    expect(await greet('Linus'), 'Hello, Linus!', reason: '--err-t2--');
  });
```

`greet` 应该返回一个 `Future<String>`

```dart
  test('test3', () async {
    expect(greet('Bob'), isA<Future<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<String> greet(String name) async {
  return 'Hello, $name!';
}
```
