---
language: dart
exerciseType: 4
---

# --description--

`Future.wait` 是“加载多样东西，然后继续”的工具。典型写法是：构建 future 列表，对它 `await Future.wait`，然后使用得到的列表：

```dart
Future<int> stock() => Future.delayed(const Duration(milliseconds: 10), () => 8);
Future<int> orders() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<void> main() async {
  final counts = await Future.wait([stock(), orders()]);
  print(counts); // [8, 3]
}
```

`orders()` 先完成，但列表仍然按照调用顺序排列：`stock()` 在前，`orders()` 在后。

# --instructions--

排列这些条目，定义一个函数 `one`，它在 `10` 毫秒后以 `1` 完成；一个函数 `two`，它在 `5` 毫秒后以 `2` 完成；以及一个 `async` 的 `main`，它打印 `loading`，用 `Future.wait` 等待这两个函数并存入 `values`，然后打印 `values`。先定义 `one`，再定义 `two`，最后定义 `main`。程序必须打印：

```
loading
[1, 2]
```

# --answers--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
```

```dart
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);
```

```dart
Future<void> main() async {
```

```dart
  print('loading');
```

```dart
  final values = await Future.wait([one(), two()]);
```

```dart
  print(values);
```

```dart
}
```

# --solutions--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);

Future<void> main() async {
  print('loading');
  final values = await Future.wait([one(), two()]);
  print(values);
}
```

# --output--

loading
[1, 2]
