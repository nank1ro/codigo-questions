---
language: dart
exerciseType: 2
---

# --description--

错误会**传播**到每一个没有捕获它的 `await`。如果 `load()` 失败，`loadTwice()` 里的 `await load()` 就会抛出；由于 `loadTwice` 没有 `try`/`catch`，它自己的 future 也以同样的错误失败；就这样沿着调用链一路向上，直到某个 `await` 被 `try`/`catch` 包住：

```dart
Future<int> load() async {
  throw StateError('offline');
}

Future<int> loadTwice() async {
  final n = await load();   // throws here, loadTwice fails too
  return n * 2;             // never runs
}

Future<void> main() async {
  try {
    print(await loadTwice());
  } catch (e) {
    print('failed: $e');    // failed: Bad state: offline
  }
}
```

这与异常在同步调用中传播的方式一致：你只在知道该怎么办的那一层处理一次。

# --instructions--

补全代码，让 `fetch` 是异步的并抛出异常，`fetchLength` await 它，而 `main` 捕获错误并打印 `failed: ` 加上错误。程序必须打印 `failed: Bad state: no server`。

# --seed--

```dart
Future<String> fetch() [/] {
  throw StateError('no server');
}

Future<int> fetchLength() async {
  final text = [/] fetch();
  return text.length;
}

Future<void> main() async {
  [/] {
    print(await fetchLength());
  } [/] (e) {
    print('failed: $e');
  }
}
```

# --answers--

- async
- await
- try
- catch
- then
- finally
- sync*

# --solutions--

```dart
Future<String> fetch() async {
  throw StateError('no server');
}

Future<int> fetchLength() async {
  final text = await fetch();
  return text.length;
}

Future<void> main() async {
  try {
    print(await fetchLength());
  } catch (e) {
    print('failed: $e');
  }
}
```

# --output--

failed: Bad state: no server
