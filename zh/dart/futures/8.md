---
language: dart
exerciseType: 1
---

# --description--

future 也可以以**错误**完成。当 `async` 函数抛出异常时，异常不会立刻逃逸出去：它会成为所返回 future 的错误。对该 future 使用 `await` 的一方会在 `await` 处看到这个错误被抛出，因此可以用普通的 `try`/`catch` 处理：

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s); // throws FormatException for 'abc'
}

Future<int> orZero(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return 0;
  }
}
```

`try` 里面的 `await` 至关重要：`return parseLater(s);` 会把 future **不加等待地**交给调用方，于是错误会在 `try` 块已经结束之后才到达，`catch` 永远不会执行。

# --instructions--

函数 `parseLater(s)` 返回一个 `Future<int>`，它以 `s` 中写的数字完成，若 `s` 不是数字则以错误完成。编写一个 `async` 函数 `safeParse`，它接收一个 `String` `s`，返回一个 `Future<int>`，以解析出的数字完成，若 `parseLater` 失败则以 `-1` 完成。

示例：
```dart
print(await safeParse('12'));  // 12
print(await safeParse('abc')); // -1
```

# --before-seed--

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s);
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeParse('12')` 应该以 `12` 完成

```dart
  test('test1', () async {
    expect(await safeParse('12'), 12, reason: '--err-t1--');
  });
```

`safeParse('abc')` 应该以 `-1` 完成，而不是抛出异常

```dart
  test('test2', () async {
    expect(await safeParse('abc'), -1, reason: '--err-t2--');
  });
```

`safeParse('-7')` 应该以 `-7` 完成，`safeParse('')` 以 `-1` 完成

```dart
  test('test3', () async {
    expect(await safeParse('-7'), -7, reason: '--err-t3--');
    expect(await safeParse(''), -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> safeParse(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return -1;
  }
}
```
