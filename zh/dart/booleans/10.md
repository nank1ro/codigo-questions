---
language: dart
exerciseType: 1
---

# --instructions--

`&&` 运算符（AND）仅当**两个**操作数都为 `true` 时才返回 `true`。

声明一个名为 `result` 的布尔变量，使用 `&&` 运算符组合 `true && true`。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` 必须等于 `true`。

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result` 必须是 `bool` 类型。

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = true && true;
```
