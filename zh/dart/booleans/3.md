---
language: dart
exerciseType: 2
---

# --description--

布尔变量也可以保存值 `false`。
当某个事情不是事实时，使用 `false`。

```dart
bool isLoggedIn = false;
print(isLoggedIn); // false
```

就像 `true` 一样，`false` 也必须以小写形式书写。

# --instructions--

声明一个名为 `isExpired` 的布尔变量，将其赋值为 `false`，然后打印它。

# --seed--

```dart
[/] [/] = [/];
print([/]);
```

# --answers--

- bool
- isExpired
- false
- true
- print
- String

# --solutions--

```dart
bool isExpired = false;
print(isExpired);
```

# --output--

false
