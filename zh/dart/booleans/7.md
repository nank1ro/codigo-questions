---
language: dart
exerciseType: 1
---

# --instructions--

声明一个名为 `isReady` 的布尔变量，并将其赋值为 `true`。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isReady` 必须等于 `true`。

```dart
  test('test1', () {
    expect(isReady, true, reason: '--err-t1--');
  });
```

`isReady` 必须是 `bool` 类型。

```dart
  test('test2', () {
    expect(isReady, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isReady = true;
```
