---
language: dart
exerciseType: 4
---

# --instructions--

排序项目以检查数字 `7` 是否在 `1` 到 `10` 的范围内（包括端点）并打印结果。

# --answers--

```dart
int number = 7;
```

```dart
bool lowerBound = number >= 1;
```

```dart
bool upperBound = number <= 10;
```

```dart
bool inRange = lowerBound && upperBound;
```

```dart
print(inRange);
```

# --solutions--

```dart
int number = 7;
bool lowerBound = number >= 1;
bool upperBound = number <= 10;
bool inRange = lowerBound && upperBound;
print(inRange);
```

```dart
int number = 7;
bool upperBound = number <= 10;
bool lowerBound = number >= 1;
bool inRange = lowerBound && upperBound;
print(inRange);
```

# --output--

true
