---
language: dart
exerciseType: 1
---

# --instructions--

声明一个名为 `flag` 的布尔变量，值为 `true`，然后声明另一个名为 `isFlag` 的布尔变量，并使用 `is` 关键字检查 `flag` 是否为 `bool` 类型。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`flag` 必须等于 `true`。

```dart
  test('test1', () {
    expect(flag, true, reason: '--err-t1--');
  });
```

`isFlag` 必须为 `true`（`is bool` 检查的结果）。

```dart
  test('test2', () {
    expect(isFlag, true, reason: '--err-t2--');
  });
```

`isFlag` 必须是 `bool` 类型。

```dart
  test('test3', () {
    expect(isFlag, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool flag = true;
bool isFlag = flag is bool;
```
