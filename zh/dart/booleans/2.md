---
language: dart
exerciseType: 2
---

# --description--

就像任何其他变量一样，你可以使用 `print()` 打印布尔值。
当你打印布尔值时，Dart 输出文本 `true` 或 `false`。

```dart
bool isSunny = true;
print(isSunny); // true
```

# --instructions--

声明一个名为 `isOpen` 的布尔变量，值为 `true`，然后打印它。

# --seed--

```dart
[/] isOpen = [/];
[/](isOpen);
```

# --answers--

- bool
- true
- print
- false
- output
- int

# --solutions--

```dart
bool isOpen = true;
print(isOpen);
```

# --output--

true
