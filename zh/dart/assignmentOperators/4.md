---
language: dart
exerciseType: 2
---

# --description--

**乘法赋值运算符** `*=` 将变量与值相乘并将结果存储回该变量。

```dart
int side = 4;
side *= 3; // side 现在是 12
```

这是 `side = side * 3` 的简写。

# --instructions--

完成代码，使用 `*=` 将 `price` 乘以 `3`。

# --seed--

```dart
void main() {
  int price = 7;
  price [/] 3;
  print(price);
}
```

# --answers--

- *=
- =
- +=
- *

# --solutions--

```dart
void main() {
  int price = 7;
  price *= 3;
  print(price);
}
```

# --output--

21
