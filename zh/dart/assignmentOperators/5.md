---
language: dart
exerciseType: 3
---

# --description--

赋值运算符将算术运算与赋值组合为一个步骤。

例如，`x += 3` 是 `x = x + 3` 的简写。

# --instructions--

当 `x` 当前为 `10` 时，`x += 5` 做什么？

# --answers--

- 将 `x` 设置为 `15`
- 将 `x` 设置为 `5`
- 将 `x` 设置为 `10`
- 将 `x` 设置为 `50`

# --solutions--

- 将 `x` 设置为 `15`
