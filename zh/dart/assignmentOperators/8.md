---
language: dart
exerciseType: 1
---

# --instructions--

声明`int` 变量 `points`，值为 `20`。然后使用 `+=` 给它添加 `15`。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int points = 20;
```

# --after-seed--

```dart
  return points;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`points` 应该是 `35`

```dart
  test('test1', () {
    expect(result(), 35, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int points = 20;
points += 15;
```
