---
language: dart
exerciseType: 1
---

# --instructions--

声明顶层 `int` 变量 `points`，值为 `20`。然后使用 `+=` 给它添加 `15`。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`points` 应该是 `35`

```dart
  test('test1', () {
    expect(points, 35, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int points = 20;

void main() {
  points += 15;
  print(points);
}
```
