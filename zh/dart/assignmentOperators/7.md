---
language: dart
exerciseType: 3
---

# --instructions--

当 `x` 为 `9` 时，`x %= 4` 做什么？

# --answers--

- 将 `x` 设置为 `9 / 4` 的余数，即 `1`
- 将 `x` 设置为 `9 / 4` 的结果，即 `2.25`
- 将 `x` 设置为 `4`
- 将 `x` 设置为 `36`

# --solutions--

- 将 `x` 设置为 `9 / 4` 的余数，即 `1`
