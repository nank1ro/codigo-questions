---
language: dart
exerciseType: 1
---

# --instructions--

声明顶层 `int` 变量 `n`，值为 `2`。然后在 `main` 中依次应用以下赋值运算符：
- `n *= 10`（乘以 10）
- `n += 5`（加 5）
- `n -= 3`（减 3）

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`n` 应该是 `22`

```dart
  test('test1', () {
    expect(n, 22, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 2;

void main() {
  n *= 10;
  n += 5;
  n -= 3;
  print(n);
}
```
