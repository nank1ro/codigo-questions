---
language: dart
exerciseType: 2
---

# --description--

**加法赋值运算符** `+=` 将值添加到变量并将结果存储回该变量。

```dart
int score = 10;
score += 5; // score 现在是 15
```

这是 `score = score + 5` 的简写。

# --instructions--

完成代码，使用 `+=` 将 `8` 添加到变量 `total`。

# --seed--

```dart
void main() {
  int total = 12;
  total [/] 8;
  print(total);
}
```

# --answers--

- +=
- =
- -=
- +

# --solutions--

```dart
void main() {
  int total = 12;
  total += 8;
  print(total);
}
```

# --output--

20
