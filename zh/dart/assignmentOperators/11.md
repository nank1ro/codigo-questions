---
language: dart
exerciseType: 1
---

# --instructions--

声明`double` 变量 `value`，值为 `20.0`。然后使用 `/=` 将它除以 `4`。注意，在 Dart 中 `/=` 总是产生 `double`。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
double value = 20.0;
```

# --after-seed--

```dart
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value` 应该是 `5.0`

```dart
  test('test1', () {
    expect(result(), closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;
value /= 4;
```
