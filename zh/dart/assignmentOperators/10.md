---
language: dart
exerciseType: 1
---

# --instructions--

声明`int` 变量 `width`，值为 `6`。然后使用 `*=` 将它乘以 `4`。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int width = 6;
```

# --after-seed--

```dart
  return width;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`width` 应该是 `24`

```dart
  test('test1', () {
    expect(result(), 24, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int width = 6;
width *= 4;
```
