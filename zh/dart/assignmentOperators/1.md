---
language: dart
exerciseType: 2
---

# --description--

**赋值运算符** `=` 将值存储在变量中。

```dart
int x = 5;
String name = 'Alice';
```

赋值后，变量会保持该值，直到被重新赋值。

# --instructions--

完成代码，将值 `42` 赋值给变量 `answer`。

# --seed--

```dart
void main() {
  int answer [/] 42;
  print(answer);
}
```

# --answers--

- =
- ==
- +=
- =>

# --solutions--

```dart
void main() {
  int answer = 42;
  print(answer);
}
```

# --output--

42
