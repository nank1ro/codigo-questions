---
language: dart
exerciseType: 2
---

# --description--

**减法赋值运算符** `-=` 从变量中减去值并将结果存储回该变量。

```dart
int lives = 3;
lives -= 1; // lives 现在是 2
```

这是 `lives = lives - 1` 的简写。

# --instructions--

完成代码，使用 `-=` 从变量 `stock` 中减去 `5`。

# --seed--

```dart
void main() {
  int stock = 20;
  stock [/] 5;
  print(stock);
}
```

# --answers--

- -=
- =
- +=
- -

# --solutions--

```dart
void main() {
  int stock = 20;
  stock -= 5;
  print(stock);
}
```

# --output--

15
