---
language: dart
exerciseType: 1
---

# --instructions--

声明顶层 `int` 变量 `fuel`，值为 `100`。然后使用 `-=` 从它减去 `35`。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`fuel` 应该是 `65`

```dart
  test('test1', () {
    expect(fuel, 65, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int fuel = 100;

void main() {
  fuel -= 35;
  print(fuel);
}
```
