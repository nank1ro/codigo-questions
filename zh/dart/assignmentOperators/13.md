---
language: dart
exerciseType: 1
---

# --instructions--

声明`int` 变量 `remainder`，值为 `29`。然后使用 `%=` 将其赋值为除以 `6` 的余数。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int remainder = 29;
```

# --after-seed--

```dart
  return remainder;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`remainder` 应该是 `5`

```dart
  test('test1', () {
    expect(result(), 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 29;
remainder %= 6;
```
