---
language: dart
exerciseType: 4
---

# --description--

可空性决定了你如何声明**命名参数**。类型可空的命名参数是可选的：调用方省略它时，它就是 `null`。

```dart
String label({String? title}) => title ?? 'untitled';

print(label());               // untitled
print(label(title: 'Notes')); // Notes
```

类型非空且没有默认值的命名参数在被省略时将没有值，所以 Dart 要求你把它标记为 `required`；这样调用方就必须始终传入它：

```dart
String label({required String name, String? title}) { ... }

label(name: 'Ada');               // ok
label(name: 'Ada', title: 'Dr.'); // ok
label(title: 'Dr.');              // error: the named parameter 'name' is required
```

# --instructions--

排列这些条目，定义一个函数 `label`，它有一个必需的命名参数 `name` 和一个可选的可空命名参数 `title`；当 `title` 为 `null` 时只返回 `name`，否则返回 `'$title $name'`。先定义函数，再定义 `main`。程序必须打印：

```
Ada
Countess Lovelace
```

# --answers--

```dart
String label({required String name, String? title}) {
```

```dart
  if (title == null) return name;
```

```dart
  return '$title $name';
```

```dart
}
```

```dart
void main() {
```

```dart
  print(label(name: 'Ada'));
```

```dart
  print(label(name: 'Lovelace', title: 'Countess'));
```

```dart
}
```

# --solutions--

```dart
String label({required String name, String? title}) {
  if (title == null) return name;
  return '$title $name';
}

void main() {
  print(label(name: 'Ada'));
  print(label(name: 'Lovelace', title: 'Countess'));
}
```

# --output--

Ada
Countess Lovelace
