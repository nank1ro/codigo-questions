---
language: dart
exerciseType: 2
---

# --description--

可空性同样适用于集合的**元素**。`List<int>` 永远不包含 `null`，而 `List<int?>` 可以：

```dart
List<int?> scores = [7, null, 9];
```

注意它与 `List<int>?` 的区别：后者是列表本身可能缺失，但只要存在，里面就只有真正的数字。

要去掉 `null` 元素，`nonNulls` 会返回一个只包含存在值的 `Iterable`，其类型不带 `?`：

```dart
var present = scores.nonNulls.toList(); // List<int>
print(present); // [7, 9]
```

`whereType<int>()` 也能做到同样的事，并且在列表混合了多种类型时同样有效。

# --instructions--

补全代码，声明一个可空 `int` 值的列表，然后用 `nonNulls` 只保留非空的值。程序必须先打印 `[7, 9]`，再打印 `2`。

# --seed--

```dart
void main() {
  List<[/]> scores = [7, null, 9, null];
  var present = scores.[/].toList();
  print(present);
  print(present.length);
}
```

# --answers--

- int?
- nonNulls
- int
- notNull
- nonNull
- compact

# --solutions--

```dart
void main() {
  List<int?> scores = [7, null, 9, null];
  var present = scores.nonNulls.toList();
  print(present);
  print(present.length);
}
```

# --output--

[7, 9]
2
