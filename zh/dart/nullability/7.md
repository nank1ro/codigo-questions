---
language: dart
exerciseType: 1
---

# --description--

有时**你**知道某个可空值在某处不是 `null`，即使编译器无法判断。**空断言**运算符 `!` 通过承诺值一定存在，把 `String?` 转换成 `String`：

```dart
String? text = 'Dart';
String sure = text!;
print(sure.length); // 4
```

要小心：`!` 把检查从编译期挪到了运行期。如果值**确实**是 `null`，程序会抛出错误并停止：

```dart
String? text;
print(text!.length); // Null check operator used on a null value
```

请谨慎使用 `!`，只在那里出现 `null` 本来就是 bug 的情况下使用。

# --instructions--

编写一个函数 `shout`，它接受一个可空的 `String` 参数 `text`，返回它的大写形式并在末尾加上 `!`。请使用 `!` 运算符，使函数在 `text` 为 `null` 时抛出错误。

示例：
```dart
print(shout('hello')); // HELLO!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shout('hello')` 应该是 `'HELLO!'`

```dart
  test('test1', () {
    expect(shout('hello'), 'HELLO!', reason: '--err-t1--');
  });
```

`shout('Dart')` 应该是 `'DART!'`

```dart
  test('test2', () {
    expect(shout('Dart'), 'DART!', reason: '--err-t2--');
  });
```

`shout(null)` 应该抛出错误

```dart
  test('test3', () {
    expect(() => shout(null), throwsA(anything), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String shout(String? text) {
  return '${text!.toUpperCase()}!';
}
```
