---
language: dart
exerciseType: 4
---

# --description--

还有两个运算符拥有空安全变体。

**空安全级联** `?..` 只在对象不是 `null` 时才执行整条级联操作链，否则全部跳过：

```dart
List<int>? numbers;
numbers?..add(1)..add(2); // nothing happens, numbers is still null
```

**空安全展开** `...?` 把可空集合的元素插入到字面量中；当集合为 `null` 时则什么也不添加：

```dart
List<int>? extra;
print([0, ...?extra]); // [0]

extra = [1, 2];
print([0, ...?extra]); // [0, 1, 2]
```

如果没有 `?`，在 `List<int>?` 上写 `...extra` 会是编译错误。

# --instructions--

排列这些条目，定义一个函数 `fill`，用 `?..` 向一个可空列表添加 `1` 和 `2`，然后在 `main` 中使用它。先定义函数，再定义 `main`。程序必须打印：

```
[0]
[1, 2, 3]
```

# --answers--

```dart
void fill(List<int>? target) {
```

```dart
  target?..add(1)..add(2);
```

```dart
}
```

```dart
void main() {
```

```dart
  List<int>? numbers;
```

```dart
  print([0, ...?numbers]);
```

```dart
  numbers = [];
```

```dart
  fill(numbers);
```

```dart
  print([...?numbers, 3]);
```

```dart
}
```

# --solutions--

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  print([0, ...?numbers]);
  numbers = [];
  fill(numbers);
  print([...?numbers, 3]);
}
```

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  numbers = [];
  print([0, ...?numbers]);
  fill(numbers);
  print([...?numbers, 3]);
}
```

# --output--

[0]
[1, 2, 3]
