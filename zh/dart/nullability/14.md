---
language: dart
exerciseType: 1
---

# --description--

许多库函数用 `null` 来表示某件事**无法完成**。把字符串转换成数字是经典例子：当文本不是数字时，`int.parse` 会抛出 `FormatException`，而 `int.tryParse` 则返回 `null`，让你决定如何处理：

```dart
print(int.tryParse('42'));  // 42
print(int.tryParse('4x2')); // null
print(int.tryParse(''));    // null
```

`int.tryParse` 的返回类型是 `int?`，所以你学到的一切都适用：用 `??` 提供默认值，用 `?.` 链式调用，用 `if` 检查来实现类型提升。`double.tryParse` 的工作方式相同。

# --instructions--

编写一个函数 `parseOrZero`，它接受一个 `String` 参数 `text`，返回其中包含的整数；当文本不是有效整数时返回 `0`。请使用 `int.tryParse`。

示例：
```dart
print(parseOrZero('42'));  // 42
print(parseOrZero('abc')); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parseOrZero('42')` 应该是 `42`

```dart
  test('test1', () {
    expect(parseOrZero('42'), 42, reason: '--err-t1--');
  });
```

`parseOrZero('-5')` 应该是 `-5`

```dart
  test('test2', () {
    expect(parseOrZero('-5'), -5, reason: '--err-t2--');
  });
```

`parseOrZero('abc')` 和 `parseOrZero('')` 应该是 `0`

```dart
  test('test3', () {
    expect(parseOrZero('abc'), 0, reason: '--err-t3--');
    expect(parseOrZero(''), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int parseOrZero(String text) {
  return int.tryParse(text) ?? 0;
}
```
