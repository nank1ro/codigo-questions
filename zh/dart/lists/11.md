---
language: dart
exerciseType: 1
---

# --instructions--

从包含 `['cat', 'dog', 'bird']` 的 `List<String>` 开始。使用 `.remove()` 删除 `'dog'`。返回结果列表。

# --before-seed--

```dart
List<String> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

列表不再包含 'dog'

```dart
  test('dog removed', () {
    expect(computeValue(), ['cat', 'bird'], reason: '--err-t1--');
  });
```

长度为 2

```dart
  test('length is 2', () {
    expect(computeValue().length, 2, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<String> result = ['cat', 'dog', 'bird'];
  result.remove('dog');
```
