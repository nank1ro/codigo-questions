---
language: dart
exerciseType: 1
---

# --instructions--

给定列表 `[1, 2, 3, 4, 5, 6]`，使用 `.where()` 过滤出偶数。将结果作为 `List<int>` 返回。

# --before-seed--

```dart
List<int> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

仅返回偶数

```dart
  test('evens are [2, 4, 6]', () {
    expect(computeValue(), [2, 4, 6], reason: '--err-t1--');
  });
```

长度为 3

```dart
  test('length is 3', () {
    expect(computeValue().length, 3, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var numbers = [1, 2, 3, 4, 5, 6];
  List<int> result = numbers.where((n) => n % 2 == 0).toList();
```
