---
language: dart
exerciseType: 2
---

# --description--

`.length` 属性返回列表中的元素个数：

```dart
var scores = [95, 87, 72, 100];
print(scores.length); // 4
```

空列表的长度为 `0`：

```dart
var empty = [];
print(empty.length); // 0
```

# --instructions--

补全代码，打印列表的长度。

# --seed--

```dart
var items = ['a', 'b', 'c', 'd', 'e'];
print(items.[/]);
```

# --answers--

- length
- size
- count
- len()
- length()
- total

# --solutions--

```dart
var items = ['a', 'b', 'c', 'd', 'e'];
print(items.length);
```

# --output--

5
