---
language: dart
exerciseType: 1
---

# --instructions--

给定列表 `[3, 7, 2, 9, 4]`，使用 `for-in` 循环对所有元素求和。返回总和。

# --before-seed--

```dart
int computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

总和为 25

```dart
  test('sum is 25', () {
    expect(computeValue(), 25, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var numbers = [3, 7, 2, 9, 4];
  int result = 0;
  for (var n in numbers) {
    result += n;
  }
```
