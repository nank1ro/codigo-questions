---
language: dart
exerciseType: 2
---

# --description--

`.contains()` 方法检查列表是否包含给定的值。如果找到则返回 `true`，否则返回 `false`：

```dart
var fruits = ['apple', 'mango', 'grape'];
print(fruits.contains('mango'));  // true
print(fruits.contains('orange')); // false
```

这对于不使用循环进行成员检查非常有用。

# --instructions--

补全代码，检查 `42` 是否在列表中并打印结果。

# --seed--

```dart
var numbers = [10, 42, 77, 5];
print(numbers.[/](42));
```

# --answers--

- contains
- includes
- has
- find
- exists
- indexOf

# --solutions--

```dart
var numbers = [10, 42, 77, 5];
print(numbers.contains(42));
```

# --output--

true
