---
language: dart
exerciseType: 1
---

# --instructions--

从包含 `[1, 2, 3]` 的 `List<int>` 开始。使用 `.add()` 添加值 `4`。返回该列表。

# --before-seed--

```dart
List<int> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

列表包含已添加的元素

```dart
  test('list is [1, 2, 3, 4]', () {
    expect(computeValue(), [1, 2, 3, 4], reason: '--err-t1--');
  });
```

长度为 4

```dart
  test('length is 4', () {
    expect(computeValue().length, 4, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<int> result = [1, 2, 3];
  result.add(4);
```
