---
language: dart
exerciseType: 1
---

# --instructions--

给定列表 `['apple', 'banana', 'cherry', 'date']`，使用 `.indexOf()` 查找 `'cherry'` 的索引。返回该索引。

# --before-seed--

```dart
int computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

'cherry' 的索引为 2

```dart
  test('indexOf cherry is 2', () {
    expect(computeValue(), 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var fruits = ['apple', 'banana', 'cherry', 'date'];
  int result = fruits.indexOf('cherry');
```
