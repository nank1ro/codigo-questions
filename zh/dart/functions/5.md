---
language: dart
exerciseType: 2
---

# --description--

Dart 支持**命名参数**——用花括号 `{}` 包裹的参数。命名参数必须通过名称调用，可以有默认值或标记为 `required`：

```dart
void printInfo({required String name, int age = 0}) {
  print("$name is $age years old");
}

void main() {
  printInfo(name: "Alice", age: 30);
  // prints Alice is 30 years old
}
```

命名参数提高了代码的可读性，尤其是当函数有多个参数时。

# --instructions--

补全函数 `describe`，使其接受一个类型为 `String` 的命名参数 `item`（必填）并将其打印。

# --seed--

```dart
void describe([/]required String item[/]) {
  print(item);
}

void main() {
  describe(item: "laptop");
}
```

# --answers--

- {
- }
- (
- )
- @

# --solutions--

```dart
void describe({required String item}) {
  print(item);
}

void main() {
  describe(item: "laptop");
}
```

# --output--

laptop
