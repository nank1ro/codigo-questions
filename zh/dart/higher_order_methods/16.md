---
language: dart
exerciseType: 1
---

# --description--

把这些方法组合起来，就能替代大多数手写的循环。一条流水线通常分三个阶段来读：用 `where` **筛选**元素，用 `map` **转换**它们，再用 `fold` **合并**它们：

```dart
final prices = [12, 40, 7];
final cheapTotal = prices.where((p) => p < 20).fold(0, (acc, p) => acc + p);
print(cheapTotal); // 19
```

因为 `fold` 自己选择初始值，它也能让一条链以一个与元素毫无关系的类型收尾，比如一点一点拼起来的 `String`：

```dart
final words = ['fig', 'kiwi'];
final firstLetters = words.fold('', (acc, w) => acc + w[0]);
print(firstLetters); // fk
```

每个阶段都很简短，而且清楚地说明了自己在做什么，这才是比起一个同时做完三件事的循环更应该选择它们的真正理由。

# --instructions--

编写一个函数 `initials`，接收一个 `List<String>` `names`，忽略其中的空字符串，返回一个由剩下每个名字的首字母组成的 `String`，全部大写并保持顺序。

示例：
```dart
print(initials(['ada', '', 'linus'])); // AL
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`initials(['ada', '', 'linus'])` 应该返回 `'AL'`

```dart
  test('test1', () {
    expect(initials(['ada', '', 'linus']), 'AL', reason: '--err-t1--');
  });
```

字母应该是大写并保持顺序，已经是大写的名字应该保持原样

```dart
  test('test2', () {
    expect(initials(['grace', 'Bob', 'ada']), 'GBA', reason: '--err-t2--');
    expect(initials(['x']), 'X', reason: '--err-t2--');
  });
```

当没有非空名字时 `initials` 应该返回空 `String`

```dart
  test('test3', () {
    expect(initials(<String>[]), '', reason: '--err-t3--');
    expect(initials(['', '']), '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String initials(List<String> names) {
  return names
      .where((n) => n.isNotEmpty)
      .map((n) => n[0].toUpperCase())
      .fold('', (acc, letter) => acc + letter);
}
```
