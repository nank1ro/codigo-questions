---
language: dart
exerciseType: 1
---

# --description--

Dart 没有 `sorted` 方法。`sort` 属于 `List`，它**就地**重排列表并且不返回任何东西：

```dart
final numbers = [3, 1, 2];
numbers.sort();
print(numbers); // [1, 2, 3]
```

因为它返回 `void`，你根本无法使用它的结果：`final sorted = numbers.sort();` 得到的值编译器不允许你读取。得到一份排好序的**副本**的惯用写法是 `toList()` 后面接级联 `..sort()`：`toList()` 做出副本，而 `..` 在副本上执行 `sort`，同时仍然把副本本身交还给你。

```dart
final numbers = [3, 1, 2];
final sorted = numbers.toList()..sort();
print(sorted);  // [1, 2, 3]
print(numbers); // [3, 1, 2]，未改变
```

`sort` 还接受一个**比较器**：一个接收两个元素的函数，当第一个排在第二个前面时返回负数，相等时返回 `0`，否则返回正数。`compareTo` 产出的正是这样的值，所以按任意键排序只要一行：

```dart
final words = ['kiwi', 'fig', 'banana'];
print(words.toList()..sort((a, b) => a.length.compareTo(b.length)));
// [fig, kiwi, banana]
```

# --instructions--

编写一个函数 `byLength`，接收一个 `List<String>` `words`，返回一个**新**列表，里面是同样的单词，按从短到长排序，并保持 `words` 不变。

示例：
```dart
print(byLength(['kiwi', 'fig', 'banana'])); // [fig, kiwi, banana]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`byLength(['kiwi', 'fig', 'banana'])` 应该返回 `['fig', 'kiwi', 'banana']`

```dart
  test('test1', () {
    expect(byLength(['kiwi', 'fig', 'banana']), ['fig', 'kiwi', 'banana'],
        reason: '--err-t1--');
  });
```

`byLength` 应该返回一个 `List<String>`，并且对空列表也能正常工作

```dart
  test('test2', () {
    expect(byLength(['bb', 'a']), isA<List<String>>(), reason: '--err-t2--');
    expect(byLength(['bb', 'a']), ['a', 'bb'], reason: '--err-t2--');
    expect(byLength(<String>[]), <String>[], reason: '--err-t2--');
  });
```

`byLength` 不能修改它接收到的列表

```dart
  test('test3', () {
    final original = ['kiwi', 'fig', 'banana'];
    final result = byLength(original);
    expect(original, ['kiwi', 'fig', 'banana'], reason: '--err-t3--');
    expect(result, ['fig', 'kiwi', 'banana'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<String> byLength(List<String> words) {
  return words.toList()..sort((a, b) => a.length.compareTo(b.length));
}
```
