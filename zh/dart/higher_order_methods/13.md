---
language: dart
exerciseType: 1
---

# --description--

这些方法每一个都返回 `Iterable`，而每个 `Iterable` 又都拥有这些方法。正是这一点让它们可以**链式调用**：整个计算从左到右读起来就像一条流水线，每一步都作用在上一步产出的结果上。

```dart
final words = ['kiwi', 'fig', 'banana', 'date'];
print(words.where((w) => w.length == 4).map((w) => w.toUpperCase()).toList());
// [KIWI, DATE]
```

只有最后一步需要 `toList()`：在中间调用它只会构建一个没人保留的列表。

类型会沿着链条变化，下一个函数接收到的东西也随之变化：对 `List<String>` 执行 `where` 之后你拿到的仍然是字符串，但在 `map((w) => w.length)` 之后，下一步看到的就是数字了。

因为每一步都是惰性的，顺序影响的不只是结果，还有实际完成的工作量：先用 `where` 过滤意味着 `map` 只会在更少的元素上被调用。

# --instructions--

编写一个函数 `shortUpper`，接收一个 `List<String>` `words`，返回一个新的 `List<String>`，其中是长度不超过 `4` 个字符的单词，转成大写，并保持原来的顺序。

示例：
```dart
print(shortUpper(['kiwi', 'banana', 'fig'])); // [KIWI, FIG]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shortUpper(['kiwi', 'banana', 'fig'])` 应该返回 `['KIWI', 'FIG']`

```dart
  test('test1', () {
    expect(shortUpper(['kiwi', 'banana', 'fig']), ['KIWI', 'FIG'],
        reason: '--err-t1--');
  });
```

正好 `4` 个字符的单词应该保留，更长的应该丢弃

```dart
  test('test2', () {
    expect(shortUpper(['date', 'grape']), ['DATE'], reason: '--err-t2--');
    expect(shortUpper(['melon']), <String>[], reason: '--err-t2--');
  });
```

`shortUpper` 应该返回一个 `List<String>`，并且不能修改它接收到的列表

```dart
  test('test3', () {
    expect(shortUpper(['a']), isA<List<String>>(), reason: '--err-t3--');
    final original = ['fig', 'banana'];
    shortUpper(original);
    expect(original, ['fig', 'banana'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<String> shortUpper(List<String> words) {
  return words.where((w) => w.length <= 4).map((w) => w.toUpperCase()).toList();
}
```
