---
language: dart
exerciseType: 2
---

# --description--

你已经知道如何给自己编写的类添加方法。但 `String`、`int` 或 `List` 呢？它们的代码位于 Dart SDK 中。你无法修改它们，却常常希望它们能多一个方法。

**扩展**解决了这个问题：它为一个**现有**类型添加新的成员，无需改动其源代码，也无需创建子类。语法如下：

```dart
extension ExtensionName on Type {
  // new methods and getters
}
```

在扩展内部，`this` 指代调用该成员的值。扩展一旦声明，其成员的调用方式与类型自身的成员完全相同：

```dart
extension Greeting on String {
  String greet() => 'Hello, $this!';
}

void main() {
  var name = 'Ada';
  print(name.greet()); // Hello, Ada!
}
```

扩展声明在文件的**顶层**，与类和函数并列，绝不在 `main` 内部。

# --instructions--

补全代码，声明一个名为 `Shout` 的 `String` 扩展，其中有一个方法 `shout`，返回全大写的字符串并在末尾加上 `!`，使程序打印 `HELLO!`。

# --seed--

```dart
[/] Shout [/] String {
  String shout() => [/].toUpperCase() + '!';
}

void main() {
  print('hello'.shout());
}
```

# --answers--

- extension
- on
- this
- class
- extends
- self

# --solutions--

```dart
extension Shout on String {
  String shout() => this.toUpperCase() + '!';
}

void main() {
  print('hello'.shout());
}
```

# --output--

HELLO!
