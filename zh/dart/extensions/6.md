---
language: dart
exerciseType: 2
---

# --description--

作用于 `String` 的扩展不能在 `String?` 上调用：值可能是 `null`，编译器会拒绝这次调用。如果把扩展声明在**可空**类型上，方法就可以直接在 `String?` 上调用，此时内部的 `this` 类型是 `String?`，因此你必须自己处理 `null` 的情况，例如用 `??`：

```dart
extension Defaults on int? {
  int orZero() => this ?? 0;
}

void main() {
  int? count = null;
  print(count.orZero()); // 0
  print(5.orZero());     // 5
}
```

在需要 `int?` 的地方可以传入非空的 `int`，所以这个扩展对两者都有效。

# --instructions--

补全扩展，使 `orEmpty` 可以在可空字符串上调用，并返回字符串本身；当它为 `null` 时返回空字符串。程序必须打印 `[]` 和 `[Ada]`。

# --seed--

```dart
extension Defaults on [/] {
  String orEmpty() => this [/] '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --answers--

- String?
- ??
- String
- +
- ?.
- !

# --solutions--

```dart
extension Defaults on String? {
  String orEmpty() => this ?? '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --output--

[]
[Ada]
