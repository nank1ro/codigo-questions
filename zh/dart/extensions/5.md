---
language: dart
exerciseType: 1
---

# --description--

作用于 `List<int>` 的扩展不能用在 `List<String>` 上。要编写一个对**任意**元素类型都有效的扩展，可以给扩展一个**类型参数**，写在它名字后面的尖括号中，并在 `on` 的类型里使用它：

```dart
extension Firsts<T> on List<T> {
  T get firstOrLast => length > 1 ? this[0] : this[length - 1];
}
```

`T` 是“元素类型是什么”的占位符：在 `List<int>` 上它就是 `int`，在 `List<String>` 上它就是 `String`，因此上面的 getter 会相应地返回 `int` 或 `String`。编译器会在每次调用时替你填入 `T`。

```dart
print([7, 8, 9].firstOrLast); // 7
print(['a', 'b'].firstOrLast); // a
```

# --instructions--

编写一个带类型参数 `T` 的扩展 `Seconds`，作用于 `List<T>`，其中有一个 `T?` 类型的 getter `secondOrNull`，返回索引 `1` 处的元素；当列表元素少于两个时返回 `null`。

示例：
```dart
print([10, 20, 30].secondOrNull); // 20
print(['solo'].secondOrNull);     // null
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`[10, 20, 30].secondOrNull` 应该是 `20`

```dart
  test('test1', () {
    expect([10, 20, 30].secondOrNull, 20, reason: '--err-t1--');
  });
```

`['a', 'b'].secondOrNull` 应该是 `'b'`

```dart
  test('test2', () {
    expect(['a', 'b'].secondOrNull, 'b', reason: '--err-t2--');
  });
```

`['solo'].secondOrNull` 和 `<int>[].secondOrNull` 应该是 `null`

```dart
  test('test3', () {
    expect(['solo'].secondOrNull, null, reason: '--err-t3--');
    expect(<int>[].secondOrNull, null, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Seconds<T> on List<T> {
  T? get secondOrNull => length > 1 ? this[1] : null;
}
```
