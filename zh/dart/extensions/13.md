---
language: dart
exerciseType: 2
---

# --description--

泛型扩展可以接受**函数**作为参数，与 `where` 和 `map` 的做法完全一样。函数类型用元素类型 `T` 来书写，因此回调收到的元素具有正确的类型：

```dart
extension Checks<T> on List<T> {
  bool all(bool Function(T) test) {
    for (final item in this) {
      if (!test(item)) return false;
    }
    return true;
  }
}

void main() {
  print([2, 4, 6].all((n) => n.isEven)); // true
}
```

# --instructions--

补全扩展，使 `countWhere` 返回满足给定 `test` 函数的元素个数，使程序先打印 `2`，再打印 `1`。

# --seed--

```dart
extension Counting<T> on List<T> {
  int countWhere([/] Function(T) test) {
    var count = 0;
    for (final item in [/]) {
      if (test(item)) count++;
    }
    return count;
  }
}

void main() {
  print([1, 2, 3, 4].countWhere((n) => n.isEven));
  print(['a', 'bb', 'c'].countWhere((s) => s.length > 1));
}
```

# --answers--

- bool
- this
- int
- void
- T
- list

# --solutions--

```dart
extension Counting<T> on List<T> {
  int countWhere(bool Function(T) test) {
    var count = 0;
    for (final item in this) {
      if (test(item)) count++;
    }
    return count;
  }
}

void main() {
  print([1, 2, 3, 4].countWhere((n) => n.isEven));
  print(['a', 'bb', 'c'].countWhere((s) => s.length > 1));
}
```

# --output--

2
1
