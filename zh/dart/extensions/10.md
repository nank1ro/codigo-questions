---
language: dart
exerciseType: 1
---

# --description--

Dart 允许一个类型定义 `+`、`*` 或 `==` 这类运算符对它的值意味着什么，做法是使用一个名字为关键字 `operator` 加上符号的方法。运算符的右侧就是方法的参数：

```dart
extension Scaling on List<int> {
  List<int> operator *(int factor) => map((n) => n * factor).toList();
}

void main() {
  print([1, 2, 3] * 10); // [10, 20, 30]
}
```

由于扩展可以声明运算符，你可以给现有类型添加一个它还没有的新运算符。`String` 有 `+` 和 `*`，但没有 `-`，所以扩展可以定义 `'hello world' - 'o'` 的含义。

# --instructions--

编写一个名为 `Removing` 的 `String` 扩展，其中有一个运算符 `-`，它接受一个 `String` 参数 `part`，返回删除了所有 `part` 出现位置的字符串。请使用 `replaceAll`。

示例：
```dart
print('hello world' - 'o'); // hell wrld
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`'hello world' - 'o'` 应该是 `'hell wrld'`

```dart
  test('test1', () {
    expect('hello world' - 'o', 'hell wrld', reason: '--err-t1--');
  });
```

`'banana' - 'an'` 应该是 `'ba'`

```dart
  test('test2', () {
    expect('banana' - 'an', 'ba', reason: '--err-t2--');
  });
```

`'abc' - 'z'` 应该是 `'abc'`，`'aaa' - 'a'` 应该是 `''`

```dart
  test('test3', () {
    expect('abc' - 'z', 'abc', reason: '--err-t3--');
    expect('aaa' - 'a', '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Removing on String {
  String operator -(String part) => replaceAll(part, '');
}
```
