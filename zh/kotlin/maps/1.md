---
language: kotlin
exerciseType: 2
---

# --description--

`Map` 存储**键值对**：每个值都通过其键来查找，而不是通过索引。
在一个 map 中键是唯一的，而值可以重复。

你可以使用 `mapOf` 创建一个只读的 map，通过中缀函数 `to` 将每个键与其值配对：
```kotlin
val capitals = mapOf("Italy" to "Rome", "France" to "Paris")
println(capitals)
// prints {Italy=Rome, France=Paris}
```
这里 `"Italy"` 和 `"France"` 是键，`"Rome"` 和 `"Paris"` 是它们的值。

# --instructions--

补全代码以创建一个有效的 map，将 `"Alice"` 与 `30` 配对，将 `"Bob"` 与 `25` 配对

# --seed--

```kotlin
val ages = [/]("Alice"[/]30, "Bob"[/]25)
println(ages)
```

# --answers--

- mapOf
- listOf
- setOf
-  to 
-  to 
-  : 
-  = 

# --solutions--

```kotlin
val ages = mapOf("Alice" to 30, "Bob" to 25)
println(ages)
```

# --output--

{Alice=30, Bob=25}
