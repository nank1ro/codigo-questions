---
language: kotlin
exerciseType: 4
---

# --instructions--

你能排列这些代码行来统计每种水果出现的次数并打印 `{apple=2, pear=1}` 吗？请在 `counts` 之前声明 `fruits`。

# --answers--

```kotlin
val fruits = listOf("apple", "pear", "apple")
```

```kotlin
val counts = mutableMapOf<String, Int>()
```

```kotlin
for (fruit in fruits) {
```

```kotlin
    counts[fruit] = counts.getOrDefault(fruit, 0) + 1
```

```kotlin
}
```

```kotlin
println(counts)
```

# --solutions--

```kotlin
val fruits = listOf("apple", "pear", "apple")
val counts = mutableMapOf<String, Int>()
for (fruit in fruits) {
    counts[fruit] = counts.getOrDefault(fruit, 0) + 1
}
println(counts)
```

# --output--

{apple=2, pear=1}
