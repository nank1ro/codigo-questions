---
language: kotlin
exerciseType: 2
---

# --description--

当一个键可能不存在时，`getOrDefault` 让你选择用来代替 `null` 的值：
```kotlin
val ages = mapOf("Alice" to 30)
println(ages.getOrDefault("Alice", 0)) // 30
println(ages.getOrDefault("Zoe", 0))   // 0
```
第一个参数是键，第二个参数是找不到该键时返回的默认值。

# --instructions--

补全代码以打印 `"kiwi"` 的库存，如果该水果不在 map 中则使用 `0`

# --seed--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.[/]("kiwi", [/]))
```

# --answers--

- getOrDefault
- get
- getOrNull
- 0
- null
- "kiwi"

# --solutions--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.getOrDefault("kiwi", 0))
```

# --output--

0
