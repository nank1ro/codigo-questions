---
language: kotlin
exerciseType: 4
---

# --description--

除了函数，你还可以添加**扩展属性**。它用 `val`、接收者类型、一个点和名字来声明，后面跟一个 `get()`，每次读取属性时都会计算出值：
```kotlin
val String.wordCount: Int
    get() = split(" ").size

println("Kotlin is fun".wordCount) // 3
```
扩展属性不能存储任何东西：它没有幕后字段，所以像 `val String.label = "text"` 这样的初始化器是编译错误。它只能根据接收者计算出自己的值。
与扩展函数不同，扩展属性不能声明在函数内部（不允许局部扩展属性）。

# --instructions--

你能排列这些代码行，打印 `3`，并把 `wordCount` 属性声明在 `main` 之前吗？

# --answers--

```kotlin
val String.wordCount: Int
    get() = split(" ").size
```

```kotlin
fun main() {
```

```kotlin
    println("Kotlin is fun".wordCount)
```

```kotlin
}
```

# --solutions--

```kotlin
val String.wordCount: Int
    get() = split(" ").size
fun main() {
    println("Kotlin is fun".wordCount)
}
```

# --output--

3
