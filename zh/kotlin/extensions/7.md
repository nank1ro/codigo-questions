---
language: kotlin
exerciseType: 2
---

# --description--

接收者类型可以是**可空的**。`String?` 上的扩展可以在可能持有 `null` 的变量上调用，函数内部的 `this` 是 `String?`，所以要由你自己处理 `null` 的情况，通常用你在可空性课程中见过的 Elvis 运算符 `?:`：
```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val name: String? = null
println(name.orDash())  // -
println("Ada".orDash()) // Ada
```
在 `null` 值上调用 `name.orDash()` 是安全的：不需要 `?.`，因为函数本身就接受 `null` 接收者。

# --instructions--

补全这个扩展，让它可以在可空的 `String` 上调用，并在接收者为 `null` 时返回 `"-"`，使程序在两行中打印 `-` 和 `Ada`

# --seed--

```kotlin
fun String[/].orDash(): String = [/] ?: "-"
val name: String? = null
println(name.orDash())
println("Ada".orDash())
```

# --answers--

- ?
- this
- !!
- it
- name
- ?.

# --solutions--

```kotlin
fun String?.orDash(): String = this ?: "-"
val name: String? = null
println(name.orDash())
println("Ada".orDash())
```

# --output--

-
Ada
