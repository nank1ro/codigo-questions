---
language: kotlin
exerciseType: 2
---

# --description--

集合也可以持有可空元素：`List<Int?>` 可以包含 `null` 条目，而 `List<Int>` 永远不会。
`filterNotNull()` 返回一个移除了 `null` 条目的新列表，并且它的元素类型变为非空，因此你可以放心地使用这些元素：
```kotlin
val scores: List<Int?> = listOf(10, null, 20)
val valid = scores.filterNotNull() // List<Int> [10, 20]
println(valid.sum())               // 30
```

# --instructions--

补全代码，先声明一个可以包含 `null` 数字的列表，然后移除 `null` 条目，并打印剩余的值及它们的总和

# --seed--

```kotlin
val values: List<[/]> = listOf(3, null, 5, null)
val present = values.[/]()
println(present)
println(present.sum())
```

# --answers--

- Int?
- Int
- filterNotNull
- filter
- mapNotNull
- requireNotNull

# --solutions--

```kotlin
val values: List<Int?> = listOf(3, null, 5, null)
val present = values.filterNotNull()
println(present)
println(present.sum())
```

# --output--

[3, 5]
8
