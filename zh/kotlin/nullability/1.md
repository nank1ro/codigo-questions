---
language: kotlin
exerciseType: 2
---

# --description--

有时一个值就是缺失的：没有中间名的用户、没有找到任何结果的搜索、无法转换成数字的文本。
Kotlin 用 `null` 表示缺失的值，但普通变量永远不能持有它。每个类型默认都是**非空**的：
```kotlin
var city: String = null // error: Null can not be a value of a non-null type String
```
要允许值缺失，你可以在类型后面加上问号 `?` 来声明一个**可空**类型。
`String?` 既可以持有 `String`，也可以持有 `null`：
```kotlin
var city: String? = "Rome"
city = null // allowed
println(city) // null
```
`String` 和 `String?` 是两个不同的类型：`String` 永远不会缺失，`String?` 则可能会。

# --instructions--

补全代码，用接受 `null` 的类型声明 `nickname`，然后把它设为 `null` 并打印它

# --seed--

```kotlin
var nickname: [/] = "ace"
nickname = [/]
println(nickname)
```

# --answers--

- String?
- String
- null
- nil
- None

# --solutions--

```kotlin
var nickname: String? = "ace"
nickname = null
println(nickname)
```

# --output--

null
