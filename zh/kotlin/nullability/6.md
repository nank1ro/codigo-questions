---
language: kotlin
exerciseType: 4
---

# --description--

安全调用链在处理嵌套对象时大放异彩，因为任何一层都可能缺失：
```kotlin
class Address(val city: String?)
class User(val address: Address?)

val user = User(Address("Rome"))
println(user.address?.city ?: "unknown") // Rome
val nobody = User(null)
println(nobody.address?.city ?: "unknown") // unknown
```
每个 `?.` 都保护下一步，最后的 `?:` 提供默认值。

# --instructions--

你能排列这些代码行，为没有地址的用户打印 `unknown` 吗？请先定义类，`Address` 在 `User` 之前

# --answers--

```kotlin
class Address(val city: String?)
```

```kotlin
class User(val address: Address?)
```

```kotlin
val bob = User(null)
```

```kotlin
println(bob.address?.city ?: "unknown")
```

# --solutions--

```kotlin
class Address(val city: String?)
class User(val address: Address?)
val bob = User(null)
println(bob.address?.city ?: "unknown")
```

# --output--

unknown
