---
language: kotlin
exerciseType: 1
---

# --instructions--

通过设置 `counter` 变量的值在输出中显示数字 `5` 到 `8`

# --seed--

```kotlin
fun main() {
    var counter =

    while (counter < 9) {
        println(counter)
        counter += 1
    }
}
```

# --solutions--

```kotlin
fun main() {
    var counter = 5

    while (counter < 9) {
        println(counter)
        counter += 1
    }
}
```

# --output--

5
6
7
8
