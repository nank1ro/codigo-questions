---
language: kotlin
exerciseType: 2
---

# --description--

`run` 就是换了一种对象命名方式的 `let`：在代码块内对象是 `this`，因此它的成员不需要前缀，而调用返回**最后一个表达式的结果**。

它适合这样的场景：你需要读取同一个对象的多个成员来计算出一个值：
```kotlin
class Rect(val w: Int, val h: Int)

val area = Rect(3, 4).run { w * h }
println(area) // 12
```
把它与 `apply` 比较一下：`apply` 以完全相同的方式使用 `this`，但返回的是对象而不是代码块的结果。

# --instructions--

用一个返回其代码块结果的作用域函数补全代码，让程序打印矩形的面积

# --seed--

```kotlin
class Rect(val w: Int, val h: Int)

fun main() {
    val area = Rect(3, 4).[/] { [/] * h }
    println(area)
}
```

# --answers--

- run
- w
- apply
- also
- let
- h

# --solutions--

```kotlin
class Rect(val w: Int, val h: Int)

fun main() {
    val area = Rect(3, 4).run { w * h }
    println(area)
}
```

# --output--

12
