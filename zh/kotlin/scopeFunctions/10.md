---
language: kotlin
exerciseType: 3
---

# --description--

`with` 做的工作与 `run` 相同，但它**不是**扩展：对象作为第一个参数传入，而不是作为点调用的接收者。
```kotlin
val text = with(StringBuilder()) {
    append("Hello")
    append(", world")
    toString()
}
println(text) // Hello, world
```
在代码块内对象是 `this`，调用返回最后一个表达式，与 `run` 完全一样。当你已经拥有一个非空对象、并想对它进行一组调用时，首选 `with`；当对象来自某个链式调用、或者可能需要安全调用（`obj?.run { ... }`）时，首选 `run`。

# --instructions--

这个程序会打印什么？

```kotlin
class Point(val x: Int, val y: Int)

fun main() {
    val point = Point(2, 5)
    val result = with(point) {
        x + y
    }
    println(result)
}
```

# --answers--

- `7`，因为 `with` 返回其代码块最后一个表达式的结果
- `2`，因为 `with` 返回对象的第一个属性
- 对象 `point`，因为 `with` 返回它接收到的对象
- 什么都不打印：无法编译，`with` 必须以 `point.with { ... }` 的方式调用

# --solutions--

- `7`，因为 `with` 返回其代码块最后一个表达式的结果
