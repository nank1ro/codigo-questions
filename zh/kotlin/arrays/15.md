---
language: kotlin
exerciseType: 4
---

# --instructions--

你能排列这些代码行，先构建 `0`、`1`、`2` 和 `3` 的平方，把它们变成标签，然后打印每个标签及其索引吗？

# --answers--

```kotlin
val squares = Array(4) { it * it }
```

```kotlin
val labels = squares.map { "sq=$it" }
```

```kotlin
for ((i, label) in labels.withIndex()) {
```

```kotlin
    println("$i $label")
```

```kotlin
}
```

# --solutions--

```kotlin
val squares = Array(4) { it * it }
val labels = squares.map { "sq=$it" }
for ((i, label) in labels.withIndex()) {
    println("$i $label")
}
```

# --output--

0 sq=0
1 sq=1
2 sq=4
3 sq=9
