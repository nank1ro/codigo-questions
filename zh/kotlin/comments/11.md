---
language: kotlin
exerciseType: 4
---

# --description--

在摘要之后，文档注释可以用 **KDoc 标签**来描述参数和返回值，这些标签总是以 `@` 开头：
```kotlin
/**
 * Returns the number of seconds in the given minutes.
 * @param minutes a whole number of minutes, never negative
 * @return [minutes] multiplied by sixty
 */
fun toSeconds(minutes: Int): Int {
    return minutes * 60
}
```
`@param` 后面跟参数名，然后是参数的描述；每个参数对应一个 `@param`。`@return` 描述函数返回的值。顺序总是固定的：先摘要，然后是 `@param` 标签，最后是 `@return`。

# --instructions--

排列这些条目，为 `twice` 添加文档：一个摘要、然后一个 `@param` 标签、再一个 `@return` 标签，全部放在函数正上方的同一个文档注释中，然后打印 `twice(21)`

# --answers--

```kotlin
/**
 * Returns the double of a number.
```

```kotlin
 * @param n the number to double
```

```kotlin
 * @return [n] multiplied by two
```

```kotlin
 */
```

```kotlin
fun twice(n: Int): Int {
    return n * 2
}
```

```kotlin
println(twice(21))
```

# --solutions--

```kotlin
/**
 * Returns the double of a number.
 * @param n the number to double
 * @return [n] multiplied by two
 */
fun twice(n: Int): Int {
    return n * 2
}
println(twice(21))
```

# --output--

42
