---
language: kotlin
exerciseType: 4
---

# --description--

块注释的一个常见用途是**头部注释**：放在函数正上方的一小段注释，说明函数的功能以及各参数的含义。
```kotlin
/*
Returns the number of seconds in the given minutes.
minutes is a whole number, never negative.
*/
fun toSeconds(minutes: Int): Int {
    return minutes * 60
}
```
调用 `toSeconds` 的人现在可以直接阅读头部注释，而不必看函数体。请把头部注释放在函数旁边，以便二者一起更新。

# --instructions--

排列这些条目，构建一个定义 `area` 的程序：`area` 的正上方有一个头部注释作为文档，然后打印 `area(3, 4)`

# --answers--

```kotlin
/*
```

```kotlin
Returns the area of a rectangle.
```

```kotlin
*/
```

```kotlin
fun area(w: Int, h: Int): Int {
    return w * h
}
```

```kotlin
println(area(3, 4))
```

# --solutions--

```kotlin
/*
Returns the area of a rectangle.
*/
fun area(w: Int, h: Int): Int {
    return w * h
}
println(area(3, 4))
```

# --output--

12
