---
language: kotlin
exerciseType: 2
---

# --description--

在链式调用中，`it` 的类型在每一步都会变化：在 `List<String>` 上执行 `filter` 之后你仍然拿到字符串，但在 `map { it.length }` 之后你得到的是 `List<Int>`，所以下一个 lambda 看到的是数字。
```kotlin
val words = listOf("kiwi", "fig")
println(words.map { it.length }.filter { it > 3 }) // [4]
```
每一步都返回一个**新**列表，从不改动前一个，因此可以把一条链拆成若干命名的中间值而不改变结果。

# --instructions--

补全代码，使 `result` 保存以 `a` 开头的水果并转成大写，让程序输出 `[APPLE, AVOCADO]`

# --seed--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.[/] { it.startsWith("a") }.[/] { it.[/] }
println(result)
```

# --answers--

- filter
- map
- uppercase()
- forEach
- count
- lowercase()
- length

# --solutions--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.filter { it.startsWith("a") }.map { it.uppercase() }
println(result)
```

# --output--

[APPLE, AVOCADO]
