---
language: kotlin
exerciseType: 4
---

# --description--

由于 `split` 返回一个 `List`，你可以像遍历任何其他列表一样遍历它的元素：
```kotlin
for (part in "a-b".split("-")) {
    println(part)
}
// prints a, then b
```

# --instructions--

你能排列这些代码行，把 `sentence` 的每个单词转成大写并逐行打印出来吗？

# --answers--

```kotlin
val sentence = "the quick brown fox"
```

```kotlin
val words = sentence.split(" ")
```

```kotlin
for (word in words) {
```

```kotlin
    println(word.uppercase())
```

```kotlin
}
```

# --solutions--

```kotlin
val sentence = "the quick brown fox"
val words = sentence.split(" ")
for (word in words) {
    println(word.uppercase())
}
```

# --output--

THE
QUICK
BROWN
FOX
