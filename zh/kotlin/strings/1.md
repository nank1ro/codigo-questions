---
language: kotlin
exerciseType: 2
---

# --description--

`String` 是写在双引号之间的字符序列。
`length` 属性表示字符串包含多少个字符，包括空格：
```kotlin
val greeting = "Hello world"
println(greeting.length) // 11
println("".length)       // 0
```

# --instructions--

补全代码以打印 `word` 中的字符数量

# --seed--

```kotlin
val word = "Kotlin"
println(word.[/])
```

# --answers--

- length
- size
- len
- length()
- count

# --solutions--

```kotlin
val word = "Kotlin"
println(word.length)
```

# --output--

6
