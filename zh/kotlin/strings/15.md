---
language: kotlin
exerciseType: 2
---

# --instructions--

补全代码，先打印小写的 `clean`，再打印字符 `@` 的索引

# --seed--

```kotlin
val email = "  User@Example.com "
val clean = email.trim()
println(clean.[/]())
println(clean.[/]("@"))
```

# --answers--

- lowercase
- uppercase
- indexOf
- contains
- trim
- length

# --solutions--

```kotlin
val email = "  User@Example.com "
val clean = email.trim()
println(clean.lowercase())
println(clean.indexOf("@"))
```

# --output--

user@example.com
4
