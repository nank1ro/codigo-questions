---
language: kotlin
exerciseType: 1
---

# --description--

抛出和捕获异常并非没有开销，对于常见的转换，Kotlin 提供了一种更省事的变体：它们不抛出异常，而是直接返回 `null`：`toIntOrNull()`、`toDoubleOrNull()`、`toLongOrNull()`。

```kotlin
println("42".toIntOrNull())  // 42
println("abc".toIntOrNull()) // null
```
结合 elvis 运算符 `?:`——当它左边的值为 `null` 时提供一个替代值——整个 `try`/`catch` 可以压缩成一行：
```kotlin
val n = "abc".toIntOrNull() ?: 0
println(n) // 0
```
当失败确实属于例外情况时使用 `try`/`catch`；当预料到会出现非法输入时使用 `toIntOrNull()`。

# --instructions--

编写一个函数 `safeSum(a: String, b: String): Int`，返回两个数字之和，任何不是数字的文本都按 `0` 计算。使用 `toIntOrNull()` 和 elvis 运算符，而不要用 `try`/`catch`。

函数调用示例：
```kotlin
println(safeSum("2", "x"))
// prints 2
```

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --seed--

```kotlin
fun safeSum(a: String, b: String): Int {
    
}
```

# --asserts--

`safeSum("2", "3")` 必须返回 `5`

```kotlin
    tryCatch(safeSum("2", "3") == 5)
```

`safeSum("2", "x")` 必须返回 `2`

```kotlin
    tryCatch(safeSum("2", "x") == 2)
```

`safeSum("a", "b")` 必须返回 `0`

```kotlin
    tryCatch(safeSum("a", "b") == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun safeSum(a: String, b: String): Int {
    return (a.toIntOrNull() ?: 0) + (b.toIntOrNull() ?: 0)
}
```
