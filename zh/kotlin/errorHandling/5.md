---
language: kotlin
exerciseType: 2
---

# --description--

可以在最后添加一个 `finally` 块。**无论发生什么**它都会运行：`try` 成功之后、`catch` 完成恢复之后，甚至在异常完全没有被捕获时也是如此。

```kotlin
try {
    println("reading")
    println("abc".toInt())
} catch (e: NumberFormatException) {
    println("bad number")
} finally {
    println("closing")
}
```
```
reading
bad number
closing
```
这使它成为放置绝不能被跳过的清理工作的位置，比如关闭一个文件。一个 `try` 至少需要有一个 `catch` 或一个 `finally`，但也可以两者都有。

# --instructions--

补全代码，使错误的转换被捕获，且清理这一行总是运行。

# --seed--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } [/] (e: NumberFormatException) {
        println("bad number")
    } [/] {
        println("closing")
    }
}
```

# --answers--

- catch
- finally
- try
- throw
- else
- do

# --solutions--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } catch (e: NumberFormatException) {
        println("bad number")
    } finally {
        println("closing")
    }
}
```

# --output--

bad number
closing
