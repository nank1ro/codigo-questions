---
language: kotlin
exerciseType: 4
---

# --description--

因为 `Result` 是一个普通的值，它可以被存放在 `val` 中，并且可以随你喜欢地反复查询：

```kotlin
val result = runCatching { "abc".toInt() }

println(result.getOrElse { 0 })  // 0
println(result.getOrNull())      // null
println(result.isSuccess)        // false
```
`try`/`catch` 做不到这一点。在那里，结果只在失败发生的那一刻被处理一次，之后就消失了。`Result` 会把失败保留下来，因此对它做出反应的代码不必待在失败发生的地方。

# --instructions--

对这些条目排序，使程序先保存转换的结果，然后打印恢复出的值，接着打印原始结果，最后打印这次尝试是否成功。

# --answers--

```kotlin
val result = runCatching { "abc".toInt() }
```

```kotlin
val value = result.getOrElse { 0 }
```

```kotlin
println("value: $value")
```

```kotlin
println("raw: ${result.getOrNull()}")
```

```kotlin
println("success: ${result.isSuccess}")
```

# --solutions--

```kotlin
val result = runCatching { "abc".toInt() }
val value = result.getOrElse { 0 }
println("value: $value")
println("raw: ${result.getOrNull()}")
println("success: ${result.isSuccess}")
```

# --output--

value: 0
raw: null
success: false
