---
language: kotlin
exerciseType: 3
---

# --instructions--

给定 `enum class Size { SMALL, MEDIUM, LARGE }`，下面这段代码会发生什么？

```kotlin
fun label(size: Size): String = when (size) {
    Size.SMALL -> "S"
    Size.MEDIUM -> "M"
}
```

# --answers--

- 无法编译：对枚举的 `when` 表达式必须覆盖所有枚举项，或者添加 `else` 分支
- 可以编译，对 `LARGE` 返回 `null`
- 可以编译，对 `LARGE` 在运行时抛出异常
- 可以编译，对 `LARGE` 自动返回 `"L"`

# --solutions--

- 无法编译：对枚举的 `when` 表达式必须覆盖所有枚举项，或者添加 `else` 分支
