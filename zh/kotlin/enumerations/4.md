---
language: kotlin
exerciseType: 2
---

# --description--

枚举类可以像普通类一样拥有**构造函数**。每个枚举项传入自己的参数，这些值会被存储在属性中：
```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
println(Planet.MARS.moons) // 2
```
构造函数属性通常用 `val` 声明，因为一个枚举项的数据不应该改变。

# --instructions--

补全代码，使每个 `Coin` 都以美分为单位存储其面值，然后打印 `PENNY`（价值 `1` 美分）的面值

# --seed--

```kotlin
enum class Coin([/] cents: Int) {
    PENNY([/]),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.[/])
}
```

# --answers--

- val
- fun
- const
- 1
- "1"
- cents
- name
- ordinal

# --solutions--

```kotlin
enum class Coin(val cents: Int) {
    PENNY(1),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.cents)
}
```

# --output--

1
