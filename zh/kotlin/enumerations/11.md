---
language: kotlin
exerciseType: 4
---

# --instructions--

排序这些代码块以打印出 `Slow down`。先声明枚举，再声明函数 `action`，最后是 `main`，并保持 `when` 分支的顺序与枚举项的顺序一致。

# --answers--

```kotlin
enum class Light {
    RED, YELLOW, GREEN
}
```

```kotlin
fun action(light: Light): String = when (light) {
```

```kotlin
    Light.RED -> "Stop"
```

```kotlin
    Light.YELLOW -> "Slow down"
```

```kotlin
    Light.GREEN -> "Go"
```

```kotlin
}
```

```kotlin
fun main() {
    println(action(Light.YELLOW))
}
```

# --solutions--

```kotlin
enum class Light {
    RED, YELLOW, GREEN
}
fun action(light: Light): String = when (light) {
    Light.RED -> "Stop"
    Light.YELLOW -> "Slow down"
    Light.GREEN -> "Go"
}
fun main() {
    println(action(Light.YELLOW))
}
```

# --output--

Slow down
