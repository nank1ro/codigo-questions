---
language: kotlin
exerciseType: 1
---

# --description--

`replace` 重写文本：它返回一个**新**字符串，其中每个匹配都被替换内容换掉，而原始字符串保持不变。
```kotlin
println(Regex("""\d+""").replace("a1 b22", "#")) // a# b#
```
在替换字符串中，`$1`、`$2`……代表该匹配捕获的组，所以你可以重新排列或复用匹配到的部分：
```kotlin
val name = Regex("""(\w+) (\w+)""")
println(name.replace("Ann Lee", "$2 $1")) // Lee Ann
```
`$0` 是整个匹配。如果你需要在替换中出现字面意义上的 `$`，把它转义为 `\$`。
`replace` 重写**每一个**匹配，所以当只想重写完整的字符串时，用**锚点** `^`（文本开头）和 `$`（文本结尾）来固定模式：
```kotlin
println(Regex("""^\w+$""").replace("one two", "x")) // one two, nothing is replaced
```

# --instructions--

编写一个函数 `lastNameFirst(name: String): String`，使用带组引用的 `replace` 把 `"Ann Lee"` 变成 `"Lee, Ann"`。当 `name` 不是由一个空格分隔的两个单词时，原样返回它。

示例：`lastNameFirst("Ann Lee")` 返回 `Lee, Ann`。

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`lastNameFirst("Ann Lee")` 必须返回 `Lee, Ann`

```kotlin
    tryCatch(lastNameFirst("Ann Lee") == "Lee, Ann")
```

`lastNameFirst("Marco Rossi")` 必须返回 `Rossi, Marco`

```kotlin
    tryCatch(lastNameFirst("Marco Rossi") == "Rossi, Marco")
```

`lastNameFirst("Cher")` 必须返回 `Cher`

```kotlin
    tryCatch(lastNameFirst("Cher") == "Cher")
```

`lastNameFirst("")` 必须返回一个空字符串

```kotlin
    tryCatch(lastNameFirst("") == "")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun lastNameFirst(name: String): String {
    return Regex("""^(\w+) (\w+)$""").replace(name, "$2, $1")
}
```
