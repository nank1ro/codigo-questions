---
language: kotlin
exerciseType: 3
---

# --instructions--

我如何访问 `forEach` 循环的元素？

# --answers--

- 使用 `it` 关键字
- 使用在 `in` 之前声明的变量名
- 使用 `variableName ->`

# --solutions--

- 使用 `it` 关键字
- 使用 `variableName ->`

