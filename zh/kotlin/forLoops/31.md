---
language: kotlin
exerciseType: 3
---

# --instructions--

`..` 和 `until` 有什么区别？

# --answers--

- `..` 包含其末端元素，而 `until` 不包含
- `until` 包含其末端元素，而 `..` 不包含
- 它们是一样的

# --solutions--

- `..` 包含其末端元素，而 `until` 不包含

