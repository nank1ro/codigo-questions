---
language: kotlin
exerciseType: 2
---

# --description--

单表达式函数使用 `=` 代替块体：
```kotlin
fun triple(n: Int) = n * 3
```
这是以下写法的简写：
```kotlin
fun triple(n: Int): Int {
    return n * 3
}
```

# --instructions--

补全这个将两个整数相加的单表达式函数。

# --seed--

```kotlin
fun add(a: Int, b: Int) [/] a [/] b
fun main() {
    println(add(3, 4))
}
```

# --answers--

- =
- +
- -
- *
- return
- ->

# --solutions--

```kotlin
fun add(a: Int, b: Int) = a + b
fun main() {
    println(add(3, 4))
}
```

# --output--

7
