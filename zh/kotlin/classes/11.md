---
language: kotlin
exerciseType: 2
---

# --instructions--

补全代码，在 `AppConfig` 中添加一个 `companion object`，包含值为 `"1.0"` 的 `val` 属性 `version`。

# --seed--

```kotlin
class AppConfig {
    [/] [/] {
        val version = "1.0"
    }
}
```

# --answers--

- companion
- object
- class
- static
- init
- val

# --solutions--

```kotlin
class AppConfig {
    companion object {
        val version = "1.0"
    }
}
```
