---
language: kotlin
exerciseType: 2
---

# --instructions--

补全代码，定义一个名为 `Book` 的 `class`，其主构造函数包含 `String` 类型的 `val` 属性 `title`。

# --seed--

```kotlin
[/] Book([/] title: String)
```

# --answers--

- class
- val
- var
- fun
- object
- data

# --solutions--

```kotlin
class Book(val title: String)
```
