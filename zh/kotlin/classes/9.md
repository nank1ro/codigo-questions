---
language: kotlin
exerciseType: 2
---

# --instructions--

补全代码，声明一个名为 `Color` 的 `data class`，包含 `Int` 类型的 `val` 属性 `red`、`green` 和 `blue`。

# --seed--

```kotlin
[/] [/] Color(val red: Int, val green: Int, val blue: Int)
```

# --answers--

- data
- class
- object
- open
- abstract
- sealed

# --solutions--

```kotlin
data class Color(val red: Int, val green: Int, val blue: Int)
```
