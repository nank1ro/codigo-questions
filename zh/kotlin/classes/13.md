---
language: kotlin
exerciseType: 3
---

# --instructions--

`data class` 会自动为你生成什么？

# --answers--

- equals()、hashCode()、toString() 和 copy()
- 仅 toString()
- 仅 equals() 和 hashCode()
- 什么都不生成——你必须自己编写所有内容

# --solutions--

- equals()、hashCode()、toString() 和 copy()
