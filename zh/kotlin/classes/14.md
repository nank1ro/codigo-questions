---
language: kotlin
exerciseType: 4
---

# --instructions--

你能将这些行排列好，定义一个 `data class` `Student` 并打印其 `toString()` 吗？

# --answers--

```kotlin
data class Student(val name: String, val grade: Int)
```

```kotlin
fun main() {
```

```kotlin
    val s = Student("Alice", 90)
```

```kotlin
    println(s.toString())
```

```kotlin
}
```

# --solutions--

```kotlin
data class Student(val name: String, val grade: Int)
fun main() {
    val s = Student("Alice", 90)
    println(s.toString())
}
```

# --output--

Student(name=Alice, grade=90)
