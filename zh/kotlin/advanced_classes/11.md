---
language: kotlin
exerciseType: 1
---

# --description--

`interface`（接口）列出了一个类型能做什么。它的成员默认是抽象的，但接口也可以提供**默认实现**——一个每个实现类都免费继承并且可以覆盖的方法体：

```kotlin
interface Greeter {
    val name: String              // abstract, the class must provide it
    fun greet(): String = "Hi, $name"  // default implementation
}

class Person(override val name: String) : Greeter

class Robot(override val name: String) : Greeter {
    override fun greet(): String = "BEEP $name"
}

println(Person("Ann").greet()) // Hi, Ann
println(Robot("R2").greet())   // BEEP R2
```

接口不能存储状态（它没有幕后字段），所以抽象属性必须由类来实现，通常是通过构造函数中的 `override val`。与类不同，一个类型可以实现任意多个接口。

# --instructions--

声明一个接口 `Greeter`，包含一个 `String` 类型的抽象 `val` 属性 `name` 和一个方法 `greet(): String`，其默认实现对 `"Ann"` 这个 `name` 返回 `"Hello, Ann!"`。

然后声明一个实现 `Greeter` 并在其构造函数中接受 `name` 的类 `Person`，以及一个同样实现 `Greeter` 但覆盖 `greet()` 使其对 `"Bo"` 这个 `name` 返回 `"Ahoy, Bo!"` 的类 `Pirate`。

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`Person("Ann").greet()` 必须返回 `"Hello, Ann!"`

```kotlin
    tryCatch(Person("Ann").greet() == "Hello, Ann!")
```

`Person("Cy").name` 必须返回 `"Cy"`

```kotlin
    tryCatch(Person("Cy").name == "Cy")
```

`Pirate("Bo").greet()` 必须返回 `"Ahoy, Bo!"`

```kotlin
    tryCatch(Pirate("Bo").greet() == "Ahoy, Bo!")
```

两个类都必须能作为 `Greeter` 使用

```kotlin
    val greeters: List<Greeter> = listOf(Person("Ann"), Pirate("Bo"))
    tryCatch(greeters.map { it.greet() } == listOf("Hello, Ann!", "Ahoy, Bo!"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
interface Greeter {
    val name: String
    fun greet(): String = "Hello, $name!"
}

class Person(override val name: String) : Greeter

class Pirate(override val name: String) : Greeter {
    override fun greet(): String = "Ahoy, $name!"
}
```
