---
language: kotlin
exerciseType: 1
---

# --description--

`sealed` 类描述了一个**封闭**的备选项集合：只允许编写在同一个包和模块中的子类，因此编译器知道每一个子类。

```kotlin
sealed class Shape
data class Circle(val radius: Int) : Shape()
data class Square(val side: Int) : Shape()
```

它的回报是**穷举的 `when`**：当你对密封类型分支并覆盖每个子类时，可以省略 `else` 分支。之后新增一个子类时，编译器会报告每一个你忘记更新的 `when`，而不是默默走 `else` 分支。

```kotlin
fun name(shape: Shape): String = when (shape) {
    is Circle -> "circle"
    is Square -> "square"
}
```

在 `is Circle` 之后，值会被智能转换，因此在该分支内可以直接使用 `shape.radius`，无需任何手动转换。

# --instructions--

声明一个密封类 `Shape`，包含两个 data class 子类：`Square`，带有 `Int` 类型的 `val` 属性 `side`；以及 `Rectangle`，带有 `Int` 类型的 `val` 属性 `width` 和 `height`。

然后编写一个函数 `area(shape: Shape): Int`，使用不带 `else` 分支的穷举 `when` 返回形状的面积。

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`area(Square(3))` 必须返回 `9`

```kotlin
    tryCatch(area(Square(3)) == 9)
```

`area(Rectangle(2, 5))` 必须返回 `10`

```kotlin
    tryCatch(area(Rectangle(2, 5)) == 10)
```

`area(Square(0))` 必须返回 `0`

```kotlin
    tryCatch(area(Square(0)) == 0)
```

`Square` 和 `Rectangle` 必须是 `Shape` 的子类

```kotlin
    val shapes: List<Shape> = listOf(Square(4), Rectangle(3, 3))
    tryCatch(shapes.map { area(it) } == listOf(16, 9))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
sealed class Shape
data class Square(val side: Int) : Shape()
data class Rectangle(val width: Int, val height: Int) : Shape()

fun area(shape: Shape): Int = when (shape) {
    is Square -> shape.side * shape.side
    is Rectangle -> shape.width * shape.height
}
```
