---
language: kotlin
exerciseType: 2
---

# --instructions--

补全代码，使 `ofKilometres` 成为 `Distance` 的一个工厂函数，根据公里数构建一个 `Distance`

# --seed--

```kotlin
class Distance private constructor(val meters: Int) {
    [/] object {
        fun ofKilometres(km: Int) = [/](km * 1000)
    }
}

fun main() {
    println(Distance.ofKilometres(3).meters)
}
```

# --answers--

- companion
- Distance
- data
- inner
- Int
- meters

# --solutions--

```kotlin
class Distance private constructor(val meters: Int) {
    companion object {
        fun ofKilometres(km: Int) = Distance(km * 1000)
    }
}

fun main() {
    println(Distance.ofKilometres(3).meters)
}
```

# --output--

3000
