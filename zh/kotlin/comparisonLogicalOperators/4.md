---
language: kotlin
exerciseType: 2
---

# --description--

让我们继续学习**小于** `<` 比较运算符。
它返回一个**布尔值**（`true` 或 `false`），表示一个表达式是否小于另一个，例如：
```kotlin
println(2 < 2) // false
println(2 < 3) // true
```

# --instructions--

打印一个布尔值，检查 `num` 是否小于 `4`

# --seed--

```kotlin
var num = 5
println([/][/][/])
```

# --answers--

- num 
- num 
- != 
- < 
- << 
- 4

# --solutions--

```kotlin
var num = 5
println(num < 4)
```

# --output--

false
