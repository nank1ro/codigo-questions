---
language: kotlin
exerciseType: 2
---

# --description--

让我们从**等于** `==` 比较运算符开始。
它返回一个**布尔值**（`true` 或 `false`），表示两个表达式是否相等，例如：
```kotlin
println(2 == 2) // true
println(2 == 3) // false
```

# --instructions--

打印一个布尔值，检查 `num` 是否等于 `5`

# --seed--

```kotlin
var num = 5
println([/][/][/])
```

# --answers--

- num 
- num 
- == 
- = 
- 5

# --solutions--

```kotlin
var num = 5
println(num == 5)
```

# --output--

true
