---
language: kotlin
exerciseType: 2
---

# --description--

在 `${}` 中我们还可以放置条件表达式，例如：
```kotlin
println("${if (true) "Correct" else "Wrong"}")
// prints Correct
```

# --instructions--

打印 `ERROR`

# --seed--

```kotlin
println("${if ([/]) "OK" else [/]}")
```

# --answers--

- false
- true
- "ERROR"
- ERROR

# --solutions--

```kotlin
println("${if (false) "OK" else "ERROR"}")
```

# --output--

ERROR
