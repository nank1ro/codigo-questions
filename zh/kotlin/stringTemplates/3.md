---
language: kotlin
exerciseType: 2
---

# --description--

字符串模板允许我们在不产生任何错误的情况下显示表达式，例如将字符串与数字相加。
将表达式放在 `${}` 中可以对其求值。
返回值会被转换为字符串并插入到结果字符串中

# --seed--

```kotlin
println([/])
```

# --answers--

- "${5} friends"

# --solutions--

```kotlin
println("${5} friends")
```

# --output--

5 friends
