---
language: kotlin
exerciseType: 2
---

# --instructions--

找到一种方法将 `"monkey"` 字符串添加到 `"elephant"` 之前。

# --seed--

```kotlin
val animals = mutableListOf("cat", "turtle", "elephant")
val indexOfElephant = animals.[/] { [/] == [/]}
animals.add([/], "monkey")
println(animals)
```

# --answers--

- index
- it
- "elephant"
- elephant
- indexOfFirst
- indexOfElephant
- insert
- append
- add

# --solutions--

```kotlin
val animals = mutableListOf("cat", "turtle", "elephant")
val indexOfElephant = animals.indexOfFirst { it == "elephant"}
animals.add(indexOfElephant, "monkey")
println(animals)
```

# --output--

[cat, turtle, monkey, elephant]
