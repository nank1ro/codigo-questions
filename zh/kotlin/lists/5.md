---
language: kotlin
exerciseType: 2
---

# --description--

和字符串一样，列表有一个通过 `size` 属性获取的**长度**。
列表的长度是它所包含的元素个数。

# --instructions--

补全代码以打印列表的长度

# --seed--

```kotlin
val numbers = listOf(5, 6, 8, 4)
println([/][/][/])
```

# --answers--

- len
- length
- numbers
- count
- .
- size

# --solutions--

```kotlin
val numbers = listOf(5, 6, 8, 4)
println(numbers.size)
```

# --output--

4
