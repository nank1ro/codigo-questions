---
language: kotlin
exerciseType: 2
---

# --description--

让我们继续上一个例子。
```kotlin
var niceWeather = true
if (niceWeather) {
    // play outside
}
```
我们已经看到 `if` 语句只在条件为 `true` 时执行代码块。
另一个需要注意的重要事项是**花括号** `{}`，它们表示一个代码块。

# --instructions--

通过添加*花括号* `{}` 来修复 `if` 语句

# --seed--

```kotlin
var time = 8
if (time < 12) [/]
    println("It's morning")
[/]
```

# --answers--

- if
- ==
- =
- else
- ?
- {
- }

# --solutions--

```kotlin
var time = 8
if (time < 12) {
    println("It's morning")
}
```

# --output--

It's morning
