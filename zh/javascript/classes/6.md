---
language: javascript
exerciseType: 2
---

# --description--

JavaScript 允许我们使用 `extends` 关键字创建一个类作为另一个类的子类

# --instructions--

创建一个新的 `Dog` 类作为 `Animal` 类的子类

# --seed--

```javascript
class Animal {
    constructor(gender, legs = 4) {
        this.gender = gender;
        this.legs = legs;
    }
    description() {
        console.log(this.gender);
        console.log(this.legs);
    }
}
[/][/][/]Animal {
    speak() {
      console.log("Bau Bau!!");
    }
}
let dog = new [/]("female");
dog.description();
dog.speak();
```

# --answers--

- class 
- Dog
- Animal
- Dog
- ()
-  extends 
- Dog[]

# --solutions--

```javascript
class Animal {
    constructor(gender, legs = 4) {
        this.gender = gender;
        this.legs = legs;
    }
    description() {
        console.log(this.gender);
        console.log(this.legs);
    }
}
class Dog extends Animal {
    speak() {
      console.log("Bau Bau!!");
    }
}
let dog = new Dog("female");
dog.description();
dog.speak();
```

# --output--

female
4
Bau Bau!!
