---
language: javascript
exerciseType: 4
---

# --description--

在函数声明中，模式可以取代参数名，这样解包就会在调用发生时进行：
```javascript
function area({ width, height }) {
    return width * height;
}
console.log(area({ width: 4, height: 3 }));
// 打印 12
```
在函数体内部完全没有对象变量，只有 `width` 和 `height`。调用者传入一个对象，但签名清楚地记录了函数读取的是哪些键，而且这些键可以以任意顺序到达。

# --instructions--

将这些代码块排列成一个程序：先定义函数，然后输出矩形的面积

# --answers--

```javascript
function area({ width, height }) {
```

```javascript
  return width * height;
}
```

```javascript
const rect = { width: 4, height: 3 };
```

```javascript
console.log(area(rect));
```

# --solutions--

```javascript
function area({ width, height }) {
  return width * height;
}
const rect = { width: 4, height: 3 };
console.log(area(rect));
```

# --output--

12
