---
language: javascript
exerciseType: 4
---

# --description--

对象模式用键来为变量命名，当键含义模糊或者已经被占用时就会很别扭。写上 `key: newName` 可以**重命名**变量：
```javascript
const row = { n: "Ada", y: 1815 };
const { n: name, y: born } = row;
console.log(name, born);
// 打印 Ada 1815
```
把它读作“取出 `n`，叫它 `name`”。冒号并不声明类型，而且 `n` 本身永远不会被创建为变量，只有 `name` 会。被重命名的名字仍然可以有默认值，写在它的后面：`{ n: name = "unknown" }`。

# --instructions--

将这些代码块排列成一个程序：重命名 response 的短键并输出 `Ada from London`

# --answers--

```javascript
const response = { n: "Ada", c: "London" };
```

```javascript
const { n: name, c: city } = response;
```

```javascript
const line = `${name} from ${city}`;
```

```javascript
console.log(line);
```

# --solutions--

```javascript
const response = { n: "Ada", c: "London" };
const { n: name, c: city } = response;
const line = `${name} from ${city}`;
console.log(line);
```

# --output--

Ada from London
