---
language: javascript
exerciseType: 2
---

# --description--

我们可以使用 `<` 来循环到下一个数字（不包含该数字），或者使用 `<=` 来循环到下一个数字（包含该数字）

# --instructions--

补全以下代码中的 `for` 循环，使代码块执行 `3` 次

# --seed--

```javascript
for (let i = 1; i[/]3; i++) {
    console.log(i);
}
```

# --answers--

- ...
- ..<
- ..>
- .<
-  <= 
-  < 
-  >= 

# --solutions--

```javascript
for (let i = 1; i <= 3; i++) {
    console.log(i);
}
```

# --output--

1
2
3
