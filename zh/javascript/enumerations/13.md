---
language: javascript
exerciseType: 4
---

# --description--

当每个成员都需要多项数据或自己的方法时，**类**可以扮演枚举的角色。每个成员都是该类的一个实例，存储在一个 `static` 属性中，也就是属于类本身而不是属于每个实例的属性：
```javascript
class Planet {
  static MERCURY = new Planet("Mercury", 0.4);
  static EARTH = new Planet("Earth", 1);

  constructor(name, gravity) {
    this.name = name;
    this.gravity = gravity;
  }
}
console.log(Planet.EARTH.name);
// prints Earth
```
在类之后调用 `Object.freeze(Planet)`，防止任何人添加或替换成员，并在构造函数中用 `Object.freeze(this)` 冻结每个实例，使成员本身保持只读。

# --instructions--

对这些代码行排序，以声明基于类的枚举 `Coin`，并打印 `DIME` 成员的 cents。
`PENNY` 要声明在 `DIME` 之前，两者都要在构造函数之前，并且要在打印之前冻结类

# --answers--

```javascript
class Coin {
```

```javascript
  static PENNY = new Coin("penny", 1);
```

```javascript
  static DIME = new Coin("dime", 10);
```

```javascript
  constructor(name, cents) { this.name = name; this.cents = cents; }
```

```javascript
}
```

```javascript
Object.freeze(Coin);
```

```javascript
console.log(Coin.DIME.cents);
```

# --solutions--

```javascript
class Coin {
  static PENNY = new Coin("penny", 1);
  static DIME = new Coin("dime", 10);
  constructor(name, cents) { this.name = name; this.cents = cents; }
}
Object.freeze(Coin);
console.log(Coin.DIME.cents);
```

# --output--

10
