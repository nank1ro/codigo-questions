---
language: javascript
exerciseType: 1
---

# --instructions--

声明冻结的枚举 `Suit`，包含成员 `HEARTS`、`DIAMONDS`、`CLUBS` 和 `SPADES`，每个成员都持有一个 `Symbol`，其描述是该成员名称的小写形式

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Suit = 
```

# --asserts--

`Suit` 的每个成员都必须是 symbol。

```javascript
tryCatch(Object.values(Suit).every((s) => typeof s === "symbol"));
```

`Suit.HEARTS.description` 必须是 `"hearts"`。

```javascript
tryCatch(Suit.HEARTS.description === "hearts");
```

`Suit.DIAMONDS`、`Suit.CLUBS` 和 `Suit.SPADES` 的描述必须分别是 `"diamonds"`、`"clubs"` 和 `"spades"`。

```javascript
tryCatch(Suit.DIAMONDS.description === "diamonds" && Suit.CLUBS.description === "clubs" && Suit.SPADES.description === "spades");
```

`Suit` 必须恰好有四个成员，且彼此都不相同。

```javascript
tryCatch(new Set(Object.values(Suit)).size === 4);
```

`Suit` 必须被冻结。

```javascript
tryCatch(Object.isFrozen(Suit) === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Suit = Object.freeze({
  HEARTS: Symbol("hearts"),
  DIAMONDS: Symbol("diamonds"),
  CLUBS: Symbol("clubs"),
  SPADES: Symbol("spades"),
});
```
