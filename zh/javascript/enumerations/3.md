---
language: javascript
exerciseType: 3
---

# --description--

为什么要冻结对象呢？被冻结的对象会拒绝任何更改：对现有成员赋值、添加新成员或删除成员都不会产生任何效果。
拒绝的表现方式取决于代码运行的模式：
- 在**非严格模式**（sloppy mode，普通脚本的默认模式）下，赋值会被**静默忽略**
- 在**严格模式**（strict mode，以 `"use strict"` 开头的文件、ES 模块以及类的内部）下，它会**抛出** `TypeError`

```javascript
const Size = Object.freeze({ SMALL: "s", LARGE: "l" });
Size.SMALL = "xs";
Size.MEDIUM = "m";
console.log(Size.SMALL);
// prints s
console.log(Size.MEDIUM);
// prints undefined
```
无论哪种情况，枚举都会保留你定义的值，这正是一组常量所需要的。

# --instructions--

以下代码在非严格模式（sloppy mode）下运行时会打印什么？
```javascript
const Level = Object.freeze({ LOW: 1, HIGH: 3 });
Level.LOW = 0;
console.log(Level.LOW);
```

# --answers--

- 1
- 0
- undefined
- 一个 TypeError，因为 Level 已被冻结

# --solutions--

- 1
