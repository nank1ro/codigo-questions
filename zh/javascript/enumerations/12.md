---
language: javascript
exerciseType: 2
---

# --description--

枚举的值经常被用作另一个对象的**键**，例如将每个成员映射到一个标签或价格。在对象字面量中，把一个键用方括号 `[ ]` 包裹起来会对表达式求值，并将其结果用作键（**计算属性名**）。这对字符串成员和 symbol 成员都适用：
```javascript
const Status = Object.freeze({ ACTIVE: "active", DONE: "done" });
const labels = {
  [Status.ACTIVE]: "In progress",
  [Status.DONE]: "Completed",
};
console.log(labels[Status.DONE]);
// prints Completed
```
如果不加方括号，`Status.DONE: "Completed"` 会是语法错误，而 `"Status.DONE"` 只会是一个普通的字符串键。

# --instructions--

使用枚举成员作为键，构建对象 `prices`，将 `Size.SMALL` 映射到 `3`，将 `Size.LARGE` 映射到 `5`，然后打印大号尺寸的价格

# --seed--

```javascript
const Size = Object.freeze({ SMALL: "small", LARGE: "large" });
const prices = {
  [/]: 3,
  [/]: 5,
};
console.log(prices[/]);
```

# --answers--

- [Size.SMALL]
- [Size.LARGE]
- [Size.LARGE]
- Size.SMALL
- Size.LARGE
- .LARGE
- (Size.LARGE)

# --solutions--

```javascript
const Size = Object.freeze({ SMALL: "small", LARGE: "large" });
const prices = {
  [Size.SMALL]: 3,
  [Size.LARGE]: 5,
};
console.log(prices[Size.LARGE]);
```

# --output--

5
