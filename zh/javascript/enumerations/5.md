---
language: javascript
exerciseType: 1
---

# --description--

因为枚举只是一个对象，常见的对象工具方法可以用来检查它：
- `Object.keys(Enum)` 返回一个包含成员**名称**的数组
- `Object.values(Enum)` 返回一个包含成员**值**的数组
- `Object.entries(Enum)` 返回一个 `[名称, 值]` 对的数组

```javascript
const Color = Object.freeze({ RED: "red", BLUE: "blue" });
console.log(Object.keys(Color));
// prints [ 'RED', 'BLUE' ]
console.log(Object.values(Color));
// prints [ 'red', 'blue' ]
```
将 `Object.values()` 与数组方法 `includes()` 结合使用，是检查任意值（例如从用户输入中读取的值）是否为有效成员的标准方式：
```javascript
console.log(Object.values(Color).includes("red"));
// prints true
console.log(Object.values(Color).includes("pink"));
// prints false
```

# --instructions--

编写一个函数 `isColor(value)`，如果 `value` 是 `Color` 枚举的某个值则返回 `true`，否则返回 `false`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Color = Object.freeze({
  RED: "red",
  GREEN: "green",
  BLUE: "blue",
});

function isColor(value) {

}
```

# --asserts--

`isColor("red")` 必须返回 `true`。

```javascript
tryCatch(isColor("red") === true);
```

`isColor("blue")` 必须返回 `true`。

```javascript
tryCatch(isColor("blue") === true);
```

`isColor("pink")` 必须返回 `false`。

```javascript
tryCatch(isColor("pink") === false);
```

`isColor("RED")` 必须返回 `false`，因为 `"RED"` 是成员名称，而不是值。

```javascript
tryCatch(isColor("RED") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Color = Object.freeze({
  RED: "red",
  GREEN: "green",
  BLUE: "blue",
});

function isColor(value) {
  return Object.values(Color).includes(value);
}
```
