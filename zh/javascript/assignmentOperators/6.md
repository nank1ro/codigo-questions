---
language: javascript
exerciseType: 2
---

# --description--

让我们来看看**取余赋值** `%=` 运算符。
它计算变量与一个值的余数，并将结果赋给该变量。
所以下面两种写法完全相同
```javascript
num = num % 5;
// is equal to
num %= 5;
```

# --instructions--

使用*取余赋值*运算符计算 `result` 除以值 `3` 的余数，然后打印它的值

# --seed--

```javascript
var result = 5;
result [/][/];
console.log(result);
```

# --answers--

- % 
- =% 
- %= 
- 3

# --solutions--

```javascript
var result = 5;
result %= 3;
console.log(result);
```

# --output--

2
