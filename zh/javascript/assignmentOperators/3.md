---
language: javascript
exerciseType: 2
---

# --description--

就像加法赋值一样，我们还有**减法赋值** `-=`。
功能是一样的，唯一的区别是它执行的是减法运算。
所以下面两种写法完全相同
```javascript
var num = num - 5;
// is equal to
num -= 5;
```

# --instructions--

使用*减法赋值*运算符从变量 `result` 中减去值 `15`，然后打印它的值

# --seed--

```javascript
var result = 75;
result [/][/];
console.log(result);
```

# --answers--

- - 
- -= 
- =- 
- 50
- 15

# --solutions--

```javascript
var result = 75;
result -= 15;
console.log(result);
```

# --output--

60
