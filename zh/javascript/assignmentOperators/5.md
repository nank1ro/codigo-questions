---
language: javascript
exerciseType: 2
---

# --description--

让我们来看看**除法赋值** `/=` 运算符。
它将变量除以一个值，并将结果赋给该变量。
所以下面两种写法完全相同
```javascript
num = num / 5;
// is equal to
num /= 5;
```

# --instructions--

使用*除法赋值*运算符将 `result` 的值除以 `5` 来计算商，然后打印它的值

# --seed--

```javascript
var result = 50;
result [/][/];
console.log(result);
```

# --answers--

- / 
- /= 
- =/ 
- 5
- 25

# --solutions--

```javascript
var result = 50;
result /= 5;
console.log(result);
```

# --output--

10
