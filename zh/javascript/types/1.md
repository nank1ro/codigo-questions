---
language: javascript
exerciseType: 2
---

# --description--

JavaScript 中的每个值都有一个**类型**。共有七种**原始**类型：
- `number` 表示任何数字，比如 `42` 或 `3.14`
- `string` 表示文本，比如 `"Ana"`
- `boolean` 表示 `true` 和 `false`
- `undefined` 表示从未提供的值
- `null` 表示有意为空的值
- `bigint` 表示任意大小的整数，比如 `9007199254740993n`
- `symbol` 表示用 `Symbol()` 创建的唯一标识符

其他所有东西（数组、函数、用 `{}` 创建的对象、日期……）都是 `object`。
`typeof` 运算符以字符串的形式告诉你一个值的类型：
```javascript
console.log(typeof 42, typeof "Ana", typeof true);
// prints number string boolean
let city;
console.log(typeof city);
// prints undefined
```

# --instructions--

补全代码，使 `age` 存放数字 `42`，`name` 存放字符串 `"Ana"`，`active` 存放布尔值 `true`，并且 `city` 声明时不带值，使输出为 `number string boolean undefined`

# --seed--

```javascript
const age = [/];
const name = [/];
const active = [/];
let [/];
console.log(typeof age, typeof name, typeof active, typeof city);
```

# --answers--

- 42
- "Ana"
- true
- city
- null
- 42n
- city = null

# --solutions--

```javascript
const age = 42;
const name = "Ana";
const active = true;
let city;
console.log(typeof age, typeof name, typeof active, typeof city);
```

# --output--

number string boolean undefined
