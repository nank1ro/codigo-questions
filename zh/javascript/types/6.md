---
language: javascript
exerciseType: 2
---

# --description--

有两种方式可以询问"这是 `NaN` 吗？"，而它们回答的是不同的问题。
旧的全局函数 `isNaN(value)` 会先把 `value` **转换**为数字，然后再检查。因此对任何无法变成数字的值它都返回 `true`，即使它根本不是 `NaN`：
```javascript
console.log(isNaN("hello"));
// prints true, because Number("hello") is NaN
console.log(isNaN("42"));
// prints false, because Number("42") is 42
```
`Number.isNaN(value)` **不会**转换：只有当 `value` 确实是数字 `NaN` 时它才返回 `true`：
```javascript
console.log(Number.isNaN("hello"));
// prints false, a string is not NaN
console.log(Number.isNaN(Number("hello")));
// prints true
```
优先使用 `Number.isNaN`；如果你想知道一次转换是否失败，请先做转换。

# --instructions--

补全代码，使第一行使用全局的 `isNaN`，第二行直接对字符串本身使用 `Number.isNaN`，第三行在检查前先把 `value` 转换为数字，使输出为 `true`、`false`、`true`

# --seed--

```javascript
const value = "hello";
console.log([/](value));
console.log([/](value));
console.log(Number.isNaN([/]));
```

# --answers--

- isNaN
- Number.isNaN
- Number(value)
- String(value)
- value
- isNumber

# --solutions--

```javascript
const value = "hello";
console.log(isNaN(value));
console.log(Number.isNaN(value));
console.log(Number.isNaN(Number(value)));
```

# --output--

true
false
true
