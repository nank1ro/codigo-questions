---
language: javascript
exerciseType: 2
---

# --description--

你可以通过把类型当作函数来调用，从而**显式**地把一个值转换为另一种类型：
- `Number(value)` 转换为数字：`Number("42")` 是 `42`
- `String(value)` 转换为字符串：`String(42)` 是 `"42"`
- `Boolean(value)` 转换为布尔值：`Boolean("")` 是 `false`

结果是一个全新的值；原值不会被改变：
```javascript
const input = "7";
const count = Number(input) + 1;
console.log(count, typeof count);
// prints 8 number
console.log(String(count) + "!");
// prints 8!
```
显式转换能让你的意图一目了然：任何读到 `Number(input)` 的人都知道 `input` 原本是文本。

# --instructions--

补全代码，使 `count` 等于 `input` 转换为数字后加 `1`，`label` 等于 `count` 转换为字符串后重复两次，`flag` 等于空字符串转换为布尔值，使输出为 `43 4343 false`

# --seed--

```javascript
const input = "42";
const count = [/](input) + 1;
const label = [/](count).repeat(2);
const flag = [/]("");
console.log(count, label, flag);
```

# --answers--

- Number
- String
- Boolean
- Int
- Bool
- toString

# --solutions--

```javascript
const input = "42";
const count = Number(input) + 1;
const label = String(count).repeat(2);
const flag = Boolean("");
console.log(count, label, flag);
```

# --output--

43 4343 false
