---
language: javascript
exerciseType: 4
---

# --description--

JavaScript 只有一个 `number` 类型，同时用于整数和小数：每个数字都是 64 位浮点值（*双精度*）。所以 `5` 和 `5.0` 是同一个值，也不存在单独的整数类型：
```javascript
console.log(5 === 5.0, 10 / 2);
// prints true 5
```
要判断一个数字没有小数部分，请使用 `Number.isInteger`：
```javascript
console.log(Number.isInteger(5.0), Number.isInteger(3.5));
// prints true false
```
模板字面量会按照与 `String()` 相同的规则把插值转换为字符串，所以 `${5.0}` 会变成 `"5"`，而不是 `"5.0"`。

# --instructions--

将各行排序以定义 `half`，它把 `n` 除以二并返回一句说明结果是否为整数的话，然后打印 `10` 和 `7` 的结果。先定义函数

# --answers--

```javascript
function half(n) {
```

```javascript
  const result = n / 2;
```

```javascript
  if (Number.isInteger(result)) {
```

```javascript
    return `${result} is whole`;
```

```javascript
  }
```

```javascript
  return `${result} is not whole`;
```

```javascript
}
```

```javascript
console.log(half(10));
```

```javascript
console.log(half(7));
```

# --solutions--

```javascript
function half(n) {
  const result = n / 2;
  if (Number.isInteger(result)) {
    return `${result} is whole`;
  }
  return `${result} is not whole`;
}
console.log(half(10));
console.log(half(7));
```

# --output--

5 is whole
3.5 is not whole
