---
language: javascript
exerciseType: 1
---

# --description--

当 JavaScript 需要一个布尔值时，比如在 `if` 条件中或 `Boolean(value)` 中，它会对值进行转换。只有八个值会变成 `false`，它们被称为**假值**（falsy）：
`false`、`0`、`-0`、`0n`、`""`、`null`、`undefined` 和 `NaN`。
**其他所有值都是真值（truthy）**，包括一些看起来为空的值：
```javascript
console.log(Boolean(0), Boolean(""), Boolean(NaN));
// prints false false false
console.log(Boolean("0"), Boolean("false"), Boolean([]), Boolean({}));
// prints true true true true
```
`"0"` 是非空字符串，所以它是真值；空数组是对象，所以它也是真值。

# --instructions--

编写函数 `countTruthy(values)`，返回数组 `values` 中有多少个元素是真值

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function countTruthy(values) {

}
```

# --asserts--

`countTruthy([1, "a", true])` 必须返回 `3`。

```javascript
tryCatch(countTruthy([1, "a", true]) === 3);
```

`countTruthy([0, "", null, undefined, NaN, false])` 必须返回 `0`。

```javascript
tryCatch(countTruthy([0, "", null, undefined, NaN, false]) === 0);
```

`countTruthy(["0", "false", [], {}])` 必须返回 `4`。

```javascript
tryCatch(countTruthy(["0", "false", [], {}]) === 4);
```

`countTruthy([0n, -0, " "])` 必须返回 `1`，因为只有空格字符串是真值。

```javascript
tryCatch(countTruthy([0n, -0, " "]) === 1);
```

`countTruthy([])` 必须返回 `0`。

```javascript
tryCatch(countTruthy([]) === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function countTruthy(values) {
  let count = 0;
  for (const value of values) {
    if (value) {
      count++;
    }
  }
  return count;
}
```
