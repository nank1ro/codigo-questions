---
language: javascript
exerciseType: 2
---

# --description--

`.then`、`.catch` 和 `.finally` 是同一条链上的环节。拒绝会跳过每一个 `.then`，直到遇到一个 `.catch`；一旦 `.catch` 回调返回了一个值，链就重新兑现并继续正常运行。

**`.finally(callback)`** 在链定型时运行，无论它是兑现还是被拒绝。它的回调不接收参数，其返回值会被忽略，因此值会继续流向下一个 `.then`。它是进行清理工作的位置，比如隐藏加载指示器：
```javascript
Promise.reject(new Error("no network"))
  .then((value) => `ok: ${value}`)
  .catch((error) => `error: ${error.message}`)
  .finally(() => console.log("cleanup"))
  .then((message) => console.log(message));
// prints cleanup, then error: no network
```

# --instructions--

补全这条链，让失败被转换成文本 `error: ` 加上消息，无论发生什么都会打印 `cleanup`，并且文本最后才打印

# --seed--

```javascript
function load(ok) {
  return ok ? Promise.resolve("data") : Promise.reject(new Error("no network"));
}

load(false)
  .then((value) => `ok: ${value}`)
  .[/]((error) => `error: ${error.message}`)
  .[/](() => console.log("cleanup"))
  .then((message) => console.log(message));
```

# --answers--

- catch
- finally
- then
- reject
- always

# --solutions--

```javascript
function load(ok) {
  return ok ? Promise.resolve("data") : Promise.reject(new Error("no network"));
}

load(false)
  .then((value) => `ok: ${value}`)
  .catch((error) => `error: ${error.message}`)
  .finally(() => console.log("cleanup"))
  .then((message) => console.log(message));
```

# --output--

cleanup
error: no network
