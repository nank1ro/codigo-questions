---
language: javascript
exerciseType: 1
---

# --description--

在函数前面加上 `async` 会改变它交回的东西：函数体仍然计算出一个普通的值，但调用者收到的是包裹着它的一个 promise。
```javascript
function shout(text) {
  return text.toUpperCase();
}
async function shoutLater(text) {
  return text.toUpperCase();
}

console.log(shout("hi"));
// prints HI
console.log(shoutLater("hi"));
// prints Promise { 'HI' }
```
这两个函数包含相同的代码；不同的只是读取结果的方式。`shoutLater("hi")` 必须在另一个 `async` 函数中被 await，才能得到 `"HI"`。

在没有等待要做时，把函数标记为 `async` 没有任何代价，而且正是它让你以后能在函数内部使用 `await`。

# --instructions--

编写一个 `async` 函数 `greet`，接收一个字符串 `name`，返回 `Hello, ` 加上这个名字和一个感叹号，例如 `Hello, Ada!`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
async function greet(name) {
  
}
```

# --asserts--

`greet` 必须用 `async` 关键字声明。

```json
{
  "regex": "async\\s+function\\s+greet",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`await greet("Ada")` 必须是 `"Hello, Ada!"`。

```javascript
(async () => {
  tryCatch((await greet("Ada")) === "Hello, Ada!");
})().catch(() => tryCatch(false));
```

`await greet("Bob")` 必须是 `"Hello, Bob!"`。

```javascript
(async () => {
  tryCatch((await greet("Bob")) === "Hello, Bob!");
})().catch(() => tryCatch(false));
```

`await greet("")` 必须是 `"Hello, !"`。

```javascript
(async () => {
  tryCatch((await greet("")) === "Hello, !");
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
async function greet(name) {
  return `Hello, ${name}!`;
}
```
