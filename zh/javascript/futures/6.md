---
language: javascript
exerciseType: 4
---

# --description--

`async` 函数和其他函数一样自上而下阅读：`await` 只是把它暂停，直到被 await 的 promise 兑现，然后执行继续到下一行。
```javascript
async function main() {
  console.log("start");
  const value = await Promise.resolve("data");
  console.log(value);
  console.log("done");
}

main();
// prints start, then data, then done
```
注意最后一行：`async` 函数仍然必须被**调用**。写 `main` 而不带括号只是定义了工作但从不启动它，什么都不会打印。

# --instructions--

将这些条目排序成一个程序：定义一个返回 `data` 的 `async` 函数 `load`，然后定义一个 `async` 函数 `main`，它打印 `start`，把 `load()` await 到 `value` 中，打印 `value`，最后打印 `done`。先定义 `load`，再定义 `main`，并在最后一行调用 `main()`

# --answers--

```javascript
async function load() {
  return "data";
}
```

```javascript
async function main() {
```

```javascript
  console.log("start");
```

```javascript
  const value = await load();
```

```javascript
  console.log(value);
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
async function load() {
  return "data";
}
async function main() {
  console.log("start");
  const value = await load();
  console.log(value);
  console.log("done");
}
main();
```

# --output--

start
data
done
