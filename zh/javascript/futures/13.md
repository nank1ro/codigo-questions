---
language: javascript
exerciseType: 4
---

# --description--

**顺序**等待与**并行**等待的区别取决于你把 `await` 放在*哪里*：
```javascript
// sequential: about 300 + 300 = 600 ms
const a = await load("a");
const b = await load("b");

// parallel: about 300 ms
const [a, b] = await Promise.all([load("a"), load("b")]);
```
在第一种版本中，第二次下载只有等第一次完成后才会开始，因为 `await` 在那一行暂停了函数。在第二种版本中，两个调用都在任何 await 之前发出，所以当 `Promise.all` 等待时，两个下载都已经在运行。

只有当第二个任务确实需要第一个任务的结果时，才使用顺序的 await。否则先启动所有任务，再一起 await。

# --instructions--

将这些条目排序成一个程序：定义 `later`，然后定义一个 `async` 函数 `main`，它打印 `loading`，用 `Promise.all` 同时启动两个 `later` 调用，打印用空格连接的两个结果，最后打印 `done`。先定义 `later`，再定义 `main`，并在最后一行调用 `main()`

# --answers--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
```

```javascript
async function main() {
```

```javascript
  console.log("loading");
```

```javascript
  const results = await Promise.all([later("one", 30), later("two", 10)]);
```

```javascript
  console.log(results.join(" "));
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
async function main() {
  console.log("loading");
  const results = await Promise.all([later("one", 30), later("two", 10)]);
  console.log(results.join(" "));
  console.log("done");
}
main();
```

# --output--

loading
ready two
ready one
one two
done
