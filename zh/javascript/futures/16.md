---
language: javascript
exerciseType: 1
---

# --description--

把最后几块拼在一起，就得到一个几乎每个真实应用都会用到的小工具：截止时间。构建一个在 `ms` 毫秒后被拒绝的 promise，让它与真正的工作竞争，先定型的那个决定结果：
```javascript
const timer = new Promise((resolve, reject) => {
  setTimeout(() => reject(new Error("timeout")), ms);
});
return Promise.race([work, timer]);
```
从 `async` 函数返回一个 promise 是没有问题的：函数交回的 promise 会跟随它，所以调用者 await 到的是最终的值，而不是 promise 的 promise。

# --instructions--

编写一个 `async` 函数 `withTimeout`，接收一个 promise 和一个毫秒数 `ms`，如果该 promise 在 `ms` 内定型，就返回它的结果，否则以 `new Error("timeout")` 被拒绝。使用 `Promise.race`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
async function withTimeout(promise, ms) {
  
}
```

# --asserts--

一个已经兑现的 promise 必须给出它的值。

```javascript
(async () => {
  tryCatch((await withTimeout(Promise.resolve("ok"), 50)) === "ok");
})().catch(() => tryCatch(false));
```

一个在截止时间之前被拒绝的 promise 必须保留它自己的错误。

```javascript
(async () => {
  const failing = Promise.reject(new Error("broken"));
  let message = "";
  await withTimeout(failing, 60).catch((error) => { message = error.message; });
  tryCatch(message === "broken");
})().catch(() => tryCatch(false));
```

一个足够快的 promise 必须给出它的值，而不是一个 promise。

```javascript
(async () => {
  const quick = new Promise((resolve) => setTimeout(() => resolve(7), 10));
  tryCatch((await withTimeout(quick, 120)) === 7);
})().catch(() => tryCatch(false));
```

一个太慢的 promise 必须被拒绝，消息为 `"timeout"`。

```javascript
(async () => {
  const slow = new Promise((resolve) => setTimeout(() => resolve("late"), 150));
  let message = "";
  await withTimeout(slow, 20).catch((error) => { message = error.message; });
  tryCatch(message === "timeout");
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
async function withTimeout(promise, ms) {
  const timer = new Promise((resolve, reject) => {
    setTimeout(() => reject(new Error("timeout")), ms);
  });
  return Promise.race([promise, timer]);
}
```
