---
language: javascript
exerciseType: 2
---

# --description--

`await` 不是读取 promise 的唯一方式。每个 promise 都有一个 **`.then(callback)`** 方法：一旦 promise 兑现，回调就会收到这个值。
```javascript
Promise.resolve(21).then((n) => {
  console.log(n);
  // prints 21
});
```
**`Promise.resolve(value)`** 构建一个已经以 `value` 兑现的 promise，当你手头就有这个值但必须返回一个 promise 时，它非常方便。

.then 返回一个以回调返回的任何内容兑现的**新** promise，因此调用可以**链式**进行，每一步都处理上一步的结果：
```javascript
Promise.resolve(21)
  .then((n) => n * 2)
  .then((n) => console.log(n));
// prints 42
```

# --instructions--

补全代码，让 `double` 返回一个已经以 `n * 2` 兑现的 promise，并用回调读取它的结果，使程序打印 `42`

# --seed--

```javascript
function double(n) {
  return Promise.[/](n * 2);
}

double(21).[/]((result) => {
  console.log(result);
});
```

# --answers--

- resolve
- then
- all
- catch
- await

# --solutions--

```javascript
function double(n) {
  return Promise.resolve(n * 2);
}

double(21).then((result) => {
  console.log(result);
});
```

# --output--

42
