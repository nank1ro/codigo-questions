---
language: javascript
exerciseType: 2
---

# --description--

你已经知道 `extends` 会让一个类成为另一个类的子类。这个关键字真正带给你的是**继承**：子类免费获得父类的每一个属性和方法，并且还可以在此基础上添加自己的内容。

让继承真正有用的是 **`super`**。在子类的构造函数中，`super(...)` 会调用父类的构造函数，这样父类就能设置它所拥有的那部分对象：
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
```
这里 `super(name)` 把 `name` 交给 `Animal`，由它来存储，而 `Dog` 只需要关心 `breed`。

# --instructions--

补全代码，使 `Dog` 继承自 `Animal`，并把 `name` 传递给父类构造函数

# --seed--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog [/] Animal {
    constructor(name, breed) {
        [/](name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --answers--

- extends
- inherits
- super
- parent
- new Animal

# --solutions--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --output--

Max is a Beagle
