---
language: javascript
exerciseType: 1
---

# --description--

**`instanceof`** 运算符询问一个对象是否由某个类构建，或者由继承自它的任何类构建：
```javascript
class Animal {}
class Dog extends Animal {}
const max = new Dog();
console.log(max instanceof Dog);    // true
console.log(max instanceof Animal); // true, Dog extends Animal
```
JavaScript 没有 `abstract` 关键字，但同样的思想可以手写出来：基类定义形状，而子类*必须*提供的每个方法直接抛出错误：
```javascript
class Shape {
    area() {
        throw new Error("area() must be implemented");
    }
}
```
忘记重写 `area` 的子类会在第一次被使用时就大声地失败，而不是悄悄地返回 `undefined`。

# --instructions--

编写一个类 `Shape`，其方法 `area` 抛出一个 `Error`；再编写一个继承它的类 `Square`，接收一个 `side` 并重写 `area` 以返回正方形的面积

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Shape {
  
}

class Square {
  
}
```

# --asserts--

`new Square(3).area()` 必须返回 `9`。

```javascript
tryCatch(new Square(3).area() === 9);
```

`new Square(5).area()` 必须返回 `25`。

```javascript
tryCatch(new Square(5).area() === 25);
```

正方形必须是一个 `Shape`。

```javascript
tryCatch(new Square(3) instanceof Shape === true);
```

在基类上调用 `area` 必须抛出错误。

```javascript
let _threw = false;
try {
  new Shape().area();
} catch (e) {
  _threw = true;
}
tryCatch(_threw === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Shape {
  area() {
    throw new Error("area() must be implemented");
  }
}

class Square extends Shape {
  constructor(side) {
    super();
    this.side = side;
  }
  area() {
    return this.side * this.side;
  }
}
```
