---
language: javascript
exerciseType: 4
---

# --description--

静态方法一个非常常见的用途是**工厂（factory）**：一种从其他形状的数据构建实例并返回它的方法。它把 `new` 集中在一个地方，并给这个构建过程起了一个能说明其用途的名字：
```javascript
class Duration {
    constructor(seconds) {
        this.seconds = seconds;
    }
    static fromMinutes(minutes) {
        return new Duration(minutes * 60);
    }
}
console.log(Duration.fromMinutes(2).seconds);
// 打印 120
```
工厂可以在任何实例存在之前被调用，这是普通方法做不到的。

# --instructions--

将这些代码块排列成一个能正常运行的程序：从数组构建一个点并打印它的坐标

# --answers--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
```

```javascript
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
```

```javascript
const p = Point.fromArray([3, 4]);
```

```javascript
console.log(`${p.x},${p.y}`);
```

# --solutions--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
const p = Point.fromArray([3, 4]);
console.log(`${p.x},${p.y}`);
```

# --output--

3,4
