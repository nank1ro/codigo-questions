---
language: javascript
exerciseType: 2
---

# --description--

当一边是字符串、另一边是数字时，`<=` 会先把字符串转换成数字，所以 `"7" <= 8` 的结果是 `true`。

# --instructions--

检查 `num` 是否**小于或等于** `8`，打印布尔值结果

# --seed--

```javascript
var num = 7;
console.log([/][/][/]);
```

# --answers--

- num
- 'num'
-  <= 
-  = 
-  >= 
- 8

# --solutions--

```javascript
var num = 7;
console.log(num <= 8);
```

# --output--

true
