---
language: javascript
exerciseType: 2
---

# --description--

任何涉及 `NaN` 的比较都会返回 `false`，即使是相反的比较也是如此：`NaN < 3` 和 `NaN >= 3` 都为 `false`，所以 `<` 不成立并不总是意味着左边更大。

# --instructions--

检查 `num` 是否**小于** `3`，打印布尔值结果

# --seed--

```javascript
var num = 5;
console.log([/][/][/]);
```

# --answers--

- num 
- 'num' 
- == 
- < 
- num 
- <= 
- >= 
- 3

# --solutions--

```javascript
var num = 5;
console.log(num < 3);
```

# --output--

false
