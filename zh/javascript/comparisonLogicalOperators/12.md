---
language: javascript
exerciseType: 2
---

# --description--

当两边都是字符串时，`>` 会按字符编码顺序逐字符比较，而不是比较长度，所以 `"b" > "a"` 为 `true`，`"apple" > "ant"` 也同样为 `true`。

# --instructions--

检查 `num` 是否**大于** `7`，打印布尔值结果

# --seed--

```javascript
var num = 8;
console.log([/][/][/]);
```

# --answers--

- num 
- 'num' 
- == 
- > 
- num 
- >= 
- 7

# --solutions--

```javascript
var num = 8;
console.log(num > 7);
```

# --output--

true
