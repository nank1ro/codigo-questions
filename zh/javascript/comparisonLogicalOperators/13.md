---
language: javascript
exerciseType: 2
---

# --description--

`>=` 只要满足名字中的任意一半条件即可：`8 >= 8` 为 `true`，因为两个值相等，而更严格的 `8 > 8` 则为 `false`。

# --instructions--

检查 `num` 是否**大于或等于** `8`，打印布尔值结果

# --seed--

```javascript
var num = 8;
console.log([/][/][/]);
```

# --answers--

- num 
- 'num'
- == 
- = 
- >= 
- 8

# --solutions--

```javascript
var num = 8;
console.log(num >= 8);
```

# --output--

true
