---
language: javascript
exerciseType: 4
---

# --instructions--

排序代码行，打印首字母大写的 `name`

# --answers--

```javascript
let name = "alice";
```

```javascript
let first = name.charAt(0).toUpperCase();
```

```javascript
let capitalized = first + name.slice(1);
```

```javascript
console.log(capitalized);
```

# --solutions--

```javascript
let name = "alice";
let first = name.charAt(0).toUpperCase();
let capitalized = first + name.slice(1);
console.log(capitalized);
```

# --output--

Alice
