---
language: javascript
exerciseType: 4
---

# --instructions--

排序代码行，打印一行收据：`item` 用点填充到 8 个字符，后面跟着右对齐、宽度为 6 个字符的价格

# --answers--

```javascript
let item = "Tea";
```

```javascript
let row = item.padEnd(8, ".");
```

```javascript
row = row + "2.50".padStart(6);
```

```javascript
console.log(row);
```

# --solutions--

```javascript
let item = "Tea";
let row = item.padEnd(8, ".");
row = row + "2.50".padStart(6);
console.log(row);
```

# --output--

Tea.....  2.50
