---
language: javascript
exerciseType: 1
---

# --description--

要检查一个字符串是否包含另一个字符串，可以使用以下方法，它们都返回一个布尔值：
- 如果 `text` 出现在任意位置，`includes(text)` 为 `true`
- 如果字符串以 `text` 开头，`startsWith(text)` 为 `true`
- 如果字符串以 `text` 结尾，`endsWith(text)` 为 `true`

```javascript
let file = "photo.png";
console.log(file.includes("."));
// prints true
console.log(file.startsWith("ph"));
// prints true
console.log(file.endsWith(".jpg"));
// prints false
```
比较是区分大小写的：`"Hello".includes("h")` 为 `false`。

# --instructions--

编写一个函数 `isImage(fileName)`，当 `fileName` 以 `".png"` 结尾时返回 `true`，否则返回 `false`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isImage(fileName) {

}
```

# --asserts--

`isImage("cat.png")` 必须返回 `true`。

```javascript
tryCatch(isImage("cat.png") === true);
```

`isImage("cat.jpg")` 必须返回 `false`。

```javascript
tryCatch(isImage("cat.jpg") === false);
```

`isImage("png.txt")` 必须返回 `false`。

```javascript
tryCatch(isImage("png.txt") === false);
```

`isImage("a.png.txt")` 必须返回 `false`。

```javascript
tryCatch(isImage("a.png.txt") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isImage(fileName) {
  return fileName.endsWith(".png");
}
```
