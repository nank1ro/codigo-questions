---
language: javascript
exerciseType: 2
---

# --description--

`if` 是 JavaScript 中用来引出条件语句的关键字。这里没有 `elif` 关键字——第二个条件要用 `else if` 引出，写成两个独立的单词。

# --instructions--

填空以创建一个条件语句

# --seed--

```javascript
[/] (true) {
    console.log("It's True!");
}
```

# --answers--

- if
- else
- elif
- else if

# --solutions--

```javascript
if (true) {
    console.log("It's True!");
}
```

# --output--

It's True!
