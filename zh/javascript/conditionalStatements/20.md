---
language: javascript
exerciseType: 2
---

# --description--

条件不一定要是布尔值：JavaScript 会把它遇到的值转换为布尔值，所以 `if (1)` 会执行代码块，而 `if (0)` 不会。字面量 `true` 则完全不需要转换。

# --instructions--

填空以执行代码块

# --seed--

```javascript
[/]([/])[/]
    console.log("I'm ready!");
}
```

# --answers--

- true
- false
- "true"
- "false"
- if 
- else 
- else if 
- }
-  {

# --solutions--

```javascript
if (true) {
    console.log("I'm ready!");
}
```

# --output--

I'm ready!
