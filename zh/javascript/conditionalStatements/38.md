---
language: javascript
exerciseType: 3
---

# --description--

像 `if`、`else if` 和 `else` 这样，根据条件是 `true` 还是 `false` 来决定执行还是跳过代码的语句，被称为**条件语句**。

# --instructions--

根据条件为 `true` 或 `false` 来运行或跳过代码的语句叫什么？

# --answers--

- 条件语句
- 判断语句
- 比较

# --solutions--

- 条件语句

