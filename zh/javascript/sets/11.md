---
language: javascript
exerciseType: 2
---

# --instructions--

构建数组 `common`，包含同时存在于 `a` 和 `b` 中的数字，然后打印它

# --seed--

```javascript
let a = new Set([1, 2, 3]);
let b = new Set([2, 3, 4]);
let common = [...a].[/]((x) => b.[/](x));
console.log(common);
```

# --answers--

- filter
- has
- map
- find
- includes
- contains

# --solutions--

```javascript
let a = new Set([1, 2, 3]);
let b = new Set([2, 3, 4]);
let common = [...a].filter((x) => b.has(x));
console.log(common);
```

# --output--

[ 2, 3 ]
