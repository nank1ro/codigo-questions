---
language: javascript
exerciseType: 4
---

# --instructions--

排列这些代码行，把 `"pear"` 添加到 set 中，把每个水果打印在单独一行，然后打印这个 set 包含多少个水果

# --answers--

```javascript
const fruits = new Set(["apple", "kiwi"]);
```

```javascript
fruits.add("pear");
```

```javascript
fruits.forEach((fruit) => {
```

```javascript
  console.log(fruit);
```

```javascript
});
```

```javascript
console.log(fruits.size);
```

# --solutions--

```javascript
const fruits = new Set(["apple", "kiwi"]);
fruits.add("pear");
fruits.forEach((fruit) => {
  console.log(fruit);
});
console.log(fruits.size);
```

# --output--

apple
kiwi
pear
3
