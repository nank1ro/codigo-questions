---
language: javascript
exerciseType: 2
---

# --description--

JavaScript 文件可以以一行特殊的内容开头，叫做 **shebang**（或 hashbang）：`#!` 后面跟着应当运行该文件的程序路径。在类 Unix 系统上，它让你可以直接从终端运行脚本，比如 `./hello.js`，而不必先输入 `node`：
```javascript
#!/usr/bin/env node
console.log("Hello from Node");
```
JavaScript 会像对待注释一样忽略这一行，但仅限它是文件的**第一行**：写在其他任何位置，`#!` 都是语法错误。`/usr/bin/env node` 的意思是"在这个系统上找到 `node` 并使用它"。

# --instructions--

补全代码，使第一行是用 `node` 运行该文件的 shebang，然后程序输出 `Hello from Node`

# --seed--

```javascript
[/]/usr/bin/env node
console.log("Hello from Node");
```

# --answers--

- #!
- !#
- #
- --

# --solutions--

```javascript
#!/usr/bin/env node
console.log("Hello from Node");
```

# --output--

Hello from Node
