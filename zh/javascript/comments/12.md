---
language: javascript
exerciseType: 4
---

# --description--

JSDoc 注释内部的顺序始终一致：先是概述，然后按参数声明顺序每个参数一行 `@param`，最后是 `@returns`。开头的 `/**` 和结尾的 ` */` 把这一切包起来，而注释就位于它所描述的函数正上方：
```javascript
/**
 * Returns the number of seconds in the given minutes.
 * @param {number} minutes a whole number of minutes
 * @returns {number} minutes multiplied by sixty
 */
function toSeconds(minutes) {
  return minutes * 60;
}
```

# --instructions--

排列这些条目，用一行概述、一行 `@param` 和一行 `@returns` 按此顺序为 `twice` 编写文档，并直接放在函数上方，然后在定义函数之后输出 `twice(21)`

# --answers--

```javascript
/**
```

```javascript
 * Returns the double of a number.
```

```javascript
 * @param {number} n the number to double
```

```javascript
 * @returns {number} n multiplied by two
```

```javascript
 */
```

```javascript
function twice(n) {
  return n * 2;
}
```

```javascript
console.log(twice(21));
```

# --solutions--

```javascript
/**
 * Returns the double of a number.
 * @param {number} n the number to double
 * @returns {number} n multiplied by two
 */
function twice(n) {
  return n * 2;
}
console.log(twice(21));
```

# --output--

42
