---
language: javascript
exerciseType: 3
---

# --description--

好的注释说明代码**为什么**这样做，而不是它**做了什么**。代码本身已经展示了发生的事情；用文字重复一遍只会增加噪音，而且代码一改动它就过时了：
```javascript
// set timeout to 30
const timeout = 30;
```
这个数字背后的原因才是读者猜不到的：
```javascript
// the server drops idle connections after 35 seconds, so stop earlier
const timeout = 30;
```
如果一条注释只是复述它下面那一行，就删掉它，或者换成写明原因。

# --instructions--

在这一行上方，哪条注释最有用？
```javascript
const maxRetries = 3;
```

# --answers--

- `// the payment API blocks the client after the fourth failed attempt`
- `// set maxRetries to 3`
- `// maxRetries is a constant holding a number`
- `// this line declares a constant`

# --solutions--

- `// the payment API blocks the client after the fourth failed attempt`
