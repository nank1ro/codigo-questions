---
language: javascript
exerciseType: 1
---

# --description--

再有两个构件，你就能描述几乎任何形状的文本。

**字符类**是写在方括号之间的一组字符；它恰好匹配其中的一个。连字符表示一个范围，开头的 `^` 表示对集合取反：
```javascript
/[aeiou]/   // one vowel
/[a-z]/     // one lowercase letter
/[A-Z0-9]/  // one uppercase letter or one digit
/[^0-9]/    // one character that is not a digit
```
**量词**表示前一个部分重复多少次：`+` 一个或多个，`*` 零个或多个，`?` 零个或一个，`{n}` 恰好 `n` 次。

最后，**锚点**把模式固定到文本的两端：`^` 表示"从这里开始"，`$` 表示"到这里结束"。没有它们时，模式可以匹配字符串内部的任意位置，所以 `/\d{2}/.test("abc12def")` 是 `true`。两个锚点都用上时，整个字符串必须匹配：
```javascript
console.log(/^\d{2}$/.test("abc12def"));
// prints false
console.log(/^\d{2}$/.test("12"));
// prints true
```

# --instructions--

编写一个函数 `isCode`，接收字符串 `text`，仅当 `text` 恰好是三个大写字母后跟两个数字时返回 `true`，例如 `ABC12`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isCode(text) {
  
}
```

# --asserts--

`isCode("ABC12")` 必须返回 `true`。

```javascript
tryCatch(isCode("ABC12") === true);
```

`isCode("abc12")` 必须返回 `false`。

```javascript
tryCatch(isCode("abc12") === false);
```

`isCode("ABC123")` 必须返回 `false`。

```javascript
tryCatch(isCode("ABC123") === false);
```

`isCode("xABC12")` 必须返回 `false`。

```javascript
tryCatch(isCode("xABC12") === false);
```

`isCode("AB12")` 必须返回 `false`。

```javascript
tryCatch(isCode("AB12") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isCode(text) {
  return /^[A-Z]{3}\d{2}$/.test(text);
}
```
