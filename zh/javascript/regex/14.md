---
language: javascript
exerciseType: 4
---

# --description--

替换的内容不必是字符串。当你传入一个**函数**时，每个匹配会调用它一次，它返回什么就把什么插入到该匹配的位置。

函数先接收整个匹配，然后接收各个捕获组：
```javascript
console.log("hello world".replace(/\w+/g, (word) => word.length));
// 打印 5 5

console.log("ann lee".replace(/(\w)(\w*)/g, (whole, first, rest) => first.toUpperCase() + rest));
// 打印 Ann Lee
```
这是根据匹配到的文本计算替换内容的唯一方式，仅靠 `$1` 做不到这一点。

# --instructions--

排列这些条目，组成一个程序：打印文本，把其中每个数字替换成它的两倍。先声明文本，再声明函数

# --answers--

```javascript
const text = "a1 b2 c3";
```

```javascript
function double(digit) {
```

```javascript
  return Number(digit) * 2;
```

```javascript
}
```

```javascript
console.log(text.replace(/\d/g, double));
```

# --solutions--

```javascript
const text = "a1 b2 c3";
function double(digit) {
  return Number(digit) * 2;
}
console.log(text.replace(/\d/g, double));
```

# --output--

a2 b4 c6
