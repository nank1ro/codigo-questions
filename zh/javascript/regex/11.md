---
language: javascript
exerciseType: 1
---

# --description--

`g` 标志会给你每一个匹配到的字符串，但它把组丢掉了。当你需要*每一个*匹配的组时，使用 **`matchAll`**。它返回一个迭代器，其中是完整的匹配对象，每个都和普通 `match` 的结果完全一样：
```javascript
const text = "a=1;b=2";
for (const match of text.matchAll(/(?<key>\w+)=(?<value>\w+)/g)) {
  console.log(`${match.groups.key} -> ${match.groups.value}`);
}
// 打印 a -> 1
// 打印 b -> 2
```
`matchAll` 要求带有 `g` 标志；没有它会抛出 `TypeError`。由于它返回的是迭代器，当你想要真正的数组时，可以用 `[...text.matchAll(pattern)]` 展开它；还要注意，当模式从不匹配时，它不会产出任何东西。

# --instructions--

编写一个函数 `parsePairs`，接收字符串 `text`，找出其中每一对 `key=value`（键和值都是一段连续的单词字符），并返回一个把每个键映射到其值的对象。例如 `parsePairs("host=local;port=80")` 返回 `{ host: "local", port: "80" }`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function parsePairs(text) {
  
}
```

# --asserts--

`parsePairs("host=local;port=80")` 必须返回一个包含 `host` 和 `port` 的对象。

```javascript
tryCatch(JSON.stringify(parsePairs("host=local;port=80")) === '{"host":"local","port":"80"}');
```

`parsePairs("a=1")` 必须返回一个只含 `a` 的对象。

```javascript
tryCatch(JSON.stringify(parsePairs("a=1")) === '{"a":"1"}');
```

`parsePairs("nothing here")` 必须返回一个空对象。

```javascript
tryCatch(JSON.stringify(parsePairs("nothing here")) === '{}');
```

`parsePairs("x=1 y=2 z=3")` 必须返回全部三对。

```javascript
tryCatch(JSON.stringify(parsePairs("x=1 y=2 z=3")) === '{"x":"1","y":"2","z":"3"}');
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function parsePairs(text) {
  const pairs = {};
  for (const match of text.matchAll(/(?<key>\w+)=(?<value>\w+)/g)) {
    pairs[match.groups.key] = match.groups.value;
  }
  return pairs;
}
```
