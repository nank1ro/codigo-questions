---
language: javascript
exerciseType: 4
---

# --description--

比较函数可以查看元素的任何部分，因此只要比较某个属性，就能按该属性排序一个对象数组：
```javascript
const items = [{ name: "b", size: 3 }, { name: "a", size: 1 }];
const bySize = [...items].sort((x, y) => x.size - y.size);
console.log(bySize.map((item) => item.name));
// prints [ 'a', 'b' ]
```
排序副本使 `items` 保持其原始顺序。

# --instructions--

排列这些行：把 `players` 的副本按 `score` 升序排序，用 `map` 按该顺序打印他们的名字，然后打印 `players` 第一个元素的名字以说明它没有被改变。在 `names` 之前声明 `ranked`

# --answers--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
```

```javascript
const ranked = [...players].sort((a, b) => a.score - b.score);
```

```javascript
const names = ranked.map((p) => p.name);
```

```javascript
console.log(names);
```

```javascript
console.log(players[0].name);
```

# --solutions--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
const ranked = [...players].sort((a, b) => a.score - b.score);
const names = ranked.map((p) => p.name);
console.log(names);
console.log(players[0].name);
```

# --output--

[ 'Leo', 'Zoe', 'Mia' ]
Zoe
