---
language: javascript
exerciseType: 4
---

# --description--

真实的程序会把这些方法组合成**管道**：筛选出你关心的元素，把它们映射成你需要的值，再把它们归约成一个结果。把中间数组存入常量能让每一步保持清晰，也便于复用：
```javascript
const adults = people.filter((p) => p.age >= 18);
const names = adults.map((p) => p.name);
const totalAge = adults.reduce((sum, p) => sum + p.age, 0);
```
`names.join(", ")` 把字符串数组变成一个字符串，元素之间用逗号加空格分隔。

# --instructions--

排列这些行：只保留购物车中 `qty` 为正的商品，收集它们的名字，为它们累加 `price * qty`，并在同一行打印用 `", "` 连接的名字和总额。按 pen、book、bag 的顺序列出商品，并按 `inStock`、`names`、`total` 的顺序声明它们

# --answers--

```javascript
const cart = [
```

```javascript
  { name: "pen", price: 2, qty: 3 },
```

```javascript
  { name: "book", price: 12, qty: 1 },
```

```javascript
  { name: "bag", price: 30, qty: 0 },
```

```javascript
];
```

```javascript
const inStock = cart.filter((item) => item.qty > 0);
```

```javascript
const names = inStock.map((item) => item.name);
```

```javascript
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
```

```javascript
console.log(names.join(", "), total);
```

# --solutions--

```javascript
const cart = [
  { name: "pen", price: 2, qty: 3 },
  { name: "book", price: 12, qty: 1 },
  { name: "bag", price: 30, qty: 0 },
];
const inStock = cart.filter((item) => item.qty > 0);
const names = inStock.map((item) => item.name);
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
console.log(names.join(", "), total);
```

# --output--

pen, book 18
