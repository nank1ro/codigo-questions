---
language: javascript
exerciseType: 1
---

# --description--

每个函数都有一个 `bind` 方法，它返回一个预先固定了某些内容的**新**函数。它的第一个参数会成为新函数的 `this`；其余参数会被放在调用新函数时所传参数的前面（一种**部分应用**）：
```javascript
function multiply(a, b) {
  return a * b;
}
const double = multiply.bind(null, 2);
console.log(double(21));
// prints 42
```
固定 `this` 对方法很重要。当一个方法被从它的对象中取出并单独调用时，`this` 不再指向该对象，于是 `this.name` 变成 `undefined`。`bind` 会把它锁定到该对象上：
```javascript
const user = {
  name: "Ana",
  hello() { return `Hi ${this.name}`; },
};
const loose = user.hello;
console.log(loose());
// prints Hi undefined
const bound = user.hello.bind(user);
console.log(bound());
// prints Hi Ana
```
原函数永远不会被改变：`bind` 总是构建一个新函数，它的 `name` 是原名称加上前缀 `bound `。

# --instructions--

函数 `power` 和对象 `counter` 已经给出。把 `power` 绑定 `this` 为 `null` 且 `exponent` 固定为 `2` 的版本存入常量 `square`，把 `counter.increment` 绑定到 `counter` 的版本存入常量 `tick`，使它在被单独调用时仍然有效

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = 
const tick = 
```

# --asserts--

`square(3)` 必须返回 `9`。

```javascript
tryCatch(square(3) === 9);
```

`square(12)` 必须返回 `144`。

```javascript
tryCatch(square(12) === 144);
```

`square` 必须用 `bind` 创建，因此它的名字是 `"bound power"`。

```javascript
tryCatch(square.name === "bound power");
```

`tick` 必须是用 `bind` 创建的新函数，而不是 `counter.increment` 本身。

```javascript
tryCatch(tick !== counter.increment && tick.name === "bound increment");
```

单独调用 `tick()` 两次必须依次返回 `1` 和 `2`，并且 `counter.count` 必须是 `2`。

```javascript
tryCatch(tick() === 1 && tick() === 2 && counter.count === 2);
```

`tick` 存入另一个变量并从那里调用时也必须仍然有效。

```javascript
tryCatch((() => { const alone = tick; return alone() === 3; })());
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = power.bind(null, 2);
const tick = counter.increment.bind(counter);
```
