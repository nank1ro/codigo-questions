---
language: javascript
exerciseType: 2
---

# --description--

因为函数是值，你可以写一个高阶函数把两个函数**组合**成一个新函数。`compose(f, g)` 返回一个函数，它先应用 `g`，再把 `f` 应用到结果上，对应数学记号 *f(g(x))*：
```javascript
const compose = (f, g) => (x) => f(g(x));
const trim = (s) => s.trim();
const shout = (s) => s.toUpperCase();
const clean = compose(shout, trim);
console.log(clean("  hi  "));
// prints HI
```
顺序很重要：`compose(f, g)` 先运行 `g`，再运行 `f`。用这种方式把小函数粘合起来构建程序，称为**函数组合**。

# --instructions--

补全 `compose`，让它返回一个先应用 `g` 再把 `f` 应用到结果上的函数，然后打印 `compose(double, inc)(5)` 和 `compose(inc, double)(5)`，产生输出 `12 11`

# --seed--

```javascript
const compose = (f, g) => (x) => [/]([/](x));
const inc = (n) => n + 1;
const double = (n) => n * 2;
console.log(compose(double, inc)(5), compose(inc, double)(5));
```

# --answers--

- f
- g
- x
- compose

# --solutions--

```javascript
const compose = (f, g) => (x) => f(g(x));
const inc = (n) => n + 1;
const double = (n) => n * 2;
console.log(compose(double, inc)(5), compose(inc, double)(5));
```

# --output--

12 11
