---
language: javascript
exerciseType: 2
---

# --description--

`reduce` 的累加器不一定是数字：它可以是字符串、数组或对象。从空对象 `{}` 开始，你可以一次遍历就完成计数或分组。记得从回调中**返回累加器**，否则下一步会收到 `undefined`：
```javascript
const votes = ["yes", "no", "yes"];
const tally = votes.reduce((acc, vote) => {
  acc[vote] = (acc[vote] ?? 0) + 1;
  return acc;
}, {});
console.log(tally);
// prints { yes: 2, no: 1 }
```
`acc[vote] ?? 0` 读取当前计数，当该键还不存在时读取到 `0`。

# --instructions--

补全代码，使 `counts` 把每个首字母映射到以它开头的单词数量：使用 `reduce`，以空对象作为初始累加器，并用 `?? 0` 处理缺失的键，产生输出 `{ a: 2, b: 2, c: 1 }`

# --seed--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.[/]((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] [/] 0) + 1;
  return [/];
}, [/]);
console.log(counts);
```

# --answers--

- reduce
- ??
- acc
- {}
- map
- filter
- &&
- word
- 0

# --solutions--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.reduce((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] ?? 0) + 1;
  return acc;
}, {});
console.log(counts);
```

# --output--

{ a: 2, b: 2, c: 1 }
