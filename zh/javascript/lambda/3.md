---
language: javascript
exerciseType: 3
---

# --description--

隐式返回有一个陷阱。当箭头函数的函数体以 `{` 开头时，它会被解读为**块级函数体**，而绝不会是对象字面量：
```javascript
const make = (name) => { name: name };
console.log(make("Ana"));
// prints undefined
```
这里 `{ name: name }` 是一个块，其中包含标签 `name:`，后面跟着表达式 `name`。没有返回任何东西，所以调用结果是 `undefined`。
要在同一行返回对象字面量，请用**圆括号**把它包裹起来，让 JavaScript 将其视为表达式：
```javascript
const make = (name) => ({ name: name });
console.log(make("Ana"));
// prints { name: 'Ana' }
```

# --instructions--

这段代码会打印什么？
```javascript
const wrap = (n) => { value: n };
console.log(wrap(7));
```

# --answers--

- undefined
- { value: 7 }
- 7
- 一个 SyntaxError，因为块级函数体不能包含标签

# --solutions--

- undefined
