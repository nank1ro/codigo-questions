---
language: javascript
exerciseType: 2
---

# --description--

用圆括号包裹对象字面量是使用单行箭头函数构建对象的标准方式，例如当你把几个值变成一条记录时：
```javascript
const user = (name, age) => ({ name: name, age: age });
console.log(user("Ana", 30).age);
// prints 30
```
没有参数的箭头函数以一对空圆括号 `()` 开头：
```javascript
const empty = () => ({});
console.log(empty());
// prints {}
```

# --instructions--

补全 `point`，使其返回一个带有 `x` 和 `y` 属性的对象，并补全 `origin`，使其不接受任何参数，然后打印 `point(3, 4).y` 和 `origin().x`

# --seed--

```javascript
const point = (x, y) => [/]{ x: x, y: y }[/];
const origin = [/] => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --answers--

- (
- )
- ()
- [
- ]
- function
- this

# --solutions--

```javascript
const point = (x, y) => ({ x: x, y: y });
const origin = () => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --output--

4
0
