---
language: javascript
exerciseType: 4
---

# --instructions--

对代码行排序：定义 `makeCounter`，它返回一个递增并返回其私有 `count` 的箭头函数，然后创建计数器 `next`，并在同一行打印两次调用的结果

# --answers--

```javascript
const makeCounter = () => {
```

```javascript
  let count = 0;
```

```javascript
  return () => {
```

```javascript
    count += 1;
```

```javascript
    return count;
```

```javascript
  };
```

```javascript
};
```

```javascript
const next = makeCounter();
```

```javascript
console.log(next(), next());
```

# --solutions--

```javascript
const makeCounter = () => {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
};
const next = makeCounter();
console.log(next(), next());
```

# --output--

1 2
