---
language: javascript
exerciseType: 3
---

# --description--

函数会记住它被**创建**时所处作用域中的变量，即使那个作用域已经运行结束。这被称为**闭包**。
经典的例子是计数器工厂：每次调用 `makeCounter` 都会创建一个全新的 `count`，并返回一个持续使用同一个 `count` 的箭头函数：
```javascript
const makeCounter = () => {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
};
const next = makeCounter();
console.log(next());
// prints 1
console.log(next());
// prints 2
```
其他任何人都无法读取或重置 `count`：它只存在于返回的函数内部。再次调用 `makeCounter()` 会创建一个独立的计数器，它拥有自己的 `count`。

# --instructions--

这段代码会打印什么？（`makeCounter` 是描述中的函数）
```javascript
const a = makeCounter();
const b = makeCounter();
a();
a();
console.log(a() + b());
```

# --answers--

- 4
- 6
- 2
- 3

# --solutions--

- 4
