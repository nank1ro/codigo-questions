---
language: javascript
exerciseType: 4
---

# --description--

还有两个数组方法需要传入回调。
`forEach(callback)` 对每个元素调用一次回调且不返回任何东西；可以用它来执行打印这类副作用操作。
`reduce(callback, initialValue)` 把数组折叠成单个值：回调接收到目前为止的累积值和当前元素，并返回新的累积值：
```javascript
const numbers = [1, 2, 3];
numbers.forEach((n) => console.log(n));
// prints 1, 2 and 3 on three lines
const total = numbers.reduce((sum, n) => sum + n, 0);
console.log(total);
// prints 6
```

# --instructions--

对代码行排序：在声明数组之后立即用 `reduce` 计算 `prices` 的总和，用 `forEach` 在单独的行上打印每个价格，然后打印总和

# --answers--

```javascript
const prices = [4, 6, 10];
```

```javascript
const total = prices.reduce((sum, p) => sum + p, 0);
```

```javascript
prices.forEach((p) => console.log(p));
```

```javascript
console.log(total);
```

# --solutions--

```javascript
const prices = [4, 6, 10];
const total = prices.reduce((sum, p) => sum + p, 0);
prices.forEach((p) => console.log(p));
console.log(total);
```

# --output--

4
6
10
20
