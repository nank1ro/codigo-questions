---
language: kotlin
exerciseType: 1
---

# --description--

हर आर्ग्युमेंट पर `if (...) throw IllegalArgumentException(...)` लिखना शोरगुल भरा हो जाता है, इसलिए Kotlin दो शॉर्टहैंड देता है जो सादे वाक्यों की तरह पढ़े जाते हैं:

```kotlin
require(n >= 0) { "n must not be negative" }   // IllegalArgumentException थ्रो करता है
check(started) { "not started" }               // IllegalStateException थ्रो करता है
```
दोनों एक कंडीशन और मैसेज बनाने वाला एक ब्लॉक लेते हैं, और दोनों **कंडीशन के false होने पर** थ्रो करते हैं। इनमें बस एक्सेप्शन टाइप का फ़र्क है, और यह फ़र्क पढ़ने वाले के लिए एक संदेश है:

* `require` कॉलर द्वारा पास किए गए **आर्ग्युमेंट्स** की रक्षा करता है, और `IllegalArgumentException` के साथ विफल होता है।
* `check` ऑब्जेक्ट या प्रोग्राम की **स्थिति** की रक्षा करता है, और `IllegalStateException` के साथ विफल होता है।

ब्लॉक तभी एग्ज़ीक्यूट होता है जब जाँच विफल होती है, इसलिए मैसेज बनाने में सामान्य स्थिति में कुछ भी खर्च नहीं होता।

# --instructions--

दो फ़ंक्शन लिखें।

`square(n: Int): Int` को नेगेटिव `n` को अस्वीकार करने के लिए मैसेज `n must not be negative` के साथ `require` का उपयोग करना चाहिए, फिर `n * n` लौटाना चाहिए।

`finish(started: Boolean): String` को `false` को अस्वीकार करने के लिए मैसेज `not started` के साथ `check` का उपयोग करना चाहिए, फिर `done` लौटाना चाहिए।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --seed--

```kotlin
fun square(n: Int): Int {
    
}

fun finish(started: Boolean): String {
    
}
```

# --asserts--

`square(4)` must return `16`

```kotlin
    tryCatch(square(4) == 16)
```

`square(-1)` must throw an `IllegalArgumentException` saying `n must not be negative`

```kotlin
    var argMessage = ""
    try { square(-1) } catch (e: IllegalArgumentException) { argMessage = e.message ?: "" }
    tryCatch(argMessage == "n must not be negative")
```

`finish(true)` must return `done`

```kotlin
    tryCatch(finish(true) == "done")
```

`finish(false)` must throw an `IllegalStateException` saying `not started`

```kotlin
    var stateMessage = ""
    try { finish(false) } catch (e: IllegalStateException) { stateMessage = e.message ?: "" }
    tryCatch(stateMessage == "not started")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun square(n: Int): Int {
    require(n >= 0) { "n must not be negative" }
    return n * n
}

fun finish(started: Boolean): String {
    check(started) { "not started" }
    return "done"
}
```
