---
language: kotlin
exerciseType: 1
---

# --description--

जब कोई भी बिल्ट-इन टाइप आपकी विफलता को ठीक से नहीं बताता, तो अपना खुद का टाइप घोषित करें। एक एक्सेप्शन एक सामान्य क्लास है जो `Exception` को एक्सटेंड करती है और अपना टेक्स्ट पैरेंट को सौंप देती है:

```kotlin
class InsufficientFundsException(message: String) : Exception(message)
```
यह अकेली लाइन एक पूरा एक्सेप्शन टाइप है। इसे किसी भी दूसरे एक्सेप्शन की तरह थ्रो और कैच किया जाता है, और `e.message` वही टेक्स्ट लौटाता है जिसके साथ यह बनाया गया था:
```kotlin
try {
    throw InsufficientFundsException("balance too low")
} catch (e: InsufficientFundsException) {
    println(e.message) // balance too low
}
```
इसका फ़ायदा सटीकता है: एक कॉलर सिर्फ़ `InsufficientFundsException` को कैच कर सकता है और बाकी हर विफलता को आगे जाने दे सकता है।

# --instructions--

एक्सेप्शन क्लास `InsufficientFundsException` पहले से घोषित है।

एक फ़ंक्शन `withdraw(balance: Int, amount: Int): Int` लिखें जो `amount` के `balance` से ज़्यादा होने पर मैसेज `balance too low` के साथ इसे थ्रो करे, और अन्यथा बचा हुआ बैलेंस लौटाए।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```kotlin
class InsufficientFundsException(message: String) : Exception(message)

fun withdraw(balance: Int, amount: Int): Int {
    
}
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`withdraw(100, 30)` must return `70`

```kotlin
    tryCatch(withdraw(100, 30) == 70)
```

`withdraw(10, 50)` must throw an `InsufficientFundsException` saying `balance too low`

```kotlin
    var message = ""
    try { withdraw(10, 50) } catch (e: InsufficientFundsException) { message = e.message ?: "" }
    tryCatch(message == "balance too low")
```

Taking the whole balance must be allowed

```kotlin
    tryCatch(withdraw(40, 40) == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class InsufficientFundsException(message: String) : Exception(message)

fun withdraw(balance: Int, amount: Int): Int {
    if (amount > balance) throw InsufficientFundsException("balance too low")
    return balance - amount
}
```
