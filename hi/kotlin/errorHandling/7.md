---
language: kotlin
exerciseType: 2
---

# --description--

चूँकि `try` एक एक्सप्रेशन है, इसे वहाँ भी उपयोग किया जा सकता है जहाँ एक मान अपेक्षित हो — किसी फ़ंक्शन के पूरे बॉडी के रूप में भी, जो `=` से लिखा गया हो:
```kotlin
fun length(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    -1
}
```
दोनों ब्लॉक को एक ही टाइप का मान देना होगा, यहाँ `Int`। फ़ॉलबैक को `catch` ब्लॉक की आख़िरी एक्सप्रेशन के रूप में लिखें; किसी भी ब्लॉक के अंदर कोई `return` नहीं है।

# --instructions--

फ़ंक्शन को पूरा करें ताकि वह `text` में मौजूद संख्या लौटाए, और टेक्स्ट के संख्या न होने पर `0`।

# --seed--

```kotlin
fun size(text: String): Int = [/] {
    text.toInt()
} [/] (e: NumberFormatException) {
    [/]
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --answers--

- try
- catch
- 0
- finally
- throw
- 1

# --solutions--

```kotlin
fun size(text: String): Int = try {
    text.toInt()
} catch (e: NumberFormatException) {
    0
}

fun main() {
    println(size("15"))
    println(size("fifteen"))
}
```

# --output--

15
0
