---
language: kotlin
exerciseType: 2
---

# --description--

आख़िर में एक `finally` ब्लॉक जोड़ा जा सकता है। यह **चाहे कुछ भी हो** चलता है: एक सफल `try` के बाद, `catch` के रिकवर करने के बाद, और यहाँ तक कि जब एक्सेप्शन कैच ही न हो।

```kotlin
try {
    println("reading")
    println("abc".toInt())
} catch (e: NumberFormatException) {
    println("bad number")
} finally {
    println("closing")
}
```
```
reading
bad number
closing
```
इसीलिए यह ऐसे क्लीन-अप के लिए सही जगह है जिसे कभी छोड़ा नहीं जाना चाहिए, जैसे किसी फ़ाइल को बंद करना। एक `try` को कम से कम एक `catch` या `finally` चाहिए, लेकिन उसमें दोनों भी हो सकते हैं।

# --instructions--

कोड को पूरा करें ताकि गलत रूपांतरण कैच हो जाए और क्लीन-अप वाली लाइन हमेशा चले।

# --seed--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } [/] (e: NumberFormatException) {
        println("bad number")
    } [/] {
        println("closing")
    }
}
```

# --answers--

- catch
- finally
- try
- throw
- else
- do

# --solutions--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } catch (e: NumberFormatException) {
        println("bad number")
    } finally {
        println("closing")
    }
}
```

# --output--

bad number
closing
