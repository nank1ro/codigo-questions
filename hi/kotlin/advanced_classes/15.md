---
language: kotlin
exerciseType: 3
---

# --instructions--

इस कोड के साथ क्या होता है?

```kotlin
class Outer(val name: String) {
    class Nested {
        fun show(): String = name
    }
}
```

# --answers--

- यह कंपाइल नहीं होता: एक नेस्टेड क्लास बाहरी मेंबर को नहीं देख सकती, `Nested` को `inner` होना चाहिए
- यह कंपाइल होता है, और `show()` बाहरी इंस्टेंस का `name` लौटाता है
- यह कंपाइल होता है, और `show()` हमेशा एक खाली स्ट्रिंग लौटाता है
- यह कंपाइल नहीं होता: Kotlin किसी क्लास को दूसरी क्लास के अंदर अनुमति नहीं देता

# --solutions--

- यह कंपाइल नहीं होता: एक नेस्टेड क्लास बाहरी मेंबर को नहीं देख सकती, `Nested` को `inner` होना चाहिए
