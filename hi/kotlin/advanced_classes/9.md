---
language: kotlin
exerciseType: 1
---

# --description--

एक `companion object` वह सिंगलटन होता है जो एक क्लास से जुड़ा होता है। कॉन्स्टेंट के अलावा, उसका स्वाभाविक काम **फ़ैक्टरी फ़ंक्शन** रखना है: ऐसे फ़ंक्शन जो इंस्टेंस बनाने से पहले इनपुट की जांच करते हैं या उसे रूपांतरित करते हैं, और जो `null` लौटा सकते हैं जब इनपुट का कोई मतलब न हो।

कंस्ट्रक्टर को `private` चिह्नित करने से हर कॉलर को फ़ैक्टरी से गुज़रना पड़ता है:

```kotlin
class Age private constructor(val years: Int) {
    companion object {
        fun of(years: Int): Age? = if (years >= 0) Age(years) else null
    }
}

println(Age.of(30)?.years) // 30
println(Age.of(-1))        // null
```

कंपेनियन को क्लास के नाम पर कॉल किया जाता है, `Age.of(...)`, और वह प्राइवेट कंस्ट्रक्टर तक पहुँच सकता है क्योंकि वह क्लास के अंदर रहता है।

# --instructions--

एक क्लास `Temperature` घोषित करें जिसका एक **प्राइवेट** कंस्ट्रक्टर हो जो `Int` टाइप की एक `val` प्रॉपर्टी `celsius` लेता है।

एक companion object जोड़ें जिसमें फ़ैक्टरी फ़ंक्शन `fromCelsius(value: Int): Temperature?` हो जो `value` कम से कम `-273` होने पर एक `Temperature` लौटाता है, और अन्यथा `null` लौटाता है।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`Temperature.fromCelsius(20)` में `20` डिग्री होने चाहिए

```kotlin
    tryCatch(Temperature.fromCelsius(20)?.celsius == 20)
```

`Temperature.fromCelsius(-273)` को स्वीकार किया जाना चाहिए

```kotlin
    tryCatch(Temperature.fromCelsius(-273)?.celsius == -273)
```

`Temperature.fromCelsius(-300)` को `null` लौटाना चाहिए

```kotlin
    tryCatch(Temperature.fromCelsius(-300) == null)
```

`Temperature.fromCelsius(0)` को स्वीकार किया जाना चाहिए

```kotlin
    tryCatch(Temperature.fromCelsius(0)?.celsius == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
class Temperature private constructor(val celsius: Int) {
    companion object {
        fun fromCelsius(value: Int): Temperature? {
            return if (value >= -273) Temperature(value) else null
        }
    }
}
```
