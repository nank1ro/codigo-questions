---
language: kotlin
exerciseType: 1
---

# --description--

प्राइमरी कंस्ट्रक्टर की हर प्रॉपर्टी के लिए एक data class एक `componentN()` फ़ंक्शन भी जनरेट करती है: पहली प्रॉपर्टी के लिए `component1()`, दूसरी के लिए `component2()`, और आगे भी इसी तरह।

ये फ़ंक्शन **डीस्ट्रक्चरिंग घोषणाओं** (destructuring declarations) को ताकत देते हैं, जिनमें आप एक ऑब्जेक्ट को एक ही पंक्ति में कई वेरिएबल में खोलते हैं:

```kotlin
data class Point(val x: Int, val y: Int)

val point = Point(3, 7)
val (x, y) = point
println(x)               // 3
println(point.component2()) // 7
```

वेरिएबल का क्रम प्रॉपर्टी के क्रम का पालन करता है, उनके नामों का नहीं। जिसकी आपको ज़रूरत नहीं है उसे छोड़ने के लिए `_` का उपयोग करें:

```kotlin
val (_, onlyY) = point
```

# --instructions--

एक data class `Point` घोषित करें जिसमें `Int` टाइप की `val` प्रॉपर्टी `x` और `y` हों।

फिर एक फ़ंक्शन `describe(point: Point): String` लिखें जो दोनों प्रॉपर्टी पढ़ने के लिए एक डीस्ट्रक्चरिंग घोषणा का उपयोग करे और उन्हें `"x=3, y=7"` के रूप में लौटाए।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`describe(Point(3, 7))` को `"x=3, y=7"` लौटाना चाहिए

```kotlin
    tryCatch(describe(Point(3, 7)) == "x=3, y=7")
```

`describe(Point(-4, 0))` को `"x=-4, y=0"` लौटाना चाहिए

```kotlin
    tryCatch(describe(Point(-4, 0)) == "x=-4, y=0")
```

`Point` को `component1()` और `component2()` जनरेट करने चाहिए

```kotlin
    val point = Point(1, 2)
    tryCatch(point.component1() == 1 && point.component2() == 2)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
data class Point(val x: Int, val y: Int)

fun describe(point: Point): String {
    val (x, y) = point
    return "x=$x, y=$y"
}
```
