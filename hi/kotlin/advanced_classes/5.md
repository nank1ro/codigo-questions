---
language: kotlin
exerciseType: 1
---

# --description--

एक `sealed` क्लास विकल्पों का एक **बंद** सेट बताती है: केवल वही सब-क्लासेस मान्य हैं जो उसी पैकेज और मॉड्यूल में लिखी गई हैं, इसलिए कंपाइलर उन सबको जानता है।

```kotlin
sealed class Shape
data class Circle(val radius: Int) : Shape()
data class Square(val side: Int) : Shape()
```

इसका फ़ायदा **एग्ज़ॉस्टिव `when`** है: जब आप एक sealed टाइप पर ब्रांच करते हैं और हर सब-क्लास को कवर करते हैं, तो आप `else` ब्रांच हटा सकते हैं। बाद में कोई नई सब-क्लास जोड़ेंगे तो कंपाइलर चुपचाप `else` लेने के बजाय हर उस `when` की रिपोर्ट करेगा जिसे अपडेट करना आप भूल गए।

```kotlin
fun name(shape: Shape): String = when (shape) {
    is Circle -> "circle"
    is Square -> "square"
}
```

`is Circle` के बाद मान स्मार्ट कास्ट हो जाता है, इसलिए उस ब्रांच के अंदर `shape.radius` बिना किसी मैनुअल कास्ट के उपलब्ध होता है।

# --instructions--

दो data class सब-क्लासेस के साथ एक sealed क्लास `Shape` घोषित करें: `Square`, जिसमें `Int` टाइप की एक `val` प्रॉपर्टी `side` हो, और `Rectangle`, जिसमें `Int` टाइप की `val` प्रॉपर्टी `width` और `height` हों।

फिर एक फ़ंक्शन `area(shape: Shape): Int` लिखें जो शेप का क्षेत्रफल लौटाए, बिना `else` ब्रांच वाले एक एग्ज़ॉस्टिव `when` का उपयोग करके।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`area(Square(3))` को `9` लौटाना चाहिए

```kotlin
    tryCatch(area(Square(3)) == 9)
```

`area(Rectangle(2, 5))` को `10` लौटाना चाहिए

```kotlin
    tryCatch(area(Rectangle(2, 5)) == 10)
```

`area(Square(0))` को `0` लौटाना चाहिए

```kotlin
    tryCatch(area(Square(0)) == 0)
```

`Square` और `Rectangle` को `Shape` की सब-क्लासेस होना चाहिए

```kotlin
    val shapes: List<Shape> = listOf(Square(4), Rectangle(3, 3))
    tryCatch(shapes.map { area(it) } == listOf(16, 9))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
sealed class Shape
data class Square(val side: Int) : Shape()
data class Rectangle(val width: Int, val height: Int) : Shape()

fun area(shape: Shape): Int = when (shape) {
    is Square -> shape.side * shape.side
    is Rectangle -> shape.width * shape.height
}
```
