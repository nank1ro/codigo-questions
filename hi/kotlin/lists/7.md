---
language: kotlin
exerciseType: 2
---

# --description--

एक म्यूटेबल लिस्ट की लंबाई निश्चित नहीं होती।
आप जब चाहें लिस्ट के अंत में आइटम जोड़ सकते हैं!
म्यूटेबल लिस्ट में आइटम जोड़ने के लिए हम `add` फंक्शन या `+=` शॉर्टकट का उपयोग करते हैं:
```kotlin
val letters = mutableListOf("a", "b")
letters.add("c")
println(letters)
// prints [a, b, c]
```

# --instructions--

`numbers` लिस्ट में तीन और आइटम जोड़ने के लिए कोड पूरा करें, इस क्रम में `6`, फिर `8` और अंत में `9`

# --seed--

```kotlin
val numbers = mutableListOf(1, 3, 9)
numbers.[/]([/])
numbers.add([/])
[/].add(9)
println(numbers)
```

# --answers--

- append
- add
- 6
- 8
- numbers

# --solutions--

```kotlin
val numbers = mutableListOf(1, 3, 9)
numbers.add(6)
numbers.add(8)
numbers.add(9)
println(numbers)
```

# --output--

[1, 3, 9, 6, 8, 9]
