---
language: kotlin
exerciseType: 2
---

# --description--

Kotlin में हम `for..in` कीवर्ड्स का उपयोग करके बहुत सरल तरीके से लिस्ट में लूप कर सकते हैं:
```kotlin
val numbers = listOf(1, 2, 3)
for (num in numbers) {
    println(num)
}
// prints 1, 2, 3
```
`for` कीवर्ड के बाद एक वेरिएबल नाम आता है, जिसे बारी-बारी से लिस्ट के प्रत्येक आइटम का मान असाइन किया जाएगा।

# --instructions--

लिस्ट के प्रत्येक आइटम को `2` से गुणा करके प्रिंट करें

# --seed--

```kotlin
val numbers = listOf(2, 4, 6)
[/]([/][/][/]) {
    println([/][/][/])
}
```

# --answers--

- for 
- num
-  in 
- numbers
- num
-  * 
-  ** 
- 2

# --solutions--

```kotlin
val numbers = listOf(2, 4, 6)
for (num in numbers) {
    println(num * 2)
}
```

# --output--

4
8
12
