---
language: kotlin
exerciseType: 4
---

# --description--

चूंकि `findAll` एक sequence देता है, आपके द्वारा पहले से जानी जाने वाली एग्रीगेटिंग मेथड मैच पर भी काम करती हैं: `sumOf`, `maxOfOrNull`, `filter`, `sortedBy`। फ्री टेक्स्ट से संख्याएँ निकालना एक दो-चरण का काम है: पहले उन्हें मैच करें, फिर टेक्स्ट को एक संख्या में बदलें।
```kotlin
val total = Regex("""\d+""").findAll("2 apples, 3 pears").sumOf { it.value.toInt() }
println(total) // 5
```

# --instructions--

क्या आप इन पंक्तियों को इस तरह क्रमबद्ध कर सकते हैं कि टेक्स्ट की हर संख्या जोड़कर `total: 17` प्रिंट हो?

# --answers--

```kotlin
val text = "ann scored 8 and bob scored 9"
```

```kotlin
val numbers = Regex("""\d+""").findAll(text)
```

```kotlin
val total = numbers.sumOf { it.value.toInt() }
```

```kotlin
println("total: $total")
```

# --solutions--

```kotlin
val text = "ann scored 8 and bob scored 9"
val numbers = Regex("""\d+""").findAll(text)
val total = numbers.sumOf { it.value.toInt() }
println("total: $total")
```

# --output--

total: 17
