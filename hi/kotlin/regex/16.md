---
language: kotlin
exerciseType: 1
---

# --description--

एक यथार्थवादी पैटर्न आमतौर पर सब कुछ एक साथ मिलाता है: ग्रुप, जो आपके काम के हिस्से रखते हैं; literal डॉट के लिए एस्केप किया हुआ `\.`; और उनके चारों ओर टेक्स्ट को फिर से बनाने के लिए एक लैम्ब्डा रिप्लेसमेंट।
```kotlin
val email = Regex("""(\w+)@(\w+\.\w+)""")
println(email.replace("ping ann@mail.com now") { m -> "<" + m.groupValues[2] + ">" })
// ping <mail.com> now
```
पैटर्न को जितना सरल काम अनुमति देता है उतना सरल रखें: हर कानूनी ईमेल एड्रेस का वर्णन करने की कोशिश करने वाला पैटर्न अपठनीय होता है, जबकि `\w+@\w+\.\w+` एक वाक्य में एड्रेस खोजने के लिए काफी है।

# --instructions--

एक फ़ंक्शन `redactEmails(text: String): String` लिखें जो हर एड्रेस के `@` से पहले वाले नाम को `***` से बदल दे, डोमेन को रखते हुए। एक एड्रेस होता है: वर्ड कैरेक्टर की एक श्रृंखला, एक `@`, वर्ड कैरेक्टर की एक श्रृंखला, एक डॉट और वर्ड कैरेक्टर की एक श्रृंखला।

उदाहरण: `redactEmails("write to ann@mail.com")` `write to ***@mail.com` लौटाता है।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`redactEmails("write to ann@mail.com")` को `write to ***@mail.com` लौटाना चाहिए

```kotlin
    tryCatch(redactEmails("write to ann@mail.com") == "write to ***@mail.com")
```

`redactEmails("bob@site.org and cy@site.org")` को दोनों एड्रेस redact करने चाहिए

```kotlin
    tryCatch(redactEmails("bob@site.org and cy@site.org") == "***@site.org and ***@site.org")
```

`redactEmails("no address here")` को टेक्स्ट अपरिवर्तित लौटाना चाहिए

```kotlin
    tryCatch(redactEmails("no address here") == "no address here")
```

`redactEmails("")` को एक खाली स्ट्रिंग लौटानी चाहिए

```kotlin
    tryCatch(redactEmails("") == "")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun redactEmails(text: String): String {
    return Regex("""(\w+)@(\w+\.\w+)""").replace(text) { m -> "***@" + m.groupValues[2] }
}
```
