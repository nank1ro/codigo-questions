---
language: kotlin
exerciseType: 4
---

# --instructions--

क्या आप हर फल के आने की संख्या गिनकर `{apple=2, pear=1}` प्रिंट करने के लिए इन पंक्तियों को सही क्रम में लगा सकते हैं? `fruits` को `counts` से पहले घोषित करें।

# --answers--

```kotlin
val fruits = listOf("apple", "pear", "apple")
```

```kotlin
val counts = mutableMapOf<String, Int>()
```

```kotlin
for (fruit in fruits) {
```

```kotlin
    counts[fruit] = counts.getOrDefault(fruit, 0) + 1
```

```kotlin
}
```

```kotlin
println(counts)
```

# --solutions--

```kotlin
val fruits = listOf("apple", "pear", "apple")
val counts = mutableMapOf<String, Int>()
for (fruit in fruits) {
    counts[fruit] = counts.getOrDefault(fruit, 0) + 1
}
println(counts)
```

# --output--

{apple=2, pear=1}
