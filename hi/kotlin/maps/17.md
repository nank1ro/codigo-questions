---
language: kotlin
exerciseType: 2
---

# --description--

`map` एक map की हर entry को एक नए element में बदल देता है। `filter` के विपरीत, परिणाम `Map` नहीं बल्कि `List` होता है:
```kotlin
val ages = mapOf("Alice" to 30, "Bob" to 25)
val labels = ages.map { (name, age) -> "$name: $age" }
println(labels) // [Alice: 30, Bob: 25]
```

# --instructions--

`ages` की हर entry के लिए `"name is age"` फ़ॉर्मेट में एक स्ट्रिंग वाली लिस्ट बनाने के लिए कोड पूरा करें

# --seed--

```kotlin
val ages = mapOf("Ann" to 30, "Ben" to 25)
val labels = ages.[/] { ([/], [/]) -> "$name is $age" }
println(labels)
```

# --answers--

- map
- filter
- forEach
- name
- age
- key
- value

# --solutions--

```kotlin
val ages = mapOf("Ann" to 30, "Ben" to 25)
val labels = ages.map { (name, age) -> "$name is $age" }
println(labels)
```

# --output--

[Ann is 30, Ben is 25]
