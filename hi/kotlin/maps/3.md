---
language: kotlin
exerciseType: 2
---

# --description--

जब कोई key गायब हो सकती है, तो `getOrDefault` आपको `null` की जगह उपयोग करने के लिए मान चुनने देता है:
```kotlin
val ages = mapOf("Alice" to 30)
println(ages.getOrDefault("Alice", 0)) // 30
println(ages.getOrDefault("Zoe", 0))   // 0
```
पहला argument key है, दूसरा वह डिफ़ॉल्ट मान है जो key न मिलने पर लौटाया जाता है।

# --instructions--

`"kiwi"` का स्टॉक प्रिंट करने के लिए कोड पूरा करें, और अगर वह फल map में नहीं है तो `0` का उपयोग करें

# --seed--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.[/]("kiwi", [/]))
```

# --answers--

- getOrDefault
- get
- getOrNull
- 0
- null
- "kiwi"

# --solutions--

```kotlin
val stock = mapOf("apple" to 3, "pear" to 5)
println(stock.getOrDefault("kiwi", 0))
```

# --output--

0
