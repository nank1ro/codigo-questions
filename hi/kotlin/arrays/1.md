---
language: kotlin
exerciseType: 2
---

# --description--

एक **ऐरे** एक ही वेरिएबल नाम के तहत समान टाइप के मानों की एक निश्चित संख्या स्टोर करता है।
आप इसे `arrayOf` से बनाते हैं, स्क्वायर ब्रैकेट और `0` से शुरू होने वाले **इंडेक्स** का उपयोग करके एलिमेंट पढ़ते हैं, और `size` से एलिमेंट्स की संख्या प्राप्त करते हैं:
```kotlin
val colors = arrayOf("red", "green", "blue")
println(colors[0])   // red
println(colors.size) // 3
```
अंतिम एलिमेंट इंडेक्स `size - 1` पर होता है।

# --instructions--

कोड को पूरा करें ताकि `arrayOf` से ऐरे बने, फिर उसके एलिमेंट्स की संख्या प्रिंट हो, और अंत में उसका अंतिम एलिमेंट प्रिंट हो

# --seed--

```kotlin
val numbers = [/](3, 5, 7)
println(numbers.[/])
println(numbers[[/]])
```

# --answers--

- arrayOf
- Array
- size
- length
- count
- 2
- 3

# --solutions--

```kotlin
val numbers = arrayOf(3, 5, 7)
println(numbers.size)
println(numbers[2])
```

# --output--

3
7
