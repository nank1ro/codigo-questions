---
language: kotlin
exerciseType: 2
---

# --description--

हर ऐरे में इंडेक्स के साथ काम करने के लिए दो उपयोगी प्रॉपर्टीज़ होती हैं:
- `indices` वैध इंडेक्स की रेंज है, `0` से लेकर आखिरी तक
- `lastIndex` अंतिम एलिमेंट का इंडेक्स है, यानी `size - 1`
```kotlin
val letters = arrayOf("x", "y")
println(letters.lastIndex) // 1
for (i in letters.indices) {
    println(letters[i])
}
// prints x, then y
```

# --instructions--

कोड को पूरा करें ताकि `letters` का अंतिम इंडेक्स प्रिंट हो, फिर हर इंडेक्स उसके एलिमेंट के साथ प्रिंट हो

# --seed--

```kotlin
val letters = arrayOf("a", "b", "c")
println(letters.[/])
for (i in letters.[/]) {
    println("$i: ${letters[i]}")
}
```

# --answers--

- lastIndex
- indices
- size
- length
- last

# --solutions--

```kotlin
val letters = arrayOf("a", "b", "c")
println(letters.lastIndex)
for (i in letters.indices) {
    println("$i: ${letters[i]}")
}
```

# --output--

2
0: a
1: b
2: c
