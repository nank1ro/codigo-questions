---
language: kotlin
exerciseType: 2
---

# --description--

ऐरे को सीधे प्रिंट करने पर उसके एलिमेंट्स नहीं दिखते, बल्कि `[Ljava.lang.String;@1b6d3586` जैसा कुछ प्रिंट होता है।
पढ़ने लायक स्ट्रिंग बनाने के लिए `joinToString` का उपयोग करें, चाहें तो एक कस्टम सेपरेटर के साथ (डिफ़ॉल्ट `", "` है), या स्क्वायर ब्रैकेट के बीच एलिमेंट्स पाने के लिए `contentToString` का उपयोग करें:
```kotlin
val nums = intArrayOf(1, 2, 3)
println(nums.joinToString())      // 1, 2, 3
println(nums.joinToString("-"))   // 1-2-3
println(nums.contentToString())   // [1, 2, 3]
```

# --instructions--

कोड को पूरा करें ताकि `fruits` को `" - "` से अलग करके प्रिंट किया जाए, फिर पूरे ऐरे को स्क्वायर ब्रैकेट के बीच प्रिंट किया जाए

# --seed--

```kotlin
val fruits = arrayOf("apple", "kiwi", "plum")
println(fruits.[/]([/]))
println(fruits.[/]())
```

# --answers--

- joinToString
- contentToString
- toString
- join
- " - "
- "-"

# --solutions--

```kotlin
val fruits = arrayOf("apple", "kiwi", "plum")
println(fruits.joinToString(" - "))
println(fruits.contentToString())
```

# --output--

apple - kiwi - plum
[apple, kiwi, plum]
