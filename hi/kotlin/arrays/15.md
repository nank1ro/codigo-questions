---
language: kotlin
exerciseType: 4
---

# --instructions--

क्या आप इन पंक्तियों को इस तरह क्रमबद्ध कर सकते हैं कि `0`, `1`, `2` और `3` के वर्ग बनें, उन्हें लेबल में बदला जाए, और हर लेबल को उसके इंडेक्स के साथ प्रिंट किया जाए?

# --answers--

```kotlin
val squares = Array(4) { it * it }
```

```kotlin
val labels = squares.map { "sq=$it" }
```

```kotlin
for ((i, label) in labels.withIndex()) {
```

```kotlin
    println("$i $label")
```

```kotlin
}
```

# --solutions--

```kotlin
val squares = Array(4) { it * it }
val labels = squares.map { "sq=$it" }
for ((i, label) in labels.withIndex()) {
    println("$i $label")
}
```

# --output--

0 sq=0
1 sq=1
2 sq=4
3 sq=9
