---
language: kotlin
exerciseType: 2
---

# --instructions--

`clean` को लोअरकेस में प्रिंट करने के लिए कोड को पूरा करें, फिर `@` वर्ण के इंडेक्स को प्रिंट करें

# --seed--

```kotlin
val email = "  User@Example.com "
val clean = email.trim()
println(clean.[/]())
println(clean.[/]("@"))
```

# --answers--

- lowercase
- uppercase
- indexOf
- contains
- trim
- length

# --solutions--

```kotlin
val email = "  User@Example.com "
val clean = email.trim()
println(clean.lowercase())
println(clean.indexOf("@"))
```

# --output--

user@example.com
4
