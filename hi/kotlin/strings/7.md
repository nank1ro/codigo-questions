---
language: kotlin
exerciseType: 2
---

# --description--

`split` किसी सेपरेटर के आधार पर स्ट्रिंग को टुकड़ों की एक `List` में तोड़ता है, जबकि `joinToString` इसका उल्टा करता है: यह किसी कलेक्शन के तत्वों को आपके चुने हुए सेपरेटर के साथ एक स्ट्रिंग में जोड़ देता है:
```kotlin
val csv = "a,b,c"
val parts = csv.split(",")
println(parts)                  // [a, b, c]
println(parts.joinToString("/")) // a/b/c
```

# --instructions--

`csv` को कॉमा पर विभाजित करने और रंगों को `" - "` से अलग करके प्रिंट करने के लिए कोड को पूरा करें

# --seed--

```kotlin
val csv = "red,green,blue"
val colors = csv.[/](",")
println(colors.[/](" - "))
```

# --answers--

- split
- joinToString
- join
- separate
- substring

# --solutions--

```kotlin
val csv = "red,green,blue"
val colors = csv.split(",")
println(colors.joinToString(" - "))
```

# --output--

red - green - blue
