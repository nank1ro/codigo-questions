---
language: kotlin
exerciseType: 1
---

# --description--

`substring` किसी स्ट्रिंग का एक हिस्सा निकालता है। दो आर्गुमेंट के साथ, यह शुरुआती इंडेक्स से लेकर अंतिम इंडेक्स तक के वर्ण लेता है, लेकिन अंतिम इंडेक्स को **शामिल नहीं करता**।
एक आर्गुमेंट के साथ, यह उस इंडेक्स से अंत तक सब कुछ लेता है:
```kotlin
val text = "Kotlin"
println(text.substring(0, 3)) // Kot
println(text.substring(3))    // lin
```
`indexOf` और `substring` को मिलाकर आप किसी मार्कर के आसपास स्ट्रिंग को काट सकते हैं:
```kotlin
val email = "ann@mail.com"
println(email.substring(email.indexOf("@") + 1)) // mail.com
```

# --instructions--

एक फंक्शन `extension(fileName: String): String` लिखें जो `fileName` के **आखिरी** डॉट के बाद की टेक्स्ट को, बिना डॉट के, लौटाए।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`"photo.png"` का एक्सटेंशन `"png"` होना चाहिए

```kotlin
    tryCatch(extension("photo.png") == "png")
```

`"archive.tar.gz"` का एक्सटेंशन `"gz"` होना चाहिए

```kotlin
    tryCatch(extension("archive.tar.gz") == "gz")
```

`"notes.txt"` का एक्सटेंशन `"txt"` होना चाहिए

```kotlin
    tryCatch(extension("notes.txt") == "txt")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun extension(fileName: String): String {
    return fileName.substring(fileName.lastIndexOf(".") + 1)
}
```
