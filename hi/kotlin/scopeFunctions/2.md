---
language: kotlin
exerciseType: 1
---

# --description--

चूंकि `let` अपनी आखिरी एक्सप्रेशन का मान लौटाता है, यह बिना किसी मध्यवर्ती वेरिएबल के नाम दिए एक मान को **किसी और चीज़ में बदलने** का एक आसान तरीका है:
```kotlin
val price = 12
val label = price.let { "$it EUR" }
println(label) // 12 EUR
```
ब्लॉक के अंदर आप `it` का उपयोग जितनी बार चाहें कर सकते हैं:
```kotlin
println("kiwi".let { "${it.uppercase()} has ${it.length} letters" })
// KIWI has 4 letters
```

# --instructions--

एक फंक्शन `describe(word: String): String` लिखें जो शब्द को अपरकेस में, उसके बाद एक स्पेस और गोल कोष्ठकों में उसकी लंबाई के साथ लौटाए। `let` का उपयोग करें ताकि `word` को केवल `it` के माध्यम से पढ़ा जाए।

उदाहरण: `describe("kiwi")` `KIWI (4)` लौटाता है।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`describe("kiwi")` का मान `KIWI (4)` होना चाहिए

```kotlin
    tryCatch(describe("kiwi") == "KIWI (4)")
```

`describe("a")` का मान `A (1)` होना चाहिए

```kotlin
    tryCatch(describe("a") == "A (1)")
```

`describe("")` का मान ` (0)` होना चाहिए

```kotlin
    tryCatch(describe("") == " (0)")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun describe(word: String): String {
    return word.let { "${it.uppercase()} (${it.length})" }
}
```
