---
language: kotlin
exerciseType: 4
---

# --instructions--

क्या आप इन आइटम्स को ऐसे क्रम में लगा सकते हैं कि प्रोग्राम नया एलिमेंट जुड़ने के बाद लिस्ट प्रिंट करे, और फिर उसका आकार?

# --answers--

```kotlin
val words = mutableListOf("fig")
```

```kotlin
val count = words
```

```kotlin
    .apply { add("kiwi") }
```

```kotlin
    .also { println("now: $it") }
```

```kotlin
    .size
```

```kotlin
println("size: $count")
```

# --solutions--

```kotlin
val words = mutableListOf("fig")
val count = words
    .apply { add("kiwi") }
    .also { println("now: $it") }
    .size
println("size: $count")
```

# --output--

now: [fig, kiwi]
size: 2
