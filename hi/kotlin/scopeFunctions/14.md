---
language: kotlin
exerciseType: 2
---

# --instructions--

कोड पूरा करें ताकि कुत्ते का विवरण एक `with` ब्लॉक के अंदर बने और प्रोग्राम उसे प्रिंट करे।

# --seed--

```kotlin
class Dog(val name: String, val age: Int)

fun main() {
    val dog = Dog("Rex", 4)
    val info = [/](dog) {
        name + " is " + [/]
    }
    println(info)
}
```

# --answers--

- with
- age
- run
- also
- apply
- name

# --solutions--

```kotlin
class Dog(val name: String, val age: Int)

fun main() {
    val dog = Dog("Rex", 4)
    val info = with(dog) {
        name + " is " + age
    }
    println(info)
}
```

# --output--

Rex is 4
