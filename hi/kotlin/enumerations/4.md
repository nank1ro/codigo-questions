---
language: kotlin
exerciseType: 2
---

# --description--

एक enum class में, एक सामान्य class की तरह, एक **constructor** हो सकता है। हर entry अपने खुद के arguments पास करती है, और वे मान properties में संग्रहीत हो जाते हैं:
```kotlin
enum class Planet(val moons: Int) {
    MERCURY(0),
    EARTH(1),
    MARS(2)
}
println(Planet.MARS.moons) // 2
```
constructor की properties आमतौर पर `val` के साथ घोषित की जाती हैं, क्योंकि किसी entry का डेटा बदलने के लिए नहीं होता।

# --instructions--

कोड को पूरा करें ताकि हर `Coin` अपना मूल्य सेंट में संग्रहीत करे, फिर `PENNY` (जिसकी कीमत `1` सेंट है) का मूल्य प्रिंट करें

# --seed--

```kotlin
enum class Coin([/] cents: Int) {
    PENNY([/]),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.[/])
}
```

# --answers--

- val
- fun
- const
- 1
- "1"
- cents
- name
- ordinal

# --solutions--

```kotlin
enum class Coin(val cents: Int) {
    PENNY(1),
    DIME(10),
    QUARTER(25)
}

fun main() {
    println(Coin.PENNY.cents)
}
```

# --output--

1
