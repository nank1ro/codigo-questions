---
language: kotlin
exerciseType: 3
---

# --instructions--

`enum class Size { SMALL, MEDIUM, LARGE }` दिए जाने पर, नीचे दिए गए कोड के साथ क्या होता है?

```kotlin
fun label(size: Size): String = when (size) {
    Size.SMALL -> "S"
    Size.MEDIUM -> "M"
}
```

# --answers--

- यह compile नहीं होता: किसी enum पर `when` expression को हर entry कवर करनी होगी या एक `else` branch जोड़नी होगी
- यह compile होता है और `LARGE` के लिए `null` लौटाता है
- यह compile होता है और `LARGE` के लिए रनटाइम पर एक exception throw करता है
- यह compile होता है और `LARGE` के लिए अपने आप `"L"` लौटाता है

# --solutions--

- यह compile नहीं होता: किसी enum पर `when` expression को हर entry कवर करनी होगी या एक `else` branch जोड़नी होगी
