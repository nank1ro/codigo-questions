---
language: kotlin
exerciseType: 1
---

# --description--

किसी `String` से वापस entry पाने के लिए, `valueOf` फ़ंक्शन का उपयोग करें। यह उस entry को खोजता है जिसका `name` बिल्कुल मेल खाता हो:
```kotlin
val direction = Direction.valueOf("EAST")
println(direction == Direction.EAST) // true
```
यह मिलान case-sensitive होता है: `Direction.valueOf("east")` एक `IllegalArgumentException` throw करता है क्योंकि उस नाम की कोई entry मौजूद नहीं है।

# --instructions--

`ACTIVE` और `INACTIVE` entries वाली एक enum class `Status` घोषित करें। फिर एक फ़ंक्शन `statusOf(text: String)` लिखें जो `text` से मेल खाने वाला `Status` लौटाए, चाहे उसका case कुछ भी हो (यानी `"active"`, `"Active"` और `"ACTIVE"` सभी `Status.ACTIVE` दें)।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`statusOf("ACTIVE")` `Status.ACTIVE` लौटाना चाहिए

```kotlin
    tryCatch(statusOf("ACTIVE") == Status.ACTIVE)
```

`statusOf("inactive")` `Status.INACTIVE` लौटाना चाहिए

```kotlin
    tryCatch(statusOf("inactive") == Status.INACTIVE)
```

`statusOf("Active")` `Status.ACTIVE` लौटाना चाहिए

```kotlin
    tryCatch(statusOf("Active") == Status.ACTIVE)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
enum class Status {
    ACTIVE, INACTIVE
}

fun statusOf(text: String): Status {
    return Status.valueOf(text.uppercase())
}
```
