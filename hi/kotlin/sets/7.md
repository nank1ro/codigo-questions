---
language: kotlin
exerciseType: 2
---

# --description--

`Set` को `List` में बदलने के लिए हम `toList()` फ़ंक्शन का उपयोग कर सकते हैं

# --instructions--

`Set` को `List` में बदलने के लिए कोड पूरा करें

# --seed--

```kotlin
val uniqueNumbers = setOf(1, 2, 3, 3)
var numbers = [/][/][/]
numbers += 4
println(numbers)
```

# --answers--

- uniqueNumbers
- .
- toList()
- toList
- distinct()
- asList()

# --solutions--

```kotlin
val uniqueNumbers = setOf(1, 2, 3, 3)
var numbers = uniqueNumbers.toList()
numbers += 4
println(numbers)
```

# --output--

[1, 2, 3, 4]
