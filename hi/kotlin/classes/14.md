---
language: kotlin
exerciseType: 4
---

# --instructions--

क्या आप एक `data class` `Student` परिभाषित करने और उसका `toString()` प्रिंट करने के लिए इन पंक्तियों को सही क्रम में लगा सकते हैं?

# --answers--

```kotlin
data class Student(val name: String, val grade: Int)
```

```kotlin
fun main() {
```

```kotlin
    val s = Student("Alice", 90)
```

```kotlin
    println(s.toString())
```

```kotlin
}
```

# --solutions--

```kotlin
data class Student(val name: String, val grade: Int)
fun main() {
    val s = Student("Alice", 90)
    println(s.toString())
}
```

# --output--

Student(name=Alice, grade=90)
