---
language: kotlin
exerciseType: 2
---

# --instructions--

कोड को पूरा करें ताकि `Cat`, `Animal` से इनहेरिट करे और `speak()` मेथड को ओवरराइड करके `"Meow"` लौटाए।

# --seed--

```kotlin
open class Animal(val name: String) {
    open fun speak(): String = "..."
}

class Cat(name: String) [/] Animal(name) {
    [/] fun speak(): String = "Meow"
}
```

# --answers--

- :
- override
- extends
- open
- super
- implements

# --solutions--

```kotlin
open class Animal(val name: String) {
    open fun speak(): String = "..."
}

class Cat(name: String) : Animal(name) {
    override fun speak(): String = "Meow"
}
```
