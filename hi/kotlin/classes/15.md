---
language: kotlin
exerciseType: 4
---

# --instructions--

क्या आप एक `open` क्लास `Shape` को `open` मेथड `area()` के साथ, एक सब-क्लास `Square` जो उसे ओवरराइड करे, और क्षेत्रफल प्रिंट करने के लिए इन पंक्तियों को सही क्रम में लगा सकते हैं?

# --answers--

```kotlin
open class Shape {
```

```kotlin
    open fun area(): Int = 0
```

```kotlin
}
```

```kotlin
class Square(val side: Int) : Shape() {
```

```kotlin
    override fun area(): Int = side * side
```

```kotlin
}
```

```kotlin
fun main() {
```

```kotlin
    println(Square(4).area())
```

```kotlin
}
```

# --solutions--

```kotlin
open class Shape {
    open fun area(): Int = 0
}
class Square(val side: Int) : Shape() {
    override fun area(): Int = side * side
}
fun main() {
    println(Square(4).area())
}
```

# --output--

16
