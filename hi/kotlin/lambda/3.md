---
language: kotlin
exerciseType: 1
---

# --description--

लैम्ब्डा की बॉडी कई लाइनों तक फैल सकती है। कोई `return` कीवर्ड नहीं होता: **अंतिम एक्सप्रेशन** का मान ही वह होता है जो लैम्ब्डा लौटाता है।
```kotlin
val describe: (Int) -> String = { n ->
    val half = n / 2
    "half of $n is $half" // returned
}
println(describe(10)) // half of 10 is 5
```
चूंकि Kotlin में `if` एक एक्सप्रेशन है, इसलिए यह अंतिम लाइन हो सकता है और परिणाम तय कर सकता है:
```kotlin
val parity: (Int) -> String = { n -> if (n % 2 == 0) "even" else "odd" }
```

# --instructions--

`sign` में टाइप `(Int) -> String` का एक लैम्ब्डा संग्रहीत करें जो संख्या के `0` से बड़ा होने पर `"positive"` लौटाए, `0` से छोटा होने पर `"negative"` लौटाए, और अन्यथा `"zero"` लौटाए।

उदाहरण: `sign(7)`, `"positive"` लौटाता है।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --seed--

```kotlin
val sign: (Int) -> String = { n ->
    
}
```

# --asserts--

`sign(7)` को `"positive"` लौटाना चाहिए

```kotlin
    tryCatch(sign(7) == "positive")
```

`sign(-2)` को `"negative"` लौटाना चाहिए

```kotlin
    tryCatch(sign(-2) == "negative")
```

`sign(0)` को `"zero"` लौटाना चाहिए

```kotlin
    tryCatch(sign(0) == "zero")
```

`sign(100)` को `"positive"` लौटाना चाहिए

```kotlin
    tryCatch(sign(100) == "positive")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
val sign: (Int) -> String = { n ->
    if (n > 0) "positive" else if (n < 0) "negative" else "zero"
}
```
