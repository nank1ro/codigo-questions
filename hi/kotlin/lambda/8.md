---
language: kotlin
exerciseType: 1
---

# --description--

`reduce` के साथ परिणाम का आकार लैम्ब्डा पर निर्भर करता है। दो मानों को जोड़ने वाली कोई भी क्रिया काम करती है: एक योग, एक गुणन, या दोनों में से बड़ा मान रखना।
```kotlin
val numbers = listOf(3, 9, 4)
println(numbers.reduce { acc, n -> if (n > acc) n else acc }) // 9
```
ध्यान दें कि `reduce` एक खाली लिस्ट पर अपवाद (exception) फेंकता है, क्योंकि शुरू करने के लिए कोई पहला तत्व नहीं होता।

# --instructions--

एक फ़ंक्शन `product(numbers: List<Int>): Int` लिखें जो `reduce` का उपयोग करके लिस्ट की सभी संख्याओं का गुणनफल लौटाए। लिस्ट कभी खाली नहीं होती।

उदाहरण: `product(listOf(2, 3, 4))`, `24` लौटाता है।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`product(listOf(2, 3, 4))` को `24` लौटाना चाहिए

```kotlin
    tryCatch(product(listOf(2, 3, 4)) == 24)
```

`product(listOf(5))` को `5` लौटाना चाहिए

```kotlin
    tryCatch(product(listOf(5)) == 5)
```

`product(listOf(2, -3, 5))` को `-30` लौटाना चाहिए

```kotlin
    tryCatch(product(listOf(2, -3, 5)) == -30)
```

`product(listOf(7, 0, 9))` को `0` लौटाना चाहिए

```kotlin
    tryCatch(product(listOf(7, 0, 9)) == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun product(numbers: List<Int>): Int {
    return numbers.reduce { acc, n -> acc * n }
}
```
