---
language: kotlin
exerciseType: 1
---

# --description--

कुछ फ़ंक्शन **receiver वाला लैम्ब्डा** लेते हैं: लैम्ब्डा के अंदर, `this` एक विशिष्ट ऑब्जेक्ट होता है, इसलिए आप उसके मेंबर को उसका नाम दिए बिना सीधे कॉल कर सकते हैं।
`buildString` इसका एक सामान्य उदाहरण है: उसके लैम्ब्डा के अंदर `this` एक `StringBuilder` होता है, इसलिए `append` को ऐसे कॉल किया जा सकता है मानो वह एक लोकल फ़ंक्शन हो:
```kotlin
val text = buildString {
    append("Hello")
    append(", ")
    append("world")
}
println(text) // Hello, world
```
`buildString` अंतिम स्ट्रिंग लौटाता है। यह लूप में `+` से कंकैटिनेट करने का एक सुविधाजनक विकल्प है।

# --instructions--

एक फ़ंक्शन `bracketed(items: List<String>): String` लिखें जो `buildString` का उपयोग करके हर आइटम को वर्गाकार कोष्ठकों में लपेटकर, एक के बाद एक, लौटाए।

उदाहरण: `bracketed(listOf("a", "b"))`, `"[a][b]"` लौटाता है।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`bracketed(listOf("a", "b"))` को `"[a][b]"` लौटाना चाहिए

```kotlin
    tryCatch(bracketed(listOf("a", "b")) == "[a][b]")
```

`bracketed(listOf("kotlin"))` को `"[kotlin]"` लौटाना चाहिए

```kotlin
    tryCatch(bracketed(listOf("kotlin")) == "[kotlin]")
```

`bracketed(listOf())` को एक खाली स्ट्रिंग लौटानी चाहिए

```kotlin
    tryCatch(bracketed(listOf()) == "")
```

`bracketed(listOf("x", "y", "z"))` को `"[x][y][z]"` लौटाना चाहिए

```kotlin
    tryCatch(bracketed(listOf("x", "y", "z")) == "[x][y][z]")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun bracketed(items: List<String>): String {
    return buildString {
        items.forEach { append("[").append(it).append("]") }
    }
}
```
