---
language: kotlin
exerciseType: 4
---

# --description--

समरी के बाद, एक डॉक्युमेंटेशन कमेंट **KDoc टैग** के साथ पैरामीटर और लौटाए गए मान का वर्णन कर सकता है, जो हमेशा `@` से शुरू होते हैं:
```kotlin
/**
 * Returns the number of seconds in the given minutes.
 * @param minutes a whole number of minutes, never negative
 * @return [minutes] multiplied by sixty
 */
fun toSeconds(minutes: Int): Int {
    return minutes * 60
}
```
`@param` के बाद पैरामीटर का नाम और फिर उसका विवरण आता है; हर पैरामीटर का अपना एक `@param` होता है। `@return` फ़ंक्शन द्वारा लौटाए गए मान का वर्णन करता है। क्रम हमेशा वही रहता है: पहले समरी, फिर `@param` टैग, फिर `@return`।

# --instructions--

आइटम्स को क्रम में लगाएँ ताकि `twice` का दस्तावेज़ीकरण हो — एक समरी, फिर एक `@param` टैग, फिर एक `@return` टैग, सब एक ही डॉक्युमेंटेशन कमेंट के अंदर जो फ़ंक्शन के ठीक ऊपर रखा हो — और फिर `twice(21)` प्रिंट करे

# --answers--

```kotlin
/**
 * Returns the double of a number.
```

```kotlin
 * @param n the number to double
```

```kotlin
 * @return [n] multiplied by two
```

```kotlin
 */
```

```kotlin
fun twice(n: Int): Int {
    return n * 2
}
```

```kotlin
println(twice(21))
```

# --solutions--

```kotlin
/**
 * Returns the double of a number.
 * @param n the number to double
 * @return [n] multiplied by two
 */
fun twice(n: Int): Int {
    return n * 2
}
println(twice(21))
```

# --output--

42
