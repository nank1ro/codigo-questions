---
language: kotlin
exerciseType: 1
---

# --description--

`filter` एक ऐसा लैम्ब्डा लेती है जो `Boolean` लौटाता है, जिसे **प्रेडिकेट** कहते हैं, और केवल उन एलिमेंट्स के साथ एक नई लिस्ट लौटाती है जिनके लिए प्रेडिकेट `true` है:
```kotlin
val numbers = listOf(4, -2, 7, 0)
println(numbers.filter { it > 0 }) // [4, 7]
```
`it` के बजाय आप पैरामीटर को नाम दे सकते हैं, जिसके बाद एक तीर `->` आता है। नाम वाला पैरामीटर लंबे लैम्ब्डा को पढ़ने में आसान बनाता है, और जब एक लैम्ब्डा दूसरे के भीतर नेस्टेड हो तो यह ज़रूरी है, क्योंकि भीतरी `it` बाहरी एलिमेंट को छिपा देता है:
```kotlin
val minLength = 4
val words = listOf("fig", "banana", "kiwi")
println(words.filter { word -> word.length >= minLength }) // [banana, kiwi]
```

# --instructions--

एक फ़ंक्शन `longWords(words: List<String>, minLength: Int): List<String>` लिखें जो `filter` का उपयोग करके `words` के वे शब्द उसी क्रम में लौटाए जिनमें कम से कम `minLength` अक्षर हों।

उदाहरण: `longWords(listOf("fig", "banana", "kiwi"), 4)` `[banana, kiwi]` लौटाता है।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`longWords(listOf("fig", "banana", "kiwi"), 4)` को `[banana, kiwi]` लौटाना चाहिए

```kotlin
    tryCatch(longWords(listOf("fig", "banana", "kiwi"), 4) == listOf("banana", "kiwi"))
```

`longWords(listOf("fig", "banana", "kiwi"), 3)` को सभी शब्द लौटाने चाहिए

```kotlin
    tryCatch(longWords(listOf("fig", "banana", "kiwi"), 3) == listOf("fig", "banana", "kiwi"))
```

`longWords(listOf("fig", "banana", "kiwi"), 7)` को एक खाली लिस्ट लौटानी चाहिए

```kotlin
    tryCatch(longWords(listOf("fig", "banana", "kiwi"), 7) == listOf<String>())
```

`longWords(listOf(), 1)` को एक खाली लिस्ट लौटानी चाहिए

```kotlin
    tryCatch(longWords(listOf(), 1) == listOf<String>())
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun longWords(words: List<String>, minLength: Int): List<String> {
    return words.filter { word -> word.length >= minLength }
}
```
