---
language: kotlin
exerciseType: 1
---

# --description--

`let` अपने ब्लॉक की आखिरी एक्सप्रेशन का मान **लौटाता** भी है, इसलिए `?.let` एक nullable मान को बदल सकता है और उसके `null` होने पर `?:` डिफ़ॉल्ट मान भर सकता है:
```kotlin
fun priceLabel(price: Double?): String {
    return price?.let { "$it EUR" } ?: "free"
}
println(priceLabel(9.5))  // 9.5 EUR
println(priceLabel(null)) // free
```
जब `price` `null` होता है तो `let` ब्लॉक छोड़ दिया जाता है, एक्सप्रेशन `null` हो जाती है और Elvis ऑपरेटर `"free"` लौटाता है।

# --instructions--

एक फंक्शन `ageLabel(age: Int?): String` लिखें जो `age` के `null` न होने पर `"Age: "` के बाद संख्या लौटाए, या `null` होने पर `"Age unknown"` लौटाए। `?.let` और `?:` का उपयोग करें।

उदाहरण: `ageLabel(30)` `"Age: 30"` लौटाता है।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`ageLabel(30)` का मान `"Age: 30"` होना चाहिए

```kotlin
    tryCatch(ageLabel(30) == "Age: 30")
```

`ageLabel(null)` का मान `"Age unknown"` होना चाहिए

```kotlin
    tryCatch(ageLabel(null) == "Age unknown")
```

`ageLabel(0)` का मान `"Age: 0"` होना चाहिए

```kotlin
    tryCatch(ageLabel(0) == "Age: 0")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun ageLabel(age: Int?): String {
    return age?.let { "Age: $it" } ?: "Age unknown"
}
```
