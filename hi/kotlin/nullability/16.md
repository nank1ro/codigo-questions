---
language: kotlin
exerciseType: 1
---

# --description--

`?:` का दायां हिस्सा कोई भी एक्सप्रेशन हो सकता है, जिसमें `return` भी शामिल है। इससे मान के अनुपस्थित होते ही फंक्शन से बाहर निकलने का एक संक्षिप्त तरीका मिलता है:
```kotlin
fun firstUpper(text: String?): Char? {
    val first = text?.firstOrNull() ?: return null
    return first.uppercaseChar() // first is a Char here
}
```
आपके द्वारा देखे गए हर टूल अच्छी तरह मिलते हैं: nullable पैरामीटर और रिटर्न टाइप बताते हैं कि मान *कहां* अनुपस्थित हो सकता है, और `?.`, `?:`, `let`, स्मार्ट कास्ट तथा `toIntOrNull` उसे कभी क्रैश हुए बिना संभालते हैं।

# --instructions--

एक फंक्शन `divide(a: String?, b: String?): Int?` लिखें जो दोनों स्ट्रिंग्स को पूर्ण संख्याओं में बदलकर `a / b` लौटाए। जब कोई भी स्ट्रिंग `null` हो, कोई भी पूर्ण संख्या न हो, या `b` `0` हो, तो इसे `null` लौटाना चाहिए।

उदाहरण: `divide("10", "2")` `5` लौटाता है, `divide("10", "x")` `null` लौटाता है।

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`divide("10", "2")` का मान `5` होना चाहिए

```kotlin
    tryCatch(divide("10", "2") == 5)
```

`divide("10", "x")` का मान `null` होना चाहिए

```kotlin
    tryCatch(divide("10", "x") == null)
```

`divide(null, "2")` का मान `null` होना चाहिए

```kotlin
    tryCatch(divide(null, "2") == null)
```

`divide("9", null)` का मान `null` होना चाहिए

```kotlin
    tryCatch(divide("9", null) == null)
```

`divide("7", "0")` का मान `null` होना चाहिए

```kotlin
    tryCatch(divide("7", "0") == null)
```

`divide("-9", "3")` का मान `-3` होना चाहिए

```kotlin
    tryCatch(divide("-9", "3") == -3)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun divide(a: String?, b: String?): Int? {
    val x = a?.toIntOrNull() ?: return null
    val y = b?.toIntOrNull() ?: return null
    if (y == 0) return null
    return x / y
}
```
