---
language: kotlin
exerciseType: 2
---

# --description--

कलेक्शंस nullable तत्व भी रख सकते हैं: एक `List<Int?>` में `null` entries हो सकती हैं, जबकि एक `List<Int>` में कभी नहीं होतीं।
`filterNotNull()` एक नई लिस्ट लौटाता है जिसमें `null` entries हटा दी जाती हैं, और उसका एलिमेंट टाइप non-null हो जाता है, इसलिए आप तत्वों का स्वतंत्र रूप से उपयोग कर सकते हैं:
```kotlin
val scores: List<Int?> = listOf(10, null, 20)
val valid = scores.filterNotNull() // List<Int> [10, 20]
println(valid.sum())               // 30
```

# --instructions--

एक ऐसी लिस्ट घोषित करने के लिए कोड को पूरा करें जिसमें `null` संख्याएँ हो सकती हैं, फिर `null` entries हटाकर शेष मान और उनका योग प्रिंट करें

# --seed--

```kotlin
val values: List<[/]> = listOf(3, null, 5, null)
val present = values.[/]()
println(present)
println(present.sum())
```

# --answers--

- Int?
- Int
- filterNotNull
- filter
- mapNotNull
- requireNotNull

# --solutions--

```kotlin
val values: List<Int?> = listOf(3, null, 5, null)
val present = values.filterNotNull()
println(present)
println(present.sum())
```

# --output--

[3, 5]
8
