---
language: kotlin
exerciseType: 4
---

# --description--

एक एक्सटेंशन फंक्शन को **nullable रिसीवर** पर घोषित किया जा सकता है, इसलिए उसे `null` मान पर भी कॉल किया जा सकता है। अंदर, `this` nullable होता है और उसे जांचा जाना चाहिए:
```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val none: String? = null
println(none.orDash()) // -
```
ध्यान दें कि कॉल साइट पर `?.` की आवश्यकता नहीं है: फंक्शन स्वयं `null` केस को संभालता है।
स्टैंडर्ड लाइब्रेरी इस तरीके का उपयोग `isNullOrEmpty()` और `orEmpty()` में करती है, जिन्हें किसी भी `String?` पर सुरक्षित रूप से कॉल किया जा सकता है।

# --instructions--

क्या आप इन पंक्तियों को सही क्रम में लगाकर एक्सटेंशन फंक्शन `orDash` परिभाषित कर सकते हैं और अनुपस्थित नाम के लिए `-` प्रिंट कर सकते हैं, फिर मौजूद नाम के लिए `kotlin`? पहले फंक्शन परिभाषित करें

# --answers--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
```

```kotlin
val a: String? = null
val b: String? = "kotlin"
```

```kotlin
println(a.orDash())
```

```kotlin
println(b.orDash())
```

# --solutions--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val a: String? = null
val b: String? = "kotlin"
println(a.orDash())
println(b.orDash())
```

# --output--

-
kotlin
