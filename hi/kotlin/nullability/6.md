---
language: kotlin
exerciseType: 4
---

# --description--

सेफ कॉल चेन नेस्टेड ऑब्जेक्ट्स के साथ बेहतरीन काम करती हैं, जहां कोई भी स्तर अनुपस्थित हो सकता है:
```kotlin
class Address(val city: String?)
class User(val address: Address?)

val user = User(Address("Rome"))
println(user.address?.city ?: "unknown") // Rome
val nobody = User(null)
println(nobody.address?.city ?: "unknown") // unknown
```
हर `?.` अगला चरण सुरक्षित रखता है, और अंतिम `?:` डिफ़ॉल्ट मान देता है।

# --instructions--

क्या आप इन पंक्तियों को सही क्रम में लगाकर बिना पते वाले उपयोगकर्ता के लिए `unknown` प्रिंट कर सकते हैं? पहले क्लास घोषित करें, `User` से पहले `Address`

# --answers--

```kotlin
class Address(val city: String?)
```

```kotlin
class User(val address: Address?)
```

```kotlin
val bob = User(null)
```

```kotlin
println(bob.address?.city ?: "unknown")
```

# --solutions--

```kotlin
class Address(val city: String?)
class User(val address: Address?)
val bob = User(null)
println(bob.address?.city ?: "unknown")
```

# --output--

unknown
