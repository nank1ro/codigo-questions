---
language: dart
exerciseType: 1
---

# --instructions--

`['apple', 'banana', 'cherry', 'date']` सूची दी गई है, `'cherry'` का इंडेक्स खोजने के लिए `.indexOf()` का उपयोग करें। इंडेक्स लौटाएं।

# --before-seed--

```dart
int computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

'cherry' का इंडेक्स 2 है

```dart
  test('indexOf cherry is 2', () {
    expect(computeValue(), 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var fruits = ['apple', 'banana', 'cherry', 'date'];
  int result = fruits.indexOf('cherry');
```
