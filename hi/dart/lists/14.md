---
language: dart
exerciseType: 1
---

# --instructions--

एक खाली `List<int>` बनाएं। यदि यह `.isEmpty` है तो `true` लौटाएं, फिर उसमें `1` जोड़ें और यदि यह `.isNotEmpty` है तो `true` लौटाएं। दोनों बूलियन को `List<bool>` के रूप में लौटाएं।

# --before-seed--

```dart
List<bool> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

पहला मान true है (खाली थी)

```dart
  test('first is true', () {
    expect(computeValue()[0], true, reason: '--err-t1--');
  });
```

दूसरा मान true है (जोड़ने के बाद खाली नहीं है)

```dart
  test('second is true', () {
    expect(computeValue()[1], true, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<int> nums = [];
  bool wasEmpty = nums.isEmpty;
  nums.add(1);
  bool nowNotEmpty = nums.isNotEmpty;
  List<bool> result = [wasEmpty, nowNotEmpty];
```
