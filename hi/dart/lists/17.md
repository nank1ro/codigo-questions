---
language: dart
exerciseType: 1
---

# --instructions--

`[1, 2, 3, 4, 5, 6]` सूची दी गई है, केवल सम संख्याएं फ़िल्टर करने के लिए `.where()` का उपयोग करें। परिणाम को `List<int>` के रूप में लौटाएं।

# --before-seed--

```dart
List<int> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

केवल सम संख्याएं लौटाता है

```dart
  test('evens are [2, 4, 6]', () {
    expect(computeValue(), [2, 4, 6], reason: '--err-t1--');
  });
```

लंबाई 3 है

```dart
  test('length is 3', () {
    expect(computeValue().length, 3, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var numbers = [1, 2, 3, 4, 5, 6];
  List<int> result = numbers.where((n) => n % 2 == 0).toList();
```
