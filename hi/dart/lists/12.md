---
language: dart
exerciseType: 1
---

# --instructions--

`[5, 10, 15, 20]` सूची दी गई है, यदि इसमें `15` है तो `true` लौटाएं, अन्यथा `false` लौटाएं। `.contains()` का उपयोग करें।

# --before-seed--

```dart
bool computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

15 के लिए true लौटाता है

```dart
  test('contains 15', () {
    expect(computeValue(), true, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var numbers = [5, 10, 15, 20];
  bool result = numbers.contains(15);
```
