---
language: dart
exerciseType: 2
---

# --description--

`.contains()` मेथड जाँचता है कि किसी सूची में कोई दिया गया मान है या नहीं। यदि मिलता है तो `true` लौटाता है, अन्यथा `false`:

```dart
var fruits = ['apple', 'mango', 'grape'];
print(fruits.contains('mango'));  // true
print(fruits.contains('orange')); // false
```

यह लूप की आवश्यकता के बिना सदस्यता जाँच के लिए उपयोगी है।

# --instructions--

`42` सूची में है या नहीं यह जाँचने और परिणाम प्रिंट करने के लिए कोड पूरा करें।

# --seed--

```dart
var numbers = [10, 42, 77, 5];
print(numbers.[/](42));
```

# --answers--

- contains
- includes
- has
- find
- exists
- indexOf

# --solutions--

```dart
var numbers = [10, 42, 77, 5];
print(numbers.contains(42));
```

# --output--

true
