---
language: dart
exerciseType: 1
---

# --instructions--

`[100, 200, 300, 400, 500]` सूची दी गई है, एक `List<int>` लौटाएं जहाँ इंडेक्स `0` पर `.first` तत्व और इंडेक्स `1` पर `.last` तत्व हो।

# --before-seed--

```dart
List<int> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

पहला तत्व 100 है

```dart
  test('first is 100', () {
    expect(computeValue()[0], 100, reason: '--err-t1--');
  });
```

अंतिम तत्व 500 है

```dart
  test('last is 500', () {
    expect(computeValue()[1], 500, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var nums = [100, 200, 300, 400, 500];
  List<int> result = [nums.first, nums.last];
```
