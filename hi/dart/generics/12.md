---
language: dart
exerciseType: 1
---

# --description--

एक बाउंड टाइप पैरामीटर को स्वयं भी उल्लेखित कर सकता है। `Comparable<T>` उन सब चीज़ों का इंटरफ़ेस है जो `compareTo` के ज़रिए जानती हैं कि अपने ही क़िस्म से खुद की तुलना कैसे करनी है:

```dart
print('fig'.compareTo('kiwi')); // नेगेटिव: fig पहले आता है
print('kiwi'.compareTo('fig')); // positive
print('fig'.compareTo('fig'));  // zero
```

इसलिए `T extends Comparable<T>` पढ़ा जाता है "कोई भी टाइप जिसकी तुलना उसके अपने ही जैसे से हो सकती है", और यही एक सॉर्टिंग या अधिकतम फंक्शन को चाहिए होता है:

```dart
T maxOf<T extends Comparable<T>>(T a, T b) => a.compareTo(b) >= 0 ? a : b;

print(maxOf('fig', 'kiwi')); // kiwi
```

`String` और `DateTime` दोनों इसे सीधे संतुष्ट करते हैं। `int` और `double` `Comparable<num>` इम्प्लीमेंट करते हैं, इसलिए संख्याओं की एक सूची आसानी से `num` की तरह तुलना की जाती है।

# --instructions--

एक जेनेरिक फंक्शन `largest` लिखें जिसका टाइप पैरामीटर `T` `Comparable<T>` से बाउंडेड हो, जो एक गैर-खाली `List<T>` `items` ले और सबसे बड़ा एलिमेंट, `compareTo` से तुलना करके, लौटाए।

उदाहरण:
```dart
print(largest(['fig', 'kiwi', 'apple'])); // kiwi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest(['fig', 'kiwi', 'apple'])` को `'kiwi'` लौटाना चाहिए

```dart
  test('test1', () {
    expect(largest(['fig', 'kiwi', 'apple']), 'kiwi', reason: '--err-t1--');
  });
```

`largest` को संख्याओं और तारीखों के साथ भी काम करना चाहिए

```dart
  test('test2', () {
    expect(largest([3, 9, 5]), 9, reason: '--err-t2--');
    expect(
      largest([DateTime(2020), DateTime(2024), DateTime(2001)]),
      DateTime(2024),
      reason: '--err-t2--',
    );
  });
```

`largest` को एक-एलिमेंट वाली सूची का वही एकमात्र एलिमेंट लौटाना चाहिए, अंतिम एलिमेंट तब लौटाना चाहिए जब वह सबसे बड़ा हो, और उसे `largest<String>(['a', 'z'])` जैसा स्पष्ट टाइप आर्ग्युमेंट स्वीकार करना चाहिए

```dart
  test('test3', () {
    expect(largest<String>(['a', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['only']), 'only', reason: '--err-t3--');
    expect(largest(['a', 'b', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['z', 'b', 'a']), 'z', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
T largest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) > 0) {
      best = item;
    }
  }
  return best;
}
```
