---
language: dart
exerciseType: 4
---

# --description--

वही बाउंड सबसे छोटे एलिमेंट के लिए भी उतना ही अच्छा काम करता है: बदलती केवल तुलना का चिह्न है। `compareTo` एक ऋणात्मक संख्या लौटाता है जब रिसीवर पहले आता है, इसलिए `item.compareTo(best) < 0` का अर्थ है "यह वाला छोटा है"।

# --instructions--

`Comparable<T>` से बाउंडेड एक जेनेरिक फंक्शन `smallest` परिभाषित करने के लिए आइटम्स को क्रमबद्ध करें, जो सूची का सबसे छोटा एलिमेंट लौटाए, और एक `main` जो `['pear', 'fig', 'kiwi']` में से सबसे छोटा प्रिंट करे। फंक्शन को `main` से पहले परिभाषित करें। प्रोग्राम को प्रिंट करना चाहिए:

```
fig
```

# --answers--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
```

```dart
  var best = items.first;
```

```dart
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
```

```dart
  return best;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(smallest(['pear', 'fig', 'kiwi']));
```

```dart
}
```

# --solutions--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
  return best;
}

void main() {
  print(smallest(['pear', 'fig', 'kiwi']));
}
```

# --output--

fig
