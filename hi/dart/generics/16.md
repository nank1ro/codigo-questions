---
language: dart
exerciseType: 1
---

# --description--

एक `typedef` किसी टाइप को एक नाम देता है, और वह अपने टाइप पैरामीटर भी ले सकता है। आम वजह यह है कि फंक्शन टाइप्स के एक परिवार को हर उपयोग पर खुलकर लिखने की बजाय एक बार नाम दे दिया जाए:

```dart
typedef Transform<I, O> = O Function(I input);

final Transform<String, int> length = (word) => word.length;
print(length('kiwi')); // 4
```

`Transform<String, int>` केवल `int Function(String)` लिखने का दूसरा तरीका है, इसलिए दोनों आपस में बदल-बदलाकर उपयोग हो सकते हैं। फ़ायदा पठनीयता का है: `Transform<I, O> transform` घोषित पैरामीटर बताता है कि फंक्शन किस काम का है, जबकि `O Function(I)` केवल यह कि वह कैसा दिखता है।

एक जेनेरिक typedef और एक जेनेरिक फंक्शन सहज रूप से मिलकर काम करते हैं, जिसमें फंक्शन के अपने टाइप पैरामीटर typedef के पैरामीटर भर देते हैं।

# --instructions--

एक जेनेरिक typedef `Transform` घोषित करें जिसके दो टाइप पैरामीटर `I` और `O` हों और जो एक `I` लेकर एक `O` लौटाने वाले फंक्शन को नाम दे।

फिर टाइप पैरामीटर `I` और `O` वाला एक जेनेरिक फंक्शन `convertAll` लिखें जो एक `List<I>` `items` और एक `Transform<I, O>` `transform` ले, और हर बदले हुए एलिमेंट को, उसी क्रम में, रखने वाली एक `List<O>` लौटाए।

उदाहरण:
```dart
print(convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)); // [3, 4]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`convertAll<String, int>(['fig', 'kiwi'], (w) => w.length)` को `[3, 4]` लौटाना चाहिए

```dart
  test('test1', () {
    expect(
      convertAll<String, int>(['fig', 'kiwi'], (w) => w.length),
      [3, 4],
      reason: '--err-t1--',
    );
  });
```

`convertAll` को एक `List<O>` लौटाना चाहिए, इसलिए इंट्स को स्ट्रिंग्स में बदलने पर एक `List<String>` मिलनी चाहिए

```dart
  test('test2', () {
    final result = convertAll<int, String>([1, 2], (n) => 'n$n');
    expect(result, ['n1', 'n2'], reason: '--err-t2--');
    expect(result, isA<List<String>>(), reason: '--err-t2--');
  });
```

typedef `Transform` को एक फंक्शन टाइप को नाम देना चाहिए, और खाली सूची को एक खाली सूची मिलनी चाहिए

```dart
  test('test3', () {
    final Transform<int, int> twice = (n) => n * 2;
    expect(convertAll<int, int>([1, 2, 3], twice), [2, 4, 6], reason: '--err-t3--');
    expect(convertAll<String, int>(<String>[], (w) => w.length), <int>[], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
typedef Transform<I, O> = O Function(I input);

List<O> convertAll<I, O>(List<I> items, Transform<I, O> transform) {
  return items.map(transform).toList();
}
```
