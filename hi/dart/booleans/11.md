---
language: dart
exerciseType: 1
---

# --instructions--

`||` ऑपरेटर (OR) तब `true` रिटर्न करता है जब **कम से कम एक** ऑपरेंड `true` हो।

`||` ऑपरेटर का उपयोग करके `false || true` को संयोजित करने वाला एक बूलियन चर `result` घोषित करें।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` का मान `true` के बराबर होना चाहिए।

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result` का प्रकार `bool` होना चाहिए।

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = false || true;
```
