---
language: dart
exerciseType: 1
---

# --instructions--

एक बूलियन चर `flag` घोषित करें जिसका मान `true` हो, फिर एक अन्य बूलियन चर `isFlag` घोषित करें और `is` कीवर्ड का उपयोग करके जांचें कि `flag` `bool` प्रकार का है।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`flag` का मान `true` के बराबर होना चाहिए।

```dart
  test('test1', () {
    expect(flag, true, reason: '--err-t1--');
  });
```

`isFlag` `true` होना चाहिए (`is bool` जांच का परिणाम)।

```dart
  test('test2', () {
    expect(isFlag, true, reason: '--err-t2--');
  });
```

`isFlag` का प्रकार `bool` होना चाहिए।

```dart
  test('test3', () {
    expect(isFlag, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool flag = true;
bool isFlag = flag is bool;
```
