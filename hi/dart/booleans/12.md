---
language: dart
exerciseType: 1
---

# --instructions--

`&&`, `||` और `!` को संयोजित करके एक परिणाम बनाएं।

इस सटीक अभिव्यक्ति का उपयोग करके एक बूलियन चर `result` घोषित करें: `!false && (true || false)`।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` का मान `true` के बराबर होना चाहिए।

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result` का प्रकार `bool` होना चाहिए।

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = !false && (true || false);
```
