---
language: dart
exerciseType: 1
---

# --instructions--

दो शीर्ष-स्तरीय `int` वेरिएबल घोषित करें: `x` मान `25` के साथ और `y` मान `9` के साथ। फिर एक शीर्ष-स्तरीय `int` वेरिएबल `diff` घोषित करें जो `y` को `x` से घटाने के परिणाम को संग्रहीत करता है।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`diff` `16` के बराबर होना चाहिए

```dart
  test('test1', () {
    expect(diff, 16, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 25;
int y = 9;
int diff = x - y;

void main() {
  print(diff);
}
```
