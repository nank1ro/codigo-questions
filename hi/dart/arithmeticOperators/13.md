---
language: dart
exerciseType: 1
---

# --instructions--

एक शीर्ष-स्तरीय `double` वेरिएबल `result` घोषित करें जो `7` को `2` से विभाजित करने के परिणाम को संग्रहीत करता है। याद रखें कि Dart में `/` हमेशा एक `double` लौटाता है।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` मान `3.5` के साथ एक `double` होना चाहिए

```dart
  test('test1', () {
    expect(result, isA<double>(), reason: '--err-t1--');
    expect(result, closeTo(3.5, 0.001), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double result = 7 / 2;

void main() {
  print(result);
}
```
