---
language: dart
exerciseType: 1
---

# --instructions--

तीन शीर्ष-स्तरीय `int` वेरिएबल घोषित करें: `a` मान `10` के साथ, `b` मान `3` के साथ, और `c` मान `2` के साथ। फिर घोषित करें:
- `sum` = `a + b`
- `product` = `b * c`
- `combined` = `sum - product`

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` `13` होना चाहिए, `product` `6` होना चाहिए, और `combined` `7` होना चाहिए

```dart
  test('test1', () {
    expect(sum, 13, reason: '--err-t1--');
    expect(product, 6, reason: '--err-t2--');
    expect(combined, 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 10;
int b = 3;
int c = 2;
int sum = a + b;
int product = b * c;
int combined = sum - product;

void main() {
  print(combined);
}
```
