---
language: dart
exerciseType: 1
---

# --instructions--

मापांक ऑपरेटर `%` का उपयोग करके `17` को `5` से विभाजित करने पर शेषफल खोजें। परिणाम को `remainder` नामके एक शीर्ष-स्तरीय वेरिएबल में संग्रहीत करें।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

वेरिएबल `remainder` `2` के बराबर होना चाहिए

```dart
  test('test1', () {
    expect(remainder, 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 17 % 5;

void main() {
  print(remainder);
}
```
