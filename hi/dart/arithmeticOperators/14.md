---
language: dart
exerciseType: 1
---

# --instructions--

दो शीर्ष-स्तरीय `int` वेरिएबल घोषित करें: `total` मान `29` के साथ और `perGroup` मान `4` के साथ। फिर एक शीर्ष-स्तरीय `int` वेरिएबल `groups` घोषित करें जो संग्रहीत करता है कि कितने पूर्ण समूह `4` के `29` में फिट होते हैं (पूर्णांक विभाजन `~/` का उपयोग करें)।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`groups` `7` के बराबर होना चाहिए

```dart
  test('test1', () {
    expect(groups, 7, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 29;
int perGroup = 4;
int groups = total ~/ perGroup;

void main() {
  print(groups);
}
```
