---
language: dart
exerciseType: 1
---

# --instructions--

दो शीर्ष-स्तरीय `int` वेरिएबल घोषित करें: `a` मान `12` के साथ और `b` मान `8` के साथ। फिर एक तीसरा शीर्ष-स्तरीय `int` वेरिएबल `total` घोषित करें जो उनके योग को संग्रहीत करता है।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`a` `12` होना चाहिए, `b` `8` होना चाहिए, और `total` `20` होना चाहिए

```dart
  test('test1', () {
    expect(a, 12, reason: '--err-t1--');
    expect(b, 8, reason: '--err-t2--');
    expect(total, 20, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 12;
int b = 8;
int total = a + b;

void main() {
  print(total);
}
```
