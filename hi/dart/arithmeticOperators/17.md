---
language: dart
exerciseType: 1
---

# --instructions--

एक दुकान `itemCount` आइटम को `pricePerItem` डॉलर में बेचती है, कुल से घटाई गई `discount` डॉलर राशि के साथ। ये तीन शीर्ष-स्तरीय `int` वेरिएबल घोषित करें: `itemCount` = `6`, `pricePerItem` = `15`, `discount` = `10`। फिर एक शीर्ष-स्तरीय `int` वेरिएबल `totalPrice` घोषित करें जो छूट के बाद कुल की गणना करता है।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`totalPrice` `80` के बराबर होना चाहिए

```dart
  test('test1', () {
    expect(totalPrice, 80, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int itemCount = 6;
int pricePerItem = 15;
int discount = 10;
int totalPrice = itemCount * pricePerItem - discount;

void main() {
  print(totalPrice);
}
```
