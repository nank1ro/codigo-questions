---
language: dart
exerciseType: 4
---

# --description--

स्पॉन किया गया isolate हमेशा वही चार क़दम चलता है: मेलबॉक्स खोलो, वर्कर को उसके पते के साथ स्पॉन करो, जवाब का इंतज़ार करो, उसका उपयोग करो।

```dart
import 'dart:isolate';

void worker(SendPort port) {
  port.send('done');
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

`Isolate.spawn` के आगे वाला `await` isolate के *शुरू होने* का इंतज़ार करता है, उसके काम के पूरा होने का नहीं: नतीजा बाद में पोर्ट के ज़रिए आता है।

# --instructions--

आइटम क्रम में लगाओ ताकि एक प्रोग्राम बने जो एक `worker` isolate स्पॉन करे जो main isolate को `7 * 6` वापस भेजे, जो उसे छापता है। import को सबसे पहले रखो और `worker` को `main` से पहले परिभाषित करो। प्रोग्राम को `42` छापना चाहिए।

# --answers--

```dart
import 'dart:isolate';
```

```dart
void worker(SendPort port) {
```

```dart
  port.send(7 * 6);
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  final receivePort = ReceivePort();
```

```dart
  await Isolate.spawn(worker, receivePort.sendPort);
```

```dart
  print(await receivePort.first);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
void worker(SendPort port) {
  port.send(7 * 6);
}
Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(worker, receivePort.sendPort);
  print(await receivePort.first);
}
```

# --output--

42
