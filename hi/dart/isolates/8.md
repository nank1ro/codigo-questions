---
language: dart
exerciseType: 1
---

# --description--

`Isolate.spawn` entry point को ठीक **एक** संदेश देता है, और वर्कर को आम तौर पर जवाब देने के लिए एक `SendPort` और काम करने के लिए कुछ डेटा, दोनों चाहिए होते हैं। आम तरकीब यह है कि सब कुछ एक `List` में बाँधो और दूसरी तरफ़ खोलो:

```dart
void multiply(List<Object> message) {
  final port = message[0] as SendPort;
  final value = message[1] as int;
  port.send(value * 2);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(multiply, <Object>[receivePort.sendPort, 21]);
  print(await receivePort.first); // 42
}
```

सूची जाते समय कॉपी हो जाती है, इसलिए वर्कर अपने ही मान पढ़ता है। एक `SendPort` उन चंद चीज़ों में से एक है जो कॉपी नहीं होती बल्कि साझा की जाती हैं: वह उस isolate के मेलबॉक्स की ओर इशारा करता रहता है जिसने उसे बनाया, और यही वजह है कि उसे वापसी के पते की तरह इस्तेमाल किया जा सकता है।

# --instructions--

एक टॉप-लेवल फ़ंक्शन `sendSum` लिखो जो एक `List<Object>` `message` लेता है जिसका एलिमेंट `0` एक `SendPort` है और एलिमेंट `1` तथा `2` `int` हैं, और पोर्ट पर उनका योग भेजता है।

फिर एक फ़ंक्शन `addInIsolate` लिखो जो दो `int` `a` और `b` लेता है और एक `Future<int>` लौटाता है जो स्पॉन किए गए `sendSum` isolate द्वारा गणना किए गए योग के साथ पूरा होता है।

उदाहरण:
```dart
print(await addInIsolate(2, 3)); // 5
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'dart:isolate';

import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sendSum` को संदेश की दोनों संख्याओं का योग पोर्ट पर भेजना चाहिए

```dart
  test('test1', () async {
    final port = ReceivePort();
    sendSum(<Object>[port.sendPort, 4, 6]);
    expect(await port.first, 10, reason: '--err-t1--');
  });
```

`addInIsolate(2, 3)` को `5` के साथ पूरा होना चाहिए

```dart
  test('test2', () async {
    expect(await addInIsolate(2, 3), 5, reason: '--err-t2--');
  });
```

`addInIsolate(10, -4)` को `6` के साथ पूरा होना चाहिए और `addInIsolate` को एक `Future<int>` लौटाना चाहिए

```dart
  test('test3', () async {
    expect(await addInIsolate(10, -4), 6, reason: '--err-t3--');
    expect(addInIsolate(1, 1), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
void sendSum(List<Object> message) {
  final port = message[0] as SendPort;
  final a = message[1] as int;
  final b = message[2] as int;
  port.send(a + b);
}

Future<int> addInIsolate(int a, int b) async {
  final receivePort = ReceivePort();
  await Isolate.spawn(sendSum, <Object>[receivePort.sendPort, a, b]);
  return await receivePort.first as int;
}
```
