---
language: dart
exerciseType: 2
---

# --description--

`first` एक संदेश पढ़ता है और मेलबॉक्स बंद कर देता है। एक `ReceivePort` एक **`Stream`** भी है, इसलिए कई संदेश पढ़ने के लिए तुम उस पर `await for` से लूप चलाते हो।

लूप अपने आप कभी ख़त्म नहीं होता: पोर्ट खुला रहता है और ऐसे संदेश का इंतज़ार करता रहता है जो शायद कभी न आए। इसलिए वर्कर आख़िरी मान एक संकेत की तरह भेजता है, अक्सर `null`, और लिसनर उस पर **`close()`** बुलाकर प्रतिक्रिया देता है, जो स्ट्रीम और लूप को ख़त्म कर देता है:

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(countdown, receivePort.sendPort);
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --instructions--

कोड पूरा करो ताकि स्पॉन किया गया isolate `3`, `2`, `1` और फिर `null` भेजे, और main isolate हर संख्या छापे, `null` पर मेलबॉक्स बंद करे और आख़िर में `done` छापे।

# --seed--

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.[/](countdown, receivePort.sendPort);
  await [/] (final message in receivePort) {
    if (message == null) {
      receivePort.[/]();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --answers--

- spawn
- for
- close
- run
- while
- listen
- first

# --solutions--

```dart
import 'dart:isolate';

void countdown(SendPort port) {
  port.send(3);
  port.send(2);
  port.send(1);
  port.send(null);
}

Future<void> main() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(countdown, receivePort.sendPort);
  await for (final message in receivePort) {
    if (message == null) {
      receivePort.close();
    } else {
      print(message);
    }
  }
  print('done');
}
```

# --output--

3
2
1
done
