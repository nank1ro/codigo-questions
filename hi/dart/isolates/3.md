---
language: dart
exerciseType: 1
---

# --description--

isolate के बीच केवल **डेटा** कॉपी होता है; **कोड** नहीं। प्रोग्राम का हर isolate प्रोग्राम के सभी टॉप-लेवल फ़ंक्शन और क्लास पहले से देख सकता है, इसलिए `Isolate.run` को दी गई गणना उन्हें आज़ादी से कॉल कर सकती है:

```dart
int slowLength(String text) => text.length;

Future<int> lengthInIsolate(String text) {
  return Isolate.run(() => slowLength(text));
}
```

सफ़र करता है केवल कैप्चर किया गया `text` जाते समय और नतीजे वाला `int` आते समय, हर एक एक प्रति। पैटर्न हमेशा वही है: भारी फ़ंक्शन को वहीं रहने दो, और **कॉल** को `Isolate.run` में लपेटो।

# --instructions--

फ़ंक्शन `wordCount(text)` एक स्ट्रिंग के शब्दों की गिनती करता है। एक फ़ंक्शन `countWords` लिखो जो एक `String` `text` लेता है और एक `Future<int>` लौटाता है जो `wordCount(text)` के साथ पूरा होता है, जिसकी गणना `Isolate.run` से दूसरे isolate पर होती है।

उदाहरण:
```dart
print(await countWords('hello isolate world')); // 3
```

# --before-seed--

```dart
import 'dart:isolate';

int wordCount(String text) {
  return text.split(' ').where((w) => w.isNotEmpty).length;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countWords('hello isolate world')` को `3` के साथ पूरा होना चाहिए

```dart
  test('test1', () async {
    expect(await countWords('hello isolate world'), 3, reason: '--err-t1--');
  });
```

`countWords('Dart')` को `1` के साथ पूरा होना चाहिए और `countWords('')` को `0` के साथ

```dart
  test('test2', () async {
    expect(await countWords('Dart'), 1, reason: '--err-t2--');
    expect(await countWords(''), 0, reason: '--err-t2--');
  });
```

`countWords('  a  b ')` को `2` के साथ पूरा होना चाहिए और `countWords` को एक `Future<int>` लौटाना चाहिए

```dart
  test('test3', () async {
    expect(await countWords('  a  b '), 2, reason: '--err-t3--');
    expect(countWords('one two'), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<int> countWords(String text) {
  return Isolate.run(() => wordCount(text));
}
```
