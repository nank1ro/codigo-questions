---
language: dart
exerciseType: 1
---

# --instructions--

दो `String` प्रॉपर्टी वाली `Car` क्लास बनाएं: `brand` और `model`। एक कंस्ट्रक्टर जोड़ें जो शॉर्टहैंड सिंटैक्स का उपयोग करके दोनों को सेट करे।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Car('Toyota', 'Corolla')` बनाने पर `brand` का मान `'Toyota'` होना चाहिए

```dart
  test('test1', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.brand, 'Toyota', reason: '--err-t1--');
  });
```

`Car('Toyota', 'Corolla')` बनाने पर `model` का मान `'Corolla'` होना चाहिए

```dart
  test('test2', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.model, 'Corolla', reason: '--err-t2--');
  });
```

`Car('Honda', 'Civic')` बनाने पर `brand` का मान `'Honda'` होना चाहिए

```dart
  test('test3', () {
    final car = Car('Honda', 'Civic');
    expect(car.brand, 'Honda', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Car {
  String brand;
  String model;
  Car(this.brand, this.model);
}
```
