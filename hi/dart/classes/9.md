---
language: dart
exerciseType: 1
---

# --instructions--

दो `int` प्रॉपर्टी `x` और `y` वाली `Point` क्लास बनाएं। एक कंस्ट्रक्टर जोड़ें जो दोनों को सेट करे। साथ ही एक नामित कंस्ट्रक्टर `Point.origin()` जोड़ें जो `x` और `y` दोनों को `0` पर सेट करे।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Point(3, 5)` का `x` मान `3` और `y` मान `5` होना चाहिए

```dart
  test('test1', () {
    final p = Point(3, 5);
    expect(p.x, 3, reason: '--err-t1--');
    expect(p.y, 5, reason: '--err-t1--');
  });
```

`Point.origin()` का `x` और `y` दोनों का मान `0` होना चाहिए

```dart
  test('test2', () {
    final p = Point.origin();
    expect(p.x, 0, reason: '--err-t2--');
    expect(p.y, 0, reason: '--err-t2--');
  });
```

`Point(-1, 4)` का `x` मान `-1` होना चाहिए

```dart
  test('test3', () {
    final p = Point(-1, 4);
    expect(p.x, -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Point {
  int x;
  int y;
  Point(this.x, this.y);
  Point.origin() : x = 0, y = 0;
}
```
