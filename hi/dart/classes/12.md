---
language: dart
exerciseType: 1
---

# --description--

**Method overriding** एक subclass को superclass में पहले से मौजूद किसी मेथड का अपना खुद का implementation देने देता है। इसके लिए `@override` एनोटेशन का उपयोग करें:

```dart
class Shape {
  double area() => 0;
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() => 3.14159 * radius * radius;
}
```

`@override` एनोटेशन Dart (और अन्य डेवलपर्स) को बताता है कि आप जानबूझकर superclass के मेथड को बदल रहे हैं।

# --instructions--

एक क्लास `Animal` बनाएं जिसमें एक मेथड `sound()` हो जो `String` `'...'` लौटाए। फिर एक subclass `Dog` बनाएं जो `sound()` को override करके `'Woof'` लौटाए। एक और subclass `Cat` जोड़ें जो `sound()` को override करके `'Meow'` लौटाए।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Animal.sound()`, `'...'` लौटाता है

```dart
    test('animal sound', () {
      expect(Animal().sound(), '...', reason: '--err-t1--');
    });
```

`Dog.sound()`, `'Woof'` लौटाता है

```dart
    test('dog sound', () {
      Animal dog = Dog();
      expect(dog.sound(), 'Woof', reason: '--err-t2--');
    });
```

`Cat.sound()`, `'Meow'` लौटाता है

```dart
    test('cat sound', () {
      Animal cat = Cat();
      expect(cat.sound(), 'Meow', reason: '--err-t3--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String sound() => '...';
}

class Dog extends Animal {
  @override
  String sound() => 'Woof';
}

class Cat extends Animal {
  @override
  String sound() => 'Meow';
}
```
