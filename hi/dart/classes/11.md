---
language: dart
exerciseType: 2
---

# --description--

जब किसी subclass के कंस्ट्रक्टर को superclass के कंस्ट्रक्टर को कॉल करना हो, तो **initializer list** में `super` कीवर्ड का उपयोग करें:

```dart
class Vehicle {
  String brand;
  Vehicle(this.brand);
}

class Car extends Vehicle {
  int doors;
  Car(String brand, this.doors) : super(brand);
}
```

`super(brand)`, `brand` आर्गुमेंट को `Vehicle` के कंस्ट्रक्टर तक आगे भेज देता है।

# --instructions--

`Cat` कंस्ट्रक्टर को इस तरह पूरा करें कि वह `name` के साथ `Animal` सुपरक्लास के कंस्ट्रक्टर को कॉल करे।

# --seed--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Cat extends Animal {
  Cat(String name) : [/](name);
}
```

# --answers--

- super
- Animal
- parent
- base
- this

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Cat extends Animal {
  Cat(String name) : super(name);
}
```
