---
language: dart
exerciseType: 1
---

# --instructions--

`int` फ़ील्ड `width` और `height` वाली `Rectangle` क्लास बनाएं और एक कंस्ट्रक्टर जो उन्हें सेट करे। एक गेटर `area` जोड़ें जो `width * height` लौटाए, और एक गेटर `perimeter` जो `2 * (width + height)` लौटाए।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Rectangle(4, 5).area` का मान `20` होना चाहिए

```dart
  test('test1', () {
    final r = Rectangle(4, 5);
    expect(r.area, 20, reason: '--err-t1--');
  });
```

`Rectangle(4, 5).perimeter` का मान `18` होना चाहिए

```dart
  test('test2', () {
    final r = Rectangle(4, 5);
    expect(r.perimeter, 18, reason: '--err-t2--');
  });
```

`Rectangle(3, 3).area` का मान `9` होना चाहिए

```dart
  test('test3', () {
    final r = Rectangle(3, 3);
    expect(r.area, 9, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.height);

  int get area => width * height;
  int get perimeter => 2 * (width + height);
}
```
