---
language: dart
exerciseType: 1
---

# --instructions--

एक `abstract` क्लास `Shape` बनाएं जिसमें एक एब्स्ट्रैक्ट मेथड `double area()` हो। फिर एक क्लास `Circle` बनाएं जो `Shape` को एक्सटेंड करे, जिसमें एक `double` फ़ील्ड `radius`, एक कंस्ट्रक्टर हो और `area()` को ओवरराइड करके `3.14 * radius * radius` लौटाए।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Circle(5).area()` का मान `78.5` होना चाहिए

```dart
  test('test1', () {
    final c = Circle(5);
    expect(c.area(), 78.5, reason: '--err-t1--');
  });
```

`Circle(10).area()` का मान `314.0` होना चाहिए

```dart
  test('test2', () {
    final c = Circle(10);
    expect(c.area(), 314.0, reason: '--err-t2--');
  });
```

`Circle`, `Shape` का एक इंस्टेंस होना चाहिए

```dart
  test('test3', () {
    final c = Circle(3);
    expect(c is Shape, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
abstract class Shape {
  double area();
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() {
    return 3.14 * radius * radius;
  }
}
```
