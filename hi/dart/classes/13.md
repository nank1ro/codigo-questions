---
language: dart
exerciseType: 1
---

# --instructions--

`String` फ़ील्ड `name` और एक कंस्ट्रक्टर वाली पैरेंट क्लास `Animal` बनाएं। फिर एक चाइल्ड क्लास `Dog` बनाएं जो `Animal` को एक्सटेंड करे। `Dog` में एक अतिरिक्त `String` फ़ील्ड `breed` होना चाहिए और एक कंस्ट्रक्टर जो `name` को `Animal` को पास करने के लिए `super` का उपयोग करे।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Dog('Rex', 'Labrador').name` का मान `'Rex'` होना चाहिए

```dart
  test('test1', () {
    final dog = Dog('Rex', 'Labrador');
    expect(dog.name, 'Rex', reason: '--err-t1--');
  });
```

`Dog('Rex', 'Labrador').breed` का मान `'Labrador'` होना चाहिए

```dart
  test('test2', () {
    final dog = Dog('Rex', 'Labrador');
    expect(dog.breed, 'Labrador', reason: '--err-t2--');
  });
```

`Dog`, `Animal` का एक इंस्टेंस होना चाहिए

```dart
  test('test3', () {
    final dog = Dog('Buddy', 'Poodle');
    expect(dog is Animal, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Dog extends Animal {
  String breed;
  Dog(String name, this.breed) : super(name);
}
```
