---
language: dart
exerciseType: 2
---

# --description--

एक **क्लास** ऑब्जेक्ट बनाने का एक खाका है। यह संबंधित डेटा (फ़ील्ड) और व्यवहार (मेथड) को एक साथ समूहित करती है।

Dart में, आप `class` कीवर्ड के बाद क्लास का नाम (परंपरा के अनुसार, PascalCase) लिखकर क्लास घोषित करते हैं:

```dart
class Car {
  String brand = 'Toyota';
}
```

आप इस प्रकार क्लास का एक **इंस्टेंस** (ऑब्जेक्ट) बनाते हैं:

```dart
final car = Car();
print(car.brand); // Toyota
```

# --instructions--

कोड पूरा करें जिसमें `Person` नाम की एक क्लास घोषित की जाए जिसमें `String` फ़ील्ड `name` को `'Alice'` से आरंभ किया गया हो, फिर एक इंस्टेंस बनाएं और उसका `name` प्रिंट करें।

# --seed--

```dart
[/] Person {
  String [/] = 'Alice';
}

void main() {
  final p = [/]();
  print(p.[/]);
}
```

# --answers--

- class
- name
- Person
- brand
- age
- void
- p

# --solutions--

```dart
class Person {
  String name = 'Alice';
}

void main() {
  final p = Person();
  print(p.name);
}
```

# --output--

Alice
