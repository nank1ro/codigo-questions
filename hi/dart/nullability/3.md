---
language: dart
exerciseType: 2
---

# --description--

`null` पर मेथड कॉल करना या प्रॉपर्टी पढ़ना क्रैश कर देगा, इसलिए Dart आपको नलेबल मान पर सामान्य डॉट के साथ ऐसा नहीं करने देता:

```dart
String? text;
print(text.length); // error: the property 'length' can't be unconditionally accessed
```

**नल-अवेयर एक्सेस** ऑपरेटर `?.` इसे हल करता है: अगर मान `null` है तो पूरा एक्सप्रेशन `null` होता है और आगे कुछ भी मूल्यांकित नहीं होता, वरना यह सामान्य `.` की तरह काम करता है:

```dart
String? text = 'Dart';
print(text?.length); // 4

text = null;
print(text?.length); // null
```

चूँकि परिणाम `null` हो सकता है, इसका टाइप नलेबल है: `text?.length` एक `int?` है, `int` नहीं।

# --instructions--

फंक्शन `lengthOf` को पूरा करें ताकि यह एक नलेबल `String` स्वीकार करे और उसकी लंबाई लौटाए, या टेक्स्ट अनुपस्थित होने पर `null` लौटाए। प्रोग्राम को पहले `4` और फिर `null` प्रिंट करना चाहिए।

# --seed--

```dart
int? lengthOf([/] text) {
  return text[/]length;
}

void main() {
  print(lengthOf('Dart'));
  print(lengthOf(null));
}
```

# --answers--

- String?
- ?.
- String
- .
- !.
- ??

# --solutions--

```dart
int? lengthOf(String? text) {
  return text?.length;
}

void main() {
  print(lengthOf('Dart'));
  print(lengthOf(null));
}
```

# --output--

4
null
