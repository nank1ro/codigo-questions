---
language: dart
exerciseType: 2
---

# --description--

**if-null असाइनमेंट** ऑपरेटर `??=` किसी वेरिएबल को मान **केवल तभी** असाइन करता है जब वह वेरिएबल इस समय `null` हो; वरना उसे वैसा ही छोड़ देता है:

```dart
int? retries;
retries ??= 3;
print(retries); // 3

retries ??= 10;
print(retries); // 3, it already had a value
```

यह map की एंट्रियों पर भी काम करता है, जो नलेबल होती हैं क्योंकि कोई की मौजूद न भी हो सकती है:

```dart
var stock = {'apple': 4};
stock['pear'] ??= 1;  // added
stock['apple'] ??= 9; // ignored
print(stock); // {apple: 4, pear: 1}
```

# --instructions--

कोड पूरा करें ताकि सहेजी गई थीम और मौजूदा फ़ॉन्ट साइज़ बने रहें जबकि अनुपस्थित मार्जिन जुड़ जाए। प्रोग्राम को पहले `dark` और फिर `{font: 12, margin: 8}` प्रिंट करना चाहिए।

# --seed--

```dart
String? savedTheme() => 'dark';

void main() {
  String? theme = savedTheme();
  var sizes = {'font': 12};
  theme [/] 'light';
  sizes['font'] [/] 14;
  sizes['margin'] ??= 8;
  print(theme);
  print(sizes);
}
```

# --answers--

- ??=
- ??=
- =
- ?=
- =??
- :=

# --solutions--

```dart
String? savedTheme() => 'dark';

void main() {
  String? theme = savedTheme();
  var sizes = {'font': 12};
  theme ??= 'light';
  sizes['font'] ??= 14;
  sizes['margin'] ??= 8;
  print(theme);
  print(sizes);
}
```

# --output--

dark
{font: 12, margin: 8}
