---
language: dart
exerciseType: 1
---

# --description--

कभी-कभी **आप** जानते हैं कि किसी बिंदु पर नलेबल मान `null` नहीं है, भले ही कंपाइलर यह न बता सके। **नल असर्शन** ऑपरेटर `!` यह वादा करते हुए कि मान मौजूद है, `String?` को `String` में बदल देता है:

```dart
String? text = 'Dart';
String sure = text!;
print(sure.length); // 4
```

सावधान रहें: `!` जाँच को कंपाइल समय से रन टाइम पर खिसका देता है। अगर मान सचमुच `null` **हुआ**, तो प्रोग्राम एरर फेंकता है और रुक जाता है:

```dart
String? text;
print(text!.length); // Null check operator used on a null value
```

`!` का उपयोग कम से कम करें, और केवल तब जब वहाँ `null` होना वैसे भी एक बग होता।

# --instructions--

एक फंक्शन `shout` लिखें जो नलेबल `String` पैरामीटर `text` लेता है और उसे बड़े अक्षरों में `!` के साथ लौटाता है। `!` ऑपरेटर का उपयोग करें, ताकि `text` के `null` होने पर फंक्शन एरर फेंके।

उदाहरण:
```dart
print(shout('hello')); // HELLO!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shout('hello')` `'HELLO!'` होना चाहिए

```dart
  test('test1', () {
    expect(shout('hello'), 'HELLO!', reason: '--err-t1--');
  });
```

`shout('Dart')` `'DART!'` होना चाहिए

```dart
  test('test2', () {
    expect(shout('Dart'), 'DART!', reason: '--err-t2--');
  });
```

`shout(null)` को एरर फेंकना चाहिए

```dart
  test('test3', () {
    expect(() => shout(null), throwsA(anything), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String shout(String? text) {
  return '${text!.toUpperCase()}!';
}
```
