---
language: dart
exerciseType: 1
---

# --description--

आपका अपना कोड उसी तरह फेंकता है जिस तरह लाइब्रेरी करती है। `Exception('message')` एक सादा अपवाद बनाता है जो एक छोटी व्याख्या ले जाता है, और `throw` उसे रवाना कर देता है:

```dart
if (amount > balance) {
  throw Exception('insufficient funds');
}
```

संदेश खोता नहीं है: `toString()` शब्द `Exception`, एक कोलन और संदेश को साथ जोड़ देता है, जो ठीक वही है जो unhandled-exception रिपोर्ट प्रिंट करती है।

```dart
print(Exception('insufficient funds')); // Exception: insufficient funds
```

फेंकना `-1` जैसी बनाई-गई कोई मान लौटाने से बेहतर है: कॉलर उसे देखना भूल नहीं सकता, और कारण उसके साथ ही यात्रा करता है।

# --instructions--

एक फंक्शन `withdraw` लिखें जो दो `int` मान `balance` और `amount` लेता है और बची हुई बैलेंस लौटाता है।

जब `amount`, `balance` से बड़ा हो, तो इसके बजाय `Exception('insufficient funds')` फेंकें।

उदाहरण:
```dart
print(withdraw(100, 30)); // 70
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`withdraw(100, 30)` को `70` लौटाना चाहिए

```dart
  test('test1', () {
    expect(withdraw(100, 30), 70, reason: '--err-t1--');
    expect(withdraw(50, 50), 0, reason: '--err-t1--');
  });
```

`withdraw(100, 300)` को एक `Exception` फेंकना चाहिए

```dart
  test('test2', () {
    expect(() => withdraw(100, 300), throwsA(isA<Exception>()), reason: '--err-t2--');
  });
```

फेंका गया अपवाद `Exception: insufficient funds` के रूप में प्रिंट होना चाहिए

```dart
  test('test3', () {
    try {
      withdraw(0, 1);
      fail('--err-t3--');
    } catch (e) {
      expect(e.toString(), 'Exception: insufficient funds', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int withdraw(int balance, int amount) {
  if (amount > balance) {
    throw Exception('insufficient funds');
  }
  return balance - amount;
}
```
