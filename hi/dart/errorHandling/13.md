---
language: dart
exerciseType: 1
---

# --description--

`firstWhere` जाँच से मेल खाने वाला पहला तत्व लौटाता है। जब कुछ मेल न खाए तो लौटाने को कोई तत्व नहीं होता, इसलिए वह एक `StateError` फेंकता है:

```dart
final words = ['a', 'fg'];
print(words.firstWhere((w) => w.length > 3)); // Bad state: No element
```

`int.tryParse` की तरह, लाइब्रेरी एक रास्ता भी देती है। नामित पैरामीटर `orElse` एक फंक्शन लेता है जो वह मान बनाता है जो तब उपयोग हो जब कुछ मेल न खाए:

```dart
print(words.firstWhere((w) => w.length > 3, orElse: () => 'none')); // none
```

चुनाव वही है जो पहले था: `orElse` जब "कुछ मेल नहीं मिला" एक साधारण परिणाम हो, और बिना `orElse` वाला कॉल जब उसका मतलब हो कि डेटा खराब है और उसके बारे में किसी को पता होना चाहिए।

# --instructions--

दो फंक्शन लिखें जो एक `List<String>` `words` लेते हैं।

`firstLong` `firstWhere` का उपयोग करके तीन अक्षरों से लंबा पहला शब्द लौटाता है, या `'none'` जब कोई न हो, `orElse` के साथ।

`firstLongStrict` वही `firstWhere` के साथ करता है, पर `orElse` के बिना, इसलिए जब कोई न हो तो वह एक `StateError` फेंकता है।

उदाहरण:
```dart
print(firstLong(['a', 'bcde', 'fg'])); // bcde
print(firstLong(['a', 'fg'])); // none
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`firstLong(['a', 'bcde', 'fg'])` को `'bcde'` लौटाना चाहिए

```dart
  test('test1', () {
    expect(firstLong(['a', 'bcde', 'fg']), 'bcde', reason: '--err-t1--');
    expect(firstLong(['long', 'longer']), 'long', reason: '--err-t1--');
  });
```

`firstLong` को `'none'` लौटाना चाहिए जब कोई शब्द तीन अक्षरों से लंबा न हो

```dart
  test('test2', () {
    expect(firstLong(['a', 'fg']), 'none', reason: '--err-t2--');
    expect(firstLong(<String>[]), 'none', reason: '--err-t2--');
    expect(firstLong(['abc']), 'none', reason: '--err-t2--');
  });
```

`firstLongStrict` को शब्द लौटाना चाहिए जब वह हो और अन्यथा एक `StateError` फेंकना चाहिए

```dart
  test('test3', () {
    expect(firstLongStrict(['a', 'bcde']), 'bcde', reason: '--err-t3--');
    expect(() => firstLongStrict(['a', 'fg']), throwsStateError, reason: '--err-t3--');
    expect(() => firstLongStrict(<String>[]), throwsStateError, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String firstLong(List<String> words) {
  return words.firstWhere((w) => w.length > 3, orElse: () => 'none');
}

String firstLongStrict(List<String> words) {
  return words.firstWhere((w) => w.length > 3);
}
```
