---
language: dart
exerciseType: 4
---

# --description--

हैंडलरों के बाद एक तीसरा ब्लॉक आ सकता है। `finally` **हर हाल में** चलता है: `try` ब्लॉक के सामान्य रूप से पूरा होने के बाद, किसी हैंडलर के रिकवर करने के बाद, और तब भी जब कुछ भी मेल न खाए और विफलता अभी भी बाहर की ओर यात्रा कर रही हो।

```dart
try {
  return 'parsed ${int.parse(text)}';
} on FormatException {
  return 'failed';
} finally {
  print('done');
}
```

वह `return` के अपना मान लौटाने से पहले भी चलता है, इसीलिए ऊपर वाला संदेश कॉलर के परिणाम देखने से पहले प्रिंट होता है। इसीलिए `finally` उस काम की जगह बन जाता है जो किसी भी हालत में होना चाहिए, जैसे खोली हुई चीज़ बंद करना।

# --instructions--

एक फंक्शन `attempt` परिभाषित करने के लिए आइटम्स को क्रमित करें जो `'parsed '` के बाद संख्या लौटाता है, या `'failed'` जब टेक्स्ट संख्या न हो, और दोनों ही हालत में एक `finally` ब्लॉक में `done` प्रिंट करता है। फंक्शन को `main` से पहले परिभाषित करें। प्रोग्राम को यह प्रिंट करना चाहिए:

```
done
parsed 7
done
failed
```

# --answers--

```dart
String attempt(String text) {
```

```dart
  try {
    return 'parsed ${int.parse(text)}';
```

```dart
  } on FormatException {
    return 'failed';
```

```dart
  } finally {
    print('done');
  }
}
```

```dart
void main() {
```

```dart
  print(attempt('7'));
```

```dart
  print(attempt('x'));
```

```dart
}
```

# --solutions--

```dart
String attempt(String text) {
  try {
    return 'parsed ${int.parse(text)}';
  } on FormatException {
    return 'failed';
  } finally {
    print('done');
  }
}

void main() {
  print(attempt('7'));
  print(attempt('x'));
}
```

# --output--

done
parsed 7
done
failed
