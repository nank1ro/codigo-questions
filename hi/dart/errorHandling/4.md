---
language: dart
exerciseType: 1
---

# --description--

एक `try` ब्लॉक के बाद **कई** क्लॉज़ हो सकते हैं, हर एक किसी अलग विफलता से रिकवर करता हुआ। Dart ऊपर से नीचे तक फेंकी गई वस्तु की उनसे तुलना करता है और जो **पहला** मेल खाता है उसे चलाता है:

```dart
try {
  return names[int.parse(text)];
} on FormatException {
  return 'not a number';
} on RangeError {
  return 'out of range';
} catch (e) {
  return 'unknown problem';
}
```

किसी सूची को ऐसे इंडेक्स से पढ़ना जो मौजूद नहीं है एक **`RangeError`** फेंकता है, इसलिए उस एक पंक्ति की दोनों विफलताओं को अलग-अलग उत्तर मिलते हैं।

चूँकि पहला मेल जीत जाता है, क्रम मायने रखता है: किसी सामान्य टाइप वाला क्लॉज़ किसी अधिक विशिष्ट क्लॉज़ के ऊपर रखा जाए तो हमेशा जीत जाएगा, और विशिष्ट क्लॉज़ अप्राप्य हो जाएगा। विशिष्ट क्लॉज़ पहले लिखें, और अंत में एक खाली `catch` रखें यदि आप सुरक्षा जाल चाहते हैं।

ऊपर वाला `on RangeError` क्लॉज़ यहाँ केवल यह दिखाने के लिए है कि कई क्लॉज़ कैसे क्रमित होते हैं। `RangeError` प्रोग्राम के नियंत्रण से बाहर किसी स्थिति के बजाय कोड में गलती का संकेत देता है, और आगे एक अभ्यास समझाता है कि ऐसी विफलता को पकड़ने के बजाय रोका क्यों जाना चाहिए।

# --instructions--

एक फंक्शन `lookup` लिखें जो एक `List<int>` `scores` और एक `String` `text` लेता है, और `'score '` के बाद `scores` का वह तत्व लौटाता है जिस स्थिति का `text` वर्णन करता है।

`'not a number'` लौटाएँ जब `text` कोई पूर्ण संख्या न हो, `'out of range'` जब स्थिति मौजूद न हो, और `'unknown problem'` बाकी हर स्थिति में।

उदाहरण:
```dart
print(lookup([10, 20, 30], '1')); // score 20
print(lookup([10, 20, 30], 'two')); // not a number
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`lookup([10, 20, 30], '1')` को `'score 20'` लौटाना चाहिए

```dart
  test('test1', () {
    expect(lookup([10, 20, 30], '1'), 'score 20', reason: '--err-t1--');
    expect(lookup([10, 20, 30], '0'), 'score 10', reason: '--err-t1--');
  });
```

`lookup([10, 20, 30], 'two')` को `'not a number'` लौटाना चाहिए

```dart
  test('test2', () {
    expect(lookup([10, 20, 30], 'two'), 'not a number', reason: '--err-t2--');
    expect(lookup([10, 20, 30], ''), 'not a number', reason: '--err-t2--');
  });
```

`lookup([10, 20, 30], '7')` को `'out of range'` लौटाना चाहिए

```dart
  test('test3', () {
    expect(lookup([10, 20, 30], '7'), 'out of range', reason: '--err-t3--');
    expect(lookup([10, 20, 30], '-1'), 'out of range', reason: '--err-t3--');
    expect(lookup(<int>[], '0'), 'out of range', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String lookup(List<int> scores, String text) {
  try {
    return 'score ${scores[int.parse(text)]}';
  } on FormatException {
    return 'not a number';
  } on RangeError {
    return 'out of range';
  } catch (e) {
    return 'unknown problem';
  }
}
```
