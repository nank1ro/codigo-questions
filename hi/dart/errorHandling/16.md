---
language: dart
exerciseType: 1
---

# --description--

आख़िरी टुकड़ा है जानबूझकर एक `Error` फेंकना। जो फंक्शन बताता है कि वह क्या स्वीकार करता है, उसे बाकी सब कुछ ऊँची आवाज़ में ठुकरा देना चाहिए, और इसके लिए `ArgumentError` वही वस्तु है:

```dart
int setVolume(int level) {
  if (level < 0 || level > 100) {
    throw ArgumentError('level must be between 0 and 100');
  }
  return level;
}
```

संदेश `e.message` के रूप में मिलता है, और `toString()` उसके आगे `Invalid argument(s): ` प्रिंट करता है।

यह पहले वाले नियम के विपरीत नहीं है। एक `ArgumentError` फेंकना सही है, एक को पकड़ना नहीं: वह *कॉलर के* लेखक को बताता है कि कॉल स्वयं गलत है, और समाधान है कॉल से पहले की एक जाँच, उसके चारों ओर कोई हैंडलर नहीं।

# --instructions--

एक फंक्शन `checkAge` लिखें जो एक `int` `age` लेता है और उसे लौटाता है, पर `ArgumentError('age must be between 0 and 120')` फेंकता है जब उम्र `0` से कम या `120` से अधिक हो।

उदाहरण:
```dart
print(checkAge(30)); // 30
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`checkAge(30)` को `30` लौटाना चाहिए, और सीमा के दोनों सिरे स्वीकार होने चाहिए

```dart
  test('test1', () {
    expect(checkAge(30), 30, reason: '--err-t1--');
    expect(checkAge(0), 0, reason: '--err-t1--');
    expect(checkAge(120), 120, reason: '--err-t1--');
  });
```

`checkAge(-1)` और `checkAge(121)` को एक `ArgumentError` फेंकना चाहिए

```dart
  test('test2', () {
    expect(() => checkAge(-1), throwsArgumentError, reason: '--err-t2--');
    expect(() => checkAge(121), throwsArgumentError, reason: '--err-t2--');
  });
```

फेंकी गई त्रुटि में संदेश `'age must be between 0 and 120'` होना चाहिए

```dart
  test('test3', () {
    try {
      checkAge(200);
      fail('--err-t3--');
    } on ArgumentError catch (e) {
      expect(e.message, 'age must be between 0 and 120', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int checkAge(int age) {
  if (age < 0 || age > 120) {
    throw ArgumentError('age must be between 0 and 120');
  }
  return age;
}
```
