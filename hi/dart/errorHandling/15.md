---
language: dart
exerciseType: 1
---

# --description--

`try` ब्लॉक कहाँ बैठता है, यह तय करता है कि एक अकेली विफलता कितना काम बर्बाद कर देती है। लूप के चारों ओर होने पर पहला खराब तत्व पूरे बैच का अंत कर देता है; लूप के **अंदर** केवल वही तत्व खोता है और बाकी अब भी प्रोसेस होते हैं:

```dart
for (final text in texts) {
  try {
    total += int.parse(text);
  } on FormatException {
    continue;
  }
}
```

यही रोज़मर्रा का ढाँचा है किसी फ़ाइल को इंपोर्ट करने, सेटिंग्स की सूची पढ़ने या संदेशों की कतार संभालने के लिए: एक खराब पंक्ति से सही पंक्तियों को नहीं फेंकना चाहिए। नियम वही रहता है जो पहले था — `try` ब्लॉक को उस कथन के चारों ओर रखें जो विफल हो सकता है, और उससे बड़ा नहीं।

# --instructions--

एक फंक्शन `sumOf` लिखें जो एक `List<String>` `texts` लेता है और उनमें से जो पूर्ण संख्या दर्शाते हैं उनका योग लौटाता है, बाकी को नज़रअंदाज़ करते हुए।

लूप के अंदर `int.parse` को `try`/`catch` के साथ उपयोग करें, ताकि एक अपठनीय प्रविष्टि बाकी को न रोके।

उदाहरण:
```dart
print(sumOf(['1', 'x', '3'])); // 4
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sumOf(['1', 'x', '3'])` को `4` लौटाना चाहिए

```dart
  test('test1', () {
    expect(sumOf(['1', 'x', '3']), 4, reason: '--err-t1--');
  });
```

`sumOf(['10', '20', '30'])` को `60` लौटाना चाहिए

```dart
  test('test2', () {
    expect(sumOf(['10', '20', '30']), 60, reason: '--err-t2--');
    expect(sumOf(['-5', '5']), 0, reason: '--err-t2--');
  });
```

बिना पढ़ने योग्य संख्या वाली सूची और खाली सूची, दोनों को `0` देना चाहिए

```dart
  test('test3', () {
    expect(sumOf(['x', 'y', '']), 0, reason: '--err-t3--');
    expect(sumOf(<String>[]), 0, reason: '--err-t3--');
    expect(sumOf(['x', '7']), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sumOf(List<String> texts) {
  var total = 0;
  for (final text in texts) {
    try {
      total += int.parse(text);
    } on FormatException {
      continue;
    }
  }
  return total;
}
```
