---
language: dart
exerciseType: 1
---

# --description--

कोई future **त्रुटि** के साथ भी पूरा हो सकता है। जब कोई `async` फ़ंक्शन थ्रो करता है, तो अपवाद तुरंत बाहर नहीं निकलता: वह लौटाए गए future की त्रुटि बन जाता है। जो उस future का `await` करता है उसे त्रुटि `await` पर थ्रो होती दिखती है, इसलिए उसे सामान्य `try`/`catch` से संभाला जा सकता है:

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s); // throws FormatException for 'abc'
}

Future<int> orZero(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return 0;
  }
}
```

`try` के अंदर का `await` ज़रूरी है: `return parseLater(s);` future को **बिना प्रतीक्षा किए** कॉलर को सौंप देता, इसलिए त्रुटि तब आती जब `try` ब्लॉक पहले ही समाप्त हो चुका होता और `catch` कभी नहीं चलता।

# --instructions--

फ़ंक्शन `parseLater(s)` एक `Future<int>` लौटाता है जो `s` में लिखी संख्या के साथ पूरा होता है, या त्रुटि के साथ यदि `s` कोई संख्या नहीं है। एक `async` फ़ंक्शन `safeParse` लिखो जो एक `String` `s` लेता है और एक `Future<int>` लौटाता है जो पार्स की गई संख्या के साथ पूरा होता है, या `-1` के साथ यदि `parseLater` विफल हो।

उदाहरण:
```dart
print(await safeParse('12'));  // 12
print(await safeParse('abc')); // -1
```

# --before-seed--

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s);
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeParse('12')` को `12` के साथ पूरा होना चाहिए

```dart
  test('test1', () async {
    expect(await safeParse('12'), 12, reason: '--err-t1--');
  });
```

`safeParse('abc')` को थ्रो करने के बजाय `-1` के साथ पूरा होना चाहिए

```dart
  test('test2', () async {
    expect(await safeParse('abc'), -1, reason: '--err-t2--');
  });
```

`safeParse('-7')` को `-7` के साथ और `safeParse('')` को `-1` के साथ पूरा होना चाहिए

```dart
  test('test3', () async {
    expect(await safeParse('-7'), -7, reason: '--err-t3--');
    expect(await safeParse(''), -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> safeParse(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return -1;
  }
}
```
