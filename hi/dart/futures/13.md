---
language: dart
exerciseType: 1
---

# --description--

दो future को एक ही समय पर चलाने के लिए तुम्हें `Future.wait` की ज़रूरत नहीं है। कोई `async` फ़ंक्शन **बुलाए** जाते ही अपने पहले `await` तक चलने लगता है; जो future तुम्हें वापस मिलता है वह पहले से चल रहा काम है। इसलिए तरकीब यह है: **पहले बुलाओ, बाद में await करो**:

```dart
Future<int> pages() => Future.delayed(const Duration(milliseconds: 20), () => 300);
Future<int> words() => Future.delayed(const Duration(milliseconds: 20), () => 90000);

Future<double> average() async {
  final p = pages();          // both timers start now
  final w = words();
  return await w / await p;   // waits once, about 20 ms in total
}
```

`return await words() / await pages();` से तुलना करो, जहाँ `pages()` तभी बुलाया जाता है जब `words()` पूरा हो चुका हो: परिणाम वही, समय दोगुना। जब भी दूसरी कॉल को पहली के परिणाम की ज़रूरत न हो, समवर्ती रूप को प्राथमिकता दो।

# --instructions--

फ़ंक्शन `slow(value)` एक `Future<int>` लौटाता है जो थोड़ी देरी के बाद `value` के साथ पूरा होता है, और यह बुलाए जाने पर `start <value>` और पूरा होने पर `end <value>` सूची `calls` में दर्ज करता है। एक `async` फ़ंक्शन `addConcurrently` लिखो जो दो `int` `a` और `b` लेता है, `slow(a)` और `slow(b)` को इस तरह बुलाता है कि दोनों **एक ही समय पर** चलें, और एक `Future<int>` लौटाता है जो दोनों परिणामों के योग के साथ पूरा होता है।

उदाहरण:
```dart
print(await addConcurrently(2, 3)); // 5
print(calls); // [start 2, start 3, end 2, end 3]
```

# --before-seed--

```dart
final calls = <String>[];

Future<int> slow(int value) async {
  calls.add('start $value');
  await Future.delayed(const Duration(milliseconds: 5));
  calls.add('end $value');
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`addConcurrently(2, 3)` को `5` के साथ पूरा होना चाहिए

```dart
  test('test1', () async {
    calls.clear();
    expect(await addConcurrently(2, 3), 5, reason: '--err-t1--');
  });
```

`addConcurrently(2, 3)` को किसी एक के पूरा होने से पहले `slow(2)` और `slow(3)` को बुलाना चाहिए, इसलिए `calls` `[start 2, start 3, end 2, end 3]` होना चाहिए

```dart
  test('test2', () async {
    calls.clear();
    await addConcurrently(2, 3);
    expect(calls, ['start 2', 'start 3', 'end 2', 'end 3'], reason: '--err-t2--');
  });
```

`addConcurrently(10, -4)` को `6` के साथ पूरा होना चाहिए और किसी एक के पूरा होने से पहले दोनों कॉल शुरू हो जानी चाहिए

```dart
  test('test3', () async {
    calls.clear();
    expect(await addConcurrently(10, -4), 6, reason: '--err-t3--');
    expect(calls, ['start 10', 'start -4', 'end 10', 'end -4'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> addConcurrently(int a, int b) async {
  final first = slow(a);
  final second = slow(b);
  return await first + await second;
}
```
