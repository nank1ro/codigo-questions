---
language: dart
exerciseType: 1
---

# --description--

किसी `async` फ़ंक्शन के अंदर, `await` तुम्हें एसिंक्रोनस चरणों को सामान्य क्रमिक कोड की तरह लिखने देता है। हर `await` अपने future की प्रतीक्षा करता है, और अगली पंक्ति तभी चलती है जब मान आ चुका हो:

```dart
Future<int> width() => Future.delayed(const Duration(milliseconds: 5), () => 4);
Future<int> height() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<int> area() async {
  final w = await width();
  final h = await height();
  return w * h; // 12
}
```

`await` को सीधे किसी अभिव्यक्ति के भीतर भी इस्तेमाल किया जा सकता है: `return await width() * await height();` वही परिणाम देता है।

# --instructions--

फ़ंक्शन `price(item)` `item` की कीमत के साथ एक `Future<int>` लौटाता है। `await` का उपयोग करके `'apple'` की कीमत और `'milk'` की कीमत लो, और उनका योग `total` में रखो।

# --before-seed--

```dart
const prices = {'apple': 3, 'bread': 5, 'milk': 2};

Future<int> price(String item) {
  return Future.delayed(const Duration(milliseconds: 5), () => prices[item]!);
}

Future<dynamic> result() async {
```

# --seed--

```dart
var total = 0;
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` `5` होना चाहिए

```dart
  test('test1', () async {
    expect(await result(), 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var total = 0;
total = await price('apple') + await price('milk');
```
