---
language: dart
exerciseType: 1
---

# --description--

किसी फ़ंक्शन को `async` चिह्नित करने से दो चीज़ें होती हैं: बॉडी के अंदर `await` की अनुमति मिलती है, और फ़ंक्शन **एक `Future` लौटाने** लगता है। तुम `return` से जो देते हो वही मान बनता है जिसके साथ future पूरा होता है, इसलिए घोषित रिटर्न प्रकार `Future<T>` होता है, भले ही बॉडी सादा `T` लौटाती हो:

```dart
Future<String> label(int n) async {
  return 'item $n';
}

Future<void> main() async {
  print(await label(3)); // item 3
}
```

यहाँ तुम्हें `Future.value` की ज़रूरत नहीं है: `async` कीवर्ड लौटाए गए मान को तुम्हारे लिए लपेट देता है।

# --instructions--

एक `async` फ़ंक्शन `greet` लिखो जो एक `String` `name` लेता है और एक `Future<String>` लौटाता है जो `Hello, ` के बाद नाम और एक विस्मयादिबोधक चिह्न के साथ पूरा होता है।

उदाहरण:
```dart
print(await greet('Ada')); // Hello, Ada!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`greet('Ada')` को `'Hello, Ada!'` के साथ पूरा होना चाहिए

```dart
  test('test1', () async {
    expect(await greet('Ada'), 'Hello, Ada!', reason: '--err-t1--');
  });
```

`greet('Linus')` को `'Hello, Linus!'` के साथ पूरा होना चाहिए

```dart
  test('test2', () async {
    expect(await greet('Linus'), 'Hello, Linus!', reason: '--err-t2--');
  });
```

`greet` को एक `Future<String>` लौटाना चाहिए

```dart
  test('test3', () async {
    expect(greet('Bob'), isA<Future<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<String> greet(String name) async {
  return 'Hello, $name!';
}
```
