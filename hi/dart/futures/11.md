---
language: dart
exerciseType: 1
---

# --description--

जब कई future एक-दूसरे पर निर्भर नहीं होते, तो उन सबको **`Future.wait`** को सौंप दो: यह एक `List<Future<T>>` लेता है, उन्हें एक ही समय पर चलने देता है, और एक अकेला `Future<List<T>>` लौटाता है जो तब पूरा होता है जब **सभी** समाप्त हो जाएँ। परिणाम इनपुट सूची का क्रम बनाए रखते हैं, चाहे कोई भी future पहले समाप्त हुआ हो:

```dart
Future<String> slow() => Future.delayed(const Duration(milliseconds: 20), () => 'slow');
Future<String> fast() => Future.delayed(const Duration(milliseconds: 5), () => 'fast');

final results = await Future.wait([slow(), fast()]);
print(results); // [slow, fast]
```

`await slow(); await fast();` में `25` मिलीसेकंड लगते हैं, क्योंकि `fast()` तभी बुलाया जाता है जब `slow()` पूरा हो चुका हो; `await Future.wait([slow(), fast()])` में लगभग `20` लगते हैं, यानी सबसे लंबे वाले की अवधि।

# --instructions--

एक `async` फ़ंक्शन `sumOfFutures` लिखो जो एक `List<Future<int>>` `futures` लेता है, `Future.wait` से उन सबकी प्रतीक्षा करता है, और एक `Future<int>` लौटाता है जो उनके मानों के योग के साथ पूरा होता है। खाली सूची `0` देती है।

उदाहरण:
```dart
final futures = [Future.value(1), Future.delayed(const Duration(milliseconds: 5), () => 2)];
print(await sumOfFutures(futures)); // 3
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`1`, `2` और `3` के साथ पूरे होने वाले future पर `sumOfFutures` को `6` के साथ पूरा होना चाहिए

```dart
  test('test1', () async {
    final futures = [
      Future.value(1),
      Future.delayed(const Duration(milliseconds: 5), () => 2),
      Future.value(3),
    ];
    expect(await sumOfFutures(futures), 6, reason: '--err-t1--');
  });
```

`10` के साथ पूरे होने वाले एकल future पर `sumOfFutures` को `10` के साथ पूरा होना चाहिए

```dart
  test('test2', () async {
    expect(await sumOfFutures([Future.delayed(const Duration(milliseconds: 5), () => 10)]), 10, reason: '--err-t2--');
  });
```

खाली सूची पर `sumOfFutures` को `0` के साथ पूरा होना चाहिए और `sumOfFutures` को एक `Future<int>` लौटाना चाहिए

```dart
  test('test3', () async {
    expect(await sumOfFutures(<Future<int>>[]), 0, reason: '--err-t3--');
    expect(sumOfFutures([Future.value(4)]), isA<Future<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> sumOfFutures(List<Future<int>> futures) async {
  final values = await Future.wait(futures);
  var total = 0;
  for (final v in values) {
    total += v;
  }
  return total;
}
```
