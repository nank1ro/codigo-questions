---
language: dart
exerciseType: 4
---

# --description--

`Future.wait` "कई चीज़ें लोड करो, फिर आगे बढ़ो" के लिए औज़ार है। सामान्य ढाँचा यह है: future की सूची बनाओ, उस पर `await Future.wait` करो, फिर मिली हुई सूची का उपयोग करो:

```dart
Future<int> stock() => Future.delayed(const Duration(milliseconds: 10), () => 8);
Future<int> orders() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<void> main() async {
  final counts = await Future.wait([stock(), orders()]);
  print(counts); // [8, 3]
}
```

`orders()` पहले पूरा होता है, पर सूची फिर भी कॉल के क्रम का पालन करती है: पहले `stock()`, दूसरा `orders()`।

# --instructions--

आइटम क्रम में लगाओ ताकि एक फ़ंक्शन `one` परिभाषित हो जो `10` मिलीसेकंड बाद `1` के साथ पूरा हो, एक फ़ंक्शन `two` जो `5` मिलीसेकंड बाद `2` के साथ पूरा हो, और एक `async` `main` जो `loading` छापे, `Future.wait` से दोनों फ़ंक्शनों की प्रतीक्षा करके `values` में रखे, और `values` छापे। पहले `one`, फिर `two`, फिर `main` परिभाषित करो। प्रोग्राम को यह छापना चाहिए:

```
loading
[1, 2]
```

# --answers--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
```

```dart
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);
```

```dart
Future<void> main() async {
```

```dart
  print('loading');
```

```dart
  final values = await Future.wait([one(), two()]);
```

```dart
  print(values);
```

```dart
}
```

# --solutions--

```dart
Future<int> one() => Future.delayed(const Duration(milliseconds: 10), () => 1);
Future<int> two() => Future.delayed(const Duration(milliseconds: 5), () => 2);

Future<void> main() async {
  print('loading');
  final values = await Future.wait([one(), two()]);
  print(values);
}
```

# --output--

loading
[1, 2]
