---
language: dart
exerciseType: 1
---

# --description--

`Future.value` तुरंत पूरा हो जाता है। समय लेने वाले काम की नकल करने के लिए **`Future.delayed`** का उपयोग करो: यह एक `Duration` और एक फ़ंक्शन लेता है, उस अवधि तक प्रतीक्षा करता है, फिर फ़ंक्शन जो लौटाए उसके साथ पूरा होता है:

```dart
Future<String> slowHello() {
  return Future.delayed(const Duration(milliseconds: 10), () => 'hello');
}
```

`Duration` को `seconds`, `milliseconds` या `minutes` जैसे नामित पैरामीटर से बनाया जाता है। किसी `async` फ़ंक्शन के अंदर तुम केवल रुकने के लिए, बिना किसी मान के, अकेली देरी का भी await कर सकते हो:

```dart
Future<int> slowDouble(int n) async {
  await Future.delayed(const Duration(milliseconds: 10));
  return n * 2;
}
```

दोनों शैलियाँ आम हैं; दूसरी सामान्य क्रमिक कोड की तरह पढ़ी जाती है।

# --instructions--

एक फ़ंक्शन `slowSquare` लिखो जो एक `int` `n` लेता है और एक `Future<int>` लौटाता है जो `10` मिलीसेकंड की देरी के बाद `n * n` के साथ पूरा होता है।

उदाहरण:
```dart
print(await slowSquare(4)); // 16, after 10 ms
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`slowSquare(4)` को `16` के साथ पूरा होना चाहिए

```dart
  test('test1', () async {
    expect(await slowSquare(4), 16, reason: '--err-t1--');
  });
```

`slowSquare(-3)` को `9` के साथ और `slowSquare(0)` को `0` के साथ पूरा होना चाहिए

```dart
  test('test2', () async {
    expect(await slowSquare(-3), 9, reason: '--err-t2--');
    expect(await slowSquare(0), 0, reason: '--err-t2--');
  });
```

`slowSquare` को ऐसा `Future<int>` लौटाना चाहिए जिसे पूरा होने में कम से कम `10` मिलीसेकंड लगें

```dart
  test('test3', () async {
    final future = slowSquare(5);
    expect(future, isA<Future<int>>(), reason: '--err-t3--');
    final watch = Stopwatch()..start();
    await future;
    expect(watch.elapsedMilliseconds, greaterThanOrEqualTo(9), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> slowSquare(int n) {
  return Future.delayed(const Duration(milliseconds: 10), () => n * n);
}
```
