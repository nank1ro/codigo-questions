---
language: dart
exerciseType: 1
---

# --description--

चूँकि `await` future की त्रुटियों को सामान्य अपवादों में बदल देता है, `try`/`catch` के सभी सामान्य पैटर्न एसिंक्रोनस कोड पर भी लागू होते हैं, दोबारा कोशिश करने वाले लूप सहित। किसी `catch` ब्लॉक के अंदर **`rethrow`** उसी त्रुटि को फिर से थ्रो करता है, और इसी तरह तुम आख़िरी कोशिश के बाद हार मानते हो:

```dart
Future<String> onceThenGiveUp(Future<String> Function() task) async {
  try {
    return await task();
  } catch (e) {
    print('first attempt failed');
    rethrow; // the caller sees the original error
  }
}
```

`Future<String> Function() task` जैसा फ़ंक्शन-प्रकार का पैरामीटर स्वयं **फ़ंक्शन** पाता है, कोई future नहीं: `task()` की हर कॉल एक नई कोशिश शुरू करती है।

# --instructions--

एक `async` फ़ंक्शन `retry` लिखो जो एक `Future<int> Function()` `task` और एक `int` `attempts` लेता है, और एक `Future<int>` लौटाता है। `task()` को बुलाओ और उसके मान के साथ पूरा हो जाओ; यदि वह थ्रो करे, तो फिर कोशिश करो, `task` को कुल मिलाकर अधिकतम `attempts` बार बुलाते हुए। यदि आख़िरी कोशिश भी विफल हो, तो उसकी त्रुटि `rethrow` कर दो।

उदाहरण:
```dart
var count = 0;
Future<int> flaky() async {
  count++;
  if (count < 3) throw StateError('busy');
  return 7;
}
print(await retry(flaky, 5)); // 7, after 3 calls
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

हमेशा सफल होने वाले टास्क पर `retry` को एक ही कॉल के बाद उसके मान के साथ पूरा होना चाहिए

```dart
  test('test1', () async {
    var count = 0;
    Future<int> fine() async {
      count++;
      return 42;
    }
    expect(await retry(fine, 3), 42, reason: '--err-t1--');
    expect(count, 1, reason: '--err-t1--');
  });
```

दो बार विफल होकर फिर `7` लौटाने वाले टास्क पर `retry` को `3` कॉल के बाद `7` के साथ पूरा होना चाहिए

```dart
  test('test2', () async {
    var count = 0;
    Future<int> flaky() async {
      count++;
      if (count < 3) throw StateError('busy');
      return 7;
    }
    expect(await retry(flaky, 5), 7, reason: '--err-t2--');
    expect(count, 3, reason: '--err-t2--');
  });
```

हमेशा विफल होने वाले टास्क पर `retry` को उसे ठीक `attempts` बार बुलाना चाहिए और फिर उसी त्रुटि के साथ विफल होना चाहिए

```dart
  test('test3', () async {
    var count = 0;
    Future<int> broken() async {
      count++;
      throw FormatException('bad');
    }
    await expectLater(retry(broken, 2), throwsA(isA<FormatException>()), reason: '--err-t3--');
    expect(count, 2, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> retry(Future<int> Function() task, int attempts) async {
  var failures = 0;
  while (true) {
    try {
      return await task();
    } catch (e) {
      failures++;
      if (failures == attempts) rethrow;
    }
  }
}
```
