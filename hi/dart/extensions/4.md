---
language: dart
exerciseType: 4
---

# --description--

`on` के बाद का टाइप एक **पैरामीटराइज़्ड** टाइप हो सकता है, जैसे `List<int>`। तब एक्सटेंशन केवल उस एलिमेंट टाइप की लिस्टों पर लागू होता है: `[1, 2].total()` काम करता है, `['a', 'b'].total()` कंपाइल नहीं होता।

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}
```

एक्सटेंशन के अंदर, `this` वही लिस्ट है, इसलिए आप उस पर लूप कर सकते हैं, उसे इंडेक्स कर सकते हैं, या हमेशा की तरह `length` कॉल कर सकते हैं।

# --instructions--

आइटमों को क्रमबद्ध करें ताकि `List<int>` पर एक मेथड `total` के साथ एक एक्सटेंशन `Totals` घोषित हो, जो एलिमेंटों को एक `for` लूप से जोड़ती है, और फिर एक `main` हो जो `[3, 4, 5]` का कुल योग प्रिंट करे। एक्सटेंशन को `main` से पहले घोषित करें। प्रोग्राम को प्रिंट करना चाहिए:

```
12
```

# --answers--

```dart
extension Totals on List<int> {
```

```dart
  int total() {
```

```dart
    var sum = 0;
```

```dart
    for (final n in this) {
```

```dart
      sum += n;
```

```dart
    }
```

```dart
    return sum;
```

```dart
  }
```

```dart
}
```

```dart
void main() {
```

```dart
  print([3, 4, 5].total());
```

```dart
}
```

# --solutions--

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}

void main() {
  print([3, 4, 5].total());
}
```

# --output--

12
