---
language: dart
exerciseType: 1
---

# --description--

`List<int>` पर एक एक्सटेंशन को `List<String>` पर उपयोग नहीं किया जा सकता। एक ऐसा एक्सटेंशन लिखने के लिए जो **हर** एलिमेंट टाइप के लिए काम करे, एक्सटेंशन को एक **टाइप पैरामीटर** दें, जो उसके नाम के बाद एंगल ब्रैकेटों के बीच लिखा जाता है, और उसे `on` टाइप में उपयोग करें:

```dart
extension Firsts<T> on List<T> {
  T get firstOrLast => length > 1 ? this[0] : this[length - 1];
}
```

`T`, "एलिमेंट टाइप जो भी हो" के लिए एक प्लेसहोल्डर है: `List<int>` पर यह `int` बन जाता है, `List<String>` पर यह `String` बन जाता है, इसलिए ऊपर का गेटर तदनुसार एक `int` या एक `String` लौटाता है। कंपाइलर हर कॉल पर `T` को आपके लिए भर देता है।

```dart
print([7, 8, 9].firstOrLast); // 7
print(['a', 'b'].firstOrLast); // a
```

# --instructions--

`List<T>` पर टाइप पैरामीटर `T` के साथ एक एक्सटेंशन `Seconds` लिखें, जिसमें `T?` टाइप का एक गेटर `secondOrNull` हो, जो इंडेक्स `1` पर एलिमेंट लौटाता है, या `null` जब लिस्ट में दो से कम एलिमेंट हों।

उदाहरण:
```dart
print([10, 20, 30].secondOrNull); // 20
print(['solo'].secondOrNull);     // null
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`[10, 20, 30].secondOrNull` `20` होना चाहिए

```dart
  test('test1', () {
    expect([10, 20, 30].secondOrNull, 20, reason: '--err-t1--');
  });
```

`['a', 'b'].secondOrNull` `'b'` होना चाहिए

```dart
  test('test2', () {
    expect(['a', 'b'].secondOrNull, 'b', reason: '--err-t2--');
  });
```

`['solo'].secondOrNull` और `<int>[].secondOrNull` `null` होना चाहिए

```dart
  test('test3', () {
    expect(['solo'].secondOrNull, null, reason: '--err-t3--');
    expect(<int>[].secondOrNull, null, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Seconds<T> on List<T> {
  T? get secondOrNull => length > 1 ? this[1] : null;
}
```
