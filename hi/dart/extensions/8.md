---
language: dart
exerciseType: 1
---

# --description--

एक एक्सटेंशन **स्टैटिक** सदस्य घोषित कर सकता है। क्लास की तरह, वे स्वयं एक्सटेंशन के होते हैं, किसी मान के नहीं, और उन्हें **एक्सटेंशन के नाम** के माध्यम से एक्सेस किया जाता है, उस टाइप के माध्यम से नहीं जिसे वह एक्सटेंड करता है:

```dart
extension Temperatures on double {
  static const double boiling = 100.0;

  static bool isBoiling(double celsius) => celsius >= boiling;
}

void main() {
  print(Temperatures.boiling);         // 100.0
  print(Temperatures.isBoiling(37.5)); // false
  print(double.boiling);               // error: 'boiling' isn't defined for 'double'
}
```

स्टैटिक सदस्य एक्सटेंडेड टाइप से संबंधित कॉन्स्टेंट और हेल्पर फंक्शन के लिए एक उपयोगी जगह हैं।

# --instructions--

`int` पर एक एक्सटेंशन `Dice` लिखें जिसमें:
- `int` टाइप का एक स्टैटिक कॉन्स्टेंट `sides` हो, मान `6` के साथ
- एक स्टैटिक मेथड `isValid` हो जो एक `int` पैरामीटर `value` लेती है और `true` लौटाती है जब वह `1` और `sides` के बीच हो (दोनों सहित)

उदाहरण:
```dart
print(Dice.sides);      // 6
print(Dice.isValid(7)); // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Dice.sides` `6` होना चाहिए

```dart
  test('test1', () {
    expect(Dice.sides, 6, reason: '--err-t1--');
  });
```

`Dice.isValid(1)` और `Dice.isValid(6)` `true` होना चाहिए

```dart
  test('test2', () {
    expect(Dice.isValid(1), true, reason: '--err-t2--');
    expect(Dice.isValid(6), true, reason: '--err-t2--');
  });
```

`Dice.isValid(0)` और `Dice.isValid(7)` `false` होना चाहिए

```dart
  test('test3', () {
    expect(Dice.isValid(0), false, reason: '--err-t3--');
    expect(Dice.isValid(7), false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Dice on int {
  static const int sides = 6;

  static bool isValid(int value) => value >= 1 && value <= sides;
}
```
