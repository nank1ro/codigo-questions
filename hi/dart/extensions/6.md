---
language: dart
exerciseType: 2
---

# --description--

`String` पर एक एक्सटेंशन को `String?` पर कॉल नहीं किया जा सकता: मान `null` हो सकता है, और कंपाइलर कॉल को अस्वीकार कर देता है। यदि आप इसके बजाय एक्सटेंशन को **नलेबल** टाइप पर घोषित करें, तो मेथड को सीधे `String?` पर कॉल किया जा सकता है, और उसके अंदर `this` का टाइप `String?` होता है, इसलिए आपको `null` केस को स्वयं संभालना होगा, उदाहरण के लिए `??` के साथ:

```dart
extension Defaults on int? {
  int orZero() => this ?? 0;
}

void main() {
  int? count = null;
  print(count.orZero()); // 0
  print(5.orZero());     // 5
}
```

एक नॉन-नलेबल `int` को वहाँ पास किया जा सकता है जहाँ एक `int?` अपेक्षित है, इसलिए एक्सटेंशन दोनों पर काम करता है।

# --instructions--

एक्सटेंशन को इस तरह पूरा करें कि `orEmpty` को एक नलेबल स्ट्रिंग पर कॉल किया जा सके और वह स्ट्रिंग स्वयं लौटाए, या खाली स्ट्रिंग लौटाए जब वह `null` हो। प्रोग्राम को `[]` और `[Ada]` प्रिंट करना चाहिए।

# --seed--

```dart
extension Defaults on [/] {
  String orEmpty() => this [/] '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --answers--

- String?
- ??
- String
- +
- ?.
- !

# --solutions--

```dart
extension Defaults on String? {
  String orEmpty() => this ?? '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --output--

[]
[Ada]
