---
language: dart
exerciseType: 1
---

# --description--

एक्सटेंशन किसी भी टाइप पर काम करते हैं, संख्याओं सहित। यह एक्सटेंशन हर `int` को एक ऐसी मेथड देता है जो उसे दोगुना कर देती है:

```dart
extension Doubling on int {
  int doubled() => this * 2;
}
```

चूँकि एक्सटेंशन **टाइप** पर लागू होता है, आप मेथड को किसी वेरिएबल पर या सीधे **लिटरल** पर कॉल कर सकते हैं। नकारात्मक लिटरल को कोष्ठकों की आवश्यकता होती है, वरना डॉट को माइनस चिह्न से पहले पढ़ा जाता है:

```dart
var n = 21;
print(n.doubled());    // 42
print(4.doubled());    // 8
print((-3).doubled()); // -6
```

# --instructions--

`int` पर एक एक्सटेंशन `Squaring` लिखें, जिसमें एक मेथड `squared` हो जो कोई पैरामीटर नहीं लेती और संख्या को स्वयं से गुणा करके लौटाती है।

उदाहरण:
```dart
print(5.squared()); // 25
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`5.squared()` `25` होना चाहिए

```dart
  test('test1', () {
    expect(5.squared(), 25, reason: '--err-t1--');
  });
```

`(-3).squared()` `9` होना चाहिए

```dart
  test('test2', () {
    expect((-3).squared(), 9, reason: '--err-t2--');
  });
```

`0.squared()` `0` होना चाहिए और परिणाम एक `int` होना चाहिए

```dart
  test('test3', () {
    expect(0.squared(), 0, reason: '--err-t3--');
    expect(12.squared(), isA<int>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Squaring on int {
  int squared() => this * this;
}
```
