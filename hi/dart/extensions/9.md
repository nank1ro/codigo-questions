---
language: dart
exerciseType: 4
---

# --description--

एक्सटेंशन केवल SDK टाइप के लिए नहीं हैं: आप **अपनी खुद की क्लास** को भी एक्सटेंड कर सकते हैं। यह तब उपयोगी है जब क्लास किसी ऐसे पैकेज से आती है जिसे आप नियंत्रित नहीं करते, या जब आप क्लास को छोटा रखना चाहते हैं और वैकल्पिक हेल्पर को उस कोड के बगल में जोड़ना चाहते हैं जिसे उनकी आवश्यकता है।

```dart
class Circle {
  final double radius;
  Circle(this.radius);
}

extension CircleMath on Circle {
  double get diameter => radius * 2;
}

void main() {
  print(Circle(3).diameter); // 6.0
}
```

एक्सटेंशन को क्लास के पब्लिक फ़ील्ड और मेथड दिखाई देते हैं, बिल्कुल क्लास के बाहर लिखे गए कोड की तरह।

# --instructions--

आइटमों को क्रमबद्ध करें ताकि `width` और `height` के साथ एक क्लास `Rectangle` परिभाषित हो, फिर `Rectangle` पर एक गेटर `area` के साथ एक एक्सटेंशन `RectangleMath` हो, और फिर एक `main` हो जो `4` और `2.5` वाले आयत का क्षेत्रफल प्रिंट करे। पहले क्लास परिभाषित करें, फिर एक्सटेंशन, फिर `main`। क्लास के अंदर, दोनों फ़ील्ड को कंस्ट्रक्टर से पहले घोषित करें। प्रोग्राम को प्रिंट करना चाहिए:

```
10.0
```

# --answers--

```dart
class Rectangle {
```

```dart
  final double width;
```

```dart
  final double height;
```

```dart
  Rectangle(this.width, this.height);
```

```dart
}
```

```dart
extension RectangleMath on Rectangle {
```

```dart
  double get area => width * height;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Rectangle(4, 2.5).area);
```

```dart
}
```

# --solutions--

```dart
class Rectangle {
  final double width;
  final double height;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

```dart
class Rectangle {
  final double height;
  final double width;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

# --output--

10.0
