---
language: dart
exerciseType: 1
---

# --description--

एक एक्सटेंशन **गेटर** भी घोषित कर सकता है, जिन्हें प्रॉपर्टी की तरह, बिना कोष्ठकों के पढ़ा जाता है। एक्सटेंशन के अंदर आप टाइप के अपने सदस्यों को सीधे कॉल कर सकते हैं: `this.` वैकल्पिक है, बिल्कुल क्लास के अंदर की तरह।

```dart
extension Sizes on String {
  bool get isLong => length > 10;      // same as this.length
  String get firstChar => this[0];
}

void main() {
  print('Dart'.isLong);           // false
  print('extension'.firstChar);   // e
}
```

गेटर चुनें जब सदस्य केवल एक मान को **पढ़ता** हो और कोई पैरामीटर न लेता हो; मेथड चुनें जब वह काम करती हो या तर्कों की आवश्यकता हो।

# --instructions--

`String` पर एक एक्सटेंशन `Blank` लिखें जिसमें `bool` टाइप का एक गेटर `isBlank` हो, जो `true` हो जब स्ट्रिंग खाली हो या उसमें केवल स्पेस हों। `trim()` और `isEmpty` का उपयोग करें।

उदाहरण:
```dart
print('   '.isBlank);  // true
print(' hi '.isBlank); // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`''.isBlank` और `'   '.isBlank` `true` होना चाहिए

```dart
  test('test1', () {
    expect(''.isBlank, true, reason: '--err-t1--');
    expect('   '.isBlank, true, reason: '--err-t1--');
  });
```

`' hi '.isBlank` `false` होना चाहिए

```dart
  test('test2', () {
    expect(' hi '.isBlank, false, reason: '--err-t2--');
    expect('x'.isBlank, false, reason: '--err-t2--');
  });
```

`isBlank` एक `bool` होना चाहिए

```dart
  test('test3', () {
    expect('  '.isBlank, isA<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Blank on String {
  bool get isBlank => trim().isEmpty;
}
```
