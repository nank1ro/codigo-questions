---
language: dart
exerciseType: 1
---

# --description--

हर स्ट्रिंग को अपनी `.length` प्रॉपर्टी के माध्यम से पता होता है कि उसमें कितने वर्ण हैं। स्पेस और विराम चिह्न भी वर्णों के रूप में गिने जाते हैं:

```dart
var word = 'hello';
print(word.length);      // 5
print('a b'.length);     // 3
```

आप स्क्वायर ब्रैकेट और उसके **इंडेक्स** (`0` से शुरू होकर) का उपयोग करके एक ही वर्ण पढ़ सकते हैं। परिणाम एक-वर्ण वाला `String` होता है:

```dart
print(word[0]);                // h
print(word[word.length - 1]);  // o
```

स्ट्रिंग की सीमा के बाहर का इंडेक्स (जैसे `word[5]`) पढ़ने पर एक एरर आता है।

# --instructions--

दी गई स्ट्रिंग `word` के लिए, उसके वर्णों की संख्या को `int` `size` में, उसके पहले वर्ण को `String` `first` में, और उसके अंतिम वर्ण को `String` `last` में स्टोर करें।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var word = 'Flutter';
```

# --after-seed--

```dart
  return [size, first, last];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`size` `7` होना चाहिए

```dart
  test('test1', () {
    expect(result()[0], 7, reason: '--err-t1--');
  });
```

`first` `'F'` होना चाहिए

```dart
  test('test2', () {
    expect(result()[1], 'F', reason: '--err-t2--');
  });
```

`last` `'r'` होना चाहिए

```dart
  test('test3', () {
    expect(result()[2], 'r', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var word = 'Flutter';
int size = word.length;
String first = word[0];
String last = word[word.length - 1];
```
