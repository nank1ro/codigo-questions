---
language: dart
exerciseType: 1
---

# --description--

स्ट्रिंग के अंदर खोजने के लिए कई मेथड मौजूद हैं:

- `.contains(other)` `true` लौटाता है अगर `other` स्ट्रिंग में कहीं भी मौजूद हो
- `.startsWith(other)` और `.endsWith(other)` शुरुआत और अंत की जांच करते हैं
- `.indexOf(other)` पहली बार आने की इंडेक्स लौटाता है, या न मिलने पर `-1` लौटाता है

```dart
var file = 'report.pdf';
print(file.contains('port'));    // true
print(file.startsWith('rep'));   // true
print(file.endsWith('.txt'));    // false
print(file.indexOf('.'));        // 6
print(file.indexOf('x'));        // -1
```

ये सभी केस-सेंसिटिव हैं: `'Dart'.contains('dart')` `false` है।

# --instructions--

दी गई स्ट्रिंग `email` के लिए, क्या उसमें `@` वर्ण मौजूद है यह `bool` `hasAt` में, उस वर्ण की इंडेक्स `int` `atIndex` में, और क्या ईमेल `example.com` पर समाप्त होता है यह `bool` `isExample` में स्टोर करें।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var email = 'ada@example.com';
```

# --after-seed--

```dart
  return [hasAt, atIndex, isExample];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasAt` `true` होना चाहिए

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`atIndex` `3` होना चाहिए

```dart
  test('test2', () {
    expect(result()[1], 3, reason: '--err-t2--');
  });
```

`isExample` `true` होना चाहिए

```dart
  test('test3', () {
    expect(result()[2], true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var email = 'ada@example.com';
bool hasAt = email.contains('@');
int atIndex = email.indexOf('@');
bool isExample = email.endsWith('example.com');
```
