---
language: dart
exerciseType: 1
---

# --description--

`.split(separator)` मेथड सेपरेटर के हर occurrence पर काटकर एक स्ट्रिंग को `List<String>` में बदल देता है। इसका उल्टा `.join(separator)` है, जो सूचियों (lists) का एक मेथड है और तत्वों को एक स्ट्रिंग में जोड़ देता है:

```dart
var csv = 'a,b,c';
List<String> parts = csv.split(',');
print(parts);            // [a, b, c]
print(parts.join(' - ')); // a - b - c
```

खाली सेपरेटर के साथ `.split('')` कॉल करने पर आपको हर एक वर्ण वाली एक सूची मिलती है।

# --instructions--

दी गई स्ट्रिंग `csv` के लिए, कॉमा से अलग किए गए उसके हिस्सों को `List<String>` `colors` में, और उन हिस्सों को `' | '` से जोड़कर `String` `joined` में स्टोर करें।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var csv = 'red,green,blue';
```

# --after-seed--

```dart
  return [colors, joined];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`colors` `['red', 'green', 'blue']` होना चाहिए

```dart
  test('test1', () {
    expect(result()[0], ['red', 'green', 'blue'], reason: '--err-t1--');
  });
```

`joined` `'red | green | blue'` होना चाहिए

```dart
  test('test2', () {
    expect(result()[1], 'red | green | blue', reason: '--err-t2--');
  });
```

`colors` एक `List` होना चाहिए

```dart
  test('test3', () {
    expect(result()[0], isA<List>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var csv = 'red,green,blue';
List<String> colors = csv.split(',');
String joined = colors.join(' | ');
```
