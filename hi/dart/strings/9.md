---
language: dart
exerciseType: 2
---

# --description--

`.replaceAll(from, to)` मेथड एक नई स्ट्रिंग लौटाता है जिसमें `from` की **हर** occurrence को `to` से बदल दिया गया हो। `.replaceFirst(from, to)` केवल पहली occurrence को बदलता है:

```dart
var text = 'a-b-c';
print(text.replaceAll('-', '+'));    // a+b+c
print(text.replaceFirst('-', '+'));  // a+b-c
```

# --instructions--

हर `cats` को `dogs` से बदलने और परिणाम प्रिंट करने के लिए कोड को पूरा करें।

# --seed--

```dart
var text = 'cats here, cats there';
print(text.[/]('cats', [/]));
```

# --answers--

- replaceAll
- 'dogs'
- replaceFirst
- replace
- 'cats'
- dogs

# --solutions--

```dart
var text = 'cats here, cats there';
print(text.replaceAll('cats', 'dogs'));
```

# --output--

dogs here, dogs there
