---
language: dart
exerciseType: 1
---

# --description--

`.substring(start, end)` मेथड इंडेक्स `start` से इंडेक्स `end` तक (`end` को **शामिल किए बिना**) स्ट्रिंग का हिस्सा लौटाता है। अगर आप `end` छोड़ देते हैं, तो यह स्ट्रिंग के अंत तक सब कुछ लेता है:

```dart
var text = 'Hello Dart';
print(text.substring(0, 5));  // Hello
print(text.substring(6));     // Dart
```

# --instructions--

`YYYY-MM-DD` फ़ॉर्मेट में दी गई स्ट्रिंग `date` के लिए, पहले चार वर्णों को `String` `year` में, और अंतिम दो वर्णों को `String` `day` में स्टोर करें।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var date = '2024-06-15';
```

# --after-seed--

```dart
  return [year, day];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`year` `'2024'` होना चाहिए

```dart
  test('test1', () {
    expect(result()[0], '2024', reason: '--err-t1--');
  });
```

`day` `'15'` होना चाहिए

```dart
  test('test2', () {
    expect(result()[1], '15', reason: '--err-t2--');
  });
```

`year` एक `String` होना चाहिए

```dart
  test('test3', () {
    expect(result()[0], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var date = '2024-06-15';
String year = date.substring(0, 4);
String day = date.substring(8);
```
