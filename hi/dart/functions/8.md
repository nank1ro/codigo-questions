---
language: dart
exerciseType: 1
---

# --instructions--

एक फंक्शन `add` लिखें जो दो `int` पैरामीटर लेता है और उनका योग लौटाता है।

उदाहरण:
```dart
print(add(2, 3)); // 5
print(add(10, 5)); // 15
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`add(2, 3)` को `5` लौटाना चाहिए
```dart
  test('test1', () {
    expect(add(2, 3), 5, reason: '--err-t1--');
  });
```

`add(10, 5)` को `15` लौटाना चाहिए
```dart
  test('test2', () {
    expect(add(10, 5), 15, reason: '--err-t2--');
  });
```

`add(0, 0)` को `0` लौटाना चाहिए
```dart
  test('test3', () {
    expect(add(0, 0), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int add(int a, int b) {
  return a + b;
}
```
