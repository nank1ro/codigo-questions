---
language: dart
exerciseType: 1
---

# --instructions--

एक फंक्शन `isEven` लिखें जो `int` पैरामीटर `n` लेता है और `true` लौटाता है यदि `n` सम है, अन्यथा `false` लौटाता है।

उदाहरण:
```dart
print(isEven(4));  // true
print(isEven(7));  // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isEven(4)` को `true` लौटाना चाहिए
```dart
  test('test1', () {
    expect(isEven(4), true, reason: '--err-t1--');
  });
```

`isEven(7)` को `false` लौटाना चाहिए
```dart
  test('test2', () {
    expect(isEven(7), false, reason: '--err-t2--');
  });
```

`isEven(0)` को `true` लौटाना चाहिए
```dart
  test('test3', () {
    expect(isEven(0), true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isEven(int n) {
  return n % 2 == 0;
}
```
