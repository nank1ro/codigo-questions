---
language: dart
exerciseType: 1
---

# --instructions--

वेरिएबल `number` के मान को `25` से अपडेट करें

# --before-seed--

```dart
dynamic number() {
```

# --seed--

```dart
int number = 1;
```

# --after-seed--

```dart
  return number;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`number` का मान `25` के बराबर होना चाहिए।

```dart
    test('test1', () {
      expect(number(), 25, reason: '--err-t1--');
    });
```

`number` का प्रकार `int` होना चाहिए

```dart
    test('test2', () {
      expect(number(), TypeMatcher<int>(), reason: '--err-t2--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int number = 1;
number = 25;
```
