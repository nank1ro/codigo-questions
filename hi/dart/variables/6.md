---
language: dart
exerciseType: 1
---

# --description--

यदि आप जोखिम के साथ जीना पसंद करते हैं, तो हम `dynamic` प्रकार का उपयोग करके भाषा की __टाइप-सेफ्टी__ को पूरी तरह अनदेखा कर सकते हैं।
यह आपको वेरिएबल को किसी भी प्रकार का मान असाइन करने की अनुमति देता है।

```dart
dynamic number;
number = 1; // OK
number = 3.14159; // OK
number = '10'; // OK
```

इस दृष्टिकोण को दृढ़ता से हतोत्साहित किया जाता है क्योंकि एरर अब कोड के _एनालाइज़र_ द्वारा नहीं पकड़े जाते, बल्कि केवल _रनटाइम_ पर (जब प्रोग्राम चल रहा होता है)।

# --instructions--

वेरिएबल `x` को संख्या `10` और प्रकार `dynamic` के साथ इनिशियलाइज़ करें।
वेरिएबल को स्ट्रिंग `Hello` से अपडेट करें।

# --before-seed--

```dart
dynamic number() {
```

# --after-seed--

```dart
  return x;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x` का मान `"Hello"` के बराबर होना चाहिए।

```dart
  test('test1', () {
    expect(number(), "Hello", reason: '--err-t1--');
  });
```

`x` का प्रकार `dynamic` होना चाहिए

```dart
  test('test2', () {
    expect(number(), TypeMatcher<dynamic>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
dynamic x = 10;
x = "Hello";
```
