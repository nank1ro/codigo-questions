---
language: dart
exerciseType: 1
---

# --description--

पिछले उदाहरण में, हमने एक वेरिएबल बनाना देखा:

```dart
int number = 1;
```

`=` चिह्न से भ्रमित न हों।
यह गणित की तरह बराबरी का चिह्न नहीं है, बल्कि इसे __असाइनमेंट ऑपरेटर__ के रूप में जाना जाता है क्योंकि यह वेरिएबल को एक मान असाइन करता है।
दूसरी ओर, बराबरी का चिह्न `==` है।

# --instructions--

वेरिएबल `x` को मान `5` असाइन करें।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x` का मान `5` के बराबर होना चाहिए।

```dart
  test('test1', () {
    expect(x, 5, reason: '--err-t1--');
  });
```

`x` का प्रकार `int` होना चाहिए

```dart
  test('test2', () {
    expect(x, TypeMatcher<int>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 5;
```
