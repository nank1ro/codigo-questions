---
language: dart
exerciseType: 1
---

# --description--

एक खाली मैप लिटरल `{}` में टाइप का अनुमान लगाने के लिए कोई जोड़ा नहीं होता, इसलिए इसे `<K, V>{}` से या टाइप एनोटेशन से स्पष्ट टाइप दें:

```dart
var cart = <String, int>{};
Map<String, int> other = {};
```

`.isEmpty` प्रॉपर्टी `true` होती है जब मैप में कोई जोड़ा न हो, और `.isNotEmpty` `true` होती है जब कम से कम एक जोड़ा हो:

```dart
print(cart.isEmpty); // true
cart['pen'] = 2;
print(cart.isNotEmpty); // true
```

# --instructions--

एक खाली `Map<String, int>` बनाएं जिसे `cart` कहा जाए। यह `.isEmpty` है या नहीं यह `wasEmpty` नामक `bool` में संग्रहीत करें, फिर की `'pen'` को वैल्यू `2` के साथ जोड़ें और यह `.isNotEmpty` है या नहीं यह `hasItems` नामक `bool` में संग्रहीत करें।

# --before-seed--

```dart
dynamic result() {
```

# --after-seed--

```dart
  return [wasEmpty, hasItems, cart];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`wasEmpty` `true` होना चाहिए

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasItems` `true` होना चाहिए

```dart
  test('test2', () {
    expect(result()[1], true, reason: '--err-t2--');
  });
```

`cart` `{'pen': 2}` होना चाहिए

```dart
  test('test3', () {
    expect(result()[2], {'pen': 2}, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var cart = <String, int>{};
bool wasEmpty = cart.isEmpty;
cart['pen'] = 2;
bool hasItems = cart.isNotEmpty;
```
