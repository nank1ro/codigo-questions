---
language: dart
exerciseType: 1
---

# --description--

`.putIfAbsent(key, ifAbsent)` मेथड एक जोड़ा **केवल तभी** जोड़ती है जब की अभी मैप में न हो। दूसरा आर्गुमेंट एक फ़ंक्शन है जो वैल्यू बनाता है। यदि की पहले से मौजूद है, तो मैप अपरिवर्तित रहता है। दोनों ही स्थितियों में, अब उस की के अंतर्गत संग्रहीत वैल्यू लौटाई जाती है:

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99); // Ann is already there, nothing changes
ages.putIfAbsent('Bob', () => 25); // Bob is added
print(ages); // {Ann: 30, Bob: 25}
```

# --instructions--

मैप `ages` दिया गया है, `.putIfAbsent()` का उपयोग दो बार करें: पहले की `'Ann'` के लिए वैल्यू `99` के साथ, फिर की `'Bob'` के लिए वैल्यू `25` के साथ।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var ages = {'Ann': 30};
```

# --after-seed--

```dart
  return ages;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`ages` `{'Ann': 30, 'Bob': 25}` होना चाहिए

```dart
  test('test1', () {
    expect(result(), {'Ann': 30, 'Bob': 25}, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var ages = {'Ann': 30};
ages.putIfAbsent('Ann', () => 99);
ages.putIfAbsent('Bob', () => 25);
```
