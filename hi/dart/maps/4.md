---
language: dart
exerciseType: 2
---

# --description--

`.remove(key)` मेथड मैप से एक की और उसकी वैल्यू को हटा देती है। यह हटाई गई वैल्यू लौटाती है, या यदि की मौजूद नहीं थी तो `null` लौटाती है:

```dart
var ages = {'Ann': 30, 'Bob': 25};
int? removed = ages.remove('Ann');
print(removed); // 30
print(ages); // {Bob: 25}
```

`.length` प्रॉपर्टी की-वैल्यू जोड़ों की संख्या लौटाती है:

```dart
print(ages.length); // 1
```

# --instructions--

मैप से `Bob` को हटाने के लिए, और फिर कितने जोड़े बचे हैं यह प्रिंट करने के लिए कोड पूरा करें।

# --seed--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.[/]('Bob');
print(ages.[/]);
```

# --answers--

- remove
- length
- delete
- removeAt
- size
- count

# --solutions--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
ages.remove('Bob');
print(ages.length);
```

# --output--

2
