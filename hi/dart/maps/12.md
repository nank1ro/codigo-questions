---
language: dart
exerciseType: 1
---

# --description--

एक मैप `Iterable` नहीं है, इसलिए आप इसे सीधे `for-in` से लूप नहीं कर सकते। इसके बजाय, `.entries` पर लूप करें: प्रत्येक तत्व एक `MapEntry` होता है जिसमें एक `.key` और एक `.value` होती है:

```dart
var ages = {'Ann': 30, 'Bob': 25};
for (var entry in ages.entries) {
  print('${entry.key}: ${entry.value}');
}
// Ann: 30
// Bob: 25
```

# --instructions--

मैप `ages` दिया गया है, `for-in` से इसकी `.entries` पर लूप करें और हर जोड़े के लिए `'Ann is 30'` जैसे रूप में एक स्ट्रिंग `lines` नामक `List<String>` में जोड़ें।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
```

# --after-seed--

```dart
  return lines;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`lines` `['Ann is 30', 'Bob is 25', 'Cid is 41']` होना चाहिए

```dart
  test('test1', () {
    expect(result(), ['Ann is 30', 'Bob is 25', 'Cid is 41'], reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var ages = {'Ann': 30, 'Bob': 25, 'Cid': 41};
List<String> lines = [];
for (var entry in ages.entries) {
  lines.add('${entry.key} is ${entry.value}');
}
```
