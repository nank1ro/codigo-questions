---
language: dart
exerciseType: 1
---

# --description--

यह जाँचने के लिए कि किसी मैप में कोई दी गई की है या नहीं, `.containsKey(key)` का उपयोग करें। यह जाँचने के लिए कि क्या कोई जोड़ा दी गई वैल्यू संग्रहीत करता है, `.containsValue(value)` का उपयोग करें। दोनों एक `bool` लौटाते हैं:

```dart
var ages = {'Ann': 30, 'Bob': 25};
print(ages.containsKey('Ann')); // true
print(ages.containsKey('Zed')); // false
print(ages.containsValue(25)); // true
```

# --instructions--

मैप `ages` दिया गया है, इसमें की `'Ann'` है या नहीं यह `hasAnn` नामक `bool` में संग्रहीत करें, और इसमें वैल्यू `99` है या नहीं यह `has99` नामक `bool` में संग्रहीत करें।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var ages = {'Ann': 30, 'Bob': 25};
```

# --after-seed--

```dart
  return [hasAnn, has99];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasAnn` `true` होना चाहिए

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`has99` `false` होना चाहिए

```dart
  test('test2', () {
    expect(result()[1], false, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var ages = {'Ann': 30, 'Bob': 25};
bool hasAnn = ages.containsKey('Ann');
bool has99 = ages.containsValue(99);
```
