---
language: dart
exerciseType: 1
---

# --description--

`.keys` प्रॉपर्टी मैप की सभी कीज़ देती है और `.values` सभी वैल्यूज़ देती है, सम्मिलन क्रम में। ये lazy `Iterable` होती हैं, इसलिए जब आपको वास्तविक `List` चाहिए तो `.toList()` कॉल करें:

```dart
var ages = {'Ann': 30, 'Bob': 25};
List<String> names = ages.keys.toList();
List<int> years = ages.values.toList();
print(names); // [Ann, Bob]
print(years); // [30, 25]
```

# --instructions--

मैप `scores` दिया गया है, इसकी कीज़ के साथ एक `List<String>` `subjects` बनाएं और इसकी वैल्यूज़ के साथ एक `List<int>` `points` बनाएं।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var scores = {'math': 90, 'art': 75, 'gym': 60};
```

# --after-seed--

```dart
  return [subjects, points];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`subjects` `['math', 'art', 'gym']` होना चाहिए

```dart
  test('test1', () {
    expect(result()[0], ['math', 'art', 'gym'], reason: '--err-t1--');
  });
```

`points` `[90, 75, 60]` होना चाहिए

```dart
  test('test2', () {
    expect(result()[1], [90, 75, 60], reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var scores = {'math': 90, 'art': 75, 'gym': 60};
List<String> subjects = scores.keys.toList();
List<int> points = scores.values.toList();
```
