---
language: dart
exerciseType: 1
---

# --instructions--

तीन शीर्ष-स्तर `int` चर घोषित करें: `number` मान `42` के साथ, `min` मान `1` के साथ और `max` मान `100` के साथ। फिर एक शीर्ष-स्तर `bool` चर घोषित करें जिसका नाम `inRange` है जो `true` है जब `number` `min` से अधिक या बराबर है AND `max` से कम या बराबर है।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`inRange` `true` होना चाहिए और `bool` प्रकार का होना चाहिए

```dart
  test('test1', () {
    expect(inRange, true, reason: '--err-t1--');
    expect(inRange, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int number = 42;
int min = 1;
int max = 100;
bool inRange = number >= min && number <= max;

void main() {
  print(inRange);
}
```
