---
language: dart
exerciseType: 4
---

# --instructions--

कोड की पंक्तियों को क्रमबद्ध करें एक तापमान मान की घोषणा करने के लिए, जांचने के लिए कि क्या यह `&&` का उपयोग करके `0` से `100` समावेशी की सीमा में है, परिणाम को `isValid` में संग्रहीत करने के लिए और इसे प्रिंट करने के लिए। आउटपुट `true` होना चाहिए।

# --answers--

```dart
void main() {
```

```dart
  int temperature = 37;
```

```dart
  bool isValid = temperature >= 0 && temperature <= 100;
```

```dart
  print(isValid);
```

```dart
}
```

# --solutions--

```dart
void main() {
  int temperature = 37;
  bool isValid = temperature >= 0 && temperature <= 100;
  print(isValid);
}
```

# --output--

true
