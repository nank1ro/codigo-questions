---
language: dart
exerciseType: 1
---

# --instructions--

एक शीर्ष-स्तर `int` चर घोषित करें जिसका नाम `age` है मान `35` के साथ। फिर एक शीर्ष-स्तर `bool` चर घोषित करें जिसका नाम `isWorkingAge` है जो `true` है जब `age` `18` से अधिक या बराबर है AND `65` से कम या बराबर है।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isWorkingAge` `true` होना चाहिए और `bool` प्रकार का होना चाहिए

```dart
  test('test1', () {
    expect(isWorkingAge, true, reason: '--err-t1--');
    expect(isWorkingAge, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int age = 35;
bool isWorkingAge = age >= 18 && age <= 65;

void main() {
  print(isWorkingAge);
}
```
