---
language: dart
exerciseType: 1
---

# --instructions--

दो शीर्ष-स्तर `int` चर घोषित करें: `a` मान `5` के साथ और `b` मान `9` के साथ। फिर एक शीर्ष-स्तर `bool` चर घोषित करें जिसका नाम `notEqual` है जो `!=` ऑपरेटर से `a` और `b` की तुलना का परिणाम संग्रहीत करता है।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`notEqual` `true` होना चाहिए और `bool` प्रकार का होना चाहिए

```dart
  test('test1', () {
    expect(notEqual, true, reason: '--err-t1--');
    expect(notEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 5;
int b = 9;
bool notEqual = a != b;

void main() {
  print(notEqual);
}
```
