---
language: dart
exerciseType: 4
---

# --instructions--

कोड की पंक्तियों को क्रमबद्ध करें दो बूलियन चर की घोषणा करने के लिए, उन्हें `||` और `&&` से जोड़ने के लिए जैसा दिखाया गया है, परिणाम को `outcome` में संग्रहीत करने के लिए और इसे प्रिंट करने के लिए। आउटपुट `true` होना चाहिए।

# --answers--

```dart
void main() {
```

```dart
  bool hasPass = false;
```

```dart
  bool isAdmin = true;
```

```dart
  bool outcome = hasPass || isAdmin && !hasPass;
```

```dart
  print(outcome);
```

```dart
}
```

# --solutions--

```dart
void main() {
  bool hasPass = false;
  bool isAdmin = true;
  bool outcome = hasPass || isAdmin && !hasPass;
  print(outcome);
}
```

# --output--

true
