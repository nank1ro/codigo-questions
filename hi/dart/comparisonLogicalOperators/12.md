---
language: dart
exerciseType: 1
---

# --instructions--

दो शीर्ष-स्तर `int` चर घोषित करें: `high` मान `20` के साथ और `low` मान `12` के साथ। फिर एक शीर्ष-स्तर `bool` चर घोषित करें जिसका नाम `isHigher` है जो यह जांचने का परिणाम संग्रहीत करता है कि `high` `>` ऑपरेटर का उपयोग करके `low` से अधिक है या नहीं।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHigher` `true` होना चाहिए और `bool` प्रकार का होना चाहिए

```dart
  test('test1', () {
    expect(isHigher, true, reason: '--err-t1--');
    expect(isHigher, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int high = 20;
int low = 12;
bool isHigher = high > low;

void main() {
  print(isHigher);
}
```
