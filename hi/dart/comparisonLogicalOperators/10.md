---
language: dart
exerciseType: 1
---

# --instructions--

दो शीर्ष-स्तर `int` चर घोषित करें: `x` मान `8` के साथ और `y` मान `8` के साथ। फिर एक शीर्ष-स्तर `bool` चर घोषित करें जिसका नाम `areEqual` है जो `==` ऑपरेटर से `x` और `y` की तुलना का परिणाम संग्रहीत करता है।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`areEqual` `true` होना चाहिए और `bool` प्रकार का होना चाहिए

```dart
  test('test1', () {
    expect(areEqual, true, reason: '--err-t1--');
    expect(areEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 8;
int y = 8;
bool areEqual = x == y;

void main() {
  print(areEqual);
}
```
