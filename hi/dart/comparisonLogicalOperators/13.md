---
language: dart
exerciseType: 1
---

# --instructions--

दो शीर्ष-स्तर `int` चर घोषित करें: `price` मान `30` के साथ और `budget` मान `50` के साथ। फिर एक शीर्ष-स्तर `bool` चर घोषित करें जिसका नाम `canAfford` है जो यह जांचने का परिणाम संग्रहीत करता है कि `price` `<` ऑपरेटर का उपयोग करके `budget` से कम है या नहीं।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canAfford` `true` होना चाहिए और `bool` प्रकार का होना चाहिए

```dart
  test('test1', () {
    expect(canAfford, true, reason: '--err-t1--');
    expect(canAfford, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int price = 30;
int budget = 50;
bool canAfford = price < budget;

void main() {
  print(canAfford);
}
```
