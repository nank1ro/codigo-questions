---
language: dart
exerciseType: 1
---

# --description--

कुछ हायर-ऑर्डर मेथड नया कलेक्शन बनाने के बजाय कलेक्शन के बारे में एक प्रश्न का उत्तर देते हैं। वे एक predicate लेते हैं और एक `bool` लौटाते हैं:

- `any` तब `true` होता है जब **कम से कम एक** तत्व predicate को संतुष्ट करता हो
- `every` तब `true` होता है जब **सभी** तत्व उसे संतुष्ट करते हों

```dart
final numbers = [1, 2, 3];
print(numbers.any((n) => n > 2));   // true
print(numbers.every((n) => n > 2)); // false
```

दोनों जैसे ही उत्तर निश्चित हो जाता है, रुक जाते हैं: `any` पहले मैच करने वाले तत्व पर, `every` पहले उस तत्व पर जो मैच नहीं करता।

एक खाली कलेक्शन पर `any` `false` होता है और `every` `true`: पहले को सिद्ध करने के लिए कोई तत्व नहीं होता, और दूसरे को तोड़ने के लिए भी कोई नहीं।

# --instructions--

दो फंक्शन लिखें, दोनों एक `List<int>` `numbers` लेते हुए:

- `hasNegative` `true` लौटाता है यदि कम से कम एक तत्व `0` से छोटा हो
- `allEven` `true` लौटाता है यदि हर तत्व सम हो

Example:
```dart
print(hasNegative([1, -2, 3])); // true
print(allEven([2, 4]));         // true
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasNegative([1, -2, 3])` को `true` होना चाहिए और `hasNegative([1, 2, 3])` को `false`

```dart
  test('test1', () {
    expect(hasNegative([1, -2, 3]), true, reason: '--err-t1--');
    expect(hasNegative([1, 2, 3]), false, reason: '--err-t1--');
  });
```

`allEven([2, 4, 6])` को `true` होना चाहिए और `allEven([2, 5])` को `false`

```dart
  test('test2', () {
    expect(allEven([2, 4, 6]), true, reason: '--err-t2--');
    expect(allEven([2, 5]), false, reason: '--err-t2--');
  });
```

एक खाली सूची पर `hasNegative` को `false` होना चाहिए और `allEven` को `true`

```dart
  test('test3', () {
    expect(hasNegative(<int>[]), false, reason: '--err-t3--');
    expect(allEven(<int>[]), true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool hasNegative(List<int> numbers) {
  return numbers.any((n) => n < 0);
}

bool allEven(List<int> numbers) {
  return numbers.every((n) => n % 2 == 0);
}
```
