---
language: dart
exerciseType: 1
---

# --description--

इन सबको एक साथ रखने पर, ये मेथड अधिकांश हाथ से लिखे लूप की जगह ले लेते हैं। एक पाइपलाइन आमतौर पर तीन चरणों में पढ़ी जाती है: `where` से तत्वों को **चुनें**, `map` से उन्हें **बदलें**, फिर `fold` से उन्हें **जोड़ें**:

```dart
final prices = [12, 40, 7];
final cheapTotal = prices.where((p) => p < 20).fold(0, (acc, p) => acc + p);
print(cheapTotal); // 19
```

चूंकि `fold` अपना प्रारंभिक मान स्वयं चुनता है, वह एक शृंखला को ऐसे टाइप के साथ भी समाप्त कर सकता है जिसका तत्वों से कोई लेना-देना नहीं, जैसे एक बार में एक टुकड़ा बढ़ती हुई एक `String`:

```dart
final words = ['fig', 'kiwi'];
final firstLetters = words.fold('', (acc, w) => acc + w[0]);
print(firstLetters); // fk
```

हर चरण छोटा रहता है और बताता है कि वह क्या करता है, और यही असली कारण है कि उन्हें तीनों काम एक साथ करने वाले लूप से बेहतर क्यों माना जाता है।

# --instructions--

एक फंक्शन `initials` लिखें जो एक `List<String>` `names` ले, खाली नामों को नजरअंदाज करे, और बचे हुए हर नाम के पहले अक्षर से बनी एक `String` लौटाए, अपर केस में और क्रम में।

Example:
```dart
print(initials(['ada', '', 'linus'])); // AL
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`initials(['ada', '', 'linus'])` को `'AL'` लौटाना चाहिए

```dart
  test('test1', () {
    expect(initials(['ada', '', 'linus']), 'AL', reason: '--err-t1--');
  });
```

अक्षर अपर केस में और क्रम में होने चाहिए, और पहले से अपर-केस नाम जैसे हैं वैसे ही रहने चाहिए

```dart
  test('test2', () {
    expect(initials(['grace', 'Bob', 'ada']), 'GBA', reason: '--err-t2--');
    expect(initials(['x']), 'X', reason: '--err-t2--');
  });
```

जब कोई गैर-खाली नाम न हो तो `initials` को एक खाली `String` लौटानी चाहिए

```dart
  test('test3', () {
    expect(initials(<String>[]), '', reason: '--err-t3--');
    expect(initials(['', '']), '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String initials(List<String> names) {
  return names
      .where((n) => n.isNotEmpty)
      .map((n) => n[0].toUpperCase())
      .fold('', (acc, letter) => acc + letter);
}
```
