---
language: dart
exerciseType: 1
---

# --instructions--

एक शीर्ष-स्तरीय `int` चर `remainder` घोषित करें जिसका मान `29` है। फिर `%=` का उपयोग करके इसे `6` से विभाजित करने का शेषफल असाइन करें।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`remainder` `5` होना चाहिए

```dart
  test('test1', () {
    expect(remainder, 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 29;

void main() {
  remainder %= 6;
  print(remainder);
}
```
