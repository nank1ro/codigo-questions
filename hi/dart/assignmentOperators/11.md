---
language: dart
exerciseType: 1
---

# --instructions--

एक `double` चर `value` घोषित करें जिसका मान `20.0` है। फिर इसे `4` से विभाजित करने के लिए `/=` का उपयोग करें। ध्यान दें कि `/=` Dart में हमेशा एक `double` उत्पन्न करता है।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
double value = 20.0;
```

# --after-seed--

```dart
  return value;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value` `5.0` होना चाहिए

```dart
  test('test1', () {
    expect(result(), closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;
value /= 4;
```
