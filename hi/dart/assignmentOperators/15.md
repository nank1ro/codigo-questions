---
language: dart
exerciseType: 1
---

# --instructions--

एक दुकान किसी वस्तु पर 20% की छूट लागू कर रही है। एक शीर्ष-स्तरीय `double` चर `price` घोषित करें जिसका मान `50.0` है। फिर `price` को `0.8` से गुणा करके छूट लागू करने के लिए `*=` का उपयोग करें।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`price` `40.0` होना चाहिए

```dart
  test('test1', () {
    expect(price, closeTo(40.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double price = 50.0;

void main() {
  price *= 0.8;
  print(price);
}
```
