---
language: dart
exerciseType: 3
---

# --instructions--

`x += 5` क्या करता है जब `x` वर्तमान में `10` है?

# --answers--

- `x` को `15` पर सेट करता है
- `x` को `5` पर सेट करता है
- `x` को `10` पर सेट करता है
- `x` को `50` पर सेट करता है

# --solutions--

- `x` को `15` पर सेट करता है
