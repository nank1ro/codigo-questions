---
language: dart
exerciseType: 1
---

# --instructions--

एक शीर्ष-स्तरीय `int` चर `fuel` घोषित करें जिसका मान `100` है। फिर इससे `35` घटाने के लिए `-=` का उपयोग करें।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`fuel` `65` होना चाहिए

```dart
  test('test1', () {
    expect(fuel, 65, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int fuel = 100;

void main() {
  fuel -= 35;
  print(fuel);
}
```
