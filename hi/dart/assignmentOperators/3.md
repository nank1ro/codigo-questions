---
language: dart
exerciseType: 2
---

# --description--

**घटाव असाइनमेंट ऑपरेटर** `-=` एक मान को एक चर से घटाता है और परिणाम को वापस उस चर में संग्रहीत करता है।

```dart
int lives = 3;
lives -= 1; // lives अब 2 है
```

यह `lives = lives - 1` का संक्षिप्त रूप है।

# --instructions--

कोड को पूरा करें ताकि `-=` का उपयोग करके चर `stock` से `5` घटाया जा सके।

# --seed--

```dart
void main() {
  int stock = 20;
  stock [/] 5;
  print(stock);
}
```

# --answers--

- -=
- =
- +=
- -

# --solutions--

```dart
void main() {
  int stock = 20;
  stock -= 5;
  print(stock);
}
```

# --output--

15
