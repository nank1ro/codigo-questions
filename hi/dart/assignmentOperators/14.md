---
language: dart
exerciseType: 1
---

# --instructions--

एक `int` चर `n` घोषित करें जिसका मान `2` है। फिर, `main` में, निम्नलिखित असाइनमेंट ऑपरेटर को क्रम में लागू करें:
- `n *= 10` (10 से गुणा करें)
- `n += 5` (5 जोड़ें)
- `n -= 3` (3 घटाएं)

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int n = 2;
```

# --after-seed--

```dart
  return n;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`n` `22` होना चाहिए

```dart
  test('test1', () {
    expect(result(), 22, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 2;
n *= 10;
n += 5;
n -= 3;
```
