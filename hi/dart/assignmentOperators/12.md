---
language: dart
exerciseType: 1
---

# --instructions--

एक शीर्ष-स्तरीय `int` चर `apples` घोषित करें जिसका मान `17` है। फिर इसे `5` से पूर्णांक-विभाजित करने के लिए `~/=` का उपयोग करें। `~/=` ऑपरेटर पूर्णांक विभाजन करता है और हमेशा एक `int` लौटाता है।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`apples` `3` होना चाहिए

```dart
  test('test1', () {
    expect(apples, 3, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int apples = 17;

void main() {
  apples ~/= 5;
  print(apples);
}
```
