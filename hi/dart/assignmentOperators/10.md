---
language: dart
exerciseType: 1
---

# --instructions--

एक शीर्ष-स्तरीय `int` चर `width` घोषित करें जिसका मान `6` है। फिर इसे `4` से गुणा करने के लिए `*=` का उपयोग करें।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`width` `24` होना चाहिए

```dart
  test('test1', () {
    expect(width, 24, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int width = 6;

void main() {
  width *= 4;
  print(width);
}
```
