---
language: dart
exerciseType: 1
---

# --description--

मिक्सिन की बॉडी क्लास की बॉडी जैसी दिखती है: मेथड, गेटर और फ़ील्ड, बिल्कुल उसी तरह लिखे जाते हैं। अंतर घोषणा के साथ आप क्या कर सकते हैं, इसमें है।

```dart
mixin Timestamped {
  String get stamp => '[log]';

  String format(String text) => '$stamp $text';
}

class Server with Timestamped {}
class Printer with Timestamped {}
```

मिक्सिन का नाम एक **टाइप** भी है, इसलिए `Server() is Timestamped` का मान `true` होता है और एक वेरिएबल को `Timestamped t = Server();` की तरह घोषित किया जा सकता है। अब दो असंबंधित क्लासें एक ही इम्प्लीमेंटेशन साझा करती हैं, बिना एक-दूसरे को इनहेरिट किए।

# --instructions--

एक मिक्सिन `Shouter` घोषित करें, जिसमें एक मेथड `shout` हो जो एक `String` `text` लेती है और उसे अपरकेस में और उसके बाद एक विस्मयादिबोधक चिह्न लगाकर लौटाती है।

एक क्लास `Speaker` घोषित करें जो `Shouter` का उपयोग करती है और अपनी तरफ से कुछ नहीं जोड़ती।

उदाहरण:
```dart
print(Speaker().shout('hi')); // HI!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Speaker().shout('hi')` `'HI!'` होना चाहिए

```dart
  test('test1', () {
    expect(Speaker().shout('hi'), 'HI!', reason: '--err-t1--');
  });
```

`Speaker().shout('Dart rocks')` `'DART ROCKS!'` होना चाहिए और `Speaker().shout('')` `'!'` होना चाहिए

```dart
  test('test2', () {
    expect(Speaker().shout('Dart rocks'), 'DART ROCKS!', reason: '--err-t2--');
    expect(Speaker().shout(''), '!', reason: '--err-t2--');
  });
```

`Speaker` को `shout` मिक्सिन से मिलना चाहिए, इसलिए एक `Speaker` एक `Shouter` भी होना चाहिए

```dart
  test('test3', () {
    expect(Speaker(), isA<Shouter>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Shouter {
  String shout(String text) => '${text.toUpperCase()}!';
}

class Speaker with Shouter {}
```
