---
language: dart
exerciseType: 1
---

# --description--

क्लास बॉडी चेन के बिल्कुल अंत में होती है, इसलिए क्लास में घोषित कोई सदस्य उसके किसी भी मिक्सिन से आए उसी सदस्य को **ओवरराइड** कर देता है। ओवरराइड के अंदर, **`super`** मिक्सिन द्वारा दिए गए वर्शन तक पहुँचता है:

```dart
mixin Polite {
  String greet() => 'Hello';
}

class Host with Polite {
  @override
  String greet() => '${super.greet()}, welcome!';
}

class Guest with Polite {}

void main() {
  print(Host().greet());  // Hello, welcome!
  print(Guest().greet()); // Hello
}
```

मिक्सिन स्वयं अछूता रहता है: `Guest` को अब भी मूल `greet` मिलता है। `super.greet()` ही है जो `Host` को साझा व्यवहार की प्रति बनाने के बजाय उस पर निर्माण करने देता है।

# --instructions--

एक मिक्सिन `Signed` घोषित करें, जिसमें एक मेथड `sign` हो जो `'signed'` लौटाती है।

एक क्लास `Letter` घोषित करें जो `Signed` का उपयोग करती है और `sign` को ओवरराइड करे, ताकि वह मिक्सिन के परिणाम के बाद `' by Ada'` लौटाए, जो `super` को कॉल करके मिलता है।

एक क्लास `Note` घोषित करें जो `Signed` का उपयोग करती है और कुछ भी ओवरराइड नहीं करती।

उदाहरण:
```dart
print(Letter().sign()); // signed by Ada
print(Note().sign());   // signed
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Letter().sign()` `'signed by Ada'` होना चाहिए

```dart
  test('test1', () {
    expect(Letter().sign(), 'signed by Ada', reason: '--err-t1--');
  });
```

`Note().sign()` अब भी `'signed'` होना चाहिए, क्योंकि मिक्सिन अपरिवर्तित है

```dart
  test('test2', () {
    expect(Note().sign(), 'signed', reason: '--err-t2--');
  });
```

दोनों क्लासों को मिक्सिन का उपयोग करना चाहिए, और ओवरराइड का उपयोग मिक्सिन टाइप के ज़रिए भी होना चाहिए

```dart
  test('test3', () {
    final Signed letter = Letter();
    expect(letter.sign(), 'signed by Ada', reason: '--err-t3--');
    expect(Note(), isA<Signed>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Signed {
  String sign() => 'signed';
}

class Letter with Signed {
  @override
  String sign() => '${super.sign()} by Ada';
}

class Note with Signed {}
```
