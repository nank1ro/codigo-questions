---
language: dart
exerciseType: 2
---

# --description--

`.remove(value)` मेथड सेट से एक मान हटाती है। यदि मान मौजूद था तो यह `true` लौटाती है, अन्यथा `false`:

```dart
var numbers = {1, 2, 3};
print(numbers.remove(2)); // true
print(numbers.remove(9)); // false
print(numbers); // {1, 3}
```

सभी तत्वों को एक साथ हटाने के लिए `.clear()` का उपयोग करें।

# --instructions--

कोड पूरा करें ताकि सेट से संख्या `2` हटाई जाए, फिर सेट को प्रिंट करें।

# --seed--

```dart
var numbers = {1, 2, 3};
numbers.[/](2);
print(numbers);
```

# --answers--

- remove
- removeAt
- delete
- drop
- clear
- pop

# --solutions--

```dart
var numbers = {1, 2, 3};
numbers.remove(2);
print(numbers);
```

# --output--

{1, 3}
