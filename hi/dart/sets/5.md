---
language: dart
exerciseType: 1
---

# --description--

किसी मान के सेट में होने की जांच करने के लिए `.contains(value)` का उपयोग करें, जो एक `bool` लौटाती है। `.isEmpty` प्रॉपर्टी `true` होती है जब सेट में कोई तत्व नहीं होता, और `.isNotEmpty` तब `true` होती है जब उसमें कम से कम एक तत्व हो:

```dart
var seen = {'x', 'y'};
print(seen.contains('x')); // true
print(seen.contains('z')); // false
print(seen.isEmpty);       // false
print(seen.isNotEmpty);    // true
```

# --instructions--

सेट `seen` दिया गया है, यह `'x'` को शामिल करता है या नहीं यह `bool` `hasX` में, यह `'z'` को शामिल करता है या नहीं यह `bool` `hasZ` में, और यह खाली है या नहीं यह `bool` `empty` में संग्रहीत करें।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var seen = {'x', 'y'};
```

# --after-seed--

```dart
  return [hasX, hasZ, empty];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasX` `true` होना चाहिए

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasZ` `false` होना चाहिए

```dart
  test('test2', () {
    expect(result()[1], false, reason: '--err-t2--');
  });
```

`empty` `false` होना चाहिए

```dart
  test('test3', () {
    expect(result()[2], false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var seen = {'x', 'y'};
bool hasX = seen.contains('x');
bool hasZ = seen.contains('z');
bool empty = seen.isEmpty;
```
