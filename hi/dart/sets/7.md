---
language: dart
exerciseType: 1
---

# --description--

एक सेट एक `Iterable` है, इसलिए आप इसके तत्वों पर सीधे `for-in` से लूप चला सकते हैं, बिल्कुल एक सूची की तरह। इसमें कोई इंडेक्स नहीं होते: तत्वों को इंसर्शन ऑर्डर में देखा जाता है:

```dart
var numbers = {3, 1, 4};
for (var n in numbers) {
  print(n);
}
// 3
// 1
// 4
```

# --instructions--

सेट `numbers` दिया गया है, उस पर `for-in` से लूप चलाएं और उसके सभी तत्वों को जोड़कर एक `int` `total` में संग्रहीत करें।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var numbers = {3, 1, 4, 1, 5};
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` `13` होना चाहिए

```dart
  test('test1', () {
    expect(result(), 13, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var numbers = {3, 1, 4, 1, 5};
int total = 0;
for (var n in numbers) {
  total += n;
}
```
