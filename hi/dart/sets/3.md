---
language: dart
exerciseType: 1
---

# --description--

`.add(value)` मेथड एक मान जोड़ती है। यदि मान जोड़ा गया तो यह `true` लौटाती है, और यदि वह पहले से सेट में मौजूद था तो `false` लौटाती है, जिस स्थिति में कुछ नहीं बदलता। `.addAll(iterable)` मेथड किसी सूची या दूसरे सेट के हर तत्व को जोड़ती है, और पहले से मौजूद तत्वों को भी छोड़ देती है:

```dart
var tags = {'dart'};
tags.add('web');    // true
tags.add('dart');   // false, already there
tags.addAll(['web', 'mobile']);
print(tags); // {dart, web, mobile}
```

एक खाली `{}` लिटरल सेट नहीं, बल्कि एक **मैप** है। खाली सेट बनाने के लिए उसे एक टाइप दें:

```dart
var empty = <String>{};
Set<int> other = {};
```

# --instructions--

सेट `tags` दिया गया है, `.add()` से उसमें `'web'` जोड़ें, फिर `.addAll()` से सूची `['web', 'mobile']` के सभी तत्व जोड़ें।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var tags = {'dart'};
```

# --after-seed--

```dart
  return tags;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`tags` `{'dart', 'web', 'mobile'}` होना चाहिए

```dart
  test('test1', () {
    expect(result(), {'dart', 'web', 'mobile'}, reason: '--err-t1--');
  });
```

`tags` में 3 तत्व होने चाहिए

```dart
  test('test2', () {
    expect(result().length, 3, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var tags = {'dart'};
tags.add('web');
tags.addAll(['web', 'mobile']);
```
