---
language: dart
exerciseType: 1
---

# --instructions--

`10` से `1` (सम्मिलित) तक उल्टी गिनती करने के लिए `while` लूप का उपयोग करें। की गई पुनरावृत्तियों की संख्या लौटाएँ।

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return iterations;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`iterations` का मान `10` होना चाहिए

```dart
  test('test1', () {
    expect(computeValue(), 10, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 10;
int iterations = 0;
while (n >= 1) {
  iterations++;
  n--;
}
```
