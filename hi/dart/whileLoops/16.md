---
language: dart
exerciseType: 1
---

# --instructions--

`product = 1` से शुरू करें और `while` लूप का उपयोग करके `product` के `100` से अधिक या बराबर होने तक `2` से गुणा करते रहें। `product` लौटाएँ।

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return product;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`product` का मान `128` होना चाहिए

```dart
  test('test1', () {
    expect(computeValue(), 128, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int product = 1;
while (product < 100) {
  product *= 2;
}
```
