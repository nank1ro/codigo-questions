---
language: dart
exerciseType: 2
---

# --description--

`while` लूप की शर्त में कोई भी बूलियन एक्सप्रेशन हो सकता है। जैसे ही शर्त `false` हो जाती है, लूप समाप्त हो जाता है।

```dart
int n = 100;
while (n > 1) {
  n ~/= 2;
}
print(n); // 0
```

यहाँ, `n` को पूर्णांक विभाजन (`~/`) का उपयोग करके प्रत्येक पुनरावृत्ति में आधा किया जाता है।

# --instructions--

`while` लूप को पूरा करें जो `n` को (`~/= 2` का उपयोग करके) तब तक आधा करता रहता है जब तक यह `1` से अधिक हो, फिर परिणाम प्रिंट करता है।

# --seed--

```dart
void main() {
  int n = 64;
  while (n [/] 1) {
    n [/] 2;
  }
  print(n);
}
```

# --answers--

- >
- <
- ~/=
- /=
- *=

# --solutions--

```dart
void main() {
  int n = 64;
  while (n > 1) {
    n ~/= 2;
  }
  print(n);
}
```

# --output--

1
