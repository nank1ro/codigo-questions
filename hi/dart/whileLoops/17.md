---
language: dart
exerciseType: 1
---

# --instructions--

`30` से बड़ा पहला नंबर खोजने के लिए `while` लूप का उपयोग करें जो `3` और `5` दोनों से विभाज्य हो। वह नंबर लौटाएँ।

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` का मान `45` होना चाहिए

```dart
  test('test1', () {
    expect(computeValue(), 45, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int result = 31;
while (result % 3 != 0 || result % 5 != 0) {
  result++;
}
```
