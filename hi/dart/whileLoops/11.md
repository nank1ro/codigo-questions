---
language: dart
exerciseType: 1
---

# --instructions--

`0` से शुरू होने वाले वेरिएबल `total` में `3` जोड़ते रहने के लिए `do-while` लूप का उपयोग करें, जब तक `total` `12` तक न पहुँच जाए या उससे अधिक न हो जाए। `total` लौटाएँ।

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` का मान `12` होना चाहिए

```dart
  test('test1', () {
    expect(computeValue(), 12, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 0;
do {
  total += 3;
} while (total < 12);
```
