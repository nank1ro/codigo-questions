---
language: dart
exerciseType: 1
---

# --instructions--

`1` से `10` (सम्मिलित) तक के नंबरों का योग गणना करने के लिए `while` लूप का उपयोग करें और परिणाम लौटाएँ।

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` का मान `55` होना चाहिए

```dart
  test('test1', () {
    expect(computeValue(), 55, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sum = 0;
int i = 1;
while (i <= 10) {
  sum += i;
  i++;
}
```
