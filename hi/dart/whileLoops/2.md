---
language: dart
exerciseType: 2
---

# --description--

एक काउंटर वेरिएबल नियंत्रित करता है कि `while` लूप कितनी बार चलता है।

```dart
int count = 0;
while (count < 5) {
  count++;
}
print(count); // 5
```

काउंटर `0` से शुरू होता है, और लूप प्रत्येक पुनरावृत्ति में इसे तब तक बढ़ाता है जब तक `count < 5` `false` न हो जाए।

# --instructions--

उस लूप को पूरा करें जो `1` से `5` तक गिनता है और प्रत्येक मान प्रिंट करता है।

# --seed--

```dart
void main() {
  int count = [/];
  while (count [/] 5) {
    print(count);
    [/];
  }
}
```

# --answers--

- 1
- 0
- <=
- <
- count++
- count--
- count += 2

# --solutions--

```dart
void main() {
  int count = 1;
  while (count <= 5) {
    print(count);
    count++;
  }
}
```

# --output--

1
2
3
4
5
