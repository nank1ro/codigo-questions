---
language: dart
exerciseType: 1
---

# --description--

enum values शानदार **map keys** बनती हैं: ये यूनिक होती हैं, इन्हें compare करना आसान होता है, और कंपाइलर यह सुनिश्चित करता है कि आप केवल वास्तविक values का ही उपयोग करें। enum को key type के रूप में उपयोग करते हुए map घोषित करें और `[]` से values खोजें:

```dart
enum Direction { north, south, east, west }

Map<Direction, String> arrows = {
  Direction.north: '^',
  Direction.south: 'v',
  Direction.east: '>',
  Direction.west: '<',
};

print(arrows[Direction.east]); // >
```

किसी भी map की तरह, key न मिलने पर लुकअप `null` लौटाता है, इसलिए एक फ़ॉलबैक देने के लिए `??` का उपयोग करें।

# --instructions--

`enum Size { small, medium, large }` दिए जाने पर, एक टॉप-लेवल `Map<Size, int>` जिसका नाम `prices` हो, घोषित करें जिसमें `small` की कीमत `2`, `medium` की `3` और `large` की `4` हो। फिर एक फ़ंक्शन `totalPrice(List<Size> order)` लिखें जो `order` में मौजूद हर size की कीमतों का योग लौटाए।

# --seed--

```dart
enum Size { small, medium, large }
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`prices[Size.medium]`, `3` होना चाहिए

```dart
  test('test1', () {
    expect(prices[Size.medium], 3, reason: '--err-t1--');
  });
```

`totalPrice([Size.small, Size.large])`, `6` होना चाहिए

```dart
  test('test2', () {
    expect(totalPrice([Size.small, Size.large]), 6, reason: '--err-t2--');
  });
```

`totalPrice([Size.medium, Size.medium, Size.small])`, `8` होना चाहिए

```dart
  test('test3', () {
    expect(totalPrice([Size.medium, Size.medium, Size.small]), 8, reason: '--err-t3--');
  });
```

`totalPrice([])`, `0` होना चाहिए

```dart
  test('test4', () {
    expect(totalPrice([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Size { small, medium, large }

Map<Size, int> prices = {
  Size.small: 2,
  Size.medium: 3,
  Size.large: 4,
};

int totalPrice(List<Size> order) {
  int total = 0;
  for (var size in order) {
    total += prices[size] ?? 0;
  }
  return total;
}
```
