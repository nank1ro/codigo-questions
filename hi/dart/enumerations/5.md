---
language: dart
exerciseType: 4
---

# --description--

`values` पर लूप चलाना किसी enum के हर मान को प्रोसेस करने का सामान्य तरीका है। लूप के अंदर वर्तमान मान किसी भी अन्य ऑब्जेक्ट की तरह व्यवहार करता है, इसलिए आप उसका `index` और `name` पढ़कर उन्हें सीधे स्ट्रिंग इंटरपोलेशन में उपयोग कर सकते हैं:

```dart
enum Direction { north, south, east, west }

for (var direction in Direction.values) {
  print('${direction.index}: ${direction.name}');
}
```

# --instructions--

enum `Level` घोषित करने के लिए आइटम्स को सही क्रम में लगाएँ और हर value के लिए उसका index और नाम प्रिंट करें। enum को `main` से पहले घोषित करें। प्रोग्राम को यह प्रिंट करना चाहिए:

```
0: low
1: medium
2: high
```

# --answers--

```dart
enum Level { low, medium, high }
```

```dart
void main() {
```

```dart
  for (var level in Level.values) {
```

```dart
    print('${level.index}: ${level.name}');
```

```dart
  }
}
```

# --solutions--

```dart
enum Level { low, medium, high }
void main() {
  for (var level in Level.values) {
    print('${level.index}: ${level.name}');
  }
}
```

# --output--

0: low
1: medium
2: high
