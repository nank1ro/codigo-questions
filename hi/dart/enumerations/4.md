---
language: dart
exerciseType: 1
---

# --description--

चूँकि `values` एक list है, आप इसे उन list methods के साथ जोड़ सकते हैं जिन्हें आप पहले से जानते हैं। उदाहरण के लिए `.name` के साथ `.map()` का उपयोग करके values को strings की list में बदला जा सकता है:

```dart
enum Direction { north, south, east, west }

List<String> names = Direction.values.map((d) => d.name).toList();
print(names); // [north, south, east, west]
```

# --instructions--

values `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday` और `sunday` को इसी क्रम में रखने वाला एक enum `Weekday` घोषित करें। फिर एक फ़ंक्शन `dayNames()` लिखें जो घोषणा क्रम में हर weekday का नाम रखने वाली `List<String>` लौटाए।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Weekday.values.length`, `7` होना चाहिए

```dart
  test('test1', () {
    expect(Weekday.values.length, 7, reason: '--err-t1--');
  });
```

`Weekday.values[2]`, `Weekday.wednesday` होना चाहिए

```dart
  test('test2', () {
    expect(Weekday.values[2], Weekday.wednesday, reason: '--err-t2--');
  });
```

`dayNames()` को सातों नाम क्रम में लौटाने चाहिए

```dart
  test('test3', () {
    expect(dayNames(), ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Weekday { monday, tuesday, wednesday, thursday, friday, saturday, sunday }

List<String> dayNames() {
  return Weekday.values.map((day) => day.name).toList();
}
```
