---
language: dart
exerciseType: 1
---

# --description--

एक enum `implements` कीवर्ड से एक **interface को implement** कर सकता है। फिर enum, interface द्वारा घोषित हर member को उपलब्ध कराने का वादा करता है, और उसकी values का उपयोग वहाँ किया जा सकता है जहाँ भी वह interface type अपेक्षित हो:

```dart
abstract class Describable {
  String describe();
}

enum Animal implements Describable {
  dog,
  cat;

  @override
  String describe() => 'I am a $name';
}

Describable pet = Animal.cat;
print(pet.describe()); // I am a cat
```

interface में घोषित किया गया getter, या तो एक getter से या उसी नाम की एक `final` field से implement किया जा सकता है।

# --instructions--

एक enum `Ticket` घोषित करें जो `Priced` को implement करे, जिसमें एक `final int` field `price` हो और values `child(5)`, `adult(12)` तथा `senior(8)` इसी क्रम में हों। फिर एक फ़ंक्शन `total(List<Priced> items)` लिखें जो सभी items की कीमतों का योग लौटाए।

# --seed--

```dart
abstract class Priced {
  int get price;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Ticket.adult.price`, `12` होना चाहिए

```dart
  test('test1', () {
    expect(Ticket.adult.price, 12, reason: '--err-t1--');
  });
```

एक `Ticket` को `Priced` के रूप में उपयोग किया जा सकता है

```dart
  test('test2', () {
    Priced item = Ticket.senior;
    expect(item.price, 8, reason: '--err-t2--');
  });
```

`total([Ticket.child, Ticket.adult, Ticket.senior])`, `25` होना चाहिए

```dart
  test('test3', () {
    expect(total([Ticket.child, Ticket.adult, Ticket.senior]), 25, reason: '--err-t3--');
  });
```

`total([])`, `0` होना चाहिए

```dart
  test('test4', () {
    expect(total([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
abstract class Priced {
  int get price;
}

enum Ticket implements Priced {
  child(5),
  adult(12),
  senior(8);

  @override
  final int price;

  const Ticket(this.price);
}

int total(List<Priced> items) {
  int sum = 0;
  for (var item in items) {
    sum += item.price;
  }
  return sum;
}
```
