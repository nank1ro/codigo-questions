---
language: dart
exerciseType: 2
---

# --description--

Dart 3 से `switch` का उपयोग एक **expression** के रूप में भी किया जा सकता है जो एक value देता है। हर case को `pattern => value` के रूप में लिखा जाता है और cases को कॉमा से अलग किया जाता है, बिना किसी `case` कीवर्ड या `break` के:

```dart
enum Direction { north, south, east, west }

String arrow(Direction direction) => switch (direction) {
      Direction.north => '^',
      Direction.south => 'v',
      Direction.east => '>',
      Direction.west => '<',
    };
```

स्टेटमेंट रूप की तरह ही, enum पर switch expression भी exhaustive होनी चाहिए।

# --instructions--

switch expression को पूरा करें ताकि `label` हर size का पहला अक्षर लौटाए, फिर `Size.large` का label प्रिंट करें।

# --seed--

```dart
enum Size { small, medium, large }

String label(Size size) => [/] (size) {
      Size.small [/] 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --answers--

- switch
- =>
- case
- when
- ->
- :

# --solutions--

```dart
enum Size { small, medium, large }

String label(Size size) => switch (size) {
      Size.small => 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --output--

L
