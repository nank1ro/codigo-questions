---
language: dart
exerciseType: 1
---

# --description--

Dart 2.17 से एक enum, क्लास की तरह ही, **fields** और एक **constructor** घोषित कर सकता है। इसे *enhanced enum* कहा जाता है। हर value फिर अपने खुद के आर्ग्युमेंट्स constructor को पास करती है:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);
}

print(Planet.mars.moons); // 2
```

तीन नियमों पर ध्यान दें: values की लिस्ट **सेमीकोलन** `;` पर समाप्त होती है, fields ज़रूर `final` होने चाहिए, और constructor ज़रूर `const` होना चाहिए।

# --instructions--

एक `final int` field `cents` और values `penny(1)`, `nickel(5)`, `dime(10)` तथा `quarter(25)` को इसी क्रम में रखने वाला एक enum `Coin` घोषित करें।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Coin.penny.cents`, `1` होना चाहिए और `Coin.nickel.cents`, `5` होना चाहिए

```dart
  test('test1', () {
    expect(Coin.penny.cents, 1, reason: '--err-t1--');
    expect(Coin.nickel.cents, 5, reason: '--err-t2--');
  });
```

`Coin.dime.cents`, `10` होना चाहिए

```dart
  test('test2', () {
    expect(Coin.dime.cents, 10, reason: '--err-t3--');
  });
```

`Coin.quarter.cents`, `25` होना चाहिए

```dart
  test('test3', () {
    expect(Coin.quarter.cents, 25, reason: '--err-t4--');
  });
```

`Coin.values` में 4 values होनी चाहिए, जिनमें `penny` पहली हो

```dart
  test('test4', () {
    expect(Coin.values.length, 4, reason: '--err-t5--');
    expect(Coin.values[0], Coin.penny, reason: '--err-t6--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Coin {
  penny(1),
  nickel(5),
  dime(10),
  quarter(25);

  final int cents;

  const Coin(this.cents);
}
```
