---
language: dart
exerciseType: 2
---

# --instructions--

enhanced enum `Planet` को पूरा करें ताकि हर value अपने चंद्रमाओं की संख्या संग्रहीत करे, फिर `Planet.mars` के चंद्रमाओं की संख्या प्रिंट करें।

# --seed--

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2)[/]

  final int moons;

  [/] Planet(this.moons);
}

void main() {
  print(Planet.mars.moons);
}
```

# --answers--

- ;
- const
- ,
- final
- static
- new

# --solutions--

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);
}

void main() {
  print(Planet.mars.moons);
}
```

# --output--

2
