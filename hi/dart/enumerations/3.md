---
language: dart
exerciseType: 2
---

# --description--

हर enum के पास एक constant list भी होती है जिसे `values` कहा जाता है, जो घोषणा के क्रम में उसके सभी values रखती है। आप इसे किसी भी list की तरह index कर सकते हैं, इसकी `length` पढ़ सकते हैं, या `for-in` से इस पर loop चला सकते हैं:

```dart
enum Direction { north, south, east, west }

print(Direction.values.length); // 4
print(Direction.values[1]);     // Direction.south

for (var direction in Direction.values) {
  print(direction.name);
}
```

# --instructions--

कोड को पूरा करें ताकि `Planet.earth` का नाम, उसका index और ग्रहों की कुल संख्या प्रिंट हो।

# --seed--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.[/]);
  print(Planet.earth.[/]);
  print(Planet.[/].length);
}
```

# --answers--

- name
- index
- values
- ordinal
- position
- count

# --solutions--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.name);
  print(Planet.earth.index);
  print(Planet.values.length);
}
```

# --output--

earth
2
3
