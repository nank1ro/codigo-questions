---
language: dart
exerciseType: 2
---

# --description--

कंट्रोलर नामित कंस्ट्रक्टर `StreamController<T>.broadcast()` से सीधे एक ब्रॉडकास्ट स्ट्रीम बना सकता है। उसकी `stream` कितने भी सुनने वालों को स्वीकार करती है, और हर इवेंट उन सबको उसी क्रम में पहुँचाया जाता है जिस क्रम में उन्होंने सदस्यता ली:

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>.broadcast();
  controller.stream.listen((msg) => print('first: $msg'));
  controller.stream.listen((msg) => print('second: $msg'));
  controller.add('hi');
  controller.close();
}
// first: hi
// second: hi
```

हर ब्रॉडकास्ट स्ट्रीम की तरह यह भी बफ़र नहीं करती: किसी सुनने वाले की सदस्यता से पहले जोड़े गए इवेंट्स उसके लिए खो जाते हैं।

# --instructions--

कोड पूरा करो ताकि एक ब्रॉडकास्ट कंट्रोलर बने, दो सुनने वाले `A` और `B` सदस्यता लें, मान `1` और `2` भेजे जाएँ, और फिर कंट्रोलर बंद हो, ताकि प्रोग्राम `A got 1`, `B got 1`, `A got 2`, `B got 2` प्रिंट करे।

# --seed--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.[/]();
  controller.stream.[/]((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.[/](1);
  controller.add(2);
  controller.[/]();
}
```

# --answers--

- broadcast
- listen
- add
- close
- asBroadcastStream
- subscribe
- sink

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.broadcast();
  controller.stream.listen((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
```

# --output--

A got 1
B got 1
A got 2
B got 2
