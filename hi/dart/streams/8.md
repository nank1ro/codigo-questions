---
language: dart
exerciseType: 2
---

# --description--

चूँकि `where`, `map` और `take` सभी एक स्ट्रीम लौटाते हैं, तुम इन्हें चेन कर सकते हो और परिणाम को लिस्ट के रूप में पाने के लिए `toList()` से ख़त्म कर सकते हो। केवल आख़िरी `toList()` को `await` चाहिए, क्योंकि वही एकमात्र कॉल है जो `Future` लौटाती है:

```dart
final numbers = Stream.fromIterable([5, 12, 8, 20, 3]);
final big = await numbers.where((n) => n > 6).take(2).toList();
print(big); // [12, 8]
```

# --instructions--

कोड पूरा करो ताकि केवल ठीक `3` अक्षरों वाले शब्द रखे जाएँ, उनमें से पहले `2` के बाद रुका जाए, और उन्हें एक लिस्ट में इकट्ठा किया जाए, ताकि प्रोग्राम `[sun, sky]` प्रिंट करे।

# --seed--

```dart
Future<void> main() async {
  final words = Stream.fromIterable(['sun', 'moon', 'star', 'sky', 'fog']);
  final short = await words.[/]((w) => w.length == 3).[/](2).[/]();
  print(short);
}
```

# --answers--

- where
- take
- toList
- map
- skip
- first

# --solutions--

```dart
Future<void> main() async {
  final words = Stream.fromIterable(['sun', 'moon', 'star', 'sky', 'fog']);
  final short = await words.where((w) => w.length == 3).take(2).toList();
  print(short);
}
```

# --output--

[sun, sky]
