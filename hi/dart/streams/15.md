---
language: dart
exerciseType: 4
---

# --description--

स्ट्रीम मानों के साथ-साथ **त्रुटियाँ** भी ले जा सकती है। `async*` जनरेटर के अंदर `throw` एक त्रुटि इवेंट भेजता है और स्ट्रीम को ख़त्म कर देता है; `StreamController` `addError` से ऐसी इवेंट भेज सकता है।

उपयोग करने वाले पक्ष पर, `await for` लूप त्रुटि को वहीं दोबारा फेंकता है जहाँ लूप है, इसलिए तुम उसे लूप के चारों ओर सामान्य `try`/`catch` से सँभालते हो:

```dart
Stream<int> risky() async* {
  yield 1;
  throw StateError('sensor offline');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('caught: $e');
  }
}
// 1
// caught: Bad state: sensor offline
```

`listen` के साथ इसके बजाय एक `onError` कॉलबैक पास करो: `stream.listen(print, onError: (e) => print('caught: $e'));`

# --instructions--

आइटम्स को क्रम में लगाओ ताकि एक `async*` फ़ंक्शन `risky` बने जो `1` देता है और फिर `FormatException('bad')` फेंकता है, और एक `async` `main` बने जो `risky()` का हर मान `try`/`catch` के अंदर प्रिंट करता है, त्रुटि पकड़ने पर `error: ` और उसके बाद त्रुटि प्रिंट करता है, और पूरे `try`/`catch` ब्लॉक के बाद, उसके बाहर, `finished` प्रिंट करता है। `risky` को `main` से पहले परिभाषित करो। प्रोग्राम को यह प्रिंट करना चाहिए:

```
1
error: FormatException: bad
finished
```

# --answers--

```dart
Stream<int> risky() async* {
```

```dart
  yield 1;
```

```dart
  throw FormatException('bad');
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  try {
```

```dart
    await for (final n in risky()) {
```

```dart
      print(n);
```

```dart
    }
```

```dart
  } catch (e) {
```

```dart
    print('error: $e');
```

```dart
  }
```

```dart
  print('finished');
```

```dart
}
```

# --solutions--

```dart
Stream<int> risky() async* {
  yield 1;
  throw FormatException('bad');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('error: $e');
  }
  print('finished');
}
```

# --output--

1
error: FormatException: bad
finished
