---
language: dart
exerciseType: 1
---

# --description--

`Stream.fromIterable` को सारे मान पहले से चाहिए। मानों को एक-एक करके **उत्पन्न** करने के लिए एक **असिंक्रोनस जनरेटर** लिखो: ऐसा फ़ंक्शन जिसका बॉडी `async*` से चिह्नित हो और जिसका रिटर्न टाइप `Stream<T>` हो। उसके अंदर `yield` स्ट्रीम को एक इवेंट भेजता है:

```dart
Stream<int> countTo(int n) async* {
  for (var i = 1; i <= n; i++) {
    yield i;
  }
}
```

जब तुम `countTo(3)` कॉल करते हो तब बॉडी नहीं चलता: वह आलस्य से चलता है, जैसे-जैसे सुनने वाला मान माँगता है, और बॉडी ख़त्म होने पर स्ट्रीम समाप्त हो जाती है।

हर इवेंट को एक `List` में इकट्ठा करने के लिए `toList()` कॉल करो। यह `Future<List<T>>` लौटाता है, इसलिए तुम इसे `await` करते हो:

```dart
final values = await countTo(3).toList();
print(values); // [1, 2, 3]
```

# --instructions--

एक `async*` फ़ंक्शन `countdown` लिखो जो एक `int` `from` लेता है और एक `Stream<int>` लौटाता है जो `from` से नीचे `1` तक के मान देता है। यदि `from` `0` या उससे कम है, तो स्ट्रीम कुछ नहीं भेजती।

उदाहरण:
```dart
print(await countdown(3).toList()); // [3, 2, 1]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countdown(3)` को `3`, `2`, `1` भेजना चाहिए

```dart
  test('test1', () async {
    expect(await countdown(3).toList(), [3, 2, 1], reason: '--err-t1--');
  });
```

`countdown(1)` को केवल `1` भेजना चाहिए

```dart
  test('test2', () async {
    expect(await countdown(1).toList(), [1], reason: '--err-t2--');
  });
```

`countdown(0)` को कुछ नहीं भेजना चाहिए और `countdown` को `Stream<int>` लौटाना चाहिए

```dart
  test('test3', () async {
    expect(await countdown(0).toList(), <int>[], reason: '--err-t3--');
    expect(countdown(2), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> countdown(int from) async* {
  for (var i = from; i >= 1; i--) {
    yield i;
  }
}
```
