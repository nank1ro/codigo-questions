---
language: dart
exerciseType: 1
---

# --description--

`await for` लूप प्रिंट करने से ज़्यादा कर सकता है: यह लूप से पहले घोषित किसी वेरिएबल को अपडेट कर सकता है। जो फ़ंक्शन किसी स्ट्रीम का उपयोग करके कोई परिणाम निकालता है उसे `async` से चिह्नित होना चाहिए, और वह उस परिणाम का `Future` लौटाता है:

```dart
Future<int> count(Stream<String> words) async {
  var total = 0;
  await for (final word in words) {
    total += word.length;
  }
  return total;
}
```

फ़ंक्शन `return` तक तभी पहुँचता है जब स्ट्रीम समाप्त हो जाती है, इसलिए कॉल करने वाले को अंतिम मान तब मिलता है जब वह उस future का इंतज़ार करता है:

```dart
print(await count(Stream.fromIterable(['hi', 'dart']))); // 6
```

# --instructions--

फ़ंक्शन `scores()` एक `Stream<int>` लौटाता है। `scores()` पर `await for` लूप का उपयोग करके हर मान को `total` में जोड़ो।

# --before-seed--

```dart
Stream<int> scores() => Stream.fromIterable([4, 7, 1, 9]);

Future<dynamic> result() async {
```

# --seed--

```dart
var total = 0;
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` `21` होना चाहिए

```dart
  test('test1', () async {
    expect(await result(), 21, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var total = 0;
await for (final score in scores()) {
  total += score;
}
```
