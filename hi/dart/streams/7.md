---
language: dart
exerciseType: 1
---

# --description--

`Iterable` की तरह, `Stream` के पास भी ऐसे मेथड हैं जो किसी मौजूदा स्ट्रीम से एक **नई स्ट्रीम** बनाते हैं:

- `map` हर इवेंट को बदलता है
- `where` केवल उन इवेंट्स को रखता है जो शर्त पूरी करते हैं
- `take` दी गई संख्या के इवेंट्स के बाद रुक जाता है

```dart
final numbers = Stream.fromIterable([1, 2, 3, 4, 5, 6]);
final firstOdds = numbers.where((n) => n.isOdd).take(2);
print(await firstOdds.toList()); // [1, 3]
```

ये मेथड **आलसी** हैं: जब तक कोई परिणामी स्ट्रीम को नहीं सुनता, कुछ नहीं चलता। इन्हें चेन किया जा सकता है, और स्रोत स्ट्रीम कभी नहीं बदलती।

# --instructions--

एक फ़ंक्शन `evenSquares` लिखो जो एक `Stream<int>` `source` लेता है और एक `Stream<int>` लौटाता है जिसमें `source` के हर **सम** मान का वर्ग उसी क्रम में हो।

उदाहरण:
```dart
final source = Stream.fromIterable([1, 2, 3, 4]);
print(await evenSquares(source).toList()); // [4, 16]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`1, 2, 3, 4` वाली स्ट्रीम पर `evenSquares` को `4, 16` भेजना चाहिए

```dart
  test('test1', () async {
    expect(await evenSquares(Stream.fromIterable([1, 2, 3, 4])).toList(), [4, 16], reason: '--err-t1--');
  });
```

`6, 5, 10` वाली स्ट्रीम पर `evenSquares` को `36, 100` भेजना चाहिए

```dart
  test('test2', () async {
    expect(await evenSquares(Stream.fromIterable([6, 5, 10])).toList(), [36, 100], reason: '--err-t2--');
  });
```

`1, 3` वाली स्ट्रीम पर `evenSquares` को कुछ नहीं भेजना चाहिए और उसे `Stream<int>` लौटाना चाहिए

```dart
  test('test3', () async {
    expect(await evenSquares(Stream.fromIterable([1, 3])).toList(), <int>[], reason: '--err-t3--');
    expect(evenSquares(Stream.fromIterable([2])), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> evenSquares(Stream<int> source) {
  return source.where((n) => n.isEven).map((n) => n * n);
}
```
