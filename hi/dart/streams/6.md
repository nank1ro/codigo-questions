---
language: dart
exerciseType: 4
---

# --description--

एक `async*` जनरेटर `yield*` (yield-star) से **किसी दूसरी स्ट्रीम के हर इवेंट** को आगे भेज सकता है। यह उस `await for` लूप जैसा है जो हर मान yield करता है, बस एक ही पंक्ति में:

```dart
Stream<int> ones() async* {
  yield 1;
  yield 1;
}

Stream<int> sequence() async* {
  yield 0;
  yield* ones();
  yield 2;
}
// sequence() emits 0, 1, 1, 2
```

भीतरी स्ट्रीम समाप्त होते ही बाहरी स्ट्रीम अपने `yield` के साथ आगे बढ़ती है।

# --instructions--

आइटम्स को क्रम में लगाओ ताकि एक `async*` फ़ंक्शन `pair` बने जो `1` और `2` देता है, एक `async*` फ़ंक्शन `all` बने जो `0`, फिर `pair()` का हर मान, फिर `3` देता है, और एक `main` बने जो `all()` का हर मान प्रिंट करता है। पहले `pair`, फिर `all`, फिर `main` परिभाषित करो। प्रोग्राम को यह प्रिंट करना चाहिए:

```
0
1
2
3
```

# --answers--

```dart
Stream<int> pair() async* {
```

```dart
  yield 1;
```

```dart
  yield 2;
```

```dart
}
```

```dart
Stream<int> all() async* {
```

```dart
  yield 0;
```

```dart
  yield* pair();
```

```dart
  yield 3;
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  await for (final n in all()) {
```

```dart
    print(n);
```

```dart
  }
```

```dart
}
```

# --solutions--

```dart
Stream<int> pair() async* {
  yield 1;
  yield 2;
}

Stream<int> all() async* {
  yield 0;
  yield* pair();
  yield 3;
}

Future<void> main() async {
  await for (final n in all()) {
    print(n);
  }
}
```

# --output--

0
1
2
3
