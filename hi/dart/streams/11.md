---
language: dart
exerciseType: 1
---

# --description--

जनरेटर एक ही फ़ंक्शन के अंदर से इवेंट्स बनाते हैं। जब इवेंट्स **कहीं और** से आते हैं (किसी बटन से, किसी नेटवर्क कॉलबैक से, किसी दूसरे ऑब्जेक्ट से) तो तुम्हें एक **`StreamController`** चाहिए। यह `dart:async` लाइब्रेरी में रहता है, इसलिए फ़ाइल की शुरुआत `import 'dart:async';` से होनी चाहिए।

कंट्रोलर एक स्ट्रीम का मालिक होता है और तुम्हें उसमें इवेंट्स डालने देता है:

```dart
import 'dart:async';

Stream<int> dice() {
  final controller = StreamController<int>();
  controller.add(4);
  controller.add(2);
  controller.close();
  return controller.stream;
}
```

- `add(value)` एक इवेंट भेजता है
- `close()` स्ट्रीम को ख़त्म करता है; इसे भूलने का मतलब है कि सुनने वाले हमेशा इंतज़ार करते रहेंगे
- `stream` वह `Stream` है जिसे सुनने वाले उपयोग करते हैं

किसी के सुनने से पहले जोड़े गए इवेंट्स एक बफ़र में रखे जाते हैं, इसलिए ऊपर वाला कोड सुरक्षित है: बाद में आने वाले सुनने वाले को भी `4` और `2` मिलते हैं।

# --instructions--

एक फ़ंक्शन `announce` लिखो जो एक `List<String>` `names` लेता है और `StreamController` से बनी एक `Stream<String>` लौटाता है: हर नाम को बड़े अक्षरों में जोड़ो, कंट्रोलर बंद करो और उसकी स्ट्रीम लौटाओ। `dart:async` का इम्पोर्ट याद रखो।

उदाहरण:
```dart
print(await announce(['ada', 'linus']).toList()); // [ADA, LINUS]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`announce(['ada', 'linus'])` को `'ADA'`, `'LINUS'` भेजना चाहिए

```dart
  test('test1', () async {
    expect(await announce(['ada', 'linus']).toList(), ['ADA', 'LINUS'], reason: '--err-t1--');
  });
```

`announce(['Dart'])` को `'DART'` भेजना चाहिए

```dart
  test('test2', () async {
    expect(await announce(['Dart']).toList(), ['DART'], reason: '--err-t2--');
  });
```

`announce([])` को कुछ नहीं भेजना चाहिए और ख़त्म हो जाना चाहिए, और `announce` को `Stream<String>` लौटाना चाहिए

```dart
  test('test3', () async {
    expect(await announce([]).toList(), <String>[], reason: '--err-t3--');
    expect(announce(['x']), isA<Stream<String>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
import 'dart:async';

Stream<String> announce(List<String> names) {
  final controller = StreamController<String>();
  for (final name in names) {
    controller.add(name.toUpperCase());
  }
  controller.close();
  return controller.stream;
}
```
