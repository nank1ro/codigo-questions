---
language: dart
exerciseType: 1
---

# --instructions--
एक ऐसा फ़ंक्शन लिखें जो `for` लूप का उपयोग करके 1 से `n` तक (सम्मिलित) सभी पूर्णांकों का योग लौटाए।

# --before-seed--
```dart
int sumTo(int n) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
1 से 5 तक का योग 15 है
```dart
    test('sum 1..5', () {
      expect(sumTo(5), 15, reason: '--err-t1--');
    });
```

1 से 1 तक का योग 1 है
```dart
    test('sum 1..1', () {
      expect(sumTo(1), 1, reason: '--err-t2--');
    });
```

1 से 10 तक का योग 55 है
```dart
    test('sum 1..10', () {
      expect(sumTo(10), 55, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = 1; i <= n; i++) {
    result += i;
  }
```
