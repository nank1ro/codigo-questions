---
language: dart
exerciseType: 1
---

# --instructions--
एक ऐसा फ़ंक्शन लिखें जो `start` से `end` (दोनों सम्मिलित) की सीमा में सम संख्याओं की गिनती लौटाए।

# --before-seed--
```dart
int countEvens(int start, int end) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
1 से 10 की सीमा में 5 सम संख्याएँ हैं
```dart
    test('1 to 10', () {
      expect(countEvens(1, 10), 5, reason: '--err-t1--');
    });
```

2 से 6 की सीमा में 3 सम संख्याएँ हैं
```dart
    test('2 to 6', () {
      expect(countEvens(2, 6), 3, reason: '--err-t2--');
    });
```

1 से 1 की सीमा में 0 सम संख्याएँ हैं
```dart
    test('1 to 1', () {
      expect(countEvens(1, 1), 0, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = start; i <= end; i++) {
    if (i % 2 == 0) result++;
  }
```
