---
language: dart
exerciseType: 1
---

# --instructions--
एक ऐसा फ़ंक्शन लिखें जो एक `List<String>` लेता है और `for-in` लूप का उपयोग करके सभी तत्वों के बीच एक स्पेस लगाकर एक ही स्ट्रिंग में जोड़कर लौटाता है।

# --before-seed--
```dart
String joinWords(List<String> words) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
तीन शब्दों को स्पेस के साथ जोड़ता है
```dart
    test('three words', () {
      expect(joinWords(['hello', 'dart', 'world']), 'hello dart world', reason: '--err-t1--');
    });
```

एकल शब्द वही शब्द लौटाता है
```dart
    test('single word', () {
      expect(joinWords(['only']), 'only', reason: '--err-t2--');
    });
```

खाली सूची खाली स्ट्रिंग लौटाती है
```dart
    test('empty list', () {
      expect(joinWords([]), '', reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  String result = '';
  for (var word in words) {
    if (result.isEmpty) {
      result = word;
    } else {
      result += ' $word';
    }
  }
```
