---
language: dart
exerciseType: 1
---

# --instructions--
एक ऐसा फ़ंक्शन लिखें जो गुणन तालिका में (`row`, `col`) स्थान पर मान लौटाए। नेस्टेड `for` लूप का उपयोग करें जहाँ बाहरी लूप पंक्तियों पर और आंतरिक लूप स्तंभों पर पुनरावृत्ति करे (दोनों 1-इंडेक्स्ड)। दी गई स्थिति पर गुणनफल लौटाएँ।

# --before-seed--
```dart
int multiplicationCell(int row, int col) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
पंक्ति 3, स्तंभ 4 का मान 12 है
```dart
    test('3x4 = 12', () {
      expect(multiplicationCell(3, 4), 12, reason: '--err-t1--');
    });
```

पंक्ति 1, स्तंभ 1 का मान 1 है
```dart
    test('1x1 = 1', () {
      expect(multiplicationCell(1, 1), 1, reason: '--err-t2--');
    });
```

पंक्ति 7, स्तंभ 8 का मान 56 है
```dart
    test('7x8 = 56', () {
      expect(multiplicationCell(7, 8), 56, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int r = 1; r <= row; r++) {
    for (int c = 1; c <= col; c++) {
      if (r == row && c == col) {
        result = r * c;
      }
    }
  }
```
