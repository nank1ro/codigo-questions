---
language: dart
exerciseType: 1
---

# --instructions--
`for` लूप का उपयोग करके एक गैर-ऋणात्मक पूर्णांक `n` का फ़ैक्टोरियल गणना करने वाला फ़ंक्शन लिखें। 0 का फ़ैक्टोरियल 1 होता है।

# --before-seed--
```dart
int factorial(int n) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
5 का फ़ैक्टोरियल 120 है
```dart
    test('5! = 120', () {
      expect(factorial(5), 120, reason: '--err-t1--');
    });
```

0 का फ़ैक्टोरियल 1 है
```dart
    test('0! = 1', () {
      expect(factorial(0), 1, reason: '--err-t2--');
    });
```

1 का फ़ैक्टोरियल 1 है
```dart
    test('1! = 1', () {
      expect(factorial(1), 1, reason: '--err-t3--');
    });
```

6 का फ़ैक्टोरियल 720 है
```dart
    test('6! = 720', () {
      expect(factorial(6), 720, reason: '--err-t4--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 1;
  for (int i = 2; i <= n; i++) {
    result *= i;
  }
```
