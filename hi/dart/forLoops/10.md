---
language: dart
exerciseType: 1
---

# --instructions--
एक ऐसा फ़ंक्शन लिखें जो एक `List<int>` लेता है और एक नई सूची लौटाता है जहाँ हर तत्व को 2 से गुणा किया गया हो।

# --before-seed--
```dart
List<int> doubleElements(List<int> items) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
हर तत्व को दोगुना करता है
```dart
    test('doubles elements', () {
      expect(doubleElements([1, 2, 3]), [2, 4, 6], reason: '--err-t1--');
    });
```

खाली सूची खाली सूची लौटाती है
```dart
    test('empty list', () {
      expect(doubleElements([]), [], reason: '--err-t2--');
    });
```

एकल तत्व वाली सूची
```dart
    test('single element', () {
      expect(doubleElements([5]), [10], reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  List<int> result = [];
  for (var item in items) {
    result.add(item * 2);
  }
```
