---
language: dart
exerciseType: 1
---

# --instructions--

`if-else` श्रृंखला का उपयोग करके `ageGroup(int age)` फ़ंक्शन लिखें जो `age < 13` के लिए `'child'`, `age < 18` के लिए `'teenager'`, `age < 65` के लिए `'adult'`, और `age >= 65` के लिए `'senior'` लौटाए।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`ageGroup(8)` को `'child'` लौटाना चाहिए

```dart
  test('test1', () {
    expect(ageGroup(8), 'child', reason: '--err-t1--');
  });
```

`ageGroup(15)` को `'teenager'` लौटाना चाहिए

```dart
  test('test2', () {
    expect(ageGroup(15), 'teenager', reason: '--err-t2--');
  });
```

`ageGroup(30)` को `'adult'` लौटाना चाहिए

```dart
  test('test3', () {
    expect(ageGroup(30), 'adult', reason: '--err-t3--');
  });
```

`ageGroup(70)` को `'senior'` लौटाना चाहिए

```dart
  test('test4', () {
    expect(ageGroup(70), 'senior', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String ageGroup(int age) {
  if (age < 13) {
    return 'child';
  } else if (age < 18) {
    return 'teenager';
  } else if (age < 65) {
    return 'adult';
  } else {
    return 'senior';
  }
}

void main() {
  print(ageGroup(30));
}
```
