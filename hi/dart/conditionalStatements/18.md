---
language: dart
exerciseType: 1
---

# --instructions--

`if-else` कथन का उपयोग करके `parity(int n)` फ़ंक्शन लिखें जो `n`, `2` से विभाज्य होने पर `'even'` और अन्यथा `'odd'` लौटाए।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parity(4)` को `'even'` लौटाना चाहिए

```dart
  test('test1', () {
    expect(parity(4), 'even', reason: '--err-t1--');
  });
```

`parity(7)` को `'odd'` लौटाना चाहिए

```dart
  test('test2', () {
    expect(parity(7), 'odd', reason: '--err-t2--');
  });
```

`parity(0)` को `'even'` लौटाना चाहिए

```dart
  test('test3', () {
    expect(parity(0), 'even', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String parity(int n) {
  if (n % 2 == 0) {
    return 'even';
  } else {
    return 'odd';
  }
}

void main() {
  print(parity(4));
}
```
