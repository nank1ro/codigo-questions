---
language: dart
exerciseType: 1
---

# --instructions--

नेस्टेड `if` कथनों का उपयोग करके `canVote(int age, bool isCitizen)` फ़ंक्शन लिखें जो केवल तभी `true` लौटाए जब `age >= 18` **और** `isCitizen` का मान `true` हो।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canVote(20, true)` को `true` लौटाना चाहिए

```dart
  test('test1', () {
    expect(canVote(20, true), true, reason: '--err-t1--');
  });
```

`canVote(16, true)` को `false` लौटाना चाहिए

```dart
  test('test2', () {
    expect(canVote(16, true), false, reason: '--err-t2--');
  });
```

`canVote(25, false)` को `false` लौटाना चाहिए

```dart
  test('test3', () {
    expect(canVote(25, false), false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool canVote(int age, bool isCitizen) {
  if (age >= 18) {
    if (isCitizen) {
      return true;
    }
  }
  return false;
}

void main() {
  print(canVote(20, true));
}
```
