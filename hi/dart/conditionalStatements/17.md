---
language: dart
exerciseType: 1
---

# --instructions--

`default` खंड के साथ `switch` कथन का उपयोग करके `trafficLight(String color)` फ़ंक्शन लिखें जो `'green'` के लिए `'Go'`, `'yellow'` के लिए `'Slow down'`, `'red'` के लिए `'Stop'`, और किसी भी अन्य मान के लिए `'Invalid'` लौटाए।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`trafficLight('green')` को `'Go'` लौटाना चाहिए

```dart
  test('test1', () {
    expect(trafficLight('green'), 'Go', reason: '--err-t1--');
  });
```

`trafficLight('yellow')` को `'Slow down'` लौटाना चाहिए

```dart
  test('test2', () {
    expect(trafficLight('yellow'), 'Slow down', reason: '--err-t2--');
  });
```

`trafficLight('red')` को `'Stop'` लौटाना चाहिए

```dart
  test('test3', () {
    expect(trafficLight('red'), 'Stop', reason: '--err-t3--');
  });
```

`trafficLight('blue')` को `'Invalid'` लौटाना चाहिए

```dart
  test('test4', () {
    expect(trafficLight('blue'), 'Invalid', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String trafficLight(String color) {
  switch (color) {
    case 'green':
      return 'Go';
    case 'yellow':
      return 'Slow down';
    case 'red':
      return 'Stop';
    default:
      return 'Invalid';
  }
}

void main() {
  print(trafficLight('green'));
}
```
