---
language: dart
exerciseType: 1
---

# --instructions--

`isHot` नामक एक `bool` चर घोषित करें। `if` कथन का उपयोग करके इसे `true` असाइन करें जब `int` चर `temp` (जो `35` पर सेट है) `30` से बड़ा हो।

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int temp = 35;
bool isHot = false;
```

# --after-seed--

```dart
  return isHot;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHot` का मान `true` होना चाहिए

```dart
  test('test1', () {
    expect(result(), true, reason: '--err-t1--');
    expect(result(), TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int temp = 35;
bool isHot = false;
if (temp > 30) {
  isHot = true;
}
```
