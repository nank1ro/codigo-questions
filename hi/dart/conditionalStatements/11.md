---
language: dart
exerciseType: 1
---

# --instructions--

`isHot` नामक एक शीर्ष-स्तरीय `bool` चर घोषित करें। `if` कथन का उपयोग करके इसे `true` असाइन करें जब शीर्ष-स्तरीय `int` चर `temp` (जो `35` पर सेट है) `30` से बड़ा हो।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`temp` का मान `35` होना चाहिए

```dart
  test('test1', () {
    expect(temp, 35, reason: '--err-t1--');
  });
```

`isHot` का मान `true` होना चाहिए

```dart
  test('test2', () {
    expect(isHot, true, reason: '--err-t2--');
    expect(isHot, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int temp = 35;
bool isHot = false;

void main() {
  if (temp > 30) {
    isHot = true;
  }
  print(isHot);
}
```
