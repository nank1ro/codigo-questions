---
language: dart
exerciseType: 1
---

# --instructions--

`if-else` का उपयोग करके `describe(int n)` फ़ंक्शन लिखें जो `n`, `0` से बड़ा होने पर `'positive'` और अन्यथा `'non-positive'` लौटाए।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`describe(10)` को `'positive'` लौटाना चाहिए

```dart
  test('test1', () {
    expect(describe(10), 'positive', reason: '--err-t1--');
  });
```

`describe(0)` को `'non-positive'` लौटाना चाहिए

```dart
  test('test2', () {
    expect(describe(0), 'non-positive', reason: '--err-t2--');
  });
```

`describe(-5)` को `'non-positive'` लौटाना चाहिए

```dart
  test('test3', () {
    expect(describe(-5), 'non-positive', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String describe(int n) {
  if (n > 0) {
    return 'positive';
  } else {
    return 'non-positive';
  }
}

void main() {
  print(describe(10));
}
```
