---
language: dart
exerciseType: 1
---

# --instructions--

`else if` श्रृंखला का उपयोग करके `letterGrade(int score)` फ़ंक्शन लिखें जो एक अक्षर ग्रेड लौटाए: `score >= 90` के लिए `'A'`, `score >= 80` के लिए `'B'`, `score >= 70` के लिए `'C'`, `score >= 60` के लिए `'D'`, और `60` से नीचे किसी भी मान के लिए `'F'`।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`letterGrade(92)` को `'A'` लौटाना चाहिए

```dart
  test('test1', () {
    expect(letterGrade(92), 'A', reason: '--err-t1--');
  });
```

`letterGrade(85)` को `'B'` लौटाना चाहिए

```dart
  test('test2', () {
    expect(letterGrade(85), 'B', reason: '--err-t2--');
  });
```

`letterGrade(73)` को `'C'` लौटाना चाहिए

```dart
  test('test3', () {
    expect(letterGrade(73), 'C', reason: '--err-t3--');
  });
```

`letterGrade(61)` को `'D'` लौटाना चाहिए

```dart
  test('test4', () {
    expect(letterGrade(61), 'D', reason: '--err-t4--');
  });
```

`letterGrade(45)` को `'F'` लौटाना चाहिए

```dart
  test('test5', () {
    expect(letterGrade(45), 'F', reason: '--err-t5--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String letterGrade(int score) {
  if (score >= 90) {
    return 'A';
  } else if (score >= 80) {
    return 'B';
  } else if (score >= 70) {
    return 'C';
  } else if (score >= 60) {
    return 'D';
  } else {
    return 'F';
  }
}

void main() {
  print(letterGrade(85));
}
```
