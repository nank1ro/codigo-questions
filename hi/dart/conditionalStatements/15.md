---
language: dart
exerciseType: 1
---

# --instructions--

टर्नरी ऑपरेटर का उपयोग करके `absoluteValue(int n)` फ़ंक्शन लिखें जो `n` का निरपेक्ष मान लौटाए। फ़ंक्शन को `n` लौटाना चाहिए जब वह ऋणेतर हो, और अन्यथा `-n` लौटाना चाहिए।

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`absoluteValue(7)` को `7` लौटाना चाहिए

```dart
  test('test1', () {
    expect(absoluteValue(7), 7, reason: '--err-t1--');
  });
```

`absoluteValue(-4)` को `4` लौटाना चाहिए

```dart
  test('test2', () {
    expect(absoluteValue(-4), 4, reason: '--err-t2--');
  });
```

`absoluteValue(0)` को `0` लौटाना चाहिए

```dart
  test('test3', () {
    expect(absoluteValue(0), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int absoluteValue(int n) {
  return n >= 0 ? n : -n;
}

void main() {
  print(absoluteValue(-4));
}
```
