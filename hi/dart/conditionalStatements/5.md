---
language: dart
exerciseType: 2
---

# --description--

**टर्नरी ऑपरेटर** `condition ? expr1 : expr2` एक सरल `if-else` अभिव्यक्ति लिखने का संक्षिप्त तरीका है।

```dart
int x = 10;
String label = x > 0 ? 'positive' : 'non-positive';
print(label); // positive
```

यदि शर्त `true` है तो `expr1` उपयोग होता है; अन्यथा `expr2` उपयोग होता है।

# --instructions--

टर्नरी अभिव्यक्ति पूरी करें ताकि जब `n`, `2` से विभाज्य हो तो `parity` को `'Even'` असाइन हो, अन्यथा `'Odd'` असाइन हो।

# --seed--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 [/] 'Even' [/] 'Odd';
  print(parity);
}
```

# --answers--

- ?
- :
- ==
- ;

# --solutions--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 ? 'Even' : 'Odd';
  print(parity);
}
```

# --output--

Even
