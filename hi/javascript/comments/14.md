---
language: javascript
exerciseType: 3
---

# --description--

एक अच्छी टिप्पणी बताती है कि कोड कुछ **क्यों** करता है, न कि वह **क्या** करता है। कोड पहले ही दिखा देता है कि क्या होता है; उसे शब्दों में दोहराने से शोर बढ़ता है और कोड बदलते ही वह पुरानी पड़ जाती है:
```javascript
// timeout को 30 सेट करें
const timeout = 30;
```
उस संख्या के पीछे का कारण ही वह चीज़ है जिसका पाठक अनुमान नहीं लगा सकता:
```javascript
// सर्वर 35 सेकंड के बाद निष्क्रिय कनेक्शन बंद कर देता है, इसलिए पहले ही रोक दें
const timeout = 30;
```
यदि कोई टिप्पणी केवल नीचे की पंक्ति दोहराती है, तो उसे हटा दें या उसकी जगह कारण लिखें।

# --instructions--

इस पंक्ति के ऊपर कौन-सी टिप्पणी सबसे उपयोगी है?
```javascript
const maxRetries = 3;
```

# --answers--

- `// पेमेंट API चौथे असफल प्रयास के बाद क्लाइंट को ब्लॉक कर देता है`
- `// maxRetries को 3 सेट करें`
- `// maxRetries एक कॉन्स्टेंट है जो एक नंबर रखता है`
- `// यह लाइन एक कॉन्स्टेंट घोषित करती है`

# --solutions--

- `// पेमेंट API चौथे असफल प्रयास के बाद क्लाइंट को ब्लॉक कर देता है`
