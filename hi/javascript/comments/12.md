---
language: javascript
exerciseType: 4
---

# --description--

JSDoc टिप्पणी के अंदर क्रम हमेशा एक जैसा होता है: पहले सारांश, फिर हर पैरामीटर के लिए एक `@param` उसी क्रम में जिसमें वे घोषित हैं, और अंत में `@returns`। शुरुआती `/**` और समापन ` */` सब कुछ लपेटते हैं, और टिप्पणी उसी फ़ंक्शन के ठीक ऊपर बैठती है जिसका वह वर्णन करती है:
```javascript
/**
 * Returns the number of seconds in the given minutes.
 * @param {number} minutes a whole number of minutes
 * @returns {number} minutes multiplied by sixty
 */
function toSeconds(minutes) {
  return minutes * 60;
}
```

# --instructions--

आइटम्स को क्रमबद्ध करें ताकि `twice` एक सारांश, एक `@param` पंक्ति और एक `@returns` पंक्ति के साथ इसी क्रम में और फ़ंक्शन के ठीक ऊपर दर्ज हो, फिर फ़ंक्शन परिभाषित करने के बाद `twice(21)` प्रिंट करें

# --answers--

```javascript
/**
```

```javascript
 * Returns the double of a number.
```

```javascript
 * @param {number} n the number to double
```

```javascript
 * @returns {number} n multiplied by two
```

```javascript
 */
```

```javascript
function twice(n) {
  return n * 2;
}
```

```javascript
console.log(twice(21));
```

# --solutions--

```javascript
/**
 * Returns the double of a number.
 * @param {number} n the number to double
 * @returns {number} n multiplied by two
 */
function twice(n) {
  return n * 2;
}
console.log(twice(21));
```

# --output--

42
