---
language: javascript
exerciseType: 1
---

# --description--

टिप्पणी को अपनी अलग पंक्ति की ज़रूरत नहीं होती: यह उसी पंक्ति में कोड के बाद आ सकती है। यह **इनलाइन टिप्पणी** (या ट्रेलिंग टिप्पणी) कहलाती है, और उस विशेष कथन के बारे में छोटा नोट लिखने के लिए यह अच्छी जगह है:
```javascript
const retries = 3; // give up after three attempts
```
`//` से पंक्ति के अंत तक सब कुछ अनदेखा कर दिया जाता है, जबकि उससे पहले का कोड सामान्य रूप से चलता है।

# --instructions--

`let seats = 4;` के बाद एक इनलाइन टिप्पणी जोड़ें जो इस मान को समझाए, उदाहरण के लिए `// seats per table`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
let seats = 4;
```

# --asserts--

The line `let seats = 4;` must end with a `//` comment.

```json
{
  "regex": "let seats = 4;[ \\t]*//[ \\t]*\\S",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`seats` must still be equal to `4`.

```javascript
tryCatch(seats === 4);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
let seats = 4; // seats per table
```
