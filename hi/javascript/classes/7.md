---
language: javascript
exerciseType: 2
---

# --description--

आप _dot syntax_ का उपयोग करके किसी instance के गुणों तक पहुँच सकते हैं।
dot syntax में, आप property का नाम instance के नाम के तुरंत बाद, बिना किसी स्पेस के, एक पीरियड `.` से अलग करके लिखते हैं:
```javascript
someInstance.someProperty
```
इसी syntax का उपयोग करके हम किसी property के मान को भी अपडेट कर सकते हैं

# --instructions--

`Animal` instance को `"female"` gender असाइन करें।
`Animal` instance का gender प्रिंट करें

# --seed--

```javascript
class Animal {
    constructor(gender, legs = 4) {
        this.gender = gender;
        this.legs = legs;
    }
}
let dog = new Animal(gender= "male");
[/].[/] = [/];
console.log(dog.gender);
```

# --answers--

- class 
- Dog
- Animal
- Dog
- ()
- Dog()
- dog
- .
- gender
- "female"

# --solutions--

```javascript
class Animal {
    constructor(gender, legs = 4) {
        this.gender = gender;
        this.legs = legs;
    }
}
let dog = new Animal(gender= "male");
dog.gender = "female";
console.log(dog.gender);
```

# --output--

female
