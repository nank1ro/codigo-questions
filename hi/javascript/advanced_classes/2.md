---
language: javascript
exerciseType: 1
---

# --description--

एक child class को parent द्वारा पहले से दी गई चीज़ों को फिर से परिभाषित करने की ज़रूरत नहीं होती। मेथड्स भी इनहेरिट होते हैं, इसलिए child का एक instance उन्हें कॉल कर सकता है जैसे कि वे उसके अपने हों:
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
    speak() {
        return `${this.name} makes a sound`;
    }
}
class Dog extends Animal {}
console.log(new Dog("Max").speak());
// prints Max makes a sound
```
जब एक child अपना constructor घोषित करता है, तो उसमें `super(...)` को कॉल करना **अनिवार्य** होता है: इसके बिना ऑब्जेक्ट कभी इनिशियलाइज़ नहीं होता और JavaScript एक `ReferenceError` थ्रो करता है। बिना constructor वाला child बिल्कुल ठीक रहता है, क्योंकि JavaScript खुद एक ऐसा constructor लिख देता है जो हर argument को parent तक पहुँचा देता है।

# --instructions--

एक class `Cat` लिखें जो `Animal` को extends करे, `name` और `lives` ले, `name` को parent के constructor को पास करे और `lives` को instance पर संग्रहीत करे

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Cat {
  
}
```

# --asserts--

`new Cat("Kitty", 9).name` को `"Kitty"` होना चाहिए।

```javascript
tryCatch(new Cat("Kitty", 9).name === "Kitty");
```

`new Cat("Kitty", 9).lives` को `9` होना चाहिए।

```javascript
tryCatch(new Cat("Kitty", 9).lives === 9);
```

`Cat` को `Animal` को extend करना चाहिए।

```javascript
tryCatch(new Cat("Kitty", 9) instanceof Animal);
```

`Cat` को `Animal` से `speak` इनहेरिट करना चाहिए।

```javascript
tryCatch(new Cat("Kitty", 9).speak() === "Kitty makes a sound");
```

दूसरी बिल्ली को अपनी वैल्यूज़ बनाए रखनी चाहिए।

```javascript
tryCatch(new Cat("Tom", 7).lives === 7);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Cat extends Animal {
  constructor(name, lives) {
    super(name);
    this.lives = lives;
  }
}
```
