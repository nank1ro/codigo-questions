---
language: javascript
exerciseType: 1
---

# --description--

मेथड्स भी प्राइवेट हो सकते हैं। नाम के आगे `#` लगाएँ और मेथड class की सार्वजनिक सतह से ग़ायब हो जाता है, जबकि वह अब भी किसी अन्य मेथड से `this.#name(...)` के साथ कॉल किया जा सकता है:
```javascript
class Receipt {
    #format(n) {
        return `$${n}`;
    }
    print(n) {
        return this.#format(n);
    }
}
console.log(new Receipt().print(7));
// $7 प्रिंट करता है
```
इसी तरह आप हेल्पर चरणों को API से बाहर रखते हैं: कॉलर को `print` दिखता है, उसके पीछे की फ़ॉर्मैटिंग की बातें नहीं। प्राइवेट फ़ील्ड्स और प्राइवेट मेथड्स मिलकर एक class को स्पष्ट अंदरूनी और बाहरी हिस्सा देते हैं।

# --instructions--

एक class `Score` लिखें जिसमें `0` से शुरू होने वाला एक प्राइवेट फ़ील्ड `#points` हो, एक प्राइवेट मेथड `#add` हो जो केवल `0` से बड़ी संख्या जोड़े, एक सार्वजनिक मेथड `record` हो जो अपना argument `#add` को पहुँचाए, और एक getter `total` हो जो पॉइंट्स लौटाए

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Score {
  
}
```

# --asserts--

एक नए स्कोर की शुरुआत `0` से होनी चाहिए।

```javascript
tryCatch(new Score().total === 0);
```

सकारात्मक वैल्यूज़ को जोड़ा जाना चाहिए।

```javascript
const s1 = new Score();
s1.record(5);
s1.record(3);
tryCatch(s1.total === 8);
```

नकारात्मक वैल्यूज़ को अनदेखा किया जाना चाहिए।

```javascript
const s2 = new Score();
s2.record(5);
s2.record(-4);
tryCatch(s2.total === 5);
```

शून्य को अनदेखा किया जाना चाहिए।

```javascript
const s3 = new Score();
s3.record(0);
tryCatch(s3.total === 0);
```

पॉइंट्स प्राइवेट होने चाहिए, इसलिए instance के पास कोई सार्वजनिक प्रॉपर्टी नहीं होनी चाहिए।

```javascript
tryCatch(Object.keys(new Score()).length === 0);
```

हेल्पर एक प्राइवेट मेथड होना चाहिए।

```json
{
  "regex": "this\\.#add\\s*\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Score {
  #points = 0;
  #add(value) {
    if (value > 0) {
      this.#points += value;
    }
  }
  record(value) {
    this.#add(value);
  }
  get total() {
    return this.#points;
  }
}
```
