---
language: javascript
exerciseType: 4
---

# --description--

एक static मेथड का एक बहुत सामान्य उपयोग **फ़ैक्टरी (factory)** है: एक ऐसा मेथड जो किसी दूसरे आकार के डेटा से एक instance बनाकर लौटाता है। यह `new` को एक ही जगह रखता है और निर्माण को एक ऐसा नाम देता है जो बताता है कि वह क्या करता है:
```javascript
class Duration {
    constructor(seconds) {
        this.seconds = seconds;
    }
    static fromMinutes(minutes) {
        return new Duration(minutes * 60);
    }
}
console.log(Duration.fromMinutes(2).seconds);
// prints 120
```
एक फ़ैक्टरी को किसी भी instance के बनने से पहले कॉल किया जा सकता है, जो एक साधारण मेथड से संभव नहीं था।

# --instructions--

आइटम्स को क्रमबद्ध करें ताकि एक चलने योग्य प्रोग्राम बने जो एक ऐरे से पॉइंट बनाकर उसके कोऑर्डिनेट्स प्रिंट करे

# --answers--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
```

```javascript
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
```

```javascript
const p = Point.fromArray([3, 4]);
```

```javascript
console.log(`${p.x},${p.y}`);
```

# --solutions--

```javascript
class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
  static fromArray(values) {
    return new Point(values[0], values[1]);
  }
}
const p = Point.fromArray([3, 4]);
console.log(`${p.x},${p.y}`);
```

# --output--

3,4
