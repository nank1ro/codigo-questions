---
language: javascript
exerciseType: 1
---

# --description--

**`instanceof`** ऑपरेटर पूछता है कि क्या कोई ऑब्जेक्ट किसी class से, या उससे इनहेरिट करने वाली किसी भी class से बना था:
```javascript
class Animal {}
class Dog extends Animal {}
const max = new Dog();
console.log(max instanceof Dog);    // true
console.log(max instanceof Animal); // true, Dog extends Animal
```
JavaScript में कोई `abstract` कीवर्ड नहीं है, लेकिन वही विचार हाथ से लिखा जाता है: एक base class आकार परिभाषित करती है और हर वह मेथड जिसे child को *अवश्य* देना होता है, सीधे एरर थ्रो कर देता है:
```javascript
class Shape {
    area() {
        throw new Error("area() must be implemented");
    }
}
```
जो child `area` को ओवरराइड करना भूल जाए, वह पहली बार उपयोग किए जाने पर ज़ोर से विफल होता है, चुपचाप `undefined` लौटाने के बजाय।

# --instructions--

एक class `Shape` लिखें जिसकी मेथड `area` एक `Error` थ्रो करे, और एक class `Square` लिखें जो उसे extends करे, एक `side` ले और `area` को ओवरराइड करके वर्ग का क्षेत्रफल लौटाए

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
class Shape {
  
}

class Square {
  
}
```

# --asserts--

`new Square(3).area()` को `9` लौटाना चाहिए।

```javascript
tryCatch(new Square(3).area() === 9);
```

`new Square(5).area()` को `25` लौटाना चाहिए।

```javascript
tryCatch(new Square(5).area() === 25);
```

एक वर्ग एक `Shape` होना चाहिए।

```javascript
tryCatch(new Square(3) instanceof Shape === true);
```

base class पर `area` को कॉल करने पर एरर आना चाहिए।

```javascript
let _threw = false;
try {
  new Shape().area();
} catch (e) {
  _threw = true;
}
tryCatch(_threw === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
class Shape {
  area() {
    throw new Error("area() must be implemented");
  }
}

class Square extends Shape {
  constructor(side) {
    super();
    this.side = side;
  }
  area() {
    return this.side * this.side;
  }
}
```
