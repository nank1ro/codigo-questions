---
language: javascript
exerciseType: 2
---

# --description--

`static` फ़ील्ड्स पर भी काम करता है। एक **static प्रॉपर्टी** class पर एक बार संग्रहीत होती है, हर instance पर नहीं, जिससे वह शेयर किए गए काउंटर या किसी कॉन्स्टेंट के लिए सहज जगह बन जाती है:
```javascript
class Circle {
    static PI = 3.14;
}
console.log(Circle.PI);
// prints 3.14
```
चूँकि केवल एक ही कॉपी होती है, इसे अपडेट करने वाला हर instance एक ही वैल्यू को अपडेट करता है। constructor के अंदर आप इसे class के नाम, `Circle.PI`, से पहुँचते हैं, `this` से नहीं: `this.PI` instance पर प्रॉपर्टी ढूँढेगा, कुछ नहीं पाएगा, और आपको `undefined` दे देगा।

# --instructions--

कोड पूरा करें ताकि `User` गिनता हो कि कितने यूज़र्स बनाए गए हैं, फिर कुल संख्या प्रिंट करें

# --seed--

```javascript
class User {
    static [/] = 0;
    constructor(name) {
        this.name = name;
        [/].count += 1;
    }
}
new User("ann");
new User("bob");
console.log(User.count);
```

# --answers--

- count
- static count
- User
- this
- new User

# --solutions--

```javascript
class User {
    static count = 0;
    constructor(name) {
        this.name = name;
        User.count += 1;
    }
}
new User("ann");
new User("bob");
console.log(User.count);
```

# --output--

2
