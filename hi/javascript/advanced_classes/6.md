---
language: javascript
exerciseType: 4
---

# --description--

जब classes एक-दूसरे से इनहेरिट करती हैं तो क्रम मायने रखता है। एक `class` घोषणा को `function` की तरह **होइस्ट नहीं** किया जाता: नाम केवल उस पंक्ति से मौजूद होता है जहाँ class लिखी गई है और आगे। इसलिए child class को अपने parent के *बाद* आना चाहिए, अन्यथा `extends` क्लॉज़ एक `ReferenceError` के साथ विफल हो जाता है।

# --instructions--

आइटम्स को क्रमबद्ध करें ताकि एक चलने योग्य प्रोग्राम बने। याद रखें कि parent class को child से पहले परिभाषित किया जाना चाहिए

# --answers--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
```

```javascript
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
```

```javascript
console.log(new Car().describe());
```

# --solutions--

```javascript
class Vehicle {
  describe() {
    return "a vehicle";
  }
}
class Car extends Vehicle {
  describe() {
    return super.describe() + " with four wheels";
  }
}
console.log(new Car().describe());
```

# --output--

a vehicle with four wheels
