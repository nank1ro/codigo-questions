---
language: javascript
exerciseType: 4
---

# --description--

असली प्रोग्राम इन मेथड्स को एक **पाइपलाइन** में जोड़ते हैं: जिन एलिमेंट्स की परवाह है उन्हें छानें, उन्हें ज़रूरी मानों में बदलें, और उन्हें एक परिणाम में समेटें। बीच के array को स्थिरांकों में रखने से हर चरण पढ़ने लायक़ रहता है और उन्हें दोबारा उपयोग किया जा सकता है:
```javascript
const adults = people.filter((p) => p.age >= 18);
const names = adults.map((p) => p.name);
const totalAge = adults.reduce((sum, p) => sum + p.age, 0);
```
`names.join(", ")` स्ट्रिंग्स के array को एक ही स्ट्रिंग में बदल देता है जिसमें एलिमेंट अल्पविराम और स्पेस से अलग होते हैं।

# --instructions--

पंक्तियों को क्रम में लगाएँ ताकि कार्ट में सिर्फ़ धनात्मक `qty` वाले आइटम रखे जाएँ, उनके नाम इकट्ठे हों, उनके लिए `price * qty` जोड़ा जाए और `", "` से जुड़े नाम तथा कुल एक ही पंक्ति में प्रिंट हों। आइटम pen, book, bag के क्रम में लिखें और `inStock`, `names` तथा `total` को इसी क्रम में घोषित करें

# --answers--

```javascript
const cart = [
```

```javascript
  { name: "pen", price: 2, qty: 3 },
```

```javascript
  { name: "book", price: 12, qty: 1 },
```

```javascript
  { name: "bag", price: 30, qty: 0 },
```

```javascript
];
```

```javascript
const inStock = cart.filter((item) => item.qty > 0);
```

```javascript
const names = inStock.map((item) => item.name);
```

```javascript
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
```

```javascript
console.log(names.join(", "), total);
```

# --solutions--

```javascript
const cart = [
  { name: "pen", price: 2, qty: 3 },
  { name: "book", price: 12, qty: 1 },
  { name: "bag", price: 30, qty: 0 },
];
const inStock = cart.filter((item) => item.qty > 0);
const names = inStock.map((item) => item.name);
const total = inStock.reduce((sum, item) => sum + item.price * item.qty, 0);
console.log(names.join(", "), total);
```

# --output--

pen, book 18
