---
language: javascript
exerciseType: 1
---

# --description--

तीन मेथड किसी प्रेडिकेट की मदद से array के बारे में सवालों के जवाब देते हैं:
- `find(predicate)` वह **पहला** एलिमेंट लौटाता है जिसके लिए प्रेडिकेट `true` है, और कोई न हो तो `undefined`
- `some(predicate)` `true` लौटाता है अगर **कम से कम एक** एलिमेंट प्रेडिकेट को पूरा करता है
- `every(predicate)` `true` लौटाता है अगर **सभी** एलिमेंट उसे पूरा करते हैं (और ख़ाली array के लिए भी `true`)
```javascript
const scores = [72, 45, 90];
console.log(scores.find((s) => s < 60));
// prints 45
console.log(scores.some((s) => s === 90), scores.every((s) => s >= 60));
// prints true false
```
तीनों उत्तर मिलते ही रुक जाते हैं, इसलिए वे कभी ज़रूरत से ज़्यादा एलिमेंट नहीं देखते।

# --instructions--

एक फ़ंक्शन `report(scores)` लिखें जो `every` और `find` का उपयोग करते हुए `"all passed"` लौटाए जब हर स्कोर कम से कम `60` हो, और अन्यथा `"first failing: X"` लौटाए जहाँ `X` `60` से कम पहला स्कोर है

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function report(scores) {

}
```

# --asserts--

`report([70, 80])` को `"all passed"` लौटाना चाहिए।

```javascript
tryCatch(report([70, 80]) === "all passed");
```

`report([70, 40, 30])` को `"first failing: 40"` लौटाना चाहिए।

```javascript
tryCatch(report([70, 40, 30]) === "first failing: 40");
```

`report([59])` को `"first failing: 59"` लौटाना चाहिए।

```javascript
tryCatch(report([59]) === "first failing: 59");
```

`report([])` को `"all passed"` लौटाना चाहिए।

```javascript
tryCatch(report([]) === "all passed");
```

फ़ंक्शन को `every` और `find` का उपयोग करना चाहिए।

```javascript
tryCatch(/\.every\(/.test(report.toString()) && /\.find\(/.test(report.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function report(scores) {
  if (scores.every((s) => s >= 60)) {
    return "all passed";
  }
  return `first failing: ${scores.find((s) => s < 60)}`;
}
```
