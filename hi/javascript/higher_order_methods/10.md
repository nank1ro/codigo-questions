---
language: javascript
exerciseType: 2
---

# --description--

चूँकि फ़ंक्शन मान होते हैं, आप एक ऐसा हायर-ऑर्डर फ़ंक्शन लिख सकते हैं जो दो फ़ंक्शनों को एक नए फ़ंक्शन में **जोड़** दे। `compose(f, g)` एक ऐसा फ़ंक्शन लौटाता है जो पहले `g` लगाता है और फिर परिणाम पर `f`, ठीक गणितीय संकेत *f(g(x))* की तरह:
```javascript
const compose = (f, g) => (x) => f(g(x));
const trim = (s) => s.trim();
const shout = (s) => s.toUpperCase();
const clean = compose(shout, trim);
console.log(clean("  hi  "));
// prints HI
```
क्रम मायने रखता है: `compose(f, g)` पहले `g` चलाता है, फिर `f`। इस तरह छोटे फ़ंक्शनों को जोड़कर प्रोग्राम बनाने को **फ़ंक्शन कंपोज़िशन** कहते हैं।

# --instructions--

`compose` को पूरा करें ताकि वह एक ऐसा फ़ंक्शन लौटाए जो पहले `g` और फिर परिणाम पर `f` लगाए, फिर `compose(double, inc)(5)` और `compose(inc, double)(5)` प्रिंट करें, जिससे आउटपुट `12 11` बने

# --seed--

```javascript
const compose = (f, g) => (x) => [/]([/](x));
const inc = (n) => n + 1;
const double = (n) => n * 2;
console.log(compose(double, inc)(5), compose(inc, double)(5));
```

# --answers--

- f
- g
- x
- compose

# --solutions--

```javascript
const compose = (f, g) => (x) => f(g(x));
const inc = (n) => n + 1;
const double = (n) => n * 2;
console.log(compose(double, inc)(5), compose(inc, double)(5));
```

# --output--

12 11
