---
language: javascript
exerciseType: 1
---

# --description--

हर फ़ंक्शन के पास `bind` मेथड होता है जो एक **नया** फ़ंक्शन लौटाता है जिसमें कुछ चीज़ें पहले से तय होती हैं। इसका पहला आर्ग्युमेंट नए फ़ंक्शन का `this` बन जाता है; बाक़ी आर्ग्युमेंट उन आर्ग्युमेंट्स से पहले रख दिए जाते हैं जिनके साथ नया फ़ंक्शन कॉल होगा (एक **आंशिक अनुप्रयोग**):
```javascript
function multiply(a, b) {
  return a * b;
}
const double = multiply.bind(null, 2);
console.log(double(21));
// prints 42
```
मेथड्स के लिए `this` तय करना मायने रखता है। जब कोई मेथड अपने ऑब्जेक्ट से बाहर निकालकर अकेले कॉल किया जाता है, तो `this` उस ऑब्जेक्ट को नहीं दर्शाता, इसलिए `this.name` `undefined` हो जाता है। `bind` उसे ऑब्जेक्ट से बाँध देता है:
```javascript
const user = {
  name: "Ana",
  hello() { return `Hi ${this.name}`; },
};
const loose = user.hello;
console.log(loose());
// prints Hi undefined
const bound = user.hello.bind(user);
console.log(bound());
// prints Hi Ana
```
मूल फ़ंक्शन कभी नहीं बदलता: `bind` हमेशा एक नया फ़ंक्शन बनाता है, जिसका `name` मूल नाम के आगे `bound ` लगाकर बनता है।

# --instructions--

फ़ंक्शन `power` और ऑब्जेक्ट `counter` दिए गए हैं। स्थिरांक `square` में `power` का वह रूप रखें जिसमें `this` `null` पर और `exponent` `2` पर बँधा हो, और स्थिरांक `tick` में `counter.increment` का वह रूप रखें जो `counter` से बँधा हो, ताकि अकेले कॉल करने पर भी वह काम करता रहे

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = 
const tick = 
```

# --asserts--

`square(3)` को `9` लौटाना चाहिए।

```javascript
tryCatch(square(3) === 9);
```

`square(12)` को `144` लौटाना चाहिए।

```javascript
tryCatch(square(12) === 144);
```

`square` `bind` से बना होना चाहिए, इसलिए उसका नाम `"bound power"` है।

```javascript
tryCatch(square.name === "bound power");
```

`tick` `bind` से बना नया फ़ंक्शन होना चाहिए, ख़ुद `counter.increment` नहीं।

```javascript
tryCatch(tick !== counter.increment && tick.name === "bound increment");
```

`tick()` को अकेले दो बार कॉल करने पर `1` और फिर `2` मिलना चाहिए, और `counter.count` `2` होना चाहिए।

```javascript
tryCatch(tick() === 1 && tick() === 2 && counter.count === 2);
```

`tick` तब भी काम करना चाहिए जब उसे किसी दूसरे वेरिएबल में रखकर वहाँ से कॉल किया जाए।

```javascript
tryCatch((() => { const alone = tick; return alone() === 3; })());
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function power(exponent, base) {
  return base ** exponent;
}
const counter = {
  count: 0,
  increment() {
    this.count += 1;
    return this.count;
  },
};
const square = power.bind(null, 2);
const tick = counter.increment.bind(counter);
```
