---
language: javascript
exerciseType: 2
---

# --description--

`reduce` का संचायक संख्या ही हो, यह ज़रूरी नहीं: वह स्ट्रिंग, array या ऑब्जेक्ट भी हो सकता है। ख़ाली ऑब्जेक्ट `{}` से शुरू करके आप एक ही चक्कर में गिनती या समूहीकरण कर सकते हैं। callback से **संचायक लौटाना** याद रखें, वरना अगले चरण को `undefined` मिलेगा:
```javascript
const votes = ["yes", "no", "yes"];
const tally = votes.reduce((acc, vote) => {
  acc[vote] = (acc[vote] ?? 0) + 1;
  return acc;
}, {});
console.log(tally);
// prints { yes: 2, no: 1 }
```
`acc[vote] ?? 0` मौजूदा गिनती पढ़ता है, और जब वह key अभी मौजूद नहीं है तो `0`।

# --instructions--

कोड पूरा करें ताकि `counts` हर पहले अक्षर के साथ यह जोड़े कि उससे कितने शब्द शुरू होते हैं: `reduce` का उपयोग करें, शुरुआती संचायक के रूप में ख़ाली ऑब्जेक्ट लें और ग़ैरमौजूद keys के लिए `?? 0` का उपयोग करें, जिससे आउटपुट `{ a: 2, b: 2, c: 1 }` बने

# --seed--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.[/]((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] [/] 0) + 1;
  return [/];
}, [/]);
console.log(counts);
```

# --answers--

- reduce
- ??
- acc
- {}
- map
- filter
- &&
- word
- 0

# --solutions--

```javascript
const words = ["apple", "avocado", "banana", "blueberry", "cherry"];
const counts = words.reduce((acc, word) => {
  const letter = word[0];
  acc[letter] = (acc[letter] ?? 0) + 1;
  return acc;
}, {});
console.log(counts);
```

# --output--

{ a: 2, b: 2, c: 1 }
