---
language: javascript
exerciseType: 4
---

# --description--

तुलना फ़ंक्शन एलिमेंट्स के किसी भी हिस्से को देख सकता है, इसलिए ऑब्जेक्ट्स का array उनकी किसी एक प्रॉपर्टी की तुलना करके ही उस प्रॉपर्टी के अनुसार क्रमबद्ध हो जाता है:
```javascript
const items = [{ name: "b", size: 3 }, { name: "a", size: 1 }];
const bySize = [...items].sort((x, y) => x.size - y.size);
console.log(bySize.map((item) => item.name));
// prints [ 'a', 'b' ]
```
कॉपी को क्रमबद्ध करने से `items` अपने मूल क्रम में ही रहता है।

# --instructions--

पंक्तियों को क्रम में लगाएँ ताकि `players` की एक कॉपी `score` के आरोही क्रम में क्रमबद्ध हो, उनके नाम उसी क्रम में `map` से प्रिंट हों, और फिर `players` के पहले एलिमेंट का नाम प्रिंट हो जिससे पता चले कि वह बदला नहीं। `ranked` को `names` से पहले घोषित करें

# --answers--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
```

```javascript
const ranked = [...players].sort((a, b) => a.score - b.score);
```

```javascript
const names = ranked.map((p) => p.name);
```

```javascript
console.log(names);
```

```javascript
console.log(players[0].name);
```

# --solutions--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
const ranked = [...players].sort((a, b) => a.score - b.score);
const names = ranked.map((p) => p.name);
console.log(names);
console.log(players[0].name);
```

# --output--

[ 'Leo', 'Zoe', 'Mia' ]
Zoe
