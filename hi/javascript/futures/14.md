---
language: javascript
exerciseType: 1
---

# --description--

`Promise.all` जैसे ही एक प्रॉमिस rejected हो जाए, हार मान लेता है। जब आप फिर भी हर परिणाम चाहते हैं, तो **`Promise.allSettled(promises)`** का उपयोग करें: वह कभी rejected नहीं होता, और हर प्रॉमिस के लिए एक छोटा ऑब्जेक्ट लेकर, उसी क्रम में fulfilled होता है:

- `{ status: "fulfilled", value: ... }` उनके लिए जो सफल रहे;
- `{ status: "rejected", reason: ... }` उनके लिए जो विफल रहे।

```javascript
const results = await Promise.allSettled([
  Promise.resolve(1),
  Promise.reject(new Error("nope")),
]);
console.log(results[0].status);
// prints fulfilled
console.log(results[1].reason.message);
// prints nope
```
`value` केवल तब पढ़ें जब `status` `"fulfilled"` हो, और `reason` केवल तब जब वह `"rejected"` हो: दूसरी प्रॉपर्टी बस मौजूद नहीं होती।

# --instructions--

एक `async` फ़ंक्शन `countFulfilled` लिखें जो प्रॉमिसों की एक ऐरे ले, उन सबका `Promise.allSettled` से इंतज़ार करे, और लौटाए कि उनमें से कितने fulfilled हुए

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
async function countFulfilled(promises) {
  
}
```

# --asserts--

`await countFulfilled([])` को `0` होना चाहिए।

```javascript
(async () => {
  tryCatch((await countFulfilled([])) === 0);
})().catch(() => tryCatch(false));
```

`await countFulfilled([Promise.resolve(1), Promise.resolve(2)])` को `2` होना चाहिए।

```javascript
(async () => {
  const promises = [Promise.resolve(1), Promise.resolve(2)];
  tryCatch((await countFulfilled(promises)) === 2);
})().catch(() => tryCatch(false));
```

तीन में से एक प्रॉमिस के rejected होने पर उत्तर `2` होना चाहिए।

```javascript
(async () => {
  const promises = [
    Promise.resolve(1),
    Promise.reject(new Error("nope")),
    Promise.resolve(3),
  ];
  tryCatch((await countFulfilled(promises)) === 2);
})().catch(() => tryCatch(false));
```

जब हर प्रॉमिस rejected हो, तो उत्तर `0` होना चाहिए, और `countFulfilled` rejected नहीं होना चाहिए।

```javascript
(async () => {
  const promises = [Promise.reject(new Error("a")), Promise.reject(new Error("b"))];
  tryCatch((await countFulfilled(promises)) === 0);
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
async function countFulfilled(promises) {
  const results = await Promise.allSettled(promises);
  let count = 0;
  for (const result of results) {
    if (result.status === "fulfilled") {
      count += 1;
    }
  }
  return count;
}
```
