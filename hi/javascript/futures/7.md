---
language: javascript
exerciseType: 1
---

# --description--

एसिंक्रोनस काम विफल भी हो सकता है। `new Promise` को दिए गए फ़ंक्शन को एक दूसरी callback भी मिलती है, **`reject`**: `reject(error)` कॉल करें और प्रॉमिस fulfilled होने के बजाय rejected हो जाता है।
```javascript
function readAge(age) {
  return new Promise((resolve, reject) => {
    if (age >= 0) {
      resolve(age);
    } else {
      reject(new Error("negative age"));
    }
  });
}
```
हमेशा एक `Error` ऑब्जेक्ट के साथ reject करें: उसमें एक `message` और एक स्टैक ट्रेस होता है, जो किसी खाली स्ट्रिंग में नहीं होता।

एक rejection को **`.catch(callback)`** से पढ़ा जाता है, जो `.then` का दर्पण है। **`Promise.reject(error)`** ऐसा प्रॉमिस बनाता है जो पहले से rejected है, बिलकुल वैसे जैसे `Promise.resolve` fulfilled बनाता है:
```javascript
readAge(-1).catch((error) => {
  console.log(error.message);
  // prints negative age
});
```
`resolve` और `reject` दोनों को कॉल करने, या दो बार कॉल करने से कुछ नहीं बदलता: केवल पहली कॉल गिनती है।

# --instructions--

एक फ़ंक्शन `checkAge` लिखें जो एक संख्या `age` ले और एक प्रॉमिस लौटाए जो `age` के कम से कम `18` होने पर `age` के साथ fulfilled हो, और अन्यथा `new Error("too young")` के साथ rejected हो। `new Promise` का उपयोग करें

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function checkAge(age) {
  
}
```

# --asserts--

`await checkAge(20)` को `20` होना चाहिए।

```javascript
(async () => {
  tryCatch((await checkAge(20)) === 20);
})().catch(() => tryCatch(false));
```

`await checkAge(18)` को `18` होना चाहिए, क्योंकि `18` पर्याप्त उम्र है।

```javascript
(async () => {
  tryCatch((await checkAge(18)) === 18);
})().catch(() => tryCatch(false));
```

`checkAge(10)` को `"too young"` संदेश के साथ rejected होना चाहिए।

```javascript
(async () => {
  let message = "";
  await checkAge(10).catch((error) => { message = error.message; });
  tryCatch(message === "too young");
})().catch(() => tryCatch(false));
```

`checkAge(0)` को किसी स्ट्रिंग के बजाय एक `Error` ऑब्जेक्ट के साथ rejected होना चाहिए।

```javascript
(async () => {
  let reason = null;
  await checkAge(0).catch((error) => { reason = error; });
  tryCatch(reason instanceof Error);
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function checkAge(age) {
  return new Promise((resolve, reject) => {
    if (age >= 18) {
      resolve(age);
    } else {
      reject(new Error("too young"));
    }
  });
}
```
