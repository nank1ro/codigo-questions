---
language: javascript
exerciseType: 4
---

# --description--

**क्रमिक** और **समानांतर** इंतज़ार का फ़र्क़ इस बात से तय होता है कि आप `await` *कहाँ* रखते हैं:
```javascript
// sequential: about 300 + 300 = 600 ms
const a = await load("a");
const b = await load("b");

// parallel: about 300 ms
const [a, b] = await Promise.all([load("a"), load("b")]);
```
पहले रूप में दूसरा डाउनलोड तभी शुरू होता है जब पहला पूरा हो चुका हो, क्योंकि `await` फ़ंक्शन को उसी पंक्ति पर रोक देता है। दूसरे में, दोनों कॉल किसी भी await से पहले कर दी जाती हैं, इसलिए `Promise.all` के इंतज़ार के दौरान दोनों डाउनलोड पहले से चल रहे होते हैं।

क्रमिक await केवल तब उपयोग करें जब दूसरे काम को सचमुच पहले के परिणाम की ज़रूरत हो। अन्यथा पहले सब कुछ शुरू करें और एक साथ await करें।

# --instructions--

आइटम को एक ऐसे प्रोग्राम में क्रमबद्ध करें जो `later` परिभाषित करता है, फिर एक `async` फ़ंक्शन `main` जो `loading` प्रिंट करता है, `later` की दोनों कॉल `Promise.all` से एक साथ शुरू करता है, दोनों परिणाम एक स्पेस से जोड़कर प्रिंट करता है और अंत में `done` प्रिंट करता है। पहले `later` परिभाषित करें, फिर `main`, और आख़िरी पंक्ति पर `main()` कॉल करें

# --answers--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
```

```javascript
async function main() {
```

```javascript
  console.log("loading");
```

```javascript
  const results = await Promise.all([later("one", 30), later("two", 10)]);
```

```javascript
  console.log(results.join(" "));
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
async function main() {
  console.log("loading");
  const results = await Promise.all([later("one", 30), later("two", 10)]);
  console.log(results.join(" "));
  console.log("done");
}
main();
```

# --output--

loading
ready two
ready one
one two
done
