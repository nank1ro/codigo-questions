---
language: javascript
exerciseType: 1
---

# --description--

`Promise.all` किसी भी लंबाई की ऐरे के साथ काम करता है, खाली ऐरे सहित: `Promise.all([])` को await करने पर तुरंत एक खाली ऐरे ही वापस मिलती है। इससे रन टाइम पर बनी कोई सूची पास करना सुरक्षित रहता है, बिना "इंतज़ार करने जैसा कुछ नहीं" के लिए किसी विशेष मामले की ज़रूरत के।
```javascript
const values = await Promise.all(items);
console.log(values.length === items.length);
// prints true
```
जो ऐरे वह वापस देता है उसमें हमेशा ठीक उतनी ही प्रविष्टियाँ होती हैं जितनी उसे मिली थीं, उन्हीं स्थितियों पर, इसलिए उसे किसी और ऐरे की तरह लूप किया जा सकता है।

# --instructions--

एक `async` फ़ंक्शन `sumAll` लिखें जो संख्याओं के प्रॉमिसों की एक ऐरे ले, उन सभी का एक साथ `Promise.all` से इंतज़ार करे, और उनका योग लौटाए। खाली ऐरे को `0` देना चाहिए

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
async function sumAll(promises) {
  
}
```

# --asserts--

`await sumAll([])` को `0` होना चाहिए।

```javascript
(async () => {
  tryCatch((await sumAll([])) === 0);
})().catch(() => tryCatch(false));
```

`await sumAll([Promise.resolve(1), Promise.resolve(2), Promise.resolve(3)])` को `6` होना चाहिए।

```javascript
(async () => {
  const promises = [Promise.resolve(1), Promise.resolve(2), Promise.resolve(3)];
  tryCatch((await sumAll(promises)) === 6);
})().catch(() => tryCatch(false));
```

`await sumAll([Promise.resolve(-4), Promise.resolve(4)])` को `0` होना चाहिए।

```javascript
(async () => {
  const promises = [Promise.resolve(-4), Promise.resolve(4)];
  tryCatch((await sumAll(promises)) === 0);
})().catch(() => tryCatch(false));
```

`sumAll` उन प्रॉमिसों के साथ भी काम करना चाहिए जिनमें अलग-अलग समय लगता है।

```javascript
(async () => {
  const later = (n, ms) => new Promise((resolve) => setTimeout(() => resolve(n), ms));
  tryCatch((await sumAll([later(10, 30), later(5, 5)])) === 15);
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
async function sumAll(promises) {
  const values = await Promise.all(promises);
  let total = 0;
  for (const value of values) {
    total += value;
  }
  return total;
}
```
