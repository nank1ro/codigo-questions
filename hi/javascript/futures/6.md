---
language: javascript
exerciseType: 4
---

# --description--

कोई `async` फ़ंक्शन किसी और फ़ंक्शन की तरह ऊपर से नीचे पढ़ा जाता है: `await` बस उसे तब तक रोक देता है जब तक awaited किया गया प्रॉमिस fulfilled न हो जाए, फिर निष्पादन अगली पंक्ति से आगे बढ़ जाता है।
```javascript
async function main() {
  console.log("start");
  const value = await Promise.resolve("data");
  console.log(value);
  console.log("done");
}

main();
// prints start, then data, then done
```
आख़िरी पंक्ति पर ध्यान दें: किसी `async` फ़ंक्शन को अब भी **कॉल** करना पड़ता है। `main` को कोष्ठक के बिना लिखना काम को परिभाषित तो करता है पर कभी शुरू नहीं करता, और कुछ भी प्रिंट नहीं होता।

# --instructions--

आइटम को एक ऐसे प्रोग्राम में क्रमबद्ध करें जो `data` लौटाने वाला एक `async` फ़ंक्शन `load` परिभाषित करता है, फिर एक `async` फ़ंक्शन `main` जो `start` प्रिंट करता है, `load()` को await करके `value` में रखता है, `value` प्रिंट करता है और अंत में `done` प्रिंट करता है। पहले `load` परिभाषित करें, फिर `main`, और आख़िरी पंक्ति पर `main()` कॉल करें

# --answers--

```javascript
async function load() {
  return "data";
}
```

```javascript
async function main() {
```

```javascript
  console.log("start");
```

```javascript
  const value = await load();
```

```javascript
  console.log(value);
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
async function load() {
  return "data";
}
async function main() {
  console.log("start");
  const value = await load();
  console.log(value);
  console.log("done");
}
main();
```

# --output--

start
data
done
