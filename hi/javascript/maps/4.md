---
language: javascript
exerciseType: 1
---

# --description--

एक साधारण ऑब्जेक्ट में हर की को स्ट्रिंग में बदल दिया जाता है: `user[1]` और `user["1"]` एक ही की हैं।
मैप अपनी कीज़ का **टाइप** बनाए रखता है, इसलिए एक number, एक string, एक boolean, या यहाँ तक कि एक object भी अलग-अलग की बन सकते हैं:
```javascript
let lookup = new Map();
lookup.set(1, "number one");
lookup.set("1", "string one");
console.log(lookup.size);
// prints 2
let alice = { name: "Alice" };
lookup.set(alice, "an object key");
console.log(lookup.get(alice));
// prints an object key
```
ऑब्जेक्ट कीज़ की तुलना पहचान (identity) से होती है: केवल वही ऑब्जेक्ट वापस वैल्यू पा सकता है।

# --instructions--

एक मैप `scores` बनाएं जो नंबर की `7` के अंतर्गत `90`, स्ट्रिंग की `"7"` के अंतर्गत `"seven"`, और ऑब्जेक्ट `alice` के अंतर्गत `100` संग्रहीत करे

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
let alice = { name: "Alice" };
```

# --asserts--

`scores.get(7)` को `90` लौटाना चाहिए

```javascript
tryCatch(scores.get(7) === 90);
```

`scores.get("7")` को `"seven"` लौटाना चाहिए

```javascript
tryCatch(scores.get("7") === "seven");
```

`scores.get(alice)` को `100` लौटाना चाहिए

```javascript
tryCatch(scores.get(alice) === 100);
```

`scores.size` `3` होना चाहिए

```javascript
tryCatch(scores.size === 3);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
let alice = { name: "Alice" };
let scores = new Map();
scores.set(7, 90);
scores.set("7", "seven");
scores.set(alice, 100);
```
