---
language: javascript
exerciseType: 2
---

# --description--

एक मैप उस क्रम को याद रखता है जिसमें जोड़े जोड़े गए थे, और आप इसे `for...of` से लूप कर सकते हैं।
`entries()` मेथड हर जोड़े को `[key, value]` ऐरे के रूप में देता है, जिसे आप लूप के अंदर ही अनपैक कर सकते हैं:
```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
// prints apple: 3
// prints pear: 5
```
मैप पर सीधे लूप करना, `for (const [name, qty] of stock)`, बिल्कुल वही काम करता है।

# --instructions--

`stock` की एंट्रीज़ पर लूप करें और हर नाम को उसकी मात्रा (quantity) के साथ प्रिंट करें

# --seed--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [/] of stock.[/]()) {
  console.log(`${name}: ${qty}`);
}
```

# --answers--

- [name, qty]
- [qty, name]
- name, qty
- entries
- pairs
- items

# --solutions--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
```

# --output--

apple: 3
pear: 5
