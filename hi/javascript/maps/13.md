---
language: javascript
exerciseType: 2
---

# --description--

`delete(key)` `true` लौटाता है जब कोई जोड़ा हटाया गया हो, और `false` जब वह की मौजूद नहीं थी।
एक ही बार में **सभी** जोड़ों को हटाने के लिए, `clear()` को कॉल करें:
```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

`cart` से `"pen"` को हटाते हुए हटाने का परिणाम प्रिंट करें, फिर पूरे मैप को खाली करें और बचे हुए जोड़ों की संख्या प्रिंट करें

# --seed--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
