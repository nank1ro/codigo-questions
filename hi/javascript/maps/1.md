---
language: javascript
exerciseType: 2
---

# --description--

एक **मैप** **की-वैल्यू जोड़े** संग्रहीत करता है: हर वैल्यू एक की के अंतर्गत सेव होती है, और आप उस की का उपयोग करके वैल्यू को फिर से खोज सकते हैं।
आप `new Map()` से एक खाली मैप बनाते हैं, `set(key, value)` से एक जोड़ा जोड़ते हैं और `get(key)` से एक वैल्यू पढ़ते हैं:
```javascript
let ages = new Map();
ages.set("Ann", 30);
ages.set("Bob", 25);
console.log(ages.get("Ann"));
// prints 30
```
पहले से मौजूद की के साथ `set()` को कॉल करने पर उसकी वैल्यू बदल जाती है।

# --instructions--

एक खाली मैप `ages` बनाएं, की `"Ann"` के अंतर्गत `30` संग्रहीत करें, फिर `"Ann"` के लिए संग्रहीत वैल्यू प्रिंट करें

# --seed--

```javascript
let ages = new [/]();
ages.[/]("Ann", 30);
console.log(ages.[/]("Ann"));
```

# --answers--

- Map
- set
- get
- Object
- add
- put
- read

# --solutions--

```javascript
let ages = new Map();
ages.set("Ann", 30);
console.log(ages.get("Ann"));
```

# --output--

30
