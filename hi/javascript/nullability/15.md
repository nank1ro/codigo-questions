---
language: javascript
exerciseType: 4
---

# --instructions--

लाइनों को इस क्रम में लगाएँ ताकि `finish` परिभाषित हो, जो किसी टास्क के ऑप्शनल `onDone` कॉलबैक को कॉल करे और उसका `name` या `"untitled"` लौटाए, फिर `build` नाम के टास्क के लिए परिणाम प्रिंट करे जिसका कॉलबैक `done` प्रिंट करता हो, और एक खाली टास्क के लिए भी। फ़ंक्शन को सबसे पहले परिभाषित करें

# --answers--

```javascript
function finish(task) {
```

```javascript
  task.onDone?.();
```

```javascript
  return task.name ?? "untitled";
```

```javascript
}
```

```javascript
console.log(finish({ name: "build", onDone: () => console.log("done") }));
```

```javascript
console.log(finish({}));
```

# --solutions--

```javascript
function finish(task) {
  task.onDone?.();
  return task.name ?? "untitled";
}
console.log(finish({ name: "build", onDone: () => console.log("done") }));
console.log(finish({}));
```

# --output--

done
build
untitled
