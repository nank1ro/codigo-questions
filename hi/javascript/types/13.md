---
language: javascript
exerciseType: 1
---

# --description--

क्योंकि संख्याएँ double होती हैं, कुछ दशमलव सटीक रूप से संग्रहीत नहीं हो सकते और छोटी त्रुटियाँ दिखाई देने लगती हैं:
```javascript
console.log(0.1 + 0.2);
// prints 0.30000000000000004
console.log(0.1 + 0.2 === 0.3);
// prints false
```
`toFixed(digits)` मेथड एक संख्या को `digits` दशमलवों तक राउंड करता है, लेकिन वह एक **स्ट्रिंग** लौटाता है, जो प्रदर्शन के लिए ठीक है और आगे के गणित के लिए गलत:
```javascript
const price = (0.1 + 0.2).toFixed(2);
console.log(price, typeof price);
// prints 0.30 string
```
एक राउंड की हुई **संख्या** पाने के लिए, परिणाम को `Number()` से वापस बदलें:
```javascript
console.log(Number((0.1 + 0.2).toFixed(1)));
// prints 0.3
```

# --instructions--

एक फंक्शन `roundTo(value, decimals)` लिखें जो `value` को `decimals` दशमलव स्थानों तक राउंड करके एक **संख्या** के रूप में लौटाए, `toFixed` का उपयोग करके और परिणाम को वापस बदलकर

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function roundTo(value, decimals) {

}
```

# --asserts--

`roundTo(3.14159, 2)` को `3.14` लौटाना चाहिए।

```javascript
tryCatch(roundTo(3.14159, 2) === 3.14);
```

`roundTo(0.1 + 0.2, 1)` को `0.3` लौटाना चाहिए।

```javascript
tryCatch(roundTo(0.1 + 0.2, 1) === 0.3);
```

`roundTo(2.5, 0)` को `3` लौटाना चाहिए।

```javascript
tryCatch(roundTo(2.5, 0) === 3);
```

`roundTo(10, 2)` को `10` लौटाना चाहिए।

```javascript
tryCatch(roundTo(10, 2) === 10);
```

परिणाम एक संख्या होनी चाहिए, स्ट्रिंग नहीं।

```javascript
tryCatch(typeof roundTo(1.005, 1) === "number");
```

फंक्शन को `toFixed` का उपयोग करना चाहिए।

```javascript
tryCatch(/toFixed\(/.test(roundTo.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function roundTo(value, decimals) {
  return Number(value.toFixed(decimals));
}
```
