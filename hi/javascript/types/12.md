---
language: javascript
exerciseType: 4
---

# --description--

JavaScript में पूर्ण संख्याओं और दशमलव के लिए एक ही `number` टाइप होता है: हर संख्या एक 64-बिट फ्लोटिंग पॉइंट मान (एक *double*) होती है। इसलिए `5` और `5.0` एक ही मान हैं, और कोई अलग इंटीजर टाइप नहीं है:
```javascript
console.log(5 === 5.0, 10 / 2);
// prints true 5
```
यह पता लगाने के लिए कि क्या संख्या का दशमलव भाग नहीं है, `Number.isInteger` का उपयोग करें:
```javascript
console.log(Number.isInteger(5.0), Number.isInteger(3.5));
// prints true false
```
टेम्पलेट लिटरल्स इंटरपोलेट किए गए मान को `String()` के उन्हीं नियमों से स्ट्रिंग में बदलते हैं, इसलिए `${5.0}` `"5"` बनता है, `"5.0"` नहीं।

# --instructions--

`half` को परिभाषित करने के लिए लाइनों को सही क्रम में लगाएं, जो `n` को दो से विभाजित करता है और एक वाक्य लौटाता है जो बताता है कि परिणाम पूर्ण है या नहीं, फिर `10` और `7` के लिए परिणाम प्रिंट करें। पहले फंक्शन को परिभाषित करें

# --answers--

```javascript
function half(n) {
```

```javascript
  const result = n / 2;
```

```javascript
  if (Number.isInteger(result)) {
```

```javascript
    return `${result} is whole`;
```

```javascript
  }
```

```javascript
  return `${result} is not whole`;
```

```javascript
}
```

```javascript
console.log(half(10));
```

```javascript
console.log(half(7));
```

# --solutions--

```javascript
function half(n) {
  const result = n / 2;
  if (Number.isInteger(result)) {
    return `${result} is whole`;
  }
  return `${result} is not whole`;
}
console.log(half(10));
console.log(half(7));
```

# --output--

5 is whole
3.5 is not whole
