---
language: javascript
exerciseType: 2
---

# --description--

आप किसी मान को दूसरे टाइप में **स्पष्ट रूप से** बदल सकते हैं, टाइप को एक फंक्शन की तरह कॉल करके:
- `Number(value)` संख्या में बदलता है: `Number("42")` का परिणाम `42` है
- `String(value)` स्ट्रिंग में बदलता है: `String(42)` का परिणाम `"42"` है
- `Boolean(value)` बूलियन में बदलता है: `Boolean("")` का परिणाम `false` है

परिणाम एक बिल्कुल नया मान होता है; मूल मान नहीं बदलता:
```javascript
const input = "7";
const count = Number(input) + 1;
console.log(count, typeof count);
// prints 8 number
console.log(String(count) + "!");
// prints 8!
```
स्पष्ट रूप से बदलना आपके इरादे को साफ दिखाता है: जो कोई भी `Number(input)` पढ़ता है, उसे पता होता है कि `input` टेक्स्ट था।

# --instructions--

कोड को इस तरह पूरा करें कि `count`, `input` को संख्या में बदलकर उसमें `1` जोड़ने से बने, `label`, `count` को स्ट्रिंग में बदलकर दो बार दोहराने से बने, और `flag`, खाली स्ट्रिंग को बूलियन में बदलने से बने, जिससे आउटपुट `43 4343 false` प्राप्त हो

# --seed--

```javascript
const input = "42";
const count = [/](input) + 1;
const label = [/](count).repeat(2);
const flag = [/]("");
console.log(count, label, flag);
```

# --answers--

- Number
- String
- Boolean
- Int
- Bool
- toString

# --solutions--

```javascript
const input = "42";
const count = Number(input) + 1;
const label = String(count).repeat(2);
const flag = Boolean("");
console.log(count, label, flag);
```

# --output--

43 4343 false
