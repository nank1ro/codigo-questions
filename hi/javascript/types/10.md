---
language: javascript
exerciseType: 1
---

# --description--

जब JavaScript को एक बूलियन की ज़रूरत होती है, उदाहरण के लिए एक `if` शर्त में या `Boolean(value)` में, तो वह मान को बदल देता है। केवल आठ मान `false` बनते हैं; उन्हें **falsy** कहा जाता है:
`false`, `0`, `-0`, `0n`, `""`, `null`, `undefined` और `NaN`।
**बाकी सब कुछ truthy है**, जिनमें कुछ ऐसे मान भी शामिल हैं जो खाली दिखते हैं:
```javascript
console.log(Boolean(0), Boolean(""), Boolean(NaN));
// prints false false false
console.log(Boolean("0"), Boolean("false"), Boolean([]), Boolean({}));
// prints true true true true
```
`"0"` एक गैर-खाली स्ट्रिंग है, इसलिए वह truthy है; एक खाली ऐरे एक ऑब्जेक्ट है, इसलिए वह भी truthy है।

# --instructions--

एक फंक्शन `countTruthy(values)` लिखें जो बताए कि ऐरे `values` के कितने तत्व truthy हैं

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function countTruthy(values) {

}
```

# --asserts--

`countTruthy([1, "a", true])` को `3` लौटाना चाहिए।

```javascript
tryCatch(countTruthy([1, "a", true]) === 3);
```

`countTruthy([0, "", null, undefined, NaN, false])` को `0` लौटाना चाहिए।

```javascript
tryCatch(countTruthy([0, "", null, undefined, NaN, false]) === 0);
```

`countTruthy(["0", "false", [], {}])` को `4` लौटाना चाहिए।

```javascript
tryCatch(countTruthy(["0", "false", [], {}]) === 4);
```

`countTruthy([0n, -0, " "])` को `1` लौटाना चाहिए, क्योंकि केवल स्पेस वाली स्ट्रिंग truthy है।

```javascript
tryCatch(countTruthy([0n, -0, " "]) === 1);
```

`countTruthy([])` को `0` लौटाना चाहिए।

```javascript
tryCatch(countTruthy([]) === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function countTruthy(values) {
  let count = 0;
  for (const value of values) {
    if (value) {
      count++;
    }
  }
  return count;
}
```
