---
language: javascript
exerciseType: 2
---

# --description--

कभी-कभी, आप ऐरे के केवल एक हिस्से को एक्सेस करना चाहते हैं।
निम्नलिखित कोड पर विचार करें:
```javascript
let numbers = [1, 2, 3, 4];
let slice = numbers.slice(1, 3);
console.log(slice);
// prints [2, 3]
```
पहले, हम `numbers` नामक एक ऐरे बनाते हैं।
फिर, हम ऐरे का एक उपभाग लेते हैं और इसे slice ऐरे में संग्रहीत करते हैं।
हम यह ऐरे के नाम के बाद इंडेक्स को परिभाषित करके करते हैं: `numbers.slice(1, 3)`।
ध्यान रखें कि दायां इंडेक्स शामिल नहीं होता

# --instructions--

`numbers` से केवल दो बीच की संख्याओं को शामिल करते हुए `middle` नामक एक ऐरे बनाएं

# --seed--

```javascript
let numbers = [10, 5, 5, 5, 3, 2, 1, 0];
let middle = [/].slice([/][/][/]);
console.log(middle);
```

# --answers--

- numbers
- 2
- :
- 3
- -1
- 6
- 3
- 5
- ...
- , 
- ..<

# --solutions--

```javascript
let numbers = [10, 5, 5, 5, 3, 2, 1, 0];
let middle = numbers.slice(3, 5);
console.log(middle);
```

# --output--

[5, 3]
