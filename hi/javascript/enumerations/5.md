---
language: javascript
exerciseType: 1
---

# --description--

क्योंकि एनम बस एक ऑब्जेक्ट है, इसलिए सामान्य ऑब्जेक्ट हेल्पर आपको इसकी जांच करने देते हैं:
- `Object.keys(Enum)` सदस्यों के **नामों** वाला एक ऐरे लौटाता है
- `Object.values(Enum)` सदस्यों के **मानों** वाला एक ऐरे लौटाता है
- `Object.entries(Enum)` `[नाम, मान]` जोड़ों का एक ऐरे लौटाता है

```javascript
const Color = Object.freeze({ RED: "red", BLUE: "blue" });
console.log(Object.keys(Color));
// prints [ 'RED', 'BLUE' ]
console.log(Object.values(Color));
// prints [ 'red', 'blue' ]
```
`Object.values()` को ऐरे मेथड `includes()` के साथ मिलाना, यह जांचने का मानक तरीका है कि कोई मनमाना मान, उदाहरण के लिए यूज़र इनपुट से पढ़ा गया मान, कोई मान्य सदस्य है या नहीं:
```javascript
console.log(Object.values(Color).includes("red"));
// prints true
console.log(Object.values(Color).includes("pink"));
// prints false
```

# --instructions--

एक फंक्शन `isColor(value)` लिखें जो `true` लौटाए अगर `value`, `Color` एनम के मानों में से एक है, अन्यथा `false` लौटाए

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Color = Object.freeze({
  RED: "red",
  GREEN: "green",
  BLUE: "blue",
});

function isColor(value) {

}
```

# --asserts--

`isColor("red")` को `true` लौटाना चाहिए।

```javascript
tryCatch(isColor("red") === true);
```

`isColor("blue")` को `true` लौटाना चाहिए।

```javascript
tryCatch(isColor("blue") === true);
```

`isColor("pink")` को `false` लौटाना चाहिए।

```javascript
tryCatch(isColor("pink") === false);
```

`isColor("RED")` को `false` लौटाना चाहिए, क्योंकि `"RED"` एक मान नहीं बल्कि एक सदस्य का नाम है।

```javascript
tryCatch(isColor("RED") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Color = Object.freeze({
  RED: "red",
  GREEN: "green",
  BLUE: "blue",
});

function isColor(value) {
  return Object.values(Color).includes(value);
}
```
