---
language: javascript
exerciseType: 1
---

# --instructions--

`HEARTS`, `DIAMONDS`, `CLUBS` और `SPADES` सदस्यों के साथ फ़्रीज़ किया गया एनम `Suit` डिक्लेयर करें, जिनमें से हर एक एक `Symbol` रखता हो जिसका विवरण सदस्य के नाम को लोअरकेस में रखता हो

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Suit = 
```

# --asserts--

`Suit` के हर सदस्य को एक सिंबल होना चाहिए।

```javascript
tryCatch(Object.values(Suit).every((s) => typeof s === "symbol"));
```

`Suit.HEARTS.description` `"hearts"` होना चाहिए।

```javascript
tryCatch(Suit.HEARTS.description === "hearts");
```

`Suit.DIAMONDS`, `Suit.CLUBS` और `Suit.SPADES` के विवरण क्रमशः `"diamonds"`, `"clubs"` और `"spades"` होने चाहिए।

```javascript
tryCatch(Suit.DIAMONDS.description === "diamonds" && Suit.CLUBS.description === "clubs" && Suit.SPADES.description === "spades");
```

`Suit` में ठीक चार सदस्य होने चाहिए, जो एक-दूसरे से अलग हों।

```javascript
tryCatch(new Set(Object.values(Suit)).size === 4);
```

`Suit` फ़्रीज़ किया हुआ होना चाहिए।

```javascript
tryCatch(Object.isFrozen(Suit) === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Suit = Object.freeze({
  HEARTS: Symbol("hearts"),
  DIAMONDS: Symbol("diamonds"),
  CLUBS: Symbol("clubs"),
  SPADES: Symbol("spades"),
});
```
