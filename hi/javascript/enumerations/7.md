---
language: javascript
exerciseType: 1
---

# --description--

एनम स्वाभाविक रूप से `switch` स्टेटमेंट के साथ जोड़े में काम करते हैं, जो एक मान की तुलना `case` लेबल की एक सूची से करता है और पहले मैच करने वाले का कोड चलाता है।
हर शाखा `return` या `break` पर खत्म होती है, और वैकल्पिक `default` शाखा तब चलती है जब कुछ भी मैच नहीं करता:
```javascript
const Light = Object.freeze({ RED: "red", GREEN: "green" });

function action(light) {
  switch (light) {
    case Light.RED:
      return "stop";
    case Light.GREEN:
      return "go";
    default:
      return "unknown";
  }
}
console.log(action(Light.GREEN));
// prints go
```
हमेशा सदस्यों (`Light.RED`) से तुलना करें, कभी कच्चे मानों (`"red"`) से नहीं: अगर मान कभी बदल जाए, तो भी `switch` काम करता रहेगा।

# --instructions--

एक फंक्शन `weekend(day)` लिखें जो `Day` एनम पर `switch` का उपयोग करे और `Day.SATURDAY` तथा `Day.SUNDAY` के लिए `true`, और किसी भी अन्य मान के लिए `false` लौटाए

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {

}
```

# --asserts--

`weekend(Day.SATURDAY)` को `true` लौटाना चाहिए।

```javascript
tryCatch(weekend(Day.SATURDAY) === true);
```

`weekend(Day.SUNDAY)` को `true` लौटाना चाहिए।

```javascript
tryCatch(weekend(Day.SUNDAY) === true);
```

`weekend(Day.MONDAY)` को `false` लौटाना चाहिए।

```javascript
tryCatch(weekend(Day.MONDAY) === false);
```

`weekend("holiday")` को `false` लौटाना चाहिए, क्योंकि यह कोई सदस्य नहीं है।

```javascript
tryCatch(weekend("holiday") === false);
```

फंक्शन में एक `switch` स्टेटमेंट का उपयोग होना चाहिए।

```javascript
tryCatch(/switch\s*\(/.test(weekend.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Day = Object.freeze({
  MONDAY: "monday",
  FRIDAY: "friday",
  SATURDAY: "saturday",
  SUNDAY: "sunday",
});

function weekend(day) {
  switch (day) {
    case Day.SATURDAY:
    case Day.SUNDAY:
      return true;
    default:
      return false;
  }
}
```
