---
language: javascript
exerciseType: 4
---

# --description--

जब हर सदस्य को कई डेटा या अपने खुद के मेथड की ज़रूरत होती है, तो एक **क्लास** एनम की भूमिका निभा सकती है। हर सदस्य उस क्लास का एक इंस्टेंस होता है, जिसे एक `static` प्रॉपर्टी में स्टोर किया जाता है, यानी ऐसी प्रॉपर्टी जो हर इंस्टेंस के बजाय खुद क्लास से संबंधित होती है:
```javascript
class Planet {
  static MERCURY = new Planet("Mercury", 0.4);
  static EARTH = new Planet("Earth", 1);

  constructor(name, gravity) {
    this.name = name;
    this.gravity = gravity;
  }
}
console.log(Planet.EARTH.name);
// prints Earth
```
क्लास के बाद `Object.freeze(Planet)` को कॉल करें ताकि कोई भी सदस्य जोड़ या बदल न सके, और कंस्ट्रक्टर में `Object.freeze(this)` से हर इंस्टेंस को फ़्रीज़ करें ताकि सदस्य खुद रीड-ओनली बने रहें।

# --instructions--

पंक्तियों को इस तरह क्रमबद्ध करें कि क्लास-आधारित एनम `Coin` डिक्लेयर हो और `DIME` सदस्य के सेंट प्रिंट हों।
`PENNY` को `DIME` से पहले, दोनों को कंस्ट्रक्टर से पहले डिक्लेयर करें, और प्रिंट करने से पहले क्लास को फ़्रीज़ करें

# --answers--

```javascript
class Coin {
```

```javascript
  static PENNY = new Coin("penny", 1);
```

```javascript
  static DIME = new Coin("dime", 10);
```

```javascript
  constructor(name, cents) { this.name = name; this.cents = cents; }
```

```javascript
}
```

```javascript
Object.freeze(Coin);
```

```javascript
console.log(Coin.DIME.cents);
```

# --solutions--

```javascript
class Coin {
  static PENNY = new Coin("penny", 1);
  static DIME = new Coin("dime", 10);
  constructor(name, cents) { this.name = name; this.cents = cents; }
}
Object.freeze(Coin);
console.log(Coin.DIME.cents);
```

# --output--

10
