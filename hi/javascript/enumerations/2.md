---
language: javascript
exerciseType: 1
---

# --description--

हर सदस्य में कौन-सा मान स्टोर करना है, यह आप पर निर्भर है। **स्ट्रिंग्स** सबसे आम विकल्प हैं क्योंकि प्रिंट, लॉग या फ़ाइल में सेव करने पर ये पढ़ने में आसान होती हैं:
```javascript
const Status = Object.freeze({
  ACTIVE: "active",
  DONE: "done",
});
console.log(Status.DONE);
// prints done
```
एक बार फ़्रीज़ हो जाने पर ऑब्जेक्ट को नई प्रॉपर्टीज़ भी नहीं मिल सकतीं, और `Object.isFrozen(obj)` बताता है कि कोई ऑब्जेक्ट फ़्रीज़ किया गया है या नहीं:
```javascript
console.log(Object.isFrozen(Status));
// prints true
```

# --instructions--

`UP`, `DOWN`, `LEFT` और `RIGHT` सदस्यों के साथ फ़्रीज़ किया गया एनम `Direction` डिक्लेयर करें, जिनमें से हर एक अपने ही नाम को लोअरकेस में रखता हो (`"up"`, `"down"`, `"left"`, `"right"`)

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const Direction = 
```

# --asserts--

`Direction.UP` `"up"` होना चाहिए।

```javascript
tryCatch(Direction.UP === "up");
```

`Direction.DOWN` `"down"` होना चाहिए।

```javascript
tryCatch(Direction.DOWN === "down");
```

`Direction.LEFT` `"left"` और `Direction.RIGHT` `"right"` होना चाहिए।

```javascript
tryCatch(Direction.LEFT === "left" && Direction.RIGHT === "right");
```

`Direction` में ठीक चार सदस्य होने चाहिए।

```javascript
tryCatch(Object.keys(Direction).length === 4);
```

`Direction` फ़्रीज़ किया हुआ होना चाहिए।

```javascript
tryCatch(Object.isFrozen(Direction) === true);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const Direction = Object.freeze({
  UP: "up",
  DOWN: "down",
  LEFT: "left",
  RIGHT: "right",
});
```
