---
language: javascript
exerciseType: 2
---

# --description--

सदस्य **नंबर** भी रख सकते हैं। संख्यात्मक मान तब उपयोगी होते हैं जब सदस्यों के बीच एक स्वाभाविक क्रम होता है, क्योंकि आप उन्हें सामान्य ऑपरेटरों से तुलना कर सकते हैं:
```javascript
const Priority = Object.freeze({
  LOW: 1,
  MEDIUM: 2,
  HIGH: 3,
});
console.log(Priority.HIGH > Priority.LOW);
// prints true
```
इसकी कीमत पठनीयता है: `Priority.HIGH` प्रिंट करने पर `3` दिखता है, जो स्ट्रिंग `"high"` की तुलना में बहुत कम जानकारी देता है।

# --instructions--

`EASY` को `1`, `NORMAL` को `2` और `HARD` को `3` के बराबर रखते हुए फ़्रीज़ किया गया संख्यात्मक एनम `Level` डिक्लेयर करें, फिर प्रिंट करें कि क्या `HARD`, `EASY` से बड़ा है और `HARD` तथा `EASY` के बीच का अंतर भी प्रिंट करें

# --seed--

```javascript
const Level = Object.freeze({
  EASY: [/],
  NORMAL: 2,
  HARD: [/],
});
console.log(Level.HARD [/] Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --answers--

- 1
- 3
- >
- 0
- <
- ===

# --solutions--

```javascript
const Level = Object.freeze({
  EASY: 1,
  NORMAL: 2,
  HARD: 3,
});
console.log(Level.HARD > Level.EASY);
console.log(Level.HARD - Level.EASY);
```

# --output--

true
2
