---
language: javascript
exerciseType: 1
---

# --description--

एक **रेस्ट पैरामीटर** `...name` किसी भी संख्या में आर्ग्युमेंट्स को एक ऐरे में इकट्ठा करता है, और यह एरो फंक्शन्स में भी काम करता है:
```javascript
const count = (...items) => items.length;
console.log(count("a", "b", "c"));
// prints 3
```
सामान्य फंक्शन्स में एक छिपा हुआ ऐरे-जैसा `arguments` ऑब्जेक्ट भी होता है जिसमें उन्हें मिले हर आर्ग्युमेंट होते हैं। एरो फंक्शन्स में **ऐसा कुछ नहीं होता**: एरो के अंदर, `arguments` चारों ओर के फंक्शन के `arguments` को संदर्भित करता है या बिल्कुल मौजूद नहीं होता। जब भी आपको एरो फंक्शन में "सभी आर्ग्युमेंट्स" चाहिए, एक रेस्ट पैरामीटर का उपयोग करें।

# --instructions--

कांस्टेंट `sum` में एक एरो फंक्शन स्टोर करें जो एक रेस्ट पैरामीटर के ज़रिए किसी भी संख्या में संख्याएँ स्वीकार करे और उनका कुल योग लौटाए, या बिना आर्ग्युमेंट्स के कॉल करने पर `0` लौटाए

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
const sum = 
```

# --asserts--

`sum(1, 2, 3)` को `6` लौटाना चाहिए।

```javascript
tryCatch(sum(1, 2, 3) === 6);
```

`sum(5)` को `5` लौटाना चाहिए।

```javascript
tryCatch(sum(5) === 5);
```

`sum()` को `0` लौटाना चाहिए।

```javascript
tryCatch(sum() === 0);
```

`sum(10, -4, 2.5)` को `8.5` लौटाना चाहिए।

```javascript
tryCatch(sum(10, -4, 2.5) === 8.5);
```

`sum` एक ऐसा एरो फंक्शन होना चाहिए जो रेस्ट पैरामीटर का उपयोग करता है।

```javascript
tryCatch(!/function/.test(sum.toString()) && /\(\s*\.\.\.\w+\s*\)/.test(sum.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
const sum = (...numbers) => numbers.reduce((total, n) => total + n, 0);
```
