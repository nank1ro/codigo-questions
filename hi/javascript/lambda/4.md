---
language: javascript
exerciseType: 2
---

# --description--

ऑब्जेक्ट लिटरल को कोष्ठकों में लपेटना एक वन-लाइन एरो फंक्शन से ऑब्जेक्ट बनाने का मानक तरीका है, उदाहरण के लिए जब आप कुछ मानों को एक रिकॉर्ड में बदलते हैं:
```javascript
const user = (name, age) => ({ name: name, age: age });
console.log(user("Ana", 30).age);
// prints 30
```
बिना पैरामीटर्स वाला एरो फंक्शन खाली कोष्ठकों के जोड़े `()` से शुरू होता है:
```javascript
const empty = () => ({});
console.log(empty());
// prints {}
```

# --instructions--

`point` को पूरा करें ताकि वह प्रॉपर्टीज़ `x` और `y` वाला एक ऑब्जेक्ट लौटाए, और `origin` को इस तरह पूरा करें कि वह कोई पैरामीटर न ले, फिर `point(3, 4).y` और `origin().x` प्रिंट करें

# --seed--

```javascript
const point = (x, y) => [/]{ x: x, y: y }[/];
const origin = [/] => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --answers--

- (
- )
- ()
- [
- ]
- function
- this

# --solutions--

```javascript
const point = (x, y) => ({ x: x, y: y });
const origin = () => ({ x: 0, y: 0 });
console.log(point(3, 4).y);
console.log(origin().x);
```

# --output--

4
0
