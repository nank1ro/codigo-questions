---
language: javascript
exerciseType: 4
---

# --instructions--

पंक्तियों को क्रमबद्ध करें ताकि `makeCounter` डिफ़ाइन हो, जो एक ऐसा एरो फंक्शन लौटाता है जो अपने निजी `count` को बढ़ाकर लौटाता है, फिर काउंटर `next` बनाएं और एक ही पंक्ति पर दो कॉल्स का परिणाम प्रिंट करें

# --answers--

```javascript
const makeCounter = () => {
```

```javascript
  let count = 0;
```

```javascript
  return () => {
```

```javascript
    count += 1;
```

```javascript
    return count;
```

```javascript
  };
```

```javascript
};
```

```javascript
const next = makeCounter();
```

```javascript
console.log(next(), next());
```

# --solutions--

```javascript
const makeCounter = () => {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
};
const next = makeCounter();
console.log(next(), next());
```

# --output--

1 2
