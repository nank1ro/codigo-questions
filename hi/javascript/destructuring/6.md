---
language: javascript
exerciseType: 2
---

# --description--

ऑब्जेक्ट्स को भी डिस्ट्रक्चर किया जा सकता है, चौकोर ब्रैकेट की जगह घुंघराले ब्रेसिज़ के साथ। यहाँ स्थिति का कोई मतलब नहीं: हर नाम को उस **कुंजी** से मिलाया जाता है जिसकी वर्तनी बिल्कुल वैसी ही हो:
```javascript
const user = { name: "Ada", age: 36 };
const { age, name } = user;
console.log(name, age);
// prints Ada 36
```
पैटर्न में `age` और `name` की जगह बदलने से कुछ नहीं बदलता, और पैटर्न में जिन कुंजियों का ज़िक्र नहीं है वे वैसी की वैसी छूट जाती हैं। जिस नाम की कोई मेल खाती कुंजी नहीं होती वह `undefined` हो जाता है।

# --instructions--

दोनों पैटर्न पूरे करें ताकि प्रोग्राम `Dune (1965) by Herbert` प्रिंट करे

# --seed--

```javascript
const book = { title: "Dune", author: "Herbert", year: 1965 };
const { title, [/] } = book;
const { author } = [/];
console.log(`${title} (${year}) by ${author}`);
```

# --answers--

- year
- book
- title
- published
- novel
- 1965

# --solutions--

```javascript
const book = { title: "Dune", author: "Herbert", year: 1965 };
const { title, year } = book;
const { author } = book;
console.log(`${title} (${year}) by ${author}`);
```

# --output--

Dune (1965) by Herbert
