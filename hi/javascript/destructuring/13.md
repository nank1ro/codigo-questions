---
language: javascript
exerciseType: 4
---

# --description--

फंक्शन घोषणा में पैटर्न किसी पैरामीटर के नाम की जगह ले सकता है, ताकि खोलना उसी समय हो जब कॉल किया जाता है:
```javascript
function area({ width, height }) {
    return width * height;
}
console.log(area({ width: 4, height: 3 }));
// 12 प्रिंट करता है
```
बॉडी के अंदर कोई ऑब्जेक्ट वेरिएबल बिल्कुल नहीं होता, केवल `width` और `height`। कॉल करने वाला एक ऑब्जेक्ट पास करता है, लेकिन सिग्नेचर साफ़ बता देता है कि फंक्शन कौन-सी कुंजियाँ पढ़ता है, और कुंजियाँ किसी भी क्रम में आ सकती हैं।

# --instructions--

आइटम्स को उस प्रोग्राम के सही क्रम में लगाएं जो पहले फंक्शन घोषित करता है और फिर आयत का क्षेत्रफल प्रिंट करता है

# --answers--

```javascript
function area({ width, height }) {
```

```javascript
  return width * height;
}
```

```javascript
const rect = { width: 4, height: 3 };
```

```javascript
console.log(area(rect));
```

# --solutions--

```javascript
function area({ width, height }) {
  return width * height;
}
const rect = { width: 4, height: 3 };
console.log(area(rect));
```

# --output--

12
