---
language: javascript
exerciseType: 4
---

# --description--

`Object.entries(obj)` किसी ऑब्जेक्ट को `[key, value]` जोड़ों के ऐरे में बदल देता है। `for...of` लूप के हेड में एक ऐरे पैटर्न रखें और लूप चलते समय हर जोड़ा खुल जाता है:
```javascript
const ages = { ada: 36, bob: 41 };
for (const [name, age] of Object.entries(ages)) {
    console.log(`${name} is ${age}`);
}
// prints ada is 36
// prints bob is 41
```
किसी ऑब्जेक्ट पर चलने का यह सबसे पढ़ने-योग्य तरीका है: न कोई इंडेक्स, न कोई लुकअप, बस वही दो नाम जिनकी आपको परवाह है। `Object.keys` और `Object.values` हर एक केवल एक पक्ष देते हैं, `Object.entries` दोनों देता है।

# --instructions--

आइटम्स को उस प्रोग्राम के सही क्रम में लगाएं जो प्रिंट करता है कि स्टॉक में कितने प्रकार के फल हैं और फिर हर एंट्री के लिए एक पंक्ति

# --answers--

```javascript
const stock = { apples: 3, pears: 5 };
```

```javascript
console.log(`Stock (${Object.keys(stock).length}):`);
```

```javascript
for (const [item, count] of Object.entries(stock)) {
```

```javascript
  console.log(`${item}: ${count}`);
}
```

# --solutions--

```javascript
const stock = { apples: 3, pears: 5 };
console.log(`Stock (${Object.keys(stock).length}):`);
for (const [item, count] of Object.entries(stock)) {
  console.log(`${item}: ${count}`);
}
```

# --output--

Stock (2):
apples: 3
pears: 5
