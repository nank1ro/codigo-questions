---
language: javascript
exerciseType: 4
---

# --instructions--

पंक्तियों को क्रम में लगाकर `name` को उसके पहले अक्षर को अपरकेस करके प्रिंट करें

# --answers--

```javascript
let name = "alice";
```

```javascript
let first = name.charAt(0).toUpperCase();
```

```javascript
let capitalized = first + name.slice(1);
```

```javascript
console.log(capitalized);
```

# --solutions--

```javascript
let name = "alice";
let first = name.charAt(0).toUpperCase();
let capitalized = first + name.slice(1);
console.log(capitalized);
```

# --output--

Alice
