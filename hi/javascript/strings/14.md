---
language: javascript
exerciseType: 1
---

# --description--

`padStart(targetLength, padString)` मेथड स्ट्रिंग के `targetLength` वर्णों तक पहुँचने तक उसके **शुरुआत** में `padString` जोड़ता है। `padEnd()` अंत में भी वही काम करता है:
```javascript
console.log("7".padStart(3, "0"));
// prints 007
console.log("Tea".padEnd(6, "."));
// prints Tea...
```
अगर स्ट्रिंग पहले से ही पर्याप्त लंबी है, तो वह बिना बदले लौटा दी जाती है।
संख्याओं में स्ट्रिंग मेथड नहीं होते, इसलिए पहले उन्हें `String(number)` से बदलें।

# --instructions--

एक फंक्शन `twoDigits(number)` लिखें जो `number` को कम से कम दो वर्णों की स्ट्रिंग के रूप में लौटाए, जिसके बाईं ओर `"0"` से पैडिंग की गई हो

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function twoDigits(number) {

}
```

# --asserts--

`twoDigits(5)` को `"05"` लौटाना चाहिए।

```javascript
tryCatch(twoDigits(5) === "05");
```

`twoDigits(12)` को `"12"` लौटाना चाहिए।

```javascript
tryCatch(twoDigits(12) === "12");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function twoDigits(number) {
  return String(number).padStart(2, "0");
}
```
