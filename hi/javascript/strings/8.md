---
language: javascript
exerciseType: 4
---

# --instructions--

पंक्तियों को क्रम में लगाकर `file` का एक्सटेंशन अपरकेस में प्रिंट करें

# --answers--

```javascript
let file = "report.pdf";
```

```javascript
let extension = file.slice(-3);
```

```javascript
console.log(extension.toUpperCase());
```

# --solutions--

```javascript
let file = "report.pdf";
let extension = file.slice(-3);
console.log(extension.toUpperCase());
```

# --output--

PDF
