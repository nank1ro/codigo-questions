---
language: javascript
exerciseType: 1
---

# --description--

यह जाँचने के लिए कि कोई स्ट्रिंग दूसरी स्ट्रिंग को शामिल करती है या नहीं, इन मेथड का उपयोग करें, जो सभी एक बूलियन लौटाते हैं:
- `includes(text)` `true` होता है अगर `text` कहीं भी मौजूद हो
- `startsWith(text)` `true` होता है अगर स्ट्रिंग `text` से शुरू होती हो
- `endsWith(text)` `true` होता है अगर स्ट्रिंग `text` पर समाप्त होती हो

```javascript
let file = "photo.png";
console.log(file.includes("."));
// prints true
console.log(file.startsWith("ph"));
// prints true
console.log(file.endsWith(".jpg"));
// prints false
```
तुलना केस-सेंसिटिव होती है: `"Hello".includes("h")` `false` है।

# --instructions--

एक फंक्शन `isImage(fileName)` लिखें जो `true` लौटाए जब `fileName` `".png"` पर समाप्त हो, और अन्यथा `false` लौटाए

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isImage(fileName) {

}
```

# --asserts--

`isImage("cat.png")` को `true` लौटाना चाहिए।

```javascript
tryCatch(isImage("cat.png") === true);
```

`isImage("cat.jpg")` को `false` लौटाना चाहिए।

```javascript
tryCatch(isImage("cat.jpg") === false);
```

`isImage("png.txt")` को `false` लौटाना चाहिए।

```javascript
tryCatch(isImage("png.txt") === false);
```

`isImage("a.png.txt")` को `false` लौटाना चाहिए।

```javascript
tryCatch(isImage("a.png.txt") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isImage(fileName) {
  return fileName.endsWith(".png");
}
```
