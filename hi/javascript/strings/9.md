---
language: javascript
exerciseType: 2
---

# --description--

`split(separator)` मेथड हर `separator` पर काटकर एक स्ट्रिंग को टुकड़ों की एक **ऐरे** में तोड़ता है:
```javascript
let sentence = "I like JavaScript";
let words = sentence.split(" ");
console.log(words);
// prints [ 'I', 'like', 'JavaScript' ]
```
इसका उल्टा ऐरे मेथड `join(separator)` है, जो टुकड़ों को वापस एक स्ट्रिंग में जोड़ देता है:
```javascript
console.log(words.join("-"));
// prints I-like-JavaScript
```

# --instructions--

`csv` को हर कॉमा पर विभाजित करें और यह प्रिंट करें कि इसमें कितने रंग हैं

# --seed--

```javascript
let csv = "red,green,blue";
let colors = csv.[/]([/]);
console.log(colors.[/]);
```

# --answers--

- split
- ","
- " "
- join
- length
- size

# --solutions--

```javascript
let csv = "red,green,blue";
let colors = csv.split(",");
console.log(colors.length);
```

# --output--

3
