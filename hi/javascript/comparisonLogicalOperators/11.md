---
language: javascript
exerciseType: 2
---

# --description--

`!=` तुलना करने से पहले बदलाव करता है, बिल्कुल `==` की तरह, इसलिए `"5" != 5` का मान `false` होता है। इसका सख्त समकक्ष `!==` एक स्ट्रिंग और एक संख्या को अलग मानता है चाहे उनमें कुछ भी हो।
```javascript
console.log("5" !== 5); // true
```

# --instructions--

जाँचें कि `word` `dog` के **बराबर नहीं** है, boolean परिणाम प्रिंट करें

# --seed--

```javascript
var word = "cat";
console.log([/][/][/]);
```

# --answers--

- word 
-  "dog"
- == 
- = 
- dog
- !=

# --solutions--

```javascript
var word = "cat";
console.log(word != "dog");
```

# --output--

true
