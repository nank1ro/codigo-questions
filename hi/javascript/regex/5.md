---
language: javascript
exerciseType: 4
---

# --description--

`test` केवल हाँ या ना बताता है। मेल खाने वाला टेक्स्ट स्वयं पाने के लिए स्ट्रिंग पर **`match`** कॉल करें:
```javascript
const match = "order 42 shipped".match(/\d+/);
```
जब कुछ भी मेल नहीं खाता, तो `match` `null` लौटाता है। जब कुछ मेल खाता है, तो वह एक ऐरे-जैसा परिणाम लौटाता है:
- `match[0]` वह टेक्स्ट है जो मेल खाया था
- `match.index` वह स्थिति है जहाँ मेल शुरू होता है
- `match.input` पूरी स्ट्रिंग है जो खोजी गई थी

```javascript
console.log(match[0]);
// 42 प्रिंट करता है
console.log(match.index);
// 6 प्रिंट करता है
```
चूँकि परिणाम `null` हो सकता है, `match[0]` पढ़ने से पहले उसे जाँचें।

# --instructions--

आइटमों को एक ऐसे प्रोग्राम में क्रमबद्ध करें जो टेक्स्ट की पहली अंक-शृंखला प्रिंट करे और फिर वह स्थिति जहाँ वह शुरू होती है। पहले टेक्स्ट घोषित करें, फिर मेल

# --answers--

```javascript
const text = "order 42 shipped";
```

```javascript
const match = text.match(/\d+/);
```

```javascript
console.log(match[0]);
```

```javascript
console.log(match.index);
```

# --solutions--

```javascript
const text = "order 42 shipped";
const match = text.match(/\d+/);
console.log(match[0]);
console.log(match.index);
```

# --output--

42
6
