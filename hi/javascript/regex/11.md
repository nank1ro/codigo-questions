---
language: javascript
exerciseType: 1
---

# --description--

`g` फ्लैग आपको हर मेल खाने वाली स्ट्रिंग देता है, लेकिन वह ग्रुप को छोड़ देता है। जब आपको *हर* मेल के ग्रुप चाहिए, तो **`matchAll`** का उपयोग करें। वह पूर्ण मेल ऑब्जेक्ट्स का एक इटरेटर लौटाता है, जिनमें से प्रत्येक बिल्कुल साधारण `match` के परिणाम जैसा होता है:
```javascript
const text = "a=1;b=2";
for (const match of text.matchAll(/(?<key>\w+)=(?<value>\w+)/g)) {
  console.log(`${match.groups.key} -> ${match.groups.value}`);
}
// a -> 1 प्रिंट करता है
// b -> 2 प्रिंट करता है
```
`matchAll` को `g` फ्लैग की आवश्यकता होती है; उसके बिना वह `TypeError` फेंकता है। चूँकि वह एक इटरेटर लौटाता है, एक वास्तविक ऐरे चाहने पर उसे `[...text.matchAll(pattern)]` से स्प्रेड करें, और ध्यान रखें कि पैटर्न के कभी मेल न खाने पर वह बिल्कुल कुछ नहीं देता।

# --instructions--

एक फंक्शन `parsePairs` लिखें जो एक स्ट्रिंग `text` लेता है, उसमें हर `key=value` जोड़ी ढूँढता है, जहाँ कुंजियाँ और मान वर्ड कैरेक्टर की शृंखलाएँ होती हैं, और एक ऑब्जेक्ट लौटाता है जो हर कुंजी को उसके मान से जोड़ता है। उदाहरण के लिए `parsePairs("host=local;port=80")` `{ host: "local", port: "80" }` लौटाता है

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function parsePairs(text) {
  
}
```

# --asserts--

`parsePairs("host=local;port=80")` को `host` और `port` वाला एक ऑब्जेक्ट लौटाना चाहिए।

```javascript
tryCatch(JSON.stringify(parsePairs("host=local;port=80")) === '{"host":"local","port":"80"}');
```

`parsePairs("a=1")` को केवल `a` वाला एक ऑब्जेक्ट लौटाना चाहिए।

```javascript
tryCatch(JSON.stringify(parsePairs("a=1")) === '{"a":"1"}');
```

`parsePairs("nothing here")` को एक खाली ऑब्जेक्ट लौटाना चाहिए।

```javascript
tryCatch(JSON.stringify(parsePairs("nothing here")) === '{}');
```

`parsePairs("x=1 y=2 z=3")` को तीनों जोड़ियाँ लौटानी चाहिए।

```javascript
tryCatch(JSON.stringify(parsePairs("x=1 y=2 z=3")) === '{"x":"1","y":"2","z":"3"}');
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function parsePairs(text) {
  const pairs = {};
  for (const match of text.matchAll(/(?<key>\w+)=(?<value>\w+)/g)) {
    pairs[match.groups.key] = match.groups.value;
  }
  return pairs;
}
```
