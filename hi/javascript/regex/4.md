---
language: javascript
exerciseType: 1
---

# --description--

दो और बिल्डिंग ब्लॉक आपको लगभग कोई भी टेक्स्ट आकृति बताने देते हैं।

एक **कैरेक्टर क्लास** वर्गाकार कोष्ठकों के बीच वर्णों का एक समूह है; वह उनमें से ठीक एक से मेल खाता है। एक डैश एक रेंज लिखता है, और शुरुआत में `^` समूह को नकार देता है:
```javascript
/[aeiou]/   // एक स्वर
/[a-z]/     // एक लोअरकेस अक्षर
/[A-Z0-9]/  // एक अपरकेस अक्षर या एक अंक
/[^0-9]/    // एक कैरेक्टर जो अंक नहीं है
```
**क्वांटिफ़ायर** बताते हैं कि पिछला भाग कितनी बार दोहराया जाता है: `+` एक या अधिक, `*` शून्य या अधिक, `?` शून्य या एक, और `{n}` ठीक `n` बार।

अंत में, **एंकर** पैटर्न को टेक्स्ट के सिरों से बाँधते हैं: `^` का अर्थ है "यहाँ शुरू" और `$` का अर्थ है "यहाँ अंत"। इनके बिना एक पैटर्न स्ट्रिंग में कहीं भी मेल खा सकता है, इसलिए `/\d{2}/.test("abc12def")` का मान `true` है। दोनों एंकरों के साथ पूरी स्ट्रिंग के मेल खाना ज़रूरी हो जाता है:
```javascript
console.log(/^\d{2}$/.test("abc12def"));
// false प्रिंट करता है
console.log(/^\d{2}$/.test("12"));
// true प्रिंट करता है
```

# --instructions--

एक फंक्शन `isCode` लिखें जो एक स्ट्रिंग `text` लेता है और केवल तभी `true` लौटाता है जब `text` ठीक तीन बड़े अक्षरों के बाद दो अंकों से बना हो, उदाहरण के लिए `ABC12`

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isCode(text) {
  
}
```

# --asserts--

`isCode("ABC12")` को `true` लौटाना चाहिए।

```javascript
tryCatch(isCode("ABC12") === true);
```

`isCode("abc12")` को `false` लौटाना चाहिए।

```javascript
tryCatch(isCode("abc12") === false);
```

`isCode("ABC123")` को `false` लौटाना चाहिए।

```javascript
tryCatch(isCode("ABC123") === false);
```

`isCode("xABC12")` को `false` लौटाना चाहिए।

```javascript
tryCatch(isCode("xABC12") === false);
```

`isCode("AB12")` को `false` लौटाना चाहिए।

```javascript
tryCatch(isCode("AB12") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isCode(text) {
  return /^[A-Z]{3}\d{2}$/.test(text);
}
```
