---
language: javascript
exerciseType: 4
---

# --description--

रिप्लेसमेंट का एक स्ट्रिंग होना ज़रूरी नहीं। जब आप एक **फंक्शन** पास करते हैं, तो उसे हर मेल पर एक बार कॉल किया जाता है और जो कुछ वह लौटाता है वह उस मेल की जगह डाल दिया जाता है।

फंक्शन को पहले पूरा मेल मिलता है, फिर हर कैप्चर ग्रुप:
```javascript
console.log("hello world".replace(/\w+/g, (word) => word.length));
// 5 5 प्रिंट करता है

console.log("ann lee".replace(/(\w)(\w*)/g, (whole, first, rest) => first.toUpperCase() + rest));
// Ann Lee प्रिंट करता है
```
मेल खाए टेक्स्ट से रिप्लेसमेंट की गणना करने का यही एकमात्र तरीका है, जो अकेले `$1` नहीं कर सकता।

# --instructions--

आइटमों को एक ऐसे प्रोग्राम में क्रमबद्ध करें जो टेक्स्ट को हर अंक को उसके दोगुने से बदलकर प्रिंट करे। पहले टेक्स्ट घोषित करें, फिर फंक्शन

# --answers--

```javascript
const text = "a1 b2 c3";
```

```javascript
function double(digit) {
```

```javascript
  return Number(digit) * 2;
```

```javascript
}
```

```javascript
console.log(text.replace(/\d/g, double));
```

# --solutions--

```javascript
const text = "a1 b2 c3";
function double(digit) {
  return Number(digit) * 2;
}
console.log(text.replace(/\d/g, double));
```

# --output--

a2 b4 c6
