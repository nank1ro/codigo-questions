---
language: javascript
exerciseType: 1
---

# --description--

पैटर्न तब उपयोगी बनते हैं जब वे किसी एक सटीक वर्ण के बजाय वर्ण के एक *प्रकार* का वर्णन करते हैं। कुछ **एस्केप सीक्वेंस** ज़्यादातर ज़रूरतें पूरी कर देते हैं:
- `\d` कोई भी अंक, `0` से `9` तक
- `\w` कोई भी वर्ड कैरेक्टर: एक अक्षर, एक अंक या `_`
- `\s` कोई भी व्हाइटस्पेस: एक स्पेस, एक टैब, एक न्यूलाइन

```javascript
console.log(/\d/.test("room 12"));
// prints true
console.log(/\d/.test("lobby"));
// prints false
```
एक **क्वांटिफ़ायर** बताता है कि पिछला भाग कितनी बार दोहराया जा सकता है। सबसे सामान्य `+` है, जिसका अर्थ है "एक या अधिक":
```javascript
console.log(/\d+/.test("42"));
// prints true
```
इसलिए `/\d/` एक अकेले अंक से मेल खाता है और `/\d+/` अंकों की एक शृंखला से मेल खाता है। साधारण `test` के लिए दोनों एक जैसा व्यवहार करते हैं, क्योंकि दोनों को केवल एक अंक के मौजूद होने की ज़रूरत होती है।

# --instructions--

एक फंक्शन `hasDigit` लिखें जो एक स्ट्रिंग `text` लेता है और `test` का उपयोग करके `true` लौटाता है जब `text` में कम से कम एक अंक हो

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function hasDigit(text) {
  
}
```

# --asserts--

`test` मेथड का उपयोग किया जाना चाहिए।

```json
{
  "regex": "\\.test\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`hasDigit("room 12")` को `true` लौटाना चाहिए।

```javascript
tryCatch(hasDigit("room 12") === true);
```

`hasDigit("lobby")` को `false` लौटाना चाहिए।

```javascript
tryCatch(hasDigit("lobby") === false);
```

`hasDigit("7")` को `true` लौटाना चाहिए।

```javascript
tryCatch(hasDigit("7") === true);
```

`hasDigit("")` को `false` लौटाना चाहिए।

```javascript
tryCatch(hasDigit("") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function hasDigit(text) {
  return /\d/.test(text);
}
```
