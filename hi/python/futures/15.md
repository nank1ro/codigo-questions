---
language: python
exerciseType: 4
---

# --description--

विफल हो रहे कोरूटीन को संभालना सिंक्रोनस पैटर्न जैसा ही है: कोरूटीन अपवाद उठाता है, कॉलर `await` को `try`/`except` में लपेटता है और तय करता है कि एरर ऑब्जेक्ट का क्या करना है, उदाहरण के लिए उसका संदेश `print(e)` से प्रिंट करना।

# --instructions--

आइटम को सॉर्ट करें ताकि ऐसा प्रोग्राम बने जो इंपोर्ट से शुरू हो, `main` से पहले `load` को परिभाषित करे, `"config"` लोड करने का परिणाम प्रिंट करे, फिर खाली नाम लोड करने से उठने वाले एरर को पकड़े और उसका संदेश प्रिंट करे, जिससे आउटपुट `loaded config`, `empty name` प्राप्त हो

# --answers--

```python
import asyncio
```

```python
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
```

```python
async def main():
    print(await load("config"))
```

```python
    try:
        await load("")
```

```python
    except ValueError as e:
        print(e)
```

```python
asyncio.run(main())
```

# --solutions--

```python
import asyncio
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
async def main():
    print(await load("config"))
    try:
        await load("")
    except ValueError as e:
        print(e)
asyncio.run(main())
```

# --output--

loaded config
empty name
