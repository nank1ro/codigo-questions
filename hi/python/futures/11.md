---
language: python
exerciseType: 2
---

# --description--

एक टास्क तब भी चलता रहता है चाहे कोई उसका इंतज़ार कर रहा हो या नहीं। उसे पहले बना लें, दूसरा काम करें, और उसे `await` केवल उस बिंदु पर करें जहाँ उसके परिणाम की ज़रूरत हो: इंतज़ार छोटा हो जाता है क्योंकि टास्क का कुछ हिस्सा पहले ही बैकग्राउंड में चल चुका होता है। टास्क की जाँच `task.done()` से भी की जा सकती है, जो उसके पूरा होने पर `True` लौटाता है।

# --instructions--

कोड को पूरा करें ताकि `compute()` एक टास्क के रूप में शेड्यूल हो, उसके चलते समय `"waiting"` प्रिंट हो, और फिर उसका मान पाने के लिए टास्क को await किया जाए, जिससे आउटपुट `waiting`, `42` प्राप्त हो

# --seed--

```python
import asyncio

async def compute():
    await asyncio.sleep(0.01)
    return 42

async def main():
    task = asyncio.[/](compute())
    print("waiting")
    value = [/] task
    print(value)

asyncio.run(main())
```

# --answers--

- create_task
- await
- gather
- run
- sleep
- result

# --solutions--

```python
import asyncio

async def compute():
    await asyncio.sleep(0.01)
    return 42

async def main():
    task = asyncio.create_task(compute())
    print("waiting")
    value = await task
    print(value)

asyncio.run(main())
```

# --output--

waiting
42
