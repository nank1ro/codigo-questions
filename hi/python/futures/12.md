---
language: python
exerciseType: 1
---

# --description--

कोरूटीन के अंदर उठाया गया अपवाद उस जगह नहीं दिखता जहाँ कोरूटीन ऑब्जेक्ट बनाया गया था: यह उस `await` पर उठता है जो उसे चलाता है, या सबसे बाहरी कोरूटीन के लिए `asyncio.run` द्वारा। इसलिए `try`/`except` को **`await`** को लपेटना (wrap) होता है:
```python
import asyncio

async def load(path):
    await asyncio.sleep(0.01)
    if path == "":
        raise FileNotFoundError("empty path")
    return "contents"

async def safe_load(path):
    try:
        return await load(path)
    except FileNotFoundError:
        return ""

print(asyncio.run(safe_load("")))  # prints an empty line
```
कोई अपवाद जिसे कोई नहीं पकड़ता, वह हर `await` से होता हुआ `asyncio.run` तक फैलता है, जो उसे सिंक्रोनस कोड में दोबारा उठा देता है, बिल्कुल साधारण कॉल स्टैक की तरह।

# --instructions--

एक कोरूटीन फ़ंक्शन `divide(a, b)` लिखें जो `0.01` सेकंड इंतज़ार करे और `a / b` लौटाए, और एक कोरूटीन फ़ंक्शन `safe_divide(a, b)` लिखें जो `divide(a, b)` को await करे और उसका परिणाम लौटाए, या `ZeroDivisionError` उठने पर `None` लौटाए

# --before-asserts--

```python
import asyncio
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`divide(10, 4)` को `2.5` लौटाना चाहिए

```python
    def test_1(self):
        self.assertEqual(asyncio.run(divide(10, 4)), 2.5, "--err-t1--")
```

`divide(1, 0)` चलाने पर एक `ZeroDivisionError` उठाना चाहिए

```python
    def test_2(self):
        with self.assertRaises(ZeroDivisionError, msg="--err-t2--"):
            asyncio.run(divide(1, 0))
```

`safe_divide(10, 2)` को `5.0` लौटाना चाहिए और `safe_divide(1, 0)` को `None` लौटाना चाहिए

```python
    def test_3(self):
        self.assertEqual(asyncio.run(safe_divide(10, 2)), 5.0, "--err-t3--")
        self.assertIsNone(asyncio.run(safe_divide(1, 0)), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def divide(a, b):
    await asyncio.sleep(0.01)
    return a / b

async def safe_divide(a, b):
    try:
        return await divide(a, b)
    except ZeroDivisionError:
        return None
```
