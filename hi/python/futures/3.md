---
language: python
exerciseType: 1
---

# --description--

`await` वर्तमान कोरूटीन को तब तक रोक देता है जब तक awaited संक्रिया पूरी न हो जाए, और फिर उसका परिणाम लौटा देता है। जिस दौरान कोरूटीन रुका रहता है, इवेंट लूप अन्य कोरूटीन चलाने के लिए स्वतंत्र रहता है। `await` केवल `async def` के अंदर ही अनुमत है।

`asyncio.sleep(seconds)` सबसे सरल awaitable है: यह लूप को ब्लॉक किए बिना दिए गए समय तक इंतज़ार करता है। `time.sleep` के विपरीत, इसे await किया जाना ज़रूरी है:
```python
import asyncio

async def countdown():
    print("ready")
    await asyncio.sleep(0.05)
    print("go")

asyncio.run(countdown())
```
प्रोग्राम `ready` प्रिंट करता है, 50 मिलीसेकंड इंतज़ार करता है और `go` प्रिंट करता है। `asyncio.sleep(0.05)` को `await` के बिना लिखने पर कोरूटीन ऑब्जेक्ट तो बनता है लेकिन वह कभी नहीं चलता, इसलिए कोई इंतज़ार नहीं होता।

# --instructions--

एक कोरूटीन फ़ंक्शन `wait_and_return(value, delay)` लिखें जो `asyncio.sleep` से `delay` सेकंड इंतज़ार करे और फिर `value` लौटाए

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`wait_and_return("ok", 0.01)` को `"ok"` लौटाना चाहिए

```python
    def test_1(self):
        self.assertEqual(asyncio.run(wait_and_return("ok", 0.01)), "ok", "--err-t1--")
```

`wait_and_return(7, 0.02)` को `7` लौटाना चाहिए और उसमें कम से कम `0.02` सेकंड लगने चाहिए

```python
    def test_2(self):
        start = time.perf_counter()
        result = asyncio.run(wait_and_return(7, 0.02))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, 7, "--err-t2--")
        self.assertGreaterEqual(elapsed, 0.018, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def wait_and_return(value, delay):
    await asyncio.sleep(delay)
    return value
```
