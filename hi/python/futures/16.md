---
language: python
exerciseType: 1
---

# --description--

ये सभी हिस्से सहज रूप से आपस में जुड़ते हैं। कई संक्रियाएँ समानांतर रूप से चलाने के लिए, हर एक की अपनी समय सीमा के साथ, हर संक्रिया को एक छोटे कोरूटीन में लपेटें जो `wait_for` लागू करे और `TimeoutError` को पकड़े, फिर सभी रैपर को `gather` करें:
```python
async def guarded(coro, limit):
    try:
        return await asyncio.wait_for(coro, timeout=limit)
    except TimeoutError:
        return None
```
`gather(*[guarded(job(x), limit) for x in items])` फिर समय पर पूरा होने वाले हर जॉब के लिए एक मान और समय पर पूरा न होने वाले हर जॉब के लिए `None` लौटाता है, मूल क्रम में, और पूरा बैच अधिकतर लगभग `limit` सेकंड लेता है।

# --instructions--

एक कोरूटीन फ़ंक्शन `job(delay)` लिखें जो `delay` सेकंड इंतज़ार करे और `delay` लौटाए, और एक कोरूटीन फ़ंक्शन `run_jobs(delays, limit)` लिखें जो हर delay के लिए एक `job` को **समानांतर** रूप से चलाए, हर एक को `asyncio.wait_for` के साथ `limit` सेकंड तक सीमित रखे, और एक लिस्ट लौटाए जिसमें समय पर पूरा होने वाले हर जॉब का delay और टाइमआउट हो जाने वाले हर जॉब के लिए `None` हो, मूल क्रम में

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`job(0.01)` को `0.01` लौटाना चाहिए

```python
    def test_1(self):
        self.assertEqual(asyncio.run(job(0.01)), 0.01, "--err-t1--")
```

`run_jobs([0.01, 0.06, 0.02], 0.04)` को `[0.01, None, 0.02]` लौटाना चाहिए और `run_jobs([], 0.01)` को `[]` लौटाना चाहिए

```python
    def test_2(self):
        self.assertEqual(asyncio.run(run_jobs([0.01, 0.06, 0.02], 0.04)), [0.01, None, 0.02], "--err-t2--")
        self.assertEqual(asyncio.run(run_jobs([], 0.01)), [], "--err-t2--")
```

`0.02` सेकंड के पाँच जॉब, `0.05` की सीमा के साथ, सभी पूरे होने चाहिए और समानांतर रूप से चलने चाहिए, जिसमें `0.1` सेकंड से बहुत कम समय लगे

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(run_jobs([0.02] * 5, 0.05))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [0.02] * 5, "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def job(delay):
    await asyncio.sleep(delay)
    return delay

async def guarded(delay, limit):
    try:
        return await asyncio.wait_for(job(delay), timeout=limit)
    except TimeoutError:
        return None

async def run_jobs(delays, limit):
    return await asyncio.gather(*[guarded(d, limit) for d in delays])
```
