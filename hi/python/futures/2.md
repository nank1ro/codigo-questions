---
language: python
exerciseType: 1
---

# --description--

एक कोरूटीन पैरामीटर ले सकता है और साधारण फ़ंक्शन की तरह ही `return` कर सकता है। कोरूटीन ऑब्जेक्ट बनाते समय वह मान उपलब्ध नहीं होता, केवल कोरूटीन के चलने के बाद ही मिलता है। `asyncio.run` वह मान लौटाता है जो कोरूटीन ने लौटाया हो:
```python
import asyncio

async def double(n):
    return n * 2

result = asyncio.run(double(21))
print(result)  # 42
```
फ़ंक्शन के बारे में जो कुछ भी आप जानते हैं वह `async def` के अंदर भी लागू होता है: लोकल वेरिएबल, शर्तें, लूप और एक से अधिक `return` कथन।

# --instructions--

`async def` के साथ एक कोरूटीन फ़ंक्शन `add(a, b)` लिखें जो `a` और `b` का योग लौटाता है

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`add` एक कोरूटीन फ़ंक्शन होना चाहिए

```python
    def test_1(self):
        self.assertTrue(inspect.iscoroutinefunction(add), "--err-t1--")
```

`add(1, 3)` चलाने पर `4` लौटाना चाहिए

```python
    def test_2(self):
        self.assertEqual(asyncio.run(add(1, 3)), 4, "--err-t2--")
```

`add(200, 210)` चलाने पर `410` लौटाना चाहिए

```python
    def test_3(self):
        self.assertEqual(asyncio.run(add(200, 210)), 410, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
async def add(a, b):
    return a + b
```
