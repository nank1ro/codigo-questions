---
language: python
exerciseType: 2
---

# --description--

`asyncio.wait_for(awaitable, timeout)` किसी चीज़ का इंतज़ार करता है लेकिन `timeout` सेकंड के बाद हार मान लेता है: संक्रिया रद्द कर दी जाती है और एक `TimeoutError` उठाया जाता है, जिसे किसी भी अपवाद की तरह पकड़ा जा सकता है:
```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())  # timed out
```
`0.1` के टाइमआउट के साथ वही कोड `done` प्रिंट करता। `asyncio.TimeoutError` बिल्ट-इन `TimeoutError` का दूसरा नाम है।

# --instructions--

कोड को पूरा करें ताकि `slow()` को `0.01` सेकंड के टाइमआउट के साथ await किया जाए और टाइमआउट पकड़ा जाए, जिससे आउटपुट `timed out` प्राप्त हो

# --seed--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.[/](slow(), [/]=0.01)
        print(result)
    except [/]:
        print("timed out")

asyncio.run(main())
```

# --answers--

- wait_for
- timeout
- TimeoutError
- gather
- delay
- ValueError

# --solutions--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())
```

# --output--

timed out
