---
language: python
exerciseType: 1
---

# --description--

कोरूटीन एक-दूसरे को `await` से कॉल करते हैं। एक कोरूटीन किसी भी दूसरे कोरूटीन को await कर सकता है, उसका रिटर्न मान प्राप्त कर सकता है और आगे बढ़ सकता है, बिल्कुल फ़ंक्शन कॉल की श्रृंखला की तरह:
```python
import asyncio

async def fetch_price(item):
    await asyncio.sleep(0.01)  # simulates a slow lookup
    return 10

async def total(item, quantity):
    price = await fetch_price(item)
    return price * quantity

print(asyncio.run(total("pen", 3)))  # 30
```
केवल सबसे बाहरी कोरूटीन `asyncio.run` से गुजरता है; हर भीतरी कोरूटीन तक `await` से पहुँचा जाता है। यदि `total` एक साधारण `def` होता, तो वह `await` बिल्कुल इस्तेमाल नहीं कर सकता था: `async` कीवर्ड श्रृंखला के हर उस फ़ंक्शन तक फैल जाता है जिसे इंतज़ार करना होता है।

# --instructions--

एक कोरूटीन फ़ंक्शन `fetch_user(user_id)` लिखें जो `0.01` सेकंड इंतज़ार करे और id के आगे `"user"` स्ट्रिंग लौटाए, और एक कोरूटीन फ़ंक्शन `greet_user(user_id)` लिखें जो `fetch_user` को await करे और प्राप्त नाम के आगे `"Hello, "` लौटाए

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch_user(1)` को `"user1"` लौटाना चाहिए और `fetch_user(42)` को `"user42"` लौटाना चाहिए

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch_user(1)), "user1", "--err-t1--")
        self.assertEqual(asyncio.run(fetch_user(42)), "user42", "--err-t1--")
```

`greet_user(1)` को `"Hello, user1"` लौटाना चाहिए

```python
    def test_2(self):
        self.assertEqual(asyncio.run(greet_user(1)), "Hello, user1", "--err-t2--")
```

`fetch_user` और `greet_user` दोनों कोरूटीन फ़ंक्शन होने चाहिए

```python
    def test_3(self):
        self.assertTrue(inspect.iscoroutinefunction(fetch_user), "--err-t3--")
        self.assertTrue(inspect.iscoroutinefunction(greet_user), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch_user(user_id):
    await asyncio.sleep(0.01)
    return f"user{user_id}"

async def greet_user(user_id):
    name = await fetch_user(user_id)
    return f"Hello, {name}"
```
