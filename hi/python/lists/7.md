---
language: python
exerciseType: 2
---

# --description--

आप एक स्ट्रिंग को ठीक लिस्ट की तरह स्लाइस कर सकते हैं! वास्तव में, आप स्ट्रिंग्स को अक्षरों की लिस्ट के रूप में सोच सकते हैं: प्रत्येक अक्षर लिस्ट में एक क्रमिक आइटम है, जो इंडेक्स `0` से शुरू होता है।
```python
list_name[:2]
# पहले दो आइटम लेता है
list_name[3:]
# चौथे से अंतिम आइटम तक लेता है
```
यदि आपके लिस्ट स्लाइस में लिस्ट (या स्ट्रिंग) का पहला या अंतिम आइटम शामिल है, तो उस आइटम के लिए इंडेक्स शामिल करना आवश्यक नहीं है।

# --instructions--

- `first_name` को `full_name` का इंडेक्स `0` से इंडेक्स `4` तक (*लेकिन इंडेक्स `4` को शामिल न करते हुए*) स्लाइस असाइन करें।
- `last_name` को `full_name` का इंडेक्स `4` से स्ट्रिंग के अंत तक का स्लाइस असाइन करें

# --seed--

```python
full_name = "Sam Smith"
first_name = [/][/]
last_name = [/][/]
print(first_name)
print(last_name)
```

# --answers--

- full_name
- [:4]
- [:3]
- full_name
- [4:]
- [4:2]

# --solutions--

```python
full_name = "Sam Smith"
first_name = full_name[:3]
last_name = full_name[4:]
print(first_name)
print(last_name)
```

# --output--

Sam
Smith
