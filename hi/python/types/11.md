---
language: python
exerciseType: 1
---

# --description--

फॉर्मेटिंग केवल यह बदलती है कि एक संख्या कैसे प्रदर्शित होती है। एक राउंड किया हुआ **मान** पाने के लिए बिल्ट-इन `round()` का उपयोग करें:
```python
print(round(3.14159, 2))  # 3.14
print(round(2.71828, 1))  # 2.7
```
एक आर्ग्युमेंट के साथ `round()` निकटतम पूर्ण संख्या तक राउंड करके एक `int` वापस करता है; दशमलवों की संख्या देने पर वह एक `float` वापस करता है:
```python
print(round(3.7))     # 4
print(round(3.7, 0))  # 4.0
```
ध्यान दें कि दो संख्याओं के ठीक बीच में आने वाले मान **सम** संख्या की ओर राउंड होते हैं: `round(2.5)` का परिणाम `2` है और `round(3.5)` का `4`।

# --instructions--

एक फंक्शन `average` लिखें जो संख्याओं की एक लिस्ट लेता है और उनका औसत दो दशमलवों तक राउंड करके वापस करता है

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको `round()` का उपयोग करना चाहिए

```json
{
  "regex": "round\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`average([1, 2, 3, 4])` को `2.5` वापस करना चाहिए

```python
    def test_1(self):
        self.assertEqual(average([1, 2, 3, 4]), 2.5, "--err-t1--")
```

`average([1, 2, 2])` को `1.67` वापस करना चाहिए

```python
    def test_2(self):
        self.assertEqual(average([1, 2, 2]), 1.67, "--err-t2--")
        self.assertEqual(average([1, 1, 1, 2]), 1.25, "--err-t2--")
```

`average([10])` को float `10.0` वापस करना चाहिए

```python
    def test_3(self):
        self.assertIsInstance(average([10]), float, "--err-t3--")
        self.assertEqual(average([10]), 10.0, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def average(numbers):
    return round(sum(numbers) / len(numbers), 2)
```
