---
language: python
exerciseType: 2
---

# --description--

docstring फेंकी नहीं जाती: Python उसे फंक्शन के `__doc__` एट्रिब्यूट में संग्रहीत करता है, ताकि प्रोग्राम चलते समय अपना डॉक्यूमेंटेशन पढ़ सके:
```python
def greet(name):
    """Returns the greeting for name."""
    return "Hi, " + name + "!"

print(greet.__doc__)  # Returns the greeting for name.
```
जब किसी फंक्शन की docstring नहीं होती, तो `__doc__` `None` होता है। `help(greet)` यही प्रिंट करता है, और एडिटर भी नाम पर होवर करने पर यही दिखाता है।

# --instructions--

कोड को पूरा करें ताकि `greet` को docstring मिले और प्रोग्राम उसे प्रिंट करे

# --seed--

```python
def greet(name):
    [/]Returns a greeting for name.[/]
    return "Hi, " + name + "!"

print(greet.[/])
```

# --answers--

- """
- """
- __doc__
- __name__
- #

# --solutions--

```python
def greet(name):
    """Returns a greeting for name."""
    return "Hi, " + name + "!"

print(greet.__doc__)
```

# --output--

Returns a greeting for name.
