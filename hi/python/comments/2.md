---
language: python
exerciseType: 1
---

# --description--

कमेंट को अपनी अलग लाइन की ज़रूरत नहीं होती: यह उसी लाइन पर कोड के बाद आ सकता है। इसे **इनलाइन कमेंट** कहते हैं, और यह उस विशेष स्टेटमेंट के बारे में छोटा नोट लिखने की अच्छी जगह है:
```python
retries = 3  # give up after three attempts
```
`#` से लाइन के अंत तक की सब कुछ अनदेखा कर दिया जाता है, जबकि उससे पहले का कोड सामान्य रूप से चलता है।

Python की स्टाइल गाइड, **PEP 8**, यहाँ थोड़ी स्पेसिंग माँगती है: कोड और `#` के बीच कम से कम **दो स्पेस**, और `#` के बाद **एक स्पेस**। अपनी अलग लाइन पर लिखे कमेंट को केवल `#` के बाद की स्पेस चाहिए।

# --instructions--

`seats = 4` के बाद एक इनलाइन कमेंट जोड़ें जो मान को समझाता है, उदाहरण के लिए `# seats per table`। `#` से पहले दो स्पेस छोड़ें

# --seed--

```python
seats = 4
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

लाइन `seats = 4` का अंत एक `#` कमेंट होना चाहिए, जिसमें `#` से पहले दो स्पेस और उसके बाद एक स्पेस हो

```json
{
  "regex": "seats = 4[ \\t]{2,}#[ \\t]\\S",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`seats` अभी भी `4` के बराबर होना चाहिए

```python
    def test_1(self):
        self.assertEqual(seats, 4, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
seats = 4  # seats per table
```
