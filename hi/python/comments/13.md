---
language: python
exerciseType: 1
---

# --description--

कुछ कमेंट्स एक ऐसी परंपरा का पालन करते हैं जिसे एडिटर समझते हैं। सबसे आम **मार्कर** ये हैं:
- `# TODO: ...` उस चीज़ को चिह्नित करता है जिसे अभी लिखा जाना बाकी है
- `# FIXME: ...` उस कोड को चिह्नित करता है जो ज्ञात रूप से ग़लत है और ठीक किया जाना चाहिए

```python
limit = 10
# TODO: read the limit from the settings
```
Python के लिए ये सामान्य कमेंट्स हैं; एडिटर इन्हें एक अलग पैनल में सूचीबद्ध करते हैं, ताकि बचा हुआ काम आसानी से मिल जाए। एक `TODO` आमतौर पर एक प्लेसहोल्डर के पास होता है जो असली कोड लिखे जाने तक प्रोग्राम को चलता रखता है।

जब आप काम पूरा कर लें, तो उसी बदलाव में प्लेसहोल्डर बदलें और मार्कर हटा दें, ताकि कमेंट कोड की स्थिति के बारे में कभी झूठ न बोले।

# --instructions--

`is_even` में `TODO` कमेंट में बताया गया काम पूरा करें, फिर काम हो जाने के कारण उस कमेंट को हटा दें

# --seed--

```python
def is_even(n):
    # TODO: return True when n is even, False otherwise
    return False
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`TODO` कमेंट हटाया जाना चाहिए

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`is_even(4)` को `True` लौटाना चाहिए

```python
    def test_1(self):
        self.assertEqual(is_even(4), True, "--err-t1--")
```

`is_even(7)` को `False` लौटाना चाहिए

```python
    def test_2(self):
        self.assertEqual(is_even(7), False, "--err-t2--")
```

`is_even(0)` को `True` लौटाना चाहिए

```python
    def test_3(self):
        self.assertEqual(is_even(0), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_even(n):
    return n % 2 == 0
```
