---
language: python
exerciseType: 4
---

# --description--

docstring को **बॉडी की पहली लाइन** होना चाहिए, हर दूसरे स्टेटमेंट के ऊपर। `return` के बाद, या बॉडी में कहीं और लिखी स्ट्रिंग सिर्फ़ एक स्ट्रिंग है: `__doc__` `None` ही रहता है और कोई टूल उसे कभी नहीं दिखाएगा।

# --instructions--

आइटम्स को क्रमबद्ध करें ताकि बनने वाला प्रोग्राम `to_seconds` को परिभाषित करे जिसकी docstring बॉडी की पहली लाइन हो, और फिर `to_seconds(2)` प्रिंट करे

# --answers--

```python
def to_seconds(minutes):
```

```python
    """Returns the number of seconds in the given minutes."""
```

```python
    return minutes * 60
```

```python
print(to_seconds(2))
```

# --solutions--

```python
def to_seconds(minutes):
    """Returns the number of seconds in the given minutes."""
    return minutes * 60
print(to_seconds(2))
```

# --output--

120
