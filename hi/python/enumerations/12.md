---
language: python
exerciseType: 4
---

# --description--

Enums का `match` स्टेटमेंट (Python 3.10 से उपलब्ध) के साथ स्वाभाविक मेल बैठता है, जो एक वैल्यू की तुलना `case` पैटर्न्स की एक श्रृंखला से करता है और पहला मैच होने वाला चलाता है:
```python
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
```
हमेशा मेंबर को उसकी क्लास के साथ लिखें, जैसे `Light.RED`: `case RED:` जैसा एक खाली नाम कुछ भी तुलना नहीं करेगा, वह बस वैल्यू को एक नए वेरिएबल `RED` में कैप्चर कर लेगा और हर चीज़ से मैच कर देगा।
वाइल्डकार्ड `case _:` डिफ़ॉल्ट है और आखिरी में आना चाहिए, क्योंकि उसके बाद कोई भी पैटर्न कभी नहीं पहुँचा जा सकता।

# --instructions--

इन आइटम्स को क्रमबद्ध करें ताकि enum `Light` घोषित हो, `action()` परिभाषित हो — पहले `RED` केस, फिर `GREEN` केस, फिर डिफ़ॉल्ट — और `Light.YELLOW` के लिए एक्शन प्रिंट हो

# --answers--

```python
from enum import Enum
```

```python
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
```

```python
def action(light):
```

```python
    match light:
```

```python
        case Light.RED:
            return "Stop"
```

```python
        case Light.GREEN:
            return "Go"
```

```python
        case _:
            return "Slow down"
```

```python
print(action(Light.YELLOW))
```

# --solutions--

```python
from enum import Enum
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
print(action(Light.YELLOW))
```

# --output--

Slow down
