---
language: python
exerciseType: 1
---

# --description--

एक `Flag` ऐसा enum है जिसकी मेंबर्स को **जोड़ा (combine)** जा सकता है: एक वैल्यू एक साथ कई मेंबर्स रख सकती है, जैसे विकल्पों का एक सेट।
इसकी मेंबर्स को `auto()` से घोषित करें, जो एक `Flag` के लिए दो की घातें (`1`, `2`, `4`, ...) असाइन करता है, ताकि हर कॉम्बिनेशन की एक अलग वैल्यू हो:
```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```
मेंबर्स को जोड़ने के लिए `|`, यह जाँचने के लिए `in` कि कोई मेंबर किसी कॉम्बिनेशन का हिस्सा है, और रिज़ल्टिंग संख्या देखने के लिए `value` का उपयोग करें:
```python
full = Permission.READ | Permission.WRITE | Permission.EXECUTE
print(Permission.WRITE in full)  # True
print(full.value)                # 7
print(full)                      # Permission.READ|WRITE|EXECUTE
```

# --instructions--

`editor` में `|` का उपयोग करके `Permission.READ` और `Permission.WRITE` का कॉम्बिनेशन स्टोर करें, `can_write` में यह कि क्या `Permission.WRITE`, `editor` में है, और `bits` में `editor` की वैल्यू स्टोर करें

# --seed--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको `|` ऑपरेटर का उपयोग करना चाहिए

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

आपको `in` ऑपरेटर का उपयोग करना चाहिए

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`editor` का मान `Permission.READ | Permission.WRITE` के बराबर होना चाहिए

```python
    def test_1(self):
        self.assertEqual(editor, Permission.READ | Permission.WRITE, "--err-t1--")
```

`can_write`, `True` होना चाहिए

```python
    def test_2(self):
        self.assertEqual(can_write, True, "--err-t2--")
```

`bits` का मान `3` के बराबर होना चाहिए

```python
    def test_3(self):
        self.assertEqual(bits, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Flag, auto

class Permission(Flag):
    READ = auto()
    WRITE = auto()
    EXECUTE = auto()

editor = Permission.READ | Permission.WRITE
can_write = Permission.WRITE in editor
bits = editor.value
```
