---
language: python
exerciseType: 2
---

# --description--

अगर दो मेंबर्स की वैल्यू एक ही हो, तो दूसरी कोई नई मेंबर नहीं होती बल्कि पहली का एक **उपनाम (alias)** होती है:
```python
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # alias of ACTIVE

print(Status.ENABLED)  # Status.ACTIVE
print(len(Status))     # 1
```
इटरेट करते समय उपनाम छोड़ दिए जाते हैं और `len()` उन्हें गिनता नहीं है।
आमतौर पर डुप्लीकेट वैल्यू एक गलती होती है। `enum` से इम्पोर्ट किया गया `unique` डेकोरेटर Python को उसी क्षण एक `ValueError` उठाने पर मजबूर करता है जैसे ही उपनामों वाला कोई enum घोषित होता है:
```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    ENABLED = 1  # ValueError: duplicate values found
```

# --instructions--

`unique` डेकोरेटर को इम्पोर्ट करने और उसे `Status` पर लागू करने के लिए कोड को पूरा करें, ताकि डुप्लीकेट वैल्यूज़ अस्वीकार कर दी जाएँ, फिर मेंबर्स की संख्या प्रिंट करें

# --seed--

```python
from enum import Enum, [/]

[/]
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --answers--

- unique
- @unique
- auto
- @auto
- @property
- @classmethod

# --solutions--

```python
from enum import Enum, unique

@unique
class Status(Enum):
    ACTIVE = 1
    DISABLED = 2

print(len(Status))
```

# --output--

2
