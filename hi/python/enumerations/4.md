---
language: python
exerciseType: 4
---

# --description--

एक enum क्लास **इटरेबल** होती है: क्लास पर एक `for` लूप हर मेंबर से उनके घोषित होने के क्रम में गुज़रता है:
```python
for color in Color:
    print(color.name, color.value)
```
`len()` बताता है कि enum में कितनी मेंबर्स हैं, और `list(Color)` उनकी एक लिस्ट बनाता है:
```python
print(len(Color))    # 3
print(list(Color))   # [<Color.RED: 1>, <Color.GREEN: 2>, <Color.BLUE: 3>]
```
लिस्ट के अंदर मेंबर्स अपने `repr()` के साथ दिखाए जाते हैं, जिसमें वैल्यू एंगल ब्रैकेट्स के बीच होती है।

# --instructions--

इन आइटम्स को क्रमबद्ध करें ताकि enum `Weekday` घोषित हो, फिर यह प्रिंट हो कि उसमें कितनी मेंबर्स हैं, और फिर हर मेंबर का नाम और वैल्यू, हर लाइन में एक, प्रिंट हो

# --answers--

```python
from enum import Enum
```

```python
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
```

```python
print(len(Weekday))
```

```python
for day in Weekday:
```

```python
    print(day.name, day.value)
```

# --solutions--

```python
from enum import Enum
class Weekday(Enum):
    MON = 1
    TUE = 2
    WED = 3
print(len(Weekday))
for day in Weekday:
    print(day.name, day.value)
```

# --output--

3
MON 1
TUE 2
WED 3
