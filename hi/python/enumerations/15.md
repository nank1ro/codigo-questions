---
language: python
exerciseType: 4
---

# --description--

क्लास पर इटरेट करना और मेंबर्स को नाम से खोजना साथ में अच्छी तरह काम करते हैं जब आप बाहर से आने वाला डेटा प्रोसेस करते हैं, जैसे लॉग लाइन्स या कोई फ़ाइल।
क्लास पर एक डिक्शनरी कॉम्प्रिहेंशन हर मेंबर के लिए एक एंट्री तैयार करती है, फिर `Level[name]` हर आने वाली स्ट्रिंग को मैच करने वाली मेंबर में बदलता है:
```python
counts = {level: 0 for level in Level}
counts[Level["INFO"]] += 1
```
चूँकि enum घोषणा का क्रम बनाए रखता है, बाद में `counts` पर इटरेट करना उसी क्रम में मेंबर्स देता है।

# --instructions--

इन आइटम्स को क्रमबद्ध करें ताकि `Level` ऑटोमैटिक वैल्यूज़ के साथ घोषित हो, हर मेंबर के लिए एक काउंटर तैयार हो, लिस्ट के नाम गिने जाएँ, फिर हर मेंबर का नाम उसकी गिनती के साथ प्रिंट हो

# --answers--

```python
from enum import Enum, auto
```

```python
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
```

```python
counts = {level: 0 for level in Level}
```

```python
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
```

```python
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --solutions--

```python
from enum import Enum, auto
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
counts = {level: 0 for level in Level}
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --output--

DEBUG: 0
INFO: 2
ERROR: 1
