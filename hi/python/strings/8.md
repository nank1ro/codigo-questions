---
language: python
exerciseType: 1
---

# --description--

स्ट्रिंग्स में कई बिल्ट-इन **मेथड्स (methods)** होते हैं: ऐसे फ़ंक्शन जिन्हें स्ट्रिंग के बाद डॉट लगाकर कॉल किया जाता है।
`upper()` टेक्स्ट को कैपिटल लेटर्स में लौटाता है, `lower()` स्मॉल लेटर्स में:
```python
word = "Python"
print(word.upper())  # PYTHON
print(word.lower())  # python
print(word)          # Python
```
ध्यान दें कि ये मेथड्स **एक नई स्ट्रिंग लौटाते** हैं: मूल `word` नहीं बदलता।
`lower()` का उपयोग अक्सर केस को नज़रअंदाज़ करते हुए टेक्स्ट की तुलना करने के लिए किया जाता है: `"Yes".lower() == "yes"`।

# --instructions--

`word` का अपरकेस वर्शन `shout` में और लोअरकेस वर्शन `quiet` में स्टोर करें

# --seed--

```python
word = "Python"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`shout` का मान `"PYTHON"` के बराबर होना चाहिए

```python
    def test_1(self):
        self.assertEqual(shout, "PYTHON", "--err-t1--")
```

`quiet` का मान `"python"` के बराबर होना चाहिए

```python
    def test_2(self):
        self.assertEqual(quiet, "python", "--err-t2--")
```

`word` अभी भी `"Python"` के बराबर होना चाहिए

```python
    def test_3(self):
        self.assertEqual(word, "Python", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
word = "Python"
shout = word.upper()
quiet = word.lower()
```
