---
language: python
exerciseType: 1
---

# --description--

`find(sub)` `sub` के पहले occurrence का **इंडेक्स** लौटाता है, या अगर वह न मिले तो `-1` लौटाता है:
```python
text = "hello world"
print(text.find("o"))  # 4
print(text.find("z"))  # -1
```
`count(sub)` लौटाता है कि `sub` **कितनी बार** आता है:
```python
print(text.count("o"))   # 2
print(text.count("ll"))  # 1
```

# --instructions--

`text` में पहले `o` का इंडेक्स `position` में, `z` को खोजने का रिज़ल्ट `missing` में, और `text` में कितने `o` हैं वह `total` में स्टोर करें

# --seed--

```python
text = "hello world"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको `find()` का उपयोग करना चाहिए

```json
{
  "regex": "\\.find\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

आपको `count()` का उपयोग करना चाहिए

```json
{
  "regex": "\\.count\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`position` का मान `4` के बराबर होना चाहिए

```python
    def test_1(self):
        self.assertEqual(position, 4, "--err-t1--")
```

`missing` का मान `-1` के बराबर होना चाहिए

```python
    def test_2(self):
        self.assertEqual(missing, -1, "--err-t2--")
```

`total` का मान `2` के बराबर होना चाहिए

```python
    def test_3(self):
        self.assertEqual(total, 2, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
text = "hello world"
position = text.find("o")
missing = text.find("z")
total = text.count("o")
```
