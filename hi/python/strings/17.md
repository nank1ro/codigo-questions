---
language: python
exerciseType: 4
---

# --description--

चूँकि हर स्ट्रिंग मेथड एक नई स्ट्रिंग लौटाता है, आप एक के बाद एक मेथड्स को **चेन** कर सकते हैं।
हर कॉल पिछले वाले के रिज़ल्ट पर काम करती है:
```python
text = "  Hello World  "
print(text.strip().lower())  # hello world
```
एक स्लाइस तीसरी वैल्यू भी स्वीकार करती है, **स्टेप (step)**।
स्टेप `-1` स्ट्रिंग में पीछे की ओर चलता है, जो स्ट्रिंग को उल्टा करने का क्लासिक तरीका है:
```python
print("abc"[::-1])  # cba
```

# --instructions--

इन आइटम्स को क्रमबद्ध करें ताकि `word` को साफ़ किया जाए, उसे उल्टा किया जाए, और यह प्रिंट किया जाए कि साफ़ किया गया शब्द पैलिंड्रोम (उल्टा पढ़ने पर भी वही) है या नहीं

# --answers--

```python
word = "  Level  "
```

```python
clean = word.strip().lower()
```

```python
reversed_word = clean[::-1]
```

```python
print(clean == reversed_word)
```

# --solutions--

```python
word = "  Level  "
clean = word.strip().lower()
reversed_word = clean[::-1]
print(clean == reversed_word)
```

# --output--

True
