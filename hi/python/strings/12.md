---
language: python
exerciseType: 4
---

# --description--

`replace(old, new)` स्ट्रिंग की एक कॉपी लौटाता है जिसमें `old` के **हर** occurrence को `new` से बदल दिया गया है:
```python
text = "a-b-c"
print(text.replace("-", "+"))  # a+b+c
```
चूँकि स्ट्रिंग्स इम्यूटेबल होती हैं, अगर आप रिज़ल्ट रखना चाहते हैं तो उसे स्टोर करना न भूलें।

# --instructions--

इन आइटम्स को क्रमबद्ध करें ताकि `msg` में हर `cats` को `dogs` से बदला जाए और रिज़ल्ट प्रिंट किया जाए

# --answers--

```python
msg = "I like cats, cats are great"
```

```python
new_msg = msg.replace("cats", "dogs")
```

```python
print(new_msg)
```

# --solutions--

```python
msg = "I like cats, cats are great"
new_msg = msg.replace("cats", "dogs")
print(new_msg)
```

# --output--

I like dogs, dogs are great
