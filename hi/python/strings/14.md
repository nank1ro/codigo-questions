---
language: python
exerciseType: 1
---

# --description--

`startswith(prefix)` और `endswith(suffix)` इस पर निर्भर करते हुए `True` या `False` लौटाते हैं कि स्ट्रिंग कैसे शुरू या खत्म होती है:
```python
url = "https://codigo.dev"
print(url.startswith("https"))  # True
print(url.endswith(".com"))     # False
```
फ़ाइल एक्सटेंशन, प्रोटोकॉल या प्रीफ़िक्स चेक करने का यह सामान्य तरीका है।

# --instructions--

एक फ़ंक्शन `is_python_file(name)` लिखें जो `True` लौटाए अगर `name` `.py` पर खत्म होता है, और एक फ़ंक्शन `is_comment(line)` लिखें जो `True` लौटाए अगर `line` `#` से शुरू होती है

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको `endswith()` का उपयोग करना चाहिए

```json
{
  "regex": "\\.endswith\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

आपको `startswith()` का उपयोग करना चाहिए

```json
{
  "regex": "\\.startswith\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`is_python_file("main.py")` को `True` लौटाना चाहिए

```python
    def test_1(self):
        self.assertEqual(is_python_file("main.py"), True, "--err-t1--")
```

`is_python_file("notes.txt")` को `False` लौटाना चाहिए

```python
    def test_2(self):
        self.assertEqual(is_python_file("notes.txt"), False, "--err-t2--")
```

`is_comment("# todo")` को `True` लौटाना चाहिए

```python
    def test_3(self):
        self.assertEqual(is_comment("# todo"), True, "--err-t3--")
```

`is_comment("x = 1  # todo")` को `False` लौटाना चाहिए

```python
    def test_4(self):
        self.assertEqual(is_comment("x = 1  # todo"), False, "--err-t4--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_python_file(name):
    return name.endswith(".py")

def is_comment(line):
    return line.startswith("#")
```
