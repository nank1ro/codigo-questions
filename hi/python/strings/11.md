---
language: python
exerciseType: 1
---

# --description--

`split()` एक स्ट्रिंग को टुकड़ों की **लिस्ट** में तोड़ता है।
बिना आर्गुमेंट के यह व्हाइटस्पेस पर स्प्लिट करता है; आर्गुमेंट देने पर उस सेपरेटर पर स्प्लिट करता है:
```python
print("a b  c".split())      # ['a', 'b', 'c']
print("2024-01-31".split("-"))  # ['2024', '01', '31']
```
`join()` इसका उल्टा करता है: यह लिस्ट के आइटम्स को एक स्ट्रिंग में जोड़ता है।
इसे **सेपरेटर** पर कॉल किया जाता है, और लिस्ट आर्गुमेंट होती है:
```python
words = ["one", "two", "three"]
print(", ".join(words))  # one, two, three
print("".join(words))    # onetwothree
```

# --instructions--

`sentence` के शब्दों की लिस्ट को `words` में स्टोर करें, फिर उन्हीं शब्दों को `-` से जोड़कर `joined` में स्टोर करें

# --seed--

```python
sentence = "the quick brown fox"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको `split()` का उपयोग करना चाहिए

```json
{
  "regex": "\\.split\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

आपको `join()` का उपयोग करना चाहिए

```json
{
  "regex": "\\.join\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`words` का मान `['the', 'quick', 'brown', 'fox']` के बराबर होना चाहिए

```python
    def test_1(self):
        self.assertEqual(words, ["the", "quick", "brown", "fox"], "--err-t1--")
```

`joined` का मान `"the-quick-brown-fox"` के बराबर होना चाहिए

```python
    def test_2(self):
        self.assertEqual(joined, "the-quick-brown-fox", "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
sentence = "the quick brown fox"
words = sentence.split()
joined = "-".join(words)
```
