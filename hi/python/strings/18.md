---
language: python
exerciseType: 1
---

# --description--

एक **स्लग (slug)** किसी टाइटल का URL-फ्रेंडली वर्शन होता है: लोअरकेस, आसपास बिना स्पेस के, और शब्द डैश से अलग किए गए, जैसे `hello-world`।
इसे बनाना बस उन मेथड्स की एक चेन है जो आपने सीखी हैं: `strip()`, `lower()` और `replace()`।

# --instructions--

एक फ़ंक्शन `slugify(title)` लिखें जो `title` को आसपास के स्पेस हटाकर, लोअरकेस में, और हर स्पेस को `-` से बदलकर लौटाए

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको `replace()` का उपयोग करना चाहिए

```json
{
  "regex": "\\.replace\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`slugify("Hello World")` को `"hello-world"` लौटाना चाहिए

```python
    def test_1(self):
        self.assertEqual(slugify("Hello World"), "hello-world", "--err-t1--")
```

`slugify("  Learn Python Strings  ")` को `"learn-python-strings"` लौटाना चाहिए

```python
    def test_2(self):
        self.assertEqual(slugify("  Learn Python Strings  "), "learn-python-strings", "--err-t2--")
```

`slugify("codigo")` को `"codigo"` लौटाना चाहिए

```python
    def test_3(self):
        self.assertEqual(slugify("codigo"), "codigo", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def slugify(title):
    return title.strip().lower().replace(" ", "-")
```
