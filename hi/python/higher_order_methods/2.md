---
language: python
exerciseType: 1
---

# --description--

फ़ंक्शन को आर्ग्युमेंट के रूप में पास करने से कॉल करने वाला तय करता है **क्या** करना है, जबकि higher-order फ़ंक्शन तय करता है **कितनी बार** या **किस पर**। फ़ंक्शन पैरामीटर को जितनी बार जरूरत हो कॉल किया जा सकता है, और इसका परिणाम दोबारा इसमें फीड किया जा सकता है:
```python
def repeat(func, value, times):
    for _ in range(times):
        value = func(value)
    return value

def add_one(n):
    return n + 1

print(repeat(add_one, 0, 3))  # 3
```
कोई भी callable काम करता है: एक `def` फ़ंक्शन, `len` जैसा कोई बिल्ट-इन या एक `lambda`।

# --instructions--

एक फ़ंक्शन `apply_twice` लिखें जो एक फ़ंक्शन `func` और एक `value` लेता है, और `func` को `value` पर लगातार दो बार अप्लाई करने का परिणाम रिटर्न करता है

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`apply_twice(lambda x: x * 2, 3)` को `12` रिटर्न करना चाहिए

```python
    def test_1(self):
        self.assertEqual(apply_twice(lambda x: x * 2, 3), 12, "--err-t1--")
```

`apply_twice(lambda s: s + "!", "hi")` को `"hi!!"` रिटर्न करना चाहिए

```python
    def test_2(self):
        self.assertEqual(apply_twice(lambda s: s + "!", "hi"), "hi!!", "--err-t2--")
```

दूसरी कॉल को पहली कॉल का परिणाम प्राप्त होना चाहिए

```python
    def test_3(self):
        seen = []
        def record(x):
            seen.append(x)
            return x + 10
        self.assertEqual(apply_twice(record, 0), 20, "--err-t3--")
        self.assertEqual(seen, [0, 10], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def apply_twice(func, value):
    return func(func(value))
```
