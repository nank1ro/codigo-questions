---
language: python
exerciseType: 1
---

# --description--

जो बिल्ट-इन ऑप्शंस लेते हैं उनके साथ `partial` बहुत काम आता है। `int(text, base=16)` एक हेक्साडेसिमल स्ट्रिंग को पार्स करता है; बेस को फिक्स करने से एक वन-आर्ग्युमेंट कन्वर्टर बनता है जो `map` में फिट हो जाता है:
```python
from functools import partial

hex_to_int = partial(int, base=16)
print(hex_to_int("ff"))                      # 255
print(list(map(hex_to_int, ["a", "10"])))    # [10, 16]
```
partial ऑब्जेक्ट याद रखता है कि वह किसे रैप करता है: `hex_to_int.func` `int` है, और `hex_to_int.keywords` `{'base': 16}` है।

# --instructions--

`partial` के साथ दो फ़ंक्शन परिभाषित करें: `hex_to_int` जो एक हेक्साडेसिमल स्ट्रिंग को एक `int` में बदलता है, और `binary_to_int` जो एक बाइनरी स्ट्रिंग को एक `int` में बदलता है

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`hex_to_int("ff")` को `255` रिटर्न करना चाहिए

```python
    def test_1(self):
        self.assertEqual(hex_to_int("ff"), 255, "--err-t1--")
        self.assertEqual(hex_to_int("10"), 16, "--err-t1--")
```

`binary_to_int("1010")` को `10` रिटर्न करना चाहिए

```python
    def test_2(self):
        self.assertEqual(binary_to_int("1010"), 10, "--err-t2--")
        self.assertEqual(binary_to_int("11"), 3, "--err-t2--")
```

दोनों `int` को रैप करने वाले partial ऑब्जेक्ट होने चाहिए

```python
    def test_3(self):
        self.assertIs(hex_to_int.func, int, "--err-t3--")
        self.assertIs(binary_to_int.func, int, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import partial

hex_to_int = partial(int, base=16)
binary_to_int = partial(int, base=2)
```
