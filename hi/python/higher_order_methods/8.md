---
language: python
exerciseType: 2
---

# --description--

एक closure अपने एनक्लोज़िंग फ़ंक्शन के वेरिएबल्स को पढ़ सकता है, लेकिन उनमें से किसी एक को असाइन करने पर इसकी जगह एक **नया लोकल** वेरिएबल बन जाता है। आउटर वेरिएबल को अपडेट करने के लिए, इनर फ़ंक्शन के अंदर उसे `nonlocal` के साथ डिक्लेयर करें:
```python
def make_counter():
    count = 0
    def increment():
        nonlocal count
        count += 1
        return count
    return increment
```
`global` `count` को मॉड्यूल लेवल पर खोजेगा, जहाँ वह मौजूद नहीं है। `nonlocal` के साथ, रिटर्न किए गए फ़ंक्शन की हर कॉल उसी `count` को अपडेट करती है, इसलिए closure कॉल्स के बीच स्टेट कैरी करता है, बिल्कुल एक छोटी ऑब्जेक्ट की तरह।

# --instructions--

कोड को पूरा करें ताकि काउंटर कॉल्स के बीच अपनी वैल्यू बनाए रखे और `make_counter` इनर फ़ंक्शन रिटर्न करे, ताकि आउटपुट `3` बने

# --seed--

```python
def make_counter():
    count = 0
    def increment():
        [/] count
        count += 1
        return count
    return [/]

counter = make_counter()
counter()
counter()
print(counter())
```

# --answers--

- nonlocal
- increment
- global
- increment()
- count

# --solutions--

```python
def make_counter():
    count = 0
    def increment():
        nonlocal count
        count += 1
        return count
    return increment

counter = make_counter()
counter()
counter()
print(counter())
```

# --output--

3
