---
language: python
exerciseType: 1
---

# --description--

एक फ़ंक्शन किसी दूसरे फ़ंक्शन को **रिटर्न** भी कर सकता है। `def` के साथ एक इनर फ़ंक्शन परिभाषित करें और उसे बिना कॉल किए रिटर्न करें:
```python
def make_greeter(greeting):
    def greet(name):
        return greeting + ", " + name
    return greet

hello = make_greeter("Hello")
print(hello("Ada"))  # Hello, Ada
```
इनर फ़ंक्शन `make_greeter` के खत्म होने के बाद भी `greeting` का उपयोग करता रहता है: यह उस स्कोप के वेरिएबल्स को **याद** रखता है जहाँ वह बनाया गया था। ऐसे फ़ंक्शन को **closure** कहा जाता है। `make_greeter` की हर कॉल अपने `greeting` वाला एक नया, स्वतंत्र closure बनाती है।

# --instructions--

एक फ़ंक्शन `make_multiplier` लिखें जो एक संख्या `factor` लेता है और एक ऐसा फ़ंक्शन रिटर्न करता है जो एक संख्या लेता है और उसे `factor` से गुणा करके रिटर्न करता है

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`make_multiplier(2)` को एक फ़ंक्शन रिटर्न करना चाहिए

```python
    def test_1(self):
        self.assertTrue(callable(make_multiplier(2)), "--err-t1--")
```

`make_multiplier(2)(5)` को `10` रिटर्न करना चाहिए

```python
    def test_2(self):
        double = make_multiplier(2)
        self.assertEqual(double(5), 10, "--err-t2--")
        self.assertEqual(double(0), 0, "--err-t2--")
```

हर मल्टीप्लायर को अपना खुद का फैक्टर याद रखना चाहिए

```python
    def test_3(self):
        double = make_multiplier(2)
        triple = make_multiplier(3)
        self.assertEqual(triple(4), 12, "--err-t3--")
        self.assertEqual(double(4), 8, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def make_multiplier(factor):
    def multiply(number):
        return number * factor
    return multiply
```
