---
language: python
exerciseType: 4
---

# --instructions--

आइटम्स को इस क्रम में लगाएँ कि केवल तीन अक्षरों से लंबे शब्द रहें, उन्हें अपर केस में बदला जाए और उन्हें लंबाई के अनुसार सॉर्ट करके प्रिंट किया जाए

# --answers--

```python
words = ["pear", "fig", "banana", "kiwi"]
```

```python
long_words = filter(lambda w: len(w) > 3, words)
```

```python
shouted = map(str.upper, long_words)
```

```python
print(sorted(shouted, key=len))
```

# --solutions--

```python
words = ["pear", "fig", "banana", "kiwi"]
long_words = filter(lambda w: len(w) > 3, words)
shouted = map(str.upper, long_words)
print(sorted(shouted, key=len))
```

# --output--

['PEAR', 'KIWI', 'BANANA']
