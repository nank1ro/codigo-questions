---
language: python
exerciseType: 1
---

# --description--

बिल्ट-इन `filter(func, iterable)` केवल उन्हीं एलिमेंट्स को रखता है जिनके लिए `func` एक ट्रू वैल्यू रिटर्न करता है। `map` की तरह, यह एक लेज़ी ऑब्जेक्ट रिटर्न करता है जिसे लिस्ट में बदलना जरूरी है:
```python
numbers = [4, -2, 7, 0]
print(list(filter(lambda n: n > 0, numbers)))  # [4, 7]
```
`filter` को पास किए गए फ़ंक्शन को **predicate** कहा जाता है: यह एक एलिमेंट लेता है और उसके बारे में एक हाँ/नहीं सवाल का जवाब देता है। फ़ंक्शन की जगह `None` पास करने पर वे एलिमेंट्स रह जाते हैं जो खुद से ट्रू होते हैं, और `0`, `""` तथा `None` ड्रॉप हो जाते हैं।

# --instructions--

`filter` का उपयोग करके एक फ़ंक्शन `positives` लिखें जो संख्याओं की एक लिस्ट लेता है और केवल शून्य से बड़ी संख्याओं वाली एक नई लिस्ट रिटर्न करता है

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको `filter` का उपयोग करना चाहिए

```json
{
  "regex": "filter\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`positives([3, -1, 0, 5])` को `[3, 5]` रिटर्न करना चाहिए

```python
    def test_1(self):
        self.assertEqual(positives([3, -1, 0, 5]), [3, 5], "--err-t1--")
```

परिणाम एक लिस्ट होना चाहिए

```python
    def test_2(self):
        self.assertIsInstance(positives([1, 2]), list, "--err-t2--")
        self.assertEqual(positives([1, 2]), [1, 2], "--err-t2--")
```

`positives([-4, 0])` को एक खाली लिस्ट रिटर्न करनी चाहिए

```python
    def test_3(self):
        self.assertEqual(positives([-4, 0]), [], "--err-t3--")
        self.assertEqual(positives([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def positives(numbers):
    return list(filter(lambda n: n > 0, numbers))
```
