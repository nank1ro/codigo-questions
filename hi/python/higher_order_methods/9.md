---
language: python
exerciseType: 1
---

# --description--

`functools` मॉड्यूल का `reduce(func, iterable, initial)` एक सीक्वेंस को एक **सिंगल वैल्यू** में फोल्ड करता है। यह `func` को अब तक के परिणाम और अगले एलिमेंट के साथ कॉल करता है, `initial` से शुरू करके:
```python
from functools import reduce

total = reduce(lambda acc, n: acc + n, [1, 2, 3], 0)
print(total)  # 6
```
स्टेप्स हैं `0 + 1`, फिर `1 + 2`, फिर `3 + 3`। जब `initial` छोड़ दिया जाता है, तो पहले एलिमेंट को शुरुआती वैल्यू के रूप में इस्तेमाल किया जाता है, लेकिन तब एक खाली सीक्वेंस `TypeError` रेज़ करता है, इसलिए जब भी सीक्वेंस खाली हो सकता हो एक इनिशियल वैल्यू दें।

# --instructions--

`reduce` का उपयोग करके एक फ़ंक्शन `product` लिखें जो संख्याओं की एक लिस्ट लेता है और उन सबको गुणा करके रिटर्न करता है; एक खाली लिस्ट का प्रोडक्ट `1` होना चाहिए

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको `reduce` का उपयोग करना चाहिए

```json
{
  "regex": "reduce\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`product([2, 3, 4])` को `24` रिटर्न करना चाहिए

```python
    def test_1(self):
        self.assertEqual(product([2, 3, 4]), 24, "--err-t1--")
```

`product([5])` को `5` रिटर्न करना चाहिए

```python
    def test_2(self):
        self.assertEqual(product([5]), 5, "--err-t2--")
```

`product([])` को `1` रिटर्न करना चाहिए

```python
    def test_3(self):
        self.assertEqual(product([]), 1, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import reduce

def product(numbers):
    return reduce(lambda acc, n: acc * n, numbers, 1)
```
