---
language: python
exerciseType: 1
---

# --description--

`try` ब्लॉक **पहले** उस स्टेटमेंट पर रुक जाता है जो अपवाद उत्पन्न करता है; उसके बाद की लाइनें छोड़ दी जाती हैं और कंट्रोल `except` ब्लॉक पर चला जाता है। `try` ब्लॉक में कुछ भी वापस नहीं किया जाता, इसलिए इसे जितना संभव हो उतना छोटा रखें:
```python
def half(n):
    try:
        return 10 / n
    except ZeroDivisionError:
        return "undefined"

print(half(2))  # 5.0
print(half(0))  # undefined
```
`except` के अंदर `return`, किसी और `return` की तरह ही काम करता है, जिससे `try`/`except` क्रैश होने के बजाय एक फॉलबैक मान लौटाने का सहज तरीका बन जाता है।

# --instructions--

एक फंक्शन `safe_divide(a, b)` लिखें जो `a / b` लौटाता है, और `b` के `0` होने पर `"undefined"` स्ट्रिंग लौटाता है, `ZeroDivisionError` को पकड़कर

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_divide(9, 3)` को `3.0` लौटाना चाहिए

```python
    def test_1(self):
        self.assertEqual(safe_divide(9, 3), 3.0, "--err-t1--")
```

`safe_divide(1, 0)` को `"undefined"` लौटाना चाहिए

```python
    def test_2(self):
        self.assertEqual(safe_divide(1, 0), "undefined", "--err-t2--")
```

`safe_divide(-5, 2)` को `-2.5` लौटाना चाहिए

```python
    def test_3(self):
        self.assertEqual(safe_divide(-5, 2), -2.5, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "undefined"
```
