---
language: python
exerciseType: 4
---

# --description--

सेट्स को एक-दूसरे से **कंपेयर** भी किया जा सकता है।
`a.issubset(b)`, या `a <= b`, तब `True` होता है जब `a` का हर एलिमेंट `b` में भी हो।
`a.issuperset(b)`, या `a >= b`, तब `True` होता है जब `a` में `b` का हर एलिमेंट हो।
`a.isdisjoint(b)` तब `True` होता है जब दोनों सेट्स में **कोई भी** कॉमन एलिमेंट न हो:
```python
small = {1, 2}
big = {1, 2, 3}
print(small <= big)              # True
print(big.issuperset(small))     # True
print(small.isdisjoint({8, 9}))  # True
```

# --instructions--

आइटम्स को क्रमबद्ध करें ताकि `primary` से `palette` बने, फिर इसी क्रम में प्रिंट करें: क्या `primary`, `palette` का सबसेट है, क्या `palette`, `primary` का सबसेट है, और क्या `primary` और `{"black"}` डिसजॉइंट हैं

# --answers--

```python
primary = {"red", "blue", "yellow"}
```

```python
palette = primary | {"green"}
```

```python
print(primary <= palette)
```

```python
print(palette.issubset(primary))
```

```python
print(primary.isdisjoint({"black"}))
```

# --solutions--

```python
primary = {"red", "blue", "yellow"}
palette = primary | {"green"}
print(primary <= palette)
print(palette.issubset(primary))
print(primary.isdisjoint({"black"}))
```

# --output--

True
False
True
