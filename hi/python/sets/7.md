---
language: python
exerciseType: 1
---

# --description--

दो सेट्स का **यूनियन (union)** एक नया सेट होता है जिसमें **दोनों** के एलिमेंट्स बिना डुप्लीकेट के होते हैं।
`|` ऑपरेटर या `union()` मेथड का उपयोग करें:
```python
a = {1, 2}
b = {2, 3}
print(a | b)        # {1, 2, 3}
print(a.union(b))   # {1, 2, 3}
```
न तो `a` और न ही `b` बदलता है: सेट ऑपरेशंस हमेशा एक नया सेट लौटाते हैं।

# --instructions--

`|` ऑपरेटर का उपयोग करके `frontend` और `backend` का यूनियन `skills` में स्टोर करें, फिर उसमें मौजूद अलग-अलग स्किल्स की संख्या `total` में स्टोर करें

# --seed--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको `|` ऑपरेटर का उपयोग करना चाहिए

```json
{
  "regex": "\\|",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`skills` का मान `{'html', 'css', 'js', 'python', 'sql'}` के बराबर होना चाहिए

```python
    def test_1(self):
        self.assertEqual(skills, {"html", "css", "js", "python", "sql"}, "--err-t1--")
```

`total` का मान `5` के बराबर होना चाहिए

```python
    def test_2(self):
        self.assertEqual(total, 5, "--err-t2--")
```

`frontend` में बदलाव नहीं होना चाहिए

```python
    def test_3(self):
        self.assertEqual(frontend, {"html", "css", "js"}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
frontend = {"html", "css", "js"}
backend = {"python", "js", "sql"}
skills = frontend | backend
total = len(skills)
```
