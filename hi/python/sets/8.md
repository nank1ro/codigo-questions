---
language: python
exerciseType: 2
---

# --description--

दो सेट्स का **इंटरसेक्शन (intersection)** केवल वे एलिमेंट्स रखता है जो **दोनों** में मौजूद हैं।
`&` ऑपरेटर या `intersection()` मेथड का उपयोग करें:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a & b)               # {2, 3}
print(a.intersection(b))   # {2, 3}
```
अगर सेट्स में कुछ भी कॉमन नहीं है, तो नतीजा एक खाली सेट होता है।

# --instructions--

कोड को पूरा करें ताकि `a` और `b` दोनों में मौजूद एलिमेंट्स `common` में स्टोर हों, और प्रोग्राम `[3, 4]` प्रिंट करे

# --seed--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a [/] b
print(sorted(common))
```

# --answers--

- &
- |
- -
- ^
- and

# --solutions--

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}
common = a & b
print(sorted(common))
```

# --output--

[3, 4]
