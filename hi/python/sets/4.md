---
language: python
exerciseType: 2
---

# --description--

सेट **म्यूटेबल** होते हैं: बनाने के बाद भी आप एलिमेंट्स जोड़ और हटा सकते हैं।
`add(value)` एक वैल्यू जोड़ता है; पहले से मौजूद वैल्यू को जोड़ने से कुछ नहीं बदलता:
```python
letters = {"a", "b"}
letters.add("c")
letters.add("a")
print(len(letters))  # 3
```
एलिमेंट हटाने के दो तरीके हैं:
- `remove(value)` उसे हटा देता है, लेकिन अगर वह वैल्यू सेट में नहीं है तो `KeyError` आता है
- `discard(value)` अगर वैल्यू मौजूद है तो उसे हटा देता है, वरना बिना किसी एरर के **कुछ नहीं करता**
```python
letters.remove("a")
letters.discard("z")  # "z" is not there, but no error
letters.remove("z")   # KeyError: 'z'
```

# --instructions--

कोड को पूरा करें ताकि `letters` में `"c"` जोड़ा जाए, फिर (सेट में मौजूद न होने वाले) `"z"` को बिना एरर दिए हटाया जाए, और फिर सेट में मौजूद एलिमेंट्स की संख्या प्रिंट की जाए

# --seed--

```python
letters = {"a", "b"}
letters.[/]("c")
letters.[/]("z")
print([/](letters))
```

# --answers--

- add
- discard
- len
- remove
- append
- size

# --solutions--

```python
letters = {"a", "b"}
letters.add("c")
letters.discard("z")
print(len(letters))
```

# --output--

3
