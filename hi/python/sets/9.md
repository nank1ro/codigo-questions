---
language: python
exerciseType: 1
---

# --description--

**डिफरेंस (difference)** `a - b` में `a` के वे एलिमेंट्स होते हैं जो `b` में **नहीं** हैं।
क्रम मायने रखता है: `a - b` और `b - a` आमतौर पर अलग होते हैं:
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a - b)  # {1}
print(b - a)  # {4}
```
**सिमेट्रिक डिफरेंस (symmetric difference)** `a ^ b` में वे एलिमेंट्स होते हैं जो दोनों सेट्स में से **किसी एक** में हैं:
```python
print(a ^ b)  # {1, 4}
```
मेथड रूप हैं `difference()` और `symmetric_difference()`।

# --instructions--

`-` का उपयोग करके `ann` के उन शौक को `only_ann` में स्टोर करें जो `bob` के पास नहीं हैं, और `^` का उपयोग करके उन शौक को `not_shared` में स्टोर करें जो दोनों में से किसी एक के पास हैं

# --seed--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको `-` ऑपरेटर का उपयोग करना चाहिए

```json
{
  "regex": "\\s-\\s",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

आपको `^` ऑपरेटर का उपयोग करना चाहिए

```json
{
  "regex": "\\^",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`only_ann` का मान `{'tennis', 'yoga'}` के बराबर होना चाहिए

```python
    def test_1(self):
        self.assertEqual(only_ann, {"tennis", "yoga"}, "--err-t1--")
```

`not_shared` का मान `{'tennis', 'yoga', 'swimming'}` के बराबर होना चाहिए

```python
    def test_2(self):
        self.assertEqual(not_shared, {"tennis", "yoga", "swimming"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
ann = {"tennis", "chess", "yoga"}
bob = {"chess", "swimming"}
only_ann = ann - bob
not_shared = ann ^ bob
```
