---
language: python
exerciseType: 2
---

# --description--

हर मान का एक **सत्य मान** होता है। `bool(value)` `0`, `0.0`, `None`, खाली स्ट्रिंग `""` और `[]`, `{}` तथा `set()` जैसे खाली कंटेनरों के लिए `False` लौटाता है; बाकी हर मान सत्य है, जिसमें `"0"` और `[0]` भी शामिल हैं:
```python
print(bool(0), bool(""), bool(None))   # False False False
print(bool(42), bool("0"), bool([0]))  # True True True
```
`if`, `while`, `and`, `or` और `not` इसी नियम का उपयोग करते हैं, इसलिए `if items:` जाँचता है कि लिस्ट खाली नहीं है और `not name` जाँचता है कि स्ट्रिंग खाली है; `len(items) > 0` या `name == ""` लिखने की आवश्यकता नहीं है।

# --instructions--

कोड को पूरा करें ताकि पहली पंक्ति `cart`, `name` और `count` का सत्य मान प्रदर्शित करे, और `if` तब `empty cart` प्रदर्शित करे जब लिस्ट खाली हो, जिससे आउटपुट `False True False`, `empty cart` बने

# --seed--

```python
cart = []
name = "Ada"
count = 0
print([/](cart), bool(name), bool(count))
if [/] cart:
    print("empty cart")
```

# --answers--

- bool
- not
- len
- str
- is

# --solutions--

```python
cart = []
name = "Ada"
count = 0
print(bool(cart), bool(name), bool(count))
if not cart:
    print("empty cart")
```

# --output--

False True False
empty cart
