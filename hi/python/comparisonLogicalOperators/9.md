---
language: python
exerciseType: 4
---

# --description--

लॉजिकल ऑपरेटर **शॉर्ट-सर्किट** होते हैं: `and` तभी रुक जाता है जब कोई ऑपरेंड असत्य हो और `or` तभी जब कोई सत्य हो, क्योंकि परिणाम पहले से ही ज्ञात होता है। बचे हुए ऑपरेंड का मूल्यांकन कभी नहीं होता, इसलिए यदि वे फ़ंक्शन कॉल हैं तो वे चलते नहीं हैं:
```python
def check(n):
    print("checking", n)
    return n > 0

check(-1) and check(5)  # prints only "checking -1"
check(2) or check(-3)   # prints only "checking 2"
```

# --instructions--

आइटम्स को क्रमबद्ध करें ताकि `is_positive` परिभाषित हो, `first` की गणना `and` से और फिर `second` की `or` से हो, और दोनों परिणाम प्रदर्शित हों, जिससे आउटपुट `checking -1`, `checking 2`, `False True` बने

# --answers--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
```

```python
first = is_positive(-1) and is_positive(5)
```

```python
second = is_positive(2) or is_positive(-3)
```

```python
print(first, second)
```

# --solutions--

```python
def is_positive(n):
    print(f"checking {n}")
    return n > 0
first = is_positive(-1) and is_positive(5)
second = is_positive(2) or is_positive(-3)
print(first, second)
```

# --output--

checking -1
checking 2
False True
