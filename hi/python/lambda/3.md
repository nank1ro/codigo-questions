---
language: python
exerciseType: 2
---

# --description--

एक lambda **एक से अधिक पैरामीटर** ले सकता है।
उन्हें कॉमा से अलग करें, बिल्कुल `def` फ़ंक्शन की तरह:
```python
power = lambda base, exp: base ** exp
print(power(2, 3))  # 8
```

# --instructions--

lambda को पूरा करें ताकि यह पैरामीटर `a` और `b` ले और उनका योग रिटर्न करे

# --seed--

```python
add = lambda a[/] b[/] a + b
print(add(3, 4))
```

# --answers--

- ,
- :
- ;
- ->

# --solutions--

```python
add = lambda a, b: a + b
print(add(3, 4))
```

# --output--

7
