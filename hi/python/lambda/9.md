---
language: python
exerciseType: 1
---

# --description--

`map()` लिस्ट के **हर आइटम** पर एक फ़ंक्शन लागू करता है।
यह एक खास _map ऑब्जेक्ट_ रिटर्न करता है, इसलिए वैल्यू देखने के लिए इसे `list()` में लपेटें:
```python
nums = [1, 2, 3]
doubled = list(map(lambda n: n * 2, nums))
print(doubled)  # [2, 4, 6]
```

# --instructions--

वेरिएबल `squares` में `map()` को lambda के साथ इस्तेमाल करके `nums` के वर्गों की लिस्ट स्टोर करें

# --seed--

```python
nums = [1, 2, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको एक `lambda` एक्सप्रेशन का उपयोग करना चाहिए

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

आपको `map()` का उपयोग करना चाहिए

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`squares` का मान `[1, 4, 9, 16]` के बराबर होना चाहिए

```python
    def test_1(self):
        self.assertEqual(squares, [1, 4, 9, 16], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 3, 4]
squares = list(map(lambda n: n * n, nums))
```
