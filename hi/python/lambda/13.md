---
language: python
exerciseType: 4
---

# --description--

`max()` और `min()` भी, `sorted()` की तरह ही, `key` फ़ंक्शन स्वीकार करते हैं:
```python
words = ["hi", "hello", "hey"]
print(max(words, key=lambda w: len(w)))  # hello
```

# --instructions--

आइटम्स को क्रमबद्ध करें ताकि नामों को बड़े-छोटे अक्षरों को नज़रअंदाज़ करते हुए वर्णानुक्रम में प्रिंट किया जाए, और फिर सबसे लंबा नाम प्रिंट किया जाए

# --answers--

```python
names = ["bob", "Alice", "Charlie"]
```

```python
ordered = sorted(names, key=lambda s: s.lower())
```

```python
longest = max(ordered, key=lambda s: len(s))
```

```python
print(ordered)
print(longest)
```

# --solutions--

```python
names = ["bob", "Alice", "Charlie"]
ordered = sorted(names, key=lambda s: s.lower())
longest = max(ordered, key=lambda s: len(s))
print(ordered)
print(longest)
```

# --output--

['Alice', 'bob', 'Charlie']
Charlie
