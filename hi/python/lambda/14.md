---
language: python
exerciseType: 1
---

# --description--

**तो `def` को कब प्राथमिकता देनी चाहिए?**
एक lambda आर्गुमेंट के रूप में पास किए जाने वाले छोटे, एक-बार इस्तेमाल होने वाले फ़ंक्शन के लिए बढ़िया है।
अगर लॉजिक को नाम, कई लाइनों, docstring की जरूरत है, या इसे कई जगहों पर दोबारा इस्तेमाल किया जाता है, तो एक `def` फ़ंक्शन ज्यादा स्पष्ट होता है।
आखिरी ट्रिक: `sorted()` `reverse=True` भी स्वीकार करता है ताकि सबसे बड़े मान पहले मिलें:
```python
print(sorted([3, 1, 2], reverse=True))  # [3, 2, 1]
```

# --instructions--

`ranking` में लिस्ट `students` को `grade` के अनुसार सबसे ज्यादा से सबसे कम के क्रम में स्टोर करें, फिर `names` में `ranking` के नामों की लिस्ट को `sorted()` और `map()` को lambda के साथ इस्तेमाल करके स्टोर करें

# --seed--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको एक `lambda` एक्सप्रेशन का उपयोग करना चाहिए

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

आपको `map()` का उपयोग करना चाहिए

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ranking` की शुरुआत विद्यार्थी `Cy` से होनी चाहिए

```python
    def test_1(self):
        self.assertEqual(ranking[0]["name"], "Cy", "--err-t1--")
```

`names` का मान `['Cy', 'Ana', 'Bo']` के बराबर होना चाहिए

```python
    def test_2(self):
        self.assertEqual(names, ["Cy", "Ana", "Bo"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
ranking = sorted(students, key=lambda s: s["grade"], reverse=True)
names = list(map(lambda s: s["name"], ranking))
```
