---
language: python
exerciseType: 2
---

# --description--

`None` एक कंडीशन में **false** गिना जाता है, इसलिए `if not value:` तब `True` होता है जब `value`, `None` हो। इसे `None` जाँच की तरह इस्तेमाल करना आकर्षक लगता है, लेकिन वही टेस्ट `0`, `""`, `[]` और हर दूसरे खाली मान के लिए भी `True` होता है:
```python
count = 0
if not count:
    print("missing?")   # printed, but 0 is a real value!
```
जब "कोई मान नहीं" और "खाली मान" को अलग-अलग ट्रीट करना हो, तो पहले `is None` जाँचें, फिर truthiness:
```python
if count is None:
    print("no count yet")
elif not count:
    print("count is zero")
```
`if not value:` का उपयोग केवल तब करें जब आप सच में `None` और खाली मानों को एक जैसा ट्रीट करना चाहते हों।

# --instructions--

फंक्शन को पूरा करें ताकि वह `None` के लिए `missing`, खाली मानों के लिए `empty` और अन्यथा `present` लौटाए, जिससे आउटपुट `missing`, `empty`, `present` आए

# --seed--

```python
def describe(value):
    if value [/] None:
        return "missing"
    if [/] value:
        return "empty"
    return "present"

print(describe(None))
print(describe(0))
print(describe("hi"))
```

# --answers--

- is
- not
- is not
- in
- and

# --solutions--

```python
def describe(value):
    if value is None:
        return "missing"
    if not value:
        return "empty"
    return "present"

print(describe(None))
print(describe(0))
print(describe("hi"))
```

# --output--

missing
empty
present
