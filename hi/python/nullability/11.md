---
language: python
exerciseType: 2
---

# --description--

एक लिस्ट में असली मानों के बगल में `None` हो सकता है, जैसे वे रीडिंग्स जो फेल हो गईं या वे जवाब जो छोड़ दिए गए। ज़्यादातर ऑपरेशन इसे स्वीकार नहीं करते: `sum([8, None])` एक `TypeError` उठाता है।
`None` मानों को एक list comprehension से फ़िल्टर करें जिसकी कंडीशन `is not None` हो:
```python
readings = [3, None, 5]
valid = [r for r in readings if r is not None]
print(valid)  # [3, 5]
```
इसकी जगह `if r` लिखने पर हर `0` भी छूट जाएगा, इसलिए जब शून्य एक मान्य रीडिंग हो तो साफ़ तौर पर लिखें।

# --instructions--

कोड को पूरा करें ताकि `valid` में केवल वे स्कोर रहें जो `None` नहीं हैं, फिर कुल योग प्रिंट करे, जिससे आउटपुट `[8, 0, 9]`, `3`, `17` आए

# --seed--

```python
scores = [8, None, 0, None, 9]
valid = [s for s in scores if s [/] None]
print(valid)
print(len(valid))
print([/](valid))
```

# --answers--

- is not
- sum
- is
- max
- len
- and

# --solutions--

```python
scores = [8, None, 0, None, 9]
valid = [s for s in scores if s is not None]
print(valid)
print(len(valid))
print(sum(valid))
```

# --output--

[8, 0, 9]
3
17
