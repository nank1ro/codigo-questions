---
language: python
exerciseType: 1
---

# --description--

जो फंक्शन कोई संख्या **या** `None` लौटाता है, उसे यह अपनी सिग्नेचर में बताना चाहिए। एक **type hint** एक एनोटेशन है जो अपेक्षित टाइप को दर्शाता है: पैरामीटर के लिए `name: str` और रिटर्न वैल्यू के लिए `-> int`। Python हिंट्स को फ़ोर्स नहीं करता, लेकिन एडिटर्स और पाठक उन पर भरोसा करते हैं:
```python
def parse_age(text: str) -> int | None:
    if text.isdigit():
        return int(text)
    return None
```
`int | None` का मतलब है "एक `int` या `None`"। `typing` मॉड्यूल की पुरानी वर्तनी `Optional[int]` बिल्कुल वही बात कहती है, और आप इसे मौजूदा कोड में अब भी मिलेगी।
जब भी आप सिग्नेचर में `| None` देखें, उसे उपयोग करने से पहले रिज़ल्ट की जाँच करना याद रखें।

# --instructions--

एक फंक्शन `parse_age` type hint के साथ लिखें जो एक स्ट्रिंग लेता है और उसे `int` में बदलकर लौटाता है जब उसमें केवल अंक हों, और अन्यथा `None` लौटाता है

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

रिटर्न टाइप `int | None` के रूप में हिंट किया जाना चाहिए

```json
{
  "regex": "->\\s*(int\\s*\\|\\s*None|None\\s*\\|\\s*int|Optional\\[int\\])",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`parse_age("42")` को `42` लौटाना चाहिए

```python
    def test_1(self):
        self.assertEqual(parse_age("42"), 42, "--err-t1--")
        self.assertIsInstance(parse_age("42"), int, "--err-t1--")
```

`parse_age("abc")` को `None` लौटाना चाहिए

```python
    def test_2(self):
        self.assertIsNone(parse_age("abc"), "--err-t2--")
        self.assertIsNone(parse_age("4a"), "--err-t2--")
```

`parse_age("")` को `None` लौटाना चाहिए

```python
    def test_3(self):
        self.assertIsNone(parse_age(""), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def parse_age(text: str) -> int | None:
    if text.isdigit():
        return int(text)
    return None
```
