---
language: python
exerciseType: 1
---

# --description--

हर फंक्शन कॉल एक मान पैदा करता है, तब भी जब फंक्शन कुछ लौटाता नहीं दिखता। **कोई** `return` स्टेटमेंट न रखने वाला फंक्शन, या खाली (bare) `return` रखने वाला फंक्शन, `None` लौटाता है:
```python
def say_hello():
    print("hello")

result = say_hello()
print(result)  # None
```
इसीलिए `print(my_list.append(3))` को कॉल करने पर `None` दिखता है: `append` लिस्ट को उसी जगह (in place) बदल देता है और कुछ भी लौटाता नहीं।
जो फंक्शन केवल कोई क्रिया करता है (प्रिंट करना, सेव करना, लिस्ट बदलना), वह आम तौर पर `None` लौटाता है, जबकि जो फंक्शन कुछ कैलकुलेट करता है उसे उसे साफ़ तौर पर `return` करना चाहिए।

# --instructions--

एक फंक्शन `store` लिखें जो एक मान लेता है और उसे लिस्ट `saved` में जोड़ देता है, बिना कुछ लौटाए

# --seed--

```python
saved = []

def store(value):
    pass
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको `append` का उपयोग करना चाहिए

```json
{
  "regex": "\\.append\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`store("a")` को `None` लौटाना चाहिए

```python
    def test_1(self):
        self.assertIsNone(store("a"), "--err-t1--")
```

`store("b")` के बाद लिस्ट `saved` का आखिरी एलिमेंट `"b"` होना चाहिए

```python
    def test_2(self):
        store("b")
        self.assertEqual(saved[-1], "b", "--err-t2--")
```

स्टोर किया गया हर मान उसी क्रम में सुरक्षित रहना चाहिए

```python
    def test_3(self):
        before = len(saved)
        store(1)
        store(2)
        self.assertEqual(saved[before:], [1, 2], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
saved = []

def store(value):
    saved.append(value)
```
