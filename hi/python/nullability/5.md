---
language: python
exerciseType: 1
---

# --description--

एक पैरामीटर में **डिफ़ॉल्ट मान** हो सकता है, जिसका उपयोग तब होता है जब कॉलर आर्ग्युमेंट छोड़ देता है। "प्रोवाइड नहीं किया गया" के लिए `None` आम डिफ़ॉल्ट है:
```python
def greet(name=None):
    if name is None:
        name = "stranger"
    print("Hello, " + name)

greet()       # Hello, stranger
greet("Ada")  # Hello, Ada
```
यह लिस्टों और डिक्शनरीज़ के लिए मायने रखता है। एक डिफ़ॉल्ट का मूल्यांकन **एक बार** होता है, जब फंक्शन परिभाषित होता है, इसलिए `def add(item, items=[])` उन हर कॉल में वही लिस्ट शेयर करता है जो `items` छोड़ देती हैं, और आइटम जमा होते जाते हैं। समाधान यह है कि डिफ़ॉल्ट `None` रखें और फंक्शन के अंदर एक नई लिस्ट बनाएं:
```python
def add(item, items=None):
    if items is None:
        items = []
    items.append(item)
    return items
```

# --instructions--

एक फंक्शन `add_tag` लिखें जो एक स्ट्रिंग `tag` और एक ऑप्शनल लिस्ट `tags` लेता है; उसे `tag` को `tags` में जोड़ना चाहिए और लिस्ट लौटानी चाहिए, `tags` प्रोवाइड न होने पर एक नई लिस्ट बनाते हुए

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`tags` का डिफ़ॉल्ट मान `None` होना चाहिए

```json
{
  "regex": "tags\\s*=\\s*None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`add_tag("new")` को `["new"]` लौटाना चाहिए

```python
    def test_1(self):
        self.assertEqual(add_tag("new"), ["new"], "--err-t1--")
```

`tags` के बिना की गई दोनों कॉल को स्वतंत्र लिस्टें लौटानी चाहिए

```python
    def test_2(self):
        first = add_tag("a")
        second = add_tag("b")
        self.assertEqual(first, ["a"], "--err-t2--")
        self.assertEqual(second, ["b"], "--err-t2--")
```

`add_tag("c", ["a", "b"])` को `["a", "b", "c"]` लौटाना चाहिए

```python
    def test_3(self):
        existing = ["a", "b"]
        self.assertEqual(add_tag("c", existing), ["a", "b", "c"], "--err-t3--")
        self.assertIs(add_tag("d", existing), existing, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def add_tag(tag, tags=None):
    if tags is None:
        tags = []
    tags.append(tag)
    return tags
```
