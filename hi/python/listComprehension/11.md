---
language: python
exerciseType: 1
---

# --description--

एक कॉम्प्रिहेंशन में **एक से ज्यादा `for`** हो सकते हैं।
ये नेस्टेड लूप्स की तरह काम करते हैं: पहला `for` बाहरी लूप होता है, दूसरा अंदरूनी।
```python
pairs = [a + b for a in ["x", "y"] for b in ["1", "2"]]
print(pairs)  # ['x1', 'x2', 'y1', 'y2']
```

# --instructions--

दो `for` हिस्सों वाले लिस्ट कॉम्प्रिहेंशन का उपयोग करके, `colors` और `sizes` के हर कॉम्बिनेशन को `f"{color} {size}"` के रूप में वेरिएबल `combos` में स्टोर करें

# --seed--

```python
colors = ["red", "blue"]
sizes = ["S", "M"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको दो `for` हिस्सों वाले लिस्ट कॉम्प्रिहेंशन का उपयोग करना चाहिए

```json
{
  "regex": "\\[.* for .* in .* for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`combos` का मान `['red S', 'red M', 'blue S', 'blue M']` के बराबर होना चाहिए

```python
    def test_1(self):
        self.assertEqual(combos, ["red S", "red M", "blue S", "blue M"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
colors = ["red", "blue"]
sizes = ["S", "M"]
combos = [f"{color} {size}" for color in colors for size in sizes]
```
