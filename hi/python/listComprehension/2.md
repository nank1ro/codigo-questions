---
language: python
exerciseType: 1
---

# --description--

बाईं ओर का एक्सप्रेशन कुछ भी हो सकता है जो एक मान बनाता है: एक कैलकुलेशन, एक फ़ंक्शन कॉल, एक मेथड कॉल।
लूप वेरिएबल का कोई भी नाम हो सकता है, और यह केवल स्क्वेयर ब्रैकेट्स के अंदर ही मौजूद होता है:
```python
prices = [10, 20]
with_tax = [price * 1.2 for price in prices]
print(with_tax)  # [12.0, 24.0]
```

# --instructions--

लिस्ट कॉम्प्रिहेंशन का उपयोग करके, `nums` के वर्गों (squares) की लिस्ट को वेरिएबल `squares` में स्टोर करें

# --seed--

```python
nums = [1, 2, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको एक लिस्ट कॉम्प्रिहेंशन का उपयोग करना चाहिए

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`squares` का मान `[1, 4, 9, 16]` के बराबर होना चाहिए

```python
    def test_1(self):
        self.assertEqual(squares, [1, 4, 9, 16], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 3, 4]
squares = [n * n for n in nums]
```
