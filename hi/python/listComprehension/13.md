---
language: python
exerciseType: 1
---

# --description--

आप एक **डिक्शनरी** पर भी लूप कर सकते हैं।
`.items()` के साथ `for` वाला हिस्सा हर पेयर को दो वेरिएबल्स में अनपैक करता है, एक कुंजी (key) और एक मान (value):
```python
ages = {"ana": 31, "bob": 25}
lines = [f"{name} is {age}" for name, age in ages.items()]
print(lines)  # ['ana is 31', 'bob is 25']
```

# --instructions--

`prices.items()` पर लिस्ट कॉम्प्रिहेंशन का उपयोग करके, `prices` में `2` से महंगे प्रोडक्ट्स के नामों की लिस्ट को वेरिएबल `expensive` में स्टोर करें

# --seed--

```python
prices = {"apple": 1, "steak": 12, "bread": 2, "cheese": 5}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको `.items()` पर लिस्ट कॉम्प्रिहेंशन का उपयोग करना चाहिए

```json
{
  "regex": "\\[.* for .* in .*\\.items\\(\\).*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`expensive` का मान `['steak', 'cheese']` के बराबर होना चाहिए

```python
    def test_1(self):
        self.assertEqual(expensive, ["steak", "cheese"], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
prices = {"apple": 1, "steak": 12, "bread": 2, "cheese": 5}
expensive = [name for name, price in prices.items() if price > 2]
```
