---
language: python
exerciseType: 1
---

# --description--

**कॉम्प्रिहेंशन का उपयोग कब करना चाहिए?**
यह तब परफेक्ट होता है जब रिजल्ट एक लिस्ट (या डिक्शनरी, या सेट) हो और लॉजिक एक पढ़ने में आसान लाइन में समा जाए: एक सिंपल ट्रांसफॉर्मेशन, एक ऑप्शनल फ़िल्टर।
अगर आपको कई स्टेटमेंट्स चाहिए, दो से ज्यादा नेस्टेड `for` चाहिए, या लाइन पढ़ने में मुश्किल हो जाती है, तो इसके बजाय एक सामान्य `for` लूप लिखें: कोड लंबा होगा, लेकिन ज्यादा साफ़ होगा।
एक कॉम्प्रिहेंशन lambda के साथ इस्तेमाल होने वाले `map()` और `filter()` के ज्यादातर उपयोगों की जगह भी लेता है:
```python
doubled = list(map(lambda n: n * 2, nums))
doubled = [n * 2 for n in nums]  # same result, easier to read
```

# --instructions--

एक फ़ंक्शन `initials(names)` डिफाइन करें जो `names` में हर नाम के पहले अक्षर को अपरकेस में लिस्ट के रूप में रिटर्न करे, खाली स्ट्रिंग्स को स्किप करते हुए। लिस्ट कॉम्प्रिहेंशन का उपयोग करें

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

आपको एक लिस्ट कॉम्प्रिहेंशन का उपयोग करना चाहिए

```json
{
  "regex": "\\[.*for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`initials(["ana", "bob"])` को `['A', 'B']` रिटर्न करना चाहिए

```python
    def test_1(self):
        self.assertEqual(initials(["ana", "bob"]), ["A", "B"], "--err-t1--")
```

`initials(["", "cy", ""])` को `['C']` रिटर्न करना चाहिए

```python
    def test_2(self):
        self.assertEqual(initials(["", "cy", ""]), ["C"], "--err-t2--")
```

`initials([])` को `[]` रिटर्न करना चाहिए

```python
    def test_3(self):
        self.assertEqual(initials([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def initials(names):
    return [name[0].upper() for name in names if name != ""]
```
