---
language: python
exerciseType: 1
---

# --description--

जब हर मैच की स्थिति या ग्रुप चाहिए हों, तो `re.finditer(pattern, text)` सही कॉल है: यह टेक्स्ट पर चलता है और हर मैच के लिए एक **match ऑब्जेक्ट** देता है, एक बार में एक:
```python
import re

for match in re.finditer(r"\d+", "a1 bb22"):
    print(match.group(), match.start())
# 1 1
# 22 5
```
`finditer` एक इटरेटर देता है, लिस्ट नहीं, इसलिए इसे एक `for` लूप में या किसी कॉम्प्रिहेंशन में उपयोग किया जा सकता है। जहाँ `findall` केवल टेक्स्ट देता है, वहीं `finditer` वह सब देता है जो एक match ऑब्जेक्ट जानता है।

# --instructions--

एक फ़ंक्शन `word_positions(text)` लिखें जो टपल की एक लिस्ट लौटाता है, टेक्स्ट में अक्षरों के हर क्रम के लिए एक, जिसमें वर्ड और वह इंडेक्स हो जहाँ वह शुरू होता है

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`word_positions("hi there")` को `[("hi", 0), ("there", 3)]` लौटाना चाहिए

```python
    def test_1(self):
        self.assertEqual(word_positions("hi there"), [("hi", 0), ("there", 3)], "--err-t1--")
```

अंक और सिंबल वर्ड का हिस्सा नहीं होने चाहिए

```python
    def test_2(self):
        self.assertEqual(word_positions("a1b"), [("a", 0), ("b", 2)], "--err-t2--")
        self.assertEqual(word_positions("Ada, Bob"), [("Ada", 0), ("Bob", 5)], "--err-t2--")
```

बिना अक्षरों वाले टेक्स्ट को एक खाली लिस्ट लौटानी चाहिए

```python
    def test_3(self):
        self.assertEqual(word_positions("123 456"), [], "--err-t3--")
        self.assertEqual(word_positions(""), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def word_positions(text):
    return [(match.group(), match.start()) for match in re.finditer(r"[a-zA-Z]+", text)]
```
