---
language: python
exerciseType: 1
---

# --description--

`re.sub(pattern, replacement, text)` एक नई स्ट्रिंग लौटाता है जिसमें हर मैच बदल दिया गया है। स्ट्रिंग्स इम्यूटेबल होती हैं, इसलिए मूल टेक्स्ट अछूता रहता है:
```python
import re

print(re.sub(r"\d", "#", "call 555 now"))     # call ### now
print(re.sub(r"\s+", " ", "too    many gaps"))  # too many gaps
```

रिप्लेसमेंट कैप्चर ग्रुप को `\1`, `\2`, ... (या किसी नाम वाले ग्रुप के लिए `\g<name>`) से देख सकता है, जिससे टेक्स्ट को फिर से क्रम में लगाना एक वन-लाइनर बन जाता है। रिप्लेसमेंट भी एक raw स्ट्रिंग होता है, उसी बैकस्लैश वाली वजह से:
```python
import re

print(re.sub(r"(\w+), (\w+)", r"\2 \1", "Lovelace, Ada"))  # Ada Lovelace
```
एक `count` आर्ग्युमेंट बताता है कि कितने मैच बदले जाएँ: `re.sub(r"\d", "#", "1 2 3", count=1)` देता है `# 2 3`।

# --instructions--

एक फ़ंक्शन `mask_digits(text)` लिखें जो हर अंक को `#` से बदले, और एक फ़ंक्शन `swap_names(text)` लिखें जो एक `surname, name` जोड़ी को एक बैकरेफ़रेंस से `name surname` में बदले

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`mask_digits` को हर अंक बदलना चाहिए

```python
    def test_1(self):
        self.assertEqual(mask_digits("call 555 now"), "call ### now", "--err-t1--")
        self.assertEqual(mask_digits("no digits"), "no digits", "--err-t1--")
```

`swap_names` को नाम को सरनेम से पहले रखना चाहिए

```python
    def test_2(self):
        self.assertEqual(swap_names("Lovelace, Ada"), "Ada Lovelace", "--err-t2--")
        self.assertEqual(swap_names("Turing, Alan"), "Alan Turing", "--err-t2--")
```

`swap_names` को बिना कॉमा वाले टेक्स्ट को अपरिवर्तित छोड़ देना चाहिए

```python
    def test_3(self):
        self.assertEqual(swap_names("Ada Lovelace"), "Ada Lovelace", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def mask_digits(text):
    return re.sub(r"\d", "#", text)

def swap_names(text):
    return re.sub(r"(\w+), (\w+)", r"\2 \1", text)
```
