---
language: python
exerciseType: 1
---

# --description--

ग्रुप `3` ढूँढने के लिए कोष्ठकों की गिनती करना जल्दी उबाऊ हो जाता है। एक ग्रुप को `(?P<name>...)` से नाम दिया जा सकता है और फिर `match.group("name")` से पढ़ा जा सकता है:
```python
import re

match = re.search(r"(?P<hour>\d\d):(?P<minute>\d\d)", "starts at 09:30")
print(match.group("hour"))    # 09
print(match.group("minute"))  # 30
print(match.groupdict())      # {'hour': '09', 'minute': '30'}
```
`match.groupdict()` हर नाम वाले ग्रुप को एक डिक्शनरी के रूप में लौटाता है। नाम वाले ग्रुप अपना नंबर भी रखते हैं, इसलिए `match.group(1)` अब भी काम करता है।

उदाहरण ब्रेसिज़ के साथ एक **क्वांटिफायर** भी उपयोग करता है: `\d{2}` का मतलब ठीक दो अंक, `\d{2,4}` का मतलब दो से चार के बीच, और `\d{2,}` का मतलब दो या अधिक। ये `+` (एक या अधिक), `*` (शून्य या अधिक) और `?` (शून्य या एक) का सटीक रूप हैं।

# --instructions--

एक फ़ंक्शन `parse_date(text)` लिखें जो चार अंकों, एक डैश, दो अंकों, एक डैश और दो अंकों के रूप में लिखी तारीख ढूँढे, और `year`, `month` तथा `day` कीज़ वाली एक डिक्शनरी लौटाए, या `None` जब ऐसी कोई तारीख न हो

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`parse_date` को तारीख के तीनों हिस्से पढ़ने चाहिए

```python
    def test_1(self):
        self.assertEqual(parse_date("due 2026-09-13 ok"), {"year": "2026", "month": "09", "day": "13"}, "--err-t1--")
```

`parse_date` को तब भी काम करना चाहिए जब तारीख पूरा टेक्स्ट हो

```python
    def test_2(self):
        self.assertEqual(parse_date("1999-12-31"), {"year": "1999", "month": "12", "day": "31"}, "--err-t2--")
```

`parse_date` को `None` लौटाना चाहिए जब कोई तारीख न हो

```python
    def test_3(self):
        self.assertIsNone(parse_date("no date here"), "--err-t3--")
        self.assertIsNone(parse_date("26-09-13"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def parse_date(text):
    match = re.search(r"(?P<year>\d{4})-(?P<month>\d{2})-(?P<day>\d{2})", text)
    if match is None:
        return None
    return match.groupdict()
```
