---
language: c
exerciseType: 2
---

# --description--

एक लिस्ट इंडेक्स किसी अन्य वेरिएबल नाम की तरह व्यवहार करता है! इसका उपयोग मान एक्सेस करने के साथ-साथ असाइन करने के लिए भी किया जा सकता है।
आपने देखा कि लिस्ट इंडेक्स को इस तरह कैसे एक्सेस करें:
```c
int numbers[4] = {5, 6, 7, 8};
numbers[0]; // मान 5 प्राप्त करता है
```
असाइनमेंट इस तरह काम करता है:
```c
int numbers[4] = {5, 6, 7, 8};
numbers[0] = 1;
printf("%d\n", numbers[0]); // नया मान 1 प्रिंट करता है
```

# --instructions--

वर्तमान में `5` मान रखने वाले आइटम को नए मान `10` से बदलने के लिए कोड को पूरा करें

# --seed--

```c
int numbers[/] = {10, 4, 6, 5};

numbers[/] = 10;

for (int i = 0; i < 4; i++) {
    printf("%d\n", [/][i]);
}
```

# --answers--

- [0]
- [1]
- [2]
- [3]
- [4]
- numbers

# --solutions--

```c
int numbers[4] = {10, 4, 6, 5};

numbers[3] = 10;

for (int i = 0; i < 4; i++) {
    printf("%d\n", numbers[i]);
}
```

# --output--

10
4
6
10
