---
language: c
exerciseType: 4
---

# --description--

`sprintf`, `printf` की तरह काम करता है, लेकिन फ़ॉर्मेटेड टेक्स्ट को स्क्रीन के बजाय एक `char` ऐरे में लिखता है:
```c
char buffer[30];
sprintf(buffer, "%d items", 3); // buffer is "3 items"
```
बफर इतना बड़ा होना चाहिए कि उसमें पूरा टेक्स्ट और उसका `'\0'` समा सके।

# --instructions--

`buffer` के अंदर टेक्स्ट `Age: 25` बनाकर उसे प्रिंट करने के लिए लाइनों को सॉर्ट करें। `buffer` को `age` से पहले घोषित करें

# --answers--

```c
char buffer[30];
```

```c
int age = 25;
```

```c
sprintf(buffer, "Age: %d", age);
```

```c
printf("%s\n", buffer);
```

# --solutions--

```c
char buffer[30];
int age = 25;
sprintf(buffer, "Age: %d", age);
printf("%s\n", buffer);
```

# --output--

Age: 25
