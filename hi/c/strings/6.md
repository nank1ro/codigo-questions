---
language: c
exerciseType: 4
---

# --description--

एक ऐरे को घोषणा के बाद `=` से असाइन नहीं किया जा सकता:
```c
char copy[20];
copy = "Codigo"; // error
```
स्ट्रिंग कॉपी करने के लिए `string.h` के `strcpy(destination, source)` का उपयोग करें।
डेस्टिनेशन इतना बड़ा होना चाहिए कि वह सभी कैरेक्टर्स और `'\0'` को समा सके।

# --instructions--

`source` को `copy` में कॉपी करके प्रिंट करने के लिए लाइनों को सॉर्ट करें। `source` को `copy` से पहले घोषित करें

# --answers--

```c
char source[] = "Codigo";
```

```c
char copy[20];
```

```c
strcpy(copy, source);
```

```c
printf("%s\n", copy);
```

# --solutions--

```c
char source[] = "Codigo";
char copy[20];
strcpy(copy, source);
printf("%s\n", copy);
```

# --output--

Codigo
