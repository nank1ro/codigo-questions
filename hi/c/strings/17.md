---
language: c
exerciseType: 4
---

# --instructions--

प्रोग्राम `password` की `attempt` से तुलना करे और `Access granted` प्रिंट करे, इसके लिए लाइनों को सॉर्ट करें। `password` को `attempt` से पहले घोषित करें और हर संदेश को उसकी अपनी शाखा के अंदर रखें

# --answers--

```c
char password[] = "secret";
```

```c
char attempt[] = "secret";
```

```c
if (strcmp(password, attempt) == 0) {
```

```c
    printf("Access granted\n");
```

```c
} else {
```

```c
    printf("Access denied\n");
```

```c
}
```

# --solutions--

```c
char password[] = "secret";
char attempt[] = "secret";
if (strcmp(password, attempt) == 0) {
    printf("Access granted\n");
} else {
    printf("Access denied\n");
}
```

# --output--

Access granted
