---
language: c
exerciseType: 2
---

# --description--

हेडर `ctype.h` एक अकेले कैरेक्टर पर काम करने वाले फ़ंक्शन प्रदान करता है।
`toupper(c)` किसी अक्षर का अपरकेस रूप लौटाता है और `tolower(c)` लोअरकेस रूप; कोई भी अन्य कैरेक्टर बिना बदलाव के लौटाया जाता है:
```c
char letter = toupper('a'); // 'A'
```

# --instructions--

`first` को अपरकेस और `second` को लोअरकेस में बदलकर `Ab` प्रिंट करें

# --seed--

```c
#include <stdio.h>
#include <ctype.h>

int main() {
    char first = 'a';
    char second = 'B';
    printf("%c%c\n", [/](first), [/](second));
    return 0;
}
```

# --answers--

- toupper
- tolower
- upper
- lower
- capitalize

# --solutions--

```c
#include <stdio.h>
#include <ctype.h>

int main() {
    char first = 'a';
    char second = 'B';
    printf("%c%c\n", toupper(first), tolower(second));
    return 0;
}
```

# --output--

Ab
