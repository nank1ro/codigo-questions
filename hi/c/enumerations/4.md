---
language: c
exerciseType: 2
---

# --description--

एनम एक प्रकार (type) भी है: आप `enum` के बाद एनम का नाम लिखकर उस प्रकार का वेरिएबल घोषित कर सकते हैं, और उसे इसके किसी एक स्थिरांक का मान दे सकते हैं:
```c
enum Color favorite = GREEN;
```
चूँकि स्थिरांक पूर्णांक होते हैं, आप एनम वेरिएबल की तुलना सामान्य ऑपरेटरों से करते हैं:
```c
if (favorite == GREEN) {
    printf("Green it is\n");
}
```

# --instructions--

कोड को पूरा करें ताकि `enum Light` प्रकार का वेरिएबल `light` घोषित हो, जिसमें `GREEN` हो, ताकि प्रोग्राम `Go` आउटपुट करे

# --seed--

```c
enum Light { RED, YELLOW, GREEN };
[/] Light light = [/];
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --answers--

- enum
- struct
- GREEN
- RED
- YELLOW
- "GREEN"

# --solutions--

```c
enum Light { RED, YELLOW, GREEN };
enum Light light = GREEN;
if (light == GREEN) {
    printf("Go\n");
} else {
    printf("Stop\n");
}
```

# --output--

Go
