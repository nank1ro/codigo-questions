---
language: c
exerciseType: 4
---

# --description--

सब कुछ मिलाकर: मान के प्रकार के अनुसार टाइप चुनें, हर `printf` स्पेसिफायर को उसके आर्गुमेंट के टाइप से मिलाएँ, और जब किसी गणना को उसके ऑपरेंड्स से अलग टाइप में करना हो तो कास्ट करें।

# --instructions--

पंक्तियों को इस तरह क्रमबद्ध करें कि एक प्रोग्राम बने जो `total` में `7` और `count` में `2` स्टोर करे, `total` को `count` से भाग देकर `double` के रूप में `avg` में परिणाम निकाले, और पूर्णांक भाग `total / count` के बाद `avg` को एक दशमलव अंक के साथ प्रिंट करे

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int total = 7, count = 2;
```

```c
    double avg = (double) total / count;
```

```c
    printf("%d %.1f\n", total / count, avg);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int total = 7, count = 2;
    double avg = (double) total / count;
    printf("%d %.1f\n", total / count, avg);
    return 0;
}
```

# --output--

3 3.5
