---
language: c
exerciseType: 2
---

# --description--

एक `while` लूप तब तक चलता रहता है जब तक उसकी condition गैर-शून्य है, इसलिए एक तुलना यह तय करती है कि यह कब रुकेगा:
```c
int i = 0;
while (i < 3) {
    printf("%d\n", i);
    i++;
}
// 0, 1, 2 प्रिंट करता है
```
`<` और `<=` में से चुनना यह बदल देता है कि आखिरी मान शामिल होगा या नहीं।

# --instructions--

कोड को पूरा करें ताकि लूप `1` से `3` तक की संख्याएँ, एक-एक करके अलग लाइन में प्रिंट करे

# --seed--

```c
int count = [/];
while (count [/] 3) {
    printf("%d\n", count);
    count++;
}
```

# --answers--

- 1
- <=
- 0
- <
- !=
- >

# --solutions--

```c
int count = 1;
while (count <= 3) {
    printf("%d\n", count);
    count++;
}
```

# --output--

1
2
3
