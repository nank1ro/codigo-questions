---
language: c
exerciseType: 4
---

# --description--

एक `bool` पैरामीटर को `true` से तुलना किए बिना सीधे `&&` या `||` के operand के रूप में इस्तेमाल किया जा सकता है: `citizen == true` की जगह `age >= 18 && citizen` लिखें।

# --instructions--

पंक्तियों को इस तरह क्रमबद्ध करें कि `can_vote` परिभाषित हो, जो `true` लौटाता है जब `age` कम से कम `18` हो और `citizen` `true` हो, और एक `main` जो `20, true` के लिए और फिर `20, false` के लिए परिणाम प्रिंट करे। फ़ंक्शन को `main` से पहले परिभाषित करें

# --answers--

```c
#include <stdio.h>
#include <stdbool.h>
```

```c
bool can_vote(int age, bool citizen) {
```

```c
    return age >= 18 && citizen;
}
```

```c
int main() {
```

```c
    printf("%d\n", can_vote(20, true));
```

```c
    printf("%d\n", can_vote(20, false));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
#include <stdbool.h>
bool can_vote(int age, bool citizen) {
    return age >= 18 && citizen;
}
int main() {
    printf("%d\n", can_vote(20, true));
    printf("%d\n", can_vote(20, false));
    return 0;
}
```

# --output--

1
0
