---
language: c
exerciseType: 4
---

# --description--

ऑपरेटरों की एक **प्राथमिकता** होती है जो तय करती है कि पहले क्या गणना होगी:
1. `!` सबसे पहले लागू होता है
2. फिर रिलेशनल तुलनाएँ `<`, `>`, `<=`, `>=`
3. फिर इक्वैलिटी तुलनाएँ `==`, `!=`
4. फिर `&&`
5. फिर `||`

इसलिए `a > 0 && a < 10` को कोष्ठक की ज़रूरत नहीं है: दोनों तुलनाएँ `&&` से पहले गणना हो जाती हैं।
और `x == 1 || y == 2 && z == 3` का मतलब है `x == 1 || (y == 2 && z == 3)`, क्योंकि `&&`, `||` से अधिक मजबूती से बंधता है।

# --instructions--

पंक्तियों को इस तरह क्रमबद्ध करें कि `inside` में यह स्टोर हो कि `a`, `1` और `9` के बीच है या नहीं, `outside` में इसका निषेध स्टोर हो, और दोनों मान एक स्पेस से अलग करके प्रिंट हों

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int a = 5;
```

```c
    int inside = a > 0 && a < 10;
```

```c
    int outside = !inside;
```

```c
    printf("%d %d\n", inside, outside);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int a = 5;
    int inside = a > 0 && a < 10;
    int outside = !inside;
    printf("%d %d\n", inside, outside);
    return 0;
}
```

# --output--

1 0
