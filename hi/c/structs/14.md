---
language: c
exerciseType: 2
---

# --description--

स्ट्रक्चर अक्सर टेक्स्ट रखता है, जिसे एक निश्चित आकार वाले `char` ऐरे सदस्य में संग्रहीत किया जाता है:
```c
typedef struct {
    char name[20];
    int age;
} Person;
```
घोषणा के बाद किसी ऐरे सदस्य को `=` से असाइन नहीं किया जा सकता: `p.name = "Ann"` कंपाइल नहीं होता। `string.h` के `strcpy` से टेक्स्ट को उसमें कॉपी करें, सदस्य को डेस्टिनेशन के रूप में पास करके:
```c
Person p;
strcpy(p.name, "Ann");
p.age = 30;
```
केवल घोषणा के समय दिया गया ब्रेस इनिशियलाइज़र ही स्ट्रिंग को सीधे स्वीकार करता है: `Person p = {"Ann", 30};`।

# --instructions--

कोड को पूरा करें ताकि `p` के नाम को `"Ann"` से बदला जाए, फिर नाम और उम्र प्रिंट की जाए

# --seed--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
[/](p.name, "Ann");
printf("%s is %d\n", p[/]name, p.age);
```

# --answers--

- strcpy
- strcat
- strcmp
- strlen
- .
- ->

# --solutions--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
strcpy(p.name, "Ann");
printf("%s is %d\n", p.name, p.age);
```

# --output--

Ann is 30
