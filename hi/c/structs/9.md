---
language: c
exerciseType: 2
---

# --description--

Through a pointer the members are reached with the **arrow** operator `->` instead of the dot:
```c
void grow(Box *b) {
    b->size = b->size * 2;
}
```
`b->size` is a shortcut for `(*b).size`: first follow the pointer, then take the member. The dot works only on a struct, the arrow only on a pointer to a struct.
The caller passes the address of its variable with `&`, and the change made through the pointer is visible after the call.

# --instructions--

Complete `grow` so it doubles `size` through the pointer, and complete the call so it passes the address of `box`

# --seed--

```c
#include <stdio.h>

typedef struct { int size; } Box;

void grow(Box *b) {
    b[/]size = b->size * 2;
}

int main() {
    Box box = {5};
    grow([/]box);
    printf("%d\n", box.size);
    return 0;
}
```

# --answers--

- ->
- .
- &
- *
- (*b)

# --solutions--

```c
#include <stdio.h>

typedef struct { int size; } Box;

void grow(Box *b) {
    b->size = b->size * 2;
}

int main() {
    Box box = {5};
    grow(&box);
    printf("%d\n", box.size);
    return 0;
}
```

# --output--

10
