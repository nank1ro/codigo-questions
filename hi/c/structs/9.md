---
language: c
exerciseType: 2
---

# --description--

पॉइंटर के ज़रिए सदस्यों तक डॉट की जगह **एरो** ऑपरेटर `->` से पहुँचा जाता है:
```c
void grow(Box *b) {
    b->size = b->size * 2;
}
```
`b->size`, `(*b).size` का संक्षिप्त रूप है: पहले पॉइंटर का अनुसरण करें, फिर सदस्य लें। डॉट केवल स्ट्रक्चर पर काम करता है, एरो केवल स्ट्रक्चर की ओर इशारा करने वाले पॉइंटर पर।
कॉलर `&` से अपने वेरिएबल का एड्रेस पास करता है, और पॉइंटर के ज़रिए किया गया बदलाव कॉल के बाद दिखता है।

# --instructions--

`grow` को पूरा करें ताकि वह पॉइंटर के ज़रिए `size` को दोगुना करे, और कॉल को पूरा करें ताकि वह `box` का एड्रेस पास करे

# --seed--

```c
#include <stdio.h>

typedef struct { int size; } Box;

void grow(Box *b) {
    b[/]size = b->size * 2;
}

int main() {
    Box box = {5};
    grow([/]box);
    printf("%d\n", box.size);
    return 0;
}
```

# --answers--

- ->
- .
- &
- *
- (*b)

# --solutions--

```c
#include <stdio.h>

typedef struct { int size; } Box;

void grow(Box *b) {
    b->size = b->size * 2;
}

int main() {
    Box box = {5};
    grow(&box);
    printf("%d\n", box.size);
    return 0;
}
```

# --output--

10
