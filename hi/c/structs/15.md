---
language: c
exerciseType: 4
---

# --description--

`printf` के पास पूरे स्ट्रक्चर के लिए कोई स्पेसिफ़ायर नहीं है। सामान्य समाधान एक छोटा फ़ंक्शन है जो सदस्यों को एक तय फ़ॉर्मेट में प्रिंट करता है, ताकि प्रोग्राम का हर हिस्सा मान को एक ही तरह दिखाए:
```c
void print_person(Person p) {
    printf("%s (%d)\n", p.name, p.age);
}
```

# --instructions--

लाइनों को इस क्रम में लगाएँ ताकि `Point` प्रकार, एक फ़ंक्शन `print_point` जो पॉइंट को `(x, y)` के रूप में प्रिंट करे, और एक `main` जो पॉइंट `(3, 4)` प्रिंट करे, परिभाषित हो जाएँ। फ़ंक्शन को `main` से पहले परिभाषित करें

# --answers--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
```

```c
void print_point(Point p) {
```

```c
    printf("(%d, %d)\n", p.x, p.y);
```

```c
}
```

```c
int main() {
```

```c
    Point p = {3, 4};
```

```c
    print_point(p);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
void print_point(Point p) {
    printf("(%d, %d)\n", p.x, p.y);
}
int main() {
    Point p = {3, 4};
    print_point(p);
    return 0;
}
```

# --output--

(3, 4)
