---
language: c
exerciseType: 4
---

# --description--

एक `char` की ओर इशारा करने वाला पॉइंटर किसी भी दूसरे पॉइंटर की तरह काम करता है: वह एक कैरेक्टर का एड्रेस रखता है, और `*p` उस कैरेक्टर को पढ़ता या लिखता है:
```c
char grade = 'B';
char *p = &grade;
*p = 'A';
printf("%c\n", grade); // prints "A"
```
पॉइंटर से पढ़ने और उसके ज़रिए लिखने को आज़ादी से मिलाया जा सकता है: `*p = *p + 1` `'A'` को `'B'` में बदल देता है।

# --instructions--

लाइनों को क्रम में लगाएँ ताकि `letter` को `'a'` के रूप में घोषित किया जाए, `p` को उसकी ओर इशारा किया जाए, पॉइंटर के ज़रिए लेटर को `'b'` से बदला जाए और `letter` प्रिंट किया जाए

# --answers--

```c
char letter = 'a';
```

```c
char *p = &letter;
```

```c
*p = 'b';
```

```c
printf("%c\n", letter);
```


# --solutions--

```c
char letter = 'a';
char *p = &letter;
*p = 'b';
printf("%c\n", letter);
```

# --output--

b
