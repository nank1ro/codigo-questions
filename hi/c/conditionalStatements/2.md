---
language: c
exerciseType: 2
---

# --description--

आइए पिछले उदाहरण को जारी रखें।
```c
bool nice_weather = true;
if (nice_weather) {
    // play outside
}
```
हमने देखा कि `if` स्टेटमेंट कोड ब्लॉक को केवल तभी निष्पादित करता है जब शर्त `true` हो।
एक और महत्वपूर्ण बात **कर्ली ब्रैकेट** `{}` हैं जो एक कोड ब्लॉक को दर्शाती हैं।

# --instructions--

*कर्ली ब्रैकेट* `{}` जोड़कर `if` स्टेटमेंट को ठीक करें

# --seed--

```c
int time = 8;
[/] (time[/]12) [/]
    printf("It's morning\n");
[/]
```

# --answers--

- if
-  == 
-  = 
- else
-  ? 
-  < 
- {
- }

# --solutions--

```c
int time = 8;
if (time < 12) {
    printf("It's morning\n");
}
```

# --output--

It's morning
