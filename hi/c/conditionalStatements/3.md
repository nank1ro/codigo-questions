---
language: c
exerciseType: 2
---

# --description--

हमने अभी देखा कि किसी शर्त पूरी होने पर कोड ब्लॉक कैसे निष्पादित करें, अब देखते हैं कि पहली शर्त विफल होने पर दूसरा कोड ब्लॉक कैसे निष्पादित करें।
हम बाहर खेलने जाते हैं अगर मौसम अच्छा है; अन्यथा, हम घर पर रहते हैं।
C में हम `else` स्टेटमेंट का उपयोग कर सकते हैं, जैसे:
```c
bool nice_weather = false;
if (nice_weather) {
    // play outside
} else {
    // stay home
}
```

# --instructions--

एक `else` स्टेटमेंट बनाएं और कोड ब्लॉक में स्ट्रिंग `"It's afternoon"` प्रिंट करें

# --seed--

```c
int time = 15;
if (time < 12) {
    printf("It's morning\n");
}[/]{
    printf("[/]\n");
}
```

# --answers--

- It's afternoon
- 12
-  else 
-  < 
-  > 
- :
- ;

# --solutions--

```c
int time = 15;
if (time < 12) {
    printf("It's morning\n");
} else {
    printf("It's afternoon\n");
}
```

# --output--

It's afternoon
