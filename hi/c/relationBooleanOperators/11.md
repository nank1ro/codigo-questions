---
language: c
exerciseType: 2
---

# --instructions--

जाँचें कि `word` `dog` के बराबर **नहीं** है, boolean परिणाम प्रिंट करें।
ध्यान रखें कि `strcmp` केवल तभी `0` लौटाता है जब स्ट्रिंग्स **बराबर** होती हैं, इसलिए यह न सोचें कि इसका लौटाया गया 0 false boolean जैसा है

# --seed--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat")[/][/];
printf("%d\n", [/]);
```

# --answers--

- word
- "dog"
-  == 
-  = 
- dog
-  != 
- 0
- are_words_equal

# --solutions--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat") == 0;
printf("%d\n", are_words_equal);
```

# --output--

1
