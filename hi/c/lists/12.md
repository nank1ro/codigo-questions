---
language: c
exerciseType: 2
---

# --description--

पहला नोड हटाना `push_front` का उल्टा है: दूसरे नोड का एड्रेस सेव करें, पहले नोड को मुक्त करें और सेव किया गया एड्रेस नए हेड के रूप में लौटाएँ। कॉलर परिणाम को अपने हेड वेरिएबल में वापस संग्रहीत कर लेता है:
```c
Node *next = head->next;
free(head);
return next;
```
हेड के `NULL` होने तक पॉप करने पर पूरी लिस्ट मुक्त हो जाती है, हर कॉल में एक नोड।

# --instructions--

`pop_front` को पूरा करें ताकि वह पहला नोड मुक्त करे और दूसरे नोड को नए हेड के रूप में लौटाए

# --seed--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

Node *pop_front(Node *head) {
    Node *next = [/];
    [/](head);
    return [/];
}

int main() {
    Node *head = NULL;
    head = push_front(head, 3);
    head = push_front(head, 2);
    head = push_front(head, 1);
    head = pop_front(head);
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        head = pop_front(head);
    }
    return 0;
}
```

# --answers--

- head->next
- free
- next
- head
- NULL

# --solutions--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

Node *pop_front(Node *head) {
    Node *next = head->next;
    free(head);
    return next;
}

int main() {
    Node *head = NULL;
    head = push_front(head, 3);
    head = push_front(head, 2);
    head = push_front(head, 1);
    head = pop_front(head);
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        head = pop_front(head);
    }
    return 0;
}
```

# --output--

2
3
