---
language: c
exerciseType: 2
---

# --description--

सबको जोड़ते हुए: लिस्ट अक्सर एक ऐरे से बनाई जाती है। शुरुआत में पुश करने से क्रम उलट जाता है, इसलिए ऐरे को **उलटे** चला जाता है, आखिरी एलिमेंट से पहले एलिमेंट तक, और पहला एलिमेंट हेड पर आ ठहरता है। प्रोग्राम फिर एक ट्रैवर्सल से लिस्ट प्रिंट करता है और उसे नोड-दर-नोड मुक्त करता है, ताकि हर `malloc` की एक `free` से जोड़ बने।

# --instructions--

`push_front` और आखिरी लूप को पूरा करें ताकि प्रोग्राम ऐरे से लिस्ट `10, 20, 30` बनाए, उसे प्रिंट करे और हर नोड मुक्त करे

# --seed--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = [/];
    return [/];
}

int main() {
    int values[3] = {10, 20, 30};
    Node *head = NULL;
    for (int i = 2; i >= 0; i--) {
        head = push_front(head, values[i]);
    }
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        Node *next = head->next;
        [/](head);
        head = next;
    }
    return 0;
}
```

# --answers--

- head
- node
- NULL
- free
- values[i]

# --solutions--

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int value;
    struct Node *next;
} Node;

Node *push_front(Node *head, int value) {
    Node *node = malloc(sizeof(Node));
    node->value = value;
    node->next = head;
    return node;
}

int main() {
    int values[3] = {10, 20, 30};
    Node *head = NULL;
    for (int i = 2; i >= 0; i--) {
        head = push_front(head, values[i]);
    }
    for (Node *n = head; n != NULL; n = n->next) {
        printf("%d\n", n->value);
    }
    while (head != NULL) {
        Node *next = head->next;
        free(head);
        head = next;
    }
    return 0;
}
```

# --output--

10
20
30
