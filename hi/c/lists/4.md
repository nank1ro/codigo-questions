---
language: c
exerciseType: 2
---

# --description--

लिस्ट को चलना, जिसे **ट्रैवर्सल** कहा जाता है, एक लूप है जो हेड से शुरू होकर `NULL` तक पहुँचने तक `next` का अनुसरण करता है। एक `for` लूप इसे लूप वेरिएबल के रूप में पॉइंटर के साथ एक ही लाइन में व्यक्त करता है:
```c
for (Node *n = head; n != NULL; n = n->next) {
    printf("%d\n", n->value);
}
```
लिस्ट में कोई इंडेक्स नहीं होता: किसी नोड तक पहुँचने का एकमात्र तरीका उससे पहले वाले नोड में संग्रहीत पॉइंटर है।

# --instructions--

लूप को पूरा करें ताकि वह `a` से शुरू होकर लिस्ट के हर नोड का मान प्रिंट करे

# --seed--

```c
typedef struct Node {
    int value;
    struct Node *next;
} Node;
Node c = {3, NULL};
Node b = {2, &c};
Node a = {1, &b};
for (Node *n = [/]; n [/] NULL; n = [/]) {
    printf("%d\n", n->value);
}
```

# --answers--

- &a
- a
- !=
- ==
- n->next
- n.next

# --solutions--

```c
typedef struct Node {
    int value;
    struct Node *next;
} Node;
Node c = {3, NULL};
Node b = {2, &c};
Node a = {1, &b};
for (Node *n = &a; n != NULL; n = n->next) {
    printf("%d\n", n->value);
}
```

# --output--

1
2
3
