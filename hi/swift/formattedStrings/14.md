---
language: swift
exerciseType: 4
---

# --description--

तालिका प्रिंट करने के लिए दोनों टूल्स को साथ में रखें: `padding` हर पंक्ति के टेक्स्ट को बाईं ओर संरेखित करता है, `String(format:)` संख्याओं को नियत चौड़ाई और प्रेसिशन के साथ दाईं ओर संरेखित करता है:
```swift
import Foundation

let items = [("Tea", 2.5), ("Cake", 12.0)]
for (name, price) in items {
    let label = name.padding(toLength: 6, withPad: ".", startingAt: 0)
    print(label + String(format: "%6.2f", price))
}
// Tea...  2.50
// Cake.. 12.00
```
चूँकि हर पंक्ति की चौड़ाई एक जैसी होती है, इसलिए दशमलव बिंदु एक ही कॉलम में आ जाते हैं।

# --instructions--

आइटम्स को क्रम में लगाकर नीचे की तालिका प्रिंट करें, `import Foundation` से शुरू करें और `label` को प्रिंट करने से पहले निकालें
```
Tea...  2.50
Cake.. 12.00
```

# --answers--

```swift
import Foundation
```

```swift
let items = [("Tea", 2.5), ("Cake", 12.0)]
```

```swift
for (name, price) in items {
```

```swift
    let label = name.padding(toLength: 6, withPad: ".", startingAt: 0)
```

```swift
    print(label + String(format: "%6.2f", price))
```

```swift
}
```

# --solutions--

```swift
import Foundation
let items = [("Tea", 2.5), ("Cake", 12.0)]
for (name, price) in items {
    let label = name.padding(toLength: 6, withPad: ".", startingAt: 0)
    print(label + String(format: "%6.2f", price))
}
```

# --output--

Tea...  2.50
Cake.. 12.00
