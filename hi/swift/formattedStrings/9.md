---
language: swift
exerciseType: 1
---

# --description--

एक `String` को फ़ॉर्मेट स्ट्रिंग में डालने के लिए, स्पेसिफ़ायर `%@` का उपयोग करें:
```swift
let name = "Ada"
let age = 36
print(String(format: "%@ is %d years old", name, age)) // Ada is 36 years old
```
`%@` एक Swift `String` सीधे स्वीकार करता है। Swift स्ट्रिंग के साथ `%s` का उपयोग न करें: वह स्पेसिफ़ायर एक C स्ट्रिंग की अपेक्षा करता है और बेकार आउटपुट प्रिंट करता है या क्रैश हो जाता है।

# --instructions--

एक फ़ंक्शन `greeting` लिखें जो एक नाम (`String`) और एक उम्र (`Int`) ले, दोनों आर्गुमेंट लेबल के बिना, और `%@` तथा `%d` के साथ `String(format:)` का उपयोग करके `<name> is <age> years old` लौटाए

फ़ंक्शन कॉल का उदाहरण:
```swift
print(greeting("Ada", 36))
// prints Ada is 36 years old
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func greeting() {

}
```

# --asserts--

फ़ंक्शन को `%@` के साथ `String(format:` का उपयोग करना चाहिए

```json
{
  "regex": "String\\(format:[^\\n]*%@",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`greeting("Ada", 36)` को `"Ada is 36 years old"` लौटाना चाहिए

```swift
tryCatch(greeting("Ada", 36) == "Ada is 36 years old")
```

`greeting("Linus", 54)` को `"Linus is 54 years old"` लौटाना चाहिए

```swift
tryCatch(greeting("Linus", 54) == "Linus is 54 years old")
```

`greeting("Bo", 7)` को `"Bo is 7 years old"` लौटाना चाहिए

```swift
tryCatch(greeting("Bo", 7) == "Bo is 7 years old")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greeting(_ name: String, _ age: Int) -> String {
    return String(format: "%@ is %d years old", name, age)
}
```
