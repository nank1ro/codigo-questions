---
language: swift
exerciseType: 2
---

# --description--

विफल हो सकने वाले कन्वर्ज़न्स स्वाभाविक रूप से `guard let` के साथ जोड़ी बनाते हैं: कन्वर्ट करें, परिणाम `nil` होने पर बाहर निकलें, फिर सामान्य मान के साथ काम करें।

# --instructions--

फ़ंक्शन को इस तरह पूरा करें कि यह टेक्स्ट को `Int` में बदले और उसका दोगुना लौटाए, या कन्वर्ज़न विफल होने पर `nil` लौटाए।
फिर परिणामों को प्रिंट करें, फॉलबैक मान के रूप में `0` का उपयोग करते हुए

# --seed--

```swift
func doubled(_ text: String) -> Int? {
    guard let number = [/](text) else { return [/] }
    return number * 2
}
print(doubled("21") [/] 0)
print(doubled("x") ?? 0)
```

# --answers--

- Int
- nil
- ??
- Int?
- String
- !

# --solutions--

```swift
func doubled(_ text: String) -> Int? {
    guard let number = Int(text) else { return nil }
    return number * 2
}
print(doubled("21") ?? 0)
print(doubled("x") ?? 0)
```

# --output--

42
0
