---
language: swift
exerciseType: 4
---

# --description--

कई ऑप्शनल्स को एक ही `if let` में बाइंड करना कोड को सरल रखता है: एक ही `else` शाखा हर अनुपस्थित मान की स्थिति को कवर करती है।

# --instructions--

आइटम्स को इस तरह क्रमबद्ध करें कि जब `width` और `height` दोनों के पास मान हो तो `Area: 20` प्रिंट हो, और अन्यथा `Missing size` प्रिंट हो।
`width` को `height` से पहले घोषित करें

# --answers--

```swift
let width: Int? = 4
```

```swift
let height: Int? = 5
```

```swift
if let w = width, let h = height {
```

```swift
    print("Area: \(w * h)")
```

```swift
} else {
    print("Missing size")
}
```

# --solutions--

```swift
let width: Int? = 4
let height: Int? = 5
if let w = width, let h = height {
    print("Area: \(w * h)")
} else {
    print("Missing size")
}
```

# --output--

Area: 20
