---
language: swift
exerciseType: 1
---

# --description--

कई ऑपरेशन विफल हो सकते हैं, और Swift एक ऑप्शनल लौटाकर विफलता की सूचना देता है।
टेक्स्ट को संख्या में बदलना इसका क्लासिक उदाहरण है: `Int("42")` `42` रखने वाला एक `Int?` लौटाता है, जबकि `Int("abc")` `nil` लौटाता है।
`Int("3.5")` भी `nil` है, क्योंकि टेक्स्ट एक पूर्ण संख्या नहीं है; दशमलव के लिए `Double("3.5")` का उपयोग करें।
```swift
let typed = "42"
if let number = Int(typed) {
    print(number + 1) // 43
}
```
अन्य उदाहरण हैं `array.first` (खाली ऐरे के लिए `nil`) और `dictionary[key]` (कुंजी अनुपस्थित होने पर `nil`)।

# --instructions--

एक फ़ंक्शन `parseAge` लिखें जो एक `String` (आर्ग्युमेंट लेबल छोड़ दें) लेता है और इसे `Int` में बदलकर लौटाता है, या टेक्स्ट के वैध पूर्ण संख्या न होने पर `-1` लौटाता है

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`parseAge("42")` को `42` लौटाना चाहिए

```swift
tryCatch(parseAge("42") == 42)
```

`parseAge("abc")` को `-1` लौटाना चाहिए

```swift
tryCatch(parseAge("abc") == -1)
```

`parseAge("3.5")` को `-1` लौटाना चाहिए

```swift
tryCatch(parseAge("3.5") == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func parseAge(_ text: String) -> Int {
    return Int(text) ?? -1
}
```
