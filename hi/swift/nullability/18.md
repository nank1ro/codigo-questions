---
language: swift
exerciseType: 1
---

# --description--

कभी-कभी आप किसी ऑप्शनल के अंदर के मान को बदलना चाहते हैं और परिणाम को ऑप्शनल ही रखना चाहते हैं, बिना हाथ से अनरैप और फिर से रैप किए।
ऑप्शनल्स के पास एक `map` मेथड होता है: यदि कोई मान है तो यह क्लोज़र को उस पर लागू करता है, अन्यथा `nil` लौटाता है।
```swift
let score: Int? = 10
let doubled = score.map { $0 * 2 } // 20 रखने वाला Int?
let missing: Int? = nil
let stillMissing = missing.map { $0 * 2 } // nil
```
एक विफल हो सकने वाले कन्वर्ज़न के साथ मिलाकर यह `Int(text).map { $0 + 1 }` जैसी एक संक्षिप्त पाइपलाइन बनाता है।

# --instructions--

एक फ़ंक्शन `squareText` लिखें जो एक `String` (आर्ग्युमेंट लेबल छोड़ दें) लेता है, इसे `Int` में बदलता है और इसका वर्ग `Int?` के रूप में लौटाता है, या कन्वर्ज़न विफल होने पर `nil` लौटाता है।
`map` का उपयोग करें

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`squareText("4")` को `16` लौटाना चाहिए

```swift
tryCatch(squareText("4") == 16)
```

`squareText("12")` को `144` लौटाना चाहिए

```swift
tryCatch(squareText("12") == 144)
```

`squareText("x")` को `nil` लौटाना चाहिए

```swift
tryCatch(squareText("x") == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func squareText(_ text: String) -> Int? {
    return Int(text).map { $0 * $0 }
}
```
