---
language: swift
exerciseType: 4
---

# --description--

फोर्स्ड अनरैपिंग तभी सुरक्षित है जब आपने पहले ही जाँच लिया हो कि ऑप्शनल `nil` नहीं है:
```swift
if score != nil {
    print(score! * 2)
}
```

# --instructions--

आइटम्स को इस तरह क्रमबद्ध करें कि `price` को केवल यह जाँचने के बाद दोगुना करके प्रिंट किया जाए कि वह `nil` नहीं है

# --answers--

```swift
let price: Double? = 9.5
```

```swift
if price != nil {
```

```swift
    print(price! * 2)
```

```swift
}
```

# --solutions--

```swift
let price: Double? = 9.5
if price != nil {
    print(price! * 2)
}
```

# --output--

19.0
