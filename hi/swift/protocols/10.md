---
language: swift
exerciseType: 4
---

# --description--

एक प्रोटोकॉल एक्सटेंशन ऐसे मेंबर्स भी जोड़ सकता है जिन्हें प्रोटोकॉल ने कभी आवश्यकता के रूप में सूचीबद्ध नहीं किया। ये अतिरिक्त सुविधाएँ हैं, जो हर अनुरूप बनने वाले टाइप पर उपलब्ध हैं:
```swift
protocol Sized {
    var count: Int { get }
}

extension Sized {
    var isEmpty: Bool {
        return count == 0
    }
}
```
`isEmpty` कोई आवश्यकता नहीं है, इसलिए अनुरूप बनने वाले टाइप को उसे देने की ज़रूरत नहीं — उसे वह बस मिल जाता है।

# --instructions--

आइटम्स को क्रमबद्ध करें ताकि `HEY` प्रिंट हो, पहले प्रोटोकॉल परिभाषित हो, फिर उसका एक्सटेंशन, फिर स्ट्रक्चर, फिर `print`

# --answers--

```swift
protocol Loud {
    var text: String { get }
}
```

```swift
extension Loud {
    func shout() -> String { return text.uppercased() }
}
```

```swift
struct Message: Loud {
    var text: String
}
```

```swift
print(Message(text: "hey").shout())
```

# --solutions--

```swift
protocol Loud {
    var text: String { get }
}
extension Loud {
    func shout() -> String { return text.uppercased() }
}
struct Message: Loud {
    var text: String
}
print(Message(text: "hey").shout())
```

# --output--

HEY
