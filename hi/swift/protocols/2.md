---
language: swift
exerciseType: 1
---

# --description--

एक प्रोटोकॉल **मेथड्स** की आवश्यकता भी रख सकता है। आप सिग्नेचर — नाम, पैरामीटर्स और रिटर्न टाइप — लिखते हैं और वहीं रुक जाते हैं, बिना बॉडी के:
```swift
protocol Greeter {
    func greet() -> String
}
```
अनुरूप बनने वाले टाइप को बिल्कुल उसी सिग्नेचर वाली मेथड घोषित करनी होती है, और बॉडी वह देता है:
```swift
struct Robot: Greeter {
    func greet() -> String {
        return "BEEP"
    }
}

print(Robot().greet()) // BEEP
```
अगर कुछ भी अलग हो — नाम, कोई पैरामीटर टाइप या रिटर्न टाइप — तो टाइप अनुरूप नहीं बनता, और कंपाइलर बताता है कि कौन सी आवश्यकता पूरी नहीं हुई।

# --instructions--

`Friendly` को आवश्यक `greet()` मेथड जोड़कर `Greeter` के अनुरूप बनाएं, जो नाम को `Hello, ` और `!` के बीच रखकर लौटाती है

उपयोग का उदाहरण:
```swift
print(Friendly(name: "Ada").greet())
// Hello, Ada! प्रिंट करता है
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Greeter {
    func greet() -> String
}

struct Friendly {
    var name: String
}
```

# --before-asserts--

```swift
func _asGreeter(_ g: Greeter) -> String {
    return g.greet()
}
```

# --asserts--

`Friendly(name: "Ada")` को `Hello, Ada!` के साथ ग्रीट करना चाहिए

```swift
tryCatch(_asGreeter(Friendly(name: "Ada")) == "Hello, Ada!")
```

`Friendly(name: "Bo")` को `Hello, Bo!` के साथ ग्रीट करना चाहिए

```swift
tryCatch(_asGreeter(Friendly(name: "Bo")) == "Hello, Bo!")
```

एक खाली नाम को `Hello, !` के साथ ग्रीट करना चाहिए

```swift
tryCatch(_asGreeter(Friendly(name: "")) == "Hello, !")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Greeter {
    func greet() -> String
}

struct Friendly: Greeter {
    var name: String

    func greet() -> String {
        return "Hello, \(name)!"
    }
}
```
