---
language: swift
exerciseType: 4
---

# --instructions--

आइटम्स को क्रमबद्ध करें ताकि `[G]` प्रिंट हो, पहले प्रोटोकॉल परिभाषित हो, फिर उसका एक्सटेंशन, फिर एनम, फिर `print`

# --answers--

```swift
protocol Badge {
    var symbol: String { get }
}
```

```swift
extension Badge {
    func banner() -> String { return "[\(symbol)]" }
}
```

```swift
enum Rank: Badge {
    case gold

    var symbol: String { return "G" }
}
```

```swift
print(Rank.gold.banner())
```

# --solutions--

```swift
protocol Badge {
    var symbol: String { get }
}
extension Badge {
    func banner() -> String { return "[\(symbol)]" }
}
enum Rank: Badge {
    case gold

    var symbol: String { return "G" }
}
print(Rank.gold.banner())
```

# --output--

[G]
