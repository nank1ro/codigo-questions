---
language: swift
exerciseType: 4
---

# --description--

`Int`, `String`, ऐरे और स्ट्रक्ट्स **वैल्यू टाइप्स** हैं: कोई मेथड उस मान को नहीं बदल सकती जिस पर उसे कॉल किया जाता है, जब तक कि वह `mutating` से चिह्नित न हो। एक्सटेंशन म्यूटेटिंग मेथड्स भी जोड़ सकते हैं:
```swift
extension Int {
    mutating func increment() {
        self += 1
    }
}
var count = 1
count.increment()
print(count) // 2
```
एक म्यूटेटिंग मेथड के अंदर आप `self` को असाइन कर सकते हैं। मान को एक `var` में संग्रहीत होना चाहिए: किसी `let` कॉन्स्टेंट पर `increment()` कॉल करना एक कंपाइल एरर है।

# --instructions--

आइटम्स को क्रमबद्ध करें ताकि पहले एक म्यूटेटिंग मेथड `double()` के साथ `Int` का एक्सटेंशन परिभाषित हो, फिर मान `4` के साथ एक वेरिएबल `n` घोषित करें, उसे डबल करें और प्रिंट करें

# --answers--

```swift
extension Int {
```

```swift
    mutating func double() { self *= 2 }
```

```swift
}
```

```swift
var n = 4
```

```swift
n.double()
```

```swift
print(n)
```

# --solutions--

```swift
extension Int {
    mutating func double() { self *= 2 }
}
var n = 4
n.double()
print(n)
```

# --output--

8
