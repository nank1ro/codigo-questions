---
language: swift
exerciseType: 2
---

# --description--

मेथड्स के अलावा, एक्सटेंशन **कंप्यूटेड प्रॉपर्टीज़** भी जोड़ सकता है: ऐसी प्रॉपर्टीज़ जो मान संग्रहीत नहीं करतीं बल्कि हर बार पढ़े जाने पर उसकी गणना करती हैं।
कंप्यूटेड प्रॉपर्टी को `var`, एक टाइप एनोटेशन और कर्ली ब्रेसेस के बीच एक बॉडी के साथ घोषित किया जाता है जो मान लौटाती है:
```swift
extension Int {
    var isNegative: Bool {
        return self < 0
    }
}
print((-3).isNegative) // true
print(7.isNegative)    // false
```
इसे किसी भी प्रॉपर्टी की तरह, बिना कोष्ठकों के पढ़ा जाता है: `7.isNegative`, न कि `7.isNegative()`।

# --instructions--

एक्सटेंशन को पूरा करें ताकि `isEven` एक कंप्यूटेड प्रॉपर्टी हो जो बताती है कि संख्या सम है या नहीं, जिससे प्रोग्राम `true` और `false` प्रिंट करे

# --seed--

```swift
extension Int {
    [/] isEven: [/] {
        return self % 2 == 0
    }
}
print(4.isEven)
print(7.isEven)
```

# --answers--

- var
- Bool
- let
- func
- Int

# --solutions--

```swift
extension Int {
    var isEven: Bool {
        return self % 2 == 0
    }
}
print(4.isEven)
print(7.isEven)
```

# --output--

true
false
