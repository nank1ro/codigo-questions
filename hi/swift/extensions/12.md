---
language: swift
exerciseType: 1
---

# --description--

`Array` एक जेनेरिक टाइप है: `[Int]` और `[String]` दोनों ऐरे हैं, जिनका **`Element`** टाइप अलग है। `Array` का एक एक्सटेंशन उन सभी पर लागू होता है, जो एक समस्या है जब नया मेंबर केवल कुछ एलिमेंट्स के लिए ही मायने रखता है: आप उन संख्याओं को नहीं जोड़ सकते जो स्ट्रिंग्स हैं।
एक `where` क्लॉज़ एक्सटेंशन को उन ऐरे तक सीमित कर देता है जिनका `Element` दिया गया टाइप है:
```swift
extension Array where Element == Int {
    var largest: Int {
        var result = Int.min
        for number in self {
            if number > result {
                result = number
            }
        }
        return result
    }
}
print([3, 9, 2].largest) // 9
```
`[3, 9, 2].largest` काम करता है, जबकि `["a", "b"].largest` एक कंपाइल एरर है: प्रॉपर्टी `[String]` पर मौजूद नहीं है।

# --instructions--

`Element == Int` तक सीमित `Array` का एक एक्सटेंशन लिखें जिसमें एक मेथड `sum()` हो जो एलिमेंट्स का योग लौटाती है। `reduce` का उपयोग न करें

मेथड कॉल का उदाहरण:
```swift
print([1, 2, 3].sum())
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

आपको एक `where Element == Int` क्लॉज़ के साथ `Array` के एक एक्सटेंशन का उपयोग करना चाहिए

```json
{
  "regex": "extension\\s+Array\\s+where\\s+Element\\s*==\\s*Int",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

आपको `reduce` का उपयोग नहीं करना चाहिए

```json
{
  "regex": "reduce",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`[1, 2, 3].sum()` को `6` लौटाना चाहिए

```swift
tryCatch([1, 2, 3].sum() == 6)
```

`[10, -4].sum()` को `6` लौटाना चाहिए

```swift
tryCatch([10, -4].sum() == 6)
```

`[7].sum()` को `7` लौटाना चाहिए

```swift
tryCatch([7].sum() == 7)
```

एक खाली ऐरे का योग `0` होना चाहिए

```swift
let _empty: [Int] = []
tryCatch(_empty.sum() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension Array where Element == Int {
    func sum() -> Int {
        var total = 0
        for number in self {
            total += number
        }
        return total
    }
}
```
