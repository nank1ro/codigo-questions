---
language: swift
exerciseType: 2
---

# --description--

एक एक्सटेंशन किसी टाइप में नए **इनिशियलाइज़र** जोड़ सकता है। एक स्ट्रक्ट के लिए यह उन्हें रखने की सबसे अच्छी जगह है: स्ट्रक्ट बॉडी के अंदर लिखा गया एक `init` ऑटोमैटिक मेंबरवाइज़ इनिशियलाइज़र को बदल देता है, जबकि एक्सटेंशन में जोड़ा गया उसे बनाए रखता है।
नया इनिशियलाइज़र आम तौर पर `self.init(...)` के साथ किसी मौजूदा इनिशियलाइज़र को काम सौंपता है:
```swift
struct Size {
    var width: Double
    var height: Double
}
extension Size {
    init(square side: Double) {
        self.init(width: side, height: side)
    }
}
let a = Size(square: 3)          // from the extension
let b = Size(width: 2, height: 5) // memberwise, still available
```

# --instructions--

एक्सटेंशन को पूरा करें ताकि `Point(both: 2.5)` `x` और `y` के बराबर `2.5` वाला एक पॉइंट बनाए, जिससे प्रोग्राम `2.5 2.5` और `1.0 2.0` प्रिंट करे

# --seed--

```swift
struct Point {
    var x: Double
    var y: Double
}
extension Point {
    [/](both: Double) {
        [/](x: both, y: both)
    }
}
let p = Point(both: 2.5)
let q = Point(x: 1, y: 2)
print(p.x, p.y)
print(q.x, q.y)
```

# --answers--

- init
- self.init
- func
- Point
- self

# --solutions--

```swift
struct Point {
    var x: Double
    var y: Double
}
extension Point {
    init(both: Double) {
        self.init(x: both, y: both)
    }
}
let p = Point(both: 2.5)
let q = Point(x: 1, y: 2)
print(p.x, p.y)
print(q.x, q.y)
```

# --output--

2.5 2.5
1.0 2.0
