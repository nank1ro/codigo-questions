---
language: swift
exerciseType: 1
---

# --description--

एक्सटेंशन में स्टैटिक मेथड्स छोटे फ़ैक्टरी फंक्शन्स के लिए एक अच्छी जगह हैं जो टाइप का एक मान बनाते हैं। `String(repeating:count:)` वह स्टैंडर्ड इनिशियलाइज़र है जो एक टेक्स्ट को एक संख्या की बार दोहराता है:
```swift
extension String {
    static func dashes(_ count: Int) -> String {
        return String(repeating: "-", count: count)
    }
}
print(String.dashes(4)) // ----
```

# --instructions--

`String` का एक एक्सटेंशन लिखें जिसमें एक स्टैटिक मेथड `stars(_ count: Int)` हो जो `count` एस्टरिस्क `*` से बनी एक स्ट्रिंग लौटाती है

मेथड कॉल का उदाहरण:
```swift
print(String.stars(3))
// prints ***
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

आपको `String` के एक एक्सटेंशन का उपयोग करना चाहिए

```json
{
  "regex": "extension\\s+String",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

मेथड `static` होनी चाहिए

```json
{
  "regex": "static\\s+func\\s+stars",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`String.stars(3)` को `"***"` लौटाना चाहिए

```swift
tryCatch(String.stars(3) == "***")
```

`String.stars(1)` को `"*"` लौटाना चाहिए

```swift
tryCatch(String.stars(1) == "*")
```

`String.stars(0)` को एक खाली स्ट्रिंग लौटानी चाहिए

```swift
tryCatch(String.stars(0) == "")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
extension String {
    static func stars(_ count: Int) -> String {
        return String(repeating: "*", count: count)
    }
}
```
