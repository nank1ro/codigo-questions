---
language: swift
exerciseType: 1
---

# --description--

इंडेक्स के साथ काम करना लंबा होता है, इसलिए Swift सबसे सामान्य मामलों के लिए शॉर्टकट देता है:
- `first` और `last` पहले और आखिरी वर्ण को एक ऑप्शनल `Character?` के रूप में लौटाते हैं (खाली स्ट्रिंग के लिए `nil`)
- `prefix(n)` पहले `n` वर्ण लौटाता है और `suffix(n)` आखिरी `n` वर्ण
```swift
let word = "Swift"
print(word.first!)     // S
print(word.prefix(2))  // Sw
print(word.suffix(3))  // ift
```
`prefix` और `suffix` एक `Substring` लौटाते हैं, जो मूल टेक्स्ट पर एक व्यू है। इसे असली `String` के रूप में सहेजने के लिए इसे `String(...)` में लपेटें। अगर `n`, `count` से बड़ा है, तो आपको बस पूरी स्ट्रिंग मिल जाती है।

# --instructions--

एक फंक्शन `firstTwo` लिखें जो एक `String` ले (आर्गुमेंट लेबल छोड़ें) और उसके पहले दो वर्णों को `String` के रूप में लौटाए, और एक फंक्शन `lastTwo` लिखें जो उसके आखिरी दो वर्णों को `String` के रूप में लौटाए

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`firstTwo("Swift")` को `"Sw"` लौटाना चाहिए

```swift
tryCatch(firstTwo("Swift") == "Sw")
```

`firstTwo("a")` को `"a"` लौटाना चाहिए

```swift
tryCatch(firstTwo("a") == "a")
```

`lastTwo("Swift")` को `"ft"` लौटाना चाहिए

```swift
tryCatch(lastTwo("Swift") == "ft")
```

`lastTwo("")` को `""` लौटाना चाहिए

```swift
tryCatch(lastTwo("") == "")
```

`firstTwo` को एक `String` लौटाना चाहिए

```swift
tryCatch(type(of: firstTwo("Swift")) == String.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstTwo(_ text: String) -> String {
    return String(text.prefix(2))
}

func lastTwo(_ text: String) -> String {
    return String(text.suffix(2))
}
```
