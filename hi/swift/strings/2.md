---
language: swift
exerciseType: 1
---

# --description--

**स्ट्रिंग इंटरपोलेशन** किसी एक्सप्रेशन के मान को स्ट्रिंग लिटरल के अंदर डालता है। एक्सप्रेशन को `\()` में लपेटें:
```swift
let name = "Ada"
let age = 36
print("\(name) is \(age) years old") // Ada is 36 years old
```
किसी भी टाइप को इंटरपोलेट किया जा सकता है: संख्याएँ, बूलियन और अन्य स्ट्रिंग्स सभी अपने आप टेक्स्ट में बदल जाते हैं।

# --instructions--

एक फंक्शन `greet` लिखें जो एक `String` ले (आर्गुमेंट लेबल छोड़ें) और टेक्स्ट `Hello, <name>!` लौटाए, जहाँ `<name>` वह आर्गुमेंट है।

फ़ंक्शन कॉल का उदाहरण:
```swift
print(greet("Ada"))
// prints Hello, Ada!
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`greet("Ada")` को `"Hello, Ada!"` लौटाना चाहिए

```swift
tryCatch(greet("Ada") == "Hello, Ada!")
```

`greet("Swift")` को `"Hello, Swift!"` लौटाना चाहिए

```swift
tryCatch(greet("Swift") == "Hello, Swift!")
```

`greet("")` को `"Hello, !"` लौटाना चाहिए

```swift
tryCatch(greet("") == "Hello, !")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greet(_ name: String) -> String {
    return "Hello, \(name)!"
}
```
