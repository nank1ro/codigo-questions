---
language: swift
exerciseType: 2
---

# --description--

आप एक संयुक्त रिटर्न मान के हिस्से के रूप में कई मान वापस करने के लिए फ़ंक्शन के रिटर्न प्रकार के रूप में टपल प्रकार का उपयोग कर सकते हैं।

# --instructions--

संख्याओं की सरणी `[15, 24, 31, 79, 44]` पास करते हुए `minMax` नामक एक वैध फ़ंक्शन बनाने के लिए कोड को पूरा करें

# --seed--

```swift
func minMax(array: [Int]) -> [/]min: Int, max: Int[/] {
    var currentMin = array[0]
    var currentMax = array[0]
    array.forEach { num in
        if num < currentMin {
            currentMin = num
        } else if num > currentMax {
            currentMax = num
        }
    }
    return [/][/], [/][/]
}
let numbers: [Int] = [15, 24, 31, 79, 44]
print(minMax([/]: numbers))
```

# --answers--

- (
- )
- [
- ]
- {
- }
- (
- )
- array
- numbers
- currentMin
- currentMax

# --solutions--

```swift
func minMax(array: [Int]) -> (min: Int, max: Int) {
    var currentMin = array[0]
    var currentMax = array[0]
    array.forEach { num in
        if num < currentMin {
            currentMin = num
        } else if num > currentMax {
            currentMax = num
        }
    }
    return (currentMin, currentMax)
}
let numbers: [Int] = [15, 24, 31, 79, 44]
print(minMax(array: numbers))
```

# --output--

(min: 15, max: 79)
