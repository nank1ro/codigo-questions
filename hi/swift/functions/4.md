---
language: swift
exerciseType: 2
---

# --description--

फ़ंक्शन में कई इनपुट पैरामीटर हो सकते हैं, जो फ़ंक्शन के कोष्ठकों के अंदर अल्पविराम से अलग करके लिखे जाते हैं।
```swift
func sayHello(name: String, newUser: Bool) -> String {
  var greet: String = "Hello \(name)!"
  if (newUser) {
    greet += " Welcome on board :)"
  }
  return greet
}
// "Hello Smith! Welcome on board :)" प्रिंट करता है
print(sayHello(name: "Smith", newUser: true))
```

# --instructions--

संख्याओं की सरणी `[15, 24, 31, 79]` और परिणाम प्रिंट करने के लिए `true` पास करते हुए `sumAllNumbers` नामक एक वैध फ़ंक्शन बनाने के लिए कोड को पूरा करें

# --seed--

```swift
func sumAllNumbers(_ nums: [Int], _ printResult: Bool) -> Int {
    /* reduce मेथड का उपयोग सभी संख्याओं को जोड़ने के लिए किया जाता है, 0 से शुरू करके और + चिह्न का उपयोग करते हुए
    */
    let result = nums.reduce(0, +)
    if [/] { print(result) }
    return result
}
let result = [/]([/]15, 24, 31, [/][/], [/])
```

# --answers--

- sumAllNumbers
- sum
- 79
- ]
- [
- true
- false
- True
- printResult
- print
- true

# --solutions--

```swift
func sumAllNumbers(_ nums: [Int], _ printResult: Bool) -> Int {
    /* reduce मेथड का उपयोग सभी संख्याओं को जोड़ने के लिए किया जाता है, 0 से शुरू करके और + चिह्न का उपयोग करते हुए
    */
    let result = nums.reduce(0, +)
    if printResult { print(result) }
    return result
}
let result = sumAllNumbers([15, 24, 31, 79], true)
```

# --output--

149
