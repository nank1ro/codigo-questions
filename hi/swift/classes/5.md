---
language: swift
exerciseType: 2
---

# --description--

जब किसी क्लास के अपने फंक्शन होते हैं, तो उन फंक्शन को __मेथड__ कहा जाता है।

# --instructions--

अपनी `Animal` क्लास में `description` नामक एक मेथड जोड़ें।
दो अलग-अलग `print` स्टेटमेंट का उपयोग करके, इसे उस जानवर का `gender` और `legs` प्रिंट करना चाहिए जिस पर इसे कॉल किया गया है।
फिर `description` मेथड को कॉल करें।

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4

    [/][/]() {
        if let gender = self.gender { print(gender) }
        print([/].legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.[/]
```

# --answers--

-  description
- func
- description()
- self
- description[]
- function
- method

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4

    func description() {
        if let gender = self.gender { print(gender) }
        print(self.legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.description()
```

# --output--

female
4
