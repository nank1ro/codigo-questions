---
language: swift
exerciseType: 1
---

# --description--

एक **कंप्यूटेड प्रॉपर्टी** देखने में प्रॉपर्टी जैसी लगती है पर व्यवहार में मेथड जैसी होती है: यह कुछ भी संग्रहीत नहीं करती, बल्कि हर बार पढ़ने पर अपना मान गणना करती है। आप टाइप लिखते हैं, फिर मान लौटाने वाला कोड का एक ब्लॉक:
```swift
struct Rectangle {
    var width: Int
    var height: Int

    var area: Int {
        return width * height
    }
}

let r = Rectangle(width: 3, height: 4)
print(r.area) // 12, no parentheses
```
कंप्यूटेड प्रॉपर्टीज़ मेम्बरवाइज़ इनिशियलाइज़र का हिस्सा नहीं होतीं, क्योंकि संग्रहीत करने को कुछ नहीं होता। जब मान बाकी मानों से प्राप्त होता हो तो कंप्यूटेड प्रॉपर्टी उपयोग करें, और जब काम के लिए पैरामीटर चाहिए हों तो मेथड।

# --instructions--

`Temperature` में `Double` टाइप की एक कंप्यूटेड प्रॉपर्टी `fahrenheit` जोड़ें, जो celsius मान को `9` से गुणा करके, `5` से भाग देकर, उसमें `32` जोड़कर लौटाती है

उपयोग का उदाहरण:
```swift
print(Temperature(celsius: 100).fahrenheit)
// prints 212.0
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Temperature {
    var celsius: Double

}
```

# --asserts--

`100` डिग्री celsius का `212` fahrenheit होना चाहिए

```swift
tryCatch(Temperature(celsius: 100).fahrenheit == 212)
```

`0` डिग्री celsius का `32` fahrenheit होना चाहिए

```swift
tryCatch(Temperature(celsius: 0).fahrenheit == 32)
```

`-40` डिग्री celsius का `-40` fahrenheit होना चाहिए

```swift
tryCatch(Temperature(celsius: -40).fahrenheit == -40)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Temperature {
    var celsius: Double

    var fahrenheit: Double {
        return celsius * 9 / 5 + 32
    }
}
```
