---
language: swift
exerciseType: 4
---

# --instructions--

`EUR 12` प्रिंट करने के लिए आइटम्स को क्रमबद्ध करें — पहले स्ट्रक्चर घोषित करें और इंस्टेंस को `print` से पहले बनाएँ

# --answers--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int
}
```

```swift
let price = Money(amount: 12)
```

```swift
print("\(Money.currency) \(price.amount)")
```

# --solutions--

```swift
struct Money {
    static let currency = "EUR"
    var amount: Int
}
let price = Money(amount: 12)
print("\(Money.currency) \(price.amount)")
```

# --output--

EUR 12
