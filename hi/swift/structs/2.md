---
language: swift
exerciseType: 1
---

# --description--

आपने कभी भी `Book` बनाने वाला कोड नहीं लिखा, फिर भी `Book(title: "Swift", pages: 120)` काम कर गया। Swift यह कोड आपके लिए लिख देता है: हर स्ट्रक्चर को एक मुफ्त **मेम्बरवाइज़ इनिशियलाइज़र** मिलता है, यानी एक ऐसा इनिशियलाइज़र जिसके पैरामीटर उसकी स्टोर्ड प्रॉपर्टीज़ होती हैं, घोषित होने के क्रम में, और हर एक प्रॉपर्टी के नाम को अपने आर्ग्युमेंट लेबल के रूप में उपयोग करता है:
```swift
struct Point {
    var x: Int
    var y: Int
}

let p = Point(x: 3, y: 4)
print(p.x) // 3
```
क्लासेस को यह मुफ्त नहीं मिलता, और यही एक कारण है कि किसी मान को मॉडल करने का सबसे तेज़ तरीका स्ट्रक्चर है।

# --instructions--

एक फ़ंक्शन `makePoint` लिखें जो बिना आर्ग्युमेंट लेबल के दो पूर्णांक लेता है और मेम्बरवाइज़ इनिशियलाइज़र से बनाया गया एक `Point` लौटाता है

फ़ंक्शन कॉल का उदाहरण:
```swift
print(makePoint(3, 4).x)
// prints 3
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Point {
    var x: Int
    var y: Int
}

func makePoint() {

}
```

# --asserts--

`makePoint(3, 4)` का `x` `3` के बराबर होना चाहिए

```swift
tryCatch(makePoint(3, 4).x == 3)
```

`makePoint(3, 4)` का `y` `4` के बराबर होना चाहिए

```swift
tryCatch(makePoint(3, 4).y == 4)
```

`makePoint(0, -5)` का `y` `-5` के बराबर होना चाहिए

```swift
tryCatch(makePoint(0, -5).y == -5)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Point {
    var x: Int
    var y: Int
}

func makePoint(_ x: Int, _ y: Int) -> Point {
    return Point(x: x, y: y)
}
```
