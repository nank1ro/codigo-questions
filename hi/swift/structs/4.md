---
language: swift
exerciseType: 1
---

# --description--

एक स्ट्रक्चर **मेथड्स** भी रख सकता है: ब्रेसिज़ के अंदर लिखे गए फ़ंक्शन जो उसी इंस्टेंस पर काम करते हैं जिस पर उन्हें कॉल किया जाता है। किसी मेथड के अंदर आप प्रॉपर्टी के नाम सीधे उपयोग करते हैं, बिना किसी प्रीफ़िक्स के:
```swift
struct Rectangle {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}

let r = Rectangle(width: 3, height: 4)
print(r.area()) // 12
```
यदि मेथड का कोई पैरामीटर किसी प्रॉपर्टी के नाम को शैडो कर दे, तो इंस्टेंस की प्रॉपर्टी का बोध कराने के लिए `self.width` लिखें।

# --instructions--

`Rectangle` में एक मेथड `area` जोड़ें जो कोई पैरामीटर नहीं लेता और चौड़ाई को ऊँचाई से गुणा करके लौटाता है

फ़ंक्शन कॉल का उदाहरण:
```swift
print(Rectangle(width: 3, height: 4).area())
// prints 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
struct Rectangle {
    var width: Int
    var height: Int

}
```

# --asserts--

`3` गुणा `4` वाले आयत का क्षेत्रफल `12` होना चाहिए

```swift
tryCatch(Rectangle(width: 3, height: 4).area() == 12)
```

`1` गुणा `1` वाले आयत का क्षेत्रफल `1` होना चाहिए

```swift
tryCatch(Rectangle(width: 1, height: 1).area() == 1)
```

`0` गुणा `7` वाले आयत का क्षेत्रफल `0` होना चाहिए

```swift
tryCatch(Rectangle(width: 0, height: 7).area() == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
struct Rectangle {
    var width: Int
    var height: Int

    func area() -> Int {
        return width * height
    }
}
```
