---
language: swift
exerciseType: 4
---

# --instructions--

`12` प्रिंट करने के लिए आइटम्स को क्रमबद्ध करें — पहले स्ट्रक्चर घोषित करें और इंस्टेंस को `print` से पहले बनाएँ

# --answers--

```swift
struct Rectangle {
    var width: Int
    var height: Int
    func area() -> Int {
        return width * height
    }
}
```

```swift
let r = Rectangle(width: 3, height: 4)
```

```swift
print(r.area())
```

# --solutions--

```swift
struct Rectangle {
    var width: Int
    var height: Int
    func area() -> Int {
        return width * height
    }
}
let r = Rectangle(width: 3, height: 4)
print(r.area())
```

# --output--

12
