---
language: swift
exerciseType: 2
---

# --description--

एक स्टोर्ड प्रॉपर्टी को उसकी घोषणा के स्थान पर ही एक **डिफ़ॉल्ट मान** दिया जा सकता है। Swift उस मान से उसका टाइप पता लगा लेता है, इसलिए आप टाइप एनोटेशन हटा सकते हैं:
```swift
struct Counter {
    var label: String
    var value = 0
}
```
मेम्बरवाइज़ इनिशियलाइज़र हर डिफ़ॉल्ट मान वाली प्रॉपर्टी को एक वैकल्पिक आर्ग्युमेंट बना देता है: डिफ़ॉल्ट को बदलने के लिए उसे पास करें, वही रखने के लिए छोड़ दें।
```swift
let a = Counter(label: "clicks")
print(a.value) // 0

let b = Counter(label: "clicks", value: 7)
print(b.value) // 7
```

# --instructions--

कोड को इस तरह पूरा करें कि `value` `0` से शुरू हो और `c` बिना `value` पास किए बनाया जाए

# --seed--

```swift
struct Counter {
    var label: String
    var value = [/]
}

let c = Counter([/]: "clicks")
print(c.label)
print(c.value)
```

# --answers--

- 0
- label
- 1
- value
- Counter
- name

# --solutions--

```swift
struct Counter {
    var label: String
    var value = 0
}

let c = Counter(label: "clicks")
print(c.label)
print(c.value)
```

# --output--

clicks
0
