---
language: swift
exerciseType: 4
---

# --description--

`matches(of:)` पहले एक के बजाय **हर** मैच को एक ऐरे के रूप में लौटाता है। ऐरे कभी `nil` नहीं होता: कुछ मैच न होने पर वह बस खाली होता है, इसलिए उस पर सीधे लूप चलाया जा सकता है या उसे बदला जा सकता है:
```swift
let text = "a1 b22"
print(text.matches(of: #/\d+/#).count) // 2
```
हर एलिमेंट एक मैच है, इसलिए `map` के अंदर `$0.0` मैच हुआ टेक्स्ट है:
```swift
let found = text.matches(of: #/\d+/#).map { String($0.0) }
print(found) // ["1", "22"]
```

# --instructions--

आइटम्स को क्रमबद्ध करें ताकि एक ऐसा प्रोग्राम बने जो `text` की हर अंक-शृंखला को `found` में इकट्ठा करे और उसे प्रिंट करे

# --answers--

```swift
let text = "a1 b22 c333"
```

```swift
let pattern = #/\d+/#
```

```swift
let found = text.matches(of: pattern).map { String($0.0) }
```

```swift
print(found)
```

# --solutions--

```swift
let text = "a1 b22 c333"
let pattern = #/\d+/#
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

```swift
let pattern = #/\d+/#
let text = "a1 b22 c333"
let found = text.matches(of: pattern).map { String($0.0) }
print(found)
```

# --output--

["1", "22", "333"]
