---
language: swift
exerciseType: 1
---

# --description--

एक `#/ ... /#` लिटरल कंपाइल करते समय निश्चित होता है। जब पैटर्न केवल रन टाइम पर पता चलता है, उदाहरण के लिए क्योंकि किसी उपयोगकर्ता ने उसे टाइप किया हो, तो उसे `Regex(_:)` से बनाएँ:
```swift
let regex = try Regex("[0-9]+")
print("abc123".contains(regex)) // true
```
यह इनिशियलाइज़र **throws** करता है: `"["` जैसा कोई अमान्य पैटर्न केवल प्रोग्राम चलते समय पकड़ा जाता है, इसलिए कॉल को `try` की ज़रूरत है, और एरर को या तो `do`/`catch` (या `try?`) से संभाला जाना चाहिए या आस-पास के फ़ंक्शन को `throws` चिह्नित करके आगे बढ़ाया जाना चाहिए, जैसा यह अभ्यास करता है। इस तरह बनाए गए regex की कोई क्रमांकित प्रॉपर्टी कंपाइल टाइम पर ज्ञात नहीं होती, लेकिन `contains`, `matches(of:)` और `replacing` बिल्कुल पहले की तरह काम करते हैं।

# --instructions--

एक फ़ंक्शन `countMatches` लिखें जो एक टेक्स्ट (`String`) और एक पैटर्न (`String`) लेता है, दोनों बिना आर्ग्युमेंट लेबल के, `throws` से चिह्नित हो, और लौटाता है कि पैटर्न टेक्स्ट में कितनी बार मिलता है। एक अमान्य पैटर्न को एरर को बाहर जाने देना चाहिए

फ़ंक्शन कॉल का उदाहरण:
```swift
print(try countMatches("a1 b22", "[0-9]+"))
// 2 प्रिंट करता है
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func countMatches() {

}
```

# --asserts--

फ़ंक्शन को regex को `try Regex(` से बनाना चाहिए

```json
{
  "regex": "try\\s+Regex\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`countMatches("a1 b22", "[0-9]+")` को `2` लौटाना चाहिए

```swift
tryCatch((try? countMatches("a1 b22", "[0-9]+")) == 2)
```

`countMatches("abc", "[0-9]+")` को `0` लौटाना चाहिए

```swift
tryCatch((try? countMatches("abc", "[0-9]+")) == 0)
```

`countMatches("hello world", "o")` को `2` लौटाना चाहिए

```swift
tryCatch((try? countMatches("hello world", "o")) == 2)
```

`countMatches("abc", "[")` को throw करना चाहिए

```swift
tryCatch((try? countMatches("abc", "[")) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func countMatches(_ text: String, _ pattern: String) throws -> Int {
    let regex = try Regex(pattern)
    return text.matches(of: regex).count
}
```
