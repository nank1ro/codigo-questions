---
language: swift
exerciseType: 1
---

# --description--

चूंकि `Int(text)` और `Double(text)` विफल होने पर `nil` लौटाते हैं, परिणाम की तुलना `nil` से करने पर आपको पता चल जाता है कि कोई टेक्स्ट उस तरह की संख्या है या नहीं:
```swift
print(Int("42") != nil)     // true
print(Double("4.2") != nil) // true
print(Double("42") != nil)  // true, a whole number is also a valid Double
```
ध्यान दें कि अंतिम लाइन: `Int` द्वारा स्वीकार किया गया हर टेक्स्ट `Double` द्वारा भी स्वीकार किया जाता है, इसलिए जब आप उन्हें अलग करना चाहते हैं तो पहले `Int` की जांच करें।

# --instructions--

एक फ़ंक्शन `classify` लिखें जो एक `String` लेता है (आर्ग्युमेंट लेबल छोड़ दें) और लौटाता है `"Int"` यदि टेक्स्ट एक वैध पूर्ण संख्या है, `"Double"` यदि वह एक वैध दशमलव संख्या है, और अन्यथा `"String"` लौटाता है

फ़ंक्शन कॉल का उदाहरण:
```swift
print(classify("3.5"))
// prints Double
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`classify("42")` को `"Int"` लौटाना चाहिए

```swift
tryCatch(classify("42") == "Int")
```

`classify("-7")` को `"Int"` लौटाना चाहिए

```swift
tryCatch(classify("-7") == "Int")
```

`classify("3.5")` को `"Double"` लौटाना चाहिए

```swift
tryCatch(classify("3.5") == "Double")
```

`classify("hello")` को `"String"` लौटाना चाहिए

```swift
tryCatch(classify("hello") == "String")
```

`classify("")` को `"String"` लौटाना चाहिए

```swift
tryCatch(classify("") == "String")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func classify(_ text: String) -> String {
    if Int(text) != nil {
        return "Int"
    }
    if Double(text) != nil {
        return "Double"
    }
    return "String"
}
```
