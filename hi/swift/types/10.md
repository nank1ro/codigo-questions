---
language: swift
exerciseType: 4
---

# --description--

एक **टाइप एलियास** किसी मौजूदा टाइप को `typealias` कीवर्ड से एक नया नाम देता है:
```swift
typealias Score = Int
let best: Score = 100
print(best + 1) // 101
```
`Score` और `Int` एक ही टाइप हैं, इसलिए वे आपस में बेझिझक मिलते हैं। एक एलियास कोई सुरक्षा नहीं जोड़ता: यह केवल कोड को बेहतर तरीके से पढ़ने योग्य बनाता है जब आपके प्रोग्राम में किसी साधारण टाइप का एक विशिष्ट अर्थ हो।

# --instructions--

पहली लाइन पर `Celsius` को `Double` का एलियास परिभाषित करने, पानी के क्वथनांक को फ़ारेनहाइट में कन्वर्ट करने और उसे प्रिंट करने के लिए आइटम्स को क्रम में लगाएं

# --answers--

```swift
typealias Celsius = Double
```

```swift
let boiling: Celsius = 100
```

```swift
let fahrenheit = boiling * 9 / 5 + 32
```

```swift
print(fahrenheit)
```

# --solutions--

```swift
typealias Celsius = Double
let boiling: Celsius = 100
let fahrenheit = boiling * 9 / 5 + 32
print(fahrenheit)
```

# --output--

212.0
