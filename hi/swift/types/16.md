---
language: swift
exerciseType: 1
---

# --description--

कंडीशनल कास्ट `else if` के साथ स्वाभाविक रूप से श्रृंखलाबद्ध होते हैं ताकि कई संभावित टाइप्स को संभाला जा सके, हर एक को उस टाइप में कन्वर्ट करते हुए जिसकी आपको परिणाम के लिए ज़रूरत है:
```swift
let item: Any = 2.5
if let number = item as? Int {
    print(Double(number))
} else if let number = item as? Double {
    print(number)
}
```
`Any` में संग्रहीत एक `Int` अब भी एक `Int` है: उस पर `as? Double` की मान `nil` है, क्योंकि `as?` टाइप की जांच करता है, वह संख्याओं को कन्वर्ट नहीं करता।

# --instructions--

एक फ़ंक्शन `sumNumbers` लिखें जो `Any` का एक ऐरे लेता है (आर्ग्युमेंट लेबल छोड़ दें) और उसके `Int` और `Double` एलिमेंट्स का योग एक `Double` के रूप में लौटाता है, किसी अन्य टाइप के एलिमेंट्स को नज़रअंदाज़ करते हुए

फ़ंक्शन कॉल का उदाहरण:
```swift
print(sumNumbers([1, 2.5, "x"]))
// prints 3.5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`sumNumbers([1, 2.5, "x"])` को `3.5` लौटाना चाहिए

```swift
tryCatch(sumNumbers([1, 2.5, "x"]) == 3.5)
```

`sumNumbers(["3", 4])` को `4.0` लौटाना चाहिए

```swift
tryCatch(sumNumbers(["3", 4]) == 4.0)
```

`sumNumbers([0.5, 0.25])` को `0.75` लौटाना चाहिए

```swift
tryCatch(sumNumbers([0.5, 0.25]) == 0.75)
```

`sumNumbers(["a", "b"])` को `0.0` लौटाना चाहिए

```swift
tryCatch(sumNumbers(["a", "b"]) == 0.0)
```

`sumNumbers([10])` को एक `Double` लौटाना चाहिए

```swift
tryCatch(type(of: sumNumbers([10])) == Double.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func sumNumbers(_ values: [Any]) -> Double {
    var total = 0.0
    for value in values {
        if let number = value as? Int {
            total += Double(number)
        } else if let number = value as? Double {
            total += number
        }
    }
    return total
}
```
