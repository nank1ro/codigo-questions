---
language: swift
exerciseType: 1
---

# --description--

`TODO` आमतौर पर किसी प्लेसहोल्डर के पास होता है जो असली इंप्लीमेंटेशन लिखे जाने तक कोड को कंपाइल होने लायक बनाए रखता है। जब आप काम पूरा करें, तो उसी बदलाव में प्लेसहोल्डर बदलें और मार्कर हटा दें, ताकि कमेंट कोड की स्थिति के बारे में कभी झूठ न बोले।

# --instructions--

`isEven` में `TODO` कमेंट में बताया गया काम पूरा करें, फिर काम हो जाने के बाद वह कमेंट हटा दें

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func isEven(_ n: Int) -> Bool {
    // TODO: return true when n is even, false otherwise
    return false
}
```

# --asserts--

The `TODO` comment must be removed

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`isEven(4)` must return `true`

```swift
tryCatch(isEven(4) == true)
```

`isEven(7)` must return `false`

```swift
tryCatch(isEven(7) == false)
```

`isEven(0)` must return `true`

```swift
tryCatch(isEven(0) == true)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func isEven(_ n: Int) -> Bool {
    return n % 2 == 0
}
```
