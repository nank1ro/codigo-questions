---
language: swift
exerciseType: 1
---

# --description--

डॉक्युमेंटेशन कमेंट की पहली लाइन **सारांश** होती है: एक छोटा वाक्य जो बताता है कि फ़ंक्शन क्या करता है। इसे तीसरे पुरुष में लिखें, मानो फ़ंक्शन का वर्णन कर रहे हों: "Returns...", "Adds...", "Checks..."।
```swift
/// Returns `true` when `n` is divisible by two.
func isEven(_ n: Int) -> Bool {
    return n % 2 == 0
}
```
कमेंट डिक्लेरेशन के ठीक ऊपर होना चाहिए, बीच में कोई खाली लाइन नहीं, वरना Xcode उसे फ़ंक्शन से नहीं जोड़ता।

# --instructions--

`square` के ठीक ऊपर एक-लाइन सारांश वाला `///` डॉक्युमेंटेशन कमेंट जोड़ें, उदाहरण के लिए `/// Returns `n` multiplied by itself.`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func square(_ n: Int) -> Int {
    return n * n
}
```

# --asserts--

A `///` comment must be placed directly above `func square`

```json
{
  "regex": "///\\s*\\S[^\\n]*\\n\\s*func square",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`square(3)` must return `9`

```swift
tryCatch(square(3) == 9)
```

`square(-5)` must return `25`

```swift
tryCatch(square(-5) == 25)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
/// Returns `n` multiplied by itself.
func square(_ n: Int) -> Int {
    return n * n
}
```
