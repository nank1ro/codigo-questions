---
language: swift
exerciseType: 1
---

# --description--

`//` के विपरीत, जो लाइन के अंत पर रुक जाता है, `/*` कमेंट केवल `*/` पर रुकता है। अगर आप उसे बंद करना भूल जाएँ, तो कंपाइलर बाद के पूरे कोड को कमेंट का हिस्सा मान लेता है और एरर देता है:
```swift
let width = 10 /* in centimetres
print(width) // still inside the comment: error, the comment is never closed
```
`//` और `/* */` दोनों ट्रेलिंग कमेंट के रूप में काम करते हैं, लेकिन `/*` के साथ हमेशा सुनिश्चित करें कि `*/` मौजूद हो।

# --instructions--

`twice` कंपाइल नहीं होता क्योंकि पहली लाइन का ट्रेलिंग कमेंट कभी बंद नहीं होता, इसलिए वह `return` को निगल जाता है। उसी लाइन के अंत में कमेंट को बंद करें

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func twice(_ n: Int) -> Int {
    let result = n * 2 /* twice the input
    return result
}
```

# --asserts--

The comment must be closed on the same line

```json
{
  "regex": "/\\* twice the input \\*/",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`twice(4)` must return `8`

```swift
tryCatch(twice(4) == 8)
```

`twice(-3)` must return `-6`

```swift
tryCatch(twice(-3) == -6)
```

`twice(0)` must return `0`

```swift
tryCatch(twice(0) == 0)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func twice(_ n: Int) -> Int {
    let result = n * 2 /* twice the input */
    return result
}
```
