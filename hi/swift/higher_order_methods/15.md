---
language: swift
exerciseType: 1
---

# --description--

आप अपने हायर-ऑर्डर फ़ंक्शन खुद लिख सकते हैं। एक क्लोजर लेने वाला और उससे बना **एक नया क्लोजर लौटाने** वाला फ़ंक्शन एक सामान्य पैटर्न है: लौटाया गया क्लोजर मूल क्लोजर को कैप्चर करता है, इसलिए पैरामीटर `@escaping` होना चाहिए।
उदाहरण के लिए, `negate` एक प्रेडिकेट को उसके विपरीत में बदल देता है, `filter` को पास करने के लिए तैयार:
```swift
func negate(_ predicate: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { !predicate($0) }
}
let isEven: (Int) -> Bool = { $0 % 2 == 0 }
print([1, 2, 3, 4].filter(negate(isEven))) // [1, 3]
```
ध्यान दें कि `filter(negate(isEven))` क्लोजर को एक सामान्य आर्ग्युमेंट के रूप में पास करता है, बिना ट्रेलिंग क्लोजर सिंटैक्स के।

# --instructions--

एक फ़ंक्शन `both` लिखें जो `(Int) -> Bool` टाइप के दो क्लोजर्स (दोनों आर्ग्युमेंट लेबल छोड़ दें) लेता है और उसी टाइप का एक क्लोजर लौटाता है जो `true` केवल तब होता है जब समान आर्ग्युमेंट के लिए दोनों `true` हों

फ़ंक्शन कॉल का उदाहरण:
```swift
let smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
print([4, 12, 7, 8].filter(smallEven))
// prints [4, 8]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`both({ $0 < 10 }, { $0 % 2 == 0 })` को `4` के लिए `true` होना चाहिए

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(4) == true)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })` को `12` के लिए `false` होना चाहिए

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(12) == false)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })` को `7` के लिए `false` होना चाहिए

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(7) == false)
```

लौटाया गया क्लोजर `filter` के साथ काम करना चाहिए

```swift
let _smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
tryCatch([4, 12, 7, 8].filter(_smallEven) == [4, 8])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func both(_ first: @escaping (Int) -> Bool, _ second: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { first($0) && second($0) }
}
```
