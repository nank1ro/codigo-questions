---
language: swift
exerciseType: 2
---

# --description--

जब कोई क्लोजर केवल एक प्रॉपर्टी पढ़ता है, तो आप इसकी जगह एक **की पाथ** पास कर सकते हैं: `\.name` का अर्थ है "एलिमेंट की `name` प्रॉपर्टी", और `map(\.name)` वही है जो `map { $0.name }` है।
किसी प्रॉपर्टी के आधार पर सॉर्ट करना सामान्य दो-आर्ग्युमेंट क्लोजर का उपयोग करता है, जो दोनों एलिमेंट्स पर उस प्रॉपर्टी की तुलना करता है:
```swift
struct City {
    var name: String
    var population: Int
}
let cities = [City(name: "Oslo", population: 700), City(name: "Rome", population: 2800)]
let byPopulation = cities.sorted { $0.population > $1.population }
print(byPopulation.map(\.name)) // ["Rome", "Oslo"]
```

# --instructions--

कोड को पूरा करें ताकि खिलाड़ियों को सबसे ऊँचे `score` से सबसे नीचे तक क्रमबद्ध किया जाए और उनके नाम एक की पाथ से प्रिंट किए जाएं, जिससे प्रोग्राम `["Mia", "Leo", "Zoe"]` प्रिंट करे

# --seed--

```swift
struct Player {
    var name: String
    var score: Int
}
let players = [Player(name: "Leo", score: 40), Player(name: "Mia", score: 75), Player(name: "Zoe", score: 12)]
let ranking = players.sorted { $0.score [/] $1.score }
print(ranking.map([/]))
```

# --answers--

- >
- \.name
- <
- \.score
- $0.name

# --solutions--

```swift
struct Player {
    var name: String
    var score: Int
}
let players = [Player(name: "Leo", score: 40), Player(name: "Mia", score: 75), Player(name: "Zoe", score: 12)]
let ranking = players.sorted { $0.score > $1.score }
print(ranking.map(\.name))
```

# --output--

["Mia", "Leo", "Zoe"]
