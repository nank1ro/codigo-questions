---
language: swift
exerciseType: 1
---

# --description--

`flatMap` उन क्लोजर्स के लिए है जो एक **ऐरे** लौटाते हैं: ऐरे का ऐरे बनाने की जगह, यह सभी लौटाए गए ऐरे को एक फ्लैट परिणाम में जोड़ देता है:
```swift
let teams = [["Ann", "Bob"], ["Cid"]]
print(teams.map { $0 })     // [["Ann", "Bob"], ["Cid"]]
print(teams.flatMap { $0 }) // ["Ann", "Bob", "Cid"]
```
क्लोजर फ्लैट होने से पहले हर इनर ऐरे को बदल भी सकता है, उदाहरण के लिए `teams.flatMap { $0.reversed() }` का परिणाम `["Bob", "Ann", "Cid"]` होता है।

# --instructions--

एक फ़ंक्शन `flatten` लिखें जो `Int` के ऐरे का एक ऐरे (आर्ग्युमेंट लेबल छोड़ दें) लेता है और `flatMap` का उपयोग करके क्रम में सभी संख्याओं वाला एक ऐरे लौटाता है

फ़ंक्शन कॉल का उदाहरण:
```swift
print(flatten([[1, 2], [3]]))
// prints [1, 2, 3]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

आपको `flatMap` का उपयोग करना चाहिए

```json
{
  "regex": "\\.flatMap",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`flatten([[1, 2], [3]])` को `[1, 2, 3]` लौटाना चाहिए

```swift
tryCatch(flatten([[1, 2], [3]]) == [1, 2, 3])
```

`flatten([[5], [], [6, 7]])` को `[5, 6, 7]` लौटाना चाहिए

```swift
tryCatch(flatten([[5], [], [6, 7]]) == [5, 6, 7])
```

`flatten([])` को `[]` लौटाना चाहिए

```swift
tryCatch(flatten([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func flatten(_ lists: [[Int]]) -> [Int] {
    return lists.flatMap { $0 }
}
```
