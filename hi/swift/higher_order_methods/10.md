---
language: swift
exerciseType: 1
---

# --description--

`enumerated()` एक ऐरे को `(offset, element)` जोड़े की एक सीक्वेंस में बदल देता है, ताकि कोई क्लोजर हर एलिमेंट की स्थिति का उपयोग उसके मान के साथ कर सके:
```swift
let steps = ["mix", "bake"]
let numbered = steps.enumerated().map { pair in
    "\(pair.offset + 1). \(pair.element)"
}
print(numbered) // ["1. mix", "2. bake"]
```
चूंकि हर जोड़ा एक टपल है, क्लोजर उसे डिस्ट्रक्चर भी कर सकता है: `.map { (i, step) in "\(i + 1). \(step)" }`।

# --instructions--

एक फ़ंक्शन `evenPositions` लिखें जो `String` का एक ऐरे (आर्ग्युमेंट लेबल छोड़ दें) लेता है और `enumerated()`, `filter` और `map` का उपयोग करके सम इंडेक्स (0, 2, 4, ...) पर मिलने वाले एलिमेंट्स लौटाता है

फ़ंक्शन कॉल का उदाहरण:
```swift
print(evenPositions(["a", "b", "c", "d", "e"]))
// prints ["a", "c", "e"]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

आपको `enumerated()` का उपयोग करना चाहिए

```json
{
  "regex": "\\.enumerated\\s*\\(\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

आपको किसी `for` लूप का उपयोग नहीं करना चाहिए

```json
{
  "regex": "\\bfor\\b",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`evenPositions(["a", "b", "c", "d", "e"])` को `["a", "c", "e"]` लौटाना चाहिए

```swift
tryCatch(evenPositions(["a", "b", "c", "d", "e"]) == ["a", "c", "e"])
```

`evenPositions(["x", "y"])` को `["x"]` लौटाना चाहिए

```swift
tryCatch(evenPositions(["x", "y"]) == ["x"])
```

`evenPositions([])` को `[]` लौटाना चाहिए

```swift
tryCatch(evenPositions([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func evenPositions(_ items: [String]) -> [String] {
    return items.enumerated().filter { $0.offset % 2 == 0 }.map { $0.element }
}
```
