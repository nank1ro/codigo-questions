---
language: swift
exerciseType: 1
---

# --description--

कुछ हायर-ऑर्डर फ़ंक्शन कलेक्शन को बदलने की जगह उसके बारे में एक सवाल का जवाब देते हैं। ये सभी एक ऐसा क्लोजर लेते हैं जो `Bool` लौटाता है:
- `first(where:)` पहला एलिमेंट लौटाता है जो क्लोजर को संतुष्ट करता है, या कोई न होने पर `nil`
- `contains(where:)` `true` लौटाता है यदि कम से कम एक एलिमेंट इसे संतुष्ट करता है
- `allSatisfy` `true` लौटाता है यदि हर एलिमेंट इसे संतुष्ट करता है

```swift
let nums = [4, 9, 16]
print(nums.first(where: { $0 > 5 }))  // Optional(9)
print(nums.contains(where: { $0 > 5 })) // true
print(nums.allSatisfy { $0 > 5 })       // false
```
`filter` के विपरीत, `first(where:)` पहली मैच पर रुक जाता है और कोई नया ऐरे नहीं बनाता।

# --instructions--

एक फ़ंक्शन `firstLong` लिखें जो `String` का एक ऐरे और `minLength` नाम का एक `Int` (दोनों आर्ग्युमेंट लेबल छोड़ दें) लेता है और `first(where:)` का उपयोग करके कम से कम `minLength` वर्णों वाली पहली स्ट्रिंग लौटाता है, या कोई न होने पर `nil`

फ़ंक्शन कॉल का उदाहरण:
```swift
print(firstLong(["hi", "hello", "hey"], 4))
// prints Optional("hello")
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

आपको `first(where:)` का उपयोग करना चाहिए

```json
{
  "regex": "\\.first\\s*\\(\\s*where",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`firstLong(["hi", "hello", "hey"], 4)` को `"hello"` लौटाना चाहिए

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 4) == "hello")
```

`firstLong(["hi", "hello", "hey"], 2)` को `"hi"` लौटाना चाहिए

```swift
tryCatch(firstLong(["hi", "hello", "hey"], 2) == "hi")
```

`firstLong(["hi", "hey"], 4)` को `nil` लौटाना चाहिए

```swift
tryCatch(firstLong(["hi", "hey"], 4) == nil)
```

`firstLong([], 1)` को `nil` लौटाना चाहिए

```swift
tryCatch(firstLong([], 1) == nil)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstLong(_ words: [String], _ minLength: Int) -> String? {
    return words.first(where: { $0.count >= minLength })
}
```
