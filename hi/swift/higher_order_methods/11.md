---
language: swift
exerciseType: 2
---

# --description--

`zip` दो सीक्वेंस के एलिमेंट्स को स्थिति दर स्थिति जोड़े में बाँधता है, जिससे टपल की एक सीक्वेंस बनती है। यह छोटे सीक्वेंस के अंत पर रुक जाता है:
```swift
let names = ["Ann", "Bob"]
let ages = [31, 27, 99]
let pairs = zip(names, ages).map { "\($0) is \($1)" }
print(pairs) // ["Ann is 31", "Bob is 27"]
```
क्लोजर के अंदर `$0` पहली सीक्वेंस का एलिमेंट है और `$1` दूसरी सीक्वेंस का। `zip` एक फ्री फ़ंक्शन है, मेथड नहीं: आप `zip(a, b)` लिखते हैं, `a.zip(b)` नहीं।

# --instructions--

कोड को पूरा करें ताकि हर शहर को उसके तापमान के साथ जोड़ा जाए और केवल `20` डिग्री से अधिक गर्म शहर रखे जाएं, जिससे प्रोग्राम `["Rome", "Cairo"]` प्रिंट करे

# --seed--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = [/](cities, temps).[/] { $1 > 20 }.map { $0.0 }
print(warm)
```

# --answers--

- zip
- filter
- map
- enumerated
- compactMap

# --solutions--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = zip(cities, temps).filter { $1 > 20 }.map { $0.0 }
print(warm)
```

# --output--

["Rome", "Cairo"]
