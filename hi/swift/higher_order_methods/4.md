---
language: swift
exerciseType: 2
---

# --description--

`reduce` हर स्टेप पर एक नया संचित मान बनाता है, जो तब बर्बादी है जब परिणाम एक ऐरे या डिक्शनरी हो। `reduce(into:)` क्लोजर को एक्यूम्युलेटर एक `inout` पैरामीटर के रूप में देता है, ताकि उसे बिना `return` के अपनी जगह पर संशोधित किया जा सके:
```swift
let words = ["a", "b", "a"]
let counts = words.reduce(into: [String: Int]()) { result, word in
    result[word, default: 0] += 1
}
print(counts["a"]!) // 2
```
`[String: Int]()` एक खाली डिक्शनरी बनाता है, और `result[word, default: 0]` वर्तमान गिनती पढ़ता है या कुंजी न मिलने पर `0`।

# --instructions--

कोड को पूरा करें ताकि `reduce(into:)` के साथ गिना जा सके कि हर अक्षर कितनी बार आता है, जिससे प्रोग्राम `3` प्रिंट करे

# --seed--

```swift
let letters = ["x", "y", "x", "x"]
let counts = letters.reduce([/] [String: Int]()) { result, letter in
    result[letter, [/] 0] += 1
}
print(counts["x"]!)
```

# --answers--

- into:
- default:
- 0,
- initial:
- or:

# --solutions--

```swift
let letters = ["x", "y", "x", "x"]
let counts = letters.reduce(into: [String: Int]()) { result, letter in
    result[letter, default: 0] += 1
}
print(counts["x"]!)
```

# --output--

3
