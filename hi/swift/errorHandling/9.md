---
language: swift
exerciseType: 1
---

# --description--

चूँकि `try?` एक ऑप्शनल बनाता है, nil-coalescing ऑपरेटर `??` एक फॉलबैक देकर काम पूरा कर देता है:
```swift
let port = (try? readPort(text)) ?? 8080
```
कोष्ठक मायने रखते हैं। अन्यथा `try?` पूरे एक्सप्रेशन को `??` समेत कवर करने की कोशिश करता, और कंपाइलर आपसे यह स्पष्ट करने को कहता है कि विफल होने वाली कॉल कहाँ खत्म होती है।

इस लाइन को एक वाक्य की तरह पढ़ें: *जो पोर्ट पढ़ने में सफल रहे उसका उपयोग करें, अन्यथा 8080*। जब रिकवरी वास्तव में सिर्फ़ एक डिफ़ॉल्ट मान हो, तो `do`/`catch` की दो लाइनें एक में बदल जाती हैं।

# --instructions--

`portOrDefault` को ऐसा पूरा करें कि वह `text` से पढ़ा गया पोर्ट लौटाए, या `readPort` के थ्रो करने पर `8080` लौटाए

फ़ंक्शन कॉल का उदाहरण:
```swift
print(portOrDefault("abc"))
// prints 8080
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum ConfigError: Error {
    case notANumber
}

func readPort(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ConfigError.notANumber
    }
    return value
}

func portOrDefault(_ text: String) -> Int {
    
}
```

# --asserts--

`portOrDefault("3000")` को `3000` लौटाना चाहिए

```swift
tryCatch(portOrDefault("3000") == 3000)
```

`portOrDefault("80")` को `80` लौटाना चाहिए

```swift
tryCatch(portOrDefault("80") == 80)
```

`portOrDefault("abc")` को `8080` पर फॉलबैक करना चाहिए

```swift
tryCatch(portOrDefault("abc") == 8080)
```

`portOrDefault("")` को `8080` पर फॉलबैक करना चाहिए

```swift
tryCatch(portOrDefault("") == 8080)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum ConfigError: Error {
    case notANumber
}

func readPort(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ConfigError.notANumber
    }
    return value
}

func portOrDefault(_ text: String) -> Int {
    return (try? readPort(text)) ?? 8080
}
```
