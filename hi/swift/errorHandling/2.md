---
language: swift
exerciseType: 1
---

# --description--

थ्रो करने वाला फ़ंक्शन अभी भी एक मान लौटा सकता है। `throws` कीवर्ड पैरामीटर लिस्ट और रिटर्न ऐरो के बीच आता है:
```swift
func square(_ n: Int) throws -> Int {
    if n < 0 {
        throw SquareError.negative
    }
    return n * n
}
```
इसे ज़ोर से पढ़ें तो: *square एक `Int` लेता है, हो सकता है थ्रो करे, और एक `Int` लौटाता है*।

कॉल साइट पर यह मान तभी मौजूद होता है जब कुछ भी थ्रो नहीं हुआ हो, इसलिए असाइनमेंट `do` ब्लॉक के अंदर रहता है:
```swift
do {
    let result = try square(4)
    print(result) // 16
} catch {
    print("failed")
}
```
`try` कोई वैकल्पिक सजावट नहीं है: इसके बिना कंपाइलर कॉल को अस्वीकार कर देता है, ताकि पढ़ने वाले को हमेशा दिखे कि कौन सी लाइनें विफल हो सकती हैं।

# --instructions--

`half` को थ्रो करने वाला चिह्नित करें, `n` विषम होने पर इसे `MathError.notEven` थ्रो करवाएँ, और अन्यथा `n / 2` लौटाएँ

फ़ंक्शन कॉल का उदाहरण:
```swift
do {
    print(try half(10))
} catch {
    print("odd")
}
// prints 5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum MathError: Error {
    case notEven
}

func half(_ n: Int) -> Int {
    
}
```

# --before-asserts--

```swift
func _half(_ n: Int) -> Int {
    do {
        return try half(n)
    } catch {
        return -999
    }
}
```

# --asserts--

`half(10)` को `5` लौटाना चाहिए

```swift
tryCatch(_half(10) == 5)
```

`half(0)` को `0` लौटाना चाहिए

```swift
tryCatch(_half(0) == 0)
```

`half(7)` को थ्रो करना चाहिए

```swift
tryCatch(_half(7) == -999)
```

`half(-4)` को `-2` लौटाना चाहिए

```swift
tryCatch(_half(-4) == -2)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum MathError: Error {
    case notEven
}

func half(_ n: Int) throws -> Int {
    if n % 2 != 0 {
        throw MathError.notEven
    }
    return n / 2
}
```
