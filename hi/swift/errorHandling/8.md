---
language: swift
exerciseType: 2
---

# --description--

कभी-कभी आपको यह परवाह नहीं होती कि कोई चीज़ *क्यों* विफल हुई, सिर्फ़ इतना कि वह विफल हुई। `try?` एक थ्रो करने वाली कॉल को एक **ऑप्शनल** में बदल देता है: सफल होने पर मान, थ्रो करने पर `nil`।
```swift
enum ParseError: Error {
    case notANumber
}

func toInt(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ParseError.notANumber
    }
    return value
}

if let number = try? toInt("42") {
    print(number) // 42
}
```
न `do`, न `catch`: विफलता उसी ऑप्शनल में समा जाती है जिसे अनरैप करना आप पहले से जानते हैं। कीमत यह है कि एरर का मान फेंक दिया जाता है, इसलिए `try?` का उपयोग तभी करें जब सच में रिपोर्ट करने को कुछ न हो।

# --instructions--

कोड पूरा करें ताकि सफल कॉल अनरैप हो जाए और विफल होने वाली कॉल `nil` के रूप में पहचानी जाए, जिससे `42` और फिर `not a number` प्रिंट हो

# --seed--

```swift
enum ParseError: Error {
    case notANumber
}

func toInt(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ParseError.notANumber
    }
    return value
}

let good = [/] toInt("42")
if let number = [/] {
    print(number)
}
if (try? toInt("abc")) == [/] {
    print("not a number")
}
```

# --answers--

- try?
- good
- nil
- try
- bad
- 0

# --solutions--

```swift
enum ParseError: Error {
    case notANumber
}

func toInt(_ text: String) throws -> Int {
    guard let value = Int(text) else {
        throw ParseError.notANumber
    }
    return value
}

let good = try? toInt("42")
if let number = good {
    print(number)
}
if (try? toInt("abc")) == nil {
    print("not a number")
}
```

# --output--

42
not a number
