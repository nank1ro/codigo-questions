---
language: swift
exerciseType: 1
---

# --description--

एक वैलिडेटर सबसे अच्छा पढ़ा जाता है जब अस्वीकृतियाँ पहले आती हैं और असली काम नीचे बिना इंडेंट के रह जाता है। `guard` इसी के लिए बना है: यह उस शर्त को बताता है जो पूरी होनी चाहिए, और शर्त पूरी न होने पर इसका `else` ब्लॉक चलता है।
```swift
func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
`guard` के `else` ब्लॉक को मौजूदा स्कोप छोड़ना ही होता है, और `throw` उसके तरीकों में से एक है, `return`, `break` और `continue` की तरह। फ़ंक्शन के शुरू में एक के ऊपर एक लगे कई `guard` इनपुट पर लागू होने वाले नियमों की सूची जैसे पढ़े जाते हैं।

# --instructions--

`priceFor` को एक ऐसे `guard` के साथ पूरा करें जो तब तक `OrderError.notPositive` थ्रो करे जब तक `quantity`, `0` से बड़ा न हो जाए

फ़ंक्शन कॉल का उदाहरण:
```swift
do {
    print(try priceFor(4))
} catch {
    print("invalid")
}
// prints 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    
    return quantity * 3
}
```

# --before-asserts--

```swift
func _price(_ quantity: Int) -> Int {
    do {
        return try priceFor(quantity)
    } catch OrderError.notPositive {
        return -1
    } catch {
        return -2
    }
}
```

# --asserts--

`priceFor(4)` को `12` लौटाना चाहिए

```swift
tryCatch(_price(4) == 12)
```

`priceFor(1)` को `3` लौटाना चाहिए

```swift
tryCatch(_price(1) == 3)
```

`priceFor(0)` को `notPositive` थ्रो करना चाहिए

```swift
tryCatch(_price(0) == -1)
```

`priceFor(-5)` को `notPositive` थ्रो करना चाहिए

```swift
tryCatch(_price(-5) == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
