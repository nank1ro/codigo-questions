---
language: swift
exerciseType: 4
---

# --description--

आमतौर पर एक ही enum में एक काम के विफल होने के सभी तरीके होते हैं, हर कारण के लिए एक case:
```swift
enum FormError: Error {
    case empty
    case tooLong
}
```
हर case के लिए अलग `catch` लिखना दोहराव भरा हो जाता है। इसके बजाय, पूरे टाइप को एक साथ कैच करें और मान पर `switch` करें:
```swift
} catch let error as FormError {
    switch error {
    case .empty: print("empty")
    case .tooLong: print("too long")
    }
} catch {
    print("unknown")
}
```
`catch let error as FormError` का मतलब है *जो कुछ भी `FormError` है उसे कैच करें, और उसे `error` कहें*। ब्लॉक के अंदर `error` का टाइप enum है, इसलिए `switch` को cases दिखते हैं और वह जाँचता है कि आपने उन सबको कवर किया है। अंतिम बिना पैटर्न वाला `catch` फिर भी ज़रूरी है, क्योंकि कोई दूसरे टाइप का एरर इस `do` ब्लॉक तक पहुँच सकता है।

# --instructions--

आइटम्स को क्रम में लगाकर `too long` प्रिंट कराएँ, पहले एरर टाइप घोषित करते हुए और फ़ंक्शन को `do` ब्लॉक से पहले रखते हुए

# --answers--

```swift
enum FormError: Error {
    case empty
    case tooLong
}
```

```swift
func check(_ text: String) throws {
    if text.isEmpty {
        throw FormError.empty
    }
    if text.count > 5 {
        throw FormError.tooLong
    }
}
```

```swift
do {
    try check("abcdefgh")
} catch let error as FormError {
```

```swift
    switch error {
    case .empty: print("empty")
    case .tooLong: print("too long")
    }
} catch {
    print("unknown")
}
```

# --solutions--

```swift
enum FormError: Error {
    case empty
    case tooLong
}
func check(_ text: String) throws {
    if text.isEmpty {
        throw FormError.empty
    }
    if text.count > 5 {
        throw FormError.tooLong
    }
}
do {
    try check("abcdefgh")
} catch let error as FormError {
    switch error {
    case .empty: print("empty")
    case .tooLong: print("too long")
    }
} catch {
    print("unknown")
}
```

# --output--

too long
