---
language: swift
exerciseType: 1
---

# --instructions--

इस अध्याय की सब चीज़ों का उपयोग करके एक वैलिडेटर लिखें:
- `FormError` को `Int` टाइप का `maximum` रखने वाला दूसरा case `tooLong` दें
- `validate`, नाम खाली होने पर `FormError.empty` थ्रो करे, 10 अक्षरों से लंबा होने पर `FormError.tooLong(maximum: 10)` थ्रो करे, और अन्यथा नाम लौटाए
- `safeName`, सत्यापित नाम लौटाए, या `validate` के थ्रो करने पर `"guest"` लौटाए

फ़ंक्शन कॉल का उदाहरण:
```swift
print(safeName(""))
// prints guest
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum FormError: Error {
    case empty
}

func validate(_ name: String) throws -> String {
    
}

func safeName(_ name: String) -> String {
    
}
```

# --before-asserts--

```swift
func _kind(_ name: String) -> String {
    do {
        return try validate(name)
    } catch FormError.empty {
        return "empty"
    } catch FormError.tooLong(let maximum) {
        return "max \(maximum)"
    } catch {
        return "other"
    }
}
```

# --asserts--

`validate("Ada")` को `Ada` लौटाना चाहिए

```swift
tryCatch(_kind("Ada") == "Ada")
```

`validate("")` को `empty` थ्रो करना चाहिए

```swift
tryCatch(_kind("") == "empty")
```

`validate("12345678901")` को `10` के साथ `tooLong` थ्रो करना चाहिए

```swift
tryCatch(_kind("12345678901") == "max 10")
```

ठीक 10 अक्षरों वाला नाम स्वीकार किया जाना चाहिए

```swift
tryCatch(_kind("1234567890") == "1234567890")
```

`safeName("")` को `guest` लौटाना चाहिए

```swift
tryCatch(safeName("") == "guest")
```

`safeName("Ada")` को `Ada` लौटाना चाहिए

```swift
tryCatch(safeName("Ada") == "Ada")
```

`safeName("12345678901")` को `guest` लौटाना चाहिए

```swift
tryCatch(safeName("12345678901") == "guest")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum FormError: Error {
    case empty
    case tooLong(maximum: Int)
}

func validate(_ name: String) throws -> String {
    guard !name.isEmpty else {
        throw FormError.empty
    }
    guard name.count <= 10 else {
        throw FormError.tooLong(maximum: 10)
    }
    return name
}

func safeName(_ name: String) -> String {
    return (try? validate(name)) ?? "guest"
}
```
