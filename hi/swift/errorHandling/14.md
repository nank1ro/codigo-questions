---
language: swift
exerciseType: 2
---

# --description--

एक `do` ब्लॉक केवल एक ही एरर टाइप तक सीमित नहीं है। हर कदम अपने तरीके से विफल हो सकता है, और हर विफलता का अपना `catch` बनता है:
```swift
do {
    let text = try load(false)
    let value = try parse(text)
    print(value)
} catch NetworkError.offline {
    print("offline")
} catch ParseError.badFormat {
    print("bad format")
} catch {
    print("unknown")
}
```
थ्रो करने वाला पहला `try` ब्लॉक को खत्म कर देता है, इसलिए बाद के कदम कभी नहीं चलते — वह मान कभी मौजूद ही नहीं होता। यही इस आकार को पढ़ने में आसान बनाता है: सफल पाथ ऊपर एक सीधी लाइन में रहता है, और गलत होने का हर संभावित तरीका नीचे सूचीबद्ध रहता है।

# --instructions--

कोड पूरा करें ताकि नेटवर्क विफलता और पार्सिंग विफलता अलग-अलग संभाली जाएँ, जिससे `offline` प्रिंट हो

# --seed--

```swift
enum NetworkError: Error {
    case offline
}

enum ParseError: Error {
    case badFormat
}

func load(_ online: Bool) throws -> String {
    guard online else {
        throw NetworkError.offline
    }
    return "raw"
}

func parse(_ text: String) throws -> Int {
    throw ParseError.badFormat
}

do {
    let text = try load(false)
    let value = try parse(text)
    print(value)
} catch [/] {
    print("offline")
} catch [/] {
    print("bad format")
} [/] {
    print("unknown")
}
```

# --answers--

- NetworkError.offline
- ParseError.badFormat
- catch
- NetworkError.badFormat
- ParseError.offline
- finally

# --solutions--

```swift
enum NetworkError: Error {
    case offline
}

enum ParseError: Error {
    case badFormat
}

func load(_ online: Bool) throws -> String {
    guard online else {
        throw NetworkError.offline
    }
    return "raw"
}

func parse(_ text: String) throws -> Int {
    throw ParseError.badFormat
}

do {
    let text = try load(false)
    let value = try parse(text)
    print(value)
} catch NetworkError.offline {
    print("offline")
} catch ParseError.badFormat {
    print("bad format")
} catch {
    print("unknown")
}
```

# --output--

offline
