---
language: swift
exerciseType: 1
---

# --description--

जब कोई क्लोजर किसी फ़ंक्शन का **आखिरी** आर्ग्युमेंट होता है, तो आप इसे कॉल के क्लोजिंग पैरेंथेसिस के बाद लिख सकते हैं। यह **ट्रेलिंग क्लोजर** सिंटैक्स है:
```swift
print(apply(5) { $0 + 1 }) // 6
```
अगर क्लोजर ही एकमात्र आर्ग्युमेंट है, तो पैरेंथेसिस को पूरी तरह हटाया जा सकता है:
```swift
func run(_ task: () -> Int) -> Int {
    return task()
}
print(run { 42 }) // 42
```
दोनों रूप बिल्कुल एक ही फ़ंक्शन को कॉल करते हैं, बस जब क्लोजर लंबा हो तो ट्रेलिंग सिंटैक्स पढ़ने में आसान होता है।

# --instructions--

एक फ़ंक्शन `applyTwice` लिखें जो एक `Int` और `(Int) -> Int` टाइप का एक क्लोजर लेता है (दोनों के आर्ग्युमेंट लेबल छोड़ दें) और उस नंबर पर क्लोजर को दो बार लागू करने का परिणाम लौटाता है

फ़ंक्शन कॉल का उदाहरण:
```swift
print(applyTwice(3) { $0 * 2 })
// prints 12
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`applyTwice(3) { $0 * 2 }` को `12` लौटाना चाहिए

```swift
tryCatch(applyTwice(3) { $0 * 2 } == 12)
```

`applyTwice(1) { $0 + 10 }` को `21` लौटाना चाहिए

```swift
tryCatch(applyTwice(1) { $0 + 10 } == 21)
```

`applyTwice(2, { $0 * $0 })` को `16` लौटाना चाहिए

```swift
tryCatch(applyTwice(2, { $0 * $0 }) == 16)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func applyTwice(_ n: Int, _ operation: (Int) -> Int) -> Int {
    return operation(operation(n))
}
```
