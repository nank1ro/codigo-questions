---
language: swift
exerciseType: 4
---

# --description--

क्लोजर उन ऐरे मेथड्स के साथ चमकते हैं जो इसे आर्ग्युमेंट के रूप में लेते हैं। `map` हर एलिमेंट पर क्लोजर को कॉल करता है और परिणामों के साथ एक नया ऐरे लौटाता है:
```swift
let nums = [1, 2, 3]
let doubled = nums.map { $0 * 2 }
print(doubled) // [2, 4, 6]
```
मूल ऐरे में कोई बदलाव नहीं होता। चूंकि `map` एक ही क्लोजर आर्ग्युमेंट लेता है, इसलिए इसे कॉल करने का सामान्य तरीका ट्रेलिंग क्लोजर सिंटैक्स है।

# --instructions--

`[1, 2, 3, 4]` के वर्गों वाला एक ऐरे `map` का उपयोग करके बनाने के लिए आइटम्स को क्रम में लगाएं, और उसे प्रिंट करें

# --answers--

```swift
let nums = [1, 2, 3, 4]
```

```swift
let squares = nums.map { $0 * $0 }
```

```swift
print(squares)
```

# --solutions--

```swift
let nums = [1, 2, 3, 4]
let squares = nums.map { $0 * $0 }
print(squares)
```

# --output--

[1, 4, 9, 16]
