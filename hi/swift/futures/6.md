---
language: swift
exerciseType: 4
---

# --description--

एक-दूसरे के बाद लिखी कई `await` कॉल्स **क्रमिक (sequentially)** रूप से चलती हैं: दूसरी कॉल तब तक शुरू भी नहीं होती जब तक पहली वापस न आ जाए। कोड ऊपर से नीचे पढ़ा जाता है, बिल्कुल साधारण कोड की तरह:
```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}

let a = await step("A")
print(a)
let b = await step("B")
print(b)
// start A
// done A
// start B
// done B
```
यही आप चाहते हैं जब दूसरी कॉल को पहली के परिणाम की ज़रूरत होती है। जब कॉल्स स्वतंत्र हों, तो दूसरी शुरू करने से पहले एक के लिए इंतज़ार करना बर्बाद हुआ समय है, और अगले एक्सरसाइज़ दिखाते हैं कि इससे कैसे बचें।

# --instructions--

पंक्तियों को इस तरह क्रमबद्ध करें कि नीचे दिया गया आउटपुट प्रिंट हो, `step` को टॉप-लेवल कोड से पहले घोषित करते हुए
```
start A
done A
start B
done B
```

# --answers--

```swift
func step(_ name: String) async -> String {
```

```swift
    print("start \(name)")
```

```swift
    return "done \(name)"
```

```swift
}
```

```swift
let a = await step("A")
```

```swift
print(a)
```

```swift
let b = await step("B")
```

```swift
print(b)
```

# --solutions--

```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}
let a = await step("A")
print(a)
let b = await step("B")
print(b)
```

# --output--

start A
done A
start B
done B
