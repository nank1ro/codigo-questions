---
language: swift
exerciseType: 1
---

# --description--

`Task.sleep` मौजूदा टास्क को कुछ समय के लिए रोक देता है, बिना किसी और चीज़ को ब्लॉक किए। इसे बीच में बाधित किया जा सकता है, इसलिए यह एक throwing एसिंक्रोनस कॉल है और इसे `try await` चाहिए। अवधि `.seconds`, `.milliseconds` या `.nanoseconds` जैसे हेल्पर्स से दी जाती है:
```swift
func slowGreeting() async throws -> String {
    try await Task.sleep(for: .milliseconds(50))
    return "hello"
}
```
असली नेटवर्क कॉल की जगह, किसी उदाहरण में धीमा काम दिखाने का यही मानक तरीका है। ध्यान दें कि इससे प्रोग्राम फ्रीज़ नहीं होता: जब एक टास्क सोता है, बाकी चलते रहते हैं।

# --instructions--

एक फ़ंक्शन `delayed` लिखें जो बिना आर्ग्युमेंट लेबल एक पूर्णांक लेता है, एसिंक्रोनस है, throw कर सकता है, `Task.sleep` से `50` मिलीसेकंड इंतज़ार करता है, और फिर संख्या को `3` से गुणा करके लौटाता है

फ़ंक्शन कॉल का उदाहरण:
```swift
print(try await delayed(2))
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func delayed() {

}
```

# --asserts--

फ़ंक्शन को `async throws` चिह्नित होना चाहिए

```json
{
  "regex": "func\\s+delayed\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

फ़ंक्शन को `Task.sleep` का उपयोग करना चाहिए

```json
{
  "regex": "Task\\.sleep",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`delayed(2)` को `6` लौटाना चाहिए

```swift
tryCatch((try? await delayed(2)) == 6)
```

`delayed(0)` को `0` लौटाना चाहिए

```swift
tryCatch((try? await delayed(0)) == 0)
```

`delayed(-4)` को `-12` लौटाना चाहिए

```swift
tryCatch((try? await delayed(-4)) == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func delayed(_ n: Int) async throws -> Int {
    try await Task.sleep(for: .milliseconds(50))
    return n * 3
}
```
