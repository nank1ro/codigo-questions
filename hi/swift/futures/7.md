---
language: swift
exerciseType: 1
---

# --description--

दो स्वतंत्र कॉल्स को एक साथ चलाने के लिए, उन्हें **`async let`** से घोषित करें। काम तुरंत शुरू हो जाता है, और प्रोग्राम इंतज़ार किए बिना आगे बढ़ता रहता है:
```swift
async let left = step("A")
async let right = step("B")
```
मान अभी उपलब्ध नहीं होता, इसलिए आप बाइंडिंग को सीधे उपयोग नहीं कर सकते: आपको उसे उस जगह `await` करना होता है जहाँ आपको अंततः उसकी ज़रूरत होती है। एक्सप्रेशन के आगे लगा एक `await` उसके अंदर के हर `async let` को कवर कर लेता है:
```swift
let both = await left + right
```
अगर हर कॉल में एक सेकंड लगता है, तो क्रमिक वर्ज़न को दो सेकंड चाहिए जबकि `async let` वर्ज़न को लगभग एक लगता है, क्योंकि दोनों कॉल्स ओवरलैप हो जाती हैं।

# --instructions--

एक फ़ंक्शन `total` लिखें जो बिना आर्ग्युमेंट लेबल दो पूर्णांक लेता है, एसिंक्रोनस है, और दोनों पर लागू किए गए `slowValue` का योग लौटाता है, दोनों कॉल्स को `async let` के साथ एक साथ (concurrently) शुरू करते हुए

फ़ंक्शन कॉल का उदाहरण:
```swift
print(await total(1, 2))
// 30 प्रिंट करता है
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total() {

}
```

# --asserts--

फ़ंक्शन को `async let` का उपयोग करना चाहिए

```json
{
  "regex": "async\\s+let",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`total(1, 2)` को `30` लौटाना चाहिए

```swift
tryCatch(await total(1, 2) == 30)
```

`total(0, 0)` को `0` लौटाना चाहिए

```swift
tryCatch(await total(0, 0) == 0)
```

`total(5, -2)` को `30` लौटाना चाहिए

```swift
tryCatch(await total(5, -2) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total(_ a: Int, _ b: Int) async -> Int {
    async let first = slowValue(a)
    async let second = slowValue(b)
    return await first + second
}
```
