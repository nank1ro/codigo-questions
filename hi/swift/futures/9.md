---
language: swift
exerciseType: 1
---

# --description--

`async let` उस स्कोप से बंधा होता है जहाँ उसे लिखा गया है। समानांतर काम शुरू करने और उस पर एक हैंडल रखने के लिए, एक **`Task`** का उपयोग करें। `Task { }` को दिया गया क्लोज़र अपने आप पर चलता है, और टास्क को स्टोर, पास या रिटर्न किया जा सकता है:
```swift
let job = Task {
    return await double(21)
}
```
परिणाम बाद में **`.value`** से पढ़ा जाता है, जिसे await किया जाता है:
```swift
print(await job.value)
// prints 42
```
हैंडल का टाइप बताता है कि वह क्या देता है और क्या throw कर सकता है: `Task<Int, Never>` एक ऐसा टास्क है जो एक `Int` लौटाता है और कभी throw नहीं करता। `async let` के विपरीत, एक `Task` साधारण, गैर-एसिंक्रोनस कोड से बनाया जा सकता है।

# --instructions--

एक फ़ंक्शन `startDoubling` लिखें जो बिना आर्ग्युमेंट लेबल एक पूर्णांक लेता है और उस संख्या का दोगुना देने वाला एक `Task<Int, Never>` लौटाता है

फ़ंक्शन कॉल का उदाहरण:
```swift
print(await startDoubling(4).value)
// prints 8
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func startDoubling() {

}
```

# --asserts--

फ़ंक्शन को एक `Task` बनाना चाहिए

```json
{
  "regex": "Task\\s*\\{",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`startDoubling(4).value` का `8` होना चाहिए

```swift
tryCatch(await startDoubling(4).value == 8)
```

`startDoubling(0).value` का `0` होना चाहिए

```swift
tryCatch(await startDoubling(0).value == 0)
```

`startDoubling(-6).value` का `-12` होना चाहिए

```swift
tryCatch(await startDoubling(-6).value == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func startDoubling(_ n: Int) -> Task<Int, Never> {
    return Task {
        return n * 2
    }
}
```
