---
language: swift
exerciseType: 4
---

# --description--

Sets की एक-दूसरे से तुलना भी की जा सकती है। ये मेथड `Bool` लौटाते हैं:
- `a.isSubset(of: b)` `true` होता है जब `a` का हर एलिमेंट `b` में भी हो
- `a.isSuperset(of: b)` `true` होता है जब `a` में `b` का हर एलिमेंट हो
- `a.isDisjoint(with: b)` `true` होता है जब `a` और `b` में कोई कॉमन एलिमेंट न हो
```swift
let small: Set<Int> = [1, 2]
let big: Set<Int> = [1, 2, 3]
print(small.isSubset(of: big))   // true
print(big.isSuperset(of: small)) // true
print(small.isDisjoint(with: big)) // false
```

# --instructions--

आइटम्स को इस क्रम में लगाएं कि पहले जांचा जाए कि `basket` का हर फल `fruits` में भी है या नहीं, फिर जांचा जाए कि दोनों sets में कुछ भी कॉमन नहीं है।
`basket` से पहले `fruits` घोषित करें

# --answers--

```swift
let fruits: Set<String> = ["apple", "pear", "plum"]
```

```swift
let basket: Set<String> = ["pear", "plum"]
```

```swift
print(basket.isSubset(of: fruits))
```

```swift
print(fruits.isDisjoint(with: basket))
```

# --solutions--

```swift
let fruits: Set<String> = ["apple", "pear", "plum"]
let basket: Set<String> = ["pear", "plum"]
print(basket.isSubset(of: fruits))
print(fruits.isDisjoint(with: basket))
```

# --output--

true
false
