---
language: swift
exerciseType: 1
---

# --description--

ऐरे की तरह, set भी constant (`let`) या variable (`var`) हो सकते हैं। बनने के बाद केवल `var` वाला set ही बदला जा सकता है।
खाली set बनाने के लिए आप टाइप का initializer कॉल करते हैं, क्योंकि अकेली खाली लिटरल `[]` से Swift को यह पता नहीं चलेगा कि किस एलिमेंट टाइप का उपयोग करना है:
```swift
var visited = Set<String>()
print(visited.isEmpty) // true
```
`isEmpty` प्रॉपर्टी `true` होती है जब set में कोई एलिमेंट नहीं होता, बिल्कुल ऐरे की तरह।

# --instructions--

`"red"`, `"green"` और `"blue"` वाला एक स्ट्रिंग का constant set `colors` घोषित करें, और इंटीजर का एक खाली variable set `scores` घोषित करें

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`colors` में ठीक `"red"`, `"green"` और `"blue"` होने चाहिए

```swift
tryCatch(colors == ["red", "green", "blue"])
```

`colors` एक `Set<String>` होना चाहिए

```swift
tryCatch(type(of: colors) == Set<String>.self)
```

`scores` खाली होना चाहिए

```swift
tryCatch(scores.isEmpty)
```

`scores` एक `Set<Int>` होना चाहिए

```swift
tryCatch(type(of: scores) == Set<Int>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let colors: Set<String> = ["red", "green", "blue"]
var scores = Set<Int>()
```
