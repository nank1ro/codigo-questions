---
language: swift
exerciseType: 1
---

# --description--

दो और ऑपरेशन इस समूह को पूरा करते हैं:
- `a.subtracting(b)` में `a` के वे एलिमेंट होते हैं जो `b` में **नहीं** हैं
- `a.symmetricDifference(b)` में वे एलिमेंट होते हैं जो `a` या `b` में हों, लेकिन **दोनों में नहीं**
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.subtracting(b).sorted())          // [1, 2]
print(a.symmetricDifference(b).sorted())  // [1, 2, 4]
```
`union` और `intersection` के विपरीत, `subtracting` सममित (symmetric) नहीं है: `a.subtracting(b)` और `b.subtracting(a)` आमतौर पर अलग होते हैं।

# --instructions--

दो फ़ंक्शन लिखें जो दो `Set<String>` लेते हैं (argument labels छोड़ दें):
- `common` दोनों sets में मौजूद मानों का set लौटाता है
- `onlyInFirst` पहले set में मौजूद लेकिन दूसरे में नहीं मौजूद मानों का set लौटाता है

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`common(["a", "b", "c"], ["b", "c", "d"])` को `["b", "c"]` लौटाना चाहिए

```swift
tryCatch(common(["a", "b", "c"], ["b", "c", "d"]) == ["b", "c"])
```

`common(["a"], ["b"])` को एक खाली set लौटाना चाहिए

```swift
tryCatch(common(["a"], ["b"]).isEmpty)
```

`onlyInFirst(["a", "b", "c"], ["b", "c", "d"])` को `["a"]` लौटाना चाहिए

```swift
tryCatch(onlyInFirst(["a", "b", "c"], ["b", "c", "d"]) == ["a"])
```

`onlyInFirst(["x", "y"], ["z"])` को `["x", "y"]` लौटाना चाहिए

```swift
tryCatch(onlyInFirst(["x", "y"], ["z"]) == ["x", "y"])
```

दोनों फ़ंक्शन को एक `Set<String>` लौटाना चाहिए

```swift
tryCatch(type(of: common(["a"], ["a"])) == Set<String>.self && type(of: onlyInFirst(["a"], ["a"])) == Set<String>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func common(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.intersection(second)
}

func onlyInFirst(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.subtracting(second)
}
```
