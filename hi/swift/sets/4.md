---
language: swift
exerciseType: 2
---

# --description--

यह जांचने के लिए कि कोई मान set में है या नहीं, `contains(_:)` मेथड का उपयोग करें, जो एक `Bool` लौटाता है:
```swift
let primes: Set<Int> = [2, 3, 5, 7]
print(primes.contains(5)) // true
print(primes.contains(6)) // false
```
यह जांच set पर बहुत तेज़ होती है, भले ही उसमें हजारों एलिमेंट हों, और membership जांचने के लिए ऐरे की जगह set पसंद करने का यह एक मुख्य कारण है।

# --instructions--

अगर `role` अनुमति प्राप्त roles में से एक है तो `Welcome` प्रिंट करें, अन्यथा `Access denied` प्रिंट करें

# --seed--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed[/]([/]) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --answers--

- .contains
- .has
- .includes
- role
- "role"
- allowed

# --solutions--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed.contains(role) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --output--

Welcome
