---
language: swift
exerciseType: 1
---

# --instructions--

वेरिएबल `power` में मान `true` स्टोर करें

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`power` का मान `true` के बराबर होना चाहिए।

```swift
    func test1() {
        XCTAssertEqual(power, true, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
let power = true;
```
