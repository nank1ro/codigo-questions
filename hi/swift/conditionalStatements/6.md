---
language: swift
exerciseType: 2
---

# --description--

हम एक कंडीशनल स्टेटमेंट (`if`, `else if` या `else`) को दूसरे कंडीशनल स्टेटमेंट के अंदर नेस्ट भी कर सकते हैं, एक अधिक जटिल संरचना बनाने के लिए।
```swift
var num = 4
if num < 3 {
    print("the number is lower than 3")
} else {
    if num == 3 {
        print("the number is 3")
    } else if num == 4 {
        print("the number is 4")
    } else {
        print("the number is greather than 4")
    }
}
```
और इस कोड का आउटपुट `the number is 4` है।

# --instructions--

दिन के समय के प्रोग्राम को बदलते हैं, `else` स्टेटमेंट के अंदर एक कंडीशनल स्टेटमेंट नेस्ट करते हैं।
नेस्टेड कंडीशनल स्टेटमेंट को जांचना चाहिए कि `time` `18` से *कम* है या नहीं और कोड ब्लॉक में `"It's afternoon"` प्रिंट करना चाहिए।
यदि पिछली शर्त `false` है, तो `"It's evening"` प्रिंट करें

# --seed--

```swift
var time = 19
if time > 0 && time < 12 {
    print("It's morning")
} else if time == 12 {
    print("It's midday")
} else if time == 0 {
    print("It's midnight")
} else {
    [/][/][/][/] {
        print("It's afternoon")
    } [/] {
        print("It's evening")
    }
}
```

# --answers--

- 6
- elif 
-  < 
-  == 
- :
- ;
- time
- else if:
- elseif:
- if 
- else

# --solutions--

```swift
var time = 19
if time > 0 && time < 12 {
    print("It's morning")
} else if time == 12 {
    print("It's midday")
} else if time == 0 {
    print("It's midnight")
} else {
    if time < 6 {
        print("It's afternoon")
    } else {
        print("It's evening")
    }
}
```

# --output--

It's evening
