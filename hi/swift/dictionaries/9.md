---
language: swift
exerciseType: 2
---

# --description--

हम arrays के साथ उपयोग की गई `isEmpty` property का उपयोग यह निर्धारित करने के लिए भी कर सकते हैं कि dictionary खाली है या नहीं

# --instructions--

जाँचें कि `user` dictionary खाली है या नहीं

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if ([/][/][/]) {
    print("user dictionary is empty")
} else {
    print("user dictionary is NOT empty")
}
```

# --answers--

- user
- .
- isEmpty
- isEmpty()
- is_empty

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if (user.isEmpty) {
    print("user dictionary is empty")
} else {
    print("user dictionary is NOT empty")
}
```

# --output--

user dictionary is NOT empty
