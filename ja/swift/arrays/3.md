---
language: swift
exerciseType: 2
---

# --description--

配列のインデックスは他の変数名と同じように動作します。
値のアクセスだけでなく、値の代入にも使用できます。
配列のインデックスにアクセスする方法は次の通りです：
```swift
var names = ["Jeremiah", "Barney", "Ivan", "Noel"]
// 値 "Jeremiah" を出力
print(names[0])
```
代入は次のように行います：
```swift
var names = ["Jeremiah", "Barney", "Ivan", "Noel"]
// 新しい値 "Jordan" を代入
names[0] = "Jordan"
// 値 "Jordan" を出力
print(names[0])
```

# --instructions--

現在`"Mia"`という値を持つ要素を`"Sophie"`という名前に置き換えるためにコードを完成させてください

# --seed--

```swift
var names = ["Charlotte", "Mia", "Jessica", "Hollie"]
[/][/] = "Sophie"
for name in names {
    print(name)
}
```

# --answers--

- [0]
- [1]
- [2]
- [3]
- [4]
- names

# --solutions--

```swift
var names = ["Charlotte", "Mia", "Jessica", "Hollie"]
names[1] = "Sophie"
for name in names {
    print(name)
}
```

# --output--

Charlotte
Sophie
Jessica
Hollie
