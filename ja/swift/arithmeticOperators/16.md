---
language: swift
exerciseType: 1
---

# --instructions--

新しい変数 `revenue` を作成し、`earnings` と `costs` の差を代入して、その値を出力してください

# --seed--

```swift
var earnings = 2000
var costs = 750
```

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`revenue` は `earnings` と `costs` の差と等しくなければなりません。

```swift
    func test1() {
        XCTAssertEqual(revenue, earnings - costs, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
var earnings = 2000
var costs = 750
var revenue = earnings - costs;
```

# --output--

1250
