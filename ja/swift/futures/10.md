---
language: swift
exerciseType: 1
---

# --description--

`Task.sleep`は他の何もブロックせずに、現在のタスクをしばらく一時停止させます。中断される可能性があるため、スローする非同期呼び出しであり、`try await`が必要です。時間は`.seconds`、`.milliseconds`、`.nanoseconds`などのヘルパーで指定します:
```swift
func slowGreeting() async throws -> String {
    try await Task.sleep(for: .milliseconds(50))
    return "hello"
}
```
実際のネットワーク呼び出しの代わりに、例の中で遅い処理をシミュレートする標準的な方法です。プログラムが固まるわけではない点に注意してください。1つのタスクがスリープしている間も、他のタスクは実行を続けます。

# --instructions--

整数を引数ラベルなしで受け取り、非同期で、スローでき、`Task.sleep`で`50`ミリ秒待ってから、その数を`3`倍して返す関数`delayed`を書いてください

関数呼び出しの例:
```swift
print(try await delayed(2))
// prints 6
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func delayed() {

}
```

# --asserts--

関数は`async throws`が付けられていなければなりません

```json
{
  "regex": "func\\s+delayed\\s*\\([^)]*\\)\\s*async\\s+throws",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

関数は`Task.sleep`を使用しなければなりません

```json
{
  "regex": "Task\\.sleep",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`delayed(2)`は`6`を返さなければなりません

```swift
tryCatch((try? await delayed(2)) == 6)
```

`delayed(0)`は`0`を返さなければなりません

```swift
tryCatch((try? await delayed(0)) == 0)
```

`delayed(-4)`は`-12`を返さなければなりません

```swift
tryCatch((try? await delayed(-4)) == -12)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func delayed(_ n: Int) async throws -> Int {
    try await Task.sleep(for: .milliseconds(50))
    return n * 3
}
```
