---
language: swift
exerciseType: 4
---

# --description--

続けて書かれた複数の`await`呼び出しは**順次**実行されます。1番目の呼び出しが返るまで、2番目の呼び出しは開始さえしません。コードは上から下へ、普通のコードとまったく同じように読めます:
```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}

let a = await step("A")
print(a)
let b = await step("B")
print(b)
// start A
// done A
// start B
// done B
```
2番目の呼び出しが1番目の結果を必要とするなら、これが望ましい動作です。呼び出しが互いに独立している場合は、片方を開始する前にもう片方を待つのは時間の無駄です。次の演習ではその避け方を紹介します。

# --instructions--

トップレベルのコードの前に`step`を定義し、下の出力を出力するように行を並べ替えてください
```
start A
done A
start B
done B
```

# --answers--

```swift
func step(_ name: String) async -> String {
```

```swift
    print("start \(name)")
```

```swift
    return "done \(name)"
```

```swift
}
```

```swift
let a = await step("A")
```

```swift
print(a)
```

```swift
let b = await step("B")
```

```swift
print(b)
```

# --solutions--

```swift
func step(_ name: String) async -> String {
    print("start \(name)")
    return "done \(name)"
}
let a = await step("A")
print(a)
let b = await step("B")
print(b)
```

# --output--

start A
done A
start B
done B
