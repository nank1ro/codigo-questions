---
language: swift
exerciseType: 1
---

# --description--

2つの独立した呼び出しを同時に実行するには、**`async let`**で宣言します。処理はすぐに開始され、プログラムは待たずに先へ進みます:
```swift
async let left = step("A")
async let right = step("B")
```
値はまだそこにないので、バインディングを直接使うことはできません。最終的に必要になる位置で`await`しなければなりません。式の前の1つの`await`で、その中のすべての`async let`をカバーします:
```swift
let both = await left + right
```
各呼び出しに1秒かかるなら、順次バージョンは2秒かかりますが、`async let`バージョンは2つの呼び出しが重なるため約1秒で済みます。

# --instructions--

2つの整数を引数ラベルなしで受け取り、非同期で、両方に適用した`slowValue`の合計を返す関数`total`を書いてください。2つの呼び出しは`async let`で並行して開始してください

関数呼び出しの例:
```swift
print(await total(1, 2))
// 30 を出力
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total() {

}
```

# --asserts--

関数は`async let`を使用しなければなりません

```json
{
  "regex": "async\\s+let",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`total(1, 2)`は`30`を返さなければなりません

```swift
tryCatch(await total(1, 2) == 30)
```

`total(0, 0)`は`0`を返さなければなりません

```swift
tryCatch(await total(0, 0) == 0)
```

`total(5, -2)`は`30`を返さなければなりません

```swift
tryCatch(await total(5, -2) == 30)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func slowValue(_ n: Int) async -> Int {
    return n * 10
}

func total(_ a: Int, _ b: Int) async -> Int {
    async let first = slowValue(a)
    async let second = slowValue(b)
    return await first + second
}
```
