---
language: swift
exerciseType: 4
---

# --description--

定数に格納されたクロージャは、パラメータの引数ラベルを使って、クロージャの引数が期待される場所ならどこにでも渡すことができます:
```swift
let ascending = { (a: Int, b: Int) -> Bool in a < b }
print([3, 1, 2].sorted(by: ascending)) // [1, 2, 3]
```

# --instructions--

項目を並べ替えて、2つの文字列を短いものから長いものへと並べるクロージャを`byLength`に格納し、それを使って`words`をソートして出力してください。
`words`を`byLength`より前に宣言してください

# --answers--

```swift
let words = ["kiwi", "fig", "banana"]
```

```swift
let byLength = { (a: String, b: String) -> Bool in
```

```swift
    a.count < b.count
```

```swift
}
```

```swift
print(words.sorted(by: byLength))
```

# --solutions--

```swift
let words = ["kiwi", "fig", "banana"]
let byLength = { (a: String, b: String) -> Bool in
    a.count < b.count
}
print(words.sorted(by: byLength))
```

# --output--

["fig", "kiwi", "banana"]
