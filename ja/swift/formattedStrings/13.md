---
language: swift
exerciseType: 2
---

# --description--

Foundationには右側のパディングのための既成のヘルパーもあります: `padding(toLength:withPad:startingAt:)`です。これはパッドテキストを繰り返して文字列を指定の長さまで伸ばし、長すぎる場合は切り詰めます:
```swift
import Foundation

let name = "Ada"
print(name.padding(toLength: 8, withPad: " ", startingAt: 0) + "|") // Ada     |
print("Tea".padding(toLength: 6, withPad: ".", startingAt: 0))     // Tea...
```
`startingAt`はパッドテキスト内で繰り返しが始まるインデックスです。1文字のパッドでは常に`0`です。

# --instructions--

`name`がスペースで埋められた`8`文字のフィールド内で左揃えになり、プログラムが`Ada     |`を出力するようにコードを完成させてください

# --seed--

```swift
import Foundation

let name = "Ada"
print(name.[/](toLength: 8, [/]: " ", startingAt: 0) + "|")
```

# --answers--

- padding
- withPad
- padded
- pad
- withText
- withSpaces

# --solutions--

```swift
import Foundation

let name = "Ada"
print(name.padding(toLength: 8, withPad: " ", startingAt: 0) + "|")
```

# --output--

Ada     |
