---
language: swift
exerciseType: 1
---

# --description--

もう1つのフラグは`0`です。スペースの代わりに、フィールドが左側からゼロで埋められます。これが`007`や`00042`のような数値を作る方法です:
```swift
print(String(format: "%05d", 42))  // 00042
print(String(format: "%03d", 7))   // 007
print(String(format: "%03d", 1234)) // 1234
```
スペースの場合と同様に、幅より長い値が切り詰められることはありません。

# --instructions--

引数ラベルなしで整数を受け取り、`String(format:)`を使って、`T-`の後に幅`5`までゼロで埋めた数値を付けた文字列を返す関数`ticket`を書いてください

関数呼び出しの例:
```swift
print(ticket(42))
// prints T-00042
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func ticket() {

}
```

# --asserts--

関数は`String(format:`を使用しなければなりません

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ticket(42)`は`"T-00042"`を返さなければなりません

```swift
tryCatch(ticket(42) == "T-00042")
```

`ticket(7)`は`"T-00007"`を返さなければなりません

```swift
tryCatch(ticket(7) == "T-00007")
```

`ticket(12345)`は`"T-12345"`を返さなければなりません

```swift
tryCatch(ticket(12345) == "T-12345")
```

`ticket(123456)`は`"T-123456"`を返さなければなりません

```swift
tryCatch(ticket(123456) == "T-123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func ticket(_ n: Int) -> String {
    return String(format: "T-%05d", n)
}
```
