---
language: swift
exerciseType: 2
---

# --description--

小数（`Double`）の書式指定子は`%f`です。これ単体では小数点以下が常に6桁で出力されます:
```swift
print(String(format: "%f", 3.5)) // 3.500000
```
小数の桁数を選ぶには、`%`と`f`の間にドットと数値を書きます。これが**精度**であり、値はそれに合わせて丸められます:
```swift
print(String(format: "%.2f", 3.5))     // 3.50
print(String(format: "%.1f", 3.14159)) // 3.1
print(String(format: "%.0f", 2.71))    // 3
```
`%.2f`は常にちょうど2桁の小数を表示するため、価格にはよく使われます。

# --instructions--

プログラムが`Price: 9.50`を出力するようにフォーマット文字列を完成させてください

# --seed--

```swift
import Foundation

let price = 9.5
print(String(format: "Price: [/]", price))
```

# --answers--

- %.2f
- %f
- %d
- %.1f
- %2f

# --solutions--

```swift
import Foundation

let price = 9.5
print(String(format: "Price: %.2f", price))
```

# --output--

Price: 9.50
