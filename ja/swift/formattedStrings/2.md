---
language: swift
exerciseType: 1
---

# --description--

2つ目の手段は`String(format:)`です。これは**Foundation**フレームワークに含まれるため、ファイルは`import Foundation`で始まる必要があります。

挿入する値を**フォーマット文字列**の後に渡します。フォーマット文字列の中では、`%`で始まる**書式指定子**が各値の場所とその書き方を示します。整数の書式指定子は`%d`です:
```swift
import Foundation

let count = 7
let text = String(format: "Item %d", count)
print(text) // Item 7
```
`String(format:)`は通常の`String`を返すので、出力したり、変数に格納したり、関数から返したりできます。

# --instructions--

引数ラベルなしで整数を受け取り、`%d`付きの`String(format:)`を使って、数字の前に`Item `というテキストを付けた文字列を返す関数`label`を書いてください

関数呼び出しの例:
```swift
print(label(7))
// prints Item 7
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func label() {

}
```

# --asserts--

関数は`String(format:`を使用しなければなりません

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`label(7)`は`"Item 7"`を返さなければなりません

```swift
tryCatch(label(7) == "Item 7")
```

`label(42)`は`"Item 42"`を返さなければなりません

```swift
tryCatch(label(42) == "Item 42")
```

`label(0)`は`"Item 0"`を返さなければなりません

```swift
tryCatch(label(0) == "Item 0")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func label(_ n: Int) -> String {
    return String(format: "Item %d", n)
}
```
