---
language: swift
exerciseType: 1
---

# --description--

`%`と文字の間の数値はフィールドの**最小幅**を設定します。値が短ければ左側にスペースが追加されて**右揃え**になり、長ければ何も切り詰められません:
```swift
print(String(format: "%5d|", 42))    //    42|
print(String(format: "%5d|", 12345)) // 12345|
```
幅と精度は組み合わせられます。`%8.2f`は「幅が最低8文字で小数2桁」という意味です:
```swift
print(String(format: "%8.2f|", 3.14159)) //     3.14|
```
固定幅こそがテーブルの列を揃える仕組みです。

# --instructions--

引数ラベルなしで整数を受け取り、`String(format:)`を使って幅`5`のフィールド内で右揃えにした文字列として返す関数`rightAligned`を書いてください

関数呼び出しの例:
```swift
print(rightAligned(42) + "|")
// prints    42|
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func rightAligned() {

}
```

# --asserts--

関数は`String(format:`を使用しなければなりません

```json
{
  "regex": "String\\(format:",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`rightAligned(42)`は`"   42"`を返さなければなりません

```swift
tryCatch(rightAligned(42) == "   42")
```

`rightAligned(7)`は`"    7"`を返さなければなりません

```swift
tryCatch(rightAligned(7) == "    7")
```

`rightAligned(12345)`は`"12345"`を返さなければなりません

```swift
tryCatch(rightAligned(12345) == "12345")
```

`rightAligned(123456)`は`"123456"`を返さなければなりません（何も切り詰められません）

```swift
tryCatch(rightAligned(123456) == "123456")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func rightAligned(_ n: Int) -> String {
    return String(format: "%5d", n)
}
```
