---
language: swift
exerciseType: 2
---

# --description--

クラスが独自の関数を持つ場合、それらの関数は__メソッド__と呼ばれます。

# --instructions--

`Animal`クラスに`description`というメソッドを追加してください。
2つの別々の`print`文を使って、呼び出されたアニマルの`gender`と`legs`を出力する必要があります。
その後、`description`メソッドを呼び出してください。

# --seed--

```swift
class Animal {
    var gender: String?
    var legs = 4

    [/][/]() {
        if let gender = self.gender { print(gender) }
        print([/].legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.[/]
```

# --answers--

-  description
- func
- description()
- self
- description[]
- function
- method

# --solutions--

```swift
class Animal {
    var gender: String?
    var legs = 4

    func description() {
        if let gender = self.gender { print(gender) }
        print(self.legs)
    }
}
let dog = Animal()
dog.gender = "female"
dog.description()
```

# --output--

female
4
