---
language: swift
exerciseType: 1
---

# --instructions--

2つの文字列配列`today`と`yesterday`を受け取り（引数ラベルは省略）、`today`には存在するが`yesterday`には存在しない名前を、重複なく昇順で返す関数`newcomers`を書いてください

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`newcomers(["ada", "bob", "ada", "cy"], ["bob"])`は`["ada", "cy"]`を返さなければなりません

```swift
tryCatch(newcomers(["ada", "bob", "ada", "cy"], ["bob"]) == ["ada", "cy"])
```

`newcomers(["zed", "amy"], [])`は`["amy", "zed"]`を返さなければなりません

```swift
tryCatch(newcomers(["zed", "amy"], []) == ["amy", "zed"])
```

`newcomers(["ada", "bob"], ["bob", "ada", "cy"])`は空の配列を返さなければなりません

```swift
tryCatch(newcomers(["ada", "bob"], ["bob", "ada", "cy"]) == [])
```

この関数は文字列の配列を返さなければなりません

```swift
tryCatch(type(of: newcomers(["a"], ["b"])) == [String].self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func newcomers(_ today: [String], _ yesterday: [String]) -> [String] {
    return Set(today).subtracting(yesterday).sorted()
}
```
