---
language: swift
exerciseType: 2
---

# --description--

値がセットに含まれているかを確認するには、`Bool`を返す`contains(_:)`メソッドを使います：
```swift
let primes: Set<Int> = [2, 3, 5, 7]
print(primes.contains(5)) // true
print(primes.contains(6)) // false
```
この確認は、要素が数千個あってもセットでは非常に高速です。これは、値の存在確認において配列よりセットを優先すべき主な理由の1つです。

# --instructions--

`role`が許可されたロールのいずれかであれば`Welcome`を、そうでなければ`Access denied`を出力してください

# --seed--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed[/]([/]) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --answers--

- .contains
- .has
- .includes
- role
- "role"
- allowed

# --solutions--

```swift
let allowed: Set<String> = ["admin", "editor"]
let role = "editor"
if allowed.contains(role) {
    print("Welcome")
} else {
    print("Access denied")
}
```

# --output--

Welcome
