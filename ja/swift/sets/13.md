---
language: swift
exerciseType: 1
---

# --instructions--

2つの`Set<String>`を受け取り（引数ラベルは省略）、`Bool`を返す2つの関数を書いてください：
- `canAccess`は必要な権限と付与された権限を受け取り、必要な権限がすべて付与されている場合にのみ`true`を返します
- `noOverlap`は、2つのセットに共通する権限がない場合に`true`を返します

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`canAccess(["read"], ["read", "write"])`は`true`を返さなければなりません

```swift
tryCatch(canAccess(["read"], ["read", "write"]) == true)
```

`canAccess(["read", "delete"], ["read", "write"])`は`false`を返さなければなりません

```swift
tryCatch(canAccess(["read", "delete"], ["read", "write"]) == false)
```

`canAccess([], ["read"])`は`true`を返さなければなりません

```swift
tryCatch(canAccess([], ["read"]) == true)
```

`noOverlap(["read"], ["write"])`は`true`を返さなければなりません

```swift
tryCatch(noOverlap(["read"], ["write"]) == true)
```

`noOverlap(["read", "write"], ["write"])`は`false`を返さなければなりません

```swift
tryCatch(noOverlap(["read", "write"], ["write"]) == false)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func canAccess(_ required: Set<String>, _ granted: Set<String>) -> Bool {
    return required.isSubset(of: granted)
}

func noOverlap(_ first: Set<String>, _ second: Set<String>) -> Bool {
    return first.isDisjoint(with: second)
}
```
