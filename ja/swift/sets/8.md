---
language: swift
exerciseType: 4
---

# --description--

セットは集合論の古典的な演算をサポートします。それぞれが**新しい**セットを返し、元のセットは変更されません：
- `a.union(b)`（和集合）は、`a`、`b`、またはその両方に含まれるすべての要素を含みます
- `a.intersection(b)`（積集合）は、`a`と`b`の**両方**に含まれる要素のみを含みます
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.union(b).sorted())        // [1, 2, 3, 4]
print(a.intersection(b).sorted()) // [3]
```

# --instructions--

`odds`または`primes`に含まれるすべての数値を昇順で出力するように項目を並べ替えてください。
`primes`より先に`odds`を宣言してください

# --answers--

```swift
let odds: Set<Int> = [1, 3, 5]
```

```swift
let primes: Set<Int> = [2, 3, 5]
```

```swift
let all = odds.union(primes)
```

```swift
print(all.sorted())
```

# --solutions--

```swift
let odds: Set<Int> = [1, 3, 5]
let primes: Set<Int> = [2, 3, 5]
let all = odds.union(primes)
print(all.sorted())
```

# --output--

[1, 2, 3, 5]
