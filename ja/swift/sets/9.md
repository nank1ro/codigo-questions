---
language: swift
exerciseType: 1
---

# --description--

さらに2つの演算があり、これで演算の一群が揃います：
- `a.subtracting(b)`（差集合）は、`a`の要素のうち`b`に**含まれない**ものを含みます
- `a.symmetricDifference(b)`（対称差）は、`a`または`b`のどちらか一方にのみ含まれる要素、つまり**両方には含まれない**要素を含みます
```swift
let a: Set<Int> = [1, 2, 3]
let b: Set<Int> = [3, 4]
print(a.subtracting(b).sorted())          // [1, 2]
print(a.symmetricDifference(b).sorted())  // [1, 2, 4]
```
`union`や`intersection`とは異なり、`subtracting`は対称ではありません：`a.subtracting(b)`と`b.subtracting(a)`は通常異なります。

# --instructions--

2つの`Set<String>`を受け取る（引数ラベルは省略）2つの関数を書いてください：
- `common`は両方のセットに存在する値のセットを返します
- `onlyInFirst`は1つ目のセットには存在するが2つ目のセットには存在しない値のセットを返します

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`common(["a", "b", "c"], ["b", "c", "d"])`は`["b", "c"]`を返さなければなりません

```swift
tryCatch(common(["a", "b", "c"], ["b", "c", "d"]) == ["b", "c"])
```

`common(["a"], ["b"])`は空のセットを返さなければなりません

```swift
tryCatch(common(["a"], ["b"]).isEmpty)
```

`onlyInFirst(["a", "b", "c"], ["b", "c", "d"])`は`["a"]`を返さなければなりません

```swift
tryCatch(onlyInFirst(["a", "b", "c"], ["b", "c", "d"]) == ["a"])
```

`onlyInFirst(["x", "y"], ["z"])`は`["x", "y"]`を返さなければなりません

```swift
tryCatch(onlyInFirst(["x", "y"], ["z"]) == ["x", "y"])
```

両方の関数は`Set<String>`を返さなければなりません

```swift
tryCatch(type(of: common(["a"], ["a"])) == Set<String>.self && type(of: onlyInFirst(["a"], ["a"])) == Set<String>.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func common(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.intersection(second)
}

func onlyInFirst(_ first: Set<String>, _ second: Set<String>) -> Set<String> {
    return first.subtracting(second)
}
```
