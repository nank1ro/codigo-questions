---
language: swift
exerciseType: 1
---

# --description--

**文字列補間**は、式の値を文字列リテラルの中に挿入します。式は`\()`で囲みます:
```swift
let name = "Ada"
let age = 36
print("\(name) is \(age) years old") // Ada is 36 years old
```
どんな型でも補間できます。数値、ブーリアン、他の文字列はすべて自動的にテキストに変換されます。

# --instructions--

`String`を受け取る（引数ラベルは省略する）関数`greet`を書いてください。この関数はテキスト`Hello, <name>!`を返します。ここで`<name>`は引数です。

関数呼び出しの例:
```swift
print(greet("Ada"))
// prints Hello, Ada!
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`greet("Ada")`は`"Hello, Ada!"`を返さなければなりません

```swift
tryCatch(greet("Ada") == "Hello, Ada!")
```

`greet("Swift")`は`"Hello, Swift!"`を返さなければなりません

```swift
tryCatch(greet("Swift") == "Hello, Swift!")
```

`greet("")`は`"Hello, !"`を返さなければなりません

```swift
tryCatch(greet("") == "Hello, !")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func greet(_ name: String) -> String {
    return "Hello, \(name)!"
}
```
