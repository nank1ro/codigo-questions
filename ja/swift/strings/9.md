---
language: swift
exerciseType: 1
---

# --description--

インデックスを扱うのは冗長なので、Swiftはよくあるケース向けのショートカットを用意しています:
- `first`と`last`は最初と最後の文字をオプショナルの`Character?`として返します（空文字列の場合は`nil`）
- `prefix(n)`は最初の`n`文字を、`suffix(n)`は最後の`n`文字を返します
```swift
let word = "Swift"
print(word.first!)     // S
print(word.prefix(2))  // Sw
print(word.suffix(3))  // ift
```
`prefix`と`suffix`は、元のテキストのビューである`Substring`を返します。本物の`String`として保存するには`String(...)`で包んでください。`n`が`count`より大きい場合は、単に文字列全体が得られます。

# --instructions--

`String`を受け取り（引数ラベルは省略する）その最初の2文字を`String`として返す関数`firstTwo`と、最後の2文字を`String`として返す関数`lastTwo`を書いてください

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`firstTwo("Swift")`は`"Sw"`を返さなければなりません

```swift
tryCatch(firstTwo("Swift") == "Sw")
```

`firstTwo("a")`は`"a"`を返さなければなりません

```swift
tryCatch(firstTwo("a") == "a")
```

`lastTwo("Swift")`は`"ft"`を返さなければなりません

```swift
tryCatch(lastTwo("Swift") == "ft")
```

`lastTwo("")`は`""`を返さなければなりません

```swift
tryCatch(lastTwo("") == "")
```

`firstTwo`は`String`を返さなければなりません

```swift
tryCatch(type(of: firstTwo("Swift")) == String.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func firstTwo(_ text: String) -> String {
    return String(text.prefix(2))
}

func lastTwo(_ text: String) -> String {
    return String(text.suffix(2))
}
```
