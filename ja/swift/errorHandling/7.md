---
language: swift
exerciseType: 1
---

# --description--

バリデータは、拒否を先に書いて本題の処理を下部にインデントなしで残すと、最も読みやすくなります。`guard`はそのために作られています。成り立つべき条件を宣言し、成り立たないときに`else`ブロックが実行されます:
```swift
func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
`guard`の`else`ブロックは現在のスコープを抜けなければならず、`throw`は`return`、`break`、`continue`と並ぶ、そのための方法の1つです。関数の先頭に積み重ねられた複数の`guard`は、入力が満たすべきルールのリストのように読めます。

# --instructions--

`quantity`が`0`より大きくなければ`OrderError.notPositive`をスローする`guard`を使って、`priceFor`を完成させてください

関数呼び出しの例:
```swift
do {
    print(try priceFor(4))
} catch {
    print("invalid")
}
// 12 を出力
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    
    return quantity * 3
}
```

# --before-asserts--

```swift
func _price(_ quantity: Int) -> Int {
    do {
        return try priceFor(quantity)
    } catch OrderError.notPositive {
        return -1
    } catch {
        return -2
    }
}
```

# --asserts--

`priceFor(4)`は`12`を返さなければなりません

```swift
tryCatch(_price(4) == 12)
```

`priceFor(1)`は`3`を返さなければなりません

```swift
tryCatch(_price(1) == 3)
```

`priceFor(0)`は`notPositive`をスローしなければなりません

```swift
tryCatch(_price(0) == -1)
```

`priceFor(-5)`は`notPositive`をスローしなければなりません

```swift
tryCatch(_price(-5) == -1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum OrderError: Error {
    case notPositive
}

func priceFor(_ quantity: Int) throws -> Int {
    guard quantity > 0 else {
        throw OrderError.notPositive
    }
    return quantity * 3
}
```
