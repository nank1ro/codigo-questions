---
language: swift
exerciseType: 1
---

# --description--

関数がスローすると、`throw`の後のすべてがスキップされます。ファイルを閉じるはずだった行やロックを解放するはずだった行も含めてです。`defer`はそれを解決します。ブロックをその場で登録し、現在のスコープがどんな形で終わっても、終わったときに実行します。
```swift
func load() throws {
    print("open")
    defer { print("close") }
    throw FileError.missing
}
```
呼び出しは`open`を出力し、次に`close`を出力し、その後に初めてエラーが呼び出し元へ伝わります。関数が普通に返っていたとしても、`close`は出力されていたはずです。それがポイントです。クリーンアップをセットアップのすぐ隣に置き、コードがどの出口を通るかを心配するのをやめましょう。

# --instructions--

`run`の中に`log`へ`"cleanup"`を追加する`defer`ブロックを付け、`run`が普通に返るときにもスローするときにも実行されるようにしてください

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
enum TaskError: Error {
    case failed
}

func steps(_ shouldFail: Bool) -> [String] {
    var log: [String] = []
    func run() throws {
        log.append("start")
        
        if shouldFail {
            throw TaskError.failed
        }
        log.append("done")
    }
    do {
        try run()
    } catch {
        log.append("caught")
    }
    return log
}
```

# --asserts--

成功のパスでは、ログは`start`、`done`、`cleanup`でなければなりません

```swift
let _ok: [String] = steps(false)
tryCatch(_ok == ["start", "done", "cleanup"])
```

スローのパスでは、ログは`start`、`cleanup`、`caught`でなければなりません

```swift
let _bad: [String] = steps(true)
tryCatch(_bad == ["start", "cleanup", "caught"])
```

クリーンアップは両方のパスで実行されなければなりません

```swift
tryCatch(steps(false).contains("cleanup") && steps(true).contains("cleanup"))
```

クリーンアップはスローのパスでちょうど1回実行されなければなりません

```swift
let _once: [String] = steps(true)
tryCatch(_once.filter { $0 == "cleanup" }.count == 1)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
enum TaskError: Error {
    case failed
}

func steps(_ shouldFail: Bool) -> [String] {
    var log: [String] = []
    func run() throws {
        log.append("start")
        defer { log.append("cleanup") }
        if shouldFail {
            throw TaskError.failed
        }
        log.append("done")
    }
    do {
        try run()
    } catch {
        log.append("caught")
    }
    return log
}
```
