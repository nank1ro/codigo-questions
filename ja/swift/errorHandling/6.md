---
language: swift
exerciseType: 4
---

# --description--

1つの列挙型は通常、1つの処理が失敗しうるすべての方法を持ちます。理由ごとに1つのケースです:
```swift
enum FormError: Error {
    case empty
    case tooLong
}
```
ケースごとに`catch`を書くのは繰り返しになります。代わりに型ごとまとめてキャッチし、値に対して`switch`します:
```swift
} catch let error as FormError {
    switch error {
    case .empty: print("empty")
    case .tooLong: print("too long")
    }
} catch {
    print("unknown")
}
```
`catch let error as FormError`は*`FormError`であるものは何でもキャッチし、それを`error`と呼ぶ*という意味です。ブロックの中では`error`は列挙型を持つので、`switch`はケースを認識し、すべてカバーしているかチェックしてくれます。最後のパターンなしの`catch`も依然として必要です。他の型のエラーがこの`do`ブロックに届く可能性があるからです。

# --instructions--

エラー型を最初に宣言し、`do`ブロックの前に関数を置いて、`too long`を出力するようにアイテムを並べ替えてください

# --answers--

```swift
enum FormError: Error {
    case empty
    case tooLong
}
```

```swift
func check(_ text: String) throws {
    if text.isEmpty {
        throw FormError.empty
    }
    if text.count > 5 {
        throw FormError.tooLong
    }
}
```

```swift
do {
    try check("abcdefgh")
} catch let error as FormError {
```

```swift
    switch error {
    case .empty: print("empty")
    case .tooLong: print("too long")
    }
} catch {
    print("unknown")
}
```

# --solutions--

```swift
enum FormError: Error {
    case empty
    case tooLong
}
func check(_ text: String) throws {
    if text.isEmpty {
        throw FormError.empty
    }
    if text.count > 5 {
        throw FormError.tooLong
    }
}
do {
    try check("abcdefgh")
} catch let error as FormError {
    switch error {
    case .empty: print("empty")
    case .tooLong: print("too long")
    }
} catch {
    print("unknown")
}
```

# --output--

too long
