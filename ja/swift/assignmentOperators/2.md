---
language: swift
exerciseType: 2
---

# --description--

初期化済みの変数 `total` があるとします
```swift
var total = 5
```
変数 `total` に数値 `2` を加えたい場合、次のように書くことができます
```swift
total = total + 2
```
これで動きます！しかし、同じことをより短く書く方法があります：
```swift
total += 2
```
`+=` 記号は**加算代入**と呼ばれます。
変数の値に値を加算し、その結果をその変数に代入します。

# --instructions--

*加算代入*演算子を使って変数 `num` に値 `15` を加算し、その値を出力してください

# --seed--

```swift
var num = 75
[/][/][/]
print(num)
```

# --answers--

- +
- +=
- =+
- num 
-  15

# --solutions--

```swift
var num = 75
num += 15
print(num)
```

# --output--

90
