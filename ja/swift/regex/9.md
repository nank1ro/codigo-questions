---
language: swift
exerciseType: 2
---

# --description--

パターンの一部を丸括弧で囲むと**キャプチャグループ**が作られます。マッチ全体はこれまでどおり`m.0`で、括弧の中の部分が`m.1`になります:
```swift
let text = "id-42"
if let m = text.firstMatch(of: #/id-(\d+)/#) {
    print(m.0) // id-42
    print(m.1) // 42
}
```
こうして必要な部分を取り出し、周りのテキストを捨てます。括弧がないと`m.1`はまったく存在せず、コードはコンパイルできません。

# --instructions--

`id-`の後の数字だけを取り出して`42`という出力になるように、コードを完成させてください

# --seed--

```swift
let text = "id-42"
if let m = text.firstMatch(of: #/id-([/]+)/#) {
    print(m.[/])
}
```

# --answers--

- \d
- \s
- [a-z]
- [A-Z]
- 0
- 1

# --solutions--

```swift
let text = "id-42"
if let m = text.firstMatch(of: #/id-(\d+)/#) {
    print(m.1)
}
```

# --output--

42
