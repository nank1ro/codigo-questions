---
language: swift
exerciseType: 4
---

# --description--

`^`と`$`でテキスト全体を検証しても動きますが、`wholeMatch(of:)`はそれを直接表します。パターンがテキストの最初の文字から最後の文字までを覆うときにだけマッチを返し、それ以外は`nil`を返します:
```swift
print("1a2b".wholeMatch(of: #/[0-9a-f]+/#) != nil) // true
print("1z".wholeMatch(of: #/[0-9a-f]+/#) != nil)   // false
```
テキストの中から何かを見つけるときは`firstMatch(of:)`を、テキストが決まった1つの形を持つかを調べるときは`wholeMatch(of:)`を使います。

# --instructions--

アイテムを並べ替えて、ループの前に`isHex`を定義し、`values`のすべての値について結果を出力するプログラムを作ってください

# --answers--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
```

```swift
let values = ["1a2b", "1z", "ff"]
```

```swift
for value in values {
    print(isHex(value))
}
```

# --solutions--

```swift
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
let values = ["1a2b", "1z", "ff"]
for value in values {
    print(isHex(value))
}
```

```swift
let values = ["1a2b", "1z", "ff"]
func isHex(_ text: String) -> Bool {
    return text.wholeMatch(of: #/[0-9a-f]+/#) != nil
}
for value in values {
    print(isHex(value))
}
```

# --output--

true
false
true
