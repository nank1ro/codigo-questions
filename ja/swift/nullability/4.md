---
language: swift
exerciseType: 4
---

# --description--

強制アンラップは、オプショナルが`nil`でないことをすでに確認している場合にのみ安全です:
```swift
if score != nil {
    print(score! * 2)
}
```

# --instructions--

`price`が`nil`でないことを確認した後にのみ、それを2倍にして出力するようにアイテムを並べ替えてください

# --answers--

```swift
let price: Double? = 9.5
```

```swift
if price != nil {
```

```swift
    print(price! * 2)
```

```swift
}
```

# --solutions--

```swift
let price: Double? = 9.5
if price != nil {
    print(price! * 2)
}
```

# --output--

19.0
