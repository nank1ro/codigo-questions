---
language: swift
exerciseType: 2
---

# --description--

メソッドに加えて、拡張は**計算プロパティ**も追加できます。計算プロパティは値を格納せず、読み出されるたびに値を計算するプロパティです。
計算プロパティは`var`、型注釈、そして値を返す波括弧の本体を使って宣言します:
```swift
extension Int {
    var isNegative: Bool {
        return self < 0
    }
}
print((-3).isNegative) // true
print(7.isNegative)    // false
```
他のプロパティと同じように、括弧を付けずに読み取ります。`7.isNegative()`ではなく`7.isNegative`です。

# --instructions--

拡張を完成させて、`isEven`が数値が偶数かどうかを示す計算プロパティになるようにし、プログラムが`true`と`false`を出力するようにしてください

# --seed--

```swift
extension Int {
    [/] isEven: [/] {
        return self % 2 == 0
    }
}
print(4.isEven)
print(7.isEven)
```

# --answers--

- var
- Bool
- let
- func
- Int

# --solutions--

```swift
extension Int {
    var isEven: Bool {
        return self % 2 == 0
    }
}
print(4.isEven)
print(7.isEven)
```

# --output--

true
false
