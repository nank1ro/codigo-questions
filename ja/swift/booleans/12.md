---
language: swift
exerciseType: 1
---

# --instructions--

変数`isOn`に値`false`を格納してください

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`isOn`は`false`と等しくなければなりません。

```swift
    func test1() {
        XCTAssertEqual(isOn, false, "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
let isOn = false;
```
