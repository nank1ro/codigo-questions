---
language: swift
exerciseType: 1
---

# --instructions--

`amount`に値`"50 dollars"`を設定してください。

# --seed--

```swift
var amount = "20 dollars"
// write after this line
```

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`amount`は`"50 dollars"`と等しくなければなりません。

```swift
    func test1() {
        XCTAssertEqual(amount, "50 dollars", "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
var amount = "20 dollars"
amount = "50 dollars"
```
