---
language: swift
exerciseType: 1
---

# --description--

Swiftでは、文字列変数はダブルクォートのみを使用して宣言できます：
```swift
let x = "May"
```

# --instructions--

`month`という名前の変数を値`june`で宣言してください。

# --before-asserts--

```swift
import Foundation
import XCTest

class CodigoTests: XCTestCase {
```

# --asserts--

`month`は`"june"`と等しくなければなりません。

```swift
    func test1() {
        XCTAssertEqual(month, "june", "--err-t1--")
    }
```

# --after-asserts--

```swift
}

extension CodigoTests {
    static var allTests : [(String, (CodigoTests) -> () throws -> Void)] {
        return [
            ("test1", test1),
        ]
    }
}

XCTMain([testCase(CodigoTests.allTests)])
```

# --solutions--

```swift
let month = "june"
```
