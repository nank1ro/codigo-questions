---
language: swift
exerciseType: 1
---

# --description--

`flatMap`は、**配列**を返すクロージャのためのものです。配列の配列を作る代わりに、返されたすべての配列を1つの平坦な結果に結合します:
```swift
let teams = [["Ann", "Bob"], ["Cid"]]
print(teams.map { $0 })     // [["Ann", "Bob"], ["Cid"]]
print(teams.flatMap { $0 }) // ["Ann", "Bob", "Cid"]
```
クロージャは、内側の配列を平坦化する前に変換することもできます。例えば、`teams.flatMap { $0.reversed() }`は`["Bob", "Ann", "Cid"]`になります。

# --instructions--

関数`flatten`を書いてください。`Int`の配列の配列を受け取り(引数ラベルは省略)、`flatMap`を使ってすべての数値を順番に含む1つの配列を返します

関数呼び出しの例:
```swift
print(flatten([[1, 2], [3]]))
// prints [1, 2, 3]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`flatMap`を使う必要があります

```json
{
  "regex": "\\.flatMap",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`flatten([[1, 2], [3]])`は`[1, 2, 3]`を返さなければなりません

```swift
tryCatch(flatten([[1, 2], [3]]) == [1, 2, 3])
```

`flatten([[5], [], [6, 7]])`は`[5, 6, 7]`を返さなければなりません

```swift
tryCatch(flatten([[5], [], [6, 7]]) == [5, 6, 7])
```

`flatten([])`は`[]`を返さなければなりません

```swift
tryCatch(flatten([]) == [])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func flatten(_ lists: [[Int]]) -> [Int] {
    return lists.flatMap { $0 }
}
```
