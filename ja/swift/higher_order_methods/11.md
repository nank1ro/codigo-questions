---
language: swift
exerciseType: 2
---

# --description--

`zip`は2つのシーケンスの要素を位置ごとにペアにし、タプルのシーケンスを作ります。短い方のシーケンスの終わりで停止します:
```swift
let names = ["Ann", "Bob"]
let ages = [31, 27, 99]
let pairs = zip(names, ages).map { "\($0) is \($1)" }
print(pairs) // ["Ann is 31", "Bob is 27"]
```
クロージャの中では、`$0`が最初のシーケンスの要素、`$1`が2番目のシーケンスの要素です。`zip`はメソッドではなくフリー関数です。つまり、`a.zip(b)`ではなく`zip(a, b)`と書きます。

# --instructions--

各都市をその気温とペアにし、`20`度より暖かい都市だけを残すコードを完成させて、プログラムが`["Rome", "Cairo"]`を出力するようにしてください

# --seed--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = [/](cities, temps).[/] { $1 > 20 }.map { $0.0 }
print(warm)
```

# --answers--

- zip
- filter
- map
- enumerated
- compactMap

# --solutions--

```swift
let cities = ["Oslo", "Rome", "Cairo"]
let temps = [8, 24, 31]
let warm = zip(cities, temps).filter { $1 > 20 }.map { $0.0 }
print(warm)
```

# --output--

["Rome", "Cairo"]
