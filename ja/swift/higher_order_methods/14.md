---
language: swift
exerciseType: 4
---

# --description--

`prefix(while:)`は、クロージャが`true`を返す**限り**先頭から要素を取り込み、たとえ後の要素がまた条件を満たすとしても、最初に失敗する要素で停止します。`drop(while:)`はその補完です。同じ先頭の連続部分をスキップし、残りをすべて返します:
```swift
let temps = [12, 15, 21, 14]
print(temps.prefix { $0 < 20 }) // [12, 15]
print(temps.drop { $0 < 20 })   // [21, 14]
```
どちらも`ArraySlice`を返します。これは元の配列に対するビューで、配列のように表示され、`Array(...)`で配列に変換できます。

# --instructions--

項目を並べ替えて、`readings`を`8`未満の値の先頭の連続部分とそれ以外とに分割し、両方を出力してください。
出力の前に両方の定数を宣言してください。`rising`を先に宣言してください

# --answers--

```swift
let readings = [3, 5, 8, 2, 9]
```

```swift
let rising = readings.prefix { $0 < 8 }
```

```swift
let rest = readings.drop { $0 < 8 }
```

```swift
print(rising)
```

```swift
print(rest)
```

# --solutions--

```swift
let readings = [3, 5, 8, 2, 9]
let rising = readings.prefix { $0 < 8 }
let rest = readings.drop { $0 < 8 }
print(rising)
print(rest)
```

# --output--

[3, 5]
[8, 2, 9]
