---
language: swift
exerciseType: 1
---

# --description--

自分自身の高階関数を書くこともできます。クロージャを受け取り、それをもとに**新しいクロージャを返す**関数はよくあるパターンです。返されたクロージャは元のクロージャをキャプチャするので、引数は`@escaping`でなければなりません。
例えば、`negate`は述語をその反対に変え、`filter`に渡せるようにします:
```swift
func negate(_ predicate: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { !predicate($0) }
}
let isEven: (Int) -> Bool = { $0 % 2 == 0 }
print([1, 2, 3, 4].filter(negate(isEven))) // [1, 3]
```
`filter(negate(isEven))`は、トレーリングクロージャ構文を使わずに、クロージャを普通の引数として渡していることに注意してください。

# --instructions--

関数`both`を書いてください。`(Int) -> Bool`型の2つのクロージャを受け取り(両方とも引数ラベルは省略)、同じ引数に対して両方が`true`になるときにだけ`true`を返す、同じ型のクロージャを返します

関数呼び出しの例:
```swift
let smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
print([4, 12, 7, 8].filter(smallEven))
// prints [4, 8]
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`both({ $0 < 10 }, { $0 % 2 == 0 })`は`4`に対して`true`にならなければなりません

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(4) == true)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })`は`12`に対して`false`にならなければなりません

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(12) == false)
```

`both({ $0 < 10 }, { $0 % 2 == 0 })`は`7`に対して`false`にならなければなりません

```swift
tryCatch(both({ $0 < 10 }, { $0 % 2 == 0 })(7) == false)
```

返されたクロージャは`filter`と一緒に使えなければなりません

```swift
let _smallEven = both({ $0 < 10 }, { $0 % 2 == 0 })
tryCatch([4, 12, 7, 8].filter(_smallEven) == [4, 8])
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func both(_ first: @escaping (Int) -> Bool, _ second: @escaping (Int) -> Bool) -> (Int) -> Bool {
    return { first($0) && second($0) }
}
```
