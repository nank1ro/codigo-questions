---
language: swift
exerciseType: 4
---

# --description--

`contains(where:)`と`allSatisfy`は、フラグ変数を持つループというよくあるパターンを置き換えます。どちらも答えが分かった時点で停止します。`contains(where:)`は最初の一致で、`allSatisfy`は最初に失敗する要素で停止します。
```swift
let ages = [15, 22, 40]
let anyMinor = ages.contains { $0 < 18 }  // true
let allAdults = ages.allSatisfy { $0 >= 18 } // false
```
`contains { ... }`は`contains(where:)`のトレーリングクロージャの形であり、特定の値を探す`contains(_:)`と混同しないでください。

# --instructions--

項目を並べ替えて、すべてのスコアが`60`以上かどうか、そしてスコアがちょうど`100`のものがあるかどうかを確認し、両方の結果を1行で出力してください。
`scores`を最初に宣言し、出力は最後に行ってください

# --answers--

```swift
let scores = [72, 100, 64]
```

```swift
let allPassed = scores.allSatisfy { $0 >= 60 }
```

```swift
let anyPerfect = scores.contains { $0 == 100 }
```

```swift
print("\(allPassed) \(anyPerfect)")
```

# --solutions--

```swift
let scores = [72, 100, 64]
let allPassed = scores.allSatisfy { $0 >= 60 }
let anyPerfect = scores.contains { $0 == 100 }
print("\(allPassed) \(anyPerfect)")
```

```swift
let scores = [72, 100, 64]
let anyPerfect = scores.contains { $0 == 100 }
let allPassed = scores.allSatisfy { $0 >= 60 }
print("\(allPassed) \(anyPerfect)")
```

# --output--

true true
