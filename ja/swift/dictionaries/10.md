---
language: swift
exerciseType: 2
---

# --description--

辞書に値を__追加__または__変更__するには、`updateValue(_:forKey:)`メソッドも使用できます

# --instructions--

辞書の`age`キーの値を`20`に更新してください

# --seed--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let [/] = [/][/][/]([/], forKey: [/]) {
    print("The old age was \(oldAge)")
}
print(user)
```

# --answers--

- update
- add
- insert
- user
- .
- updateValue
- 20
- "age"
- oldAge

# --solutions--

```swift
var user: [String: Any] = ["first_name": "John", "last_name": "Hood", "age": 30]
if let oldAge = user.updateValue(20, forKey: "age") {
    print("The old age was \(oldAge)")
}
print(user)
```

# --output--

The old age was 30
["age": 20, "first_name": "John", "last_name": "Hood"]
