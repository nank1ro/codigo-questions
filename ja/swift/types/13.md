---
language: swift
exerciseType: 2
---

# --description--

stringsのレッスンで見たように、`String`を反復処理すると1度に1つの`Character`が得られます。`Character`は`String`ではないため、テキストとして使うには`String(c)`で変換します。
文字が数字のとき、`wholeNumberValue`プロパティはその数値を`Int?`として返します。数字ではない文字に対しては`nil`になります。
```swift
for c in "a1" {
    print(c.wholeNumberValue)
}
// nil
// Optional(1)
```

# --instructions--

ループを完成させて、文字列の桁を合計し、プログラムが`8`を出力するようにしてください

# --seed--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.[/] {
        sum [/] digit
    }
}
print(sum)
```

# --answers--

- wholeNumberValue
- +=
- asciiValue
- isNumber
- -=
- =

# --solutions--

```swift
var sum = 0
for c in "2024" {
    if let digit = c.wholeNumberValue {
        sum += digit
    }
}
print(sum)
```

# --output--

8
