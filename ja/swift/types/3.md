---
language: swift
exerciseType: 1
---

# --description--

`type(of:)`関数は値の型を返します。Swiftが何を推論したかを確認するのに便利です:
```swift
print(type(of: 42))    // Int
print(type(of: 2.5))   // Double
print(type(of: "hi"))  // String
```
推論された型とは異なる型にしたい場合は、アノテーションを追加します。整数リテラルは`Double`や`Float`の定数に格納でき、1文字のリテラルは`Character`の定数に格納できます:
```swift
let ratio: Double = 3       // 3.0, not an Int
let half: Float = 0.5
let initial: Character = "S"
print(type(of: ratio))      // Double
```

# --instructions--

3つの定数を宣言してください:
- `ratio`: 整数リテラル`3`を格納する`Double`
- `half`: `0.5`を格納する`Float`
- `initial`: `"S"`を格納する`Character`

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`ratio`は`3.0`と等しい`Double`でなければなりません

```swift
tryCatch(type(of: ratio) == Double.self && ratio == 3.0)
```

`half`は`0.5`と等しい`Float`でなければなりません

```swift
tryCatch(type(of: half) == Float.self && half == 0.5)
```

`initial`は`"S"`と等しい`Character`でなければなりません

```swift
tryCatch(type(of: initial) == Character.self && initial == "S")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
let ratio: Double = 3
let half: Float = 0.5
let initial: Character = "S"
```
