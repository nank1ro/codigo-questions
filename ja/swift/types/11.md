---
language: swift
exerciseType: 3
---

# --description--

`Int`は64ビットを使うため、固定された範囲の数しか表現できません。最大値と最小値は`Int.max`と`Int.min`として利用できます:
```swift
print(Int.max) // 9223372036854775807
print(Int.min) // -9223372036854775808
```
これらの限界を超えることは**オーバーフロー**と呼ばれます。他の多くの言語と異なり、Swiftは範囲の反対側へ静かに折り返しません。オーバーフローする演算はプログラムを停止させる**ランタイムエラー**になります。

# --instructions--

このコードを実行すると何が起きますか？
```swift
func next(_ n: Int) -> Int {
    return n + 1
}
print(next(Int.max))
```

# --answers--

- プログラムはオーバーフローエラーでクラッシュします
- -9223372036854775808を出力します
- 0を出力します
- 9223372036854775807を出力します

# --solutions--

- プログラムはオーバーフローエラーでクラッシュします
