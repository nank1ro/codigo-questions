---
language: swift
exerciseType: 1
---

# --description--

条件付きキャストは`else if`と自然に連鎖して、複数の可能性のある型を扱えます。結果に必要な型へそれぞれ変換します:
```swift
let item: Any = 2.5
if let number = item as? Int {
    print(Double(number))
} else if let number = item as? Double {
    print(number)
}
```
`Any`に格納された`Int`は依然として`Int`です。それに対する`as? Double`は`nil`を返します。`as?`は型をチェックするのであって、数値を変換するわけではないからです。

# --instructions--

`Any`の配列を引数に受け取り（引数ラベルは省略）、その`Int`と`Double`の要素の合計を`Double`として返す関数`sumNumbers`を書いてください。他の型の要素は無視します

関数呼び出しの例:
```swift
print(sumNumbers([1, 2.5, "x"]))
// prints 3.5
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --asserts--

`sumNumbers([1, 2.5, "x"])`は`3.5`を返さなければなりません

```swift
tryCatch(sumNumbers([1, 2.5, "x"]) == 3.5)
```

`sumNumbers(["3", 4])`は`4.0`を返さなければなりません

```swift
tryCatch(sumNumbers(["3", 4]) == 4.0)
```

`sumNumbers([0.5, 0.25])`は`0.75`を返さなければなりません

```swift
tryCatch(sumNumbers([0.5, 0.25]) == 0.75)
```

`sumNumbers(["a", "b"])`は`0.0`を返さなければなりません

```swift
tryCatch(sumNumbers(["a", "b"]) == 0.0)
```

`sumNumbers([10])`は`Double`を返さなければなりません

```swift
tryCatch(type(of: sumNumbers([10])) == Double.self)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func sumNumbers(_ values: [Any]) -> Double {
    var total = 0.0
    for value in values {
        if let number = value as? Int {
            total += Double(number)
        } else if let number = value as? Double {
            total += number
        }
    }
    return total
}
```
