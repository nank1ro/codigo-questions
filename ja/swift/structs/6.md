---
language: swift
exerciseType: 4
---

# --instructions--

アイテムを並べ替えて`12`を出力してください。構造体を先に宣言し、`print`の前にインスタンスを作成します

# --answers--

```swift
struct Rectangle {
    var width: Int
    var height: Int
    func area() -> Int {
        return width * height
    }
}
```

```swift
let r = Rectangle(width: 3, height: 4)
```

```swift
print(r.area())
```

# --solutions--

```swift
struct Rectangle {
    var width: Int
    var height: Int
    func area() -> Int {
        return width * height
    }
}
let r = Rectangle(width: 3, height: 4)
print(r.area())
```

# --output--

12
