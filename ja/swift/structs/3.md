---
language: swift
exerciseType: 2
---

# --description--

格納プロパティには、宣言した場所で直接**デフォルト値**を与えることができます。Swiftはその値から型を推論するので、型アノテーションは省略できます:
```swift
struct Counter {
    var label: String
    var value = 0
}
```
メンバーワイズイニシャライザでは、デフォルト値を持つプロパティは省略可能な引数になります。渡せばデフォルトを上書きし、省略すればデフォルトがそのまま使われます。
```swift
let a = Counter(label: "clicks")
print(a.value) // 0

let b = Counter(label: "clicks", value: 7)
print(b.value) // 7
```

# --instructions--

`value`が`0`で始まり、`value`を渡さずに`c`が作られるように、コードを完成させてください

# --seed--

```swift
struct Counter {
    var label: String
    var value = [/]
}

let c = Counter([/]: "clicks")
print(c.label)
print(c.value)
```

# --answers--

- 0
- label
- 1
- value
- Counter
- name

# --solutions--

```swift
struct Counter {
    var label: String
    var value = 0
}

let c = Counter(label: "clicks")
print(c.label)
print(c.value)
```

# --output--

clicks
0
