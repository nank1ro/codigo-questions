---
language: swift
exerciseType: 2
---

# --description--

型が対応していると宣言しない限り、2つの構造体を`==`で比較することはできません。それを行うには、`Equatable`**プロトコル**に準拠します。宣言ではコロンの後に書きます:
```swift
struct Point: Equatable {
    var x: Int
    var y: Int
}

let a = Point(x: 1, y: 2)
let b = Point(x: 1, y: 2)
print(a == b) // true
```
`==`を自分で書く必要はありません。すべての格納プロパティがすでに`Equatable`なら、Swiftがプロパティを1つずつ比較する`==`を自動的に合成してくれます。すべてのプロパティが等しいときに2つのインスタンスは等しく、これは値に期待する動作そのものです。

# --instructions--

`Point`が比較できるようにコードを完成させ、`a`と`b`が等しいかどうかを出力してください

# --seed--

```swift
struct Point: [/] {
    var x: Int
    var y: Int
}

let a = Point(x: 1, y: 2)
let b = Point(x: 1, y: 2)
print(a [/] b)
```

# --answers--

- Equatable
- ==
- Comparable
- !=
- ===
- Codable

# --solutions--

```swift
struct Point: Equatable {
    var x: Int
    var y: Int
}

let a = Point(x: 1, y: 2)
let b = Point(x: 1, y: 2)
print(a == b)
```

# --output--

true
