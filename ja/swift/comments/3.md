---
language: swift
exerciseType: 1
---

# --description--

コンパイラはコメントを完全に取り除くため、コメントを追加したり削除したりしても、プログラムの動作は決して変わりません。実行されるのは、コメント化**されていない**コードだけです。

このため、`//`はコードの行を削除せずに無効にする手軽な方法になります。これは**コメントアウト**と呼ばれます:
```swift
var total = 10
// total = total + 5
print(total) // prints 10
```
2行目はコメントになったため、`total`は`10`のままです。`//`を取り除くと、その行は再び実行されるようになります。

コメントアウトは実験している間は便利ですが、後片付けを忘れないでください。長い間コメントアウトされたままのコードは、次に読む人を混乱させるだけです。

# --instructions--

`score`が初期値を保つように、`score = 0`という行をコメントアウトしてください

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
var score = 10
score = 0
```

# --asserts--

`score = 0`という行は`//`でコメントアウトされたまま残っていなければなりません

```json
{
  "regex": "//\\s*score = 0",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`score`は`10`と等しくなければなりません

```swift
tryCatch(score == 10)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
var score = 10
// score = 0
```
