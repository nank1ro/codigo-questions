---
language: swift
exerciseType: 4
---

# --description--

要約の後に、ドキュメンテーションコメントはXcodeが認識する特別なMarkdownのリスト項目で、パラメータと戻り値を説明できます:
```swift
/// 与えられた分を秒数にして返す。
/// - Parameter minutes: 分を表す整数、負の値にはならない
/// - Returns: `minutes` に60を掛けた値
func toSeconds(_ minutes: Int) -> Int {
    return minutes * 60
}
```
順序は常に同じです。最初に要約、次にパラメータごとに`- Parameter name:`、そして`- Returns:`です。

# --instructions--

要約、`Parameter`行、`Returns`行をこの順序で関数のすぐ上に置いて`twice`をドキュメント化し、`twice(21)`を出力するようにアイテムを並べ替えてください

# --answers--

```swift
/// 数値の2倍を返す。
```

```swift
/// - Parameter n: 2倍にする数値
```

```swift
/// - Returns: `n` に2を掛けた値
```

```swift
func twice(_ n: Int) -> Int {
    return n * 2
}
```

```swift
print(twice(21))
```

# --solutions--

```swift
/// 数値の2倍を返す。
/// - Parameter n: 2倍にする数値
/// - Returns: `n` に2を掛けた値
func twice(_ n: Int) -> Int {
    return n * 2
}
print(twice(21))
```

# --output--

42
