---
language: swift
exerciseType: 1
---

# --description--

`TODO`は通常、本物の実装が書かれるまでコードをコンパイル可能な状態に保つプレースホルダーの隣に置かれます。作業を完了したら、同じ変更の中でプレースホルダーを置き換え、マーカーを削除してください。そうすれば、コメントがコードの状態について嘘をつくことはありません。

# --instructions--

`isEven`内の`TODO`コメントが示す作業を完了させてから、作業が終わったのでコメントを削除してください

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
func isEven(_ n: Int) -> Bool {
    // TODO: return true when n is even, false otherwise
    return false
}
```

# --asserts--

`TODO`コメントは削除されていなければなりません

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`isEven(4)`は`true`を返さなければなりません

```swift
tryCatch(isEven(4) == true)
```

`isEven(7)`は`false`を返さなければなりません

```swift
tryCatch(isEven(7) == false)
```

`isEven(0)`は`true`を返さなければなりません

```swift
tryCatch(isEven(0) == true)
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
func isEven(_ n: Int) -> Bool {
    return n % 2 == 0
}
```
