---
language: swift
exerciseType: 2
---

# --description--

`CustomStringConvertible`は、`print`が自分の型に対して何を表示するかを決めます。その唯一の要件は`description`プロパティです:
```swift
struct Coin: CustomStringConvertible {
    var value: Int

    var description: String {
        return "\(value)c"
    }
}

print(Coin(value: 25)) // 25c
```
準拠がなければ、構造体の表示は`Coin(value: 25)`のようなデフォルトの出力にフォールバックします。そして`description`プロパティだけでは何も変わりません。`print`はプロトコルを探すからです。文字列補間も`description`を使います。

# --instructions--

`Card`を表示すると`Ace of spades`となるように、コードを完成させてください

# --seed--

```swift
struct Card: [/] {
    var rank: String
    var suit: String

    var [/]: String {
        return "\(rank) of \(suit)"
    }
}

print(Card(rank: "Ace", suit: "spades"))
```

# --answers--

- CustomStringConvertible
- description
- Equatable
- label
- text
- String

# --solutions--

```swift
struct Card: CustomStringConvertible {
    var rank: String
    var suit: String

    var description: String {
        return "\(rank) of \(suit)"
    }
}

print(Card(rank: "Ace", suit: "spades"))
```

# --output--

Ace of spades
