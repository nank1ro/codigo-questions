---
language: swift
exerciseType: 1
---

# --description--

**プロトコル**の拡張は別の道具です。過去も未来も、準拠するすべての型にメンバーを追加します。これが、要件に**デフォルト実装**を与える方法です:
```swift
protocol Greeter {
    var name: String { get }
    func greet() -> String
}

extension Greeter {
    func greet() -> String {
        return "Hi, \(name)"
    }
}

struct Person: Greeter {
    var name: String
}

print(Person(name: "Ada").greet()) // Hi, Ada
```
`Person`は`greet()`を一度も書いていませんが、それでも準拠しています。プロトコル拡張の中では、プロトコルのすべての要件（ここでは`name`）を使えます。準拠する型は必ずそれを持つことが保証されているからです。

# --instructions--

`Basket`の拡張を追加し、`summary()`にアイテムの個数に` items`を続けて返すデフォルト実装を与えてください。そうすれば`Fruits`はメソッドを書かずに準拠できます

使用例:
```swift
print(Fruits(items: ["fig", "plum"]).summary())
// prints 2 items
```

# --before-seed--

```swift
// DO NOT EDIT FROM HERE
import Foundation

var _testCount = 0
var _testFailedCount = 0
func tryCatch(_ assertion: Bool) {
    _testCount += 1
    if !assertion {
        _testFailedCount += 1
        print("Test Case '--err-t\(_testCount)--' failed")
    }
}
// DO NOT EDIT UNTIL HERE
```

# --seed--

```swift
protocol Basket {
    var items: [String] { get }
    func summary() -> String
}

struct Fruits: Basket {
    var items: [String]
}
```

# --before-asserts--

```swift
struct _Veg: Basket {
    var items: [String]
}
```

# --asserts--

`fig`と`plum`入りのバスケットは`2 items`と要約されなければなりません

```swift
tryCatch(Fruits(items: ["fig", "plum"]).summary() == "2 items")
```

空のバスケットは`0 items`と要約されなければなりません

```swift
tryCatch(Fruits(items: []).summary() == "0 items")
```

3つの果物入りのバスケットは`3 items`と要約されなければなりません

```swift
tryCatch(Fruits(items: ["fig", "plum", "pear"]).summary() == "3 items")
```

メソッドを一切書かない別の`Basket`型は`1 items`と要約されなければなりません

```swift
tryCatch(_Veg(items: ["pea"]).summary() == "1 items")
```

# --after-asserts--

```swift
print("Executed \(_testCount) tests, with \(_testFailedCount) failures")
```

# --solutions--

```swift
protocol Basket {
    var items: [String] { get }
    func summary() -> String
}

extension Basket {
    func summary() -> String {
        return "\(items.count) items"
    }
}

struct Fruits: Basket {
    var items: [String]
}
```
