---
language: python
exerciseType: 1
---

# --instructions--

`store` に `60` 個の商品があります。日中に `50` 個を販売し、`55` 個の新しい商品を `bought`（購入）しました。
`store` の数量を更新してください。

# --seed--

```python
store = 60
sold = 50
bought = 55
# write after this line
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`store` の数量は65と等しくなければなりません

```python
    def test_1(self):
        self.assertEqual(store, 65, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
store = 60
sold = 50
bought = 55
store += bought - sold
```
