---
language: python
exerciseType: 2
---

# --description--

`not`は読み取った値を変更するのではなく、新しいブール値を作り出します。そのため、`is_afternoon = not is_morning`の後でも、変数`is_morning`は元の値を保持したままです。

# --instructions--

コードブロックが実行されるように空欄を埋めてください

# --seed--

```python
is_morning = True
is_afternoon = not is_morning
if ([/]):
    print("Good morning!")
```

# --answers--

- is_morning
- is_afternoon
- "is_morning"
- "is_afternoon"

# --solutions--

```python
is_morning = True
is_afternoon = not is_morning
if (is_morning):
    print("Good morning!")
```

# --output--

Good morning!
