---
language: python
exerciseType: 2
---

# --description--

変数に`True`を代入すると、それを使う条件が成立するので、その下のブロックが実行されます。

# --instructions--

変数に値を代入してコードブロックを実行させてください

# --seed--

```python
coffee_time = [/]
if (coffee_time):
    print("It's time for coffee!")
```

# --answers--

- True
- False
- "True"
- "False"

# --solutions--

```python
coffee_time = True
if (coffee_time):
    print("It's time for coffee!")
```

# --output--

It's time for coffee!
