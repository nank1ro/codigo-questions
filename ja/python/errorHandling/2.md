---
language: python
exerciseType: 1
---

# --description--

`try`ブロックは例外を発生させる**最初の**文で停止し、それ以降の行はスキップされて制御が`except`ブロックに移ります。`try`ブロック内の処理は何も元に戻されないため、できるだけ短く保ちましょう：
```python
def half(n):
    try:
        return 10 / n
    except ZeroDivisionError:
        return "undefined"

print(half(2))  # 5.0
print(half(0))  # undefined
```
`except`内の`return`は他の`return`と同じように機能するため、`try`/`except`はクラッシュする代わりにフォールバック値を返す自然な方法になります。

# --instructions--

`a / b` を返し、`b` が `0` のときは文字列 `"undefined"` を返す関数 `safe_divide(a, b)` を、`ZeroDivisionError` を捕捉して書いてください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_divide(9, 3)` は `3.0` を返さなければなりません

```python
    def test_1(self):
        self.assertEqual(safe_divide(9, 3), 3.0, "--err-t1--")
```

`safe_divide(1, 0)` は `"undefined"` を返さなければなりません

```python
    def test_2(self):
        self.assertEqual(safe_divide(1, 0), "undefined", "--err-t2--")
```

`safe_divide(-5, 2)` は `-2.5` を返さなければなりません

```python
    def test_3(self):
        self.assertEqual(safe_divide(-5, 2), -2.5, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "undefined"
```
