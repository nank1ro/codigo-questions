---
language: python
exerciseType: 1
---

# --description--

複数の失敗に**同じ**回復処理がふさわしいときは、ブロックを繰り返す代わりに、1つの節にタプルとして並べる方が簡潔です：
```python
try:
    value = int(text) / divisor
except (ValueError, ZeroDivisionError):
    value = 0
```
括弧は必須です：`except ValueError, ZeroDivisionError:` はPython 3では構文エラーになります。タプルであっても、明示的な型のリストであることに変わりありません。

# --instructions--

両方の型をタプルにした1つの `except` 節を使い、`a / b` を返し、除算が `ZeroDivisionError` または `TypeError` を発生させたときは `None` を返す関数 `safe_ratio(a, b)` を書いてください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`safe_ratio(9, 3)` は `3.0` を返さなければなりません

```python
    def test_1(self):
        self.assertEqual(safe_ratio(9, 3), 3.0, "--err-t1--")
```

`safe_ratio(1, 0)` は `None` を返さなければなりません

```python
    def test_2(self):
        self.assertIsNone(safe_ratio(1, 0), "--err-t2--")
```

`safe_ratio(1, "x")` は `None` を返さなければなりません

```python
    def test_3(self):
        self.assertIsNone(safe_ratio(1, "x"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def safe_ratio(a, b):
    try:
        return a / b
    except (ZeroDivisionError, TypeError):
        return None
```
