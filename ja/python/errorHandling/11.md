---
language: python
exerciseType: 1
---

# --description--

`finally` ブロックは**何が起こっても**実行されます：問題なく終わった `try` ブロックの後でも、`except` ブロックの後でも、誰にも捕捉されない例外が外側へ伝わっている最中でも、そして `try` や `except` ブロックが `return` を実行するときでも実行されます：
```python
def read(text):
    try:
        return int(text)
    except ValueError:
        return 0
    finally:
        print("done")
```
どちらの経路でも、値が関数から出る前に `done` が出力されます。この保証のために `finally` があります：ファイルを閉じる、ロックを解放する、設定を元に戻す、といった処理です。完全な形は `try` / `except` / `else` / `finally` です。`try` には少なくとも1つの `except` か `finally` が必要で、`else` は `except` と一緒のときにしか使えません。

# --instructions--

空のリストから始めて `int(text)` を追加し、`int(text)` が `ValueError` を発生させたときは代わりに文字列 `"bad"` を追加し、`finally` ブロックで必ず `"done"` を追加して、そのリストを返す関数 `record(text)` を書いてください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`record("5")` は `[5, "done"]` を返さなければなりません

```python
    def test_1(self):
        self.assertEqual(record("5"), [5, "done"], "--err-t1--")
```

`record("x")` は `["bad", "done"]` を返さなければなりません

```python
    def test_2(self):
        self.assertEqual(record("x"), ["bad", "done"], "--err-t2--")
```

各呼び出しは新しいリストから始まらなければなりません

```python
    def test_3(self):
        record("1")
        self.assertEqual(record("2"), [2, "done"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def record(text):
    log = []
    try:
        log.append(int(text))
    except ValueError:
        log.append("bad")
    finally:
        log.append("done")
    return log
```
