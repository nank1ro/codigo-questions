---
language: python
exerciseType: 1
---

# --description--

組み込みの型がどれも合わないときは、`Exception` のサブクラスを作って独自の型を定義します。空の本体で通常は十分です。名前自体が読み手へのメッセージです：
```python
class ConfigError(Exception):
    pass
```
他の例外と同じように振る舞います：`raise ConfigError("bad port")` と発生させ、`except ConfigError:` で捕捉できます。

低レベルの失敗を自分の型に変換することはよくありますが、その過程で元のエラーを失ってはいけません。`raise NewError(...) from original` は2つを**チェーン（連鎖）**させます：`original` を新しい例外の `__cause__` 属性に格納し、トレースバックには *The above exception was the direct cause of the following exception* の下に両方が表示されます：
```python
try:
    port = int(text)
except ValueError as e:
    raise ConfigError("bad port") from e
```
`from e` がなくても2つは暗黙的にリンクされますが、`from` は最初のエラーが2番目を引き起こしたことをはっきり述べます。

# --instructions--

`Exception` をサブクラス化する例外クラス `ConfigError` を定義し、`int(text)` を返し、それが `ValueError` を発生させたときは `raise ... from` でチェーンされた `ConfigError("bad port")` を発生させる関数 `read_port(text)` を書いてください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`read_port("8080")` は `8080` を返し、`ConfigError` は `Exception` をサブクラス化しなければなりません

```python
    def test_1(self):
        self.assertEqual(read_port("8080"), 8080, "--err-t1--")
        self.assertTrue(issubclass(ConfigError, Exception), "--err-t1--")
```

`read_port("abc")` はメッセージ `"bad port"` 付きの `ConfigError` を発生させなければなりません

```python
    def test_2(self):
        with self.assertRaises(ConfigError, msg="--err-t2--") as ctx:
            read_port("abc")
        self.assertEqual(str(ctx.exception), "bad port", "--err-t2--")
```

発生した `ConfigError` は元の `ValueError` からチェーンされていなければなりません

```python
    def test_3(self):
        with self.assertRaises(ConfigError, msg="--err-t3--") as ctx:
            read_port("abc")
        self.assertIsInstance(ctx.exception.__cause__, ValueError, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
class ConfigError(Exception):
    pass

def read_port(text):
    try:
        return int(text)
    except ValueError as e:
        raise ConfigError("bad port") from e
```
