---
language: python
exerciseType: 4
---

# --description--

ハンドラは、失敗の責任を負うことなく反応すべきときがあります：ログに記録し、カウントし、何かを閉じる — その後、呼び出し元に任せます。`except` ブロックの中で単独の `raise` は、処理中の例外を元の型、メッセージ、トレースバックをそのままにして**再発生（re-raise）**させます：
```python
try:
    value = int(text)
except ValueError:
    print("could not read the value")
    raise
```
代わりに `raise ValueError(...)` と書くと、新しいトレースバックを持つ別の例外が作られます——それはもう捕まえたのと同じ失敗ではなくなり、単独の `raise` はまさにその同一性を保持します。

# --instructions--

アイテムを並べ替えて、`load` を定義し、`"5"` を読み込んだ結果を出力し、その後 `"abc"` を引数にした呼び出しを失敗を報告する `try` ブロックの中で行い、`load` からのメッセージ、次に呼び出し元からのメッセージを出力するプログラムを作ってください

# --answers--

```python
def load(text):
```

```python
    try:
        return int(text)
```

```python
    except ValueError:
        print(f"could not read {text}")
        raise
```

```python
print(load("5"))
```

```python
try:
    load("abc")
except ValueError:
    print("handled by the caller")
```

# --solutions--

```python
def load(text):
    try:
        return int(text)
    except ValueError:
        print(f"could not read {text}")
        raise
print(load("5"))
try:
    load("abc")
except ValueError:
    print("handled by the caller")
```

# --output--

5
could not read abc
handled by the caller
