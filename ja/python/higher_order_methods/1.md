---
language: python
exerciseType: 2
---

# --description--

Pythonでは関数は**値**であり、数値や文字列と同じものです。変数に格納したり、リストに入れたり、別の関数に渡したりできます。呼び出すのは丸括弧だけで、`shout` は関数そのもの、`shout("hi")` はその結果です：
```python
def shout(text):
    return text.upper() + "!"

say = shout
print(say("hi"))  # HI!
```
別の関数をパラメータとして受け取る、または返す関数は**高階関数**と呼ばれます。その内部では、パラメータは他の関数と同じように丸括弧を付けて呼び出されます：
```python
def apply(func, value):
    return func(value)

print(apply(shout, "bye"))  # BYE!
print(apply(len, "bye"))    # 3
```

# --instructions--

`apply` が受け取った関数を呼び出し、`say` が `shout` を呼び出さずに参照するようにコードを完成させ、出力が `HI!`、`3` になるようにしてください

# --seed--

```python
def shout(text):
    return text.upper() + "!"

def apply(func, value):
    return [/](value)

say = [/]
print(say("hi"))
print(apply(len, "bye"))
```

# --answers--

- func
- shout
- shout()
- value
- print

# --solutions--

```python
def shout(text):
    return text.upper() + "!"

def apply(func, value):
    return func(value)

say = shout
print(say("hi"))
print(apply(len, "bye"))
```

# --output--

HI!
3
