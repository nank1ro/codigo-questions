---
language: python
exerciseType: 3
---

# --description--

`functools` の `partial(func, *fixed)` は、一部の引数が**すでに埋められた**新しい関数を作ります。結果の関数を呼び出すと、残りの引数を渡します：
```python
from functools import partial

def power(base, exponent):
    return base ** exponent

square = partial(power, exponent=2)
print(square(5))  # 25
```
`partial` に渡した位置引数は最初のパラメータを埋めます。キーワード引数は名前でパラメータを固定しますが、呼び出し時に上書きすることもできます。partialは普通の呼び出し可能オブジェクトなので、`map`、`sorted`、その他の高階関数に渡せます。

# --instructions--

このコードは何を出力しますか？
```python
from functools import partial

def power(base, exponent):
    return base ** exponent

square = partial(power, exponent=2)
print(square(5), square(base=3))
```

# --answers--

- `25 9`、`exponent` が `2` に固定されており、呼び出しが底を渡すためです
- `32 8`、`partial` の最初の引数が底であるためです
- `25 8`、`square(base=3)` が固定された指数を上書きするためです
- `TypeError`、`base` が `partial` でも必要なためです

# --solutions--

- `25 9`、`exponent` が `2` に固定されており、呼び出しが底を渡すためです
