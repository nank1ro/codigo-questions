---
language: python
exerciseType: 4
---

# --instructions--

アイテムを並べ替えて、3文字より長い単語だけを残し、大文字に変換し、長さ順に並べて出力するようにしてください

# --answers--

```python
words = ["pear", "fig", "banana", "kiwi"]
```

```python
long_words = filter(lambda w: len(w) > 3, words)
```

```python
shouted = map(str.upper, long_words)
```

```python
print(sorted(shouted, key=len))
```

# --solutions--

```python
words = ["pear", "fig", "banana", "kiwi"]
long_words = filter(lambda w: len(w) > 3, words)
shouted = map(str.upper, long_words)
print(sorted(shouted, key=len))
```

# --output--

['PEAR', 'KIWI', 'BANANA']
