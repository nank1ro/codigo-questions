---
language: python
exerciseType: 1
---

# --description--

組み込みの `filter(func, iterable)` は、`func` が真の値を返す要素だけを残します。`map` と同じように、リストに変換する必要がある遅延オブジェクトを返します：
```python
numbers = [4, -2, 7, 0]
print(list(filter(lambda n: n > 0, numbers)))  # [4, 7]
```
`filter` に渡す関数は**述語**と呼ばれます。1つの要素を受け取り、その要素についてのはい/いいえの質問に答えます。関数の代わりに `None` を渡すと、それ自体が真の要素だけが残り、`0`、`""`、`None` は捨てられます。

# --instructions--

`filter` を使って、数値のリストを受け取り、ゼロより大きい数値だけを含む新しいリストを返す関数 `positives` を書いてください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`filter` を使用する必要があります

```json
{
  "regex": "filter\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`positives([3, -1, 0, 5])` は `[3, 5]` を返さなければなりません

```python
    def test_1(self):
        self.assertEqual(positives([3, -1, 0, 5]), [3, 5], "--err-t1--")
```

結果はリストでなければなりません

```python
    def test_2(self):
        self.assertIsInstance(positives([1, 2]), list, "--err-t2--")
        self.assertEqual(positives([1, 2]), [1, 2], "--err-t2--")
```

`positives([-4, 0])` は空のリストを返さなければなりません

```python
    def test_3(self):
        self.assertEqual(positives([-4, 0]), [], "--err-t3--")
        self.assertEqual(positives([]), [], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def positives(numbers):
    return list(filter(lambda n: n > 0, numbers))
```
