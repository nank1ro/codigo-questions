---
language: python
exerciseType: 1
---

# --description--

`partial` はオプションを取る組み込み関数と相性が良いです。`int(text, base=16)` は16進数の文字列を解析します。baseを固定すると、`map` にぴったりの1引数の変換関数が得られます：
```python
from functools import partial

hex_to_int = partial(int, base=16)
print(hex_to_int("ff"))                      # 255
print(list(map(hex_to_int, ["a", "10"])))    # [10, 16]
```
partialオブジェクトは包んでいるものを覚えています。`hex_to_int.func` は `int` で、`hex_to_int.keywords` は `{'base': 16}` です。

# --instructions--

`partial` を使って2つの関数を定義してください。16進数の文字列を `int` に変換する `hex_to_int` と、2進数の文字列を `int` に変換する `binary_to_int` です

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`hex_to_int("ff")` は `255` を返さなければなりません

```python
    def test_1(self):
        self.assertEqual(hex_to_int("ff"), 255, "--err-t1--")
        self.assertEqual(hex_to_int("10"), 16, "--err-t1--")
```

`binary_to_int("1010")` は `10` を返さなければなりません

```python
    def test_2(self):
        self.assertEqual(binary_to_int("1010"), 10, "--err-t2--")
        self.assertEqual(binary_to_int("11"), 3, "--err-t2--")
```

どちらも `int` を包むpartialオブジェクトでなければなりません

```python
    def test_3(self):
        self.assertIs(hex_to_int.func, int, "--err-t3--")
        self.assertIs(binary_to_int.func, int, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from functools import partial

hex_to_int = partial(int, base=16)
binary_to_int = partial(int, base=2)
```
