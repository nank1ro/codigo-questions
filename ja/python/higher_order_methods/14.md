---
language: python
exerciseType: 2
---

# --description--

`any(iterable)` は**少なくとも1つ**の要素が真なら `True` を返し、`all(iterable)` は**すべての**要素が真なら `True` を返します。これらは**ジェネレータ式**と自然に組み合わせられます。ジェネレータ式は角括弧を書かないリスト内包表記で、リストを作る代わりに値を1つずつ生成します：
```python
ages = [21, 34, 17]
print(any(age >= 18 for age in ages))  # True
print(all(age >= 18 for age in ages))  # False
```
値は遅延的に生成されるので、`any` は最初の `True` で、`all` は最初の `False` で停止し、残りを評価しません。`sum` もジェネレータ式を受け取ります。`sum(1 for age in ages if age >= 18)` は大人の人数を数えます。

# --instructions--

大人の年齢が1つでもあるか、すべての年齢が大人か、大人の年齢が何個あるかを出力するようにコードを完成させ、出力が `True`、`False`、`2` になるようにしてください

# --seed--

```python
ages = [21, 34, 17]
print([/](age >= 18 for age in ages))
print([/](age >= 18 for age in ages))
print([/](1 for age in ages if age >= 18))
```

# --answers--

- any
- all
- sum
- len
- filter

# --solutions--

```python
ages = [21, 34, 17]
print(any(age >= 18 for age in ages))
print(all(age >= 18 for age in ages))
print(sum(1 for age in ages if age >= 18))
```

# --output--

True
False
2
