---
language: python
exerciseType: 1
---

# --description--

関数は関数を**返す**こともできます。`def` で内部関数を定義し、呼び出さずに返します：
```python
def make_greeter(greeting):
    def greet(name):
        return greeting + ", " + name
    return greet

hello = make_greeter("Hello")
print(hello("Ada"))  # Hello, Ada
```
内部関数は、`make_greeter` が終了した後も `greeting` を使い続けます。自分が作られたスコープの変数を**覚えて**いるのです。このような関数は**クロージャ**と呼ばれます。`make_greeter` を呼び出すたびに、それぞれ独自の `greeting` を持つ新しい独立したクロージャが作られます。

# --instructions--

数値 `factor` を受け取り、数値を受け取ってそれに `factor` を掛けた値を返す関数を返す関数 `make_multiplier` を書いてください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`make_multiplier(2)` は関数を返さなければなりません

```python
    def test_1(self):
        self.assertTrue(callable(make_multiplier(2)), "--err-t1--")
```

`make_multiplier(2)(5)` は `10` を返さなければなりません

```python
    def test_2(self):
        double = make_multiplier(2)
        self.assertEqual(double(5), 10, "--err-t2--")
        self.assertEqual(double(0), 0, "--err-t2--")
```

それぞれの乗数は自分の `factor` を覚えていなければなりません

```python
    def test_3(self):
        double = make_multiplier(2)
        triple = make_multiplier(3)
        self.assertEqual(triple(4), 12, "--err-t3--")
        self.assertEqual(double(4), 8, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def make_multiplier(factor):
    def multiply(number):
        return number * factor
    return multiply
```
