---
language: python
exerciseType: 2
---

# --description--

組み込みの `map(func, iterable)` はすべての要素に対して `func` を呼び出し、要素ごとに1つの結果を生成します。*mapオブジェクト* という遅延オブジェクトを返すので、値を見るには `list()` で包みます：
```python
numbers = [1, 2, 3]
print(list(map(lambda n: n * 10, numbers)))  # [10, 20, 30]
```
lambdaだけでなく、どんな呼び出し可能オブジェクトでも渡せます。`len` のような組み込み関数や、そのクラスから取り出したメソッド、たとえば文字列を最初の引数として取る `str.upper` などです：
```python
names = ["ada", "linus"]
print(list(map(str.upper, names)))  # ['ADA', 'LINUS']
print(list(map(len, names)))        # [3, 5]
```

# --instructions--

`upper` に大文字にした名前、`lengths` にそれぞれの長さが入るようにコードを完成させ、出力が `['ADA', 'LINUS']`、`[3, 5]` になるようにしてください

# --seed--

```python
names = ["ada", "linus"]
upper = list([/](str.upper, names))
lengths = list(map([/], names))
print(upper)
print(lengths)
```

# --answers--

- map
- len
- filter
- str
- sorted

# --solutions--

```python
names = ["ada", "linus"]
upper = list(map(str.upper, names))
lengths = list(map(len, names))
print(upper)
print(lengths)
```

# --output--

['ADA', 'LINUS']
[3, 5]
