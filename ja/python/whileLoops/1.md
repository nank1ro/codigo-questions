---
language: python
exerciseType: 2
---

# --description--

プログラミングでは、コードのブロックを繰り返す必要がよくあります。例えば：
```python
print("2 seconds")
print("3 seconds")
print("4 seconds")
print("5 seconds")
```
これにより、以下の出力が生成されます：
```python
2 seconds
3 seconds
4 seconds
5 seconds
```
明らかに、長い文ではコードを書くのに多くの時間がかかりますが、幸いなことにループを使うことができます。
上記と同じ出力を得る`while`ループを学びましょう。
```python
count = 2
while (count <= 5):
    print(f"{count} seconds")
    count += 1
```
変数`count`を作成し、初期値として`2`を代入しました。
次に、条件`count <= 5`が`True`である間、コードブロックを実行する`while`文を使用しました。
コードブロックの中で、`count += 1`の行を追加することを**忘れてはいけません**。
これは`count`の値をインクリメントします。そうしないと、ループが無限になります

# --instructions--

`while`ループで空欄を埋めて、コードブロックを繰り返してください

# --seed--

```python
[/] True:
    print("repeat")
```

# --answers--

- while
- "while"
- for
- loop

# --solutions--

```python
while True:
    print("repeat")
```

# --output--

repeat
repeat
repeat
repeat
...
