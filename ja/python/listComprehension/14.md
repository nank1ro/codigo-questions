---
language: python
exerciseType: 2
---

# --description--

同じ考え方は辞書にも通用します。**辞書内包表記**は波括弧と `key: value` の式を使います。
```python
names = ["ana", "bob"]
lengths = {name: len(name) for name in names}
print(lengths)  # {'ana': 3, 'bob': 3}
```

# --instructions--

プログラムが `{1: 1, 2: 4, 3: 9}` を出力するように、各数値をその2乗に対応付ける辞書内包表記でコードを完成させてください

# --seed--

```python
squares = [/]n: n * n for n in range(1, 4)[/]
print(squares)
```

# --answers--

- {
- }
- [
- ]
- (

# --solutions--

```python
squares = {n: n * n for n in range(1, 4)}
print(squares)
```

# --output--

{1: 1, 2: 4, 3: 9}
