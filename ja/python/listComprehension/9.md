---
language: python
exerciseType: 1
---

# --description--

2つの条件を同じ内包表記の中で組み合わせることができます。値を選ぶための `if-else` と、一部の要素を取り除くための末尾のフィルタ `if` です。
```python
nums = [1, 2, 3, 4]
result = ["big" if n > 2 else "small" for n in nums if n != 3]
print(result)  # ['small', 'small', 'big']
```

# --instructions--

`if-else` 式を使ったリスト内包表記を使って、`nums` の負の数をすべて `0` に置き換えたコピーを変数 `clipped` に格納してください

# --seed--

```python
nums = [4, -2, 7, -5]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`if-else` 式を使ったリスト内包表記を使用する必要があります

```json
{
  "regex": "\\[.* if .* else .* for .* in .*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`clipped` は `[4, 0, 7, 0]` と等しくなければなりません

```python
    def test_1(self):
        self.assertEqual(clipped, [4, 0, 7, 0], "--err-t1--")
```

`nums` は変更されてはいけません

```python
    def test_2(self):
        self.assertEqual(nums, [4, -2, 7, -5], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [4, -2, 7, -5]
clipped = [n if n >= 0 else 0 for n in nums]
```
