---
language: python
exerciseType: 2
---

# --description--

メンバーの値は**タプル**にすることができ、各メンバーに複数のデータを持たせることができます。
列挙型が `__init__` メソッドを定義していると、Pythonはメンバーごとに1回それを呼び出し、タプルを引数に展開します。そのため、各データを個々の属性に保存できます：
```python
from enum import Enum

class Planet(Enum):
    EARTH = (5.97, 6371)
    MARS = (0.64, 3390)

    def __init__(self, mass, radius):
        self.mass = mass
        self.radius = radius

print(Planet.MARS.radius)  # 3390
print(Planet.MARS.value)   # (0.64, 3390)
```
メンバーの `value` はタプル全体のままです。

# --instructions--

コードを完成させて、`__init__` で各メンバーのタプルを展開し、2番目の要素を属性 `metal` に保存した上で、`Coin.PENNY` の金属を出力してください

# --seed--

```python
from enum import Enum

class Coin(Enum):
    PENNY = (1, "copper")
    DIME = (10, "silver")

    def [/](self, cents, metal):
        self.cents = cents
        self.[/] = metal

print(Coin.DIME.cents)
print(Coin.PENNY.[/])
```

# --answers--

- __init__
- metal
- metal
- __new__
- value
- name
- cents

# --solutions--

```python
from enum import Enum

class Coin(Enum):
    PENNY = (1, "copper")
    DIME = (10, "silver")

    def __init__(self, cents, metal):
        self.cents = cents
        self.metal = metal

print(Coin.DIME.cents)
print(Coin.PENNY.metal)
```

# --output--

10
copper
