---
language: python
exerciseType: 1
---

# --description--

クラスを関数のように呼び出せば**値**から、角括弧を使えば**名前**からメンバーを取得できます：
```python
print(Color(2))        # Color.GREEN
print(Color["BLUE"])   # Color.BLUE
```
どちらも、値や名前がファイルやユーザー入力のようにプログラムの外部から来る場合に便利です。
何も一致しない場合、`Color(9)` は `ValueError` を発生させ、`Color["PINK"]` は `KeyError` を発生させます。

# --instructions--

`Status(...)` を呼び出して値が `code` である `Status` のメンバーを `by_value` に、`Status[...]` を使って名前が `label` であるメンバーを `by_name` に格納してください

# --seed--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`code` を指定して `Status(...)` を呼び出す必要があります

```json
{
  "regex": "Status\\(\\s*code\\s*\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`label` を指定して `Status[...]` を使用する必要があります

```json
{
  "regex": "Status\\[\\s*label\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`by_value` は `Status.DONE` でなければなりません

```python
    def test_1(self):
        self.assertIs(by_value, Status.DONE, "--err-t1--")
```

`by_name` は `Status.FAILED` でなければなりません

```python
    def test_2(self):
        self.assertIs(by_name, Status.FAILED, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Status(Enum):
    PENDING = 0
    DONE = 1
    FAILED = 2

code = 1
label = "FAILED"
by_value = Status(code)
by_name = Status[label]
```
