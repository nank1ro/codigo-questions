---
language: python
exerciseType: 1
---

# --description--

列挙型のすべてのメンバーには2つの属性があります：クラスに書いた識別子である `name` と、代入した値である `value` です：
```python
from enum import Enum

class Color(Enum):
    RED = 1
    GREEN = 2

print(Color.GREEN.name)   # GREEN
print(Color.GREEN.value)  # 2
```
値は整数に限らず、どんな型でも構いません：文字列、タプル、浮動小数点数がよく使われます。
メンバーは普通のオブジェクトなので、変数に格納して後からその属性を読み取ることができます：
```python
chosen = Color.RED
print(chosen.value)  # 1
```

# --instructions--

`home` にメンバー `Planet.EARTH` を、`label` にその名前を、`position` にその値を格納してください

# --seed--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`.name` を使用する必要があります

```json
{
  "regex": "\\.name\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`.value` を使用する必要があります

```json
{
  "regex": "\\.value\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`home` は `Planet.EARTH` でなければなりません

```python
    def test_1(self):
        self.assertIs(home, Planet.EARTH, "--err-t1--")
```

`label` は `"EARTH"` と等しくなければなりません

```python
    def test_2(self):
        self.assertEqual(label, "EARTH", "--err-t2--")
```

`position` は `3` と等しくなければなりません

```python
    def test_3(self):
        self.assertEqual(position, 3, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Planet(Enum):
    MERCURY = 1
    VENUS = 2
    EARTH = 3

home = Planet.EARTH
label = home.name
position = home.value
```
