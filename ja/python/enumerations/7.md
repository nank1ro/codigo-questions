---
language: python
exerciseType: 1
---

# --description--

通常の `Enum` のメンバーは `<` で比較したり、数値に加算したりすることはできません。
メンバーが順序付けが必要な**レベル**を表す場合は、代わりに `IntEnum` を継承します：そのメンバーは整数でもあるため、比較、算術演算、ソートに対応しています：
```python
from enum import IntEnum

class Priority(IntEnum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

print(Priority.HIGH > Priority.LOW)  # True
print(Priority.LOW + 1)              # 2
print(int(Priority.MEDIUM))          # 2
```
`IntEnum` のメンバーはその整数値とも等しくなります：`Priority.LOW == 1` は `True` です。

# --instructions--

`is_urgent` に `Priority.HIGH` が `Priority.MEDIUM` より大きいかどうかを、`ordered` に `sorted()` でソートしたリスト `tasks` を、`score` に `Priority.LOW` と `Priority.HIGH` の合計を格納してください

# --seed--

```python
from enum import IntEnum

class Priority(IntEnum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

tasks = [Priority.HIGH, Priority.LOW, Priority.MEDIUM]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`sorted()` を使用する必要があります

```json
{
  "regex": "sorted\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`is_urgent` は `True` でなければなりません

```python
    def test_1(self):
        self.assertEqual(is_urgent, True, "--err-t1--")
```

`ordered` は `[Priority.LOW, Priority.MEDIUM, Priority.HIGH]` と等しくなければなりません

```python
    def test_2(self):
        self.assertEqual(ordered, [Priority.LOW, Priority.MEDIUM, Priority.HIGH], "--err-t2--")
```

`score` は `4` と等しくなければなりません

```python
    def test_3(self):
        self.assertEqual(score, 4, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import IntEnum

class Priority(IntEnum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

tasks = [Priority.HIGH, Priority.LOW, Priority.MEDIUM]
is_urgent = Priority.HIGH > Priority.MEDIUM
ordered = sorted(tasks)
score = Priority.LOW + Priority.HIGH
```
