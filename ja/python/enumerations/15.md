---
language: python
exerciseType: 4
---

# --description--

クラスの反復と名前によるメンバー検索は、ログ行やファイルのように外部から来るデータを処理する際に、うまく連携します。
クラスに対する辞書内包表記でメンバーごとに1つのエントリを用意し、次に `Level[name]` が入力される各文字列を対応するメンバーに変換します：
```python
counts = {level: 0 for level in Level}
counts[Level["INFO"]] += 1
```
列挙型は宣言順を保持するため、後で `counts` を反復すると、メンバーが同じ順序で得られます。

# --instructions--

項目を並べ替えて、自動的な値で `Level` を宣言し、すべてのメンバーのカウンターを用意し、リスト内の名前を数え、その後、各メンバー名とそのカウントを出力してください

# --answers--

```python
from enum import Enum, auto
```

```python
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
```

```python
counts = {level: 0 for level in Level}
```

```python
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
```

```python
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --solutions--

```python
from enum import Enum, auto
class Level(Enum):
    DEBUG = auto()
    INFO = auto()
    ERROR = auto()
counts = {level: 0 for level in Level}
for name in ["INFO", "ERROR", "INFO"]:
    counts[Level[name]] += 1
for level, count in counts.items():
    print(f"{level.name}: {count}")
```

# --output--

DEBUG: 0
INFO: 2
ERROR: 1
