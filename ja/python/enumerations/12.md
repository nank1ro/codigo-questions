---
language: python
exerciseType: 4
---

# --description--

列挙型は `match` 文（Python 3.10以降で利用可能）と自然に組み合わせられます。`match` 文は値を一連の `case` パターンと比較し、最初に一致したものを実行します：
```python
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
```
メンバーは必ず `Light.RED` のようにクラスと一緒に書いてください：`case RED:` のような裸の名前は何も比較せず、単に値を新しい変数 `RED` に捕捉してすべてに一致してしまいます。
ワイルドカードの `case _:` がデフォルトであり、その後のパターンには決して到達できないため、最後に置かなければなりません。

# --instructions--

項目を並べ替えて、列挙型 `Light` を宣言し、`RED` のケースを最初に、次に `GREEN` のケース、最後にデフォルトの順で `action()` を定義し、`Light.YELLOW` に対するアクションを出力してください

# --answers--

```python
from enum import Enum
```

```python
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
```

```python
def action(light):
```

```python
    match light:
```

```python
        case Light.RED:
            return "Stop"
```

```python
        case Light.GREEN:
            return "Go"
```

```python
        case _:
            return "Slow down"
```

```python
print(action(Light.YELLOW))
```

# --solutions--

```python
from enum import Enum
class Light(Enum):
    RED = 1
    YELLOW = 2
    GREEN = 3
def action(light):
    match light:
        case Light.RED:
            return "Stop"
        case Light.GREEN:
            return "Go"
        case _:
            return "Slow down"
print(action(Light.YELLOW))
```

# --output--

Slow down
