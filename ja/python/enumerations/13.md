---
language: python
exerciseType: 1
---

# --description--

列挙型のメンバーは**ハッシュ可能**なので、辞書のキーやセットの要素として使うことができます。
列挙型をキーとする辞書は、各メンバーにデータを結び付けるきれいな方法です。また、メンバーで検索する方は、打ち間違いのおそれがある生の文字列を使うよりも安全です：
```python
class Size(Enum):
    SMALL = 1
    LARGE = 2

prices = {Size.SMALL: 2.5, Size.LARGE: 4.5}
print(prices[Size.LARGE])  # 4.5
```
メンバーはどのコレクションにも現れることができるので、リスト、セット、内包表記について知っていることはすべてそのまま使えます：
```python
order = [Size.LARGE, Size.SMALL, Size.LARGE]
print(len(set(order)))  # 2
```

# --instructions--

`large_price` に `prices` から読み取った `Size.LARGE` の価格を、`total` に `order` 内のサイズの価格の合計を、`sizes` に `order` 内の異なるサイズのセットを格納してください

# --seed--

```python
from enum import Enum

class Size(Enum):
    SMALL = 1
    MEDIUM = 2
    LARGE = 3

prices = {Size.SMALL: 2.5, Size.MEDIUM: 3.5, Size.LARGE: 4.5}
order = [Size.LARGE, Size.SMALL, Size.LARGE]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`Size.LARGE` をキーとして `prices` を読み取る必要があります

```json
{
  "regex": "prices\\[\\s*Size\\.LARGE\\s*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`large_price` は `4.5` と等しくなければなりません

```python
    def test_1(self):
        self.assertEqual(large_price, 4.5, "--err-t1--")
```

`total` は `11.5` と等しくなければなりません

```python
    def test_2(self):
        self.assertEqual(total, 11.5, "--err-t2--")
```

`sizes` は `{Size.SMALL, Size.LARGE}` と等しくなければなりません

```python
    def test_3(self):
        self.assertEqual(sizes, {Size.SMALL, Size.LARGE}, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum

class Size(Enum):
    SMALL = 1
    MEDIUM = 2
    LARGE = 3

prices = {Size.SMALL: 2.5, Size.MEDIUM: 3.5, Size.LARGE: 4.5}
order = [Size.LARGE, Size.SMALL, Size.LARGE]
large_price = prices[Size.LARGE]
total = sum(prices[size] for size in order)
sizes = set(order)
```
