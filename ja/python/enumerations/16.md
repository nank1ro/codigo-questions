---
language: python
exerciseType: 1
---

# --description--

通常のメソッドに加えて、列挙型は `@classmethod` で**クラスメソッド**を定義できます。クラスメソッドは列挙型のクラスそのものを `cls` として受け取るので、メンバーを見つける別の方法を置くのに適した場所です：
```python
class Color(Enum):
    RED = auto()
    BLUE = auto()

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]

print(Color.parse(" blue "))  # Color.BLUE
```
`auto()`、メソッド、プロパティ、検索と組み合わせれば、独自の振る舞いを備えた列挙型を作れます。

# --instructions--

`auto()` を使って、メンバー `NORTH`、`EAST`、`SOUTH`、`WEST` をこの順序で持つ列挙型 `Direction` を宣言してください。
反対側のメンバーを返すメソッド `opposite()`（`NORTH` と `SOUTH`、`EAST` と `WEST`）と、大文字小文字と前後の空白を無視して `text` に一致する名前のメンバーを返すクラスメソッド `parse(text)` を追加してください

# --seed--

```python
from enum import Enum, auto
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`auto()` を使用する必要があります

```json
{
  "regex": "auto\\(\\)",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`@classmethod` デコレータを使用する必要があります

```json
{
  "regex": "@classmethod",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`Direction` は、`1` から `4` の値を持つメンバー `NORTH`、`EAST`、`SOUTH`、`WEST` を持たなければなりません

```python
    def test_1(self):
        self.assertEqual([d.name for d in Direction], ["NORTH", "EAST", "SOUTH", "WEST"], "--err-t1--")
        self.assertEqual([d.value for d in Direction], [1, 2, 3, 4], "--err-t1--")
```

`Direction.NORTH.opposite()` は `Direction.SOUTH` であり、`Direction.WEST.opposite()` は `Direction.EAST` でなければなりません

```python
    def test_2(self):
        self.assertIs(Direction.NORTH.opposite(), Direction.SOUTH, "--err-t2--")
        self.assertIs(Direction.SOUTH.opposite(), Direction.NORTH, "--err-t2--")
        self.assertIs(Direction.EAST.opposite(), Direction.WEST, "--err-t2--")
        self.assertIs(Direction.WEST.opposite(), Direction.EAST, "--err-t2--")
```

`Direction.parse(" west ")` は `Direction.WEST` でなければなりません

```python
    def test_3(self):
        self.assertIs(Direction.parse(" west "), Direction.WEST, "--err-t3--")
        self.assertIs(Direction.parse("North"), Direction.NORTH, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
from enum import Enum, auto

class Direction(Enum):
    NORTH = auto()
    EAST = auto()
    SOUTH = auto()
    WEST = auto()

    def opposite(self):
        opposites = {
            Direction.NORTH: Direction.SOUTH,
            Direction.SOUTH: Direction.NORTH,
            Direction.EAST: Direction.WEST,
            Direction.WEST: Direction.EAST,
        }
        return opposites[self]

    @classmethod
    def parse(cls, text):
        return cls[text.strip().upper()]
```
