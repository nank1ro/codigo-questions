---
language: python
exerciseType: 1
---

# --description--

組み込み関数 `set()` は、リストや文字列など任意のコレクションからセットを構築します。
セットは各値を一度しか保持しないため、これは**重複を取り除く**ための定番の方法です：
```python
nums = [1, 2, 2, 3]
unique = set(nums)
print(len(unique))  # 3
```
値が含まれているかどうかを確認するには `in` 演算子を使います。これは `True` か `False` を返します：
```python
print(2 in unique)  # True
print(9 in unique)  # False
```
セットに対するメンバーシップの確認は、要素が数千個あっても非常に高速です。

# --instructions--

変数 `unique` に `set()` を使って `nums` から構築したセットを、`has_three` に `in` を使って値 `3` が `unique` に含まれているかどうかを格納してください

# --seed--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`set()` を使用する必要があります

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`in` 演算子を使用する必要があります

```json
{
  "regex": "\\bin\\b",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`unique` は `{1, 2, 3, 4}` と等しくなければなりません

```python
    def test_1(self):
        self.assertEqual(unique, {1, 2, 3, 4}, "--err-t1--")
```

`has_three` は `True` でなければなりません

```python
    def test_2(self):
        self.assertEqual(has_three, True, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 2, 3, 3, 3, 4]
unique = set(nums)
has_three = 3 in unique
```
