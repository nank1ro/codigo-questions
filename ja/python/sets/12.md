---
language: python
exerciseType: 1
---

# --description--

セットには**ハッシュ可能**な要素、つまり変更できない値しか含められません：数値、文字列、`True`/`False`、そして**タプル**です。
リスト、辞書、または別のセットを追加しようとすると `TypeError` が発生します：
```python
points = set()
points.add((1, 2))  # ok, a tuple
points.add([1, 2])  # TypeError: unhashable type: 'list'
```
タプルのセットは、座標や (name, age) のような一意なペアを管理するのに便利です：
```python
visits = [(1, 2), (1, 2), (3, 4)]
print(len(set(visits)))  # 2
```

# --instructions--

`visits` に含まれる異なる座標のセットを `points` に格納し、それに点 `(0, 0)` を追加してから、それに含まれる異なる点の数を `count` に格納してください

# --seed--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`add()` を使用する必要があります

```json
{
  "regex": "\\.add\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`points` は `{(0, 0), (1, 2), (3, 4), (5, 6)}` と等しくなければなりません

```python
    def test_1(self):
        self.assertEqual(points, {(0, 0), (1, 2), (3, 4), (5, 6)}, "--err-t1--")
```

`count` は `4` と等しくなければなりません

```python
    def test_2(self):
        self.assertEqual(count, 4, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
visits = [(1, 2), (3, 4), (1, 2), (5, 6), (3, 4)]
points = set(visits)
points.add((0, 0))
count = len(points)
```
