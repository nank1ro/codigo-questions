---
language: python
exerciseType: 1
---

# --description--

2つのセットは、書かれた順序に関係なく同じ要素を含んでいれば**等しい**とみなされます：
```python
print({1, 2, 3} == {3, 1, 2})  # True
```
コレクションの長さとそのセットの長さを比較するのは、重複を検出する手早い方法です：セットの方が**小さい**場合、何らかの値が複数回出現しています：
```python
emails = ["a@x.com", "b@x.com", "a@x.com"]
print(len(set(emails)) < len(emails))  # True
```
リストのメソッド `count(value)` は、ある値が何回出現するかを教えてくれるので、*どの*値が重複しているかを見つけるのに役立ちます。

# --instructions--

リストの長さとそのセットの長さを比較して、`emails` に繰り返されたアドレスが含まれているかどうかを `has_duplicates` に格納し、次に集合内包表記と `count()` を使って、2回以上出現するアドレスのセットを `duplicates` に格納してください

# --seed--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`set()` を使用する必要があります

```json
{
  "regex": "set\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

集合内包表記を使用する必要があります

```json
{
  "regex": "\\{[^}]*\\bfor\\b[^}]*\\}",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_duplicates` は `True` でなければなりません

```python
    def test_1(self):
        self.assertEqual(has_duplicates, True, "--err-t1--")
```

`duplicates` は `{'ann@x.com', 'bob@x.com'}` と等しくなければなりません

```python
    def test_2(self):
        self.assertEqual(duplicates, {"ann@x.com", "bob@x.com"}, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
emails = ["ann@x.com", "bob@x.com", "ann@x.com", "cy@x.com", "bob@x.com"]
has_duplicates = len(set(emails)) < len(emails)
duplicates = {e for e in emails if emails.count(e) > 1}
```
