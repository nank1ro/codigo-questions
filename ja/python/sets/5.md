---
language: python
exerciseType: 1
---

# --description--

`pop()` はセットから**任意の要素**を削除し、それを返します。
セットには順序がないため、どの要素が削除されるかを選ぶことはできません。空のセットに対して `pop()` を呼び出すと `KeyError` が発生します：
```python
tickets = {101, 102, 103}
picked = tickets.pop()
print(len(tickets))  # 2
```
`clear()` は**すべて**の要素を削除し、空のセットを残します：
```python
tickets.clear()
print(len(tickets))  # 0
```

# --instructions--

`pop()` を使って `tickets` から1つの要素を削除し、それを変数 `picked` に格納してから、`clear()` を使ってセット `drafts` を空にしてください

# --seed--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`pop()` を使用する必要があります

```json
{
  "regex": "\\.pop\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`clear()` を使用する必要があります

```json
{
  "regex": "\\.clear\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`picked` は元の tickets のいずれかでなければなりません

```python
    def test_1(self):
        self.assertIn(picked, {101, 102, 103}, "--err-t1--")
```

`tickets` は2つの要素を含み、`picked` をもう含んではいけません

```python
    def test_2(self):
        self.assertEqual(len(tickets), 2, "--err-t2--")
        self.assertNotIn(picked, tickets, "--err-t2--")
```

`drafts` は空でなければなりません

```python
    def test_3(self):
        self.assertEqual(drafts, set(), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
tickets = {101, 102, 103}
drafts = {"draft1", "draft2"}
picked = tickets.pop()
drafts.clear()
```
