---
language: python
exerciseType: 3
---

# --description--

Python は**動的型付け**です：型は変数ではなく**値**に属します。
変数は値に付けられた名前にすぎず、いつでも別の型の値に付け替えることができます：
```python
x = 10
print(type(x))  # <class 'int'>
x = "ten"
print(type(x))  # <class 'str'>
```
宣言も変換も必要ありません：古い値は単に忘れられます。
これは便利ですが、プログラムが実行されるまで変数の型が分からないことも意味します。そのため、うっかり型を混ぜると、実行前にではなく実行時にエラーとして現れます。

# --instructions--

このコードは何を出力しますか？
```python
x = 10
x = "ten"
print(type(x))
```

# --answers--

- `<class 'str'>`：`x` は今や文字列を参照しているため
- `<class 'int'>`：`x` の型は最初の代入で固定されたため
- `TypeError`：文字列は整数の変数に代入できないため
- `<class 'int'> <class 'str'>`：`x` は両方の値を保持しているため

# --solutions--

- `<class 'str'>`：`x` は今や文字列を参照しているため
