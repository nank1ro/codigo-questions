---
language: python
exerciseType: 1
---

# --description--

多くの言語と違って、Python の整数には**最大サイズがありません**：`int` は必要なだけ桁を保持できるように大きくなるので、大きな計算も正確なままです：
```python
print(3 ** 100)  # 515377520732011331036461129765621272702107522001
```
一方、`float` は約15桁の有効数字しか保持しないので、同じ累乗も float としては精度を失います：
```python
print(3.0 ** 100)  # 5.153775207320113e+47
```
`str()` は任意の `int` に使えるので、数の桁数を数える手っ取り早い方法は、そのテキストの長さを測ることです。

# --instructions--

`2` の `200` 乗の値を `big` に格納してから、正の `int` を受け取り、その桁数を返す関数 `digits` を書いてください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`str()` を使用する必要があります

```json
{
  "regex": "str\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`big` は `2 ** 200` でなければなりません

```python
    def test_1(self):
        self.assertEqual(big, 2 ** 200, "--err-t1--")
        self.assertIsInstance(big, int, "--err-t1--")
```

`digits(big)` は `61` を返す必要があります

```python
    def test_2(self):
        self.assertEqual(digits(big), 61, "--err-t2--")
```

`digits(7)` は `1` を返し、`digits(10 ** 50)` は `51` を返す必要があります

```python
    def test_3(self):
        self.assertEqual(digits(7), 1, "--err-t3--")
        self.assertEqual(digits(10 ** 50), 51, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
big = 2 ** 200

def digits(number):
    return len(str(number))
```
