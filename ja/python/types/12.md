---
language: python
exerciseType: 3
---

# --description--

`float` は固定されたビット数で2進数として格納されるため、ほとんどの10進数は**近似**しかできません。誤差はごくわずかですが、算術では現れます：
```python
print(0.1 + 0.2)   # 0.30000000000000004
```
このため、float を完全な等価で比較すべきではありません。両側を丸めるか、微小な許容差の範囲で2つの数が等しいかを確認する `math.isclose()` を使います：
```python
import math
print(round(0.1 + 0.2, 2) == 0.3)  # True
print(math.isclose(0.1 + 0.2, 0.3))  # True
```
整数にはこの問題がありません：`1 + 2 == 3` は常に `True` です。

# --instructions--

このコードは何を出力しますか？
```python
print(0.1 + 0.2 == 0.3)
```

# --answers--

- `False`：`0.1 + 0.2` は `0.3` よりわずかに大きいため
- `True`：`0.1 + 0.2` は `0.3` であるため
- `0.3`：`print` は足し算の結果を表示するため
- `TypeError`：float は `==` で比較できないため

# --solutions--

- `False`：`0.1 + 0.2` は `0.3` よりわずかに大きいため
