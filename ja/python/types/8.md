---
language: python
exerciseType: 2
---

# --description--

`bool` は `int` の**サブクラス**です：数値が期待される場所では、`True` は `1` のように、`False` は `0` のように振る舞います：
```python
print(True + 1)     # 2
print(True == 1)    # True
print(False * 10)   # 0
```
`sum()` はリストの項目を合計するので、ブーリアンのリストを合計すると `True` の個数を**数える**ことになります：
```python
answers = [True, False, True]
print(sum(answers))  # 2
```
このサブクラス関係のため、`isinstance(True, int)` は `True` を返しますが、`type(True)` はやはり `bool` です。

# --instructions--

コードを完成させて、正解の数を数え、`True` に `1` を加え、`True` が `int` のインスタンスかどうかを確認して、`3`、`2`、`True` を出力してください

# --seed--

```python
answers = [True, False, True, True]
correct = [/](answers)
print(correct)
print(True [/] 1)
print(isinstance(True, [/]))
```

# --answers--

- sum
- +
- int
- len
- ==
- str

# --solutions--

```python
answers = [True, False, True, True]
correct = sum(answers)
print(correct)
print(True + 1)
print(isinstance(True, int))
```

# --output--

3
2
True
