---
language: python
exerciseType: 2
---

# --description--

変換は組み合わせられます。`int("3.7")` は失敗しますが、`float("3.7")` はうまくいき、`float` に対する `int()` は小数部分を切り捨てます：
```python
number = float("3.7")   # 3.7
print(int(number))      # 3
```
`bool()` は真偽のルールに従います：`bool("")` は `False` で、`bool("False")` は空でない文字列であるため `True` になることに注意してください。

# --instructions--

コードを完成させて、`text` を数値に変換し、その2倍を出力し、次にその数値を `int` に変換したものと空の文字列のブーリアン値を出力して、`7.4`、`3`、`False` と表示されるようにしてください

# --seed--

```python
text = "3.7"
number = [/](text)
print(number * 2)
print([/](number))
print([/](""))
```

# --answers--

- float
- int
- bool
- str
- len

# --solutions--

```python
text = "3.7"
number = float(text)
print(number * 2)
print(int(number))
print(bool(""))
```

# --output--

7.4
3
False
