---
language: python
exerciseType: 2
---

# --description--

Python のすべての値には**型**があり、それがどんな種類のデータで、何ができるのかを教えてくれます。
基本的な組み込み型は次のとおりです：
- `int`：`42` や `-3` のような整数
- `float`：`3.5` のような小数部分を持つ数値
- `str`：`"hello"` のようなテキスト
- `bool`：`True` と `False` の2つの値のいずれか
- `NoneType`：「値がない」ことを意味する特別な値 `None` の型

組み込み関数 `type()` は値の型を返します。printするとクラス名が表示されます：
```python
print(type(42))     # <class 'int'>
print(type("hi"))   # <class 'str'>
```
`NoneType` を自分で書くことはありません：`type(None)` はそれを返しますが、この名前は他の4つのような組み込みではありません。

# --instructions--

コードを完成させて、`int`、`float`、`str`、`bool` の型をこの順序で出力してください

# --seed--

```python
print(type([/]))
print(type([/]))
print(type([/]))
print(type([/]))
```

# --answers--

- 42
- 3.5
- "hi"
- True
- None
- int

# --solutions--

```python
print(type(42))
print(type(3.5))
print(type("hi"))
print(type(True))
```

# --output--

<class 'int'>
<class 'float'>
<class 'str'>
<class 'bool'>
