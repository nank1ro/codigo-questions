---
language: python
exerciseType: 1
---

# --description--

変数を更新すると、以前の値は忘れられます。ここでは、`x`変数を2回表示して、値がどのように更新されるかを確認できます。
```python
>>> x = 5
>>> print(x)
5
>>> x = 10
>>> print(x)
10
```

# --instructions--

変数`x`の値を文字列`"updated"`に更新してください。

# --seed--

```python
x = 1
# write after this line
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`x`は`"updated"`と等しくなければなりません。

```python
    def test_1(self):
        self.assertEqual(x, "updated", "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
x = 1
x = "updated"
```
