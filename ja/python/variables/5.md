---
language: python
exerciseType: 1
---

# --description--

文字列変数は、シングルクォートまたはダブルクォートのどちらを使っても宣言できます：
```python
>>> x = "May"
>>> x = 'May'
```
どちらも同じ意味です。

# --instructions--

`month`という名前の変数に`june`という値を宣言してください。

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`month`は`"june"`と等しくなければなりません。

```python
    def test_1(self):
        self.assertEqual(month, "june", "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
month = "june"
```
