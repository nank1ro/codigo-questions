---
language: python
exerciseType: 1
---

# --description--

他の変数の値を変数に代入することもできます。ここでは、`y`変数に`x`の値を代入できます。
```python
>>> x = 5
>>> y = x
>>> print(y)
5
```

# --instructions--

新しい変数`y`を作成し、変数`x`の値を代入してください。

# --seed--

```python
x = 1
# write after this line
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`y`は`x`と等しくなければなりません。

```python
    def test_1(self):
        self.assertEqual(x, y, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
x = 1
y = x
```
