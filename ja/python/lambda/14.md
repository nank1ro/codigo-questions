---
language: python
exerciseType: 1
---

# --description--

**では、いつ `def` を使うべきでしょうか？**
lambdaは、引数として渡す短い使い捨ての関数に最適です。
ロジックに名前、複数行、docstring が必要な場合や、多くの場所で再利用される場合は、`def` 関数の方が明確です。
最後にもう一つ：`sorted()` は `reverse=True` も受け取ることができ、最大の値から先に取得できます。
```python
print(sorted([3, 1, 2], reverse=True))  # [3, 2, 1]
```

# --instructions--

`ranking` に、`grade` が高い順から低い順に並べ替えたリスト `students` を格納し、次に `names` に `ranking` の名前のリストを、`sorted()` と `map()` をlambdaと共に使用して格納してください

# --seed--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`lambda` 式を使用する必要があります

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`map()` を使用する必要があります

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`ranking` は生徒 `Cy` から始まらなければなりません

```python
    def test_1(self):
        self.assertEqual(ranking[0]["name"], "Cy", "--err-t1--")
```

`names` は `['Cy', 'Ana', 'Bo']` と等しくなければなりません

```python
    def test_2(self):
        self.assertEqual(names, ["Cy", "Ana", "Bo"], "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
students = [
    {"name": "Ana", "grade": 88},
    {"name": "Bo", "grade": 72},
    {"name": "Cy", "grade": 95},
]
ranking = sorted(students, key=lambda s: s["grade"], reverse=True)
names = list(map(lambda s: s["name"], ranking))
```
