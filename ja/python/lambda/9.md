---
language: python
exerciseType: 1
---

# --description--

`map()` はリストの**すべてのアイテム**に関数を適用します。
これは特別な _mapオブジェクト_ を返すので、値を見るには `list()` で包んでください。
```python
nums = [1, 2, 3]
doubled = list(map(lambda n: n * 2, nums))
print(doubled)  # [2, 4, 6]
```

# --instructions--

変数 `squares` に、`map()` をlambdaと共に使用して `nums` の各要素の2乗のリストを格納してください

# --seed--

```python
nums = [1, 2, 3, 4]
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`lambda` 式を使用する必要があります

```json
{
  "regex": "lambda",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`map()` を使用する必要があります

```json
{
  "regex": "map\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`squares` は `[1, 4, 9, 16]` と等しくなければなりません

```python
    def test_1(self):
        self.assertEqual(squares, [1, 4, 9, 16], "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
nums = [1, 2, 3, 4]
squares = list(map(lambda n: n * n, nums))
```
