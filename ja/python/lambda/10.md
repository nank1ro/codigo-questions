---
language: python
exerciseType: 2
---

# --description--

`filter()` は関数が `True` を返すアイテムだけを残します。
```python
nums = [5, 12, 8, 20]
big = list(filter(lambda n: n > 10, nums))
print(big)  # [12, 20]
```

# --instructions--

コードを完成させて偶数だけを残し、プログラムが `[2, 4]` を出力するようにしてください

# --seed--

```python
nums = [1, 2, 3, 4]
evens = list([/](lambda n: n % 2 [/] 0, nums))
print(evens)
```

# --answers--

- filter
- map
- sorted
- ==
- !=

# --solutions--

```python
nums = [1, 2, 3, 4]
evens = list(filter(lambda n: n % 2 == 0, nums))
print(evens)
```

# --output--

[2, 4]
