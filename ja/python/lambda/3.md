---
language: python
exerciseType: 2
---

# --description--

lambdaは**複数のパラメータ**を取ることができます。
`def` 関数の場合とまったく同じように、カンマで区切ります。
```python
power = lambda base, exp: base ** exp
print(power(2, 3))  # 8
```

# --instructions--

パラメータ `a` と `b` を取り、それらの合計を返すようにlambdaを完成させてください

# --seed--

```python
add = lambda a[/] b[/] a + b
print(add(3, 4))
```

# --answers--

- ,
- :
- ;
- ->

# --solutions--

```python
add = lambda a, b: a + b
print(add(3, 4))
```

# --output--

7
