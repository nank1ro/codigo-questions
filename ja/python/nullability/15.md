---
language: python
exerciseType: 1
---

# --instructions--

`"name"` キーと省略可能な `"age"` キー（存在しない、または `None` の可能性がある）を持つ辞書のリストを受け取る関数 `oldest` を書いてください。最も高い年齢の人物の名前を返す必要があります。年齢のない人物は無視し、誰も年齢を持たないときは `None` を返します

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`get` を使用する必要があります

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`is None` または `is not None` でチェックする必要があります

```json
{
  "regex": "is\\s+(not\\s+)?None",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

Ada (36)、Linus (28)、Grace (45) の中で最も年上なのは `"Grace"` でなければなりません

```python
    def test_1(self):
        people = [{"name": "Ada", "age": 36}, {"name": "Linus", "age": 28}, {"name": "Grace", "age": 45}]
        self.assertEqual(oldest(people), "Grace", "--err-t1--")
```

年齢が存在しない、または `None` の人物は無視される必要があります

```python
    def test_2(self):
        people = [{"name": "Tim"}, {"name": "Ada", "age": 36}, {"name": "Bob", "age": None}]
        self.assertEqual(oldest(people), "Ada", "--err-t2--")
```

誰も年齢を持たないときは結果が `None` でなければならず、年齢 `0` も数えられる必要があります

```python
    def test_3(self):
        self.assertIsNone(oldest([{"name": "Tim"}, {"name": "Bob", "age": None}]), "--err-t3--")
        self.assertIsNone(oldest([]), "--err-t3--")
        self.assertEqual(oldest([{"name": "Baby", "age": 0}, {"name": "Tim"}]), "Baby", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def oldest(people):
    best_name = None
    best_age = None
    for person in people:
        age = person.get("age")
        if age is None:
            continue
        if best_age is None or age > best_age:
            best_age = age
            best_name = person["name"]
    return best_name
```
