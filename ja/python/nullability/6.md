---
language: python
exerciseType: 2
---

# --description--

`[]` で辞書から存在しないキーを読もうとすると、`KeyError` が発生します。`get` メソッドは安全な代替手段です。キーが存在すればその値を返し、存在しなければ `None` を返します：
```python
ages = {"Ada": 36}
print(ages.get("Ada"))    # 36
print(ages.get("Grace"))  # None
```
`get` は第2引数を受け取ります。キーが見つからないときに `None` の**代わりに**返す値です：
```python
print(ages.get("Grace", 0))  # 0
```
これは、日常のコードで `None` が現れる最も一般的なケースです。何も見つからなかった検索です。

# --instructions--

コードを完成させて、`Ada` の年齢を出力し、次に存在しない `Grace` に対して `None` を出力し、さらにデフォルトを使って `Grace` に対して `-1` を出力して、`36`、`None`、`-1`、`36` を出力してください

# --seed--

```python
ages = {"Ada": 36}
print(ages.get([/]))
print(ages.get("Grace"))
print(ages.get("Grace", [/]))
print(ages.get("Ada", -1))
```

# --answers--

- "Ada"
- "Grace"
- -1
- None
- 0

# --solutions--

```python
ages = {"Ada": 36}
print(ages.get("Ada"))
print(ages.get("Grace"))
print(ages.get("Grace", -1))
print(ages.get("Ada", -1))
```

# --output--

36
None
-1
36
