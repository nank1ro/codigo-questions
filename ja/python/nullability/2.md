---
language: python
exerciseType: 1
---

# --description--

すべての関数呼び出しは値を生み出します。たとえ関数が何も返していないように見えてもです。`return` 文が**ない**関数や、裸の `return` だけの関数は `None` を返します：
```python
def say_hello():
    print("hello")

result = say_hello()
print(result)  # None
```
これが `print(my_list.append(3))` が `None` と表示される理由です：`append` はリストをその場で変更し、何も返しません。
動作だけを行う関数（print、保存、リストの変更）は通常 `None` を返し、何かを計算する関数は明示的に `return` しなければなりません。

# --instructions--

値を受け取り、何も返さずにリスト `saved` に追加する関数 `store` を書いてください

# --seed--

```python
saved = []

def store(value):
    pass
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`append` を使用する必要があります

```json
{
  "regex": "\\.append\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`store("a")` は `None` を返す必要があります

```python
    def test_1(self):
        self.assertIsNone(store("a"), "--err-t1--")
```

`store("b")` の後、リスト `saved` は `"b"` で終わる必要があります

```python
    def test_2(self):
        store("b")
        self.assertEqual(saved[-1], "b", "--err-t2--")
```

格納されたすべての値は順番通りに保持される必要があります

```python
    def test_3(self):
        before = len(saved)
        store(1)
        store(2)
        self.assertEqual(saved[before:], [1, 2], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
saved = []

def store(value):
    saved.append(value)
```
