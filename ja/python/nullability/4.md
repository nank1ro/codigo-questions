---
language: python
exerciseType: 2
---

# --description--

`None` は条件の中で**偽**として扱われるため、`value` が `None` なら `if not value:` は `True` になります。これを `None` チェックとして使いたくなりますが、同じテストは `0`、`""`、`[]` およびその他のすべての空の値に対しても `True` になります：
```python
count = 0
if not count:
    print("missing?")   # printed, but 0 is a real value!
```
「値がない」ことと「値が空である」ことを別々に扱わなければならないときは、まず `is None` をチェックし、それから真偽値を調べます：
```python
if count is None:
    print("no count yet")
elif not count:
    print("count is zero")
```
`if not value:` を使うのは、`None` と空の値を本当に同じように扱いたいときだけにしてください。

# --instructions--

関数を完成させて、`None` に対しては `missing` を、空の値に対しては `empty` を、それ以外には `present` を返すようにし、`missing`、`empty`、`present` を出力してください

# --seed--

```python
def describe(value):
    if value [/] None:
        return "missing"
    if [/] value:
        return "empty"
    return "present"

print(describe(None))
print(describe(0))
print(describe("hi"))
```

# --answers--

- is
- not
- is not
- in
- and

# --solutions--

```python
def describe(value):
    if value is None:
        return "missing"
    if not value:
        return "empty"
    return "present"

print(describe(None))
print(describe(0))
print(describe("hi"))
```

# --output--

missing
empty
present
