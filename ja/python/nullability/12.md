---
language: python
exerciseType: 1
---

# --description--

検索の後に `None` チェックが続くとき、通常は2行必要です。結果を保存する1行と、それをテストする1行です。**代入式**の演算子 `:=` は*セイウチ*と呼ばれ、式の**中**で値を代入するので、両方のステップが `if` に収まります：
```python
ages = {"Ada": 36}
if (age := ages.get("Ada")) is not None:
    print(age + 1)  # 37
```
括弧は必須です。括弧がないと、`:=` は比較全体を代入しようとしてしまいます。`if` の後も、`age` は他の変数と同じように使えます。

# --instructions--

名前を年齢に対応させる辞書 `users` と `name` を受け取る関数 `describe_user` を書いてください。名前が存在するときは `"<name> is <age>"` を返し、そうでなければ `"unknown user"` を返す必要があります。セイウチ演算子を使って `get` の結果を保存してください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`:=` 演算子を使用する必要があります

```json
{
  "regex": ":=",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`get` を使用する必要があります

```json
{
  "regex": "\\.get\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`describe_user({"Ada": 36}, "Ada")` は `"Ada is 36"` を返す必要があります

```python
    def test_1(self):
        self.assertEqual(describe_user({"Ada": 36}, "Ada"), "Ada is 36", "--err-t1--")
```

`describe_user({"Ada": 36}, "Grace")` は `"unknown user"` を返す必要があります

```python
    def test_2(self):
        self.assertEqual(describe_user({"Ada": 36}, "Grace"), "unknown user", "--err-t2--")
```

年齢 `0` でも存在するものとして数えられる必要があります

```python
    def test_3(self):
        self.assertEqual(describe_user({"Tim": 0}, "Tim"), "Tim is 0", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def describe_user(users, name):
    if (age := users.get(name)) is not None:
        return f"{name} is {age}"
    return "unknown user"
```
