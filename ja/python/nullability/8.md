---
language: python
exerciseType: 4
---

# --description--

`None` を受け取る可能性のある関数は、しばしば**ガード**から始まります。これは、作業すべきものがないときに早く返す `if` です。こうすることで、関数の残りの部分では、すべてを `else` の中に入れ子にせずに、値が存在すると仮定できます：
```python
def shout(text):
    if text is None:
        return None
    return text.upper() + "!"
```
ガードは最初に、チェックが行われなければならない順序で置きます。`text` が `None` でないと分かる前に `text.split()` を呼ぶことはできません。

# --instructions--

アイテムを並べ替えて、`text` が `None` または単語を含まないときに `None` を返し、そうでなければ最初の単語を返す `first_word` を定義してください。依存するコードの前にガードを置いてください

# --answers--

```python
def first_word(text):
```

```python
    if text is None:
        return None
```

```python
    words = text.split()
```

```python
    if len(words) == 0:
        return None
```

```python
    return words[0]
```

```python
print(first_word("hello world"))
print(first_word(None))
print(first_word("   "))
```

# --solutions--

```python
def first_word(text):
    if text is None:
        return None
    words = text.split()
    if len(words) == 0:
        return None
    return words[0]
print(first_word("hello world"))
print(first_word(None))
print(first_word("   "))
```

# --output--

hello
None
None
