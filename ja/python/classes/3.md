---
language: python
exerciseType: 2
---

# --description--

`pass`を別のものに置き換えましょう。
`__init__()`メソッドはクラスに必須であり、作成するオブジェクトを__初期化__するために使用されます。
`__init__()`は常に少なくとも1つの引数`self`を取り、これは作成されるオブジェクトを参照します。
`__init__()`は、クラスが作成する各オブジェクトを起動するメソッドと考えることができます。

# --instructions--

`Animal`クラスに`__init__()`メソッドを定義してください。
今のところ、引数として`self`を渡してください。
最後に、`__init__()`の定義のコードブロックに`pass`を入れてください

# --seed--

```python
class Animal:
    def [/]([/]):
        [/]
```

# --answers--

- __init__
- self
- object
- pass

# --solutions--

```python
class Animal:
    def __init__(self):
        pass
```
