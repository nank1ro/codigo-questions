---
language: python
exerciseType: 3
---

# --description--

文字列は**イミュータブル（不変）**です：一度作られると、その文字を変更することはできません。
インデックスへの代入は `TypeError` を発生させます：
```python
word = "dog"
word[1] = "i"  # TypeError: 'str' object does not support item assignment
```
文字列を「変更」するには、たとえばスライスと連結を使って新しい文字列を作り、それを変数に格納します：
```python
word = word[0] + "i" + word[2:]
print(word)  # dig
```

# --instructions--

このコードを実行すると何が起こりますか？
```python
word = "cat"
word[0] = "b"
print(word)
```

# --answers--

- `TypeError`：文字列はイミュータブルなので、その場で文字を置き換えることはできない
- `bat` と表示される
- `b` と表示される
- `cat` と表示され、代入は黙って無視される

# --solutions--

- `TypeError`：文字列はイミュータブルなので、その場で文字を置き換えることはできない
