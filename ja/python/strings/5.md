---
language: python
exerciseType: 1
---

# --description--

**スライス**は文字列の一部を取り出します。
角括弧の中に `[start:end]` と書くと、`start` の位置の文字は含まれ、`end` の位置の文字は含まれません：
```python
word = "python"
print(word[0:3])  # pyt
print(word[2:4])  # th
```
`start` を省略すると先頭から、`end` を省略すると末尾までスライスできます：
```python
print(word[:2])  # py
print(word[2:])  # thon
```
スライスはエラーを起こしません。`end` が文字列の長さより大きい場合は、単に最後の文字で止まります。

# --instructions--

スライスを使って、`word` の最初の4文字を変数 `first` に、残りの文字をすべて変数 `rest` に格納してください

# --seed--

```python
word = "notebook"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

スライス `[:]` を使う必要があります

```json
{
  "regex": "\\[[^\\]]*:[^\\]]*\\]",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`first` は `"note"` と等しくなければなりません

```python
    def test_1(self):
        self.assertEqual(first, "note", "--err-t1--")
```

`rest` は `"book"` と等しくなければなりません

```python
    def test_2(self):
        self.assertEqual(rest, "book", "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
word = "notebook"
first = word[:4]
rest = word[4:]
```
