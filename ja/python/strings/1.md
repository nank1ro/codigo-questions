---
language: python
exerciseType: 2
---

# --description--

**文字列（string）**とは、引用符で囲まれた文字の並び、つまりテキストの一部です。
Pythonではシングルクォート `'...'` とダブルクォート `"..."` のどちらも使え、両者は全く同じように動作します：
```python
name = 'Ada'
language = "Python"
```
テキスト自体に引用符が含まれる場合は、どちらを選ぶかが重要になります。
シングルクォートの中にアポストロフィがあると、そこで文字列が早く終わってしまうので、その場合はダブルクォートで囲みましょう：
```python
print("It's sunny")  # It's sunny
```

# --instructions--

正しい種類の引用符でコードを完成させ、プログラムが `It's Python` と表示されるようにしてください

# --seed--

```python
msg = [/]It's Python"
print(msg)
```

# --answers--

- "
- '
- `

# --solutions--

```python
msg = "It's Python"
print(msg)
```

# --output--

It's Python
