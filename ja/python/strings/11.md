---
language: python
exerciseType: 1
---

# --description--

`split()` は文字列を分割して断片の**リスト**にします。
引数がない場合は空白で分割し、引数がある場合はその区切り文字で分割します：
```python
print("a b  c".split())      # ['a', 'b', 'c']
print("2024-01-31".split("-"))  # ['2024', '01', '31']
```
`join()` はその逆で、リストの要素を1つの文字列にくっつけます。
これは**区切り文字**に対して呼び出され、リストは引数として渡されます：
```python
words = ["one", "two", "three"]
print(", ".join(words))  # one, two, three
print("".join(words))    # onetwothree
```

# --instructions--

`sentence` の単語のリストを `words` に格納し、それらの単語を `-` でつなげたものを `joined` に格納してください

# --seed--

```python
sentence = "the quick brown fox"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`split()` を使う必要があります

```json
{
  "regex": "\\.split\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`join()` を使う必要があります

```json
{
  "regex": "\\.join\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`words` は `['the', 'quick', 'brown', 'fox']` と等しくなければなりません

```python
    def test_1(self):
        self.assertEqual(words, ["the", "quick", "brown", "fox"], "--err-t1--")
```

`joined` は `"the-quick-brown-fox"` と等しくなければなりません

```python
    def test_2(self):
        self.assertEqual(joined, "the-quick-brown-fox", "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
sentence = "the quick brown fox"
words = sentence.split()
joined = "-".join(words)
```
