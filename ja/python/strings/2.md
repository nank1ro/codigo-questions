---
language: python
exerciseType: 1
---

# --description--

組み込み関数 `len()` は文字列の**長さ**、つまりそれが含む文字数を返します。
スペースや句読点も文字として数えられます：
```python
print(len("hello"))     # 5
print(len("hi there"))  # 8
print(len(""))          # 0
```

# --instructions--

`len()` を使って、`word` の文字数を変数 `length` に格納してください

# --seed--

```python
word = "notebook"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`len()` を使う必要があります

```json
{
  "regex": "len\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`length` は `8` と等しくなければなりません

```python
    def test_1(self):
        self.assertEqual(length, 8, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
word = "notebook"
length = len(word)
```
