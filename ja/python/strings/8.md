---
language: python
exerciseType: 1
---

# --description--

文字列には多くの組み込み**メソッド**が用意されています：文字列の後にドットを付けて呼び出す関数です。
`upper()` はテキストを大文字にして返し、`lower()` は小文字にして返します：
```python
word = "Python"
print(word.upper())  # PYTHON
print(word.lower())  # python
print(word)          # Python
```
これらのメソッドは**新しい文字列を返す**ことに注意してください：元の `word` は変更されません。
`lower()` は大文字小文字を無視してテキストを比較するのによく使われます：`"Yes".lower() == "yes"`。

# --instructions--

`word` の大文字版を `shout` に、小文字版を `quiet` に格納してください

# --seed--

```python
word = "Python"
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`shout` は `"PYTHON"` と等しくなければなりません

```python
    def test_1(self):
        self.assertEqual(shout, "PYTHON", "--err-t1--")
```

`quiet` は `"python"` と等しくなければなりません

```python
    def test_2(self):
        self.assertEqual(quiet, "python", "--err-t2--")
```

`word` はまだ `"Python"` と等しくなければなりません

```python
    def test_3(self):
        self.assertEqual(word, "Python", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
word = "Python"
shout = word.upper()
quiet = word.lower()
```
