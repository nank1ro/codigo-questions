---
language: python
exerciseType: 1
---

# --description--

**スラッグ（slug）**とは、タイトルをURLに使いやすい形式にしたものです：小文字で、前後にスペースがなく、単語はダッシュで区切られます。例：`hello-world`。
スラッグを作るのは、これまで学んだメソッド `strip()`、`lower()`、`replace()` をつなげるだけです。

# --instructions--

`title` の前後のスペースを取り除き、小文字にし、すべてのスペースを `-` に置き換えたものを返す関数 `slugify(title)` を書いてください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`replace()` を使う必要があります

```json
{
  "regex": "\\.replace\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`slugify("Hello World")` は `"hello-world"` を返さなければなりません

```python
    def test_1(self):
        self.assertEqual(slugify("Hello World"), "hello-world", "--err-t1--")
```

`slugify("  Learn Python Strings  ")` は `"learn-python-strings"` を返さなければなりません

```python
    def test_2(self):
        self.assertEqual(slugify("  Learn Python Strings  "), "learn-python-strings", "--err-t2--")
```

`slugify("codigo")` は `"codigo"` を返さなければなりません

```python
    def test_3(self):
        self.assertEqual(slugify("codigo"), "codigo", "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def slugify(title):
    return title.strip().lower().replace(" ", "-")
```
