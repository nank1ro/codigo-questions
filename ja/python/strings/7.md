---
language: python
exerciseType: 1
---

# --description--

`in` 演算子は、ある文字列が別の文字列を**含んでいる**かどうかを調べます。
`True` か `False` を返すので、`if` の中で自然に使えます：
```python
email = "ada@mail.com"
print("@" in email)      # True
print("xyz" in email)    # False
if " " in email:
    print("No spaces allowed")
```
`not in` は逆の確認を行います。

# --instructions--

`in` 演算子を使って、`text` に少なくとも1つスペースが含まれていれば `True`、そうでなければ `False` を返す関数 `has_space(text)` を書いてください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`in` 演算子を使う必要があります

```json
{
  "regex": "\" \" in |' ' in ",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`has_space("hello world")` は `True` を返さなければなりません

```python
    def test_1(self):
        self.assertEqual(has_space("hello world"), True, "--err-t1--")
```

`has_space("hello")` は `False` を返さなければなりません

```python
    def test_2(self):
        self.assertEqual(has_space("hello"), False, "--err-t2--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def has_space(text):
    return " " in text
```
