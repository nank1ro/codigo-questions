---
language: python
exerciseType: 4
---

# --description--

すべての文字列メソッドは新しい文字列を返すので、メソッドを次々に**チェーン**させることができます。
各呼び出しは、その前の結果に対して作用します：
```python
text = "  Hello World  "
print(text.strip().lower())  # hello world
```
スライスは3つ目の値、**ステップ**も受け取れます。
ステップ `-1` は文字列を逆向きにたどるもので、これは文字列を反転させる定番のテクニックです：
```python
print("abc"[::-1])  # cba
```

# --instructions--

これらの項目を並べ替えて、`word` をきれいにし、それを反転させ、きれいにした単語が回文（逆から読んでも同じ）かどうかを表示してください

# --answers--

```python
word = "  Level  "
```

```python
clean = word.strip().lower()
```

```python
reversed_word = clean[::-1]
```

```python
print(clean == reversed_word)
```

# --solutions--

```python
word = "  Level  "
clean = word.strip().lower()
reversed_word = clean[::-1]
print(clean == reversed_word)
```

# --output--

True
