---
language: python
exerciseType: 1
---

# --description--

関数が何をするかを説明するために関数の上に置かれたコメントは、間違った場所にあります。そのための場所はdocstringです。docstringは関数に紐付けられ、`help()` はそれを見つけ、エディタはそれを表示します。一方、`def` の上にある `#` コメントは、そのどれからも見えません。

```python
# a と b を足す
def add(a, b):
    return a + b
```
同じ文を1行下へ、トリプルクォートの間に移動させると、本物のドキュメントになります：
```python
def add(a, b):
    """Returns the sum of a and b."""
    return a + b
```

# --instructions--

`subtract` の上にある2つの `#` コメントを削除し、代わりにdocstringで関数をドキュメント化してください

# --seed--

```python
# subtract
# this function subtracts b from a
def subtract(a, b):
    return a - b
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`subtract` の上の `#` コメントは削除されていなければなりません

```json
{
  "regex": "#\\s*this function subtracts",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`# subtract` コメントも削除されていなければなりません

```json
{
  "regex": "#\\s*subtract",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`subtract` はdocstringを持っていなければなりません

```python
    def test_1(self):
        self.assertIsNotNone(subtract.__doc__, "--err-t1--")
```

`subtract(5, 3)` は `2` を返さなければなりません

```python
    def test_2(self):
        self.assertEqual(subtract(5, 3), 2, "--err-t2--")
```

`subtract(0, 4)` は `-4` を返さなければなりません

```python
    def test_3(self):
        self.assertEqual(subtract(0, 4), -4, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def subtract(a, b):
    """Returns b subtracted from a."""
    return a - b
```
