---
language: python
exerciseType: 4
---

# --description--

docstringは本体の**最初の行**でなければならず、他のどの文よりも上に置きます。`return` の後や本体の他の場所に書かれた文字列は、単なる文字列にすぎません。`__doc__` は `None` のままになり、どのツールもそれを表示することはありません。

# --instructions--

本体の最初の行としてdocstringを持つ `to_seconds` を定義し、その後で `to_seconds(2)` を出力するプログラムになるようにアイテムを並べ替えてください

# --answers--

```python
def to_seconds(minutes):
```

```python
    """Returns the number of seconds in the given minutes."""
```

```python
    return minutes * 60
```

```python
print(to_seconds(2))
```

# --solutions--

```python
def to_seconds(minutes):
    """Returns the number of seconds in the given minutes."""
    return minutes * 60
print(to_seconds(2))
```

# --output--

120
