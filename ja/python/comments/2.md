---
language: python
exerciseType: 1
---

# --description--

コメントは独立した行である必要はありません。コードと同じ行に続けることができます。これは**行内コメント**と呼ばれ、その行の処理についての短いメモを書くのに適した場所です：
```python
retries = 3  # give up after three attempts
```
`#`から行の終わりまではすべて無視されますが、その前のコードは通常どおり実行されます。

Pythonのスタイルガイド**PEP 8**は、ここで少しスペースを入れることを求めています。コードと `#` の間には**2つ以上のスペース**、`#` の後には**1つのスペース**を入れます。独立した行にあるコメントは、`#` の後のスペースだけで構いません。

# --instructions--

`seats = 4` の後に値を説明する行内コメントを追加してください。例：`# seats per table`。`#` の前には2つのスペースを入れてください

# --seed--

```python
seats = 4
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`seats = 4` の行は `#` コメントで終わっていなければなりません。`#` の前には2つのスペース、`#` の後には1つのスペースを入れます

```json
{
  "regex": "seats = 4[ \\t]{2,}#[ \\t]\\S",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`seats` は `4` と等しいままでなければなりません

```python
    def test_1(self):
        self.assertEqual(seats, 4, "--err-t1--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
seats = 4  # seats per table
```
