---
language: python
exerciseType: 1
---

# --description--

エディタが理解できる慣習に従うコメントもあります。最も一般的な**マーカー**は次のとおりです：
- `# TODO: ...` は、まだ書く必要があるものを示します
- `# FIXME: ...` は、間違っていると分かっていて修正されなければならないコードを示します

```python
limit = 10
# TODO: read the limit from the settings
```
Pythonにとってこれらは普通のコメントですが、エディタは専用のパネルにそれらを集めるので、残されている作業を見つけやすくなります。`TODO` は通常、実際のコードが書かれるまでプログラムを動かし続けるプレースホルダーのそばに置かれます。

作業を終えたら、同じ変更の中でプレースホルダーを置き換え、マーカーを削除してください。そうすれば、コメントがコードの状態について嘘をつくことはありません。

# --instructions--

`is_even` の `TODO` コメントが示す作業を完成させ、作業が済んだのでコメントを削除してください

# --seed--

```python
def is_even(n):
    # TODO: return True when n is even, False otherwise
    return False
```

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`TODO` コメントは削除されていなければなりません

```json
{
  "regex": "TODO",
  "modifiers": ["m"],
  "shouldMatch": false
}
```

`is_even(4)` は `True` を返さなければなりません

```python
    def test_1(self):
        self.assertEqual(is_even(4), True, "--err-t1--")
```

`is_even(7)` は `False` を返さなければなりません

```python
    def test_2(self):
        self.assertEqual(is_even(7), False, "--err-t2--")
```

`is_even(0)` は `True` を返さなければなりません

```python
    def test_3(self):
        self.assertEqual(is_even(0), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_even(n):
    return n % 2 == 0
```
