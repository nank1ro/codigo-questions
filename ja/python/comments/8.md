---
language: python
exerciseType: 4
---

# --description--

ファイルもドキュメント化できます。どのimportや定義よりも前に、**ファイルの最初の文**として書かれた文字列は**モジュールdocstring**です。ファイル全体が何のためのものかを示します。
```python
"""Tools for working with rectangles."""

def area(width, height):
    """Returns the area of a rectangle."""
    return width * height
```
最初の文だけが対象になります。コメントはその上に置いても構いませんが、間に実際のコードがあると、文字列は役に立たない普通の文字列に戻ってしまいます。

# --instructions--

1行目がモジュールdocstringで始まり、次に `area` を定義し、最後に `area(3, 4)` を出力するプログラムになるようにアイテムを並べ替えてください

# --answers--

```python
"""Tools for working with rectangles."""
```

```python
def area(width, height):
    """Returns the area of a rectangle."""
    return width * height
```

```python
print(area(3, 4))
```

# --solutions--

```python
"""Tools for working with rectangles."""
def area(width, height):
    """Returns the area of a rectangle."""
    return width * height
print(area(3, 4))
```

# --output--

12
