---
language: python
exerciseType: 3
---

# --description--

良いコメントは、コードが**何を**するかではなく、**なぜ**それをするのかを説明します。コードはすでに何が起こるかを示しています。それを言葉で繰り返すのはノイズを加えるだけで、コードが変わるとすぐに古くなってしまいます：
```python
# timeout を30に設定
timeout = 30
```
その数字の裏にある理由こそ、読む人には推測できないものです：
```python
# サーバーは35秒アイドルだと接続を切るので、早めに止める
timeout = 30
```
コメントが下の行を言い換えているだけなら、削除するか理由に置き換えてください。最良のコメントは、コードでは語れないことを語るものです。

# --instructions--

この行の上にあるコメントとして、最も役立つものはどれですか？
```python
max_retries = 3
```

# --answers--

- `# 支払いAPIは4回失敗した後にクライアントをブロックする`
- `# max_retries を3に設定`
- `# max_retries は int 型の変数`
- `# この行は値を代入する`

# --solutions--

- `# 支払いAPIは4回失敗した後にクライアントをブロックする`
