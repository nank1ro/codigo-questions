---
language: python
exerciseType: 1
---

# --description--

`str.split` は固定のセパレーターでしか分割できません。`re.split(pattern, text)` はパターンが記述する任意のものを区切りとして分割できます。これは乱れた入力で通常必要とされる動作です：
```python
import re

print("a, b;c".split(","))            # ['a', ' b;c']
print(re.split(r"[,;]", "a, b;c"))    # ['a', ' b', 'c']
print(re.split(r"[,;\s]+", "a, b;c")) # ['a', 'b', 'c']
```
セパレーターを `[,;\s]+` と書くと、カンマ、セミコロン、空白の連なり全体が1回の分割として数えられ、それらの間に空文字列が残らなくなります。

`maxsplit` 引数は指定した回数の分割で止まり、テキストの残りを最後の要素に残します。`re.split(r"\s+", "a b c", maxsplit=1)` は `['a', 'b c']` になります。

# --instructions--

行内のフィールドのリストを返す関数 `split_fields(line)` を書いてください。カンマ、セミコロン、空白の連なりは1つのセパレーターとして扱います

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`split_fields` はカンマ、セミコロン、空白で分割できなければなりません

```python
    def test_1(self):
        self.assertEqual(split_fields("a, b;c"), ["a", "b", "c"], "--err-t1--")
        self.assertEqual(split_fields("red green blue"), ["red", "green", "blue"], "--err-t1--")
```

セパレーターの連なりは1回の分割になる必要があります

```python
    def test_2(self):
        self.assertEqual(split_fields("x ; ; y"), ["x", "y"], "--err-t2--")
        self.assertEqual(split_fields("1,,2"), ["1", "2"], "--err-t2--")
```

セパレーターを含まない行は1つのフィールドを返す必要があります

```python
    def test_3(self):
        self.assertEqual(split_fields("solo"), ["solo"], "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def split_fields(line):
    return re.split(r"[,;\s]+", line)
```
