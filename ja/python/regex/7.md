---
language: python
exerciseType: 4
---

# --description--

角括弧は**文字クラス**を定義します。文字の集合を表し、その位置でいずれか1文字が受け入れられます。`[aeiou]` は母音1文字、`[0-9]` は数字1文字、`[a-z]` は小文字の英字1文字にマッチします。開き括弧の直後の `^` は意味を反転させ、`[^0-9]` は数字*以外*の任意の文字にマッチします。

文字クラスの外では、`^` と `$` は**アンカー**です。`^` はパターンをテキストの先頭に、`$` は末尾に結び付けます。`re.fullmatch` ではアンカーが暗黙に含まれるため、バリデーションはそちらの方が読みやすくなります：
```python
import re

print(bool(re.fullmatch(r"[a-z]{3,8}", "ada")))  # True
print(bool(re.fullmatch(r"[a-z]{3,8}", "Ada")))  # False
```

# --instructions--

項目を並べ替えて、importで始まり、最後の `print` の前に `is_valid` を定義し、3〜8個の小文字からなる名前だけを受け入れ、3つの名前に対して結果を出力するプログラムを作成してください。出力が `True False False` になるようにしてください

# --answers--

```python
import re
```

```python
def is_valid(name):
```

```python
    return re.fullmatch(r"[a-z]{3,8}", name) is not None
```

```python
print(is_valid("ada"), is_valid("Ada"), is_valid("ab"))
```

# --solutions--

```python
import re
def is_valid(name):
    return re.fullmatch(r"[a-z]{3,8}", name) is not None
print(is_valid("ada"), is_valid("Ada"), is_valid("ab"))
```

# --output--

True False False
