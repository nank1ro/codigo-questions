---
language: python
exerciseType: 1
---

# --description--

マッチオブジェクトはマッチしたテキスト以上の情報を持っています。`.group()` のほかに、元の文字列の中でのマッチの位置も提供します：
```python
import re

match = re.search(r"\d+", "order 42 shipped")
print(match.group())  # 42
print(match.start())  # 6
print(match.end())    # 8
print(match.span())   # (6, 8)
```
`.start()` はマッチした最初の文字のインデックス、`.end()` は最後の文字の直後のインデックスで、`.span()` はその両方をタプルで返します。つまり `text[match.start():match.end()]` は常に `match.group()` と等しくなります。

`re.search` は `None` を返すことがあるため、何もマッチしなかったときに `.group()` をすぐに読むと `AttributeError` が発生します。まず結果を確認してください。

# --instructions--

最初の数字の並びとその開始インデックス、終了インデックスをタプルで返す関数 `first_number(text)` を書いてください。テキストに数字が含まれない場合は `None` を返します

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_number("order 42 shipped")` は `("42", 6, 8)` を返す必要があります

```python
    def test_1(self):
        self.assertEqual(first_number("order 42 shipped"), ("42", 6, 8), "--err-t1--")
```

テキストが数字で始まるとき、マッチはインデックス `0` から始まる必要があります

```python
    def test_2(self):
        self.assertEqual(first_number("7up"), ("7", 0, 1), "--err-t2--")
        self.assertEqual(first_number("100 items"), ("100", 0, 3), "--err-t2--")
```

数字がないとき `first_number` は `None` を返す必要があります

```python
    def test_3(self):
        self.assertIsNone(first_number("no digits here"), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import re

def first_number(text):
    match = re.search(r"\d+", text)
    if match is None:
        return None
    return (match.group(), match.start(), match.end())
```
