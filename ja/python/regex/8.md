---
language: python
exerciseType: 2
---

# --description--

`re.search` は最初のマッチで止まります。`re.findall(pattern, text)` はその代わりに**すべての**マッチを集め、文字列のリストとして返します：
```python
import re

print(re.findall(r"\d+", "a1 bb22 ccc333"))  # ['1', '22', '333']
print(re.findall(r"\d+", "no digits"))       # []
```
何もマッチしないときリストは空になるため、チェックすべき `None` はありません。そのままループで回したり `len(...)` で測ったりできます。`findall` はマッチオブジェクトではなく普通の文字列を返すため、位置は利用できないことに注意してください。

# --instructions--

すべての数字の並びを集め、リストに続いて見つかった個数を出力するようにコードを完成させ、出力が `['1', '22', '333'] 3` になるようにしてください

# --seed--

```python
import re

numbers = re.[/](r"\d+", "a1 bb22 ccc333")
print(numbers, [/](numbers))
```

# --answers--

- findall
- len
- search
- finditer
- sum

# --solutions--

```python
import re

numbers = re.findall(r"\d+", "a1 bb22 ccc333")
print(numbers, len(numbers))
```

# --output--

['1', '22', '333'] 3
