---
language: python
exerciseType: 1
---

# --description--

`any(iterable)` は少なくとも1つの要素が真ならば `True` を返し、`all(iterable)` はすべての要素が真ならば `True` を返します (`all([])` は `True` です)。ジェネレータ式と組み合わせるとコレクション全体に対する条件を表現でき、`or` や `and` と同じように短絡評価されます:
```python
print(any(ch.isdigit() for ch in "abc1"))  # True
print(all(ch.isalpha() for ch in "ab1"))   # False
```

# --instructions--

文字列が `8` 文字以上 `20` 文字以下で、少なくとも1つの数字を含み、空白を含まないときだけ `True` を返し、それ以外は `False` を返す関数 `is_valid_password(password)` を書いてください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`"secret123"` は有効で、ちょうど `8` 文字または `20` 文字のパスワードも有効です

```python
    def test_1(self):
        self.assertIs(is_valid_password("secret123"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefg1"), True, "--err-t1--")
        self.assertIs(is_valid_password("abcdefghij123456789x"), True, "--err-t1--")
```

短すぎる、または長すぎるパスワードは有効ではありません

```python
    def test_2(self):
        self.assertIs(is_valid_password("short1"), False, "--err-t2--")
        self.assertIs(is_valid_password("abcdefghij1234567890x"), False, "--err-t2--")
```

数字を含まない、または空白を含むパスワードは有効ではありません

```python
    def test_3(self):
        self.assertIs(is_valid_password("nodigitshere"), False, "--err-t3--")
        self.assertIs(is_valid_password("pass word1"), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_valid_password(password):
    return (
        8 <= len(password) <= 20
        and any(ch.isdigit() for ch in password)
        and " " not in password
    )
```
