---
language: python
exerciseType: 1
---

# --description--

短絡評価は、一部の値で失敗する処理を**ガード**する安全な方法です。`word is not None and len(word) < 4` では、`len(word)` は `word` が `None` でないときにだけ実行されるので、呼び出しが例外を送出することはありません。ガードは先に書かなければなりません:
```python
def is_short(word):
    return word is not None and len(word) < 4

print(is_short(None))   # False
print(is_short("cat"))  # True
```
`and` はオペランドを返すことに注意してください。`word and len(word) < 4` は `None` に対して `None` を、空文字列に対して `""` を返し、`False` にはなりません。ブール値が必要なときは本物の比較でガードしてください。

# --instructions--

リスト `numbers` が空でなく最初の要素が `0` のとき `True` を返し、それ以外は `False` を返す関数 `first_is_zero(numbers)` を書いてください。空のリストでも結果はブール値でなければなりません

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`first_is_zero([0, 5])` は `True` を返さなければなりません

```python
    def test_1(self):
        self.assertIs(first_is_zero([0, 5]), True, "--err-t1--")
        self.assertIs(first_is_zero([0]), True, "--err-t1--")
```

`first_is_zero([3, 0])` は `False` を返さなければなりません

```python
    def test_2(self):
        self.assertIs(first_is_zero([3, 0]), False, "--err-t2--")
```

`first_is_zero([])` は例外を送出せずに `False` を返さなければなりません

```python
    def test_3(self):
        self.assertIs(first_is_zero([]), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def first_is_zero(numbers):
    return len(numbers) > 0 and numbers[0] == 0
```
