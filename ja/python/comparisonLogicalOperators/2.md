---
language: python
exerciseType: 1
---

# --description--

比較演算子は数値に限りません。文字列はコードポイントを使って1文字ずつ比較されるため、`"apple" < "banana"` は `True` であり、大文字はすべて小文字より前に来るので `"Zoo" < "apple"` も `True` になります。リストとタプルも同じように要素ごとに比較されます:
```python
print("cat" < "dog")       # True
print([1, 2, 3] < [1, 3])  # True
print((1, 2) == (1, 2))    # True
```
比較は式なので、関数は `if` で包まずに直接 `return a < b` と書けます。

# --instructions--

文字列 `a` が文字列 `b` より前に並ぶとき `True` を返し、等しいか `a` が `b` より後のとき `False` を返す関数 `comes_first(a, b)` を書いてください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`comes_first("apple", "banana")` は `True` を返さなければなりません

```python
    def test_1(self):
        self.assertIs(comes_first("apple", "banana"), True, "--err-t1--")
```

`comes_first("pear", "apple")` と `comes_first("kiwi", "kiwi")` は `False` を返さなければなりません

```python
    def test_2(self):
        self.assertIs(comes_first("pear", "apple"), False, "--err-t2--")
        self.assertIs(comes_first("kiwi", "kiwi"), False, "--err-t2--")
```

大文字は小文字より前に来るので、`comes_first("Zoo", "apple")` は `True` を返さなければなりません

```python
    def test_3(self):
        self.assertIs(comes_first("Zoo", "apple"), True, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def comes_first(a, b):
    return a < b
```
