---
language: python
exerciseType: 1
---

# --description--

論理演算子はブール値を組み合わせます。`and` は両辺が `True` のときだけ `True` になり、`or` は少なくとも一方が `True` のとき、`not` は1つの値を反転します:
```python
age = 20
member = False
print(age >= 18 and member)  # False
print(age >= 18 or member)   # True
print(not member)            # True
```
比較は論理演算子より優先順位が高いので、`age >= 18 and member` に括弧は不要です。`and` の中に `or` をまとめるには括弧が必要です: `a and (b or c)`。

# --instructions--

`year` が `4` で割り切れて `100` で割り切れないとき `True` を返し、ただし `400` でも割り切れる場合は `True`、それ以外は `False` を返す関数 `is_leap(year)` を書いてください

# --before-asserts--

```python
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`2024` と `2000` はうるう年です

```python
    def test_1(self):
        self.assertIs(is_leap(2024), True, "--err-t1--")
        self.assertIs(is_leap(2000), True, "--err-t1--")
```

`2023` はうるう年ではありません

```python
    def test_2(self):
        self.assertIs(is_leap(2023), False, "--err-t2--")
```

`1900` は `100` で割り切れますが `400` では割り切れないので、うるう年ではありません

```python
    def test_3(self):
        self.assertIs(is_leap(1900), False, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
def is_leap(year):
    return year % 4 == 0 and (year % 100 != 0 or year % 400 == 0)
```
