---
language: python
exerciseType: 2
---

# --description--

読み込み、サーバーへの呼び出し、タイマーの待機など、時間のかかる操作があります。Python は `asyncio` モジュールでそのようなコードを**非同期に**実行でき、あるタスクが他のタスクを妨げることなく待機できます。

`async def` で定義された関数は**コルーチン関数**です。呼び出しても本体は実行されません：実行すべき作業を記述する**コルーチンオブジェクト**を返します。`asyncio.run(coro)` はイベントループを開始し、コルーチンを最後まで実行して、ループを停止します：
```python
import asyncio

async def greet():
    print("hello")

asyncio.run(greet())  # hello
```
`asyncio.run` は非同期プログラムのエントリーポイントであり、通常の同期コードから一度だけ呼び出されます。

# --instructions--

コードを完成させて、`greet` がコルーチン関数になり、プログラムが `asyncio.run` でそれを実行するようにし、`hello` を出力してください

# --seed--

```python
import asyncio

[/] def greet():
    print("hello")

asyncio.[/](greet())
```

# --answers--

- async
- run
- await
- def
- start

# --solutions--

```python
import asyncio

async def greet():
    print("hello")

asyncio.run(greet())
```

# --output--

hello
