---
language: python
exerciseType: 3
---

# --description--

`gather` に渡したコルーチンの1つが例外を発生させると、その例外は `await asyncio.gather(...)` の行に伝わり、他のコルーチンは実行を続けているのに、その結果は失われます。`return_exceptions=True` を渡すとこれが変わります：`gather` は決して例外を発生させず、リストの中で失われた結果の代わりに例外オブジェクトが入ります：
```python
import asyncio

async def ok():
    return 1

async def fail():
    raise ValueError("bad")

async def main():
    results = await asyncio.gather(ok(), fail(), return_exceptions=True)
    print(results)  # [1, ValueError('bad')]

asyncio.run(main())
```
その後、各要素を `isinstance(result, Exception)` でチェックすれば、失敗と値を区別できます。

# --instructions--

このコードは何を出力しますか？
```python
import asyncio

async def ok():
    return 1

async def fail():
    raise ValueError("bad")

async def main():
    results = await asyncio.gather(ok(), fail(), return_exceptions=True)
    print(len(results), isinstance(results[1], ValueError))

asyncio.run(main())
```

# --answers--

- `2 True` です。`return_exceptions=True` のときは例外が発生する代わりにリストに返されるためです
- `ValueError` です。どのコルーチンの例外も常に `gather` の外に伝わるためです
- `1 False` です。失敗したコルーチンは結果から除外されるためです
- `2 False` です。`gather` は失敗した結果を `None` に置き換えるためです

# --solutions--

- `2 True` です。`return_exceptions=True` のときは例外が発生する代わりにリストに返されるためです
