---
language: python
exerciseType: 1
---

# --description--

コルーチンは `await` でお互いを呼び出します。コルーチンは他のコルーチンを await し、その戻り値を受け取って処理を続けられます。関数呼び出しの連鎖と全く同じです：
```python
import asyncio

async def fetch_price(item):
    await asyncio.sleep(0.01)  # simulates a slow lookup
    return 10

async def total(item, quantity):
    price = await fetch_price(item)
    return price * quantity

print(asyncio.run(total("pen", 3)))  # 30
```
`asyncio.run` を通すのは最も外側のコルーチンだけで、内側のものはすべて `await` で到達します。もし `total` が普通の `def` だったら、`await` を一切使えません：待機が必要なチェーン内のすべての関数に `async` キーワードが広がるのです。

# --instructions--

`0.01` 秒待って、id を後ろに付けた文字列 `"user"` を返すコルーチン関数 `fetch_user(user_id)` と、`fetch_user` を await して、取得した名前の前に `"Hello, "` を付けた文字列を返すコルーチン関数 `greet_user(user_id)` を書いてください

# --before-asserts--

```python
import asyncio
import inspect
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch_user(1)` は `"user1"` を返し、`fetch_user(42)` は `"user42"` を返す必要があります

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch_user(1)), "user1", "--err-t1--")
        self.assertEqual(asyncio.run(fetch_user(42)), "user42", "--err-t1--")
```

`greet_user(1)` は `"Hello, user1"` を返す必要があります

```python
    def test_2(self):
        self.assertEqual(asyncio.run(greet_user(1)), "Hello, user1", "--err-t2--")
```

`fetch_user` と `greet_user` の両方がコルーチン関数である必要があります

```python
    def test_3(self):
        self.assertTrue(inspect.iscoroutinefunction(fetch_user), "--err-t3--")
        self.assertTrue(inspect.iscoroutinefunction(greet_user), "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch_user(user_id):
    await asyncio.sleep(0.01)
    return f"user{user_id}"

async def greet_user(user_id):
    name = await fetch_user(user_id)
    return f"Hello, {name}"
```
