---
language: python
exerciseType: 2
---

# --description--

`asyncio.wait_for(awaitable, timeout)` は対象を await しますが、`timeout` 秒後には諦めます。操作はキャンセルされ、`TimeoutError` が発生します。これは他の例外と同じように捕捉できます：
```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())  # timed out
```
タイムアウトが `0.1` なら、同じコードは `done` を出力します。`asyncio.TimeoutError` は組み込みの `TimeoutError` の別名です。

# --instructions--

コードを完成させて、`slow()` が `0.01` 秒のタイムアウト付きで await され、タイムアウトが捕捉されるようにし、`timed out` を出力してください

# --seed--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.[/](slow(), [/]=0.01)
        print(result)
    except [/]:
        print("timed out")

asyncio.run(main())
```

# --answers--

- wait_for
- timeout
- TimeoutError
- gather
- delay
- ValueError

# --solutions--

```python
import asyncio

async def slow():
    await asyncio.sleep(0.05)
    return "done"

async def main():
    try:
        result = await asyncio.wait_for(slow(), timeout=0.01)
        print(result)
    except TimeoutError:
        print("timed out")

asyncio.run(main())
```

# --output--

timed out
