---
language: python
exerciseType: 1
---

# --description--

操作の数が固定でないときこそ、並行処理が役立ちます。リスト内包表記でコルーチンオブジェクトを作り、それを `gather` に展開して、バッチ全体を一度に await します：
```python
import asyncio

async def fetch(url):
    await asyncio.sleep(0.02)
    return len(url)

async def fetch_all(urls):
    return await asyncio.gather(*[fetch(u) for u in urls])

print(asyncio.run(fetch_all(["a.com", "bb.com"])))  # [5, 6]
```
10個の URL でも合計では 200ミリ秒ではなく約20ミリ秒しかかからず、結果は URL の順序のまま保たれます。

# --instructions--

`0.02` 秒待って `item_id * 10` を返すコルーチン関数 `fetch(item_id)` と、すべての id に対して `fetch` を `asyncio.gather` で**並行して**実行し、結果のリストを返すコルーチン関数 `fetch_all(ids)` を書いてください

# --before-asserts--

```python
import asyncio
import time
import unittest

class CodigoTests(unittest.TestCase):
```

# --asserts--

`fetch(3)` は `30` を返す必要があります

```python
    def test_1(self):
        self.assertEqual(asyncio.run(fetch(3)), 30, "--err-t1--")
```

`fetch_all([1, 2, 3])` は `[10, 20, 30]` を返し、`fetch_all([])` は `[]` を返す必要があります

```python
    def test_2(self):
        self.assertEqual(asyncio.run(fetch_all([1, 2, 3])), [10, 20, 30], "--err-t2--")
        self.assertEqual(asyncio.run(fetch_all([])), [], "--err-t2--")
```

5つの id での `fetch_all` はそれらを並行して実行し、`0.1` 秒を大きく下回る必要があります

```python
    def test_3(self):
        start = time.perf_counter()
        result = asyncio.run(fetch_all([1, 2, 3, 4, 5]))
        elapsed = time.perf_counter() - start
        self.assertEqual(result, [10, 20, 30, 40, 50], "--err-t3--")
        self.assertLess(elapsed, 0.08, "--err-t3--")
```

# --after-asserts--

```python
if __name__ == "__main__":
    unittest.main()
```

# --solutions--

```python
import asyncio

async def fetch(item_id):
    await asyncio.sleep(0.02)
    return item_id * 10

async def fetch_all(ids):
    return await asyncio.gather(*[fetch(i) for i in ids])
```
