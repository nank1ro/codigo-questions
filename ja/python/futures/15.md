---
language: python
exerciseType: 4
---

# --description--

失敗するコルーチンの処理は同期のパターンに従います：コルーチンが例外を発生させ、呼び出し側が `await` を `try`/`except` で包み、エラーオブジェクトをどうするか決めます。たとえば `print(e)` でそのメッセージを出力します。

# --instructions--

アイテムを並べ替えて、インポートから始まり、`main` の前に `load` を定義し、`"config"` の読み込み結果を出力し、次に空の名前の読み込みで発生したエラーを捕捉してそのメッセージを出力するプログラムを作り、`loaded config`、`empty name` を出力してください

# --answers--

```python
import asyncio
```

```python
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
```

```python
async def main():
    print(await load("config"))
```

```python
    try:
        await load("")
```

```python
    except ValueError as e:
        print(e)
```

```python
asyncio.run(main())
```

# --solutions--

```python
import asyncio
async def load(name):
    await asyncio.sleep(0.01)
    if name == "":
        raise ValueError("empty name")
    return f"loaded {name}"
async def main():
    print(await load("config"))
    try:
        await load("")
    except ValueError as e:
        print(e)
asyncio.run(main())
```

# --output--

loaded config
empty name
