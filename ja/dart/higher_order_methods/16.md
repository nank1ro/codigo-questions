---
language: dart
exerciseType: 1
---

# --description--

組み合わせれば、これらのメソッドは手書きのループのほとんどを置き換えます。パイプラインは通常、3つの段階で読み解けます：`where` で要素を**選択**し、`map` で**変換**し、それから `fold` で**結合**します：

```dart
final prices = [12, 40, 7];
final cheapTotal = prices.where((p) => p < 20).fold(0, (acc, p) => acc + p);
print(cheapTotal); // 19
```

`fold` は独自の初期値を選べるため、要素とは無関係の型でチェーンを終えることもできます。たとえば少しずつ育っていく `String` です：

```dart
final words = ['fig', 'kiwi'];
final firstLetters = words.fold('', (acc, w) => acc + w[0]);
print(firstLetters); // fk
```

各段階は短く保たれ、何をするかがひと目で分かります。3つすべてを一度に行うループよりもこれらを好む本当の理由はここにあります。

# --instructions--

`List<String>` 型の `names` を受け取り、空のものを無視し、残りの各名前の最初の文字から作られる大文字の `String` を順序どおりに返す関数 `initials` を記述してください。

例：
```dart
print(initials(['ada', '', 'linus'])); // AL
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`initials(['ada', '', 'linus'])` は `'AL'` であるべきです

```dart
  test('test1', () {
    expect(initials(['ada', '', 'linus']), 'AL', reason: '--err-t1--');
  });
```

文字は大文字で正しい順序であるべきで、すでに大文字の名前はそのまま保たれるべきです

```dart
  test('test2', () {
    expect(initials(['grace', 'Bob', 'ada']), 'GBA', reason: '--err-t2--');
    expect(initials(['x']), 'X', reason: '--err-t2--');
  });
```

空でない名前が1つもない場合、`initials` は空の `String` を返すべきです

```dart
  test('test3', () {
    expect(initials(<String>[]), '', reason: '--err-t3--');
    expect(initials(['', '']), '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String initials(List<String> names) {
  return names
      .where((n) => n.isNotEmpty)
      .map((n) => n[0].toUpperCase())
      .fold('', (acc, letter) => acc + letter);
}
```
