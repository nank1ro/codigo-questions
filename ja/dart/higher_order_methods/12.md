---
language: dart
exerciseType: 3
---

# --description--

`fold` と `reduce` はよく似ています。どちらを選ぶかは2つの質問に帰着します：コレクションが空であり得るか、そして結果は要素と同じ型か？

```dart
final words = ['fig', 'kiwi'];
final joined = words.reduce((a, b) => '$a, $b'); // StringsからのString
final letters = words.fold(0, (acc, w) => acc + w.length); // StringsからのInt
print(joined);  // fig, kiwi
print(letters); // 7
```

`reduce` は要素から始まるため、返せるのは常に要素の型だけです。`fold` は自分で選んだ値から始まるため、アキュムレータは数え上げる `int`、育っていく `String`、構築中の `List` でさえあり得ます。そしてその初期値がすでに存在するので、空のコレクションに対しては `fold` は初期値をそのまま返すだけです。一方 `reduce` には返すものがなく、例外を投げます。

# --instructions--

`fold` と `reduce` の違いは何ですか？

# --answers--

- `fold` は初期値を受け取るため、空のコレクションでも動作し、結果の型は要素と異なっていてもかまいません；`reduce` は最初の要素から始まり、空のコレクションでは例外を投げます
- それらは同じメソッドの2つの名前です
- `reduce` は初期値を受け取り、`fold` は最初の要素から始まります
- `fold` は新しいリストを返し、`reduce` は単一の値を返します

# --solutions--

- `fold` は初期値を受け取るため、空のコレクションでも動作し、結果の型は要素と異なっていてもかまいません；`reduce` は最初の要素から始まり、空のコレクションでは例外を投げます
