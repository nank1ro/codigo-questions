---
language: dart
exerciseType: 1
---

# --instructions--

`[1, 2, 3]` を含む `List<int>` から始めてください。`.add()` を使って値 `4` を追加してください。リストを返してください。

# --before-seed--

```dart
List<int> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

リストに追加された要素が含まれている

```dart
  test('list is [1, 2, 3, 4]', () {
    expect(computeValue(), [1, 2, 3, 4], reason: '--err-t1--');
  });
```

長さは4

```dart
  test('length is 4', () {
    expect(computeValue().length, 4, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<int> result = [1, 2, 3];
  result.add(4);
```
