---
language: dart
exerciseType: 1
---

# --instructions--

空の `List<int>` を作成してください。`.isEmpty` であれば `true` を返し、値 `1` を追加して `.isNotEmpty` であれば `true` を返してください。両方のブール値を `List<bool>` として返してください。

# --before-seed--

```dart
List<bool> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

最初の値は true（空だった）

```dart
  test('first is true', () {
    expect(computeValue()[0], true, reason: '--err-t1--');
  });
```

2番目の値は true（追加後は空でない）

```dart
  test('second is true', () {
    expect(computeValue()[1], true, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  List<int> nums = [];
  bool wasEmpty = nums.isEmpty;
  nums.add(1);
  bool nowNotEmpty = nums.isNotEmpty;
  List<bool> result = [wasEmpty, nowNotEmpty];
```
