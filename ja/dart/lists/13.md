---
language: dart
exerciseType: 1
---

# --instructions--

リスト `[100, 200, 300, 400, 500]` が与えられたとき、インデックス `0` が `.first` 要素、インデックス `1` が `.last` 要素となる `List<int>` を返してください。

# --before-seed--

```dart
List<int> computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

最初の要素は100

```dart
  test('first is 100', () {
    expect(computeValue()[0], 100, reason: '--err-t1--');
  });
```

最後の要素は500

```dart
  test('last is 500', () {
    expect(computeValue()[1], 500, reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var nums = [100, 200, 300, 400, 500];
  List<int> result = [nums.first, nums.last];
```
