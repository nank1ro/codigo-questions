---
language: dart
exerciseType: 2
---

# --description--

`.contains()` メソッドはリストに指定した値が含まれているかどうかを確認します。見つかった場合は `true`、そうでない場合は `false` を返します：

```dart
var fruits = ['apple', 'mango', 'grape'];
print(fruits.contains('mango'));  // true
print(fruits.contains('orange')); // false
```

これはループを使わずにメンバーシップの確認をするのに便利です。

# --instructions--

`42` がリストに含まれているかどうかを確認して結果を出力するコードを完成させてください。

# --seed--

```dart
var numbers = [10, 42, 77, 5];
print(numbers.[/](42));
```

# --answers--

- contains
- includes
- has
- find
- exists
- indexOf

# --solutions--

```dart
var numbers = [10, 42, 77, 5];
print(numbers.contains(42));
```

# --output--

true
