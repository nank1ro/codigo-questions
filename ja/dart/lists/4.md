---
language: dart
exerciseType: 2
---

# --description--

`.length` プロパティはリスト内の要素数を返します：

```dart
var scores = [95, 87, 72, 100];
print(scores.length); // 4
```

空のリストの長さは `0` です：

```dart
var empty = [];
print(empty.length); // 0
```

# --instructions--

リストの長さを出力するコードを完成させてください。

# --seed--

```dart
var items = ['a', 'b', 'c', 'd', 'e'];
print(items.[/]);
```

# --answers--

- length
- size
- count
- len()
- length()
- total

# --solutions--

```dart
var items = ['a', 'b', 'c', 'd', 'e'];
print(items.length);
```

# --output--

5
