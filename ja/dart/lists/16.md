---
language: dart
exerciseType: 1
---

# --description--

`.indexOf(value)` メソッドは、`value` と等しい最初の要素のインデックスを返します。値がリストに存在しない場合は `-1` を返します。

```dart
var colors = ['red', 'green', 'blue'];
print(colors.indexOf('green')); // 1
print(colors.indexOf('pink')); // -1
```

# --instructions--

リスト `['apple', 'banana', 'cherry', 'date']` が与えられたとき、`.indexOf()` を使って `'cherry'` のインデックスを見つけてください。インデックスを返してください。

# --before-seed--

```dart
int computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

'cherry' のインデックスは2

```dart
  test('indexOf cherry is 2', () {
    expect(computeValue(), 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
  var fruits = ['apple', 'banana', 'cherry', 'date'];
  int result = fruits.indexOf('cherry');
```
