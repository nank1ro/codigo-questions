---
language: dart
exerciseType: 1
---

# --description--

それぞれの `Isolate.run` は自分専用の isolate を起動します。だから複数あれば、マシンが持つコアの数だけ、本当に同じ瞬間に実行されます。パターンは future ですでに知っているものです。まずすべての計算を始め、それから `Future.wait` で全部を待ちます。`Future.wait` は結果を入力の順序のまま保ちます。

```dart
Future<List<int>> doubleAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * 2)));
}
```

isolate の起動は無料ではありません。メモリと数ミリ秒のコストがかかります。1つの長い計算を少数の isolate に分割するのは見合いますが、些細な足し算一千個を千個の isolate に送るのは見合いません。

# --instructions--

`List<int>` の `numbers` を受け取り、各数値の2乗を同じ順序で含む `Future<List<int>>` を返す関数 `squareAll` を書いてください。それぞれの2乗は、それぞれ自分専用の isolate の上で計算します。

例：
```dart
print(await squareAll([1, 2, 3])); // [1, 4, 9]
```

# --before-seed--

```dart
import 'dart:isolate';
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`squareAll([1, 2, 3])` は `[1, 4, 9]` で完了するべきです

```dart
  test('test1', () async {
    expect(await squareAll([1, 2, 3]), [1, 4, 9], reason: '--err-t1--');
  });
```

`squareAll([-4, 5])` は `[16, 25]` で完了し、入力の順序を保つべきです

```dart
  test('test2', () async {
    expect(await squareAll([-4, 5]), [16, 25], reason: '--err-t2--');
  });
```

`squareAll([])` は空のリストで完了し、`squareAll` は `Future<List<int>>` を返すべきです

```dart
  test('test3', () async {
    expect(await squareAll(<int>[]), <int>[], reason: '--err-t3--');
    expect(squareAll([2]), isA<Future<List<int>>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 30)));
}
```

# --solutions--

```dart
Future<List<int>> squareAll(List<int> numbers) {
  return Future.wait(numbers.map((n) => Isolate.run(() => n * n)));
}
```
