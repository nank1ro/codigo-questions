---
language: dart
exerciseType: 4
---

# --description--

まとめると、`Isolate.run` を使ったプログラムはふつうの逐次コードのように読めます。呼び出しの前の行はメインの isolate で実行され、計算は別の場所で実行され、`await` の後の行は結果を手にしてメインの isolate に戻って実行されます。

```dart
import 'dart:isolate';

int double(int n) => n * 2;

Future<void> main() async {
  print('start');
  final result = await Isolate.run(() => double(4));
  print(result);
}
// start
// 8
```

# --instructions--

項目を並び替えて、`start` を出力し、`Isolate.run` で別の isolate の上で `9` を2乗し、その結果を出力するプログラムを作ってください。import を最初に置き、`square` を `main` の前に定義してください。プログラムは次を出力しなければなりません：

```
start
81
```

# --answers--

```dart
import 'dart:isolate';
```

```dart
int square(int n) => n * n;
```

```dart
Future<void> main() async {
```

```dart
  print('start');
```

```dart
  final result = await Isolate.run(() => square(9));
```

```dart
  print(result);
```

```dart
}
```

# --solutions--

```dart
import 'dart:isolate';
int square(int n) => n * n;
Future<void> main() async {
  print('start');
  final result = await Isolate.run(() => square(9));
  print(result);
}
```

# --output--

start
81
