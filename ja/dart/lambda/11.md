---
language: dart
exerciseType: 2
---

# --description--

関数は**関数を返す**こともできます。この場合、戻り値の型は関数型になり、本体は無名関数を返します：

```dart
int Function(int) makeAdder(int amount) {
  return (int n) => n + amount;
}

var addTen = makeAdder(10);
print(addTen(5)); // 15
```

返された関数は、`makeAdder` が終了した後でも、`makeAdder` のパラメータである `amount` を使い続けます。このように自分を取り囲む変数を覚えている関数は**クロージャ**と呼ばれます。

# --instructions--

引数に `factor` を掛けた値を返す関数を返すように `makeMultiplier` を完成させ、返された関数を呼び出してプログラムが `15` を出力するようにしてください。

# --seed--

```dart
int Function(int) makeMultiplier(int factor) {
  return ([/] n) => n [/] factor;
}

void main() {
  var triple = makeMultiplier(3);
  print(triple([/]));
}
```

# --answers--

- int
- *
- 5
- +
- 15
- String

# --solutions--

```dart
int Function(int) makeMultiplier(int factor) {
  return (int n) => n * factor;
}

void main() {
  var triple = makeMultiplier(3);
  print(triple(5));
}
```

# --output--

15
