---
language: dart
exerciseType: 1
---

# --description--

`map` と `where` はどちらも `Iterable` を返すため、その呼び出しは次々と**チェーン**できます。各ステップは前のステップの結果を受け取り、最後に一度だけ `toList()` を呼びます：

```dart
var numbers = [1, 2, 3, 4, 5, 6];

var result = numbers.where((n) => n > 3).map((n) => n * 10).toList();
print(result); // [40, 50, 60]
```

# --instructions--

`List<int>` 型のパラメータ `numbers` を受け取り、奇数だけの2乗を元の順序で含む `List<int>` を返す関数 `oddSquares` を記述してください。無名関数と一緒に `where` と `map` を使ってください。

例：
```dart
print(oddSquares([1, 2, 3, 4, 5])); // [1, 9, 25]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`oddSquares([1, 2, 3, 4, 5])` は `[1, 9, 25]` であるべきです

```dart
  test('test1', () {
    expect(oddSquares([1, 2, 3, 4, 5]), [1, 9, 25], reason: '--err-t1--');
  });
```

`oddSquares([2, 4, 6])` は `[]` であるべきです

```dart
  test('test2', () {
    expect(oddSquares([2, 4, 6]), [], reason: '--err-t2--');
  });
```

`oddSquares([7, 10, 3])` は `[49, 9]` であるべきです

```dart
  test('test3', () {
    expect(oddSquares([7, 10, 3]), [49, 9], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
List<int> oddSquares(List<int> numbers) {
  return numbers.where((n) => n % 2 != 0).map((n) => n * n).toList();
}
```
