---
language: dart
exerciseType: 1
---

# --description--

境界のない `T` は何であってもよいので、本体の中ではすべてのオブジェクトが持つものしか使えません。次のコードはコンパイルできません：

```dart
T twice<T>(T value) => value + value; // error: + is not defined for T
```

**境界（bound）**がこれを解決します。`T extends num` と書くと「`T` は数値しか取れない」ことを意味し、その見返りとして本体では `num` が提供するすべてを使えます：

```dart
T twice<T extends num>(T value) => (value + value) as T;

num half<T extends num>(T value) => value / 2;
```

境界は呼び出し側でチェックされます。`half(4)` と `half(2.5)` は問題なく、`half('fig')` はコンパイル時エラーです。境界は双方向の約束です。より狭い引数と引き換えに、内部ではより多くのことができます。

# --instructions--

型パラメータ `T` が `num` で境界付けられたジェネリック関数 `total` を書いてください。`List<T>` の `values` を受け取り、要素の合計を `num` として返します。空のリストは `0` になります。

例：
```dart
print(total([1, 2, 3]));  // 6
print(total([1.5, 2.5])); // 4.0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total([1, 2, 3])` は `6` を返すはずです

```dart
  test('test1', () {
    expect(total([1, 2, 3]), 6, reason: '--err-t1--');
  });
```

`total` はdoubleでも動くはずです。`total([1.5, 2.5])` は `4.0` を返し、`total<int>([1, 2])` のような明示的な型引数も受け付けるはずです

```dart
  test('test2', () {
    expect(total([1.5, 2.5]), 4.0, reason: '--err-t2--');
    expect(total([10, 20, 30, 40]), 100, reason: '--err-t2--');
    expect(total<int>([1, 2]), 3, reason: '--err-t2--');
    expect(total<double>([1.5, 1.5]), 3.0, reason: '--err-t2--');
  });
```

`total` は空のリストに対して `0` を返すはずです

```dart
  test('test3', () {
    expect(total(<int>[]), 0, reason: '--err-t3--');
    expect(total(<double>[]), 0, reason: '--err-t3--');
    expect(total([-5, 5, 7]), 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
num total<T extends num>(List<T> values) {
  num sum = 0;
  for (final value in values) {
    sum += value;
  }
  return sum;
}
```
