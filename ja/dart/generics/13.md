---
language: dart
exerciseType: 4
---

# --description--

同じ境界は最小の要素を求めるのにもそのまま使えます。変わるのは比較の符号だけです。`compareTo` はレシーバが先に来るときに負の数を返すので、`item.compareTo(best) < 0` は「こちらのほうが小さい」ことを意味します。

# --instructions--

項目を並べ替えて、`Comparable<T>` で境界付けられ、リストの最小の要素を返すジェネリック関数 `smallest` を定義し、続いて `['pear', 'fig', 'kiwi']` の最小値を出力する `main` を定義してください。関数は `main` の前に定義します。プログラムは次のように出力しなければなりません：

```
fig
```

# --answers--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
```

```dart
  var best = items.first;
```

```dart
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
```

```dart
  return best;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(smallest(['pear', 'fig', 'kiwi']));
```

```dart
}
```

# --solutions--

```dart
T smallest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) < 0) best = item;
  }
  return best;
}

void main() {
  print(smallest(['pear', 'fig', 'kiwi']));
}
```

# --output--

fig
