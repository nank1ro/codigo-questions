---
language: dart
exerciseType: 1
---

# --description--

境界は型パラメータ自身に言及することもできます。`Comparable<T>` は、`compareTo` を通じて自分と同じ種類のものと比較する方法を知っているすべてのもののインターフェースです：

```dart
print('fig'.compareTo('kiwi')); // 負: figが先
print('kiwi'.compareTo('fig')); // positive
print('fig'.compareTo('fig'));  // zero
```

したがって `T extends Comparable<T>` は「自分自身と比較できる任意の型」と読めます。これはソートや最大値を求める関数がまさに必要としているものです：

```dart
T maxOf<T extends Comparable<T>>(T a, T b) => a.compareTo(b) >= 0 ? a : b;

print(maxOf('fig', 'kiwi')); // kiwi
```

`String` と `DateTime` はどちらも直接これを満たします。`int` と `double` は `Comparable<num>` を実装しているので、数値のリストは単に `num` として比較されます。

# --instructions--

型パラメータ `T` が `Comparable<T>` で境界付けられたジェネリック関数 `largest` を書いてください。空でない `List<T>` の `items` を受け取り、`compareTo` で比較して最大の要素を返します。

例：
```dart
print(largest(['fig', 'kiwi', 'apple'])); // kiwi
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`largest(['fig', 'kiwi', 'apple'])` は `'kiwi'` を返すはずです

```dart
  test('test1', () {
    expect(largest(['fig', 'kiwi', 'apple']), 'kiwi', reason: '--err-t1--');
  });
```

`largest` は数値でも日付でも動くはずです

```dart
  test('test2', () {
    expect(largest([3, 9, 5]), 9, reason: '--err-t2--');
    expect(
      largest([DateTime(2020), DateTime(2024), DateTime(2001)]),
      DateTime(2024),
      reason: '--err-t2--',
    );
  });
```

`largest` は1要素のリストに対してはその唯一の要素を返し、最大の要素が最後にある場合にもそれを返すはずです。また、`largest<String>(['a', 'z'])` のような明示的な型引数も受け付けるはずです

```dart
  test('test3', () {
    expect(largest<String>(['a', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['only']), 'only', reason: '--err-t3--');
    expect(largest(['a', 'b', 'z']), 'z', reason: '--err-t3--');
    expect(largest(['z', 'b', 'a']), 'z', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
T largest<T extends Comparable<T>>(List<T> items) {
  var best = items.first;
  for (final item in items) {
    if (item.compareTo(best) > 0) {
      best = item;
    }
  }
  return best;
}
```
