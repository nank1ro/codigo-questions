---
language: dart
exerciseType: 2
---

# --description--

健全なnull安全のもとでは、クエスチョンマークが現れる場所は2つあり、それぞれ異なる意味を持ちます：

```dart
Box<int?> a = Box(null); // a box that exists and holds a nullable int
Box<int>? b = null;      // no box at all, but if there is one it holds an int
```

`Box<int?>` では**型引数**がnull許容なので、`a.value` の型は `int?` で `null` かもしれませんが、`a` 自体は常に存在します。`Box<int>?` では**変数**がnull許容なので、`b` は `null` かもしれず、中に入るには `b?.value` か `b!.value` が必要です。

裸の `T` は `T extends Object?` を意味するので、`Box<int?>` のようなnull許容の型引数はまったく問題なく合法です。

# --instructions--

`holdsNull` が `null` を保持するボックスになり、`missing` がボックスをまったく保持しないかもしれない変数になるようにコードを完成させてください。

# --seed--

```dart
class Box<T> {
  final T value;
  Box(this.value);
}

void main() {
  final holdsNull = Box<[/]>(null);
  Box<int>[/] missing = null;
  print('value: ${holdsNull.value}');
  print('box: $missing');
}
```

# --answers--

- int?
- ?
- int
- !
- ??

# --solutions--

```dart
class Box<T> {
  final T value;
  Box(this.value);
}

void main() {
  final holdsNull = Box<int?>(null);
  Box<int>? missing = null;
  print('value: ${holdsNull.value}');
  print('box: $missing');
}
```

# --output--

value: null
box: null
