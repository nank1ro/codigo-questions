---
language: dart
exerciseType: 1
---

# --description--

クラスは複数の型パラメータをコンマで区切って宣言できます。組み込みの例が `Map<K, V>` です。キーの型が1つ、値の型が1つです。

```dart
class Entry<K, V> {
  final K key;
  final V value;

  Entry(this.key, this.value);
}

final e = Entry<String, int>('age', 30);
```

**順番**は型の一部です。`Entry<String, int>` と `Entry<int, String>` は無関係な型であり、片方の値をもう片方に代入することはできません。型パラメータは戻り値の型の中で並べ替えることもできるので、メソッドはオブジェクトの入れ替わったバージョンを手渡せます：

```dart
Entry<V, K> get flipped => Entry(value, key);
```

# --instructions--

2つの型パラメータ `A` と `B` を持ち、コンストラクタで設定される型 `A` の `final` フィールド `first` と型 `B` の `final` フィールド `second`、そして同じ2つの値を逆の順序で保持する `Pair<B, A>` を返すゲッター `swapped` を持つジェネリッククラス `Pair` を宣言してください。

例：
```dart
final p = Pair<String, int>('age', 30);
print(p.first);         // age
print(p.swapped.first); // 30
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Pair<String, int>('age', 30)` は `first` が `'age'`、`second` が `30` になるはずです

```dart
  test('test1', () {
    final pair = Pair<String, int>('age', 30);
    expect(pair.first, 'age', reason: '--err-t1--');
    expect(pair.second, 30, reason: '--err-t1--');
  });
```

`swapped` は値を入れ替えた `Pair<int, String>` を返すはずです

```dart
  test('test2', () {
    final swapped = Pair<String, int>('age', 30).swapped;
    expect(swapped, isA<Pair<int, String>>(), reason: '--err-t2--');
    expect(swapped.first, 30, reason: '--err-t2--');
    expect(swapped.second, 'age', reason: '--err-t2--');
  });
```

`Pair` は任意の2つの型で動くはずです。2回入れ替えると元の値に戻るはずです

```dart
  test('test3', () {
    final pair = Pair<bool, String>(true, 'yes');
    expect(pair.swapped.first, 'yes', reason: '--err-t3--');
    expect(pair.swapped.second, true, reason: '--err-t3--');
    expect(pair.swapped.swapped, isA<Pair<bool, String>>(), reason: '--err-t3--');
    expect(pair.swapped.swapped.first, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Pair<A, B> {
  final A first;
  final B second;

  Pair(this.first, this.second);

  Pair<B, A> get swapped => Pair(second, first);
}
```
