---
language: dart
exerciseType: 2
---

# --description--

**継承**を使うと、あるクラスが別のクラスのフィールドとメソッドを再利用できます。`extends`キーワードを使ってサブクラス（子クラス）を作成します:

```dart
class Animal {
  String name;
  Animal(this.name);

  void speak() {
    print('...');
  }
}

class Cat extends Animal {
  Cat(String name) : super(name);

  @override
  void speak() {
    print('Meow');
  }
}
```

子クラスは`super(...)`で親コンストラクタを呼び出します。`@override`アノテーションはそのメソッドが親のバージョンを上書きすることを示します。

# --instructions--

`Cat`が`Animal`を継承し、親コンストラクタを呼び出し、`speak()`をオーバーライドして`'Meow'`を出力するようにコードを完成させてください。

# --seed--

```dart
class Animal {
  String name;
  Animal(this.name);

  void speak() {
    print('...');
  }
}

class Cat [/] Animal {
  Cat(String name) : [/](name);

  @[/]
  void speak() {
    print('Meow');
  }
}

void main() {
  final cat = Cat('Whiskers');
  cat.[/]();
}
```

# --answers--

- extends
- super
- override
- speak
- implements
- this
- Cat

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);

  void speak() {
    print('...');
  }
}

class Cat extends Animal {
  Cat(String name) : super(name);

  @override
  void speak() {
    print('Meow');
  }
}

void main() {
  final cat = Cat('Whiskers');
  cat.speak();
}
```

# --output--

Meow
