---
language: dart
exerciseType: 1
---

# --instructions--

抽象メソッド`double area()`を持つ`abstract`クラス`Shape`を作成してください。次に`Shape`を継承する`Circle`クラスを作成してください。`Circle`には`double`フィールド`radius`、コンストラクタがあり、`area()`をオーバーライドして`3.14 * radius * radius`を返すようにしてください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Circle(5).area()`は`78.5`に等しい

```dart
  test('test1', () {
    final c = Circle(5);
    expect(c.area(), 78.5, reason: '--err-t1--');
  });
```

`Circle(10).area()`は`314.0`に等しい

```dart
  test('test2', () {
    final c = Circle(10);
    expect(c.area(), 314.0, reason: '--err-t2--');
  });
```

`Circle`は`Shape`のインスタンスである

```dart
  test('test3', () {
    final c = Circle(3);
    expect(c is Shape, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
abstract class Shape {
  double area();
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() {
    return 3.14 * radius * radius;
  }
}
```
