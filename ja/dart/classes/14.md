---
language: dart
exerciseType: 2
---

# --description--

**静的メンバー**は、特定のインスタンスではなくクラス自体に属します。`static`キーワードで宣言し、クラスに対して直接アクセスします:

```dart
class MathHelper {
  static const double pi = 3.14159;

  static double circleArea(double r) => pi * r * r;
}

void main() {
  // access without creating an object:
  print(MathHelper.pi);
  print(MathHelper.circleArea(5));
}
```

静的なフィールドとメソッドは、すべてのインスタンスで共有されます。

# --instructions--

`a + b`を返す、`Calculator`クラス内の静的メソッド`add`を完成させてください。

# --seed--

```dart
class Calculator {
  [/] int add(int a, int b) => a [/] b;
}
```

# --answers--

- static
- +
- -
- void
- final
- *

# --solutions--

```dart
class Calculator {
  static int add(int a, int b) => a + b;
}
```
