---
language: dart
exerciseType: 1
---

# --instructions--

`String`フィールド`name`、`int`フィールド`age`、コンストラクタを持つ`Person`クラスを作成してください。`toString()`をオーバーライドして`'Name: <name>, Age: <age>'`という文字列を返すようにしてください（例: `'Name: Alice, Age: 30'`）。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Person('Alice', 30).toString()`は`'Name: Alice, Age: 30'`を返す

```dart
  test('test1', () {
    final p = Person('Alice', 30);
    expect(p.toString(), 'Name: Alice, Age: 30', reason: '--err-t1--');
  });
```

`Person('Bob', 25).toString()`は`'Name: Bob, Age: 25'`を返す

```dart
  test('test2', () {
    final p = Person('Bob', 25);
    expect(p.toString(), 'Name: Bob, Age: 25', reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Person {
  String name;
  int age;
  Person(this.name, this.age);

  @override
  String toString() {
    return 'Name: $name, Age: $age';
  }
}
```
