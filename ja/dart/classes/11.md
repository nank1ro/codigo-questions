---
language: dart
exerciseType: 1
---

# --instructions--

`int`フィールド`width`と`height`、およびそれらを設定するコンストラクタを持つ`Rectangle`クラスを作成してください。`width * height`を返すゲッター`area`と、`2 * (width + height)`を返すゲッター`perimeter`を追加してください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Rectangle(4, 5).area`は`20`に等しい

```dart
  test('test1', () {
    final r = Rectangle(4, 5);
    expect(r.area, 20, reason: '--err-t1--');
  });
```

`Rectangle(4, 5).perimeter`は`18`に等しい

```dart
  test('test2', () {
    final r = Rectangle(4, 5);
    expect(r.perimeter, 18, reason: '--err-t2--');
  });
```

`Rectangle(3, 3).area`は`9`に等しい

```dart
  test('test3', () {
    final r = Rectangle(3, 3);
    expect(r.area, 9, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.height);

  int get area => width * height;
  int get perimeter => 2 * (width + height);
}
```
