---
language: dart
exerciseType: 1
---

# --instructions--

2つの`String`プロパティ`brand`と`model`を持つ`Car`クラスを作成してください。省略記法を使って両方を設定するコンストラクタを追加してください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Car('Toyota', 'Corolla')`を作成すると`brand`が`'Toyota'`になる

```dart
  test('test1', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.brand, 'Toyota', reason: '--err-t1--');
  });
```

`Car('Toyota', 'Corolla')`を作成すると`model`が`'Corolla'`になる

```dart
  test('test2', () {
    final car = Car('Toyota', 'Corolla');
    expect(car.model, 'Corolla', reason: '--err-t2--');
  });
```

`Car('Honda', 'Civic')`を作成すると`brand`が`'Honda'`になる

```dart
  test('test3', () {
    final car = Car('Honda', 'Civic');
    expect(car.brand, 'Honda', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Car {
  String brand;
  String model;
  Car(this.brand, this.model);
}
```
