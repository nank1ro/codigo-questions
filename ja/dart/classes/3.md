---
language: dart
exerciseType: 2
---

# --description--

**メソッド**はクラスの中で定義された関数です。メソッドはオブジェクトの振る舞いを表します。

```dart
class Circle {
  double radius;
  Circle(this.radius);

  double area() {
    return 3.14 * radius * radius;
  }
}

void main() {
  final c = Circle(5);
  print(c.area()); // 78.5
}
```

メソッドはオブジェクト自身のフィールドに、名前を直接使って（または`this.fieldName`で）アクセスできます。

# --instructions--

`int`フィールド`width`と`height`、コンストラクタ、および`2 * (width + height)`を返すメソッド`perimeter()`を持つ`Rectangle`クラスを完成させてください。そのメソッドを呼び出して結果を出力してください。

# --seed--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.[/]);

  int [/]() {
    return 2 * ([/] + height);
  }
}

void main() {
  final r = Rectangle(4, 6);
  print(r.[/]());
}
```

# --answers--

- height
- perimeter
- width
- area
- this
- 3
- r

# --solutions--

```dart
class Rectangle {
  int width;
  int height;
  Rectangle(this.width, this.height);

  int perimeter() {
    return 2 * (width + height);
  }
}

void main() {
  final r = Rectangle(4, 6);
  print(r.perimeter());
}
```

# --output--

20
