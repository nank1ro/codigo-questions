---
language: dart
exerciseType: 1
---

# --instructions--

`0`で初期化された`int`フィールド`count`を持つ`Counter`クラスを作成してください。`count`を`1`増やすメソッド`increment()`と、`count`を`0`に戻すメソッド`reset()`を追加してください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`increment()`を3回呼び出すと`count`が`3`になる

```dart
  test('test1', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.increment();
    expect(c.count, 3, reason: '--err-t1--');
  });
```

`reset()`を呼び出すと`count`が`0`に戻る

```dart
  test('test2', () {
    final c = Counter();
    c.increment();
    c.increment();
    c.reset();
    expect(c.count, 0, reason: '--err-t2--');
  });
```

初期値の`count`は`0`である

```dart
  test('test3', () {
    final c = Counter();
    expect(c.count, 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Counter {
  int count = 0;

  void increment() {
    count++;
  }

  void reset() {
    count = 0;
  }
}
```
