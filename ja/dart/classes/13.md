---
language: dart
exerciseType: 1
---

# --instructions--

`String`フィールド`name`とコンストラクタを持つ親クラス`Animal`を作成してください。次に`Animal`を継承する子クラス`Dog`を作成してください。`Dog`には追加の`String`フィールド`breed`と、`super`を使って`name`を`Animal`に渡すコンストラクタが必要です。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Dog('Rex', 'Labrador').name`は`'Rex'`に等しい

```dart
  test('test1', () {
    final dog = Dog('Rex', 'Labrador');
    expect(dog.name, 'Rex', reason: '--err-t1--');
  });
```

`Dog('Rex', 'Labrador').breed`は`'Labrador'`に等しい

```dart
  test('test2', () {
    final dog = Dog('Rex', 'Labrador');
    expect(dog.breed, 'Labrador', reason: '--err-t2--');
  });
```

`Dog`は`Animal`のインスタンスである

```dart
  test('test3', () {
    final dog = Dog('Buddy', 'Poodle');
    expect(dog is Animal, true, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
class Animal {
  String name;
  Animal(this.name);
}

class Dog extends Animal {
  String breed;
  Dog(String name, this.breed) : super(name);
}
```
