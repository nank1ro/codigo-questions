---
language: dart
exerciseType: 2
---

# --description--

**抽象クラス**は直接インスタンス化できないクラスです。サブクラスが実装しなければならないメソッド、つまり契約を定義する基底として使われます。本体を省略することで抽象メソッドを示します:

```dart
abstract class Shape {
  double area(); // abstract method — no body
}

class Circle extends Shape {
  double radius;
  Circle(this.radius);

  @override
  double area() => 3.14159 * radius * radius;
}
```

`Shape()`を直接インスタンス化しようとするとエラーになります。

# --instructions--

抽象メソッド`fuelType`を持つ`Vehicle`の抽象クラス定義を完成させてください。

# --seed--

```dart
[/] class Vehicle {
  String fuelType();
}
```

# --answers--

- abstract
- Abstract
- interface
- base
- virtual

# --solutions--

```dart
abstract class Vehicle {
  String fuelType();
}
```
