---
language: dart
exerciseType: 3
---

# --description--

リストとセットはどちらも `.contains()` メソッドを持っていますが、その動作は大きく異なります。リストは要素を先頭から1つずつ確認するため、長いリストでの検索はリストが大きくなるほど遅くなります。セットはハッシュによって要素を格納するため、要素数に関わらず `.contains()` はほぼ一定時間で値を見つけます。

メンバーシップを何度もチェックする必要があり、順序や重複が問題にならない場合は、セットが適したツールです：

```dart
var banned = {'spam', 'scam'};
print(banned.contains('spam')); // fast, even with millions of elements
```

# --instructions--

一意のユーザー名の大きなコレクションがあり、あるユーザー名がすでに使われているかを何千回も確認する必要があります。どの型が最適ですか？

# --answers--

- `Set<String>`
- `List<String>`
- `String`
- `int`

# --solutions--

- `Set<String>`
