---
language: dart
exerciseType: 1
---

# --instructions--

リスト `numbers` が与えられたとき、そこに含まれる異なる値の数を `int` の `uniqueCount` に、それらの値を重複なく最初に現れた順序で `List<int>` の `unique` に格納してください。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var numbers = [4, 8, 4, 15, 8, 16, 4];
```

# --after-seed--

```dart
  return [uniqueCount, unique];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`uniqueCount` は `4` であるべきです

```dart
  test('test1', () {
    expect(result()[0], 4, reason: '--err-t1--');
  });
```

`unique` は `[4, 8, 15, 16]` であるべきです

```dart
  test('test2', () {
    expect(result()[1], [4, 8, 15, 16], reason: '--err-t2--');
  });
```

`unique` は `List` であるべきです

```dart
  test('test3', () {
    expect(result()[1], isA<List>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var numbers = [4, 8, 4, 15, 8, 16, 4];
int uniqueCount = numbers.toSet().length;
List<int> unique = numbers.toSet().toList();
```
