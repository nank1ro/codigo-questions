---
language: dart
exerciseType: 1
---

# --description--

セットに値が含まれているかを調べるには、`bool` を返す `.contains(value)` を使います。`.isEmpty` プロパティはセットに要素がない場合に `true`、`.isNotEmpty` は少なくとも1つ要素がある場合に `true` になります：

```dart
var seen = {'x', 'y'};
print(seen.contains('x')); // true
print(seen.contains('z')); // false
print(seen.isEmpty);       // false
print(seen.isNotEmpty);    // true
```

# --instructions--

セット `seen` が与えられたとき、`'x'` を含むかどうかを `bool` の `hasX` に、`'z'` を含むかどうかを `bool` の `hasZ` に、セットが空かどうかを `bool` の `empty` に格納してください。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var seen = {'x', 'y'};
```

# --after-seed--

```dart
  return [hasX, hasZ, empty];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`hasX` は `true` であるべきです

```dart
  test('test1', () {
    expect(result()[0], true, reason: '--err-t1--');
  });
```

`hasZ` は `false` であるべきです

```dart
  test('test2', () {
    expect(result()[1], false, reason: '--err-t2--');
  });
```

`empty` は `false` であるべきです

```dart
  test('test3', () {
    expect(result()[2], false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var seen = {'x', 'y'};
bool hasX = seen.contains('x');
bool hasZ = seen.contains('z');
bool empty = seen.isEmpty;
```
