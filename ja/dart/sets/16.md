---
language: dart
exerciseType: 1
---

# --instructions--

リスト `words` が与えられたとき、リスト内に**2回以上**出現する単語だけを含む `Set<String>` の `duplicates` を構築してください。ヒント：`.add()` は値がすでにセットに存在する場合に `false` を返します。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var words = ['sun', 'moon', 'sun', 'star', 'moon', 'sun'];
```

# --after-seed--

```dart
  return duplicates;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`duplicates` は `{'sun', 'moon'}` であるべきです

```dart
  test('test1', () {
    expect(result(), {'sun', 'moon'}, reason: '--err-t1--');
  });
```

`duplicates` は `Set` であるべきです

```dart
  test('test2', () {
    expect(result(), isA<Set>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var words = ['sun', 'moon', 'sun', 'star', 'moon', 'sun'];
var seen = <String>{};
var duplicates = <String>{};
for (var word in words) {
  if (!seen.add(word)) {
    duplicates.add(word);
  }
}
```
