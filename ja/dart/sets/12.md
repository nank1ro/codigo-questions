---
language: dart
exerciseType: 1
---

# --description--

リストをセットに変換することは、**重複を取り除く**最も簡単な方法です：すべてのリストは `.toSet()` メソッドを持ち、最初に現れた順序で一意の要素を持つセットを返します。セットには逆方向の変換を行う `.toList()` メソッドがあるので、この2つを連結すれば重複のないリストが得られます：

```dart
var votes = ['a', 'b', 'a', 'c', 'b'];
Set<String> unique = votes.toSet();
print(unique); // {a, b, c}
List<String> cleaned = votes.toSet().toList();
print(cleaned); // [a, b, c]
```

# --instructions--

リスト `votes` が与えられたとき、重複を除いたその要素を最初に現れた順序で持つ `List<String>` の `cleaned` を作成してください。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var votes = ['yes', 'no', 'yes', 'maybe', 'no', 'yes'];
```

# --after-seed--

```dart
  return cleaned;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`cleaned` は `['yes', 'no', 'maybe']` であるべきです

```dart
  test('test1', () {
    expect(result(), ['yes', 'no', 'maybe'], reason: '--err-t1--');
  });
```

`cleaned` は `List` であるべきです

```dart
  test('test2', () {
    expect(result(), isA<List>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var votes = ['yes', 'no', 'yes', 'maybe', 'no', 'yes'];
List<String> cleaned = votes.toSet().toList();
```
