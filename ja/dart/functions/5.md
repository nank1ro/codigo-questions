---
language: dart
exerciseType: 2
---

# --description--

Dartは**名前付きパラメータ**をサポートしています — 波括弧 `{}` で囲まれたパラメータです。名前付きパラメータは名前で呼び出す必要があり、デフォルト値を持つか `required` としてマークできます：

```dart
void printInfo({required String name, int age = 0}) {
  print("$name is $age years old");
}

void main() {
  printInfo(name: "Alice", age: 30);
  // prints Alice is 30 years old
}
```

名前付きパラメータは可読性を向上させます。特に関数に多くのパラメータがある場合に有効です。

# --instructions--

`String` 型の名前付きパラメータ `item`（必須）を受け取り、それを出力する関数 `describe` を完成させてください。

# --seed--

```dart
void describe([/]required String item[/]) {
  print(item);
}

void main() {
  describe(item: "laptop");
}
```

# --answers--

- {
- }
- (
- )
- @

# --solutions--

```dart
void describe({required String item}) {
  print(item);
}

void main() {
  describe(item: "laptop");
}
```

# --output--

laptop
