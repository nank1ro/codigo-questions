---
language: dart
exerciseType: 1
---

# --instructions--

`int` パラメータ `n` を取り `n * n` を返す**アロー関数** `square` を記述してください。

`=>` 構文を使ってください（波括弧なし、`return` キーワードなし）。

例：
```dart
print(square(4));  // 16
print(square(5));  // 25
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`square(4)` は `16` を返す必要があります
```dart
  test('test1', () {
    expect(square(4), 16, reason: '--err-t1--');
  });
```

`square(5)` は `25` を返す必要があります
```dart
  test('test2', () {
    expect(square(5), 25, reason: '--err-t2--');
  });
```

`square(0)` は `0` を返す必要があります
```dart
  test('test3', () {
    expect(square(0), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int square(int n) => n * n;
```
