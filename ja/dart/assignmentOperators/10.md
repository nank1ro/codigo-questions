---
language: dart
exerciseType: 1
---

# --instructions--

`int` 変数 `width` を値 `6` で宣言します。次に、`*=` を使用してそれに `4` を掛けます。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int width = 6;
```

# --after-seed--

```dart
  return width;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`width` は `24` である必要があります

```dart
  test('test1', () {
    expect(result(), 24, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int width = 6;
width *= 4;
```
