---
language: dart
exerciseType: 1
---

# --instructions--

トップレベルの `int` 変数 `width` を値 `6` で宣言します。次に、`*=` を使用してそれに `4` を掛けます。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`width` は `24` である必要があります

```dart
  test('test1', () {
    expect(width, 24, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int width = 6;

void main() {
  width *= 4;
  print(width);
}
```
