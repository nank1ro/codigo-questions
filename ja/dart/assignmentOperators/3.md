---
language: dart
exerciseType: 2
---

# --description--

**減算代入演算子** `-=` は変数から値を減らし、結果をその変数に格納します。

```dart
int lives = 3;
lives -= 1; // lives は今 2
```

これは `lives = lives - 1` の省略形です。

# --instructions--

コードを完成させて、`-=` を使用して変数 `stock` から `5` を減らしてください。

# --seed--

```dart
void main() {
  int stock = 20;
  stock [/] 5;
  print(stock);
}
```

# --answers--

- -=
- =
- +=
- -

# --solutions--

```dart
void main() {
  int stock = 20;
  stock -= 5;
  print(stock);
}
```

# --output--

15
