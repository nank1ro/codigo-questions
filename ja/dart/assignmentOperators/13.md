---
language: dart
exerciseType: 1
---

# --instructions--

トップレベルの `int` 変数 `remainder` を値 `29` で宣言します。次に、`%=` を使用して `6` で割った剰余を割り当てます。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`remainder` は `5` である必要があります

```dart
  test('test1', () {
    expect(remainder, 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 29;

void main() {
  remainder %= 6;
  print(remainder);
}
```
