---
language: dart
exerciseType: 1
---

# --instructions--

ショップが商品に20%の割引を適用しています。トップレベルの `double` 変数 `price` を値 `50.0` で宣言します。次に、`*=` を使用して `price` に `0.8` を掛けることで割引を適用します。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`price` は `40.0` である必要があります

```dart
  test('test1', () {
    expect(price, closeTo(40.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double price = 50.0;

void main() {
  price *= 0.8;
  print(price);
}
```
