---
language: dart
exerciseType: 1
---

# --instructions--

ショップが商品に20%の割引を適用しています。`double` 変数 `price` を値 `50.0` で宣言します。次に、`*=` を使用して `price` に `0.8` を掛けることで割引を適用します。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
double price = 50.0;
```

# --after-seed--

```dart
  return price;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`price` は `40.0` である必要があります

```dart
  test('test1', () {
    expect(result(), closeTo(40.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double price = 50.0;
price *= 0.8;
```
