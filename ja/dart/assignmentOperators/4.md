---
language: dart
exerciseType: 2
---

# --description--

**乗算代入演算子** `*=` は変数に値を掛け、結果をその変数に格納します。

```dart
int side = 4;
side *= 3; // side は今 12
```

これは `side = side * 3` の省略形です。

# --instructions--

コードを完成させて、`*=` を使用して `price` に `3` を掛けてください。

# --seed--

```dart
void main() {
  int price = 7;
  price [/] 3;
  print(price);
}
```

# --answers--

- *=
- =
- +=
- *

# --solutions--

```dart
void main() {
  int price = 7;
  price *= 3;
  print(price);
}
```

# --output--

21
