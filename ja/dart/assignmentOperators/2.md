---
language: dart
exerciseType: 2
---

# --description--

**加算代入演算子** `+=` は値を変数に加え、結果をその変数に格納します。

```dart
int score = 10;
score += 5; // score は今 15
```

これは `score = score + 5` の省略形です。

# --instructions--

コードを完成させて、`+=` を使用して変数 `total` に `8` を追加してください。

# --seed--

```dart
void main() {
  int total = 12;
  total [/] 8;
  print(total);
}
```

# --answers--

- +=
- =
- -=
- +

# --solutions--

```dart
void main() {
  int total = 12;
  total += 8;
  print(total);
}
```

# --output--

20
