---
language: dart
exerciseType: 1
---

# --instructions--

トップレベルの `double` 変数 `value` を値 `20.0` で宣言します。次に、`/=` を使用してそれを `4` で割ります。Dart では `/=` は常に `double` を生成することに注意してください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`value` は `5.0` である必要があります

```dart
  test('test1', () {
    expect(value, closeTo(5.0, 0.0001), reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double value = 20.0;

void main() {
  value /= 4;
  print(value);
}
```
