---
language: dart
exerciseType: 1
---

# --instructions--

トップレベルの `int` 変数 `n` を値 `2` で宣言します。次に、`main` で、次の代入演算子を順番に適用します:
- `n *= 10` (10を掛ける)
- `n += 5` (5を追加する)
- `n -= 3` (3を減らす)

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`n` は `22` である必要があります

```dart
  test('test1', () {
    expect(n, 22, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int n = 2;

void main() {
  n *= 10;
  n += 5;
  n -= 3;
  print(n);
}
```
