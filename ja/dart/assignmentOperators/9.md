---
language: dart
exerciseType: 1
---

# --instructions--

トップレベルの `int` 変数 `fuel` を値 `100` で宣言します。次に、`-=` を使用してそれから `35` を減らします。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`fuel` は `65` である必要があります

```dart
  test('test1', () {
    expect(fuel, 65, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int fuel = 100;

void main() {
  fuel -= 35;
  print(fuel);
}
```
