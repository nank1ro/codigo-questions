---
language: dart
exerciseType: 3
---

# --instructions--

`x` が現在 `10` のとき、`x += 5` は何をしますか?

# --answers--

- `x` を `15` に設定します
- `x` を `5` に設定します
- `x` を `10` に設定します
- `x` を `50` に設定します

# --solutions--

- `x` を `15` に設定します
