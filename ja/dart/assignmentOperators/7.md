---
language: dart
exerciseType: 3
---

# --instructions--

`x` が `9` のとき、`x %= 4` は何をしますか?

# --answers--

- `x` を `9 / 4` の剰余に設定します。これは `1` です
- `x` を `9 / 4` の結果に設定します。これは `2.25` です
- `x` を `4` に設定します
- `x` を `36` に設定します

# --solutions--

- `x` を `9 / 4` の剰余に設定します。これは `1` です
