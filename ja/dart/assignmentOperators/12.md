---
language: dart
exerciseType: 1
---

# --instructions--

トップレベルの `int` 変数 `apples` を値 `17` で宣言します。次に、`~/=` を使用してそれを `5` で整数除算します。`~/=` 演算子は整数除算を行い、常に `int` を返します。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`apples` は `3` である必要があります

```dart
  test('test1', () {
    expect(apples, 3, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int apples = 17;

void main() {
  apples ~/= 5;
  print(apples);
}
```
