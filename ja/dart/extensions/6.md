---
language: dart
exerciseType: 2
---

# --description--

`String` に対する拡張は `String?` に対しては呼び出せません。値が `null` かもしれず、コンパイラが呼び出しを拒否するためです。代わりに**null許容**の型に対して拡張を宣言すると、メソッドは `String?` に対して直接呼び出せます。その中で `this` は `String?` 型を持つため、`null` のケースは `??` を使うなどして自分で処理しなければなりません：

```dart
extension Defaults on int? {
  int orZero() => this ?? 0;
}

void main() {
  int? count = null;
  print(count.orZero()); // 0
  print(5.orZero());     // 5
}
```

非null許容の `int` は `int?` が期待される場所に渡せるため、この拡張は両方に対して動作します。

# --instructions--

拡張を完成させて、`orEmpty` がnull許容の文字列に対して呼び出せ、文字列そのものを返し、`null` のときは空文字列を返すようにしてください。プログラムは `[]` と `[Ada]` を出力しなければなりません。

# --seed--

```dart
extension Defaults on [/] {
  String orEmpty() => this [/] '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --answers--

- String?
- ??
- String
- +
- ?.
- !

# --solutions--

```dart
extension Defaults on String? {
  String orEmpty() => this ?? '';
}

void main() {
  String? missing = null;
  String? name = 'Ada';
  print('[${missing.orEmpty()}]');
  print('[${name.orEmpty()}]');
}
```

# --output--

[]
[Ada]
