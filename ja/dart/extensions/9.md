---
language: dart
exerciseType: 4
---

# --description--

拡張はSDKの型のためだけのものではありません。**自分で書いたクラス**を拡張することもできます。これは、クラスが自分の管理外のパッケージに由来するときや、クラスを小さく保ち、ヘルパーを必要とするコードのそばに追加のヘルパーを置きたいときに役立ちます。

```dart
class Circle {
  final double radius;
  Circle(this.radius);
}

extension CircleMath on Circle {
  double get diameter => radius * 2;
}

void main() {
  print(Circle(3).diameter); // 6.0
}
```

拡張からは、クラスの外に書いたコードとまったく同じように、クラスの公開フィールドとメソッドが見えます。

# --instructions--

項目を並べ替えて、`width` と `height` を持つクラス `Rectangle` を定義し、そのあとにゲッター `area` を持つ `Rectangle` に対する拡張 `RectangleMath`、さらに `4` × `2.5` の長方形の面積を出力する `main` を定義してください。クラスを最初に定義し、次に拡張、最後に `main` を定義します。クラスの中では、2つのフィールドをコンストラクタより前に宣言します。プログラムは次のように出力しなければなりません：

```
10.0
```

# --answers--

```dart
class Rectangle {
```

```dart
  final double width;
```

```dart
  final double height;
```

```dart
  Rectangle(this.width, this.height);
```

```dart
}
```

```dart
extension RectangleMath on Rectangle {
```

```dart
  double get area => width * height;
```

```dart
}
```

```dart
void main() {
```

```dart
  print(Rectangle(4, 2.5).area);
```

```dart
}
```

# --solutions--

```dart
class Rectangle {
  final double width;
  final double height;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

```dart
class Rectangle {
  final double height;
  final double width;
  Rectangle(this.width, this.height);
}

extension RectangleMath on Rectangle {
  double get area => width * height;
}

void main() {
  print(Rectangle(4, 2.5).area);
}
```

# --output--

10.0
