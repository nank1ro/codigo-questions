---
language: dart
exerciseType: 4
---

# --description--

`on` の後ろの型は、`List<int>` のような**パラメータ化された**型でも構いません。この場合、拡張はその要素型のリストにのみ適用されます。`[1, 2].total()` は動作しますが、`['a', 'b'].total()` はコンパイルできません。

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}
```

拡張の中では、`this` はリストそのものなので、いつもどおりループしたり、インデックスでアクセスしたり、`length` を呼び出したりできます。

# --instructions--

項目を並べ替えて、`for` ループで要素を合計するメソッド `total` を持つ `List<int>` に対する拡張 `Totals` を宣言し、そのあとに `[3, 4, 5]` の合計を出力する `main` を定義してください。拡張は `main` より前に宣言します。プログラムは次のように出力しなければなりません：

```
12
```

# --answers--

```dart
extension Totals on List<int> {
```

```dart
  int total() {
```

```dart
    var sum = 0;
```

```dart
    for (final n in this) {
```

```dart
      sum += n;
```

```dart
    }
```

```dart
    return sum;
```

```dart
  }
```

```dart
}
```

```dart
void main() {
```

```dart
  print([3, 4, 5].total());
```

```dart
}
```

# --solutions--

```dart
extension Totals on List<int> {
  int total() {
    var sum = 0;
    for (final n in this) {
      sum += n;
    }
    return sum;
  }
}

void main() {
  print([3, 4, 5].total());
}
```

# --output--

12
