---
language: dart
exerciseType: 1
---

# --description--

拡張は**静的（static）**メンバーを宣言できます。クラスの場合と同じように、静的メンバーはどの値にも属さず拡張そのものに属し、拡張先の型を通じてではなく**拡張の名前**を通じてアクセスされます：

```dart
extension Temperatures on double {
  static const double boiling = 100.0;

  static bool isBoiling(double celsius) => celsius >= boiling;
}

void main() {
  print(Temperatures.boiling);         // 100.0
  print(Temperatures.isBoiling(37.5)); // false
  print(double.boiling);               // error: 'boiling' isn't defined for 'double'
}
```

静的メンバーは、拡張対象の型に関係する定数やヘルパー関数を置くのに便利な場所です。

# --instructions--

`int` に対する `Dice` という名前の拡張を記述してください：
- 値が `6` の `int` 型の静的定数 `sides`
- `int` 型のパラメータ `value` を取り、それが `1` から `sides` の間（両端を含む）にあるときに `true` を返す静的メソッド `isValid`

例：
```dart
print(Dice.sides);      // 6
print(Dice.isValid(7)); // false
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Dice.sides` は `6` であるべきです

```dart
  test('test1', () {
    expect(Dice.sides, 6, reason: '--err-t1--');
  });
```

`Dice.isValid(1)` と `Dice.isValid(6)` は `true` であるべきです

```dart
  test('test2', () {
    expect(Dice.isValid(1), true, reason: '--err-t2--');
    expect(Dice.isValid(6), true, reason: '--err-t2--');
  });
```

`Dice.isValid(0)` と `Dice.isValid(7)` は `false` であるべきです

```dart
  test('test3', () {
    expect(Dice.isValid(0), false, reason: '--err-t3--');
    expect(Dice.isValid(7), false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Dice on int {
  static const int sides = 6;

  static bool isValid(int value) => value >= 1 && value <= sides;
}
```
