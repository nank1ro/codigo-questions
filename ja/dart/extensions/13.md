---
language: dart
exerciseType: 2
---

# --description--

ジェネリックな拡張は、`where` や `map` がするのとまったく同じように、**関数**をパラメータとして取れます。関数型は要素型 `T` を使って書くため、コールバックは正しい型の要素を受け取ります：

```dart
extension Checks<T> on List<T> {
  bool all(bool Function(T) test) {
    for (final item in this) {
      if (!test(item)) return false;
    }
    return true;
  }
}

void main() {
  print([2, 4, 6].all((n) => n.isEven)); // true
}
```

# --instructions--

拡張を完成させて、`countWhere` が与えられた `test` 関数を満たす要素の個数を返し、プログラムが `2`、続いて `1` を出力するようにしてください。

# --seed--

```dart
extension Counting<T> on List<T> {
  int countWhere([/] Function(T) test) {
    var count = 0;
    for (final item in [/]) {
      if (test(item)) count++;
    }
    return count;
  }
}

void main() {
  print([1, 2, 3, 4].countWhere((n) => n.isEven));
  print(['a', 'bb', 'c'].countWhere((s) => s.length > 1));
}
```

# --answers--

- bool
- this
- int
- void
- T
- list

# --solutions--

```dart
extension Counting<T> on List<T> {
  int countWhere(bool Function(T) test) {
    var count = 0;
    for (final item in this) {
      if (test(item)) count++;
    }
    return count;
  }
}

void main() {
  print([1, 2, 3, 4].countWhere((n) => n.isEven));
  print(['a', 'bb', 'c'].countWhere((s) => s.length > 1));
}
```

# --output--

2
1
