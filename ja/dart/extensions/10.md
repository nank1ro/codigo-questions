---
language: dart
exerciseType: 1
---

# --description--

Dartでは、`operator` キーワードの後に記号を付けた名前のメソッドを使って、`+`、`*`、`==` などの演算子がその型の値に対して何を意味するかを型に定義させられます。演算子の右側はメソッドのパラメータです：

```dart
extension Scaling on List<int> {
  List<int> operator *(int factor) => map((n) => n * factor).toList();
}

void main() {
  print([1, 2, 3] * 10); // [10, 20, 30]
}
```

拡張は演算子を宣言できるため、既存の型にまだ持っていない新しい演算子を与えられます。`String` は `+` と `*` を持ちますが `-` は持たないので、拡張は `'hello world' - 'o'` が何を意味するかを定義できます。

# --instructions--

`String` に対する `Removing` という名前の拡張を記述してください。演算子 `-` は `String` 型のパラメータ `part` を取り、`part` の出現をすべて取り除いた文字列を返します。`replaceAll` を使ってください。

例：
```dart
print('hello world' - 'o'); // hell wrld
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`'hello world' - 'o'` は `'hell wrld'` であるべきです

```dart
  test('test1', () {
    expect('hello world' - 'o', 'hell wrld', reason: '--err-t1--');
  });
```

`'banana' - 'an'` は `'ba'` であるべきです

```dart
  test('test2', () {
    expect('banana' - 'an', 'ba', reason: '--err-t2--');
  });
```

`'abc' - 'z'` は `'abc'`、`'aaa' - 'a'` は `''` であるべきです

```dart
  test('test3', () {
    expect('abc' - 'z', 'abc', reason: '--err-t3--');
    expect('aaa' - 'a', '', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Removing on String {
  String operator -(String part) => replaceAll(part, '');
}
```
