---
language: dart
exerciseType: 1
---

# --description--

拡張は数値を含む任意の型に対して動作します。この拡張は、すべての `int` に2倍するメソッドを与えます：

```dart
extension Doubling on int {
  int doubled() => this * 2;
}
```

拡張は**型**に適用されるため、メソッドは変数に対しても、**リテラル**に対して直接呼び出すこともできます。負のリテラルには括弧が必要です。そうしないと、マイナス記号よりも先にドットが読まれてしまいます：

```dart
var n = 21;
print(n.doubled());    // 42
print(4.doubled());    // 8
print((-3).doubled()); // -6
```

# --instructions--

パラメータを取らず、数値を自分自身で掛けた結果を返すメソッド `squared` を持つ、`int` に対する `Squaring` という名前の拡張を記述してください。

例：
```dart
print(5.squared()); // 25
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`5.squared()` は `25` であるべきです

```dart
  test('test1', () {
    expect(5.squared(), 25, reason: '--err-t1--');
  });
```

`(-3).squared()` は `9` であるべきです

```dart
  test('test2', () {
    expect((-3).squared(), 9, reason: '--err-t2--');
  });
```

`0.squared()` は `0` であるべきで、結果は `int` であるべきです

```dart
  test('test3', () {
    expect(0.squared(), 0, reason: '--err-t3--');
    expect(12.squared(), isA<int>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
extension Squaring on int {
  int squared() => this * this;
}
```
