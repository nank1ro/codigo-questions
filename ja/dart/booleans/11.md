---
language: dart
exerciseType: 1
---

# --instructions--

`||`演算子（OR）は、**少なくとも1つの**オペランドが`true`の場合に`true`を返します。

`||`演算子を使用して`false || true`を組み合わせるブーリアン変数`result`を宣言してください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result`は`true`と等しくなければなりません。

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result`は`bool`型でなければなりません。

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = false || true;
```
