---
language: dart
exerciseType: 1
---

# --instructions--

`&&`、`||`、および`!`を組み合わせて結果を生成します。

この正確な式を使用してブーリアン変数`result`を宣言してください：`!false && (true || false)`。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result`は`true`と等しくなければなりません。

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
  });
```

`result`は`bool`型でなければなりません。

```dart
  test('test2', () {
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool result = !false && (true || false);
```
