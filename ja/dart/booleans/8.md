---
language: dart
exerciseType: 1
---

# --instructions--

`isEmpty`というブーリアン変数を宣言し、値`false`を割り当ててください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isEmpty`は`false`と等しくなければなりません。

```dart
  test('test1', () {
    expect(isEmpty, false, reason: '--err-t1--');
  });
```

`isEmpty`は`bool`型でなければなりません。

```dart
  test('test2', () {
    expect(isEmpty, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isEmpty = false;
```
