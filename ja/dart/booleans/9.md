---
language: dart
exerciseType: 1
---

# --instructions--

値`true`を持つ`flag`というブーリアン変数を宣言してから、別のブーリアン変数`isFlag`を宣言し、`is`キーワードを使用して`flag`が`bool`型であるかどうかを確認してください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`flag`は`true`と等しくなければなりません。

```dart
  test('test1', () {
    expect(flag, true, reason: '--err-t1--');
  });
```

`isFlag`は`true`である必要があります（`is bool`チェックの結果）。

```dart
  test('test2', () {
    expect(isFlag, true, reason: '--err-t2--');
  });
```

`isFlag`は`bool`型でなければなりません。

```dart
  test('test3', () {
    expect(isFlag, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool flag = true;
bool isFlag = flag is bool;
```
