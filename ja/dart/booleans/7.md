---
language: dart
exerciseType: 1
---

# --instructions--

`isReady`というブーリアン変数を宣言し、値`true`を割り当ててください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isReady`は`true`と等しくなければなりません。

```dart
  test('test1', () {
    expect(isReady, true, reason: '--err-t1--');
  });
```

`isReady`は`bool`型でなければなりません。

```dart
  test('test2', () {
    expect(isReady, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool isReady = true;
```
