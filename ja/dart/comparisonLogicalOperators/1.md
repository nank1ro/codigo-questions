---
language: dart
exerciseType: 2
---

# --description--

**等号演算子** `==` は2つの値を比較し、等しい場合は `true`、そうでない場合は `false` を返します。

```dart
int a = 5;
int b = 5;
bool result = a == b; // true
```

比較の結果は常に `bool` 値です。

# --instructions--

`10` が `10` と等しいかどうかを確認するコードを完成させ、結果を `isEqual` に保存します。

# --seed--

```dart
void main() {
  bool isEqual = 10 [/] 10;
  print(isEqual);
}
```

# --answers--

- ==
- !=
- =
- >=

# --solutions--

```dart
void main() {
  bool isEqual = 10 == 10;
  print(isEqual);
}
```

# --output--

true
