---
language: dart
exerciseType: 1
---

# --instructions--

2つのトップレベル `int` 変数を宣言します: 値 `5` を持つ `a` と値 `9` を持つ `b`。その後、`!=` 演算子で `a` と `b` を比較した結果を格納する `notEqual` という名前のトップレベル `bool` 変数を宣言します。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`notEqual` は `true` で、`bool` 型でなければならない

```dart
  test('test1', () {
    expect(notEqual, true, reason: '--err-t1--');
    expect(notEqual, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 5;
int b = 9;
bool notEqual = a != b;

void main() {
  print(notEqual);
}
```
