---
language: dart
exerciseType: 1
---

# --instructions--

3つのトップレベル `int` 変数を宣言します: 値 `5` を持つ `x`、値 `3` を持つ `y`、値 `0` を持つ `z`。その後、`!(x > 10) || (y < 5 && z == 0)` を評価する `result` という名前のトップレベル `bool` 変数を宣言します。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` は `true` で、`bool` 型でなければならない

```dart
  test('test1', () {
    expect(result, true, reason: '--err-t1--');
    expect(result, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 5;
int y = 3;
int z = 0;
bool result = !(x > 10) || (y < 5 && z == 0);

void main() {
  print(result);
}
```
