---
language: dart
exerciseType: 1
---

# --instructions--

3つのトップレベル `int` 変数を宣言します: 値 `42` を持つ `number`、値 `1` を持つ `min`、値 `100` を持つ `max`。その後、`number` が `min` 以上 AND `max` 以下のときに `true` になる `inRange` という名前のトップレベル `bool` 変数を宣言します。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`inRange` は `true` で、`bool` 型でなければならない

```dart
  test('test1', () {
    expect(inRange, true, reason: '--err-t1--');
    expect(inRange, TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int number = 42;
int min = 1;
int max = 100;
bool inRange = number >= min && number <= max;

void main() {
  print(inRange);
}
```
