---
language: dart
exerciseType: 2
---

# --description--

**不等号演算子** `!=` は、2つの値が**異なる**場合は `true`、同じ場合は `false` を返します。

```dart
int x = 3;
int y = 7;
bool result = x != y; // true
```

`==` の反対です。

# --instructions--

`4` が `9` と等しくないかどうかを確認するコードを完成させ、結果を `isDifferent` に保存します。

# --seed--

```dart
void main() {
  bool isDifferent = 4 [/] 9;
  print(isDifferent);
}
```

# --answers--

- !=
- ==
- !
- <>

# --solutions--

```dart
void main() {
  bool isDifferent = 4 != 9;
  print(isDifferent);
}
```

# --output--

true
