---
language: dart
exerciseType: 4
---

# --instructions--

コード行を並べ替えて、温度値を宣言し、`&&` を使ってそれが `0` から `100` の範囲内（両端を含む）にあるかどうかを確認し、結果を `isValid` に保存し、それを出力します。出力は `true` である必要があります。

# --answers--

```dart
void main() {
```

```dart
  int temperature = 37;
```

```dart
  bool isValid = temperature >= 0 && temperature <= 100;
```

```dart
  print(isValid);
```

```dart
}
```

# --solutions--

```dart
void main() {
  int temperature = 37;
  bool isValid = temperature >= 0 && temperature <= 100;
  print(isValid);
}
```

# --output--

true
