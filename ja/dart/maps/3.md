---
language: dart
exerciseType: 2
---

# --description--

`map[key] = value` で代入すると、キーがまだマップにない場合は新しいペアを**追加**し、既存のキーの場合はその値を**更新**します：

```dart
var ages = {'Ann': 30};
ages['Bob'] = 25; // adds Bob
ages['Ann'] = 31; // updates Ann
print(ages); // {Ann: 31, Bob: 25}
```

新しいキーは既存のキーの後に追加されるため、マップは挿入順を記憶します。

# --instructions--

`apple` の数量を `5` に更新し、その後 `pear` を数量 `2` で追加するようにコードを完成させてください。

# --seed--

```dart
var stock = {'apple': 3};
stock[/] = 5;
stock[/] = 2;
print(stock);
```

# --answers--

- ['apple']
- ['pear']
- .apple
- ('pear')
- 'apple'
- [0]

# --solutions--

```dart
var stock = {'apple': 3};
stock['apple'] = 5;
stock['pear'] = 2;
print(stock);
```

# --output--

{apple: 5, pear: 2}
