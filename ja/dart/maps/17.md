---
language: dart
exerciseType: 4
---

# --instructions--

`ifAbsent` を使った `.update()` でリストの各単語の出現回数を数え、結果のマップを出力するようにコードの行を並び替えてください。

# --answers--

```dart
void main() {
```

```dart
  var counts = <String, int>{};
```

```dart
  for (var word in ['a', 'b', 'a']) {
```

```dart
    counts.update(word, (n) => n + 1, ifAbsent: () => 1);
```

```dart
  }
```

```dart
  print(counts);
```

```dart
}
```

# --solutions--

```dart
void main() {
  var counts = <String, int>{};
  for (var word in ['a', 'b', 'a']) {
    counts.update(word, (n) => n + 1, ifAbsent: () => 1);
  }
  print(counts);
}
```

# --output--

{a: 2, b: 1}
