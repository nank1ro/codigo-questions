---
language: dart
exerciseType: 2
---

# --description--

一部の文字は引用符の中に直接入力できません。**エスケープシーケンス**はバックスラッシュで始まります: `\n` は改行、`\t` はタブ、`\\` はバックスラッシュ、`\$` はドル記号そのもの（そうしないと `$` は補間を開始してしまいます）です:

```dart
print('one\ntwo');   // prints one and two on separate lines
print('Cost: \$5');  // Cost: $5
```

**生文字列**（raw string）は `r` を接頭辞として付けます。その中ではバックスラッシュと `$` は単なる文字であり、エスケープも補間も行われません:

```dart
print(r'C:\new\folder'); // C:\new\folder
print(r'Cost: $5');      // Cost: $5
```

複数行にまたがるテキストには、トリプルクォート `'''` または `"""` で区切られた**複数行文字列**を使います。その中の改行はそのまま保持されます。

```dart
var poem = '''
roses are red
violets are blue''';
```

# --instructions--

生文字列を使って `Price: $5` を出力し、その後エスケープシーケンスを使って `first` と `second` を2行に分けて出力するようにコードを完成させてください。

# --seed--

```dart
print([/]'Price: $5');
print('first[/]second');
```

# --answers--

- r
- \n
- raw
- R
- \t
- /n

# --solutions--

```dart
print(r'Price: $5');
print('first\nsecond');
```

# --output--

Price: $5
first
second
