---
language: dart
exerciseType: 4
---

# --description--

ユーザーが入力したテキストには、前後に余分なスペースが入っていることがよくあります。`.trim()` メソッドは、先頭と末尾の空白（スペース、タブ、改行）を取り除いたコピーを返します。`.trimLeft()` と `.trimRight()` はどちらか一方だけを取り除きます:

```dart
var input = '   hello  ';
print('[${input.trim()}]');      // [hello]
print('[${input.trimLeft()}]');  // [hello  ]
```

# --instructions--

`input` の前後のスペースを取り除き、クリーンなテキストを大文字で出力するようにコード行を並べ替えてください。

# --answers--

```dart
void main() {
```

```dart
  var input = '  hello  ';
```

```dart
  var clean = input.trim();
```

```dart
  print(clean.toUpperCase());
```

```dart
}
```

# --solutions--

```dart
void main() {
  var input = '  hello  ';
  var clean = input.trim();
  print(clean.toUpperCase());
}
```

# --output--

HELLO
