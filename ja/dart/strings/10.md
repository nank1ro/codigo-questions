---
language: dart
exerciseType: 1
---

# --description--

`.split(separator)` メソッドは、セパレーターが現れるたびに文字列を切り分けて `List<String>` にします。その逆が `.join(separator)` で、これはリストのメソッドであり、要素を1つの文字列につなぎ合わせます:

```dart
var csv = 'a,b,c';
List<String> parts = csv.split(',');
print(parts);            // [a, b, c]
print(parts.join(' - ')); // a - b - c
```

空のセパレーターで `.split('')` を呼び出すと、1文字ずつのリストが得られます。

# --instructions--

文字列 `csv` が与えられたとき、カンマで区切られたその部分を `List<String>` の `colors` に、それらの部分を `' | '` で連結したものを `String` の `joined` に格納してください。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var csv = 'red,green,blue';
```

# --after-seed--

```dart
  return [colors, joined];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`colors` は `['red', 'green', 'blue']` である必要があります

```dart
  test('test1', () {
    expect(result()[0], ['red', 'green', 'blue'], reason: '--err-t1--');
  });
```

`joined` は `'red | green | blue'` である必要があります

```dart
  test('test2', () {
    expect(result()[1], 'red | green | blue', reason: '--err-t2--');
  });
```

`colors` は `List` である必要があります

```dart
  test('test3', () {
    expect(result()[0], isA<List>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var csv = 'red,green,blue';
List<String> colors = csv.split(',');
String joined = colors.join(' | ');
```
