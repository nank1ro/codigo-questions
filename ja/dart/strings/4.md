---
language: dart
exerciseType: 1
---

# --description--

すべての文字列は `.length` プロパティを通じて、自身が持つ文字数を把握しています。スペースや句読点も文字としてカウントされます:

```dart
var word = 'hello';
print(word.length);      // 5
print('a b'.length);     // 3
```

角括弧とその**インデックス**（`0` から始まる）を使って、1文字だけを読み取ることができます。結果は1文字の `String` になります:

```dart
print(word[0]);                // h
print(word[word.length - 1]);  // o
```

文字列の範囲外のインデックス（`word[5]` など）を読み取るとエラーが発生します。

# --instructions--

文字列 `word` が与えられたとき、その文字数を `int` の `size` に、最初の文字を `String` の `first` に、最後の文字を `String` の `last` に格納してください。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var word = 'Flutter';
```

# --after-seed--

```dart
  return [size, first, last];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`size` は `7` である必要があります

```dart
  test('test1', () {
    expect(result()[0], 7, reason: '--err-t1--');
  });
```

`first` は `'F'` である必要があります

```dart
  test('test2', () {
    expect(result()[1], 'F', reason: '--err-t2--');
  });
```

`last` は `'r'` である必要があります

```dart
  test('test3', () {
    expect(result()[2], 'r', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var word = 'Flutter';
int size = word.length;
String first = word[0];
String last = word[word.length - 1];
```
