---
language: dart
exerciseType: 1
---

# --description--

内部では、文字列のすべての文字は数値、つまり**コードユニット**（UTF-16コード）として格納されています。`.codeUnitAt(index)` は1文字分のコードを、`.codeUnits` はそのリスト全体を返します。`String.fromCharCode(code)` は逆に、コードから文字列を作成します:

```dart
var word = 'AB';
print(word.codeUnitAt(0));         // 65
print(word.codeUnits);             // [65, 66]
print(String.fromCharCode(67));    // C
```

連続する文字は連続するコードを持ちます。`'A'` は65、`'B'` は66、というように続きます。

# --instructions--

文字列 `letter` が与えられたとき、そのコードユニットを `int` の `code` に、コードが `code + 1` である文字を `String` の `next` に格納してください。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
var letter = 'D';
```

# --after-seed--

```dart
  return [code, next];
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`code` は `68` である必要があります

```dart
  test('test1', () {
    expect(result()[0], 68, reason: '--err-t1--');
  });
```

`next` は `'E'` である必要があります

```dart
  test('test2', () {
    expect(result()[1], 'E', reason: '--err-t2--');
  });
```

`next` は `String` である必要があります

```dart
  test('test3', () {
    expect(result()[1], isA<String>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var letter = 'D';
int code = letter.codeUnitAt(0);
String next = String.fromCharCode(code + 1);
```
