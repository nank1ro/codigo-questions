---
language: dart
exerciseType: 1
---

# --instructions--

2つのトップレベル `int` 変数を宣言します：値 `12` の `a` と値 `8` の `b`。次に、合計を格納するトップレベル `int` 変数 `total` を宣言します。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`a` は `12`、`b` は `8`、`total` は `20` である必要があります

```dart
  test('test1', () {
    expect(a, 12, reason: '--err-t1--');
    expect(b, 8, reason: '--err-t2--');
    expect(total, 20, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 12;
int b = 8;
int total = a + b;

void main() {
  print(total);
}
```
