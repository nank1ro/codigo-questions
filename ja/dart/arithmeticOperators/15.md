---
language: dart
exerciseType: 1
---

# --instructions--

3つのトップレベル `int` 変数を宣言します：値 `10` の `a`、値 `3` の `b`、値 `2` の `c`。次に以下を宣言します：
- `sum` = `a + b`
- `product` = `b * c`
- `combined` = `sum - product`

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` は `13`、`product` は `6`、`combined` は `7` である必要があります

```dart
  test('test1', () {
    expect(sum, 13, reason: '--err-t1--');
    expect(product, 6, reason: '--err-t2--');
    expect(combined, 7, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int a = 10;
int b = 3;
int c = 2;
int sum = a + b;
int product = b * c;
int combined = sum - product;

void main() {
  print(combined);
}
```
