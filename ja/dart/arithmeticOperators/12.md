---
language: dart
exerciseType: 1
---

# --instructions--

2つのトップレベル `int` 変数を宣言します：値 `7` の `width` と値 `9` の `height`。次に、それらの積を格納するトップレベル `int` 変数 `area` を宣言します。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`area` は `63` に等しい必要があります

```dart
  test('test1', () {
    expect(area, 63, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int width = 7;
int height = 9;
int area = width * height;

void main() {
  print(area);
}
```
