---
language: dart
exerciseType: 1
---

# --instructions--

トップレベル `double` 変数 `result` を宣言して、`7` を `2` で除算した結果を格納します。Dartでは `/` は常に `double` を返すことを忘れないでください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` は値 `3.5` の `double` である必要があります

```dart
  test('test1', () {
    expect(result, isA<double>(), reason: '--err-t1--');
    expect(result, closeTo(3.5, 0.001), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
double result = 7 / 2;

void main() {
  print(result);
}
```
