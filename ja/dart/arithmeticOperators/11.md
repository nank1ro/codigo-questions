---
language: dart
exerciseType: 1
---

# --instructions--

2つのトップレベル `int` 変数を宣言します：値 `25` の `x` と値 `9` の `y`。次に、`y` を `x` から減算した結果を格納するトップレベル `int` 変数 `diff` を宣言します。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`diff` は `16` に等しい必要があります

```dart
  test('test1', () {
    expect(diff, 16, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 25;
int y = 9;
int diff = x - y;

void main() {
  print(diff);
}
```
