---
language: dart
exerciseType: 2
---

# --description--

**除算演算子** `/` は一方の値を別の値で除算します。Dartでは、`/` は結果が整数の場合でも**常に `double` を返します**。

```dart
int a = 10;
int b = 4;
double result = a / b; // 2.5

double exact = 10 / 2; // 5.0  (not 5!)
```

`/` の結果を格納するには常に `double` 変数を使用してください。

# --instructions--

コードを完成させて `9` を `4` で除算し、結果を `result` に格納します。

# --seed--

```dart
void main() {
  double result = 9 [/] 4;
  print(result);
}
```

# --answers--

- /
- *
- +
- -

# --solutions--

```dart
void main() {
  double result = 9 / 4;
  print(result);
}
```

# --output--

2.25
