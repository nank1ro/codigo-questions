---
language: dart
exerciseType: 1
---

# --instructions--

2つのトップレベル `int` 変数を宣言します：値 `29` の `total` と値 `4` の `perGroup`。次に、`29` に何個の完全な `4` グループが収まるかを格納するトップレベル `int` 変数 `groups` を宣言します（整数除算 `~/` を使用）。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`groups` は `7` に等しい必要があります

```dart
  test('test1', () {
    expect(groups, 7, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int total = 29;
int perGroup = 4;
int groups = total ~/ perGroup;

void main() {
  print(groups);
}
```
