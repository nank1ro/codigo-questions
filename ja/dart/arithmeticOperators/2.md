---
language: dart
exerciseType: 2
---

# --description--

**減算演算子** `-` は一方の値からもう一方の値を減算します。

```dart
int a = 10;
int b = 4;
int diff = a - b; // 6
```

2番目のオペランドが最初のオペランドより大きい場合、結果は負になる可能性があります。

# --instructions--

コードを完成させて `20` から `9` を減算し、結果を `diff` に格納します。

# --seed--

```dart
void main() {
  int diff = 20 [/] 9;
  print(diff);
}
```

# --answers--

- -
- +
- *
- /

# --solutions--

```dart
void main() {
  int diff = 20 - 9;
  print(diff);
}
```

# --output--

11
