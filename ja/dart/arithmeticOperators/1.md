---
language: dart
exerciseType: 2
---

# --description--

**加算演算子** `+` は2つの値を加算します。

```dart
int a = 3;
int b = 4;
int sum = a + b; // 7
```

`int` または `double` の値で `+` を使用できます。

# --instructions--

コードを完成させて `15` と `7` を加算し、結果を `sum` に格納します。

# --seed--

```dart
void main() {
  int sum = 15 [/] 7;
  print(sum);
}
```

# --answers--

- +
- -
- *
- /

# --solutions--

```dart
void main() {
  int sum = 15 + 7;
  print(sum);
}
```

# --output--

22
