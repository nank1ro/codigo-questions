---
language: dart
exerciseType: 2
---

# --description--

**整数除算演算子** `~/` は2つの数を除算し、結果を整数に**切り詰めます**（小数部分を破棄します）。

```dart
int a = 10;
int b = 3;
int result = a ~/ b; // 3  (not 3.333...)
```

余りのない整数商が必要な場合は `~/` を使用してください。

# --instructions--

コードを完成させて `17` を `5` で整数除算し、結果を `result` に格納します。

# --seed--

```dart
void main() {
  int result = 17 [/] 5;
  print(result);
}
```

# --answers--

- ~/
- /
- %
- *

# --solutions--

```dart
void main() {
  int result = 17 ~/ 5;
  print(result);
}
```

# --output--

3
