---
language: dart
exerciseType: 1
---

# --instructions--

店舗が `itemCount` 個の商品を `pricePerItem` ドルで販売し、`discount` ドルの割引を合計から差し引きます。これら3つのトップレベル `int` 変数を宣言します：`itemCount` = `6`、`pricePerItem` = `15`、`discount` = `10`。次に、割引後の合計を計算するトップレベル `int` 変数 `totalPrice` を宣言します。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`totalPrice` は `80` に等しい必要があります

```dart
  test('test1', () {
    expect(totalPrice, 80, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int itemCount = 6;
int pricePerItem = 15;
int discount = 10;
int totalPrice = itemCount * pricePerItem - discount;

void main() {
  print(totalPrice);
}
```
