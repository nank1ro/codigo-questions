---
language: dart
exerciseType: 1
---

# --instructions--

モジュロ演算子 `%` を使用して `17` を `5` で除算した余りを求めます。結果を `remainder` という名前のトップレベル変数に格納します。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

変数 `remainder` は `2` に等しい必要があります

```dart
  test('test1', () {
    expect(remainder, 2, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int remainder = 17 % 5;

void main() {
  print(remainder);
}
```
