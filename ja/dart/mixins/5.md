---
language: dart
exerciseType: 1
---

# --description--

ミックスインはメンバーを**本体なしで**宣言することもできます。そのようなメンバーは抽象メンバーです。ミックスインがそれを使い、ミックスインを取り込むクラスがそれを提供しなければなりません。

```dart
mixin Greeting {
  String get name;                       // no body: the class provides it

  String greet() => 'Hello, $name!';
}

class Person with Greeting {
  @override
  final String name;

  Person(this.name);
}

void main() {
  print(Person('Ada').greet()); // Hello, Ada!
}
```

振る舞いはミックスインが、データはクラスがもたらします。`final String name;` のようなクラスのフィールドがあれば、同名の抽象ゲッターを満たすのに十分です。

# --instructions--

次の内容を持つミックスイン `Labelled` を宣言してください：
- 本体なしで宣言された `String` 型の抽象ゲッター `title`
- `title` を角括弧で囲んで返すメソッド `label`

`Labelled` を使い、`final String` 型のフィールド `title` とそれを受け取るコンストラクタを持つクラス `Book` を宣言してください。

例：
```dart
print(Book('Dune').label()); // [Dune]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Book('Dune').label()` は `'[Dune]'` であるべきです

```dart
  test('test1', () {
    expect(Book('Dune').label(), '[Dune]', reason: '--err-t1--');
  });
```

`Book('').label()` は `'[]'` であり、`Book('a b').label()` は `'[a b]'` であるべきです

```dart
  test('test2', () {
    expect(Book('').label(), '[]', reason: '--err-t2--');
    expect(Book('a b').label(), '[a b]', reason: '--err-t2--');
  });
```

`Book` は自分の `title` を保ち、`Labelled` でもあるべきです

```dart
  test('test3', () {
    final book = Book('Dart');
    expect(book.title, 'Dart', reason: '--err-t3--');
    expect(book, isA<Labelled>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Labelled {
  String get title;

  String label() => '[$title]';
}

class Book with Labelled {
  @override
  final String title;

  Book(this.title);
}
```
