---
language: dart
exerciseType: 2
---

# --description--

最後のミックスインが勝つため、`with` リストの順序はスタイルの細部ではなくクラスの意味の一部です。並べ替えると、オブジェクトが最終的に持つ実装が変わります：

```dart
mixin Plain {
  String format(String text) => text;
}

mixin Starred {
  String format(String text) => '*$text*';
}

class Fancy with Plain, Starred {}  // format comes from Starred
class Simple with Starred, Plain {} // format comes from Plain
```

1つのミックスインだけが宣言しているメンバーは決して競合しません。順序にかかわらず利用できます。`with X, Y` は「`X` から始めて、`Y` にそれを上書きさせる」と読みます。

# --instructions--

`with` リストを完成させて、`Message` が各ミックスインを1回ずつ使うようにしてください。`format` はテキストをそのまま残し、`frame` が使える状態になります。

# --seed--

```dart
mixin Plain {
  String format(String text) => text;
}

mixin Starred {
  String format(String text) => '*$text*';

  String frame(String text) => '($text)';
}

class Message with [/], [/] {}

void main() {
  print(Message().format('hi'));
  print(Message().frame('hi'));
}
```

# --answers--

- Starred
- Plain
- Message
- format
- frame

# --solutions--

```dart
mixin Plain {
  String format(String text) => text;
}

mixin Starred {
  String format(String text) => '*$text*';

  String frame(String text) => '($text)';
}

class Message with Starred, Plain {}

void main() {
  print(Message().format('hi'));
  print(Message().frame('hi'));
}
```

# --output--

hi
(hi)
