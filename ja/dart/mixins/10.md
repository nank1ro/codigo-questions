---
language: dart
exerciseType: 1
---

# --description--

クラスの本体はチェーンのいちばん末尾にあるので、クラスで宣言したメンバーは、そのミックスインから来る同名のメンバーを**上書き**します。上書きの中では、**`super`** でミックスインが提供したバージョンに届きます：

```dart
mixin Polite {
  String greet() => 'Hello';
}

class Host with Polite {
  @override
  String greet() => '${super.greet()}, welcome!';
}

class Guest with Polite {}

void main() {
  print(Host().greet());  // Hello, welcome!
  print(Guest().greet()); // Hello
}
```

ミックスイン自身はそのままです。`Guest` は元の `greet` をそのまま受け取ります。`super.greet()` があるからこそ、`Host` は共有される振る舞いをコピーせずにその上に構築できるのです。

# --instructions--

`'signed'` を返すメソッド `sign` を持つミックスイン `Signed` を宣言してください。

`Signed` を使い、`super` を呼び出して得たミックスインの結果の後に `' by Ada'` を付けたものを返すよう `sign` を上書きするクラス `Letter` を宣言してください。

`Signed` を使い、何も上書きしないクラス `Note` を宣言してください。

例：
```dart
print(Letter().sign()); // signed by Ada
print(Note().sign());   // signed
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Letter().sign()` は `'signed by Ada'` であるべきです

```dart
  test('test1', () {
    expect(Letter().sign(), 'signed by Ada', reason: '--err-t1--');
  });
```

ミックスインは変わっていないので、`Note().sign()` はやはり `'signed'` であるべきです

```dart
  test('test2', () {
    expect(Note().sign(), 'signed', reason: '--err-t2--');
  });
```

両方のクラスはミックスインを使うべきで、上書きはミックスイン型を通しても使われるべきです

```dart
  test('test3', () {
    final Signed letter = Letter();
    expect(letter.sign(), 'signed by Ada', reason: '--err-t3--');
    expect(Note(), isA<Signed>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
mixin Signed {
  String sign() => 'signed';
}

class Letter with Signed {
  @override
  String sign() => '${super.sign()} by Ada';
}

class Note with Signed {}
```
