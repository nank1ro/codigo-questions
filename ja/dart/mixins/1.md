---
language: dart
exerciseType: 2
---

# --description--

クラスが `extend` できるスーパークラスは1つだけですが、それ以外に共通点のないクラスどうしが同じ振る舞いを必要とすることはよくあります。**ミックスイン（mixin）**は、任意の数のクラスが取り込める再利用可能な振る舞いの断片です。

**`mixin`** キーワードで宣言し、クラスは **`with`** キーワードで取り込みます：

```dart
mixin Swimmer {
  String swim() => 'swimming';
}

class Fish with Swimmer {}

void main() {
  print(Fish().swim()); // swimming
}
```

`Fish` は自分のメンバーを1つも宣言していませんが、すべての `Fish` は `swim` を持ちます。ミックスインのメンバーがクラスのメンバーになるからです。ミックスインは、関係のあるなしにかかわらず、好きな数のクラスで使えます。

# --instructions--

コードを完成させて、`Greeter` がミックスインになり、`Robot` がそれを使うようにしてください。プログラムが `hello` を出力するようにします。

# --seed--

```dart
[/] Greeter {
  String greet() => 'hello';
}

class Robot [/] Greeter {}

void main() {
  print(Robot().greet());
}
```

# --answers--

- mixin
- with
- extends
- implements
- on
- abstract

# --solutions--

```dart
mixin Greeter {
  String greet() => 'hello';
}

class Robot with Greeter {}

void main() {
  print(Robot().greet());
}
```

# --output--

hello
