---
language: dart
exerciseType: 4
---

# --description--

`StreamController` は同じ場所で作られて消費されることがよくあります。`listen` で `controller.stream` を購読し、それから `add` でイベントを追加し、`close` でコントローラーを閉じます。`listen` は待たないので、イベントは現在のコードが終わったあとに配信されますが、必ず追加された順に届きます：

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>();
  controller.stream.listen((n) => print('got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
// got 1
// got 2
```

# --instructions--

項目を並べ替えて、`dart:async` をインポートし、`StreamController<String>` を作り、そのストリームを購読して各値を出力し、`hello` に続いて `world` を追加し、最後にコントローラーを閉じてください。値を追加する前に `listen` で購読します。プログラムは次のように出力しなければなりません：

```
hello
world
```

# --answers--

```dart
import 'dart:async';
```

```dart
void main() {
```

```dart
  final controller = StreamController<String>();
```

```dart
  controller.stream.listen((word) => print(word));
```

```dart
  controller.add('hello');
```

```dart
  controller.add('world');
```

```dart
  controller.close();
```

```dart
}
```

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>();
  controller.stream.listen((word) => print(word));
  controller.add('hello');
  controller.add('world');
  controller.close();
}
```

# --output--

hello
world
