---
language: dart
exerciseType: 4
---

# --description--

`async*` ジェネレーターは `yield*`（yield-star）で**別のストリームのすべてのイベント**を転送できます。各値を yield する `await for` ループを、1行で書いたようなものです：

```dart
Stream<int> ones() async* {
  yield 1;
  yield 1;
}

Stream<int> sequence() async* {
  yield 0;
  yield* ones();
  yield 2;
}
// sequence() emits 0, 1, 1, 2
```

内側のストリームが完了すると、外側のストリームは自分の `yield` を続けます。

# --instructions--

項目を並べ替えて、`1` と `2` を送り出す `async*` 関数 `pair`、`0`、続いて `pair()` のすべての値、続いて `3` を送り出す `async*` 関数 `all`、そして `all()` の各値を出力する `main` を定義してください。先に `pair`、次に `all`、次に `main` を定義します。プログラムは次のように出力しなければなりません：

```
0
1
2
3
```

# --answers--

```dart
Stream<int> pair() async* {
```

```dart
  yield 1;
```

```dart
  yield 2;
```

```dart
}
```

```dart
Stream<int> all() async* {
```

```dart
  yield 0;
```

```dart
  yield* pair();
```

```dart
  yield 3;
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  await for (final n in all()) {
```

```dart
    print(n);
```

```dart
  }
```

```dart
}
```

# --solutions--

```dart
Stream<int> pair() async* {
  yield 1;
  yield 2;
}

Stream<int> all() async* {
  yield 0;
  yield* pair();
  yield 3;
}

Future<void> main() async {
  await for (final n in all()) {
    print(n);
  }
}
```

# --output--

0
1
2
3
