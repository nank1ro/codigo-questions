---
language: dart
exerciseType: 2
---

# --description--

コントローラーは名前付きコンストラクター `StreamController<T>.broadcast()` で直接ブロードキャストストリームを作れます。その `stream` はいくつでもリスナーを受け入れ、各イベントは購読した順に全員へ配信されます：

```dart
import 'dart:async';

void main() {
  final controller = StreamController<String>.broadcast();
  controller.stream.listen((msg) => print('first: $msg'));
  controller.stream.listen((msg) => print('second: $msg'));
  controller.add('hi');
  controller.close();
}
// first: hi
// second: hi
```

どのブロードキャストストリームとも同じく、バッファーは持ちません。リスナーが購読する前に追加されたイベントは、そのリスナーにとっては失われます。

# --instructions--

ブロードキャストコントローラーを作り、2つのリスナー `A` と `B` を購読させ、値 `1` と `2` を送り、それからコントローラーを閉じるようにコードを完成させ、プログラムが `A got 1`、`B got 1`、`A got 2`、`B got 2` を出力するようにしてください。

# --seed--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.[/]();
  controller.stream.[/]((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.[/](1);
  controller.add(2);
  controller.[/]();
}
```

# --answers--

- broadcast
- listen
- add
- close
- asBroadcastStream
- subscribe
- sink

# --solutions--

```dart
import 'dart:async';

void main() {
  final controller = StreamController<int>.broadcast();
  controller.stream.listen((n) => print('A got $n'));
  controller.stream.listen((n) => print('B got $n'));
  controller.add(1);
  controller.add(2);
  controller.close();
}
```

# --output--

A got 1
B got 1
A got 2
B got 2
