---
language: dart
exerciseType: 2
---

# --description--

`await for` はストリームが完了するまで現在の関数を一時停止します。**待たずに**イベントへ反応したいときは、`listen` を呼んでコールバックを渡します。コールバックはイベントごとに1回呼ばれ、`listen` のあとのコードはすぐに実行されます。

```dart
final ticks = Stream.fromIterable([1, 2]);
ticks.listen((tick) => print('tick $tick'));
print('not waiting');
// not waiting
// tick 1
// tick 2
```

`listen` は名前付きパラメーター `onDone` も受け取ります。これはストリームが終わったときに呼ばれる、引数のない関数です：

```dart
final ticks2 = Stream.fromIterable([1, 2]);
ticks2.listen((tick) => print(tick), onDone: () => print('no more ticks'));
```

# --instructions--

ストリームが購読され、各文字が出力され、ストリームが終わったときに `done` が出力されるようにコードを完成させてください。プログラムは `listening`、続いて `a`、`b`、`done` を出力しなければなりません。

# --seed--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.[/]((letter) => print(letter), [/]: () => print('done'));
  print('listening');
}
```

# --answers--

- listen
- onDone
- onError
- forEach
- await

# --solutions--

```dart
void main() {
  final letters = Stream.fromIterable(['a', 'b']);
  letters.listen((letter) => print(letter), onDone: () => print('done'));
  print('listening');
}
```

# --output--

listening
a
b
done
