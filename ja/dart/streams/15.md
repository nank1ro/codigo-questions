---
language: dart
exerciseType: 4
---

# --description--

ストリームは値だけでなく**エラー**も運べます。`async*` ジェネレーターの中では `throw` がエラーイベントを送ってストリームを終わらせます。`StreamController` は `addError` でエラーを送れます。

消費する側では、`await for` ループがループのある場所でエラーを再スローするので、ループを普通の `try`/`catch` で囲んで処理します：

```dart
Stream<int> risky() async* {
  yield 1;
  throw StateError('sensor offline');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('caught: $e');
  }
}
// 1
// caught: Bad state: sensor offline
```

`listen` の場合は、代わりに `onError` コールバックを渡します：`stream.listen(print, onError: (e) => print('caught: $e'));`

# --instructions--

項目を並べ替えて、`1` を送り出したあと `FormatException('bad')` を投げる `async*` 関数 `risky` と、`try`/`catch` の中で `risky()` の各値を出力し、捕まえたときに `error: ` に続けてエラーを出力し、`try`/`catch` ブロック全体のあと、その外側で `finished` を出力する `async` の `main` を定義してください。`risky` は `main` より前に定義します。プログラムは次のように出力しなければなりません：

```
1
error: FormatException: bad
finished
```

# --answers--

```dart
Stream<int> risky() async* {
```

```dart
  yield 1;
```

```dart
  throw FormatException('bad');
```

```dart
}
```

```dart
Future<void> main() async {
```

```dart
  try {
```

```dart
    await for (final n in risky()) {
```

```dart
      print(n);
```

```dart
    }
```

```dart
  } catch (e) {
```

```dart
    print('error: $e');
```

```dart
  }
```

```dart
  print('finished');
```

```dart
}
```

# --solutions--

```dart
Stream<int> risky() async* {
  yield 1;
  throw FormatException('bad');
}

Future<void> main() async {
  try {
    await for (final n in risky()) {
      print(n);
    }
  } catch (e) {
    print('error: $e');
  }
  print('finished');
}
```

# --output--

1
error: FormatException: bad
finished
