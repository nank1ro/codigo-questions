---
language: dart
exerciseType: 1
---

# --description--

`Stream.fromIterable` にはすべての値を前もって渡す必要があります。値を1つずつ**生成**するには、**非同期ジェネレーター**を書きます。本体が `async*` で印を付けられ、戻り値の型が `Stream<T>` の関数です。その中で `yield` がストリームに1つのイベントを送ります：

```dart
Stream<int> countTo(int n) async* {
  for (var i = 1; i <= n; i++) {
    yield i;
  }
}
```

`countTo(3)` を呼んでも本体は実行されません。リスナーが値を求めるのに応じて遅延実行され、本体が終わるとストリームは完了します。

すべてのイベントを `List` に集めるには `toList()` を呼びます。これは `Future<List<T>>` を返すので、`await` で待ちます：

```dart
final values = await countTo(3).toList();
print(values); // [1, 2, 3]
```

# --instructions--

`int` の `from` を受け取り、`from` から `1` まで下っていく値を送り出す `Stream<int>` を返す `async*` 関数 `countdown` を書いてください。`from` が `0` 以下なら、ストリームは何も送り出しません。

例：
```dart
print(await countdown(3).toList()); // [3, 2, 1]
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`countdown(3)` は `3`、`2`、`1` を送り出すべきです

```dart
  test('test1', () async {
    expect(await countdown(3).toList(), [3, 2, 1], reason: '--err-t1--');
  });
```

`countdown(1)` は `1` だけを送り出すべきです

```dart
  test('test2', () async {
    expect(await countdown(1).toList(), [1], reason: '--err-t2--');
  });
```

`countdown(0)` は何も送り出さず、`countdown` は `Stream<int>` を返すべきです

```dart
  test('test3', () async {
    expect(await countdown(0).toList(), <int>[], reason: '--err-t3--');
    expect(countdown(2), isA<Stream<int>>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Stream<int> countdown(int from) async* {
  for (var i = from; i >= 1; i--) {
    yield i;
  }
}
```
