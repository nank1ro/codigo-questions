---
language: dart
exerciseType: 1
---

# --instructions--
`List<String>` を受け取り、`for-in` ループを使ってすべての要素をスペースで区切って 1 つの文字列に結合して返す関数を書いてください。

# --before-seed--
```dart
String joinWords(List<String> words) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
3 つの単語をスペースで結合する
```dart
    test('three words', () {
      expect(joinWords(['hello', 'dart', 'world']), 'hello dart world', reason: '--err-t1--');
    });
```

単語が 1 つの場合はその単語を返す
```dart
    test('single word', () {
      expect(joinWords(['only']), 'only', reason: '--err-t2--');
    });
```

空のリストは空の文字列を返す
```dart
    test('empty list', () {
      expect(joinWords([]), '', reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  String result = '';
  for (var word in words) {
    if (result.isEmpty) {
      result = word;
    } else {
      result += ' $word';
    }
  }
```
