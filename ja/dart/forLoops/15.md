---
language: dart
exerciseType: 1
---

# --instructions--
かけ算表の（`row`、`col`）の位置にある値を返す関数を書いてください。外側のループが行を反復し、内側のループが列を反復するネストされた `for` ループを使ってください（どちらも 1 始まり）。指定された位置の積を返してください。

# --before-seed--
```dart
int multiplicationCell(int row, int col) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
行 3、列 4 は 12
```dart
    test('3x4 = 12', () {
      expect(multiplicationCell(3, 4), 12, reason: '--err-t1--');
    });
```

行 1、列 1 は 1
```dart
    test('1x1 = 1', () {
      expect(multiplicationCell(1, 1), 1, reason: '--err-t2--');
    });
```

行 7、列 8 は 56
```dart
    test('7x8 = 56', () {
      expect(multiplicationCell(7, 8), 56, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int r = 1; r <= row; r++) {
    for (int c = 1; c <= col; c++) {
      if (r == row && c == col) {
        result = r * c;
      }
    }
  }
```
