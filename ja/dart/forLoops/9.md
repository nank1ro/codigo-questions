---
language: dart
exerciseType: 1
---

# --instructions--
`for` ループを使って 1 から `n`（両端含む）までのすべての整数の合計を返す関数を書いてください。

# --before-seed--
```dart
int sumTo(int n) {
```

# --after-seed--
```dart
  return result;
}
```

# --before-asserts--
```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--
1 から 5 の合計は 15
```dart
    test('sum 1..5', () {
      expect(sumTo(5), 15, reason: '--err-t1--');
    });
```

1 から 1 の合計は 1
```dart
    test('sum 1..1', () {
      expect(sumTo(1), 1, reason: '--err-t2--');
    });
```

1 から 10 の合計は 55
```dart
    test('sum 1..10', () {
      expect(sumTo(10), 55, reason: '--err-t3--');
    });
```

# --after-asserts--
```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--
```dart
  int result = 0;
  for (int i = 1; i <= n; i++) {
    result += i;
  }
```
