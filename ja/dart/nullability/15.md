---
language: dart
exerciseType: 4
---

# --description--

さらに2つの演算子にnull安全版があります。

**null安全カスケード** `?..` は、オブジェクトが `null` でないときだけカスケード操作の連鎖を実行し、そうでなければすべてをスキップします：

```dart
List<int>? numbers;
numbers?..add(1)..add(2); // nothing happens, numbers is still null
```

**null安全スプレッド** `...?` は、null許容のコレクションの要素をリテラルに挿入し、コレクションが `null` のときは何も追加しません：

```dart
List<int>? extra;
print([0, ...?extra]); // [0]

extra = [1, 2];
print([0, ...?extra]); // [0, 1, 2]
```

`?` がなければ、`List<int>?` に対する `...extra` はコンパイルエラーになります。

# --instructions--

項目を並べ替えて、`?..` を使ってnull許容のリストに `1` と `2` を追加する関数 `fill` を定義し、それを `main` で使ってください。先に関数を定義し、そのあとに `main` を定義してください。プログラムは次のように出力しなければなりません：

```
[0]
[1, 2, 3]
```

# --answers--

```dart
void fill(List<int>? target) {
```

```dart
  target?..add(1)..add(2);
```

```dart
}
```

```dart
void main() {
```

```dart
  List<int>? numbers;
```

```dart
  print([0, ...?numbers]);
```

```dart
  numbers = [];
```

```dart
  fill(numbers);
```

```dart
  print([...?numbers, 3]);
```

```dart
}
```

# --solutions--

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  print([0, ...?numbers]);
  numbers = [];
  fill(numbers);
  print([...?numbers, 3]);
}
```

```dart
void fill(List<int>? target) {
  target?..add(1)..add(2);
}

void main() {
  List<int>? numbers;
  numbers = [];
  print([0, ...?numbers]);
  fill(numbers);
  print([...?numbers, 3]);
}
```

# --output--

[0]
[1, 2, 3]
