---
language: dart
exerciseType: 1
---

# --description--

コンパイラには判断できなくても、ある地点でnull許容の値が `null` でないと**あなた**にはわかっていることがあります。**null断定**演算子 `!` は、値が存在すると約束することで `String?` を `String` に変えます：

```dart
String? text = 'Dart';
String sure = text!;
print(sure.length); // 4
```

注意してください。`!` はチェックをコンパイル時から実行時へ移します。値が**実際に** `null` だった場合、プログラムはエラーを投げて停止します：

```dart
String? text;
print(text!.length); // Null check operator used on a null value
```

`!` は控えめに、そこに `null` が来ること自体がバグである場合にだけ使ってください。

# --instructions--

null許容の `String` 引数 `text` を受け取り、それを大文字にして末尾に `!` を付けて返す関数 `shout` を書いてください。`!` 演算子を使い、`text` が `null` のときは関数がエラーを投げるようにしてください。

例：
```dart
print(shout('hello')); // HELLO!
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`shout('hello')` は `'HELLO!'` になるはずです

```dart
  test('test1', () {
    expect(shout('hello'), 'HELLO!', reason: '--err-t1--');
  });
```

`shout('Dart')` は `'DART!'` になるはずです

```dart
  test('test2', () {
    expect(shout('Dart'), 'DART!', reason: '--err-t2--');
  });
```

`shout(null)` はエラーを投げるはずです

```dart
  test('test3', () {
    expect(() => shout(null), throwsA(anything), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String shout(String? text) {
  return '${text!.toUpperCase()}!';
}
```
