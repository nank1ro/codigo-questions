---
language: dart
exerciseType: 1
---

# --description--

多くのライブラリ関数は、何かが**できなかった**ことを知らせるために `null` を使います。文字列を数値に変換する処理はその典型です。`int.parse` は文字列が数値でないとき `FormatException` を投げますが、`int.tryParse` は代わりに `null` を返し、どうするかをあなたに委ねます：

```dart
print(int.tryParse('42'));  // 42
print(int.tryParse('4x2')); // null
print(int.tryParse(''));    // null
```

`int.tryParse` の戻り値の型は `int?` なので、学んできたことがすべて当てはまります。既定値には `??`、連鎖には `?.`、プロモーションには `if` チェックです。`double.tryParse` も同じように動きます。

# --instructions--

`String` 引数 `text` を受け取り、そこに含まれる整数を返し、テキストが有効な整数でないときは `0` を返す関数 `parseOrZero` を書いてください。`int.tryParse` を使ってください。

例：
```dart
print(parseOrZero('42'));  // 42
print(parseOrZero('abc')); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parseOrZero('42')` は `42` になるはずです

```dart
  test('test1', () {
    expect(parseOrZero('42'), 42, reason: '--err-t1--');
  });
```

`parseOrZero('-5')` は `-5` になるはずです

```dart
  test('test2', () {
    expect(parseOrZero('-5'), -5, reason: '--err-t2--');
  });
```

`parseOrZero('abc')` と `parseOrZero('')` は `0` になるはずです

```dart
  test('test3', () {
    expect(parseOrZero('abc'), 0, reason: '--err-t3--');
    expect(parseOrZero(''), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int parseOrZero(String text) {
  return int.tryParse(text) ?? 0;
}
```
