---
language: dart
exerciseType: 1
---

# --description--

null許容の値を `if` でチェックするのは `!` より安全で、Dartはそれに報いてくれます。`if (x != null)` のようなチェックのあと、コンパイラはそのブロック内で `x` が `null` になりえないと知っているので、そこでは `x` を非null許容として扱います。これを**型プロモーション**と呼びます：

```dart
int twice(int? n) {
  if (n != null) {
    return n * 2; // here n is an int, no ! needed
  }
  return 0;
}
```

早期リターンのあとでもプロモーションは働きます：

```dart
int twice(int? n) {
  if (n == null) return 0;
  return n * 2; // n is an int from here on
}
```

プロモーションが適用されるのは**ローカル変数と引数**です。これらの値は、チェックと使用の間に知らないうちに変わることがありません。

# --instructions--

null許容の `int` 引数 `n` を受け取り、整数除算 `~/` で `n` を `2` で割った結果を返し、`n` が `null` のときは `0` を返す関数 `half` を書いてください。`!` ではなく `if` によるnullチェックを使ってください。

例：
```dart
print(half(10));   // 5
print(half(null)); // 0
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`half(10)` は `5` になるはずです

```dart
  test('test1', () {
    expect(half(10), 5, reason: '--err-t1--');
  });
```

`half(7)` は `3` になるはずです

```dart
  test('test2', () {
    expect(half(7), 3, reason: '--err-t2--');
  });
```

`half(null)` は `0` になるはずです

```dart
  test('test3', () {
    expect(half(null), 0, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int half(int? n) {
  if (n != null) {
    return n ~/ 2;
  }
  return 0;
}
```
