---
language: dart
exerciseType: 2
---

# --description--

null許容性はコレクションの**要素**にも当てはまります。`List<int>` は決して `null` を含みませんが、`List<int?>` は含むことができます：

```dart
List<int?> scores = [7, null, 9];
```

`List<int>?` との違いに注意してください。こちらはリスト自体が存在しないことがあり、存在する場合には実在する数値だけを持ちます。

`null` の要素を取り除くには `nonNulls` を使います。これは存在する値だけを含み、`?` の付かない型を持つ `Iterable` を返します：

```dart
var present = scores.nonNulls.toList(); // List<int>
print(present); // [7, 9]
```

`whereType<int>()` も同じことをし、リストに複数の型が混ざっている場合にも使えます。

# --instructions--

コードを完成させて、null許容の `int` 値のリストを宣言し、`nonNulls` でnullでない値だけを残してください。プログラムは `[7, 9]`、続いて `2` を出力しなければなりません。

# --seed--

```dart
void main() {
  List<[/]> scores = [7, null, 9, null];
  var present = scores.[/].toList();
  print(present);
  print(present.length);
}
```

# --answers--

- int?
- nonNulls
- int
- notNull
- nonNull
- compact

# --solutions--

```dart
void main() {
  List<int?> scores = [7, null, 9, null];
  var present = scores.nonNulls.toList();
  print(present);
  print(present.length);
}
```

# --output--

[7, 9]
2
