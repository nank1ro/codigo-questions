---
language: dart
exerciseType: 1
---

# --description--

enumの値は**マップのキー**として非常に優れています。一意であり、比較が容易で、コンパイラが実在する値しか使われないことを保証してくれます。enumをキーの型としてマップを宣言し、`[]`で値を検索します:

```dart
enum Direction { north, south, east, west }

Map<Direction, String> arrows = {
  Direction.north: '^',
  Direction.south: 'v',
  Direction.east: '>',
  Direction.west: '<',
};

print(arrows[Direction.east]); // >
```

通常のマップと同様に、キーが存在しない場合の検索結果は`null`になるため、`??`でフォールバックを用意してください。

# --instructions--

`enum Size { small, medium, large }`が与えられたとき、`small`が`2`、`medium`が`3`、`large`が`4`のコストを持つ、`prices`という名前のトップレベルの`Map<Size, int>`を宣言してください。次に、`order`内のすべてのサイズのコストの合計を返す関数`totalPrice(List<Size> order)`を作成してください。

# --seed--

```dart
enum Size { small, medium, large }
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`prices[Size.medium]`は`3`でなければなりません

```dart
  test('test1', () {
    expect(prices[Size.medium], 3, reason: '--err-t1--');
  });
```

`totalPrice([Size.small, Size.large])`は`6`でなければなりません

```dart
  test('test2', () {
    expect(totalPrice([Size.small, Size.large]), 6, reason: '--err-t2--');
  });
```

`totalPrice([Size.medium, Size.medium, Size.small])`は`8`でなければなりません

```dart
  test('test3', () {
    expect(totalPrice([Size.medium, Size.medium, Size.small]), 8, reason: '--err-t3--');
  });
```

`totalPrice([])`は`0`でなければなりません

```dart
  test('test4', () {
    expect(totalPrice([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Size { small, medium, large }

Map<Size, int> prices = {
  Size.small: 2,
  Size.medium: 3,
  Size.large: 4,
};

int totalPrice(List<Size> order) {
  int total = 0;
  for (var size in order) {
    total += prices[size] ?? 0;
  }
  return total;
}
```
