---
language: dart
exerciseType: 4
---

# --description--

`values`をループするのは、enumのすべての値を処理する一般的な方法です。ループの中では現在の値は他のオブジェクトと同じように扱えるので、その`index`と`name`を読み取り、そのまま文字列補間で使えます:

```dart
enum Direction { north, south, east, west }

for (var direction in Direction.values) {
  print('${direction.index}: ${direction.name}');
}
```

# --instructions--

アイテムを並べ替えてenum `Level`を宣言し、各値についてそのインデックスと名前を出力してください。enumは`main`より前に宣言してください。プログラムは次を出力する必要があります:

```
0: low
1: medium
2: high
```

# --answers--

```dart
enum Level { low, medium, high }
```

```dart
void main() {
```

```dart
  for (var level in Level.values) {
```

```dart
    print('${level.index}: ${level.name}');
```

```dart
  }
}
```

# --solutions--

```dart
enum Level { low, medium, high }
void main() {
  for (var level in Level.values) {
    print('${level.index}: ${level.name}');
  }
}
```

# --output--

0: low
1: medium
2: high
