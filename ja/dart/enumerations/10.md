---
language: dart
exerciseType: 1
---

# --description--

Dart 2.17以降、enumはクラスと同じように**フィールド**と**コンストラクタ**を宣言できます。これは*拡張enum*と呼ばれます。各値はコンストラクタに独自の引数を渡します:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);
}

print(Planet.mars.moons); // 2
```

3つのルールに注目してください: 値のリストは**セミコロン**`;`で終わること、フィールドは`final`でなければならないこと、コンストラクタは`const`でなければならないことです。

# --instructions--

`final int`型のフィールド`cents`と、値`penny(1)`、`nickel(5)`、`dime(10)`、`quarter(25)`をこの順序で持つenum `Coin`を宣言してください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Coin.penny.cents`は`1`、`Coin.nickel.cents`は`5`でなければなりません

```dart
  test('test1', () {
    expect(Coin.penny.cents, 1, reason: '--err-t1--');
    expect(Coin.nickel.cents, 5, reason: '--err-t2--');
  });
```

`Coin.dime.cents`は`10`でなければなりません

```dart
  test('test2', () {
    expect(Coin.dime.cents, 10, reason: '--err-t3--');
  });
```

`Coin.quarter.cents`は`25`でなければなりません

```dart
  test('test3', () {
    expect(Coin.quarter.cents, 25, reason: '--err-t4--');
  });
```

`Coin.values`は4つの値を持ち、`penny`が最初でなければなりません

```dart
  test('test4', () {
    expect(Coin.values.length, 4, reason: '--err-t5--');
    expect(Coin.values[0], Coin.penny, reason: '--err-t6--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Coin {
  penny(1),
  nickel(5),
  dime(10),
  quarter(25);

  final int cents;

  const Coin(this.cents);
}
```
