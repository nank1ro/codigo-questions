---
language: dart
exerciseType: 1
---

# --description--

enumは`implements`キーワードを使って**インターフェースを実装**できます。enumはインターフェースが宣言するすべてのメンバーを提供することを約束し、その値はそのインターフェース型が期待される場所ならどこでも使用できます:

```dart
abstract class Describable {
  String describe();
}

enum Animal implements Describable {
  dog,
  cat;

  @override
  String describe() => 'I am a $name';
}

Describable pet = Animal.cat;
print(pet.describe()); // I am a cat
```

インターフェースで宣言されたゲッターは、ゲッターまたは同名の`final`フィールドのどちらでも実装できます。

# --instructions--

`Priced`を実装するenum `Ticket`を宣言してください。`final int`型のフィールド`price`と、値`child(5)`、`adult(12)`、`senior(8)`をこの順序で持たせてください。次に、すべてのアイテムの価格の合計を返す関数`total(List<Priced> items)`を作成してください。

# --seed--

```dart
abstract class Priced {
  int get price;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Ticket.adult.price`は`12`でなければなりません

```dart
  test('test1', () {
    expect(Ticket.adult.price, 12, reason: '--err-t1--');
  });
```

`Ticket`は`Priced`として使用できなければなりません

```dart
  test('test2', () {
    Priced item = Ticket.senior;
    expect(item.price, 8, reason: '--err-t2--');
  });
```

`total([Ticket.child, Ticket.adult, Ticket.senior])`は`25`でなければなりません

```dart
  test('test3', () {
    expect(total([Ticket.child, Ticket.adult, Ticket.senior]), 25, reason: '--err-t3--');
  });
```

`total([])`は`0`でなければなりません

```dart
  test('test4', () {
    expect(total([]), 0, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
abstract class Priced {
  int get price;
}

enum Ticket implements Priced {
  child(5),
  adult(12),
  senior(8);

  @override
  final int price;

  const Ticket(this.price);
}

int total(List<Priced> items) {
  int sum = 0;
  for (var item in items) {
    sum += item.price;
  }
  return sum;
}
```
