---
language: dart
exerciseType: 2
---

# --description--

Dart 3以降、`switch`は値を生成する**式**としても使えます。各ケースは`pattern => value`の形で書き、`case`キーワードや`break`を使わずにカンマで区切ります:

```dart
enum Direction { north, south, east, west }

String arrow(Direction direction) => switch (direction) {
      Direction.north => '^',
      Direction.south => 'v',
      Direction.east => '>',
      Direction.west => '<',
    };
```

文形式と同様に、enumに対するswitch式も網羅的でなければなりません。

# --instructions--

switch式を完成させて、`label`が各サイズの頭文字を返すようにし、`Size.large`のラベルを出力してください。

# --seed--

```dart
enum Size { small, medium, large }

String label(Size size) => [/] (size) {
      Size.small [/] 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --answers--

- switch
- =>
- case
- when
- ->
- :

# --solutions--

```dart
enum Size { small, medium, large }

String label(Size size) => switch (size) {
      Size.small => 'S',
      Size.medium => 'M',
      Size.large => 'L',
    };

void main() {
  print(label(Size.large));
}
```

# --output--

L
