---
language: dart
exerciseType: 2
---

# --description--

すべてのenumには、宣言順にすべての値を保持する`values`という定数のリストもあります。通常のリストと同様にインデックスでアクセスしたり、`length`を読み取ったり、`for-in`でループしたりできます:

```dart
enum Direction { north, south, east, west }

print(Direction.values.length); // 4
print(Direction.values[1]);     // Direction.south

for (var direction in Direction.values) {
  print(direction.name);
}
```

# --instructions--

コードを完成させて、`Planet.earth`の名前、そのインデックス、惑星の総数を出力してください。

# --seed--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.[/]);
  print(Planet.earth.[/]);
  print(Planet.[/].length);
}
```

# --answers--

- name
- index
- values
- ordinal
- position
- count

# --solutions--

```dart
enum Planet { mercury, venus, earth }

void main() {
  print(Planet.earth.name);
  print(Planet.earth.index);
  print(Planet.values.length);
}
```

# --output--

earth
2
3
