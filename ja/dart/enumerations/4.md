---
language: dart
exerciseType: 1
---

# --description--

`values`はリストなので、すでに知っているリストのメソッドと組み合わせて使えます。例えば`.map()`と`.name`を使うと、値を文字列のリストに変換できます:

```dart
enum Direction { north, south, east, west }

List<String> names = Direction.values.map((d) => d.name).toList();
print(names); // [north, south, east, west]
```

# --instructions--

値`monday`、`tuesday`、`wednesday`、`thursday`、`friday`、`saturday`、`sunday`をこの順序で持つenum `Weekday`を宣言してください。次に、宣言順にすべての曜日の名前を持つ`List<String>`を返す関数`dayNames()`を作成してください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Weekday.values.length`は`7`でなければなりません

```dart
  test('test1', () {
    expect(Weekday.values.length, 7, reason: '--err-t1--');
  });
```

`Weekday.values[2]`は`Weekday.wednesday`でなければなりません

```dart
  test('test2', () {
    expect(Weekday.values[2], Weekday.wednesday, reason: '--err-t2--');
  });
```

`dayNames()`は7つの名前を順序通りに返さなければなりません

```dart
  test('test3', () {
    expect(dayNames(), ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday'], reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Weekday { monday, tuesday, wednesday, thursday, friday, saturday, sunday }

List<String> dayNames() {
  return Weekday.values.map((day) => day.name).toList();
}
```
