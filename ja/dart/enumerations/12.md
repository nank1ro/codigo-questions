---
language: dart
exerciseType: 1
---

# --description--

拡張enumは**メソッド**や**ゲッター**も宣言できます。それらの中では`this`は現在の値を指すため、その`name`、`index`、フィールドを直接使うことができます:

```dart
enum Planet {
  mercury(0),
  earth(1),
  mars(2);

  final int moons;

  const Planet(this.moons);

  bool get hasMoons => moons > 0;

  String describe() => '$name has $moons moon(s)';
}

print(Planet.earth.hasMoons);   // true
print(Planet.mars.describe()); // mars has 2 moon(s)
```

フィールドを持たないenumでも、メソッドを宣言できます。その場合、値のリストは`;`で終わり、その後にメンバーが続きます。

# --instructions--

値`north`、`east`、`south`、`west`をこの順序で持つenum `Direction`を宣言してください。反対側の値を返すゲッター`opposite`（`north`<->`south`、`east`<->`west`）と、次の値を返すメソッド`turnRight()`（`west`から`north`へ折り返す）を追加してください。

ヒント: `index`、`values`、`%`演算子を組み合わせてください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`Direction.north.opposite`は`Direction.south`でなければなりません

```dart
  test('test1', () {
    expect(Direction.north.opposite, Direction.south, reason: '--err-t1--');
  });
```

`Direction.west.opposite`は`Direction.east`でなければなりません

```dart
  test('test2', () {
    expect(Direction.west.opposite, Direction.east, reason: '--err-t2--');
  });
```

`Direction.north.turnRight()`は`Direction.east`でなければなりません

```dart
  test('test3', () {
    expect(Direction.north.turnRight(), Direction.east, reason: '--err-t3--');
  });
```

`Direction.west.turnRight()`は`Direction.north`でなければなりません

```dart
  test('test4', () {
    expect(Direction.west.turnRight(), Direction.north, reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
enum Direction {
  north,
  east,
  south,
  west;

  Direction get opposite => Direction.values[(index + 2) % 4];

  Direction turnRight() => Direction.values[(index + 1) % 4];
}
```
