---
language: dart
exerciseType: 1
---

# --instructions--

`while` ループを使って、`30` より大きく `3` と `5` の両方で割り切れる最初の数値を見つけてください。その数値を返してください。

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return result;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`result` は `45` と等しくなければなりません

```dart
  test('test1', () {
    expect(computeValue(), 45, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int result = 31;
while (result % 3 != 0 || result % 5 != 0) {
  result++;
}
```
