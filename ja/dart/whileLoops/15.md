---
language: dart
exerciseType: 1
---

# --instructions--

`while` ループを使って `2` から `20`（含む）までの**偶数**の合計を計算してください。合計を返してください。

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return sum;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`sum` は `110` と等しくなければなりません

```dart
  test('test1', () {
    expect(computeValue(), 110, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int sum = 0;
int i = 2;
while (i <= 20) {
  sum += i;
  i += 2;
}
```
