---
language: dart
exerciseType: 1
---

# --instructions--

`while` ループを使って `6` の階乗（すなわち `6!`）を計算してください。結果を返してください。

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return factorial;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`factorial` は `720` と等しくなければなりません

```dart
  test('test1', () {
    expect(computeValue(), 720, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int factorial = 1;
int i = 1;
while (i <= 6) {
  factorial *= i;
  i++;
}
```
