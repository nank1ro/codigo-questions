---
language: dart
exerciseType: 1
---

# --instructions--

`1` から `5` までカウントし、最終的なカウンターの値を返す `while` ループを書いてください。

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return counter;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`counter` は `5` と等しくなければなりません

```dart
  test('test1', () {
    expect(computeValue(), 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int counter = 1;
while (counter < 5) {
  counter++;
}
```
