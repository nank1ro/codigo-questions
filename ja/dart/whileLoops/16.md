---
language: dart
exerciseType: 1
---

# --instructions--

`product = 1` から始め、`while` ループを使って `product` が `100` 以上になるまで `2` をかけ続けてください。`product` を返してください。

# --before-seed--

```dart
dynamic computeValue() {
```

# --after-seed--

```dart
  return product;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`product` は `128` と等しくなければなりません

```dart
  test('test1', () {
    expect(computeValue(), 128, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int product = 1;
while (product < 100) {
  product *= 2;
}
```
