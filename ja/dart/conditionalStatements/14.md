---
language: dart
exerciseType: 1
---

# --instructions--

ネストされた `if` 文を使って、`age >= 18` **かつ** `isCitizen` が `true` のときのみ `true` を返す関数 `canVote(int age, bool isCitizen)` を書いてください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`canVote(20, true)` は `true` を返さなければならない

```dart
  test('test1', () {
    expect(canVote(20, true), true, reason: '--err-t1--');
  });
```

`canVote(16, true)` は `false` を返さなければならない

```dart
  test('test2', () {
    expect(canVote(16, true), false, reason: '--err-t2--');
  });
```

`canVote(25, false)` は `false` を返さなければならない

```dart
  test('test3', () {
    expect(canVote(25, false), false, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
bool canVote(int age, bool isCitizen) {
  if (age >= 18) {
    if (isCitizen) {
      return true;
    }
  }
  return false;
}

void main() {
  print(canVote(20, true));
}
```
