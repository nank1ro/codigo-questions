---
language: dart
exerciseType: 3
---

# --instructions--

Dart の `if` 文は何をしますか？

# --answers--

- ブロック内のコードを常に実行する
- 条件が `true` のときだけブロックを実行する
- 条件が `true` の間ブロックを繰り返す
- 新しい関数を定義する

# --solutions--

- 条件が `true` のときだけブロックを実行する
