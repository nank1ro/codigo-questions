---
language: dart
exerciseType: 1
---

# --instructions--

`else if` チェーンを使って文字のグレードを返す関数 `letterGrade(int score)` を書いてください：`score >= 90` のとき `'A'`、`score >= 80` のとき `'B'`、`score >= 70` のとき `'C'`、`score >= 60` のとき `'D'`、`60` 未満のとき `'F'`。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`letterGrade(92)` は `'A'` を返さなければならない

```dart
  test('test1', () {
    expect(letterGrade(92), 'A', reason: '--err-t1--');
  });
```

`letterGrade(85)` は `'B'` を返さなければならない

```dart
  test('test2', () {
    expect(letterGrade(85), 'B', reason: '--err-t2--');
  });
```

`letterGrade(73)` は `'C'` を返さなければならない

```dart
  test('test3', () {
    expect(letterGrade(73), 'C', reason: '--err-t3--');
  });
```

`letterGrade(61)` は `'D'` を返さなければならない

```dart
  test('test4', () {
    expect(letterGrade(61), 'D', reason: '--err-t4--');
  });
```

`letterGrade(45)` は `'F'` を返さなければならない

```dart
  test('test5', () {
    expect(letterGrade(45), 'F', reason: '--err-t5--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String letterGrade(int score) {
  if (score >= 90) {
    return 'A';
  } else if (score >= 80) {
    return 'B';
  } else if (score >= 70) {
    return 'C';
  } else if (score >= 60) {
    return 'D';
  } else {
    return 'F';
  }
}

void main() {
  print(letterGrade(85));
}
```
