---
language: dart
exerciseType: 1
---

# --instructions--

`isHot` という名前のトップレベルの `bool` 変数を宣言してください。トップレベルの `int` 変数 `temp`（`35` に設定）が `30` より大きいとき、`if` 文を使って `isHot` に `true` を代入してください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`temp` は `35` でなければならない

```dart
  test('test1', () {
    expect(temp, 35, reason: '--err-t1--');
  });
```

`isHot` は `true` でなければならない

```dart
  test('test2', () {
    expect(isHot, true, reason: '--err-t2--');
    expect(isHot, TypeMatcher<bool>(), reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int temp = 35;
bool isHot = false;

void main() {
  if (temp > 30) {
    isHot = true;
  }
  print(isHot);
}
```
