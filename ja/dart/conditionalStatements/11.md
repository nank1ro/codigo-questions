---
language: dart
exerciseType: 1
---

# --instructions--

`isHot` という名前の`bool` 変数を宣言してください。`int` 変数 `temp`（`35` に設定）が `30` より大きいとき、`if` 文を使って `isHot` に `true` を代入してください。

# --before-seed--

```dart
dynamic result() {
```

# --seed--

```dart
int temp = 35;
bool isHot = false;
```

# --after-seed--

```dart
  return isHot;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`isHot` は `true` でなければならない

```dart
  test('test1', () {
    expect(result(), true, reason: '--err-t1--');
    expect(result(), TypeMatcher<bool>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int temp = 35;
bool isHot = false;
if (temp > 30) {
  isHot = true;
}
```
