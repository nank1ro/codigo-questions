---
language: dart
exerciseType: 2
---

# --description--

**三項演算子** `condition ? expr1 : expr2` は、シンプルな `if-else` 式をコンパクトに書く方法です。

```dart
int x = 10;
String label = x > 0 ? 'positive' : 'non-positive';
print(label); // positive
```

条件が `true` なら `expr1` が使われ、そうでなければ `expr2` が使われます。

# --instructions--

`n` が `2` で割り切れるとき `parity` に `'Even'` を、そうでなければ `'Odd'` を代入するように三項式を完成させてください。

# --seed--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 [/] 'Even' [/] 'Odd';
  print(parity);
}
```

# --answers--

- ?
- :
- ==
- ;

# --solutions--

```dart
void main() {
  int n = 4;
  String parity = n % 2 == 0 ? 'Even' : 'Odd';
  print(parity);
}
```

# --output--

Even
