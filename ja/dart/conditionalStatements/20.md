---
language: dart
exerciseType: 1
---

# --instructions--

`if-else` チェーンを使って、`age < 13` のとき `'child'`、`age < 18` のとき `'teenager'`、`age < 65` のとき `'adult'`、`age >= 65` のとき `'senior'` を返す関数 `ageGroup(int age)` を書いてください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`ageGroup(8)` は `'child'` を返さなければならない

```dart
  test('test1', () {
    expect(ageGroup(8), 'child', reason: '--err-t1--');
  });
```

`ageGroup(15)` は `'teenager'` を返さなければならない

```dart
  test('test2', () {
    expect(ageGroup(15), 'teenager', reason: '--err-t2--');
  });
```

`ageGroup(30)` は `'adult'` を返さなければならない

```dart
  test('test3', () {
    expect(ageGroup(30), 'adult', reason: '--err-t3--');
  });
```

`ageGroup(70)` は `'senior'` を返さなければならない

```dart
  test('test4', () {
    expect(ageGroup(70), 'senior', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String ageGroup(int age) {
  if (age < 13) {
    return 'child';
  } else if (age < 18) {
    return 'teenager';
  } else if (age < 65) {
    return 'adult';
  } else {
    return 'senior';
  }
}

void main() {
  print(ageGroup(30));
}
```
