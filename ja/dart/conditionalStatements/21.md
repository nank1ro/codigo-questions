---
language: dart
exerciseType: 1
---

# --instructions--

三項演算子を使って二つの数のうち小さい方を返す関数 `minOf(int a, int b)` を書いてください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`minOf(3, 7)` は `3` を返さなければならない

```dart
  test('test1', () {
    expect(minOf(3, 7), 3, reason: '--err-t1--');
  });
```

`minOf(10, 2)` は `2` を返さなければならない

```dart
  test('test2', () {
    expect(minOf(10, 2), 2, reason: '--err-t2--');
  });
```

`minOf(5, 5)` は `5` を返さなければならない

```dart
  test('test3', () {
    expect(minOf(5, 5), 5, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int minOf(int a, int b) {
  return a < b ? a : b;
}

void main() {
  print(minOf(3, 7));
}
```
