---
language: dart
exerciseType: 1
---

# --instructions--

`default` 節を持つ `switch` 文を使って、`'green'` のとき `'Go'`、`'yellow'` のとき `'Slow down'`、`'red'` のとき `'Stop'`、それ以外の値のとき `'Invalid'` を返す関数 `trafficLight(String color)` を書いてください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`trafficLight('green')` は `'Go'` を返さなければならない

```dart
  test('test1', () {
    expect(trafficLight('green'), 'Go', reason: '--err-t1--');
  });
```

`trafficLight('yellow')` は `'Slow down'` を返さなければならない

```dart
  test('test2', () {
    expect(trafficLight('yellow'), 'Slow down', reason: '--err-t2--');
  });
```

`trafficLight('red')` は `'Stop'` を返さなければならない

```dart
  test('test3', () {
    expect(trafficLight('red'), 'Stop', reason: '--err-t3--');
  });
```

`trafficLight('blue')` は `'Invalid'` を返さなければならない

```dart
  test('test4', () {
    expect(trafficLight('blue'), 'Invalid', reason: '--err-t4--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String trafficLight(String color) {
  switch (color) {
    case 'green':
      return 'Go';
    case 'yellow':
      return 'Slow down';
    case 'red':
      return 'Stop';
    default:
      return 'Invalid';
  }
}

void main() {
  print(trafficLight('green'));
}
```
