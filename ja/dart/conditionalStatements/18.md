---
language: dart
exerciseType: 1
---

# --instructions--

`if-else` 文を使って、`n` が `2` で割り切れるとき `'even'` を、そうでなければ `'odd'` を返す関数 `parity(int n)` を書いてください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`parity(4)` は `'even'` を返さなければならない

```dart
  test('test1', () {
    expect(parity(4), 'even', reason: '--err-t1--');
  });
```

`parity(7)` は `'odd'` を返さなければならない

```dart
  test('test2', () {
    expect(parity(7), 'odd', reason: '--err-t2--');
  });
```

`parity(0)` は `'even'` を返さなければならない

```dart
  test('test3', () {
    expect(parity(0), 'even', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String parity(int n) {
  if (n % 2 == 0) {
    return 'even';
  } else {
    return 'odd';
  }
}

void main() {
  print(parity(4));
}
```
