---
language: dart
exerciseType: 3
---

# --instructions--

`switch` 文の `default` 節は何をしますか？

# --answers--

- どの `case` の値にも一致しないときに実行される
- 値に関係なく常に実行される
- プログラムを停止する
- 最初にチェックするケースを定義する

# --solutions--

- どの `case` の値にも一致しないときに実行される
