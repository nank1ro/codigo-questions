---
language: dart
exerciseType: 1
---

# --instructions--

`if-else` を使って、`n` が `0` より大きいとき `'positive'` を、そうでなければ `'non-positive'` を返す関数 `describe(int n)` を書いてください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`describe(10)` は `'positive'` を返さなければならない

```dart
  test('test1', () {
    expect(describe(10), 'positive', reason: '--err-t1--');
  });
```

`describe(0)` は `'non-positive'` を返さなければならない

```dart
  test('test2', () {
    expect(describe(0), 'non-positive', reason: '--err-t2--');
  });
```

`describe(-5)` は `'non-positive'` を返さなければならない

```dart
  test('test3', () {
    expect(describe(-5), 'non-positive', reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
String describe(int n) {
  if (n > 0) {
    return 'positive';
  } else {
    return 'non-positive';
  }
}

void main() {
  print(describe(10));
}
```
