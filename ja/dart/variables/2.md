---
language: dart
exerciseType: 1
---

# --description--

前の例では、変数の作成について学びました:

```dart
int number = 1;
```

`=`記号に惑わされないでください。
これは数学のような等号ではなく、変数に値を割り当てるための__代入演算子__として知られています。
一方、等号は`==`です。

# --instructions--

変数`x`に値`5`を代入してください。

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x`は`5`と等しくなければなりません。

```dart
  test('test1', () {
    expect(x, 5, reason: '--err-t1--');
  });
```

`x`は`int`型でなければなりません

```dart
  test('test2', () {
    expect(x, TypeMatcher<int>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int x = 5;
```
