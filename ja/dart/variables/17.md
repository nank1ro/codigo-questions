---
language: dart
exerciseType: 1
---

# --instructions--

変数`x`を数値`1`で初期化し、エラーを発生させずに値`"hello"`で更新してください。

# --before-seed--

```dart
dynamic value() {
```

# --after-seed--

```dart
  return x;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`x`は`"hello"`と等しくなければなりません。

```dart
  test('test1', () {
    expect(value(), "hello", reason: '--err-t1--');
  });
```

`x`は`dynamic`型でなければなりません

```dart
  test('test2', () {
    expect(value(), TypeMatcher<dynamic>(), reason: '--err-t2--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
dynamic x = 1;
x = "hello";
```
