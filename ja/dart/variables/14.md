---
language: dart
exerciseType: 1
---

# --instructions--

変数`number`の値を`25`に更新してください

# --before-seed--

```dart
dynamic number() {
```

# --seed--

```dart
int number = 1;
```

# --after-seed--

```dart
  return number;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`number`は`25`と等しくなければなりません。

```dart
    test('test1', () {
      expect(number(), 25, reason: '--err-t1--');
    });
```

`number`は`int`型でなければなりません

```dart
    test('test2', () {
      expect(number(), TypeMatcher<int>(), reason: '--err-t2--');
    });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int number = 1;
number = 25;
```
