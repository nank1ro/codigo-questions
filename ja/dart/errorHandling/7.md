---
language: dart
exerciseType: 1
---

# --description--

あなた自身のコードも、ライブラリと同じやり方で投げます。`Exception('message')` は短い説明を持つ素朴な例外を作り、`throw` がそれを送り出します：

```dart
if (amount > balance) {
  throw Exception('insufficient funds');
}
```

メッセージは失われません：`toString()` は `Exception` という語とコロンとメッセージをつなげます。これはまさに未処理の例外のレポートが出力する内容です。

```dart
print(Exception('insufficient funds')); // Exception: insufficient funds
```

投げることは `-1` のようなでっち上げの値を返すことに勝ります：呼び出し元はそれを見ることを忘れられませんし、理由も一緒に運ばれます。

# --instructions--

2つの `int` 値 `balance` と `amount` を受け取り、残りの残高を返す関数 `withdraw` を書いてください。

`amount` が `balance` より大きいときは、代わりに `Exception('insufficient funds')` を投げてください。

例：
```dart
print(withdraw(100, 30)); // 70
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`withdraw(100, 30)` は `70` を返すべきです

```dart
  test('test1', () {
    expect(withdraw(100, 30), 70, reason: '--err-t1--');
    expect(withdraw(50, 50), 0, reason: '--err-t1--');
  });
```

`withdraw(100, 300)` は `Exception` を投げるべきです

```dart
  test('test2', () {
    expect(() => withdraw(100, 300), throwsA(isA<Exception>()), reason: '--err-t2--');
  });
```

投げられた例外は `Exception: insufficient funds` と出力されるべきです

```dart
  test('test3', () {
    try {
      withdraw(0, 1);
      fail('--err-t3--');
    } catch (e) {
      expect(e.toString(), 'Exception: insufficient funds', reason: '--err-t3--');
    }
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
int withdraw(int balance, int amount) {
  if (amount > balance) {
    throw Exception('insufficient funds');
  }
  return balance - amount;
}
```
