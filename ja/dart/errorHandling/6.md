---
language: dart
exerciseType: 4
---

# --description--

ハンドラーの後ろには3つ目のブロックを続けられます。`finally` は**どんな場合でも**実行されます：`try` ブロックが普通に終わった後、ハンドラーが回復した後、そして何も一致せず失敗がまだ外へ伝播しているときにも。

```dart
try {
  return 'parsed ${int.parse(text)}';
} on FormatException {
  return 'failed';
} finally {
  print('done');
}
```

`return` が値を渡し戻す前でさえ実行されるので、上のメッセージは呼び出し元が結果を見る前に出力されます。そのため `finally` は、開いたものを閉じるような、どのみち実行しなければならない仕事の場所になります。

# --instructions--

項目を並べ替えて、`'parsed '` に数を続けて返し、テキストが数値でないときは `'failed'` を返し、どのみち `finally` ブロックで `done` を出力する関数 `attempt` を定義してください。関数は `main` の前に定義します。プログラムは次のように出力しなければなりません：

```
done
parsed 7
done
failed
```

# --answers--

```dart
String attempt(String text) {
```

```dart
  try {
    return 'parsed ${int.parse(text)}';
```

```dart
  } on FormatException {
    return 'failed';
```

```dart
  } finally {
    print('done');
  }
}
```

```dart
void main() {
```

```dart
  print(attempt('7'));
```

```dart
  print(attempt('x'));
```

```dart
}
```

# --solutions--

```dart
String attempt(String text) {
  try {
    return 'parsed ${int.parse(text)}';
  } on FormatException {
    return 'failed';
  } finally {
    print('done');
  }
}

void main() {
  print(attempt('7'));
  print(attempt('x'));
}
```

# --output--

done
parsed 7
done
failed
