---
language: dart
exerciseType: 1
---

# --description--

future は**エラー**で完了することもあります。`async` 関数が例外を投げても、それはすぐには外に出ません。返された future のエラーになります。その future を `await` した側は `await` の位置で例外が投げられたように見えるので、普通の `try`/`catch` で処理できます：

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s); // throws FormatException for 'abc'
}

Future<int> orZero(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return 0;
  }
}
```

`try` の中の `await` が肝心です。`return parseLater(s);` は future を**待たずに**呼び出し側へ渡してしまうので、エラーは `try` ブロックがすでに終わったあとに届き、`catch` は決して実行されません。

# --instructions--

関数 `parseLater(s)` は `s` に書かれた数値で完了する `Future<int>` を返し、`s` が数値でなければエラーで完了します。`String` の `s` を受け取り、解析した数値で完了する、または `parseLater` が失敗したら `-1` で完了する `Future<int>` を返す `async` 関数 `safeParse` を書いてください。

例：
```dart
print(await safeParse('12'));  // 12
print(await safeParse('abc')); // -1
```

# --before-seed--

```dart
Future<int> parseLater(String s) async {
  await Future.delayed(const Duration(milliseconds: 5));
  return int.parse(s);
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`safeParse('12')` は `12` で完了するべきです

```dart
  test('test1', () async {
    expect(await safeParse('12'), 12, reason: '--err-t1--');
  });
```

`safeParse('abc')` は例外を投げるのではなく `-1` で完了するべきです

```dart
  test('test2', () async {
    expect(await safeParse('abc'), -1, reason: '--err-t2--');
  });
```

`safeParse('-7')` は `-7` で、`safeParse('')` は `-1` で完了するべきです

```dart
  test('test3', () async {
    expect(await safeParse('-7'), -7, reason: '--err-t3--');
    expect(await safeParse(''), -1, reason: '--err-t3--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
Future<int> safeParse(String s) async {
  try {
    return await parseLater(s);
  } catch (e) {
    return -1;
  }
}
```
