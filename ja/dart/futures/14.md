---
language: dart
exerciseType: 2
---

# --description--

エラーは、それを捕まえないすべての `await` を通って**伝播**します。`load()` が失敗すると、`loadTwice()` の中の `await load()` が例外を投げます。`loadTwice` には `try`/`catch` がないので、その future も同じエラーで失敗します。こうして、どこかの `await` が `try`/`catch` で囲まれるまで、連鎖をさかのぼっていきます：

```dart
Future<int> load() async {
  throw StateError('offline');
}

Future<int> loadTwice() async {
  final n = await load();   // throws here, loadTwice fails too
  return n * 2;             // never runs
}

Future<void> main() async {
  try {
    print(await loadTwice());
  } catch (e) {
    print('failed: $e');    // failed: Bad state: offline
  }
}
```

これは同期呼び出しで例外が伝播するのと同じしくみです。どうすべきかを知っている階層で、一度だけ処理します。

# --instructions--

`fetch` が非同期で例外を投げ、`fetchLength` がそれを await し、`main` がエラーを捕まえて `failed: ` にそれを続けて出力するようにコードを補完してください。プログラムは `failed: Bad state: no server` を出力しなければなりません。

# --seed--

```dart
Future<String> fetch() [/] {
  throw StateError('no server');
}

Future<int> fetchLength() async {
  final text = [/] fetch();
  return text.length;
}

Future<void> main() async {
  [/] {
    print(await fetchLength());
  } [/] (e) {
    print('failed: $e');
  }
}
```

# --answers--

- async
- await
- try
- catch
- then
- finally
- sync*

# --solutions--

```dart
Future<String> fetch() async {
  throw StateError('no server');
}

Future<int> fetchLength() async {
  final text = await fetch();
  return text.length;
}

Future<void> main() async {
  try {
    print(await fetchLength());
  } catch (e) {
    print('failed: $e');
  }
}
```

# --output--

failed: Bad state: no server
