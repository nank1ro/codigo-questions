---
language: dart
exerciseType: 1
---

# --description--

`async` 関数の中では、`await` のおかげで非同期の手順を普通の逐次コードのように書けます。それぞれの `await` は自分の future を待ち、次の行は値がそろってから初めて実行されます：

```dart
Future<int> width() => Future.delayed(const Duration(milliseconds: 5), () => 4);
Future<int> height() => Future.delayed(const Duration(milliseconds: 5), () => 3);

Future<int> area() async {
  final w = await width();
  final h = await height();
  return w * h; // 12
}
```

`await` は式の中で直接使うこともできます。`return await width() * await height();` でも同じ結果になります。

# --instructions--

関数 `price(item)` は `item` の価格を持つ `Future<int>` を返します。`await` を使って `'apple'` の価格と `'milk'` の価格を取得し、その合計を `total` に入れてください。

# --before-seed--

```dart
const prices = {'apple': 3, 'bread': 5, 'milk': 2};

Future<int> price(String item) {
  return Future.delayed(const Duration(milliseconds: 5), () => prices[item]!);
}

Future<dynamic> result() async {
```

# --seed--

```dart
var total = 0;
```

# --after-seed--

```dart
  return total;
}
```

# --before-asserts--

```dart
import 'package:dart_runner/main.dart';
import 'package:test/test.dart';

void main() {
  group('MainTest -', () {
```

# --asserts--

`total` は `5` であるべきです

```dart
  test('test1', () async {
    expect(await result(), 5, reason: '--err-t1--');
  });
```

# --after-asserts--

```dart
  }, timeout: Timeout(const Duration(seconds: 1)));
}
```

# --solutions--

```dart
var total = 0;
total = await price('apple') + await price('milk');
```
