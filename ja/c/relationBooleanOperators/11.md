---
language: c
exerciseType: 2
---

# --instructions--

`word` が `dog` と等しく**ない**かどうかを確認し、ブール値の結果を出力してください。
`strcmp` は文字列が**等しい**場合にのみ `0` を返すことに注意してください。返される 0 が偽のブール値と同じだと誤解しないようにしましょう

# --seed--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat")[/][/];
printf("%d\n", [/]);
```

# --answers--

- word
- "dog"
-  == 
-  = 
- dog
-  != 
- 0
- are_words_equal

# --solutions--

```c
char word[] = "cat";
bool are_words_equal = strcmp(word, "cat") == 0;
printf("%d\n", are_words_equal);
```

# --output--

1
