---
language: c
exerciseType: 2
---

# --description--

**右シフト** 演算子 `>>` は、すべてのビットを右に移動させます。右端からあふれたビットは捨てられます。符号なしの値では、左側に空いた場所はゼロで埋められます:
```c
//  00001100   (12)
//  >> 2
//  00000011   (3)
printf("%u\n", 12u >> 2);
// "3" を出力する
```
`n`ビット右にシフトすると、符号なしの値は2の`n`乗で割られ、余りは捨てられます。
*負の*値の右シフトは移植性がないため、これもビット操作を`unsigned`型で行うべきもう1つの理由です。

# --instructions--

`value`を3ビット右にシフトした結果を出力するようにコードを完成させてください

# --seed--

```c
unsigned int value = 80;
printf("%u\n", value [/] 3);
```

# --answers--

- >>
- <<
- /
- -

# --solutions--

```c
unsigned int value = 80;
printf("%u\n", value >> 3);
```

# --output--

10
