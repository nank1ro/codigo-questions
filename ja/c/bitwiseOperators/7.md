---
language: c
exerciseType: 4
---

# --description--

それぞれの2項ビット演算子には、変数をその場で更新する**複合代入**の形があります。`&=`、`|=`、`^=`、`<<=`、`>>=`です。
```c
unsigned int x = 12;
x &= 10;  // same as x = x & 10;
x |= 1;   // same as x = x | 1;
x ^= 3;   // same as x = x ^ 3;
x <<= 1;  // same as x = x << 1;
x >>= 2;  // same as x = x >> 2;
```
これらは変数名を繰り返すよりも読みやすく、フラグ変数のビットを変更する一般的な方法です。

# --instructions--

`flags`が`0x01`で始まり、次に`|=`で値`4`に相当するビットをオンにし、それから`<<=`で2ビット左にシフトし、最後に出力するように行を並べ替えてください

# --answers--

```c
unsigned int flags = 0x01;
```

```c
flags |= 0x04;
```

```c
flags <<= 2;
```

```c
printf("%u\n", flags);
```

# --solutions--

```c
unsigned int flags = 0x01;
flags |= 0x04;
flags <<= 2;
printf("%u\n", flags);
```

# --output--

20
