---
language: c
exerciseType: 4
---

# --description--

ブロックコメントの一般的な使い方の1つが**ヘッダーコメント**です: 関数の真上に置かれる短いブロックで、その関数が何をするか、パラメータが何を意味するか、何を返すかを説明します。
```c
/*
 * Returns the number of seconds in the given minutes.
 * minutes: a whole number of minutes, never negative
 */
int to_seconds(int minutes) {
    return minutes * 60;
}
```
`to_seconds`を呼び出す人は、本体を読む代わりにヘッダーを読めばよいことになります。一緒に更新されるよう、ヘッダーは関数のそばに置いておきましょう。

# --instructions--

行を並べ替えて、ヘッダーコメントが真上に付いた`max_of`を`main`の前に定義し、`max_of(9, 4)`を出力するプログラムを作ってください

# --answers--

```c
#include <stdio.h>
```

```c
/* Returns the larger of a and b */
```

```c
int max_of(int a, int b) {
```

```c
    if (a > b) {
        return a;
    }
```

```c
    return b;
}
```

```c
int main() {
    printf("%d\n", max_of(9, 4));
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
/* Returns the larger of a and b */
int max_of(int a, int b) {
    if (a > b) {
        return a;
    }
    return b;
}
int main() {
    printf("%d\n", max_of(9, 4));
    return 0;
}
```

# --output--

9
