---
language: c
exerciseType: 2
---

# --description--

コンパイラはすべてのコメントを1つのスペースに置き換えます。つまり、`/* */`コメントはスペースを書ける場所ならどこにでも現れ、文や式の途中にあってもよいということです:
```c
int area = width /* cm */ * height /* cm */;
```
これは、オペランドや呼び出しの引数にラベルを付けるのにたまに役立ちます。`//`コメントにはこれができません。その後のコードを含む行の残り全部がコメントアウトされてしまうからです。

# --instructions--

両方のラベルがブロックコメントになり、プログラムが`36`を出力するようにコードを完成させてください

# --seed--

```c
int price = 12;
int qty = 3;
printf("%d\n", price [/] unit price */ * qty /* items [/]);
```

# --answers--

- /*
- */
- //
- *
- **

# --solutions--

```c
int price = 12;
int qty = 3;
printf("%d\n", price /* unit price */ * qty /* items */);
```

# --output--

36
