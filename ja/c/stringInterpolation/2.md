---
language: c
exerciseType: 4
---

# --description--

`sprintf`は`printf`とまったく同じように動作しますが、画面に書き出す代わりに、フォーマット済みのテキストを**バッファ**と呼ばれる`char`配列に、終端文字`'\0'`を付けて書き込みます:
```c
char buffer[32];
sprintf(buffer, "%d-%d", 3, 7);
printf("%s\n", buffer); // prints "3-7"
```
バッファは呼び出しの前に宣言し、テキスト全体と終端文字を格納できる十分な大きさが必要です。そうしないと`sprintf`は配列の末尾を超えて書き込みます。

# --instructions--

バッファを宣言し、`sprintf`で`50%`を書き込み、バッファを出力するように行を並べ替えてください

# --answers--

```c
char buffer[32];
```

```c
sprintf(buffer, "%d%%", 50);
```

```c
printf("%s\n", buffer);
```

# --solutions--

```c
char buffer[32];
sprintf(buffer, "%d%%", 50);
printf("%s\n", buffer);
```

# --output--

50%
