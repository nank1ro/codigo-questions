---
language: c
exerciseType: 4
---

# --description--

文字列はフォーマット文字列を使わずに結合することもできます。`string.h`の`strcat`は、第2引数のコピーを第1引数の末尾に追加します。第1引数には十分な空き容量が必要で、`strncat`は指定した文字数までしか追加しません:
```c
char text[32] = "Hi";
strcat(text, "!!!");        // text は "Hi!!!" になる
strncat(text, "abcdef", 2); // text は "Hi!!!ab" になる
```
どちらも、追加した文字の後に必ず終端文字`'\0'`を付け加えます。

# --instructions--

`Hello`から始め、`, `を追加し、続けて`worldwide`の最初の5文字を追加し、結果を出力するように行を並べ替えてください

# --answers--

```c
char greeting[32] = "Hello";
```

```c
strcat(greeting, ", ");
```

```c
strncat(greeting, "worldwide", 5);
```

```c
printf("%s\n", greeting);
```

# --solutions--

```c
char greeting[32] = "Hello";
strcat(greeting, ", ");
strncat(greeting, "worldwide", 5);
printf("%s\n", greeting);
```

# --output--

Hello, world
