---
language: c
exerciseType: 4
---

# --description--

総まとめ: 値の種類に応じて型を選び、すべての`printf`指定子をその引数の型に合わせ、計算がオペランドとは異なる型で行われる必要があるときはキャストしてください。

# --instructions--

`total`に`7`、`count`に`2`を格納し、`total`を`count`で割った結果を`double`として`avg`に計算し、整数除算`total / count`とそれに続けて`avg`を小数点以下1桁で出力するプログラムを作るように行を並べ替えてください

# --answers--

```c
#include <stdio.h>
int main() {
```

```c
    int total = 7, count = 2;
```

```c
    double avg = (double) total / count;
```

```c
    printf("%d %.1f\n", total / count, avg);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
int main() {
    int total = 7, count = 2;
    double avg = (double) total / count;
    printf("%d %.1f\n", total / count, avg);
    return 0;
}
```

# --output--

3 3.5
