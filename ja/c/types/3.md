---
language: c
exerciseType: 2
---

# --description--

`%f`は小数点以下6桁を出力しますが、それが欲しい場合はほとんどありません。`%`と`f`の間に精度を指定すると、表示する小数桁数を選べます: `%.2f`は小数点以下2桁、`%.1f`は1桁を出力し、値は切り捨てではなく**四捨五入**されます:
```c
double price = 9.987;
printf("%.2f\n", price); // "9.99" と出力される
printf("%.1f\n", price); // "10.0" と出力される
```
`float`は`double`と同じ指定子で出力されます: `printf`に渡されるときに自動的に`double`へ変換されるためです。

# --instructions--

`price`を`float`として宣言し、価格をちょうど小数点以下2桁で出力するようフォーマット文字列を完成させてください

# --seed--

```c
[/] price = 9.99f;
printf("[/]\n", price);
```

# --answers--

- float
- int
- char
- %.2f
- %.1f
- %f
- %d

# --solutions--

```c
float price = 9.99f;
printf("%.2f\n", price);
```

# --output--

9.99
