---
language: c
exerciseType: 2
---

# --description--

既に初期化された変数 `total` があります
```c
int total = 5;
```
変数 `total` に数値 `2` を加えたい場合、次のように書くことができます
```c
total = total + 2;
```
これで動きます！しかし、同じことをより短く書く方法があります：
```c
total += 2;
```
`+=` 記号は**加算代入**と呼ばれます。
変数の値に値を加え、その結果をその変数に代入します。

# --instructions--

*加算代入*演算子を使って変数 `num` に値 `15` を加え、その値を出力してください

# --seed--

```c
int num = 75;
[/][/][/];
printf("%d\n", num);
```

# --answers--

-  + 
-  += 
-  =+ 
- num
- 15

# --solutions--

```c
int num = 75;
num += 15;
printf("%d\n", num);
```

# --output--

90
