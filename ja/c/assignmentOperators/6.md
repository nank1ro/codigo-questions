---
language: c
exerciseType: 2
---

# --description--

**剰余代入** `%=` 演算子を見てみましょう。
変数と値の剰余を計算し、その結果をその変数に代入します。
つまり、以下は全く同じです
```c
num = num % 5;
// is equal to
num %= 5;
```

# --instructions--

*剰余代入*演算子を使って `result` と値 `3` の剰余を計算し、その値を出力してください

# --seed--

```c
int result = 5;
result[/][/];
printf("%d\n", [/]);
```

# --answers--

-  % 
-  =% 
-  %= 
- 3
- result

# --solutions--

```c
int result = 5;
result %= 3;
printf("%d\n", result);
```

# --output--

2
