---
language: c
exerciseType: 2
---

# --description--

**乗算代入** `*=` 演算子を見てみましょう。
変数に値を掛け、その結果をその変数に代入します。
つまり、以下は全く同じです
```c
num = num * 5;
// is equal to
num *= 5;
```

# --instructions--

*乗算代入*演算子を使って `result` と値 `5` の積を計算し、その値を出力してください

# --seed--

```c
int result = 5;
[/][/][/];
printf("%d\n", result);
```

# --answers--

-  * 
-  *= 
-  =* 
- 5
- 25
- result

# --solutions--

```c
int result = 5;
result *= 5;
printf("%d\n", result);
```

# --output--

25
