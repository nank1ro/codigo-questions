---
language: c
exerciseType: 2
---

# --description--

**除算代入** `/=` 演算子を見てみましょう。
変数を値で割り、その結果をその変数に代入します。
つまり、以下は全く同じです
```c
num = num / 5;
// is equal to
num /= 5;
```

# --instructions--

*除算代入*演算子を使って `result` の値を `5` で割った商を計算し、その値を出力してください

# --seed--

```c
int result = 50;
[/][/][/];
printf("%d\n", result);
```

# --answers--

-  / 
-  /= 
-  =/ 
- 5
- 25
- result

# --solutions--

```c
int result = 50;
result /= 5;
printf("%d\n", result);
```

# --output--

10
