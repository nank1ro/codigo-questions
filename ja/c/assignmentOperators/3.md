---
language: c
exerciseType: 2
---

# --description--

加算代入と同様に、**減算代入** `-=` があります。
機能は同じで、唯一の違いは減算を行うことです。
つまり、以下は全く同じです
```c
num = num - 5;
// is equal to
num -= 5;
```

# --instructions--

*減算代入*演算子を使って変数 `result` から値 `15` を引き、その値を出力してください

# --seed--

```c
int result = 75;
result[/][/];
printf("%d\n", [/]);
```

# --answers--

-  - 
-  -= 
-  =- 
- 50
- 15
- result

# --solutions--

```c
int result = 75;
result -= 15;
printf("%d\n", result);
```

# --output--

60
