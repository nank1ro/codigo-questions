---
language: c
exerciseType: 2
---

# --description--

`strcat(destination, source)`は`source`を`destination`の末尾に追加します:
```c
char text[20] = "Hello";
strcat(text, " World");
// text is now "Hello World"
```
`strcpy`と同様に、コピー先の配列は結果を格納できるだけの十分な大きさが必要です。

# --instructions--

プログラムが`Hello World`を出力するように`" World"`を`greeting`に追加してください

# --seed--

```c
char greeting[20] = "Hello";
[/](greeting, [/]);
printf("%s\n", greeting);
```

# --answers--

- strcat
- strcpy
- strlen
- " World"
- "World"
- 'World'

# --solutions--

```c
char greeting[20] = "Hello";
strcat(greeting, " World");
printf("%s\n", greeting);
```

# --output--

Hello World
