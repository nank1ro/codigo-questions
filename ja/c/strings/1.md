---
language: c
exerciseType: 2
---

# --description--

Cには専用の文字列型がありません。**文字列**とは、特殊な文字である**ヌル終端文字**`'\0'`で終わる`char`の配列です。
文字列を作る最も簡単な方法は、ダブルクォートで囲んだ文字列リテラルを使うことです:
```c
char name[] = "Codigo";
```
コンパイラが文字数を数え、末尾に自動的に`'\0'`を追加してくれます。
文字列を出力するには`%s`プレースホルダーを使います:
```c
printf("%s\n", name);
// prints "Codigo"
```

# --instructions--

テキスト`Codigo`を`char`配列`name`に格納して出力するようにコードを完成させてください

# --seed--

```c
char [/][] = [/];
printf("[/]\n", name);
```

# --answers--

- name
- "name"
- "Codigo"
- 'Codigo'
- %s
- %c

# --solutions--

```c
char name[] = "Codigo";
printf("%s\n", name);
```

# --output--

Codigo
