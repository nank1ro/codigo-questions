---
language: c
exerciseType: 2
---

# --description--

構造体はテキストを保持することがよくあります。テキストは固定サイズの`char`配列メンバとして格納します:
```c
typedef struct {
    char name[20];
    int age;
} Person;
```
配列メンバは、宣言後に`=`で代入できません: `p.name = "Ann"`はコンパイルできません。`string.h`の`strcpy`を使い、コピー先としてメンバを渡してテキストをコピーします:
```c
Person p;
strcpy(p.name, "Ann");
p.age = 30;
```
宣言時の波括弧初期化子だけが文字列を直接受け付けます: `Person p = {"Ann", 30};`

# --instructions--

`p`の名前を`"Ann"`に置き換え、名前と年齢を出力するようにコードを完成させてください

# --seed--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
[/](p.name, "Ann");
printf("%s is %d\n", p[/]name, p.age);
```

# --answers--

- strcpy
- strcat
- strcmp
- strlen
- .
- ->

# --solutions--

```c
typedef struct {
    char name[20];
    int age;
} Person;
Person p = {"Bob", 30};
strcpy(p.name, "Ann");
printf("%s is %d\n", p.name, p.age);
```

# --output--

Ann is 30
