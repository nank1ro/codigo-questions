---
language: c
exerciseType: 4
---

# --description--

構造体は、他の型と同じように配列に格納できます。各要素はそれぞれの波括弧で初期化し、ループで1つずつ訪れます。まずインデックスでアクセスし、その後にドットを使います:
```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};

for (int i = 0; i < 3; i++) {
    printf("%d\n", path[i].y);
}
```

# --instructions--

`Point`型を定義し、3つの点を`path`に格納し、それらの`x`メンバを`sum`に足し合わせて出力するように行を並べ替えてください。`path`は`sum`より前に宣言してください

# --answers--

```c
typedef struct { int x; int y; } Point;
```

```c
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
```

```c
int sum = 0;
```

```c
for (int i = 0; i < 3; i++) {
```

```c
    sum += path[i].x;
```

```c
}
```

```c
printf("%d\n", sum);
```

# --solutions--

```c
typedef struct { int x; int y; } Point;
Point path[3] = {{0, 0}, {2, 1}, {4, 3}};
int sum = 0;
for (int i = 0; i < 3; i++) {
    sum += path[i].x;
}
printf("%d\n", sum);
```

# --output--

6
