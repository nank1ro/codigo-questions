---
language: c
exerciseType: 4
---

# --description--

`printf`には構造体全体のための指定子はありません。一般的な解決策は、メンバを固定のフォーマットで出力する小さな関数を書くことです。そうすればプログラムのどの部分でも同じ方法で値を表示できます:
```c
void print_person(Person p) {
    printf("%s (%d)\n", p.name, p.age);
}
```

# --instructions--

`Point`型、点を`(x, y)`として出力する関数`print_point`、そして点`(3, 4)`を出力する`main`を定義するように行を並べ替えてください。関数は`main`より前に定義してください

# --answers--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
```

```c
void print_point(Point p) {
```

```c
    printf("(%d, %d)\n", p.x, p.y);
```

```c
}
```

```c
int main() {
```

```c
    Point p = {3, 4};
```

```c
    print_point(p);
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
typedef struct { int x; int y; } Point;
void print_point(Point p) {
    printf("(%d, %d)\n", p.x, p.y);
}
int main() {
    Point p = {3, 4};
    print_point(p);
    return 0;
}
```

# --output--

(3, 4)
