---
language: c
exerciseType: 3
---

# --description--

Cでよくある間違いは、`==`を書くべきところに`=`を書いてしまうことです。代入は代入した値そのものを値とする式なので、このコードはそれでもコンパイルできてしまいます:
```c
int x = 5;
if (x = 0) { ... } // x に 0 を代入し、条件は 0（偽）になる
if (x = 3) { ... } // x に 3 を代入し、条件は 3（真）になる
```
ほとんどのコンパイラはこれについて警告を出すので、条件式の挙動がおかしいときは警告を確認してください。

# --instructions--

次のコードの出力は何ですか？
```c
int x = 5;
if (x = 0) {
    printf("zero");
} else {
    printf("not zero");
}
```

# --answers--

- not zero
- zero
- コンパイルエラー
- 5

# --solutions--

- not zero
