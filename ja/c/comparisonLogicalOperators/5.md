---
language: c
exerciseType: 2
---

# --description--

**または**演算子`||`は少なくとも一方が真のときに`1`になり、両辺がともに偽のときだけ`0`になります:
```c
int day = 1;
printf("%d\n", day == 1 || day == 7); // "1" と出力される
```
両辺はそれぞれ完全な比較式でなければなりません。`day == 6 || 7`は「6または7」という意味にはなりません（理由は後で分かります）。

# --instructions--

`day`が`6`または`7`のときに`weekend`が`1`になるようにコードを完成させ、それを出力してください

# --seed--

```c
int day = 7;
int weekend = day == 6 [/] day == [/];
printf("%d\n", weekend);
```

# --answers--

- ||
- 7
- &&
- 6
- =

# --solutions--

```c
int day = 7;
int weekend = day == 6 || day == 7;
printf("%d\n", weekend);
```

# --output--

1
