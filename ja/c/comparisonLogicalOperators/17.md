---
language: c
exerciseType: 4
---

# --description--

`bool`型の引数は、`true`と比較しなくてもそのまま`&&`や`||`のオペランドとして使えます。`citizen == true`ではなく`age >= 18 && citizen`のように書きます。

# --instructions--

`age`が`18`以上かつ`citizen`が`true`のときに`true`を返す`can_vote`と、`20, true`の場合と`20, false`の場合の結果を出力する`main`を定義するように行を並べ替えてください。関数は`main`より前に定義してください

# --answers--

```c
#include <stdio.h>
#include <stdbool.h>
```

```c
bool can_vote(int age, bool citizen) {
```

```c
    return age >= 18 && citizen;
}
```

```c
int main() {
```

```c
    printf("%d\n", can_vote(20, true));
```

```c
    printf("%d\n", can_vote(20, false));
```

```c
    return 0;
}
```

# --solutions--

```c
#include <stdio.h>
#include <stdbool.h>
bool can_vote(int age, bool citizen) {
    return age >= 18 && citizen;
}
int main() {
    printf("%d\n", can_vote(20, true));
    printf("%d\n", can_vote(20, false));
    return 0;
}
```

# --output--

1
0
