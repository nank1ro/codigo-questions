---
language: c
exerciseType: 2
---

# --description--

`char`は小さな整数なので、同じ演算子で文字を比較できます。
シングルクォートで囲んだ文字リテラルと比較します:
```c
char answer = 'y';
printf("%d\n", answer == 'y'); // "1" と出力される
```
ダブルクォートを使うと文字列になり、この方法では比較できません。

# --instructions--

`grade`が文字`B`と等しいかどうかを出力するようコードを完成させてください

# --seed--

```c
char grade = 'B';
printf("%d\n", grade [/] [/]);
```

# --answers--

- ==
- 'B'
- "B"
- =
- 'b'

# --solutions--

```c
char grade = 'B';
printf("%d\n", grade == 'B');
```

# --output--

1
