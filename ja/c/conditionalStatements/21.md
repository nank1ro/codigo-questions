---
language: c
exerciseType: 2
---

# --description--

`bool`変数が条件として機能するのは、`if`がその時点で変数に格納されている値だけを見るからです。`online`に`false`が入っている場合、`if (online)`は`if (false)`とまったく同じように振る舞います。

# --instructions--

コードブロックが実行**されない**ように空欄を埋めてください

# --seed--

```c
bool online = [/];
if ([/]) {
    printf("Hello User!\n");
}
```

# --answers--

- online
- "online"
- Online
- false
- true

# --solutions--

```c
bool online = false;
if (online) {
    printf("Hello User!\n");
}
```
