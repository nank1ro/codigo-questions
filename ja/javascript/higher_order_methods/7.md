---
language: javascript
exerciseType: 4
---

# --description--

比較関数は要素のどの部分でも見られるので、あるプロパティを比較するだけでオブジェクトの配列をそのプロパティ順に並べ替えられます。
```javascript
const items = [{ name: "b", size: 3 }, { name: "a", size: 1 }];
const bySize = [...items].sort((x, y) => x.size - y.size);
console.log(bySize.map((item) => item.name));
// prints [ 'a', 'b' ]
```
コピーを並べ替えれば `items` は元の順序のまま残ります。

# --instructions--

行を並べ替えて、`players` のコピーを `score` の昇順で並べ替え、その順序で名前を `map` を使って出力し、その後 `players` の最初の要素の名前を出力して変更されていないことを示してください。`ranked` を `names` より先に宣言すること

# --answers--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
```

```javascript
const ranked = [...players].sort((a, b) => a.score - b.score);
```

```javascript
const names = ranked.map((p) => p.name);
```

```javascript
console.log(names);
```

```javascript
console.log(players[0].name);
```

# --solutions--

```javascript
const players = [{ name: "Zoe", score: 80 }, { name: "Leo", score: 65 }, { name: "Mia", score: 92 }];
const ranked = [...players].sort((a, b) => a.score - b.score);
const names = ranked.map((p) => p.name);
console.log(names);
console.log(players[0].name);
```

# --output--

[ 'Leo', 'Zoe', 'Mia' ]
Zoe
