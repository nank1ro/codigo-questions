---
language: javascript
exerciseType: 2
---

# --description--

`sort(compare)`は、2つの要素を受け取り、1つ目を先にすべきなら負の数、2つ目を先にすべきなら正の数、等しいなら`0`を返すコールバックを使って、配列をその場で並べ替えます。数値の場合、`(a, b) => a - b`は昇順に、`(a, b) => b - a`は降順に並べ替えます。
`find(callback)`はコールバックが`true`を返す最初の要素を返し、なければ`undefined`を返します：
```javascript
const scores = [50, 90, 70];
scores.sort((a, b) => a - b);
console.log(scores);
// prints [ 50, 70, 90 ]
console.log(scores.find((s) => s > 60));
// prints 70
```

# --instructions--

`ages`を昇順に並べ替え、`21`より大きい最初の年齢を見つけ、並べ替えた配列とその年齢の両方を出力してください

# --seed--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a [/] b);
const adult = ages.find((age) => age [/] 21);
console.log(ages);
console.log(adult);
```

# --answers--

- -
- >
- +
- <
- ===
- *

# --solutions--

```javascript
const ages = [31, 18, 25];
ages.sort((a, b) => a - b);
const adult = ages.find((age) => age > 21);
console.log(ages);
console.log(adult);
```

# --output--

[ 18, 25, 31 ]
25
