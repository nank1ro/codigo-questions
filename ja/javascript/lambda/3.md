---
language: javascript
exerciseType: 3
---

# --description--

暗黙のreturnには1つの罠があります。本体が`{`で始まるアロー関数は、オブジェクトリテラルとしてではなく**ブロック本体**として解釈されます：
```javascript
const make = (name) => { name: name };
console.log(make("Ana"));
// prints undefined
```
ここで`{ name: name }`は、ラベル`name:`とその後の式`name`を含むブロックです。何も返されないため、呼び出しは`undefined`になります。
1行でオブジェクトリテラルを返すには、**括弧**で囲んでJavaScriptに式として扱わせます：
```javascript
const make = (name) => ({ name: name });
console.log(make("Ana"));
// prints { name: 'Ana' }
```

# --instructions--

次のコードは何を出力しますか？
```javascript
const wrap = (n) => { value: n };
console.log(wrap(7));
```

# --answers--

- undefined
- { value: 7 }
- 7
- `SyntaxError`、なぜならブロック本体にはラベルを含められないから

# --solutions--

- undefined
