---
language: javascript
exerciseType: 4
---

# --description--

コールバックを取る配列メソッドがもう2つあります。
`forEach(callback)`は要素ごとにコールバックを1回呼び出し、何も返しません。出力などの副作用のために使います。
`reduce(callback, initialValue)`は配列を1つの値に畳み込みます。コールバックはこれまでの累積値と現在の要素を受け取り、新しい累積値を返します：
```javascript
const numbers = [1, 2, 3];
numbers.forEach((n) => console.log(n));
// prints 1, 2 and 3 on three lines
const total = numbers.reduce((sum, n) => sum + n, 0);
console.log(total);
// prints 6
```

# --instructions--

配列を宣言した直後に`reduce`で`prices`の合計を計算し、`forEach`ですべての価格を1行に1つずつ出力し、その後に合計を出力するように行を並べ替えてください

# --answers--

```javascript
const prices = [4, 6, 10];
```

```javascript
const total = prices.reduce((sum, p) => sum + p, 0);
```

```javascript
prices.forEach((p) => console.log(p));
```

```javascript
console.log(total);
```

# --solutions--

```javascript
const prices = [4, 6, 10];
const total = prices.reduce((sum, p) => sum + p, 0);
prices.forEach((p) => console.log(p));
console.log(total);
```

# --output--

4
6
10
20
