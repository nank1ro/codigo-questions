---
language: javascript
exerciseType: 4
---

# --instructions--

プライベートな`count`を増やして返すアロー関数を返す`makeCounter`を定義し、カウンター`next`を作成して、2回の呼び出しの結果を同じ行に出力するように行を並べ替えてください

# --answers--

```javascript
const makeCounter = () => {
```

```javascript
  let count = 0;
```

```javascript
  return () => {
```

```javascript
    count += 1;
```

```javascript
    return count;
```

```javascript
  };
```

```javascript
};
```

```javascript
const next = makeCounter();
```

```javascript
console.log(next(), next());
```

# --solutions--

```javascript
const makeCounter = () => {
  let count = 0;
  return () => {
    count += 1;
    return count;
  };
};
const next = makeCounter();
console.log(next(), next());
```

# --output--

1 2
