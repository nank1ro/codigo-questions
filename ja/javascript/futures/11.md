---
language: javascript
exerciseType: 2
---

# --description--

複数の結果が必要なとき、1つずつawaitしていては時間の無駄です。それぞれが前のものが終わってからでないと始まらないからです。**`Promise.all(promises)`**は、すでに実行中のプロミスの配列を受け取り、すべての値の配列で履行される1つのプロミスを返します：
```javascript
const results = await Promise.all([fetchUser(), fetchOrders()]);
```
覚えておく価値のある2つのルールがあります：

- 値は終わった順ではなく、**配列の順序どおり**に返ってきます
- どれかのプロミスが拒否されると、`Promise.all`が返すプロミスはその最初のエラーですぐに拒否され、他の値は失われます

# --instructions--

両方の値を一緒にawaitし、2つの結果がダッシュでつなげられて出力され、`a-b`になるようにコードを完成させてください

# --seed--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => resolve(value), ms));
}

async function main() {
  const results = await Promise.[/]([later("a", 30), later("b", 10)]);
  console.log(results.[/]("-"));
}

main();
```

# --answers--

- all
- join
- race
- push
- allSettled

# --solutions--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => resolve(value), ms));
}

async function main() {
  const results = await Promise.all([later("a", 30), later("b", 10)]);
  console.log(results.join("-"));
}

main();
```

# --output--

a-b
