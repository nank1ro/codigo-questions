---
language: javascript
exerciseType: 1
---

# --description--

関数の前に`async`を付けると、その関数が返すものが変わります。本体は今までどおり普通の値を計算しますが、呼び出し元はその値を包んだプロミスを受け取ります。
```javascript
function shout(text) {
  return text.toUpperCase();
}
async function shoutLater(text) {
  return text.toUpperCase();
}

console.log(shout("hi"));
// prints HI
console.log(shoutLater("hi"));
// prints Promise { 'HI' }
```
2つの関数の中身は同じコードで、結果の読み取り方だけが異なります。`shoutLater("hi")`が`"HI"`を返すようにするには、別の`async`関数の中でawaitする必要があります。

待つことが何もない場合、関数に`async`を付けてもコストはかかりません。それでいて、後でその中で`await`を使えるようになります。

# --instructions--

文字列`name`を受け取り、`Hello, `の後に名前と感嘆符を続けたもの（例：`Hello, Ada!`）を返す`async`関数`greet`を書いてください

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
async function greet(name) {
  
}
```

# --asserts--

`greet` は `async` キーワードで宣言されている必要があります

```json
{
  "regex": "async\\s+function\\s+greet",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`await greet("Ada")` は `"Hello, Ada!"` であるべきです

```javascript
(async () => {
  tryCatch((await greet("Ada")) === "Hello, Ada!");
})().catch(() => tryCatch(false));
```

`await greet("Bob")` は `"Hello, Bob!"` であるべきです

```javascript
(async () => {
  tryCatch((await greet("Bob")) === "Hello, Bob!");
})().catch(() => tryCatch(false));
```

`await greet("")` は `"Hello, !"` であるべきです

```javascript
(async () => {
  tryCatch((await greet("")) === "Hello, !");
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
async function greet(name) {
  return `Hello, ${name}!`;
}
```
