---
language: javascript
exerciseType: 4
---

# --description--

**逐次**と**並列**の待機の違いは、`await`を*どこに*置くかで決まります：
```javascript
// sequential: about 300 + 300 = 600 ms
const a = await load("a");
const b = await load("b");

// parallel: about 300 ms
const [a, b] = await Promise.all([load("a"), load("b")]);
```
最初のバージョンでは、`await`がその行で関数を一時停止するため、2番目のダウンロードは1番目が終わってから始まります。2番目のバージョンでは、何かをawaitする前に両方の呼び出しが行われるので、`Promise.all`が待っている間に、両方のダウンロードはすでに実行されています。

2番目のタスクが本当に1番目の結果を必要とするときだけ、逐次のawaitを使ってください。そうでなければ、まずすべてを開始してからまとめてawaitします。

# --instructions--

`later`を定義し、次に`loading`を出力し、両方の`later`呼び出しを`Promise.all`で一緒に開始し、2つの結果をスペースでつなげて出力し、最後に`done`を出力する`async`関数`main`を定義するプログラムに項目を並べ替えてください。最初に`later`、次に`main`を定義し、最後の行で`main()`を呼び出してください

# --answers--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
```

```javascript
async function main() {
```

```javascript
  console.log("loading");
```

```javascript
  const results = await Promise.all([later("one", 30), later("two", 10)]);
```

```javascript
  console.log(results.join(" "));
```

```javascript
  console.log("done");
```

```javascript
}
```

```javascript
main();
```

# --solutions--

```javascript
function later(value, ms) {
  return new Promise((resolve) => setTimeout(() => {
    console.log(`ready ${value}`);
    resolve(value);
  }, ms));
}
async function main() {
  console.log("loading");
  const results = await Promise.all([later("one", 30), later("two", 10)]);
  console.log(results.join(" "));
  console.log("done");
}
main();
```

# --output--

loading
ready two
ready one
one two
done
