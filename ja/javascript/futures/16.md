---
language: javascript
exerciseType: 1
---

# --description--

最後のピースを組み合わせると、ほとんどすべての実際のアプリケーションで使われる小さなツールができます。締め切りです。`ms`ミリ秒後に拒否されるプロミスを作り、それを実際の作業と競わせると、先に確定したほうが結果を決めます：
```javascript
const timer = new Promise((resolve, reject) => {
  setTimeout(() => reject(new Error("timeout")), ms);
});
return Promise.race([work, timer]);
```
`async`関数からプロミスを返すのも問題ありません。関数が返すプロミスはそれに追従するので、呼び出し元がawaitするのは、プロミスの中のプロミスではなく最終的な値です。

# --instructions--

プロミスとミリ秒数`ms`を受け取り、プロミスが`ms`以内に確定すればその結果を返し、そうでなければ`new Error("timeout")`で拒否される`async`関数`withTimeout`を書いてください。`Promise.race`を使ってください

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
async function withTimeout(promise, ms) {
  
}
```

# --asserts--

すでに履行されたプロミスは、その値を返す必要があります

```javascript
(async () => {
  tryCatch((await withTimeout(Promise.resolve("ok"), 50)) === "ok");
})().catch(() => tryCatch(false));
```

締め切りより前に拒否されたプロミスは、その独自のエラーを保つ必要があります

```javascript
(async () => {
  const failing = Promise.reject(new Error("broken"));
  let message = "";
  await withTimeout(failing, 60).catch((error) => { message = error.message; });
  tryCatch(message === "broken");
})().catch(() => tryCatch(false));
```

十分に速いプロミスは、プロミスではなくその値を返す必要があります

```javascript
(async () => {
  const quick = new Promise((resolve) => setTimeout(() => resolve(7), 10));
  tryCatch((await withTimeout(quick, 120)) === 7);
})().catch(() => tryCatch(false));
```

遅すぎるプロミスは、メッセージ `"timeout"` で拒否される必要があります

```javascript
(async () => {
  const slow = new Promise((resolve) => setTimeout(() => resolve("late"), 150));
  let message = "";
  await withTimeout(slow, 20).catch((error) => { message = error.message; });
  tryCatch(message === "timeout");
})().catch(() => tryCatch(false));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
async function withTimeout(promise, ms) {
  const timer = new Promise((resolve, reject) => {
    setTimeout(() => reject(new Error("timeout")), ms);
  });
  return Promise.race([promise, timer]);
}
```
