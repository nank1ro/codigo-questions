---
language: javascript
exerciseType: 2
---

# --description--

前の例の続きを見てみましょう。
```javascript
var niceWeather = true;
if (niceWeather) {
    // play outside
}
```
`if`文は条件が`true`の場合にのみコードブロックを実行することがわかりました。
もう一つ重要なことは、コードブロックを示す**波括弧**`{}`です。

# --instructions--

*波括弧*`{}`を追加して`if`文を修正してください

# --seed--

```javascript
var time = 8;
if (time < 12) [/]
    console.log("It's morning");
[/]
```

# --answers--

- if
- ==
- =
- else
- ?
- {
- }

# --solutions--

```javascript
var time = 8;
if (time < 12) {
    console.log("It's morning");
}
```

# --output--

It's morning
