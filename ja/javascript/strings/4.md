---
language: javascript
exerciseType: 1
---

# --description--

文字列が別の文字列を含んでいるかを調べるには、次のいずれもブール値を返すメソッドを使います:
- `includes(text)`は`text`がどこかに現れれば`true`
- `startsWith(text)`は文字列が`text`で始まれば`true`
- `endsWith(text)`は文字列が`text`で終われば`true`

```javascript
let file = "photo.png";
console.log(file.includes("."));
// prints true
console.log(file.startsWith("ph"));
// prints true
console.log(file.endsWith(".jpg"));
// prints false
```
比較は大文字・小文字を区別します: `"Hello".includes("h")`は`false`です。

# --instructions--

`fileName`が`".png"`で終わる場合は`true`、それ以外は`false`を返す関数`isImage(fileName)`を書いてください

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function isImage(fileName) {

}
```

# --asserts--

`isImage("cat.png")`は`true`を返さなければなりません。

```javascript
tryCatch(isImage("cat.png") === true);
```

`isImage("cat.jpg")`は`false`を返さなければなりません。

```javascript
tryCatch(isImage("cat.jpg") === false);
```

`isImage("png.txt")`は`false`を返さなければなりません。

```javascript
tryCatch(isImage("png.txt") === false);
```

`isImage("a.png.txt")`は`false`を返さなければなりません。

```javascript
tryCatch(isImage("a.png.txt") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function isImage(fileName) {
  return fileName.endsWith(".png");
}
```
