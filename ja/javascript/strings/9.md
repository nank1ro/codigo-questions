---
language: javascript
exerciseType: 2
---

# --description--

`split(separator)`メソッドは、`separator`が現れるたびに区切って、文字列を断片の**配列**に分割します:
```javascript
let sentence = "I like JavaScript";
let words = sentence.split(" ");
console.log(words);
// prints [ 'I', 'like', 'JavaScript' ]
```
その逆が配列メソッドの`join(separator)`で、断片を再び1つの文字列につなぎ合わせます:
```javascript
console.log(words.join("-"));
// prints I-like-JavaScript
```

# --instructions--

`csv`をカンマごとに分割し、含まれる色の数を出力してください

# --seed--

```javascript
let csv = "red,green,blue";
let colors = csv.[/]([/]);
console.log(colors.[/]);
```

# --answers--

- split
- ","
- " "
- join
- length
- size

# --solutions--

```javascript
let csv = "red,green,blue";
let colors = csv.split(",");
console.log(colors.length);
```

# --output--

3
