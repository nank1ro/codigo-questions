---
language: javascript
exerciseType: 2
---

# --description--

型を関数として呼び出すことで、値を別の型に**明示的に**変換できます:
- `Number(value)`は数値に変換します: `Number("42")`は`42`
- `String(value)`は文字列に変換します: `String(42)`は`"42"`
- `Boolean(value)`はブーリアンに変換します: `Boolean("")`は`false`

結果はまったく新しい値であり、元の値は変更されません:
```javascript
const input = "7";
const count = Number(input) + 1;
console.log(count, typeof count);
// prints 8 number
console.log(String(count) + "!");
// prints 8!
```
明示的に変換すると意図がはっきりします。`Number(input)`を読めば、`input`がテキストだったことがわかります。

# --instructions--

`count`が`input`を数値に変換したものに`1`を足した値、`label`が`count`を文字列に変換して2回繰り返した値、`flag`が空文字列をブーリアンに変換した値になり、出力`43 4343 false`が得られるようにコードを完成させてください

# --seed--

```javascript
const input = "42";
const count = [/](input) + 1;
const label = [/](count).repeat(2);
const flag = [/]("");
console.log(count, label, flag);
```

# --answers--

- Number
- String
- Boolean
- Int
- Bool
- toString

# --solutions--

```javascript
const input = "42";
const count = Number(input) + 1;
const label = String(count).repeat(2);
const flag = Boolean("");
console.log(count, label, flag);
```

# --output--

43 4343 false
