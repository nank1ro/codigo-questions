---
language: javascript
exerciseType: 4
---

# --description--

JavaScriptには整数と小数のための`number`型が1つだけあります。すべての数値は64ビット浮動小数点値（*double*）です。そのため`5`と`5.0`は同じ値であり、別の整数型は存在しません:
```javascript
console.log(5 === 5.0, 10 / 2);
// prints true 5
```
数値に小数部分がないかを調べるには、`Number.isInteger`を使います:
```javascript
console.log(Number.isInteger(5.0), Number.isInteger(3.5));
// prints true false
```
テンプレートリテラルは補間された値を`String()`と同じルールで文字列に変換するので、`${5.0}`は`"5.0"`ではなく`"5"`になります。

# --instructions--

行を並べ替えて、`n`を2で割り、結果が整数かどうかを言う文を返す`half`を定義し、その結果を`10`と`7`に対して表示するようにしてください。最初に関数を定義します

# --answers--

```javascript
function half(n) {
```

```javascript
  const result = n / 2;
```

```javascript
  if (Number.isInteger(result)) {
```

```javascript
    return `${result} is whole`;
```

```javascript
  }
```

```javascript
  return `${result} is not whole`;
```

```javascript
}
```

```javascript
console.log(half(10));
```

```javascript
console.log(half(7));
```

# --solutions--

```javascript
function half(n) {
  const result = n / 2;
  if (Number.isInteger(result)) {
    return `${result} is whole`;
  }
  return `${result} is not whole`;
}
console.log(half(10));
console.log(half(7));
```

# --output--

5 is whole
3.5 is not whole
