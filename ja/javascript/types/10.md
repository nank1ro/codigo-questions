---
language: javascript
exerciseType: 1
---

# --description--

JavaScriptがブーリアンを必要とするとき、例えば`if`の条件や`Boolean(value)`の中では、値が変換されます。`false`になる値は8つだけで、それらは**falsy**と呼ばれます:
`false`、`0`、`-0`、`0n`、`""`、`null`、`undefined`、`NaN`です。
**それ以外のすべてはtruthyです**。空に見える値もいくつか含まれます:
```javascript
console.log(Boolean(0), Boolean(""), Boolean(NaN));
// prints false false false
console.log(Boolean("0"), Boolean("false"), Boolean([]), Boolean({}));
// prints true true true true
```
`"0"`は空でない文字列なのでtruthyです。空の配列はオブジェクトなので、これもtruthyです。

# --instructions--

配列`values`の要素のうちtruthyなものがいくつあるかを返す関数`countTruthy(values)`を書いてください

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function countTruthy(values) {

}
```

# --asserts--

`countTruthy([1, "a", true])` は `3` を返すべきです

```javascript
tryCatch(countTruthy([1, "a", true]) === 3);
```

`countTruthy([0, "", null, undefined, NaN, false])` は `0` を返すべきです

```javascript
tryCatch(countTruthy([0, "", null, undefined, NaN, false]) === 0);
```

`countTruthy(["0", "false", [], {}])` は `4` を返すべきです

```javascript
tryCatch(countTruthy(["0", "false", [], {}]) === 4);
```

`countTruthy([0n, -0, " "])` は `1` を返すべきです。truthyなのは空白の文字列だけだからです

```javascript
tryCatch(countTruthy([0n, -0, " "]) === 1);
```

`countTruthy([])` は `0` を返すべきです

```javascript
tryCatch(countTruthy([]) === 0);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function countTruthy(values) {
  let count = 0;
  for (const value of values) {
    if (value) {
      count++;
    }
  }
  return count;
}
```
