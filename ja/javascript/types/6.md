---
language: javascript
exerciseType: 2
---

# --description--

「これは`NaN`か？」と尋ねる方法は2つあり、それぞれ異なる問いに答えます。
古いグローバル関数`isNaN(value)`は、まず`value`を数値に**変換**してからチェックします。そのため、`NaN`ですらないものに対しても、数値になれないものなら何でも`true`を返します:
```javascript
console.log(isNaN("hello"));
// trueを出力、Number("hello") はNaNであるため
console.log(isNaN("42"));
// falseを出力、Number("42") は42であるため
```
`Number.isNaN(value)`は変換し**ません**。`value`が本当に数値の`NaN`であるときにだけ`true`になります:
```javascript
console.log(Number.isNaN("hello"));
// falseを出力、文字列はNaNではない
console.log(Number.isNaN(Number("hello")));
// true を出力
```
`Number.isNaN`を優先して使い、変換が失敗したかどうかを知りたいときは先に変換してください。

# --instructions--

最初の行はグローバルの`isNaN`を使い、2行目は文字列そのものに`Number.isNaN`を使い、3行目はチェックする前に`value`を数値に変換するようにし、出力`true`、`false`、`true`が得られるようにコードを完成させてください

# --seed--

```javascript
const value = "hello";
console.log([/](value));
console.log([/](value));
console.log(Number.isNaN([/]));
```

# --answers--

- isNaN
- Number.isNaN
- Number(value)
- String(value)
- value
- isNumber

# --solutions--

```javascript
const value = "hello";
console.log(isNaN(value));
console.log(Number.isNaN(value));
console.log(Number.isNaN(Number(value)));
```

# --output--

true
false
true
