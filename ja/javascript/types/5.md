---
language: javascript
exerciseType: 1
---

# --description--

`Number()`は厳密です。文字列全体が数値でなければならず、そうでなければ結果は`NaN`（"Not a Number"）になります:
```javascript
console.log(Number("12px"));
// prints NaN
```
`parseInt()`と`parseFloat()`はより寛容です。文字列の先頭から数字を読み、先頭の空白をスキップし、数値の一部ではない最初の文字で止まります。`parseInt`は整数部分だけを保持します:
```javascript
console.log(parseInt("12px"), parseFloat("1.5kg"));
// prints 12 1.5
console.log(parseInt("3.9em"), parseInt("-4px"));
// prints 3 -4
```
文字列が数値の始まりになり得るもの（任意の符号とその後の数字）で始まっていない場合、これらも`NaN`を返します:
```javascript
console.log(parseInt("auto"));
// prints NaN
```
`NaN`は自分自身と等しくない唯一の値です。そのため`x === NaN`は常に`false`になります。`NaN`を検出するには`Number.isNaN(x)`を使います。

# --instructions--

`parseInt`を使って、`text`の先頭にある整数（`"12px"`に対する`12`など）を返し、`text`が数値で始まっていない場合には`0`を返す関数`toPixels(text)`を書いてください

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function toPixels(text) {

}
```

# --asserts--

`toPixels("12px")` は `12` を返すべきです

```javascript
tryCatch(toPixels("12px") === 12);
```

`toPixels("3.9em")` は `3` を返すべきです

```javascript
tryCatch(toPixels("3.9em") === 3);
```

`toPixels("-4px")` は `-4` を返すべきです

```javascript
tryCatch(toPixels("-4px") === -4);
```

`toPixels("auto")` は `0` を返すべきです

```javascript
tryCatch(toPixels("auto") === 0);
```

`toPixels("")` は `0` を返すべきです

```javascript
tryCatch(toPixels("") === 0);
```

関数は `parseInt` を使うべきです

```javascript
tryCatch(/parseInt\(/.test(toPixels.toString()));
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function toPixels(text) {
  const value = parseInt(text);
  if (Number.isNaN(value)) {
    return 0;
  }
  return value;
}
```
