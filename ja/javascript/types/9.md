---
language: javascript
exerciseType: 2
---

# --description--

**緩い**等値演算子`==`は、比較の前に2つの側を共通の型に変換します。そのルールは覚えるのが難しいものです:
```javascript
console.log("5" == 5);
// prints true, "5" becomes 5
console.log(0 == "");
// prints true, "" becomes 0
console.log(0 == false, "1" == true);
// prints true true
```
**厳密**等値演算子`===`は決して変換しません。異なる型の値は単純に等しくありません:
```javascript
console.log("5" === 5, 0 === "", 0 === false);
// prints false false false
```
デフォルトでは`===`（と`!==`）を使ってください。唯一のよくある例外は`value == null`で、これは`null`と`undefined`をまとめてチェックします。

# --instructions--

1行目と3行目は緩い等値演算子を使い、2行目と4行目は厳密等値演算子を使い、出力`true`、`false`、`true`、`false`が得られるようにコードを完成させてください

# --seed--

```javascript
console.log("5" [/] 5);
console.log("5" [/] 5);
console.log(0 [/] "");
console.log(0 [/] false);
```

# --answers--

- ==
- ===
- ==
- ===
- =
- is
- <=>

# --solutions--

```javascript
console.log("5" == 5);
console.log("5" === 5);
console.log(0 == "");
console.log(0 === false);
```

# --output--

true
false
true
false
