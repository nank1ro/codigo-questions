---
language: javascript
exerciseType: 2
---

# --description--

JavaScriptのすべての値には**型**があります。**プリミティブ**型は7つあります:
- `number`は`42`や`3.14`のような任意の数値
- `string`は`"Ana"`のようなテキスト
- `boolean`は`true`と`false`
- `undefined`は一度も提供されなかった値
- `null`は意図的に空にされた値
- `bigint`は`9007199254740993n`のような任意の大きさの整数
- `symbol`は`Symbol()`で作成される一意の識別子

それ以外のすべて（配列、関数、`{}`で作成されたオブジェクト、日付...）は`object`です。
`typeof`演算子は値の型を文字列として教えてくれます:
```javascript
console.log(typeof 42, typeof "Ana", typeof true);
// prints number string boolean
let city;
console.log(typeof city);
// prints undefined
```

# --instructions--

`age`が数値`42`を保持し、`name`が文字列`"Ana"`を保持し、`active`がブーリアン`true`を保持し、`city`が値なしで宣言され、出力`number string boolean undefined`が得られるようにコードを完成させてください

# --seed--

```javascript
const age = [/];
const name = [/];
const active = [/];
let [/];
console.log(typeof age, typeof name, typeof active, typeof city);
```

# --answers--

- 42
- "Ana"
- true
- city
- null
- 42n
- city = null

# --solutions--

```javascript
const age = 42;
const name = "Ana";
const active = true;
let city;
console.log(typeof age, typeof name, typeof active, typeof city);
```

# --output--

number string boolean undefined
