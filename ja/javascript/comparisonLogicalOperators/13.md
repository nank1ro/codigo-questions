---
language: javascript
exerciseType: 2
---

# --description--

`>=`はその名前の半分のどちらが成立しても真になります。2つの値が等しいので`8 >= 8`は`true`ですが、より厳密な`8 > 8`は`false`です。

# --instructions--

`num` が `8` **以上**かどうかを確認し、ブール値の結果を出力してください

# --seed--

```javascript
var num = 8;
console.log([/][/][/]);
```

# --answers--

- num 
- 'num'
- == 
- = 
- >= 
- 8

# --solutions--

```javascript
var num = 8;
console.log(num >= 8);
```

# --output--

true
