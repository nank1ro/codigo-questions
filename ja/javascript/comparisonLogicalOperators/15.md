---
language: javascript
exerciseType: 2
---

# --description--

一方が文字列でもう一方が数値の場合、`<=`はまず文字列を数値に変換します。そのため`"7" <= 8`は`true`になります。

# --instructions--

`num` が `8` **以下**かどうかを確認し、ブール値の結果を出力してください

# --seed--

```javascript
var num = 7;
console.log([/][/][/]);
```

# --answers--

- num
- 'num'
-  <= 
-  = 
-  >= 
- 8

# --solutions--

```javascript
var num = 7;
console.log(num <= 8);
```

# --output--

true
