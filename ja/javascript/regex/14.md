---
language: javascript
exerciseType: 4
---

# --description--

置換には文字列である必要はありません。**関数**を渡すと、マッチごとに1回呼び出され、その関数が返したものがそのマッチの位置に挿入されます。

関数はまずマッチ全体を受け取り、その後に各キャプチャグループを受け取ります:
```javascript
console.log("hello world".replace(/\w+/g, (word) => word.length));
// 5 5 を出力

console.log("ann lee".replace(/(\w)(\w*)/g, (whole, first, rest) => first.toUpperCase() + rest));
// Ann Lee を出力
```
マッチしたテキストから置換結果を計算できるのは、この方法だけです。`$1` だけではこれはできません。

# --instructions--

すべての数字をその2倍の値に置き換えたテキストを出力するプログラムになるように項目を並び替えてください。テキストを先に宣言し、その後に関数を宣言します

# --answers--

```javascript
const text = "a1 b2 c3";
```

```javascript
function double(digit) {
```

```javascript
  return Number(digit) * 2;
```

```javascript
}
```

```javascript
console.log(text.replace(/\d/g, double));
```

# --solutions--

```javascript
const text = "a1 b2 c3";
function double(digit) {
  return Number(digit) * 2;
}
console.log(text.replace(/\d/g, double));
```

# --output--

a2 b4 c6
