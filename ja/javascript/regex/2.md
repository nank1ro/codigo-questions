---
language: javascript
exerciseType: 1
---

# --description--

パターンは、1つの正確な文字の代わりに文字の*種類*を記述すると実用的になります。ほとんどのニーズは少数の**エスケープシーケンス**でカバーできます:
- `\d` 任意の数字、`0` から `9`
- `\w` 任意の単語文字、つまり英字・数字・`_`
- `\s` 任意の空白文字、つまりスペース・タブ・改行

```javascript
console.log(/\d/.test("room 12"));
// true を出力
console.log(/\d/.test("lobby"));
// false を出力
```
**量指定子**は、直前の部分が何回繰り返せるかを示します。最も一般的なのは「1つ以上」を意味する `+` です:
```javascript
console.log(/\d+/.test("42"));
// true を出力
```
つまり `/\d/` は1桁の数字1つにマッチし、`/\d+/` は数字の連なりにマッチします。単純な `test` では、どちらも数字が1つあればよいので、同じように動作します。

# --instructions--

`test` を使って、文字列 `text` を受け取り、`text` に少なくとも1つの数字が含まれるときに `true` を返す関数 `hasDigit` を書いてください

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function hasDigit(text) {
  
}
```

# --asserts--

`test` メソッドを使う必要があります

```json
{
  "regex": "\\.test\\(",
  "modifiers": ["m"],
  "shouldMatch": true
}
```

`hasDigit("room 12")` は `true` を返す必要があります

```javascript
tryCatch(hasDigit("room 12") === true);
```

`hasDigit("lobby")` は `false` を返す必要があります

```javascript
tryCatch(hasDigit("lobby") === false);
```

`hasDigit("7")` は `true` を返す必要があります

```javascript
tryCatch(hasDigit("7") === true);
```

`hasDigit("")` は `false` を返す必要があります

```javascript
tryCatch(hasDigit("") === false);
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function hasDigit(text) {
  return /\d/.test(text);
}
```
