---
language: javascript
exerciseType: 1
---

# --description--

最後のフラグで一式がそろいます。デフォルトでは `^` と `$` は**文字列全体**の先頭と末尾を意味するので、`^` でアンカーされたパターンは、テキストが複数行にわたる場合でも、ごく最初の位置にしかマッチできません。

**`m`**（マルチライン）フラグはこれを変えます: `^` と `$` は、それぞれすべての改行の直後と直前にもマッチするようになり、各行が独立してアンカーされます:
```javascript
const text = "note a\nb\nnote c";
console.log(text.match(/^note.*/g));
// [ 'note a' ] を出力
console.log(text.match(/^note.*/gm));
// [ 'note a', 'note c' ] を出力
```
ここで重要な詳細が2つあります。デフォルトでは `.` は改行にマッチしないので（それを変えるのは `s` フラグだけです）、`.*` はそれだけで行末で止まります。また、`g` フラグ付きの `match` は、何もマッチしないときに空の配列ではなく `null` を返すので、配列を返すと約束するときは `?? []` と組み合わせてください。

# --instructions--

文字列 `text` を受け取り、`todo` で始まるすべての行（大文字小文字は無視）の配列を返す関数 `todoLines` を書いてください。1行もなければ空の配列を返します

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function todoLines(text) {
  
}
```

# --asserts--

`todoLines("todo a\nb\nTODO c")` は両方の todo 行を返す必要があります

```javascript
tryCatch(JSON.stringify(todoLines("todo a\nb\nTODO c")) === '["todo a","TODO c"]');
```

`todoLines("nothing here")` は空の配列を返す必要があります

```javascript
tryCatch(JSON.stringify(todoLines("nothing here")) === '[]');
```

`todoLines("Todo write tests")` はその行を返す必要があります

```javascript
tryCatch(JSON.stringify(todoLines("Todo write tests")) === '["Todo write tests"]');
```

`todoLines("a todo b")` は空の配列を返す必要があります。その行は todo で始まらないからです

```javascript
tryCatch(JSON.stringify(todoLines("a todo b")) === '[]');
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function todoLines(text) {
  return text.match(/^todo.*/gim) ?? [];
}
```
