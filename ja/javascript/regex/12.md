---
language: javascript
exerciseType: 2
---

# --description--

**`replace`** は、マッチを別のものに置き換えた新しい文字列を返します。元の文字列は決して変更されません。
```javascript
console.log("the cat sleeps".replace(/cat/, "dog"));
// prints the dog sleeps
```
置換文字列の中では、いくつかのシーケンスが特別な意味を持ちます:
- `$1`、`$2`、... グループ1、グループ2、... がキャプチャしたテキスト
- `$<name>` 名前付きグループがキャプチャしたテキスト
- `$&` マッチ全体

これこそが `replace` を、単なる置き換えではなく書き換えツールにしています:
```javascript
console.log("2026-09-12".replace(/(\d{4})-(\d{2})-(\d{2})/, "$3/$2/$1"));
// prints 12/09/2026
```
`g` フラグがないと、**最初の**マッチだけが置き換えられます。

# --instructions--

2つの単語がカンマとスペースで区切られて逆の順序で出力されるようにコードを完成させてください

# --seed--

```javascript
const name = "Ann Lee";
const swapped = name.replace([/], "[/]");
console.log(swapped);
```

# --answers--

- /(\w+) (\w+)/
- /\w+ \w+/
- $2, $1
- $1, $2
- \2, \1

# --solutions--

```javascript
const name = "Ann Lee";
const swapped = name.replace(/(\w+) (\w+)/, "$2, $1");
console.log(swapped);
```

# --output--

Lee, Ann
