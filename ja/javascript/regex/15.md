---
language: javascript
exerciseType: 2
---

# --description--

**`split`** は文字列を配列に切り分けます。普通の文字列を渡すとそのテキストそのもので切り分けますが、正規表現を渡すとパターンにマッチするたびに切り分けるので、1回の呼び出しで変化する区切り文字に対応できます:
```javascript
console.log("a, b;c".split(", "));
// prints [ 'a', 'b;c' ]
console.log("a, b;c".split(/[,;]\s*/));
// prints [ 'a', 'b', 'c' ]
```
区切り文字そのものは結果には含まれません。文字列の先頭や末尾にある区切り文字には注意してください: その側に空のフィールドがあるため、配列の中に空文字列ができます。

# --instructions--

3つのフィールドがスペースなしで縦棒でつなげられて出力されるようにコードを完成させてください

# --seed--

```javascript
const line = "a, b;c";
const fields = line.split([/]);
console.log(fields.join([/]));
```

# --answers--

- /[,;]\s*/
- /[,;]/
- /,/
- "|"
- ", "

# --solutions--

```javascript
const line = "a, b;c";
const fields = line.split(/[,;]\s*/);
console.log(fields.join("|"));
```

# --output--

a|b|c
