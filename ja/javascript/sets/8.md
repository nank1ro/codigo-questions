---
language: javascript
exerciseType: 2
---

# --description--

`delete(value)`は、値が削除された場合は`true`を、セットに存在しなかった場合は`false`を返します。
**すべて**の値を一度に削除するには、`clear()`を呼び出します：
```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

`cart`から`"pen"`を削除し、その削除結果を出力してから、セット全体を空にして残っている値の数を出力してください

# --seed--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Set(["pen", "ink"]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
