---
language: javascript
exerciseType: 2
---

# --description--

配列を`new Set()`に渡すことで、一度にセットを作ることができます。配列内の重複は取り除かれるため、これは配列の異なる値を見つける最も手っ取り早い方法です：
```javascript
let nums = [1, 2, 2, 3, 3, 3];
let distinct = new Set(nums);
console.log(distinct.size);
// prints 3
```
**スプレッド**演算子`...`はその逆で、セットを配列に戻します：
```javascript
let unique = [...distinct];
console.log(unique);
// prints [ 1, 2, 3 ]
```
`Array.from(distinct)`も同じことを行います。

# --instructions--

`nums`の異なる値を持つ配列`unique`を作成し、その要素数を出力してください

# --seed--

```javascript
let nums = [1, 2, 2, 3, 3, 3];
let unique = [...new Set([/])];
console.log(unique.[/]);
```

# --answers--

- nums
- [nums]
- ...nums
- length
- size
- count

# --solutions--

```javascript
let nums = [1, 2, 2, 3, 3, 3];
let unique = [...new Set(nums)];
console.log(unique.length);
```

# --output--

3
