---
language: javascript
exerciseType: 2
---

# --description--

**セット**は**ユニークな**値のコレクションです：各値は最大1回だけ現れることができ、位置で値にアクセスするためのインデックスはありません。
セットは、値が*何回*あるいは*どんな順序で*あるかではなく、*どの*値が存在するかだけを気にする場合に最適です。
`new Set()`で空のセットを作成し、`add(value)`で値を追加し、`has(value)`で値が存在するかどうかを確認します：
```javascript
let colors = new Set();
colors.add("red");
colors.add("blue");
console.log(colors.has("red"));
// prints true
console.log(colors.has("green"));
// prints false
```

# --instructions--

空のセット`colors`を作成し、値`"red"`を追加してから、セットに`"red"`が含まれているかどうかを出力してください

# --seed--

```javascript
let colors = new [/]();
colors.[/]("red");
console.log(colors.[/]("red"));
```

# --answers--

- Set
- add
- has
- Map
- push
- includes
- get

# --solutions--

```javascript
let colors = new Set();
colors.add("red");
console.log(colors.has("red"));
```

# --output--

true
