---
language: javascript
exerciseType: 2
---

# --description--

マップはペアが追加された順序を記憶しており、`for...of`でループできます。
`entries()`メソッドは各ペアを`[key, value]`の配列として返すので、ループの中でそのまま分割代入できます：
```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
// apple: 3 を出力
// pear: 5 を出力
```
マップを直接ループする`for (const [name, qty] of stock)`も、まったく同じことをします。

# --instructions--

`stock`のエントリをループし、それぞれの名前とその数量を出力してください

# --seed--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [/] of stock.[/]()) {
  console.log(`${name}: ${qty}`);
}
```

# --answers--

- [name, qty]
- [qty, name]
- name, qty
- entries
- pairs
- items

# --solutions--

```javascript
let stock = new Map();
stock.set("apple", 3);
stock.set("pear", 5);
for (const [name, qty] of stock.entries()) {
  console.log(`${name}: ${qty}`);
}
```

# --output--

apple: 3
pear: 5
