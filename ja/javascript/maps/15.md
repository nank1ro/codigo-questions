---
language: javascript
exerciseType: 3
---

# --description--

では、プレーンなオブジェクトの代わりに`Map`を使うべきなのはどんなときでしょうか？
- キーが実行時に追加・削除される場合、キーが文字列でない場合、または`size`と信頼できる順序が必要な場合は**Map**を使う
- `{ name, email }`のようにフィールド名が決まっている固定のレコードの場合や、データをJSONに変換する必要がある場合は**オブジェクト**を使う。`JSON.stringify()`はマップの中身を無視するため

# --instructions--

次のうち、プレーンなオブジェクトよりも`Map`のほうが適している状況はどれですか？

# --answers--

- ページの各ボタンのオブジェクト自体をキーとして使い、それぞれが何回クリックされたかを追跡する
- name と email というフィールドでユーザーを表現する
- 設定をJSONとしてサーバーに送信する
- 長方形の幅と高さを保存する

# --solutions--

- ページの各ボタンのオブジェクト自体をキーとして使い、それぞれが何回クリックされたかを追跡する
