---
language: javascript
exerciseType: 2
---

# --description--

`delete(key)`は、ペアが削除されたときは`true`を、キーが存在しなかったときは`false`を返します。
**すべての**ペアを一度に削除するには、`clear()`を呼び出します：
```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
// prints true
console.log(cart.delete("pen"));
// prints false
cart.clear();
console.log(cart.size);
// prints 0
```

# --instructions--

`cart`から`"pen"`を削除してその結果を出力し、マップ全体を空にしてから残っているペアの数を出力してください

# --seed--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.[/]("pen"));
cart.[/]();
console.log(cart.[/]);
```

# --answers--

- delete
- clear
- size
- remove
- removeAll
- length

# --solutions--

```javascript
let cart = new Map([["pen", 2], ["ink", 1]]);
console.log(cart.delete("pen"));
cart.clear();
console.log(cart.size);
```

# --output--

true
0
