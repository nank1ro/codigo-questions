---
language: javascript
exerciseType: 4
---

# --description--

JSDoc コメントの中の順序はいつも同じです。まず要約、次に宣言された順に各パラメータの `@param`、最後に `@returns` です。開始の `/**` と終了の ` */` が全体を囲み、コメントは説明対象の関数のすぐ上に置かれます。
```javascript
/**
 * Returns the number of seconds in the given minutes.
 * @param {number} minutes a whole number of minutes
 * @returns {number} minutes multiplied by sixty
 */
function toSeconds(minutes) {
  return minutes * 60;
}
```

# --instructions--

要約、`@param` の行、`@returns` の行の順で `twice` を記述し、それを関数のすぐ上に置き、関数を定義した後に `twice(21)` を出力するように項目を並べ替えてください

# --answers--

```javascript
/**
```

```javascript
 * Returns the double of a number.
```

```javascript
 * @param {number} n the number to double
```

```javascript
 * @returns {number} n multiplied by two
```

```javascript
 */
```

```javascript
function twice(n) {
  return n * 2;
}
```

```javascript
console.log(twice(21));
```

# --solutions--

```javascript
/**
 * Returns the double of a number.
 * @param {number} n the number to double
 * @returns {number} n multiplied by two
 */
function twice(n) {
  return n * 2;
}
console.log(twice(21));
```

# --output--

42
