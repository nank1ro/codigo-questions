---
language: javascript
exerciseType: 4
---

# --description--

各メンバーが複数のデータや独自のメソッドを必要とする場合は、**クラス**が列挙型の役割を果たすことができます。すべてのメンバーはそのクラスのインスタンスであり、`static`プロパティ、つまり各インスタンスではなくクラス自体に属するプロパティに格納されます。
```javascript
class Planet {
  static MERCURY = new Planet("Mercury", 0.4);
  static EARTH = new Planet("Earth", 1);

  constructor(name, gravity) {
    this.name = name;
    this.gravity = gravity;
  }
}
console.log(Planet.EARTH.name);
// prints Earth
```
クラスの後に`Object.freeze(Planet)`を呼び出すことで、誰もメンバーを追加したり置き換えたりできないようにし、コンストラクタの中で`Object.freeze(this)`を使って各インスタンスを凍結することで、メンバー自体を読み取り専用に保ちます。

# --instructions--

行を並べ替えて、クラスベースの列挙型`Coin`を宣言し、`DIME`メンバーのセント数を出力してください。
`PENNY`は`DIME`より前に、両方ともコンストラクタより前に宣言し、出力の前にクラスを凍結してください

# --answers--

```javascript
class Coin {
```

```javascript
  static PENNY = new Coin("penny", 1);
```

```javascript
  static DIME = new Coin("dime", 10);
```

```javascript
  constructor(name, cents) { this.name = name; this.cents = cents; }
```

```javascript
}
```

```javascript
Object.freeze(Coin);
```

```javascript
console.log(Coin.DIME.cents);
```

# --solutions--

```javascript
class Coin {
  static PENNY = new Coin("penny", 1);
  static DIME = new Coin("dime", 10);
  constructor(name, cents) { this.name = name; this.cents = cents; }
}
Object.freeze(Coin);
console.log(Coin.DIME.cents);
```

# --output--

10
