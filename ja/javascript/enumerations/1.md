---
language: javascript
exerciseType: 2
---

# --description--

**列挙型**（*enum*）とは、曜日、トランプのスート、注文の状態のように、関連する値の小さな固定グループのための共通の型です。
多くの言語と異なり、JavaScriptには`enum`キーワードが**存在しません**。慣用的な代替手段は、メンバーをプロパティとして持つ通常のオブジェクトを`Object.freeze()`に渡し、後から誰も変更できないようにすることです。
```javascript
const Color = Object.freeze({
  RED: "red",
  GREEN: "green",
  BLUE: "blue",
});
console.log(Color.RED);
// prints red
```
慣習として、このオブジェクトは`const`で宣言され、名前は大文字で始まり、メンバー名は他の定数と同じように`UPPER_CASE`で書かれます。

# --instructions--

`SPRING`と`SUMMER`というメンバーにそれぞれ`"spring"`と`"summer"`という文字列を持つ、凍結された列挙型`Season`を宣言し、`SUMMER`の値を出力してください

# --seed--

```javascript
const Season = Object.[/]({
  SPRING: "spring",
  [/]: "summer",
});
console.log(Season.[/]);
```

# --answers--

- freeze
- SUMMER
- SUMMER
- seal
- lock
- summer
- Summer

# --solutions--

```javascript
const Season = Object.freeze({
  SPRING: "spring",
  SUMMER: "summer",
});
console.log(Season.SUMMER);
```

# --output--

summer
