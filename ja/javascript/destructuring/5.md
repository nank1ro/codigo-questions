---
language: javascript
exerciseType: 1
---

# --description--

パターンは、`const` や `let` を手前に付けずに通常の代入の左側に置くこともでき、その場合はすでに存在する変数に書き込みます。これにより、2 つの値の入れ替えが一時変数なしの 1 行になります:
```javascript
let a = 1;
let b = 2;
[a, b] = [b, a];
console.log(a, b);
// 2 1 を出力
```
右辺が先に作られるので、代入が行われるときには両方の古い値がすでに一時配列の中に安全に収まっています。前の行のセミコロンに注意してください。そうしないと、`[` で始まる行は直前のものに対するインデックスとして読まれてしまいます。

# --instructions--

2 つの値を受け取り、小さい方を先頭にした 2 要素の配列を返す関数 `sortPair` を書いてください。順序が逆のときは分割代入で入れ替えてください

# --before-seed--

```javascript
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
var assert = require('assert')
const tryCatch = (...args) => {
  _testCount++
  try { assert(...args) }
  catch (e) {
    _testFailedCount++
    console.log(`Test Case '--err-t${_testCount}--' failed`);
  }
};
// DO NOT EDIT UNTIL HERE
```

# --seed--

```javascript
function sortPair(a, b) {
  
}
```

# --asserts--

`sortPair(5, 2)` は `2` で始まらなければなりません。

```javascript
tryCatch(sortPair(5, 2)[0] === 2);
```

`sortPair(5, 2)` は `5` で終わらなければなりません。

```javascript
tryCatch(sortPair(5, 2)[1] === 5);
```

すでにソートされたペアはそのままにしなければなりません。

```javascript
tryCatch(sortPair(1, 9).join(",") === "1,9");
```

等しい 2 つの値はそのままにしなければなりません。

```javascript
tryCatch(sortPair(4, 4).join(",") === "4,4");
```

負の値でも正しく動かなければなりません。

```javascript
tryCatch(sortPair(3, -8).join(",") === "-8,3");
```

# --after-asserts--

```javascript
// DO NOT EDIT FROM HERE 
console.log(`Executed ${_testCount} tests, with ${_testFailedCount} failures`);
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```javascript
function sortPair(a, b) {
  if (a > b) {
    [a, b] = [b, a];
  }
  return [a, b];
}
```
