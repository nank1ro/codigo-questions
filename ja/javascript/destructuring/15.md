---
language: javascript
exerciseType: 4
---

# --description--

`Object.entries(obj)` はオブジェクトを `[key, value]` ペアの配列に変換します。`for...of` ループのヘッドに配列パターンを置くと、ループが回るたびに各ペアが展開されます:
```javascript
const ages = { ada: 36, bob: 41 };
for (const [name, age] of Object.entries(ages)) {
    console.log(`${name} is ${age}`);
}
// ada is 36 を出力
// bob is 41 を出力
```
これはオブジェクトをたどる読みやすい方法です。インデックスもルックアップもなく、関心のある 2 つの名前だけがあります。`Object.keys` と `Object.values` はそれぞれ片側だけを返し、`Object.entries` は両方を返します。

# --instructions--

在庫にある果物の種類の数を出力し、その後でエントリごとに 1 行を出力するプログラムになるように項目を並べ替えてください

# --answers--

```javascript
const stock = { apples: 3, pears: 5 };
```

```javascript
console.log(`Stock (${Object.keys(stock).length}):`);
```

```javascript
for (const [item, count] of Object.entries(stock)) {
```

```javascript
  console.log(`${item}: ${count}`);
}
```

# --solutions--

```javascript
const stock = { apples: 3, pears: 5 };
console.log(`Stock (${Object.keys(stock).length}):`);
for (const [item, count] of Object.entries(stock)) {
  console.log(`${item}: ${count}`);
}
```

# --output--

Stock (2):
apples: 3
pears: 5
