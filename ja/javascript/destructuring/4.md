---
language: javascript
exerciseType: 2
---

# --description--

配列がパターンの期待どおりの長さとは限りません。名前の後に `= value` と書くと**デフォルト値**が与えられ、配列にその位置の要素がないときに使われます:
```javascript
const size = [1920];
const [width, height = 1080] = size;
console.log(width, height);
// prints 1920 1080
```
デフォルト値は必要なときにだけ評価されるので、関数呼び出しであってもかまいません。また、デフォルト値は最後の位置だけでなく任意の位置に付けられます。

# --instructions--

`fontSize` にデフォルト値 `14` を与え、出力 `dark 14` が得られるようにしてください

# --seed--

```javascript
const settings = ["dark"];
const [theme, fontSize [/] [/]] = settings;
console.log(`${theme} ${fontSize}`);
```

# --answers--

- =
- 14
- :
- 16
- ??
- ||

# --solutions--

```javascript
const settings = ["dark"];
const [theme, fontSize = 14] = settings;
console.log(`${theme} ${fontSize}`);
```

# --output--

dark 14
