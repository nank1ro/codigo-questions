---
language: javascript
exerciseType: 4
---

# --description--

関数宣言では、パターンをパラメータ名の代わりに置けます。そのため、呼び出しのときに展開が行われます:
```javascript
function area({ width, height }) {
    return width * height;
}
console.log(area({ width: 4, height: 3 }));
// prints 12
```
本体の中にはオブジェクト変数はまったくなく、`width` と `height` だけがあります。呼び出し側は 1 つのオブジェクトを渡しますが、シグネチャは関数がどのキーを読むかを正確に文書化し、キーはどんな順序で到達してもかまいません。

# --instructions--

関数を先に定義し、その後で長方形の面積を出力するプログラムになるように項目を並べ替えてください

# --answers--

```javascript
function area({ width, height }) {
```

```javascript
  return width * height;
}
```

```javascript
const rect = { width: 4, height: 3 };
```

```javascript
console.log(area(rect));
```

# --solutions--

```javascript
function area({ width, height }) {
  return width * height;
}
const rect = { width: 4, height: 3 };
console.log(area(rect));
```

# --output--

12
