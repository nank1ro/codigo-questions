---
language: javascript
exerciseType: 2
---

# --description--

オブジェクトも、角括弧の代わりに波括弧を使って分割代入できます。ここでは位置は何の意味も持ちません。各名前は、同じつづりの**キー**と照合されます:
```javascript
const user = { name: "Ada", age: 36 };
const { age, name } = user;
console.log(name, age);
// Ada 36 を出力
```
パターン内で `age` と `name` を入れ替えても結果は変わらず、パターンが言及しないキーは単にそのまま残されます。対応するキーのない名前は `undefined` になります。

# --instructions--

プログラムが `Dune (1965) by Herbert` を出力するように 2 つのパターンを完成させてください

# --seed--

```javascript
const book = { title: "Dune", author: "Herbert", year: 1965 };
const { title, [/] } = book;
const { author } = [/];
console.log(`${title} (${year}) by ${author}`);
```

# --answers--

- year
- book
- title
- published
- novel
- 1965

# --solutions--

```javascript
const book = { title: "Dune", author: "Herbert", year: 1965 };
const { title, year } = book;
const { author } = book;
console.log(`${title} (${year}) by ${author}`);
```

# --output--

Dune (1965) by Herbert
