---
language: javascript
exerciseType: 2
---

# --description--

すでに学んだように、`extends` を使うとあるクラスが別のクラスの子になります。このキーワードが実際に与えてくれるのは**継承**です。子クラスは親のすべてのプロパティとメソッドを無料で受け取り、その上に自分のものを追加できます。

継承を便利にする鍵となるのが **`super`** です。子のコンストラクタ内で `super(...)` と書くと親のコンストラクタが呼ばれ、親は自分が担当する部分のオブジェクトをセットアップできます：
```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
```
ここでは `super(name)` が `name` を `Animal` に渡して格納させ、`Dog` は `breed` だけを考えればよいことになります。

# --instructions--

`Dog` が `Animal` を継承し、`name` を親のコンストラクタに渡すようにコードを完成させてください

# --seed--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog [/] Animal {
    constructor(name, breed) {
        [/](name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --answers--

- extends
- inherits
- super
- parent
- new Animal

# --solutions--

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }
}
class Dog extends Animal {
    constructor(name, breed) {
        super(name);
        this.breed = breed;
    }
}
const max = new Dog("Max", "Beagle");
console.log(`${max.name} is a ${max.breed}`);
```

# --output--

Max is a Beagle
