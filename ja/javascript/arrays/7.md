---
language: javascript
exerciseType: 2
---

# --description--

JavaScriptでは配列を自由にスライスできます！
```javascript
// 最初の2つの要素を取得
listName.slice(0, 2);
// 4番目から最後までの要素を取得
listName.slice(3);
```
配列のスライスに最初の要素や最後の要素が含まれる場合、その要素のインデックスを含める必要はありません

# --instructions--

`animals`を2つのグループにちょうど半分に分けてください

# --seed--

```javascript
let animals = ["dog", "cat", "frog", "bear", "lion", "butterfly"];
let firstGroup = [/].[/]([/], [/]);
let secondGroup = [/].slice([/]);
console.log(firstGroup);
console.log(secondGroup);
```

# --answers--

- 0
- 3
- 4
- 1
- 3
- ..<
- ...
- animals
- animals
- slice

# --solutions--

```javascript
let animals = ["dog", "cat", "frog", "bear", "lion", "butterfly"];
let firstGroup = animals.slice(0, 3);
let secondGroup = animals.slice(3);
console.log(firstGroup);
console.log(secondGroup);
```

# --output--

["dog", "cat", "frog"]
["bear", "lion", "butterfly"]
