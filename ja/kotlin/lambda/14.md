---
language: kotlin
exerciseType: 4
---

# --description--

**匿名関数**は `fun` で宣言された名前のない関数です。関数の値を作るもう1つの方法です:
```kotlin
val square = fun(x: Int): Int {
    return x * x
}
println(square(4)) // 16
```
ラムダとは異なり、戻り値の型を明示的に宣言でき、値を生成するために `return` を使います。
匿名関数とラムダは互いに置き換えられます。どちらも `map`、`filter`、関数型を受け取る任意の関数に渡せます。

# --instructions--

匿名関数 `square` で `numbers` を map して `[1, 4, 9]` を出力できるように、これらの行を並べ替えられますか？

# --answers--

```kotlin
val square = fun(x: Int): Int {
    return x * x
}
```

```kotlin
val numbers = listOf(1, 2, 3)
```

```kotlin
println(numbers.map(square))
```

# --solutions--

```kotlin
val square = fun(x: Int): Int {
    return x * x
}
val numbers = listOf(1, 2, 3)
println(numbers.map(square))
```

```kotlin
val numbers = listOf(1, 2, 3)
val square = fun(x: Int): Int {
    return x * x
}
println(numbers.map(square))
```

# --output--

[1, 4, 9]
