---
language: kotlin
exerciseType: 4
---

# --description--

拡張関数は**null許容のレシーバ**に対して宣言できるため、`null`の値に対しても呼び出すことができます。関数内では`this`はnull許容であり、チェックが必要です：
```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val none: String? = null
println(none.orDash()) // -
```
呼び出し箇所では`?.`が不要なことに注意してください：関数自体が`null`の場合を処理します。
標準ライブラリは、任意の`String?`に対して安全に呼び出せる`isNullOrEmpty()`や`orEmpty()`でこのテクニックを使用しています。

# --instructions--

拡張関数`orDash`を定義し、存在しない名前に対しては`-`を、存在する名前に対しては`kotlin`を出力するように、これらの行を並べ替えられますか？ 関数を先に定義してください

# --answers--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
```

```kotlin
val a: String? = null
val b: String? = "kotlin"
```

```kotlin
println(a.orDash())
```

```kotlin
println(b.orDash())
```

# --solutions--

```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val a: String? = null
val b: String? = "kotlin"
println(a.orDash())
println(b.orDash())
```

# --output--

-
kotlin
