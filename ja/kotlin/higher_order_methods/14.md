---
language: kotlin
exerciseType: 2
---

# --description--

チェーンの中では `it` の型が段階ごとに変わります。`List<String>` に `filter` をかけたあとはまだ文字列ですが、`map { it.length }` のあとは `List<Int>` になるので、次のラムダには数値が渡されます。
```kotlin
val words = listOf("kiwi", "fig")
println(words.map { it.length }.filter { it > 3 }) // [4]
```
どの段階も**新しい**リストを返し、前のリストには決して手を触れません。ですからチェーンは、結果を変えずに名前付きの中間の値へ分割できます。

# --instructions--

`result` が `a` で始まる果物を大文字にして持ち、プログラムが `[APPLE, AVOCADO]` を出力するようにコードを完成させてください

# --seed--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.[/] { it.startsWith("a") }.[/] { it.[/] }
println(result)
```

# --answers--

- filter
- map
- uppercase()
- forEach
- count
- lowercase()
- length

# --solutions--

```kotlin
val fruits = listOf("apple", "kiwi", "avocado", "plum")
val result = fruits.filter { it.startsWith("a") }.map { it.uppercase() }
println(result)
```

# --output--

[APPLE, AVOCADO]
