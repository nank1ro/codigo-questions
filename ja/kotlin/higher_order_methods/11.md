---
language: kotlin
exerciseType: 1
---

# --description--

`zip` は 2 つのリストの要素を位置ごとに組み合わせます。ラムダなしなら `Pair` の値のリストを返し、その両側は `.first` と `.second` で読み取ります。ラムダを渡すと、各位置の 2 つの要素がラムダに渡され、その結果がリストにまとめられます。
```kotlin
val names = listOf("Ann", "Bob")
val ages = listOf(31, 25)
println(names.zip(ages))                            // [(Ann, 31), (Bob, 25)]
println(names.zip(ages) { name, age -> "$name:$age" }) // [Ann:31, Bob:25]
```
結果の長さは 2 つのリストのうち**短いほう**に合わせられ、長いほうの余った要素は無視されます。

# --instructions--

`zip` を使って `a` と `b` の要素を位置ごとに掛け合わせ、その積の合計を返す関数 `dotProduct(a: List<Int>, b: List<Int>): Int` を書いてください。長いほうのリストの余った要素は無視されます。

例: `dotProduct(listOf(1, 2, 3), listOf(4, 5, 6))` は `32`、つまり `1*4 + 2*5 + 3*6` を返します。

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`dotProduct(listOf(1, 2, 3), listOf(4, 5, 6))` は `32` を返さなければなりません

```kotlin
    tryCatch(dotProduct(listOf(1, 2, 3), listOf(4, 5, 6)) == 32)
```

`dotProduct(listOf(1, 2, 3), listOf(10, 10))` は余った `3` を無視して `30` を返さなければなりません

```kotlin
    tryCatch(dotProduct(listOf(1, 2, 3), listOf(10, 10)) == 30)
```

`dotProduct(listOf(2), listOf(-3, 100))` は `-6` を返さなければなりません

```kotlin
    tryCatch(dotProduct(listOf(2), listOf(-3, 100)) == -6)
```

`dotProduct(listOf(), listOf(1, 2))` は `0` を返さなければなりません

```kotlin
    tryCatch(dotProduct(listOf(), listOf(1, 2)) == 0)
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun dotProduct(a: List<Int>, b: List<Int>): Int {
    return a.zip(b) { x, y -> x * y }.sum()
}
```
