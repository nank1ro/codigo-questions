---
language: kotlin
exerciseType: 2
---

# --description--

このコードスニペットを分析しましょう。
```kotlin
var counter = 0 // [1]
while (counter < 100) { // [2]
    counter += 10 // [3]
    println(counter)
}
```
- __[1]__: `counter` 変数を __0__ で初期化します。
- __[2]__: _while_ の条件式は「counter が _100_ より小さい限り、本体の文を繰り返す」ことを意味します。
- __[3]__: `+=` 演算子は `counter` に _10_ を加え、その結果を一つの操作で `counter` に代入します。

上記のコードの出力は _10_, _20_, _30_, _40_, _50_, _60_, _70_, _80_, _90_, _100_ です。

# --instructions--

_5_ の最初の5つの倍数を表示するために、空欄を `while` ループで埋めてください。

# --seed--

```kotlin
var counter = 1
[/] (counter[/]5) {
    println(counter[/]5)
    counter[/]
}
```

# --answers--

- while
- "while"
- loop
-  <= 
-  < 
- for
-  * 
-  ** 
- ++


# --solutions--

```kotlin
var counter = 1
while (counter <= 5) {
    println(counter * 5)
    counter++
}
```

# --output--

5
10
15
20
25
