---
language: kotlin
exerciseType: 2
---

# --description--

> コンピュータは繰り返し作業に最適です。

最も基本的な繰り返しの形式は `while` キーワードを使います。
これは制御する_ブール式_が真である限り、ブロックを繰り返します:

```kotlin
while (Boolean-expression) {
  // Code to be repeated
}
```
ブール式はループの最初に一度評価され、
ブロックを通る各反復の前に再度評価されます。

```kotlin
var x = 3
while (x > 0) {
    println(x)
    x--
}
```
ここでは変数 `x` を作成し、初期値として __3__ を代入しています。

次に、条件 `x > 0` が `true` である限りコードブロックを実行する `while` 文を使用しています。

コードブロックの中で、`x--` の行を追加することを**忘れてはいけません**。
これは `x` の値を減少させます。そうしないと、ループは無限になります。

# --instructions--

空欄を `while` ループで埋めて、コードブロックを繰り返してください。

> これが無限ループであっても心配しないでください

# --seed--

```kotlin
[/] (true) {
    println("repeat")
}
```

# --answers--

- while
- "while"
- for
- loop

# --solutions--

```kotlin
while (true) {
    println("repeat")
}
```

# --output--

repeat
repeat
repeat
repeat
...
