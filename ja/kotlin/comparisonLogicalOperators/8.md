---
language: kotlin
exerciseType: 2
---

# --description--

**or** `||` 論理演算子を見ていきましょう。
この演算子は、*true* と評価される最初のオペランドを返し、すべてが *false* の場合は最後のオペランドを返します。
```kotlin
println(2 == 2 || 2 == 3) // true
println(1 == 2 || 1 == 3) // false
```

# --instructions--

`num` が `5` と等しい**または** `num2` が `3` と等しくないかどうかを確認するブール値を出力してください（この順序で）

# --seed--

```kotlin
var num = 5
var num2 = 4
println([/][/][/][/][/][/][/])
```

# --answers--

- num 
- num2 
- == 
- != 
- 5
-  and 
- != 
-  || 
- 3

# --solutions--

```kotlin
var num = 5
var num2 = 4
println(num == 5 || num2 != 3)
```

# --output--

true
