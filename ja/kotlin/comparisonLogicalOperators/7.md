---
language: kotlin
exerciseType: 2
---

# --description--

次に**論理**演算子を見ていきましょう。最初は __AND__ `&&` です。
この演算子は、*false* と評価される最初のオペランドを返し、すべてが *true* の場合は最後のオペランドを返します。
```kotlin
println(2 == 2 && 2 == 3) // false
println(1 == 1 && 1.0 == 1.0) // true
```

# --instructions--

`num` と `num2` の両方が 5 と等しいかどうかを確認するブール値を出力してください（この順序で）

# --seed--

```kotlin
var num = 5
var num2 = 5
println([/][/][/][/][/][/][/])
```

# --answers--

- num 
- num2 
- == 
- == 
- 5
-  && 
- != 
- 5

# --solutions--

```kotlin
var num = 5
var num2 = 5
println(num == 5 && num2 == 5)
```

# --output--

true
