---
language: kotlin
exerciseType: 2
---

# --instructions--

変数`isOpen`を正しく出力してください

# --seed--

```kotlin
var openingHour = 9
var closingHour = 18
var currentHour = 12
var isOpen = currentHour[/]openingHour[/]currentHour <= closingHour
println(isOpen)
```

# --answers--

-  && 
-  and 
-  or 
-  || 
-  >= 
-  <= 

# --solutions--

```kotlin
var openingHour = 9
var closingHour = 18
var currentHour = 12
var isOpen = currentHour >= openingHour && currentHour <= closingHour
println(isOpen)
```

# --output--

true

