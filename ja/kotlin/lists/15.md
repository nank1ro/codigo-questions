---
language: kotlin
exerciseType: 3
---

# --instructions--

リストが空かどうかを確認するにはどうすればよいですか？

# --answers--

- if (listName.size == 0)
- if (listName == 0)
- if (listName.length == 0)
- if (listName[0] == false)
- if (listName.isEmpty())

# --solutions--

- if (listName.size == 0)
- if (listName.isEmpty())
