---
language: kotlin
exerciseType: 2
---

# --instructions--

`numbers`リストにさらに3つのアイテムを次の順序で追加するためにコードを完成させてください：`2`、次に`7`、最後に`9`

# --seed--

```kotlin
val numbers = mutableListOf(12, 8, 2)
val newNumbers = listOf([/], [/], [/])
numbers.[/](newNumbers)
println(numbers)
```

# --answers--

- append
- appendAll
- add
- 2
- 7
- 9
- numbers
- newNumbers
- addAll

# --solutions--

```kotlin
val numbers = mutableListOf(12, 8, 2)
val newNumbers = listOf(2, 7, 9)
numbers.addAll(newNumbers)
println(numbers)
```

# --output--

[12, 8, 2, 2, 7, 9]
