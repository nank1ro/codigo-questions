---
language: kotlin
exerciseType: 4
---

# --instructions--

`Slow down`を出力するように項目を並べ替えてください。まずenumを宣言し、次に`action`関数、最後に`main`を配置し、`when`の各分岐はenumエントリと同じ順序にしてください。

# --answers--

```kotlin
enum class Light {
    RED, YELLOW, GREEN
}
```

```kotlin
fun action(light: Light): String = when (light) {
```

```kotlin
    Light.RED -> "Stop"
```

```kotlin
    Light.YELLOW -> "Slow down"
```

```kotlin
    Light.GREEN -> "Go"
```

```kotlin
}
```

```kotlin
fun main() {
    println(action(Light.YELLOW))
}
```

# --solutions--

```kotlin
enum class Light {
    RED, YELLOW, GREEN
}
fun action(light: Light): String = when (light) {
    Light.RED -> "Stop"
    Light.YELLOW -> "Slow down"
    Light.GREEN -> "Go"
}
fun main() {
    println(action(Light.YELLOW))
}
```

# --output--

Slow down
