---
language: kotlin
exerciseType: 3
---

# --description--

Javaではブロックコメントの中に別のブロックコメントを含められませんが、Kotlinでは**ネストできます**。それぞれの `/*` には対応する独自の `*/` が必要で、コメントは最も外側のものが閉じられたときにだけ終わります。
```kotlin
/* outer /* inner */ still a comment */
println("done")
```
ここで `still a comment */` は外側のコメントの一部なので、出力されるのは `done` だけです。この仕組みのおかげで、すでに `/* */` コメントを含んでいるコードブロック全体でもコメントアウトできます。

# --instructions--

このコードは何を出力しますか？
```kotlin
/* start
   /* nested */
   println("hidden")
*/
println("done")
```

# --answers--

- done
- `hidden` のあとに `done`
- コンパイルエラーになる:最初の `*/` でコメントが終わってしまう
- 何も出力されない

# --solutions--

- done
