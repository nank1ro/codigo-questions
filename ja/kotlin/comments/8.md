---
language: kotlin
exerciseType: 4
---

# --description--

ブロックコメントの一般的な使い方の1つが**ヘッダーコメント**です。関数の直上に置かれる短いブロックで、その関数が何をするのか、パラメーターが何を意味するのかを説明します。
```kotlin
/*
Returns the number of seconds in the given minutes.
minutes is a whole number, never negative.
*/
fun toSeconds(minutes: Int): Int {
    return minutes * 60
}
```
`toSeconds` を呼び出す人は、本体を読む代わりにヘッダーを読めます。ヘッダーは関数の隣に置き、一緒に更新するようにしてください。

# --instructions--

これらの項目を並べ替えて、`area` を定義し、その直上に置かれたヘッダーコメントでドキュメントした上で、`area(3, 4)` を出力するプログラムを組み立ててください

# --answers--

```kotlin
/*
```

```kotlin
Returns the area of a rectangle.
```

```kotlin
*/
```

```kotlin
fun area(w: Int, h: Int): Int {
    return w * h
}
```

```kotlin
println(area(3, 4))
```

# --solutions--

```kotlin
/*
Returns the area of a rectangle.
*/
fun area(w: Int, h: Int): Int {
    return w * h
}
println(area(3, 4))
```

# --output--

12
