---
language: kotlin
exerciseType: 4
---

# --description--

要約の後に、ドキュメンテーションコメントは**KDocタグ**でパラメーターと戻り値を説明できます。KDocタグは常に `@` で始まります:
```kotlin
/**
 * Returns the number of seconds in the given minutes.
 * @param minutes a whole number of minutes, never negative
 * @return [minutes] multiplied by sixty
 */
fun toSeconds(minutes: Int): Int {
    return minutes * 60
}
```
`@param` の後にはパラメーターの名前が続き、その後に説明が来ます。パラメーターごとに `@param` が1つあります。`@return` は関数が返す値を説明します。順序は常に同じです。まず要約、次に `@param` タグ、そして `@return` です。

# --instructions--

これらの項目を並べ替えて、関数の直上に置いた1つのドキュメンテーションコメントの中で、要約、`@param` タグ、`@return` タグの順に `twice` をドキュメントし、その後 `twice(21)` を出力してください

# --answers--

```kotlin
/**
 * Returns the double of a number.
```

```kotlin
 * @param n the number to double
```

```kotlin
 * @return [n] multiplied by two
```

```kotlin
 */
```

```kotlin
fun twice(n: Int): Int {
    return n * 2
}
```

```kotlin
println(twice(21))
```

# --solutions--

```kotlin
/**
 * Returns the double of a number.
 * @param n the number to double
 * @return [n] multiplied by two
 */
fun twice(n: Int): Int {
    return n * 2
}
println(twice(21))
```

# --output--

42
