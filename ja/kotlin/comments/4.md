---
language: kotlin
exerciseType: 2
---

# --description--

コメントが複数行にわたるときは、Kotlinの**複数行コメント**（ブロックコメントとも呼ばれます）が使えます。`/*`で始まり `*/` で終わり、間にあるものは改行も含めてすべて無視されます。
```kotlin
/*
  Prints the welcome banner.
  Called once when the app starts.
*/
println("Welcome!")
```
ブロックコメントは短くして1行に収めることもできます: `/* like this */`

# --instructions--

2行のテキストが複数行コメントになり、プログラムが `5` を出力するようにコードを完成させてください

# --seed--

```kotlin
[/]
  Prints the sum
  of two numbers
[/]
println(2 + 3)
```

# --answers--

- /*
- */
- //
- *
- /

# --solutions--

```kotlin
/*
  Prints the sum
  of two numbers
*/
println(2 + 3)
```

# --output--

5
