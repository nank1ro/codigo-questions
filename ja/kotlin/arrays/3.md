---
language: kotlin
exerciseType: 2
---

# --description--

すべての配列には、インデックスを扱うための便利なプロパティが2つあります：
- `indices`は有効なインデックスの範囲で、`0`から最後のインデックスまでです
- `lastIndex`は最後の要素のインデックス、つまり`size - 1`です
```kotlin
val letters = arrayOf("x", "y")
println(letters.lastIndex) // 1
for (i in letters.indices) {
    println(letters[i])
}
// prints x, then y
```

# --instructions--

`letters`の最後のインデックスを出力し、続けて各インデックスとその要素を出力するようにコードを完成させてください

# --seed--

```kotlin
val letters = arrayOf("a", "b", "c")
println(letters.[/])
for (i in letters.[/]) {
    println("$i: ${letters[i]}")
}
```

# --answers--

- lastIndex
- indices
- size
- length
- last

# --solutions--

```kotlin
val letters = arrayOf("a", "b", "c")
println(letters.lastIndex)
for (i in letters.indices) {
    println("$i: ${letters[i]}")
}
```

# --output--

2
0: a
1: b
2: c
