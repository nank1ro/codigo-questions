---
language: kotlin
exerciseType: 2
---

# --description--

`finally` ブロックは最後に追加できます。これは**何が起ころうとも**実行されます。`try` が成功したあとでも、`catch` が回復したあとでも、例外がまったくキャッチされなかったときでさえもです。

```kotlin
try {
    println("reading")
    println("abc".toInt())
} catch (e: NumberFormatException) {
    println("bad number")
} finally {
    println("closing")
}
```
```
reading
bad number
closing
```
そのため、ファイルを閉じるといったスキップできない後片付けの場所として適しています。`try` には `catch` か `finally` の少なくとも一方が必要ですが、両方を持つこともできます。

# --instructions--

不正な変換がキャッチされ、後片付けの行が必ず実行されるようにコードを完成させてください。

# --seed--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } [/] (e: NumberFormatException) {
        println("bad number")
    } [/] {
        println("closing")
    }
}
```

# --answers--

- catch
- finally
- try
- throw
- else
- do

# --solutions--

```kotlin
fun main() {
    try {
        println("abc".toInt())
    } catch (e: NumberFormatException) {
        println("bad number")
    } finally {
        println("closing")
    }
}
```

# --output--

bad number
closing
