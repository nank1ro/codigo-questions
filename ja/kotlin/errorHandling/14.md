---
language: kotlin
exerciseType: 4
---

# --description--

`Result` は普通の値なので、`val` に格納していつでも好きなだけ問い合わせることができます:

```kotlin
val result = runCatching { "abc".toInt() }

println(result.getOrElse { 0 })  // 0
println(result.getOrNull())      // null
println(result.isSuccess)        // false
```
`try`/`catch` にはこれができません。そこでは結果は失敗が起きたその場で一度だけ処理され、その後は消えてしまいます。`Result` は失敗を保持し続けるので、それに反応するコードは失敗が起きた場所にある必要がありません。

# --instructions--

変換の結果を格納し、そのあとで回復した値、生の結果、そして試行が成功したかどうかを出力するように、項目を並べてください。

# --answers--

```kotlin
val result = runCatching { "abc".toInt() }
```

```kotlin
val value = result.getOrElse { 0 }
```

```kotlin
println("value: $value")
```

```kotlin
println("raw: ${result.getOrNull()}")
```

```kotlin
println("success: ${result.isSuccess}")
```

# --solutions--

```kotlin
val result = runCatching { "abc".toInt() }
val value = result.getOrElse { 0 }
println("value: $value")
println("raw: ${result.getOrNull()}")
println("success: ${result.isSuccess}")
```

# --output--

value: 0
raw: null
success: false
