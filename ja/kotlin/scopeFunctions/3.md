---
language: kotlin
exerciseType: 3
---

# --description--

`let` はセーフコールと組み合わせると真に役立ちます。`?.let { ... }` は値が `null` でないときに**だけ**ブロックを実行し、ブロックの中の `it` は非nullの値になるので、追加のチェックは不要です:
```kotlin
val email: String? = "ada@example.com"
email?.let { println("Sending to ${it.uppercase()}") }
```
値が `null` のときは式全体が `null` になり、ブロックは決して実行されません。そのため、フォールバックを用意するにはElvis演算子 `?:` が自然な相棒になります:
```kotlin
fun label(city: String?): String {
    return city?.let { "City: $it" } ?: "No city"
}
```

# --instructions--

このプログラムはどうなりますか？

```kotlin
fun main() {
    val city: String? = null
    val result = city?.let { "City: $it" } ?: "No city"
    println(result)
}
```

# --answers--

- `No city` と出力される。`city` が `null` のとき `let` ブロックはスキップされるため
- `City: null` と出力される。ブロックの中で `it` が `null` になるため
- `null` と出力される。`let` は `null` を返すため
- 何も出力されない：コンパイルされない。`let` はnull許容の値には使えないため

# --solutions--

- `No city` と出力される。`city` が `null` のとき `let` ブロックはスキップされるため
