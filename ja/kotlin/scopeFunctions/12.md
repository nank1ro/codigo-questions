---
language: kotlin
exerciseType: 4
---

# --instructions--

新しい要素が追加されたあとのリストを出力し、そのあとにそのサイズを出力するように、これらの項目を並べ替えてください。

# --answers--

```kotlin
val words = mutableListOf("fig")
```

```kotlin
val count = words
```

```kotlin
    .apply { add("kiwi") }
```

```kotlin
    .also { println("now: $it") }
```

```kotlin
    .size
```

```kotlin
println("size: $count")
```

# --solutions--

```kotlin
val words = mutableListOf("fig")
val count = words
    .apply { add("kiwi") }
    .also { println("now: $it") }
    .size
println("size: $count")
```

# --output--

now: [fig, kiwi]
size: 2
