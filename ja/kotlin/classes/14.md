---
language: kotlin
exerciseType: 4
---

# --instructions--

`data class` `Student`を定義してその`toString()`を出力するために、これらの行を正しい順序に並べられますか？

# --answers--

```kotlin
data class Student(val name: String, val grade: Int)
```

```kotlin
fun main() {
```

```kotlin
    val s = Student("Alice", 90)
```

```kotlin
    println(s.toString())
```

```kotlin
}
```

# --solutions--

```kotlin
data class Student(val name: String, val grade: Int)
fun main() {
    val s = Student("Alice", 90)
    println(s.toString())
}
```

# --output--

Student(name=Alice, grade=90)
