---
language: kotlin
exerciseType: 2
---

# --instructions--

`Cat`が`Animal`を継承し、`speak()`メソッドをオーバーライドして`"Meow"`を返すようにするコードを完成させてください。

# --seed--

```kotlin
open class Animal(val name: String) {
    open fun speak(): String = "..."
}

class Cat(name: String) [/] Animal(name) {
    [/] fun speak(): String = "Meow"
}
```

# --answers--

- :
- override
- extends
- open
- super
- implements

# --solutions--

```kotlin
open class Animal(val name: String) {
    open fun speak(): String = "..."
}

class Cat(name: String) : Animal(name) {
    override fun speak(): String = "Meow"
}
```
