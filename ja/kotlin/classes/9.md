---
language: kotlin
exerciseType: 2
---

# --instructions--

`Int`型の`val`プロパティ`red`、`green`、`blue`を持つ`Color`という名前の`data class`を宣言するコードを完成させてください。

# --seed--

```kotlin
[/] [/] Color(val red: Int, val green: Int, val blue: Int)
```

# --answers--

- data
- class
- object
- open
- abstract
- sealed

# --solutions--

```kotlin
data class Color(val red: Int, val green: Int, val blue: Int)
```
