---
language: kotlin
exerciseType: 2
---

# --description--

単一式関数はブロック本体の代わりに `=` を使います:
```kotlin
fun triple(n: Int) = n * 3
```
これは以下の省略形です:
```kotlin
fun triple(n: Int): Int {
    return n * 3
}
```

# --instructions--

2つの整数を加算する単一式関数を完成させてください。

# --seed--

```kotlin
fun add(a: Int, b: Int) [/] a [/] b
fun main() {
    println(add(3, 4))
}
```

# --answers--

- =
- +
- -
- *
- return
- ->

# --solutions--

```kotlin
fun add(a: Int, b: Int) = a + b
fun main() {
    println(add(3, 4))
}
```

# --output--

7
