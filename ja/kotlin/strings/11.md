---
language: kotlin
exerciseType: 1
---

# --description--

`repeat(n)`は文字列を`n`回連結したものを返します:
```kotlin
println("ab".repeat(3)) // ababab
```
`padStart(width, char)`は、文字列が`width`文字になるまで先頭に`char`を追加します。`padEnd`は末尾に追加します。
文字列がすでに十分な長さであれば、変更されずにそのまま返されます:
```kotlin
println("7".padStart(3, '0'))   // 007
println("hi".padEnd(5, '.'))    // hi...
println("1234".padStart(2, '0')) // 1234
```
数値は文字列ではないので、`42.toString().padStart(4, '0')`のように、まず`toString()`を呼び出してください。

# --instructions--

`n`を文字列として返し、`width`文字になるまで先頭に`'0'`を詰める関数`padNumber(n: Int, width: Int): String`を書いてください

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
fun main() {
```

# --asserts--

`padNumber(7, 3)`は`"007"`を返さなければなりません

```kotlin
    tryCatch(padNumber(7, 3) == "007")
```

`padNumber(42, 5)`は`"00042"`を返さなければなりません

```kotlin
    tryCatch(padNumber(42, 5) == "00042")
```

`padNumber(1234, 2)`は`"1234"`を返さなければなりません

```kotlin
    tryCatch(padNumber(1234, 2) == "1234")
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
fun padNumber(n: Int, width: Int): String {
    return n.toString().padStart(width, '0')
}
```
