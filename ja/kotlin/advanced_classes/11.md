---
language: kotlin
exerciseType: 1
---

# --description--

`interface`は型が何をできるかを列挙します。そのメンバーはデフォルトで抽象ですが、インターフェースは**デフォルト実装**も提供できます。デフォルト実装とは、実装するすべてのクラスが無料で継承し、オーバーライドしてもよい本体のことです：

```kotlin
interface Greeter {
    val name: String              // abstract、クラス側で実装する必要がある
    fun greet(): String = "Hi, $name"  // デフォルト実装
}

class Person(override val name: String) : Greeter

class Robot(override val name: String) : Greeter {
    override fun greet(): String = "BEEP $name"
}

println(Person("Ann").greet()) // Hi, Ann
println(Robot("R2").greet())   // BEEP R2
```

インターフェースは状態を保存できない（バッキングフィールドを持たない）ため、抽象プロパティはクラスによって実装されなければならず、通常はコンストラクタで`override val`を使います。クラスとは異なり、型は好きなだけ多くのインターフェースを実装できます。

# --instructions--

`String`型の抽象`val`プロパティ`name`と、`name`が`"Ann"`のときに`"Hello, Ann!"`を返すデフォルト実装を持つメソッド`greet(): String`を備えたインターフェース`Greeter`を宣言してください。

その後、`Greeter`を実装しコンストラクタで`name`を受け取るクラス`Person`と、同じく`Greeter`を実装するが`greet()`をオーバーライドして`name`が`"Bo"`のときに`"Ahoy, Bo!"`を返すクラス`Pirate`を宣言してください。

# --before-seed--

```kotlin
// DO NOT EDIT FROM HERE
var _testFailedCount = 0;
var _testCount = 0;
fun tryCatch(assertion: Boolean) {
  _testCount++
    try { 
        if (!assertion) throw Exception()
    }
    catch (e: Throwable) {
        _testFailedCount++
        println("Test Case '--err-t$_testCount--' failed");
  }
};
// DO NOT EDIT UNTIL HERE
```

# --before-asserts--

```kotlin
fun main() {
```

# --asserts--

`Person("Ann").greet()`は`"Hello, Ann!"`を返さなければならない

```kotlin
    tryCatch(Person("Ann").greet() == "Hello, Ann!")
```

`Person("Cy").name`は`"Cy"`を返さなければならない

```kotlin
    tryCatch(Person("Cy").name == "Cy")
```

`Pirate("Bo").greet()`は`"Ahoy, Bo!"`を返さなければならない

```kotlin
    tryCatch(Pirate("Bo").greet() == "Ahoy, Bo!")
```

両方のクラスは`Greeter`として使用できなければならない

```kotlin
    val greeters: List<Greeter> = listOf(Person("Ann"), Pirate("Bo"))
    tryCatch(greeters.map { it.greet() } == listOf("Hello, Ann!", "Ahoy, Bo!"))
```

# --after-asserts--

```kotlin
// DO NOT EDIT FROM HERE 
    println("Executed $_testCount tests, with $_testFailedCount failures");
}
// DO NOT EDIT UNTIL HERE
```

# --solutions--

```kotlin
interface Greeter {
    val name: String
    fun greet(): String = "Hello, $name!"
}

class Person(override val name: String) : Greeter

class Pirate(override val name: String) : Greeter {
    override fun greet(): String = "Ahoy, $name!"
}
```
