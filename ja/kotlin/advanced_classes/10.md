---
language: kotlin
exerciseType: 2
---

# --instructions--

`ofKilometres`がキロメートルから`Distance`を作成する`Distance`のファクトリ関数になるようにコードを完成させてください。

# --seed--

```kotlin
class Distance private constructor(val meters: Int) {
    [/] object {
        fun ofKilometres(km: Int) = [/](km * 1000)
    }
}

fun main() {
    println(Distance.ofKilometres(3).meters)
}
```

# --answers--

- companion
- Distance
- data
- inner
- Int
- meters

# --solutions--

```kotlin
class Distance private constructor(val meters: Int) {
    companion object {
        fun ofKilometres(km: Int) = Distance(km * 1000)
    }
}

fun main() {
    println(Distance.ofKilometres(3).meters)
}
```

# --output--

3000
