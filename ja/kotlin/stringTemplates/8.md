---
language: kotlin
exerciseType: 2
---

# --description--

`${}`の中には条件式も入れることができます。例えば：
```kotlin
println("${if (true) "Correct" else "Wrong"}")
// Correct を出力
```

# --instructions--

`ERROR`を出力してください

# --seed--

```kotlin
println("${if ([/]) "OK" else [/]}")
```

# --answers--

- false
- true
- "ERROR"
- ERROR

# --solutions--

```kotlin
println("${if (false) "OK" else "ERROR"}")
```

# --output--

ERROR
