---
language: kotlin
exerciseType: 2
---

# --description--

レシーバー型は**null許容**にもできます。`String?` の拡張は `null` を保持しているかもしれない変数の上でも呼び出せ、関数の中の `this` は `String?` になるので、`null` の場合は自分で処理します。ふつうは null 許容性のレッスンで見たエルビス演算子 `?:` を使います。
```kotlin
fun String?.orDash(): String {
    return this ?: "-"
}
val name: String? = null
println(name.orDash())  // -
println("Ada".orDash()) // Ada
```
`null` の値に対して `name.orDash()` を呼ぶのは安全です。関数自体が `null` のレシーバーを受け付けるので、`?.` は必要ありません。

# --instructions--

null許容の `String` に対して呼び出せて、レシーバーが `null` のときに `"-"` を返すように拡張を完成させ、プログラムが `-` と `Ada` を2行に出力するようにしてください

# --seed--

```kotlin
fun String[/].orDash(): String = [/] ?: "-"
val name: String? = null
println(name.orDash())
println("Ada".orDash())
```

# --answers--

- ?
- this
- !!
- it
- name
- ?.

# --solutions--

```kotlin
fun String?.orDash(): String = this ?: "-"
val name: String? = null
println(name.orDash())
println("Ada".orDash())
```

# --output--

-
Ada
